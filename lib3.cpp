#include "lib.h"
#include <iostream>
int func_12000(int& val) {
    func_in_12000(val);
    const int x = 12000;
    std::cout << 12000;
    for (int i = 0; i < 12000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12000(int& val) {
    const int x = 12000;
    std::cout << 12000;
    for (int i = 0; i < 12000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12001(int& val) {
    func_in_12001(val);
    const int x = 12001;
    std::cout << 12001;
    for (int i = 0; i < 12001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12001(int& val) {
    const int x = 12001;
    std::cout << 12001;
    for (int i = 0; i < 12001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12002(int& val) {
    func_in_12002(val);
    const int x = 12002;
    std::cout << 12002;
    for (int i = 0; i < 12002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12002(int& val) {
    const int x = 12002;
    std::cout << 12002;
    for (int i = 0; i < 12002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12003(int& val) {
    func_in_12003(val);
    const int x = 12003;
    std::cout << 12003;
    for (int i = 0; i < 12003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12003(int& val) {
    const int x = 12003;
    std::cout << 12003;
    for (int i = 0; i < 12003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12004(int& val) {
    func_in_12004(val);
    const int x = 12004;
    std::cout << 12004;
    for (int i = 0; i < 12004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12004(int& val) {
    const int x = 12004;
    std::cout << 12004;
    for (int i = 0; i < 12004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12005(int& val) {
    func_in_12005(val);
    const int x = 12005;
    std::cout << 12005;
    for (int i = 0; i < 12005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12005(int& val) {
    const int x = 12005;
    std::cout << 12005;
    for (int i = 0; i < 12005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12006(int& val) {
    func_in_12006(val);
    const int x = 12006;
    std::cout << 12006;
    for (int i = 0; i < 12006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12006(int& val) {
    const int x = 12006;
    std::cout << 12006;
    for (int i = 0; i < 12006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12007(int& val) {
    func_in_12007(val);
    const int x = 12007;
    std::cout << 12007;
    for (int i = 0; i < 12007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12007(int& val) {
    const int x = 12007;
    std::cout << 12007;
    for (int i = 0; i < 12007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12008(int& val) {
    func_in_12008(val);
    const int x = 12008;
    std::cout << 12008;
    for (int i = 0; i < 12008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12008(int& val) {
    const int x = 12008;
    std::cout << 12008;
    for (int i = 0; i < 12008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12009(int& val) {
    func_in_12009(val);
    const int x = 12009;
    std::cout << 12009;
    for (int i = 0; i < 12009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12009(int& val) {
    const int x = 12009;
    std::cout << 12009;
    for (int i = 0; i < 12009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12010(int& val) {
    func_in_12010(val);
    const int x = 12010;
    std::cout << 12010;
    for (int i = 0; i < 12010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12010(int& val) {
    const int x = 12010;
    std::cout << 12010;
    for (int i = 0; i < 12010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12011(int& val) {
    func_in_12011(val);
    const int x = 12011;
    std::cout << 12011;
    for (int i = 0; i < 12011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12011(int& val) {
    const int x = 12011;
    std::cout << 12011;
    for (int i = 0; i < 12011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12012(int& val) {
    func_in_12012(val);
    const int x = 12012;
    std::cout << 12012;
    for (int i = 0; i < 12012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12012(int& val) {
    const int x = 12012;
    std::cout << 12012;
    for (int i = 0; i < 12012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12013(int& val) {
    func_in_12013(val);
    const int x = 12013;
    std::cout << 12013;
    for (int i = 0; i < 12013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12013(int& val) {
    const int x = 12013;
    std::cout << 12013;
    for (int i = 0; i < 12013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12014(int& val) {
    func_in_12014(val);
    const int x = 12014;
    std::cout << 12014;
    for (int i = 0; i < 12014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12014(int& val) {
    const int x = 12014;
    std::cout << 12014;
    for (int i = 0; i < 12014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12015(int& val) {
    func_in_12015(val);
    const int x = 12015;
    std::cout << 12015;
    for (int i = 0; i < 12015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12015(int& val) {
    const int x = 12015;
    std::cout << 12015;
    for (int i = 0; i < 12015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12016(int& val) {
    func_in_12016(val);
    const int x = 12016;
    std::cout << 12016;
    for (int i = 0; i < 12016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12016(int& val) {
    const int x = 12016;
    std::cout << 12016;
    for (int i = 0; i < 12016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12017(int& val) {
    func_in_12017(val);
    const int x = 12017;
    std::cout << 12017;
    for (int i = 0; i < 12017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12017(int& val) {
    const int x = 12017;
    std::cout << 12017;
    for (int i = 0; i < 12017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12018(int& val) {
    func_in_12018(val);
    const int x = 12018;
    std::cout << 12018;
    for (int i = 0; i < 12018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12018(int& val) {
    const int x = 12018;
    std::cout << 12018;
    for (int i = 0; i < 12018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12019(int& val) {
    func_in_12019(val);
    const int x = 12019;
    std::cout << 12019;
    for (int i = 0; i < 12019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12019(int& val) {
    const int x = 12019;
    std::cout << 12019;
    for (int i = 0; i < 12019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12020(int& val) {
    func_in_12020(val);
    const int x = 12020;
    std::cout << 12020;
    for (int i = 0; i < 12020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12020(int& val) {
    const int x = 12020;
    std::cout << 12020;
    for (int i = 0; i < 12020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12021(int& val) {
    func_in_12021(val);
    const int x = 12021;
    std::cout << 12021;
    for (int i = 0; i < 12021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12021(int& val) {
    const int x = 12021;
    std::cout << 12021;
    for (int i = 0; i < 12021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12022(int& val) {
    func_in_12022(val);
    const int x = 12022;
    std::cout << 12022;
    for (int i = 0; i < 12022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12022(int& val) {
    const int x = 12022;
    std::cout << 12022;
    for (int i = 0; i < 12022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12023(int& val) {
    func_in_12023(val);
    const int x = 12023;
    std::cout << 12023;
    for (int i = 0; i < 12023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12023(int& val) {
    const int x = 12023;
    std::cout << 12023;
    for (int i = 0; i < 12023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12024(int& val) {
    func_in_12024(val);
    const int x = 12024;
    std::cout << 12024;
    for (int i = 0; i < 12024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12024(int& val) {
    const int x = 12024;
    std::cout << 12024;
    for (int i = 0; i < 12024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12025(int& val) {
    func_in_12025(val);
    const int x = 12025;
    std::cout << 12025;
    for (int i = 0; i < 12025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12025(int& val) {
    const int x = 12025;
    std::cout << 12025;
    for (int i = 0; i < 12025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12026(int& val) {
    func_in_12026(val);
    const int x = 12026;
    std::cout << 12026;
    for (int i = 0; i < 12026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12026(int& val) {
    const int x = 12026;
    std::cout << 12026;
    for (int i = 0; i < 12026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12027(int& val) {
    func_in_12027(val);
    const int x = 12027;
    std::cout << 12027;
    for (int i = 0; i < 12027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12027(int& val) {
    const int x = 12027;
    std::cout << 12027;
    for (int i = 0; i < 12027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12028(int& val) {
    func_in_12028(val);
    const int x = 12028;
    std::cout << 12028;
    for (int i = 0; i < 12028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12028(int& val) {
    const int x = 12028;
    std::cout << 12028;
    for (int i = 0; i < 12028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12029(int& val) {
    func_in_12029(val);
    const int x = 12029;
    std::cout << 12029;
    for (int i = 0; i < 12029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12029(int& val) {
    const int x = 12029;
    std::cout << 12029;
    for (int i = 0; i < 12029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12030(int& val) {
    func_in_12030(val);
    const int x = 12030;
    std::cout << 12030;
    for (int i = 0; i < 12030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12030(int& val) {
    const int x = 12030;
    std::cout << 12030;
    for (int i = 0; i < 12030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12031(int& val) {
    func_in_12031(val);
    const int x = 12031;
    std::cout << 12031;
    for (int i = 0; i < 12031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12031(int& val) {
    const int x = 12031;
    std::cout << 12031;
    for (int i = 0; i < 12031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12032(int& val) {
    func_in_12032(val);
    const int x = 12032;
    std::cout << 12032;
    for (int i = 0; i < 12032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12032(int& val) {
    const int x = 12032;
    std::cout << 12032;
    for (int i = 0; i < 12032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12033(int& val) {
    func_in_12033(val);
    const int x = 12033;
    std::cout << 12033;
    for (int i = 0; i < 12033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12033(int& val) {
    const int x = 12033;
    std::cout << 12033;
    for (int i = 0; i < 12033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12034(int& val) {
    func_in_12034(val);
    const int x = 12034;
    std::cout << 12034;
    for (int i = 0; i < 12034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12034(int& val) {
    const int x = 12034;
    std::cout << 12034;
    for (int i = 0; i < 12034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12035(int& val) {
    func_in_12035(val);
    const int x = 12035;
    std::cout << 12035;
    for (int i = 0; i < 12035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12035(int& val) {
    const int x = 12035;
    std::cout << 12035;
    for (int i = 0; i < 12035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12036(int& val) {
    func_in_12036(val);
    const int x = 12036;
    std::cout << 12036;
    for (int i = 0; i < 12036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12036(int& val) {
    const int x = 12036;
    std::cout << 12036;
    for (int i = 0; i < 12036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12037(int& val) {
    func_in_12037(val);
    const int x = 12037;
    std::cout << 12037;
    for (int i = 0; i < 12037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12037(int& val) {
    const int x = 12037;
    std::cout << 12037;
    for (int i = 0; i < 12037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12038(int& val) {
    func_in_12038(val);
    const int x = 12038;
    std::cout << 12038;
    for (int i = 0; i < 12038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12038(int& val) {
    const int x = 12038;
    std::cout << 12038;
    for (int i = 0; i < 12038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12039(int& val) {
    func_in_12039(val);
    const int x = 12039;
    std::cout << 12039;
    for (int i = 0; i < 12039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12039(int& val) {
    const int x = 12039;
    std::cout << 12039;
    for (int i = 0; i < 12039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12040(int& val) {
    func_in_12040(val);
    const int x = 12040;
    std::cout << 12040;
    for (int i = 0; i < 12040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12040(int& val) {
    const int x = 12040;
    std::cout << 12040;
    for (int i = 0; i < 12040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12041(int& val) {
    func_in_12041(val);
    const int x = 12041;
    std::cout << 12041;
    for (int i = 0; i < 12041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12041(int& val) {
    const int x = 12041;
    std::cout << 12041;
    for (int i = 0; i < 12041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12042(int& val) {
    func_in_12042(val);
    const int x = 12042;
    std::cout << 12042;
    for (int i = 0; i < 12042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12042(int& val) {
    const int x = 12042;
    std::cout << 12042;
    for (int i = 0; i < 12042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12043(int& val) {
    func_in_12043(val);
    const int x = 12043;
    std::cout << 12043;
    for (int i = 0; i < 12043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12043(int& val) {
    const int x = 12043;
    std::cout << 12043;
    for (int i = 0; i < 12043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12044(int& val) {
    func_in_12044(val);
    const int x = 12044;
    std::cout << 12044;
    for (int i = 0; i < 12044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12044(int& val) {
    const int x = 12044;
    std::cout << 12044;
    for (int i = 0; i < 12044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12045(int& val) {
    func_in_12045(val);
    const int x = 12045;
    std::cout << 12045;
    for (int i = 0; i < 12045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12045(int& val) {
    const int x = 12045;
    std::cout << 12045;
    for (int i = 0; i < 12045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12046(int& val) {
    func_in_12046(val);
    const int x = 12046;
    std::cout << 12046;
    for (int i = 0; i < 12046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12046(int& val) {
    const int x = 12046;
    std::cout << 12046;
    for (int i = 0; i < 12046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12047(int& val) {
    func_in_12047(val);
    const int x = 12047;
    std::cout << 12047;
    for (int i = 0; i < 12047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12047(int& val) {
    const int x = 12047;
    std::cout << 12047;
    for (int i = 0; i < 12047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12048(int& val) {
    func_in_12048(val);
    const int x = 12048;
    std::cout << 12048;
    for (int i = 0; i < 12048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12048(int& val) {
    const int x = 12048;
    std::cout << 12048;
    for (int i = 0; i < 12048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12049(int& val) {
    func_in_12049(val);
    const int x = 12049;
    std::cout << 12049;
    for (int i = 0; i < 12049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12049(int& val) {
    const int x = 12049;
    std::cout << 12049;
    for (int i = 0; i < 12049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12050(int& val) {
    func_in_12050(val);
    const int x = 12050;
    std::cout << 12050;
    for (int i = 0; i < 12050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12050(int& val) {
    const int x = 12050;
    std::cout << 12050;
    for (int i = 0; i < 12050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12051(int& val) {
    func_in_12051(val);
    const int x = 12051;
    std::cout << 12051;
    for (int i = 0; i < 12051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12051(int& val) {
    const int x = 12051;
    std::cout << 12051;
    for (int i = 0; i < 12051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12052(int& val) {
    func_in_12052(val);
    const int x = 12052;
    std::cout << 12052;
    for (int i = 0; i < 12052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12052(int& val) {
    const int x = 12052;
    std::cout << 12052;
    for (int i = 0; i < 12052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12053(int& val) {
    func_in_12053(val);
    const int x = 12053;
    std::cout << 12053;
    for (int i = 0; i < 12053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12053(int& val) {
    const int x = 12053;
    std::cout << 12053;
    for (int i = 0; i < 12053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12054(int& val) {
    func_in_12054(val);
    const int x = 12054;
    std::cout << 12054;
    for (int i = 0; i < 12054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12054(int& val) {
    const int x = 12054;
    std::cout << 12054;
    for (int i = 0; i < 12054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12055(int& val) {
    func_in_12055(val);
    const int x = 12055;
    std::cout << 12055;
    for (int i = 0; i < 12055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12055(int& val) {
    const int x = 12055;
    std::cout << 12055;
    for (int i = 0; i < 12055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12056(int& val) {
    func_in_12056(val);
    const int x = 12056;
    std::cout << 12056;
    for (int i = 0; i < 12056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12056(int& val) {
    const int x = 12056;
    std::cout << 12056;
    for (int i = 0; i < 12056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12057(int& val) {
    func_in_12057(val);
    const int x = 12057;
    std::cout << 12057;
    for (int i = 0; i < 12057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12057(int& val) {
    const int x = 12057;
    std::cout << 12057;
    for (int i = 0; i < 12057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12058(int& val) {
    func_in_12058(val);
    const int x = 12058;
    std::cout << 12058;
    for (int i = 0; i < 12058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12058(int& val) {
    const int x = 12058;
    std::cout << 12058;
    for (int i = 0; i < 12058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12059(int& val) {
    func_in_12059(val);
    const int x = 12059;
    std::cout << 12059;
    for (int i = 0; i < 12059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12059(int& val) {
    const int x = 12059;
    std::cout << 12059;
    for (int i = 0; i < 12059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12060(int& val) {
    func_in_12060(val);
    const int x = 12060;
    std::cout << 12060;
    for (int i = 0; i < 12060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12060(int& val) {
    const int x = 12060;
    std::cout << 12060;
    for (int i = 0; i < 12060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12061(int& val) {
    func_in_12061(val);
    const int x = 12061;
    std::cout << 12061;
    for (int i = 0; i < 12061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12061(int& val) {
    const int x = 12061;
    std::cout << 12061;
    for (int i = 0; i < 12061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12062(int& val) {
    func_in_12062(val);
    const int x = 12062;
    std::cout << 12062;
    for (int i = 0; i < 12062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12062(int& val) {
    const int x = 12062;
    std::cout << 12062;
    for (int i = 0; i < 12062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12063(int& val) {
    func_in_12063(val);
    const int x = 12063;
    std::cout << 12063;
    for (int i = 0; i < 12063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12063(int& val) {
    const int x = 12063;
    std::cout << 12063;
    for (int i = 0; i < 12063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12064(int& val) {
    func_in_12064(val);
    const int x = 12064;
    std::cout << 12064;
    for (int i = 0; i < 12064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12064(int& val) {
    const int x = 12064;
    std::cout << 12064;
    for (int i = 0; i < 12064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12065(int& val) {
    func_in_12065(val);
    const int x = 12065;
    std::cout << 12065;
    for (int i = 0; i < 12065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12065(int& val) {
    const int x = 12065;
    std::cout << 12065;
    for (int i = 0; i < 12065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12066(int& val) {
    func_in_12066(val);
    const int x = 12066;
    std::cout << 12066;
    for (int i = 0; i < 12066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12066(int& val) {
    const int x = 12066;
    std::cout << 12066;
    for (int i = 0; i < 12066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12067(int& val) {
    func_in_12067(val);
    const int x = 12067;
    std::cout << 12067;
    for (int i = 0; i < 12067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12067(int& val) {
    const int x = 12067;
    std::cout << 12067;
    for (int i = 0; i < 12067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12068(int& val) {
    func_in_12068(val);
    const int x = 12068;
    std::cout << 12068;
    for (int i = 0; i < 12068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12068(int& val) {
    const int x = 12068;
    std::cout << 12068;
    for (int i = 0; i < 12068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12069(int& val) {
    func_in_12069(val);
    const int x = 12069;
    std::cout << 12069;
    for (int i = 0; i < 12069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12069(int& val) {
    const int x = 12069;
    std::cout << 12069;
    for (int i = 0; i < 12069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12070(int& val) {
    func_in_12070(val);
    const int x = 12070;
    std::cout << 12070;
    for (int i = 0; i < 12070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12070(int& val) {
    const int x = 12070;
    std::cout << 12070;
    for (int i = 0; i < 12070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12071(int& val) {
    func_in_12071(val);
    const int x = 12071;
    std::cout << 12071;
    for (int i = 0; i < 12071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12071(int& val) {
    const int x = 12071;
    std::cout << 12071;
    for (int i = 0; i < 12071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12072(int& val) {
    func_in_12072(val);
    const int x = 12072;
    std::cout << 12072;
    for (int i = 0; i < 12072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12072(int& val) {
    const int x = 12072;
    std::cout << 12072;
    for (int i = 0; i < 12072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12073(int& val) {
    func_in_12073(val);
    const int x = 12073;
    std::cout << 12073;
    for (int i = 0; i < 12073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12073(int& val) {
    const int x = 12073;
    std::cout << 12073;
    for (int i = 0; i < 12073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12074(int& val) {
    func_in_12074(val);
    const int x = 12074;
    std::cout << 12074;
    for (int i = 0; i < 12074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12074(int& val) {
    const int x = 12074;
    std::cout << 12074;
    for (int i = 0; i < 12074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12075(int& val) {
    func_in_12075(val);
    const int x = 12075;
    std::cout << 12075;
    for (int i = 0; i < 12075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12075(int& val) {
    const int x = 12075;
    std::cout << 12075;
    for (int i = 0; i < 12075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12076(int& val) {
    func_in_12076(val);
    const int x = 12076;
    std::cout << 12076;
    for (int i = 0; i < 12076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12076(int& val) {
    const int x = 12076;
    std::cout << 12076;
    for (int i = 0; i < 12076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12077(int& val) {
    func_in_12077(val);
    const int x = 12077;
    std::cout << 12077;
    for (int i = 0; i < 12077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12077(int& val) {
    const int x = 12077;
    std::cout << 12077;
    for (int i = 0; i < 12077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12078(int& val) {
    func_in_12078(val);
    const int x = 12078;
    std::cout << 12078;
    for (int i = 0; i < 12078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12078(int& val) {
    const int x = 12078;
    std::cout << 12078;
    for (int i = 0; i < 12078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12079(int& val) {
    func_in_12079(val);
    const int x = 12079;
    std::cout << 12079;
    for (int i = 0; i < 12079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12079(int& val) {
    const int x = 12079;
    std::cout << 12079;
    for (int i = 0; i < 12079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12080(int& val) {
    func_in_12080(val);
    const int x = 12080;
    std::cout << 12080;
    for (int i = 0; i < 12080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12080(int& val) {
    const int x = 12080;
    std::cout << 12080;
    for (int i = 0; i < 12080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12081(int& val) {
    func_in_12081(val);
    const int x = 12081;
    std::cout << 12081;
    for (int i = 0; i < 12081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12081(int& val) {
    const int x = 12081;
    std::cout << 12081;
    for (int i = 0; i < 12081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12082(int& val) {
    func_in_12082(val);
    const int x = 12082;
    std::cout << 12082;
    for (int i = 0; i < 12082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12082(int& val) {
    const int x = 12082;
    std::cout << 12082;
    for (int i = 0; i < 12082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12083(int& val) {
    func_in_12083(val);
    const int x = 12083;
    std::cout << 12083;
    for (int i = 0; i < 12083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12083(int& val) {
    const int x = 12083;
    std::cout << 12083;
    for (int i = 0; i < 12083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12084(int& val) {
    func_in_12084(val);
    const int x = 12084;
    std::cout << 12084;
    for (int i = 0; i < 12084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12084(int& val) {
    const int x = 12084;
    std::cout << 12084;
    for (int i = 0; i < 12084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12085(int& val) {
    func_in_12085(val);
    const int x = 12085;
    std::cout << 12085;
    for (int i = 0; i < 12085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12085(int& val) {
    const int x = 12085;
    std::cout << 12085;
    for (int i = 0; i < 12085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12086(int& val) {
    func_in_12086(val);
    const int x = 12086;
    std::cout << 12086;
    for (int i = 0; i < 12086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12086(int& val) {
    const int x = 12086;
    std::cout << 12086;
    for (int i = 0; i < 12086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12087(int& val) {
    func_in_12087(val);
    const int x = 12087;
    std::cout << 12087;
    for (int i = 0; i < 12087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12087(int& val) {
    const int x = 12087;
    std::cout << 12087;
    for (int i = 0; i < 12087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12088(int& val) {
    func_in_12088(val);
    const int x = 12088;
    std::cout << 12088;
    for (int i = 0; i < 12088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12088(int& val) {
    const int x = 12088;
    std::cout << 12088;
    for (int i = 0; i < 12088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12089(int& val) {
    func_in_12089(val);
    const int x = 12089;
    std::cout << 12089;
    for (int i = 0; i < 12089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12089(int& val) {
    const int x = 12089;
    std::cout << 12089;
    for (int i = 0; i < 12089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12090(int& val) {
    func_in_12090(val);
    const int x = 12090;
    std::cout << 12090;
    for (int i = 0; i < 12090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12090(int& val) {
    const int x = 12090;
    std::cout << 12090;
    for (int i = 0; i < 12090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12091(int& val) {
    func_in_12091(val);
    const int x = 12091;
    std::cout << 12091;
    for (int i = 0; i < 12091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12091(int& val) {
    const int x = 12091;
    std::cout << 12091;
    for (int i = 0; i < 12091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12092(int& val) {
    func_in_12092(val);
    const int x = 12092;
    std::cout << 12092;
    for (int i = 0; i < 12092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12092(int& val) {
    const int x = 12092;
    std::cout << 12092;
    for (int i = 0; i < 12092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12093(int& val) {
    func_in_12093(val);
    const int x = 12093;
    std::cout << 12093;
    for (int i = 0; i < 12093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12093(int& val) {
    const int x = 12093;
    std::cout << 12093;
    for (int i = 0; i < 12093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12094(int& val) {
    func_in_12094(val);
    const int x = 12094;
    std::cout << 12094;
    for (int i = 0; i < 12094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12094(int& val) {
    const int x = 12094;
    std::cout << 12094;
    for (int i = 0; i < 12094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12095(int& val) {
    func_in_12095(val);
    const int x = 12095;
    std::cout << 12095;
    for (int i = 0; i < 12095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12095(int& val) {
    const int x = 12095;
    std::cout << 12095;
    for (int i = 0; i < 12095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12096(int& val) {
    func_in_12096(val);
    const int x = 12096;
    std::cout << 12096;
    for (int i = 0; i < 12096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12096(int& val) {
    const int x = 12096;
    std::cout << 12096;
    for (int i = 0; i < 12096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12097(int& val) {
    func_in_12097(val);
    const int x = 12097;
    std::cout << 12097;
    for (int i = 0; i < 12097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12097(int& val) {
    const int x = 12097;
    std::cout << 12097;
    for (int i = 0; i < 12097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12098(int& val) {
    func_in_12098(val);
    const int x = 12098;
    std::cout << 12098;
    for (int i = 0; i < 12098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12098(int& val) {
    const int x = 12098;
    std::cout << 12098;
    for (int i = 0; i < 12098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12099(int& val) {
    func_in_12099(val);
    const int x = 12099;
    std::cout << 12099;
    for (int i = 0; i < 12099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12099(int& val) {
    const int x = 12099;
    std::cout << 12099;
    for (int i = 0; i < 12099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12100(int& val) {
    func_in_12100(val);
    const int x = 12100;
    std::cout << 12100;
    for (int i = 0; i < 12100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12100(int& val) {
    const int x = 12100;
    std::cout << 12100;
    for (int i = 0; i < 12100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12101(int& val) {
    func_in_12101(val);
    const int x = 12101;
    std::cout << 12101;
    for (int i = 0; i < 12101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12101(int& val) {
    const int x = 12101;
    std::cout << 12101;
    for (int i = 0; i < 12101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12102(int& val) {
    func_in_12102(val);
    const int x = 12102;
    std::cout << 12102;
    for (int i = 0; i < 12102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12102(int& val) {
    const int x = 12102;
    std::cout << 12102;
    for (int i = 0; i < 12102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12103(int& val) {
    func_in_12103(val);
    const int x = 12103;
    std::cout << 12103;
    for (int i = 0; i < 12103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12103(int& val) {
    const int x = 12103;
    std::cout << 12103;
    for (int i = 0; i < 12103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12104(int& val) {
    func_in_12104(val);
    const int x = 12104;
    std::cout << 12104;
    for (int i = 0; i < 12104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12104(int& val) {
    const int x = 12104;
    std::cout << 12104;
    for (int i = 0; i < 12104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12105(int& val) {
    func_in_12105(val);
    const int x = 12105;
    std::cout << 12105;
    for (int i = 0; i < 12105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12105(int& val) {
    const int x = 12105;
    std::cout << 12105;
    for (int i = 0; i < 12105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12106(int& val) {
    func_in_12106(val);
    const int x = 12106;
    std::cout << 12106;
    for (int i = 0; i < 12106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12106(int& val) {
    const int x = 12106;
    std::cout << 12106;
    for (int i = 0; i < 12106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12107(int& val) {
    func_in_12107(val);
    const int x = 12107;
    std::cout << 12107;
    for (int i = 0; i < 12107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12107(int& val) {
    const int x = 12107;
    std::cout << 12107;
    for (int i = 0; i < 12107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12108(int& val) {
    func_in_12108(val);
    const int x = 12108;
    std::cout << 12108;
    for (int i = 0; i < 12108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12108(int& val) {
    const int x = 12108;
    std::cout << 12108;
    for (int i = 0; i < 12108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12109(int& val) {
    func_in_12109(val);
    const int x = 12109;
    std::cout << 12109;
    for (int i = 0; i < 12109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12109(int& val) {
    const int x = 12109;
    std::cout << 12109;
    for (int i = 0; i < 12109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12110(int& val) {
    func_in_12110(val);
    const int x = 12110;
    std::cout << 12110;
    for (int i = 0; i < 12110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12110(int& val) {
    const int x = 12110;
    std::cout << 12110;
    for (int i = 0; i < 12110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12111(int& val) {
    func_in_12111(val);
    const int x = 12111;
    std::cout << 12111;
    for (int i = 0; i < 12111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12111(int& val) {
    const int x = 12111;
    std::cout << 12111;
    for (int i = 0; i < 12111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12112(int& val) {
    func_in_12112(val);
    const int x = 12112;
    std::cout << 12112;
    for (int i = 0; i < 12112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12112(int& val) {
    const int x = 12112;
    std::cout << 12112;
    for (int i = 0; i < 12112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12113(int& val) {
    func_in_12113(val);
    const int x = 12113;
    std::cout << 12113;
    for (int i = 0; i < 12113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12113(int& val) {
    const int x = 12113;
    std::cout << 12113;
    for (int i = 0; i < 12113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12114(int& val) {
    func_in_12114(val);
    const int x = 12114;
    std::cout << 12114;
    for (int i = 0; i < 12114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12114(int& val) {
    const int x = 12114;
    std::cout << 12114;
    for (int i = 0; i < 12114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12115(int& val) {
    func_in_12115(val);
    const int x = 12115;
    std::cout << 12115;
    for (int i = 0; i < 12115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12115(int& val) {
    const int x = 12115;
    std::cout << 12115;
    for (int i = 0; i < 12115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12116(int& val) {
    func_in_12116(val);
    const int x = 12116;
    std::cout << 12116;
    for (int i = 0; i < 12116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12116(int& val) {
    const int x = 12116;
    std::cout << 12116;
    for (int i = 0; i < 12116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12117(int& val) {
    func_in_12117(val);
    const int x = 12117;
    std::cout << 12117;
    for (int i = 0; i < 12117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12117(int& val) {
    const int x = 12117;
    std::cout << 12117;
    for (int i = 0; i < 12117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12118(int& val) {
    func_in_12118(val);
    const int x = 12118;
    std::cout << 12118;
    for (int i = 0; i < 12118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12118(int& val) {
    const int x = 12118;
    std::cout << 12118;
    for (int i = 0; i < 12118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12119(int& val) {
    func_in_12119(val);
    const int x = 12119;
    std::cout << 12119;
    for (int i = 0; i < 12119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12119(int& val) {
    const int x = 12119;
    std::cout << 12119;
    for (int i = 0; i < 12119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12120(int& val) {
    func_in_12120(val);
    const int x = 12120;
    std::cout << 12120;
    for (int i = 0; i < 12120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12120(int& val) {
    const int x = 12120;
    std::cout << 12120;
    for (int i = 0; i < 12120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12121(int& val) {
    func_in_12121(val);
    const int x = 12121;
    std::cout << 12121;
    for (int i = 0; i < 12121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12121(int& val) {
    const int x = 12121;
    std::cout << 12121;
    for (int i = 0; i < 12121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12122(int& val) {
    func_in_12122(val);
    const int x = 12122;
    std::cout << 12122;
    for (int i = 0; i < 12122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12122(int& val) {
    const int x = 12122;
    std::cout << 12122;
    for (int i = 0; i < 12122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12123(int& val) {
    func_in_12123(val);
    const int x = 12123;
    std::cout << 12123;
    for (int i = 0; i < 12123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12123(int& val) {
    const int x = 12123;
    std::cout << 12123;
    for (int i = 0; i < 12123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12124(int& val) {
    func_in_12124(val);
    const int x = 12124;
    std::cout << 12124;
    for (int i = 0; i < 12124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12124(int& val) {
    const int x = 12124;
    std::cout << 12124;
    for (int i = 0; i < 12124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12125(int& val) {
    func_in_12125(val);
    const int x = 12125;
    std::cout << 12125;
    for (int i = 0; i < 12125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12125(int& val) {
    const int x = 12125;
    std::cout << 12125;
    for (int i = 0; i < 12125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12126(int& val) {
    func_in_12126(val);
    const int x = 12126;
    std::cout << 12126;
    for (int i = 0; i < 12126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12126(int& val) {
    const int x = 12126;
    std::cout << 12126;
    for (int i = 0; i < 12126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12127(int& val) {
    func_in_12127(val);
    const int x = 12127;
    std::cout << 12127;
    for (int i = 0; i < 12127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12127(int& val) {
    const int x = 12127;
    std::cout << 12127;
    for (int i = 0; i < 12127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12128(int& val) {
    func_in_12128(val);
    const int x = 12128;
    std::cout << 12128;
    for (int i = 0; i < 12128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12128(int& val) {
    const int x = 12128;
    std::cout << 12128;
    for (int i = 0; i < 12128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12129(int& val) {
    func_in_12129(val);
    const int x = 12129;
    std::cout << 12129;
    for (int i = 0; i < 12129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12129(int& val) {
    const int x = 12129;
    std::cout << 12129;
    for (int i = 0; i < 12129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12130(int& val) {
    func_in_12130(val);
    const int x = 12130;
    std::cout << 12130;
    for (int i = 0; i < 12130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12130(int& val) {
    const int x = 12130;
    std::cout << 12130;
    for (int i = 0; i < 12130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12131(int& val) {
    func_in_12131(val);
    const int x = 12131;
    std::cout << 12131;
    for (int i = 0; i < 12131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12131(int& val) {
    const int x = 12131;
    std::cout << 12131;
    for (int i = 0; i < 12131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12132(int& val) {
    func_in_12132(val);
    const int x = 12132;
    std::cout << 12132;
    for (int i = 0; i < 12132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12132(int& val) {
    const int x = 12132;
    std::cout << 12132;
    for (int i = 0; i < 12132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12133(int& val) {
    func_in_12133(val);
    const int x = 12133;
    std::cout << 12133;
    for (int i = 0; i < 12133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12133(int& val) {
    const int x = 12133;
    std::cout << 12133;
    for (int i = 0; i < 12133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12134(int& val) {
    func_in_12134(val);
    const int x = 12134;
    std::cout << 12134;
    for (int i = 0; i < 12134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12134(int& val) {
    const int x = 12134;
    std::cout << 12134;
    for (int i = 0; i < 12134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12135(int& val) {
    func_in_12135(val);
    const int x = 12135;
    std::cout << 12135;
    for (int i = 0; i < 12135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12135(int& val) {
    const int x = 12135;
    std::cout << 12135;
    for (int i = 0; i < 12135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12136(int& val) {
    func_in_12136(val);
    const int x = 12136;
    std::cout << 12136;
    for (int i = 0; i < 12136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12136(int& val) {
    const int x = 12136;
    std::cout << 12136;
    for (int i = 0; i < 12136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12137(int& val) {
    func_in_12137(val);
    const int x = 12137;
    std::cout << 12137;
    for (int i = 0; i < 12137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12137(int& val) {
    const int x = 12137;
    std::cout << 12137;
    for (int i = 0; i < 12137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12138(int& val) {
    func_in_12138(val);
    const int x = 12138;
    std::cout << 12138;
    for (int i = 0; i < 12138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12138(int& val) {
    const int x = 12138;
    std::cout << 12138;
    for (int i = 0; i < 12138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12139(int& val) {
    func_in_12139(val);
    const int x = 12139;
    std::cout << 12139;
    for (int i = 0; i < 12139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12139(int& val) {
    const int x = 12139;
    std::cout << 12139;
    for (int i = 0; i < 12139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12140(int& val) {
    func_in_12140(val);
    const int x = 12140;
    std::cout << 12140;
    for (int i = 0; i < 12140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12140(int& val) {
    const int x = 12140;
    std::cout << 12140;
    for (int i = 0; i < 12140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12141(int& val) {
    func_in_12141(val);
    const int x = 12141;
    std::cout << 12141;
    for (int i = 0; i < 12141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12141(int& val) {
    const int x = 12141;
    std::cout << 12141;
    for (int i = 0; i < 12141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12142(int& val) {
    func_in_12142(val);
    const int x = 12142;
    std::cout << 12142;
    for (int i = 0; i < 12142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12142(int& val) {
    const int x = 12142;
    std::cout << 12142;
    for (int i = 0; i < 12142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12143(int& val) {
    func_in_12143(val);
    const int x = 12143;
    std::cout << 12143;
    for (int i = 0; i < 12143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12143(int& val) {
    const int x = 12143;
    std::cout << 12143;
    for (int i = 0; i < 12143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12144(int& val) {
    func_in_12144(val);
    const int x = 12144;
    std::cout << 12144;
    for (int i = 0; i < 12144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12144(int& val) {
    const int x = 12144;
    std::cout << 12144;
    for (int i = 0; i < 12144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12145(int& val) {
    func_in_12145(val);
    const int x = 12145;
    std::cout << 12145;
    for (int i = 0; i < 12145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12145(int& val) {
    const int x = 12145;
    std::cout << 12145;
    for (int i = 0; i < 12145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12146(int& val) {
    func_in_12146(val);
    const int x = 12146;
    std::cout << 12146;
    for (int i = 0; i < 12146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12146(int& val) {
    const int x = 12146;
    std::cout << 12146;
    for (int i = 0; i < 12146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12147(int& val) {
    func_in_12147(val);
    const int x = 12147;
    std::cout << 12147;
    for (int i = 0; i < 12147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12147(int& val) {
    const int x = 12147;
    std::cout << 12147;
    for (int i = 0; i < 12147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12148(int& val) {
    func_in_12148(val);
    const int x = 12148;
    std::cout << 12148;
    for (int i = 0; i < 12148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12148(int& val) {
    const int x = 12148;
    std::cout << 12148;
    for (int i = 0; i < 12148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12149(int& val) {
    func_in_12149(val);
    const int x = 12149;
    std::cout << 12149;
    for (int i = 0; i < 12149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12149(int& val) {
    const int x = 12149;
    std::cout << 12149;
    for (int i = 0; i < 12149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12150(int& val) {
    func_in_12150(val);
    const int x = 12150;
    std::cout << 12150;
    for (int i = 0; i < 12150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12150(int& val) {
    const int x = 12150;
    std::cout << 12150;
    for (int i = 0; i < 12150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12151(int& val) {
    func_in_12151(val);
    const int x = 12151;
    std::cout << 12151;
    for (int i = 0; i < 12151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12151(int& val) {
    const int x = 12151;
    std::cout << 12151;
    for (int i = 0; i < 12151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12152(int& val) {
    func_in_12152(val);
    const int x = 12152;
    std::cout << 12152;
    for (int i = 0; i < 12152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12152(int& val) {
    const int x = 12152;
    std::cout << 12152;
    for (int i = 0; i < 12152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12153(int& val) {
    func_in_12153(val);
    const int x = 12153;
    std::cout << 12153;
    for (int i = 0; i < 12153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12153(int& val) {
    const int x = 12153;
    std::cout << 12153;
    for (int i = 0; i < 12153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12154(int& val) {
    func_in_12154(val);
    const int x = 12154;
    std::cout << 12154;
    for (int i = 0; i < 12154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12154(int& val) {
    const int x = 12154;
    std::cout << 12154;
    for (int i = 0; i < 12154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12155(int& val) {
    func_in_12155(val);
    const int x = 12155;
    std::cout << 12155;
    for (int i = 0; i < 12155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12155(int& val) {
    const int x = 12155;
    std::cout << 12155;
    for (int i = 0; i < 12155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12156(int& val) {
    func_in_12156(val);
    const int x = 12156;
    std::cout << 12156;
    for (int i = 0; i < 12156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12156(int& val) {
    const int x = 12156;
    std::cout << 12156;
    for (int i = 0; i < 12156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12157(int& val) {
    func_in_12157(val);
    const int x = 12157;
    std::cout << 12157;
    for (int i = 0; i < 12157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12157(int& val) {
    const int x = 12157;
    std::cout << 12157;
    for (int i = 0; i < 12157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12158(int& val) {
    func_in_12158(val);
    const int x = 12158;
    std::cout << 12158;
    for (int i = 0; i < 12158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12158(int& val) {
    const int x = 12158;
    std::cout << 12158;
    for (int i = 0; i < 12158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12159(int& val) {
    func_in_12159(val);
    const int x = 12159;
    std::cout << 12159;
    for (int i = 0; i < 12159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12159(int& val) {
    const int x = 12159;
    std::cout << 12159;
    for (int i = 0; i < 12159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12160(int& val) {
    func_in_12160(val);
    const int x = 12160;
    std::cout << 12160;
    for (int i = 0; i < 12160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12160(int& val) {
    const int x = 12160;
    std::cout << 12160;
    for (int i = 0; i < 12160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12161(int& val) {
    func_in_12161(val);
    const int x = 12161;
    std::cout << 12161;
    for (int i = 0; i < 12161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12161(int& val) {
    const int x = 12161;
    std::cout << 12161;
    for (int i = 0; i < 12161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12162(int& val) {
    func_in_12162(val);
    const int x = 12162;
    std::cout << 12162;
    for (int i = 0; i < 12162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12162(int& val) {
    const int x = 12162;
    std::cout << 12162;
    for (int i = 0; i < 12162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12163(int& val) {
    func_in_12163(val);
    const int x = 12163;
    std::cout << 12163;
    for (int i = 0; i < 12163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12163(int& val) {
    const int x = 12163;
    std::cout << 12163;
    for (int i = 0; i < 12163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12164(int& val) {
    func_in_12164(val);
    const int x = 12164;
    std::cout << 12164;
    for (int i = 0; i < 12164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12164(int& val) {
    const int x = 12164;
    std::cout << 12164;
    for (int i = 0; i < 12164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12165(int& val) {
    func_in_12165(val);
    const int x = 12165;
    std::cout << 12165;
    for (int i = 0; i < 12165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12165(int& val) {
    const int x = 12165;
    std::cout << 12165;
    for (int i = 0; i < 12165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12166(int& val) {
    func_in_12166(val);
    const int x = 12166;
    std::cout << 12166;
    for (int i = 0; i < 12166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12166(int& val) {
    const int x = 12166;
    std::cout << 12166;
    for (int i = 0; i < 12166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12167(int& val) {
    func_in_12167(val);
    const int x = 12167;
    std::cout << 12167;
    for (int i = 0; i < 12167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12167(int& val) {
    const int x = 12167;
    std::cout << 12167;
    for (int i = 0; i < 12167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12168(int& val) {
    func_in_12168(val);
    const int x = 12168;
    std::cout << 12168;
    for (int i = 0; i < 12168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12168(int& val) {
    const int x = 12168;
    std::cout << 12168;
    for (int i = 0; i < 12168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12169(int& val) {
    func_in_12169(val);
    const int x = 12169;
    std::cout << 12169;
    for (int i = 0; i < 12169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12169(int& val) {
    const int x = 12169;
    std::cout << 12169;
    for (int i = 0; i < 12169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12170(int& val) {
    func_in_12170(val);
    const int x = 12170;
    std::cout << 12170;
    for (int i = 0; i < 12170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12170(int& val) {
    const int x = 12170;
    std::cout << 12170;
    for (int i = 0; i < 12170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12171(int& val) {
    func_in_12171(val);
    const int x = 12171;
    std::cout << 12171;
    for (int i = 0; i < 12171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12171(int& val) {
    const int x = 12171;
    std::cout << 12171;
    for (int i = 0; i < 12171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12172(int& val) {
    func_in_12172(val);
    const int x = 12172;
    std::cout << 12172;
    for (int i = 0; i < 12172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12172(int& val) {
    const int x = 12172;
    std::cout << 12172;
    for (int i = 0; i < 12172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12173(int& val) {
    func_in_12173(val);
    const int x = 12173;
    std::cout << 12173;
    for (int i = 0; i < 12173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12173(int& val) {
    const int x = 12173;
    std::cout << 12173;
    for (int i = 0; i < 12173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12174(int& val) {
    func_in_12174(val);
    const int x = 12174;
    std::cout << 12174;
    for (int i = 0; i < 12174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12174(int& val) {
    const int x = 12174;
    std::cout << 12174;
    for (int i = 0; i < 12174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12175(int& val) {
    func_in_12175(val);
    const int x = 12175;
    std::cout << 12175;
    for (int i = 0; i < 12175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12175(int& val) {
    const int x = 12175;
    std::cout << 12175;
    for (int i = 0; i < 12175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12176(int& val) {
    func_in_12176(val);
    const int x = 12176;
    std::cout << 12176;
    for (int i = 0; i < 12176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12176(int& val) {
    const int x = 12176;
    std::cout << 12176;
    for (int i = 0; i < 12176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12177(int& val) {
    func_in_12177(val);
    const int x = 12177;
    std::cout << 12177;
    for (int i = 0; i < 12177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12177(int& val) {
    const int x = 12177;
    std::cout << 12177;
    for (int i = 0; i < 12177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12178(int& val) {
    func_in_12178(val);
    const int x = 12178;
    std::cout << 12178;
    for (int i = 0; i < 12178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12178(int& val) {
    const int x = 12178;
    std::cout << 12178;
    for (int i = 0; i < 12178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12179(int& val) {
    func_in_12179(val);
    const int x = 12179;
    std::cout << 12179;
    for (int i = 0; i < 12179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12179(int& val) {
    const int x = 12179;
    std::cout << 12179;
    for (int i = 0; i < 12179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12180(int& val) {
    func_in_12180(val);
    const int x = 12180;
    std::cout << 12180;
    for (int i = 0; i < 12180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12180(int& val) {
    const int x = 12180;
    std::cout << 12180;
    for (int i = 0; i < 12180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12181(int& val) {
    func_in_12181(val);
    const int x = 12181;
    std::cout << 12181;
    for (int i = 0; i < 12181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12181(int& val) {
    const int x = 12181;
    std::cout << 12181;
    for (int i = 0; i < 12181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12182(int& val) {
    func_in_12182(val);
    const int x = 12182;
    std::cout << 12182;
    for (int i = 0; i < 12182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12182(int& val) {
    const int x = 12182;
    std::cout << 12182;
    for (int i = 0; i < 12182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12183(int& val) {
    func_in_12183(val);
    const int x = 12183;
    std::cout << 12183;
    for (int i = 0; i < 12183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12183(int& val) {
    const int x = 12183;
    std::cout << 12183;
    for (int i = 0; i < 12183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12184(int& val) {
    func_in_12184(val);
    const int x = 12184;
    std::cout << 12184;
    for (int i = 0; i < 12184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12184(int& val) {
    const int x = 12184;
    std::cout << 12184;
    for (int i = 0; i < 12184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12185(int& val) {
    func_in_12185(val);
    const int x = 12185;
    std::cout << 12185;
    for (int i = 0; i < 12185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12185(int& val) {
    const int x = 12185;
    std::cout << 12185;
    for (int i = 0; i < 12185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12186(int& val) {
    func_in_12186(val);
    const int x = 12186;
    std::cout << 12186;
    for (int i = 0; i < 12186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12186(int& val) {
    const int x = 12186;
    std::cout << 12186;
    for (int i = 0; i < 12186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12187(int& val) {
    func_in_12187(val);
    const int x = 12187;
    std::cout << 12187;
    for (int i = 0; i < 12187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12187(int& val) {
    const int x = 12187;
    std::cout << 12187;
    for (int i = 0; i < 12187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12188(int& val) {
    func_in_12188(val);
    const int x = 12188;
    std::cout << 12188;
    for (int i = 0; i < 12188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12188(int& val) {
    const int x = 12188;
    std::cout << 12188;
    for (int i = 0; i < 12188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12189(int& val) {
    func_in_12189(val);
    const int x = 12189;
    std::cout << 12189;
    for (int i = 0; i < 12189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12189(int& val) {
    const int x = 12189;
    std::cout << 12189;
    for (int i = 0; i < 12189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12190(int& val) {
    func_in_12190(val);
    const int x = 12190;
    std::cout << 12190;
    for (int i = 0; i < 12190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12190(int& val) {
    const int x = 12190;
    std::cout << 12190;
    for (int i = 0; i < 12190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12191(int& val) {
    func_in_12191(val);
    const int x = 12191;
    std::cout << 12191;
    for (int i = 0; i < 12191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12191(int& val) {
    const int x = 12191;
    std::cout << 12191;
    for (int i = 0; i < 12191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12192(int& val) {
    func_in_12192(val);
    const int x = 12192;
    std::cout << 12192;
    for (int i = 0; i < 12192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12192(int& val) {
    const int x = 12192;
    std::cout << 12192;
    for (int i = 0; i < 12192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12193(int& val) {
    func_in_12193(val);
    const int x = 12193;
    std::cout << 12193;
    for (int i = 0; i < 12193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12193(int& val) {
    const int x = 12193;
    std::cout << 12193;
    for (int i = 0; i < 12193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12194(int& val) {
    func_in_12194(val);
    const int x = 12194;
    std::cout << 12194;
    for (int i = 0; i < 12194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12194(int& val) {
    const int x = 12194;
    std::cout << 12194;
    for (int i = 0; i < 12194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12195(int& val) {
    func_in_12195(val);
    const int x = 12195;
    std::cout << 12195;
    for (int i = 0; i < 12195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12195(int& val) {
    const int x = 12195;
    std::cout << 12195;
    for (int i = 0; i < 12195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12196(int& val) {
    func_in_12196(val);
    const int x = 12196;
    std::cout << 12196;
    for (int i = 0; i < 12196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12196(int& val) {
    const int x = 12196;
    std::cout << 12196;
    for (int i = 0; i < 12196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12197(int& val) {
    func_in_12197(val);
    const int x = 12197;
    std::cout << 12197;
    for (int i = 0; i < 12197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12197(int& val) {
    const int x = 12197;
    std::cout << 12197;
    for (int i = 0; i < 12197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12198(int& val) {
    func_in_12198(val);
    const int x = 12198;
    std::cout << 12198;
    for (int i = 0; i < 12198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12198(int& val) {
    const int x = 12198;
    std::cout << 12198;
    for (int i = 0; i < 12198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12199(int& val) {
    func_in_12199(val);
    const int x = 12199;
    std::cout << 12199;
    for (int i = 0; i < 12199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12199(int& val) {
    const int x = 12199;
    std::cout << 12199;
    for (int i = 0; i < 12199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12200(int& val) {
    func_in_12200(val);
    const int x = 12200;
    std::cout << 12200;
    for (int i = 0; i < 12200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12200(int& val) {
    const int x = 12200;
    std::cout << 12200;
    for (int i = 0; i < 12200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12201(int& val) {
    func_in_12201(val);
    const int x = 12201;
    std::cout << 12201;
    for (int i = 0; i < 12201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12201(int& val) {
    const int x = 12201;
    std::cout << 12201;
    for (int i = 0; i < 12201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12202(int& val) {
    func_in_12202(val);
    const int x = 12202;
    std::cout << 12202;
    for (int i = 0; i < 12202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12202(int& val) {
    const int x = 12202;
    std::cout << 12202;
    for (int i = 0; i < 12202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12203(int& val) {
    func_in_12203(val);
    const int x = 12203;
    std::cout << 12203;
    for (int i = 0; i < 12203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12203(int& val) {
    const int x = 12203;
    std::cout << 12203;
    for (int i = 0; i < 12203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12204(int& val) {
    func_in_12204(val);
    const int x = 12204;
    std::cout << 12204;
    for (int i = 0; i < 12204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12204(int& val) {
    const int x = 12204;
    std::cout << 12204;
    for (int i = 0; i < 12204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12205(int& val) {
    func_in_12205(val);
    const int x = 12205;
    std::cout << 12205;
    for (int i = 0; i < 12205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12205(int& val) {
    const int x = 12205;
    std::cout << 12205;
    for (int i = 0; i < 12205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12206(int& val) {
    func_in_12206(val);
    const int x = 12206;
    std::cout << 12206;
    for (int i = 0; i < 12206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12206(int& val) {
    const int x = 12206;
    std::cout << 12206;
    for (int i = 0; i < 12206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12207(int& val) {
    func_in_12207(val);
    const int x = 12207;
    std::cout << 12207;
    for (int i = 0; i < 12207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12207(int& val) {
    const int x = 12207;
    std::cout << 12207;
    for (int i = 0; i < 12207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12208(int& val) {
    func_in_12208(val);
    const int x = 12208;
    std::cout << 12208;
    for (int i = 0; i < 12208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12208(int& val) {
    const int x = 12208;
    std::cout << 12208;
    for (int i = 0; i < 12208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12209(int& val) {
    func_in_12209(val);
    const int x = 12209;
    std::cout << 12209;
    for (int i = 0; i < 12209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12209(int& val) {
    const int x = 12209;
    std::cout << 12209;
    for (int i = 0; i < 12209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12210(int& val) {
    func_in_12210(val);
    const int x = 12210;
    std::cout << 12210;
    for (int i = 0; i < 12210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12210(int& val) {
    const int x = 12210;
    std::cout << 12210;
    for (int i = 0; i < 12210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12211(int& val) {
    func_in_12211(val);
    const int x = 12211;
    std::cout << 12211;
    for (int i = 0; i < 12211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12211(int& val) {
    const int x = 12211;
    std::cout << 12211;
    for (int i = 0; i < 12211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12212(int& val) {
    func_in_12212(val);
    const int x = 12212;
    std::cout << 12212;
    for (int i = 0; i < 12212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12212(int& val) {
    const int x = 12212;
    std::cout << 12212;
    for (int i = 0; i < 12212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12213(int& val) {
    func_in_12213(val);
    const int x = 12213;
    std::cout << 12213;
    for (int i = 0; i < 12213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12213(int& val) {
    const int x = 12213;
    std::cout << 12213;
    for (int i = 0; i < 12213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12214(int& val) {
    func_in_12214(val);
    const int x = 12214;
    std::cout << 12214;
    for (int i = 0; i < 12214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12214(int& val) {
    const int x = 12214;
    std::cout << 12214;
    for (int i = 0; i < 12214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12215(int& val) {
    func_in_12215(val);
    const int x = 12215;
    std::cout << 12215;
    for (int i = 0; i < 12215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12215(int& val) {
    const int x = 12215;
    std::cout << 12215;
    for (int i = 0; i < 12215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12216(int& val) {
    func_in_12216(val);
    const int x = 12216;
    std::cout << 12216;
    for (int i = 0; i < 12216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12216(int& val) {
    const int x = 12216;
    std::cout << 12216;
    for (int i = 0; i < 12216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12217(int& val) {
    func_in_12217(val);
    const int x = 12217;
    std::cout << 12217;
    for (int i = 0; i < 12217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12217(int& val) {
    const int x = 12217;
    std::cout << 12217;
    for (int i = 0; i < 12217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12218(int& val) {
    func_in_12218(val);
    const int x = 12218;
    std::cout << 12218;
    for (int i = 0; i < 12218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12218(int& val) {
    const int x = 12218;
    std::cout << 12218;
    for (int i = 0; i < 12218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12219(int& val) {
    func_in_12219(val);
    const int x = 12219;
    std::cout << 12219;
    for (int i = 0; i < 12219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12219(int& val) {
    const int x = 12219;
    std::cout << 12219;
    for (int i = 0; i < 12219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12220(int& val) {
    func_in_12220(val);
    const int x = 12220;
    std::cout << 12220;
    for (int i = 0; i < 12220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12220(int& val) {
    const int x = 12220;
    std::cout << 12220;
    for (int i = 0; i < 12220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12221(int& val) {
    func_in_12221(val);
    const int x = 12221;
    std::cout << 12221;
    for (int i = 0; i < 12221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12221(int& val) {
    const int x = 12221;
    std::cout << 12221;
    for (int i = 0; i < 12221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12222(int& val) {
    func_in_12222(val);
    const int x = 12222;
    std::cout << 12222;
    for (int i = 0; i < 12222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12222(int& val) {
    const int x = 12222;
    std::cout << 12222;
    for (int i = 0; i < 12222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12223(int& val) {
    func_in_12223(val);
    const int x = 12223;
    std::cout << 12223;
    for (int i = 0; i < 12223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12223(int& val) {
    const int x = 12223;
    std::cout << 12223;
    for (int i = 0; i < 12223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12224(int& val) {
    func_in_12224(val);
    const int x = 12224;
    std::cout << 12224;
    for (int i = 0; i < 12224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12224(int& val) {
    const int x = 12224;
    std::cout << 12224;
    for (int i = 0; i < 12224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12225(int& val) {
    func_in_12225(val);
    const int x = 12225;
    std::cout << 12225;
    for (int i = 0; i < 12225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12225(int& val) {
    const int x = 12225;
    std::cout << 12225;
    for (int i = 0; i < 12225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12226(int& val) {
    func_in_12226(val);
    const int x = 12226;
    std::cout << 12226;
    for (int i = 0; i < 12226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12226(int& val) {
    const int x = 12226;
    std::cout << 12226;
    for (int i = 0; i < 12226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12227(int& val) {
    func_in_12227(val);
    const int x = 12227;
    std::cout << 12227;
    for (int i = 0; i < 12227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12227(int& val) {
    const int x = 12227;
    std::cout << 12227;
    for (int i = 0; i < 12227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12228(int& val) {
    func_in_12228(val);
    const int x = 12228;
    std::cout << 12228;
    for (int i = 0; i < 12228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12228(int& val) {
    const int x = 12228;
    std::cout << 12228;
    for (int i = 0; i < 12228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12229(int& val) {
    func_in_12229(val);
    const int x = 12229;
    std::cout << 12229;
    for (int i = 0; i < 12229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12229(int& val) {
    const int x = 12229;
    std::cout << 12229;
    for (int i = 0; i < 12229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12230(int& val) {
    func_in_12230(val);
    const int x = 12230;
    std::cout << 12230;
    for (int i = 0; i < 12230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12230(int& val) {
    const int x = 12230;
    std::cout << 12230;
    for (int i = 0; i < 12230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12231(int& val) {
    func_in_12231(val);
    const int x = 12231;
    std::cout << 12231;
    for (int i = 0; i < 12231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12231(int& val) {
    const int x = 12231;
    std::cout << 12231;
    for (int i = 0; i < 12231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12232(int& val) {
    func_in_12232(val);
    const int x = 12232;
    std::cout << 12232;
    for (int i = 0; i < 12232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12232(int& val) {
    const int x = 12232;
    std::cout << 12232;
    for (int i = 0; i < 12232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12233(int& val) {
    func_in_12233(val);
    const int x = 12233;
    std::cout << 12233;
    for (int i = 0; i < 12233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12233(int& val) {
    const int x = 12233;
    std::cout << 12233;
    for (int i = 0; i < 12233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12234(int& val) {
    func_in_12234(val);
    const int x = 12234;
    std::cout << 12234;
    for (int i = 0; i < 12234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12234(int& val) {
    const int x = 12234;
    std::cout << 12234;
    for (int i = 0; i < 12234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12235(int& val) {
    func_in_12235(val);
    const int x = 12235;
    std::cout << 12235;
    for (int i = 0; i < 12235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12235(int& val) {
    const int x = 12235;
    std::cout << 12235;
    for (int i = 0; i < 12235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12236(int& val) {
    func_in_12236(val);
    const int x = 12236;
    std::cout << 12236;
    for (int i = 0; i < 12236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12236(int& val) {
    const int x = 12236;
    std::cout << 12236;
    for (int i = 0; i < 12236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12237(int& val) {
    func_in_12237(val);
    const int x = 12237;
    std::cout << 12237;
    for (int i = 0; i < 12237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12237(int& val) {
    const int x = 12237;
    std::cout << 12237;
    for (int i = 0; i < 12237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12238(int& val) {
    func_in_12238(val);
    const int x = 12238;
    std::cout << 12238;
    for (int i = 0; i < 12238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12238(int& val) {
    const int x = 12238;
    std::cout << 12238;
    for (int i = 0; i < 12238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12239(int& val) {
    func_in_12239(val);
    const int x = 12239;
    std::cout << 12239;
    for (int i = 0; i < 12239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12239(int& val) {
    const int x = 12239;
    std::cout << 12239;
    for (int i = 0; i < 12239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12240(int& val) {
    func_in_12240(val);
    const int x = 12240;
    std::cout << 12240;
    for (int i = 0; i < 12240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12240(int& val) {
    const int x = 12240;
    std::cout << 12240;
    for (int i = 0; i < 12240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12241(int& val) {
    func_in_12241(val);
    const int x = 12241;
    std::cout << 12241;
    for (int i = 0; i < 12241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12241(int& val) {
    const int x = 12241;
    std::cout << 12241;
    for (int i = 0; i < 12241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12242(int& val) {
    func_in_12242(val);
    const int x = 12242;
    std::cout << 12242;
    for (int i = 0; i < 12242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12242(int& val) {
    const int x = 12242;
    std::cout << 12242;
    for (int i = 0; i < 12242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12243(int& val) {
    func_in_12243(val);
    const int x = 12243;
    std::cout << 12243;
    for (int i = 0; i < 12243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12243(int& val) {
    const int x = 12243;
    std::cout << 12243;
    for (int i = 0; i < 12243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12244(int& val) {
    func_in_12244(val);
    const int x = 12244;
    std::cout << 12244;
    for (int i = 0; i < 12244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12244(int& val) {
    const int x = 12244;
    std::cout << 12244;
    for (int i = 0; i < 12244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12245(int& val) {
    func_in_12245(val);
    const int x = 12245;
    std::cout << 12245;
    for (int i = 0; i < 12245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12245(int& val) {
    const int x = 12245;
    std::cout << 12245;
    for (int i = 0; i < 12245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12246(int& val) {
    func_in_12246(val);
    const int x = 12246;
    std::cout << 12246;
    for (int i = 0; i < 12246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12246(int& val) {
    const int x = 12246;
    std::cout << 12246;
    for (int i = 0; i < 12246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12247(int& val) {
    func_in_12247(val);
    const int x = 12247;
    std::cout << 12247;
    for (int i = 0; i < 12247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12247(int& val) {
    const int x = 12247;
    std::cout << 12247;
    for (int i = 0; i < 12247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12248(int& val) {
    func_in_12248(val);
    const int x = 12248;
    std::cout << 12248;
    for (int i = 0; i < 12248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12248(int& val) {
    const int x = 12248;
    std::cout << 12248;
    for (int i = 0; i < 12248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12249(int& val) {
    func_in_12249(val);
    const int x = 12249;
    std::cout << 12249;
    for (int i = 0; i < 12249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12249(int& val) {
    const int x = 12249;
    std::cout << 12249;
    for (int i = 0; i < 12249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12250(int& val) {
    func_in_12250(val);
    const int x = 12250;
    std::cout << 12250;
    for (int i = 0; i < 12250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12250(int& val) {
    const int x = 12250;
    std::cout << 12250;
    for (int i = 0; i < 12250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12251(int& val) {
    func_in_12251(val);
    const int x = 12251;
    std::cout << 12251;
    for (int i = 0; i < 12251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12251(int& val) {
    const int x = 12251;
    std::cout << 12251;
    for (int i = 0; i < 12251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12252(int& val) {
    func_in_12252(val);
    const int x = 12252;
    std::cout << 12252;
    for (int i = 0; i < 12252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12252(int& val) {
    const int x = 12252;
    std::cout << 12252;
    for (int i = 0; i < 12252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12253(int& val) {
    func_in_12253(val);
    const int x = 12253;
    std::cout << 12253;
    for (int i = 0; i < 12253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12253(int& val) {
    const int x = 12253;
    std::cout << 12253;
    for (int i = 0; i < 12253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12254(int& val) {
    func_in_12254(val);
    const int x = 12254;
    std::cout << 12254;
    for (int i = 0; i < 12254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12254(int& val) {
    const int x = 12254;
    std::cout << 12254;
    for (int i = 0; i < 12254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12255(int& val) {
    func_in_12255(val);
    const int x = 12255;
    std::cout << 12255;
    for (int i = 0; i < 12255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12255(int& val) {
    const int x = 12255;
    std::cout << 12255;
    for (int i = 0; i < 12255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12256(int& val) {
    func_in_12256(val);
    const int x = 12256;
    std::cout << 12256;
    for (int i = 0; i < 12256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12256(int& val) {
    const int x = 12256;
    std::cout << 12256;
    for (int i = 0; i < 12256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12257(int& val) {
    func_in_12257(val);
    const int x = 12257;
    std::cout << 12257;
    for (int i = 0; i < 12257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12257(int& val) {
    const int x = 12257;
    std::cout << 12257;
    for (int i = 0; i < 12257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12258(int& val) {
    func_in_12258(val);
    const int x = 12258;
    std::cout << 12258;
    for (int i = 0; i < 12258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12258(int& val) {
    const int x = 12258;
    std::cout << 12258;
    for (int i = 0; i < 12258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12259(int& val) {
    func_in_12259(val);
    const int x = 12259;
    std::cout << 12259;
    for (int i = 0; i < 12259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12259(int& val) {
    const int x = 12259;
    std::cout << 12259;
    for (int i = 0; i < 12259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12260(int& val) {
    func_in_12260(val);
    const int x = 12260;
    std::cout << 12260;
    for (int i = 0; i < 12260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12260(int& val) {
    const int x = 12260;
    std::cout << 12260;
    for (int i = 0; i < 12260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12261(int& val) {
    func_in_12261(val);
    const int x = 12261;
    std::cout << 12261;
    for (int i = 0; i < 12261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12261(int& val) {
    const int x = 12261;
    std::cout << 12261;
    for (int i = 0; i < 12261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12262(int& val) {
    func_in_12262(val);
    const int x = 12262;
    std::cout << 12262;
    for (int i = 0; i < 12262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12262(int& val) {
    const int x = 12262;
    std::cout << 12262;
    for (int i = 0; i < 12262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12263(int& val) {
    func_in_12263(val);
    const int x = 12263;
    std::cout << 12263;
    for (int i = 0; i < 12263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12263(int& val) {
    const int x = 12263;
    std::cout << 12263;
    for (int i = 0; i < 12263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12264(int& val) {
    func_in_12264(val);
    const int x = 12264;
    std::cout << 12264;
    for (int i = 0; i < 12264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12264(int& val) {
    const int x = 12264;
    std::cout << 12264;
    for (int i = 0; i < 12264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12265(int& val) {
    func_in_12265(val);
    const int x = 12265;
    std::cout << 12265;
    for (int i = 0; i < 12265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12265(int& val) {
    const int x = 12265;
    std::cout << 12265;
    for (int i = 0; i < 12265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12266(int& val) {
    func_in_12266(val);
    const int x = 12266;
    std::cout << 12266;
    for (int i = 0; i < 12266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12266(int& val) {
    const int x = 12266;
    std::cout << 12266;
    for (int i = 0; i < 12266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12267(int& val) {
    func_in_12267(val);
    const int x = 12267;
    std::cout << 12267;
    for (int i = 0; i < 12267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12267(int& val) {
    const int x = 12267;
    std::cout << 12267;
    for (int i = 0; i < 12267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12268(int& val) {
    func_in_12268(val);
    const int x = 12268;
    std::cout << 12268;
    for (int i = 0; i < 12268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12268(int& val) {
    const int x = 12268;
    std::cout << 12268;
    for (int i = 0; i < 12268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12269(int& val) {
    func_in_12269(val);
    const int x = 12269;
    std::cout << 12269;
    for (int i = 0; i < 12269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12269(int& val) {
    const int x = 12269;
    std::cout << 12269;
    for (int i = 0; i < 12269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12270(int& val) {
    func_in_12270(val);
    const int x = 12270;
    std::cout << 12270;
    for (int i = 0; i < 12270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12270(int& val) {
    const int x = 12270;
    std::cout << 12270;
    for (int i = 0; i < 12270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12271(int& val) {
    func_in_12271(val);
    const int x = 12271;
    std::cout << 12271;
    for (int i = 0; i < 12271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12271(int& val) {
    const int x = 12271;
    std::cout << 12271;
    for (int i = 0; i < 12271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12272(int& val) {
    func_in_12272(val);
    const int x = 12272;
    std::cout << 12272;
    for (int i = 0; i < 12272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12272(int& val) {
    const int x = 12272;
    std::cout << 12272;
    for (int i = 0; i < 12272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12273(int& val) {
    func_in_12273(val);
    const int x = 12273;
    std::cout << 12273;
    for (int i = 0; i < 12273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12273(int& val) {
    const int x = 12273;
    std::cout << 12273;
    for (int i = 0; i < 12273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12274(int& val) {
    func_in_12274(val);
    const int x = 12274;
    std::cout << 12274;
    for (int i = 0; i < 12274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12274(int& val) {
    const int x = 12274;
    std::cout << 12274;
    for (int i = 0; i < 12274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12275(int& val) {
    func_in_12275(val);
    const int x = 12275;
    std::cout << 12275;
    for (int i = 0; i < 12275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12275(int& val) {
    const int x = 12275;
    std::cout << 12275;
    for (int i = 0; i < 12275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12276(int& val) {
    func_in_12276(val);
    const int x = 12276;
    std::cout << 12276;
    for (int i = 0; i < 12276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12276(int& val) {
    const int x = 12276;
    std::cout << 12276;
    for (int i = 0; i < 12276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12277(int& val) {
    func_in_12277(val);
    const int x = 12277;
    std::cout << 12277;
    for (int i = 0; i < 12277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12277(int& val) {
    const int x = 12277;
    std::cout << 12277;
    for (int i = 0; i < 12277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12278(int& val) {
    func_in_12278(val);
    const int x = 12278;
    std::cout << 12278;
    for (int i = 0; i < 12278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12278(int& val) {
    const int x = 12278;
    std::cout << 12278;
    for (int i = 0; i < 12278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12279(int& val) {
    func_in_12279(val);
    const int x = 12279;
    std::cout << 12279;
    for (int i = 0; i < 12279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12279(int& val) {
    const int x = 12279;
    std::cout << 12279;
    for (int i = 0; i < 12279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12280(int& val) {
    func_in_12280(val);
    const int x = 12280;
    std::cout << 12280;
    for (int i = 0; i < 12280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12280(int& val) {
    const int x = 12280;
    std::cout << 12280;
    for (int i = 0; i < 12280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12281(int& val) {
    func_in_12281(val);
    const int x = 12281;
    std::cout << 12281;
    for (int i = 0; i < 12281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12281(int& val) {
    const int x = 12281;
    std::cout << 12281;
    for (int i = 0; i < 12281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12282(int& val) {
    func_in_12282(val);
    const int x = 12282;
    std::cout << 12282;
    for (int i = 0; i < 12282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12282(int& val) {
    const int x = 12282;
    std::cout << 12282;
    for (int i = 0; i < 12282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12283(int& val) {
    func_in_12283(val);
    const int x = 12283;
    std::cout << 12283;
    for (int i = 0; i < 12283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12283(int& val) {
    const int x = 12283;
    std::cout << 12283;
    for (int i = 0; i < 12283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12284(int& val) {
    func_in_12284(val);
    const int x = 12284;
    std::cout << 12284;
    for (int i = 0; i < 12284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12284(int& val) {
    const int x = 12284;
    std::cout << 12284;
    for (int i = 0; i < 12284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12285(int& val) {
    func_in_12285(val);
    const int x = 12285;
    std::cout << 12285;
    for (int i = 0; i < 12285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12285(int& val) {
    const int x = 12285;
    std::cout << 12285;
    for (int i = 0; i < 12285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12286(int& val) {
    func_in_12286(val);
    const int x = 12286;
    std::cout << 12286;
    for (int i = 0; i < 12286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12286(int& val) {
    const int x = 12286;
    std::cout << 12286;
    for (int i = 0; i < 12286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12287(int& val) {
    func_in_12287(val);
    const int x = 12287;
    std::cout << 12287;
    for (int i = 0; i < 12287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12287(int& val) {
    const int x = 12287;
    std::cout << 12287;
    for (int i = 0; i < 12287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12288(int& val) {
    func_in_12288(val);
    const int x = 12288;
    std::cout << 12288;
    for (int i = 0; i < 12288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12288(int& val) {
    const int x = 12288;
    std::cout << 12288;
    for (int i = 0; i < 12288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12289(int& val) {
    func_in_12289(val);
    const int x = 12289;
    std::cout << 12289;
    for (int i = 0; i < 12289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12289(int& val) {
    const int x = 12289;
    std::cout << 12289;
    for (int i = 0; i < 12289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12290(int& val) {
    func_in_12290(val);
    const int x = 12290;
    std::cout << 12290;
    for (int i = 0; i < 12290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12290(int& val) {
    const int x = 12290;
    std::cout << 12290;
    for (int i = 0; i < 12290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12291(int& val) {
    func_in_12291(val);
    const int x = 12291;
    std::cout << 12291;
    for (int i = 0; i < 12291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12291(int& val) {
    const int x = 12291;
    std::cout << 12291;
    for (int i = 0; i < 12291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12292(int& val) {
    func_in_12292(val);
    const int x = 12292;
    std::cout << 12292;
    for (int i = 0; i < 12292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12292(int& val) {
    const int x = 12292;
    std::cout << 12292;
    for (int i = 0; i < 12292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12293(int& val) {
    func_in_12293(val);
    const int x = 12293;
    std::cout << 12293;
    for (int i = 0; i < 12293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12293(int& val) {
    const int x = 12293;
    std::cout << 12293;
    for (int i = 0; i < 12293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12294(int& val) {
    func_in_12294(val);
    const int x = 12294;
    std::cout << 12294;
    for (int i = 0; i < 12294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12294(int& val) {
    const int x = 12294;
    std::cout << 12294;
    for (int i = 0; i < 12294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12295(int& val) {
    func_in_12295(val);
    const int x = 12295;
    std::cout << 12295;
    for (int i = 0; i < 12295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12295(int& val) {
    const int x = 12295;
    std::cout << 12295;
    for (int i = 0; i < 12295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12296(int& val) {
    func_in_12296(val);
    const int x = 12296;
    std::cout << 12296;
    for (int i = 0; i < 12296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12296(int& val) {
    const int x = 12296;
    std::cout << 12296;
    for (int i = 0; i < 12296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12297(int& val) {
    func_in_12297(val);
    const int x = 12297;
    std::cout << 12297;
    for (int i = 0; i < 12297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12297(int& val) {
    const int x = 12297;
    std::cout << 12297;
    for (int i = 0; i < 12297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12298(int& val) {
    func_in_12298(val);
    const int x = 12298;
    std::cout << 12298;
    for (int i = 0; i < 12298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12298(int& val) {
    const int x = 12298;
    std::cout << 12298;
    for (int i = 0; i < 12298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12299(int& val) {
    func_in_12299(val);
    const int x = 12299;
    std::cout << 12299;
    for (int i = 0; i < 12299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12299(int& val) {
    const int x = 12299;
    std::cout << 12299;
    for (int i = 0; i < 12299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12300(int& val) {
    func_in_12300(val);
    const int x = 12300;
    std::cout << 12300;
    for (int i = 0; i < 12300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12300(int& val) {
    const int x = 12300;
    std::cout << 12300;
    for (int i = 0; i < 12300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12301(int& val) {
    func_in_12301(val);
    const int x = 12301;
    std::cout << 12301;
    for (int i = 0; i < 12301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12301(int& val) {
    const int x = 12301;
    std::cout << 12301;
    for (int i = 0; i < 12301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12302(int& val) {
    func_in_12302(val);
    const int x = 12302;
    std::cout << 12302;
    for (int i = 0; i < 12302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12302(int& val) {
    const int x = 12302;
    std::cout << 12302;
    for (int i = 0; i < 12302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12303(int& val) {
    func_in_12303(val);
    const int x = 12303;
    std::cout << 12303;
    for (int i = 0; i < 12303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12303(int& val) {
    const int x = 12303;
    std::cout << 12303;
    for (int i = 0; i < 12303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12304(int& val) {
    func_in_12304(val);
    const int x = 12304;
    std::cout << 12304;
    for (int i = 0; i < 12304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12304(int& val) {
    const int x = 12304;
    std::cout << 12304;
    for (int i = 0; i < 12304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12305(int& val) {
    func_in_12305(val);
    const int x = 12305;
    std::cout << 12305;
    for (int i = 0; i < 12305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12305(int& val) {
    const int x = 12305;
    std::cout << 12305;
    for (int i = 0; i < 12305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12306(int& val) {
    func_in_12306(val);
    const int x = 12306;
    std::cout << 12306;
    for (int i = 0; i < 12306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12306(int& val) {
    const int x = 12306;
    std::cout << 12306;
    for (int i = 0; i < 12306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12307(int& val) {
    func_in_12307(val);
    const int x = 12307;
    std::cout << 12307;
    for (int i = 0; i < 12307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12307(int& val) {
    const int x = 12307;
    std::cout << 12307;
    for (int i = 0; i < 12307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12308(int& val) {
    func_in_12308(val);
    const int x = 12308;
    std::cout << 12308;
    for (int i = 0; i < 12308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12308(int& val) {
    const int x = 12308;
    std::cout << 12308;
    for (int i = 0; i < 12308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12309(int& val) {
    func_in_12309(val);
    const int x = 12309;
    std::cout << 12309;
    for (int i = 0; i < 12309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12309(int& val) {
    const int x = 12309;
    std::cout << 12309;
    for (int i = 0; i < 12309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12310(int& val) {
    func_in_12310(val);
    const int x = 12310;
    std::cout << 12310;
    for (int i = 0; i < 12310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12310(int& val) {
    const int x = 12310;
    std::cout << 12310;
    for (int i = 0; i < 12310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12311(int& val) {
    func_in_12311(val);
    const int x = 12311;
    std::cout << 12311;
    for (int i = 0; i < 12311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12311(int& val) {
    const int x = 12311;
    std::cout << 12311;
    for (int i = 0; i < 12311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12312(int& val) {
    func_in_12312(val);
    const int x = 12312;
    std::cout << 12312;
    for (int i = 0; i < 12312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12312(int& val) {
    const int x = 12312;
    std::cout << 12312;
    for (int i = 0; i < 12312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12313(int& val) {
    func_in_12313(val);
    const int x = 12313;
    std::cout << 12313;
    for (int i = 0; i < 12313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12313(int& val) {
    const int x = 12313;
    std::cout << 12313;
    for (int i = 0; i < 12313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12314(int& val) {
    func_in_12314(val);
    const int x = 12314;
    std::cout << 12314;
    for (int i = 0; i < 12314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12314(int& val) {
    const int x = 12314;
    std::cout << 12314;
    for (int i = 0; i < 12314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12315(int& val) {
    func_in_12315(val);
    const int x = 12315;
    std::cout << 12315;
    for (int i = 0; i < 12315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12315(int& val) {
    const int x = 12315;
    std::cout << 12315;
    for (int i = 0; i < 12315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12316(int& val) {
    func_in_12316(val);
    const int x = 12316;
    std::cout << 12316;
    for (int i = 0; i < 12316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12316(int& val) {
    const int x = 12316;
    std::cout << 12316;
    for (int i = 0; i < 12316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12317(int& val) {
    func_in_12317(val);
    const int x = 12317;
    std::cout << 12317;
    for (int i = 0; i < 12317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12317(int& val) {
    const int x = 12317;
    std::cout << 12317;
    for (int i = 0; i < 12317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12318(int& val) {
    func_in_12318(val);
    const int x = 12318;
    std::cout << 12318;
    for (int i = 0; i < 12318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12318(int& val) {
    const int x = 12318;
    std::cout << 12318;
    for (int i = 0; i < 12318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12319(int& val) {
    func_in_12319(val);
    const int x = 12319;
    std::cout << 12319;
    for (int i = 0; i < 12319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12319(int& val) {
    const int x = 12319;
    std::cout << 12319;
    for (int i = 0; i < 12319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12320(int& val) {
    func_in_12320(val);
    const int x = 12320;
    std::cout << 12320;
    for (int i = 0; i < 12320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12320(int& val) {
    const int x = 12320;
    std::cout << 12320;
    for (int i = 0; i < 12320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12321(int& val) {
    func_in_12321(val);
    const int x = 12321;
    std::cout << 12321;
    for (int i = 0; i < 12321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12321(int& val) {
    const int x = 12321;
    std::cout << 12321;
    for (int i = 0; i < 12321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12322(int& val) {
    func_in_12322(val);
    const int x = 12322;
    std::cout << 12322;
    for (int i = 0; i < 12322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12322(int& val) {
    const int x = 12322;
    std::cout << 12322;
    for (int i = 0; i < 12322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12323(int& val) {
    func_in_12323(val);
    const int x = 12323;
    std::cout << 12323;
    for (int i = 0; i < 12323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12323(int& val) {
    const int x = 12323;
    std::cout << 12323;
    for (int i = 0; i < 12323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12324(int& val) {
    func_in_12324(val);
    const int x = 12324;
    std::cout << 12324;
    for (int i = 0; i < 12324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12324(int& val) {
    const int x = 12324;
    std::cout << 12324;
    for (int i = 0; i < 12324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12325(int& val) {
    func_in_12325(val);
    const int x = 12325;
    std::cout << 12325;
    for (int i = 0; i < 12325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12325(int& val) {
    const int x = 12325;
    std::cout << 12325;
    for (int i = 0; i < 12325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12326(int& val) {
    func_in_12326(val);
    const int x = 12326;
    std::cout << 12326;
    for (int i = 0; i < 12326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12326(int& val) {
    const int x = 12326;
    std::cout << 12326;
    for (int i = 0; i < 12326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12327(int& val) {
    func_in_12327(val);
    const int x = 12327;
    std::cout << 12327;
    for (int i = 0; i < 12327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12327(int& val) {
    const int x = 12327;
    std::cout << 12327;
    for (int i = 0; i < 12327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12328(int& val) {
    func_in_12328(val);
    const int x = 12328;
    std::cout << 12328;
    for (int i = 0; i < 12328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12328(int& val) {
    const int x = 12328;
    std::cout << 12328;
    for (int i = 0; i < 12328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12329(int& val) {
    func_in_12329(val);
    const int x = 12329;
    std::cout << 12329;
    for (int i = 0; i < 12329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12329(int& val) {
    const int x = 12329;
    std::cout << 12329;
    for (int i = 0; i < 12329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12330(int& val) {
    func_in_12330(val);
    const int x = 12330;
    std::cout << 12330;
    for (int i = 0; i < 12330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12330(int& val) {
    const int x = 12330;
    std::cout << 12330;
    for (int i = 0; i < 12330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12331(int& val) {
    func_in_12331(val);
    const int x = 12331;
    std::cout << 12331;
    for (int i = 0; i < 12331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12331(int& val) {
    const int x = 12331;
    std::cout << 12331;
    for (int i = 0; i < 12331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12332(int& val) {
    func_in_12332(val);
    const int x = 12332;
    std::cout << 12332;
    for (int i = 0; i < 12332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12332(int& val) {
    const int x = 12332;
    std::cout << 12332;
    for (int i = 0; i < 12332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12333(int& val) {
    func_in_12333(val);
    const int x = 12333;
    std::cout << 12333;
    for (int i = 0; i < 12333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12333(int& val) {
    const int x = 12333;
    std::cout << 12333;
    for (int i = 0; i < 12333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12334(int& val) {
    func_in_12334(val);
    const int x = 12334;
    std::cout << 12334;
    for (int i = 0; i < 12334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12334(int& val) {
    const int x = 12334;
    std::cout << 12334;
    for (int i = 0; i < 12334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12335(int& val) {
    func_in_12335(val);
    const int x = 12335;
    std::cout << 12335;
    for (int i = 0; i < 12335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12335(int& val) {
    const int x = 12335;
    std::cout << 12335;
    for (int i = 0; i < 12335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12336(int& val) {
    func_in_12336(val);
    const int x = 12336;
    std::cout << 12336;
    for (int i = 0; i < 12336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12336(int& val) {
    const int x = 12336;
    std::cout << 12336;
    for (int i = 0; i < 12336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12337(int& val) {
    func_in_12337(val);
    const int x = 12337;
    std::cout << 12337;
    for (int i = 0; i < 12337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12337(int& val) {
    const int x = 12337;
    std::cout << 12337;
    for (int i = 0; i < 12337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12338(int& val) {
    func_in_12338(val);
    const int x = 12338;
    std::cout << 12338;
    for (int i = 0; i < 12338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12338(int& val) {
    const int x = 12338;
    std::cout << 12338;
    for (int i = 0; i < 12338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12339(int& val) {
    func_in_12339(val);
    const int x = 12339;
    std::cout << 12339;
    for (int i = 0; i < 12339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12339(int& val) {
    const int x = 12339;
    std::cout << 12339;
    for (int i = 0; i < 12339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12340(int& val) {
    func_in_12340(val);
    const int x = 12340;
    std::cout << 12340;
    for (int i = 0; i < 12340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12340(int& val) {
    const int x = 12340;
    std::cout << 12340;
    for (int i = 0; i < 12340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12341(int& val) {
    func_in_12341(val);
    const int x = 12341;
    std::cout << 12341;
    for (int i = 0; i < 12341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12341(int& val) {
    const int x = 12341;
    std::cout << 12341;
    for (int i = 0; i < 12341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12342(int& val) {
    func_in_12342(val);
    const int x = 12342;
    std::cout << 12342;
    for (int i = 0; i < 12342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12342(int& val) {
    const int x = 12342;
    std::cout << 12342;
    for (int i = 0; i < 12342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12343(int& val) {
    func_in_12343(val);
    const int x = 12343;
    std::cout << 12343;
    for (int i = 0; i < 12343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12343(int& val) {
    const int x = 12343;
    std::cout << 12343;
    for (int i = 0; i < 12343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12344(int& val) {
    func_in_12344(val);
    const int x = 12344;
    std::cout << 12344;
    for (int i = 0; i < 12344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12344(int& val) {
    const int x = 12344;
    std::cout << 12344;
    for (int i = 0; i < 12344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12345(int& val) {
    func_in_12345(val);
    const int x = 12345;
    std::cout << 12345;
    for (int i = 0; i < 12345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12345(int& val) {
    const int x = 12345;
    std::cout << 12345;
    for (int i = 0; i < 12345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12346(int& val) {
    func_in_12346(val);
    const int x = 12346;
    std::cout << 12346;
    for (int i = 0; i < 12346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12346(int& val) {
    const int x = 12346;
    std::cout << 12346;
    for (int i = 0; i < 12346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12347(int& val) {
    func_in_12347(val);
    const int x = 12347;
    std::cout << 12347;
    for (int i = 0; i < 12347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12347(int& val) {
    const int x = 12347;
    std::cout << 12347;
    for (int i = 0; i < 12347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12348(int& val) {
    func_in_12348(val);
    const int x = 12348;
    std::cout << 12348;
    for (int i = 0; i < 12348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12348(int& val) {
    const int x = 12348;
    std::cout << 12348;
    for (int i = 0; i < 12348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12349(int& val) {
    func_in_12349(val);
    const int x = 12349;
    std::cout << 12349;
    for (int i = 0; i < 12349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12349(int& val) {
    const int x = 12349;
    std::cout << 12349;
    for (int i = 0; i < 12349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12350(int& val) {
    func_in_12350(val);
    const int x = 12350;
    std::cout << 12350;
    for (int i = 0; i < 12350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12350(int& val) {
    const int x = 12350;
    std::cout << 12350;
    for (int i = 0; i < 12350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12351(int& val) {
    func_in_12351(val);
    const int x = 12351;
    std::cout << 12351;
    for (int i = 0; i < 12351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12351(int& val) {
    const int x = 12351;
    std::cout << 12351;
    for (int i = 0; i < 12351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12352(int& val) {
    func_in_12352(val);
    const int x = 12352;
    std::cout << 12352;
    for (int i = 0; i < 12352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12352(int& val) {
    const int x = 12352;
    std::cout << 12352;
    for (int i = 0; i < 12352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12353(int& val) {
    func_in_12353(val);
    const int x = 12353;
    std::cout << 12353;
    for (int i = 0; i < 12353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12353(int& val) {
    const int x = 12353;
    std::cout << 12353;
    for (int i = 0; i < 12353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12354(int& val) {
    func_in_12354(val);
    const int x = 12354;
    std::cout << 12354;
    for (int i = 0; i < 12354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12354(int& val) {
    const int x = 12354;
    std::cout << 12354;
    for (int i = 0; i < 12354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12355(int& val) {
    func_in_12355(val);
    const int x = 12355;
    std::cout << 12355;
    for (int i = 0; i < 12355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12355(int& val) {
    const int x = 12355;
    std::cout << 12355;
    for (int i = 0; i < 12355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12356(int& val) {
    func_in_12356(val);
    const int x = 12356;
    std::cout << 12356;
    for (int i = 0; i < 12356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12356(int& val) {
    const int x = 12356;
    std::cout << 12356;
    for (int i = 0; i < 12356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12357(int& val) {
    func_in_12357(val);
    const int x = 12357;
    std::cout << 12357;
    for (int i = 0; i < 12357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12357(int& val) {
    const int x = 12357;
    std::cout << 12357;
    for (int i = 0; i < 12357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12358(int& val) {
    func_in_12358(val);
    const int x = 12358;
    std::cout << 12358;
    for (int i = 0; i < 12358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12358(int& val) {
    const int x = 12358;
    std::cout << 12358;
    for (int i = 0; i < 12358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12359(int& val) {
    func_in_12359(val);
    const int x = 12359;
    std::cout << 12359;
    for (int i = 0; i < 12359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12359(int& val) {
    const int x = 12359;
    std::cout << 12359;
    for (int i = 0; i < 12359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12360(int& val) {
    func_in_12360(val);
    const int x = 12360;
    std::cout << 12360;
    for (int i = 0; i < 12360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12360(int& val) {
    const int x = 12360;
    std::cout << 12360;
    for (int i = 0; i < 12360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12361(int& val) {
    func_in_12361(val);
    const int x = 12361;
    std::cout << 12361;
    for (int i = 0; i < 12361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12361(int& val) {
    const int x = 12361;
    std::cout << 12361;
    for (int i = 0; i < 12361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12362(int& val) {
    func_in_12362(val);
    const int x = 12362;
    std::cout << 12362;
    for (int i = 0; i < 12362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12362(int& val) {
    const int x = 12362;
    std::cout << 12362;
    for (int i = 0; i < 12362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12363(int& val) {
    func_in_12363(val);
    const int x = 12363;
    std::cout << 12363;
    for (int i = 0; i < 12363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12363(int& val) {
    const int x = 12363;
    std::cout << 12363;
    for (int i = 0; i < 12363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12364(int& val) {
    func_in_12364(val);
    const int x = 12364;
    std::cout << 12364;
    for (int i = 0; i < 12364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12364(int& val) {
    const int x = 12364;
    std::cout << 12364;
    for (int i = 0; i < 12364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12365(int& val) {
    func_in_12365(val);
    const int x = 12365;
    std::cout << 12365;
    for (int i = 0; i < 12365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12365(int& val) {
    const int x = 12365;
    std::cout << 12365;
    for (int i = 0; i < 12365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12366(int& val) {
    func_in_12366(val);
    const int x = 12366;
    std::cout << 12366;
    for (int i = 0; i < 12366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12366(int& val) {
    const int x = 12366;
    std::cout << 12366;
    for (int i = 0; i < 12366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12367(int& val) {
    func_in_12367(val);
    const int x = 12367;
    std::cout << 12367;
    for (int i = 0; i < 12367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12367(int& val) {
    const int x = 12367;
    std::cout << 12367;
    for (int i = 0; i < 12367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12368(int& val) {
    func_in_12368(val);
    const int x = 12368;
    std::cout << 12368;
    for (int i = 0; i < 12368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12368(int& val) {
    const int x = 12368;
    std::cout << 12368;
    for (int i = 0; i < 12368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12369(int& val) {
    func_in_12369(val);
    const int x = 12369;
    std::cout << 12369;
    for (int i = 0; i < 12369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12369(int& val) {
    const int x = 12369;
    std::cout << 12369;
    for (int i = 0; i < 12369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12370(int& val) {
    func_in_12370(val);
    const int x = 12370;
    std::cout << 12370;
    for (int i = 0; i < 12370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12370(int& val) {
    const int x = 12370;
    std::cout << 12370;
    for (int i = 0; i < 12370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12371(int& val) {
    func_in_12371(val);
    const int x = 12371;
    std::cout << 12371;
    for (int i = 0; i < 12371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12371(int& val) {
    const int x = 12371;
    std::cout << 12371;
    for (int i = 0; i < 12371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12372(int& val) {
    func_in_12372(val);
    const int x = 12372;
    std::cout << 12372;
    for (int i = 0; i < 12372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12372(int& val) {
    const int x = 12372;
    std::cout << 12372;
    for (int i = 0; i < 12372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12373(int& val) {
    func_in_12373(val);
    const int x = 12373;
    std::cout << 12373;
    for (int i = 0; i < 12373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12373(int& val) {
    const int x = 12373;
    std::cout << 12373;
    for (int i = 0; i < 12373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12374(int& val) {
    func_in_12374(val);
    const int x = 12374;
    std::cout << 12374;
    for (int i = 0; i < 12374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12374(int& val) {
    const int x = 12374;
    std::cout << 12374;
    for (int i = 0; i < 12374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12375(int& val) {
    func_in_12375(val);
    const int x = 12375;
    std::cout << 12375;
    for (int i = 0; i < 12375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12375(int& val) {
    const int x = 12375;
    std::cout << 12375;
    for (int i = 0; i < 12375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12376(int& val) {
    func_in_12376(val);
    const int x = 12376;
    std::cout << 12376;
    for (int i = 0; i < 12376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12376(int& val) {
    const int x = 12376;
    std::cout << 12376;
    for (int i = 0; i < 12376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12377(int& val) {
    func_in_12377(val);
    const int x = 12377;
    std::cout << 12377;
    for (int i = 0; i < 12377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12377(int& val) {
    const int x = 12377;
    std::cout << 12377;
    for (int i = 0; i < 12377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12378(int& val) {
    func_in_12378(val);
    const int x = 12378;
    std::cout << 12378;
    for (int i = 0; i < 12378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12378(int& val) {
    const int x = 12378;
    std::cout << 12378;
    for (int i = 0; i < 12378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12379(int& val) {
    func_in_12379(val);
    const int x = 12379;
    std::cout << 12379;
    for (int i = 0; i < 12379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12379(int& val) {
    const int x = 12379;
    std::cout << 12379;
    for (int i = 0; i < 12379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12380(int& val) {
    func_in_12380(val);
    const int x = 12380;
    std::cout << 12380;
    for (int i = 0; i < 12380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12380(int& val) {
    const int x = 12380;
    std::cout << 12380;
    for (int i = 0; i < 12380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12381(int& val) {
    func_in_12381(val);
    const int x = 12381;
    std::cout << 12381;
    for (int i = 0; i < 12381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12381(int& val) {
    const int x = 12381;
    std::cout << 12381;
    for (int i = 0; i < 12381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12382(int& val) {
    func_in_12382(val);
    const int x = 12382;
    std::cout << 12382;
    for (int i = 0; i < 12382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12382(int& val) {
    const int x = 12382;
    std::cout << 12382;
    for (int i = 0; i < 12382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12383(int& val) {
    func_in_12383(val);
    const int x = 12383;
    std::cout << 12383;
    for (int i = 0; i < 12383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12383(int& val) {
    const int x = 12383;
    std::cout << 12383;
    for (int i = 0; i < 12383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12384(int& val) {
    func_in_12384(val);
    const int x = 12384;
    std::cout << 12384;
    for (int i = 0; i < 12384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12384(int& val) {
    const int x = 12384;
    std::cout << 12384;
    for (int i = 0; i < 12384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12385(int& val) {
    func_in_12385(val);
    const int x = 12385;
    std::cout << 12385;
    for (int i = 0; i < 12385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12385(int& val) {
    const int x = 12385;
    std::cout << 12385;
    for (int i = 0; i < 12385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12386(int& val) {
    func_in_12386(val);
    const int x = 12386;
    std::cout << 12386;
    for (int i = 0; i < 12386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12386(int& val) {
    const int x = 12386;
    std::cout << 12386;
    for (int i = 0; i < 12386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12387(int& val) {
    func_in_12387(val);
    const int x = 12387;
    std::cout << 12387;
    for (int i = 0; i < 12387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12387(int& val) {
    const int x = 12387;
    std::cout << 12387;
    for (int i = 0; i < 12387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12388(int& val) {
    func_in_12388(val);
    const int x = 12388;
    std::cout << 12388;
    for (int i = 0; i < 12388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12388(int& val) {
    const int x = 12388;
    std::cout << 12388;
    for (int i = 0; i < 12388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12389(int& val) {
    func_in_12389(val);
    const int x = 12389;
    std::cout << 12389;
    for (int i = 0; i < 12389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12389(int& val) {
    const int x = 12389;
    std::cout << 12389;
    for (int i = 0; i < 12389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12390(int& val) {
    func_in_12390(val);
    const int x = 12390;
    std::cout << 12390;
    for (int i = 0; i < 12390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12390(int& val) {
    const int x = 12390;
    std::cout << 12390;
    for (int i = 0; i < 12390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12391(int& val) {
    func_in_12391(val);
    const int x = 12391;
    std::cout << 12391;
    for (int i = 0; i < 12391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12391(int& val) {
    const int x = 12391;
    std::cout << 12391;
    for (int i = 0; i < 12391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12392(int& val) {
    func_in_12392(val);
    const int x = 12392;
    std::cout << 12392;
    for (int i = 0; i < 12392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12392(int& val) {
    const int x = 12392;
    std::cout << 12392;
    for (int i = 0; i < 12392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12393(int& val) {
    func_in_12393(val);
    const int x = 12393;
    std::cout << 12393;
    for (int i = 0; i < 12393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12393(int& val) {
    const int x = 12393;
    std::cout << 12393;
    for (int i = 0; i < 12393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12394(int& val) {
    func_in_12394(val);
    const int x = 12394;
    std::cout << 12394;
    for (int i = 0; i < 12394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12394(int& val) {
    const int x = 12394;
    std::cout << 12394;
    for (int i = 0; i < 12394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12395(int& val) {
    func_in_12395(val);
    const int x = 12395;
    std::cout << 12395;
    for (int i = 0; i < 12395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12395(int& val) {
    const int x = 12395;
    std::cout << 12395;
    for (int i = 0; i < 12395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12396(int& val) {
    func_in_12396(val);
    const int x = 12396;
    std::cout << 12396;
    for (int i = 0; i < 12396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12396(int& val) {
    const int x = 12396;
    std::cout << 12396;
    for (int i = 0; i < 12396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12397(int& val) {
    func_in_12397(val);
    const int x = 12397;
    std::cout << 12397;
    for (int i = 0; i < 12397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12397(int& val) {
    const int x = 12397;
    std::cout << 12397;
    for (int i = 0; i < 12397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12398(int& val) {
    func_in_12398(val);
    const int x = 12398;
    std::cout << 12398;
    for (int i = 0; i < 12398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12398(int& val) {
    const int x = 12398;
    std::cout << 12398;
    for (int i = 0; i < 12398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12399(int& val) {
    func_in_12399(val);
    const int x = 12399;
    std::cout << 12399;
    for (int i = 0; i < 12399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12399(int& val) {
    const int x = 12399;
    std::cout << 12399;
    for (int i = 0; i < 12399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12400(int& val) {
    func_in_12400(val);
    const int x = 12400;
    std::cout << 12400;
    for (int i = 0; i < 12400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12400(int& val) {
    const int x = 12400;
    std::cout << 12400;
    for (int i = 0; i < 12400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12401(int& val) {
    func_in_12401(val);
    const int x = 12401;
    std::cout << 12401;
    for (int i = 0; i < 12401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12401(int& val) {
    const int x = 12401;
    std::cout << 12401;
    for (int i = 0; i < 12401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12402(int& val) {
    func_in_12402(val);
    const int x = 12402;
    std::cout << 12402;
    for (int i = 0; i < 12402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12402(int& val) {
    const int x = 12402;
    std::cout << 12402;
    for (int i = 0; i < 12402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12403(int& val) {
    func_in_12403(val);
    const int x = 12403;
    std::cout << 12403;
    for (int i = 0; i < 12403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12403(int& val) {
    const int x = 12403;
    std::cout << 12403;
    for (int i = 0; i < 12403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12404(int& val) {
    func_in_12404(val);
    const int x = 12404;
    std::cout << 12404;
    for (int i = 0; i < 12404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12404(int& val) {
    const int x = 12404;
    std::cout << 12404;
    for (int i = 0; i < 12404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12405(int& val) {
    func_in_12405(val);
    const int x = 12405;
    std::cout << 12405;
    for (int i = 0; i < 12405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12405(int& val) {
    const int x = 12405;
    std::cout << 12405;
    for (int i = 0; i < 12405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12406(int& val) {
    func_in_12406(val);
    const int x = 12406;
    std::cout << 12406;
    for (int i = 0; i < 12406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12406(int& val) {
    const int x = 12406;
    std::cout << 12406;
    for (int i = 0; i < 12406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12407(int& val) {
    func_in_12407(val);
    const int x = 12407;
    std::cout << 12407;
    for (int i = 0; i < 12407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12407(int& val) {
    const int x = 12407;
    std::cout << 12407;
    for (int i = 0; i < 12407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12408(int& val) {
    func_in_12408(val);
    const int x = 12408;
    std::cout << 12408;
    for (int i = 0; i < 12408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12408(int& val) {
    const int x = 12408;
    std::cout << 12408;
    for (int i = 0; i < 12408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12409(int& val) {
    func_in_12409(val);
    const int x = 12409;
    std::cout << 12409;
    for (int i = 0; i < 12409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12409(int& val) {
    const int x = 12409;
    std::cout << 12409;
    for (int i = 0; i < 12409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12410(int& val) {
    func_in_12410(val);
    const int x = 12410;
    std::cout << 12410;
    for (int i = 0; i < 12410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12410(int& val) {
    const int x = 12410;
    std::cout << 12410;
    for (int i = 0; i < 12410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12411(int& val) {
    func_in_12411(val);
    const int x = 12411;
    std::cout << 12411;
    for (int i = 0; i < 12411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12411(int& val) {
    const int x = 12411;
    std::cout << 12411;
    for (int i = 0; i < 12411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12412(int& val) {
    func_in_12412(val);
    const int x = 12412;
    std::cout << 12412;
    for (int i = 0; i < 12412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12412(int& val) {
    const int x = 12412;
    std::cout << 12412;
    for (int i = 0; i < 12412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12413(int& val) {
    func_in_12413(val);
    const int x = 12413;
    std::cout << 12413;
    for (int i = 0; i < 12413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12413(int& val) {
    const int x = 12413;
    std::cout << 12413;
    for (int i = 0; i < 12413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12414(int& val) {
    func_in_12414(val);
    const int x = 12414;
    std::cout << 12414;
    for (int i = 0; i < 12414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12414(int& val) {
    const int x = 12414;
    std::cout << 12414;
    for (int i = 0; i < 12414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12415(int& val) {
    func_in_12415(val);
    const int x = 12415;
    std::cout << 12415;
    for (int i = 0; i < 12415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12415(int& val) {
    const int x = 12415;
    std::cout << 12415;
    for (int i = 0; i < 12415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12416(int& val) {
    func_in_12416(val);
    const int x = 12416;
    std::cout << 12416;
    for (int i = 0; i < 12416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12416(int& val) {
    const int x = 12416;
    std::cout << 12416;
    for (int i = 0; i < 12416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12417(int& val) {
    func_in_12417(val);
    const int x = 12417;
    std::cout << 12417;
    for (int i = 0; i < 12417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12417(int& val) {
    const int x = 12417;
    std::cout << 12417;
    for (int i = 0; i < 12417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12418(int& val) {
    func_in_12418(val);
    const int x = 12418;
    std::cout << 12418;
    for (int i = 0; i < 12418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12418(int& val) {
    const int x = 12418;
    std::cout << 12418;
    for (int i = 0; i < 12418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12419(int& val) {
    func_in_12419(val);
    const int x = 12419;
    std::cout << 12419;
    for (int i = 0; i < 12419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12419(int& val) {
    const int x = 12419;
    std::cout << 12419;
    for (int i = 0; i < 12419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12420(int& val) {
    func_in_12420(val);
    const int x = 12420;
    std::cout << 12420;
    for (int i = 0; i < 12420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12420(int& val) {
    const int x = 12420;
    std::cout << 12420;
    for (int i = 0; i < 12420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12421(int& val) {
    func_in_12421(val);
    const int x = 12421;
    std::cout << 12421;
    for (int i = 0; i < 12421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12421(int& val) {
    const int x = 12421;
    std::cout << 12421;
    for (int i = 0; i < 12421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12422(int& val) {
    func_in_12422(val);
    const int x = 12422;
    std::cout << 12422;
    for (int i = 0; i < 12422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12422(int& val) {
    const int x = 12422;
    std::cout << 12422;
    for (int i = 0; i < 12422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12423(int& val) {
    func_in_12423(val);
    const int x = 12423;
    std::cout << 12423;
    for (int i = 0; i < 12423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12423(int& val) {
    const int x = 12423;
    std::cout << 12423;
    for (int i = 0; i < 12423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12424(int& val) {
    func_in_12424(val);
    const int x = 12424;
    std::cout << 12424;
    for (int i = 0; i < 12424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12424(int& val) {
    const int x = 12424;
    std::cout << 12424;
    for (int i = 0; i < 12424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12425(int& val) {
    func_in_12425(val);
    const int x = 12425;
    std::cout << 12425;
    for (int i = 0; i < 12425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12425(int& val) {
    const int x = 12425;
    std::cout << 12425;
    for (int i = 0; i < 12425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12426(int& val) {
    func_in_12426(val);
    const int x = 12426;
    std::cout << 12426;
    for (int i = 0; i < 12426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12426(int& val) {
    const int x = 12426;
    std::cout << 12426;
    for (int i = 0; i < 12426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12427(int& val) {
    func_in_12427(val);
    const int x = 12427;
    std::cout << 12427;
    for (int i = 0; i < 12427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12427(int& val) {
    const int x = 12427;
    std::cout << 12427;
    for (int i = 0; i < 12427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12428(int& val) {
    func_in_12428(val);
    const int x = 12428;
    std::cout << 12428;
    for (int i = 0; i < 12428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12428(int& val) {
    const int x = 12428;
    std::cout << 12428;
    for (int i = 0; i < 12428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12429(int& val) {
    func_in_12429(val);
    const int x = 12429;
    std::cout << 12429;
    for (int i = 0; i < 12429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12429(int& val) {
    const int x = 12429;
    std::cout << 12429;
    for (int i = 0; i < 12429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12430(int& val) {
    func_in_12430(val);
    const int x = 12430;
    std::cout << 12430;
    for (int i = 0; i < 12430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12430(int& val) {
    const int x = 12430;
    std::cout << 12430;
    for (int i = 0; i < 12430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12431(int& val) {
    func_in_12431(val);
    const int x = 12431;
    std::cout << 12431;
    for (int i = 0; i < 12431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12431(int& val) {
    const int x = 12431;
    std::cout << 12431;
    for (int i = 0; i < 12431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12432(int& val) {
    func_in_12432(val);
    const int x = 12432;
    std::cout << 12432;
    for (int i = 0; i < 12432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12432(int& val) {
    const int x = 12432;
    std::cout << 12432;
    for (int i = 0; i < 12432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12433(int& val) {
    func_in_12433(val);
    const int x = 12433;
    std::cout << 12433;
    for (int i = 0; i < 12433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12433(int& val) {
    const int x = 12433;
    std::cout << 12433;
    for (int i = 0; i < 12433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12434(int& val) {
    func_in_12434(val);
    const int x = 12434;
    std::cout << 12434;
    for (int i = 0; i < 12434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12434(int& val) {
    const int x = 12434;
    std::cout << 12434;
    for (int i = 0; i < 12434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12435(int& val) {
    func_in_12435(val);
    const int x = 12435;
    std::cout << 12435;
    for (int i = 0; i < 12435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12435(int& val) {
    const int x = 12435;
    std::cout << 12435;
    for (int i = 0; i < 12435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12436(int& val) {
    func_in_12436(val);
    const int x = 12436;
    std::cout << 12436;
    for (int i = 0; i < 12436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12436(int& val) {
    const int x = 12436;
    std::cout << 12436;
    for (int i = 0; i < 12436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12437(int& val) {
    func_in_12437(val);
    const int x = 12437;
    std::cout << 12437;
    for (int i = 0; i < 12437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12437(int& val) {
    const int x = 12437;
    std::cout << 12437;
    for (int i = 0; i < 12437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12438(int& val) {
    func_in_12438(val);
    const int x = 12438;
    std::cout << 12438;
    for (int i = 0; i < 12438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12438(int& val) {
    const int x = 12438;
    std::cout << 12438;
    for (int i = 0; i < 12438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12439(int& val) {
    func_in_12439(val);
    const int x = 12439;
    std::cout << 12439;
    for (int i = 0; i < 12439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12439(int& val) {
    const int x = 12439;
    std::cout << 12439;
    for (int i = 0; i < 12439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12440(int& val) {
    func_in_12440(val);
    const int x = 12440;
    std::cout << 12440;
    for (int i = 0; i < 12440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12440(int& val) {
    const int x = 12440;
    std::cout << 12440;
    for (int i = 0; i < 12440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12441(int& val) {
    func_in_12441(val);
    const int x = 12441;
    std::cout << 12441;
    for (int i = 0; i < 12441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12441(int& val) {
    const int x = 12441;
    std::cout << 12441;
    for (int i = 0; i < 12441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12442(int& val) {
    func_in_12442(val);
    const int x = 12442;
    std::cout << 12442;
    for (int i = 0; i < 12442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12442(int& val) {
    const int x = 12442;
    std::cout << 12442;
    for (int i = 0; i < 12442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12443(int& val) {
    func_in_12443(val);
    const int x = 12443;
    std::cout << 12443;
    for (int i = 0; i < 12443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12443(int& val) {
    const int x = 12443;
    std::cout << 12443;
    for (int i = 0; i < 12443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12444(int& val) {
    func_in_12444(val);
    const int x = 12444;
    std::cout << 12444;
    for (int i = 0; i < 12444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12444(int& val) {
    const int x = 12444;
    std::cout << 12444;
    for (int i = 0; i < 12444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12445(int& val) {
    func_in_12445(val);
    const int x = 12445;
    std::cout << 12445;
    for (int i = 0; i < 12445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12445(int& val) {
    const int x = 12445;
    std::cout << 12445;
    for (int i = 0; i < 12445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12446(int& val) {
    func_in_12446(val);
    const int x = 12446;
    std::cout << 12446;
    for (int i = 0; i < 12446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12446(int& val) {
    const int x = 12446;
    std::cout << 12446;
    for (int i = 0; i < 12446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12447(int& val) {
    func_in_12447(val);
    const int x = 12447;
    std::cout << 12447;
    for (int i = 0; i < 12447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12447(int& val) {
    const int x = 12447;
    std::cout << 12447;
    for (int i = 0; i < 12447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12448(int& val) {
    func_in_12448(val);
    const int x = 12448;
    std::cout << 12448;
    for (int i = 0; i < 12448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12448(int& val) {
    const int x = 12448;
    std::cout << 12448;
    for (int i = 0; i < 12448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12449(int& val) {
    func_in_12449(val);
    const int x = 12449;
    std::cout << 12449;
    for (int i = 0; i < 12449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12449(int& val) {
    const int x = 12449;
    std::cout << 12449;
    for (int i = 0; i < 12449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12450(int& val) {
    func_in_12450(val);
    const int x = 12450;
    std::cout << 12450;
    for (int i = 0; i < 12450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12450(int& val) {
    const int x = 12450;
    std::cout << 12450;
    for (int i = 0; i < 12450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12451(int& val) {
    func_in_12451(val);
    const int x = 12451;
    std::cout << 12451;
    for (int i = 0; i < 12451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12451(int& val) {
    const int x = 12451;
    std::cout << 12451;
    for (int i = 0; i < 12451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12452(int& val) {
    func_in_12452(val);
    const int x = 12452;
    std::cout << 12452;
    for (int i = 0; i < 12452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12452(int& val) {
    const int x = 12452;
    std::cout << 12452;
    for (int i = 0; i < 12452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12453(int& val) {
    func_in_12453(val);
    const int x = 12453;
    std::cout << 12453;
    for (int i = 0; i < 12453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12453(int& val) {
    const int x = 12453;
    std::cout << 12453;
    for (int i = 0; i < 12453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12454(int& val) {
    func_in_12454(val);
    const int x = 12454;
    std::cout << 12454;
    for (int i = 0; i < 12454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12454(int& val) {
    const int x = 12454;
    std::cout << 12454;
    for (int i = 0; i < 12454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12455(int& val) {
    func_in_12455(val);
    const int x = 12455;
    std::cout << 12455;
    for (int i = 0; i < 12455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12455(int& val) {
    const int x = 12455;
    std::cout << 12455;
    for (int i = 0; i < 12455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12456(int& val) {
    func_in_12456(val);
    const int x = 12456;
    std::cout << 12456;
    for (int i = 0; i < 12456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12456(int& val) {
    const int x = 12456;
    std::cout << 12456;
    for (int i = 0; i < 12456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12457(int& val) {
    func_in_12457(val);
    const int x = 12457;
    std::cout << 12457;
    for (int i = 0; i < 12457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12457(int& val) {
    const int x = 12457;
    std::cout << 12457;
    for (int i = 0; i < 12457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12458(int& val) {
    func_in_12458(val);
    const int x = 12458;
    std::cout << 12458;
    for (int i = 0; i < 12458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12458(int& val) {
    const int x = 12458;
    std::cout << 12458;
    for (int i = 0; i < 12458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12459(int& val) {
    func_in_12459(val);
    const int x = 12459;
    std::cout << 12459;
    for (int i = 0; i < 12459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12459(int& val) {
    const int x = 12459;
    std::cout << 12459;
    for (int i = 0; i < 12459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12460(int& val) {
    func_in_12460(val);
    const int x = 12460;
    std::cout << 12460;
    for (int i = 0; i < 12460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12460(int& val) {
    const int x = 12460;
    std::cout << 12460;
    for (int i = 0; i < 12460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12461(int& val) {
    func_in_12461(val);
    const int x = 12461;
    std::cout << 12461;
    for (int i = 0; i < 12461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12461(int& val) {
    const int x = 12461;
    std::cout << 12461;
    for (int i = 0; i < 12461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12462(int& val) {
    func_in_12462(val);
    const int x = 12462;
    std::cout << 12462;
    for (int i = 0; i < 12462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12462(int& val) {
    const int x = 12462;
    std::cout << 12462;
    for (int i = 0; i < 12462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12463(int& val) {
    func_in_12463(val);
    const int x = 12463;
    std::cout << 12463;
    for (int i = 0; i < 12463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12463(int& val) {
    const int x = 12463;
    std::cout << 12463;
    for (int i = 0; i < 12463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12464(int& val) {
    func_in_12464(val);
    const int x = 12464;
    std::cout << 12464;
    for (int i = 0; i < 12464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12464(int& val) {
    const int x = 12464;
    std::cout << 12464;
    for (int i = 0; i < 12464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12465(int& val) {
    func_in_12465(val);
    const int x = 12465;
    std::cout << 12465;
    for (int i = 0; i < 12465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12465(int& val) {
    const int x = 12465;
    std::cout << 12465;
    for (int i = 0; i < 12465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12466(int& val) {
    func_in_12466(val);
    const int x = 12466;
    std::cout << 12466;
    for (int i = 0; i < 12466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12466(int& val) {
    const int x = 12466;
    std::cout << 12466;
    for (int i = 0; i < 12466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12467(int& val) {
    func_in_12467(val);
    const int x = 12467;
    std::cout << 12467;
    for (int i = 0; i < 12467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12467(int& val) {
    const int x = 12467;
    std::cout << 12467;
    for (int i = 0; i < 12467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12468(int& val) {
    func_in_12468(val);
    const int x = 12468;
    std::cout << 12468;
    for (int i = 0; i < 12468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12468(int& val) {
    const int x = 12468;
    std::cout << 12468;
    for (int i = 0; i < 12468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12469(int& val) {
    func_in_12469(val);
    const int x = 12469;
    std::cout << 12469;
    for (int i = 0; i < 12469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12469(int& val) {
    const int x = 12469;
    std::cout << 12469;
    for (int i = 0; i < 12469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12470(int& val) {
    func_in_12470(val);
    const int x = 12470;
    std::cout << 12470;
    for (int i = 0; i < 12470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12470(int& val) {
    const int x = 12470;
    std::cout << 12470;
    for (int i = 0; i < 12470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12471(int& val) {
    func_in_12471(val);
    const int x = 12471;
    std::cout << 12471;
    for (int i = 0; i < 12471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12471(int& val) {
    const int x = 12471;
    std::cout << 12471;
    for (int i = 0; i < 12471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12472(int& val) {
    func_in_12472(val);
    const int x = 12472;
    std::cout << 12472;
    for (int i = 0; i < 12472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12472(int& val) {
    const int x = 12472;
    std::cout << 12472;
    for (int i = 0; i < 12472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12473(int& val) {
    func_in_12473(val);
    const int x = 12473;
    std::cout << 12473;
    for (int i = 0; i < 12473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12473(int& val) {
    const int x = 12473;
    std::cout << 12473;
    for (int i = 0; i < 12473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12474(int& val) {
    func_in_12474(val);
    const int x = 12474;
    std::cout << 12474;
    for (int i = 0; i < 12474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12474(int& val) {
    const int x = 12474;
    std::cout << 12474;
    for (int i = 0; i < 12474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12475(int& val) {
    func_in_12475(val);
    const int x = 12475;
    std::cout << 12475;
    for (int i = 0; i < 12475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12475(int& val) {
    const int x = 12475;
    std::cout << 12475;
    for (int i = 0; i < 12475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12476(int& val) {
    func_in_12476(val);
    const int x = 12476;
    std::cout << 12476;
    for (int i = 0; i < 12476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12476(int& val) {
    const int x = 12476;
    std::cout << 12476;
    for (int i = 0; i < 12476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12477(int& val) {
    func_in_12477(val);
    const int x = 12477;
    std::cout << 12477;
    for (int i = 0; i < 12477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12477(int& val) {
    const int x = 12477;
    std::cout << 12477;
    for (int i = 0; i < 12477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12478(int& val) {
    func_in_12478(val);
    const int x = 12478;
    std::cout << 12478;
    for (int i = 0; i < 12478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12478(int& val) {
    const int x = 12478;
    std::cout << 12478;
    for (int i = 0; i < 12478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12479(int& val) {
    func_in_12479(val);
    const int x = 12479;
    std::cout << 12479;
    for (int i = 0; i < 12479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12479(int& val) {
    const int x = 12479;
    std::cout << 12479;
    for (int i = 0; i < 12479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12480(int& val) {
    func_in_12480(val);
    const int x = 12480;
    std::cout << 12480;
    for (int i = 0; i < 12480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12480(int& val) {
    const int x = 12480;
    std::cout << 12480;
    for (int i = 0; i < 12480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12481(int& val) {
    func_in_12481(val);
    const int x = 12481;
    std::cout << 12481;
    for (int i = 0; i < 12481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12481(int& val) {
    const int x = 12481;
    std::cout << 12481;
    for (int i = 0; i < 12481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12482(int& val) {
    func_in_12482(val);
    const int x = 12482;
    std::cout << 12482;
    for (int i = 0; i < 12482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12482(int& val) {
    const int x = 12482;
    std::cout << 12482;
    for (int i = 0; i < 12482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12483(int& val) {
    func_in_12483(val);
    const int x = 12483;
    std::cout << 12483;
    for (int i = 0; i < 12483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12483(int& val) {
    const int x = 12483;
    std::cout << 12483;
    for (int i = 0; i < 12483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12484(int& val) {
    func_in_12484(val);
    const int x = 12484;
    std::cout << 12484;
    for (int i = 0; i < 12484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12484(int& val) {
    const int x = 12484;
    std::cout << 12484;
    for (int i = 0; i < 12484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12485(int& val) {
    func_in_12485(val);
    const int x = 12485;
    std::cout << 12485;
    for (int i = 0; i < 12485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12485(int& val) {
    const int x = 12485;
    std::cout << 12485;
    for (int i = 0; i < 12485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12486(int& val) {
    func_in_12486(val);
    const int x = 12486;
    std::cout << 12486;
    for (int i = 0; i < 12486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12486(int& val) {
    const int x = 12486;
    std::cout << 12486;
    for (int i = 0; i < 12486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12487(int& val) {
    func_in_12487(val);
    const int x = 12487;
    std::cout << 12487;
    for (int i = 0; i < 12487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12487(int& val) {
    const int x = 12487;
    std::cout << 12487;
    for (int i = 0; i < 12487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12488(int& val) {
    func_in_12488(val);
    const int x = 12488;
    std::cout << 12488;
    for (int i = 0; i < 12488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12488(int& val) {
    const int x = 12488;
    std::cout << 12488;
    for (int i = 0; i < 12488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12489(int& val) {
    func_in_12489(val);
    const int x = 12489;
    std::cout << 12489;
    for (int i = 0; i < 12489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12489(int& val) {
    const int x = 12489;
    std::cout << 12489;
    for (int i = 0; i < 12489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12490(int& val) {
    func_in_12490(val);
    const int x = 12490;
    std::cout << 12490;
    for (int i = 0; i < 12490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12490(int& val) {
    const int x = 12490;
    std::cout << 12490;
    for (int i = 0; i < 12490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12491(int& val) {
    func_in_12491(val);
    const int x = 12491;
    std::cout << 12491;
    for (int i = 0; i < 12491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12491(int& val) {
    const int x = 12491;
    std::cout << 12491;
    for (int i = 0; i < 12491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12492(int& val) {
    func_in_12492(val);
    const int x = 12492;
    std::cout << 12492;
    for (int i = 0; i < 12492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12492(int& val) {
    const int x = 12492;
    std::cout << 12492;
    for (int i = 0; i < 12492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12493(int& val) {
    func_in_12493(val);
    const int x = 12493;
    std::cout << 12493;
    for (int i = 0; i < 12493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12493(int& val) {
    const int x = 12493;
    std::cout << 12493;
    for (int i = 0; i < 12493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12494(int& val) {
    func_in_12494(val);
    const int x = 12494;
    std::cout << 12494;
    for (int i = 0; i < 12494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12494(int& val) {
    const int x = 12494;
    std::cout << 12494;
    for (int i = 0; i < 12494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12495(int& val) {
    func_in_12495(val);
    const int x = 12495;
    std::cout << 12495;
    for (int i = 0; i < 12495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12495(int& val) {
    const int x = 12495;
    std::cout << 12495;
    for (int i = 0; i < 12495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12496(int& val) {
    func_in_12496(val);
    const int x = 12496;
    std::cout << 12496;
    for (int i = 0; i < 12496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12496(int& val) {
    const int x = 12496;
    std::cout << 12496;
    for (int i = 0; i < 12496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12497(int& val) {
    func_in_12497(val);
    const int x = 12497;
    std::cout << 12497;
    for (int i = 0; i < 12497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12497(int& val) {
    const int x = 12497;
    std::cout << 12497;
    for (int i = 0; i < 12497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12498(int& val) {
    func_in_12498(val);
    const int x = 12498;
    std::cout << 12498;
    for (int i = 0; i < 12498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12498(int& val) {
    const int x = 12498;
    std::cout << 12498;
    for (int i = 0; i < 12498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12499(int& val) {
    func_in_12499(val);
    const int x = 12499;
    std::cout << 12499;
    for (int i = 0; i < 12499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12499(int& val) {
    const int x = 12499;
    std::cout << 12499;
    for (int i = 0; i < 12499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12500(int& val) {
    func_in_12500(val);
    const int x = 12500;
    std::cout << 12500;
    for (int i = 0; i < 12500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12500(int& val) {
    const int x = 12500;
    std::cout << 12500;
    for (int i = 0; i < 12500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12501(int& val) {
    func_in_12501(val);
    const int x = 12501;
    std::cout << 12501;
    for (int i = 0; i < 12501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12501(int& val) {
    const int x = 12501;
    std::cout << 12501;
    for (int i = 0; i < 12501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12502(int& val) {
    func_in_12502(val);
    const int x = 12502;
    std::cout << 12502;
    for (int i = 0; i < 12502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12502(int& val) {
    const int x = 12502;
    std::cout << 12502;
    for (int i = 0; i < 12502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12503(int& val) {
    func_in_12503(val);
    const int x = 12503;
    std::cout << 12503;
    for (int i = 0; i < 12503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12503(int& val) {
    const int x = 12503;
    std::cout << 12503;
    for (int i = 0; i < 12503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12504(int& val) {
    func_in_12504(val);
    const int x = 12504;
    std::cout << 12504;
    for (int i = 0; i < 12504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12504(int& val) {
    const int x = 12504;
    std::cout << 12504;
    for (int i = 0; i < 12504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12505(int& val) {
    func_in_12505(val);
    const int x = 12505;
    std::cout << 12505;
    for (int i = 0; i < 12505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12505(int& val) {
    const int x = 12505;
    std::cout << 12505;
    for (int i = 0; i < 12505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12506(int& val) {
    func_in_12506(val);
    const int x = 12506;
    std::cout << 12506;
    for (int i = 0; i < 12506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12506(int& val) {
    const int x = 12506;
    std::cout << 12506;
    for (int i = 0; i < 12506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12507(int& val) {
    func_in_12507(val);
    const int x = 12507;
    std::cout << 12507;
    for (int i = 0; i < 12507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12507(int& val) {
    const int x = 12507;
    std::cout << 12507;
    for (int i = 0; i < 12507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12508(int& val) {
    func_in_12508(val);
    const int x = 12508;
    std::cout << 12508;
    for (int i = 0; i < 12508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12508(int& val) {
    const int x = 12508;
    std::cout << 12508;
    for (int i = 0; i < 12508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12509(int& val) {
    func_in_12509(val);
    const int x = 12509;
    std::cout << 12509;
    for (int i = 0; i < 12509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12509(int& val) {
    const int x = 12509;
    std::cout << 12509;
    for (int i = 0; i < 12509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12510(int& val) {
    func_in_12510(val);
    const int x = 12510;
    std::cout << 12510;
    for (int i = 0; i < 12510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12510(int& val) {
    const int x = 12510;
    std::cout << 12510;
    for (int i = 0; i < 12510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12511(int& val) {
    func_in_12511(val);
    const int x = 12511;
    std::cout << 12511;
    for (int i = 0; i < 12511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12511(int& val) {
    const int x = 12511;
    std::cout << 12511;
    for (int i = 0; i < 12511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12512(int& val) {
    func_in_12512(val);
    const int x = 12512;
    std::cout << 12512;
    for (int i = 0; i < 12512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12512(int& val) {
    const int x = 12512;
    std::cout << 12512;
    for (int i = 0; i < 12512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12513(int& val) {
    func_in_12513(val);
    const int x = 12513;
    std::cout << 12513;
    for (int i = 0; i < 12513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12513(int& val) {
    const int x = 12513;
    std::cout << 12513;
    for (int i = 0; i < 12513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12514(int& val) {
    func_in_12514(val);
    const int x = 12514;
    std::cout << 12514;
    for (int i = 0; i < 12514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12514(int& val) {
    const int x = 12514;
    std::cout << 12514;
    for (int i = 0; i < 12514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12515(int& val) {
    func_in_12515(val);
    const int x = 12515;
    std::cout << 12515;
    for (int i = 0; i < 12515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12515(int& val) {
    const int x = 12515;
    std::cout << 12515;
    for (int i = 0; i < 12515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12516(int& val) {
    func_in_12516(val);
    const int x = 12516;
    std::cout << 12516;
    for (int i = 0; i < 12516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12516(int& val) {
    const int x = 12516;
    std::cout << 12516;
    for (int i = 0; i < 12516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12517(int& val) {
    func_in_12517(val);
    const int x = 12517;
    std::cout << 12517;
    for (int i = 0; i < 12517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12517(int& val) {
    const int x = 12517;
    std::cout << 12517;
    for (int i = 0; i < 12517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12518(int& val) {
    func_in_12518(val);
    const int x = 12518;
    std::cout << 12518;
    for (int i = 0; i < 12518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12518(int& val) {
    const int x = 12518;
    std::cout << 12518;
    for (int i = 0; i < 12518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12519(int& val) {
    func_in_12519(val);
    const int x = 12519;
    std::cout << 12519;
    for (int i = 0; i < 12519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12519(int& val) {
    const int x = 12519;
    std::cout << 12519;
    for (int i = 0; i < 12519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12520(int& val) {
    func_in_12520(val);
    const int x = 12520;
    std::cout << 12520;
    for (int i = 0; i < 12520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12520(int& val) {
    const int x = 12520;
    std::cout << 12520;
    for (int i = 0; i < 12520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12521(int& val) {
    func_in_12521(val);
    const int x = 12521;
    std::cout << 12521;
    for (int i = 0; i < 12521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12521(int& val) {
    const int x = 12521;
    std::cout << 12521;
    for (int i = 0; i < 12521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12522(int& val) {
    func_in_12522(val);
    const int x = 12522;
    std::cout << 12522;
    for (int i = 0; i < 12522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12522(int& val) {
    const int x = 12522;
    std::cout << 12522;
    for (int i = 0; i < 12522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12523(int& val) {
    func_in_12523(val);
    const int x = 12523;
    std::cout << 12523;
    for (int i = 0; i < 12523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12523(int& val) {
    const int x = 12523;
    std::cout << 12523;
    for (int i = 0; i < 12523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12524(int& val) {
    func_in_12524(val);
    const int x = 12524;
    std::cout << 12524;
    for (int i = 0; i < 12524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12524(int& val) {
    const int x = 12524;
    std::cout << 12524;
    for (int i = 0; i < 12524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12525(int& val) {
    func_in_12525(val);
    const int x = 12525;
    std::cout << 12525;
    for (int i = 0; i < 12525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12525(int& val) {
    const int x = 12525;
    std::cout << 12525;
    for (int i = 0; i < 12525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12526(int& val) {
    func_in_12526(val);
    const int x = 12526;
    std::cout << 12526;
    for (int i = 0; i < 12526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12526(int& val) {
    const int x = 12526;
    std::cout << 12526;
    for (int i = 0; i < 12526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12527(int& val) {
    func_in_12527(val);
    const int x = 12527;
    std::cout << 12527;
    for (int i = 0; i < 12527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12527(int& val) {
    const int x = 12527;
    std::cout << 12527;
    for (int i = 0; i < 12527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12528(int& val) {
    func_in_12528(val);
    const int x = 12528;
    std::cout << 12528;
    for (int i = 0; i < 12528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12528(int& val) {
    const int x = 12528;
    std::cout << 12528;
    for (int i = 0; i < 12528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12529(int& val) {
    func_in_12529(val);
    const int x = 12529;
    std::cout << 12529;
    for (int i = 0; i < 12529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12529(int& val) {
    const int x = 12529;
    std::cout << 12529;
    for (int i = 0; i < 12529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12530(int& val) {
    func_in_12530(val);
    const int x = 12530;
    std::cout << 12530;
    for (int i = 0; i < 12530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12530(int& val) {
    const int x = 12530;
    std::cout << 12530;
    for (int i = 0; i < 12530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12531(int& val) {
    func_in_12531(val);
    const int x = 12531;
    std::cout << 12531;
    for (int i = 0; i < 12531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12531(int& val) {
    const int x = 12531;
    std::cout << 12531;
    for (int i = 0; i < 12531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12532(int& val) {
    func_in_12532(val);
    const int x = 12532;
    std::cout << 12532;
    for (int i = 0; i < 12532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12532(int& val) {
    const int x = 12532;
    std::cout << 12532;
    for (int i = 0; i < 12532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12533(int& val) {
    func_in_12533(val);
    const int x = 12533;
    std::cout << 12533;
    for (int i = 0; i < 12533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12533(int& val) {
    const int x = 12533;
    std::cout << 12533;
    for (int i = 0; i < 12533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12534(int& val) {
    func_in_12534(val);
    const int x = 12534;
    std::cout << 12534;
    for (int i = 0; i < 12534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12534(int& val) {
    const int x = 12534;
    std::cout << 12534;
    for (int i = 0; i < 12534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12535(int& val) {
    func_in_12535(val);
    const int x = 12535;
    std::cout << 12535;
    for (int i = 0; i < 12535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12535(int& val) {
    const int x = 12535;
    std::cout << 12535;
    for (int i = 0; i < 12535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12536(int& val) {
    func_in_12536(val);
    const int x = 12536;
    std::cout << 12536;
    for (int i = 0; i < 12536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12536(int& val) {
    const int x = 12536;
    std::cout << 12536;
    for (int i = 0; i < 12536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12537(int& val) {
    func_in_12537(val);
    const int x = 12537;
    std::cout << 12537;
    for (int i = 0; i < 12537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12537(int& val) {
    const int x = 12537;
    std::cout << 12537;
    for (int i = 0; i < 12537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12538(int& val) {
    func_in_12538(val);
    const int x = 12538;
    std::cout << 12538;
    for (int i = 0; i < 12538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12538(int& val) {
    const int x = 12538;
    std::cout << 12538;
    for (int i = 0; i < 12538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12539(int& val) {
    func_in_12539(val);
    const int x = 12539;
    std::cout << 12539;
    for (int i = 0; i < 12539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12539(int& val) {
    const int x = 12539;
    std::cout << 12539;
    for (int i = 0; i < 12539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12540(int& val) {
    func_in_12540(val);
    const int x = 12540;
    std::cout << 12540;
    for (int i = 0; i < 12540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12540(int& val) {
    const int x = 12540;
    std::cout << 12540;
    for (int i = 0; i < 12540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12541(int& val) {
    func_in_12541(val);
    const int x = 12541;
    std::cout << 12541;
    for (int i = 0; i < 12541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12541(int& val) {
    const int x = 12541;
    std::cout << 12541;
    for (int i = 0; i < 12541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12542(int& val) {
    func_in_12542(val);
    const int x = 12542;
    std::cout << 12542;
    for (int i = 0; i < 12542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12542(int& val) {
    const int x = 12542;
    std::cout << 12542;
    for (int i = 0; i < 12542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12543(int& val) {
    func_in_12543(val);
    const int x = 12543;
    std::cout << 12543;
    for (int i = 0; i < 12543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12543(int& val) {
    const int x = 12543;
    std::cout << 12543;
    for (int i = 0; i < 12543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12544(int& val) {
    func_in_12544(val);
    const int x = 12544;
    std::cout << 12544;
    for (int i = 0; i < 12544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12544(int& val) {
    const int x = 12544;
    std::cout << 12544;
    for (int i = 0; i < 12544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12545(int& val) {
    func_in_12545(val);
    const int x = 12545;
    std::cout << 12545;
    for (int i = 0; i < 12545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12545(int& val) {
    const int x = 12545;
    std::cout << 12545;
    for (int i = 0; i < 12545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12546(int& val) {
    func_in_12546(val);
    const int x = 12546;
    std::cout << 12546;
    for (int i = 0; i < 12546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12546(int& val) {
    const int x = 12546;
    std::cout << 12546;
    for (int i = 0; i < 12546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12547(int& val) {
    func_in_12547(val);
    const int x = 12547;
    std::cout << 12547;
    for (int i = 0; i < 12547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12547(int& val) {
    const int x = 12547;
    std::cout << 12547;
    for (int i = 0; i < 12547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12548(int& val) {
    func_in_12548(val);
    const int x = 12548;
    std::cout << 12548;
    for (int i = 0; i < 12548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12548(int& val) {
    const int x = 12548;
    std::cout << 12548;
    for (int i = 0; i < 12548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12549(int& val) {
    func_in_12549(val);
    const int x = 12549;
    std::cout << 12549;
    for (int i = 0; i < 12549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12549(int& val) {
    const int x = 12549;
    std::cout << 12549;
    for (int i = 0; i < 12549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12550(int& val) {
    func_in_12550(val);
    const int x = 12550;
    std::cout << 12550;
    for (int i = 0; i < 12550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12550(int& val) {
    const int x = 12550;
    std::cout << 12550;
    for (int i = 0; i < 12550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12551(int& val) {
    func_in_12551(val);
    const int x = 12551;
    std::cout << 12551;
    for (int i = 0; i < 12551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12551(int& val) {
    const int x = 12551;
    std::cout << 12551;
    for (int i = 0; i < 12551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12552(int& val) {
    func_in_12552(val);
    const int x = 12552;
    std::cout << 12552;
    for (int i = 0; i < 12552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12552(int& val) {
    const int x = 12552;
    std::cout << 12552;
    for (int i = 0; i < 12552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12553(int& val) {
    func_in_12553(val);
    const int x = 12553;
    std::cout << 12553;
    for (int i = 0; i < 12553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12553(int& val) {
    const int x = 12553;
    std::cout << 12553;
    for (int i = 0; i < 12553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12554(int& val) {
    func_in_12554(val);
    const int x = 12554;
    std::cout << 12554;
    for (int i = 0; i < 12554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12554(int& val) {
    const int x = 12554;
    std::cout << 12554;
    for (int i = 0; i < 12554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12555(int& val) {
    func_in_12555(val);
    const int x = 12555;
    std::cout << 12555;
    for (int i = 0; i < 12555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12555(int& val) {
    const int x = 12555;
    std::cout << 12555;
    for (int i = 0; i < 12555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12556(int& val) {
    func_in_12556(val);
    const int x = 12556;
    std::cout << 12556;
    for (int i = 0; i < 12556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12556(int& val) {
    const int x = 12556;
    std::cout << 12556;
    for (int i = 0; i < 12556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12557(int& val) {
    func_in_12557(val);
    const int x = 12557;
    std::cout << 12557;
    for (int i = 0; i < 12557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12557(int& val) {
    const int x = 12557;
    std::cout << 12557;
    for (int i = 0; i < 12557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12558(int& val) {
    func_in_12558(val);
    const int x = 12558;
    std::cout << 12558;
    for (int i = 0; i < 12558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12558(int& val) {
    const int x = 12558;
    std::cout << 12558;
    for (int i = 0; i < 12558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12559(int& val) {
    func_in_12559(val);
    const int x = 12559;
    std::cout << 12559;
    for (int i = 0; i < 12559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12559(int& val) {
    const int x = 12559;
    std::cout << 12559;
    for (int i = 0; i < 12559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12560(int& val) {
    func_in_12560(val);
    const int x = 12560;
    std::cout << 12560;
    for (int i = 0; i < 12560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12560(int& val) {
    const int x = 12560;
    std::cout << 12560;
    for (int i = 0; i < 12560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12561(int& val) {
    func_in_12561(val);
    const int x = 12561;
    std::cout << 12561;
    for (int i = 0; i < 12561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12561(int& val) {
    const int x = 12561;
    std::cout << 12561;
    for (int i = 0; i < 12561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12562(int& val) {
    func_in_12562(val);
    const int x = 12562;
    std::cout << 12562;
    for (int i = 0; i < 12562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12562(int& val) {
    const int x = 12562;
    std::cout << 12562;
    for (int i = 0; i < 12562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12563(int& val) {
    func_in_12563(val);
    const int x = 12563;
    std::cout << 12563;
    for (int i = 0; i < 12563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12563(int& val) {
    const int x = 12563;
    std::cout << 12563;
    for (int i = 0; i < 12563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12564(int& val) {
    func_in_12564(val);
    const int x = 12564;
    std::cout << 12564;
    for (int i = 0; i < 12564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12564(int& val) {
    const int x = 12564;
    std::cout << 12564;
    for (int i = 0; i < 12564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12565(int& val) {
    func_in_12565(val);
    const int x = 12565;
    std::cout << 12565;
    for (int i = 0; i < 12565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12565(int& val) {
    const int x = 12565;
    std::cout << 12565;
    for (int i = 0; i < 12565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12566(int& val) {
    func_in_12566(val);
    const int x = 12566;
    std::cout << 12566;
    for (int i = 0; i < 12566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12566(int& val) {
    const int x = 12566;
    std::cout << 12566;
    for (int i = 0; i < 12566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12567(int& val) {
    func_in_12567(val);
    const int x = 12567;
    std::cout << 12567;
    for (int i = 0; i < 12567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12567(int& val) {
    const int x = 12567;
    std::cout << 12567;
    for (int i = 0; i < 12567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12568(int& val) {
    func_in_12568(val);
    const int x = 12568;
    std::cout << 12568;
    for (int i = 0; i < 12568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12568(int& val) {
    const int x = 12568;
    std::cout << 12568;
    for (int i = 0; i < 12568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12569(int& val) {
    func_in_12569(val);
    const int x = 12569;
    std::cout << 12569;
    for (int i = 0; i < 12569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12569(int& val) {
    const int x = 12569;
    std::cout << 12569;
    for (int i = 0; i < 12569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12570(int& val) {
    func_in_12570(val);
    const int x = 12570;
    std::cout << 12570;
    for (int i = 0; i < 12570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12570(int& val) {
    const int x = 12570;
    std::cout << 12570;
    for (int i = 0; i < 12570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12571(int& val) {
    func_in_12571(val);
    const int x = 12571;
    std::cout << 12571;
    for (int i = 0; i < 12571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12571(int& val) {
    const int x = 12571;
    std::cout << 12571;
    for (int i = 0; i < 12571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12572(int& val) {
    func_in_12572(val);
    const int x = 12572;
    std::cout << 12572;
    for (int i = 0; i < 12572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12572(int& val) {
    const int x = 12572;
    std::cout << 12572;
    for (int i = 0; i < 12572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12573(int& val) {
    func_in_12573(val);
    const int x = 12573;
    std::cout << 12573;
    for (int i = 0; i < 12573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12573(int& val) {
    const int x = 12573;
    std::cout << 12573;
    for (int i = 0; i < 12573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12574(int& val) {
    func_in_12574(val);
    const int x = 12574;
    std::cout << 12574;
    for (int i = 0; i < 12574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12574(int& val) {
    const int x = 12574;
    std::cout << 12574;
    for (int i = 0; i < 12574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12575(int& val) {
    func_in_12575(val);
    const int x = 12575;
    std::cout << 12575;
    for (int i = 0; i < 12575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12575(int& val) {
    const int x = 12575;
    std::cout << 12575;
    for (int i = 0; i < 12575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12576(int& val) {
    func_in_12576(val);
    const int x = 12576;
    std::cout << 12576;
    for (int i = 0; i < 12576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12576(int& val) {
    const int x = 12576;
    std::cout << 12576;
    for (int i = 0; i < 12576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12577(int& val) {
    func_in_12577(val);
    const int x = 12577;
    std::cout << 12577;
    for (int i = 0; i < 12577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12577(int& val) {
    const int x = 12577;
    std::cout << 12577;
    for (int i = 0; i < 12577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12578(int& val) {
    func_in_12578(val);
    const int x = 12578;
    std::cout << 12578;
    for (int i = 0; i < 12578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12578(int& val) {
    const int x = 12578;
    std::cout << 12578;
    for (int i = 0; i < 12578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12579(int& val) {
    func_in_12579(val);
    const int x = 12579;
    std::cout << 12579;
    for (int i = 0; i < 12579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12579(int& val) {
    const int x = 12579;
    std::cout << 12579;
    for (int i = 0; i < 12579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12580(int& val) {
    func_in_12580(val);
    const int x = 12580;
    std::cout << 12580;
    for (int i = 0; i < 12580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12580(int& val) {
    const int x = 12580;
    std::cout << 12580;
    for (int i = 0; i < 12580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12581(int& val) {
    func_in_12581(val);
    const int x = 12581;
    std::cout << 12581;
    for (int i = 0; i < 12581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12581(int& val) {
    const int x = 12581;
    std::cout << 12581;
    for (int i = 0; i < 12581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12582(int& val) {
    func_in_12582(val);
    const int x = 12582;
    std::cout << 12582;
    for (int i = 0; i < 12582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12582(int& val) {
    const int x = 12582;
    std::cout << 12582;
    for (int i = 0; i < 12582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12583(int& val) {
    func_in_12583(val);
    const int x = 12583;
    std::cout << 12583;
    for (int i = 0; i < 12583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12583(int& val) {
    const int x = 12583;
    std::cout << 12583;
    for (int i = 0; i < 12583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12584(int& val) {
    func_in_12584(val);
    const int x = 12584;
    std::cout << 12584;
    for (int i = 0; i < 12584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12584(int& val) {
    const int x = 12584;
    std::cout << 12584;
    for (int i = 0; i < 12584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12585(int& val) {
    func_in_12585(val);
    const int x = 12585;
    std::cout << 12585;
    for (int i = 0; i < 12585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12585(int& val) {
    const int x = 12585;
    std::cout << 12585;
    for (int i = 0; i < 12585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12586(int& val) {
    func_in_12586(val);
    const int x = 12586;
    std::cout << 12586;
    for (int i = 0; i < 12586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12586(int& val) {
    const int x = 12586;
    std::cout << 12586;
    for (int i = 0; i < 12586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12587(int& val) {
    func_in_12587(val);
    const int x = 12587;
    std::cout << 12587;
    for (int i = 0; i < 12587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12587(int& val) {
    const int x = 12587;
    std::cout << 12587;
    for (int i = 0; i < 12587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12588(int& val) {
    func_in_12588(val);
    const int x = 12588;
    std::cout << 12588;
    for (int i = 0; i < 12588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12588(int& val) {
    const int x = 12588;
    std::cout << 12588;
    for (int i = 0; i < 12588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12589(int& val) {
    func_in_12589(val);
    const int x = 12589;
    std::cout << 12589;
    for (int i = 0; i < 12589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12589(int& val) {
    const int x = 12589;
    std::cout << 12589;
    for (int i = 0; i < 12589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12590(int& val) {
    func_in_12590(val);
    const int x = 12590;
    std::cout << 12590;
    for (int i = 0; i < 12590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12590(int& val) {
    const int x = 12590;
    std::cout << 12590;
    for (int i = 0; i < 12590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12591(int& val) {
    func_in_12591(val);
    const int x = 12591;
    std::cout << 12591;
    for (int i = 0; i < 12591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12591(int& val) {
    const int x = 12591;
    std::cout << 12591;
    for (int i = 0; i < 12591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12592(int& val) {
    func_in_12592(val);
    const int x = 12592;
    std::cout << 12592;
    for (int i = 0; i < 12592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12592(int& val) {
    const int x = 12592;
    std::cout << 12592;
    for (int i = 0; i < 12592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12593(int& val) {
    func_in_12593(val);
    const int x = 12593;
    std::cout << 12593;
    for (int i = 0; i < 12593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12593(int& val) {
    const int x = 12593;
    std::cout << 12593;
    for (int i = 0; i < 12593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12594(int& val) {
    func_in_12594(val);
    const int x = 12594;
    std::cout << 12594;
    for (int i = 0; i < 12594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12594(int& val) {
    const int x = 12594;
    std::cout << 12594;
    for (int i = 0; i < 12594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12595(int& val) {
    func_in_12595(val);
    const int x = 12595;
    std::cout << 12595;
    for (int i = 0; i < 12595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12595(int& val) {
    const int x = 12595;
    std::cout << 12595;
    for (int i = 0; i < 12595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12596(int& val) {
    func_in_12596(val);
    const int x = 12596;
    std::cout << 12596;
    for (int i = 0; i < 12596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12596(int& val) {
    const int x = 12596;
    std::cout << 12596;
    for (int i = 0; i < 12596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12597(int& val) {
    func_in_12597(val);
    const int x = 12597;
    std::cout << 12597;
    for (int i = 0; i < 12597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12597(int& val) {
    const int x = 12597;
    std::cout << 12597;
    for (int i = 0; i < 12597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12598(int& val) {
    func_in_12598(val);
    const int x = 12598;
    std::cout << 12598;
    for (int i = 0; i < 12598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12598(int& val) {
    const int x = 12598;
    std::cout << 12598;
    for (int i = 0; i < 12598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12599(int& val) {
    func_in_12599(val);
    const int x = 12599;
    std::cout << 12599;
    for (int i = 0; i < 12599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12599(int& val) {
    const int x = 12599;
    std::cout << 12599;
    for (int i = 0; i < 12599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12600(int& val) {
    func_in_12600(val);
    const int x = 12600;
    std::cout << 12600;
    for (int i = 0; i < 12600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12600(int& val) {
    const int x = 12600;
    std::cout << 12600;
    for (int i = 0; i < 12600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12601(int& val) {
    func_in_12601(val);
    const int x = 12601;
    std::cout << 12601;
    for (int i = 0; i < 12601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12601(int& val) {
    const int x = 12601;
    std::cout << 12601;
    for (int i = 0; i < 12601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12602(int& val) {
    func_in_12602(val);
    const int x = 12602;
    std::cout << 12602;
    for (int i = 0; i < 12602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12602(int& val) {
    const int x = 12602;
    std::cout << 12602;
    for (int i = 0; i < 12602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12603(int& val) {
    func_in_12603(val);
    const int x = 12603;
    std::cout << 12603;
    for (int i = 0; i < 12603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12603(int& val) {
    const int x = 12603;
    std::cout << 12603;
    for (int i = 0; i < 12603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12604(int& val) {
    func_in_12604(val);
    const int x = 12604;
    std::cout << 12604;
    for (int i = 0; i < 12604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12604(int& val) {
    const int x = 12604;
    std::cout << 12604;
    for (int i = 0; i < 12604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12605(int& val) {
    func_in_12605(val);
    const int x = 12605;
    std::cout << 12605;
    for (int i = 0; i < 12605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12605(int& val) {
    const int x = 12605;
    std::cout << 12605;
    for (int i = 0; i < 12605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12606(int& val) {
    func_in_12606(val);
    const int x = 12606;
    std::cout << 12606;
    for (int i = 0; i < 12606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12606(int& val) {
    const int x = 12606;
    std::cout << 12606;
    for (int i = 0; i < 12606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12607(int& val) {
    func_in_12607(val);
    const int x = 12607;
    std::cout << 12607;
    for (int i = 0; i < 12607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12607(int& val) {
    const int x = 12607;
    std::cout << 12607;
    for (int i = 0; i < 12607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12608(int& val) {
    func_in_12608(val);
    const int x = 12608;
    std::cout << 12608;
    for (int i = 0; i < 12608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12608(int& val) {
    const int x = 12608;
    std::cout << 12608;
    for (int i = 0; i < 12608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12609(int& val) {
    func_in_12609(val);
    const int x = 12609;
    std::cout << 12609;
    for (int i = 0; i < 12609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12609(int& val) {
    const int x = 12609;
    std::cout << 12609;
    for (int i = 0; i < 12609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12610(int& val) {
    func_in_12610(val);
    const int x = 12610;
    std::cout << 12610;
    for (int i = 0; i < 12610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12610(int& val) {
    const int x = 12610;
    std::cout << 12610;
    for (int i = 0; i < 12610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12611(int& val) {
    func_in_12611(val);
    const int x = 12611;
    std::cout << 12611;
    for (int i = 0; i < 12611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12611(int& val) {
    const int x = 12611;
    std::cout << 12611;
    for (int i = 0; i < 12611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12612(int& val) {
    func_in_12612(val);
    const int x = 12612;
    std::cout << 12612;
    for (int i = 0; i < 12612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12612(int& val) {
    const int x = 12612;
    std::cout << 12612;
    for (int i = 0; i < 12612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12613(int& val) {
    func_in_12613(val);
    const int x = 12613;
    std::cout << 12613;
    for (int i = 0; i < 12613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12613(int& val) {
    const int x = 12613;
    std::cout << 12613;
    for (int i = 0; i < 12613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12614(int& val) {
    func_in_12614(val);
    const int x = 12614;
    std::cout << 12614;
    for (int i = 0; i < 12614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12614(int& val) {
    const int x = 12614;
    std::cout << 12614;
    for (int i = 0; i < 12614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12615(int& val) {
    func_in_12615(val);
    const int x = 12615;
    std::cout << 12615;
    for (int i = 0; i < 12615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12615(int& val) {
    const int x = 12615;
    std::cout << 12615;
    for (int i = 0; i < 12615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12616(int& val) {
    func_in_12616(val);
    const int x = 12616;
    std::cout << 12616;
    for (int i = 0; i < 12616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12616(int& val) {
    const int x = 12616;
    std::cout << 12616;
    for (int i = 0; i < 12616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12617(int& val) {
    func_in_12617(val);
    const int x = 12617;
    std::cout << 12617;
    for (int i = 0; i < 12617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12617(int& val) {
    const int x = 12617;
    std::cout << 12617;
    for (int i = 0; i < 12617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12618(int& val) {
    func_in_12618(val);
    const int x = 12618;
    std::cout << 12618;
    for (int i = 0; i < 12618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12618(int& val) {
    const int x = 12618;
    std::cout << 12618;
    for (int i = 0; i < 12618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12619(int& val) {
    func_in_12619(val);
    const int x = 12619;
    std::cout << 12619;
    for (int i = 0; i < 12619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12619(int& val) {
    const int x = 12619;
    std::cout << 12619;
    for (int i = 0; i < 12619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12620(int& val) {
    func_in_12620(val);
    const int x = 12620;
    std::cout << 12620;
    for (int i = 0; i < 12620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12620(int& val) {
    const int x = 12620;
    std::cout << 12620;
    for (int i = 0; i < 12620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12621(int& val) {
    func_in_12621(val);
    const int x = 12621;
    std::cout << 12621;
    for (int i = 0; i < 12621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12621(int& val) {
    const int x = 12621;
    std::cout << 12621;
    for (int i = 0; i < 12621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12622(int& val) {
    func_in_12622(val);
    const int x = 12622;
    std::cout << 12622;
    for (int i = 0; i < 12622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12622(int& val) {
    const int x = 12622;
    std::cout << 12622;
    for (int i = 0; i < 12622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12623(int& val) {
    func_in_12623(val);
    const int x = 12623;
    std::cout << 12623;
    for (int i = 0; i < 12623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12623(int& val) {
    const int x = 12623;
    std::cout << 12623;
    for (int i = 0; i < 12623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12624(int& val) {
    func_in_12624(val);
    const int x = 12624;
    std::cout << 12624;
    for (int i = 0; i < 12624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12624(int& val) {
    const int x = 12624;
    std::cout << 12624;
    for (int i = 0; i < 12624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12625(int& val) {
    func_in_12625(val);
    const int x = 12625;
    std::cout << 12625;
    for (int i = 0; i < 12625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12625(int& val) {
    const int x = 12625;
    std::cout << 12625;
    for (int i = 0; i < 12625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12626(int& val) {
    func_in_12626(val);
    const int x = 12626;
    std::cout << 12626;
    for (int i = 0; i < 12626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12626(int& val) {
    const int x = 12626;
    std::cout << 12626;
    for (int i = 0; i < 12626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12627(int& val) {
    func_in_12627(val);
    const int x = 12627;
    std::cout << 12627;
    for (int i = 0; i < 12627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12627(int& val) {
    const int x = 12627;
    std::cout << 12627;
    for (int i = 0; i < 12627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12628(int& val) {
    func_in_12628(val);
    const int x = 12628;
    std::cout << 12628;
    for (int i = 0; i < 12628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12628(int& val) {
    const int x = 12628;
    std::cout << 12628;
    for (int i = 0; i < 12628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12629(int& val) {
    func_in_12629(val);
    const int x = 12629;
    std::cout << 12629;
    for (int i = 0; i < 12629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12629(int& val) {
    const int x = 12629;
    std::cout << 12629;
    for (int i = 0; i < 12629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12630(int& val) {
    func_in_12630(val);
    const int x = 12630;
    std::cout << 12630;
    for (int i = 0; i < 12630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12630(int& val) {
    const int x = 12630;
    std::cout << 12630;
    for (int i = 0; i < 12630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12631(int& val) {
    func_in_12631(val);
    const int x = 12631;
    std::cout << 12631;
    for (int i = 0; i < 12631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12631(int& val) {
    const int x = 12631;
    std::cout << 12631;
    for (int i = 0; i < 12631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12632(int& val) {
    func_in_12632(val);
    const int x = 12632;
    std::cout << 12632;
    for (int i = 0; i < 12632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12632(int& val) {
    const int x = 12632;
    std::cout << 12632;
    for (int i = 0; i < 12632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12633(int& val) {
    func_in_12633(val);
    const int x = 12633;
    std::cout << 12633;
    for (int i = 0; i < 12633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12633(int& val) {
    const int x = 12633;
    std::cout << 12633;
    for (int i = 0; i < 12633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12634(int& val) {
    func_in_12634(val);
    const int x = 12634;
    std::cout << 12634;
    for (int i = 0; i < 12634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12634(int& val) {
    const int x = 12634;
    std::cout << 12634;
    for (int i = 0; i < 12634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12635(int& val) {
    func_in_12635(val);
    const int x = 12635;
    std::cout << 12635;
    for (int i = 0; i < 12635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12635(int& val) {
    const int x = 12635;
    std::cout << 12635;
    for (int i = 0; i < 12635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12636(int& val) {
    func_in_12636(val);
    const int x = 12636;
    std::cout << 12636;
    for (int i = 0; i < 12636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12636(int& val) {
    const int x = 12636;
    std::cout << 12636;
    for (int i = 0; i < 12636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12637(int& val) {
    func_in_12637(val);
    const int x = 12637;
    std::cout << 12637;
    for (int i = 0; i < 12637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12637(int& val) {
    const int x = 12637;
    std::cout << 12637;
    for (int i = 0; i < 12637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12638(int& val) {
    func_in_12638(val);
    const int x = 12638;
    std::cout << 12638;
    for (int i = 0; i < 12638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12638(int& val) {
    const int x = 12638;
    std::cout << 12638;
    for (int i = 0; i < 12638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12639(int& val) {
    func_in_12639(val);
    const int x = 12639;
    std::cout << 12639;
    for (int i = 0; i < 12639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12639(int& val) {
    const int x = 12639;
    std::cout << 12639;
    for (int i = 0; i < 12639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12640(int& val) {
    func_in_12640(val);
    const int x = 12640;
    std::cout << 12640;
    for (int i = 0; i < 12640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12640(int& val) {
    const int x = 12640;
    std::cout << 12640;
    for (int i = 0; i < 12640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12641(int& val) {
    func_in_12641(val);
    const int x = 12641;
    std::cout << 12641;
    for (int i = 0; i < 12641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12641(int& val) {
    const int x = 12641;
    std::cout << 12641;
    for (int i = 0; i < 12641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12642(int& val) {
    func_in_12642(val);
    const int x = 12642;
    std::cout << 12642;
    for (int i = 0; i < 12642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12642(int& val) {
    const int x = 12642;
    std::cout << 12642;
    for (int i = 0; i < 12642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12643(int& val) {
    func_in_12643(val);
    const int x = 12643;
    std::cout << 12643;
    for (int i = 0; i < 12643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12643(int& val) {
    const int x = 12643;
    std::cout << 12643;
    for (int i = 0; i < 12643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12644(int& val) {
    func_in_12644(val);
    const int x = 12644;
    std::cout << 12644;
    for (int i = 0; i < 12644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12644(int& val) {
    const int x = 12644;
    std::cout << 12644;
    for (int i = 0; i < 12644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12645(int& val) {
    func_in_12645(val);
    const int x = 12645;
    std::cout << 12645;
    for (int i = 0; i < 12645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12645(int& val) {
    const int x = 12645;
    std::cout << 12645;
    for (int i = 0; i < 12645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12646(int& val) {
    func_in_12646(val);
    const int x = 12646;
    std::cout << 12646;
    for (int i = 0; i < 12646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12646(int& val) {
    const int x = 12646;
    std::cout << 12646;
    for (int i = 0; i < 12646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12647(int& val) {
    func_in_12647(val);
    const int x = 12647;
    std::cout << 12647;
    for (int i = 0; i < 12647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12647(int& val) {
    const int x = 12647;
    std::cout << 12647;
    for (int i = 0; i < 12647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12648(int& val) {
    func_in_12648(val);
    const int x = 12648;
    std::cout << 12648;
    for (int i = 0; i < 12648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12648(int& val) {
    const int x = 12648;
    std::cout << 12648;
    for (int i = 0; i < 12648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12649(int& val) {
    func_in_12649(val);
    const int x = 12649;
    std::cout << 12649;
    for (int i = 0; i < 12649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12649(int& val) {
    const int x = 12649;
    std::cout << 12649;
    for (int i = 0; i < 12649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12650(int& val) {
    func_in_12650(val);
    const int x = 12650;
    std::cout << 12650;
    for (int i = 0; i < 12650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12650(int& val) {
    const int x = 12650;
    std::cout << 12650;
    for (int i = 0; i < 12650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12651(int& val) {
    func_in_12651(val);
    const int x = 12651;
    std::cout << 12651;
    for (int i = 0; i < 12651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12651(int& val) {
    const int x = 12651;
    std::cout << 12651;
    for (int i = 0; i < 12651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12652(int& val) {
    func_in_12652(val);
    const int x = 12652;
    std::cout << 12652;
    for (int i = 0; i < 12652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12652(int& val) {
    const int x = 12652;
    std::cout << 12652;
    for (int i = 0; i < 12652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12653(int& val) {
    func_in_12653(val);
    const int x = 12653;
    std::cout << 12653;
    for (int i = 0; i < 12653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12653(int& val) {
    const int x = 12653;
    std::cout << 12653;
    for (int i = 0; i < 12653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12654(int& val) {
    func_in_12654(val);
    const int x = 12654;
    std::cout << 12654;
    for (int i = 0; i < 12654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12654(int& val) {
    const int x = 12654;
    std::cout << 12654;
    for (int i = 0; i < 12654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12655(int& val) {
    func_in_12655(val);
    const int x = 12655;
    std::cout << 12655;
    for (int i = 0; i < 12655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12655(int& val) {
    const int x = 12655;
    std::cout << 12655;
    for (int i = 0; i < 12655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12656(int& val) {
    func_in_12656(val);
    const int x = 12656;
    std::cout << 12656;
    for (int i = 0; i < 12656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12656(int& val) {
    const int x = 12656;
    std::cout << 12656;
    for (int i = 0; i < 12656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12657(int& val) {
    func_in_12657(val);
    const int x = 12657;
    std::cout << 12657;
    for (int i = 0; i < 12657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12657(int& val) {
    const int x = 12657;
    std::cout << 12657;
    for (int i = 0; i < 12657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12658(int& val) {
    func_in_12658(val);
    const int x = 12658;
    std::cout << 12658;
    for (int i = 0; i < 12658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12658(int& val) {
    const int x = 12658;
    std::cout << 12658;
    for (int i = 0; i < 12658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12659(int& val) {
    func_in_12659(val);
    const int x = 12659;
    std::cout << 12659;
    for (int i = 0; i < 12659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12659(int& val) {
    const int x = 12659;
    std::cout << 12659;
    for (int i = 0; i < 12659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12660(int& val) {
    func_in_12660(val);
    const int x = 12660;
    std::cout << 12660;
    for (int i = 0; i < 12660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12660(int& val) {
    const int x = 12660;
    std::cout << 12660;
    for (int i = 0; i < 12660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12661(int& val) {
    func_in_12661(val);
    const int x = 12661;
    std::cout << 12661;
    for (int i = 0; i < 12661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12661(int& val) {
    const int x = 12661;
    std::cout << 12661;
    for (int i = 0; i < 12661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12662(int& val) {
    func_in_12662(val);
    const int x = 12662;
    std::cout << 12662;
    for (int i = 0; i < 12662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12662(int& val) {
    const int x = 12662;
    std::cout << 12662;
    for (int i = 0; i < 12662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12663(int& val) {
    func_in_12663(val);
    const int x = 12663;
    std::cout << 12663;
    for (int i = 0; i < 12663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12663(int& val) {
    const int x = 12663;
    std::cout << 12663;
    for (int i = 0; i < 12663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12664(int& val) {
    func_in_12664(val);
    const int x = 12664;
    std::cout << 12664;
    for (int i = 0; i < 12664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12664(int& val) {
    const int x = 12664;
    std::cout << 12664;
    for (int i = 0; i < 12664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12665(int& val) {
    func_in_12665(val);
    const int x = 12665;
    std::cout << 12665;
    for (int i = 0; i < 12665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12665(int& val) {
    const int x = 12665;
    std::cout << 12665;
    for (int i = 0; i < 12665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12666(int& val) {
    func_in_12666(val);
    const int x = 12666;
    std::cout << 12666;
    for (int i = 0; i < 12666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12666(int& val) {
    const int x = 12666;
    std::cout << 12666;
    for (int i = 0; i < 12666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12667(int& val) {
    func_in_12667(val);
    const int x = 12667;
    std::cout << 12667;
    for (int i = 0; i < 12667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12667(int& val) {
    const int x = 12667;
    std::cout << 12667;
    for (int i = 0; i < 12667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12668(int& val) {
    func_in_12668(val);
    const int x = 12668;
    std::cout << 12668;
    for (int i = 0; i < 12668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12668(int& val) {
    const int x = 12668;
    std::cout << 12668;
    for (int i = 0; i < 12668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12669(int& val) {
    func_in_12669(val);
    const int x = 12669;
    std::cout << 12669;
    for (int i = 0; i < 12669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12669(int& val) {
    const int x = 12669;
    std::cout << 12669;
    for (int i = 0; i < 12669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12670(int& val) {
    func_in_12670(val);
    const int x = 12670;
    std::cout << 12670;
    for (int i = 0; i < 12670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12670(int& val) {
    const int x = 12670;
    std::cout << 12670;
    for (int i = 0; i < 12670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12671(int& val) {
    func_in_12671(val);
    const int x = 12671;
    std::cout << 12671;
    for (int i = 0; i < 12671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12671(int& val) {
    const int x = 12671;
    std::cout << 12671;
    for (int i = 0; i < 12671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12672(int& val) {
    func_in_12672(val);
    const int x = 12672;
    std::cout << 12672;
    for (int i = 0; i < 12672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12672(int& val) {
    const int x = 12672;
    std::cout << 12672;
    for (int i = 0; i < 12672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12673(int& val) {
    func_in_12673(val);
    const int x = 12673;
    std::cout << 12673;
    for (int i = 0; i < 12673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12673(int& val) {
    const int x = 12673;
    std::cout << 12673;
    for (int i = 0; i < 12673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12674(int& val) {
    func_in_12674(val);
    const int x = 12674;
    std::cout << 12674;
    for (int i = 0; i < 12674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12674(int& val) {
    const int x = 12674;
    std::cout << 12674;
    for (int i = 0; i < 12674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12675(int& val) {
    func_in_12675(val);
    const int x = 12675;
    std::cout << 12675;
    for (int i = 0; i < 12675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12675(int& val) {
    const int x = 12675;
    std::cout << 12675;
    for (int i = 0; i < 12675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12676(int& val) {
    func_in_12676(val);
    const int x = 12676;
    std::cout << 12676;
    for (int i = 0; i < 12676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12676(int& val) {
    const int x = 12676;
    std::cout << 12676;
    for (int i = 0; i < 12676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12677(int& val) {
    func_in_12677(val);
    const int x = 12677;
    std::cout << 12677;
    for (int i = 0; i < 12677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12677(int& val) {
    const int x = 12677;
    std::cout << 12677;
    for (int i = 0; i < 12677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12678(int& val) {
    func_in_12678(val);
    const int x = 12678;
    std::cout << 12678;
    for (int i = 0; i < 12678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12678(int& val) {
    const int x = 12678;
    std::cout << 12678;
    for (int i = 0; i < 12678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12679(int& val) {
    func_in_12679(val);
    const int x = 12679;
    std::cout << 12679;
    for (int i = 0; i < 12679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12679(int& val) {
    const int x = 12679;
    std::cout << 12679;
    for (int i = 0; i < 12679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12680(int& val) {
    func_in_12680(val);
    const int x = 12680;
    std::cout << 12680;
    for (int i = 0; i < 12680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12680(int& val) {
    const int x = 12680;
    std::cout << 12680;
    for (int i = 0; i < 12680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12681(int& val) {
    func_in_12681(val);
    const int x = 12681;
    std::cout << 12681;
    for (int i = 0; i < 12681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12681(int& val) {
    const int x = 12681;
    std::cout << 12681;
    for (int i = 0; i < 12681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12682(int& val) {
    func_in_12682(val);
    const int x = 12682;
    std::cout << 12682;
    for (int i = 0; i < 12682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12682(int& val) {
    const int x = 12682;
    std::cout << 12682;
    for (int i = 0; i < 12682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12683(int& val) {
    func_in_12683(val);
    const int x = 12683;
    std::cout << 12683;
    for (int i = 0; i < 12683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12683(int& val) {
    const int x = 12683;
    std::cout << 12683;
    for (int i = 0; i < 12683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12684(int& val) {
    func_in_12684(val);
    const int x = 12684;
    std::cout << 12684;
    for (int i = 0; i < 12684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12684(int& val) {
    const int x = 12684;
    std::cout << 12684;
    for (int i = 0; i < 12684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12685(int& val) {
    func_in_12685(val);
    const int x = 12685;
    std::cout << 12685;
    for (int i = 0; i < 12685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12685(int& val) {
    const int x = 12685;
    std::cout << 12685;
    for (int i = 0; i < 12685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12686(int& val) {
    func_in_12686(val);
    const int x = 12686;
    std::cout << 12686;
    for (int i = 0; i < 12686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12686(int& val) {
    const int x = 12686;
    std::cout << 12686;
    for (int i = 0; i < 12686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12687(int& val) {
    func_in_12687(val);
    const int x = 12687;
    std::cout << 12687;
    for (int i = 0; i < 12687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12687(int& val) {
    const int x = 12687;
    std::cout << 12687;
    for (int i = 0; i < 12687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12688(int& val) {
    func_in_12688(val);
    const int x = 12688;
    std::cout << 12688;
    for (int i = 0; i < 12688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12688(int& val) {
    const int x = 12688;
    std::cout << 12688;
    for (int i = 0; i < 12688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12689(int& val) {
    func_in_12689(val);
    const int x = 12689;
    std::cout << 12689;
    for (int i = 0; i < 12689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12689(int& val) {
    const int x = 12689;
    std::cout << 12689;
    for (int i = 0; i < 12689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12690(int& val) {
    func_in_12690(val);
    const int x = 12690;
    std::cout << 12690;
    for (int i = 0; i < 12690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12690(int& val) {
    const int x = 12690;
    std::cout << 12690;
    for (int i = 0; i < 12690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12691(int& val) {
    func_in_12691(val);
    const int x = 12691;
    std::cout << 12691;
    for (int i = 0; i < 12691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12691(int& val) {
    const int x = 12691;
    std::cout << 12691;
    for (int i = 0; i < 12691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12692(int& val) {
    func_in_12692(val);
    const int x = 12692;
    std::cout << 12692;
    for (int i = 0; i < 12692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12692(int& val) {
    const int x = 12692;
    std::cout << 12692;
    for (int i = 0; i < 12692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12693(int& val) {
    func_in_12693(val);
    const int x = 12693;
    std::cout << 12693;
    for (int i = 0; i < 12693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12693(int& val) {
    const int x = 12693;
    std::cout << 12693;
    for (int i = 0; i < 12693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12694(int& val) {
    func_in_12694(val);
    const int x = 12694;
    std::cout << 12694;
    for (int i = 0; i < 12694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12694(int& val) {
    const int x = 12694;
    std::cout << 12694;
    for (int i = 0; i < 12694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12695(int& val) {
    func_in_12695(val);
    const int x = 12695;
    std::cout << 12695;
    for (int i = 0; i < 12695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12695(int& val) {
    const int x = 12695;
    std::cout << 12695;
    for (int i = 0; i < 12695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12696(int& val) {
    func_in_12696(val);
    const int x = 12696;
    std::cout << 12696;
    for (int i = 0; i < 12696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12696(int& val) {
    const int x = 12696;
    std::cout << 12696;
    for (int i = 0; i < 12696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12697(int& val) {
    func_in_12697(val);
    const int x = 12697;
    std::cout << 12697;
    for (int i = 0; i < 12697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12697(int& val) {
    const int x = 12697;
    std::cout << 12697;
    for (int i = 0; i < 12697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12698(int& val) {
    func_in_12698(val);
    const int x = 12698;
    std::cout << 12698;
    for (int i = 0; i < 12698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12698(int& val) {
    const int x = 12698;
    std::cout << 12698;
    for (int i = 0; i < 12698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12699(int& val) {
    func_in_12699(val);
    const int x = 12699;
    std::cout << 12699;
    for (int i = 0; i < 12699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12699(int& val) {
    const int x = 12699;
    std::cout << 12699;
    for (int i = 0; i < 12699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12700(int& val) {
    func_in_12700(val);
    const int x = 12700;
    std::cout << 12700;
    for (int i = 0; i < 12700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12700(int& val) {
    const int x = 12700;
    std::cout << 12700;
    for (int i = 0; i < 12700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12701(int& val) {
    func_in_12701(val);
    const int x = 12701;
    std::cout << 12701;
    for (int i = 0; i < 12701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12701(int& val) {
    const int x = 12701;
    std::cout << 12701;
    for (int i = 0; i < 12701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12702(int& val) {
    func_in_12702(val);
    const int x = 12702;
    std::cout << 12702;
    for (int i = 0; i < 12702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12702(int& val) {
    const int x = 12702;
    std::cout << 12702;
    for (int i = 0; i < 12702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12703(int& val) {
    func_in_12703(val);
    const int x = 12703;
    std::cout << 12703;
    for (int i = 0; i < 12703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12703(int& val) {
    const int x = 12703;
    std::cout << 12703;
    for (int i = 0; i < 12703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12704(int& val) {
    func_in_12704(val);
    const int x = 12704;
    std::cout << 12704;
    for (int i = 0; i < 12704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12704(int& val) {
    const int x = 12704;
    std::cout << 12704;
    for (int i = 0; i < 12704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12705(int& val) {
    func_in_12705(val);
    const int x = 12705;
    std::cout << 12705;
    for (int i = 0; i < 12705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12705(int& val) {
    const int x = 12705;
    std::cout << 12705;
    for (int i = 0; i < 12705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12706(int& val) {
    func_in_12706(val);
    const int x = 12706;
    std::cout << 12706;
    for (int i = 0; i < 12706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12706(int& val) {
    const int x = 12706;
    std::cout << 12706;
    for (int i = 0; i < 12706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12707(int& val) {
    func_in_12707(val);
    const int x = 12707;
    std::cout << 12707;
    for (int i = 0; i < 12707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12707(int& val) {
    const int x = 12707;
    std::cout << 12707;
    for (int i = 0; i < 12707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12708(int& val) {
    func_in_12708(val);
    const int x = 12708;
    std::cout << 12708;
    for (int i = 0; i < 12708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12708(int& val) {
    const int x = 12708;
    std::cout << 12708;
    for (int i = 0; i < 12708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12709(int& val) {
    func_in_12709(val);
    const int x = 12709;
    std::cout << 12709;
    for (int i = 0; i < 12709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12709(int& val) {
    const int x = 12709;
    std::cout << 12709;
    for (int i = 0; i < 12709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12710(int& val) {
    func_in_12710(val);
    const int x = 12710;
    std::cout << 12710;
    for (int i = 0; i < 12710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12710(int& val) {
    const int x = 12710;
    std::cout << 12710;
    for (int i = 0; i < 12710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12711(int& val) {
    func_in_12711(val);
    const int x = 12711;
    std::cout << 12711;
    for (int i = 0; i < 12711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12711(int& val) {
    const int x = 12711;
    std::cout << 12711;
    for (int i = 0; i < 12711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12712(int& val) {
    func_in_12712(val);
    const int x = 12712;
    std::cout << 12712;
    for (int i = 0; i < 12712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12712(int& val) {
    const int x = 12712;
    std::cout << 12712;
    for (int i = 0; i < 12712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12713(int& val) {
    func_in_12713(val);
    const int x = 12713;
    std::cout << 12713;
    for (int i = 0; i < 12713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12713(int& val) {
    const int x = 12713;
    std::cout << 12713;
    for (int i = 0; i < 12713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12714(int& val) {
    func_in_12714(val);
    const int x = 12714;
    std::cout << 12714;
    for (int i = 0; i < 12714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12714(int& val) {
    const int x = 12714;
    std::cout << 12714;
    for (int i = 0; i < 12714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12715(int& val) {
    func_in_12715(val);
    const int x = 12715;
    std::cout << 12715;
    for (int i = 0; i < 12715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12715(int& val) {
    const int x = 12715;
    std::cout << 12715;
    for (int i = 0; i < 12715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12716(int& val) {
    func_in_12716(val);
    const int x = 12716;
    std::cout << 12716;
    for (int i = 0; i < 12716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12716(int& val) {
    const int x = 12716;
    std::cout << 12716;
    for (int i = 0; i < 12716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12717(int& val) {
    func_in_12717(val);
    const int x = 12717;
    std::cout << 12717;
    for (int i = 0; i < 12717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12717(int& val) {
    const int x = 12717;
    std::cout << 12717;
    for (int i = 0; i < 12717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12718(int& val) {
    func_in_12718(val);
    const int x = 12718;
    std::cout << 12718;
    for (int i = 0; i < 12718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12718(int& val) {
    const int x = 12718;
    std::cout << 12718;
    for (int i = 0; i < 12718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12719(int& val) {
    func_in_12719(val);
    const int x = 12719;
    std::cout << 12719;
    for (int i = 0; i < 12719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12719(int& val) {
    const int x = 12719;
    std::cout << 12719;
    for (int i = 0; i < 12719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12720(int& val) {
    func_in_12720(val);
    const int x = 12720;
    std::cout << 12720;
    for (int i = 0; i < 12720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12720(int& val) {
    const int x = 12720;
    std::cout << 12720;
    for (int i = 0; i < 12720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12721(int& val) {
    func_in_12721(val);
    const int x = 12721;
    std::cout << 12721;
    for (int i = 0; i < 12721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12721(int& val) {
    const int x = 12721;
    std::cout << 12721;
    for (int i = 0; i < 12721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12722(int& val) {
    func_in_12722(val);
    const int x = 12722;
    std::cout << 12722;
    for (int i = 0; i < 12722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12722(int& val) {
    const int x = 12722;
    std::cout << 12722;
    for (int i = 0; i < 12722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12723(int& val) {
    func_in_12723(val);
    const int x = 12723;
    std::cout << 12723;
    for (int i = 0; i < 12723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12723(int& val) {
    const int x = 12723;
    std::cout << 12723;
    for (int i = 0; i < 12723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12724(int& val) {
    func_in_12724(val);
    const int x = 12724;
    std::cout << 12724;
    for (int i = 0; i < 12724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12724(int& val) {
    const int x = 12724;
    std::cout << 12724;
    for (int i = 0; i < 12724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12725(int& val) {
    func_in_12725(val);
    const int x = 12725;
    std::cout << 12725;
    for (int i = 0; i < 12725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12725(int& val) {
    const int x = 12725;
    std::cout << 12725;
    for (int i = 0; i < 12725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12726(int& val) {
    func_in_12726(val);
    const int x = 12726;
    std::cout << 12726;
    for (int i = 0; i < 12726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12726(int& val) {
    const int x = 12726;
    std::cout << 12726;
    for (int i = 0; i < 12726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12727(int& val) {
    func_in_12727(val);
    const int x = 12727;
    std::cout << 12727;
    for (int i = 0; i < 12727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12727(int& val) {
    const int x = 12727;
    std::cout << 12727;
    for (int i = 0; i < 12727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12728(int& val) {
    func_in_12728(val);
    const int x = 12728;
    std::cout << 12728;
    for (int i = 0; i < 12728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12728(int& val) {
    const int x = 12728;
    std::cout << 12728;
    for (int i = 0; i < 12728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12729(int& val) {
    func_in_12729(val);
    const int x = 12729;
    std::cout << 12729;
    for (int i = 0; i < 12729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12729(int& val) {
    const int x = 12729;
    std::cout << 12729;
    for (int i = 0; i < 12729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12730(int& val) {
    func_in_12730(val);
    const int x = 12730;
    std::cout << 12730;
    for (int i = 0; i < 12730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12730(int& val) {
    const int x = 12730;
    std::cout << 12730;
    for (int i = 0; i < 12730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12731(int& val) {
    func_in_12731(val);
    const int x = 12731;
    std::cout << 12731;
    for (int i = 0; i < 12731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12731(int& val) {
    const int x = 12731;
    std::cout << 12731;
    for (int i = 0; i < 12731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12732(int& val) {
    func_in_12732(val);
    const int x = 12732;
    std::cout << 12732;
    for (int i = 0; i < 12732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12732(int& val) {
    const int x = 12732;
    std::cout << 12732;
    for (int i = 0; i < 12732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12733(int& val) {
    func_in_12733(val);
    const int x = 12733;
    std::cout << 12733;
    for (int i = 0; i < 12733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12733(int& val) {
    const int x = 12733;
    std::cout << 12733;
    for (int i = 0; i < 12733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12734(int& val) {
    func_in_12734(val);
    const int x = 12734;
    std::cout << 12734;
    for (int i = 0; i < 12734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12734(int& val) {
    const int x = 12734;
    std::cout << 12734;
    for (int i = 0; i < 12734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12735(int& val) {
    func_in_12735(val);
    const int x = 12735;
    std::cout << 12735;
    for (int i = 0; i < 12735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12735(int& val) {
    const int x = 12735;
    std::cout << 12735;
    for (int i = 0; i < 12735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12736(int& val) {
    func_in_12736(val);
    const int x = 12736;
    std::cout << 12736;
    for (int i = 0; i < 12736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12736(int& val) {
    const int x = 12736;
    std::cout << 12736;
    for (int i = 0; i < 12736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12737(int& val) {
    func_in_12737(val);
    const int x = 12737;
    std::cout << 12737;
    for (int i = 0; i < 12737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12737(int& val) {
    const int x = 12737;
    std::cout << 12737;
    for (int i = 0; i < 12737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12738(int& val) {
    func_in_12738(val);
    const int x = 12738;
    std::cout << 12738;
    for (int i = 0; i < 12738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12738(int& val) {
    const int x = 12738;
    std::cout << 12738;
    for (int i = 0; i < 12738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12739(int& val) {
    func_in_12739(val);
    const int x = 12739;
    std::cout << 12739;
    for (int i = 0; i < 12739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12739(int& val) {
    const int x = 12739;
    std::cout << 12739;
    for (int i = 0; i < 12739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12740(int& val) {
    func_in_12740(val);
    const int x = 12740;
    std::cout << 12740;
    for (int i = 0; i < 12740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12740(int& val) {
    const int x = 12740;
    std::cout << 12740;
    for (int i = 0; i < 12740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12741(int& val) {
    func_in_12741(val);
    const int x = 12741;
    std::cout << 12741;
    for (int i = 0; i < 12741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12741(int& val) {
    const int x = 12741;
    std::cout << 12741;
    for (int i = 0; i < 12741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12742(int& val) {
    func_in_12742(val);
    const int x = 12742;
    std::cout << 12742;
    for (int i = 0; i < 12742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12742(int& val) {
    const int x = 12742;
    std::cout << 12742;
    for (int i = 0; i < 12742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12743(int& val) {
    func_in_12743(val);
    const int x = 12743;
    std::cout << 12743;
    for (int i = 0; i < 12743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12743(int& val) {
    const int x = 12743;
    std::cout << 12743;
    for (int i = 0; i < 12743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12744(int& val) {
    func_in_12744(val);
    const int x = 12744;
    std::cout << 12744;
    for (int i = 0; i < 12744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12744(int& val) {
    const int x = 12744;
    std::cout << 12744;
    for (int i = 0; i < 12744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12745(int& val) {
    func_in_12745(val);
    const int x = 12745;
    std::cout << 12745;
    for (int i = 0; i < 12745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12745(int& val) {
    const int x = 12745;
    std::cout << 12745;
    for (int i = 0; i < 12745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12746(int& val) {
    func_in_12746(val);
    const int x = 12746;
    std::cout << 12746;
    for (int i = 0; i < 12746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12746(int& val) {
    const int x = 12746;
    std::cout << 12746;
    for (int i = 0; i < 12746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12747(int& val) {
    func_in_12747(val);
    const int x = 12747;
    std::cout << 12747;
    for (int i = 0; i < 12747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12747(int& val) {
    const int x = 12747;
    std::cout << 12747;
    for (int i = 0; i < 12747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12748(int& val) {
    func_in_12748(val);
    const int x = 12748;
    std::cout << 12748;
    for (int i = 0; i < 12748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12748(int& val) {
    const int x = 12748;
    std::cout << 12748;
    for (int i = 0; i < 12748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12749(int& val) {
    func_in_12749(val);
    const int x = 12749;
    std::cout << 12749;
    for (int i = 0; i < 12749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12749(int& val) {
    const int x = 12749;
    std::cout << 12749;
    for (int i = 0; i < 12749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12750(int& val) {
    func_in_12750(val);
    const int x = 12750;
    std::cout << 12750;
    for (int i = 0; i < 12750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12750(int& val) {
    const int x = 12750;
    std::cout << 12750;
    for (int i = 0; i < 12750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12751(int& val) {
    func_in_12751(val);
    const int x = 12751;
    std::cout << 12751;
    for (int i = 0; i < 12751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12751(int& val) {
    const int x = 12751;
    std::cout << 12751;
    for (int i = 0; i < 12751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12752(int& val) {
    func_in_12752(val);
    const int x = 12752;
    std::cout << 12752;
    for (int i = 0; i < 12752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12752(int& val) {
    const int x = 12752;
    std::cout << 12752;
    for (int i = 0; i < 12752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12753(int& val) {
    func_in_12753(val);
    const int x = 12753;
    std::cout << 12753;
    for (int i = 0; i < 12753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12753(int& val) {
    const int x = 12753;
    std::cout << 12753;
    for (int i = 0; i < 12753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12754(int& val) {
    func_in_12754(val);
    const int x = 12754;
    std::cout << 12754;
    for (int i = 0; i < 12754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12754(int& val) {
    const int x = 12754;
    std::cout << 12754;
    for (int i = 0; i < 12754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12755(int& val) {
    func_in_12755(val);
    const int x = 12755;
    std::cout << 12755;
    for (int i = 0; i < 12755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12755(int& val) {
    const int x = 12755;
    std::cout << 12755;
    for (int i = 0; i < 12755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12756(int& val) {
    func_in_12756(val);
    const int x = 12756;
    std::cout << 12756;
    for (int i = 0; i < 12756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12756(int& val) {
    const int x = 12756;
    std::cout << 12756;
    for (int i = 0; i < 12756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12757(int& val) {
    func_in_12757(val);
    const int x = 12757;
    std::cout << 12757;
    for (int i = 0; i < 12757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12757(int& val) {
    const int x = 12757;
    std::cout << 12757;
    for (int i = 0; i < 12757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12758(int& val) {
    func_in_12758(val);
    const int x = 12758;
    std::cout << 12758;
    for (int i = 0; i < 12758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12758(int& val) {
    const int x = 12758;
    std::cout << 12758;
    for (int i = 0; i < 12758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12759(int& val) {
    func_in_12759(val);
    const int x = 12759;
    std::cout << 12759;
    for (int i = 0; i < 12759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12759(int& val) {
    const int x = 12759;
    std::cout << 12759;
    for (int i = 0; i < 12759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12760(int& val) {
    func_in_12760(val);
    const int x = 12760;
    std::cout << 12760;
    for (int i = 0; i < 12760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12760(int& val) {
    const int x = 12760;
    std::cout << 12760;
    for (int i = 0; i < 12760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12761(int& val) {
    func_in_12761(val);
    const int x = 12761;
    std::cout << 12761;
    for (int i = 0; i < 12761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12761(int& val) {
    const int x = 12761;
    std::cout << 12761;
    for (int i = 0; i < 12761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12762(int& val) {
    func_in_12762(val);
    const int x = 12762;
    std::cout << 12762;
    for (int i = 0; i < 12762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12762(int& val) {
    const int x = 12762;
    std::cout << 12762;
    for (int i = 0; i < 12762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12763(int& val) {
    func_in_12763(val);
    const int x = 12763;
    std::cout << 12763;
    for (int i = 0; i < 12763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12763(int& val) {
    const int x = 12763;
    std::cout << 12763;
    for (int i = 0; i < 12763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12764(int& val) {
    func_in_12764(val);
    const int x = 12764;
    std::cout << 12764;
    for (int i = 0; i < 12764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12764(int& val) {
    const int x = 12764;
    std::cout << 12764;
    for (int i = 0; i < 12764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12765(int& val) {
    func_in_12765(val);
    const int x = 12765;
    std::cout << 12765;
    for (int i = 0; i < 12765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12765(int& val) {
    const int x = 12765;
    std::cout << 12765;
    for (int i = 0; i < 12765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12766(int& val) {
    func_in_12766(val);
    const int x = 12766;
    std::cout << 12766;
    for (int i = 0; i < 12766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12766(int& val) {
    const int x = 12766;
    std::cout << 12766;
    for (int i = 0; i < 12766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12767(int& val) {
    func_in_12767(val);
    const int x = 12767;
    std::cout << 12767;
    for (int i = 0; i < 12767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12767(int& val) {
    const int x = 12767;
    std::cout << 12767;
    for (int i = 0; i < 12767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12768(int& val) {
    func_in_12768(val);
    const int x = 12768;
    std::cout << 12768;
    for (int i = 0; i < 12768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12768(int& val) {
    const int x = 12768;
    std::cout << 12768;
    for (int i = 0; i < 12768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12769(int& val) {
    func_in_12769(val);
    const int x = 12769;
    std::cout << 12769;
    for (int i = 0; i < 12769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12769(int& val) {
    const int x = 12769;
    std::cout << 12769;
    for (int i = 0; i < 12769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12770(int& val) {
    func_in_12770(val);
    const int x = 12770;
    std::cout << 12770;
    for (int i = 0; i < 12770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12770(int& val) {
    const int x = 12770;
    std::cout << 12770;
    for (int i = 0; i < 12770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12771(int& val) {
    func_in_12771(val);
    const int x = 12771;
    std::cout << 12771;
    for (int i = 0; i < 12771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12771(int& val) {
    const int x = 12771;
    std::cout << 12771;
    for (int i = 0; i < 12771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12772(int& val) {
    func_in_12772(val);
    const int x = 12772;
    std::cout << 12772;
    for (int i = 0; i < 12772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12772(int& val) {
    const int x = 12772;
    std::cout << 12772;
    for (int i = 0; i < 12772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12773(int& val) {
    func_in_12773(val);
    const int x = 12773;
    std::cout << 12773;
    for (int i = 0; i < 12773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12773(int& val) {
    const int x = 12773;
    std::cout << 12773;
    for (int i = 0; i < 12773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12774(int& val) {
    func_in_12774(val);
    const int x = 12774;
    std::cout << 12774;
    for (int i = 0; i < 12774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12774(int& val) {
    const int x = 12774;
    std::cout << 12774;
    for (int i = 0; i < 12774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12775(int& val) {
    func_in_12775(val);
    const int x = 12775;
    std::cout << 12775;
    for (int i = 0; i < 12775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12775(int& val) {
    const int x = 12775;
    std::cout << 12775;
    for (int i = 0; i < 12775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12776(int& val) {
    func_in_12776(val);
    const int x = 12776;
    std::cout << 12776;
    for (int i = 0; i < 12776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12776(int& val) {
    const int x = 12776;
    std::cout << 12776;
    for (int i = 0; i < 12776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12777(int& val) {
    func_in_12777(val);
    const int x = 12777;
    std::cout << 12777;
    for (int i = 0; i < 12777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12777(int& val) {
    const int x = 12777;
    std::cout << 12777;
    for (int i = 0; i < 12777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12778(int& val) {
    func_in_12778(val);
    const int x = 12778;
    std::cout << 12778;
    for (int i = 0; i < 12778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12778(int& val) {
    const int x = 12778;
    std::cout << 12778;
    for (int i = 0; i < 12778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12779(int& val) {
    func_in_12779(val);
    const int x = 12779;
    std::cout << 12779;
    for (int i = 0; i < 12779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12779(int& val) {
    const int x = 12779;
    std::cout << 12779;
    for (int i = 0; i < 12779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12780(int& val) {
    func_in_12780(val);
    const int x = 12780;
    std::cout << 12780;
    for (int i = 0; i < 12780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12780(int& val) {
    const int x = 12780;
    std::cout << 12780;
    for (int i = 0; i < 12780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12781(int& val) {
    func_in_12781(val);
    const int x = 12781;
    std::cout << 12781;
    for (int i = 0; i < 12781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12781(int& val) {
    const int x = 12781;
    std::cout << 12781;
    for (int i = 0; i < 12781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12782(int& val) {
    func_in_12782(val);
    const int x = 12782;
    std::cout << 12782;
    for (int i = 0; i < 12782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12782(int& val) {
    const int x = 12782;
    std::cout << 12782;
    for (int i = 0; i < 12782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12783(int& val) {
    func_in_12783(val);
    const int x = 12783;
    std::cout << 12783;
    for (int i = 0; i < 12783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12783(int& val) {
    const int x = 12783;
    std::cout << 12783;
    for (int i = 0; i < 12783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12784(int& val) {
    func_in_12784(val);
    const int x = 12784;
    std::cout << 12784;
    for (int i = 0; i < 12784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12784(int& val) {
    const int x = 12784;
    std::cout << 12784;
    for (int i = 0; i < 12784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12785(int& val) {
    func_in_12785(val);
    const int x = 12785;
    std::cout << 12785;
    for (int i = 0; i < 12785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12785(int& val) {
    const int x = 12785;
    std::cout << 12785;
    for (int i = 0; i < 12785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12786(int& val) {
    func_in_12786(val);
    const int x = 12786;
    std::cout << 12786;
    for (int i = 0; i < 12786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12786(int& val) {
    const int x = 12786;
    std::cout << 12786;
    for (int i = 0; i < 12786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12787(int& val) {
    func_in_12787(val);
    const int x = 12787;
    std::cout << 12787;
    for (int i = 0; i < 12787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12787(int& val) {
    const int x = 12787;
    std::cout << 12787;
    for (int i = 0; i < 12787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12788(int& val) {
    func_in_12788(val);
    const int x = 12788;
    std::cout << 12788;
    for (int i = 0; i < 12788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12788(int& val) {
    const int x = 12788;
    std::cout << 12788;
    for (int i = 0; i < 12788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12789(int& val) {
    func_in_12789(val);
    const int x = 12789;
    std::cout << 12789;
    for (int i = 0; i < 12789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12789(int& val) {
    const int x = 12789;
    std::cout << 12789;
    for (int i = 0; i < 12789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12790(int& val) {
    func_in_12790(val);
    const int x = 12790;
    std::cout << 12790;
    for (int i = 0; i < 12790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12790(int& val) {
    const int x = 12790;
    std::cout << 12790;
    for (int i = 0; i < 12790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12791(int& val) {
    func_in_12791(val);
    const int x = 12791;
    std::cout << 12791;
    for (int i = 0; i < 12791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12791(int& val) {
    const int x = 12791;
    std::cout << 12791;
    for (int i = 0; i < 12791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12792(int& val) {
    func_in_12792(val);
    const int x = 12792;
    std::cout << 12792;
    for (int i = 0; i < 12792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12792(int& val) {
    const int x = 12792;
    std::cout << 12792;
    for (int i = 0; i < 12792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12793(int& val) {
    func_in_12793(val);
    const int x = 12793;
    std::cout << 12793;
    for (int i = 0; i < 12793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12793(int& val) {
    const int x = 12793;
    std::cout << 12793;
    for (int i = 0; i < 12793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12794(int& val) {
    func_in_12794(val);
    const int x = 12794;
    std::cout << 12794;
    for (int i = 0; i < 12794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12794(int& val) {
    const int x = 12794;
    std::cout << 12794;
    for (int i = 0; i < 12794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12795(int& val) {
    func_in_12795(val);
    const int x = 12795;
    std::cout << 12795;
    for (int i = 0; i < 12795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12795(int& val) {
    const int x = 12795;
    std::cout << 12795;
    for (int i = 0; i < 12795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12796(int& val) {
    func_in_12796(val);
    const int x = 12796;
    std::cout << 12796;
    for (int i = 0; i < 12796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12796(int& val) {
    const int x = 12796;
    std::cout << 12796;
    for (int i = 0; i < 12796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12797(int& val) {
    func_in_12797(val);
    const int x = 12797;
    std::cout << 12797;
    for (int i = 0; i < 12797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12797(int& val) {
    const int x = 12797;
    std::cout << 12797;
    for (int i = 0; i < 12797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12798(int& val) {
    func_in_12798(val);
    const int x = 12798;
    std::cout << 12798;
    for (int i = 0; i < 12798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12798(int& val) {
    const int x = 12798;
    std::cout << 12798;
    for (int i = 0; i < 12798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12799(int& val) {
    func_in_12799(val);
    const int x = 12799;
    std::cout << 12799;
    for (int i = 0; i < 12799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12799(int& val) {
    const int x = 12799;
    std::cout << 12799;
    for (int i = 0; i < 12799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12800(int& val) {
    func_in_12800(val);
    const int x = 12800;
    std::cout << 12800;
    for (int i = 0; i < 12800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12800(int& val) {
    const int x = 12800;
    std::cout << 12800;
    for (int i = 0; i < 12800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12801(int& val) {
    func_in_12801(val);
    const int x = 12801;
    std::cout << 12801;
    for (int i = 0; i < 12801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12801(int& val) {
    const int x = 12801;
    std::cout << 12801;
    for (int i = 0; i < 12801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12802(int& val) {
    func_in_12802(val);
    const int x = 12802;
    std::cout << 12802;
    for (int i = 0; i < 12802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12802(int& val) {
    const int x = 12802;
    std::cout << 12802;
    for (int i = 0; i < 12802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12803(int& val) {
    func_in_12803(val);
    const int x = 12803;
    std::cout << 12803;
    for (int i = 0; i < 12803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12803(int& val) {
    const int x = 12803;
    std::cout << 12803;
    for (int i = 0; i < 12803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12804(int& val) {
    func_in_12804(val);
    const int x = 12804;
    std::cout << 12804;
    for (int i = 0; i < 12804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12804(int& val) {
    const int x = 12804;
    std::cout << 12804;
    for (int i = 0; i < 12804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12805(int& val) {
    func_in_12805(val);
    const int x = 12805;
    std::cout << 12805;
    for (int i = 0; i < 12805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12805(int& val) {
    const int x = 12805;
    std::cout << 12805;
    for (int i = 0; i < 12805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12806(int& val) {
    func_in_12806(val);
    const int x = 12806;
    std::cout << 12806;
    for (int i = 0; i < 12806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12806(int& val) {
    const int x = 12806;
    std::cout << 12806;
    for (int i = 0; i < 12806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12807(int& val) {
    func_in_12807(val);
    const int x = 12807;
    std::cout << 12807;
    for (int i = 0; i < 12807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12807(int& val) {
    const int x = 12807;
    std::cout << 12807;
    for (int i = 0; i < 12807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12808(int& val) {
    func_in_12808(val);
    const int x = 12808;
    std::cout << 12808;
    for (int i = 0; i < 12808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12808(int& val) {
    const int x = 12808;
    std::cout << 12808;
    for (int i = 0; i < 12808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12809(int& val) {
    func_in_12809(val);
    const int x = 12809;
    std::cout << 12809;
    for (int i = 0; i < 12809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12809(int& val) {
    const int x = 12809;
    std::cout << 12809;
    for (int i = 0; i < 12809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12810(int& val) {
    func_in_12810(val);
    const int x = 12810;
    std::cout << 12810;
    for (int i = 0; i < 12810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12810(int& val) {
    const int x = 12810;
    std::cout << 12810;
    for (int i = 0; i < 12810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12811(int& val) {
    func_in_12811(val);
    const int x = 12811;
    std::cout << 12811;
    for (int i = 0; i < 12811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12811(int& val) {
    const int x = 12811;
    std::cout << 12811;
    for (int i = 0; i < 12811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12812(int& val) {
    func_in_12812(val);
    const int x = 12812;
    std::cout << 12812;
    for (int i = 0; i < 12812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12812(int& val) {
    const int x = 12812;
    std::cout << 12812;
    for (int i = 0; i < 12812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12813(int& val) {
    func_in_12813(val);
    const int x = 12813;
    std::cout << 12813;
    for (int i = 0; i < 12813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12813(int& val) {
    const int x = 12813;
    std::cout << 12813;
    for (int i = 0; i < 12813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12814(int& val) {
    func_in_12814(val);
    const int x = 12814;
    std::cout << 12814;
    for (int i = 0; i < 12814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12814(int& val) {
    const int x = 12814;
    std::cout << 12814;
    for (int i = 0; i < 12814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12815(int& val) {
    func_in_12815(val);
    const int x = 12815;
    std::cout << 12815;
    for (int i = 0; i < 12815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12815(int& val) {
    const int x = 12815;
    std::cout << 12815;
    for (int i = 0; i < 12815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12816(int& val) {
    func_in_12816(val);
    const int x = 12816;
    std::cout << 12816;
    for (int i = 0; i < 12816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12816(int& val) {
    const int x = 12816;
    std::cout << 12816;
    for (int i = 0; i < 12816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12817(int& val) {
    func_in_12817(val);
    const int x = 12817;
    std::cout << 12817;
    for (int i = 0; i < 12817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12817(int& val) {
    const int x = 12817;
    std::cout << 12817;
    for (int i = 0; i < 12817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12818(int& val) {
    func_in_12818(val);
    const int x = 12818;
    std::cout << 12818;
    for (int i = 0; i < 12818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12818(int& val) {
    const int x = 12818;
    std::cout << 12818;
    for (int i = 0; i < 12818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12819(int& val) {
    func_in_12819(val);
    const int x = 12819;
    std::cout << 12819;
    for (int i = 0; i < 12819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12819(int& val) {
    const int x = 12819;
    std::cout << 12819;
    for (int i = 0; i < 12819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12820(int& val) {
    func_in_12820(val);
    const int x = 12820;
    std::cout << 12820;
    for (int i = 0; i < 12820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12820(int& val) {
    const int x = 12820;
    std::cout << 12820;
    for (int i = 0; i < 12820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12821(int& val) {
    func_in_12821(val);
    const int x = 12821;
    std::cout << 12821;
    for (int i = 0; i < 12821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12821(int& val) {
    const int x = 12821;
    std::cout << 12821;
    for (int i = 0; i < 12821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12822(int& val) {
    func_in_12822(val);
    const int x = 12822;
    std::cout << 12822;
    for (int i = 0; i < 12822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12822(int& val) {
    const int x = 12822;
    std::cout << 12822;
    for (int i = 0; i < 12822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12823(int& val) {
    func_in_12823(val);
    const int x = 12823;
    std::cout << 12823;
    for (int i = 0; i < 12823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12823(int& val) {
    const int x = 12823;
    std::cout << 12823;
    for (int i = 0; i < 12823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12824(int& val) {
    func_in_12824(val);
    const int x = 12824;
    std::cout << 12824;
    for (int i = 0; i < 12824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12824(int& val) {
    const int x = 12824;
    std::cout << 12824;
    for (int i = 0; i < 12824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12825(int& val) {
    func_in_12825(val);
    const int x = 12825;
    std::cout << 12825;
    for (int i = 0; i < 12825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12825(int& val) {
    const int x = 12825;
    std::cout << 12825;
    for (int i = 0; i < 12825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12826(int& val) {
    func_in_12826(val);
    const int x = 12826;
    std::cout << 12826;
    for (int i = 0; i < 12826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12826(int& val) {
    const int x = 12826;
    std::cout << 12826;
    for (int i = 0; i < 12826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12827(int& val) {
    func_in_12827(val);
    const int x = 12827;
    std::cout << 12827;
    for (int i = 0; i < 12827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12827(int& val) {
    const int x = 12827;
    std::cout << 12827;
    for (int i = 0; i < 12827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12828(int& val) {
    func_in_12828(val);
    const int x = 12828;
    std::cout << 12828;
    for (int i = 0; i < 12828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12828(int& val) {
    const int x = 12828;
    std::cout << 12828;
    for (int i = 0; i < 12828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12829(int& val) {
    func_in_12829(val);
    const int x = 12829;
    std::cout << 12829;
    for (int i = 0; i < 12829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12829(int& val) {
    const int x = 12829;
    std::cout << 12829;
    for (int i = 0; i < 12829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12830(int& val) {
    func_in_12830(val);
    const int x = 12830;
    std::cout << 12830;
    for (int i = 0; i < 12830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12830(int& val) {
    const int x = 12830;
    std::cout << 12830;
    for (int i = 0; i < 12830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12831(int& val) {
    func_in_12831(val);
    const int x = 12831;
    std::cout << 12831;
    for (int i = 0; i < 12831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12831(int& val) {
    const int x = 12831;
    std::cout << 12831;
    for (int i = 0; i < 12831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12832(int& val) {
    func_in_12832(val);
    const int x = 12832;
    std::cout << 12832;
    for (int i = 0; i < 12832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12832(int& val) {
    const int x = 12832;
    std::cout << 12832;
    for (int i = 0; i < 12832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12833(int& val) {
    func_in_12833(val);
    const int x = 12833;
    std::cout << 12833;
    for (int i = 0; i < 12833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12833(int& val) {
    const int x = 12833;
    std::cout << 12833;
    for (int i = 0; i < 12833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12834(int& val) {
    func_in_12834(val);
    const int x = 12834;
    std::cout << 12834;
    for (int i = 0; i < 12834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12834(int& val) {
    const int x = 12834;
    std::cout << 12834;
    for (int i = 0; i < 12834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12835(int& val) {
    func_in_12835(val);
    const int x = 12835;
    std::cout << 12835;
    for (int i = 0; i < 12835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12835(int& val) {
    const int x = 12835;
    std::cout << 12835;
    for (int i = 0; i < 12835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12836(int& val) {
    func_in_12836(val);
    const int x = 12836;
    std::cout << 12836;
    for (int i = 0; i < 12836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12836(int& val) {
    const int x = 12836;
    std::cout << 12836;
    for (int i = 0; i < 12836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12837(int& val) {
    func_in_12837(val);
    const int x = 12837;
    std::cout << 12837;
    for (int i = 0; i < 12837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12837(int& val) {
    const int x = 12837;
    std::cout << 12837;
    for (int i = 0; i < 12837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12838(int& val) {
    func_in_12838(val);
    const int x = 12838;
    std::cout << 12838;
    for (int i = 0; i < 12838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12838(int& val) {
    const int x = 12838;
    std::cout << 12838;
    for (int i = 0; i < 12838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12839(int& val) {
    func_in_12839(val);
    const int x = 12839;
    std::cout << 12839;
    for (int i = 0; i < 12839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12839(int& val) {
    const int x = 12839;
    std::cout << 12839;
    for (int i = 0; i < 12839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12840(int& val) {
    func_in_12840(val);
    const int x = 12840;
    std::cout << 12840;
    for (int i = 0; i < 12840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12840(int& val) {
    const int x = 12840;
    std::cout << 12840;
    for (int i = 0; i < 12840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12841(int& val) {
    func_in_12841(val);
    const int x = 12841;
    std::cout << 12841;
    for (int i = 0; i < 12841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12841(int& val) {
    const int x = 12841;
    std::cout << 12841;
    for (int i = 0; i < 12841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12842(int& val) {
    func_in_12842(val);
    const int x = 12842;
    std::cout << 12842;
    for (int i = 0; i < 12842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12842(int& val) {
    const int x = 12842;
    std::cout << 12842;
    for (int i = 0; i < 12842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12843(int& val) {
    func_in_12843(val);
    const int x = 12843;
    std::cout << 12843;
    for (int i = 0; i < 12843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12843(int& val) {
    const int x = 12843;
    std::cout << 12843;
    for (int i = 0; i < 12843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12844(int& val) {
    func_in_12844(val);
    const int x = 12844;
    std::cout << 12844;
    for (int i = 0; i < 12844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12844(int& val) {
    const int x = 12844;
    std::cout << 12844;
    for (int i = 0; i < 12844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12845(int& val) {
    func_in_12845(val);
    const int x = 12845;
    std::cout << 12845;
    for (int i = 0; i < 12845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12845(int& val) {
    const int x = 12845;
    std::cout << 12845;
    for (int i = 0; i < 12845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12846(int& val) {
    func_in_12846(val);
    const int x = 12846;
    std::cout << 12846;
    for (int i = 0; i < 12846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12846(int& val) {
    const int x = 12846;
    std::cout << 12846;
    for (int i = 0; i < 12846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12847(int& val) {
    func_in_12847(val);
    const int x = 12847;
    std::cout << 12847;
    for (int i = 0; i < 12847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12847(int& val) {
    const int x = 12847;
    std::cout << 12847;
    for (int i = 0; i < 12847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12848(int& val) {
    func_in_12848(val);
    const int x = 12848;
    std::cout << 12848;
    for (int i = 0; i < 12848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12848(int& val) {
    const int x = 12848;
    std::cout << 12848;
    for (int i = 0; i < 12848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12849(int& val) {
    func_in_12849(val);
    const int x = 12849;
    std::cout << 12849;
    for (int i = 0; i < 12849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12849(int& val) {
    const int x = 12849;
    std::cout << 12849;
    for (int i = 0; i < 12849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12850(int& val) {
    func_in_12850(val);
    const int x = 12850;
    std::cout << 12850;
    for (int i = 0; i < 12850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12850(int& val) {
    const int x = 12850;
    std::cout << 12850;
    for (int i = 0; i < 12850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12851(int& val) {
    func_in_12851(val);
    const int x = 12851;
    std::cout << 12851;
    for (int i = 0; i < 12851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12851(int& val) {
    const int x = 12851;
    std::cout << 12851;
    for (int i = 0; i < 12851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12852(int& val) {
    func_in_12852(val);
    const int x = 12852;
    std::cout << 12852;
    for (int i = 0; i < 12852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12852(int& val) {
    const int x = 12852;
    std::cout << 12852;
    for (int i = 0; i < 12852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12853(int& val) {
    func_in_12853(val);
    const int x = 12853;
    std::cout << 12853;
    for (int i = 0; i < 12853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12853(int& val) {
    const int x = 12853;
    std::cout << 12853;
    for (int i = 0; i < 12853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12854(int& val) {
    func_in_12854(val);
    const int x = 12854;
    std::cout << 12854;
    for (int i = 0; i < 12854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12854(int& val) {
    const int x = 12854;
    std::cout << 12854;
    for (int i = 0; i < 12854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12855(int& val) {
    func_in_12855(val);
    const int x = 12855;
    std::cout << 12855;
    for (int i = 0; i < 12855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12855(int& val) {
    const int x = 12855;
    std::cout << 12855;
    for (int i = 0; i < 12855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12856(int& val) {
    func_in_12856(val);
    const int x = 12856;
    std::cout << 12856;
    for (int i = 0; i < 12856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12856(int& val) {
    const int x = 12856;
    std::cout << 12856;
    for (int i = 0; i < 12856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12857(int& val) {
    func_in_12857(val);
    const int x = 12857;
    std::cout << 12857;
    for (int i = 0; i < 12857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12857(int& val) {
    const int x = 12857;
    std::cout << 12857;
    for (int i = 0; i < 12857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12858(int& val) {
    func_in_12858(val);
    const int x = 12858;
    std::cout << 12858;
    for (int i = 0; i < 12858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12858(int& val) {
    const int x = 12858;
    std::cout << 12858;
    for (int i = 0; i < 12858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12859(int& val) {
    func_in_12859(val);
    const int x = 12859;
    std::cout << 12859;
    for (int i = 0; i < 12859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12859(int& val) {
    const int x = 12859;
    std::cout << 12859;
    for (int i = 0; i < 12859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12860(int& val) {
    func_in_12860(val);
    const int x = 12860;
    std::cout << 12860;
    for (int i = 0; i < 12860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12860(int& val) {
    const int x = 12860;
    std::cout << 12860;
    for (int i = 0; i < 12860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12861(int& val) {
    func_in_12861(val);
    const int x = 12861;
    std::cout << 12861;
    for (int i = 0; i < 12861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12861(int& val) {
    const int x = 12861;
    std::cout << 12861;
    for (int i = 0; i < 12861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12862(int& val) {
    func_in_12862(val);
    const int x = 12862;
    std::cout << 12862;
    for (int i = 0; i < 12862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12862(int& val) {
    const int x = 12862;
    std::cout << 12862;
    for (int i = 0; i < 12862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12863(int& val) {
    func_in_12863(val);
    const int x = 12863;
    std::cout << 12863;
    for (int i = 0; i < 12863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12863(int& val) {
    const int x = 12863;
    std::cout << 12863;
    for (int i = 0; i < 12863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12864(int& val) {
    func_in_12864(val);
    const int x = 12864;
    std::cout << 12864;
    for (int i = 0; i < 12864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12864(int& val) {
    const int x = 12864;
    std::cout << 12864;
    for (int i = 0; i < 12864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12865(int& val) {
    func_in_12865(val);
    const int x = 12865;
    std::cout << 12865;
    for (int i = 0; i < 12865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12865(int& val) {
    const int x = 12865;
    std::cout << 12865;
    for (int i = 0; i < 12865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12866(int& val) {
    func_in_12866(val);
    const int x = 12866;
    std::cout << 12866;
    for (int i = 0; i < 12866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12866(int& val) {
    const int x = 12866;
    std::cout << 12866;
    for (int i = 0; i < 12866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12867(int& val) {
    func_in_12867(val);
    const int x = 12867;
    std::cout << 12867;
    for (int i = 0; i < 12867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12867(int& val) {
    const int x = 12867;
    std::cout << 12867;
    for (int i = 0; i < 12867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12868(int& val) {
    func_in_12868(val);
    const int x = 12868;
    std::cout << 12868;
    for (int i = 0; i < 12868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12868(int& val) {
    const int x = 12868;
    std::cout << 12868;
    for (int i = 0; i < 12868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12869(int& val) {
    func_in_12869(val);
    const int x = 12869;
    std::cout << 12869;
    for (int i = 0; i < 12869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12869(int& val) {
    const int x = 12869;
    std::cout << 12869;
    for (int i = 0; i < 12869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12870(int& val) {
    func_in_12870(val);
    const int x = 12870;
    std::cout << 12870;
    for (int i = 0; i < 12870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12870(int& val) {
    const int x = 12870;
    std::cout << 12870;
    for (int i = 0; i < 12870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12871(int& val) {
    func_in_12871(val);
    const int x = 12871;
    std::cout << 12871;
    for (int i = 0; i < 12871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12871(int& val) {
    const int x = 12871;
    std::cout << 12871;
    for (int i = 0; i < 12871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12872(int& val) {
    func_in_12872(val);
    const int x = 12872;
    std::cout << 12872;
    for (int i = 0; i < 12872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12872(int& val) {
    const int x = 12872;
    std::cout << 12872;
    for (int i = 0; i < 12872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12873(int& val) {
    func_in_12873(val);
    const int x = 12873;
    std::cout << 12873;
    for (int i = 0; i < 12873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12873(int& val) {
    const int x = 12873;
    std::cout << 12873;
    for (int i = 0; i < 12873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12874(int& val) {
    func_in_12874(val);
    const int x = 12874;
    std::cout << 12874;
    for (int i = 0; i < 12874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12874(int& val) {
    const int x = 12874;
    std::cout << 12874;
    for (int i = 0; i < 12874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12875(int& val) {
    func_in_12875(val);
    const int x = 12875;
    std::cout << 12875;
    for (int i = 0; i < 12875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12875(int& val) {
    const int x = 12875;
    std::cout << 12875;
    for (int i = 0; i < 12875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12876(int& val) {
    func_in_12876(val);
    const int x = 12876;
    std::cout << 12876;
    for (int i = 0; i < 12876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12876(int& val) {
    const int x = 12876;
    std::cout << 12876;
    for (int i = 0; i < 12876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12877(int& val) {
    func_in_12877(val);
    const int x = 12877;
    std::cout << 12877;
    for (int i = 0; i < 12877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12877(int& val) {
    const int x = 12877;
    std::cout << 12877;
    for (int i = 0; i < 12877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12878(int& val) {
    func_in_12878(val);
    const int x = 12878;
    std::cout << 12878;
    for (int i = 0; i < 12878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12878(int& val) {
    const int x = 12878;
    std::cout << 12878;
    for (int i = 0; i < 12878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12879(int& val) {
    func_in_12879(val);
    const int x = 12879;
    std::cout << 12879;
    for (int i = 0; i < 12879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12879(int& val) {
    const int x = 12879;
    std::cout << 12879;
    for (int i = 0; i < 12879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12880(int& val) {
    func_in_12880(val);
    const int x = 12880;
    std::cout << 12880;
    for (int i = 0; i < 12880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12880(int& val) {
    const int x = 12880;
    std::cout << 12880;
    for (int i = 0; i < 12880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12881(int& val) {
    func_in_12881(val);
    const int x = 12881;
    std::cout << 12881;
    for (int i = 0; i < 12881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12881(int& val) {
    const int x = 12881;
    std::cout << 12881;
    for (int i = 0; i < 12881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12882(int& val) {
    func_in_12882(val);
    const int x = 12882;
    std::cout << 12882;
    for (int i = 0; i < 12882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12882(int& val) {
    const int x = 12882;
    std::cout << 12882;
    for (int i = 0; i < 12882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12883(int& val) {
    func_in_12883(val);
    const int x = 12883;
    std::cout << 12883;
    for (int i = 0; i < 12883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12883(int& val) {
    const int x = 12883;
    std::cout << 12883;
    for (int i = 0; i < 12883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12884(int& val) {
    func_in_12884(val);
    const int x = 12884;
    std::cout << 12884;
    for (int i = 0; i < 12884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12884(int& val) {
    const int x = 12884;
    std::cout << 12884;
    for (int i = 0; i < 12884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12885(int& val) {
    func_in_12885(val);
    const int x = 12885;
    std::cout << 12885;
    for (int i = 0; i < 12885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12885(int& val) {
    const int x = 12885;
    std::cout << 12885;
    for (int i = 0; i < 12885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12886(int& val) {
    func_in_12886(val);
    const int x = 12886;
    std::cout << 12886;
    for (int i = 0; i < 12886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12886(int& val) {
    const int x = 12886;
    std::cout << 12886;
    for (int i = 0; i < 12886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12887(int& val) {
    func_in_12887(val);
    const int x = 12887;
    std::cout << 12887;
    for (int i = 0; i < 12887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12887(int& val) {
    const int x = 12887;
    std::cout << 12887;
    for (int i = 0; i < 12887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12888(int& val) {
    func_in_12888(val);
    const int x = 12888;
    std::cout << 12888;
    for (int i = 0; i < 12888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12888(int& val) {
    const int x = 12888;
    std::cout << 12888;
    for (int i = 0; i < 12888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12889(int& val) {
    func_in_12889(val);
    const int x = 12889;
    std::cout << 12889;
    for (int i = 0; i < 12889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12889(int& val) {
    const int x = 12889;
    std::cout << 12889;
    for (int i = 0; i < 12889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12890(int& val) {
    func_in_12890(val);
    const int x = 12890;
    std::cout << 12890;
    for (int i = 0; i < 12890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12890(int& val) {
    const int x = 12890;
    std::cout << 12890;
    for (int i = 0; i < 12890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12891(int& val) {
    func_in_12891(val);
    const int x = 12891;
    std::cout << 12891;
    for (int i = 0; i < 12891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12891(int& val) {
    const int x = 12891;
    std::cout << 12891;
    for (int i = 0; i < 12891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12892(int& val) {
    func_in_12892(val);
    const int x = 12892;
    std::cout << 12892;
    for (int i = 0; i < 12892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12892(int& val) {
    const int x = 12892;
    std::cout << 12892;
    for (int i = 0; i < 12892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12893(int& val) {
    func_in_12893(val);
    const int x = 12893;
    std::cout << 12893;
    for (int i = 0; i < 12893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12893(int& val) {
    const int x = 12893;
    std::cout << 12893;
    for (int i = 0; i < 12893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12894(int& val) {
    func_in_12894(val);
    const int x = 12894;
    std::cout << 12894;
    for (int i = 0; i < 12894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12894(int& val) {
    const int x = 12894;
    std::cout << 12894;
    for (int i = 0; i < 12894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12895(int& val) {
    func_in_12895(val);
    const int x = 12895;
    std::cout << 12895;
    for (int i = 0; i < 12895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12895(int& val) {
    const int x = 12895;
    std::cout << 12895;
    for (int i = 0; i < 12895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12896(int& val) {
    func_in_12896(val);
    const int x = 12896;
    std::cout << 12896;
    for (int i = 0; i < 12896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12896(int& val) {
    const int x = 12896;
    std::cout << 12896;
    for (int i = 0; i < 12896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12897(int& val) {
    func_in_12897(val);
    const int x = 12897;
    std::cout << 12897;
    for (int i = 0; i < 12897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12897(int& val) {
    const int x = 12897;
    std::cout << 12897;
    for (int i = 0; i < 12897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12898(int& val) {
    func_in_12898(val);
    const int x = 12898;
    std::cout << 12898;
    for (int i = 0; i < 12898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12898(int& val) {
    const int x = 12898;
    std::cout << 12898;
    for (int i = 0; i < 12898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12899(int& val) {
    func_in_12899(val);
    const int x = 12899;
    std::cout << 12899;
    for (int i = 0; i < 12899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12899(int& val) {
    const int x = 12899;
    std::cout << 12899;
    for (int i = 0; i < 12899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12900(int& val) {
    func_in_12900(val);
    const int x = 12900;
    std::cout << 12900;
    for (int i = 0; i < 12900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12900(int& val) {
    const int x = 12900;
    std::cout << 12900;
    for (int i = 0; i < 12900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12901(int& val) {
    func_in_12901(val);
    const int x = 12901;
    std::cout << 12901;
    for (int i = 0; i < 12901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12901(int& val) {
    const int x = 12901;
    std::cout << 12901;
    for (int i = 0; i < 12901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12902(int& val) {
    func_in_12902(val);
    const int x = 12902;
    std::cout << 12902;
    for (int i = 0; i < 12902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12902(int& val) {
    const int x = 12902;
    std::cout << 12902;
    for (int i = 0; i < 12902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12903(int& val) {
    func_in_12903(val);
    const int x = 12903;
    std::cout << 12903;
    for (int i = 0; i < 12903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12903(int& val) {
    const int x = 12903;
    std::cout << 12903;
    for (int i = 0; i < 12903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12904(int& val) {
    func_in_12904(val);
    const int x = 12904;
    std::cout << 12904;
    for (int i = 0; i < 12904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12904(int& val) {
    const int x = 12904;
    std::cout << 12904;
    for (int i = 0; i < 12904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12905(int& val) {
    func_in_12905(val);
    const int x = 12905;
    std::cout << 12905;
    for (int i = 0; i < 12905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12905(int& val) {
    const int x = 12905;
    std::cout << 12905;
    for (int i = 0; i < 12905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12906(int& val) {
    func_in_12906(val);
    const int x = 12906;
    std::cout << 12906;
    for (int i = 0; i < 12906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12906(int& val) {
    const int x = 12906;
    std::cout << 12906;
    for (int i = 0; i < 12906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12907(int& val) {
    func_in_12907(val);
    const int x = 12907;
    std::cout << 12907;
    for (int i = 0; i < 12907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12907(int& val) {
    const int x = 12907;
    std::cout << 12907;
    for (int i = 0; i < 12907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12908(int& val) {
    func_in_12908(val);
    const int x = 12908;
    std::cout << 12908;
    for (int i = 0; i < 12908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12908(int& val) {
    const int x = 12908;
    std::cout << 12908;
    for (int i = 0; i < 12908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12909(int& val) {
    func_in_12909(val);
    const int x = 12909;
    std::cout << 12909;
    for (int i = 0; i < 12909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12909(int& val) {
    const int x = 12909;
    std::cout << 12909;
    for (int i = 0; i < 12909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12910(int& val) {
    func_in_12910(val);
    const int x = 12910;
    std::cout << 12910;
    for (int i = 0; i < 12910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12910(int& val) {
    const int x = 12910;
    std::cout << 12910;
    for (int i = 0; i < 12910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12911(int& val) {
    func_in_12911(val);
    const int x = 12911;
    std::cout << 12911;
    for (int i = 0; i < 12911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12911(int& val) {
    const int x = 12911;
    std::cout << 12911;
    for (int i = 0; i < 12911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12912(int& val) {
    func_in_12912(val);
    const int x = 12912;
    std::cout << 12912;
    for (int i = 0; i < 12912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12912(int& val) {
    const int x = 12912;
    std::cout << 12912;
    for (int i = 0; i < 12912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12913(int& val) {
    func_in_12913(val);
    const int x = 12913;
    std::cout << 12913;
    for (int i = 0; i < 12913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12913(int& val) {
    const int x = 12913;
    std::cout << 12913;
    for (int i = 0; i < 12913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12914(int& val) {
    func_in_12914(val);
    const int x = 12914;
    std::cout << 12914;
    for (int i = 0; i < 12914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12914(int& val) {
    const int x = 12914;
    std::cout << 12914;
    for (int i = 0; i < 12914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12915(int& val) {
    func_in_12915(val);
    const int x = 12915;
    std::cout << 12915;
    for (int i = 0; i < 12915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12915(int& val) {
    const int x = 12915;
    std::cout << 12915;
    for (int i = 0; i < 12915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12916(int& val) {
    func_in_12916(val);
    const int x = 12916;
    std::cout << 12916;
    for (int i = 0; i < 12916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12916(int& val) {
    const int x = 12916;
    std::cout << 12916;
    for (int i = 0; i < 12916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12917(int& val) {
    func_in_12917(val);
    const int x = 12917;
    std::cout << 12917;
    for (int i = 0; i < 12917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12917(int& val) {
    const int x = 12917;
    std::cout << 12917;
    for (int i = 0; i < 12917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12918(int& val) {
    func_in_12918(val);
    const int x = 12918;
    std::cout << 12918;
    for (int i = 0; i < 12918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12918(int& val) {
    const int x = 12918;
    std::cout << 12918;
    for (int i = 0; i < 12918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12919(int& val) {
    func_in_12919(val);
    const int x = 12919;
    std::cout << 12919;
    for (int i = 0; i < 12919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12919(int& val) {
    const int x = 12919;
    std::cout << 12919;
    for (int i = 0; i < 12919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12920(int& val) {
    func_in_12920(val);
    const int x = 12920;
    std::cout << 12920;
    for (int i = 0; i < 12920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12920(int& val) {
    const int x = 12920;
    std::cout << 12920;
    for (int i = 0; i < 12920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12921(int& val) {
    func_in_12921(val);
    const int x = 12921;
    std::cout << 12921;
    for (int i = 0; i < 12921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12921(int& val) {
    const int x = 12921;
    std::cout << 12921;
    for (int i = 0; i < 12921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12922(int& val) {
    func_in_12922(val);
    const int x = 12922;
    std::cout << 12922;
    for (int i = 0; i < 12922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12922(int& val) {
    const int x = 12922;
    std::cout << 12922;
    for (int i = 0; i < 12922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12923(int& val) {
    func_in_12923(val);
    const int x = 12923;
    std::cout << 12923;
    for (int i = 0; i < 12923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12923(int& val) {
    const int x = 12923;
    std::cout << 12923;
    for (int i = 0; i < 12923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12924(int& val) {
    func_in_12924(val);
    const int x = 12924;
    std::cout << 12924;
    for (int i = 0; i < 12924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12924(int& val) {
    const int x = 12924;
    std::cout << 12924;
    for (int i = 0; i < 12924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12925(int& val) {
    func_in_12925(val);
    const int x = 12925;
    std::cout << 12925;
    for (int i = 0; i < 12925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12925(int& val) {
    const int x = 12925;
    std::cout << 12925;
    for (int i = 0; i < 12925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12926(int& val) {
    func_in_12926(val);
    const int x = 12926;
    std::cout << 12926;
    for (int i = 0; i < 12926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12926(int& val) {
    const int x = 12926;
    std::cout << 12926;
    for (int i = 0; i < 12926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12927(int& val) {
    func_in_12927(val);
    const int x = 12927;
    std::cout << 12927;
    for (int i = 0; i < 12927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12927(int& val) {
    const int x = 12927;
    std::cout << 12927;
    for (int i = 0; i < 12927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12928(int& val) {
    func_in_12928(val);
    const int x = 12928;
    std::cout << 12928;
    for (int i = 0; i < 12928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12928(int& val) {
    const int x = 12928;
    std::cout << 12928;
    for (int i = 0; i < 12928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12929(int& val) {
    func_in_12929(val);
    const int x = 12929;
    std::cout << 12929;
    for (int i = 0; i < 12929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12929(int& val) {
    const int x = 12929;
    std::cout << 12929;
    for (int i = 0; i < 12929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12930(int& val) {
    func_in_12930(val);
    const int x = 12930;
    std::cout << 12930;
    for (int i = 0; i < 12930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12930(int& val) {
    const int x = 12930;
    std::cout << 12930;
    for (int i = 0; i < 12930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12931(int& val) {
    func_in_12931(val);
    const int x = 12931;
    std::cout << 12931;
    for (int i = 0; i < 12931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12931(int& val) {
    const int x = 12931;
    std::cout << 12931;
    for (int i = 0; i < 12931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12932(int& val) {
    func_in_12932(val);
    const int x = 12932;
    std::cout << 12932;
    for (int i = 0; i < 12932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12932(int& val) {
    const int x = 12932;
    std::cout << 12932;
    for (int i = 0; i < 12932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12933(int& val) {
    func_in_12933(val);
    const int x = 12933;
    std::cout << 12933;
    for (int i = 0; i < 12933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12933(int& val) {
    const int x = 12933;
    std::cout << 12933;
    for (int i = 0; i < 12933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12934(int& val) {
    func_in_12934(val);
    const int x = 12934;
    std::cout << 12934;
    for (int i = 0; i < 12934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12934(int& val) {
    const int x = 12934;
    std::cout << 12934;
    for (int i = 0; i < 12934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12935(int& val) {
    func_in_12935(val);
    const int x = 12935;
    std::cout << 12935;
    for (int i = 0; i < 12935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12935(int& val) {
    const int x = 12935;
    std::cout << 12935;
    for (int i = 0; i < 12935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12936(int& val) {
    func_in_12936(val);
    const int x = 12936;
    std::cout << 12936;
    for (int i = 0; i < 12936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12936(int& val) {
    const int x = 12936;
    std::cout << 12936;
    for (int i = 0; i < 12936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12937(int& val) {
    func_in_12937(val);
    const int x = 12937;
    std::cout << 12937;
    for (int i = 0; i < 12937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12937(int& val) {
    const int x = 12937;
    std::cout << 12937;
    for (int i = 0; i < 12937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12938(int& val) {
    func_in_12938(val);
    const int x = 12938;
    std::cout << 12938;
    for (int i = 0; i < 12938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12938(int& val) {
    const int x = 12938;
    std::cout << 12938;
    for (int i = 0; i < 12938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12939(int& val) {
    func_in_12939(val);
    const int x = 12939;
    std::cout << 12939;
    for (int i = 0; i < 12939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12939(int& val) {
    const int x = 12939;
    std::cout << 12939;
    for (int i = 0; i < 12939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12940(int& val) {
    func_in_12940(val);
    const int x = 12940;
    std::cout << 12940;
    for (int i = 0; i < 12940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12940(int& val) {
    const int x = 12940;
    std::cout << 12940;
    for (int i = 0; i < 12940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12941(int& val) {
    func_in_12941(val);
    const int x = 12941;
    std::cout << 12941;
    for (int i = 0; i < 12941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12941(int& val) {
    const int x = 12941;
    std::cout << 12941;
    for (int i = 0; i < 12941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12942(int& val) {
    func_in_12942(val);
    const int x = 12942;
    std::cout << 12942;
    for (int i = 0; i < 12942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12942(int& val) {
    const int x = 12942;
    std::cout << 12942;
    for (int i = 0; i < 12942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12943(int& val) {
    func_in_12943(val);
    const int x = 12943;
    std::cout << 12943;
    for (int i = 0; i < 12943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12943(int& val) {
    const int x = 12943;
    std::cout << 12943;
    for (int i = 0; i < 12943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12944(int& val) {
    func_in_12944(val);
    const int x = 12944;
    std::cout << 12944;
    for (int i = 0; i < 12944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12944(int& val) {
    const int x = 12944;
    std::cout << 12944;
    for (int i = 0; i < 12944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12945(int& val) {
    func_in_12945(val);
    const int x = 12945;
    std::cout << 12945;
    for (int i = 0; i < 12945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12945(int& val) {
    const int x = 12945;
    std::cout << 12945;
    for (int i = 0; i < 12945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12946(int& val) {
    func_in_12946(val);
    const int x = 12946;
    std::cout << 12946;
    for (int i = 0; i < 12946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12946(int& val) {
    const int x = 12946;
    std::cout << 12946;
    for (int i = 0; i < 12946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12947(int& val) {
    func_in_12947(val);
    const int x = 12947;
    std::cout << 12947;
    for (int i = 0; i < 12947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12947(int& val) {
    const int x = 12947;
    std::cout << 12947;
    for (int i = 0; i < 12947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12948(int& val) {
    func_in_12948(val);
    const int x = 12948;
    std::cout << 12948;
    for (int i = 0; i < 12948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12948(int& val) {
    const int x = 12948;
    std::cout << 12948;
    for (int i = 0; i < 12948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12949(int& val) {
    func_in_12949(val);
    const int x = 12949;
    std::cout << 12949;
    for (int i = 0; i < 12949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12949(int& val) {
    const int x = 12949;
    std::cout << 12949;
    for (int i = 0; i < 12949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12950(int& val) {
    func_in_12950(val);
    const int x = 12950;
    std::cout << 12950;
    for (int i = 0; i < 12950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12950(int& val) {
    const int x = 12950;
    std::cout << 12950;
    for (int i = 0; i < 12950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12951(int& val) {
    func_in_12951(val);
    const int x = 12951;
    std::cout << 12951;
    for (int i = 0; i < 12951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12951(int& val) {
    const int x = 12951;
    std::cout << 12951;
    for (int i = 0; i < 12951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12952(int& val) {
    func_in_12952(val);
    const int x = 12952;
    std::cout << 12952;
    for (int i = 0; i < 12952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12952(int& val) {
    const int x = 12952;
    std::cout << 12952;
    for (int i = 0; i < 12952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12953(int& val) {
    func_in_12953(val);
    const int x = 12953;
    std::cout << 12953;
    for (int i = 0; i < 12953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12953(int& val) {
    const int x = 12953;
    std::cout << 12953;
    for (int i = 0; i < 12953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12954(int& val) {
    func_in_12954(val);
    const int x = 12954;
    std::cout << 12954;
    for (int i = 0; i < 12954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12954(int& val) {
    const int x = 12954;
    std::cout << 12954;
    for (int i = 0; i < 12954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12955(int& val) {
    func_in_12955(val);
    const int x = 12955;
    std::cout << 12955;
    for (int i = 0; i < 12955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12955(int& val) {
    const int x = 12955;
    std::cout << 12955;
    for (int i = 0; i < 12955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12956(int& val) {
    func_in_12956(val);
    const int x = 12956;
    std::cout << 12956;
    for (int i = 0; i < 12956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12956(int& val) {
    const int x = 12956;
    std::cout << 12956;
    for (int i = 0; i < 12956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12957(int& val) {
    func_in_12957(val);
    const int x = 12957;
    std::cout << 12957;
    for (int i = 0; i < 12957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12957(int& val) {
    const int x = 12957;
    std::cout << 12957;
    for (int i = 0; i < 12957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12958(int& val) {
    func_in_12958(val);
    const int x = 12958;
    std::cout << 12958;
    for (int i = 0; i < 12958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12958(int& val) {
    const int x = 12958;
    std::cout << 12958;
    for (int i = 0; i < 12958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12959(int& val) {
    func_in_12959(val);
    const int x = 12959;
    std::cout << 12959;
    for (int i = 0; i < 12959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12959(int& val) {
    const int x = 12959;
    std::cout << 12959;
    for (int i = 0; i < 12959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12960(int& val) {
    func_in_12960(val);
    const int x = 12960;
    std::cout << 12960;
    for (int i = 0; i < 12960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12960(int& val) {
    const int x = 12960;
    std::cout << 12960;
    for (int i = 0; i < 12960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12961(int& val) {
    func_in_12961(val);
    const int x = 12961;
    std::cout << 12961;
    for (int i = 0; i < 12961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12961(int& val) {
    const int x = 12961;
    std::cout << 12961;
    for (int i = 0; i < 12961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12962(int& val) {
    func_in_12962(val);
    const int x = 12962;
    std::cout << 12962;
    for (int i = 0; i < 12962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12962(int& val) {
    const int x = 12962;
    std::cout << 12962;
    for (int i = 0; i < 12962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12963(int& val) {
    func_in_12963(val);
    const int x = 12963;
    std::cout << 12963;
    for (int i = 0; i < 12963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12963(int& val) {
    const int x = 12963;
    std::cout << 12963;
    for (int i = 0; i < 12963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12964(int& val) {
    func_in_12964(val);
    const int x = 12964;
    std::cout << 12964;
    for (int i = 0; i < 12964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12964(int& val) {
    const int x = 12964;
    std::cout << 12964;
    for (int i = 0; i < 12964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12965(int& val) {
    func_in_12965(val);
    const int x = 12965;
    std::cout << 12965;
    for (int i = 0; i < 12965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12965(int& val) {
    const int x = 12965;
    std::cout << 12965;
    for (int i = 0; i < 12965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12966(int& val) {
    func_in_12966(val);
    const int x = 12966;
    std::cout << 12966;
    for (int i = 0; i < 12966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12966(int& val) {
    const int x = 12966;
    std::cout << 12966;
    for (int i = 0; i < 12966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12967(int& val) {
    func_in_12967(val);
    const int x = 12967;
    std::cout << 12967;
    for (int i = 0; i < 12967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12967(int& val) {
    const int x = 12967;
    std::cout << 12967;
    for (int i = 0; i < 12967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12968(int& val) {
    func_in_12968(val);
    const int x = 12968;
    std::cout << 12968;
    for (int i = 0; i < 12968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12968(int& val) {
    const int x = 12968;
    std::cout << 12968;
    for (int i = 0; i < 12968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12969(int& val) {
    func_in_12969(val);
    const int x = 12969;
    std::cout << 12969;
    for (int i = 0; i < 12969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12969(int& val) {
    const int x = 12969;
    std::cout << 12969;
    for (int i = 0; i < 12969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12970(int& val) {
    func_in_12970(val);
    const int x = 12970;
    std::cout << 12970;
    for (int i = 0; i < 12970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12970(int& val) {
    const int x = 12970;
    std::cout << 12970;
    for (int i = 0; i < 12970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12971(int& val) {
    func_in_12971(val);
    const int x = 12971;
    std::cout << 12971;
    for (int i = 0; i < 12971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12971(int& val) {
    const int x = 12971;
    std::cout << 12971;
    for (int i = 0; i < 12971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12972(int& val) {
    func_in_12972(val);
    const int x = 12972;
    std::cout << 12972;
    for (int i = 0; i < 12972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12972(int& val) {
    const int x = 12972;
    std::cout << 12972;
    for (int i = 0; i < 12972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12973(int& val) {
    func_in_12973(val);
    const int x = 12973;
    std::cout << 12973;
    for (int i = 0; i < 12973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12973(int& val) {
    const int x = 12973;
    std::cout << 12973;
    for (int i = 0; i < 12973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12974(int& val) {
    func_in_12974(val);
    const int x = 12974;
    std::cout << 12974;
    for (int i = 0; i < 12974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12974(int& val) {
    const int x = 12974;
    std::cout << 12974;
    for (int i = 0; i < 12974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12975(int& val) {
    func_in_12975(val);
    const int x = 12975;
    std::cout << 12975;
    for (int i = 0; i < 12975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12975(int& val) {
    const int x = 12975;
    std::cout << 12975;
    for (int i = 0; i < 12975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12976(int& val) {
    func_in_12976(val);
    const int x = 12976;
    std::cout << 12976;
    for (int i = 0; i < 12976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12976(int& val) {
    const int x = 12976;
    std::cout << 12976;
    for (int i = 0; i < 12976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12977(int& val) {
    func_in_12977(val);
    const int x = 12977;
    std::cout << 12977;
    for (int i = 0; i < 12977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12977(int& val) {
    const int x = 12977;
    std::cout << 12977;
    for (int i = 0; i < 12977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12978(int& val) {
    func_in_12978(val);
    const int x = 12978;
    std::cout << 12978;
    for (int i = 0; i < 12978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12978(int& val) {
    const int x = 12978;
    std::cout << 12978;
    for (int i = 0; i < 12978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12979(int& val) {
    func_in_12979(val);
    const int x = 12979;
    std::cout << 12979;
    for (int i = 0; i < 12979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12979(int& val) {
    const int x = 12979;
    std::cout << 12979;
    for (int i = 0; i < 12979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12980(int& val) {
    func_in_12980(val);
    const int x = 12980;
    std::cout << 12980;
    for (int i = 0; i < 12980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12980(int& val) {
    const int x = 12980;
    std::cout << 12980;
    for (int i = 0; i < 12980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12981(int& val) {
    func_in_12981(val);
    const int x = 12981;
    std::cout << 12981;
    for (int i = 0; i < 12981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12981(int& val) {
    const int x = 12981;
    std::cout << 12981;
    for (int i = 0; i < 12981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12982(int& val) {
    func_in_12982(val);
    const int x = 12982;
    std::cout << 12982;
    for (int i = 0; i < 12982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12982(int& val) {
    const int x = 12982;
    std::cout << 12982;
    for (int i = 0; i < 12982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12983(int& val) {
    func_in_12983(val);
    const int x = 12983;
    std::cout << 12983;
    for (int i = 0; i < 12983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12983(int& val) {
    const int x = 12983;
    std::cout << 12983;
    for (int i = 0; i < 12983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12984(int& val) {
    func_in_12984(val);
    const int x = 12984;
    std::cout << 12984;
    for (int i = 0; i < 12984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12984(int& val) {
    const int x = 12984;
    std::cout << 12984;
    for (int i = 0; i < 12984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12985(int& val) {
    func_in_12985(val);
    const int x = 12985;
    std::cout << 12985;
    for (int i = 0; i < 12985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12985(int& val) {
    const int x = 12985;
    std::cout << 12985;
    for (int i = 0; i < 12985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12986(int& val) {
    func_in_12986(val);
    const int x = 12986;
    std::cout << 12986;
    for (int i = 0; i < 12986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12986(int& val) {
    const int x = 12986;
    std::cout << 12986;
    for (int i = 0; i < 12986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12987(int& val) {
    func_in_12987(val);
    const int x = 12987;
    std::cout << 12987;
    for (int i = 0; i < 12987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12987(int& val) {
    const int x = 12987;
    std::cout << 12987;
    for (int i = 0; i < 12987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12988(int& val) {
    func_in_12988(val);
    const int x = 12988;
    std::cout << 12988;
    for (int i = 0; i < 12988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12988(int& val) {
    const int x = 12988;
    std::cout << 12988;
    for (int i = 0; i < 12988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12989(int& val) {
    func_in_12989(val);
    const int x = 12989;
    std::cout << 12989;
    for (int i = 0; i < 12989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12989(int& val) {
    const int x = 12989;
    std::cout << 12989;
    for (int i = 0; i < 12989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12990(int& val) {
    func_in_12990(val);
    const int x = 12990;
    std::cout << 12990;
    for (int i = 0; i < 12990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12990(int& val) {
    const int x = 12990;
    std::cout << 12990;
    for (int i = 0; i < 12990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12991(int& val) {
    func_in_12991(val);
    const int x = 12991;
    std::cout << 12991;
    for (int i = 0; i < 12991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12991(int& val) {
    const int x = 12991;
    std::cout << 12991;
    for (int i = 0; i < 12991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12992(int& val) {
    func_in_12992(val);
    const int x = 12992;
    std::cout << 12992;
    for (int i = 0; i < 12992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12992(int& val) {
    const int x = 12992;
    std::cout << 12992;
    for (int i = 0; i < 12992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12993(int& val) {
    func_in_12993(val);
    const int x = 12993;
    std::cout << 12993;
    for (int i = 0; i < 12993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12993(int& val) {
    const int x = 12993;
    std::cout << 12993;
    for (int i = 0; i < 12993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12994(int& val) {
    func_in_12994(val);
    const int x = 12994;
    std::cout << 12994;
    for (int i = 0; i < 12994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12994(int& val) {
    const int x = 12994;
    std::cout << 12994;
    for (int i = 0; i < 12994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12995(int& val) {
    func_in_12995(val);
    const int x = 12995;
    std::cout << 12995;
    for (int i = 0; i < 12995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12995(int& val) {
    const int x = 12995;
    std::cout << 12995;
    for (int i = 0; i < 12995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12996(int& val) {
    func_in_12996(val);
    const int x = 12996;
    std::cout << 12996;
    for (int i = 0; i < 12996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12996(int& val) {
    const int x = 12996;
    std::cout << 12996;
    for (int i = 0; i < 12996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12997(int& val) {
    func_in_12997(val);
    const int x = 12997;
    std::cout << 12997;
    for (int i = 0; i < 12997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12997(int& val) {
    const int x = 12997;
    std::cout << 12997;
    for (int i = 0; i < 12997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12998(int& val) {
    func_in_12998(val);
    const int x = 12998;
    std::cout << 12998;
    for (int i = 0; i < 12998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12998(int& val) {
    const int x = 12998;
    std::cout << 12998;
    for (int i = 0; i < 12998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12999(int& val) {
    func_in_12999(val);
    const int x = 12999;
    std::cout << 12999;
    for (int i = 0; i < 12999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12999(int& val) {
    const int x = 12999;
    std::cout << 12999;
    for (int i = 0; i < 12999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13000(int& val) {
    func_in_13000(val);
    const int x = 13000;
    std::cout << 13000;
    for (int i = 0; i < 13000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13000(int& val) {
    const int x = 13000;
    std::cout << 13000;
    for (int i = 0; i < 13000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13001(int& val) {
    func_in_13001(val);
    const int x = 13001;
    std::cout << 13001;
    for (int i = 0; i < 13001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13001(int& val) {
    const int x = 13001;
    std::cout << 13001;
    for (int i = 0; i < 13001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13002(int& val) {
    func_in_13002(val);
    const int x = 13002;
    std::cout << 13002;
    for (int i = 0; i < 13002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13002(int& val) {
    const int x = 13002;
    std::cout << 13002;
    for (int i = 0; i < 13002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13003(int& val) {
    func_in_13003(val);
    const int x = 13003;
    std::cout << 13003;
    for (int i = 0; i < 13003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13003(int& val) {
    const int x = 13003;
    std::cout << 13003;
    for (int i = 0; i < 13003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13004(int& val) {
    func_in_13004(val);
    const int x = 13004;
    std::cout << 13004;
    for (int i = 0; i < 13004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13004(int& val) {
    const int x = 13004;
    std::cout << 13004;
    for (int i = 0; i < 13004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13005(int& val) {
    func_in_13005(val);
    const int x = 13005;
    std::cout << 13005;
    for (int i = 0; i < 13005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13005(int& val) {
    const int x = 13005;
    std::cout << 13005;
    for (int i = 0; i < 13005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13006(int& val) {
    func_in_13006(val);
    const int x = 13006;
    std::cout << 13006;
    for (int i = 0; i < 13006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13006(int& val) {
    const int x = 13006;
    std::cout << 13006;
    for (int i = 0; i < 13006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13007(int& val) {
    func_in_13007(val);
    const int x = 13007;
    std::cout << 13007;
    for (int i = 0; i < 13007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13007(int& val) {
    const int x = 13007;
    std::cout << 13007;
    for (int i = 0; i < 13007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13008(int& val) {
    func_in_13008(val);
    const int x = 13008;
    std::cout << 13008;
    for (int i = 0; i < 13008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13008(int& val) {
    const int x = 13008;
    std::cout << 13008;
    for (int i = 0; i < 13008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13009(int& val) {
    func_in_13009(val);
    const int x = 13009;
    std::cout << 13009;
    for (int i = 0; i < 13009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13009(int& val) {
    const int x = 13009;
    std::cout << 13009;
    for (int i = 0; i < 13009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13010(int& val) {
    func_in_13010(val);
    const int x = 13010;
    std::cout << 13010;
    for (int i = 0; i < 13010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13010(int& val) {
    const int x = 13010;
    std::cout << 13010;
    for (int i = 0; i < 13010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13011(int& val) {
    func_in_13011(val);
    const int x = 13011;
    std::cout << 13011;
    for (int i = 0; i < 13011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13011(int& val) {
    const int x = 13011;
    std::cout << 13011;
    for (int i = 0; i < 13011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13012(int& val) {
    func_in_13012(val);
    const int x = 13012;
    std::cout << 13012;
    for (int i = 0; i < 13012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13012(int& val) {
    const int x = 13012;
    std::cout << 13012;
    for (int i = 0; i < 13012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13013(int& val) {
    func_in_13013(val);
    const int x = 13013;
    std::cout << 13013;
    for (int i = 0; i < 13013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13013(int& val) {
    const int x = 13013;
    std::cout << 13013;
    for (int i = 0; i < 13013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13014(int& val) {
    func_in_13014(val);
    const int x = 13014;
    std::cout << 13014;
    for (int i = 0; i < 13014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13014(int& val) {
    const int x = 13014;
    std::cout << 13014;
    for (int i = 0; i < 13014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13015(int& val) {
    func_in_13015(val);
    const int x = 13015;
    std::cout << 13015;
    for (int i = 0; i < 13015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13015(int& val) {
    const int x = 13015;
    std::cout << 13015;
    for (int i = 0; i < 13015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13016(int& val) {
    func_in_13016(val);
    const int x = 13016;
    std::cout << 13016;
    for (int i = 0; i < 13016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13016(int& val) {
    const int x = 13016;
    std::cout << 13016;
    for (int i = 0; i < 13016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13017(int& val) {
    func_in_13017(val);
    const int x = 13017;
    std::cout << 13017;
    for (int i = 0; i < 13017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13017(int& val) {
    const int x = 13017;
    std::cout << 13017;
    for (int i = 0; i < 13017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13018(int& val) {
    func_in_13018(val);
    const int x = 13018;
    std::cout << 13018;
    for (int i = 0; i < 13018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13018(int& val) {
    const int x = 13018;
    std::cout << 13018;
    for (int i = 0; i < 13018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13019(int& val) {
    func_in_13019(val);
    const int x = 13019;
    std::cout << 13019;
    for (int i = 0; i < 13019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13019(int& val) {
    const int x = 13019;
    std::cout << 13019;
    for (int i = 0; i < 13019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13020(int& val) {
    func_in_13020(val);
    const int x = 13020;
    std::cout << 13020;
    for (int i = 0; i < 13020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13020(int& val) {
    const int x = 13020;
    std::cout << 13020;
    for (int i = 0; i < 13020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13021(int& val) {
    func_in_13021(val);
    const int x = 13021;
    std::cout << 13021;
    for (int i = 0; i < 13021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13021(int& val) {
    const int x = 13021;
    std::cout << 13021;
    for (int i = 0; i < 13021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13022(int& val) {
    func_in_13022(val);
    const int x = 13022;
    std::cout << 13022;
    for (int i = 0; i < 13022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13022(int& val) {
    const int x = 13022;
    std::cout << 13022;
    for (int i = 0; i < 13022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13023(int& val) {
    func_in_13023(val);
    const int x = 13023;
    std::cout << 13023;
    for (int i = 0; i < 13023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13023(int& val) {
    const int x = 13023;
    std::cout << 13023;
    for (int i = 0; i < 13023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13024(int& val) {
    func_in_13024(val);
    const int x = 13024;
    std::cout << 13024;
    for (int i = 0; i < 13024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13024(int& val) {
    const int x = 13024;
    std::cout << 13024;
    for (int i = 0; i < 13024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13025(int& val) {
    func_in_13025(val);
    const int x = 13025;
    std::cout << 13025;
    for (int i = 0; i < 13025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13025(int& val) {
    const int x = 13025;
    std::cout << 13025;
    for (int i = 0; i < 13025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13026(int& val) {
    func_in_13026(val);
    const int x = 13026;
    std::cout << 13026;
    for (int i = 0; i < 13026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13026(int& val) {
    const int x = 13026;
    std::cout << 13026;
    for (int i = 0; i < 13026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13027(int& val) {
    func_in_13027(val);
    const int x = 13027;
    std::cout << 13027;
    for (int i = 0; i < 13027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13027(int& val) {
    const int x = 13027;
    std::cout << 13027;
    for (int i = 0; i < 13027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13028(int& val) {
    func_in_13028(val);
    const int x = 13028;
    std::cout << 13028;
    for (int i = 0; i < 13028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13028(int& val) {
    const int x = 13028;
    std::cout << 13028;
    for (int i = 0; i < 13028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13029(int& val) {
    func_in_13029(val);
    const int x = 13029;
    std::cout << 13029;
    for (int i = 0; i < 13029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13029(int& val) {
    const int x = 13029;
    std::cout << 13029;
    for (int i = 0; i < 13029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13030(int& val) {
    func_in_13030(val);
    const int x = 13030;
    std::cout << 13030;
    for (int i = 0; i < 13030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13030(int& val) {
    const int x = 13030;
    std::cout << 13030;
    for (int i = 0; i < 13030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13031(int& val) {
    func_in_13031(val);
    const int x = 13031;
    std::cout << 13031;
    for (int i = 0; i < 13031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13031(int& val) {
    const int x = 13031;
    std::cout << 13031;
    for (int i = 0; i < 13031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13032(int& val) {
    func_in_13032(val);
    const int x = 13032;
    std::cout << 13032;
    for (int i = 0; i < 13032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13032(int& val) {
    const int x = 13032;
    std::cout << 13032;
    for (int i = 0; i < 13032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13033(int& val) {
    func_in_13033(val);
    const int x = 13033;
    std::cout << 13033;
    for (int i = 0; i < 13033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13033(int& val) {
    const int x = 13033;
    std::cout << 13033;
    for (int i = 0; i < 13033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13034(int& val) {
    func_in_13034(val);
    const int x = 13034;
    std::cout << 13034;
    for (int i = 0; i < 13034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13034(int& val) {
    const int x = 13034;
    std::cout << 13034;
    for (int i = 0; i < 13034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13035(int& val) {
    func_in_13035(val);
    const int x = 13035;
    std::cout << 13035;
    for (int i = 0; i < 13035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13035(int& val) {
    const int x = 13035;
    std::cout << 13035;
    for (int i = 0; i < 13035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13036(int& val) {
    func_in_13036(val);
    const int x = 13036;
    std::cout << 13036;
    for (int i = 0; i < 13036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13036(int& val) {
    const int x = 13036;
    std::cout << 13036;
    for (int i = 0; i < 13036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13037(int& val) {
    func_in_13037(val);
    const int x = 13037;
    std::cout << 13037;
    for (int i = 0; i < 13037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13037(int& val) {
    const int x = 13037;
    std::cout << 13037;
    for (int i = 0; i < 13037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13038(int& val) {
    func_in_13038(val);
    const int x = 13038;
    std::cout << 13038;
    for (int i = 0; i < 13038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13038(int& val) {
    const int x = 13038;
    std::cout << 13038;
    for (int i = 0; i < 13038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13039(int& val) {
    func_in_13039(val);
    const int x = 13039;
    std::cout << 13039;
    for (int i = 0; i < 13039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13039(int& val) {
    const int x = 13039;
    std::cout << 13039;
    for (int i = 0; i < 13039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13040(int& val) {
    func_in_13040(val);
    const int x = 13040;
    std::cout << 13040;
    for (int i = 0; i < 13040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13040(int& val) {
    const int x = 13040;
    std::cout << 13040;
    for (int i = 0; i < 13040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13041(int& val) {
    func_in_13041(val);
    const int x = 13041;
    std::cout << 13041;
    for (int i = 0; i < 13041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13041(int& val) {
    const int x = 13041;
    std::cout << 13041;
    for (int i = 0; i < 13041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13042(int& val) {
    func_in_13042(val);
    const int x = 13042;
    std::cout << 13042;
    for (int i = 0; i < 13042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13042(int& val) {
    const int x = 13042;
    std::cout << 13042;
    for (int i = 0; i < 13042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13043(int& val) {
    func_in_13043(val);
    const int x = 13043;
    std::cout << 13043;
    for (int i = 0; i < 13043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13043(int& val) {
    const int x = 13043;
    std::cout << 13043;
    for (int i = 0; i < 13043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13044(int& val) {
    func_in_13044(val);
    const int x = 13044;
    std::cout << 13044;
    for (int i = 0; i < 13044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13044(int& val) {
    const int x = 13044;
    std::cout << 13044;
    for (int i = 0; i < 13044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13045(int& val) {
    func_in_13045(val);
    const int x = 13045;
    std::cout << 13045;
    for (int i = 0; i < 13045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13045(int& val) {
    const int x = 13045;
    std::cout << 13045;
    for (int i = 0; i < 13045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13046(int& val) {
    func_in_13046(val);
    const int x = 13046;
    std::cout << 13046;
    for (int i = 0; i < 13046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13046(int& val) {
    const int x = 13046;
    std::cout << 13046;
    for (int i = 0; i < 13046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13047(int& val) {
    func_in_13047(val);
    const int x = 13047;
    std::cout << 13047;
    for (int i = 0; i < 13047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13047(int& val) {
    const int x = 13047;
    std::cout << 13047;
    for (int i = 0; i < 13047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13048(int& val) {
    func_in_13048(val);
    const int x = 13048;
    std::cout << 13048;
    for (int i = 0; i < 13048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13048(int& val) {
    const int x = 13048;
    std::cout << 13048;
    for (int i = 0; i < 13048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13049(int& val) {
    func_in_13049(val);
    const int x = 13049;
    std::cout << 13049;
    for (int i = 0; i < 13049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13049(int& val) {
    const int x = 13049;
    std::cout << 13049;
    for (int i = 0; i < 13049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13050(int& val) {
    func_in_13050(val);
    const int x = 13050;
    std::cout << 13050;
    for (int i = 0; i < 13050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13050(int& val) {
    const int x = 13050;
    std::cout << 13050;
    for (int i = 0; i < 13050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13051(int& val) {
    func_in_13051(val);
    const int x = 13051;
    std::cout << 13051;
    for (int i = 0; i < 13051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13051(int& val) {
    const int x = 13051;
    std::cout << 13051;
    for (int i = 0; i < 13051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13052(int& val) {
    func_in_13052(val);
    const int x = 13052;
    std::cout << 13052;
    for (int i = 0; i < 13052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13052(int& val) {
    const int x = 13052;
    std::cout << 13052;
    for (int i = 0; i < 13052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13053(int& val) {
    func_in_13053(val);
    const int x = 13053;
    std::cout << 13053;
    for (int i = 0; i < 13053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13053(int& val) {
    const int x = 13053;
    std::cout << 13053;
    for (int i = 0; i < 13053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13054(int& val) {
    func_in_13054(val);
    const int x = 13054;
    std::cout << 13054;
    for (int i = 0; i < 13054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13054(int& val) {
    const int x = 13054;
    std::cout << 13054;
    for (int i = 0; i < 13054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13055(int& val) {
    func_in_13055(val);
    const int x = 13055;
    std::cout << 13055;
    for (int i = 0; i < 13055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13055(int& val) {
    const int x = 13055;
    std::cout << 13055;
    for (int i = 0; i < 13055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13056(int& val) {
    func_in_13056(val);
    const int x = 13056;
    std::cout << 13056;
    for (int i = 0; i < 13056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13056(int& val) {
    const int x = 13056;
    std::cout << 13056;
    for (int i = 0; i < 13056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13057(int& val) {
    func_in_13057(val);
    const int x = 13057;
    std::cout << 13057;
    for (int i = 0; i < 13057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13057(int& val) {
    const int x = 13057;
    std::cout << 13057;
    for (int i = 0; i < 13057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13058(int& val) {
    func_in_13058(val);
    const int x = 13058;
    std::cout << 13058;
    for (int i = 0; i < 13058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13058(int& val) {
    const int x = 13058;
    std::cout << 13058;
    for (int i = 0; i < 13058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13059(int& val) {
    func_in_13059(val);
    const int x = 13059;
    std::cout << 13059;
    for (int i = 0; i < 13059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13059(int& val) {
    const int x = 13059;
    std::cout << 13059;
    for (int i = 0; i < 13059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13060(int& val) {
    func_in_13060(val);
    const int x = 13060;
    std::cout << 13060;
    for (int i = 0; i < 13060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13060(int& val) {
    const int x = 13060;
    std::cout << 13060;
    for (int i = 0; i < 13060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13061(int& val) {
    func_in_13061(val);
    const int x = 13061;
    std::cout << 13061;
    for (int i = 0; i < 13061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13061(int& val) {
    const int x = 13061;
    std::cout << 13061;
    for (int i = 0; i < 13061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13062(int& val) {
    func_in_13062(val);
    const int x = 13062;
    std::cout << 13062;
    for (int i = 0; i < 13062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13062(int& val) {
    const int x = 13062;
    std::cout << 13062;
    for (int i = 0; i < 13062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13063(int& val) {
    func_in_13063(val);
    const int x = 13063;
    std::cout << 13063;
    for (int i = 0; i < 13063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13063(int& val) {
    const int x = 13063;
    std::cout << 13063;
    for (int i = 0; i < 13063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13064(int& val) {
    func_in_13064(val);
    const int x = 13064;
    std::cout << 13064;
    for (int i = 0; i < 13064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13064(int& val) {
    const int x = 13064;
    std::cout << 13064;
    for (int i = 0; i < 13064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13065(int& val) {
    func_in_13065(val);
    const int x = 13065;
    std::cout << 13065;
    for (int i = 0; i < 13065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13065(int& val) {
    const int x = 13065;
    std::cout << 13065;
    for (int i = 0; i < 13065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13066(int& val) {
    func_in_13066(val);
    const int x = 13066;
    std::cout << 13066;
    for (int i = 0; i < 13066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13066(int& val) {
    const int x = 13066;
    std::cout << 13066;
    for (int i = 0; i < 13066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13067(int& val) {
    func_in_13067(val);
    const int x = 13067;
    std::cout << 13067;
    for (int i = 0; i < 13067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13067(int& val) {
    const int x = 13067;
    std::cout << 13067;
    for (int i = 0; i < 13067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13068(int& val) {
    func_in_13068(val);
    const int x = 13068;
    std::cout << 13068;
    for (int i = 0; i < 13068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13068(int& val) {
    const int x = 13068;
    std::cout << 13068;
    for (int i = 0; i < 13068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13069(int& val) {
    func_in_13069(val);
    const int x = 13069;
    std::cout << 13069;
    for (int i = 0; i < 13069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13069(int& val) {
    const int x = 13069;
    std::cout << 13069;
    for (int i = 0; i < 13069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13070(int& val) {
    func_in_13070(val);
    const int x = 13070;
    std::cout << 13070;
    for (int i = 0; i < 13070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13070(int& val) {
    const int x = 13070;
    std::cout << 13070;
    for (int i = 0; i < 13070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13071(int& val) {
    func_in_13071(val);
    const int x = 13071;
    std::cout << 13071;
    for (int i = 0; i < 13071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13071(int& val) {
    const int x = 13071;
    std::cout << 13071;
    for (int i = 0; i < 13071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13072(int& val) {
    func_in_13072(val);
    const int x = 13072;
    std::cout << 13072;
    for (int i = 0; i < 13072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13072(int& val) {
    const int x = 13072;
    std::cout << 13072;
    for (int i = 0; i < 13072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13073(int& val) {
    func_in_13073(val);
    const int x = 13073;
    std::cout << 13073;
    for (int i = 0; i < 13073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13073(int& val) {
    const int x = 13073;
    std::cout << 13073;
    for (int i = 0; i < 13073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13074(int& val) {
    func_in_13074(val);
    const int x = 13074;
    std::cout << 13074;
    for (int i = 0; i < 13074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13074(int& val) {
    const int x = 13074;
    std::cout << 13074;
    for (int i = 0; i < 13074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13075(int& val) {
    func_in_13075(val);
    const int x = 13075;
    std::cout << 13075;
    for (int i = 0; i < 13075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13075(int& val) {
    const int x = 13075;
    std::cout << 13075;
    for (int i = 0; i < 13075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13076(int& val) {
    func_in_13076(val);
    const int x = 13076;
    std::cout << 13076;
    for (int i = 0; i < 13076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13076(int& val) {
    const int x = 13076;
    std::cout << 13076;
    for (int i = 0; i < 13076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13077(int& val) {
    func_in_13077(val);
    const int x = 13077;
    std::cout << 13077;
    for (int i = 0; i < 13077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13077(int& val) {
    const int x = 13077;
    std::cout << 13077;
    for (int i = 0; i < 13077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13078(int& val) {
    func_in_13078(val);
    const int x = 13078;
    std::cout << 13078;
    for (int i = 0; i < 13078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13078(int& val) {
    const int x = 13078;
    std::cout << 13078;
    for (int i = 0; i < 13078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13079(int& val) {
    func_in_13079(val);
    const int x = 13079;
    std::cout << 13079;
    for (int i = 0; i < 13079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13079(int& val) {
    const int x = 13079;
    std::cout << 13079;
    for (int i = 0; i < 13079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13080(int& val) {
    func_in_13080(val);
    const int x = 13080;
    std::cout << 13080;
    for (int i = 0; i < 13080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13080(int& val) {
    const int x = 13080;
    std::cout << 13080;
    for (int i = 0; i < 13080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13081(int& val) {
    func_in_13081(val);
    const int x = 13081;
    std::cout << 13081;
    for (int i = 0; i < 13081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13081(int& val) {
    const int x = 13081;
    std::cout << 13081;
    for (int i = 0; i < 13081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13082(int& val) {
    func_in_13082(val);
    const int x = 13082;
    std::cout << 13082;
    for (int i = 0; i < 13082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13082(int& val) {
    const int x = 13082;
    std::cout << 13082;
    for (int i = 0; i < 13082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13083(int& val) {
    func_in_13083(val);
    const int x = 13083;
    std::cout << 13083;
    for (int i = 0; i < 13083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13083(int& val) {
    const int x = 13083;
    std::cout << 13083;
    for (int i = 0; i < 13083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13084(int& val) {
    func_in_13084(val);
    const int x = 13084;
    std::cout << 13084;
    for (int i = 0; i < 13084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13084(int& val) {
    const int x = 13084;
    std::cout << 13084;
    for (int i = 0; i < 13084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13085(int& val) {
    func_in_13085(val);
    const int x = 13085;
    std::cout << 13085;
    for (int i = 0; i < 13085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13085(int& val) {
    const int x = 13085;
    std::cout << 13085;
    for (int i = 0; i < 13085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13086(int& val) {
    func_in_13086(val);
    const int x = 13086;
    std::cout << 13086;
    for (int i = 0; i < 13086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13086(int& val) {
    const int x = 13086;
    std::cout << 13086;
    for (int i = 0; i < 13086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13087(int& val) {
    func_in_13087(val);
    const int x = 13087;
    std::cout << 13087;
    for (int i = 0; i < 13087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13087(int& val) {
    const int x = 13087;
    std::cout << 13087;
    for (int i = 0; i < 13087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13088(int& val) {
    func_in_13088(val);
    const int x = 13088;
    std::cout << 13088;
    for (int i = 0; i < 13088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13088(int& val) {
    const int x = 13088;
    std::cout << 13088;
    for (int i = 0; i < 13088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13089(int& val) {
    func_in_13089(val);
    const int x = 13089;
    std::cout << 13089;
    for (int i = 0; i < 13089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13089(int& val) {
    const int x = 13089;
    std::cout << 13089;
    for (int i = 0; i < 13089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13090(int& val) {
    func_in_13090(val);
    const int x = 13090;
    std::cout << 13090;
    for (int i = 0; i < 13090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13090(int& val) {
    const int x = 13090;
    std::cout << 13090;
    for (int i = 0; i < 13090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13091(int& val) {
    func_in_13091(val);
    const int x = 13091;
    std::cout << 13091;
    for (int i = 0; i < 13091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13091(int& val) {
    const int x = 13091;
    std::cout << 13091;
    for (int i = 0; i < 13091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13092(int& val) {
    func_in_13092(val);
    const int x = 13092;
    std::cout << 13092;
    for (int i = 0; i < 13092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13092(int& val) {
    const int x = 13092;
    std::cout << 13092;
    for (int i = 0; i < 13092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13093(int& val) {
    func_in_13093(val);
    const int x = 13093;
    std::cout << 13093;
    for (int i = 0; i < 13093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13093(int& val) {
    const int x = 13093;
    std::cout << 13093;
    for (int i = 0; i < 13093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13094(int& val) {
    func_in_13094(val);
    const int x = 13094;
    std::cout << 13094;
    for (int i = 0; i < 13094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13094(int& val) {
    const int x = 13094;
    std::cout << 13094;
    for (int i = 0; i < 13094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13095(int& val) {
    func_in_13095(val);
    const int x = 13095;
    std::cout << 13095;
    for (int i = 0; i < 13095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13095(int& val) {
    const int x = 13095;
    std::cout << 13095;
    for (int i = 0; i < 13095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13096(int& val) {
    func_in_13096(val);
    const int x = 13096;
    std::cout << 13096;
    for (int i = 0; i < 13096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13096(int& val) {
    const int x = 13096;
    std::cout << 13096;
    for (int i = 0; i < 13096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13097(int& val) {
    func_in_13097(val);
    const int x = 13097;
    std::cout << 13097;
    for (int i = 0; i < 13097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13097(int& val) {
    const int x = 13097;
    std::cout << 13097;
    for (int i = 0; i < 13097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13098(int& val) {
    func_in_13098(val);
    const int x = 13098;
    std::cout << 13098;
    for (int i = 0; i < 13098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13098(int& val) {
    const int x = 13098;
    std::cout << 13098;
    for (int i = 0; i < 13098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13099(int& val) {
    func_in_13099(val);
    const int x = 13099;
    std::cout << 13099;
    for (int i = 0; i < 13099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13099(int& val) {
    const int x = 13099;
    std::cout << 13099;
    for (int i = 0; i < 13099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13100(int& val) {
    func_in_13100(val);
    const int x = 13100;
    std::cout << 13100;
    for (int i = 0; i < 13100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13100(int& val) {
    const int x = 13100;
    std::cout << 13100;
    for (int i = 0; i < 13100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13101(int& val) {
    func_in_13101(val);
    const int x = 13101;
    std::cout << 13101;
    for (int i = 0; i < 13101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13101(int& val) {
    const int x = 13101;
    std::cout << 13101;
    for (int i = 0; i < 13101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13102(int& val) {
    func_in_13102(val);
    const int x = 13102;
    std::cout << 13102;
    for (int i = 0; i < 13102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13102(int& val) {
    const int x = 13102;
    std::cout << 13102;
    for (int i = 0; i < 13102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13103(int& val) {
    func_in_13103(val);
    const int x = 13103;
    std::cout << 13103;
    for (int i = 0; i < 13103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13103(int& val) {
    const int x = 13103;
    std::cout << 13103;
    for (int i = 0; i < 13103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13104(int& val) {
    func_in_13104(val);
    const int x = 13104;
    std::cout << 13104;
    for (int i = 0; i < 13104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13104(int& val) {
    const int x = 13104;
    std::cout << 13104;
    for (int i = 0; i < 13104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13105(int& val) {
    func_in_13105(val);
    const int x = 13105;
    std::cout << 13105;
    for (int i = 0; i < 13105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13105(int& val) {
    const int x = 13105;
    std::cout << 13105;
    for (int i = 0; i < 13105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13106(int& val) {
    func_in_13106(val);
    const int x = 13106;
    std::cout << 13106;
    for (int i = 0; i < 13106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13106(int& val) {
    const int x = 13106;
    std::cout << 13106;
    for (int i = 0; i < 13106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13107(int& val) {
    func_in_13107(val);
    const int x = 13107;
    std::cout << 13107;
    for (int i = 0; i < 13107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13107(int& val) {
    const int x = 13107;
    std::cout << 13107;
    for (int i = 0; i < 13107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13108(int& val) {
    func_in_13108(val);
    const int x = 13108;
    std::cout << 13108;
    for (int i = 0; i < 13108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13108(int& val) {
    const int x = 13108;
    std::cout << 13108;
    for (int i = 0; i < 13108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13109(int& val) {
    func_in_13109(val);
    const int x = 13109;
    std::cout << 13109;
    for (int i = 0; i < 13109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13109(int& val) {
    const int x = 13109;
    std::cout << 13109;
    for (int i = 0; i < 13109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13110(int& val) {
    func_in_13110(val);
    const int x = 13110;
    std::cout << 13110;
    for (int i = 0; i < 13110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13110(int& val) {
    const int x = 13110;
    std::cout << 13110;
    for (int i = 0; i < 13110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13111(int& val) {
    func_in_13111(val);
    const int x = 13111;
    std::cout << 13111;
    for (int i = 0; i < 13111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13111(int& val) {
    const int x = 13111;
    std::cout << 13111;
    for (int i = 0; i < 13111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13112(int& val) {
    func_in_13112(val);
    const int x = 13112;
    std::cout << 13112;
    for (int i = 0; i < 13112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13112(int& val) {
    const int x = 13112;
    std::cout << 13112;
    for (int i = 0; i < 13112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13113(int& val) {
    func_in_13113(val);
    const int x = 13113;
    std::cout << 13113;
    for (int i = 0; i < 13113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13113(int& val) {
    const int x = 13113;
    std::cout << 13113;
    for (int i = 0; i < 13113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13114(int& val) {
    func_in_13114(val);
    const int x = 13114;
    std::cout << 13114;
    for (int i = 0; i < 13114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13114(int& val) {
    const int x = 13114;
    std::cout << 13114;
    for (int i = 0; i < 13114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13115(int& val) {
    func_in_13115(val);
    const int x = 13115;
    std::cout << 13115;
    for (int i = 0; i < 13115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13115(int& val) {
    const int x = 13115;
    std::cout << 13115;
    for (int i = 0; i < 13115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13116(int& val) {
    func_in_13116(val);
    const int x = 13116;
    std::cout << 13116;
    for (int i = 0; i < 13116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13116(int& val) {
    const int x = 13116;
    std::cout << 13116;
    for (int i = 0; i < 13116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13117(int& val) {
    func_in_13117(val);
    const int x = 13117;
    std::cout << 13117;
    for (int i = 0; i < 13117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13117(int& val) {
    const int x = 13117;
    std::cout << 13117;
    for (int i = 0; i < 13117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13118(int& val) {
    func_in_13118(val);
    const int x = 13118;
    std::cout << 13118;
    for (int i = 0; i < 13118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13118(int& val) {
    const int x = 13118;
    std::cout << 13118;
    for (int i = 0; i < 13118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13119(int& val) {
    func_in_13119(val);
    const int x = 13119;
    std::cout << 13119;
    for (int i = 0; i < 13119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13119(int& val) {
    const int x = 13119;
    std::cout << 13119;
    for (int i = 0; i < 13119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13120(int& val) {
    func_in_13120(val);
    const int x = 13120;
    std::cout << 13120;
    for (int i = 0; i < 13120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13120(int& val) {
    const int x = 13120;
    std::cout << 13120;
    for (int i = 0; i < 13120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13121(int& val) {
    func_in_13121(val);
    const int x = 13121;
    std::cout << 13121;
    for (int i = 0; i < 13121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13121(int& val) {
    const int x = 13121;
    std::cout << 13121;
    for (int i = 0; i < 13121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13122(int& val) {
    func_in_13122(val);
    const int x = 13122;
    std::cout << 13122;
    for (int i = 0; i < 13122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13122(int& val) {
    const int x = 13122;
    std::cout << 13122;
    for (int i = 0; i < 13122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13123(int& val) {
    func_in_13123(val);
    const int x = 13123;
    std::cout << 13123;
    for (int i = 0; i < 13123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13123(int& val) {
    const int x = 13123;
    std::cout << 13123;
    for (int i = 0; i < 13123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13124(int& val) {
    func_in_13124(val);
    const int x = 13124;
    std::cout << 13124;
    for (int i = 0; i < 13124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13124(int& val) {
    const int x = 13124;
    std::cout << 13124;
    for (int i = 0; i < 13124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13125(int& val) {
    func_in_13125(val);
    const int x = 13125;
    std::cout << 13125;
    for (int i = 0; i < 13125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13125(int& val) {
    const int x = 13125;
    std::cout << 13125;
    for (int i = 0; i < 13125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13126(int& val) {
    func_in_13126(val);
    const int x = 13126;
    std::cout << 13126;
    for (int i = 0; i < 13126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13126(int& val) {
    const int x = 13126;
    std::cout << 13126;
    for (int i = 0; i < 13126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13127(int& val) {
    func_in_13127(val);
    const int x = 13127;
    std::cout << 13127;
    for (int i = 0; i < 13127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13127(int& val) {
    const int x = 13127;
    std::cout << 13127;
    for (int i = 0; i < 13127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13128(int& val) {
    func_in_13128(val);
    const int x = 13128;
    std::cout << 13128;
    for (int i = 0; i < 13128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13128(int& val) {
    const int x = 13128;
    std::cout << 13128;
    for (int i = 0; i < 13128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13129(int& val) {
    func_in_13129(val);
    const int x = 13129;
    std::cout << 13129;
    for (int i = 0; i < 13129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13129(int& val) {
    const int x = 13129;
    std::cout << 13129;
    for (int i = 0; i < 13129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13130(int& val) {
    func_in_13130(val);
    const int x = 13130;
    std::cout << 13130;
    for (int i = 0; i < 13130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13130(int& val) {
    const int x = 13130;
    std::cout << 13130;
    for (int i = 0; i < 13130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13131(int& val) {
    func_in_13131(val);
    const int x = 13131;
    std::cout << 13131;
    for (int i = 0; i < 13131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13131(int& val) {
    const int x = 13131;
    std::cout << 13131;
    for (int i = 0; i < 13131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13132(int& val) {
    func_in_13132(val);
    const int x = 13132;
    std::cout << 13132;
    for (int i = 0; i < 13132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13132(int& val) {
    const int x = 13132;
    std::cout << 13132;
    for (int i = 0; i < 13132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13133(int& val) {
    func_in_13133(val);
    const int x = 13133;
    std::cout << 13133;
    for (int i = 0; i < 13133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13133(int& val) {
    const int x = 13133;
    std::cout << 13133;
    for (int i = 0; i < 13133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13134(int& val) {
    func_in_13134(val);
    const int x = 13134;
    std::cout << 13134;
    for (int i = 0; i < 13134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13134(int& val) {
    const int x = 13134;
    std::cout << 13134;
    for (int i = 0; i < 13134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13135(int& val) {
    func_in_13135(val);
    const int x = 13135;
    std::cout << 13135;
    for (int i = 0; i < 13135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13135(int& val) {
    const int x = 13135;
    std::cout << 13135;
    for (int i = 0; i < 13135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13136(int& val) {
    func_in_13136(val);
    const int x = 13136;
    std::cout << 13136;
    for (int i = 0; i < 13136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13136(int& val) {
    const int x = 13136;
    std::cout << 13136;
    for (int i = 0; i < 13136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13137(int& val) {
    func_in_13137(val);
    const int x = 13137;
    std::cout << 13137;
    for (int i = 0; i < 13137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13137(int& val) {
    const int x = 13137;
    std::cout << 13137;
    for (int i = 0; i < 13137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13138(int& val) {
    func_in_13138(val);
    const int x = 13138;
    std::cout << 13138;
    for (int i = 0; i < 13138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13138(int& val) {
    const int x = 13138;
    std::cout << 13138;
    for (int i = 0; i < 13138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13139(int& val) {
    func_in_13139(val);
    const int x = 13139;
    std::cout << 13139;
    for (int i = 0; i < 13139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13139(int& val) {
    const int x = 13139;
    std::cout << 13139;
    for (int i = 0; i < 13139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13140(int& val) {
    func_in_13140(val);
    const int x = 13140;
    std::cout << 13140;
    for (int i = 0; i < 13140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13140(int& val) {
    const int x = 13140;
    std::cout << 13140;
    for (int i = 0; i < 13140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13141(int& val) {
    func_in_13141(val);
    const int x = 13141;
    std::cout << 13141;
    for (int i = 0; i < 13141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13141(int& val) {
    const int x = 13141;
    std::cout << 13141;
    for (int i = 0; i < 13141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13142(int& val) {
    func_in_13142(val);
    const int x = 13142;
    std::cout << 13142;
    for (int i = 0; i < 13142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13142(int& val) {
    const int x = 13142;
    std::cout << 13142;
    for (int i = 0; i < 13142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13143(int& val) {
    func_in_13143(val);
    const int x = 13143;
    std::cout << 13143;
    for (int i = 0; i < 13143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13143(int& val) {
    const int x = 13143;
    std::cout << 13143;
    for (int i = 0; i < 13143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13144(int& val) {
    func_in_13144(val);
    const int x = 13144;
    std::cout << 13144;
    for (int i = 0; i < 13144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13144(int& val) {
    const int x = 13144;
    std::cout << 13144;
    for (int i = 0; i < 13144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13145(int& val) {
    func_in_13145(val);
    const int x = 13145;
    std::cout << 13145;
    for (int i = 0; i < 13145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13145(int& val) {
    const int x = 13145;
    std::cout << 13145;
    for (int i = 0; i < 13145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13146(int& val) {
    func_in_13146(val);
    const int x = 13146;
    std::cout << 13146;
    for (int i = 0; i < 13146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13146(int& val) {
    const int x = 13146;
    std::cout << 13146;
    for (int i = 0; i < 13146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13147(int& val) {
    func_in_13147(val);
    const int x = 13147;
    std::cout << 13147;
    for (int i = 0; i < 13147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13147(int& val) {
    const int x = 13147;
    std::cout << 13147;
    for (int i = 0; i < 13147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13148(int& val) {
    func_in_13148(val);
    const int x = 13148;
    std::cout << 13148;
    for (int i = 0; i < 13148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13148(int& val) {
    const int x = 13148;
    std::cout << 13148;
    for (int i = 0; i < 13148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13149(int& val) {
    func_in_13149(val);
    const int x = 13149;
    std::cout << 13149;
    for (int i = 0; i < 13149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13149(int& val) {
    const int x = 13149;
    std::cout << 13149;
    for (int i = 0; i < 13149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13150(int& val) {
    func_in_13150(val);
    const int x = 13150;
    std::cout << 13150;
    for (int i = 0; i < 13150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13150(int& val) {
    const int x = 13150;
    std::cout << 13150;
    for (int i = 0; i < 13150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13151(int& val) {
    func_in_13151(val);
    const int x = 13151;
    std::cout << 13151;
    for (int i = 0; i < 13151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13151(int& val) {
    const int x = 13151;
    std::cout << 13151;
    for (int i = 0; i < 13151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13152(int& val) {
    func_in_13152(val);
    const int x = 13152;
    std::cout << 13152;
    for (int i = 0; i < 13152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13152(int& val) {
    const int x = 13152;
    std::cout << 13152;
    for (int i = 0; i < 13152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13153(int& val) {
    func_in_13153(val);
    const int x = 13153;
    std::cout << 13153;
    for (int i = 0; i < 13153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13153(int& val) {
    const int x = 13153;
    std::cout << 13153;
    for (int i = 0; i < 13153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13154(int& val) {
    func_in_13154(val);
    const int x = 13154;
    std::cout << 13154;
    for (int i = 0; i < 13154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13154(int& val) {
    const int x = 13154;
    std::cout << 13154;
    for (int i = 0; i < 13154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13155(int& val) {
    func_in_13155(val);
    const int x = 13155;
    std::cout << 13155;
    for (int i = 0; i < 13155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13155(int& val) {
    const int x = 13155;
    std::cout << 13155;
    for (int i = 0; i < 13155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13156(int& val) {
    func_in_13156(val);
    const int x = 13156;
    std::cout << 13156;
    for (int i = 0; i < 13156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13156(int& val) {
    const int x = 13156;
    std::cout << 13156;
    for (int i = 0; i < 13156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13157(int& val) {
    func_in_13157(val);
    const int x = 13157;
    std::cout << 13157;
    for (int i = 0; i < 13157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13157(int& val) {
    const int x = 13157;
    std::cout << 13157;
    for (int i = 0; i < 13157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13158(int& val) {
    func_in_13158(val);
    const int x = 13158;
    std::cout << 13158;
    for (int i = 0; i < 13158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13158(int& val) {
    const int x = 13158;
    std::cout << 13158;
    for (int i = 0; i < 13158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13159(int& val) {
    func_in_13159(val);
    const int x = 13159;
    std::cout << 13159;
    for (int i = 0; i < 13159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13159(int& val) {
    const int x = 13159;
    std::cout << 13159;
    for (int i = 0; i < 13159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13160(int& val) {
    func_in_13160(val);
    const int x = 13160;
    std::cout << 13160;
    for (int i = 0; i < 13160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13160(int& val) {
    const int x = 13160;
    std::cout << 13160;
    for (int i = 0; i < 13160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13161(int& val) {
    func_in_13161(val);
    const int x = 13161;
    std::cout << 13161;
    for (int i = 0; i < 13161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13161(int& val) {
    const int x = 13161;
    std::cout << 13161;
    for (int i = 0; i < 13161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13162(int& val) {
    func_in_13162(val);
    const int x = 13162;
    std::cout << 13162;
    for (int i = 0; i < 13162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13162(int& val) {
    const int x = 13162;
    std::cout << 13162;
    for (int i = 0; i < 13162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13163(int& val) {
    func_in_13163(val);
    const int x = 13163;
    std::cout << 13163;
    for (int i = 0; i < 13163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13163(int& val) {
    const int x = 13163;
    std::cout << 13163;
    for (int i = 0; i < 13163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13164(int& val) {
    func_in_13164(val);
    const int x = 13164;
    std::cout << 13164;
    for (int i = 0; i < 13164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13164(int& val) {
    const int x = 13164;
    std::cout << 13164;
    for (int i = 0; i < 13164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13165(int& val) {
    func_in_13165(val);
    const int x = 13165;
    std::cout << 13165;
    for (int i = 0; i < 13165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13165(int& val) {
    const int x = 13165;
    std::cout << 13165;
    for (int i = 0; i < 13165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13166(int& val) {
    func_in_13166(val);
    const int x = 13166;
    std::cout << 13166;
    for (int i = 0; i < 13166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13166(int& val) {
    const int x = 13166;
    std::cout << 13166;
    for (int i = 0; i < 13166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13167(int& val) {
    func_in_13167(val);
    const int x = 13167;
    std::cout << 13167;
    for (int i = 0; i < 13167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13167(int& val) {
    const int x = 13167;
    std::cout << 13167;
    for (int i = 0; i < 13167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13168(int& val) {
    func_in_13168(val);
    const int x = 13168;
    std::cout << 13168;
    for (int i = 0; i < 13168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13168(int& val) {
    const int x = 13168;
    std::cout << 13168;
    for (int i = 0; i < 13168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13169(int& val) {
    func_in_13169(val);
    const int x = 13169;
    std::cout << 13169;
    for (int i = 0; i < 13169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13169(int& val) {
    const int x = 13169;
    std::cout << 13169;
    for (int i = 0; i < 13169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13170(int& val) {
    func_in_13170(val);
    const int x = 13170;
    std::cout << 13170;
    for (int i = 0; i < 13170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13170(int& val) {
    const int x = 13170;
    std::cout << 13170;
    for (int i = 0; i < 13170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13171(int& val) {
    func_in_13171(val);
    const int x = 13171;
    std::cout << 13171;
    for (int i = 0; i < 13171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13171(int& val) {
    const int x = 13171;
    std::cout << 13171;
    for (int i = 0; i < 13171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13172(int& val) {
    func_in_13172(val);
    const int x = 13172;
    std::cout << 13172;
    for (int i = 0; i < 13172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13172(int& val) {
    const int x = 13172;
    std::cout << 13172;
    for (int i = 0; i < 13172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13173(int& val) {
    func_in_13173(val);
    const int x = 13173;
    std::cout << 13173;
    for (int i = 0; i < 13173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13173(int& val) {
    const int x = 13173;
    std::cout << 13173;
    for (int i = 0; i < 13173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13174(int& val) {
    func_in_13174(val);
    const int x = 13174;
    std::cout << 13174;
    for (int i = 0; i < 13174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13174(int& val) {
    const int x = 13174;
    std::cout << 13174;
    for (int i = 0; i < 13174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13175(int& val) {
    func_in_13175(val);
    const int x = 13175;
    std::cout << 13175;
    for (int i = 0; i < 13175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13175(int& val) {
    const int x = 13175;
    std::cout << 13175;
    for (int i = 0; i < 13175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13176(int& val) {
    func_in_13176(val);
    const int x = 13176;
    std::cout << 13176;
    for (int i = 0; i < 13176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13176(int& val) {
    const int x = 13176;
    std::cout << 13176;
    for (int i = 0; i < 13176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13177(int& val) {
    func_in_13177(val);
    const int x = 13177;
    std::cout << 13177;
    for (int i = 0; i < 13177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13177(int& val) {
    const int x = 13177;
    std::cout << 13177;
    for (int i = 0; i < 13177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13178(int& val) {
    func_in_13178(val);
    const int x = 13178;
    std::cout << 13178;
    for (int i = 0; i < 13178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13178(int& val) {
    const int x = 13178;
    std::cout << 13178;
    for (int i = 0; i < 13178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13179(int& val) {
    func_in_13179(val);
    const int x = 13179;
    std::cout << 13179;
    for (int i = 0; i < 13179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13179(int& val) {
    const int x = 13179;
    std::cout << 13179;
    for (int i = 0; i < 13179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13180(int& val) {
    func_in_13180(val);
    const int x = 13180;
    std::cout << 13180;
    for (int i = 0; i < 13180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13180(int& val) {
    const int x = 13180;
    std::cout << 13180;
    for (int i = 0; i < 13180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13181(int& val) {
    func_in_13181(val);
    const int x = 13181;
    std::cout << 13181;
    for (int i = 0; i < 13181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13181(int& val) {
    const int x = 13181;
    std::cout << 13181;
    for (int i = 0; i < 13181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13182(int& val) {
    func_in_13182(val);
    const int x = 13182;
    std::cout << 13182;
    for (int i = 0; i < 13182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13182(int& val) {
    const int x = 13182;
    std::cout << 13182;
    for (int i = 0; i < 13182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13183(int& val) {
    func_in_13183(val);
    const int x = 13183;
    std::cout << 13183;
    for (int i = 0; i < 13183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13183(int& val) {
    const int x = 13183;
    std::cout << 13183;
    for (int i = 0; i < 13183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13184(int& val) {
    func_in_13184(val);
    const int x = 13184;
    std::cout << 13184;
    for (int i = 0; i < 13184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13184(int& val) {
    const int x = 13184;
    std::cout << 13184;
    for (int i = 0; i < 13184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13185(int& val) {
    func_in_13185(val);
    const int x = 13185;
    std::cout << 13185;
    for (int i = 0; i < 13185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13185(int& val) {
    const int x = 13185;
    std::cout << 13185;
    for (int i = 0; i < 13185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13186(int& val) {
    func_in_13186(val);
    const int x = 13186;
    std::cout << 13186;
    for (int i = 0; i < 13186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13186(int& val) {
    const int x = 13186;
    std::cout << 13186;
    for (int i = 0; i < 13186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13187(int& val) {
    func_in_13187(val);
    const int x = 13187;
    std::cout << 13187;
    for (int i = 0; i < 13187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13187(int& val) {
    const int x = 13187;
    std::cout << 13187;
    for (int i = 0; i < 13187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13188(int& val) {
    func_in_13188(val);
    const int x = 13188;
    std::cout << 13188;
    for (int i = 0; i < 13188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13188(int& val) {
    const int x = 13188;
    std::cout << 13188;
    for (int i = 0; i < 13188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13189(int& val) {
    func_in_13189(val);
    const int x = 13189;
    std::cout << 13189;
    for (int i = 0; i < 13189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13189(int& val) {
    const int x = 13189;
    std::cout << 13189;
    for (int i = 0; i < 13189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13190(int& val) {
    func_in_13190(val);
    const int x = 13190;
    std::cout << 13190;
    for (int i = 0; i < 13190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13190(int& val) {
    const int x = 13190;
    std::cout << 13190;
    for (int i = 0; i < 13190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13191(int& val) {
    func_in_13191(val);
    const int x = 13191;
    std::cout << 13191;
    for (int i = 0; i < 13191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13191(int& val) {
    const int x = 13191;
    std::cout << 13191;
    for (int i = 0; i < 13191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13192(int& val) {
    func_in_13192(val);
    const int x = 13192;
    std::cout << 13192;
    for (int i = 0; i < 13192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13192(int& val) {
    const int x = 13192;
    std::cout << 13192;
    for (int i = 0; i < 13192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13193(int& val) {
    func_in_13193(val);
    const int x = 13193;
    std::cout << 13193;
    for (int i = 0; i < 13193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13193(int& val) {
    const int x = 13193;
    std::cout << 13193;
    for (int i = 0; i < 13193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13194(int& val) {
    func_in_13194(val);
    const int x = 13194;
    std::cout << 13194;
    for (int i = 0; i < 13194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13194(int& val) {
    const int x = 13194;
    std::cout << 13194;
    for (int i = 0; i < 13194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13195(int& val) {
    func_in_13195(val);
    const int x = 13195;
    std::cout << 13195;
    for (int i = 0; i < 13195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13195(int& val) {
    const int x = 13195;
    std::cout << 13195;
    for (int i = 0; i < 13195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13196(int& val) {
    func_in_13196(val);
    const int x = 13196;
    std::cout << 13196;
    for (int i = 0; i < 13196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13196(int& val) {
    const int x = 13196;
    std::cout << 13196;
    for (int i = 0; i < 13196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13197(int& val) {
    func_in_13197(val);
    const int x = 13197;
    std::cout << 13197;
    for (int i = 0; i < 13197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13197(int& val) {
    const int x = 13197;
    std::cout << 13197;
    for (int i = 0; i < 13197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13198(int& val) {
    func_in_13198(val);
    const int x = 13198;
    std::cout << 13198;
    for (int i = 0; i < 13198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13198(int& val) {
    const int x = 13198;
    std::cout << 13198;
    for (int i = 0; i < 13198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13199(int& val) {
    func_in_13199(val);
    const int x = 13199;
    std::cout << 13199;
    for (int i = 0; i < 13199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13199(int& val) {
    const int x = 13199;
    std::cout << 13199;
    for (int i = 0; i < 13199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13200(int& val) {
    func_in_13200(val);
    const int x = 13200;
    std::cout << 13200;
    for (int i = 0; i < 13200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13200(int& val) {
    const int x = 13200;
    std::cout << 13200;
    for (int i = 0; i < 13200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13201(int& val) {
    func_in_13201(val);
    const int x = 13201;
    std::cout << 13201;
    for (int i = 0; i < 13201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13201(int& val) {
    const int x = 13201;
    std::cout << 13201;
    for (int i = 0; i < 13201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13202(int& val) {
    func_in_13202(val);
    const int x = 13202;
    std::cout << 13202;
    for (int i = 0; i < 13202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13202(int& val) {
    const int x = 13202;
    std::cout << 13202;
    for (int i = 0; i < 13202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13203(int& val) {
    func_in_13203(val);
    const int x = 13203;
    std::cout << 13203;
    for (int i = 0; i < 13203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13203(int& val) {
    const int x = 13203;
    std::cout << 13203;
    for (int i = 0; i < 13203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13204(int& val) {
    func_in_13204(val);
    const int x = 13204;
    std::cout << 13204;
    for (int i = 0; i < 13204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13204(int& val) {
    const int x = 13204;
    std::cout << 13204;
    for (int i = 0; i < 13204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13205(int& val) {
    func_in_13205(val);
    const int x = 13205;
    std::cout << 13205;
    for (int i = 0; i < 13205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13205(int& val) {
    const int x = 13205;
    std::cout << 13205;
    for (int i = 0; i < 13205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13206(int& val) {
    func_in_13206(val);
    const int x = 13206;
    std::cout << 13206;
    for (int i = 0; i < 13206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13206(int& val) {
    const int x = 13206;
    std::cout << 13206;
    for (int i = 0; i < 13206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13207(int& val) {
    func_in_13207(val);
    const int x = 13207;
    std::cout << 13207;
    for (int i = 0; i < 13207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13207(int& val) {
    const int x = 13207;
    std::cout << 13207;
    for (int i = 0; i < 13207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13208(int& val) {
    func_in_13208(val);
    const int x = 13208;
    std::cout << 13208;
    for (int i = 0; i < 13208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13208(int& val) {
    const int x = 13208;
    std::cout << 13208;
    for (int i = 0; i < 13208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13209(int& val) {
    func_in_13209(val);
    const int x = 13209;
    std::cout << 13209;
    for (int i = 0; i < 13209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13209(int& val) {
    const int x = 13209;
    std::cout << 13209;
    for (int i = 0; i < 13209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13210(int& val) {
    func_in_13210(val);
    const int x = 13210;
    std::cout << 13210;
    for (int i = 0; i < 13210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13210(int& val) {
    const int x = 13210;
    std::cout << 13210;
    for (int i = 0; i < 13210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13211(int& val) {
    func_in_13211(val);
    const int x = 13211;
    std::cout << 13211;
    for (int i = 0; i < 13211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13211(int& val) {
    const int x = 13211;
    std::cout << 13211;
    for (int i = 0; i < 13211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13212(int& val) {
    func_in_13212(val);
    const int x = 13212;
    std::cout << 13212;
    for (int i = 0; i < 13212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13212(int& val) {
    const int x = 13212;
    std::cout << 13212;
    for (int i = 0; i < 13212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13213(int& val) {
    func_in_13213(val);
    const int x = 13213;
    std::cout << 13213;
    for (int i = 0; i < 13213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13213(int& val) {
    const int x = 13213;
    std::cout << 13213;
    for (int i = 0; i < 13213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13214(int& val) {
    func_in_13214(val);
    const int x = 13214;
    std::cout << 13214;
    for (int i = 0; i < 13214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13214(int& val) {
    const int x = 13214;
    std::cout << 13214;
    for (int i = 0; i < 13214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13215(int& val) {
    func_in_13215(val);
    const int x = 13215;
    std::cout << 13215;
    for (int i = 0; i < 13215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13215(int& val) {
    const int x = 13215;
    std::cout << 13215;
    for (int i = 0; i < 13215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13216(int& val) {
    func_in_13216(val);
    const int x = 13216;
    std::cout << 13216;
    for (int i = 0; i < 13216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13216(int& val) {
    const int x = 13216;
    std::cout << 13216;
    for (int i = 0; i < 13216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13217(int& val) {
    func_in_13217(val);
    const int x = 13217;
    std::cout << 13217;
    for (int i = 0; i < 13217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13217(int& val) {
    const int x = 13217;
    std::cout << 13217;
    for (int i = 0; i < 13217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13218(int& val) {
    func_in_13218(val);
    const int x = 13218;
    std::cout << 13218;
    for (int i = 0; i < 13218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13218(int& val) {
    const int x = 13218;
    std::cout << 13218;
    for (int i = 0; i < 13218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13219(int& val) {
    func_in_13219(val);
    const int x = 13219;
    std::cout << 13219;
    for (int i = 0; i < 13219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13219(int& val) {
    const int x = 13219;
    std::cout << 13219;
    for (int i = 0; i < 13219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13220(int& val) {
    func_in_13220(val);
    const int x = 13220;
    std::cout << 13220;
    for (int i = 0; i < 13220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13220(int& val) {
    const int x = 13220;
    std::cout << 13220;
    for (int i = 0; i < 13220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13221(int& val) {
    func_in_13221(val);
    const int x = 13221;
    std::cout << 13221;
    for (int i = 0; i < 13221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13221(int& val) {
    const int x = 13221;
    std::cout << 13221;
    for (int i = 0; i < 13221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13222(int& val) {
    func_in_13222(val);
    const int x = 13222;
    std::cout << 13222;
    for (int i = 0; i < 13222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13222(int& val) {
    const int x = 13222;
    std::cout << 13222;
    for (int i = 0; i < 13222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13223(int& val) {
    func_in_13223(val);
    const int x = 13223;
    std::cout << 13223;
    for (int i = 0; i < 13223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13223(int& val) {
    const int x = 13223;
    std::cout << 13223;
    for (int i = 0; i < 13223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13224(int& val) {
    func_in_13224(val);
    const int x = 13224;
    std::cout << 13224;
    for (int i = 0; i < 13224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13224(int& val) {
    const int x = 13224;
    std::cout << 13224;
    for (int i = 0; i < 13224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13225(int& val) {
    func_in_13225(val);
    const int x = 13225;
    std::cout << 13225;
    for (int i = 0; i < 13225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13225(int& val) {
    const int x = 13225;
    std::cout << 13225;
    for (int i = 0; i < 13225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13226(int& val) {
    func_in_13226(val);
    const int x = 13226;
    std::cout << 13226;
    for (int i = 0; i < 13226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13226(int& val) {
    const int x = 13226;
    std::cout << 13226;
    for (int i = 0; i < 13226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13227(int& val) {
    func_in_13227(val);
    const int x = 13227;
    std::cout << 13227;
    for (int i = 0; i < 13227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13227(int& val) {
    const int x = 13227;
    std::cout << 13227;
    for (int i = 0; i < 13227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13228(int& val) {
    func_in_13228(val);
    const int x = 13228;
    std::cout << 13228;
    for (int i = 0; i < 13228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13228(int& val) {
    const int x = 13228;
    std::cout << 13228;
    for (int i = 0; i < 13228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13229(int& val) {
    func_in_13229(val);
    const int x = 13229;
    std::cout << 13229;
    for (int i = 0; i < 13229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13229(int& val) {
    const int x = 13229;
    std::cout << 13229;
    for (int i = 0; i < 13229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13230(int& val) {
    func_in_13230(val);
    const int x = 13230;
    std::cout << 13230;
    for (int i = 0; i < 13230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13230(int& val) {
    const int x = 13230;
    std::cout << 13230;
    for (int i = 0; i < 13230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13231(int& val) {
    func_in_13231(val);
    const int x = 13231;
    std::cout << 13231;
    for (int i = 0; i < 13231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13231(int& val) {
    const int x = 13231;
    std::cout << 13231;
    for (int i = 0; i < 13231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13232(int& val) {
    func_in_13232(val);
    const int x = 13232;
    std::cout << 13232;
    for (int i = 0; i < 13232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13232(int& val) {
    const int x = 13232;
    std::cout << 13232;
    for (int i = 0; i < 13232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13233(int& val) {
    func_in_13233(val);
    const int x = 13233;
    std::cout << 13233;
    for (int i = 0; i < 13233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13233(int& val) {
    const int x = 13233;
    std::cout << 13233;
    for (int i = 0; i < 13233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13234(int& val) {
    func_in_13234(val);
    const int x = 13234;
    std::cout << 13234;
    for (int i = 0; i < 13234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13234(int& val) {
    const int x = 13234;
    std::cout << 13234;
    for (int i = 0; i < 13234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13235(int& val) {
    func_in_13235(val);
    const int x = 13235;
    std::cout << 13235;
    for (int i = 0; i < 13235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13235(int& val) {
    const int x = 13235;
    std::cout << 13235;
    for (int i = 0; i < 13235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13236(int& val) {
    func_in_13236(val);
    const int x = 13236;
    std::cout << 13236;
    for (int i = 0; i < 13236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13236(int& val) {
    const int x = 13236;
    std::cout << 13236;
    for (int i = 0; i < 13236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13237(int& val) {
    func_in_13237(val);
    const int x = 13237;
    std::cout << 13237;
    for (int i = 0; i < 13237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13237(int& val) {
    const int x = 13237;
    std::cout << 13237;
    for (int i = 0; i < 13237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13238(int& val) {
    func_in_13238(val);
    const int x = 13238;
    std::cout << 13238;
    for (int i = 0; i < 13238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13238(int& val) {
    const int x = 13238;
    std::cout << 13238;
    for (int i = 0; i < 13238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13239(int& val) {
    func_in_13239(val);
    const int x = 13239;
    std::cout << 13239;
    for (int i = 0; i < 13239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13239(int& val) {
    const int x = 13239;
    std::cout << 13239;
    for (int i = 0; i < 13239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13240(int& val) {
    func_in_13240(val);
    const int x = 13240;
    std::cout << 13240;
    for (int i = 0; i < 13240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13240(int& val) {
    const int x = 13240;
    std::cout << 13240;
    for (int i = 0; i < 13240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13241(int& val) {
    func_in_13241(val);
    const int x = 13241;
    std::cout << 13241;
    for (int i = 0; i < 13241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13241(int& val) {
    const int x = 13241;
    std::cout << 13241;
    for (int i = 0; i < 13241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13242(int& val) {
    func_in_13242(val);
    const int x = 13242;
    std::cout << 13242;
    for (int i = 0; i < 13242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13242(int& val) {
    const int x = 13242;
    std::cout << 13242;
    for (int i = 0; i < 13242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13243(int& val) {
    func_in_13243(val);
    const int x = 13243;
    std::cout << 13243;
    for (int i = 0; i < 13243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13243(int& val) {
    const int x = 13243;
    std::cout << 13243;
    for (int i = 0; i < 13243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13244(int& val) {
    func_in_13244(val);
    const int x = 13244;
    std::cout << 13244;
    for (int i = 0; i < 13244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13244(int& val) {
    const int x = 13244;
    std::cout << 13244;
    for (int i = 0; i < 13244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13245(int& val) {
    func_in_13245(val);
    const int x = 13245;
    std::cout << 13245;
    for (int i = 0; i < 13245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13245(int& val) {
    const int x = 13245;
    std::cout << 13245;
    for (int i = 0; i < 13245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13246(int& val) {
    func_in_13246(val);
    const int x = 13246;
    std::cout << 13246;
    for (int i = 0; i < 13246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13246(int& val) {
    const int x = 13246;
    std::cout << 13246;
    for (int i = 0; i < 13246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13247(int& val) {
    func_in_13247(val);
    const int x = 13247;
    std::cout << 13247;
    for (int i = 0; i < 13247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13247(int& val) {
    const int x = 13247;
    std::cout << 13247;
    for (int i = 0; i < 13247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13248(int& val) {
    func_in_13248(val);
    const int x = 13248;
    std::cout << 13248;
    for (int i = 0; i < 13248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13248(int& val) {
    const int x = 13248;
    std::cout << 13248;
    for (int i = 0; i < 13248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13249(int& val) {
    func_in_13249(val);
    const int x = 13249;
    std::cout << 13249;
    for (int i = 0; i < 13249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13249(int& val) {
    const int x = 13249;
    std::cout << 13249;
    for (int i = 0; i < 13249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13250(int& val) {
    func_in_13250(val);
    const int x = 13250;
    std::cout << 13250;
    for (int i = 0; i < 13250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13250(int& val) {
    const int x = 13250;
    std::cout << 13250;
    for (int i = 0; i < 13250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13251(int& val) {
    func_in_13251(val);
    const int x = 13251;
    std::cout << 13251;
    for (int i = 0; i < 13251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13251(int& val) {
    const int x = 13251;
    std::cout << 13251;
    for (int i = 0; i < 13251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13252(int& val) {
    func_in_13252(val);
    const int x = 13252;
    std::cout << 13252;
    for (int i = 0; i < 13252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13252(int& val) {
    const int x = 13252;
    std::cout << 13252;
    for (int i = 0; i < 13252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13253(int& val) {
    func_in_13253(val);
    const int x = 13253;
    std::cout << 13253;
    for (int i = 0; i < 13253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13253(int& val) {
    const int x = 13253;
    std::cout << 13253;
    for (int i = 0; i < 13253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13254(int& val) {
    func_in_13254(val);
    const int x = 13254;
    std::cout << 13254;
    for (int i = 0; i < 13254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13254(int& val) {
    const int x = 13254;
    std::cout << 13254;
    for (int i = 0; i < 13254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13255(int& val) {
    func_in_13255(val);
    const int x = 13255;
    std::cout << 13255;
    for (int i = 0; i < 13255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13255(int& val) {
    const int x = 13255;
    std::cout << 13255;
    for (int i = 0; i < 13255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13256(int& val) {
    func_in_13256(val);
    const int x = 13256;
    std::cout << 13256;
    for (int i = 0; i < 13256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13256(int& val) {
    const int x = 13256;
    std::cout << 13256;
    for (int i = 0; i < 13256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13257(int& val) {
    func_in_13257(val);
    const int x = 13257;
    std::cout << 13257;
    for (int i = 0; i < 13257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13257(int& val) {
    const int x = 13257;
    std::cout << 13257;
    for (int i = 0; i < 13257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13258(int& val) {
    func_in_13258(val);
    const int x = 13258;
    std::cout << 13258;
    for (int i = 0; i < 13258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13258(int& val) {
    const int x = 13258;
    std::cout << 13258;
    for (int i = 0; i < 13258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13259(int& val) {
    func_in_13259(val);
    const int x = 13259;
    std::cout << 13259;
    for (int i = 0; i < 13259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13259(int& val) {
    const int x = 13259;
    std::cout << 13259;
    for (int i = 0; i < 13259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13260(int& val) {
    func_in_13260(val);
    const int x = 13260;
    std::cout << 13260;
    for (int i = 0; i < 13260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13260(int& val) {
    const int x = 13260;
    std::cout << 13260;
    for (int i = 0; i < 13260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13261(int& val) {
    func_in_13261(val);
    const int x = 13261;
    std::cout << 13261;
    for (int i = 0; i < 13261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13261(int& val) {
    const int x = 13261;
    std::cout << 13261;
    for (int i = 0; i < 13261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13262(int& val) {
    func_in_13262(val);
    const int x = 13262;
    std::cout << 13262;
    for (int i = 0; i < 13262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13262(int& val) {
    const int x = 13262;
    std::cout << 13262;
    for (int i = 0; i < 13262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13263(int& val) {
    func_in_13263(val);
    const int x = 13263;
    std::cout << 13263;
    for (int i = 0; i < 13263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13263(int& val) {
    const int x = 13263;
    std::cout << 13263;
    for (int i = 0; i < 13263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13264(int& val) {
    func_in_13264(val);
    const int x = 13264;
    std::cout << 13264;
    for (int i = 0; i < 13264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13264(int& val) {
    const int x = 13264;
    std::cout << 13264;
    for (int i = 0; i < 13264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13265(int& val) {
    func_in_13265(val);
    const int x = 13265;
    std::cout << 13265;
    for (int i = 0; i < 13265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13265(int& val) {
    const int x = 13265;
    std::cout << 13265;
    for (int i = 0; i < 13265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13266(int& val) {
    func_in_13266(val);
    const int x = 13266;
    std::cout << 13266;
    for (int i = 0; i < 13266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13266(int& val) {
    const int x = 13266;
    std::cout << 13266;
    for (int i = 0; i < 13266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13267(int& val) {
    func_in_13267(val);
    const int x = 13267;
    std::cout << 13267;
    for (int i = 0; i < 13267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13267(int& val) {
    const int x = 13267;
    std::cout << 13267;
    for (int i = 0; i < 13267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13268(int& val) {
    func_in_13268(val);
    const int x = 13268;
    std::cout << 13268;
    for (int i = 0; i < 13268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13268(int& val) {
    const int x = 13268;
    std::cout << 13268;
    for (int i = 0; i < 13268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13269(int& val) {
    func_in_13269(val);
    const int x = 13269;
    std::cout << 13269;
    for (int i = 0; i < 13269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13269(int& val) {
    const int x = 13269;
    std::cout << 13269;
    for (int i = 0; i < 13269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13270(int& val) {
    func_in_13270(val);
    const int x = 13270;
    std::cout << 13270;
    for (int i = 0; i < 13270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13270(int& val) {
    const int x = 13270;
    std::cout << 13270;
    for (int i = 0; i < 13270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13271(int& val) {
    func_in_13271(val);
    const int x = 13271;
    std::cout << 13271;
    for (int i = 0; i < 13271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13271(int& val) {
    const int x = 13271;
    std::cout << 13271;
    for (int i = 0; i < 13271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13272(int& val) {
    func_in_13272(val);
    const int x = 13272;
    std::cout << 13272;
    for (int i = 0; i < 13272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13272(int& val) {
    const int x = 13272;
    std::cout << 13272;
    for (int i = 0; i < 13272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13273(int& val) {
    func_in_13273(val);
    const int x = 13273;
    std::cout << 13273;
    for (int i = 0; i < 13273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13273(int& val) {
    const int x = 13273;
    std::cout << 13273;
    for (int i = 0; i < 13273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13274(int& val) {
    func_in_13274(val);
    const int x = 13274;
    std::cout << 13274;
    for (int i = 0; i < 13274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13274(int& val) {
    const int x = 13274;
    std::cout << 13274;
    for (int i = 0; i < 13274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13275(int& val) {
    func_in_13275(val);
    const int x = 13275;
    std::cout << 13275;
    for (int i = 0; i < 13275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13275(int& val) {
    const int x = 13275;
    std::cout << 13275;
    for (int i = 0; i < 13275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13276(int& val) {
    func_in_13276(val);
    const int x = 13276;
    std::cout << 13276;
    for (int i = 0; i < 13276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13276(int& val) {
    const int x = 13276;
    std::cout << 13276;
    for (int i = 0; i < 13276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13277(int& val) {
    func_in_13277(val);
    const int x = 13277;
    std::cout << 13277;
    for (int i = 0; i < 13277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13277(int& val) {
    const int x = 13277;
    std::cout << 13277;
    for (int i = 0; i < 13277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13278(int& val) {
    func_in_13278(val);
    const int x = 13278;
    std::cout << 13278;
    for (int i = 0; i < 13278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13278(int& val) {
    const int x = 13278;
    std::cout << 13278;
    for (int i = 0; i < 13278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13279(int& val) {
    func_in_13279(val);
    const int x = 13279;
    std::cout << 13279;
    for (int i = 0; i < 13279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13279(int& val) {
    const int x = 13279;
    std::cout << 13279;
    for (int i = 0; i < 13279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13280(int& val) {
    func_in_13280(val);
    const int x = 13280;
    std::cout << 13280;
    for (int i = 0; i < 13280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13280(int& val) {
    const int x = 13280;
    std::cout << 13280;
    for (int i = 0; i < 13280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13281(int& val) {
    func_in_13281(val);
    const int x = 13281;
    std::cout << 13281;
    for (int i = 0; i < 13281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13281(int& val) {
    const int x = 13281;
    std::cout << 13281;
    for (int i = 0; i < 13281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13282(int& val) {
    func_in_13282(val);
    const int x = 13282;
    std::cout << 13282;
    for (int i = 0; i < 13282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13282(int& val) {
    const int x = 13282;
    std::cout << 13282;
    for (int i = 0; i < 13282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13283(int& val) {
    func_in_13283(val);
    const int x = 13283;
    std::cout << 13283;
    for (int i = 0; i < 13283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13283(int& val) {
    const int x = 13283;
    std::cout << 13283;
    for (int i = 0; i < 13283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13284(int& val) {
    func_in_13284(val);
    const int x = 13284;
    std::cout << 13284;
    for (int i = 0; i < 13284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13284(int& val) {
    const int x = 13284;
    std::cout << 13284;
    for (int i = 0; i < 13284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13285(int& val) {
    func_in_13285(val);
    const int x = 13285;
    std::cout << 13285;
    for (int i = 0; i < 13285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13285(int& val) {
    const int x = 13285;
    std::cout << 13285;
    for (int i = 0; i < 13285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13286(int& val) {
    func_in_13286(val);
    const int x = 13286;
    std::cout << 13286;
    for (int i = 0; i < 13286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13286(int& val) {
    const int x = 13286;
    std::cout << 13286;
    for (int i = 0; i < 13286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13287(int& val) {
    func_in_13287(val);
    const int x = 13287;
    std::cout << 13287;
    for (int i = 0; i < 13287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13287(int& val) {
    const int x = 13287;
    std::cout << 13287;
    for (int i = 0; i < 13287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13288(int& val) {
    func_in_13288(val);
    const int x = 13288;
    std::cout << 13288;
    for (int i = 0; i < 13288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13288(int& val) {
    const int x = 13288;
    std::cout << 13288;
    for (int i = 0; i < 13288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13289(int& val) {
    func_in_13289(val);
    const int x = 13289;
    std::cout << 13289;
    for (int i = 0; i < 13289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13289(int& val) {
    const int x = 13289;
    std::cout << 13289;
    for (int i = 0; i < 13289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13290(int& val) {
    func_in_13290(val);
    const int x = 13290;
    std::cout << 13290;
    for (int i = 0; i < 13290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13290(int& val) {
    const int x = 13290;
    std::cout << 13290;
    for (int i = 0; i < 13290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13291(int& val) {
    func_in_13291(val);
    const int x = 13291;
    std::cout << 13291;
    for (int i = 0; i < 13291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13291(int& val) {
    const int x = 13291;
    std::cout << 13291;
    for (int i = 0; i < 13291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13292(int& val) {
    func_in_13292(val);
    const int x = 13292;
    std::cout << 13292;
    for (int i = 0; i < 13292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13292(int& val) {
    const int x = 13292;
    std::cout << 13292;
    for (int i = 0; i < 13292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13293(int& val) {
    func_in_13293(val);
    const int x = 13293;
    std::cout << 13293;
    for (int i = 0; i < 13293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13293(int& val) {
    const int x = 13293;
    std::cout << 13293;
    for (int i = 0; i < 13293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13294(int& val) {
    func_in_13294(val);
    const int x = 13294;
    std::cout << 13294;
    for (int i = 0; i < 13294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13294(int& val) {
    const int x = 13294;
    std::cout << 13294;
    for (int i = 0; i < 13294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13295(int& val) {
    func_in_13295(val);
    const int x = 13295;
    std::cout << 13295;
    for (int i = 0; i < 13295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13295(int& val) {
    const int x = 13295;
    std::cout << 13295;
    for (int i = 0; i < 13295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13296(int& val) {
    func_in_13296(val);
    const int x = 13296;
    std::cout << 13296;
    for (int i = 0; i < 13296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13296(int& val) {
    const int x = 13296;
    std::cout << 13296;
    for (int i = 0; i < 13296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13297(int& val) {
    func_in_13297(val);
    const int x = 13297;
    std::cout << 13297;
    for (int i = 0; i < 13297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13297(int& val) {
    const int x = 13297;
    std::cout << 13297;
    for (int i = 0; i < 13297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13298(int& val) {
    func_in_13298(val);
    const int x = 13298;
    std::cout << 13298;
    for (int i = 0; i < 13298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13298(int& val) {
    const int x = 13298;
    std::cout << 13298;
    for (int i = 0; i < 13298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13299(int& val) {
    func_in_13299(val);
    const int x = 13299;
    std::cout << 13299;
    for (int i = 0; i < 13299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13299(int& val) {
    const int x = 13299;
    std::cout << 13299;
    for (int i = 0; i < 13299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13300(int& val) {
    func_in_13300(val);
    const int x = 13300;
    std::cout << 13300;
    for (int i = 0; i < 13300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13300(int& val) {
    const int x = 13300;
    std::cout << 13300;
    for (int i = 0; i < 13300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13301(int& val) {
    func_in_13301(val);
    const int x = 13301;
    std::cout << 13301;
    for (int i = 0; i < 13301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13301(int& val) {
    const int x = 13301;
    std::cout << 13301;
    for (int i = 0; i < 13301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13302(int& val) {
    func_in_13302(val);
    const int x = 13302;
    std::cout << 13302;
    for (int i = 0; i < 13302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13302(int& val) {
    const int x = 13302;
    std::cout << 13302;
    for (int i = 0; i < 13302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13303(int& val) {
    func_in_13303(val);
    const int x = 13303;
    std::cout << 13303;
    for (int i = 0; i < 13303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13303(int& val) {
    const int x = 13303;
    std::cout << 13303;
    for (int i = 0; i < 13303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13304(int& val) {
    func_in_13304(val);
    const int x = 13304;
    std::cout << 13304;
    for (int i = 0; i < 13304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13304(int& val) {
    const int x = 13304;
    std::cout << 13304;
    for (int i = 0; i < 13304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13305(int& val) {
    func_in_13305(val);
    const int x = 13305;
    std::cout << 13305;
    for (int i = 0; i < 13305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13305(int& val) {
    const int x = 13305;
    std::cout << 13305;
    for (int i = 0; i < 13305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13306(int& val) {
    func_in_13306(val);
    const int x = 13306;
    std::cout << 13306;
    for (int i = 0; i < 13306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13306(int& val) {
    const int x = 13306;
    std::cout << 13306;
    for (int i = 0; i < 13306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13307(int& val) {
    func_in_13307(val);
    const int x = 13307;
    std::cout << 13307;
    for (int i = 0; i < 13307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13307(int& val) {
    const int x = 13307;
    std::cout << 13307;
    for (int i = 0; i < 13307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13308(int& val) {
    func_in_13308(val);
    const int x = 13308;
    std::cout << 13308;
    for (int i = 0; i < 13308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13308(int& val) {
    const int x = 13308;
    std::cout << 13308;
    for (int i = 0; i < 13308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13309(int& val) {
    func_in_13309(val);
    const int x = 13309;
    std::cout << 13309;
    for (int i = 0; i < 13309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13309(int& val) {
    const int x = 13309;
    std::cout << 13309;
    for (int i = 0; i < 13309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13310(int& val) {
    func_in_13310(val);
    const int x = 13310;
    std::cout << 13310;
    for (int i = 0; i < 13310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13310(int& val) {
    const int x = 13310;
    std::cout << 13310;
    for (int i = 0; i < 13310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13311(int& val) {
    func_in_13311(val);
    const int x = 13311;
    std::cout << 13311;
    for (int i = 0; i < 13311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13311(int& val) {
    const int x = 13311;
    std::cout << 13311;
    for (int i = 0; i < 13311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13312(int& val) {
    func_in_13312(val);
    const int x = 13312;
    std::cout << 13312;
    for (int i = 0; i < 13312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13312(int& val) {
    const int x = 13312;
    std::cout << 13312;
    for (int i = 0; i < 13312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13313(int& val) {
    func_in_13313(val);
    const int x = 13313;
    std::cout << 13313;
    for (int i = 0; i < 13313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13313(int& val) {
    const int x = 13313;
    std::cout << 13313;
    for (int i = 0; i < 13313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13314(int& val) {
    func_in_13314(val);
    const int x = 13314;
    std::cout << 13314;
    for (int i = 0; i < 13314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13314(int& val) {
    const int x = 13314;
    std::cout << 13314;
    for (int i = 0; i < 13314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13315(int& val) {
    func_in_13315(val);
    const int x = 13315;
    std::cout << 13315;
    for (int i = 0; i < 13315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13315(int& val) {
    const int x = 13315;
    std::cout << 13315;
    for (int i = 0; i < 13315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13316(int& val) {
    func_in_13316(val);
    const int x = 13316;
    std::cout << 13316;
    for (int i = 0; i < 13316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13316(int& val) {
    const int x = 13316;
    std::cout << 13316;
    for (int i = 0; i < 13316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13317(int& val) {
    func_in_13317(val);
    const int x = 13317;
    std::cout << 13317;
    for (int i = 0; i < 13317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13317(int& val) {
    const int x = 13317;
    std::cout << 13317;
    for (int i = 0; i < 13317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13318(int& val) {
    func_in_13318(val);
    const int x = 13318;
    std::cout << 13318;
    for (int i = 0; i < 13318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13318(int& val) {
    const int x = 13318;
    std::cout << 13318;
    for (int i = 0; i < 13318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13319(int& val) {
    func_in_13319(val);
    const int x = 13319;
    std::cout << 13319;
    for (int i = 0; i < 13319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13319(int& val) {
    const int x = 13319;
    std::cout << 13319;
    for (int i = 0; i < 13319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13320(int& val) {
    func_in_13320(val);
    const int x = 13320;
    std::cout << 13320;
    for (int i = 0; i < 13320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13320(int& val) {
    const int x = 13320;
    std::cout << 13320;
    for (int i = 0; i < 13320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13321(int& val) {
    func_in_13321(val);
    const int x = 13321;
    std::cout << 13321;
    for (int i = 0; i < 13321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13321(int& val) {
    const int x = 13321;
    std::cout << 13321;
    for (int i = 0; i < 13321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13322(int& val) {
    func_in_13322(val);
    const int x = 13322;
    std::cout << 13322;
    for (int i = 0; i < 13322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13322(int& val) {
    const int x = 13322;
    std::cout << 13322;
    for (int i = 0; i < 13322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13323(int& val) {
    func_in_13323(val);
    const int x = 13323;
    std::cout << 13323;
    for (int i = 0; i < 13323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13323(int& val) {
    const int x = 13323;
    std::cout << 13323;
    for (int i = 0; i < 13323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13324(int& val) {
    func_in_13324(val);
    const int x = 13324;
    std::cout << 13324;
    for (int i = 0; i < 13324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13324(int& val) {
    const int x = 13324;
    std::cout << 13324;
    for (int i = 0; i < 13324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13325(int& val) {
    func_in_13325(val);
    const int x = 13325;
    std::cout << 13325;
    for (int i = 0; i < 13325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13325(int& val) {
    const int x = 13325;
    std::cout << 13325;
    for (int i = 0; i < 13325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13326(int& val) {
    func_in_13326(val);
    const int x = 13326;
    std::cout << 13326;
    for (int i = 0; i < 13326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13326(int& val) {
    const int x = 13326;
    std::cout << 13326;
    for (int i = 0; i < 13326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13327(int& val) {
    func_in_13327(val);
    const int x = 13327;
    std::cout << 13327;
    for (int i = 0; i < 13327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13327(int& val) {
    const int x = 13327;
    std::cout << 13327;
    for (int i = 0; i < 13327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13328(int& val) {
    func_in_13328(val);
    const int x = 13328;
    std::cout << 13328;
    for (int i = 0; i < 13328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13328(int& val) {
    const int x = 13328;
    std::cout << 13328;
    for (int i = 0; i < 13328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13329(int& val) {
    func_in_13329(val);
    const int x = 13329;
    std::cout << 13329;
    for (int i = 0; i < 13329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13329(int& val) {
    const int x = 13329;
    std::cout << 13329;
    for (int i = 0; i < 13329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13330(int& val) {
    func_in_13330(val);
    const int x = 13330;
    std::cout << 13330;
    for (int i = 0; i < 13330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13330(int& val) {
    const int x = 13330;
    std::cout << 13330;
    for (int i = 0; i < 13330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13331(int& val) {
    func_in_13331(val);
    const int x = 13331;
    std::cout << 13331;
    for (int i = 0; i < 13331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13331(int& val) {
    const int x = 13331;
    std::cout << 13331;
    for (int i = 0; i < 13331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13332(int& val) {
    func_in_13332(val);
    const int x = 13332;
    std::cout << 13332;
    for (int i = 0; i < 13332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13332(int& val) {
    const int x = 13332;
    std::cout << 13332;
    for (int i = 0; i < 13332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13333(int& val) {
    func_in_13333(val);
    const int x = 13333;
    std::cout << 13333;
    for (int i = 0; i < 13333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13333(int& val) {
    const int x = 13333;
    std::cout << 13333;
    for (int i = 0; i < 13333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13334(int& val) {
    func_in_13334(val);
    const int x = 13334;
    std::cout << 13334;
    for (int i = 0; i < 13334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13334(int& val) {
    const int x = 13334;
    std::cout << 13334;
    for (int i = 0; i < 13334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13335(int& val) {
    func_in_13335(val);
    const int x = 13335;
    std::cout << 13335;
    for (int i = 0; i < 13335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13335(int& val) {
    const int x = 13335;
    std::cout << 13335;
    for (int i = 0; i < 13335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13336(int& val) {
    func_in_13336(val);
    const int x = 13336;
    std::cout << 13336;
    for (int i = 0; i < 13336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13336(int& val) {
    const int x = 13336;
    std::cout << 13336;
    for (int i = 0; i < 13336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13337(int& val) {
    func_in_13337(val);
    const int x = 13337;
    std::cout << 13337;
    for (int i = 0; i < 13337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13337(int& val) {
    const int x = 13337;
    std::cout << 13337;
    for (int i = 0; i < 13337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13338(int& val) {
    func_in_13338(val);
    const int x = 13338;
    std::cout << 13338;
    for (int i = 0; i < 13338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13338(int& val) {
    const int x = 13338;
    std::cout << 13338;
    for (int i = 0; i < 13338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13339(int& val) {
    func_in_13339(val);
    const int x = 13339;
    std::cout << 13339;
    for (int i = 0; i < 13339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13339(int& val) {
    const int x = 13339;
    std::cout << 13339;
    for (int i = 0; i < 13339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13340(int& val) {
    func_in_13340(val);
    const int x = 13340;
    std::cout << 13340;
    for (int i = 0; i < 13340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13340(int& val) {
    const int x = 13340;
    std::cout << 13340;
    for (int i = 0; i < 13340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13341(int& val) {
    func_in_13341(val);
    const int x = 13341;
    std::cout << 13341;
    for (int i = 0; i < 13341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13341(int& val) {
    const int x = 13341;
    std::cout << 13341;
    for (int i = 0; i < 13341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13342(int& val) {
    func_in_13342(val);
    const int x = 13342;
    std::cout << 13342;
    for (int i = 0; i < 13342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13342(int& val) {
    const int x = 13342;
    std::cout << 13342;
    for (int i = 0; i < 13342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13343(int& val) {
    func_in_13343(val);
    const int x = 13343;
    std::cout << 13343;
    for (int i = 0; i < 13343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13343(int& val) {
    const int x = 13343;
    std::cout << 13343;
    for (int i = 0; i < 13343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13344(int& val) {
    func_in_13344(val);
    const int x = 13344;
    std::cout << 13344;
    for (int i = 0; i < 13344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13344(int& val) {
    const int x = 13344;
    std::cout << 13344;
    for (int i = 0; i < 13344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13345(int& val) {
    func_in_13345(val);
    const int x = 13345;
    std::cout << 13345;
    for (int i = 0; i < 13345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13345(int& val) {
    const int x = 13345;
    std::cout << 13345;
    for (int i = 0; i < 13345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13346(int& val) {
    func_in_13346(val);
    const int x = 13346;
    std::cout << 13346;
    for (int i = 0; i < 13346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13346(int& val) {
    const int x = 13346;
    std::cout << 13346;
    for (int i = 0; i < 13346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13347(int& val) {
    func_in_13347(val);
    const int x = 13347;
    std::cout << 13347;
    for (int i = 0; i < 13347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13347(int& val) {
    const int x = 13347;
    std::cout << 13347;
    for (int i = 0; i < 13347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13348(int& val) {
    func_in_13348(val);
    const int x = 13348;
    std::cout << 13348;
    for (int i = 0; i < 13348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13348(int& val) {
    const int x = 13348;
    std::cout << 13348;
    for (int i = 0; i < 13348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13349(int& val) {
    func_in_13349(val);
    const int x = 13349;
    std::cout << 13349;
    for (int i = 0; i < 13349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13349(int& val) {
    const int x = 13349;
    std::cout << 13349;
    for (int i = 0; i < 13349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13350(int& val) {
    func_in_13350(val);
    const int x = 13350;
    std::cout << 13350;
    for (int i = 0; i < 13350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13350(int& val) {
    const int x = 13350;
    std::cout << 13350;
    for (int i = 0; i < 13350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13351(int& val) {
    func_in_13351(val);
    const int x = 13351;
    std::cout << 13351;
    for (int i = 0; i < 13351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13351(int& val) {
    const int x = 13351;
    std::cout << 13351;
    for (int i = 0; i < 13351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13352(int& val) {
    func_in_13352(val);
    const int x = 13352;
    std::cout << 13352;
    for (int i = 0; i < 13352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13352(int& val) {
    const int x = 13352;
    std::cout << 13352;
    for (int i = 0; i < 13352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13353(int& val) {
    func_in_13353(val);
    const int x = 13353;
    std::cout << 13353;
    for (int i = 0; i < 13353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13353(int& val) {
    const int x = 13353;
    std::cout << 13353;
    for (int i = 0; i < 13353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13354(int& val) {
    func_in_13354(val);
    const int x = 13354;
    std::cout << 13354;
    for (int i = 0; i < 13354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13354(int& val) {
    const int x = 13354;
    std::cout << 13354;
    for (int i = 0; i < 13354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13355(int& val) {
    func_in_13355(val);
    const int x = 13355;
    std::cout << 13355;
    for (int i = 0; i < 13355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13355(int& val) {
    const int x = 13355;
    std::cout << 13355;
    for (int i = 0; i < 13355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13356(int& val) {
    func_in_13356(val);
    const int x = 13356;
    std::cout << 13356;
    for (int i = 0; i < 13356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13356(int& val) {
    const int x = 13356;
    std::cout << 13356;
    for (int i = 0; i < 13356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13357(int& val) {
    func_in_13357(val);
    const int x = 13357;
    std::cout << 13357;
    for (int i = 0; i < 13357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13357(int& val) {
    const int x = 13357;
    std::cout << 13357;
    for (int i = 0; i < 13357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13358(int& val) {
    func_in_13358(val);
    const int x = 13358;
    std::cout << 13358;
    for (int i = 0; i < 13358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13358(int& val) {
    const int x = 13358;
    std::cout << 13358;
    for (int i = 0; i < 13358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13359(int& val) {
    func_in_13359(val);
    const int x = 13359;
    std::cout << 13359;
    for (int i = 0; i < 13359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13359(int& val) {
    const int x = 13359;
    std::cout << 13359;
    for (int i = 0; i < 13359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13360(int& val) {
    func_in_13360(val);
    const int x = 13360;
    std::cout << 13360;
    for (int i = 0; i < 13360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13360(int& val) {
    const int x = 13360;
    std::cout << 13360;
    for (int i = 0; i < 13360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13361(int& val) {
    func_in_13361(val);
    const int x = 13361;
    std::cout << 13361;
    for (int i = 0; i < 13361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13361(int& val) {
    const int x = 13361;
    std::cout << 13361;
    for (int i = 0; i < 13361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13362(int& val) {
    func_in_13362(val);
    const int x = 13362;
    std::cout << 13362;
    for (int i = 0; i < 13362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13362(int& val) {
    const int x = 13362;
    std::cout << 13362;
    for (int i = 0; i < 13362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13363(int& val) {
    func_in_13363(val);
    const int x = 13363;
    std::cout << 13363;
    for (int i = 0; i < 13363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13363(int& val) {
    const int x = 13363;
    std::cout << 13363;
    for (int i = 0; i < 13363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13364(int& val) {
    func_in_13364(val);
    const int x = 13364;
    std::cout << 13364;
    for (int i = 0; i < 13364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13364(int& val) {
    const int x = 13364;
    std::cout << 13364;
    for (int i = 0; i < 13364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13365(int& val) {
    func_in_13365(val);
    const int x = 13365;
    std::cout << 13365;
    for (int i = 0; i < 13365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13365(int& val) {
    const int x = 13365;
    std::cout << 13365;
    for (int i = 0; i < 13365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13366(int& val) {
    func_in_13366(val);
    const int x = 13366;
    std::cout << 13366;
    for (int i = 0; i < 13366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13366(int& val) {
    const int x = 13366;
    std::cout << 13366;
    for (int i = 0; i < 13366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13367(int& val) {
    func_in_13367(val);
    const int x = 13367;
    std::cout << 13367;
    for (int i = 0; i < 13367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13367(int& val) {
    const int x = 13367;
    std::cout << 13367;
    for (int i = 0; i < 13367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13368(int& val) {
    func_in_13368(val);
    const int x = 13368;
    std::cout << 13368;
    for (int i = 0; i < 13368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13368(int& val) {
    const int x = 13368;
    std::cout << 13368;
    for (int i = 0; i < 13368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13369(int& val) {
    func_in_13369(val);
    const int x = 13369;
    std::cout << 13369;
    for (int i = 0; i < 13369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13369(int& val) {
    const int x = 13369;
    std::cout << 13369;
    for (int i = 0; i < 13369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13370(int& val) {
    func_in_13370(val);
    const int x = 13370;
    std::cout << 13370;
    for (int i = 0; i < 13370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13370(int& val) {
    const int x = 13370;
    std::cout << 13370;
    for (int i = 0; i < 13370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13371(int& val) {
    func_in_13371(val);
    const int x = 13371;
    std::cout << 13371;
    for (int i = 0; i < 13371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13371(int& val) {
    const int x = 13371;
    std::cout << 13371;
    for (int i = 0; i < 13371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13372(int& val) {
    func_in_13372(val);
    const int x = 13372;
    std::cout << 13372;
    for (int i = 0; i < 13372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13372(int& val) {
    const int x = 13372;
    std::cout << 13372;
    for (int i = 0; i < 13372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13373(int& val) {
    func_in_13373(val);
    const int x = 13373;
    std::cout << 13373;
    for (int i = 0; i < 13373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13373(int& val) {
    const int x = 13373;
    std::cout << 13373;
    for (int i = 0; i < 13373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13374(int& val) {
    func_in_13374(val);
    const int x = 13374;
    std::cout << 13374;
    for (int i = 0; i < 13374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13374(int& val) {
    const int x = 13374;
    std::cout << 13374;
    for (int i = 0; i < 13374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13375(int& val) {
    func_in_13375(val);
    const int x = 13375;
    std::cout << 13375;
    for (int i = 0; i < 13375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13375(int& val) {
    const int x = 13375;
    std::cout << 13375;
    for (int i = 0; i < 13375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13376(int& val) {
    func_in_13376(val);
    const int x = 13376;
    std::cout << 13376;
    for (int i = 0; i < 13376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13376(int& val) {
    const int x = 13376;
    std::cout << 13376;
    for (int i = 0; i < 13376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13377(int& val) {
    func_in_13377(val);
    const int x = 13377;
    std::cout << 13377;
    for (int i = 0; i < 13377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13377(int& val) {
    const int x = 13377;
    std::cout << 13377;
    for (int i = 0; i < 13377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13378(int& val) {
    func_in_13378(val);
    const int x = 13378;
    std::cout << 13378;
    for (int i = 0; i < 13378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13378(int& val) {
    const int x = 13378;
    std::cout << 13378;
    for (int i = 0; i < 13378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13379(int& val) {
    func_in_13379(val);
    const int x = 13379;
    std::cout << 13379;
    for (int i = 0; i < 13379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13379(int& val) {
    const int x = 13379;
    std::cout << 13379;
    for (int i = 0; i < 13379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13380(int& val) {
    func_in_13380(val);
    const int x = 13380;
    std::cout << 13380;
    for (int i = 0; i < 13380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13380(int& val) {
    const int x = 13380;
    std::cout << 13380;
    for (int i = 0; i < 13380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13381(int& val) {
    func_in_13381(val);
    const int x = 13381;
    std::cout << 13381;
    for (int i = 0; i < 13381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13381(int& val) {
    const int x = 13381;
    std::cout << 13381;
    for (int i = 0; i < 13381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13382(int& val) {
    func_in_13382(val);
    const int x = 13382;
    std::cout << 13382;
    for (int i = 0; i < 13382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13382(int& val) {
    const int x = 13382;
    std::cout << 13382;
    for (int i = 0; i < 13382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13383(int& val) {
    func_in_13383(val);
    const int x = 13383;
    std::cout << 13383;
    for (int i = 0; i < 13383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13383(int& val) {
    const int x = 13383;
    std::cout << 13383;
    for (int i = 0; i < 13383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13384(int& val) {
    func_in_13384(val);
    const int x = 13384;
    std::cout << 13384;
    for (int i = 0; i < 13384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13384(int& val) {
    const int x = 13384;
    std::cout << 13384;
    for (int i = 0; i < 13384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13385(int& val) {
    func_in_13385(val);
    const int x = 13385;
    std::cout << 13385;
    for (int i = 0; i < 13385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13385(int& val) {
    const int x = 13385;
    std::cout << 13385;
    for (int i = 0; i < 13385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13386(int& val) {
    func_in_13386(val);
    const int x = 13386;
    std::cout << 13386;
    for (int i = 0; i < 13386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13386(int& val) {
    const int x = 13386;
    std::cout << 13386;
    for (int i = 0; i < 13386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13387(int& val) {
    func_in_13387(val);
    const int x = 13387;
    std::cout << 13387;
    for (int i = 0; i < 13387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13387(int& val) {
    const int x = 13387;
    std::cout << 13387;
    for (int i = 0; i < 13387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13388(int& val) {
    func_in_13388(val);
    const int x = 13388;
    std::cout << 13388;
    for (int i = 0; i < 13388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13388(int& val) {
    const int x = 13388;
    std::cout << 13388;
    for (int i = 0; i < 13388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13389(int& val) {
    func_in_13389(val);
    const int x = 13389;
    std::cout << 13389;
    for (int i = 0; i < 13389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13389(int& val) {
    const int x = 13389;
    std::cout << 13389;
    for (int i = 0; i < 13389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13390(int& val) {
    func_in_13390(val);
    const int x = 13390;
    std::cout << 13390;
    for (int i = 0; i < 13390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13390(int& val) {
    const int x = 13390;
    std::cout << 13390;
    for (int i = 0; i < 13390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13391(int& val) {
    func_in_13391(val);
    const int x = 13391;
    std::cout << 13391;
    for (int i = 0; i < 13391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13391(int& val) {
    const int x = 13391;
    std::cout << 13391;
    for (int i = 0; i < 13391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13392(int& val) {
    func_in_13392(val);
    const int x = 13392;
    std::cout << 13392;
    for (int i = 0; i < 13392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13392(int& val) {
    const int x = 13392;
    std::cout << 13392;
    for (int i = 0; i < 13392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13393(int& val) {
    func_in_13393(val);
    const int x = 13393;
    std::cout << 13393;
    for (int i = 0; i < 13393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13393(int& val) {
    const int x = 13393;
    std::cout << 13393;
    for (int i = 0; i < 13393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13394(int& val) {
    func_in_13394(val);
    const int x = 13394;
    std::cout << 13394;
    for (int i = 0; i < 13394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13394(int& val) {
    const int x = 13394;
    std::cout << 13394;
    for (int i = 0; i < 13394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13395(int& val) {
    func_in_13395(val);
    const int x = 13395;
    std::cout << 13395;
    for (int i = 0; i < 13395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13395(int& val) {
    const int x = 13395;
    std::cout << 13395;
    for (int i = 0; i < 13395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13396(int& val) {
    func_in_13396(val);
    const int x = 13396;
    std::cout << 13396;
    for (int i = 0; i < 13396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13396(int& val) {
    const int x = 13396;
    std::cout << 13396;
    for (int i = 0; i < 13396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13397(int& val) {
    func_in_13397(val);
    const int x = 13397;
    std::cout << 13397;
    for (int i = 0; i < 13397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13397(int& val) {
    const int x = 13397;
    std::cout << 13397;
    for (int i = 0; i < 13397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13398(int& val) {
    func_in_13398(val);
    const int x = 13398;
    std::cout << 13398;
    for (int i = 0; i < 13398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13398(int& val) {
    const int x = 13398;
    std::cout << 13398;
    for (int i = 0; i < 13398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13399(int& val) {
    func_in_13399(val);
    const int x = 13399;
    std::cout << 13399;
    for (int i = 0; i < 13399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13399(int& val) {
    const int x = 13399;
    std::cout << 13399;
    for (int i = 0; i < 13399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13400(int& val) {
    func_in_13400(val);
    const int x = 13400;
    std::cout << 13400;
    for (int i = 0; i < 13400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13400(int& val) {
    const int x = 13400;
    std::cout << 13400;
    for (int i = 0; i < 13400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13401(int& val) {
    func_in_13401(val);
    const int x = 13401;
    std::cout << 13401;
    for (int i = 0; i < 13401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13401(int& val) {
    const int x = 13401;
    std::cout << 13401;
    for (int i = 0; i < 13401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13402(int& val) {
    func_in_13402(val);
    const int x = 13402;
    std::cout << 13402;
    for (int i = 0; i < 13402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13402(int& val) {
    const int x = 13402;
    std::cout << 13402;
    for (int i = 0; i < 13402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13403(int& val) {
    func_in_13403(val);
    const int x = 13403;
    std::cout << 13403;
    for (int i = 0; i < 13403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13403(int& val) {
    const int x = 13403;
    std::cout << 13403;
    for (int i = 0; i < 13403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13404(int& val) {
    func_in_13404(val);
    const int x = 13404;
    std::cout << 13404;
    for (int i = 0; i < 13404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13404(int& val) {
    const int x = 13404;
    std::cout << 13404;
    for (int i = 0; i < 13404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13405(int& val) {
    func_in_13405(val);
    const int x = 13405;
    std::cout << 13405;
    for (int i = 0; i < 13405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13405(int& val) {
    const int x = 13405;
    std::cout << 13405;
    for (int i = 0; i < 13405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13406(int& val) {
    func_in_13406(val);
    const int x = 13406;
    std::cout << 13406;
    for (int i = 0; i < 13406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13406(int& val) {
    const int x = 13406;
    std::cout << 13406;
    for (int i = 0; i < 13406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13407(int& val) {
    func_in_13407(val);
    const int x = 13407;
    std::cout << 13407;
    for (int i = 0; i < 13407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13407(int& val) {
    const int x = 13407;
    std::cout << 13407;
    for (int i = 0; i < 13407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13408(int& val) {
    func_in_13408(val);
    const int x = 13408;
    std::cout << 13408;
    for (int i = 0; i < 13408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13408(int& val) {
    const int x = 13408;
    std::cout << 13408;
    for (int i = 0; i < 13408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13409(int& val) {
    func_in_13409(val);
    const int x = 13409;
    std::cout << 13409;
    for (int i = 0; i < 13409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13409(int& val) {
    const int x = 13409;
    std::cout << 13409;
    for (int i = 0; i < 13409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13410(int& val) {
    func_in_13410(val);
    const int x = 13410;
    std::cout << 13410;
    for (int i = 0; i < 13410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13410(int& val) {
    const int x = 13410;
    std::cout << 13410;
    for (int i = 0; i < 13410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13411(int& val) {
    func_in_13411(val);
    const int x = 13411;
    std::cout << 13411;
    for (int i = 0; i < 13411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13411(int& val) {
    const int x = 13411;
    std::cout << 13411;
    for (int i = 0; i < 13411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13412(int& val) {
    func_in_13412(val);
    const int x = 13412;
    std::cout << 13412;
    for (int i = 0; i < 13412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13412(int& val) {
    const int x = 13412;
    std::cout << 13412;
    for (int i = 0; i < 13412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13413(int& val) {
    func_in_13413(val);
    const int x = 13413;
    std::cout << 13413;
    for (int i = 0; i < 13413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13413(int& val) {
    const int x = 13413;
    std::cout << 13413;
    for (int i = 0; i < 13413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13414(int& val) {
    func_in_13414(val);
    const int x = 13414;
    std::cout << 13414;
    for (int i = 0; i < 13414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13414(int& val) {
    const int x = 13414;
    std::cout << 13414;
    for (int i = 0; i < 13414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13415(int& val) {
    func_in_13415(val);
    const int x = 13415;
    std::cout << 13415;
    for (int i = 0; i < 13415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13415(int& val) {
    const int x = 13415;
    std::cout << 13415;
    for (int i = 0; i < 13415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13416(int& val) {
    func_in_13416(val);
    const int x = 13416;
    std::cout << 13416;
    for (int i = 0; i < 13416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13416(int& val) {
    const int x = 13416;
    std::cout << 13416;
    for (int i = 0; i < 13416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13417(int& val) {
    func_in_13417(val);
    const int x = 13417;
    std::cout << 13417;
    for (int i = 0; i < 13417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13417(int& val) {
    const int x = 13417;
    std::cout << 13417;
    for (int i = 0; i < 13417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13418(int& val) {
    func_in_13418(val);
    const int x = 13418;
    std::cout << 13418;
    for (int i = 0; i < 13418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13418(int& val) {
    const int x = 13418;
    std::cout << 13418;
    for (int i = 0; i < 13418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13419(int& val) {
    func_in_13419(val);
    const int x = 13419;
    std::cout << 13419;
    for (int i = 0; i < 13419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13419(int& val) {
    const int x = 13419;
    std::cout << 13419;
    for (int i = 0; i < 13419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13420(int& val) {
    func_in_13420(val);
    const int x = 13420;
    std::cout << 13420;
    for (int i = 0; i < 13420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13420(int& val) {
    const int x = 13420;
    std::cout << 13420;
    for (int i = 0; i < 13420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13421(int& val) {
    func_in_13421(val);
    const int x = 13421;
    std::cout << 13421;
    for (int i = 0; i < 13421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13421(int& val) {
    const int x = 13421;
    std::cout << 13421;
    for (int i = 0; i < 13421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13422(int& val) {
    func_in_13422(val);
    const int x = 13422;
    std::cout << 13422;
    for (int i = 0; i < 13422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13422(int& val) {
    const int x = 13422;
    std::cout << 13422;
    for (int i = 0; i < 13422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13423(int& val) {
    func_in_13423(val);
    const int x = 13423;
    std::cout << 13423;
    for (int i = 0; i < 13423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13423(int& val) {
    const int x = 13423;
    std::cout << 13423;
    for (int i = 0; i < 13423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13424(int& val) {
    func_in_13424(val);
    const int x = 13424;
    std::cout << 13424;
    for (int i = 0; i < 13424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13424(int& val) {
    const int x = 13424;
    std::cout << 13424;
    for (int i = 0; i < 13424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13425(int& val) {
    func_in_13425(val);
    const int x = 13425;
    std::cout << 13425;
    for (int i = 0; i < 13425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13425(int& val) {
    const int x = 13425;
    std::cout << 13425;
    for (int i = 0; i < 13425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13426(int& val) {
    func_in_13426(val);
    const int x = 13426;
    std::cout << 13426;
    for (int i = 0; i < 13426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13426(int& val) {
    const int x = 13426;
    std::cout << 13426;
    for (int i = 0; i < 13426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13427(int& val) {
    func_in_13427(val);
    const int x = 13427;
    std::cout << 13427;
    for (int i = 0; i < 13427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13427(int& val) {
    const int x = 13427;
    std::cout << 13427;
    for (int i = 0; i < 13427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13428(int& val) {
    func_in_13428(val);
    const int x = 13428;
    std::cout << 13428;
    for (int i = 0; i < 13428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13428(int& val) {
    const int x = 13428;
    std::cout << 13428;
    for (int i = 0; i < 13428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13429(int& val) {
    func_in_13429(val);
    const int x = 13429;
    std::cout << 13429;
    for (int i = 0; i < 13429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13429(int& val) {
    const int x = 13429;
    std::cout << 13429;
    for (int i = 0; i < 13429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13430(int& val) {
    func_in_13430(val);
    const int x = 13430;
    std::cout << 13430;
    for (int i = 0; i < 13430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13430(int& val) {
    const int x = 13430;
    std::cout << 13430;
    for (int i = 0; i < 13430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13431(int& val) {
    func_in_13431(val);
    const int x = 13431;
    std::cout << 13431;
    for (int i = 0; i < 13431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13431(int& val) {
    const int x = 13431;
    std::cout << 13431;
    for (int i = 0; i < 13431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13432(int& val) {
    func_in_13432(val);
    const int x = 13432;
    std::cout << 13432;
    for (int i = 0; i < 13432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13432(int& val) {
    const int x = 13432;
    std::cout << 13432;
    for (int i = 0; i < 13432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13433(int& val) {
    func_in_13433(val);
    const int x = 13433;
    std::cout << 13433;
    for (int i = 0; i < 13433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13433(int& val) {
    const int x = 13433;
    std::cout << 13433;
    for (int i = 0; i < 13433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13434(int& val) {
    func_in_13434(val);
    const int x = 13434;
    std::cout << 13434;
    for (int i = 0; i < 13434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13434(int& val) {
    const int x = 13434;
    std::cout << 13434;
    for (int i = 0; i < 13434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13435(int& val) {
    func_in_13435(val);
    const int x = 13435;
    std::cout << 13435;
    for (int i = 0; i < 13435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13435(int& val) {
    const int x = 13435;
    std::cout << 13435;
    for (int i = 0; i < 13435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13436(int& val) {
    func_in_13436(val);
    const int x = 13436;
    std::cout << 13436;
    for (int i = 0; i < 13436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13436(int& val) {
    const int x = 13436;
    std::cout << 13436;
    for (int i = 0; i < 13436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13437(int& val) {
    func_in_13437(val);
    const int x = 13437;
    std::cout << 13437;
    for (int i = 0; i < 13437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13437(int& val) {
    const int x = 13437;
    std::cout << 13437;
    for (int i = 0; i < 13437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13438(int& val) {
    func_in_13438(val);
    const int x = 13438;
    std::cout << 13438;
    for (int i = 0; i < 13438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13438(int& val) {
    const int x = 13438;
    std::cout << 13438;
    for (int i = 0; i < 13438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13439(int& val) {
    func_in_13439(val);
    const int x = 13439;
    std::cout << 13439;
    for (int i = 0; i < 13439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13439(int& val) {
    const int x = 13439;
    std::cout << 13439;
    for (int i = 0; i < 13439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13440(int& val) {
    func_in_13440(val);
    const int x = 13440;
    std::cout << 13440;
    for (int i = 0; i < 13440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13440(int& val) {
    const int x = 13440;
    std::cout << 13440;
    for (int i = 0; i < 13440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13441(int& val) {
    func_in_13441(val);
    const int x = 13441;
    std::cout << 13441;
    for (int i = 0; i < 13441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13441(int& val) {
    const int x = 13441;
    std::cout << 13441;
    for (int i = 0; i < 13441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13442(int& val) {
    func_in_13442(val);
    const int x = 13442;
    std::cout << 13442;
    for (int i = 0; i < 13442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13442(int& val) {
    const int x = 13442;
    std::cout << 13442;
    for (int i = 0; i < 13442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13443(int& val) {
    func_in_13443(val);
    const int x = 13443;
    std::cout << 13443;
    for (int i = 0; i < 13443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13443(int& val) {
    const int x = 13443;
    std::cout << 13443;
    for (int i = 0; i < 13443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13444(int& val) {
    func_in_13444(val);
    const int x = 13444;
    std::cout << 13444;
    for (int i = 0; i < 13444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13444(int& val) {
    const int x = 13444;
    std::cout << 13444;
    for (int i = 0; i < 13444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13445(int& val) {
    func_in_13445(val);
    const int x = 13445;
    std::cout << 13445;
    for (int i = 0; i < 13445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13445(int& val) {
    const int x = 13445;
    std::cout << 13445;
    for (int i = 0; i < 13445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13446(int& val) {
    func_in_13446(val);
    const int x = 13446;
    std::cout << 13446;
    for (int i = 0; i < 13446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13446(int& val) {
    const int x = 13446;
    std::cout << 13446;
    for (int i = 0; i < 13446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13447(int& val) {
    func_in_13447(val);
    const int x = 13447;
    std::cout << 13447;
    for (int i = 0; i < 13447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13447(int& val) {
    const int x = 13447;
    std::cout << 13447;
    for (int i = 0; i < 13447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13448(int& val) {
    func_in_13448(val);
    const int x = 13448;
    std::cout << 13448;
    for (int i = 0; i < 13448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13448(int& val) {
    const int x = 13448;
    std::cout << 13448;
    for (int i = 0; i < 13448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13449(int& val) {
    func_in_13449(val);
    const int x = 13449;
    std::cout << 13449;
    for (int i = 0; i < 13449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13449(int& val) {
    const int x = 13449;
    std::cout << 13449;
    for (int i = 0; i < 13449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13450(int& val) {
    func_in_13450(val);
    const int x = 13450;
    std::cout << 13450;
    for (int i = 0; i < 13450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13450(int& val) {
    const int x = 13450;
    std::cout << 13450;
    for (int i = 0; i < 13450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13451(int& val) {
    func_in_13451(val);
    const int x = 13451;
    std::cout << 13451;
    for (int i = 0; i < 13451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13451(int& val) {
    const int x = 13451;
    std::cout << 13451;
    for (int i = 0; i < 13451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13452(int& val) {
    func_in_13452(val);
    const int x = 13452;
    std::cout << 13452;
    for (int i = 0; i < 13452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13452(int& val) {
    const int x = 13452;
    std::cout << 13452;
    for (int i = 0; i < 13452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13453(int& val) {
    func_in_13453(val);
    const int x = 13453;
    std::cout << 13453;
    for (int i = 0; i < 13453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13453(int& val) {
    const int x = 13453;
    std::cout << 13453;
    for (int i = 0; i < 13453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13454(int& val) {
    func_in_13454(val);
    const int x = 13454;
    std::cout << 13454;
    for (int i = 0; i < 13454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13454(int& val) {
    const int x = 13454;
    std::cout << 13454;
    for (int i = 0; i < 13454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13455(int& val) {
    func_in_13455(val);
    const int x = 13455;
    std::cout << 13455;
    for (int i = 0; i < 13455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13455(int& val) {
    const int x = 13455;
    std::cout << 13455;
    for (int i = 0; i < 13455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13456(int& val) {
    func_in_13456(val);
    const int x = 13456;
    std::cout << 13456;
    for (int i = 0; i < 13456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13456(int& val) {
    const int x = 13456;
    std::cout << 13456;
    for (int i = 0; i < 13456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13457(int& val) {
    func_in_13457(val);
    const int x = 13457;
    std::cout << 13457;
    for (int i = 0; i < 13457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13457(int& val) {
    const int x = 13457;
    std::cout << 13457;
    for (int i = 0; i < 13457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13458(int& val) {
    func_in_13458(val);
    const int x = 13458;
    std::cout << 13458;
    for (int i = 0; i < 13458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13458(int& val) {
    const int x = 13458;
    std::cout << 13458;
    for (int i = 0; i < 13458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13459(int& val) {
    func_in_13459(val);
    const int x = 13459;
    std::cout << 13459;
    for (int i = 0; i < 13459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13459(int& val) {
    const int x = 13459;
    std::cout << 13459;
    for (int i = 0; i < 13459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13460(int& val) {
    func_in_13460(val);
    const int x = 13460;
    std::cout << 13460;
    for (int i = 0; i < 13460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13460(int& val) {
    const int x = 13460;
    std::cout << 13460;
    for (int i = 0; i < 13460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13461(int& val) {
    func_in_13461(val);
    const int x = 13461;
    std::cout << 13461;
    for (int i = 0; i < 13461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13461(int& val) {
    const int x = 13461;
    std::cout << 13461;
    for (int i = 0; i < 13461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13462(int& val) {
    func_in_13462(val);
    const int x = 13462;
    std::cout << 13462;
    for (int i = 0; i < 13462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13462(int& val) {
    const int x = 13462;
    std::cout << 13462;
    for (int i = 0; i < 13462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13463(int& val) {
    func_in_13463(val);
    const int x = 13463;
    std::cout << 13463;
    for (int i = 0; i < 13463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13463(int& val) {
    const int x = 13463;
    std::cout << 13463;
    for (int i = 0; i < 13463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13464(int& val) {
    func_in_13464(val);
    const int x = 13464;
    std::cout << 13464;
    for (int i = 0; i < 13464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13464(int& val) {
    const int x = 13464;
    std::cout << 13464;
    for (int i = 0; i < 13464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13465(int& val) {
    func_in_13465(val);
    const int x = 13465;
    std::cout << 13465;
    for (int i = 0; i < 13465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13465(int& val) {
    const int x = 13465;
    std::cout << 13465;
    for (int i = 0; i < 13465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13466(int& val) {
    func_in_13466(val);
    const int x = 13466;
    std::cout << 13466;
    for (int i = 0; i < 13466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13466(int& val) {
    const int x = 13466;
    std::cout << 13466;
    for (int i = 0; i < 13466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13467(int& val) {
    func_in_13467(val);
    const int x = 13467;
    std::cout << 13467;
    for (int i = 0; i < 13467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13467(int& val) {
    const int x = 13467;
    std::cout << 13467;
    for (int i = 0; i < 13467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13468(int& val) {
    func_in_13468(val);
    const int x = 13468;
    std::cout << 13468;
    for (int i = 0; i < 13468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13468(int& val) {
    const int x = 13468;
    std::cout << 13468;
    for (int i = 0; i < 13468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13469(int& val) {
    func_in_13469(val);
    const int x = 13469;
    std::cout << 13469;
    for (int i = 0; i < 13469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13469(int& val) {
    const int x = 13469;
    std::cout << 13469;
    for (int i = 0; i < 13469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13470(int& val) {
    func_in_13470(val);
    const int x = 13470;
    std::cout << 13470;
    for (int i = 0; i < 13470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13470(int& val) {
    const int x = 13470;
    std::cout << 13470;
    for (int i = 0; i < 13470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13471(int& val) {
    func_in_13471(val);
    const int x = 13471;
    std::cout << 13471;
    for (int i = 0; i < 13471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13471(int& val) {
    const int x = 13471;
    std::cout << 13471;
    for (int i = 0; i < 13471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13472(int& val) {
    func_in_13472(val);
    const int x = 13472;
    std::cout << 13472;
    for (int i = 0; i < 13472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13472(int& val) {
    const int x = 13472;
    std::cout << 13472;
    for (int i = 0; i < 13472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13473(int& val) {
    func_in_13473(val);
    const int x = 13473;
    std::cout << 13473;
    for (int i = 0; i < 13473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13473(int& val) {
    const int x = 13473;
    std::cout << 13473;
    for (int i = 0; i < 13473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13474(int& val) {
    func_in_13474(val);
    const int x = 13474;
    std::cout << 13474;
    for (int i = 0; i < 13474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13474(int& val) {
    const int x = 13474;
    std::cout << 13474;
    for (int i = 0; i < 13474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13475(int& val) {
    func_in_13475(val);
    const int x = 13475;
    std::cout << 13475;
    for (int i = 0; i < 13475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13475(int& val) {
    const int x = 13475;
    std::cout << 13475;
    for (int i = 0; i < 13475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13476(int& val) {
    func_in_13476(val);
    const int x = 13476;
    std::cout << 13476;
    for (int i = 0; i < 13476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13476(int& val) {
    const int x = 13476;
    std::cout << 13476;
    for (int i = 0; i < 13476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13477(int& val) {
    func_in_13477(val);
    const int x = 13477;
    std::cout << 13477;
    for (int i = 0; i < 13477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13477(int& val) {
    const int x = 13477;
    std::cout << 13477;
    for (int i = 0; i < 13477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13478(int& val) {
    func_in_13478(val);
    const int x = 13478;
    std::cout << 13478;
    for (int i = 0; i < 13478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13478(int& val) {
    const int x = 13478;
    std::cout << 13478;
    for (int i = 0; i < 13478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13479(int& val) {
    func_in_13479(val);
    const int x = 13479;
    std::cout << 13479;
    for (int i = 0; i < 13479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13479(int& val) {
    const int x = 13479;
    std::cout << 13479;
    for (int i = 0; i < 13479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13480(int& val) {
    func_in_13480(val);
    const int x = 13480;
    std::cout << 13480;
    for (int i = 0; i < 13480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13480(int& val) {
    const int x = 13480;
    std::cout << 13480;
    for (int i = 0; i < 13480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13481(int& val) {
    func_in_13481(val);
    const int x = 13481;
    std::cout << 13481;
    for (int i = 0; i < 13481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13481(int& val) {
    const int x = 13481;
    std::cout << 13481;
    for (int i = 0; i < 13481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13482(int& val) {
    func_in_13482(val);
    const int x = 13482;
    std::cout << 13482;
    for (int i = 0; i < 13482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13482(int& val) {
    const int x = 13482;
    std::cout << 13482;
    for (int i = 0; i < 13482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13483(int& val) {
    func_in_13483(val);
    const int x = 13483;
    std::cout << 13483;
    for (int i = 0; i < 13483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13483(int& val) {
    const int x = 13483;
    std::cout << 13483;
    for (int i = 0; i < 13483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13484(int& val) {
    func_in_13484(val);
    const int x = 13484;
    std::cout << 13484;
    for (int i = 0; i < 13484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13484(int& val) {
    const int x = 13484;
    std::cout << 13484;
    for (int i = 0; i < 13484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13485(int& val) {
    func_in_13485(val);
    const int x = 13485;
    std::cout << 13485;
    for (int i = 0; i < 13485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13485(int& val) {
    const int x = 13485;
    std::cout << 13485;
    for (int i = 0; i < 13485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13486(int& val) {
    func_in_13486(val);
    const int x = 13486;
    std::cout << 13486;
    for (int i = 0; i < 13486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13486(int& val) {
    const int x = 13486;
    std::cout << 13486;
    for (int i = 0; i < 13486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13487(int& val) {
    func_in_13487(val);
    const int x = 13487;
    std::cout << 13487;
    for (int i = 0; i < 13487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13487(int& val) {
    const int x = 13487;
    std::cout << 13487;
    for (int i = 0; i < 13487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13488(int& val) {
    func_in_13488(val);
    const int x = 13488;
    std::cout << 13488;
    for (int i = 0; i < 13488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13488(int& val) {
    const int x = 13488;
    std::cout << 13488;
    for (int i = 0; i < 13488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13489(int& val) {
    func_in_13489(val);
    const int x = 13489;
    std::cout << 13489;
    for (int i = 0; i < 13489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13489(int& val) {
    const int x = 13489;
    std::cout << 13489;
    for (int i = 0; i < 13489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13490(int& val) {
    func_in_13490(val);
    const int x = 13490;
    std::cout << 13490;
    for (int i = 0; i < 13490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13490(int& val) {
    const int x = 13490;
    std::cout << 13490;
    for (int i = 0; i < 13490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13491(int& val) {
    func_in_13491(val);
    const int x = 13491;
    std::cout << 13491;
    for (int i = 0; i < 13491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13491(int& val) {
    const int x = 13491;
    std::cout << 13491;
    for (int i = 0; i < 13491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13492(int& val) {
    func_in_13492(val);
    const int x = 13492;
    std::cout << 13492;
    for (int i = 0; i < 13492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13492(int& val) {
    const int x = 13492;
    std::cout << 13492;
    for (int i = 0; i < 13492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13493(int& val) {
    func_in_13493(val);
    const int x = 13493;
    std::cout << 13493;
    for (int i = 0; i < 13493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13493(int& val) {
    const int x = 13493;
    std::cout << 13493;
    for (int i = 0; i < 13493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13494(int& val) {
    func_in_13494(val);
    const int x = 13494;
    std::cout << 13494;
    for (int i = 0; i < 13494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13494(int& val) {
    const int x = 13494;
    std::cout << 13494;
    for (int i = 0; i < 13494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13495(int& val) {
    func_in_13495(val);
    const int x = 13495;
    std::cout << 13495;
    for (int i = 0; i < 13495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13495(int& val) {
    const int x = 13495;
    std::cout << 13495;
    for (int i = 0; i < 13495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13496(int& val) {
    func_in_13496(val);
    const int x = 13496;
    std::cout << 13496;
    for (int i = 0; i < 13496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13496(int& val) {
    const int x = 13496;
    std::cout << 13496;
    for (int i = 0; i < 13496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13497(int& val) {
    func_in_13497(val);
    const int x = 13497;
    std::cout << 13497;
    for (int i = 0; i < 13497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13497(int& val) {
    const int x = 13497;
    std::cout << 13497;
    for (int i = 0; i < 13497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13498(int& val) {
    func_in_13498(val);
    const int x = 13498;
    std::cout << 13498;
    for (int i = 0; i < 13498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13498(int& val) {
    const int x = 13498;
    std::cout << 13498;
    for (int i = 0; i < 13498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13499(int& val) {
    func_in_13499(val);
    const int x = 13499;
    std::cout << 13499;
    for (int i = 0; i < 13499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13499(int& val) {
    const int x = 13499;
    std::cout << 13499;
    for (int i = 0; i < 13499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13500(int& val) {
    func_in_13500(val);
    const int x = 13500;
    std::cout << 13500;
    for (int i = 0; i < 13500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13500(int& val) {
    const int x = 13500;
    std::cout << 13500;
    for (int i = 0; i < 13500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13501(int& val) {
    func_in_13501(val);
    const int x = 13501;
    std::cout << 13501;
    for (int i = 0; i < 13501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13501(int& val) {
    const int x = 13501;
    std::cout << 13501;
    for (int i = 0; i < 13501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13502(int& val) {
    func_in_13502(val);
    const int x = 13502;
    std::cout << 13502;
    for (int i = 0; i < 13502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13502(int& val) {
    const int x = 13502;
    std::cout << 13502;
    for (int i = 0; i < 13502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13503(int& val) {
    func_in_13503(val);
    const int x = 13503;
    std::cout << 13503;
    for (int i = 0; i < 13503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13503(int& val) {
    const int x = 13503;
    std::cout << 13503;
    for (int i = 0; i < 13503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13504(int& val) {
    func_in_13504(val);
    const int x = 13504;
    std::cout << 13504;
    for (int i = 0; i < 13504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13504(int& val) {
    const int x = 13504;
    std::cout << 13504;
    for (int i = 0; i < 13504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13505(int& val) {
    func_in_13505(val);
    const int x = 13505;
    std::cout << 13505;
    for (int i = 0; i < 13505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13505(int& val) {
    const int x = 13505;
    std::cout << 13505;
    for (int i = 0; i < 13505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13506(int& val) {
    func_in_13506(val);
    const int x = 13506;
    std::cout << 13506;
    for (int i = 0; i < 13506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13506(int& val) {
    const int x = 13506;
    std::cout << 13506;
    for (int i = 0; i < 13506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13507(int& val) {
    func_in_13507(val);
    const int x = 13507;
    std::cout << 13507;
    for (int i = 0; i < 13507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13507(int& val) {
    const int x = 13507;
    std::cout << 13507;
    for (int i = 0; i < 13507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13508(int& val) {
    func_in_13508(val);
    const int x = 13508;
    std::cout << 13508;
    for (int i = 0; i < 13508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13508(int& val) {
    const int x = 13508;
    std::cout << 13508;
    for (int i = 0; i < 13508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13509(int& val) {
    func_in_13509(val);
    const int x = 13509;
    std::cout << 13509;
    for (int i = 0; i < 13509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13509(int& val) {
    const int x = 13509;
    std::cout << 13509;
    for (int i = 0; i < 13509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13510(int& val) {
    func_in_13510(val);
    const int x = 13510;
    std::cout << 13510;
    for (int i = 0; i < 13510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13510(int& val) {
    const int x = 13510;
    std::cout << 13510;
    for (int i = 0; i < 13510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13511(int& val) {
    func_in_13511(val);
    const int x = 13511;
    std::cout << 13511;
    for (int i = 0; i < 13511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13511(int& val) {
    const int x = 13511;
    std::cout << 13511;
    for (int i = 0; i < 13511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13512(int& val) {
    func_in_13512(val);
    const int x = 13512;
    std::cout << 13512;
    for (int i = 0; i < 13512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13512(int& val) {
    const int x = 13512;
    std::cout << 13512;
    for (int i = 0; i < 13512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13513(int& val) {
    func_in_13513(val);
    const int x = 13513;
    std::cout << 13513;
    for (int i = 0; i < 13513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13513(int& val) {
    const int x = 13513;
    std::cout << 13513;
    for (int i = 0; i < 13513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13514(int& val) {
    func_in_13514(val);
    const int x = 13514;
    std::cout << 13514;
    for (int i = 0; i < 13514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13514(int& val) {
    const int x = 13514;
    std::cout << 13514;
    for (int i = 0; i < 13514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13515(int& val) {
    func_in_13515(val);
    const int x = 13515;
    std::cout << 13515;
    for (int i = 0; i < 13515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13515(int& val) {
    const int x = 13515;
    std::cout << 13515;
    for (int i = 0; i < 13515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13516(int& val) {
    func_in_13516(val);
    const int x = 13516;
    std::cout << 13516;
    for (int i = 0; i < 13516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13516(int& val) {
    const int x = 13516;
    std::cout << 13516;
    for (int i = 0; i < 13516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13517(int& val) {
    func_in_13517(val);
    const int x = 13517;
    std::cout << 13517;
    for (int i = 0; i < 13517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13517(int& val) {
    const int x = 13517;
    std::cout << 13517;
    for (int i = 0; i < 13517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13518(int& val) {
    func_in_13518(val);
    const int x = 13518;
    std::cout << 13518;
    for (int i = 0; i < 13518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13518(int& val) {
    const int x = 13518;
    std::cout << 13518;
    for (int i = 0; i < 13518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13519(int& val) {
    func_in_13519(val);
    const int x = 13519;
    std::cout << 13519;
    for (int i = 0; i < 13519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13519(int& val) {
    const int x = 13519;
    std::cout << 13519;
    for (int i = 0; i < 13519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13520(int& val) {
    func_in_13520(val);
    const int x = 13520;
    std::cout << 13520;
    for (int i = 0; i < 13520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13520(int& val) {
    const int x = 13520;
    std::cout << 13520;
    for (int i = 0; i < 13520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13521(int& val) {
    func_in_13521(val);
    const int x = 13521;
    std::cout << 13521;
    for (int i = 0; i < 13521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13521(int& val) {
    const int x = 13521;
    std::cout << 13521;
    for (int i = 0; i < 13521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13522(int& val) {
    func_in_13522(val);
    const int x = 13522;
    std::cout << 13522;
    for (int i = 0; i < 13522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13522(int& val) {
    const int x = 13522;
    std::cout << 13522;
    for (int i = 0; i < 13522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13523(int& val) {
    func_in_13523(val);
    const int x = 13523;
    std::cout << 13523;
    for (int i = 0; i < 13523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13523(int& val) {
    const int x = 13523;
    std::cout << 13523;
    for (int i = 0; i < 13523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13524(int& val) {
    func_in_13524(val);
    const int x = 13524;
    std::cout << 13524;
    for (int i = 0; i < 13524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13524(int& val) {
    const int x = 13524;
    std::cout << 13524;
    for (int i = 0; i < 13524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13525(int& val) {
    func_in_13525(val);
    const int x = 13525;
    std::cout << 13525;
    for (int i = 0; i < 13525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13525(int& val) {
    const int x = 13525;
    std::cout << 13525;
    for (int i = 0; i < 13525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13526(int& val) {
    func_in_13526(val);
    const int x = 13526;
    std::cout << 13526;
    for (int i = 0; i < 13526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13526(int& val) {
    const int x = 13526;
    std::cout << 13526;
    for (int i = 0; i < 13526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13527(int& val) {
    func_in_13527(val);
    const int x = 13527;
    std::cout << 13527;
    for (int i = 0; i < 13527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13527(int& val) {
    const int x = 13527;
    std::cout << 13527;
    for (int i = 0; i < 13527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13528(int& val) {
    func_in_13528(val);
    const int x = 13528;
    std::cout << 13528;
    for (int i = 0; i < 13528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13528(int& val) {
    const int x = 13528;
    std::cout << 13528;
    for (int i = 0; i < 13528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13529(int& val) {
    func_in_13529(val);
    const int x = 13529;
    std::cout << 13529;
    for (int i = 0; i < 13529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13529(int& val) {
    const int x = 13529;
    std::cout << 13529;
    for (int i = 0; i < 13529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13530(int& val) {
    func_in_13530(val);
    const int x = 13530;
    std::cout << 13530;
    for (int i = 0; i < 13530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13530(int& val) {
    const int x = 13530;
    std::cout << 13530;
    for (int i = 0; i < 13530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13531(int& val) {
    func_in_13531(val);
    const int x = 13531;
    std::cout << 13531;
    for (int i = 0; i < 13531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13531(int& val) {
    const int x = 13531;
    std::cout << 13531;
    for (int i = 0; i < 13531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13532(int& val) {
    func_in_13532(val);
    const int x = 13532;
    std::cout << 13532;
    for (int i = 0; i < 13532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13532(int& val) {
    const int x = 13532;
    std::cout << 13532;
    for (int i = 0; i < 13532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13533(int& val) {
    func_in_13533(val);
    const int x = 13533;
    std::cout << 13533;
    for (int i = 0; i < 13533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13533(int& val) {
    const int x = 13533;
    std::cout << 13533;
    for (int i = 0; i < 13533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13534(int& val) {
    func_in_13534(val);
    const int x = 13534;
    std::cout << 13534;
    for (int i = 0; i < 13534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13534(int& val) {
    const int x = 13534;
    std::cout << 13534;
    for (int i = 0; i < 13534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13535(int& val) {
    func_in_13535(val);
    const int x = 13535;
    std::cout << 13535;
    for (int i = 0; i < 13535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13535(int& val) {
    const int x = 13535;
    std::cout << 13535;
    for (int i = 0; i < 13535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13536(int& val) {
    func_in_13536(val);
    const int x = 13536;
    std::cout << 13536;
    for (int i = 0; i < 13536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13536(int& val) {
    const int x = 13536;
    std::cout << 13536;
    for (int i = 0; i < 13536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13537(int& val) {
    func_in_13537(val);
    const int x = 13537;
    std::cout << 13537;
    for (int i = 0; i < 13537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13537(int& val) {
    const int x = 13537;
    std::cout << 13537;
    for (int i = 0; i < 13537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13538(int& val) {
    func_in_13538(val);
    const int x = 13538;
    std::cout << 13538;
    for (int i = 0; i < 13538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13538(int& val) {
    const int x = 13538;
    std::cout << 13538;
    for (int i = 0; i < 13538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13539(int& val) {
    func_in_13539(val);
    const int x = 13539;
    std::cout << 13539;
    for (int i = 0; i < 13539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13539(int& val) {
    const int x = 13539;
    std::cout << 13539;
    for (int i = 0; i < 13539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13540(int& val) {
    func_in_13540(val);
    const int x = 13540;
    std::cout << 13540;
    for (int i = 0; i < 13540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13540(int& val) {
    const int x = 13540;
    std::cout << 13540;
    for (int i = 0; i < 13540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13541(int& val) {
    func_in_13541(val);
    const int x = 13541;
    std::cout << 13541;
    for (int i = 0; i < 13541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13541(int& val) {
    const int x = 13541;
    std::cout << 13541;
    for (int i = 0; i < 13541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13542(int& val) {
    func_in_13542(val);
    const int x = 13542;
    std::cout << 13542;
    for (int i = 0; i < 13542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13542(int& val) {
    const int x = 13542;
    std::cout << 13542;
    for (int i = 0; i < 13542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13543(int& val) {
    func_in_13543(val);
    const int x = 13543;
    std::cout << 13543;
    for (int i = 0; i < 13543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13543(int& val) {
    const int x = 13543;
    std::cout << 13543;
    for (int i = 0; i < 13543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13544(int& val) {
    func_in_13544(val);
    const int x = 13544;
    std::cout << 13544;
    for (int i = 0; i < 13544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13544(int& val) {
    const int x = 13544;
    std::cout << 13544;
    for (int i = 0; i < 13544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13545(int& val) {
    func_in_13545(val);
    const int x = 13545;
    std::cout << 13545;
    for (int i = 0; i < 13545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13545(int& val) {
    const int x = 13545;
    std::cout << 13545;
    for (int i = 0; i < 13545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13546(int& val) {
    func_in_13546(val);
    const int x = 13546;
    std::cout << 13546;
    for (int i = 0; i < 13546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13546(int& val) {
    const int x = 13546;
    std::cout << 13546;
    for (int i = 0; i < 13546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13547(int& val) {
    func_in_13547(val);
    const int x = 13547;
    std::cout << 13547;
    for (int i = 0; i < 13547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13547(int& val) {
    const int x = 13547;
    std::cout << 13547;
    for (int i = 0; i < 13547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13548(int& val) {
    func_in_13548(val);
    const int x = 13548;
    std::cout << 13548;
    for (int i = 0; i < 13548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13548(int& val) {
    const int x = 13548;
    std::cout << 13548;
    for (int i = 0; i < 13548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13549(int& val) {
    func_in_13549(val);
    const int x = 13549;
    std::cout << 13549;
    for (int i = 0; i < 13549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13549(int& val) {
    const int x = 13549;
    std::cout << 13549;
    for (int i = 0; i < 13549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13550(int& val) {
    func_in_13550(val);
    const int x = 13550;
    std::cout << 13550;
    for (int i = 0; i < 13550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13550(int& val) {
    const int x = 13550;
    std::cout << 13550;
    for (int i = 0; i < 13550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13551(int& val) {
    func_in_13551(val);
    const int x = 13551;
    std::cout << 13551;
    for (int i = 0; i < 13551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13551(int& val) {
    const int x = 13551;
    std::cout << 13551;
    for (int i = 0; i < 13551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13552(int& val) {
    func_in_13552(val);
    const int x = 13552;
    std::cout << 13552;
    for (int i = 0; i < 13552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13552(int& val) {
    const int x = 13552;
    std::cout << 13552;
    for (int i = 0; i < 13552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13553(int& val) {
    func_in_13553(val);
    const int x = 13553;
    std::cout << 13553;
    for (int i = 0; i < 13553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13553(int& val) {
    const int x = 13553;
    std::cout << 13553;
    for (int i = 0; i < 13553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13554(int& val) {
    func_in_13554(val);
    const int x = 13554;
    std::cout << 13554;
    for (int i = 0; i < 13554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13554(int& val) {
    const int x = 13554;
    std::cout << 13554;
    for (int i = 0; i < 13554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13555(int& val) {
    func_in_13555(val);
    const int x = 13555;
    std::cout << 13555;
    for (int i = 0; i < 13555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13555(int& val) {
    const int x = 13555;
    std::cout << 13555;
    for (int i = 0; i < 13555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13556(int& val) {
    func_in_13556(val);
    const int x = 13556;
    std::cout << 13556;
    for (int i = 0; i < 13556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13556(int& val) {
    const int x = 13556;
    std::cout << 13556;
    for (int i = 0; i < 13556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13557(int& val) {
    func_in_13557(val);
    const int x = 13557;
    std::cout << 13557;
    for (int i = 0; i < 13557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13557(int& val) {
    const int x = 13557;
    std::cout << 13557;
    for (int i = 0; i < 13557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13558(int& val) {
    func_in_13558(val);
    const int x = 13558;
    std::cout << 13558;
    for (int i = 0; i < 13558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13558(int& val) {
    const int x = 13558;
    std::cout << 13558;
    for (int i = 0; i < 13558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13559(int& val) {
    func_in_13559(val);
    const int x = 13559;
    std::cout << 13559;
    for (int i = 0; i < 13559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13559(int& val) {
    const int x = 13559;
    std::cout << 13559;
    for (int i = 0; i < 13559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13560(int& val) {
    func_in_13560(val);
    const int x = 13560;
    std::cout << 13560;
    for (int i = 0; i < 13560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13560(int& val) {
    const int x = 13560;
    std::cout << 13560;
    for (int i = 0; i < 13560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13561(int& val) {
    func_in_13561(val);
    const int x = 13561;
    std::cout << 13561;
    for (int i = 0; i < 13561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13561(int& val) {
    const int x = 13561;
    std::cout << 13561;
    for (int i = 0; i < 13561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13562(int& val) {
    func_in_13562(val);
    const int x = 13562;
    std::cout << 13562;
    for (int i = 0; i < 13562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13562(int& val) {
    const int x = 13562;
    std::cout << 13562;
    for (int i = 0; i < 13562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13563(int& val) {
    func_in_13563(val);
    const int x = 13563;
    std::cout << 13563;
    for (int i = 0; i < 13563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13563(int& val) {
    const int x = 13563;
    std::cout << 13563;
    for (int i = 0; i < 13563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13564(int& val) {
    func_in_13564(val);
    const int x = 13564;
    std::cout << 13564;
    for (int i = 0; i < 13564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13564(int& val) {
    const int x = 13564;
    std::cout << 13564;
    for (int i = 0; i < 13564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13565(int& val) {
    func_in_13565(val);
    const int x = 13565;
    std::cout << 13565;
    for (int i = 0; i < 13565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13565(int& val) {
    const int x = 13565;
    std::cout << 13565;
    for (int i = 0; i < 13565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13566(int& val) {
    func_in_13566(val);
    const int x = 13566;
    std::cout << 13566;
    for (int i = 0; i < 13566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13566(int& val) {
    const int x = 13566;
    std::cout << 13566;
    for (int i = 0; i < 13566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13567(int& val) {
    func_in_13567(val);
    const int x = 13567;
    std::cout << 13567;
    for (int i = 0; i < 13567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13567(int& val) {
    const int x = 13567;
    std::cout << 13567;
    for (int i = 0; i < 13567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13568(int& val) {
    func_in_13568(val);
    const int x = 13568;
    std::cout << 13568;
    for (int i = 0; i < 13568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13568(int& val) {
    const int x = 13568;
    std::cout << 13568;
    for (int i = 0; i < 13568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13569(int& val) {
    func_in_13569(val);
    const int x = 13569;
    std::cout << 13569;
    for (int i = 0; i < 13569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13569(int& val) {
    const int x = 13569;
    std::cout << 13569;
    for (int i = 0; i < 13569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13570(int& val) {
    func_in_13570(val);
    const int x = 13570;
    std::cout << 13570;
    for (int i = 0; i < 13570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13570(int& val) {
    const int x = 13570;
    std::cout << 13570;
    for (int i = 0; i < 13570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13571(int& val) {
    func_in_13571(val);
    const int x = 13571;
    std::cout << 13571;
    for (int i = 0; i < 13571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13571(int& val) {
    const int x = 13571;
    std::cout << 13571;
    for (int i = 0; i < 13571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13572(int& val) {
    func_in_13572(val);
    const int x = 13572;
    std::cout << 13572;
    for (int i = 0; i < 13572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13572(int& val) {
    const int x = 13572;
    std::cout << 13572;
    for (int i = 0; i < 13572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13573(int& val) {
    func_in_13573(val);
    const int x = 13573;
    std::cout << 13573;
    for (int i = 0; i < 13573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13573(int& val) {
    const int x = 13573;
    std::cout << 13573;
    for (int i = 0; i < 13573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13574(int& val) {
    func_in_13574(val);
    const int x = 13574;
    std::cout << 13574;
    for (int i = 0; i < 13574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13574(int& val) {
    const int x = 13574;
    std::cout << 13574;
    for (int i = 0; i < 13574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13575(int& val) {
    func_in_13575(val);
    const int x = 13575;
    std::cout << 13575;
    for (int i = 0; i < 13575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13575(int& val) {
    const int x = 13575;
    std::cout << 13575;
    for (int i = 0; i < 13575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13576(int& val) {
    func_in_13576(val);
    const int x = 13576;
    std::cout << 13576;
    for (int i = 0; i < 13576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13576(int& val) {
    const int x = 13576;
    std::cout << 13576;
    for (int i = 0; i < 13576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13577(int& val) {
    func_in_13577(val);
    const int x = 13577;
    std::cout << 13577;
    for (int i = 0; i < 13577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13577(int& val) {
    const int x = 13577;
    std::cout << 13577;
    for (int i = 0; i < 13577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13578(int& val) {
    func_in_13578(val);
    const int x = 13578;
    std::cout << 13578;
    for (int i = 0; i < 13578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13578(int& val) {
    const int x = 13578;
    std::cout << 13578;
    for (int i = 0; i < 13578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13579(int& val) {
    func_in_13579(val);
    const int x = 13579;
    std::cout << 13579;
    for (int i = 0; i < 13579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13579(int& val) {
    const int x = 13579;
    std::cout << 13579;
    for (int i = 0; i < 13579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13580(int& val) {
    func_in_13580(val);
    const int x = 13580;
    std::cout << 13580;
    for (int i = 0; i < 13580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13580(int& val) {
    const int x = 13580;
    std::cout << 13580;
    for (int i = 0; i < 13580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13581(int& val) {
    func_in_13581(val);
    const int x = 13581;
    std::cout << 13581;
    for (int i = 0; i < 13581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13581(int& val) {
    const int x = 13581;
    std::cout << 13581;
    for (int i = 0; i < 13581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13582(int& val) {
    func_in_13582(val);
    const int x = 13582;
    std::cout << 13582;
    for (int i = 0; i < 13582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13582(int& val) {
    const int x = 13582;
    std::cout << 13582;
    for (int i = 0; i < 13582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13583(int& val) {
    func_in_13583(val);
    const int x = 13583;
    std::cout << 13583;
    for (int i = 0; i < 13583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13583(int& val) {
    const int x = 13583;
    std::cout << 13583;
    for (int i = 0; i < 13583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13584(int& val) {
    func_in_13584(val);
    const int x = 13584;
    std::cout << 13584;
    for (int i = 0; i < 13584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13584(int& val) {
    const int x = 13584;
    std::cout << 13584;
    for (int i = 0; i < 13584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13585(int& val) {
    func_in_13585(val);
    const int x = 13585;
    std::cout << 13585;
    for (int i = 0; i < 13585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13585(int& val) {
    const int x = 13585;
    std::cout << 13585;
    for (int i = 0; i < 13585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13586(int& val) {
    func_in_13586(val);
    const int x = 13586;
    std::cout << 13586;
    for (int i = 0; i < 13586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13586(int& val) {
    const int x = 13586;
    std::cout << 13586;
    for (int i = 0; i < 13586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13587(int& val) {
    func_in_13587(val);
    const int x = 13587;
    std::cout << 13587;
    for (int i = 0; i < 13587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13587(int& val) {
    const int x = 13587;
    std::cout << 13587;
    for (int i = 0; i < 13587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13588(int& val) {
    func_in_13588(val);
    const int x = 13588;
    std::cout << 13588;
    for (int i = 0; i < 13588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13588(int& val) {
    const int x = 13588;
    std::cout << 13588;
    for (int i = 0; i < 13588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13589(int& val) {
    func_in_13589(val);
    const int x = 13589;
    std::cout << 13589;
    for (int i = 0; i < 13589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13589(int& val) {
    const int x = 13589;
    std::cout << 13589;
    for (int i = 0; i < 13589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13590(int& val) {
    func_in_13590(val);
    const int x = 13590;
    std::cout << 13590;
    for (int i = 0; i < 13590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13590(int& val) {
    const int x = 13590;
    std::cout << 13590;
    for (int i = 0; i < 13590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13591(int& val) {
    func_in_13591(val);
    const int x = 13591;
    std::cout << 13591;
    for (int i = 0; i < 13591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13591(int& val) {
    const int x = 13591;
    std::cout << 13591;
    for (int i = 0; i < 13591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13592(int& val) {
    func_in_13592(val);
    const int x = 13592;
    std::cout << 13592;
    for (int i = 0; i < 13592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13592(int& val) {
    const int x = 13592;
    std::cout << 13592;
    for (int i = 0; i < 13592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13593(int& val) {
    func_in_13593(val);
    const int x = 13593;
    std::cout << 13593;
    for (int i = 0; i < 13593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13593(int& val) {
    const int x = 13593;
    std::cout << 13593;
    for (int i = 0; i < 13593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13594(int& val) {
    func_in_13594(val);
    const int x = 13594;
    std::cout << 13594;
    for (int i = 0; i < 13594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13594(int& val) {
    const int x = 13594;
    std::cout << 13594;
    for (int i = 0; i < 13594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13595(int& val) {
    func_in_13595(val);
    const int x = 13595;
    std::cout << 13595;
    for (int i = 0; i < 13595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13595(int& val) {
    const int x = 13595;
    std::cout << 13595;
    for (int i = 0; i < 13595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13596(int& val) {
    func_in_13596(val);
    const int x = 13596;
    std::cout << 13596;
    for (int i = 0; i < 13596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13596(int& val) {
    const int x = 13596;
    std::cout << 13596;
    for (int i = 0; i < 13596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13597(int& val) {
    func_in_13597(val);
    const int x = 13597;
    std::cout << 13597;
    for (int i = 0; i < 13597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13597(int& val) {
    const int x = 13597;
    std::cout << 13597;
    for (int i = 0; i < 13597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13598(int& val) {
    func_in_13598(val);
    const int x = 13598;
    std::cout << 13598;
    for (int i = 0; i < 13598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13598(int& val) {
    const int x = 13598;
    std::cout << 13598;
    for (int i = 0; i < 13598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13599(int& val) {
    func_in_13599(val);
    const int x = 13599;
    std::cout << 13599;
    for (int i = 0; i < 13599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13599(int& val) {
    const int x = 13599;
    std::cout << 13599;
    for (int i = 0; i < 13599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13600(int& val) {
    func_in_13600(val);
    const int x = 13600;
    std::cout << 13600;
    for (int i = 0; i < 13600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13600(int& val) {
    const int x = 13600;
    std::cout << 13600;
    for (int i = 0; i < 13600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13601(int& val) {
    func_in_13601(val);
    const int x = 13601;
    std::cout << 13601;
    for (int i = 0; i < 13601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13601(int& val) {
    const int x = 13601;
    std::cout << 13601;
    for (int i = 0; i < 13601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13602(int& val) {
    func_in_13602(val);
    const int x = 13602;
    std::cout << 13602;
    for (int i = 0; i < 13602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13602(int& val) {
    const int x = 13602;
    std::cout << 13602;
    for (int i = 0; i < 13602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13603(int& val) {
    func_in_13603(val);
    const int x = 13603;
    std::cout << 13603;
    for (int i = 0; i < 13603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13603(int& val) {
    const int x = 13603;
    std::cout << 13603;
    for (int i = 0; i < 13603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13604(int& val) {
    func_in_13604(val);
    const int x = 13604;
    std::cout << 13604;
    for (int i = 0; i < 13604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13604(int& val) {
    const int x = 13604;
    std::cout << 13604;
    for (int i = 0; i < 13604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13605(int& val) {
    func_in_13605(val);
    const int x = 13605;
    std::cout << 13605;
    for (int i = 0; i < 13605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13605(int& val) {
    const int x = 13605;
    std::cout << 13605;
    for (int i = 0; i < 13605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13606(int& val) {
    func_in_13606(val);
    const int x = 13606;
    std::cout << 13606;
    for (int i = 0; i < 13606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13606(int& val) {
    const int x = 13606;
    std::cout << 13606;
    for (int i = 0; i < 13606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13607(int& val) {
    func_in_13607(val);
    const int x = 13607;
    std::cout << 13607;
    for (int i = 0; i < 13607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13607(int& val) {
    const int x = 13607;
    std::cout << 13607;
    for (int i = 0; i < 13607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13608(int& val) {
    func_in_13608(val);
    const int x = 13608;
    std::cout << 13608;
    for (int i = 0; i < 13608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13608(int& val) {
    const int x = 13608;
    std::cout << 13608;
    for (int i = 0; i < 13608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13609(int& val) {
    func_in_13609(val);
    const int x = 13609;
    std::cout << 13609;
    for (int i = 0; i < 13609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13609(int& val) {
    const int x = 13609;
    std::cout << 13609;
    for (int i = 0; i < 13609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13610(int& val) {
    func_in_13610(val);
    const int x = 13610;
    std::cout << 13610;
    for (int i = 0; i < 13610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13610(int& val) {
    const int x = 13610;
    std::cout << 13610;
    for (int i = 0; i < 13610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13611(int& val) {
    func_in_13611(val);
    const int x = 13611;
    std::cout << 13611;
    for (int i = 0; i < 13611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13611(int& val) {
    const int x = 13611;
    std::cout << 13611;
    for (int i = 0; i < 13611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13612(int& val) {
    func_in_13612(val);
    const int x = 13612;
    std::cout << 13612;
    for (int i = 0; i < 13612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13612(int& val) {
    const int x = 13612;
    std::cout << 13612;
    for (int i = 0; i < 13612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13613(int& val) {
    func_in_13613(val);
    const int x = 13613;
    std::cout << 13613;
    for (int i = 0; i < 13613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13613(int& val) {
    const int x = 13613;
    std::cout << 13613;
    for (int i = 0; i < 13613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13614(int& val) {
    func_in_13614(val);
    const int x = 13614;
    std::cout << 13614;
    for (int i = 0; i < 13614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13614(int& val) {
    const int x = 13614;
    std::cout << 13614;
    for (int i = 0; i < 13614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13615(int& val) {
    func_in_13615(val);
    const int x = 13615;
    std::cout << 13615;
    for (int i = 0; i < 13615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13615(int& val) {
    const int x = 13615;
    std::cout << 13615;
    for (int i = 0; i < 13615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13616(int& val) {
    func_in_13616(val);
    const int x = 13616;
    std::cout << 13616;
    for (int i = 0; i < 13616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13616(int& val) {
    const int x = 13616;
    std::cout << 13616;
    for (int i = 0; i < 13616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13617(int& val) {
    func_in_13617(val);
    const int x = 13617;
    std::cout << 13617;
    for (int i = 0; i < 13617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13617(int& val) {
    const int x = 13617;
    std::cout << 13617;
    for (int i = 0; i < 13617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13618(int& val) {
    func_in_13618(val);
    const int x = 13618;
    std::cout << 13618;
    for (int i = 0; i < 13618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13618(int& val) {
    const int x = 13618;
    std::cout << 13618;
    for (int i = 0; i < 13618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13619(int& val) {
    func_in_13619(val);
    const int x = 13619;
    std::cout << 13619;
    for (int i = 0; i < 13619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13619(int& val) {
    const int x = 13619;
    std::cout << 13619;
    for (int i = 0; i < 13619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13620(int& val) {
    func_in_13620(val);
    const int x = 13620;
    std::cout << 13620;
    for (int i = 0; i < 13620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13620(int& val) {
    const int x = 13620;
    std::cout << 13620;
    for (int i = 0; i < 13620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13621(int& val) {
    func_in_13621(val);
    const int x = 13621;
    std::cout << 13621;
    for (int i = 0; i < 13621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13621(int& val) {
    const int x = 13621;
    std::cout << 13621;
    for (int i = 0; i < 13621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13622(int& val) {
    func_in_13622(val);
    const int x = 13622;
    std::cout << 13622;
    for (int i = 0; i < 13622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13622(int& val) {
    const int x = 13622;
    std::cout << 13622;
    for (int i = 0; i < 13622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13623(int& val) {
    func_in_13623(val);
    const int x = 13623;
    std::cout << 13623;
    for (int i = 0; i < 13623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13623(int& val) {
    const int x = 13623;
    std::cout << 13623;
    for (int i = 0; i < 13623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13624(int& val) {
    func_in_13624(val);
    const int x = 13624;
    std::cout << 13624;
    for (int i = 0; i < 13624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13624(int& val) {
    const int x = 13624;
    std::cout << 13624;
    for (int i = 0; i < 13624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13625(int& val) {
    func_in_13625(val);
    const int x = 13625;
    std::cout << 13625;
    for (int i = 0; i < 13625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13625(int& val) {
    const int x = 13625;
    std::cout << 13625;
    for (int i = 0; i < 13625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13626(int& val) {
    func_in_13626(val);
    const int x = 13626;
    std::cout << 13626;
    for (int i = 0; i < 13626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13626(int& val) {
    const int x = 13626;
    std::cout << 13626;
    for (int i = 0; i < 13626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13627(int& val) {
    func_in_13627(val);
    const int x = 13627;
    std::cout << 13627;
    for (int i = 0; i < 13627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13627(int& val) {
    const int x = 13627;
    std::cout << 13627;
    for (int i = 0; i < 13627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13628(int& val) {
    func_in_13628(val);
    const int x = 13628;
    std::cout << 13628;
    for (int i = 0; i < 13628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13628(int& val) {
    const int x = 13628;
    std::cout << 13628;
    for (int i = 0; i < 13628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13629(int& val) {
    func_in_13629(val);
    const int x = 13629;
    std::cout << 13629;
    for (int i = 0; i < 13629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13629(int& val) {
    const int x = 13629;
    std::cout << 13629;
    for (int i = 0; i < 13629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13630(int& val) {
    func_in_13630(val);
    const int x = 13630;
    std::cout << 13630;
    for (int i = 0; i < 13630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13630(int& val) {
    const int x = 13630;
    std::cout << 13630;
    for (int i = 0; i < 13630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13631(int& val) {
    func_in_13631(val);
    const int x = 13631;
    std::cout << 13631;
    for (int i = 0; i < 13631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13631(int& val) {
    const int x = 13631;
    std::cout << 13631;
    for (int i = 0; i < 13631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13632(int& val) {
    func_in_13632(val);
    const int x = 13632;
    std::cout << 13632;
    for (int i = 0; i < 13632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13632(int& val) {
    const int x = 13632;
    std::cout << 13632;
    for (int i = 0; i < 13632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13633(int& val) {
    func_in_13633(val);
    const int x = 13633;
    std::cout << 13633;
    for (int i = 0; i < 13633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13633(int& val) {
    const int x = 13633;
    std::cout << 13633;
    for (int i = 0; i < 13633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13634(int& val) {
    func_in_13634(val);
    const int x = 13634;
    std::cout << 13634;
    for (int i = 0; i < 13634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13634(int& val) {
    const int x = 13634;
    std::cout << 13634;
    for (int i = 0; i < 13634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13635(int& val) {
    func_in_13635(val);
    const int x = 13635;
    std::cout << 13635;
    for (int i = 0; i < 13635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13635(int& val) {
    const int x = 13635;
    std::cout << 13635;
    for (int i = 0; i < 13635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13636(int& val) {
    func_in_13636(val);
    const int x = 13636;
    std::cout << 13636;
    for (int i = 0; i < 13636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13636(int& val) {
    const int x = 13636;
    std::cout << 13636;
    for (int i = 0; i < 13636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13637(int& val) {
    func_in_13637(val);
    const int x = 13637;
    std::cout << 13637;
    for (int i = 0; i < 13637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13637(int& val) {
    const int x = 13637;
    std::cout << 13637;
    for (int i = 0; i < 13637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13638(int& val) {
    func_in_13638(val);
    const int x = 13638;
    std::cout << 13638;
    for (int i = 0; i < 13638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13638(int& val) {
    const int x = 13638;
    std::cout << 13638;
    for (int i = 0; i < 13638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13639(int& val) {
    func_in_13639(val);
    const int x = 13639;
    std::cout << 13639;
    for (int i = 0; i < 13639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13639(int& val) {
    const int x = 13639;
    std::cout << 13639;
    for (int i = 0; i < 13639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13640(int& val) {
    func_in_13640(val);
    const int x = 13640;
    std::cout << 13640;
    for (int i = 0; i < 13640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13640(int& val) {
    const int x = 13640;
    std::cout << 13640;
    for (int i = 0; i < 13640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13641(int& val) {
    func_in_13641(val);
    const int x = 13641;
    std::cout << 13641;
    for (int i = 0; i < 13641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13641(int& val) {
    const int x = 13641;
    std::cout << 13641;
    for (int i = 0; i < 13641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13642(int& val) {
    func_in_13642(val);
    const int x = 13642;
    std::cout << 13642;
    for (int i = 0; i < 13642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13642(int& val) {
    const int x = 13642;
    std::cout << 13642;
    for (int i = 0; i < 13642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13643(int& val) {
    func_in_13643(val);
    const int x = 13643;
    std::cout << 13643;
    for (int i = 0; i < 13643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13643(int& val) {
    const int x = 13643;
    std::cout << 13643;
    for (int i = 0; i < 13643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13644(int& val) {
    func_in_13644(val);
    const int x = 13644;
    std::cout << 13644;
    for (int i = 0; i < 13644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13644(int& val) {
    const int x = 13644;
    std::cout << 13644;
    for (int i = 0; i < 13644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13645(int& val) {
    func_in_13645(val);
    const int x = 13645;
    std::cout << 13645;
    for (int i = 0; i < 13645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13645(int& val) {
    const int x = 13645;
    std::cout << 13645;
    for (int i = 0; i < 13645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13646(int& val) {
    func_in_13646(val);
    const int x = 13646;
    std::cout << 13646;
    for (int i = 0; i < 13646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13646(int& val) {
    const int x = 13646;
    std::cout << 13646;
    for (int i = 0; i < 13646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13647(int& val) {
    func_in_13647(val);
    const int x = 13647;
    std::cout << 13647;
    for (int i = 0; i < 13647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13647(int& val) {
    const int x = 13647;
    std::cout << 13647;
    for (int i = 0; i < 13647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13648(int& val) {
    func_in_13648(val);
    const int x = 13648;
    std::cout << 13648;
    for (int i = 0; i < 13648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13648(int& val) {
    const int x = 13648;
    std::cout << 13648;
    for (int i = 0; i < 13648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13649(int& val) {
    func_in_13649(val);
    const int x = 13649;
    std::cout << 13649;
    for (int i = 0; i < 13649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13649(int& val) {
    const int x = 13649;
    std::cout << 13649;
    for (int i = 0; i < 13649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13650(int& val) {
    func_in_13650(val);
    const int x = 13650;
    std::cout << 13650;
    for (int i = 0; i < 13650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13650(int& val) {
    const int x = 13650;
    std::cout << 13650;
    for (int i = 0; i < 13650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13651(int& val) {
    func_in_13651(val);
    const int x = 13651;
    std::cout << 13651;
    for (int i = 0; i < 13651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13651(int& val) {
    const int x = 13651;
    std::cout << 13651;
    for (int i = 0; i < 13651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13652(int& val) {
    func_in_13652(val);
    const int x = 13652;
    std::cout << 13652;
    for (int i = 0; i < 13652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13652(int& val) {
    const int x = 13652;
    std::cout << 13652;
    for (int i = 0; i < 13652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13653(int& val) {
    func_in_13653(val);
    const int x = 13653;
    std::cout << 13653;
    for (int i = 0; i < 13653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13653(int& val) {
    const int x = 13653;
    std::cout << 13653;
    for (int i = 0; i < 13653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13654(int& val) {
    func_in_13654(val);
    const int x = 13654;
    std::cout << 13654;
    for (int i = 0; i < 13654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13654(int& val) {
    const int x = 13654;
    std::cout << 13654;
    for (int i = 0; i < 13654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13655(int& val) {
    func_in_13655(val);
    const int x = 13655;
    std::cout << 13655;
    for (int i = 0; i < 13655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13655(int& val) {
    const int x = 13655;
    std::cout << 13655;
    for (int i = 0; i < 13655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13656(int& val) {
    func_in_13656(val);
    const int x = 13656;
    std::cout << 13656;
    for (int i = 0; i < 13656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13656(int& val) {
    const int x = 13656;
    std::cout << 13656;
    for (int i = 0; i < 13656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13657(int& val) {
    func_in_13657(val);
    const int x = 13657;
    std::cout << 13657;
    for (int i = 0; i < 13657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13657(int& val) {
    const int x = 13657;
    std::cout << 13657;
    for (int i = 0; i < 13657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13658(int& val) {
    func_in_13658(val);
    const int x = 13658;
    std::cout << 13658;
    for (int i = 0; i < 13658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13658(int& val) {
    const int x = 13658;
    std::cout << 13658;
    for (int i = 0; i < 13658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13659(int& val) {
    func_in_13659(val);
    const int x = 13659;
    std::cout << 13659;
    for (int i = 0; i < 13659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13659(int& val) {
    const int x = 13659;
    std::cout << 13659;
    for (int i = 0; i < 13659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13660(int& val) {
    func_in_13660(val);
    const int x = 13660;
    std::cout << 13660;
    for (int i = 0; i < 13660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13660(int& val) {
    const int x = 13660;
    std::cout << 13660;
    for (int i = 0; i < 13660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13661(int& val) {
    func_in_13661(val);
    const int x = 13661;
    std::cout << 13661;
    for (int i = 0; i < 13661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13661(int& val) {
    const int x = 13661;
    std::cout << 13661;
    for (int i = 0; i < 13661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13662(int& val) {
    func_in_13662(val);
    const int x = 13662;
    std::cout << 13662;
    for (int i = 0; i < 13662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13662(int& val) {
    const int x = 13662;
    std::cout << 13662;
    for (int i = 0; i < 13662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13663(int& val) {
    func_in_13663(val);
    const int x = 13663;
    std::cout << 13663;
    for (int i = 0; i < 13663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13663(int& val) {
    const int x = 13663;
    std::cout << 13663;
    for (int i = 0; i < 13663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13664(int& val) {
    func_in_13664(val);
    const int x = 13664;
    std::cout << 13664;
    for (int i = 0; i < 13664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13664(int& val) {
    const int x = 13664;
    std::cout << 13664;
    for (int i = 0; i < 13664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13665(int& val) {
    func_in_13665(val);
    const int x = 13665;
    std::cout << 13665;
    for (int i = 0; i < 13665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13665(int& val) {
    const int x = 13665;
    std::cout << 13665;
    for (int i = 0; i < 13665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13666(int& val) {
    func_in_13666(val);
    const int x = 13666;
    std::cout << 13666;
    for (int i = 0; i < 13666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13666(int& val) {
    const int x = 13666;
    std::cout << 13666;
    for (int i = 0; i < 13666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13667(int& val) {
    func_in_13667(val);
    const int x = 13667;
    std::cout << 13667;
    for (int i = 0; i < 13667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13667(int& val) {
    const int x = 13667;
    std::cout << 13667;
    for (int i = 0; i < 13667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13668(int& val) {
    func_in_13668(val);
    const int x = 13668;
    std::cout << 13668;
    for (int i = 0; i < 13668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13668(int& val) {
    const int x = 13668;
    std::cout << 13668;
    for (int i = 0; i < 13668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13669(int& val) {
    func_in_13669(val);
    const int x = 13669;
    std::cout << 13669;
    for (int i = 0; i < 13669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13669(int& val) {
    const int x = 13669;
    std::cout << 13669;
    for (int i = 0; i < 13669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13670(int& val) {
    func_in_13670(val);
    const int x = 13670;
    std::cout << 13670;
    for (int i = 0; i < 13670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13670(int& val) {
    const int x = 13670;
    std::cout << 13670;
    for (int i = 0; i < 13670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13671(int& val) {
    func_in_13671(val);
    const int x = 13671;
    std::cout << 13671;
    for (int i = 0; i < 13671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13671(int& val) {
    const int x = 13671;
    std::cout << 13671;
    for (int i = 0; i < 13671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13672(int& val) {
    func_in_13672(val);
    const int x = 13672;
    std::cout << 13672;
    for (int i = 0; i < 13672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13672(int& val) {
    const int x = 13672;
    std::cout << 13672;
    for (int i = 0; i < 13672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13673(int& val) {
    func_in_13673(val);
    const int x = 13673;
    std::cout << 13673;
    for (int i = 0; i < 13673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13673(int& val) {
    const int x = 13673;
    std::cout << 13673;
    for (int i = 0; i < 13673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13674(int& val) {
    func_in_13674(val);
    const int x = 13674;
    std::cout << 13674;
    for (int i = 0; i < 13674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13674(int& val) {
    const int x = 13674;
    std::cout << 13674;
    for (int i = 0; i < 13674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13675(int& val) {
    func_in_13675(val);
    const int x = 13675;
    std::cout << 13675;
    for (int i = 0; i < 13675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13675(int& val) {
    const int x = 13675;
    std::cout << 13675;
    for (int i = 0; i < 13675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13676(int& val) {
    func_in_13676(val);
    const int x = 13676;
    std::cout << 13676;
    for (int i = 0; i < 13676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13676(int& val) {
    const int x = 13676;
    std::cout << 13676;
    for (int i = 0; i < 13676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13677(int& val) {
    func_in_13677(val);
    const int x = 13677;
    std::cout << 13677;
    for (int i = 0; i < 13677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13677(int& val) {
    const int x = 13677;
    std::cout << 13677;
    for (int i = 0; i < 13677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13678(int& val) {
    func_in_13678(val);
    const int x = 13678;
    std::cout << 13678;
    for (int i = 0; i < 13678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13678(int& val) {
    const int x = 13678;
    std::cout << 13678;
    for (int i = 0; i < 13678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13679(int& val) {
    func_in_13679(val);
    const int x = 13679;
    std::cout << 13679;
    for (int i = 0; i < 13679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13679(int& val) {
    const int x = 13679;
    std::cout << 13679;
    for (int i = 0; i < 13679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13680(int& val) {
    func_in_13680(val);
    const int x = 13680;
    std::cout << 13680;
    for (int i = 0; i < 13680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13680(int& val) {
    const int x = 13680;
    std::cout << 13680;
    for (int i = 0; i < 13680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13681(int& val) {
    func_in_13681(val);
    const int x = 13681;
    std::cout << 13681;
    for (int i = 0; i < 13681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13681(int& val) {
    const int x = 13681;
    std::cout << 13681;
    for (int i = 0; i < 13681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13682(int& val) {
    func_in_13682(val);
    const int x = 13682;
    std::cout << 13682;
    for (int i = 0; i < 13682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13682(int& val) {
    const int x = 13682;
    std::cout << 13682;
    for (int i = 0; i < 13682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13683(int& val) {
    func_in_13683(val);
    const int x = 13683;
    std::cout << 13683;
    for (int i = 0; i < 13683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13683(int& val) {
    const int x = 13683;
    std::cout << 13683;
    for (int i = 0; i < 13683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13684(int& val) {
    func_in_13684(val);
    const int x = 13684;
    std::cout << 13684;
    for (int i = 0; i < 13684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13684(int& val) {
    const int x = 13684;
    std::cout << 13684;
    for (int i = 0; i < 13684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13685(int& val) {
    func_in_13685(val);
    const int x = 13685;
    std::cout << 13685;
    for (int i = 0; i < 13685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13685(int& val) {
    const int x = 13685;
    std::cout << 13685;
    for (int i = 0; i < 13685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13686(int& val) {
    func_in_13686(val);
    const int x = 13686;
    std::cout << 13686;
    for (int i = 0; i < 13686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13686(int& val) {
    const int x = 13686;
    std::cout << 13686;
    for (int i = 0; i < 13686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13687(int& val) {
    func_in_13687(val);
    const int x = 13687;
    std::cout << 13687;
    for (int i = 0; i < 13687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13687(int& val) {
    const int x = 13687;
    std::cout << 13687;
    for (int i = 0; i < 13687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13688(int& val) {
    func_in_13688(val);
    const int x = 13688;
    std::cout << 13688;
    for (int i = 0; i < 13688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13688(int& val) {
    const int x = 13688;
    std::cout << 13688;
    for (int i = 0; i < 13688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13689(int& val) {
    func_in_13689(val);
    const int x = 13689;
    std::cout << 13689;
    for (int i = 0; i < 13689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13689(int& val) {
    const int x = 13689;
    std::cout << 13689;
    for (int i = 0; i < 13689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13690(int& val) {
    func_in_13690(val);
    const int x = 13690;
    std::cout << 13690;
    for (int i = 0; i < 13690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13690(int& val) {
    const int x = 13690;
    std::cout << 13690;
    for (int i = 0; i < 13690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13691(int& val) {
    func_in_13691(val);
    const int x = 13691;
    std::cout << 13691;
    for (int i = 0; i < 13691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13691(int& val) {
    const int x = 13691;
    std::cout << 13691;
    for (int i = 0; i < 13691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13692(int& val) {
    func_in_13692(val);
    const int x = 13692;
    std::cout << 13692;
    for (int i = 0; i < 13692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13692(int& val) {
    const int x = 13692;
    std::cout << 13692;
    for (int i = 0; i < 13692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13693(int& val) {
    func_in_13693(val);
    const int x = 13693;
    std::cout << 13693;
    for (int i = 0; i < 13693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13693(int& val) {
    const int x = 13693;
    std::cout << 13693;
    for (int i = 0; i < 13693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13694(int& val) {
    func_in_13694(val);
    const int x = 13694;
    std::cout << 13694;
    for (int i = 0; i < 13694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13694(int& val) {
    const int x = 13694;
    std::cout << 13694;
    for (int i = 0; i < 13694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13695(int& val) {
    func_in_13695(val);
    const int x = 13695;
    std::cout << 13695;
    for (int i = 0; i < 13695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13695(int& val) {
    const int x = 13695;
    std::cout << 13695;
    for (int i = 0; i < 13695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13696(int& val) {
    func_in_13696(val);
    const int x = 13696;
    std::cout << 13696;
    for (int i = 0; i < 13696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13696(int& val) {
    const int x = 13696;
    std::cout << 13696;
    for (int i = 0; i < 13696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13697(int& val) {
    func_in_13697(val);
    const int x = 13697;
    std::cout << 13697;
    for (int i = 0; i < 13697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13697(int& val) {
    const int x = 13697;
    std::cout << 13697;
    for (int i = 0; i < 13697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13698(int& val) {
    func_in_13698(val);
    const int x = 13698;
    std::cout << 13698;
    for (int i = 0; i < 13698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13698(int& val) {
    const int x = 13698;
    std::cout << 13698;
    for (int i = 0; i < 13698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13699(int& val) {
    func_in_13699(val);
    const int x = 13699;
    std::cout << 13699;
    for (int i = 0; i < 13699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13699(int& val) {
    const int x = 13699;
    std::cout << 13699;
    for (int i = 0; i < 13699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13700(int& val) {
    func_in_13700(val);
    const int x = 13700;
    std::cout << 13700;
    for (int i = 0; i < 13700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13700(int& val) {
    const int x = 13700;
    std::cout << 13700;
    for (int i = 0; i < 13700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13701(int& val) {
    func_in_13701(val);
    const int x = 13701;
    std::cout << 13701;
    for (int i = 0; i < 13701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13701(int& val) {
    const int x = 13701;
    std::cout << 13701;
    for (int i = 0; i < 13701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13702(int& val) {
    func_in_13702(val);
    const int x = 13702;
    std::cout << 13702;
    for (int i = 0; i < 13702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13702(int& val) {
    const int x = 13702;
    std::cout << 13702;
    for (int i = 0; i < 13702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13703(int& val) {
    func_in_13703(val);
    const int x = 13703;
    std::cout << 13703;
    for (int i = 0; i < 13703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13703(int& val) {
    const int x = 13703;
    std::cout << 13703;
    for (int i = 0; i < 13703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13704(int& val) {
    func_in_13704(val);
    const int x = 13704;
    std::cout << 13704;
    for (int i = 0; i < 13704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13704(int& val) {
    const int x = 13704;
    std::cout << 13704;
    for (int i = 0; i < 13704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13705(int& val) {
    func_in_13705(val);
    const int x = 13705;
    std::cout << 13705;
    for (int i = 0; i < 13705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13705(int& val) {
    const int x = 13705;
    std::cout << 13705;
    for (int i = 0; i < 13705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13706(int& val) {
    func_in_13706(val);
    const int x = 13706;
    std::cout << 13706;
    for (int i = 0; i < 13706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13706(int& val) {
    const int x = 13706;
    std::cout << 13706;
    for (int i = 0; i < 13706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13707(int& val) {
    func_in_13707(val);
    const int x = 13707;
    std::cout << 13707;
    for (int i = 0; i < 13707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13707(int& val) {
    const int x = 13707;
    std::cout << 13707;
    for (int i = 0; i < 13707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13708(int& val) {
    func_in_13708(val);
    const int x = 13708;
    std::cout << 13708;
    for (int i = 0; i < 13708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13708(int& val) {
    const int x = 13708;
    std::cout << 13708;
    for (int i = 0; i < 13708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13709(int& val) {
    func_in_13709(val);
    const int x = 13709;
    std::cout << 13709;
    for (int i = 0; i < 13709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13709(int& val) {
    const int x = 13709;
    std::cout << 13709;
    for (int i = 0; i < 13709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13710(int& val) {
    func_in_13710(val);
    const int x = 13710;
    std::cout << 13710;
    for (int i = 0; i < 13710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13710(int& val) {
    const int x = 13710;
    std::cout << 13710;
    for (int i = 0; i < 13710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13711(int& val) {
    func_in_13711(val);
    const int x = 13711;
    std::cout << 13711;
    for (int i = 0; i < 13711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13711(int& val) {
    const int x = 13711;
    std::cout << 13711;
    for (int i = 0; i < 13711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13712(int& val) {
    func_in_13712(val);
    const int x = 13712;
    std::cout << 13712;
    for (int i = 0; i < 13712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13712(int& val) {
    const int x = 13712;
    std::cout << 13712;
    for (int i = 0; i < 13712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13713(int& val) {
    func_in_13713(val);
    const int x = 13713;
    std::cout << 13713;
    for (int i = 0; i < 13713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13713(int& val) {
    const int x = 13713;
    std::cout << 13713;
    for (int i = 0; i < 13713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13714(int& val) {
    func_in_13714(val);
    const int x = 13714;
    std::cout << 13714;
    for (int i = 0; i < 13714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13714(int& val) {
    const int x = 13714;
    std::cout << 13714;
    for (int i = 0; i < 13714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13715(int& val) {
    func_in_13715(val);
    const int x = 13715;
    std::cout << 13715;
    for (int i = 0; i < 13715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13715(int& val) {
    const int x = 13715;
    std::cout << 13715;
    for (int i = 0; i < 13715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13716(int& val) {
    func_in_13716(val);
    const int x = 13716;
    std::cout << 13716;
    for (int i = 0; i < 13716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13716(int& val) {
    const int x = 13716;
    std::cout << 13716;
    for (int i = 0; i < 13716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13717(int& val) {
    func_in_13717(val);
    const int x = 13717;
    std::cout << 13717;
    for (int i = 0; i < 13717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13717(int& val) {
    const int x = 13717;
    std::cout << 13717;
    for (int i = 0; i < 13717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13718(int& val) {
    func_in_13718(val);
    const int x = 13718;
    std::cout << 13718;
    for (int i = 0; i < 13718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13718(int& val) {
    const int x = 13718;
    std::cout << 13718;
    for (int i = 0; i < 13718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13719(int& val) {
    func_in_13719(val);
    const int x = 13719;
    std::cout << 13719;
    for (int i = 0; i < 13719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13719(int& val) {
    const int x = 13719;
    std::cout << 13719;
    for (int i = 0; i < 13719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13720(int& val) {
    func_in_13720(val);
    const int x = 13720;
    std::cout << 13720;
    for (int i = 0; i < 13720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13720(int& val) {
    const int x = 13720;
    std::cout << 13720;
    for (int i = 0; i < 13720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13721(int& val) {
    func_in_13721(val);
    const int x = 13721;
    std::cout << 13721;
    for (int i = 0; i < 13721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13721(int& val) {
    const int x = 13721;
    std::cout << 13721;
    for (int i = 0; i < 13721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13722(int& val) {
    func_in_13722(val);
    const int x = 13722;
    std::cout << 13722;
    for (int i = 0; i < 13722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13722(int& val) {
    const int x = 13722;
    std::cout << 13722;
    for (int i = 0; i < 13722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13723(int& val) {
    func_in_13723(val);
    const int x = 13723;
    std::cout << 13723;
    for (int i = 0; i < 13723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13723(int& val) {
    const int x = 13723;
    std::cout << 13723;
    for (int i = 0; i < 13723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13724(int& val) {
    func_in_13724(val);
    const int x = 13724;
    std::cout << 13724;
    for (int i = 0; i < 13724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13724(int& val) {
    const int x = 13724;
    std::cout << 13724;
    for (int i = 0; i < 13724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13725(int& val) {
    func_in_13725(val);
    const int x = 13725;
    std::cout << 13725;
    for (int i = 0; i < 13725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13725(int& val) {
    const int x = 13725;
    std::cout << 13725;
    for (int i = 0; i < 13725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13726(int& val) {
    func_in_13726(val);
    const int x = 13726;
    std::cout << 13726;
    for (int i = 0; i < 13726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13726(int& val) {
    const int x = 13726;
    std::cout << 13726;
    for (int i = 0; i < 13726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13727(int& val) {
    func_in_13727(val);
    const int x = 13727;
    std::cout << 13727;
    for (int i = 0; i < 13727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13727(int& val) {
    const int x = 13727;
    std::cout << 13727;
    for (int i = 0; i < 13727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13728(int& val) {
    func_in_13728(val);
    const int x = 13728;
    std::cout << 13728;
    for (int i = 0; i < 13728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13728(int& val) {
    const int x = 13728;
    std::cout << 13728;
    for (int i = 0; i < 13728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13729(int& val) {
    func_in_13729(val);
    const int x = 13729;
    std::cout << 13729;
    for (int i = 0; i < 13729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13729(int& val) {
    const int x = 13729;
    std::cout << 13729;
    for (int i = 0; i < 13729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13730(int& val) {
    func_in_13730(val);
    const int x = 13730;
    std::cout << 13730;
    for (int i = 0; i < 13730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13730(int& val) {
    const int x = 13730;
    std::cout << 13730;
    for (int i = 0; i < 13730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13731(int& val) {
    func_in_13731(val);
    const int x = 13731;
    std::cout << 13731;
    for (int i = 0; i < 13731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13731(int& val) {
    const int x = 13731;
    std::cout << 13731;
    for (int i = 0; i < 13731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13732(int& val) {
    func_in_13732(val);
    const int x = 13732;
    std::cout << 13732;
    for (int i = 0; i < 13732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13732(int& val) {
    const int x = 13732;
    std::cout << 13732;
    for (int i = 0; i < 13732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13733(int& val) {
    func_in_13733(val);
    const int x = 13733;
    std::cout << 13733;
    for (int i = 0; i < 13733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13733(int& val) {
    const int x = 13733;
    std::cout << 13733;
    for (int i = 0; i < 13733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13734(int& val) {
    func_in_13734(val);
    const int x = 13734;
    std::cout << 13734;
    for (int i = 0; i < 13734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13734(int& val) {
    const int x = 13734;
    std::cout << 13734;
    for (int i = 0; i < 13734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13735(int& val) {
    func_in_13735(val);
    const int x = 13735;
    std::cout << 13735;
    for (int i = 0; i < 13735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13735(int& val) {
    const int x = 13735;
    std::cout << 13735;
    for (int i = 0; i < 13735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13736(int& val) {
    func_in_13736(val);
    const int x = 13736;
    std::cout << 13736;
    for (int i = 0; i < 13736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13736(int& val) {
    const int x = 13736;
    std::cout << 13736;
    for (int i = 0; i < 13736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13737(int& val) {
    func_in_13737(val);
    const int x = 13737;
    std::cout << 13737;
    for (int i = 0; i < 13737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13737(int& val) {
    const int x = 13737;
    std::cout << 13737;
    for (int i = 0; i < 13737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13738(int& val) {
    func_in_13738(val);
    const int x = 13738;
    std::cout << 13738;
    for (int i = 0; i < 13738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13738(int& val) {
    const int x = 13738;
    std::cout << 13738;
    for (int i = 0; i < 13738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13739(int& val) {
    func_in_13739(val);
    const int x = 13739;
    std::cout << 13739;
    for (int i = 0; i < 13739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13739(int& val) {
    const int x = 13739;
    std::cout << 13739;
    for (int i = 0; i < 13739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13740(int& val) {
    func_in_13740(val);
    const int x = 13740;
    std::cout << 13740;
    for (int i = 0; i < 13740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13740(int& val) {
    const int x = 13740;
    std::cout << 13740;
    for (int i = 0; i < 13740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13741(int& val) {
    func_in_13741(val);
    const int x = 13741;
    std::cout << 13741;
    for (int i = 0; i < 13741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13741(int& val) {
    const int x = 13741;
    std::cout << 13741;
    for (int i = 0; i < 13741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13742(int& val) {
    func_in_13742(val);
    const int x = 13742;
    std::cout << 13742;
    for (int i = 0; i < 13742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13742(int& val) {
    const int x = 13742;
    std::cout << 13742;
    for (int i = 0; i < 13742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13743(int& val) {
    func_in_13743(val);
    const int x = 13743;
    std::cout << 13743;
    for (int i = 0; i < 13743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13743(int& val) {
    const int x = 13743;
    std::cout << 13743;
    for (int i = 0; i < 13743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13744(int& val) {
    func_in_13744(val);
    const int x = 13744;
    std::cout << 13744;
    for (int i = 0; i < 13744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13744(int& val) {
    const int x = 13744;
    std::cout << 13744;
    for (int i = 0; i < 13744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13745(int& val) {
    func_in_13745(val);
    const int x = 13745;
    std::cout << 13745;
    for (int i = 0; i < 13745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13745(int& val) {
    const int x = 13745;
    std::cout << 13745;
    for (int i = 0; i < 13745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13746(int& val) {
    func_in_13746(val);
    const int x = 13746;
    std::cout << 13746;
    for (int i = 0; i < 13746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13746(int& val) {
    const int x = 13746;
    std::cout << 13746;
    for (int i = 0; i < 13746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13747(int& val) {
    func_in_13747(val);
    const int x = 13747;
    std::cout << 13747;
    for (int i = 0; i < 13747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13747(int& val) {
    const int x = 13747;
    std::cout << 13747;
    for (int i = 0; i < 13747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13748(int& val) {
    func_in_13748(val);
    const int x = 13748;
    std::cout << 13748;
    for (int i = 0; i < 13748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13748(int& val) {
    const int x = 13748;
    std::cout << 13748;
    for (int i = 0; i < 13748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13749(int& val) {
    func_in_13749(val);
    const int x = 13749;
    std::cout << 13749;
    for (int i = 0; i < 13749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13749(int& val) {
    const int x = 13749;
    std::cout << 13749;
    for (int i = 0; i < 13749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13750(int& val) {
    func_in_13750(val);
    const int x = 13750;
    std::cout << 13750;
    for (int i = 0; i < 13750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13750(int& val) {
    const int x = 13750;
    std::cout << 13750;
    for (int i = 0; i < 13750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13751(int& val) {
    func_in_13751(val);
    const int x = 13751;
    std::cout << 13751;
    for (int i = 0; i < 13751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13751(int& val) {
    const int x = 13751;
    std::cout << 13751;
    for (int i = 0; i < 13751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13752(int& val) {
    func_in_13752(val);
    const int x = 13752;
    std::cout << 13752;
    for (int i = 0; i < 13752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13752(int& val) {
    const int x = 13752;
    std::cout << 13752;
    for (int i = 0; i < 13752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13753(int& val) {
    func_in_13753(val);
    const int x = 13753;
    std::cout << 13753;
    for (int i = 0; i < 13753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13753(int& val) {
    const int x = 13753;
    std::cout << 13753;
    for (int i = 0; i < 13753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13754(int& val) {
    func_in_13754(val);
    const int x = 13754;
    std::cout << 13754;
    for (int i = 0; i < 13754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13754(int& val) {
    const int x = 13754;
    std::cout << 13754;
    for (int i = 0; i < 13754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13755(int& val) {
    func_in_13755(val);
    const int x = 13755;
    std::cout << 13755;
    for (int i = 0; i < 13755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13755(int& val) {
    const int x = 13755;
    std::cout << 13755;
    for (int i = 0; i < 13755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13756(int& val) {
    func_in_13756(val);
    const int x = 13756;
    std::cout << 13756;
    for (int i = 0; i < 13756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13756(int& val) {
    const int x = 13756;
    std::cout << 13756;
    for (int i = 0; i < 13756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13757(int& val) {
    func_in_13757(val);
    const int x = 13757;
    std::cout << 13757;
    for (int i = 0; i < 13757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13757(int& val) {
    const int x = 13757;
    std::cout << 13757;
    for (int i = 0; i < 13757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13758(int& val) {
    func_in_13758(val);
    const int x = 13758;
    std::cout << 13758;
    for (int i = 0; i < 13758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13758(int& val) {
    const int x = 13758;
    std::cout << 13758;
    for (int i = 0; i < 13758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13759(int& val) {
    func_in_13759(val);
    const int x = 13759;
    std::cout << 13759;
    for (int i = 0; i < 13759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13759(int& val) {
    const int x = 13759;
    std::cout << 13759;
    for (int i = 0; i < 13759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13760(int& val) {
    func_in_13760(val);
    const int x = 13760;
    std::cout << 13760;
    for (int i = 0; i < 13760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13760(int& val) {
    const int x = 13760;
    std::cout << 13760;
    for (int i = 0; i < 13760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13761(int& val) {
    func_in_13761(val);
    const int x = 13761;
    std::cout << 13761;
    for (int i = 0; i < 13761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13761(int& val) {
    const int x = 13761;
    std::cout << 13761;
    for (int i = 0; i < 13761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13762(int& val) {
    func_in_13762(val);
    const int x = 13762;
    std::cout << 13762;
    for (int i = 0; i < 13762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13762(int& val) {
    const int x = 13762;
    std::cout << 13762;
    for (int i = 0; i < 13762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13763(int& val) {
    func_in_13763(val);
    const int x = 13763;
    std::cout << 13763;
    for (int i = 0; i < 13763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13763(int& val) {
    const int x = 13763;
    std::cout << 13763;
    for (int i = 0; i < 13763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13764(int& val) {
    func_in_13764(val);
    const int x = 13764;
    std::cout << 13764;
    for (int i = 0; i < 13764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13764(int& val) {
    const int x = 13764;
    std::cout << 13764;
    for (int i = 0; i < 13764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13765(int& val) {
    func_in_13765(val);
    const int x = 13765;
    std::cout << 13765;
    for (int i = 0; i < 13765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13765(int& val) {
    const int x = 13765;
    std::cout << 13765;
    for (int i = 0; i < 13765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13766(int& val) {
    func_in_13766(val);
    const int x = 13766;
    std::cout << 13766;
    for (int i = 0; i < 13766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13766(int& val) {
    const int x = 13766;
    std::cout << 13766;
    for (int i = 0; i < 13766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13767(int& val) {
    func_in_13767(val);
    const int x = 13767;
    std::cout << 13767;
    for (int i = 0; i < 13767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13767(int& val) {
    const int x = 13767;
    std::cout << 13767;
    for (int i = 0; i < 13767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13768(int& val) {
    func_in_13768(val);
    const int x = 13768;
    std::cout << 13768;
    for (int i = 0; i < 13768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13768(int& val) {
    const int x = 13768;
    std::cout << 13768;
    for (int i = 0; i < 13768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13769(int& val) {
    func_in_13769(val);
    const int x = 13769;
    std::cout << 13769;
    for (int i = 0; i < 13769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13769(int& val) {
    const int x = 13769;
    std::cout << 13769;
    for (int i = 0; i < 13769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13770(int& val) {
    func_in_13770(val);
    const int x = 13770;
    std::cout << 13770;
    for (int i = 0; i < 13770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13770(int& val) {
    const int x = 13770;
    std::cout << 13770;
    for (int i = 0; i < 13770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13771(int& val) {
    func_in_13771(val);
    const int x = 13771;
    std::cout << 13771;
    for (int i = 0; i < 13771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13771(int& val) {
    const int x = 13771;
    std::cout << 13771;
    for (int i = 0; i < 13771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13772(int& val) {
    func_in_13772(val);
    const int x = 13772;
    std::cout << 13772;
    for (int i = 0; i < 13772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13772(int& val) {
    const int x = 13772;
    std::cout << 13772;
    for (int i = 0; i < 13772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13773(int& val) {
    func_in_13773(val);
    const int x = 13773;
    std::cout << 13773;
    for (int i = 0; i < 13773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13773(int& val) {
    const int x = 13773;
    std::cout << 13773;
    for (int i = 0; i < 13773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13774(int& val) {
    func_in_13774(val);
    const int x = 13774;
    std::cout << 13774;
    for (int i = 0; i < 13774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13774(int& val) {
    const int x = 13774;
    std::cout << 13774;
    for (int i = 0; i < 13774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13775(int& val) {
    func_in_13775(val);
    const int x = 13775;
    std::cout << 13775;
    for (int i = 0; i < 13775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13775(int& val) {
    const int x = 13775;
    std::cout << 13775;
    for (int i = 0; i < 13775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13776(int& val) {
    func_in_13776(val);
    const int x = 13776;
    std::cout << 13776;
    for (int i = 0; i < 13776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13776(int& val) {
    const int x = 13776;
    std::cout << 13776;
    for (int i = 0; i < 13776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13777(int& val) {
    func_in_13777(val);
    const int x = 13777;
    std::cout << 13777;
    for (int i = 0; i < 13777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13777(int& val) {
    const int x = 13777;
    std::cout << 13777;
    for (int i = 0; i < 13777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13778(int& val) {
    func_in_13778(val);
    const int x = 13778;
    std::cout << 13778;
    for (int i = 0; i < 13778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13778(int& val) {
    const int x = 13778;
    std::cout << 13778;
    for (int i = 0; i < 13778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13779(int& val) {
    func_in_13779(val);
    const int x = 13779;
    std::cout << 13779;
    for (int i = 0; i < 13779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13779(int& val) {
    const int x = 13779;
    std::cout << 13779;
    for (int i = 0; i < 13779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13780(int& val) {
    func_in_13780(val);
    const int x = 13780;
    std::cout << 13780;
    for (int i = 0; i < 13780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13780(int& val) {
    const int x = 13780;
    std::cout << 13780;
    for (int i = 0; i < 13780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13781(int& val) {
    func_in_13781(val);
    const int x = 13781;
    std::cout << 13781;
    for (int i = 0; i < 13781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13781(int& val) {
    const int x = 13781;
    std::cout << 13781;
    for (int i = 0; i < 13781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13782(int& val) {
    func_in_13782(val);
    const int x = 13782;
    std::cout << 13782;
    for (int i = 0; i < 13782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13782(int& val) {
    const int x = 13782;
    std::cout << 13782;
    for (int i = 0; i < 13782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13783(int& val) {
    func_in_13783(val);
    const int x = 13783;
    std::cout << 13783;
    for (int i = 0; i < 13783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13783(int& val) {
    const int x = 13783;
    std::cout << 13783;
    for (int i = 0; i < 13783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13784(int& val) {
    func_in_13784(val);
    const int x = 13784;
    std::cout << 13784;
    for (int i = 0; i < 13784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13784(int& val) {
    const int x = 13784;
    std::cout << 13784;
    for (int i = 0; i < 13784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13785(int& val) {
    func_in_13785(val);
    const int x = 13785;
    std::cout << 13785;
    for (int i = 0; i < 13785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13785(int& val) {
    const int x = 13785;
    std::cout << 13785;
    for (int i = 0; i < 13785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13786(int& val) {
    func_in_13786(val);
    const int x = 13786;
    std::cout << 13786;
    for (int i = 0; i < 13786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13786(int& val) {
    const int x = 13786;
    std::cout << 13786;
    for (int i = 0; i < 13786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13787(int& val) {
    func_in_13787(val);
    const int x = 13787;
    std::cout << 13787;
    for (int i = 0; i < 13787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13787(int& val) {
    const int x = 13787;
    std::cout << 13787;
    for (int i = 0; i < 13787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13788(int& val) {
    func_in_13788(val);
    const int x = 13788;
    std::cout << 13788;
    for (int i = 0; i < 13788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13788(int& val) {
    const int x = 13788;
    std::cout << 13788;
    for (int i = 0; i < 13788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13789(int& val) {
    func_in_13789(val);
    const int x = 13789;
    std::cout << 13789;
    for (int i = 0; i < 13789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13789(int& val) {
    const int x = 13789;
    std::cout << 13789;
    for (int i = 0; i < 13789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13790(int& val) {
    func_in_13790(val);
    const int x = 13790;
    std::cout << 13790;
    for (int i = 0; i < 13790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13790(int& val) {
    const int x = 13790;
    std::cout << 13790;
    for (int i = 0; i < 13790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13791(int& val) {
    func_in_13791(val);
    const int x = 13791;
    std::cout << 13791;
    for (int i = 0; i < 13791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13791(int& val) {
    const int x = 13791;
    std::cout << 13791;
    for (int i = 0; i < 13791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13792(int& val) {
    func_in_13792(val);
    const int x = 13792;
    std::cout << 13792;
    for (int i = 0; i < 13792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13792(int& val) {
    const int x = 13792;
    std::cout << 13792;
    for (int i = 0; i < 13792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13793(int& val) {
    func_in_13793(val);
    const int x = 13793;
    std::cout << 13793;
    for (int i = 0; i < 13793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13793(int& val) {
    const int x = 13793;
    std::cout << 13793;
    for (int i = 0; i < 13793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13794(int& val) {
    func_in_13794(val);
    const int x = 13794;
    std::cout << 13794;
    for (int i = 0; i < 13794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13794(int& val) {
    const int x = 13794;
    std::cout << 13794;
    for (int i = 0; i < 13794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13795(int& val) {
    func_in_13795(val);
    const int x = 13795;
    std::cout << 13795;
    for (int i = 0; i < 13795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13795(int& val) {
    const int x = 13795;
    std::cout << 13795;
    for (int i = 0; i < 13795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13796(int& val) {
    func_in_13796(val);
    const int x = 13796;
    std::cout << 13796;
    for (int i = 0; i < 13796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13796(int& val) {
    const int x = 13796;
    std::cout << 13796;
    for (int i = 0; i < 13796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13797(int& val) {
    func_in_13797(val);
    const int x = 13797;
    std::cout << 13797;
    for (int i = 0; i < 13797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13797(int& val) {
    const int x = 13797;
    std::cout << 13797;
    for (int i = 0; i < 13797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13798(int& val) {
    func_in_13798(val);
    const int x = 13798;
    std::cout << 13798;
    for (int i = 0; i < 13798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13798(int& val) {
    const int x = 13798;
    std::cout << 13798;
    for (int i = 0; i < 13798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13799(int& val) {
    func_in_13799(val);
    const int x = 13799;
    std::cout << 13799;
    for (int i = 0; i < 13799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13799(int& val) {
    const int x = 13799;
    std::cout << 13799;
    for (int i = 0; i < 13799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13800(int& val) {
    func_in_13800(val);
    const int x = 13800;
    std::cout << 13800;
    for (int i = 0; i < 13800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13800(int& val) {
    const int x = 13800;
    std::cout << 13800;
    for (int i = 0; i < 13800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13801(int& val) {
    func_in_13801(val);
    const int x = 13801;
    std::cout << 13801;
    for (int i = 0; i < 13801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13801(int& val) {
    const int x = 13801;
    std::cout << 13801;
    for (int i = 0; i < 13801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13802(int& val) {
    func_in_13802(val);
    const int x = 13802;
    std::cout << 13802;
    for (int i = 0; i < 13802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13802(int& val) {
    const int x = 13802;
    std::cout << 13802;
    for (int i = 0; i < 13802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13803(int& val) {
    func_in_13803(val);
    const int x = 13803;
    std::cout << 13803;
    for (int i = 0; i < 13803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13803(int& val) {
    const int x = 13803;
    std::cout << 13803;
    for (int i = 0; i < 13803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13804(int& val) {
    func_in_13804(val);
    const int x = 13804;
    std::cout << 13804;
    for (int i = 0; i < 13804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13804(int& val) {
    const int x = 13804;
    std::cout << 13804;
    for (int i = 0; i < 13804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13805(int& val) {
    func_in_13805(val);
    const int x = 13805;
    std::cout << 13805;
    for (int i = 0; i < 13805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13805(int& val) {
    const int x = 13805;
    std::cout << 13805;
    for (int i = 0; i < 13805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13806(int& val) {
    func_in_13806(val);
    const int x = 13806;
    std::cout << 13806;
    for (int i = 0; i < 13806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13806(int& val) {
    const int x = 13806;
    std::cout << 13806;
    for (int i = 0; i < 13806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13807(int& val) {
    func_in_13807(val);
    const int x = 13807;
    std::cout << 13807;
    for (int i = 0; i < 13807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13807(int& val) {
    const int x = 13807;
    std::cout << 13807;
    for (int i = 0; i < 13807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13808(int& val) {
    func_in_13808(val);
    const int x = 13808;
    std::cout << 13808;
    for (int i = 0; i < 13808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13808(int& val) {
    const int x = 13808;
    std::cout << 13808;
    for (int i = 0; i < 13808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13809(int& val) {
    func_in_13809(val);
    const int x = 13809;
    std::cout << 13809;
    for (int i = 0; i < 13809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13809(int& val) {
    const int x = 13809;
    std::cout << 13809;
    for (int i = 0; i < 13809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13810(int& val) {
    func_in_13810(val);
    const int x = 13810;
    std::cout << 13810;
    for (int i = 0; i < 13810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13810(int& val) {
    const int x = 13810;
    std::cout << 13810;
    for (int i = 0; i < 13810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13811(int& val) {
    func_in_13811(val);
    const int x = 13811;
    std::cout << 13811;
    for (int i = 0; i < 13811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13811(int& val) {
    const int x = 13811;
    std::cout << 13811;
    for (int i = 0; i < 13811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13812(int& val) {
    func_in_13812(val);
    const int x = 13812;
    std::cout << 13812;
    for (int i = 0; i < 13812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13812(int& val) {
    const int x = 13812;
    std::cout << 13812;
    for (int i = 0; i < 13812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13813(int& val) {
    func_in_13813(val);
    const int x = 13813;
    std::cout << 13813;
    for (int i = 0; i < 13813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13813(int& val) {
    const int x = 13813;
    std::cout << 13813;
    for (int i = 0; i < 13813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13814(int& val) {
    func_in_13814(val);
    const int x = 13814;
    std::cout << 13814;
    for (int i = 0; i < 13814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13814(int& val) {
    const int x = 13814;
    std::cout << 13814;
    for (int i = 0; i < 13814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13815(int& val) {
    func_in_13815(val);
    const int x = 13815;
    std::cout << 13815;
    for (int i = 0; i < 13815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13815(int& val) {
    const int x = 13815;
    std::cout << 13815;
    for (int i = 0; i < 13815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13816(int& val) {
    func_in_13816(val);
    const int x = 13816;
    std::cout << 13816;
    for (int i = 0; i < 13816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13816(int& val) {
    const int x = 13816;
    std::cout << 13816;
    for (int i = 0; i < 13816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13817(int& val) {
    func_in_13817(val);
    const int x = 13817;
    std::cout << 13817;
    for (int i = 0; i < 13817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13817(int& val) {
    const int x = 13817;
    std::cout << 13817;
    for (int i = 0; i < 13817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13818(int& val) {
    func_in_13818(val);
    const int x = 13818;
    std::cout << 13818;
    for (int i = 0; i < 13818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13818(int& val) {
    const int x = 13818;
    std::cout << 13818;
    for (int i = 0; i < 13818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13819(int& val) {
    func_in_13819(val);
    const int x = 13819;
    std::cout << 13819;
    for (int i = 0; i < 13819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13819(int& val) {
    const int x = 13819;
    std::cout << 13819;
    for (int i = 0; i < 13819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13820(int& val) {
    func_in_13820(val);
    const int x = 13820;
    std::cout << 13820;
    for (int i = 0; i < 13820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13820(int& val) {
    const int x = 13820;
    std::cout << 13820;
    for (int i = 0; i < 13820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13821(int& val) {
    func_in_13821(val);
    const int x = 13821;
    std::cout << 13821;
    for (int i = 0; i < 13821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13821(int& val) {
    const int x = 13821;
    std::cout << 13821;
    for (int i = 0; i < 13821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13822(int& val) {
    func_in_13822(val);
    const int x = 13822;
    std::cout << 13822;
    for (int i = 0; i < 13822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13822(int& val) {
    const int x = 13822;
    std::cout << 13822;
    for (int i = 0; i < 13822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13823(int& val) {
    func_in_13823(val);
    const int x = 13823;
    std::cout << 13823;
    for (int i = 0; i < 13823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13823(int& val) {
    const int x = 13823;
    std::cout << 13823;
    for (int i = 0; i < 13823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13824(int& val) {
    func_in_13824(val);
    const int x = 13824;
    std::cout << 13824;
    for (int i = 0; i < 13824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13824(int& val) {
    const int x = 13824;
    std::cout << 13824;
    for (int i = 0; i < 13824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13825(int& val) {
    func_in_13825(val);
    const int x = 13825;
    std::cout << 13825;
    for (int i = 0; i < 13825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13825(int& val) {
    const int x = 13825;
    std::cout << 13825;
    for (int i = 0; i < 13825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13826(int& val) {
    func_in_13826(val);
    const int x = 13826;
    std::cout << 13826;
    for (int i = 0; i < 13826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13826(int& val) {
    const int x = 13826;
    std::cout << 13826;
    for (int i = 0; i < 13826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13827(int& val) {
    func_in_13827(val);
    const int x = 13827;
    std::cout << 13827;
    for (int i = 0; i < 13827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13827(int& val) {
    const int x = 13827;
    std::cout << 13827;
    for (int i = 0; i < 13827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13828(int& val) {
    func_in_13828(val);
    const int x = 13828;
    std::cout << 13828;
    for (int i = 0; i < 13828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13828(int& val) {
    const int x = 13828;
    std::cout << 13828;
    for (int i = 0; i < 13828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13829(int& val) {
    func_in_13829(val);
    const int x = 13829;
    std::cout << 13829;
    for (int i = 0; i < 13829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13829(int& val) {
    const int x = 13829;
    std::cout << 13829;
    for (int i = 0; i < 13829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13830(int& val) {
    func_in_13830(val);
    const int x = 13830;
    std::cout << 13830;
    for (int i = 0; i < 13830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13830(int& val) {
    const int x = 13830;
    std::cout << 13830;
    for (int i = 0; i < 13830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13831(int& val) {
    func_in_13831(val);
    const int x = 13831;
    std::cout << 13831;
    for (int i = 0; i < 13831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13831(int& val) {
    const int x = 13831;
    std::cout << 13831;
    for (int i = 0; i < 13831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13832(int& val) {
    func_in_13832(val);
    const int x = 13832;
    std::cout << 13832;
    for (int i = 0; i < 13832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13832(int& val) {
    const int x = 13832;
    std::cout << 13832;
    for (int i = 0; i < 13832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13833(int& val) {
    func_in_13833(val);
    const int x = 13833;
    std::cout << 13833;
    for (int i = 0; i < 13833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13833(int& val) {
    const int x = 13833;
    std::cout << 13833;
    for (int i = 0; i < 13833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13834(int& val) {
    func_in_13834(val);
    const int x = 13834;
    std::cout << 13834;
    for (int i = 0; i < 13834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13834(int& val) {
    const int x = 13834;
    std::cout << 13834;
    for (int i = 0; i < 13834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13835(int& val) {
    func_in_13835(val);
    const int x = 13835;
    std::cout << 13835;
    for (int i = 0; i < 13835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13835(int& val) {
    const int x = 13835;
    std::cout << 13835;
    for (int i = 0; i < 13835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13836(int& val) {
    func_in_13836(val);
    const int x = 13836;
    std::cout << 13836;
    for (int i = 0; i < 13836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13836(int& val) {
    const int x = 13836;
    std::cout << 13836;
    for (int i = 0; i < 13836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13837(int& val) {
    func_in_13837(val);
    const int x = 13837;
    std::cout << 13837;
    for (int i = 0; i < 13837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13837(int& val) {
    const int x = 13837;
    std::cout << 13837;
    for (int i = 0; i < 13837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13838(int& val) {
    func_in_13838(val);
    const int x = 13838;
    std::cout << 13838;
    for (int i = 0; i < 13838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13838(int& val) {
    const int x = 13838;
    std::cout << 13838;
    for (int i = 0; i < 13838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13839(int& val) {
    func_in_13839(val);
    const int x = 13839;
    std::cout << 13839;
    for (int i = 0; i < 13839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13839(int& val) {
    const int x = 13839;
    std::cout << 13839;
    for (int i = 0; i < 13839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13840(int& val) {
    func_in_13840(val);
    const int x = 13840;
    std::cout << 13840;
    for (int i = 0; i < 13840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13840(int& val) {
    const int x = 13840;
    std::cout << 13840;
    for (int i = 0; i < 13840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13841(int& val) {
    func_in_13841(val);
    const int x = 13841;
    std::cout << 13841;
    for (int i = 0; i < 13841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13841(int& val) {
    const int x = 13841;
    std::cout << 13841;
    for (int i = 0; i < 13841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13842(int& val) {
    func_in_13842(val);
    const int x = 13842;
    std::cout << 13842;
    for (int i = 0; i < 13842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13842(int& val) {
    const int x = 13842;
    std::cout << 13842;
    for (int i = 0; i < 13842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13843(int& val) {
    func_in_13843(val);
    const int x = 13843;
    std::cout << 13843;
    for (int i = 0; i < 13843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13843(int& val) {
    const int x = 13843;
    std::cout << 13843;
    for (int i = 0; i < 13843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13844(int& val) {
    func_in_13844(val);
    const int x = 13844;
    std::cout << 13844;
    for (int i = 0; i < 13844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13844(int& val) {
    const int x = 13844;
    std::cout << 13844;
    for (int i = 0; i < 13844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13845(int& val) {
    func_in_13845(val);
    const int x = 13845;
    std::cout << 13845;
    for (int i = 0; i < 13845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13845(int& val) {
    const int x = 13845;
    std::cout << 13845;
    for (int i = 0; i < 13845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13846(int& val) {
    func_in_13846(val);
    const int x = 13846;
    std::cout << 13846;
    for (int i = 0; i < 13846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13846(int& val) {
    const int x = 13846;
    std::cout << 13846;
    for (int i = 0; i < 13846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13847(int& val) {
    func_in_13847(val);
    const int x = 13847;
    std::cout << 13847;
    for (int i = 0; i < 13847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13847(int& val) {
    const int x = 13847;
    std::cout << 13847;
    for (int i = 0; i < 13847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13848(int& val) {
    func_in_13848(val);
    const int x = 13848;
    std::cout << 13848;
    for (int i = 0; i < 13848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13848(int& val) {
    const int x = 13848;
    std::cout << 13848;
    for (int i = 0; i < 13848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13849(int& val) {
    func_in_13849(val);
    const int x = 13849;
    std::cout << 13849;
    for (int i = 0; i < 13849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13849(int& val) {
    const int x = 13849;
    std::cout << 13849;
    for (int i = 0; i < 13849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13850(int& val) {
    func_in_13850(val);
    const int x = 13850;
    std::cout << 13850;
    for (int i = 0; i < 13850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13850(int& val) {
    const int x = 13850;
    std::cout << 13850;
    for (int i = 0; i < 13850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13851(int& val) {
    func_in_13851(val);
    const int x = 13851;
    std::cout << 13851;
    for (int i = 0; i < 13851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13851(int& val) {
    const int x = 13851;
    std::cout << 13851;
    for (int i = 0; i < 13851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13852(int& val) {
    func_in_13852(val);
    const int x = 13852;
    std::cout << 13852;
    for (int i = 0; i < 13852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13852(int& val) {
    const int x = 13852;
    std::cout << 13852;
    for (int i = 0; i < 13852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13853(int& val) {
    func_in_13853(val);
    const int x = 13853;
    std::cout << 13853;
    for (int i = 0; i < 13853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13853(int& val) {
    const int x = 13853;
    std::cout << 13853;
    for (int i = 0; i < 13853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13854(int& val) {
    func_in_13854(val);
    const int x = 13854;
    std::cout << 13854;
    for (int i = 0; i < 13854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13854(int& val) {
    const int x = 13854;
    std::cout << 13854;
    for (int i = 0; i < 13854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13855(int& val) {
    func_in_13855(val);
    const int x = 13855;
    std::cout << 13855;
    for (int i = 0; i < 13855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13855(int& val) {
    const int x = 13855;
    std::cout << 13855;
    for (int i = 0; i < 13855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13856(int& val) {
    func_in_13856(val);
    const int x = 13856;
    std::cout << 13856;
    for (int i = 0; i < 13856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13856(int& val) {
    const int x = 13856;
    std::cout << 13856;
    for (int i = 0; i < 13856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13857(int& val) {
    func_in_13857(val);
    const int x = 13857;
    std::cout << 13857;
    for (int i = 0; i < 13857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13857(int& val) {
    const int x = 13857;
    std::cout << 13857;
    for (int i = 0; i < 13857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13858(int& val) {
    func_in_13858(val);
    const int x = 13858;
    std::cout << 13858;
    for (int i = 0; i < 13858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13858(int& val) {
    const int x = 13858;
    std::cout << 13858;
    for (int i = 0; i < 13858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13859(int& val) {
    func_in_13859(val);
    const int x = 13859;
    std::cout << 13859;
    for (int i = 0; i < 13859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13859(int& val) {
    const int x = 13859;
    std::cout << 13859;
    for (int i = 0; i < 13859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13860(int& val) {
    func_in_13860(val);
    const int x = 13860;
    std::cout << 13860;
    for (int i = 0; i < 13860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13860(int& val) {
    const int x = 13860;
    std::cout << 13860;
    for (int i = 0; i < 13860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13861(int& val) {
    func_in_13861(val);
    const int x = 13861;
    std::cout << 13861;
    for (int i = 0; i < 13861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13861(int& val) {
    const int x = 13861;
    std::cout << 13861;
    for (int i = 0; i < 13861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13862(int& val) {
    func_in_13862(val);
    const int x = 13862;
    std::cout << 13862;
    for (int i = 0; i < 13862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13862(int& val) {
    const int x = 13862;
    std::cout << 13862;
    for (int i = 0; i < 13862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13863(int& val) {
    func_in_13863(val);
    const int x = 13863;
    std::cout << 13863;
    for (int i = 0; i < 13863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13863(int& val) {
    const int x = 13863;
    std::cout << 13863;
    for (int i = 0; i < 13863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13864(int& val) {
    func_in_13864(val);
    const int x = 13864;
    std::cout << 13864;
    for (int i = 0; i < 13864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13864(int& val) {
    const int x = 13864;
    std::cout << 13864;
    for (int i = 0; i < 13864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13865(int& val) {
    func_in_13865(val);
    const int x = 13865;
    std::cout << 13865;
    for (int i = 0; i < 13865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13865(int& val) {
    const int x = 13865;
    std::cout << 13865;
    for (int i = 0; i < 13865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13866(int& val) {
    func_in_13866(val);
    const int x = 13866;
    std::cout << 13866;
    for (int i = 0; i < 13866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13866(int& val) {
    const int x = 13866;
    std::cout << 13866;
    for (int i = 0; i < 13866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13867(int& val) {
    func_in_13867(val);
    const int x = 13867;
    std::cout << 13867;
    for (int i = 0; i < 13867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13867(int& val) {
    const int x = 13867;
    std::cout << 13867;
    for (int i = 0; i < 13867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13868(int& val) {
    func_in_13868(val);
    const int x = 13868;
    std::cout << 13868;
    for (int i = 0; i < 13868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13868(int& val) {
    const int x = 13868;
    std::cout << 13868;
    for (int i = 0; i < 13868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13869(int& val) {
    func_in_13869(val);
    const int x = 13869;
    std::cout << 13869;
    for (int i = 0; i < 13869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13869(int& val) {
    const int x = 13869;
    std::cout << 13869;
    for (int i = 0; i < 13869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13870(int& val) {
    func_in_13870(val);
    const int x = 13870;
    std::cout << 13870;
    for (int i = 0; i < 13870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13870(int& val) {
    const int x = 13870;
    std::cout << 13870;
    for (int i = 0; i < 13870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13871(int& val) {
    func_in_13871(val);
    const int x = 13871;
    std::cout << 13871;
    for (int i = 0; i < 13871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13871(int& val) {
    const int x = 13871;
    std::cout << 13871;
    for (int i = 0; i < 13871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13872(int& val) {
    func_in_13872(val);
    const int x = 13872;
    std::cout << 13872;
    for (int i = 0; i < 13872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13872(int& val) {
    const int x = 13872;
    std::cout << 13872;
    for (int i = 0; i < 13872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13873(int& val) {
    func_in_13873(val);
    const int x = 13873;
    std::cout << 13873;
    for (int i = 0; i < 13873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13873(int& val) {
    const int x = 13873;
    std::cout << 13873;
    for (int i = 0; i < 13873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13874(int& val) {
    func_in_13874(val);
    const int x = 13874;
    std::cout << 13874;
    for (int i = 0; i < 13874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13874(int& val) {
    const int x = 13874;
    std::cout << 13874;
    for (int i = 0; i < 13874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13875(int& val) {
    func_in_13875(val);
    const int x = 13875;
    std::cout << 13875;
    for (int i = 0; i < 13875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13875(int& val) {
    const int x = 13875;
    std::cout << 13875;
    for (int i = 0; i < 13875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13876(int& val) {
    func_in_13876(val);
    const int x = 13876;
    std::cout << 13876;
    for (int i = 0; i < 13876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13876(int& val) {
    const int x = 13876;
    std::cout << 13876;
    for (int i = 0; i < 13876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13877(int& val) {
    func_in_13877(val);
    const int x = 13877;
    std::cout << 13877;
    for (int i = 0; i < 13877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13877(int& val) {
    const int x = 13877;
    std::cout << 13877;
    for (int i = 0; i < 13877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13878(int& val) {
    func_in_13878(val);
    const int x = 13878;
    std::cout << 13878;
    for (int i = 0; i < 13878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13878(int& val) {
    const int x = 13878;
    std::cout << 13878;
    for (int i = 0; i < 13878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13879(int& val) {
    func_in_13879(val);
    const int x = 13879;
    std::cout << 13879;
    for (int i = 0; i < 13879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13879(int& val) {
    const int x = 13879;
    std::cout << 13879;
    for (int i = 0; i < 13879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13880(int& val) {
    func_in_13880(val);
    const int x = 13880;
    std::cout << 13880;
    for (int i = 0; i < 13880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13880(int& val) {
    const int x = 13880;
    std::cout << 13880;
    for (int i = 0; i < 13880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13881(int& val) {
    func_in_13881(val);
    const int x = 13881;
    std::cout << 13881;
    for (int i = 0; i < 13881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13881(int& val) {
    const int x = 13881;
    std::cout << 13881;
    for (int i = 0; i < 13881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13882(int& val) {
    func_in_13882(val);
    const int x = 13882;
    std::cout << 13882;
    for (int i = 0; i < 13882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13882(int& val) {
    const int x = 13882;
    std::cout << 13882;
    for (int i = 0; i < 13882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13883(int& val) {
    func_in_13883(val);
    const int x = 13883;
    std::cout << 13883;
    for (int i = 0; i < 13883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13883(int& val) {
    const int x = 13883;
    std::cout << 13883;
    for (int i = 0; i < 13883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13884(int& val) {
    func_in_13884(val);
    const int x = 13884;
    std::cout << 13884;
    for (int i = 0; i < 13884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13884(int& val) {
    const int x = 13884;
    std::cout << 13884;
    for (int i = 0; i < 13884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13885(int& val) {
    func_in_13885(val);
    const int x = 13885;
    std::cout << 13885;
    for (int i = 0; i < 13885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13885(int& val) {
    const int x = 13885;
    std::cout << 13885;
    for (int i = 0; i < 13885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13886(int& val) {
    func_in_13886(val);
    const int x = 13886;
    std::cout << 13886;
    for (int i = 0; i < 13886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13886(int& val) {
    const int x = 13886;
    std::cout << 13886;
    for (int i = 0; i < 13886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13887(int& val) {
    func_in_13887(val);
    const int x = 13887;
    std::cout << 13887;
    for (int i = 0; i < 13887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13887(int& val) {
    const int x = 13887;
    std::cout << 13887;
    for (int i = 0; i < 13887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13888(int& val) {
    func_in_13888(val);
    const int x = 13888;
    std::cout << 13888;
    for (int i = 0; i < 13888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13888(int& val) {
    const int x = 13888;
    std::cout << 13888;
    for (int i = 0; i < 13888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13889(int& val) {
    func_in_13889(val);
    const int x = 13889;
    std::cout << 13889;
    for (int i = 0; i < 13889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13889(int& val) {
    const int x = 13889;
    std::cout << 13889;
    for (int i = 0; i < 13889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13890(int& val) {
    func_in_13890(val);
    const int x = 13890;
    std::cout << 13890;
    for (int i = 0; i < 13890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13890(int& val) {
    const int x = 13890;
    std::cout << 13890;
    for (int i = 0; i < 13890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13891(int& val) {
    func_in_13891(val);
    const int x = 13891;
    std::cout << 13891;
    for (int i = 0; i < 13891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13891(int& val) {
    const int x = 13891;
    std::cout << 13891;
    for (int i = 0; i < 13891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13892(int& val) {
    func_in_13892(val);
    const int x = 13892;
    std::cout << 13892;
    for (int i = 0; i < 13892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13892(int& val) {
    const int x = 13892;
    std::cout << 13892;
    for (int i = 0; i < 13892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13893(int& val) {
    func_in_13893(val);
    const int x = 13893;
    std::cout << 13893;
    for (int i = 0; i < 13893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13893(int& val) {
    const int x = 13893;
    std::cout << 13893;
    for (int i = 0; i < 13893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13894(int& val) {
    func_in_13894(val);
    const int x = 13894;
    std::cout << 13894;
    for (int i = 0; i < 13894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13894(int& val) {
    const int x = 13894;
    std::cout << 13894;
    for (int i = 0; i < 13894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13895(int& val) {
    func_in_13895(val);
    const int x = 13895;
    std::cout << 13895;
    for (int i = 0; i < 13895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13895(int& val) {
    const int x = 13895;
    std::cout << 13895;
    for (int i = 0; i < 13895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13896(int& val) {
    func_in_13896(val);
    const int x = 13896;
    std::cout << 13896;
    for (int i = 0; i < 13896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13896(int& val) {
    const int x = 13896;
    std::cout << 13896;
    for (int i = 0; i < 13896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13897(int& val) {
    func_in_13897(val);
    const int x = 13897;
    std::cout << 13897;
    for (int i = 0; i < 13897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13897(int& val) {
    const int x = 13897;
    std::cout << 13897;
    for (int i = 0; i < 13897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13898(int& val) {
    func_in_13898(val);
    const int x = 13898;
    std::cout << 13898;
    for (int i = 0; i < 13898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13898(int& val) {
    const int x = 13898;
    std::cout << 13898;
    for (int i = 0; i < 13898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13899(int& val) {
    func_in_13899(val);
    const int x = 13899;
    std::cout << 13899;
    for (int i = 0; i < 13899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13899(int& val) {
    const int x = 13899;
    std::cout << 13899;
    for (int i = 0; i < 13899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13900(int& val) {
    func_in_13900(val);
    const int x = 13900;
    std::cout << 13900;
    for (int i = 0; i < 13900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13900(int& val) {
    const int x = 13900;
    std::cout << 13900;
    for (int i = 0; i < 13900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13901(int& val) {
    func_in_13901(val);
    const int x = 13901;
    std::cout << 13901;
    for (int i = 0; i < 13901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13901(int& val) {
    const int x = 13901;
    std::cout << 13901;
    for (int i = 0; i < 13901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13902(int& val) {
    func_in_13902(val);
    const int x = 13902;
    std::cout << 13902;
    for (int i = 0; i < 13902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13902(int& val) {
    const int x = 13902;
    std::cout << 13902;
    for (int i = 0; i < 13902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13903(int& val) {
    func_in_13903(val);
    const int x = 13903;
    std::cout << 13903;
    for (int i = 0; i < 13903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13903(int& val) {
    const int x = 13903;
    std::cout << 13903;
    for (int i = 0; i < 13903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13904(int& val) {
    func_in_13904(val);
    const int x = 13904;
    std::cout << 13904;
    for (int i = 0; i < 13904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13904(int& val) {
    const int x = 13904;
    std::cout << 13904;
    for (int i = 0; i < 13904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13905(int& val) {
    func_in_13905(val);
    const int x = 13905;
    std::cout << 13905;
    for (int i = 0; i < 13905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13905(int& val) {
    const int x = 13905;
    std::cout << 13905;
    for (int i = 0; i < 13905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13906(int& val) {
    func_in_13906(val);
    const int x = 13906;
    std::cout << 13906;
    for (int i = 0; i < 13906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13906(int& val) {
    const int x = 13906;
    std::cout << 13906;
    for (int i = 0; i < 13906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13907(int& val) {
    func_in_13907(val);
    const int x = 13907;
    std::cout << 13907;
    for (int i = 0; i < 13907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13907(int& val) {
    const int x = 13907;
    std::cout << 13907;
    for (int i = 0; i < 13907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13908(int& val) {
    func_in_13908(val);
    const int x = 13908;
    std::cout << 13908;
    for (int i = 0; i < 13908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13908(int& val) {
    const int x = 13908;
    std::cout << 13908;
    for (int i = 0; i < 13908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13909(int& val) {
    func_in_13909(val);
    const int x = 13909;
    std::cout << 13909;
    for (int i = 0; i < 13909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13909(int& val) {
    const int x = 13909;
    std::cout << 13909;
    for (int i = 0; i < 13909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13910(int& val) {
    func_in_13910(val);
    const int x = 13910;
    std::cout << 13910;
    for (int i = 0; i < 13910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13910(int& val) {
    const int x = 13910;
    std::cout << 13910;
    for (int i = 0; i < 13910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13911(int& val) {
    func_in_13911(val);
    const int x = 13911;
    std::cout << 13911;
    for (int i = 0; i < 13911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13911(int& val) {
    const int x = 13911;
    std::cout << 13911;
    for (int i = 0; i < 13911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13912(int& val) {
    func_in_13912(val);
    const int x = 13912;
    std::cout << 13912;
    for (int i = 0; i < 13912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13912(int& val) {
    const int x = 13912;
    std::cout << 13912;
    for (int i = 0; i < 13912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13913(int& val) {
    func_in_13913(val);
    const int x = 13913;
    std::cout << 13913;
    for (int i = 0; i < 13913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13913(int& val) {
    const int x = 13913;
    std::cout << 13913;
    for (int i = 0; i < 13913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13914(int& val) {
    func_in_13914(val);
    const int x = 13914;
    std::cout << 13914;
    for (int i = 0; i < 13914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13914(int& val) {
    const int x = 13914;
    std::cout << 13914;
    for (int i = 0; i < 13914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13915(int& val) {
    func_in_13915(val);
    const int x = 13915;
    std::cout << 13915;
    for (int i = 0; i < 13915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13915(int& val) {
    const int x = 13915;
    std::cout << 13915;
    for (int i = 0; i < 13915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13916(int& val) {
    func_in_13916(val);
    const int x = 13916;
    std::cout << 13916;
    for (int i = 0; i < 13916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13916(int& val) {
    const int x = 13916;
    std::cout << 13916;
    for (int i = 0; i < 13916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13917(int& val) {
    func_in_13917(val);
    const int x = 13917;
    std::cout << 13917;
    for (int i = 0; i < 13917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13917(int& val) {
    const int x = 13917;
    std::cout << 13917;
    for (int i = 0; i < 13917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13918(int& val) {
    func_in_13918(val);
    const int x = 13918;
    std::cout << 13918;
    for (int i = 0; i < 13918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13918(int& val) {
    const int x = 13918;
    std::cout << 13918;
    for (int i = 0; i < 13918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13919(int& val) {
    func_in_13919(val);
    const int x = 13919;
    std::cout << 13919;
    for (int i = 0; i < 13919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13919(int& val) {
    const int x = 13919;
    std::cout << 13919;
    for (int i = 0; i < 13919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13920(int& val) {
    func_in_13920(val);
    const int x = 13920;
    std::cout << 13920;
    for (int i = 0; i < 13920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13920(int& val) {
    const int x = 13920;
    std::cout << 13920;
    for (int i = 0; i < 13920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13921(int& val) {
    func_in_13921(val);
    const int x = 13921;
    std::cout << 13921;
    for (int i = 0; i < 13921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13921(int& val) {
    const int x = 13921;
    std::cout << 13921;
    for (int i = 0; i < 13921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13922(int& val) {
    func_in_13922(val);
    const int x = 13922;
    std::cout << 13922;
    for (int i = 0; i < 13922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13922(int& val) {
    const int x = 13922;
    std::cout << 13922;
    for (int i = 0; i < 13922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13923(int& val) {
    func_in_13923(val);
    const int x = 13923;
    std::cout << 13923;
    for (int i = 0; i < 13923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13923(int& val) {
    const int x = 13923;
    std::cout << 13923;
    for (int i = 0; i < 13923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13924(int& val) {
    func_in_13924(val);
    const int x = 13924;
    std::cout << 13924;
    for (int i = 0; i < 13924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13924(int& val) {
    const int x = 13924;
    std::cout << 13924;
    for (int i = 0; i < 13924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13925(int& val) {
    func_in_13925(val);
    const int x = 13925;
    std::cout << 13925;
    for (int i = 0; i < 13925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13925(int& val) {
    const int x = 13925;
    std::cout << 13925;
    for (int i = 0; i < 13925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13926(int& val) {
    func_in_13926(val);
    const int x = 13926;
    std::cout << 13926;
    for (int i = 0; i < 13926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13926(int& val) {
    const int x = 13926;
    std::cout << 13926;
    for (int i = 0; i < 13926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13927(int& val) {
    func_in_13927(val);
    const int x = 13927;
    std::cout << 13927;
    for (int i = 0; i < 13927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13927(int& val) {
    const int x = 13927;
    std::cout << 13927;
    for (int i = 0; i < 13927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13928(int& val) {
    func_in_13928(val);
    const int x = 13928;
    std::cout << 13928;
    for (int i = 0; i < 13928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13928(int& val) {
    const int x = 13928;
    std::cout << 13928;
    for (int i = 0; i < 13928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13929(int& val) {
    func_in_13929(val);
    const int x = 13929;
    std::cout << 13929;
    for (int i = 0; i < 13929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13929(int& val) {
    const int x = 13929;
    std::cout << 13929;
    for (int i = 0; i < 13929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13930(int& val) {
    func_in_13930(val);
    const int x = 13930;
    std::cout << 13930;
    for (int i = 0; i < 13930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13930(int& val) {
    const int x = 13930;
    std::cout << 13930;
    for (int i = 0; i < 13930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13931(int& val) {
    func_in_13931(val);
    const int x = 13931;
    std::cout << 13931;
    for (int i = 0; i < 13931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13931(int& val) {
    const int x = 13931;
    std::cout << 13931;
    for (int i = 0; i < 13931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13932(int& val) {
    func_in_13932(val);
    const int x = 13932;
    std::cout << 13932;
    for (int i = 0; i < 13932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13932(int& val) {
    const int x = 13932;
    std::cout << 13932;
    for (int i = 0; i < 13932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13933(int& val) {
    func_in_13933(val);
    const int x = 13933;
    std::cout << 13933;
    for (int i = 0; i < 13933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13933(int& val) {
    const int x = 13933;
    std::cout << 13933;
    for (int i = 0; i < 13933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13934(int& val) {
    func_in_13934(val);
    const int x = 13934;
    std::cout << 13934;
    for (int i = 0; i < 13934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13934(int& val) {
    const int x = 13934;
    std::cout << 13934;
    for (int i = 0; i < 13934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13935(int& val) {
    func_in_13935(val);
    const int x = 13935;
    std::cout << 13935;
    for (int i = 0; i < 13935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13935(int& val) {
    const int x = 13935;
    std::cout << 13935;
    for (int i = 0; i < 13935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13936(int& val) {
    func_in_13936(val);
    const int x = 13936;
    std::cout << 13936;
    for (int i = 0; i < 13936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13936(int& val) {
    const int x = 13936;
    std::cout << 13936;
    for (int i = 0; i < 13936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13937(int& val) {
    func_in_13937(val);
    const int x = 13937;
    std::cout << 13937;
    for (int i = 0; i < 13937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13937(int& val) {
    const int x = 13937;
    std::cout << 13937;
    for (int i = 0; i < 13937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13938(int& val) {
    func_in_13938(val);
    const int x = 13938;
    std::cout << 13938;
    for (int i = 0; i < 13938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13938(int& val) {
    const int x = 13938;
    std::cout << 13938;
    for (int i = 0; i < 13938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13939(int& val) {
    func_in_13939(val);
    const int x = 13939;
    std::cout << 13939;
    for (int i = 0; i < 13939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13939(int& val) {
    const int x = 13939;
    std::cout << 13939;
    for (int i = 0; i < 13939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13940(int& val) {
    func_in_13940(val);
    const int x = 13940;
    std::cout << 13940;
    for (int i = 0; i < 13940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13940(int& val) {
    const int x = 13940;
    std::cout << 13940;
    for (int i = 0; i < 13940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13941(int& val) {
    func_in_13941(val);
    const int x = 13941;
    std::cout << 13941;
    for (int i = 0; i < 13941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13941(int& val) {
    const int x = 13941;
    std::cout << 13941;
    for (int i = 0; i < 13941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13942(int& val) {
    func_in_13942(val);
    const int x = 13942;
    std::cout << 13942;
    for (int i = 0; i < 13942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13942(int& val) {
    const int x = 13942;
    std::cout << 13942;
    for (int i = 0; i < 13942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13943(int& val) {
    func_in_13943(val);
    const int x = 13943;
    std::cout << 13943;
    for (int i = 0; i < 13943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13943(int& val) {
    const int x = 13943;
    std::cout << 13943;
    for (int i = 0; i < 13943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13944(int& val) {
    func_in_13944(val);
    const int x = 13944;
    std::cout << 13944;
    for (int i = 0; i < 13944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13944(int& val) {
    const int x = 13944;
    std::cout << 13944;
    for (int i = 0; i < 13944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13945(int& val) {
    func_in_13945(val);
    const int x = 13945;
    std::cout << 13945;
    for (int i = 0; i < 13945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13945(int& val) {
    const int x = 13945;
    std::cout << 13945;
    for (int i = 0; i < 13945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13946(int& val) {
    func_in_13946(val);
    const int x = 13946;
    std::cout << 13946;
    for (int i = 0; i < 13946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13946(int& val) {
    const int x = 13946;
    std::cout << 13946;
    for (int i = 0; i < 13946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13947(int& val) {
    func_in_13947(val);
    const int x = 13947;
    std::cout << 13947;
    for (int i = 0; i < 13947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13947(int& val) {
    const int x = 13947;
    std::cout << 13947;
    for (int i = 0; i < 13947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13948(int& val) {
    func_in_13948(val);
    const int x = 13948;
    std::cout << 13948;
    for (int i = 0; i < 13948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13948(int& val) {
    const int x = 13948;
    std::cout << 13948;
    for (int i = 0; i < 13948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13949(int& val) {
    func_in_13949(val);
    const int x = 13949;
    std::cout << 13949;
    for (int i = 0; i < 13949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13949(int& val) {
    const int x = 13949;
    std::cout << 13949;
    for (int i = 0; i < 13949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13950(int& val) {
    func_in_13950(val);
    const int x = 13950;
    std::cout << 13950;
    for (int i = 0; i < 13950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13950(int& val) {
    const int x = 13950;
    std::cout << 13950;
    for (int i = 0; i < 13950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13951(int& val) {
    func_in_13951(val);
    const int x = 13951;
    std::cout << 13951;
    for (int i = 0; i < 13951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13951(int& val) {
    const int x = 13951;
    std::cout << 13951;
    for (int i = 0; i < 13951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13952(int& val) {
    func_in_13952(val);
    const int x = 13952;
    std::cout << 13952;
    for (int i = 0; i < 13952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13952(int& val) {
    const int x = 13952;
    std::cout << 13952;
    for (int i = 0; i < 13952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13953(int& val) {
    func_in_13953(val);
    const int x = 13953;
    std::cout << 13953;
    for (int i = 0; i < 13953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13953(int& val) {
    const int x = 13953;
    std::cout << 13953;
    for (int i = 0; i < 13953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13954(int& val) {
    func_in_13954(val);
    const int x = 13954;
    std::cout << 13954;
    for (int i = 0; i < 13954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13954(int& val) {
    const int x = 13954;
    std::cout << 13954;
    for (int i = 0; i < 13954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13955(int& val) {
    func_in_13955(val);
    const int x = 13955;
    std::cout << 13955;
    for (int i = 0; i < 13955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13955(int& val) {
    const int x = 13955;
    std::cout << 13955;
    for (int i = 0; i < 13955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13956(int& val) {
    func_in_13956(val);
    const int x = 13956;
    std::cout << 13956;
    for (int i = 0; i < 13956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13956(int& val) {
    const int x = 13956;
    std::cout << 13956;
    for (int i = 0; i < 13956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13957(int& val) {
    func_in_13957(val);
    const int x = 13957;
    std::cout << 13957;
    for (int i = 0; i < 13957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13957(int& val) {
    const int x = 13957;
    std::cout << 13957;
    for (int i = 0; i < 13957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13958(int& val) {
    func_in_13958(val);
    const int x = 13958;
    std::cout << 13958;
    for (int i = 0; i < 13958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13958(int& val) {
    const int x = 13958;
    std::cout << 13958;
    for (int i = 0; i < 13958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13959(int& val) {
    func_in_13959(val);
    const int x = 13959;
    std::cout << 13959;
    for (int i = 0; i < 13959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13959(int& val) {
    const int x = 13959;
    std::cout << 13959;
    for (int i = 0; i < 13959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13960(int& val) {
    func_in_13960(val);
    const int x = 13960;
    std::cout << 13960;
    for (int i = 0; i < 13960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13960(int& val) {
    const int x = 13960;
    std::cout << 13960;
    for (int i = 0; i < 13960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13961(int& val) {
    func_in_13961(val);
    const int x = 13961;
    std::cout << 13961;
    for (int i = 0; i < 13961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13961(int& val) {
    const int x = 13961;
    std::cout << 13961;
    for (int i = 0; i < 13961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13962(int& val) {
    func_in_13962(val);
    const int x = 13962;
    std::cout << 13962;
    for (int i = 0; i < 13962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13962(int& val) {
    const int x = 13962;
    std::cout << 13962;
    for (int i = 0; i < 13962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13963(int& val) {
    func_in_13963(val);
    const int x = 13963;
    std::cout << 13963;
    for (int i = 0; i < 13963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13963(int& val) {
    const int x = 13963;
    std::cout << 13963;
    for (int i = 0; i < 13963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13964(int& val) {
    func_in_13964(val);
    const int x = 13964;
    std::cout << 13964;
    for (int i = 0; i < 13964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13964(int& val) {
    const int x = 13964;
    std::cout << 13964;
    for (int i = 0; i < 13964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13965(int& val) {
    func_in_13965(val);
    const int x = 13965;
    std::cout << 13965;
    for (int i = 0; i < 13965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13965(int& val) {
    const int x = 13965;
    std::cout << 13965;
    for (int i = 0; i < 13965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13966(int& val) {
    func_in_13966(val);
    const int x = 13966;
    std::cout << 13966;
    for (int i = 0; i < 13966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13966(int& val) {
    const int x = 13966;
    std::cout << 13966;
    for (int i = 0; i < 13966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13967(int& val) {
    func_in_13967(val);
    const int x = 13967;
    std::cout << 13967;
    for (int i = 0; i < 13967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13967(int& val) {
    const int x = 13967;
    std::cout << 13967;
    for (int i = 0; i < 13967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13968(int& val) {
    func_in_13968(val);
    const int x = 13968;
    std::cout << 13968;
    for (int i = 0; i < 13968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13968(int& val) {
    const int x = 13968;
    std::cout << 13968;
    for (int i = 0; i < 13968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13969(int& val) {
    func_in_13969(val);
    const int x = 13969;
    std::cout << 13969;
    for (int i = 0; i < 13969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13969(int& val) {
    const int x = 13969;
    std::cout << 13969;
    for (int i = 0; i < 13969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13970(int& val) {
    func_in_13970(val);
    const int x = 13970;
    std::cout << 13970;
    for (int i = 0; i < 13970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13970(int& val) {
    const int x = 13970;
    std::cout << 13970;
    for (int i = 0; i < 13970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13971(int& val) {
    func_in_13971(val);
    const int x = 13971;
    std::cout << 13971;
    for (int i = 0; i < 13971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13971(int& val) {
    const int x = 13971;
    std::cout << 13971;
    for (int i = 0; i < 13971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13972(int& val) {
    func_in_13972(val);
    const int x = 13972;
    std::cout << 13972;
    for (int i = 0; i < 13972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13972(int& val) {
    const int x = 13972;
    std::cout << 13972;
    for (int i = 0; i < 13972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13973(int& val) {
    func_in_13973(val);
    const int x = 13973;
    std::cout << 13973;
    for (int i = 0; i < 13973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13973(int& val) {
    const int x = 13973;
    std::cout << 13973;
    for (int i = 0; i < 13973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13974(int& val) {
    func_in_13974(val);
    const int x = 13974;
    std::cout << 13974;
    for (int i = 0; i < 13974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13974(int& val) {
    const int x = 13974;
    std::cout << 13974;
    for (int i = 0; i < 13974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13975(int& val) {
    func_in_13975(val);
    const int x = 13975;
    std::cout << 13975;
    for (int i = 0; i < 13975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13975(int& val) {
    const int x = 13975;
    std::cout << 13975;
    for (int i = 0; i < 13975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13976(int& val) {
    func_in_13976(val);
    const int x = 13976;
    std::cout << 13976;
    for (int i = 0; i < 13976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13976(int& val) {
    const int x = 13976;
    std::cout << 13976;
    for (int i = 0; i < 13976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13977(int& val) {
    func_in_13977(val);
    const int x = 13977;
    std::cout << 13977;
    for (int i = 0; i < 13977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13977(int& val) {
    const int x = 13977;
    std::cout << 13977;
    for (int i = 0; i < 13977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13978(int& val) {
    func_in_13978(val);
    const int x = 13978;
    std::cout << 13978;
    for (int i = 0; i < 13978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13978(int& val) {
    const int x = 13978;
    std::cout << 13978;
    for (int i = 0; i < 13978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13979(int& val) {
    func_in_13979(val);
    const int x = 13979;
    std::cout << 13979;
    for (int i = 0; i < 13979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13979(int& val) {
    const int x = 13979;
    std::cout << 13979;
    for (int i = 0; i < 13979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13980(int& val) {
    func_in_13980(val);
    const int x = 13980;
    std::cout << 13980;
    for (int i = 0; i < 13980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13980(int& val) {
    const int x = 13980;
    std::cout << 13980;
    for (int i = 0; i < 13980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13981(int& val) {
    func_in_13981(val);
    const int x = 13981;
    std::cout << 13981;
    for (int i = 0; i < 13981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13981(int& val) {
    const int x = 13981;
    std::cout << 13981;
    for (int i = 0; i < 13981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13982(int& val) {
    func_in_13982(val);
    const int x = 13982;
    std::cout << 13982;
    for (int i = 0; i < 13982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13982(int& val) {
    const int x = 13982;
    std::cout << 13982;
    for (int i = 0; i < 13982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13983(int& val) {
    func_in_13983(val);
    const int x = 13983;
    std::cout << 13983;
    for (int i = 0; i < 13983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13983(int& val) {
    const int x = 13983;
    std::cout << 13983;
    for (int i = 0; i < 13983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13984(int& val) {
    func_in_13984(val);
    const int x = 13984;
    std::cout << 13984;
    for (int i = 0; i < 13984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13984(int& val) {
    const int x = 13984;
    std::cout << 13984;
    for (int i = 0; i < 13984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13985(int& val) {
    func_in_13985(val);
    const int x = 13985;
    std::cout << 13985;
    for (int i = 0; i < 13985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13985(int& val) {
    const int x = 13985;
    std::cout << 13985;
    for (int i = 0; i < 13985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13986(int& val) {
    func_in_13986(val);
    const int x = 13986;
    std::cout << 13986;
    for (int i = 0; i < 13986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13986(int& val) {
    const int x = 13986;
    std::cout << 13986;
    for (int i = 0; i < 13986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13987(int& val) {
    func_in_13987(val);
    const int x = 13987;
    std::cout << 13987;
    for (int i = 0; i < 13987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13987(int& val) {
    const int x = 13987;
    std::cout << 13987;
    for (int i = 0; i < 13987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13988(int& val) {
    func_in_13988(val);
    const int x = 13988;
    std::cout << 13988;
    for (int i = 0; i < 13988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13988(int& val) {
    const int x = 13988;
    std::cout << 13988;
    for (int i = 0; i < 13988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13989(int& val) {
    func_in_13989(val);
    const int x = 13989;
    std::cout << 13989;
    for (int i = 0; i < 13989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13989(int& val) {
    const int x = 13989;
    std::cout << 13989;
    for (int i = 0; i < 13989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13990(int& val) {
    func_in_13990(val);
    const int x = 13990;
    std::cout << 13990;
    for (int i = 0; i < 13990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13990(int& val) {
    const int x = 13990;
    std::cout << 13990;
    for (int i = 0; i < 13990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13991(int& val) {
    func_in_13991(val);
    const int x = 13991;
    std::cout << 13991;
    for (int i = 0; i < 13991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13991(int& val) {
    const int x = 13991;
    std::cout << 13991;
    for (int i = 0; i < 13991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13992(int& val) {
    func_in_13992(val);
    const int x = 13992;
    std::cout << 13992;
    for (int i = 0; i < 13992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13992(int& val) {
    const int x = 13992;
    std::cout << 13992;
    for (int i = 0; i < 13992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13993(int& val) {
    func_in_13993(val);
    const int x = 13993;
    std::cout << 13993;
    for (int i = 0; i < 13993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13993(int& val) {
    const int x = 13993;
    std::cout << 13993;
    for (int i = 0; i < 13993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13994(int& val) {
    func_in_13994(val);
    const int x = 13994;
    std::cout << 13994;
    for (int i = 0; i < 13994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13994(int& val) {
    const int x = 13994;
    std::cout << 13994;
    for (int i = 0; i < 13994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13995(int& val) {
    func_in_13995(val);
    const int x = 13995;
    std::cout << 13995;
    for (int i = 0; i < 13995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13995(int& val) {
    const int x = 13995;
    std::cout << 13995;
    for (int i = 0; i < 13995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13996(int& val) {
    func_in_13996(val);
    const int x = 13996;
    std::cout << 13996;
    for (int i = 0; i < 13996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13996(int& val) {
    const int x = 13996;
    std::cout << 13996;
    for (int i = 0; i < 13996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13997(int& val) {
    func_in_13997(val);
    const int x = 13997;
    std::cout << 13997;
    for (int i = 0; i < 13997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13997(int& val) {
    const int x = 13997;
    std::cout << 13997;
    for (int i = 0; i < 13997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13998(int& val) {
    func_in_13998(val);
    const int x = 13998;
    std::cout << 13998;
    for (int i = 0; i < 13998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13998(int& val) {
    const int x = 13998;
    std::cout << 13998;
    for (int i = 0; i < 13998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13999(int& val) {
    func_in_13999(val);
    const int x = 13999;
    std::cout << 13999;
    for (int i = 0; i < 13999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13999(int& val) {
    const int x = 13999;
    std::cout << 13999;
    for (int i = 0; i < 13999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14000(int& val) {
    func_in_14000(val);
    const int x = 14000;
    std::cout << 14000;
    for (int i = 0; i < 14000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14000(int& val) {
    const int x = 14000;
    std::cout << 14000;
    for (int i = 0; i < 14000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14001(int& val) {
    func_in_14001(val);
    const int x = 14001;
    std::cout << 14001;
    for (int i = 0; i < 14001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14001(int& val) {
    const int x = 14001;
    std::cout << 14001;
    for (int i = 0; i < 14001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14002(int& val) {
    func_in_14002(val);
    const int x = 14002;
    std::cout << 14002;
    for (int i = 0; i < 14002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14002(int& val) {
    const int x = 14002;
    std::cout << 14002;
    for (int i = 0; i < 14002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14003(int& val) {
    func_in_14003(val);
    const int x = 14003;
    std::cout << 14003;
    for (int i = 0; i < 14003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14003(int& val) {
    const int x = 14003;
    std::cout << 14003;
    for (int i = 0; i < 14003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14004(int& val) {
    func_in_14004(val);
    const int x = 14004;
    std::cout << 14004;
    for (int i = 0; i < 14004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14004(int& val) {
    const int x = 14004;
    std::cout << 14004;
    for (int i = 0; i < 14004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14005(int& val) {
    func_in_14005(val);
    const int x = 14005;
    std::cout << 14005;
    for (int i = 0; i < 14005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14005(int& val) {
    const int x = 14005;
    std::cout << 14005;
    for (int i = 0; i < 14005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14006(int& val) {
    func_in_14006(val);
    const int x = 14006;
    std::cout << 14006;
    for (int i = 0; i < 14006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14006(int& val) {
    const int x = 14006;
    std::cout << 14006;
    for (int i = 0; i < 14006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14007(int& val) {
    func_in_14007(val);
    const int x = 14007;
    std::cout << 14007;
    for (int i = 0; i < 14007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14007(int& val) {
    const int x = 14007;
    std::cout << 14007;
    for (int i = 0; i < 14007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14008(int& val) {
    func_in_14008(val);
    const int x = 14008;
    std::cout << 14008;
    for (int i = 0; i < 14008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14008(int& val) {
    const int x = 14008;
    std::cout << 14008;
    for (int i = 0; i < 14008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14009(int& val) {
    func_in_14009(val);
    const int x = 14009;
    std::cout << 14009;
    for (int i = 0; i < 14009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14009(int& val) {
    const int x = 14009;
    std::cout << 14009;
    for (int i = 0; i < 14009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14010(int& val) {
    func_in_14010(val);
    const int x = 14010;
    std::cout << 14010;
    for (int i = 0; i < 14010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14010(int& val) {
    const int x = 14010;
    std::cout << 14010;
    for (int i = 0; i < 14010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14011(int& val) {
    func_in_14011(val);
    const int x = 14011;
    std::cout << 14011;
    for (int i = 0; i < 14011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14011(int& val) {
    const int x = 14011;
    std::cout << 14011;
    for (int i = 0; i < 14011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14012(int& val) {
    func_in_14012(val);
    const int x = 14012;
    std::cout << 14012;
    for (int i = 0; i < 14012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14012(int& val) {
    const int x = 14012;
    std::cout << 14012;
    for (int i = 0; i < 14012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14013(int& val) {
    func_in_14013(val);
    const int x = 14013;
    std::cout << 14013;
    for (int i = 0; i < 14013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14013(int& val) {
    const int x = 14013;
    std::cout << 14013;
    for (int i = 0; i < 14013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14014(int& val) {
    func_in_14014(val);
    const int x = 14014;
    std::cout << 14014;
    for (int i = 0; i < 14014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14014(int& val) {
    const int x = 14014;
    std::cout << 14014;
    for (int i = 0; i < 14014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14015(int& val) {
    func_in_14015(val);
    const int x = 14015;
    std::cout << 14015;
    for (int i = 0; i < 14015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14015(int& val) {
    const int x = 14015;
    std::cout << 14015;
    for (int i = 0; i < 14015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14016(int& val) {
    func_in_14016(val);
    const int x = 14016;
    std::cout << 14016;
    for (int i = 0; i < 14016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14016(int& val) {
    const int x = 14016;
    std::cout << 14016;
    for (int i = 0; i < 14016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14017(int& val) {
    func_in_14017(val);
    const int x = 14017;
    std::cout << 14017;
    for (int i = 0; i < 14017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14017(int& val) {
    const int x = 14017;
    std::cout << 14017;
    for (int i = 0; i < 14017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14018(int& val) {
    func_in_14018(val);
    const int x = 14018;
    std::cout << 14018;
    for (int i = 0; i < 14018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14018(int& val) {
    const int x = 14018;
    std::cout << 14018;
    for (int i = 0; i < 14018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14019(int& val) {
    func_in_14019(val);
    const int x = 14019;
    std::cout << 14019;
    for (int i = 0; i < 14019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14019(int& val) {
    const int x = 14019;
    std::cout << 14019;
    for (int i = 0; i < 14019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14020(int& val) {
    func_in_14020(val);
    const int x = 14020;
    std::cout << 14020;
    for (int i = 0; i < 14020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14020(int& val) {
    const int x = 14020;
    std::cout << 14020;
    for (int i = 0; i < 14020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14021(int& val) {
    func_in_14021(val);
    const int x = 14021;
    std::cout << 14021;
    for (int i = 0; i < 14021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14021(int& val) {
    const int x = 14021;
    std::cout << 14021;
    for (int i = 0; i < 14021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14022(int& val) {
    func_in_14022(val);
    const int x = 14022;
    std::cout << 14022;
    for (int i = 0; i < 14022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14022(int& val) {
    const int x = 14022;
    std::cout << 14022;
    for (int i = 0; i < 14022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14023(int& val) {
    func_in_14023(val);
    const int x = 14023;
    std::cout << 14023;
    for (int i = 0; i < 14023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14023(int& val) {
    const int x = 14023;
    std::cout << 14023;
    for (int i = 0; i < 14023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14024(int& val) {
    func_in_14024(val);
    const int x = 14024;
    std::cout << 14024;
    for (int i = 0; i < 14024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14024(int& val) {
    const int x = 14024;
    std::cout << 14024;
    for (int i = 0; i < 14024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14025(int& val) {
    func_in_14025(val);
    const int x = 14025;
    std::cout << 14025;
    for (int i = 0; i < 14025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14025(int& val) {
    const int x = 14025;
    std::cout << 14025;
    for (int i = 0; i < 14025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14026(int& val) {
    func_in_14026(val);
    const int x = 14026;
    std::cout << 14026;
    for (int i = 0; i < 14026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14026(int& val) {
    const int x = 14026;
    std::cout << 14026;
    for (int i = 0; i < 14026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14027(int& val) {
    func_in_14027(val);
    const int x = 14027;
    std::cout << 14027;
    for (int i = 0; i < 14027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14027(int& val) {
    const int x = 14027;
    std::cout << 14027;
    for (int i = 0; i < 14027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14028(int& val) {
    func_in_14028(val);
    const int x = 14028;
    std::cout << 14028;
    for (int i = 0; i < 14028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14028(int& val) {
    const int x = 14028;
    std::cout << 14028;
    for (int i = 0; i < 14028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14029(int& val) {
    func_in_14029(val);
    const int x = 14029;
    std::cout << 14029;
    for (int i = 0; i < 14029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14029(int& val) {
    const int x = 14029;
    std::cout << 14029;
    for (int i = 0; i < 14029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14030(int& val) {
    func_in_14030(val);
    const int x = 14030;
    std::cout << 14030;
    for (int i = 0; i < 14030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14030(int& val) {
    const int x = 14030;
    std::cout << 14030;
    for (int i = 0; i < 14030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14031(int& val) {
    func_in_14031(val);
    const int x = 14031;
    std::cout << 14031;
    for (int i = 0; i < 14031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14031(int& val) {
    const int x = 14031;
    std::cout << 14031;
    for (int i = 0; i < 14031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14032(int& val) {
    func_in_14032(val);
    const int x = 14032;
    std::cout << 14032;
    for (int i = 0; i < 14032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14032(int& val) {
    const int x = 14032;
    std::cout << 14032;
    for (int i = 0; i < 14032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14033(int& val) {
    func_in_14033(val);
    const int x = 14033;
    std::cout << 14033;
    for (int i = 0; i < 14033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14033(int& val) {
    const int x = 14033;
    std::cout << 14033;
    for (int i = 0; i < 14033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14034(int& val) {
    func_in_14034(val);
    const int x = 14034;
    std::cout << 14034;
    for (int i = 0; i < 14034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14034(int& val) {
    const int x = 14034;
    std::cout << 14034;
    for (int i = 0; i < 14034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14035(int& val) {
    func_in_14035(val);
    const int x = 14035;
    std::cout << 14035;
    for (int i = 0; i < 14035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14035(int& val) {
    const int x = 14035;
    std::cout << 14035;
    for (int i = 0; i < 14035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14036(int& val) {
    func_in_14036(val);
    const int x = 14036;
    std::cout << 14036;
    for (int i = 0; i < 14036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14036(int& val) {
    const int x = 14036;
    std::cout << 14036;
    for (int i = 0; i < 14036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14037(int& val) {
    func_in_14037(val);
    const int x = 14037;
    std::cout << 14037;
    for (int i = 0; i < 14037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14037(int& val) {
    const int x = 14037;
    std::cout << 14037;
    for (int i = 0; i < 14037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14038(int& val) {
    func_in_14038(val);
    const int x = 14038;
    std::cout << 14038;
    for (int i = 0; i < 14038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14038(int& val) {
    const int x = 14038;
    std::cout << 14038;
    for (int i = 0; i < 14038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14039(int& val) {
    func_in_14039(val);
    const int x = 14039;
    std::cout << 14039;
    for (int i = 0; i < 14039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14039(int& val) {
    const int x = 14039;
    std::cout << 14039;
    for (int i = 0; i < 14039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14040(int& val) {
    func_in_14040(val);
    const int x = 14040;
    std::cout << 14040;
    for (int i = 0; i < 14040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14040(int& val) {
    const int x = 14040;
    std::cout << 14040;
    for (int i = 0; i < 14040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14041(int& val) {
    func_in_14041(val);
    const int x = 14041;
    std::cout << 14041;
    for (int i = 0; i < 14041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14041(int& val) {
    const int x = 14041;
    std::cout << 14041;
    for (int i = 0; i < 14041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14042(int& val) {
    func_in_14042(val);
    const int x = 14042;
    std::cout << 14042;
    for (int i = 0; i < 14042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14042(int& val) {
    const int x = 14042;
    std::cout << 14042;
    for (int i = 0; i < 14042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14043(int& val) {
    func_in_14043(val);
    const int x = 14043;
    std::cout << 14043;
    for (int i = 0; i < 14043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14043(int& val) {
    const int x = 14043;
    std::cout << 14043;
    for (int i = 0; i < 14043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14044(int& val) {
    func_in_14044(val);
    const int x = 14044;
    std::cout << 14044;
    for (int i = 0; i < 14044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14044(int& val) {
    const int x = 14044;
    std::cout << 14044;
    for (int i = 0; i < 14044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14045(int& val) {
    func_in_14045(val);
    const int x = 14045;
    std::cout << 14045;
    for (int i = 0; i < 14045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14045(int& val) {
    const int x = 14045;
    std::cout << 14045;
    for (int i = 0; i < 14045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14046(int& val) {
    func_in_14046(val);
    const int x = 14046;
    std::cout << 14046;
    for (int i = 0; i < 14046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14046(int& val) {
    const int x = 14046;
    std::cout << 14046;
    for (int i = 0; i < 14046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14047(int& val) {
    func_in_14047(val);
    const int x = 14047;
    std::cout << 14047;
    for (int i = 0; i < 14047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14047(int& val) {
    const int x = 14047;
    std::cout << 14047;
    for (int i = 0; i < 14047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14048(int& val) {
    func_in_14048(val);
    const int x = 14048;
    std::cout << 14048;
    for (int i = 0; i < 14048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14048(int& val) {
    const int x = 14048;
    std::cout << 14048;
    for (int i = 0; i < 14048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14049(int& val) {
    func_in_14049(val);
    const int x = 14049;
    std::cout << 14049;
    for (int i = 0; i < 14049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14049(int& val) {
    const int x = 14049;
    std::cout << 14049;
    for (int i = 0; i < 14049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14050(int& val) {
    func_in_14050(val);
    const int x = 14050;
    std::cout << 14050;
    for (int i = 0; i < 14050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14050(int& val) {
    const int x = 14050;
    std::cout << 14050;
    for (int i = 0; i < 14050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14051(int& val) {
    func_in_14051(val);
    const int x = 14051;
    std::cout << 14051;
    for (int i = 0; i < 14051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14051(int& val) {
    const int x = 14051;
    std::cout << 14051;
    for (int i = 0; i < 14051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14052(int& val) {
    func_in_14052(val);
    const int x = 14052;
    std::cout << 14052;
    for (int i = 0; i < 14052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14052(int& val) {
    const int x = 14052;
    std::cout << 14052;
    for (int i = 0; i < 14052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14053(int& val) {
    func_in_14053(val);
    const int x = 14053;
    std::cout << 14053;
    for (int i = 0; i < 14053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14053(int& val) {
    const int x = 14053;
    std::cout << 14053;
    for (int i = 0; i < 14053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14054(int& val) {
    func_in_14054(val);
    const int x = 14054;
    std::cout << 14054;
    for (int i = 0; i < 14054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14054(int& val) {
    const int x = 14054;
    std::cout << 14054;
    for (int i = 0; i < 14054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14055(int& val) {
    func_in_14055(val);
    const int x = 14055;
    std::cout << 14055;
    for (int i = 0; i < 14055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14055(int& val) {
    const int x = 14055;
    std::cout << 14055;
    for (int i = 0; i < 14055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14056(int& val) {
    func_in_14056(val);
    const int x = 14056;
    std::cout << 14056;
    for (int i = 0; i < 14056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14056(int& val) {
    const int x = 14056;
    std::cout << 14056;
    for (int i = 0; i < 14056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14057(int& val) {
    func_in_14057(val);
    const int x = 14057;
    std::cout << 14057;
    for (int i = 0; i < 14057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14057(int& val) {
    const int x = 14057;
    std::cout << 14057;
    for (int i = 0; i < 14057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14058(int& val) {
    func_in_14058(val);
    const int x = 14058;
    std::cout << 14058;
    for (int i = 0; i < 14058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14058(int& val) {
    const int x = 14058;
    std::cout << 14058;
    for (int i = 0; i < 14058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14059(int& val) {
    func_in_14059(val);
    const int x = 14059;
    std::cout << 14059;
    for (int i = 0; i < 14059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14059(int& val) {
    const int x = 14059;
    std::cout << 14059;
    for (int i = 0; i < 14059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14060(int& val) {
    func_in_14060(val);
    const int x = 14060;
    std::cout << 14060;
    for (int i = 0; i < 14060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14060(int& val) {
    const int x = 14060;
    std::cout << 14060;
    for (int i = 0; i < 14060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14061(int& val) {
    func_in_14061(val);
    const int x = 14061;
    std::cout << 14061;
    for (int i = 0; i < 14061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14061(int& val) {
    const int x = 14061;
    std::cout << 14061;
    for (int i = 0; i < 14061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14062(int& val) {
    func_in_14062(val);
    const int x = 14062;
    std::cout << 14062;
    for (int i = 0; i < 14062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14062(int& val) {
    const int x = 14062;
    std::cout << 14062;
    for (int i = 0; i < 14062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14063(int& val) {
    func_in_14063(val);
    const int x = 14063;
    std::cout << 14063;
    for (int i = 0; i < 14063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14063(int& val) {
    const int x = 14063;
    std::cout << 14063;
    for (int i = 0; i < 14063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14064(int& val) {
    func_in_14064(val);
    const int x = 14064;
    std::cout << 14064;
    for (int i = 0; i < 14064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14064(int& val) {
    const int x = 14064;
    std::cout << 14064;
    for (int i = 0; i < 14064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14065(int& val) {
    func_in_14065(val);
    const int x = 14065;
    std::cout << 14065;
    for (int i = 0; i < 14065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14065(int& val) {
    const int x = 14065;
    std::cout << 14065;
    for (int i = 0; i < 14065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14066(int& val) {
    func_in_14066(val);
    const int x = 14066;
    std::cout << 14066;
    for (int i = 0; i < 14066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14066(int& val) {
    const int x = 14066;
    std::cout << 14066;
    for (int i = 0; i < 14066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14067(int& val) {
    func_in_14067(val);
    const int x = 14067;
    std::cout << 14067;
    for (int i = 0; i < 14067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14067(int& val) {
    const int x = 14067;
    std::cout << 14067;
    for (int i = 0; i < 14067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14068(int& val) {
    func_in_14068(val);
    const int x = 14068;
    std::cout << 14068;
    for (int i = 0; i < 14068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14068(int& val) {
    const int x = 14068;
    std::cout << 14068;
    for (int i = 0; i < 14068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14069(int& val) {
    func_in_14069(val);
    const int x = 14069;
    std::cout << 14069;
    for (int i = 0; i < 14069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14069(int& val) {
    const int x = 14069;
    std::cout << 14069;
    for (int i = 0; i < 14069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14070(int& val) {
    func_in_14070(val);
    const int x = 14070;
    std::cout << 14070;
    for (int i = 0; i < 14070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14070(int& val) {
    const int x = 14070;
    std::cout << 14070;
    for (int i = 0; i < 14070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14071(int& val) {
    func_in_14071(val);
    const int x = 14071;
    std::cout << 14071;
    for (int i = 0; i < 14071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14071(int& val) {
    const int x = 14071;
    std::cout << 14071;
    for (int i = 0; i < 14071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14072(int& val) {
    func_in_14072(val);
    const int x = 14072;
    std::cout << 14072;
    for (int i = 0; i < 14072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14072(int& val) {
    const int x = 14072;
    std::cout << 14072;
    for (int i = 0; i < 14072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14073(int& val) {
    func_in_14073(val);
    const int x = 14073;
    std::cout << 14073;
    for (int i = 0; i < 14073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14073(int& val) {
    const int x = 14073;
    std::cout << 14073;
    for (int i = 0; i < 14073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14074(int& val) {
    func_in_14074(val);
    const int x = 14074;
    std::cout << 14074;
    for (int i = 0; i < 14074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14074(int& val) {
    const int x = 14074;
    std::cout << 14074;
    for (int i = 0; i < 14074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14075(int& val) {
    func_in_14075(val);
    const int x = 14075;
    std::cout << 14075;
    for (int i = 0; i < 14075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14075(int& val) {
    const int x = 14075;
    std::cout << 14075;
    for (int i = 0; i < 14075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14076(int& val) {
    func_in_14076(val);
    const int x = 14076;
    std::cout << 14076;
    for (int i = 0; i < 14076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14076(int& val) {
    const int x = 14076;
    std::cout << 14076;
    for (int i = 0; i < 14076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14077(int& val) {
    func_in_14077(val);
    const int x = 14077;
    std::cout << 14077;
    for (int i = 0; i < 14077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14077(int& val) {
    const int x = 14077;
    std::cout << 14077;
    for (int i = 0; i < 14077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14078(int& val) {
    func_in_14078(val);
    const int x = 14078;
    std::cout << 14078;
    for (int i = 0; i < 14078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14078(int& val) {
    const int x = 14078;
    std::cout << 14078;
    for (int i = 0; i < 14078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14079(int& val) {
    func_in_14079(val);
    const int x = 14079;
    std::cout << 14079;
    for (int i = 0; i < 14079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14079(int& val) {
    const int x = 14079;
    std::cout << 14079;
    for (int i = 0; i < 14079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14080(int& val) {
    func_in_14080(val);
    const int x = 14080;
    std::cout << 14080;
    for (int i = 0; i < 14080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14080(int& val) {
    const int x = 14080;
    std::cout << 14080;
    for (int i = 0; i < 14080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14081(int& val) {
    func_in_14081(val);
    const int x = 14081;
    std::cout << 14081;
    for (int i = 0; i < 14081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14081(int& val) {
    const int x = 14081;
    std::cout << 14081;
    for (int i = 0; i < 14081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14082(int& val) {
    func_in_14082(val);
    const int x = 14082;
    std::cout << 14082;
    for (int i = 0; i < 14082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14082(int& val) {
    const int x = 14082;
    std::cout << 14082;
    for (int i = 0; i < 14082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14083(int& val) {
    func_in_14083(val);
    const int x = 14083;
    std::cout << 14083;
    for (int i = 0; i < 14083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14083(int& val) {
    const int x = 14083;
    std::cout << 14083;
    for (int i = 0; i < 14083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14084(int& val) {
    func_in_14084(val);
    const int x = 14084;
    std::cout << 14084;
    for (int i = 0; i < 14084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14084(int& val) {
    const int x = 14084;
    std::cout << 14084;
    for (int i = 0; i < 14084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14085(int& val) {
    func_in_14085(val);
    const int x = 14085;
    std::cout << 14085;
    for (int i = 0; i < 14085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14085(int& val) {
    const int x = 14085;
    std::cout << 14085;
    for (int i = 0; i < 14085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14086(int& val) {
    func_in_14086(val);
    const int x = 14086;
    std::cout << 14086;
    for (int i = 0; i < 14086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14086(int& val) {
    const int x = 14086;
    std::cout << 14086;
    for (int i = 0; i < 14086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14087(int& val) {
    func_in_14087(val);
    const int x = 14087;
    std::cout << 14087;
    for (int i = 0; i < 14087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14087(int& val) {
    const int x = 14087;
    std::cout << 14087;
    for (int i = 0; i < 14087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14088(int& val) {
    func_in_14088(val);
    const int x = 14088;
    std::cout << 14088;
    for (int i = 0; i < 14088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14088(int& val) {
    const int x = 14088;
    std::cout << 14088;
    for (int i = 0; i < 14088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14089(int& val) {
    func_in_14089(val);
    const int x = 14089;
    std::cout << 14089;
    for (int i = 0; i < 14089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14089(int& val) {
    const int x = 14089;
    std::cout << 14089;
    for (int i = 0; i < 14089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14090(int& val) {
    func_in_14090(val);
    const int x = 14090;
    std::cout << 14090;
    for (int i = 0; i < 14090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14090(int& val) {
    const int x = 14090;
    std::cout << 14090;
    for (int i = 0; i < 14090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14091(int& val) {
    func_in_14091(val);
    const int x = 14091;
    std::cout << 14091;
    for (int i = 0; i < 14091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14091(int& val) {
    const int x = 14091;
    std::cout << 14091;
    for (int i = 0; i < 14091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14092(int& val) {
    func_in_14092(val);
    const int x = 14092;
    std::cout << 14092;
    for (int i = 0; i < 14092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14092(int& val) {
    const int x = 14092;
    std::cout << 14092;
    for (int i = 0; i < 14092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14093(int& val) {
    func_in_14093(val);
    const int x = 14093;
    std::cout << 14093;
    for (int i = 0; i < 14093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14093(int& val) {
    const int x = 14093;
    std::cout << 14093;
    for (int i = 0; i < 14093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14094(int& val) {
    func_in_14094(val);
    const int x = 14094;
    std::cout << 14094;
    for (int i = 0; i < 14094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14094(int& val) {
    const int x = 14094;
    std::cout << 14094;
    for (int i = 0; i < 14094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14095(int& val) {
    func_in_14095(val);
    const int x = 14095;
    std::cout << 14095;
    for (int i = 0; i < 14095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14095(int& val) {
    const int x = 14095;
    std::cout << 14095;
    for (int i = 0; i < 14095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14096(int& val) {
    func_in_14096(val);
    const int x = 14096;
    std::cout << 14096;
    for (int i = 0; i < 14096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14096(int& val) {
    const int x = 14096;
    std::cout << 14096;
    for (int i = 0; i < 14096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14097(int& val) {
    func_in_14097(val);
    const int x = 14097;
    std::cout << 14097;
    for (int i = 0; i < 14097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14097(int& val) {
    const int x = 14097;
    std::cout << 14097;
    for (int i = 0; i < 14097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14098(int& val) {
    func_in_14098(val);
    const int x = 14098;
    std::cout << 14098;
    for (int i = 0; i < 14098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14098(int& val) {
    const int x = 14098;
    std::cout << 14098;
    for (int i = 0; i < 14098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14099(int& val) {
    func_in_14099(val);
    const int x = 14099;
    std::cout << 14099;
    for (int i = 0; i < 14099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14099(int& val) {
    const int x = 14099;
    std::cout << 14099;
    for (int i = 0; i < 14099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14100(int& val) {
    func_in_14100(val);
    const int x = 14100;
    std::cout << 14100;
    for (int i = 0; i < 14100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14100(int& val) {
    const int x = 14100;
    std::cout << 14100;
    for (int i = 0; i < 14100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14101(int& val) {
    func_in_14101(val);
    const int x = 14101;
    std::cout << 14101;
    for (int i = 0; i < 14101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14101(int& val) {
    const int x = 14101;
    std::cout << 14101;
    for (int i = 0; i < 14101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14102(int& val) {
    func_in_14102(val);
    const int x = 14102;
    std::cout << 14102;
    for (int i = 0; i < 14102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14102(int& val) {
    const int x = 14102;
    std::cout << 14102;
    for (int i = 0; i < 14102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14103(int& val) {
    func_in_14103(val);
    const int x = 14103;
    std::cout << 14103;
    for (int i = 0; i < 14103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14103(int& val) {
    const int x = 14103;
    std::cout << 14103;
    for (int i = 0; i < 14103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14104(int& val) {
    func_in_14104(val);
    const int x = 14104;
    std::cout << 14104;
    for (int i = 0; i < 14104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14104(int& val) {
    const int x = 14104;
    std::cout << 14104;
    for (int i = 0; i < 14104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14105(int& val) {
    func_in_14105(val);
    const int x = 14105;
    std::cout << 14105;
    for (int i = 0; i < 14105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14105(int& val) {
    const int x = 14105;
    std::cout << 14105;
    for (int i = 0; i < 14105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14106(int& val) {
    func_in_14106(val);
    const int x = 14106;
    std::cout << 14106;
    for (int i = 0; i < 14106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14106(int& val) {
    const int x = 14106;
    std::cout << 14106;
    for (int i = 0; i < 14106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14107(int& val) {
    func_in_14107(val);
    const int x = 14107;
    std::cout << 14107;
    for (int i = 0; i < 14107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14107(int& val) {
    const int x = 14107;
    std::cout << 14107;
    for (int i = 0; i < 14107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14108(int& val) {
    func_in_14108(val);
    const int x = 14108;
    std::cout << 14108;
    for (int i = 0; i < 14108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14108(int& val) {
    const int x = 14108;
    std::cout << 14108;
    for (int i = 0; i < 14108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14109(int& val) {
    func_in_14109(val);
    const int x = 14109;
    std::cout << 14109;
    for (int i = 0; i < 14109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14109(int& val) {
    const int x = 14109;
    std::cout << 14109;
    for (int i = 0; i < 14109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14110(int& val) {
    func_in_14110(val);
    const int x = 14110;
    std::cout << 14110;
    for (int i = 0; i < 14110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14110(int& val) {
    const int x = 14110;
    std::cout << 14110;
    for (int i = 0; i < 14110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14111(int& val) {
    func_in_14111(val);
    const int x = 14111;
    std::cout << 14111;
    for (int i = 0; i < 14111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14111(int& val) {
    const int x = 14111;
    std::cout << 14111;
    for (int i = 0; i < 14111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14112(int& val) {
    func_in_14112(val);
    const int x = 14112;
    std::cout << 14112;
    for (int i = 0; i < 14112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14112(int& val) {
    const int x = 14112;
    std::cout << 14112;
    for (int i = 0; i < 14112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14113(int& val) {
    func_in_14113(val);
    const int x = 14113;
    std::cout << 14113;
    for (int i = 0; i < 14113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14113(int& val) {
    const int x = 14113;
    std::cout << 14113;
    for (int i = 0; i < 14113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14114(int& val) {
    func_in_14114(val);
    const int x = 14114;
    std::cout << 14114;
    for (int i = 0; i < 14114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14114(int& val) {
    const int x = 14114;
    std::cout << 14114;
    for (int i = 0; i < 14114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14115(int& val) {
    func_in_14115(val);
    const int x = 14115;
    std::cout << 14115;
    for (int i = 0; i < 14115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14115(int& val) {
    const int x = 14115;
    std::cout << 14115;
    for (int i = 0; i < 14115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14116(int& val) {
    func_in_14116(val);
    const int x = 14116;
    std::cout << 14116;
    for (int i = 0; i < 14116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14116(int& val) {
    const int x = 14116;
    std::cout << 14116;
    for (int i = 0; i < 14116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14117(int& val) {
    func_in_14117(val);
    const int x = 14117;
    std::cout << 14117;
    for (int i = 0; i < 14117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14117(int& val) {
    const int x = 14117;
    std::cout << 14117;
    for (int i = 0; i < 14117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14118(int& val) {
    func_in_14118(val);
    const int x = 14118;
    std::cout << 14118;
    for (int i = 0; i < 14118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14118(int& val) {
    const int x = 14118;
    std::cout << 14118;
    for (int i = 0; i < 14118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14119(int& val) {
    func_in_14119(val);
    const int x = 14119;
    std::cout << 14119;
    for (int i = 0; i < 14119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14119(int& val) {
    const int x = 14119;
    std::cout << 14119;
    for (int i = 0; i < 14119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14120(int& val) {
    func_in_14120(val);
    const int x = 14120;
    std::cout << 14120;
    for (int i = 0; i < 14120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14120(int& val) {
    const int x = 14120;
    std::cout << 14120;
    for (int i = 0; i < 14120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14121(int& val) {
    func_in_14121(val);
    const int x = 14121;
    std::cout << 14121;
    for (int i = 0; i < 14121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14121(int& val) {
    const int x = 14121;
    std::cout << 14121;
    for (int i = 0; i < 14121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14122(int& val) {
    func_in_14122(val);
    const int x = 14122;
    std::cout << 14122;
    for (int i = 0; i < 14122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14122(int& val) {
    const int x = 14122;
    std::cout << 14122;
    for (int i = 0; i < 14122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14123(int& val) {
    func_in_14123(val);
    const int x = 14123;
    std::cout << 14123;
    for (int i = 0; i < 14123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14123(int& val) {
    const int x = 14123;
    std::cout << 14123;
    for (int i = 0; i < 14123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14124(int& val) {
    func_in_14124(val);
    const int x = 14124;
    std::cout << 14124;
    for (int i = 0; i < 14124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14124(int& val) {
    const int x = 14124;
    std::cout << 14124;
    for (int i = 0; i < 14124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14125(int& val) {
    func_in_14125(val);
    const int x = 14125;
    std::cout << 14125;
    for (int i = 0; i < 14125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14125(int& val) {
    const int x = 14125;
    std::cout << 14125;
    for (int i = 0; i < 14125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14126(int& val) {
    func_in_14126(val);
    const int x = 14126;
    std::cout << 14126;
    for (int i = 0; i < 14126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14126(int& val) {
    const int x = 14126;
    std::cout << 14126;
    for (int i = 0; i < 14126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14127(int& val) {
    func_in_14127(val);
    const int x = 14127;
    std::cout << 14127;
    for (int i = 0; i < 14127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14127(int& val) {
    const int x = 14127;
    std::cout << 14127;
    for (int i = 0; i < 14127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14128(int& val) {
    func_in_14128(val);
    const int x = 14128;
    std::cout << 14128;
    for (int i = 0; i < 14128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14128(int& val) {
    const int x = 14128;
    std::cout << 14128;
    for (int i = 0; i < 14128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14129(int& val) {
    func_in_14129(val);
    const int x = 14129;
    std::cout << 14129;
    for (int i = 0; i < 14129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14129(int& val) {
    const int x = 14129;
    std::cout << 14129;
    for (int i = 0; i < 14129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14130(int& val) {
    func_in_14130(val);
    const int x = 14130;
    std::cout << 14130;
    for (int i = 0; i < 14130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14130(int& val) {
    const int x = 14130;
    std::cout << 14130;
    for (int i = 0; i < 14130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14131(int& val) {
    func_in_14131(val);
    const int x = 14131;
    std::cout << 14131;
    for (int i = 0; i < 14131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14131(int& val) {
    const int x = 14131;
    std::cout << 14131;
    for (int i = 0; i < 14131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14132(int& val) {
    func_in_14132(val);
    const int x = 14132;
    std::cout << 14132;
    for (int i = 0; i < 14132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14132(int& val) {
    const int x = 14132;
    std::cout << 14132;
    for (int i = 0; i < 14132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14133(int& val) {
    func_in_14133(val);
    const int x = 14133;
    std::cout << 14133;
    for (int i = 0; i < 14133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14133(int& val) {
    const int x = 14133;
    std::cout << 14133;
    for (int i = 0; i < 14133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14134(int& val) {
    func_in_14134(val);
    const int x = 14134;
    std::cout << 14134;
    for (int i = 0; i < 14134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14134(int& val) {
    const int x = 14134;
    std::cout << 14134;
    for (int i = 0; i < 14134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14135(int& val) {
    func_in_14135(val);
    const int x = 14135;
    std::cout << 14135;
    for (int i = 0; i < 14135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14135(int& val) {
    const int x = 14135;
    std::cout << 14135;
    for (int i = 0; i < 14135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14136(int& val) {
    func_in_14136(val);
    const int x = 14136;
    std::cout << 14136;
    for (int i = 0; i < 14136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14136(int& val) {
    const int x = 14136;
    std::cout << 14136;
    for (int i = 0; i < 14136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14137(int& val) {
    func_in_14137(val);
    const int x = 14137;
    std::cout << 14137;
    for (int i = 0; i < 14137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14137(int& val) {
    const int x = 14137;
    std::cout << 14137;
    for (int i = 0; i < 14137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14138(int& val) {
    func_in_14138(val);
    const int x = 14138;
    std::cout << 14138;
    for (int i = 0; i < 14138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14138(int& val) {
    const int x = 14138;
    std::cout << 14138;
    for (int i = 0; i < 14138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14139(int& val) {
    func_in_14139(val);
    const int x = 14139;
    std::cout << 14139;
    for (int i = 0; i < 14139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14139(int& val) {
    const int x = 14139;
    std::cout << 14139;
    for (int i = 0; i < 14139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14140(int& val) {
    func_in_14140(val);
    const int x = 14140;
    std::cout << 14140;
    for (int i = 0; i < 14140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14140(int& val) {
    const int x = 14140;
    std::cout << 14140;
    for (int i = 0; i < 14140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14141(int& val) {
    func_in_14141(val);
    const int x = 14141;
    std::cout << 14141;
    for (int i = 0; i < 14141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14141(int& val) {
    const int x = 14141;
    std::cout << 14141;
    for (int i = 0; i < 14141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14142(int& val) {
    func_in_14142(val);
    const int x = 14142;
    std::cout << 14142;
    for (int i = 0; i < 14142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14142(int& val) {
    const int x = 14142;
    std::cout << 14142;
    for (int i = 0; i < 14142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14143(int& val) {
    func_in_14143(val);
    const int x = 14143;
    std::cout << 14143;
    for (int i = 0; i < 14143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14143(int& val) {
    const int x = 14143;
    std::cout << 14143;
    for (int i = 0; i < 14143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14144(int& val) {
    func_in_14144(val);
    const int x = 14144;
    std::cout << 14144;
    for (int i = 0; i < 14144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14144(int& val) {
    const int x = 14144;
    std::cout << 14144;
    for (int i = 0; i < 14144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14145(int& val) {
    func_in_14145(val);
    const int x = 14145;
    std::cout << 14145;
    for (int i = 0; i < 14145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14145(int& val) {
    const int x = 14145;
    std::cout << 14145;
    for (int i = 0; i < 14145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14146(int& val) {
    func_in_14146(val);
    const int x = 14146;
    std::cout << 14146;
    for (int i = 0; i < 14146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14146(int& val) {
    const int x = 14146;
    std::cout << 14146;
    for (int i = 0; i < 14146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14147(int& val) {
    func_in_14147(val);
    const int x = 14147;
    std::cout << 14147;
    for (int i = 0; i < 14147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14147(int& val) {
    const int x = 14147;
    std::cout << 14147;
    for (int i = 0; i < 14147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14148(int& val) {
    func_in_14148(val);
    const int x = 14148;
    std::cout << 14148;
    for (int i = 0; i < 14148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14148(int& val) {
    const int x = 14148;
    std::cout << 14148;
    for (int i = 0; i < 14148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14149(int& val) {
    func_in_14149(val);
    const int x = 14149;
    std::cout << 14149;
    for (int i = 0; i < 14149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14149(int& val) {
    const int x = 14149;
    std::cout << 14149;
    for (int i = 0; i < 14149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14150(int& val) {
    func_in_14150(val);
    const int x = 14150;
    std::cout << 14150;
    for (int i = 0; i < 14150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14150(int& val) {
    const int x = 14150;
    std::cout << 14150;
    for (int i = 0; i < 14150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14151(int& val) {
    func_in_14151(val);
    const int x = 14151;
    std::cout << 14151;
    for (int i = 0; i < 14151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14151(int& val) {
    const int x = 14151;
    std::cout << 14151;
    for (int i = 0; i < 14151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14152(int& val) {
    func_in_14152(val);
    const int x = 14152;
    std::cout << 14152;
    for (int i = 0; i < 14152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14152(int& val) {
    const int x = 14152;
    std::cout << 14152;
    for (int i = 0; i < 14152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14153(int& val) {
    func_in_14153(val);
    const int x = 14153;
    std::cout << 14153;
    for (int i = 0; i < 14153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14153(int& val) {
    const int x = 14153;
    std::cout << 14153;
    for (int i = 0; i < 14153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14154(int& val) {
    func_in_14154(val);
    const int x = 14154;
    std::cout << 14154;
    for (int i = 0; i < 14154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14154(int& val) {
    const int x = 14154;
    std::cout << 14154;
    for (int i = 0; i < 14154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14155(int& val) {
    func_in_14155(val);
    const int x = 14155;
    std::cout << 14155;
    for (int i = 0; i < 14155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14155(int& val) {
    const int x = 14155;
    std::cout << 14155;
    for (int i = 0; i < 14155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14156(int& val) {
    func_in_14156(val);
    const int x = 14156;
    std::cout << 14156;
    for (int i = 0; i < 14156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14156(int& val) {
    const int x = 14156;
    std::cout << 14156;
    for (int i = 0; i < 14156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14157(int& val) {
    func_in_14157(val);
    const int x = 14157;
    std::cout << 14157;
    for (int i = 0; i < 14157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14157(int& val) {
    const int x = 14157;
    std::cout << 14157;
    for (int i = 0; i < 14157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14158(int& val) {
    func_in_14158(val);
    const int x = 14158;
    std::cout << 14158;
    for (int i = 0; i < 14158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14158(int& val) {
    const int x = 14158;
    std::cout << 14158;
    for (int i = 0; i < 14158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14159(int& val) {
    func_in_14159(val);
    const int x = 14159;
    std::cout << 14159;
    for (int i = 0; i < 14159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14159(int& val) {
    const int x = 14159;
    std::cout << 14159;
    for (int i = 0; i < 14159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14160(int& val) {
    func_in_14160(val);
    const int x = 14160;
    std::cout << 14160;
    for (int i = 0; i < 14160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14160(int& val) {
    const int x = 14160;
    std::cout << 14160;
    for (int i = 0; i < 14160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14161(int& val) {
    func_in_14161(val);
    const int x = 14161;
    std::cout << 14161;
    for (int i = 0; i < 14161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14161(int& val) {
    const int x = 14161;
    std::cout << 14161;
    for (int i = 0; i < 14161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14162(int& val) {
    func_in_14162(val);
    const int x = 14162;
    std::cout << 14162;
    for (int i = 0; i < 14162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14162(int& val) {
    const int x = 14162;
    std::cout << 14162;
    for (int i = 0; i < 14162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14163(int& val) {
    func_in_14163(val);
    const int x = 14163;
    std::cout << 14163;
    for (int i = 0; i < 14163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14163(int& val) {
    const int x = 14163;
    std::cout << 14163;
    for (int i = 0; i < 14163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14164(int& val) {
    func_in_14164(val);
    const int x = 14164;
    std::cout << 14164;
    for (int i = 0; i < 14164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14164(int& val) {
    const int x = 14164;
    std::cout << 14164;
    for (int i = 0; i < 14164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14165(int& val) {
    func_in_14165(val);
    const int x = 14165;
    std::cout << 14165;
    for (int i = 0; i < 14165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14165(int& val) {
    const int x = 14165;
    std::cout << 14165;
    for (int i = 0; i < 14165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14166(int& val) {
    func_in_14166(val);
    const int x = 14166;
    std::cout << 14166;
    for (int i = 0; i < 14166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14166(int& val) {
    const int x = 14166;
    std::cout << 14166;
    for (int i = 0; i < 14166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14167(int& val) {
    func_in_14167(val);
    const int x = 14167;
    std::cout << 14167;
    for (int i = 0; i < 14167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14167(int& val) {
    const int x = 14167;
    std::cout << 14167;
    for (int i = 0; i < 14167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14168(int& val) {
    func_in_14168(val);
    const int x = 14168;
    std::cout << 14168;
    for (int i = 0; i < 14168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14168(int& val) {
    const int x = 14168;
    std::cout << 14168;
    for (int i = 0; i < 14168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14169(int& val) {
    func_in_14169(val);
    const int x = 14169;
    std::cout << 14169;
    for (int i = 0; i < 14169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14169(int& val) {
    const int x = 14169;
    std::cout << 14169;
    for (int i = 0; i < 14169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14170(int& val) {
    func_in_14170(val);
    const int x = 14170;
    std::cout << 14170;
    for (int i = 0; i < 14170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14170(int& val) {
    const int x = 14170;
    std::cout << 14170;
    for (int i = 0; i < 14170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14171(int& val) {
    func_in_14171(val);
    const int x = 14171;
    std::cout << 14171;
    for (int i = 0; i < 14171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14171(int& val) {
    const int x = 14171;
    std::cout << 14171;
    for (int i = 0; i < 14171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14172(int& val) {
    func_in_14172(val);
    const int x = 14172;
    std::cout << 14172;
    for (int i = 0; i < 14172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14172(int& val) {
    const int x = 14172;
    std::cout << 14172;
    for (int i = 0; i < 14172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14173(int& val) {
    func_in_14173(val);
    const int x = 14173;
    std::cout << 14173;
    for (int i = 0; i < 14173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14173(int& val) {
    const int x = 14173;
    std::cout << 14173;
    for (int i = 0; i < 14173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14174(int& val) {
    func_in_14174(val);
    const int x = 14174;
    std::cout << 14174;
    for (int i = 0; i < 14174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14174(int& val) {
    const int x = 14174;
    std::cout << 14174;
    for (int i = 0; i < 14174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14175(int& val) {
    func_in_14175(val);
    const int x = 14175;
    std::cout << 14175;
    for (int i = 0; i < 14175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14175(int& val) {
    const int x = 14175;
    std::cout << 14175;
    for (int i = 0; i < 14175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14176(int& val) {
    func_in_14176(val);
    const int x = 14176;
    std::cout << 14176;
    for (int i = 0; i < 14176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14176(int& val) {
    const int x = 14176;
    std::cout << 14176;
    for (int i = 0; i < 14176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14177(int& val) {
    func_in_14177(val);
    const int x = 14177;
    std::cout << 14177;
    for (int i = 0; i < 14177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14177(int& val) {
    const int x = 14177;
    std::cout << 14177;
    for (int i = 0; i < 14177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14178(int& val) {
    func_in_14178(val);
    const int x = 14178;
    std::cout << 14178;
    for (int i = 0; i < 14178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14178(int& val) {
    const int x = 14178;
    std::cout << 14178;
    for (int i = 0; i < 14178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14179(int& val) {
    func_in_14179(val);
    const int x = 14179;
    std::cout << 14179;
    for (int i = 0; i < 14179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14179(int& val) {
    const int x = 14179;
    std::cout << 14179;
    for (int i = 0; i < 14179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14180(int& val) {
    func_in_14180(val);
    const int x = 14180;
    std::cout << 14180;
    for (int i = 0; i < 14180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14180(int& val) {
    const int x = 14180;
    std::cout << 14180;
    for (int i = 0; i < 14180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14181(int& val) {
    func_in_14181(val);
    const int x = 14181;
    std::cout << 14181;
    for (int i = 0; i < 14181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14181(int& val) {
    const int x = 14181;
    std::cout << 14181;
    for (int i = 0; i < 14181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14182(int& val) {
    func_in_14182(val);
    const int x = 14182;
    std::cout << 14182;
    for (int i = 0; i < 14182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14182(int& val) {
    const int x = 14182;
    std::cout << 14182;
    for (int i = 0; i < 14182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14183(int& val) {
    func_in_14183(val);
    const int x = 14183;
    std::cout << 14183;
    for (int i = 0; i < 14183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14183(int& val) {
    const int x = 14183;
    std::cout << 14183;
    for (int i = 0; i < 14183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14184(int& val) {
    func_in_14184(val);
    const int x = 14184;
    std::cout << 14184;
    for (int i = 0; i < 14184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14184(int& val) {
    const int x = 14184;
    std::cout << 14184;
    for (int i = 0; i < 14184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14185(int& val) {
    func_in_14185(val);
    const int x = 14185;
    std::cout << 14185;
    for (int i = 0; i < 14185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14185(int& val) {
    const int x = 14185;
    std::cout << 14185;
    for (int i = 0; i < 14185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14186(int& val) {
    func_in_14186(val);
    const int x = 14186;
    std::cout << 14186;
    for (int i = 0; i < 14186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14186(int& val) {
    const int x = 14186;
    std::cout << 14186;
    for (int i = 0; i < 14186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14187(int& val) {
    func_in_14187(val);
    const int x = 14187;
    std::cout << 14187;
    for (int i = 0; i < 14187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14187(int& val) {
    const int x = 14187;
    std::cout << 14187;
    for (int i = 0; i < 14187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14188(int& val) {
    func_in_14188(val);
    const int x = 14188;
    std::cout << 14188;
    for (int i = 0; i < 14188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14188(int& val) {
    const int x = 14188;
    std::cout << 14188;
    for (int i = 0; i < 14188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14189(int& val) {
    func_in_14189(val);
    const int x = 14189;
    std::cout << 14189;
    for (int i = 0; i < 14189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14189(int& val) {
    const int x = 14189;
    std::cout << 14189;
    for (int i = 0; i < 14189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14190(int& val) {
    func_in_14190(val);
    const int x = 14190;
    std::cout << 14190;
    for (int i = 0; i < 14190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14190(int& val) {
    const int x = 14190;
    std::cout << 14190;
    for (int i = 0; i < 14190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14191(int& val) {
    func_in_14191(val);
    const int x = 14191;
    std::cout << 14191;
    for (int i = 0; i < 14191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14191(int& val) {
    const int x = 14191;
    std::cout << 14191;
    for (int i = 0; i < 14191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14192(int& val) {
    func_in_14192(val);
    const int x = 14192;
    std::cout << 14192;
    for (int i = 0; i < 14192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14192(int& val) {
    const int x = 14192;
    std::cout << 14192;
    for (int i = 0; i < 14192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14193(int& val) {
    func_in_14193(val);
    const int x = 14193;
    std::cout << 14193;
    for (int i = 0; i < 14193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14193(int& val) {
    const int x = 14193;
    std::cout << 14193;
    for (int i = 0; i < 14193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14194(int& val) {
    func_in_14194(val);
    const int x = 14194;
    std::cout << 14194;
    for (int i = 0; i < 14194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14194(int& val) {
    const int x = 14194;
    std::cout << 14194;
    for (int i = 0; i < 14194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14195(int& val) {
    func_in_14195(val);
    const int x = 14195;
    std::cout << 14195;
    for (int i = 0; i < 14195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14195(int& val) {
    const int x = 14195;
    std::cout << 14195;
    for (int i = 0; i < 14195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14196(int& val) {
    func_in_14196(val);
    const int x = 14196;
    std::cout << 14196;
    for (int i = 0; i < 14196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14196(int& val) {
    const int x = 14196;
    std::cout << 14196;
    for (int i = 0; i < 14196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14197(int& val) {
    func_in_14197(val);
    const int x = 14197;
    std::cout << 14197;
    for (int i = 0; i < 14197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14197(int& val) {
    const int x = 14197;
    std::cout << 14197;
    for (int i = 0; i < 14197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14198(int& val) {
    func_in_14198(val);
    const int x = 14198;
    std::cout << 14198;
    for (int i = 0; i < 14198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14198(int& val) {
    const int x = 14198;
    std::cout << 14198;
    for (int i = 0; i < 14198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14199(int& val) {
    func_in_14199(val);
    const int x = 14199;
    std::cout << 14199;
    for (int i = 0; i < 14199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14199(int& val) {
    const int x = 14199;
    std::cout << 14199;
    for (int i = 0; i < 14199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14200(int& val) {
    func_in_14200(val);
    const int x = 14200;
    std::cout << 14200;
    for (int i = 0; i < 14200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14200(int& val) {
    const int x = 14200;
    std::cout << 14200;
    for (int i = 0; i < 14200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14201(int& val) {
    func_in_14201(val);
    const int x = 14201;
    std::cout << 14201;
    for (int i = 0; i < 14201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14201(int& val) {
    const int x = 14201;
    std::cout << 14201;
    for (int i = 0; i < 14201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14202(int& val) {
    func_in_14202(val);
    const int x = 14202;
    std::cout << 14202;
    for (int i = 0; i < 14202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14202(int& val) {
    const int x = 14202;
    std::cout << 14202;
    for (int i = 0; i < 14202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14203(int& val) {
    func_in_14203(val);
    const int x = 14203;
    std::cout << 14203;
    for (int i = 0; i < 14203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14203(int& val) {
    const int x = 14203;
    std::cout << 14203;
    for (int i = 0; i < 14203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14204(int& val) {
    func_in_14204(val);
    const int x = 14204;
    std::cout << 14204;
    for (int i = 0; i < 14204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14204(int& val) {
    const int x = 14204;
    std::cout << 14204;
    for (int i = 0; i < 14204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14205(int& val) {
    func_in_14205(val);
    const int x = 14205;
    std::cout << 14205;
    for (int i = 0; i < 14205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14205(int& val) {
    const int x = 14205;
    std::cout << 14205;
    for (int i = 0; i < 14205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14206(int& val) {
    func_in_14206(val);
    const int x = 14206;
    std::cout << 14206;
    for (int i = 0; i < 14206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14206(int& val) {
    const int x = 14206;
    std::cout << 14206;
    for (int i = 0; i < 14206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14207(int& val) {
    func_in_14207(val);
    const int x = 14207;
    std::cout << 14207;
    for (int i = 0; i < 14207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14207(int& val) {
    const int x = 14207;
    std::cout << 14207;
    for (int i = 0; i < 14207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14208(int& val) {
    func_in_14208(val);
    const int x = 14208;
    std::cout << 14208;
    for (int i = 0; i < 14208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14208(int& val) {
    const int x = 14208;
    std::cout << 14208;
    for (int i = 0; i < 14208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14209(int& val) {
    func_in_14209(val);
    const int x = 14209;
    std::cout << 14209;
    for (int i = 0; i < 14209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14209(int& val) {
    const int x = 14209;
    std::cout << 14209;
    for (int i = 0; i < 14209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14210(int& val) {
    func_in_14210(val);
    const int x = 14210;
    std::cout << 14210;
    for (int i = 0; i < 14210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14210(int& val) {
    const int x = 14210;
    std::cout << 14210;
    for (int i = 0; i < 14210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14211(int& val) {
    func_in_14211(val);
    const int x = 14211;
    std::cout << 14211;
    for (int i = 0; i < 14211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14211(int& val) {
    const int x = 14211;
    std::cout << 14211;
    for (int i = 0; i < 14211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14212(int& val) {
    func_in_14212(val);
    const int x = 14212;
    std::cout << 14212;
    for (int i = 0; i < 14212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14212(int& val) {
    const int x = 14212;
    std::cout << 14212;
    for (int i = 0; i < 14212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14213(int& val) {
    func_in_14213(val);
    const int x = 14213;
    std::cout << 14213;
    for (int i = 0; i < 14213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14213(int& val) {
    const int x = 14213;
    std::cout << 14213;
    for (int i = 0; i < 14213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14214(int& val) {
    func_in_14214(val);
    const int x = 14214;
    std::cout << 14214;
    for (int i = 0; i < 14214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14214(int& val) {
    const int x = 14214;
    std::cout << 14214;
    for (int i = 0; i < 14214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14215(int& val) {
    func_in_14215(val);
    const int x = 14215;
    std::cout << 14215;
    for (int i = 0; i < 14215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14215(int& val) {
    const int x = 14215;
    std::cout << 14215;
    for (int i = 0; i < 14215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14216(int& val) {
    func_in_14216(val);
    const int x = 14216;
    std::cout << 14216;
    for (int i = 0; i < 14216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14216(int& val) {
    const int x = 14216;
    std::cout << 14216;
    for (int i = 0; i < 14216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14217(int& val) {
    func_in_14217(val);
    const int x = 14217;
    std::cout << 14217;
    for (int i = 0; i < 14217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14217(int& val) {
    const int x = 14217;
    std::cout << 14217;
    for (int i = 0; i < 14217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14218(int& val) {
    func_in_14218(val);
    const int x = 14218;
    std::cout << 14218;
    for (int i = 0; i < 14218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14218(int& val) {
    const int x = 14218;
    std::cout << 14218;
    for (int i = 0; i < 14218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14219(int& val) {
    func_in_14219(val);
    const int x = 14219;
    std::cout << 14219;
    for (int i = 0; i < 14219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14219(int& val) {
    const int x = 14219;
    std::cout << 14219;
    for (int i = 0; i < 14219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14220(int& val) {
    func_in_14220(val);
    const int x = 14220;
    std::cout << 14220;
    for (int i = 0; i < 14220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14220(int& val) {
    const int x = 14220;
    std::cout << 14220;
    for (int i = 0; i < 14220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14221(int& val) {
    func_in_14221(val);
    const int x = 14221;
    std::cout << 14221;
    for (int i = 0; i < 14221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14221(int& val) {
    const int x = 14221;
    std::cout << 14221;
    for (int i = 0; i < 14221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14222(int& val) {
    func_in_14222(val);
    const int x = 14222;
    std::cout << 14222;
    for (int i = 0; i < 14222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14222(int& val) {
    const int x = 14222;
    std::cout << 14222;
    for (int i = 0; i < 14222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14223(int& val) {
    func_in_14223(val);
    const int x = 14223;
    std::cout << 14223;
    for (int i = 0; i < 14223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14223(int& val) {
    const int x = 14223;
    std::cout << 14223;
    for (int i = 0; i < 14223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14224(int& val) {
    func_in_14224(val);
    const int x = 14224;
    std::cout << 14224;
    for (int i = 0; i < 14224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14224(int& val) {
    const int x = 14224;
    std::cout << 14224;
    for (int i = 0; i < 14224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14225(int& val) {
    func_in_14225(val);
    const int x = 14225;
    std::cout << 14225;
    for (int i = 0; i < 14225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14225(int& val) {
    const int x = 14225;
    std::cout << 14225;
    for (int i = 0; i < 14225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14226(int& val) {
    func_in_14226(val);
    const int x = 14226;
    std::cout << 14226;
    for (int i = 0; i < 14226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14226(int& val) {
    const int x = 14226;
    std::cout << 14226;
    for (int i = 0; i < 14226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14227(int& val) {
    func_in_14227(val);
    const int x = 14227;
    std::cout << 14227;
    for (int i = 0; i < 14227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14227(int& val) {
    const int x = 14227;
    std::cout << 14227;
    for (int i = 0; i < 14227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14228(int& val) {
    func_in_14228(val);
    const int x = 14228;
    std::cout << 14228;
    for (int i = 0; i < 14228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14228(int& val) {
    const int x = 14228;
    std::cout << 14228;
    for (int i = 0; i < 14228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14229(int& val) {
    func_in_14229(val);
    const int x = 14229;
    std::cout << 14229;
    for (int i = 0; i < 14229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14229(int& val) {
    const int x = 14229;
    std::cout << 14229;
    for (int i = 0; i < 14229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14230(int& val) {
    func_in_14230(val);
    const int x = 14230;
    std::cout << 14230;
    for (int i = 0; i < 14230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14230(int& val) {
    const int x = 14230;
    std::cout << 14230;
    for (int i = 0; i < 14230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14231(int& val) {
    func_in_14231(val);
    const int x = 14231;
    std::cout << 14231;
    for (int i = 0; i < 14231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14231(int& val) {
    const int x = 14231;
    std::cout << 14231;
    for (int i = 0; i < 14231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14232(int& val) {
    func_in_14232(val);
    const int x = 14232;
    std::cout << 14232;
    for (int i = 0; i < 14232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14232(int& val) {
    const int x = 14232;
    std::cout << 14232;
    for (int i = 0; i < 14232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14233(int& val) {
    func_in_14233(val);
    const int x = 14233;
    std::cout << 14233;
    for (int i = 0; i < 14233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14233(int& val) {
    const int x = 14233;
    std::cout << 14233;
    for (int i = 0; i < 14233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14234(int& val) {
    func_in_14234(val);
    const int x = 14234;
    std::cout << 14234;
    for (int i = 0; i < 14234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14234(int& val) {
    const int x = 14234;
    std::cout << 14234;
    for (int i = 0; i < 14234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14235(int& val) {
    func_in_14235(val);
    const int x = 14235;
    std::cout << 14235;
    for (int i = 0; i < 14235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14235(int& val) {
    const int x = 14235;
    std::cout << 14235;
    for (int i = 0; i < 14235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14236(int& val) {
    func_in_14236(val);
    const int x = 14236;
    std::cout << 14236;
    for (int i = 0; i < 14236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14236(int& val) {
    const int x = 14236;
    std::cout << 14236;
    for (int i = 0; i < 14236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14237(int& val) {
    func_in_14237(val);
    const int x = 14237;
    std::cout << 14237;
    for (int i = 0; i < 14237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14237(int& val) {
    const int x = 14237;
    std::cout << 14237;
    for (int i = 0; i < 14237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14238(int& val) {
    func_in_14238(val);
    const int x = 14238;
    std::cout << 14238;
    for (int i = 0; i < 14238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14238(int& val) {
    const int x = 14238;
    std::cout << 14238;
    for (int i = 0; i < 14238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14239(int& val) {
    func_in_14239(val);
    const int x = 14239;
    std::cout << 14239;
    for (int i = 0; i < 14239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14239(int& val) {
    const int x = 14239;
    std::cout << 14239;
    for (int i = 0; i < 14239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14240(int& val) {
    func_in_14240(val);
    const int x = 14240;
    std::cout << 14240;
    for (int i = 0; i < 14240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14240(int& val) {
    const int x = 14240;
    std::cout << 14240;
    for (int i = 0; i < 14240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14241(int& val) {
    func_in_14241(val);
    const int x = 14241;
    std::cout << 14241;
    for (int i = 0; i < 14241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14241(int& val) {
    const int x = 14241;
    std::cout << 14241;
    for (int i = 0; i < 14241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14242(int& val) {
    func_in_14242(val);
    const int x = 14242;
    std::cout << 14242;
    for (int i = 0; i < 14242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14242(int& val) {
    const int x = 14242;
    std::cout << 14242;
    for (int i = 0; i < 14242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14243(int& val) {
    func_in_14243(val);
    const int x = 14243;
    std::cout << 14243;
    for (int i = 0; i < 14243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14243(int& val) {
    const int x = 14243;
    std::cout << 14243;
    for (int i = 0; i < 14243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14244(int& val) {
    func_in_14244(val);
    const int x = 14244;
    std::cout << 14244;
    for (int i = 0; i < 14244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14244(int& val) {
    const int x = 14244;
    std::cout << 14244;
    for (int i = 0; i < 14244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14245(int& val) {
    func_in_14245(val);
    const int x = 14245;
    std::cout << 14245;
    for (int i = 0; i < 14245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14245(int& val) {
    const int x = 14245;
    std::cout << 14245;
    for (int i = 0; i < 14245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14246(int& val) {
    func_in_14246(val);
    const int x = 14246;
    std::cout << 14246;
    for (int i = 0; i < 14246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14246(int& val) {
    const int x = 14246;
    std::cout << 14246;
    for (int i = 0; i < 14246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14247(int& val) {
    func_in_14247(val);
    const int x = 14247;
    std::cout << 14247;
    for (int i = 0; i < 14247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14247(int& val) {
    const int x = 14247;
    std::cout << 14247;
    for (int i = 0; i < 14247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14248(int& val) {
    func_in_14248(val);
    const int x = 14248;
    std::cout << 14248;
    for (int i = 0; i < 14248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14248(int& val) {
    const int x = 14248;
    std::cout << 14248;
    for (int i = 0; i < 14248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14249(int& val) {
    func_in_14249(val);
    const int x = 14249;
    std::cout << 14249;
    for (int i = 0; i < 14249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14249(int& val) {
    const int x = 14249;
    std::cout << 14249;
    for (int i = 0; i < 14249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14250(int& val) {
    func_in_14250(val);
    const int x = 14250;
    std::cout << 14250;
    for (int i = 0; i < 14250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14250(int& val) {
    const int x = 14250;
    std::cout << 14250;
    for (int i = 0; i < 14250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14251(int& val) {
    func_in_14251(val);
    const int x = 14251;
    std::cout << 14251;
    for (int i = 0; i < 14251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14251(int& val) {
    const int x = 14251;
    std::cout << 14251;
    for (int i = 0; i < 14251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14252(int& val) {
    func_in_14252(val);
    const int x = 14252;
    std::cout << 14252;
    for (int i = 0; i < 14252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14252(int& val) {
    const int x = 14252;
    std::cout << 14252;
    for (int i = 0; i < 14252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14253(int& val) {
    func_in_14253(val);
    const int x = 14253;
    std::cout << 14253;
    for (int i = 0; i < 14253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14253(int& val) {
    const int x = 14253;
    std::cout << 14253;
    for (int i = 0; i < 14253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14254(int& val) {
    func_in_14254(val);
    const int x = 14254;
    std::cout << 14254;
    for (int i = 0; i < 14254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14254(int& val) {
    const int x = 14254;
    std::cout << 14254;
    for (int i = 0; i < 14254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14255(int& val) {
    func_in_14255(val);
    const int x = 14255;
    std::cout << 14255;
    for (int i = 0; i < 14255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14255(int& val) {
    const int x = 14255;
    std::cout << 14255;
    for (int i = 0; i < 14255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14256(int& val) {
    func_in_14256(val);
    const int x = 14256;
    std::cout << 14256;
    for (int i = 0; i < 14256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14256(int& val) {
    const int x = 14256;
    std::cout << 14256;
    for (int i = 0; i < 14256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14257(int& val) {
    func_in_14257(val);
    const int x = 14257;
    std::cout << 14257;
    for (int i = 0; i < 14257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14257(int& val) {
    const int x = 14257;
    std::cout << 14257;
    for (int i = 0; i < 14257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14258(int& val) {
    func_in_14258(val);
    const int x = 14258;
    std::cout << 14258;
    for (int i = 0; i < 14258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14258(int& val) {
    const int x = 14258;
    std::cout << 14258;
    for (int i = 0; i < 14258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14259(int& val) {
    func_in_14259(val);
    const int x = 14259;
    std::cout << 14259;
    for (int i = 0; i < 14259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14259(int& val) {
    const int x = 14259;
    std::cout << 14259;
    for (int i = 0; i < 14259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14260(int& val) {
    func_in_14260(val);
    const int x = 14260;
    std::cout << 14260;
    for (int i = 0; i < 14260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14260(int& val) {
    const int x = 14260;
    std::cout << 14260;
    for (int i = 0; i < 14260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14261(int& val) {
    func_in_14261(val);
    const int x = 14261;
    std::cout << 14261;
    for (int i = 0; i < 14261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14261(int& val) {
    const int x = 14261;
    std::cout << 14261;
    for (int i = 0; i < 14261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14262(int& val) {
    func_in_14262(val);
    const int x = 14262;
    std::cout << 14262;
    for (int i = 0; i < 14262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14262(int& val) {
    const int x = 14262;
    std::cout << 14262;
    for (int i = 0; i < 14262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14263(int& val) {
    func_in_14263(val);
    const int x = 14263;
    std::cout << 14263;
    for (int i = 0; i < 14263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14263(int& val) {
    const int x = 14263;
    std::cout << 14263;
    for (int i = 0; i < 14263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14264(int& val) {
    func_in_14264(val);
    const int x = 14264;
    std::cout << 14264;
    for (int i = 0; i < 14264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14264(int& val) {
    const int x = 14264;
    std::cout << 14264;
    for (int i = 0; i < 14264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14265(int& val) {
    func_in_14265(val);
    const int x = 14265;
    std::cout << 14265;
    for (int i = 0; i < 14265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14265(int& val) {
    const int x = 14265;
    std::cout << 14265;
    for (int i = 0; i < 14265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14266(int& val) {
    func_in_14266(val);
    const int x = 14266;
    std::cout << 14266;
    for (int i = 0; i < 14266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14266(int& val) {
    const int x = 14266;
    std::cout << 14266;
    for (int i = 0; i < 14266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14267(int& val) {
    func_in_14267(val);
    const int x = 14267;
    std::cout << 14267;
    for (int i = 0; i < 14267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14267(int& val) {
    const int x = 14267;
    std::cout << 14267;
    for (int i = 0; i < 14267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14268(int& val) {
    func_in_14268(val);
    const int x = 14268;
    std::cout << 14268;
    for (int i = 0; i < 14268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14268(int& val) {
    const int x = 14268;
    std::cout << 14268;
    for (int i = 0; i < 14268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14269(int& val) {
    func_in_14269(val);
    const int x = 14269;
    std::cout << 14269;
    for (int i = 0; i < 14269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14269(int& val) {
    const int x = 14269;
    std::cout << 14269;
    for (int i = 0; i < 14269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14270(int& val) {
    func_in_14270(val);
    const int x = 14270;
    std::cout << 14270;
    for (int i = 0; i < 14270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14270(int& val) {
    const int x = 14270;
    std::cout << 14270;
    for (int i = 0; i < 14270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14271(int& val) {
    func_in_14271(val);
    const int x = 14271;
    std::cout << 14271;
    for (int i = 0; i < 14271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14271(int& val) {
    const int x = 14271;
    std::cout << 14271;
    for (int i = 0; i < 14271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14272(int& val) {
    func_in_14272(val);
    const int x = 14272;
    std::cout << 14272;
    for (int i = 0; i < 14272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14272(int& val) {
    const int x = 14272;
    std::cout << 14272;
    for (int i = 0; i < 14272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14273(int& val) {
    func_in_14273(val);
    const int x = 14273;
    std::cout << 14273;
    for (int i = 0; i < 14273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14273(int& val) {
    const int x = 14273;
    std::cout << 14273;
    for (int i = 0; i < 14273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14274(int& val) {
    func_in_14274(val);
    const int x = 14274;
    std::cout << 14274;
    for (int i = 0; i < 14274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14274(int& val) {
    const int x = 14274;
    std::cout << 14274;
    for (int i = 0; i < 14274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14275(int& val) {
    func_in_14275(val);
    const int x = 14275;
    std::cout << 14275;
    for (int i = 0; i < 14275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14275(int& val) {
    const int x = 14275;
    std::cout << 14275;
    for (int i = 0; i < 14275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14276(int& val) {
    func_in_14276(val);
    const int x = 14276;
    std::cout << 14276;
    for (int i = 0; i < 14276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14276(int& val) {
    const int x = 14276;
    std::cout << 14276;
    for (int i = 0; i < 14276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14277(int& val) {
    func_in_14277(val);
    const int x = 14277;
    std::cout << 14277;
    for (int i = 0; i < 14277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14277(int& val) {
    const int x = 14277;
    std::cout << 14277;
    for (int i = 0; i < 14277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14278(int& val) {
    func_in_14278(val);
    const int x = 14278;
    std::cout << 14278;
    for (int i = 0; i < 14278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14278(int& val) {
    const int x = 14278;
    std::cout << 14278;
    for (int i = 0; i < 14278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14279(int& val) {
    func_in_14279(val);
    const int x = 14279;
    std::cout << 14279;
    for (int i = 0; i < 14279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14279(int& val) {
    const int x = 14279;
    std::cout << 14279;
    for (int i = 0; i < 14279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14280(int& val) {
    func_in_14280(val);
    const int x = 14280;
    std::cout << 14280;
    for (int i = 0; i < 14280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14280(int& val) {
    const int x = 14280;
    std::cout << 14280;
    for (int i = 0; i < 14280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14281(int& val) {
    func_in_14281(val);
    const int x = 14281;
    std::cout << 14281;
    for (int i = 0; i < 14281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14281(int& val) {
    const int x = 14281;
    std::cout << 14281;
    for (int i = 0; i < 14281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14282(int& val) {
    func_in_14282(val);
    const int x = 14282;
    std::cout << 14282;
    for (int i = 0; i < 14282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14282(int& val) {
    const int x = 14282;
    std::cout << 14282;
    for (int i = 0; i < 14282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14283(int& val) {
    func_in_14283(val);
    const int x = 14283;
    std::cout << 14283;
    for (int i = 0; i < 14283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14283(int& val) {
    const int x = 14283;
    std::cout << 14283;
    for (int i = 0; i < 14283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14284(int& val) {
    func_in_14284(val);
    const int x = 14284;
    std::cout << 14284;
    for (int i = 0; i < 14284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14284(int& val) {
    const int x = 14284;
    std::cout << 14284;
    for (int i = 0; i < 14284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14285(int& val) {
    func_in_14285(val);
    const int x = 14285;
    std::cout << 14285;
    for (int i = 0; i < 14285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14285(int& val) {
    const int x = 14285;
    std::cout << 14285;
    for (int i = 0; i < 14285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14286(int& val) {
    func_in_14286(val);
    const int x = 14286;
    std::cout << 14286;
    for (int i = 0; i < 14286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14286(int& val) {
    const int x = 14286;
    std::cout << 14286;
    for (int i = 0; i < 14286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14287(int& val) {
    func_in_14287(val);
    const int x = 14287;
    std::cout << 14287;
    for (int i = 0; i < 14287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14287(int& val) {
    const int x = 14287;
    std::cout << 14287;
    for (int i = 0; i < 14287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14288(int& val) {
    func_in_14288(val);
    const int x = 14288;
    std::cout << 14288;
    for (int i = 0; i < 14288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14288(int& val) {
    const int x = 14288;
    std::cout << 14288;
    for (int i = 0; i < 14288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14289(int& val) {
    func_in_14289(val);
    const int x = 14289;
    std::cout << 14289;
    for (int i = 0; i < 14289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14289(int& val) {
    const int x = 14289;
    std::cout << 14289;
    for (int i = 0; i < 14289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14290(int& val) {
    func_in_14290(val);
    const int x = 14290;
    std::cout << 14290;
    for (int i = 0; i < 14290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14290(int& val) {
    const int x = 14290;
    std::cout << 14290;
    for (int i = 0; i < 14290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14291(int& val) {
    func_in_14291(val);
    const int x = 14291;
    std::cout << 14291;
    for (int i = 0; i < 14291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14291(int& val) {
    const int x = 14291;
    std::cout << 14291;
    for (int i = 0; i < 14291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14292(int& val) {
    func_in_14292(val);
    const int x = 14292;
    std::cout << 14292;
    for (int i = 0; i < 14292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14292(int& val) {
    const int x = 14292;
    std::cout << 14292;
    for (int i = 0; i < 14292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14293(int& val) {
    func_in_14293(val);
    const int x = 14293;
    std::cout << 14293;
    for (int i = 0; i < 14293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14293(int& val) {
    const int x = 14293;
    std::cout << 14293;
    for (int i = 0; i < 14293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14294(int& val) {
    func_in_14294(val);
    const int x = 14294;
    std::cout << 14294;
    for (int i = 0; i < 14294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14294(int& val) {
    const int x = 14294;
    std::cout << 14294;
    for (int i = 0; i < 14294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14295(int& val) {
    func_in_14295(val);
    const int x = 14295;
    std::cout << 14295;
    for (int i = 0; i < 14295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14295(int& val) {
    const int x = 14295;
    std::cout << 14295;
    for (int i = 0; i < 14295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14296(int& val) {
    func_in_14296(val);
    const int x = 14296;
    std::cout << 14296;
    for (int i = 0; i < 14296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14296(int& val) {
    const int x = 14296;
    std::cout << 14296;
    for (int i = 0; i < 14296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14297(int& val) {
    func_in_14297(val);
    const int x = 14297;
    std::cout << 14297;
    for (int i = 0; i < 14297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14297(int& val) {
    const int x = 14297;
    std::cout << 14297;
    for (int i = 0; i < 14297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14298(int& val) {
    func_in_14298(val);
    const int x = 14298;
    std::cout << 14298;
    for (int i = 0; i < 14298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14298(int& val) {
    const int x = 14298;
    std::cout << 14298;
    for (int i = 0; i < 14298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14299(int& val) {
    func_in_14299(val);
    const int x = 14299;
    std::cout << 14299;
    for (int i = 0; i < 14299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14299(int& val) {
    const int x = 14299;
    std::cout << 14299;
    for (int i = 0; i < 14299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14300(int& val) {
    func_in_14300(val);
    const int x = 14300;
    std::cout << 14300;
    for (int i = 0; i < 14300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14300(int& val) {
    const int x = 14300;
    std::cout << 14300;
    for (int i = 0; i < 14300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14301(int& val) {
    func_in_14301(val);
    const int x = 14301;
    std::cout << 14301;
    for (int i = 0; i < 14301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14301(int& val) {
    const int x = 14301;
    std::cout << 14301;
    for (int i = 0; i < 14301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14302(int& val) {
    func_in_14302(val);
    const int x = 14302;
    std::cout << 14302;
    for (int i = 0; i < 14302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14302(int& val) {
    const int x = 14302;
    std::cout << 14302;
    for (int i = 0; i < 14302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14303(int& val) {
    func_in_14303(val);
    const int x = 14303;
    std::cout << 14303;
    for (int i = 0; i < 14303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14303(int& val) {
    const int x = 14303;
    std::cout << 14303;
    for (int i = 0; i < 14303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14304(int& val) {
    func_in_14304(val);
    const int x = 14304;
    std::cout << 14304;
    for (int i = 0; i < 14304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14304(int& val) {
    const int x = 14304;
    std::cout << 14304;
    for (int i = 0; i < 14304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14305(int& val) {
    func_in_14305(val);
    const int x = 14305;
    std::cout << 14305;
    for (int i = 0; i < 14305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14305(int& val) {
    const int x = 14305;
    std::cout << 14305;
    for (int i = 0; i < 14305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14306(int& val) {
    func_in_14306(val);
    const int x = 14306;
    std::cout << 14306;
    for (int i = 0; i < 14306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14306(int& val) {
    const int x = 14306;
    std::cout << 14306;
    for (int i = 0; i < 14306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14307(int& val) {
    func_in_14307(val);
    const int x = 14307;
    std::cout << 14307;
    for (int i = 0; i < 14307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14307(int& val) {
    const int x = 14307;
    std::cout << 14307;
    for (int i = 0; i < 14307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14308(int& val) {
    func_in_14308(val);
    const int x = 14308;
    std::cout << 14308;
    for (int i = 0; i < 14308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14308(int& val) {
    const int x = 14308;
    std::cout << 14308;
    for (int i = 0; i < 14308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14309(int& val) {
    func_in_14309(val);
    const int x = 14309;
    std::cout << 14309;
    for (int i = 0; i < 14309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14309(int& val) {
    const int x = 14309;
    std::cout << 14309;
    for (int i = 0; i < 14309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14310(int& val) {
    func_in_14310(val);
    const int x = 14310;
    std::cout << 14310;
    for (int i = 0; i < 14310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14310(int& val) {
    const int x = 14310;
    std::cout << 14310;
    for (int i = 0; i < 14310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14311(int& val) {
    func_in_14311(val);
    const int x = 14311;
    std::cout << 14311;
    for (int i = 0; i < 14311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14311(int& val) {
    const int x = 14311;
    std::cout << 14311;
    for (int i = 0; i < 14311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14312(int& val) {
    func_in_14312(val);
    const int x = 14312;
    std::cout << 14312;
    for (int i = 0; i < 14312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14312(int& val) {
    const int x = 14312;
    std::cout << 14312;
    for (int i = 0; i < 14312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14313(int& val) {
    func_in_14313(val);
    const int x = 14313;
    std::cout << 14313;
    for (int i = 0; i < 14313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14313(int& val) {
    const int x = 14313;
    std::cout << 14313;
    for (int i = 0; i < 14313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14314(int& val) {
    func_in_14314(val);
    const int x = 14314;
    std::cout << 14314;
    for (int i = 0; i < 14314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14314(int& val) {
    const int x = 14314;
    std::cout << 14314;
    for (int i = 0; i < 14314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14315(int& val) {
    func_in_14315(val);
    const int x = 14315;
    std::cout << 14315;
    for (int i = 0; i < 14315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14315(int& val) {
    const int x = 14315;
    std::cout << 14315;
    for (int i = 0; i < 14315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14316(int& val) {
    func_in_14316(val);
    const int x = 14316;
    std::cout << 14316;
    for (int i = 0; i < 14316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14316(int& val) {
    const int x = 14316;
    std::cout << 14316;
    for (int i = 0; i < 14316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14317(int& val) {
    func_in_14317(val);
    const int x = 14317;
    std::cout << 14317;
    for (int i = 0; i < 14317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14317(int& val) {
    const int x = 14317;
    std::cout << 14317;
    for (int i = 0; i < 14317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14318(int& val) {
    func_in_14318(val);
    const int x = 14318;
    std::cout << 14318;
    for (int i = 0; i < 14318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14318(int& val) {
    const int x = 14318;
    std::cout << 14318;
    for (int i = 0; i < 14318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14319(int& val) {
    func_in_14319(val);
    const int x = 14319;
    std::cout << 14319;
    for (int i = 0; i < 14319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14319(int& val) {
    const int x = 14319;
    std::cout << 14319;
    for (int i = 0; i < 14319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14320(int& val) {
    func_in_14320(val);
    const int x = 14320;
    std::cout << 14320;
    for (int i = 0; i < 14320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14320(int& val) {
    const int x = 14320;
    std::cout << 14320;
    for (int i = 0; i < 14320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14321(int& val) {
    func_in_14321(val);
    const int x = 14321;
    std::cout << 14321;
    for (int i = 0; i < 14321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14321(int& val) {
    const int x = 14321;
    std::cout << 14321;
    for (int i = 0; i < 14321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14322(int& val) {
    func_in_14322(val);
    const int x = 14322;
    std::cout << 14322;
    for (int i = 0; i < 14322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14322(int& val) {
    const int x = 14322;
    std::cout << 14322;
    for (int i = 0; i < 14322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14323(int& val) {
    func_in_14323(val);
    const int x = 14323;
    std::cout << 14323;
    for (int i = 0; i < 14323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14323(int& val) {
    const int x = 14323;
    std::cout << 14323;
    for (int i = 0; i < 14323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14324(int& val) {
    func_in_14324(val);
    const int x = 14324;
    std::cout << 14324;
    for (int i = 0; i < 14324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14324(int& val) {
    const int x = 14324;
    std::cout << 14324;
    for (int i = 0; i < 14324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14325(int& val) {
    func_in_14325(val);
    const int x = 14325;
    std::cout << 14325;
    for (int i = 0; i < 14325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14325(int& val) {
    const int x = 14325;
    std::cout << 14325;
    for (int i = 0; i < 14325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14326(int& val) {
    func_in_14326(val);
    const int x = 14326;
    std::cout << 14326;
    for (int i = 0; i < 14326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14326(int& val) {
    const int x = 14326;
    std::cout << 14326;
    for (int i = 0; i < 14326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14327(int& val) {
    func_in_14327(val);
    const int x = 14327;
    std::cout << 14327;
    for (int i = 0; i < 14327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14327(int& val) {
    const int x = 14327;
    std::cout << 14327;
    for (int i = 0; i < 14327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14328(int& val) {
    func_in_14328(val);
    const int x = 14328;
    std::cout << 14328;
    for (int i = 0; i < 14328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14328(int& val) {
    const int x = 14328;
    std::cout << 14328;
    for (int i = 0; i < 14328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14329(int& val) {
    func_in_14329(val);
    const int x = 14329;
    std::cout << 14329;
    for (int i = 0; i < 14329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14329(int& val) {
    const int x = 14329;
    std::cout << 14329;
    for (int i = 0; i < 14329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14330(int& val) {
    func_in_14330(val);
    const int x = 14330;
    std::cout << 14330;
    for (int i = 0; i < 14330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14330(int& val) {
    const int x = 14330;
    std::cout << 14330;
    for (int i = 0; i < 14330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14331(int& val) {
    func_in_14331(val);
    const int x = 14331;
    std::cout << 14331;
    for (int i = 0; i < 14331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14331(int& val) {
    const int x = 14331;
    std::cout << 14331;
    for (int i = 0; i < 14331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14332(int& val) {
    func_in_14332(val);
    const int x = 14332;
    std::cout << 14332;
    for (int i = 0; i < 14332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14332(int& val) {
    const int x = 14332;
    std::cout << 14332;
    for (int i = 0; i < 14332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14333(int& val) {
    func_in_14333(val);
    const int x = 14333;
    std::cout << 14333;
    for (int i = 0; i < 14333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14333(int& val) {
    const int x = 14333;
    std::cout << 14333;
    for (int i = 0; i < 14333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14334(int& val) {
    func_in_14334(val);
    const int x = 14334;
    std::cout << 14334;
    for (int i = 0; i < 14334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14334(int& val) {
    const int x = 14334;
    std::cout << 14334;
    for (int i = 0; i < 14334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14335(int& val) {
    func_in_14335(val);
    const int x = 14335;
    std::cout << 14335;
    for (int i = 0; i < 14335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14335(int& val) {
    const int x = 14335;
    std::cout << 14335;
    for (int i = 0; i < 14335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14336(int& val) {
    func_in_14336(val);
    const int x = 14336;
    std::cout << 14336;
    for (int i = 0; i < 14336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14336(int& val) {
    const int x = 14336;
    std::cout << 14336;
    for (int i = 0; i < 14336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14337(int& val) {
    func_in_14337(val);
    const int x = 14337;
    std::cout << 14337;
    for (int i = 0; i < 14337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14337(int& val) {
    const int x = 14337;
    std::cout << 14337;
    for (int i = 0; i < 14337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14338(int& val) {
    func_in_14338(val);
    const int x = 14338;
    std::cout << 14338;
    for (int i = 0; i < 14338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14338(int& val) {
    const int x = 14338;
    std::cout << 14338;
    for (int i = 0; i < 14338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14339(int& val) {
    func_in_14339(val);
    const int x = 14339;
    std::cout << 14339;
    for (int i = 0; i < 14339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14339(int& val) {
    const int x = 14339;
    std::cout << 14339;
    for (int i = 0; i < 14339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14340(int& val) {
    func_in_14340(val);
    const int x = 14340;
    std::cout << 14340;
    for (int i = 0; i < 14340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14340(int& val) {
    const int x = 14340;
    std::cout << 14340;
    for (int i = 0; i < 14340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14341(int& val) {
    func_in_14341(val);
    const int x = 14341;
    std::cout << 14341;
    for (int i = 0; i < 14341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14341(int& val) {
    const int x = 14341;
    std::cout << 14341;
    for (int i = 0; i < 14341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14342(int& val) {
    func_in_14342(val);
    const int x = 14342;
    std::cout << 14342;
    for (int i = 0; i < 14342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14342(int& val) {
    const int x = 14342;
    std::cout << 14342;
    for (int i = 0; i < 14342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14343(int& val) {
    func_in_14343(val);
    const int x = 14343;
    std::cout << 14343;
    for (int i = 0; i < 14343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14343(int& val) {
    const int x = 14343;
    std::cout << 14343;
    for (int i = 0; i < 14343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14344(int& val) {
    func_in_14344(val);
    const int x = 14344;
    std::cout << 14344;
    for (int i = 0; i < 14344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14344(int& val) {
    const int x = 14344;
    std::cout << 14344;
    for (int i = 0; i < 14344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14345(int& val) {
    func_in_14345(val);
    const int x = 14345;
    std::cout << 14345;
    for (int i = 0; i < 14345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14345(int& val) {
    const int x = 14345;
    std::cout << 14345;
    for (int i = 0; i < 14345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14346(int& val) {
    func_in_14346(val);
    const int x = 14346;
    std::cout << 14346;
    for (int i = 0; i < 14346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14346(int& val) {
    const int x = 14346;
    std::cout << 14346;
    for (int i = 0; i < 14346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14347(int& val) {
    func_in_14347(val);
    const int x = 14347;
    std::cout << 14347;
    for (int i = 0; i < 14347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14347(int& val) {
    const int x = 14347;
    std::cout << 14347;
    for (int i = 0; i < 14347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14348(int& val) {
    func_in_14348(val);
    const int x = 14348;
    std::cout << 14348;
    for (int i = 0; i < 14348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14348(int& val) {
    const int x = 14348;
    std::cout << 14348;
    for (int i = 0; i < 14348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14349(int& val) {
    func_in_14349(val);
    const int x = 14349;
    std::cout << 14349;
    for (int i = 0; i < 14349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14349(int& val) {
    const int x = 14349;
    std::cout << 14349;
    for (int i = 0; i < 14349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14350(int& val) {
    func_in_14350(val);
    const int x = 14350;
    std::cout << 14350;
    for (int i = 0; i < 14350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14350(int& val) {
    const int x = 14350;
    std::cout << 14350;
    for (int i = 0; i < 14350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14351(int& val) {
    func_in_14351(val);
    const int x = 14351;
    std::cout << 14351;
    for (int i = 0; i < 14351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14351(int& val) {
    const int x = 14351;
    std::cout << 14351;
    for (int i = 0; i < 14351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14352(int& val) {
    func_in_14352(val);
    const int x = 14352;
    std::cout << 14352;
    for (int i = 0; i < 14352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14352(int& val) {
    const int x = 14352;
    std::cout << 14352;
    for (int i = 0; i < 14352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14353(int& val) {
    func_in_14353(val);
    const int x = 14353;
    std::cout << 14353;
    for (int i = 0; i < 14353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14353(int& val) {
    const int x = 14353;
    std::cout << 14353;
    for (int i = 0; i < 14353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14354(int& val) {
    func_in_14354(val);
    const int x = 14354;
    std::cout << 14354;
    for (int i = 0; i < 14354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14354(int& val) {
    const int x = 14354;
    std::cout << 14354;
    for (int i = 0; i < 14354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14355(int& val) {
    func_in_14355(val);
    const int x = 14355;
    std::cout << 14355;
    for (int i = 0; i < 14355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14355(int& val) {
    const int x = 14355;
    std::cout << 14355;
    for (int i = 0; i < 14355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14356(int& val) {
    func_in_14356(val);
    const int x = 14356;
    std::cout << 14356;
    for (int i = 0; i < 14356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14356(int& val) {
    const int x = 14356;
    std::cout << 14356;
    for (int i = 0; i < 14356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14357(int& val) {
    func_in_14357(val);
    const int x = 14357;
    std::cout << 14357;
    for (int i = 0; i < 14357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14357(int& val) {
    const int x = 14357;
    std::cout << 14357;
    for (int i = 0; i < 14357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14358(int& val) {
    func_in_14358(val);
    const int x = 14358;
    std::cout << 14358;
    for (int i = 0; i < 14358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14358(int& val) {
    const int x = 14358;
    std::cout << 14358;
    for (int i = 0; i < 14358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14359(int& val) {
    func_in_14359(val);
    const int x = 14359;
    std::cout << 14359;
    for (int i = 0; i < 14359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14359(int& val) {
    const int x = 14359;
    std::cout << 14359;
    for (int i = 0; i < 14359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14360(int& val) {
    func_in_14360(val);
    const int x = 14360;
    std::cout << 14360;
    for (int i = 0; i < 14360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14360(int& val) {
    const int x = 14360;
    std::cout << 14360;
    for (int i = 0; i < 14360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14361(int& val) {
    func_in_14361(val);
    const int x = 14361;
    std::cout << 14361;
    for (int i = 0; i < 14361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14361(int& val) {
    const int x = 14361;
    std::cout << 14361;
    for (int i = 0; i < 14361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14362(int& val) {
    func_in_14362(val);
    const int x = 14362;
    std::cout << 14362;
    for (int i = 0; i < 14362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14362(int& val) {
    const int x = 14362;
    std::cout << 14362;
    for (int i = 0; i < 14362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14363(int& val) {
    func_in_14363(val);
    const int x = 14363;
    std::cout << 14363;
    for (int i = 0; i < 14363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14363(int& val) {
    const int x = 14363;
    std::cout << 14363;
    for (int i = 0; i < 14363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14364(int& val) {
    func_in_14364(val);
    const int x = 14364;
    std::cout << 14364;
    for (int i = 0; i < 14364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14364(int& val) {
    const int x = 14364;
    std::cout << 14364;
    for (int i = 0; i < 14364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14365(int& val) {
    func_in_14365(val);
    const int x = 14365;
    std::cout << 14365;
    for (int i = 0; i < 14365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14365(int& val) {
    const int x = 14365;
    std::cout << 14365;
    for (int i = 0; i < 14365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14366(int& val) {
    func_in_14366(val);
    const int x = 14366;
    std::cout << 14366;
    for (int i = 0; i < 14366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14366(int& val) {
    const int x = 14366;
    std::cout << 14366;
    for (int i = 0; i < 14366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14367(int& val) {
    func_in_14367(val);
    const int x = 14367;
    std::cout << 14367;
    for (int i = 0; i < 14367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14367(int& val) {
    const int x = 14367;
    std::cout << 14367;
    for (int i = 0; i < 14367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14368(int& val) {
    func_in_14368(val);
    const int x = 14368;
    std::cout << 14368;
    for (int i = 0; i < 14368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14368(int& val) {
    const int x = 14368;
    std::cout << 14368;
    for (int i = 0; i < 14368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14369(int& val) {
    func_in_14369(val);
    const int x = 14369;
    std::cout << 14369;
    for (int i = 0; i < 14369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14369(int& val) {
    const int x = 14369;
    std::cout << 14369;
    for (int i = 0; i < 14369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14370(int& val) {
    func_in_14370(val);
    const int x = 14370;
    std::cout << 14370;
    for (int i = 0; i < 14370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14370(int& val) {
    const int x = 14370;
    std::cout << 14370;
    for (int i = 0; i < 14370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14371(int& val) {
    func_in_14371(val);
    const int x = 14371;
    std::cout << 14371;
    for (int i = 0; i < 14371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14371(int& val) {
    const int x = 14371;
    std::cout << 14371;
    for (int i = 0; i < 14371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14372(int& val) {
    func_in_14372(val);
    const int x = 14372;
    std::cout << 14372;
    for (int i = 0; i < 14372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14372(int& val) {
    const int x = 14372;
    std::cout << 14372;
    for (int i = 0; i < 14372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14373(int& val) {
    func_in_14373(val);
    const int x = 14373;
    std::cout << 14373;
    for (int i = 0; i < 14373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14373(int& val) {
    const int x = 14373;
    std::cout << 14373;
    for (int i = 0; i < 14373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14374(int& val) {
    func_in_14374(val);
    const int x = 14374;
    std::cout << 14374;
    for (int i = 0; i < 14374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14374(int& val) {
    const int x = 14374;
    std::cout << 14374;
    for (int i = 0; i < 14374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14375(int& val) {
    func_in_14375(val);
    const int x = 14375;
    std::cout << 14375;
    for (int i = 0; i < 14375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14375(int& val) {
    const int x = 14375;
    std::cout << 14375;
    for (int i = 0; i < 14375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14376(int& val) {
    func_in_14376(val);
    const int x = 14376;
    std::cout << 14376;
    for (int i = 0; i < 14376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14376(int& val) {
    const int x = 14376;
    std::cout << 14376;
    for (int i = 0; i < 14376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14377(int& val) {
    func_in_14377(val);
    const int x = 14377;
    std::cout << 14377;
    for (int i = 0; i < 14377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14377(int& val) {
    const int x = 14377;
    std::cout << 14377;
    for (int i = 0; i < 14377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14378(int& val) {
    func_in_14378(val);
    const int x = 14378;
    std::cout << 14378;
    for (int i = 0; i < 14378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14378(int& val) {
    const int x = 14378;
    std::cout << 14378;
    for (int i = 0; i < 14378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14379(int& val) {
    func_in_14379(val);
    const int x = 14379;
    std::cout << 14379;
    for (int i = 0; i < 14379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14379(int& val) {
    const int x = 14379;
    std::cout << 14379;
    for (int i = 0; i < 14379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14380(int& val) {
    func_in_14380(val);
    const int x = 14380;
    std::cout << 14380;
    for (int i = 0; i < 14380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14380(int& val) {
    const int x = 14380;
    std::cout << 14380;
    for (int i = 0; i < 14380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14381(int& val) {
    func_in_14381(val);
    const int x = 14381;
    std::cout << 14381;
    for (int i = 0; i < 14381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14381(int& val) {
    const int x = 14381;
    std::cout << 14381;
    for (int i = 0; i < 14381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14382(int& val) {
    func_in_14382(val);
    const int x = 14382;
    std::cout << 14382;
    for (int i = 0; i < 14382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14382(int& val) {
    const int x = 14382;
    std::cout << 14382;
    for (int i = 0; i < 14382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14383(int& val) {
    func_in_14383(val);
    const int x = 14383;
    std::cout << 14383;
    for (int i = 0; i < 14383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14383(int& val) {
    const int x = 14383;
    std::cout << 14383;
    for (int i = 0; i < 14383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14384(int& val) {
    func_in_14384(val);
    const int x = 14384;
    std::cout << 14384;
    for (int i = 0; i < 14384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14384(int& val) {
    const int x = 14384;
    std::cout << 14384;
    for (int i = 0; i < 14384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14385(int& val) {
    func_in_14385(val);
    const int x = 14385;
    std::cout << 14385;
    for (int i = 0; i < 14385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14385(int& val) {
    const int x = 14385;
    std::cout << 14385;
    for (int i = 0; i < 14385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14386(int& val) {
    func_in_14386(val);
    const int x = 14386;
    std::cout << 14386;
    for (int i = 0; i < 14386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14386(int& val) {
    const int x = 14386;
    std::cout << 14386;
    for (int i = 0; i < 14386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14387(int& val) {
    func_in_14387(val);
    const int x = 14387;
    std::cout << 14387;
    for (int i = 0; i < 14387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14387(int& val) {
    const int x = 14387;
    std::cout << 14387;
    for (int i = 0; i < 14387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14388(int& val) {
    func_in_14388(val);
    const int x = 14388;
    std::cout << 14388;
    for (int i = 0; i < 14388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14388(int& val) {
    const int x = 14388;
    std::cout << 14388;
    for (int i = 0; i < 14388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14389(int& val) {
    func_in_14389(val);
    const int x = 14389;
    std::cout << 14389;
    for (int i = 0; i < 14389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14389(int& val) {
    const int x = 14389;
    std::cout << 14389;
    for (int i = 0; i < 14389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14390(int& val) {
    func_in_14390(val);
    const int x = 14390;
    std::cout << 14390;
    for (int i = 0; i < 14390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14390(int& val) {
    const int x = 14390;
    std::cout << 14390;
    for (int i = 0; i < 14390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14391(int& val) {
    func_in_14391(val);
    const int x = 14391;
    std::cout << 14391;
    for (int i = 0; i < 14391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14391(int& val) {
    const int x = 14391;
    std::cout << 14391;
    for (int i = 0; i < 14391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14392(int& val) {
    func_in_14392(val);
    const int x = 14392;
    std::cout << 14392;
    for (int i = 0; i < 14392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14392(int& val) {
    const int x = 14392;
    std::cout << 14392;
    for (int i = 0; i < 14392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14393(int& val) {
    func_in_14393(val);
    const int x = 14393;
    std::cout << 14393;
    for (int i = 0; i < 14393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14393(int& val) {
    const int x = 14393;
    std::cout << 14393;
    for (int i = 0; i < 14393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14394(int& val) {
    func_in_14394(val);
    const int x = 14394;
    std::cout << 14394;
    for (int i = 0; i < 14394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14394(int& val) {
    const int x = 14394;
    std::cout << 14394;
    for (int i = 0; i < 14394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14395(int& val) {
    func_in_14395(val);
    const int x = 14395;
    std::cout << 14395;
    for (int i = 0; i < 14395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14395(int& val) {
    const int x = 14395;
    std::cout << 14395;
    for (int i = 0; i < 14395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14396(int& val) {
    func_in_14396(val);
    const int x = 14396;
    std::cout << 14396;
    for (int i = 0; i < 14396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14396(int& val) {
    const int x = 14396;
    std::cout << 14396;
    for (int i = 0; i < 14396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14397(int& val) {
    func_in_14397(val);
    const int x = 14397;
    std::cout << 14397;
    for (int i = 0; i < 14397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14397(int& val) {
    const int x = 14397;
    std::cout << 14397;
    for (int i = 0; i < 14397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14398(int& val) {
    func_in_14398(val);
    const int x = 14398;
    std::cout << 14398;
    for (int i = 0; i < 14398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14398(int& val) {
    const int x = 14398;
    std::cout << 14398;
    for (int i = 0; i < 14398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14399(int& val) {
    func_in_14399(val);
    const int x = 14399;
    std::cout << 14399;
    for (int i = 0; i < 14399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14399(int& val) {
    const int x = 14399;
    std::cout << 14399;
    for (int i = 0; i < 14399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14400(int& val) {
    func_in_14400(val);
    const int x = 14400;
    std::cout << 14400;
    for (int i = 0; i < 14400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14400(int& val) {
    const int x = 14400;
    std::cout << 14400;
    for (int i = 0; i < 14400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14401(int& val) {
    func_in_14401(val);
    const int x = 14401;
    std::cout << 14401;
    for (int i = 0; i < 14401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14401(int& val) {
    const int x = 14401;
    std::cout << 14401;
    for (int i = 0; i < 14401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14402(int& val) {
    func_in_14402(val);
    const int x = 14402;
    std::cout << 14402;
    for (int i = 0; i < 14402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14402(int& val) {
    const int x = 14402;
    std::cout << 14402;
    for (int i = 0; i < 14402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14403(int& val) {
    func_in_14403(val);
    const int x = 14403;
    std::cout << 14403;
    for (int i = 0; i < 14403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14403(int& val) {
    const int x = 14403;
    std::cout << 14403;
    for (int i = 0; i < 14403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14404(int& val) {
    func_in_14404(val);
    const int x = 14404;
    std::cout << 14404;
    for (int i = 0; i < 14404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14404(int& val) {
    const int x = 14404;
    std::cout << 14404;
    for (int i = 0; i < 14404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14405(int& val) {
    func_in_14405(val);
    const int x = 14405;
    std::cout << 14405;
    for (int i = 0; i < 14405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14405(int& val) {
    const int x = 14405;
    std::cout << 14405;
    for (int i = 0; i < 14405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14406(int& val) {
    func_in_14406(val);
    const int x = 14406;
    std::cout << 14406;
    for (int i = 0; i < 14406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14406(int& val) {
    const int x = 14406;
    std::cout << 14406;
    for (int i = 0; i < 14406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14407(int& val) {
    func_in_14407(val);
    const int x = 14407;
    std::cout << 14407;
    for (int i = 0; i < 14407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14407(int& val) {
    const int x = 14407;
    std::cout << 14407;
    for (int i = 0; i < 14407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14408(int& val) {
    func_in_14408(val);
    const int x = 14408;
    std::cout << 14408;
    for (int i = 0; i < 14408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14408(int& val) {
    const int x = 14408;
    std::cout << 14408;
    for (int i = 0; i < 14408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14409(int& val) {
    func_in_14409(val);
    const int x = 14409;
    std::cout << 14409;
    for (int i = 0; i < 14409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14409(int& val) {
    const int x = 14409;
    std::cout << 14409;
    for (int i = 0; i < 14409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14410(int& val) {
    func_in_14410(val);
    const int x = 14410;
    std::cout << 14410;
    for (int i = 0; i < 14410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14410(int& val) {
    const int x = 14410;
    std::cout << 14410;
    for (int i = 0; i < 14410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14411(int& val) {
    func_in_14411(val);
    const int x = 14411;
    std::cout << 14411;
    for (int i = 0; i < 14411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14411(int& val) {
    const int x = 14411;
    std::cout << 14411;
    for (int i = 0; i < 14411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14412(int& val) {
    func_in_14412(val);
    const int x = 14412;
    std::cout << 14412;
    for (int i = 0; i < 14412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14412(int& val) {
    const int x = 14412;
    std::cout << 14412;
    for (int i = 0; i < 14412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14413(int& val) {
    func_in_14413(val);
    const int x = 14413;
    std::cout << 14413;
    for (int i = 0; i < 14413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14413(int& val) {
    const int x = 14413;
    std::cout << 14413;
    for (int i = 0; i < 14413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14414(int& val) {
    func_in_14414(val);
    const int x = 14414;
    std::cout << 14414;
    for (int i = 0; i < 14414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14414(int& val) {
    const int x = 14414;
    std::cout << 14414;
    for (int i = 0; i < 14414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14415(int& val) {
    func_in_14415(val);
    const int x = 14415;
    std::cout << 14415;
    for (int i = 0; i < 14415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14415(int& val) {
    const int x = 14415;
    std::cout << 14415;
    for (int i = 0; i < 14415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14416(int& val) {
    func_in_14416(val);
    const int x = 14416;
    std::cout << 14416;
    for (int i = 0; i < 14416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14416(int& val) {
    const int x = 14416;
    std::cout << 14416;
    for (int i = 0; i < 14416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14417(int& val) {
    func_in_14417(val);
    const int x = 14417;
    std::cout << 14417;
    for (int i = 0; i < 14417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14417(int& val) {
    const int x = 14417;
    std::cout << 14417;
    for (int i = 0; i < 14417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14418(int& val) {
    func_in_14418(val);
    const int x = 14418;
    std::cout << 14418;
    for (int i = 0; i < 14418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14418(int& val) {
    const int x = 14418;
    std::cout << 14418;
    for (int i = 0; i < 14418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14419(int& val) {
    func_in_14419(val);
    const int x = 14419;
    std::cout << 14419;
    for (int i = 0; i < 14419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14419(int& val) {
    const int x = 14419;
    std::cout << 14419;
    for (int i = 0; i < 14419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14420(int& val) {
    func_in_14420(val);
    const int x = 14420;
    std::cout << 14420;
    for (int i = 0; i < 14420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14420(int& val) {
    const int x = 14420;
    std::cout << 14420;
    for (int i = 0; i < 14420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14421(int& val) {
    func_in_14421(val);
    const int x = 14421;
    std::cout << 14421;
    for (int i = 0; i < 14421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14421(int& val) {
    const int x = 14421;
    std::cout << 14421;
    for (int i = 0; i < 14421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14422(int& val) {
    func_in_14422(val);
    const int x = 14422;
    std::cout << 14422;
    for (int i = 0; i < 14422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14422(int& val) {
    const int x = 14422;
    std::cout << 14422;
    for (int i = 0; i < 14422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14423(int& val) {
    func_in_14423(val);
    const int x = 14423;
    std::cout << 14423;
    for (int i = 0; i < 14423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14423(int& val) {
    const int x = 14423;
    std::cout << 14423;
    for (int i = 0; i < 14423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14424(int& val) {
    func_in_14424(val);
    const int x = 14424;
    std::cout << 14424;
    for (int i = 0; i < 14424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14424(int& val) {
    const int x = 14424;
    std::cout << 14424;
    for (int i = 0; i < 14424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14425(int& val) {
    func_in_14425(val);
    const int x = 14425;
    std::cout << 14425;
    for (int i = 0; i < 14425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14425(int& val) {
    const int x = 14425;
    std::cout << 14425;
    for (int i = 0; i < 14425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14426(int& val) {
    func_in_14426(val);
    const int x = 14426;
    std::cout << 14426;
    for (int i = 0; i < 14426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14426(int& val) {
    const int x = 14426;
    std::cout << 14426;
    for (int i = 0; i < 14426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14427(int& val) {
    func_in_14427(val);
    const int x = 14427;
    std::cout << 14427;
    for (int i = 0; i < 14427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14427(int& val) {
    const int x = 14427;
    std::cout << 14427;
    for (int i = 0; i < 14427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14428(int& val) {
    func_in_14428(val);
    const int x = 14428;
    std::cout << 14428;
    for (int i = 0; i < 14428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14428(int& val) {
    const int x = 14428;
    std::cout << 14428;
    for (int i = 0; i < 14428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14429(int& val) {
    func_in_14429(val);
    const int x = 14429;
    std::cout << 14429;
    for (int i = 0; i < 14429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14429(int& val) {
    const int x = 14429;
    std::cout << 14429;
    for (int i = 0; i < 14429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14430(int& val) {
    func_in_14430(val);
    const int x = 14430;
    std::cout << 14430;
    for (int i = 0; i < 14430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14430(int& val) {
    const int x = 14430;
    std::cout << 14430;
    for (int i = 0; i < 14430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14431(int& val) {
    func_in_14431(val);
    const int x = 14431;
    std::cout << 14431;
    for (int i = 0; i < 14431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14431(int& val) {
    const int x = 14431;
    std::cout << 14431;
    for (int i = 0; i < 14431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14432(int& val) {
    func_in_14432(val);
    const int x = 14432;
    std::cout << 14432;
    for (int i = 0; i < 14432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14432(int& val) {
    const int x = 14432;
    std::cout << 14432;
    for (int i = 0; i < 14432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14433(int& val) {
    func_in_14433(val);
    const int x = 14433;
    std::cout << 14433;
    for (int i = 0; i < 14433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14433(int& val) {
    const int x = 14433;
    std::cout << 14433;
    for (int i = 0; i < 14433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14434(int& val) {
    func_in_14434(val);
    const int x = 14434;
    std::cout << 14434;
    for (int i = 0; i < 14434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14434(int& val) {
    const int x = 14434;
    std::cout << 14434;
    for (int i = 0; i < 14434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14435(int& val) {
    func_in_14435(val);
    const int x = 14435;
    std::cout << 14435;
    for (int i = 0; i < 14435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14435(int& val) {
    const int x = 14435;
    std::cout << 14435;
    for (int i = 0; i < 14435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14436(int& val) {
    func_in_14436(val);
    const int x = 14436;
    std::cout << 14436;
    for (int i = 0; i < 14436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14436(int& val) {
    const int x = 14436;
    std::cout << 14436;
    for (int i = 0; i < 14436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14437(int& val) {
    func_in_14437(val);
    const int x = 14437;
    std::cout << 14437;
    for (int i = 0; i < 14437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14437(int& val) {
    const int x = 14437;
    std::cout << 14437;
    for (int i = 0; i < 14437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14438(int& val) {
    func_in_14438(val);
    const int x = 14438;
    std::cout << 14438;
    for (int i = 0; i < 14438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14438(int& val) {
    const int x = 14438;
    std::cout << 14438;
    for (int i = 0; i < 14438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14439(int& val) {
    func_in_14439(val);
    const int x = 14439;
    std::cout << 14439;
    for (int i = 0; i < 14439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14439(int& val) {
    const int x = 14439;
    std::cout << 14439;
    for (int i = 0; i < 14439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14440(int& val) {
    func_in_14440(val);
    const int x = 14440;
    std::cout << 14440;
    for (int i = 0; i < 14440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14440(int& val) {
    const int x = 14440;
    std::cout << 14440;
    for (int i = 0; i < 14440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14441(int& val) {
    func_in_14441(val);
    const int x = 14441;
    std::cout << 14441;
    for (int i = 0; i < 14441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14441(int& val) {
    const int x = 14441;
    std::cout << 14441;
    for (int i = 0; i < 14441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14442(int& val) {
    func_in_14442(val);
    const int x = 14442;
    std::cout << 14442;
    for (int i = 0; i < 14442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14442(int& val) {
    const int x = 14442;
    std::cout << 14442;
    for (int i = 0; i < 14442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14443(int& val) {
    func_in_14443(val);
    const int x = 14443;
    std::cout << 14443;
    for (int i = 0; i < 14443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14443(int& val) {
    const int x = 14443;
    std::cout << 14443;
    for (int i = 0; i < 14443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14444(int& val) {
    func_in_14444(val);
    const int x = 14444;
    std::cout << 14444;
    for (int i = 0; i < 14444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14444(int& val) {
    const int x = 14444;
    std::cout << 14444;
    for (int i = 0; i < 14444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14445(int& val) {
    func_in_14445(val);
    const int x = 14445;
    std::cout << 14445;
    for (int i = 0; i < 14445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14445(int& val) {
    const int x = 14445;
    std::cout << 14445;
    for (int i = 0; i < 14445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14446(int& val) {
    func_in_14446(val);
    const int x = 14446;
    std::cout << 14446;
    for (int i = 0; i < 14446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14446(int& val) {
    const int x = 14446;
    std::cout << 14446;
    for (int i = 0; i < 14446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14447(int& val) {
    func_in_14447(val);
    const int x = 14447;
    std::cout << 14447;
    for (int i = 0; i < 14447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14447(int& val) {
    const int x = 14447;
    std::cout << 14447;
    for (int i = 0; i < 14447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14448(int& val) {
    func_in_14448(val);
    const int x = 14448;
    std::cout << 14448;
    for (int i = 0; i < 14448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14448(int& val) {
    const int x = 14448;
    std::cout << 14448;
    for (int i = 0; i < 14448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14449(int& val) {
    func_in_14449(val);
    const int x = 14449;
    std::cout << 14449;
    for (int i = 0; i < 14449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14449(int& val) {
    const int x = 14449;
    std::cout << 14449;
    for (int i = 0; i < 14449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14450(int& val) {
    func_in_14450(val);
    const int x = 14450;
    std::cout << 14450;
    for (int i = 0; i < 14450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14450(int& val) {
    const int x = 14450;
    std::cout << 14450;
    for (int i = 0; i < 14450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14451(int& val) {
    func_in_14451(val);
    const int x = 14451;
    std::cout << 14451;
    for (int i = 0; i < 14451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14451(int& val) {
    const int x = 14451;
    std::cout << 14451;
    for (int i = 0; i < 14451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14452(int& val) {
    func_in_14452(val);
    const int x = 14452;
    std::cout << 14452;
    for (int i = 0; i < 14452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14452(int& val) {
    const int x = 14452;
    std::cout << 14452;
    for (int i = 0; i < 14452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14453(int& val) {
    func_in_14453(val);
    const int x = 14453;
    std::cout << 14453;
    for (int i = 0; i < 14453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14453(int& val) {
    const int x = 14453;
    std::cout << 14453;
    for (int i = 0; i < 14453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14454(int& val) {
    func_in_14454(val);
    const int x = 14454;
    std::cout << 14454;
    for (int i = 0; i < 14454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14454(int& val) {
    const int x = 14454;
    std::cout << 14454;
    for (int i = 0; i < 14454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14455(int& val) {
    func_in_14455(val);
    const int x = 14455;
    std::cout << 14455;
    for (int i = 0; i < 14455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14455(int& val) {
    const int x = 14455;
    std::cout << 14455;
    for (int i = 0; i < 14455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14456(int& val) {
    func_in_14456(val);
    const int x = 14456;
    std::cout << 14456;
    for (int i = 0; i < 14456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14456(int& val) {
    const int x = 14456;
    std::cout << 14456;
    for (int i = 0; i < 14456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14457(int& val) {
    func_in_14457(val);
    const int x = 14457;
    std::cout << 14457;
    for (int i = 0; i < 14457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14457(int& val) {
    const int x = 14457;
    std::cout << 14457;
    for (int i = 0; i < 14457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14458(int& val) {
    func_in_14458(val);
    const int x = 14458;
    std::cout << 14458;
    for (int i = 0; i < 14458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14458(int& val) {
    const int x = 14458;
    std::cout << 14458;
    for (int i = 0; i < 14458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14459(int& val) {
    func_in_14459(val);
    const int x = 14459;
    std::cout << 14459;
    for (int i = 0; i < 14459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14459(int& val) {
    const int x = 14459;
    std::cout << 14459;
    for (int i = 0; i < 14459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14460(int& val) {
    func_in_14460(val);
    const int x = 14460;
    std::cout << 14460;
    for (int i = 0; i < 14460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14460(int& val) {
    const int x = 14460;
    std::cout << 14460;
    for (int i = 0; i < 14460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14461(int& val) {
    func_in_14461(val);
    const int x = 14461;
    std::cout << 14461;
    for (int i = 0; i < 14461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14461(int& val) {
    const int x = 14461;
    std::cout << 14461;
    for (int i = 0; i < 14461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14462(int& val) {
    func_in_14462(val);
    const int x = 14462;
    std::cout << 14462;
    for (int i = 0; i < 14462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14462(int& val) {
    const int x = 14462;
    std::cout << 14462;
    for (int i = 0; i < 14462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14463(int& val) {
    func_in_14463(val);
    const int x = 14463;
    std::cout << 14463;
    for (int i = 0; i < 14463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14463(int& val) {
    const int x = 14463;
    std::cout << 14463;
    for (int i = 0; i < 14463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14464(int& val) {
    func_in_14464(val);
    const int x = 14464;
    std::cout << 14464;
    for (int i = 0; i < 14464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14464(int& val) {
    const int x = 14464;
    std::cout << 14464;
    for (int i = 0; i < 14464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14465(int& val) {
    func_in_14465(val);
    const int x = 14465;
    std::cout << 14465;
    for (int i = 0; i < 14465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14465(int& val) {
    const int x = 14465;
    std::cout << 14465;
    for (int i = 0; i < 14465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14466(int& val) {
    func_in_14466(val);
    const int x = 14466;
    std::cout << 14466;
    for (int i = 0; i < 14466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14466(int& val) {
    const int x = 14466;
    std::cout << 14466;
    for (int i = 0; i < 14466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14467(int& val) {
    func_in_14467(val);
    const int x = 14467;
    std::cout << 14467;
    for (int i = 0; i < 14467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14467(int& val) {
    const int x = 14467;
    std::cout << 14467;
    for (int i = 0; i < 14467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14468(int& val) {
    func_in_14468(val);
    const int x = 14468;
    std::cout << 14468;
    for (int i = 0; i < 14468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14468(int& val) {
    const int x = 14468;
    std::cout << 14468;
    for (int i = 0; i < 14468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14469(int& val) {
    func_in_14469(val);
    const int x = 14469;
    std::cout << 14469;
    for (int i = 0; i < 14469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14469(int& val) {
    const int x = 14469;
    std::cout << 14469;
    for (int i = 0; i < 14469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14470(int& val) {
    func_in_14470(val);
    const int x = 14470;
    std::cout << 14470;
    for (int i = 0; i < 14470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14470(int& val) {
    const int x = 14470;
    std::cout << 14470;
    for (int i = 0; i < 14470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14471(int& val) {
    func_in_14471(val);
    const int x = 14471;
    std::cout << 14471;
    for (int i = 0; i < 14471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14471(int& val) {
    const int x = 14471;
    std::cout << 14471;
    for (int i = 0; i < 14471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14472(int& val) {
    func_in_14472(val);
    const int x = 14472;
    std::cout << 14472;
    for (int i = 0; i < 14472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14472(int& val) {
    const int x = 14472;
    std::cout << 14472;
    for (int i = 0; i < 14472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14473(int& val) {
    func_in_14473(val);
    const int x = 14473;
    std::cout << 14473;
    for (int i = 0; i < 14473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14473(int& val) {
    const int x = 14473;
    std::cout << 14473;
    for (int i = 0; i < 14473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14474(int& val) {
    func_in_14474(val);
    const int x = 14474;
    std::cout << 14474;
    for (int i = 0; i < 14474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14474(int& val) {
    const int x = 14474;
    std::cout << 14474;
    for (int i = 0; i < 14474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14475(int& val) {
    func_in_14475(val);
    const int x = 14475;
    std::cout << 14475;
    for (int i = 0; i < 14475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14475(int& val) {
    const int x = 14475;
    std::cout << 14475;
    for (int i = 0; i < 14475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14476(int& val) {
    func_in_14476(val);
    const int x = 14476;
    std::cout << 14476;
    for (int i = 0; i < 14476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14476(int& val) {
    const int x = 14476;
    std::cout << 14476;
    for (int i = 0; i < 14476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14477(int& val) {
    func_in_14477(val);
    const int x = 14477;
    std::cout << 14477;
    for (int i = 0; i < 14477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14477(int& val) {
    const int x = 14477;
    std::cout << 14477;
    for (int i = 0; i < 14477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14478(int& val) {
    func_in_14478(val);
    const int x = 14478;
    std::cout << 14478;
    for (int i = 0; i < 14478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14478(int& val) {
    const int x = 14478;
    std::cout << 14478;
    for (int i = 0; i < 14478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14479(int& val) {
    func_in_14479(val);
    const int x = 14479;
    std::cout << 14479;
    for (int i = 0; i < 14479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14479(int& val) {
    const int x = 14479;
    std::cout << 14479;
    for (int i = 0; i < 14479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14480(int& val) {
    func_in_14480(val);
    const int x = 14480;
    std::cout << 14480;
    for (int i = 0; i < 14480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14480(int& val) {
    const int x = 14480;
    std::cout << 14480;
    for (int i = 0; i < 14480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14481(int& val) {
    func_in_14481(val);
    const int x = 14481;
    std::cout << 14481;
    for (int i = 0; i < 14481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14481(int& val) {
    const int x = 14481;
    std::cout << 14481;
    for (int i = 0; i < 14481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14482(int& val) {
    func_in_14482(val);
    const int x = 14482;
    std::cout << 14482;
    for (int i = 0; i < 14482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14482(int& val) {
    const int x = 14482;
    std::cout << 14482;
    for (int i = 0; i < 14482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14483(int& val) {
    func_in_14483(val);
    const int x = 14483;
    std::cout << 14483;
    for (int i = 0; i < 14483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14483(int& val) {
    const int x = 14483;
    std::cout << 14483;
    for (int i = 0; i < 14483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14484(int& val) {
    func_in_14484(val);
    const int x = 14484;
    std::cout << 14484;
    for (int i = 0; i < 14484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14484(int& val) {
    const int x = 14484;
    std::cout << 14484;
    for (int i = 0; i < 14484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14485(int& val) {
    func_in_14485(val);
    const int x = 14485;
    std::cout << 14485;
    for (int i = 0; i < 14485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14485(int& val) {
    const int x = 14485;
    std::cout << 14485;
    for (int i = 0; i < 14485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14486(int& val) {
    func_in_14486(val);
    const int x = 14486;
    std::cout << 14486;
    for (int i = 0; i < 14486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14486(int& val) {
    const int x = 14486;
    std::cout << 14486;
    for (int i = 0; i < 14486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14487(int& val) {
    func_in_14487(val);
    const int x = 14487;
    std::cout << 14487;
    for (int i = 0; i < 14487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14487(int& val) {
    const int x = 14487;
    std::cout << 14487;
    for (int i = 0; i < 14487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14488(int& val) {
    func_in_14488(val);
    const int x = 14488;
    std::cout << 14488;
    for (int i = 0; i < 14488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14488(int& val) {
    const int x = 14488;
    std::cout << 14488;
    for (int i = 0; i < 14488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14489(int& val) {
    func_in_14489(val);
    const int x = 14489;
    std::cout << 14489;
    for (int i = 0; i < 14489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14489(int& val) {
    const int x = 14489;
    std::cout << 14489;
    for (int i = 0; i < 14489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14490(int& val) {
    func_in_14490(val);
    const int x = 14490;
    std::cout << 14490;
    for (int i = 0; i < 14490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14490(int& val) {
    const int x = 14490;
    std::cout << 14490;
    for (int i = 0; i < 14490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14491(int& val) {
    func_in_14491(val);
    const int x = 14491;
    std::cout << 14491;
    for (int i = 0; i < 14491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14491(int& val) {
    const int x = 14491;
    std::cout << 14491;
    for (int i = 0; i < 14491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14492(int& val) {
    func_in_14492(val);
    const int x = 14492;
    std::cout << 14492;
    for (int i = 0; i < 14492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14492(int& val) {
    const int x = 14492;
    std::cout << 14492;
    for (int i = 0; i < 14492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14493(int& val) {
    func_in_14493(val);
    const int x = 14493;
    std::cout << 14493;
    for (int i = 0; i < 14493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14493(int& val) {
    const int x = 14493;
    std::cout << 14493;
    for (int i = 0; i < 14493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14494(int& val) {
    func_in_14494(val);
    const int x = 14494;
    std::cout << 14494;
    for (int i = 0; i < 14494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14494(int& val) {
    const int x = 14494;
    std::cout << 14494;
    for (int i = 0; i < 14494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14495(int& val) {
    func_in_14495(val);
    const int x = 14495;
    std::cout << 14495;
    for (int i = 0; i < 14495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14495(int& val) {
    const int x = 14495;
    std::cout << 14495;
    for (int i = 0; i < 14495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14496(int& val) {
    func_in_14496(val);
    const int x = 14496;
    std::cout << 14496;
    for (int i = 0; i < 14496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14496(int& val) {
    const int x = 14496;
    std::cout << 14496;
    for (int i = 0; i < 14496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14497(int& val) {
    func_in_14497(val);
    const int x = 14497;
    std::cout << 14497;
    for (int i = 0; i < 14497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14497(int& val) {
    const int x = 14497;
    std::cout << 14497;
    for (int i = 0; i < 14497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14498(int& val) {
    func_in_14498(val);
    const int x = 14498;
    std::cout << 14498;
    for (int i = 0; i < 14498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14498(int& val) {
    const int x = 14498;
    std::cout << 14498;
    for (int i = 0; i < 14498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14499(int& val) {
    func_in_14499(val);
    const int x = 14499;
    std::cout << 14499;
    for (int i = 0; i < 14499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14499(int& val) {
    const int x = 14499;
    std::cout << 14499;
    for (int i = 0; i < 14499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14500(int& val) {
    func_in_14500(val);
    const int x = 14500;
    std::cout << 14500;
    for (int i = 0; i < 14500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14500(int& val) {
    const int x = 14500;
    std::cout << 14500;
    for (int i = 0; i < 14500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14501(int& val) {
    func_in_14501(val);
    const int x = 14501;
    std::cout << 14501;
    for (int i = 0; i < 14501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14501(int& val) {
    const int x = 14501;
    std::cout << 14501;
    for (int i = 0; i < 14501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14502(int& val) {
    func_in_14502(val);
    const int x = 14502;
    std::cout << 14502;
    for (int i = 0; i < 14502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14502(int& val) {
    const int x = 14502;
    std::cout << 14502;
    for (int i = 0; i < 14502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14503(int& val) {
    func_in_14503(val);
    const int x = 14503;
    std::cout << 14503;
    for (int i = 0; i < 14503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14503(int& val) {
    const int x = 14503;
    std::cout << 14503;
    for (int i = 0; i < 14503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14504(int& val) {
    func_in_14504(val);
    const int x = 14504;
    std::cout << 14504;
    for (int i = 0; i < 14504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14504(int& val) {
    const int x = 14504;
    std::cout << 14504;
    for (int i = 0; i < 14504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14505(int& val) {
    func_in_14505(val);
    const int x = 14505;
    std::cout << 14505;
    for (int i = 0; i < 14505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14505(int& val) {
    const int x = 14505;
    std::cout << 14505;
    for (int i = 0; i < 14505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14506(int& val) {
    func_in_14506(val);
    const int x = 14506;
    std::cout << 14506;
    for (int i = 0; i < 14506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14506(int& val) {
    const int x = 14506;
    std::cout << 14506;
    for (int i = 0; i < 14506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14507(int& val) {
    func_in_14507(val);
    const int x = 14507;
    std::cout << 14507;
    for (int i = 0; i < 14507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14507(int& val) {
    const int x = 14507;
    std::cout << 14507;
    for (int i = 0; i < 14507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14508(int& val) {
    func_in_14508(val);
    const int x = 14508;
    std::cout << 14508;
    for (int i = 0; i < 14508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14508(int& val) {
    const int x = 14508;
    std::cout << 14508;
    for (int i = 0; i < 14508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14509(int& val) {
    func_in_14509(val);
    const int x = 14509;
    std::cout << 14509;
    for (int i = 0; i < 14509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14509(int& val) {
    const int x = 14509;
    std::cout << 14509;
    for (int i = 0; i < 14509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14510(int& val) {
    func_in_14510(val);
    const int x = 14510;
    std::cout << 14510;
    for (int i = 0; i < 14510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14510(int& val) {
    const int x = 14510;
    std::cout << 14510;
    for (int i = 0; i < 14510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14511(int& val) {
    func_in_14511(val);
    const int x = 14511;
    std::cout << 14511;
    for (int i = 0; i < 14511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14511(int& val) {
    const int x = 14511;
    std::cout << 14511;
    for (int i = 0; i < 14511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14512(int& val) {
    func_in_14512(val);
    const int x = 14512;
    std::cout << 14512;
    for (int i = 0; i < 14512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14512(int& val) {
    const int x = 14512;
    std::cout << 14512;
    for (int i = 0; i < 14512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14513(int& val) {
    func_in_14513(val);
    const int x = 14513;
    std::cout << 14513;
    for (int i = 0; i < 14513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14513(int& val) {
    const int x = 14513;
    std::cout << 14513;
    for (int i = 0; i < 14513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14514(int& val) {
    func_in_14514(val);
    const int x = 14514;
    std::cout << 14514;
    for (int i = 0; i < 14514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14514(int& val) {
    const int x = 14514;
    std::cout << 14514;
    for (int i = 0; i < 14514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14515(int& val) {
    func_in_14515(val);
    const int x = 14515;
    std::cout << 14515;
    for (int i = 0; i < 14515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14515(int& val) {
    const int x = 14515;
    std::cout << 14515;
    for (int i = 0; i < 14515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14516(int& val) {
    func_in_14516(val);
    const int x = 14516;
    std::cout << 14516;
    for (int i = 0; i < 14516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14516(int& val) {
    const int x = 14516;
    std::cout << 14516;
    for (int i = 0; i < 14516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14517(int& val) {
    func_in_14517(val);
    const int x = 14517;
    std::cout << 14517;
    for (int i = 0; i < 14517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14517(int& val) {
    const int x = 14517;
    std::cout << 14517;
    for (int i = 0; i < 14517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14518(int& val) {
    func_in_14518(val);
    const int x = 14518;
    std::cout << 14518;
    for (int i = 0; i < 14518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14518(int& val) {
    const int x = 14518;
    std::cout << 14518;
    for (int i = 0; i < 14518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14519(int& val) {
    func_in_14519(val);
    const int x = 14519;
    std::cout << 14519;
    for (int i = 0; i < 14519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14519(int& val) {
    const int x = 14519;
    std::cout << 14519;
    for (int i = 0; i < 14519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14520(int& val) {
    func_in_14520(val);
    const int x = 14520;
    std::cout << 14520;
    for (int i = 0; i < 14520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14520(int& val) {
    const int x = 14520;
    std::cout << 14520;
    for (int i = 0; i < 14520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14521(int& val) {
    func_in_14521(val);
    const int x = 14521;
    std::cout << 14521;
    for (int i = 0; i < 14521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14521(int& val) {
    const int x = 14521;
    std::cout << 14521;
    for (int i = 0; i < 14521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14522(int& val) {
    func_in_14522(val);
    const int x = 14522;
    std::cout << 14522;
    for (int i = 0; i < 14522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14522(int& val) {
    const int x = 14522;
    std::cout << 14522;
    for (int i = 0; i < 14522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14523(int& val) {
    func_in_14523(val);
    const int x = 14523;
    std::cout << 14523;
    for (int i = 0; i < 14523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14523(int& val) {
    const int x = 14523;
    std::cout << 14523;
    for (int i = 0; i < 14523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14524(int& val) {
    func_in_14524(val);
    const int x = 14524;
    std::cout << 14524;
    for (int i = 0; i < 14524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14524(int& val) {
    const int x = 14524;
    std::cout << 14524;
    for (int i = 0; i < 14524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14525(int& val) {
    func_in_14525(val);
    const int x = 14525;
    std::cout << 14525;
    for (int i = 0; i < 14525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14525(int& val) {
    const int x = 14525;
    std::cout << 14525;
    for (int i = 0; i < 14525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14526(int& val) {
    func_in_14526(val);
    const int x = 14526;
    std::cout << 14526;
    for (int i = 0; i < 14526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14526(int& val) {
    const int x = 14526;
    std::cout << 14526;
    for (int i = 0; i < 14526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14527(int& val) {
    func_in_14527(val);
    const int x = 14527;
    std::cout << 14527;
    for (int i = 0; i < 14527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14527(int& val) {
    const int x = 14527;
    std::cout << 14527;
    for (int i = 0; i < 14527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14528(int& val) {
    func_in_14528(val);
    const int x = 14528;
    std::cout << 14528;
    for (int i = 0; i < 14528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14528(int& val) {
    const int x = 14528;
    std::cout << 14528;
    for (int i = 0; i < 14528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14529(int& val) {
    func_in_14529(val);
    const int x = 14529;
    std::cout << 14529;
    for (int i = 0; i < 14529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14529(int& val) {
    const int x = 14529;
    std::cout << 14529;
    for (int i = 0; i < 14529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14530(int& val) {
    func_in_14530(val);
    const int x = 14530;
    std::cout << 14530;
    for (int i = 0; i < 14530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14530(int& val) {
    const int x = 14530;
    std::cout << 14530;
    for (int i = 0; i < 14530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14531(int& val) {
    func_in_14531(val);
    const int x = 14531;
    std::cout << 14531;
    for (int i = 0; i < 14531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14531(int& val) {
    const int x = 14531;
    std::cout << 14531;
    for (int i = 0; i < 14531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14532(int& val) {
    func_in_14532(val);
    const int x = 14532;
    std::cout << 14532;
    for (int i = 0; i < 14532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14532(int& val) {
    const int x = 14532;
    std::cout << 14532;
    for (int i = 0; i < 14532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14533(int& val) {
    func_in_14533(val);
    const int x = 14533;
    std::cout << 14533;
    for (int i = 0; i < 14533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14533(int& val) {
    const int x = 14533;
    std::cout << 14533;
    for (int i = 0; i < 14533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14534(int& val) {
    func_in_14534(val);
    const int x = 14534;
    std::cout << 14534;
    for (int i = 0; i < 14534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14534(int& val) {
    const int x = 14534;
    std::cout << 14534;
    for (int i = 0; i < 14534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14535(int& val) {
    func_in_14535(val);
    const int x = 14535;
    std::cout << 14535;
    for (int i = 0; i < 14535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14535(int& val) {
    const int x = 14535;
    std::cout << 14535;
    for (int i = 0; i < 14535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14536(int& val) {
    func_in_14536(val);
    const int x = 14536;
    std::cout << 14536;
    for (int i = 0; i < 14536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14536(int& val) {
    const int x = 14536;
    std::cout << 14536;
    for (int i = 0; i < 14536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14537(int& val) {
    func_in_14537(val);
    const int x = 14537;
    std::cout << 14537;
    for (int i = 0; i < 14537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14537(int& val) {
    const int x = 14537;
    std::cout << 14537;
    for (int i = 0; i < 14537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14538(int& val) {
    func_in_14538(val);
    const int x = 14538;
    std::cout << 14538;
    for (int i = 0; i < 14538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14538(int& val) {
    const int x = 14538;
    std::cout << 14538;
    for (int i = 0; i < 14538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14539(int& val) {
    func_in_14539(val);
    const int x = 14539;
    std::cout << 14539;
    for (int i = 0; i < 14539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14539(int& val) {
    const int x = 14539;
    std::cout << 14539;
    for (int i = 0; i < 14539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14540(int& val) {
    func_in_14540(val);
    const int x = 14540;
    std::cout << 14540;
    for (int i = 0; i < 14540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14540(int& val) {
    const int x = 14540;
    std::cout << 14540;
    for (int i = 0; i < 14540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14541(int& val) {
    func_in_14541(val);
    const int x = 14541;
    std::cout << 14541;
    for (int i = 0; i < 14541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14541(int& val) {
    const int x = 14541;
    std::cout << 14541;
    for (int i = 0; i < 14541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14542(int& val) {
    func_in_14542(val);
    const int x = 14542;
    std::cout << 14542;
    for (int i = 0; i < 14542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14542(int& val) {
    const int x = 14542;
    std::cout << 14542;
    for (int i = 0; i < 14542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14543(int& val) {
    func_in_14543(val);
    const int x = 14543;
    std::cout << 14543;
    for (int i = 0; i < 14543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14543(int& val) {
    const int x = 14543;
    std::cout << 14543;
    for (int i = 0; i < 14543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14544(int& val) {
    func_in_14544(val);
    const int x = 14544;
    std::cout << 14544;
    for (int i = 0; i < 14544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14544(int& val) {
    const int x = 14544;
    std::cout << 14544;
    for (int i = 0; i < 14544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14545(int& val) {
    func_in_14545(val);
    const int x = 14545;
    std::cout << 14545;
    for (int i = 0; i < 14545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14545(int& val) {
    const int x = 14545;
    std::cout << 14545;
    for (int i = 0; i < 14545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14546(int& val) {
    func_in_14546(val);
    const int x = 14546;
    std::cout << 14546;
    for (int i = 0; i < 14546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14546(int& val) {
    const int x = 14546;
    std::cout << 14546;
    for (int i = 0; i < 14546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14547(int& val) {
    func_in_14547(val);
    const int x = 14547;
    std::cout << 14547;
    for (int i = 0; i < 14547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14547(int& val) {
    const int x = 14547;
    std::cout << 14547;
    for (int i = 0; i < 14547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14548(int& val) {
    func_in_14548(val);
    const int x = 14548;
    std::cout << 14548;
    for (int i = 0; i < 14548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14548(int& val) {
    const int x = 14548;
    std::cout << 14548;
    for (int i = 0; i < 14548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14549(int& val) {
    func_in_14549(val);
    const int x = 14549;
    std::cout << 14549;
    for (int i = 0; i < 14549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14549(int& val) {
    const int x = 14549;
    std::cout << 14549;
    for (int i = 0; i < 14549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14550(int& val) {
    func_in_14550(val);
    const int x = 14550;
    std::cout << 14550;
    for (int i = 0; i < 14550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14550(int& val) {
    const int x = 14550;
    std::cout << 14550;
    for (int i = 0; i < 14550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14551(int& val) {
    func_in_14551(val);
    const int x = 14551;
    std::cout << 14551;
    for (int i = 0; i < 14551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14551(int& val) {
    const int x = 14551;
    std::cout << 14551;
    for (int i = 0; i < 14551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14552(int& val) {
    func_in_14552(val);
    const int x = 14552;
    std::cout << 14552;
    for (int i = 0; i < 14552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14552(int& val) {
    const int x = 14552;
    std::cout << 14552;
    for (int i = 0; i < 14552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14553(int& val) {
    func_in_14553(val);
    const int x = 14553;
    std::cout << 14553;
    for (int i = 0; i < 14553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14553(int& val) {
    const int x = 14553;
    std::cout << 14553;
    for (int i = 0; i < 14553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14554(int& val) {
    func_in_14554(val);
    const int x = 14554;
    std::cout << 14554;
    for (int i = 0; i < 14554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14554(int& val) {
    const int x = 14554;
    std::cout << 14554;
    for (int i = 0; i < 14554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14555(int& val) {
    func_in_14555(val);
    const int x = 14555;
    std::cout << 14555;
    for (int i = 0; i < 14555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14555(int& val) {
    const int x = 14555;
    std::cout << 14555;
    for (int i = 0; i < 14555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14556(int& val) {
    func_in_14556(val);
    const int x = 14556;
    std::cout << 14556;
    for (int i = 0; i < 14556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14556(int& val) {
    const int x = 14556;
    std::cout << 14556;
    for (int i = 0; i < 14556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14557(int& val) {
    func_in_14557(val);
    const int x = 14557;
    std::cout << 14557;
    for (int i = 0; i < 14557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14557(int& val) {
    const int x = 14557;
    std::cout << 14557;
    for (int i = 0; i < 14557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14558(int& val) {
    func_in_14558(val);
    const int x = 14558;
    std::cout << 14558;
    for (int i = 0; i < 14558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14558(int& val) {
    const int x = 14558;
    std::cout << 14558;
    for (int i = 0; i < 14558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14559(int& val) {
    func_in_14559(val);
    const int x = 14559;
    std::cout << 14559;
    for (int i = 0; i < 14559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14559(int& val) {
    const int x = 14559;
    std::cout << 14559;
    for (int i = 0; i < 14559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14560(int& val) {
    func_in_14560(val);
    const int x = 14560;
    std::cout << 14560;
    for (int i = 0; i < 14560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14560(int& val) {
    const int x = 14560;
    std::cout << 14560;
    for (int i = 0; i < 14560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14561(int& val) {
    func_in_14561(val);
    const int x = 14561;
    std::cout << 14561;
    for (int i = 0; i < 14561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14561(int& val) {
    const int x = 14561;
    std::cout << 14561;
    for (int i = 0; i < 14561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14562(int& val) {
    func_in_14562(val);
    const int x = 14562;
    std::cout << 14562;
    for (int i = 0; i < 14562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14562(int& val) {
    const int x = 14562;
    std::cout << 14562;
    for (int i = 0; i < 14562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14563(int& val) {
    func_in_14563(val);
    const int x = 14563;
    std::cout << 14563;
    for (int i = 0; i < 14563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14563(int& val) {
    const int x = 14563;
    std::cout << 14563;
    for (int i = 0; i < 14563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14564(int& val) {
    func_in_14564(val);
    const int x = 14564;
    std::cout << 14564;
    for (int i = 0; i < 14564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14564(int& val) {
    const int x = 14564;
    std::cout << 14564;
    for (int i = 0; i < 14564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14565(int& val) {
    func_in_14565(val);
    const int x = 14565;
    std::cout << 14565;
    for (int i = 0; i < 14565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14565(int& val) {
    const int x = 14565;
    std::cout << 14565;
    for (int i = 0; i < 14565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14566(int& val) {
    func_in_14566(val);
    const int x = 14566;
    std::cout << 14566;
    for (int i = 0; i < 14566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14566(int& val) {
    const int x = 14566;
    std::cout << 14566;
    for (int i = 0; i < 14566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14567(int& val) {
    func_in_14567(val);
    const int x = 14567;
    std::cout << 14567;
    for (int i = 0; i < 14567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14567(int& val) {
    const int x = 14567;
    std::cout << 14567;
    for (int i = 0; i < 14567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14568(int& val) {
    func_in_14568(val);
    const int x = 14568;
    std::cout << 14568;
    for (int i = 0; i < 14568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14568(int& val) {
    const int x = 14568;
    std::cout << 14568;
    for (int i = 0; i < 14568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14569(int& val) {
    func_in_14569(val);
    const int x = 14569;
    std::cout << 14569;
    for (int i = 0; i < 14569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14569(int& val) {
    const int x = 14569;
    std::cout << 14569;
    for (int i = 0; i < 14569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14570(int& val) {
    func_in_14570(val);
    const int x = 14570;
    std::cout << 14570;
    for (int i = 0; i < 14570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14570(int& val) {
    const int x = 14570;
    std::cout << 14570;
    for (int i = 0; i < 14570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14571(int& val) {
    func_in_14571(val);
    const int x = 14571;
    std::cout << 14571;
    for (int i = 0; i < 14571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14571(int& val) {
    const int x = 14571;
    std::cout << 14571;
    for (int i = 0; i < 14571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14572(int& val) {
    func_in_14572(val);
    const int x = 14572;
    std::cout << 14572;
    for (int i = 0; i < 14572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14572(int& val) {
    const int x = 14572;
    std::cout << 14572;
    for (int i = 0; i < 14572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14573(int& val) {
    func_in_14573(val);
    const int x = 14573;
    std::cout << 14573;
    for (int i = 0; i < 14573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14573(int& val) {
    const int x = 14573;
    std::cout << 14573;
    for (int i = 0; i < 14573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14574(int& val) {
    func_in_14574(val);
    const int x = 14574;
    std::cout << 14574;
    for (int i = 0; i < 14574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14574(int& val) {
    const int x = 14574;
    std::cout << 14574;
    for (int i = 0; i < 14574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14575(int& val) {
    func_in_14575(val);
    const int x = 14575;
    std::cout << 14575;
    for (int i = 0; i < 14575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14575(int& val) {
    const int x = 14575;
    std::cout << 14575;
    for (int i = 0; i < 14575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14576(int& val) {
    func_in_14576(val);
    const int x = 14576;
    std::cout << 14576;
    for (int i = 0; i < 14576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14576(int& val) {
    const int x = 14576;
    std::cout << 14576;
    for (int i = 0; i < 14576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14577(int& val) {
    func_in_14577(val);
    const int x = 14577;
    std::cout << 14577;
    for (int i = 0; i < 14577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14577(int& val) {
    const int x = 14577;
    std::cout << 14577;
    for (int i = 0; i < 14577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14578(int& val) {
    func_in_14578(val);
    const int x = 14578;
    std::cout << 14578;
    for (int i = 0; i < 14578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14578(int& val) {
    const int x = 14578;
    std::cout << 14578;
    for (int i = 0; i < 14578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14579(int& val) {
    func_in_14579(val);
    const int x = 14579;
    std::cout << 14579;
    for (int i = 0; i < 14579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14579(int& val) {
    const int x = 14579;
    std::cout << 14579;
    for (int i = 0; i < 14579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14580(int& val) {
    func_in_14580(val);
    const int x = 14580;
    std::cout << 14580;
    for (int i = 0; i < 14580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14580(int& val) {
    const int x = 14580;
    std::cout << 14580;
    for (int i = 0; i < 14580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14581(int& val) {
    func_in_14581(val);
    const int x = 14581;
    std::cout << 14581;
    for (int i = 0; i < 14581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14581(int& val) {
    const int x = 14581;
    std::cout << 14581;
    for (int i = 0; i < 14581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14582(int& val) {
    func_in_14582(val);
    const int x = 14582;
    std::cout << 14582;
    for (int i = 0; i < 14582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14582(int& val) {
    const int x = 14582;
    std::cout << 14582;
    for (int i = 0; i < 14582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14583(int& val) {
    func_in_14583(val);
    const int x = 14583;
    std::cout << 14583;
    for (int i = 0; i < 14583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14583(int& val) {
    const int x = 14583;
    std::cout << 14583;
    for (int i = 0; i < 14583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14584(int& val) {
    func_in_14584(val);
    const int x = 14584;
    std::cout << 14584;
    for (int i = 0; i < 14584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14584(int& val) {
    const int x = 14584;
    std::cout << 14584;
    for (int i = 0; i < 14584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14585(int& val) {
    func_in_14585(val);
    const int x = 14585;
    std::cout << 14585;
    for (int i = 0; i < 14585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14585(int& val) {
    const int x = 14585;
    std::cout << 14585;
    for (int i = 0; i < 14585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14586(int& val) {
    func_in_14586(val);
    const int x = 14586;
    std::cout << 14586;
    for (int i = 0; i < 14586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14586(int& val) {
    const int x = 14586;
    std::cout << 14586;
    for (int i = 0; i < 14586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14587(int& val) {
    func_in_14587(val);
    const int x = 14587;
    std::cout << 14587;
    for (int i = 0; i < 14587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14587(int& val) {
    const int x = 14587;
    std::cout << 14587;
    for (int i = 0; i < 14587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14588(int& val) {
    func_in_14588(val);
    const int x = 14588;
    std::cout << 14588;
    for (int i = 0; i < 14588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14588(int& val) {
    const int x = 14588;
    std::cout << 14588;
    for (int i = 0; i < 14588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14589(int& val) {
    func_in_14589(val);
    const int x = 14589;
    std::cout << 14589;
    for (int i = 0; i < 14589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14589(int& val) {
    const int x = 14589;
    std::cout << 14589;
    for (int i = 0; i < 14589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14590(int& val) {
    func_in_14590(val);
    const int x = 14590;
    std::cout << 14590;
    for (int i = 0; i < 14590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14590(int& val) {
    const int x = 14590;
    std::cout << 14590;
    for (int i = 0; i < 14590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14591(int& val) {
    func_in_14591(val);
    const int x = 14591;
    std::cout << 14591;
    for (int i = 0; i < 14591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14591(int& val) {
    const int x = 14591;
    std::cout << 14591;
    for (int i = 0; i < 14591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14592(int& val) {
    func_in_14592(val);
    const int x = 14592;
    std::cout << 14592;
    for (int i = 0; i < 14592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14592(int& val) {
    const int x = 14592;
    std::cout << 14592;
    for (int i = 0; i < 14592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14593(int& val) {
    func_in_14593(val);
    const int x = 14593;
    std::cout << 14593;
    for (int i = 0; i < 14593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14593(int& val) {
    const int x = 14593;
    std::cout << 14593;
    for (int i = 0; i < 14593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14594(int& val) {
    func_in_14594(val);
    const int x = 14594;
    std::cout << 14594;
    for (int i = 0; i < 14594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14594(int& val) {
    const int x = 14594;
    std::cout << 14594;
    for (int i = 0; i < 14594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14595(int& val) {
    func_in_14595(val);
    const int x = 14595;
    std::cout << 14595;
    for (int i = 0; i < 14595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14595(int& val) {
    const int x = 14595;
    std::cout << 14595;
    for (int i = 0; i < 14595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14596(int& val) {
    func_in_14596(val);
    const int x = 14596;
    std::cout << 14596;
    for (int i = 0; i < 14596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14596(int& val) {
    const int x = 14596;
    std::cout << 14596;
    for (int i = 0; i < 14596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14597(int& val) {
    func_in_14597(val);
    const int x = 14597;
    std::cout << 14597;
    for (int i = 0; i < 14597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14597(int& val) {
    const int x = 14597;
    std::cout << 14597;
    for (int i = 0; i < 14597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14598(int& val) {
    func_in_14598(val);
    const int x = 14598;
    std::cout << 14598;
    for (int i = 0; i < 14598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14598(int& val) {
    const int x = 14598;
    std::cout << 14598;
    for (int i = 0; i < 14598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14599(int& val) {
    func_in_14599(val);
    const int x = 14599;
    std::cout << 14599;
    for (int i = 0; i < 14599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14599(int& val) {
    const int x = 14599;
    std::cout << 14599;
    for (int i = 0; i < 14599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14600(int& val) {
    func_in_14600(val);
    const int x = 14600;
    std::cout << 14600;
    for (int i = 0; i < 14600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14600(int& val) {
    const int x = 14600;
    std::cout << 14600;
    for (int i = 0; i < 14600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14601(int& val) {
    func_in_14601(val);
    const int x = 14601;
    std::cout << 14601;
    for (int i = 0; i < 14601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14601(int& val) {
    const int x = 14601;
    std::cout << 14601;
    for (int i = 0; i < 14601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14602(int& val) {
    func_in_14602(val);
    const int x = 14602;
    std::cout << 14602;
    for (int i = 0; i < 14602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14602(int& val) {
    const int x = 14602;
    std::cout << 14602;
    for (int i = 0; i < 14602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14603(int& val) {
    func_in_14603(val);
    const int x = 14603;
    std::cout << 14603;
    for (int i = 0; i < 14603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14603(int& val) {
    const int x = 14603;
    std::cout << 14603;
    for (int i = 0; i < 14603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14604(int& val) {
    func_in_14604(val);
    const int x = 14604;
    std::cout << 14604;
    for (int i = 0; i < 14604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14604(int& val) {
    const int x = 14604;
    std::cout << 14604;
    for (int i = 0; i < 14604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14605(int& val) {
    func_in_14605(val);
    const int x = 14605;
    std::cout << 14605;
    for (int i = 0; i < 14605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14605(int& val) {
    const int x = 14605;
    std::cout << 14605;
    for (int i = 0; i < 14605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14606(int& val) {
    func_in_14606(val);
    const int x = 14606;
    std::cout << 14606;
    for (int i = 0; i < 14606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14606(int& val) {
    const int x = 14606;
    std::cout << 14606;
    for (int i = 0; i < 14606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14607(int& val) {
    func_in_14607(val);
    const int x = 14607;
    std::cout << 14607;
    for (int i = 0; i < 14607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14607(int& val) {
    const int x = 14607;
    std::cout << 14607;
    for (int i = 0; i < 14607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14608(int& val) {
    func_in_14608(val);
    const int x = 14608;
    std::cout << 14608;
    for (int i = 0; i < 14608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14608(int& val) {
    const int x = 14608;
    std::cout << 14608;
    for (int i = 0; i < 14608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14609(int& val) {
    func_in_14609(val);
    const int x = 14609;
    std::cout << 14609;
    for (int i = 0; i < 14609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14609(int& val) {
    const int x = 14609;
    std::cout << 14609;
    for (int i = 0; i < 14609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14610(int& val) {
    func_in_14610(val);
    const int x = 14610;
    std::cout << 14610;
    for (int i = 0; i < 14610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14610(int& val) {
    const int x = 14610;
    std::cout << 14610;
    for (int i = 0; i < 14610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14611(int& val) {
    func_in_14611(val);
    const int x = 14611;
    std::cout << 14611;
    for (int i = 0; i < 14611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14611(int& val) {
    const int x = 14611;
    std::cout << 14611;
    for (int i = 0; i < 14611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14612(int& val) {
    func_in_14612(val);
    const int x = 14612;
    std::cout << 14612;
    for (int i = 0; i < 14612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14612(int& val) {
    const int x = 14612;
    std::cout << 14612;
    for (int i = 0; i < 14612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14613(int& val) {
    func_in_14613(val);
    const int x = 14613;
    std::cout << 14613;
    for (int i = 0; i < 14613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14613(int& val) {
    const int x = 14613;
    std::cout << 14613;
    for (int i = 0; i < 14613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14614(int& val) {
    func_in_14614(val);
    const int x = 14614;
    std::cout << 14614;
    for (int i = 0; i < 14614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14614(int& val) {
    const int x = 14614;
    std::cout << 14614;
    for (int i = 0; i < 14614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14615(int& val) {
    func_in_14615(val);
    const int x = 14615;
    std::cout << 14615;
    for (int i = 0; i < 14615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14615(int& val) {
    const int x = 14615;
    std::cout << 14615;
    for (int i = 0; i < 14615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14616(int& val) {
    func_in_14616(val);
    const int x = 14616;
    std::cout << 14616;
    for (int i = 0; i < 14616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14616(int& val) {
    const int x = 14616;
    std::cout << 14616;
    for (int i = 0; i < 14616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14617(int& val) {
    func_in_14617(val);
    const int x = 14617;
    std::cout << 14617;
    for (int i = 0; i < 14617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14617(int& val) {
    const int x = 14617;
    std::cout << 14617;
    for (int i = 0; i < 14617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14618(int& val) {
    func_in_14618(val);
    const int x = 14618;
    std::cout << 14618;
    for (int i = 0; i < 14618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14618(int& val) {
    const int x = 14618;
    std::cout << 14618;
    for (int i = 0; i < 14618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14619(int& val) {
    func_in_14619(val);
    const int x = 14619;
    std::cout << 14619;
    for (int i = 0; i < 14619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14619(int& val) {
    const int x = 14619;
    std::cout << 14619;
    for (int i = 0; i < 14619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14620(int& val) {
    func_in_14620(val);
    const int x = 14620;
    std::cout << 14620;
    for (int i = 0; i < 14620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14620(int& val) {
    const int x = 14620;
    std::cout << 14620;
    for (int i = 0; i < 14620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14621(int& val) {
    func_in_14621(val);
    const int x = 14621;
    std::cout << 14621;
    for (int i = 0; i < 14621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14621(int& val) {
    const int x = 14621;
    std::cout << 14621;
    for (int i = 0; i < 14621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14622(int& val) {
    func_in_14622(val);
    const int x = 14622;
    std::cout << 14622;
    for (int i = 0; i < 14622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14622(int& val) {
    const int x = 14622;
    std::cout << 14622;
    for (int i = 0; i < 14622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14623(int& val) {
    func_in_14623(val);
    const int x = 14623;
    std::cout << 14623;
    for (int i = 0; i < 14623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14623(int& val) {
    const int x = 14623;
    std::cout << 14623;
    for (int i = 0; i < 14623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14624(int& val) {
    func_in_14624(val);
    const int x = 14624;
    std::cout << 14624;
    for (int i = 0; i < 14624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14624(int& val) {
    const int x = 14624;
    std::cout << 14624;
    for (int i = 0; i < 14624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14625(int& val) {
    func_in_14625(val);
    const int x = 14625;
    std::cout << 14625;
    for (int i = 0; i < 14625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14625(int& val) {
    const int x = 14625;
    std::cout << 14625;
    for (int i = 0; i < 14625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14626(int& val) {
    func_in_14626(val);
    const int x = 14626;
    std::cout << 14626;
    for (int i = 0; i < 14626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14626(int& val) {
    const int x = 14626;
    std::cout << 14626;
    for (int i = 0; i < 14626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14627(int& val) {
    func_in_14627(val);
    const int x = 14627;
    std::cout << 14627;
    for (int i = 0; i < 14627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14627(int& val) {
    const int x = 14627;
    std::cout << 14627;
    for (int i = 0; i < 14627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14628(int& val) {
    func_in_14628(val);
    const int x = 14628;
    std::cout << 14628;
    for (int i = 0; i < 14628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14628(int& val) {
    const int x = 14628;
    std::cout << 14628;
    for (int i = 0; i < 14628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14629(int& val) {
    func_in_14629(val);
    const int x = 14629;
    std::cout << 14629;
    for (int i = 0; i < 14629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14629(int& val) {
    const int x = 14629;
    std::cout << 14629;
    for (int i = 0; i < 14629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14630(int& val) {
    func_in_14630(val);
    const int x = 14630;
    std::cout << 14630;
    for (int i = 0; i < 14630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14630(int& val) {
    const int x = 14630;
    std::cout << 14630;
    for (int i = 0; i < 14630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14631(int& val) {
    func_in_14631(val);
    const int x = 14631;
    std::cout << 14631;
    for (int i = 0; i < 14631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14631(int& val) {
    const int x = 14631;
    std::cout << 14631;
    for (int i = 0; i < 14631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14632(int& val) {
    func_in_14632(val);
    const int x = 14632;
    std::cout << 14632;
    for (int i = 0; i < 14632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14632(int& val) {
    const int x = 14632;
    std::cout << 14632;
    for (int i = 0; i < 14632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14633(int& val) {
    func_in_14633(val);
    const int x = 14633;
    std::cout << 14633;
    for (int i = 0; i < 14633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14633(int& val) {
    const int x = 14633;
    std::cout << 14633;
    for (int i = 0; i < 14633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14634(int& val) {
    func_in_14634(val);
    const int x = 14634;
    std::cout << 14634;
    for (int i = 0; i < 14634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14634(int& val) {
    const int x = 14634;
    std::cout << 14634;
    for (int i = 0; i < 14634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14635(int& val) {
    func_in_14635(val);
    const int x = 14635;
    std::cout << 14635;
    for (int i = 0; i < 14635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14635(int& val) {
    const int x = 14635;
    std::cout << 14635;
    for (int i = 0; i < 14635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14636(int& val) {
    func_in_14636(val);
    const int x = 14636;
    std::cout << 14636;
    for (int i = 0; i < 14636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14636(int& val) {
    const int x = 14636;
    std::cout << 14636;
    for (int i = 0; i < 14636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14637(int& val) {
    func_in_14637(val);
    const int x = 14637;
    std::cout << 14637;
    for (int i = 0; i < 14637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14637(int& val) {
    const int x = 14637;
    std::cout << 14637;
    for (int i = 0; i < 14637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14638(int& val) {
    func_in_14638(val);
    const int x = 14638;
    std::cout << 14638;
    for (int i = 0; i < 14638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14638(int& val) {
    const int x = 14638;
    std::cout << 14638;
    for (int i = 0; i < 14638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14639(int& val) {
    func_in_14639(val);
    const int x = 14639;
    std::cout << 14639;
    for (int i = 0; i < 14639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14639(int& val) {
    const int x = 14639;
    std::cout << 14639;
    for (int i = 0; i < 14639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14640(int& val) {
    func_in_14640(val);
    const int x = 14640;
    std::cout << 14640;
    for (int i = 0; i < 14640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14640(int& val) {
    const int x = 14640;
    std::cout << 14640;
    for (int i = 0; i < 14640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14641(int& val) {
    func_in_14641(val);
    const int x = 14641;
    std::cout << 14641;
    for (int i = 0; i < 14641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14641(int& val) {
    const int x = 14641;
    std::cout << 14641;
    for (int i = 0; i < 14641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14642(int& val) {
    func_in_14642(val);
    const int x = 14642;
    std::cout << 14642;
    for (int i = 0; i < 14642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14642(int& val) {
    const int x = 14642;
    std::cout << 14642;
    for (int i = 0; i < 14642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14643(int& val) {
    func_in_14643(val);
    const int x = 14643;
    std::cout << 14643;
    for (int i = 0; i < 14643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14643(int& val) {
    const int x = 14643;
    std::cout << 14643;
    for (int i = 0; i < 14643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14644(int& val) {
    func_in_14644(val);
    const int x = 14644;
    std::cout << 14644;
    for (int i = 0; i < 14644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14644(int& val) {
    const int x = 14644;
    std::cout << 14644;
    for (int i = 0; i < 14644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14645(int& val) {
    func_in_14645(val);
    const int x = 14645;
    std::cout << 14645;
    for (int i = 0; i < 14645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14645(int& val) {
    const int x = 14645;
    std::cout << 14645;
    for (int i = 0; i < 14645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14646(int& val) {
    func_in_14646(val);
    const int x = 14646;
    std::cout << 14646;
    for (int i = 0; i < 14646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14646(int& val) {
    const int x = 14646;
    std::cout << 14646;
    for (int i = 0; i < 14646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14647(int& val) {
    func_in_14647(val);
    const int x = 14647;
    std::cout << 14647;
    for (int i = 0; i < 14647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14647(int& val) {
    const int x = 14647;
    std::cout << 14647;
    for (int i = 0; i < 14647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14648(int& val) {
    func_in_14648(val);
    const int x = 14648;
    std::cout << 14648;
    for (int i = 0; i < 14648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14648(int& val) {
    const int x = 14648;
    std::cout << 14648;
    for (int i = 0; i < 14648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14649(int& val) {
    func_in_14649(val);
    const int x = 14649;
    std::cout << 14649;
    for (int i = 0; i < 14649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14649(int& val) {
    const int x = 14649;
    std::cout << 14649;
    for (int i = 0; i < 14649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14650(int& val) {
    func_in_14650(val);
    const int x = 14650;
    std::cout << 14650;
    for (int i = 0; i < 14650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14650(int& val) {
    const int x = 14650;
    std::cout << 14650;
    for (int i = 0; i < 14650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14651(int& val) {
    func_in_14651(val);
    const int x = 14651;
    std::cout << 14651;
    for (int i = 0; i < 14651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14651(int& val) {
    const int x = 14651;
    std::cout << 14651;
    for (int i = 0; i < 14651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14652(int& val) {
    func_in_14652(val);
    const int x = 14652;
    std::cout << 14652;
    for (int i = 0; i < 14652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14652(int& val) {
    const int x = 14652;
    std::cout << 14652;
    for (int i = 0; i < 14652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14653(int& val) {
    func_in_14653(val);
    const int x = 14653;
    std::cout << 14653;
    for (int i = 0; i < 14653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14653(int& val) {
    const int x = 14653;
    std::cout << 14653;
    for (int i = 0; i < 14653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14654(int& val) {
    func_in_14654(val);
    const int x = 14654;
    std::cout << 14654;
    for (int i = 0; i < 14654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14654(int& val) {
    const int x = 14654;
    std::cout << 14654;
    for (int i = 0; i < 14654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14655(int& val) {
    func_in_14655(val);
    const int x = 14655;
    std::cout << 14655;
    for (int i = 0; i < 14655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14655(int& val) {
    const int x = 14655;
    std::cout << 14655;
    for (int i = 0; i < 14655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14656(int& val) {
    func_in_14656(val);
    const int x = 14656;
    std::cout << 14656;
    for (int i = 0; i < 14656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14656(int& val) {
    const int x = 14656;
    std::cout << 14656;
    for (int i = 0; i < 14656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14657(int& val) {
    func_in_14657(val);
    const int x = 14657;
    std::cout << 14657;
    for (int i = 0; i < 14657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14657(int& val) {
    const int x = 14657;
    std::cout << 14657;
    for (int i = 0; i < 14657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14658(int& val) {
    func_in_14658(val);
    const int x = 14658;
    std::cout << 14658;
    for (int i = 0; i < 14658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14658(int& val) {
    const int x = 14658;
    std::cout << 14658;
    for (int i = 0; i < 14658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14659(int& val) {
    func_in_14659(val);
    const int x = 14659;
    std::cout << 14659;
    for (int i = 0; i < 14659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14659(int& val) {
    const int x = 14659;
    std::cout << 14659;
    for (int i = 0; i < 14659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14660(int& val) {
    func_in_14660(val);
    const int x = 14660;
    std::cout << 14660;
    for (int i = 0; i < 14660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14660(int& val) {
    const int x = 14660;
    std::cout << 14660;
    for (int i = 0; i < 14660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14661(int& val) {
    func_in_14661(val);
    const int x = 14661;
    std::cout << 14661;
    for (int i = 0; i < 14661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14661(int& val) {
    const int x = 14661;
    std::cout << 14661;
    for (int i = 0; i < 14661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14662(int& val) {
    func_in_14662(val);
    const int x = 14662;
    std::cout << 14662;
    for (int i = 0; i < 14662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14662(int& val) {
    const int x = 14662;
    std::cout << 14662;
    for (int i = 0; i < 14662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14663(int& val) {
    func_in_14663(val);
    const int x = 14663;
    std::cout << 14663;
    for (int i = 0; i < 14663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14663(int& val) {
    const int x = 14663;
    std::cout << 14663;
    for (int i = 0; i < 14663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14664(int& val) {
    func_in_14664(val);
    const int x = 14664;
    std::cout << 14664;
    for (int i = 0; i < 14664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14664(int& val) {
    const int x = 14664;
    std::cout << 14664;
    for (int i = 0; i < 14664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14665(int& val) {
    func_in_14665(val);
    const int x = 14665;
    std::cout << 14665;
    for (int i = 0; i < 14665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14665(int& val) {
    const int x = 14665;
    std::cout << 14665;
    for (int i = 0; i < 14665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14666(int& val) {
    func_in_14666(val);
    const int x = 14666;
    std::cout << 14666;
    for (int i = 0; i < 14666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14666(int& val) {
    const int x = 14666;
    std::cout << 14666;
    for (int i = 0; i < 14666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14667(int& val) {
    func_in_14667(val);
    const int x = 14667;
    std::cout << 14667;
    for (int i = 0; i < 14667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14667(int& val) {
    const int x = 14667;
    std::cout << 14667;
    for (int i = 0; i < 14667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14668(int& val) {
    func_in_14668(val);
    const int x = 14668;
    std::cout << 14668;
    for (int i = 0; i < 14668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14668(int& val) {
    const int x = 14668;
    std::cout << 14668;
    for (int i = 0; i < 14668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14669(int& val) {
    func_in_14669(val);
    const int x = 14669;
    std::cout << 14669;
    for (int i = 0; i < 14669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14669(int& val) {
    const int x = 14669;
    std::cout << 14669;
    for (int i = 0; i < 14669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14670(int& val) {
    func_in_14670(val);
    const int x = 14670;
    std::cout << 14670;
    for (int i = 0; i < 14670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14670(int& val) {
    const int x = 14670;
    std::cout << 14670;
    for (int i = 0; i < 14670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14671(int& val) {
    func_in_14671(val);
    const int x = 14671;
    std::cout << 14671;
    for (int i = 0; i < 14671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14671(int& val) {
    const int x = 14671;
    std::cout << 14671;
    for (int i = 0; i < 14671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14672(int& val) {
    func_in_14672(val);
    const int x = 14672;
    std::cout << 14672;
    for (int i = 0; i < 14672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14672(int& val) {
    const int x = 14672;
    std::cout << 14672;
    for (int i = 0; i < 14672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14673(int& val) {
    func_in_14673(val);
    const int x = 14673;
    std::cout << 14673;
    for (int i = 0; i < 14673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14673(int& val) {
    const int x = 14673;
    std::cout << 14673;
    for (int i = 0; i < 14673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14674(int& val) {
    func_in_14674(val);
    const int x = 14674;
    std::cout << 14674;
    for (int i = 0; i < 14674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14674(int& val) {
    const int x = 14674;
    std::cout << 14674;
    for (int i = 0; i < 14674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14675(int& val) {
    func_in_14675(val);
    const int x = 14675;
    std::cout << 14675;
    for (int i = 0; i < 14675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14675(int& val) {
    const int x = 14675;
    std::cout << 14675;
    for (int i = 0; i < 14675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14676(int& val) {
    func_in_14676(val);
    const int x = 14676;
    std::cout << 14676;
    for (int i = 0; i < 14676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14676(int& val) {
    const int x = 14676;
    std::cout << 14676;
    for (int i = 0; i < 14676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14677(int& val) {
    func_in_14677(val);
    const int x = 14677;
    std::cout << 14677;
    for (int i = 0; i < 14677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14677(int& val) {
    const int x = 14677;
    std::cout << 14677;
    for (int i = 0; i < 14677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14678(int& val) {
    func_in_14678(val);
    const int x = 14678;
    std::cout << 14678;
    for (int i = 0; i < 14678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14678(int& val) {
    const int x = 14678;
    std::cout << 14678;
    for (int i = 0; i < 14678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14679(int& val) {
    func_in_14679(val);
    const int x = 14679;
    std::cout << 14679;
    for (int i = 0; i < 14679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14679(int& val) {
    const int x = 14679;
    std::cout << 14679;
    for (int i = 0; i < 14679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14680(int& val) {
    func_in_14680(val);
    const int x = 14680;
    std::cout << 14680;
    for (int i = 0; i < 14680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14680(int& val) {
    const int x = 14680;
    std::cout << 14680;
    for (int i = 0; i < 14680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14681(int& val) {
    func_in_14681(val);
    const int x = 14681;
    std::cout << 14681;
    for (int i = 0; i < 14681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14681(int& val) {
    const int x = 14681;
    std::cout << 14681;
    for (int i = 0; i < 14681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14682(int& val) {
    func_in_14682(val);
    const int x = 14682;
    std::cout << 14682;
    for (int i = 0; i < 14682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14682(int& val) {
    const int x = 14682;
    std::cout << 14682;
    for (int i = 0; i < 14682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14683(int& val) {
    func_in_14683(val);
    const int x = 14683;
    std::cout << 14683;
    for (int i = 0; i < 14683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14683(int& val) {
    const int x = 14683;
    std::cout << 14683;
    for (int i = 0; i < 14683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14684(int& val) {
    func_in_14684(val);
    const int x = 14684;
    std::cout << 14684;
    for (int i = 0; i < 14684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14684(int& val) {
    const int x = 14684;
    std::cout << 14684;
    for (int i = 0; i < 14684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14685(int& val) {
    func_in_14685(val);
    const int x = 14685;
    std::cout << 14685;
    for (int i = 0; i < 14685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14685(int& val) {
    const int x = 14685;
    std::cout << 14685;
    for (int i = 0; i < 14685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14686(int& val) {
    func_in_14686(val);
    const int x = 14686;
    std::cout << 14686;
    for (int i = 0; i < 14686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14686(int& val) {
    const int x = 14686;
    std::cout << 14686;
    for (int i = 0; i < 14686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14687(int& val) {
    func_in_14687(val);
    const int x = 14687;
    std::cout << 14687;
    for (int i = 0; i < 14687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14687(int& val) {
    const int x = 14687;
    std::cout << 14687;
    for (int i = 0; i < 14687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14688(int& val) {
    func_in_14688(val);
    const int x = 14688;
    std::cout << 14688;
    for (int i = 0; i < 14688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14688(int& val) {
    const int x = 14688;
    std::cout << 14688;
    for (int i = 0; i < 14688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14689(int& val) {
    func_in_14689(val);
    const int x = 14689;
    std::cout << 14689;
    for (int i = 0; i < 14689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14689(int& val) {
    const int x = 14689;
    std::cout << 14689;
    for (int i = 0; i < 14689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14690(int& val) {
    func_in_14690(val);
    const int x = 14690;
    std::cout << 14690;
    for (int i = 0; i < 14690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14690(int& val) {
    const int x = 14690;
    std::cout << 14690;
    for (int i = 0; i < 14690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14691(int& val) {
    func_in_14691(val);
    const int x = 14691;
    std::cout << 14691;
    for (int i = 0; i < 14691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14691(int& val) {
    const int x = 14691;
    std::cout << 14691;
    for (int i = 0; i < 14691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14692(int& val) {
    func_in_14692(val);
    const int x = 14692;
    std::cout << 14692;
    for (int i = 0; i < 14692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14692(int& val) {
    const int x = 14692;
    std::cout << 14692;
    for (int i = 0; i < 14692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14693(int& val) {
    func_in_14693(val);
    const int x = 14693;
    std::cout << 14693;
    for (int i = 0; i < 14693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14693(int& val) {
    const int x = 14693;
    std::cout << 14693;
    for (int i = 0; i < 14693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14694(int& val) {
    func_in_14694(val);
    const int x = 14694;
    std::cout << 14694;
    for (int i = 0; i < 14694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14694(int& val) {
    const int x = 14694;
    std::cout << 14694;
    for (int i = 0; i < 14694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14695(int& val) {
    func_in_14695(val);
    const int x = 14695;
    std::cout << 14695;
    for (int i = 0; i < 14695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14695(int& val) {
    const int x = 14695;
    std::cout << 14695;
    for (int i = 0; i < 14695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14696(int& val) {
    func_in_14696(val);
    const int x = 14696;
    std::cout << 14696;
    for (int i = 0; i < 14696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14696(int& val) {
    const int x = 14696;
    std::cout << 14696;
    for (int i = 0; i < 14696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14697(int& val) {
    func_in_14697(val);
    const int x = 14697;
    std::cout << 14697;
    for (int i = 0; i < 14697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14697(int& val) {
    const int x = 14697;
    std::cout << 14697;
    for (int i = 0; i < 14697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14698(int& val) {
    func_in_14698(val);
    const int x = 14698;
    std::cout << 14698;
    for (int i = 0; i < 14698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14698(int& val) {
    const int x = 14698;
    std::cout << 14698;
    for (int i = 0; i < 14698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14699(int& val) {
    func_in_14699(val);
    const int x = 14699;
    std::cout << 14699;
    for (int i = 0; i < 14699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14699(int& val) {
    const int x = 14699;
    std::cout << 14699;
    for (int i = 0; i < 14699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14700(int& val) {
    func_in_14700(val);
    const int x = 14700;
    std::cout << 14700;
    for (int i = 0; i < 14700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14700(int& val) {
    const int x = 14700;
    std::cout << 14700;
    for (int i = 0; i < 14700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14701(int& val) {
    func_in_14701(val);
    const int x = 14701;
    std::cout << 14701;
    for (int i = 0; i < 14701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14701(int& val) {
    const int x = 14701;
    std::cout << 14701;
    for (int i = 0; i < 14701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14702(int& val) {
    func_in_14702(val);
    const int x = 14702;
    std::cout << 14702;
    for (int i = 0; i < 14702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14702(int& val) {
    const int x = 14702;
    std::cout << 14702;
    for (int i = 0; i < 14702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14703(int& val) {
    func_in_14703(val);
    const int x = 14703;
    std::cout << 14703;
    for (int i = 0; i < 14703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14703(int& val) {
    const int x = 14703;
    std::cout << 14703;
    for (int i = 0; i < 14703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14704(int& val) {
    func_in_14704(val);
    const int x = 14704;
    std::cout << 14704;
    for (int i = 0; i < 14704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14704(int& val) {
    const int x = 14704;
    std::cout << 14704;
    for (int i = 0; i < 14704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14705(int& val) {
    func_in_14705(val);
    const int x = 14705;
    std::cout << 14705;
    for (int i = 0; i < 14705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14705(int& val) {
    const int x = 14705;
    std::cout << 14705;
    for (int i = 0; i < 14705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14706(int& val) {
    func_in_14706(val);
    const int x = 14706;
    std::cout << 14706;
    for (int i = 0; i < 14706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14706(int& val) {
    const int x = 14706;
    std::cout << 14706;
    for (int i = 0; i < 14706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14707(int& val) {
    func_in_14707(val);
    const int x = 14707;
    std::cout << 14707;
    for (int i = 0; i < 14707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14707(int& val) {
    const int x = 14707;
    std::cout << 14707;
    for (int i = 0; i < 14707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14708(int& val) {
    func_in_14708(val);
    const int x = 14708;
    std::cout << 14708;
    for (int i = 0; i < 14708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14708(int& val) {
    const int x = 14708;
    std::cout << 14708;
    for (int i = 0; i < 14708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14709(int& val) {
    func_in_14709(val);
    const int x = 14709;
    std::cout << 14709;
    for (int i = 0; i < 14709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14709(int& val) {
    const int x = 14709;
    std::cout << 14709;
    for (int i = 0; i < 14709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14710(int& val) {
    func_in_14710(val);
    const int x = 14710;
    std::cout << 14710;
    for (int i = 0; i < 14710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14710(int& val) {
    const int x = 14710;
    std::cout << 14710;
    for (int i = 0; i < 14710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14711(int& val) {
    func_in_14711(val);
    const int x = 14711;
    std::cout << 14711;
    for (int i = 0; i < 14711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14711(int& val) {
    const int x = 14711;
    std::cout << 14711;
    for (int i = 0; i < 14711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14712(int& val) {
    func_in_14712(val);
    const int x = 14712;
    std::cout << 14712;
    for (int i = 0; i < 14712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14712(int& val) {
    const int x = 14712;
    std::cout << 14712;
    for (int i = 0; i < 14712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14713(int& val) {
    func_in_14713(val);
    const int x = 14713;
    std::cout << 14713;
    for (int i = 0; i < 14713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14713(int& val) {
    const int x = 14713;
    std::cout << 14713;
    for (int i = 0; i < 14713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14714(int& val) {
    func_in_14714(val);
    const int x = 14714;
    std::cout << 14714;
    for (int i = 0; i < 14714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14714(int& val) {
    const int x = 14714;
    std::cout << 14714;
    for (int i = 0; i < 14714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14715(int& val) {
    func_in_14715(val);
    const int x = 14715;
    std::cout << 14715;
    for (int i = 0; i < 14715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14715(int& val) {
    const int x = 14715;
    std::cout << 14715;
    for (int i = 0; i < 14715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14716(int& val) {
    func_in_14716(val);
    const int x = 14716;
    std::cout << 14716;
    for (int i = 0; i < 14716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14716(int& val) {
    const int x = 14716;
    std::cout << 14716;
    for (int i = 0; i < 14716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14717(int& val) {
    func_in_14717(val);
    const int x = 14717;
    std::cout << 14717;
    for (int i = 0; i < 14717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14717(int& val) {
    const int x = 14717;
    std::cout << 14717;
    for (int i = 0; i < 14717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14718(int& val) {
    func_in_14718(val);
    const int x = 14718;
    std::cout << 14718;
    for (int i = 0; i < 14718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14718(int& val) {
    const int x = 14718;
    std::cout << 14718;
    for (int i = 0; i < 14718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14719(int& val) {
    func_in_14719(val);
    const int x = 14719;
    std::cout << 14719;
    for (int i = 0; i < 14719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14719(int& val) {
    const int x = 14719;
    std::cout << 14719;
    for (int i = 0; i < 14719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14720(int& val) {
    func_in_14720(val);
    const int x = 14720;
    std::cout << 14720;
    for (int i = 0; i < 14720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14720(int& val) {
    const int x = 14720;
    std::cout << 14720;
    for (int i = 0; i < 14720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14721(int& val) {
    func_in_14721(val);
    const int x = 14721;
    std::cout << 14721;
    for (int i = 0; i < 14721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14721(int& val) {
    const int x = 14721;
    std::cout << 14721;
    for (int i = 0; i < 14721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14722(int& val) {
    func_in_14722(val);
    const int x = 14722;
    std::cout << 14722;
    for (int i = 0; i < 14722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14722(int& val) {
    const int x = 14722;
    std::cout << 14722;
    for (int i = 0; i < 14722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14723(int& val) {
    func_in_14723(val);
    const int x = 14723;
    std::cout << 14723;
    for (int i = 0; i < 14723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14723(int& val) {
    const int x = 14723;
    std::cout << 14723;
    for (int i = 0; i < 14723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14724(int& val) {
    func_in_14724(val);
    const int x = 14724;
    std::cout << 14724;
    for (int i = 0; i < 14724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14724(int& val) {
    const int x = 14724;
    std::cout << 14724;
    for (int i = 0; i < 14724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14725(int& val) {
    func_in_14725(val);
    const int x = 14725;
    std::cout << 14725;
    for (int i = 0; i < 14725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14725(int& val) {
    const int x = 14725;
    std::cout << 14725;
    for (int i = 0; i < 14725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14726(int& val) {
    func_in_14726(val);
    const int x = 14726;
    std::cout << 14726;
    for (int i = 0; i < 14726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14726(int& val) {
    const int x = 14726;
    std::cout << 14726;
    for (int i = 0; i < 14726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14727(int& val) {
    func_in_14727(val);
    const int x = 14727;
    std::cout << 14727;
    for (int i = 0; i < 14727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14727(int& val) {
    const int x = 14727;
    std::cout << 14727;
    for (int i = 0; i < 14727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14728(int& val) {
    func_in_14728(val);
    const int x = 14728;
    std::cout << 14728;
    for (int i = 0; i < 14728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14728(int& val) {
    const int x = 14728;
    std::cout << 14728;
    for (int i = 0; i < 14728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14729(int& val) {
    func_in_14729(val);
    const int x = 14729;
    std::cout << 14729;
    for (int i = 0; i < 14729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14729(int& val) {
    const int x = 14729;
    std::cout << 14729;
    for (int i = 0; i < 14729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14730(int& val) {
    func_in_14730(val);
    const int x = 14730;
    std::cout << 14730;
    for (int i = 0; i < 14730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14730(int& val) {
    const int x = 14730;
    std::cout << 14730;
    for (int i = 0; i < 14730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14731(int& val) {
    func_in_14731(val);
    const int x = 14731;
    std::cout << 14731;
    for (int i = 0; i < 14731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14731(int& val) {
    const int x = 14731;
    std::cout << 14731;
    for (int i = 0; i < 14731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14732(int& val) {
    func_in_14732(val);
    const int x = 14732;
    std::cout << 14732;
    for (int i = 0; i < 14732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14732(int& val) {
    const int x = 14732;
    std::cout << 14732;
    for (int i = 0; i < 14732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14733(int& val) {
    func_in_14733(val);
    const int x = 14733;
    std::cout << 14733;
    for (int i = 0; i < 14733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14733(int& val) {
    const int x = 14733;
    std::cout << 14733;
    for (int i = 0; i < 14733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14734(int& val) {
    func_in_14734(val);
    const int x = 14734;
    std::cout << 14734;
    for (int i = 0; i < 14734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14734(int& val) {
    const int x = 14734;
    std::cout << 14734;
    for (int i = 0; i < 14734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14735(int& val) {
    func_in_14735(val);
    const int x = 14735;
    std::cout << 14735;
    for (int i = 0; i < 14735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14735(int& val) {
    const int x = 14735;
    std::cout << 14735;
    for (int i = 0; i < 14735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14736(int& val) {
    func_in_14736(val);
    const int x = 14736;
    std::cout << 14736;
    for (int i = 0; i < 14736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14736(int& val) {
    const int x = 14736;
    std::cout << 14736;
    for (int i = 0; i < 14736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14737(int& val) {
    func_in_14737(val);
    const int x = 14737;
    std::cout << 14737;
    for (int i = 0; i < 14737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14737(int& val) {
    const int x = 14737;
    std::cout << 14737;
    for (int i = 0; i < 14737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14738(int& val) {
    func_in_14738(val);
    const int x = 14738;
    std::cout << 14738;
    for (int i = 0; i < 14738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14738(int& val) {
    const int x = 14738;
    std::cout << 14738;
    for (int i = 0; i < 14738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14739(int& val) {
    func_in_14739(val);
    const int x = 14739;
    std::cout << 14739;
    for (int i = 0; i < 14739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14739(int& val) {
    const int x = 14739;
    std::cout << 14739;
    for (int i = 0; i < 14739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14740(int& val) {
    func_in_14740(val);
    const int x = 14740;
    std::cout << 14740;
    for (int i = 0; i < 14740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14740(int& val) {
    const int x = 14740;
    std::cout << 14740;
    for (int i = 0; i < 14740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14741(int& val) {
    func_in_14741(val);
    const int x = 14741;
    std::cout << 14741;
    for (int i = 0; i < 14741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14741(int& val) {
    const int x = 14741;
    std::cout << 14741;
    for (int i = 0; i < 14741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14742(int& val) {
    func_in_14742(val);
    const int x = 14742;
    std::cout << 14742;
    for (int i = 0; i < 14742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14742(int& val) {
    const int x = 14742;
    std::cout << 14742;
    for (int i = 0; i < 14742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14743(int& val) {
    func_in_14743(val);
    const int x = 14743;
    std::cout << 14743;
    for (int i = 0; i < 14743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14743(int& val) {
    const int x = 14743;
    std::cout << 14743;
    for (int i = 0; i < 14743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14744(int& val) {
    func_in_14744(val);
    const int x = 14744;
    std::cout << 14744;
    for (int i = 0; i < 14744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14744(int& val) {
    const int x = 14744;
    std::cout << 14744;
    for (int i = 0; i < 14744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14745(int& val) {
    func_in_14745(val);
    const int x = 14745;
    std::cout << 14745;
    for (int i = 0; i < 14745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14745(int& val) {
    const int x = 14745;
    std::cout << 14745;
    for (int i = 0; i < 14745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14746(int& val) {
    func_in_14746(val);
    const int x = 14746;
    std::cout << 14746;
    for (int i = 0; i < 14746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14746(int& val) {
    const int x = 14746;
    std::cout << 14746;
    for (int i = 0; i < 14746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14747(int& val) {
    func_in_14747(val);
    const int x = 14747;
    std::cout << 14747;
    for (int i = 0; i < 14747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14747(int& val) {
    const int x = 14747;
    std::cout << 14747;
    for (int i = 0; i < 14747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14748(int& val) {
    func_in_14748(val);
    const int x = 14748;
    std::cout << 14748;
    for (int i = 0; i < 14748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14748(int& val) {
    const int x = 14748;
    std::cout << 14748;
    for (int i = 0; i < 14748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14749(int& val) {
    func_in_14749(val);
    const int x = 14749;
    std::cout << 14749;
    for (int i = 0; i < 14749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14749(int& val) {
    const int x = 14749;
    std::cout << 14749;
    for (int i = 0; i < 14749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14750(int& val) {
    func_in_14750(val);
    const int x = 14750;
    std::cout << 14750;
    for (int i = 0; i < 14750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14750(int& val) {
    const int x = 14750;
    std::cout << 14750;
    for (int i = 0; i < 14750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14751(int& val) {
    func_in_14751(val);
    const int x = 14751;
    std::cout << 14751;
    for (int i = 0; i < 14751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14751(int& val) {
    const int x = 14751;
    std::cout << 14751;
    for (int i = 0; i < 14751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14752(int& val) {
    func_in_14752(val);
    const int x = 14752;
    std::cout << 14752;
    for (int i = 0; i < 14752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14752(int& val) {
    const int x = 14752;
    std::cout << 14752;
    for (int i = 0; i < 14752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14753(int& val) {
    func_in_14753(val);
    const int x = 14753;
    std::cout << 14753;
    for (int i = 0; i < 14753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14753(int& val) {
    const int x = 14753;
    std::cout << 14753;
    for (int i = 0; i < 14753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14754(int& val) {
    func_in_14754(val);
    const int x = 14754;
    std::cout << 14754;
    for (int i = 0; i < 14754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14754(int& val) {
    const int x = 14754;
    std::cout << 14754;
    for (int i = 0; i < 14754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14755(int& val) {
    func_in_14755(val);
    const int x = 14755;
    std::cout << 14755;
    for (int i = 0; i < 14755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14755(int& val) {
    const int x = 14755;
    std::cout << 14755;
    for (int i = 0; i < 14755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14756(int& val) {
    func_in_14756(val);
    const int x = 14756;
    std::cout << 14756;
    for (int i = 0; i < 14756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14756(int& val) {
    const int x = 14756;
    std::cout << 14756;
    for (int i = 0; i < 14756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14757(int& val) {
    func_in_14757(val);
    const int x = 14757;
    std::cout << 14757;
    for (int i = 0; i < 14757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14757(int& val) {
    const int x = 14757;
    std::cout << 14757;
    for (int i = 0; i < 14757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14758(int& val) {
    func_in_14758(val);
    const int x = 14758;
    std::cout << 14758;
    for (int i = 0; i < 14758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14758(int& val) {
    const int x = 14758;
    std::cout << 14758;
    for (int i = 0; i < 14758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14759(int& val) {
    func_in_14759(val);
    const int x = 14759;
    std::cout << 14759;
    for (int i = 0; i < 14759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14759(int& val) {
    const int x = 14759;
    std::cout << 14759;
    for (int i = 0; i < 14759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14760(int& val) {
    func_in_14760(val);
    const int x = 14760;
    std::cout << 14760;
    for (int i = 0; i < 14760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14760(int& val) {
    const int x = 14760;
    std::cout << 14760;
    for (int i = 0; i < 14760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14761(int& val) {
    func_in_14761(val);
    const int x = 14761;
    std::cout << 14761;
    for (int i = 0; i < 14761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14761(int& val) {
    const int x = 14761;
    std::cout << 14761;
    for (int i = 0; i < 14761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14762(int& val) {
    func_in_14762(val);
    const int x = 14762;
    std::cout << 14762;
    for (int i = 0; i < 14762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14762(int& val) {
    const int x = 14762;
    std::cout << 14762;
    for (int i = 0; i < 14762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14763(int& val) {
    func_in_14763(val);
    const int x = 14763;
    std::cout << 14763;
    for (int i = 0; i < 14763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14763(int& val) {
    const int x = 14763;
    std::cout << 14763;
    for (int i = 0; i < 14763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14764(int& val) {
    func_in_14764(val);
    const int x = 14764;
    std::cout << 14764;
    for (int i = 0; i < 14764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14764(int& val) {
    const int x = 14764;
    std::cout << 14764;
    for (int i = 0; i < 14764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14765(int& val) {
    func_in_14765(val);
    const int x = 14765;
    std::cout << 14765;
    for (int i = 0; i < 14765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14765(int& val) {
    const int x = 14765;
    std::cout << 14765;
    for (int i = 0; i < 14765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14766(int& val) {
    func_in_14766(val);
    const int x = 14766;
    std::cout << 14766;
    for (int i = 0; i < 14766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14766(int& val) {
    const int x = 14766;
    std::cout << 14766;
    for (int i = 0; i < 14766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14767(int& val) {
    func_in_14767(val);
    const int x = 14767;
    std::cout << 14767;
    for (int i = 0; i < 14767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14767(int& val) {
    const int x = 14767;
    std::cout << 14767;
    for (int i = 0; i < 14767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14768(int& val) {
    func_in_14768(val);
    const int x = 14768;
    std::cout << 14768;
    for (int i = 0; i < 14768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14768(int& val) {
    const int x = 14768;
    std::cout << 14768;
    for (int i = 0; i < 14768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14769(int& val) {
    func_in_14769(val);
    const int x = 14769;
    std::cout << 14769;
    for (int i = 0; i < 14769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14769(int& val) {
    const int x = 14769;
    std::cout << 14769;
    for (int i = 0; i < 14769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14770(int& val) {
    func_in_14770(val);
    const int x = 14770;
    std::cout << 14770;
    for (int i = 0; i < 14770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14770(int& val) {
    const int x = 14770;
    std::cout << 14770;
    for (int i = 0; i < 14770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14771(int& val) {
    func_in_14771(val);
    const int x = 14771;
    std::cout << 14771;
    for (int i = 0; i < 14771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14771(int& val) {
    const int x = 14771;
    std::cout << 14771;
    for (int i = 0; i < 14771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14772(int& val) {
    func_in_14772(val);
    const int x = 14772;
    std::cout << 14772;
    for (int i = 0; i < 14772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14772(int& val) {
    const int x = 14772;
    std::cout << 14772;
    for (int i = 0; i < 14772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14773(int& val) {
    func_in_14773(val);
    const int x = 14773;
    std::cout << 14773;
    for (int i = 0; i < 14773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14773(int& val) {
    const int x = 14773;
    std::cout << 14773;
    for (int i = 0; i < 14773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14774(int& val) {
    func_in_14774(val);
    const int x = 14774;
    std::cout << 14774;
    for (int i = 0; i < 14774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14774(int& val) {
    const int x = 14774;
    std::cout << 14774;
    for (int i = 0; i < 14774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14775(int& val) {
    func_in_14775(val);
    const int x = 14775;
    std::cout << 14775;
    for (int i = 0; i < 14775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14775(int& val) {
    const int x = 14775;
    std::cout << 14775;
    for (int i = 0; i < 14775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14776(int& val) {
    func_in_14776(val);
    const int x = 14776;
    std::cout << 14776;
    for (int i = 0; i < 14776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14776(int& val) {
    const int x = 14776;
    std::cout << 14776;
    for (int i = 0; i < 14776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14777(int& val) {
    func_in_14777(val);
    const int x = 14777;
    std::cout << 14777;
    for (int i = 0; i < 14777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14777(int& val) {
    const int x = 14777;
    std::cout << 14777;
    for (int i = 0; i < 14777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14778(int& val) {
    func_in_14778(val);
    const int x = 14778;
    std::cout << 14778;
    for (int i = 0; i < 14778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14778(int& val) {
    const int x = 14778;
    std::cout << 14778;
    for (int i = 0; i < 14778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14779(int& val) {
    func_in_14779(val);
    const int x = 14779;
    std::cout << 14779;
    for (int i = 0; i < 14779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14779(int& val) {
    const int x = 14779;
    std::cout << 14779;
    for (int i = 0; i < 14779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14780(int& val) {
    func_in_14780(val);
    const int x = 14780;
    std::cout << 14780;
    for (int i = 0; i < 14780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14780(int& val) {
    const int x = 14780;
    std::cout << 14780;
    for (int i = 0; i < 14780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14781(int& val) {
    func_in_14781(val);
    const int x = 14781;
    std::cout << 14781;
    for (int i = 0; i < 14781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14781(int& val) {
    const int x = 14781;
    std::cout << 14781;
    for (int i = 0; i < 14781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14782(int& val) {
    func_in_14782(val);
    const int x = 14782;
    std::cout << 14782;
    for (int i = 0; i < 14782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14782(int& val) {
    const int x = 14782;
    std::cout << 14782;
    for (int i = 0; i < 14782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14783(int& val) {
    func_in_14783(val);
    const int x = 14783;
    std::cout << 14783;
    for (int i = 0; i < 14783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14783(int& val) {
    const int x = 14783;
    std::cout << 14783;
    for (int i = 0; i < 14783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14784(int& val) {
    func_in_14784(val);
    const int x = 14784;
    std::cout << 14784;
    for (int i = 0; i < 14784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14784(int& val) {
    const int x = 14784;
    std::cout << 14784;
    for (int i = 0; i < 14784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14785(int& val) {
    func_in_14785(val);
    const int x = 14785;
    std::cout << 14785;
    for (int i = 0; i < 14785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14785(int& val) {
    const int x = 14785;
    std::cout << 14785;
    for (int i = 0; i < 14785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14786(int& val) {
    func_in_14786(val);
    const int x = 14786;
    std::cout << 14786;
    for (int i = 0; i < 14786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14786(int& val) {
    const int x = 14786;
    std::cout << 14786;
    for (int i = 0; i < 14786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14787(int& val) {
    func_in_14787(val);
    const int x = 14787;
    std::cout << 14787;
    for (int i = 0; i < 14787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14787(int& val) {
    const int x = 14787;
    std::cout << 14787;
    for (int i = 0; i < 14787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14788(int& val) {
    func_in_14788(val);
    const int x = 14788;
    std::cout << 14788;
    for (int i = 0; i < 14788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14788(int& val) {
    const int x = 14788;
    std::cout << 14788;
    for (int i = 0; i < 14788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14789(int& val) {
    func_in_14789(val);
    const int x = 14789;
    std::cout << 14789;
    for (int i = 0; i < 14789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14789(int& val) {
    const int x = 14789;
    std::cout << 14789;
    for (int i = 0; i < 14789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14790(int& val) {
    func_in_14790(val);
    const int x = 14790;
    std::cout << 14790;
    for (int i = 0; i < 14790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14790(int& val) {
    const int x = 14790;
    std::cout << 14790;
    for (int i = 0; i < 14790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14791(int& val) {
    func_in_14791(val);
    const int x = 14791;
    std::cout << 14791;
    for (int i = 0; i < 14791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14791(int& val) {
    const int x = 14791;
    std::cout << 14791;
    for (int i = 0; i < 14791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14792(int& val) {
    func_in_14792(val);
    const int x = 14792;
    std::cout << 14792;
    for (int i = 0; i < 14792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14792(int& val) {
    const int x = 14792;
    std::cout << 14792;
    for (int i = 0; i < 14792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14793(int& val) {
    func_in_14793(val);
    const int x = 14793;
    std::cout << 14793;
    for (int i = 0; i < 14793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14793(int& val) {
    const int x = 14793;
    std::cout << 14793;
    for (int i = 0; i < 14793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14794(int& val) {
    func_in_14794(val);
    const int x = 14794;
    std::cout << 14794;
    for (int i = 0; i < 14794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14794(int& val) {
    const int x = 14794;
    std::cout << 14794;
    for (int i = 0; i < 14794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14795(int& val) {
    func_in_14795(val);
    const int x = 14795;
    std::cout << 14795;
    for (int i = 0; i < 14795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14795(int& val) {
    const int x = 14795;
    std::cout << 14795;
    for (int i = 0; i < 14795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14796(int& val) {
    func_in_14796(val);
    const int x = 14796;
    std::cout << 14796;
    for (int i = 0; i < 14796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14796(int& val) {
    const int x = 14796;
    std::cout << 14796;
    for (int i = 0; i < 14796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14797(int& val) {
    func_in_14797(val);
    const int x = 14797;
    std::cout << 14797;
    for (int i = 0; i < 14797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14797(int& val) {
    const int x = 14797;
    std::cout << 14797;
    for (int i = 0; i < 14797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14798(int& val) {
    func_in_14798(val);
    const int x = 14798;
    std::cout << 14798;
    for (int i = 0; i < 14798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14798(int& val) {
    const int x = 14798;
    std::cout << 14798;
    for (int i = 0; i < 14798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14799(int& val) {
    func_in_14799(val);
    const int x = 14799;
    std::cout << 14799;
    for (int i = 0; i < 14799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14799(int& val) {
    const int x = 14799;
    std::cout << 14799;
    for (int i = 0; i < 14799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14800(int& val) {
    func_in_14800(val);
    const int x = 14800;
    std::cout << 14800;
    for (int i = 0; i < 14800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14800(int& val) {
    const int x = 14800;
    std::cout << 14800;
    for (int i = 0; i < 14800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14801(int& val) {
    func_in_14801(val);
    const int x = 14801;
    std::cout << 14801;
    for (int i = 0; i < 14801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14801(int& val) {
    const int x = 14801;
    std::cout << 14801;
    for (int i = 0; i < 14801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14802(int& val) {
    func_in_14802(val);
    const int x = 14802;
    std::cout << 14802;
    for (int i = 0; i < 14802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14802(int& val) {
    const int x = 14802;
    std::cout << 14802;
    for (int i = 0; i < 14802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14803(int& val) {
    func_in_14803(val);
    const int x = 14803;
    std::cout << 14803;
    for (int i = 0; i < 14803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14803(int& val) {
    const int x = 14803;
    std::cout << 14803;
    for (int i = 0; i < 14803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14804(int& val) {
    func_in_14804(val);
    const int x = 14804;
    std::cout << 14804;
    for (int i = 0; i < 14804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14804(int& val) {
    const int x = 14804;
    std::cout << 14804;
    for (int i = 0; i < 14804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14805(int& val) {
    func_in_14805(val);
    const int x = 14805;
    std::cout << 14805;
    for (int i = 0; i < 14805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14805(int& val) {
    const int x = 14805;
    std::cout << 14805;
    for (int i = 0; i < 14805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14806(int& val) {
    func_in_14806(val);
    const int x = 14806;
    std::cout << 14806;
    for (int i = 0; i < 14806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14806(int& val) {
    const int x = 14806;
    std::cout << 14806;
    for (int i = 0; i < 14806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14807(int& val) {
    func_in_14807(val);
    const int x = 14807;
    std::cout << 14807;
    for (int i = 0; i < 14807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14807(int& val) {
    const int x = 14807;
    std::cout << 14807;
    for (int i = 0; i < 14807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14808(int& val) {
    func_in_14808(val);
    const int x = 14808;
    std::cout << 14808;
    for (int i = 0; i < 14808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14808(int& val) {
    const int x = 14808;
    std::cout << 14808;
    for (int i = 0; i < 14808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14809(int& val) {
    func_in_14809(val);
    const int x = 14809;
    std::cout << 14809;
    for (int i = 0; i < 14809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14809(int& val) {
    const int x = 14809;
    std::cout << 14809;
    for (int i = 0; i < 14809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14810(int& val) {
    func_in_14810(val);
    const int x = 14810;
    std::cout << 14810;
    for (int i = 0; i < 14810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14810(int& val) {
    const int x = 14810;
    std::cout << 14810;
    for (int i = 0; i < 14810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14811(int& val) {
    func_in_14811(val);
    const int x = 14811;
    std::cout << 14811;
    for (int i = 0; i < 14811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14811(int& val) {
    const int x = 14811;
    std::cout << 14811;
    for (int i = 0; i < 14811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14812(int& val) {
    func_in_14812(val);
    const int x = 14812;
    std::cout << 14812;
    for (int i = 0; i < 14812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14812(int& val) {
    const int x = 14812;
    std::cout << 14812;
    for (int i = 0; i < 14812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14813(int& val) {
    func_in_14813(val);
    const int x = 14813;
    std::cout << 14813;
    for (int i = 0; i < 14813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14813(int& val) {
    const int x = 14813;
    std::cout << 14813;
    for (int i = 0; i < 14813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14814(int& val) {
    func_in_14814(val);
    const int x = 14814;
    std::cout << 14814;
    for (int i = 0; i < 14814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14814(int& val) {
    const int x = 14814;
    std::cout << 14814;
    for (int i = 0; i < 14814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14815(int& val) {
    func_in_14815(val);
    const int x = 14815;
    std::cout << 14815;
    for (int i = 0; i < 14815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14815(int& val) {
    const int x = 14815;
    std::cout << 14815;
    for (int i = 0; i < 14815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14816(int& val) {
    func_in_14816(val);
    const int x = 14816;
    std::cout << 14816;
    for (int i = 0; i < 14816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14816(int& val) {
    const int x = 14816;
    std::cout << 14816;
    for (int i = 0; i < 14816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14817(int& val) {
    func_in_14817(val);
    const int x = 14817;
    std::cout << 14817;
    for (int i = 0; i < 14817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14817(int& val) {
    const int x = 14817;
    std::cout << 14817;
    for (int i = 0; i < 14817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14818(int& val) {
    func_in_14818(val);
    const int x = 14818;
    std::cout << 14818;
    for (int i = 0; i < 14818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14818(int& val) {
    const int x = 14818;
    std::cout << 14818;
    for (int i = 0; i < 14818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14819(int& val) {
    func_in_14819(val);
    const int x = 14819;
    std::cout << 14819;
    for (int i = 0; i < 14819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14819(int& val) {
    const int x = 14819;
    std::cout << 14819;
    for (int i = 0; i < 14819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14820(int& val) {
    func_in_14820(val);
    const int x = 14820;
    std::cout << 14820;
    for (int i = 0; i < 14820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14820(int& val) {
    const int x = 14820;
    std::cout << 14820;
    for (int i = 0; i < 14820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14821(int& val) {
    func_in_14821(val);
    const int x = 14821;
    std::cout << 14821;
    for (int i = 0; i < 14821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14821(int& val) {
    const int x = 14821;
    std::cout << 14821;
    for (int i = 0; i < 14821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14822(int& val) {
    func_in_14822(val);
    const int x = 14822;
    std::cout << 14822;
    for (int i = 0; i < 14822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14822(int& val) {
    const int x = 14822;
    std::cout << 14822;
    for (int i = 0; i < 14822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14823(int& val) {
    func_in_14823(val);
    const int x = 14823;
    std::cout << 14823;
    for (int i = 0; i < 14823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14823(int& val) {
    const int x = 14823;
    std::cout << 14823;
    for (int i = 0; i < 14823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14824(int& val) {
    func_in_14824(val);
    const int x = 14824;
    std::cout << 14824;
    for (int i = 0; i < 14824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14824(int& val) {
    const int x = 14824;
    std::cout << 14824;
    for (int i = 0; i < 14824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14825(int& val) {
    func_in_14825(val);
    const int x = 14825;
    std::cout << 14825;
    for (int i = 0; i < 14825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14825(int& val) {
    const int x = 14825;
    std::cout << 14825;
    for (int i = 0; i < 14825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14826(int& val) {
    func_in_14826(val);
    const int x = 14826;
    std::cout << 14826;
    for (int i = 0; i < 14826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14826(int& val) {
    const int x = 14826;
    std::cout << 14826;
    for (int i = 0; i < 14826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14827(int& val) {
    func_in_14827(val);
    const int x = 14827;
    std::cout << 14827;
    for (int i = 0; i < 14827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14827(int& val) {
    const int x = 14827;
    std::cout << 14827;
    for (int i = 0; i < 14827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14828(int& val) {
    func_in_14828(val);
    const int x = 14828;
    std::cout << 14828;
    for (int i = 0; i < 14828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14828(int& val) {
    const int x = 14828;
    std::cout << 14828;
    for (int i = 0; i < 14828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14829(int& val) {
    func_in_14829(val);
    const int x = 14829;
    std::cout << 14829;
    for (int i = 0; i < 14829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14829(int& val) {
    const int x = 14829;
    std::cout << 14829;
    for (int i = 0; i < 14829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14830(int& val) {
    func_in_14830(val);
    const int x = 14830;
    std::cout << 14830;
    for (int i = 0; i < 14830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14830(int& val) {
    const int x = 14830;
    std::cout << 14830;
    for (int i = 0; i < 14830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14831(int& val) {
    func_in_14831(val);
    const int x = 14831;
    std::cout << 14831;
    for (int i = 0; i < 14831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14831(int& val) {
    const int x = 14831;
    std::cout << 14831;
    for (int i = 0; i < 14831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14832(int& val) {
    func_in_14832(val);
    const int x = 14832;
    std::cout << 14832;
    for (int i = 0; i < 14832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14832(int& val) {
    const int x = 14832;
    std::cout << 14832;
    for (int i = 0; i < 14832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14833(int& val) {
    func_in_14833(val);
    const int x = 14833;
    std::cout << 14833;
    for (int i = 0; i < 14833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14833(int& val) {
    const int x = 14833;
    std::cout << 14833;
    for (int i = 0; i < 14833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14834(int& val) {
    func_in_14834(val);
    const int x = 14834;
    std::cout << 14834;
    for (int i = 0; i < 14834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14834(int& val) {
    const int x = 14834;
    std::cout << 14834;
    for (int i = 0; i < 14834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14835(int& val) {
    func_in_14835(val);
    const int x = 14835;
    std::cout << 14835;
    for (int i = 0; i < 14835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14835(int& val) {
    const int x = 14835;
    std::cout << 14835;
    for (int i = 0; i < 14835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14836(int& val) {
    func_in_14836(val);
    const int x = 14836;
    std::cout << 14836;
    for (int i = 0; i < 14836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14836(int& val) {
    const int x = 14836;
    std::cout << 14836;
    for (int i = 0; i < 14836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14837(int& val) {
    func_in_14837(val);
    const int x = 14837;
    std::cout << 14837;
    for (int i = 0; i < 14837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14837(int& val) {
    const int x = 14837;
    std::cout << 14837;
    for (int i = 0; i < 14837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14838(int& val) {
    func_in_14838(val);
    const int x = 14838;
    std::cout << 14838;
    for (int i = 0; i < 14838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14838(int& val) {
    const int x = 14838;
    std::cout << 14838;
    for (int i = 0; i < 14838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14839(int& val) {
    func_in_14839(val);
    const int x = 14839;
    std::cout << 14839;
    for (int i = 0; i < 14839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14839(int& val) {
    const int x = 14839;
    std::cout << 14839;
    for (int i = 0; i < 14839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14840(int& val) {
    func_in_14840(val);
    const int x = 14840;
    std::cout << 14840;
    for (int i = 0; i < 14840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14840(int& val) {
    const int x = 14840;
    std::cout << 14840;
    for (int i = 0; i < 14840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14841(int& val) {
    func_in_14841(val);
    const int x = 14841;
    std::cout << 14841;
    for (int i = 0; i < 14841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14841(int& val) {
    const int x = 14841;
    std::cout << 14841;
    for (int i = 0; i < 14841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14842(int& val) {
    func_in_14842(val);
    const int x = 14842;
    std::cout << 14842;
    for (int i = 0; i < 14842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14842(int& val) {
    const int x = 14842;
    std::cout << 14842;
    for (int i = 0; i < 14842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14843(int& val) {
    func_in_14843(val);
    const int x = 14843;
    std::cout << 14843;
    for (int i = 0; i < 14843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14843(int& val) {
    const int x = 14843;
    std::cout << 14843;
    for (int i = 0; i < 14843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14844(int& val) {
    func_in_14844(val);
    const int x = 14844;
    std::cout << 14844;
    for (int i = 0; i < 14844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14844(int& val) {
    const int x = 14844;
    std::cout << 14844;
    for (int i = 0; i < 14844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14845(int& val) {
    func_in_14845(val);
    const int x = 14845;
    std::cout << 14845;
    for (int i = 0; i < 14845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14845(int& val) {
    const int x = 14845;
    std::cout << 14845;
    for (int i = 0; i < 14845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14846(int& val) {
    func_in_14846(val);
    const int x = 14846;
    std::cout << 14846;
    for (int i = 0; i < 14846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14846(int& val) {
    const int x = 14846;
    std::cout << 14846;
    for (int i = 0; i < 14846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14847(int& val) {
    func_in_14847(val);
    const int x = 14847;
    std::cout << 14847;
    for (int i = 0; i < 14847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14847(int& val) {
    const int x = 14847;
    std::cout << 14847;
    for (int i = 0; i < 14847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14848(int& val) {
    func_in_14848(val);
    const int x = 14848;
    std::cout << 14848;
    for (int i = 0; i < 14848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14848(int& val) {
    const int x = 14848;
    std::cout << 14848;
    for (int i = 0; i < 14848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14849(int& val) {
    func_in_14849(val);
    const int x = 14849;
    std::cout << 14849;
    for (int i = 0; i < 14849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14849(int& val) {
    const int x = 14849;
    std::cout << 14849;
    for (int i = 0; i < 14849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14850(int& val) {
    func_in_14850(val);
    const int x = 14850;
    std::cout << 14850;
    for (int i = 0; i < 14850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14850(int& val) {
    const int x = 14850;
    std::cout << 14850;
    for (int i = 0; i < 14850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14851(int& val) {
    func_in_14851(val);
    const int x = 14851;
    std::cout << 14851;
    for (int i = 0; i < 14851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14851(int& val) {
    const int x = 14851;
    std::cout << 14851;
    for (int i = 0; i < 14851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14852(int& val) {
    func_in_14852(val);
    const int x = 14852;
    std::cout << 14852;
    for (int i = 0; i < 14852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14852(int& val) {
    const int x = 14852;
    std::cout << 14852;
    for (int i = 0; i < 14852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14853(int& val) {
    func_in_14853(val);
    const int x = 14853;
    std::cout << 14853;
    for (int i = 0; i < 14853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14853(int& val) {
    const int x = 14853;
    std::cout << 14853;
    for (int i = 0; i < 14853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14854(int& val) {
    func_in_14854(val);
    const int x = 14854;
    std::cout << 14854;
    for (int i = 0; i < 14854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14854(int& val) {
    const int x = 14854;
    std::cout << 14854;
    for (int i = 0; i < 14854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14855(int& val) {
    func_in_14855(val);
    const int x = 14855;
    std::cout << 14855;
    for (int i = 0; i < 14855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14855(int& val) {
    const int x = 14855;
    std::cout << 14855;
    for (int i = 0; i < 14855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14856(int& val) {
    func_in_14856(val);
    const int x = 14856;
    std::cout << 14856;
    for (int i = 0; i < 14856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14856(int& val) {
    const int x = 14856;
    std::cout << 14856;
    for (int i = 0; i < 14856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14857(int& val) {
    func_in_14857(val);
    const int x = 14857;
    std::cout << 14857;
    for (int i = 0; i < 14857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14857(int& val) {
    const int x = 14857;
    std::cout << 14857;
    for (int i = 0; i < 14857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14858(int& val) {
    func_in_14858(val);
    const int x = 14858;
    std::cout << 14858;
    for (int i = 0; i < 14858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14858(int& val) {
    const int x = 14858;
    std::cout << 14858;
    for (int i = 0; i < 14858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14859(int& val) {
    func_in_14859(val);
    const int x = 14859;
    std::cout << 14859;
    for (int i = 0; i < 14859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14859(int& val) {
    const int x = 14859;
    std::cout << 14859;
    for (int i = 0; i < 14859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14860(int& val) {
    func_in_14860(val);
    const int x = 14860;
    std::cout << 14860;
    for (int i = 0; i < 14860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14860(int& val) {
    const int x = 14860;
    std::cout << 14860;
    for (int i = 0; i < 14860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14861(int& val) {
    func_in_14861(val);
    const int x = 14861;
    std::cout << 14861;
    for (int i = 0; i < 14861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14861(int& val) {
    const int x = 14861;
    std::cout << 14861;
    for (int i = 0; i < 14861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14862(int& val) {
    func_in_14862(val);
    const int x = 14862;
    std::cout << 14862;
    for (int i = 0; i < 14862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14862(int& val) {
    const int x = 14862;
    std::cout << 14862;
    for (int i = 0; i < 14862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14863(int& val) {
    func_in_14863(val);
    const int x = 14863;
    std::cout << 14863;
    for (int i = 0; i < 14863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14863(int& val) {
    const int x = 14863;
    std::cout << 14863;
    for (int i = 0; i < 14863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14864(int& val) {
    func_in_14864(val);
    const int x = 14864;
    std::cout << 14864;
    for (int i = 0; i < 14864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14864(int& val) {
    const int x = 14864;
    std::cout << 14864;
    for (int i = 0; i < 14864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14865(int& val) {
    func_in_14865(val);
    const int x = 14865;
    std::cout << 14865;
    for (int i = 0; i < 14865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14865(int& val) {
    const int x = 14865;
    std::cout << 14865;
    for (int i = 0; i < 14865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14866(int& val) {
    func_in_14866(val);
    const int x = 14866;
    std::cout << 14866;
    for (int i = 0; i < 14866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14866(int& val) {
    const int x = 14866;
    std::cout << 14866;
    for (int i = 0; i < 14866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14867(int& val) {
    func_in_14867(val);
    const int x = 14867;
    std::cout << 14867;
    for (int i = 0; i < 14867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14867(int& val) {
    const int x = 14867;
    std::cout << 14867;
    for (int i = 0; i < 14867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14868(int& val) {
    func_in_14868(val);
    const int x = 14868;
    std::cout << 14868;
    for (int i = 0; i < 14868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14868(int& val) {
    const int x = 14868;
    std::cout << 14868;
    for (int i = 0; i < 14868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14869(int& val) {
    func_in_14869(val);
    const int x = 14869;
    std::cout << 14869;
    for (int i = 0; i < 14869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14869(int& val) {
    const int x = 14869;
    std::cout << 14869;
    for (int i = 0; i < 14869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14870(int& val) {
    func_in_14870(val);
    const int x = 14870;
    std::cout << 14870;
    for (int i = 0; i < 14870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14870(int& val) {
    const int x = 14870;
    std::cout << 14870;
    for (int i = 0; i < 14870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14871(int& val) {
    func_in_14871(val);
    const int x = 14871;
    std::cout << 14871;
    for (int i = 0; i < 14871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14871(int& val) {
    const int x = 14871;
    std::cout << 14871;
    for (int i = 0; i < 14871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14872(int& val) {
    func_in_14872(val);
    const int x = 14872;
    std::cout << 14872;
    for (int i = 0; i < 14872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14872(int& val) {
    const int x = 14872;
    std::cout << 14872;
    for (int i = 0; i < 14872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14873(int& val) {
    func_in_14873(val);
    const int x = 14873;
    std::cout << 14873;
    for (int i = 0; i < 14873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14873(int& val) {
    const int x = 14873;
    std::cout << 14873;
    for (int i = 0; i < 14873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14874(int& val) {
    func_in_14874(val);
    const int x = 14874;
    std::cout << 14874;
    for (int i = 0; i < 14874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14874(int& val) {
    const int x = 14874;
    std::cout << 14874;
    for (int i = 0; i < 14874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14875(int& val) {
    func_in_14875(val);
    const int x = 14875;
    std::cout << 14875;
    for (int i = 0; i < 14875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14875(int& val) {
    const int x = 14875;
    std::cout << 14875;
    for (int i = 0; i < 14875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14876(int& val) {
    func_in_14876(val);
    const int x = 14876;
    std::cout << 14876;
    for (int i = 0; i < 14876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14876(int& val) {
    const int x = 14876;
    std::cout << 14876;
    for (int i = 0; i < 14876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14877(int& val) {
    func_in_14877(val);
    const int x = 14877;
    std::cout << 14877;
    for (int i = 0; i < 14877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14877(int& val) {
    const int x = 14877;
    std::cout << 14877;
    for (int i = 0; i < 14877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14878(int& val) {
    func_in_14878(val);
    const int x = 14878;
    std::cout << 14878;
    for (int i = 0; i < 14878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14878(int& val) {
    const int x = 14878;
    std::cout << 14878;
    for (int i = 0; i < 14878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14879(int& val) {
    func_in_14879(val);
    const int x = 14879;
    std::cout << 14879;
    for (int i = 0; i < 14879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14879(int& val) {
    const int x = 14879;
    std::cout << 14879;
    for (int i = 0; i < 14879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14880(int& val) {
    func_in_14880(val);
    const int x = 14880;
    std::cout << 14880;
    for (int i = 0; i < 14880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14880(int& val) {
    const int x = 14880;
    std::cout << 14880;
    for (int i = 0; i < 14880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14881(int& val) {
    func_in_14881(val);
    const int x = 14881;
    std::cout << 14881;
    for (int i = 0; i < 14881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14881(int& val) {
    const int x = 14881;
    std::cout << 14881;
    for (int i = 0; i < 14881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14882(int& val) {
    func_in_14882(val);
    const int x = 14882;
    std::cout << 14882;
    for (int i = 0; i < 14882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14882(int& val) {
    const int x = 14882;
    std::cout << 14882;
    for (int i = 0; i < 14882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14883(int& val) {
    func_in_14883(val);
    const int x = 14883;
    std::cout << 14883;
    for (int i = 0; i < 14883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14883(int& val) {
    const int x = 14883;
    std::cout << 14883;
    for (int i = 0; i < 14883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14884(int& val) {
    func_in_14884(val);
    const int x = 14884;
    std::cout << 14884;
    for (int i = 0; i < 14884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14884(int& val) {
    const int x = 14884;
    std::cout << 14884;
    for (int i = 0; i < 14884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14885(int& val) {
    func_in_14885(val);
    const int x = 14885;
    std::cout << 14885;
    for (int i = 0; i < 14885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14885(int& val) {
    const int x = 14885;
    std::cout << 14885;
    for (int i = 0; i < 14885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14886(int& val) {
    func_in_14886(val);
    const int x = 14886;
    std::cout << 14886;
    for (int i = 0; i < 14886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14886(int& val) {
    const int x = 14886;
    std::cout << 14886;
    for (int i = 0; i < 14886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14887(int& val) {
    func_in_14887(val);
    const int x = 14887;
    std::cout << 14887;
    for (int i = 0; i < 14887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14887(int& val) {
    const int x = 14887;
    std::cout << 14887;
    for (int i = 0; i < 14887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14888(int& val) {
    func_in_14888(val);
    const int x = 14888;
    std::cout << 14888;
    for (int i = 0; i < 14888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14888(int& val) {
    const int x = 14888;
    std::cout << 14888;
    for (int i = 0; i < 14888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14889(int& val) {
    func_in_14889(val);
    const int x = 14889;
    std::cout << 14889;
    for (int i = 0; i < 14889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14889(int& val) {
    const int x = 14889;
    std::cout << 14889;
    for (int i = 0; i < 14889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14890(int& val) {
    func_in_14890(val);
    const int x = 14890;
    std::cout << 14890;
    for (int i = 0; i < 14890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14890(int& val) {
    const int x = 14890;
    std::cout << 14890;
    for (int i = 0; i < 14890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14891(int& val) {
    func_in_14891(val);
    const int x = 14891;
    std::cout << 14891;
    for (int i = 0; i < 14891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14891(int& val) {
    const int x = 14891;
    std::cout << 14891;
    for (int i = 0; i < 14891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14892(int& val) {
    func_in_14892(val);
    const int x = 14892;
    std::cout << 14892;
    for (int i = 0; i < 14892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14892(int& val) {
    const int x = 14892;
    std::cout << 14892;
    for (int i = 0; i < 14892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14893(int& val) {
    func_in_14893(val);
    const int x = 14893;
    std::cout << 14893;
    for (int i = 0; i < 14893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14893(int& val) {
    const int x = 14893;
    std::cout << 14893;
    for (int i = 0; i < 14893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14894(int& val) {
    func_in_14894(val);
    const int x = 14894;
    std::cout << 14894;
    for (int i = 0; i < 14894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14894(int& val) {
    const int x = 14894;
    std::cout << 14894;
    for (int i = 0; i < 14894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14895(int& val) {
    func_in_14895(val);
    const int x = 14895;
    std::cout << 14895;
    for (int i = 0; i < 14895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14895(int& val) {
    const int x = 14895;
    std::cout << 14895;
    for (int i = 0; i < 14895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14896(int& val) {
    func_in_14896(val);
    const int x = 14896;
    std::cout << 14896;
    for (int i = 0; i < 14896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14896(int& val) {
    const int x = 14896;
    std::cout << 14896;
    for (int i = 0; i < 14896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14897(int& val) {
    func_in_14897(val);
    const int x = 14897;
    std::cout << 14897;
    for (int i = 0; i < 14897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14897(int& val) {
    const int x = 14897;
    std::cout << 14897;
    for (int i = 0; i < 14897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14898(int& val) {
    func_in_14898(val);
    const int x = 14898;
    std::cout << 14898;
    for (int i = 0; i < 14898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14898(int& val) {
    const int x = 14898;
    std::cout << 14898;
    for (int i = 0; i < 14898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14899(int& val) {
    func_in_14899(val);
    const int x = 14899;
    std::cout << 14899;
    for (int i = 0; i < 14899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14899(int& val) {
    const int x = 14899;
    std::cout << 14899;
    for (int i = 0; i < 14899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14900(int& val) {
    func_in_14900(val);
    const int x = 14900;
    std::cout << 14900;
    for (int i = 0; i < 14900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14900(int& val) {
    const int x = 14900;
    std::cout << 14900;
    for (int i = 0; i < 14900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14901(int& val) {
    func_in_14901(val);
    const int x = 14901;
    std::cout << 14901;
    for (int i = 0; i < 14901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14901(int& val) {
    const int x = 14901;
    std::cout << 14901;
    for (int i = 0; i < 14901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14902(int& val) {
    func_in_14902(val);
    const int x = 14902;
    std::cout << 14902;
    for (int i = 0; i < 14902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14902(int& val) {
    const int x = 14902;
    std::cout << 14902;
    for (int i = 0; i < 14902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14903(int& val) {
    func_in_14903(val);
    const int x = 14903;
    std::cout << 14903;
    for (int i = 0; i < 14903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14903(int& val) {
    const int x = 14903;
    std::cout << 14903;
    for (int i = 0; i < 14903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14904(int& val) {
    func_in_14904(val);
    const int x = 14904;
    std::cout << 14904;
    for (int i = 0; i < 14904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14904(int& val) {
    const int x = 14904;
    std::cout << 14904;
    for (int i = 0; i < 14904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14905(int& val) {
    func_in_14905(val);
    const int x = 14905;
    std::cout << 14905;
    for (int i = 0; i < 14905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14905(int& val) {
    const int x = 14905;
    std::cout << 14905;
    for (int i = 0; i < 14905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14906(int& val) {
    func_in_14906(val);
    const int x = 14906;
    std::cout << 14906;
    for (int i = 0; i < 14906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14906(int& val) {
    const int x = 14906;
    std::cout << 14906;
    for (int i = 0; i < 14906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14907(int& val) {
    func_in_14907(val);
    const int x = 14907;
    std::cout << 14907;
    for (int i = 0; i < 14907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14907(int& val) {
    const int x = 14907;
    std::cout << 14907;
    for (int i = 0; i < 14907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14908(int& val) {
    func_in_14908(val);
    const int x = 14908;
    std::cout << 14908;
    for (int i = 0; i < 14908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14908(int& val) {
    const int x = 14908;
    std::cout << 14908;
    for (int i = 0; i < 14908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14909(int& val) {
    func_in_14909(val);
    const int x = 14909;
    std::cout << 14909;
    for (int i = 0; i < 14909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14909(int& val) {
    const int x = 14909;
    std::cout << 14909;
    for (int i = 0; i < 14909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14910(int& val) {
    func_in_14910(val);
    const int x = 14910;
    std::cout << 14910;
    for (int i = 0; i < 14910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14910(int& val) {
    const int x = 14910;
    std::cout << 14910;
    for (int i = 0; i < 14910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14911(int& val) {
    func_in_14911(val);
    const int x = 14911;
    std::cout << 14911;
    for (int i = 0; i < 14911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14911(int& val) {
    const int x = 14911;
    std::cout << 14911;
    for (int i = 0; i < 14911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14912(int& val) {
    func_in_14912(val);
    const int x = 14912;
    std::cout << 14912;
    for (int i = 0; i < 14912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14912(int& val) {
    const int x = 14912;
    std::cout << 14912;
    for (int i = 0; i < 14912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14913(int& val) {
    func_in_14913(val);
    const int x = 14913;
    std::cout << 14913;
    for (int i = 0; i < 14913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14913(int& val) {
    const int x = 14913;
    std::cout << 14913;
    for (int i = 0; i < 14913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14914(int& val) {
    func_in_14914(val);
    const int x = 14914;
    std::cout << 14914;
    for (int i = 0; i < 14914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14914(int& val) {
    const int x = 14914;
    std::cout << 14914;
    for (int i = 0; i < 14914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14915(int& val) {
    func_in_14915(val);
    const int x = 14915;
    std::cout << 14915;
    for (int i = 0; i < 14915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14915(int& val) {
    const int x = 14915;
    std::cout << 14915;
    for (int i = 0; i < 14915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14916(int& val) {
    func_in_14916(val);
    const int x = 14916;
    std::cout << 14916;
    for (int i = 0; i < 14916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14916(int& val) {
    const int x = 14916;
    std::cout << 14916;
    for (int i = 0; i < 14916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14917(int& val) {
    func_in_14917(val);
    const int x = 14917;
    std::cout << 14917;
    for (int i = 0; i < 14917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14917(int& val) {
    const int x = 14917;
    std::cout << 14917;
    for (int i = 0; i < 14917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14918(int& val) {
    func_in_14918(val);
    const int x = 14918;
    std::cout << 14918;
    for (int i = 0; i < 14918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14918(int& val) {
    const int x = 14918;
    std::cout << 14918;
    for (int i = 0; i < 14918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14919(int& val) {
    func_in_14919(val);
    const int x = 14919;
    std::cout << 14919;
    for (int i = 0; i < 14919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14919(int& val) {
    const int x = 14919;
    std::cout << 14919;
    for (int i = 0; i < 14919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14920(int& val) {
    func_in_14920(val);
    const int x = 14920;
    std::cout << 14920;
    for (int i = 0; i < 14920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14920(int& val) {
    const int x = 14920;
    std::cout << 14920;
    for (int i = 0; i < 14920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14921(int& val) {
    func_in_14921(val);
    const int x = 14921;
    std::cout << 14921;
    for (int i = 0; i < 14921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14921(int& val) {
    const int x = 14921;
    std::cout << 14921;
    for (int i = 0; i < 14921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14922(int& val) {
    func_in_14922(val);
    const int x = 14922;
    std::cout << 14922;
    for (int i = 0; i < 14922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14922(int& val) {
    const int x = 14922;
    std::cout << 14922;
    for (int i = 0; i < 14922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14923(int& val) {
    func_in_14923(val);
    const int x = 14923;
    std::cout << 14923;
    for (int i = 0; i < 14923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14923(int& val) {
    const int x = 14923;
    std::cout << 14923;
    for (int i = 0; i < 14923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14924(int& val) {
    func_in_14924(val);
    const int x = 14924;
    std::cout << 14924;
    for (int i = 0; i < 14924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14924(int& val) {
    const int x = 14924;
    std::cout << 14924;
    for (int i = 0; i < 14924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14925(int& val) {
    func_in_14925(val);
    const int x = 14925;
    std::cout << 14925;
    for (int i = 0; i < 14925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14925(int& val) {
    const int x = 14925;
    std::cout << 14925;
    for (int i = 0; i < 14925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14926(int& val) {
    func_in_14926(val);
    const int x = 14926;
    std::cout << 14926;
    for (int i = 0; i < 14926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14926(int& val) {
    const int x = 14926;
    std::cout << 14926;
    for (int i = 0; i < 14926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14927(int& val) {
    func_in_14927(val);
    const int x = 14927;
    std::cout << 14927;
    for (int i = 0; i < 14927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14927(int& val) {
    const int x = 14927;
    std::cout << 14927;
    for (int i = 0; i < 14927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14928(int& val) {
    func_in_14928(val);
    const int x = 14928;
    std::cout << 14928;
    for (int i = 0; i < 14928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14928(int& val) {
    const int x = 14928;
    std::cout << 14928;
    for (int i = 0; i < 14928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14929(int& val) {
    func_in_14929(val);
    const int x = 14929;
    std::cout << 14929;
    for (int i = 0; i < 14929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14929(int& val) {
    const int x = 14929;
    std::cout << 14929;
    for (int i = 0; i < 14929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14930(int& val) {
    func_in_14930(val);
    const int x = 14930;
    std::cout << 14930;
    for (int i = 0; i < 14930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14930(int& val) {
    const int x = 14930;
    std::cout << 14930;
    for (int i = 0; i < 14930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14931(int& val) {
    func_in_14931(val);
    const int x = 14931;
    std::cout << 14931;
    for (int i = 0; i < 14931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14931(int& val) {
    const int x = 14931;
    std::cout << 14931;
    for (int i = 0; i < 14931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14932(int& val) {
    func_in_14932(val);
    const int x = 14932;
    std::cout << 14932;
    for (int i = 0; i < 14932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14932(int& val) {
    const int x = 14932;
    std::cout << 14932;
    for (int i = 0; i < 14932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14933(int& val) {
    func_in_14933(val);
    const int x = 14933;
    std::cout << 14933;
    for (int i = 0; i < 14933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14933(int& val) {
    const int x = 14933;
    std::cout << 14933;
    for (int i = 0; i < 14933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14934(int& val) {
    func_in_14934(val);
    const int x = 14934;
    std::cout << 14934;
    for (int i = 0; i < 14934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14934(int& val) {
    const int x = 14934;
    std::cout << 14934;
    for (int i = 0; i < 14934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14935(int& val) {
    func_in_14935(val);
    const int x = 14935;
    std::cout << 14935;
    for (int i = 0; i < 14935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14935(int& val) {
    const int x = 14935;
    std::cout << 14935;
    for (int i = 0; i < 14935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14936(int& val) {
    func_in_14936(val);
    const int x = 14936;
    std::cout << 14936;
    for (int i = 0; i < 14936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14936(int& val) {
    const int x = 14936;
    std::cout << 14936;
    for (int i = 0; i < 14936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14937(int& val) {
    func_in_14937(val);
    const int x = 14937;
    std::cout << 14937;
    for (int i = 0; i < 14937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14937(int& val) {
    const int x = 14937;
    std::cout << 14937;
    for (int i = 0; i < 14937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14938(int& val) {
    func_in_14938(val);
    const int x = 14938;
    std::cout << 14938;
    for (int i = 0; i < 14938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14938(int& val) {
    const int x = 14938;
    std::cout << 14938;
    for (int i = 0; i < 14938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14939(int& val) {
    func_in_14939(val);
    const int x = 14939;
    std::cout << 14939;
    for (int i = 0; i < 14939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14939(int& val) {
    const int x = 14939;
    std::cout << 14939;
    for (int i = 0; i < 14939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14940(int& val) {
    func_in_14940(val);
    const int x = 14940;
    std::cout << 14940;
    for (int i = 0; i < 14940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14940(int& val) {
    const int x = 14940;
    std::cout << 14940;
    for (int i = 0; i < 14940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14941(int& val) {
    func_in_14941(val);
    const int x = 14941;
    std::cout << 14941;
    for (int i = 0; i < 14941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14941(int& val) {
    const int x = 14941;
    std::cout << 14941;
    for (int i = 0; i < 14941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14942(int& val) {
    func_in_14942(val);
    const int x = 14942;
    std::cout << 14942;
    for (int i = 0; i < 14942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14942(int& val) {
    const int x = 14942;
    std::cout << 14942;
    for (int i = 0; i < 14942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14943(int& val) {
    func_in_14943(val);
    const int x = 14943;
    std::cout << 14943;
    for (int i = 0; i < 14943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14943(int& val) {
    const int x = 14943;
    std::cout << 14943;
    for (int i = 0; i < 14943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14944(int& val) {
    func_in_14944(val);
    const int x = 14944;
    std::cout << 14944;
    for (int i = 0; i < 14944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14944(int& val) {
    const int x = 14944;
    std::cout << 14944;
    for (int i = 0; i < 14944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14945(int& val) {
    func_in_14945(val);
    const int x = 14945;
    std::cout << 14945;
    for (int i = 0; i < 14945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14945(int& val) {
    const int x = 14945;
    std::cout << 14945;
    for (int i = 0; i < 14945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14946(int& val) {
    func_in_14946(val);
    const int x = 14946;
    std::cout << 14946;
    for (int i = 0; i < 14946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14946(int& val) {
    const int x = 14946;
    std::cout << 14946;
    for (int i = 0; i < 14946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14947(int& val) {
    func_in_14947(val);
    const int x = 14947;
    std::cout << 14947;
    for (int i = 0; i < 14947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14947(int& val) {
    const int x = 14947;
    std::cout << 14947;
    for (int i = 0; i < 14947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14948(int& val) {
    func_in_14948(val);
    const int x = 14948;
    std::cout << 14948;
    for (int i = 0; i < 14948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14948(int& val) {
    const int x = 14948;
    std::cout << 14948;
    for (int i = 0; i < 14948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14949(int& val) {
    func_in_14949(val);
    const int x = 14949;
    std::cout << 14949;
    for (int i = 0; i < 14949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14949(int& val) {
    const int x = 14949;
    std::cout << 14949;
    for (int i = 0; i < 14949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14950(int& val) {
    func_in_14950(val);
    const int x = 14950;
    std::cout << 14950;
    for (int i = 0; i < 14950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14950(int& val) {
    const int x = 14950;
    std::cout << 14950;
    for (int i = 0; i < 14950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14951(int& val) {
    func_in_14951(val);
    const int x = 14951;
    std::cout << 14951;
    for (int i = 0; i < 14951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14951(int& val) {
    const int x = 14951;
    std::cout << 14951;
    for (int i = 0; i < 14951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14952(int& val) {
    func_in_14952(val);
    const int x = 14952;
    std::cout << 14952;
    for (int i = 0; i < 14952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14952(int& val) {
    const int x = 14952;
    std::cout << 14952;
    for (int i = 0; i < 14952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14953(int& val) {
    func_in_14953(val);
    const int x = 14953;
    std::cout << 14953;
    for (int i = 0; i < 14953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14953(int& val) {
    const int x = 14953;
    std::cout << 14953;
    for (int i = 0; i < 14953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14954(int& val) {
    func_in_14954(val);
    const int x = 14954;
    std::cout << 14954;
    for (int i = 0; i < 14954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14954(int& val) {
    const int x = 14954;
    std::cout << 14954;
    for (int i = 0; i < 14954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14955(int& val) {
    func_in_14955(val);
    const int x = 14955;
    std::cout << 14955;
    for (int i = 0; i < 14955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14955(int& val) {
    const int x = 14955;
    std::cout << 14955;
    for (int i = 0; i < 14955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14956(int& val) {
    func_in_14956(val);
    const int x = 14956;
    std::cout << 14956;
    for (int i = 0; i < 14956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14956(int& val) {
    const int x = 14956;
    std::cout << 14956;
    for (int i = 0; i < 14956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14957(int& val) {
    func_in_14957(val);
    const int x = 14957;
    std::cout << 14957;
    for (int i = 0; i < 14957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14957(int& val) {
    const int x = 14957;
    std::cout << 14957;
    for (int i = 0; i < 14957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14958(int& val) {
    func_in_14958(val);
    const int x = 14958;
    std::cout << 14958;
    for (int i = 0; i < 14958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14958(int& val) {
    const int x = 14958;
    std::cout << 14958;
    for (int i = 0; i < 14958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14959(int& val) {
    func_in_14959(val);
    const int x = 14959;
    std::cout << 14959;
    for (int i = 0; i < 14959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14959(int& val) {
    const int x = 14959;
    std::cout << 14959;
    for (int i = 0; i < 14959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14960(int& val) {
    func_in_14960(val);
    const int x = 14960;
    std::cout << 14960;
    for (int i = 0; i < 14960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14960(int& val) {
    const int x = 14960;
    std::cout << 14960;
    for (int i = 0; i < 14960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14961(int& val) {
    func_in_14961(val);
    const int x = 14961;
    std::cout << 14961;
    for (int i = 0; i < 14961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14961(int& val) {
    const int x = 14961;
    std::cout << 14961;
    for (int i = 0; i < 14961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14962(int& val) {
    func_in_14962(val);
    const int x = 14962;
    std::cout << 14962;
    for (int i = 0; i < 14962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14962(int& val) {
    const int x = 14962;
    std::cout << 14962;
    for (int i = 0; i < 14962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14963(int& val) {
    func_in_14963(val);
    const int x = 14963;
    std::cout << 14963;
    for (int i = 0; i < 14963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14963(int& val) {
    const int x = 14963;
    std::cout << 14963;
    for (int i = 0; i < 14963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14964(int& val) {
    func_in_14964(val);
    const int x = 14964;
    std::cout << 14964;
    for (int i = 0; i < 14964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14964(int& val) {
    const int x = 14964;
    std::cout << 14964;
    for (int i = 0; i < 14964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14965(int& val) {
    func_in_14965(val);
    const int x = 14965;
    std::cout << 14965;
    for (int i = 0; i < 14965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14965(int& val) {
    const int x = 14965;
    std::cout << 14965;
    for (int i = 0; i < 14965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14966(int& val) {
    func_in_14966(val);
    const int x = 14966;
    std::cout << 14966;
    for (int i = 0; i < 14966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14966(int& val) {
    const int x = 14966;
    std::cout << 14966;
    for (int i = 0; i < 14966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14967(int& val) {
    func_in_14967(val);
    const int x = 14967;
    std::cout << 14967;
    for (int i = 0; i < 14967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14967(int& val) {
    const int x = 14967;
    std::cout << 14967;
    for (int i = 0; i < 14967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14968(int& val) {
    func_in_14968(val);
    const int x = 14968;
    std::cout << 14968;
    for (int i = 0; i < 14968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14968(int& val) {
    const int x = 14968;
    std::cout << 14968;
    for (int i = 0; i < 14968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14969(int& val) {
    func_in_14969(val);
    const int x = 14969;
    std::cout << 14969;
    for (int i = 0; i < 14969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14969(int& val) {
    const int x = 14969;
    std::cout << 14969;
    for (int i = 0; i < 14969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14970(int& val) {
    func_in_14970(val);
    const int x = 14970;
    std::cout << 14970;
    for (int i = 0; i < 14970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14970(int& val) {
    const int x = 14970;
    std::cout << 14970;
    for (int i = 0; i < 14970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14971(int& val) {
    func_in_14971(val);
    const int x = 14971;
    std::cout << 14971;
    for (int i = 0; i < 14971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14971(int& val) {
    const int x = 14971;
    std::cout << 14971;
    for (int i = 0; i < 14971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14972(int& val) {
    func_in_14972(val);
    const int x = 14972;
    std::cout << 14972;
    for (int i = 0; i < 14972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14972(int& val) {
    const int x = 14972;
    std::cout << 14972;
    for (int i = 0; i < 14972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14973(int& val) {
    func_in_14973(val);
    const int x = 14973;
    std::cout << 14973;
    for (int i = 0; i < 14973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14973(int& val) {
    const int x = 14973;
    std::cout << 14973;
    for (int i = 0; i < 14973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14974(int& val) {
    func_in_14974(val);
    const int x = 14974;
    std::cout << 14974;
    for (int i = 0; i < 14974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14974(int& val) {
    const int x = 14974;
    std::cout << 14974;
    for (int i = 0; i < 14974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14975(int& val) {
    func_in_14975(val);
    const int x = 14975;
    std::cout << 14975;
    for (int i = 0; i < 14975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14975(int& val) {
    const int x = 14975;
    std::cout << 14975;
    for (int i = 0; i < 14975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14976(int& val) {
    func_in_14976(val);
    const int x = 14976;
    std::cout << 14976;
    for (int i = 0; i < 14976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14976(int& val) {
    const int x = 14976;
    std::cout << 14976;
    for (int i = 0; i < 14976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14977(int& val) {
    func_in_14977(val);
    const int x = 14977;
    std::cout << 14977;
    for (int i = 0; i < 14977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14977(int& val) {
    const int x = 14977;
    std::cout << 14977;
    for (int i = 0; i < 14977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14978(int& val) {
    func_in_14978(val);
    const int x = 14978;
    std::cout << 14978;
    for (int i = 0; i < 14978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14978(int& val) {
    const int x = 14978;
    std::cout << 14978;
    for (int i = 0; i < 14978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14979(int& val) {
    func_in_14979(val);
    const int x = 14979;
    std::cout << 14979;
    for (int i = 0; i < 14979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14979(int& val) {
    const int x = 14979;
    std::cout << 14979;
    for (int i = 0; i < 14979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14980(int& val) {
    func_in_14980(val);
    const int x = 14980;
    std::cout << 14980;
    for (int i = 0; i < 14980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14980(int& val) {
    const int x = 14980;
    std::cout << 14980;
    for (int i = 0; i < 14980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14981(int& val) {
    func_in_14981(val);
    const int x = 14981;
    std::cout << 14981;
    for (int i = 0; i < 14981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14981(int& val) {
    const int x = 14981;
    std::cout << 14981;
    for (int i = 0; i < 14981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14982(int& val) {
    func_in_14982(val);
    const int x = 14982;
    std::cout << 14982;
    for (int i = 0; i < 14982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14982(int& val) {
    const int x = 14982;
    std::cout << 14982;
    for (int i = 0; i < 14982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14983(int& val) {
    func_in_14983(val);
    const int x = 14983;
    std::cout << 14983;
    for (int i = 0; i < 14983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14983(int& val) {
    const int x = 14983;
    std::cout << 14983;
    for (int i = 0; i < 14983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14984(int& val) {
    func_in_14984(val);
    const int x = 14984;
    std::cout << 14984;
    for (int i = 0; i < 14984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14984(int& val) {
    const int x = 14984;
    std::cout << 14984;
    for (int i = 0; i < 14984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14985(int& val) {
    func_in_14985(val);
    const int x = 14985;
    std::cout << 14985;
    for (int i = 0; i < 14985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14985(int& val) {
    const int x = 14985;
    std::cout << 14985;
    for (int i = 0; i < 14985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14986(int& val) {
    func_in_14986(val);
    const int x = 14986;
    std::cout << 14986;
    for (int i = 0; i < 14986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14986(int& val) {
    const int x = 14986;
    std::cout << 14986;
    for (int i = 0; i < 14986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14987(int& val) {
    func_in_14987(val);
    const int x = 14987;
    std::cout << 14987;
    for (int i = 0; i < 14987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14987(int& val) {
    const int x = 14987;
    std::cout << 14987;
    for (int i = 0; i < 14987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14988(int& val) {
    func_in_14988(val);
    const int x = 14988;
    std::cout << 14988;
    for (int i = 0; i < 14988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14988(int& val) {
    const int x = 14988;
    std::cout << 14988;
    for (int i = 0; i < 14988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14989(int& val) {
    func_in_14989(val);
    const int x = 14989;
    std::cout << 14989;
    for (int i = 0; i < 14989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14989(int& val) {
    const int x = 14989;
    std::cout << 14989;
    for (int i = 0; i < 14989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14990(int& val) {
    func_in_14990(val);
    const int x = 14990;
    std::cout << 14990;
    for (int i = 0; i < 14990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14990(int& val) {
    const int x = 14990;
    std::cout << 14990;
    for (int i = 0; i < 14990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14991(int& val) {
    func_in_14991(val);
    const int x = 14991;
    std::cout << 14991;
    for (int i = 0; i < 14991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14991(int& val) {
    const int x = 14991;
    std::cout << 14991;
    for (int i = 0; i < 14991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14992(int& val) {
    func_in_14992(val);
    const int x = 14992;
    std::cout << 14992;
    for (int i = 0; i < 14992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14992(int& val) {
    const int x = 14992;
    std::cout << 14992;
    for (int i = 0; i < 14992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14993(int& val) {
    func_in_14993(val);
    const int x = 14993;
    std::cout << 14993;
    for (int i = 0; i < 14993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14993(int& val) {
    const int x = 14993;
    std::cout << 14993;
    for (int i = 0; i < 14993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14994(int& val) {
    func_in_14994(val);
    const int x = 14994;
    std::cout << 14994;
    for (int i = 0; i < 14994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14994(int& val) {
    const int x = 14994;
    std::cout << 14994;
    for (int i = 0; i < 14994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14995(int& val) {
    func_in_14995(val);
    const int x = 14995;
    std::cout << 14995;
    for (int i = 0; i < 14995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14995(int& val) {
    const int x = 14995;
    std::cout << 14995;
    for (int i = 0; i < 14995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14996(int& val) {
    func_in_14996(val);
    const int x = 14996;
    std::cout << 14996;
    for (int i = 0; i < 14996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14996(int& val) {
    const int x = 14996;
    std::cout << 14996;
    for (int i = 0; i < 14996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14997(int& val) {
    func_in_14997(val);
    const int x = 14997;
    std::cout << 14997;
    for (int i = 0; i < 14997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14997(int& val) {
    const int x = 14997;
    std::cout << 14997;
    for (int i = 0; i < 14997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14998(int& val) {
    func_in_14998(val);
    const int x = 14998;
    std::cout << 14998;
    for (int i = 0; i < 14998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14998(int& val) {
    const int x = 14998;
    std::cout << 14998;
    for (int i = 0; i < 14998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14999(int& val) {
    func_in_14999(val);
    const int x = 14999;
    std::cout << 14999;
    for (int i = 0; i < 14999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14999(int& val) {
    const int x = 14999;
    std::cout << 14999;
    for (int i = 0; i < 14999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15000(int& val) {
    func_in_15000(val);
    const int x = 15000;
    std::cout << 15000;
    for (int i = 0; i < 15000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15000(int& val) {
    const int x = 15000;
    std::cout << 15000;
    for (int i = 0; i < 15000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15001(int& val) {
    func_in_15001(val);
    const int x = 15001;
    std::cout << 15001;
    for (int i = 0; i < 15001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15001(int& val) {
    const int x = 15001;
    std::cout << 15001;
    for (int i = 0; i < 15001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15002(int& val) {
    func_in_15002(val);
    const int x = 15002;
    std::cout << 15002;
    for (int i = 0; i < 15002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15002(int& val) {
    const int x = 15002;
    std::cout << 15002;
    for (int i = 0; i < 15002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15003(int& val) {
    func_in_15003(val);
    const int x = 15003;
    std::cout << 15003;
    for (int i = 0; i < 15003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15003(int& val) {
    const int x = 15003;
    std::cout << 15003;
    for (int i = 0; i < 15003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15004(int& val) {
    func_in_15004(val);
    const int x = 15004;
    std::cout << 15004;
    for (int i = 0; i < 15004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15004(int& val) {
    const int x = 15004;
    std::cout << 15004;
    for (int i = 0; i < 15004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15005(int& val) {
    func_in_15005(val);
    const int x = 15005;
    std::cout << 15005;
    for (int i = 0; i < 15005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15005(int& val) {
    const int x = 15005;
    std::cout << 15005;
    for (int i = 0; i < 15005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15006(int& val) {
    func_in_15006(val);
    const int x = 15006;
    std::cout << 15006;
    for (int i = 0; i < 15006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15006(int& val) {
    const int x = 15006;
    std::cout << 15006;
    for (int i = 0; i < 15006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15007(int& val) {
    func_in_15007(val);
    const int x = 15007;
    std::cout << 15007;
    for (int i = 0; i < 15007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15007(int& val) {
    const int x = 15007;
    std::cout << 15007;
    for (int i = 0; i < 15007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15008(int& val) {
    func_in_15008(val);
    const int x = 15008;
    std::cout << 15008;
    for (int i = 0; i < 15008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15008(int& val) {
    const int x = 15008;
    std::cout << 15008;
    for (int i = 0; i < 15008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15009(int& val) {
    func_in_15009(val);
    const int x = 15009;
    std::cout << 15009;
    for (int i = 0; i < 15009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15009(int& val) {
    const int x = 15009;
    std::cout << 15009;
    for (int i = 0; i < 15009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15010(int& val) {
    func_in_15010(val);
    const int x = 15010;
    std::cout << 15010;
    for (int i = 0; i < 15010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15010(int& val) {
    const int x = 15010;
    std::cout << 15010;
    for (int i = 0; i < 15010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15011(int& val) {
    func_in_15011(val);
    const int x = 15011;
    std::cout << 15011;
    for (int i = 0; i < 15011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15011(int& val) {
    const int x = 15011;
    std::cout << 15011;
    for (int i = 0; i < 15011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15012(int& val) {
    func_in_15012(val);
    const int x = 15012;
    std::cout << 15012;
    for (int i = 0; i < 15012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15012(int& val) {
    const int x = 15012;
    std::cout << 15012;
    for (int i = 0; i < 15012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15013(int& val) {
    func_in_15013(val);
    const int x = 15013;
    std::cout << 15013;
    for (int i = 0; i < 15013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15013(int& val) {
    const int x = 15013;
    std::cout << 15013;
    for (int i = 0; i < 15013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15014(int& val) {
    func_in_15014(val);
    const int x = 15014;
    std::cout << 15014;
    for (int i = 0; i < 15014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15014(int& val) {
    const int x = 15014;
    std::cout << 15014;
    for (int i = 0; i < 15014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15015(int& val) {
    func_in_15015(val);
    const int x = 15015;
    std::cout << 15015;
    for (int i = 0; i < 15015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15015(int& val) {
    const int x = 15015;
    std::cout << 15015;
    for (int i = 0; i < 15015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15016(int& val) {
    func_in_15016(val);
    const int x = 15016;
    std::cout << 15016;
    for (int i = 0; i < 15016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15016(int& val) {
    const int x = 15016;
    std::cout << 15016;
    for (int i = 0; i < 15016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15017(int& val) {
    func_in_15017(val);
    const int x = 15017;
    std::cout << 15017;
    for (int i = 0; i < 15017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15017(int& val) {
    const int x = 15017;
    std::cout << 15017;
    for (int i = 0; i < 15017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15018(int& val) {
    func_in_15018(val);
    const int x = 15018;
    std::cout << 15018;
    for (int i = 0; i < 15018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15018(int& val) {
    const int x = 15018;
    std::cout << 15018;
    for (int i = 0; i < 15018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15019(int& val) {
    func_in_15019(val);
    const int x = 15019;
    std::cout << 15019;
    for (int i = 0; i < 15019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15019(int& val) {
    const int x = 15019;
    std::cout << 15019;
    for (int i = 0; i < 15019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15020(int& val) {
    func_in_15020(val);
    const int x = 15020;
    std::cout << 15020;
    for (int i = 0; i < 15020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15020(int& val) {
    const int x = 15020;
    std::cout << 15020;
    for (int i = 0; i < 15020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15021(int& val) {
    func_in_15021(val);
    const int x = 15021;
    std::cout << 15021;
    for (int i = 0; i < 15021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15021(int& val) {
    const int x = 15021;
    std::cout << 15021;
    for (int i = 0; i < 15021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15022(int& val) {
    func_in_15022(val);
    const int x = 15022;
    std::cout << 15022;
    for (int i = 0; i < 15022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15022(int& val) {
    const int x = 15022;
    std::cout << 15022;
    for (int i = 0; i < 15022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15023(int& val) {
    func_in_15023(val);
    const int x = 15023;
    std::cout << 15023;
    for (int i = 0; i < 15023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15023(int& val) {
    const int x = 15023;
    std::cout << 15023;
    for (int i = 0; i < 15023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15024(int& val) {
    func_in_15024(val);
    const int x = 15024;
    std::cout << 15024;
    for (int i = 0; i < 15024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15024(int& val) {
    const int x = 15024;
    std::cout << 15024;
    for (int i = 0; i < 15024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15025(int& val) {
    func_in_15025(val);
    const int x = 15025;
    std::cout << 15025;
    for (int i = 0; i < 15025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15025(int& val) {
    const int x = 15025;
    std::cout << 15025;
    for (int i = 0; i < 15025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15026(int& val) {
    func_in_15026(val);
    const int x = 15026;
    std::cout << 15026;
    for (int i = 0; i < 15026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15026(int& val) {
    const int x = 15026;
    std::cout << 15026;
    for (int i = 0; i < 15026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15027(int& val) {
    func_in_15027(val);
    const int x = 15027;
    std::cout << 15027;
    for (int i = 0; i < 15027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15027(int& val) {
    const int x = 15027;
    std::cout << 15027;
    for (int i = 0; i < 15027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15028(int& val) {
    func_in_15028(val);
    const int x = 15028;
    std::cout << 15028;
    for (int i = 0; i < 15028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15028(int& val) {
    const int x = 15028;
    std::cout << 15028;
    for (int i = 0; i < 15028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15029(int& val) {
    func_in_15029(val);
    const int x = 15029;
    std::cout << 15029;
    for (int i = 0; i < 15029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15029(int& val) {
    const int x = 15029;
    std::cout << 15029;
    for (int i = 0; i < 15029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15030(int& val) {
    func_in_15030(val);
    const int x = 15030;
    std::cout << 15030;
    for (int i = 0; i < 15030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15030(int& val) {
    const int x = 15030;
    std::cout << 15030;
    for (int i = 0; i < 15030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15031(int& val) {
    func_in_15031(val);
    const int x = 15031;
    std::cout << 15031;
    for (int i = 0; i < 15031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15031(int& val) {
    const int x = 15031;
    std::cout << 15031;
    for (int i = 0; i < 15031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15032(int& val) {
    func_in_15032(val);
    const int x = 15032;
    std::cout << 15032;
    for (int i = 0; i < 15032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15032(int& val) {
    const int x = 15032;
    std::cout << 15032;
    for (int i = 0; i < 15032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15033(int& val) {
    func_in_15033(val);
    const int x = 15033;
    std::cout << 15033;
    for (int i = 0; i < 15033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15033(int& val) {
    const int x = 15033;
    std::cout << 15033;
    for (int i = 0; i < 15033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15034(int& val) {
    func_in_15034(val);
    const int x = 15034;
    std::cout << 15034;
    for (int i = 0; i < 15034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15034(int& val) {
    const int x = 15034;
    std::cout << 15034;
    for (int i = 0; i < 15034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15035(int& val) {
    func_in_15035(val);
    const int x = 15035;
    std::cout << 15035;
    for (int i = 0; i < 15035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15035(int& val) {
    const int x = 15035;
    std::cout << 15035;
    for (int i = 0; i < 15035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15036(int& val) {
    func_in_15036(val);
    const int x = 15036;
    std::cout << 15036;
    for (int i = 0; i < 15036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15036(int& val) {
    const int x = 15036;
    std::cout << 15036;
    for (int i = 0; i < 15036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15037(int& val) {
    func_in_15037(val);
    const int x = 15037;
    std::cout << 15037;
    for (int i = 0; i < 15037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15037(int& val) {
    const int x = 15037;
    std::cout << 15037;
    for (int i = 0; i < 15037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15038(int& val) {
    func_in_15038(val);
    const int x = 15038;
    std::cout << 15038;
    for (int i = 0; i < 15038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15038(int& val) {
    const int x = 15038;
    std::cout << 15038;
    for (int i = 0; i < 15038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15039(int& val) {
    func_in_15039(val);
    const int x = 15039;
    std::cout << 15039;
    for (int i = 0; i < 15039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15039(int& val) {
    const int x = 15039;
    std::cout << 15039;
    for (int i = 0; i < 15039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15040(int& val) {
    func_in_15040(val);
    const int x = 15040;
    std::cout << 15040;
    for (int i = 0; i < 15040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15040(int& val) {
    const int x = 15040;
    std::cout << 15040;
    for (int i = 0; i < 15040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15041(int& val) {
    func_in_15041(val);
    const int x = 15041;
    std::cout << 15041;
    for (int i = 0; i < 15041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15041(int& val) {
    const int x = 15041;
    std::cout << 15041;
    for (int i = 0; i < 15041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15042(int& val) {
    func_in_15042(val);
    const int x = 15042;
    std::cout << 15042;
    for (int i = 0; i < 15042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15042(int& val) {
    const int x = 15042;
    std::cout << 15042;
    for (int i = 0; i < 15042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15043(int& val) {
    func_in_15043(val);
    const int x = 15043;
    std::cout << 15043;
    for (int i = 0; i < 15043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15043(int& val) {
    const int x = 15043;
    std::cout << 15043;
    for (int i = 0; i < 15043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15044(int& val) {
    func_in_15044(val);
    const int x = 15044;
    std::cout << 15044;
    for (int i = 0; i < 15044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15044(int& val) {
    const int x = 15044;
    std::cout << 15044;
    for (int i = 0; i < 15044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15045(int& val) {
    func_in_15045(val);
    const int x = 15045;
    std::cout << 15045;
    for (int i = 0; i < 15045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15045(int& val) {
    const int x = 15045;
    std::cout << 15045;
    for (int i = 0; i < 15045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15046(int& val) {
    func_in_15046(val);
    const int x = 15046;
    std::cout << 15046;
    for (int i = 0; i < 15046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15046(int& val) {
    const int x = 15046;
    std::cout << 15046;
    for (int i = 0; i < 15046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15047(int& val) {
    func_in_15047(val);
    const int x = 15047;
    std::cout << 15047;
    for (int i = 0; i < 15047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15047(int& val) {
    const int x = 15047;
    std::cout << 15047;
    for (int i = 0; i < 15047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15048(int& val) {
    func_in_15048(val);
    const int x = 15048;
    std::cout << 15048;
    for (int i = 0; i < 15048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15048(int& val) {
    const int x = 15048;
    std::cout << 15048;
    for (int i = 0; i < 15048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15049(int& val) {
    func_in_15049(val);
    const int x = 15049;
    std::cout << 15049;
    for (int i = 0; i < 15049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15049(int& val) {
    const int x = 15049;
    std::cout << 15049;
    for (int i = 0; i < 15049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15050(int& val) {
    func_in_15050(val);
    const int x = 15050;
    std::cout << 15050;
    for (int i = 0; i < 15050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15050(int& val) {
    const int x = 15050;
    std::cout << 15050;
    for (int i = 0; i < 15050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15051(int& val) {
    func_in_15051(val);
    const int x = 15051;
    std::cout << 15051;
    for (int i = 0; i < 15051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15051(int& val) {
    const int x = 15051;
    std::cout << 15051;
    for (int i = 0; i < 15051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15052(int& val) {
    func_in_15052(val);
    const int x = 15052;
    std::cout << 15052;
    for (int i = 0; i < 15052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15052(int& val) {
    const int x = 15052;
    std::cout << 15052;
    for (int i = 0; i < 15052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15053(int& val) {
    func_in_15053(val);
    const int x = 15053;
    std::cout << 15053;
    for (int i = 0; i < 15053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15053(int& val) {
    const int x = 15053;
    std::cout << 15053;
    for (int i = 0; i < 15053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15054(int& val) {
    func_in_15054(val);
    const int x = 15054;
    std::cout << 15054;
    for (int i = 0; i < 15054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15054(int& val) {
    const int x = 15054;
    std::cout << 15054;
    for (int i = 0; i < 15054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15055(int& val) {
    func_in_15055(val);
    const int x = 15055;
    std::cout << 15055;
    for (int i = 0; i < 15055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15055(int& val) {
    const int x = 15055;
    std::cout << 15055;
    for (int i = 0; i < 15055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15056(int& val) {
    func_in_15056(val);
    const int x = 15056;
    std::cout << 15056;
    for (int i = 0; i < 15056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15056(int& val) {
    const int x = 15056;
    std::cout << 15056;
    for (int i = 0; i < 15056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15057(int& val) {
    func_in_15057(val);
    const int x = 15057;
    std::cout << 15057;
    for (int i = 0; i < 15057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15057(int& val) {
    const int x = 15057;
    std::cout << 15057;
    for (int i = 0; i < 15057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15058(int& val) {
    func_in_15058(val);
    const int x = 15058;
    std::cout << 15058;
    for (int i = 0; i < 15058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15058(int& val) {
    const int x = 15058;
    std::cout << 15058;
    for (int i = 0; i < 15058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15059(int& val) {
    func_in_15059(val);
    const int x = 15059;
    std::cout << 15059;
    for (int i = 0; i < 15059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15059(int& val) {
    const int x = 15059;
    std::cout << 15059;
    for (int i = 0; i < 15059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15060(int& val) {
    func_in_15060(val);
    const int x = 15060;
    std::cout << 15060;
    for (int i = 0; i < 15060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15060(int& val) {
    const int x = 15060;
    std::cout << 15060;
    for (int i = 0; i < 15060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15061(int& val) {
    func_in_15061(val);
    const int x = 15061;
    std::cout << 15061;
    for (int i = 0; i < 15061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15061(int& val) {
    const int x = 15061;
    std::cout << 15061;
    for (int i = 0; i < 15061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15062(int& val) {
    func_in_15062(val);
    const int x = 15062;
    std::cout << 15062;
    for (int i = 0; i < 15062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15062(int& val) {
    const int x = 15062;
    std::cout << 15062;
    for (int i = 0; i < 15062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15063(int& val) {
    func_in_15063(val);
    const int x = 15063;
    std::cout << 15063;
    for (int i = 0; i < 15063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15063(int& val) {
    const int x = 15063;
    std::cout << 15063;
    for (int i = 0; i < 15063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15064(int& val) {
    func_in_15064(val);
    const int x = 15064;
    std::cout << 15064;
    for (int i = 0; i < 15064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15064(int& val) {
    const int x = 15064;
    std::cout << 15064;
    for (int i = 0; i < 15064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15065(int& val) {
    func_in_15065(val);
    const int x = 15065;
    std::cout << 15065;
    for (int i = 0; i < 15065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15065(int& val) {
    const int x = 15065;
    std::cout << 15065;
    for (int i = 0; i < 15065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15066(int& val) {
    func_in_15066(val);
    const int x = 15066;
    std::cout << 15066;
    for (int i = 0; i < 15066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15066(int& val) {
    const int x = 15066;
    std::cout << 15066;
    for (int i = 0; i < 15066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15067(int& val) {
    func_in_15067(val);
    const int x = 15067;
    std::cout << 15067;
    for (int i = 0; i < 15067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15067(int& val) {
    const int x = 15067;
    std::cout << 15067;
    for (int i = 0; i < 15067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15068(int& val) {
    func_in_15068(val);
    const int x = 15068;
    std::cout << 15068;
    for (int i = 0; i < 15068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15068(int& val) {
    const int x = 15068;
    std::cout << 15068;
    for (int i = 0; i < 15068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15069(int& val) {
    func_in_15069(val);
    const int x = 15069;
    std::cout << 15069;
    for (int i = 0; i < 15069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15069(int& val) {
    const int x = 15069;
    std::cout << 15069;
    for (int i = 0; i < 15069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15070(int& val) {
    func_in_15070(val);
    const int x = 15070;
    std::cout << 15070;
    for (int i = 0; i < 15070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15070(int& val) {
    const int x = 15070;
    std::cout << 15070;
    for (int i = 0; i < 15070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15071(int& val) {
    func_in_15071(val);
    const int x = 15071;
    std::cout << 15071;
    for (int i = 0; i < 15071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15071(int& val) {
    const int x = 15071;
    std::cout << 15071;
    for (int i = 0; i < 15071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15072(int& val) {
    func_in_15072(val);
    const int x = 15072;
    std::cout << 15072;
    for (int i = 0; i < 15072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15072(int& val) {
    const int x = 15072;
    std::cout << 15072;
    for (int i = 0; i < 15072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15073(int& val) {
    func_in_15073(val);
    const int x = 15073;
    std::cout << 15073;
    for (int i = 0; i < 15073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15073(int& val) {
    const int x = 15073;
    std::cout << 15073;
    for (int i = 0; i < 15073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15074(int& val) {
    func_in_15074(val);
    const int x = 15074;
    std::cout << 15074;
    for (int i = 0; i < 15074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15074(int& val) {
    const int x = 15074;
    std::cout << 15074;
    for (int i = 0; i < 15074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15075(int& val) {
    func_in_15075(val);
    const int x = 15075;
    std::cout << 15075;
    for (int i = 0; i < 15075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15075(int& val) {
    const int x = 15075;
    std::cout << 15075;
    for (int i = 0; i < 15075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15076(int& val) {
    func_in_15076(val);
    const int x = 15076;
    std::cout << 15076;
    for (int i = 0; i < 15076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15076(int& val) {
    const int x = 15076;
    std::cout << 15076;
    for (int i = 0; i < 15076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15077(int& val) {
    func_in_15077(val);
    const int x = 15077;
    std::cout << 15077;
    for (int i = 0; i < 15077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15077(int& val) {
    const int x = 15077;
    std::cout << 15077;
    for (int i = 0; i < 15077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15078(int& val) {
    func_in_15078(val);
    const int x = 15078;
    std::cout << 15078;
    for (int i = 0; i < 15078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15078(int& val) {
    const int x = 15078;
    std::cout << 15078;
    for (int i = 0; i < 15078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15079(int& val) {
    func_in_15079(val);
    const int x = 15079;
    std::cout << 15079;
    for (int i = 0; i < 15079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15079(int& val) {
    const int x = 15079;
    std::cout << 15079;
    for (int i = 0; i < 15079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15080(int& val) {
    func_in_15080(val);
    const int x = 15080;
    std::cout << 15080;
    for (int i = 0; i < 15080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15080(int& val) {
    const int x = 15080;
    std::cout << 15080;
    for (int i = 0; i < 15080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15081(int& val) {
    func_in_15081(val);
    const int x = 15081;
    std::cout << 15081;
    for (int i = 0; i < 15081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15081(int& val) {
    const int x = 15081;
    std::cout << 15081;
    for (int i = 0; i < 15081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15082(int& val) {
    func_in_15082(val);
    const int x = 15082;
    std::cout << 15082;
    for (int i = 0; i < 15082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15082(int& val) {
    const int x = 15082;
    std::cout << 15082;
    for (int i = 0; i < 15082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15083(int& val) {
    func_in_15083(val);
    const int x = 15083;
    std::cout << 15083;
    for (int i = 0; i < 15083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15083(int& val) {
    const int x = 15083;
    std::cout << 15083;
    for (int i = 0; i < 15083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15084(int& val) {
    func_in_15084(val);
    const int x = 15084;
    std::cout << 15084;
    for (int i = 0; i < 15084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15084(int& val) {
    const int x = 15084;
    std::cout << 15084;
    for (int i = 0; i < 15084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15085(int& val) {
    func_in_15085(val);
    const int x = 15085;
    std::cout << 15085;
    for (int i = 0; i < 15085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15085(int& val) {
    const int x = 15085;
    std::cout << 15085;
    for (int i = 0; i < 15085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15086(int& val) {
    func_in_15086(val);
    const int x = 15086;
    std::cout << 15086;
    for (int i = 0; i < 15086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15086(int& val) {
    const int x = 15086;
    std::cout << 15086;
    for (int i = 0; i < 15086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15087(int& val) {
    func_in_15087(val);
    const int x = 15087;
    std::cout << 15087;
    for (int i = 0; i < 15087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15087(int& val) {
    const int x = 15087;
    std::cout << 15087;
    for (int i = 0; i < 15087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15088(int& val) {
    func_in_15088(val);
    const int x = 15088;
    std::cout << 15088;
    for (int i = 0; i < 15088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15088(int& val) {
    const int x = 15088;
    std::cout << 15088;
    for (int i = 0; i < 15088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15089(int& val) {
    func_in_15089(val);
    const int x = 15089;
    std::cout << 15089;
    for (int i = 0; i < 15089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15089(int& val) {
    const int x = 15089;
    std::cout << 15089;
    for (int i = 0; i < 15089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15090(int& val) {
    func_in_15090(val);
    const int x = 15090;
    std::cout << 15090;
    for (int i = 0; i < 15090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15090(int& val) {
    const int x = 15090;
    std::cout << 15090;
    for (int i = 0; i < 15090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15091(int& val) {
    func_in_15091(val);
    const int x = 15091;
    std::cout << 15091;
    for (int i = 0; i < 15091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15091(int& val) {
    const int x = 15091;
    std::cout << 15091;
    for (int i = 0; i < 15091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15092(int& val) {
    func_in_15092(val);
    const int x = 15092;
    std::cout << 15092;
    for (int i = 0; i < 15092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15092(int& val) {
    const int x = 15092;
    std::cout << 15092;
    for (int i = 0; i < 15092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15093(int& val) {
    func_in_15093(val);
    const int x = 15093;
    std::cout << 15093;
    for (int i = 0; i < 15093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15093(int& val) {
    const int x = 15093;
    std::cout << 15093;
    for (int i = 0; i < 15093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15094(int& val) {
    func_in_15094(val);
    const int x = 15094;
    std::cout << 15094;
    for (int i = 0; i < 15094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15094(int& val) {
    const int x = 15094;
    std::cout << 15094;
    for (int i = 0; i < 15094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15095(int& val) {
    func_in_15095(val);
    const int x = 15095;
    std::cout << 15095;
    for (int i = 0; i < 15095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15095(int& val) {
    const int x = 15095;
    std::cout << 15095;
    for (int i = 0; i < 15095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15096(int& val) {
    func_in_15096(val);
    const int x = 15096;
    std::cout << 15096;
    for (int i = 0; i < 15096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15096(int& val) {
    const int x = 15096;
    std::cout << 15096;
    for (int i = 0; i < 15096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15097(int& val) {
    func_in_15097(val);
    const int x = 15097;
    std::cout << 15097;
    for (int i = 0; i < 15097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15097(int& val) {
    const int x = 15097;
    std::cout << 15097;
    for (int i = 0; i < 15097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15098(int& val) {
    func_in_15098(val);
    const int x = 15098;
    std::cout << 15098;
    for (int i = 0; i < 15098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15098(int& val) {
    const int x = 15098;
    std::cout << 15098;
    for (int i = 0; i < 15098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15099(int& val) {
    func_in_15099(val);
    const int x = 15099;
    std::cout << 15099;
    for (int i = 0; i < 15099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15099(int& val) {
    const int x = 15099;
    std::cout << 15099;
    for (int i = 0; i < 15099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15100(int& val) {
    func_in_15100(val);
    const int x = 15100;
    std::cout << 15100;
    for (int i = 0; i < 15100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15100(int& val) {
    const int x = 15100;
    std::cout << 15100;
    for (int i = 0; i < 15100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15101(int& val) {
    func_in_15101(val);
    const int x = 15101;
    std::cout << 15101;
    for (int i = 0; i < 15101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15101(int& val) {
    const int x = 15101;
    std::cout << 15101;
    for (int i = 0; i < 15101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15102(int& val) {
    func_in_15102(val);
    const int x = 15102;
    std::cout << 15102;
    for (int i = 0; i < 15102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15102(int& val) {
    const int x = 15102;
    std::cout << 15102;
    for (int i = 0; i < 15102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15103(int& val) {
    func_in_15103(val);
    const int x = 15103;
    std::cout << 15103;
    for (int i = 0; i < 15103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15103(int& val) {
    const int x = 15103;
    std::cout << 15103;
    for (int i = 0; i < 15103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15104(int& val) {
    func_in_15104(val);
    const int x = 15104;
    std::cout << 15104;
    for (int i = 0; i < 15104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15104(int& val) {
    const int x = 15104;
    std::cout << 15104;
    for (int i = 0; i < 15104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15105(int& val) {
    func_in_15105(val);
    const int x = 15105;
    std::cout << 15105;
    for (int i = 0; i < 15105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15105(int& val) {
    const int x = 15105;
    std::cout << 15105;
    for (int i = 0; i < 15105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15106(int& val) {
    func_in_15106(val);
    const int x = 15106;
    std::cout << 15106;
    for (int i = 0; i < 15106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15106(int& val) {
    const int x = 15106;
    std::cout << 15106;
    for (int i = 0; i < 15106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15107(int& val) {
    func_in_15107(val);
    const int x = 15107;
    std::cout << 15107;
    for (int i = 0; i < 15107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15107(int& val) {
    const int x = 15107;
    std::cout << 15107;
    for (int i = 0; i < 15107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15108(int& val) {
    func_in_15108(val);
    const int x = 15108;
    std::cout << 15108;
    for (int i = 0; i < 15108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15108(int& val) {
    const int x = 15108;
    std::cout << 15108;
    for (int i = 0; i < 15108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15109(int& val) {
    func_in_15109(val);
    const int x = 15109;
    std::cout << 15109;
    for (int i = 0; i < 15109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15109(int& val) {
    const int x = 15109;
    std::cout << 15109;
    for (int i = 0; i < 15109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15110(int& val) {
    func_in_15110(val);
    const int x = 15110;
    std::cout << 15110;
    for (int i = 0; i < 15110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15110(int& val) {
    const int x = 15110;
    std::cout << 15110;
    for (int i = 0; i < 15110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15111(int& val) {
    func_in_15111(val);
    const int x = 15111;
    std::cout << 15111;
    for (int i = 0; i < 15111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15111(int& val) {
    const int x = 15111;
    std::cout << 15111;
    for (int i = 0; i < 15111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15112(int& val) {
    func_in_15112(val);
    const int x = 15112;
    std::cout << 15112;
    for (int i = 0; i < 15112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15112(int& val) {
    const int x = 15112;
    std::cout << 15112;
    for (int i = 0; i < 15112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15113(int& val) {
    func_in_15113(val);
    const int x = 15113;
    std::cout << 15113;
    for (int i = 0; i < 15113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15113(int& val) {
    const int x = 15113;
    std::cout << 15113;
    for (int i = 0; i < 15113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15114(int& val) {
    func_in_15114(val);
    const int x = 15114;
    std::cout << 15114;
    for (int i = 0; i < 15114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15114(int& val) {
    const int x = 15114;
    std::cout << 15114;
    for (int i = 0; i < 15114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15115(int& val) {
    func_in_15115(val);
    const int x = 15115;
    std::cout << 15115;
    for (int i = 0; i < 15115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15115(int& val) {
    const int x = 15115;
    std::cout << 15115;
    for (int i = 0; i < 15115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15116(int& val) {
    func_in_15116(val);
    const int x = 15116;
    std::cout << 15116;
    for (int i = 0; i < 15116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15116(int& val) {
    const int x = 15116;
    std::cout << 15116;
    for (int i = 0; i < 15116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15117(int& val) {
    func_in_15117(val);
    const int x = 15117;
    std::cout << 15117;
    for (int i = 0; i < 15117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15117(int& val) {
    const int x = 15117;
    std::cout << 15117;
    for (int i = 0; i < 15117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15118(int& val) {
    func_in_15118(val);
    const int x = 15118;
    std::cout << 15118;
    for (int i = 0; i < 15118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15118(int& val) {
    const int x = 15118;
    std::cout << 15118;
    for (int i = 0; i < 15118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15119(int& val) {
    func_in_15119(val);
    const int x = 15119;
    std::cout << 15119;
    for (int i = 0; i < 15119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15119(int& val) {
    const int x = 15119;
    std::cout << 15119;
    for (int i = 0; i < 15119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15120(int& val) {
    func_in_15120(val);
    const int x = 15120;
    std::cout << 15120;
    for (int i = 0; i < 15120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15120(int& val) {
    const int x = 15120;
    std::cout << 15120;
    for (int i = 0; i < 15120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15121(int& val) {
    func_in_15121(val);
    const int x = 15121;
    std::cout << 15121;
    for (int i = 0; i < 15121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15121(int& val) {
    const int x = 15121;
    std::cout << 15121;
    for (int i = 0; i < 15121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15122(int& val) {
    func_in_15122(val);
    const int x = 15122;
    std::cout << 15122;
    for (int i = 0; i < 15122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15122(int& val) {
    const int x = 15122;
    std::cout << 15122;
    for (int i = 0; i < 15122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15123(int& val) {
    func_in_15123(val);
    const int x = 15123;
    std::cout << 15123;
    for (int i = 0; i < 15123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15123(int& val) {
    const int x = 15123;
    std::cout << 15123;
    for (int i = 0; i < 15123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15124(int& val) {
    func_in_15124(val);
    const int x = 15124;
    std::cout << 15124;
    for (int i = 0; i < 15124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15124(int& val) {
    const int x = 15124;
    std::cout << 15124;
    for (int i = 0; i < 15124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15125(int& val) {
    func_in_15125(val);
    const int x = 15125;
    std::cout << 15125;
    for (int i = 0; i < 15125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15125(int& val) {
    const int x = 15125;
    std::cout << 15125;
    for (int i = 0; i < 15125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15126(int& val) {
    func_in_15126(val);
    const int x = 15126;
    std::cout << 15126;
    for (int i = 0; i < 15126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15126(int& val) {
    const int x = 15126;
    std::cout << 15126;
    for (int i = 0; i < 15126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15127(int& val) {
    func_in_15127(val);
    const int x = 15127;
    std::cout << 15127;
    for (int i = 0; i < 15127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15127(int& val) {
    const int x = 15127;
    std::cout << 15127;
    for (int i = 0; i < 15127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15128(int& val) {
    func_in_15128(val);
    const int x = 15128;
    std::cout << 15128;
    for (int i = 0; i < 15128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15128(int& val) {
    const int x = 15128;
    std::cout << 15128;
    for (int i = 0; i < 15128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15129(int& val) {
    func_in_15129(val);
    const int x = 15129;
    std::cout << 15129;
    for (int i = 0; i < 15129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15129(int& val) {
    const int x = 15129;
    std::cout << 15129;
    for (int i = 0; i < 15129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15130(int& val) {
    func_in_15130(val);
    const int x = 15130;
    std::cout << 15130;
    for (int i = 0; i < 15130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15130(int& val) {
    const int x = 15130;
    std::cout << 15130;
    for (int i = 0; i < 15130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15131(int& val) {
    func_in_15131(val);
    const int x = 15131;
    std::cout << 15131;
    for (int i = 0; i < 15131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15131(int& val) {
    const int x = 15131;
    std::cout << 15131;
    for (int i = 0; i < 15131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15132(int& val) {
    func_in_15132(val);
    const int x = 15132;
    std::cout << 15132;
    for (int i = 0; i < 15132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15132(int& val) {
    const int x = 15132;
    std::cout << 15132;
    for (int i = 0; i < 15132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15133(int& val) {
    func_in_15133(val);
    const int x = 15133;
    std::cout << 15133;
    for (int i = 0; i < 15133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15133(int& val) {
    const int x = 15133;
    std::cout << 15133;
    for (int i = 0; i < 15133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15134(int& val) {
    func_in_15134(val);
    const int x = 15134;
    std::cout << 15134;
    for (int i = 0; i < 15134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15134(int& val) {
    const int x = 15134;
    std::cout << 15134;
    for (int i = 0; i < 15134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15135(int& val) {
    func_in_15135(val);
    const int x = 15135;
    std::cout << 15135;
    for (int i = 0; i < 15135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15135(int& val) {
    const int x = 15135;
    std::cout << 15135;
    for (int i = 0; i < 15135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15136(int& val) {
    func_in_15136(val);
    const int x = 15136;
    std::cout << 15136;
    for (int i = 0; i < 15136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15136(int& val) {
    const int x = 15136;
    std::cout << 15136;
    for (int i = 0; i < 15136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15137(int& val) {
    func_in_15137(val);
    const int x = 15137;
    std::cout << 15137;
    for (int i = 0; i < 15137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15137(int& val) {
    const int x = 15137;
    std::cout << 15137;
    for (int i = 0; i < 15137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15138(int& val) {
    func_in_15138(val);
    const int x = 15138;
    std::cout << 15138;
    for (int i = 0; i < 15138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15138(int& val) {
    const int x = 15138;
    std::cout << 15138;
    for (int i = 0; i < 15138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15139(int& val) {
    func_in_15139(val);
    const int x = 15139;
    std::cout << 15139;
    for (int i = 0; i < 15139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15139(int& val) {
    const int x = 15139;
    std::cout << 15139;
    for (int i = 0; i < 15139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15140(int& val) {
    func_in_15140(val);
    const int x = 15140;
    std::cout << 15140;
    for (int i = 0; i < 15140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15140(int& val) {
    const int x = 15140;
    std::cout << 15140;
    for (int i = 0; i < 15140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15141(int& val) {
    func_in_15141(val);
    const int x = 15141;
    std::cout << 15141;
    for (int i = 0; i < 15141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15141(int& val) {
    const int x = 15141;
    std::cout << 15141;
    for (int i = 0; i < 15141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15142(int& val) {
    func_in_15142(val);
    const int x = 15142;
    std::cout << 15142;
    for (int i = 0; i < 15142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15142(int& val) {
    const int x = 15142;
    std::cout << 15142;
    for (int i = 0; i < 15142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15143(int& val) {
    func_in_15143(val);
    const int x = 15143;
    std::cout << 15143;
    for (int i = 0; i < 15143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15143(int& val) {
    const int x = 15143;
    std::cout << 15143;
    for (int i = 0; i < 15143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15144(int& val) {
    func_in_15144(val);
    const int x = 15144;
    std::cout << 15144;
    for (int i = 0; i < 15144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15144(int& val) {
    const int x = 15144;
    std::cout << 15144;
    for (int i = 0; i < 15144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15145(int& val) {
    func_in_15145(val);
    const int x = 15145;
    std::cout << 15145;
    for (int i = 0; i < 15145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15145(int& val) {
    const int x = 15145;
    std::cout << 15145;
    for (int i = 0; i < 15145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15146(int& val) {
    func_in_15146(val);
    const int x = 15146;
    std::cout << 15146;
    for (int i = 0; i < 15146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15146(int& val) {
    const int x = 15146;
    std::cout << 15146;
    for (int i = 0; i < 15146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15147(int& val) {
    func_in_15147(val);
    const int x = 15147;
    std::cout << 15147;
    for (int i = 0; i < 15147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15147(int& val) {
    const int x = 15147;
    std::cout << 15147;
    for (int i = 0; i < 15147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15148(int& val) {
    func_in_15148(val);
    const int x = 15148;
    std::cout << 15148;
    for (int i = 0; i < 15148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15148(int& val) {
    const int x = 15148;
    std::cout << 15148;
    for (int i = 0; i < 15148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15149(int& val) {
    func_in_15149(val);
    const int x = 15149;
    std::cout << 15149;
    for (int i = 0; i < 15149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15149(int& val) {
    const int x = 15149;
    std::cout << 15149;
    for (int i = 0; i < 15149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15150(int& val) {
    func_in_15150(val);
    const int x = 15150;
    std::cout << 15150;
    for (int i = 0; i < 15150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15150(int& val) {
    const int x = 15150;
    std::cout << 15150;
    for (int i = 0; i < 15150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15151(int& val) {
    func_in_15151(val);
    const int x = 15151;
    std::cout << 15151;
    for (int i = 0; i < 15151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15151(int& val) {
    const int x = 15151;
    std::cout << 15151;
    for (int i = 0; i < 15151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15152(int& val) {
    func_in_15152(val);
    const int x = 15152;
    std::cout << 15152;
    for (int i = 0; i < 15152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15152(int& val) {
    const int x = 15152;
    std::cout << 15152;
    for (int i = 0; i < 15152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15153(int& val) {
    func_in_15153(val);
    const int x = 15153;
    std::cout << 15153;
    for (int i = 0; i < 15153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15153(int& val) {
    const int x = 15153;
    std::cout << 15153;
    for (int i = 0; i < 15153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15154(int& val) {
    func_in_15154(val);
    const int x = 15154;
    std::cout << 15154;
    for (int i = 0; i < 15154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15154(int& val) {
    const int x = 15154;
    std::cout << 15154;
    for (int i = 0; i < 15154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15155(int& val) {
    func_in_15155(val);
    const int x = 15155;
    std::cout << 15155;
    for (int i = 0; i < 15155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15155(int& val) {
    const int x = 15155;
    std::cout << 15155;
    for (int i = 0; i < 15155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15156(int& val) {
    func_in_15156(val);
    const int x = 15156;
    std::cout << 15156;
    for (int i = 0; i < 15156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15156(int& val) {
    const int x = 15156;
    std::cout << 15156;
    for (int i = 0; i < 15156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15157(int& val) {
    func_in_15157(val);
    const int x = 15157;
    std::cout << 15157;
    for (int i = 0; i < 15157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15157(int& val) {
    const int x = 15157;
    std::cout << 15157;
    for (int i = 0; i < 15157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15158(int& val) {
    func_in_15158(val);
    const int x = 15158;
    std::cout << 15158;
    for (int i = 0; i < 15158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15158(int& val) {
    const int x = 15158;
    std::cout << 15158;
    for (int i = 0; i < 15158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15159(int& val) {
    func_in_15159(val);
    const int x = 15159;
    std::cout << 15159;
    for (int i = 0; i < 15159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15159(int& val) {
    const int x = 15159;
    std::cout << 15159;
    for (int i = 0; i < 15159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15160(int& val) {
    func_in_15160(val);
    const int x = 15160;
    std::cout << 15160;
    for (int i = 0; i < 15160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15160(int& val) {
    const int x = 15160;
    std::cout << 15160;
    for (int i = 0; i < 15160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15161(int& val) {
    func_in_15161(val);
    const int x = 15161;
    std::cout << 15161;
    for (int i = 0; i < 15161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15161(int& val) {
    const int x = 15161;
    std::cout << 15161;
    for (int i = 0; i < 15161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15162(int& val) {
    func_in_15162(val);
    const int x = 15162;
    std::cout << 15162;
    for (int i = 0; i < 15162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15162(int& val) {
    const int x = 15162;
    std::cout << 15162;
    for (int i = 0; i < 15162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15163(int& val) {
    func_in_15163(val);
    const int x = 15163;
    std::cout << 15163;
    for (int i = 0; i < 15163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15163(int& val) {
    const int x = 15163;
    std::cout << 15163;
    for (int i = 0; i < 15163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15164(int& val) {
    func_in_15164(val);
    const int x = 15164;
    std::cout << 15164;
    for (int i = 0; i < 15164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15164(int& val) {
    const int x = 15164;
    std::cout << 15164;
    for (int i = 0; i < 15164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15165(int& val) {
    func_in_15165(val);
    const int x = 15165;
    std::cout << 15165;
    for (int i = 0; i < 15165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15165(int& val) {
    const int x = 15165;
    std::cout << 15165;
    for (int i = 0; i < 15165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15166(int& val) {
    func_in_15166(val);
    const int x = 15166;
    std::cout << 15166;
    for (int i = 0; i < 15166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15166(int& val) {
    const int x = 15166;
    std::cout << 15166;
    for (int i = 0; i < 15166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15167(int& val) {
    func_in_15167(val);
    const int x = 15167;
    std::cout << 15167;
    for (int i = 0; i < 15167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15167(int& val) {
    const int x = 15167;
    std::cout << 15167;
    for (int i = 0; i < 15167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15168(int& val) {
    func_in_15168(val);
    const int x = 15168;
    std::cout << 15168;
    for (int i = 0; i < 15168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15168(int& val) {
    const int x = 15168;
    std::cout << 15168;
    for (int i = 0; i < 15168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15169(int& val) {
    func_in_15169(val);
    const int x = 15169;
    std::cout << 15169;
    for (int i = 0; i < 15169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15169(int& val) {
    const int x = 15169;
    std::cout << 15169;
    for (int i = 0; i < 15169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15170(int& val) {
    func_in_15170(val);
    const int x = 15170;
    std::cout << 15170;
    for (int i = 0; i < 15170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15170(int& val) {
    const int x = 15170;
    std::cout << 15170;
    for (int i = 0; i < 15170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15171(int& val) {
    func_in_15171(val);
    const int x = 15171;
    std::cout << 15171;
    for (int i = 0; i < 15171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15171(int& val) {
    const int x = 15171;
    std::cout << 15171;
    for (int i = 0; i < 15171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15172(int& val) {
    func_in_15172(val);
    const int x = 15172;
    std::cout << 15172;
    for (int i = 0; i < 15172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15172(int& val) {
    const int x = 15172;
    std::cout << 15172;
    for (int i = 0; i < 15172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15173(int& val) {
    func_in_15173(val);
    const int x = 15173;
    std::cout << 15173;
    for (int i = 0; i < 15173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15173(int& val) {
    const int x = 15173;
    std::cout << 15173;
    for (int i = 0; i < 15173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15174(int& val) {
    func_in_15174(val);
    const int x = 15174;
    std::cout << 15174;
    for (int i = 0; i < 15174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15174(int& val) {
    const int x = 15174;
    std::cout << 15174;
    for (int i = 0; i < 15174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15175(int& val) {
    func_in_15175(val);
    const int x = 15175;
    std::cout << 15175;
    for (int i = 0; i < 15175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15175(int& val) {
    const int x = 15175;
    std::cout << 15175;
    for (int i = 0; i < 15175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15176(int& val) {
    func_in_15176(val);
    const int x = 15176;
    std::cout << 15176;
    for (int i = 0; i < 15176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15176(int& val) {
    const int x = 15176;
    std::cout << 15176;
    for (int i = 0; i < 15176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15177(int& val) {
    func_in_15177(val);
    const int x = 15177;
    std::cout << 15177;
    for (int i = 0; i < 15177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15177(int& val) {
    const int x = 15177;
    std::cout << 15177;
    for (int i = 0; i < 15177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15178(int& val) {
    func_in_15178(val);
    const int x = 15178;
    std::cout << 15178;
    for (int i = 0; i < 15178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15178(int& val) {
    const int x = 15178;
    std::cout << 15178;
    for (int i = 0; i < 15178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15179(int& val) {
    func_in_15179(val);
    const int x = 15179;
    std::cout << 15179;
    for (int i = 0; i < 15179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15179(int& val) {
    const int x = 15179;
    std::cout << 15179;
    for (int i = 0; i < 15179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15180(int& val) {
    func_in_15180(val);
    const int x = 15180;
    std::cout << 15180;
    for (int i = 0; i < 15180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15180(int& val) {
    const int x = 15180;
    std::cout << 15180;
    for (int i = 0; i < 15180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15181(int& val) {
    func_in_15181(val);
    const int x = 15181;
    std::cout << 15181;
    for (int i = 0; i < 15181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15181(int& val) {
    const int x = 15181;
    std::cout << 15181;
    for (int i = 0; i < 15181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15182(int& val) {
    func_in_15182(val);
    const int x = 15182;
    std::cout << 15182;
    for (int i = 0; i < 15182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15182(int& val) {
    const int x = 15182;
    std::cout << 15182;
    for (int i = 0; i < 15182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15183(int& val) {
    func_in_15183(val);
    const int x = 15183;
    std::cout << 15183;
    for (int i = 0; i < 15183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15183(int& val) {
    const int x = 15183;
    std::cout << 15183;
    for (int i = 0; i < 15183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15184(int& val) {
    func_in_15184(val);
    const int x = 15184;
    std::cout << 15184;
    for (int i = 0; i < 15184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15184(int& val) {
    const int x = 15184;
    std::cout << 15184;
    for (int i = 0; i < 15184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15185(int& val) {
    func_in_15185(val);
    const int x = 15185;
    std::cout << 15185;
    for (int i = 0; i < 15185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15185(int& val) {
    const int x = 15185;
    std::cout << 15185;
    for (int i = 0; i < 15185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15186(int& val) {
    func_in_15186(val);
    const int x = 15186;
    std::cout << 15186;
    for (int i = 0; i < 15186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15186(int& val) {
    const int x = 15186;
    std::cout << 15186;
    for (int i = 0; i < 15186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15187(int& val) {
    func_in_15187(val);
    const int x = 15187;
    std::cout << 15187;
    for (int i = 0; i < 15187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15187(int& val) {
    const int x = 15187;
    std::cout << 15187;
    for (int i = 0; i < 15187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15188(int& val) {
    func_in_15188(val);
    const int x = 15188;
    std::cout << 15188;
    for (int i = 0; i < 15188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15188(int& val) {
    const int x = 15188;
    std::cout << 15188;
    for (int i = 0; i < 15188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15189(int& val) {
    func_in_15189(val);
    const int x = 15189;
    std::cout << 15189;
    for (int i = 0; i < 15189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15189(int& val) {
    const int x = 15189;
    std::cout << 15189;
    for (int i = 0; i < 15189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15190(int& val) {
    func_in_15190(val);
    const int x = 15190;
    std::cout << 15190;
    for (int i = 0; i < 15190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15190(int& val) {
    const int x = 15190;
    std::cout << 15190;
    for (int i = 0; i < 15190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15191(int& val) {
    func_in_15191(val);
    const int x = 15191;
    std::cout << 15191;
    for (int i = 0; i < 15191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15191(int& val) {
    const int x = 15191;
    std::cout << 15191;
    for (int i = 0; i < 15191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15192(int& val) {
    func_in_15192(val);
    const int x = 15192;
    std::cout << 15192;
    for (int i = 0; i < 15192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15192(int& val) {
    const int x = 15192;
    std::cout << 15192;
    for (int i = 0; i < 15192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15193(int& val) {
    func_in_15193(val);
    const int x = 15193;
    std::cout << 15193;
    for (int i = 0; i < 15193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15193(int& val) {
    const int x = 15193;
    std::cout << 15193;
    for (int i = 0; i < 15193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15194(int& val) {
    func_in_15194(val);
    const int x = 15194;
    std::cout << 15194;
    for (int i = 0; i < 15194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15194(int& val) {
    const int x = 15194;
    std::cout << 15194;
    for (int i = 0; i < 15194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15195(int& val) {
    func_in_15195(val);
    const int x = 15195;
    std::cout << 15195;
    for (int i = 0; i < 15195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15195(int& val) {
    const int x = 15195;
    std::cout << 15195;
    for (int i = 0; i < 15195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15196(int& val) {
    func_in_15196(val);
    const int x = 15196;
    std::cout << 15196;
    for (int i = 0; i < 15196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15196(int& val) {
    const int x = 15196;
    std::cout << 15196;
    for (int i = 0; i < 15196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15197(int& val) {
    func_in_15197(val);
    const int x = 15197;
    std::cout << 15197;
    for (int i = 0; i < 15197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15197(int& val) {
    const int x = 15197;
    std::cout << 15197;
    for (int i = 0; i < 15197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15198(int& val) {
    func_in_15198(val);
    const int x = 15198;
    std::cout << 15198;
    for (int i = 0; i < 15198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15198(int& val) {
    const int x = 15198;
    std::cout << 15198;
    for (int i = 0; i < 15198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15199(int& val) {
    func_in_15199(val);
    const int x = 15199;
    std::cout << 15199;
    for (int i = 0; i < 15199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15199(int& val) {
    const int x = 15199;
    std::cout << 15199;
    for (int i = 0; i < 15199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15200(int& val) {
    func_in_15200(val);
    const int x = 15200;
    std::cout << 15200;
    for (int i = 0; i < 15200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15200(int& val) {
    const int x = 15200;
    std::cout << 15200;
    for (int i = 0; i < 15200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15201(int& val) {
    func_in_15201(val);
    const int x = 15201;
    std::cout << 15201;
    for (int i = 0; i < 15201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15201(int& val) {
    const int x = 15201;
    std::cout << 15201;
    for (int i = 0; i < 15201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15202(int& val) {
    func_in_15202(val);
    const int x = 15202;
    std::cout << 15202;
    for (int i = 0; i < 15202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15202(int& val) {
    const int x = 15202;
    std::cout << 15202;
    for (int i = 0; i < 15202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15203(int& val) {
    func_in_15203(val);
    const int x = 15203;
    std::cout << 15203;
    for (int i = 0; i < 15203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15203(int& val) {
    const int x = 15203;
    std::cout << 15203;
    for (int i = 0; i < 15203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15204(int& val) {
    func_in_15204(val);
    const int x = 15204;
    std::cout << 15204;
    for (int i = 0; i < 15204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15204(int& val) {
    const int x = 15204;
    std::cout << 15204;
    for (int i = 0; i < 15204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15205(int& val) {
    func_in_15205(val);
    const int x = 15205;
    std::cout << 15205;
    for (int i = 0; i < 15205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15205(int& val) {
    const int x = 15205;
    std::cout << 15205;
    for (int i = 0; i < 15205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15206(int& val) {
    func_in_15206(val);
    const int x = 15206;
    std::cout << 15206;
    for (int i = 0; i < 15206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15206(int& val) {
    const int x = 15206;
    std::cout << 15206;
    for (int i = 0; i < 15206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15207(int& val) {
    func_in_15207(val);
    const int x = 15207;
    std::cout << 15207;
    for (int i = 0; i < 15207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15207(int& val) {
    const int x = 15207;
    std::cout << 15207;
    for (int i = 0; i < 15207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15208(int& val) {
    func_in_15208(val);
    const int x = 15208;
    std::cout << 15208;
    for (int i = 0; i < 15208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15208(int& val) {
    const int x = 15208;
    std::cout << 15208;
    for (int i = 0; i < 15208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15209(int& val) {
    func_in_15209(val);
    const int x = 15209;
    std::cout << 15209;
    for (int i = 0; i < 15209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15209(int& val) {
    const int x = 15209;
    std::cout << 15209;
    for (int i = 0; i < 15209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15210(int& val) {
    func_in_15210(val);
    const int x = 15210;
    std::cout << 15210;
    for (int i = 0; i < 15210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15210(int& val) {
    const int x = 15210;
    std::cout << 15210;
    for (int i = 0; i < 15210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15211(int& val) {
    func_in_15211(val);
    const int x = 15211;
    std::cout << 15211;
    for (int i = 0; i < 15211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15211(int& val) {
    const int x = 15211;
    std::cout << 15211;
    for (int i = 0; i < 15211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15212(int& val) {
    func_in_15212(val);
    const int x = 15212;
    std::cout << 15212;
    for (int i = 0; i < 15212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15212(int& val) {
    const int x = 15212;
    std::cout << 15212;
    for (int i = 0; i < 15212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15213(int& val) {
    func_in_15213(val);
    const int x = 15213;
    std::cout << 15213;
    for (int i = 0; i < 15213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15213(int& val) {
    const int x = 15213;
    std::cout << 15213;
    for (int i = 0; i < 15213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15214(int& val) {
    func_in_15214(val);
    const int x = 15214;
    std::cout << 15214;
    for (int i = 0; i < 15214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15214(int& val) {
    const int x = 15214;
    std::cout << 15214;
    for (int i = 0; i < 15214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15215(int& val) {
    func_in_15215(val);
    const int x = 15215;
    std::cout << 15215;
    for (int i = 0; i < 15215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15215(int& val) {
    const int x = 15215;
    std::cout << 15215;
    for (int i = 0; i < 15215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15216(int& val) {
    func_in_15216(val);
    const int x = 15216;
    std::cout << 15216;
    for (int i = 0; i < 15216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15216(int& val) {
    const int x = 15216;
    std::cout << 15216;
    for (int i = 0; i < 15216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15217(int& val) {
    func_in_15217(val);
    const int x = 15217;
    std::cout << 15217;
    for (int i = 0; i < 15217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15217(int& val) {
    const int x = 15217;
    std::cout << 15217;
    for (int i = 0; i < 15217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15218(int& val) {
    func_in_15218(val);
    const int x = 15218;
    std::cout << 15218;
    for (int i = 0; i < 15218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15218(int& val) {
    const int x = 15218;
    std::cout << 15218;
    for (int i = 0; i < 15218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15219(int& val) {
    func_in_15219(val);
    const int x = 15219;
    std::cout << 15219;
    for (int i = 0; i < 15219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15219(int& val) {
    const int x = 15219;
    std::cout << 15219;
    for (int i = 0; i < 15219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15220(int& val) {
    func_in_15220(val);
    const int x = 15220;
    std::cout << 15220;
    for (int i = 0; i < 15220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15220(int& val) {
    const int x = 15220;
    std::cout << 15220;
    for (int i = 0; i < 15220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15221(int& val) {
    func_in_15221(val);
    const int x = 15221;
    std::cout << 15221;
    for (int i = 0; i < 15221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15221(int& val) {
    const int x = 15221;
    std::cout << 15221;
    for (int i = 0; i < 15221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15222(int& val) {
    func_in_15222(val);
    const int x = 15222;
    std::cout << 15222;
    for (int i = 0; i < 15222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15222(int& val) {
    const int x = 15222;
    std::cout << 15222;
    for (int i = 0; i < 15222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15223(int& val) {
    func_in_15223(val);
    const int x = 15223;
    std::cout << 15223;
    for (int i = 0; i < 15223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15223(int& val) {
    const int x = 15223;
    std::cout << 15223;
    for (int i = 0; i < 15223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15224(int& val) {
    func_in_15224(val);
    const int x = 15224;
    std::cout << 15224;
    for (int i = 0; i < 15224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15224(int& val) {
    const int x = 15224;
    std::cout << 15224;
    for (int i = 0; i < 15224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15225(int& val) {
    func_in_15225(val);
    const int x = 15225;
    std::cout << 15225;
    for (int i = 0; i < 15225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15225(int& val) {
    const int x = 15225;
    std::cout << 15225;
    for (int i = 0; i < 15225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15226(int& val) {
    func_in_15226(val);
    const int x = 15226;
    std::cout << 15226;
    for (int i = 0; i < 15226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15226(int& val) {
    const int x = 15226;
    std::cout << 15226;
    for (int i = 0; i < 15226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15227(int& val) {
    func_in_15227(val);
    const int x = 15227;
    std::cout << 15227;
    for (int i = 0; i < 15227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15227(int& val) {
    const int x = 15227;
    std::cout << 15227;
    for (int i = 0; i < 15227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15228(int& val) {
    func_in_15228(val);
    const int x = 15228;
    std::cout << 15228;
    for (int i = 0; i < 15228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15228(int& val) {
    const int x = 15228;
    std::cout << 15228;
    for (int i = 0; i < 15228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15229(int& val) {
    func_in_15229(val);
    const int x = 15229;
    std::cout << 15229;
    for (int i = 0; i < 15229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15229(int& val) {
    const int x = 15229;
    std::cout << 15229;
    for (int i = 0; i < 15229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15230(int& val) {
    func_in_15230(val);
    const int x = 15230;
    std::cout << 15230;
    for (int i = 0; i < 15230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15230(int& val) {
    const int x = 15230;
    std::cout << 15230;
    for (int i = 0; i < 15230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15231(int& val) {
    func_in_15231(val);
    const int x = 15231;
    std::cout << 15231;
    for (int i = 0; i < 15231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15231(int& val) {
    const int x = 15231;
    std::cout << 15231;
    for (int i = 0; i < 15231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15232(int& val) {
    func_in_15232(val);
    const int x = 15232;
    std::cout << 15232;
    for (int i = 0; i < 15232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15232(int& val) {
    const int x = 15232;
    std::cout << 15232;
    for (int i = 0; i < 15232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15233(int& val) {
    func_in_15233(val);
    const int x = 15233;
    std::cout << 15233;
    for (int i = 0; i < 15233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15233(int& val) {
    const int x = 15233;
    std::cout << 15233;
    for (int i = 0; i < 15233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15234(int& val) {
    func_in_15234(val);
    const int x = 15234;
    std::cout << 15234;
    for (int i = 0; i < 15234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15234(int& val) {
    const int x = 15234;
    std::cout << 15234;
    for (int i = 0; i < 15234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15235(int& val) {
    func_in_15235(val);
    const int x = 15235;
    std::cout << 15235;
    for (int i = 0; i < 15235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15235(int& val) {
    const int x = 15235;
    std::cout << 15235;
    for (int i = 0; i < 15235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15236(int& val) {
    func_in_15236(val);
    const int x = 15236;
    std::cout << 15236;
    for (int i = 0; i < 15236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15236(int& val) {
    const int x = 15236;
    std::cout << 15236;
    for (int i = 0; i < 15236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15237(int& val) {
    func_in_15237(val);
    const int x = 15237;
    std::cout << 15237;
    for (int i = 0; i < 15237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15237(int& val) {
    const int x = 15237;
    std::cout << 15237;
    for (int i = 0; i < 15237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15238(int& val) {
    func_in_15238(val);
    const int x = 15238;
    std::cout << 15238;
    for (int i = 0; i < 15238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15238(int& val) {
    const int x = 15238;
    std::cout << 15238;
    for (int i = 0; i < 15238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15239(int& val) {
    func_in_15239(val);
    const int x = 15239;
    std::cout << 15239;
    for (int i = 0; i < 15239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15239(int& val) {
    const int x = 15239;
    std::cout << 15239;
    for (int i = 0; i < 15239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15240(int& val) {
    func_in_15240(val);
    const int x = 15240;
    std::cout << 15240;
    for (int i = 0; i < 15240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15240(int& val) {
    const int x = 15240;
    std::cout << 15240;
    for (int i = 0; i < 15240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15241(int& val) {
    func_in_15241(val);
    const int x = 15241;
    std::cout << 15241;
    for (int i = 0; i < 15241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15241(int& val) {
    const int x = 15241;
    std::cout << 15241;
    for (int i = 0; i < 15241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15242(int& val) {
    func_in_15242(val);
    const int x = 15242;
    std::cout << 15242;
    for (int i = 0; i < 15242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15242(int& val) {
    const int x = 15242;
    std::cout << 15242;
    for (int i = 0; i < 15242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15243(int& val) {
    func_in_15243(val);
    const int x = 15243;
    std::cout << 15243;
    for (int i = 0; i < 15243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15243(int& val) {
    const int x = 15243;
    std::cout << 15243;
    for (int i = 0; i < 15243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15244(int& val) {
    func_in_15244(val);
    const int x = 15244;
    std::cout << 15244;
    for (int i = 0; i < 15244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15244(int& val) {
    const int x = 15244;
    std::cout << 15244;
    for (int i = 0; i < 15244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15245(int& val) {
    func_in_15245(val);
    const int x = 15245;
    std::cout << 15245;
    for (int i = 0; i < 15245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15245(int& val) {
    const int x = 15245;
    std::cout << 15245;
    for (int i = 0; i < 15245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15246(int& val) {
    func_in_15246(val);
    const int x = 15246;
    std::cout << 15246;
    for (int i = 0; i < 15246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15246(int& val) {
    const int x = 15246;
    std::cout << 15246;
    for (int i = 0; i < 15246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15247(int& val) {
    func_in_15247(val);
    const int x = 15247;
    std::cout << 15247;
    for (int i = 0; i < 15247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15247(int& val) {
    const int x = 15247;
    std::cout << 15247;
    for (int i = 0; i < 15247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15248(int& val) {
    func_in_15248(val);
    const int x = 15248;
    std::cout << 15248;
    for (int i = 0; i < 15248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15248(int& val) {
    const int x = 15248;
    std::cout << 15248;
    for (int i = 0; i < 15248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15249(int& val) {
    func_in_15249(val);
    const int x = 15249;
    std::cout << 15249;
    for (int i = 0; i < 15249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15249(int& val) {
    const int x = 15249;
    std::cout << 15249;
    for (int i = 0; i < 15249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15250(int& val) {
    func_in_15250(val);
    const int x = 15250;
    std::cout << 15250;
    for (int i = 0; i < 15250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15250(int& val) {
    const int x = 15250;
    std::cout << 15250;
    for (int i = 0; i < 15250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15251(int& val) {
    func_in_15251(val);
    const int x = 15251;
    std::cout << 15251;
    for (int i = 0; i < 15251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15251(int& val) {
    const int x = 15251;
    std::cout << 15251;
    for (int i = 0; i < 15251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15252(int& val) {
    func_in_15252(val);
    const int x = 15252;
    std::cout << 15252;
    for (int i = 0; i < 15252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15252(int& val) {
    const int x = 15252;
    std::cout << 15252;
    for (int i = 0; i < 15252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15253(int& val) {
    func_in_15253(val);
    const int x = 15253;
    std::cout << 15253;
    for (int i = 0; i < 15253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15253(int& val) {
    const int x = 15253;
    std::cout << 15253;
    for (int i = 0; i < 15253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15254(int& val) {
    func_in_15254(val);
    const int x = 15254;
    std::cout << 15254;
    for (int i = 0; i < 15254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15254(int& val) {
    const int x = 15254;
    std::cout << 15254;
    for (int i = 0; i < 15254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15255(int& val) {
    func_in_15255(val);
    const int x = 15255;
    std::cout << 15255;
    for (int i = 0; i < 15255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15255(int& val) {
    const int x = 15255;
    std::cout << 15255;
    for (int i = 0; i < 15255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15256(int& val) {
    func_in_15256(val);
    const int x = 15256;
    std::cout << 15256;
    for (int i = 0; i < 15256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15256(int& val) {
    const int x = 15256;
    std::cout << 15256;
    for (int i = 0; i < 15256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15257(int& val) {
    func_in_15257(val);
    const int x = 15257;
    std::cout << 15257;
    for (int i = 0; i < 15257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15257(int& val) {
    const int x = 15257;
    std::cout << 15257;
    for (int i = 0; i < 15257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15258(int& val) {
    func_in_15258(val);
    const int x = 15258;
    std::cout << 15258;
    for (int i = 0; i < 15258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15258(int& val) {
    const int x = 15258;
    std::cout << 15258;
    for (int i = 0; i < 15258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15259(int& val) {
    func_in_15259(val);
    const int x = 15259;
    std::cout << 15259;
    for (int i = 0; i < 15259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15259(int& val) {
    const int x = 15259;
    std::cout << 15259;
    for (int i = 0; i < 15259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15260(int& val) {
    func_in_15260(val);
    const int x = 15260;
    std::cout << 15260;
    for (int i = 0; i < 15260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15260(int& val) {
    const int x = 15260;
    std::cout << 15260;
    for (int i = 0; i < 15260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15261(int& val) {
    func_in_15261(val);
    const int x = 15261;
    std::cout << 15261;
    for (int i = 0; i < 15261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15261(int& val) {
    const int x = 15261;
    std::cout << 15261;
    for (int i = 0; i < 15261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15262(int& val) {
    func_in_15262(val);
    const int x = 15262;
    std::cout << 15262;
    for (int i = 0; i < 15262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15262(int& val) {
    const int x = 15262;
    std::cout << 15262;
    for (int i = 0; i < 15262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15263(int& val) {
    func_in_15263(val);
    const int x = 15263;
    std::cout << 15263;
    for (int i = 0; i < 15263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15263(int& val) {
    const int x = 15263;
    std::cout << 15263;
    for (int i = 0; i < 15263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15264(int& val) {
    func_in_15264(val);
    const int x = 15264;
    std::cout << 15264;
    for (int i = 0; i < 15264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15264(int& val) {
    const int x = 15264;
    std::cout << 15264;
    for (int i = 0; i < 15264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15265(int& val) {
    func_in_15265(val);
    const int x = 15265;
    std::cout << 15265;
    for (int i = 0; i < 15265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15265(int& val) {
    const int x = 15265;
    std::cout << 15265;
    for (int i = 0; i < 15265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15266(int& val) {
    func_in_15266(val);
    const int x = 15266;
    std::cout << 15266;
    for (int i = 0; i < 15266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15266(int& val) {
    const int x = 15266;
    std::cout << 15266;
    for (int i = 0; i < 15266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15267(int& val) {
    func_in_15267(val);
    const int x = 15267;
    std::cout << 15267;
    for (int i = 0; i < 15267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15267(int& val) {
    const int x = 15267;
    std::cout << 15267;
    for (int i = 0; i < 15267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15268(int& val) {
    func_in_15268(val);
    const int x = 15268;
    std::cout << 15268;
    for (int i = 0; i < 15268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15268(int& val) {
    const int x = 15268;
    std::cout << 15268;
    for (int i = 0; i < 15268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15269(int& val) {
    func_in_15269(val);
    const int x = 15269;
    std::cout << 15269;
    for (int i = 0; i < 15269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15269(int& val) {
    const int x = 15269;
    std::cout << 15269;
    for (int i = 0; i < 15269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15270(int& val) {
    func_in_15270(val);
    const int x = 15270;
    std::cout << 15270;
    for (int i = 0; i < 15270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15270(int& val) {
    const int x = 15270;
    std::cout << 15270;
    for (int i = 0; i < 15270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15271(int& val) {
    func_in_15271(val);
    const int x = 15271;
    std::cout << 15271;
    for (int i = 0; i < 15271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15271(int& val) {
    const int x = 15271;
    std::cout << 15271;
    for (int i = 0; i < 15271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15272(int& val) {
    func_in_15272(val);
    const int x = 15272;
    std::cout << 15272;
    for (int i = 0; i < 15272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15272(int& val) {
    const int x = 15272;
    std::cout << 15272;
    for (int i = 0; i < 15272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15273(int& val) {
    func_in_15273(val);
    const int x = 15273;
    std::cout << 15273;
    for (int i = 0; i < 15273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15273(int& val) {
    const int x = 15273;
    std::cout << 15273;
    for (int i = 0; i < 15273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15274(int& val) {
    func_in_15274(val);
    const int x = 15274;
    std::cout << 15274;
    for (int i = 0; i < 15274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15274(int& val) {
    const int x = 15274;
    std::cout << 15274;
    for (int i = 0; i < 15274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15275(int& val) {
    func_in_15275(val);
    const int x = 15275;
    std::cout << 15275;
    for (int i = 0; i < 15275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15275(int& val) {
    const int x = 15275;
    std::cout << 15275;
    for (int i = 0; i < 15275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15276(int& val) {
    func_in_15276(val);
    const int x = 15276;
    std::cout << 15276;
    for (int i = 0; i < 15276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15276(int& val) {
    const int x = 15276;
    std::cout << 15276;
    for (int i = 0; i < 15276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15277(int& val) {
    func_in_15277(val);
    const int x = 15277;
    std::cout << 15277;
    for (int i = 0; i < 15277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15277(int& val) {
    const int x = 15277;
    std::cout << 15277;
    for (int i = 0; i < 15277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15278(int& val) {
    func_in_15278(val);
    const int x = 15278;
    std::cout << 15278;
    for (int i = 0; i < 15278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15278(int& val) {
    const int x = 15278;
    std::cout << 15278;
    for (int i = 0; i < 15278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15279(int& val) {
    func_in_15279(val);
    const int x = 15279;
    std::cout << 15279;
    for (int i = 0; i < 15279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15279(int& val) {
    const int x = 15279;
    std::cout << 15279;
    for (int i = 0; i < 15279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15280(int& val) {
    func_in_15280(val);
    const int x = 15280;
    std::cout << 15280;
    for (int i = 0; i < 15280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15280(int& val) {
    const int x = 15280;
    std::cout << 15280;
    for (int i = 0; i < 15280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15281(int& val) {
    func_in_15281(val);
    const int x = 15281;
    std::cout << 15281;
    for (int i = 0; i < 15281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15281(int& val) {
    const int x = 15281;
    std::cout << 15281;
    for (int i = 0; i < 15281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15282(int& val) {
    func_in_15282(val);
    const int x = 15282;
    std::cout << 15282;
    for (int i = 0; i < 15282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15282(int& val) {
    const int x = 15282;
    std::cout << 15282;
    for (int i = 0; i < 15282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15283(int& val) {
    func_in_15283(val);
    const int x = 15283;
    std::cout << 15283;
    for (int i = 0; i < 15283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15283(int& val) {
    const int x = 15283;
    std::cout << 15283;
    for (int i = 0; i < 15283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15284(int& val) {
    func_in_15284(val);
    const int x = 15284;
    std::cout << 15284;
    for (int i = 0; i < 15284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15284(int& val) {
    const int x = 15284;
    std::cout << 15284;
    for (int i = 0; i < 15284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15285(int& val) {
    func_in_15285(val);
    const int x = 15285;
    std::cout << 15285;
    for (int i = 0; i < 15285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15285(int& val) {
    const int x = 15285;
    std::cout << 15285;
    for (int i = 0; i < 15285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15286(int& val) {
    func_in_15286(val);
    const int x = 15286;
    std::cout << 15286;
    for (int i = 0; i < 15286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15286(int& val) {
    const int x = 15286;
    std::cout << 15286;
    for (int i = 0; i < 15286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15287(int& val) {
    func_in_15287(val);
    const int x = 15287;
    std::cout << 15287;
    for (int i = 0; i < 15287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15287(int& val) {
    const int x = 15287;
    std::cout << 15287;
    for (int i = 0; i < 15287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15288(int& val) {
    func_in_15288(val);
    const int x = 15288;
    std::cout << 15288;
    for (int i = 0; i < 15288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15288(int& val) {
    const int x = 15288;
    std::cout << 15288;
    for (int i = 0; i < 15288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15289(int& val) {
    func_in_15289(val);
    const int x = 15289;
    std::cout << 15289;
    for (int i = 0; i < 15289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15289(int& val) {
    const int x = 15289;
    std::cout << 15289;
    for (int i = 0; i < 15289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15290(int& val) {
    func_in_15290(val);
    const int x = 15290;
    std::cout << 15290;
    for (int i = 0; i < 15290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15290(int& val) {
    const int x = 15290;
    std::cout << 15290;
    for (int i = 0; i < 15290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15291(int& val) {
    func_in_15291(val);
    const int x = 15291;
    std::cout << 15291;
    for (int i = 0; i < 15291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15291(int& val) {
    const int x = 15291;
    std::cout << 15291;
    for (int i = 0; i < 15291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15292(int& val) {
    func_in_15292(val);
    const int x = 15292;
    std::cout << 15292;
    for (int i = 0; i < 15292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15292(int& val) {
    const int x = 15292;
    std::cout << 15292;
    for (int i = 0; i < 15292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15293(int& val) {
    func_in_15293(val);
    const int x = 15293;
    std::cout << 15293;
    for (int i = 0; i < 15293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15293(int& val) {
    const int x = 15293;
    std::cout << 15293;
    for (int i = 0; i < 15293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15294(int& val) {
    func_in_15294(val);
    const int x = 15294;
    std::cout << 15294;
    for (int i = 0; i < 15294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15294(int& val) {
    const int x = 15294;
    std::cout << 15294;
    for (int i = 0; i < 15294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15295(int& val) {
    func_in_15295(val);
    const int x = 15295;
    std::cout << 15295;
    for (int i = 0; i < 15295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15295(int& val) {
    const int x = 15295;
    std::cout << 15295;
    for (int i = 0; i < 15295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15296(int& val) {
    func_in_15296(val);
    const int x = 15296;
    std::cout << 15296;
    for (int i = 0; i < 15296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15296(int& val) {
    const int x = 15296;
    std::cout << 15296;
    for (int i = 0; i < 15296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15297(int& val) {
    func_in_15297(val);
    const int x = 15297;
    std::cout << 15297;
    for (int i = 0; i < 15297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15297(int& val) {
    const int x = 15297;
    std::cout << 15297;
    for (int i = 0; i < 15297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15298(int& val) {
    func_in_15298(val);
    const int x = 15298;
    std::cout << 15298;
    for (int i = 0; i < 15298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15298(int& val) {
    const int x = 15298;
    std::cout << 15298;
    for (int i = 0; i < 15298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15299(int& val) {
    func_in_15299(val);
    const int x = 15299;
    std::cout << 15299;
    for (int i = 0; i < 15299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15299(int& val) {
    const int x = 15299;
    std::cout << 15299;
    for (int i = 0; i < 15299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15300(int& val) {
    func_in_15300(val);
    const int x = 15300;
    std::cout << 15300;
    for (int i = 0; i < 15300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15300(int& val) {
    const int x = 15300;
    std::cout << 15300;
    for (int i = 0; i < 15300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15301(int& val) {
    func_in_15301(val);
    const int x = 15301;
    std::cout << 15301;
    for (int i = 0; i < 15301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15301(int& val) {
    const int x = 15301;
    std::cout << 15301;
    for (int i = 0; i < 15301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15302(int& val) {
    func_in_15302(val);
    const int x = 15302;
    std::cout << 15302;
    for (int i = 0; i < 15302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15302(int& val) {
    const int x = 15302;
    std::cout << 15302;
    for (int i = 0; i < 15302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15303(int& val) {
    func_in_15303(val);
    const int x = 15303;
    std::cout << 15303;
    for (int i = 0; i < 15303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15303(int& val) {
    const int x = 15303;
    std::cout << 15303;
    for (int i = 0; i < 15303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15304(int& val) {
    func_in_15304(val);
    const int x = 15304;
    std::cout << 15304;
    for (int i = 0; i < 15304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15304(int& val) {
    const int x = 15304;
    std::cout << 15304;
    for (int i = 0; i < 15304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15305(int& val) {
    func_in_15305(val);
    const int x = 15305;
    std::cout << 15305;
    for (int i = 0; i < 15305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15305(int& val) {
    const int x = 15305;
    std::cout << 15305;
    for (int i = 0; i < 15305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15306(int& val) {
    func_in_15306(val);
    const int x = 15306;
    std::cout << 15306;
    for (int i = 0; i < 15306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15306(int& val) {
    const int x = 15306;
    std::cout << 15306;
    for (int i = 0; i < 15306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15307(int& val) {
    func_in_15307(val);
    const int x = 15307;
    std::cout << 15307;
    for (int i = 0; i < 15307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15307(int& val) {
    const int x = 15307;
    std::cout << 15307;
    for (int i = 0; i < 15307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15308(int& val) {
    func_in_15308(val);
    const int x = 15308;
    std::cout << 15308;
    for (int i = 0; i < 15308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15308(int& val) {
    const int x = 15308;
    std::cout << 15308;
    for (int i = 0; i < 15308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15309(int& val) {
    func_in_15309(val);
    const int x = 15309;
    std::cout << 15309;
    for (int i = 0; i < 15309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15309(int& val) {
    const int x = 15309;
    std::cout << 15309;
    for (int i = 0; i < 15309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15310(int& val) {
    func_in_15310(val);
    const int x = 15310;
    std::cout << 15310;
    for (int i = 0; i < 15310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15310(int& val) {
    const int x = 15310;
    std::cout << 15310;
    for (int i = 0; i < 15310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15311(int& val) {
    func_in_15311(val);
    const int x = 15311;
    std::cout << 15311;
    for (int i = 0; i < 15311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15311(int& val) {
    const int x = 15311;
    std::cout << 15311;
    for (int i = 0; i < 15311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15312(int& val) {
    func_in_15312(val);
    const int x = 15312;
    std::cout << 15312;
    for (int i = 0; i < 15312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15312(int& val) {
    const int x = 15312;
    std::cout << 15312;
    for (int i = 0; i < 15312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15313(int& val) {
    func_in_15313(val);
    const int x = 15313;
    std::cout << 15313;
    for (int i = 0; i < 15313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15313(int& val) {
    const int x = 15313;
    std::cout << 15313;
    for (int i = 0; i < 15313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15314(int& val) {
    func_in_15314(val);
    const int x = 15314;
    std::cout << 15314;
    for (int i = 0; i < 15314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15314(int& val) {
    const int x = 15314;
    std::cout << 15314;
    for (int i = 0; i < 15314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15315(int& val) {
    func_in_15315(val);
    const int x = 15315;
    std::cout << 15315;
    for (int i = 0; i < 15315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15315(int& val) {
    const int x = 15315;
    std::cout << 15315;
    for (int i = 0; i < 15315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15316(int& val) {
    func_in_15316(val);
    const int x = 15316;
    std::cout << 15316;
    for (int i = 0; i < 15316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15316(int& val) {
    const int x = 15316;
    std::cout << 15316;
    for (int i = 0; i < 15316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15317(int& val) {
    func_in_15317(val);
    const int x = 15317;
    std::cout << 15317;
    for (int i = 0; i < 15317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15317(int& val) {
    const int x = 15317;
    std::cout << 15317;
    for (int i = 0; i < 15317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15318(int& val) {
    func_in_15318(val);
    const int x = 15318;
    std::cout << 15318;
    for (int i = 0; i < 15318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15318(int& val) {
    const int x = 15318;
    std::cout << 15318;
    for (int i = 0; i < 15318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15319(int& val) {
    func_in_15319(val);
    const int x = 15319;
    std::cout << 15319;
    for (int i = 0; i < 15319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15319(int& val) {
    const int x = 15319;
    std::cout << 15319;
    for (int i = 0; i < 15319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15320(int& val) {
    func_in_15320(val);
    const int x = 15320;
    std::cout << 15320;
    for (int i = 0; i < 15320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15320(int& val) {
    const int x = 15320;
    std::cout << 15320;
    for (int i = 0; i < 15320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15321(int& val) {
    func_in_15321(val);
    const int x = 15321;
    std::cout << 15321;
    for (int i = 0; i < 15321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15321(int& val) {
    const int x = 15321;
    std::cout << 15321;
    for (int i = 0; i < 15321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15322(int& val) {
    func_in_15322(val);
    const int x = 15322;
    std::cout << 15322;
    for (int i = 0; i < 15322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15322(int& val) {
    const int x = 15322;
    std::cout << 15322;
    for (int i = 0; i < 15322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15323(int& val) {
    func_in_15323(val);
    const int x = 15323;
    std::cout << 15323;
    for (int i = 0; i < 15323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15323(int& val) {
    const int x = 15323;
    std::cout << 15323;
    for (int i = 0; i < 15323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15324(int& val) {
    func_in_15324(val);
    const int x = 15324;
    std::cout << 15324;
    for (int i = 0; i < 15324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15324(int& val) {
    const int x = 15324;
    std::cout << 15324;
    for (int i = 0; i < 15324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15325(int& val) {
    func_in_15325(val);
    const int x = 15325;
    std::cout << 15325;
    for (int i = 0; i < 15325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15325(int& val) {
    const int x = 15325;
    std::cout << 15325;
    for (int i = 0; i < 15325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15326(int& val) {
    func_in_15326(val);
    const int x = 15326;
    std::cout << 15326;
    for (int i = 0; i < 15326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15326(int& val) {
    const int x = 15326;
    std::cout << 15326;
    for (int i = 0; i < 15326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15327(int& val) {
    func_in_15327(val);
    const int x = 15327;
    std::cout << 15327;
    for (int i = 0; i < 15327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15327(int& val) {
    const int x = 15327;
    std::cout << 15327;
    for (int i = 0; i < 15327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15328(int& val) {
    func_in_15328(val);
    const int x = 15328;
    std::cout << 15328;
    for (int i = 0; i < 15328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15328(int& val) {
    const int x = 15328;
    std::cout << 15328;
    for (int i = 0; i < 15328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15329(int& val) {
    func_in_15329(val);
    const int x = 15329;
    std::cout << 15329;
    for (int i = 0; i < 15329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15329(int& val) {
    const int x = 15329;
    std::cout << 15329;
    for (int i = 0; i < 15329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15330(int& val) {
    func_in_15330(val);
    const int x = 15330;
    std::cout << 15330;
    for (int i = 0; i < 15330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15330(int& val) {
    const int x = 15330;
    std::cout << 15330;
    for (int i = 0; i < 15330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15331(int& val) {
    func_in_15331(val);
    const int x = 15331;
    std::cout << 15331;
    for (int i = 0; i < 15331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15331(int& val) {
    const int x = 15331;
    std::cout << 15331;
    for (int i = 0; i < 15331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15332(int& val) {
    func_in_15332(val);
    const int x = 15332;
    std::cout << 15332;
    for (int i = 0; i < 15332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15332(int& val) {
    const int x = 15332;
    std::cout << 15332;
    for (int i = 0; i < 15332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15333(int& val) {
    func_in_15333(val);
    const int x = 15333;
    std::cout << 15333;
    for (int i = 0; i < 15333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15333(int& val) {
    const int x = 15333;
    std::cout << 15333;
    for (int i = 0; i < 15333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15334(int& val) {
    func_in_15334(val);
    const int x = 15334;
    std::cout << 15334;
    for (int i = 0; i < 15334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15334(int& val) {
    const int x = 15334;
    std::cout << 15334;
    for (int i = 0; i < 15334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15335(int& val) {
    func_in_15335(val);
    const int x = 15335;
    std::cout << 15335;
    for (int i = 0; i < 15335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15335(int& val) {
    const int x = 15335;
    std::cout << 15335;
    for (int i = 0; i < 15335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15336(int& val) {
    func_in_15336(val);
    const int x = 15336;
    std::cout << 15336;
    for (int i = 0; i < 15336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15336(int& val) {
    const int x = 15336;
    std::cout << 15336;
    for (int i = 0; i < 15336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15337(int& val) {
    func_in_15337(val);
    const int x = 15337;
    std::cout << 15337;
    for (int i = 0; i < 15337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15337(int& val) {
    const int x = 15337;
    std::cout << 15337;
    for (int i = 0; i < 15337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15338(int& val) {
    func_in_15338(val);
    const int x = 15338;
    std::cout << 15338;
    for (int i = 0; i < 15338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15338(int& val) {
    const int x = 15338;
    std::cout << 15338;
    for (int i = 0; i < 15338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15339(int& val) {
    func_in_15339(val);
    const int x = 15339;
    std::cout << 15339;
    for (int i = 0; i < 15339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15339(int& val) {
    const int x = 15339;
    std::cout << 15339;
    for (int i = 0; i < 15339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15340(int& val) {
    func_in_15340(val);
    const int x = 15340;
    std::cout << 15340;
    for (int i = 0; i < 15340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15340(int& val) {
    const int x = 15340;
    std::cout << 15340;
    for (int i = 0; i < 15340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15341(int& val) {
    func_in_15341(val);
    const int x = 15341;
    std::cout << 15341;
    for (int i = 0; i < 15341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15341(int& val) {
    const int x = 15341;
    std::cout << 15341;
    for (int i = 0; i < 15341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15342(int& val) {
    func_in_15342(val);
    const int x = 15342;
    std::cout << 15342;
    for (int i = 0; i < 15342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15342(int& val) {
    const int x = 15342;
    std::cout << 15342;
    for (int i = 0; i < 15342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15343(int& val) {
    func_in_15343(val);
    const int x = 15343;
    std::cout << 15343;
    for (int i = 0; i < 15343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15343(int& val) {
    const int x = 15343;
    std::cout << 15343;
    for (int i = 0; i < 15343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15344(int& val) {
    func_in_15344(val);
    const int x = 15344;
    std::cout << 15344;
    for (int i = 0; i < 15344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15344(int& val) {
    const int x = 15344;
    std::cout << 15344;
    for (int i = 0; i < 15344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15345(int& val) {
    func_in_15345(val);
    const int x = 15345;
    std::cout << 15345;
    for (int i = 0; i < 15345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15345(int& val) {
    const int x = 15345;
    std::cout << 15345;
    for (int i = 0; i < 15345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15346(int& val) {
    func_in_15346(val);
    const int x = 15346;
    std::cout << 15346;
    for (int i = 0; i < 15346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15346(int& val) {
    const int x = 15346;
    std::cout << 15346;
    for (int i = 0; i < 15346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15347(int& val) {
    func_in_15347(val);
    const int x = 15347;
    std::cout << 15347;
    for (int i = 0; i < 15347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15347(int& val) {
    const int x = 15347;
    std::cout << 15347;
    for (int i = 0; i < 15347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15348(int& val) {
    func_in_15348(val);
    const int x = 15348;
    std::cout << 15348;
    for (int i = 0; i < 15348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15348(int& val) {
    const int x = 15348;
    std::cout << 15348;
    for (int i = 0; i < 15348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15349(int& val) {
    func_in_15349(val);
    const int x = 15349;
    std::cout << 15349;
    for (int i = 0; i < 15349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15349(int& val) {
    const int x = 15349;
    std::cout << 15349;
    for (int i = 0; i < 15349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15350(int& val) {
    func_in_15350(val);
    const int x = 15350;
    std::cout << 15350;
    for (int i = 0; i < 15350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15350(int& val) {
    const int x = 15350;
    std::cout << 15350;
    for (int i = 0; i < 15350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15351(int& val) {
    func_in_15351(val);
    const int x = 15351;
    std::cout << 15351;
    for (int i = 0; i < 15351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15351(int& val) {
    const int x = 15351;
    std::cout << 15351;
    for (int i = 0; i < 15351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15352(int& val) {
    func_in_15352(val);
    const int x = 15352;
    std::cout << 15352;
    for (int i = 0; i < 15352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15352(int& val) {
    const int x = 15352;
    std::cout << 15352;
    for (int i = 0; i < 15352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15353(int& val) {
    func_in_15353(val);
    const int x = 15353;
    std::cout << 15353;
    for (int i = 0; i < 15353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15353(int& val) {
    const int x = 15353;
    std::cout << 15353;
    for (int i = 0; i < 15353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15354(int& val) {
    func_in_15354(val);
    const int x = 15354;
    std::cout << 15354;
    for (int i = 0; i < 15354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15354(int& val) {
    const int x = 15354;
    std::cout << 15354;
    for (int i = 0; i < 15354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15355(int& val) {
    func_in_15355(val);
    const int x = 15355;
    std::cout << 15355;
    for (int i = 0; i < 15355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15355(int& val) {
    const int x = 15355;
    std::cout << 15355;
    for (int i = 0; i < 15355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15356(int& val) {
    func_in_15356(val);
    const int x = 15356;
    std::cout << 15356;
    for (int i = 0; i < 15356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15356(int& val) {
    const int x = 15356;
    std::cout << 15356;
    for (int i = 0; i < 15356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15357(int& val) {
    func_in_15357(val);
    const int x = 15357;
    std::cout << 15357;
    for (int i = 0; i < 15357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15357(int& val) {
    const int x = 15357;
    std::cout << 15357;
    for (int i = 0; i < 15357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15358(int& val) {
    func_in_15358(val);
    const int x = 15358;
    std::cout << 15358;
    for (int i = 0; i < 15358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15358(int& val) {
    const int x = 15358;
    std::cout << 15358;
    for (int i = 0; i < 15358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15359(int& val) {
    func_in_15359(val);
    const int x = 15359;
    std::cout << 15359;
    for (int i = 0; i < 15359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15359(int& val) {
    const int x = 15359;
    std::cout << 15359;
    for (int i = 0; i < 15359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15360(int& val) {
    func_in_15360(val);
    const int x = 15360;
    std::cout << 15360;
    for (int i = 0; i < 15360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15360(int& val) {
    const int x = 15360;
    std::cout << 15360;
    for (int i = 0; i < 15360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15361(int& val) {
    func_in_15361(val);
    const int x = 15361;
    std::cout << 15361;
    for (int i = 0; i < 15361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15361(int& val) {
    const int x = 15361;
    std::cout << 15361;
    for (int i = 0; i < 15361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15362(int& val) {
    func_in_15362(val);
    const int x = 15362;
    std::cout << 15362;
    for (int i = 0; i < 15362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15362(int& val) {
    const int x = 15362;
    std::cout << 15362;
    for (int i = 0; i < 15362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15363(int& val) {
    func_in_15363(val);
    const int x = 15363;
    std::cout << 15363;
    for (int i = 0; i < 15363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15363(int& val) {
    const int x = 15363;
    std::cout << 15363;
    for (int i = 0; i < 15363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15364(int& val) {
    func_in_15364(val);
    const int x = 15364;
    std::cout << 15364;
    for (int i = 0; i < 15364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15364(int& val) {
    const int x = 15364;
    std::cout << 15364;
    for (int i = 0; i < 15364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15365(int& val) {
    func_in_15365(val);
    const int x = 15365;
    std::cout << 15365;
    for (int i = 0; i < 15365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15365(int& val) {
    const int x = 15365;
    std::cout << 15365;
    for (int i = 0; i < 15365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15366(int& val) {
    func_in_15366(val);
    const int x = 15366;
    std::cout << 15366;
    for (int i = 0; i < 15366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15366(int& val) {
    const int x = 15366;
    std::cout << 15366;
    for (int i = 0; i < 15366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15367(int& val) {
    func_in_15367(val);
    const int x = 15367;
    std::cout << 15367;
    for (int i = 0; i < 15367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15367(int& val) {
    const int x = 15367;
    std::cout << 15367;
    for (int i = 0; i < 15367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15368(int& val) {
    func_in_15368(val);
    const int x = 15368;
    std::cout << 15368;
    for (int i = 0; i < 15368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15368(int& val) {
    const int x = 15368;
    std::cout << 15368;
    for (int i = 0; i < 15368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15369(int& val) {
    func_in_15369(val);
    const int x = 15369;
    std::cout << 15369;
    for (int i = 0; i < 15369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15369(int& val) {
    const int x = 15369;
    std::cout << 15369;
    for (int i = 0; i < 15369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15370(int& val) {
    func_in_15370(val);
    const int x = 15370;
    std::cout << 15370;
    for (int i = 0; i < 15370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15370(int& val) {
    const int x = 15370;
    std::cout << 15370;
    for (int i = 0; i < 15370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15371(int& val) {
    func_in_15371(val);
    const int x = 15371;
    std::cout << 15371;
    for (int i = 0; i < 15371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15371(int& val) {
    const int x = 15371;
    std::cout << 15371;
    for (int i = 0; i < 15371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15372(int& val) {
    func_in_15372(val);
    const int x = 15372;
    std::cout << 15372;
    for (int i = 0; i < 15372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15372(int& val) {
    const int x = 15372;
    std::cout << 15372;
    for (int i = 0; i < 15372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15373(int& val) {
    func_in_15373(val);
    const int x = 15373;
    std::cout << 15373;
    for (int i = 0; i < 15373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15373(int& val) {
    const int x = 15373;
    std::cout << 15373;
    for (int i = 0; i < 15373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15374(int& val) {
    func_in_15374(val);
    const int x = 15374;
    std::cout << 15374;
    for (int i = 0; i < 15374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15374(int& val) {
    const int x = 15374;
    std::cout << 15374;
    for (int i = 0; i < 15374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15375(int& val) {
    func_in_15375(val);
    const int x = 15375;
    std::cout << 15375;
    for (int i = 0; i < 15375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15375(int& val) {
    const int x = 15375;
    std::cout << 15375;
    for (int i = 0; i < 15375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15376(int& val) {
    func_in_15376(val);
    const int x = 15376;
    std::cout << 15376;
    for (int i = 0; i < 15376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15376(int& val) {
    const int x = 15376;
    std::cout << 15376;
    for (int i = 0; i < 15376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15377(int& val) {
    func_in_15377(val);
    const int x = 15377;
    std::cout << 15377;
    for (int i = 0; i < 15377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15377(int& val) {
    const int x = 15377;
    std::cout << 15377;
    for (int i = 0; i < 15377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15378(int& val) {
    func_in_15378(val);
    const int x = 15378;
    std::cout << 15378;
    for (int i = 0; i < 15378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15378(int& val) {
    const int x = 15378;
    std::cout << 15378;
    for (int i = 0; i < 15378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15379(int& val) {
    func_in_15379(val);
    const int x = 15379;
    std::cout << 15379;
    for (int i = 0; i < 15379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15379(int& val) {
    const int x = 15379;
    std::cout << 15379;
    for (int i = 0; i < 15379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15380(int& val) {
    func_in_15380(val);
    const int x = 15380;
    std::cout << 15380;
    for (int i = 0; i < 15380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15380(int& val) {
    const int x = 15380;
    std::cout << 15380;
    for (int i = 0; i < 15380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15381(int& val) {
    func_in_15381(val);
    const int x = 15381;
    std::cout << 15381;
    for (int i = 0; i < 15381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15381(int& val) {
    const int x = 15381;
    std::cout << 15381;
    for (int i = 0; i < 15381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15382(int& val) {
    func_in_15382(val);
    const int x = 15382;
    std::cout << 15382;
    for (int i = 0; i < 15382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15382(int& val) {
    const int x = 15382;
    std::cout << 15382;
    for (int i = 0; i < 15382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15383(int& val) {
    func_in_15383(val);
    const int x = 15383;
    std::cout << 15383;
    for (int i = 0; i < 15383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15383(int& val) {
    const int x = 15383;
    std::cout << 15383;
    for (int i = 0; i < 15383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15384(int& val) {
    func_in_15384(val);
    const int x = 15384;
    std::cout << 15384;
    for (int i = 0; i < 15384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15384(int& val) {
    const int x = 15384;
    std::cout << 15384;
    for (int i = 0; i < 15384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15385(int& val) {
    func_in_15385(val);
    const int x = 15385;
    std::cout << 15385;
    for (int i = 0; i < 15385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15385(int& val) {
    const int x = 15385;
    std::cout << 15385;
    for (int i = 0; i < 15385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15386(int& val) {
    func_in_15386(val);
    const int x = 15386;
    std::cout << 15386;
    for (int i = 0; i < 15386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15386(int& val) {
    const int x = 15386;
    std::cout << 15386;
    for (int i = 0; i < 15386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15387(int& val) {
    func_in_15387(val);
    const int x = 15387;
    std::cout << 15387;
    for (int i = 0; i < 15387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15387(int& val) {
    const int x = 15387;
    std::cout << 15387;
    for (int i = 0; i < 15387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15388(int& val) {
    func_in_15388(val);
    const int x = 15388;
    std::cout << 15388;
    for (int i = 0; i < 15388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15388(int& val) {
    const int x = 15388;
    std::cout << 15388;
    for (int i = 0; i < 15388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15389(int& val) {
    func_in_15389(val);
    const int x = 15389;
    std::cout << 15389;
    for (int i = 0; i < 15389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15389(int& val) {
    const int x = 15389;
    std::cout << 15389;
    for (int i = 0; i < 15389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15390(int& val) {
    func_in_15390(val);
    const int x = 15390;
    std::cout << 15390;
    for (int i = 0; i < 15390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15390(int& val) {
    const int x = 15390;
    std::cout << 15390;
    for (int i = 0; i < 15390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15391(int& val) {
    func_in_15391(val);
    const int x = 15391;
    std::cout << 15391;
    for (int i = 0; i < 15391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15391(int& val) {
    const int x = 15391;
    std::cout << 15391;
    for (int i = 0; i < 15391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15392(int& val) {
    func_in_15392(val);
    const int x = 15392;
    std::cout << 15392;
    for (int i = 0; i < 15392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15392(int& val) {
    const int x = 15392;
    std::cout << 15392;
    for (int i = 0; i < 15392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15393(int& val) {
    func_in_15393(val);
    const int x = 15393;
    std::cout << 15393;
    for (int i = 0; i < 15393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15393(int& val) {
    const int x = 15393;
    std::cout << 15393;
    for (int i = 0; i < 15393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15394(int& val) {
    func_in_15394(val);
    const int x = 15394;
    std::cout << 15394;
    for (int i = 0; i < 15394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15394(int& val) {
    const int x = 15394;
    std::cout << 15394;
    for (int i = 0; i < 15394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15395(int& val) {
    func_in_15395(val);
    const int x = 15395;
    std::cout << 15395;
    for (int i = 0; i < 15395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15395(int& val) {
    const int x = 15395;
    std::cout << 15395;
    for (int i = 0; i < 15395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15396(int& val) {
    func_in_15396(val);
    const int x = 15396;
    std::cout << 15396;
    for (int i = 0; i < 15396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15396(int& val) {
    const int x = 15396;
    std::cout << 15396;
    for (int i = 0; i < 15396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15397(int& val) {
    func_in_15397(val);
    const int x = 15397;
    std::cout << 15397;
    for (int i = 0; i < 15397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15397(int& val) {
    const int x = 15397;
    std::cout << 15397;
    for (int i = 0; i < 15397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15398(int& val) {
    func_in_15398(val);
    const int x = 15398;
    std::cout << 15398;
    for (int i = 0; i < 15398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15398(int& val) {
    const int x = 15398;
    std::cout << 15398;
    for (int i = 0; i < 15398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15399(int& val) {
    func_in_15399(val);
    const int x = 15399;
    std::cout << 15399;
    for (int i = 0; i < 15399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15399(int& val) {
    const int x = 15399;
    std::cout << 15399;
    for (int i = 0; i < 15399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15400(int& val) {
    func_in_15400(val);
    const int x = 15400;
    std::cout << 15400;
    for (int i = 0; i < 15400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15400(int& val) {
    const int x = 15400;
    std::cout << 15400;
    for (int i = 0; i < 15400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15401(int& val) {
    func_in_15401(val);
    const int x = 15401;
    std::cout << 15401;
    for (int i = 0; i < 15401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15401(int& val) {
    const int x = 15401;
    std::cout << 15401;
    for (int i = 0; i < 15401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15402(int& val) {
    func_in_15402(val);
    const int x = 15402;
    std::cout << 15402;
    for (int i = 0; i < 15402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15402(int& val) {
    const int x = 15402;
    std::cout << 15402;
    for (int i = 0; i < 15402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15403(int& val) {
    func_in_15403(val);
    const int x = 15403;
    std::cout << 15403;
    for (int i = 0; i < 15403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15403(int& val) {
    const int x = 15403;
    std::cout << 15403;
    for (int i = 0; i < 15403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15404(int& val) {
    func_in_15404(val);
    const int x = 15404;
    std::cout << 15404;
    for (int i = 0; i < 15404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15404(int& val) {
    const int x = 15404;
    std::cout << 15404;
    for (int i = 0; i < 15404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15405(int& val) {
    func_in_15405(val);
    const int x = 15405;
    std::cout << 15405;
    for (int i = 0; i < 15405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15405(int& val) {
    const int x = 15405;
    std::cout << 15405;
    for (int i = 0; i < 15405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15406(int& val) {
    func_in_15406(val);
    const int x = 15406;
    std::cout << 15406;
    for (int i = 0; i < 15406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15406(int& val) {
    const int x = 15406;
    std::cout << 15406;
    for (int i = 0; i < 15406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15407(int& val) {
    func_in_15407(val);
    const int x = 15407;
    std::cout << 15407;
    for (int i = 0; i < 15407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15407(int& val) {
    const int x = 15407;
    std::cout << 15407;
    for (int i = 0; i < 15407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15408(int& val) {
    func_in_15408(val);
    const int x = 15408;
    std::cout << 15408;
    for (int i = 0; i < 15408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15408(int& val) {
    const int x = 15408;
    std::cout << 15408;
    for (int i = 0; i < 15408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15409(int& val) {
    func_in_15409(val);
    const int x = 15409;
    std::cout << 15409;
    for (int i = 0; i < 15409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15409(int& val) {
    const int x = 15409;
    std::cout << 15409;
    for (int i = 0; i < 15409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15410(int& val) {
    func_in_15410(val);
    const int x = 15410;
    std::cout << 15410;
    for (int i = 0; i < 15410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15410(int& val) {
    const int x = 15410;
    std::cout << 15410;
    for (int i = 0; i < 15410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15411(int& val) {
    func_in_15411(val);
    const int x = 15411;
    std::cout << 15411;
    for (int i = 0; i < 15411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15411(int& val) {
    const int x = 15411;
    std::cout << 15411;
    for (int i = 0; i < 15411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15412(int& val) {
    func_in_15412(val);
    const int x = 15412;
    std::cout << 15412;
    for (int i = 0; i < 15412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15412(int& val) {
    const int x = 15412;
    std::cout << 15412;
    for (int i = 0; i < 15412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15413(int& val) {
    func_in_15413(val);
    const int x = 15413;
    std::cout << 15413;
    for (int i = 0; i < 15413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15413(int& val) {
    const int x = 15413;
    std::cout << 15413;
    for (int i = 0; i < 15413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15414(int& val) {
    func_in_15414(val);
    const int x = 15414;
    std::cout << 15414;
    for (int i = 0; i < 15414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15414(int& val) {
    const int x = 15414;
    std::cout << 15414;
    for (int i = 0; i < 15414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15415(int& val) {
    func_in_15415(val);
    const int x = 15415;
    std::cout << 15415;
    for (int i = 0; i < 15415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15415(int& val) {
    const int x = 15415;
    std::cout << 15415;
    for (int i = 0; i < 15415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15416(int& val) {
    func_in_15416(val);
    const int x = 15416;
    std::cout << 15416;
    for (int i = 0; i < 15416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15416(int& val) {
    const int x = 15416;
    std::cout << 15416;
    for (int i = 0; i < 15416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15417(int& val) {
    func_in_15417(val);
    const int x = 15417;
    std::cout << 15417;
    for (int i = 0; i < 15417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15417(int& val) {
    const int x = 15417;
    std::cout << 15417;
    for (int i = 0; i < 15417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15418(int& val) {
    func_in_15418(val);
    const int x = 15418;
    std::cout << 15418;
    for (int i = 0; i < 15418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15418(int& val) {
    const int x = 15418;
    std::cout << 15418;
    for (int i = 0; i < 15418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15419(int& val) {
    func_in_15419(val);
    const int x = 15419;
    std::cout << 15419;
    for (int i = 0; i < 15419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15419(int& val) {
    const int x = 15419;
    std::cout << 15419;
    for (int i = 0; i < 15419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15420(int& val) {
    func_in_15420(val);
    const int x = 15420;
    std::cout << 15420;
    for (int i = 0; i < 15420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15420(int& val) {
    const int x = 15420;
    std::cout << 15420;
    for (int i = 0; i < 15420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15421(int& val) {
    func_in_15421(val);
    const int x = 15421;
    std::cout << 15421;
    for (int i = 0; i < 15421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15421(int& val) {
    const int x = 15421;
    std::cout << 15421;
    for (int i = 0; i < 15421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15422(int& val) {
    func_in_15422(val);
    const int x = 15422;
    std::cout << 15422;
    for (int i = 0; i < 15422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15422(int& val) {
    const int x = 15422;
    std::cout << 15422;
    for (int i = 0; i < 15422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15423(int& val) {
    func_in_15423(val);
    const int x = 15423;
    std::cout << 15423;
    for (int i = 0; i < 15423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15423(int& val) {
    const int x = 15423;
    std::cout << 15423;
    for (int i = 0; i < 15423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15424(int& val) {
    func_in_15424(val);
    const int x = 15424;
    std::cout << 15424;
    for (int i = 0; i < 15424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15424(int& val) {
    const int x = 15424;
    std::cout << 15424;
    for (int i = 0; i < 15424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15425(int& val) {
    func_in_15425(val);
    const int x = 15425;
    std::cout << 15425;
    for (int i = 0; i < 15425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15425(int& val) {
    const int x = 15425;
    std::cout << 15425;
    for (int i = 0; i < 15425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15426(int& val) {
    func_in_15426(val);
    const int x = 15426;
    std::cout << 15426;
    for (int i = 0; i < 15426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15426(int& val) {
    const int x = 15426;
    std::cout << 15426;
    for (int i = 0; i < 15426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15427(int& val) {
    func_in_15427(val);
    const int x = 15427;
    std::cout << 15427;
    for (int i = 0; i < 15427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15427(int& val) {
    const int x = 15427;
    std::cout << 15427;
    for (int i = 0; i < 15427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15428(int& val) {
    func_in_15428(val);
    const int x = 15428;
    std::cout << 15428;
    for (int i = 0; i < 15428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15428(int& val) {
    const int x = 15428;
    std::cout << 15428;
    for (int i = 0; i < 15428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15429(int& val) {
    func_in_15429(val);
    const int x = 15429;
    std::cout << 15429;
    for (int i = 0; i < 15429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15429(int& val) {
    const int x = 15429;
    std::cout << 15429;
    for (int i = 0; i < 15429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15430(int& val) {
    func_in_15430(val);
    const int x = 15430;
    std::cout << 15430;
    for (int i = 0; i < 15430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15430(int& val) {
    const int x = 15430;
    std::cout << 15430;
    for (int i = 0; i < 15430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15431(int& val) {
    func_in_15431(val);
    const int x = 15431;
    std::cout << 15431;
    for (int i = 0; i < 15431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15431(int& val) {
    const int x = 15431;
    std::cout << 15431;
    for (int i = 0; i < 15431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15432(int& val) {
    func_in_15432(val);
    const int x = 15432;
    std::cout << 15432;
    for (int i = 0; i < 15432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15432(int& val) {
    const int x = 15432;
    std::cout << 15432;
    for (int i = 0; i < 15432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15433(int& val) {
    func_in_15433(val);
    const int x = 15433;
    std::cout << 15433;
    for (int i = 0; i < 15433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15433(int& val) {
    const int x = 15433;
    std::cout << 15433;
    for (int i = 0; i < 15433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15434(int& val) {
    func_in_15434(val);
    const int x = 15434;
    std::cout << 15434;
    for (int i = 0; i < 15434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15434(int& val) {
    const int x = 15434;
    std::cout << 15434;
    for (int i = 0; i < 15434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15435(int& val) {
    func_in_15435(val);
    const int x = 15435;
    std::cout << 15435;
    for (int i = 0; i < 15435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15435(int& val) {
    const int x = 15435;
    std::cout << 15435;
    for (int i = 0; i < 15435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15436(int& val) {
    func_in_15436(val);
    const int x = 15436;
    std::cout << 15436;
    for (int i = 0; i < 15436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15436(int& val) {
    const int x = 15436;
    std::cout << 15436;
    for (int i = 0; i < 15436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15437(int& val) {
    func_in_15437(val);
    const int x = 15437;
    std::cout << 15437;
    for (int i = 0; i < 15437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15437(int& val) {
    const int x = 15437;
    std::cout << 15437;
    for (int i = 0; i < 15437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15438(int& val) {
    func_in_15438(val);
    const int x = 15438;
    std::cout << 15438;
    for (int i = 0; i < 15438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15438(int& val) {
    const int x = 15438;
    std::cout << 15438;
    for (int i = 0; i < 15438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15439(int& val) {
    func_in_15439(val);
    const int x = 15439;
    std::cout << 15439;
    for (int i = 0; i < 15439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15439(int& val) {
    const int x = 15439;
    std::cout << 15439;
    for (int i = 0; i < 15439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15440(int& val) {
    func_in_15440(val);
    const int x = 15440;
    std::cout << 15440;
    for (int i = 0; i < 15440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15440(int& val) {
    const int x = 15440;
    std::cout << 15440;
    for (int i = 0; i < 15440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15441(int& val) {
    func_in_15441(val);
    const int x = 15441;
    std::cout << 15441;
    for (int i = 0; i < 15441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15441(int& val) {
    const int x = 15441;
    std::cout << 15441;
    for (int i = 0; i < 15441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15442(int& val) {
    func_in_15442(val);
    const int x = 15442;
    std::cout << 15442;
    for (int i = 0; i < 15442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15442(int& val) {
    const int x = 15442;
    std::cout << 15442;
    for (int i = 0; i < 15442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15443(int& val) {
    func_in_15443(val);
    const int x = 15443;
    std::cout << 15443;
    for (int i = 0; i < 15443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15443(int& val) {
    const int x = 15443;
    std::cout << 15443;
    for (int i = 0; i < 15443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15444(int& val) {
    func_in_15444(val);
    const int x = 15444;
    std::cout << 15444;
    for (int i = 0; i < 15444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15444(int& val) {
    const int x = 15444;
    std::cout << 15444;
    for (int i = 0; i < 15444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15445(int& val) {
    func_in_15445(val);
    const int x = 15445;
    std::cout << 15445;
    for (int i = 0; i < 15445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15445(int& val) {
    const int x = 15445;
    std::cout << 15445;
    for (int i = 0; i < 15445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15446(int& val) {
    func_in_15446(val);
    const int x = 15446;
    std::cout << 15446;
    for (int i = 0; i < 15446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15446(int& val) {
    const int x = 15446;
    std::cout << 15446;
    for (int i = 0; i < 15446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15447(int& val) {
    func_in_15447(val);
    const int x = 15447;
    std::cout << 15447;
    for (int i = 0; i < 15447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15447(int& val) {
    const int x = 15447;
    std::cout << 15447;
    for (int i = 0; i < 15447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15448(int& val) {
    func_in_15448(val);
    const int x = 15448;
    std::cout << 15448;
    for (int i = 0; i < 15448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15448(int& val) {
    const int x = 15448;
    std::cout << 15448;
    for (int i = 0; i < 15448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15449(int& val) {
    func_in_15449(val);
    const int x = 15449;
    std::cout << 15449;
    for (int i = 0; i < 15449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15449(int& val) {
    const int x = 15449;
    std::cout << 15449;
    for (int i = 0; i < 15449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15450(int& val) {
    func_in_15450(val);
    const int x = 15450;
    std::cout << 15450;
    for (int i = 0; i < 15450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15450(int& val) {
    const int x = 15450;
    std::cout << 15450;
    for (int i = 0; i < 15450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15451(int& val) {
    func_in_15451(val);
    const int x = 15451;
    std::cout << 15451;
    for (int i = 0; i < 15451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15451(int& val) {
    const int x = 15451;
    std::cout << 15451;
    for (int i = 0; i < 15451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15452(int& val) {
    func_in_15452(val);
    const int x = 15452;
    std::cout << 15452;
    for (int i = 0; i < 15452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15452(int& val) {
    const int x = 15452;
    std::cout << 15452;
    for (int i = 0; i < 15452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15453(int& val) {
    func_in_15453(val);
    const int x = 15453;
    std::cout << 15453;
    for (int i = 0; i < 15453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15453(int& val) {
    const int x = 15453;
    std::cout << 15453;
    for (int i = 0; i < 15453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15454(int& val) {
    func_in_15454(val);
    const int x = 15454;
    std::cout << 15454;
    for (int i = 0; i < 15454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15454(int& val) {
    const int x = 15454;
    std::cout << 15454;
    for (int i = 0; i < 15454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15455(int& val) {
    func_in_15455(val);
    const int x = 15455;
    std::cout << 15455;
    for (int i = 0; i < 15455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15455(int& val) {
    const int x = 15455;
    std::cout << 15455;
    for (int i = 0; i < 15455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15456(int& val) {
    func_in_15456(val);
    const int x = 15456;
    std::cout << 15456;
    for (int i = 0; i < 15456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15456(int& val) {
    const int x = 15456;
    std::cout << 15456;
    for (int i = 0; i < 15456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15457(int& val) {
    func_in_15457(val);
    const int x = 15457;
    std::cout << 15457;
    for (int i = 0; i < 15457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15457(int& val) {
    const int x = 15457;
    std::cout << 15457;
    for (int i = 0; i < 15457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15458(int& val) {
    func_in_15458(val);
    const int x = 15458;
    std::cout << 15458;
    for (int i = 0; i < 15458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15458(int& val) {
    const int x = 15458;
    std::cout << 15458;
    for (int i = 0; i < 15458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15459(int& val) {
    func_in_15459(val);
    const int x = 15459;
    std::cout << 15459;
    for (int i = 0; i < 15459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15459(int& val) {
    const int x = 15459;
    std::cout << 15459;
    for (int i = 0; i < 15459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15460(int& val) {
    func_in_15460(val);
    const int x = 15460;
    std::cout << 15460;
    for (int i = 0; i < 15460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15460(int& val) {
    const int x = 15460;
    std::cout << 15460;
    for (int i = 0; i < 15460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15461(int& val) {
    func_in_15461(val);
    const int x = 15461;
    std::cout << 15461;
    for (int i = 0; i < 15461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15461(int& val) {
    const int x = 15461;
    std::cout << 15461;
    for (int i = 0; i < 15461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15462(int& val) {
    func_in_15462(val);
    const int x = 15462;
    std::cout << 15462;
    for (int i = 0; i < 15462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15462(int& val) {
    const int x = 15462;
    std::cout << 15462;
    for (int i = 0; i < 15462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15463(int& val) {
    func_in_15463(val);
    const int x = 15463;
    std::cout << 15463;
    for (int i = 0; i < 15463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15463(int& val) {
    const int x = 15463;
    std::cout << 15463;
    for (int i = 0; i < 15463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15464(int& val) {
    func_in_15464(val);
    const int x = 15464;
    std::cout << 15464;
    for (int i = 0; i < 15464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15464(int& val) {
    const int x = 15464;
    std::cout << 15464;
    for (int i = 0; i < 15464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15465(int& val) {
    func_in_15465(val);
    const int x = 15465;
    std::cout << 15465;
    for (int i = 0; i < 15465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15465(int& val) {
    const int x = 15465;
    std::cout << 15465;
    for (int i = 0; i < 15465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15466(int& val) {
    func_in_15466(val);
    const int x = 15466;
    std::cout << 15466;
    for (int i = 0; i < 15466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15466(int& val) {
    const int x = 15466;
    std::cout << 15466;
    for (int i = 0; i < 15466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15467(int& val) {
    func_in_15467(val);
    const int x = 15467;
    std::cout << 15467;
    for (int i = 0; i < 15467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15467(int& val) {
    const int x = 15467;
    std::cout << 15467;
    for (int i = 0; i < 15467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15468(int& val) {
    func_in_15468(val);
    const int x = 15468;
    std::cout << 15468;
    for (int i = 0; i < 15468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15468(int& val) {
    const int x = 15468;
    std::cout << 15468;
    for (int i = 0; i < 15468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15469(int& val) {
    func_in_15469(val);
    const int x = 15469;
    std::cout << 15469;
    for (int i = 0; i < 15469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15469(int& val) {
    const int x = 15469;
    std::cout << 15469;
    for (int i = 0; i < 15469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15470(int& val) {
    func_in_15470(val);
    const int x = 15470;
    std::cout << 15470;
    for (int i = 0; i < 15470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15470(int& val) {
    const int x = 15470;
    std::cout << 15470;
    for (int i = 0; i < 15470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15471(int& val) {
    func_in_15471(val);
    const int x = 15471;
    std::cout << 15471;
    for (int i = 0; i < 15471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15471(int& val) {
    const int x = 15471;
    std::cout << 15471;
    for (int i = 0; i < 15471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15472(int& val) {
    func_in_15472(val);
    const int x = 15472;
    std::cout << 15472;
    for (int i = 0; i < 15472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15472(int& val) {
    const int x = 15472;
    std::cout << 15472;
    for (int i = 0; i < 15472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15473(int& val) {
    func_in_15473(val);
    const int x = 15473;
    std::cout << 15473;
    for (int i = 0; i < 15473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15473(int& val) {
    const int x = 15473;
    std::cout << 15473;
    for (int i = 0; i < 15473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15474(int& val) {
    func_in_15474(val);
    const int x = 15474;
    std::cout << 15474;
    for (int i = 0; i < 15474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15474(int& val) {
    const int x = 15474;
    std::cout << 15474;
    for (int i = 0; i < 15474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15475(int& val) {
    func_in_15475(val);
    const int x = 15475;
    std::cout << 15475;
    for (int i = 0; i < 15475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15475(int& val) {
    const int x = 15475;
    std::cout << 15475;
    for (int i = 0; i < 15475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15476(int& val) {
    func_in_15476(val);
    const int x = 15476;
    std::cout << 15476;
    for (int i = 0; i < 15476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15476(int& val) {
    const int x = 15476;
    std::cout << 15476;
    for (int i = 0; i < 15476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15477(int& val) {
    func_in_15477(val);
    const int x = 15477;
    std::cout << 15477;
    for (int i = 0; i < 15477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15477(int& val) {
    const int x = 15477;
    std::cout << 15477;
    for (int i = 0; i < 15477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15478(int& val) {
    func_in_15478(val);
    const int x = 15478;
    std::cout << 15478;
    for (int i = 0; i < 15478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15478(int& val) {
    const int x = 15478;
    std::cout << 15478;
    for (int i = 0; i < 15478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15479(int& val) {
    func_in_15479(val);
    const int x = 15479;
    std::cout << 15479;
    for (int i = 0; i < 15479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15479(int& val) {
    const int x = 15479;
    std::cout << 15479;
    for (int i = 0; i < 15479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15480(int& val) {
    func_in_15480(val);
    const int x = 15480;
    std::cout << 15480;
    for (int i = 0; i < 15480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15480(int& val) {
    const int x = 15480;
    std::cout << 15480;
    for (int i = 0; i < 15480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15481(int& val) {
    func_in_15481(val);
    const int x = 15481;
    std::cout << 15481;
    for (int i = 0; i < 15481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15481(int& val) {
    const int x = 15481;
    std::cout << 15481;
    for (int i = 0; i < 15481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15482(int& val) {
    func_in_15482(val);
    const int x = 15482;
    std::cout << 15482;
    for (int i = 0; i < 15482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15482(int& val) {
    const int x = 15482;
    std::cout << 15482;
    for (int i = 0; i < 15482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15483(int& val) {
    func_in_15483(val);
    const int x = 15483;
    std::cout << 15483;
    for (int i = 0; i < 15483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15483(int& val) {
    const int x = 15483;
    std::cout << 15483;
    for (int i = 0; i < 15483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15484(int& val) {
    func_in_15484(val);
    const int x = 15484;
    std::cout << 15484;
    for (int i = 0; i < 15484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15484(int& val) {
    const int x = 15484;
    std::cout << 15484;
    for (int i = 0; i < 15484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15485(int& val) {
    func_in_15485(val);
    const int x = 15485;
    std::cout << 15485;
    for (int i = 0; i < 15485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15485(int& val) {
    const int x = 15485;
    std::cout << 15485;
    for (int i = 0; i < 15485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15486(int& val) {
    func_in_15486(val);
    const int x = 15486;
    std::cout << 15486;
    for (int i = 0; i < 15486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15486(int& val) {
    const int x = 15486;
    std::cout << 15486;
    for (int i = 0; i < 15486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15487(int& val) {
    func_in_15487(val);
    const int x = 15487;
    std::cout << 15487;
    for (int i = 0; i < 15487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15487(int& val) {
    const int x = 15487;
    std::cout << 15487;
    for (int i = 0; i < 15487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15488(int& val) {
    func_in_15488(val);
    const int x = 15488;
    std::cout << 15488;
    for (int i = 0; i < 15488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15488(int& val) {
    const int x = 15488;
    std::cout << 15488;
    for (int i = 0; i < 15488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15489(int& val) {
    func_in_15489(val);
    const int x = 15489;
    std::cout << 15489;
    for (int i = 0; i < 15489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15489(int& val) {
    const int x = 15489;
    std::cout << 15489;
    for (int i = 0; i < 15489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15490(int& val) {
    func_in_15490(val);
    const int x = 15490;
    std::cout << 15490;
    for (int i = 0; i < 15490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15490(int& val) {
    const int x = 15490;
    std::cout << 15490;
    for (int i = 0; i < 15490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15491(int& val) {
    func_in_15491(val);
    const int x = 15491;
    std::cout << 15491;
    for (int i = 0; i < 15491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15491(int& val) {
    const int x = 15491;
    std::cout << 15491;
    for (int i = 0; i < 15491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15492(int& val) {
    func_in_15492(val);
    const int x = 15492;
    std::cout << 15492;
    for (int i = 0; i < 15492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15492(int& val) {
    const int x = 15492;
    std::cout << 15492;
    for (int i = 0; i < 15492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15493(int& val) {
    func_in_15493(val);
    const int x = 15493;
    std::cout << 15493;
    for (int i = 0; i < 15493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15493(int& val) {
    const int x = 15493;
    std::cout << 15493;
    for (int i = 0; i < 15493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15494(int& val) {
    func_in_15494(val);
    const int x = 15494;
    std::cout << 15494;
    for (int i = 0; i < 15494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15494(int& val) {
    const int x = 15494;
    std::cout << 15494;
    for (int i = 0; i < 15494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15495(int& val) {
    func_in_15495(val);
    const int x = 15495;
    std::cout << 15495;
    for (int i = 0; i < 15495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15495(int& val) {
    const int x = 15495;
    std::cout << 15495;
    for (int i = 0; i < 15495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15496(int& val) {
    func_in_15496(val);
    const int x = 15496;
    std::cout << 15496;
    for (int i = 0; i < 15496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15496(int& val) {
    const int x = 15496;
    std::cout << 15496;
    for (int i = 0; i < 15496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15497(int& val) {
    func_in_15497(val);
    const int x = 15497;
    std::cout << 15497;
    for (int i = 0; i < 15497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15497(int& val) {
    const int x = 15497;
    std::cout << 15497;
    for (int i = 0; i < 15497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15498(int& val) {
    func_in_15498(val);
    const int x = 15498;
    std::cout << 15498;
    for (int i = 0; i < 15498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15498(int& val) {
    const int x = 15498;
    std::cout << 15498;
    for (int i = 0; i < 15498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15499(int& val) {
    func_in_15499(val);
    const int x = 15499;
    std::cout << 15499;
    for (int i = 0; i < 15499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15499(int& val) {
    const int x = 15499;
    std::cout << 15499;
    for (int i = 0; i < 15499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15500(int& val) {
    func_in_15500(val);
    const int x = 15500;
    std::cout << 15500;
    for (int i = 0; i < 15500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15500(int& val) {
    const int x = 15500;
    std::cout << 15500;
    for (int i = 0; i < 15500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15501(int& val) {
    func_in_15501(val);
    const int x = 15501;
    std::cout << 15501;
    for (int i = 0; i < 15501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15501(int& val) {
    const int x = 15501;
    std::cout << 15501;
    for (int i = 0; i < 15501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15502(int& val) {
    func_in_15502(val);
    const int x = 15502;
    std::cout << 15502;
    for (int i = 0; i < 15502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15502(int& val) {
    const int x = 15502;
    std::cout << 15502;
    for (int i = 0; i < 15502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15503(int& val) {
    func_in_15503(val);
    const int x = 15503;
    std::cout << 15503;
    for (int i = 0; i < 15503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15503(int& val) {
    const int x = 15503;
    std::cout << 15503;
    for (int i = 0; i < 15503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15504(int& val) {
    func_in_15504(val);
    const int x = 15504;
    std::cout << 15504;
    for (int i = 0; i < 15504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15504(int& val) {
    const int x = 15504;
    std::cout << 15504;
    for (int i = 0; i < 15504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15505(int& val) {
    func_in_15505(val);
    const int x = 15505;
    std::cout << 15505;
    for (int i = 0; i < 15505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15505(int& val) {
    const int x = 15505;
    std::cout << 15505;
    for (int i = 0; i < 15505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15506(int& val) {
    func_in_15506(val);
    const int x = 15506;
    std::cout << 15506;
    for (int i = 0; i < 15506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15506(int& val) {
    const int x = 15506;
    std::cout << 15506;
    for (int i = 0; i < 15506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15507(int& val) {
    func_in_15507(val);
    const int x = 15507;
    std::cout << 15507;
    for (int i = 0; i < 15507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15507(int& val) {
    const int x = 15507;
    std::cout << 15507;
    for (int i = 0; i < 15507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15508(int& val) {
    func_in_15508(val);
    const int x = 15508;
    std::cout << 15508;
    for (int i = 0; i < 15508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15508(int& val) {
    const int x = 15508;
    std::cout << 15508;
    for (int i = 0; i < 15508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15509(int& val) {
    func_in_15509(val);
    const int x = 15509;
    std::cout << 15509;
    for (int i = 0; i < 15509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15509(int& val) {
    const int x = 15509;
    std::cout << 15509;
    for (int i = 0; i < 15509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15510(int& val) {
    func_in_15510(val);
    const int x = 15510;
    std::cout << 15510;
    for (int i = 0; i < 15510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15510(int& val) {
    const int x = 15510;
    std::cout << 15510;
    for (int i = 0; i < 15510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15511(int& val) {
    func_in_15511(val);
    const int x = 15511;
    std::cout << 15511;
    for (int i = 0; i < 15511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15511(int& val) {
    const int x = 15511;
    std::cout << 15511;
    for (int i = 0; i < 15511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15512(int& val) {
    func_in_15512(val);
    const int x = 15512;
    std::cout << 15512;
    for (int i = 0; i < 15512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15512(int& val) {
    const int x = 15512;
    std::cout << 15512;
    for (int i = 0; i < 15512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15513(int& val) {
    func_in_15513(val);
    const int x = 15513;
    std::cout << 15513;
    for (int i = 0; i < 15513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15513(int& val) {
    const int x = 15513;
    std::cout << 15513;
    for (int i = 0; i < 15513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15514(int& val) {
    func_in_15514(val);
    const int x = 15514;
    std::cout << 15514;
    for (int i = 0; i < 15514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15514(int& val) {
    const int x = 15514;
    std::cout << 15514;
    for (int i = 0; i < 15514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15515(int& val) {
    func_in_15515(val);
    const int x = 15515;
    std::cout << 15515;
    for (int i = 0; i < 15515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15515(int& val) {
    const int x = 15515;
    std::cout << 15515;
    for (int i = 0; i < 15515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15516(int& val) {
    func_in_15516(val);
    const int x = 15516;
    std::cout << 15516;
    for (int i = 0; i < 15516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15516(int& val) {
    const int x = 15516;
    std::cout << 15516;
    for (int i = 0; i < 15516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15517(int& val) {
    func_in_15517(val);
    const int x = 15517;
    std::cout << 15517;
    for (int i = 0; i < 15517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15517(int& val) {
    const int x = 15517;
    std::cout << 15517;
    for (int i = 0; i < 15517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15518(int& val) {
    func_in_15518(val);
    const int x = 15518;
    std::cout << 15518;
    for (int i = 0; i < 15518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15518(int& val) {
    const int x = 15518;
    std::cout << 15518;
    for (int i = 0; i < 15518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15519(int& val) {
    func_in_15519(val);
    const int x = 15519;
    std::cout << 15519;
    for (int i = 0; i < 15519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15519(int& val) {
    const int x = 15519;
    std::cout << 15519;
    for (int i = 0; i < 15519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15520(int& val) {
    func_in_15520(val);
    const int x = 15520;
    std::cout << 15520;
    for (int i = 0; i < 15520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15520(int& val) {
    const int x = 15520;
    std::cout << 15520;
    for (int i = 0; i < 15520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15521(int& val) {
    func_in_15521(val);
    const int x = 15521;
    std::cout << 15521;
    for (int i = 0; i < 15521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15521(int& val) {
    const int x = 15521;
    std::cout << 15521;
    for (int i = 0; i < 15521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15522(int& val) {
    func_in_15522(val);
    const int x = 15522;
    std::cout << 15522;
    for (int i = 0; i < 15522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15522(int& val) {
    const int x = 15522;
    std::cout << 15522;
    for (int i = 0; i < 15522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15523(int& val) {
    func_in_15523(val);
    const int x = 15523;
    std::cout << 15523;
    for (int i = 0; i < 15523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15523(int& val) {
    const int x = 15523;
    std::cout << 15523;
    for (int i = 0; i < 15523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15524(int& val) {
    func_in_15524(val);
    const int x = 15524;
    std::cout << 15524;
    for (int i = 0; i < 15524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15524(int& val) {
    const int x = 15524;
    std::cout << 15524;
    for (int i = 0; i < 15524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15525(int& val) {
    func_in_15525(val);
    const int x = 15525;
    std::cout << 15525;
    for (int i = 0; i < 15525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15525(int& val) {
    const int x = 15525;
    std::cout << 15525;
    for (int i = 0; i < 15525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15526(int& val) {
    func_in_15526(val);
    const int x = 15526;
    std::cout << 15526;
    for (int i = 0; i < 15526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15526(int& val) {
    const int x = 15526;
    std::cout << 15526;
    for (int i = 0; i < 15526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15527(int& val) {
    func_in_15527(val);
    const int x = 15527;
    std::cout << 15527;
    for (int i = 0; i < 15527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15527(int& val) {
    const int x = 15527;
    std::cout << 15527;
    for (int i = 0; i < 15527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15528(int& val) {
    func_in_15528(val);
    const int x = 15528;
    std::cout << 15528;
    for (int i = 0; i < 15528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15528(int& val) {
    const int x = 15528;
    std::cout << 15528;
    for (int i = 0; i < 15528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15529(int& val) {
    func_in_15529(val);
    const int x = 15529;
    std::cout << 15529;
    for (int i = 0; i < 15529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15529(int& val) {
    const int x = 15529;
    std::cout << 15529;
    for (int i = 0; i < 15529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15530(int& val) {
    func_in_15530(val);
    const int x = 15530;
    std::cout << 15530;
    for (int i = 0; i < 15530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15530(int& val) {
    const int x = 15530;
    std::cout << 15530;
    for (int i = 0; i < 15530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15531(int& val) {
    func_in_15531(val);
    const int x = 15531;
    std::cout << 15531;
    for (int i = 0; i < 15531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15531(int& val) {
    const int x = 15531;
    std::cout << 15531;
    for (int i = 0; i < 15531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15532(int& val) {
    func_in_15532(val);
    const int x = 15532;
    std::cout << 15532;
    for (int i = 0; i < 15532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15532(int& val) {
    const int x = 15532;
    std::cout << 15532;
    for (int i = 0; i < 15532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15533(int& val) {
    func_in_15533(val);
    const int x = 15533;
    std::cout << 15533;
    for (int i = 0; i < 15533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15533(int& val) {
    const int x = 15533;
    std::cout << 15533;
    for (int i = 0; i < 15533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15534(int& val) {
    func_in_15534(val);
    const int x = 15534;
    std::cout << 15534;
    for (int i = 0; i < 15534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15534(int& val) {
    const int x = 15534;
    std::cout << 15534;
    for (int i = 0; i < 15534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15535(int& val) {
    func_in_15535(val);
    const int x = 15535;
    std::cout << 15535;
    for (int i = 0; i < 15535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15535(int& val) {
    const int x = 15535;
    std::cout << 15535;
    for (int i = 0; i < 15535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15536(int& val) {
    func_in_15536(val);
    const int x = 15536;
    std::cout << 15536;
    for (int i = 0; i < 15536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15536(int& val) {
    const int x = 15536;
    std::cout << 15536;
    for (int i = 0; i < 15536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15537(int& val) {
    func_in_15537(val);
    const int x = 15537;
    std::cout << 15537;
    for (int i = 0; i < 15537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15537(int& val) {
    const int x = 15537;
    std::cout << 15537;
    for (int i = 0; i < 15537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15538(int& val) {
    func_in_15538(val);
    const int x = 15538;
    std::cout << 15538;
    for (int i = 0; i < 15538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15538(int& val) {
    const int x = 15538;
    std::cout << 15538;
    for (int i = 0; i < 15538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15539(int& val) {
    func_in_15539(val);
    const int x = 15539;
    std::cout << 15539;
    for (int i = 0; i < 15539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15539(int& val) {
    const int x = 15539;
    std::cout << 15539;
    for (int i = 0; i < 15539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15540(int& val) {
    func_in_15540(val);
    const int x = 15540;
    std::cout << 15540;
    for (int i = 0; i < 15540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15540(int& val) {
    const int x = 15540;
    std::cout << 15540;
    for (int i = 0; i < 15540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15541(int& val) {
    func_in_15541(val);
    const int x = 15541;
    std::cout << 15541;
    for (int i = 0; i < 15541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15541(int& val) {
    const int x = 15541;
    std::cout << 15541;
    for (int i = 0; i < 15541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15542(int& val) {
    func_in_15542(val);
    const int x = 15542;
    std::cout << 15542;
    for (int i = 0; i < 15542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15542(int& val) {
    const int x = 15542;
    std::cout << 15542;
    for (int i = 0; i < 15542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15543(int& val) {
    func_in_15543(val);
    const int x = 15543;
    std::cout << 15543;
    for (int i = 0; i < 15543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15543(int& val) {
    const int x = 15543;
    std::cout << 15543;
    for (int i = 0; i < 15543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15544(int& val) {
    func_in_15544(val);
    const int x = 15544;
    std::cout << 15544;
    for (int i = 0; i < 15544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15544(int& val) {
    const int x = 15544;
    std::cout << 15544;
    for (int i = 0; i < 15544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15545(int& val) {
    func_in_15545(val);
    const int x = 15545;
    std::cout << 15545;
    for (int i = 0; i < 15545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15545(int& val) {
    const int x = 15545;
    std::cout << 15545;
    for (int i = 0; i < 15545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15546(int& val) {
    func_in_15546(val);
    const int x = 15546;
    std::cout << 15546;
    for (int i = 0; i < 15546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15546(int& val) {
    const int x = 15546;
    std::cout << 15546;
    for (int i = 0; i < 15546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15547(int& val) {
    func_in_15547(val);
    const int x = 15547;
    std::cout << 15547;
    for (int i = 0; i < 15547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15547(int& val) {
    const int x = 15547;
    std::cout << 15547;
    for (int i = 0; i < 15547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15548(int& val) {
    func_in_15548(val);
    const int x = 15548;
    std::cout << 15548;
    for (int i = 0; i < 15548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15548(int& val) {
    const int x = 15548;
    std::cout << 15548;
    for (int i = 0; i < 15548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15549(int& val) {
    func_in_15549(val);
    const int x = 15549;
    std::cout << 15549;
    for (int i = 0; i < 15549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15549(int& val) {
    const int x = 15549;
    std::cout << 15549;
    for (int i = 0; i < 15549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15550(int& val) {
    func_in_15550(val);
    const int x = 15550;
    std::cout << 15550;
    for (int i = 0; i < 15550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15550(int& val) {
    const int x = 15550;
    std::cout << 15550;
    for (int i = 0; i < 15550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15551(int& val) {
    func_in_15551(val);
    const int x = 15551;
    std::cout << 15551;
    for (int i = 0; i < 15551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15551(int& val) {
    const int x = 15551;
    std::cout << 15551;
    for (int i = 0; i < 15551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15552(int& val) {
    func_in_15552(val);
    const int x = 15552;
    std::cout << 15552;
    for (int i = 0; i < 15552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15552(int& val) {
    const int x = 15552;
    std::cout << 15552;
    for (int i = 0; i < 15552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15553(int& val) {
    func_in_15553(val);
    const int x = 15553;
    std::cout << 15553;
    for (int i = 0; i < 15553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15553(int& val) {
    const int x = 15553;
    std::cout << 15553;
    for (int i = 0; i < 15553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15554(int& val) {
    func_in_15554(val);
    const int x = 15554;
    std::cout << 15554;
    for (int i = 0; i < 15554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15554(int& val) {
    const int x = 15554;
    std::cout << 15554;
    for (int i = 0; i < 15554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15555(int& val) {
    func_in_15555(val);
    const int x = 15555;
    std::cout << 15555;
    for (int i = 0; i < 15555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15555(int& val) {
    const int x = 15555;
    std::cout << 15555;
    for (int i = 0; i < 15555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15556(int& val) {
    func_in_15556(val);
    const int x = 15556;
    std::cout << 15556;
    for (int i = 0; i < 15556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15556(int& val) {
    const int x = 15556;
    std::cout << 15556;
    for (int i = 0; i < 15556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15557(int& val) {
    func_in_15557(val);
    const int x = 15557;
    std::cout << 15557;
    for (int i = 0; i < 15557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15557(int& val) {
    const int x = 15557;
    std::cout << 15557;
    for (int i = 0; i < 15557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15558(int& val) {
    func_in_15558(val);
    const int x = 15558;
    std::cout << 15558;
    for (int i = 0; i < 15558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15558(int& val) {
    const int x = 15558;
    std::cout << 15558;
    for (int i = 0; i < 15558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15559(int& val) {
    func_in_15559(val);
    const int x = 15559;
    std::cout << 15559;
    for (int i = 0; i < 15559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15559(int& val) {
    const int x = 15559;
    std::cout << 15559;
    for (int i = 0; i < 15559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15560(int& val) {
    func_in_15560(val);
    const int x = 15560;
    std::cout << 15560;
    for (int i = 0; i < 15560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15560(int& val) {
    const int x = 15560;
    std::cout << 15560;
    for (int i = 0; i < 15560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15561(int& val) {
    func_in_15561(val);
    const int x = 15561;
    std::cout << 15561;
    for (int i = 0; i < 15561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15561(int& val) {
    const int x = 15561;
    std::cout << 15561;
    for (int i = 0; i < 15561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15562(int& val) {
    func_in_15562(val);
    const int x = 15562;
    std::cout << 15562;
    for (int i = 0; i < 15562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15562(int& val) {
    const int x = 15562;
    std::cout << 15562;
    for (int i = 0; i < 15562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15563(int& val) {
    func_in_15563(val);
    const int x = 15563;
    std::cout << 15563;
    for (int i = 0; i < 15563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15563(int& val) {
    const int x = 15563;
    std::cout << 15563;
    for (int i = 0; i < 15563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15564(int& val) {
    func_in_15564(val);
    const int x = 15564;
    std::cout << 15564;
    for (int i = 0; i < 15564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15564(int& val) {
    const int x = 15564;
    std::cout << 15564;
    for (int i = 0; i < 15564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15565(int& val) {
    func_in_15565(val);
    const int x = 15565;
    std::cout << 15565;
    for (int i = 0; i < 15565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15565(int& val) {
    const int x = 15565;
    std::cout << 15565;
    for (int i = 0; i < 15565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15566(int& val) {
    func_in_15566(val);
    const int x = 15566;
    std::cout << 15566;
    for (int i = 0; i < 15566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15566(int& val) {
    const int x = 15566;
    std::cout << 15566;
    for (int i = 0; i < 15566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15567(int& val) {
    func_in_15567(val);
    const int x = 15567;
    std::cout << 15567;
    for (int i = 0; i < 15567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15567(int& val) {
    const int x = 15567;
    std::cout << 15567;
    for (int i = 0; i < 15567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15568(int& val) {
    func_in_15568(val);
    const int x = 15568;
    std::cout << 15568;
    for (int i = 0; i < 15568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15568(int& val) {
    const int x = 15568;
    std::cout << 15568;
    for (int i = 0; i < 15568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15569(int& val) {
    func_in_15569(val);
    const int x = 15569;
    std::cout << 15569;
    for (int i = 0; i < 15569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15569(int& val) {
    const int x = 15569;
    std::cout << 15569;
    for (int i = 0; i < 15569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15570(int& val) {
    func_in_15570(val);
    const int x = 15570;
    std::cout << 15570;
    for (int i = 0; i < 15570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15570(int& val) {
    const int x = 15570;
    std::cout << 15570;
    for (int i = 0; i < 15570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15571(int& val) {
    func_in_15571(val);
    const int x = 15571;
    std::cout << 15571;
    for (int i = 0; i < 15571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15571(int& val) {
    const int x = 15571;
    std::cout << 15571;
    for (int i = 0; i < 15571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15572(int& val) {
    func_in_15572(val);
    const int x = 15572;
    std::cout << 15572;
    for (int i = 0; i < 15572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15572(int& val) {
    const int x = 15572;
    std::cout << 15572;
    for (int i = 0; i < 15572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15573(int& val) {
    func_in_15573(val);
    const int x = 15573;
    std::cout << 15573;
    for (int i = 0; i < 15573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15573(int& val) {
    const int x = 15573;
    std::cout << 15573;
    for (int i = 0; i < 15573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15574(int& val) {
    func_in_15574(val);
    const int x = 15574;
    std::cout << 15574;
    for (int i = 0; i < 15574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15574(int& val) {
    const int x = 15574;
    std::cout << 15574;
    for (int i = 0; i < 15574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15575(int& val) {
    func_in_15575(val);
    const int x = 15575;
    std::cout << 15575;
    for (int i = 0; i < 15575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15575(int& val) {
    const int x = 15575;
    std::cout << 15575;
    for (int i = 0; i < 15575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15576(int& val) {
    func_in_15576(val);
    const int x = 15576;
    std::cout << 15576;
    for (int i = 0; i < 15576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15576(int& val) {
    const int x = 15576;
    std::cout << 15576;
    for (int i = 0; i < 15576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15577(int& val) {
    func_in_15577(val);
    const int x = 15577;
    std::cout << 15577;
    for (int i = 0; i < 15577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15577(int& val) {
    const int x = 15577;
    std::cout << 15577;
    for (int i = 0; i < 15577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15578(int& val) {
    func_in_15578(val);
    const int x = 15578;
    std::cout << 15578;
    for (int i = 0; i < 15578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15578(int& val) {
    const int x = 15578;
    std::cout << 15578;
    for (int i = 0; i < 15578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15579(int& val) {
    func_in_15579(val);
    const int x = 15579;
    std::cout << 15579;
    for (int i = 0; i < 15579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15579(int& val) {
    const int x = 15579;
    std::cout << 15579;
    for (int i = 0; i < 15579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15580(int& val) {
    func_in_15580(val);
    const int x = 15580;
    std::cout << 15580;
    for (int i = 0; i < 15580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15580(int& val) {
    const int x = 15580;
    std::cout << 15580;
    for (int i = 0; i < 15580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15581(int& val) {
    func_in_15581(val);
    const int x = 15581;
    std::cout << 15581;
    for (int i = 0; i < 15581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15581(int& val) {
    const int x = 15581;
    std::cout << 15581;
    for (int i = 0; i < 15581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15582(int& val) {
    func_in_15582(val);
    const int x = 15582;
    std::cout << 15582;
    for (int i = 0; i < 15582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15582(int& val) {
    const int x = 15582;
    std::cout << 15582;
    for (int i = 0; i < 15582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15583(int& val) {
    func_in_15583(val);
    const int x = 15583;
    std::cout << 15583;
    for (int i = 0; i < 15583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15583(int& val) {
    const int x = 15583;
    std::cout << 15583;
    for (int i = 0; i < 15583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15584(int& val) {
    func_in_15584(val);
    const int x = 15584;
    std::cout << 15584;
    for (int i = 0; i < 15584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15584(int& val) {
    const int x = 15584;
    std::cout << 15584;
    for (int i = 0; i < 15584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15585(int& val) {
    func_in_15585(val);
    const int x = 15585;
    std::cout << 15585;
    for (int i = 0; i < 15585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15585(int& val) {
    const int x = 15585;
    std::cout << 15585;
    for (int i = 0; i < 15585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15586(int& val) {
    func_in_15586(val);
    const int x = 15586;
    std::cout << 15586;
    for (int i = 0; i < 15586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15586(int& val) {
    const int x = 15586;
    std::cout << 15586;
    for (int i = 0; i < 15586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15587(int& val) {
    func_in_15587(val);
    const int x = 15587;
    std::cout << 15587;
    for (int i = 0; i < 15587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15587(int& val) {
    const int x = 15587;
    std::cout << 15587;
    for (int i = 0; i < 15587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15588(int& val) {
    func_in_15588(val);
    const int x = 15588;
    std::cout << 15588;
    for (int i = 0; i < 15588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15588(int& val) {
    const int x = 15588;
    std::cout << 15588;
    for (int i = 0; i < 15588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15589(int& val) {
    func_in_15589(val);
    const int x = 15589;
    std::cout << 15589;
    for (int i = 0; i < 15589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15589(int& val) {
    const int x = 15589;
    std::cout << 15589;
    for (int i = 0; i < 15589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15590(int& val) {
    func_in_15590(val);
    const int x = 15590;
    std::cout << 15590;
    for (int i = 0; i < 15590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15590(int& val) {
    const int x = 15590;
    std::cout << 15590;
    for (int i = 0; i < 15590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15591(int& val) {
    func_in_15591(val);
    const int x = 15591;
    std::cout << 15591;
    for (int i = 0; i < 15591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15591(int& val) {
    const int x = 15591;
    std::cout << 15591;
    for (int i = 0; i < 15591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15592(int& val) {
    func_in_15592(val);
    const int x = 15592;
    std::cout << 15592;
    for (int i = 0; i < 15592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15592(int& val) {
    const int x = 15592;
    std::cout << 15592;
    for (int i = 0; i < 15592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15593(int& val) {
    func_in_15593(val);
    const int x = 15593;
    std::cout << 15593;
    for (int i = 0; i < 15593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15593(int& val) {
    const int x = 15593;
    std::cout << 15593;
    for (int i = 0; i < 15593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15594(int& val) {
    func_in_15594(val);
    const int x = 15594;
    std::cout << 15594;
    for (int i = 0; i < 15594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15594(int& val) {
    const int x = 15594;
    std::cout << 15594;
    for (int i = 0; i < 15594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15595(int& val) {
    func_in_15595(val);
    const int x = 15595;
    std::cout << 15595;
    for (int i = 0; i < 15595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15595(int& val) {
    const int x = 15595;
    std::cout << 15595;
    for (int i = 0; i < 15595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15596(int& val) {
    func_in_15596(val);
    const int x = 15596;
    std::cout << 15596;
    for (int i = 0; i < 15596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15596(int& val) {
    const int x = 15596;
    std::cout << 15596;
    for (int i = 0; i < 15596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15597(int& val) {
    func_in_15597(val);
    const int x = 15597;
    std::cout << 15597;
    for (int i = 0; i < 15597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15597(int& val) {
    const int x = 15597;
    std::cout << 15597;
    for (int i = 0; i < 15597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15598(int& val) {
    func_in_15598(val);
    const int x = 15598;
    std::cout << 15598;
    for (int i = 0; i < 15598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15598(int& val) {
    const int x = 15598;
    std::cout << 15598;
    for (int i = 0; i < 15598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15599(int& val) {
    func_in_15599(val);
    const int x = 15599;
    std::cout << 15599;
    for (int i = 0; i < 15599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15599(int& val) {
    const int x = 15599;
    std::cout << 15599;
    for (int i = 0; i < 15599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15600(int& val) {
    func_in_15600(val);
    const int x = 15600;
    std::cout << 15600;
    for (int i = 0; i < 15600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15600(int& val) {
    const int x = 15600;
    std::cout << 15600;
    for (int i = 0; i < 15600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15601(int& val) {
    func_in_15601(val);
    const int x = 15601;
    std::cout << 15601;
    for (int i = 0; i < 15601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15601(int& val) {
    const int x = 15601;
    std::cout << 15601;
    for (int i = 0; i < 15601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15602(int& val) {
    func_in_15602(val);
    const int x = 15602;
    std::cout << 15602;
    for (int i = 0; i < 15602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15602(int& val) {
    const int x = 15602;
    std::cout << 15602;
    for (int i = 0; i < 15602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15603(int& val) {
    func_in_15603(val);
    const int x = 15603;
    std::cout << 15603;
    for (int i = 0; i < 15603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15603(int& val) {
    const int x = 15603;
    std::cout << 15603;
    for (int i = 0; i < 15603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15604(int& val) {
    func_in_15604(val);
    const int x = 15604;
    std::cout << 15604;
    for (int i = 0; i < 15604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15604(int& val) {
    const int x = 15604;
    std::cout << 15604;
    for (int i = 0; i < 15604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15605(int& val) {
    func_in_15605(val);
    const int x = 15605;
    std::cout << 15605;
    for (int i = 0; i < 15605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15605(int& val) {
    const int x = 15605;
    std::cout << 15605;
    for (int i = 0; i < 15605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15606(int& val) {
    func_in_15606(val);
    const int x = 15606;
    std::cout << 15606;
    for (int i = 0; i < 15606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15606(int& val) {
    const int x = 15606;
    std::cout << 15606;
    for (int i = 0; i < 15606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15607(int& val) {
    func_in_15607(val);
    const int x = 15607;
    std::cout << 15607;
    for (int i = 0; i < 15607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15607(int& val) {
    const int x = 15607;
    std::cout << 15607;
    for (int i = 0; i < 15607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15608(int& val) {
    func_in_15608(val);
    const int x = 15608;
    std::cout << 15608;
    for (int i = 0; i < 15608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15608(int& val) {
    const int x = 15608;
    std::cout << 15608;
    for (int i = 0; i < 15608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15609(int& val) {
    func_in_15609(val);
    const int x = 15609;
    std::cout << 15609;
    for (int i = 0; i < 15609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15609(int& val) {
    const int x = 15609;
    std::cout << 15609;
    for (int i = 0; i < 15609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15610(int& val) {
    func_in_15610(val);
    const int x = 15610;
    std::cout << 15610;
    for (int i = 0; i < 15610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15610(int& val) {
    const int x = 15610;
    std::cout << 15610;
    for (int i = 0; i < 15610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15611(int& val) {
    func_in_15611(val);
    const int x = 15611;
    std::cout << 15611;
    for (int i = 0; i < 15611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15611(int& val) {
    const int x = 15611;
    std::cout << 15611;
    for (int i = 0; i < 15611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15612(int& val) {
    func_in_15612(val);
    const int x = 15612;
    std::cout << 15612;
    for (int i = 0; i < 15612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15612(int& val) {
    const int x = 15612;
    std::cout << 15612;
    for (int i = 0; i < 15612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15613(int& val) {
    func_in_15613(val);
    const int x = 15613;
    std::cout << 15613;
    for (int i = 0; i < 15613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15613(int& val) {
    const int x = 15613;
    std::cout << 15613;
    for (int i = 0; i < 15613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15614(int& val) {
    func_in_15614(val);
    const int x = 15614;
    std::cout << 15614;
    for (int i = 0; i < 15614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15614(int& val) {
    const int x = 15614;
    std::cout << 15614;
    for (int i = 0; i < 15614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15615(int& val) {
    func_in_15615(val);
    const int x = 15615;
    std::cout << 15615;
    for (int i = 0; i < 15615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15615(int& val) {
    const int x = 15615;
    std::cout << 15615;
    for (int i = 0; i < 15615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15616(int& val) {
    func_in_15616(val);
    const int x = 15616;
    std::cout << 15616;
    for (int i = 0; i < 15616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15616(int& val) {
    const int x = 15616;
    std::cout << 15616;
    for (int i = 0; i < 15616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15617(int& val) {
    func_in_15617(val);
    const int x = 15617;
    std::cout << 15617;
    for (int i = 0; i < 15617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15617(int& val) {
    const int x = 15617;
    std::cout << 15617;
    for (int i = 0; i < 15617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15618(int& val) {
    func_in_15618(val);
    const int x = 15618;
    std::cout << 15618;
    for (int i = 0; i < 15618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15618(int& val) {
    const int x = 15618;
    std::cout << 15618;
    for (int i = 0; i < 15618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15619(int& val) {
    func_in_15619(val);
    const int x = 15619;
    std::cout << 15619;
    for (int i = 0; i < 15619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15619(int& val) {
    const int x = 15619;
    std::cout << 15619;
    for (int i = 0; i < 15619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15620(int& val) {
    func_in_15620(val);
    const int x = 15620;
    std::cout << 15620;
    for (int i = 0; i < 15620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15620(int& val) {
    const int x = 15620;
    std::cout << 15620;
    for (int i = 0; i < 15620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15621(int& val) {
    func_in_15621(val);
    const int x = 15621;
    std::cout << 15621;
    for (int i = 0; i < 15621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15621(int& val) {
    const int x = 15621;
    std::cout << 15621;
    for (int i = 0; i < 15621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15622(int& val) {
    func_in_15622(val);
    const int x = 15622;
    std::cout << 15622;
    for (int i = 0; i < 15622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15622(int& val) {
    const int x = 15622;
    std::cout << 15622;
    for (int i = 0; i < 15622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15623(int& val) {
    func_in_15623(val);
    const int x = 15623;
    std::cout << 15623;
    for (int i = 0; i < 15623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15623(int& val) {
    const int x = 15623;
    std::cout << 15623;
    for (int i = 0; i < 15623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15624(int& val) {
    func_in_15624(val);
    const int x = 15624;
    std::cout << 15624;
    for (int i = 0; i < 15624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15624(int& val) {
    const int x = 15624;
    std::cout << 15624;
    for (int i = 0; i < 15624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15625(int& val) {
    func_in_15625(val);
    const int x = 15625;
    std::cout << 15625;
    for (int i = 0; i < 15625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15625(int& val) {
    const int x = 15625;
    std::cout << 15625;
    for (int i = 0; i < 15625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15626(int& val) {
    func_in_15626(val);
    const int x = 15626;
    std::cout << 15626;
    for (int i = 0; i < 15626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15626(int& val) {
    const int x = 15626;
    std::cout << 15626;
    for (int i = 0; i < 15626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15627(int& val) {
    func_in_15627(val);
    const int x = 15627;
    std::cout << 15627;
    for (int i = 0; i < 15627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15627(int& val) {
    const int x = 15627;
    std::cout << 15627;
    for (int i = 0; i < 15627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15628(int& val) {
    func_in_15628(val);
    const int x = 15628;
    std::cout << 15628;
    for (int i = 0; i < 15628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15628(int& val) {
    const int x = 15628;
    std::cout << 15628;
    for (int i = 0; i < 15628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15629(int& val) {
    func_in_15629(val);
    const int x = 15629;
    std::cout << 15629;
    for (int i = 0; i < 15629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15629(int& val) {
    const int x = 15629;
    std::cout << 15629;
    for (int i = 0; i < 15629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15630(int& val) {
    func_in_15630(val);
    const int x = 15630;
    std::cout << 15630;
    for (int i = 0; i < 15630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15630(int& val) {
    const int x = 15630;
    std::cout << 15630;
    for (int i = 0; i < 15630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15631(int& val) {
    func_in_15631(val);
    const int x = 15631;
    std::cout << 15631;
    for (int i = 0; i < 15631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15631(int& val) {
    const int x = 15631;
    std::cout << 15631;
    for (int i = 0; i < 15631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15632(int& val) {
    func_in_15632(val);
    const int x = 15632;
    std::cout << 15632;
    for (int i = 0; i < 15632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15632(int& val) {
    const int x = 15632;
    std::cout << 15632;
    for (int i = 0; i < 15632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15633(int& val) {
    func_in_15633(val);
    const int x = 15633;
    std::cout << 15633;
    for (int i = 0; i < 15633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15633(int& val) {
    const int x = 15633;
    std::cout << 15633;
    for (int i = 0; i < 15633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15634(int& val) {
    func_in_15634(val);
    const int x = 15634;
    std::cout << 15634;
    for (int i = 0; i < 15634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15634(int& val) {
    const int x = 15634;
    std::cout << 15634;
    for (int i = 0; i < 15634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15635(int& val) {
    func_in_15635(val);
    const int x = 15635;
    std::cout << 15635;
    for (int i = 0; i < 15635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15635(int& val) {
    const int x = 15635;
    std::cout << 15635;
    for (int i = 0; i < 15635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15636(int& val) {
    func_in_15636(val);
    const int x = 15636;
    std::cout << 15636;
    for (int i = 0; i < 15636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15636(int& val) {
    const int x = 15636;
    std::cout << 15636;
    for (int i = 0; i < 15636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15637(int& val) {
    func_in_15637(val);
    const int x = 15637;
    std::cout << 15637;
    for (int i = 0; i < 15637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15637(int& val) {
    const int x = 15637;
    std::cout << 15637;
    for (int i = 0; i < 15637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15638(int& val) {
    func_in_15638(val);
    const int x = 15638;
    std::cout << 15638;
    for (int i = 0; i < 15638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15638(int& val) {
    const int x = 15638;
    std::cout << 15638;
    for (int i = 0; i < 15638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15639(int& val) {
    func_in_15639(val);
    const int x = 15639;
    std::cout << 15639;
    for (int i = 0; i < 15639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15639(int& val) {
    const int x = 15639;
    std::cout << 15639;
    for (int i = 0; i < 15639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15640(int& val) {
    func_in_15640(val);
    const int x = 15640;
    std::cout << 15640;
    for (int i = 0; i < 15640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15640(int& val) {
    const int x = 15640;
    std::cout << 15640;
    for (int i = 0; i < 15640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15641(int& val) {
    func_in_15641(val);
    const int x = 15641;
    std::cout << 15641;
    for (int i = 0; i < 15641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15641(int& val) {
    const int x = 15641;
    std::cout << 15641;
    for (int i = 0; i < 15641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15642(int& val) {
    func_in_15642(val);
    const int x = 15642;
    std::cout << 15642;
    for (int i = 0; i < 15642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15642(int& val) {
    const int x = 15642;
    std::cout << 15642;
    for (int i = 0; i < 15642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15643(int& val) {
    func_in_15643(val);
    const int x = 15643;
    std::cout << 15643;
    for (int i = 0; i < 15643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15643(int& val) {
    const int x = 15643;
    std::cout << 15643;
    for (int i = 0; i < 15643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15644(int& val) {
    func_in_15644(val);
    const int x = 15644;
    std::cout << 15644;
    for (int i = 0; i < 15644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15644(int& val) {
    const int x = 15644;
    std::cout << 15644;
    for (int i = 0; i < 15644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15645(int& val) {
    func_in_15645(val);
    const int x = 15645;
    std::cout << 15645;
    for (int i = 0; i < 15645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15645(int& val) {
    const int x = 15645;
    std::cout << 15645;
    for (int i = 0; i < 15645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15646(int& val) {
    func_in_15646(val);
    const int x = 15646;
    std::cout << 15646;
    for (int i = 0; i < 15646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15646(int& val) {
    const int x = 15646;
    std::cout << 15646;
    for (int i = 0; i < 15646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15647(int& val) {
    func_in_15647(val);
    const int x = 15647;
    std::cout << 15647;
    for (int i = 0; i < 15647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15647(int& val) {
    const int x = 15647;
    std::cout << 15647;
    for (int i = 0; i < 15647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15648(int& val) {
    func_in_15648(val);
    const int x = 15648;
    std::cout << 15648;
    for (int i = 0; i < 15648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15648(int& val) {
    const int x = 15648;
    std::cout << 15648;
    for (int i = 0; i < 15648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15649(int& val) {
    func_in_15649(val);
    const int x = 15649;
    std::cout << 15649;
    for (int i = 0; i < 15649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15649(int& val) {
    const int x = 15649;
    std::cout << 15649;
    for (int i = 0; i < 15649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15650(int& val) {
    func_in_15650(val);
    const int x = 15650;
    std::cout << 15650;
    for (int i = 0; i < 15650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15650(int& val) {
    const int x = 15650;
    std::cout << 15650;
    for (int i = 0; i < 15650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15651(int& val) {
    func_in_15651(val);
    const int x = 15651;
    std::cout << 15651;
    for (int i = 0; i < 15651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15651(int& val) {
    const int x = 15651;
    std::cout << 15651;
    for (int i = 0; i < 15651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15652(int& val) {
    func_in_15652(val);
    const int x = 15652;
    std::cout << 15652;
    for (int i = 0; i < 15652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15652(int& val) {
    const int x = 15652;
    std::cout << 15652;
    for (int i = 0; i < 15652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15653(int& val) {
    func_in_15653(val);
    const int x = 15653;
    std::cout << 15653;
    for (int i = 0; i < 15653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15653(int& val) {
    const int x = 15653;
    std::cout << 15653;
    for (int i = 0; i < 15653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15654(int& val) {
    func_in_15654(val);
    const int x = 15654;
    std::cout << 15654;
    for (int i = 0; i < 15654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15654(int& val) {
    const int x = 15654;
    std::cout << 15654;
    for (int i = 0; i < 15654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15655(int& val) {
    func_in_15655(val);
    const int x = 15655;
    std::cout << 15655;
    for (int i = 0; i < 15655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15655(int& val) {
    const int x = 15655;
    std::cout << 15655;
    for (int i = 0; i < 15655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15656(int& val) {
    func_in_15656(val);
    const int x = 15656;
    std::cout << 15656;
    for (int i = 0; i < 15656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15656(int& val) {
    const int x = 15656;
    std::cout << 15656;
    for (int i = 0; i < 15656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15657(int& val) {
    func_in_15657(val);
    const int x = 15657;
    std::cout << 15657;
    for (int i = 0; i < 15657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15657(int& val) {
    const int x = 15657;
    std::cout << 15657;
    for (int i = 0; i < 15657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15658(int& val) {
    func_in_15658(val);
    const int x = 15658;
    std::cout << 15658;
    for (int i = 0; i < 15658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15658(int& val) {
    const int x = 15658;
    std::cout << 15658;
    for (int i = 0; i < 15658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15659(int& val) {
    func_in_15659(val);
    const int x = 15659;
    std::cout << 15659;
    for (int i = 0; i < 15659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15659(int& val) {
    const int x = 15659;
    std::cout << 15659;
    for (int i = 0; i < 15659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15660(int& val) {
    func_in_15660(val);
    const int x = 15660;
    std::cout << 15660;
    for (int i = 0; i < 15660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15660(int& val) {
    const int x = 15660;
    std::cout << 15660;
    for (int i = 0; i < 15660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15661(int& val) {
    func_in_15661(val);
    const int x = 15661;
    std::cout << 15661;
    for (int i = 0; i < 15661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15661(int& val) {
    const int x = 15661;
    std::cout << 15661;
    for (int i = 0; i < 15661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15662(int& val) {
    func_in_15662(val);
    const int x = 15662;
    std::cout << 15662;
    for (int i = 0; i < 15662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15662(int& val) {
    const int x = 15662;
    std::cout << 15662;
    for (int i = 0; i < 15662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15663(int& val) {
    func_in_15663(val);
    const int x = 15663;
    std::cout << 15663;
    for (int i = 0; i < 15663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15663(int& val) {
    const int x = 15663;
    std::cout << 15663;
    for (int i = 0; i < 15663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15664(int& val) {
    func_in_15664(val);
    const int x = 15664;
    std::cout << 15664;
    for (int i = 0; i < 15664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15664(int& val) {
    const int x = 15664;
    std::cout << 15664;
    for (int i = 0; i < 15664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15665(int& val) {
    func_in_15665(val);
    const int x = 15665;
    std::cout << 15665;
    for (int i = 0; i < 15665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15665(int& val) {
    const int x = 15665;
    std::cout << 15665;
    for (int i = 0; i < 15665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15666(int& val) {
    func_in_15666(val);
    const int x = 15666;
    std::cout << 15666;
    for (int i = 0; i < 15666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15666(int& val) {
    const int x = 15666;
    std::cout << 15666;
    for (int i = 0; i < 15666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15667(int& val) {
    func_in_15667(val);
    const int x = 15667;
    std::cout << 15667;
    for (int i = 0; i < 15667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15667(int& val) {
    const int x = 15667;
    std::cout << 15667;
    for (int i = 0; i < 15667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15668(int& val) {
    func_in_15668(val);
    const int x = 15668;
    std::cout << 15668;
    for (int i = 0; i < 15668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15668(int& val) {
    const int x = 15668;
    std::cout << 15668;
    for (int i = 0; i < 15668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15669(int& val) {
    func_in_15669(val);
    const int x = 15669;
    std::cout << 15669;
    for (int i = 0; i < 15669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15669(int& val) {
    const int x = 15669;
    std::cout << 15669;
    for (int i = 0; i < 15669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15670(int& val) {
    func_in_15670(val);
    const int x = 15670;
    std::cout << 15670;
    for (int i = 0; i < 15670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15670(int& val) {
    const int x = 15670;
    std::cout << 15670;
    for (int i = 0; i < 15670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15671(int& val) {
    func_in_15671(val);
    const int x = 15671;
    std::cout << 15671;
    for (int i = 0; i < 15671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15671(int& val) {
    const int x = 15671;
    std::cout << 15671;
    for (int i = 0; i < 15671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15672(int& val) {
    func_in_15672(val);
    const int x = 15672;
    std::cout << 15672;
    for (int i = 0; i < 15672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15672(int& val) {
    const int x = 15672;
    std::cout << 15672;
    for (int i = 0; i < 15672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15673(int& val) {
    func_in_15673(val);
    const int x = 15673;
    std::cout << 15673;
    for (int i = 0; i < 15673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15673(int& val) {
    const int x = 15673;
    std::cout << 15673;
    for (int i = 0; i < 15673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15674(int& val) {
    func_in_15674(val);
    const int x = 15674;
    std::cout << 15674;
    for (int i = 0; i < 15674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15674(int& val) {
    const int x = 15674;
    std::cout << 15674;
    for (int i = 0; i < 15674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15675(int& val) {
    func_in_15675(val);
    const int x = 15675;
    std::cout << 15675;
    for (int i = 0; i < 15675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15675(int& val) {
    const int x = 15675;
    std::cout << 15675;
    for (int i = 0; i < 15675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15676(int& val) {
    func_in_15676(val);
    const int x = 15676;
    std::cout << 15676;
    for (int i = 0; i < 15676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15676(int& val) {
    const int x = 15676;
    std::cout << 15676;
    for (int i = 0; i < 15676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15677(int& val) {
    func_in_15677(val);
    const int x = 15677;
    std::cout << 15677;
    for (int i = 0; i < 15677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15677(int& val) {
    const int x = 15677;
    std::cout << 15677;
    for (int i = 0; i < 15677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15678(int& val) {
    func_in_15678(val);
    const int x = 15678;
    std::cout << 15678;
    for (int i = 0; i < 15678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15678(int& val) {
    const int x = 15678;
    std::cout << 15678;
    for (int i = 0; i < 15678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15679(int& val) {
    func_in_15679(val);
    const int x = 15679;
    std::cout << 15679;
    for (int i = 0; i < 15679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15679(int& val) {
    const int x = 15679;
    std::cout << 15679;
    for (int i = 0; i < 15679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15680(int& val) {
    func_in_15680(val);
    const int x = 15680;
    std::cout << 15680;
    for (int i = 0; i < 15680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15680(int& val) {
    const int x = 15680;
    std::cout << 15680;
    for (int i = 0; i < 15680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15681(int& val) {
    func_in_15681(val);
    const int x = 15681;
    std::cout << 15681;
    for (int i = 0; i < 15681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15681(int& val) {
    const int x = 15681;
    std::cout << 15681;
    for (int i = 0; i < 15681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15682(int& val) {
    func_in_15682(val);
    const int x = 15682;
    std::cout << 15682;
    for (int i = 0; i < 15682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15682(int& val) {
    const int x = 15682;
    std::cout << 15682;
    for (int i = 0; i < 15682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15683(int& val) {
    func_in_15683(val);
    const int x = 15683;
    std::cout << 15683;
    for (int i = 0; i < 15683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15683(int& val) {
    const int x = 15683;
    std::cout << 15683;
    for (int i = 0; i < 15683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15684(int& val) {
    func_in_15684(val);
    const int x = 15684;
    std::cout << 15684;
    for (int i = 0; i < 15684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15684(int& val) {
    const int x = 15684;
    std::cout << 15684;
    for (int i = 0; i < 15684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15685(int& val) {
    func_in_15685(val);
    const int x = 15685;
    std::cout << 15685;
    for (int i = 0; i < 15685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15685(int& val) {
    const int x = 15685;
    std::cout << 15685;
    for (int i = 0; i < 15685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15686(int& val) {
    func_in_15686(val);
    const int x = 15686;
    std::cout << 15686;
    for (int i = 0; i < 15686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15686(int& val) {
    const int x = 15686;
    std::cout << 15686;
    for (int i = 0; i < 15686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15687(int& val) {
    func_in_15687(val);
    const int x = 15687;
    std::cout << 15687;
    for (int i = 0; i < 15687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15687(int& val) {
    const int x = 15687;
    std::cout << 15687;
    for (int i = 0; i < 15687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15688(int& val) {
    func_in_15688(val);
    const int x = 15688;
    std::cout << 15688;
    for (int i = 0; i < 15688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15688(int& val) {
    const int x = 15688;
    std::cout << 15688;
    for (int i = 0; i < 15688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15689(int& val) {
    func_in_15689(val);
    const int x = 15689;
    std::cout << 15689;
    for (int i = 0; i < 15689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15689(int& val) {
    const int x = 15689;
    std::cout << 15689;
    for (int i = 0; i < 15689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15690(int& val) {
    func_in_15690(val);
    const int x = 15690;
    std::cout << 15690;
    for (int i = 0; i < 15690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15690(int& val) {
    const int x = 15690;
    std::cout << 15690;
    for (int i = 0; i < 15690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15691(int& val) {
    func_in_15691(val);
    const int x = 15691;
    std::cout << 15691;
    for (int i = 0; i < 15691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15691(int& val) {
    const int x = 15691;
    std::cout << 15691;
    for (int i = 0; i < 15691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15692(int& val) {
    func_in_15692(val);
    const int x = 15692;
    std::cout << 15692;
    for (int i = 0; i < 15692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15692(int& val) {
    const int x = 15692;
    std::cout << 15692;
    for (int i = 0; i < 15692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15693(int& val) {
    func_in_15693(val);
    const int x = 15693;
    std::cout << 15693;
    for (int i = 0; i < 15693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15693(int& val) {
    const int x = 15693;
    std::cout << 15693;
    for (int i = 0; i < 15693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15694(int& val) {
    func_in_15694(val);
    const int x = 15694;
    std::cout << 15694;
    for (int i = 0; i < 15694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15694(int& val) {
    const int x = 15694;
    std::cout << 15694;
    for (int i = 0; i < 15694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15695(int& val) {
    func_in_15695(val);
    const int x = 15695;
    std::cout << 15695;
    for (int i = 0; i < 15695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15695(int& val) {
    const int x = 15695;
    std::cout << 15695;
    for (int i = 0; i < 15695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15696(int& val) {
    func_in_15696(val);
    const int x = 15696;
    std::cout << 15696;
    for (int i = 0; i < 15696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15696(int& val) {
    const int x = 15696;
    std::cout << 15696;
    for (int i = 0; i < 15696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15697(int& val) {
    func_in_15697(val);
    const int x = 15697;
    std::cout << 15697;
    for (int i = 0; i < 15697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15697(int& val) {
    const int x = 15697;
    std::cout << 15697;
    for (int i = 0; i < 15697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15698(int& val) {
    func_in_15698(val);
    const int x = 15698;
    std::cout << 15698;
    for (int i = 0; i < 15698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15698(int& val) {
    const int x = 15698;
    std::cout << 15698;
    for (int i = 0; i < 15698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15699(int& val) {
    func_in_15699(val);
    const int x = 15699;
    std::cout << 15699;
    for (int i = 0; i < 15699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15699(int& val) {
    const int x = 15699;
    std::cout << 15699;
    for (int i = 0; i < 15699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15700(int& val) {
    func_in_15700(val);
    const int x = 15700;
    std::cout << 15700;
    for (int i = 0; i < 15700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15700(int& val) {
    const int x = 15700;
    std::cout << 15700;
    for (int i = 0; i < 15700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15701(int& val) {
    func_in_15701(val);
    const int x = 15701;
    std::cout << 15701;
    for (int i = 0; i < 15701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15701(int& val) {
    const int x = 15701;
    std::cout << 15701;
    for (int i = 0; i < 15701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15702(int& val) {
    func_in_15702(val);
    const int x = 15702;
    std::cout << 15702;
    for (int i = 0; i < 15702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15702(int& val) {
    const int x = 15702;
    std::cout << 15702;
    for (int i = 0; i < 15702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15703(int& val) {
    func_in_15703(val);
    const int x = 15703;
    std::cout << 15703;
    for (int i = 0; i < 15703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15703(int& val) {
    const int x = 15703;
    std::cout << 15703;
    for (int i = 0; i < 15703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15704(int& val) {
    func_in_15704(val);
    const int x = 15704;
    std::cout << 15704;
    for (int i = 0; i < 15704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15704(int& val) {
    const int x = 15704;
    std::cout << 15704;
    for (int i = 0; i < 15704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15705(int& val) {
    func_in_15705(val);
    const int x = 15705;
    std::cout << 15705;
    for (int i = 0; i < 15705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15705(int& val) {
    const int x = 15705;
    std::cout << 15705;
    for (int i = 0; i < 15705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15706(int& val) {
    func_in_15706(val);
    const int x = 15706;
    std::cout << 15706;
    for (int i = 0; i < 15706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15706(int& val) {
    const int x = 15706;
    std::cout << 15706;
    for (int i = 0; i < 15706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15707(int& val) {
    func_in_15707(val);
    const int x = 15707;
    std::cout << 15707;
    for (int i = 0; i < 15707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15707(int& val) {
    const int x = 15707;
    std::cout << 15707;
    for (int i = 0; i < 15707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15708(int& val) {
    func_in_15708(val);
    const int x = 15708;
    std::cout << 15708;
    for (int i = 0; i < 15708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15708(int& val) {
    const int x = 15708;
    std::cout << 15708;
    for (int i = 0; i < 15708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15709(int& val) {
    func_in_15709(val);
    const int x = 15709;
    std::cout << 15709;
    for (int i = 0; i < 15709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15709(int& val) {
    const int x = 15709;
    std::cout << 15709;
    for (int i = 0; i < 15709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15710(int& val) {
    func_in_15710(val);
    const int x = 15710;
    std::cout << 15710;
    for (int i = 0; i < 15710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15710(int& val) {
    const int x = 15710;
    std::cout << 15710;
    for (int i = 0; i < 15710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15711(int& val) {
    func_in_15711(val);
    const int x = 15711;
    std::cout << 15711;
    for (int i = 0; i < 15711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15711(int& val) {
    const int x = 15711;
    std::cout << 15711;
    for (int i = 0; i < 15711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15712(int& val) {
    func_in_15712(val);
    const int x = 15712;
    std::cout << 15712;
    for (int i = 0; i < 15712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15712(int& val) {
    const int x = 15712;
    std::cout << 15712;
    for (int i = 0; i < 15712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15713(int& val) {
    func_in_15713(val);
    const int x = 15713;
    std::cout << 15713;
    for (int i = 0; i < 15713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15713(int& val) {
    const int x = 15713;
    std::cout << 15713;
    for (int i = 0; i < 15713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15714(int& val) {
    func_in_15714(val);
    const int x = 15714;
    std::cout << 15714;
    for (int i = 0; i < 15714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15714(int& val) {
    const int x = 15714;
    std::cout << 15714;
    for (int i = 0; i < 15714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15715(int& val) {
    func_in_15715(val);
    const int x = 15715;
    std::cout << 15715;
    for (int i = 0; i < 15715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15715(int& val) {
    const int x = 15715;
    std::cout << 15715;
    for (int i = 0; i < 15715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15716(int& val) {
    func_in_15716(val);
    const int x = 15716;
    std::cout << 15716;
    for (int i = 0; i < 15716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15716(int& val) {
    const int x = 15716;
    std::cout << 15716;
    for (int i = 0; i < 15716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15717(int& val) {
    func_in_15717(val);
    const int x = 15717;
    std::cout << 15717;
    for (int i = 0; i < 15717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15717(int& val) {
    const int x = 15717;
    std::cout << 15717;
    for (int i = 0; i < 15717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15718(int& val) {
    func_in_15718(val);
    const int x = 15718;
    std::cout << 15718;
    for (int i = 0; i < 15718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15718(int& val) {
    const int x = 15718;
    std::cout << 15718;
    for (int i = 0; i < 15718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15719(int& val) {
    func_in_15719(val);
    const int x = 15719;
    std::cout << 15719;
    for (int i = 0; i < 15719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15719(int& val) {
    const int x = 15719;
    std::cout << 15719;
    for (int i = 0; i < 15719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15720(int& val) {
    func_in_15720(val);
    const int x = 15720;
    std::cout << 15720;
    for (int i = 0; i < 15720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15720(int& val) {
    const int x = 15720;
    std::cout << 15720;
    for (int i = 0; i < 15720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15721(int& val) {
    func_in_15721(val);
    const int x = 15721;
    std::cout << 15721;
    for (int i = 0; i < 15721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15721(int& val) {
    const int x = 15721;
    std::cout << 15721;
    for (int i = 0; i < 15721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15722(int& val) {
    func_in_15722(val);
    const int x = 15722;
    std::cout << 15722;
    for (int i = 0; i < 15722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15722(int& val) {
    const int x = 15722;
    std::cout << 15722;
    for (int i = 0; i < 15722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15723(int& val) {
    func_in_15723(val);
    const int x = 15723;
    std::cout << 15723;
    for (int i = 0; i < 15723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15723(int& val) {
    const int x = 15723;
    std::cout << 15723;
    for (int i = 0; i < 15723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15724(int& val) {
    func_in_15724(val);
    const int x = 15724;
    std::cout << 15724;
    for (int i = 0; i < 15724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15724(int& val) {
    const int x = 15724;
    std::cout << 15724;
    for (int i = 0; i < 15724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15725(int& val) {
    func_in_15725(val);
    const int x = 15725;
    std::cout << 15725;
    for (int i = 0; i < 15725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15725(int& val) {
    const int x = 15725;
    std::cout << 15725;
    for (int i = 0; i < 15725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15726(int& val) {
    func_in_15726(val);
    const int x = 15726;
    std::cout << 15726;
    for (int i = 0; i < 15726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15726(int& val) {
    const int x = 15726;
    std::cout << 15726;
    for (int i = 0; i < 15726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15727(int& val) {
    func_in_15727(val);
    const int x = 15727;
    std::cout << 15727;
    for (int i = 0; i < 15727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15727(int& val) {
    const int x = 15727;
    std::cout << 15727;
    for (int i = 0; i < 15727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15728(int& val) {
    func_in_15728(val);
    const int x = 15728;
    std::cout << 15728;
    for (int i = 0; i < 15728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15728(int& val) {
    const int x = 15728;
    std::cout << 15728;
    for (int i = 0; i < 15728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15729(int& val) {
    func_in_15729(val);
    const int x = 15729;
    std::cout << 15729;
    for (int i = 0; i < 15729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15729(int& val) {
    const int x = 15729;
    std::cout << 15729;
    for (int i = 0; i < 15729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15730(int& val) {
    func_in_15730(val);
    const int x = 15730;
    std::cout << 15730;
    for (int i = 0; i < 15730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15730(int& val) {
    const int x = 15730;
    std::cout << 15730;
    for (int i = 0; i < 15730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15731(int& val) {
    func_in_15731(val);
    const int x = 15731;
    std::cout << 15731;
    for (int i = 0; i < 15731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15731(int& val) {
    const int x = 15731;
    std::cout << 15731;
    for (int i = 0; i < 15731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15732(int& val) {
    func_in_15732(val);
    const int x = 15732;
    std::cout << 15732;
    for (int i = 0; i < 15732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15732(int& val) {
    const int x = 15732;
    std::cout << 15732;
    for (int i = 0; i < 15732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15733(int& val) {
    func_in_15733(val);
    const int x = 15733;
    std::cout << 15733;
    for (int i = 0; i < 15733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15733(int& val) {
    const int x = 15733;
    std::cout << 15733;
    for (int i = 0; i < 15733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15734(int& val) {
    func_in_15734(val);
    const int x = 15734;
    std::cout << 15734;
    for (int i = 0; i < 15734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15734(int& val) {
    const int x = 15734;
    std::cout << 15734;
    for (int i = 0; i < 15734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15735(int& val) {
    func_in_15735(val);
    const int x = 15735;
    std::cout << 15735;
    for (int i = 0; i < 15735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15735(int& val) {
    const int x = 15735;
    std::cout << 15735;
    for (int i = 0; i < 15735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15736(int& val) {
    func_in_15736(val);
    const int x = 15736;
    std::cout << 15736;
    for (int i = 0; i < 15736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15736(int& val) {
    const int x = 15736;
    std::cout << 15736;
    for (int i = 0; i < 15736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15737(int& val) {
    func_in_15737(val);
    const int x = 15737;
    std::cout << 15737;
    for (int i = 0; i < 15737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15737(int& val) {
    const int x = 15737;
    std::cout << 15737;
    for (int i = 0; i < 15737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15738(int& val) {
    func_in_15738(val);
    const int x = 15738;
    std::cout << 15738;
    for (int i = 0; i < 15738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15738(int& val) {
    const int x = 15738;
    std::cout << 15738;
    for (int i = 0; i < 15738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15739(int& val) {
    func_in_15739(val);
    const int x = 15739;
    std::cout << 15739;
    for (int i = 0; i < 15739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15739(int& val) {
    const int x = 15739;
    std::cout << 15739;
    for (int i = 0; i < 15739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15740(int& val) {
    func_in_15740(val);
    const int x = 15740;
    std::cout << 15740;
    for (int i = 0; i < 15740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15740(int& val) {
    const int x = 15740;
    std::cout << 15740;
    for (int i = 0; i < 15740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15741(int& val) {
    func_in_15741(val);
    const int x = 15741;
    std::cout << 15741;
    for (int i = 0; i < 15741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15741(int& val) {
    const int x = 15741;
    std::cout << 15741;
    for (int i = 0; i < 15741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15742(int& val) {
    func_in_15742(val);
    const int x = 15742;
    std::cout << 15742;
    for (int i = 0; i < 15742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15742(int& val) {
    const int x = 15742;
    std::cout << 15742;
    for (int i = 0; i < 15742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15743(int& val) {
    func_in_15743(val);
    const int x = 15743;
    std::cout << 15743;
    for (int i = 0; i < 15743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15743(int& val) {
    const int x = 15743;
    std::cout << 15743;
    for (int i = 0; i < 15743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15744(int& val) {
    func_in_15744(val);
    const int x = 15744;
    std::cout << 15744;
    for (int i = 0; i < 15744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15744(int& val) {
    const int x = 15744;
    std::cout << 15744;
    for (int i = 0; i < 15744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15745(int& val) {
    func_in_15745(val);
    const int x = 15745;
    std::cout << 15745;
    for (int i = 0; i < 15745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15745(int& val) {
    const int x = 15745;
    std::cout << 15745;
    for (int i = 0; i < 15745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15746(int& val) {
    func_in_15746(val);
    const int x = 15746;
    std::cout << 15746;
    for (int i = 0; i < 15746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15746(int& val) {
    const int x = 15746;
    std::cout << 15746;
    for (int i = 0; i < 15746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15747(int& val) {
    func_in_15747(val);
    const int x = 15747;
    std::cout << 15747;
    for (int i = 0; i < 15747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15747(int& val) {
    const int x = 15747;
    std::cout << 15747;
    for (int i = 0; i < 15747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15748(int& val) {
    func_in_15748(val);
    const int x = 15748;
    std::cout << 15748;
    for (int i = 0; i < 15748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15748(int& val) {
    const int x = 15748;
    std::cout << 15748;
    for (int i = 0; i < 15748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15749(int& val) {
    func_in_15749(val);
    const int x = 15749;
    std::cout << 15749;
    for (int i = 0; i < 15749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15749(int& val) {
    const int x = 15749;
    std::cout << 15749;
    for (int i = 0; i < 15749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15750(int& val) {
    func_in_15750(val);
    const int x = 15750;
    std::cout << 15750;
    for (int i = 0; i < 15750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15750(int& val) {
    const int x = 15750;
    std::cout << 15750;
    for (int i = 0; i < 15750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15751(int& val) {
    func_in_15751(val);
    const int x = 15751;
    std::cout << 15751;
    for (int i = 0; i < 15751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15751(int& val) {
    const int x = 15751;
    std::cout << 15751;
    for (int i = 0; i < 15751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15752(int& val) {
    func_in_15752(val);
    const int x = 15752;
    std::cout << 15752;
    for (int i = 0; i < 15752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15752(int& val) {
    const int x = 15752;
    std::cout << 15752;
    for (int i = 0; i < 15752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15753(int& val) {
    func_in_15753(val);
    const int x = 15753;
    std::cout << 15753;
    for (int i = 0; i < 15753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15753(int& val) {
    const int x = 15753;
    std::cout << 15753;
    for (int i = 0; i < 15753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15754(int& val) {
    func_in_15754(val);
    const int x = 15754;
    std::cout << 15754;
    for (int i = 0; i < 15754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15754(int& val) {
    const int x = 15754;
    std::cout << 15754;
    for (int i = 0; i < 15754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15755(int& val) {
    func_in_15755(val);
    const int x = 15755;
    std::cout << 15755;
    for (int i = 0; i < 15755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15755(int& val) {
    const int x = 15755;
    std::cout << 15755;
    for (int i = 0; i < 15755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15756(int& val) {
    func_in_15756(val);
    const int x = 15756;
    std::cout << 15756;
    for (int i = 0; i < 15756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15756(int& val) {
    const int x = 15756;
    std::cout << 15756;
    for (int i = 0; i < 15756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15757(int& val) {
    func_in_15757(val);
    const int x = 15757;
    std::cout << 15757;
    for (int i = 0; i < 15757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15757(int& val) {
    const int x = 15757;
    std::cout << 15757;
    for (int i = 0; i < 15757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15758(int& val) {
    func_in_15758(val);
    const int x = 15758;
    std::cout << 15758;
    for (int i = 0; i < 15758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15758(int& val) {
    const int x = 15758;
    std::cout << 15758;
    for (int i = 0; i < 15758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15759(int& val) {
    func_in_15759(val);
    const int x = 15759;
    std::cout << 15759;
    for (int i = 0; i < 15759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15759(int& val) {
    const int x = 15759;
    std::cout << 15759;
    for (int i = 0; i < 15759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15760(int& val) {
    func_in_15760(val);
    const int x = 15760;
    std::cout << 15760;
    for (int i = 0; i < 15760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15760(int& val) {
    const int x = 15760;
    std::cout << 15760;
    for (int i = 0; i < 15760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15761(int& val) {
    func_in_15761(val);
    const int x = 15761;
    std::cout << 15761;
    for (int i = 0; i < 15761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15761(int& val) {
    const int x = 15761;
    std::cout << 15761;
    for (int i = 0; i < 15761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15762(int& val) {
    func_in_15762(val);
    const int x = 15762;
    std::cout << 15762;
    for (int i = 0; i < 15762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15762(int& val) {
    const int x = 15762;
    std::cout << 15762;
    for (int i = 0; i < 15762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15763(int& val) {
    func_in_15763(val);
    const int x = 15763;
    std::cout << 15763;
    for (int i = 0; i < 15763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15763(int& val) {
    const int x = 15763;
    std::cout << 15763;
    for (int i = 0; i < 15763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15764(int& val) {
    func_in_15764(val);
    const int x = 15764;
    std::cout << 15764;
    for (int i = 0; i < 15764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15764(int& val) {
    const int x = 15764;
    std::cout << 15764;
    for (int i = 0; i < 15764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15765(int& val) {
    func_in_15765(val);
    const int x = 15765;
    std::cout << 15765;
    for (int i = 0; i < 15765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15765(int& val) {
    const int x = 15765;
    std::cout << 15765;
    for (int i = 0; i < 15765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15766(int& val) {
    func_in_15766(val);
    const int x = 15766;
    std::cout << 15766;
    for (int i = 0; i < 15766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15766(int& val) {
    const int x = 15766;
    std::cout << 15766;
    for (int i = 0; i < 15766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15767(int& val) {
    func_in_15767(val);
    const int x = 15767;
    std::cout << 15767;
    for (int i = 0; i < 15767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15767(int& val) {
    const int x = 15767;
    std::cout << 15767;
    for (int i = 0; i < 15767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15768(int& val) {
    func_in_15768(val);
    const int x = 15768;
    std::cout << 15768;
    for (int i = 0; i < 15768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15768(int& val) {
    const int x = 15768;
    std::cout << 15768;
    for (int i = 0; i < 15768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15769(int& val) {
    func_in_15769(val);
    const int x = 15769;
    std::cout << 15769;
    for (int i = 0; i < 15769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15769(int& val) {
    const int x = 15769;
    std::cout << 15769;
    for (int i = 0; i < 15769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15770(int& val) {
    func_in_15770(val);
    const int x = 15770;
    std::cout << 15770;
    for (int i = 0; i < 15770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15770(int& val) {
    const int x = 15770;
    std::cout << 15770;
    for (int i = 0; i < 15770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15771(int& val) {
    func_in_15771(val);
    const int x = 15771;
    std::cout << 15771;
    for (int i = 0; i < 15771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15771(int& val) {
    const int x = 15771;
    std::cout << 15771;
    for (int i = 0; i < 15771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15772(int& val) {
    func_in_15772(val);
    const int x = 15772;
    std::cout << 15772;
    for (int i = 0; i < 15772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15772(int& val) {
    const int x = 15772;
    std::cout << 15772;
    for (int i = 0; i < 15772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15773(int& val) {
    func_in_15773(val);
    const int x = 15773;
    std::cout << 15773;
    for (int i = 0; i < 15773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15773(int& val) {
    const int x = 15773;
    std::cout << 15773;
    for (int i = 0; i < 15773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15774(int& val) {
    func_in_15774(val);
    const int x = 15774;
    std::cout << 15774;
    for (int i = 0; i < 15774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15774(int& val) {
    const int x = 15774;
    std::cout << 15774;
    for (int i = 0; i < 15774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15775(int& val) {
    func_in_15775(val);
    const int x = 15775;
    std::cout << 15775;
    for (int i = 0; i < 15775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15775(int& val) {
    const int x = 15775;
    std::cout << 15775;
    for (int i = 0; i < 15775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15776(int& val) {
    func_in_15776(val);
    const int x = 15776;
    std::cout << 15776;
    for (int i = 0; i < 15776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15776(int& val) {
    const int x = 15776;
    std::cout << 15776;
    for (int i = 0; i < 15776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15777(int& val) {
    func_in_15777(val);
    const int x = 15777;
    std::cout << 15777;
    for (int i = 0; i < 15777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15777(int& val) {
    const int x = 15777;
    std::cout << 15777;
    for (int i = 0; i < 15777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15778(int& val) {
    func_in_15778(val);
    const int x = 15778;
    std::cout << 15778;
    for (int i = 0; i < 15778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15778(int& val) {
    const int x = 15778;
    std::cout << 15778;
    for (int i = 0; i < 15778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15779(int& val) {
    func_in_15779(val);
    const int x = 15779;
    std::cout << 15779;
    for (int i = 0; i < 15779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15779(int& val) {
    const int x = 15779;
    std::cout << 15779;
    for (int i = 0; i < 15779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15780(int& val) {
    func_in_15780(val);
    const int x = 15780;
    std::cout << 15780;
    for (int i = 0; i < 15780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15780(int& val) {
    const int x = 15780;
    std::cout << 15780;
    for (int i = 0; i < 15780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15781(int& val) {
    func_in_15781(val);
    const int x = 15781;
    std::cout << 15781;
    for (int i = 0; i < 15781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15781(int& val) {
    const int x = 15781;
    std::cout << 15781;
    for (int i = 0; i < 15781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15782(int& val) {
    func_in_15782(val);
    const int x = 15782;
    std::cout << 15782;
    for (int i = 0; i < 15782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15782(int& val) {
    const int x = 15782;
    std::cout << 15782;
    for (int i = 0; i < 15782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15783(int& val) {
    func_in_15783(val);
    const int x = 15783;
    std::cout << 15783;
    for (int i = 0; i < 15783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15783(int& val) {
    const int x = 15783;
    std::cout << 15783;
    for (int i = 0; i < 15783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15784(int& val) {
    func_in_15784(val);
    const int x = 15784;
    std::cout << 15784;
    for (int i = 0; i < 15784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15784(int& val) {
    const int x = 15784;
    std::cout << 15784;
    for (int i = 0; i < 15784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15785(int& val) {
    func_in_15785(val);
    const int x = 15785;
    std::cout << 15785;
    for (int i = 0; i < 15785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15785(int& val) {
    const int x = 15785;
    std::cout << 15785;
    for (int i = 0; i < 15785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15786(int& val) {
    func_in_15786(val);
    const int x = 15786;
    std::cout << 15786;
    for (int i = 0; i < 15786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15786(int& val) {
    const int x = 15786;
    std::cout << 15786;
    for (int i = 0; i < 15786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15787(int& val) {
    func_in_15787(val);
    const int x = 15787;
    std::cout << 15787;
    for (int i = 0; i < 15787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15787(int& val) {
    const int x = 15787;
    std::cout << 15787;
    for (int i = 0; i < 15787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15788(int& val) {
    func_in_15788(val);
    const int x = 15788;
    std::cout << 15788;
    for (int i = 0; i < 15788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15788(int& val) {
    const int x = 15788;
    std::cout << 15788;
    for (int i = 0; i < 15788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15789(int& val) {
    func_in_15789(val);
    const int x = 15789;
    std::cout << 15789;
    for (int i = 0; i < 15789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15789(int& val) {
    const int x = 15789;
    std::cout << 15789;
    for (int i = 0; i < 15789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15790(int& val) {
    func_in_15790(val);
    const int x = 15790;
    std::cout << 15790;
    for (int i = 0; i < 15790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15790(int& val) {
    const int x = 15790;
    std::cout << 15790;
    for (int i = 0; i < 15790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15791(int& val) {
    func_in_15791(val);
    const int x = 15791;
    std::cout << 15791;
    for (int i = 0; i < 15791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15791(int& val) {
    const int x = 15791;
    std::cout << 15791;
    for (int i = 0; i < 15791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15792(int& val) {
    func_in_15792(val);
    const int x = 15792;
    std::cout << 15792;
    for (int i = 0; i < 15792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15792(int& val) {
    const int x = 15792;
    std::cout << 15792;
    for (int i = 0; i < 15792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15793(int& val) {
    func_in_15793(val);
    const int x = 15793;
    std::cout << 15793;
    for (int i = 0; i < 15793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15793(int& val) {
    const int x = 15793;
    std::cout << 15793;
    for (int i = 0; i < 15793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15794(int& val) {
    func_in_15794(val);
    const int x = 15794;
    std::cout << 15794;
    for (int i = 0; i < 15794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15794(int& val) {
    const int x = 15794;
    std::cout << 15794;
    for (int i = 0; i < 15794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15795(int& val) {
    func_in_15795(val);
    const int x = 15795;
    std::cout << 15795;
    for (int i = 0; i < 15795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15795(int& val) {
    const int x = 15795;
    std::cout << 15795;
    for (int i = 0; i < 15795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15796(int& val) {
    func_in_15796(val);
    const int x = 15796;
    std::cout << 15796;
    for (int i = 0; i < 15796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15796(int& val) {
    const int x = 15796;
    std::cout << 15796;
    for (int i = 0; i < 15796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15797(int& val) {
    func_in_15797(val);
    const int x = 15797;
    std::cout << 15797;
    for (int i = 0; i < 15797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15797(int& val) {
    const int x = 15797;
    std::cout << 15797;
    for (int i = 0; i < 15797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15798(int& val) {
    func_in_15798(val);
    const int x = 15798;
    std::cout << 15798;
    for (int i = 0; i < 15798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15798(int& val) {
    const int x = 15798;
    std::cout << 15798;
    for (int i = 0; i < 15798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15799(int& val) {
    func_in_15799(val);
    const int x = 15799;
    std::cout << 15799;
    for (int i = 0; i < 15799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15799(int& val) {
    const int x = 15799;
    std::cout << 15799;
    for (int i = 0; i < 15799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15800(int& val) {
    func_in_15800(val);
    const int x = 15800;
    std::cout << 15800;
    for (int i = 0; i < 15800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15800(int& val) {
    const int x = 15800;
    std::cout << 15800;
    for (int i = 0; i < 15800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15801(int& val) {
    func_in_15801(val);
    const int x = 15801;
    std::cout << 15801;
    for (int i = 0; i < 15801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15801(int& val) {
    const int x = 15801;
    std::cout << 15801;
    for (int i = 0; i < 15801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15802(int& val) {
    func_in_15802(val);
    const int x = 15802;
    std::cout << 15802;
    for (int i = 0; i < 15802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15802(int& val) {
    const int x = 15802;
    std::cout << 15802;
    for (int i = 0; i < 15802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15803(int& val) {
    func_in_15803(val);
    const int x = 15803;
    std::cout << 15803;
    for (int i = 0; i < 15803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15803(int& val) {
    const int x = 15803;
    std::cout << 15803;
    for (int i = 0; i < 15803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15804(int& val) {
    func_in_15804(val);
    const int x = 15804;
    std::cout << 15804;
    for (int i = 0; i < 15804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15804(int& val) {
    const int x = 15804;
    std::cout << 15804;
    for (int i = 0; i < 15804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15805(int& val) {
    func_in_15805(val);
    const int x = 15805;
    std::cout << 15805;
    for (int i = 0; i < 15805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15805(int& val) {
    const int x = 15805;
    std::cout << 15805;
    for (int i = 0; i < 15805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15806(int& val) {
    func_in_15806(val);
    const int x = 15806;
    std::cout << 15806;
    for (int i = 0; i < 15806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15806(int& val) {
    const int x = 15806;
    std::cout << 15806;
    for (int i = 0; i < 15806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15807(int& val) {
    func_in_15807(val);
    const int x = 15807;
    std::cout << 15807;
    for (int i = 0; i < 15807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15807(int& val) {
    const int x = 15807;
    std::cout << 15807;
    for (int i = 0; i < 15807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15808(int& val) {
    func_in_15808(val);
    const int x = 15808;
    std::cout << 15808;
    for (int i = 0; i < 15808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15808(int& val) {
    const int x = 15808;
    std::cout << 15808;
    for (int i = 0; i < 15808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15809(int& val) {
    func_in_15809(val);
    const int x = 15809;
    std::cout << 15809;
    for (int i = 0; i < 15809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15809(int& val) {
    const int x = 15809;
    std::cout << 15809;
    for (int i = 0; i < 15809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15810(int& val) {
    func_in_15810(val);
    const int x = 15810;
    std::cout << 15810;
    for (int i = 0; i < 15810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15810(int& val) {
    const int x = 15810;
    std::cout << 15810;
    for (int i = 0; i < 15810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15811(int& val) {
    func_in_15811(val);
    const int x = 15811;
    std::cout << 15811;
    for (int i = 0; i < 15811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15811(int& val) {
    const int x = 15811;
    std::cout << 15811;
    for (int i = 0; i < 15811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15812(int& val) {
    func_in_15812(val);
    const int x = 15812;
    std::cout << 15812;
    for (int i = 0; i < 15812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15812(int& val) {
    const int x = 15812;
    std::cout << 15812;
    for (int i = 0; i < 15812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15813(int& val) {
    func_in_15813(val);
    const int x = 15813;
    std::cout << 15813;
    for (int i = 0; i < 15813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15813(int& val) {
    const int x = 15813;
    std::cout << 15813;
    for (int i = 0; i < 15813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15814(int& val) {
    func_in_15814(val);
    const int x = 15814;
    std::cout << 15814;
    for (int i = 0; i < 15814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15814(int& val) {
    const int x = 15814;
    std::cout << 15814;
    for (int i = 0; i < 15814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15815(int& val) {
    func_in_15815(val);
    const int x = 15815;
    std::cout << 15815;
    for (int i = 0; i < 15815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15815(int& val) {
    const int x = 15815;
    std::cout << 15815;
    for (int i = 0; i < 15815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15816(int& val) {
    func_in_15816(val);
    const int x = 15816;
    std::cout << 15816;
    for (int i = 0; i < 15816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15816(int& val) {
    const int x = 15816;
    std::cout << 15816;
    for (int i = 0; i < 15816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15817(int& val) {
    func_in_15817(val);
    const int x = 15817;
    std::cout << 15817;
    for (int i = 0; i < 15817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15817(int& val) {
    const int x = 15817;
    std::cout << 15817;
    for (int i = 0; i < 15817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15818(int& val) {
    func_in_15818(val);
    const int x = 15818;
    std::cout << 15818;
    for (int i = 0; i < 15818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15818(int& val) {
    const int x = 15818;
    std::cout << 15818;
    for (int i = 0; i < 15818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15819(int& val) {
    func_in_15819(val);
    const int x = 15819;
    std::cout << 15819;
    for (int i = 0; i < 15819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15819(int& val) {
    const int x = 15819;
    std::cout << 15819;
    for (int i = 0; i < 15819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15820(int& val) {
    func_in_15820(val);
    const int x = 15820;
    std::cout << 15820;
    for (int i = 0; i < 15820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15820(int& val) {
    const int x = 15820;
    std::cout << 15820;
    for (int i = 0; i < 15820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15821(int& val) {
    func_in_15821(val);
    const int x = 15821;
    std::cout << 15821;
    for (int i = 0; i < 15821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15821(int& val) {
    const int x = 15821;
    std::cout << 15821;
    for (int i = 0; i < 15821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15822(int& val) {
    func_in_15822(val);
    const int x = 15822;
    std::cout << 15822;
    for (int i = 0; i < 15822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15822(int& val) {
    const int x = 15822;
    std::cout << 15822;
    for (int i = 0; i < 15822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15823(int& val) {
    func_in_15823(val);
    const int x = 15823;
    std::cout << 15823;
    for (int i = 0; i < 15823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15823(int& val) {
    const int x = 15823;
    std::cout << 15823;
    for (int i = 0; i < 15823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15824(int& val) {
    func_in_15824(val);
    const int x = 15824;
    std::cout << 15824;
    for (int i = 0; i < 15824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15824(int& val) {
    const int x = 15824;
    std::cout << 15824;
    for (int i = 0; i < 15824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15825(int& val) {
    func_in_15825(val);
    const int x = 15825;
    std::cout << 15825;
    for (int i = 0; i < 15825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15825(int& val) {
    const int x = 15825;
    std::cout << 15825;
    for (int i = 0; i < 15825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15826(int& val) {
    func_in_15826(val);
    const int x = 15826;
    std::cout << 15826;
    for (int i = 0; i < 15826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15826(int& val) {
    const int x = 15826;
    std::cout << 15826;
    for (int i = 0; i < 15826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15827(int& val) {
    func_in_15827(val);
    const int x = 15827;
    std::cout << 15827;
    for (int i = 0; i < 15827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15827(int& val) {
    const int x = 15827;
    std::cout << 15827;
    for (int i = 0; i < 15827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15828(int& val) {
    func_in_15828(val);
    const int x = 15828;
    std::cout << 15828;
    for (int i = 0; i < 15828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15828(int& val) {
    const int x = 15828;
    std::cout << 15828;
    for (int i = 0; i < 15828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15829(int& val) {
    func_in_15829(val);
    const int x = 15829;
    std::cout << 15829;
    for (int i = 0; i < 15829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15829(int& val) {
    const int x = 15829;
    std::cout << 15829;
    for (int i = 0; i < 15829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15830(int& val) {
    func_in_15830(val);
    const int x = 15830;
    std::cout << 15830;
    for (int i = 0; i < 15830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15830(int& val) {
    const int x = 15830;
    std::cout << 15830;
    for (int i = 0; i < 15830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15831(int& val) {
    func_in_15831(val);
    const int x = 15831;
    std::cout << 15831;
    for (int i = 0; i < 15831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15831(int& val) {
    const int x = 15831;
    std::cout << 15831;
    for (int i = 0; i < 15831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15832(int& val) {
    func_in_15832(val);
    const int x = 15832;
    std::cout << 15832;
    for (int i = 0; i < 15832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15832(int& val) {
    const int x = 15832;
    std::cout << 15832;
    for (int i = 0; i < 15832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15833(int& val) {
    func_in_15833(val);
    const int x = 15833;
    std::cout << 15833;
    for (int i = 0; i < 15833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15833(int& val) {
    const int x = 15833;
    std::cout << 15833;
    for (int i = 0; i < 15833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15834(int& val) {
    func_in_15834(val);
    const int x = 15834;
    std::cout << 15834;
    for (int i = 0; i < 15834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15834(int& val) {
    const int x = 15834;
    std::cout << 15834;
    for (int i = 0; i < 15834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15835(int& val) {
    func_in_15835(val);
    const int x = 15835;
    std::cout << 15835;
    for (int i = 0; i < 15835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15835(int& val) {
    const int x = 15835;
    std::cout << 15835;
    for (int i = 0; i < 15835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15836(int& val) {
    func_in_15836(val);
    const int x = 15836;
    std::cout << 15836;
    for (int i = 0; i < 15836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15836(int& val) {
    const int x = 15836;
    std::cout << 15836;
    for (int i = 0; i < 15836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15837(int& val) {
    func_in_15837(val);
    const int x = 15837;
    std::cout << 15837;
    for (int i = 0; i < 15837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15837(int& val) {
    const int x = 15837;
    std::cout << 15837;
    for (int i = 0; i < 15837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15838(int& val) {
    func_in_15838(val);
    const int x = 15838;
    std::cout << 15838;
    for (int i = 0; i < 15838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15838(int& val) {
    const int x = 15838;
    std::cout << 15838;
    for (int i = 0; i < 15838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15839(int& val) {
    func_in_15839(val);
    const int x = 15839;
    std::cout << 15839;
    for (int i = 0; i < 15839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15839(int& val) {
    const int x = 15839;
    std::cout << 15839;
    for (int i = 0; i < 15839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15840(int& val) {
    func_in_15840(val);
    const int x = 15840;
    std::cout << 15840;
    for (int i = 0; i < 15840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15840(int& val) {
    const int x = 15840;
    std::cout << 15840;
    for (int i = 0; i < 15840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15841(int& val) {
    func_in_15841(val);
    const int x = 15841;
    std::cout << 15841;
    for (int i = 0; i < 15841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15841(int& val) {
    const int x = 15841;
    std::cout << 15841;
    for (int i = 0; i < 15841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15842(int& val) {
    func_in_15842(val);
    const int x = 15842;
    std::cout << 15842;
    for (int i = 0; i < 15842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15842(int& val) {
    const int x = 15842;
    std::cout << 15842;
    for (int i = 0; i < 15842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15843(int& val) {
    func_in_15843(val);
    const int x = 15843;
    std::cout << 15843;
    for (int i = 0; i < 15843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15843(int& val) {
    const int x = 15843;
    std::cout << 15843;
    for (int i = 0; i < 15843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15844(int& val) {
    func_in_15844(val);
    const int x = 15844;
    std::cout << 15844;
    for (int i = 0; i < 15844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15844(int& val) {
    const int x = 15844;
    std::cout << 15844;
    for (int i = 0; i < 15844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15845(int& val) {
    func_in_15845(val);
    const int x = 15845;
    std::cout << 15845;
    for (int i = 0; i < 15845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15845(int& val) {
    const int x = 15845;
    std::cout << 15845;
    for (int i = 0; i < 15845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15846(int& val) {
    func_in_15846(val);
    const int x = 15846;
    std::cout << 15846;
    for (int i = 0; i < 15846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15846(int& val) {
    const int x = 15846;
    std::cout << 15846;
    for (int i = 0; i < 15846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15847(int& val) {
    func_in_15847(val);
    const int x = 15847;
    std::cout << 15847;
    for (int i = 0; i < 15847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15847(int& val) {
    const int x = 15847;
    std::cout << 15847;
    for (int i = 0; i < 15847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15848(int& val) {
    func_in_15848(val);
    const int x = 15848;
    std::cout << 15848;
    for (int i = 0; i < 15848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15848(int& val) {
    const int x = 15848;
    std::cout << 15848;
    for (int i = 0; i < 15848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15849(int& val) {
    func_in_15849(val);
    const int x = 15849;
    std::cout << 15849;
    for (int i = 0; i < 15849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15849(int& val) {
    const int x = 15849;
    std::cout << 15849;
    for (int i = 0; i < 15849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15850(int& val) {
    func_in_15850(val);
    const int x = 15850;
    std::cout << 15850;
    for (int i = 0; i < 15850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15850(int& val) {
    const int x = 15850;
    std::cout << 15850;
    for (int i = 0; i < 15850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15851(int& val) {
    func_in_15851(val);
    const int x = 15851;
    std::cout << 15851;
    for (int i = 0; i < 15851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15851(int& val) {
    const int x = 15851;
    std::cout << 15851;
    for (int i = 0; i < 15851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15852(int& val) {
    func_in_15852(val);
    const int x = 15852;
    std::cout << 15852;
    for (int i = 0; i < 15852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15852(int& val) {
    const int x = 15852;
    std::cout << 15852;
    for (int i = 0; i < 15852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15853(int& val) {
    func_in_15853(val);
    const int x = 15853;
    std::cout << 15853;
    for (int i = 0; i < 15853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15853(int& val) {
    const int x = 15853;
    std::cout << 15853;
    for (int i = 0; i < 15853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15854(int& val) {
    func_in_15854(val);
    const int x = 15854;
    std::cout << 15854;
    for (int i = 0; i < 15854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15854(int& val) {
    const int x = 15854;
    std::cout << 15854;
    for (int i = 0; i < 15854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15855(int& val) {
    func_in_15855(val);
    const int x = 15855;
    std::cout << 15855;
    for (int i = 0; i < 15855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15855(int& val) {
    const int x = 15855;
    std::cout << 15855;
    for (int i = 0; i < 15855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15856(int& val) {
    func_in_15856(val);
    const int x = 15856;
    std::cout << 15856;
    for (int i = 0; i < 15856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15856(int& val) {
    const int x = 15856;
    std::cout << 15856;
    for (int i = 0; i < 15856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15857(int& val) {
    func_in_15857(val);
    const int x = 15857;
    std::cout << 15857;
    for (int i = 0; i < 15857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15857(int& val) {
    const int x = 15857;
    std::cout << 15857;
    for (int i = 0; i < 15857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15858(int& val) {
    func_in_15858(val);
    const int x = 15858;
    std::cout << 15858;
    for (int i = 0; i < 15858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15858(int& val) {
    const int x = 15858;
    std::cout << 15858;
    for (int i = 0; i < 15858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15859(int& val) {
    func_in_15859(val);
    const int x = 15859;
    std::cout << 15859;
    for (int i = 0; i < 15859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15859(int& val) {
    const int x = 15859;
    std::cout << 15859;
    for (int i = 0; i < 15859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15860(int& val) {
    func_in_15860(val);
    const int x = 15860;
    std::cout << 15860;
    for (int i = 0; i < 15860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15860(int& val) {
    const int x = 15860;
    std::cout << 15860;
    for (int i = 0; i < 15860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15861(int& val) {
    func_in_15861(val);
    const int x = 15861;
    std::cout << 15861;
    for (int i = 0; i < 15861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15861(int& val) {
    const int x = 15861;
    std::cout << 15861;
    for (int i = 0; i < 15861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15862(int& val) {
    func_in_15862(val);
    const int x = 15862;
    std::cout << 15862;
    for (int i = 0; i < 15862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15862(int& val) {
    const int x = 15862;
    std::cout << 15862;
    for (int i = 0; i < 15862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15863(int& val) {
    func_in_15863(val);
    const int x = 15863;
    std::cout << 15863;
    for (int i = 0; i < 15863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15863(int& val) {
    const int x = 15863;
    std::cout << 15863;
    for (int i = 0; i < 15863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15864(int& val) {
    func_in_15864(val);
    const int x = 15864;
    std::cout << 15864;
    for (int i = 0; i < 15864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15864(int& val) {
    const int x = 15864;
    std::cout << 15864;
    for (int i = 0; i < 15864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15865(int& val) {
    func_in_15865(val);
    const int x = 15865;
    std::cout << 15865;
    for (int i = 0; i < 15865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15865(int& val) {
    const int x = 15865;
    std::cout << 15865;
    for (int i = 0; i < 15865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15866(int& val) {
    func_in_15866(val);
    const int x = 15866;
    std::cout << 15866;
    for (int i = 0; i < 15866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15866(int& val) {
    const int x = 15866;
    std::cout << 15866;
    for (int i = 0; i < 15866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15867(int& val) {
    func_in_15867(val);
    const int x = 15867;
    std::cout << 15867;
    for (int i = 0; i < 15867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15867(int& val) {
    const int x = 15867;
    std::cout << 15867;
    for (int i = 0; i < 15867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15868(int& val) {
    func_in_15868(val);
    const int x = 15868;
    std::cout << 15868;
    for (int i = 0; i < 15868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15868(int& val) {
    const int x = 15868;
    std::cout << 15868;
    for (int i = 0; i < 15868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15869(int& val) {
    func_in_15869(val);
    const int x = 15869;
    std::cout << 15869;
    for (int i = 0; i < 15869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15869(int& val) {
    const int x = 15869;
    std::cout << 15869;
    for (int i = 0; i < 15869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15870(int& val) {
    func_in_15870(val);
    const int x = 15870;
    std::cout << 15870;
    for (int i = 0; i < 15870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15870(int& val) {
    const int x = 15870;
    std::cout << 15870;
    for (int i = 0; i < 15870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15871(int& val) {
    func_in_15871(val);
    const int x = 15871;
    std::cout << 15871;
    for (int i = 0; i < 15871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15871(int& val) {
    const int x = 15871;
    std::cout << 15871;
    for (int i = 0; i < 15871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15872(int& val) {
    func_in_15872(val);
    const int x = 15872;
    std::cout << 15872;
    for (int i = 0; i < 15872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15872(int& val) {
    const int x = 15872;
    std::cout << 15872;
    for (int i = 0; i < 15872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15873(int& val) {
    func_in_15873(val);
    const int x = 15873;
    std::cout << 15873;
    for (int i = 0; i < 15873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15873(int& val) {
    const int x = 15873;
    std::cout << 15873;
    for (int i = 0; i < 15873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15874(int& val) {
    func_in_15874(val);
    const int x = 15874;
    std::cout << 15874;
    for (int i = 0; i < 15874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15874(int& val) {
    const int x = 15874;
    std::cout << 15874;
    for (int i = 0; i < 15874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15875(int& val) {
    func_in_15875(val);
    const int x = 15875;
    std::cout << 15875;
    for (int i = 0; i < 15875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15875(int& val) {
    const int x = 15875;
    std::cout << 15875;
    for (int i = 0; i < 15875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15876(int& val) {
    func_in_15876(val);
    const int x = 15876;
    std::cout << 15876;
    for (int i = 0; i < 15876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15876(int& val) {
    const int x = 15876;
    std::cout << 15876;
    for (int i = 0; i < 15876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15877(int& val) {
    func_in_15877(val);
    const int x = 15877;
    std::cout << 15877;
    for (int i = 0; i < 15877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15877(int& val) {
    const int x = 15877;
    std::cout << 15877;
    for (int i = 0; i < 15877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15878(int& val) {
    func_in_15878(val);
    const int x = 15878;
    std::cout << 15878;
    for (int i = 0; i < 15878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15878(int& val) {
    const int x = 15878;
    std::cout << 15878;
    for (int i = 0; i < 15878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15879(int& val) {
    func_in_15879(val);
    const int x = 15879;
    std::cout << 15879;
    for (int i = 0; i < 15879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15879(int& val) {
    const int x = 15879;
    std::cout << 15879;
    for (int i = 0; i < 15879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15880(int& val) {
    func_in_15880(val);
    const int x = 15880;
    std::cout << 15880;
    for (int i = 0; i < 15880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15880(int& val) {
    const int x = 15880;
    std::cout << 15880;
    for (int i = 0; i < 15880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15881(int& val) {
    func_in_15881(val);
    const int x = 15881;
    std::cout << 15881;
    for (int i = 0; i < 15881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15881(int& val) {
    const int x = 15881;
    std::cout << 15881;
    for (int i = 0; i < 15881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15882(int& val) {
    func_in_15882(val);
    const int x = 15882;
    std::cout << 15882;
    for (int i = 0; i < 15882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15882(int& val) {
    const int x = 15882;
    std::cout << 15882;
    for (int i = 0; i < 15882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15883(int& val) {
    func_in_15883(val);
    const int x = 15883;
    std::cout << 15883;
    for (int i = 0; i < 15883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15883(int& val) {
    const int x = 15883;
    std::cout << 15883;
    for (int i = 0; i < 15883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15884(int& val) {
    func_in_15884(val);
    const int x = 15884;
    std::cout << 15884;
    for (int i = 0; i < 15884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15884(int& val) {
    const int x = 15884;
    std::cout << 15884;
    for (int i = 0; i < 15884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15885(int& val) {
    func_in_15885(val);
    const int x = 15885;
    std::cout << 15885;
    for (int i = 0; i < 15885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15885(int& val) {
    const int x = 15885;
    std::cout << 15885;
    for (int i = 0; i < 15885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15886(int& val) {
    func_in_15886(val);
    const int x = 15886;
    std::cout << 15886;
    for (int i = 0; i < 15886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15886(int& val) {
    const int x = 15886;
    std::cout << 15886;
    for (int i = 0; i < 15886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15887(int& val) {
    func_in_15887(val);
    const int x = 15887;
    std::cout << 15887;
    for (int i = 0; i < 15887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15887(int& val) {
    const int x = 15887;
    std::cout << 15887;
    for (int i = 0; i < 15887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15888(int& val) {
    func_in_15888(val);
    const int x = 15888;
    std::cout << 15888;
    for (int i = 0; i < 15888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15888(int& val) {
    const int x = 15888;
    std::cout << 15888;
    for (int i = 0; i < 15888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15889(int& val) {
    func_in_15889(val);
    const int x = 15889;
    std::cout << 15889;
    for (int i = 0; i < 15889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15889(int& val) {
    const int x = 15889;
    std::cout << 15889;
    for (int i = 0; i < 15889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15890(int& val) {
    func_in_15890(val);
    const int x = 15890;
    std::cout << 15890;
    for (int i = 0; i < 15890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15890(int& val) {
    const int x = 15890;
    std::cout << 15890;
    for (int i = 0; i < 15890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15891(int& val) {
    func_in_15891(val);
    const int x = 15891;
    std::cout << 15891;
    for (int i = 0; i < 15891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15891(int& val) {
    const int x = 15891;
    std::cout << 15891;
    for (int i = 0; i < 15891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15892(int& val) {
    func_in_15892(val);
    const int x = 15892;
    std::cout << 15892;
    for (int i = 0; i < 15892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15892(int& val) {
    const int x = 15892;
    std::cout << 15892;
    for (int i = 0; i < 15892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15893(int& val) {
    func_in_15893(val);
    const int x = 15893;
    std::cout << 15893;
    for (int i = 0; i < 15893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15893(int& val) {
    const int x = 15893;
    std::cout << 15893;
    for (int i = 0; i < 15893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15894(int& val) {
    func_in_15894(val);
    const int x = 15894;
    std::cout << 15894;
    for (int i = 0; i < 15894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15894(int& val) {
    const int x = 15894;
    std::cout << 15894;
    for (int i = 0; i < 15894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15895(int& val) {
    func_in_15895(val);
    const int x = 15895;
    std::cout << 15895;
    for (int i = 0; i < 15895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15895(int& val) {
    const int x = 15895;
    std::cout << 15895;
    for (int i = 0; i < 15895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15896(int& val) {
    func_in_15896(val);
    const int x = 15896;
    std::cout << 15896;
    for (int i = 0; i < 15896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15896(int& val) {
    const int x = 15896;
    std::cout << 15896;
    for (int i = 0; i < 15896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15897(int& val) {
    func_in_15897(val);
    const int x = 15897;
    std::cout << 15897;
    for (int i = 0; i < 15897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15897(int& val) {
    const int x = 15897;
    std::cout << 15897;
    for (int i = 0; i < 15897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15898(int& val) {
    func_in_15898(val);
    const int x = 15898;
    std::cout << 15898;
    for (int i = 0; i < 15898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15898(int& val) {
    const int x = 15898;
    std::cout << 15898;
    for (int i = 0; i < 15898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15899(int& val) {
    func_in_15899(val);
    const int x = 15899;
    std::cout << 15899;
    for (int i = 0; i < 15899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15899(int& val) {
    const int x = 15899;
    std::cout << 15899;
    for (int i = 0; i < 15899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15900(int& val) {
    func_in_15900(val);
    const int x = 15900;
    std::cout << 15900;
    for (int i = 0; i < 15900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15900(int& val) {
    const int x = 15900;
    std::cout << 15900;
    for (int i = 0; i < 15900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15901(int& val) {
    func_in_15901(val);
    const int x = 15901;
    std::cout << 15901;
    for (int i = 0; i < 15901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15901(int& val) {
    const int x = 15901;
    std::cout << 15901;
    for (int i = 0; i < 15901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15902(int& val) {
    func_in_15902(val);
    const int x = 15902;
    std::cout << 15902;
    for (int i = 0; i < 15902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15902(int& val) {
    const int x = 15902;
    std::cout << 15902;
    for (int i = 0; i < 15902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15903(int& val) {
    func_in_15903(val);
    const int x = 15903;
    std::cout << 15903;
    for (int i = 0; i < 15903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15903(int& val) {
    const int x = 15903;
    std::cout << 15903;
    for (int i = 0; i < 15903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15904(int& val) {
    func_in_15904(val);
    const int x = 15904;
    std::cout << 15904;
    for (int i = 0; i < 15904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15904(int& val) {
    const int x = 15904;
    std::cout << 15904;
    for (int i = 0; i < 15904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15905(int& val) {
    func_in_15905(val);
    const int x = 15905;
    std::cout << 15905;
    for (int i = 0; i < 15905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15905(int& val) {
    const int x = 15905;
    std::cout << 15905;
    for (int i = 0; i < 15905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15906(int& val) {
    func_in_15906(val);
    const int x = 15906;
    std::cout << 15906;
    for (int i = 0; i < 15906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15906(int& val) {
    const int x = 15906;
    std::cout << 15906;
    for (int i = 0; i < 15906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15907(int& val) {
    func_in_15907(val);
    const int x = 15907;
    std::cout << 15907;
    for (int i = 0; i < 15907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15907(int& val) {
    const int x = 15907;
    std::cout << 15907;
    for (int i = 0; i < 15907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15908(int& val) {
    func_in_15908(val);
    const int x = 15908;
    std::cout << 15908;
    for (int i = 0; i < 15908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15908(int& val) {
    const int x = 15908;
    std::cout << 15908;
    for (int i = 0; i < 15908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15909(int& val) {
    func_in_15909(val);
    const int x = 15909;
    std::cout << 15909;
    for (int i = 0; i < 15909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15909(int& val) {
    const int x = 15909;
    std::cout << 15909;
    for (int i = 0; i < 15909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15910(int& val) {
    func_in_15910(val);
    const int x = 15910;
    std::cout << 15910;
    for (int i = 0; i < 15910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15910(int& val) {
    const int x = 15910;
    std::cout << 15910;
    for (int i = 0; i < 15910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15911(int& val) {
    func_in_15911(val);
    const int x = 15911;
    std::cout << 15911;
    for (int i = 0; i < 15911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15911(int& val) {
    const int x = 15911;
    std::cout << 15911;
    for (int i = 0; i < 15911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15912(int& val) {
    func_in_15912(val);
    const int x = 15912;
    std::cout << 15912;
    for (int i = 0; i < 15912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15912(int& val) {
    const int x = 15912;
    std::cout << 15912;
    for (int i = 0; i < 15912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15913(int& val) {
    func_in_15913(val);
    const int x = 15913;
    std::cout << 15913;
    for (int i = 0; i < 15913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15913(int& val) {
    const int x = 15913;
    std::cout << 15913;
    for (int i = 0; i < 15913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15914(int& val) {
    func_in_15914(val);
    const int x = 15914;
    std::cout << 15914;
    for (int i = 0; i < 15914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15914(int& val) {
    const int x = 15914;
    std::cout << 15914;
    for (int i = 0; i < 15914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15915(int& val) {
    func_in_15915(val);
    const int x = 15915;
    std::cout << 15915;
    for (int i = 0; i < 15915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15915(int& val) {
    const int x = 15915;
    std::cout << 15915;
    for (int i = 0; i < 15915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15916(int& val) {
    func_in_15916(val);
    const int x = 15916;
    std::cout << 15916;
    for (int i = 0; i < 15916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15916(int& val) {
    const int x = 15916;
    std::cout << 15916;
    for (int i = 0; i < 15916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15917(int& val) {
    func_in_15917(val);
    const int x = 15917;
    std::cout << 15917;
    for (int i = 0; i < 15917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15917(int& val) {
    const int x = 15917;
    std::cout << 15917;
    for (int i = 0; i < 15917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15918(int& val) {
    func_in_15918(val);
    const int x = 15918;
    std::cout << 15918;
    for (int i = 0; i < 15918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15918(int& val) {
    const int x = 15918;
    std::cout << 15918;
    for (int i = 0; i < 15918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15919(int& val) {
    func_in_15919(val);
    const int x = 15919;
    std::cout << 15919;
    for (int i = 0; i < 15919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15919(int& val) {
    const int x = 15919;
    std::cout << 15919;
    for (int i = 0; i < 15919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15920(int& val) {
    func_in_15920(val);
    const int x = 15920;
    std::cout << 15920;
    for (int i = 0; i < 15920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15920(int& val) {
    const int x = 15920;
    std::cout << 15920;
    for (int i = 0; i < 15920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15921(int& val) {
    func_in_15921(val);
    const int x = 15921;
    std::cout << 15921;
    for (int i = 0; i < 15921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15921(int& val) {
    const int x = 15921;
    std::cout << 15921;
    for (int i = 0; i < 15921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15922(int& val) {
    func_in_15922(val);
    const int x = 15922;
    std::cout << 15922;
    for (int i = 0; i < 15922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15922(int& val) {
    const int x = 15922;
    std::cout << 15922;
    for (int i = 0; i < 15922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15923(int& val) {
    func_in_15923(val);
    const int x = 15923;
    std::cout << 15923;
    for (int i = 0; i < 15923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15923(int& val) {
    const int x = 15923;
    std::cout << 15923;
    for (int i = 0; i < 15923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15924(int& val) {
    func_in_15924(val);
    const int x = 15924;
    std::cout << 15924;
    for (int i = 0; i < 15924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15924(int& val) {
    const int x = 15924;
    std::cout << 15924;
    for (int i = 0; i < 15924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15925(int& val) {
    func_in_15925(val);
    const int x = 15925;
    std::cout << 15925;
    for (int i = 0; i < 15925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15925(int& val) {
    const int x = 15925;
    std::cout << 15925;
    for (int i = 0; i < 15925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15926(int& val) {
    func_in_15926(val);
    const int x = 15926;
    std::cout << 15926;
    for (int i = 0; i < 15926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15926(int& val) {
    const int x = 15926;
    std::cout << 15926;
    for (int i = 0; i < 15926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15927(int& val) {
    func_in_15927(val);
    const int x = 15927;
    std::cout << 15927;
    for (int i = 0; i < 15927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15927(int& val) {
    const int x = 15927;
    std::cout << 15927;
    for (int i = 0; i < 15927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15928(int& val) {
    func_in_15928(val);
    const int x = 15928;
    std::cout << 15928;
    for (int i = 0; i < 15928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15928(int& val) {
    const int x = 15928;
    std::cout << 15928;
    for (int i = 0; i < 15928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15929(int& val) {
    func_in_15929(val);
    const int x = 15929;
    std::cout << 15929;
    for (int i = 0; i < 15929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15929(int& val) {
    const int x = 15929;
    std::cout << 15929;
    for (int i = 0; i < 15929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15930(int& val) {
    func_in_15930(val);
    const int x = 15930;
    std::cout << 15930;
    for (int i = 0; i < 15930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15930(int& val) {
    const int x = 15930;
    std::cout << 15930;
    for (int i = 0; i < 15930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15931(int& val) {
    func_in_15931(val);
    const int x = 15931;
    std::cout << 15931;
    for (int i = 0; i < 15931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15931(int& val) {
    const int x = 15931;
    std::cout << 15931;
    for (int i = 0; i < 15931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15932(int& val) {
    func_in_15932(val);
    const int x = 15932;
    std::cout << 15932;
    for (int i = 0; i < 15932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15932(int& val) {
    const int x = 15932;
    std::cout << 15932;
    for (int i = 0; i < 15932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15933(int& val) {
    func_in_15933(val);
    const int x = 15933;
    std::cout << 15933;
    for (int i = 0; i < 15933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15933(int& val) {
    const int x = 15933;
    std::cout << 15933;
    for (int i = 0; i < 15933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15934(int& val) {
    func_in_15934(val);
    const int x = 15934;
    std::cout << 15934;
    for (int i = 0; i < 15934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15934(int& val) {
    const int x = 15934;
    std::cout << 15934;
    for (int i = 0; i < 15934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15935(int& val) {
    func_in_15935(val);
    const int x = 15935;
    std::cout << 15935;
    for (int i = 0; i < 15935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15935(int& val) {
    const int x = 15935;
    std::cout << 15935;
    for (int i = 0; i < 15935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15936(int& val) {
    func_in_15936(val);
    const int x = 15936;
    std::cout << 15936;
    for (int i = 0; i < 15936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15936(int& val) {
    const int x = 15936;
    std::cout << 15936;
    for (int i = 0; i < 15936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15937(int& val) {
    func_in_15937(val);
    const int x = 15937;
    std::cout << 15937;
    for (int i = 0; i < 15937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15937(int& val) {
    const int x = 15937;
    std::cout << 15937;
    for (int i = 0; i < 15937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15938(int& val) {
    func_in_15938(val);
    const int x = 15938;
    std::cout << 15938;
    for (int i = 0; i < 15938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15938(int& val) {
    const int x = 15938;
    std::cout << 15938;
    for (int i = 0; i < 15938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15939(int& val) {
    func_in_15939(val);
    const int x = 15939;
    std::cout << 15939;
    for (int i = 0; i < 15939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15939(int& val) {
    const int x = 15939;
    std::cout << 15939;
    for (int i = 0; i < 15939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15940(int& val) {
    func_in_15940(val);
    const int x = 15940;
    std::cout << 15940;
    for (int i = 0; i < 15940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15940(int& val) {
    const int x = 15940;
    std::cout << 15940;
    for (int i = 0; i < 15940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15941(int& val) {
    func_in_15941(val);
    const int x = 15941;
    std::cout << 15941;
    for (int i = 0; i < 15941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15941(int& val) {
    const int x = 15941;
    std::cout << 15941;
    for (int i = 0; i < 15941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15942(int& val) {
    func_in_15942(val);
    const int x = 15942;
    std::cout << 15942;
    for (int i = 0; i < 15942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15942(int& val) {
    const int x = 15942;
    std::cout << 15942;
    for (int i = 0; i < 15942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15943(int& val) {
    func_in_15943(val);
    const int x = 15943;
    std::cout << 15943;
    for (int i = 0; i < 15943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15943(int& val) {
    const int x = 15943;
    std::cout << 15943;
    for (int i = 0; i < 15943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15944(int& val) {
    func_in_15944(val);
    const int x = 15944;
    std::cout << 15944;
    for (int i = 0; i < 15944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15944(int& val) {
    const int x = 15944;
    std::cout << 15944;
    for (int i = 0; i < 15944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15945(int& val) {
    func_in_15945(val);
    const int x = 15945;
    std::cout << 15945;
    for (int i = 0; i < 15945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15945(int& val) {
    const int x = 15945;
    std::cout << 15945;
    for (int i = 0; i < 15945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15946(int& val) {
    func_in_15946(val);
    const int x = 15946;
    std::cout << 15946;
    for (int i = 0; i < 15946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15946(int& val) {
    const int x = 15946;
    std::cout << 15946;
    for (int i = 0; i < 15946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15947(int& val) {
    func_in_15947(val);
    const int x = 15947;
    std::cout << 15947;
    for (int i = 0; i < 15947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15947(int& val) {
    const int x = 15947;
    std::cout << 15947;
    for (int i = 0; i < 15947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15948(int& val) {
    func_in_15948(val);
    const int x = 15948;
    std::cout << 15948;
    for (int i = 0; i < 15948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15948(int& val) {
    const int x = 15948;
    std::cout << 15948;
    for (int i = 0; i < 15948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15949(int& val) {
    func_in_15949(val);
    const int x = 15949;
    std::cout << 15949;
    for (int i = 0; i < 15949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15949(int& val) {
    const int x = 15949;
    std::cout << 15949;
    for (int i = 0; i < 15949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15950(int& val) {
    func_in_15950(val);
    const int x = 15950;
    std::cout << 15950;
    for (int i = 0; i < 15950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15950(int& val) {
    const int x = 15950;
    std::cout << 15950;
    for (int i = 0; i < 15950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15951(int& val) {
    func_in_15951(val);
    const int x = 15951;
    std::cout << 15951;
    for (int i = 0; i < 15951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15951(int& val) {
    const int x = 15951;
    std::cout << 15951;
    for (int i = 0; i < 15951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15952(int& val) {
    func_in_15952(val);
    const int x = 15952;
    std::cout << 15952;
    for (int i = 0; i < 15952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15952(int& val) {
    const int x = 15952;
    std::cout << 15952;
    for (int i = 0; i < 15952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15953(int& val) {
    func_in_15953(val);
    const int x = 15953;
    std::cout << 15953;
    for (int i = 0; i < 15953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15953(int& val) {
    const int x = 15953;
    std::cout << 15953;
    for (int i = 0; i < 15953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15954(int& val) {
    func_in_15954(val);
    const int x = 15954;
    std::cout << 15954;
    for (int i = 0; i < 15954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15954(int& val) {
    const int x = 15954;
    std::cout << 15954;
    for (int i = 0; i < 15954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15955(int& val) {
    func_in_15955(val);
    const int x = 15955;
    std::cout << 15955;
    for (int i = 0; i < 15955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15955(int& val) {
    const int x = 15955;
    std::cout << 15955;
    for (int i = 0; i < 15955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15956(int& val) {
    func_in_15956(val);
    const int x = 15956;
    std::cout << 15956;
    for (int i = 0; i < 15956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15956(int& val) {
    const int x = 15956;
    std::cout << 15956;
    for (int i = 0; i < 15956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15957(int& val) {
    func_in_15957(val);
    const int x = 15957;
    std::cout << 15957;
    for (int i = 0; i < 15957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15957(int& val) {
    const int x = 15957;
    std::cout << 15957;
    for (int i = 0; i < 15957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15958(int& val) {
    func_in_15958(val);
    const int x = 15958;
    std::cout << 15958;
    for (int i = 0; i < 15958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15958(int& val) {
    const int x = 15958;
    std::cout << 15958;
    for (int i = 0; i < 15958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15959(int& val) {
    func_in_15959(val);
    const int x = 15959;
    std::cout << 15959;
    for (int i = 0; i < 15959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15959(int& val) {
    const int x = 15959;
    std::cout << 15959;
    for (int i = 0; i < 15959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15960(int& val) {
    func_in_15960(val);
    const int x = 15960;
    std::cout << 15960;
    for (int i = 0; i < 15960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15960(int& val) {
    const int x = 15960;
    std::cout << 15960;
    for (int i = 0; i < 15960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15961(int& val) {
    func_in_15961(val);
    const int x = 15961;
    std::cout << 15961;
    for (int i = 0; i < 15961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15961(int& val) {
    const int x = 15961;
    std::cout << 15961;
    for (int i = 0; i < 15961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15962(int& val) {
    func_in_15962(val);
    const int x = 15962;
    std::cout << 15962;
    for (int i = 0; i < 15962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15962(int& val) {
    const int x = 15962;
    std::cout << 15962;
    for (int i = 0; i < 15962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15963(int& val) {
    func_in_15963(val);
    const int x = 15963;
    std::cout << 15963;
    for (int i = 0; i < 15963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15963(int& val) {
    const int x = 15963;
    std::cout << 15963;
    for (int i = 0; i < 15963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15964(int& val) {
    func_in_15964(val);
    const int x = 15964;
    std::cout << 15964;
    for (int i = 0; i < 15964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15964(int& val) {
    const int x = 15964;
    std::cout << 15964;
    for (int i = 0; i < 15964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15965(int& val) {
    func_in_15965(val);
    const int x = 15965;
    std::cout << 15965;
    for (int i = 0; i < 15965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15965(int& val) {
    const int x = 15965;
    std::cout << 15965;
    for (int i = 0; i < 15965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15966(int& val) {
    func_in_15966(val);
    const int x = 15966;
    std::cout << 15966;
    for (int i = 0; i < 15966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15966(int& val) {
    const int x = 15966;
    std::cout << 15966;
    for (int i = 0; i < 15966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15967(int& val) {
    func_in_15967(val);
    const int x = 15967;
    std::cout << 15967;
    for (int i = 0; i < 15967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15967(int& val) {
    const int x = 15967;
    std::cout << 15967;
    for (int i = 0; i < 15967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15968(int& val) {
    func_in_15968(val);
    const int x = 15968;
    std::cout << 15968;
    for (int i = 0; i < 15968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15968(int& val) {
    const int x = 15968;
    std::cout << 15968;
    for (int i = 0; i < 15968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15969(int& val) {
    func_in_15969(val);
    const int x = 15969;
    std::cout << 15969;
    for (int i = 0; i < 15969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15969(int& val) {
    const int x = 15969;
    std::cout << 15969;
    for (int i = 0; i < 15969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15970(int& val) {
    func_in_15970(val);
    const int x = 15970;
    std::cout << 15970;
    for (int i = 0; i < 15970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15970(int& val) {
    const int x = 15970;
    std::cout << 15970;
    for (int i = 0; i < 15970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15971(int& val) {
    func_in_15971(val);
    const int x = 15971;
    std::cout << 15971;
    for (int i = 0; i < 15971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15971(int& val) {
    const int x = 15971;
    std::cout << 15971;
    for (int i = 0; i < 15971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15972(int& val) {
    func_in_15972(val);
    const int x = 15972;
    std::cout << 15972;
    for (int i = 0; i < 15972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15972(int& val) {
    const int x = 15972;
    std::cout << 15972;
    for (int i = 0; i < 15972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15973(int& val) {
    func_in_15973(val);
    const int x = 15973;
    std::cout << 15973;
    for (int i = 0; i < 15973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15973(int& val) {
    const int x = 15973;
    std::cout << 15973;
    for (int i = 0; i < 15973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15974(int& val) {
    func_in_15974(val);
    const int x = 15974;
    std::cout << 15974;
    for (int i = 0; i < 15974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15974(int& val) {
    const int x = 15974;
    std::cout << 15974;
    for (int i = 0; i < 15974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15975(int& val) {
    func_in_15975(val);
    const int x = 15975;
    std::cout << 15975;
    for (int i = 0; i < 15975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15975(int& val) {
    const int x = 15975;
    std::cout << 15975;
    for (int i = 0; i < 15975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15976(int& val) {
    func_in_15976(val);
    const int x = 15976;
    std::cout << 15976;
    for (int i = 0; i < 15976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15976(int& val) {
    const int x = 15976;
    std::cout << 15976;
    for (int i = 0; i < 15976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15977(int& val) {
    func_in_15977(val);
    const int x = 15977;
    std::cout << 15977;
    for (int i = 0; i < 15977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15977(int& val) {
    const int x = 15977;
    std::cout << 15977;
    for (int i = 0; i < 15977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15978(int& val) {
    func_in_15978(val);
    const int x = 15978;
    std::cout << 15978;
    for (int i = 0; i < 15978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15978(int& val) {
    const int x = 15978;
    std::cout << 15978;
    for (int i = 0; i < 15978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15979(int& val) {
    func_in_15979(val);
    const int x = 15979;
    std::cout << 15979;
    for (int i = 0; i < 15979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15979(int& val) {
    const int x = 15979;
    std::cout << 15979;
    for (int i = 0; i < 15979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15980(int& val) {
    func_in_15980(val);
    const int x = 15980;
    std::cout << 15980;
    for (int i = 0; i < 15980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15980(int& val) {
    const int x = 15980;
    std::cout << 15980;
    for (int i = 0; i < 15980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15981(int& val) {
    func_in_15981(val);
    const int x = 15981;
    std::cout << 15981;
    for (int i = 0; i < 15981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15981(int& val) {
    const int x = 15981;
    std::cout << 15981;
    for (int i = 0; i < 15981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15982(int& val) {
    func_in_15982(val);
    const int x = 15982;
    std::cout << 15982;
    for (int i = 0; i < 15982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15982(int& val) {
    const int x = 15982;
    std::cout << 15982;
    for (int i = 0; i < 15982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15983(int& val) {
    func_in_15983(val);
    const int x = 15983;
    std::cout << 15983;
    for (int i = 0; i < 15983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15983(int& val) {
    const int x = 15983;
    std::cout << 15983;
    for (int i = 0; i < 15983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15984(int& val) {
    func_in_15984(val);
    const int x = 15984;
    std::cout << 15984;
    for (int i = 0; i < 15984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15984(int& val) {
    const int x = 15984;
    std::cout << 15984;
    for (int i = 0; i < 15984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15985(int& val) {
    func_in_15985(val);
    const int x = 15985;
    std::cout << 15985;
    for (int i = 0; i < 15985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15985(int& val) {
    const int x = 15985;
    std::cout << 15985;
    for (int i = 0; i < 15985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15986(int& val) {
    func_in_15986(val);
    const int x = 15986;
    std::cout << 15986;
    for (int i = 0; i < 15986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15986(int& val) {
    const int x = 15986;
    std::cout << 15986;
    for (int i = 0; i < 15986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15987(int& val) {
    func_in_15987(val);
    const int x = 15987;
    std::cout << 15987;
    for (int i = 0; i < 15987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15987(int& val) {
    const int x = 15987;
    std::cout << 15987;
    for (int i = 0; i < 15987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15988(int& val) {
    func_in_15988(val);
    const int x = 15988;
    std::cout << 15988;
    for (int i = 0; i < 15988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15988(int& val) {
    const int x = 15988;
    std::cout << 15988;
    for (int i = 0; i < 15988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15989(int& val) {
    func_in_15989(val);
    const int x = 15989;
    std::cout << 15989;
    for (int i = 0; i < 15989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15989(int& val) {
    const int x = 15989;
    std::cout << 15989;
    for (int i = 0; i < 15989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15990(int& val) {
    func_in_15990(val);
    const int x = 15990;
    std::cout << 15990;
    for (int i = 0; i < 15990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15990(int& val) {
    const int x = 15990;
    std::cout << 15990;
    for (int i = 0; i < 15990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15991(int& val) {
    func_in_15991(val);
    const int x = 15991;
    std::cout << 15991;
    for (int i = 0; i < 15991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15991(int& val) {
    const int x = 15991;
    std::cout << 15991;
    for (int i = 0; i < 15991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15992(int& val) {
    func_in_15992(val);
    const int x = 15992;
    std::cout << 15992;
    for (int i = 0; i < 15992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15992(int& val) {
    const int x = 15992;
    std::cout << 15992;
    for (int i = 0; i < 15992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15993(int& val) {
    func_in_15993(val);
    const int x = 15993;
    std::cout << 15993;
    for (int i = 0; i < 15993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15993(int& val) {
    const int x = 15993;
    std::cout << 15993;
    for (int i = 0; i < 15993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15994(int& val) {
    func_in_15994(val);
    const int x = 15994;
    std::cout << 15994;
    for (int i = 0; i < 15994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15994(int& val) {
    const int x = 15994;
    std::cout << 15994;
    for (int i = 0; i < 15994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15995(int& val) {
    func_in_15995(val);
    const int x = 15995;
    std::cout << 15995;
    for (int i = 0; i < 15995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15995(int& val) {
    const int x = 15995;
    std::cout << 15995;
    for (int i = 0; i < 15995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15996(int& val) {
    func_in_15996(val);
    const int x = 15996;
    std::cout << 15996;
    for (int i = 0; i < 15996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15996(int& val) {
    const int x = 15996;
    std::cout << 15996;
    for (int i = 0; i < 15996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15997(int& val) {
    func_in_15997(val);
    const int x = 15997;
    std::cout << 15997;
    for (int i = 0; i < 15997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15997(int& val) {
    const int x = 15997;
    std::cout << 15997;
    for (int i = 0; i < 15997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15998(int& val) {
    func_in_15998(val);
    const int x = 15998;
    std::cout << 15998;
    for (int i = 0; i < 15998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15998(int& val) {
    const int x = 15998;
    std::cout << 15998;
    for (int i = 0; i < 15998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15999(int& val) {
    func_in_15999(val);
    const int x = 15999;
    std::cout << 15999;
    for (int i = 0; i < 15999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15999(int& val) {
    const int x = 15999;
    std::cout << 15999;
    for (int i = 0; i < 15999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

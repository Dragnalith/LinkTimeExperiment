#include "lib.h"
#include <iostream>
int func_36000(int& val) {
    func_in_36000(val);
    const int x = 36000;
    std::cout << 36000;
    for (int i = 0; i < 36000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36000(int& val) {
    const int x = 36000;
    std::cout << 36000;
    for (int i = 0; i < 36000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36001(int& val) {
    func_in_36001(val);
    const int x = 36001;
    std::cout << 36001;
    for (int i = 0; i < 36001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36001(int& val) {
    const int x = 36001;
    std::cout << 36001;
    for (int i = 0; i < 36001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36002(int& val) {
    func_in_36002(val);
    const int x = 36002;
    std::cout << 36002;
    for (int i = 0; i < 36002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36002(int& val) {
    const int x = 36002;
    std::cout << 36002;
    for (int i = 0; i < 36002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36003(int& val) {
    func_in_36003(val);
    const int x = 36003;
    std::cout << 36003;
    for (int i = 0; i < 36003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36003(int& val) {
    const int x = 36003;
    std::cout << 36003;
    for (int i = 0; i < 36003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36004(int& val) {
    func_in_36004(val);
    const int x = 36004;
    std::cout << 36004;
    for (int i = 0; i < 36004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36004(int& val) {
    const int x = 36004;
    std::cout << 36004;
    for (int i = 0; i < 36004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36005(int& val) {
    func_in_36005(val);
    const int x = 36005;
    std::cout << 36005;
    for (int i = 0; i < 36005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36005(int& val) {
    const int x = 36005;
    std::cout << 36005;
    for (int i = 0; i < 36005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36006(int& val) {
    func_in_36006(val);
    const int x = 36006;
    std::cout << 36006;
    for (int i = 0; i < 36006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36006(int& val) {
    const int x = 36006;
    std::cout << 36006;
    for (int i = 0; i < 36006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36007(int& val) {
    func_in_36007(val);
    const int x = 36007;
    std::cout << 36007;
    for (int i = 0; i < 36007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36007(int& val) {
    const int x = 36007;
    std::cout << 36007;
    for (int i = 0; i < 36007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36008(int& val) {
    func_in_36008(val);
    const int x = 36008;
    std::cout << 36008;
    for (int i = 0; i < 36008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36008(int& val) {
    const int x = 36008;
    std::cout << 36008;
    for (int i = 0; i < 36008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36009(int& val) {
    func_in_36009(val);
    const int x = 36009;
    std::cout << 36009;
    for (int i = 0; i < 36009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36009(int& val) {
    const int x = 36009;
    std::cout << 36009;
    for (int i = 0; i < 36009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36010(int& val) {
    func_in_36010(val);
    const int x = 36010;
    std::cout << 36010;
    for (int i = 0; i < 36010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36010(int& val) {
    const int x = 36010;
    std::cout << 36010;
    for (int i = 0; i < 36010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36011(int& val) {
    func_in_36011(val);
    const int x = 36011;
    std::cout << 36011;
    for (int i = 0; i < 36011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36011(int& val) {
    const int x = 36011;
    std::cout << 36011;
    for (int i = 0; i < 36011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36012(int& val) {
    func_in_36012(val);
    const int x = 36012;
    std::cout << 36012;
    for (int i = 0; i < 36012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36012(int& val) {
    const int x = 36012;
    std::cout << 36012;
    for (int i = 0; i < 36012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36013(int& val) {
    func_in_36013(val);
    const int x = 36013;
    std::cout << 36013;
    for (int i = 0; i < 36013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36013(int& val) {
    const int x = 36013;
    std::cout << 36013;
    for (int i = 0; i < 36013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36014(int& val) {
    func_in_36014(val);
    const int x = 36014;
    std::cout << 36014;
    for (int i = 0; i < 36014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36014(int& val) {
    const int x = 36014;
    std::cout << 36014;
    for (int i = 0; i < 36014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36015(int& val) {
    func_in_36015(val);
    const int x = 36015;
    std::cout << 36015;
    for (int i = 0; i < 36015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36015(int& val) {
    const int x = 36015;
    std::cout << 36015;
    for (int i = 0; i < 36015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36016(int& val) {
    func_in_36016(val);
    const int x = 36016;
    std::cout << 36016;
    for (int i = 0; i < 36016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36016(int& val) {
    const int x = 36016;
    std::cout << 36016;
    for (int i = 0; i < 36016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36017(int& val) {
    func_in_36017(val);
    const int x = 36017;
    std::cout << 36017;
    for (int i = 0; i < 36017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36017(int& val) {
    const int x = 36017;
    std::cout << 36017;
    for (int i = 0; i < 36017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36018(int& val) {
    func_in_36018(val);
    const int x = 36018;
    std::cout << 36018;
    for (int i = 0; i < 36018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36018(int& val) {
    const int x = 36018;
    std::cout << 36018;
    for (int i = 0; i < 36018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36019(int& val) {
    func_in_36019(val);
    const int x = 36019;
    std::cout << 36019;
    for (int i = 0; i < 36019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36019(int& val) {
    const int x = 36019;
    std::cout << 36019;
    for (int i = 0; i < 36019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36020(int& val) {
    func_in_36020(val);
    const int x = 36020;
    std::cout << 36020;
    for (int i = 0; i < 36020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36020(int& val) {
    const int x = 36020;
    std::cout << 36020;
    for (int i = 0; i < 36020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36021(int& val) {
    func_in_36021(val);
    const int x = 36021;
    std::cout << 36021;
    for (int i = 0; i < 36021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36021(int& val) {
    const int x = 36021;
    std::cout << 36021;
    for (int i = 0; i < 36021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36022(int& val) {
    func_in_36022(val);
    const int x = 36022;
    std::cout << 36022;
    for (int i = 0; i < 36022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36022(int& val) {
    const int x = 36022;
    std::cout << 36022;
    for (int i = 0; i < 36022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36023(int& val) {
    func_in_36023(val);
    const int x = 36023;
    std::cout << 36023;
    for (int i = 0; i < 36023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36023(int& val) {
    const int x = 36023;
    std::cout << 36023;
    for (int i = 0; i < 36023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36024(int& val) {
    func_in_36024(val);
    const int x = 36024;
    std::cout << 36024;
    for (int i = 0; i < 36024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36024(int& val) {
    const int x = 36024;
    std::cout << 36024;
    for (int i = 0; i < 36024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36025(int& val) {
    func_in_36025(val);
    const int x = 36025;
    std::cout << 36025;
    for (int i = 0; i < 36025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36025(int& val) {
    const int x = 36025;
    std::cout << 36025;
    for (int i = 0; i < 36025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36026(int& val) {
    func_in_36026(val);
    const int x = 36026;
    std::cout << 36026;
    for (int i = 0; i < 36026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36026(int& val) {
    const int x = 36026;
    std::cout << 36026;
    for (int i = 0; i < 36026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36027(int& val) {
    func_in_36027(val);
    const int x = 36027;
    std::cout << 36027;
    for (int i = 0; i < 36027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36027(int& val) {
    const int x = 36027;
    std::cout << 36027;
    for (int i = 0; i < 36027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36028(int& val) {
    func_in_36028(val);
    const int x = 36028;
    std::cout << 36028;
    for (int i = 0; i < 36028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36028(int& val) {
    const int x = 36028;
    std::cout << 36028;
    for (int i = 0; i < 36028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36029(int& val) {
    func_in_36029(val);
    const int x = 36029;
    std::cout << 36029;
    for (int i = 0; i < 36029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36029(int& val) {
    const int x = 36029;
    std::cout << 36029;
    for (int i = 0; i < 36029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36030(int& val) {
    func_in_36030(val);
    const int x = 36030;
    std::cout << 36030;
    for (int i = 0; i < 36030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36030(int& val) {
    const int x = 36030;
    std::cout << 36030;
    for (int i = 0; i < 36030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36031(int& val) {
    func_in_36031(val);
    const int x = 36031;
    std::cout << 36031;
    for (int i = 0; i < 36031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36031(int& val) {
    const int x = 36031;
    std::cout << 36031;
    for (int i = 0; i < 36031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36032(int& val) {
    func_in_36032(val);
    const int x = 36032;
    std::cout << 36032;
    for (int i = 0; i < 36032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36032(int& val) {
    const int x = 36032;
    std::cout << 36032;
    for (int i = 0; i < 36032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36033(int& val) {
    func_in_36033(val);
    const int x = 36033;
    std::cout << 36033;
    for (int i = 0; i < 36033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36033(int& val) {
    const int x = 36033;
    std::cout << 36033;
    for (int i = 0; i < 36033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36034(int& val) {
    func_in_36034(val);
    const int x = 36034;
    std::cout << 36034;
    for (int i = 0; i < 36034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36034(int& val) {
    const int x = 36034;
    std::cout << 36034;
    for (int i = 0; i < 36034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36035(int& val) {
    func_in_36035(val);
    const int x = 36035;
    std::cout << 36035;
    for (int i = 0; i < 36035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36035(int& val) {
    const int x = 36035;
    std::cout << 36035;
    for (int i = 0; i < 36035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36036(int& val) {
    func_in_36036(val);
    const int x = 36036;
    std::cout << 36036;
    for (int i = 0; i < 36036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36036(int& val) {
    const int x = 36036;
    std::cout << 36036;
    for (int i = 0; i < 36036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36037(int& val) {
    func_in_36037(val);
    const int x = 36037;
    std::cout << 36037;
    for (int i = 0; i < 36037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36037(int& val) {
    const int x = 36037;
    std::cout << 36037;
    for (int i = 0; i < 36037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36038(int& val) {
    func_in_36038(val);
    const int x = 36038;
    std::cout << 36038;
    for (int i = 0; i < 36038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36038(int& val) {
    const int x = 36038;
    std::cout << 36038;
    for (int i = 0; i < 36038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36039(int& val) {
    func_in_36039(val);
    const int x = 36039;
    std::cout << 36039;
    for (int i = 0; i < 36039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36039(int& val) {
    const int x = 36039;
    std::cout << 36039;
    for (int i = 0; i < 36039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36040(int& val) {
    func_in_36040(val);
    const int x = 36040;
    std::cout << 36040;
    for (int i = 0; i < 36040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36040(int& val) {
    const int x = 36040;
    std::cout << 36040;
    for (int i = 0; i < 36040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36041(int& val) {
    func_in_36041(val);
    const int x = 36041;
    std::cout << 36041;
    for (int i = 0; i < 36041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36041(int& val) {
    const int x = 36041;
    std::cout << 36041;
    for (int i = 0; i < 36041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36042(int& val) {
    func_in_36042(val);
    const int x = 36042;
    std::cout << 36042;
    for (int i = 0; i < 36042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36042(int& val) {
    const int x = 36042;
    std::cout << 36042;
    for (int i = 0; i < 36042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36043(int& val) {
    func_in_36043(val);
    const int x = 36043;
    std::cout << 36043;
    for (int i = 0; i < 36043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36043(int& val) {
    const int x = 36043;
    std::cout << 36043;
    for (int i = 0; i < 36043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36044(int& val) {
    func_in_36044(val);
    const int x = 36044;
    std::cout << 36044;
    for (int i = 0; i < 36044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36044(int& val) {
    const int x = 36044;
    std::cout << 36044;
    for (int i = 0; i < 36044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36045(int& val) {
    func_in_36045(val);
    const int x = 36045;
    std::cout << 36045;
    for (int i = 0; i < 36045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36045(int& val) {
    const int x = 36045;
    std::cout << 36045;
    for (int i = 0; i < 36045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36046(int& val) {
    func_in_36046(val);
    const int x = 36046;
    std::cout << 36046;
    for (int i = 0; i < 36046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36046(int& val) {
    const int x = 36046;
    std::cout << 36046;
    for (int i = 0; i < 36046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36047(int& val) {
    func_in_36047(val);
    const int x = 36047;
    std::cout << 36047;
    for (int i = 0; i < 36047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36047(int& val) {
    const int x = 36047;
    std::cout << 36047;
    for (int i = 0; i < 36047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36048(int& val) {
    func_in_36048(val);
    const int x = 36048;
    std::cout << 36048;
    for (int i = 0; i < 36048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36048(int& val) {
    const int x = 36048;
    std::cout << 36048;
    for (int i = 0; i < 36048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36049(int& val) {
    func_in_36049(val);
    const int x = 36049;
    std::cout << 36049;
    for (int i = 0; i < 36049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36049(int& val) {
    const int x = 36049;
    std::cout << 36049;
    for (int i = 0; i < 36049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36050(int& val) {
    func_in_36050(val);
    const int x = 36050;
    std::cout << 36050;
    for (int i = 0; i < 36050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36050(int& val) {
    const int x = 36050;
    std::cout << 36050;
    for (int i = 0; i < 36050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36051(int& val) {
    func_in_36051(val);
    const int x = 36051;
    std::cout << 36051;
    for (int i = 0; i < 36051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36051(int& val) {
    const int x = 36051;
    std::cout << 36051;
    for (int i = 0; i < 36051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36052(int& val) {
    func_in_36052(val);
    const int x = 36052;
    std::cout << 36052;
    for (int i = 0; i < 36052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36052(int& val) {
    const int x = 36052;
    std::cout << 36052;
    for (int i = 0; i < 36052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36053(int& val) {
    func_in_36053(val);
    const int x = 36053;
    std::cout << 36053;
    for (int i = 0; i < 36053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36053(int& val) {
    const int x = 36053;
    std::cout << 36053;
    for (int i = 0; i < 36053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36054(int& val) {
    func_in_36054(val);
    const int x = 36054;
    std::cout << 36054;
    for (int i = 0; i < 36054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36054(int& val) {
    const int x = 36054;
    std::cout << 36054;
    for (int i = 0; i < 36054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36055(int& val) {
    func_in_36055(val);
    const int x = 36055;
    std::cout << 36055;
    for (int i = 0; i < 36055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36055(int& val) {
    const int x = 36055;
    std::cout << 36055;
    for (int i = 0; i < 36055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36056(int& val) {
    func_in_36056(val);
    const int x = 36056;
    std::cout << 36056;
    for (int i = 0; i < 36056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36056(int& val) {
    const int x = 36056;
    std::cout << 36056;
    for (int i = 0; i < 36056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36057(int& val) {
    func_in_36057(val);
    const int x = 36057;
    std::cout << 36057;
    for (int i = 0; i < 36057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36057(int& val) {
    const int x = 36057;
    std::cout << 36057;
    for (int i = 0; i < 36057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36058(int& val) {
    func_in_36058(val);
    const int x = 36058;
    std::cout << 36058;
    for (int i = 0; i < 36058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36058(int& val) {
    const int x = 36058;
    std::cout << 36058;
    for (int i = 0; i < 36058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36059(int& val) {
    func_in_36059(val);
    const int x = 36059;
    std::cout << 36059;
    for (int i = 0; i < 36059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36059(int& val) {
    const int x = 36059;
    std::cout << 36059;
    for (int i = 0; i < 36059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36060(int& val) {
    func_in_36060(val);
    const int x = 36060;
    std::cout << 36060;
    for (int i = 0; i < 36060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36060(int& val) {
    const int x = 36060;
    std::cout << 36060;
    for (int i = 0; i < 36060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36061(int& val) {
    func_in_36061(val);
    const int x = 36061;
    std::cout << 36061;
    for (int i = 0; i < 36061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36061(int& val) {
    const int x = 36061;
    std::cout << 36061;
    for (int i = 0; i < 36061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36062(int& val) {
    func_in_36062(val);
    const int x = 36062;
    std::cout << 36062;
    for (int i = 0; i < 36062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36062(int& val) {
    const int x = 36062;
    std::cout << 36062;
    for (int i = 0; i < 36062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36063(int& val) {
    func_in_36063(val);
    const int x = 36063;
    std::cout << 36063;
    for (int i = 0; i < 36063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36063(int& val) {
    const int x = 36063;
    std::cout << 36063;
    for (int i = 0; i < 36063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36064(int& val) {
    func_in_36064(val);
    const int x = 36064;
    std::cout << 36064;
    for (int i = 0; i < 36064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36064(int& val) {
    const int x = 36064;
    std::cout << 36064;
    for (int i = 0; i < 36064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36065(int& val) {
    func_in_36065(val);
    const int x = 36065;
    std::cout << 36065;
    for (int i = 0; i < 36065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36065(int& val) {
    const int x = 36065;
    std::cout << 36065;
    for (int i = 0; i < 36065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36066(int& val) {
    func_in_36066(val);
    const int x = 36066;
    std::cout << 36066;
    for (int i = 0; i < 36066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36066(int& val) {
    const int x = 36066;
    std::cout << 36066;
    for (int i = 0; i < 36066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36067(int& val) {
    func_in_36067(val);
    const int x = 36067;
    std::cout << 36067;
    for (int i = 0; i < 36067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36067(int& val) {
    const int x = 36067;
    std::cout << 36067;
    for (int i = 0; i < 36067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36068(int& val) {
    func_in_36068(val);
    const int x = 36068;
    std::cout << 36068;
    for (int i = 0; i < 36068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36068(int& val) {
    const int x = 36068;
    std::cout << 36068;
    for (int i = 0; i < 36068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36069(int& val) {
    func_in_36069(val);
    const int x = 36069;
    std::cout << 36069;
    for (int i = 0; i < 36069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36069(int& val) {
    const int x = 36069;
    std::cout << 36069;
    for (int i = 0; i < 36069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36070(int& val) {
    func_in_36070(val);
    const int x = 36070;
    std::cout << 36070;
    for (int i = 0; i < 36070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36070(int& val) {
    const int x = 36070;
    std::cout << 36070;
    for (int i = 0; i < 36070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36071(int& val) {
    func_in_36071(val);
    const int x = 36071;
    std::cout << 36071;
    for (int i = 0; i < 36071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36071(int& val) {
    const int x = 36071;
    std::cout << 36071;
    for (int i = 0; i < 36071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36072(int& val) {
    func_in_36072(val);
    const int x = 36072;
    std::cout << 36072;
    for (int i = 0; i < 36072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36072(int& val) {
    const int x = 36072;
    std::cout << 36072;
    for (int i = 0; i < 36072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36073(int& val) {
    func_in_36073(val);
    const int x = 36073;
    std::cout << 36073;
    for (int i = 0; i < 36073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36073(int& val) {
    const int x = 36073;
    std::cout << 36073;
    for (int i = 0; i < 36073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36074(int& val) {
    func_in_36074(val);
    const int x = 36074;
    std::cout << 36074;
    for (int i = 0; i < 36074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36074(int& val) {
    const int x = 36074;
    std::cout << 36074;
    for (int i = 0; i < 36074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36075(int& val) {
    func_in_36075(val);
    const int x = 36075;
    std::cout << 36075;
    for (int i = 0; i < 36075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36075(int& val) {
    const int x = 36075;
    std::cout << 36075;
    for (int i = 0; i < 36075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36076(int& val) {
    func_in_36076(val);
    const int x = 36076;
    std::cout << 36076;
    for (int i = 0; i < 36076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36076(int& val) {
    const int x = 36076;
    std::cout << 36076;
    for (int i = 0; i < 36076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36077(int& val) {
    func_in_36077(val);
    const int x = 36077;
    std::cout << 36077;
    for (int i = 0; i < 36077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36077(int& val) {
    const int x = 36077;
    std::cout << 36077;
    for (int i = 0; i < 36077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36078(int& val) {
    func_in_36078(val);
    const int x = 36078;
    std::cout << 36078;
    for (int i = 0; i < 36078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36078(int& val) {
    const int x = 36078;
    std::cout << 36078;
    for (int i = 0; i < 36078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36079(int& val) {
    func_in_36079(val);
    const int x = 36079;
    std::cout << 36079;
    for (int i = 0; i < 36079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36079(int& val) {
    const int x = 36079;
    std::cout << 36079;
    for (int i = 0; i < 36079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36080(int& val) {
    func_in_36080(val);
    const int x = 36080;
    std::cout << 36080;
    for (int i = 0; i < 36080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36080(int& val) {
    const int x = 36080;
    std::cout << 36080;
    for (int i = 0; i < 36080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36081(int& val) {
    func_in_36081(val);
    const int x = 36081;
    std::cout << 36081;
    for (int i = 0; i < 36081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36081(int& val) {
    const int x = 36081;
    std::cout << 36081;
    for (int i = 0; i < 36081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36082(int& val) {
    func_in_36082(val);
    const int x = 36082;
    std::cout << 36082;
    for (int i = 0; i < 36082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36082(int& val) {
    const int x = 36082;
    std::cout << 36082;
    for (int i = 0; i < 36082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36083(int& val) {
    func_in_36083(val);
    const int x = 36083;
    std::cout << 36083;
    for (int i = 0; i < 36083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36083(int& val) {
    const int x = 36083;
    std::cout << 36083;
    for (int i = 0; i < 36083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36084(int& val) {
    func_in_36084(val);
    const int x = 36084;
    std::cout << 36084;
    for (int i = 0; i < 36084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36084(int& val) {
    const int x = 36084;
    std::cout << 36084;
    for (int i = 0; i < 36084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36085(int& val) {
    func_in_36085(val);
    const int x = 36085;
    std::cout << 36085;
    for (int i = 0; i < 36085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36085(int& val) {
    const int x = 36085;
    std::cout << 36085;
    for (int i = 0; i < 36085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36086(int& val) {
    func_in_36086(val);
    const int x = 36086;
    std::cout << 36086;
    for (int i = 0; i < 36086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36086(int& val) {
    const int x = 36086;
    std::cout << 36086;
    for (int i = 0; i < 36086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36087(int& val) {
    func_in_36087(val);
    const int x = 36087;
    std::cout << 36087;
    for (int i = 0; i < 36087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36087(int& val) {
    const int x = 36087;
    std::cout << 36087;
    for (int i = 0; i < 36087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36088(int& val) {
    func_in_36088(val);
    const int x = 36088;
    std::cout << 36088;
    for (int i = 0; i < 36088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36088(int& val) {
    const int x = 36088;
    std::cout << 36088;
    for (int i = 0; i < 36088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36089(int& val) {
    func_in_36089(val);
    const int x = 36089;
    std::cout << 36089;
    for (int i = 0; i < 36089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36089(int& val) {
    const int x = 36089;
    std::cout << 36089;
    for (int i = 0; i < 36089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36090(int& val) {
    func_in_36090(val);
    const int x = 36090;
    std::cout << 36090;
    for (int i = 0; i < 36090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36090(int& val) {
    const int x = 36090;
    std::cout << 36090;
    for (int i = 0; i < 36090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36091(int& val) {
    func_in_36091(val);
    const int x = 36091;
    std::cout << 36091;
    for (int i = 0; i < 36091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36091(int& val) {
    const int x = 36091;
    std::cout << 36091;
    for (int i = 0; i < 36091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36092(int& val) {
    func_in_36092(val);
    const int x = 36092;
    std::cout << 36092;
    for (int i = 0; i < 36092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36092(int& val) {
    const int x = 36092;
    std::cout << 36092;
    for (int i = 0; i < 36092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36093(int& val) {
    func_in_36093(val);
    const int x = 36093;
    std::cout << 36093;
    for (int i = 0; i < 36093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36093(int& val) {
    const int x = 36093;
    std::cout << 36093;
    for (int i = 0; i < 36093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36094(int& val) {
    func_in_36094(val);
    const int x = 36094;
    std::cout << 36094;
    for (int i = 0; i < 36094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36094(int& val) {
    const int x = 36094;
    std::cout << 36094;
    for (int i = 0; i < 36094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36095(int& val) {
    func_in_36095(val);
    const int x = 36095;
    std::cout << 36095;
    for (int i = 0; i < 36095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36095(int& val) {
    const int x = 36095;
    std::cout << 36095;
    for (int i = 0; i < 36095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36096(int& val) {
    func_in_36096(val);
    const int x = 36096;
    std::cout << 36096;
    for (int i = 0; i < 36096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36096(int& val) {
    const int x = 36096;
    std::cout << 36096;
    for (int i = 0; i < 36096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36097(int& val) {
    func_in_36097(val);
    const int x = 36097;
    std::cout << 36097;
    for (int i = 0; i < 36097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36097(int& val) {
    const int x = 36097;
    std::cout << 36097;
    for (int i = 0; i < 36097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36098(int& val) {
    func_in_36098(val);
    const int x = 36098;
    std::cout << 36098;
    for (int i = 0; i < 36098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36098(int& val) {
    const int x = 36098;
    std::cout << 36098;
    for (int i = 0; i < 36098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36099(int& val) {
    func_in_36099(val);
    const int x = 36099;
    std::cout << 36099;
    for (int i = 0; i < 36099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36099(int& val) {
    const int x = 36099;
    std::cout << 36099;
    for (int i = 0; i < 36099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36100(int& val) {
    func_in_36100(val);
    const int x = 36100;
    std::cout << 36100;
    for (int i = 0; i < 36100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36100(int& val) {
    const int x = 36100;
    std::cout << 36100;
    for (int i = 0; i < 36100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36101(int& val) {
    func_in_36101(val);
    const int x = 36101;
    std::cout << 36101;
    for (int i = 0; i < 36101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36101(int& val) {
    const int x = 36101;
    std::cout << 36101;
    for (int i = 0; i < 36101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36102(int& val) {
    func_in_36102(val);
    const int x = 36102;
    std::cout << 36102;
    for (int i = 0; i < 36102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36102(int& val) {
    const int x = 36102;
    std::cout << 36102;
    for (int i = 0; i < 36102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36103(int& val) {
    func_in_36103(val);
    const int x = 36103;
    std::cout << 36103;
    for (int i = 0; i < 36103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36103(int& val) {
    const int x = 36103;
    std::cout << 36103;
    for (int i = 0; i < 36103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36104(int& val) {
    func_in_36104(val);
    const int x = 36104;
    std::cout << 36104;
    for (int i = 0; i < 36104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36104(int& val) {
    const int x = 36104;
    std::cout << 36104;
    for (int i = 0; i < 36104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36105(int& val) {
    func_in_36105(val);
    const int x = 36105;
    std::cout << 36105;
    for (int i = 0; i < 36105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36105(int& val) {
    const int x = 36105;
    std::cout << 36105;
    for (int i = 0; i < 36105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36106(int& val) {
    func_in_36106(val);
    const int x = 36106;
    std::cout << 36106;
    for (int i = 0; i < 36106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36106(int& val) {
    const int x = 36106;
    std::cout << 36106;
    for (int i = 0; i < 36106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36107(int& val) {
    func_in_36107(val);
    const int x = 36107;
    std::cout << 36107;
    for (int i = 0; i < 36107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36107(int& val) {
    const int x = 36107;
    std::cout << 36107;
    for (int i = 0; i < 36107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36108(int& val) {
    func_in_36108(val);
    const int x = 36108;
    std::cout << 36108;
    for (int i = 0; i < 36108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36108(int& val) {
    const int x = 36108;
    std::cout << 36108;
    for (int i = 0; i < 36108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36109(int& val) {
    func_in_36109(val);
    const int x = 36109;
    std::cout << 36109;
    for (int i = 0; i < 36109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36109(int& val) {
    const int x = 36109;
    std::cout << 36109;
    for (int i = 0; i < 36109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36110(int& val) {
    func_in_36110(val);
    const int x = 36110;
    std::cout << 36110;
    for (int i = 0; i < 36110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36110(int& val) {
    const int x = 36110;
    std::cout << 36110;
    for (int i = 0; i < 36110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36111(int& val) {
    func_in_36111(val);
    const int x = 36111;
    std::cout << 36111;
    for (int i = 0; i < 36111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36111(int& val) {
    const int x = 36111;
    std::cout << 36111;
    for (int i = 0; i < 36111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36112(int& val) {
    func_in_36112(val);
    const int x = 36112;
    std::cout << 36112;
    for (int i = 0; i < 36112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36112(int& val) {
    const int x = 36112;
    std::cout << 36112;
    for (int i = 0; i < 36112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36113(int& val) {
    func_in_36113(val);
    const int x = 36113;
    std::cout << 36113;
    for (int i = 0; i < 36113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36113(int& val) {
    const int x = 36113;
    std::cout << 36113;
    for (int i = 0; i < 36113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36114(int& val) {
    func_in_36114(val);
    const int x = 36114;
    std::cout << 36114;
    for (int i = 0; i < 36114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36114(int& val) {
    const int x = 36114;
    std::cout << 36114;
    for (int i = 0; i < 36114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36115(int& val) {
    func_in_36115(val);
    const int x = 36115;
    std::cout << 36115;
    for (int i = 0; i < 36115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36115(int& val) {
    const int x = 36115;
    std::cout << 36115;
    for (int i = 0; i < 36115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36116(int& val) {
    func_in_36116(val);
    const int x = 36116;
    std::cout << 36116;
    for (int i = 0; i < 36116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36116(int& val) {
    const int x = 36116;
    std::cout << 36116;
    for (int i = 0; i < 36116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36117(int& val) {
    func_in_36117(val);
    const int x = 36117;
    std::cout << 36117;
    for (int i = 0; i < 36117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36117(int& val) {
    const int x = 36117;
    std::cout << 36117;
    for (int i = 0; i < 36117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36118(int& val) {
    func_in_36118(val);
    const int x = 36118;
    std::cout << 36118;
    for (int i = 0; i < 36118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36118(int& val) {
    const int x = 36118;
    std::cout << 36118;
    for (int i = 0; i < 36118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36119(int& val) {
    func_in_36119(val);
    const int x = 36119;
    std::cout << 36119;
    for (int i = 0; i < 36119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36119(int& val) {
    const int x = 36119;
    std::cout << 36119;
    for (int i = 0; i < 36119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36120(int& val) {
    func_in_36120(val);
    const int x = 36120;
    std::cout << 36120;
    for (int i = 0; i < 36120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36120(int& val) {
    const int x = 36120;
    std::cout << 36120;
    for (int i = 0; i < 36120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36121(int& val) {
    func_in_36121(val);
    const int x = 36121;
    std::cout << 36121;
    for (int i = 0; i < 36121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36121(int& val) {
    const int x = 36121;
    std::cout << 36121;
    for (int i = 0; i < 36121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36122(int& val) {
    func_in_36122(val);
    const int x = 36122;
    std::cout << 36122;
    for (int i = 0; i < 36122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36122(int& val) {
    const int x = 36122;
    std::cout << 36122;
    for (int i = 0; i < 36122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36123(int& val) {
    func_in_36123(val);
    const int x = 36123;
    std::cout << 36123;
    for (int i = 0; i < 36123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36123(int& val) {
    const int x = 36123;
    std::cout << 36123;
    for (int i = 0; i < 36123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36124(int& val) {
    func_in_36124(val);
    const int x = 36124;
    std::cout << 36124;
    for (int i = 0; i < 36124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36124(int& val) {
    const int x = 36124;
    std::cout << 36124;
    for (int i = 0; i < 36124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36125(int& val) {
    func_in_36125(val);
    const int x = 36125;
    std::cout << 36125;
    for (int i = 0; i < 36125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36125(int& val) {
    const int x = 36125;
    std::cout << 36125;
    for (int i = 0; i < 36125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36126(int& val) {
    func_in_36126(val);
    const int x = 36126;
    std::cout << 36126;
    for (int i = 0; i < 36126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36126(int& val) {
    const int x = 36126;
    std::cout << 36126;
    for (int i = 0; i < 36126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36127(int& val) {
    func_in_36127(val);
    const int x = 36127;
    std::cout << 36127;
    for (int i = 0; i < 36127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36127(int& val) {
    const int x = 36127;
    std::cout << 36127;
    for (int i = 0; i < 36127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36128(int& val) {
    func_in_36128(val);
    const int x = 36128;
    std::cout << 36128;
    for (int i = 0; i < 36128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36128(int& val) {
    const int x = 36128;
    std::cout << 36128;
    for (int i = 0; i < 36128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36129(int& val) {
    func_in_36129(val);
    const int x = 36129;
    std::cout << 36129;
    for (int i = 0; i < 36129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36129(int& val) {
    const int x = 36129;
    std::cout << 36129;
    for (int i = 0; i < 36129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36130(int& val) {
    func_in_36130(val);
    const int x = 36130;
    std::cout << 36130;
    for (int i = 0; i < 36130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36130(int& val) {
    const int x = 36130;
    std::cout << 36130;
    for (int i = 0; i < 36130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36131(int& val) {
    func_in_36131(val);
    const int x = 36131;
    std::cout << 36131;
    for (int i = 0; i < 36131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36131(int& val) {
    const int x = 36131;
    std::cout << 36131;
    for (int i = 0; i < 36131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36132(int& val) {
    func_in_36132(val);
    const int x = 36132;
    std::cout << 36132;
    for (int i = 0; i < 36132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36132(int& val) {
    const int x = 36132;
    std::cout << 36132;
    for (int i = 0; i < 36132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36133(int& val) {
    func_in_36133(val);
    const int x = 36133;
    std::cout << 36133;
    for (int i = 0; i < 36133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36133(int& val) {
    const int x = 36133;
    std::cout << 36133;
    for (int i = 0; i < 36133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36134(int& val) {
    func_in_36134(val);
    const int x = 36134;
    std::cout << 36134;
    for (int i = 0; i < 36134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36134(int& val) {
    const int x = 36134;
    std::cout << 36134;
    for (int i = 0; i < 36134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36135(int& val) {
    func_in_36135(val);
    const int x = 36135;
    std::cout << 36135;
    for (int i = 0; i < 36135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36135(int& val) {
    const int x = 36135;
    std::cout << 36135;
    for (int i = 0; i < 36135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36136(int& val) {
    func_in_36136(val);
    const int x = 36136;
    std::cout << 36136;
    for (int i = 0; i < 36136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36136(int& val) {
    const int x = 36136;
    std::cout << 36136;
    for (int i = 0; i < 36136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36137(int& val) {
    func_in_36137(val);
    const int x = 36137;
    std::cout << 36137;
    for (int i = 0; i < 36137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36137(int& val) {
    const int x = 36137;
    std::cout << 36137;
    for (int i = 0; i < 36137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36138(int& val) {
    func_in_36138(val);
    const int x = 36138;
    std::cout << 36138;
    for (int i = 0; i < 36138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36138(int& val) {
    const int x = 36138;
    std::cout << 36138;
    for (int i = 0; i < 36138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36139(int& val) {
    func_in_36139(val);
    const int x = 36139;
    std::cout << 36139;
    for (int i = 0; i < 36139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36139(int& val) {
    const int x = 36139;
    std::cout << 36139;
    for (int i = 0; i < 36139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36140(int& val) {
    func_in_36140(val);
    const int x = 36140;
    std::cout << 36140;
    for (int i = 0; i < 36140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36140(int& val) {
    const int x = 36140;
    std::cout << 36140;
    for (int i = 0; i < 36140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36141(int& val) {
    func_in_36141(val);
    const int x = 36141;
    std::cout << 36141;
    for (int i = 0; i < 36141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36141(int& val) {
    const int x = 36141;
    std::cout << 36141;
    for (int i = 0; i < 36141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36142(int& val) {
    func_in_36142(val);
    const int x = 36142;
    std::cout << 36142;
    for (int i = 0; i < 36142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36142(int& val) {
    const int x = 36142;
    std::cout << 36142;
    for (int i = 0; i < 36142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36143(int& val) {
    func_in_36143(val);
    const int x = 36143;
    std::cout << 36143;
    for (int i = 0; i < 36143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36143(int& val) {
    const int x = 36143;
    std::cout << 36143;
    for (int i = 0; i < 36143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36144(int& val) {
    func_in_36144(val);
    const int x = 36144;
    std::cout << 36144;
    for (int i = 0; i < 36144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36144(int& val) {
    const int x = 36144;
    std::cout << 36144;
    for (int i = 0; i < 36144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36145(int& val) {
    func_in_36145(val);
    const int x = 36145;
    std::cout << 36145;
    for (int i = 0; i < 36145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36145(int& val) {
    const int x = 36145;
    std::cout << 36145;
    for (int i = 0; i < 36145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36146(int& val) {
    func_in_36146(val);
    const int x = 36146;
    std::cout << 36146;
    for (int i = 0; i < 36146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36146(int& val) {
    const int x = 36146;
    std::cout << 36146;
    for (int i = 0; i < 36146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36147(int& val) {
    func_in_36147(val);
    const int x = 36147;
    std::cout << 36147;
    for (int i = 0; i < 36147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36147(int& val) {
    const int x = 36147;
    std::cout << 36147;
    for (int i = 0; i < 36147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36148(int& val) {
    func_in_36148(val);
    const int x = 36148;
    std::cout << 36148;
    for (int i = 0; i < 36148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36148(int& val) {
    const int x = 36148;
    std::cout << 36148;
    for (int i = 0; i < 36148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36149(int& val) {
    func_in_36149(val);
    const int x = 36149;
    std::cout << 36149;
    for (int i = 0; i < 36149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36149(int& val) {
    const int x = 36149;
    std::cout << 36149;
    for (int i = 0; i < 36149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36150(int& val) {
    func_in_36150(val);
    const int x = 36150;
    std::cout << 36150;
    for (int i = 0; i < 36150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36150(int& val) {
    const int x = 36150;
    std::cout << 36150;
    for (int i = 0; i < 36150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36151(int& val) {
    func_in_36151(val);
    const int x = 36151;
    std::cout << 36151;
    for (int i = 0; i < 36151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36151(int& val) {
    const int x = 36151;
    std::cout << 36151;
    for (int i = 0; i < 36151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36152(int& val) {
    func_in_36152(val);
    const int x = 36152;
    std::cout << 36152;
    for (int i = 0; i < 36152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36152(int& val) {
    const int x = 36152;
    std::cout << 36152;
    for (int i = 0; i < 36152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36153(int& val) {
    func_in_36153(val);
    const int x = 36153;
    std::cout << 36153;
    for (int i = 0; i < 36153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36153(int& val) {
    const int x = 36153;
    std::cout << 36153;
    for (int i = 0; i < 36153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36154(int& val) {
    func_in_36154(val);
    const int x = 36154;
    std::cout << 36154;
    for (int i = 0; i < 36154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36154(int& val) {
    const int x = 36154;
    std::cout << 36154;
    for (int i = 0; i < 36154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36155(int& val) {
    func_in_36155(val);
    const int x = 36155;
    std::cout << 36155;
    for (int i = 0; i < 36155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36155(int& val) {
    const int x = 36155;
    std::cout << 36155;
    for (int i = 0; i < 36155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36156(int& val) {
    func_in_36156(val);
    const int x = 36156;
    std::cout << 36156;
    for (int i = 0; i < 36156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36156(int& val) {
    const int x = 36156;
    std::cout << 36156;
    for (int i = 0; i < 36156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36157(int& val) {
    func_in_36157(val);
    const int x = 36157;
    std::cout << 36157;
    for (int i = 0; i < 36157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36157(int& val) {
    const int x = 36157;
    std::cout << 36157;
    for (int i = 0; i < 36157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36158(int& val) {
    func_in_36158(val);
    const int x = 36158;
    std::cout << 36158;
    for (int i = 0; i < 36158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36158(int& val) {
    const int x = 36158;
    std::cout << 36158;
    for (int i = 0; i < 36158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36159(int& val) {
    func_in_36159(val);
    const int x = 36159;
    std::cout << 36159;
    for (int i = 0; i < 36159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36159(int& val) {
    const int x = 36159;
    std::cout << 36159;
    for (int i = 0; i < 36159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36160(int& val) {
    func_in_36160(val);
    const int x = 36160;
    std::cout << 36160;
    for (int i = 0; i < 36160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36160(int& val) {
    const int x = 36160;
    std::cout << 36160;
    for (int i = 0; i < 36160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36161(int& val) {
    func_in_36161(val);
    const int x = 36161;
    std::cout << 36161;
    for (int i = 0; i < 36161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36161(int& val) {
    const int x = 36161;
    std::cout << 36161;
    for (int i = 0; i < 36161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36162(int& val) {
    func_in_36162(val);
    const int x = 36162;
    std::cout << 36162;
    for (int i = 0; i < 36162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36162(int& val) {
    const int x = 36162;
    std::cout << 36162;
    for (int i = 0; i < 36162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36163(int& val) {
    func_in_36163(val);
    const int x = 36163;
    std::cout << 36163;
    for (int i = 0; i < 36163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36163(int& val) {
    const int x = 36163;
    std::cout << 36163;
    for (int i = 0; i < 36163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36164(int& val) {
    func_in_36164(val);
    const int x = 36164;
    std::cout << 36164;
    for (int i = 0; i < 36164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36164(int& val) {
    const int x = 36164;
    std::cout << 36164;
    for (int i = 0; i < 36164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36165(int& val) {
    func_in_36165(val);
    const int x = 36165;
    std::cout << 36165;
    for (int i = 0; i < 36165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36165(int& val) {
    const int x = 36165;
    std::cout << 36165;
    for (int i = 0; i < 36165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36166(int& val) {
    func_in_36166(val);
    const int x = 36166;
    std::cout << 36166;
    for (int i = 0; i < 36166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36166(int& val) {
    const int x = 36166;
    std::cout << 36166;
    for (int i = 0; i < 36166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36167(int& val) {
    func_in_36167(val);
    const int x = 36167;
    std::cout << 36167;
    for (int i = 0; i < 36167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36167(int& val) {
    const int x = 36167;
    std::cout << 36167;
    for (int i = 0; i < 36167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36168(int& val) {
    func_in_36168(val);
    const int x = 36168;
    std::cout << 36168;
    for (int i = 0; i < 36168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36168(int& val) {
    const int x = 36168;
    std::cout << 36168;
    for (int i = 0; i < 36168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36169(int& val) {
    func_in_36169(val);
    const int x = 36169;
    std::cout << 36169;
    for (int i = 0; i < 36169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36169(int& val) {
    const int x = 36169;
    std::cout << 36169;
    for (int i = 0; i < 36169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36170(int& val) {
    func_in_36170(val);
    const int x = 36170;
    std::cout << 36170;
    for (int i = 0; i < 36170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36170(int& val) {
    const int x = 36170;
    std::cout << 36170;
    for (int i = 0; i < 36170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36171(int& val) {
    func_in_36171(val);
    const int x = 36171;
    std::cout << 36171;
    for (int i = 0; i < 36171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36171(int& val) {
    const int x = 36171;
    std::cout << 36171;
    for (int i = 0; i < 36171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36172(int& val) {
    func_in_36172(val);
    const int x = 36172;
    std::cout << 36172;
    for (int i = 0; i < 36172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36172(int& val) {
    const int x = 36172;
    std::cout << 36172;
    for (int i = 0; i < 36172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36173(int& val) {
    func_in_36173(val);
    const int x = 36173;
    std::cout << 36173;
    for (int i = 0; i < 36173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36173(int& val) {
    const int x = 36173;
    std::cout << 36173;
    for (int i = 0; i < 36173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36174(int& val) {
    func_in_36174(val);
    const int x = 36174;
    std::cout << 36174;
    for (int i = 0; i < 36174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36174(int& val) {
    const int x = 36174;
    std::cout << 36174;
    for (int i = 0; i < 36174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36175(int& val) {
    func_in_36175(val);
    const int x = 36175;
    std::cout << 36175;
    for (int i = 0; i < 36175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36175(int& val) {
    const int x = 36175;
    std::cout << 36175;
    for (int i = 0; i < 36175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36176(int& val) {
    func_in_36176(val);
    const int x = 36176;
    std::cout << 36176;
    for (int i = 0; i < 36176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36176(int& val) {
    const int x = 36176;
    std::cout << 36176;
    for (int i = 0; i < 36176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36177(int& val) {
    func_in_36177(val);
    const int x = 36177;
    std::cout << 36177;
    for (int i = 0; i < 36177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36177(int& val) {
    const int x = 36177;
    std::cout << 36177;
    for (int i = 0; i < 36177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36178(int& val) {
    func_in_36178(val);
    const int x = 36178;
    std::cout << 36178;
    for (int i = 0; i < 36178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36178(int& val) {
    const int x = 36178;
    std::cout << 36178;
    for (int i = 0; i < 36178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36179(int& val) {
    func_in_36179(val);
    const int x = 36179;
    std::cout << 36179;
    for (int i = 0; i < 36179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36179(int& val) {
    const int x = 36179;
    std::cout << 36179;
    for (int i = 0; i < 36179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36180(int& val) {
    func_in_36180(val);
    const int x = 36180;
    std::cout << 36180;
    for (int i = 0; i < 36180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36180(int& val) {
    const int x = 36180;
    std::cout << 36180;
    for (int i = 0; i < 36180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36181(int& val) {
    func_in_36181(val);
    const int x = 36181;
    std::cout << 36181;
    for (int i = 0; i < 36181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36181(int& val) {
    const int x = 36181;
    std::cout << 36181;
    for (int i = 0; i < 36181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36182(int& val) {
    func_in_36182(val);
    const int x = 36182;
    std::cout << 36182;
    for (int i = 0; i < 36182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36182(int& val) {
    const int x = 36182;
    std::cout << 36182;
    for (int i = 0; i < 36182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36183(int& val) {
    func_in_36183(val);
    const int x = 36183;
    std::cout << 36183;
    for (int i = 0; i < 36183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36183(int& val) {
    const int x = 36183;
    std::cout << 36183;
    for (int i = 0; i < 36183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36184(int& val) {
    func_in_36184(val);
    const int x = 36184;
    std::cout << 36184;
    for (int i = 0; i < 36184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36184(int& val) {
    const int x = 36184;
    std::cout << 36184;
    for (int i = 0; i < 36184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36185(int& val) {
    func_in_36185(val);
    const int x = 36185;
    std::cout << 36185;
    for (int i = 0; i < 36185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36185(int& val) {
    const int x = 36185;
    std::cout << 36185;
    for (int i = 0; i < 36185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36186(int& val) {
    func_in_36186(val);
    const int x = 36186;
    std::cout << 36186;
    for (int i = 0; i < 36186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36186(int& val) {
    const int x = 36186;
    std::cout << 36186;
    for (int i = 0; i < 36186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36187(int& val) {
    func_in_36187(val);
    const int x = 36187;
    std::cout << 36187;
    for (int i = 0; i < 36187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36187(int& val) {
    const int x = 36187;
    std::cout << 36187;
    for (int i = 0; i < 36187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36188(int& val) {
    func_in_36188(val);
    const int x = 36188;
    std::cout << 36188;
    for (int i = 0; i < 36188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36188(int& val) {
    const int x = 36188;
    std::cout << 36188;
    for (int i = 0; i < 36188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36189(int& val) {
    func_in_36189(val);
    const int x = 36189;
    std::cout << 36189;
    for (int i = 0; i < 36189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36189(int& val) {
    const int x = 36189;
    std::cout << 36189;
    for (int i = 0; i < 36189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36190(int& val) {
    func_in_36190(val);
    const int x = 36190;
    std::cout << 36190;
    for (int i = 0; i < 36190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36190(int& val) {
    const int x = 36190;
    std::cout << 36190;
    for (int i = 0; i < 36190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36191(int& val) {
    func_in_36191(val);
    const int x = 36191;
    std::cout << 36191;
    for (int i = 0; i < 36191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36191(int& val) {
    const int x = 36191;
    std::cout << 36191;
    for (int i = 0; i < 36191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36192(int& val) {
    func_in_36192(val);
    const int x = 36192;
    std::cout << 36192;
    for (int i = 0; i < 36192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36192(int& val) {
    const int x = 36192;
    std::cout << 36192;
    for (int i = 0; i < 36192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36193(int& val) {
    func_in_36193(val);
    const int x = 36193;
    std::cout << 36193;
    for (int i = 0; i < 36193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36193(int& val) {
    const int x = 36193;
    std::cout << 36193;
    for (int i = 0; i < 36193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36194(int& val) {
    func_in_36194(val);
    const int x = 36194;
    std::cout << 36194;
    for (int i = 0; i < 36194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36194(int& val) {
    const int x = 36194;
    std::cout << 36194;
    for (int i = 0; i < 36194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36195(int& val) {
    func_in_36195(val);
    const int x = 36195;
    std::cout << 36195;
    for (int i = 0; i < 36195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36195(int& val) {
    const int x = 36195;
    std::cout << 36195;
    for (int i = 0; i < 36195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36196(int& val) {
    func_in_36196(val);
    const int x = 36196;
    std::cout << 36196;
    for (int i = 0; i < 36196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36196(int& val) {
    const int x = 36196;
    std::cout << 36196;
    for (int i = 0; i < 36196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36197(int& val) {
    func_in_36197(val);
    const int x = 36197;
    std::cout << 36197;
    for (int i = 0; i < 36197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36197(int& val) {
    const int x = 36197;
    std::cout << 36197;
    for (int i = 0; i < 36197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36198(int& val) {
    func_in_36198(val);
    const int x = 36198;
    std::cout << 36198;
    for (int i = 0; i < 36198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36198(int& val) {
    const int x = 36198;
    std::cout << 36198;
    for (int i = 0; i < 36198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36199(int& val) {
    func_in_36199(val);
    const int x = 36199;
    std::cout << 36199;
    for (int i = 0; i < 36199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36199(int& val) {
    const int x = 36199;
    std::cout << 36199;
    for (int i = 0; i < 36199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36200(int& val) {
    func_in_36200(val);
    const int x = 36200;
    std::cout << 36200;
    for (int i = 0; i < 36200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36200(int& val) {
    const int x = 36200;
    std::cout << 36200;
    for (int i = 0; i < 36200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36201(int& val) {
    func_in_36201(val);
    const int x = 36201;
    std::cout << 36201;
    for (int i = 0; i < 36201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36201(int& val) {
    const int x = 36201;
    std::cout << 36201;
    for (int i = 0; i < 36201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36202(int& val) {
    func_in_36202(val);
    const int x = 36202;
    std::cout << 36202;
    for (int i = 0; i < 36202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36202(int& val) {
    const int x = 36202;
    std::cout << 36202;
    for (int i = 0; i < 36202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36203(int& val) {
    func_in_36203(val);
    const int x = 36203;
    std::cout << 36203;
    for (int i = 0; i < 36203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36203(int& val) {
    const int x = 36203;
    std::cout << 36203;
    for (int i = 0; i < 36203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36204(int& val) {
    func_in_36204(val);
    const int x = 36204;
    std::cout << 36204;
    for (int i = 0; i < 36204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36204(int& val) {
    const int x = 36204;
    std::cout << 36204;
    for (int i = 0; i < 36204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36205(int& val) {
    func_in_36205(val);
    const int x = 36205;
    std::cout << 36205;
    for (int i = 0; i < 36205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36205(int& val) {
    const int x = 36205;
    std::cout << 36205;
    for (int i = 0; i < 36205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36206(int& val) {
    func_in_36206(val);
    const int x = 36206;
    std::cout << 36206;
    for (int i = 0; i < 36206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36206(int& val) {
    const int x = 36206;
    std::cout << 36206;
    for (int i = 0; i < 36206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36207(int& val) {
    func_in_36207(val);
    const int x = 36207;
    std::cout << 36207;
    for (int i = 0; i < 36207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36207(int& val) {
    const int x = 36207;
    std::cout << 36207;
    for (int i = 0; i < 36207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36208(int& val) {
    func_in_36208(val);
    const int x = 36208;
    std::cout << 36208;
    for (int i = 0; i < 36208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36208(int& val) {
    const int x = 36208;
    std::cout << 36208;
    for (int i = 0; i < 36208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36209(int& val) {
    func_in_36209(val);
    const int x = 36209;
    std::cout << 36209;
    for (int i = 0; i < 36209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36209(int& val) {
    const int x = 36209;
    std::cout << 36209;
    for (int i = 0; i < 36209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36210(int& val) {
    func_in_36210(val);
    const int x = 36210;
    std::cout << 36210;
    for (int i = 0; i < 36210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36210(int& val) {
    const int x = 36210;
    std::cout << 36210;
    for (int i = 0; i < 36210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36211(int& val) {
    func_in_36211(val);
    const int x = 36211;
    std::cout << 36211;
    for (int i = 0; i < 36211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36211(int& val) {
    const int x = 36211;
    std::cout << 36211;
    for (int i = 0; i < 36211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36212(int& val) {
    func_in_36212(val);
    const int x = 36212;
    std::cout << 36212;
    for (int i = 0; i < 36212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36212(int& val) {
    const int x = 36212;
    std::cout << 36212;
    for (int i = 0; i < 36212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36213(int& val) {
    func_in_36213(val);
    const int x = 36213;
    std::cout << 36213;
    for (int i = 0; i < 36213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36213(int& val) {
    const int x = 36213;
    std::cout << 36213;
    for (int i = 0; i < 36213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36214(int& val) {
    func_in_36214(val);
    const int x = 36214;
    std::cout << 36214;
    for (int i = 0; i < 36214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36214(int& val) {
    const int x = 36214;
    std::cout << 36214;
    for (int i = 0; i < 36214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36215(int& val) {
    func_in_36215(val);
    const int x = 36215;
    std::cout << 36215;
    for (int i = 0; i < 36215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36215(int& val) {
    const int x = 36215;
    std::cout << 36215;
    for (int i = 0; i < 36215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36216(int& val) {
    func_in_36216(val);
    const int x = 36216;
    std::cout << 36216;
    for (int i = 0; i < 36216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36216(int& val) {
    const int x = 36216;
    std::cout << 36216;
    for (int i = 0; i < 36216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36217(int& val) {
    func_in_36217(val);
    const int x = 36217;
    std::cout << 36217;
    for (int i = 0; i < 36217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36217(int& val) {
    const int x = 36217;
    std::cout << 36217;
    for (int i = 0; i < 36217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36218(int& val) {
    func_in_36218(val);
    const int x = 36218;
    std::cout << 36218;
    for (int i = 0; i < 36218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36218(int& val) {
    const int x = 36218;
    std::cout << 36218;
    for (int i = 0; i < 36218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36219(int& val) {
    func_in_36219(val);
    const int x = 36219;
    std::cout << 36219;
    for (int i = 0; i < 36219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36219(int& val) {
    const int x = 36219;
    std::cout << 36219;
    for (int i = 0; i < 36219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36220(int& val) {
    func_in_36220(val);
    const int x = 36220;
    std::cout << 36220;
    for (int i = 0; i < 36220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36220(int& val) {
    const int x = 36220;
    std::cout << 36220;
    for (int i = 0; i < 36220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36221(int& val) {
    func_in_36221(val);
    const int x = 36221;
    std::cout << 36221;
    for (int i = 0; i < 36221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36221(int& val) {
    const int x = 36221;
    std::cout << 36221;
    for (int i = 0; i < 36221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36222(int& val) {
    func_in_36222(val);
    const int x = 36222;
    std::cout << 36222;
    for (int i = 0; i < 36222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36222(int& val) {
    const int x = 36222;
    std::cout << 36222;
    for (int i = 0; i < 36222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36223(int& val) {
    func_in_36223(val);
    const int x = 36223;
    std::cout << 36223;
    for (int i = 0; i < 36223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36223(int& val) {
    const int x = 36223;
    std::cout << 36223;
    for (int i = 0; i < 36223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36224(int& val) {
    func_in_36224(val);
    const int x = 36224;
    std::cout << 36224;
    for (int i = 0; i < 36224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36224(int& val) {
    const int x = 36224;
    std::cout << 36224;
    for (int i = 0; i < 36224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36225(int& val) {
    func_in_36225(val);
    const int x = 36225;
    std::cout << 36225;
    for (int i = 0; i < 36225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36225(int& val) {
    const int x = 36225;
    std::cout << 36225;
    for (int i = 0; i < 36225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36226(int& val) {
    func_in_36226(val);
    const int x = 36226;
    std::cout << 36226;
    for (int i = 0; i < 36226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36226(int& val) {
    const int x = 36226;
    std::cout << 36226;
    for (int i = 0; i < 36226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36227(int& val) {
    func_in_36227(val);
    const int x = 36227;
    std::cout << 36227;
    for (int i = 0; i < 36227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36227(int& val) {
    const int x = 36227;
    std::cout << 36227;
    for (int i = 0; i < 36227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36228(int& val) {
    func_in_36228(val);
    const int x = 36228;
    std::cout << 36228;
    for (int i = 0; i < 36228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36228(int& val) {
    const int x = 36228;
    std::cout << 36228;
    for (int i = 0; i < 36228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36229(int& val) {
    func_in_36229(val);
    const int x = 36229;
    std::cout << 36229;
    for (int i = 0; i < 36229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36229(int& val) {
    const int x = 36229;
    std::cout << 36229;
    for (int i = 0; i < 36229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36230(int& val) {
    func_in_36230(val);
    const int x = 36230;
    std::cout << 36230;
    for (int i = 0; i < 36230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36230(int& val) {
    const int x = 36230;
    std::cout << 36230;
    for (int i = 0; i < 36230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36231(int& val) {
    func_in_36231(val);
    const int x = 36231;
    std::cout << 36231;
    for (int i = 0; i < 36231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36231(int& val) {
    const int x = 36231;
    std::cout << 36231;
    for (int i = 0; i < 36231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36232(int& val) {
    func_in_36232(val);
    const int x = 36232;
    std::cout << 36232;
    for (int i = 0; i < 36232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36232(int& val) {
    const int x = 36232;
    std::cout << 36232;
    for (int i = 0; i < 36232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36233(int& val) {
    func_in_36233(val);
    const int x = 36233;
    std::cout << 36233;
    for (int i = 0; i < 36233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36233(int& val) {
    const int x = 36233;
    std::cout << 36233;
    for (int i = 0; i < 36233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36234(int& val) {
    func_in_36234(val);
    const int x = 36234;
    std::cout << 36234;
    for (int i = 0; i < 36234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36234(int& val) {
    const int x = 36234;
    std::cout << 36234;
    for (int i = 0; i < 36234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36235(int& val) {
    func_in_36235(val);
    const int x = 36235;
    std::cout << 36235;
    for (int i = 0; i < 36235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36235(int& val) {
    const int x = 36235;
    std::cout << 36235;
    for (int i = 0; i < 36235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36236(int& val) {
    func_in_36236(val);
    const int x = 36236;
    std::cout << 36236;
    for (int i = 0; i < 36236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36236(int& val) {
    const int x = 36236;
    std::cout << 36236;
    for (int i = 0; i < 36236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36237(int& val) {
    func_in_36237(val);
    const int x = 36237;
    std::cout << 36237;
    for (int i = 0; i < 36237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36237(int& val) {
    const int x = 36237;
    std::cout << 36237;
    for (int i = 0; i < 36237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36238(int& val) {
    func_in_36238(val);
    const int x = 36238;
    std::cout << 36238;
    for (int i = 0; i < 36238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36238(int& val) {
    const int x = 36238;
    std::cout << 36238;
    for (int i = 0; i < 36238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36239(int& val) {
    func_in_36239(val);
    const int x = 36239;
    std::cout << 36239;
    for (int i = 0; i < 36239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36239(int& val) {
    const int x = 36239;
    std::cout << 36239;
    for (int i = 0; i < 36239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36240(int& val) {
    func_in_36240(val);
    const int x = 36240;
    std::cout << 36240;
    for (int i = 0; i < 36240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36240(int& val) {
    const int x = 36240;
    std::cout << 36240;
    for (int i = 0; i < 36240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36241(int& val) {
    func_in_36241(val);
    const int x = 36241;
    std::cout << 36241;
    for (int i = 0; i < 36241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36241(int& val) {
    const int x = 36241;
    std::cout << 36241;
    for (int i = 0; i < 36241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36242(int& val) {
    func_in_36242(val);
    const int x = 36242;
    std::cout << 36242;
    for (int i = 0; i < 36242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36242(int& val) {
    const int x = 36242;
    std::cout << 36242;
    for (int i = 0; i < 36242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36243(int& val) {
    func_in_36243(val);
    const int x = 36243;
    std::cout << 36243;
    for (int i = 0; i < 36243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36243(int& val) {
    const int x = 36243;
    std::cout << 36243;
    for (int i = 0; i < 36243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36244(int& val) {
    func_in_36244(val);
    const int x = 36244;
    std::cout << 36244;
    for (int i = 0; i < 36244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36244(int& val) {
    const int x = 36244;
    std::cout << 36244;
    for (int i = 0; i < 36244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36245(int& val) {
    func_in_36245(val);
    const int x = 36245;
    std::cout << 36245;
    for (int i = 0; i < 36245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36245(int& val) {
    const int x = 36245;
    std::cout << 36245;
    for (int i = 0; i < 36245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36246(int& val) {
    func_in_36246(val);
    const int x = 36246;
    std::cout << 36246;
    for (int i = 0; i < 36246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36246(int& val) {
    const int x = 36246;
    std::cout << 36246;
    for (int i = 0; i < 36246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36247(int& val) {
    func_in_36247(val);
    const int x = 36247;
    std::cout << 36247;
    for (int i = 0; i < 36247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36247(int& val) {
    const int x = 36247;
    std::cout << 36247;
    for (int i = 0; i < 36247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36248(int& val) {
    func_in_36248(val);
    const int x = 36248;
    std::cout << 36248;
    for (int i = 0; i < 36248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36248(int& val) {
    const int x = 36248;
    std::cout << 36248;
    for (int i = 0; i < 36248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36249(int& val) {
    func_in_36249(val);
    const int x = 36249;
    std::cout << 36249;
    for (int i = 0; i < 36249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36249(int& val) {
    const int x = 36249;
    std::cout << 36249;
    for (int i = 0; i < 36249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36250(int& val) {
    func_in_36250(val);
    const int x = 36250;
    std::cout << 36250;
    for (int i = 0; i < 36250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36250(int& val) {
    const int x = 36250;
    std::cout << 36250;
    for (int i = 0; i < 36250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36251(int& val) {
    func_in_36251(val);
    const int x = 36251;
    std::cout << 36251;
    for (int i = 0; i < 36251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36251(int& val) {
    const int x = 36251;
    std::cout << 36251;
    for (int i = 0; i < 36251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36252(int& val) {
    func_in_36252(val);
    const int x = 36252;
    std::cout << 36252;
    for (int i = 0; i < 36252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36252(int& val) {
    const int x = 36252;
    std::cout << 36252;
    for (int i = 0; i < 36252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36253(int& val) {
    func_in_36253(val);
    const int x = 36253;
    std::cout << 36253;
    for (int i = 0; i < 36253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36253(int& val) {
    const int x = 36253;
    std::cout << 36253;
    for (int i = 0; i < 36253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36254(int& val) {
    func_in_36254(val);
    const int x = 36254;
    std::cout << 36254;
    for (int i = 0; i < 36254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36254(int& val) {
    const int x = 36254;
    std::cout << 36254;
    for (int i = 0; i < 36254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36255(int& val) {
    func_in_36255(val);
    const int x = 36255;
    std::cout << 36255;
    for (int i = 0; i < 36255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36255(int& val) {
    const int x = 36255;
    std::cout << 36255;
    for (int i = 0; i < 36255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36256(int& val) {
    func_in_36256(val);
    const int x = 36256;
    std::cout << 36256;
    for (int i = 0; i < 36256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36256(int& val) {
    const int x = 36256;
    std::cout << 36256;
    for (int i = 0; i < 36256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36257(int& val) {
    func_in_36257(val);
    const int x = 36257;
    std::cout << 36257;
    for (int i = 0; i < 36257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36257(int& val) {
    const int x = 36257;
    std::cout << 36257;
    for (int i = 0; i < 36257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36258(int& val) {
    func_in_36258(val);
    const int x = 36258;
    std::cout << 36258;
    for (int i = 0; i < 36258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36258(int& val) {
    const int x = 36258;
    std::cout << 36258;
    for (int i = 0; i < 36258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36259(int& val) {
    func_in_36259(val);
    const int x = 36259;
    std::cout << 36259;
    for (int i = 0; i < 36259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36259(int& val) {
    const int x = 36259;
    std::cout << 36259;
    for (int i = 0; i < 36259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36260(int& val) {
    func_in_36260(val);
    const int x = 36260;
    std::cout << 36260;
    for (int i = 0; i < 36260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36260(int& val) {
    const int x = 36260;
    std::cout << 36260;
    for (int i = 0; i < 36260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36261(int& val) {
    func_in_36261(val);
    const int x = 36261;
    std::cout << 36261;
    for (int i = 0; i < 36261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36261(int& val) {
    const int x = 36261;
    std::cout << 36261;
    for (int i = 0; i < 36261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36262(int& val) {
    func_in_36262(val);
    const int x = 36262;
    std::cout << 36262;
    for (int i = 0; i < 36262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36262(int& val) {
    const int x = 36262;
    std::cout << 36262;
    for (int i = 0; i < 36262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36263(int& val) {
    func_in_36263(val);
    const int x = 36263;
    std::cout << 36263;
    for (int i = 0; i < 36263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36263(int& val) {
    const int x = 36263;
    std::cout << 36263;
    for (int i = 0; i < 36263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36264(int& val) {
    func_in_36264(val);
    const int x = 36264;
    std::cout << 36264;
    for (int i = 0; i < 36264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36264(int& val) {
    const int x = 36264;
    std::cout << 36264;
    for (int i = 0; i < 36264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36265(int& val) {
    func_in_36265(val);
    const int x = 36265;
    std::cout << 36265;
    for (int i = 0; i < 36265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36265(int& val) {
    const int x = 36265;
    std::cout << 36265;
    for (int i = 0; i < 36265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36266(int& val) {
    func_in_36266(val);
    const int x = 36266;
    std::cout << 36266;
    for (int i = 0; i < 36266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36266(int& val) {
    const int x = 36266;
    std::cout << 36266;
    for (int i = 0; i < 36266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36267(int& val) {
    func_in_36267(val);
    const int x = 36267;
    std::cout << 36267;
    for (int i = 0; i < 36267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36267(int& val) {
    const int x = 36267;
    std::cout << 36267;
    for (int i = 0; i < 36267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36268(int& val) {
    func_in_36268(val);
    const int x = 36268;
    std::cout << 36268;
    for (int i = 0; i < 36268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36268(int& val) {
    const int x = 36268;
    std::cout << 36268;
    for (int i = 0; i < 36268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36269(int& val) {
    func_in_36269(val);
    const int x = 36269;
    std::cout << 36269;
    for (int i = 0; i < 36269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36269(int& val) {
    const int x = 36269;
    std::cout << 36269;
    for (int i = 0; i < 36269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36270(int& val) {
    func_in_36270(val);
    const int x = 36270;
    std::cout << 36270;
    for (int i = 0; i < 36270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36270(int& val) {
    const int x = 36270;
    std::cout << 36270;
    for (int i = 0; i < 36270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36271(int& val) {
    func_in_36271(val);
    const int x = 36271;
    std::cout << 36271;
    for (int i = 0; i < 36271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36271(int& val) {
    const int x = 36271;
    std::cout << 36271;
    for (int i = 0; i < 36271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36272(int& val) {
    func_in_36272(val);
    const int x = 36272;
    std::cout << 36272;
    for (int i = 0; i < 36272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36272(int& val) {
    const int x = 36272;
    std::cout << 36272;
    for (int i = 0; i < 36272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36273(int& val) {
    func_in_36273(val);
    const int x = 36273;
    std::cout << 36273;
    for (int i = 0; i < 36273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36273(int& val) {
    const int x = 36273;
    std::cout << 36273;
    for (int i = 0; i < 36273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36274(int& val) {
    func_in_36274(val);
    const int x = 36274;
    std::cout << 36274;
    for (int i = 0; i < 36274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36274(int& val) {
    const int x = 36274;
    std::cout << 36274;
    for (int i = 0; i < 36274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36275(int& val) {
    func_in_36275(val);
    const int x = 36275;
    std::cout << 36275;
    for (int i = 0; i < 36275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36275(int& val) {
    const int x = 36275;
    std::cout << 36275;
    for (int i = 0; i < 36275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36276(int& val) {
    func_in_36276(val);
    const int x = 36276;
    std::cout << 36276;
    for (int i = 0; i < 36276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36276(int& val) {
    const int x = 36276;
    std::cout << 36276;
    for (int i = 0; i < 36276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36277(int& val) {
    func_in_36277(val);
    const int x = 36277;
    std::cout << 36277;
    for (int i = 0; i < 36277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36277(int& val) {
    const int x = 36277;
    std::cout << 36277;
    for (int i = 0; i < 36277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36278(int& val) {
    func_in_36278(val);
    const int x = 36278;
    std::cout << 36278;
    for (int i = 0; i < 36278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36278(int& val) {
    const int x = 36278;
    std::cout << 36278;
    for (int i = 0; i < 36278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36279(int& val) {
    func_in_36279(val);
    const int x = 36279;
    std::cout << 36279;
    for (int i = 0; i < 36279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36279(int& val) {
    const int x = 36279;
    std::cout << 36279;
    for (int i = 0; i < 36279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36280(int& val) {
    func_in_36280(val);
    const int x = 36280;
    std::cout << 36280;
    for (int i = 0; i < 36280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36280(int& val) {
    const int x = 36280;
    std::cout << 36280;
    for (int i = 0; i < 36280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36281(int& val) {
    func_in_36281(val);
    const int x = 36281;
    std::cout << 36281;
    for (int i = 0; i < 36281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36281(int& val) {
    const int x = 36281;
    std::cout << 36281;
    for (int i = 0; i < 36281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36282(int& val) {
    func_in_36282(val);
    const int x = 36282;
    std::cout << 36282;
    for (int i = 0; i < 36282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36282(int& val) {
    const int x = 36282;
    std::cout << 36282;
    for (int i = 0; i < 36282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36283(int& val) {
    func_in_36283(val);
    const int x = 36283;
    std::cout << 36283;
    for (int i = 0; i < 36283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36283(int& val) {
    const int x = 36283;
    std::cout << 36283;
    for (int i = 0; i < 36283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36284(int& val) {
    func_in_36284(val);
    const int x = 36284;
    std::cout << 36284;
    for (int i = 0; i < 36284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36284(int& val) {
    const int x = 36284;
    std::cout << 36284;
    for (int i = 0; i < 36284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36285(int& val) {
    func_in_36285(val);
    const int x = 36285;
    std::cout << 36285;
    for (int i = 0; i < 36285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36285(int& val) {
    const int x = 36285;
    std::cout << 36285;
    for (int i = 0; i < 36285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36286(int& val) {
    func_in_36286(val);
    const int x = 36286;
    std::cout << 36286;
    for (int i = 0; i < 36286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36286(int& val) {
    const int x = 36286;
    std::cout << 36286;
    for (int i = 0; i < 36286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36287(int& val) {
    func_in_36287(val);
    const int x = 36287;
    std::cout << 36287;
    for (int i = 0; i < 36287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36287(int& val) {
    const int x = 36287;
    std::cout << 36287;
    for (int i = 0; i < 36287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36288(int& val) {
    func_in_36288(val);
    const int x = 36288;
    std::cout << 36288;
    for (int i = 0; i < 36288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36288(int& val) {
    const int x = 36288;
    std::cout << 36288;
    for (int i = 0; i < 36288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36289(int& val) {
    func_in_36289(val);
    const int x = 36289;
    std::cout << 36289;
    for (int i = 0; i < 36289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36289(int& val) {
    const int x = 36289;
    std::cout << 36289;
    for (int i = 0; i < 36289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36290(int& val) {
    func_in_36290(val);
    const int x = 36290;
    std::cout << 36290;
    for (int i = 0; i < 36290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36290(int& val) {
    const int x = 36290;
    std::cout << 36290;
    for (int i = 0; i < 36290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36291(int& val) {
    func_in_36291(val);
    const int x = 36291;
    std::cout << 36291;
    for (int i = 0; i < 36291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36291(int& val) {
    const int x = 36291;
    std::cout << 36291;
    for (int i = 0; i < 36291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36292(int& val) {
    func_in_36292(val);
    const int x = 36292;
    std::cout << 36292;
    for (int i = 0; i < 36292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36292(int& val) {
    const int x = 36292;
    std::cout << 36292;
    for (int i = 0; i < 36292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36293(int& val) {
    func_in_36293(val);
    const int x = 36293;
    std::cout << 36293;
    for (int i = 0; i < 36293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36293(int& val) {
    const int x = 36293;
    std::cout << 36293;
    for (int i = 0; i < 36293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36294(int& val) {
    func_in_36294(val);
    const int x = 36294;
    std::cout << 36294;
    for (int i = 0; i < 36294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36294(int& val) {
    const int x = 36294;
    std::cout << 36294;
    for (int i = 0; i < 36294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36295(int& val) {
    func_in_36295(val);
    const int x = 36295;
    std::cout << 36295;
    for (int i = 0; i < 36295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36295(int& val) {
    const int x = 36295;
    std::cout << 36295;
    for (int i = 0; i < 36295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36296(int& val) {
    func_in_36296(val);
    const int x = 36296;
    std::cout << 36296;
    for (int i = 0; i < 36296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36296(int& val) {
    const int x = 36296;
    std::cout << 36296;
    for (int i = 0; i < 36296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36297(int& val) {
    func_in_36297(val);
    const int x = 36297;
    std::cout << 36297;
    for (int i = 0; i < 36297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36297(int& val) {
    const int x = 36297;
    std::cout << 36297;
    for (int i = 0; i < 36297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36298(int& val) {
    func_in_36298(val);
    const int x = 36298;
    std::cout << 36298;
    for (int i = 0; i < 36298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36298(int& val) {
    const int x = 36298;
    std::cout << 36298;
    for (int i = 0; i < 36298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36299(int& val) {
    func_in_36299(val);
    const int x = 36299;
    std::cout << 36299;
    for (int i = 0; i < 36299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36299(int& val) {
    const int x = 36299;
    std::cout << 36299;
    for (int i = 0; i < 36299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36300(int& val) {
    func_in_36300(val);
    const int x = 36300;
    std::cout << 36300;
    for (int i = 0; i < 36300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36300(int& val) {
    const int x = 36300;
    std::cout << 36300;
    for (int i = 0; i < 36300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36301(int& val) {
    func_in_36301(val);
    const int x = 36301;
    std::cout << 36301;
    for (int i = 0; i < 36301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36301(int& val) {
    const int x = 36301;
    std::cout << 36301;
    for (int i = 0; i < 36301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36302(int& val) {
    func_in_36302(val);
    const int x = 36302;
    std::cout << 36302;
    for (int i = 0; i < 36302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36302(int& val) {
    const int x = 36302;
    std::cout << 36302;
    for (int i = 0; i < 36302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36303(int& val) {
    func_in_36303(val);
    const int x = 36303;
    std::cout << 36303;
    for (int i = 0; i < 36303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36303(int& val) {
    const int x = 36303;
    std::cout << 36303;
    for (int i = 0; i < 36303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36304(int& val) {
    func_in_36304(val);
    const int x = 36304;
    std::cout << 36304;
    for (int i = 0; i < 36304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36304(int& val) {
    const int x = 36304;
    std::cout << 36304;
    for (int i = 0; i < 36304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36305(int& val) {
    func_in_36305(val);
    const int x = 36305;
    std::cout << 36305;
    for (int i = 0; i < 36305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36305(int& val) {
    const int x = 36305;
    std::cout << 36305;
    for (int i = 0; i < 36305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36306(int& val) {
    func_in_36306(val);
    const int x = 36306;
    std::cout << 36306;
    for (int i = 0; i < 36306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36306(int& val) {
    const int x = 36306;
    std::cout << 36306;
    for (int i = 0; i < 36306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36307(int& val) {
    func_in_36307(val);
    const int x = 36307;
    std::cout << 36307;
    for (int i = 0; i < 36307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36307(int& val) {
    const int x = 36307;
    std::cout << 36307;
    for (int i = 0; i < 36307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36308(int& val) {
    func_in_36308(val);
    const int x = 36308;
    std::cout << 36308;
    for (int i = 0; i < 36308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36308(int& val) {
    const int x = 36308;
    std::cout << 36308;
    for (int i = 0; i < 36308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36309(int& val) {
    func_in_36309(val);
    const int x = 36309;
    std::cout << 36309;
    for (int i = 0; i < 36309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36309(int& val) {
    const int x = 36309;
    std::cout << 36309;
    for (int i = 0; i < 36309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36310(int& val) {
    func_in_36310(val);
    const int x = 36310;
    std::cout << 36310;
    for (int i = 0; i < 36310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36310(int& val) {
    const int x = 36310;
    std::cout << 36310;
    for (int i = 0; i < 36310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36311(int& val) {
    func_in_36311(val);
    const int x = 36311;
    std::cout << 36311;
    for (int i = 0; i < 36311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36311(int& val) {
    const int x = 36311;
    std::cout << 36311;
    for (int i = 0; i < 36311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36312(int& val) {
    func_in_36312(val);
    const int x = 36312;
    std::cout << 36312;
    for (int i = 0; i < 36312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36312(int& val) {
    const int x = 36312;
    std::cout << 36312;
    for (int i = 0; i < 36312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36313(int& val) {
    func_in_36313(val);
    const int x = 36313;
    std::cout << 36313;
    for (int i = 0; i < 36313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36313(int& val) {
    const int x = 36313;
    std::cout << 36313;
    for (int i = 0; i < 36313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36314(int& val) {
    func_in_36314(val);
    const int x = 36314;
    std::cout << 36314;
    for (int i = 0; i < 36314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36314(int& val) {
    const int x = 36314;
    std::cout << 36314;
    for (int i = 0; i < 36314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36315(int& val) {
    func_in_36315(val);
    const int x = 36315;
    std::cout << 36315;
    for (int i = 0; i < 36315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36315(int& val) {
    const int x = 36315;
    std::cout << 36315;
    for (int i = 0; i < 36315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36316(int& val) {
    func_in_36316(val);
    const int x = 36316;
    std::cout << 36316;
    for (int i = 0; i < 36316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36316(int& val) {
    const int x = 36316;
    std::cout << 36316;
    for (int i = 0; i < 36316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36317(int& val) {
    func_in_36317(val);
    const int x = 36317;
    std::cout << 36317;
    for (int i = 0; i < 36317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36317(int& val) {
    const int x = 36317;
    std::cout << 36317;
    for (int i = 0; i < 36317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36318(int& val) {
    func_in_36318(val);
    const int x = 36318;
    std::cout << 36318;
    for (int i = 0; i < 36318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36318(int& val) {
    const int x = 36318;
    std::cout << 36318;
    for (int i = 0; i < 36318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36319(int& val) {
    func_in_36319(val);
    const int x = 36319;
    std::cout << 36319;
    for (int i = 0; i < 36319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36319(int& val) {
    const int x = 36319;
    std::cout << 36319;
    for (int i = 0; i < 36319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36320(int& val) {
    func_in_36320(val);
    const int x = 36320;
    std::cout << 36320;
    for (int i = 0; i < 36320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36320(int& val) {
    const int x = 36320;
    std::cout << 36320;
    for (int i = 0; i < 36320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36321(int& val) {
    func_in_36321(val);
    const int x = 36321;
    std::cout << 36321;
    for (int i = 0; i < 36321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36321(int& val) {
    const int x = 36321;
    std::cout << 36321;
    for (int i = 0; i < 36321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36322(int& val) {
    func_in_36322(val);
    const int x = 36322;
    std::cout << 36322;
    for (int i = 0; i < 36322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36322(int& val) {
    const int x = 36322;
    std::cout << 36322;
    for (int i = 0; i < 36322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36323(int& val) {
    func_in_36323(val);
    const int x = 36323;
    std::cout << 36323;
    for (int i = 0; i < 36323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36323(int& val) {
    const int x = 36323;
    std::cout << 36323;
    for (int i = 0; i < 36323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36324(int& val) {
    func_in_36324(val);
    const int x = 36324;
    std::cout << 36324;
    for (int i = 0; i < 36324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36324(int& val) {
    const int x = 36324;
    std::cout << 36324;
    for (int i = 0; i < 36324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36325(int& val) {
    func_in_36325(val);
    const int x = 36325;
    std::cout << 36325;
    for (int i = 0; i < 36325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36325(int& val) {
    const int x = 36325;
    std::cout << 36325;
    for (int i = 0; i < 36325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36326(int& val) {
    func_in_36326(val);
    const int x = 36326;
    std::cout << 36326;
    for (int i = 0; i < 36326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36326(int& val) {
    const int x = 36326;
    std::cout << 36326;
    for (int i = 0; i < 36326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36327(int& val) {
    func_in_36327(val);
    const int x = 36327;
    std::cout << 36327;
    for (int i = 0; i < 36327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36327(int& val) {
    const int x = 36327;
    std::cout << 36327;
    for (int i = 0; i < 36327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36328(int& val) {
    func_in_36328(val);
    const int x = 36328;
    std::cout << 36328;
    for (int i = 0; i < 36328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36328(int& val) {
    const int x = 36328;
    std::cout << 36328;
    for (int i = 0; i < 36328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36329(int& val) {
    func_in_36329(val);
    const int x = 36329;
    std::cout << 36329;
    for (int i = 0; i < 36329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36329(int& val) {
    const int x = 36329;
    std::cout << 36329;
    for (int i = 0; i < 36329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36330(int& val) {
    func_in_36330(val);
    const int x = 36330;
    std::cout << 36330;
    for (int i = 0; i < 36330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36330(int& val) {
    const int x = 36330;
    std::cout << 36330;
    for (int i = 0; i < 36330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36331(int& val) {
    func_in_36331(val);
    const int x = 36331;
    std::cout << 36331;
    for (int i = 0; i < 36331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36331(int& val) {
    const int x = 36331;
    std::cout << 36331;
    for (int i = 0; i < 36331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36332(int& val) {
    func_in_36332(val);
    const int x = 36332;
    std::cout << 36332;
    for (int i = 0; i < 36332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36332(int& val) {
    const int x = 36332;
    std::cout << 36332;
    for (int i = 0; i < 36332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36333(int& val) {
    func_in_36333(val);
    const int x = 36333;
    std::cout << 36333;
    for (int i = 0; i < 36333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36333(int& val) {
    const int x = 36333;
    std::cout << 36333;
    for (int i = 0; i < 36333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36334(int& val) {
    func_in_36334(val);
    const int x = 36334;
    std::cout << 36334;
    for (int i = 0; i < 36334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36334(int& val) {
    const int x = 36334;
    std::cout << 36334;
    for (int i = 0; i < 36334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36335(int& val) {
    func_in_36335(val);
    const int x = 36335;
    std::cout << 36335;
    for (int i = 0; i < 36335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36335(int& val) {
    const int x = 36335;
    std::cout << 36335;
    for (int i = 0; i < 36335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36336(int& val) {
    func_in_36336(val);
    const int x = 36336;
    std::cout << 36336;
    for (int i = 0; i < 36336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36336(int& val) {
    const int x = 36336;
    std::cout << 36336;
    for (int i = 0; i < 36336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36337(int& val) {
    func_in_36337(val);
    const int x = 36337;
    std::cout << 36337;
    for (int i = 0; i < 36337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36337(int& val) {
    const int x = 36337;
    std::cout << 36337;
    for (int i = 0; i < 36337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36338(int& val) {
    func_in_36338(val);
    const int x = 36338;
    std::cout << 36338;
    for (int i = 0; i < 36338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36338(int& val) {
    const int x = 36338;
    std::cout << 36338;
    for (int i = 0; i < 36338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36339(int& val) {
    func_in_36339(val);
    const int x = 36339;
    std::cout << 36339;
    for (int i = 0; i < 36339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36339(int& val) {
    const int x = 36339;
    std::cout << 36339;
    for (int i = 0; i < 36339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36340(int& val) {
    func_in_36340(val);
    const int x = 36340;
    std::cout << 36340;
    for (int i = 0; i < 36340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36340(int& val) {
    const int x = 36340;
    std::cout << 36340;
    for (int i = 0; i < 36340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36341(int& val) {
    func_in_36341(val);
    const int x = 36341;
    std::cout << 36341;
    for (int i = 0; i < 36341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36341(int& val) {
    const int x = 36341;
    std::cout << 36341;
    for (int i = 0; i < 36341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36342(int& val) {
    func_in_36342(val);
    const int x = 36342;
    std::cout << 36342;
    for (int i = 0; i < 36342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36342(int& val) {
    const int x = 36342;
    std::cout << 36342;
    for (int i = 0; i < 36342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36343(int& val) {
    func_in_36343(val);
    const int x = 36343;
    std::cout << 36343;
    for (int i = 0; i < 36343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36343(int& val) {
    const int x = 36343;
    std::cout << 36343;
    for (int i = 0; i < 36343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36344(int& val) {
    func_in_36344(val);
    const int x = 36344;
    std::cout << 36344;
    for (int i = 0; i < 36344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36344(int& val) {
    const int x = 36344;
    std::cout << 36344;
    for (int i = 0; i < 36344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36345(int& val) {
    func_in_36345(val);
    const int x = 36345;
    std::cout << 36345;
    for (int i = 0; i < 36345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36345(int& val) {
    const int x = 36345;
    std::cout << 36345;
    for (int i = 0; i < 36345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36346(int& val) {
    func_in_36346(val);
    const int x = 36346;
    std::cout << 36346;
    for (int i = 0; i < 36346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36346(int& val) {
    const int x = 36346;
    std::cout << 36346;
    for (int i = 0; i < 36346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36347(int& val) {
    func_in_36347(val);
    const int x = 36347;
    std::cout << 36347;
    for (int i = 0; i < 36347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36347(int& val) {
    const int x = 36347;
    std::cout << 36347;
    for (int i = 0; i < 36347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36348(int& val) {
    func_in_36348(val);
    const int x = 36348;
    std::cout << 36348;
    for (int i = 0; i < 36348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36348(int& val) {
    const int x = 36348;
    std::cout << 36348;
    for (int i = 0; i < 36348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36349(int& val) {
    func_in_36349(val);
    const int x = 36349;
    std::cout << 36349;
    for (int i = 0; i < 36349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36349(int& val) {
    const int x = 36349;
    std::cout << 36349;
    for (int i = 0; i < 36349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36350(int& val) {
    func_in_36350(val);
    const int x = 36350;
    std::cout << 36350;
    for (int i = 0; i < 36350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36350(int& val) {
    const int x = 36350;
    std::cout << 36350;
    for (int i = 0; i < 36350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36351(int& val) {
    func_in_36351(val);
    const int x = 36351;
    std::cout << 36351;
    for (int i = 0; i < 36351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36351(int& val) {
    const int x = 36351;
    std::cout << 36351;
    for (int i = 0; i < 36351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36352(int& val) {
    func_in_36352(val);
    const int x = 36352;
    std::cout << 36352;
    for (int i = 0; i < 36352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36352(int& val) {
    const int x = 36352;
    std::cout << 36352;
    for (int i = 0; i < 36352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36353(int& val) {
    func_in_36353(val);
    const int x = 36353;
    std::cout << 36353;
    for (int i = 0; i < 36353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36353(int& val) {
    const int x = 36353;
    std::cout << 36353;
    for (int i = 0; i < 36353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36354(int& val) {
    func_in_36354(val);
    const int x = 36354;
    std::cout << 36354;
    for (int i = 0; i < 36354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36354(int& val) {
    const int x = 36354;
    std::cout << 36354;
    for (int i = 0; i < 36354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36355(int& val) {
    func_in_36355(val);
    const int x = 36355;
    std::cout << 36355;
    for (int i = 0; i < 36355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36355(int& val) {
    const int x = 36355;
    std::cout << 36355;
    for (int i = 0; i < 36355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36356(int& val) {
    func_in_36356(val);
    const int x = 36356;
    std::cout << 36356;
    for (int i = 0; i < 36356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36356(int& val) {
    const int x = 36356;
    std::cout << 36356;
    for (int i = 0; i < 36356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36357(int& val) {
    func_in_36357(val);
    const int x = 36357;
    std::cout << 36357;
    for (int i = 0; i < 36357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36357(int& val) {
    const int x = 36357;
    std::cout << 36357;
    for (int i = 0; i < 36357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36358(int& val) {
    func_in_36358(val);
    const int x = 36358;
    std::cout << 36358;
    for (int i = 0; i < 36358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36358(int& val) {
    const int x = 36358;
    std::cout << 36358;
    for (int i = 0; i < 36358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36359(int& val) {
    func_in_36359(val);
    const int x = 36359;
    std::cout << 36359;
    for (int i = 0; i < 36359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36359(int& val) {
    const int x = 36359;
    std::cout << 36359;
    for (int i = 0; i < 36359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36360(int& val) {
    func_in_36360(val);
    const int x = 36360;
    std::cout << 36360;
    for (int i = 0; i < 36360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36360(int& val) {
    const int x = 36360;
    std::cout << 36360;
    for (int i = 0; i < 36360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36361(int& val) {
    func_in_36361(val);
    const int x = 36361;
    std::cout << 36361;
    for (int i = 0; i < 36361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36361(int& val) {
    const int x = 36361;
    std::cout << 36361;
    for (int i = 0; i < 36361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36362(int& val) {
    func_in_36362(val);
    const int x = 36362;
    std::cout << 36362;
    for (int i = 0; i < 36362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36362(int& val) {
    const int x = 36362;
    std::cout << 36362;
    for (int i = 0; i < 36362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36363(int& val) {
    func_in_36363(val);
    const int x = 36363;
    std::cout << 36363;
    for (int i = 0; i < 36363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36363(int& val) {
    const int x = 36363;
    std::cout << 36363;
    for (int i = 0; i < 36363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36364(int& val) {
    func_in_36364(val);
    const int x = 36364;
    std::cout << 36364;
    for (int i = 0; i < 36364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36364(int& val) {
    const int x = 36364;
    std::cout << 36364;
    for (int i = 0; i < 36364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36365(int& val) {
    func_in_36365(val);
    const int x = 36365;
    std::cout << 36365;
    for (int i = 0; i < 36365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36365(int& val) {
    const int x = 36365;
    std::cout << 36365;
    for (int i = 0; i < 36365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36366(int& val) {
    func_in_36366(val);
    const int x = 36366;
    std::cout << 36366;
    for (int i = 0; i < 36366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36366(int& val) {
    const int x = 36366;
    std::cout << 36366;
    for (int i = 0; i < 36366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36367(int& val) {
    func_in_36367(val);
    const int x = 36367;
    std::cout << 36367;
    for (int i = 0; i < 36367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36367(int& val) {
    const int x = 36367;
    std::cout << 36367;
    for (int i = 0; i < 36367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36368(int& val) {
    func_in_36368(val);
    const int x = 36368;
    std::cout << 36368;
    for (int i = 0; i < 36368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36368(int& val) {
    const int x = 36368;
    std::cout << 36368;
    for (int i = 0; i < 36368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36369(int& val) {
    func_in_36369(val);
    const int x = 36369;
    std::cout << 36369;
    for (int i = 0; i < 36369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36369(int& val) {
    const int x = 36369;
    std::cout << 36369;
    for (int i = 0; i < 36369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36370(int& val) {
    func_in_36370(val);
    const int x = 36370;
    std::cout << 36370;
    for (int i = 0; i < 36370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36370(int& val) {
    const int x = 36370;
    std::cout << 36370;
    for (int i = 0; i < 36370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36371(int& val) {
    func_in_36371(val);
    const int x = 36371;
    std::cout << 36371;
    for (int i = 0; i < 36371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36371(int& val) {
    const int x = 36371;
    std::cout << 36371;
    for (int i = 0; i < 36371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36372(int& val) {
    func_in_36372(val);
    const int x = 36372;
    std::cout << 36372;
    for (int i = 0; i < 36372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36372(int& val) {
    const int x = 36372;
    std::cout << 36372;
    for (int i = 0; i < 36372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36373(int& val) {
    func_in_36373(val);
    const int x = 36373;
    std::cout << 36373;
    for (int i = 0; i < 36373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36373(int& val) {
    const int x = 36373;
    std::cout << 36373;
    for (int i = 0; i < 36373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36374(int& val) {
    func_in_36374(val);
    const int x = 36374;
    std::cout << 36374;
    for (int i = 0; i < 36374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36374(int& val) {
    const int x = 36374;
    std::cout << 36374;
    for (int i = 0; i < 36374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36375(int& val) {
    func_in_36375(val);
    const int x = 36375;
    std::cout << 36375;
    for (int i = 0; i < 36375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36375(int& val) {
    const int x = 36375;
    std::cout << 36375;
    for (int i = 0; i < 36375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36376(int& val) {
    func_in_36376(val);
    const int x = 36376;
    std::cout << 36376;
    for (int i = 0; i < 36376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36376(int& val) {
    const int x = 36376;
    std::cout << 36376;
    for (int i = 0; i < 36376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36377(int& val) {
    func_in_36377(val);
    const int x = 36377;
    std::cout << 36377;
    for (int i = 0; i < 36377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36377(int& val) {
    const int x = 36377;
    std::cout << 36377;
    for (int i = 0; i < 36377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36378(int& val) {
    func_in_36378(val);
    const int x = 36378;
    std::cout << 36378;
    for (int i = 0; i < 36378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36378(int& val) {
    const int x = 36378;
    std::cout << 36378;
    for (int i = 0; i < 36378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36379(int& val) {
    func_in_36379(val);
    const int x = 36379;
    std::cout << 36379;
    for (int i = 0; i < 36379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36379(int& val) {
    const int x = 36379;
    std::cout << 36379;
    for (int i = 0; i < 36379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36380(int& val) {
    func_in_36380(val);
    const int x = 36380;
    std::cout << 36380;
    for (int i = 0; i < 36380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36380(int& val) {
    const int x = 36380;
    std::cout << 36380;
    for (int i = 0; i < 36380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36381(int& val) {
    func_in_36381(val);
    const int x = 36381;
    std::cout << 36381;
    for (int i = 0; i < 36381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36381(int& val) {
    const int x = 36381;
    std::cout << 36381;
    for (int i = 0; i < 36381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36382(int& val) {
    func_in_36382(val);
    const int x = 36382;
    std::cout << 36382;
    for (int i = 0; i < 36382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36382(int& val) {
    const int x = 36382;
    std::cout << 36382;
    for (int i = 0; i < 36382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36383(int& val) {
    func_in_36383(val);
    const int x = 36383;
    std::cout << 36383;
    for (int i = 0; i < 36383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36383(int& val) {
    const int x = 36383;
    std::cout << 36383;
    for (int i = 0; i < 36383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36384(int& val) {
    func_in_36384(val);
    const int x = 36384;
    std::cout << 36384;
    for (int i = 0; i < 36384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36384(int& val) {
    const int x = 36384;
    std::cout << 36384;
    for (int i = 0; i < 36384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36385(int& val) {
    func_in_36385(val);
    const int x = 36385;
    std::cout << 36385;
    for (int i = 0; i < 36385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36385(int& val) {
    const int x = 36385;
    std::cout << 36385;
    for (int i = 0; i < 36385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36386(int& val) {
    func_in_36386(val);
    const int x = 36386;
    std::cout << 36386;
    for (int i = 0; i < 36386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36386(int& val) {
    const int x = 36386;
    std::cout << 36386;
    for (int i = 0; i < 36386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36387(int& val) {
    func_in_36387(val);
    const int x = 36387;
    std::cout << 36387;
    for (int i = 0; i < 36387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36387(int& val) {
    const int x = 36387;
    std::cout << 36387;
    for (int i = 0; i < 36387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36388(int& val) {
    func_in_36388(val);
    const int x = 36388;
    std::cout << 36388;
    for (int i = 0; i < 36388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36388(int& val) {
    const int x = 36388;
    std::cout << 36388;
    for (int i = 0; i < 36388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36389(int& val) {
    func_in_36389(val);
    const int x = 36389;
    std::cout << 36389;
    for (int i = 0; i < 36389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36389(int& val) {
    const int x = 36389;
    std::cout << 36389;
    for (int i = 0; i < 36389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36390(int& val) {
    func_in_36390(val);
    const int x = 36390;
    std::cout << 36390;
    for (int i = 0; i < 36390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36390(int& val) {
    const int x = 36390;
    std::cout << 36390;
    for (int i = 0; i < 36390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36391(int& val) {
    func_in_36391(val);
    const int x = 36391;
    std::cout << 36391;
    for (int i = 0; i < 36391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36391(int& val) {
    const int x = 36391;
    std::cout << 36391;
    for (int i = 0; i < 36391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36392(int& val) {
    func_in_36392(val);
    const int x = 36392;
    std::cout << 36392;
    for (int i = 0; i < 36392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36392(int& val) {
    const int x = 36392;
    std::cout << 36392;
    for (int i = 0; i < 36392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36393(int& val) {
    func_in_36393(val);
    const int x = 36393;
    std::cout << 36393;
    for (int i = 0; i < 36393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36393(int& val) {
    const int x = 36393;
    std::cout << 36393;
    for (int i = 0; i < 36393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36394(int& val) {
    func_in_36394(val);
    const int x = 36394;
    std::cout << 36394;
    for (int i = 0; i < 36394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36394(int& val) {
    const int x = 36394;
    std::cout << 36394;
    for (int i = 0; i < 36394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36395(int& val) {
    func_in_36395(val);
    const int x = 36395;
    std::cout << 36395;
    for (int i = 0; i < 36395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36395(int& val) {
    const int x = 36395;
    std::cout << 36395;
    for (int i = 0; i < 36395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36396(int& val) {
    func_in_36396(val);
    const int x = 36396;
    std::cout << 36396;
    for (int i = 0; i < 36396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36396(int& val) {
    const int x = 36396;
    std::cout << 36396;
    for (int i = 0; i < 36396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36397(int& val) {
    func_in_36397(val);
    const int x = 36397;
    std::cout << 36397;
    for (int i = 0; i < 36397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36397(int& val) {
    const int x = 36397;
    std::cout << 36397;
    for (int i = 0; i < 36397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36398(int& val) {
    func_in_36398(val);
    const int x = 36398;
    std::cout << 36398;
    for (int i = 0; i < 36398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36398(int& val) {
    const int x = 36398;
    std::cout << 36398;
    for (int i = 0; i < 36398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36399(int& val) {
    func_in_36399(val);
    const int x = 36399;
    std::cout << 36399;
    for (int i = 0; i < 36399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36399(int& val) {
    const int x = 36399;
    std::cout << 36399;
    for (int i = 0; i < 36399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36400(int& val) {
    func_in_36400(val);
    const int x = 36400;
    std::cout << 36400;
    for (int i = 0; i < 36400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36400(int& val) {
    const int x = 36400;
    std::cout << 36400;
    for (int i = 0; i < 36400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36401(int& val) {
    func_in_36401(val);
    const int x = 36401;
    std::cout << 36401;
    for (int i = 0; i < 36401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36401(int& val) {
    const int x = 36401;
    std::cout << 36401;
    for (int i = 0; i < 36401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36402(int& val) {
    func_in_36402(val);
    const int x = 36402;
    std::cout << 36402;
    for (int i = 0; i < 36402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36402(int& val) {
    const int x = 36402;
    std::cout << 36402;
    for (int i = 0; i < 36402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36403(int& val) {
    func_in_36403(val);
    const int x = 36403;
    std::cout << 36403;
    for (int i = 0; i < 36403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36403(int& val) {
    const int x = 36403;
    std::cout << 36403;
    for (int i = 0; i < 36403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36404(int& val) {
    func_in_36404(val);
    const int x = 36404;
    std::cout << 36404;
    for (int i = 0; i < 36404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36404(int& val) {
    const int x = 36404;
    std::cout << 36404;
    for (int i = 0; i < 36404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36405(int& val) {
    func_in_36405(val);
    const int x = 36405;
    std::cout << 36405;
    for (int i = 0; i < 36405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36405(int& val) {
    const int x = 36405;
    std::cout << 36405;
    for (int i = 0; i < 36405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36406(int& val) {
    func_in_36406(val);
    const int x = 36406;
    std::cout << 36406;
    for (int i = 0; i < 36406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36406(int& val) {
    const int x = 36406;
    std::cout << 36406;
    for (int i = 0; i < 36406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36407(int& val) {
    func_in_36407(val);
    const int x = 36407;
    std::cout << 36407;
    for (int i = 0; i < 36407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36407(int& val) {
    const int x = 36407;
    std::cout << 36407;
    for (int i = 0; i < 36407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36408(int& val) {
    func_in_36408(val);
    const int x = 36408;
    std::cout << 36408;
    for (int i = 0; i < 36408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36408(int& val) {
    const int x = 36408;
    std::cout << 36408;
    for (int i = 0; i < 36408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36409(int& val) {
    func_in_36409(val);
    const int x = 36409;
    std::cout << 36409;
    for (int i = 0; i < 36409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36409(int& val) {
    const int x = 36409;
    std::cout << 36409;
    for (int i = 0; i < 36409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36410(int& val) {
    func_in_36410(val);
    const int x = 36410;
    std::cout << 36410;
    for (int i = 0; i < 36410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36410(int& val) {
    const int x = 36410;
    std::cout << 36410;
    for (int i = 0; i < 36410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36411(int& val) {
    func_in_36411(val);
    const int x = 36411;
    std::cout << 36411;
    for (int i = 0; i < 36411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36411(int& val) {
    const int x = 36411;
    std::cout << 36411;
    for (int i = 0; i < 36411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36412(int& val) {
    func_in_36412(val);
    const int x = 36412;
    std::cout << 36412;
    for (int i = 0; i < 36412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36412(int& val) {
    const int x = 36412;
    std::cout << 36412;
    for (int i = 0; i < 36412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36413(int& val) {
    func_in_36413(val);
    const int x = 36413;
    std::cout << 36413;
    for (int i = 0; i < 36413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36413(int& val) {
    const int x = 36413;
    std::cout << 36413;
    for (int i = 0; i < 36413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36414(int& val) {
    func_in_36414(val);
    const int x = 36414;
    std::cout << 36414;
    for (int i = 0; i < 36414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36414(int& val) {
    const int x = 36414;
    std::cout << 36414;
    for (int i = 0; i < 36414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36415(int& val) {
    func_in_36415(val);
    const int x = 36415;
    std::cout << 36415;
    for (int i = 0; i < 36415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36415(int& val) {
    const int x = 36415;
    std::cout << 36415;
    for (int i = 0; i < 36415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36416(int& val) {
    func_in_36416(val);
    const int x = 36416;
    std::cout << 36416;
    for (int i = 0; i < 36416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36416(int& val) {
    const int x = 36416;
    std::cout << 36416;
    for (int i = 0; i < 36416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36417(int& val) {
    func_in_36417(val);
    const int x = 36417;
    std::cout << 36417;
    for (int i = 0; i < 36417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36417(int& val) {
    const int x = 36417;
    std::cout << 36417;
    for (int i = 0; i < 36417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36418(int& val) {
    func_in_36418(val);
    const int x = 36418;
    std::cout << 36418;
    for (int i = 0; i < 36418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36418(int& val) {
    const int x = 36418;
    std::cout << 36418;
    for (int i = 0; i < 36418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36419(int& val) {
    func_in_36419(val);
    const int x = 36419;
    std::cout << 36419;
    for (int i = 0; i < 36419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36419(int& val) {
    const int x = 36419;
    std::cout << 36419;
    for (int i = 0; i < 36419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36420(int& val) {
    func_in_36420(val);
    const int x = 36420;
    std::cout << 36420;
    for (int i = 0; i < 36420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36420(int& val) {
    const int x = 36420;
    std::cout << 36420;
    for (int i = 0; i < 36420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36421(int& val) {
    func_in_36421(val);
    const int x = 36421;
    std::cout << 36421;
    for (int i = 0; i < 36421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36421(int& val) {
    const int x = 36421;
    std::cout << 36421;
    for (int i = 0; i < 36421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36422(int& val) {
    func_in_36422(val);
    const int x = 36422;
    std::cout << 36422;
    for (int i = 0; i < 36422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36422(int& val) {
    const int x = 36422;
    std::cout << 36422;
    for (int i = 0; i < 36422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36423(int& val) {
    func_in_36423(val);
    const int x = 36423;
    std::cout << 36423;
    for (int i = 0; i < 36423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36423(int& val) {
    const int x = 36423;
    std::cout << 36423;
    for (int i = 0; i < 36423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36424(int& val) {
    func_in_36424(val);
    const int x = 36424;
    std::cout << 36424;
    for (int i = 0; i < 36424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36424(int& val) {
    const int x = 36424;
    std::cout << 36424;
    for (int i = 0; i < 36424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36425(int& val) {
    func_in_36425(val);
    const int x = 36425;
    std::cout << 36425;
    for (int i = 0; i < 36425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36425(int& val) {
    const int x = 36425;
    std::cout << 36425;
    for (int i = 0; i < 36425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36426(int& val) {
    func_in_36426(val);
    const int x = 36426;
    std::cout << 36426;
    for (int i = 0; i < 36426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36426(int& val) {
    const int x = 36426;
    std::cout << 36426;
    for (int i = 0; i < 36426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36427(int& val) {
    func_in_36427(val);
    const int x = 36427;
    std::cout << 36427;
    for (int i = 0; i < 36427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36427(int& val) {
    const int x = 36427;
    std::cout << 36427;
    for (int i = 0; i < 36427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36428(int& val) {
    func_in_36428(val);
    const int x = 36428;
    std::cout << 36428;
    for (int i = 0; i < 36428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36428(int& val) {
    const int x = 36428;
    std::cout << 36428;
    for (int i = 0; i < 36428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36429(int& val) {
    func_in_36429(val);
    const int x = 36429;
    std::cout << 36429;
    for (int i = 0; i < 36429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36429(int& val) {
    const int x = 36429;
    std::cout << 36429;
    for (int i = 0; i < 36429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36430(int& val) {
    func_in_36430(val);
    const int x = 36430;
    std::cout << 36430;
    for (int i = 0; i < 36430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36430(int& val) {
    const int x = 36430;
    std::cout << 36430;
    for (int i = 0; i < 36430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36431(int& val) {
    func_in_36431(val);
    const int x = 36431;
    std::cout << 36431;
    for (int i = 0; i < 36431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36431(int& val) {
    const int x = 36431;
    std::cout << 36431;
    for (int i = 0; i < 36431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36432(int& val) {
    func_in_36432(val);
    const int x = 36432;
    std::cout << 36432;
    for (int i = 0; i < 36432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36432(int& val) {
    const int x = 36432;
    std::cout << 36432;
    for (int i = 0; i < 36432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36433(int& val) {
    func_in_36433(val);
    const int x = 36433;
    std::cout << 36433;
    for (int i = 0; i < 36433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36433(int& val) {
    const int x = 36433;
    std::cout << 36433;
    for (int i = 0; i < 36433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36434(int& val) {
    func_in_36434(val);
    const int x = 36434;
    std::cout << 36434;
    for (int i = 0; i < 36434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36434(int& val) {
    const int x = 36434;
    std::cout << 36434;
    for (int i = 0; i < 36434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36435(int& val) {
    func_in_36435(val);
    const int x = 36435;
    std::cout << 36435;
    for (int i = 0; i < 36435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36435(int& val) {
    const int x = 36435;
    std::cout << 36435;
    for (int i = 0; i < 36435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36436(int& val) {
    func_in_36436(val);
    const int x = 36436;
    std::cout << 36436;
    for (int i = 0; i < 36436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36436(int& val) {
    const int x = 36436;
    std::cout << 36436;
    for (int i = 0; i < 36436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36437(int& val) {
    func_in_36437(val);
    const int x = 36437;
    std::cout << 36437;
    for (int i = 0; i < 36437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36437(int& val) {
    const int x = 36437;
    std::cout << 36437;
    for (int i = 0; i < 36437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36438(int& val) {
    func_in_36438(val);
    const int x = 36438;
    std::cout << 36438;
    for (int i = 0; i < 36438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36438(int& val) {
    const int x = 36438;
    std::cout << 36438;
    for (int i = 0; i < 36438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36439(int& val) {
    func_in_36439(val);
    const int x = 36439;
    std::cout << 36439;
    for (int i = 0; i < 36439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36439(int& val) {
    const int x = 36439;
    std::cout << 36439;
    for (int i = 0; i < 36439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36440(int& val) {
    func_in_36440(val);
    const int x = 36440;
    std::cout << 36440;
    for (int i = 0; i < 36440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36440(int& val) {
    const int x = 36440;
    std::cout << 36440;
    for (int i = 0; i < 36440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36441(int& val) {
    func_in_36441(val);
    const int x = 36441;
    std::cout << 36441;
    for (int i = 0; i < 36441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36441(int& val) {
    const int x = 36441;
    std::cout << 36441;
    for (int i = 0; i < 36441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36442(int& val) {
    func_in_36442(val);
    const int x = 36442;
    std::cout << 36442;
    for (int i = 0; i < 36442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36442(int& val) {
    const int x = 36442;
    std::cout << 36442;
    for (int i = 0; i < 36442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36443(int& val) {
    func_in_36443(val);
    const int x = 36443;
    std::cout << 36443;
    for (int i = 0; i < 36443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36443(int& val) {
    const int x = 36443;
    std::cout << 36443;
    for (int i = 0; i < 36443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36444(int& val) {
    func_in_36444(val);
    const int x = 36444;
    std::cout << 36444;
    for (int i = 0; i < 36444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36444(int& val) {
    const int x = 36444;
    std::cout << 36444;
    for (int i = 0; i < 36444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36445(int& val) {
    func_in_36445(val);
    const int x = 36445;
    std::cout << 36445;
    for (int i = 0; i < 36445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36445(int& val) {
    const int x = 36445;
    std::cout << 36445;
    for (int i = 0; i < 36445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36446(int& val) {
    func_in_36446(val);
    const int x = 36446;
    std::cout << 36446;
    for (int i = 0; i < 36446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36446(int& val) {
    const int x = 36446;
    std::cout << 36446;
    for (int i = 0; i < 36446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36447(int& val) {
    func_in_36447(val);
    const int x = 36447;
    std::cout << 36447;
    for (int i = 0; i < 36447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36447(int& val) {
    const int x = 36447;
    std::cout << 36447;
    for (int i = 0; i < 36447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36448(int& val) {
    func_in_36448(val);
    const int x = 36448;
    std::cout << 36448;
    for (int i = 0; i < 36448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36448(int& val) {
    const int x = 36448;
    std::cout << 36448;
    for (int i = 0; i < 36448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36449(int& val) {
    func_in_36449(val);
    const int x = 36449;
    std::cout << 36449;
    for (int i = 0; i < 36449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36449(int& val) {
    const int x = 36449;
    std::cout << 36449;
    for (int i = 0; i < 36449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36450(int& val) {
    func_in_36450(val);
    const int x = 36450;
    std::cout << 36450;
    for (int i = 0; i < 36450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36450(int& val) {
    const int x = 36450;
    std::cout << 36450;
    for (int i = 0; i < 36450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36451(int& val) {
    func_in_36451(val);
    const int x = 36451;
    std::cout << 36451;
    for (int i = 0; i < 36451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36451(int& val) {
    const int x = 36451;
    std::cout << 36451;
    for (int i = 0; i < 36451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36452(int& val) {
    func_in_36452(val);
    const int x = 36452;
    std::cout << 36452;
    for (int i = 0; i < 36452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36452(int& val) {
    const int x = 36452;
    std::cout << 36452;
    for (int i = 0; i < 36452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36453(int& val) {
    func_in_36453(val);
    const int x = 36453;
    std::cout << 36453;
    for (int i = 0; i < 36453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36453(int& val) {
    const int x = 36453;
    std::cout << 36453;
    for (int i = 0; i < 36453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36454(int& val) {
    func_in_36454(val);
    const int x = 36454;
    std::cout << 36454;
    for (int i = 0; i < 36454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36454(int& val) {
    const int x = 36454;
    std::cout << 36454;
    for (int i = 0; i < 36454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36455(int& val) {
    func_in_36455(val);
    const int x = 36455;
    std::cout << 36455;
    for (int i = 0; i < 36455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36455(int& val) {
    const int x = 36455;
    std::cout << 36455;
    for (int i = 0; i < 36455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36456(int& val) {
    func_in_36456(val);
    const int x = 36456;
    std::cout << 36456;
    for (int i = 0; i < 36456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36456(int& val) {
    const int x = 36456;
    std::cout << 36456;
    for (int i = 0; i < 36456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36457(int& val) {
    func_in_36457(val);
    const int x = 36457;
    std::cout << 36457;
    for (int i = 0; i < 36457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36457(int& val) {
    const int x = 36457;
    std::cout << 36457;
    for (int i = 0; i < 36457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36458(int& val) {
    func_in_36458(val);
    const int x = 36458;
    std::cout << 36458;
    for (int i = 0; i < 36458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36458(int& val) {
    const int x = 36458;
    std::cout << 36458;
    for (int i = 0; i < 36458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36459(int& val) {
    func_in_36459(val);
    const int x = 36459;
    std::cout << 36459;
    for (int i = 0; i < 36459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36459(int& val) {
    const int x = 36459;
    std::cout << 36459;
    for (int i = 0; i < 36459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36460(int& val) {
    func_in_36460(val);
    const int x = 36460;
    std::cout << 36460;
    for (int i = 0; i < 36460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36460(int& val) {
    const int x = 36460;
    std::cout << 36460;
    for (int i = 0; i < 36460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36461(int& val) {
    func_in_36461(val);
    const int x = 36461;
    std::cout << 36461;
    for (int i = 0; i < 36461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36461(int& val) {
    const int x = 36461;
    std::cout << 36461;
    for (int i = 0; i < 36461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36462(int& val) {
    func_in_36462(val);
    const int x = 36462;
    std::cout << 36462;
    for (int i = 0; i < 36462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36462(int& val) {
    const int x = 36462;
    std::cout << 36462;
    for (int i = 0; i < 36462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36463(int& val) {
    func_in_36463(val);
    const int x = 36463;
    std::cout << 36463;
    for (int i = 0; i < 36463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36463(int& val) {
    const int x = 36463;
    std::cout << 36463;
    for (int i = 0; i < 36463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36464(int& val) {
    func_in_36464(val);
    const int x = 36464;
    std::cout << 36464;
    for (int i = 0; i < 36464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36464(int& val) {
    const int x = 36464;
    std::cout << 36464;
    for (int i = 0; i < 36464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36465(int& val) {
    func_in_36465(val);
    const int x = 36465;
    std::cout << 36465;
    for (int i = 0; i < 36465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36465(int& val) {
    const int x = 36465;
    std::cout << 36465;
    for (int i = 0; i < 36465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36466(int& val) {
    func_in_36466(val);
    const int x = 36466;
    std::cout << 36466;
    for (int i = 0; i < 36466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36466(int& val) {
    const int x = 36466;
    std::cout << 36466;
    for (int i = 0; i < 36466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36467(int& val) {
    func_in_36467(val);
    const int x = 36467;
    std::cout << 36467;
    for (int i = 0; i < 36467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36467(int& val) {
    const int x = 36467;
    std::cout << 36467;
    for (int i = 0; i < 36467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36468(int& val) {
    func_in_36468(val);
    const int x = 36468;
    std::cout << 36468;
    for (int i = 0; i < 36468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36468(int& val) {
    const int x = 36468;
    std::cout << 36468;
    for (int i = 0; i < 36468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36469(int& val) {
    func_in_36469(val);
    const int x = 36469;
    std::cout << 36469;
    for (int i = 0; i < 36469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36469(int& val) {
    const int x = 36469;
    std::cout << 36469;
    for (int i = 0; i < 36469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36470(int& val) {
    func_in_36470(val);
    const int x = 36470;
    std::cout << 36470;
    for (int i = 0; i < 36470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36470(int& val) {
    const int x = 36470;
    std::cout << 36470;
    for (int i = 0; i < 36470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36471(int& val) {
    func_in_36471(val);
    const int x = 36471;
    std::cout << 36471;
    for (int i = 0; i < 36471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36471(int& val) {
    const int x = 36471;
    std::cout << 36471;
    for (int i = 0; i < 36471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36472(int& val) {
    func_in_36472(val);
    const int x = 36472;
    std::cout << 36472;
    for (int i = 0; i < 36472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36472(int& val) {
    const int x = 36472;
    std::cout << 36472;
    for (int i = 0; i < 36472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36473(int& val) {
    func_in_36473(val);
    const int x = 36473;
    std::cout << 36473;
    for (int i = 0; i < 36473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36473(int& val) {
    const int x = 36473;
    std::cout << 36473;
    for (int i = 0; i < 36473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36474(int& val) {
    func_in_36474(val);
    const int x = 36474;
    std::cout << 36474;
    for (int i = 0; i < 36474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36474(int& val) {
    const int x = 36474;
    std::cout << 36474;
    for (int i = 0; i < 36474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36475(int& val) {
    func_in_36475(val);
    const int x = 36475;
    std::cout << 36475;
    for (int i = 0; i < 36475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36475(int& val) {
    const int x = 36475;
    std::cout << 36475;
    for (int i = 0; i < 36475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36476(int& val) {
    func_in_36476(val);
    const int x = 36476;
    std::cout << 36476;
    for (int i = 0; i < 36476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36476(int& val) {
    const int x = 36476;
    std::cout << 36476;
    for (int i = 0; i < 36476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36477(int& val) {
    func_in_36477(val);
    const int x = 36477;
    std::cout << 36477;
    for (int i = 0; i < 36477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36477(int& val) {
    const int x = 36477;
    std::cout << 36477;
    for (int i = 0; i < 36477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36478(int& val) {
    func_in_36478(val);
    const int x = 36478;
    std::cout << 36478;
    for (int i = 0; i < 36478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36478(int& val) {
    const int x = 36478;
    std::cout << 36478;
    for (int i = 0; i < 36478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36479(int& val) {
    func_in_36479(val);
    const int x = 36479;
    std::cout << 36479;
    for (int i = 0; i < 36479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36479(int& val) {
    const int x = 36479;
    std::cout << 36479;
    for (int i = 0; i < 36479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36480(int& val) {
    func_in_36480(val);
    const int x = 36480;
    std::cout << 36480;
    for (int i = 0; i < 36480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36480(int& val) {
    const int x = 36480;
    std::cout << 36480;
    for (int i = 0; i < 36480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36481(int& val) {
    func_in_36481(val);
    const int x = 36481;
    std::cout << 36481;
    for (int i = 0; i < 36481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36481(int& val) {
    const int x = 36481;
    std::cout << 36481;
    for (int i = 0; i < 36481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36482(int& val) {
    func_in_36482(val);
    const int x = 36482;
    std::cout << 36482;
    for (int i = 0; i < 36482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36482(int& val) {
    const int x = 36482;
    std::cout << 36482;
    for (int i = 0; i < 36482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36483(int& val) {
    func_in_36483(val);
    const int x = 36483;
    std::cout << 36483;
    for (int i = 0; i < 36483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36483(int& val) {
    const int x = 36483;
    std::cout << 36483;
    for (int i = 0; i < 36483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36484(int& val) {
    func_in_36484(val);
    const int x = 36484;
    std::cout << 36484;
    for (int i = 0; i < 36484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36484(int& val) {
    const int x = 36484;
    std::cout << 36484;
    for (int i = 0; i < 36484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36485(int& val) {
    func_in_36485(val);
    const int x = 36485;
    std::cout << 36485;
    for (int i = 0; i < 36485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36485(int& val) {
    const int x = 36485;
    std::cout << 36485;
    for (int i = 0; i < 36485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36486(int& val) {
    func_in_36486(val);
    const int x = 36486;
    std::cout << 36486;
    for (int i = 0; i < 36486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36486(int& val) {
    const int x = 36486;
    std::cout << 36486;
    for (int i = 0; i < 36486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36487(int& val) {
    func_in_36487(val);
    const int x = 36487;
    std::cout << 36487;
    for (int i = 0; i < 36487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36487(int& val) {
    const int x = 36487;
    std::cout << 36487;
    for (int i = 0; i < 36487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36488(int& val) {
    func_in_36488(val);
    const int x = 36488;
    std::cout << 36488;
    for (int i = 0; i < 36488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36488(int& val) {
    const int x = 36488;
    std::cout << 36488;
    for (int i = 0; i < 36488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36489(int& val) {
    func_in_36489(val);
    const int x = 36489;
    std::cout << 36489;
    for (int i = 0; i < 36489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36489(int& val) {
    const int x = 36489;
    std::cout << 36489;
    for (int i = 0; i < 36489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36490(int& val) {
    func_in_36490(val);
    const int x = 36490;
    std::cout << 36490;
    for (int i = 0; i < 36490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36490(int& val) {
    const int x = 36490;
    std::cout << 36490;
    for (int i = 0; i < 36490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36491(int& val) {
    func_in_36491(val);
    const int x = 36491;
    std::cout << 36491;
    for (int i = 0; i < 36491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36491(int& val) {
    const int x = 36491;
    std::cout << 36491;
    for (int i = 0; i < 36491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36492(int& val) {
    func_in_36492(val);
    const int x = 36492;
    std::cout << 36492;
    for (int i = 0; i < 36492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36492(int& val) {
    const int x = 36492;
    std::cout << 36492;
    for (int i = 0; i < 36492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36493(int& val) {
    func_in_36493(val);
    const int x = 36493;
    std::cout << 36493;
    for (int i = 0; i < 36493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36493(int& val) {
    const int x = 36493;
    std::cout << 36493;
    for (int i = 0; i < 36493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36494(int& val) {
    func_in_36494(val);
    const int x = 36494;
    std::cout << 36494;
    for (int i = 0; i < 36494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36494(int& val) {
    const int x = 36494;
    std::cout << 36494;
    for (int i = 0; i < 36494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36495(int& val) {
    func_in_36495(val);
    const int x = 36495;
    std::cout << 36495;
    for (int i = 0; i < 36495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36495(int& val) {
    const int x = 36495;
    std::cout << 36495;
    for (int i = 0; i < 36495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36496(int& val) {
    func_in_36496(val);
    const int x = 36496;
    std::cout << 36496;
    for (int i = 0; i < 36496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36496(int& val) {
    const int x = 36496;
    std::cout << 36496;
    for (int i = 0; i < 36496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36497(int& val) {
    func_in_36497(val);
    const int x = 36497;
    std::cout << 36497;
    for (int i = 0; i < 36497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36497(int& val) {
    const int x = 36497;
    std::cout << 36497;
    for (int i = 0; i < 36497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36498(int& val) {
    func_in_36498(val);
    const int x = 36498;
    std::cout << 36498;
    for (int i = 0; i < 36498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36498(int& val) {
    const int x = 36498;
    std::cout << 36498;
    for (int i = 0; i < 36498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36499(int& val) {
    func_in_36499(val);
    const int x = 36499;
    std::cout << 36499;
    for (int i = 0; i < 36499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36499(int& val) {
    const int x = 36499;
    std::cout << 36499;
    for (int i = 0; i < 36499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36500(int& val) {
    func_in_36500(val);
    const int x = 36500;
    std::cout << 36500;
    for (int i = 0; i < 36500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36500(int& val) {
    const int x = 36500;
    std::cout << 36500;
    for (int i = 0; i < 36500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36501(int& val) {
    func_in_36501(val);
    const int x = 36501;
    std::cout << 36501;
    for (int i = 0; i < 36501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36501(int& val) {
    const int x = 36501;
    std::cout << 36501;
    for (int i = 0; i < 36501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36502(int& val) {
    func_in_36502(val);
    const int x = 36502;
    std::cout << 36502;
    for (int i = 0; i < 36502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36502(int& val) {
    const int x = 36502;
    std::cout << 36502;
    for (int i = 0; i < 36502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36503(int& val) {
    func_in_36503(val);
    const int x = 36503;
    std::cout << 36503;
    for (int i = 0; i < 36503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36503(int& val) {
    const int x = 36503;
    std::cout << 36503;
    for (int i = 0; i < 36503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36504(int& val) {
    func_in_36504(val);
    const int x = 36504;
    std::cout << 36504;
    for (int i = 0; i < 36504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36504(int& val) {
    const int x = 36504;
    std::cout << 36504;
    for (int i = 0; i < 36504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36505(int& val) {
    func_in_36505(val);
    const int x = 36505;
    std::cout << 36505;
    for (int i = 0; i < 36505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36505(int& val) {
    const int x = 36505;
    std::cout << 36505;
    for (int i = 0; i < 36505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36506(int& val) {
    func_in_36506(val);
    const int x = 36506;
    std::cout << 36506;
    for (int i = 0; i < 36506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36506(int& val) {
    const int x = 36506;
    std::cout << 36506;
    for (int i = 0; i < 36506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36507(int& val) {
    func_in_36507(val);
    const int x = 36507;
    std::cout << 36507;
    for (int i = 0; i < 36507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36507(int& val) {
    const int x = 36507;
    std::cout << 36507;
    for (int i = 0; i < 36507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36508(int& val) {
    func_in_36508(val);
    const int x = 36508;
    std::cout << 36508;
    for (int i = 0; i < 36508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36508(int& val) {
    const int x = 36508;
    std::cout << 36508;
    for (int i = 0; i < 36508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36509(int& val) {
    func_in_36509(val);
    const int x = 36509;
    std::cout << 36509;
    for (int i = 0; i < 36509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36509(int& val) {
    const int x = 36509;
    std::cout << 36509;
    for (int i = 0; i < 36509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36510(int& val) {
    func_in_36510(val);
    const int x = 36510;
    std::cout << 36510;
    for (int i = 0; i < 36510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36510(int& val) {
    const int x = 36510;
    std::cout << 36510;
    for (int i = 0; i < 36510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36511(int& val) {
    func_in_36511(val);
    const int x = 36511;
    std::cout << 36511;
    for (int i = 0; i < 36511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36511(int& val) {
    const int x = 36511;
    std::cout << 36511;
    for (int i = 0; i < 36511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36512(int& val) {
    func_in_36512(val);
    const int x = 36512;
    std::cout << 36512;
    for (int i = 0; i < 36512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36512(int& val) {
    const int x = 36512;
    std::cout << 36512;
    for (int i = 0; i < 36512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36513(int& val) {
    func_in_36513(val);
    const int x = 36513;
    std::cout << 36513;
    for (int i = 0; i < 36513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36513(int& val) {
    const int x = 36513;
    std::cout << 36513;
    for (int i = 0; i < 36513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36514(int& val) {
    func_in_36514(val);
    const int x = 36514;
    std::cout << 36514;
    for (int i = 0; i < 36514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36514(int& val) {
    const int x = 36514;
    std::cout << 36514;
    for (int i = 0; i < 36514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36515(int& val) {
    func_in_36515(val);
    const int x = 36515;
    std::cout << 36515;
    for (int i = 0; i < 36515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36515(int& val) {
    const int x = 36515;
    std::cout << 36515;
    for (int i = 0; i < 36515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36516(int& val) {
    func_in_36516(val);
    const int x = 36516;
    std::cout << 36516;
    for (int i = 0; i < 36516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36516(int& val) {
    const int x = 36516;
    std::cout << 36516;
    for (int i = 0; i < 36516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36517(int& val) {
    func_in_36517(val);
    const int x = 36517;
    std::cout << 36517;
    for (int i = 0; i < 36517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36517(int& val) {
    const int x = 36517;
    std::cout << 36517;
    for (int i = 0; i < 36517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36518(int& val) {
    func_in_36518(val);
    const int x = 36518;
    std::cout << 36518;
    for (int i = 0; i < 36518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36518(int& val) {
    const int x = 36518;
    std::cout << 36518;
    for (int i = 0; i < 36518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36519(int& val) {
    func_in_36519(val);
    const int x = 36519;
    std::cout << 36519;
    for (int i = 0; i < 36519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36519(int& val) {
    const int x = 36519;
    std::cout << 36519;
    for (int i = 0; i < 36519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36520(int& val) {
    func_in_36520(val);
    const int x = 36520;
    std::cout << 36520;
    for (int i = 0; i < 36520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36520(int& val) {
    const int x = 36520;
    std::cout << 36520;
    for (int i = 0; i < 36520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36521(int& val) {
    func_in_36521(val);
    const int x = 36521;
    std::cout << 36521;
    for (int i = 0; i < 36521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36521(int& val) {
    const int x = 36521;
    std::cout << 36521;
    for (int i = 0; i < 36521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36522(int& val) {
    func_in_36522(val);
    const int x = 36522;
    std::cout << 36522;
    for (int i = 0; i < 36522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36522(int& val) {
    const int x = 36522;
    std::cout << 36522;
    for (int i = 0; i < 36522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36523(int& val) {
    func_in_36523(val);
    const int x = 36523;
    std::cout << 36523;
    for (int i = 0; i < 36523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36523(int& val) {
    const int x = 36523;
    std::cout << 36523;
    for (int i = 0; i < 36523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36524(int& val) {
    func_in_36524(val);
    const int x = 36524;
    std::cout << 36524;
    for (int i = 0; i < 36524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36524(int& val) {
    const int x = 36524;
    std::cout << 36524;
    for (int i = 0; i < 36524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36525(int& val) {
    func_in_36525(val);
    const int x = 36525;
    std::cout << 36525;
    for (int i = 0; i < 36525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36525(int& val) {
    const int x = 36525;
    std::cout << 36525;
    for (int i = 0; i < 36525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36526(int& val) {
    func_in_36526(val);
    const int x = 36526;
    std::cout << 36526;
    for (int i = 0; i < 36526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36526(int& val) {
    const int x = 36526;
    std::cout << 36526;
    for (int i = 0; i < 36526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36527(int& val) {
    func_in_36527(val);
    const int x = 36527;
    std::cout << 36527;
    for (int i = 0; i < 36527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36527(int& val) {
    const int x = 36527;
    std::cout << 36527;
    for (int i = 0; i < 36527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36528(int& val) {
    func_in_36528(val);
    const int x = 36528;
    std::cout << 36528;
    for (int i = 0; i < 36528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36528(int& val) {
    const int x = 36528;
    std::cout << 36528;
    for (int i = 0; i < 36528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36529(int& val) {
    func_in_36529(val);
    const int x = 36529;
    std::cout << 36529;
    for (int i = 0; i < 36529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36529(int& val) {
    const int x = 36529;
    std::cout << 36529;
    for (int i = 0; i < 36529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36530(int& val) {
    func_in_36530(val);
    const int x = 36530;
    std::cout << 36530;
    for (int i = 0; i < 36530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36530(int& val) {
    const int x = 36530;
    std::cout << 36530;
    for (int i = 0; i < 36530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36531(int& val) {
    func_in_36531(val);
    const int x = 36531;
    std::cout << 36531;
    for (int i = 0; i < 36531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36531(int& val) {
    const int x = 36531;
    std::cout << 36531;
    for (int i = 0; i < 36531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36532(int& val) {
    func_in_36532(val);
    const int x = 36532;
    std::cout << 36532;
    for (int i = 0; i < 36532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36532(int& val) {
    const int x = 36532;
    std::cout << 36532;
    for (int i = 0; i < 36532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36533(int& val) {
    func_in_36533(val);
    const int x = 36533;
    std::cout << 36533;
    for (int i = 0; i < 36533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36533(int& val) {
    const int x = 36533;
    std::cout << 36533;
    for (int i = 0; i < 36533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36534(int& val) {
    func_in_36534(val);
    const int x = 36534;
    std::cout << 36534;
    for (int i = 0; i < 36534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36534(int& val) {
    const int x = 36534;
    std::cout << 36534;
    for (int i = 0; i < 36534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36535(int& val) {
    func_in_36535(val);
    const int x = 36535;
    std::cout << 36535;
    for (int i = 0; i < 36535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36535(int& val) {
    const int x = 36535;
    std::cout << 36535;
    for (int i = 0; i < 36535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36536(int& val) {
    func_in_36536(val);
    const int x = 36536;
    std::cout << 36536;
    for (int i = 0; i < 36536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36536(int& val) {
    const int x = 36536;
    std::cout << 36536;
    for (int i = 0; i < 36536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36537(int& val) {
    func_in_36537(val);
    const int x = 36537;
    std::cout << 36537;
    for (int i = 0; i < 36537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36537(int& val) {
    const int x = 36537;
    std::cout << 36537;
    for (int i = 0; i < 36537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36538(int& val) {
    func_in_36538(val);
    const int x = 36538;
    std::cout << 36538;
    for (int i = 0; i < 36538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36538(int& val) {
    const int x = 36538;
    std::cout << 36538;
    for (int i = 0; i < 36538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36539(int& val) {
    func_in_36539(val);
    const int x = 36539;
    std::cout << 36539;
    for (int i = 0; i < 36539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36539(int& val) {
    const int x = 36539;
    std::cout << 36539;
    for (int i = 0; i < 36539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36540(int& val) {
    func_in_36540(val);
    const int x = 36540;
    std::cout << 36540;
    for (int i = 0; i < 36540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36540(int& val) {
    const int x = 36540;
    std::cout << 36540;
    for (int i = 0; i < 36540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36541(int& val) {
    func_in_36541(val);
    const int x = 36541;
    std::cout << 36541;
    for (int i = 0; i < 36541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36541(int& val) {
    const int x = 36541;
    std::cout << 36541;
    for (int i = 0; i < 36541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36542(int& val) {
    func_in_36542(val);
    const int x = 36542;
    std::cout << 36542;
    for (int i = 0; i < 36542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36542(int& val) {
    const int x = 36542;
    std::cout << 36542;
    for (int i = 0; i < 36542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36543(int& val) {
    func_in_36543(val);
    const int x = 36543;
    std::cout << 36543;
    for (int i = 0; i < 36543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36543(int& val) {
    const int x = 36543;
    std::cout << 36543;
    for (int i = 0; i < 36543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36544(int& val) {
    func_in_36544(val);
    const int x = 36544;
    std::cout << 36544;
    for (int i = 0; i < 36544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36544(int& val) {
    const int x = 36544;
    std::cout << 36544;
    for (int i = 0; i < 36544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36545(int& val) {
    func_in_36545(val);
    const int x = 36545;
    std::cout << 36545;
    for (int i = 0; i < 36545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36545(int& val) {
    const int x = 36545;
    std::cout << 36545;
    for (int i = 0; i < 36545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36546(int& val) {
    func_in_36546(val);
    const int x = 36546;
    std::cout << 36546;
    for (int i = 0; i < 36546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36546(int& val) {
    const int x = 36546;
    std::cout << 36546;
    for (int i = 0; i < 36546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36547(int& val) {
    func_in_36547(val);
    const int x = 36547;
    std::cout << 36547;
    for (int i = 0; i < 36547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36547(int& val) {
    const int x = 36547;
    std::cout << 36547;
    for (int i = 0; i < 36547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36548(int& val) {
    func_in_36548(val);
    const int x = 36548;
    std::cout << 36548;
    for (int i = 0; i < 36548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36548(int& val) {
    const int x = 36548;
    std::cout << 36548;
    for (int i = 0; i < 36548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36549(int& val) {
    func_in_36549(val);
    const int x = 36549;
    std::cout << 36549;
    for (int i = 0; i < 36549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36549(int& val) {
    const int x = 36549;
    std::cout << 36549;
    for (int i = 0; i < 36549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36550(int& val) {
    func_in_36550(val);
    const int x = 36550;
    std::cout << 36550;
    for (int i = 0; i < 36550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36550(int& val) {
    const int x = 36550;
    std::cout << 36550;
    for (int i = 0; i < 36550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36551(int& val) {
    func_in_36551(val);
    const int x = 36551;
    std::cout << 36551;
    for (int i = 0; i < 36551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36551(int& val) {
    const int x = 36551;
    std::cout << 36551;
    for (int i = 0; i < 36551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36552(int& val) {
    func_in_36552(val);
    const int x = 36552;
    std::cout << 36552;
    for (int i = 0; i < 36552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36552(int& val) {
    const int x = 36552;
    std::cout << 36552;
    for (int i = 0; i < 36552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36553(int& val) {
    func_in_36553(val);
    const int x = 36553;
    std::cout << 36553;
    for (int i = 0; i < 36553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36553(int& val) {
    const int x = 36553;
    std::cout << 36553;
    for (int i = 0; i < 36553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36554(int& val) {
    func_in_36554(val);
    const int x = 36554;
    std::cout << 36554;
    for (int i = 0; i < 36554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36554(int& val) {
    const int x = 36554;
    std::cout << 36554;
    for (int i = 0; i < 36554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36555(int& val) {
    func_in_36555(val);
    const int x = 36555;
    std::cout << 36555;
    for (int i = 0; i < 36555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36555(int& val) {
    const int x = 36555;
    std::cout << 36555;
    for (int i = 0; i < 36555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36556(int& val) {
    func_in_36556(val);
    const int x = 36556;
    std::cout << 36556;
    for (int i = 0; i < 36556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36556(int& val) {
    const int x = 36556;
    std::cout << 36556;
    for (int i = 0; i < 36556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36557(int& val) {
    func_in_36557(val);
    const int x = 36557;
    std::cout << 36557;
    for (int i = 0; i < 36557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36557(int& val) {
    const int x = 36557;
    std::cout << 36557;
    for (int i = 0; i < 36557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36558(int& val) {
    func_in_36558(val);
    const int x = 36558;
    std::cout << 36558;
    for (int i = 0; i < 36558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36558(int& val) {
    const int x = 36558;
    std::cout << 36558;
    for (int i = 0; i < 36558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36559(int& val) {
    func_in_36559(val);
    const int x = 36559;
    std::cout << 36559;
    for (int i = 0; i < 36559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36559(int& val) {
    const int x = 36559;
    std::cout << 36559;
    for (int i = 0; i < 36559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36560(int& val) {
    func_in_36560(val);
    const int x = 36560;
    std::cout << 36560;
    for (int i = 0; i < 36560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36560(int& val) {
    const int x = 36560;
    std::cout << 36560;
    for (int i = 0; i < 36560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36561(int& val) {
    func_in_36561(val);
    const int x = 36561;
    std::cout << 36561;
    for (int i = 0; i < 36561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36561(int& val) {
    const int x = 36561;
    std::cout << 36561;
    for (int i = 0; i < 36561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36562(int& val) {
    func_in_36562(val);
    const int x = 36562;
    std::cout << 36562;
    for (int i = 0; i < 36562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36562(int& val) {
    const int x = 36562;
    std::cout << 36562;
    for (int i = 0; i < 36562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36563(int& val) {
    func_in_36563(val);
    const int x = 36563;
    std::cout << 36563;
    for (int i = 0; i < 36563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36563(int& val) {
    const int x = 36563;
    std::cout << 36563;
    for (int i = 0; i < 36563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36564(int& val) {
    func_in_36564(val);
    const int x = 36564;
    std::cout << 36564;
    for (int i = 0; i < 36564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36564(int& val) {
    const int x = 36564;
    std::cout << 36564;
    for (int i = 0; i < 36564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36565(int& val) {
    func_in_36565(val);
    const int x = 36565;
    std::cout << 36565;
    for (int i = 0; i < 36565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36565(int& val) {
    const int x = 36565;
    std::cout << 36565;
    for (int i = 0; i < 36565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36566(int& val) {
    func_in_36566(val);
    const int x = 36566;
    std::cout << 36566;
    for (int i = 0; i < 36566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36566(int& val) {
    const int x = 36566;
    std::cout << 36566;
    for (int i = 0; i < 36566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36567(int& val) {
    func_in_36567(val);
    const int x = 36567;
    std::cout << 36567;
    for (int i = 0; i < 36567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36567(int& val) {
    const int x = 36567;
    std::cout << 36567;
    for (int i = 0; i < 36567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36568(int& val) {
    func_in_36568(val);
    const int x = 36568;
    std::cout << 36568;
    for (int i = 0; i < 36568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36568(int& val) {
    const int x = 36568;
    std::cout << 36568;
    for (int i = 0; i < 36568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36569(int& val) {
    func_in_36569(val);
    const int x = 36569;
    std::cout << 36569;
    for (int i = 0; i < 36569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36569(int& val) {
    const int x = 36569;
    std::cout << 36569;
    for (int i = 0; i < 36569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36570(int& val) {
    func_in_36570(val);
    const int x = 36570;
    std::cout << 36570;
    for (int i = 0; i < 36570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36570(int& val) {
    const int x = 36570;
    std::cout << 36570;
    for (int i = 0; i < 36570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36571(int& val) {
    func_in_36571(val);
    const int x = 36571;
    std::cout << 36571;
    for (int i = 0; i < 36571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36571(int& val) {
    const int x = 36571;
    std::cout << 36571;
    for (int i = 0; i < 36571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36572(int& val) {
    func_in_36572(val);
    const int x = 36572;
    std::cout << 36572;
    for (int i = 0; i < 36572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36572(int& val) {
    const int x = 36572;
    std::cout << 36572;
    for (int i = 0; i < 36572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36573(int& val) {
    func_in_36573(val);
    const int x = 36573;
    std::cout << 36573;
    for (int i = 0; i < 36573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36573(int& val) {
    const int x = 36573;
    std::cout << 36573;
    for (int i = 0; i < 36573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36574(int& val) {
    func_in_36574(val);
    const int x = 36574;
    std::cout << 36574;
    for (int i = 0; i < 36574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36574(int& val) {
    const int x = 36574;
    std::cout << 36574;
    for (int i = 0; i < 36574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36575(int& val) {
    func_in_36575(val);
    const int x = 36575;
    std::cout << 36575;
    for (int i = 0; i < 36575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36575(int& val) {
    const int x = 36575;
    std::cout << 36575;
    for (int i = 0; i < 36575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36576(int& val) {
    func_in_36576(val);
    const int x = 36576;
    std::cout << 36576;
    for (int i = 0; i < 36576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36576(int& val) {
    const int x = 36576;
    std::cout << 36576;
    for (int i = 0; i < 36576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36577(int& val) {
    func_in_36577(val);
    const int x = 36577;
    std::cout << 36577;
    for (int i = 0; i < 36577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36577(int& val) {
    const int x = 36577;
    std::cout << 36577;
    for (int i = 0; i < 36577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36578(int& val) {
    func_in_36578(val);
    const int x = 36578;
    std::cout << 36578;
    for (int i = 0; i < 36578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36578(int& val) {
    const int x = 36578;
    std::cout << 36578;
    for (int i = 0; i < 36578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36579(int& val) {
    func_in_36579(val);
    const int x = 36579;
    std::cout << 36579;
    for (int i = 0; i < 36579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36579(int& val) {
    const int x = 36579;
    std::cout << 36579;
    for (int i = 0; i < 36579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36580(int& val) {
    func_in_36580(val);
    const int x = 36580;
    std::cout << 36580;
    for (int i = 0; i < 36580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36580(int& val) {
    const int x = 36580;
    std::cout << 36580;
    for (int i = 0; i < 36580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36581(int& val) {
    func_in_36581(val);
    const int x = 36581;
    std::cout << 36581;
    for (int i = 0; i < 36581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36581(int& val) {
    const int x = 36581;
    std::cout << 36581;
    for (int i = 0; i < 36581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36582(int& val) {
    func_in_36582(val);
    const int x = 36582;
    std::cout << 36582;
    for (int i = 0; i < 36582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36582(int& val) {
    const int x = 36582;
    std::cout << 36582;
    for (int i = 0; i < 36582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36583(int& val) {
    func_in_36583(val);
    const int x = 36583;
    std::cout << 36583;
    for (int i = 0; i < 36583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36583(int& val) {
    const int x = 36583;
    std::cout << 36583;
    for (int i = 0; i < 36583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36584(int& val) {
    func_in_36584(val);
    const int x = 36584;
    std::cout << 36584;
    for (int i = 0; i < 36584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36584(int& val) {
    const int x = 36584;
    std::cout << 36584;
    for (int i = 0; i < 36584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36585(int& val) {
    func_in_36585(val);
    const int x = 36585;
    std::cout << 36585;
    for (int i = 0; i < 36585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36585(int& val) {
    const int x = 36585;
    std::cout << 36585;
    for (int i = 0; i < 36585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36586(int& val) {
    func_in_36586(val);
    const int x = 36586;
    std::cout << 36586;
    for (int i = 0; i < 36586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36586(int& val) {
    const int x = 36586;
    std::cout << 36586;
    for (int i = 0; i < 36586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36587(int& val) {
    func_in_36587(val);
    const int x = 36587;
    std::cout << 36587;
    for (int i = 0; i < 36587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36587(int& val) {
    const int x = 36587;
    std::cout << 36587;
    for (int i = 0; i < 36587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36588(int& val) {
    func_in_36588(val);
    const int x = 36588;
    std::cout << 36588;
    for (int i = 0; i < 36588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36588(int& val) {
    const int x = 36588;
    std::cout << 36588;
    for (int i = 0; i < 36588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36589(int& val) {
    func_in_36589(val);
    const int x = 36589;
    std::cout << 36589;
    for (int i = 0; i < 36589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36589(int& val) {
    const int x = 36589;
    std::cout << 36589;
    for (int i = 0; i < 36589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36590(int& val) {
    func_in_36590(val);
    const int x = 36590;
    std::cout << 36590;
    for (int i = 0; i < 36590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36590(int& val) {
    const int x = 36590;
    std::cout << 36590;
    for (int i = 0; i < 36590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36591(int& val) {
    func_in_36591(val);
    const int x = 36591;
    std::cout << 36591;
    for (int i = 0; i < 36591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36591(int& val) {
    const int x = 36591;
    std::cout << 36591;
    for (int i = 0; i < 36591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36592(int& val) {
    func_in_36592(val);
    const int x = 36592;
    std::cout << 36592;
    for (int i = 0; i < 36592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36592(int& val) {
    const int x = 36592;
    std::cout << 36592;
    for (int i = 0; i < 36592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36593(int& val) {
    func_in_36593(val);
    const int x = 36593;
    std::cout << 36593;
    for (int i = 0; i < 36593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36593(int& val) {
    const int x = 36593;
    std::cout << 36593;
    for (int i = 0; i < 36593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36594(int& val) {
    func_in_36594(val);
    const int x = 36594;
    std::cout << 36594;
    for (int i = 0; i < 36594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36594(int& val) {
    const int x = 36594;
    std::cout << 36594;
    for (int i = 0; i < 36594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36595(int& val) {
    func_in_36595(val);
    const int x = 36595;
    std::cout << 36595;
    for (int i = 0; i < 36595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36595(int& val) {
    const int x = 36595;
    std::cout << 36595;
    for (int i = 0; i < 36595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36596(int& val) {
    func_in_36596(val);
    const int x = 36596;
    std::cout << 36596;
    for (int i = 0; i < 36596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36596(int& val) {
    const int x = 36596;
    std::cout << 36596;
    for (int i = 0; i < 36596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36597(int& val) {
    func_in_36597(val);
    const int x = 36597;
    std::cout << 36597;
    for (int i = 0; i < 36597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36597(int& val) {
    const int x = 36597;
    std::cout << 36597;
    for (int i = 0; i < 36597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36598(int& val) {
    func_in_36598(val);
    const int x = 36598;
    std::cout << 36598;
    for (int i = 0; i < 36598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36598(int& val) {
    const int x = 36598;
    std::cout << 36598;
    for (int i = 0; i < 36598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36599(int& val) {
    func_in_36599(val);
    const int x = 36599;
    std::cout << 36599;
    for (int i = 0; i < 36599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36599(int& val) {
    const int x = 36599;
    std::cout << 36599;
    for (int i = 0; i < 36599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36600(int& val) {
    func_in_36600(val);
    const int x = 36600;
    std::cout << 36600;
    for (int i = 0; i < 36600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36600(int& val) {
    const int x = 36600;
    std::cout << 36600;
    for (int i = 0; i < 36600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36601(int& val) {
    func_in_36601(val);
    const int x = 36601;
    std::cout << 36601;
    for (int i = 0; i < 36601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36601(int& val) {
    const int x = 36601;
    std::cout << 36601;
    for (int i = 0; i < 36601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36602(int& val) {
    func_in_36602(val);
    const int x = 36602;
    std::cout << 36602;
    for (int i = 0; i < 36602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36602(int& val) {
    const int x = 36602;
    std::cout << 36602;
    for (int i = 0; i < 36602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36603(int& val) {
    func_in_36603(val);
    const int x = 36603;
    std::cout << 36603;
    for (int i = 0; i < 36603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36603(int& val) {
    const int x = 36603;
    std::cout << 36603;
    for (int i = 0; i < 36603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36604(int& val) {
    func_in_36604(val);
    const int x = 36604;
    std::cout << 36604;
    for (int i = 0; i < 36604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36604(int& val) {
    const int x = 36604;
    std::cout << 36604;
    for (int i = 0; i < 36604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36605(int& val) {
    func_in_36605(val);
    const int x = 36605;
    std::cout << 36605;
    for (int i = 0; i < 36605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36605(int& val) {
    const int x = 36605;
    std::cout << 36605;
    for (int i = 0; i < 36605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36606(int& val) {
    func_in_36606(val);
    const int x = 36606;
    std::cout << 36606;
    for (int i = 0; i < 36606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36606(int& val) {
    const int x = 36606;
    std::cout << 36606;
    for (int i = 0; i < 36606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36607(int& val) {
    func_in_36607(val);
    const int x = 36607;
    std::cout << 36607;
    for (int i = 0; i < 36607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36607(int& val) {
    const int x = 36607;
    std::cout << 36607;
    for (int i = 0; i < 36607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36608(int& val) {
    func_in_36608(val);
    const int x = 36608;
    std::cout << 36608;
    for (int i = 0; i < 36608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36608(int& val) {
    const int x = 36608;
    std::cout << 36608;
    for (int i = 0; i < 36608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36609(int& val) {
    func_in_36609(val);
    const int x = 36609;
    std::cout << 36609;
    for (int i = 0; i < 36609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36609(int& val) {
    const int x = 36609;
    std::cout << 36609;
    for (int i = 0; i < 36609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36610(int& val) {
    func_in_36610(val);
    const int x = 36610;
    std::cout << 36610;
    for (int i = 0; i < 36610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36610(int& val) {
    const int x = 36610;
    std::cout << 36610;
    for (int i = 0; i < 36610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36611(int& val) {
    func_in_36611(val);
    const int x = 36611;
    std::cout << 36611;
    for (int i = 0; i < 36611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36611(int& val) {
    const int x = 36611;
    std::cout << 36611;
    for (int i = 0; i < 36611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36612(int& val) {
    func_in_36612(val);
    const int x = 36612;
    std::cout << 36612;
    for (int i = 0; i < 36612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36612(int& val) {
    const int x = 36612;
    std::cout << 36612;
    for (int i = 0; i < 36612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36613(int& val) {
    func_in_36613(val);
    const int x = 36613;
    std::cout << 36613;
    for (int i = 0; i < 36613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36613(int& val) {
    const int x = 36613;
    std::cout << 36613;
    for (int i = 0; i < 36613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36614(int& val) {
    func_in_36614(val);
    const int x = 36614;
    std::cout << 36614;
    for (int i = 0; i < 36614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36614(int& val) {
    const int x = 36614;
    std::cout << 36614;
    for (int i = 0; i < 36614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36615(int& val) {
    func_in_36615(val);
    const int x = 36615;
    std::cout << 36615;
    for (int i = 0; i < 36615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36615(int& val) {
    const int x = 36615;
    std::cout << 36615;
    for (int i = 0; i < 36615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36616(int& val) {
    func_in_36616(val);
    const int x = 36616;
    std::cout << 36616;
    for (int i = 0; i < 36616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36616(int& val) {
    const int x = 36616;
    std::cout << 36616;
    for (int i = 0; i < 36616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36617(int& val) {
    func_in_36617(val);
    const int x = 36617;
    std::cout << 36617;
    for (int i = 0; i < 36617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36617(int& val) {
    const int x = 36617;
    std::cout << 36617;
    for (int i = 0; i < 36617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36618(int& val) {
    func_in_36618(val);
    const int x = 36618;
    std::cout << 36618;
    for (int i = 0; i < 36618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36618(int& val) {
    const int x = 36618;
    std::cout << 36618;
    for (int i = 0; i < 36618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36619(int& val) {
    func_in_36619(val);
    const int x = 36619;
    std::cout << 36619;
    for (int i = 0; i < 36619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36619(int& val) {
    const int x = 36619;
    std::cout << 36619;
    for (int i = 0; i < 36619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36620(int& val) {
    func_in_36620(val);
    const int x = 36620;
    std::cout << 36620;
    for (int i = 0; i < 36620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36620(int& val) {
    const int x = 36620;
    std::cout << 36620;
    for (int i = 0; i < 36620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36621(int& val) {
    func_in_36621(val);
    const int x = 36621;
    std::cout << 36621;
    for (int i = 0; i < 36621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36621(int& val) {
    const int x = 36621;
    std::cout << 36621;
    for (int i = 0; i < 36621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36622(int& val) {
    func_in_36622(val);
    const int x = 36622;
    std::cout << 36622;
    for (int i = 0; i < 36622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36622(int& val) {
    const int x = 36622;
    std::cout << 36622;
    for (int i = 0; i < 36622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36623(int& val) {
    func_in_36623(val);
    const int x = 36623;
    std::cout << 36623;
    for (int i = 0; i < 36623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36623(int& val) {
    const int x = 36623;
    std::cout << 36623;
    for (int i = 0; i < 36623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36624(int& val) {
    func_in_36624(val);
    const int x = 36624;
    std::cout << 36624;
    for (int i = 0; i < 36624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36624(int& val) {
    const int x = 36624;
    std::cout << 36624;
    for (int i = 0; i < 36624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36625(int& val) {
    func_in_36625(val);
    const int x = 36625;
    std::cout << 36625;
    for (int i = 0; i < 36625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36625(int& val) {
    const int x = 36625;
    std::cout << 36625;
    for (int i = 0; i < 36625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36626(int& val) {
    func_in_36626(val);
    const int x = 36626;
    std::cout << 36626;
    for (int i = 0; i < 36626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36626(int& val) {
    const int x = 36626;
    std::cout << 36626;
    for (int i = 0; i < 36626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36627(int& val) {
    func_in_36627(val);
    const int x = 36627;
    std::cout << 36627;
    for (int i = 0; i < 36627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36627(int& val) {
    const int x = 36627;
    std::cout << 36627;
    for (int i = 0; i < 36627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36628(int& val) {
    func_in_36628(val);
    const int x = 36628;
    std::cout << 36628;
    for (int i = 0; i < 36628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36628(int& val) {
    const int x = 36628;
    std::cout << 36628;
    for (int i = 0; i < 36628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36629(int& val) {
    func_in_36629(val);
    const int x = 36629;
    std::cout << 36629;
    for (int i = 0; i < 36629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36629(int& val) {
    const int x = 36629;
    std::cout << 36629;
    for (int i = 0; i < 36629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36630(int& val) {
    func_in_36630(val);
    const int x = 36630;
    std::cout << 36630;
    for (int i = 0; i < 36630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36630(int& val) {
    const int x = 36630;
    std::cout << 36630;
    for (int i = 0; i < 36630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36631(int& val) {
    func_in_36631(val);
    const int x = 36631;
    std::cout << 36631;
    for (int i = 0; i < 36631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36631(int& val) {
    const int x = 36631;
    std::cout << 36631;
    for (int i = 0; i < 36631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36632(int& val) {
    func_in_36632(val);
    const int x = 36632;
    std::cout << 36632;
    for (int i = 0; i < 36632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36632(int& val) {
    const int x = 36632;
    std::cout << 36632;
    for (int i = 0; i < 36632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36633(int& val) {
    func_in_36633(val);
    const int x = 36633;
    std::cout << 36633;
    for (int i = 0; i < 36633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36633(int& val) {
    const int x = 36633;
    std::cout << 36633;
    for (int i = 0; i < 36633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36634(int& val) {
    func_in_36634(val);
    const int x = 36634;
    std::cout << 36634;
    for (int i = 0; i < 36634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36634(int& val) {
    const int x = 36634;
    std::cout << 36634;
    for (int i = 0; i < 36634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36635(int& val) {
    func_in_36635(val);
    const int x = 36635;
    std::cout << 36635;
    for (int i = 0; i < 36635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36635(int& val) {
    const int x = 36635;
    std::cout << 36635;
    for (int i = 0; i < 36635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36636(int& val) {
    func_in_36636(val);
    const int x = 36636;
    std::cout << 36636;
    for (int i = 0; i < 36636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36636(int& val) {
    const int x = 36636;
    std::cout << 36636;
    for (int i = 0; i < 36636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36637(int& val) {
    func_in_36637(val);
    const int x = 36637;
    std::cout << 36637;
    for (int i = 0; i < 36637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36637(int& val) {
    const int x = 36637;
    std::cout << 36637;
    for (int i = 0; i < 36637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36638(int& val) {
    func_in_36638(val);
    const int x = 36638;
    std::cout << 36638;
    for (int i = 0; i < 36638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36638(int& val) {
    const int x = 36638;
    std::cout << 36638;
    for (int i = 0; i < 36638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36639(int& val) {
    func_in_36639(val);
    const int x = 36639;
    std::cout << 36639;
    for (int i = 0; i < 36639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36639(int& val) {
    const int x = 36639;
    std::cout << 36639;
    for (int i = 0; i < 36639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36640(int& val) {
    func_in_36640(val);
    const int x = 36640;
    std::cout << 36640;
    for (int i = 0; i < 36640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36640(int& val) {
    const int x = 36640;
    std::cout << 36640;
    for (int i = 0; i < 36640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36641(int& val) {
    func_in_36641(val);
    const int x = 36641;
    std::cout << 36641;
    for (int i = 0; i < 36641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36641(int& val) {
    const int x = 36641;
    std::cout << 36641;
    for (int i = 0; i < 36641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36642(int& val) {
    func_in_36642(val);
    const int x = 36642;
    std::cout << 36642;
    for (int i = 0; i < 36642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36642(int& val) {
    const int x = 36642;
    std::cout << 36642;
    for (int i = 0; i < 36642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36643(int& val) {
    func_in_36643(val);
    const int x = 36643;
    std::cout << 36643;
    for (int i = 0; i < 36643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36643(int& val) {
    const int x = 36643;
    std::cout << 36643;
    for (int i = 0; i < 36643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36644(int& val) {
    func_in_36644(val);
    const int x = 36644;
    std::cout << 36644;
    for (int i = 0; i < 36644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36644(int& val) {
    const int x = 36644;
    std::cout << 36644;
    for (int i = 0; i < 36644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36645(int& val) {
    func_in_36645(val);
    const int x = 36645;
    std::cout << 36645;
    for (int i = 0; i < 36645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36645(int& val) {
    const int x = 36645;
    std::cout << 36645;
    for (int i = 0; i < 36645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36646(int& val) {
    func_in_36646(val);
    const int x = 36646;
    std::cout << 36646;
    for (int i = 0; i < 36646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36646(int& val) {
    const int x = 36646;
    std::cout << 36646;
    for (int i = 0; i < 36646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36647(int& val) {
    func_in_36647(val);
    const int x = 36647;
    std::cout << 36647;
    for (int i = 0; i < 36647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36647(int& val) {
    const int x = 36647;
    std::cout << 36647;
    for (int i = 0; i < 36647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36648(int& val) {
    func_in_36648(val);
    const int x = 36648;
    std::cout << 36648;
    for (int i = 0; i < 36648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36648(int& val) {
    const int x = 36648;
    std::cout << 36648;
    for (int i = 0; i < 36648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36649(int& val) {
    func_in_36649(val);
    const int x = 36649;
    std::cout << 36649;
    for (int i = 0; i < 36649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36649(int& val) {
    const int x = 36649;
    std::cout << 36649;
    for (int i = 0; i < 36649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36650(int& val) {
    func_in_36650(val);
    const int x = 36650;
    std::cout << 36650;
    for (int i = 0; i < 36650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36650(int& val) {
    const int x = 36650;
    std::cout << 36650;
    for (int i = 0; i < 36650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36651(int& val) {
    func_in_36651(val);
    const int x = 36651;
    std::cout << 36651;
    for (int i = 0; i < 36651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36651(int& val) {
    const int x = 36651;
    std::cout << 36651;
    for (int i = 0; i < 36651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36652(int& val) {
    func_in_36652(val);
    const int x = 36652;
    std::cout << 36652;
    for (int i = 0; i < 36652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36652(int& val) {
    const int x = 36652;
    std::cout << 36652;
    for (int i = 0; i < 36652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36653(int& val) {
    func_in_36653(val);
    const int x = 36653;
    std::cout << 36653;
    for (int i = 0; i < 36653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36653(int& val) {
    const int x = 36653;
    std::cout << 36653;
    for (int i = 0; i < 36653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36654(int& val) {
    func_in_36654(val);
    const int x = 36654;
    std::cout << 36654;
    for (int i = 0; i < 36654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36654(int& val) {
    const int x = 36654;
    std::cout << 36654;
    for (int i = 0; i < 36654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36655(int& val) {
    func_in_36655(val);
    const int x = 36655;
    std::cout << 36655;
    for (int i = 0; i < 36655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36655(int& val) {
    const int x = 36655;
    std::cout << 36655;
    for (int i = 0; i < 36655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36656(int& val) {
    func_in_36656(val);
    const int x = 36656;
    std::cout << 36656;
    for (int i = 0; i < 36656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36656(int& val) {
    const int x = 36656;
    std::cout << 36656;
    for (int i = 0; i < 36656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36657(int& val) {
    func_in_36657(val);
    const int x = 36657;
    std::cout << 36657;
    for (int i = 0; i < 36657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36657(int& val) {
    const int x = 36657;
    std::cout << 36657;
    for (int i = 0; i < 36657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36658(int& val) {
    func_in_36658(val);
    const int x = 36658;
    std::cout << 36658;
    for (int i = 0; i < 36658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36658(int& val) {
    const int x = 36658;
    std::cout << 36658;
    for (int i = 0; i < 36658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36659(int& val) {
    func_in_36659(val);
    const int x = 36659;
    std::cout << 36659;
    for (int i = 0; i < 36659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36659(int& val) {
    const int x = 36659;
    std::cout << 36659;
    for (int i = 0; i < 36659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36660(int& val) {
    func_in_36660(val);
    const int x = 36660;
    std::cout << 36660;
    for (int i = 0; i < 36660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36660(int& val) {
    const int x = 36660;
    std::cout << 36660;
    for (int i = 0; i < 36660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36661(int& val) {
    func_in_36661(val);
    const int x = 36661;
    std::cout << 36661;
    for (int i = 0; i < 36661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36661(int& val) {
    const int x = 36661;
    std::cout << 36661;
    for (int i = 0; i < 36661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36662(int& val) {
    func_in_36662(val);
    const int x = 36662;
    std::cout << 36662;
    for (int i = 0; i < 36662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36662(int& val) {
    const int x = 36662;
    std::cout << 36662;
    for (int i = 0; i < 36662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36663(int& val) {
    func_in_36663(val);
    const int x = 36663;
    std::cout << 36663;
    for (int i = 0; i < 36663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36663(int& val) {
    const int x = 36663;
    std::cout << 36663;
    for (int i = 0; i < 36663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36664(int& val) {
    func_in_36664(val);
    const int x = 36664;
    std::cout << 36664;
    for (int i = 0; i < 36664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36664(int& val) {
    const int x = 36664;
    std::cout << 36664;
    for (int i = 0; i < 36664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36665(int& val) {
    func_in_36665(val);
    const int x = 36665;
    std::cout << 36665;
    for (int i = 0; i < 36665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36665(int& val) {
    const int x = 36665;
    std::cout << 36665;
    for (int i = 0; i < 36665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36666(int& val) {
    func_in_36666(val);
    const int x = 36666;
    std::cout << 36666;
    for (int i = 0; i < 36666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36666(int& val) {
    const int x = 36666;
    std::cout << 36666;
    for (int i = 0; i < 36666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36667(int& val) {
    func_in_36667(val);
    const int x = 36667;
    std::cout << 36667;
    for (int i = 0; i < 36667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36667(int& val) {
    const int x = 36667;
    std::cout << 36667;
    for (int i = 0; i < 36667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36668(int& val) {
    func_in_36668(val);
    const int x = 36668;
    std::cout << 36668;
    for (int i = 0; i < 36668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36668(int& val) {
    const int x = 36668;
    std::cout << 36668;
    for (int i = 0; i < 36668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36669(int& val) {
    func_in_36669(val);
    const int x = 36669;
    std::cout << 36669;
    for (int i = 0; i < 36669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36669(int& val) {
    const int x = 36669;
    std::cout << 36669;
    for (int i = 0; i < 36669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36670(int& val) {
    func_in_36670(val);
    const int x = 36670;
    std::cout << 36670;
    for (int i = 0; i < 36670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36670(int& val) {
    const int x = 36670;
    std::cout << 36670;
    for (int i = 0; i < 36670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36671(int& val) {
    func_in_36671(val);
    const int x = 36671;
    std::cout << 36671;
    for (int i = 0; i < 36671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36671(int& val) {
    const int x = 36671;
    std::cout << 36671;
    for (int i = 0; i < 36671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36672(int& val) {
    func_in_36672(val);
    const int x = 36672;
    std::cout << 36672;
    for (int i = 0; i < 36672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36672(int& val) {
    const int x = 36672;
    std::cout << 36672;
    for (int i = 0; i < 36672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36673(int& val) {
    func_in_36673(val);
    const int x = 36673;
    std::cout << 36673;
    for (int i = 0; i < 36673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36673(int& val) {
    const int x = 36673;
    std::cout << 36673;
    for (int i = 0; i < 36673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36674(int& val) {
    func_in_36674(val);
    const int x = 36674;
    std::cout << 36674;
    for (int i = 0; i < 36674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36674(int& val) {
    const int x = 36674;
    std::cout << 36674;
    for (int i = 0; i < 36674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36675(int& val) {
    func_in_36675(val);
    const int x = 36675;
    std::cout << 36675;
    for (int i = 0; i < 36675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36675(int& val) {
    const int x = 36675;
    std::cout << 36675;
    for (int i = 0; i < 36675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36676(int& val) {
    func_in_36676(val);
    const int x = 36676;
    std::cout << 36676;
    for (int i = 0; i < 36676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36676(int& val) {
    const int x = 36676;
    std::cout << 36676;
    for (int i = 0; i < 36676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36677(int& val) {
    func_in_36677(val);
    const int x = 36677;
    std::cout << 36677;
    for (int i = 0; i < 36677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36677(int& val) {
    const int x = 36677;
    std::cout << 36677;
    for (int i = 0; i < 36677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36678(int& val) {
    func_in_36678(val);
    const int x = 36678;
    std::cout << 36678;
    for (int i = 0; i < 36678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36678(int& val) {
    const int x = 36678;
    std::cout << 36678;
    for (int i = 0; i < 36678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36679(int& val) {
    func_in_36679(val);
    const int x = 36679;
    std::cout << 36679;
    for (int i = 0; i < 36679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36679(int& val) {
    const int x = 36679;
    std::cout << 36679;
    for (int i = 0; i < 36679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36680(int& val) {
    func_in_36680(val);
    const int x = 36680;
    std::cout << 36680;
    for (int i = 0; i < 36680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36680(int& val) {
    const int x = 36680;
    std::cout << 36680;
    for (int i = 0; i < 36680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36681(int& val) {
    func_in_36681(val);
    const int x = 36681;
    std::cout << 36681;
    for (int i = 0; i < 36681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36681(int& val) {
    const int x = 36681;
    std::cout << 36681;
    for (int i = 0; i < 36681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36682(int& val) {
    func_in_36682(val);
    const int x = 36682;
    std::cout << 36682;
    for (int i = 0; i < 36682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36682(int& val) {
    const int x = 36682;
    std::cout << 36682;
    for (int i = 0; i < 36682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36683(int& val) {
    func_in_36683(val);
    const int x = 36683;
    std::cout << 36683;
    for (int i = 0; i < 36683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36683(int& val) {
    const int x = 36683;
    std::cout << 36683;
    for (int i = 0; i < 36683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36684(int& val) {
    func_in_36684(val);
    const int x = 36684;
    std::cout << 36684;
    for (int i = 0; i < 36684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36684(int& val) {
    const int x = 36684;
    std::cout << 36684;
    for (int i = 0; i < 36684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36685(int& val) {
    func_in_36685(val);
    const int x = 36685;
    std::cout << 36685;
    for (int i = 0; i < 36685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36685(int& val) {
    const int x = 36685;
    std::cout << 36685;
    for (int i = 0; i < 36685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36686(int& val) {
    func_in_36686(val);
    const int x = 36686;
    std::cout << 36686;
    for (int i = 0; i < 36686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36686(int& val) {
    const int x = 36686;
    std::cout << 36686;
    for (int i = 0; i < 36686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36687(int& val) {
    func_in_36687(val);
    const int x = 36687;
    std::cout << 36687;
    for (int i = 0; i < 36687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36687(int& val) {
    const int x = 36687;
    std::cout << 36687;
    for (int i = 0; i < 36687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36688(int& val) {
    func_in_36688(val);
    const int x = 36688;
    std::cout << 36688;
    for (int i = 0; i < 36688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36688(int& val) {
    const int x = 36688;
    std::cout << 36688;
    for (int i = 0; i < 36688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36689(int& val) {
    func_in_36689(val);
    const int x = 36689;
    std::cout << 36689;
    for (int i = 0; i < 36689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36689(int& val) {
    const int x = 36689;
    std::cout << 36689;
    for (int i = 0; i < 36689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36690(int& val) {
    func_in_36690(val);
    const int x = 36690;
    std::cout << 36690;
    for (int i = 0; i < 36690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36690(int& val) {
    const int x = 36690;
    std::cout << 36690;
    for (int i = 0; i < 36690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36691(int& val) {
    func_in_36691(val);
    const int x = 36691;
    std::cout << 36691;
    for (int i = 0; i < 36691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36691(int& val) {
    const int x = 36691;
    std::cout << 36691;
    for (int i = 0; i < 36691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36692(int& val) {
    func_in_36692(val);
    const int x = 36692;
    std::cout << 36692;
    for (int i = 0; i < 36692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36692(int& val) {
    const int x = 36692;
    std::cout << 36692;
    for (int i = 0; i < 36692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36693(int& val) {
    func_in_36693(val);
    const int x = 36693;
    std::cout << 36693;
    for (int i = 0; i < 36693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36693(int& val) {
    const int x = 36693;
    std::cout << 36693;
    for (int i = 0; i < 36693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36694(int& val) {
    func_in_36694(val);
    const int x = 36694;
    std::cout << 36694;
    for (int i = 0; i < 36694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36694(int& val) {
    const int x = 36694;
    std::cout << 36694;
    for (int i = 0; i < 36694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36695(int& val) {
    func_in_36695(val);
    const int x = 36695;
    std::cout << 36695;
    for (int i = 0; i < 36695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36695(int& val) {
    const int x = 36695;
    std::cout << 36695;
    for (int i = 0; i < 36695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36696(int& val) {
    func_in_36696(val);
    const int x = 36696;
    std::cout << 36696;
    for (int i = 0; i < 36696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36696(int& val) {
    const int x = 36696;
    std::cout << 36696;
    for (int i = 0; i < 36696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36697(int& val) {
    func_in_36697(val);
    const int x = 36697;
    std::cout << 36697;
    for (int i = 0; i < 36697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36697(int& val) {
    const int x = 36697;
    std::cout << 36697;
    for (int i = 0; i < 36697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36698(int& val) {
    func_in_36698(val);
    const int x = 36698;
    std::cout << 36698;
    for (int i = 0; i < 36698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36698(int& val) {
    const int x = 36698;
    std::cout << 36698;
    for (int i = 0; i < 36698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36699(int& val) {
    func_in_36699(val);
    const int x = 36699;
    std::cout << 36699;
    for (int i = 0; i < 36699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36699(int& val) {
    const int x = 36699;
    std::cout << 36699;
    for (int i = 0; i < 36699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36700(int& val) {
    func_in_36700(val);
    const int x = 36700;
    std::cout << 36700;
    for (int i = 0; i < 36700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36700(int& val) {
    const int x = 36700;
    std::cout << 36700;
    for (int i = 0; i < 36700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36701(int& val) {
    func_in_36701(val);
    const int x = 36701;
    std::cout << 36701;
    for (int i = 0; i < 36701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36701(int& val) {
    const int x = 36701;
    std::cout << 36701;
    for (int i = 0; i < 36701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36702(int& val) {
    func_in_36702(val);
    const int x = 36702;
    std::cout << 36702;
    for (int i = 0; i < 36702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36702(int& val) {
    const int x = 36702;
    std::cout << 36702;
    for (int i = 0; i < 36702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36703(int& val) {
    func_in_36703(val);
    const int x = 36703;
    std::cout << 36703;
    for (int i = 0; i < 36703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36703(int& val) {
    const int x = 36703;
    std::cout << 36703;
    for (int i = 0; i < 36703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36704(int& val) {
    func_in_36704(val);
    const int x = 36704;
    std::cout << 36704;
    for (int i = 0; i < 36704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36704(int& val) {
    const int x = 36704;
    std::cout << 36704;
    for (int i = 0; i < 36704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36705(int& val) {
    func_in_36705(val);
    const int x = 36705;
    std::cout << 36705;
    for (int i = 0; i < 36705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36705(int& val) {
    const int x = 36705;
    std::cout << 36705;
    for (int i = 0; i < 36705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36706(int& val) {
    func_in_36706(val);
    const int x = 36706;
    std::cout << 36706;
    for (int i = 0; i < 36706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36706(int& val) {
    const int x = 36706;
    std::cout << 36706;
    for (int i = 0; i < 36706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36707(int& val) {
    func_in_36707(val);
    const int x = 36707;
    std::cout << 36707;
    for (int i = 0; i < 36707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36707(int& val) {
    const int x = 36707;
    std::cout << 36707;
    for (int i = 0; i < 36707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36708(int& val) {
    func_in_36708(val);
    const int x = 36708;
    std::cout << 36708;
    for (int i = 0; i < 36708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36708(int& val) {
    const int x = 36708;
    std::cout << 36708;
    for (int i = 0; i < 36708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36709(int& val) {
    func_in_36709(val);
    const int x = 36709;
    std::cout << 36709;
    for (int i = 0; i < 36709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36709(int& val) {
    const int x = 36709;
    std::cout << 36709;
    for (int i = 0; i < 36709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36710(int& val) {
    func_in_36710(val);
    const int x = 36710;
    std::cout << 36710;
    for (int i = 0; i < 36710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36710(int& val) {
    const int x = 36710;
    std::cout << 36710;
    for (int i = 0; i < 36710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36711(int& val) {
    func_in_36711(val);
    const int x = 36711;
    std::cout << 36711;
    for (int i = 0; i < 36711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36711(int& val) {
    const int x = 36711;
    std::cout << 36711;
    for (int i = 0; i < 36711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36712(int& val) {
    func_in_36712(val);
    const int x = 36712;
    std::cout << 36712;
    for (int i = 0; i < 36712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36712(int& val) {
    const int x = 36712;
    std::cout << 36712;
    for (int i = 0; i < 36712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36713(int& val) {
    func_in_36713(val);
    const int x = 36713;
    std::cout << 36713;
    for (int i = 0; i < 36713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36713(int& val) {
    const int x = 36713;
    std::cout << 36713;
    for (int i = 0; i < 36713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36714(int& val) {
    func_in_36714(val);
    const int x = 36714;
    std::cout << 36714;
    for (int i = 0; i < 36714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36714(int& val) {
    const int x = 36714;
    std::cout << 36714;
    for (int i = 0; i < 36714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36715(int& val) {
    func_in_36715(val);
    const int x = 36715;
    std::cout << 36715;
    for (int i = 0; i < 36715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36715(int& val) {
    const int x = 36715;
    std::cout << 36715;
    for (int i = 0; i < 36715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36716(int& val) {
    func_in_36716(val);
    const int x = 36716;
    std::cout << 36716;
    for (int i = 0; i < 36716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36716(int& val) {
    const int x = 36716;
    std::cout << 36716;
    for (int i = 0; i < 36716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36717(int& val) {
    func_in_36717(val);
    const int x = 36717;
    std::cout << 36717;
    for (int i = 0; i < 36717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36717(int& val) {
    const int x = 36717;
    std::cout << 36717;
    for (int i = 0; i < 36717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36718(int& val) {
    func_in_36718(val);
    const int x = 36718;
    std::cout << 36718;
    for (int i = 0; i < 36718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36718(int& val) {
    const int x = 36718;
    std::cout << 36718;
    for (int i = 0; i < 36718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36719(int& val) {
    func_in_36719(val);
    const int x = 36719;
    std::cout << 36719;
    for (int i = 0; i < 36719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36719(int& val) {
    const int x = 36719;
    std::cout << 36719;
    for (int i = 0; i < 36719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36720(int& val) {
    func_in_36720(val);
    const int x = 36720;
    std::cout << 36720;
    for (int i = 0; i < 36720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36720(int& val) {
    const int x = 36720;
    std::cout << 36720;
    for (int i = 0; i < 36720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36721(int& val) {
    func_in_36721(val);
    const int x = 36721;
    std::cout << 36721;
    for (int i = 0; i < 36721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36721(int& val) {
    const int x = 36721;
    std::cout << 36721;
    for (int i = 0; i < 36721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36722(int& val) {
    func_in_36722(val);
    const int x = 36722;
    std::cout << 36722;
    for (int i = 0; i < 36722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36722(int& val) {
    const int x = 36722;
    std::cout << 36722;
    for (int i = 0; i < 36722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36723(int& val) {
    func_in_36723(val);
    const int x = 36723;
    std::cout << 36723;
    for (int i = 0; i < 36723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36723(int& val) {
    const int x = 36723;
    std::cout << 36723;
    for (int i = 0; i < 36723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36724(int& val) {
    func_in_36724(val);
    const int x = 36724;
    std::cout << 36724;
    for (int i = 0; i < 36724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36724(int& val) {
    const int x = 36724;
    std::cout << 36724;
    for (int i = 0; i < 36724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36725(int& val) {
    func_in_36725(val);
    const int x = 36725;
    std::cout << 36725;
    for (int i = 0; i < 36725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36725(int& val) {
    const int x = 36725;
    std::cout << 36725;
    for (int i = 0; i < 36725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36726(int& val) {
    func_in_36726(val);
    const int x = 36726;
    std::cout << 36726;
    for (int i = 0; i < 36726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36726(int& val) {
    const int x = 36726;
    std::cout << 36726;
    for (int i = 0; i < 36726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36727(int& val) {
    func_in_36727(val);
    const int x = 36727;
    std::cout << 36727;
    for (int i = 0; i < 36727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36727(int& val) {
    const int x = 36727;
    std::cout << 36727;
    for (int i = 0; i < 36727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36728(int& val) {
    func_in_36728(val);
    const int x = 36728;
    std::cout << 36728;
    for (int i = 0; i < 36728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36728(int& val) {
    const int x = 36728;
    std::cout << 36728;
    for (int i = 0; i < 36728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36729(int& val) {
    func_in_36729(val);
    const int x = 36729;
    std::cout << 36729;
    for (int i = 0; i < 36729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36729(int& val) {
    const int x = 36729;
    std::cout << 36729;
    for (int i = 0; i < 36729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36730(int& val) {
    func_in_36730(val);
    const int x = 36730;
    std::cout << 36730;
    for (int i = 0; i < 36730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36730(int& val) {
    const int x = 36730;
    std::cout << 36730;
    for (int i = 0; i < 36730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36731(int& val) {
    func_in_36731(val);
    const int x = 36731;
    std::cout << 36731;
    for (int i = 0; i < 36731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36731(int& val) {
    const int x = 36731;
    std::cout << 36731;
    for (int i = 0; i < 36731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36732(int& val) {
    func_in_36732(val);
    const int x = 36732;
    std::cout << 36732;
    for (int i = 0; i < 36732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36732(int& val) {
    const int x = 36732;
    std::cout << 36732;
    for (int i = 0; i < 36732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36733(int& val) {
    func_in_36733(val);
    const int x = 36733;
    std::cout << 36733;
    for (int i = 0; i < 36733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36733(int& val) {
    const int x = 36733;
    std::cout << 36733;
    for (int i = 0; i < 36733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36734(int& val) {
    func_in_36734(val);
    const int x = 36734;
    std::cout << 36734;
    for (int i = 0; i < 36734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36734(int& val) {
    const int x = 36734;
    std::cout << 36734;
    for (int i = 0; i < 36734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36735(int& val) {
    func_in_36735(val);
    const int x = 36735;
    std::cout << 36735;
    for (int i = 0; i < 36735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36735(int& val) {
    const int x = 36735;
    std::cout << 36735;
    for (int i = 0; i < 36735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36736(int& val) {
    func_in_36736(val);
    const int x = 36736;
    std::cout << 36736;
    for (int i = 0; i < 36736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36736(int& val) {
    const int x = 36736;
    std::cout << 36736;
    for (int i = 0; i < 36736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36737(int& val) {
    func_in_36737(val);
    const int x = 36737;
    std::cout << 36737;
    for (int i = 0; i < 36737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36737(int& val) {
    const int x = 36737;
    std::cout << 36737;
    for (int i = 0; i < 36737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36738(int& val) {
    func_in_36738(val);
    const int x = 36738;
    std::cout << 36738;
    for (int i = 0; i < 36738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36738(int& val) {
    const int x = 36738;
    std::cout << 36738;
    for (int i = 0; i < 36738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36739(int& val) {
    func_in_36739(val);
    const int x = 36739;
    std::cout << 36739;
    for (int i = 0; i < 36739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36739(int& val) {
    const int x = 36739;
    std::cout << 36739;
    for (int i = 0; i < 36739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36740(int& val) {
    func_in_36740(val);
    const int x = 36740;
    std::cout << 36740;
    for (int i = 0; i < 36740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36740(int& val) {
    const int x = 36740;
    std::cout << 36740;
    for (int i = 0; i < 36740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36741(int& val) {
    func_in_36741(val);
    const int x = 36741;
    std::cout << 36741;
    for (int i = 0; i < 36741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36741(int& val) {
    const int x = 36741;
    std::cout << 36741;
    for (int i = 0; i < 36741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36742(int& val) {
    func_in_36742(val);
    const int x = 36742;
    std::cout << 36742;
    for (int i = 0; i < 36742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36742(int& val) {
    const int x = 36742;
    std::cout << 36742;
    for (int i = 0; i < 36742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36743(int& val) {
    func_in_36743(val);
    const int x = 36743;
    std::cout << 36743;
    for (int i = 0; i < 36743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36743(int& val) {
    const int x = 36743;
    std::cout << 36743;
    for (int i = 0; i < 36743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36744(int& val) {
    func_in_36744(val);
    const int x = 36744;
    std::cout << 36744;
    for (int i = 0; i < 36744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36744(int& val) {
    const int x = 36744;
    std::cout << 36744;
    for (int i = 0; i < 36744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36745(int& val) {
    func_in_36745(val);
    const int x = 36745;
    std::cout << 36745;
    for (int i = 0; i < 36745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36745(int& val) {
    const int x = 36745;
    std::cout << 36745;
    for (int i = 0; i < 36745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36746(int& val) {
    func_in_36746(val);
    const int x = 36746;
    std::cout << 36746;
    for (int i = 0; i < 36746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36746(int& val) {
    const int x = 36746;
    std::cout << 36746;
    for (int i = 0; i < 36746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36747(int& val) {
    func_in_36747(val);
    const int x = 36747;
    std::cout << 36747;
    for (int i = 0; i < 36747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36747(int& val) {
    const int x = 36747;
    std::cout << 36747;
    for (int i = 0; i < 36747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36748(int& val) {
    func_in_36748(val);
    const int x = 36748;
    std::cout << 36748;
    for (int i = 0; i < 36748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36748(int& val) {
    const int x = 36748;
    std::cout << 36748;
    for (int i = 0; i < 36748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36749(int& val) {
    func_in_36749(val);
    const int x = 36749;
    std::cout << 36749;
    for (int i = 0; i < 36749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36749(int& val) {
    const int x = 36749;
    std::cout << 36749;
    for (int i = 0; i < 36749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36750(int& val) {
    func_in_36750(val);
    const int x = 36750;
    std::cout << 36750;
    for (int i = 0; i < 36750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36750(int& val) {
    const int x = 36750;
    std::cout << 36750;
    for (int i = 0; i < 36750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36751(int& val) {
    func_in_36751(val);
    const int x = 36751;
    std::cout << 36751;
    for (int i = 0; i < 36751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36751(int& val) {
    const int x = 36751;
    std::cout << 36751;
    for (int i = 0; i < 36751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36752(int& val) {
    func_in_36752(val);
    const int x = 36752;
    std::cout << 36752;
    for (int i = 0; i < 36752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36752(int& val) {
    const int x = 36752;
    std::cout << 36752;
    for (int i = 0; i < 36752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36753(int& val) {
    func_in_36753(val);
    const int x = 36753;
    std::cout << 36753;
    for (int i = 0; i < 36753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36753(int& val) {
    const int x = 36753;
    std::cout << 36753;
    for (int i = 0; i < 36753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36754(int& val) {
    func_in_36754(val);
    const int x = 36754;
    std::cout << 36754;
    for (int i = 0; i < 36754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36754(int& val) {
    const int x = 36754;
    std::cout << 36754;
    for (int i = 0; i < 36754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36755(int& val) {
    func_in_36755(val);
    const int x = 36755;
    std::cout << 36755;
    for (int i = 0; i < 36755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36755(int& val) {
    const int x = 36755;
    std::cout << 36755;
    for (int i = 0; i < 36755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36756(int& val) {
    func_in_36756(val);
    const int x = 36756;
    std::cout << 36756;
    for (int i = 0; i < 36756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36756(int& val) {
    const int x = 36756;
    std::cout << 36756;
    for (int i = 0; i < 36756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36757(int& val) {
    func_in_36757(val);
    const int x = 36757;
    std::cout << 36757;
    for (int i = 0; i < 36757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36757(int& val) {
    const int x = 36757;
    std::cout << 36757;
    for (int i = 0; i < 36757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36758(int& val) {
    func_in_36758(val);
    const int x = 36758;
    std::cout << 36758;
    for (int i = 0; i < 36758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36758(int& val) {
    const int x = 36758;
    std::cout << 36758;
    for (int i = 0; i < 36758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36759(int& val) {
    func_in_36759(val);
    const int x = 36759;
    std::cout << 36759;
    for (int i = 0; i < 36759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36759(int& val) {
    const int x = 36759;
    std::cout << 36759;
    for (int i = 0; i < 36759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36760(int& val) {
    func_in_36760(val);
    const int x = 36760;
    std::cout << 36760;
    for (int i = 0; i < 36760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36760(int& val) {
    const int x = 36760;
    std::cout << 36760;
    for (int i = 0; i < 36760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36761(int& val) {
    func_in_36761(val);
    const int x = 36761;
    std::cout << 36761;
    for (int i = 0; i < 36761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36761(int& val) {
    const int x = 36761;
    std::cout << 36761;
    for (int i = 0; i < 36761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36762(int& val) {
    func_in_36762(val);
    const int x = 36762;
    std::cout << 36762;
    for (int i = 0; i < 36762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36762(int& val) {
    const int x = 36762;
    std::cout << 36762;
    for (int i = 0; i < 36762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36763(int& val) {
    func_in_36763(val);
    const int x = 36763;
    std::cout << 36763;
    for (int i = 0; i < 36763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36763(int& val) {
    const int x = 36763;
    std::cout << 36763;
    for (int i = 0; i < 36763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36764(int& val) {
    func_in_36764(val);
    const int x = 36764;
    std::cout << 36764;
    for (int i = 0; i < 36764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36764(int& val) {
    const int x = 36764;
    std::cout << 36764;
    for (int i = 0; i < 36764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36765(int& val) {
    func_in_36765(val);
    const int x = 36765;
    std::cout << 36765;
    for (int i = 0; i < 36765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36765(int& val) {
    const int x = 36765;
    std::cout << 36765;
    for (int i = 0; i < 36765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36766(int& val) {
    func_in_36766(val);
    const int x = 36766;
    std::cout << 36766;
    for (int i = 0; i < 36766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36766(int& val) {
    const int x = 36766;
    std::cout << 36766;
    for (int i = 0; i < 36766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36767(int& val) {
    func_in_36767(val);
    const int x = 36767;
    std::cout << 36767;
    for (int i = 0; i < 36767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36767(int& val) {
    const int x = 36767;
    std::cout << 36767;
    for (int i = 0; i < 36767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36768(int& val) {
    func_in_36768(val);
    const int x = 36768;
    std::cout << 36768;
    for (int i = 0; i < 36768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36768(int& val) {
    const int x = 36768;
    std::cout << 36768;
    for (int i = 0; i < 36768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36769(int& val) {
    func_in_36769(val);
    const int x = 36769;
    std::cout << 36769;
    for (int i = 0; i < 36769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36769(int& val) {
    const int x = 36769;
    std::cout << 36769;
    for (int i = 0; i < 36769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36770(int& val) {
    func_in_36770(val);
    const int x = 36770;
    std::cout << 36770;
    for (int i = 0; i < 36770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36770(int& val) {
    const int x = 36770;
    std::cout << 36770;
    for (int i = 0; i < 36770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36771(int& val) {
    func_in_36771(val);
    const int x = 36771;
    std::cout << 36771;
    for (int i = 0; i < 36771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36771(int& val) {
    const int x = 36771;
    std::cout << 36771;
    for (int i = 0; i < 36771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36772(int& val) {
    func_in_36772(val);
    const int x = 36772;
    std::cout << 36772;
    for (int i = 0; i < 36772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36772(int& val) {
    const int x = 36772;
    std::cout << 36772;
    for (int i = 0; i < 36772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36773(int& val) {
    func_in_36773(val);
    const int x = 36773;
    std::cout << 36773;
    for (int i = 0; i < 36773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36773(int& val) {
    const int x = 36773;
    std::cout << 36773;
    for (int i = 0; i < 36773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36774(int& val) {
    func_in_36774(val);
    const int x = 36774;
    std::cout << 36774;
    for (int i = 0; i < 36774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36774(int& val) {
    const int x = 36774;
    std::cout << 36774;
    for (int i = 0; i < 36774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36775(int& val) {
    func_in_36775(val);
    const int x = 36775;
    std::cout << 36775;
    for (int i = 0; i < 36775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36775(int& val) {
    const int x = 36775;
    std::cout << 36775;
    for (int i = 0; i < 36775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36776(int& val) {
    func_in_36776(val);
    const int x = 36776;
    std::cout << 36776;
    for (int i = 0; i < 36776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36776(int& val) {
    const int x = 36776;
    std::cout << 36776;
    for (int i = 0; i < 36776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36777(int& val) {
    func_in_36777(val);
    const int x = 36777;
    std::cout << 36777;
    for (int i = 0; i < 36777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36777(int& val) {
    const int x = 36777;
    std::cout << 36777;
    for (int i = 0; i < 36777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36778(int& val) {
    func_in_36778(val);
    const int x = 36778;
    std::cout << 36778;
    for (int i = 0; i < 36778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36778(int& val) {
    const int x = 36778;
    std::cout << 36778;
    for (int i = 0; i < 36778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36779(int& val) {
    func_in_36779(val);
    const int x = 36779;
    std::cout << 36779;
    for (int i = 0; i < 36779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36779(int& val) {
    const int x = 36779;
    std::cout << 36779;
    for (int i = 0; i < 36779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36780(int& val) {
    func_in_36780(val);
    const int x = 36780;
    std::cout << 36780;
    for (int i = 0; i < 36780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36780(int& val) {
    const int x = 36780;
    std::cout << 36780;
    for (int i = 0; i < 36780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36781(int& val) {
    func_in_36781(val);
    const int x = 36781;
    std::cout << 36781;
    for (int i = 0; i < 36781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36781(int& val) {
    const int x = 36781;
    std::cout << 36781;
    for (int i = 0; i < 36781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36782(int& val) {
    func_in_36782(val);
    const int x = 36782;
    std::cout << 36782;
    for (int i = 0; i < 36782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36782(int& val) {
    const int x = 36782;
    std::cout << 36782;
    for (int i = 0; i < 36782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36783(int& val) {
    func_in_36783(val);
    const int x = 36783;
    std::cout << 36783;
    for (int i = 0; i < 36783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36783(int& val) {
    const int x = 36783;
    std::cout << 36783;
    for (int i = 0; i < 36783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36784(int& val) {
    func_in_36784(val);
    const int x = 36784;
    std::cout << 36784;
    for (int i = 0; i < 36784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36784(int& val) {
    const int x = 36784;
    std::cout << 36784;
    for (int i = 0; i < 36784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36785(int& val) {
    func_in_36785(val);
    const int x = 36785;
    std::cout << 36785;
    for (int i = 0; i < 36785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36785(int& val) {
    const int x = 36785;
    std::cout << 36785;
    for (int i = 0; i < 36785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36786(int& val) {
    func_in_36786(val);
    const int x = 36786;
    std::cout << 36786;
    for (int i = 0; i < 36786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36786(int& val) {
    const int x = 36786;
    std::cout << 36786;
    for (int i = 0; i < 36786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36787(int& val) {
    func_in_36787(val);
    const int x = 36787;
    std::cout << 36787;
    for (int i = 0; i < 36787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36787(int& val) {
    const int x = 36787;
    std::cout << 36787;
    for (int i = 0; i < 36787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36788(int& val) {
    func_in_36788(val);
    const int x = 36788;
    std::cout << 36788;
    for (int i = 0; i < 36788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36788(int& val) {
    const int x = 36788;
    std::cout << 36788;
    for (int i = 0; i < 36788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36789(int& val) {
    func_in_36789(val);
    const int x = 36789;
    std::cout << 36789;
    for (int i = 0; i < 36789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36789(int& val) {
    const int x = 36789;
    std::cout << 36789;
    for (int i = 0; i < 36789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36790(int& val) {
    func_in_36790(val);
    const int x = 36790;
    std::cout << 36790;
    for (int i = 0; i < 36790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36790(int& val) {
    const int x = 36790;
    std::cout << 36790;
    for (int i = 0; i < 36790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36791(int& val) {
    func_in_36791(val);
    const int x = 36791;
    std::cout << 36791;
    for (int i = 0; i < 36791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36791(int& val) {
    const int x = 36791;
    std::cout << 36791;
    for (int i = 0; i < 36791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36792(int& val) {
    func_in_36792(val);
    const int x = 36792;
    std::cout << 36792;
    for (int i = 0; i < 36792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36792(int& val) {
    const int x = 36792;
    std::cout << 36792;
    for (int i = 0; i < 36792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36793(int& val) {
    func_in_36793(val);
    const int x = 36793;
    std::cout << 36793;
    for (int i = 0; i < 36793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36793(int& val) {
    const int x = 36793;
    std::cout << 36793;
    for (int i = 0; i < 36793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36794(int& val) {
    func_in_36794(val);
    const int x = 36794;
    std::cout << 36794;
    for (int i = 0; i < 36794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36794(int& val) {
    const int x = 36794;
    std::cout << 36794;
    for (int i = 0; i < 36794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36795(int& val) {
    func_in_36795(val);
    const int x = 36795;
    std::cout << 36795;
    for (int i = 0; i < 36795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36795(int& val) {
    const int x = 36795;
    std::cout << 36795;
    for (int i = 0; i < 36795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36796(int& val) {
    func_in_36796(val);
    const int x = 36796;
    std::cout << 36796;
    for (int i = 0; i < 36796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36796(int& val) {
    const int x = 36796;
    std::cout << 36796;
    for (int i = 0; i < 36796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36797(int& val) {
    func_in_36797(val);
    const int x = 36797;
    std::cout << 36797;
    for (int i = 0; i < 36797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36797(int& val) {
    const int x = 36797;
    std::cout << 36797;
    for (int i = 0; i < 36797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36798(int& val) {
    func_in_36798(val);
    const int x = 36798;
    std::cout << 36798;
    for (int i = 0; i < 36798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36798(int& val) {
    const int x = 36798;
    std::cout << 36798;
    for (int i = 0; i < 36798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36799(int& val) {
    func_in_36799(val);
    const int x = 36799;
    std::cout << 36799;
    for (int i = 0; i < 36799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36799(int& val) {
    const int x = 36799;
    std::cout << 36799;
    for (int i = 0; i < 36799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36800(int& val) {
    func_in_36800(val);
    const int x = 36800;
    std::cout << 36800;
    for (int i = 0; i < 36800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36800(int& val) {
    const int x = 36800;
    std::cout << 36800;
    for (int i = 0; i < 36800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36801(int& val) {
    func_in_36801(val);
    const int x = 36801;
    std::cout << 36801;
    for (int i = 0; i < 36801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36801(int& val) {
    const int x = 36801;
    std::cout << 36801;
    for (int i = 0; i < 36801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36802(int& val) {
    func_in_36802(val);
    const int x = 36802;
    std::cout << 36802;
    for (int i = 0; i < 36802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36802(int& val) {
    const int x = 36802;
    std::cout << 36802;
    for (int i = 0; i < 36802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36803(int& val) {
    func_in_36803(val);
    const int x = 36803;
    std::cout << 36803;
    for (int i = 0; i < 36803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36803(int& val) {
    const int x = 36803;
    std::cout << 36803;
    for (int i = 0; i < 36803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36804(int& val) {
    func_in_36804(val);
    const int x = 36804;
    std::cout << 36804;
    for (int i = 0; i < 36804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36804(int& val) {
    const int x = 36804;
    std::cout << 36804;
    for (int i = 0; i < 36804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36805(int& val) {
    func_in_36805(val);
    const int x = 36805;
    std::cout << 36805;
    for (int i = 0; i < 36805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36805(int& val) {
    const int x = 36805;
    std::cout << 36805;
    for (int i = 0; i < 36805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36806(int& val) {
    func_in_36806(val);
    const int x = 36806;
    std::cout << 36806;
    for (int i = 0; i < 36806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36806(int& val) {
    const int x = 36806;
    std::cout << 36806;
    for (int i = 0; i < 36806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36807(int& val) {
    func_in_36807(val);
    const int x = 36807;
    std::cout << 36807;
    for (int i = 0; i < 36807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36807(int& val) {
    const int x = 36807;
    std::cout << 36807;
    for (int i = 0; i < 36807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36808(int& val) {
    func_in_36808(val);
    const int x = 36808;
    std::cout << 36808;
    for (int i = 0; i < 36808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36808(int& val) {
    const int x = 36808;
    std::cout << 36808;
    for (int i = 0; i < 36808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36809(int& val) {
    func_in_36809(val);
    const int x = 36809;
    std::cout << 36809;
    for (int i = 0; i < 36809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36809(int& val) {
    const int x = 36809;
    std::cout << 36809;
    for (int i = 0; i < 36809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36810(int& val) {
    func_in_36810(val);
    const int x = 36810;
    std::cout << 36810;
    for (int i = 0; i < 36810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36810(int& val) {
    const int x = 36810;
    std::cout << 36810;
    for (int i = 0; i < 36810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36811(int& val) {
    func_in_36811(val);
    const int x = 36811;
    std::cout << 36811;
    for (int i = 0; i < 36811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36811(int& val) {
    const int x = 36811;
    std::cout << 36811;
    for (int i = 0; i < 36811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36812(int& val) {
    func_in_36812(val);
    const int x = 36812;
    std::cout << 36812;
    for (int i = 0; i < 36812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36812(int& val) {
    const int x = 36812;
    std::cout << 36812;
    for (int i = 0; i < 36812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36813(int& val) {
    func_in_36813(val);
    const int x = 36813;
    std::cout << 36813;
    for (int i = 0; i < 36813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36813(int& val) {
    const int x = 36813;
    std::cout << 36813;
    for (int i = 0; i < 36813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36814(int& val) {
    func_in_36814(val);
    const int x = 36814;
    std::cout << 36814;
    for (int i = 0; i < 36814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36814(int& val) {
    const int x = 36814;
    std::cout << 36814;
    for (int i = 0; i < 36814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36815(int& val) {
    func_in_36815(val);
    const int x = 36815;
    std::cout << 36815;
    for (int i = 0; i < 36815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36815(int& val) {
    const int x = 36815;
    std::cout << 36815;
    for (int i = 0; i < 36815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36816(int& val) {
    func_in_36816(val);
    const int x = 36816;
    std::cout << 36816;
    for (int i = 0; i < 36816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36816(int& val) {
    const int x = 36816;
    std::cout << 36816;
    for (int i = 0; i < 36816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36817(int& val) {
    func_in_36817(val);
    const int x = 36817;
    std::cout << 36817;
    for (int i = 0; i < 36817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36817(int& val) {
    const int x = 36817;
    std::cout << 36817;
    for (int i = 0; i < 36817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36818(int& val) {
    func_in_36818(val);
    const int x = 36818;
    std::cout << 36818;
    for (int i = 0; i < 36818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36818(int& val) {
    const int x = 36818;
    std::cout << 36818;
    for (int i = 0; i < 36818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36819(int& val) {
    func_in_36819(val);
    const int x = 36819;
    std::cout << 36819;
    for (int i = 0; i < 36819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36819(int& val) {
    const int x = 36819;
    std::cout << 36819;
    for (int i = 0; i < 36819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36820(int& val) {
    func_in_36820(val);
    const int x = 36820;
    std::cout << 36820;
    for (int i = 0; i < 36820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36820(int& val) {
    const int x = 36820;
    std::cout << 36820;
    for (int i = 0; i < 36820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36821(int& val) {
    func_in_36821(val);
    const int x = 36821;
    std::cout << 36821;
    for (int i = 0; i < 36821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36821(int& val) {
    const int x = 36821;
    std::cout << 36821;
    for (int i = 0; i < 36821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36822(int& val) {
    func_in_36822(val);
    const int x = 36822;
    std::cout << 36822;
    for (int i = 0; i < 36822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36822(int& val) {
    const int x = 36822;
    std::cout << 36822;
    for (int i = 0; i < 36822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36823(int& val) {
    func_in_36823(val);
    const int x = 36823;
    std::cout << 36823;
    for (int i = 0; i < 36823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36823(int& val) {
    const int x = 36823;
    std::cout << 36823;
    for (int i = 0; i < 36823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36824(int& val) {
    func_in_36824(val);
    const int x = 36824;
    std::cout << 36824;
    for (int i = 0; i < 36824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36824(int& val) {
    const int x = 36824;
    std::cout << 36824;
    for (int i = 0; i < 36824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36825(int& val) {
    func_in_36825(val);
    const int x = 36825;
    std::cout << 36825;
    for (int i = 0; i < 36825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36825(int& val) {
    const int x = 36825;
    std::cout << 36825;
    for (int i = 0; i < 36825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36826(int& val) {
    func_in_36826(val);
    const int x = 36826;
    std::cout << 36826;
    for (int i = 0; i < 36826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36826(int& val) {
    const int x = 36826;
    std::cout << 36826;
    for (int i = 0; i < 36826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36827(int& val) {
    func_in_36827(val);
    const int x = 36827;
    std::cout << 36827;
    for (int i = 0; i < 36827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36827(int& val) {
    const int x = 36827;
    std::cout << 36827;
    for (int i = 0; i < 36827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36828(int& val) {
    func_in_36828(val);
    const int x = 36828;
    std::cout << 36828;
    for (int i = 0; i < 36828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36828(int& val) {
    const int x = 36828;
    std::cout << 36828;
    for (int i = 0; i < 36828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36829(int& val) {
    func_in_36829(val);
    const int x = 36829;
    std::cout << 36829;
    for (int i = 0; i < 36829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36829(int& val) {
    const int x = 36829;
    std::cout << 36829;
    for (int i = 0; i < 36829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36830(int& val) {
    func_in_36830(val);
    const int x = 36830;
    std::cout << 36830;
    for (int i = 0; i < 36830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36830(int& val) {
    const int x = 36830;
    std::cout << 36830;
    for (int i = 0; i < 36830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36831(int& val) {
    func_in_36831(val);
    const int x = 36831;
    std::cout << 36831;
    for (int i = 0; i < 36831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36831(int& val) {
    const int x = 36831;
    std::cout << 36831;
    for (int i = 0; i < 36831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36832(int& val) {
    func_in_36832(val);
    const int x = 36832;
    std::cout << 36832;
    for (int i = 0; i < 36832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36832(int& val) {
    const int x = 36832;
    std::cout << 36832;
    for (int i = 0; i < 36832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36833(int& val) {
    func_in_36833(val);
    const int x = 36833;
    std::cout << 36833;
    for (int i = 0; i < 36833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36833(int& val) {
    const int x = 36833;
    std::cout << 36833;
    for (int i = 0; i < 36833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36834(int& val) {
    func_in_36834(val);
    const int x = 36834;
    std::cout << 36834;
    for (int i = 0; i < 36834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36834(int& val) {
    const int x = 36834;
    std::cout << 36834;
    for (int i = 0; i < 36834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36835(int& val) {
    func_in_36835(val);
    const int x = 36835;
    std::cout << 36835;
    for (int i = 0; i < 36835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36835(int& val) {
    const int x = 36835;
    std::cout << 36835;
    for (int i = 0; i < 36835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36836(int& val) {
    func_in_36836(val);
    const int x = 36836;
    std::cout << 36836;
    for (int i = 0; i < 36836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36836(int& val) {
    const int x = 36836;
    std::cout << 36836;
    for (int i = 0; i < 36836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36837(int& val) {
    func_in_36837(val);
    const int x = 36837;
    std::cout << 36837;
    for (int i = 0; i < 36837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36837(int& val) {
    const int x = 36837;
    std::cout << 36837;
    for (int i = 0; i < 36837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36838(int& val) {
    func_in_36838(val);
    const int x = 36838;
    std::cout << 36838;
    for (int i = 0; i < 36838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36838(int& val) {
    const int x = 36838;
    std::cout << 36838;
    for (int i = 0; i < 36838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36839(int& val) {
    func_in_36839(val);
    const int x = 36839;
    std::cout << 36839;
    for (int i = 0; i < 36839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36839(int& val) {
    const int x = 36839;
    std::cout << 36839;
    for (int i = 0; i < 36839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36840(int& val) {
    func_in_36840(val);
    const int x = 36840;
    std::cout << 36840;
    for (int i = 0; i < 36840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36840(int& val) {
    const int x = 36840;
    std::cout << 36840;
    for (int i = 0; i < 36840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36841(int& val) {
    func_in_36841(val);
    const int x = 36841;
    std::cout << 36841;
    for (int i = 0; i < 36841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36841(int& val) {
    const int x = 36841;
    std::cout << 36841;
    for (int i = 0; i < 36841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36842(int& val) {
    func_in_36842(val);
    const int x = 36842;
    std::cout << 36842;
    for (int i = 0; i < 36842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36842(int& val) {
    const int x = 36842;
    std::cout << 36842;
    for (int i = 0; i < 36842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36843(int& val) {
    func_in_36843(val);
    const int x = 36843;
    std::cout << 36843;
    for (int i = 0; i < 36843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36843(int& val) {
    const int x = 36843;
    std::cout << 36843;
    for (int i = 0; i < 36843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36844(int& val) {
    func_in_36844(val);
    const int x = 36844;
    std::cout << 36844;
    for (int i = 0; i < 36844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36844(int& val) {
    const int x = 36844;
    std::cout << 36844;
    for (int i = 0; i < 36844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36845(int& val) {
    func_in_36845(val);
    const int x = 36845;
    std::cout << 36845;
    for (int i = 0; i < 36845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36845(int& val) {
    const int x = 36845;
    std::cout << 36845;
    for (int i = 0; i < 36845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36846(int& val) {
    func_in_36846(val);
    const int x = 36846;
    std::cout << 36846;
    for (int i = 0; i < 36846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36846(int& val) {
    const int x = 36846;
    std::cout << 36846;
    for (int i = 0; i < 36846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36847(int& val) {
    func_in_36847(val);
    const int x = 36847;
    std::cout << 36847;
    for (int i = 0; i < 36847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36847(int& val) {
    const int x = 36847;
    std::cout << 36847;
    for (int i = 0; i < 36847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36848(int& val) {
    func_in_36848(val);
    const int x = 36848;
    std::cout << 36848;
    for (int i = 0; i < 36848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36848(int& val) {
    const int x = 36848;
    std::cout << 36848;
    for (int i = 0; i < 36848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36849(int& val) {
    func_in_36849(val);
    const int x = 36849;
    std::cout << 36849;
    for (int i = 0; i < 36849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36849(int& val) {
    const int x = 36849;
    std::cout << 36849;
    for (int i = 0; i < 36849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36850(int& val) {
    func_in_36850(val);
    const int x = 36850;
    std::cout << 36850;
    for (int i = 0; i < 36850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36850(int& val) {
    const int x = 36850;
    std::cout << 36850;
    for (int i = 0; i < 36850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36851(int& val) {
    func_in_36851(val);
    const int x = 36851;
    std::cout << 36851;
    for (int i = 0; i < 36851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36851(int& val) {
    const int x = 36851;
    std::cout << 36851;
    for (int i = 0; i < 36851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36852(int& val) {
    func_in_36852(val);
    const int x = 36852;
    std::cout << 36852;
    for (int i = 0; i < 36852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36852(int& val) {
    const int x = 36852;
    std::cout << 36852;
    for (int i = 0; i < 36852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36853(int& val) {
    func_in_36853(val);
    const int x = 36853;
    std::cout << 36853;
    for (int i = 0; i < 36853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36853(int& val) {
    const int x = 36853;
    std::cout << 36853;
    for (int i = 0; i < 36853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36854(int& val) {
    func_in_36854(val);
    const int x = 36854;
    std::cout << 36854;
    for (int i = 0; i < 36854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36854(int& val) {
    const int x = 36854;
    std::cout << 36854;
    for (int i = 0; i < 36854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36855(int& val) {
    func_in_36855(val);
    const int x = 36855;
    std::cout << 36855;
    for (int i = 0; i < 36855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36855(int& val) {
    const int x = 36855;
    std::cout << 36855;
    for (int i = 0; i < 36855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36856(int& val) {
    func_in_36856(val);
    const int x = 36856;
    std::cout << 36856;
    for (int i = 0; i < 36856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36856(int& val) {
    const int x = 36856;
    std::cout << 36856;
    for (int i = 0; i < 36856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36857(int& val) {
    func_in_36857(val);
    const int x = 36857;
    std::cout << 36857;
    for (int i = 0; i < 36857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36857(int& val) {
    const int x = 36857;
    std::cout << 36857;
    for (int i = 0; i < 36857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36858(int& val) {
    func_in_36858(val);
    const int x = 36858;
    std::cout << 36858;
    for (int i = 0; i < 36858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36858(int& val) {
    const int x = 36858;
    std::cout << 36858;
    for (int i = 0; i < 36858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36859(int& val) {
    func_in_36859(val);
    const int x = 36859;
    std::cout << 36859;
    for (int i = 0; i < 36859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36859(int& val) {
    const int x = 36859;
    std::cout << 36859;
    for (int i = 0; i < 36859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36860(int& val) {
    func_in_36860(val);
    const int x = 36860;
    std::cout << 36860;
    for (int i = 0; i < 36860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36860(int& val) {
    const int x = 36860;
    std::cout << 36860;
    for (int i = 0; i < 36860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36861(int& val) {
    func_in_36861(val);
    const int x = 36861;
    std::cout << 36861;
    for (int i = 0; i < 36861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36861(int& val) {
    const int x = 36861;
    std::cout << 36861;
    for (int i = 0; i < 36861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36862(int& val) {
    func_in_36862(val);
    const int x = 36862;
    std::cout << 36862;
    for (int i = 0; i < 36862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36862(int& val) {
    const int x = 36862;
    std::cout << 36862;
    for (int i = 0; i < 36862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36863(int& val) {
    func_in_36863(val);
    const int x = 36863;
    std::cout << 36863;
    for (int i = 0; i < 36863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36863(int& val) {
    const int x = 36863;
    std::cout << 36863;
    for (int i = 0; i < 36863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36864(int& val) {
    func_in_36864(val);
    const int x = 36864;
    std::cout << 36864;
    for (int i = 0; i < 36864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36864(int& val) {
    const int x = 36864;
    std::cout << 36864;
    for (int i = 0; i < 36864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36865(int& val) {
    func_in_36865(val);
    const int x = 36865;
    std::cout << 36865;
    for (int i = 0; i < 36865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36865(int& val) {
    const int x = 36865;
    std::cout << 36865;
    for (int i = 0; i < 36865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36866(int& val) {
    func_in_36866(val);
    const int x = 36866;
    std::cout << 36866;
    for (int i = 0; i < 36866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36866(int& val) {
    const int x = 36866;
    std::cout << 36866;
    for (int i = 0; i < 36866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36867(int& val) {
    func_in_36867(val);
    const int x = 36867;
    std::cout << 36867;
    for (int i = 0; i < 36867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36867(int& val) {
    const int x = 36867;
    std::cout << 36867;
    for (int i = 0; i < 36867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36868(int& val) {
    func_in_36868(val);
    const int x = 36868;
    std::cout << 36868;
    for (int i = 0; i < 36868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36868(int& val) {
    const int x = 36868;
    std::cout << 36868;
    for (int i = 0; i < 36868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36869(int& val) {
    func_in_36869(val);
    const int x = 36869;
    std::cout << 36869;
    for (int i = 0; i < 36869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36869(int& val) {
    const int x = 36869;
    std::cout << 36869;
    for (int i = 0; i < 36869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36870(int& val) {
    func_in_36870(val);
    const int x = 36870;
    std::cout << 36870;
    for (int i = 0; i < 36870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36870(int& val) {
    const int x = 36870;
    std::cout << 36870;
    for (int i = 0; i < 36870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36871(int& val) {
    func_in_36871(val);
    const int x = 36871;
    std::cout << 36871;
    for (int i = 0; i < 36871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36871(int& val) {
    const int x = 36871;
    std::cout << 36871;
    for (int i = 0; i < 36871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36872(int& val) {
    func_in_36872(val);
    const int x = 36872;
    std::cout << 36872;
    for (int i = 0; i < 36872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36872(int& val) {
    const int x = 36872;
    std::cout << 36872;
    for (int i = 0; i < 36872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36873(int& val) {
    func_in_36873(val);
    const int x = 36873;
    std::cout << 36873;
    for (int i = 0; i < 36873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36873(int& val) {
    const int x = 36873;
    std::cout << 36873;
    for (int i = 0; i < 36873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36874(int& val) {
    func_in_36874(val);
    const int x = 36874;
    std::cout << 36874;
    for (int i = 0; i < 36874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36874(int& val) {
    const int x = 36874;
    std::cout << 36874;
    for (int i = 0; i < 36874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36875(int& val) {
    func_in_36875(val);
    const int x = 36875;
    std::cout << 36875;
    for (int i = 0; i < 36875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36875(int& val) {
    const int x = 36875;
    std::cout << 36875;
    for (int i = 0; i < 36875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36876(int& val) {
    func_in_36876(val);
    const int x = 36876;
    std::cout << 36876;
    for (int i = 0; i < 36876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36876(int& val) {
    const int x = 36876;
    std::cout << 36876;
    for (int i = 0; i < 36876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36877(int& val) {
    func_in_36877(val);
    const int x = 36877;
    std::cout << 36877;
    for (int i = 0; i < 36877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36877(int& val) {
    const int x = 36877;
    std::cout << 36877;
    for (int i = 0; i < 36877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36878(int& val) {
    func_in_36878(val);
    const int x = 36878;
    std::cout << 36878;
    for (int i = 0; i < 36878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36878(int& val) {
    const int x = 36878;
    std::cout << 36878;
    for (int i = 0; i < 36878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36879(int& val) {
    func_in_36879(val);
    const int x = 36879;
    std::cout << 36879;
    for (int i = 0; i < 36879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36879(int& val) {
    const int x = 36879;
    std::cout << 36879;
    for (int i = 0; i < 36879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36880(int& val) {
    func_in_36880(val);
    const int x = 36880;
    std::cout << 36880;
    for (int i = 0; i < 36880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36880(int& val) {
    const int x = 36880;
    std::cout << 36880;
    for (int i = 0; i < 36880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36881(int& val) {
    func_in_36881(val);
    const int x = 36881;
    std::cout << 36881;
    for (int i = 0; i < 36881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36881(int& val) {
    const int x = 36881;
    std::cout << 36881;
    for (int i = 0; i < 36881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36882(int& val) {
    func_in_36882(val);
    const int x = 36882;
    std::cout << 36882;
    for (int i = 0; i < 36882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36882(int& val) {
    const int x = 36882;
    std::cout << 36882;
    for (int i = 0; i < 36882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36883(int& val) {
    func_in_36883(val);
    const int x = 36883;
    std::cout << 36883;
    for (int i = 0; i < 36883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36883(int& val) {
    const int x = 36883;
    std::cout << 36883;
    for (int i = 0; i < 36883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36884(int& val) {
    func_in_36884(val);
    const int x = 36884;
    std::cout << 36884;
    for (int i = 0; i < 36884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36884(int& val) {
    const int x = 36884;
    std::cout << 36884;
    for (int i = 0; i < 36884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36885(int& val) {
    func_in_36885(val);
    const int x = 36885;
    std::cout << 36885;
    for (int i = 0; i < 36885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36885(int& val) {
    const int x = 36885;
    std::cout << 36885;
    for (int i = 0; i < 36885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36886(int& val) {
    func_in_36886(val);
    const int x = 36886;
    std::cout << 36886;
    for (int i = 0; i < 36886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36886(int& val) {
    const int x = 36886;
    std::cout << 36886;
    for (int i = 0; i < 36886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36887(int& val) {
    func_in_36887(val);
    const int x = 36887;
    std::cout << 36887;
    for (int i = 0; i < 36887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36887(int& val) {
    const int x = 36887;
    std::cout << 36887;
    for (int i = 0; i < 36887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36888(int& val) {
    func_in_36888(val);
    const int x = 36888;
    std::cout << 36888;
    for (int i = 0; i < 36888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36888(int& val) {
    const int x = 36888;
    std::cout << 36888;
    for (int i = 0; i < 36888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36889(int& val) {
    func_in_36889(val);
    const int x = 36889;
    std::cout << 36889;
    for (int i = 0; i < 36889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36889(int& val) {
    const int x = 36889;
    std::cout << 36889;
    for (int i = 0; i < 36889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36890(int& val) {
    func_in_36890(val);
    const int x = 36890;
    std::cout << 36890;
    for (int i = 0; i < 36890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36890(int& val) {
    const int x = 36890;
    std::cout << 36890;
    for (int i = 0; i < 36890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36891(int& val) {
    func_in_36891(val);
    const int x = 36891;
    std::cout << 36891;
    for (int i = 0; i < 36891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36891(int& val) {
    const int x = 36891;
    std::cout << 36891;
    for (int i = 0; i < 36891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36892(int& val) {
    func_in_36892(val);
    const int x = 36892;
    std::cout << 36892;
    for (int i = 0; i < 36892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36892(int& val) {
    const int x = 36892;
    std::cout << 36892;
    for (int i = 0; i < 36892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36893(int& val) {
    func_in_36893(val);
    const int x = 36893;
    std::cout << 36893;
    for (int i = 0; i < 36893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36893(int& val) {
    const int x = 36893;
    std::cout << 36893;
    for (int i = 0; i < 36893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36894(int& val) {
    func_in_36894(val);
    const int x = 36894;
    std::cout << 36894;
    for (int i = 0; i < 36894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36894(int& val) {
    const int x = 36894;
    std::cout << 36894;
    for (int i = 0; i < 36894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36895(int& val) {
    func_in_36895(val);
    const int x = 36895;
    std::cout << 36895;
    for (int i = 0; i < 36895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36895(int& val) {
    const int x = 36895;
    std::cout << 36895;
    for (int i = 0; i < 36895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36896(int& val) {
    func_in_36896(val);
    const int x = 36896;
    std::cout << 36896;
    for (int i = 0; i < 36896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36896(int& val) {
    const int x = 36896;
    std::cout << 36896;
    for (int i = 0; i < 36896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36897(int& val) {
    func_in_36897(val);
    const int x = 36897;
    std::cout << 36897;
    for (int i = 0; i < 36897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36897(int& val) {
    const int x = 36897;
    std::cout << 36897;
    for (int i = 0; i < 36897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36898(int& val) {
    func_in_36898(val);
    const int x = 36898;
    std::cout << 36898;
    for (int i = 0; i < 36898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36898(int& val) {
    const int x = 36898;
    std::cout << 36898;
    for (int i = 0; i < 36898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36899(int& val) {
    func_in_36899(val);
    const int x = 36899;
    std::cout << 36899;
    for (int i = 0; i < 36899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36899(int& val) {
    const int x = 36899;
    std::cout << 36899;
    for (int i = 0; i < 36899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36900(int& val) {
    func_in_36900(val);
    const int x = 36900;
    std::cout << 36900;
    for (int i = 0; i < 36900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36900(int& val) {
    const int x = 36900;
    std::cout << 36900;
    for (int i = 0; i < 36900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36901(int& val) {
    func_in_36901(val);
    const int x = 36901;
    std::cout << 36901;
    for (int i = 0; i < 36901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36901(int& val) {
    const int x = 36901;
    std::cout << 36901;
    for (int i = 0; i < 36901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36902(int& val) {
    func_in_36902(val);
    const int x = 36902;
    std::cout << 36902;
    for (int i = 0; i < 36902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36902(int& val) {
    const int x = 36902;
    std::cout << 36902;
    for (int i = 0; i < 36902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36903(int& val) {
    func_in_36903(val);
    const int x = 36903;
    std::cout << 36903;
    for (int i = 0; i < 36903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36903(int& val) {
    const int x = 36903;
    std::cout << 36903;
    for (int i = 0; i < 36903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36904(int& val) {
    func_in_36904(val);
    const int x = 36904;
    std::cout << 36904;
    for (int i = 0; i < 36904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36904(int& val) {
    const int x = 36904;
    std::cout << 36904;
    for (int i = 0; i < 36904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36905(int& val) {
    func_in_36905(val);
    const int x = 36905;
    std::cout << 36905;
    for (int i = 0; i < 36905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36905(int& val) {
    const int x = 36905;
    std::cout << 36905;
    for (int i = 0; i < 36905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36906(int& val) {
    func_in_36906(val);
    const int x = 36906;
    std::cout << 36906;
    for (int i = 0; i < 36906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36906(int& val) {
    const int x = 36906;
    std::cout << 36906;
    for (int i = 0; i < 36906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36907(int& val) {
    func_in_36907(val);
    const int x = 36907;
    std::cout << 36907;
    for (int i = 0; i < 36907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36907(int& val) {
    const int x = 36907;
    std::cout << 36907;
    for (int i = 0; i < 36907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36908(int& val) {
    func_in_36908(val);
    const int x = 36908;
    std::cout << 36908;
    for (int i = 0; i < 36908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36908(int& val) {
    const int x = 36908;
    std::cout << 36908;
    for (int i = 0; i < 36908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36909(int& val) {
    func_in_36909(val);
    const int x = 36909;
    std::cout << 36909;
    for (int i = 0; i < 36909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36909(int& val) {
    const int x = 36909;
    std::cout << 36909;
    for (int i = 0; i < 36909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36910(int& val) {
    func_in_36910(val);
    const int x = 36910;
    std::cout << 36910;
    for (int i = 0; i < 36910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36910(int& val) {
    const int x = 36910;
    std::cout << 36910;
    for (int i = 0; i < 36910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36911(int& val) {
    func_in_36911(val);
    const int x = 36911;
    std::cout << 36911;
    for (int i = 0; i < 36911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36911(int& val) {
    const int x = 36911;
    std::cout << 36911;
    for (int i = 0; i < 36911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36912(int& val) {
    func_in_36912(val);
    const int x = 36912;
    std::cout << 36912;
    for (int i = 0; i < 36912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36912(int& val) {
    const int x = 36912;
    std::cout << 36912;
    for (int i = 0; i < 36912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36913(int& val) {
    func_in_36913(val);
    const int x = 36913;
    std::cout << 36913;
    for (int i = 0; i < 36913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36913(int& val) {
    const int x = 36913;
    std::cout << 36913;
    for (int i = 0; i < 36913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36914(int& val) {
    func_in_36914(val);
    const int x = 36914;
    std::cout << 36914;
    for (int i = 0; i < 36914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36914(int& val) {
    const int x = 36914;
    std::cout << 36914;
    for (int i = 0; i < 36914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36915(int& val) {
    func_in_36915(val);
    const int x = 36915;
    std::cout << 36915;
    for (int i = 0; i < 36915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36915(int& val) {
    const int x = 36915;
    std::cout << 36915;
    for (int i = 0; i < 36915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36916(int& val) {
    func_in_36916(val);
    const int x = 36916;
    std::cout << 36916;
    for (int i = 0; i < 36916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36916(int& val) {
    const int x = 36916;
    std::cout << 36916;
    for (int i = 0; i < 36916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36917(int& val) {
    func_in_36917(val);
    const int x = 36917;
    std::cout << 36917;
    for (int i = 0; i < 36917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36917(int& val) {
    const int x = 36917;
    std::cout << 36917;
    for (int i = 0; i < 36917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36918(int& val) {
    func_in_36918(val);
    const int x = 36918;
    std::cout << 36918;
    for (int i = 0; i < 36918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36918(int& val) {
    const int x = 36918;
    std::cout << 36918;
    for (int i = 0; i < 36918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36919(int& val) {
    func_in_36919(val);
    const int x = 36919;
    std::cout << 36919;
    for (int i = 0; i < 36919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36919(int& val) {
    const int x = 36919;
    std::cout << 36919;
    for (int i = 0; i < 36919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36920(int& val) {
    func_in_36920(val);
    const int x = 36920;
    std::cout << 36920;
    for (int i = 0; i < 36920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36920(int& val) {
    const int x = 36920;
    std::cout << 36920;
    for (int i = 0; i < 36920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36921(int& val) {
    func_in_36921(val);
    const int x = 36921;
    std::cout << 36921;
    for (int i = 0; i < 36921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36921(int& val) {
    const int x = 36921;
    std::cout << 36921;
    for (int i = 0; i < 36921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36922(int& val) {
    func_in_36922(val);
    const int x = 36922;
    std::cout << 36922;
    for (int i = 0; i < 36922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36922(int& val) {
    const int x = 36922;
    std::cout << 36922;
    for (int i = 0; i < 36922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36923(int& val) {
    func_in_36923(val);
    const int x = 36923;
    std::cout << 36923;
    for (int i = 0; i < 36923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36923(int& val) {
    const int x = 36923;
    std::cout << 36923;
    for (int i = 0; i < 36923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36924(int& val) {
    func_in_36924(val);
    const int x = 36924;
    std::cout << 36924;
    for (int i = 0; i < 36924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36924(int& val) {
    const int x = 36924;
    std::cout << 36924;
    for (int i = 0; i < 36924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36925(int& val) {
    func_in_36925(val);
    const int x = 36925;
    std::cout << 36925;
    for (int i = 0; i < 36925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36925(int& val) {
    const int x = 36925;
    std::cout << 36925;
    for (int i = 0; i < 36925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36926(int& val) {
    func_in_36926(val);
    const int x = 36926;
    std::cout << 36926;
    for (int i = 0; i < 36926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36926(int& val) {
    const int x = 36926;
    std::cout << 36926;
    for (int i = 0; i < 36926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36927(int& val) {
    func_in_36927(val);
    const int x = 36927;
    std::cout << 36927;
    for (int i = 0; i < 36927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36927(int& val) {
    const int x = 36927;
    std::cout << 36927;
    for (int i = 0; i < 36927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36928(int& val) {
    func_in_36928(val);
    const int x = 36928;
    std::cout << 36928;
    for (int i = 0; i < 36928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36928(int& val) {
    const int x = 36928;
    std::cout << 36928;
    for (int i = 0; i < 36928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36929(int& val) {
    func_in_36929(val);
    const int x = 36929;
    std::cout << 36929;
    for (int i = 0; i < 36929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36929(int& val) {
    const int x = 36929;
    std::cout << 36929;
    for (int i = 0; i < 36929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36930(int& val) {
    func_in_36930(val);
    const int x = 36930;
    std::cout << 36930;
    for (int i = 0; i < 36930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36930(int& val) {
    const int x = 36930;
    std::cout << 36930;
    for (int i = 0; i < 36930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36931(int& val) {
    func_in_36931(val);
    const int x = 36931;
    std::cout << 36931;
    for (int i = 0; i < 36931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36931(int& val) {
    const int x = 36931;
    std::cout << 36931;
    for (int i = 0; i < 36931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36932(int& val) {
    func_in_36932(val);
    const int x = 36932;
    std::cout << 36932;
    for (int i = 0; i < 36932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36932(int& val) {
    const int x = 36932;
    std::cout << 36932;
    for (int i = 0; i < 36932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36933(int& val) {
    func_in_36933(val);
    const int x = 36933;
    std::cout << 36933;
    for (int i = 0; i < 36933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36933(int& val) {
    const int x = 36933;
    std::cout << 36933;
    for (int i = 0; i < 36933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36934(int& val) {
    func_in_36934(val);
    const int x = 36934;
    std::cout << 36934;
    for (int i = 0; i < 36934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36934(int& val) {
    const int x = 36934;
    std::cout << 36934;
    for (int i = 0; i < 36934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36935(int& val) {
    func_in_36935(val);
    const int x = 36935;
    std::cout << 36935;
    for (int i = 0; i < 36935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36935(int& val) {
    const int x = 36935;
    std::cout << 36935;
    for (int i = 0; i < 36935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36936(int& val) {
    func_in_36936(val);
    const int x = 36936;
    std::cout << 36936;
    for (int i = 0; i < 36936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36936(int& val) {
    const int x = 36936;
    std::cout << 36936;
    for (int i = 0; i < 36936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36937(int& val) {
    func_in_36937(val);
    const int x = 36937;
    std::cout << 36937;
    for (int i = 0; i < 36937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36937(int& val) {
    const int x = 36937;
    std::cout << 36937;
    for (int i = 0; i < 36937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36938(int& val) {
    func_in_36938(val);
    const int x = 36938;
    std::cout << 36938;
    for (int i = 0; i < 36938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36938(int& val) {
    const int x = 36938;
    std::cout << 36938;
    for (int i = 0; i < 36938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36939(int& val) {
    func_in_36939(val);
    const int x = 36939;
    std::cout << 36939;
    for (int i = 0; i < 36939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36939(int& val) {
    const int x = 36939;
    std::cout << 36939;
    for (int i = 0; i < 36939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36940(int& val) {
    func_in_36940(val);
    const int x = 36940;
    std::cout << 36940;
    for (int i = 0; i < 36940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36940(int& val) {
    const int x = 36940;
    std::cout << 36940;
    for (int i = 0; i < 36940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36941(int& val) {
    func_in_36941(val);
    const int x = 36941;
    std::cout << 36941;
    for (int i = 0; i < 36941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36941(int& val) {
    const int x = 36941;
    std::cout << 36941;
    for (int i = 0; i < 36941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36942(int& val) {
    func_in_36942(val);
    const int x = 36942;
    std::cout << 36942;
    for (int i = 0; i < 36942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36942(int& val) {
    const int x = 36942;
    std::cout << 36942;
    for (int i = 0; i < 36942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36943(int& val) {
    func_in_36943(val);
    const int x = 36943;
    std::cout << 36943;
    for (int i = 0; i < 36943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36943(int& val) {
    const int x = 36943;
    std::cout << 36943;
    for (int i = 0; i < 36943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36944(int& val) {
    func_in_36944(val);
    const int x = 36944;
    std::cout << 36944;
    for (int i = 0; i < 36944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36944(int& val) {
    const int x = 36944;
    std::cout << 36944;
    for (int i = 0; i < 36944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36945(int& val) {
    func_in_36945(val);
    const int x = 36945;
    std::cout << 36945;
    for (int i = 0; i < 36945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36945(int& val) {
    const int x = 36945;
    std::cout << 36945;
    for (int i = 0; i < 36945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36946(int& val) {
    func_in_36946(val);
    const int x = 36946;
    std::cout << 36946;
    for (int i = 0; i < 36946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36946(int& val) {
    const int x = 36946;
    std::cout << 36946;
    for (int i = 0; i < 36946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36947(int& val) {
    func_in_36947(val);
    const int x = 36947;
    std::cout << 36947;
    for (int i = 0; i < 36947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36947(int& val) {
    const int x = 36947;
    std::cout << 36947;
    for (int i = 0; i < 36947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36948(int& val) {
    func_in_36948(val);
    const int x = 36948;
    std::cout << 36948;
    for (int i = 0; i < 36948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36948(int& val) {
    const int x = 36948;
    std::cout << 36948;
    for (int i = 0; i < 36948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36949(int& val) {
    func_in_36949(val);
    const int x = 36949;
    std::cout << 36949;
    for (int i = 0; i < 36949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36949(int& val) {
    const int x = 36949;
    std::cout << 36949;
    for (int i = 0; i < 36949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36950(int& val) {
    func_in_36950(val);
    const int x = 36950;
    std::cout << 36950;
    for (int i = 0; i < 36950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36950(int& val) {
    const int x = 36950;
    std::cout << 36950;
    for (int i = 0; i < 36950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36951(int& val) {
    func_in_36951(val);
    const int x = 36951;
    std::cout << 36951;
    for (int i = 0; i < 36951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36951(int& val) {
    const int x = 36951;
    std::cout << 36951;
    for (int i = 0; i < 36951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36952(int& val) {
    func_in_36952(val);
    const int x = 36952;
    std::cout << 36952;
    for (int i = 0; i < 36952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36952(int& val) {
    const int x = 36952;
    std::cout << 36952;
    for (int i = 0; i < 36952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36953(int& val) {
    func_in_36953(val);
    const int x = 36953;
    std::cout << 36953;
    for (int i = 0; i < 36953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36953(int& val) {
    const int x = 36953;
    std::cout << 36953;
    for (int i = 0; i < 36953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36954(int& val) {
    func_in_36954(val);
    const int x = 36954;
    std::cout << 36954;
    for (int i = 0; i < 36954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36954(int& val) {
    const int x = 36954;
    std::cout << 36954;
    for (int i = 0; i < 36954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36955(int& val) {
    func_in_36955(val);
    const int x = 36955;
    std::cout << 36955;
    for (int i = 0; i < 36955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36955(int& val) {
    const int x = 36955;
    std::cout << 36955;
    for (int i = 0; i < 36955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36956(int& val) {
    func_in_36956(val);
    const int x = 36956;
    std::cout << 36956;
    for (int i = 0; i < 36956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36956(int& val) {
    const int x = 36956;
    std::cout << 36956;
    for (int i = 0; i < 36956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36957(int& val) {
    func_in_36957(val);
    const int x = 36957;
    std::cout << 36957;
    for (int i = 0; i < 36957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36957(int& val) {
    const int x = 36957;
    std::cout << 36957;
    for (int i = 0; i < 36957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36958(int& val) {
    func_in_36958(val);
    const int x = 36958;
    std::cout << 36958;
    for (int i = 0; i < 36958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36958(int& val) {
    const int x = 36958;
    std::cout << 36958;
    for (int i = 0; i < 36958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36959(int& val) {
    func_in_36959(val);
    const int x = 36959;
    std::cout << 36959;
    for (int i = 0; i < 36959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36959(int& val) {
    const int x = 36959;
    std::cout << 36959;
    for (int i = 0; i < 36959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36960(int& val) {
    func_in_36960(val);
    const int x = 36960;
    std::cout << 36960;
    for (int i = 0; i < 36960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36960(int& val) {
    const int x = 36960;
    std::cout << 36960;
    for (int i = 0; i < 36960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36961(int& val) {
    func_in_36961(val);
    const int x = 36961;
    std::cout << 36961;
    for (int i = 0; i < 36961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36961(int& val) {
    const int x = 36961;
    std::cout << 36961;
    for (int i = 0; i < 36961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36962(int& val) {
    func_in_36962(val);
    const int x = 36962;
    std::cout << 36962;
    for (int i = 0; i < 36962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36962(int& val) {
    const int x = 36962;
    std::cout << 36962;
    for (int i = 0; i < 36962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36963(int& val) {
    func_in_36963(val);
    const int x = 36963;
    std::cout << 36963;
    for (int i = 0; i < 36963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36963(int& val) {
    const int x = 36963;
    std::cout << 36963;
    for (int i = 0; i < 36963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36964(int& val) {
    func_in_36964(val);
    const int x = 36964;
    std::cout << 36964;
    for (int i = 0; i < 36964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36964(int& val) {
    const int x = 36964;
    std::cout << 36964;
    for (int i = 0; i < 36964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36965(int& val) {
    func_in_36965(val);
    const int x = 36965;
    std::cout << 36965;
    for (int i = 0; i < 36965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36965(int& val) {
    const int x = 36965;
    std::cout << 36965;
    for (int i = 0; i < 36965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36966(int& val) {
    func_in_36966(val);
    const int x = 36966;
    std::cout << 36966;
    for (int i = 0; i < 36966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36966(int& val) {
    const int x = 36966;
    std::cout << 36966;
    for (int i = 0; i < 36966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36967(int& val) {
    func_in_36967(val);
    const int x = 36967;
    std::cout << 36967;
    for (int i = 0; i < 36967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36967(int& val) {
    const int x = 36967;
    std::cout << 36967;
    for (int i = 0; i < 36967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36968(int& val) {
    func_in_36968(val);
    const int x = 36968;
    std::cout << 36968;
    for (int i = 0; i < 36968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36968(int& val) {
    const int x = 36968;
    std::cout << 36968;
    for (int i = 0; i < 36968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36969(int& val) {
    func_in_36969(val);
    const int x = 36969;
    std::cout << 36969;
    for (int i = 0; i < 36969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36969(int& val) {
    const int x = 36969;
    std::cout << 36969;
    for (int i = 0; i < 36969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36970(int& val) {
    func_in_36970(val);
    const int x = 36970;
    std::cout << 36970;
    for (int i = 0; i < 36970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36970(int& val) {
    const int x = 36970;
    std::cout << 36970;
    for (int i = 0; i < 36970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36971(int& val) {
    func_in_36971(val);
    const int x = 36971;
    std::cout << 36971;
    for (int i = 0; i < 36971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36971(int& val) {
    const int x = 36971;
    std::cout << 36971;
    for (int i = 0; i < 36971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36972(int& val) {
    func_in_36972(val);
    const int x = 36972;
    std::cout << 36972;
    for (int i = 0; i < 36972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36972(int& val) {
    const int x = 36972;
    std::cout << 36972;
    for (int i = 0; i < 36972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36973(int& val) {
    func_in_36973(val);
    const int x = 36973;
    std::cout << 36973;
    for (int i = 0; i < 36973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36973(int& val) {
    const int x = 36973;
    std::cout << 36973;
    for (int i = 0; i < 36973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36974(int& val) {
    func_in_36974(val);
    const int x = 36974;
    std::cout << 36974;
    for (int i = 0; i < 36974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36974(int& val) {
    const int x = 36974;
    std::cout << 36974;
    for (int i = 0; i < 36974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36975(int& val) {
    func_in_36975(val);
    const int x = 36975;
    std::cout << 36975;
    for (int i = 0; i < 36975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36975(int& val) {
    const int x = 36975;
    std::cout << 36975;
    for (int i = 0; i < 36975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36976(int& val) {
    func_in_36976(val);
    const int x = 36976;
    std::cout << 36976;
    for (int i = 0; i < 36976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36976(int& val) {
    const int x = 36976;
    std::cout << 36976;
    for (int i = 0; i < 36976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36977(int& val) {
    func_in_36977(val);
    const int x = 36977;
    std::cout << 36977;
    for (int i = 0; i < 36977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36977(int& val) {
    const int x = 36977;
    std::cout << 36977;
    for (int i = 0; i < 36977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36978(int& val) {
    func_in_36978(val);
    const int x = 36978;
    std::cout << 36978;
    for (int i = 0; i < 36978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36978(int& val) {
    const int x = 36978;
    std::cout << 36978;
    for (int i = 0; i < 36978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36979(int& val) {
    func_in_36979(val);
    const int x = 36979;
    std::cout << 36979;
    for (int i = 0; i < 36979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36979(int& val) {
    const int x = 36979;
    std::cout << 36979;
    for (int i = 0; i < 36979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36980(int& val) {
    func_in_36980(val);
    const int x = 36980;
    std::cout << 36980;
    for (int i = 0; i < 36980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36980(int& val) {
    const int x = 36980;
    std::cout << 36980;
    for (int i = 0; i < 36980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36981(int& val) {
    func_in_36981(val);
    const int x = 36981;
    std::cout << 36981;
    for (int i = 0; i < 36981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36981(int& val) {
    const int x = 36981;
    std::cout << 36981;
    for (int i = 0; i < 36981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36982(int& val) {
    func_in_36982(val);
    const int x = 36982;
    std::cout << 36982;
    for (int i = 0; i < 36982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36982(int& val) {
    const int x = 36982;
    std::cout << 36982;
    for (int i = 0; i < 36982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36983(int& val) {
    func_in_36983(val);
    const int x = 36983;
    std::cout << 36983;
    for (int i = 0; i < 36983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36983(int& val) {
    const int x = 36983;
    std::cout << 36983;
    for (int i = 0; i < 36983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36984(int& val) {
    func_in_36984(val);
    const int x = 36984;
    std::cout << 36984;
    for (int i = 0; i < 36984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36984(int& val) {
    const int x = 36984;
    std::cout << 36984;
    for (int i = 0; i < 36984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36985(int& val) {
    func_in_36985(val);
    const int x = 36985;
    std::cout << 36985;
    for (int i = 0; i < 36985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36985(int& val) {
    const int x = 36985;
    std::cout << 36985;
    for (int i = 0; i < 36985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36986(int& val) {
    func_in_36986(val);
    const int x = 36986;
    std::cout << 36986;
    for (int i = 0; i < 36986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36986(int& val) {
    const int x = 36986;
    std::cout << 36986;
    for (int i = 0; i < 36986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36987(int& val) {
    func_in_36987(val);
    const int x = 36987;
    std::cout << 36987;
    for (int i = 0; i < 36987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36987(int& val) {
    const int x = 36987;
    std::cout << 36987;
    for (int i = 0; i < 36987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36988(int& val) {
    func_in_36988(val);
    const int x = 36988;
    std::cout << 36988;
    for (int i = 0; i < 36988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36988(int& val) {
    const int x = 36988;
    std::cout << 36988;
    for (int i = 0; i < 36988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36989(int& val) {
    func_in_36989(val);
    const int x = 36989;
    std::cout << 36989;
    for (int i = 0; i < 36989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36989(int& val) {
    const int x = 36989;
    std::cout << 36989;
    for (int i = 0; i < 36989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36990(int& val) {
    func_in_36990(val);
    const int x = 36990;
    std::cout << 36990;
    for (int i = 0; i < 36990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36990(int& val) {
    const int x = 36990;
    std::cout << 36990;
    for (int i = 0; i < 36990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36991(int& val) {
    func_in_36991(val);
    const int x = 36991;
    std::cout << 36991;
    for (int i = 0; i < 36991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36991(int& val) {
    const int x = 36991;
    std::cout << 36991;
    for (int i = 0; i < 36991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36992(int& val) {
    func_in_36992(val);
    const int x = 36992;
    std::cout << 36992;
    for (int i = 0; i < 36992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36992(int& val) {
    const int x = 36992;
    std::cout << 36992;
    for (int i = 0; i < 36992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36993(int& val) {
    func_in_36993(val);
    const int x = 36993;
    std::cout << 36993;
    for (int i = 0; i < 36993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36993(int& val) {
    const int x = 36993;
    std::cout << 36993;
    for (int i = 0; i < 36993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36994(int& val) {
    func_in_36994(val);
    const int x = 36994;
    std::cout << 36994;
    for (int i = 0; i < 36994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36994(int& val) {
    const int x = 36994;
    std::cout << 36994;
    for (int i = 0; i < 36994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36995(int& val) {
    func_in_36995(val);
    const int x = 36995;
    std::cout << 36995;
    for (int i = 0; i < 36995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36995(int& val) {
    const int x = 36995;
    std::cout << 36995;
    for (int i = 0; i < 36995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36996(int& val) {
    func_in_36996(val);
    const int x = 36996;
    std::cout << 36996;
    for (int i = 0; i < 36996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36996(int& val) {
    const int x = 36996;
    std::cout << 36996;
    for (int i = 0; i < 36996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36997(int& val) {
    func_in_36997(val);
    const int x = 36997;
    std::cout << 36997;
    for (int i = 0; i < 36997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36997(int& val) {
    const int x = 36997;
    std::cout << 36997;
    for (int i = 0; i < 36997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36998(int& val) {
    func_in_36998(val);
    const int x = 36998;
    std::cout << 36998;
    for (int i = 0; i < 36998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36998(int& val) {
    const int x = 36998;
    std::cout << 36998;
    for (int i = 0; i < 36998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36999(int& val) {
    func_in_36999(val);
    const int x = 36999;
    std::cout << 36999;
    for (int i = 0; i < 36999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36999(int& val) {
    const int x = 36999;
    std::cout << 36999;
    for (int i = 0; i < 36999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37000(int& val) {
    func_in_37000(val);
    const int x = 37000;
    std::cout << 37000;
    for (int i = 0; i < 37000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37000(int& val) {
    const int x = 37000;
    std::cout << 37000;
    for (int i = 0; i < 37000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37001(int& val) {
    func_in_37001(val);
    const int x = 37001;
    std::cout << 37001;
    for (int i = 0; i < 37001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37001(int& val) {
    const int x = 37001;
    std::cout << 37001;
    for (int i = 0; i < 37001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37002(int& val) {
    func_in_37002(val);
    const int x = 37002;
    std::cout << 37002;
    for (int i = 0; i < 37002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37002(int& val) {
    const int x = 37002;
    std::cout << 37002;
    for (int i = 0; i < 37002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37003(int& val) {
    func_in_37003(val);
    const int x = 37003;
    std::cout << 37003;
    for (int i = 0; i < 37003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37003(int& val) {
    const int x = 37003;
    std::cout << 37003;
    for (int i = 0; i < 37003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37004(int& val) {
    func_in_37004(val);
    const int x = 37004;
    std::cout << 37004;
    for (int i = 0; i < 37004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37004(int& val) {
    const int x = 37004;
    std::cout << 37004;
    for (int i = 0; i < 37004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37005(int& val) {
    func_in_37005(val);
    const int x = 37005;
    std::cout << 37005;
    for (int i = 0; i < 37005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37005(int& val) {
    const int x = 37005;
    std::cout << 37005;
    for (int i = 0; i < 37005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37006(int& val) {
    func_in_37006(val);
    const int x = 37006;
    std::cout << 37006;
    for (int i = 0; i < 37006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37006(int& val) {
    const int x = 37006;
    std::cout << 37006;
    for (int i = 0; i < 37006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37007(int& val) {
    func_in_37007(val);
    const int x = 37007;
    std::cout << 37007;
    for (int i = 0; i < 37007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37007(int& val) {
    const int x = 37007;
    std::cout << 37007;
    for (int i = 0; i < 37007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37008(int& val) {
    func_in_37008(val);
    const int x = 37008;
    std::cout << 37008;
    for (int i = 0; i < 37008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37008(int& val) {
    const int x = 37008;
    std::cout << 37008;
    for (int i = 0; i < 37008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37009(int& val) {
    func_in_37009(val);
    const int x = 37009;
    std::cout << 37009;
    for (int i = 0; i < 37009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37009(int& val) {
    const int x = 37009;
    std::cout << 37009;
    for (int i = 0; i < 37009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37010(int& val) {
    func_in_37010(val);
    const int x = 37010;
    std::cout << 37010;
    for (int i = 0; i < 37010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37010(int& val) {
    const int x = 37010;
    std::cout << 37010;
    for (int i = 0; i < 37010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37011(int& val) {
    func_in_37011(val);
    const int x = 37011;
    std::cout << 37011;
    for (int i = 0; i < 37011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37011(int& val) {
    const int x = 37011;
    std::cout << 37011;
    for (int i = 0; i < 37011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37012(int& val) {
    func_in_37012(val);
    const int x = 37012;
    std::cout << 37012;
    for (int i = 0; i < 37012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37012(int& val) {
    const int x = 37012;
    std::cout << 37012;
    for (int i = 0; i < 37012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37013(int& val) {
    func_in_37013(val);
    const int x = 37013;
    std::cout << 37013;
    for (int i = 0; i < 37013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37013(int& val) {
    const int x = 37013;
    std::cout << 37013;
    for (int i = 0; i < 37013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37014(int& val) {
    func_in_37014(val);
    const int x = 37014;
    std::cout << 37014;
    for (int i = 0; i < 37014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37014(int& val) {
    const int x = 37014;
    std::cout << 37014;
    for (int i = 0; i < 37014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37015(int& val) {
    func_in_37015(val);
    const int x = 37015;
    std::cout << 37015;
    for (int i = 0; i < 37015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37015(int& val) {
    const int x = 37015;
    std::cout << 37015;
    for (int i = 0; i < 37015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37016(int& val) {
    func_in_37016(val);
    const int x = 37016;
    std::cout << 37016;
    for (int i = 0; i < 37016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37016(int& val) {
    const int x = 37016;
    std::cout << 37016;
    for (int i = 0; i < 37016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37017(int& val) {
    func_in_37017(val);
    const int x = 37017;
    std::cout << 37017;
    for (int i = 0; i < 37017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37017(int& val) {
    const int x = 37017;
    std::cout << 37017;
    for (int i = 0; i < 37017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37018(int& val) {
    func_in_37018(val);
    const int x = 37018;
    std::cout << 37018;
    for (int i = 0; i < 37018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37018(int& val) {
    const int x = 37018;
    std::cout << 37018;
    for (int i = 0; i < 37018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37019(int& val) {
    func_in_37019(val);
    const int x = 37019;
    std::cout << 37019;
    for (int i = 0; i < 37019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37019(int& val) {
    const int x = 37019;
    std::cout << 37019;
    for (int i = 0; i < 37019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37020(int& val) {
    func_in_37020(val);
    const int x = 37020;
    std::cout << 37020;
    for (int i = 0; i < 37020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37020(int& val) {
    const int x = 37020;
    std::cout << 37020;
    for (int i = 0; i < 37020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37021(int& val) {
    func_in_37021(val);
    const int x = 37021;
    std::cout << 37021;
    for (int i = 0; i < 37021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37021(int& val) {
    const int x = 37021;
    std::cout << 37021;
    for (int i = 0; i < 37021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37022(int& val) {
    func_in_37022(val);
    const int x = 37022;
    std::cout << 37022;
    for (int i = 0; i < 37022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37022(int& val) {
    const int x = 37022;
    std::cout << 37022;
    for (int i = 0; i < 37022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37023(int& val) {
    func_in_37023(val);
    const int x = 37023;
    std::cout << 37023;
    for (int i = 0; i < 37023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37023(int& val) {
    const int x = 37023;
    std::cout << 37023;
    for (int i = 0; i < 37023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37024(int& val) {
    func_in_37024(val);
    const int x = 37024;
    std::cout << 37024;
    for (int i = 0; i < 37024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37024(int& val) {
    const int x = 37024;
    std::cout << 37024;
    for (int i = 0; i < 37024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37025(int& val) {
    func_in_37025(val);
    const int x = 37025;
    std::cout << 37025;
    for (int i = 0; i < 37025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37025(int& val) {
    const int x = 37025;
    std::cout << 37025;
    for (int i = 0; i < 37025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37026(int& val) {
    func_in_37026(val);
    const int x = 37026;
    std::cout << 37026;
    for (int i = 0; i < 37026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37026(int& val) {
    const int x = 37026;
    std::cout << 37026;
    for (int i = 0; i < 37026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37027(int& val) {
    func_in_37027(val);
    const int x = 37027;
    std::cout << 37027;
    for (int i = 0; i < 37027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37027(int& val) {
    const int x = 37027;
    std::cout << 37027;
    for (int i = 0; i < 37027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37028(int& val) {
    func_in_37028(val);
    const int x = 37028;
    std::cout << 37028;
    for (int i = 0; i < 37028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37028(int& val) {
    const int x = 37028;
    std::cout << 37028;
    for (int i = 0; i < 37028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37029(int& val) {
    func_in_37029(val);
    const int x = 37029;
    std::cout << 37029;
    for (int i = 0; i < 37029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37029(int& val) {
    const int x = 37029;
    std::cout << 37029;
    for (int i = 0; i < 37029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37030(int& val) {
    func_in_37030(val);
    const int x = 37030;
    std::cout << 37030;
    for (int i = 0; i < 37030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37030(int& val) {
    const int x = 37030;
    std::cout << 37030;
    for (int i = 0; i < 37030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37031(int& val) {
    func_in_37031(val);
    const int x = 37031;
    std::cout << 37031;
    for (int i = 0; i < 37031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37031(int& val) {
    const int x = 37031;
    std::cout << 37031;
    for (int i = 0; i < 37031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37032(int& val) {
    func_in_37032(val);
    const int x = 37032;
    std::cout << 37032;
    for (int i = 0; i < 37032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37032(int& val) {
    const int x = 37032;
    std::cout << 37032;
    for (int i = 0; i < 37032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37033(int& val) {
    func_in_37033(val);
    const int x = 37033;
    std::cout << 37033;
    for (int i = 0; i < 37033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37033(int& val) {
    const int x = 37033;
    std::cout << 37033;
    for (int i = 0; i < 37033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37034(int& val) {
    func_in_37034(val);
    const int x = 37034;
    std::cout << 37034;
    for (int i = 0; i < 37034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37034(int& val) {
    const int x = 37034;
    std::cout << 37034;
    for (int i = 0; i < 37034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37035(int& val) {
    func_in_37035(val);
    const int x = 37035;
    std::cout << 37035;
    for (int i = 0; i < 37035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37035(int& val) {
    const int x = 37035;
    std::cout << 37035;
    for (int i = 0; i < 37035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37036(int& val) {
    func_in_37036(val);
    const int x = 37036;
    std::cout << 37036;
    for (int i = 0; i < 37036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37036(int& val) {
    const int x = 37036;
    std::cout << 37036;
    for (int i = 0; i < 37036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37037(int& val) {
    func_in_37037(val);
    const int x = 37037;
    std::cout << 37037;
    for (int i = 0; i < 37037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37037(int& val) {
    const int x = 37037;
    std::cout << 37037;
    for (int i = 0; i < 37037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37038(int& val) {
    func_in_37038(val);
    const int x = 37038;
    std::cout << 37038;
    for (int i = 0; i < 37038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37038(int& val) {
    const int x = 37038;
    std::cout << 37038;
    for (int i = 0; i < 37038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37039(int& val) {
    func_in_37039(val);
    const int x = 37039;
    std::cout << 37039;
    for (int i = 0; i < 37039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37039(int& val) {
    const int x = 37039;
    std::cout << 37039;
    for (int i = 0; i < 37039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37040(int& val) {
    func_in_37040(val);
    const int x = 37040;
    std::cout << 37040;
    for (int i = 0; i < 37040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37040(int& val) {
    const int x = 37040;
    std::cout << 37040;
    for (int i = 0; i < 37040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37041(int& val) {
    func_in_37041(val);
    const int x = 37041;
    std::cout << 37041;
    for (int i = 0; i < 37041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37041(int& val) {
    const int x = 37041;
    std::cout << 37041;
    for (int i = 0; i < 37041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37042(int& val) {
    func_in_37042(val);
    const int x = 37042;
    std::cout << 37042;
    for (int i = 0; i < 37042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37042(int& val) {
    const int x = 37042;
    std::cout << 37042;
    for (int i = 0; i < 37042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37043(int& val) {
    func_in_37043(val);
    const int x = 37043;
    std::cout << 37043;
    for (int i = 0; i < 37043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37043(int& val) {
    const int x = 37043;
    std::cout << 37043;
    for (int i = 0; i < 37043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37044(int& val) {
    func_in_37044(val);
    const int x = 37044;
    std::cout << 37044;
    for (int i = 0; i < 37044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37044(int& val) {
    const int x = 37044;
    std::cout << 37044;
    for (int i = 0; i < 37044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37045(int& val) {
    func_in_37045(val);
    const int x = 37045;
    std::cout << 37045;
    for (int i = 0; i < 37045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37045(int& val) {
    const int x = 37045;
    std::cout << 37045;
    for (int i = 0; i < 37045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37046(int& val) {
    func_in_37046(val);
    const int x = 37046;
    std::cout << 37046;
    for (int i = 0; i < 37046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37046(int& val) {
    const int x = 37046;
    std::cout << 37046;
    for (int i = 0; i < 37046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37047(int& val) {
    func_in_37047(val);
    const int x = 37047;
    std::cout << 37047;
    for (int i = 0; i < 37047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37047(int& val) {
    const int x = 37047;
    std::cout << 37047;
    for (int i = 0; i < 37047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37048(int& val) {
    func_in_37048(val);
    const int x = 37048;
    std::cout << 37048;
    for (int i = 0; i < 37048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37048(int& val) {
    const int x = 37048;
    std::cout << 37048;
    for (int i = 0; i < 37048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37049(int& val) {
    func_in_37049(val);
    const int x = 37049;
    std::cout << 37049;
    for (int i = 0; i < 37049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37049(int& val) {
    const int x = 37049;
    std::cout << 37049;
    for (int i = 0; i < 37049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37050(int& val) {
    func_in_37050(val);
    const int x = 37050;
    std::cout << 37050;
    for (int i = 0; i < 37050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37050(int& val) {
    const int x = 37050;
    std::cout << 37050;
    for (int i = 0; i < 37050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37051(int& val) {
    func_in_37051(val);
    const int x = 37051;
    std::cout << 37051;
    for (int i = 0; i < 37051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37051(int& val) {
    const int x = 37051;
    std::cout << 37051;
    for (int i = 0; i < 37051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37052(int& val) {
    func_in_37052(val);
    const int x = 37052;
    std::cout << 37052;
    for (int i = 0; i < 37052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37052(int& val) {
    const int x = 37052;
    std::cout << 37052;
    for (int i = 0; i < 37052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37053(int& val) {
    func_in_37053(val);
    const int x = 37053;
    std::cout << 37053;
    for (int i = 0; i < 37053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37053(int& val) {
    const int x = 37053;
    std::cout << 37053;
    for (int i = 0; i < 37053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37054(int& val) {
    func_in_37054(val);
    const int x = 37054;
    std::cout << 37054;
    for (int i = 0; i < 37054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37054(int& val) {
    const int x = 37054;
    std::cout << 37054;
    for (int i = 0; i < 37054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37055(int& val) {
    func_in_37055(val);
    const int x = 37055;
    std::cout << 37055;
    for (int i = 0; i < 37055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37055(int& val) {
    const int x = 37055;
    std::cout << 37055;
    for (int i = 0; i < 37055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37056(int& val) {
    func_in_37056(val);
    const int x = 37056;
    std::cout << 37056;
    for (int i = 0; i < 37056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37056(int& val) {
    const int x = 37056;
    std::cout << 37056;
    for (int i = 0; i < 37056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37057(int& val) {
    func_in_37057(val);
    const int x = 37057;
    std::cout << 37057;
    for (int i = 0; i < 37057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37057(int& val) {
    const int x = 37057;
    std::cout << 37057;
    for (int i = 0; i < 37057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37058(int& val) {
    func_in_37058(val);
    const int x = 37058;
    std::cout << 37058;
    for (int i = 0; i < 37058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37058(int& val) {
    const int x = 37058;
    std::cout << 37058;
    for (int i = 0; i < 37058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37059(int& val) {
    func_in_37059(val);
    const int x = 37059;
    std::cout << 37059;
    for (int i = 0; i < 37059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37059(int& val) {
    const int x = 37059;
    std::cout << 37059;
    for (int i = 0; i < 37059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37060(int& val) {
    func_in_37060(val);
    const int x = 37060;
    std::cout << 37060;
    for (int i = 0; i < 37060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37060(int& val) {
    const int x = 37060;
    std::cout << 37060;
    for (int i = 0; i < 37060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37061(int& val) {
    func_in_37061(val);
    const int x = 37061;
    std::cout << 37061;
    for (int i = 0; i < 37061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37061(int& val) {
    const int x = 37061;
    std::cout << 37061;
    for (int i = 0; i < 37061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37062(int& val) {
    func_in_37062(val);
    const int x = 37062;
    std::cout << 37062;
    for (int i = 0; i < 37062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37062(int& val) {
    const int x = 37062;
    std::cout << 37062;
    for (int i = 0; i < 37062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37063(int& val) {
    func_in_37063(val);
    const int x = 37063;
    std::cout << 37063;
    for (int i = 0; i < 37063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37063(int& val) {
    const int x = 37063;
    std::cout << 37063;
    for (int i = 0; i < 37063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37064(int& val) {
    func_in_37064(val);
    const int x = 37064;
    std::cout << 37064;
    for (int i = 0; i < 37064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37064(int& val) {
    const int x = 37064;
    std::cout << 37064;
    for (int i = 0; i < 37064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37065(int& val) {
    func_in_37065(val);
    const int x = 37065;
    std::cout << 37065;
    for (int i = 0; i < 37065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37065(int& val) {
    const int x = 37065;
    std::cout << 37065;
    for (int i = 0; i < 37065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37066(int& val) {
    func_in_37066(val);
    const int x = 37066;
    std::cout << 37066;
    for (int i = 0; i < 37066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37066(int& val) {
    const int x = 37066;
    std::cout << 37066;
    for (int i = 0; i < 37066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37067(int& val) {
    func_in_37067(val);
    const int x = 37067;
    std::cout << 37067;
    for (int i = 0; i < 37067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37067(int& val) {
    const int x = 37067;
    std::cout << 37067;
    for (int i = 0; i < 37067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37068(int& val) {
    func_in_37068(val);
    const int x = 37068;
    std::cout << 37068;
    for (int i = 0; i < 37068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37068(int& val) {
    const int x = 37068;
    std::cout << 37068;
    for (int i = 0; i < 37068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37069(int& val) {
    func_in_37069(val);
    const int x = 37069;
    std::cout << 37069;
    for (int i = 0; i < 37069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37069(int& val) {
    const int x = 37069;
    std::cout << 37069;
    for (int i = 0; i < 37069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37070(int& val) {
    func_in_37070(val);
    const int x = 37070;
    std::cout << 37070;
    for (int i = 0; i < 37070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37070(int& val) {
    const int x = 37070;
    std::cout << 37070;
    for (int i = 0; i < 37070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37071(int& val) {
    func_in_37071(val);
    const int x = 37071;
    std::cout << 37071;
    for (int i = 0; i < 37071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37071(int& val) {
    const int x = 37071;
    std::cout << 37071;
    for (int i = 0; i < 37071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37072(int& val) {
    func_in_37072(val);
    const int x = 37072;
    std::cout << 37072;
    for (int i = 0; i < 37072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37072(int& val) {
    const int x = 37072;
    std::cout << 37072;
    for (int i = 0; i < 37072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37073(int& val) {
    func_in_37073(val);
    const int x = 37073;
    std::cout << 37073;
    for (int i = 0; i < 37073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37073(int& val) {
    const int x = 37073;
    std::cout << 37073;
    for (int i = 0; i < 37073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37074(int& val) {
    func_in_37074(val);
    const int x = 37074;
    std::cout << 37074;
    for (int i = 0; i < 37074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37074(int& val) {
    const int x = 37074;
    std::cout << 37074;
    for (int i = 0; i < 37074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37075(int& val) {
    func_in_37075(val);
    const int x = 37075;
    std::cout << 37075;
    for (int i = 0; i < 37075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37075(int& val) {
    const int x = 37075;
    std::cout << 37075;
    for (int i = 0; i < 37075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37076(int& val) {
    func_in_37076(val);
    const int x = 37076;
    std::cout << 37076;
    for (int i = 0; i < 37076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37076(int& val) {
    const int x = 37076;
    std::cout << 37076;
    for (int i = 0; i < 37076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37077(int& val) {
    func_in_37077(val);
    const int x = 37077;
    std::cout << 37077;
    for (int i = 0; i < 37077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37077(int& val) {
    const int x = 37077;
    std::cout << 37077;
    for (int i = 0; i < 37077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37078(int& val) {
    func_in_37078(val);
    const int x = 37078;
    std::cout << 37078;
    for (int i = 0; i < 37078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37078(int& val) {
    const int x = 37078;
    std::cout << 37078;
    for (int i = 0; i < 37078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37079(int& val) {
    func_in_37079(val);
    const int x = 37079;
    std::cout << 37079;
    for (int i = 0; i < 37079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37079(int& val) {
    const int x = 37079;
    std::cout << 37079;
    for (int i = 0; i < 37079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37080(int& val) {
    func_in_37080(val);
    const int x = 37080;
    std::cout << 37080;
    for (int i = 0; i < 37080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37080(int& val) {
    const int x = 37080;
    std::cout << 37080;
    for (int i = 0; i < 37080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37081(int& val) {
    func_in_37081(val);
    const int x = 37081;
    std::cout << 37081;
    for (int i = 0; i < 37081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37081(int& val) {
    const int x = 37081;
    std::cout << 37081;
    for (int i = 0; i < 37081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37082(int& val) {
    func_in_37082(val);
    const int x = 37082;
    std::cout << 37082;
    for (int i = 0; i < 37082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37082(int& val) {
    const int x = 37082;
    std::cout << 37082;
    for (int i = 0; i < 37082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37083(int& val) {
    func_in_37083(val);
    const int x = 37083;
    std::cout << 37083;
    for (int i = 0; i < 37083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37083(int& val) {
    const int x = 37083;
    std::cout << 37083;
    for (int i = 0; i < 37083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37084(int& val) {
    func_in_37084(val);
    const int x = 37084;
    std::cout << 37084;
    for (int i = 0; i < 37084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37084(int& val) {
    const int x = 37084;
    std::cout << 37084;
    for (int i = 0; i < 37084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37085(int& val) {
    func_in_37085(val);
    const int x = 37085;
    std::cout << 37085;
    for (int i = 0; i < 37085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37085(int& val) {
    const int x = 37085;
    std::cout << 37085;
    for (int i = 0; i < 37085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37086(int& val) {
    func_in_37086(val);
    const int x = 37086;
    std::cout << 37086;
    for (int i = 0; i < 37086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37086(int& val) {
    const int x = 37086;
    std::cout << 37086;
    for (int i = 0; i < 37086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37087(int& val) {
    func_in_37087(val);
    const int x = 37087;
    std::cout << 37087;
    for (int i = 0; i < 37087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37087(int& val) {
    const int x = 37087;
    std::cout << 37087;
    for (int i = 0; i < 37087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37088(int& val) {
    func_in_37088(val);
    const int x = 37088;
    std::cout << 37088;
    for (int i = 0; i < 37088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37088(int& val) {
    const int x = 37088;
    std::cout << 37088;
    for (int i = 0; i < 37088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37089(int& val) {
    func_in_37089(val);
    const int x = 37089;
    std::cout << 37089;
    for (int i = 0; i < 37089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37089(int& val) {
    const int x = 37089;
    std::cout << 37089;
    for (int i = 0; i < 37089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37090(int& val) {
    func_in_37090(val);
    const int x = 37090;
    std::cout << 37090;
    for (int i = 0; i < 37090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37090(int& val) {
    const int x = 37090;
    std::cout << 37090;
    for (int i = 0; i < 37090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37091(int& val) {
    func_in_37091(val);
    const int x = 37091;
    std::cout << 37091;
    for (int i = 0; i < 37091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37091(int& val) {
    const int x = 37091;
    std::cout << 37091;
    for (int i = 0; i < 37091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37092(int& val) {
    func_in_37092(val);
    const int x = 37092;
    std::cout << 37092;
    for (int i = 0; i < 37092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37092(int& val) {
    const int x = 37092;
    std::cout << 37092;
    for (int i = 0; i < 37092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37093(int& val) {
    func_in_37093(val);
    const int x = 37093;
    std::cout << 37093;
    for (int i = 0; i < 37093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37093(int& val) {
    const int x = 37093;
    std::cout << 37093;
    for (int i = 0; i < 37093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37094(int& val) {
    func_in_37094(val);
    const int x = 37094;
    std::cout << 37094;
    for (int i = 0; i < 37094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37094(int& val) {
    const int x = 37094;
    std::cout << 37094;
    for (int i = 0; i < 37094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37095(int& val) {
    func_in_37095(val);
    const int x = 37095;
    std::cout << 37095;
    for (int i = 0; i < 37095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37095(int& val) {
    const int x = 37095;
    std::cout << 37095;
    for (int i = 0; i < 37095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37096(int& val) {
    func_in_37096(val);
    const int x = 37096;
    std::cout << 37096;
    for (int i = 0; i < 37096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37096(int& val) {
    const int x = 37096;
    std::cout << 37096;
    for (int i = 0; i < 37096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37097(int& val) {
    func_in_37097(val);
    const int x = 37097;
    std::cout << 37097;
    for (int i = 0; i < 37097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37097(int& val) {
    const int x = 37097;
    std::cout << 37097;
    for (int i = 0; i < 37097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37098(int& val) {
    func_in_37098(val);
    const int x = 37098;
    std::cout << 37098;
    for (int i = 0; i < 37098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37098(int& val) {
    const int x = 37098;
    std::cout << 37098;
    for (int i = 0; i < 37098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37099(int& val) {
    func_in_37099(val);
    const int x = 37099;
    std::cout << 37099;
    for (int i = 0; i < 37099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37099(int& val) {
    const int x = 37099;
    std::cout << 37099;
    for (int i = 0; i < 37099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37100(int& val) {
    func_in_37100(val);
    const int x = 37100;
    std::cout << 37100;
    for (int i = 0; i < 37100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37100(int& val) {
    const int x = 37100;
    std::cout << 37100;
    for (int i = 0; i < 37100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37101(int& val) {
    func_in_37101(val);
    const int x = 37101;
    std::cout << 37101;
    for (int i = 0; i < 37101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37101(int& val) {
    const int x = 37101;
    std::cout << 37101;
    for (int i = 0; i < 37101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37102(int& val) {
    func_in_37102(val);
    const int x = 37102;
    std::cout << 37102;
    for (int i = 0; i < 37102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37102(int& val) {
    const int x = 37102;
    std::cout << 37102;
    for (int i = 0; i < 37102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37103(int& val) {
    func_in_37103(val);
    const int x = 37103;
    std::cout << 37103;
    for (int i = 0; i < 37103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37103(int& val) {
    const int x = 37103;
    std::cout << 37103;
    for (int i = 0; i < 37103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37104(int& val) {
    func_in_37104(val);
    const int x = 37104;
    std::cout << 37104;
    for (int i = 0; i < 37104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37104(int& val) {
    const int x = 37104;
    std::cout << 37104;
    for (int i = 0; i < 37104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37105(int& val) {
    func_in_37105(val);
    const int x = 37105;
    std::cout << 37105;
    for (int i = 0; i < 37105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37105(int& val) {
    const int x = 37105;
    std::cout << 37105;
    for (int i = 0; i < 37105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37106(int& val) {
    func_in_37106(val);
    const int x = 37106;
    std::cout << 37106;
    for (int i = 0; i < 37106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37106(int& val) {
    const int x = 37106;
    std::cout << 37106;
    for (int i = 0; i < 37106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37107(int& val) {
    func_in_37107(val);
    const int x = 37107;
    std::cout << 37107;
    for (int i = 0; i < 37107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37107(int& val) {
    const int x = 37107;
    std::cout << 37107;
    for (int i = 0; i < 37107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37108(int& val) {
    func_in_37108(val);
    const int x = 37108;
    std::cout << 37108;
    for (int i = 0; i < 37108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37108(int& val) {
    const int x = 37108;
    std::cout << 37108;
    for (int i = 0; i < 37108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37109(int& val) {
    func_in_37109(val);
    const int x = 37109;
    std::cout << 37109;
    for (int i = 0; i < 37109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37109(int& val) {
    const int x = 37109;
    std::cout << 37109;
    for (int i = 0; i < 37109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37110(int& val) {
    func_in_37110(val);
    const int x = 37110;
    std::cout << 37110;
    for (int i = 0; i < 37110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37110(int& val) {
    const int x = 37110;
    std::cout << 37110;
    for (int i = 0; i < 37110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37111(int& val) {
    func_in_37111(val);
    const int x = 37111;
    std::cout << 37111;
    for (int i = 0; i < 37111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37111(int& val) {
    const int x = 37111;
    std::cout << 37111;
    for (int i = 0; i < 37111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37112(int& val) {
    func_in_37112(val);
    const int x = 37112;
    std::cout << 37112;
    for (int i = 0; i < 37112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37112(int& val) {
    const int x = 37112;
    std::cout << 37112;
    for (int i = 0; i < 37112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37113(int& val) {
    func_in_37113(val);
    const int x = 37113;
    std::cout << 37113;
    for (int i = 0; i < 37113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37113(int& val) {
    const int x = 37113;
    std::cout << 37113;
    for (int i = 0; i < 37113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37114(int& val) {
    func_in_37114(val);
    const int x = 37114;
    std::cout << 37114;
    for (int i = 0; i < 37114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37114(int& val) {
    const int x = 37114;
    std::cout << 37114;
    for (int i = 0; i < 37114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37115(int& val) {
    func_in_37115(val);
    const int x = 37115;
    std::cout << 37115;
    for (int i = 0; i < 37115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37115(int& val) {
    const int x = 37115;
    std::cout << 37115;
    for (int i = 0; i < 37115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37116(int& val) {
    func_in_37116(val);
    const int x = 37116;
    std::cout << 37116;
    for (int i = 0; i < 37116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37116(int& val) {
    const int x = 37116;
    std::cout << 37116;
    for (int i = 0; i < 37116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37117(int& val) {
    func_in_37117(val);
    const int x = 37117;
    std::cout << 37117;
    for (int i = 0; i < 37117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37117(int& val) {
    const int x = 37117;
    std::cout << 37117;
    for (int i = 0; i < 37117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37118(int& val) {
    func_in_37118(val);
    const int x = 37118;
    std::cout << 37118;
    for (int i = 0; i < 37118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37118(int& val) {
    const int x = 37118;
    std::cout << 37118;
    for (int i = 0; i < 37118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37119(int& val) {
    func_in_37119(val);
    const int x = 37119;
    std::cout << 37119;
    for (int i = 0; i < 37119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37119(int& val) {
    const int x = 37119;
    std::cout << 37119;
    for (int i = 0; i < 37119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37120(int& val) {
    func_in_37120(val);
    const int x = 37120;
    std::cout << 37120;
    for (int i = 0; i < 37120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37120(int& val) {
    const int x = 37120;
    std::cout << 37120;
    for (int i = 0; i < 37120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37121(int& val) {
    func_in_37121(val);
    const int x = 37121;
    std::cout << 37121;
    for (int i = 0; i < 37121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37121(int& val) {
    const int x = 37121;
    std::cout << 37121;
    for (int i = 0; i < 37121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37122(int& val) {
    func_in_37122(val);
    const int x = 37122;
    std::cout << 37122;
    for (int i = 0; i < 37122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37122(int& val) {
    const int x = 37122;
    std::cout << 37122;
    for (int i = 0; i < 37122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37123(int& val) {
    func_in_37123(val);
    const int x = 37123;
    std::cout << 37123;
    for (int i = 0; i < 37123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37123(int& val) {
    const int x = 37123;
    std::cout << 37123;
    for (int i = 0; i < 37123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37124(int& val) {
    func_in_37124(val);
    const int x = 37124;
    std::cout << 37124;
    for (int i = 0; i < 37124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37124(int& val) {
    const int x = 37124;
    std::cout << 37124;
    for (int i = 0; i < 37124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37125(int& val) {
    func_in_37125(val);
    const int x = 37125;
    std::cout << 37125;
    for (int i = 0; i < 37125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37125(int& val) {
    const int x = 37125;
    std::cout << 37125;
    for (int i = 0; i < 37125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37126(int& val) {
    func_in_37126(val);
    const int x = 37126;
    std::cout << 37126;
    for (int i = 0; i < 37126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37126(int& val) {
    const int x = 37126;
    std::cout << 37126;
    for (int i = 0; i < 37126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37127(int& val) {
    func_in_37127(val);
    const int x = 37127;
    std::cout << 37127;
    for (int i = 0; i < 37127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37127(int& val) {
    const int x = 37127;
    std::cout << 37127;
    for (int i = 0; i < 37127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37128(int& val) {
    func_in_37128(val);
    const int x = 37128;
    std::cout << 37128;
    for (int i = 0; i < 37128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37128(int& val) {
    const int x = 37128;
    std::cout << 37128;
    for (int i = 0; i < 37128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37129(int& val) {
    func_in_37129(val);
    const int x = 37129;
    std::cout << 37129;
    for (int i = 0; i < 37129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37129(int& val) {
    const int x = 37129;
    std::cout << 37129;
    for (int i = 0; i < 37129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37130(int& val) {
    func_in_37130(val);
    const int x = 37130;
    std::cout << 37130;
    for (int i = 0; i < 37130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37130(int& val) {
    const int x = 37130;
    std::cout << 37130;
    for (int i = 0; i < 37130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37131(int& val) {
    func_in_37131(val);
    const int x = 37131;
    std::cout << 37131;
    for (int i = 0; i < 37131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37131(int& val) {
    const int x = 37131;
    std::cout << 37131;
    for (int i = 0; i < 37131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37132(int& val) {
    func_in_37132(val);
    const int x = 37132;
    std::cout << 37132;
    for (int i = 0; i < 37132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37132(int& val) {
    const int x = 37132;
    std::cout << 37132;
    for (int i = 0; i < 37132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37133(int& val) {
    func_in_37133(val);
    const int x = 37133;
    std::cout << 37133;
    for (int i = 0; i < 37133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37133(int& val) {
    const int x = 37133;
    std::cout << 37133;
    for (int i = 0; i < 37133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37134(int& val) {
    func_in_37134(val);
    const int x = 37134;
    std::cout << 37134;
    for (int i = 0; i < 37134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37134(int& val) {
    const int x = 37134;
    std::cout << 37134;
    for (int i = 0; i < 37134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37135(int& val) {
    func_in_37135(val);
    const int x = 37135;
    std::cout << 37135;
    for (int i = 0; i < 37135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37135(int& val) {
    const int x = 37135;
    std::cout << 37135;
    for (int i = 0; i < 37135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37136(int& val) {
    func_in_37136(val);
    const int x = 37136;
    std::cout << 37136;
    for (int i = 0; i < 37136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37136(int& val) {
    const int x = 37136;
    std::cout << 37136;
    for (int i = 0; i < 37136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37137(int& val) {
    func_in_37137(val);
    const int x = 37137;
    std::cout << 37137;
    for (int i = 0; i < 37137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37137(int& val) {
    const int x = 37137;
    std::cout << 37137;
    for (int i = 0; i < 37137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37138(int& val) {
    func_in_37138(val);
    const int x = 37138;
    std::cout << 37138;
    for (int i = 0; i < 37138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37138(int& val) {
    const int x = 37138;
    std::cout << 37138;
    for (int i = 0; i < 37138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37139(int& val) {
    func_in_37139(val);
    const int x = 37139;
    std::cout << 37139;
    for (int i = 0; i < 37139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37139(int& val) {
    const int x = 37139;
    std::cout << 37139;
    for (int i = 0; i < 37139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37140(int& val) {
    func_in_37140(val);
    const int x = 37140;
    std::cout << 37140;
    for (int i = 0; i < 37140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37140(int& val) {
    const int x = 37140;
    std::cout << 37140;
    for (int i = 0; i < 37140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37141(int& val) {
    func_in_37141(val);
    const int x = 37141;
    std::cout << 37141;
    for (int i = 0; i < 37141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37141(int& val) {
    const int x = 37141;
    std::cout << 37141;
    for (int i = 0; i < 37141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37142(int& val) {
    func_in_37142(val);
    const int x = 37142;
    std::cout << 37142;
    for (int i = 0; i < 37142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37142(int& val) {
    const int x = 37142;
    std::cout << 37142;
    for (int i = 0; i < 37142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37143(int& val) {
    func_in_37143(val);
    const int x = 37143;
    std::cout << 37143;
    for (int i = 0; i < 37143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37143(int& val) {
    const int x = 37143;
    std::cout << 37143;
    for (int i = 0; i < 37143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37144(int& val) {
    func_in_37144(val);
    const int x = 37144;
    std::cout << 37144;
    for (int i = 0; i < 37144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37144(int& val) {
    const int x = 37144;
    std::cout << 37144;
    for (int i = 0; i < 37144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37145(int& val) {
    func_in_37145(val);
    const int x = 37145;
    std::cout << 37145;
    for (int i = 0; i < 37145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37145(int& val) {
    const int x = 37145;
    std::cout << 37145;
    for (int i = 0; i < 37145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37146(int& val) {
    func_in_37146(val);
    const int x = 37146;
    std::cout << 37146;
    for (int i = 0; i < 37146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37146(int& val) {
    const int x = 37146;
    std::cout << 37146;
    for (int i = 0; i < 37146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37147(int& val) {
    func_in_37147(val);
    const int x = 37147;
    std::cout << 37147;
    for (int i = 0; i < 37147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37147(int& val) {
    const int x = 37147;
    std::cout << 37147;
    for (int i = 0; i < 37147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37148(int& val) {
    func_in_37148(val);
    const int x = 37148;
    std::cout << 37148;
    for (int i = 0; i < 37148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37148(int& val) {
    const int x = 37148;
    std::cout << 37148;
    for (int i = 0; i < 37148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37149(int& val) {
    func_in_37149(val);
    const int x = 37149;
    std::cout << 37149;
    for (int i = 0; i < 37149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37149(int& val) {
    const int x = 37149;
    std::cout << 37149;
    for (int i = 0; i < 37149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37150(int& val) {
    func_in_37150(val);
    const int x = 37150;
    std::cout << 37150;
    for (int i = 0; i < 37150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37150(int& val) {
    const int x = 37150;
    std::cout << 37150;
    for (int i = 0; i < 37150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37151(int& val) {
    func_in_37151(val);
    const int x = 37151;
    std::cout << 37151;
    for (int i = 0; i < 37151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37151(int& val) {
    const int x = 37151;
    std::cout << 37151;
    for (int i = 0; i < 37151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37152(int& val) {
    func_in_37152(val);
    const int x = 37152;
    std::cout << 37152;
    for (int i = 0; i < 37152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37152(int& val) {
    const int x = 37152;
    std::cout << 37152;
    for (int i = 0; i < 37152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37153(int& val) {
    func_in_37153(val);
    const int x = 37153;
    std::cout << 37153;
    for (int i = 0; i < 37153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37153(int& val) {
    const int x = 37153;
    std::cout << 37153;
    for (int i = 0; i < 37153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37154(int& val) {
    func_in_37154(val);
    const int x = 37154;
    std::cout << 37154;
    for (int i = 0; i < 37154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37154(int& val) {
    const int x = 37154;
    std::cout << 37154;
    for (int i = 0; i < 37154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37155(int& val) {
    func_in_37155(val);
    const int x = 37155;
    std::cout << 37155;
    for (int i = 0; i < 37155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37155(int& val) {
    const int x = 37155;
    std::cout << 37155;
    for (int i = 0; i < 37155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37156(int& val) {
    func_in_37156(val);
    const int x = 37156;
    std::cout << 37156;
    for (int i = 0; i < 37156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37156(int& val) {
    const int x = 37156;
    std::cout << 37156;
    for (int i = 0; i < 37156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37157(int& val) {
    func_in_37157(val);
    const int x = 37157;
    std::cout << 37157;
    for (int i = 0; i < 37157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37157(int& val) {
    const int x = 37157;
    std::cout << 37157;
    for (int i = 0; i < 37157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37158(int& val) {
    func_in_37158(val);
    const int x = 37158;
    std::cout << 37158;
    for (int i = 0; i < 37158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37158(int& val) {
    const int x = 37158;
    std::cout << 37158;
    for (int i = 0; i < 37158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37159(int& val) {
    func_in_37159(val);
    const int x = 37159;
    std::cout << 37159;
    for (int i = 0; i < 37159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37159(int& val) {
    const int x = 37159;
    std::cout << 37159;
    for (int i = 0; i < 37159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37160(int& val) {
    func_in_37160(val);
    const int x = 37160;
    std::cout << 37160;
    for (int i = 0; i < 37160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37160(int& val) {
    const int x = 37160;
    std::cout << 37160;
    for (int i = 0; i < 37160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37161(int& val) {
    func_in_37161(val);
    const int x = 37161;
    std::cout << 37161;
    for (int i = 0; i < 37161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37161(int& val) {
    const int x = 37161;
    std::cout << 37161;
    for (int i = 0; i < 37161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37162(int& val) {
    func_in_37162(val);
    const int x = 37162;
    std::cout << 37162;
    for (int i = 0; i < 37162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37162(int& val) {
    const int x = 37162;
    std::cout << 37162;
    for (int i = 0; i < 37162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37163(int& val) {
    func_in_37163(val);
    const int x = 37163;
    std::cout << 37163;
    for (int i = 0; i < 37163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37163(int& val) {
    const int x = 37163;
    std::cout << 37163;
    for (int i = 0; i < 37163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37164(int& val) {
    func_in_37164(val);
    const int x = 37164;
    std::cout << 37164;
    for (int i = 0; i < 37164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37164(int& val) {
    const int x = 37164;
    std::cout << 37164;
    for (int i = 0; i < 37164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37165(int& val) {
    func_in_37165(val);
    const int x = 37165;
    std::cout << 37165;
    for (int i = 0; i < 37165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37165(int& val) {
    const int x = 37165;
    std::cout << 37165;
    for (int i = 0; i < 37165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37166(int& val) {
    func_in_37166(val);
    const int x = 37166;
    std::cout << 37166;
    for (int i = 0; i < 37166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37166(int& val) {
    const int x = 37166;
    std::cout << 37166;
    for (int i = 0; i < 37166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37167(int& val) {
    func_in_37167(val);
    const int x = 37167;
    std::cout << 37167;
    for (int i = 0; i < 37167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37167(int& val) {
    const int x = 37167;
    std::cout << 37167;
    for (int i = 0; i < 37167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37168(int& val) {
    func_in_37168(val);
    const int x = 37168;
    std::cout << 37168;
    for (int i = 0; i < 37168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37168(int& val) {
    const int x = 37168;
    std::cout << 37168;
    for (int i = 0; i < 37168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37169(int& val) {
    func_in_37169(val);
    const int x = 37169;
    std::cout << 37169;
    for (int i = 0; i < 37169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37169(int& val) {
    const int x = 37169;
    std::cout << 37169;
    for (int i = 0; i < 37169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37170(int& val) {
    func_in_37170(val);
    const int x = 37170;
    std::cout << 37170;
    for (int i = 0; i < 37170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37170(int& val) {
    const int x = 37170;
    std::cout << 37170;
    for (int i = 0; i < 37170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37171(int& val) {
    func_in_37171(val);
    const int x = 37171;
    std::cout << 37171;
    for (int i = 0; i < 37171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37171(int& val) {
    const int x = 37171;
    std::cout << 37171;
    for (int i = 0; i < 37171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37172(int& val) {
    func_in_37172(val);
    const int x = 37172;
    std::cout << 37172;
    for (int i = 0; i < 37172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37172(int& val) {
    const int x = 37172;
    std::cout << 37172;
    for (int i = 0; i < 37172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37173(int& val) {
    func_in_37173(val);
    const int x = 37173;
    std::cout << 37173;
    for (int i = 0; i < 37173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37173(int& val) {
    const int x = 37173;
    std::cout << 37173;
    for (int i = 0; i < 37173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37174(int& val) {
    func_in_37174(val);
    const int x = 37174;
    std::cout << 37174;
    for (int i = 0; i < 37174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37174(int& val) {
    const int x = 37174;
    std::cout << 37174;
    for (int i = 0; i < 37174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37175(int& val) {
    func_in_37175(val);
    const int x = 37175;
    std::cout << 37175;
    for (int i = 0; i < 37175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37175(int& val) {
    const int x = 37175;
    std::cout << 37175;
    for (int i = 0; i < 37175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37176(int& val) {
    func_in_37176(val);
    const int x = 37176;
    std::cout << 37176;
    for (int i = 0; i < 37176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37176(int& val) {
    const int x = 37176;
    std::cout << 37176;
    for (int i = 0; i < 37176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37177(int& val) {
    func_in_37177(val);
    const int x = 37177;
    std::cout << 37177;
    for (int i = 0; i < 37177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37177(int& val) {
    const int x = 37177;
    std::cout << 37177;
    for (int i = 0; i < 37177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37178(int& val) {
    func_in_37178(val);
    const int x = 37178;
    std::cout << 37178;
    for (int i = 0; i < 37178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37178(int& val) {
    const int x = 37178;
    std::cout << 37178;
    for (int i = 0; i < 37178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37179(int& val) {
    func_in_37179(val);
    const int x = 37179;
    std::cout << 37179;
    for (int i = 0; i < 37179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37179(int& val) {
    const int x = 37179;
    std::cout << 37179;
    for (int i = 0; i < 37179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37180(int& val) {
    func_in_37180(val);
    const int x = 37180;
    std::cout << 37180;
    for (int i = 0; i < 37180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37180(int& val) {
    const int x = 37180;
    std::cout << 37180;
    for (int i = 0; i < 37180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37181(int& val) {
    func_in_37181(val);
    const int x = 37181;
    std::cout << 37181;
    for (int i = 0; i < 37181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37181(int& val) {
    const int x = 37181;
    std::cout << 37181;
    for (int i = 0; i < 37181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37182(int& val) {
    func_in_37182(val);
    const int x = 37182;
    std::cout << 37182;
    for (int i = 0; i < 37182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37182(int& val) {
    const int x = 37182;
    std::cout << 37182;
    for (int i = 0; i < 37182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37183(int& val) {
    func_in_37183(val);
    const int x = 37183;
    std::cout << 37183;
    for (int i = 0; i < 37183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37183(int& val) {
    const int x = 37183;
    std::cout << 37183;
    for (int i = 0; i < 37183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37184(int& val) {
    func_in_37184(val);
    const int x = 37184;
    std::cout << 37184;
    for (int i = 0; i < 37184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37184(int& val) {
    const int x = 37184;
    std::cout << 37184;
    for (int i = 0; i < 37184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37185(int& val) {
    func_in_37185(val);
    const int x = 37185;
    std::cout << 37185;
    for (int i = 0; i < 37185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37185(int& val) {
    const int x = 37185;
    std::cout << 37185;
    for (int i = 0; i < 37185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37186(int& val) {
    func_in_37186(val);
    const int x = 37186;
    std::cout << 37186;
    for (int i = 0; i < 37186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37186(int& val) {
    const int x = 37186;
    std::cout << 37186;
    for (int i = 0; i < 37186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37187(int& val) {
    func_in_37187(val);
    const int x = 37187;
    std::cout << 37187;
    for (int i = 0; i < 37187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37187(int& val) {
    const int x = 37187;
    std::cout << 37187;
    for (int i = 0; i < 37187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37188(int& val) {
    func_in_37188(val);
    const int x = 37188;
    std::cout << 37188;
    for (int i = 0; i < 37188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37188(int& val) {
    const int x = 37188;
    std::cout << 37188;
    for (int i = 0; i < 37188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37189(int& val) {
    func_in_37189(val);
    const int x = 37189;
    std::cout << 37189;
    for (int i = 0; i < 37189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37189(int& val) {
    const int x = 37189;
    std::cout << 37189;
    for (int i = 0; i < 37189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37190(int& val) {
    func_in_37190(val);
    const int x = 37190;
    std::cout << 37190;
    for (int i = 0; i < 37190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37190(int& val) {
    const int x = 37190;
    std::cout << 37190;
    for (int i = 0; i < 37190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37191(int& val) {
    func_in_37191(val);
    const int x = 37191;
    std::cout << 37191;
    for (int i = 0; i < 37191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37191(int& val) {
    const int x = 37191;
    std::cout << 37191;
    for (int i = 0; i < 37191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37192(int& val) {
    func_in_37192(val);
    const int x = 37192;
    std::cout << 37192;
    for (int i = 0; i < 37192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37192(int& val) {
    const int x = 37192;
    std::cout << 37192;
    for (int i = 0; i < 37192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37193(int& val) {
    func_in_37193(val);
    const int x = 37193;
    std::cout << 37193;
    for (int i = 0; i < 37193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37193(int& val) {
    const int x = 37193;
    std::cout << 37193;
    for (int i = 0; i < 37193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37194(int& val) {
    func_in_37194(val);
    const int x = 37194;
    std::cout << 37194;
    for (int i = 0; i < 37194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37194(int& val) {
    const int x = 37194;
    std::cout << 37194;
    for (int i = 0; i < 37194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37195(int& val) {
    func_in_37195(val);
    const int x = 37195;
    std::cout << 37195;
    for (int i = 0; i < 37195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37195(int& val) {
    const int x = 37195;
    std::cout << 37195;
    for (int i = 0; i < 37195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37196(int& val) {
    func_in_37196(val);
    const int x = 37196;
    std::cout << 37196;
    for (int i = 0; i < 37196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37196(int& val) {
    const int x = 37196;
    std::cout << 37196;
    for (int i = 0; i < 37196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37197(int& val) {
    func_in_37197(val);
    const int x = 37197;
    std::cout << 37197;
    for (int i = 0; i < 37197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37197(int& val) {
    const int x = 37197;
    std::cout << 37197;
    for (int i = 0; i < 37197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37198(int& val) {
    func_in_37198(val);
    const int x = 37198;
    std::cout << 37198;
    for (int i = 0; i < 37198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37198(int& val) {
    const int x = 37198;
    std::cout << 37198;
    for (int i = 0; i < 37198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37199(int& val) {
    func_in_37199(val);
    const int x = 37199;
    std::cout << 37199;
    for (int i = 0; i < 37199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37199(int& val) {
    const int x = 37199;
    std::cout << 37199;
    for (int i = 0; i < 37199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37200(int& val) {
    func_in_37200(val);
    const int x = 37200;
    std::cout << 37200;
    for (int i = 0; i < 37200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37200(int& val) {
    const int x = 37200;
    std::cout << 37200;
    for (int i = 0; i < 37200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37201(int& val) {
    func_in_37201(val);
    const int x = 37201;
    std::cout << 37201;
    for (int i = 0; i < 37201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37201(int& val) {
    const int x = 37201;
    std::cout << 37201;
    for (int i = 0; i < 37201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37202(int& val) {
    func_in_37202(val);
    const int x = 37202;
    std::cout << 37202;
    for (int i = 0; i < 37202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37202(int& val) {
    const int x = 37202;
    std::cout << 37202;
    for (int i = 0; i < 37202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37203(int& val) {
    func_in_37203(val);
    const int x = 37203;
    std::cout << 37203;
    for (int i = 0; i < 37203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37203(int& val) {
    const int x = 37203;
    std::cout << 37203;
    for (int i = 0; i < 37203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37204(int& val) {
    func_in_37204(val);
    const int x = 37204;
    std::cout << 37204;
    for (int i = 0; i < 37204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37204(int& val) {
    const int x = 37204;
    std::cout << 37204;
    for (int i = 0; i < 37204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37205(int& val) {
    func_in_37205(val);
    const int x = 37205;
    std::cout << 37205;
    for (int i = 0; i < 37205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37205(int& val) {
    const int x = 37205;
    std::cout << 37205;
    for (int i = 0; i < 37205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37206(int& val) {
    func_in_37206(val);
    const int x = 37206;
    std::cout << 37206;
    for (int i = 0; i < 37206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37206(int& val) {
    const int x = 37206;
    std::cout << 37206;
    for (int i = 0; i < 37206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37207(int& val) {
    func_in_37207(val);
    const int x = 37207;
    std::cout << 37207;
    for (int i = 0; i < 37207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37207(int& val) {
    const int x = 37207;
    std::cout << 37207;
    for (int i = 0; i < 37207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37208(int& val) {
    func_in_37208(val);
    const int x = 37208;
    std::cout << 37208;
    for (int i = 0; i < 37208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37208(int& val) {
    const int x = 37208;
    std::cout << 37208;
    for (int i = 0; i < 37208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37209(int& val) {
    func_in_37209(val);
    const int x = 37209;
    std::cout << 37209;
    for (int i = 0; i < 37209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37209(int& val) {
    const int x = 37209;
    std::cout << 37209;
    for (int i = 0; i < 37209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37210(int& val) {
    func_in_37210(val);
    const int x = 37210;
    std::cout << 37210;
    for (int i = 0; i < 37210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37210(int& val) {
    const int x = 37210;
    std::cout << 37210;
    for (int i = 0; i < 37210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37211(int& val) {
    func_in_37211(val);
    const int x = 37211;
    std::cout << 37211;
    for (int i = 0; i < 37211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37211(int& val) {
    const int x = 37211;
    std::cout << 37211;
    for (int i = 0; i < 37211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37212(int& val) {
    func_in_37212(val);
    const int x = 37212;
    std::cout << 37212;
    for (int i = 0; i < 37212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37212(int& val) {
    const int x = 37212;
    std::cout << 37212;
    for (int i = 0; i < 37212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37213(int& val) {
    func_in_37213(val);
    const int x = 37213;
    std::cout << 37213;
    for (int i = 0; i < 37213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37213(int& val) {
    const int x = 37213;
    std::cout << 37213;
    for (int i = 0; i < 37213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37214(int& val) {
    func_in_37214(val);
    const int x = 37214;
    std::cout << 37214;
    for (int i = 0; i < 37214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37214(int& val) {
    const int x = 37214;
    std::cout << 37214;
    for (int i = 0; i < 37214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37215(int& val) {
    func_in_37215(val);
    const int x = 37215;
    std::cout << 37215;
    for (int i = 0; i < 37215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37215(int& val) {
    const int x = 37215;
    std::cout << 37215;
    for (int i = 0; i < 37215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37216(int& val) {
    func_in_37216(val);
    const int x = 37216;
    std::cout << 37216;
    for (int i = 0; i < 37216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37216(int& val) {
    const int x = 37216;
    std::cout << 37216;
    for (int i = 0; i < 37216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37217(int& val) {
    func_in_37217(val);
    const int x = 37217;
    std::cout << 37217;
    for (int i = 0; i < 37217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37217(int& val) {
    const int x = 37217;
    std::cout << 37217;
    for (int i = 0; i < 37217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37218(int& val) {
    func_in_37218(val);
    const int x = 37218;
    std::cout << 37218;
    for (int i = 0; i < 37218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37218(int& val) {
    const int x = 37218;
    std::cout << 37218;
    for (int i = 0; i < 37218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37219(int& val) {
    func_in_37219(val);
    const int x = 37219;
    std::cout << 37219;
    for (int i = 0; i < 37219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37219(int& val) {
    const int x = 37219;
    std::cout << 37219;
    for (int i = 0; i < 37219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37220(int& val) {
    func_in_37220(val);
    const int x = 37220;
    std::cout << 37220;
    for (int i = 0; i < 37220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37220(int& val) {
    const int x = 37220;
    std::cout << 37220;
    for (int i = 0; i < 37220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37221(int& val) {
    func_in_37221(val);
    const int x = 37221;
    std::cout << 37221;
    for (int i = 0; i < 37221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37221(int& val) {
    const int x = 37221;
    std::cout << 37221;
    for (int i = 0; i < 37221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37222(int& val) {
    func_in_37222(val);
    const int x = 37222;
    std::cout << 37222;
    for (int i = 0; i < 37222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37222(int& val) {
    const int x = 37222;
    std::cout << 37222;
    for (int i = 0; i < 37222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37223(int& val) {
    func_in_37223(val);
    const int x = 37223;
    std::cout << 37223;
    for (int i = 0; i < 37223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37223(int& val) {
    const int x = 37223;
    std::cout << 37223;
    for (int i = 0; i < 37223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37224(int& val) {
    func_in_37224(val);
    const int x = 37224;
    std::cout << 37224;
    for (int i = 0; i < 37224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37224(int& val) {
    const int x = 37224;
    std::cout << 37224;
    for (int i = 0; i < 37224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37225(int& val) {
    func_in_37225(val);
    const int x = 37225;
    std::cout << 37225;
    for (int i = 0; i < 37225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37225(int& val) {
    const int x = 37225;
    std::cout << 37225;
    for (int i = 0; i < 37225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37226(int& val) {
    func_in_37226(val);
    const int x = 37226;
    std::cout << 37226;
    for (int i = 0; i < 37226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37226(int& val) {
    const int x = 37226;
    std::cout << 37226;
    for (int i = 0; i < 37226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37227(int& val) {
    func_in_37227(val);
    const int x = 37227;
    std::cout << 37227;
    for (int i = 0; i < 37227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37227(int& val) {
    const int x = 37227;
    std::cout << 37227;
    for (int i = 0; i < 37227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37228(int& val) {
    func_in_37228(val);
    const int x = 37228;
    std::cout << 37228;
    for (int i = 0; i < 37228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37228(int& val) {
    const int x = 37228;
    std::cout << 37228;
    for (int i = 0; i < 37228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37229(int& val) {
    func_in_37229(val);
    const int x = 37229;
    std::cout << 37229;
    for (int i = 0; i < 37229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37229(int& val) {
    const int x = 37229;
    std::cout << 37229;
    for (int i = 0; i < 37229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37230(int& val) {
    func_in_37230(val);
    const int x = 37230;
    std::cout << 37230;
    for (int i = 0; i < 37230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37230(int& val) {
    const int x = 37230;
    std::cout << 37230;
    for (int i = 0; i < 37230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37231(int& val) {
    func_in_37231(val);
    const int x = 37231;
    std::cout << 37231;
    for (int i = 0; i < 37231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37231(int& val) {
    const int x = 37231;
    std::cout << 37231;
    for (int i = 0; i < 37231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37232(int& val) {
    func_in_37232(val);
    const int x = 37232;
    std::cout << 37232;
    for (int i = 0; i < 37232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37232(int& val) {
    const int x = 37232;
    std::cout << 37232;
    for (int i = 0; i < 37232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37233(int& val) {
    func_in_37233(val);
    const int x = 37233;
    std::cout << 37233;
    for (int i = 0; i < 37233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37233(int& val) {
    const int x = 37233;
    std::cout << 37233;
    for (int i = 0; i < 37233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37234(int& val) {
    func_in_37234(val);
    const int x = 37234;
    std::cout << 37234;
    for (int i = 0; i < 37234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37234(int& val) {
    const int x = 37234;
    std::cout << 37234;
    for (int i = 0; i < 37234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37235(int& val) {
    func_in_37235(val);
    const int x = 37235;
    std::cout << 37235;
    for (int i = 0; i < 37235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37235(int& val) {
    const int x = 37235;
    std::cout << 37235;
    for (int i = 0; i < 37235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37236(int& val) {
    func_in_37236(val);
    const int x = 37236;
    std::cout << 37236;
    for (int i = 0; i < 37236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37236(int& val) {
    const int x = 37236;
    std::cout << 37236;
    for (int i = 0; i < 37236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37237(int& val) {
    func_in_37237(val);
    const int x = 37237;
    std::cout << 37237;
    for (int i = 0; i < 37237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37237(int& val) {
    const int x = 37237;
    std::cout << 37237;
    for (int i = 0; i < 37237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37238(int& val) {
    func_in_37238(val);
    const int x = 37238;
    std::cout << 37238;
    for (int i = 0; i < 37238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37238(int& val) {
    const int x = 37238;
    std::cout << 37238;
    for (int i = 0; i < 37238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37239(int& val) {
    func_in_37239(val);
    const int x = 37239;
    std::cout << 37239;
    for (int i = 0; i < 37239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37239(int& val) {
    const int x = 37239;
    std::cout << 37239;
    for (int i = 0; i < 37239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37240(int& val) {
    func_in_37240(val);
    const int x = 37240;
    std::cout << 37240;
    for (int i = 0; i < 37240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37240(int& val) {
    const int x = 37240;
    std::cout << 37240;
    for (int i = 0; i < 37240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37241(int& val) {
    func_in_37241(val);
    const int x = 37241;
    std::cout << 37241;
    for (int i = 0; i < 37241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37241(int& val) {
    const int x = 37241;
    std::cout << 37241;
    for (int i = 0; i < 37241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37242(int& val) {
    func_in_37242(val);
    const int x = 37242;
    std::cout << 37242;
    for (int i = 0; i < 37242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37242(int& val) {
    const int x = 37242;
    std::cout << 37242;
    for (int i = 0; i < 37242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37243(int& val) {
    func_in_37243(val);
    const int x = 37243;
    std::cout << 37243;
    for (int i = 0; i < 37243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37243(int& val) {
    const int x = 37243;
    std::cout << 37243;
    for (int i = 0; i < 37243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37244(int& val) {
    func_in_37244(val);
    const int x = 37244;
    std::cout << 37244;
    for (int i = 0; i < 37244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37244(int& val) {
    const int x = 37244;
    std::cout << 37244;
    for (int i = 0; i < 37244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37245(int& val) {
    func_in_37245(val);
    const int x = 37245;
    std::cout << 37245;
    for (int i = 0; i < 37245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37245(int& val) {
    const int x = 37245;
    std::cout << 37245;
    for (int i = 0; i < 37245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37246(int& val) {
    func_in_37246(val);
    const int x = 37246;
    std::cout << 37246;
    for (int i = 0; i < 37246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37246(int& val) {
    const int x = 37246;
    std::cout << 37246;
    for (int i = 0; i < 37246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37247(int& val) {
    func_in_37247(val);
    const int x = 37247;
    std::cout << 37247;
    for (int i = 0; i < 37247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37247(int& val) {
    const int x = 37247;
    std::cout << 37247;
    for (int i = 0; i < 37247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37248(int& val) {
    func_in_37248(val);
    const int x = 37248;
    std::cout << 37248;
    for (int i = 0; i < 37248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37248(int& val) {
    const int x = 37248;
    std::cout << 37248;
    for (int i = 0; i < 37248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37249(int& val) {
    func_in_37249(val);
    const int x = 37249;
    std::cout << 37249;
    for (int i = 0; i < 37249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37249(int& val) {
    const int x = 37249;
    std::cout << 37249;
    for (int i = 0; i < 37249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37250(int& val) {
    func_in_37250(val);
    const int x = 37250;
    std::cout << 37250;
    for (int i = 0; i < 37250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37250(int& val) {
    const int x = 37250;
    std::cout << 37250;
    for (int i = 0; i < 37250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37251(int& val) {
    func_in_37251(val);
    const int x = 37251;
    std::cout << 37251;
    for (int i = 0; i < 37251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37251(int& val) {
    const int x = 37251;
    std::cout << 37251;
    for (int i = 0; i < 37251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37252(int& val) {
    func_in_37252(val);
    const int x = 37252;
    std::cout << 37252;
    for (int i = 0; i < 37252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37252(int& val) {
    const int x = 37252;
    std::cout << 37252;
    for (int i = 0; i < 37252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37253(int& val) {
    func_in_37253(val);
    const int x = 37253;
    std::cout << 37253;
    for (int i = 0; i < 37253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37253(int& val) {
    const int x = 37253;
    std::cout << 37253;
    for (int i = 0; i < 37253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37254(int& val) {
    func_in_37254(val);
    const int x = 37254;
    std::cout << 37254;
    for (int i = 0; i < 37254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37254(int& val) {
    const int x = 37254;
    std::cout << 37254;
    for (int i = 0; i < 37254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37255(int& val) {
    func_in_37255(val);
    const int x = 37255;
    std::cout << 37255;
    for (int i = 0; i < 37255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37255(int& val) {
    const int x = 37255;
    std::cout << 37255;
    for (int i = 0; i < 37255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37256(int& val) {
    func_in_37256(val);
    const int x = 37256;
    std::cout << 37256;
    for (int i = 0; i < 37256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37256(int& val) {
    const int x = 37256;
    std::cout << 37256;
    for (int i = 0; i < 37256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37257(int& val) {
    func_in_37257(val);
    const int x = 37257;
    std::cout << 37257;
    for (int i = 0; i < 37257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37257(int& val) {
    const int x = 37257;
    std::cout << 37257;
    for (int i = 0; i < 37257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37258(int& val) {
    func_in_37258(val);
    const int x = 37258;
    std::cout << 37258;
    for (int i = 0; i < 37258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37258(int& val) {
    const int x = 37258;
    std::cout << 37258;
    for (int i = 0; i < 37258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37259(int& val) {
    func_in_37259(val);
    const int x = 37259;
    std::cout << 37259;
    for (int i = 0; i < 37259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37259(int& val) {
    const int x = 37259;
    std::cout << 37259;
    for (int i = 0; i < 37259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37260(int& val) {
    func_in_37260(val);
    const int x = 37260;
    std::cout << 37260;
    for (int i = 0; i < 37260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37260(int& val) {
    const int x = 37260;
    std::cout << 37260;
    for (int i = 0; i < 37260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37261(int& val) {
    func_in_37261(val);
    const int x = 37261;
    std::cout << 37261;
    for (int i = 0; i < 37261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37261(int& val) {
    const int x = 37261;
    std::cout << 37261;
    for (int i = 0; i < 37261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37262(int& val) {
    func_in_37262(val);
    const int x = 37262;
    std::cout << 37262;
    for (int i = 0; i < 37262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37262(int& val) {
    const int x = 37262;
    std::cout << 37262;
    for (int i = 0; i < 37262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37263(int& val) {
    func_in_37263(val);
    const int x = 37263;
    std::cout << 37263;
    for (int i = 0; i < 37263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37263(int& val) {
    const int x = 37263;
    std::cout << 37263;
    for (int i = 0; i < 37263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37264(int& val) {
    func_in_37264(val);
    const int x = 37264;
    std::cout << 37264;
    for (int i = 0; i < 37264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37264(int& val) {
    const int x = 37264;
    std::cout << 37264;
    for (int i = 0; i < 37264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37265(int& val) {
    func_in_37265(val);
    const int x = 37265;
    std::cout << 37265;
    for (int i = 0; i < 37265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37265(int& val) {
    const int x = 37265;
    std::cout << 37265;
    for (int i = 0; i < 37265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37266(int& val) {
    func_in_37266(val);
    const int x = 37266;
    std::cout << 37266;
    for (int i = 0; i < 37266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37266(int& val) {
    const int x = 37266;
    std::cout << 37266;
    for (int i = 0; i < 37266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37267(int& val) {
    func_in_37267(val);
    const int x = 37267;
    std::cout << 37267;
    for (int i = 0; i < 37267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37267(int& val) {
    const int x = 37267;
    std::cout << 37267;
    for (int i = 0; i < 37267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37268(int& val) {
    func_in_37268(val);
    const int x = 37268;
    std::cout << 37268;
    for (int i = 0; i < 37268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37268(int& val) {
    const int x = 37268;
    std::cout << 37268;
    for (int i = 0; i < 37268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37269(int& val) {
    func_in_37269(val);
    const int x = 37269;
    std::cout << 37269;
    for (int i = 0; i < 37269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37269(int& val) {
    const int x = 37269;
    std::cout << 37269;
    for (int i = 0; i < 37269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37270(int& val) {
    func_in_37270(val);
    const int x = 37270;
    std::cout << 37270;
    for (int i = 0; i < 37270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37270(int& val) {
    const int x = 37270;
    std::cout << 37270;
    for (int i = 0; i < 37270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37271(int& val) {
    func_in_37271(val);
    const int x = 37271;
    std::cout << 37271;
    for (int i = 0; i < 37271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37271(int& val) {
    const int x = 37271;
    std::cout << 37271;
    for (int i = 0; i < 37271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37272(int& val) {
    func_in_37272(val);
    const int x = 37272;
    std::cout << 37272;
    for (int i = 0; i < 37272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37272(int& val) {
    const int x = 37272;
    std::cout << 37272;
    for (int i = 0; i < 37272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37273(int& val) {
    func_in_37273(val);
    const int x = 37273;
    std::cout << 37273;
    for (int i = 0; i < 37273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37273(int& val) {
    const int x = 37273;
    std::cout << 37273;
    for (int i = 0; i < 37273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37274(int& val) {
    func_in_37274(val);
    const int x = 37274;
    std::cout << 37274;
    for (int i = 0; i < 37274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37274(int& val) {
    const int x = 37274;
    std::cout << 37274;
    for (int i = 0; i < 37274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37275(int& val) {
    func_in_37275(val);
    const int x = 37275;
    std::cout << 37275;
    for (int i = 0; i < 37275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37275(int& val) {
    const int x = 37275;
    std::cout << 37275;
    for (int i = 0; i < 37275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37276(int& val) {
    func_in_37276(val);
    const int x = 37276;
    std::cout << 37276;
    for (int i = 0; i < 37276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37276(int& val) {
    const int x = 37276;
    std::cout << 37276;
    for (int i = 0; i < 37276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37277(int& val) {
    func_in_37277(val);
    const int x = 37277;
    std::cout << 37277;
    for (int i = 0; i < 37277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37277(int& val) {
    const int x = 37277;
    std::cout << 37277;
    for (int i = 0; i < 37277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37278(int& val) {
    func_in_37278(val);
    const int x = 37278;
    std::cout << 37278;
    for (int i = 0; i < 37278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37278(int& val) {
    const int x = 37278;
    std::cout << 37278;
    for (int i = 0; i < 37278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37279(int& val) {
    func_in_37279(val);
    const int x = 37279;
    std::cout << 37279;
    for (int i = 0; i < 37279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37279(int& val) {
    const int x = 37279;
    std::cout << 37279;
    for (int i = 0; i < 37279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37280(int& val) {
    func_in_37280(val);
    const int x = 37280;
    std::cout << 37280;
    for (int i = 0; i < 37280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37280(int& val) {
    const int x = 37280;
    std::cout << 37280;
    for (int i = 0; i < 37280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37281(int& val) {
    func_in_37281(val);
    const int x = 37281;
    std::cout << 37281;
    for (int i = 0; i < 37281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37281(int& val) {
    const int x = 37281;
    std::cout << 37281;
    for (int i = 0; i < 37281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37282(int& val) {
    func_in_37282(val);
    const int x = 37282;
    std::cout << 37282;
    for (int i = 0; i < 37282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37282(int& val) {
    const int x = 37282;
    std::cout << 37282;
    for (int i = 0; i < 37282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37283(int& val) {
    func_in_37283(val);
    const int x = 37283;
    std::cout << 37283;
    for (int i = 0; i < 37283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37283(int& val) {
    const int x = 37283;
    std::cout << 37283;
    for (int i = 0; i < 37283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37284(int& val) {
    func_in_37284(val);
    const int x = 37284;
    std::cout << 37284;
    for (int i = 0; i < 37284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37284(int& val) {
    const int x = 37284;
    std::cout << 37284;
    for (int i = 0; i < 37284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37285(int& val) {
    func_in_37285(val);
    const int x = 37285;
    std::cout << 37285;
    for (int i = 0; i < 37285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37285(int& val) {
    const int x = 37285;
    std::cout << 37285;
    for (int i = 0; i < 37285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37286(int& val) {
    func_in_37286(val);
    const int x = 37286;
    std::cout << 37286;
    for (int i = 0; i < 37286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37286(int& val) {
    const int x = 37286;
    std::cout << 37286;
    for (int i = 0; i < 37286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37287(int& val) {
    func_in_37287(val);
    const int x = 37287;
    std::cout << 37287;
    for (int i = 0; i < 37287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37287(int& val) {
    const int x = 37287;
    std::cout << 37287;
    for (int i = 0; i < 37287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37288(int& val) {
    func_in_37288(val);
    const int x = 37288;
    std::cout << 37288;
    for (int i = 0; i < 37288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37288(int& val) {
    const int x = 37288;
    std::cout << 37288;
    for (int i = 0; i < 37288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37289(int& val) {
    func_in_37289(val);
    const int x = 37289;
    std::cout << 37289;
    for (int i = 0; i < 37289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37289(int& val) {
    const int x = 37289;
    std::cout << 37289;
    for (int i = 0; i < 37289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37290(int& val) {
    func_in_37290(val);
    const int x = 37290;
    std::cout << 37290;
    for (int i = 0; i < 37290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37290(int& val) {
    const int x = 37290;
    std::cout << 37290;
    for (int i = 0; i < 37290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37291(int& val) {
    func_in_37291(val);
    const int x = 37291;
    std::cout << 37291;
    for (int i = 0; i < 37291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37291(int& val) {
    const int x = 37291;
    std::cout << 37291;
    for (int i = 0; i < 37291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37292(int& val) {
    func_in_37292(val);
    const int x = 37292;
    std::cout << 37292;
    for (int i = 0; i < 37292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37292(int& val) {
    const int x = 37292;
    std::cout << 37292;
    for (int i = 0; i < 37292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37293(int& val) {
    func_in_37293(val);
    const int x = 37293;
    std::cout << 37293;
    for (int i = 0; i < 37293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37293(int& val) {
    const int x = 37293;
    std::cout << 37293;
    for (int i = 0; i < 37293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37294(int& val) {
    func_in_37294(val);
    const int x = 37294;
    std::cout << 37294;
    for (int i = 0; i < 37294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37294(int& val) {
    const int x = 37294;
    std::cout << 37294;
    for (int i = 0; i < 37294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37295(int& val) {
    func_in_37295(val);
    const int x = 37295;
    std::cout << 37295;
    for (int i = 0; i < 37295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37295(int& val) {
    const int x = 37295;
    std::cout << 37295;
    for (int i = 0; i < 37295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37296(int& val) {
    func_in_37296(val);
    const int x = 37296;
    std::cout << 37296;
    for (int i = 0; i < 37296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37296(int& val) {
    const int x = 37296;
    std::cout << 37296;
    for (int i = 0; i < 37296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37297(int& val) {
    func_in_37297(val);
    const int x = 37297;
    std::cout << 37297;
    for (int i = 0; i < 37297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37297(int& val) {
    const int x = 37297;
    std::cout << 37297;
    for (int i = 0; i < 37297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37298(int& val) {
    func_in_37298(val);
    const int x = 37298;
    std::cout << 37298;
    for (int i = 0; i < 37298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37298(int& val) {
    const int x = 37298;
    std::cout << 37298;
    for (int i = 0; i < 37298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37299(int& val) {
    func_in_37299(val);
    const int x = 37299;
    std::cout << 37299;
    for (int i = 0; i < 37299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37299(int& val) {
    const int x = 37299;
    std::cout << 37299;
    for (int i = 0; i < 37299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37300(int& val) {
    func_in_37300(val);
    const int x = 37300;
    std::cout << 37300;
    for (int i = 0; i < 37300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37300(int& val) {
    const int x = 37300;
    std::cout << 37300;
    for (int i = 0; i < 37300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37301(int& val) {
    func_in_37301(val);
    const int x = 37301;
    std::cout << 37301;
    for (int i = 0; i < 37301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37301(int& val) {
    const int x = 37301;
    std::cout << 37301;
    for (int i = 0; i < 37301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37302(int& val) {
    func_in_37302(val);
    const int x = 37302;
    std::cout << 37302;
    for (int i = 0; i < 37302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37302(int& val) {
    const int x = 37302;
    std::cout << 37302;
    for (int i = 0; i < 37302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37303(int& val) {
    func_in_37303(val);
    const int x = 37303;
    std::cout << 37303;
    for (int i = 0; i < 37303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37303(int& val) {
    const int x = 37303;
    std::cout << 37303;
    for (int i = 0; i < 37303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37304(int& val) {
    func_in_37304(val);
    const int x = 37304;
    std::cout << 37304;
    for (int i = 0; i < 37304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37304(int& val) {
    const int x = 37304;
    std::cout << 37304;
    for (int i = 0; i < 37304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37305(int& val) {
    func_in_37305(val);
    const int x = 37305;
    std::cout << 37305;
    for (int i = 0; i < 37305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37305(int& val) {
    const int x = 37305;
    std::cout << 37305;
    for (int i = 0; i < 37305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37306(int& val) {
    func_in_37306(val);
    const int x = 37306;
    std::cout << 37306;
    for (int i = 0; i < 37306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37306(int& val) {
    const int x = 37306;
    std::cout << 37306;
    for (int i = 0; i < 37306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37307(int& val) {
    func_in_37307(val);
    const int x = 37307;
    std::cout << 37307;
    for (int i = 0; i < 37307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37307(int& val) {
    const int x = 37307;
    std::cout << 37307;
    for (int i = 0; i < 37307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37308(int& val) {
    func_in_37308(val);
    const int x = 37308;
    std::cout << 37308;
    for (int i = 0; i < 37308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37308(int& val) {
    const int x = 37308;
    std::cout << 37308;
    for (int i = 0; i < 37308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37309(int& val) {
    func_in_37309(val);
    const int x = 37309;
    std::cout << 37309;
    for (int i = 0; i < 37309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37309(int& val) {
    const int x = 37309;
    std::cout << 37309;
    for (int i = 0; i < 37309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37310(int& val) {
    func_in_37310(val);
    const int x = 37310;
    std::cout << 37310;
    for (int i = 0; i < 37310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37310(int& val) {
    const int x = 37310;
    std::cout << 37310;
    for (int i = 0; i < 37310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37311(int& val) {
    func_in_37311(val);
    const int x = 37311;
    std::cout << 37311;
    for (int i = 0; i < 37311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37311(int& val) {
    const int x = 37311;
    std::cout << 37311;
    for (int i = 0; i < 37311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37312(int& val) {
    func_in_37312(val);
    const int x = 37312;
    std::cout << 37312;
    for (int i = 0; i < 37312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37312(int& val) {
    const int x = 37312;
    std::cout << 37312;
    for (int i = 0; i < 37312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37313(int& val) {
    func_in_37313(val);
    const int x = 37313;
    std::cout << 37313;
    for (int i = 0; i < 37313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37313(int& val) {
    const int x = 37313;
    std::cout << 37313;
    for (int i = 0; i < 37313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37314(int& val) {
    func_in_37314(val);
    const int x = 37314;
    std::cout << 37314;
    for (int i = 0; i < 37314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37314(int& val) {
    const int x = 37314;
    std::cout << 37314;
    for (int i = 0; i < 37314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37315(int& val) {
    func_in_37315(val);
    const int x = 37315;
    std::cout << 37315;
    for (int i = 0; i < 37315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37315(int& val) {
    const int x = 37315;
    std::cout << 37315;
    for (int i = 0; i < 37315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37316(int& val) {
    func_in_37316(val);
    const int x = 37316;
    std::cout << 37316;
    for (int i = 0; i < 37316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37316(int& val) {
    const int x = 37316;
    std::cout << 37316;
    for (int i = 0; i < 37316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37317(int& val) {
    func_in_37317(val);
    const int x = 37317;
    std::cout << 37317;
    for (int i = 0; i < 37317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37317(int& val) {
    const int x = 37317;
    std::cout << 37317;
    for (int i = 0; i < 37317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37318(int& val) {
    func_in_37318(val);
    const int x = 37318;
    std::cout << 37318;
    for (int i = 0; i < 37318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37318(int& val) {
    const int x = 37318;
    std::cout << 37318;
    for (int i = 0; i < 37318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37319(int& val) {
    func_in_37319(val);
    const int x = 37319;
    std::cout << 37319;
    for (int i = 0; i < 37319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37319(int& val) {
    const int x = 37319;
    std::cout << 37319;
    for (int i = 0; i < 37319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37320(int& val) {
    func_in_37320(val);
    const int x = 37320;
    std::cout << 37320;
    for (int i = 0; i < 37320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37320(int& val) {
    const int x = 37320;
    std::cout << 37320;
    for (int i = 0; i < 37320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37321(int& val) {
    func_in_37321(val);
    const int x = 37321;
    std::cout << 37321;
    for (int i = 0; i < 37321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37321(int& val) {
    const int x = 37321;
    std::cout << 37321;
    for (int i = 0; i < 37321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37322(int& val) {
    func_in_37322(val);
    const int x = 37322;
    std::cout << 37322;
    for (int i = 0; i < 37322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37322(int& val) {
    const int x = 37322;
    std::cout << 37322;
    for (int i = 0; i < 37322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37323(int& val) {
    func_in_37323(val);
    const int x = 37323;
    std::cout << 37323;
    for (int i = 0; i < 37323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37323(int& val) {
    const int x = 37323;
    std::cout << 37323;
    for (int i = 0; i < 37323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37324(int& val) {
    func_in_37324(val);
    const int x = 37324;
    std::cout << 37324;
    for (int i = 0; i < 37324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37324(int& val) {
    const int x = 37324;
    std::cout << 37324;
    for (int i = 0; i < 37324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37325(int& val) {
    func_in_37325(val);
    const int x = 37325;
    std::cout << 37325;
    for (int i = 0; i < 37325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37325(int& val) {
    const int x = 37325;
    std::cout << 37325;
    for (int i = 0; i < 37325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37326(int& val) {
    func_in_37326(val);
    const int x = 37326;
    std::cout << 37326;
    for (int i = 0; i < 37326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37326(int& val) {
    const int x = 37326;
    std::cout << 37326;
    for (int i = 0; i < 37326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37327(int& val) {
    func_in_37327(val);
    const int x = 37327;
    std::cout << 37327;
    for (int i = 0; i < 37327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37327(int& val) {
    const int x = 37327;
    std::cout << 37327;
    for (int i = 0; i < 37327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37328(int& val) {
    func_in_37328(val);
    const int x = 37328;
    std::cout << 37328;
    for (int i = 0; i < 37328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37328(int& val) {
    const int x = 37328;
    std::cout << 37328;
    for (int i = 0; i < 37328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37329(int& val) {
    func_in_37329(val);
    const int x = 37329;
    std::cout << 37329;
    for (int i = 0; i < 37329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37329(int& val) {
    const int x = 37329;
    std::cout << 37329;
    for (int i = 0; i < 37329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37330(int& val) {
    func_in_37330(val);
    const int x = 37330;
    std::cout << 37330;
    for (int i = 0; i < 37330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37330(int& val) {
    const int x = 37330;
    std::cout << 37330;
    for (int i = 0; i < 37330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37331(int& val) {
    func_in_37331(val);
    const int x = 37331;
    std::cout << 37331;
    for (int i = 0; i < 37331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37331(int& val) {
    const int x = 37331;
    std::cout << 37331;
    for (int i = 0; i < 37331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37332(int& val) {
    func_in_37332(val);
    const int x = 37332;
    std::cout << 37332;
    for (int i = 0; i < 37332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37332(int& val) {
    const int x = 37332;
    std::cout << 37332;
    for (int i = 0; i < 37332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37333(int& val) {
    func_in_37333(val);
    const int x = 37333;
    std::cout << 37333;
    for (int i = 0; i < 37333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37333(int& val) {
    const int x = 37333;
    std::cout << 37333;
    for (int i = 0; i < 37333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37334(int& val) {
    func_in_37334(val);
    const int x = 37334;
    std::cout << 37334;
    for (int i = 0; i < 37334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37334(int& val) {
    const int x = 37334;
    std::cout << 37334;
    for (int i = 0; i < 37334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37335(int& val) {
    func_in_37335(val);
    const int x = 37335;
    std::cout << 37335;
    for (int i = 0; i < 37335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37335(int& val) {
    const int x = 37335;
    std::cout << 37335;
    for (int i = 0; i < 37335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37336(int& val) {
    func_in_37336(val);
    const int x = 37336;
    std::cout << 37336;
    for (int i = 0; i < 37336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37336(int& val) {
    const int x = 37336;
    std::cout << 37336;
    for (int i = 0; i < 37336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37337(int& val) {
    func_in_37337(val);
    const int x = 37337;
    std::cout << 37337;
    for (int i = 0; i < 37337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37337(int& val) {
    const int x = 37337;
    std::cout << 37337;
    for (int i = 0; i < 37337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37338(int& val) {
    func_in_37338(val);
    const int x = 37338;
    std::cout << 37338;
    for (int i = 0; i < 37338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37338(int& val) {
    const int x = 37338;
    std::cout << 37338;
    for (int i = 0; i < 37338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37339(int& val) {
    func_in_37339(val);
    const int x = 37339;
    std::cout << 37339;
    for (int i = 0; i < 37339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37339(int& val) {
    const int x = 37339;
    std::cout << 37339;
    for (int i = 0; i < 37339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37340(int& val) {
    func_in_37340(val);
    const int x = 37340;
    std::cout << 37340;
    for (int i = 0; i < 37340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37340(int& val) {
    const int x = 37340;
    std::cout << 37340;
    for (int i = 0; i < 37340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37341(int& val) {
    func_in_37341(val);
    const int x = 37341;
    std::cout << 37341;
    for (int i = 0; i < 37341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37341(int& val) {
    const int x = 37341;
    std::cout << 37341;
    for (int i = 0; i < 37341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37342(int& val) {
    func_in_37342(val);
    const int x = 37342;
    std::cout << 37342;
    for (int i = 0; i < 37342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37342(int& val) {
    const int x = 37342;
    std::cout << 37342;
    for (int i = 0; i < 37342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37343(int& val) {
    func_in_37343(val);
    const int x = 37343;
    std::cout << 37343;
    for (int i = 0; i < 37343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37343(int& val) {
    const int x = 37343;
    std::cout << 37343;
    for (int i = 0; i < 37343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37344(int& val) {
    func_in_37344(val);
    const int x = 37344;
    std::cout << 37344;
    for (int i = 0; i < 37344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37344(int& val) {
    const int x = 37344;
    std::cout << 37344;
    for (int i = 0; i < 37344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37345(int& val) {
    func_in_37345(val);
    const int x = 37345;
    std::cout << 37345;
    for (int i = 0; i < 37345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37345(int& val) {
    const int x = 37345;
    std::cout << 37345;
    for (int i = 0; i < 37345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37346(int& val) {
    func_in_37346(val);
    const int x = 37346;
    std::cout << 37346;
    for (int i = 0; i < 37346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37346(int& val) {
    const int x = 37346;
    std::cout << 37346;
    for (int i = 0; i < 37346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37347(int& val) {
    func_in_37347(val);
    const int x = 37347;
    std::cout << 37347;
    for (int i = 0; i < 37347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37347(int& val) {
    const int x = 37347;
    std::cout << 37347;
    for (int i = 0; i < 37347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37348(int& val) {
    func_in_37348(val);
    const int x = 37348;
    std::cout << 37348;
    for (int i = 0; i < 37348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37348(int& val) {
    const int x = 37348;
    std::cout << 37348;
    for (int i = 0; i < 37348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37349(int& val) {
    func_in_37349(val);
    const int x = 37349;
    std::cout << 37349;
    for (int i = 0; i < 37349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37349(int& val) {
    const int x = 37349;
    std::cout << 37349;
    for (int i = 0; i < 37349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37350(int& val) {
    func_in_37350(val);
    const int x = 37350;
    std::cout << 37350;
    for (int i = 0; i < 37350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37350(int& val) {
    const int x = 37350;
    std::cout << 37350;
    for (int i = 0; i < 37350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37351(int& val) {
    func_in_37351(val);
    const int x = 37351;
    std::cout << 37351;
    for (int i = 0; i < 37351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37351(int& val) {
    const int x = 37351;
    std::cout << 37351;
    for (int i = 0; i < 37351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37352(int& val) {
    func_in_37352(val);
    const int x = 37352;
    std::cout << 37352;
    for (int i = 0; i < 37352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37352(int& val) {
    const int x = 37352;
    std::cout << 37352;
    for (int i = 0; i < 37352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37353(int& val) {
    func_in_37353(val);
    const int x = 37353;
    std::cout << 37353;
    for (int i = 0; i < 37353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37353(int& val) {
    const int x = 37353;
    std::cout << 37353;
    for (int i = 0; i < 37353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37354(int& val) {
    func_in_37354(val);
    const int x = 37354;
    std::cout << 37354;
    for (int i = 0; i < 37354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37354(int& val) {
    const int x = 37354;
    std::cout << 37354;
    for (int i = 0; i < 37354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37355(int& val) {
    func_in_37355(val);
    const int x = 37355;
    std::cout << 37355;
    for (int i = 0; i < 37355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37355(int& val) {
    const int x = 37355;
    std::cout << 37355;
    for (int i = 0; i < 37355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37356(int& val) {
    func_in_37356(val);
    const int x = 37356;
    std::cout << 37356;
    for (int i = 0; i < 37356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37356(int& val) {
    const int x = 37356;
    std::cout << 37356;
    for (int i = 0; i < 37356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37357(int& val) {
    func_in_37357(val);
    const int x = 37357;
    std::cout << 37357;
    for (int i = 0; i < 37357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37357(int& val) {
    const int x = 37357;
    std::cout << 37357;
    for (int i = 0; i < 37357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37358(int& val) {
    func_in_37358(val);
    const int x = 37358;
    std::cout << 37358;
    for (int i = 0; i < 37358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37358(int& val) {
    const int x = 37358;
    std::cout << 37358;
    for (int i = 0; i < 37358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37359(int& val) {
    func_in_37359(val);
    const int x = 37359;
    std::cout << 37359;
    for (int i = 0; i < 37359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37359(int& val) {
    const int x = 37359;
    std::cout << 37359;
    for (int i = 0; i < 37359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37360(int& val) {
    func_in_37360(val);
    const int x = 37360;
    std::cout << 37360;
    for (int i = 0; i < 37360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37360(int& val) {
    const int x = 37360;
    std::cout << 37360;
    for (int i = 0; i < 37360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37361(int& val) {
    func_in_37361(val);
    const int x = 37361;
    std::cout << 37361;
    for (int i = 0; i < 37361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37361(int& val) {
    const int x = 37361;
    std::cout << 37361;
    for (int i = 0; i < 37361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37362(int& val) {
    func_in_37362(val);
    const int x = 37362;
    std::cout << 37362;
    for (int i = 0; i < 37362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37362(int& val) {
    const int x = 37362;
    std::cout << 37362;
    for (int i = 0; i < 37362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37363(int& val) {
    func_in_37363(val);
    const int x = 37363;
    std::cout << 37363;
    for (int i = 0; i < 37363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37363(int& val) {
    const int x = 37363;
    std::cout << 37363;
    for (int i = 0; i < 37363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37364(int& val) {
    func_in_37364(val);
    const int x = 37364;
    std::cout << 37364;
    for (int i = 0; i < 37364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37364(int& val) {
    const int x = 37364;
    std::cout << 37364;
    for (int i = 0; i < 37364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37365(int& val) {
    func_in_37365(val);
    const int x = 37365;
    std::cout << 37365;
    for (int i = 0; i < 37365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37365(int& val) {
    const int x = 37365;
    std::cout << 37365;
    for (int i = 0; i < 37365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37366(int& val) {
    func_in_37366(val);
    const int x = 37366;
    std::cout << 37366;
    for (int i = 0; i < 37366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37366(int& val) {
    const int x = 37366;
    std::cout << 37366;
    for (int i = 0; i < 37366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37367(int& val) {
    func_in_37367(val);
    const int x = 37367;
    std::cout << 37367;
    for (int i = 0; i < 37367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37367(int& val) {
    const int x = 37367;
    std::cout << 37367;
    for (int i = 0; i < 37367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37368(int& val) {
    func_in_37368(val);
    const int x = 37368;
    std::cout << 37368;
    for (int i = 0; i < 37368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37368(int& val) {
    const int x = 37368;
    std::cout << 37368;
    for (int i = 0; i < 37368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37369(int& val) {
    func_in_37369(val);
    const int x = 37369;
    std::cout << 37369;
    for (int i = 0; i < 37369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37369(int& val) {
    const int x = 37369;
    std::cout << 37369;
    for (int i = 0; i < 37369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37370(int& val) {
    func_in_37370(val);
    const int x = 37370;
    std::cout << 37370;
    for (int i = 0; i < 37370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37370(int& val) {
    const int x = 37370;
    std::cout << 37370;
    for (int i = 0; i < 37370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37371(int& val) {
    func_in_37371(val);
    const int x = 37371;
    std::cout << 37371;
    for (int i = 0; i < 37371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37371(int& val) {
    const int x = 37371;
    std::cout << 37371;
    for (int i = 0; i < 37371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37372(int& val) {
    func_in_37372(val);
    const int x = 37372;
    std::cout << 37372;
    for (int i = 0; i < 37372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37372(int& val) {
    const int x = 37372;
    std::cout << 37372;
    for (int i = 0; i < 37372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37373(int& val) {
    func_in_37373(val);
    const int x = 37373;
    std::cout << 37373;
    for (int i = 0; i < 37373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37373(int& val) {
    const int x = 37373;
    std::cout << 37373;
    for (int i = 0; i < 37373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37374(int& val) {
    func_in_37374(val);
    const int x = 37374;
    std::cout << 37374;
    for (int i = 0; i < 37374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37374(int& val) {
    const int x = 37374;
    std::cout << 37374;
    for (int i = 0; i < 37374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37375(int& val) {
    func_in_37375(val);
    const int x = 37375;
    std::cout << 37375;
    for (int i = 0; i < 37375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37375(int& val) {
    const int x = 37375;
    std::cout << 37375;
    for (int i = 0; i < 37375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37376(int& val) {
    func_in_37376(val);
    const int x = 37376;
    std::cout << 37376;
    for (int i = 0; i < 37376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37376(int& val) {
    const int x = 37376;
    std::cout << 37376;
    for (int i = 0; i < 37376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37377(int& val) {
    func_in_37377(val);
    const int x = 37377;
    std::cout << 37377;
    for (int i = 0; i < 37377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37377(int& val) {
    const int x = 37377;
    std::cout << 37377;
    for (int i = 0; i < 37377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37378(int& val) {
    func_in_37378(val);
    const int x = 37378;
    std::cout << 37378;
    for (int i = 0; i < 37378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37378(int& val) {
    const int x = 37378;
    std::cout << 37378;
    for (int i = 0; i < 37378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37379(int& val) {
    func_in_37379(val);
    const int x = 37379;
    std::cout << 37379;
    for (int i = 0; i < 37379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37379(int& val) {
    const int x = 37379;
    std::cout << 37379;
    for (int i = 0; i < 37379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37380(int& val) {
    func_in_37380(val);
    const int x = 37380;
    std::cout << 37380;
    for (int i = 0; i < 37380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37380(int& val) {
    const int x = 37380;
    std::cout << 37380;
    for (int i = 0; i < 37380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37381(int& val) {
    func_in_37381(val);
    const int x = 37381;
    std::cout << 37381;
    for (int i = 0; i < 37381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37381(int& val) {
    const int x = 37381;
    std::cout << 37381;
    for (int i = 0; i < 37381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37382(int& val) {
    func_in_37382(val);
    const int x = 37382;
    std::cout << 37382;
    for (int i = 0; i < 37382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37382(int& val) {
    const int x = 37382;
    std::cout << 37382;
    for (int i = 0; i < 37382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37383(int& val) {
    func_in_37383(val);
    const int x = 37383;
    std::cout << 37383;
    for (int i = 0; i < 37383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37383(int& val) {
    const int x = 37383;
    std::cout << 37383;
    for (int i = 0; i < 37383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37384(int& val) {
    func_in_37384(val);
    const int x = 37384;
    std::cout << 37384;
    for (int i = 0; i < 37384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37384(int& val) {
    const int x = 37384;
    std::cout << 37384;
    for (int i = 0; i < 37384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37385(int& val) {
    func_in_37385(val);
    const int x = 37385;
    std::cout << 37385;
    for (int i = 0; i < 37385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37385(int& val) {
    const int x = 37385;
    std::cout << 37385;
    for (int i = 0; i < 37385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37386(int& val) {
    func_in_37386(val);
    const int x = 37386;
    std::cout << 37386;
    for (int i = 0; i < 37386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37386(int& val) {
    const int x = 37386;
    std::cout << 37386;
    for (int i = 0; i < 37386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37387(int& val) {
    func_in_37387(val);
    const int x = 37387;
    std::cout << 37387;
    for (int i = 0; i < 37387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37387(int& val) {
    const int x = 37387;
    std::cout << 37387;
    for (int i = 0; i < 37387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37388(int& val) {
    func_in_37388(val);
    const int x = 37388;
    std::cout << 37388;
    for (int i = 0; i < 37388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37388(int& val) {
    const int x = 37388;
    std::cout << 37388;
    for (int i = 0; i < 37388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37389(int& val) {
    func_in_37389(val);
    const int x = 37389;
    std::cout << 37389;
    for (int i = 0; i < 37389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37389(int& val) {
    const int x = 37389;
    std::cout << 37389;
    for (int i = 0; i < 37389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37390(int& val) {
    func_in_37390(val);
    const int x = 37390;
    std::cout << 37390;
    for (int i = 0; i < 37390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37390(int& val) {
    const int x = 37390;
    std::cout << 37390;
    for (int i = 0; i < 37390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37391(int& val) {
    func_in_37391(val);
    const int x = 37391;
    std::cout << 37391;
    for (int i = 0; i < 37391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37391(int& val) {
    const int x = 37391;
    std::cout << 37391;
    for (int i = 0; i < 37391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37392(int& val) {
    func_in_37392(val);
    const int x = 37392;
    std::cout << 37392;
    for (int i = 0; i < 37392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37392(int& val) {
    const int x = 37392;
    std::cout << 37392;
    for (int i = 0; i < 37392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37393(int& val) {
    func_in_37393(val);
    const int x = 37393;
    std::cout << 37393;
    for (int i = 0; i < 37393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37393(int& val) {
    const int x = 37393;
    std::cout << 37393;
    for (int i = 0; i < 37393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37394(int& val) {
    func_in_37394(val);
    const int x = 37394;
    std::cout << 37394;
    for (int i = 0; i < 37394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37394(int& val) {
    const int x = 37394;
    std::cout << 37394;
    for (int i = 0; i < 37394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37395(int& val) {
    func_in_37395(val);
    const int x = 37395;
    std::cout << 37395;
    for (int i = 0; i < 37395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37395(int& val) {
    const int x = 37395;
    std::cout << 37395;
    for (int i = 0; i < 37395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37396(int& val) {
    func_in_37396(val);
    const int x = 37396;
    std::cout << 37396;
    for (int i = 0; i < 37396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37396(int& val) {
    const int x = 37396;
    std::cout << 37396;
    for (int i = 0; i < 37396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37397(int& val) {
    func_in_37397(val);
    const int x = 37397;
    std::cout << 37397;
    for (int i = 0; i < 37397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37397(int& val) {
    const int x = 37397;
    std::cout << 37397;
    for (int i = 0; i < 37397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37398(int& val) {
    func_in_37398(val);
    const int x = 37398;
    std::cout << 37398;
    for (int i = 0; i < 37398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37398(int& val) {
    const int x = 37398;
    std::cout << 37398;
    for (int i = 0; i < 37398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37399(int& val) {
    func_in_37399(val);
    const int x = 37399;
    std::cout << 37399;
    for (int i = 0; i < 37399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37399(int& val) {
    const int x = 37399;
    std::cout << 37399;
    for (int i = 0; i < 37399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37400(int& val) {
    func_in_37400(val);
    const int x = 37400;
    std::cout << 37400;
    for (int i = 0; i < 37400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37400(int& val) {
    const int x = 37400;
    std::cout << 37400;
    for (int i = 0; i < 37400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37401(int& val) {
    func_in_37401(val);
    const int x = 37401;
    std::cout << 37401;
    for (int i = 0; i < 37401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37401(int& val) {
    const int x = 37401;
    std::cout << 37401;
    for (int i = 0; i < 37401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37402(int& val) {
    func_in_37402(val);
    const int x = 37402;
    std::cout << 37402;
    for (int i = 0; i < 37402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37402(int& val) {
    const int x = 37402;
    std::cout << 37402;
    for (int i = 0; i < 37402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37403(int& val) {
    func_in_37403(val);
    const int x = 37403;
    std::cout << 37403;
    for (int i = 0; i < 37403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37403(int& val) {
    const int x = 37403;
    std::cout << 37403;
    for (int i = 0; i < 37403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37404(int& val) {
    func_in_37404(val);
    const int x = 37404;
    std::cout << 37404;
    for (int i = 0; i < 37404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37404(int& val) {
    const int x = 37404;
    std::cout << 37404;
    for (int i = 0; i < 37404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37405(int& val) {
    func_in_37405(val);
    const int x = 37405;
    std::cout << 37405;
    for (int i = 0; i < 37405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37405(int& val) {
    const int x = 37405;
    std::cout << 37405;
    for (int i = 0; i < 37405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37406(int& val) {
    func_in_37406(val);
    const int x = 37406;
    std::cout << 37406;
    for (int i = 0; i < 37406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37406(int& val) {
    const int x = 37406;
    std::cout << 37406;
    for (int i = 0; i < 37406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37407(int& val) {
    func_in_37407(val);
    const int x = 37407;
    std::cout << 37407;
    for (int i = 0; i < 37407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37407(int& val) {
    const int x = 37407;
    std::cout << 37407;
    for (int i = 0; i < 37407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37408(int& val) {
    func_in_37408(val);
    const int x = 37408;
    std::cout << 37408;
    for (int i = 0; i < 37408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37408(int& val) {
    const int x = 37408;
    std::cout << 37408;
    for (int i = 0; i < 37408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37409(int& val) {
    func_in_37409(val);
    const int x = 37409;
    std::cout << 37409;
    for (int i = 0; i < 37409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37409(int& val) {
    const int x = 37409;
    std::cout << 37409;
    for (int i = 0; i < 37409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37410(int& val) {
    func_in_37410(val);
    const int x = 37410;
    std::cout << 37410;
    for (int i = 0; i < 37410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37410(int& val) {
    const int x = 37410;
    std::cout << 37410;
    for (int i = 0; i < 37410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37411(int& val) {
    func_in_37411(val);
    const int x = 37411;
    std::cout << 37411;
    for (int i = 0; i < 37411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37411(int& val) {
    const int x = 37411;
    std::cout << 37411;
    for (int i = 0; i < 37411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37412(int& val) {
    func_in_37412(val);
    const int x = 37412;
    std::cout << 37412;
    for (int i = 0; i < 37412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37412(int& val) {
    const int x = 37412;
    std::cout << 37412;
    for (int i = 0; i < 37412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37413(int& val) {
    func_in_37413(val);
    const int x = 37413;
    std::cout << 37413;
    for (int i = 0; i < 37413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37413(int& val) {
    const int x = 37413;
    std::cout << 37413;
    for (int i = 0; i < 37413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37414(int& val) {
    func_in_37414(val);
    const int x = 37414;
    std::cout << 37414;
    for (int i = 0; i < 37414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37414(int& val) {
    const int x = 37414;
    std::cout << 37414;
    for (int i = 0; i < 37414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37415(int& val) {
    func_in_37415(val);
    const int x = 37415;
    std::cout << 37415;
    for (int i = 0; i < 37415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37415(int& val) {
    const int x = 37415;
    std::cout << 37415;
    for (int i = 0; i < 37415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37416(int& val) {
    func_in_37416(val);
    const int x = 37416;
    std::cout << 37416;
    for (int i = 0; i < 37416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37416(int& val) {
    const int x = 37416;
    std::cout << 37416;
    for (int i = 0; i < 37416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37417(int& val) {
    func_in_37417(val);
    const int x = 37417;
    std::cout << 37417;
    for (int i = 0; i < 37417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37417(int& val) {
    const int x = 37417;
    std::cout << 37417;
    for (int i = 0; i < 37417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37418(int& val) {
    func_in_37418(val);
    const int x = 37418;
    std::cout << 37418;
    for (int i = 0; i < 37418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37418(int& val) {
    const int x = 37418;
    std::cout << 37418;
    for (int i = 0; i < 37418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37419(int& val) {
    func_in_37419(val);
    const int x = 37419;
    std::cout << 37419;
    for (int i = 0; i < 37419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37419(int& val) {
    const int x = 37419;
    std::cout << 37419;
    for (int i = 0; i < 37419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37420(int& val) {
    func_in_37420(val);
    const int x = 37420;
    std::cout << 37420;
    for (int i = 0; i < 37420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37420(int& val) {
    const int x = 37420;
    std::cout << 37420;
    for (int i = 0; i < 37420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37421(int& val) {
    func_in_37421(val);
    const int x = 37421;
    std::cout << 37421;
    for (int i = 0; i < 37421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37421(int& val) {
    const int x = 37421;
    std::cout << 37421;
    for (int i = 0; i < 37421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37422(int& val) {
    func_in_37422(val);
    const int x = 37422;
    std::cout << 37422;
    for (int i = 0; i < 37422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37422(int& val) {
    const int x = 37422;
    std::cout << 37422;
    for (int i = 0; i < 37422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37423(int& val) {
    func_in_37423(val);
    const int x = 37423;
    std::cout << 37423;
    for (int i = 0; i < 37423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37423(int& val) {
    const int x = 37423;
    std::cout << 37423;
    for (int i = 0; i < 37423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37424(int& val) {
    func_in_37424(val);
    const int x = 37424;
    std::cout << 37424;
    for (int i = 0; i < 37424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37424(int& val) {
    const int x = 37424;
    std::cout << 37424;
    for (int i = 0; i < 37424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37425(int& val) {
    func_in_37425(val);
    const int x = 37425;
    std::cout << 37425;
    for (int i = 0; i < 37425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37425(int& val) {
    const int x = 37425;
    std::cout << 37425;
    for (int i = 0; i < 37425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37426(int& val) {
    func_in_37426(val);
    const int x = 37426;
    std::cout << 37426;
    for (int i = 0; i < 37426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37426(int& val) {
    const int x = 37426;
    std::cout << 37426;
    for (int i = 0; i < 37426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37427(int& val) {
    func_in_37427(val);
    const int x = 37427;
    std::cout << 37427;
    for (int i = 0; i < 37427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37427(int& val) {
    const int x = 37427;
    std::cout << 37427;
    for (int i = 0; i < 37427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37428(int& val) {
    func_in_37428(val);
    const int x = 37428;
    std::cout << 37428;
    for (int i = 0; i < 37428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37428(int& val) {
    const int x = 37428;
    std::cout << 37428;
    for (int i = 0; i < 37428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37429(int& val) {
    func_in_37429(val);
    const int x = 37429;
    std::cout << 37429;
    for (int i = 0; i < 37429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37429(int& val) {
    const int x = 37429;
    std::cout << 37429;
    for (int i = 0; i < 37429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37430(int& val) {
    func_in_37430(val);
    const int x = 37430;
    std::cout << 37430;
    for (int i = 0; i < 37430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37430(int& val) {
    const int x = 37430;
    std::cout << 37430;
    for (int i = 0; i < 37430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37431(int& val) {
    func_in_37431(val);
    const int x = 37431;
    std::cout << 37431;
    for (int i = 0; i < 37431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37431(int& val) {
    const int x = 37431;
    std::cout << 37431;
    for (int i = 0; i < 37431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37432(int& val) {
    func_in_37432(val);
    const int x = 37432;
    std::cout << 37432;
    for (int i = 0; i < 37432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37432(int& val) {
    const int x = 37432;
    std::cout << 37432;
    for (int i = 0; i < 37432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37433(int& val) {
    func_in_37433(val);
    const int x = 37433;
    std::cout << 37433;
    for (int i = 0; i < 37433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37433(int& val) {
    const int x = 37433;
    std::cout << 37433;
    for (int i = 0; i < 37433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37434(int& val) {
    func_in_37434(val);
    const int x = 37434;
    std::cout << 37434;
    for (int i = 0; i < 37434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37434(int& val) {
    const int x = 37434;
    std::cout << 37434;
    for (int i = 0; i < 37434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37435(int& val) {
    func_in_37435(val);
    const int x = 37435;
    std::cout << 37435;
    for (int i = 0; i < 37435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37435(int& val) {
    const int x = 37435;
    std::cout << 37435;
    for (int i = 0; i < 37435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37436(int& val) {
    func_in_37436(val);
    const int x = 37436;
    std::cout << 37436;
    for (int i = 0; i < 37436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37436(int& val) {
    const int x = 37436;
    std::cout << 37436;
    for (int i = 0; i < 37436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37437(int& val) {
    func_in_37437(val);
    const int x = 37437;
    std::cout << 37437;
    for (int i = 0; i < 37437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37437(int& val) {
    const int x = 37437;
    std::cout << 37437;
    for (int i = 0; i < 37437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37438(int& val) {
    func_in_37438(val);
    const int x = 37438;
    std::cout << 37438;
    for (int i = 0; i < 37438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37438(int& val) {
    const int x = 37438;
    std::cout << 37438;
    for (int i = 0; i < 37438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37439(int& val) {
    func_in_37439(val);
    const int x = 37439;
    std::cout << 37439;
    for (int i = 0; i < 37439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37439(int& val) {
    const int x = 37439;
    std::cout << 37439;
    for (int i = 0; i < 37439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37440(int& val) {
    func_in_37440(val);
    const int x = 37440;
    std::cout << 37440;
    for (int i = 0; i < 37440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37440(int& val) {
    const int x = 37440;
    std::cout << 37440;
    for (int i = 0; i < 37440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37441(int& val) {
    func_in_37441(val);
    const int x = 37441;
    std::cout << 37441;
    for (int i = 0; i < 37441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37441(int& val) {
    const int x = 37441;
    std::cout << 37441;
    for (int i = 0; i < 37441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37442(int& val) {
    func_in_37442(val);
    const int x = 37442;
    std::cout << 37442;
    for (int i = 0; i < 37442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37442(int& val) {
    const int x = 37442;
    std::cout << 37442;
    for (int i = 0; i < 37442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37443(int& val) {
    func_in_37443(val);
    const int x = 37443;
    std::cout << 37443;
    for (int i = 0; i < 37443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37443(int& val) {
    const int x = 37443;
    std::cout << 37443;
    for (int i = 0; i < 37443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37444(int& val) {
    func_in_37444(val);
    const int x = 37444;
    std::cout << 37444;
    for (int i = 0; i < 37444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37444(int& val) {
    const int x = 37444;
    std::cout << 37444;
    for (int i = 0; i < 37444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37445(int& val) {
    func_in_37445(val);
    const int x = 37445;
    std::cout << 37445;
    for (int i = 0; i < 37445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37445(int& val) {
    const int x = 37445;
    std::cout << 37445;
    for (int i = 0; i < 37445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37446(int& val) {
    func_in_37446(val);
    const int x = 37446;
    std::cout << 37446;
    for (int i = 0; i < 37446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37446(int& val) {
    const int x = 37446;
    std::cout << 37446;
    for (int i = 0; i < 37446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37447(int& val) {
    func_in_37447(val);
    const int x = 37447;
    std::cout << 37447;
    for (int i = 0; i < 37447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37447(int& val) {
    const int x = 37447;
    std::cout << 37447;
    for (int i = 0; i < 37447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37448(int& val) {
    func_in_37448(val);
    const int x = 37448;
    std::cout << 37448;
    for (int i = 0; i < 37448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37448(int& val) {
    const int x = 37448;
    std::cout << 37448;
    for (int i = 0; i < 37448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37449(int& val) {
    func_in_37449(val);
    const int x = 37449;
    std::cout << 37449;
    for (int i = 0; i < 37449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37449(int& val) {
    const int x = 37449;
    std::cout << 37449;
    for (int i = 0; i < 37449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37450(int& val) {
    func_in_37450(val);
    const int x = 37450;
    std::cout << 37450;
    for (int i = 0; i < 37450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37450(int& val) {
    const int x = 37450;
    std::cout << 37450;
    for (int i = 0; i < 37450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37451(int& val) {
    func_in_37451(val);
    const int x = 37451;
    std::cout << 37451;
    for (int i = 0; i < 37451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37451(int& val) {
    const int x = 37451;
    std::cout << 37451;
    for (int i = 0; i < 37451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37452(int& val) {
    func_in_37452(val);
    const int x = 37452;
    std::cout << 37452;
    for (int i = 0; i < 37452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37452(int& val) {
    const int x = 37452;
    std::cout << 37452;
    for (int i = 0; i < 37452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37453(int& val) {
    func_in_37453(val);
    const int x = 37453;
    std::cout << 37453;
    for (int i = 0; i < 37453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37453(int& val) {
    const int x = 37453;
    std::cout << 37453;
    for (int i = 0; i < 37453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37454(int& val) {
    func_in_37454(val);
    const int x = 37454;
    std::cout << 37454;
    for (int i = 0; i < 37454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37454(int& val) {
    const int x = 37454;
    std::cout << 37454;
    for (int i = 0; i < 37454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37455(int& val) {
    func_in_37455(val);
    const int x = 37455;
    std::cout << 37455;
    for (int i = 0; i < 37455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37455(int& val) {
    const int x = 37455;
    std::cout << 37455;
    for (int i = 0; i < 37455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37456(int& val) {
    func_in_37456(val);
    const int x = 37456;
    std::cout << 37456;
    for (int i = 0; i < 37456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37456(int& val) {
    const int x = 37456;
    std::cout << 37456;
    for (int i = 0; i < 37456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37457(int& val) {
    func_in_37457(val);
    const int x = 37457;
    std::cout << 37457;
    for (int i = 0; i < 37457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37457(int& val) {
    const int x = 37457;
    std::cout << 37457;
    for (int i = 0; i < 37457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37458(int& val) {
    func_in_37458(val);
    const int x = 37458;
    std::cout << 37458;
    for (int i = 0; i < 37458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37458(int& val) {
    const int x = 37458;
    std::cout << 37458;
    for (int i = 0; i < 37458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37459(int& val) {
    func_in_37459(val);
    const int x = 37459;
    std::cout << 37459;
    for (int i = 0; i < 37459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37459(int& val) {
    const int x = 37459;
    std::cout << 37459;
    for (int i = 0; i < 37459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37460(int& val) {
    func_in_37460(val);
    const int x = 37460;
    std::cout << 37460;
    for (int i = 0; i < 37460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37460(int& val) {
    const int x = 37460;
    std::cout << 37460;
    for (int i = 0; i < 37460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37461(int& val) {
    func_in_37461(val);
    const int x = 37461;
    std::cout << 37461;
    for (int i = 0; i < 37461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37461(int& val) {
    const int x = 37461;
    std::cout << 37461;
    for (int i = 0; i < 37461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37462(int& val) {
    func_in_37462(val);
    const int x = 37462;
    std::cout << 37462;
    for (int i = 0; i < 37462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37462(int& val) {
    const int x = 37462;
    std::cout << 37462;
    for (int i = 0; i < 37462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37463(int& val) {
    func_in_37463(val);
    const int x = 37463;
    std::cout << 37463;
    for (int i = 0; i < 37463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37463(int& val) {
    const int x = 37463;
    std::cout << 37463;
    for (int i = 0; i < 37463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37464(int& val) {
    func_in_37464(val);
    const int x = 37464;
    std::cout << 37464;
    for (int i = 0; i < 37464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37464(int& val) {
    const int x = 37464;
    std::cout << 37464;
    for (int i = 0; i < 37464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37465(int& val) {
    func_in_37465(val);
    const int x = 37465;
    std::cout << 37465;
    for (int i = 0; i < 37465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37465(int& val) {
    const int x = 37465;
    std::cout << 37465;
    for (int i = 0; i < 37465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37466(int& val) {
    func_in_37466(val);
    const int x = 37466;
    std::cout << 37466;
    for (int i = 0; i < 37466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37466(int& val) {
    const int x = 37466;
    std::cout << 37466;
    for (int i = 0; i < 37466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37467(int& val) {
    func_in_37467(val);
    const int x = 37467;
    std::cout << 37467;
    for (int i = 0; i < 37467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37467(int& val) {
    const int x = 37467;
    std::cout << 37467;
    for (int i = 0; i < 37467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37468(int& val) {
    func_in_37468(val);
    const int x = 37468;
    std::cout << 37468;
    for (int i = 0; i < 37468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37468(int& val) {
    const int x = 37468;
    std::cout << 37468;
    for (int i = 0; i < 37468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37469(int& val) {
    func_in_37469(val);
    const int x = 37469;
    std::cout << 37469;
    for (int i = 0; i < 37469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37469(int& val) {
    const int x = 37469;
    std::cout << 37469;
    for (int i = 0; i < 37469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37470(int& val) {
    func_in_37470(val);
    const int x = 37470;
    std::cout << 37470;
    for (int i = 0; i < 37470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37470(int& val) {
    const int x = 37470;
    std::cout << 37470;
    for (int i = 0; i < 37470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37471(int& val) {
    func_in_37471(val);
    const int x = 37471;
    std::cout << 37471;
    for (int i = 0; i < 37471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37471(int& val) {
    const int x = 37471;
    std::cout << 37471;
    for (int i = 0; i < 37471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37472(int& val) {
    func_in_37472(val);
    const int x = 37472;
    std::cout << 37472;
    for (int i = 0; i < 37472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37472(int& val) {
    const int x = 37472;
    std::cout << 37472;
    for (int i = 0; i < 37472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37473(int& val) {
    func_in_37473(val);
    const int x = 37473;
    std::cout << 37473;
    for (int i = 0; i < 37473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37473(int& val) {
    const int x = 37473;
    std::cout << 37473;
    for (int i = 0; i < 37473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37474(int& val) {
    func_in_37474(val);
    const int x = 37474;
    std::cout << 37474;
    for (int i = 0; i < 37474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37474(int& val) {
    const int x = 37474;
    std::cout << 37474;
    for (int i = 0; i < 37474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37475(int& val) {
    func_in_37475(val);
    const int x = 37475;
    std::cout << 37475;
    for (int i = 0; i < 37475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37475(int& val) {
    const int x = 37475;
    std::cout << 37475;
    for (int i = 0; i < 37475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37476(int& val) {
    func_in_37476(val);
    const int x = 37476;
    std::cout << 37476;
    for (int i = 0; i < 37476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37476(int& val) {
    const int x = 37476;
    std::cout << 37476;
    for (int i = 0; i < 37476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37477(int& val) {
    func_in_37477(val);
    const int x = 37477;
    std::cout << 37477;
    for (int i = 0; i < 37477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37477(int& val) {
    const int x = 37477;
    std::cout << 37477;
    for (int i = 0; i < 37477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37478(int& val) {
    func_in_37478(val);
    const int x = 37478;
    std::cout << 37478;
    for (int i = 0; i < 37478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37478(int& val) {
    const int x = 37478;
    std::cout << 37478;
    for (int i = 0; i < 37478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37479(int& val) {
    func_in_37479(val);
    const int x = 37479;
    std::cout << 37479;
    for (int i = 0; i < 37479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37479(int& val) {
    const int x = 37479;
    std::cout << 37479;
    for (int i = 0; i < 37479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37480(int& val) {
    func_in_37480(val);
    const int x = 37480;
    std::cout << 37480;
    for (int i = 0; i < 37480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37480(int& val) {
    const int x = 37480;
    std::cout << 37480;
    for (int i = 0; i < 37480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37481(int& val) {
    func_in_37481(val);
    const int x = 37481;
    std::cout << 37481;
    for (int i = 0; i < 37481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37481(int& val) {
    const int x = 37481;
    std::cout << 37481;
    for (int i = 0; i < 37481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37482(int& val) {
    func_in_37482(val);
    const int x = 37482;
    std::cout << 37482;
    for (int i = 0; i < 37482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37482(int& val) {
    const int x = 37482;
    std::cout << 37482;
    for (int i = 0; i < 37482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37483(int& val) {
    func_in_37483(val);
    const int x = 37483;
    std::cout << 37483;
    for (int i = 0; i < 37483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37483(int& val) {
    const int x = 37483;
    std::cout << 37483;
    for (int i = 0; i < 37483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37484(int& val) {
    func_in_37484(val);
    const int x = 37484;
    std::cout << 37484;
    for (int i = 0; i < 37484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37484(int& val) {
    const int x = 37484;
    std::cout << 37484;
    for (int i = 0; i < 37484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37485(int& val) {
    func_in_37485(val);
    const int x = 37485;
    std::cout << 37485;
    for (int i = 0; i < 37485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37485(int& val) {
    const int x = 37485;
    std::cout << 37485;
    for (int i = 0; i < 37485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37486(int& val) {
    func_in_37486(val);
    const int x = 37486;
    std::cout << 37486;
    for (int i = 0; i < 37486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37486(int& val) {
    const int x = 37486;
    std::cout << 37486;
    for (int i = 0; i < 37486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37487(int& val) {
    func_in_37487(val);
    const int x = 37487;
    std::cout << 37487;
    for (int i = 0; i < 37487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37487(int& val) {
    const int x = 37487;
    std::cout << 37487;
    for (int i = 0; i < 37487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37488(int& val) {
    func_in_37488(val);
    const int x = 37488;
    std::cout << 37488;
    for (int i = 0; i < 37488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37488(int& val) {
    const int x = 37488;
    std::cout << 37488;
    for (int i = 0; i < 37488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37489(int& val) {
    func_in_37489(val);
    const int x = 37489;
    std::cout << 37489;
    for (int i = 0; i < 37489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37489(int& val) {
    const int x = 37489;
    std::cout << 37489;
    for (int i = 0; i < 37489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37490(int& val) {
    func_in_37490(val);
    const int x = 37490;
    std::cout << 37490;
    for (int i = 0; i < 37490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37490(int& val) {
    const int x = 37490;
    std::cout << 37490;
    for (int i = 0; i < 37490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37491(int& val) {
    func_in_37491(val);
    const int x = 37491;
    std::cout << 37491;
    for (int i = 0; i < 37491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37491(int& val) {
    const int x = 37491;
    std::cout << 37491;
    for (int i = 0; i < 37491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37492(int& val) {
    func_in_37492(val);
    const int x = 37492;
    std::cout << 37492;
    for (int i = 0; i < 37492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37492(int& val) {
    const int x = 37492;
    std::cout << 37492;
    for (int i = 0; i < 37492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37493(int& val) {
    func_in_37493(val);
    const int x = 37493;
    std::cout << 37493;
    for (int i = 0; i < 37493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37493(int& val) {
    const int x = 37493;
    std::cout << 37493;
    for (int i = 0; i < 37493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37494(int& val) {
    func_in_37494(val);
    const int x = 37494;
    std::cout << 37494;
    for (int i = 0; i < 37494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37494(int& val) {
    const int x = 37494;
    std::cout << 37494;
    for (int i = 0; i < 37494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37495(int& val) {
    func_in_37495(val);
    const int x = 37495;
    std::cout << 37495;
    for (int i = 0; i < 37495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37495(int& val) {
    const int x = 37495;
    std::cout << 37495;
    for (int i = 0; i < 37495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37496(int& val) {
    func_in_37496(val);
    const int x = 37496;
    std::cout << 37496;
    for (int i = 0; i < 37496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37496(int& val) {
    const int x = 37496;
    std::cout << 37496;
    for (int i = 0; i < 37496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37497(int& val) {
    func_in_37497(val);
    const int x = 37497;
    std::cout << 37497;
    for (int i = 0; i < 37497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37497(int& val) {
    const int x = 37497;
    std::cout << 37497;
    for (int i = 0; i < 37497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37498(int& val) {
    func_in_37498(val);
    const int x = 37498;
    std::cout << 37498;
    for (int i = 0; i < 37498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37498(int& val) {
    const int x = 37498;
    std::cout << 37498;
    for (int i = 0; i < 37498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37499(int& val) {
    func_in_37499(val);
    const int x = 37499;
    std::cout << 37499;
    for (int i = 0; i < 37499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37499(int& val) {
    const int x = 37499;
    std::cout << 37499;
    for (int i = 0; i < 37499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37500(int& val) {
    func_in_37500(val);
    const int x = 37500;
    std::cout << 37500;
    for (int i = 0; i < 37500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37500(int& val) {
    const int x = 37500;
    std::cout << 37500;
    for (int i = 0; i < 37500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37501(int& val) {
    func_in_37501(val);
    const int x = 37501;
    std::cout << 37501;
    for (int i = 0; i < 37501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37501(int& val) {
    const int x = 37501;
    std::cout << 37501;
    for (int i = 0; i < 37501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37502(int& val) {
    func_in_37502(val);
    const int x = 37502;
    std::cout << 37502;
    for (int i = 0; i < 37502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37502(int& val) {
    const int x = 37502;
    std::cout << 37502;
    for (int i = 0; i < 37502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37503(int& val) {
    func_in_37503(val);
    const int x = 37503;
    std::cout << 37503;
    for (int i = 0; i < 37503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37503(int& val) {
    const int x = 37503;
    std::cout << 37503;
    for (int i = 0; i < 37503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37504(int& val) {
    func_in_37504(val);
    const int x = 37504;
    std::cout << 37504;
    for (int i = 0; i < 37504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37504(int& val) {
    const int x = 37504;
    std::cout << 37504;
    for (int i = 0; i < 37504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37505(int& val) {
    func_in_37505(val);
    const int x = 37505;
    std::cout << 37505;
    for (int i = 0; i < 37505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37505(int& val) {
    const int x = 37505;
    std::cout << 37505;
    for (int i = 0; i < 37505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37506(int& val) {
    func_in_37506(val);
    const int x = 37506;
    std::cout << 37506;
    for (int i = 0; i < 37506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37506(int& val) {
    const int x = 37506;
    std::cout << 37506;
    for (int i = 0; i < 37506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37507(int& val) {
    func_in_37507(val);
    const int x = 37507;
    std::cout << 37507;
    for (int i = 0; i < 37507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37507(int& val) {
    const int x = 37507;
    std::cout << 37507;
    for (int i = 0; i < 37507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37508(int& val) {
    func_in_37508(val);
    const int x = 37508;
    std::cout << 37508;
    for (int i = 0; i < 37508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37508(int& val) {
    const int x = 37508;
    std::cout << 37508;
    for (int i = 0; i < 37508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37509(int& val) {
    func_in_37509(val);
    const int x = 37509;
    std::cout << 37509;
    for (int i = 0; i < 37509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37509(int& val) {
    const int x = 37509;
    std::cout << 37509;
    for (int i = 0; i < 37509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37510(int& val) {
    func_in_37510(val);
    const int x = 37510;
    std::cout << 37510;
    for (int i = 0; i < 37510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37510(int& val) {
    const int x = 37510;
    std::cout << 37510;
    for (int i = 0; i < 37510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37511(int& val) {
    func_in_37511(val);
    const int x = 37511;
    std::cout << 37511;
    for (int i = 0; i < 37511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37511(int& val) {
    const int x = 37511;
    std::cout << 37511;
    for (int i = 0; i < 37511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37512(int& val) {
    func_in_37512(val);
    const int x = 37512;
    std::cout << 37512;
    for (int i = 0; i < 37512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37512(int& val) {
    const int x = 37512;
    std::cout << 37512;
    for (int i = 0; i < 37512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37513(int& val) {
    func_in_37513(val);
    const int x = 37513;
    std::cout << 37513;
    for (int i = 0; i < 37513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37513(int& val) {
    const int x = 37513;
    std::cout << 37513;
    for (int i = 0; i < 37513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37514(int& val) {
    func_in_37514(val);
    const int x = 37514;
    std::cout << 37514;
    for (int i = 0; i < 37514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37514(int& val) {
    const int x = 37514;
    std::cout << 37514;
    for (int i = 0; i < 37514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37515(int& val) {
    func_in_37515(val);
    const int x = 37515;
    std::cout << 37515;
    for (int i = 0; i < 37515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37515(int& val) {
    const int x = 37515;
    std::cout << 37515;
    for (int i = 0; i < 37515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37516(int& val) {
    func_in_37516(val);
    const int x = 37516;
    std::cout << 37516;
    for (int i = 0; i < 37516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37516(int& val) {
    const int x = 37516;
    std::cout << 37516;
    for (int i = 0; i < 37516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37517(int& val) {
    func_in_37517(val);
    const int x = 37517;
    std::cout << 37517;
    for (int i = 0; i < 37517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37517(int& val) {
    const int x = 37517;
    std::cout << 37517;
    for (int i = 0; i < 37517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37518(int& val) {
    func_in_37518(val);
    const int x = 37518;
    std::cout << 37518;
    for (int i = 0; i < 37518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37518(int& val) {
    const int x = 37518;
    std::cout << 37518;
    for (int i = 0; i < 37518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37519(int& val) {
    func_in_37519(val);
    const int x = 37519;
    std::cout << 37519;
    for (int i = 0; i < 37519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37519(int& val) {
    const int x = 37519;
    std::cout << 37519;
    for (int i = 0; i < 37519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37520(int& val) {
    func_in_37520(val);
    const int x = 37520;
    std::cout << 37520;
    for (int i = 0; i < 37520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37520(int& val) {
    const int x = 37520;
    std::cout << 37520;
    for (int i = 0; i < 37520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37521(int& val) {
    func_in_37521(val);
    const int x = 37521;
    std::cout << 37521;
    for (int i = 0; i < 37521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37521(int& val) {
    const int x = 37521;
    std::cout << 37521;
    for (int i = 0; i < 37521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37522(int& val) {
    func_in_37522(val);
    const int x = 37522;
    std::cout << 37522;
    for (int i = 0; i < 37522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37522(int& val) {
    const int x = 37522;
    std::cout << 37522;
    for (int i = 0; i < 37522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37523(int& val) {
    func_in_37523(val);
    const int x = 37523;
    std::cout << 37523;
    for (int i = 0; i < 37523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37523(int& val) {
    const int x = 37523;
    std::cout << 37523;
    for (int i = 0; i < 37523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37524(int& val) {
    func_in_37524(val);
    const int x = 37524;
    std::cout << 37524;
    for (int i = 0; i < 37524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37524(int& val) {
    const int x = 37524;
    std::cout << 37524;
    for (int i = 0; i < 37524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37525(int& val) {
    func_in_37525(val);
    const int x = 37525;
    std::cout << 37525;
    for (int i = 0; i < 37525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37525(int& val) {
    const int x = 37525;
    std::cout << 37525;
    for (int i = 0; i < 37525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37526(int& val) {
    func_in_37526(val);
    const int x = 37526;
    std::cout << 37526;
    for (int i = 0; i < 37526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37526(int& val) {
    const int x = 37526;
    std::cout << 37526;
    for (int i = 0; i < 37526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37527(int& val) {
    func_in_37527(val);
    const int x = 37527;
    std::cout << 37527;
    for (int i = 0; i < 37527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37527(int& val) {
    const int x = 37527;
    std::cout << 37527;
    for (int i = 0; i < 37527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37528(int& val) {
    func_in_37528(val);
    const int x = 37528;
    std::cout << 37528;
    for (int i = 0; i < 37528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37528(int& val) {
    const int x = 37528;
    std::cout << 37528;
    for (int i = 0; i < 37528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37529(int& val) {
    func_in_37529(val);
    const int x = 37529;
    std::cout << 37529;
    for (int i = 0; i < 37529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37529(int& val) {
    const int x = 37529;
    std::cout << 37529;
    for (int i = 0; i < 37529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37530(int& val) {
    func_in_37530(val);
    const int x = 37530;
    std::cout << 37530;
    for (int i = 0; i < 37530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37530(int& val) {
    const int x = 37530;
    std::cout << 37530;
    for (int i = 0; i < 37530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37531(int& val) {
    func_in_37531(val);
    const int x = 37531;
    std::cout << 37531;
    for (int i = 0; i < 37531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37531(int& val) {
    const int x = 37531;
    std::cout << 37531;
    for (int i = 0; i < 37531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37532(int& val) {
    func_in_37532(val);
    const int x = 37532;
    std::cout << 37532;
    for (int i = 0; i < 37532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37532(int& val) {
    const int x = 37532;
    std::cout << 37532;
    for (int i = 0; i < 37532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37533(int& val) {
    func_in_37533(val);
    const int x = 37533;
    std::cout << 37533;
    for (int i = 0; i < 37533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37533(int& val) {
    const int x = 37533;
    std::cout << 37533;
    for (int i = 0; i < 37533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37534(int& val) {
    func_in_37534(val);
    const int x = 37534;
    std::cout << 37534;
    for (int i = 0; i < 37534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37534(int& val) {
    const int x = 37534;
    std::cout << 37534;
    for (int i = 0; i < 37534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37535(int& val) {
    func_in_37535(val);
    const int x = 37535;
    std::cout << 37535;
    for (int i = 0; i < 37535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37535(int& val) {
    const int x = 37535;
    std::cout << 37535;
    for (int i = 0; i < 37535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37536(int& val) {
    func_in_37536(val);
    const int x = 37536;
    std::cout << 37536;
    for (int i = 0; i < 37536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37536(int& val) {
    const int x = 37536;
    std::cout << 37536;
    for (int i = 0; i < 37536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37537(int& val) {
    func_in_37537(val);
    const int x = 37537;
    std::cout << 37537;
    for (int i = 0; i < 37537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37537(int& val) {
    const int x = 37537;
    std::cout << 37537;
    for (int i = 0; i < 37537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37538(int& val) {
    func_in_37538(val);
    const int x = 37538;
    std::cout << 37538;
    for (int i = 0; i < 37538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37538(int& val) {
    const int x = 37538;
    std::cout << 37538;
    for (int i = 0; i < 37538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37539(int& val) {
    func_in_37539(val);
    const int x = 37539;
    std::cout << 37539;
    for (int i = 0; i < 37539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37539(int& val) {
    const int x = 37539;
    std::cout << 37539;
    for (int i = 0; i < 37539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37540(int& val) {
    func_in_37540(val);
    const int x = 37540;
    std::cout << 37540;
    for (int i = 0; i < 37540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37540(int& val) {
    const int x = 37540;
    std::cout << 37540;
    for (int i = 0; i < 37540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37541(int& val) {
    func_in_37541(val);
    const int x = 37541;
    std::cout << 37541;
    for (int i = 0; i < 37541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37541(int& val) {
    const int x = 37541;
    std::cout << 37541;
    for (int i = 0; i < 37541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37542(int& val) {
    func_in_37542(val);
    const int x = 37542;
    std::cout << 37542;
    for (int i = 0; i < 37542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37542(int& val) {
    const int x = 37542;
    std::cout << 37542;
    for (int i = 0; i < 37542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37543(int& val) {
    func_in_37543(val);
    const int x = 37543;
    std::cout << 37543;
    for (int i = 0; i < 37543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37543(int& val) {
    const int x = 37543;
    std::cout << 37543;
    for (int i = 0; i < 37543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37544(int& val) {
    func_in_37544(val);
    const int x = 37544;
    std::cout << 37544;
    for (int i = 0; i < 37544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37544(int& val) {
    const int x = 37544;
    std::cout << 37544;
    for (int i = 0; i < 37544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37545(int& val) {
    func_in_37545(val);
    const int x = 37545;
    std::cout << 37545;
    for (int i = 0; i < 37545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37545(int& val) {
    const int x = 37545;
    std::cout << 37545;
    for (int i = 0; i < 37545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37546(int& val) {
    func_in_37546(val);
    const int x = 37546;
    std::cout << 37546;
    for (int i = 0; i < 37546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37546(int& val) {
    const int x = 37546;
    std::cout << 37546;
    for (int i = 0; i < 37546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37547(int& val) {
    func_in_37547(val);
    const int x = 37547;
    std::cout << 37547;
    for (int i = 0; i < 37547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37547(int& val) {
    const int x = 37547;
    std::cout << 37547;
    for (int i = 0; i < 37547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37548(int& val) {
    func_in_37548(val);
    const int x = 37548;
    std::cout << 37548;
    for (int i = 0; i < 37548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37548(int& val) {
    const int x = 37548;
    std::cout << 37548;
    for (int i = 0; i < 37548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37549(int& val) {
    func_in_37549(val);
    const int x = 37549;
    std::cout << 37549;
    for (int i = 0; i < 37549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37549(int& val) {
    const int x = 37549;
    std::cout << 37549;
    for (int i = 0; i < 37549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37550(int& val) {
    func_in_37550(val);
    const int x = 37550;
    std::cout << 37550;
    for (int i = 0; i < 37550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37550(int& val) {
    const int x = 37550;
    std::cout << 37550;
    for (int i = 0; i < 37550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37551(int& val) {
    func_in_37551(val);
    const int x = 37551;
    std::cout << 37551;
    for (int i = 0; i < 37551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37551(int& val) {
    const int x = 37551;
    std::cout << 37551;
    for (int i = 0; i < 37551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37552(int& val) {
    func_in_37552(val);
    const int x = 37552;
    std::cout << 37552;
    for (int i = 0; i < 37552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37552(int& val) {
    const int x = 37552;
    std::cout << 37552;
    for (int i = 0; i < 37552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37553(int& val) {
    func_in_37553(val);
    const int x = 37553;
    std::cout << 37553;
    for (int i = 0; i < 37553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37553(int& val) {
    const int x = 37553;
    std::cout << 37553;
    for (int i = 0; i < 37553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37554(int& val) {
    func_in_37554(val);
    const int x = 37554;
    std::cout << 37554;
    for (int i = 0; i < 37554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37554(int& val) {
    const int x = 37554;
    std::cout << 37554;
    for (int i = 0; i < 37554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37555(int& val) {
    func_in_37555(val);
    const int x = 37555;
    std::cout << 37555;
    for (int i = 0; i < 37555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37555(int& val) {
    const int x = 37555;
    std::cout << 37555;
    for (int i = 0; i < 37555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37556(int& val) {
    func_in_37556(val);
    const int x = 37556;
    std::cout << 37556;
    for (int i = 0; i < 37556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37556(int& val) {
    const int x = 37556;
    std::cout << 37556;
    for (int i = 0; i < 37556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37557(int& val) {
    func_in_37557(val);
    const int x = 37557;
    std::cout << 37557;
    for (int i = 0; i < 37557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37557(int& val) {
    const int x = 37557;
    std::cout << 37557;
    for (int i = 0; i < 37557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37558(int& val) {
    func_in_37558(val);
    const int x = 37558;
    std::cout << 37558;
    for (int i = 0; i < 37558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37558(int& val) {
    const int x = 37558;
    std::cout << 37558;
    for (int i = 0; i < 37558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37559(int& val) {
    func_in_37559(val);
    const int x = 37559;
    std::cout << 37559;
    for (int i = 0; i < 37559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37559(int& val) {
    const int x = 37559;
    std::cout << 37559;
    for (int i = 0; i < 37559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37560(int& val) {
    func_in_37560(val);
    const int x = 37560;
    std::cout << 37560;
    for (int i = 0; i < 37560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37560(int& val) {
    const int x = 37560;
    std::cout << 37560;
    for (int i = 0; i < 37560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37561(int& val) {
    func_in_37561(val);
    const int x = 37561;
    std::cout << 37561;
    for (int i = 0; i < 37561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37561(int& val) {
    const int x = 37561;
    std::cout << 37561;
    for (int i = 0; i < 37561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37562(int& val) {
    func_in_37562(val);
    const int x = 37562;
    std::cout << 37562;
    for (int i = 0; i < 37562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37562(int& val) {
    const int x = 37562;
    std::cout << 37562;
    for (int i = 0; i < 37562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37563(int& val) {
    func_in_37563(val);
    const int x = 37563;
    std::cout << 37563;
    for (int i = 0; i < 37563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37563(int& val) {
    const int x = 37563;
    std::cout << 37563;
    for (int i = 0; i < 37563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37564(int& val) {
    func_in_37564(val);
    const int x = 37564;
    std::cout << 37564;
    for (int i = 0; i < 37564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37564(int& val) {
    const int x = 37564;
    std::cout << 37564;
    for (int i = 0; i < 37564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37565(int& val) {
    func_in_37565(val);
    const int x = 37565;
    std::cout << 37565;
    for (int i = 0; i < 37565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37565(int& val) {
    const int x = 37565;
    std::cout << 37565;
    for (int i = 0; i < 37565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37566(int& val) {
    func_in_37566(val);
    const int x = 37566;
    std::cout << 37566;
    for (int i = 0; i < 37566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37566(int& val) {
    const int x = 37566;
    std::cout << 37566;
    for (int i = 0; i < 37566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37567(int& val) {
    func_in_37567(val);
    const int x = 37567;
    std::cout << 37567;
    for (int i = 0; i < 37567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37567(int& val) {
    const int x = 37567;
    std::cout << 37567;
    for (int i = 0; i < 37567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37568(int& val) {
    func_in_37568(val);
    const int x = 37568;
    std::cout << 37568;
    for (int i = 0; i < 37568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37568(int& val) {
    const int x = 37568;
    std::cout << 37568;
    for (int i = 0; i < 37568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37569(int& val) {
    func_in_37569(val);
    const int x = 37569;
    std::cout << 37569;
    for (int i = 0; i < 37569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37569(int& val) {
    const int x = 37569;
    std::cout << 37569;
    for (int i = 0; i < 37569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37570(int& val) {
    func_in_37570(val);
    const int x = 37570;
    std::cout << 37570;
    for (int i = 0; i < 37570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37570(int& val) {
    const int x = 37570;
    std::cout << 37570;
    for (int i = 0; i < 37570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37571(int& val) {
    func_in_37571(val);
    const int x = 37571;
    std::cout << 37571;
    for (int i = 0; i < 37571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37571(int& val) {
    const int x = 37571;
    std::cout << 37571;
    for (int i = 0; i < 37571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37572(int& val) {
    func_in_37572(val);
    const int x = 37572;
    std::cout << 37572;
    for (int i = 0; i < 37572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37572(int& val) {
    const int x = 37572;
    std::cout << 37572;
    for (int i = 0; i < 37572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37573(int& val) {
    func_in_37573(val);
    const int x = 37573;
    std::cout << 37573;
    for (int i = 0; i < 37573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37573(int& val) {
    const int x = 37573;
    std::cout << 37573;
    for (int i = 0; i < 37573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37574(int& val) {
    func_in_37574(val);
    const int x = 37574;
    std::cout << 37574;
    for (int i = 0; i < 37574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37574(int& val) {
    const int x = 37574;
    std::cout << 37574;
    for (int i = 0; i < 37574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37575(int& val) {
    func_in_37575(val);
    const int x = 37575;
    std::cout << 37575;
    for (int i = 0; i < 37575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37575(int& val) {
    const int x = 37575;
    std::cout << 37575;
    for (int i = 0; i < 37575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37576(int& val) {
    func_in_37576(val);
    const int x = 37576;
    std::cout << 37576;
    for (int i = 0; i < 37576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37576(int& val) {
    const int x = 37576;
    std::cout << 37576;
    for (int i = 0; i < 37576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37577(int& val) {
    func_in_37577(val);
    const int x = 37577;
    std::cout << 37577;
    for (int i = 0; i < 37577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37577(int& val) {
    const int x = 37577;
    std::cout << 37577;
    for (int i = 0; i < 37577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37578(int& val) {
    func_in_37578(val);
    const int x = 37578;
    std::cout << 37578;
    for (int i = 0; i < 37578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37578(int& val) {
    const int x = 37578;
    std::cout << 37578;
    for (int i = 0; i < 37578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37579(int& val) {
    func_in_37579(val);
    const int x = 37579;
    std::cout << 37579;
    for (int i = 0; i < 37579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37579(int& val) {
    const int x = 37579;
    std::cout << 37579;
    for (int i = 0; i < 37579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37580(int& val) {
    func_in_37580(val);
    const int x = 37580;
    std::cout << 37580;
    for (int i = 0; i < 37580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37580(int& val) {
    const int x = 37580;
    std::cout << 37580;
    for (int i = 0; i < 37580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37581(int& val) {
    func_in_37581(val);
    const int x = 37581;
    std::cout << 37581;
    for (int i = 0; i < 37581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37581(int& val) {
    const int x = 37581;
    std::cout << 37581;
    for (int i = 0; i < 37581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37582(int& val) {
    func_in_37582(val);
    const int x = 37582;
    std::cout << 37582;
    for (int i = 0; i < 37582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37582(int& val) {
    const int x = 37582;
    std::cout << 37582;
    for (int i = 0; i < 37582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37583(int& val) {
    func_in_37583(val);
    const int x = 37583;
    std::cout << 37583;
    for (int i = 0; i < 37583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37583(int& val) {
    const int x = 37583;
    std::cout << 37583;
    for (int i = 0; i < 37583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37584(int& val) {
    func_in_37584(val);
    const int x = 37584;
    std::cout << 37584;
    for (int i = 0; i < 37584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37584(int& val) {
    const int x = 37584;
    std::cout << 37584;
    for (int i = 0; i < 37584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37585(int& val) {
    func_in_37585(val);
    const int x = 37585;
    std::cout << 37585;
    for (int i = 0; i < 37585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37585(int& val) {
    const int x = 37585;
    std::cout << 37585;
    for (int i = 0; i < 37585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37586(int& val) {
    func_in_37586(val);
    const int x = 37586;
    std::cout << 37586;
    for (int i = 0; i < 37586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37586(int& val) {
    const int x = 37586;
    std::cout << 37586;
    for (int i = 0; i < 37586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37587(int& val) {
    func_in_37587(val);
    const int x = 37587;
    std::cout << 37587;
    for (int i = 0; i < 37587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37587(int& val) {
    const int x = 37587;
    std::cout << 37587;
    for (int i = 0; i < 37587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37588(int& val) {
    func_in_37588(val);
    const int x = 37588;
    std::cout << 37588;
    for (int i = 0; i < 37588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37588(int& val) {
    const int x = 37588;
    std::cout << 37588;
    for (int i = 0; i < 37588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37589(int& val) {
    func_in_37589(val);
    const int x = 37589;
    std::cout << 37589;
    for (int i = 0; i < 37589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37589(int& val) {
    const int x = 37589;
    std::cout << 37589;
    for (int i = 0; i < 37589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37590(int& val) {
    func_in_37590(val);
    const int x = 37590;
    std::cout << 37590;
    for (int i = 0; i < 37590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37590(int& val) {
    const int x = 37590;
    std::cout << 37590;
    for (int i = 0; i < 37590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37591(int& val) {
    func_in_37591(val);
    const int x = 37591;
    std::cout << 37591;
    for (int i = 0; i < 37591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37591(int& val) {
    const int x = 37591;
    std::cout << 37591;
    for (int i = 0; i < 37591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37592(int& val) {
    func_in_37592(val);
    const int x = 37592;
    std::cout << 37592;
    for (int i = 0; i < 37592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37592(int& val) {
    const int x = 37592;
    std::cout << 37592;
    for (int i = 0; i < 37592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37593(int& val) {
    func_in_37593(val);
    const int x = 37593;
    std::cout << 37593;
    for (int i = 0; i < 37593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37593(int& val) {
    const int x = 37593;
    std::cout << 37593;
    for (int i = 0; i < 37593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37594(int& val) {
    func_in_37594(val);
    const int x = 37594;
    std::cout << 37594;
    for (int i = 0; i < 37594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37594(int& val) {
    const int x = 37594;
    std::cout << 37594;
    for (int i = 0; i < 37594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37595(int& val) {
    func_in_37595(val);
    const int x = 37595;
    std::cout << 37595;
    for (int i = 0; i < 37595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37595(int& val) {
    const int x = 37595;
    std::cout << 37595;
    for (int i = 0; i < 37595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37596(int& val) {
    func_in_37596(val);
    const int x = 37596;
    std::cout << 37596;
    for (int i = 0; i < 37596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37596(int& val) {
    const int x = 37596;
    std::cout << 37596;
    for (int i = 0; i < 37596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37597(int& val) {
    func_in_37597(val);
    const int x = 37597;
    std::cout << 37597;
    for (int i = 0; i < 37597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37597(int& val) {
    const int x = 37597;
    std::cout << 37597;
    for (int i = 0; i < 37597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37598(int& val) {
    func_in_37598(val);
    const int x = 37598;
    std::cout << 37598;
    for (int i = 0; i < 37598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37598(int& val) {
    const int x = 37598;
    std::cout << 37598;
    for (int i = 0; i < 37598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37599(int& val) {
    func_in_37599(val);
    const int x = 37599;
    std::cout << 37599;
    for (int i = 0; i < 37599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37599(int& val) {
    const int x = 37599;
    std::cout << 37599;
    for (int i = 0; i < 37599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37600(int& val) {
    func_in_37600(val);
    const int x = 37600;
    std::cout << 37600;
    for (int i = 0; i < 37600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37600(int& val) {
    const int x = 37600;
    std::cout << 37600;
    for (int i = 0; i < 37600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37601(int& val) {
    func_in_37601(val);
    const int x = 37601;
    std::cout << 37601;
    for (int i = 0; i < 37601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37601(int& val) {
    const int x = 37601;
    std::cout << 37601;
    for (int i = 0; i < 37601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37602(int& val) {
    func_in_37602(val);
    const int x = 37602;
    std::cout << 37602;
    for (int i = 0; i < 37602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37602(int& val) {
    const int x = 37602;
    std::cout << 37602;
    for (int i = 0; i < 37602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37603(int& val) {
    func_in_37603(val);
    const int x = 37603;
    std::cout << 37603;
    for (int i = 0; i < 37603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37603(int& val) {
    const int x = 37603;
    std::cout << 37603;
    for (int i = 0; i < 37603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37604(int& val) {
    func_in_37604(val);
    const int x = 37604;
    std::cout << 37604;
    for (int i = 0; i < 37604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37604(int& val) {
    const int x = 37604;
    std::cout << 37604;
    for (int i = 0; i < 37604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37605(int& val) {
    func_in_37605(val);
    const int x = 37605;
    std::cout << 37605;
    for (int i = 0; i < 37605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37605(int& val) {
    const int x = 37605;
    std::cout << 37605;
    for (int i = 0; i < 37605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37606(int& val) {
    func_in_37606(val);
    const int x = 37606;
    std::cout << 37606;
    for (int i = 0; i < 37606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37606(int& val) {
    const int x = 37606;
    std::cout << 37606;
    for (int i = 0; i < 37606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37607(int& val) {
    func_in_37607(val);
    const int x = 37607;
    std::cout << 37607;
    for (int i = 0; i < 37607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37607(int& val) {
    const int x = 37607;
    std::cout << 37607;
    for (int i = 0; i < 37607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37608(int& val) {
    func_in_37608(val);
    const int x = 37608;
    std::cout << 37608;
    for (int i = 0; i < 37608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37608(int& val) {
    const int x = 37608;
    std::cout << 37608;
    for (int i = 0; i < 37608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37609(int& val) {
    func_in_37609(val);
    const int x = 37609;
    std::cout << 37609;
    for (int i = 0; i < 37609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37609(int& val) {
    const int x = 37609;
    std::cout << 37609;
    for (int i = 0; i < 37609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37610(int& val) {
    func_in_37610(val);
    const int x = 37610;
    std::cout << 37610;
    for (int i = 0; i < 37610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37610(int& val) {
    const int x = 37610;
    std::cout << 37610;
    for (int i = 0; i < 37610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37611(int& val) {
    func_in_37611(val);
    const int x = 37611;
    std::cout << 37611;
    for (int i = 0; i < 37611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37611(int& val) {
    const int x = 37611;
    std::cout << 37611;
    for (int i = 0; i < 37611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37612(int& val) {
    func_in_37612(val);
    const int x = 37612;
    std::cout << 37612;
    for (int i = 0; i < 37612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37612(int& val) {
    const int x = 37612;
    std::cout << 37612;
    for (int i = 0; i < 37612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37613(int& val) {
    func_in_37613(val);
    const int x = 37613;
    std::cout << 37613;
    for (int i = 0; i < 37613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37613(int& val) {
    const int x = 37613;
    std::cout << 37613;
    for (int i = 0; i < 37613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37614(int& val) {
    func_in_37614(val);
    const int x = 37614;
    std::cout << 37614;
    for (int i = 0; i < 37614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37614(int& val) {
    const int x = 37614;
    std::cout << 37614;
    for (int i = 0; i < 37614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37615(int& val) {
    func_in_37615(val);
    const int x = 37615;
    std::cout << 37615;
    for (int i = 0; i < 37615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37615(int& val) {
    const int x = 37615;
    std::cout << 37615;
    for (int i = 0; i < 37615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37616(int& val) {
    func_in_37616(val);
    const int x = 37616;
    std::cout << 37616;
    for (int i = 0; i < 37616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37616(int& val) {
    const int x = 37616;
    std::cout << 37616;
    for (int i = 0; i < 37616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37617(int& val) {
    func_in_37617(val);
    const int x = 37617;
    std::cout << 37617;
    for (int i = 0; i < 37617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37617(int& val) {
    const int x = 37617;
    std::cout << 37617;
    for (int i = 0; i < 37617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37618(int& val) {
    func_in_37618(val);
    const int x = 37618;
    std::cout << 37618;
    for (int i = 0; i < 37618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37618(int& val) {
    const int x = 37618;
    std::cout << 37618;
    for (int i = 0; i < 37618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37619(int& val) {
    func_in_37619(val);
    const int x = 37619;
    std::cout << 37619;
    for (int i = 0; i < 37619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37619(int& val) {
    const int x = 37619;
    std::cout << 37619;
    for (int i = 0; i < 37619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37620(int& val) {
    func_in_37620(val);
    const int x = 37620;
    std::cout << 37620;
    for (int i = 0; i < 37620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37620(int& val) {
    const int x = 37620;
    std::cout << 37620;
    for (int i = 0; i < 37620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37621(int& val) {
    func_in_37621(val);
    const int x = 37621;
    std::cout << 37621;
    for (int i = 0; i < 37621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37621(int& val) {
    const int x = 37621;
    std::cout << 37621;
    for (int i = 0; i < 37621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37622(int& val) {
    func_in_37622(val);
    const int x = 37622;
    std::cout << 37622;
    for (int i = 0; i < 37622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37622(int& val) {
    const int x = 37622;
    std::cout << 37622;
    for (int i = 0; i < 37622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37623(int& val) {
    func_in_37623(val);
    const int x = 37623;
    std::cout << 37623;
    for (int i = 0; i < 37623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37623(int& val) {
    const int x = 37623;
    std::cout << 37623;
    for (int i = 0; i < 37623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37624(int& val) {
    func_in_37624(val);
    const int x = 37624;
    std::cout << 37624;
    for (int i = 0; i < 37624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37624(int& val) {
    const int x = 37624;
    std::cout << 37624;
    for (int i = 0; i < 37624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37625(int& val) {
    func_in_37625(val);
    const int x = 37625;
    std::cout << 37625;
    for (int i = 0; i < 37625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37625(int& val) {
    const int x = 37625;
    std::cout << 37625;
    for (int i = 0; i < 37625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37626(int& val) {
    func_in_37626(val);
    const int x = 37626;
    std::cout << 37626;
    for (int i = 0; i < 37626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37626(int& val) {
    const int x = 37626;
    std::cout << 37626;
    for (int i = 0; i < 37626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37627(int& val) {
    func_in_37627(val);
    const int x = 37627;
    std::cout << 37627;
    for (int i = 0; i < 37627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37627(int& val) {
    const int x = 37627;
    std::cout << 37627;
    for (int i = 0; i < 37627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37628(int& val) {
    func_in_37628(val);
    const int x = 37628;
    std::cout << 37628;
    for (int i = 0; i < 37628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37628(int& val) {
    const int x = 37628;
    std::cout << 37628;
    for (int i = 0; i < 37628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37629(int& val) {
    func_in_37629(val);
    const int x = 37629;
    std::cout << 37629;
    for (int i = 0; i < 37629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37629(int& val) {
    const int x = 37629;
    std::cout << 37629;
    for (int i = 0; i < 37629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37630(int& val) {
    func_in_37630(val);
    const int x = 37630;
    std::cout << 37630;
    for (int i = 0; i < 37630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37630(int& val) {
    const int x = 37630;
    std::cout << 37630;
    for (int i = 0; i < 37630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37631(int& val) {
    func_in_37631(val);
    const int x = 37631;
    std::cout << 37631;
    for (int i = 0; i < 37631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37631(int& val) {
    const int x = 37631;
    std::cout << 37631;
    for (int i = 0; i < 37631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37632(int& val) {
    func_in_37632(val);
    const int x = 37632;
    std::cout << 37632;
    for (int i = 0; i < 37632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37632(int& val) {
    const int x = 37632;
    std::cout << 37632;
    for (int i = 0; i < 37632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37633(int& val) {
    func_in_37633(val);
    const int x = 37633;
    std::cout << 37633;
    for (int i = 0; i < 37633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37633(int& val) {
    const int x = 37633;
    std::cout << 37633;
    for (int i = 0; i < 37633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37634(int& val) {
    func_in_37634(val);
    const int x = 37634;
    std::cout << 37634;
    for (int i = 0; i < 37634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37634(int& val) {
    const int x = 37634;
    std::cout << 37634;
    for (int i = 0; i < 37634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37635(int& val) {
    func_in_37635(val);
    const int x = 37635;
    std::cout << 37635;
    for (int i = 0; i < 37635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37635(int& val) {
    const int x = 37635;
    std::cout << 37635;
    for (int i = 0; i < 37635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37636(int& val) {
    func_in_37636(val);
    const int x = 37636;
    std::cout << 37636;
    for (int i = 0; i < 37636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37636(int& val) {
    const int x = 37636;
    std::cout << 37636;
    for (int i = 0; i < 37636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37637(int& val) {
    func_in_37637(val);
    const int x = 37637;
    std::cout << 37637;
    for (int i = 0; i < 37637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37637(int& val) {
    const int x = 37637;
    std::cout << 37637;
    for (int i = 0; i < 37637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37638(int& val) {
    func_in_37638(val);
    const int x = 37638;
    std::cout << 37638;
    for (int i = 0; i < 37638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37638(int& val) {
    const int x = 37638;
    std::cout << 37638;
    for (int i = 0; i < 37638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37639(int& val) {
    func_in_37639(val);
    const int x = 37639;
    std::cout << 37639;
    for (int i = 0; i < 37639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37639(int& val) {
    const int x = 37639;
    std::cout << 37639;
    for (int i = 0; i < 37639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37640(int& val) {
    func_in_37640(val);
    const int x = 37640;
    std::cout << 37640;
    for (int i = 0; i < 37640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37640(int& val) {
    const int x = 37640;
    std::cout << 37640;
    for (int i = 0; i < 37640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37641(int& val) {
    func_in_37641(val);
    const int x = 37641;
    std::cout << 37641;
    for (int i = 0; i < 37641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37641(int& val) {
    const int x = 37641;
    std::cout << 37641;
    for (int i = 0; i < 37641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37642(int& val) {
    func_in_37642(val);
    const int x = 37642;
    std::cout << 37642;
    for (int i = 0; i < 37642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37642(int& val) {
    const int x = 37642;
    std::cout << 37642;
    for (int i = 0; i < 37642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37643(int& val) {
    func_in_37643(val);
    const int x = 37643;
    std::cout << 37643;
    for (int i = 0; i < 37643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37643(int& val) {
    const int x = 37643;
    std::cout << 37643;
    for (int i = 0; i < 37643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37644(int& val) {
    func_in_37644(val);
    const int x = 37644;
    std::cout << 37644;
    for (int i = 0; i < 37644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37644(int& val) {
    const int x = 37644;
    std::cout << 37644;
    for (int i = 0; i < 37644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37645(int& val) {
    func_in_37645(val);
    const int x = 37645;
    std::cout << 37645;
    for (int i = 0; i < 37645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37645(int& val) {
    const int x = 37645;
    std::cout << 37645;
    for (int i = 0; i < 37645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37646(int& val) {
    func_in_37646(val);
    const int x = 37646;
    std::cout << 37646;
    for (int i = 0; i < 37646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37646(int& val) {
    const int x = 37646;
    std::cout << 37646;
    for (int i = 0; i < 37646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37647(int& val) {
    func_in_37647(val);
    const int x = 37647;
    std::cout << 37647;
    for (int i = 0; i < 37647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37647(int& val) {
    const int x = 37647;
    std::cout << 37647;
    for (int i = 0; i < 37647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37648(int& val) {
    func_in_37648(val);
    const int x = 37648;
    std::cout << 37648;
    for (int i = 0; i < 37648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37648(int& val) {
    const int x = 37648;
    std::cout << 37648;
    for (int i = 0; i < 37648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37649(int& val) {
    func_in_37649(val);
    const int x = 37649;
    std::cout << 37649;
    for (int i = 0; i < 37649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37649(int& val) {
    const int x = 37649;
    std::cout << 37649;
    for (int i = 0; i < 37649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37650(int& val) {
    func_in_37650(val);
    const int x = 37650;
    std::cout << 37650;
    for (int i = 0; i < 37650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37650(int& val) {
    const int x = 37650;
    std::cout << 37650;
    for (int i = 0; i < 37650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37651(int& val) {
    func_in_37651(val);
    const int x = 37651;
    std::cout << 37651;
    for (int i = 0; i < 37651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37651(int& val) {
    const int x = 37651;
    std::cout << 37651;
    for (int i = 0; i < 37651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37652(int& val) {
    func_in_37652(val);
    const int x = 37652;
    std::cout << 37652;
    for (int i = 0; i < 37652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37652(int& val) {
    const int x = 37652;
    std::cout << 37652;
    for (int i = 0; i < 37652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37653(int& val) {
    func_in_37653(val);
    const int x = 37653;
    std::cout << 37653;
    for (int i = 0; i < 37653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37653(int& val) {
    const int x = 37653;
    std::cout << 37653;
    for (int i = 0; i < 37653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37654(int& val) {
    func_in_37654(val);
    const int x = 37654;
    std::cout << 37654;
    for (int i = 0; i < 37654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37654(int& val) {
    const int x = 37654;
    std::cout << 37654;
    for (int i = 0; i < 37654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37655(int& val) {
    func_in_37655(val);
    const int x = 37655;
    std::cout << 37655;
    for (int i = 0; i < 37655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37655(int& val) {
    const int x = 37655;
    std::cout << 37655;
    for (int i = 0; i < 37655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37656(int& val) {
    func_in_37656(val);
    const int x = 37656;
    std::cout << 37656;
    for (int i = 0; i < 37656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37656(int& val) {
    const int x = 37656;
    std::cout << 37656;
    for (int i = 0; i < 37656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37657(int& val) {
    func_in_37657(val);
    const int x = 37657;
    std::cout << 37657;
    for (int i = 0; i < 37657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37657(int& val) {
    const int x = 37657;
    std::cout << 37657;
    for (int i = 0; i < 37657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37658(int& val) {
    func_in_37658(val);
    const int x = 37658;
    std::cout << 37658;
    for (int i = 0; i < 37658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37658(int& val) {
    const int x = 37658;
    std::cout << 37658;
    for (int i = 0; i < 37658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37659(int& val) {
    func_in_37659(val);
    const int x = 37659;
    std::cout << 37659;
    for (int i = 0; i < 37659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37659(int& val) {
    const int x = 37659;
    std::cout << 37659;
    for (int i = 0; i < 37659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37660(int& val) {
    func_in_37660(val);
    const int x = 37660;
    std::cout << 37660;
    for (int i = 0; i < 37660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37660(int& val) {
    const int x = 37660;
    std::cout << 37660;
    for (int i = 0; i < 37660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37661(int& val) {
    func_in_37661(val);
    const int x = 37661;
    std::cout << 37661;
    for (int i = 0; i < 37661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37661(int& val) {
    const int x = 37661;
    std::cout << 37661;
    for (int i = 0; i < 37661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37662(int& val) {
    func_in_37662(val);
    const int x = 37662;
    std::cout << 37662;
    for (int i = 0; i < 37662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37662(int& val) {
    const int x = 37662;
    std::cout << 37662;
    for (int i = 0; i < 37662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37663(int& val) {
    func_in_37663(val);
    const int x = 37663;
    std::cout << 37663;
    for (int i = 0; i < 37663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37663(int& val) {
    const int x = 37663;
    std::cout << 37663;
    for (int i = 0; i < 37663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37664(int& val) {
    func_in_37664(val);
    const int x = 37664;
    std::cout << 37664;
    for (int i = 0; i < 37664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37664(int& val) {
    const int x = 37664;
    std::cout << 37664;
    for (int i = 0; i < 37664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37665(int& val) {
    func_in_37665(val);
    const int x = 37665;
    std::cout << 37665;
    for (int i = 0; i < 37665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37665(int& val) {
    const int x = 37665;
    std::cout << 37665;
    for (int i = 0; i < 37665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37666(int& val) {
    func_in_37666(val);
    const int x = 37666;
    std::cout << 37666;
    for (int i = 0; i < 37666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37666(int& val) {
    const int x = 37666;
    std::cout << 37666;
    for (int i = 0; i < 37666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37667(int& val) {
    func_in_37667(val);
    const int x = 37667;
    std::cout << 37667;
    for (int i = 0; i < 37667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37667(int& val) {
    const int x = 37667;
    std::cout << 37667;
    for (int i = 0; i < 37667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37668(int& val) {
    func_in_37668(val);
    const int x = 37668;
    std::cout << 37668;
    for (int i = 0; i < 37668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37668(int& val) {
    const int x = 37668;
    std::cout << 37668;
    for (int i = 0; i < 37668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37669(int& val) {
    func_in_37669(val);
    const int x = 37669;
    std::cout << 37669;
    for (int i = 0; i < 37669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37669(int& val) {
    const int x = 37669;
    std::cout << 37669;
    for (int i = 0; i < 37669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37670(int& val) {
    func_in_37670(val);
    const int x = 37670;
    std::cout << 37670;
    for (int i = 0; i < 37670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37670(int& val) {
    const int x = 37670;
    std::cout << 37670;
    for (int i = 0; i < 37670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37671(int& val) {
    func_in_37671(val);
    const int x = 37671;
    std::cout << 37671;
    for (int i = 0; i < 37671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37671(int& val) {
    const int x = 37671;
    std::cout << 37671;
    for (int i = 0; i < 37671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37672(int& val) {
    func_in_37672(val);
    const int x = 37672;
    std::cout << 37672;
    for (int i = 0; i < 37672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37672(int& val) {
    const int x = 37672;
    std::cout << 37672;
    for (int i = 0; i < 37672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37673(int& val) {
    func_in_37673(val);
    const int x = 37673;
    std::cout << 37673;
    for (int i = 0; i < 37673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37673(int& val) {
    const int x = 37673;
    std::cout << 37673;
    for (int i = 0; i < 37673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37674(int& val) {
    func_in_37674(val);
    const int x = 37674;
    std::cout << 37674;
    for (int i = 0; i < 37674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37674(int& val) {
    const int x = 37674;
    std::cout << 37674;
    for (int i = 0; i < 37674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37675(int& val) {
    func_in_37675(val);
    const int x = 37675;
    std::cout << 37675;
    for (int i = 0; i < 37675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37675(int& val) {
    const int x = 37675;
    std::cout << 37675;
    for (int i = 0; i < 37675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37676(int& val) {
    func_in_37676(val);
    const int x = 37676;
    std::cout << 37676;
    for (int i = 0; i < 37676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37676(int& val) {
    const int x = 37676;
    std::cout << 37676;
    for (int i = 0; i < 37676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37677(int& val) {
    func_in_37677(val);
    const int x = 37677;
    std::cout << 37677;
    for (int i = 0; i < 37677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37677(int& val) {
    const int x = 37677;
    std::cout << 37677;
    for (int i = 0; i < 37677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37678(int& val) {
    func_in_37678(val);
    const int x = 37678;
    std::cout << 37678;
    for (int i = 0; i < 37678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37678(int& val) {
    const int x = 37678;
    std::cout << 37678;
    for (int i = 0; i < 37678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37679(int& val) {
    func_in_37679(val);
    const int x = 37679;
    std::cout << 37679;
    for (int i = 0; i < 37679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37679(int& val) {
    const int x = 37679;
    std::cout << 37679;
    for (int i = 0; i < 37679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37680(int& val) {
    func_in_37680(val);
    const int x = 37680;
    std::cout << 37680;
    for (int i = 0; i < 37680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37680(int& val) {
    const int x = 37680;
    std::cout << 37680;
    for (int i = 0; i < 37680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37681(int& val) {
    func_in_37681(val);
    const int x = 37681;
    std::cout << 37681;
    for (int i = 0; i < 37681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37681(int& val) {
    const int x = 37681;
    std::cout << 37681;
    for (int i = 0; i < 37681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37682(int& val) {
    func_in_37682(val);
    const int x = 37682;
    std::cout << 37682;
    for (int i = 0; i < 37682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37682(int& val) {
    const int x = 37682;
    std::cout << 37682;
    for (int i = 0; i < 37682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37683(int& val) {
    func_in_37683(val);
    const int x = 37683;
    std::cout << 37683;
    for (int i = 0; i < 37683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37683(int& val) {
    const int x = 37683;
    std::cout << 37683;
    for (int i = 0; i < 37683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37684(int& val) {
    func_in_37684(val);
    const int x = 37684;
    std::cout << 37684;
    for (int i = 0; i < 37684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37684(int& val) {
    const int x = 37684;
    std::cout << 37684;
    for (int i = 0; i < 37684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37685(int& val) {
    func_in_37685(val);
    const int x = 37685;
    std::cout << 37685;
    for (int i = 0; i < 37685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37685(int& val) {
    const int x = 37685;
    std::cout << 37685;
    for (int i = 0; i < 37685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37686(int& val) {
    func_in_37686(val);
    const int x = 37686;
    std::cout << 37686;
    for (int i = 0; i < 37686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37686(int& val) {
    const int x = 37686;
    std::cout << 37686;
    for (int i = 0; i < 37686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37687(int& val) {
    func_in_37687(val);
    const int x = 37687;
    std::cout << 37687;
    for (int i = 0; i < 37687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37687(int& val) {
    const int x = 37687;
    std::cout << 37687;
    for (int i = 0; i < 37687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37688(int& val) {
    func_in_37688(val);
    const int x = 37688;
    std::cout << 37688;
    for (int i = 0; i < 37688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37688(int& val) {
    const int x = 37688;
    std::cout << 37688;
    for (int i = 0; i < 37688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37689(int& val) {
    func_in_37689(val);
    const int x = 37689;
    std::cout << 37689;
    for (int i = 0; i < 37689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37689(int& val) {
    const int x = 37689;
    std::cout << 37689;
    for (int i = 0; i < 37689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37690(int& val) {
    func_in_37690(val);
    const int x = 37690;
    std::cout << 37690;
    for (int i = 0; i < 37690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37690(int& val) {
    const int x = 37690;
    std::cout << 37690;
    for (int i = 0; i < 37690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37691(int& val) {
    func_in_37691(val);
    const int x = 37691;
    std::cout << 37691;
    for (int i = 0; i < 37691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37691(int& val) {
    const int x = 37691;
    std::cout << 37691;
    for (int i = 0; i < 37691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37692(int& val) {
    func_in_37692(val);
    const int x = 37692;
    std::cout << 37692;
    for (int i = 0; i < 37692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37692(int& val) {
    const int x = 37692;
    std::cout << 37692;
    for (int i = 0; i < 37692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37693(int& val) {
    func_in_37693(val);
    const int x = 37693;
    std::cout << 37693;
    for (int i = 0; i < 37693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37693(int& val) {
    const int x = 37693;
    std::cout << 37693;
    for (int i = 0; i < 37693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37694(int& val) {
    func_in_37694(val);
    const int x = 37694;
    std::cout << 37694;
    for (int i = 0; i < 37694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37694(int& val) {
    const int x = 37694;
    std::cout << 37694;
    for (int i = 0; i < 37694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37695(int& val) {
    func_in_37695(val);
    const int x = 37695;
    std::cout << 37695;
    for (int i = 0; i < 37695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37695(int& val) {
    const int x = 37695;
    std::cout << 37695;
    for (int i = 0; i < 37695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37696(int& val) {
    func_in_37696(val);
    const int x = 37696;
    std::cout << 37696;
    for (int i = 0; i < 37696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37696(int& val) {
    const int x = 37696;
    std::cout << 37696;
    for (int i = 0; i < 37696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37697(int& val) {
    func_in_37697(val);
    const int x = 37697;
    std::cout << 37697;
    for (int i = 0; i < 37697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37697(int& val) {
    const int x = 37697;
    std::cout << 37697;
    for (int i = 0; i < 37697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37698(int& val) {
    func_in_37698(val);
    const int x = 37698;
    std::cout << 37698;
    for (int i = 0; i < 37698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37698(int& val) {
    const int x = 37698;
    std::cout << 37698;
    for (int i = 0; i < 37698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37699(int& val) {
    func_in_37699(val);
    const int x = 37699;
    std::cout << 37699;
    for (int i = 0; i < 37699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37699(int& val) {
    const int x = 37699;
    std::cout << 37699;
    for (int i = 0; i < 37699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37700(int& val) {
    func_in_37700(val);
    const int x = 37700;
    std::cout << 37700;
    for (int i = 0; i < 37700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37700(int& val) {
    const int x = 37700;
    std::cout << 37700;
    for (int i = 0; i < 37700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37701(int& val) {
    func_in_37701(val);
    const int x = 37701;
    std::cout << 37701;
    for (int i = 0; i < 37701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37701(int& val) {
    const int x = 37701;
    std::cout << 37701;
    for (int i = 0; i < 37701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37702(int& val) {
    func_in_37702(val);
    const int x = 37702;
    std::cout << 37702;
    for (int i = 0; i < 37702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37702(int& val) {
    const int x = 37702;
    std::cout << 37702;
    for (int i = 0; i < 37702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37703(int& val) {
    func_in_37703(val);
    const int x = 37703;
    std::cout << 37703;
    for (int i = 0; i < 37703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37703(int& val) {
    const int x = 37703;
    std::cout << 37703;
    for (int i = 0; i < 37703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37704(int& val) {
    func_in_37704(val);
    const int x = 37704;
    std::cout << 37704;
    for (int i = 0; i < 37704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37704(int& val) {
    const int x = 37704;
    std::cout << 37704;
    for (int i = 0; i < 37704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37705(int& val) {
    func_in_37705(val);
    const int x = 37705;
    std::cout << 37705;
    for (int i = 0; i < 37705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37705(int& val) {
    const int x = 37705;
    std::cout << 37705;
    for (int i = 0; i < 37705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37706(int& val) {
    func_in_37706(val);
    const int x = 37706;
    std::cout << 37706;
    for (int i = 0; i < 37706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37706(int& val) {
    const int x = 37706;
    std::cout << 37706;
    for (int i = 0; i < 37706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37707(int& val) {
    func_in_37707(val);
    const int x = 37707;
    std::cout << 37707;
    for (int i = 0; i < 37707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37707(int& val) {
    const int x = 37707;
    std::cout << 37707;
    for (int i = 0; i < 37707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37708(int& val) {
    func_in_37708(val);
    const int x = 37708;
    std::cout << 37708;
    for (int i = 0; i < 37708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37708(int& val) {
    const int x = 37708;
    std::cout << 37708;
    for (int i = 0; i < 37708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37709(int& val) {
    func_in_37709(val);
    const int x = 37709;
    std::cout << 37709;
    for (int i = 0; i < 37709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37709(int& val) {
    const int x = 37709;
    std::cout << 37709;
    for (int i = 0; i < 37709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37710(int& val) {
    func_in_37710(val);
    const int x = 37710;
    std::cout << 37710;
    for (int i = 0; i < 37710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37710(int& val) {
    const int x = 37710;
    std::cout << 37710;
    for (int i = 0; i < 37710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37711(int& val) {
    func_in_37711(val);
    const int x = 37711;
    std::cout << 37711;
    for (int i = 0; i < 37711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37711(int& val) {
    const int x = 37711;
    std::cout << 37711;
    for (int i = 0; i < 37711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37712(int& val) {
    func_in_37712(val);
    const int x = 37712;
    std::cout << 37712;
    for (int i = 0; i < 37712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37712(int& val) {
    const int x = 37712;
    std::cout << 37712;
    for (int i = 0; i < 37712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37713(int& val) {
    func_in_37713(val);
    const int x = 37713;
    std::cout << 37713;
    for (int i = 0; i < 37713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37713(int& val) {
    const int x = 37713;
    std::cout << 37713;
    for (int i = 0; i < 37713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37714(int& val) {
    func_in_37714(val);
    const int x = 37714;
    std::cout << 37714;
    for (int i = 0; i < 37714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37714(int& val) {
    const int x = 37714;
    std::cout << 37714;
    for (int i = 0; i < 37714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37715(int& val) {
    func_in_37715(val);
    const int x = 37715;
    std::cout << 37715;
    for (int i = 0; i < 37715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37715(int& val) {
    const int x = 37715;
    std::cout << 37715;
    for (int i = 0; i < 37715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37716(int& val) {
    func_in_37716(val);
    const int x = 37716;
    std::cout << 37716;
    for (int i = 0; i < 37716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37716(int& val) {
    const int x = 37716;
    std::cout << 37716;
    for (int i = 0; i < 37716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37717(int& val) {
    func_in_37717(val);
    const int x = 37717;
    std::cout << 37717;
    for (int i = 0; i < 37717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37717(int& val) {
    const int x = 37717;
    std::cout << 37717;
    for (int i = 0; i < 37717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37718(int& val) {
    func_in_37718(val);
    const int x = 37718;
    std::cout << 37718;
    for (int i = 0; i < 37718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37718(int& val) {
    const int x = 37718;
    std::cout << 37718;
    for (int i = 0; i < 37718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37719(int& val) {
    func_in_37719(val);
    const int x = 37719;
    std::cout << 37719;
    for (int i = 0; i < 37719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37719(int& val) {
    const int x = 37719;
    std::cout << 37719;
    for (int i = 0; i < 37719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37720(int& val) {
    func_in_37720(val);
    const int x = 37720;
    std::cout << 37720;
    for (int i = 0; i < 37720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37720(int& val) {
    const int x = 37720;
    std::cout << 37720;
    for (int i = 0; i < 37720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37721(int& val) {
    func_in_37721(val);
    const int x = 37721;
    std::cout << 37721;
    for (int i = 0; i < 37721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37721(int& val) {
    const int x = 37721;
    std::cout << 37721;
    for (int i = 0; i < 37721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37722(int& val) {
    func_in_37722(val);
    const int x = 37722;
    std::cout << 37722;
    for (int i = 0; i < 37722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37722(int& val) {
    const int x = 37722;
    std::cout << 37722;
    for (int i = 0; i < 37722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37723(int& val) {
    func_in_37723(val);
    const int x = 37723;
    std::cout << 37723;
    for (int i = 0; i < 37723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37723(int& val) {
    const int x = 37723;
    std::cout << 37723;
    for (int i = 0; i < 37723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37724(int& val) {
    func_in_37724(val);
    const int x = 37724;
    std::cout << 37724;
    for (int i = 0; i < 37724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37724(int& val) {
    const int x = 37724;
    std::cout << 37724;
    for (int i = 0; i < 37724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37725(int& val) {
    func_in_37725(val);
    const int x = 37725;
    std::cout << 37725;
    for (int i = 0; i < 37725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37725(int& val) {
    const int x = 37725;
    std::cout << 37725;
    for (int i = 0; i < 37725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37726(int& val) {
    func_in_37726(val);
    const int x = 37726;
    std::cout << 37726;
    for (int i = 0; i < 37726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37726(int& val) {
    const int x = 37726;
    std::cout << 37726;
    for (int i = 0; i < 37726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37727(int& val) {
    func_in_37727(val);
    const int x = 37727;
    std::cout << 37727;
    for (int i = 0; i < 37727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37727(int& val) {
    const int x = 37727;
    std::cout << 37727;
    for (int i = 0; i < 37727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37728(int& val) {
    func_in_37728(val);
    const int x = 37728;
    std::cout << 37728;
    for (int i = 0; i < 37728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37728(int& val) {
    const int x = 37728;
    std::cout << 37728;
    for (int i = 0; i < 37728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37729(int& val) {
    func_in_37729(val);
    const int x = 37729;
    std::cout << 37729;
    for (int i = 0; i < 37729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37729(int& val) {
    const int x = 37729;
    std::cout << 37729;
    for (int i = 0; i < 37729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37730(int& val) {
    func_in_37730(val);
    const int x = 37730;
    std::cout << 37730;
    for (int i = 0; i < 37730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37730(int& val) {
    const int x = 37730;
    std::cout << 37730;
    for (int i = 0; i < 37730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37731(int& val) {
    func_in_37731(val);
    const int x = 37731;
    std::cout << 37731;
    for (int i = 0; i < 37731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37731(int& val) {
    const int x = 37731;
    std::cout << 37731;
    for (int i = 0; i < 37731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37732(int& val) {
    func_in_37732(val);
    const int x = 37732;
    std::cout << 37732;
    for (int i = 0; i < 37732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37732(int& val) {
    const int x = 37732;
    std::cout << 37732;
    for (int i = 0; i < 37732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37733(int& val) {
    func_in_37733(val);
    const int x = 37733;
    std::cout << 37733;
    for (int i = 0; i < 37733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37733(int& val) {
    const int x = 37733;
    std::cout << 37733;
    for (int i = 0; i < 37733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37734(int& val) {
    func_in_37734(val);
    const int x = 37734;
    std::cout << 37734;
    for (int i = 0; i < 37734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37734(int& val) {
    const int x = 37734;
    std::cout << 37734;
    for (int i = 0; i < 37734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37735(int& val) {
    func_in_37735(val);
    const int x = 37735;
    std::cout << 37735;
    for (int i = 0; i < 37735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37735(int& val) {
    const int x = 37735;
    std::cout << 37735;
    for (int i = 0; i < 37735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37736(int& val) {
    func_in_37736(val);
    const int x = 37736;
    std::cout << 37736;
    for (int i = 0; i < 37736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37736(int& val) {
    const int x = 37736;
    std::cout << 37736;
    for (int i = 0; i < 37736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37737(int& val) {
    func_in_37737(val);
    const int x = 37737;
    std::cout << 37737;
    for (int i = 0; i < 37737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37737(int& val) {
    const int x = 37737;
    std::cout << 37737;
    for (int i = 0; i < 37737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37738(int& val) {
    func_in_37738(val);
    const int x = 37738;
    std::cout << 37738;
    for (int i = 0; i < 37738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37738(int& val) {
    const int x = 37738;
    std::cout << 37738;
    for (int i = 0; i < 37738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37739(int& val) {
    func_in_37739(val);
    const int x = 37739;
    std::cout << 37739;
    for (int i = 0; i < 37739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37739(int& val) {
    const int x = 37739;
    std::cout << 37739;
    for (int i = 0; i < 37739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37740(int& val) {
    func_in_37740(val);
    const int x = 37740;
    std::cout << 37740;
    for (int i = 0; i < 37740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37740(int& val) {
    const int x = 37740;
    std::cout << 37740;
    for (int i = 0; i < 37740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37741(int& val) {
    func_in_37741(val);
    const int x = 37741;
    std::cout << 37741;
    for (int i = 0; i < 37741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37741(int& val) {
    const int x = 37741;
    std::cout << 37741;
    for (int i = 0; i < 37741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37742(int& val) {
    func_in_37742(val);
    const int x = 37742;
    std::cout << 37742;
    for (int i = 0; i < 37742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37742(int& val) {
    const int x = 37742;
    std::cout << 37742;
    for (int i = 0; i < 37742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37743(int& val) {
    func_in_37743(val);
    const int x = 37743;
    std::cout << 37743;
    for (int i = 0; i < 37743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37743(int& val) {
    const int x = 37743;
    std::cout << 37743;
    for (int i = 0; i < 37743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37744(int& val) {
    func_in_37744(val);
    const int x = 37744;
    std::cout << 37744;
    for (int i = 0; i < 37744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37744(int& val) {
    const int x = 37744;
    std::cout << 37744;
    for (int i = 0; i < 37744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37745(int& val) {
    func_in_37745(val);
    const int x = 37745;
    std::cout << 37745;
    for (int i = 0; i < 37745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37745(int& val) {
    const int x = 37745;
    std::cout << 37745;
    for (int i = 0; i < 37745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37746(int& val) {
    func_in_37746(val);
    const int x = 37746;
    std::cout << 37746;
    for (int i = 0; i < 37746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37746(int& val) {
    const int x = 37746;
    std::cout << 37746;
    for (int i = 0; i < 37746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37747(int& val) {
    func_in_37747(val);
    const int x = 37747;
    std::cout << 37747;
    for (int i = 0; i < 37747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37747(int& val) {
    const int x = 37747;
    std::cout << 37747;
    for (int i = 0; i < 37747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37748(int& val) {
    func_in_37748(val);
    const int x = 37748;
    std::cout << 37748;
    for (int i = 0; i < 37748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37748(int& val) {
    const int x = 37748;
    std::cout << 37748;
    for (int i = 0; i < 37748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37749(int& val) {
    func_in_37749(val);
    const int x = 37749;
    std::cout << 37749;
    for (int i = 0; i < 37749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37749(int& val) {
    const int x = 37749;
    std::cout << 37749;
    for (int i = 0; i < 37749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37750(int& val) {
    func_in_37750(val);
    const int x = 37750;
    std::cout << 37750;
    for (int i = 0; i < 37750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37750(int& val) {
    const int x = 37750;
    std::cout << 37750;
    for (int i = 0; i < 37750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37751(int& val) {
    func_in_37751(val);
    const int x = 37751;
    std::cout << 37751;
    for (int i = 0; i < 37751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37751(int& val) {
    const int x = 37751;
    std::cout << 37751;
    for (int i = 0; i < 37751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37752(int& val) {
    func_in_37752(val);
    const int x = 37752;
    std::cout << 37752;
    for (int i = 0; i < 37752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37752(int& val) {
    const int x = 37752;
    std::cout << 37752;
    for (int i = 0; i < 37752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37753(int& val) {
    func_in_37753(val);
    const int x = 37753;
    std::cout << 37753;
    for (int i = 0; i < 37753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37753(int& val) {
    const int x = 37753;
    std::cout << 37753;
    for (int i = 0; i < 37753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37754(int& val) {
    func_in_37754(val);
    const int x = 37754;
    std::cout << 37754;
    for (int i = 0; i < 37754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37754(int& val) {
    const int x = 37754;
    std::cout << 37754;
    for (int i = 0; i < 37754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37755(int& val) {
    func_in_37755(val);
    const int x = 37755;
    std::cout << 37755;
    for (int i = 0; i < 37755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37755(int& val) {
    const int x = 37755;
    std::cout << 37755;
    for (int i = 0; i < 37755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37756(int& val) {
    func_in_37756(val);
    const int x = 37756;
    std::cout << 37756;
    for (int i = 0; i < 37756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37756(int& val) {
    const int x = 37756;
    std::cout << 37756;
    for (int i = 0; i < 37756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37757(int& val) {
    func_in_37757(val);
    const int x = 37757;
    std::cout << 37757;
    for (int i = 0; i < 37757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37757(int& val) {
    const int x = 37757;
    std::cout << 37757;
    for (int i = 0; i < 37757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37758(int& val) {
    func_in_37758(val);
    const int x = 37758;
    std::cout << 37758;
    for (int i = 0; i < 37758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37758(int& val) {
    const int x = 37758;
    std::cout << 37758;
    for (int i = 0; i < 37758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37759(int& val) {
    func_in_37759(val);
    const int x = 37759;
    std::cout << 37759;
    for (int i = 0; i < 37759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37759(int& val) {
    const int x = 37759;
    std::cout << 37759;
    for (int i = 0; i < 37759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37760(int& val) {
    func_in_37760(val);
    const int x = 37760;
    std::cout << 37760;
    for (int i = 0; i < 37760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37760(int& val) {
    const int x = 37760;
    std::cout << 37760;
    for (int i = 0; i < 37760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37761(int& val) {
    func_in_37761(val);
    const int x = 37761;
    std::cout << 37761;
    for (int i = 0; i < 37761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37761(int& val) {
    const int x = 37761;
    std::cout << 37761;
    for (int i = 0; i < 37761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37762(int& val) {
    func_in_37762(val);
    const int x = 37762;
    std::cout << 37762;
    for (int i = 0; i < 37762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37762(int& val) {
    const int x = 37762;
    std::cout << 37762;
    for (int i = 0; i < 37762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37763(int& val) {
    func_in_37763(val);
    const int x = 37763;
    std::cout << 37763;
    for (int i = 0; i < 37763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37763(int& val) {
    const int x = 37763;
    std::cout << 37763;
    for (int i = 0; i < 37763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37764(int& val) {
    func_in_37764(val);
    const int x = 37764;
    std::cout << 37764;
    for (int i = 0; i < 37764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37764(int& val) {
    const int x = 37764;
    std::cout << 37764;
    for (int i = 0; i < 37764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37765(int& val) {
    func_in_37765(val);
    const int x = 37765;
    std::cout << 37765;
    for (int i = 0; i < 37765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37765(int& val) {
    const int x = 37765;
    std::cout << 37765;
    for (int i = 0; i < 37765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37766(int& val) {
    func_in_37766(val);
    const int x = 37766;
    std::cout << 37766;
    for (int i = 0; i < 37766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37766(int& val) {
    const int x = 37766;
    std::cout << 37766;
    for (int i = 0; i < 37766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37767(int& val) {
    func_in_37767(val);
    const int x = 37767;
    std::cout << 37767;
    for (int i = 0; i < 37767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37767(int& val) {
    const int x = 37767;
    std::cout << 37767;
    for (int i = 0; i < 37767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37768(int& val) {
    func_in_37768(val);
    const int x = 37768;
    std::cout << 37768;
    for (int i = 0; i < 37768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37768(int& val) {
    const int x = 37768;
    std::cout << 37768;
    for (int i = 0; i < 37768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37769(int& val) {
    func_in_37769(val);
    const int x = 37769;
    std::cout << 37769;
    for (int i = 0; i < 37769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37769(int& val) {
    const int x = 37769;
    std::cout << 37769;
    for (int i = 0; i < 37769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37770(int& val) {
    func_in_37770(val);
    const int x = 37770;
    std::cout << 37770;
    for (int i = 0; i < 37770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37770(int& val) {
    const int x = 37770;
    std::cout << 37770;
    for (int i = 0; i < 37770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37771(int& val) {
    func_in_37771(val);
    const int x = 37771;
    std::cout << 37771;
    for (int i = 0; i < 37771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37771(int& val) {
    const int x = 37771;
    std::cout << 37771;
    for (int i = 0; i < 37771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37772(int& val) {
    func_in_37772(val);
    const int x = 37772;
    std::cout << 37772;
    for (int i = 0; i < 37772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37772(int& val) {
    const int x = 37772;
    std::cout << 37772;
    for (int i = 0; i < 37772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37773(int& val) {
    func_in_37773(val);
    const int x = 37773;
    std::cout << 37773;
    for (int i = 0; i < 37773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37773(int& val) {
    const int x = 37773;
    std::cout << 37773;
    for (int i = 0; i < 37773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37774(int& val) {
    func_in_37774(val);
    const int x = 37774;
    std::cout << 37774;
    for (int i = 0; i < 37774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37774(int& val) {
    const int x = 37774;
    std::cout << 37774;
    for (int i = 0; i < 37774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37775(int& val) {
    func_in_37775(val);
    const int x = 37775;
    std::cout << 37775;
    for (int i = 0; i < 37775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37775(int& val) {
    const int x = 37775;
    std::cout << 37775;
    for (int i = 0; i < 37775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37776(int& val) {
    func_in_37776(val);
    const int x = 37776;
    std::cout << 37776;
    for (int i = 0; i < 37776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37776(int& val) {
    const int x = 37776;
    std::cout << 37776;
    for (int i = 0; i < 37776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37777(int& val) {
    func_in_37777(val);
    const int x = 37777;
    std::cout << 37777;
    for (int i = 0; i < 37777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37777(int& val) {
    const int x = 37777;
    std::cout << 37777;
    for (int i = 0; i < 37777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37778(int& val) {
    func_in_37778(val);
    const int x = 37778;
    std::cout << 37778;
    for (int i = 0; i < 37778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37778(int& val) {
    const int x = 37778;
    std::cout << 37778;
    for (int i = 0; i < 37778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37779(int& val) {
    func_in_37779(val);
    const int x = 37779;
    std::cout << 37779;
    for (int i = 0; i < 37779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37779(int& val) {
    const int x = 37779;
    std::cout << 37779;
    for (int i = 0; i < 37779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37780(int& val) {
    func_in_37780(val);
    const int x = 37780;
    std::cout << 37780;
    for (int i = 0; i < 37780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37780(int& val) {
    const int x = 37780;
    std::cout << 37780;
    for (int i = 0; i < 37780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37781(int& val) {
    func_in_37781(val);
    const int x = 37781;
    std::cout << 37781;
    for (int i = 0; i < 37781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37781(int& val) {
    const int x = 37781;
    std::cout << 37781;
    for (int i = 0; i < 37781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37782(int& val) {
    func_in_37782(val);
    const int x = 37782;
    std::cout << 37782;
    for (int i = 0; i < 37782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37782(int& val) {
    const int x = 37782;
    std::cout << 37782;
    for (int i = 0; i < 37782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37783(int& val) {
    func_in_37783(val);
    const int x = 37783;
    std::cout << 37783;
    for (int i = 0; i < 37783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37783(int& val) {
    const int x = 37783;
    std::cout << 37783;
    for (int i = 0; i < 37783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37784(int& val) {
    func_in_37784(val);
    const int x = 37784;
    std::cout << 37784;
    for (int i = 0; i < 37784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37784(int& val) {
    const int x = 37784;
    std::cout << 37784;
    for (int i = 0; i < 37784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37785(int& val) {
    func_in_37785(val);
    const int x = 37785;
    std::cout << 37785;
    for (int i = 0; i < 37785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37785(int& val) {
    const int x = 37785;
    std::cout << 37785;
    for (int i = 0; i < 37785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37786(int& val) {
    func_in_37786(val);
    const int x = 37786;
    std::cout << 37786;
    for (int i = 0; i < 37786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37786(int& val) {
    const int x = 37786;
    std::cout << 37786;
    for (int i = 0; i < 37786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37787(int& val) {
    func_in_37787(val);
    const int x = 37787;
    std::cout << 37787;
    for (int i = 0; i < 37787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37787(int& val) {
    const int x = 37787;
    std::cout << 37787;
    for (int i = 0; i < 37787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37788(int& val) {
    func_in_37788(val);
    const int x = 37788;
    std::cout << 37788;
    for (int i = 0; i < 37788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37788(int& val) {
    const int x = 37788;
    std::cout << 37788;
    for (int i = 0; i < 37788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37789(int& val) {
    func_in_37789(val);
    const int x = 37789;
    std::cout << 37789;
    for (int i = 0; i < 37789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37789(int& val) {
    const int x = 37789;
    std::cout << 37789;
    for (int i = 0; i < 37789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37790(int& val) {
    func_in_37790(val);
    const int x = 37790;
    std::cout << 37790;
    for (int i = 0; i < 37790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37790(int& val) {
    const int x = 37790;
    std::cout << 37790;
    for (int i = 0; i < 37790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37791(int& val) {
    func_in_37791(val);
    const int x = 37791;
    std::cout << 37791;
    for (int i = 0; i < 37791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37791(int& val) {
    const int x = 37791;
    std::cout << 37791;
    for (int i = 0; i < 37791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37792(int& val) {
    func_in_37792(val);
    const int x = 37792;
    std::cout << 37792;
    for (int i = 0; i < 37792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37792(int& val) {
    const int x = 37792;
    std::cout << 37792;
    for (int i = 0; i < 37792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37793(int& val) {
    func_in_37793(val);
    const int x = 37793;
    std::cout << 37793;
    for (int i = 0; i < 37793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37793(int& val) {
    const int x = 37793;
    std::cout << 37793;
    for (int i = 0; i < 37793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37794(int& val) {
    func_in_37794(val);
    const int x = 37794;
    std::cout << 37794;
    for (int i = 0; i < 37794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37794(int& val) {
    const int x = 37794;
    std::cout << 37794;
    for (int i = 0; i < 37794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37795(int& val) {
    func_in_37795(val);
    const int x = 37795;
    std::cout << 37795;
    for (int i = 0; i < 37795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37795(int& val) {
    const int x = 37795;
    std::cout << 37795;
    for (int i = 0; i < 37795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37796(int& val) {
    func_in_37796(val);
    const int x = 37796;
    std::cout << 37796;
    for (int i = 0; i < 37796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37796(int& val) {
    const int x = 37796;
    std::cout << 37796;
    for (int i = 0; i < 37796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37797(int& val) {
    func_in_37797(val);
    const int x = 37797;
    std::cout << 37797;
    for (int i = 0; i < 37797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37797(int& val) {
    const int x = 37797;
    std::cout << 37797;
    for (int i = 0; i < 37797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37798(int& val) {
    func_in_37798(val);
    const int x = 37798;
    std::cout << 37798;
    for (int i = 0; i < 37798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37798(int& val) {
    const int x = 37798;
    std::cout << 37798;
    for (int i = 0; i < 37798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37799(int& val) {
    func_in_37799(val);
    const int x = 37799;
    std::cout << 37799;
    for (int i = 0; i < 37799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37799(int& val) {
    const int x = 37799;
    std::cout << 37799;
    for (int i = 0; i < 37799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37800(int& val) {
    func_in_37800(val);
    const int x = 37800;
    std::cout << 37800;
    for (int i = 0; i < 37800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37800(int& val) {
    const int x = 37800;
    std::cout << 37800;
    for (int i = 0; i < 37800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37801(int& val) {
    func_in_37801(val);
    const int x = 37801;
    std::cout << 37801;
    for (int i = 0; i < 37801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37801(int& val) {
    const int x = 37801;
    std::cout << 37801;
    for (int i = 0; i < 37801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37802(int& val) {
    func_in_37802(val);
    const int x = 37802;
    std::cout << 37802;
    for (int i = 0; i < 37802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37802(int& val) {
    const int x = 37802;
    std::cout << 37802;
    for (int i = 0; i < 37802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37803(int& val) {
    func_in_37803(val);
    const int x = 37803;
    std::cout << 37803;
    for (int i = 0; i < 37803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37803(int& val) {
    const int x = 37803;
    std::cout << 37803;
    for (int i = 0; i < 37803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37804(int& val) {
    func_in_37804(val);
    const int x = 37804;
    std::cout << 37804;
    for (int i = 0; i < 37804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37804(int& val) {
    const int x = 37804;
    std::cout << 37804;
    for (int i = 0; i < 37804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37805(int& val) {
    func_in_37805(val);
    const int x = 37805;
    std::cout << 37805;
    for (int i = 0; i < 37805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37805(int& val) {
    const int x = 37805;
    std::cout << 37805;
    for (int i = 0; i < 37805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37806(int& val) {
    func_in_37806(val);
    const int x = 37806;
    std::cout << 37806;
    for (int i = 0; i < 37806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37806(int& val) {
    const int x = 37806;
    std::cout << 37806;
    for (int i = 0; i < 37806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37807(int& val) {
    func_in_37807(val);
    const int x = 37807;
    std::cout << 37807;
    for (int i = 0; i < 37807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37807(int& val) {
    const int x = 37807;
    std::cout << 37807;
    for (int i = 0; i < 37807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37808(int& val) {
    func_in_37808(val);
    const int x = 37808;
    std::cout << 37808;
    for (int i = 0; i < 37808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37808(int& val) {
    const int x = 37808;
    std::cout << 37808;
    for (int i = 0; i < 37808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37809(int& val) {
    func_in_37809(val);
    const int x = 37809;
    std::cout << 37809;
    for (int i = 0; i < 37809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37809(int& val) {
    const int x = 37809;
    std::cout << 37809;
    for (int i = 0; i < 37809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37810(int& val) {
    func_in_37810(val);
    const int x = 37810;
    std::cout << 37810;
    for (int i = 0; i < 37810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37810(int& val) {
    const int x = 37810;
    std::cout << 37810;
    for (int i = 0; i < 37810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37811(int& val) {
    func_in_37811(val);
    const int x = 37811;
    std::cout << 37811;
    for (int i = 0; i < 37811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37811(int& val) {
    const int x = 37811;
    std::cout << 37811;
    for (int i = 0; i < 37811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37812(int& val) {
    func_in_37812(val);
    const int x = 37812;
    std::cout << 37812;
    for (int i = 0; i < 37812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37812(int& val) {
    const int x = 37812;
    std::cout << 37812;
    for (int i = 0; i < 37812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37813(int& val) {
    func_in_37813(val);
    const int x = 37813;
    std::cout << 37813;
    for (int i = 0; i < 37813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37813(int& val) {
    const int x = 37813;
    std::cout << 37813;
    for (int i = 0; i < 37813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37814(int& val) {
    func_in_37814(val);
    const int x = 37814;
    std::cout << 37814;
    for (int i = 0; i < 37814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37814(int& val) {
    const int x = 37814;
    std::cout << 37814;
    for (int i = 0; i < 37814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37815(int& val) {
    func_in_37815(val);
    const int x = 37815;
    std::cout << 37815;
    for (int i = 0; i < 37815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37815(int& val) {
    const int x = 37815;
    std::cout << 37815;
    for (int i = 0; i < 37815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37816(int& val) {
    func_in_37816(val);
    const int x = 37816;
    std::cout << 37816;
    for (int i = 0; i < 37816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37816(int& val) {
    const int x = 37816;
    std::cout << 37816;
    for (int i = 0; i < 37816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37817(int& val) {
    func_in_37817(val);
    const int x = 37817;
    std::cout << 37817;
    for (int i = 0; i < 37817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37817(int& val) {
    const int x = 37817;
    std::cout << 37817;
    for (int i = 0; i < 37817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37818(int& val) {
    func_in_37818(val);
    const int x = 37818;
    std::cout << 37818;
    for (int i = 0; i < 37818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37818(int& val) {
    const int x = 37818;
    std::cout << 37818;
    for (int i = 0; i < 37818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37819(int& val) {
    func_in_37819(val);
    const int x = 37819;
    std::cout << 37819;
    for (int i = 0; i < 37819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37819(int& val) {
    const int x = 37819;
    std::cout << 37819;
    for (int i = 0; i < 37819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37820(int& val) {
    func_in_37820(val);
    const int x = 37820;
    std::cout << 37820;
    for (int i = 0; i < 37820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37820(int& val) {
    const int x = 37820;
    std::cout << 37820;
    for (int i = 0; i < 37820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37821(int& val) {
    func_in_37821(val);
    const int x = 37821;
    std::cout << 37821;
    for (int i = 0; i < 37821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37821(int& val) {
    const int x = 37821;
    std::cout << 37821;
    for (int i = 0; i < 37821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37822(int& val) {
    func_in_37822(val);
    const int x = 37822;
    std::cout << 37822;
    for (int i = 0; i < 37822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37822(int& val) {
    const int x = 37822;
    std::cout << 37822;
    for (int i = 0; i < 37822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37823(int& val) {
    func_in_37823(val);
    const int x = 37823;
    std::cout << 37823;
    for (int i = 0; i < 37823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37823(int& val) {
    const int x = 37823;
    std::cout << 37823;
    for (int i = 0; i < 37823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37824(int& val) {
    func_in_37824(val);
    const int x = 37824;
    std::cout << 37824;
    for (int i = 0; i < 37824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37824(int& val) {
    const int x = 37824;
    std::cout << 37824;
    for (int i = 0; i < 37824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37825(int& val) {
    func_in_37825(val);
    const int x = 37825;
    std::cout << 37825;
    for (int i = 0; i < 37825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37825(int& val) {
    const int x = 37825;
    std::cout << 37825;
    for (int i = 0; i < 37825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37826(int& val) {
    func_in_37826(val);
    const int x = 37826;
    std::cout << 37826;
    for (int i = 0; i < 37826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37826(int& val) {
    const int x = 37826;
    std::cout << 37826;
    for (int i = 0; i < 37826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37827(int& val) {
    func_in_37827(val);
    const int x = 37827;
    std::cout << 37827;
    for (int i = 0; i < 37827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37827(int& val) {
    const int x = 37827;
    std::cout << 37827;
    for (int i = 0; i < 37827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37828(int& val) {
    func_in_37828(val);
    const int x = 37828;
    std::cout << 37828;
    for (int i = 0; i < 37828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37828(int& val) {
    const int x = 37828;
    std::cout << 37828;
    for (int i = 0; i < 37828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37829(int& val) {
    func_in_37829(val);
    const int x = 37829;
    std::cout << 37829;
    for (int i = 0; i < 37829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37829(int& val) {
    const int x = 37829;
    std::cout << 37829;
    for (int i = 0; i < 37829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37830(int& val) {
    func_in_37830(val);
    const int x = 37830;
    std::cout << 37830;
    for (int i = 0; i < 37830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37830(int& val) {
    const int x = 37830;
    std::cout << 37830;
    for (int i = 0; i < 37830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37831(int& val) {
    func_in_37831(val);
    const int x = 37831;
    std::cout << 37831;
    for (int i = 0; i < 37831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37831(int& val) {
    const int x = 37831;
    std::cout << 37831;
    for (int i = 0; i < 37831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37832(int& val) {
    func_in_37832(val);
    const int x = 37832;
    std::cout << 37832;
    for (int i = 0; i < 37832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37832(int& val) {
    const int x = 37832;
    std::cout << 37832;
    for (int i = 0; i < 37832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37833(int& val) {
    func_in_37833(val);
    const int x = 37833;
    std::cout << 37833;
    for (int i = 0; i < 37833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37833(int& val) {
    const int x = 37833;
    std::cout << 37833;
    for (int i = 0; i < 37833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37834(int& val) {
    func_in_37834(val);
    const int x = 37834;
    std::cout << 37834;
    for (int i = 0; i < 37834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37834(int& val) {
    const int x = 37834;
    std::cout << 37834;
    for (int i = 0; i < 37834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37835(int& val) {
    func_in_37835(val);
    const int x = 37835;
    std::cout << 37835;
    for (int i = 0; i < 37835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37835(int& val) {
    const int x = 37835;
    std::cout << 37835;
    for (int i = 0; i < 37835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37836(int& val) {
    func_in_37836(val);
    const int x = 37836;
    std::cout << 37836;
    for (int i = 0; i < 37836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37836(int& val) {
    const int x = 37836;
    std::cout << 37836;
    for (int i = 0; i < 37836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37837(int& val) {
    func_in_37837(val);
    const int x = 37837;
    std::cout << 37837;
    for (int i = 0; i < 37837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37837(int& val) {
    const int x = 37837;
    std::cout << 37837;
    for (int i = 0; i < 37837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37838(int& val) {
    func_in_37838(val);
    const int x = 37838;
    std::cout << 37838;
    for (int i = 0; i < 37838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37838(int& val) {
    const int x = 37838;
    std::cout << 37838;
    for (int i = 0; i < 37838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37839(int& val) {
    func_in_37839(val);
    const int x = 37839;
    std::cout << 37839;
    for (int i = 0; i < 37839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37839(int& val) {
    const int x = 37839;
    std::cout << 37839;
    for (int i = 0; i < 37839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37840(int& val) {
    func_in_37840(val);
    const int x = 37840;
    std::cout << 37840;
    for (int i = 0; i < 37840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37840(int& val) {
    const int x = 37840;
    std::cout << 37840;
    for (int i = 0; i < 37840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37841(int& val) {
    func_in_37841(val);
    const int x = 37841;
    std::cout << 37841;
    for (int i = 0; i < 37841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37841(int& val) {
    const int x = 37841;
    std::cout << 37841;
    for (int i = 0; i < 37841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37842(int& val) {
    func_in_37842(val);
    const int x = 37842;
    std::cout << 37842;
    for (int i = 0; i < 37842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37842(int& val) {
    const int x = 37842;
    std::cout << 37842;
    for (int i = 0; i < 37842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37843(int& val) {
    func_in_37843(val);
    const int x = 37843;
    std::cout << 37843;
    for (int i = 0; i < 37843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37843(int& val) {
    const int x = 37843;
    std::cout << 37843;
    for (int i = 0; i < 37843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37844(int& val) {
    func_in_37844(val);
    const int x = 37844;
    std::cout << 37844;
    for (int i = 0; i < 37844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37844(int& val) {
    const int x = 37844;
    std::cout << 37844;
    for (int i = 0; i < 37844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37845(int& val) {
    func_in_37845(val);
    const int x = 37845;
    std::cout << 37845;
    for (int i = 0; i < 37845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37845(int& val) {
    const int x = 37845;
    std::cout << 37845;
    for (int i = 0; i < 37845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37846(int& val) {
    func_in_37846(val);
    const int x = 37846;
    std::cout << 37846;
    for (int i = 0; i < 37846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37846(int& val) {
    const int x = 37846;
    std::cout << 37846;
    for (int i = 0; i < 37846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37847(int& val) {
    func_in_37847(val);
    const int x = 37847;
    std::cout << 37847;
    for (int i = 0; i < 37847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37847(int& val) {
    const int x = 37847;
    std::cout << 37847;
    for (int i = 0; i < 37847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37848(int& val) {
    func_in_37848(val);
    const int x = 37848;
    std::cout << 37848;
    for (int i = 0; i < 37848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37848(int& val) {
    const int x = 37848;
    std::cout << 37848;
    for (int i = 0; i < 37848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37849(int& val) {
    func_in_37849(val);
    const int x = 37849;
    std::cout << 37849;
    for (int i = 0; i < 37849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37849(int& val) {
    const int x = 37849;
    std::cout << 37849;
    for (int i = 0; i < 37849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37850(int& val) {
    func_in_37850(val);
    const int x = 37850;
    std::cout << 37850;
    for (int i = 0; i < 37850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37850(int& val) {
    const int x = 37850;
    std::cout << 37850;
    for (int i = 0; i < 37850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37851(int& val) {
    func_in_37851(val);
    const int x = 37851;
    std::cout << 37851;
    for (int i = 0; i < 37851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37851(int& val) {
    const int x = 37851;
    std::cout << 37851;
    for (int i = 0; i < 37851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37852(int& val) {
    func_in_37852(val);
    const int x = 37852;
    std::cout << 37852;
    for (int i = 0; i < 37852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37852(int& val) {
    const int x = 37852;
    std::cout << 37852;
    for (int i = 0; i < 37852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37853(int& val) {
    func_in_37853(val);
    const int x = 37853;
    std::cout << 37853;
    for (int i = 0; i < 37853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37853(int& val) {
    const int x = 37853;
    std::cout << 37853;
    for (int i = 0; i < 37853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37854(int& val) {
    func_in_37854(val);
    const int x = 37854;
    std::cout << 37854;
    for (int i = 0; i < 37854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37854(int& val) {
    const int x = 37854;
    std::cout << 37854;
    for (int i = 0; i < 37854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37855(int& val) {
    func_in_37855(val);
    const int x = 37855;
    std::cout << 37855;
    for (int i = 0; i < 37855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37855(int& val) {
    const int x = 37855;
    std::cout << 37855;
    for (int i = 0; i < 37855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37856(int& val) {
    func_in_37856(val);
    const int x = 37856;
    std::cout << 37856;
    for (int i = 0; i < 37856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37856(int& val) {
    const int x = 37856;
    std::cout << 37856;
    for (int i = 0; i < 37856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37857(int& val) {
    func_in_37857(val);
    const int x = 37857;
    std::cout << 37857;
    for (int i = 0; i < 37857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37857(int& val) {
    const int x = 37857;
    std::cout << 37857;
    for (int i = 0; i < 37857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37858(int& val) {
    func_in_37858(val);
    const int x = 37858;
    std::cout << 37858;
    for (int i = 0; i < 37858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37858(int& val) {
    const int x = 37858;
    std::cout << 37858;
    for (int i = 0; i < 37858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37859(int& val) {
    func_in_37859(val);
    const int x = 37859;
    std::cout << 37859;
    for (int i = 0; i < 37859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37859(int& val) {
    const int x = 37859;
    std::cout << 37859;
    for (int i = 0; i < 37859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37860(int& val) {
    func_in_37860(val);
    const int x = 37860;
    std::cout << 37860;
    for (int i = 0; i < 37860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37860(int& val) {
    const int x = 37860;
    std::cout << 37860;
    for (int i = 0; i < 37860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37861(int& val) {
    func_in_37861(val);
    const int x = 37861;
    std::cout << 37861;
    for (int i = 0; i < 37861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37861(int& val) {
    const int x = 37861;
    std::cout << 37861;
    for (int i = 0; i < 37861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37862(int& val) {
    func_in_37862(val);
    const int x = 37862;
    std::cout << 37862;
    for (int i = 0; i < 37862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37862(int& val) {
    const int x = 37862;
    std::cout << 37862;
    for (int i = 0; i < 37862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37863(int& val) {
    func_in_37863(val);
    const int x = 37863;
    std::cout << 37863;
    for (int i = 0; i < 37863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37863(int& val) {
    const int x = 37863;
    std::cout << 37863;
    for (int i = 0; i < 37863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37864(int& val) {
    func_in_37864(val);
    const int x = 37864;
    std::cout << 37864;
    for (int i = 0; i < 37864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37864(int& val) {
    const int x = 37864;
    std::cout << 37864;
    for (int i = 0; i < 37864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37865(int& val) {
    func_in_37865(val);
    const int x = 37865;
    std::cout << 37865;
    for (int i = 0; i < 37865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37865(int& val) {
    const int x = 37865;
    std::cout << 37865;
    for (int i = 0; i < 37865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37866(int& val) {
    func_in_37866(val);
    const int x = 37866;
    std::cout << 37866;
    for (int i = 0; i < 37866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37866(int& val) {
    const int x = 37866;
    std::cout << 37866;
    for (int i = 0; i < 37866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37867(int& val) {
    func_in_37867(val);
    const int x = 37867;
    std::cout << 37867;
    for (int i = 0; i < 37867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37867(int& val) {
    const int x = 37867;
    std::cout << 37867;
    for (int i = 0; i < 37867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37868(int& val) {
    func_in_37868(val);
    const int x = 37868;
    std::cout << 37868;
    for (int i = 0; i < 37868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37868(int& val) {
    const int x = 37868;
    std::cout << 37868;
    for (int i = 0; i < 37868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37869(int& val) {
    func_in_37869(val);
    const int x = 37869;
    std::cout << 37869;
    for (int i = 0; i < 37869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37869(int& val) {
    const int x = 37869;
    std::cout << 37869;
    for (int i = 0; i < 37869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37870(int& val) {
    func_in_37870(val);
    const int x = 37870;
    std::cout << 37870;
    for (int i = 0; i < 37870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37870(int& val) {
    const int x = 37870;
    std::cout << 37870;
    for (int i = 0; i < 37870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37871(int& val) {
    func_in_37871(val);
    const int x = 37871;
    std::cout << 37871;
    for (int i = 0; i < 37871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37871(int& val) {
    const int x = 37871;
    std::cout << 37871;
    for (int i = 0; i < 37871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37872(int& val) {
    func_in_37872(val);
    const int x = 37872;
    std::cout << 37872;
    for (int i = 0; i < 37872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37872(int& val) {
    const int x = 37872;
    std::cout << 37872;
    for (int i = 0; i < 37872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37873(int& val) {
    func_in_37873(val);
    const int x = 37873;
    std::cout << 37873;
    for (int i = 0; i < 37873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37873(int& val) {
    const int x = 37873;
    std::cout << 37873;
    for (int i = 0; i < 37873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37874(int& val) {
    func_in_37874(val);
    const int x = 37874;
    std::cout << 37874;
    for (int i = 0; i < 37874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37874(int& val) {
    const int x = 37874;
    std::cout << 37874;
    for (int i = 0; i < 37874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37875(int& val) {
    func_in_37875(val);
    const int x = 37875;
    std::cout << 37875;
    for (int i = 0; i < 37875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37875(int& val) {
    const int x = 37875;
    std::cout << 37875;
    for (int i = 0; i < 37875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37876(int& val) {
    func_in_37876(val);
    const int x = 37876;
    std::cout << 37876;
    for (int i = 0; i < 37876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37876(int& val) {
    const int x = 37876;
    std::cout << 37876;
    for (int i = 0; i < 37876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37877(int& val) {
    func_in_37877(val);
    const int x = 37877;
    std::cout << 37877;
    for (int i = 0; i < 37877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37877(int& val) {
    const int x = 37877;
    std::cout << 37877;
    for (int i = 0; i < 37877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37878(int& val) {
    func_in_37878(val);
    const int x = 37878;
    std::cout << 37878;
    for (int i = 0; i < 37878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37878(int& val) {
    const int x = 37878;
    std::cout << 37878;
    for (int i = 0; i < 37878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37879(int& val) {
    func_in_37879(val);
    const int x = 37879;
    std::cout << 37879;
    for (int i = 0; i < 37879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37879(int& val) {
    const int x = 37879;
    std::cout << 37879;
    for (int i = 0; i < 37879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37880(int& val) {
    func_in_37880(val);
    const int x = 37880;
    std::cout << 37880;
    for (int i = 0; i < 37880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37880(int& val) {
    const int x = 37880;
    std::cout << 37880;
    for (int i = 0; i < 37880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37881(int& val) {
    func_in_37881(val);
    const int x = 37881;
    std::cout << 37881;
    for (int i = 0; i < 37881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37881(int& val) {
    const int x = 37881;
    std::cout << 37881;
    for (int i = 0; i < 37881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37882(int& val) {
    func_in_37882(val);
    const int x = 37882;
    std::cout << 37882;
    for (int i = 0; i < 37882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37882(int& val) {
    const int x = 37882;
    std::cout << 37882;
    for (int i = 0; i < 37882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37883(int& val) {
    func_in_37883(val);
    const int x = 37883;
    std::cout << 37883;
    for (int i = 0; i < 37883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37883(int& val) {
    const int x = 37883;
    std::cout << 37883;
    for (int i = 0; i < 37883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37884(int& val) {
    func_in_37884(val);
    const int x = 37884;
    std::cout << 37884;
    for (int i = 0; i < 37884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37884(int& val) {
    const int x = 37884;
    std::cout << 37884;
    for (int i = 0; i < 37884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37885(int& val) {
    func_in_37885(val);
    const int x = 37885;
    std::cout << 37885;
    for (int i = 0; i < 37885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37885(int& val) {
    const int x = 37885;
    std::cout << 37885;
    for (int i = 0; i < 37885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37886(int& val) {
    func_in_37886(val);
    const int x = 37886;
    std::cout << 37886;
    for (int i = 0; i < 37886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37886(int& val) {
    const int x = 37886;
    std::cout << 37886;
    for (int i = 0; i < 37886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37887(int& val) {
    func_in_37887(val);
    const int x = 37887;
    std::cout << 37887;
    for (int i = 0; i < 37887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37887(int& val) {
    const int x = 37887;
    std::cout << 37887;
    for (int i = 0; i < 37887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37888(int& val) {
    func_in_37888(val);
    const int x = 37888;
    std::cout << 37888;
    for (int i = 0; i < 37888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37888(int& val) {
    const int x = 37888;
    std::cout << 37888;
    for (int i = 0; i < 37888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37889(int& val) {
    func_in_37889(val);
    const int x = 37889;
    std::cout << 37889;
    for (int i = 0; i < 37889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37889(int& val) {
    const int x = 37889;
    std::cout << 37889;
    for (int i = 0; i < 37889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37890(int& val) {
    func_in_37890(val);
    const int x = 37890;
    std::cout << 37890;
    for (int i = 0; i < 37890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37890(int& val) {
    const int x = 37890;
    std::cout << 37890;
    for (int i = 0; i < 37890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37891(int& val) {
    func_in_37891(val);
    const int x = 37891;
    std::cout << 37891;
    for (int i = 0; i < 37891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37891(int& val) {
    const int x = 37891;
    std::cout << 37891;
    for (int i = 0; i < 37891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37892(int& val) {
    func_in_37892(val);
    const int x = 37892;
    std::cout << 37892;
    for (int i = 0; i < 37892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37892(int& val) {
    const int x = 37892;
    std::cout << 37892;
    for (int i = 0; i < 37892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37893(int& val) {
    func_in_37893(val);
    const int x = 37893;
    std::cout << 37893;
    for (int i = 0; i < 37893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37893(int& val) {
    const int x = 37893;
    std::cout << 37893;
    for (int i = 0; i < 37893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37894(int& val) {
    func_in_37894(val);
    const int x = 37894;
    std::cout << 37894;
    for (int i = 0; i < 37894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37894(int& val) {
    const int x = 37894;
    std::cout << 37894;
    for (int i = 0; i < 37894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37895(int& val) {
    func_in_37895(val);
    const int x = 37895;
    std::cout << 37895;
    for (int i = 0; i < 37895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37895(int& val) {
    const int x = 37895;
    std::cout << 37895;
    for (int i = 0; i < 37895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37896(int& val) {
    func_in_37896(val);
    const int x = 37896;
    std::cout << 37896;
    for (int i = 0; i < 37896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37896(int& val) {
    const int x = 37896;
    std::cout << 37896;
    for (int i = 0; i < 37896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37897(int& val) {
    func_in_37897(val);
    const int x = 37897;
    std::cout << 37897;
    for (int i = 0; i < 37897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37897(int& val) {
    const int x = 37897;
    std::cout << 37897;
    for (int i = 0; i < 37897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37898(int& val) {
    func_in_37898(val);
    const int x = 37898;
    std::cout << 37898;
    for (int i = 0; i < 37898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37898(int& val) {
    const int x = 37898;
    std::cout << 37898;
    for (int i = 0; i < 37898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37899(int& val) {
    func_in_37899(val);
    const int x = 37899;
    std::cout << 37899;
    for (int i = 0; i < 37899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37899(int& val) {
    const int x = 37899;
    std::cout << 37899;
    for (int i = 0; i < 37899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37900(int& val) {
    func_in_37900(val);
    const int x = 37900;
    std::cout << 37900;
    for (int i = 0; i < 37900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37900(int& val) {
    const int x = 37900;
    std::cout << 37900;
    for (int i = 0; i < 37900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37901(int& val) {
    func_in_37901(val);
    const int x = 37901;
    std::cout << 37901;
    for (int i = 0; i < 37901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37901(int& val) {
    const int x = 37901;
    std::cout << 37901;
    for (int i = 0; i < 37901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37902(int& val) {
    func_in_37902(val);
    const int x = 37902;
    std::cout << 37902;
    for (int i = 0; i < 37902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37902(int& val) {
    const int x = 37902;
    std::cout << 37902;
    for (int i = 0; i < 37902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37903(int& val) {
    func_in_37903(val);
    const int x = 37903;
    std::cout << 37903;
    for (int i = 0; i < 37903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37903(int& val) {
    const int x = 37903;
    std::cout << 37903;
    for (int i = 0; i < 37903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37904(int& val) {
    func_in_37904(val);
    const int x = 37904;
    std::cout << 37904;
    for (int i = 0; i < 37904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37904(int& val) {
    const int x = 37904;
    std::cout << 37904;
    for (int i = 0; i < 37904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37905(int& val) {
    func_in_37905(val);
    const int x = 37905;
    std::cout << 37905;
    for (int i = 0; i < 37905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37905(int& val) {
    const int x = 37905;
    std::cout << 37905;
    for (int i = 0; i < 37905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37906(int& val) {
    func_in_37906(val);
    const int x = 37906;
    std::cout << 37906;
    for (int i = 0; i < 37906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37906(int& val) {
    const int x = 37906;
    std::cout << 37906;
    for (int i = 0; i < 37906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37907(int& val) {
    func_in_37907(val);
    const int x = 37907;
    std::cout << 37907;
    for (int i = 0; i < 37907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37907(int& val) {
    const int x = 37907;
    std::cout << 37907;
    for (int i = 0; i < 37907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37908(int& val) {
    func_in_37908(val);
    const int x = 37908;
    std::cout << 37908;
    for (int i = 0; i < 37908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37908(int& val) {
    const int x = 37908;
    std::cout << 37908;
    for (int i = 0; i < 37908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37909(int& val) {
    func_in_37909(val);
    const int x = 37909;
    std::cout << 37909;
    for (int i = 0; i < 37909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37909(int& val) {
    const int x = 37909;
    std::cout << 37909;
    for (int i = 0; i < 37909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37910(int& val) {
    func_in_37910(val);
    const int x = 37910;
    std::cout << 37910;
    for (int i = 0; i < 37910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37910(int& val) {
    const int x = 37910;
    std::cout << 37910;
    for (int i = 0; i < 37910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37911(int& val) {
    func_in_37911(val);
    const int x = 37911;
    std::cout << 37911;
    for (int i = 0; i < 37911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37911(int& val) {
    const int x = 37911;
    std::cout << 37911;
    for (int i = 0; i < 37911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37912(int& val) {
    func_in_37912(val);
    const int x = 37912;
    std::cout << 37912;
    for (int i = 0; i < 37912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37912(int& val) {
    const int x = 37912;
    std::cout << 37912;
    for (int i = 0; i < 37912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37913(int& val) {
    func_in_37913(val);
    const int x = 37913;
    std::cout << 37913;
    for (int i = 0; i < 37913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37913(int& val) {
    const int x = 37913;
    std::cout << 37913;
    for (int i = 0; i < 37913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37914(int& val) {
    func_in_37914(val);
    const int x = 37914;
    std::cout << 37914;
    for (int i = 0; i < 37914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37914(int& val) {
    const int x = 37914;
    std::cout << 37914;
    for (int i = 0; i < 37914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37915(int& val) {
    func_in_37915(val);
    const int x = 37915;
    std::cout << 37915;
    for (int i = 0; i < 37915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37915(int& val) {
    const int x = 37915;
    std::cout << 37915;
    for (int i = 0; i < 37915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37916(int& val) {
    func_in_37916(val);
    const int x = 37916;
    std::cout << 37916;
    for (int i = 0; i < 37916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37916(int& val) {
    const int x = 37916;
    std::cout << 37916;
    for (int i = 0; i < 37916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37917(int& val) {
    func_in_37917(val);
    const int x = 37917;
    std::cout << 37917;
    for (int i = 0; i < 37917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37917(int& val) {
    const int x = 37917;
    std::cout << 37917;
    for (int i = 0; i < 37917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37918(int& val) {
    func_in_37918(val);
    const int x = 37918;
    std::cout << 37918;
    for (int i = 0; i < 37918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37918(int& val) {
    const int x = 37918;
    std::cout << 37918;
    for (int i = 0; i < 37918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37919(int& val) {
    func_in_37919(val);
    const int x = 37919;
    std::cout << 37919;
    for (int i = 0; i < 37919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37919(int& val) {
    const int x = 37919;
    std::cout << 37919;
    for (int i = 0; i < 37919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37920(int& val) {
    func_in_37920(val);
    const int x = 37920;
    std::cout << 37920;
    for (int i = 0; i < 37920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37920(int& val) {
    const int x = 37920;
    std::cout << 37920;
    for (int i = 0; i < 37920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37921(int& val) {
    func_in_37921(val);
    const int x = 37921;
    std::cout << 37921;
    for (int i = 0; i < 37921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37921(int& val) {
    const int x = 37921;
    std::cout << 37921;
    for (int i = 0; i < 37921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37922(int& val) {
    func_in_37922(val);
    const int x = 37922;
    std::cout << 37922;
    for (int i = 0; i < 37922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37922(int& val) {
    const int x = 37922;
    std::cout << 37922;
    for (int i = 0; i < 37922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37923(int& val) {
    func_in_37923(val);
    const int x = 37923;
    std::cout << 37923;
    for (int i = 0; i < 37923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37923(int& val) {
    const int x = 37923;
    std::cout << 37923;
    for (int i = 0; i < 37923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37924(int& val) {
    func_in_37924(val);
    const int x = 37924;
    std::cout << 37924;
    for (int i = 0; i < 37924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37924(int& val) {
    const int x = 37924;
    std::cout << 37924;
    for (int i = 0; i < 37924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37925(int& val) {
    func_in_37925(val);
    const int x = 37925;
    std::cout << 37925;
    for (int i = 0; i < 37925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37925(int& val) {
    const int x = 37925;
    std::cout << 37925;
    for (int i = 0; i < 37925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37926(int& val) {
    func_in_37926(val);
    const int x = 37926;
    std::cout << 37926;
    for (int i = 0; i < 37926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37926(int& val) {
    const int x = 37926;
    std::cout << 37926;
    for (int i = 0; i < 37926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37927(int& val) {
    func_in_37927(val);
    const int x = 37927;
    std::cout << 37927;
    for (int i = 0; i < 37927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37927(int& val) {
    const int x = 37927;
    std::cout << 37927;
    for (int i = 0; i < 37927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37928(int& val) {
    func_in_37928(val);
    const int x = 37928;
    std::cout << 37928;
    for (int i = 0; i < 37928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37928(int& val) {
    const int x = 37928;
    std::cout << 37928;
    for (int i = 0; i < 37928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37929(int& val) {
    func_in_37929(val);
    const int x = 37929;
    std::cout << 37929;
    for (int i = 0; i < 37929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37929(int& val) {
    const int x = 37929;
    std::cout << 37929;
    for (int i = 0; i < 37929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37930(int& val) {
    func_in_37930(val);
    const int x = 37930;
    std::cout << 37930;
    for (int i = 0; i < 37930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37930(int& val) {
    const int x = 37930;
    std::cout << 37930;
    for (int i = 0; i < 37930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37931(int& val) {
    func_in_37931(val);
    const int x = 37931;
    std::cout << 37931;
    for (int i = 0; i < 37931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37931(int& val) {
    const int x = 37931;
    std::cout << 37931;
    for (int i = 0; i < 37931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37932(int& val) {
    func_in_37932(val);
    const int x = 37932;
    std::cout << 37932;
    for (int i = 0; i < 37932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37932(int& val) {
    const int x = 37932;
    std::cout << 37932;
    for (int i = 0; i < 37932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37933(int& val) {
    func_in_37933(val);
    const int x = 37933;
    std::cout << 37933;
    for (int i = 0; i < 37933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37933(int& val) {
    const int x = 37933;
    std::cout << 37933;
    for (int i = 0; i < 37933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37934(int& val) {
    func_in_37934(val);
    const int x = 37934;
    std::cout << 37934;
    for (int i = 0; i < 37934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37934(int& val) {
    const int x = 37934;
    std::cout << 37934;
    for (int i = 0; i < 37934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37935(int& val) {
    func_in_37935(val);
    const int x = 37935;
    std::cout << 37935;
    for (int i = 0; i < 37935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37935(int& val) {
    const int x = 37935;
    std::cout << 37935;
    for (int i = 0; i < 37935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37936(int& val) {
    func_in_37936(val);
    const int x = 37936;
    std::cout << 37936;
    for (int i = 0; i < 37936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37936(int& val) {
    const int x = 37936;
    std::cout << 37936;
    for (int i = 0; i < 37936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37937(int& val) {
    func_in_37937(val);
    const int x = 37937;
    std::cout << 37937;
    for (int i = 0; i < 37937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37937(int& val) {
    const int x = 37937;
    std::cout << 37937;
    for (int i = 0; i < 37937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37938(int& val) {
    func_in_37938(val);
    const int x = 37938;
    std::cout << 37938;
    for (int i = 0; i < 37938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37938(int& val) {
    const int x = 37938;
    std::cout << 37938;
    for (int i = 0; i < 37938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37939(int& val) {
    func_in_37939(val);
    const int x = 37939;
    std::cout << 37939;
    for (int i = 0; i < 37939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37939(int& val) {
    const int x = 37939;
    std::cout << 37939;
    for (int i = 0; i < 37939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37940(int& val) {
    func_in_37940(val);
    const int x = 37940;
    std::cout << 37940;
    for (int i = 0; i < 37940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37940(int& val) {
    const int x = 37940;
    std::cout << 37940;
    for (int i = 0; i < 37940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37941(int& val) {
    func_in_37941(val);
    const int x = 37941;
    std::cout << 37941;
    for (int i = 0; i < 37941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37941(int& val) {
    const int x = 37941;
    std::cout << 37941;
    for (int i = 0; i < 37941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37942(int& val) {
    func_in_37942(val);
    const int x = 37942;
    std::cout << 37942;
    for (int i = 0; i < 37942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37942(int& val) {
    const int x = 37942;
    std::cout << 37942;
    for (int i = 0; i < 37942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37943(int& val) {
    func_in_37943(val);
    const int x = 37943;
    std::cout << 37943;
    for (int i = 0; i < 37943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37943(int& val) {
    const int x = 37943;
    std::cout << 37943;
    for (int i = 0; i < 37943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37944(int& val) {
    func_in_37944(val);
    const int x = 37944;
    std::cout << 37944;
    for (int i = 0; i < 37944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37944(int& val) {
    const int x = 37944;
    std::cout << 37944;
    for (int i = 0; i < 37944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37945(int& val) {
    func_in_37945(val);
    const int x = 37945;
    std::cout << 37945;
    for (int i = 0; i < 37945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37945(int& val) {
    const int x = 37945;
    std::cout << 37945;
    for (int i = 0; i < 37945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37946(int& val) {
    func_in_37946(val);
    const int x = 37946;
    std::cout << 37946;
    for (int i = 0; i < 37946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37946(int& val) {
    const int x = 37946;
    std::cout << 37946;
    for (int i = 0; i < 37946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37947(int& val) {
    func_in_37947(val);
    const int x = 37947;
    std::cout << 37947;
    for (int i = 0; i < 37947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37947(int& val) {
    const int x = 37947;
    std::cout << 37947;
    for (int i = 0; i < 37947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37948(int& val) {
    func_in_37948(val);
    const int x = 37948;
    std::cout << 37948;
    for (int i = 0; i < 37948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37948(int& val) {
    const int x = 37948;
    std::cout << 37948;
    for (int i = 0; i < 37948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37949(int& val) {
    func_in_37949(val);
    const int x = 37949;
    std::cout << 37949;
    for (int i = 0; i < 37949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37949(int& val) {
    const int x = 37949;
    std::cout << 37949;
    for (int i = 0; i < 37949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37950(int& val) {
    func_in_37950(val);
    const int x = 37950;
    std::cout << 37950;
    for (int i = 0; i < 37950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37950(int& val) {
    const int x = 37950;
    std::cout << 37950;
    for (int i = 0; i < 37950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37951(int& val) {
    func_in_37951(val);
    const int x = 37951;
    std::cout << 37951;
    for (int i = 0; i < 37951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37951(int& val) {
    const int x = 37951;
    std::cout << 37951;
    for (int i = 0; i < 37951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37952(int& val) {
    func_in_37952(val);
    const int x = 37952;
    std::cout << 37952;
    for (int i = 0; i < 37952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37952(int& val) {
    const int x = 37952;
    std::cout << 37952;
    for (int i = 0; i < 37952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37953(int& val) {
    func_in_37953(val);
    const int x = 37953;
    std::cout << 37953;
    for (int i = 0; i < 37953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37953(int& val) {
    const int x = 37953;
    std::cout << 37953;
    for (int i = 0; i < 37953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37954(int& val) {
    func_in_37954(val);
    const int x = 37954;
    std::cout << 37954;
    for (int i = 0; i < 37954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37954(int& val) {
    const int x = 37954;
    std::cout << 37954;
    for (int i = 0; i < 37954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37955(int& val) {
    func_in_37955(val);
    const int x = 37955;
    std::cout << 37955;
    for (int i = 0; i < 37955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37955(int& val) {
    const int x = 37955;
    std::cout << 37955;
    for (int i = 0; i < 37955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37956(int& val) {
    func_in_37956(val);
    const int x = 37956;
    std::cout << 37956;
    for (int i = 0; i < 37956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37956(int& val) {
    const int x = 37956;
    std::cout << 37956;
    for (int i = 0; i < 37956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37957(int& val) {
    func_in_37957(val);
    const int x = 37957;
    std::cout << 37957;
    for (int i = 0; i < 37957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37957(int& val) {
    const int x = 37957;
    std::cout << 37957;
    for (int i = 0; i < 37957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37958(int& val) {
    func_in_37958(val);
    const int x = 37958;
    std::cout << 37958;
    for (int i = 0; i < 37958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37958(int& val) {
    const int x = 37958;
    std::cout << 37958;
    for (int i = 0; i < 37958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37959(int& val) {
    func_in_37959(val);
    const int x = 37959;
    std::cout << 37959;
    for (int i = 0; i < 37959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37959(int& val) {
    const int x = 37959;
    std::cout << 37959;
    for (int i = 0; i < 37959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37960(int& val) {
    func_in_37960(val);
    const int x = 37960;
    std::cout << 37960;
    for (int i = 0; i < 37960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37960(int& val) {
    const int x = 37960;
    std::cout << 37960;
    for (int i = 0; i < 37960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37961(int& val) {
    func_in_37961(val);
    const int x = 37961;
    std::cout << 37961;
    for (int i = 0; i < 37961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37961(int& val) {
    const int x = 37961;
    std::cout << 37961;
    for (int i = 0; i < 37961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37962(int& val) {
    func_in_37962(val);
    const int x = 37962;
    std::cout << 37962;
    for (int i = 0; i < 37962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37962(int& val) {
    const int x = 37962;
    std::cout << 37962;
    for (int i = 0; i < 37962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37963(int& val) {
    func_in_37963(val);
    const int x = 37963;
    std::cout << 37963;
    for (int i = 0; i < 37963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37963(int& val) {
    const int x = 37963;
    std::cout << 37963;
    for (int i = 0; i < 37963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37964(int& val) {
    func_in_37964(val);
    const int x = 37964;
    std::cout << 37964;
    for (int i = 0; i < 37964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37964(int& val) {
    const int x = 37964;
    std::cout << 37964;
    for (int i = 0; i < 37964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37965(int& val) {
    func_in_37965(val);
    const int x = 37965;
    std::cout << 37965;
    for (int i = 0; i < 37965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37965(int& val) {
    const int x = 37965;
    std::cout << 37965;
    for (int i = 0; i < 37965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37966(int& val) {
    func_in_37966(val);
    const int x = 37966;
    std::cout << 37966;
    for (int i = 0; i < 37966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37966(int& val) {
    const int x = 37966;
    std::cout << 37966;
    for (int i = 0; i < 37966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37967(int& val) {
    func_in_37967(val);
    const int x = 37967;
    std::cout << 37967;
    for (int i = 0; i < 37967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37967(int& val) {
    const int x = 37967;
    std::cout << 37967;
    for (int i = 0; i < 37967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37968(int& val) {
    func_in_37968(val);
    const int x = 37968;
    std::cout << 37968;
    for (int i = 0; i < 37968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37968(int& val) {
    const int x = 37968;
    std::cout << 37968;
    for (int i = 0; i < 37968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37969(int& val) {
    func_in_37969(val);
    const int x = 37969;
    std::cout << 37969;
    for (int i = 0; i < 37969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37969(int& val) {
    const int x = 37969;
    std::cout << 37969;
    for (int i = 0; i < 37969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37970(int& val) {
    func_in_37970(val);
    const int x = 37970;
    std::cout << 37970;
    for (int i = 0; i < 37970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37970(int& val) {
    const int x = 37970;
    std::cout << 37970;
    for (int i = 0; i < 37970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37971(int& val) {
    func_in_37971(val);
    const int x = 37971;
    std::cout << 37971;
    for (int i = 0; i < 37971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37971(int& val) {
    const int x = 37971;
    std::cout << 37971;
    for (int i = 0; i < 37971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37972(int& val) {
    func_in_37972(val);
    const int x = 37972;
    std::cout << 37972;
    for (int i = 0; i < 37972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37972(int& val) {
    const int x = 37972;
    std::cout << 37972;
    for (int i = 0; i < 37972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37973(int& val) {
    func_in_37973(val);
    const int x = 37973;
    std::cout << 37973;
    for (int i = 0; i < 37973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37973(int& val) {
    const int x = 37973;
    std::cout << 37973;
    for (int i = 0; i < 37973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37974(int& val) {
    func_in_37974(val);
    const int x = 37974;
    std::cout << 37974;
    for (int i = 0; i < 37974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37974(int& val) {
    const int x = 37974;
    std::cout << 37974;
    for (int i = 0; i < 37974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37975(int& val) {
    func_in_37975(val);
    const int x = 37975;
    std::cout << 37975;
    for (int i = 0; i < 37975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37975(int& val) {
    const int x = 37975;
    std::cout << 37975;
    for (int i = 0; i < 37975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37976(int& val) {
    func_in_37976(val);
    const int x = 37976;
    std::cout << 37976;
    for (int i = 0; i < 37976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37976(int& val) {
    const int x = 37976;
    std::cout << 37976;
    for (int i = 0; i < 37976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37977(int& val) {
    func_in_37977(val);
    const int x = 37977;
    std::cout << 37977;
    for (int i = 0; i < 37977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37977(int& val) {
    const int x = 37977;
    std::cout << 37977;
    for (int i = 0; i < 37977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37978(int& val) {
    func_in_37978(val);
    const int x = 37978;
    std::cout << 37978;
    for (int i = 0; i < 37978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37978(int& val) {
    const int x = 37978;
    std::cout << 37978;
    for (int i = 0; i < 37978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37979(int& val) {
    func_in_37979(val);
    const int x = 37979;
    std::cout << 37979;
    for (int i = 0; i < 37979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37979(int& val) {
    const int x = 37979;
    std::cout << 37979;
    for (int i = 0; i < 37979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37980(int& val) {
    func_in_37980(val);
    const int x = 37980;
    std::cout << 37980;
    for (int i = 0; i < 37980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37980(int& val) {
    const int x = 37980;
    std::cout << 37980;
    for (int i = 0; i < 37980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37981(int& val) {
    func_in_37981(val);
    const int x = 37981;
    std::cout << 37981;
    for (int i = 0; i < 37981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37981(int& val) {
    const int x = 37981;
    std::cout << 37981;
    for (int i = 0; i < 37981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37982(int& val) {
    func_in_37982(val);
    const int x = 37982;
    std::cout << 37982;
    for (int i = 0; i < 37982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37982(int& val) {
    const int x = 37982;
    std::cout << 37982;
    for (int i = 0; i < 37982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37983(int& val) {
    func_in_37983(val);
    const int x = 37983;
    std::cout << 37983;
    for (int i = 0; i < 37983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37983(int& val) {
    const int x = 37983;
    std::cout << 37983;
    for (int i = 0; i < 37983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37984(int& val) {
    func_in_37984(val);
    const int x = 37984;
    std::cout << 37984;
    for (int i = 0; i < 37984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37984(int& val) {
    const int x = 37984;
    std::cout << 37984;
    for (int i = 0; i < 37984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37985(int& val) {
    func_in_37985(val);
    const int x = 37985;
    std::cout << 37985;
    for (int i = 0; i < 37985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37985(int& val) {
    const int x = 37985;
    std::cout << 37985;
    for (int i = 0; i < 37985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37986(int& val) {
    func_in_37986(val);
    const int x = 37986;
    std::cout << 37986;
    for (int i = 0; i < 37986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37986(int& val) {
    const int x = 37986;
    std::cout << 37986;
    for (int i = 0; i < 37986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37987(int& val) {
    func_in_37987(val);
    const int x = 37987;
    std::cout << 37987;
    for (int i = 0; i < 37987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37987(int& val) {
    const int x = 37987;
    std::cout << 37987;
    for (int i = 0; i < 37987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37988(int& val) {
    func_in_37988(val);
    const int x = 37988;
    std::cout << 37988;
    for (int i = 0; i < 37988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37988(int& val) {
    const int x = 37988;
    std::cout << 37988;
    for (int i = 0; i < 37988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37989(int& val) {
    func_in_37989(val);
    const int x = 37989;
    std::cout << 37989;
    for (int i = 0; i < 37989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37989(int& val) {
    const int x = 37989;
    std::cout << 37989;
    for (int i = 0; i < 37989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37990(int& val) {
    func_in_37990(val);
    const int x = 37990;
    std::cout << 37990;
    for (int i = 0; i < 37990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37990(int& val) {
    const int x = 37990;
    std::cout << 37990;
    for (int i = 0; i < 37990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37991(int& val) {
    func_in_37991(val);
    const int x = 37991;
    std::cout << 37991;
    for (int i = 0; i < 37991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37991(int& val) {
    const int x = 37991;
    std::cout << 37991;
    for (int i = 0; i < 37991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37992(int& val) {
    func_in_37992(val);
    const int x = 37992;
    std::cout << 37992;
    for (int i = 0; i < 37992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37992(int& val) {
    const int x = 37992;
    std::cout << 37992;
    for (int i = 0; i < 37992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37993(int& val) {
    func_in_37993(val);
    const int x = 37993;
    std::cout << 37993;
    for (int i = 0; i < 37993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37993(int& val) {
    const int x = 37993;
    std::cout << 37993;
    for (int i = 0; i < 37993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37994(int& val) {
    func_in_37994(val);
    const int x = 37994;
    std::cout << 37994;
    for (int i = 0; i < 37994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37994(int& val) {
    const int x = 37994;
    std::cout << 37994;
    for (int i = 0; i < 37994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37995(int& val) {
    func_in_37995(val);
    const int x = 37995;
    std::cout << 37995;
    for (int i = 0; i < 37995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37995(int& val) {
    const int x = 37995;
    std::cout << 37995;
    for (int i = 0; i < 37995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37996(int& val) {
    func_in_37996(val);
    const int x = 37996;
    std::cout << 37996;
    for (int i = 0; i < 37996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37996(int& val) {
    const int x = 37996;
    std::cout << 37996;
    for (int i = 0; i < 37996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37997(int& val) {
    func_in_37997(val);
    const int x = 37997;
    std::cout << 37997;
    for (int i = 0; i < 37997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37997(int& val) {
    const int x = 37997;
    std::cout << 37997;
    for (int i = 0; i < 37997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37998(int& val) {
    func_in_37998(val);
    const int x = 37998;
    std::cout << 37998;
    for (int i = 0; i < 37998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37998(int& val) {
    const int x = 37998;
    std::cout << 37998;
    for (int i = 0; i < 37998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37999(int& val) {
    func_in_37999(val);
    const int x = 37999;
    std::cout << 37999;
    for (int i = 0; i < 37999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37999(int& val) {
    const int x = 37999;
    std::cout << 37999;
    for (int i = 0; i < 37999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38000(int& val) {
    func_in_38000(val);
    const int x = 38000;
    std::cout << 38000;
    for (int i = 0; i < 38000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38000(int& val) {
    const int x = 38000;
    std::cout << 38000;
    for (int i = 0; i < 38000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38001(int& val) {
    func_in_38001(val);
    const int x = 38001;
    std::cout << 38001;
    for (int i = 0; i < 38001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38001(int& val) {
    const int x = 38001;
    std::cout << 38001;
    for (int i = 0; i < 38001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38002(int& val) {
    func_in_38002(val);
    const int x = 38002;
    std::cout << 38002;
    for (int i = 0; i < 38002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38002(int& val) {
    const int x = 38002;
    std::cout << 38002;
    for (int i = 0; i < 38002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38003(int& val) {
    func_in_38003(val);
    const int x = 38003;
    std::cout << 38003;
    for (int i = 0; i < 38003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38003(int& val) {
    const int x = 38003;
    std::cout << 38003;
    for (int i = 0; i < 38003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38004(int& val) {
    func_in_38004(val);
    const int x = 38004;
    std::cout << 38004;
    for (int i = 0; i < 38004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38004(int& val) {
    const int x = 38004;
    std::cout << 38004;
    for (int i = 0; i < 38004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38005(int& val) {
    func_in_38005(val);
    const int x = 38005;
    std::cout << 38005;
    for (int i = 0; i < 38005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38005(int& val) {
    const int x = 38005;
    std::cout << 38005;
    for (int i = 0; i < 38005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38006(int& val) {
    func_in_38006(val);
    const int x = 38006;
    std::cout << 38006;
    for (int i = 0; i < 38006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38006(int& val) {
    const int x = 38006;
    std::cout << 38006;
    for (int i = 0; i < 38006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38007(int& val) {
    func_in_38007(val);
    const int x = 38007;
    std::cout << 38007;
    for (int i = 0; i < 38007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38007(int& val) {
    const int x = 38007;
    std::cout << 38007;
    for (int i = 0; i < 38007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38008(int& val) {
    func_in_38008(val);
    const int x = 38008;
    std::cout << 38008;
    for (int i = 0; i < 38008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38008(int& val) {
    const int x = 38008;
    std::cout << 38008;
    for (int i = 0; i < 38008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38009(int& val) {
    func_in_38009(val);
    const int x = 38009;
    std::cout << 38009;
    for (int i = 0; i < 38009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38009(int& val) {
    const int x = 38009;
    std::cout << 38009;
    for (int i = 0; i < 38009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38010(int& val) {
    func_in_38010(val);
    const int x = 38010;
    std::cout << 38010;
    for (int i = 0; i < 38010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38010(int& val) {
    const int x = 38010;
    std::cout << 38010;
    for (int i = 0; i < 38010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38011(int& val) {
    func_in_38011(val);
    const int x = 38011;
    std::cout << 38011;
    for (int i = 0; i < 38011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38011(int& val) {
    const int x = 38011;
    std::cout << 38011;
    for (int i = 0; i < 38011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38012(int& val) {
    func_in_38012(val);
    const int x = 38012;
    std::cout << 38012;
    for (int i = 0; i < 38012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38012(int& val) {
    const int x = 38012;
    std::cout << 38012;
    for (int i = 0; i < 38012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38013(int& val) {
    func_in_38013(val);
    const int x = 38013;
    std::cout << 38013;
    for (int i = 0; i < 38013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38013(int& val) {
    const int x = 38013;
    std::cout << 38013;
    for (int i = 0; i < 38013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38014(int& val) {
    func_in_38014(val);
    const int x = 38014;
    std::cout << 38014;
    for (int i = 0; i < 38014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38014(int& val) {
    const int x = 38014;
    std::cout << 38014;
    for (int i = 0; i < 38014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38015(int& val) {
    func_in_38015(val);
    const int x = 38015;
    std::cout << 38015;
    for (int i = 0; i < 38015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38015(int& val) {
    const int x = 38015;
    std::cout << 38015;
    for (int i = 0; i < 38015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38016(int& val) {
    func_in_38016(val);
    const int x = 38016;
    std::cout << 38016;
    for (int i = 0; i < 38016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38016(int& val) {
    const int x = 38016;
    std::cout << 38016;
    for (int i = 0; i < 38016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38017(int& val) {
    func_in_38017(val);
    const int x = 38017;
    std::cout << 38017;
    for (int i = 0; i < 38017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38017(int& val) {
    const int x = 38017;
    std::cout << 38017;
    for (int i = 0; i < 38017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38018(int& val) {
    func_in_38018(val);
    const int x = 38018;
    std::cout << 38018;
    for (int i = 0; i < 38018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38018(int& val) {
    const int x = 38018;
    std::cout << 38018;
    for (int i = 0; i < 38018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38019(int& val) {
    func_in_38019(val);
    const int x = 38019;
    std::cout << 38019;
    for (int i = 0; i < 38019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38019(int& val) {
    const int x = 38019;
    std::cout << 38019;
    for (int i = 0; i < 38019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38020(int& val) {
    func_in_38020(val);
    const int x = 38020;
    std::cout << 38020;
    for (int i = 0; i < 38020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38020(int& val) {
    const int x = 38020;
    std::cout << 38020;
    for (int i = 0; i < 38020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38021(int& val) {
    func_in_38021(val);
    const int x = 38021;
    std::cout << 38021;
    for (int i = 0; i < 38021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38021(int& val) {
    const int x = 38021;
    std::cout << 38021;
    for (int i = 0; i < 38021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38022(int& val) {
    func_in_38022(val);
    const int x = 38022;
    std::cout << 38022;
    for (int i = 0; i < 38022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38022(int& val) {
    const int x = 38022;
    std::cout << 38022;
    for (int i = 0; i < 38022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38023(int& val) {
    func_in_38023(val);
    const int x = 38023;
    std::cout << 38023;
    for (int i = 0; i < 38023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38023(int& val) {
    const int x = 38023;
    std::cout << 38023;
    for (int i = 0; i < 38023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38024(int& val) {
    func_in_38024(val);
    const int x = 38024;
    std::cout << 38024;
    for (int i = 0; i < 38024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38024(int& val) {
    const int x = 38024;
    std::cout << 38024;
    for (int i = 0; i < 38024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38025(int& val) {
    func_in_38025(val);
    const int x = 38025;
    std::cout << 38025;
    for (int i = 0; i < 38025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38025(int& val) {
    const int x = 38025;
    std::cout << 38025;
    for (int i = 0; i < 38025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38026(int& val) {
    func_in_38026(val);
    const int x = 38026;
    std::cout << 38026;
    for (int i = 0; i < 38026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38026(int& val) {
    const int x = 38026;
    std::cout << 38026;
    for (int i = 0; i < 38026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38027(int& val) {
    func_in_38027(val);
    const int x = 38027;
    std::cout << 38027;
    for (int i = 0; i < 38027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38027(int& val) {
    const int x = 38027;
    std::cout << 38027;
    for (int i = 0; i < 38027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38028(int& val) {
    func_in_38028(val);
    const int x = 38028;
    std::cout << 38028;
    for (int i = 0; i < 38028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38028(int& val) {
    const int x = 38028;
    std::cout << 38028;
    for (int i = 0; i < 38028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38029(int& val) {
    func_in_38029(val);
    const int x = 38029;
    std::cout << 38029;
    for (int i = 0; i < 38029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38029(int& val) {
    const int x = 38029;
    std::cout << 38029;
    for (int i = 0; i < 38029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38030(int& val) {
    func_in_38030(val);
    const int x = 38030;
    std::cout << 38030;
    for (int i = 0; i < 38030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38030(int& val) {
    const int x = 38030;
    std::cout << 38030;
    for (int i = 0; i < 38030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38031(int& val) {
    func_in_38031(val);
    const int x = 38031;
    std::cout << 38031;
    for (int i = 0; i < 38031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38031(int& val) {
    const int x = 38031;
    std::cout << 38031;
    for (int i = 0; i < 38031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38032(int& val) {
    func_in_38032(val);
    const int x = 38032;
    std::cout << 38032;
    for (int i = 0; i < 38032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38032(int& val) {
    const int x = 38032;
    std::cout << 38032;
    for (int i = 0; i < 38032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38033(int& val) {
    func_in_38033(val);
    const int x = 38033;
    std::cout << 38033;
    for (int i = 0; i < 38033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38033(int& val) {
    const int x = 38033;
    std::cout << 38033;
    for (int i = 0; i < 38033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38034(int& val) {
    func_in_38034(val);
    const int x = 38034;
    std::cout << 38034;
    for (int i = 0; i < 38034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38034(int& val) {
    const int x = 38034;
    std::cout << 38034;
    for (int i = 0; i < 38034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38035(int& val) {
    func_in_38035(val);
    const int x = 38035;
    std::cout << 38035;
    for (int i = 0; i < 38035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38035(int& val) {
    const int x = 38035;
    std::cout << 38035;
    for (int i = 0; i < 38035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38036(int& val) {
    func_in_38036(val);
    const int x = 38036;
    std::cout << 38036;
    for (int i = 0; i < 38036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38036(int& val) {
    const int x = 38036;
    std::cout << 38036;
    for (int i = 0; i < 38036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38037(int& val) {
    func_in_38037(val);
    const int x = 38037;
    std::cout << 38037;
    for (int i = 0; i < 38037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38037(int& val) {
    const int x = 38037;
    std::cout << 38037;
    for (int i = 0; i < 38037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38038(int& val) {
    func_in_38038(val);
    const int x = 38038;
    std::cout << 38038;
    for (int i = 0; i < 38038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38038(int& val) {
    const int x = 38038;
    std::cout << 38038;
    for (int i = 0; i < 38038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38039(int& val) {
    func_in_38039(val);
    const int x = 38039;
    std::cout << 38039;
    for (int i = 0; i < 38039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38039(int& val) {
    const int x = 38039;
    std::cout << 38039;
    for (int i = 0; i < 38039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38040(int& val) {
    func_in_38040(val);
    const int x = 38040;
    std::cout << 38040;
    for (int i = 0; i < 38040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38040(int& val) {
    const int x = 38040;
    std::cout << 38040;
    for (int i = 0; i < 38040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38041(int& val) {
    func_in_38041(val);
    const int x = 38041;
    std::cout << 38041;
    for (int i = 0; i < 38041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38041(int& val) {
    const int x = 38041;
    std::cout << 38041;
    for (int i = 0; i < 38041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38042(int& val) {
    func_in_38042(val);
    const int x = 38042;
    std::cout << 38042;
    for (int i = 0; i < 38042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38042(int& val) {
    const int x = 38042;
    std::cout << 38042;
    for (int i = 0; i < 38042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38043(int& val) {
    func_in_38043(val);
    const int x = 38043;
    std::cout << 38043;
    for (int i = 0; i < 38043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38043(int& val) {
    const int x = 38043;
    std::cout << 38043;
    for (int i = 0; i < 38043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38044(int& val) {
    func_in_38044(val);
    const int x = 38044;
    std::cout << 38044;
    for (int i = 0; i < 38044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38044(int& val) {
    const int x = 38044;
    std::cout << 38044;
    for (int i = 0; i < 38044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38045(int& val) {
    func_in_38045(val);
    const int x = 38045;
    std::cout << 38045;
    for (int i = 0; i < 38045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38045(int& val) {
    const int x = 38045;
    std::cout << 38045;
    for (int i = 0; i < 38045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38046(int& val) {
    func_in_38046(val);
    const int x = 38046;
    std::cout << 38046;
    for (int i = 0; i < 38046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38046(int& val) {
    const int x = 38046;
    std::cout << 38046;
    for (int i = 0; i < 38046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38047(int& val) {
    func_in_38047(val);
    const int x = 38047;
    std::cout << 38047;
    for (int i = 0; i < 38047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38047(int& val) {
    const int x = 38047;
    std::cout << 38047;
    for (int i = 0; i < 38047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38048(int& val) {
    func_in_38048(val);
    const int x = 38048;
    std::cout << 38048;
    for (int i = 0; i < 38048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38048(int& val) {
    const int x = 38048;
    std::cout << 38048;
    for (int i = 0; i < 38048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38049(int& val) {
    func_in_38049(val);
    const int x = 38049;
    std::cout << 38049;
    for (int i = 0; i < 38049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38049(int& val) {
    const int x = 38049;
    std::cout << 38049;
    for (int i = 0; i < 38049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38050(int& val) {
    func_in_38050(val);
    const int x = 38050;
    std::cout << 38050;
    for (int i = 0; i < 38050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38050(int& val) {
    const int x = 38050;
    std::cout << 38050;
    for (int i = 0; i < 38050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38051(int& val) {
    func_in_38051(val);
    const int x = 38051;
    std::cout << 38051;
    for (int i = 0; i < 38051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38051(int& val) {
    const int x = 38051;
    std::cout << 38051;
    for (int i = 0; i < 38051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38052(int& val) {
    func_in_38052(val);
    const int x = 38052;
    std::cout << 38052;
    for (int i = 0; i < 38052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38052(int& val) {
    const int x = 38052;
    std::cout << 38052;
    for (int i = 0; i < 38052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38053(int& val) {
    func_in_38053(val);
    const int x = 38053;
    std::cout << 38053;
    for (int i = 0; i < 38053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38053(int& val) {
    const int x = 38053;
    std::cout << 38053;
    for (int i = 0; i < 38053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38054(int& val) {
    func_in_38054(val);
    const int x = 38054;
    std::cout << 38054;
    for (int i = 0; i < 38054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38054(int& val) {
    const int x = 38054;
    std::cout << 38054;
    for (int i = 0; i < 38054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38055(int& val) {
    func_in_38055(val);
    const int x = 38055;
    std::cout << 38055;
    for (int i = 0; i < 38055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38055(int& val) {
    const int x = 38055;
    std::cout << 38055;
    for (int i = 0; i < 38055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38056(int& val) {
    func_in_38056(val);
    const int x = 38056;
    std::cout << 38056;
    for (int i = 0; i < 38056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38056(int& val) {
    const int x = 38056;
    std::cout << 38056;
    for (int i = 0; i < 38056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38057(int& val) {
    func_in_38057(val);
    const int x = 38057;
    std::cout << 38057;
    for (int i = 0; i < 38057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38057(int& val) {
    const int x = 38057;
    std::cout << 38057;
    for (int i = 0; i < 38057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38058(int& val) {
    func_in_38058(val);
    const int x = 38058;
    std::cout << 38058;
    for (int i = 0; i < 38058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38058(int& val) {
    const int x = 38058;
    std::cout << 38058;
    for (int i = 0; i < 38058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38059(int& val) {
    func_in_38059(val);
    const int x = 38059;
    std::cout << 38059;
    for (int i = 0; i < 38059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38059(int& val) {
    const int x = 38059;
    std::cout << 38059;
    for (int i = 0; i < 38059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38060(int& val) {
    func_in_38060(val);
    const int x = 38060;
    std::cout << 38060;
    for (int i = 0; i < 38060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38060(int& val) {
    const int x = 38060;
    std::cout << 38060;
    for (int i = 0; i < 38060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38061(int& val) {
    func_in_38061(val);
    const int x = 38061;
    std::cout << 38061;
    for (int i = 0; i < 38061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38061(int& val) {
    const int x = 38061;
    std::cout << 38061;
    for (int i = 0; i < 38061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38062(int& val) {
    func_in_38062(val);
    const int x = 38062;
    std::cout << 38062;
    for (int i = 0; i < 38062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38062(int& val) {
    const int x = 38062;
    std::cout << 38062;
    for (int i = 0; i < 38062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38063(int& val) {
    func_in_38063(val);
    const int x = 38063;
    std::cout << 38063;
    for (int i = 0; i < 38063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38063(int& val) {
    const int x = 38063;
    std::cout << 38063;
    for (int i = 0; i < 38063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38064(int& val) {
    func_in_38064(val);
    const int x = 38064;
    std::cout << 38064;
    for (int i = 0; i < 38064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38064(int& val) {
    const int x = 38064;
    std::cout << 38064;
    for (int i = 0; i < 38064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38065(int& val) {
    func_in_38065(val);
    const int x = 38065;
    std::cout << 38065;
    for (int i = 0; i < 38065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38065(int& val) {
    const int x = 38065;
    std::cout << 38065;
    for (int i = 0; i < 38065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38066(int& val) {
    func_in_38066(val);
    const int x = 38066;
    std::cout << 38066;
    for (int i = 0; i < 38066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38066(int& val) {
    const int x = 38066;
    std::cout << 38066;
    for (int i = 0; i < 38066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38067(int& val) {
    func_in_38067(val);
    const int x = 38067;
    std::cout << 38067;
    for (int i = 0; i < 38067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38067(int& val) {
    const int x = 38067;
    std::cout << 38067;
    for (int i = 0; i < 38067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38068(int& val) {
    func_in_38068(val);
    const int x = 38068;
    std::cout << 38068;
    for (int i = 0; i < 38068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38068(int& val) {
    const int x = 38068;
    std::cout << 38068;
    for (int i = 0; i < 38068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38069(int& val) {
    func_in_38069(val);
    const int x = 38069;
    std::cout << 38069;
    for (int i = 0; i < 38069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38069(int& val) {
    const int x = 38069;
    std::cout << 38069;
    for (int i = 0; i < 38069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38070(int& val) {
    func_in_38070(val);
    const int x = 38070;
    std::cout << 38070;
    for (int i = 0; i < 38070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38070(int& val) {
    const int x = 38070;
    std::cout << 38070;
    for (int i = 0; i < 38070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38071(int& val) {
    func_in_38071(val);
    const int x = 38071;
    std::cout << 38071;
    for (int i = 0; i < 38071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38071(int& val) {
    const int x = 38071;
    std::cout << 38071;
    for (int i = 0; i < 38071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38072(int& val) {
    func_in_38072(val);
    const int x = 38072;
    std::cout << 38072;
    for (int i = 0; i < 38072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38072(int& val) {
    const int x = 38072;
    std::cout << 38072;
    for (int i = 0; i < 38072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38073(int& val) {
    func_in_38073(val);
    const int x = 38073;
    std::cout << 38073;
    for (int i = 0; i < 38073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38073(int& val) {
    const int x = 38073;
    std::cout << 38073;
    for (int i = 0; i < 38073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38074(int& val) {
    func_in_38074(val);
    const int x = 38074;
    std::cout << 38074;
    for (int i = 0; i < 38074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38074(int& val) {
    const int x = 38074;
    std::cout << 38074;
    for (int i = 0; i < 38074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38075(int& val) {
    func_in_38075(val);
    const int x = 38075;
    std::cout << 38075;
    for (int i = 0; i < 38075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38075(int& val) {
    const int x = 38075;
    std::cout << 38075;
    for (int i = 0; i < 38075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38076(int& val) {
    func_in_38076(val);
    const int x = 38076;
    std::cout << 38076;
    for (int i = 0; i < 38076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38076(int& val) {
    const int x = 38076;
    std::cout << 38076;
    for (int i = 0; i < 38076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38077(int& val) {
    func_in_38077(val);
    const int x = 38077;
    std::cout << 38077;
    for (int i = 0; i < 38077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38077(int& val) {
    const int x = 38077;
    std::cout << 38077;
    for (int i = 0; i < 38077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38078(int& val) {
    func_in_38078(val);
    const int x = 38078;
    std::cout << 38078;
    for (int i = 0; i < 38078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38078(int& val) {
    const int x = 38078;
    std::cout << 38078;
    for (int i = 0; i < 38078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38079(int& val) {
    func_in_38079(val);
    const int x = 38079;
    std::cout << 38079;
    for (int i = 0; i < 38079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38079(int& val) {
    const int x = 38079;
    std::cout << 38079;
    for (int i = 0; i < 38079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38080(int& val) {
    func_in_38080(val);
    const int x = 38080;
    std::cout << 38080;
    for (int i = 0; i < 38080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38080(int& val) {
    const int x = 38080;
    std::cout << 38080;
    for (int i = 0; i < 38080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38081(int& val) {
    func_in_38081(val);
    const int x = 38081;
    std::cout << 38081;
    for (int i = 0; i < 38081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38081(int& val) {
    const int x = 38081;
    std::cout << 38081;
    for (int i = 0; i < 38081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38082(int& val) {
    func_in_38082(val);
    const int x = 38082;
    std::cout << 38082;
    for (int i = 0; i < 38082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38082(int& val) {
    const int x = 38082;
    std::cout << 38082;
    for (int i = 0; i < 38082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38083(int& val) {
    func_in_38083(val);
    const int x = 38083;
    std::cout << 38083;
    for (int i = 0; i < 38083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38083(int& val) {
    const int x = 38083;
    std::cout << 38083;
    for (int i = 0; i < 38083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38084(int& val) {
    func_in_38084(val);
    const int x = 38084;
    std::cout << 38084;
    for (int i = 0; i < 38084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38084(int& val) {
    const int x = 38084;
    std::cout << 38084;
    for (int i = 0; i < 38084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38085(int& val) {
    func_in_38085(val);
    const int x = 38085;
    std::cout << 38085;
    for (int i = 0; i < 38085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38085(int& val) {
    const int x = 38085;
    std::cout << 38085;
    for (int i = 0; i < 38085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38086(int& val) {
    func_in_38086(val);
    const int x = 38086;
    std::cout << 38086;
    for (int i = 0; i < 38086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38086(int& val) {
    const int x = 38086;
    std::cout << 38086;
    for (int i = 0; i < 38086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38087(int& val) {
    func_in_38087(val);
    const int x = 38087;
    std::cout << 38087;
    for (int i = 0; i < 38087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38087(int& val) {
    const int x = 38087;
    std::cout << 38087;
    for (int i = 0; i < 38087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38088(int& val) {
    func_in_38088(val);
    const int x = 38088;
    std::cout << 38088;
    for (int i = 0; i < 38088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38088(int& val) {
    const int x = 38088;
    std::cout << 38088;
    for (int i = 0; i < 38088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38089(int& val) {
    func_in_38089(val);
    const int x = 38089;
    std::cout << 38089;
    for (int i = 0; i < 38089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38089(int& val) {
    const int x = 38089;
    std::cout << 38089;
    for (int i = 0; i < 38089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38090(int& val) {
    func_in_38090(val);
    const int x = 38090;
    std::cout << 38090;
    for (int i = 0; i < 38090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38090(int& val) {
    const int x = 38090;
    std::cout << 38090;
    for (int i = 0; i < 38090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38091(int& val) {
    func_in_38091(val);
    const int x = 38091;
    std::cout << 38091;
    for (int i = 0; i < 38091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38091(int& val) {
    const int x = 38091;
    std::cout << 38091;
    for (int i = 0; i < 38091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38092(int& val) {
    func_in_38092(val);
    const int x = 38092;
    std::cout << 38092;
    for (int i = 0; i < 38092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38092(int& val) {
    const int x = 38092;
    std::cout << 38092;
    for (int i = 0; i < 38092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38093(int& val) {
    func_in_38093(val);
    const int x = 38093;
    std::cout << 38093;
    for (int i = 0; i < 38093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38093(int& val) {
    const int x = 38093;
    std::cout << 38093;
    for (int i = 0; i < 38093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38094(int& val) {
    func_in_38094(val);
    const int x = 38094;
    std::cout << 38094;
    for (int i = 0; i < 38094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38094(int& val) {
    const int x = 38094;
    std::cout << 38094;
    for (int i = 0; i < 38094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38095(int& val) {
    func_in_38095(val);
    const int x = 38095;
    std::cout << 38095;
    for (int i = 0; i < 38095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38095(int& val) {
    const int x = 38095;
    std::cout << 38095;
    for (int i = 0; i < 38095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38096(int& val) {
    func_in_38096(val);
    const int x = 38096;
    std::cout << 38096;
    for (int i = 0; i < 38096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38096(int& val) {
    const int x = 38096;
    std::cout << 38096;
    for (int i = 0; i < 38096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38097(int& val) {
    func_in_38097(val);
    const int x = 38097;
    std::cout << 38097;
    for (int i = 0; i < 38097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38097(int& val) {
    const int x = 38097;
    std::cout << 38097;
    for (int i = 0; i < 38097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38098(int& val) {
    func_in_38098(val);
    const int x = 38098;
    std::cout << 38098;
    for (int i = 0; i < 38098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38098(int& val) {
    const int x = 38098;
    std::cout << 38098;
    for (int i = 0; i < 38098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38099(int& val) {
    func_in_38099(val);
    const int x = 38099;
    std::cout << 38099;
    for (int i = 0; i < 38099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38099(int& val) {
    const int x = 38099;
    std::cout << 38099;
    for (int i = 0; i < 38099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38100(int& val) {
    func_in_38100(val);
    const int x = 38100;
    std::cout << 38100;
    for (int i = 0; i < 38100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38100(int& val) {
    const int x = 38100;
    std::cout << 38100;
    for (int i = 0; i < 38100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38101(int& val) {
    func_in_38101(val);
    const int x = 38101;
    std::cout << 38101;
    for (int i = 0; i < 38101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38101(int& val) {
    const int x = 38101;
    std::cout << 38101;
    for (int i = 0; i < 38101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38102(int& val) {
    func_in_38102(val);
    const int x = 38102;
    std::cout << 38102;
    for (int i = 0; i < 38102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38102(int& val) {
    const int x = 38102;
    std::cout << 38102;
    for (int i = 0; i < 38102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38103(int& val) {
    func_in_38103(val);
    const int x = 38103;
    std::cout << 38103;
    for (int i = 0; i < 38103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38103(int& val) {
    const int x = 38103;
    std::cout << 38103;
    for (int i = 0; i < 38103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38104(int& val) {
    func_in_38104(val);
    const int x = 38104;
    std::cout << 38104;
    for (int i = 0; i < 38104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38104(int& val) {
    const int x = 38104;
    std::cout << 38104;
    for (int i = 0; i < 38104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38105(int& val) {
    func_in_38105(val);
    const int x = 38105;
    std::cout << 38105;
    for (int i = 0; i < 38105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38105(int& val) {
    const int x = 38105;
    std::cout << 38105;
    for (int i = 0; i < 38105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38106(int& val) {
    func_in_38106(val);
    const int x = 38106;
    std::cout << 38106;
    for (int i = 0; i < 38106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38106(int& val) {
    const int x = 38106;
    std::cout << 38106;
    for (int i = 0; i < 38106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38107(int& val) {
    func_in_38107(val);
    const int x = 38107;
    std::cout << 38107;
    for (int i = 0; i < 38107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38107(int& val) {
    const int x = 38107;
    std::cout << 38107;
    for (int i = 0; i < 38107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38108(int& val) {
    func_in_38108(val);
    const int x = 38108;
    std::cout << 38108;
    for (int i = 0; i < 38108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38108(int& val) {
    const int x = 38108;
    std::cout << 38108;
    for (int i = 0; i < 38108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38109(int& val) {
    func_in_38109(val);
    const int x = 38109;
    std::cout << 38109;
    for (int i = 0; i < 38109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38109(int& val) {
    const int x = 38109;
    std::cout << 38109;
    for (int i = 0; i < 38109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38110(int& val) {
    func_in_38110(val);
    const int x = 38110;
    std::cout << 38110;
    for (int i = 0; i < 38110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38110(int& val) {
    const int x = 38110;
    std::cout << 38110;
    for (int i = 0; i < 38110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38111(int& val) {
    func_in_38111(val);
    const int x = 38111;
    std::cout << 38111;
    for (int i = 0; i < 38111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38111(int& val) {
    const int x = 38111;
    std::cout << 38111;
    for (int i = 0; i < 38111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38112(int& val) {
    func_in_38112(val);
    const int x = 38112;
    std::cout << 38112;
    for (int i = 0; i < 38112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38112(int& val) {
    const int x = 38112;
    std::cout << 38112;
    for (int i = 0; i < 38112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38113(int& val) {
    func_in_38113(val);
    const int x = 38113;
    std::cout << 38113;
    for (int i = 0; i < 38113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38113(int& val) {
    const int x = 38113;
    std::cout << 38113;
    for (int i = 0; i < 38113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38114(int& val) {
    func_in_38114(val);
    const int x = 38114;
    std::cout << 38114;
    for (int i = 0; i < 38114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38114(int& val) {
    const int x = 38114;
    std::cout << 38114;
    for (int i = 0; i < 38114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38115(int& val) {
    func_in_38115(val);
    const int x = 38115;
    std::cout << 38115;
    for (int i = 0; i < 38115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38115(int& val) {
    const int x = 38115;
    std::cout << 38115;
    for (int i = 0; i < 38115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38116(int& val) {
    func_in_38116(val);
    const int x = 38116;
    std::cout << 38116;
    for (int i = 0; i < 38116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38116(int& val) {
    const int x = 38116;
    std::cout << 38116;
    for (int i = 0; i < 38116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38117(int& val) {
    func_in_38117(val);
    const int x = 38117;
    std::cout << 38117;
    for (int i = 0; i < 38117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38117(int& val) {
    const int x = 38117;
    std::cout << 38117;
    for (int i = 0; i < 38117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38118(int& val) {
    func_in_38118(val);
    const int x = 38118;
    std::cout << 38118;
    for (int i = 0; i < 38118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38118(int& val) {
    const int x = 38118;
    std::cout << 38118;
    for (int i = 0; i < 38118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38119(int& val) {
    func_in_38119(val);
    const int x = 38119;
    std::cout << 38119;
    for (int i = 0; i < 38119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38119(int& val) {
    const int x = 38119;
    std::cout << 38119;
    for (int i = 0; i < 38119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38120(int& val) {
    func_in_38120(val);
    const int x = 38120;
    std::cout << 38120;
    for (int i = 0; i < 38120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38120(int& val) {
    const int x = 38120;
    std::cout << 38120;
    for (int i = 0; i < 38120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38121(int& val) {
    func_in_38121(val);
    const int x = 38121;
    std::cout << 38121;
    for (int i = 0; i < 38121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38121(int& val) {
    const int x = 38121;
    std::cout << 38121;
    for (int i = 0; i < 38121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38122(int& val) {
    func_in_38122(val);
    const int x = 38122;
    std::cout << 38122;
    for (int i = 0; i < 38122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38122(int& val) {
    const int x = 38122;
    std::cout << 38122;
    for (int i = 0; i < 38122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38123(int& val) {
    func_in_38123(val);
    const int x = 38123;
    std::cout << 38123;
    for (int i = 0; i < 38123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38123(int& val) {
    const int x = 38123;
    std::cout << 38123;
    for (int i = 0; i < 38123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38124(int& val) {
    func_in_38124(val);
    const int x = 38124;
    std::cout << 38124;
    for (int i = 0; i < 38124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38124(int& val) {
    const int x = 38124;
    std::cout << 38124;
    for (int i = 0; i < 38124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38125(int& val) {
    func_in_38125(val);
    const int x = 38125;
    std::cout << 38125;
    for (int i = 0; i < 38125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38125(int& val) {
    const int x = 38125;
    std::cout << 38125;
    for (int i = 0; i < 38125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38126(int& val) {
    func_in_38126(val);
    const int x = 38126;
    std::cout << 38126;
    for (int i = 0; i < 38126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38126(int& val) {
    const int x = 38126;
    std::cout << 38126;
    for (int i = 0; i < 38126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38127(int& val) {
    func_in_38127(val);
    const int x = 38127;
    std::cout << 38127;
    for (int i = 0; i < 38127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38127(int& val) {
    const int x = 38127;
    std::cout << 38127;
    for (int i = 0; i < 38127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38128(int& val) {
    func_in_38128(val);
    const int x = 38128;
    std::cout << 38128;
    for (int i = 0; i < 38128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38128(int& val) {
    const int x = 38128;
    std::cout << 38128;
    for (int i = 0; i < 38128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38129(int& val) {
    func_in_38129(val);
    const int x = 38129;
    std::cout << 38129;
    for (int i = 0; i < 38129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38129(int& val) {
    const int x = 38129;
    std::cout << 38129;
    for (int i = 0; i < 38129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38130(int& val) {
    func_in_38130(val);
    const int x = 38130;
    std::cout << 38130;
    for (int i = 0; i < 38130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38130(int& val) {
    const int x = 38130;
    std::cout << 38130;
    for (int i = 0; i < 38130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38131(int& val) {
    func_in_38131(val);
    const int x = 38131;
    std::cout << 38131;
    for (int i = 0; i < 38131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38131(int& val) {
    const int x = 38131;
    std::cout << 38131;
    for (int i = 0; i < 38131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38132(int& val) {
    func_in_38132(val);
    const int x = 38132;
    std::cout << 38132;
    for (int i = 0; i < 38132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38132(int& val) {
    const int x = 38132;
    std::cout << 38132;
    for (int i = 0; i < 38132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38133(int& val) {
    func_in_38133(val);
    const int x = 38133;
    std::cout << 38133;
    for (int i = 0; i < 38133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38133(int& val) {
    const int x = 38133;
    std::cout << 38133;
    for (int i = 0; i < 38133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38134(int& val) {
    func_in_38134(val);
    const int x = 38134;
    std::cout << 38134;
    for (int i = 0; i < 38134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38134(int& val) {
    const int x = 38134;
    std::cout << 38134;
    for (int i = 0; i < 38134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38135(int& val) {
    func_in_38135(val);
    const int x = 38135;
    std::cout << 38135;
    for (int i = 0; i < 38135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38135(int& val) {
    const int x = 38135;
    std::cout << 38135;
    for (int i = 0; i < 38135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38136(int& val) {
    func_in_38136(val);
    const int x = 38136;
    std::cout << 38136;
    for (int i = 0; i < 38136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38136(int& val) {
    const int x = 38136;
    std::cout << 38136;
    for (int i = 0; i < 38136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38137(int& val) {
    func_in_38137(val);
    const int x = 38137;
    std::cout << 38137;
    for (int i = 0; i < 38137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38137(int& val) {
    const int x = 38137;
    std::cout << 38137;
    for (int i = 0; i < 38137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38138(int& val) {
    func_in_38138(val);
    const int x = 38138;
    std::cout << 38138;
    for (int i = 0; i < 38138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38138(int& val) {
    const int x = 38138;
    std::cout << 38138;
    for (int i = 0; i < 38138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38139(int& val) {
    func_in_38139(val);
    const int x = 38139;
    std::cout << 38139;
    for (int i = 0; i < 38139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38139(int& val) {
    const int x = 38139;
    std::cout << 38139;
    for (int i = 0; i < 38139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38140(int& val) {
    func_in_38140(val);
    const int x = 38140;
    std::cout << 38140;
    for (int i = 0; i < 38140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38140(int& val) {
    const int x = 38140;
    std::cout << 38140;
    for (int i = 0; i < 38140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38141(int& val) {
    func_in_38141(val);
    const int x = 38141;
    std::cout << 38141;
    for (int i = 0; i < 38141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38141(int& val) {
    const int x = 38141;
    std::cout << 38141;
    for (int i = 0; i < 38141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38142(int& val) {
    func_in_38142(val);
    const int x = 38142;
    std::cout << 38142;
    for (int i = 0; i < 38142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38142(int& val) {
    const int x = 38142;
    std::cout << 38142;
    for (int i = 0; i < 38142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38143(int& val) {
    func_in_38143(val);
    const int x = 38143;
    std::cout << 38143;
    for (int i = 0; i < 38143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38143(int& val) {
    const int x = 38143;
    std::cout << 38143;
    for (int i = 0; i < 38143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38144(int& val) {
    func_in_38144(val);
    const int x = 38144;
    std::cout << 38144;
    for (int i = 0; i < 38144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38144(int& val) {
    const int x = 38144;
    std::cout << 38144;
    for (int i = 0; i < 38144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38145(int& val) {
    func_in_38145(val);
    const int x = 38145;
    std::cout << 38145;
    for (int i = 0; i < 38145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38145(int& val) {
    const int x = 38145;
    std::cout << 38145;
    for (int i = 0; i < 38145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38146(int& val) {
    func_in_38146(val);
    const int x = 38146;
    std::cout << 38146;
    for (int i = 0; i < 38146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38146(int& val) {
    const int x = 38146;
    std::cout << 38146;
    for (int i = 0; i < 38146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38147(int& val) {
    func_in_38147(val);
    const int x = 38147;
    std::cout << 38147;
    for (int i = 0; i < 38147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38147(int& val) {
    const int x = 38147;
    std::cout << 38147;
    for (int i = 0; i < 38147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38148(int& val) {
    func_in_38148(val);
    const int x = 38148;
    std::cout << 38148;
    for (int i = 0; i < 38148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38148(int& val) {
    const int x = 38148;
    std::cout << 38148;
    for (int i = 0; i < 38148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38149(int& val) {
    func_in_38149(val);
    const int x = 38149;
    std::cout << 38149;
    for (int i = 0; i < 38149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38149(int& val) {
    const int x = 38149;
    std::cout << 38149;
    for (int i = 0; i < 38149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38150(int& val) {
    func_in_38150(val);
    const int x = 38150;
    std::cout << 38150;
    for (int i = 0; i < 38150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38150(int& val) {
    const int x = 38150;
    std::cout << 38150;
    for (int i = 0; i < 38150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38151(int& val) {
    func_in_38151(val);
    const int x = 38151;
    std::cout << 38151;
    for (int i = 0; i < 38151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38151(int& val) {
    const int x = 38151;
    std::cout << 38151;
    for (int i = 0; i < 38151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38152(int& val) {
    func_in_38152(val);
    const int x = 38152;
    std::cout << 38152;
    for (int i = 0; i < 38152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38152(int& val) {
    const int x = 38152;
    std::cout << 38152;
    for (int i = 0; i < 38152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38153(int& val) {
    func_in_38153(val);
    const int x = 38153;
    std::cout << 38153;
    for (int i = 0; i < 38153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38153(int& val) {
    const int x = 38153;
    std::cout << 38153;
    for (int i = 0; i < 38153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38154(int& val) {
    func_in_38154(val);
    const int x = 38154;
    std::cout << 38154;
    for (int i = 0; i < 38154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38154(int& val) {
    const int x = 38154;
    std::cout << 38154;
    for (int i = 0; i < 38154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38155(int& val) {
    func_in_38155(val);
    const int x = 38155;
    std::cout << 38155;
    for (int i = 0; i < 38155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38155(int& val) {
    const int x = 38155;
    std::cout << 38155;
    for (int i = 0; i < 38155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38156(int& val) {
    func_in_38156(val);
    const int x = 38156;
    std::cout << 38156;
    for (int i = 0; i < 38156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38156(int& val) {
    const int x = 38156;
    std::cout << 38156;
    for (int i = 0; i < 38156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38157(int& val) {
    func_in_38157(val);
    const int x = 38157;
    std::cout << 38157;
    for (int i = 0; i < 38157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38157(int& val) {
    const int x = 38157;
    std::cout << 38157;
    for (int i = 0; i < 38157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38158(int& val) {
    func_in_38158(val);
    const int x = 38158;
    std::cout << 38158;
    for (int i = 0; i < 38158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38158(int& val) {
    const int x = 38158;
    std::cout << 38158;
    for (int i = 0; i < 38158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38159(int& val) {
    func_in_38159(val);
    const int x = 38159;
    std::cout << 38159;
    for (int i = 0; i < 38159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38159(int& val) {
    const int x = 38159;
    std::cout << 38159;
    for (int i = 0; i < 38159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38160(int& val) {
    func_in_38160(val);
    const int x = 38160;
    std::cout << 38160;
    for (int i = 0; i < 38160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38160(int& val) {
    const int x = 38160;
    std::cout << 38160;
    for (int i = 0; i < 38160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38161(int& val) {
    func_in_38161(val);
    const int x = 38161;
    std::cout << 38161;
    for (int i = 0; i < 38161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38161(int& val) {
    const int x = 38161;
    std::cout << 38161;
    for (int i = 0; i < 38161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38162(int& val) {
    func_in_38162(val);
    const int x = 38162;
    std::cout << 38162;
    for (int i = 0; i < 38162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38162(int& val) {
    const int x = 38162;
    std::cout << 38162;
    for (int i = 0; i < 38162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38163(int& val) {
    func_in_38163(val);
    const int x = 38163;
    std::cout << 38163;
    for (int i = 0; i < 38163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38163(int& val) {
    const int x = 38163;
    std::cout << 38163;
    for (int i = 0; i < 38163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38164(int& val) {
    func_in_38164(val);
    const int x = 38164;
    std::cout << 38164;
    for (int i = 0; i < 38164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38164(int& val) {
    const int x = 38164;
    std::cout << 38164;
    for (int i = 0; i < 38164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38165(int& val) {
    func_in_38165(val);
    const int x = 38165;
    std::cout << 38165;
    for (int i = 0; i < 38165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38165(int& val) {
    const int x = 38165;
    std::cout << 38165;
    for (int i = 0; i < 38165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38166(int& val) {
    func_in_38166(val);
    const int x = 38166;
    std::cout << 38166;
    for (int i = 0; i < 38166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38166(int& val) {
    const int x = 38166;
    std::cout << 38166;
    for (int i = 0; i < 38166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38167(int& val) {
    func_in_38167(val);
    const int x = 38167;
    std::cout << 38167;
    for (int i = 0; i < 38167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38167(int& val) {
    const int x = 38167;
    std::cout << 38167;
    for (int i = 0; i < 38167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38168(int& val) {
    func_in_38168(val);
    const int x = 38168;
    std::cout << 38168;
    for (int i = 0; i < 38168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38168(int& val) {
    const int x = 38168;
    std::cout << 38168;
    for (int i = 0; i < 38168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38169(int& val) {
    func_in_38169(val);
    const int x = 38169;
    std::cout << 38169;
    for (int i = 0; i < 38169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38169(int& val) {
    const int x = 38169;
    std::cout << 38169;
    for (int i = 0; i < 38169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38170(int& val) {
    func_in_38170(val);
    const int x = 38170;
    std::cout << 38170;
    for (int i = 0; i < 38170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38170(int& val) {
    const int x = 38170;
    std::cout << 38170;
    for (int i = 0; i < 38170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38171(int& val) {
    func_in_38171(val);
    const int x = 38171;
    std::cout << 38171;
    for (int i = 0; i < 38171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38171(int& val) {
    const int x = 38171;
    std::cout << 38171;
    for (int i = 0; i < 38171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38172(int& val) {
    func_in_38172(val);
    const int x = 38172;
    std::cout << 38172;
    for (int i = 0; i < 38172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38172(int& val) {
    const int x = 38172;
    std::cout << 38172;
    for (int i = 0; i < 38172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38173(int& val) {
    func_in_38173(val);
    const int x = 38173;
    std::cout << 38173;
    for (int i = 0; i < 38173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38173(int& val) {
    const int x = 38173;
    std::cout << 38173;
    for (int i = 0; i < 38173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38174(int& val) {
    func_in_38174(val);
    const int x = 38174;
    std::cout << 38174;
    for (int i = 0; i < 38174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38174(int& val) {
    const int x = 38174;
    std::cout << 38174;
    for (int i = 0; i < 38174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38175(int& val) {
    func_in_38175(val);
    const int x = 38175;
    std::cout << 38175;
    for (int i = 0; i < 38175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38175(int& val) {
    const int x = 38175;
    std::cout << 38175;
    for (int i = 0; i < 38175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38176(int& val) {
    func_in_38176(val);
    const int x = 38176;
    std::cout << 38176;
    for (int i = 0; i < 38176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38176(int& val) {
    const int x = 38176;
    std::cout << 38176;
    for (int i = 0; i < 38176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38177(int& val) {
    func_in_38177(val);
    const int x = 38177;
    std::cout << 38177;
    for (int i = 0; i < 38177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38177(int& val) {
    const int x = 38177;
    std::cout << 38177;
    for (int i = 0; i < 38177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38178(int& val) {
    func_in_38178(val);
    const int x = 38178;
    std::cout << 38178;
    for (int i = 0; i < 38178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38178(int& val) {
    const int x = 38178;
    std::cout << 38178;
    for (int i = 0; i < 38178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38179(int& val) {
    func_in_38179(val);
    const int x = 38179;
    std::cout << 38179;
    for (int i = 0; i < 38179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38179(int& val) {
    const int x = 38179;
    std::cout << 38179;
    for (int i = 0; i < 38179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38180(int& val) {
    func_in_38180(val);
    const int x = 38180;
    std::cout << 38180;
    for (int i = 0; i < 38180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38180(int& val) {
    const int x = 38180;
    std::cout << 38180;
    for (int i = 0; i < 38180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38181(int& val) {
    func_in_38181(val);
    const int x = 38181;
    std::cout << 38181;
    for (int i = 0; i < 38181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38181(int& val) {
    const int x = 38181;
    std::cout << 38181;
    for (int i = 0; i < 38181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38182(int& val) {
    func_in_38182(val);
    const int x = 38182;
    std::cout << 38182;
    for (int i = 0; i < 38182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38182(int& val) {
    const int x = 38182;
    std::cout << 38182;
    for (int i = 0; i < 38182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38183(int& val) {
    func_in_38183(val);
    const int x = 38183;
    std::cout << 38183;
    for (int i = 0; i < 38183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38183(int& val) {
    const int x = 38183;
    std::cout << 38183;
    for (int i = 0; i < 38183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38184(int& val) {
    func_in_38184(val);
    const int x = 38184;
    std::cout << 38184;
    for (int i = 0; i < 38184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38184(int& val) {
    const int x = 38184;
    std::cout << 38184;
    for (int i = 0; i < 38184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38185(int& val) {
    func_in_38185(val);
    const int x = 38185;
    std::cout << 38185;
    for (int i = 0; i < 38185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38185(int& val) {
    const int x = 38185;
    std::cout << 38185;
    for (int i = 0; i < 38185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38186(int& val) {
    func_in_38186(val);
    const int x = 38186;
    std::cout << 38186;
    for (int i = 0; i < 38186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38186(int& val) {
    const int x = 38186;
    std::cout << 38186;
    for (int i = 0; i < 38186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38187(int& val) {
    func_in_38187(val);
    const int x = 38187;
    std::cout << 38187;
    for (int i = 0; i < 38187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38187(int& val) {
    const int x = 38187;
    std::cout << 38187;
    for (int i = 0; i < 38187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38188(int& val) {
    func_in_38188(val);
    const int x = 38188;
    std::cout << 38188;
    for (int i = 0; i < 38188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38188(int& val) {
    const int x = 38188;
    std::cout << 38188;
    for (int i = 0; i < 38188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38189(int& val) {
    func_in_38189(val);
    const int x = 38189;
    std::cout << 38189;
    for (int i = 0; i < 38189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38189(int& val) {
    const int x = 38189;
    std::cout << 38189;
    for (int i = 0; i < 38189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38190(int& val) {
    func_in_38190(val);
    const int x = 38190;
    std::cout << 38190;
    for (int i = 0; i < 38190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38190(int& val) {
    const int x = 38190;
    std::cout << 38190;
    for (int i = 0; i < 38190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38191(int& val) {
    func_in_38191(val);
    const int x = 38191;
    std::cout << 38191;
    for (int i = 0; i < 38191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38191(int& val) {
    const int x = 38191;
    std::cout << 38191;
    for (int i = 0; i < 38191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38192(int& val) {
    func_in_38192(val);
    const int x = 38192;
    std::cout << 38192;
    for (int i = 0; i < 38192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38192(int& val) {
    const int x = 38192;
    std::cout << 38192;
    for (int i = 0; i < 38192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38193(int& val) {
    func_in_38193(val);
    const int x = 38193;
    std::cout << 38193;
    for (int i = 0; i < 38193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38193(int& val) {
    const int x = 38193;
    std::cout << 38193;
    for (int i = 0; i < 38193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38194(int& val) {
    func_in_38194(val);
    const int x = 38194;
    std::cout << 38194;
    for (int i = 0; i < 38194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38194(int& val) {
    const int x = 38194;
    std::cout << 38194;
    for (int i = 0; i < 38194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38195(int& val) {
    func_in_38195(val);
    const int x = 38195;
    std::cout << 38195;
    for (int i = 0; i < 38195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38195(int& val) {
    const int x = 38195;
    std::cout << 38195;
    for (int i = 0; i < 38195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38196(int& val) {
    func_in_38196(val);
    const int x = 38196;
    std::cout << 38196;
    for (int i = 0; i < 38196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38196(int& val) {
    const int x = 38196;
    std::cout << 38196;
    for (int i = 0; i < 38196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38197(int& val) {
    func_in_38197(val);
    const int x = 38197;
    std::cout << 38197;
    for (int i = 0; i < 38197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38197(int& val) {
    const int x = 38197;
    std::cout << 38197;
    for (int i = 0; i < 38197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38198(int& val) {
    func_in_38198(val);
    const int x = 38198;
    std::cout << 38198;
    for (int i = 0; i < 38198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38198(int& val) {
    const int x = 38198;
    std::cout << 38198;
    for (int i = 0; i < 38198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38199(int& val) {
    func_in_38199(val);
    const int x = 38199;
    std::cout << 38199;
    for (int i = 0; i < 38199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38199(int& val) {
    const int x = 38199;
    std::cout << 38199;
    for (int i = 0; i < 38199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38200(int& val) {
    func_in_38200(val);
    const int x = 38200;
    std::cout << 38200;
    for (int i = 0; i < 38200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38200(int& val) {
    const int x = 38200;
    std::cout << 38200;
    for (int i = 0; i < 38200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38201(int& val) {
    func_in_38201(val);
    const int x = 38201;
    std::cout << 38201;
    for (int i = 0; i < 38201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38201(int& val) {
    const int x = 38201;
    std::cout << 38201;
    for (int i = 0; i < 38201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38202(int& val) {
    func_in_38202(val);
    const int x = 38202;
    std::cout << 38202;
    for (int i = 0; i < 38202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38202(int& val) {
    const int x = 38202;
    std::cout << 38202;
    for (int i = 0; i < 38202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38203(int& val) {
    func_in_38203(val);
    const int x = 38203;
    std::cout << 38203;
    for (int i = 0; i < 38203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38203(int& val) {
    const int x = 38203;
    std::cout << 38203;
    for (int i = 0; i < 38203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38204(int& val) {
    func_in_38204(val);
    const int x = 38204;
    std::cout << 38204;
    for (int i = 0; i < 38204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38204(int& val) {
    const int x = 38204;
    std::cout << 38204;
    for (int i = 0; i < 38204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38205(int& val) {
    func_in_38205(val);
    const int x = 38205;
    std::cout << 38205;
    for (int i = 0; i < 38205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38205(int& val) {
    const int x = 38205;
    std::cout << 38205;
    for (int i = 0; i < 38205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38206(int& val) {
    func_in_38206(val);
    const int x = 38206;
    std::cout << 38206;
    for (int i = 0; i < 38206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38206(int& val) {
    const int x = 38206;
    std::cout << 38206;
    for (int i = 0; i < 38206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38207(int& val) {
    func_in_38207(val);
    const int x = 38207;
    std::cout << 38207;
    for (int i = 0; i < 38207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38207(int& val) {
    const int x = 38207;
    std::cout << 38207;
    for (int i = 0; i < 38207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38208(int& val) {
    func_in_38208(val);
    const int x = 38208;
    std::cout << 38208;
    for (int i = 0; i < 38208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38208(int& val) {
    const int x = 38208;
    std::cout << 38208;
    for (int i = 0; i < 38208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38209(int& val) {
    func_in_38209(val);
    const int x = 38209;
    std::cout << 38209;
    for (int i = 0; i < 38209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38209(int& val) {
    const int x = 38209;
    std::cout << 38209;
    for (int i = 0; i < 38209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38210(int& val) {
    func_in_38210(val);
    const int x = 38210;
    std::cout << 38210;
    for (int i = 0; i < 38210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38210(int& val) {
    const int x = 38210;
    std::cout << 38210;
    for (int i = 0; i < 38210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38211(int& val) {
    func_in_38211(val);
    const int x = 38211;
    std::cout << 38211;
    for (int i = 0; i < 38211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38211(int& val) {
    const int x = 38211;
    std::cout << 38211;
    for (int i = 0; i < 38211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38212(int& val) {
    func_in_38212(val);
    const int x = 38212;
    std::cout << 38212;
    for (int i = 0; i < 38212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38212(int& val) {
    const int x = 38212;
    std::cout << 38212;
    for (int i = 0; i < 38212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38213(int& val) {
    func_in_38213(val);
    const int x = 38213;
    std::cout << 38213;
    for (int i = 0; i < 38213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38213(int& val) {
    const int x = 38213;
    std::cout << 38213;
    for (int i = 0; i < 38213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38214(int& val) {
    func_in_38214(val);
    const int x = 38214;
    std::cout << 38214;
    for (int i = 0; i < 38214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38214(int& val) {
    const int x = 38214;
    std::cout << 38214;
    for (int i = 0; i < 38214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38215(int& val) {
    func_in_38215(val);
    const int x = 38215;
    std::cout << 38215;
    for (int i = 0; i < 38215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38215(int& val) {
    const int x = 38215;
    std::cout << 38215;
    for (int i = 0; i < 38215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38216(int& val) {
    func_in_38216(val);
    const int x = 38216;
    std::cout << 38216;
    for (int i = 0; i < 38216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38216(int& val) {
    const int x = 38216;
    std::cout << 38216;
    for (int i = 0; i < 38216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38217(int& val) {
    func_in_38217(val);
    const int x = 38217;
    std::cout << 38217;
    for (int i = 0; i < 38217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38217(int& val) {
    const int x = 38217;
    std::cout << 38217;
    for (int i = 0; i < 38217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38218(int& val) {
    func_in_38218(val);
    const int x = 38218;
    std::cout << 38218;
    for (int i = 0; i < 38218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38218(int& val) {
    const int x = 38218;
    std::cout << 38218;
    for (int i = 0; i < 38218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38219(int& val) {
    func_in_38219(val);
    const int x = 38219;
    std::cout << 38219;
    for (int i = 0; i < 38219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38219(int& val) {
    const int x = 38219;
    std::cout << 38219;
    for (int i = 0; i < 38219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38220(int& val) {
    func_in_38220(val);
    const int x = 38220;
    std::cout << 38220;
    for (int i = 0; i < 38220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38220(int& val) {
    const int x = 38220;
    std::cout << 38220;
    for (int i = 0; i < 38220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38221(int& val) {
    func_in_38221(val);
    const int x = 38221;
    std::cout << 38221;
    for (int i = 0; i < 38221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38221(int& val) {
    const int x = 38221;
    std::cout << 38221;
    for (int i = 0; i < 38221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38222(int& val) {
    func_in_38222(val);
    const int x = 38222;
    std::cout << 38222;
    for (int i = 0; i < 38222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38222(int& val) {
    const int x = 38222;
    std::cout << 38222;
    for (int i = 0; i < 38222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38223(int& val) {
    func_in_38223(val);
    const int x = 38223;
    std::cout << 38223;
    for (int i = 0; i < 38223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38223(int& val) {
    const int x = 38223;
    std::cout << 38223;
    for (int i = 0; i < 38223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38224(int& val) {
    func_in_38224(val);
    const int x = 38224;
    std::cout << 38224;
    for (int i = 0; i < 38224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38224(int& val) {
    const int x = 38224;
    std::cout << 38224;
    for (int i = 0; i < 38224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38225(int& val) {
    func_in_38225(val);
    const int x = 38225;
    std::cout << 38225;
    for (int i = 0; i < 38225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38225(int& val) {
    const int x = 38225;
    std::cout << 38225;
    for (int i = 0; i < 38225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38226(int& val) {
    func_in_38226(val);
    const int x = 38226;
    std::cout << 38226;
    for (int i = 0; i < 38226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38226(int& val) {
    const int x = 38226;
    std::cout << 38226;
    for (int i = 0; i < 38226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38227(int& val) {
    func_in_38227(val);
    const int x = 38227;
    std::cout << 38227;
    for (int i = 0; i < 38227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38227(int& val) {
    const int x = 38227;
    std::cout << 38227;
    for (int i = 0; i < 38227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38228(int& val) {
    func_in_38228(val);
    const int x = 38228;
    std::cout << 38228;
    for (int i = 0; i < 38228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38228(int& val) {
    const int x = 38228;
    std::cout << 38228;
    for (int i = 0; i < 38228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38229(int& val) {
    func_in_38229(val);
    const int x = 38229;
    std::cout << 38229;
    for (int i = 0; i < 38229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38229(int& val) {
    const int x = 38229;
    std::cout << 38229;
    for (int i = 0; i < 38229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38230(int& val) {
    func_in_38230(val);
    const int x = 38230;
    std::cout << 38230;
    for (int i = 0; i < 38230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38230(int& val) {
    const int x = 38230;
    std::cout << 38230;
    for (int i = 0; i < 38230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38231(int& val) {
    func_in_38231(val);
    const int x = 38231;
    std::cout << 38231;
    for (int i = 0; i < 38231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38231(int& val) {
    const int x = 38231;
    std::cout << 38231;
    for (int i = 0; i < 38231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38232(int& val) {
    func_in_38232(val);
    const int x = 38232;
    std::cout << 38232;
    for (int i = 0; i < 38232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38232(int& val) {
    const int x = 38232;
    std::cout << 38232;
    for (int i = 0; i < 38232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38233(int& val) {
    func_in_38233(val);
    const int x = 38233;
    std::cout << 38233;
    for (int i = 0; i < 38233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38233(int& val) {
    const int x = 38233;
    std::cout << 38233;
    for (int i = 0; i < 38233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38234(int& val) {
    func_in_38234(val);
    const int x = 38234;
    std::cout << 38234;
    for (int i = 0; i < 38234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38234(int& val) {
    const int x = 38234;
    std::cout << 38234;
    for (int i = 0; i < 38234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38235(int& val) {
    func_in_38235(val);
    const int x = 38235;
    std::cout << 38235;
    for (int i = 0; i < 38235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38235(int& val) {
    const int x = 38235;
    std::cout << 38235;
    for (int i = 0; i < 38235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38236(int& val) {
    func_in_38236(val);
    const int x = 38236;
    std::cout << 38236;
    for (int i = 0; i < 38236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38236(int& val) {
    const int x = 38236;
    std::cout << 38236;
    for (int i = 0; i < 38236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38237(int& val) {
    func_in_38237(val);
    const int x = 38237;
    std::cout << 38237;
    for (int i = 0; i < 38237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38237(int& val) {
    const int x = 38237;
    std::cout << 38237;
    for (int i = 0; i < 38237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38238(int& val) {
    func_in_38238(val);
    const int x = 38238;
    std::cout << 38238;
    for (int i = 0; i < 38238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38238(int& val) {
    const int x = 38238;
    std::cout << 38238;
    for (int i = 0; i < 38238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38239(int& val) {
    func_in_38239(val);
    const int x = 38239;
    std::cout << 38239;
    for (int i = 0; i < 38239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38239(int& val) {
    const int x = 38239;
    std::cout << 38239;
    for (int i = 0; i < 38239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38240(int& val) {
    func_in_38240(val);
    const int x = 38240;
    std::cout << 38240;
    for (int i = 0; i < 38240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38240(int& val) {
    const int x = 38240;
    std::cout << 38240;
    for (int i = 0; i < 38240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38241(int& val) {
    func_in_38241(val);
    const int x = 38241;
    std::cout << 38241;
    for (int i = 0; i < 38241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38241(int& val) {
    const int x = 38241;
    std::cout << 38241;
    for (int i = 0; i < 38241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38242(int& val) {
    func_in_38242(val);
    const int x = 38242;
    std::cout << 38242;
    for (int i = 0; i < 38242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38242(int& val) {
    const int x = 38242;
    std::cout << 38242;
    for (int i = 0; i < 38242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38243(int& val) {
    func_in_38243(val);
    const int x = 38243;
    std::cout << 38243;
    for (int i = 0; i < 38243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38243(int& val) {
    const int x = 38243;
    std::cout << 38243;
    for (int i = 0; i < 38243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38244(int& val) {
    func_in_38244(val);
    const int x = 38244;
    std::cout << 38244;
    for (int i = 0; i < 38244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38244(int& val) {
    const int x = 38244;
    std::cout << 38244;
    for (int i = 0; i < 38244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38245(int& val) {
    func_in_38245(val);
    const int x = 38245;
    std::cout << 38245;
    for (int i = 0; i < 38245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38245(int& val) {
    const int x = 38245;
    std::cout << 38245;
    for (int i = 0; i < 38245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38246(int& val) {
    func_in_38246(val);
    const int x = 38246;
    std::cout << 38246;
    for (int i = 0; i < 38246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38246(int& val) {
    const int x = 38246;
    std::cout << 38246;
    for (int i = 0; i < 38246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38247(int& val) {
    func_in_38247(val);
    const int x = 38247;
    std::cout << 38247;
    for (int i = 0; i < 38247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38247(int& val) {
    const int x = 38247;
    std::cout << 38247;
    for (int i = 0; i < 38247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38248(int& val) {
    func_in_38248(val);
    const int x = 38248;
    std::cout << 38248;
    for (int i = 0; i < 38248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38248(int& val) {
    const int x = 38248;
    std::cout << 38248;
    for (int i = 0; i < 38248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38249(int& val) {
    func_in_38249(val);
    const int x = 38249;
    std::cout << 38249;
    for (int i = 0; i < 38249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38249(int& val) {
    const int x = 38249;
    std::cout << 38249;
    for (int i = 0; i < 38249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38250(int& val) {
    func_in_38250(val);
    const int x = 38250;
    std::cout << 38250;
    for (int i = 0; i < 38250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38250(int& val) {
    const int x = 38250;
    std::cout << 38250;
    for (int i = 0; i < 38250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38251(int& val) {
    func_in_38251(val);
    const int x = 38251;
    std::cout << 38251;
    for (int i = 0; i < 38251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38251(int& val) {
    const int x = 38251;
    std::cout << 38251;
    for (int i = 0; i < 38251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38252(int& val) {
    func_in_38252(val);
    const int x = 38252;
    std::cout << 38252;
    for (int i = 0; i < 38252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38252(int& val) {
    const int x = 38252;
    std::cout << 38252;
    for (int i = 0; i < 38252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38253(int& val) {
    func_in_38253(val);
    const int x = 38253;
    std::cout << 38253;
    for (int i = 0; i < 38253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38253(int& val) {
    const int x = 38253;
    std::cout << 38253;
    for (int i = 0; i < 38253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38254(int& val) {
    func_in_38254(val);
    const int x = 38254;
    std::cout << 38254;
    for (int i = 0; i < 38254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38254(int& val) {
    const int x = 38254;
    std::cout << 38254;
    for (int i = 0; i < 38254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38255(int& val) {
    func_in_38255(val);
    const int x = 38255;
    std::cout << 38255;
    for (int i = 0; i < 38255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38255(int& val) {
    const int x = 38255;
    std::cout << 38255;
    for (int i = 0; i < 38255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38256(int& val) {
    func_in_38256(val);
    const int x = 38256;
    std::cout << 38256;
    for (int i = 0; i < 38256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38256(int& val) {
    const int x = 38256;
    std::cout << 38256;
    for (int i = 0; i < 38256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38257(int& val) {
    func_in_38257(val);
    const int x = 38257;
    std::cout << 38257;
    for (int i = 0; i < 38257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38257(int& val) {
    const int x = 38257;
    std::cout << 38257;
    for (int i = 0; i < 38257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38258(int& val) {
    func_in_38258(val);
    const int x = 38258;
    std::cout << 38258;
    for (int i = 0; i < 38258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38258(int& val) {
    const int x = 38258;
    std::cout << 38258;
    for (int i = 0; i < 38258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38259(int& val) {
    func_in_38259(val);
    const int x = 38259;
    std::cout << 38259;
    for (int i = 0; i < 38259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38259(int& val) {
    const int x = 38259;
    std::cout << 38259;
    for (int i = 0; i < 38259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38260(int& val) {
    func_in_38260(val);
    const int x = 38260;
    std::cout << 38260;
    for (int i = 0; i < 38260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38260(int& val) {
    const int x = 38260;
    std::cout << 38260;
    for (int i = 0; i < 38260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38261(int& val) {
    func_in_38261(val);
    const int x = 38261;
    std::cout << 38261;
    for (int i = 0; i < 38261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38261(int& val) {
    const int x = 38261;
    std::cout << 38261;
    for (int i = 0; i < 38261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38262(int& val) {
    func_in_38262(val);
    const int x = 38262;
    std::cout << 38262;
    for (int i = 0; i < 38262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38262(int& val) {
    const int x = 38262;
    std::cout << 38262;
    for (int i = 0; i < 38262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38263(int& val) {
    func_in_38263(val);
    const int x = 38263;
    std::cout << 38263;
    for (int i = 0; i < 38263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38263(int& val) {
    const int x = 38263;
    std::cout << 38263;
    for (int i = 0; i < 38263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38264(int& val) {
    func_in_38264(val);
    const int x = 38264;
    std::cout << 38264;
    for (int i = 0; i < 38264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38264(int& val) {
    const int x = 38264;
    std::cout << 38264;
    for (int i = 0; i < 38264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38265(int& val) {
    func_in_38265(val);
    const int x = 38265;
    std::cout << 38265;
    for (int i = 0; i < 38265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38265(int& val) {
    const int x = 38265;
    std::cout << 38265;
    for (int i = 0; i < 38265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38266(int& val) {
    func_in_38266(val);
    const int x = 38266;
    std::cout << 38266;
    for (int i = 0; i < 38266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38266(int& val) {
    const int x = 38266;
    std::cout << 38266;
    for (int i = 0; i < 38266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38267(int& val) {
    func_in_38267(val);
    const int x = 38267;
    std::cout << 38267;
    for (int i = 0; i < 38267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38267(int& val) {
    const int x = 38267;
    std::cout << 38267;
    for (int i = 0; i < 38267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38268(int& val) {
    func_in_38268(val);
    const int x = 38268;
    std::cout << 38268;
    for (int i = 0; i < 38268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38268(int& val) {
    const int x = 38268;
    std::cout << 38268;
    for (int i = 0; i < 38268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38269(int& val) {
    func_in_38269(val);
    const int x = 38269;
    std::cout << 38269;
    for (int i = 0; i < 38269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38269(int& val) {
    const int x = 38269;
    std::cout << 38269;
    for (int i = 0; i < 38269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38270(int& val) {
    func_in_38270(val);
    const int x = 38270;
    std::cout << 38270;
    for (int i = 0; i < 38270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38270(int& val) {
    const int x = 38270;
    std::cout << 38270;
    for (int i = 0; i < 38270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38271(int& val) {
    func_in_38271(val);
    const int x = 38271;
    std::cout << 38271;
    for (int i = 0; i < 38271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38271(int& val) {
    const int x = 38271;
    std::cout << 38271;
    for (int i = 0; i < 38271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38272(int& val) {
    func_in_38272(val);
    const int x = 38272;
    std::cout << 38272;
    for (int i = 0; i < 38272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38272(int& val) {
    const int x = 38272;
    std::cout << 38272;
    for (int i = 0; i < 38272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38273(int& val) {
    func_in_38273(val);
    const int x = 38273;
    std::cout << 38273;
    for (int i = 0; i < 38273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38273(int& val) {
    const int x = 38273;
    std::cout << 38273;
    for (int i = 0; i < 38273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38274(int& val) {
    func_in_38274(val);
    const int x = 38274;
    std::cout << 38274;
    for (int i = 0; i < 38274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38274(int& val) {
    const int x = 38274;
    std::cout << 38274;
    for (int i = 0; i < 38274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38275(int& val) {
    func_in_38275(val);
    const int x = 38275;
    std::cout << 38275;
    for (int i = 0; i < 38275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38275(int& val) {
    const int x = 38275;
    std::cout << 38275;
    for (int i = 0; i < 38275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38276(int& val) {
    func_in_38276(val);
    const int x = 38276;
    std::cout << 38276;
    for (int i = 0; i < 38276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38276(int& val) {
    const int x = 38276;
    std::cout << 38276;
    for (int i = 0; i < 38276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38277(int& val) {
    func_in_38277(val);
    const int x = 38277;
    std::cout << 38277;
    for (int i = 0; i < 38277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38277(int& val) {
    const int x = 38277;
    std::cout << 38277;
    for (int i = 0; i < 38277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38278(int& val) {
    func_in_38278(val);
    const int x = 38278;
    std::cout << 38278;
    for (int i = 0; i < 38278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38278(int& val) {
    const int x = 38278;
    std::cout << 38278;
    for (int i = 0; i < 38278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38279(int& val) {
    func_in_38279(val);
    const int x = 38279;
    std::cout << 38279;
    for (int i = 0; i < 38279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38279(int& val) {
    const int x = 38279;
    std::cout << 38279;
    for (int i = 0; i < 38279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38280(int& val) {
    func_in_38280(val);
    const int x = 38280;
    std::cout << 38280;
    for (int i = 0; i < 38280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38280(int& val) {
    const int x = 38280;
    std::cout << 38280;
    for (int i = 0; i < 38280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38281(int& val) {
    func_in_38281(val);
    const int x = 38281;
    std::cout << 38281;
    for (int i = 0; i < 38281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38281(int& val) {
    const int x = 38281;
    std::cout << 38281;
    for (int i = 0; i < 38281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38282(int& val) {
    func_in_38282(val);
    const int x = 38282;
    std::cout << 38282;
    for (int i = 0; i < 38282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38282(int& val) {
    const int x = 38282;
    std::cout << 38282;
    for (int i = 0; i < 38282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38283(int& val) {
    func_in_38283(val);
    const int x = 38283;
    std::cout << 38283;
    for (int i = 0; i < 38283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38283(int& val) {
    const int x = 38283;
    std::cout << 38283;
    for (int i = 0; i < 38283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38284(int& val) {
    func_in_38284(val);
    const int x = 38284;
    std::cout << 38284;
    for (int i = 0; i < 38284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38284(int& val) {
    const int x = 38284;
    std::cout << 38284;
    for (int i = 0; i < 38284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38285(int& val) {
    func_in_38285(val);
    const int x = 38285;
    std::cout << 38285;
    for (int i = 0; i < 38285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38285(int& val) {
    const int x = 38285;
    std::cout << 38285;
    for (int i = 0; i < 38285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38286(int& val) {
    func_in_38286(val);
    const int x = 38286;
    std::cout << 38286;
    for (int i = 0; i < 38286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38286(int& val) {
    const int x = 38286;
    std::cout << 38286;
    for (int i = 0; i < 38286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38287(int& val) {
    func_in_38287(val);
    const int x = 38287;
    std::cout << 38287;
    for (int i = 0; i < 38287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38287(int& val) {
    const int x = 38287;
    std::cout << 38287;
    for (int i = 0; i < 38287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38288(int& val) {
    func_in_38288(val);
    const int x = 38288;
    std::cout << 38288;
    for (int i = 0; i < 38288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38288(int& val) {
    const int x = 38288;
    std::cout << 38288;
    for (int i = 0; i < 38288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38289(int& val) {
    func_in_38289(val);
    const int x = 38289;
    std::cout << 38289;
    for (int i = 0; i < 38289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38289(int& val) {
    const int x = 38289;
    std::cout << 38289;
    for (int i = 0; i < 38289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38290(int& val) {
    func_in_38290(val);
    const int x = 38290;
    std::cout << 38290;
    for (int i = 0; i < 38290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38290(int& val) {
    const int x = 38290;
    std::cout << 38290;
    for (int i = 0; i < 38290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38291(int& val) {
    func_in_38291(val);
    const int x = 38291;
    std::cout << 38291;
    for (int i = 0; i < 38291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38291(int& val) {
    const int x = 38291;
    std::cout << 38291;
    for (int i = 0; i < 38291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38292(int& val) {
    func_in_38292(val);
    const int x = 38292;
    std::cout << 38292;
    for (int i = 0; i < 38292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38292(int& val) {
    const int x = 38292;
    std::cout << 38292;
    for (int i = 0; i < 38292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38293(int& val) {
    func_in_38293(val);
    const int x = 38293;
    std::cout << 38293;
    for (int i = 0; i < 38293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38293(int& val) {
    const int x = 38293;
    std::cout << 38293;
    for (int i = 0; i < 38293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38294(int& val) {
    func_in_38294(val);
    const int x = 38294;
    std::cout << 38294;
    for (int i = 0; i < 38294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38294(int& val) {
    const int x = 38294;
    std::cout << 38294;
    for (int i = 0; i < 38294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38295(int& val) {
    func_in_38295(val);
    const int x = 38295;
    std::cout << 38295;
    for (int i = 0; i < 38295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38295(int& val) {
    const int x = 38295;
    std::cout << 38295;
    for (int i = 0; i < 38295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38296(int& val) {
    func_in_38296(val);
    const int x = 38296;
    std::cout << 38296;
    for (int i = 0; i < 38296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38296(int& val) {
    const int x = 38296;
    std::cout << 38296;
    for (int i = 0; i < 38296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38297(int& val) {
    func_in_38297(val);
    const int x = 38297;
    std::cout << 38297;
    for (int i = 0; i < 38297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38297(int& val) {
    const int x = 38297;
    std::cout << 38297;
    for (int i = 0; i < 38297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38298(int& val) {
    func_in_38298(val);
    const int x = 38298;
    std::cout << 38298;
    for (int i = 0; i < 38298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38298(int& val) {
    const int x = 38298;
    std::cout << 38298;
    for (int i = 0; i < 38298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38299(int& val) {
    func_in_38299(val);
    const int x = 38299;
    std::cout << 38299;
    for (int i = 0; i < 38299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38299(int& val) {
    const int x = 38299;
    std::cout << 38299;
    for (int i = 0; i < 38299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38300(int& val) {
    func_in_38300(val);
    const int x = 38300;
    std::cout << 38300;
    for (int i = 0; i < 38300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38300(int& val) {
    const int x = 38300;
    std::cout << 38300;
    for (int i = 0; i < 38300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38301(int& val) {
    func_in_38301(val);
    const int x = 38301;
    std::cout << 38301;
    for (int i = 0; i < 38301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38301(int& val) {
    const int x = 38301;
    std::cout << 38301;
    for (int i = 0; i < 38301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38302(int& val) {
    func_in_38302(val);
    const int x = 38302;
    std::cout << 38302;
    for (int i = 0; i < 38302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38302(int& val) {
    const int x = 38302;
    std::cout << 38302;
    for (int i = 0; i < 38302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38303(int& val) {
    func_in_38303(val);
    const int x = 38303;
    std::cout << 38303;
    for (int i = 0; i < 38303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38303(int& val) {
    const int x = 38303;
    std::cout << 38303;
    for (int i = 0; i < 38303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38304(int& val) {
    func_in_38304(val);
    const int x = 38304;
    std::cout << 38304;
    for (int i = 0; i < 38304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38304(int& val) {
    const int x = 38304;
    std::cout << 38304;
    for (int i = 0; i < 38304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38305(int& val) {
    func_in_38305(val);
    const int x = 38305;
    std::cout << 38305;
    for (int i = 0; i < 38305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38305(int& val) {
    const int x = 38305;
    std::cout << 38305;
    for (int i = 0; i < 38305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38306(int& val) {
    func_in_38306(val);
    const int x = 38306;
    std::cout << 38306;
    for (int i = 0; i < 38306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38306(int& val) {
    const int x = 38306;
    std::cout << 38306;
    for (int i = 0; i < 38306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38307(int& val) {
    func_in_38307(val);
    const int x = 38307;
    std::cout << 38307;
    for (int i = 0; i < 38307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38307(int& val) {
    const int x = 38307;
    std::cout << 38307;
    for (int i = 0; i < 38307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38308(int& val) {
    func_in_38308(val);
    const int x = 38308;
    std::cout << 38308;
    for (int i = 0; i < 38308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38308(int& val) {
    const int x = 38308;
    std::cout << 38308;
    for (int i = 0; i < 38308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38309(int& val) {
    func_in_38309(val);
    const int x = 38309;
    std::cout << 38309;
    for (int i = 0; i < 38309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38309(int& val) {
    const int x = 38309;
    std::cout << 38309;
    for (int i = 0; i < 38309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38310(int& val) {
    func_in_38310(val);
    const int x = 38310;
    std::cout << 38310;
    for (int i = 0; i < 38310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38310(int& val) {
    const int x = 38310;
    std::cout << 38310;
    for (int i = 0; i < 38310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38311(int& val) {
    func_in_38311(val);
    const int x = 38311;
    std::cout << 38311;
    for (int i = 0; i < 38311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38311(int& val) {
    const int x = 38311;
    std::cout << 38311;
    for (int i = 0; i < 38311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38312(int& val) {
    func_in_38312(val);
    const int x = 38312;
    std::cout << 38312;
    for (int i = 0; i < 38312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38312(int& val) {
    const int x = 38312;
    std::cout << 38312;
    for (int i = 0; i < 38312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38313(int& val) {
    func_in_38313(val);
    const int x = 38313;
    std::cout << 38313;
    for (int i = 0; i < 38313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38313(int& val) {
    const int x = 38313;
    std::cout << 38313;
    for (int i = 0; i < 38313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38314(int& val) {
    func_in_38314(val);
    const int x = 38314;
    std::cout << 38314;
    for (int i = 0; i < 38314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38314(int& val) {
    const int x = 38314;
    std::cout << 38314;
    for (int i = 0; i < 38314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38315(int& val) {
    func_in_38315(val);
    const int x = 38315;
    std::cout << 38315;
    for (int i = 0; i < 38315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38315(int& val) {
    const int x = 38315;
    std::cout << 38315;
    for (int i = 0; i < 38315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38316(int& val) {
    func_in_38316(val);
    const int x = 38316;
    std::cout << 38316;
    for (int i = 0; i < 38316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38316(int& val) {
    const int x = 38316;
    std::cout << 38316;
    for (int i = 0; i < 38316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38317(int& val) {
    func_in_38317(val);
    const int x = 38317;
    std::cout << 38317;
    for (int i = 0; i < 38317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38317(int& val) {
    const int x = 38317;
    std::cout << 38317;
    for (int i = 0; i < 38317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38318(int& val) {
    func_in_38318(val);
    const int x = 38318;
    std::cout << 38318;
    for (int i = 0; i < 38318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38318(int& val) {
    const int x = 38318;
    std::cout << 38318;
    for (int i = 0; i < 38318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38319(int& val) {
    func_in_38319(val);
    const int x = 38319;
    std::cout << 38319;
    for (int i = 0; i < 38319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38319(int& val) {
    const int x = 38319;
    std::cout << 38319;
    for (int i = 0; i < 38319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38320(int& val) {
    func_in_38320(val);
    const int x = 38320;
    std::cout << 38320;
    for (int i = 0; i < 38320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38320(int& val) {
    const int x = 38320;
    std::cout << 38320;
    for (int i = 0; i < 38320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38321(int& val) {
    func_in_38321(val);
    const int x = 38321;
    std::cout << 38321;
    for (int i = 0; i < 38321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38321(int& val) {
    const int x = 38321;
    std::cout << 38321;
    for (int i = 0; i < 38321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38322(int& val) {
    func_in_38322(val);
    const int x = 38322;
    std::cout << 38322;
    for (int i = 0; i < 38322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38322(int& val) {
    const int x = 38322;
    std::cout << 38322;
    for (int i = 0; i < 38322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38323(int& val) {
    func_in_38323(val);
    const int x = 38323;
    std::cout << 38323;
    for (int i = 0; i < 38323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38323(int& val) {
    const int x = 38323;
    std::cout << 38323;
    for (int i = 0; i < 38323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38324(int& val) {
    func_in_38324(val);
    const int x = 38324;
    std::cout << 38324;
    for (int i = 0; i < 38324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38324(int& val) {
    const int x = 38324;
    std::cout << 38324;
    for (int i = 0; i < 38324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38325(int& val) {
    func_in_38325(val);
    const int x = 38325;
    std::cout << 38325;
    for (int i = 0; i < 38325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38325(int& val) {
    const int x = 38325;
    std::cout << 38325;
    for (int i = 0; i < 38325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38326(int& val) {
    func_in_38326(val);
    const int x = 38326;
    std::cout << 38326;
    for (int i = 0; i < 38326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38326(int& val) {
    const int x = 38326;
    std::cout << 38326;
    for (int i = 0; i < 38326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38327(int& val) {
    func_in_38327(val);
    const int x = 38327;
    std::cout << 38327;
    for (int i = 0; i < 38327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38327(int& val) {
    const int x = 38327;
    std::cout << 38327;
    for (int i = 0; i < 38327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38328(int& val) {
    func_in_38328(val);
    const int x = 38328;
    std::cout << 38328;
    for (int i = 0; i < 38328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38328(int& val) {
    const int x = 38328;
    std::cout << 38328;
    for (int i = 0; i < 38328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38329(int& val) {
    func_in_38329(val);
    const int x = 38329;
    std::cout << 38329;
    for (int i = 0; i < 38329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38329(int& val) {
    const int x = 38329;
    std::cout << 38329;
    for (int i = 0; i < 38329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38330(int& val) {
    func_in_38330(val);
    const int x = 38330;
    std::cout << 38330;
    for (int i = 0; i < 38330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38330(int& val) {
    const int x = 38330;
    std::cout << 38330;
    for (int i = 0; i < 38330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38331(int& val) {
    func_in_38331(val);
    const int x = 38331;
    std::cout << 38331;
    for (int i = 0; i < 38331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38331(int& val) {
    const int x = 38331;
    std::cout << 38331;
    for (int i = 0; i < 38331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38332(int& val) {
    func_in_38332(val);
    const int x = 38332;
    std::cout << 38332;
    for (int i = 0; i < 38332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38332(int& val) {
    const int x = 38332;
    std::cout << 38332;
    for (int i = 0; i < 38332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38333(int& val) {
    func_in_38333(val);
    const int x = 38333;
    std::cout << 38333;
    for (int i = 0; i < 38333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38333(int& val) {
    const int x = 38333;
    std::cout << 38333;
    for (int i = 0; i < 38333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38334(int& val) {
    func_in_38334(val);
    const int x = 38334;
    std::cout << 38334;
    for (int i = 0; i < 38334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38334(int& val) {
    const int x = 38334;
    std::cout << 38334;
    for (int i = 0; i < 38334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38335(int& val) {
    func_in_38335(val);
    const int x = 38335;
    std::cout << 38335;
    for (int i = 0; i < 38335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38335(int& val) {
    const int x = 38335;
    std::cout << 38335;
    for (int i = 0; i < 38335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38336(int& val) {
    func_in_38336(val);
    const int x = 38336;
    std::cout << 38336;
    for (int i = 0; i < 38336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38336(int& val) {
    const int x = 38336;
    std::cout << 38336;
    for (int i = 0; i < 38336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38337(int& val) {
    func_in_38337(val);
    const int x = 38337;
    std::cout << 38337;
    for (int i = 0; i < 38337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38337(int& val) {
    const int x = 38337;
    std::cout << 38337;
    for (int i = 0; i < 38337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38338(int& val) {
    func_in_38338(val);
    const int x = 38338;
    std::cout << 38338;
    for (int i = 0; i < 38338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38338(int& val) {
    const int x = 38338;
    std::cout << 38338;
    for (int i = 0; i < 38338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38339(int& val) {
    func_in_38339(val);
    const int x = 38339;
    std::cout << 38339;
    for (int i = 0; i < 38339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38339(int& val) {
    const int x = 38339;
    std::cout << 38339;
    for (int i = 0; i < 38339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38340(int& val) {
    func_in_38340(val);
    const int x = 38340;
    std::cout << 38340;
    for (int i = 0; i < 38340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38340(int& val) {
    const int x = 38340;
    std::cout << 38340;
    for (int i = 0; i < 38340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38341(int& val) {
    func_in_38341(val);
    const int x = 38341;
    std::cout << 38341;
    for (int i = 0; i < 38341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38341(int& val) {
    const int x = 38341;
    std::cout << 38341;
    for (int i = 0; i < 38341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38342(int& val) {
    func_in_38342(val);
    const int x = 38342;
    std::cout << 38342;
    for (int i = 0; i < 38342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38342(int& val) {
    const int x = 38342;
    std::cout << 38342;
    for (int i = 0; i < 38342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38343(int& val) {
    func_in_38343(val);
    const int x = 38343;
    std::cout << 38343;
    for (int i = 0; i < 38343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38343(int& val) {
    const int x = 38343;
    std::cout << 38343;
    for (int i = 0; i < 38343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38344(int& val) {
    func_in_38344(val);
    const int x = 38344;
    std::cout << 38344;
    for (int i = 0; i < 38344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38344(int& val) {
    const int x = 38344;
    std::cout << 38344;
    for (int i = 0; i < 38344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38345(int& val) {
    func_in_38345(val);
    const int x = 38345;
    std::cout << 38345;
    for (int i = 0; i < 38345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38345(int& val) {
    const int x = 38345;
    std::cout << 38345;
    for (int i = 0; i < 38345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38346(int& val) {
    func_in_38346(val);
    const int x = 38346;
    std::cout << 38346;
    for (int i = 0; i < 38346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38346(int& val) {
    const int x = 38346;
    std::cout << 38346;
    for (int i = 0; i < 38346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38347(int& val) {
    func_in_38347(val);
    const int x = 38347;
    std::cout << 38347;
    for (int i = 0; i < 38347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38347(int& val) {
    const int x = 38347;
    std::cout << 38347;
    for (int i = 0; i < 38347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38348(int& val) {
    func_in_38348(val);
    const int x = 38348;
    std::cout << 38348;
    for (int i = 0; i < 38348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38348(int& val) {
    const int x = 38348;
    std::cout << 38348;
    for (int i = 0; i < 38348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38349(int& val) {
    func_in_38349(val);
    const int x = 38349;
    std::cout << 38349;
    for (int i = 0; i < 38349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38349(int& val) {
    const int x = 38349;
    std::cout << 38349;
    for (int i = 0; i < 38349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38350(int& val) {
    func_in_38350(val);
    const int x = 38350;
    std::cout << 38350;
    for (int i = 0; i < 38350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38350(int& val) {
    const int x = 38350;
    std::cout << 38350;
    for (int i = 0; i < 38350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38351(int& val) {
    func_in_38351(val);
    const int x = 38351;
    std::cout << 38351;
    for (int i = 0; i < 38351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38351(int& val) {
    const int x = 38351;
    std::cout << 38351;
    for (int i = 0; i < 38351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38352(int& val) {
    func_in_38352(val);
    const int x = 38352;
    std::cout << 38352;
    for (int i = 0; i < 38352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38352(int& val) {
    const int x = 38352;
    std::cout << 38352;
    for (int i = 0; i < 38352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38353(int& val) {
    func_in_38353(val);
    const int x = 38353;
    std::cout << 38353;
    for (int i = 0; i < 38353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38353(int& val) {
    const int x = 38353;
    std::cout << 38353;
    for (int i = 0; i < 38353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38354(int& val) {
    func_in_38354(val);
    const int x = 38354;
    std::cout << 38354;
    for (int i = 0; i < 38354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38354(int& val) {
    const int x = 38354;
    std::cout << 38354;
    for (int i = 0; i < 38354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38355(int& val) {
    func_in_38355(val);
    const int x = 38355;
    std::cout << 38355;
    for (int i = 0; i < 38355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38355(int& val) {
    const int x = 38355;
    std::cout << 38355;
    for (int i = 0; i < 38355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38356(int& val) {
    func_in_38356(val);
    const int x = 38356;
    std::cout << 38356;
    for (int i = 0; i < 38356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38356(int& val) {
    const int x = 38356;
    std::cout << 38356;
    for (int i = 0; i < 38356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38357(int& val) {
    func_in_38357(val);
    const int x = 38357;
    std::cout << 38357;
    for (int i = 0; i < 38357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38357(int& val) {
    const int x = 38357;
    std::cout << 38357;
    for (int i = 0; i < 38357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38358(int& val) {
    func_in_38358(val);
    const int x = 38358;
    std::cout << 38358;
    for (int i = 0; i < 38358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38358(int& val) {
    const int x = 38358;
    std::cout << 38358;
    for (int i = 0; i < 38358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38359(int& val) {
    func_in_38359(val);
    const int x = 38359;
    std::cout << 38359;
    for (int i = 0; i < 38359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38359(int& val) {
    const int x = 38359;
    std::cout << 38359;
    for (int i = 0; i < 38359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38360(int& val) {
    func_in_38360(val);
    const int x = 38360;
    std::cout << 38360;
    for (int i = 0; i < 38360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38360(int& val) {
    const int x = 38360;
    std::cout << 38360;
    for (int i = 0; i < 38360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38361(int& val) {
    func_in_38361(val);
    const int x = 38361;
    std::cout << 38361;
    for (int i = 0; i < 38361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38361(int& val) {
    const int x = 38361;
    std::cout << 38361;
    for (int i = 0; i < 38361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38362(int& val) {
    func_in_38362(val);
    const int x = 38362;
    std::cout << 38362;
    for (int i = 0; i < 38362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38362(int& val) {
    const int x = 38362;
    std::cout << 38362;
    for (int i = 0; i < 38362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38363(int& val) {
    func_in_38363(val);
    const int x = 38363;
    std::cout << 38363;
    for (int i = 0; i < 38363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38363(int& val) {
    const int x = 38363;
    std::cout << 38363;
    for (int i = 0; i < 38363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38364(int& val) {
    func_in_38364(val);
    const int x = 38364;
    std::cout << 38364;
    for (int i = 0; i < 38364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38364(int& val) {
    const int x = 38364;
    std::cout << 38364;
    for (int i = 0; i < 38364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38365(int& val) {
    func_in_38365(val);
    const int x = 38365;
    std::cout << 38365;
    for (int i = 0; i < 38365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38365(int& val) {
    const int x = 38365;
    std::cout << 38365;
    for (int i = 0; i < 38365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38366(int& val) {
    func_in_38366(val);
    const int x = 38366;
    std::cout << 38366;
    for (int i = 0; i < 38366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38366(int& val) {
    const int x = 38366;
    std::cout << 38366;
    for (int i = 0; i < 38366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38367(int& val) {
    func_in_38367(val);
    const int x = 38367;
    std::cout << 38367;
    for (int i = 0; i < 38367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38367(int& val) {
    const int x = 38367;
    std::cout << 38367;
    for (int i = 0; i < 38367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38368(int& val) {
    func_in_38368(val);
    const int x = 38368;
    std::cout << 38368;
    for (int i = 0; i < 38368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38368(int& val) {
    const int x = 38368;
    std::cout << 38368;
    for (int i = 0; i < 38368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38369(int& val) {
    func_in_38369(val);
    const int x = 38369;
    std::cout << 38369;
    for (int i = 0; i < 38369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38369(int& val) {
    const int x = 38369;
    std::cout << 38369;
    for (int i = 0; i < 38369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38370(int& val) {
    func_in_38370(val);
    const int x = 38370;
    std::cout << 38370;
    for (int i = 0; i < 38370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38370(int& val) {
    const int x = 38370;
    std::cout << 38370;
    for (int i = 0; i < 38370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38371(int& val) {
    func_in_38371(val);
    const int x = 38371;
    std::cout << 38371;
    for (int i = 0; i < 38371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38371(int& val) {
    const int x = 38371;
    std::cout << 38371;
    for (int i = 0; i < 38371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38372(int& val) {
    func_in_38372(val);
    const int x = 38372;
    std::cout << 38372;
    for (int i = 0; i < 38372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38372(int& val) {
    const int x = 38372;
    std::cout << 38372;
    for (int i = 0; i < 38372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38373(int& val) {
    func_in_38373(val);
    const int x = 38373;
    std::cout << 38373;
    for (int i = 0; i < 38373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38373(int& val) {
    const int x = 38373;
    std::cout << 38373;
    for (int i = 0; i < 38373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38374(int& val) {
    func_in_38374(val);
    const int x = 38374;
    std::cout << 38374;
    for (int i = 0; i < 38374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38374(int& val) {
    const int x = 38374;
    std::cout << 38374;
    for (int i = 0; i < 38374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38375(int& val) {
    func_in_38375(val);
    const int x = 38375;
    std::cout << 38375;
    for (int i = 0; i < 38375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38375(int& val) {
    const int x = 38375;
    std::cout << 38375;
    for (int i = 0; i < 38375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38376(int& val) {
    func_in_38376(val);
    const int x = 38376;
    std::cout << 38376;
    for (int i = 0; i < 38376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38376(int& val) {
    const int x = 38376;
    std::cout << 38376;
    for (int i = 0; i < 38376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38377(int& val) {
    func_in_38377(val);
    const int x = 38377;
    std::cout << 38377;
    for (int i = 0; i < 38377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38377(int& val) {
    const int x = 38377;
    std::cout << 38377;
    for (int i = 0; i < 38377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38378(int& val) {
    func_in_38378(val);
    const int x = 38378;
    std::cout << 38378;
    for (int i = 0; i < 38378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38378(int& val) {
    const int x = 38378;
    std::cout << 38378;
    for (int i = 0; i < 38378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38379(int& val) {
    func_in_38379(val);
    const int x = 38379;
    std::cout << 38379;
    for (int i = 0; i < 38379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38379(int& val) {
    const int x = 38379;
    std::cout << 38379;
    for (int i = 0; i < 38379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38380(int& val) {
    func_in_38380(val);
    const int x = 38380;
    std::cout << 38380;
    for (int i = 0; i < 38380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38380(int& val) {
    const int x = 38380;
    std::cout << 38380;
    for (int i = 0; i < 38380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38381(int& val) {
    func_in_38381(val);
    const int x = 38381;
    std::cout << 38381;
    for (int i = 0; i < 38381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38381(int& val) {
    const int x = 38381;
    std::cout << 38381;
    for (int i = 0; i < 38381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38382(int& val) {
    func_in_38382(val);
    const int x = 38382;
    std::cout << 38382;
    for (int i = 0; i < 38382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38382(int& val) {
    const int x = 38382;
    std::cout << 38382;
    for (int i = 0; i < 38382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38383(int& val) {
    func_in_38383(val);
    const int x = 38383;
    std::cout << 38383;
    for (int i = 0; i < 38383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38383(int& val) {
    const int x = 38383;
    std::cout << 38383;
    for (int i = 0; i < 38383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38384(int& val) {
    func_in_38384(val);
    const int x = 38384;
    std::cout << 38384;
    for (int i = 0; i < 38384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38384(int& val) {
    const int x = 38384;
    std::cout << 38384;
    for (int i = 0; i < 38384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38385(int& val) {
    func_in_38385(val);
    const int x = 38385;
    std::cout << 38385;
    for (int i = 0; i < 38385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38385(int& val) {
    const int x = 38385;
    std::cout << 38385;
    for (int i = 0; i < 38385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38386(int& val) {
    func_in_38386(val);
    const int x = 38386;
    std::cout << 38386;
    for (int i = 0; i < 38386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38386(int& val) {
    const int x = 38386;
    std::cout << 38386;
    for (int i = 0; i < 38386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38387(int& val) {
    func_in_38387(val);
    const int x = 38387;
    std::cout << 38387;
    for (int i = 0; i < 38387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38387(int& val) {
    const int x = 38387;
    std::cout << 38387;
    for (int i = 0; i < 38387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38388(int& val) {
    func_in_38388(val);
    const int x = 38388;
    std::cout << 38388;
    for (int i = 0; i < 38388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38388(int& val) {
    const int x = 38388;
    std::cout << 38388;
    for (int i = 0; i < 38388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38389(int& val) {
    func_in_38389(val);
    const int x = 38389;
    std::cout << 38389;
    for (int i = 0; i < 38389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38389(int& val) {
    const int x = 38389;
    std::cout << 38389;
    for (int i = 0; i < 38389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38390(int& val) {
    func_in_38390(val);
    const int x = 38390;
    std::cout << 38390;
    for (int i = 0; i < 38390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38390(int& val) {
    const int x = 38390;
    std::cout << 38390;
    for (int i = 0; i < 38390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38391(int& val) {
    func_in_38391(val);
    const int x = 38391;
    std::cout << 38391;
    for (int i = 0; i < 38391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38391(int& val) {
    const int x = 38391;
    std::cout << 38391;
    for (int i = 0; i < 38391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38392(int& val) {
    func_in_38392(val);
    const int x = 38392;
    std::cout << 38392;
    for (int i = 0; i < 38392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38392(int& val) {
    const int x = 38392;
    std::cout << 38392;
    for (int i = 0; i < 38392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38393(int& val) {
    func_in_38393(val);
    const int x = 38393;
    std::cout << 38393;
    for (int i = 0; i < 38393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38393(int& val) {
    const int x = 38393;
    std::cout << 38393;
    for (int i = 0; i < 38393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38394(int& val) {
    func_in_38394(val);
    const int x = 38394;
    std::cout << 38394;
    for (int i = 0; i < 38394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38394(int& val) {
    const int x = 38394;
    std::cout << 38394;
    for (int i = 0; i < 38394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38395(int& val) {
    func_in_38395(val);
    const int x = 38395;
    std::cout << 38395;
    for (int i = 0; i < 38395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38395(int& val) {
    const int x = 38395;
    std::cout << 38395;
    for (int i = 0; i < 38395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38396(int& val) {
    func_in_38396(val);
    const int x = 38396;
    std::cout << 38396;
    for (int i = 0; i < 38396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38396(int& val) {
    const int x = 38396;
    std::cout << 38396;
    for (int i = 0; i < 38396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38397(int& val) {
    func_in_38397(val);
    const int x = 38397;
    std::cout << 38397;
    for (int i = 0; i < 38397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38397(int& val) {
    const int x = 38397;
    std::cout << 38397;
    for (int i = 0; i < 38397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38398(int& val) {
    func_in_38398(val);
    const int x = 38398;
    std::cout << 38398;
    for (int i = 0; i < 38398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38398(int& val) {
    const int x = 38398;
    std::cout << 38398;
    for (int i = 0; i < 38398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38399(int& val) {
    func_in_38399(val);
    const int x = 38399;
    std::cout << 38399;
    for (int i = 0; i < 38399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38399(int& val) {
    const int x = 38399;
    std::cout << 38399;
    for (int i = 0; i < 38399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38400(int& val) {
    func_in_38400(val);
    const int x = 38400;
    std::cout << 38400;
    for (int i = 0; i < 38400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38400(int& val) {
    const int x = 38400;
    std::cout << 38400;
    for (int i = 0; i < 38400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38401(int& val) {
    func_in_38401(val);
    const int x = 38401;
    std::cout << 38401;
    for (int i = 0; i < 38401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38401(int& val) {
    const int x = 38401;
    std::cout << 38401;
    for (int i = 0; i < 38401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38402(int& val) {
    func_in_38402(val);
    const int x = 38402;
    std::cout << 38402;
    for (int i = 0; i < 38402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38402(int& val) {
    const int x = 38402;
    std::cout << 38402;
    for (int i = 0; i < 38402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38403(int& val) {
    func_in_38403(val);
    const int x = 38403;
    std::cout << 38403;
    for (int i = 0; i < 38403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38403(int& val) {
    const int x = 38403;
    std::cout << 38403;
    for (int i = 0; i < 38403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38404(int& val) {
    func_in_38404(val);
    const int x = 38404;
    std::cout << 38404;
    for (int i = 0; i < 38404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38404(int& val) {
    const int x = 38404;
    std::cout << 38404;
    for (int i = 0; i < 38404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38405(int& val) {
    func_in_38405(val);
    const int x = 38405;
    std::cout << 38405;
    for (int i = 0; i < 38405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38405(int& val) {
    const int x = 38405;
    std::cout << 38405;
    for (int i = 0; i < 38405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38406(int& val) {
    func_in_38406(val);
    const int x = 38406;
    std::cout << 38406;
    for (int i = 0; i < 38406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38406(int& val) {
    const int x = 38406;
    std::cout << 38406;
    for (int i = 0; i < 38406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38407(int& val) {
    func_in_38407(val);
    const int x = 38407;
    std::cout << 38407;
    for (int i = 0; i < 38407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38407(int& val) {
    const int x = 38407;
    std::cout << 38407;
    for (int i = 0; i < 38407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38408(int& val) {
    func_in_38408(val);
    const int x = 38408;
    std::cout << 38408;
    for (int i = 0; i < 38408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38408(int& val) {
    const int x = 38408;
    std::cout << 38408;
    for (int i = 0; i < 38408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38409(int& val) {
    func_in_38409(val);
    const int x = 38409;
    std::cout << 38409;
    for (int i = 0; i < 38409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38409(int& val) {
    const int x = 38409;
    std::cout << 38409;
    for (int i = 0; i < 38409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38410(int& val) {
    func_in_38410(val);
    const int x = 38410;
    std::cout << 38410;
    for (int i = 0; i < 38410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38410(int& val) {
    const int x = 38410;
    std::cout << 38410;
    for (int i = 0; i < 38410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38411(int& val) {
    func_in_38411(val);
    const int x = 38411;
    std::cout << 38411;
    for (int i = 0; i < 38411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38411(int& val) {
    const int x = 38411;
    std::cout << 38411;
    for (int i = 0; i < 38411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38412(int& val) {
    func_in_38412(val);
    const int x = 38412;
    std::cout << 38412;
    for (int i = 0; i < 38412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38412(int& val) {
    const int x = 38412;
    std::cout << 38412;
    for (int i = 0; i < 38412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38413(int& val) {
    func_in_38413(val);
    const int x = 38413;
    std::cout << 38413;
    for (int i = 0; i < 38413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38413(int& val) {
    const int x = 38413;
    std::cout << 38413;
    for (int i = 0; i < 38413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38414(int& val) {
    func_in_38414(val);
    const int x = 38414;
    std::cout << 38414;
    for (int i = 0; i < 38414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38414(int& val) {
    const int x = 38414;
    std::cout << 38414;
    for (int i = 0; i < 38414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38415(int& val) {
    func_in_38415(val);
    const int x = 38415;
    std::cout << 38415;
    for (int i = 0; i < 38415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38415(int& val) {
    const int x = 38415;
    std::cout << 38415;
    for (int i = 0; i < 38415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38416(int& val) {
    func_in_38416(val);
    const int x = 38416;
    std::cout << 38416;
    for (int i = 0; i < 38416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38416(int& val) {
    const int x = 38416;
    std::cout << 38416;
    for (int i = 0; i < 38416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38417(int& val) {
    func_in_38417(val);
    const int x = 38417;
    std::cout << 38417;
    for (int i = 0; i < 38417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38417(int& val) {
    const int x = 38417;
    std::cout << 38417;
    for (int i = 0; i < 38417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38418(int& val) {
    func_in_38418(val);
    const int x = 38418;
    std::cout << 38418;
    for (int i = 0; i < 38418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38418(int& val) {
    const int x = 38418;
    std::cout << 38418;
    for (int i = 0; i < 38418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38419(int& val) {
    func_in_38419(val);
    const int x = 38419;
    std::cout << 38419;
    for (int i = 0; i < 38419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38419(int& val) {
    const int x = 38419;
    std::cout << 38419;
    for (int i = 0; i < 38419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38420(int& val) {
    func_in_38420(val);
    const int x = 38420;
    std::cout << 38420;
    for (int i = 0; i < 38420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38420(int& val) {
    const int x = 38420;
    std::cout << 38420;
    for (int i = 0; i < 38420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38421(int& val) {
    func_in_38421(val);
    const int x = 38421;
    std::cout << 38421;
    for (int i = 0; i < 38421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38421(int& val) {
    const int x = 38421;
    std::cout << 38421;
    for (int i = 0; i < 38421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38422(int& val) {
    func_in_38422(val);
    const int x = 38422;
    std::cout << 38422;
    for (int i = 0; i < 38422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38422(int& val) {
    const int x = 38422;
    std::cout << 38422;
    for (int i = 0; i < 38422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38423(int& val) {
    func_in_38423(val);
    const int x = 38423;
    std::cout << 38423;
    for (int i = 0; i < 38423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38423(int& val) {
    const int x = 38423;
    std::cout << 38423;
    for (int i = 0; i < 38423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38424(int& val) {
    func_in_38424(val);
    const int x = 38424;
    std::cout << 38424;
    for (int i = 0; i < 38424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38424(int& val) {
    const int x = 38424;
    std::cout << 38424;
    for (int i = 0; i < 38424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38425(int& val) {
    func_in_38425(val);
    const int x = 38425;
    std::cout << 38425;
    for (int i = 0; i < 38425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38425(int& val) {
    const int x = 38425;
    std::cout << 38425;
    for (int i = 0; i < 38425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38426(int& val) {
    func_in_38426(val);
    const int x = 38426;
    std::cout << 38426;
    for (int i = 0; i < 38426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38426(int& val) {
    const int x = 38426;
    std::cout << 38426;
    for (int i = 0; i < 38426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38427(int& val) {
    func_in_38427(val);
    const int x = 38427;
    std::cout << 38427;
    for (int i = 0; i < 38427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38427(int& val) {
    const int x = 38427;
    std::cout << 38427;
    for (int i = 0; i < 38427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38428(int& val) {
    func_in_38428(val);
    const int x = 38428;
    std::cout << 38428;
    for (int i = 0; i < 38428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38428(int& val) {
    const int x = 38428;
    std::cout << 38428;
    for (int i = 0; i < 38428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38429(int& val) {
    func_in_38429(val);
    const int x = 38429;
    std::cout << 38429;
    for (int i = 0; i < 38429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38429(int& val) {
    const int x = 38429;
    std::cout << 38429;
    for (int i = 0; i < 38429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38430(int& val) {
    func_in_38430(val);
    const int x = 38430;
    std::cout << 38430;
    for (int i = 0; i < 38430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38430(int& val) {
    const int x = 38430;
    std::cout << 38430;
    for (int i = 0; i < 38430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38431(int& val) {
    func_in_38431(val);
    const int x = 38431;
    std::cout << 38431;
    for (int i = 0; i < 38431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38431(int& val) {
    const int x = 38431;
    std::cout << 38431;
    for (int i = 0; i < 38431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38432(int& val) {
    func_in_38432(val);
    const int x = 38432;
    std::cout << 38432;
    for (int i = 0; i < 38432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38432(int& val) {
    const int x = 38432;
    std::cout << 38432;
    for (int i = 0; i < 38432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38433(int& val) {
    func_in_38433(val);
    const int x = 38433;
    std::cout << 38433;
    for (int i = 0; i < 38433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38433(int& val) {
    const int x = 38433;
    std::cout << 38433;
    for (int i = 0; i < 38433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38434(int& val) {
    func_in_38434(val);
    const int x = 38434;
    std::cout << 38434;
    for (int i = 0; i < 38434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38434(int& val) {
    const int x = 38434;
    std::cout << 38434;
    for (int i = 0; i < 38434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38435(int& val) {
    func_in_38435(val);
    const int x = 38435;
    std::cout << 38435;
    for (int i = 0; i < 38435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38435(int& val) {
    const int x = 38435;
    std::cout << 38435;
    for (int i = 0; i < 38435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38436(int& val) {
    func_in_38436(val);
    const int x = 38436;
    std::cout << 38436;
    for (int i = 0; i < 38436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38436(int& val) {
    const int x = 38436;
    std::cout << 38436;
    for (int i = 0; i < 38436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38437(int& val) {
    func_in_38437(val);
    const int x = 38437;
    std::cout << 38437;
    for (int i = 0; i < 38437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38437(int& val) {
    const int x = 38437;
    std::cout << 38437;
    for (int i = 0; i < 38437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38438(int& val) {
    func_in_38438(val);
    const int x = 38438;
    std::cout << 38438;
    for (int i = 0; i < 38438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38438(int& val) {
    const int x = 38438;
    std::cout << 38438;
    for (int i = 0; i < 38438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38439(int& val) {
    func_in_38439(val);
    const int x = 38439;
    std::cout << 38439;
    for (int i = 0; i < 38439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38439(int& val) {
    const int x = 38439;
    std::cout << 38439;
    for (int i = 0; i < 38439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38440(int& val) {
    func_in_38440(val);
    const int x = 38440;
    std::cout << 38440;
    for (int i = 0; i < 38440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38440(int& val) {
    const int x = 38440;
    std::cout << 38440;
    for (int i = 0; i < 38440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38441(int& val) {
    func_in_38441(val);
    const int x = 38441;
    std::cout << 38441;
    for (int i = 0; i < 38441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38441(int& val) {
    const int x = 38441;
    std::cout << 38441;
    for (int i = 0; i < 38441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38442(int& val) {
    func_in_38442(val);
    const int x = 38442;
    std::cout << 38442;
    for (int i = 0; i < 38442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38442(int& val) {
    const int x = 38442;
    std::cout << 38442;
    for (int i = 0; i < 38442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38443(int& val) {
    func_in_38443(val);
    const int x = 38443;
    std::cout << 38443;
    for (int i = 0; i < 38443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38443(int& val) {
    const int x = 38443;
    std::cout << 38443;
    for (int i = 0; i < 38443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38444(int& val) {
    func_in_38444(val);
    const int x = 38444;
    std::cout << 38444;
    for (int i = 0; i < 38444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38444(int& val) {
    const int x = 38444;
    std::cout << 38444;
    for (int i = 0; i < 38444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38445(int& val) {
    func_in_38445(val);
    const int x = 38445;
    std::cout << 38445;
    for (int i = 0; i < 38445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38445(int& val) {
    const int x = 38445;
    std::cout << 38445;
    for (int i = 0; i < 38445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38446(int& val) {
    func_in_38446(val);
    const int x = 38446;
    std::cout << 38446;
    for (int i = 0; i < 38446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38446(int& val) {
    const int x = 38446;
    std::cout << 38446;
    for (int i = 0; i < 38446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38447(int& val) {
    func_in_38447(val);
    const int x = 38447;
    std::cout << 38447;
    for (int i = 0; i < 38447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38447(int& val) {
    const int x = 38447;
    std::cout << 38447;
    for (int i = 0; i < 38447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38448(int& val) {
    func_in_38448(val);
    const int x = 38448;
    std::cout << 38448;
    for (int i = 0; i < 38448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38448(int& val) {
    const int x = 38448;
    std::cout << 38448;
    for (int i = 0; i < 38448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38449(int& val) {
    func_in_38449(val);
    const int x = 38449;
    std::cout << 38449;
    for (int i = 0; i < 38449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38449(int& val) {
    const int x = 38449;
    std::cout << 38449;
    for (int i = 0; i < 38449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38450(int& val) {
    func_in_38450(val);
    const int x = 38450;
    std::cout << 38450;
    for (int i = 0; i < 38450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38450(int& val) {
    const int x = 38450;
    std::cout << 38450;
    for (int i = 0; i < 38450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38451(int& val) {
    func_in_38451(val);
    const int x = 38451;
    std::cout << 38451;
    for (int i = 0; i < 38451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38451(int& val) {
    const int x = 38451;
    std::cout << 38451;
    for (int i = 0; i < 38451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38452(int& val) {
    func_in_38452(val);
    const int x = 38452;
    std::cout << 38452;
    for (int i = 0; i < 38452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38452(int& val) {
    const int x = 38452;
    std::cout << 38452;
    for (int i = 0; i < 38452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38453(int& val) {
    func_in_38453(val);
    const int x = 38453;
    std::cout << 38453;
    for (int i = 0; i < 38453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38453(int& val) {
    const int x = 38453;
    std::cout << 38453;
    for (int i = 0; i < 38453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38454(int& val) {
    func_in_38454(val);
    const int x = 38454;
    std::cout << 38454;
    for (int i = 0; i < 38454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38454(int& val) {
    const int x = 38454;
    std::cout << 38454;
    for (int i = 0; i < 38454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38455(int& val) {
    func_in_38455(val);
    const int x = 38455;
    std::cout << 38455;
    for (int i = 0; i < 38455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38455(int& val) {
    const int x = 38455;
    std::cout << 38455;
    for (int i = 0; i < 38455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38456(int& val) {
    func_in_38456(val);
    const int x = 38456;
    std::cout << 38456;
    for (int i = 0; i < 38456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38456(int& val) {
    const int x = 38456;
    std::cout << 38456;
    for (int i = 0; i < 38456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38457(int& val) {
    func_in_38457(val);
    const int x = 38457;
    std::cout << 38457;
    for (int i = 0; i < 38457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38457(int& val) {
    const int x = 38457;
    std::cout << 38457;
    for (int i = 0; i < 38457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38458(int& val) {
    func_in_38458(val);
    const int x = 38458;
    std::cout << 38458;
    for (int i = 0; i < 38458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38458(int& val) {
    const int x = 38458;
    std::cout << 38458;
    for (int i = 0; i < 38458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38459(int& val) {
    func_in_38459(val);
    const int x = 38459;
    std::cout << 38459;
    for (int i = 0; i < 38459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38459(int& val) {
    const int x = 38459;
    std::cout << 38459;
    for (int i = 0; i < 38459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38460(int& val) {
    func_in_38460(val);
    const int x = 38460;
    std::cout << 38460;
    for (int i = 0; i < 38460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38460(int& val) {
    const int x = 38460;
    std::cout << 38460;
    for (int i = 0; i < 38460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38461(int& val) {
    func_in_38461(val);
    const int x = 38461;
    std::cout << 38461;
    for (int i = 0; i < 38461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38461(int& val) {
    const int x = 38461;
    std::cout << 38461;
    for (int i = 0; i < 38461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38462(int& val) {
    func_in_38462(val);
    const int x = 38462;
    std::cout << 38462;
    for (int i = 0; i < 38462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38462(int& val) {
    const int x = 38462;
    std::cout << 38462;
    for (int i = 0; i < 38462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38463(int& val) {
    func_in_38463(val);
    const int x = 38463;
    std::cout << 38463;
    for (int i = 0; i < 38463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38463(int& val) {
    const int x = 38463;
    std::cout << 38463;
    for (int i = 0; i < 38463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38464(int& val) {
    func_in_38464(val);
    const int x = 38464;
    std::cout << 38464;
    for (int i = 0; i < 38464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38464(int& val) {
    const int x = 38464;
    std::cout << 38464;
    for (int i = 0; i < 38464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38465(int& val) {
    func_in_38465(val);
    const int x = 38465;
    std::cout << 38465;
    for (int i = 0; i < 38465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38465(int& val) {
    const int x = 38465;
    std::cout << 38465;
    for (int i = 0; i < 38465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38466(int& val) {
    func_in_38466(val);
    const int x = 38466;
    std::cout << 38466;
    for (int i = 0; i < 38466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38466(int& val) {
    const int x = 38466;
    std::cout << 38466;
    for (int i = 0; i < 38466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38467(int& val) {
    func_in_38467(val);
    const int x = 38467;
    std::cout << 38467;
    for (int i = 0; i < 38467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38467(int& val) {
    const int x = 38467;
    std::cout << 38467;
    for (int i = 0; i < 38467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38468(int& val) {
    func_in_38468(val);
    const int x = 38468;
    std::cout << 38468;
    for (int i = 0; i < 38468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38468(int& val) {
    const int x = 38468;
    std::cout << 38468;
    for (int i = 0; i < 38468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38469(int& val) {
    func_in_38469(val);
    const int x = 38469;
    std::cout << 38469;
    for (int i = 0; i < 38469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38469(int& val) {
    const int x = 38469;
    std::cout << 38469;
    for (int i = 0; i < 38469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38470(int& val) {
    func_in_38470(val);
    const int x = 38470;
    std::cout << 38470;
    for (int i = 0; i < 38470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38470(int& val) {
    const int x = 38470;
    std::cout << 38470;
    for (int i = 0; i < 38470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38471(int& val) {
    func_in_38471(val);
    const int x = 38471;
    std::cout << 38471;
    for (int i = 0; i < 38471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38471(int& val) {
    const int x = 38471;
    std::cout << 38471;
    for (int i = 0; i < 38471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38472(int& val) {
    func_in_38472(val);
    const int x = 38472;
    std::cout << 38472;
    for (int i = 0; i < 38472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38472(int& val) {
    const int x = 38472;
    std::cout << 38472;
    for (int i = 0; i < 38472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38473(int& val) {
    func_in_38473(val);
    const int x = 38473;
    std::cout << 38473;
    for (int i = 0; i < 38473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38473(int& val) {
    const int x = 38473;
    std::cout << 38473;
    for (int i = 0; i < 38473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38474(int& val) {
    func_in_38474(val);
    const int x = 38474;
    std::cout << 38474;
    for (int i = 0; i < 38474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38474(int& val) {
    const int x = 38474;
    std::cout << 38474;
    for (int i = 0; i < 38474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38475(int& val) {
    func_in_38475(val);
    const int x = 38475;
    std::cout << 38475;
    for (int i = 0; i < 38475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38475(int& val) {
    const int x = 38475;
    std::cout << 38475;
    for (int i = 0; i < 38475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38476(int& val) {
    func_in_38476(val);
    const int x = 38476;
    std::cout << 38476;
    for (int i = 0; i < 38476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38476(int& val) {
    const int x = 38476;
    std::cout << 38476;
    for (int i = 0; i < 38476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38477(int& val) {
    func_in_38477(val);
    const int x = 38477;
    std::cout << 38477;
    for (int i = 0; i < 38477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38477(int& val) {
    const int x = 38477;
    std::cout << 38477;
    for (int i = 0; i < 38477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38478(int& val) {
    func_in_38478(val);
    const int x = 38478;
    std::cout << 38478;
    for (int i = 0; i < 38478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38478(int& val) {
    const int x = 38478;
    std::cout << 38478;
    for (int i = 0; i < 38478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38479(int& val) {
    func_in_38479(val);
    const int x = 38479;
    std::cout << 38479;
    for (int i = 0; i < 38479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38479(int& val) {
    const int x = 38479;
    std::cout << 38479;
    for (int i = 0; i < 38479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38480(int& val) {
    func_in_38480(val);
    const int x = 38480;
    std::cout << 38480;
    for (int i = 0; i < 38480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38480(int& val) {
    const int x = 38480;
    std::cout << 38480;
    for (int i = 0; i < 38480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38481(int& val) {
    func_in_38481(val);
    const int x = 38481;
    std::cout << 38481;
    for (int i = 0; i < 38481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38481(int& val) {
    const int x = 38481;
    std::cout << 38481;
    for (int i = 0; i < 38481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38482(int& val) {
    func_in_38482(val);
    const int x = 38482;
    std::cout << 38482;
    for (int i = 0; i < 38482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38482(int& val) {
    const int x = 38482;
    std::cout << 38482;
    for (int i = 0; i < 38482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38483(int& val) {
    func_in_38483(val);
    const int x = 38483;
    std::cout << 38483;
    for (int i = 0; i < 38483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38483(int& val) {
    const int x = 38483;
    std::cout << 38483;
    for (int i = 0; i < 38483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38484(int& val) {
    func_in_38484(val);
    const int x = 38484;
    std::cout << 38484;
    for (int i = 0; i < 38484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38484(int& val) {
    const int x = 38484;
    std::cout << 38484;
    for (int i = 0; i < 38484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38485(int& val) {
    func_in_38485(val);
    const int x = 38485;
    std::cout << 38485;
    for (int i = 0; i < 38485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38485(int& val) {
    const int x = 38485;
    std::cout << 38485;
    for (int i = 0; i < 38485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38486(int& val) {
    func_in_38486(val);
    const int x = 38486;
    std::cout << 38486;
    for (int i = 0; i < 38486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38486(int& val) {
    const int x = 38486;
    std::cout << 38486;
    for (int i = 0; i < 38486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38487(int& val) {
    func_in_38487(val);
    const int x = 38487;
    std::cout << 38487;
    for (int i = 0; i < 38487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38487(int& val) {
    const int x = 38487;
    std::cout << 38487;
    for (int i = 0; i < 38487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38488(int& val) {
    func_in_38488(val);
    const int x = 38488;
    std::cout << 38488;
    for (int i = 0; i < 38488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38488(int& val) {
    const int x = 38488;
    std::cout << 38488;
    for (int i = 0; i < 38488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38489(int& val) {
    func_in_38489(val);
    const int x = 38489;
    std::cout << 38489;
    for (int i = 0; i < 38489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38489(int& val) {
    const int x = 38489;
    std::cout << 38489;
    for (int i = 0; i < 38489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38490(int& val) {
    func_in_38490(val);
    const int x = 38490;
    std::cout << 38490;
    for (int i = 0; i < 38490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38490(int& val) {
    const int x = 38490;
    std::cout << 38490;
    for (int i = 0; i < 38490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38491(int& val) {
    func_in_38491(val);
    const int x = 38491;
    std::cout << 38491;
    for (int i = 0; i < 38491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38491(int& val) {
    const int x = 38491;
    std::cout << 38491;
    for (int i = 0; i < 38491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38492(int& val) {
    func_in_38492(val);
    const int x = 38492;
    std::cout << 38492;
    for (int i = 0; i < 38492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38492(int& val) {
    const int x = 38492;
    std::cout << 38492;
    for (int i = 0; i < 38492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38493(int& val) {
    func_in_38493(val);
    const int x = 38493;
    std::cout << 38493;
    for (int i = 0; i < 38493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38493(int& val) {
    const int x = 38493;
    std::cout << 38493;
    for (int i = 0; i < 38493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38494(int& val) {
    func_in_38494(val);
    const int x = 38494;
    std::cout << 38494;
    for (int i = 0; i < 38494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38494(int& val) {
    const int x = 38494;
    std::cout << 38494;
    for (int i = 0; i < 38494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38495(int& val) {
    func_in_38495(val);
    const int x = 38495;
    std::cout << 38495;
    for (int i = 0; i < 38495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38495(int& val) {
    const int x = 38495;
    std::cout << 38495;
    for (int i = 0; i < 38495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38496(int& val) {
    func_in_38496(val);
    const int x = 38496;
    std::cout << 38496;
    for (int i = 0; i < 38496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38496(int& val) {
    const int x = 38496;
    std::cout << 38496;
    for (int i = 0; i < 38496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38497(int& val) {
    func_in_38497(val);
    const int x = 38497;
    std::cout << 38497;
    for (int i = 0; i < 38497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38497(int& val) {
    const int x = 38497;
    std::cout << 38497;
    for (int i = 0; i < 38497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38498(int& val) {
    func_in_38498(val);
    const int x = 38498;
    std::cout << 38498;
    for (int i = 0; i < 38498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38498(int& val) {
    const int x = 38498;
    std::cout << 38498;
    for (int i = 0; i < 38498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38499(int& val) {
    func_in_38499(val);
    const int x = 38499;
    std::cout << 38499;
    for (int i = 0; i < 38499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38499(int& val) {
    const int x = 38499;
    std::cout << 38499;
    for (int i = 0; i < 38499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38500(int& val) {
    func_in_38500(val);
    const int x = 38500;
    std::cout << 38500;
    for (int i = 0; i < 38500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38500(int& val) {
    const int x = 38500;
    std::cout << 38500;
    for (int i = 0; i < 38500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38501(int& val) {
    func_in_38501(val);
    const int x = 38501;
    std::cout << 38501;
    for (int i = 0; i < 38501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38501(int& val) {
    const int x = 38501;
    std::cout << 38501;
    for (int i = 0; i < 38501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38502(int& val) {
    func_in_38502(val);
    const int x = 38502;
    std::cout << 38502;
    for (int i = 0; i < 38502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38502(int& val) {
    const int x = 38502;
    std::cout << 38502;
    for (int i = 0; i < 38502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38503(int& val) {
    func_in_38503(val);
    const int x = 38503;
    std::cout << 38503;
    for (int i = 0; i < 38503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38503(int& val) {
    const int x = 38503;
    std::cout << 38503;
    for (int i = 0; i < 38503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38504(int& val) {
    func_in_38504(val);
    const int x = 38504;
    std::cout << 38504;
    for (int i = 0; i < 38504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38504(int& val) {
    const int x = 38504;
    std::cout << 38504;
    for (int i = 0; i < 38504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38505(int& val) {
    func_in_38505(val);
    const int x = 38505;
    std::cout << 38505;
    for (int i = 0; i < 38505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38505(int& val) {
    const int x = 38505;
    std::cout << 38505;
    for (int i = 0; i < 38505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38506(int& val) {
    func_in_38506(val);
    const int x = 38506;
    std::cout << 38506;
    for (int i = 0; i < 38506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38506(int& val) {
    const int x = 38506;
    std::cout << 38506;
    for (int i = 0; i < 38506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38507(int& val) {
    func_in_38507(val);
    const int x = 38507;
    std::cout << 38507;
    for (int i = 0; i < 38507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38507(int& val) {
    const int x = 38507;
    std::cout << 38507;
    for (int i = 0; i < 38507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38508(int& val) {
    func_in_38508(val);
    const int x = 38508;
    std::cout << 38508;
    for (int i = 0; i < 38508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38508(int& val) {
    const int x = 38508;
    std::cout << 38508;
    for (int i = 0; i < 38508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38509(int& val) {
    func_in_38509(val);
    const int x = 38509;
    std::cout << 38509;
    for (int i = 0; i < 38509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38509(int& val) {
    const int x = 38509;
    std::cout << 38509;
    for (int i = 0; i < 38509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38510(int& val) {
    func_in_38510(val);
    const int x = 38510;
    std::cout << 38510;
    for (int i = 0; i < 38510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38510(int& val) {
    const int x = 38510;
    std::cout << 38510;
    for (int i = 0; i < 38510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38511(int& val) {
    func_in_38511(val);
    const int x = 38511;
    std::cout << 38511;
    for (int i = 0; i < 38511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38511(int& val) {
    const int x = 38511;
    std::cout << 38511;
    for (int i = 0; i < 38511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38512(int& val) {
    func_in_38512(val);
    const int x = 38512;
    std::cout << 38512;
    for (int i = 0; i < 38512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38512(int& val) {
    const int x = 38512;
    std::cout << 38512;
    for (int i = 0; i < 38512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38513(int& val) {
    func_in_38513(val);
    const int x = 38513;
    std::cout << 38513;
    for (int i = 0; i < 38513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38513(int& val) {
    const int x = 38513;
    std::cout << 38513;
    for (int i = 0; i < 38513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38514(int& val) {
    func_in_38514(val);
    const int x = 38514;
    std::cout << 38514;
    for (int i = 0; i < 38514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38514(int& val) {
    const int x = 38514;
    std::cout << 38514;
    for (int i = 0; i < 38514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38515(int& val) {
    func_in_38515(val);
    const int x = 38515;
    std::cout << 38515;
    for (int i = 0; i < 38515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38515(int& val) {
    const int x = 38515;
    std::cout << 38515;
    for (int i = 0; i < 38515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38516(int& val) {
    func_in_38516(val);
    const int x = 38516;
    std::cout << 38516;
    for (int i = 0; i < 38516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38516(int& val) {
    const int x = 38516;
    std::cout << 38516;
    for (int i = 0; i < 38516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38517(int& val) {
    func_in_38517(val);
    const int x = 38517;
    std::cout << 38517;
    for (int i = 0; i < 38517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38517(int& val) {
    const int x = 38517;
    std::cout << 38517;
    for (int i = 0; i < 38517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38518(int& val) {
    func_in_38518(val);
    const int x = 38518;
    std::cout << 38518;
    for (int i = 0; i < 38518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38518(int& val) {
    const int x = 38518;
    std::cout << 38518;
    for (int i = 0; i < 38518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38519(int& val) {
    func_in_38519(val);
    const int x = 38519;
    std::cout << 38519;
    for (int i = 0; i < 38519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38519(int& val) {
    const int x = 38519;
    std::cout << 38519;
    for (int i = 0; i < 38519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38520(int& val) {
    func_in_38520(val);
    const int x = 38520;
    std::cout << 38520;
    for (int i = 0; i < 38520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38520(int& val) {
    const int x = 38520;
    std::cout << 38520;
    for (int i = 0; i < 38520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38521(int& val) {
    func_in_38521(val);
    const int x = 38521;
    std::cout << 38521;
    for (int i = 0; i < 38521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38521(int& val) {
    const int x = 38521;
    std::cout << 38521;
    for (int i = 0; i < 38521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38522(int& val) {
    func_in_38522(val);
    const int x = 38522;
    std::cout << 38522;
    for (int i = 0; i < 38522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38522(int& val) {
    const int x = 38522;
    std::cout << 38522;
    for (int i = 0; i < 38522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38523(int& val) {
    func_in_38523(val);
    const int x = 38523;
    std::cout << 38523;
    for (int i = 0; i < 38523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38523(int& val) {
    const int x = 38523;
    std::cout << 38523;
    for (int i = 0; i < 38523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38524(int& val) {
    func_in_38524(val);
    const int x = 38524;
    std::cout << 38524;
    for (int i = 0; i < 38524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38524(int& val) {
    const int x = 38524;
    std::cout << 38524;
    for (int i = 0; i < 38524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38525(int& val) {
    func_in_38525(val);
    const int x = 38525;
    std::cout << 38525;
    for (int i = 0; i < 38525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38525(int& val) {
    const int x = 38525;
    std::cout << 38525;
    for (int i = 0; i < 38525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38526(int& val) {
    func_in_38526(val);
    const int x = 38526;
    std::cout << 38526;
    for (int i = 0; i < 38526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38526(int& val) {
    const int x = 38526;
    std::cout << 38526;
    for (int i = 0; i < 38526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38527(int& val) {
    func_in_38527(val);
    const int x = 38527;
    std::cout << 38527;
    for (int i = 0; i < 38527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38527(int& val) {
    const int x = 38527;
    std::cout << 38527;
    for (int i = 0; i < 38527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38528(int& val) {
    func_in_38528(val);
    const int x = 38528;
    std::cout << 38528;
    for (int i = 0; i < 38528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38528(int& val) {
    const int x = 38528;
    std::cout << 38528;
    for (int i = 0; i < 38528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38529(int& val) {
    func_in_38529(val);
    const int x = 38529;
    std::cout << 38529;
    for (int i = 0; i < 38529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38529(int& val) {
    const int x = 38529;
    std::cout << 38529;
    for (int i = 0; i < 38529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38530(int& val) {
    func_in_38530(val);
    const int x = 38530;
    std::cout << 38530;
    for (int i = 0; i < 38530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38530(int& val) {
    const int x = 38530;
    std::cout << 38530;
    for (int i = 0; i < 38530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38531(int& val) {
    func_in_38531(val);
    const int x = 38531;
    std::cout << 38531;
    for (int i = 0; i < 38531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38531(int& val) {
    const int x = 38531;
    std::cout << 38531;
    for (int i = 0; i < 38531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38532(int& val) {
    func_in_38532(val);
    const int x = 38532;
    std::cout << 38532;
    for (int i = 0; i < 38532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38532(int& val) {
    const int x = 38532;
    std::cout << 38532;
    for (int i = 0; i < 38532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38533(int& val) {
    func_in_38533(val);
    const int x = 38533;
    std::cout << 38533;
    for (int i = 0; i < 38533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38533(int& val) {
    const int x = 38533;
    std::cout << 38533;
    for (int i = 0; i < 38533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38534(int& val) {
    func_in_38534(val);
    const int x = 38534;
    std::cout << 38534;
    for (int i = 0; i < 38534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38534(int& val) {
    const int x = 38534;
    std::cout << 38534;
    for (int i = 0; i < 38534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38535(int& val) {
    func_in_38535(val);
    const int x = 38535;
    std::cout << 38535;
    for (int i = 0; i < 38535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38535(int& val) {
    const int x = 38535;
    std::cout << 38535;
    for (int i = 0; i < 38535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38536(int& val) {
    func_in_38536(val);
    const int x = 38536;
    std::cout << 38536;
    for (int i = 0; i < 38536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38536(int& val) {
    const int x = 38536;
    std::cout << 38536;
    for (int i = 0; i < 38536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38537(int& val) {
    func_in_38537(val);
    const int x = 38537;
    std::cout << 38537;
    for (int i = 0; i < 38537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38537(int& val) {
    const int x = 38537;
    std::cout << 38537;
    for (int i = 0; i < 38537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38538(int& val) {
    func_in_38538(val);
    const int x = 38538;
    std::cout << 38538;
    for (int i = 0; i < 38538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38538(int& val) {
    const int x = 38538;
    std::cout << 38538;
    for (int i = 0; i < 38538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38539(int& val) {
    func_in_38539(val);
    const int x = 38539;
    std::cout << 38539;
    for (int i = 0; i < 38539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38539(int& val) {
    const int x = 38539;
    std::cout << 38539;
    for (int i = 0; i < 38539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38540(int& val) {
    func_in_38540(val);
    const int x = 38540;
    std::cout << 38540;
    for (int i = 0; i < 38540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38540(int& val) {
    const int x = 38540;
    std::cout << 38540;
    for (int i = 0; i < 38540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38541(int& val) {
    func_in_38541(val);
    const int x = 38541;
    std::cout << 38541;
    for (int i = 0; i < 38541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38541(int& val) {
    const int x = 38541;
    std::cout << 38541;
    for (int i = 0; i < 38541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38542(int& val) {
    func_in_38542(val);
    const int x = 38542;
    std::cout << 38542;
    for (int i = 0; i < 38542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38542(int& val) {
    const int x = 38542;
    std::cout << 38542;
    for (int i = 0; i < 38542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38543(int& val) {
    func_in_38543(val);
    const int x = 38543;
    std::cout << 38543;
    for (int i = 0; i < 38543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38543(int& val) {
    const int x = 38543;
    std::cout << 38543;
    for (int i = 0; i < 38543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38544(int& val) {
    func_in_38544(val);
    const int x = 38544;
    std::cout << 38544;
    for (int i = 0; i < 38544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38544(int& val) {
    const int x = 38544;
    std::cout << 38544;
    for (int i = 0; i < 38544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38545(int& val) {
    func_in_38545(val);
    const int x = 38545;
    std::cout << 38545;
    for (int i = 0; i < 38545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38545(int& val) {
    const int x = 38545;
    std::cout << 38545;
    for (int i = 0; i < 38545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38546(int& val) {
    func_in_38546(val);
    const int x = 38546;
    std::cout << 38546;
    for (int i = 0; i < 38546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38546(int& val) {
    const int x = 38546;
    std::cout << 38546;
    for (int i = 0; i < 38546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38547(int& val) {
    func_in_38547(val);
    const int x = 38547;
    std::cout << 38547;
    for (int i = 0; i < 38547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38547(int& val) {
    const int x = 38547;
    std::cout << 38547;
    for (int i = 0; i < 38547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38548(int& val) {
    func_in_38548(val);
    const int x = 38548;
    std::cout << 38548;
    for (int i = 0; i < 38548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38548(int& val) {
    const int x = 38548;
    std::cout << 38548;
    for (int i = 0; i < 38548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38549(int& val) {
    func_in_38549(val);
    const int x = 38549;
    std::cout << 38549;
    for (int i = 0; i < 38549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38549(int& val) {
    const int x = 38549;
    std::cout << 38549;
    for (int i = 0; i < 38549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38550(int& val) {
    func_in_38550(val);
    const int x = 38550;
    std::cout << 38550;
    for (int i = 0; i < 38550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38550(int& val) {
    const int x = 38550;
    std::cout << 38550;
    for (int i = 0; i < 38550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38551(int& val) {
    func_in_38551(val);
    const int x = 38551;
    std::cout << 38551;
    for (int i = 0; i < 38551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38551(int& val) {
    const int x = 38551;
    std::cout << 38551;
    for (int i = 0; i < 38551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38552(int& val) {
    func_in_38552(val);
    const int x = 38552;
    std::cout << 38552;
    for (int i = 0; i < 38552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38552(int& val) {
    const int x = 38552;
    std::cout << 38552;
    for (int i = 0; i < 38552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38553(int& val) {
    func_in_38553(val);
    const int x = 38553;
    std::cout << 38553;
    for (int i = 0; i < 38553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38553(int& val) {
    const int x = 38553;
    std::cout << 38553;
    for (int i = 0; i < 38553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38554(int& val) {
    func_in_38554(val);
    const int x = 38554;
    std::cout << 38554;
    for (int i = 0; i < 38554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38554(int& val) {
    const int x = 38554;
    std::cout << 38554;
    for (int i = 0; i < 38554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38555(int& val) {
    func_in_38555(val);
    const int x = 38555;
    std::cout << 38555;
    for (int i = 0; i < 38555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38555(int& val) {
    const int x = 38555;
    std::cout << 38555;
    for (int i = 0; i < 38555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38556(int& val) {
    func_in_38556(val);
    const int x = 38556;
    std::cout << 38556;
    for (int i = 0; i < 38556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38556(int& val) {
    const int x = 38556;
    std::cout << 38556;
    for (int i = 0; i < 38556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38557(int& val) {
    func_in_38557(val);
    const int x = 38557;
    std::cout << 38557;
    for (int i = 0; i < 38557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38557(int& val) {
    const int x = 38557;
    std::cout << 38557;
    for (int i = 0; i < 38557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38558(int& val) {
    func_in_38558(val);
    const int x = 38558;
    std::cout << 38558;
    for (int i = 0; i < 38558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38558(int& val) {
    const int x = 38558;
    std::cout << 38558;
    for (int i = 0; i < 38558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38559(int& val) {
    func_in_38559(val);
    const int x = 38559;
    std::cout << 38559;
    for (int i = 0; i < 38559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38559(int& val) {
    const int x = 38559;
    std::cout << 38559;
    for (int i = 0; i < 38559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38560(int& val) {
    func_in_38560(val);
    const int x = 38560;
    std::cout << 38560;
    for (int i = 0; i < 38560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38560(int& val) {
    const int x = 38560;
    std::cout << 38560;
    for (int i = 0; i < 38560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38561(int& val) {
    func_in_38561(val);
    const int x = 38561;
    std::cout << 38561;
    for (int i = 0; i < 38561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38561(int& val) {
    const int x = 38561;
    std::cout << 38561;
    for (int i = 0; i < 38561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38562(int& val) {
    func_in_38562(val);
    const int x = 38562;
    std::cout << 38562;
    for (int i = 0; i < 38562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38562(int& val) {
    const int x = 38562;
    std::cout << 38562;
    for (int i = 0; i < 38562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38563(int& val) {
    func_in_38563(val);
    const int x = 38563;
    std::cout << 38563;
    for (int i = 0; i < 38563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38563(int& val) {
    const int x = 38563;
    std::cout << 38563;
    for (int i = 0; i < 38563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38564(int& val) {
    func_in_38564(val);
    const int x = 38564;
    std::cout << 38564;
    for (int i = 0; i < 38564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38564(int& val) {
    const int x = 38564;
    std::cout << 38564;
    for (int i = 0; i < 38564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38565(int& val) {
    func_in_38565(val);
    const int x = 38565;
    std::cout << 38565;
    for (int i = 0; i < 38565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38565(int& val) {
    const int x = 38565;
    std::cout << 38565;
    for (int i = 0; i < 38565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38566(int& val) {
    func_in_38566(val);
    const int x = 38566;
    std::cout << 38566;
    for (int i = 0; i < 38566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38566(int& val) {
    const int x = 38566;
    std::cout << 38566;
    for (int i = 0; i < 38566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38567(int& val) {
    func_in_38567(val);
    const int x = 38567;
    std::cout << 38567;
    for (int i = 0; i < 38567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38567(int& val) {
    const int x = 38567;
    std::cout << 38567;
    for (int i = 0; i < 38567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38568(int& val) {
    func_in_38568(val);
    const int x = 38568;
    std::cout << 38568;
    for (int i = 0; i < 38568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38568(int& val) {
    const int x = 38568;
    std::cout << 38568;
    for (int i = 0; i < 38568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38569(int& val) {
    func_in_38569(val);
    const int x = 38569;
    std::cout << 38569;
    for (int i = 0; i < 38569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38569(int& val) {
    const int x = 38569;
    std::cout << 38569;
    for (int i = 0; i < 38569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38570(int& val) {
    func_in_38570(val);
    const int x = 38570;
    std::cout << 38570;
    for (int i = 0; i < 38570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38570(int& val) {
    const int x = 38570;
    std::cout << 38570;
    for (int i = 0; i < 38570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38571(int& val) {
    func_in_38571(val);
    const int x = 38571;
    std::cout << 38571;
    for (int i = 0; i < 38571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38571(int& val) {
    const int x = 38571;
    std::cout << 38571;
    for (int i = 0; i < 38571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38572(int& val) {
    func_in_38572(val);
    const int x = 38572;
    std::cout << 38572;
    for (int i = 0; i < 38572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38572(int& val) {
    const int x = 38572;
    std::cout << 38572;
    for (int i = 0; i < 38572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38573(int& val) {
    func_in_38573(val);
    const int x = 38573;
    std::cout << 38573;
    for (int i = 0; i < 38573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38573(int& val) {
    const int x = 38573;
    std::cout << 38573;
    for (int i = 0; i < 38573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38574(int& val) {
    func_in_38574(val);
    const int x = 38574;
    std::cout << 38574;
    for (int i = 0; i < 38574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38574(int& val) {
    const int x = 38574;
    std::cout << 38574;
    for (int i = 0; i < 38574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38575(int& val) {
    func_in_38575(val);
    const int x = 38575;
    std::cout << 38575;
    for (int i = 0; i < 38575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38575(int& val) {
    const int x = 38575;
    std::cout << 38575;
    for (int i = 0; i < 38575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38576(int& val) {
    func_in_38576(val);
    const int x = 38576;
    std::cout << 38576;
    for (int i = 0; i < 38576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38576(int& val) {
    const int x = 38576;
    std::cout << 38576;
    for (int i = 0; i < 38576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38577(int& val) {
    func_in_38577(val);
    const int x = 38577;
    std::cout << 38577;
    for (int i = 0; i < 38577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38577(int& val) {
    const int x = 38577;
    std::cout << 38577;
    for (int i = 0; i < 38577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38578(int& val) {
    func_in_38578(val);
    const int x = 38578;
    std::cout << 38578;
    for (int i = 0; i < 38578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38578(int& val) {
    const int x = 38578;
    std::cout << 38578;
    for (int i = 0; i < 38578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38579(int& val) {
    func_in_38579(val);
    const int x = 38579;
    std::cout << 38579;
    for (int i = 0; i < 38579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38579(int& val) {
    const int x = 38579;
    std::cout << 38579;
    for (int i = 0; i < 38579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38580(int& val) {
    func_in_38580(val);
    const int x = 38580;
    std::cout << 38580;
    for (int i = 0; i < 38580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38580(int& val) {
    const int x = 38580;
    std::cout << 38580;
    for (int i = 0; i < 38580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38581(int& val) {
    func_in_38581(val);
    const int x = 38581;
    std::cout << 38581;
    for (int i = 0; i < 38581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38581(int& val) {
    const int x = 38581;
    std::cout << 38581;
    for (int i = 0; i < 38581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38582(int& val) {
    func_in_38582(val);
    const int x = 38582;
    std::cout << 38582;
    for (int i = 0; i < 38582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38582(int& val) {
    const int x = 38582;
    std::cout << 38582;
    for (int i = 0; i < 38582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38583(int& val) {
    func_in_38583(val);
    const int x = 38583;
    std::cout << 38583;
    for (int i = 0; i < 38583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38583(int& val) {
    const int x = 38583;
    std::cout << 38583;
    for (int i = 0; i < 38583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38584(int& val) {
    func_in_38584(val);
    const int x = 38584;
    std::cout << 38584;
    for (int i = 0; i < 38584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38584(int& val) {
    const int x = 38584;
    std::cout << 38584;
    for (int i = 0; i < 38584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38585(int& val) {
    func_in_38585(val);
    const int x = 38585;
    std::cout << 38585;
    for (int i = 0; i < 38585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38585(int& val) {
    const int x = 38585;
    std::cout << 38585;
    for (int i = 0; i < 38585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38586(int& val) {
    func_in_38586(val);
    const int x = 38586;
    std::cout << 38586;
    for (int i = 0; i < 38586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38586(int& val) {
    const int x = 38586;
    std::cout << 38586;
    for (int i = 0; i < 38586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38587(int& val) {
    func_in_38587(val);
    const int x = 38587;
    std::cout << 38587;
    for (int i = 0; i < 38587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38587(int& val) {
    const int x = 38587;
    std::cout << 38587;
    for (int i = 0; i < 38587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38588(int& val) {
    func_in_38588(val);
    const int x = 38588;
    std::cout << 38588;
    for (int i = 0; i < 38588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38588(int& val) {
    const int x = 38588;
    std::cout << 38588;
    for (int i = 0; i < 38588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38589(int& val) {
    func_in_38589(val);
    const int x = 38589;
    std::cout << 38589;
    for (int i = 0; i < 38589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38589(int& val) {
    const int x = 38589;
    std::cout << 38589;
    for (int i = 0; i < 38589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38590(int& val) {
    func_in_38590(val);
    const int x = 38590;
    std::cout << 38590;
    for (int i = 0; i < 38590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38590(int& val) {
    const int x = 38590;
    std::cout << 38590;
    for (int i = 0; i < 38590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38591(int& val) {
    func_in_38591(val);
    const int x = 38591;
    std::cout << 38591;
    for (int i = 0; i < 38591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38591(int& val) {
    const int x = 38591;
    std::cout << 38591;
    for (int i = 0; i < 38591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38592(int& val) {
    func_in_38592(val);
    const int x = 38592;
    std::cout << 38592;
    for (int i = 0; i < 38592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38592(int& val) {
    const int x = 38592;
    std::cout << 38592;
    for (int i = 0; i < 38592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38593(int& val) {
    func_in_38593(val);
    const int x = 38593;
    std::cout << 38593;
    for (int i = 0; i < 38593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38593(int& val) {
    const int x = 38593;
    std::cout << 38593;
    for (int i = 0; i < 38593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38594(int& val) {
    func_in_38594(val);
    const int x = 38594;
    std::cout << 38594;
    for (int i = 0; i < 38594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38594(int& val) {
    const int x = 38594;
    std::cout << 38594;
    for (int i = 0; i < 38594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38595(int& val) {
    func_in_38595(val);
    const int x = 38595;
    std::cout << 38595;
    for (int i = 0; i < 38595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38595(int& val) {
    const int x = 38595;
    std::cout << 38595;
    for (int i = 0; i < 38595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38596(int& val) {
    func_in_38596(val);
    const int x = 38596;
    std::cout << 38596;
    for (int i = 0; i < 38596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38596(int& val) {
    const int x = 38596;
    std::cout << 38596;
    for (int i = 0; i < 38596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38597(int& val) {
    func_in_38597(val);
    const int x = 38597;
    std::cout << 38597;
    for (int i = 0; i < 38597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38597(int& val) {
    const int x = 38597;
    std::cout << 38597;
    for (int i = 0; i < 38597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38598(int& val) {
    func_in_38598(val);
    const int x = 38598;
    std::cout << 38598;
    for (int i = 0; i < 38598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38598(int& val) {
    const int x = 38598;
    std::cout << 38598;
    for (int i = 0; i < 38598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38599(int& val) {
    func_in_38599(val);
    const int x = 38599;
    std::cout << 38599;
    for (int i = 0; i < 38599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38599(int& val) {
    const int x = 38599;
    std::cout << 38599;
    for (int i = 0; i < 38599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38600(int& val) {
    func_in_38600(val);
    const int x = 38600;
    std::cout << 38600;
    for (int i = 0; i < 38600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38600(int& val) {
    const int x = 38600;
    std::cout << 38600;
    for (int i = 0; i < 38600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38601(int& val) {
    func_in_38601(val);
    const int x = 38601;
    std::cout << 38601;
    for (int i = 0; i < 38601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38601(int& val) {
    const int x = 38601;
    std::cout << 38601;
    for (int i = 0; i < 38601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38602(int& val) {
    func_in_38602(val);
    const int x = 38602;
    std::cout << 38602;
    for (int i = 0; i < 38602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38602(int& val) {
    const int x = 38602;
    std::cout << 38602;
    for (int i = 0; i < 38602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38603(int& val) {
    func_in_38603(val);
    const int x = 38603;
    std::cout << 38603;
    for (int i = 0; i < 38603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38603(int& val) {
    const int x = 38603;
    std::cout << 38603;
    for (int i = 0; i < 38603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38604(int& val) {
    func_in_38604(val);
    const int x = 38604;
    std::cout << 38604;
    for (int i = 0; i < 38604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38604(int& val) {
    const int x = 38604;
    std::cout << 38604;
    for (int i = 0; i < 38604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38605(int& val) {
    func_in_38605(val);
    const int x = 38605;
    std::cout << 38605;
    for (int i = 0; i < 38605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38605(int& val) {
    const int x = 38605;
    std::cout << 38605;
    for (int i = 0; i < 38605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38606(int& val) {
    func_in_38606(val);
    const int x = 38606;
    std::cout << 38606;
    for (int i = 0; i < 38606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38606(int& val) {
    const int x = 38606;
    std::cout << 38606;
    for (int i = 0; i < 38606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38607(int& val) {
    func_in_38607(val);
    const int x = 38607;
    std::cout << 38607;
    for (int i = 0; i < 38607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38607(int& val) {
    const int x = 38607;
    std::cout << 38607;
    for (int i = 0; i < 38607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38608(int& val) {
    func_in_38608(val);
    const int x = 38608;
    std::cout << 38608;
    for (int i = 0; i < 38608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38608(int& val) {
    const int x = 38608;
    std::cout << 38608;
    for (int i = 0; i < 38608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38609(int& val) {
    func_in_38609(val);
    const int x = 38609;
    std::cout << 38609;
    for (int i = 0; i < 38609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38609(int& val) {
    const int x = 38609;
    std::cout << 38609;
    for (int i = 0; i < 38609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38610(int& val) {
    func_in_38610(val);
    const int x = 38610;
    std::cout << 38610;
    for (int i = 0; i < 38610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38610(int& val) {
    const int x = 38610;
    std::cout << 38610;
    for (int i = 0; i < 38610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38611(int& val) {
    func_in_38611(val);
    const int x = 38611;
    std::cout << 38611;
    for (int i = 0; i < 38611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38611(int& val) {
    const int x = 38611;
    std::cout << 38611;
    for (int i = 0; i < 38611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38612(int& val) {
    func_in_38612(val);
    const int x = 38612;
    std::cout << 38612;
    for (int i = 0; i < 38612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38612(int& val) {
    const int x = 38612;
    std::cout << 38612;
    for (int i = 0; i < 38612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38613(int& val) {
    func_in_38613(val);
    const int x = 38613;
    std::cout << 38613;
    for (int i = 0; i < 38613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38613(int& val) {
    const int x = 38613;
    std::cout << 38613;
    for (int i = 0; i < 38613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38614(int& val) {
    func_in_38614(val);
    const int x = 38614;
    std::cout << 38614;
    for (int i = 0; i < 38614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38614(int& val) {
    const int x = 38614;
    std::cout << 38614;
    for (int i = 0; i < 38614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38615(int& val) {
    func_in_38615(val);
    const int x = 38615;
    std::cout << 38615;
    for (int i = 0; i < 38615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38615(int& val) {
    const int x = 38615;
    std::cout << 38615;
    for (int i = 0; i < 38615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38616(int& val) {
    func_in_38616(val);
    const int x = 38616;
    std::cout << 38616;
    for (int i = 0; i < 38616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38616(int& val) {
    const int x = 38616;
    std::cout << 38616;
    for (int i = 0; i < 38616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38617(int& val) {
    func_in_38617(val);
    const int x = 38617;
    std::cout << 38617;
    for (int i = 0; i < 38617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38617(int& val) {
    const int x = 38617;
    std::cout << 38617;
    for (int i = 0; i < 38617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38618(int& val) {
    func_in_38618(val);
    const int x = 38618;
    std::cout << 38618;
    for (int i = 0; i < 38618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38618(int& val) {
    const int x = 38618;
    std::cout << 38618;
    for (int i = 0; i < 38618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38619(int& val) {
    func_in_38619(val);
    const int x = 38619;
    std::cout << 38619;
    for (int i = 0; i < 38619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38619(int& val) {
    const int x = 38619;
    std::cout << 38619;
    for (int i = 0; i < 38619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38620(int& val) {
    func_in_38620(val);
    const int x = 38620;
    std::cout << 38620;
    for (int i = 0; i < 38620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38620(int& val) {
    const int x = 38620;
    std::cout << 38620;
    for (int i = 0; i < 38620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38621(int& val) {
    func_in_38621(val);
    const int x = 38621;
    std::cout << 38621;
    for (int i = 0; i < 38621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38621(int& val) {
    const int x = 38621;
    std::cout << 38621;
    for (int i = 0; i < 38621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38622(int& val) {
    func_in_38622(val);
    const int x = 38622;
    std::cout << 38622;
    for (int i = 0; i < 38622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38622(int& val) {
    const int x = 38622;
    std::cout << 38622;
    for (int i = 0; i < 38622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38623(int& val) {
    func_in_38623(val);
    const int x = 38623;
    std::cout << 38623;
    for (int i = 0; i < 38623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38623(int& val) {
    const int x = 38623;
    std::cout << 38623;
    for (int i = 0; i < 38623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38624(int& val) {
    func_in_38624(val);
    const int x = 38624;
    std::cout << 38624;
    for (int i = 0; i < 38624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38624(int& val) {
    const int x = 38624;
    std::cout << 38624;
    for (int i = 0; i < 38624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38625(int& val) {
    func_in_38625(val);
    const int x = 38625;
    std::cout << 38625;
    for (int i = 0; i < 38625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38625(int& val) {
    const int x = 38625;
    std::cout << 38625;
    for (int i = 0; i < 38625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38626(int& val) {
    func_in_38626(val);
    const int x = 38626;
    std::cout << 38626;
    for (int i = 0; i < 38626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38626(int& val) {
    const int x = 38626;
    std::cout << 38626;
    for (int i = 0; i < 38626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38627(int& val) {
    func_in_38627(val);
    const int x = 38627;
    std::cout << 38627;
    for (int i = 0; i < 38627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38627(int& val) {
    const int x = 38627;
    std::cout << 38627;
    for (int i = 0; i < 38627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38628(int& val) {
    func_in_38628(val);
    const int x = 38628;
    std::cout << 38628;
    for (int i = 0; i < 38628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38628(int& val) {
    const int x = 38628;
    std::cout << 38628;
    for (int i = 0; i < 38628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38629(int& val) {
    func_in_38629(val);
    const int x = 38629;
    std::cout << 38629;
    for (int i = 0; i < 38629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38629(int& val) {
    const int x = 38629;
    std::cout << 38629;
    for (int i = 0; i < 38629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38630(int& val) {
    func_in_38630(val);
    const int x = 38630;
    std::cout << 38630;
    for (int i = 0; i < 38630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38630(int& val) {
    const int x = 38630;
    std::cout << 38630;
    for (int i = 0; i < 38630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38631(int& val) {
    func_in_38631(val);
    const int x = 38631;
    std::cout << 38631;
    for (int i = 0; i < 38631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38631(int& val) {
    const int x = 38631;
    std::cout << 38631;
    for (int i = 0; i < 38631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38632(int& val) {
    func_in_38632(val);
    const int x = 38632;
    std::cout << 38632;
    for (int i = 0; i < 38632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38632(int& val) {
    const int x = 38632;
    std::cout << 38632;
    for (int i = 0; i < 38632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38633(int& val) {
    func_in_38633(val);
    const int x = 38633;
    std::cout << 38633;
    for (int i = 0; i < 38633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38633(int& val) {
    const int x = 38633;
    std::cout << 38633;
    for (int i = 0; i < 38633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38634(int& val) {
    func_in_38634(val);
    const int x = 38634;
    std::cout << 38634;
    for (int i = 0; i < 38634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38634(int& val) {
    const int x = 38634;
    std::cout << 38634;
    for (int i = 0; i < 38634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38635(int& val) {
    func_in_38635(val);
    const int x = 38635;
    std::cout << 38635;
    for (int i = 0; i < 38635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38635(int& val) {
    const int x = 38635;
    std::cout << 38635;
    for (int i = 0; i < 38635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38636(int& val) {
    func_in_38636(val);
    const int x = 38636;
    std::cout << 38636;
    for (int i = 0; i < 38636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38636(int& val) {
    const int x = 38636;
    std::cout << 38636;
    for (int i = 0; i < 38636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38637(int& val) {
    func_in_38637(val);
    const int x = 38637;
    std::cout << 38637;
    for (int i = 0; i < 38637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38637(int& val) {
    const int x = 38637;
    std::cout << 38637;
    for (int i = 0; i < 38637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38638(int& val) {
    func_in_38638(val);
    const int x = 38638;
    std::cout << 38638;
    for (int i = 0; i < 38638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38638(int& val) {
    const int x = 38638;
    std::cout << 38638;
    for (int i = 0; i < 38638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38639(int& val) {
    func_in_38639(val);
    const int x = 38639;
    std::cout << 38639;
    for (int i = 0; i < 38639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38639(int& val) {
    const int x = 38639;
    std::cout << 38639;
    for (int i = 0; i < 38639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38640(int& val) {
    func_in_38640(val);
    const int x = 38640;
    std::cout << 38640;
    for (int i = 0; i < 38640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38640(int& val) {
    const int x = 38640;
    std::cout << 38640;
    for (int i = 0; i < 38640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38641(int& val) {
    func_in_38641(val);
    const int x = 38641;
    std::cout << 38641;
    for (int i = 0; i < 38641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38641(int& val) {
    const int x = 38641;
    std::cout << 38641;
    for (int i = 0; i < 38641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38642(int& val) {
    func_in_38642(val);
    const int x = 38642;
    std::cout << 38642;
    for (int i = 0; i < 38642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38642(int& val) {
    const int x = 38642;
    std::cout << 38642;
    for (int i = 0; i < 38642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38643(int& val) {
    func_in_38643(val);
    const int x = 38643;
    std::cout << 38643;
    for (int i = 0; i < 38643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38643(int& val) {
    const int x = 38643;
    std::cout << 38643;
    for (int i = 0; i < 38643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38644(int& val) {
    func_in_38644(val);
    const int x = 38644;
    std::cout << 38644;
    for (int i = 0; i < 38644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38644(int& val) {
    const int x = 38644;
    std::cout << 38644;
    for (int i = 0; i < 38644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38645(int& val) {
    func_in_38645(val);
    const int x = 38645;
    std::cout << 38645;
    for (int i = 0; i < 38645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38645(int& val) {
    const int x = 38645;
    std::cout << 38645;
    for (int i = 0; i < 38645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38646(int& val) {
    func_in_38646(val);
    const int x = 38646;
    std::cout << 38646;
    for (int i = 0; i < 38646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38646(int& val) {
    const int x = 38646;
    std::cout << 38646;
    for (int i = 0; i < 38646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38647(int& val) {
    func_in_38647(val);
    const int x = 38647;
    std::cout << 38647;
    for (int i = 0; i < 38647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38647(int& val) {
    const int x = 38647;
    std::cout << 38647;
    for (int i = 0; i < 38647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38648(int& val) {
    func_in_38648(val);
    const int x = 38648;
    std::cout << 38648;
    for (int i = 0; i < 38648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38648(int& val) {
    const int x = 38648;
    std::cout << 38648;
    for (int i = 0; i < 38648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38649(int& val) {
    func_in_38649(val);
    const int x = 38649;
    std::cout << 38649;
    for (int i = 0; i < 38649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38649(int& val) {
    const int x = 38649;
    std::cout << 38649;
    for (int i = 0; i < 38649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38650(int& val) {
    func_in_38650(val);
    const int x = 38650;
    std::cout << 38650;
    for (int i = 0; i < 38650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38650(int& val) {
    const int x = 38650;
    std::cout << 38650;
    for (int i = 0; i < 38650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38651(int& val) {
    func_in_38651(val);
    const int x = 38651;
    std::cout << 38651;
    for (int i = 0; i < 38651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38651(int& val) {
    const int x = 38651;
    std::cout << 38651;
    for (int i = 0; i < 38651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38652(int& val) {
    func_in_38652(val);
    const int x = 38652;
    std::cout << 38652;
    for (int i = 0; i < 38652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38652(int& val) {
    const int x = 38652;
    std::cout << 38652;
    for (int i = 0; i < 38652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38653(int& val) {
    func_in_38653(val);
    const int x = 38653;
    std::cout << 38653;
    for (int i = 0; i < 38653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38653(int& val) {
    const int x = 38653;
    std::cout << 38653;
    for (int i = 0; i < 38653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38654(int& val) {
    func_in_38654(val);
    const int x = 38654;
    std::cout << 38654;
    for (int i = 0; i < 38654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38654(int& val) {
    const int x = 38654;
    std::cout << 38654;
    for (int i = 0; i < 38654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38655(int& val) {
    func_in_38655(val);
    const int x = 38655;
    std::cout << 38655;
    for (int i = 0; i < 38655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38655(int& val) {
    const int x = 38655;
    std::cout << 38655;
    for (int i = 0; i < 38655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38656(int& val) {
    func_in_38656(val);
    const int x = 38656;
    std::cout << 38656;
    for (int i = 0; i < 38656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38656(int& val) {
    const int x = 38656;
    std::cout << 38656;
    for (int i = 0; i < 38656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38657(int& val) {
    func_in_38657(val);
    const int x = 38657;
    std::cout << 38657;
    for (int i = 0; i < 38657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38657(int& val) {
    const int x = 38657;
    std::cout << 38657;
    for (int i = 0; i < 38657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38658(int& val) {
    func_in_38658(val);
    const int x = 38658;
    std::cout << 38658;
    for (int i = 0; i < 38658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38658(int& val) {
    const int x = 38658;
    std::cout << 38658;
    for (int i = 0; i < 38658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38659(int& val) {
    func_in_38659(val);
    const int x = 38659;
    std::cout << 38659;
    for (int i = 0; i < 38659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38659(int& val) {
    const int x = 38659;
    std::cout << 38659;
    for (int i = 0; i < 38659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38660(int& val) {
    func_in_38660(val);
    const int x = 38660;
    std::cout << 38660;
    for (int i = 0; i < 38660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38660(int& val) {
    const int x = 38660;
    std::cout << 38660;
    for (int i = 0; i < 38660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38661(int& val) {
    func_in_38661(val);
    const int x = 38661;
    std::cout << 38661;
    for (int i = 0; i < 38661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38661(int& val) {
    const int x = 38661;
    std::cout << 38661;
    for (int i = 0; i < 38661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38662(int& val) {
    func_in_38662(val);
    const int x = 38662;
    std::cout << 38662;
    for (int i = 0; i < 38662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38662(int& val) {
    const int x = 38662;
    std::cout << 38662;
    for (int i = 0; i < 38662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38663(int& val) {
    func_in_38663(val);
    const int x = 38663;
    std::cout << 38663;
    for (int i = 0; i < 38663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38663(int& val) {
    const int x = 38663;
    std::cout << 38663;
    for (int i = 0; i < 38663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38664(int& val) {
    func_in_38664(val);
    const int x = 38664;
    std::cout << 38664;
    for (int i = 0; i < 38664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38664(int& val) {
    const int x = 38664;
    std::cout << 38664;
    for (int i = 0; i < 38664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38665(int& val) {
    func_in_38665(val);
    const int x = 38665;
    std::cout << 38665;
    for (int i = 0; i < 38665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38665(int& val) {
    const int x = 38665;
    std::cout << 38665;
    for (int i = 0; i < 38665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38666(int& val) {
    func_in_38666(val);
    const int x = 38666;
    std::cout << 38666;
    for (int i = 0; i < 38666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38666(int& val) {
    const int x = 38666;
    std::cout << 38666;
    for (int i = 0; i < 38666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38667(int& val) {
    func_in_38667(val);
    const int x = 38667;
    std::cout << 38667;
    for (int i = 0; i < 38667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38667(int& val) {
    const int x = 38667;
    std::cout << 38667;
    for (int i = 0; i < 38667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38668(int& val) {
    func_in_38668(val);
    const int x = 38668;
    std::cout << 38668;
    for (int i = 0; i < 38668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38668(int& val) {
    const int x = 38668;
    std::cout << 38668;
    for (int i = 0; i < 38668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38669(int& val) {
    func_in_38669(val);
    const int x = 38669;
    std::cout << 38669;
    for (int i = 0; i < 38669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38669(int& val) {
    const int x = 38669;
    std::cout << 38669;
    for (int i = 0; i < 38669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38670(int& val) {
    func_in_38670(val);
    const int x = 38670;
    std::cout << 38670;
    for (int i = 0; i < 38670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38670(int& val) {
    const int x = 38670;
    std::cout << 38670;
    for (int i = 0; i < 38670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38671(int& val) {
    func_in_38671(val);
    const int x = 38671;
    std::cout << 38671;
    for (int i = 0; i < 38671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38671(int& val) {
    const int x = 38671;
    std::cout << 38671;
    for (int i = 0; i < 38671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38672(int& val) {
    func_in_38672(val);
    const int x = 38672;
    std::cout << 38672;
    for (int i = 0; i < 38672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38672(int& val) {
    const int x = 38672;
    std::cout << 38672;
    for (int i = 0; i < 38672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38673(int& val) {
    func_in_38673(val);
    const int x = 38673;
    std::cout << 38673;
    for (int i = 0; i < 38673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38673(int& val) {
    const int x = 38673;
    std::cout << 38673;
    for (int i = 0; i < 38673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38674(int& val) {
    func_in_38674(val);
    const int x = 38674;
    std::cout << 38674;
    for (int i = 0; i < 38674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38674(int& val) {
    const int x = 38674;
    std::cout << 38674;
    for (int i = 0; i < 38674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38675(int& val) {
    func_in_38675(val);
    const int x = 38675;
    std::cout << 38675;
    for (int i = 0; i < 38675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38675(int& val) {
    const int x = 38675;
    std::cout << 38675;
    for (int i = 0; i < 38675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38676(int& val) {
    func_in_38676(val);
    const int x = 38676;
    std::cout << 38676;
    for (int i = 0; i < 38676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38676(int& val) {
    const int x = 38676;
    std::cout << 38676;
    for (int i = 0; i < 38676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38677(int& val) {
    func_in_38677(val);
    const int x = 38677;
    std::cout << 38677;
    for (int i = 0; i < 38677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38677(int& val) {
    const int x = 38677;
    std::cout << 38677;
    for (int i = 0; i < 38677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38678(int& val) {
    func_in_38678(val);
    const int x = 38678;
    std::cout << 38678;
    for (int i = 0; i < 38678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38678(int& val) {
    const int x = 38678;
    std::cout << 38678;
    for (int i = 0; i < 38678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38679(int& val) {
    func_in_38679(val);
    const int x = 38679;
    std::cout << 38679;
    for (int i = 0; i < 38679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38679(int& val) {
    const int x = 38679;
    std::cout << 38679;
    for (int i = 0; i < 38679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38680(int& val) {
    func_in_38680(val);
    const int x = 38680;
    std::cout << 38680;
    for (int i = 0; i < 38680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38680(int& val) {
    const int x = 38680;
    std::cout << 38680;
    for (int i = 0; i < 38680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38681(int& val) {
    func_in_38681(val);
    const int x = 38681;
    std::cout << 38681;
    for (int i = 0; i < 38681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38681(int& val) {
    const int x = 38681;
    std::cout << 38681;
    for (int i = 0; i < 38681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38682(int& val) {
    func_in_38682(val);
    const int x = 38682;
    std::cout << 38682;
    for (int i = 0; i < 38682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38682(int& val) {
    const int x = 38682;
    std::cout << 38682;
    for (int i = 0; i < 38682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38683(int& val) {
    func_in_38683(val);
    const int x = 38683;
    std::cout << 38683;
    for (int i = 0; i < 38683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38683(int& val) {
    const int x = 38683;
    std::cout << 38683;
    for (int i = 0; i < 38683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38684(int& val) {
    func_in_38684(val);
    const int x = 38684;
    std::cout << 38684;
    for (int i = 0; i < 38684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38684(int& val) {
    const int x = 38684;
    std::cout << 38684;
    for (int i = 0; i < 38684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38685(int& val) {
    func_in_38685(val);
    const int x = 38685;
    std::cout << 38685;
    for (int i = 0; i < 38685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38685(int& val) {
    const int x = 38685;
    std::cout << 38685;
    for (int i = 0; i < 38685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38686(int& val) {
    func_in_38686(val);
    const int x = 38686;
    std::cout << 38686;
    for (int i = 0; i < 38686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38686(int& val) {
    const int x = 38686;
    std::cout << 38686;
    for (int i = 0; i < 38686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38687(int& val) {
    func_in_38687(val);
    const int x = 38687;
    std::cout << 38687;
    for (int i = 0; i < 38687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38687(int& val) {
    const int x = 38687;
    std::cout << 38687;
    for (int i = 0; i < 38687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38688(int& val) {
    func_in_38688(val);
    const int x = 38688;
    std::cout << 38688;
    for (int i = 0; i < 38688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38688(int& val) {
    const int x = 38688;
    std::cout << 38688;
    for (int i = 0; i < 38688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38689(int& val) {
    func_in_38689(val);
    const int x = 38689;
    std::cout << 38689;
    for (int i = 0; i < 38689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38689(int& val) {
    const int x = 38689;
    std::cout << 38689;
    for (int i = 0; i < 38689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38690(int& val) {
    func_in_38690(val);
    const int x = 38690;
    std::cout << 38690;
    for (int i = 0; i < 38690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38690(int& val) {
    const int x = 38690;
    std::cout << 38690;
    for (int i = 0; i < 38690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38691(int& val) {
    func_in_38691(val);
    const int x = 38691;
    std::cout << 38691;
    for (int i = 0; i < 38691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38691(int& val) {
    const int x = 38691;
    std::cout << 38691;
    for (int i = 0; i < 38691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38692(int& val) {
    func_in_38692(val);
    const int x = 38692;
    std::cout << 38692;
    for (int i = 0; i < 38692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38692(int& val) {
    const int x = 38692;
    std::cout << 38692;
    for (int i = 0; i < 38692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38693(int& val) {
    func_in_38693(val);
    const int x = 38693;
    std::cout << 38693;
    for (int i = 0; i < 38693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38693(int& val) {
    const int x = 38693;
    std::cout << 38693;
    for (int i = 0; i < 38693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38694(int& val) {
    func_in_38694(val);
    const int x = 38694;
    std::cout << 38694;
    for (int i = 0; i < 38694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38694(int& val) {
    const int x = 38694;
    std::cout << 38694;
    for (int i = 0; i < 38694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38695(int& val) {
    func_in_38695(val);
    const int x = 38695;
    std::cout << 38695;
    for (int i = 0; i < 38695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38695(int& val) {
    const int x = 38695;
    std::cout << 38695;
    for (int i = 0; i < 38695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38696(int& val) {
    func_in_38696(val);
    const int x = 38696;
    std::cout << 38696;
    for (int i = 0; i < 38696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38696(int& val) {
    const int x = 38696;
    std::cout << 38696;
    for (int i = 0; i < 38696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38697(int& val) {
    func_in_38697(val);
    const int x = 38697;
    std::cout << 38697;
    for (int i = 0; i < 38697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38697(int& val) {
    const int x = 38697;
    std::cout << 38697;
    for (int i = 0; i < 38697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38698(int& val) {
    func_in_38698(val);
    const int x = 38698;
    std::cout << 38698;
    for (int i = 0; i < 38698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38698(int& val) {
    const int x = 38698;
    std::cout << 38698;
    for (int i = 0; i < 38698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38699(int& val) {
    func_in_38699(val);
    const int x = 38699;
    std::cout << 38699;
    for (int i = 0; i < 38699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38699(int& val) {
    const int x = 38699;
    std::cout << 38699;
    for (int i = 0; i < 38699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38700(int& val) {
    func_in_38700(val);
    const int x = 38700;
    std::cout << 38700;
    for (int i = 0; i < 38700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38700(int& val) {
    const int x = 38700;
    std::cout << 38700;
    for (int i = 0; i < 38700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38701(int& val) {
    func_in_38701(val);
    const int x = 38701;
    std::cout << 38701;
    for (int i = 0; i < 38701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38701(int& val) {
    const int x = 38701;
    std::cout << 38701;
    for (int i = 0; i < 38701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38702(int& val) {
    func_in_38702(val);
    const int x = 38702;
    std::cout << 38702;
    for (int i = 0; i < 38702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38702(int& val) {
    const int x = 38702;
    std::cout << 38702;
    for (int i = 0; i < 38702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38703(int& val) {
    func_in_38703(val);
    const int x = 38703;
    std::cout << 38703;
    for (int i = 0; i < 38703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38703(int& val) {
    const int x = 38703;
    std::cout << 38703;
    for (int i = 0; i < 38703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38704(int& val) {
    func_in_38704(val);
    const int x = 38704;
    std::cout << 38704;
    for (int i = 0; i < 38704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38704(int& val) {
    const int x = 38704;
    std::cout << 38704;
    for (int i = 0; i < 38704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38705(int& val) {
    func_in_38705(val);
    const int x = 38705;
    std::cout << 38705;
    for (int i = 0; i < 38705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38705(int& val) {
    const int x = 38705;
    std::cout << 38705;
    for (int i = 0; i < 38705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38706(int& val) {
    func_in_38706(val);
    const int x = 38706;
    std::cout << 38706;
    for (int i = 0; i < 38706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38706(int& val) {
    const int x = 38706;
    std::cout << 38706;
    for (int i = 0; i < 38706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38707(int& val) {
    func_in_38707(val);
    const int x = 38707;
    std::cout << 38707;
    for (int i = 0; i < 38707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38707(int& val) {
    const int x = 38707;
    std::cout << 38707;
    for (int i = 0; i < 38707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38708(int& val) {
    func_in_38708(val);
    const int x = 38708;
    std::cout << 38708;
    for (int i = 0; i < 38708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38708(int& val) {
    const int x = 38708;
    std::cout << 38708;
    for (int i = 0; i < 38708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38709(int& val) {
    func_in_38709(val);
    const int x = 38709;
    std::cout << 38709;
    for (int i = 0; i < 38709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38709(int& val) {
    const int x = 38709;
    std::cout << 38709;
    for (int i = 0; i < 38709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38710(int& val) {
    func_in_38710(val);
    const int x = 38710;
    std::cout << 38710;
    for (int i = 0; i < 38710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38710(int& val) {
    const int x = 38710;
    std::cout << 38710;
    for (int i = 0; i < 38710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38711(int& val) {
    func_in_38711(val);
    const int x = 38711;
    std::cout << 38711;
    for (int i = 0; i < 38711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38711(int& val) {
    const int x = 38711;
    std::cout << 38711;
    for (int i = 0; i < 38711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38712(int& val) {
    func_in_38712(val);
    const int x = 38712;
    std::cout << 38712;
    for (int i = 0; i < 38712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38712(int& val) {
    const int x = 38712;
    std::cout << 38712;
    for (int i = 0; i < 38712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38713(int& val) {
    func_in_38713(val);
    const int x = 38713;
    std::cout << 38713;
    for (int i = 0; i < 38713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38713(int& val) {
    const int x = 38713;
    std::cout << 38713;
    for (int i = 0; i < 38713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38714(int& val) {
    func_in_38714(val);
    const int x = 38714;
    std::cout << 38714;
    for (int i = 0; i < 38714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38714(int& val) {
    const int x = 38714;
    std::cout << 38714;
    for (int i = 0; i < 38714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38715(int& val) {
    func_in_38715(val);
    const int x = 38715;
    std::cout << 38715;
    for (int i = 0; i < 38715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38715(int& val) {
    const int x = 38715;
    std::cout << 38715;
    for (int i = 0; i < 38715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38716(int& val) {
    func_in_38716(val);
    const int x = 38716;
    std::cout << 38716;
    for (int i = 0; i < 38716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38716(int& val) {
    const int x = 38716;
    std::cout << 38716;
    for (int i = 0; i < 38716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38717(int& val) {
    func_in_38717(val);
    const int x = 38717;
    std::cout << 38717;
    for (int i = 0; i < 38717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38717(int& val) {
    const int x = 38717;
    std::cout << 38717;
    for (int i = 0; i < 38717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38718(int& val) {
    func_in_38718(val);
    const int x = 38718;
    std::cout << 38718;
    for (int i = 0; i < 38718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38718(int& val) {
    const int x = 38718;
    std::cout << 38718;
    for (int i = 0; i < 38718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38719(int& val) {
    func_in_38719(val);
    const int x = 38719;
    std::cout << 38719;
    for (int i = 0; i < 38719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38719(int& val) {
    const int x = 38719;
    std::cout << 38719;
    for (int i = 0; i < 38719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38720(int& val) {
    func_in_38720(val);
    const int x = 38720;
    std::cout << 38720;
    for (int i = 0; i < 38720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38720(int& val) {
    const int x = 38720;
    std::cout << 38720;
    for (int i = 0; i < 38720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38721(int& val) {
    func_in_38721(val);
    const int x = 38721;
    std::cout << 38721;
    for (int i = 0; i < 38721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38721(int& val) {
    const int x = 38721;
    std::cout << 38721;
    for (int i = 0; i < 38721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38722(int& val) {
    func_in_38722(val);
    const int x = 38722;
    std::cout << 38722;
    for (int i = 0; i < 38722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38722(int& val) {
    const int x = 38722;
    std::cout << 38722;
    for (int i = 0; i < 38722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38723(int& val) {
    func_in_38723(val);
    const int x = 38723;
    std::cout << 38723;
    for (int i = 0; i < 38723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38723(int& val) {
    const int x = 38723;
    std::cout << 38723;
    for (int i = 0; i < 38723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38724(int& val) {
    func_in_38724(val);
    const int x = 38724;
    std::cout << 38724;
    for (int i = 0; i < 38724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38724(int& val) {
    const int x = 38724;
    std::cout << 38724;
    for (int i = 0; i < 38724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38725(int& val) {
    func_in_38725(val);
    const int x = 38725;
    std::cout << 38725;
    for (int i = 0; i < 38725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38725(int& val) {
    const int x = 38725;
    std::cout << 38725;
    for (int i = 0; i < 38725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38726(int& val) {
    func_in_38726(val);
    const int x = 38726;
    std::cout << 38726;
    for (int i = 0; i < 38726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38726(int& val) {
    const int x = 38726;
    std::cout << 38726;
    for (int i = 0; i < 38726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38727(int& val) {
    func_in_38727(val);
    const int x = 38727;
    std::cout << 38727;
    for (int i = 0; i < 38727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38727(int& val) {
    const int x = 38727;
    std::cout << 38727;
    for (int i = 0; i < 38727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38728(int& val) {
    func_in_38728(val);
    const int x = 38728;
    std::cout << 38728;
    for (int i = 0; i < 38728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38728(int& val) {
    const int x = 38728;
    std::cout << 38728;
    for (int i = 0; i < 38728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38729(int& val) {
    func_in_38729(val);
    const int x = 38729;
    std::cout << 38729;
    for (int i = 0; i < 38729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38729(int& val) {
    const int x = 38729;
    std::cout << 38729;
    for (int i = 0; i < 38729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38730(int& val) {
    func_in_38730(val);
    const int x = 38730;
    std::cout << 38730;
    for (int i = 0; i < 38730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38730(int& val) {
    const int x = 38730;
    std::cout << 38730;
    for (int i = 0; i < 38730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38731(int& val) {
    func_in_38731(val);
    const int x = 38731;
    std::cout << 38731;
    for (int i = 0; i < 38731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38731(int& val) {
    const int x = 38731;
    std::cout << 38731;
    for (int i = 0; i < 38731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38732(int& val) {
    func_in_38732(val);
    const int x = 38732;
    std::cout << 38732;
    for (int i = 0; i < 38732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38732(int& val) {
    const int x = 38732;
    std::cout << 38732;
    for (int i = 0; i < 38732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38733(int& val) {
    func_in_38733(val);
    const int x = 38733;
    std::cout << 38733;
    for (int i = 0; i < 38733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38733(int& val) {
    const int x = 38733;
    std::cout << 38733;
    for (int i = 0; i < 38733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38734(int& val) {
    func_in_38734(val);
    const int x = 38734;
    std::cout << 38734;
    for (int i = 0; i < 38734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38734(int& val) {
    const int x = 38734;
    std::cout << 38734;
    for (int i = 0; i < 38734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38735(int& val) {
    func_in_38735(val);
    const int x = 38735;
    std::cout << 38735;
    for (int i = 0; i < 38735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38735(int& val) {
    const int x = 38735;
    std::cout << 38735;
    for (int i = 0; i < 38735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38736(int& val) {
    func_in_38736(val);
    const int x = 38736;
    std::cout << 38736;
    for (int i = 0; i < 38736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38736(int& val) {
    const int x = 38736;
    std::cout << 38736;
    for (int i = 0; i < 38736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38737(int& val) {
    func_in_38737(val);
    const int x = 38737;
    std::cout << 38737;
    for (int i = 0; i < 38737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38737(int& val) {
    const int x = 38737;
    std::cout << 38737;
    for (int i = 0; i < 38737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38738(int& val) {
    func_in_38738(val);
    const int x = 38738;
    std::cout << 38738;
    for (int i = 0; i < 38738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38738(int& val) {
    const int x = 38738;
    std::cout << 38738;
    for (int i = 0; i < 38738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38739(int& val) {
    func_in_38739(val);
    const int x = 38739;
    std::cout << 38739;
    for (int i = 0; i < 38739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38739(int& val) {
    const int x = 38739;
    std::cout << 38739;
    for (int i = 0; i < 38739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38740(int& val) {
    func_in_38740(val);
    const int x = 38740;
    std::cout << 38740;
    for (int i = 0; i < 38740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38740(int& val) {
    const int x = 38740;
    std::cout << 38740;
    for (int i = 0; i < 38740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38741(int& val) {
    func_in_38741(val);
    const int x = 38741;
    std::cout << 38741;
    for (int i = 0; i < 38741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38741(int& val) {
    const int x = 38741;
    std::cout << 38741;
    for (int i = 0; i < 38741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38742(int& val) {
    func_in_38742(val);
    const int x = 38742;
    std::cout << 38742;
    for (int i = 0; i < 38742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38742(int& val) {
    const int x = 38742;
    std::cout << 38742;
    for (int i = 0; i < 38742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38743(int& val) {
    func_in_38743(val);
    const int x = 38743;
    std::cout << 38743;
    for (int i = 0; i < 38743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38743(int& val) {
    const int x = 38743;
    std::cout << 38743;
    for (int i = 0; i < 38743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38744(int& val) {
    func_in_38744(val);
    const int x = 38744;
    std::cout << 38744;
    for (int i = 0; i < 38744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38744(int& val) {
    const int x = 38744;
    std::cout << 38744;
    for (int i = 0; i < 38744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38745(int& val) {
    func_in_38745(val);
    const int x = 38745;
    std::cout << 38745;
    for (int i = 0; i < 38745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38745(int& val) {
    const int x = 38745;
    std::cout << 38745;
    for (int i = 0; i < 38745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38746(int& val) {
    func_in_38746(val);
    const int x = 38746;
    std::cout << 38746;
    for (int i = 0; i < 38746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38746(int& val) {
    const int x = 38746;
    std::cout << 38746;
    for (int i = 0; i < 38746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38747(int& val) {
    func_in_38747(val);
    const int x = 38747;
    std::cout << 38747;
    for (int i = 0; i < 38747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38747(int& val) {
    const int x = 38747;
    std::cout << 38747;
    for (int i = 0; i < 38747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38748(int& val) {
    func_in_38748(val);
    const int x = 38748;
    std::cout << 38748;
    for (int i = 0; i < 38748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38748(int& val) {
    const int x = 38748;
    std::cout << 38748;
    for (int i = 0; i < 38748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38749(int& val) {
    func_in_38749(val);
    const int x = 38749;
    std::cout << 38749;
    for (int i = 0; i < 38749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38749(int& val) {
    const int x = 38749;
    std::cout << 38749;
    for (int i = 0; i < 38749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38750(int& val) {
    func_in_38750(val);
    const int x = 38750;
    std::cout << 38750;
    for (int i = 0; i < 38750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38750(int& val) {
    const int x = 38750;
    std::cout << 38750;
    for (int i = 0; i < 38750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38751(int& val) {
    func_in_38751(val);
    const int x = 38751;
    std::cout << 38751;
    for (int i = 0; i < 38751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38751(int& val) {
    const int x = 38751;
    std::cout << 38751;
    for (int i = 0; i < 38751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38752(int& val) {
    func_in_38752(val);
    const int x = 38752;
    std::cout << 38752;
    for (int i = 0; i < 38752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38752(int& val) {
    const int x = 38752;
    std::cout << 38752;
    for (int i = 0; i < 38752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38753(int& val) {
    func_in_38753(val);
    const int x = 38753;
    std::cout << 38753;
    for (int i = 0; i < 38753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38753(int& val) {
    const int x = 38753;
    std::cout << 38753;
    for (int i = 0; i < 38753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38754(int& val) {
    func_in_38754(val);
    const int x = 38754;
    std::cout << 38754;
    for (int i = 0; i < 38754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38754(int& val) {
    const int x = 38754;
    std::cout << 38754;
    for (int i = 0; i < 38754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38755(int& val) {
    func_in_38755(val);
    const int x = 38755;
    std::cout << 38755;
    for (int i = 0; i < 38755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38755(int& val) {
    const int x = 38755;
    std::cout << 38755;
    for (int i = 0; i < 38755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38756(int& val) {
    func_in_38756(val);
    const int x = 38756;
    std::cout << 38756;
    for (int i = 0; i < 38756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38756(int& val) {
    const int x = 38756;
    std::cout << 38756;
    for (int i = 0; i < 38756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38757(int& val) {
    func_in_38757(val);
    const int x = 38757;
    std::cout << 38757;
    for (int i = 0; i < 38757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38757(int& val) {
    const int x = 38757;
    std::cout << 38757;
    for (int i = 0; i < 38757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38758(int& val) {
    func_in_38758(val);
    const int x = 38758;
    std::cout << 38758;
    for (int i = 0; i < 38758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38758(int& val) {
    const int x = 38758;
    std::cout << 38758;
    for (int i = 0; i < 38758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38759(int& val) {
    func_in_38759(val);
    const int x = 38759;
    std::cout << 38759;
    for (int i = 0; i < 38759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38759(int& val) {
    const int x = 38759;
    std::cout << 38759;
    for (int i = 0; i < 38759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38760(int& val) {
    func_in_38760(val);
    const int x = 38760;
    std::cout << 38760;
    for (int i = 0; i < 38760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38760(int& val) {
    const int x = 38760;
    std::cout << 38760;
    for (int i = 0; i < 38760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38761(int& val) {
    func_in_38761(val);
    const int x = 38761;
    std::cout << 38761;
    for (int i = 0; i < 38761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38761(int& val) {
    const int x = 38761;
    std::cout << 38761;
    for (int i = 0; i < 38761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38762(int& val) {
    func_in_38762(val);
    const int x = 38762;
    std::cout << 38762;
    for (int i = 0; i < 38762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38762(int& val) {
    const int x = 38762;
    std::cout << 38762;
    for (int i = 0; i < 38762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38763(int& val) {
    func_in_38763(val);
    const int x = 38763;
    std::cout << 38763;
    for (int i = 0; i < 38763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38763(int& val) {
    const int x = 38763;
    std::cout << 38763;
    for (int i = 0; i < 38763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38764(int& val) {
    func_in_38764(val);
    const int x = 38764;
    std::cout << 38764;
    for (int i = 0; i < 38764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38764(int& val) {
    const int x = 38764;
    std::cout << 38764;
    for (int i = 0; i < 38764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38765(int& val) {
    func_in_38765(val);
    const int x = 38765;
    std::cout << 38765;
    for (int i = 0; i < 38765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38765(int& val) {
    const int x = 38765;
    std::cout << 38765;
    for (int i = 0; i < 38765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38766(int& val) {
    func_in_38766(val);
    const int x = 38766;
    std::cout << 38766;
    for (int i = 0; i < 38766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38766(int& val) {
    const int x = 38766;
    std::cout << 38766;
    for (int i = 0; i < 38766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38767(int& val) {
    func_in_38767(val);
    const int x = 38767;
    std::cout << 38767;
    for (int i = 0; i < 38767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38767(int& val) {
    const int x = 38767;
    std::cout << 38767;
    for (int i = 0; i < 38767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38768(int& val) {
    func_in_38768(val);
    const int x = 38768;
    std::cout << 38768;
    for (int i = 0; i < 38768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38768(int& val) {
    const int x = 38768;
    std::cout << 38768;
    for (int i = 0; i < 38768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38769(int& val) {
    func_in_38769(val);
    const int x = 38769;
    std::cout << 38769;
    for (int i = 0; i < 38769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38769(int& val) {
    const int x = 38769;
    std::cout << 38769;
    for (int i = 0; i < 38769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38770(int& val) {
    func_in_38770(val);
    const int x = 38770;
    std::cout << 38770;
    for (int i = 0; i < 38770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38770(int& val) {
    const int x = 38770;
    std::cout << 38770;
    for (int i = 0; i < 38770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38771(int& val) {
    func_in_38771(val);
    const int x = 38771;
    std::cout << 38771;
    for (int i = 0; i < 38771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38771(int& val) {
    const int x = 38771;
    std::cout << 38771;
    for (int i = 0; i < 38771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38772(int& val) {
    func_in_38772(val);
    const int x = 38772;
    std::cout << 38772;
    for (int i = 0; i < 38772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38772(int& val) {
    const int x = 38772;
    std::cout << 38772;
    for (int i = 0; i < 38772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38773(int& val) {
    func_in_38773(val);
    const int x = 38773;
    std::cout << 38773;
    for (int i = 0; i < 38773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38773(int& val) {
    const int x = 38773;
    std::cout << 38773;
    for (int i = 0; i < 38773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38774(int& val) {
    func_in_38774(val);
    const int x = 38774;
    std::cout << 38774;
    for (int i = 0; i < 38774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38774(int& val) {
    const int x = 38774;
    std::cout << 38774;
    for (int i = 0; i < 38774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38775(int& val) {
    func_in_38775(val);
    const int x = 38775;
    std::cout << 38775;
    for (int i = 0; i < 38775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38775(int& val) {
    const int x = 38775;
    std::cout << 38775;
    for (int i = 0; i < 38775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38776(int& val) {
    func_in_38776(val);
    const int x = 38776;
    std::cout << 38776;
    for (int i = 0; i < 38776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38776(int& val) {
    const int x = 38776;
    std::cout << 38776;
    for (int i = 0; i < 38776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38777(int& val) {
    func_in_38777(val);
    const int x = 38777;
    std::cout << 38777;
    for (int i = 0; i < 38777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38777(int& val) {
    const int x = 38777;
    std::cout << 38777;
    for (int i = 0; i < 38777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38778(int& val) {
    func_in_38778(val);
    const int x = 38778;
    std::cout << 38778;
    for (int i = 0; i < 38778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38778(int& val) {
    const int x = 38778;
    std::cout << 38778;
    for (int i = 0; i < 38778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38779(int& val) {
    func_in_38779(val);
    const int x = 38779;
    std::cout << 38779;
    for (int i = 0; i < 38779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38779(int& val) {
    const int x = 38779;
    std::cout << 38779;
    for (int i = 0; i < 38779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38780(int& val) {
    func_in_38780(val);
    const int x = 38780;
    std::cout << 38780;
    for (int i = 0; i < 38780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38780(int& val) {
    const int x = 38780;
    std::cout << 38780;
    for (int i = 0; i < 38780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38781(int& val) {
    func_in_38781(val);
    const int x = 38781;
    std::cout << 38781;
    for (int i = 0; i < 38781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38781(int& val) {
    const int x = 38781;
    std::cout << 38781;
    for (int i = 0; i < 38781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38782(int& val) {
    func_in_38782(val);
    const int x = 38782;
    std::cout << 38782;
    for (int i = 0; i < 38782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38782(int& val) {
    const int x = 38782;
    std::cout << 38782;
    for (int i = 0; i < 38782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38783(int& val) {
    func_in_38783(val);
    const int x = 38783;
    std::cout << 38783;
    for (int i = 0; i < 38783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38783(int& val) {
    const int x = 38783;
    std::cout << 38783;
    for (int i = 0; i < 38783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38784(int& val) {
    func_in_38784(val);
    const int x = 38784;
    std::cout << 38784;
    for (int i = 0; i < 38784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38784(int& val) {
    const int x = 38784;
    std::cout << 38784;
    for (int i = 0; i < 38784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38785(int& val) {
    func_in_38785(val);
    const int x = 38785;
    std::cout << 38785;
    for (int i = 0; i < 38785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38785(int& val) {
    const int x = 38785;
    std::cout << 38785;
    for (int i = 0; i < 38785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38786(int& val) {
    func_in_38786(val);
    const int x = 38786;
    std::cout << 38786;
    for (int i = 0; i < 38786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38786(int& val) {
    const int x = 38786;
    std::cout << 38786;
    for (int i = 0; i < 38786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38787(int& val) {
    func_in_38787(val);
    const int x = 38787;
    std::cout << 38787;
    for (int i = 0; i < 38787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38787(int& val) {
    const int x = 38787;
    std::cout << 38787;
    for (int i = 0; i < 38787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38788(int& val) {
    func_in_38788(val);
    const int x = 38788;
    std::cout << 38788;
    for (int i = 0; i < 38788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38788(int& val) {
    const int x = 38788;
    std::cout << 38788;
    for (int i = 0; i < 38788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38789(int& val) {
    func_in_38789(val);
    const int x = 38789;
    std::cout << 38789;
    for (int i = 0; i < 38789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38789(int& val) {
    const int x = 38789;
    std::cout << 38789;
    for (int i = 0; i < 38789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38790(int& val) {
    func_in_38790(val);
    const int x = 38790;
    std::cout << 38790;
    for (int i = 0; i < 38790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38790(int& val) {
    const int x = 38790;
    std::cout << 38790;
    for (int i = 0; i < 38790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38791(int& val) {
    func_in_38791(val);
    const int x = 38791;
    std::cout << 38791;
    for (int i = 0; i < 38791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38791(int& val) {
    const int x = 38791;
    std::cout << 38791;
    for (int i = 0; i < 38791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38792(int& val) {
    func_in_38792(val);
    const int x = 38792;
    std::cout << 38792;
    for (int i = 0; i < 38792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38792(int& val) {
    const int x = 38792;
    std::cout << 38792;
    for (int i = 0; i < 38792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38793(int& val) {
    func_in_38793(val);
    const int x = 38793;
    std::cout << 38793;
    for (int i = 0; i < 38793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38793(int& val) {
    const int x = 38793;
    std::cout << 38793;
    for (int i = 0; i < 38793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38794(int& val) {
    func_in_38794(val);
    const int x = 38794;
    std::cout << 38794;
    for (int i = 0; i < 38794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38794(int& val) {
    const int x = 38794;
    std::cout << 38794;
    for (int i = 0; i < 38794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38795(int& val) {
    func_in_38795(val);
    const int x = 38795;
    std::cout << 38795;
    for (int i = 0; i < 38795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38795(int& val) {
    const int x = 38795;
    std::cout << 38795;
    for (int i = 0; i < 38795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38796(int& val) {
    func_in_38796(val);
    const int x = 38796;
    std::cout << 38796;
    for (int i = 0; i < 38796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38796(int& val) {
    const int x = 38796;
    std::cout << 38796;
    for (int i = 0; i < 38796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38797(int& val) {
    func_in_38797(val);
    const int x = 38797;
    std::cout << 38797;
    for (int i = 0; i < 38797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38797(int& val) {
    const int x = 38797;
    std::cout << 38797;
    for (int i = 0; i < 38797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38798(int& val) {
    func_in_38798(val);
    const int x = 38798;
    std::cout << 38798;
    for (int i = 0; i < 38798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38798(int& val) {
    const int x = 38798;
    std::cout << 38798;
    for (int i = 0; i < 38798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38799(int& val) {
    func_in_38799(val);
    const int x = 38799;
    std::cout << 38799;
    for (int i = 0; i < 38799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38799(int& val) {
    const int x = 38799;
    std::cout << 38799;
    for (int i = 0; i < 38799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38800(int& val) {
    func_in_38800(val);
    const int x = 38800;
    std::cout << 38800;
    for (int i = 0; i < 38800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38800(int& val) {
    const int x = 38800;
    std::cout << 38800;
    for (int i = 0; i < 38800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38801(int& val) {
    func_in_38801(val);
    const int x = 38801;
    std::cout << 38801;
    for (int i = 0; i < 38801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38801(int& val) {
    const int x = 38801;
    std::cout << 38801;
    for (int i = 0; i < 38801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38802(int& val) {
    func_in_38802(val);
    const int x = 38802;
    std::cout << 38802;
    for (int i = 0; i < 38802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38802(int& val) {
    const int x = 38802;
    std::cout << 38802;
    for (int i = 0; i < 38802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38803(int& val) {
    func_in_38803(val);
    const int x = 38803;
    std::cout << 38803;
    for (int i = 0; i < 38803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38803(int& val) {
    const int x = 38803;
    std::cout << 38803;
    for (int i = 0; i < 38803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38804(int& val) {
    func_in_38804(val);
    const int x = 38804;
    std::cout << 38804;
    for (int i = 0; i < 38804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38804(int& val) {
    const int x = 38804;
    std::cout << 38804;
    for (int i = 0; i < 38804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38805(int& val) {
    func_in_38805(val);
    const int x = 38805;
    std::cout << 38805;
    for (int i = 0; i < 38805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38805(int& val) {
    const int x = 38805;
    std::cout << 38805;
    for (int i = 0; i < 38805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38806(int& val) {
    func_in_38806(val);
    const int x = 38806;
    std::cout << 38806;
    for (int i = 0; i < 38806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38806(int& val) {
    const int x = 38806;
    std::cout << 38806;
    for (int i = 0; i < 38806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38807(int& val) {
    func_in_38807(val);
    const int x = 38807;
    std::cout << 38807;
    for (int i = 0; i < 38807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38807(int& val) {
    const int x = 38807;
    std::cout << 38807;
    for (int i = 0; i < 38807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38808(int& val) {
    func_in_38808(val);
    const int x = 38808;
    std::cout << 38808;
    for (int i = 0; i < 38808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38808(int& val) {
    const int x = 38808;
    std::cout << 38808;
    for (int i = 0; i < 38808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38809(int& val) {
    func_in_38809(val);
    const int x = 38809;
    std::cout << 38809;
    for (int i = 0; i < 38809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38809(int& val) {
    const int x = 38809;
    std::cout << 38809;
    for (int i = 0; i < 38809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38810(int& val) {
    func_in_38810(val);
    const int x = 38810;
    std::cout << 38810;
    for (int i = 0; i < 38810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38810(int& val) {
    const int x = 38810;
    std::cout << 38810;
    for (int i = 0; i < 38810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38811(int& val) {
    func_in_38811(val);
    const int x = 38811;
    std::cout << 38811;
    for (int i = 0; i < 38811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38811(int& val) {
    const int x = 38811;
    std::cout << 38811;
    for (int i = 0; i < 38811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38812(int& val) {
    func_in_38812(val);
    const int x = 38812;
    std::cout << 38812;
    for (int i = 0; i < 38812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38812(int& val) {
    const int x = 38812;
    std::cout << 38812;
    for (int i = 0; i < 38812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38813(int& val) {
    func_in_38813(val);
    const int x = 38813;
    std::cout << 38813;
    for (int i = 0; i < 38813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38813(int& val) {
    const int x = 38813;
    std::cout << 38813;
    for (int i = 0; i < 38813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38814(int& val) {
    func_in_38814(val);
    const int x = 38814;
    std::cout << 38814;
    for (int i = 0; i < 38814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38814(int& val) {
    const int x = 38814;
    std::cout << 38814;
    for (int i = 0; i < 38814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38815(int& val) {
    func_in_38815(val);
    const int x = 38815;
    std::cout << 38815;
    for (int i = 0; i < 38815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38815(int& val) {
    const int x = 38815;
    std::cout << 38815;
    for (int i = 0; i < 38815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38816(int& val) {
    func_in_38816(val);
    const int x = 38816;
    std::cout << 38816;
    for (int i = 0; i < 38816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38816(int& val) {
    const int x = 38816;
    std::cout << 38816;
    for (int i = 0; i < 38816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38817(int& val) {
    func_in_38817(val);
    const int x = 38817;
    std::cout << 38817;
    for (int i = 0; i < 38817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38817(int& val) {
    const int x = 38817;
    std::cout << 38817;
    for (int i = 0; i < 38817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38818(int& val) {
    func_in_38818(val);
    const int x = 38818;
    std::cout << 38818;
    for (int i = 0; i < 38818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38818(int& val) {
    const int x = 38818;
    std::cout << 38818;
    for (int i = 0; i < 38818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38819(int& val) {
    func_in_38819(val);
    const int x = 38819;
    std::cout << 38819;
    for (int i = 0; i < 38819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38819(int& val) {
    const int x = 38819;
    std::cout << 38819;
    for (int i = 0; i < 38819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38820(int& val) {
    func_in_38820(val);
    const int x = 38820;
    std::cout << 38820;
    for (int i = 0; i < 38820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38820(int& val) {
    const int x = 38820;
    std::cout << 38820;
    for (int i = 0; i < 38820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38821(int& val) {
    func_in_38821(val);
    const int x = 38821;
    std::cout << 38821;
    for (int i = 0; i < 38821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38821(int& val) {
    const int x = 38821;
    std::cout << 38821;
    for (int i = 0; i < 38821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38822(int& val) {
    func_in_38822(val);
    const int x = 38822;
    std::cout << 38822;
    for (int i = 0; i < 38822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38822(int& val) {
    const int x = 38822;
    std::cout << 38822;
    for (int i = 0; i < 38822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38823(int& val) {
    func_in_38823(val);
    const int x = 38823;
    std::cout << 38823;
    for (int i = 0; i < 38823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38823(int& val) {
    const int x = 38823;
    std::cout << 38823;
    for (int i = 0; i < 38823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38824(int& val) {
    func_in_38824(val);
    const int x = 38824;
    std::cout << 38824;
    for (int i = 0; i < 38824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38824(int& val) {
    const int x = 38824;
    std::cout << 38824;
    for (int i = 0; i < 38824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38825(int& val) {
    func_in_38825(val);
    const int x = 38825;
    std::cout << 38825;
    for (int i = 0; i < 38825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38825(int& val) {
    const int x = 38825;
    std::cout << 38825;
    for (int i = 0; i < 38825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38826(int& val) {
    func_in_38826(val);
    const int x = 38826;
    std::cout << 38826;
    for (int i = 0; i < 38826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38826(int& val) {
    const int x = 38826;
    std::cout << 38826;
    for (int i = 0; i < 38826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38827(int& val) {
    func_in_38827(val);
    const int x = 38827;
    std::cout << 38827;
    for (int i = 0; i < 38827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38827(int& val) {
    const int x = 38827;
    std::cout << 38827;
    for (int i = 0; i < 38827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38828(int& val) {
    func_in_38828(val);
    const int x = 38828;
    std::cout << 38828;
    for (int i = 0; i < 38828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38828(int& val) {
    const int x = 38828;
    std::cout << 38828;
    for (int i = 0; i < 38828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38829(int& val) {
    func_in_38829(val);
    const int x = 38829;
    std::cout << 38829;
    for (int i = 0; i < 38829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38829(int& val) {
    const int x = 38829;
    std::cout << 38829;
    for (int i = 0; i < 38829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38830(int& val) {
    func_in_38830(val);
    const int x = 38830;
    std::cout << 38830;
    for (int i = 0; i < 38830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38830(int& val) {
    const int x = 38830;
    std::cout << 38830;
    for (int i = 0; i < 38830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38831(int& val) {
    func_in_38831(val);
    const int x = 38831;
    std::cout << 38831;
    for (int i = 0; i < 38831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38831(int& val) {
    const int x = 38831;
    std::cout << 38831;
    for (int i = 0; i < 38831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38832(int& val) {
    func_in_38832(val);
    const int x = 38832;
    std::cout << 38832;
    for (int i = 0; i < 38832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38832(int& val) {
    const int x = 38832;
    std::cout << 38832;
    for (int i = 0; i < 38832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38833(int& val) {
    func_in_38833(val);
    const int x = 38833;
    std::cout << 38833;
    for (int i = 0; i < 38833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38833(int& val) {
    const int x = 38833;
    std::cout << 38833;
    for (int i = 0; i < 38833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38834(int& val) {
    func_in_38834(val);
    const int x = 38834;
    std::cout << 38834;
    for (int i = 0; i < 38834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38834(int& val) {
    const int x = 38834;
    std::cout << 38834;
    for (int i = 0; i < 38834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38835(int& val) {
    func_in_38835(val);
    const int x = 38835;
    std::cout << 38835;
    for (int i = 0; i < 38835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38835(int& val) {
    const int x = 38835;
    std::cout << 38835;
    for (int i = 0; i < 38835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38836(int& val) {
    func_in_38836(val);
    const int x = 38836;
    std::cout << 38836;
    for (int i = 0; i < 38836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38836(int& val) {
    const int x = 38836;
    std::cout << 38836;
    for (int i = 0; i < 38836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38837(int& val) {
    func_in_38837(val);
    const int x = 38837;
    std::cout << 38837;
    for (int i = 0; i < 38837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38837(int& val) {
    const int x = 38837;
    std::cout << 38837;
    for (int i = 0; i < 38837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38838(int& val) {
    func_in_38838(val);
    const int x = 38838;
    std::cout << 38838;
    for (int i = 0; i < 38838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38838(int& val) {
    const int x = 38838;
    std::cout << 38838;
    for (int i = 0; i < 38838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38839(int& val) {
    func_in_38839(val);
    const int x = 38839;
    std::cout << 38839;
    for (int i = 0; i < 38839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38839(int& val) {
    const int x = 38839;
    std::cout << 38839;
    for (int i = 0; i < 38839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38840(int& val) {
    func_in_38840(val);
    const int x = 38840;
    std::cout << 38840;
    for (int i = 0; i < 38840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38840(int& val) {
    const int x = 38840;
    std::cout << 38840;
    for (int i = 0; i < 38840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38841(int& val) {
    func_in_38841(val);
    const int x = 38841;
    std::cout << 38841;
    for (int i = 0; i < 38841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38841(int& val) {
    const int x = 38841;
    std::cout << 38841;
    for (int i = 0; i < 38841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38842(int& val) {
    func_in_38842(val);
    const int x = 38842;
    std::cout << 38842;
    for (int i = 0; i < 38842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38842(int& val) {
    const int x = 38842;
    std::cout << 38842;
    for (int i = 0; i < 38842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38843(int& val) {
    func_in_38843(val);
    const int x = 38843;
    std::cout << 38843;
    for (int i = 0; i < 38843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38843(int& val) {
    const int x = 38843;
    std::cout << 38843;
    for (int i = 0; i < 38843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38844(int& val) {
    func_in_38844(val);
    const int x = 38844;
    std::cout << 38844;
    for (int i = 0; i < 38844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38844(int& val) {
    const int x = 38844;
    std::cout << 38844;
    for (int i = 0; i < 38844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38845(int& val) {
    func_in_38845(val);
    const int x = 38845;
    std::cout << 38845;
    for (int i = 0; i < 38845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38845(int& val) {
    const int x = 38845;
    std::cout << 38845;
    for (int i = 0; i < 38845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38846(int& val) {
    func_in_38846(val);
    const int x = 38846;
    std::cout << 38846;
    for (int i = 0; i < 38846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38846(int& val) {
    const int x = 38846;
    std::cout << 38846;
    for (int i = 0; i < 38846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38847(int& val) {
    func_in_38847(val);
    const int x = 38847;
    std::cout << 38847;
    for (int i = 0; i < 38847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38847(int& val) {
    const int x = 38847;
    std::cout << 38847;
    for (int i = 0; i < 38847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38848(int& val) {
    func_in_38848(val);
    const int x = 38848;
    std::cout << 38848;
    for (int i = 0; i < 38848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38848(int& val) {
    const int x = 38848;
    std::cout << 38848;
    for (int i = 0; i < 38848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38849(int& val) {
    func_in_38849(val);
    const int x = 38849;
    std::cout << 38849;
    for (int i = 0; i < 38849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38849(int& val) {
    const int x = 38849;
    std::cout << 38849;
    for (int i = 0; i < 38849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38850(int& val) {
    func_in_38850(val);
    const int x = 38850;
    std::cout << 38850;
    for (int i = 0; i < 38850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38850(int& val) {
    const int x = 38850;
    std::cout << 38850;
    for (int i = 0; i < 38850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38851(int& val) {
    func_in_38851(val);
    const int x = 38851;
    std::cout << 38851;
    for (int i = 0; i < 38851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38851(int& val) {
    const int x = 38851;
    std::cout << 38851;
    for (int i = 0; i < 38851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38852(int& val) {
    func_in_38852(val);
    const int x = 38852;
    std::cout << 38852;
    for (int i = 0; i < 38852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38852(int& val) {
    const int x = 38852;
    std::cout << 38852;
    for (int i = 0; i < 38852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38853(int& val) {
    func_in_38853(val);
    const int x = 38853;
    std::cout << 38853;
    for (int i = 0; i < 38853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38853(int& val) {
    const int x = 38853;
    std::cout << 38853;
    for (int i = 0; i < 38853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38854(int& val) {
    func_in_38854(val);
    const int x = 38854;
    std::cout << 38854;
    for (int i = 0; i < 38854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38854(int& val) {
    const int x = 38854;
    std::cout << 38854;
    for (int i = 0; i < 38854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38855(int& val) {
    func_in_38855(val);
    const int x = 38855;
    std::cout << 38855;
    for (int i = 0; i < 38855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38855(int& val) {
    const int x = 38855;
    std::cout << 38855;
    for (int i = 0; i < 38855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38856(int& val) {
    func_in_38856(val);
    const int x = 38856;
    std::cout << 38856;
    for (int i = 0; i < 38856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38856(int& val) {
    const int x = 38856;
    std::cout << 38856;
    for (int i = 0; i < 38856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38857(int& val) {
    func_in_38857(val);
    const int x = 38857;
    std::cout << 38857;
    for (int i = 0; i < 38857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38857(int& val) {
    const int x = 38857;
    std::cout << 38857;
    for (int i = 0; i < 38857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38858(int& val) {
    func_in_38858(val);
    const int x = 38858;
    std::cout << 38858;
    for (int i = 0; i < 38858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38858(int& val) {
    const int x = 38858;
    std::cout << 38858;
    for (int i = 0; i < 38858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38859(int& val) {
    func_in_38859(val);
    const int x = 38859;
    std::cout << 38859;
    for (int i = 0; i < 38859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38859(int& val) {
    const int x = 38859;
    std::cout << 38859;
    for (int i = 0; i < 38859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38860(int& val) {
    func_in_38860(val);
    const int x = 38860;
    std::cout << 38860;
    for (int i = 0; i < 38860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38860(int& val) {
    const int x = 38860;
    std::cout << 38860;
    for (int i = 0; i < 38860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38861(int& val) {
    func_in_38861(val);
    const int x = 38861;
    std::cout << 38861;
    for (int i = 0; i < 38861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38861(int& val) {
    const int x = 38861;
    std::cout << 38861;
    for (int i = 0; i < 38861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38862(int& val) {
    func_in_38862(val);
    const int x = 38862;
    std::cout << 38862;
    for (int i = 0; i < 38862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38862(int& val) {
    const int x = 38862;
    std::cout << 38862;
    for (int i = 0; i < 38862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38863(int& val) {
    func_in_38863(val);
    const int x = 38863;
    std::cout << 38863;
    for (int i = 0; i < 38863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38863(int& val) {
    const int x = 38863;
    std::cout << 38863;
    for (int i = 0; i < 38863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38864(int& val) {
    func_in_38864(val);
    const int x = 38864;
    std::cout << 38864;
    for (int i = 0; i < 38864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38864(int& val) {
    const int x = 38864;
    std::cout << 38864;
    for (int i = 0; i < 38864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38865(int& val) {
    func_in_38865(val);
    const int x = 38865;
    std::cout << 38865;
    for (int i = 0; i < 38865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38865(int& val) {
    const int x = 38865;
    std::cout << 38865;
    for (int i = 0; i < 38865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38866(int& val) {
    func_in_38866(val);
    const int x = 38866;
    std::cout << 38866;
    for (int i = 0; i < 38866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38866(int& val) {
    const int x = 38866;
    std::cout << 38866;
    for (int i = 0; i < 38866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38867(int& val) {
    func_in_38867(val);
    const int x = 38867;
    std::cout << 38867;
    for (int i = 0; i < 38867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38867(int& val) {
    const int x = 38867;
    std::cout << 38867;
    for (int i = 0; i < 38867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38868(int& val) {
    func_in_38868(val);
    const int x = 38868;
    std::cout << 38868;
    for (int i = 0; i < 38868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38868(int& val) {
    const int x = 38868;
    std::cout << 38868;
    for (int i = 0; i < 38868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38869(int& val) {
    func_in_38869(val);
    const int x = 38869;
    std::cout << 38869;
    for (int i = 0; i < 38869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38869(int& val) {
    const int x = 38869;
    std::cout << 38869;
    for (int i = 0; i < 38869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38870(int& val) {
    func_in_38870(val);
    const int x = 38870;
    std::cout << 38870;
    for (int i = 0; i < 38870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38870(int& val) {
    const int x = 38870;
    std::cout << 38870;
    for (int i = 0; i < 38870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38871(int& val) {
    func_in_38871(val);
    const int x = 38871;
    std::cout << 38871;
    for (int i = 0; i < 38871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38871(int& val) {
    const int x = 38871;
    std::cout << 38871;
    for (int i = 0; i < 38871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38872(int& val) {
    func_in_38872(val);
    const int x = 38872;
    std::cout << 38872;
    for (int i = 0; i < 38872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38872(int& val) {
    const int x = 38872;
    std::cout << 38872;
    for (int i = 0; i < 38872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38873(int& val) {
    func_in_38873(val);
    const int x = 38873;
    std::cout << 38873;
    for (int i = 0; i < 38873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38873(int& val) {
    const int x = 38873;
    std::cout << 38873;
    for (int i = 0; i < 38873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38874(int& val) {
    func_in_38874(val);
    const int x = 38874;
    std::cout << 38874;
    for (int i = 0; i < 38874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38874(int& val) {
    const int x = 38874;
    std::cout << 38874;
    for (int i = 0; i < 38874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38875(int& val) {
    func_in_38875(val);
    const int x = 38875;
    std::cout << 38875;
    for (int i = 0; i < 38875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38875(int& val) {
    const int x = 38875;
    std::cout << 38875;
    for (int i = 0; i < 38875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38876(int& val) {
    func_in_38876(val);
    const int x = 38876;
    std::cout << 38876;
    for (int i = 0; i < 38876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38876(int& val) {
    const int x = 38876;
    std::cout << 38876;
    for (int i = 0; i < 38876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38877(int& val) {
    func_in_38877(val);
    const int x = 38877;
    std::cout << 38877;
    for (int i = 0; i < 38877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38877(int& val) {
    const int x = 38877;
    std::cout << 38877;
    for (int i = 0; i < 38877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38878(int& val) {
    func_in_38878(val);
    const int x = 38878;
    std::cout << 38878;
    for (int i = 0; i < 38878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38878(int& val) {
    const int x = 38878;
    std::cout << 38878;
    for (int i = 0; i < 38878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38879(int& val) {
    func_in_38879(val);
    const int x = 38879;
    std::cout << 38879;
    for (int i = 0; i < 38879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38879(int& val) {
    const int x = 38879;
    std::cout << 38879;
    for (int i = 0; i < 38879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38880(int& val) {
    func_in_38880(val);
    const int x = 38880;
    std::cout << 38880;
    for (int i = 0; i < 38880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38880(int& val) {
    const int x = 38880;
    std::cout << 38880;
    for (int i = 0; i < 38880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38881(int& val) {
    func_in_38881(val);
    const int x = 38881;
    std::cout << 38881;
    for (int i = 0; i < 38881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38881(int& val) {
    const int x = 38881;
    std::cout << 38881;
    for (int i = 0; i < 38881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38882(int& val) {
    func_in_38882(val);
    const int x = 38882;
    std::cout << 38882;
    for (int i = 0; i < 38882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38882(int& val) {
    const int x = 38882;
    std::cout << 38882;
    for (int i = 0; i < 38882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38883(int& val) {
    func_in_38883(val);
    const int x = 38883;
    std::cout << 38883;
    for (int i = 0; i < 38883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38883(int& val) {
    const int x = 38883;
    std::cout << 38883;
    for (int i = 0; i < 38883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38884(int& val) {
    func_in_38884(val);
    const int x = 38884;
    std::cout << 38884;
    for (int i = 0; i < 38884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38884(int& val) {
    const int x = 38884;
    std::cout << 38884;
    for (int i = 0; i < 38884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38885(int& val) {
    func_in_38885(val);
    const int x = 38885;
    std::cout << 38885;
    for (int i = 0; i < 38885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38885(int& val) {
    const int x = 38885;
    std::cout << 38885;
    for (int i = 0; i < 38885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38886(int& val) {
    func_in_38886(val);
    const int x = 38886;
    std::cout << 38886;
    for (int i = 0; i < 38886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38886(int& val) {
    const int x = 38886;
    std::cout << 38886;
    for (int i = 0; i < 38886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38887(int& val) {
    func_in_38887(val);
    const int x = 38887;
    std::cout << 38887;
    for (int i = 0; i < 38887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38887(int& val) {
    const int x = 38887;
    std::cout << 38887;
    for (int i = 0; i < 38887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38888(int& val) {
    func_in_38888(val);
    const int x = 38888;
    std::cout << 38888;
    for (int i = 0; i < 38888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38888(int& val) {
    const int x = 38888;
    std::cout << 38888;
    for (int i = 0; i < 38888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38889(int& val) {
    func_in_38889(val);
    const int x = 38889;
    std::cout << 38889;
    for (int i = 0; i < 38889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38889(int& val) {
    const int x = 38889;
    std::cout << 38889;
    for (int i = 0; i < 38889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38890(int& val) {
    func_in_38890(val);
    const int x = 38890;
    std::cout << 38890;
    for (int i = 0; i < 38890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38890(int& val) {
    const int x = 38890;
    std::cout << 38890;
    for (int i = 0; i < 38890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38891(int& val) {
    func_in_38891(val);
    const int x = 38891;
    std::cout << 38891;
    for (int i = 0; i < 38891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38891(int& val) {
    const int x = 38891;
    std::cout << 38891;
    for (int i = 0; i < 38891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38892(int& val) {
    func_in_38892(val);
    const int x = 38892;
    std::cout << 38892;
    for (int i = 0; i < 38892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38892(int& val) {
    const int x = 38892;
    std::cout << 38892;
    for (int i = 0; i < 38892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38893(int& val) {
    func_in_38893(val);
    const int x = 38893;
    std::cout << 38893;
    for (int i = 0; i < 38893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38893(int& val) {
    const int x = 38893;
    std::cout << 38893;
    for (int i = 0; i < 38893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38894(int& val) {
    func_in_38894(val);
    const int x = 38894;
    std::cout << 38894;
    for (int i = 0; i < 38894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38894(int& val) {
    const int x = 38894;
    std::cout << 38894;
    for (int i = 0; i < 38894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38895(int& val) {
    func_in_38895(val);
    const int x = 38895;
    std::cout << 38895;
    for (int i = 0; i < 38895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38895(int& val) {
    const int x = 38895;
    std::cout << 38895;
    for (int i = 0; i < 38895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38896(int& val) {
    func_in_38896(val);
    const int x = 38896;
    std::cout << 38896;
    for (int i = 0; i < 38896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38896(int& val) {
    const int x = 38896;
    std::cout << 38896;
    for (int i = 0; i < 38896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38897(int& val) {
    func_in_38897(val);
    const int x = 38897;
    std::cout << 38897;
    for (int i = 0; i < 38897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38897(int& val) {
    const int x = 38897;
    std::cout << 38897;
    for (int i = 0; i < 38897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38898(int& val) {
    func_in_38898(val);
    const int x = 38898;
    std::cout << 38898;
    for (int i = 0; i < 38898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38898(int& val) {
    const int x = 38898;
    std::cout << 38898;
    for (int i = 0; i < 38898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38899(int& val) {
    func_in_38899(val);
    const int x = 38899;
    std::cout << 38899;
    for (int i = 0; i < 38899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38899(int& val) {
    const int x = 38899;
    std::cout << 38899;
    for (int i = 0; i < 38899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38900(int& val) {
    func_in_38900(val);
    const int x = 38900;
    std::cout << 38900;
    for (int i = 0; i < 38900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38900(int& val) {
    const int x = 38900;
    std::cout << 38900;
    for (int i = 0; i < 38900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38901(int& val) {
    func_in_38901(val);
    const int x = 38901;
    std::cout << 38901;
    for (int i = 0; i < 38901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38901(int& val) {
    const int x = 38901;
    std::cout << 38901;
    for (int i = 0; i < 38901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38902(int& val) {
    func_in_38902(val);
    const int x = 38902;
    std::cout << 38902;
    for (int i = 0; i < 38902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38902(int& val) {
    const int x = 38902;
    std::cout << 38902;
    for (int i = 0; i < 38902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38903(int& val) {
    func_in_38903(val);
    const int x = 38903;
    std::cout << 38903;
    for (int i = 0; i < 38903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38903(int& val) {
    const int x = 38903;
    std::cout << 38903;
    for (int i = 0; i < 38903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38904(int& val) {
    func_in_38904(val);
    const int x = 38904;
    std::cout << 38904;
    for (int i = 0; i < 38904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38904(int& val) {
    const int x = 38904;
    std::cout << 38904;
    for (int i = 0; i < 38904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38905(int& val) {
    func_in_38905(val);
    const int x = 38905;
    std::cout << 38905;
    for (int i = 0; i < 38905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38905(int& val) {
    const int x = 38905;
    std::cout << 38905;
    for (int i = 0; i < 38905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38906(int& val) {
    func_in_38906(val);
    const int x = 38906;
    std::cout << 38906;
    for (int i = 0; i < 38906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38906(int& val) {
    const int x = 38906;
    std::cout << 38906;
    for (int i = 0; i < 38906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38907(int& val) {
    func_in_38907(val);
    const int x = 38907;
    std::cout << 38907;
    for (int i = 0; i < 38907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38907(int& val) {
    const int x = 38907;
    std::cout << 38907;
    for (int i = 0; i < 38907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38908(int& val) {
    func_in_38908(val);
    const int x = 38908;
    std::cout << 38908;
    for (int i = 0; i < 38908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38908(int& val) {
    const int x = 38908;
    std::cout << 38908;
    for (int i = 0; i < 38908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38909(int& val) {
    func_in_38909(val);
    const int x = 38909;
    std::cout << 38909;
    for (int i = 0; i < 38909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38909(int& val) {
    const int x = 38909;
    std::cout << 38909;
    for (int i = 0; i < 38909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38910(int& val) {
    func_in_38910(val);
    const int x = 38910;
    std::cout << 38910;
    for (int i = 0; i < 38910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38910(int& val) {
    const int x = 38910;
    std::cout << 38910;
    for (int i = 0; i < 38910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38911(int& val) {
    func_in_38911(val);
    const int x = 38911;
    std::cout << 38911;
    for (int i = 0; i < 38911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38911(int& val) {
    const int x = 38911;
    std::cout << 38911;
    for (int i = 0; i < 38911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38912(int& val) {
    func_in_38912(val);
    const int x = 38912;
    std::cout << 38912;
    for (int i = 0; i < 38912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38912(int& val) {
    const int x = 38912;
    std::cout << 38912;
    for (int i = 0; i < 38912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38913(int& val) {
    func_in_38913(val);
    const int x = 38913;
    std::cout << 38913;
    for (int i = 0; i < 38913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38913(int& val) {
    const int x = 38913;
    std::cout << 38913;
    for (int i = 0; i < 38913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38914(int& val) {
    func_in_38914(val);
    const int x = 38914;
    std::cout << 38914;
    for (int i = 0; i < 38914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38914(int& val) {
    const int x = 38914;
    std::cout << 38914;
    for (int i = 0; i < 38914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38915(int& val) {
    func_in_38915(val);
    const int x = 38915;
    std::cout << 38915;
    for (int i = 0; i < 38915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38915(int& val) {
    const int x = 38915;
    std::cout << 38915;
    for (int i = 0; i < 38915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38916(int& val) {
    func_in_38916(val);
    const int x = 38916;
    std::cout << 38916;
    for (int i = 0; i < 38916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38916(int& val) {
    const int x = 38916;
    std::cout << 38916;
    for (int i = 0; i < 38916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38917(int& val) {
    func_in_38917(val);
    const int x = 38917;
    std::cout << 38917;
    for (int i = 0; i < 38917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38917(int& val) {
    const int x = 38917;
    std::cout << 38917;
    for (int i = 0; i < 38917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38918(int& val) {
    func_in_38918(val);
    const int x = 38918;
    std::cout << 38918;
    for (int i = 0; i < 38918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38918(int& val) {
    const int x = 38918;
    std::cout << 38918;
    for (int i = 0; i < 38918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38919(int& val) {
    func_in_38919(val);
    const int x = 38919;
    std::cout << 38919;
    for (int i = 0; i < 38919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38919(int& val) {
    const int x = 38919;
    std::cout << 38919;
    for (int i = 0; i < 38919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38920(int& val) {
    func_in_38920(val);
    const int x = 38920;
    std::cout << 38920;
    for (int i = 0; i < 38920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38920(int& val) {
    const int x = 38920;
    std::cout << 38920;
    for (int i = 0; i < 38920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38921(int& val) {
    func_in_38921(val);
    const int x = 38921;
    std::cout << 38921;
    for (int i = 0; i < 38921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38921(int& val) {
    const int x = 38921;
    std::cout << 38921;
    for (int i = 0; i < 38921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38922(int& val) {
    func_in_38922(val);
    const int x = 38922;
    std::cout << 38922;
    for (int i = 0; i < 38922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38922(int& val) {
    const int x = 38922;
    std::cout << 38922;
    for (int i = 0; i < 38922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38923(int& val) {
    func_in_38923(val);
    const int x = 38923;
    std::cout << 38923;
    for (int i = 0; i < 38923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38923(int& val) {
    const int x = 38923;
    std::cout << 38923;
    for (int i = 0; i < 38923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38924(int& val) {
    func_in_38924(val);
    const int x = 38924;
    std::cout << 38924;
    for (int i = 0; i < 38924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38924(int& val) {
    const int x = 38924;
    std::cout << 38924;
    for (int i = 0; i < 38924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38925(int& val) {
    func_in_38925(val);
    const int x = 38925;
    std::cout << 38925;
    for (int i = 0; i < 38925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38925(int& val) {
    const int x = 38925;
    std::cout << 38925;
    for (int i = 0; i < 38925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38926(int& val) {
    func_in_38926(val);
    const int x = 38926;
    std::cout << 38926;
    for (int i = 0; i < 38926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38926(int& val) {
    const int x = 38926;
    std::cout << 38926;
    for (int i = 0; i < 38926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38927(int& val) {
    func_in_38927(val);
    const int x = 38927;
    std::cout << 38927;
    for (int i = 0; i < 38927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38927(int& val) {
    const int x = 38927;
    std::cout << 38927;
    for (int i = 0; i < 38927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38928(int& val) {
    func_in_38928(val);
    const int x = 38928;
    std::cout << 38928;
    for (int i = 0; i < 38928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38928(int& val) {
    const int x = 38928;
    std::cout << 38928;
    for (int i = 0; i < 38928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38929(int& val) {
    func_in_38929(val);
    const int x = 38929;
    std::cout << 38929;
    for (int i = 0; i < 38929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38929(int& val) {
    const int x = 38929;
    std::cout << 38929;
    for (int i = 0; i < 38929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38930(int& val) {
    func_in_38930(val);
    const int x = 38930;
    std::cout << 38930;
    for (int i = 0; i < 38930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38930(int& val) {
    const int x = 38930;
    std::cout << 38930;
    for (int i = 0; i < 38930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38931(int& val) {
    func_in_38931(val);
    const int x = 38931;
    std::cout << 38931;
    for (int i = 0; i < 38931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38931(int& val) {
    const int x = 38931;
    std::cout << 38931;
    for (int i = 0; i < 38931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38932(int& val) {
    func_in_38932(val);
    const int x = 38932;
    std::cout << 38932;
    for (int i = 0; i < 38932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38932(int& val) {
    const int x = 38932;
    std::cout << 38932;
    for (int i = 0; i < 38932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38933(int& val) {
    func_in_38933(val);
    const int x = 38933;
    std::cout << 38933;
    for (int i = 0; i < 38933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38933(int& val) {
    const int x = 38933;
    std::cout << 38933;
    for (int i = 0; i < 38933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38934(int& val) {
    func_in_38934(val);
    const int x = 38934;
    std::cout << 38934;
    for (int i = 0; i < 38934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38934(int& val) {
    const int x = 38934;
    std::cout << 38934;
    for (int i = 0; i < 38934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38935(int& val) {
    func_in_38935(val);
    const int x = 38935;
    std::cout << 38935;
    for (int i = 0; i < 38935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38935(int& val) {
    const int x = 38935;
    std::cout << 38935;
    for (int i = 0; i < 38935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38936(int& val) {
    func_in_38936(val);
    const int x = 38936;
    std::cout << 38936;
    for (int i = 0; i < 38936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38936(int& val) {
    const int x = 38936;
    std::cout << 38936;
    for (int i = 0; i < 38936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38937(int& val) {
    func_in_38937(val);
    const int x = 38937;
    std::cout << 38937;
    for (int i = 0; i < 38937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38937(int& val) {
    const int x = 38937;
    std::cout << 38937;
    for (int i = 0; i < 38937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38938(int& val) {
    func_in_38938(val);
    const int x = 38938;
    std::cout << 38938;
    for (int i = 0; i < 38938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38938(int& val) {
    const int x = 38938;
    std::cout << 38938;
    for (int i = 0; i < 38938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38939(int& val) {
    func_in_38939(val);
    const int x = 38939;
    std::cout << 38939;
    for (int i = 0; i < 38939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38939(int& val) {
    const int x = 38939;
    std::cout << 38939;
    for (int i = 0; i < 38939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38940(int& val) {
    func_in_38940(val);
    const int x = 38940;
    std::cout << 38940;
    for (int i = 0; i < 38940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38940(int& val) {
    const int x = 38940;
    std::cout << 38940;
    for (int i = 0; i < 38940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38941(int& val) {
    func_in_38941(val);
    const int x = 38941;
    std::cout << 38941;
    for (int i = 0; i < 38941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38941(int& val) {
    const int x = 38941;
    std::cout << 38941;
    for (int i = 0; i < 38941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38942(int& val) {
    func_in_38942(val);
    const int x = 38942;
    std::cout << 38942;
    for (int i = 0; i < 38942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38942(int& val) {
    const int x = 38942;
    std::cout << 38942;
    for (int i = 0; i < 38942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38943(int& val) {
    func_in_38943(val);
    const int x = 38943;
    std::cout << 38943;
    for (int i = 0; i < 38943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38943(int& val) {
    const int x = 38943;
    std::cout << 38943;
    for (int i = 0; i < 38943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38944(int& val) {
    func_in_38944(val);
    const int x = 38944;
    std::cout << 38944;
    for (int i = 0; i < 38944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38944(int& val) {
    const int x = 38944;
    std::cout << 38944;
    for (int i = 0; i < 38944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38945(int& val) {
    func_in_38945(val);
    const int x = 38945;
    std::cout << 38945;
    for (int i = 0; i < 38945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38945(int& val) {
    const int x = 38945;
    std::cout << 38945;
    for (int i = 0; i < 38945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38946(int& val) {
    func_in_38946(val);
    const int x = 38946;
    std::cout << 38946;
    for (int i = 0; i < 38946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38946(int& val) {
    const int x = 38946;
    std::cout << 38946;
    for (int i = 0; i < 38946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38947(int& val) {
    func_in_38947(val);
    const int x = 38947;
    std::cout << 38947;
    for (int i = 0; i < 38947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38947(int& val) {
    const int x = 38947;
    std::cout << 38947;
    for (int i = 0; i < 38947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38948(int& val) {
    func_in_38948(val);
    const int x = 38948;
    std::cout << 38948;
    for (int i = 0; i < 38948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38948(int& val) {
    const int x = 38948;
    std::cout << 38948;
    for (int i = 0; i < 38948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38949(int& val) {
    func_in_38949(val);
    const int x = 38949;
    std::cout << 38949;
    for (int i = 0; i < 38949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38949(int& val) {
    const int x = 38949;
    std::cout << 38949;
    for (int i = 0; i < 38949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38950(int& val) {
    func_in_38950(val);
    const int x = 38950;
    std::cout << 38950;
    for (int i = 0; i < 38950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38950(int& val) {
    const int x = 38950;
    std::cout << 38950;
    for (int i = 0; i < 38950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38951(int& val) {
    func_in_38951(val);
    const int x = 38951;
    std::cout << 38951;
    for (int i = 0; i < 38951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38951(int& val) {
    const int x = 38951;
    std::cout << 38951;
    for (int i = 0; i < 38951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38952(int& val) {
    func_in_38952(val);
    const int x = 38952;
    std::cout << 38952;
    for (int i = 0; i < 38952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38952(int& val) {
    const int x = 38952;
    std::cout << 38952;
    for (int i = 0; i < 38952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38953(int& val) {
    func_in_38953(val);
    const int x = 38953;
    std::cout << 38953;
    for (int i = 0; i < 38953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38953(int& val) {
    const int x = 38953;
    std::cout << 38953;
    for (int i = 0; i < 38953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38954(int& val) {
    func_in_38954(val);
    const int x = 38954;
    std::cout << 38954;
    for (int i = 0; i < 38954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38954(int& val) {
    const int x = 38954;
    std::cout << 38954;
    for (int i = 0; i < 38954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38955(int& val) {
    func_in_38955(val);
    const int x = 38955;
    std::cout << 38955;
    for (int i = 0; i < 38955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38955(int& val) {
    const int x = 38955;
    std::cout << 38955;
    for (int i = 0; i < 38955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38956(int& val) {
    func_in_38956(val);
    const int x = 38956;
    std::cout << 38956;
    for (int i = 0; i < 38956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38956(int& val) {
    const int x = 38956;
    std::cout << 38956;
    for (int i = 0; i < 38956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38957(int& val) {
    func_in_38957(val);
    const int x = 38957;
    std::cout << 38957;
    for (int i = 0; i < 38957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38957(int& val) {
    const int x = 38957;
    std::cout << 38957;
    for (int i = 0; i < 38957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38958(int& val) {
    func_in_38958(val);
    const int x = 38958;
    std::cout << 38958;
    for (int i = 0; i < 38958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38958(int& val) {
    const int x = 38958;
    std::cout << 38958;
    for (int i = 0; i < 38958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38959(int& val) {
    func_in_38959(val);
    const int x = 38959;
    std::cout << 38959;
    for (int i = 0; i < 38959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38959(int& val) {
    const int x = 38959;
    std::cout << 38959;
    for (int i = 0; i < 38959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38960(int& val) {
    func_in_38960(val);
    const int x = 38960;
    std::cout << 38960;
    for (int i = 0; i < 38960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38960(int& val) {
    const int x = 38960;
    std::cout << 38960;
    for (int i = 0; i < 38960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38961(int& val) {
    func_in_38961(val);
    const int x = 38961;
    std::cout << 38961;
    for (int i = 0; i < 38961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38961(int& val) {
    const int x = 38961;
    std::cout << 38961;
    for (int i = 0; i < 38961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38962(int& val) {
    func_in_38962(val);
    const int x = 38962;
    std::cout << 38962;
    for (int i = 0; i < 38962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38962(int& val) {
    const int x = 38962;
    std::cout << 38962;
    for (int i = 0; i < 38962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38963(int& val) {
    func_in_38963(val);
    const int x = 38963;
    std::cout << 38963;
    for (int i = 0; i < 38963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38963(int& val) {
    const int x = 38963;
    std::cout << 38963;
    for (int i = 0; i < 38963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38964(int& val) {
    func_in_38964(val);
    const int x = 38964;
    std::cout << 38964;
    for (int i = 0; i < 38964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38964(int& val) {
    const int x = 38964;
    std::cout << 38964;
    for (int i = 0; i < 38964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38965(int& val) {
    func_in_38965(val);
    const int x = 38965;
    std::cout << 38965;
    for (int i = 0; i < 38965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38965(int& val) {
    const int x = 38965;
    std::cout << 38965;
    for (int i = 0; i < 38965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38966(int& val) {
    func_in_38966(val);
    const int x = 38966;
    std::cout << 38966;
    for (int i = 0; i < 38966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38966(int& val) {
    const int x = 38966;
    std::cout << 38966;
    for (int i = 0; i < 38966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38967(int& val) {
    func_in_38967(val);
    const int x = 38967;
    std::cout << 38967;
    for (int i = 0; i < 38967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38967(int& val) {
    const int x = 38967;
    std::cout << 38967;
    for (int i = 0; i < 38967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38968(int& val) {
    func_in_38968(val);
    const int x = 38968;
    std::cout << 38968;
    for (int i = 0; i < 38968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38968(int& val) {
    const int x = 38968;
    std::cout << 38968;
    for (int i = 0; i < 38968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38969(int& val) {
    func_in_38969(val);
    const int x = 38969;
    std::cout << 38969;
    for (int i = 0; i < 38969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38969(int& val) {
    const int x = 38969;
    std::cout << 38969;
    for (int i = 0; i < 38969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38970(int& val) {
    func_in_38970(val);
    const int x = 38970;
    std::cout << 38970;
    for (int i = 0; i < 38970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38970(int& val) {
    const int x = 38970;
    std::cout << 38970;
    for (int i = 0; i < 38970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38971(int& val) {
    func_in_38971(val);
    const int x = 38971;
    std::cout << 38971;
    for (int i = 0; i < 38971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38971(int& val) {
    const int x = 38971;
    std::cout << 38971;
    for (int i = 0; i < 38971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38972(int& val) {
    func_in_38972(val);
    const int x = 38972;
    std::cout << 38972;
    for (int i = 0; i < 38972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38972(int& val) {
    const int x = 38972;
    std::cout << 38972;
    for (int i = 0; i < 38972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38973(int& val) {
    func_in_38973(val);
    const int x = 38973;
    std::cout << 38973;
    for (int i = 0; i < 38973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38973(int& val) {
    const int x = 38973;
    std::cout << 38973;
    for (int i = 0; i < 38973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38974(int& val) {
    func_in_38974(val);
    const int x = 38974;
    std::cout << 38974;
    for (int i = 0; i < 38974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38974(int& val) {
    const int x = 38974;
    std::cout << 38974;
    for (int i = 0; i < 38974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38975(int& val) {
    func_in_38975(val);
    const int x = 38975;
    std::cout << 38975;
    for (int i = 0; i < 38975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38975(int& val) {
    const int x = 38975;
    std::cout << 38975;
    for (int i = 0; i < 38975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38976(int& val) {
    func_in_38976(val);
    const int x = 38976;
    std::cout << 38976;
    for (int i = 0; i < 38976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38976(int& val) {
    const int x = 38976;
    std::cout << 38976;
    for (int i = 0; i < 38976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38977(int& val) {
    func_in_38977(val);
    const int x = 38977;
    std::cout << 38977;
    for (int i = 0; i < 38977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38977(int& val) {
    const int x = 38977;
    std::cout << 38977;
    for (int i = 0; i < 38977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38978(int& val) {
    func_in_38978(val);
    const int x = 38978;
    std::cout << 38978;
    for (int i = 0; i < 38978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38978(int& val) {
    const int x = 38978;
    std::cout << 38978;
    for (int i = 0; i < 38978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38979(int& val) {
    func_in_38979(val);
    const int x = 38979;
    std::cout << 38979;
    for (int i = 0; i < 38979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38979(int& val) {
    const int x = 38979;
    std::cout << 38979;
    for (int i = 0; i < 38979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38980(int& val) {
    func_in_38980(val);
    const int x = 38980;
    std::cout << 38980;
    for (int i = 0; i < 38980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38980(int& val) {
    const int x = 38980;
    std::cout << 38980;
    for (int i = 0; i < 38980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38981(int& val) {
    func_in_38981(val);
    const int x = 38981;
    std::cout << 38981;
    for (int i = 0; i < 38981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38981(int& val) {
    const int x = 38981;
    std::cout << 38981;
    for (int i = 0; i < 38981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38982(int& val) {
    func_in_38982(val);
    const int x = 38982;
    std::cout << 38982;
    for (int i = 0; i < 38982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38982(int& val) {
    const int x = 38982;
    std::cout << 38982;
    for (int i = 0; i < 38982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38983(int& val) {
    func_in_38983(val);
    const int x = 38983;
    std::cout << 38983;
    for (int i = 0; i < 38983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38983(int& val) {
    const int x = 38983;
    std::cout << 38983;
    for (int i = 0; i < 38983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38984(int& val) {
    func_in_38984(val);
    const int x = 38984;
    std::cout << 38984;
    for (int i = 0; i < 38984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38984(int& val) {
    const int x = 38984;
    std::cout << 38984;
    for (int i = 0; i < 38984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38985(int& val) {
    func_in_38985(val);
    const int x = 38985;
    std::cout << 38985;
    for (int i = 0; i < 38985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38985(int& val) {
    const int x = 38985;
    std::cout << 38985;
    for (int i = 0; i < 38985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38986(int& val) {
    func_in_38986(val);
    const int x = 38986;
    std::cout << 38986;
    for (int i = 0; i < 38986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38986(int& val) {
    const int x = 38986;
    std::cout << 38986;
    for (int i = 0; i < 38986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38987(int& val) {
    func_in_38987(val);
    const int x = 38987;
    std::cout << 38987;
    for (int i = 0; i < 38987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38987(int& val) {
    const int x = 38987;
    std::cout << 38987;
    for (int i = 0; i < 38987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38988(int& val) {
    func_in_38988(val);
    const int x = 38988;
    std::cout << 38988;
    for (int i = 0; i < 38988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38988(int& val) {
    const int x = 38988;
    std::cout << 38988;
    for (int i = 0; i < 38988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38989(int& val) {
    func_in_38989(val);
    const int x = 38989;
    std::cout << 38989;
    for (int i = 0; i < 38989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38989(int& val) {
    const int x = 38989;
    std::cout << 38989;
    for (int i = 0; i < 38989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38990(int& val) {
    func_in_38990(val);
    const int x = 38990;
    std::cout << 38990;
    for (int i = 0; i < 38990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38990(int& val) {
    const int x = 38990;
    std::cout << 38990;
    for (int i = 0; i < 38990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38991(int& val) {
    func_in_38991(val);
    const int x = 38991;
    std::cout << 38991;
    for (int i = 0; i < 38991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38991(int& val) {
    const int x = 38991;
    std::cout << 38991;
    for (int i = 0; i < 38991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38992(int& val) {
    func_in_38992(val);
    const int x = 38992;
    std::cout << 38992;
    for (int i = 0; i < 38992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38992(int& val) {
    const int x = 38992;
    std::cout << 38992;
    for (int i = 0; i < 38992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38993(int& val) {
    func_in_38993(val);
    const int x = 38993;
    std::cout << 38993;
    for (int i = 0; i < 38993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38993(int& val) {
    const int x = 38993;
    std::cout << 38993;
    for (int i = 0; i < 38993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38994(int& val) {
    func_in_38994(val);
    const int x = 38994;
    std::cout << 38994;
    for (int i = 0; i < 38994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38994(int& val) {
    const int x = 38994;
    std::cout << 38994;
    for (int i = 0; i < 38994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38995(int& val) {
    func_in_38995(val);
    const int x = 38995;
    std::cout << 38995;
    for (int i = 0; i < 38995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38995(int& val) {
    const int x = 38995;
    std::cout << 38995;
    for (int i = 0; i < 38995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38996(int& val) {
    func_in_38996(val);
    const int x = 38996;
    std::cout << 38996;
    for (int i = 0; i < 38996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38996(int& val) {
    const int x = 38996;
    std::cout << 38996;
    for (int i = 0; i < 38996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38997(int& val) {
    func_in_38997(val);
    const int x = 38997;
    std::cout << 38997;
    for (int i = 0; i < 38997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38997(int& val) {
    const int x = 38997;
    std::cout << 38997;
    for (int i = 0; i < 38997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38998(int& val) {
    func_in_38998(val);
    const int x = 38998;
    std::cout << 38998;
    for (int i = 0; i < 38998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38998(int& val) {
    const int x = 38998;
    std::cout << 38998;
    for (int i = 0; i < 38998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38999(int& val) {
    func_in_38999(val);
    const int x = 38999;
    std::cout << 38999;
    for (int i = 0; i < 38999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38999(int& val) {
    const int x = 38999;
    std::cout << 38999;
    for (int i = 0; i < 38999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39000(int& val) {
    func_in_39000(val);
    const int x = 39000;
    std::cout << 39000;
    for (int i = 0; i < 39000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39000(int& val) {
    const int x = 39000;
    std::cout << 39000;
    for (int i = 0; i < 39000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39001(int& val) {
    func_in_39001(val);
    const int x = 39001;
    std::cout << 39001;
    for (int i = 0; i < 39001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39001(int& val) {
    const int x = 39001;
    std::cout << 39001;
    for (int i = 0; i < 39001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39002(int& val) {
    func_in_39002(val);
    const int x = 39002;
    std::cout << 39002;
    for (int i = 0; i < 39002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39002(int& val) {
    const int x = 39002;
    std::cout << 39002;
    for (int i = 0; i < 39002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39003(int& val) {
    func_in_39003(val);
    const int x = 39003;
    std::cout << 39003;
    for (int i = 0; i < 39003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39003(int& val) {
    const int x = 39003;
    std::cout << 39003;
    for (int i = 0; i < 39003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39004(int& val) {
    func_in_39004(val);
    const int x = 39004;
    std::cout << 39004;
    for (int i = 0; i < 39004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39004(int& val) {
    const int x = 39004;
    std::cout << 39004;
    for (int i = 0; i < 39004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39005(int& val) {
    func_in_39005(val);
    const int x = 39005;
    std::cout << 39005;
    for (int i = 0; i < 39005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39005(int& val) {
    const int x = 39005;
    std::cout << 39005;
    for (int i = 0; i < 39005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39006(int& val) {
    func_in_39006(val);
    const int x = 39006;
    std::cout << 39006;
    for (int i = 0; i < 39006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39006(int& val) {
    const int x = 39006;
    std::cout << 39006;
    for (int i = 0; i < 39006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39007(int& val) {
    func_in_39007(val);
    const int x = 39007;
    std::cout << 39007;
    for (int i = 0; i < 39007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39007(int& val) {
    const int x = 39007;
    std::cout << 39007;
    for (int i = 0; i < 39007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39008(int& val) {
    func_in_39008(val);
    const int x = 39008;
    std::cout << 39008;
    for (int i = 0; i < 39008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39008(int& val) {
    const int x = 39008;
    std::cout << 39008;
    for (int i = 0; i < 39008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39009(int& val) {
    func_in_39009(val);
    const int x = 39009;
    std::cout << 39009;
    for (int i = 0; i < 39009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39009(int& val) {
    const int x = 39009;
    std::cout << 39009;
    for (int i = 0; i < 39009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39010(int& val) {
    func_in_39010(val);
    const int x = 39010;
    std::cout << 39010;
    for (int i = 0; i < 39010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39010(int& val) {
    const int x = 39010;
    std::cout << 39010;
    for (int i = 0; i < 39010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39011(int& val) {
    func_in_39011(val);
    const int x = 39011;
    std::cout << 39011;
    for (int i = 0; i < 39011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39011(int& val) {
    const int x = 39011;
    std::cout << 39011;
    for (int i = 0; i < 39011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39012(int& val) {
    func_in_39012(val);
    const int x = 39012;
    std::cout << 39012;
    for (int i = 0; i < 39012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39012(int& val) {
    const int x = 39012;
    std::cout << 39012;
    for (int i = 0; i < 39012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39013(int& val) {
    func_in_39013(val);
    const int x = 39013;
    std::cout << 39013;
    for (int i = 0; i < 39013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39013(int& val) {
    const int x = 39013;
    std::cout << 39013;
    for (int i = 0; i < 39013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39014(int& val) {
    func_in_39014(val);
    const int x = 39014;
    std::cout << 39014;
    for (int i = 0; i < 39014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39014(int& val) {
    const int x = 39014;
    std::cout << 39014;
    for (int i = 0; i < 39014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39015(int& val) {
    func_in_39015(val);
    const int x = 39015;
    std::cout << 39015;
    for (int i = 0; i < 39015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39015(int& val) {
    const int x = 39015;
    std::cout << 39015;
    for (int i = 0; i < 39015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39016(int& val) {
    func_in_39016(val);
    const int x = 39016;
    std::cout << 39016;
    for (int i = 0; i < 39016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39016(int& val) {
    const int x = 39016;
    std::cout << 39016;
    for (int i = 0; i < 39016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39017(int& val) {
    func_in_39017(val);
    const int x = 39017;
    std::cout << 39017;
    for (int i = 0; i < 39017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39017(int& val) {
    const int x = 39017;
    std::cout << 39017;
    for (int i = 0; i < 39017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39018(int& val) {
    func_in_39018(val);
    const int x = 39018;
    std::cout << 39018;
    for (int i = 0; i < 39018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39018(int& val) {
    const int x = 39018;
    std::cout << 39018;
    for (int i = 0; i < 39018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39019(int& val) {
    func_in_39019(val);
    const int x = 39019;
    std::cout << 39019;
    for (int i = 0; i < 39019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39019(int& val) {
    const int x = 39019;
    std::cout << 39019;
    for (int i = 0; i < 39019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39020(int& val) {
    func_in_39020(val);
    const int x = 39020;
    std::cout << 39020;
    for (int i = 0; i < 39020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39020(int& val) {
    const int x = 39020;
    std::cout << 39020;
    for (int i = 0; i < 39020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39021(int& val) {
    func_in_39021(val);
    const int x = 39021;
    std::cout << 39021;
    for (int i = 0; i < 39021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39021(int& val) {
    const int x = 39021;
    std::cout << 39021;
    for (int i = 0; i < 39021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39022(int& val) {
    func_in_39022(val);
    const int x = 39022;
    std::cout << 39022;
    for (int i = 0; i < 39022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39022(int& val) {
    const int x = 39022;
    std::cout << 39022;
    for (int i = 0; i < 39022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39023(int& val) {
    func_in_39023(val);
    const int x = 39023;
    std::cout << 39023;
    for (int i = 0; i < 39023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39023(int& val) {
    const int x = 39023;
    std::cout << 39023;
    for (int i = 0; i < 39023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39024(int& val) {
    func_in_39024(val);
    const int x = 39024;
    std::cout << 39024;
    for (int i = 0; i < 39024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39024(int& val) {
    const int x = 39024;
    std::cout << 39024;
    for (int i = 0; i < 39024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39025(int& val) {
    func_in_39025(val);
    const int x = 39025;
    std::cout << 39025;
    for (int i = 0; i < 39025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39025(int& val) {
    const int x = 39025;
    std::cout << 39025;
    for (int i = 0; i < 39025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39026(int& val) {
    func_in_39026(val);
    const int x = 39026;
    std::cout << 39026;
    for (int i = 0; i < 39026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39026(int& val) {
    const int x = 39026;
    std::cout << 39026;
    for (int i = 0; i < 39026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39027(int& val) {
    func_in_39027(val);
    const int x = 39027;
    std::cout << 39027;
    for (int i = 0; i < 39027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39027(int& val) {
    const int x = 39027;
    std::cout << 39027;
    for (int i = 0; i < 39027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39028(int& val) {
    func_in_39028(val);
    const int x = 39028;
    std::cout << 39028;
    for (int i = 0; i < 39028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39028(int& val) {
    const int x = 39028;
    std::cout << 39028;
    for (int i = 0; i < 39028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39029(int& val) {
    func_in_39029(val);
    const int x = 39029;
    std::cout << 39029;
    for (int i = 0; i < 39029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39029(int& val) {
    const int x = 39029;
    std::cout << 39029;
    for (int i = 0; i < 39029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39030(int& val) {
    func_in_39030(val);
    const int x = 39030;
    std::cout << 39030;
    for (int i = 0; i < 39030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39030(int& val) {
    const int x = 39030;
    std::cout << 39030;
    for (int i = 0; i < 39030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39031(int& val) {
    func_in_39031(val);
    const int x = 39031;
    std::cout << 39031;
    for (int i = 0; i < 39031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39031(int& val) {
    const int x = 39031;
    std::cout << 39031;
    for (int i = 0; i < 39031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39032(int& val) {
    func_in_39032(val);
    const int x = 39032;
    std::cout << 39032;
    for (int i = 0; i < 39032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39032(int& val) {
    const int x = 39032;
    std::cout << 39032;
    for (int i = 0; i < 39032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39033(int& val) {
    func_in_39033(val);
    const int x = 39033;
    std::cout << 39033;
    for (int i = 0; i < 39033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39033(int& val) {
    const int x = 39033;
    std::cout << 39033;
    for (int i = 0; i < 39033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39034(int& val) {
    func_in_39034(val);
    const int x = 39034;
    std::cout << 39034;
    for (int i = 0; i < 39034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39034(int& val) {
    const int x = 39034;
    std::cout << 39034;
    for (int i = 0; i < 39034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39035(int& val) {
    func_in_39035(val);
    const int x = 39035;
    std::cout << 39035;
    for (int i = 0; i < 39035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39035(int& val) {
    const int x = 39035;
    std::cout << 39035;
    for (int i = 0; i < 39035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39036(int& val) {
    func_in_39036(val);
    const int x = 39036;
    std::cout << 39036;
    for (int i = 0; i < 39036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39036(int& val) {
    const int x = 39036;
    std::cout << 39036;
    for (int i = 0; i < 39036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39037(int& val) {
    func_in_39037(val);
    const int x = 39037;
    std::cout << 39037;
    for (int i = 0; i < 39037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39037(int& val) {
    const int x = 39037;
    std::cout << 39037;
    for (int i = 0; i < 39037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39038(int& val) {
    func_in_39038(val);
    const int x = 39038;
    std::cout << 39038;
    for (int i = 0; i < 39038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39038(int& val) {
    const int x = 39038;
    std::cout << 39038;
    for (int i = 0; i < 39038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39039(int& val) {
    func_in_39039(val);
    const int x = 39039;
    std::cout << 39039;
    for (int i = 0; i < 39039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39039(int& val) {
    const int x = 39039;
    std::cout << 39039;
    for (int i = 0; i < 39039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39040(int& val) {
    func_in_39040(val);
    const int x = 39040;
    std::cout << 39040;
    for (int i = 0; i < 39040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39040(int& val) {
    const int x = 39040;
    std::cout << 39040;
    for (int i = 0; i < 39040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39041(int& val) {
    func_in_39041(val);
    const int x = 39041;
    std::cout << 39041;
    for (int i = 0; i < 39041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39041(int& val) {
    const int x = 39041;
    std::cout << 39041;
    for (int i = 0; i < 39041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39042(int& val) {
    func_in_39042(val);
    const int x = 39042;
    std::cout << 39042;
    for (int i = 0; i < 39042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39042(int& val) {
    const int x = 39042;
    std::cout << 39042;
    for (int i = 0; i < 39042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39043(int& val) {
    func_in_39043(val);
    const int x = 39043;
    std::cout << 39043;
    for (int i = 0; i < 39043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39043(int& val) {
    const int x = 39043;
    std::cout << 39043;
    for (int i = 0; i < 39043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39044(int& val) {
    func_in_39044(val);
    const int x = 39044;
    std::cout << 39044;
    for (int i = 0; i < 39044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39044(int& val) {
    const int x = 39044;
    std::cout << 39044;
    for (int i = 0; i < 39044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39045(int& val) {
    func_in_39045(val);
    const int x = 39045;
    std::cout << 39045;
    for (int i = 0; i < 39045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39045(int& val) {
    const int x = 39045;
    std::cout << 39045;
    for (int i = 0; i < 39045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39046(int& val) {
    func_in_39046(val);
    const int x = 39046;
    std::cout << 39046;
    for (int i = 0; i < 39046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39046(int& val) {
    const int x = 39046;
    std::cout << 39046;
    for (int i = 0; i < 39046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39047(int& val) {
    func_in_39047(val);
    const int x = 39047;
    std::cout << 39047;
    for (int i = 0; i < 39047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39047(int& val) {
    const int x = 39047;
    std::cout << 39047;
    for (int i = 0; i < 39047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39048(int& val) {
    func_in_39048(val);
    const int x = 39048;
    std::cout << 39048;
    for (int i = 0; i < 39048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39048(int& val) {
    const int x = 39048;
    std::cout << 39048;
    for (int i = 0; i < 39048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39049(int& val) {
    func_in_39049(val);
    const int x = 39049;
    std::cout << 39049;
    for (int i = 0; i < 39049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39049(int& val) {
    const int x = 39049;
    std::cout << 39049;
    for (int i = 0; i < 39049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39050(int& val) {
    func_in_39050(val);
    const int x = 39050;
    std::cout << 39050;
    for (int i = 0; i < 39050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39050(int& val) {
    const int x = 39050;
    std::cout << 39050;
    for (int i = 0; i < 39050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39051(int& val) {
    func_in_39051(val);
    const int x = 39051;
    std::cout << 39051;
    for (int i = 0; i < 39051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39051(int& val) {
    const int x = 39051;
    std::cout << 39051;
    for (int i = 0; i < 39051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39052(int& val) {
    func_in_39052(val);
    const int x = 39052;
    std::cout << 39052;
    for (int i = 0; i < 39052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39052(int& val) {
    const int x = 39052;
    std::cout << 39052;
    for (int i = 0; i < 39052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39053(int& val) {
    func_in_39053(val);
    const int x = 39053;
    std::cout << 39053;
    for (int i = 0; i < 39053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39053(int& val) {
    const int x = 39053;
    std::cout << 39053;
    for (int i = 0; i < 39053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39054(int& val) {
    func_in_39054(val);
    const int x = 39054;
    std::cout << 39054;
    for (int i = 0; i < 39054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39054(int& val) {
    const int x = 39054;
    std::cout << 39054;
    for (int i = 0; i < 39054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39055(int& val) {
    func_in_39055(val);
    const int x = 39055;
    std::cout << 39055;
    for (int i = 0; i < 39055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39055(int& val) {
    const int x = 39055;
    std::cout << 39055;
    for (int i = 0; i < 39055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39056(int& val) {
    func_in_39056(val);
    const int x = 39056;
    std::cout << 39056;
    for (int i = 0; i < 39056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39056(int& val) {
    const int x = 39056;
    std::cout << 39056;
    for (int i = 0; i < 39056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39057(int& val) {
    func_in_39057(val);
    const int x = 39057;
    std::cout << 39057;
    for (int i = 0; i < 39057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39057(int& val) {
    const int x = 39057;
    std::cout << 39057;
    for (int i = 0; i < 39057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39058(int& val) {
    func_in_39058(val);
    const int x = 39058;
    std::cout << 39058;
    for (int i = 0; i < 39058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39058(int& val) {
    const int x = 39058;
    std::cout << 39058;
    for (int i = 0; i < 39058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39059(int& val) {
    func_in_39059(val);
    const int x = 39059;
    std::cout << 39059;
    for (int i = 0; i < 39059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39059(int& val) {
    const int x = 39059;
    std::cout << 39059;
    for (int i = 0; i < 39059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39060(int& val) {
    func_in_39060(val);
    const int x = 39060;
    std::cout << 39060;
    for (int i = 0; i < 39060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39060(int& val) {
    const int x = 39060;
    std::cout << 39060;
    for (int i = 0; i < 39060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39061(int& val) {
    func_in_39061(val);
    const int x = 39061;
    std::cout << 39061;
    for (int i = 0; i < 39061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39061(int& val) {
    const int x = 39061;
    std::cout << 39061;
    for (int i = 0; i < 39061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39062(int& val) {
    func_in_39062(val);
    const int x = 39062;
    std::cout << 39062;
    for (int i = 0; i < 39062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39062(int& val) {
    const int x = 39062;
    std::cout << 39062;
    for (int i = 0; i < 39062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39063(int& val) {
    func_in_39063(val);
    const int x = 39063;
    std::cout << 39063;
    for (int i = 0; i < 39063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39063(int& val) {
    const int x = 39063;
    std::cout << 39063;
    for (int i = 0; i < 39063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39064(int& val) {
    func_in_39064(val);
    const int x = 39064;
    std::cout << 39064;
    for (int i = 0; i < 39064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39064(int& val) {
    const int x = 39064;
    std::cout << 39064;
    for (int i = 0; i < 39064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39065(int& val) {
    func_in_39065(val);
    const int x = 39065;
    std::cout << 39065;
    for (int i = 0; i < 39065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39065(int& val) {
    const int x = 39065;
    std::cout << 39065;
    for (int i = 0; i < 39065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39066(int& val) {
    func_in_39066(val);
    const int x = 39066;
    std::cout << 39066;
    for (int i = 0; i < 39066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39066(int& val) {
    const int x = 39066;
    std::cout << 39066;
    for (int i = 0; i < 39066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39067(int& val) {
    func_in_39067(val);
    const int x = 39067;
    std::cout << 39067;
    for (int i = 0; i < 39067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39067(int& val) {
    const int x = 39067;
    std::cout << 39067;
    for (int i = 0; i < 39067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39068(int& val) {
    func_in_39068(val);
    const int x = 39068;
    std::cout << 39068;
    for (int i = 0; i < 39068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39068(int& val) {
    const int x = 39068;
    std::cout << 39068;
    for (int i = 0; i < 39068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39069(int& val) {
    func_in_39069(val);
    const int x = 39069;
    std::cout << 39069;
    for (int i = 0; i < 39069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39069(int& val) {
    const int x = 39069;
    std::cout << 39069;
    for (int i = 0; i < 39069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39070(int& val) {
    func_in_39070(val);
    const int x = 39070;
    std::cout << 39070;
    for (int i = 0; i < 39070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39070(int& val) {
    const int x = 39070;
    std::cout << 39070;
    for (int i = 0; i < 39070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39071(int& val) {
    func_in_39071(val);
    const int x = 39071;
    std::cout << 39071;
    for (int i = 0; i < 39071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39071(int& val) {
    const int x = 39071;
    std::cout << 39071;
    for (int i = 0; i < 39071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39072(int& val) {
    func_in_39072(val);
    const int x = 39072;
    std::cout << 39072;
    for (int i = 0; i < 39072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39072(int& val) {
    const int x = 39072;
    std::cout << 39072;
    for (int i = 0; i < 39072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39073(int& val) {
    func_in_39073(val);
    const int x = 39073;
    std::cout << 39073;
    for (int i = 0; i < 39073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39073(int& val) {
    const int x = 39073;
    std::cout << 39073;
    for (int i = 0; i < 39073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39074(int& val) {
    func_in_39074(val);
    const int x = 39074;
    std::cout << 39074;
    for (int i = 0; i < 39074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39074(int& val) {
    const int x = 39074;
    std::cout << 39074;
    for (int i = 0; i < 39074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39075(int& val) {
    func_in_39075(val);
    const int x = 39075;
    std::cout << 39075;
    for (int i = 0; i < 39075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39075(int& val) {
    const int x = 39075;
    std::cout << 39075;
    for (int i = 0; i < 39075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39076(int& val) {
    func_in_39076(val);
    const int x = 39076;
    std::cout << 39076;
    for (int i = 0; i < 39076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39076(int& val) {
    const int x = 39076;
    std::cout << 39076;
    for (int i = 0; i < 39076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39077(int& val) {
    func_in_39077(val);
    const int x = 39077;
    std::cout << 39077;
    for (int i = 0; i < 39077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39077(int& val) {
    const int x = 39077;
    std::cout << 39077;
    for (int i = 0; i < 39077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39078(int& val) {
    func_in_39078(val);
    const int x = 39078;
    std::cout << 39078;
    for (int i = 0; i < 39078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39078(int& val) {
    const int x = 39078;
    std::cout << 39078;
    for (int i = 0; i < 39078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39079(int& val) {
    func_in_39079(val);
    const int x = 39079;
    std::cout << 39079;
    for (int i = 0; i < 39079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39079(int& val) {
    const int x = 39079;
    std::cout << 39079;
    for (int i = 0; i < 39079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39080(int& val) {
    func_in_39080(val);
    const int x = 39080;
    std::cout << 39080;
    for (int i = 0; i < 39080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39080(int& val) {
    const int x = 39080;
    std::cout << 39080;
    for (int i = 0; i < 39080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39081(int& val) {
    func_in_39081(val);
    const int x = 39081;
    std::cout << 39081;
    for (int i = 0; i < 39081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39081(int& val) {
    const int x = 39081;
    std::cout << 39081;
    for (int i = 0; i < 39081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39082(int& val) {
    func_in_39082(val);
    const int x = 39082;
    std::cout << 39082;
    for (int i = 0; i < 39082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39082(int& val) {
    const int x = 39082;
    std::cout << 39082;
    for (int i = 0; i < 39082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39083(int& val) {
    func_in_39083(val);
    const int x = 39083;
    std::cout << 39083;
    for (int i = 0; i < 39083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39083(int& val) {
    const int x = 39083;
    std::cout << 39083;
    for (int i = 0; i < 39083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39084(int& val) {
    func_in_39084(val);
    const int x = 39084;
    std::cout << 39084;
    for (int i = 0; i < 39084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39084(int& val) {
    const int x = 39084;
    std::cout << 39084;
    for (int i = 0; i < 39084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39085(int& val) {
    func_in_39085(val);
    const int x = 39085;
    std::cout << 39085;
    for (int i = 0; i < 39085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39085(int& val) {
    const int x = 39085;
    std::cout << 39085;
    for (int i = 0; i < 39085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39086(int& val) {
    func_in_39086(val);
    const int x = 39086;
    std::cout << 39086;
    for (int i = 0; i < 39086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39086(int& val) {
    const int x = 39086;
    std::cout << 39086;
    for (int i = 0; i < 39086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39087(int& val) {
    func_in_39087(val);
    const int x = 39087;
    std::cout << 39087;
    for (int i = 0; i < 39087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39087(int& val) {
    const int x = 39087;
    std::cout << 39087;
    for (int i = 0; i < 39087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39088(int& val) {
    func_in_39088(val);
    const int x = 39088;
    std::cout << 39088;
    for (int i = 0; i < 39088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39088(int& val) {
    const int x = 39088;
    std::cout << 39088;
    for (int i = 0; i < 39088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39089(int& val) {
    func_in_39089(val);
    const int x = 39089;
    std::cout << 39089;
    for (int i = 0; i < 39089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39089(int& val) {
    const int x = 39089;
    std::cout << 39089;
    for (int i = 0; i < 39089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39090(int& val) {
    func_in_39090(val);
    const int x = 39090;
    std::cout << 39090;
    for (int i = 0; i < 39090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39090(int& val) {
    const int x = 39090;
    std::cout << 39090;
    for (int i = 0; i < 39090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39091(int& val) {
    func_in_39091(val);
    const int x = 39091;
    std::cout << 39091;
    for (int i = 0; i < 39091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39091(int& val) {
    const int x = 39091;
    std::cout << 39091;
    for (int i = 0; i < 39091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39092(int& val) {
    func_in_39092(val);
    const int x = 39092;
    std::cout << 39092;
    for (int i = 0; i < 39092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39092(int& val) {
    const int x = 39092;
    std::cout << 39092;
    for (int i = 0; i < 39092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39093(int& val) {
    func_in_39093(val);
    const int x = 39093;
    std::cout << 39093;
    for (int i = 0; i < 39093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39093(int& val) {
    const int x = 39093;
    std::cout << 39093;
    for (int i = 0; i < 39093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39094(int& val) {
    func_in_39094(val);
    const int x = 39094;
    std::cout << 39094;
    for (int i = 0; i < 39094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39094(int& val) {
    const int x = 39094;
    std::cout << 39094;
    for (int i = 0; i < 39094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39095(int& val) {
    func_in_39095(val);
    const int x = 39095;
    std::cout << 39095;
    for (int i = 0; i < 39095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39095(int& val) {
    const int x = 39095;
    std::cout << 39095;
    for (int i = 0; i < 39095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39096(int& val) {
    func_in_39096(val);
    const int x = 39096;
    std::cout << 39096;
    for (int i = 0; i < 39096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39096(int& val) {
    const int x = 39096;
    std::cout << 39096;
    for (int i = 0; i < 39096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39097(int& val) {
    func_in_39097(val);
    const int x = 39097;
    std::cout << 39097;
    for (int i = 0; i < 39097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39097(int& val) {
    const int x = 39097;
    std::cout << 39097;
    for (int i = 0; i < 39097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39098(int& val) {
    func_in_39098(val);
    const int x = 39098;
    std::cout << 39098;
    for (int i = 0; i < 39098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39098(int& val) {
    const int x = 39098;
    std::cout << 39098;
    for (int i = 0; i < 39098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39099(int& val) {
    func_in_39099(val);
    const int x = 39099;
    std::cout << 39099;
    for (int i = 0; i < 39099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39099(int& val) {
    const int x = 39099;
    std::cout << 39099;
    for (int i = 0; i < 39099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39100(int& val) {
    func_in_39100(val);
    const int x = 39100;
    std::cout << 39100;
    for (int i = 0; i < 39100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39100(int& val) {
    const int x = 39100;
    std::cout << 39100;
    for (int i = 0; i < 39100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39101(int& val) {
    func_in_39101(val);
    const int x = 39101;
    std::cout << 39101;
    for (int i = 0; i < 39101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39101(int& val) {
    const int x = 39101;
    std::cout << 39101;
    for (int i = 0; i < 39101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39102(int& val) {
    func_in_39102(val);
    const int x = 39102;
    std::cout << 39102;
    for (int i = 0; i < 39102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39102(int& val) {
    const int x = 39102;
    std::cout << 39102;
    for (int i = 0; i < 39102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39103(int& val) {
    func_in_39103(val);
    const int x = 39103;
    std::cout << 39103;
    for (int i = 0; i < 39103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39103(int& val) {
    const int x = 39103;
    std::cout << 39103;
    for (int i = 0; i < 39103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39104(int& val) {
    func_in_39104(val);
    const int x = 39104;
    std::cout << 39104;
    for (int i = 0; i < 39104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39104(int& val) {
    const int x = 39104;
    std::cout << 39104;
    for (int i = 0; i < 39104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39105(int& val) {
    func_in_39105(val);
    const int x = 39105;
    std::cout << 39105;
    for (int i = 0; i < 39105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39105(int& val) {
    const int x = 39105;
    std::cout << 39105;
    for (int i = 0; i < 39105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39106(int& val) {
    func_in_39106(val);
    const int x = 39106;
    std::cout << 39106;
    for (int i = 0; i < 39106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39106(int& val) {
    const int x = 39106;
    std::cout << 39106;
    for (int i = 0; i < 39106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39107(int& val) {
    func_in_39107(val);
    const int x = 39107;
    std::cout << 39107;
    for (int i = 0; i < 39107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39107(int& val) {
    const int x = 39107;
    std::cout << 39107;
    for (int i = 0; i < 39107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39108(int& val) {
    func_in_39108(val);
    const int x = 39108;
    std::cout << 39108;
    for (int i = 0; i < 39108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39108(int& val) {
    const int x = 39108;
    std::cout << 39108;
    for (int i = 0; i < 39108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39109(int& val) {
    func_in_39109(val);
    const int x = 39109;
    std::cout << 39109;
    for (int i = 0; i < 39109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39109(int& val) {
    const int x = 39109;
    std::cout << 39109;
    for (int i = 0; i < 39109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39110(int& val) {
    func_in_39110(val);
    const int x = 39110;
    std::cout << 39110;
    for (int i = 0; i < 39110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39110(int& val) {
    const int x = 39110;
    std::cout << 39110;
    for (int i = 0; i < 39110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39111(int& val) {
    func_in_39111(val);
    const int x = 39111;
    std::cout << 39111;
    for (int i = 0; i < 39111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39111(int& val) {
    const int x = 39111;
    std::cout << 39111;
    for (int i = 0; i < 39111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39112(int& val) {
    func_in_39112(val);
    const int x = 39112;
    std::cout << 39112;
    for (int i = 0; i < 39112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39112(int& val) {
    const int x = 39112;
    std::cout << 39112;
    for (int i = 0; i < 39112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39113(int& val) {
    func_in_39113(val);
    const int x = 39113;
    std::cout << 39113;
    for (int i = 0; i < 39113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39113(int& val) {
    const int x = 39113;
    std::cout << 39113;
    for (int i = 0; i < 39113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39114(int& val) {
    func_in_39114(val);
    const int x = 39114;
    std::cout << 39114;
    for (int i = 0; i < 39114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39114(int& val) {
    const int x = 39114;
    std::cout << 39114;
    for (int i = 0; i < 39114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39115(int& val) {
    func_in_39115(val);
    const int x = 39115;
    std::cout << 39115;
    for (int i = 0; i < 39115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39115(int& val) {
    const int x = 39115;
    std::cout << 39115;
    for (int i = 0; i < 39115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39116(int& val) {
    func_in_39116(val);
    const int x = 39116;
    std::cout << 39116;
    for (int i = 0; i < 39116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39116(int& val) {
    const int x = 39116;
    std::cout << 39116;
    for (int i = 0; i < 39116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39117(int& val) {
    func_in_39117(val);
    const int x = 39117;
    std::cout << 39117;
    for (int i = 0; i < 39117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39117(int& val) {
    const int x = 39117;
    std::cout << 39117;
    for (int i = 0; i < 39117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39118(int& val) {
    func_in_39118(val);
    const int x = 39118;
    std::cout << 39118;
    for (int i = 0; i < 39118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39118(int& val) {
    const int x = 39118;
    std::cout << 39118;
    for (int i = 0; i < 39118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39119(int& val) {
    func_in_39119(val);
    const int x = 39119;
    std::cout << 39119;
    for (int i = 0; i < 39119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39119(int& val) {
    const int x = 39119;
    std::cout << 39119;
    for (int i = 0; i < 39119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39120(int& val) {
    func_in_39120(val);
    const int x = 39120;
    std::cout << 39120;
    for (int i = 0; i < 39120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39120(int& val) {
    const int x = 39120;
    std::cout << 39120;
    for (int i = 0; i < 39120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39121(int& val) {
    func_in_39121(val);
    const int x = 39121;
    std::cout << 39121;
    for (int i = 0; i < 39121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39121(int& val) {
    const int x = 39121;
    std::cout << 39121;
    for (int i = 0; i < 39121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39122(int& val) {
    func_in_39122(val);
    const int x = 39122;
    std::cout << 39122;
    for (int i = 0; i < 39122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39122(int& val) {
    const int x = 39122;
    std::cout << 39122;
    for (int i = 0; i < 39122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39123(int& val) {
    func_in_39123(val);
    const int x = 39123;
    std::cout << 39123;
    for (int i = 0; i < 39123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39123(int& val) {
    const int x = 39123;
    std::cout << 39123;
    for (int i = 0; i < 39123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39124(int& val) {
    func_in_39124(val);
    const int x = 39124;
    std::cout << 39124;
    for (int i = 0; i < 39124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39124(int& val) {
    const int x = 39124;
    std::cout << 39124;
    for (int i = 0; i < 39124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39125(int& val) {
    func_in_39125(val);
    const int x = 39125;
    std::cout << 39125;
    for (int i = 0; i < 39125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39125(int& val) {
    const int x = 39125;
    std::cout << 39125;
    for (int i = 0; i < 39125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39126(int& val) {
    func_in_39126(val);
    const int x = 39126;
    std::cout << 39126;
    for (int i = 0; i < 39126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39126(int& val) {
    const int x = 39126;
    std::cout << 39126;
    for (int i = 0; i < 39126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39127(int& val) {
    func_in_39127(val);
    const int x = 39127;
    std::cout << 39127;
    for (int i = 0; i < 39127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39127(int& val) {
    const int x = 39127;
    std::cout << 39127;
    for (int i = 0; i < 39127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39128(int& val) {
    func_in_39128(val);
    const int x = 39128;
    std::cout << 39128;
    for (int i = 0; i < 39128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39128(int& val) {
    const int x = 39128;
    std::cout << 39128;
    for (int i = 0; i < 39128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39129(int& val) {
    func_in_39129(val);
    const int x = 39129;
    std::cout << 39129;
    for (int i = 0; i < 39129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39129(int& val) {
    const int x = 39129;
    std::cout << 39129;
    for (int i = 0; i < 39129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39130(int& val) {
    func_in_39130(val);
    const int x = 39130;
    std::cout << 39130;
    for (int i = 0; i < 39130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39130(int& val) {
    const int x = 39130;
    std::cout << 39130;
    for (int i = 0; i < 39130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39131(int& val) {
    func_in_39131(val);
    const int x = 39131;
    std::cout << 39131;
    for (int i = 0; i < 39131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39131(int& val) {
    const int x = 39131;
    std::cout << 39131;
    for (int i = 0; i < 39131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39132(int& val) {
    func_in_39132(val);
    const int x = 39132;
    std::cout << 39132;
    for (int i = 0; i < 39132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39132(int& val) {
    const int x = 39132;
    std::cout << 39132;
    for (int i = 0; i < 39132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39133(int& val) {
    func_in_39133(val);
    const int x = 39133;
    std::cout << 39133;
    for (int i = 0; i < 39133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39133(int& val) {
    const int x = 39133;
    std::cout << 39133;
    for (int i = 0; i < 39133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39134(int& val) {
    func_in_39134(val);
    const int x = 39134;
    std::cout << 39134;
    for (int i = 0; i < 39134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39134(int& val) {
    const int x = 39134;
    std::cout << 39134;
    for (int i = 0; i < 39134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39135(int& val) {
    func_in_39135(val);
    const int x = 39135;
    std::cout << 39135;
    for (int i = 0; i < 39135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39135(int& val) {
    const int x = 39135;
    std::cout << 39135;
    for (int i = 0; i < 39135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39136(int& val) {
    func_in_39136(val);
    const int x = 39136;
    std::cout << 39136;
    for (int i = 0; i < 39136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39136(int& val) {
    const int x = 39136;
    std::cout << 39136;
    for (int i = 0; i < 39136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39137(int& val) {
    func_in_39137(val);
    const int x = 39137;
    std::cout << 39137;
    for (int i = 0; i < 39137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39137(int& val) {
    const int x = 39137;
    std::cout << 39137;
    for (int i = 0; i < 39137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39138(int& val) {
    func_in_39138(val);
    const int x = 39138;
    std::cout << 39138;
    for (int i = 0; i < 39138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39138(int& val) {
    const int x = 39138;
    std::cout << 39138;
    for (int i = 0; i < 39138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39139(int& val) {
    func_in_39139(val);
    const int x = 39139;
    std::cout << 39139;
    for (int i = 0; i < 39139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39139(int& val) {
    const int x = 39139;
    std::cout << 39139;
    for (int i = 0; i < 39139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39140(int& val) {
    func_in_39140(val);
    const int x = 39140;
    std::cout << 39140;
    for (int i = 0; i < 39140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39140(int& val) {
    const int x = 39140;
    std::cout << 39140;
    for (int i = 0; i < 39140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39141(int& val) {
    func_in_39141(val);
    const int x = 39141;
    std::cout << 39141;
    for (int i = 0; i < 39141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39141(int& val) {
    const int x = 39141;
    std::cout << 39141;
    for (int i = 0; i < 39141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39142(int& val) {
    func_in_39142(val);
    const int x = 39142;
    std::cout << 39142;
    for (int i = 0; i < 39142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39142(int& val) {
    const int x = 39142;
    std::cout << 39142;
    for (int i = 0; i < 39142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39143(int& val) {
    func_in_39143(val);
    const int x = 39143;
    std::cout << 39143;
    for (int i = 0; i < 39143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39143(int& val) {
    const int x = 39143;
    std::cout << 39143;
    for (int i = 0; i < 39143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39144(int& val) {
    func_in_39144(val);
    const int x = 39144;
    std::cout << 39144;
    for (int i = 0; i < 39144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39144(int& val) {
    const int x = 39144;
    std::cout << 39144;
    for (int i = 0; i < 39144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39145(int& val) {
    func_in_39145(val);
    const int x = 39145;
    std::cout << 39145;
    for (int i = 0; i < 39145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39145(int& val) {
    const int x = 39145;
    std::cout << 39145;
    for (int i = 0; i < 39145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39146(int& val) {
    func_in_39146(val);
    const int x = 39146;
    std::cout << 39146;
    for (int i = 0; i < 39146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39146(int& val) {
    const int x = 39146;
    std::cout << 39146;
    for (int i = 0; i < 39146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39147(int& val) {
    func_in_39147(val);
    const int x = 39147;
    std::cout << 39147;
    for (int i = 0; i < 39147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39147(int& val) {
    const int x = 39147;
    std::cout << 39147;
    for (int i = 0; i < 39147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39148(int& val) {
    func_in_39148(val);
    const int x = 39148;
    std::cout << 39148;
    for (int i = 0; i < 39148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39148(int& val) {
    const int x = 39148;
    std::cout << 39148;
    for (int i = 0; i < 39148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39149(int& val) {
    func_in_39149(val);
    const int x = 39149;
    std::cout << 39149;
    for (int i = 0; i < 39149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39149(int& val) {
    const int x = 39149;
    std::cout << 39149;
    for (int i = 0; i < 39149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39150(int& val) {
    func_in_39150(val);
    const int x = 39150;
    std::cout << 39150;
    for (int i = 0; i < 39150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39150(int& val) {
    const int x = 39150;
    std::cout << 39150;
    for (int i = 0; i < 39150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39151(int& val) {
    func_in_39151(val);
    const int x = 39151;
    std::cout << 39151;
    for (int i = 0; i < 39151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39151(int& val) {
    const int x = 39151;
    std::cout << 39151;
    for (int i = 0; i < 39151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39152(int& val) {
    func_in_39152(val);
    const int x = 39152;
    std::cout << 39152;
    for (int i = 0; i < 39152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39152(int& val) {
    const int x = 39152;
    std::cout << 39152;
    for (int i = 0; i < 39152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39153(int& val) {
    func_in_39153(val);
    const int x = 39153;
    std::cout << 39153;
    for (int i = 0; i < 39153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39153(int& val) {
    const int x = 39153;
    std::cout << 39153;
    for (int i = 0; i < 39153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39154(int& val) {
    func_in_39154(val);
    const int x = 39154;
    std::cout << 39154;
    for (int i = 0; i < 39154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39154(int& val) {
    const int x = 39154;
    std::cout << 39154;
    for (int i = 0; i < 39154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39155(int& val) {
    func_in_39155(val);
    const int x = 39155;
    std::cout << 39155;
    for (int i = 0; i < 39155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39155(int& val) {
    const int x = 39155;
    std::cout << 39155;
    for (int i = 0; i < 39155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39156(int& val) {
    func_in_39156(val);
    const int x = 39156;
    std::cout << 39156;
    for (int i = 0; i < 39156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39156(int& val) {
    const int x = 39156;
    std::cout << 39156;
    for (int i = 0; i < 39156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39157(int& val) {
    func_in_39157(val);
    const int x = 39157;
    std::cout << 39157;
    for (int i = 0; i < 39157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39157(int& val) {
    const int x = 39157;
    std::cout << 39157;
    for (int i = 0; i < 39157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39158(int& val) {
    func_in_39158(val);
    const int x = 39158;
    std::cout << 39158;
    for (int i = 0; i < 39158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39158(int& val) {
    const int x = 39158;
    std::cout << 39158;
    for (int i = 0; i < 39158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39159(int& val) {
    func_in_39159(val);
    const int x = 39159;
    std::cout << 39159;
    for (int i = 0; i < 39159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39159(int& val) {
    const int x = 39159;
    std::cout << 39159;
    for (int i = 0; i < 39159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39160(int& val) {
    func_in_39160(val);
    const int x = 39160;
    std::cout << 39160;
    for (int i = 0; i < 39160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39160(int& val) {
    const int x = 39160;
    std::cout << 39160;
    for (int i = 0; i < 39160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39161(int& val) {
    func_in_39161(val);
    const int x = 39161;
    std::cout << 39161;
    for (int i = 0; i < 39161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39161(int& val) {
    const int x = 39161;
    std::cout << 39161;
    for (int i = 0; i < 39161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39162(int& val) {
    func_in_39162(val);
    const int x = 39162;
    std::cout << 39162;
    for (int i = 0; i < 39162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39162(int& val) {
    const int x = 39162;
    std::cout << 39162;
    for (int i = 0; i < 39162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39163(int& val) {
    func_in_39163(val);
    const int x = 39163;
    std::cout << 39163;
    for (int i = 0; i < 39163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39163(int& val) {
    const int x = 39163;
    std::cout << 39163;
    for (int i = 0; i < 39163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39164(int& val) {
    func_in_39164(val);
    const int x = 39164;
    std::cout << 39164;
    for (int i = 0; i < 39164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39164(int& val) {
    const int x = 39164;
    std::cout << 39164;
    for (int i = 0; i < 39164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39165(int& val) {
    func_in_39165(val);
    const int x = 39165;
    std::cout << 39165;
    for (int i = 0; i < 39165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39165(int& val) {
    const int x = 39165;
    std::cout << 39165;
    for (int i = 0; i < 39165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39166(int& val) {
    func_in_39166(val);
    const int x = 39166;
    std::cout << 39166;
    for (int i = 0; i < 39166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39166(int& val) {
    const int x = 39166;
    std::cout << 39166;
    for (int i = 0; i < 39166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39167(int& val) {
    func_in_39167(val);
    const int x = 39167;
    std::cout << 39167;
    for (int i = 0; i < 39167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39167(int& val) {
    const int x = 39167;
    std::cout << 39167;
    for (int i = 0; i < 39167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39168(int& val) {
    func_in_39168(val);
    const int x = 39168;
    std::cout << 39168;
    for (int i = 0; i < 39168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39168(int& val) {
    const int x = 39168;
    std::cout << 39168;
    for (int i = 0; i < 39168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39169(int& val) {
    func_in_39169(val);
    const int x = 39169;
    std::cout << 39169;
    for (int i = 0; i < 39169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39169(int& val) {
    const int x = 39169;
    std::cout << 39169;
    for (int i = 0; i < 39169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39170(int& val) {
    func_in_39170(val);
    const int x = 39170;
    std::cout << 39170;
    for (int i = 0; i < 39170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39170(int& val) {
    const int x = 39170;
    std::cout << 39170;
    for (int i = 0; i < 39170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39171(int& val) {
    func_in_39171(val);
    const int x = 39171;
    std::cout << 39171;
    for (int i = 0; i < 39171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39171(int& val) {
    const int x = 39171;
    std::cout << 39171;
    for (int i = 0; i < 39171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39172(int& val) {
    func_in_39172(val);
    const int x = 39172;
    std::cout << 39172;
    for (int i = 0; i < 39172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39172(int& val) {
    const int x = 39172;
    std::cout << 39172;
    for (int i = 0; i < 39172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39173(int& val) {
    func_in_39173(val);
    const int x = 39173;
    std::cout << 39173;
    for (int i = 0; i < 39173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39173(int& val) {
    const int x = 39173;
    std::cout << 39173;
    for (int i = 0; i < 39173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39174(int& val) {
    func_in_39174(val);
    const int x = 39174;
    std::cout << 39174;
    for (int i = 0; i < 39174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39174(int& val) {
    const int x = 39174;
    std::cout << 39174;
    for (int i = 0; i < 39174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39175(int& val) {
    func_in_39175(val);
    const int x = 39175;
    std::cout << 39175;
    for (int i = 0; i < 39175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39175(int& val) {
    const int x = 39175;
    std::cout << 39175;
    for (int i = 0; i < 39175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39176(int& val) {
    func_in_39176(val);
    const int x = 39176;
    std::cout << 39176;
    for (int i = 0; i < 39176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39176(int& val) {
    const int x = 39176;
    std::cout << 39176;
    for (int i = 0; i < 39176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39177(int& val) {
    func_in_39177(val);
    const int x = 39177;
    std::cout << 39177;
    for (int i = 0; i < 39177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39177(int& val) {
    const int x = 39177;
    std::cout << 39177;
    for (int i = 0; i < 39177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39178(int& val) {
    func_in_39178(val);
    const int x = 39178;
    std::cout << 39178;
    for (int i = 0; i < 39178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39178(int& val) {
    const int x = 39178;
    std::cout << 39178;
    for (int i = 0; i < 39178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39179(int& val) {
    func_in_39179(val);
    const int x = 39179;
    std::cout << 39179;
    for (int i = 0; i < 39179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39179(int& val) {
    const int x = 39179;
    std::cout << 39179;
    for (int i = 0; i < 39179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39180(int& val) {
    func_in_39180(val);
    const int x = 39180;
    std::cout << 39180;
    for (int i = 0; i < 39180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39180(int& val) {
    const int x = 39180;
    std::cout << 39180;
    for (int i = 0; i < 39180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39181(int& val) {
    func_in_39181(val);
    const int x = 39181;
    std::cout << 39181;
    for (int i = 0; i < 39181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39181(int& val) {
    const int x = 39181;
    std::cout << 39181;
    for (int i = 0; i < 39181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39182(int& val) {
    func_in_39182(val);
    const int x = 39182;
    std::cout << 39182;
    for (int i = 0; i < 39182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39182(int& val) {
    const int x = 39182;
    std::cout << 39182;
    for (int i = 0; i < 39182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39183(int& val) {
    func_in_39183(val);
    const int x = 39183;
    std::cout << 39183;
    for (int i = 0; i < 39183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39183(int& val) {
    const int x = 39183;
    std::cout << 39183;
    for (int i = 0; i < 39183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39184(int& val) {
    func_in_39184(val);
    const int x = 39184;
    std::cout << 39184;
    for (int i = 0; i < 39184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39184(int& val) {
    const int x = 39184;
    std::cout << 39184;
    for (int i = 0; i < 39184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39185(int& val) {
    func_in_39185(val);
    const int x = 39185;
    std::cout << 39185;
    for (int i = 0; i < 39185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39185(int& val) {
    const int x = 39185;
    std::cout << 39185;
    for (int i = 0; i < 39185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39186(int& val) {
    func_in_39186(val);
    const int x = 39186;
    std::cout << 39186;
    for (int i = 0; i < 39186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39186(int& val) {
    const int x = 39186;
    std::cout << 39186;
    for (int i = 0; i < 39186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39187(int& val) {
    func_in_39187(val);
    const int x = 39187;
    std::cout << 39187;
    for (int i = 0; i < 39187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39187(int& val) {
    const int x = 39187;
    std::cout << 39187;
    for (int i = 0; i < 39187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39188(int& val) {
    func_in_39188(val);
    const int x = 39188;
    std::cout << 39188;
    for (int i = 0; i < 39188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39188(int& val) {
    const int x = 39188;
    std::cout << 39188;
    for (int i = 0; i < 39188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39189(int& val) {
    func_in_39189(val);
    const int x = 39189;
    std::cout << 39189;
    for (int i = 0; i < 39189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39189(int& val) {
    const int x = 39189;
    std::cout << 39189;
    for (int i = 0; i < 39189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39190(int& val) {
    func_in_39190(val);
    const int x = 39190;
    std::cout << 39190;
    for (int i = 0; i < 39190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39190(int& val) {
    const int x = 39190;
    std::cout << 39190;
    for (int i = 0; i < 39190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39191(int& val) {
    func_in_39191(val);
    const int x = 39191;
    std::cout << 39191;
    for (int i = 0; i < 39191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39191(int& val) {
    const int x = 39191;
    std::cout << 39191;
    for (int i = 0; i < 39191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39192(int& val) {
    func_in_39192(val);
    const int x = 39192;
    std::cout << 39192;
    for (int i = 0; i < 39192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39192(int& val) {
    const int x = 39192;
    std::cout << 39192;
    for (int i = 0; i < 39192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39193(int& val) {
    func_in_39193(val);
    const int x = 39193;
    std::cout << 39193;
    for (int i = 0; i < 39193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39193(int& val) {
    const int x = 39193;
    std::cout << 39193;
    for (int i = 0; i < 39193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39194(int& val) {
    func_in_39194(val);
    const int x = 39194;
    std::cout << 39194;
    for (int i = 0; i < 39194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39194(int& val) {
    const int x = 39194;
    std::cout << 39194;
    for (int i = 0; i < 39194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39195(int& val) {
    func_in_39195(val);
    const int x = 39195;
    std::cout << 39195;
    for (int i = 0; i < 39195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39195(int& val) {
    const int x = 39195;
    std::cout << 39195;
    for (int i = 0; i < 39195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39196(int& val) {
    func_in_39196(val);
    const int x = 39196;
    std::cout << 39196;
    for (int i = 0; i < 39196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39196(int& val) {
    const int x = 39196;
    std::cout << 39196;
    for (int i = 0; i < 39196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39197(int& val) {
    func_in_39197(val);
    const int x = 39197;
    std::cout << 39197;
    for (int i = 0; i < 39197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39197(int& val) {
    const int x = 39197;
    std::cout << 39197;
    for (int i = 0; i < 39197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39198(int& val) {
    func_in_39198(val);
    const int x = 39198;
    std::cout << 39198;
    for (int i = 0; i < 39198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39198(int& val) {
    const int x = 39198;
    std::cout << 39198;
    for (int i = 0; i < 39198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39199(int& val) {
    func_in_39199(val);
    const int x = 39199;
    std::cout << 39199;
    for (int i = 0; i < 39199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39199(int& val) {
    const int x = 39199;
    std::cout << 39199;
    for (int i = 0; i < 39199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39200(int& val) {
    func_in_39200(val);
    const int x = 39200;
    std::cout << 39200;
    for (int i = 0; i < 39200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39200(int& val) {
    const int x = 39200;
    std::cout << 39200;
    for (int i = 0; i < 39200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39201(int& val) {
    func_in_39201(val);
    const int x = 39201;
    std::cout << 39201;
    for (int i = 0; i < 39201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39201(int& val) {
    const int x = 39201;
    std::cout << 39201;
    for (int i = 0; i < 39201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39202(int& val) {
    func_in_39202(val);
    const int x = 39202;
    std::cout << 39202;
    for (int i = 0; i < 39202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39202(int& val) {
    const int x = 39202;
    std::cout << 39202;
    for (int i = 0; i < 39202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39203(int& val) {
    func_in_39203(val);
    const int x = 39203;
    std::cout << 39203;
    for (int i = 0; i < 39203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39203(int& val) {
    const int x = 39203;
    std::cout << 39203;
    for (int i = 0; i < 39203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39204(int& val) {
    func_in_39204(val);
    const int x = 39204;
    std::cout << 39204;
    for (int i = 0; i < 39204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39204(int& val) {
    const int x = 39204;
    std::cout << 39204;
    for (int i = 0; i < 39204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39205(int& val) {
    func_in_39205(val);
    const int x = 39205;
    std::cout << 39205;
    for (int i = 0; i < 39205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39205(int& val) {
    const int x = 39205;
    std::cout << 39205;
    for (int i = 0; i < 39205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39206(int& val) {
    func_in_39206(val);
    const int x = 39206;
    std::cout << 39206;
    for (int i = 0; i < 39206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39206(int& val) {
    const int x = 39206;
    std::cout << 39206;
    for (int i = 0; i < 39206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39207(int& val) {
    func_in_39207(val);
    const int x = 39207;
    std::cout << 39207;
    for (int i = 0; i < 39207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39207(int& val) {
    const int x = 39207;
    std::cout << 39207;
    for (int i = 0; i < 39207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39208(int& val) {
    func_in_39208(val);
    const int x = 39208;
    std::cout << 39208;
    for (int i = 0; i < 39208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39208(int& val) {
    const int x = 39208;
    std::cout << 39208;
    for (int i = 0; i < 39208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39209(int& val) {
    func_in_39209(val);
    const int x = 39209;
    std::cout << 39209;
    for (int i = 0; i < 39209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39209(int& val) {
    const int x = 39209;
    std::cout << 39209;
    for (int i = 0; i < 39209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39210(int& val) {
    func_in_39210(val);
    const int x = 39210;
    std::cout << 39210;
    for (int i = 0; i < 39210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39210(int& val) {
    const int x = 39210;
    std::cout << 39210;
    for (int i = 0; i < 39210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39211(int& val) {
    func_in_39211(val);
    const int x = 39211;
    std::cout << 39211;
    for (int i = 0; i < 39211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39211(int& val) {
    const int x = 39211;
    std::cout << 39211;
    for (int i = 0; i < 39211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39212(int& val) {
    func_in_39212(val);
    const int x = 39212;
    std::cout << 39212;
    for (int i = 0; i < 39212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39212(int& val) {
    const int x = 39212;
    std::cout << 39212;
    for (int i = 0; i < 39212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39213(int& val) {
    func_in_39213(val);
    const int x = 39213;
    std::cout << 39213;
    for (int i = 0; i < 39213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39213(int& val) {
    const int x = 39213;
    std::cout << 39213;
    for (int i = 0; i < 39213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39214(int& val) {
    func_in_39214(val);
    const int x = 39214;
    std::cout << 39214;
    for (int i = 0; i < 39214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39214(int& val) {
    const int x = 39214;
    std::cout << 39214;
    for (int i = 0; i < 39214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39215(int& val) {
    func_in_39215(val);
    const int x = 39215;
    std::cout << 39215;
    for (int i = 0; i < 39215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39215(int& val) {
    const int x = 39215;
    std::cout << 39215;
    for (int i = 0; i < 39215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39216(int& val) {
    func_in_39216(val);
    const int x = 39216;
    std::cout << 39216;
    for (int i = 0; i < 39216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39216(int& val) {
    const int x = 39216;
    std::cout << 39216;
    for (int i = 0; i < 39216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39217(int& val) {
    func_in_39217(val);
    const int x = 39217;
    std::cout << 39217;
    for (int i = 0; i < 39217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39217(int& val) {
    const int x = 39217;
    std::cout << 39217;
    for (int i = 0; i < 39217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39218(int& val) {
    func_in_39218(val);
    const int x = 39218;
    std::cout << 39218;
    for (int i = 0; i < 39218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39218(int& val) {
    const int x = 39218;
    std::cout << 39218;
    for (int i = 0; i < 39218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39219(int& val) {
    func_in_39219(val);
    const int x = 39219;
    std::cout << 39219;
    for (int i = 0; i < 39219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39219(int& val) {
    const int x = 39219;
    std::cout << 39219;
    for (int i = 0; i < 39219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39220(int& val) {
    func_in_39220(val);
    const int x = 39220;
    std::cout << 39220;
    for (int i = 0; i < 39220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39220(int& val) {
    const int x = 39220;
    std::cout << 39220;
    for (int i = 0; i < 39220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39221(int& val) {
    func_in_39221(val);
    const int x = 39221;
    std::cout << 39221;
    for (int i = 0; i < 39221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39221(int& val) {
    const int x = 39221;
    std::cout << 39221;
    for (int i = 0; i < 39221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39222(int& val) {
    func_in_39222(val);
    const int x = 39222;
    std::cout << 39222;
    for (int i = 0; i < 39222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39222(int& val) {
    const int x = 39222;
    std::cout << 39222;
    for (int i = 0; i < 39222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39223(int& val) {
    func_in_39223(val);
    const int x = 39223;
    std::cout << 39223;
    for (int i = 0; i < 39223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39223(int& val) {
    const int x = 39223;
    std::cout << 39223;
    for (int i = 0; i < 39223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39224(int& val) {
    func_in_39224(val);
    const int x = 39224;
    std::cout << 39224;
    for (int i = 0; i < 39224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39224(int& val) {
    const int x = 39224;
    std::cout << 39224;
    for (int i = 0; i < 39224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39225(int& val) {
    func_in_39225(val);
    const int x = 39225;
    std::cout << 39225;
    for (int i = 0; i < 39225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39225(int& val) {
    const int x = 39225;
    std::cout << 39225;
    for (int i = 0; i < 39225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39226(int& val) {
    func_in_39226(val);
    const int x = 39226;
    std::cout << 39226;
    for (int i = 0; i < 39226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39226(int& val) {
    const int x = 39226;
    std::cout << 39226;
    for (int i = 0; i < 39226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39227(int& val) {
    func_in_39227(val);
    const int x = 39227;
    std::cout << 39227;
    for (int i = 0; i < 39227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39227(int& val) {
    const int x = 39227;
    std::cout << 39227;
    for (int i = 0; i < 39227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39228(int& val) {
    func_in_39228(val);
    const int x = 39228;
    std::cout << 39228;
    for (int i = 0; i < 39228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39228(int& val) {
    const int x = 39228;
    std::cout << 39228;
    for (int i = 0; i < 39228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39229(int& val) {
    func_in_39229(val);
    const int x = 39229;
    std::cout << 39229;
    for (int i = 0; i < 39229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39229(int& val) {
    const int x = 39229;
    std::cout << 39229;
    for (int i = 0; i < 39229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39230(int& val) {
    func_in_39230(val);
    const int x = 39230;
    std::cout << 39230;
    for (int i = 0; i < 39230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39230(int& val) {
    const int x = 39230;
    std::cout << 39230;
    for (int i = 0; i < 39230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39231(int& val) {
    func_in_39231(val);
    const int x = 39231;
    std::cout << 39231;
    for (int i = 0; i < 39231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39231(int& val) {
    const int x = 39231;
    std::cout << 39231;
    for (int i = 0; i < 39231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39232(int& val) {
    func_in_39232(val);
    const int x = 39232;
    std::cout << 39232;
    for (int i = 0; i < 39232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39232(int& val) {
    const int x = 39232;
    std::cout << 39232;
    for (int i = 0; i < 39232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39233(int& val) {
    func_in_39233(val);
    const int x = 39233;
    std::cout << 39233;
    for (int i = 0; i < 39233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39233(int& val) {
    const int x = 39233;
    std::cout << 39233;
    for (int i = 0; i < 39233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39234(int& val) {
    func_in_39234(val);
    const int x = 39234;
    std::cout << 39234;
    for (int i = 0; i < 39234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39234(int& val) {
    const int x = 39234;
    std::cout << 39234;
    for (int i = 0; i < 39234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39235(int& val) {
    func_in_39235(val);
    const int x = 39235;
    std::cout << 39235;
    for (int i = 0; i < 39235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39235(int& val) {
    const int x = 39235;
    std::cout << 39235;
    for (int i = 0; i < 39235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39236(int& val) {
    func_in_39236(val);
    const int x = 39236;
    std::cout << 39236;
    for (int i = 0; i < 39236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39236(int& val) {
    const int x = 39236;
    std::cout << 39236;
    for (int i = 0; i < 39236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39237(int& val) {
    func_in_39237(val);
    const int x = 39237;
    std::cout << 39237;
    for (int i = 0; i < 39237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39237(int& val) {
    const int x = 39237;
    std::cout << 39237;
    for (int i = 0; i < 39237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39238(int& val) {
    func_in_39238(val);
    const int x = 39238;
    std::cout << 39238;
    for (int i = 0; i < 39238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39238(int& val) {
    const int x = 39238;
    std::cout << 39238;
    for (int i = 0; i < 39238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39239(int& val) {
    func_in_39239(val);
    const int x = 39239;
    std::cout << 39239;
    for (int i = 0; i < 39239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39239(int& val) {
    const int x = 39239;
    std::cout << 39239;
    for (int i = 0; i < 39239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39240(int& val) {
    func_in_39240(val);
    const int x = 39240;
    std::cout << 39240;
    for (int i = 0; i < 39240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39240(int& val) {
    const int x = 39240;
    std::cout << 39240;
    for (int i = 0; i < 39240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39241(int& val) {
    func_in_39241(val);
    const int x = 39241;
    std::cout << 39241;
    for (int i = 0; i < 39241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39241(int& val) {
    const int x = 39241;
    std::cout << 39241;
    for (int i = 0; i < 39241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39242(int& val) {
    func_in_39242(val);
    const int x = 39242;
    std::cout << 39242;
    for (int i = 0; i < 39242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39242(int& val) {
    const int x = 39242;
    std::cout << 39242;
    for (int i = 0; i < 39242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39243(int& val) {
    func_in_39243(val);
    const int x = 39243;
    std::cout << 39243;
    for (int i = 0; i < 39243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39243(int& val) {
    const int x = 39243;
    std::cout << 39243;
    for (int i = 0; i < 39243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39244(int& val) {
    func_in_39244(val);
    const int x = 39244;
    std::cout << 39244;
    for (int i = 0; i < 39244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39244(int& val) {
    const int x = 39244;
    std::cout << 39244;
    for (int i = 0; i < 39244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39245(int& val) {
    func_in_39245(val);
    const int x = 39245;
    std::cout << 39245;
    for (int i = 0; i < 39245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39245(int& val) {
    const int x = 39245;
    std::cout << 39245;
    for (int i = 0; i < 39245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39246(int& val) {
    func_in_39246(val);
    const int x = 39246;
    std::cout << 39246;
    for (int i = 0; i < 39246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39246(int& val) {
    const int x = 39246;
    std::cout << 39246;
    for (int i = 0; i < 39246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39247(int& val) {
    func_in_39247(val);
    const int x = 39247;
    std::cout << 39247;
    for (int i = 0; i < 39247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39247(int& val) {
    const int x = 39247;
    std::cout << 39247;
    for (int i = 0; i < 39247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39248(int& val) {
    func_in_39248(val);
    const int x = 39248;
    std::cout << 39248;
    for (int i = 0; i < 39248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39248(int& val) {
    const int x = 39248;
    std::cout << 39248;
    for (int i = 0; i < 39248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39249(int& val) {
    func_in_39249(val);
    const int x = 39249;
    std::cout << 39249;
    for (int i = 0; i < 39249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39249(int& val) {
    const int x = 39249;
    std::cout << 39249;
    for (int i = 0; i < 39249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39250(int& val) {
    func_in_39250(val);
    const int x = 39250;
    std::cout << 39250;
    for (int i = 0; i < 39250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39250(int& val) {
    const int x = 39250;
    std::cout << 39250;
    for (int i = 0; i < 39250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39251(int& val) {
    func_in_39251(val);
    const int x = 39251;
    std::cout << 39251;
    for (int i = 0; i < 39251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39251(int& val) {
    const int x = 39251;
    std::cout << 39251;
    for (int i = 0; i < 39251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39252(int& val) {
    func_in_39252(val);
    const int x = 39252;
    std::cout << 39252;
    for (int i = 0; i < 39252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39252(int& val) {
    const int x = 39252;
    std::cout << 39252;
    for (int i = 0; i < 39252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39253(int& val) {
    func_in_39253(val);
    const int x = 39253;
    std::cout << 39253;
    for (int i = 0; i < 39253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39253(int& val) {
    const int x = 39253;
    std::cout << 39253;
    for (int i = 0; i < 39253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39254(int& val) {
    func_in_39254(val);
    const int x = 39254;
    std::cout << 39254;
    for (int i = 0; i < 39254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39254(int& val) {
    const int x = 39254;
    std::cout << 39254;
    for (int i = 0; i < 39254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39255(int& val) {
    func_in_39255(val);
    const int x = 39255;
    std::cout << 39255;
    for (int i = 0; i < 39255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39255(int& val) {
    const int x = 39255;
    std::cout << 39255;
    for (int i = 0; i < 39255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39256(int& val) {
    func_in_39256(val);
    const int x = 39256;
    std::cout << 39256;
    for (int i = 0; i < 39256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39256(int& val) {
    const int x = 39256;
    std::cout << 39256;
    for (int i = 0; i < 39256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39257(int& val) {
    func_in_39257(val);
    const int x = 39257;
    std::cout << 39257;
    for (int i = 0; i < 39257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39257(int& val) {
    const int x = 39257;
    std::cout << 39257;
    for (int i = 0; i < 39257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39258(int& val) {
    func_in_39258(val);
    const int x = 39258;
    std::cout << 39258;
    for (int i = 0; i < 39258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39258(int& val) {
    const int x = 39258;
    std::cout << 39258;
    for (int i = 0; i < 39258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39259(int& val) {
    func_in_39259(val);
    const int x = 39259;
    std::cout << 39259;
    for (int i = 0; i < 39259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39259(int& val) {
    const int x = 39259;
    std::cout << 39259;
    for (int i = 0; i < 39259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39260(int& val) {
    func_in_39260(val);
    const int x = 39260;
    std::cout << 39260;
    for (int i = 0; i < 39260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39260(int& val) {
    const int x = 39260;
    std::cout << 39260;
    for (int i = 0; i < 39260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39261(int& val) {
    func_in_39261(val);
    const int x = 39261;
    std::cout << 39261;
    for (int i = 0; i < 39261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39261(int& val) {
    const int x = 39261;
    std::cout << 39261;
    for (int i = 0; i < 39261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39262(int& val) {
    func_in_39262(val);
    const int x = 39262;
    std::cout << 39262;
    for (int i = 0; i < 39262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39262(int& val) {
    const int x = 39262;
    std::cout << 39262;
    for (int i = 0; i < 39262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39263(int& val) {
    func_in_39263(val);
    const int x = 39263;
    std::cout << 39263;
    for (int i = 0; i < 39263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39263(int& val) {
    const int x = 39263;
    std::cout << 39263;
    for (int i = 0; i < 39263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39264(int& val) {
    func_in_39264(val);
    const int x = 39264;
    std::cout << 39264;
    for (int i = 0; i < 39264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39264(int& val) {
    const int x = 39264;
    std::cout << 39264;
    for (int i = 0; i < 39264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39265(int& val) {
    func_in_39265(val);
    const int x = 39265;
    std::cout << 39265;
    for (int i = 0; i < 39265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39265(int& val) {
    const int x = 39265;
    std::cout << 39265;
    for (int i = 0; i < 39265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39266(int& val) {
    func_in_39266(val);
    const int x = 39266;
    std::cout << 39266;
    for (int i = 0; i < 39266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39266(int& val) {
    const int x = 39266;
    std::cout << 39266;
    for (int i = 0; i < 39266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39267(int& val) {
    func_in_39267(val);
    const int x = 39267;
    std::cout << 39267;
    for (int i = 0; i < 39267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39267(int& val) {
    const int x = 39267;
    std::cout << 39267;
    for (int i = 0; i < 39267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39268(int& val) {
    func_in_39268(val);
    const int x = 39268;
    std::cout << 39268;
    for (int i = 0; i < 39268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39268(int& val) {
    const int x = 39268;
    std::cout << 39268;
    for (int i = 0; i < 39268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39269(int& val) {
    func_in_39269(val);
    const int x = 39269;
    std::cout << 39269;
    for (int i = 0; i < 39269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39269(int& val) {
    const int x = 39269;
    std::cout << 39269;
    for (int i = 0; i < 39269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39270(int& val) {
    func_in_39270(val);
    const int x = 39270;
    std::cout << 39270;
    for (int i = 0; i < 39270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39270(int& val) {
    const int x = 39270;
    std::cout << 39270;
    for (int i = 0; i < 39270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39271(int& val) {
    func_in_39271(val);
    const int x = 39271;
    std::cout << 39271;
    for (int i = 0; i < 39271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39271(int& val) {
    const int x = 39271;
    std::cout << 39271;
    for (int i = 0; i < 39271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39272(int& val) {
    func_in_39272(val);
    const int x = 39272;
    std::cout << 39272;
    for (int i = 0; i < 39272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39272(int& val) {
    const int x = 39272;
    std::cout << 39272;
    for (int i = 0; i < 39272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39273(int& val) {
    func_in_39273(val);
    const int x = 39273;
    std::cout << 39273;
    for (int i = 0; i < 39273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39273(int& val) {
    const int x = 39273;
    std::cout << 39273;
    for (int i = 0; i < 39273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39274(int& val) {
    func_in_39274(val);
    const int x = 39274;
    std::cout << 39274;
    for (int i = 0; i < 39274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39274(int& val) {
    const int x = 39274;
    std::cout << 39274;
    for (int i = 0; i < 39274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39275(int& val) {
    func_in_39275(val);
    const int x = 39275;
    std::cout << 39275;
    for (int i = 0; i < 39275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39275(int& val) {
    const int x = 39275;
    std::cout << 39275;
    for (int i = 0; i < 39275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39276(int& val) {
    func_in_39276(val);
    const int x = 39276;
    std::cout << 39276;
    for (int i = 0; i < 39276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39276(int& val) {
    const int x = 39276;
    std::cout << 39276;
    for (int i = 0; i < 39276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39277(int& val) {
    func_in_39277(val);
    const int x = 39277;
    std::cout << 39277;
    for (int i = 0; i < 39277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39277(int& val) {
    const int x = 39277;
    std::cout << 39277;
    for (int i = 0; i < 39277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39278(int& val) {
    func_in_39278(val);
    const int x = 39278;
    std::cout << 39278;
    for (int i = 0; i < 39278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39278(int& val) {
    const int x = 39278;
    std::cout << 39278;
    for (int i = 0; i < 39278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39279(int& val) {
    func_in_39279(val);
    const int x = 39279;
    std::cout << 39279;
    for (int i = 0; i < 39279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39279(int& val) {
    const int x = 39279;
    std::cout << 39279;
    for (int i = 0; i < 39279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39280(int& val) {
    func_in_39280(val);
    const int x = 39280;
    std::cout << 39280;
    for (int i = 0; i < 39280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39280(int& val) {
    const int x = 39280;
    std::cout << 39280;
    for (int i = 0; i < 39280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39281(int& val) {
    func_in_39281(val);
    const int x = 39281;
    std::cout << 39281;
    for (int i = 0; i < 39281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39281(int& val) {
    const int x = 39281;
    std::cout << 39281;
    for (int i = 0; i < 39281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39282(int& val) {
    func_in_39282(val);
    const int x = 39282;
    std::cout << 39282;
    for (int i = 0; i < 39282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39282(int& val) {
    const int x = 39282;
    std::cout << 39282;
    for (int i = 0; i < 39282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39283(int& val) {
    func_in_39283(val);
    const int x = 39283;
    std::cout << 39283;
    for (int i = 0; i < 39283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39283(int& val) {
    const int x = 39283;
    std::cout << 39283;
    for (int i = 0; i < 39283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39284(int& val) {
    func_in_39284(val);
    const int x = 39284;
    std::cout << 39284;
    for (int i = 0; i < 39284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39284(int& val) {
    const int x = 39284;
    std::cout << 39284;
    for (int i = 0; i < 39284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39285(int& val) {
    func_in_39285(val);
    const int x = 39285;
    std::cout << 39285;
    for (int i = 0; i < 39285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39285(int& val) {
    const int x = 39285;
    std::cout << 39285;
    for (int i = 0; i < 39285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39286(int& val) {
    func_in_39286(val);
    const int x = 39286;
    std::cout << 39286;
    for (int i = 0; i < 39286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39286(int& val) {
    const int x = 39286;
    std::cout << 39286;
    for (int i = 0; i < 39286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39287(int& val) {
    func_in_39287(val);
    const int x = 39287;
    std::cout << 39287;
    for (int i = 0; i < 39287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39287(int& val) {
    const int x = 39287;
    std::cout << 39287;
    for (int i = 0; i < 39287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39288(int& val) {
    func_in_39288(val);
    const int x = 39288;
    std::cout << 39288;
    for (int i = 0; i < 39288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39288(int& val) {
    const int x = 39288;
    std::cout << 39288;
    for (int i = 0; i < 39288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39289(int& val) {
    func_in_39289(val);
    const int x = 39289;
    std::cout << 39289;
    for (int i = 0; i < 39289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39289(int& val) {
    const int x = 39289;
    std::cout << 39289;
    for (int i = 0; i < 39289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39290(int& val) {
    func_in_39290(val);
    const int x = 39290;
    std::cout << 39290;
    for (int i = 0; i < 39290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39290(int& val) {
    const int x = 39290;
    std::cout << 39290;
    for (int i = 0; i < 39290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39291(int& val) {
    func_in_39291(val);
    const int x = 39291;
    std::cout << 39291;
    for (int i = 0; i < 39291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39291(int& val) {
    const int x = 39291;
    std::cout << 39291;
    for (int i = 0; i < 39291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39292(int& val) {
    func_in_39292(val);
    const int x = 39292;
    std::cout << 39292;
    for (int i = 0; i < 39292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39292(int& val) {
    const int x = 39292;
    std::cout << 39292;
    for (int i = 0; i < 39292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39293(int& val) {
    func_in_39293(val);
    const int x = 39293;
    std::cout << 39293;
    for (int i = 0; i < 39293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39293(int& val) {
    const int x = 39293;
    std::cout << 39293;
    for (int i = 0; i < 39293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39294(int& val) {
    func_in_39294(val);
    const int x = 39294;
    std::cout << 39294;
    for (int i = 0; i < 39294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39294(int& val) {
    const int x = 39294;
    std::cout << 39294;
    for (int i = 0; i < 39294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39295(int& val) {
    func_in_39295(val);
    const int x = 39295;
    std::cout << 39295;
    for (int i = 0; i < 39295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39295(int& val) {
    const int x = 39295;
    std::cout << 39295;
    for (int i = 0; i < 39295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39296(int& val) {
    func_in_39296(val);
    const int x = 39296;
    std::cout << 39296;
    for (int i = 0; i < 39296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39296(int& val) {
    const int x = 39296;
    std::cout << 39296;
    for (int i = 0; i < 39296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39297(int& val) {
    func_in_39297(val);
    const int x = 39297;
    std::cout << 39297;
    for (int i = 0; i < 39297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39297(int& val) {
    const int x = 39297;
    std::cout << 39297;
    for (int i = 0; i < 39297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39298(int& val) {
    func_in_39298(val);
    const int x = 39298;
    std::cout << 39298;
    for (int i = 0; i < 39298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39298(int& val) {
    const int x = 39298;
    std::cout << 39298;
    for (int i = 0; i < 39298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39299(int& val) {
    func_in_39299(val);
    const int x = 39299;
    std::cout << 39299;
    for (int i = 0; i < 39299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39299(int& val) {
    const int x = 39299;
    std::cout << 39299;
    for (int i = 0; i < 39299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39300(int& val) {
    func_in_39300(val);
    const int x = 39300;
    std::cout << 39300;
    for (int i = 0; i < 39300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39300(int& val) {
    const int x = 39300;
    std::cout << 39300;
    for (int i = 0; i < 39300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39301(int& val) {
    func_in_39301(val);
    const int x = 39301;
    std::cout << 39301;
    for (int i = 0; i < 39301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39301(int& val) {
    const int x = 39301;
    std::cout << 39301;
    for (int i = 0; i < 39301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39302(int& val) {
    func_in_39302(val);
    const int x = 39302;
    std::cout << 39302;
    for (int i = 0; i < 39302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39302(int& val) {
    const int x = 39302;
    std::cout << 39302;
    for (int i = 0; i < 39302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39303(int& val) {
    func_in_39303(val);
    const int x = 39303;
    std::cout << 39303;
    for (int i = 0; i < 39303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39303(int& val) {
    const int x = 39303;
    std::cout << 39303;
    for (int i = 0; i < 39303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39304(int& val) {
    func_in_39304(val);
    const int x = 39304;
    std::cout << 39304;
    for (int i = 0; i < 39304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39304(int& val) {
    const int x = 39304;
    std::cout << 39304;
    for (int i = 0; i < 39304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39305(int& val) {
    func_in_39305(val);
    const int x = 39305;
    std::cout << 39305;
    for (int i = 0; i < 39305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39305(int& val) {
    const int x = 39305;
    std::cout << 39305;
    for (int i = 0; i < 39305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39306(int& val) {
    func_in_39306(val);
    const int x = 39306;
    std::cout << 39306;
    for (int i = 0; i < 39306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39306(int& val) {
    const int x = 39306;
    std::cout << 39306;
    for (int i = 0; i < 39306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39307(int& val) {
    func_in_39307(val);
    const int x = 39307;
    std::cout << 39307;
    for (int i = 0; i < 39307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39307(int& val) {
    const int x = 39307;
    std::cout << 39307;
    for (int i = 0; i < 39307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39308(int& val) {
    func_in_39308(val);
    const int x = 39308;
    std::cout << 39308;
    for (int i = 0; i < 39308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39308(int& val) {
    const int x = 39308;
    std::cout << 39308;
    for (int i = 0; i < 39308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39309(int& val) {
    func_in_39309(val);
    const int x = 39309;
    std::cout << 39309;
    for (int i = 0; i < 39309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39309(int& val) {
    const int x = 39309;
    std::cout << 39309;
    for (int i = 0; i < 39309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39310(int& val) {
    func_in_39310(val);
    const int x = 39310;
    std::cout << 39310;
    for (int i = 0; i < 39310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39310(int& val) {
    const int x = 39310;
    std::cout << 39310;
    for (int i = 0; i < 39310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39311(int& val) {
    func_in_39311(val);
    const int x = 39311;
    std::cout << 39311;
    for (int i = 0; i < 39311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39311(int& val) {
    const int x = 39311;
    std::cout << 39311;
    for (int i = 0; i < 39311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39312(int& val) {
    func_in_39312(val);
    const int x = 39312;
    std::cout << 39312;
    for (int i = 0; i < 39312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39312(int& val) {
    const int x = 39312;
    std::cout << 39312;
    for (int i = 0; i < 39312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39313(int& val) {
    func_in_39313(val);
    const int x = 39313;
    std::cout << 39313;
    for (int i = 0; i < 39313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39313(int& val) {
    const int x = 39313;
    std::cout << 39313;
    for (int i = 0; i < 39313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39314(int& val) {
    func_in_39314(val);
    const int x = 39314;
    std::cout << 39314;
    for (int i = 0; i < 39314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39314(int& val) {
    const int x = 39314;
    std::cout << 39314;
    for (int i = 0; i < 39314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39315(int& val) {
    func_in_39315(val);
    const int x = 39315;
    std::cout << 39315;
    for (int i = 0; i < 39315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39315(int& val) {
    const int x = 39315;
    std::cout << 39315;
    for (int i = 0; i < 39315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39316(int& val) {
    func_in_39316(val);
    const int x = 39316;
    std::cout << 39316;
    for (int i = 0; i < 39316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39316(int& val) {
    const int x = 39316;
    std::cout << 39316;
    for (int i = 0; i < 39316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39317(int& val) {
    func_in_39317(val);
    const int x = 39317;
    std::cout << 39317;
    for (int i = 0; i < 39317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39317(int& val) {
    const int x = 39317;
    std::cout << 39317;
    for (int i = 0; i < 39317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39318(int& val) {
    func_in_39318(val);
    const int x = 39318;
    std::cout << 39318;
    for (int i = 0; i < 39318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39318(int& val) {
    const int x = 39318;
    std::cout << 39318;
    for (int i = 0; i < 39318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39319(int& val) {
    func_in_39319(val);
    const int x = 39319;
    std::cout << 39319;
    for (int i = 0; i < 39319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39319(int& val) {
    const int x = 39319;
    std::cout << 39319;
    for (int i = 0; i < 39319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39320(int& val) {
    func_in_39320(val);
    const int x = 39320;
    std::cout << 39320;
    for (int i = 0; i < 39320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39320(int& val) {
    const int x = 39320;
    std::cout << 39320;
    for (int i = 0; i < 39320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39321(int& val) {
    func_in_39321(val);
    const int x = 39321;
    std::cout << 39321;
    for (int i = 0; i < 39321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39321(int& val) {
    const int x = 39321;
    std::cout << 39321;
    for (int i = 0; i < 39321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39322(int& val) {
    func_in_39322(val);
    const int x = 39322;
    std::cout << 39322;
    for (int i = 0; i < 39322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39322(int& val) {
    const int x = 39322;
    std::cout << 39322;
    for (int i = 0; i < 39322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39323(int& val) {
    func_in_39323(val);
    const int x = 39323;
    std::cout << 39323;
    for (int i = 0; i < 39323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39323(int& val) {
    const int x = 39323;
    std::cout << 39323;
    for (int i = 0; i < 39323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39324(int& val) {
    func_in_39324(val);
    const int x = 39324;
    std::cout << 39324;
    for (int i = 0; i < 39324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39324(int& val) {
    const int x = 39324;
    std::cout << 39324;
    for (int i = 0; i < 39324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39325(int& val) {
    func_in_39325(val);
    const int x = 39325;
    std::cout << 39325;
    for (int i = 0; i < 39325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39325(int& val) {
    const int x = 39325;
    std::cout << 39325;
    for (int i = 0; i < 39325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39326(int& val) {
    func_in_39326(val);
    const int x = 39326;
    std::cout << 39326;
    for (int i = 0; i < 39326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39326(int& val) {
    const int x = 39326;
    std::cout << 39326;
    for (int i = 0; i < 39326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39327(int& val) {
    func_in_39327(val);
    const int x = 39327;
    std::cout << 39327;
    for (int i = 0; i < 39327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39327(int& val) {
    const int x = 39327;
    std::cout << 39327;
    for (int i = 0; i < 39327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39328(int& val) {
    func_in_39328(val);
    const int x = 39328;
    std::cout << 39328;
    for (int i = 0; i < 39328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39328(int& val) {
    const int x = 39328;
    std::cout << 39328;
    for (int i = 0; i < 39328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39329(int& val) {
    func_in_39329(val);
    const int x = 39329;
    std::cout << 39329;
    for (int i = 0; i < 39329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39329(int& val) {
    const int x = 39329;
    std::cout << 39329;
    for (int i = 0; i < 39329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39330(int& val) {
    func_in_39330(val);
    const int x = 39330;
    std::cout << 39330;
    for (int i = 0; i < 39330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39330(int& val) {
    const int x = 39330;
    std::cout << 39330;
    for (int i = 0; i < 39330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39331(int& val) {
    func_in_39331(val);
    const int x = 39331;
    std::cout << 39331;
    for (int i = 0; i < 39331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39331(int& val) {
    const int x = 39331;
    std::cout << 39331;
    for (int i = 0; i < 39331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39332(int& val) {
    func_in_39332(val);
    const int x = 39332;
    std::cout << 39332;
    for (int i = 0; i < 39332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39332(int& val) {
    const int x = 39332;
    std::cout << 39332;
    for (int i = 0; i < 39332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39333(int& val) {
    func_in_39333(val);
    const int x = 39333;
    std::cout << 39333;
    for (int i = 0; i < 39333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39333(int& val) {
    const int x = 39333;
    std::cout << 39333;
    for (int i = 0; i < 39333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39334(int& val) {
    func_in_39334(val);
    const int x = 39334;
    std::cout << 39334;
    for (int i = 0; i < 39334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39334(int& val) {
    const int x = 39334;
    std::cout << 39334;
    for (int i = 0; i < 39334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39335(int& val) {
    func_in_39335(val);
    const int x = 39335;
    std::cout << 39335;
    for (int i = 0; i < 39335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39335(int& val) {
    const int x = 39335;
    std::cout << 39335;
    for (int i = 0; i < 39335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39336(int& val) {
    func_in_39336(val);
    const int x = 39336;
    std::cout << 39336;
    for (int i = 0; i < 39336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39336(int& val) {
    const int x = 39336;
    std::cout << 39336;
    for (int i = 0; i < 39336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39337(int& val) {
    func_in_39337(val);
    const int x = 39337;
    std::cout << 39337;
    for (int i = 0; i < 39337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39337(int& val) {
    const int x = 39337;
    std::cout << 39337;
    for (int i = 0; i < 39337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39338(int& val) {
    func_in_39338(val);
    const int x = 39338;
    std::cout << 39338;
    for (int i = 0; i < 39338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39338(int& val) {
    const int x = 39338;
    std::cout << 39338;
    for (int i = 0; i < 39338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39339(int& val) {
    func_in_39339(val);
    const int x = 39339;
    std::cout << 39339;
    for (int i = 0; i < 39339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39339(int& val) {
    const int x = 39339;
    std::cout << 39339;
    for (int i = 0; i < 39339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39340(int& val) {
    func_in_39340(val);
    const int x = 39340;
    std::cout << 39340;
    for (int i = 0; i < 39340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39340(int& val) {
    const int x = 39340;
    std::cout << 39340;
    for (int i = 0; i < 39340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39341(int& val) {
    func_in_39341(val);
    const int x = 39341;
    std::cout << 39341;
    for (int i = 0; i < 39341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39341(int& val) {
    const int x = 39341;
    std::cout << 39341;
    for (int i = 0; i < 39341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39342(int& val) {
    func_in_39342(val);
    const int x = 39342;
    std::cout << 39342;
    for (int i = 0; i < 39342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39342(int& val) {
    const int x = 39342;
    std::cout << 39342;
    for (int i = 0; i < 39342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39343(int& val) {
    func_in_39343(val);
    const int x = 39343;
    std::cout << 39343;
    for (int i = 0; i < 39343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39343(int& val) {
    const int x = 39343;
    std::cout << 39343;
    for (int i = 0; i < 39343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39344(int& val) {
    func_in_39344(val);
    const int x = 39344;
    std::cout << 39344;
    for (int i = 0; i < 39344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39344(int& val) {
    const int x = 39344;
    std::cout << 39344;
    for (int i = 0; i < 39344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39345(int& val) {
    func_in_39345(val);
    const int x = 39345;
    std::cout << 39345;
    for (int i = 0; i < 39345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39345(int& val) {
    const int x = 39345;
    std::cout << 39345;
    for (int i = 0; i < 39345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39346(int& val) {
    func_in_39346(val);
    const int x = 39346;
    std::cout << 39346;
    for (int i = 0; i < 39346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39346(int& val) {
    const int x = 39346;
    std::cout << 39346;
    for (int i = 0; i < 39346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39347(int& val) {
    func_in_39347(val);
    const int x = 39347;
    std::cout << 39347;
    for (int i = 0; i < 39347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39347(int& val) {
    const int x = 39347;
    std::cout << 39347;
    for (int i = 0; i < 39347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39348(int& val) {
    func_in_39348(val);
    const int x = 39348;
    std::cout << 39348;
    for (int i = 0; i < 39348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39348(int& val) {
    const int x = 39348;
    std::cout << 39348;
    for (int i = 0; i < 39348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39349(int& val) {
    func_in_39349(val);
    const int x = 39349;
    std::cout << 39349;
    for (int i = 0; i < 39349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39349(int& val) {
    const int x = 39349;
    std::cout << 39349;
    for (int i = 0; i < 39349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39350(int& val) {
    func_in_39350(val);
    const int x = 39350;
    std::cout << 39350;
    for (int i = 0; i < 39350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39350(int& val) {
    const int x = 39350;
    std::cout << 39350;
    for (int i = 0; i < 39350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39351(int& val) {
    func_in_39351(val);
    const int x = 39351;
    std::cout << 39351;
    for (int i = 0; i < 39351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39351(int& val) {
    const int x = 39351;
    std::cout << 39351;
    for (int i = 0; i < 39351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39352(int& val) {
    func_in_39352(val);
    const int x = 39352;
    std::cout << 39352;
    for (int i = 0; i < 39352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39352(int& val) {
    const int x = 39352;
    std::cout << 39352;
    for (int i = 0; i < 39352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39353(int& val) {
    func_in_39353(val);
    const int x = 39353;
    std::cout << 39353;
    for (int i = 0; i < 39353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39353(int& val) {
    const int x = 39353;
    std::cout << 39353;
    for (int i = 0; i < 39353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39354(int& val) {
    func_in_39354(val);
    const int x = 39354;
    std::cout << 39354;
    for (int i = 0; i < 39354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39354(int& val) {
    const int x = 39354;
    std::cout << 39354;
    for (int i = 0; i < 39354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39355(int& val) {
    func_in_39355(val);
    const int x = 39355;
    std::cout << 39355;
    for (int i = 0; i < 39355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39355(int& val) {
    const int x = 39355;
    std::cout << 39355;
    for (int i = 0; i < 39355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39356(int& val) {
    func_in_39356(val);
    const int x = 39356;
    std::cout << 39356;
    for (int i = 0; i < 39356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39356(int& val) {
    const int x = 39356;
    std::cout << 39356;
    for (int i = 0; i < 39356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39357(int& val) {
    func_in_39357(val);
    const int x = 39357;
    std::cout << 39357;
    for (int i = 0; i < 39357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39357(int& val) {
    const int x = 39357;
    std::cout << 39357;
    for (int i = 0; i < 39357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39358(int& val) {
    func_in_39358(val);
    const int x = 39358;
    std::cout << 39358;
    for (int i = 0; i < 39358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39358(int& val) {
    const int x = 39358;
    std::cout << 39358;
    for (int i = 0; i < 39358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39359(int& val) {
    func_in_39359(val);
    const int x = 39359;
    std::cout << 39359;
    for (int i = 0; i < 39359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39359(int& val) {
    const int x = 39359;
    std::cout << 39359;
    for (int i = 0; i < 39359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39360(int& val) {
    func_in_39360(val);
    const int x = 39360;
    std::cout << 39360;
    for (int i = 0; i < 39360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39360(int& val) {
    const int x = 39360;
    std::cout << 39360;
    for (int i = 0; i < 39360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39361(int& val) {
    func_in_39361(val);
    const int x = 39361;
    std::cout << 39361;
    for (int i = 0; i < 39361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39361(int& val) {
    const int x = 39361;
    std::cout << 39361;
    for (int i = 0; i < 39361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39362(int& val) {
    func_in_39362(val);
    const int x = 39362;
    std::cout << 39362;
    for (int i = 0; i < 39362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39362(int& val) {
    const int x = 39362;
    std::cout << 39362;
    for (int i = 0; i < 39362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39363(int& val) {
    func_in_39363(val);
    const int x = 39363;
    std::cout << 39363;
    for (int i = 0; i < 39363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39363(int& val) {
    const int x = 39363;
    std::cout << 39363;
    for (int i = 0; i < 39363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39364(int& val) {
    func_in_39364(val);
    const int x = 39364;
    std::cout << 39364;
    for (int i = 0; i < 39364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39364(int& val) {
    const int x = 39364;
    std::cout << 39364;
    for (int i = 0; i < 39364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39365(int& val) {
    func_in_39365(val);
    const int x = 39365;
    std::cout << 39365;
    for (int i = 0; i < 39365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39365(int& val) {
    const int x = 39365;
    std::cout << 39365;
    for (int i = 0; i < 39365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39366(int& val) {
    func_in_39366(val);
    const int x = 39366;
    std::cout << 39366;
    for (int i = 0; i < 39366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39366(int& val) {
    const int x = 39366;
    std::cout << 39366;
    for (int i = 0; i < 39366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39367(int& val) {
    func_in_39367(val);
    const int x = 39367;
    std::cout << 39367;
    for (int i = 0; i < 39367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39367(int& val) {
    const int x = 39367;
    std::cout << 39367;
    for (int i = 0; i < 39367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39368(int& val) {
    func_in_39368(val);
    const int x = 39368;
    std::cout << 39368;
    for (int i = 0; i < 39368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39368(int& val) {
    const int x = 39368;
    std::cout << 39368;
    for (int i = 0; i < 39368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39369(int& val) {
    func_in_39369(val);
    const int x = 39369;
    std::cout << 39369;
    for (int i = 0; i < 39369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39369(int& val) {
    const int x = 39369;
    std::cout << 39369;
    for (int i = 0; i < 39369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39370(int& val) {
    func_in_39370(val);
    const int x = 39370;
    std::cout << 39370;
    for (int i = 0; i < 39370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39370(int& val) {
    const int x = 39370;
    std::cout << 39370;
    for (int i = 0; i < 39370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39371(int& val) {
    func_in_39371(val);
    const int x = 39371;
    std::cout << 39371;
    for (int i = 0; i < 39371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39371(int& val) {
    const int x = 39371;
    std::cout << 39371;
    for (int i = 0; i < 39371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39372(int& val) {
    func_in_39372(val);
    const int x = 39372;
    std::cout << 39372;
    for (int i = 0; i < 39372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39372(int& val) {
    const int x = 39372;
    std::cout << 39372;
    for (int i = 0; i < 39372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39373(int& val) {
    func_in_39373(val);
    const int x = 39373;
    std::cout << 39373;
    for (int i = 0; i < 39373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39373(int& val) {
    const int x = 39373;
    std::cout << 39373;
    for (int i = 0; i < 39373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39374(int& val) {
    func_in_39374(val);
    const int x = 39374;
    std::cout << 39374;
    for (int i = 0; i < 39374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39374(int& val) {
    const int x = 39374;
    std::cout << 39374;
    for (int i = 0; i < 39374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39375(int& val) {
    func_in_39375(val);
    const int x = 39375;
    std::cout << 39375;
    for (int i = 0; i < 39375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39375(int& val) {
    const int x = 39375;
    std::cout << 39375;
    for (int i = 0; i < 39375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39376(int& val) {
    func_in_39376(val);
    const int x = 39376;
    std::cout << 39376;
    for (int i = 0; i < 39376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39376(int& val) {
    const int x = 39376;
    std::cout << 39376;
    for (int i = 0; i < 39376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39377(int& val) {
    func_in_39377(val);
    const int x = 39377;
    std::cout << 39377;
    for (int i = 0; i < 39377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39377(int& val) {
    const int x = 39377;
    std::cout << 39377;
    for (int i = 0; i < 39377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39378(int& val) {
    func_in_39378(val);
    const int x = 39378;
    std::cout << 39378;
    for (int i = 0; i < 39378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39378(int& val) {
    const int x = 39378;
    std::cout << 39378;
    for (int i = 0; i < 39378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39379(int& val) {
    func_in_39379(val);
    const int x = 39379;
    std::cout << 39379;
    for (int i = 0; i < 39379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39379(int& val) {
    const int x = 39379;
    std::cout << 39379;
    for (int i = 0; i < 39379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39380(int& val) {
    func_in_39380(val);
    const int x = 39380;
    std::cout << 39380;
    for (int i = 0; i < 39380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39380(int& val) {
    const int x = 39380;
    std::cout << 39380;
    for (int i = 0; i < 39380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39381(int& val) {
    func_in_39381(val);
    const int x = 39381;
    std::cout << 39381;
    for (int i = 0; i < 39381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39381(int& val) {
    const int x = 39381;
    std::cout << 39381;
    for (int i = 0; i < 39381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39382(int& val) {
    func_in_39382(val);
    const int x = 39382;
    std::cout << 39382;
    for (int i = 0; i < 39382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39382(int& val) {
    const int x = 39382;
    std::cout << 39382;
    for (int i = 0; i < 39382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39383(int& val) {
    func_in_39383(val);
    const int x = 39383;
    std::cout << 39383;
    for (int i = 0; i < 39383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39383(int& val) {
    const int x = 39383;
    std::cout << 39383;
    for (int i = 0; i < 39383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39384(int& val) {
    func_in_39384(val);
    const int x = 39384;
    std::cout << 39384;
    for (int i = 0; i < 39384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39384(int& val) {
    const int x = 39384;
    std::cout << 39384;
    for (int i = 0; i < 39384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39385(int& val) {
    func_in_39385(val);
    const int x = 39385;
    std::cout << 39385;
    for (int i = 0; i < 39385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39385(int& val) {
    const int x = 39385;
    std::cout << 39385;
    for (int i = 0; i < 39385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39386(int& val) {
    func_in_39386(val);
    const int x = 39386;
    std::cout << 39386;
    for (int i = 0; i < 39386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39386(int& val) {
    const int x = 39386;
    std::cout << 39386;
    for (int i = 0; i < 39386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39387(int& val) {
    func_in_39387(val);
    const int x = 39387;
    std::cout << 39387;
    for (int i = 0; i < 39387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39387(int& val) {
    const int x = 39387;
    std::cout << 39387;
    for (int i = 0; i < 39387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39388(int& val) {
    func_in_39388(val);
    const int x = 39388;
    std::cout << 39388;
    for (int i = 0; i < 39388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39388(int& val) {
    const int x = 39388;
    std::cout << 39388;
    for (int i = 0; i < 39388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39389(int& val) {
    func_in_39389(val);
    const int x = 39389;
    std::cout << 39389;
    for (int i = 0; i < 39389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39389(int& val) {
    const int x = 39389;
    std::cout << 39389;
    for (int i = 0; i < 39389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39390(int& val) {
    func_in_39390(val);
    const int x = 39390;
    std::cout << 39390;
    for (int i = 0; i < 39390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39390(int& val) {
    const int x = 39390;
    std::cout << 39390;
    for (int i = 0; i < 39390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39391(int& val) {
    func_in_39391(val);
    const int x = 39391;
    std::cout << 39391;
    for (int i = 0; i < 39391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39391(int& val) {
    const int x = 39391;
    std::cout << 39391;
    for (int i = 0; i < 39391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39392(int& val) {
    func_in_39392(val);
    const int x = 39392;
    std::cout << 39392;
    for (int i = 0; i < 39392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39392(int& val) {
    const int x = 39392;
    std::cout << 39392;
    for (int i = 0; i < 39392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39393(int& val) {
    func_in_39393(val);
    const int x = 39393;
    std::cout << 39393;
    for (int i = 0; i < 39393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39393(int& val) {
    const int x = 39393;
    std::cout << 39393;
    for (int i = 0; i < 39393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39394(int& val) {
    func_in_39394(val);
    const int x = 39394;
    std::cout << 39394;
    for (int i = 0; i < 39394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39394(int& val) {
    const int x = 39394;
    std::cout << 39394;
    for (int i = 0; i < 39394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39395(int& val) {
    func_in_39395(val);
    const int x = 39395;
    std::cout << 39395;
    for (int i = 0; i < 39395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39395(int& val) {
    const int x = 39395;
    std::cout << 39395;
    for (int i = 0; i < 39395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39396(int& val) {
    func_in_39396(val);
    const int x = 39396;
    std::cout << 39396;
    for (int i = 0; i < 39396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39396(int& val) {
    const int x = 39396;
    std::cout << 39396;
    for (int i = 0; i < 39396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39397(int& val) {
    func_in_39397(val);
    const int x = 39397;
    std::cout << 39397;
    for (int i = 0; i < 39397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39397(int& val) {
    const int x = 39397;
    std::cout << 39397;
    for (int i = 0; i < 39397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39398(int& val) {
    func_in_39398(val);
    const int x = 39398;
    std::cout << 39398;
    for (int i = 0; i < 39398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39398(int& val) {
    const int x = 39398;
    std::cout << 39398;
    for (int i = 0; i < 39398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39399(int& val) {
    func_in_39399(val);
    const int x = 39399;
    std::cout << 39399;
    for (int i = 0; i < 39399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39399(int& val) {
    const int x = 39399;
    std::cout << 39399;
    for (int i = 0; i < 39399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39400(int& val) {
    func_in_39400(val);
    const int x = 39400;
    std::cout << 39400;
    for (int i = 0; i < 39400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39400(int& val) {
    const int x = 39400;
    std::cout << 39400;
    for (int i = 0; i < 39400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39401(int& val) {
    func_in_39401(val);
    const int x = 39401;
    std::cout << 39401;
    for (int i = 0; i < 39401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39401(int& val) {
    const int x = 39401;
    std::cout << 39401;
    for (int i = 0; i < 39401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39402(int& val) {
    func_in_39402(val);
    const int x = 39402;
    std::cout << 39402;
    for (int i = 0; i < 39402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39402(int& val) {
    const int x = 39402;
    std::cout << 39402;
    for (int i = 0; i < 39402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39403(int& val) {
    func_in_39403(val);
    const int x = 39403;
    std::cout << 39403;
    for (int i = 0; i < 39403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39403(int& val) {
    const int x = 39403;
    std::cout << 39403;
    for (int i = 0; i < 39403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39404(int& val) {
    func_in_39404(val);
    const int x = 39404;
    std::cout << 39404;
    for (int i = 0; i < 39404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39404(int& val) {
    const int x = 39404;
    std::cout << 39404;
    for (int i = 0; i < 39404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39405(int& val) {
    func_in_39405(val);
    const int x = 39405;
    std::cout << 39405;
    for (int i = 0; i < 39405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39405(int& val) {
    const int x = 39405;
    std::cout << 39405;
    for (int i = 0; i < 39405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39406(int& val) {
    func_in_39406(val);
    const int x = 39406;
    std::cout << 39406;
    for (int i = 0; i < 39406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39406(int& val) {
    const int x = 39406;
    std::cout << 39406;
    for (int i = 0; i < 39406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39407(int& val) {
    func_in_39407(val);
    const int x = 39407;
    std::cout << 39407;
    for (int i = 0; i < 39407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39407(int& val) {
    const int x = 39407;
    std::cout << 39407;
    for (int i = 0; i < 39407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39408(int& val) {
    func_in_39408(val);
    const int x = 39408;
    std::cout << 39408;
    for (int i = 0; i < 39408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39408(int& val) {
    const int x = 39408;
    std::cout << 39408;
    for (int i = 0; i < 39408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39409(int& val) {
    func_in_39409(val);
    const int x = 39409;
    std::cout << 39409;
    for (int i = 0; i < 39409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39409(int& val) {
    const int x = 39409;
    std::cout << 39409;
    for (int i = 0; i < 39409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39410(int& val) {
    func_in_39410(val);
    const int x = 39410;
    std::cout << 39410;
    for (int i = 0; i < 39410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39410(int& val) {
    const int x = 39410;
    std::cout << 39410;
    for (int i = 0; i < 39410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39411(int& val) {
    func_in_39411(val);
    const int x = 39411;
    std::cout << 39411;
    for (int i = 0; i < 39411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39411(int& val) {
    const int x = 39411;
    std::cout << 39411;
    for (int i = 0; i < 39411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39412(int& val) {
    func_in_39412(val);
    const int x = 39412;
    std::cout << 39412;
    for (int i = 0; i < 39412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39412(int& val) {
    const int x = 39412;
    std::cout << 39412;
    for (int i = 0; i < 39412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39413(int& val) {
    func_in_39413(val);
    const int x = 39413;
    std::cout << 39413;
    for (int i = 0; i < 39413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39413(int& val) {
    const int x = 39413;
    std::cout << 39413;
    for (int i = 0; i < 39413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39414(int& val) {
    func_in_39414(val);
    const int x = 39414;
    std::cout << 39414;
    for (int i = 0; i < 39414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39414(int& val) {
    const int x = 39414;
    std::cout << 39414;
    for (int i = 0; i < 39414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39415(int& val) {
    func_in_39415(val);
    const int x = 39415;
    std::cout << 39415;
    for (int i = 0; i < 39415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39415(int& val) {
    const int x = 39415;
    std::cout << 39415;
    for (int i = 0; i < 39415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39416(int& val) {
    func_in_39416(val);
    const int x = 39416;
    std::cout << 39416;
    for (int i = 0; i < 39416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39416(int& val) {
    const int x = 39416;
    std::cout << 39416;
    for (int i = 0; i < 39416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39417(int& val) {
    func_in_39417(val);
    const int x = 39417;
    std::cout << 39417;
    for (int i = 0; i < 39417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39417(int& val) {
    const int x = 39417;
    std::cout << 39417;
    for (int i = 0; i < 39417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39418(int& val) {
    func_in_39418(val);
    const int x = 39418;
    std::cout << 39418;
    for (int i = 0; i < 39418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39418(int& val) {
    const int x = 39418;
    std::cout << 39418;
    for (int i = 0; i < 39418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39419(int& val) {
    func_in_39419(val);
    const int x = 39419;
    std::cout << 39419;
    for (int i = 0; i < 39419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39419(int& val) {
    const int x = 39419;
    std::cout << 39419;
    for (int i = 0; i < 39419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39420(int& val) {
    func_in_39420(val);
    const int x = 39420;
    std::cout << 39420;
    for (int i = 0; i < 39420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39420(int& val) {
    const int x = 39420;
    std::cout << 39420;
    for (int i = 0; i < 39420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39421(int& val) {
    func_in_39421(val);
    const int x = 39421;
    std::cout << 39421;
    for (int i = 0; i < 39421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39421(int& val) {
    const int x = 39421;
    std::cout << 39421;
    for (int i = 0; i < 39421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39422(int& val) {
    func_in_39422(val);
    const int x = 39422;
    std::cout << 39422;
    for (int i = 0; i < 39422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39422(int& val) {
    const int x = 39422;
    std::cout << 39422;
    for (int i = 0; i < 39422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39423(int& val) {
    func_in_39423(val);
    const int x = 39423;
    std::cout << 39423;
    for (int i = 0; i < 39423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39423(int& val) {
    const int x = 39423;
    std::cout << 39423;
    for (int i = 0; i < 39423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39424(int& val) {
    func_in_39424(val);
    const int x = 39424;
    std::cout << 39424;
    for (int i = 0; i < 39424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39424(int& val) {
    const int x = 39424;
    std::cout << 39424;
    for (int i = 0; i < 39424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39425(int& val) {
    func_in_39425(val);
    const int x = 39425;
    std::cout << 39425;
    for (int i = 0; i < 39425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39425(int& val) {
    const int x = 39425;
    std::cout << 39425;
    for (int i = 0; i < 39425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39426(int& val) {
    func_in_39426(val);
    const int x = 39426;
    std::cout << 39426;
    for (int i = 0; i < 39426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39426(int& val) {
    const int x = 39426;
    std::cout << 39426;
    for (int i = 0; i < 39426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39427(int& val) {
    func_in_39427(val);
    const int x = 39427;
    std::cout << 39427;
    for (int i = 0; i < 39427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39427(int& val) {
    const int x = 39427;
    std::cout << 39427;
    for (int i = 0; i < 39427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39428(int& val) {
    func_in_39428(val);
    const int x = 39428;
    std::cout << 39428;
    for (int i = 0; i < 39428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39428(int& val) {
    const int x = 39428;
    std::cout << 39428;
    for (int i = 0; i < 39428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39429(int& val) {
    func_in_39429(val);
    const int x = 39429;
    std::cout << 39429;
    for (int i = 0; i < 39429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39429(int& val) {
    const int x = 39429;
    std::cout << 39429;
    for (int i = 0; i < 39429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39430(int& val) {
    func_in_39430(val);
    const int x = 39430;
    std::cout << 39430;
    for (int i = 0; i < 39430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39430(int& val) {
    const int x = 39430;
    std::cout << 39430;
    for (int i = 0; i < 39430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39431(int& val) {
    func_in_39431(val);
    const int x = 39431;
    std::cout << 39431;
    for (int i = 0; i < 39431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39431(int& val) {
    const int x = 39431;
    std::cout << 39431;
    for (int i = 0; i < 39431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39432(int& val) {
    func_in_39432(val);
    const int x = 39432;
    std::cout << 39432;
    for (int i = 0; i < 39432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39432(int& val) {
    const int x = 39432;
    std::cout << 39432;
    for (int i = 0; i < 39432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39433(int& val) {
    func_in_39433(val);
    const int x = 39433;
    std::cout << 39433;
    for (int i = 0; i < 39433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39433(int& val) {
    const int x = 39433;
    std::cout << 39433;
    for (int i = 0; i < 39433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39434(int& val) {
    func_in_39434(val);
    const int x = 39434;
    std::cout << 39434;
    for (int i = 0; i < 39434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39434(int& val) {
    const int x = 39434;
    std::cout << 39434;
    for (int i = 0; i < 39434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39435(int& val) {
    func_in_39435(val);
    const int x = 39435;
    std::cout << 39435;
    for (int i = 0; i < 39435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39435(int& val) {
    const int x = 39435;
    std::cout << 39435;
    for (int i = 0; i < 39435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39436(int& val) {
    func_in_39436(val);
    const int x = 39436;
    std::cout << 39436;
    for (int i = 0; i < 39436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39436(int& val) {
    const int x = 39436;
    std::cout << 39436;
    for (int i = 0; i < 39436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39437(int& val) {
    func_in_39437(val);
    const int x = 39437;
    std::cout << 39437;
    for (int i = 0; i < 39437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39437(int& val) {
    const int x = 39437;
    std::cout << 39437;
    for (int i = 0; i < 39437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39438(int& val) {
    func_in_39438(val);
    const int x = 39438;
    std::cout << 39438;
    for (int i = 0; i < 39438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39438(int& val) {
    const int x = 39438;
    std::cout << 39438;
    for (int i = 0; i < 39438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39439(int& val) {
    func_in_39439(val);
    const int x = 39439;
    std::cout << 39439;
    for (int i = 0; i < 39439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39439(int& val) {
    const int x = 39439;
    std::cout << 39439;
    for (int i = 0; i < 39439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39440(int& val) {
    func_in_39440(val);
    const int x = 39440;
    std::cout << 39440;
    for (int i = 0; i < 39440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39440(int& val) {
    const int x = 39440;
    std::cout << 39440;
    for (int i = 0; i < 39440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39441(int& val) {
    func_in_39441(val);
    const int x = 39441;
    std::cout << 39441;
    for (int i = 0; i < 39441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39441(int& val) {
    const int x = 39441;
    std::cout << 39441;
    for (int i = 0; i < 39441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39442(int& val) {
    func_in_39442(val);
    const int x = 39442;
    std::cout << 39442;
    for (int i = 0; i < 39442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39442(int& val) {
    const int x = 39442;
    std::cout << 39442;
    for (int i = 0; i < 39442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39443(int& val) {
    func_in_39443(val);
    const int x = 39443;
    std::cout << 39443;
    for (int i = 0; i < 39443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39443(int& val) {
    const int x = 39443;
    std::cout << 39443;
    for (int i = 0; i < 39443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39444(int& val) {
    func_in_39444(val);
    const int x = 39444;
    std::cout << 39444;
    for (int i = 0; i < 39444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39444(int& val) {
    const int x = 39444;
    std::cout << 39444;
    for (int i = 0; i < 39444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39445(int& val) {
    func_in_39445(val);
    const int x = 39445;
    std::cout << 39445;
    for (int i = 0; i < 39445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39445(int& val) {
    const int x = 39445;
    std::cout << 39445;
    for (int i = 0; i < 39445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39446(int& val) {
    func_in_39446(val);
    const int x = 39446;
    std::cout << 39446;
    for (int i = 0; i < 39446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39446(int& val) {
    const int x = 39446;
    std::cout << 39446;
    for (int i = 0; i < 39446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39447(int& val) {
    func_in_39447(val);
    const int x = 39447;
    std::cout << 39447;
    for (int i = 0; i < 39447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39447(int& val) {
    const int x = 39447;
    std::cout << 39447;
    for (int i = 0; i < 39447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39448(int& val) {
    func_in_39448(val);
    const int x = 39448;
    std::cout << 39448;
    for (int i = 0; i < 39448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39448(int& val) {
    const int x = 39448;
    std::cout << 39448;
    for (int i = 0; i < 39448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39449(int& val) {
    func_in_39449(val);
    const int x = 39449;
    std::cout << 39449;
    for (int i = 0; i < 39449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39449(int& val) {
    const int x = 39449;
    std::cout << 39449;
    for (int i = 0; i < 39449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39450(int& val) {
    func_in_39450(val);
    const int x = 39450;
    std::cout << 39450;
    for (int i = 0; i < 39450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39450(int& val) {
    const int x = 39450;
    std::cout << 39450;
    for (int i = 0; i < 39450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39451(int& val) {
    func_in_39451(val);
    const int x = 39451;
    std::cout << 39451;
    for (int i = 0; i < 39451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39451(int& val) {
    const int x = 39451;
    std::cout << 39451;
    for (int i = 0; i < 39451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39452(int& val) {
    func_in_39452(val);
    const int x = 39452;
    std::cout << 39452;
    for (int i = 0; i < 39452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39452(int& val) {
    const int x = 39452;
    std::cout << 39452;
    for (int i = 0; i < 39452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39453(int& val) {
    func_in_39453(val);
    const int x = 39453;
    std::cout << 39453;
    for (int i = 0; i < 39453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39453(int& val) {
    const int x = 39453;
    std::cout << 39453;
    for (int i = 0; i < 39453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39454(int& val) {
    func_in_39454(val);
    const int x = 39454;
    std::cout << 39454;
    for (int i = 0; i < 39454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39454(int& val) {
    const int x = 39454;
    std::cout << 39454;
    for (int i = 0; i < 39454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39455(int& val) {
    func_in_39455(val);
    const int x = 39455;
    std::cout << 39455;
    for (int i = 0; i < 39455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39455(int& val) {
    const int x = 39455;
    std::cout << 39455;
    for (int i = 0; i < 39455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39456(int& val) {
    func_in_39456(val);
    const int x = 39456;
    std::cout << 39456;
    for (int i = 0; i < 39456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39456(int& val) {
    const int x = 39456;
    std::cout << 39456;
    for (int i = 0; i < 39456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39457(int& val) {
    func_in_39457(val);
    const int x = 39457;
    std::cout << 39457;
    for (int i = 0; i < 39457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39457(int& val) {
    const int x = 39457;
    std::cout << 39457;
    for (int i = 0; i < 39457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39458(int& val) {
    func_in_39458(val);
    const int x = 39458;
    std::cout << 39458;
    for (int i = 0; i < 39458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39458(int& val) {
    const int x = 39458;
    std::cout << 39458;
    for (int i = 0; i < 39458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39459(int& val) {
    func_in_39459(val);
    const int x = 39459;
    std::cout << 39459;
    for (int i = 0; i < 39459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39459(int& val) {
    const int x = 39459;
    std::cout << 39459;
    for (int i = 0; i < 39459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39460(int& val) {
    func_in_39460(val);
    const int x = 39460;
    std::cout << 39460;
    for (int i = 0; i < 39460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39460(int& val) {
    const int x = 39460;
    std::cout << 39460;
    for (int i = 0; i < 39460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39461(int& val) {
    func_in_39461(val);
    const int x = 39461;
    std::cout << 39461;
    for (int i = 0; i < 39461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39461(int& val) {
    const int x = 39461;
    std::cout << 39461;
    for (int i = 0; i < 39461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39462(int& val) {
    func_in_39462(val);
    const int x = 39462;
    std::cout << 39462;
    for (int i = 0; i < 39462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39462(int& val) {
    const int x = 39462;
    std::cout << 39462;
    for (int i = 0; i < 39462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39463(int& val) {
    func_in_39463(val);
    const int x = 39463;
    std::cout << 39463;
    for (int i = 0; i < 39463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39463(int& val) {
    const int x = 39463;
    std::cout << 39463;
    for (int i = 0; i < 39463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39464(int& val) {
    func_in_39464(val);
    const int x = 39464;
    std::cout << 39464;
    for (int i = 0; i < 39464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39464(int& val) {
    const int x = 39464;
    std::cout << 39464;
    for (int i = 0; i < 39464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39465(int& val) {
    func_in_39465(val);
    const int x = 39465;
    std::cout << 39465;
    for (int i = 0; i < 39465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39465(int& val) {
    const int x = 39465;
    std::cout << 39465;
    for (int i = 0; i < 39465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39466(int& val) {
    func_in_39466(val);
    const int x = 39466;
    std::cout << 39466;
    for (int i = 0; i < 39466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39466(int& val) {
    const int x = 39466;
    std::cout << 39466;
    for (int i = 0; i < 39466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39467(int& val) {
    func_in_39467(val);
    const int x = 39467;
    std::cout << 39467;
    for (int i = 0; i < 39467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39467(int& val) {
    const int x = 39467;
    std::cout << 39467;
    for (int i = 0; i < 39467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39468(int& val) {
    func_in_39468(val);
    const int x = 39468;
    std::cout << 39468;
    for (int i = 0; i < 39468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39468(int& val) {
    const int x = 39468;
    std::cout << 39468;
    for (int i = 0; i < 39468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39469(int& val) {
    func_in_39469(val);
    const int x = 39469;
    std::cout << 39469;
    for (int i = 0; i < 39469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39469(int& val) {
    const int x = 39469;
    std::cout << 39469;
    for (int i = 0; i < 39469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39470(int& val) {
    func_in_39470(val);
    const int x = 39470;
    std::cout << 39470;
    for (int i = 0; i < 39470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39470(int& val) {
    const int x = 39470;
    std::cout << 39470;
    for (int i = 0; i < 39470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39471(int& val) {
    func_in_39471(val);
    const int x = 39471;
    std::cout << 39471;
    for (int i = 0; i < 39471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39471(int& val) {
    const int x = 39471;
    std::cout << 39471;
    for (int i = 0; i < 39471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39472(int& val) {
    func_in_39472(val);
    const int x = 39472;
    std::cout << 39472;
    for (int i = 0; i < 39472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39472(int& val) {
    const int x = 39472;
    std::cout << 39472;
    for (int i = 0; i < 39472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39473(int& val) {
    func_in_39473(val);
    const int x = 39473;
    std::cout << 39473;
    for (int i = 0; i < 39473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39473(int& val) {
    const int x = 39473;
    std::cout << 39473;
    for (int i = 0; i < 39473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39474(int& val) {
    func_in_39474(val);
    const int x = 39474;
    std::cout << 39474;
    for (int i = 0; i < 39474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39474(int& val) {
    const int x = 39474;
    std::cout << 39474;
    for (int i = 0; i < 39474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39475(int& val) {
    func_in_39475(val);
    const int x = 39475;
    std::cout << 39475;
    for (int i = 0; i < 39475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39475(int& val) {
    const int x = 39475;
    std::cout << 39475;
    for (int i = 0; i < 39475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39476(int& val) {
    func_in_39476(val);
    const int x = 39476;
    std::cout << 39476;
    for (int i = 0; i < 39476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39476(int& val) {
    const int x = 39476;
    std::cout << 39476;
    for (int i = 0; i < 39476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39477(int& val) {
    func_in_39477(val);
    const int x = 39477;
    std::cout << 39477;
    for (int i = 0; i < 39477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39477(int& val) {
    const int x = 39477;
    std::cout << 39477;
    for (int i = 0; i < 39477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39478(int& val) {
    func_in_39478(val);
    const int x = 39478;
    std::cout << 39478;
    for (int i = 0; i < 39478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39478(int& val) {
    const int x = 39478;
    std::cout << 39478;
    for (int i = 0; i < 39478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39479(int& val) {
    func_in_39479(val);
    const int x = 39479;
    std::cout << 39479;
    for (int i = 0; i < 39479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39479(int& val) {
    const int x = 39479;
    std::cout << 39479;
    for (int i = 0; i < 39479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39480(int& val) {
    func_in_39480(val);
    const int x = 39480;
    std::cout << 39480;
    for (int i = 0; i < 39480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39480(int& val) {
    const int x = 39480;
    std::cout << 39480;
    for (int i = 0; i < 39480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39481(int& val) {
    func_in_39481(val);
    const int x = 39481;
    std::cout << 39481;
    for (int i = 0; i < 39481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39481(int& val) {
    const int x = 39481;
    std::cout << 39481;
    for (int i = 0; i < 39481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39482(int& val) {
    func_in_39482(val);
    const int x = 39482;
    std::cout << 39482;
    for (int i = 0; i < 39482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39482(int& val) {
    const int x = 39482;
    std::cout << 39482;
    for (int i = 0; i < 39482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39483(int& val) {
    func_in_39483(val);
    const int x = 39483;
    std::cout << 39483;
    for (int i = 0; i < 39483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39483(int& val) {
    const int x = 39483;
    std::cout << 39483;
    for (int i = 0; i < 39483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39484(int& val) {
    func_in_39484(val);
    const int x = 39484;
    std::cout << 39484;
    for (int i = 0; i < 39484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39484(int& val) {
    const int x = 39484;
    std::cout << 39484;
    for (int i = 0; i < 39484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39485(int& val) {
    func_in_39485(val);
    const int x = 39485;
    std::cout << 39485;
    for (int i = 0; i < 39485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39485(int& val) {
    const int x = 39485;
    std::cout << 39485;
    for (int i = 0; i < 39485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39486(int& val) {
    func_in_39486(val);
    const int x = 39486;
    std::cout << 39486;
    for (int i = 0; i < 39486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39486(int& val) {
    const int x = 39486;
    std::cout << 39486;
    for (int i = 0; i < 39486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39487(int& val) {
    func_in_39487(val);
    const int x = 39487;
    std::cout << 39487;
    for (int i = 0; i < 39487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39487(int& val) {
    const int x = 39487;
    std::cout << 39487;
    for (int i = 0; i < 39487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39488(int& val) {
    func_in_39488(val);
    const int x = 39488;
    std::cout << 39488;
    for (int i = 0; i < 39488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39488(int& val) {
    const int x = 39488;
    std::cout << 39488;
    for (int i = 0; i < 39488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39489(int& val) {
    func_in_39489(val);
    const int x = 39489;
    std::cout << 39489;
    for (int i = 0; i < 39489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39489(int& val) {
    const int x = 39489;
    std::cout << 39489;
    for (int i = 0; i < 39489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39490(int& val) {
    func_in_39490(val);
    const int x = 39490;
    std::cout << 39490;
    for (int i = 0; i < 39490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39490(int& val) {
    const int x = 39490;
    std::cout << 39490;
    for (int i = 0; i < 39490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39491(int& val) {
    func_in_39491(val);
    const int x = 39491;
    std::cout << 39491;
    for (int i = 0; i < 39491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39491(int& val) {
    const int x = 39491;
    std::cout << 39491;
    for (int i = 0; i < 39491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39492(int& val) {
    func_in_39492(val);
    const int x = 39492;
    std::cout << 39492;
    for (int i = 0; i < 39492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39492(int& val) {
    const int x = 39492;
    std::cout << 39492;
    for (int i = 0; i < 39492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39493(int& val) {
    func_in_39493(val);
    const int x = 39493;
    std::cout << 39493;
    for (int i = 0; i < 39493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39493(int& val) {
    const int x = 39493;
    std::cout << 39493;
    for (int i = 0; i < 39493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39494(int& val) {
    func_in_39494(val);
    const int x = 39494;
    std::cout << 39494;
    for (int i = 0; i < 39494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39494(int& val) {
    const int x = 39494;
    std::cout << 39494;
    for (int i = 0; i < 39494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39495(int& val) {
    func_in_39495(val);
    const int x = 39495;
    std::cout << 39495;
    for (int i = 0; i < 39495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39495(int& val) {
    const int x = 39495;
    std::cout << 39495;
    for (int i = 0; i < 39495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39496(int& val) {
    func_in_39496(val);
    const int x = 39496;
    std::cout << 39496;
    for (int i = 0; i < 39496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39496(int& val) {
    const int x = 39496;
    std::cout << 39496;
    for (int i = 0; i < 39496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39497(int& val) {
    func_in_39497(val);
    const int x = 39497;
    std::cout << 39497;
    for (int i = 0; i < 39497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39497(int& val) {
    const int x = 39497;
    std::cout << 39497;
    for (int i = 0; i < 39497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39498(int& val) {
    func_in_39498(val);
    const int x = 39498;
    std::cout << 39498;
    for (int i = 0; i < 39498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39498(int& val) {
    const int x = 39498;
    std::cout << 39498;
    for (int i = 0; i < 39498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39499(int& val) {
    func_in_39499(val);
    const int x = 39499;
    std::cout << 39499;
    for (int i = 0; i < 39499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39499(int& val) {
    const int x = 39499;
    std::cout << 39499;
    for (int i = 0; i < 39499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39500(int& val) {
    func_in_39500(val);
    const int x = 39500;
    std::cout << 39500;
    for (int i = 0; i < 39500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39500(int& val) {
    const int x = 39500;
    std::cout << 39500;
    for (int i = 0; i < 39500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39501(int& val) {
    func_in_39501(val);
    const int x = 39501;
    std::cout << 39501;
    for (int i = 0; i < 39501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39501(int& val) {
    const int x = 39501;
    std::cout << 39501;
    for (int i = 0; i < 39501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39502(int& val) {
    func_in_39502(val);
    const int x = 39502;
    std::cout << 39502;
    for (int i = 0; i < 39502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39502(int& val) {
    const int x = 39502;
    std::cout << 39502;
    for (int i = 0; i < 39502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39503(int& val) {
    func_in_39503(val);
    const int x = 39503;
    std::cout << 39503;
    for (int i = 0; i < 39503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39503(int& val) {
    const int x = 39503;
    std::cout << 39503;
    for (int i = 0; i < 39503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39504(int& val) {
    func_in_39504(val);
    const int x = 39504;
    std::cout << 39504;
    for (int i = 0; i < 39504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39504(int& val) {
    const int x = 39504;
    std::cout << 39504;
    for (int i = 0; i < 39504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39505(int& val) {
    func_in_39505(val);
    const int x = 39505;
    std::cout << 39505;
    for (int i = 0; i < 39505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39505(int& val) {
    const int x = 39505;
    std::cout << 39505;
    for (int i = 0; i < 39505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39506(int& val) {
    func_in_39506(val);
    const int x = 39506;
    std::cout << 39506;
    for (int i = 0; i < 39506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39506(int& val) {
    const int x = 39506;
    std::cout << 39506;
    for (int i = 0; i < 39506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39507(int& val) {
    func_in_39507(val);
    const int x = 39507;
    std::cout << 39507;
    for (int i = 0; i < 39507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39507(int& val) {
    const int x = 39507;
    std::cout << 39507;
    for (int i = 0; i < 39507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39508(int& val) {
    func_in_39508(val);
    const int x = 39508;
    std::cout << 39508;
    for (int i = 0; i < 39508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39508(int& val) {
    const int x = 39508;
    std::cout << 39508;
    for (int i = 0; i < 39508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39509(int& val) {
    func_in_39509(val);
    const int x = 39509;
    std::cout << 39509;
    for (int i = 0; i < 39509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39509(int& val) {
    const int x = 39509;
    std::cout << 39509;
    for (int i = 0; i < 39509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39510(int& val) {
    func_in_39510(val);
    const int x = 39510;
    std::cout << 39510;
    for (int i = 0; i < 39510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39510(int& val) {
    const int x = 39510;
    std::cout << 39510;
    for (int i = 0; i < 39510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39511(int& val) {
    func_in_39511(val);
    const int x = 39511;
    std::cout << 39511;
    for (int i = 0; i < 39511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39511(int& val) {
    const int x = 39511;
    std::cout << 39511;
    for (int i = 0; i < 39511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39512(int& val) {
    func_in_39512(val);
    const int x = 39512;
    std::cout << 39512;
    for (int i = 0; i < 39512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39512(int& val) {
    const int x = 39512;
    std::cout << 39512;
    for (int i = 0; i < 39512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39513(int& val) {
    func_in_39513(val);
    const int x = 39513;
    std::cout << 39513;
    for (int i = 0; i < 39513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39513(int& val) {
    const int x = 39513;
    std::cout << 39513;
    for (int i = 0; i < 39513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39514(int& val) {
    func_in_39514(val);
    const int x = 39514;
    std::cout << 39514;
    for (int i = 0; i < 39514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39514(int& val) {
    const int x = 39514;
    std::cout << 39514;
    for (int i = 0; i < 39514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39515(int& val) {
    func_in_39515(val);
    const int x = 39515;
    std::cout << 39515;
    for (int i = 0; i < 39515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39515(int& val) {
    const int x = 39515;
    std::cout << 39515;
    for (int i = 0; i < 39515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39516(int& val) {
    func_in_39516(val);
    const int x = 39516;
    std::cout << 39516;
    for (int i = 0; i < 39516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39516(int& val) {
    const int x = 39516;
    std::cout << 39516;
    for (int i = 0; i < 39516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39517(int& val) {
    func_in_39517(val);
    const int x = 39517;
    std::cout << 39517;
    for (int i = 0; i < 39517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39517(int& val) {
    const int x = 39517;
    std::cout << 39517;
    for (int i = 0; i < 39517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39518(int& val) {
    func_in_39518(val);
    const int x = 39518;
    std::cout << 39518;
    for (int i = 0; i < 39518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39518(int& val) {
    const int x = 39518;
    std::cout << 39518;
    for (int i = 0; i < 39518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39519(int& val) {
    func_in_39519(val);
    const int x = 39519;
    std::cout << 39519;
    for (int i = 0; i < 39519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39519(int& val) {
    const int x = 39519;
    std::cout << 39519;
    for (int i = 0; i < 39519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39520(int& val) {
    func_in_39520(val);
    const int x = 39520;
    std::cout << 39520;
    for (int i = 0; i < 39520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39520(int& val) {
    const int x = 39520;
    std::cout << 39520;
    for (int i = 0; i < 39520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39521(int& val) {
    func_in_39521(val);
    const int x = 39521;
    std::cout << 39521;
    for (int i = 0; i < 39521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39521(int& val) {
    const int x = 39521;
    std::cout << 39521;
    for (int i = 0; i < 39521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39522(int& val) {
    func_in_39522(val);
    const int x = 39522;
    std::cout << 39522;
    for (int i = 0; i < 39522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39522(int& val) {
    const int x = 39522;
    std::cout << 39522;
    for (int i = 0; i < 39522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39523(int& val) {
    func_in_39523(val);
    const int x = 39523;
    std::cout << 39523;
    for (int i = 0; i < 39523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39523(int& val) {
    const int x = 39523;
    std::cout << 39523;
    for (int i = 0; i < 39523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39524(int& val) {
    func_in_39524(val);
    const int x = 39524;
    std::cout << 39524;
    for (int i = 0; i < 39524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39524(int& val) {
    const int x = 39524;
    std::cout << 39524;
    for (int i = 0; i < 39524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39525(int& val) {
    func_in_39525(val);
    const int x = 39525;
    std::cout << 39525;
    for (int i = 0; i < 39525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39525(int& val) {
    const int x = 39525;
    std::cout << 39525;
    for (int i = 0; i < 39525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39526(int& val) {
    func_in_39526(val);
    const int x = 39526;
    std::cout << 39526;
    for (int i = 0; i < 39526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39526(int& val) {
    const int x = 39526;
    std::cout << 39526;
    for (int i = 0; i < 39526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39527(int& val) {
    func_in_39527(val);
    const int x = 39527;
    std::cout << 39527;
    for (int i = 0; i < 39527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39527(int& val) {
    const int x = 39527;
    std::cout << 39527;
    for (int i = 0; i < 39527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39528(int& val) {
    func_in_39528(val);
    const int x = 39528;
    std::cout << 39528;
    for (int i = 0; i < 39528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39528(int& val) {
    const int x = 39528;
    std::cout << 39528;
    for (int i = 0; i < 39528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39529(int& val) {
    func_in_39529(val);
    const int x = 39529;
    std::cout << 39529;
    for (int i = 0; i < 39529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39529(int& val) {
    const int x = 39529;
    std::cout << 39529;
    for (int i = 0; i < 39529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39530(int& val) {
    func_in_39530(val);
    const int x = 39530;
    std::cout << 39530;
    for (int i = 0; i < 39530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39530(int& val) {
    const int x = 39530;
    std::cout << 39530;
    for (int i = 0; i < 39530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39531(int& val) {
    func_in_39531(val);
    const int x = 39531;
    std::cout << 39531;
    for (int i = 0; i < 39531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39531(int& val) {
    const int x = 39531;
    std::cout << 39531;
    for (int i = 0; i < 39531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39532(int& val) {
    func_in_39532(val);
    const int x = 39532;
    std::cout << 39532;
    for (int i = 0; i < 39532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39532(int& val) {
    const int x = 39532;
    std::cout << 39532;
    for (int i = 0; i < 39532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39533(int& val) {
    func_in_39533(val);
    const int x = 39533;
    std::cout << 39533;
    for (int i = 0; i < 39533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39533(int& val) {
    const int x = 39533;
    std::cout << 39533;
    for (int i = 0; i < 39533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39534(int& val) {
    func_in_39534(val);
    const int x = 39534;
    std::cout << 39534;
    for (int i = 0; i < 39534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39534(int& val) {
    const int x = 39534;
    std::cout << 39534;
    for (int i = 0; i < 39534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39535(int& val) {
    func_in_39535(val);
    const int x = 39535;
    std::cout << 39535;
    for (int i = 0; i < 39535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39535(int& val) {
    const int x = 39535;
    std::cout << 39535;
    for (int i = 0; i < 39535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39536(int& val) {
    func_in_39536(val);
    const int x = 39536;
    std::cout << 39536;
    for (int i = 0; i < 39536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39536(int& val) {
    const int x = 39536;
    std::cout << 39536;
    for (int i = 0; i < 39536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39537(int& val) {
    func_in_39537(val);
    const int x = 39537;
    std::cout << 39537;
    for (int i = 0; i < 39537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39537(int& val) {
    const int x = 39537;
    std::cout << 39537;
    for (int i = 0; i < 39537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39538(int& val) {
    func_in_39538(val);
    const int x = 39538;
    std::cout << 39538;
    for (int i = 0; i < 39538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39538(int& val) {
    const int x = 39538;
    std::cout << 39538;
    for (int i = 0; i < 39538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39539(int& val) {
    func_in_39539(val);
    const int x = 39539;
    std::cout << 39539;
    for (int i = 0; i < 39539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39539(int& val) {
    const int x = 39539;
    std::cout << 39539;
    for (int i = 0; i < 39539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39540(int& val) {
    func_in_39540(val);
    const int x = 39540;
    std::cout << 39540;
    for (int i = 0; i < 39540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39540(int& val) {
    const int x = 39540;
    std::cout << 39540;
    for (int i = 0; i < 39540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39541(int& val) {
    func_in_39541(val);
    const int x = 39541;
    std::cout << 39541;
    for (int i = 0; i < 39541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39541(int& val) {
    const int x = 39541;
    std::cout << 39541;
    for (int i = 0; i < 39541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39542(int& val) {
    func_in_39542(val);
    const int x = 39542;
    std::cout << 39542;
    for (int i = 0; i < 39542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39542(int& val) {
    const int x = 39542;
    std::cout << 39542;
    for (int i = 0; i < 39542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39543(int& val) {
    func_in_39543(val);
    const int x = 39543;
    std::cout << 39543;
    for (int i = 0; i < 39543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39543(int& val) {
    const int x = 39543;
    std::cout << 39543;
    for (int i = 0; i < 39543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39544(int& val) {
    func_in_39544(val);
    const int x = 39544;
    std::cout << 39544;
    for (int i = 0; i < 39544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39544(int& val) {
    const int x = 39544;
    std::cout << 39544;
    for (int i = 0; i < 39544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39545(int& val) {
    func_in_39545(val);
    const int x = 39545;
    std::cout << 39545;
    for (int i = 0; i < 39545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39545(int& val) {
    const int x = 39545;
    std::cout << 39545;
    for (int i = 0; i < 39545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39546(int& val) {
    func_in_39546(val);
    const int x = 39546;
    std::cout << 39546;
    for (int i = 0; i < 39546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39546(int& val) {
    const int x = 39546;
    std::cout << 39546;
    for (int i = 0; i < 39546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39547(int& val) {
    func_in_39547(val);
    const int x = 39547;
    std::cout << 39547;
    for (int i = 0; i < 39547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39547(int& val) {
    const int x = 39547;
    std::cout << 39547;
    for (int i = 0; i < 39547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39548(int& val) {
    func_in_39548(val);
    const int x = 39548;
    std::cout << 39548;
    for (int i = 0; i < 39548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39548(int& val) {
    const int x = 39548;
    std::cout << 39548;
    for (int i = 0; i < 39548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39549(int& val) {
    func_in_39549(val);
    const int x = 39549;
    std::cout << 39549;
    for (int i = 0; i < 39549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39549(int& val) {
    const int x = 39549;
    std::cout << 39549;
    for (int i = 0; i < 39549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39550(int& val) {
    func_in_39550(val);
    const int x = 39550;
    std::cout << 39550;
    for (int i = 0; i < 39550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39550(int& val) {
    const int x = 39550;
    std::cout << 39550;
    for (int i = 0; i < 39550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39551(int& val) {
    func_in_39551(val);
    const int x = 39551;
    std::cout << 39551;
    for (int i = 0; i < 39551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39551(int& val) {
    const int x = 39551;
    std::cout << 39551;
    for (int i = 0; i < 39551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39552(int& val) {
    func_in_39552(val);
    const int x = 39552;
    std::cout << 39552;
    for (int i = 0; i < 39552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39552(int& val) {
    const int x = 39552;
    std::cout << 39552;
    for (int i = 0; i < 39552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39553(int& val) {
    func_in_39553(val);
    const int x = 39553;
    std::cout << 39553;
    for (int i = 0; i < 39553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39553(int& val) {
    const int x = 39553;
    std::cout << 39553;
    for (int i = 0; i < 39553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39554(int& val) {
    func_in_39554(val);
    const int x = 39554;
    std::cout << 39554;
    for (int i = 0; i < 39554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39554(int& val) {
    const int x = 39554;
    std::cout << 39554;
    for (int i = 0; i < 39554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39555(int& val) {
    func_in_39555(val);
    const int x = 39555;
    std::cout << 39555;
    for (int i = 0; i < 39555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39555(int& val) {
    const int x = 39555;
    std::cout << 39555;
    for (int i = 0; i < 39555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39556(int& val) {
    func_in_39556(val);
    const int x = 39556;
    std::cout << 39556;
    for (int i = 0; i < 39556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39556(int& val) {
    const int x = 39556;
    std::cout << 39556;
    for (int i = 0; i < 39556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39557(int& val) {
    func_in_39557(val);
    const int x = 39557;
    std::cout << 39557;
    for (int i = 0; i < 39557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39557(int& val) {
    const int x = 39557;
    std::cout << 39557;
    for (int i = 0; i < 39557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39558(int& val) {
    func_in_39558(val);
    const int x = 39558;
    std::cout << 39558;
    for (int i = 0; i < 39558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39558(int& val) {
    const int x = 39558;
    std::cout << 39558;
    for (int i = 0; i < 39558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39559(int& val) {
    func_in_39559(val);
    const int x = 39559;
    std::cout << 39559;
    for (int i = 0; i < 39559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39559(int& val) {
    const int x = 39559;
    std::cout << 39559;
    for (int i = 0; i < 39559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39560(int& val) {
    func_in_39560(val);
    const int x = 39560;
    std::cout << 39560;
    for (int i = 0; i < 39560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39560(int& val) {
    const int x = 39560;
    std::cout << 39560;
    for (int i = 0; i < 39560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39561(int& val) {
    func_in_39561(val);
    const int x = 39561;
    std::cout << 39561;
    for (int i = 0; i < 39561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39561(int& val) {
    const int x = 39561;
    std::cout << 39561;
    for (int i = 0; i < 39561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39562(int& val) {
    func_in_39562(val);
    const int x = 39562;
    std::cout << 39562;
    for (int i = 0; i < 39562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39562(int& val) {
    const int x = 39562;
    std::cout << 39562;
    for (int i = 0; i < 39562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39563(int& val) {
    func_in_39563(val);
    const int x = 39563;
    std::cout << 39563;
    for (int i = 0; i < 39563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39563(int& val) {
    const int x = 39563;
    std::cout << 39563;
    for (int i = 0; i < 39563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39564(int& val) {
    func_in_39564(val);
    const int x = 39564;
    std::cout << 39564;
    for (int i = 0; i < 39564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39564(int& val) {
    const int x = 39564;
    std::cout << 39564;
    for (int i = 0; i < 39564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39565(int& val) {
    func_in_39565(val);
    const int x = 39565;
    std::cout << 39565;
    for (int i = 0; i < 39565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39565(int& val) {
    const int x = 39565;
    std::cout << 39565;
    for (int i = 0; i < 39565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39566(int& val) {
    func_in_39566(val);
    const int x = 39566;
    std::cout << 39566;
    for (int i = 0; i < 39566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39566(int& val) {
    const int x = 39566;
    std::cout << 39566;
    for (int i = 0; i < 39566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39567(int& val) {
    func_in_39567(val);
    const int x = 39567;
    std::cout << 39567;
    for (int i = 0; i < 39567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39567(int& val) {
    const int x = 39567;
    std::cout << 39567;
    for (int i = 0; i < 39567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39568(int& val) {
    func_in_39568(val);
    const int x = 39568;
    std::cout << 39568;
    for (int i = 0; i < 39568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39568(int& val) {
    const int x = 39568;
    std::cout << 39568;
    for (int i = 0; i < 39568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39569(int& val) {
    func_in_39569(val);
    const int x = 39569;
    std::cout << 39569;
    for (int i = 0; i < 39569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39569(int& val) {
    const int x = 39569;
    std::cout << 39569;
    for (int i = 0; i < 39569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39570(int& val) {
    func_in_39570(val);
    const int x = 39570;
    std::cout << 39570;
    for (int i = 0; i < 39570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39570(int& val) {
    const int x = 39570;
    std::cout << 39570;
    for (int i = 0; i < 39570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39571(int& val) {
    func_in_39571(val);
    const int x = 39571;
    std::cout << 39571;
    for (int i = 0; i < 39571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39571(int& val) {
    const int x = 39571;
    std::cout << 39571;
    for (int i = 0; i < 39571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39572(int& val) {
    func_in_39572(val);
    const int x = 39572;
    std::cout << 39572;
    for (int i = 0; i < 39572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39572(int& val) {
    const int x = 39572;
    std::cout << 39572;
    for (int i = 0; i < 39572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39573(int& val) {
    func_in_39573(val);
    const int x = 39573;
    std::cout << 39573;
    for (int i = 0; i < 39573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39573(int& val) {
    const int x = 39573;
    std::cout << 39573;
    for (int i = 0; i < 39573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39574(int& val) {
    func_in_39574(val);
    const int x = 39574;
    std::cout << 39574;
    for (int i = 0; i < 39574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39574(int& val) {
    const int x = 39574;
    std::cout << 39574;
    for (int i = 0; i < 39574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39575(int& val) {
    func_in_39575(val);
    const int x = 39575;
    std::cout << 39575;
    for (int i = 0; i < 39575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39575(int& val) {
    const int x = 39575;
    std::cout << 39575;
    for (int i = 0; i < 39575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39576(int& val) {
    func_in_39576(val);
    const int x = 39576;
    std::cout << 39576;
    for (int i = 0; i < 39576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39576(int& val) {
    const int x = 39576;
    std::cout << 39576;
    for (int i = 0; i < 39576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39577(int& val) {
    func_in_39577(val);
    const int x = 39577;
    std::cout << 39577;
    for (int i = 0; i < 39577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39577(int& val) {
    const int x = 39577;
    std::cout << 39577;
    for (int i = 0; i < 39577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39578(int& val) {
    func_in_39578(val);
    const int x = 39578;
    std::cout << 39578;
    for (int i = 0; i < 39578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39578(int& val) {
    const int x = 39578;
    std::cout << 39578;
    for (int i = 0; i < 39578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39579(int& val) {
    func_in_39579(val);
    const int x = 39579;
    std::cout << 39579;
    for (int i = 0; i < 39579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39579(int& val) {
    const int x = 39579;
    std::cout << 39579;
    for (int i = 0; i < 39579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39580(int& val) {
    func_in_39580(val);
    const int x = 39580;
    std::cout << 39580;
    for (int i = 0; i < 39580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39580(int& val) {
    const int x = 39580;
    std::cout << 39580;
    for (int i = 0; i < 39580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39581(int& val) {
    func_in_39581(val);
    const int x = 39581;
    std::cout << 39581;
    for (int i = 0; i < 39581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39581(int& val) {
    const int x = 39581;
    std::cout << 39581;
    for (int i = 0; i < 39581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39582(int& val) {
    func_in_39582(val);
    const int x = 39582;
    std::cout << 39582;
    for (int i = 0; i < 39582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39582(int& val) {
    const int x = 39582;
    std::cout << 39582;
    for (int i = 0; i < 39582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39583(int& val) {
    func_in_39583(val);
    const int x = 39583;
    std::cout << 39583;
    for (int i = 0; i < 39583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39583(int& val) {
    const int x = 39583;
    std::cout << 39583;
    for (int i = 0; i < 39583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39584(int& val) {
    func_in_39584(val);
    const int x = 39584;
    std::cout << 39584;
    for (int i = 0; i < 39584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39584(int& val) {
    const int x = 39584;
    std::cout << 39584;
    for (int i = 0; i < 39584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39585(int& val) {
    func_in_39585(val);
    const int x = 39585;
    std::cout << 39585;
    for (int i = 0; i < 39585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39585(int& val) {
    const int x = 39585;
    std::cout << 39585;
    for (int i = 0; i < 39585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39586(int& val) {
    func_in_39586(val);
    const int x = 39586;
    std::cout << 39586;
    for (int i = 0; i < 39586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39586(int& val) {
    const int x = 39586;
    std::cout << 39586;
    for (int i = 0; i < 39586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39587(int& val) {
    func_in_39587(val);
    const int x = 39587;
    std::cout << 39587;
    for (int i = 0; i < 39587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39587(int& val) {
    const int x = 39587;
    std::cout << 39587;
    for (int i = 0; i < 39587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39588(int& val) {
    func_in_39588(val);
    const int x = 39588;
    std::cout << 39588;
    for (int i = 0; i < 39588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39588(int& val) {
    const int x = 39588;
    std::cout << 39588;
    for (int i = 0; i < 39588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39589(int& val) {
    func_in_39589(val);
    const int x = 39589;
    std::cout << 39589;
    for (int i = 0; i < 39589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39589(int& val) {
    const int x = 39589;
    std::cout << 39589;
    for (int i = 0; i < 39589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39590(int& val) {
    func_in_39590(val);
    const int x = 39590;
    std::cout << 39590;
    for (int i = 0; i < 39590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39590(int& val) {
    const int x = 39590;
    std::cout << 39590;
    for (int i = 0; i < 39590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39591(int& val) {
    func_in_39591(val);
    const int x = 39591;
    std::cout << 39591;
    for (int i = 0; i < 39591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39591(int& val) {
    const int x = 39591;
    std::cout << 39591;
    for (int i = 0; i < 39591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39592(int& val) {
    func_in_39592(val);
    const int x = 39592;
    std::cout << 39592;
    for (int i = 0; i < 39592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39592(int& val) {
    const int x = 39592;
    std::cout << 39592;
    for (int i = 0; i < 39592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39593(int& val) {
    func_in_39593(val);
    const int x = 39593;
    std::cout << 39593;
    for (int i = 0; i < 39593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39593(int& val) {
    const int x = 39593;
    std::cout << 39593;
    for (int i = 0; i < 39593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39594(int& val) {
    func_in_39594(val);
    const int x = 39594;
    std::cout << 39594;
    for (int i = 0; i < 39594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39594(int& val) {
    const int x = 39594;
    std::cout << 39594;
    for (int i = 0; i < 39594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39595(int& val) {
    func_in_39595(val);
    const int x = 39595;
    std::cout << 39595;
    for (int i = 0; i < 39595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39595(int& val) {
    const int x = 39595;
    std::cout << 39595;
    for (int i = 0; i < 39595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39596(int& val) {
    func_in_39596(val);
    const int x = 39596;
    std::cout << 39596;
    for (int i = 0; i < 39596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39596(int& val) {
    const int x = 39596;
    std::cout << 39596;
    for (int i = 0; i < 39596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39597(int& val) {
    func_in_39597(val);
    const int x = 39597;
    std::cout << 39597;
    for (int i = 0; i < 39597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39597(int& val) {
    const int x = 39597;
    std::cout << 39597;
    for (int i = 0; i < 39597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39598(int& val) {
    func_in_39598(val);
    const int x = 39598;
    std::cout << 39598;
    for (int i = 0; i < 39598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39598(int& val) {
    const int x = 39598;
    std::cout << 39598;
    for (int i = 0; i < 39598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39599(int& val) {
    func_in_39599(val);
    const int x = 39599;
    std::cout << 39599;
    for (int i = 0; i < 39599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39599(int& val) {
    const int x = 39599;
    std::cout << 39599;
    for (int i = 0; i < 39599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39600(int& val) {
    func_in_39600(val);
    const int x = 39600;
    std::cout << 39600;
    for (int i = 0; i < 39600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39600(int& val) {
    const int x = 39600;
    std::cout << 39600;
    for (int i = 0; i < 39600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39601(int& val) {
    func_in_39601(val);
    const int x = 39601;
    std::cout << 39601;
    for (int i = 0; i < 39601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39601(int& val) {
    const int x = 39601;
    std::cout << 39601;
    for (int i = 0; i < 39601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39602(int& val) {
    func_in_39602(val);
    const int x = 39602;
    std::cout << 39602;
    for (int i = 0; i < 39602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39602(int& val) {
    const int x = 39602;
    std::cout << 39602;
    for (int i = 0; i < 39602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39603(int& val) {
    func_in_39603(val);
    const int x = 39603;
    std::cout << 39603;
    for (int i = 0; i < 39603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39603(int& val) {
    const int x = 39603;
    std::cout << 39603;
    for (int i = 0; i < 39603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39604(int& val) {
    func_in_39604(val);
    const int x = 39604;
    std::cout << 39604;
    for (int i = 0; i < 39604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39604(int& val) {
    const int x = 39604;
    std::cout << 39604;
    for (int i = 0; i < 39604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39605(int& val) {
    func_in_39605(val);
    const int x = 39605;
    std::cout << 39605;
    for (int i = 0; i < 39605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39605(int& val) {
    const int x = 39605;
    std::cout << 39605;
    for (int i = 0; i < 39605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39606(int& val) {
    func_in_39606(val);
    const int x = 39606;
    std::cout << 39606;
    for (int i = 0; i < 39606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39606(int& val) {
    const int x = 39606;
    std::cout << 39606;
    for (int i = 0; i < 39606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39607(int& val) {
    func_in_39607(val);
    const int x = 39607;
    std::cout << 39607;
    for (int i = 0; i < 39607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39607(int& val) {
    const int x = 39607;
    std::cout << 39607;
    for (int i = 0; i < 39607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39608(int& val) {
    func_in_39608(val);
    const int x = 39608;
    std::cout << 39608;
    for (int i = 0; i < 39608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39608(int& val) {
    const int x = 39608;
    std::cout << 39608;
    for (int i = 0; i < 39608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39609(int& val) {
    func_in_39609(val);
    const int x = 39609;
    std::cout << 39609;
    for (int i = 0; i < 39609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39609(int& val) {
    const int x = 39609;
    std::cout << 39609;
    for (int i = 0; i < 39609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39610(int& val) {
    func_in_39610(val);
    const int x = 39610;
    std::cout << 39610;
    for (int i = 0; i < 39610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39610(int& val) {
    const int x = 39610;
    std::cout << 39610;
    for (int i = 0; i < 39610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39611(int& val) {
    func_in_39611(val);
    const int x = 39611;
    std::cout << 39611;
    for (int i = 0; i < 39611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39611(int& val) {
    const int x = 39611;
    std::cout << 39611;
    for (int i = 0; i < 39611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39612(int& val) {
    func_in_39612(val);
    const int x = 39612;
    std::cout << 39612;
    for (int i = 0; i < 39612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39612(int& val) {
    const int x = 39612;
    std::cout << 39612;
    for (int i = 0; i < 39612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39613(int& val) {
    func_in_39613(val);
    const int x = 39613;
    std::cout << 39613;
    for (int i = 0; i < 39613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39613(int& val) {
    const int x = 39613;
    std::cout << 39613;
    for (int i = 0; i < 39613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39614(int& val) {
    func_in_39614(val);
    const int x = 39614;
    std::cout << 39614;
    for (int i = 0; i < 39614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39614(int& val) {
    const int x = 39614;
    std::cout << 39614;
    for (int i = 0; i < 39614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39615(int& val) {
    func_in_39615(val);
    const int x = 39615;
    std::cout << 39615;
    for (int i = 0; i < 39615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39615(int& val) {
    const int x = 39615;
    std::cout << 39615;
    for (int i = 0; i < 39615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39616(int& val) {
    func_in_39616(val);
    const int x = 39616;
    std::cout << 39616;
    for (int i = 0; i < 39616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39616(int& val) {
    const int x = 39616;
    std::cout << 39616;
    for (int i = 0; i < 39616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39617(int& val) {
    func_in_39617(val);
    const int x = 39617;
    std::cout << 39617;
    for (int i = 0; i < 39617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39617(int& val) {
    const int x = 39617;
    std::cout << 39617;
    for (int i = 0; i < 39617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39618(int& val) {
    func_in_39618(val);
    const int x = 39618;
    std::cout << 39618;
    for (int i = 0; i < 39618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39618(int& val) {
    const int x = 39618;
    std::cout << 39618;
    for (int i = 0; i < 39618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39619(int& val) {
    func_in_39619(val);
    const int x = 39619;
    std::cout << 39619;
    for (int i = 0; i < 39619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39619(int& val) {
    const int x = 39619;
    std::cout << 39619;
    for (int i = 0; i < 39619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39620(int& val) {
    func_in_39620(val);
    const int x = 39620;
    std::cout << 39620;
    for (int i = 0; i < 39620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39620(int& val) {
    const int x = 39620;
    std::cout << 39620;
    for (int i = 0; i < 39620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39621(int& val) {
    func_in_39621(val);
    const int x = 39621;
    std::cout << 39621;
    for (int i = 0; i < 39621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39621(int& val) {
    const int x = 39621;
    std::cout << 39621;
    for (int i = 0; i < 39621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39622(int& val) {
    func_in_39622(val);
    const int x = 39622;
    std::cout << 39622;
    for (int i = 0; i < 39622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39622(int& val) {
    const int x = 39622;
    std::cout << 39622;
    for (int i = 0; i < 39622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39623(int& val) {
    func_in_39623(val);
    const int x = 39623;
    std::cout << 39623;
    for (int i = 0; i < 39623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39623(int& val) {
    const int x = 39623;
    std::cout << 39623;
    for (int i = 0; i < 39623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39624(int& val) {
    func_in_39624(val);
    const int x = 39624;
    std::cout << 39624;
    for (int i = 0; i < 39624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39624(int& val) {
    const int x = 39624;
    std::cout << 39624;
    for (int i = 0; i < 39624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39625(int& val) {
    func_in_39625(val);
    const int x = 39625;
    std::cout << 39625;
    for (int i = 0; i < 39625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39625(int& val) {
    const int x = 39625;
    std::cout << 39625;
    for (int i = 0; i < 39625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39626(int& val) {
    func_in_39626(val);
    const int x = 39626;
    std::cout << 39626;
    for (int i = 0; i < 39626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39626(int& val) {
    const int x = 39626;
    std::cout << 39626;
    for (int i = 0; i < 39626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39627(int& val) {
    func_in_39627(val);
    const int x = 39627;
    std::cout << 39627;
    for (int i = 0; i < 39627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39627(int& val) {
    const int x = 39627;
    std::cout << 39627;
    for (int i = 0; i < 39627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39628(int& val) {
    func_in_39628(val);
    const int x = 39628;
    std::cout << 39628;
    for (int i = 0; i < 39628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39628(int& val) {
    const int x = 39628;
    std::cout << 39628;
    for (int i = 0; i < 39628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39629(int& val) {
    func_in_39629(val);
    const int x = 39629;
    std::cout << 39629;
    for (int i = 0; i < 39629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39629(int& val) {
    const int x = 39629;
    std::cout << 39629;
    for (int i = 0; i < 39629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39630(int& val) {
    func_in_39630(val);
    const int x = 39630;
    std::cout << 39630;
    for (int i = 0; i < 39630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39630(int& val) {
    const int x = 39630;
    std::cout << 39630;
    for (int i = 0; i < 39630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39631(int& val) {
    func_in_39631(val);
    const int x = 39631;
    std::cout << 39631;
    for (int i = 0; i < 39631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39631(int& val) {
    const int x = 39631;
    std::cout << 39631;
    for (int i = 0; i < 39631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39632(int& val) {
    func_in_39632(val);
    const int x = 39632;
    std::cout << 39632;
    for (int i = 0; i < 39632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39632(int& val) {
    const int x = 39632;
    std::cout << 39632;
    for (int i = 0; i < 39632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39633(int& val) {
    func_in_39633(val);
    const int x = 39633;
    std::cout << 39633;
    for (int i = 0; i < 39633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39633(int& val) {
    const int x = 39633;
    std::cout << 39633;
    for (int i = 0; i < 39633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39634(int& val) {
    func_in_39634(val);
    const int x = 39634;
    std::cout << 39634;
    for (int i = 0; i < 39634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39634(int& val) {
    const int x = 39634;
    std::cout << 39634;
    for (int i = 0; i < 39634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39635(int& val) {
    func_in_39635(val);
    const int x = 39635;
    std::cout << 39635;
    for (int i = 0; i < 39635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39635(int& val) {
    const int x = 39635;
    std::cout << 39635;
    for (int i = 0; i < 39635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39636(int& val) {
    func_in_39636(val);
    const int x = 39636;
    std::cout << 39636;
    for (int i = 0; i < 39636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39636(int& val) {
    const int x = 39636;
    std::cout << 39636;
    for (int i = 0; i < 39636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39637(int& val) {
    func_in_39637(val);
    const int x = 39637;
    std::cout << 39637;
    for (int i = 0; i < 39637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39637(int& val) {
    const int x = 39637;
    std::cout << 39637;
    for (int i = 0; i < 39637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39638(int& val) {
    func_in_39638(val);
    const int x = 39638;
    std::cout << 39638;
    for (int i = 0; i < 39638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39638(int& val) {
    const int x = 39638;
    std::cout << 39638;
    for (int i = 0; i < 39638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39639(int& val) {
    func_in_39639(val);
    const int x = 39639;
    std::cout << 39639;
    for (int i = 0; i < 39639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39639(int& val) {
    const int x = 39639;
    std::cout << 39639;
    for (int i = 0; i < 39639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39640(int& val) {
    func_in_39640(val);
    const int x = 39640;
    std::cout << 39640;
    for (int i = 0; i < 39640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39640(int& val) {
    const int x = 39640;
    std::cout << 39640;
    for (int i = 0; i < 39640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39641(int& val) {
    func_in_39641(val);
    const int x = 39641;
    std::cout << 39641;
    for (int i = 0; i < 39641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39641(int& val) {
    const int x = 39641;
    std::cout << 39641;
    for (int i = 0; i < 39641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39642(int& val) {
    func_in_39642(val);
    const int x = 39642;
    std::cout << 39642;
    for (int i = 0; i < 39642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39642(int& val) {
    const int x = 39642;
    std::cout << 39642;
    for (int i = 0; i < 39642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39643(int& val) {
    func_in_39643(val);
    const int x = 39643;
    std::cout << 39643;
    for (int i = 0; i < 39643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39643(int& val) {
    const int x = 39643;
    std::cout << 39643;
    for (int i = 0; i < 39643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39644(int& val) {
    func_in_39644(val);
    const int x = 39644;
    std::cout << 39644;
    for (int i = 0; i < 39644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39644(int& val) {
    const int x = 39644;
    std::cout << 39644;
    for (int i = 0; i < 39644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39645(int& val) {
    func_in_39645(val);
    const int x = 39645;
    std::cout << 39645;
    for (int i = 0; i < 39645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39645(int& val) {
    const int x = 39645;
    std::cout << 39645;
    for (int i = 0; i < 39645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39646(int& val) {
    func_in_39646(val);
    const int x = 39646;
    std::cout << 39646;
    for (int i = 0; i < 39646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39646(int& val) {
    const int x = 39646;
    std::cout << 39646;
    for (int i = 0; i < 39646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39647(int& val) {
    func_in_39647(val);
    const int x = 39647;
    std::cout << 39647;
    for (int i = 0; i < 39647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39647(int& val) {
    const int x = 39647;
    std::cout << 39647;
    for (int i = 0; i < 39647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39648(int& val) {
    func_in_39648(val);
    const int x = 39648;
    std::cout << 39648;
    for (int i = 0; i < 39648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39648(int& val) {
    const int x = 39648;
    std::cout << 39648;
    for (int i = 0; i < 39648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39649(int& val) {
    func_in_39649(val);
    const int x = 39649;
    std::cout << 39649;
    for (int i = 0; i < 39649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39649(int& val) {
    const int x = 39649;
    std::cout << 39649;
    for (int i = 0; i < 39649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39650(int& val) {
    func_in_39650(val);
    const int x = 39650;
    std::cout << 39650;
    for (int i = 0; i < 39650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39650(int& val) {
    const int x = 39650;
    std::cout << 39650;
    for (int i = 0; i < 39650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39651(int& val) {
    func_in_39651(val);
    const int x = 39651;
    std::cout << 39651;
    for (int i = 0; i < 39651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39651(int& val) {
    const int x = 39651;
    std::cout << 39651;
    for (int i = 0; i < 39651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39652(int& val) {
    func_in_39652(val);
    const int x = 39652;
    std::cout << 39652;
    for (int i = 0; i < 39652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39652(int& val) {
    const int x = 39652;
    std::cout << 39652;
    for (int i = 0; i < 39652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39653(int& val) {
    func_in_39653(val);
    const int x = 39653;
    std::cout << 39653;
    for (int i = 0; i < 39653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39653(int& val) {
    const int x = 39653;
    std::cout << 39653;
    for (int i = 0; i < 39653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39654(int& val) {
    func_in_39654(val);
    const int x = 39654;
    std::cout << 39654;
    for (int i = 0; i < 39654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39654(int& val) {
    const int x = 39654;
    std::cout << 39654;
    for (int i = 0; i < 39654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39655(int& val) {
    func_in_39655(val);
    const int x = 39655;
    std::cout << 39655;
    for (int i = 0; i < 39655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39655(int& val) {
    const int x = 39655;
    std::cout << 39655;
    for (int i = 0; i < 39655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39656(int& val) {
    func_in_39656(val);
    const int x = 39656;
    std::cout << 39656;
    for (int i = 0; i < 39656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39656(int& val) {
    const int x = 39656;
    std::cout << 39656;
    for (int i = 0; i < 39656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39657(int& val) {
    func_in_39657(val);
    const int x = 39657;
    std::cout << 39657;
    for (int i = 0; i < 39657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39657(int& val) {
    const int x = 39657;
    std::cout << 39657;
    for (int i = 0; i < 39657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39658(int& val) {
    func_in_39658(val);
    const int x = 39658;
    std::cout << 39658;
    for (int i = 0; i < 39658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39658(int& val) {
    const int x = 39658;
    std::cout << 39658;
    for (int i = 0; i < 39658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39659(int& val) {
    func_in_39659(val);
    const int x = 39659;
    std::cout << 39659;
    for (int i = 0; i < 39659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39659(int& val) {
    const int x = 39659;
    std::cout << 39659;
    for (int i = 0; i < 39659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39660(int& val) {
    func_in_39660(val);
    const int x = 39660;
    std::cout << 39660;
    for (int i = 0; i < 39660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39660(int& val) {
    const int x = 39660;
    std::cout << 39660;
    for (int i = 0; i < 39660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39661(int& val) {
    func_in_39661(val);
    const int x = 39661;
    std::cout << 39661;
    for (int i = 0; i < 39661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39661(int& val) {
    const int x = 39661;
    std::cout << 39661;
    for (int i = 0; i < 39661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39662(int& val) {
    func_in_39662(val);
    const int x = 39662;
    std::cout << 39662;
    for (int i = 0; i < 39662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39662(int& val) {
    const int x = 39662;
    std::cout << 39662;
    for (int i = 0; i < 39662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39663(int& val) {
    func_in_39663(val);
    const int x = 39663;
    std::cout << 39663;
    for (int i = 0; i < 39663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39663(int& val) {
    const int x = 39663;
    std::cout << 39663;
    for (int i = 0; i < 39663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39664(int& val) {
    func_in_39664(val);
    const int x = 39664;
    std::cout << 39664;
    for (int i = 0; i < 39664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39664(int& val) {
    const int x = 39664;
    std::cout << 39664;
    for (int i = 0; i < 39664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39665(int& val) {
    func_in_39665(val);
    const int x = 39665;
    std::cout << 39665;
    for (int i = 0; i < 39665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39665(int& val) {
    const int x = 39665;
    std::cout << 39665;
    for (int i = 0; i < 39665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39666(int& val) {
    func_in_39666(val);
    const int x = 39666;
    std::cout << 39666;
    for (int i = 0; i < 39666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39666(int& val) {
    const int x = 39666;
    std::cout << 39666;
    for (int i = 0; i < 39666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39667(int& val) {
    func_in_39667(val);
    const int x = 39667;
    std::cout << 39667;
    for (int i = 0; i < 39667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39667(int& val) {
    const int x = 39667;
    std::cout << 39667;
    for (int i = 0; i < 39667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39668(int& val) {
    func_in_39668(val);
    const int x = 39668;
    std::cout << 39668;
    for (int i = 0; i < 39668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39668(int& val) {
    const int x = 39668;
    std::cout << 39668;
    for (int i = 0; i < 39668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39669(int& val) {
    func_in_39669(val);
    const int x = 39669;
    std::cout << 39669;
    for (int i = 0; i < 39669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39669(int& val) {
    const int x = 39669;
    std::cout << 39669;
    for (int i = 0; i < 39669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39670(int& val) {
    func_in_39670(val);
    const int x = 39670;
    std::cout << 39670;
    for (int i = 0; i < 39670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39670(int& val) {
    const int x = 39670;
    std::cout << 39670;
    for (int i = 0; i < 39670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39671(int& val) {
    func_in_39671(val);
    const int x = 39671;
    std::cout << 39671;
    for (int i = 0; i < 39671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39671(int& val) {
    const int x = 39671;
    std::cout << 39671;
    for (int i = 0; i < 39671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39672(int& val) {
    func_in_39672(val);
    const int x = 39672;
    std::cout << 39672;
    for (int i = 0; i < 39672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39672(int& val) {
    const int x = 39672;
    std::cout << 39672;
    for (int i = 0; i < 39672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39673(int& val) {
    func_in_39673(val);
    const int x = 39673;
    std::cout << 39673;
    for (int i = 0; i < 39673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39673(int& val) {
    const int x = 39673;
    std::cout << 39673;
    for (int i = 0; i < 39673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39674(int& val) {
    func_in_39674(val);
    const int x = 39674;
    std::cout << 39674;
    for (int i = 0; i < 39674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39674(int& val) {
    const int x = 39674;
    std::cout << 39674;
    for (int i = 0; i < 39674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39675(int& val) {
    func_in_39675(val);
    const int x = 39675;
    std::cout << 39675;
    for (int i = 0; i < 39675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39675(int& val) {
    const int x = 39675;
    std::cout << 39675;
    for (int i = 0; i < 39675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39676(int& val) {
    func_in_39676(val);
    const int x = 39676;
    std::cout << 39676;
    for (int i = 0; i < 39676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39676(int& val) {
    const int x = 39676;
    std::cout << 39676;
    for (int i = 0; i < 39676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39677(int& val) {
    func_in_39677(val);
    const int x = 39677;
    std::cout << 39677;
    for (int i = 0; i < 39677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39677(int& val) {
    const int x = 39677;
    std::cout << 39677;
    for (int i = 0; i < 39677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39678(int& val) {
    func_in_39678(val);
    const int x = 39678;
    std::cout << 39678;
    for (int i = 0; i < 39678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39678(int& val) {
    const int x = 39678;
    std::cout << 39678;
    for (int i = 0; i < 39678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39679(int& val) {
    func_in_39679(val);
    const int x = 39679;
    std::cout << 39679;
    for (int i = 0; i < 39679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39679(int& val) {
    const int x = 39679;
    std::cout << 39679;
    for (int i = 0; i < 39679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39680(int& val) {
    func_in_39680(val);
    const int x = 39680;
    std::cout << 39680;
    for (int i = 0; i < 39680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39680(int& val) {
    const int x = 39680;
    std::cout << 39680;
    for (int i = 0; i < 39680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39681(int& val) {
    func_in_39681(val);
    const int x = 39681;
    std::cout << 39681;
    for (int i = 0; i < 39681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39681(int& val) {
    const int x = 39681;
    std::cout << 39681;
    for (int i = 0; i < 39681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39682(int& val) {
    func_in_39682(val);
    const int x = 39682;
    std::cout << 39682;
    for (int i = 0; i < 39682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39682(int& val) {
    const int x = 39682;
    std::cout << 39682;
    for (int i = 0; i < 39682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39683(int& val) {
    func_in_39683(val);
    const int x = 39683;
    std::cout << 39683;
    for (int i = 0; i < 39683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39683(int& val) {
    const int x = 39683;
    std::cout << 39683;
    for (int i = 0; i < 39683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39684(int& val) {
    func_in_39684(val);
    const int x = 39684;
    std::cout << 39684;
    for (int i = 0; i < 39684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39684(int& val) {
    const int x = 39684;
    std::cout << 39684;
    for (int i = 0; i < 39684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39685(int& val) {
    func_in_39685(val);
    const int x = 39685;
    std::cout << 39685;
    for (int i = 0; i < 39685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39685(int& val) {
    const int x = 39685;
    std::cout << 39685;
    for (int i = 0; i < 39685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39686(int& val) {
    func_in_39686(val);
    const int x = 39686;
    std::cout << 39686;
    for (int i = 0; i < 39686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39686(int& val) {
    const int x = 39686;
    std::cout << 39686;
    for (int i = 0; i < 39686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39687(int& val) {
    func_in_39687(val);
    const int x = 39687;
    std::cout << 39687;
    for (int i = 0; i < 39687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39687(int& val) {
    const int x = 39687;
    std::cout << 39687;
    for (int i = 0; i < 39687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39688(int& val) {
    func_in_39688(val);
    const int x = 39688;
    std::cout << 39688;
    for (int i = 0; i < 39688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39688(int& val) {
    const int x = 39688;
    std::cout << 39688;
    for (int i = 0; i < 39688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39689(int& val) {
    func_in_39689(val);
    const int x = 39689;
    std::cout << 39689;
    for (int i = 0; i < 39689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39689(int& val) {
    const int x = 39689;
    std::cout << 39689;
    for (int i = 0; i < 39689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39690(int& val) {
    func_in_39690(val);
    const int x = 39690;
    std::cout << 39690;
    for (int i = 0; i < 39690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39690(int& val) {
    const int x = 39690;
    std::cout << 39690;
    for (int i = 0; i < 39690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39691(int& val) {
    func_in_39691(val);
    const int x = 39691;
    std::cout << 39691;
    for (int i = 0; i < 39691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39691(int& val) {
    const int x = 39691;
    std::cout << 39691;
    for (int i = 0; i < 39691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39692(int& val) {
    func_in_39692(val);
    const int x = 39692;
    std::cout << 39692;
    for (int i = 0; i < 39692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39692(int& val) {
    const int x = 39692;
    std::cout << 39692;
    for (int i = 0; i < 39692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39693(int& val) {
    func_in_39693(val);
    const int x = 39693;
    std::cout << 39693;
    for (int i = 0; i < 39693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39693(int& val) {
    const int x = 39693;
    std::cout << 39693;
    for (int i = 0; i < 39693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39694(int& val) {
    func_in_39694(val);
    const int x = 39694;
    std::cout << 39694;
    for (int i = 0; i < 39694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39694(int& val) {
    const int x = 39694;
    std::cout << 39694;
    for (int i = 0; i < 39694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39695(int& val) {
    func_in_39695(val);
    const int x = 39695;
    std::cout << 39695;
    for (int i = 0; i < 39695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39695(int& val) {
    const int x = 39695;
    std::cout << 39695;
    for (int i = 0; i < 39695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39696(int& val) {
    func_in_39696(val);
    const int x = 39696;
    std::cout << 39696;
    for (int i = 0; i < 39696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39696(int& val) {
    const int x = 39696;
    std::cout << 39696;
    for (int i = 0; i < 39696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39697(int& val) {
    func_in_39697(val);
    const int x = 39697;
    std::cout << 39697;
    for (int i = 0; i < 39697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39697(int& val) {
    const int x = 39697;
    std::cout << 39697;
    for (int i = 0; i < 39697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39698(int& val) {
    func_in_39698(val);
    const int x = 39698;
    std::cout << 39698;
    for (int i = 0; i < 39698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39698(int& val) {
    const int x = 39698;
    std::cout << 39698;
    for (int i = 0; i < 39698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39699(int& val) {
    func_in_39699(val);
    const int x = 39699;
    std::cout << 39699;
    for (int i = 0; i < 39699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39699(int& val) {
    const int x = 39699;
    std::cout << 39699;
    for (int i = 0; i < 39699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39700(int& val) {
    func_in_39700(val);
    const int x = 39700;
    std::cout << 39700;
    for (int i = 0; i < 39700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39700(int& val) {
    const int x = 39700;
    std::cout << 39700;
    for (int i = 0; i < 39700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39701(int& val) {
    func_in_39701(val);
    const int x = 39701;
    std::cout << 39701;
    for (int i = 0; i < 39701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39701(int& val) {
    const int x = 39701;
    std::cout << 39701;
    for (int i = 0; i < 39701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39702(int& val) {
    func_in_39702(val);
    const int x = 39702;
    std::cout << 39702;
    for (int i = 0; i < 39702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39702(int& val) {
    const int x = 39702;
    std::cout << 39702;
    for (int i = 0; i < 39702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39703(int& val) {
    func_in_39703(val);
    const int x = 39703;
    std::cout << 39703;
    for (int i = 0; i < 39703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39703(int& val) {
    const int x = 39703;
    std::cout << 39703;
    for (int i = 0; i < 39703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39704(int& val) {
    func_in_39704(val);
    const int x = 39704;
    std::cout << 39704;
    for (int i = 0; i < 39704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39704(int& val) {
    const int x = 39704;
    std::cout << 39704;
    for (int i = 0; i < 39704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39705(int& val) {
    func_in_39705(val);
    const int x = 39705;
    std::cout << 39705;
    for (int i = 0; i < 39705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39705(int& val) {
    const int x = 39705;
    std::cout << 39705;
    for (int i = 0; i < 39705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39706(int& val) {
    func_in_39706(val);
    const int x = 39706;
    std::cout << 39706;
    for (int i = 0; i < 39706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39706(int& val) {
    const int x = 39706;
    std::cout << 39706;
    for (int i = 0; i < 39706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39707(int& val) {
    func_in_39707(val);
    const int x = 39707;
    std::cout << 39707;
    for (int i = 0; i < 39707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39707(int& val) {
    const int x = 39707;
    std::cout << 39707;
    for (int i = 0; i < 39707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39708(int& val) {
    func_in_39708(val);
    const int x = 39708;
    std::cout << 39708;
    for (int i = 0; i < 39708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39708(int& val) {
    const int x = 39708;
    std::cout << 39708;
    for (int i = 0; i < 39708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39709(int& val) {
    func_in_39709(val);
    const int x = 39709;
    std::cout << 39709;
    for (int i = 0; i < 39709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39709(int& val) {
    const int x = 39709;
    std::cout << 39709;
    for (int i = 0; i < 39709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39710(int& val) {
    func_in_39710(val);
    const int x = 39710;
    std::cout << 39710;
    for (int i = 0; i < 39710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39710(int& val) {
    const int x = 39710;
    std::cout << 39710;
    for (int i = 0; i < 39710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39711(int& val) {
    func_in_39711(val);
    const int x = 39711;
    std::cout << 39711;
    for (int i = 0; i < 39711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39711(int& val) {
    const int x = 39711;
    std::cout << 39711;
    for (int i = 0; i < 39711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39712(int& val) {
    func_in_39712(val);
    const int x = 39712;
    std::cout << 39712;
    for (int i = 0; i < 39712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39712(int& val) {
    const int x = 39712;
    std::cout << 39712;
    for (int i = 0; i < 39712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39713(int& val) {
    func_in_39713(val);
    const int x = 39713;
    std::cout << 39713;
    for (int i = 0; i < 39713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39713(int& val) {
    const int x = 39713;
    std::cout << 39713;
    for (int i = 0; i < 39713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39714(int& val) {
    func_in_39714(val);
    const int x = 39714;
    std::cout << 39714;
    for (int i = 0; i < 39714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39714(int& val) {
    const int x = 39714;
    std::cout << 39714;
    for (int i = 0; i < 39714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39715(int& val) {
    func_in_39715(val);
    const int x = 39715;
    std::cout << 39715;
    for (int i = 0; i < 39715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39715(int& val) {
    const int x = 39715;
    std::cout << 39715;
    for (int i = 0; i < 39715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39716(int& val) {
    func_in_39716(val);
    const int x = 39716;
    std::cout << 39716;
    for (int i = 0; i < 39716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39716(int& val) {
    const int x = 39716;
    std::cout << 39716;
    for (int i = 0; i < 39716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39717(int& val) {
    func_in_39717(val);
    const int x = 39717;
    std::cout << 39717;
    for (int i = 0; i < 39717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39717(int& val) {
    const int x = 39717;
    std::cout << 39717;
    for (int i = 0; i < 39717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39718(int& val) {
    func_in_39718(val);
    const int x = 39718;
    std::cout << 39718;
    for (int i = 0; i < 39718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39718(int& val) {
    const int x = 39718;
    std::cout << 39718;
    for (int i = 0; i < 39718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39719(int& val) {
    func_in_39719(val);
    const int x = 39719;
    std::cout << 39719;
    for (int i = 0; i < 39719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39719(int& val) {
    const int x = 39719;
    std::cout << 39719;
    for (int i = 0; i < 39719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39720(int& val) {
    func_in_39720(val);
    const int x = 39720;
    std::cout << 39720;
    for (int i = 0; i < 39720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39720(int& val) {
    const int x = 39720;
    std::cout << 39720;
    for (int i = 0; i < 39720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39721(int& val) {
    func_in_39721(val);
    const int x = 39721;
    std::cout << 39721;
    for (int i = 0; i < 39721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39721(int& val) {
    const int x = 39721;
    std::cout << 39721;
    for (int i = 0; i < 39721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39722(int& val) {
    func_in_39722(val);
    const int x = 39722;
    std::cout << 39722;
    for (int i = 0; i < 39722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39722(int& val) {
    const int x = 39722;
    std::cout << 39722;
    for (int i = 0; i < 39722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39723(int& val) {
    func_in_39723(val);
    const int x = 39723;
    std::cout << 39723;
    for (int i = 0; i < 39723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39723(int& val) {
    const int x = 39723;
    std::cout << 39723;
    for (int i = 0; i < 39723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39724(int& val) {
    func_in_39724(val);
    const int x = 39724;
    std::cout << 39724;
    for (int i = 0; i < 39724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39724(int& val) {
    const int x = 39724;
    std::cout << 39724;
    for (int i = 0; i < 39724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39725(int& val) {
    func_in_39725(val);
    const int x = 39725;
    std::cout << 39725;
    for (int i = 0; i < 39725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39725(int& val) {
    const int x = 39725;
    std::cout << 39725;
    for (int i = 0; i < 39725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39726(int& val) {
    func_in_39726(val);
    const int x = 39726;
    std::cout << 39726;
    for (int i = 0; i < 39726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39726(int& val) {
    const int x = 39726;
    std::cout << 39726;
    for (int i = 0; i < 39726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39727(int& val) {
    func_in_39727(val);
    const int x = 39727;
    std::cout << 39727;
    for (int i = 0; i < 39727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39727(int& val) {
    const int x = 39727;
    std::cout << 39727;
    for (int i = 0; i < 39727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39728(int& val) {
    func_in_39728(val);
    const int x = 39728;
    std::cout << 39728;
    for (int i = 0; i < 39728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39728(int& val) {
    const int x = 39728;
    std::cout << 39728;
    for (int i = 0; i < 39728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39729(int& val) {
    func_in_39729(val);
    const int x = 39729;
    std::cout << 39729;
    for (int i = 0; i < 39729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39729(int& val) {
    const int x = 39729;
    std::cout << 39729;
    for (int i = 0; i < 39729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39730(int& val) {
    func_in_39730(val);
    const int x = 39730;
    std::cout << 39730;
    for (int i = 0; i < 39730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39730(int& val) {
    const int x = 39730;
    std::cout << 39730;
    for (int i = 0; i < 39730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39731(int& val) {
    func_in_39731(val);
    const int x = 39731;
    std::cout << 39731;
    for (int i = 0; i < 39731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39731(int& val) {
    const int x = 39731;
    std::cout << 39731;
    for (int i = 0; i < 39731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39732(int& val) {
    func_in_39732(val);
    const int x = 39732;
    std::cout << 39732;
    for (int i = 0; i < 39732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39732(int& val) {
    const int x = 39732;
    std::cout << 39732;
    for (int i = 0; i < 39732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39733(int& val) {
    func_in_39733(val);
    const int x = 39733;
    std::cout << 39733;
    for (int i = 0; i < 39733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39733(int& val) {
    const int x = 39733;
    std::cout << 39733;
    for (int i = 0; i < 39733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39734(int& val) {
    func_in_39734(val);
    const int x = 39734;
    std::cout << 39734;
    for (int i = 0; i < 39734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39734(int& val) {
    const int x = 39734;
    std::cout << 39734;
    for (int i = 0; i < 39734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39735(int& val) {
    func_in_39735(val);
    const int x = 39735;
    std::cout << 39735;
    for (int i = 0; i < 39735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39735(int& val) {
    const int x = 39735;
    std::cout << 39735;
    for (int i = 0; i < 39735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39736(int& val) {
    func_in_39736(val);
    const int x = 39736;
    std::cout << 39736;
    for (int i = 0; i < 39736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39736(int& val) {
    const int x = 39736;
    std::cout << 39736;
    for (int i = 0; i < 39736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39737(int& val) {
    func_in_39737(val);
    const int x = 39737;
    std::cout << 39737;
    for (int i = 0; i < 39737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39737(int& val) {
    const int x = 39737;
    std::cout << 39737;
    for (int i = 0; i < 39737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39738(int& val) {
    func_in_39738(val);
    const int x = 39738;
    std::cout << 39738;
    for (int i = 0; i < 39738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39738(int& val) {
    const int x = 39738;
    std::cout << 39738;
    for (int i = 0; i < 39738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39739(int& val) {
    func_in_39739(val);
    const int x = 39739;
    std::cout << 39739;
    for (int i = 0; i < 39739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39739(int& val) {
    const int x = 39739;
    std::cout << 39739;
    for (int i = 0; i < 39739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39740(int& val) {
    func_in_39740(val);
    const int x = 39740;
    std::cout << 39740;
    for (int i = 0; i < 39740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39740(int& val) {
    const int x = 39740;
    std::cout << 39740;
    for (int i = 0; i < 39740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39741(int& val) {
    func_in_39741(val);
    const int x = 39741;
    std::cout << 39741;
    for (int i = 0; i < 39741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39741(int& val) {
    const int x = 39741;
    std::cout << 39741;
    for (int i = 0; i < 39741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39742(int& val) {
    func_in_39742(val);
    const int x = 39742;
    std::cout << 39742;
    for (int i = 0; i < 39742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39742(int& val) {
    const int x = 39742;
    std::cout << 39742;
    for (int i = 0; i < 39742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39743(int& val) {
    func_in_39743(val);
    const int x = 39743;
    std::cout << 39743;
    for (int i = 0; i < 39743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39743(int& val) {
    const int x = 39743;
    std::cout << 39743;
    for (int i = 0; i < 39743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39744(int& val) {
    func_in_39744(val);
    const int x = 39744;
    std::cout << 39744;
    for (int i = 0; i < 39744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39744(int& val) {
    const int x = 39744;
    std::cout << 39744;
    for (int i = 0; i < 39744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39745(int& val) {
    func_in_39745(val);
    const int x = 39745;
    std::cout << 39745;
    for (int i = 0; i < 39745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39745(int& val) {
    const int x = 39745;
    std::cout << 39745;
    for (int i = 0; i < 39745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39746(int& val) {
    func_in_39746(val);
    const int x = 39746;
    std::cout << 39746;
    for (int i = 0; i < 39746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39746(int& val) {
    const int x = 39746;
    std::cout << 39746;
    for (int i = 0; i < 39746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39747(int& val) {
    func_in_39747(val);
    const int x = 39747;
    std::cout << 39747;
    for (int i = 0; i < 39747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39747(int& val) {
    const int x = 39747;
    std::cout << 39747;
    for (int i = 0; i < 39747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39748(int& val) {
    func_in_39748(val);
    const int x = 39748;
    std::cout << 39748;
    for (int i = 0; i < 39748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39748(int& val) {
    const int x = 39748;
    std::cout << 39748;
    for (int i = 0; i < 39748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39749(int& val) {
    func_in_39749(val);
    const int x = 39749;
    std::cout << 39749;
    for (int i = 0; i < 39749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39749(int& val) {
    const int x = 39749;
    std::cout << 39749;
    for (int i = 0; i < 39749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39750(int& val) {
    func_in_39750(val);
    const int x = 39750;
    std::cout << 39750;
    for (int i = 0; i < 39750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39750(int& val) {
    const int x = 39750;
    std::cout << 39750;
    for (int i = 0; i < 39750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39751(int& val) {
    func_in_39751(val);
    const int x = 39751;
    std::cout << 39751;
    for (int i = 0; i < 39751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39751(int& val) {
    const int x = 39751;
    std::cout << 39751;
    for (int i = 0; i < 39751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39752(int& val) {
    func_in_39752(val);
    const int x = 39752;
    std::cout << 39752;
    for (int i = 0; i < 39752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39752(int& val) {
    const int x = 39752;
    std::cout << 39752;
    for (int i = 0; i < 39752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39753(int& val) {
    func_in_39753(val);
    const int x = 39753;
    std::cout << 39753;
    for (int i = 0; i < 39753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39753(int& val) {
    const int x = 39753;
    std::cout << 39753;
    for (int i = 0; i < 39753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39754(int& val) {
    func_in_39754(val);
    const int x = 39754;
    std::cout << 39754;
    for (int i = 0; i < 39754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39754(int& val) {
    const int x = 39754;
    std::cout << 39754;
    for (int i = 0; i < 39754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39755(int& val) {
    func_in_39755(val);
    const int x = 39755;
    std::cout << 39755;
    for (int i = 0; i < 39755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39755(int& val) {
    const int x = 39755;
    std::cout << 39755;
    for (int i = 0; i < 39755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39756(int& val) {
    func_in_39756(val);
    const int x = 39756;
    std::cout << 39756;
    for (int i = 0; i < 39756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39756(int& val) {
    const int x = 39756;
    std::cout << 39756;
    for (int i = 0; i < 39756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39757(int& val) {
    func_in_39757(val);
    const int x = 39757;
    std::cout << 39757;
    for (int i = 0; i < 39757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39757(int& val) {
    const int x = 39757;
    std::cout << 39757;
    for (int i = 0; i < 39757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39758(int& val) {
    func_in_39758(val);
    const int x = 39758;
    std::cout << 39758;
    for (int i = 0; i < 39758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39758(int& val) {
    const int x = 39758;
    std::cout << 39758;
    for (int i = 0; i < 39758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39759(int& val) {
    func_in_39759(val);
    const int x = 39759;
    std::cout << 39759;
    for (int i = 0; i < 39759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39759(int& val) {
    const int x = 39759;
    std::cout << 39759;
    for (int i = 0; i < 39759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39760(int& val) {
    func_in_39760(val);
    const int x = 39760;
    std::cout << 39760;
    for (int i = 0; i < 39760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39760(int& val) {
    const int x = 39760;
    std::cout << 39760;
    for (int i = 0; i < 39760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39761(int& val) {
    func_in_39761(val);
    const int x = 39761;
    std::cout << 39761;
    for (int i = 0; i < 39761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39761(int& val) {
    const int x = 39761;
    std::cout << 39761;
    for (int i = 0; i < 39761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39762(int& val) {
    func_in_39762(val);
    const int x = 39762;
    std::cout << 39762;
    for (int i = 0; i < 39762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39762(int& val) {
    const int x = 39762;
    std::cout << 39762;
    for (int i = 0; i < 39762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39763(int& val) {
    func_in_39763(val);
    const int x = 39763;
    std::cout << 39763;
    for (int i = 0; i < 39763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39763(int& val) {
    const int x = 39763;
    std::cout << 39763;
    for (int i = 0; i < 39763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39764(int& val) {
    func_in_39764(val);
    const int x = 39764;
    std::cout << 39764;
    for (int i = 0; i < 39764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39764(int& val) {
    const int x = 39764;
    std::cout << 39764;
    for (int i = 0; i < 39764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39765(int& val) {
    func_in_39765(val);
    const int x = 39765;
    std::cout << 39765;
    for (int i = 0; i < 39765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39765(int& val) {
    const int x = 39765;
    std::cout << 39765;
    for (int i = 0; i < 39765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39766(int& val) {
    func_in_39766(val);
    const int x = 39766;
    std::cout << 39766;
    for (int i = 0; i < 39766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39766(int& val) {
    const int x = 39766;
    std::cout << 39766;
    for (int i = 0; i < 39766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39767(int& val) {
    func_in_39767(val);
    const int x = 39767;
    std::cout << 39767;
    for (int i = 0; i < 39767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39767(int& val) {
    const int x = 39767;
    std::cout << 39767;
    for (int i = 0; i < 39767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39768(int& val) {
    func_in_39768(val);
    const int x = 39768;
    std::cout << 39768;
    for (int i = 0; i < 39768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39768(int& val) {
    const int x = 39768;
    std::cout << 39768;
    for (int i = 0; i < 39768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39769(int& val) {
    func_in_39769(val);
    const int x = 39769;
    std::cout << 39769;
    for (int i = 0; i < 39769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39769(int& val) {
    const int x = 39769;
    std::cout << 39769;
    for (int i = 0; i < 39769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39770(int& val) {
    func_in_39770(val);
    const int x = 39770;
    std::cout << 39770;
    for (int i = 0; i < 39770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39770(int& val) {
    const int x = 39770;
    std::cout << 39770;
    for (int i = 0; i < 39770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39771(int& val) {
    func_in_39771(val);
    const int x = 39771;
    std::cout << 39771;
    for (int i = 0; i < 39771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39771(int& val) {
    const int x = 39771;
    std::cout << 39771;
    for (int i = 0; i < 39771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39772(int& val) {
    func_in_39772(val);
    const int x = 39772;
    std::cout << 39772;
    for (int i = 0; i < 39772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39772(int& val) {
    const int x = 39772;
    std::cout << 39772;
    for (int i = 0; i < 39772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39773(int& val) {
    func_in_39773(val);
    const int x = 39773;
    std::cout << 39773;
    for (int i = 0; i < 39773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39773(int& val) {
    const int x = 39773;
    std::cout << 39773;
    for (int i = 0; i < 39773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39774(int& val) {
    func_in_39774(val);
    const int x = 39774;
    std::cout << 39774;
    for (int i = 0; i < 39774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39774(int& val) {
    const int x = 39774;
    std::cout << 39774;
    for (int i = 0; i < 39774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39775(int& val) {
    func_in_39775(val);
    const int x = 39775;
    std::cout << 39775;
    for (int i = 0; i < 39775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39775(int& val) {
    const int x = 39775;
    std::cout << 39775;
    for (int i = 0; i < 39775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39776(int& val) {
    func_in_39776(val);
    const int x = 39776;
    std::cout << 39776;
    for (int i = 0; i < 39776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39776(int& val) {
    const int x = 39776;
    std::cout << 39776;
    for (int i = 0; i < 39776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39777(int& val) {
    func_in_39777(val);
    const int x = 39777;
    std::cout << 39777;
    for (int i = 0; i < 39777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39777(int& val) {
    const int x = 39777;
    std::cout << 39777;
    for (int i = 0; i < 39777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39778(int& val) {
    func_in_39778(val);
    const int x = 39778;
    std::cout << 39778;
    for (int i = 0; i < 39778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39778(int& val) {
    const int x = 39778;
    std::cout << 39778;
    for (int i = 0; i < 39778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39779(int& val) {
    func_in_39779(val);
    const int x = 39779;
    std::cout << 39779;
    for (int i = 0; i < 39779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39779(int& val) {
    const int x = 39779;
    std::cout << 39779;
    for (int i = 0; i < 39779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39780(int& val) {
    func_in_39780(val);
    const int x = 39780;
    std::cout << 39780;
    for (int i = 0; i < 39780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39780(int& val) {
    const int x = 39780;
    std::cout << 39780;
    for (int i = 0; i < 39780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39781(int& val) {
    func_in_39781(val);
    const int x = 39781;
    std::cout << 39781;
    for (int i = 0; i < 39781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39781(int& val) {
    const int x = 39781;
    std::cout << 39781;
    for (int i = 0; i < 39781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39782(int& val) {
    func_in_39782(val);
    const int x = 39782;
    std::cout << 39782;
    for (int i = 0; i < 39782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39782(int& val) {
    const int x = 39782;
    std::cout << 39782;
    for (int i = 0; i < 39782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39783(int& val) {
    func_in_39783(val);
    const int x = 39783;
    std::cout << 39783;
    for (int i = 0; i < 39783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39783(int& val) {
    const int x = 39783;
    std::cout << 39783;
    for (int i = 0; i < 39783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39784(int& val) {
    func_in_39784(val);
    const int x = 39784;
    std::cout << 39784;
    for (int i = 0; i < 39784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39784(int& val) {
    const int x = 39784;
    std::cout << 39784;
    for (int i = 0; i < 39784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39785(int& val) {
    func_in_39785(val);
    const int x = 39785;
    std::cout << 39785;
    for (int i = 0; i < 39785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39785(int& val) {
    const int x = 39785;
    std::cout << 39785;
    for (int i = 0; i < 39785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39786(int& val) {
    func_in_39786(val);
    const int x = 39786;
    std::cout << 39786;
    for (int i = 0; i < 39786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39786(int& val) {
    const int x = 39786;
    std::cout << 39786;
    for (int i = 0; i < 39786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39787(int& val) {
    func_in_39787(val);
    const int x = 39787;
    std::cout << 39787;
    for (int i = 0; i < 39787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39787(int& val) {
    const int x = 39787;
    std::cout << 39787;
    for (int i = 0; i < 39787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39788(int& val) {
    func_in_39788(val);
    const int x = 39788;
    std::cout << 39788;
    for (int i = 0; i < 39788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39788(int& val) {
    const int x = 39788;
    std::cout << 39788;
    for (int i = 0; i < 39788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39789(int& val) {
    func_in_39789(val);
    const int x = 39789;
    std::cout << 39789;
    for (int i = 0; i < 39789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39789(int& val) {
    const int x = 39789;
    std::cout << 39789;
    for (int i = 0; i < 39789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39790(int& val) {
    func_in_39790(val);
    const int x = 39790;
    std::cout << 39790;
    for (int i = 0; i < 39790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39790(int& val) {
    const int x = 39790;
    std::cout << 39790;
    for (int i = 0; i < 39790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39791(int& val) {
    func_in_39791(val);
    const int x = 39791;
    std::cout << 39791;
    for (int i = 0; i < 39791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39791(int& val) {
    const int x = 39791;
    std::cout << 39791;
    for (int i = 0; i < 39791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39792(int& val) {
    func_in_39792(val);
    const int x = 39792;
    std::cout << 39792;
    for (int i = 0; i < 39792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39792(int& val) {
    const int x = 39792;
    std::cout << 39792;
    for (int i = 0; i < 39792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39793(int& val) {
    func_in_39793(val);
    const int x = 39793;
    std::cout << 39793;
    for (int i = 0; i < 39793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39793(int& val) {
    const int x = 39793;
    std::cout << 39793;
    for (int i = 0; i < 39793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39794(int& val) {
    func_in_39794(val);
    const int x = 39794;
    std::cout << 39794;
    for (int i = 0; i < 39794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39794(int& val) {
    const int x = 39794;
    std::cout << 39794;
    for (int i = 0; i < 39794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39795(int& val) {
    func_in_39795(val);
    const int x = 39795;
    std::cout << 39795;
    for (int i = 0; i < 39795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39795(int& val) {
    const int x = 39795;
    std::cout << 39795;
    for (int i = 0; i < 39795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39796(int& val) {
    func_in_39796(val);
    const int x = 39796;
    std::cout << 39796;
    for (int i = 0; i < 39796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39796(int& val) {
    const int x = 39796;
    std::cout << 39796;
    for (int i = 0; i < 39796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39797(int& val) {
    func_in_39797(val);
    const int x = 39797;
    std::cout << 39797;
    for (int i = 0; i < 39797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39797(int& val) {
    const int x = 39797;
    std::cout << 39797;
    for (int i = 0; i < 39797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39798(int& val) {
    func_in_39798(val);
    const int x = 39798;
    std::cout << 39798;
    for (int i = 0; i < 39798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39798(int& val) {
    const int x = 39798;
    std::cout << 39798;
    for (int i = 0; i < 39798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39799(int& val) {
    func_in_39799(val);
    const int x = 39799;
    std::cout << 39799;
    for (int i = 0; i < 39799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39799(int& val) {
    const int x = 39799;
    std::cout << 39799;
    for (int i = 0; i < 39799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39800(int& val) {
    func_in_39800(val);
    const int x = 39800;
    std::cout << 39800;
    for (int i = 0; i < 39800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39800(int& val) {
    const int x = 39800;
    std::cout << 39800;
    for (int i = 0; i < 39800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39801(int& val) {
    func_in_39801(val);
    const int x = 39801;
    std::cout << 39801;
    for (int i = 0; i < 39801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39801(int& val) {
    const int x = 39801;
    std::cout << 39801;
    for (int i = 0; i < 39801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39802(int& val) {
    func_in_39802(val);
    const int x = 39802;
    std::cout << 39802;
    for (int i = 0; i < 39802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39802(int& val) {
    const int x = 39802;
    std::cout << 39802;
    for (int i = 0; i < 39802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39803(int& val) {
    func_in_39803(val);
    const int x = 39803;
    std::cout << 39803;
    for (int i = 0; i < 39803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39803(int& val) {
    const int x = 39803;
    std::cout << 39803;
    for (int i = 0; i < 39803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39804(int& val) {
    func_in_39804(val);
    const int x = 39804;
    std::cout << 39804;
    for (int i = 0; i < 39804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39804(int& val) {
    const int x = 39804;
    std::cout << 39804;
    for (int i = 0; i < 39804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39805(int& val) {
    func_in_39805(val);
    const int x = 39805;
    std::cout << 39805;
    for (int i = 0; i < 39805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39805(int& val) {
    const int x = 39805;
    std::cout << 39805;
    for (int i = 0; i < 39805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39806(int& val) {
    func_in_39806(val);
    const int x = 39806;
    std::cout << 39806;
    for (int i = 0; i < 39806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39806(int& val) {
    const int x = 39806;
    std::cout << 39806;
    for (int i = 0; i < 39806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39807(int& val) {
    func_in_39807(val);
    const int x = 39807;
    std::cout << 39807;
    for (int i = 0; i < 39807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39807(int& val) {
    const int x = 39807;
    std::cout << 39807;
    for (int i = 0; i < 39807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39808(int& val) {
    func_in_39808(val);
    const int x = 39808;
    std::cout << 39808;
    for (int i = 0; i < 39808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39808(int& val) {
    const int x = 39808;
    std::cout << 39808;
    for (int i = 0; i < 39808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39809(int& val) {
    func_in_39809(val);
    const int x = 39809;
    std::cout << 39809;
    for (int i = 0; i < 39809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39809(int& val) {
    const int x = 39809;
    std::cout << 39809;
    for (int i = 0; i < 39809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39810(int& val) {
    func_in_39810(val);
    const int x = 39810;
    std::cout << 39810;
    for (int i = 0; i < 39810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39810(int& val) {
    const int x = 39810;
    std::cout << 39810;
    for (int i = 0; i < 39810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39811(int& val) {
    func_in_39811(val);
    const int x = 39811;
    std::cout << 39811;
    for (int i = 0; i < 39811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39811(int& val) {
    const int x = 39811;
    std::cout << 39811;
    for (int i = 0; i < 39811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39812(int& val) {
    func_in_39812(val);
    const int x = 39812;
    std::cout << 39812;
    for (int i = 0; i < 39812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39812(int& val) {
    const int x = 39812;
    std::cout << 39812;
    for (int i = 0; i < 39812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39813(int& val) {
    func_in_39813(val);
    const int x = 39813;
    std::cout << 39813;
    for (int i = 0; i < 39813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39813(int& val) {
    const int x = 39813;
    std::cout << 39813;
    for (int i = 0; i < 39813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39814(int& val) {
    func_in_39814(val);
    const int x = 39814;
    std::cout << 39814;
    for (int i = 0; i < 39814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39814(int& val) {
    const int x = 39814;
    std::cout << 39814;
    for (int i = 0; i < 39814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39815(int& val) {
    func_in_39815(val);
    const int x = 39815;
    std::cout << 39815;
    for (int i = 0; i < 39815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39815(int& val) {
    const int x = 39815;
    std::cout << 39815;
    for (int i = 0; i < 39815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39816(int& val) {
    func_in_39816(val);
    const int x = 39816;
    std::cout << 39816;
    for (int i = 0; i < 39816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39816(int& val) {
    const int x = 39816;
    std::cout << 39816;
    for (int i = 0; i < 39816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39817(int& val) {
    func_in_39817(val);
    const int x = 39817;
    std::cout << 39817;
    for (int i = 0; i < 39817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39817(int& val) {
    const int x = 39817;
    std::cout << 39817;
    for (int i = 0; i < 39817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39818(int& val) {
    func_in_39818(val);
    const int x = 39818;
    std::cout << 39818;
    for (int i = 0; i < 39818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39818(int& val) {
    const int x = 39818;
    std::cout << 39818;
    for (int i = 0; i < 39818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39819(int& val) {
    func_in_39819(val);
    const int x = 39819;
    std::cout << 39819;
    for (int i = 0; i < 39819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39819(int& val) {
    const int x = 39819;
    std::cout << 39819;
    for (int i = 0; i < 39819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39820(int& val) {
    func_in_39820(val);
    const int x = 39820;
    std::cout << 39820;
    for (int i = 0; i < 39820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39820(int& val) {
    const int x = 39820;
    std::cout << 39820;
    for (int i = 0; i < 39820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39821(int& val) {
    func_in_39821(val);
    const int x = 39821;
    std::cout << 39821;
    for (int i = 0; i < 39821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39821(int& val) {
    const int x = 39821;
    std::cout << 39821;
    for (int i = 0; i < 39821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39822(int& val) {
    func_in_39822(val);
    const int x = 39822;
    std::cout << 39822;
    for (int i = 0; i < 39822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39822(int& val) {
    const int x = 39822;
    std::cout << 39822;
    for (int i = 0; i < 39822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39823(int& val) {
    func_in_39823(val);
    const int x = 39823;
    std::cout << 39823;
    for (int i = 0; i < 39823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39823(int& val) {
    const int x = 39823;
    std::cout << 39823;
    for (int i = 0; i < 39823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39824(int& val) {
    func_in_39824(val);
    const int x = 39824;
    std::cout << 39824;
    for (int i = 0; i < 39824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39824(int& val) {
    const int x = 39824;
    std::cout << 39824;
    for (int i = 0; i < 39824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39825(int& val) {
    func_in_39825(val);
    const int x = 39825;
    std::cout << 39825;
    for (int i = 0; i < 39825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39825(int& val) {
    const int x = 39825;
    std::cout << 39825;
    for (int i = 0; i < 39825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39826(int& val) {
    func_in_39826(val);
    const int x = 39826;
    std::cout << 39826;
    for (int i = 0; i < 39826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39826(int& val) {
    const int x = 39826;
    std::cout << 39826;
    for (int i = 0; i < 39826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39827(int& val) {
    func_in_39827(val);
    const int x = 39827;
    std::cout << 39827;
    for (int i = 0; i < 39827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39827(int& val) {
    const int x = 39827;
    std::cout << 39827;
    for (int i = 0; i < 39827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39828(int& val) {
    func_in_39828(val);
    const int x = 39828;
    std::cout << 39828;
    for (int i = 0; i < 39828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39828(int& val) {
    const int x = 39828;
    std::cout << 39828;
    for (int i = 0; i < 39828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39829(int& val) {
    func_in_39829(val);
    const int x = 39829;
    std::cout << 39829;
    for (int i = 0; i < 39829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39829(int& val) {
    const int x = 39829;
    std::cout << 39829;
    for (int i = 0; i < 39829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39830(int& val) {
    func_in_39830(val);
    const int x = 39830;
    std::cout << 39830;
    for (int i = 0; i < 39830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39830(int& val) {
    const int x = 39830;
    std::cout << 39830;
    for (int i = 0; i < 39830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39831(int& val) {
    func_in_39831(val);
    const int x = 39831;
    std::cout << 39831;
    for (int i = 0; i < 39831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39831(int& val) {
    const int x = 39831;
    std::cout << 39831;
    for (int i = 0; i < 39831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39832(int& val) {
    func_in_39832(val);
    const int x = 39832;
    std::cout << 39832;
    for (int i = 0; i < 39832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39832(int& val) {
    const int x = 39832;
    std::cout << 39832;
    for (int i = 0; i < 39832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39833(int& val) {
    func_in_39833(val);
    const int x = 39833;
    std::cout << 39833;
    for (int i = 0; i < 39833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39833(int& val) {
    const int x = 39833;
    std::cout << 39833;
    for (int i = 0; i < 39833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39834(int& val) {
    func_in_39834(val);
    const int x = 39834;
    std::cout << 39834;
    for (int i = 0; i < 39834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39834(int& val) {
    const int x = 39834;
    std::cout << 39834;
    for (int i = 0; i < 39834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39835(int& val) {
    func_in_39835(val);
    const int x = 39835;
    std::cout << 39835;
    for (int i = 0; i < 39835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39835(int& val) {
    const int x = 39835;
    std::cout << 39835;
    for (int i = 0; i < 39835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39836(int& val) {
    func_in_39836(val);
    const int x = 39836;
    std::cout << 39836;
    for (int i = 0; i < 39836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39836(int& val) {
    const int x = 39836;
    std::cout << 39836;
    for (int i = 0; i < 39836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39837(int& val) {
    func_in_39837(val);
    const int x = 39837;
    std::cout << 39837;
    for (int i = 0; i < 39837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39837(int& val) {
    const int x = 39837;
    std::cout << 39837;
    for (int i = 0; i < 39837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39838(int& val) {
    func_in_39838(val);
    const int x = 39838;
    std::cout << 39838;
    for (int i = 0; i < 39838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39838(int& val) {
    const int x = 39838;
    std::cout << 39838;
    for (int i = 0; i < 39838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39839(int& val) {
    func_in_39839(val);
    const int x = 39839;
    std::cout << 39839;
    for (int i = 0; i < 39839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39839(int& val) {
    const int x = 39839;
    std::cout << 39839;
    for (int i = 0; i < 39839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39840(int& val) {
    func_in_39840(val);
    const int x = 39840;
    std::cout << 39840;
    for (int i = 0; i < 39840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39840(int& val) {
    const int x = 39840;
    std::cout << 39840;
    for (int i = 0; i < 39840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39841(int& val) {
    func_in_39841(val);
    const int x = 39841;
    std::cout << 39841;
    for (int i = 0; i < 39841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39841(int& val) {
    const int x = 39841;
    std::cout << 39841;
    for (int i = 0; i < 39841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39842(int& val) {
    func_in_39842(val);
    const int x = 39842;
    std::cout << 39842;
    for (int i = 0; i < 39842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39842(int& val) {
    const int x = 39842;
    std::cout << 39842;
    for (int i = 0; i < 39842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39843(int& val) {
    func_in_39843(val);
    const int x = 39843;
    std::cout << 39843;
    for (int i = 0; i < 39843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39843(int& val) {
    const int x = 39843;
    std::cout << 39843;
    for (int i = 0; i < 39843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39844(int& val) {
    func_in_39844(val);
    const int x = 39844;
    std::cout << 39844;
    for (int i = 0; i < 39844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39844(int& val) {
    const int x = 39844;
    std::cout << 39844;
    for (int i = 0; i < 39844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39845(int& val) {
    func_in_39845(val);
    const int x = 39845;
    std::cout << 39845;
    for (int i = 0; i < 39845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39845(int& val) {
    const int x = 39845;
    std::cout << 39845;
    for (int i = 0; i < 39845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39846(int& val) {
    func_in_39846(val);
    const int x = 39846;
    std::cout << 39846;
    for (int i = 0; i < 39846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39846(int& val) {
    const int x = 39846;
    std::cout << 39846;
    for (int i = 0; i < 39846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39847(int& val) {
    func_in_39847(val);
    const int x = 39847;
    std::cout << 39847;
    for (int i = 0; i < 39847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39847(int& val) {
    const int x = 39847;
    std::cout << 39847;
    for (int i = 0; i < 39847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39848(int& val) {
    func_in_39848(val);
    const int x = 39848;
    std::cout << 39848;
    for (int i = 0; i < 39848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39848(int& val) {
    const int x = 39848;
    std::cout << 39848;
    for (int i = 0; i < 39848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39849(int& val) {
    func_in_39849(val);
    const int x = 39849;
    std::cout << 39849;
    for (int i = 0; i < 39849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39849(int& val) {
    const int x = 39849;
    std::cout << 39849;
    for (int i = 0; i < 39849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39850(int& val) {
    func_in_39850(val);
    const int x = 39850;
    std::cout << 39850;
    for (int i = 0; i < 39850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39850(int& val) {
    const int x = 39850;
    std::cout << 39850;
    for (int i = 0; i < 39850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39851(int& val) {
    func_in_39851(val);
    const int x = 39851;
    std::cout << 39851;
    for (int i = 0; i < 39851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39851(int& val) {
    const int x = 39851;
    std::cout << 39851;
    for (int i = 0; i < 39851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39852(int& val) {
    func_in_39852(val);
    const int x = 39852;
    std::cout << 39852;
    for (int i = 0; i < 39852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39852(int& val) {
    const int x = 39852;
    std::cout << 39852;
    for (int i = 0; i < 39852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39853(int& val) {
    func_in_39853(val);
    const int x = 39853;
    std::cout << 39853;
    for (int i = 0; i < 39853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39853(int& val) {
    const int x = 39853;
    std::cout << 39853;
    for (int i = 0; i < 39853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39854(int& val) {
    func_in_39854(val);
    const int x = 39854;
    std::cout << 39854;
    for (int i = 0; i < 39854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39854(int& val) {
    const int x = 39854;
    std::cout << 39854;
    for (int i = 0; i < 39854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39855(int& val) {
    func_in_39855(val);
    const int x = 39855;
    std::cout << 39855;
    for (int i = 0; i < 39855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39855(int& val) {
    const int x = 39855;
    std::cout << 39855;
    for (int i = 0; i < 39855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39856(int& val) {
    func_in_39856(val);
    const int x = 39856;
    std::cout << 39856;
    for (int i = 0; i < 39856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39856(int& val) {
    const int x = 39856;
    std::cout << 39856;
    for (int i = 0; i < 39856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39857(int& val) {
    func_in_39857(val);
    const int x = 39857;
    std::cout << 39857;
    for (int i = 0; i < 39857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39857(int& val) {
    const int x = 39857;
    std::cout << 39857;
    for (int i = 0; i < 39857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39858(int& val) {
    func_in_39858(val);
    const int x = 39858;
    std::cout << 39858;
    for (int i = 0; i < 39858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39858(int& val) {
    const int x = 39858;
    std::cout << 39858;
    for (int i = 0; i < 39858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39859(int& val) {
    func_in_39859(val);
    const int x = 39859;
    std::cout << 39859;
    for (int i = 0; i < 39859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39859(int& val) {
    const int x = 39859;
    std::cout << 39859;
    for (int i = 0; i < 39859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39860(int& val) {
    func_in_39860(val);
    const int x = 39860;
    std::cout << 39860;
    for (int i = 0; i < 39860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39860(int& val) {
    const int x = 39860;
    std::cout << 39860;
    for (int i = 0; i < 39860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39861(int& val) {
    func_in_39861(val);
    const int x = 39861;
    std::cout << 39861;
    for (int i = 0; i < 39861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39861(int& val) {
    const int x = 39861;
    std::cout << 39861;
    for (int i = 0; i < 39861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39862(int& val) {
    func_in_39862(val);
    const int x = 39862;
    std::cout << 39862;
    for (int i = 0; i < 39862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39862(int& val) {
    const int x = 39862;
    std::cout << 39862;
    for (int i = 0; i < 39862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39863(int& val) {
    func_in_39863(val);
    const int x = 39863;
    std::cout << 39863;
    for (int i = 0; i < 39863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39863(int& val) {
    const int x = 39863;
    std::cout << 39863;
    for (int i = 0; i < 39863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39864(int& val) {
    func_in_39864(val);
    const int x = 39864;
    std::cout << 39864;
    for (int i = 0; i < 39864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39864(int& val) {
    const int x = 39864;
    std::cout << 39864;
    for (int i = 0; i < 39864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39865(int& val) {
    func_in_39865(val);
    const int x = 39865;
    std::cout << 39865;
    for (int i = 0; i < 39865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39865(int& val) {
    const int x = 39865;
    std::cout << 39865;
    for (int i = 0; i < 39865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39866(int& val) {
    func_in_39866(val);
    const int x = 39866;
    std::cout << 39866;
    for (int i = 0; i < 39866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39866(int& val) {
    const int x = 39866;
    std::cout << 39866;
    for (int i = 0; i < 39866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39867(int& val) {
    func_in_39867(val);
    const int x = 39867;
    std::cout << 39867;
    for (int i = 0; i < 39867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39867(int& val) {
    const int x = 39867;
    std::cout << 39867;
    for (int i = 0; i < 39867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39868(int& val) {
    func_in_39868(val);
    const int x = 39868;
    std::cout << 39868;
    for (int i = 0; i < 39868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39868(int& val) {
    const int x = 39868;
    std::cout << 39868;
    for (int i = 0; i < 39868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39869(int& val) {
    func_in_39869(val);
    const int x = 39869;
    std::cout << 39869;
    for (int i = 0; i < 39869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39869(int& val) {
    const int x = 39869;
    std::cout << 39869;
    for (int i = 0; i < 39869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39870(int& val) {
    func_in_39870(val);
    const int x = 39870;
    std::cout << 39870;
    for (int i = 0; i < 39870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39870(int& val) {
    const int x = 39870;
    std::cout << 39870;
    for (int i = 0; i < 39870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39871(int& val) {
    func_in_39871(val);
    const int x = 39871;
    std::cout << 39871;
    for (int i = 0; i < 39871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39871(int& val) {
    const int x = 39871;
    std::cout << 39871;
    for (int i = 0; i < 39871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39872(int& val) {
    func_in_39872(val);
    const int x = 39872;
    std::cout << 39872;
    for (int i = 0; i < 39872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39872(int& val) {
    const int x = 39872;
    std::cout << 39872;
    for (int i = 0; i < 39872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39873(int& val) {
    func_in_39873(val);
    const int x = 39873;
    std::cout << 39873;
    for (int i = 0; i < 39873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39873(int& val) {
    const int x = 39873;
    std::cout << 39873;
    for (int i = 0; i < 39873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39874(int& val) {
    func_in_39874(val);
    const int x = 39874;
    std::cout << 39874;
    for (int i = 0; i < 39874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39874(int& val) {
    const int x = 39874;
    std::cout << 39874;
    for (int i = 0; i < 39874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39875(int& val) {
    func_in_39875(val);
    const int x = 39875;
    std::cout << 39875;
    for (int i = 0; i < 39875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39875(int& val) {
    const int x = 39875;
    std::cout << 39875;
    for (int i = 0; i < 39875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39876(int& val) {
    func_in_39876(val);
    const int x = 39876;
    std::cout << 39876;
    for (int i = 0; i < 39876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39876(int& val) {
    const int x = 39876;
    std::cout << 39876;
    for (int i = 0; i < 39876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39877(int& val) {
    func_in_39877(val);
    const int x = 39877;
    std::cout << 39877;
    for (int i = 0; i < 39877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39877(int& val) {
    const int x = 39877;
    std::cout << 39877;
    for (int i = 0; i < 39877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39878(int& val) {
    func_in_39878(val);
    const int x = 39878;
    std::cout << 39878;
    for (int i = 0; i < 39878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39878(int& val) {
    const int x = 39878;
    std::cout << 39878;
    for (int i = 0; i < 39878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39879(int& val) {
    func_in_39879(val);
    const int x = 39879;
    std::cout << 39879;
    for (int i = 0; i < 39879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39879(int& val) {
    const int x = 39879;
    std::cout << 39879;
    for (int i = 0; i < 39879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39880(int& val) {
    func_in_39880(val);
    const int x = 39880;
    std::cout << 39880;
    for (int i = 0; i < 39880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39880(int& val) {
    const int x = 39880;
    std::cout << 39880;
    for (int i = 0; i < 39880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39881(int& val) {
    func_in_39881(val);
    const int x = 39881;
    std::cout << 39881;
    for (int i = 0; i < 39881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39881(int& val) {
    const int x = 39881;
    std::cout << 39881;
    for (int i = 0; i < 39881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39882(int& val) {
    func_in_39882(val);
    const int x = 39882;
    std::cout << 39882;
    for (int i = 0; i < 39882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39882(int& val) {
    const int x = 39882;
    std::cout << 39882;
    for (int i = 0; i < 39882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39883(int& val) {
    func_in_39883(val);
    const int x = 39883;
    std::cout << 39883;
    for (int i = 0; i < 39883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39883(int& val) {
    const int x = 39883;
    std::cout << 39883;
    for (int i = 0; i < 39883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39884(int& val) {
    func_in_39884(val);
    const int x = 39884;
    std::cout << 39884;
    for (int i = 0; i < 39884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39884(int& val) {
    const int x = 39884;
    std::cout << 39884;
    for (int i = 0; i < 39884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39885(int& val) {
    func_in_39885(val);
    const int x = 39885;
    std::cout << 39885;
    for (int i = 0; i < 39885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39885(int& val) {
    const int x = 39885;
    std::cout << 39885;
    for (int i = 0; i < 39885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39886(int& val) {
    func_in_39886(val);
    const int x = 39886;
    std::cout << 39886;
    for (int i = 0; i < 39886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39886(int& val) {
    const int x = 39886;
    std::cout << 39886;
    for (int i = 0; i < 39886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39887(int& val) {
    func_in_39887(val);
    const int x = 39887;
    std::cout << 39887;
    for (int i = 0; i < 39887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39887(int& val) {
    const int x = 39887;
    std::cout << 39887;
    for (int i = 0; i < 39887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39888(int& val) {
    func_in_39888(val);
    const int x = 39888;
    std::cout << 39888;
    for (int i = 0; i < 39888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39888(int& val) {
    const int x = 39888;
    std::cout << 39888;
    for (int i = 0; i < 39888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39889(int& val) {
    func_in_39889(val);
    const int x = 39889;
    std::cout << 39889;
    for (int i = 0; i < 39889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39889(int& val) {
    const int x = 39889;
    std::cout << 39889;
    for (int i = 0; i < 39889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39890(int& val) {
    func_in_39890(val);
    const int x = 39890;
    std::cout << 39890;
    for (int i = 0; i < 39890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39890(int& val) {
    const int x = 39890;
    std::cout << 39890;
    for (int i = 0; i < 39890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39891(int& val) {
    func_in_39891(val);
    const int x = 39891;
    std::cout << 39891;
    for (int i = 0; i < 39891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39891(int& val) {
    const int x = 39891;
    std::cout << 39891;
    for (int i = 0; i < 39891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39892(int& val) {
    func_in_39892(val);
    const int x = 39892;
    std::cout << 39892;
    for (int i = 0; i < 39892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39892(int& val) {
    const int x = 39892;
    std::cout << 39892;
    for (int i = 0; i < 39892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39893(int& val) {
    func_in_39893(val);
    const int x = 39893;
    std::cout << 39893;
    for (int i = 0; i < 39893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39893(int& val) {
    const int x = 39893;
    std::cout << 39893;
    for (int i = 0; i < 39893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39894(int& val) {
    func_in_39894(val);
    const int x = 39894;
    std::cout << 39894;
    for (int i = 0; i < 39894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39894(int& val) {
    const int x = 39894;
    std::cout << 39894;
    for (int i = 0; i < 39894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39895(int& val) {
    func_in_39895(val);
    const int x = 39895;
    std::cout << 39895;
    for (int i = 0; i < 39895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39895(int& val) {
    const int x = 39895;
    std::cout << 39895;
    for (int i = 0; i < 39895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39896(int& val) {
    func_in_39896(val);
    const int x = 39896;
    std::cout << 39896;
    for (int i = 0; i < 39896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39896(int& val) {
    const int x = 39896;
    std::cout << 39896;
    for (int i = 0; i < 39896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39897(int& val) {
    func_in_39897(val);
    const int x = 39897;
    std::cout << 39897;
    for (int i = 0; i < 39897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39897(int& val) {
    const int x = 39897;
    std::cout << 39897;
    for (int i = 0; i < 39897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39898(int& val) {
    func_in_39898(val);
    const int x = 39898;
    std::cout << 39898;
    for (int i = 0; i < 39898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39898(int& val) {
    const int x = 39898;
    std::cout << 39898;
    for (int i = 0; i < 39898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39899(int& val) {
    func_in_39899(val);
    const int x = 39899;
    std::cout << 39899;
    for (int i = 0; i < 39899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39899(int& val) {
    const int x = 39899;
    std::cout << 39899;
    for (int i = 0; i < 39899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39900(int& val) {
    func_in_39900(val);
    const int x = 39900;
    std::cout << 39900;
    for (int i = 0; i < 39900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39900(int& val) {
    const int x = 39900;
    std::cout << 39900;
    for (int i = 0; i < 39900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39901(int& val) {
    func_in_39901(val);
    const int x = 39901;
    std::cout << 39901;
    for (int i = 0; i < 39901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39901(int& val) {
    const int x = 39901;
    std::cout << 39901;
    for (int i = 0; i < 39901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39902(int& val) {
    func_in_39902(val);
    const int x = 39902;
    std::cout << 39902;
    for (int i = 0; i < 39902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39902(int& val) {
    const int x = 39902;
    std::cout << 39902;
    for (int i = 0; i < 39902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39903(int& val) {
    func_in_39903(val);
    const int x = 39903;
    std::cout << 39903;
    for (int i = 0; i < 39903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39903(int& val) {
    const int x = 39903;
    std::cout << 39903;
    for (int i = 0; i < 39903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39904(int& val) {
    func_in_39904(val);
    const int x = 39904;
    std::cout << 39904;
    for (int i = 0; i < 39904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39904(int& val) {
    const int x = 39904;
    std::cout << 39904;
    for (int i = 0; i < 39904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39905(int& val) {
    func_in_39905(val);
    const int x = 39905;
    std::cout << 39905;
    for (int i = 0; i < 39905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39905(int& val) {
    const int x = 39905;
    std::cout << 39905;
    for (int i = 0; i < 39905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39906(int& val) {
    func_in_39906(val);
    const int x = 39906;
    std::cout << 39906;
    for (int i = 0; i < 39906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39906(int& val) {
    const int x = 39906;
    std::cout << 39906;
    for (int i = 0; i < 39906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39907(int& val) {
    func_in_39907(val);
    const int x = 39907;
    std::cout << 39907;
    for (int i = 0; i < 39907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39907(int& val) {
    const int x = 39907;
    std::cout << 39907;
    for (int i = 0; i < 39907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39908(int& val) {
    func_in_39908(val);
    const int x = 39908;
    std::cout << 39908;
    for (int i = 0; i < 39908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39908(int& val) {
    const int x = 39908;
    std::cout << 39908;
    for (int i = 0; i < 39908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39909(int& val) {
    func_in_39909(val);
    const int x = 39909;
    std::cout << 39909;
    for (int i = 0; i < 39909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39909(int& val) {
    const int x = 39909;
    std::cout << 39909;
    for (int i = 0; i < 39909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39910(int& val) {
    func_in_39910(val);
    const int x = 39910;
    std::cout << 39910;
    for (int i = 0; i < 39910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39910(int& val) {
    const int x = 39910;
    std::cout << 39910;
    for (int i = 0; i < 39910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39911(int& val) {
    func_in_39911(val);
    const int x = 39911;
    std::cout << 39911;
    for (int i = 0; i < 39911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39911(int& val) {
    const int x = 39911;
    std::cout << 39911;
    for (int i = 0; i < 39911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39912(int& val) {
    func_in_39912(val);
    const int x = 39912;
    std::cout << 39912;
    for (int i = 0; i < 39912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39912(int& val) {
    const int x = 39912;
    std::cout << 39912;
    for (int i = 0; i < 39912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39913(int& val) {
    func_in_39913(val);
    const int x = 39913;
    std::cout << 39913;
    for (int i = 0; i < 39913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39913(int& val) {
    const int x = 39913;
    std::cout << 39913;
    for (int i = 0; i < 39913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39914(int& val) {
    func_in_39914(val);
    const int x = 39914;
    std::cout << 39914;
    for (int i = 0; i < 39914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39914(int& val) {
    const int x = 39914;
    std::cout << 39914;
    for (int i = 0; i < 39914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39915(int& val) {
    func_in_39915(val);
    const int x = 39915;
    std::cout << 39915;
    for (int i = 0; i < 39915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39915(int& val) {
    const int x = 39915;
    std::cout << 39915;
    for (int i = 0; i < 39915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39916(int& val) {
    func_in_39916(val);
    const int x = 39916;
    std::cout << 39916;
    for (int i = 0; i < 39916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39916(int& val) {
    const int x = 39916;
    std::cout << 39916;
    for (int i = 0; i < 39916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39917(int& val) {
    func_in_39917(val);
    const int x = 39917;
    std::cout << 39917;
    for (int i = 0; i < 39917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39917(int& val) {
    const int x = 39917;
    std::cout << 39917;
    for (int i = 0; i < 39917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39918(int& val) {
    func_in_39918(val);
    const int x = 39918;
    std::cout << 39918;
    for (int i = 0; i < 39918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39918(int& val) {
    const int x = 39918;
    std::cout << 39918;
    for (int i = 0; i < 39918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39919(int& val) {
    func_in_39919(val);
    const int x = 39919;
    std::cout << 39919;
    for (int i = 0; i < 39919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39919(int& val) {
    const int x = 39919;
    std::cout << 39919;
    for (int i = 0; i < 39919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39920(int& val) {
    func_in_39920(val);
    const int x = 39920;
    std::cout << 39920;
    for (int i = 0; i < 39920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39920(int& val) {
    const int x = 39920;
    std::cout << 39920;
    for (int i = 0; i < 39920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39921(int& val) {
    func_in_39921(val);
    const int x = 39921;
    std::cout << 39921;
    for (int i = 0; i < 39921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39921(int& val) {
    const int x = 39921;
    std::cout << 39921;
    for (int i = 0; i < 39921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39922(int& val) {
    func_in_39922(val);
    const int x = 39922;
    std::cout << 39922;
    for (int i = 0; i < 39922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39922(int& val) {
    const int x = 39922;
    std::cout << 39922;
    for (int i = 0; i < 39922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39923(int& val) {
    func_in_39923(val);
    const int x = 39923;
    std::cout << 39923;
    for (int i = 0; i < 39923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39923(int& val) {
    const int x = 39923;
    std::cout << 39923;
    for (int i = 0; i < 39923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39924(int& val) {
    func_in_39924(val);
    const int x = 39924;
    std::cout << 39924;
    for (int i = 0; i < 39924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39924(int& val) {
    const int x = 39924;
    std::cout << 39924;
    for (int i = 0; i < 39924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39925(int& val) {
    func_in_39925(val);
    const int x = 39925;
    std::cout << 39925;
    for (int i = 0; i < 39925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39925(int& val) {
    const int x = 39925;
    std::cout << 39925;
    for (int i = 0; i < 39925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39926(int& val) {
    func_in_39926(val);
    const int x = 39926;
    std::cout << 39926;
    for (int i = 0; i < 39926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39926(int& val) {
    const int x = 39926;
    std::cout << 39926;
    for (int i = 0; i < 39926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39927(int& val) {
    func_in_39927(val);
    const int x = 39927;
    std::cout << 39927;
    for (int i = 0; i < 39927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39927(int& val) {
    const int x = 39927;
    std::cout << 39927;
    for (int i = 0; i < 39927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39928(int& val) {
    func_in_39928(val);
    const int x = 39928;
    std::cout << 39928;
    for (int i = 0; i < 39928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39928(int& val) {
    const int x = 39928;
    std::cout << 39928;
    for (int i = 0; i < 39928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39929(int& val) {
    func_in_39929(val);
    const int x = 39929;
    std::cout << 39929;
    for (int i = 0; i < 39929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39929(int& val) {
    const int x = 39929;
    std::cout << 39929;
    for (int i = 0; i < 39929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39930(int& val) {
    func_in_39930(val);
    const int x = 39930;
    std::cout << 39930;
    for (int i = 0; i < 39930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39930(int& val) {
    const int x = 39930;
    std::cout << 39930;
    for (int i = 0; i < 39930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39931(int& val) {
    func_in_39931(val);
    const int x = 39931;
    std::cout << 39931;
    for (int i = 0; i < 39931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39931(int& val) {
    const int x = 39931;
    std::cout << 39931;
    for (int i = 0; i < 39931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39932(int& val) {
    func_in_39932(val);
    const int x = 39932;
    std::cout << 39932;
    for (int i = 0; i < 39932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39932(int& val) {
    const int x = 39932;
    std::cout << 39932;
    for (int i = 0; i < 39932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39933(int& val) {
    func_in_39933(val);
    const int x = 39933;
    std::cout << 39933;
    for (int i = 0; i < 39933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39933(int& val) {
    const int x = 39933;
    std::cout << 39933;
    for (int i = 0; i < 39933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39934(int& val) {
    func_in_39934(val);
    const int x = 39934;
    std::cout << 39934;
    for (int i = 0; i < 39934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39934(int& val) {
    const int x = 39934;
    std::cout << 39934;
    for (int i = 0; i < 39934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39935(int& val) {
    func_in_39935(val);
    const int x = 39935;
    std::cout << 39935;
    for (int i = 0; i < 39935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39935(int& val) {
    const int x = 39935;
    std::cout << 39935;
    for (int i = 0; i < 39935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39936(int& val) {
    func_in_39936(val);
    const int x = 39936;
    std::cout << 39936;
    for (int i = 0; i < 39936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39936(int& val) {
    const int x = 39936;
    std::cout << 39936;
    for (int i = 0; i < 39936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39937(int& val) {
    func_in_39937(val);
    const int x = 39937;
    std::cout << 39937;
    for (int i = 0; i < 39937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39937(int& val) {
    const int x = 39937;
    std::cout << 39937;
    for (int i = 0; i < 39937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39938(int& val) {
    func_in_39938(val);
    const int x = 39938;
    std::cout << 39938;
    for (int i = 0; i < 39938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39938(int& val) {
    const int x = 39938;
    std::cout << 39938;
    for (int i = 0; i < 39938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39939(int& val) {
    func_in_39939(val);
    const int x = 39939;
    std::cout << 39939;
    for (int i = 0; i < 39939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39939(int& val) {
    const int x = 39939;
    std::cout << 39939;
    for (int i = 0; i < 39939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39940(int& val) {
    func_in_39940(val);
    const int x = 39940;
    std::cout << 39940;
    for (int i = 0; i < 39940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39940(int& val) {
    const int x = 39940;
    std::cout << 39940;
    for (int i = 0; i < 39940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39941(int& val) {
    func_in_39941(val);
    const int x = 39941;
    std::cout << 39941;
    for (int i = 0; i < 39941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39941(int& val) {
    const int x = 39941;
    std::cout << 39941;
    for (int i = 0; i < 39941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39942(int& val) {
    func_in_39942(val);
    const int x = 39942;
    std::cout << 39942;
    for (int i = 0; i < 39942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39942(int& val) {
    const int x = 39942;
    std::cout << 39942;
    for (int i = 0; i < 39942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39943(int& val) {
    func_in_39943(val);
    const int x = 39943;
    std::cout << 39943;
    for (int i = 0; i < 39943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39943(int& val) {
    const int x = 39943;
    std::cout << 39943;
    for (int i = 0; i < 39943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39944(int& val) {
    func_in_39944(val);
    const int x = 39944;
    std::cout << 39944;
    for (int i = 0; i < 39944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39944(int& val) {
    const int x = 39944;
    std::cout << 39944;
    for (int i = 0; i < 39944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39945(int& val) {
    func_in_39945(val);
    const int x = 39945;
    std::cout << 39945;
    for (int i = 0; i < 39945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39945(int& val) {
    const int x = 39945;
    std::cout << 39945;
    for (int i = 0; i < 39945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39946(int& val) {
    func_in_39946(val);
    const int x = 39946;
    std::cout << 39946;
    for (int i = 0; i < 39946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39946(int& val) {
    const int x = 39946;
    std::cout << 39946;
    for (int i = 0; i < 39946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39947(int& val) {
    func_in_39947(val);
    const int x = 39947;
    std::cout << 39947;
    for (int i = 0; i < 39947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39947(int& val) {
    const int x = 39947;
    std::cout << 39947;
    for (int i = 0; i < 39947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39948(int& val) {
    func_in_39948(val);
    const int x = 39948;
    std::cout << 39948;
    for (int i = 0; i < 39948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39948(int& val) {
    const int x = 39948;
    std::cout << 39948;
    for (int i = 0; i < 39948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39949(int& val) {
    func_in_39949(val);
    const int x = 39949;
    std::cout << 39949;
    for (int i = 0; i < 39949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39949(int& val) {
    const int x = 39949;
    std::cout << 39949;
    for (int i = 0; i < 39949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39950(int& val) {
    func_in_39950(val);
    const int x = 39950;
    std::cout << 39950;
    for (int i = 0; i < 39950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39950(int& val) {
    const int x = 39950;
    std::cout << 39950;
    for (int i = 0; i < 39950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39951(int& val) {
    func_in_39951(val);
    const int x = 39951;
    std::cout << 39951;
    for (int i = 0; i < 39951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39951(int& val) {
    const int x = 39951;
    std::cout << 39951;
    for (int i = 0; i < 39951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39952(int& val) {
    func_in_39952(val);
    const int x = 39952;
    std::cout << 39952;
    for (int i = 0; i < 39952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39952(int& val) {
    const int x = 39952;
    std::cout << 39952;
    for (int i = 0; i < 39952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39953(int& val) {
    func_in_39953(val);
    const int x = 39953;
    std::cout << 39953;
    for (int i = 0; i < 39953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39953(int& val) {
    const int x = 39953;
    std::cout << 39953;
    for (int i = 0; i < 39953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39954(int& val) {
    func_in_39954(val);
    const int x = 39954;
    std::cout << 39954;
    for (int i = 0; i < 39954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39954(int& val) {
    const int x = 39954;
    std::cout << 39954;
    for (int i = 0; i < 39954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39955(int& val) {
    func_in_39955(val);
    const int x = 39955;
    std::cout << 39955;
    for (int i = 0; i < 39955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39955(int& val) {
    const int x = 39955;
    std::cout << 39955;
    for (int i = 0; i < 39955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39956(int& val) {
    func_in_39956(val);
    const int x = 39956;
    std::cout << 39956;
    for (int i = 0; i < 39956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39956(int& val) {
    const int x = 39956;
    std::cout << 39956;
    for (int i = 0; i < 39956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39957(int& val) {
    func_in_39957(val);
    const int x = 39957;
    std::cout << 39957;
    for (int i = 0; i < 39957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39957(int& val) {
    const int x = 39957;
    std::cout << 39957;
    for (int i = 0; i < 39957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39958(int& val) {
    func_in_39958(val);
    const int x = 39958;
    std::cout << 39958;
    for (int i = 0; i < 39958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39958(int& val) {
    const int x = 39958;
    std::cout << 39958;
    for (int i = 0; i < 39958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39959(int& val) {
    func_in_39959(val);
    const int x = 39959;
    std::cout << 39959;
    for (int i = 0; i < 39959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39959(int& val) {
    const int x = 39959;
    std::cout << 39959;
    for (int i = 0; i < 39959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39960(int& val) {
    func_in_39960(val);
    const int x = 39960;
    std::cout << 39960;
    for (int i = 0; i < 39960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39960(int& val) {
    const int x = 39960;
    std::cout << 39960;
    for (int i = 0; i < 39960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39961(int& val) {
    func_in_39961(val);
    const int x = 39961;
    std::cout << 39961;
    for (int i = 0; i < 39961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39961(int& val) {
    const int x = 39961;
    std::cout << 39961;
    for (int i = 0; i < 39961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39962(int& val) {
    func_in_39962(val);
    const int x = 39962;
    std::cout << 39962;
    for (int i = 0; i < 39962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39962(int& val) {
    const int x = 39962;
    std::cout << 39962;
    for (int i = 0; i < 39962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39963(int& val) {
    func_in_39963(val);
    const int x = 39963;
    std::cout << 39963;
    for (int i = 0; i < 39963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39963(int& val) {
    const int x = 39963;
    std::cout << 39963;
    for (int i = 0; i < 39963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39964(int& val) {
    func_in_39964(val);
    const int x = 39964;
    std::cout << 39964;
    for (int i = 0; i < 39964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39964(int& val) {
    const int x = 39964;
    std::cout << 39964;
    for (int i = 0; i < 39964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39965(int& val) {
    func_in_39965(val);
    const int x = 39965;
    std::cout << 39965;
    for (int i = 0; i < 39965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39965(int& val) {
    const int x = 39965;
    std::cout << 39965;
    for (int i = 0; i < 39965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39966(int& val) {
    func_in_39966(val);
    const int x = 39966;
    std::cout << 39966;
    for (int i = 0; i < 39966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39966(int& val) {
    const int x = 39966;
    std::cout << 39966;
    for (int i = 0; i < 39966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39967(int& val) {
    func_in_39967(val);
    const int x = 39967;
    std::cout << 39967;
    for (int i = 0; i < 39967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39967(int& val) {
    const int x = 39967;
    std::cout << 39967;
    for (int i = 0; i < 39967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39968(int& val) {
    func_in_39968(val);
    const int x = 39968;
    std::cout << 39968;
    for (int i = 0; i < 39968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39968(int& val) {
    const int x = 39968;
    std::cout << 39968;
    for (int i = 0; i < 39968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39969(int& val) {
    func_in_39969(val);
    const int x = 39969;
    std::cout << 39969;
    for (int i = 0; i < 39969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39969(int& val) {
    const int x = 39969;
    std::cout << 39969;
    for (int i = 0; i < 39969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39970(int& val) {
    func_in_39970(val);
    const int x = 39970;
    std::cout << 39970;
    for (int i = 0; i < 39970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39970(int& val) {
    const int x = 39970;
    std::cout << 39970;
    for (int i = 0; i < 39970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39971(int& val) {
    func_in_39971(val);
    const int x = 39971;
    std::cout << 39971;
    for (int i = 0; i < 39971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39971(int& val) {
    const int x = 39971;
    std::cout << 39971;
    for (int i = 0; i < 39971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39972(int& val) {
    func_in_39972(val);
    const int x = 39972;
    std::cout << 39972;
    for (int i = 0; i < 39972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39972(int& val) {
    const int x = 39972;
    std::cout << 39972;
    for (int i = 0; i < 39972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39973(int& val) {
    func_in_39973(val);
    const int x = 39973;
    std::cout << 39973;
    for (int i = 0; i < 39973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39973(int& val) {
    const int x = 39973;
    std::cout << 39973;
    for (int i = 0; i < 39973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39974(int& val) {
    func_in_39974(val);
    const int x = 39974;
    std::cout << 39974;
    for (int i = 0; i < 39974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39974(int& val) {
    const int x = 39974;
    std::cout << 39974;
    for (int i = 0; i < 39974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39975(int& val) {
    func_in_39975(val);
    const int x = 39975;
    std::cout << 39975;
    for (int i = 0; i < 39975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39975(int& val) {
    const int x = 39975;
    std::cout << 39975;
    for (int i = 0; i < 39975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39976(int& val) {
    func_in_39976(val);
    const int x = 39976;
    std::cout << 39976;
    for (int i = 0; i < 39976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39976(int& val) {
    const int x = 39976;
    std::cout << 39976;
    for (int i = 0; i < 39976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39977(int& val) {
    func_in_39977(val);
    const int x = 39977;
    std::cout << 39977;
    for (int i = 0; i < 39977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39977(int& val) {
    const int x = 39977;
    std::cout << 39977;
    for (int i = 0; i < 39977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39978(int& val) {
    func_in_39978(val);
    const int x = 39978;
    std::cout << 39978;
    for (int i = 0; i < 39978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39978(int& val) {
    const int x = 39978;
    std::cout << 39978;
    for (int i = 0; i < 39978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39979(int& val) {
    func_in_39979(val);
    const int x = 39979;
    std::cout << 39979;
    for (int i = 0; i < 39979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39979(int& val) {
    const int x = 39979;
    std::cout << 39979;
    for (int i = 0; i < 39979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39980(int& val) {
    func_in_39980(val);
    const int x = 39980;
    std::cout << 39980;
    for (int i = 0; i < 39980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39980(int& val) {
    const int x = 39980;
    std::cout << 39980;
    for (int i = 0; i < 39980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39981(int& val) {
    func_in_39981(val);
    const int x = 39981;
    std::cout << 39981;
    for (int i = 0; i < 39981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39981(int& val) {
    const int x = 39981;
    std::cout << 39981;
    for (int i = 0; i < 39981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39982(int& val) {
    func_in_39982(val);
    const int x = 39982;
    std::cout << 39982;
    for (int i = 0; i < 39982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39982(int& val) {
    const int x = 39982;
    std::cout << 39982;
    for (int i = 0; i < 39982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39983(int& val) {
    func_in_39983(val);
    const int x = 39983;
    std::cout << 39983;
    for (int i = 0; i < 39983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39983(int& val) {
    const int x = 39983;
    std::cout << 39983;
    for (int i = 0; i < 39983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39984(int& val) {
    func_in_39984(val);
    const int x = 39984;
    std::cout << 39984;
    for (int i = 0; i < 39984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39984(int& val) {
    const int x = 39984;
    std::cout << 39984;
    for (int i = 0; i < 39984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39985(int& val) {
    func_in_39985(val);
    const int x = 39985;
    std::cout << 39985;
    for (int i = 0; i < 39985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39985(int& val) {
    const int x = 39985;
    std::cout << 39985;
    for (int i = 0; i < 39985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39986(int& val) {
    func_in_39986(val);
    const int x = 39986;
    std::cout << 39986;
    for (int i = 0; i < 39986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39986(int& val) {
    const int x = 39986;
    std::cout << 39986;
    for (int i = 0; i < 39986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39987(int& val) {
    func_in_39987(val);
    const int x = 39987;
    std::cout << 39987;
    for (int i = 0; i < 39987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39987(int& val) {
    const int x = 39987;
    std::cout << 39987;
    for (int i = 0; i < 39987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39988(int& val) {
    func_in_39988(val);
    const int x = 39988;
    std::cout << 39988;
    for (int i = 0; i < 39988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39988(int& val) {
    const int x = 39988;
    std::cout << 39988;
    for (int i = 0; i < 39988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39989(int& val) {
    func_in_39989(val);
    const int x = 39989;
    std::cout << 39989;
    for (int i = 0; i < 39989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39989(int& val) {
    const int x = 39989;
    std::cout << 39989;
    for (int i = 0; i < 39989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39990(int& val) {
    func_in_39990(val);
    const int x = 39990;
    std::cout << 39990;
    for (int i = 0; i < 39990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39990(int& val) {
    const int x = 39990;
    std::cout << 39990;
    for (int i = 0; i < 39990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39991(int& val) {
    func_in_39991(val);
    const int x = 39991;
    std::cout << 39991;
    for (int i = 0; i < 39991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39991(int& val) {
    const int x = 39991;
    std::cout << 39991;
    for (int i = 0; i < 39991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39992(int& val) {
    func_in_39992(val);
    const int x = 39992;
    std::cout << 39992;
    for (int i = 0; i < 39992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39992(int& val) {
    const int x = 39992;
    std::cout << 39992;
    for (int i = 0; i < 39992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39993(int& val) {
    func_in_39993(val);
    const int x = 39993;
    std::cout << 39993;
    for (int i = 0; i < 39993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39993(int& val) {
    const int x = 39993;
    std::cout << 39993;
    for (int i = 0; i < 39993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39994(int& val) {
    func_in_39994(val);
    const int x = 39994;
    std::cout << 39994;
    for (int i = 0; i < 39994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39994(int& val) {
    const int x = 39994;
    std::cout << 39994;
    for (int i = 0; i < 39994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39995(int& val) {
    func_in_39995(val);
    const int x = 39995;
    std::cout << 39995;
    for (int i = 0; i < 39995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39995(int& val) {
    const int x = 39995;
    std::cout << 39995;
    for (int i = 0; i < 39995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39996(int& val) {
    func_in_39996(val);
    const int x = 39996;
    std::cout << 39996;
    for (int i = 0; i < 39996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39996(int& val) {
    const int x = 39996;
    std::cout << 39996;
    for (int i = 0; i < 39996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39997(int& val) {
    func_in_39997(val);
    const int x = 39997;
    std::cout << 39997;
    for (int i = 0; i < 39997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39997(int& val) {
    const int x = 39997;
    std::cout << 39997;
    for (int i = 0; i < 39997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39998(int& val) {
    func_in_39998(val);
    const int x = 39998;
    std::cout << 39998;
    for (int i = 0; i < 39998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39998(int& val) {
    const int x = 39998;
    std::cout << 39998;
    for (int i = 0; i < 39998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39999(int& val) {
    func_in_39999(val);
    const int x = 39999;
    std::cout << 39999;
    for (int i = 0; i < 39999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39999(int& val) {
    const int x = 39999;
    std::cout << 39999;
    for (int i = 0; i < 39999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

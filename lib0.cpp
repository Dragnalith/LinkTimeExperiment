#include "lib.h"
#include <iostream>
int func_0(int& val) {
    func_in_0(val);
    const int x = 0;
    std::cout << 0;
    for (int i = 0; i < 0; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_0(int& val) {
    const int x = 0;
    std::cout << 0;
    for (int i = 0; i < 0; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1(int& val) {
    func_in_1(val);
    const int x = 1;
    std::cout << 1;
    for (int i = 0; i < 1; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1(int& val) {
    const int x = 1;
    std::cout << 1;
    for (int i = 0; i < 1; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2(int& val) {
    func_in_2(val);
    const int x = 2;
    std::cout << 2;
    for (int i = 0; i < 2; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2(int& val) {
    const int x = 2;
    std::cout << 2;
    for (int i = 0; i < 2; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3(int& val) {
    func_in_3(val);
    const int x = 3;
    std::cout << 3;
    for (int i = 0; i < 3; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3(int& val) {
    const int x = 3;
    std::cout << 3;
    for (int i = 0; i < 3; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_4(int& val) {
    func_in_4(val);
    const int x = 4;
    std::cout << 4;
    for (int i = 0; i < 4; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_4(int& val) {
    const int x = 4;
    std::cout << 4;
    for (int i = 0; i < 4; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_5(int& val) {
    func_in_5(val);
    const int x = 5;
    std::cout << 5;
    for (int i = 0; i < 5; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_5(int& val) {
    const int x = 5;
    std::cout << 5;
    for (int i = 0; i < 5; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_6(int& val) {
    func_in_6(val);
    const int x = 6;
    std::cout << 6;
    for (int i = 0; i < 6; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_6(int& val) {
    const int x = 6;
    std::cout << 6;
    for (int i = 0; i < 6; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_7(int& val) {
    func_in_7(val);
    const int x = 7;
    std::cout << 7;
    for (int i = 0; i < 7; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_7(int& val) {
    const int x = 7;
    std::cout << 7;
    for (int i = 0; i < 7; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_8(int& val) {
    func_in_8(val);
    const int x = 8;
    std::cout << 8;
    for (int i = 0; i < 8; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_8(int& val) {
    const int x = 8;
    std::cout << 8;
    for (int i = 0; i < 8; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_9(int& val) {
    func_in_9(val);
    const int x = 9;
    std::cout << 9;
    for (int i = 0; i < 9; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_9(int& val) {
    const int x = 9;
    std::cout << 9;
    for (int i = 0; i < 9; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_10(int& val) {
    func_in_10(val);
    const int x = 10;
    std::cout << 10;
    for (int i = 0; i < 10; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_10(int& val) {
    const int x = 10;
    std::cout << 10;
    for (int i = 0; i < 10; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_11(int& val) {
    func_in_11(val);
    const int x = 11;
    std::cout << 11;
    for (int i = 0; i < 11; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_11(int& val) {
    const int x = 11;
    std::cout << 11;
    for (int i = 0; i < 11; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_12(int& val) {
    func_in_12(val);
    const int x = 12;
    std::cout << 12;
    for (int i = 0; i < 12; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_12(int& val) {
    const int x = 12;
    std::cout << 12;
    for (int i = 0; i < 12; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_13(int& val) {
    func_in_13(val);
    const int x = 13;
    std::cout << 13;
    for (int i = 0; i < 13; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_13(int& val) {
    const int x = 13;
    std::cout << 13;
    for (int i = 0; i < 13; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_14(int& val) {
    func_in_14(val);
    const int x = 14;
    std::cout << 14;
    for (int i = 0; i < 14; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_14(int& val) {
    const int x = 14;
    std::cout << 14;
    for (int i = 0; i < 14; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_15(int& val) {
    func_in_15(val);
    const int x = 15;
    std::cout << 15;
    for (int i = 0; i < 15; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_15(int& val) {
    const int x = 15;
    std::cout << 15;
    for (int i = 0; i < 15; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_16(int& val) {
    func_in_16(val);
    const int x = 16;
    std::cout << 16;
    for (int i = 0; i < 16; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_16(int& val) {
    const int x = 16;
    std::cout << 16;
    for (int i = 0; i < 16; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_17(int& val) {
    func_in_17(val);
    const int x = 17;
    std::cout << 17;
    for (int i = 0; i < 17; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_17(int& val) {
    const int x = 17;
    std::cout << 17;
    for (int i = 0; i < 17; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_18(int& val) {
    func_in_18(val);
    const int x = 18;
    std::cout << 18;
    for (int i = 0; i < 18; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_18(int& val) {
    const int x = 18;
    std::cout << 18;
    for (int i = 0; i < 18; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_19(int& val) {
    func_in_19(val);
    const int x = 19;
    std::cout << 19;
    for (int i = 0; i < 19; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_19(int& val) {
    const int x = 19;
    std::cout << 19;
    for (int i = 0; i < 19; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_20(int& val) {
    func_in_20(val);
    const int x = 20;
    std::cout << 20;
    for (int i = 0; i < 20; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_20(int& val) {
    const int x = 20;
    std::cout << 20;
    for (int i = 0; i < 20; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_21(int& val) {
    func_in_21(val);
    const int x = 21;
    std::cout << 21;
    for (int i = 0; i < 21; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_21(int& val) {
    const int x = 21;
    std::cout << 21;
    for (int i = 0; i < 21; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_22(int& val) {
    func_in_22(val);
    const int x = 22;
    std::cout << 22;
    for (int i = 0; i < 22; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_22(int& val) {
    const int x = 22;
    std::cout << 22;
    for (int i = 0; i < 22; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_23(int& val) {
    func_in_23(val);
    const int x = 23;
    std::cout << 23;
    for (int i = 0; i < 23; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_23(int& val) {
    const int x = 23;
    std::cout << 23;
    for (int i = 0; i < 23; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_24(int& val) {
    func_in_24(val);
    const int x = 24;
    std::cout << 24;
    for (int i = 0; i < 24; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_24(int& val) {
    const int x = 24;
    std::cout << 24;
    for (int i = 0; i < 24; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_25(int& val) {
    func_in_25(val);
    const int x = 25;
    std::cout << 25;
    for (int i = 0; i < 25; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_25(int& val) {
    const int x = 25;
    std::cout << 25;
    for (int i = 0; i < 25; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_26(int& val) {
    func_in_26(val);
    const int x = 26;
    std::cout << 26;
    for (int i = 0; i < 26; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_26(int& val) {
    const int x = 26;
    std::cout << 26;
    for (int i = 0; i < 26; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_27(int& val) {
    func_in_27(val);
    const int x = 27;
    std::cout << 27;
    for (int i = 0; i < 27; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_27(int& val) {
    const int x = 27;
    std::cout << 27;
    for (int i = 0; i < 27; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28(int& val) {
    func_in_28(val);
    const int x = 28;
    std::cout << 28;
    for (int i = 0; i < 28; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28(int& val) {
    const int x = 28;
    std::cout << 28;
    for (int i = 0; i < 28; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29(int& val) {
    func_in_29(val);
    const int x = 29;
    std::cout << 29;
    for (int i = 0; i < 29; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29(int& val) {
    const int x = 29;
    std::cout << 29;
    for (int i = 0; i < 29; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30(int& val) {
    func_in_30(val);
    const int x = 30;
    std::cout << 30;
    for (int i = 0; i < 30; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30(int& val) {
    const int x = 30;
    std::cout << 30;
    for (int i = 0; i < 30; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31(int& val) {
    func_in_31(val);
    const int x = 31;
    std::cout << 31;
    for (int i = 0; i < 31; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31(int& val) {
    const int x = 31;
    std::cout << 31;
    for (int i = 0; i < 31; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_32(int& val) {
    func_in_32(val);
    const int x = 32;
    std::cout << 32;
    for (int i = 0; i < 32; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_32(int& val) {
    const int x = 32;
    std::cout << 32;
    for (int i = 0; i < 32; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_33(int& val) {
    func_in_33(val);
    const int x = 33;
    std::cout << 33;
    for (int i = 0; i < 33; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_33(int& val) {
    const int x = 33;
    std::cout << 33;
    for (int i = 0; i < 33; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_34(int& val) {
    func_in_34(val);
    const int x = 34;
    std::cout << 34;
    for (int i = 0; i < 34; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_34(int& val) {
    const int x = 34;
    std::cout << 34;
    for (int i = 0; i < 34; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_35(int& val) {
    func_in_35(val);
    const int x = 35;
    std::cout << 35;
    for (int i = 0; i < 35; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_35(int& val) {
    const int x = 35;
    std::cout << 35;
    for (int i = 0; i < 35; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_36(int& val) {
    func_in_36(val);
    const int x = 36;
    std::cout << 36;
    for (int i = 0; i < 36; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_36(int& val) {
    const int x = 36;
    std::cout << 36;
    for (int i = 0; i < 36; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_37(int& val) {
    func_in_37(val);
    const int x = 37;
    std::cout << 37;
    for (int i = 0; i < 37; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_37(int& val) {
    const int x = 37;
    std::cout << 37;
    for (int i = 0; i < 37; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_38(int& val) {
    func_in_38(val);
    const int x = 38;
    std::cout << 38;
    for (int i = 0; i < 38; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_38(int& val) {
    const int x = 38;
    std::cout << 38;
    for (int i = 0; i < 38; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_39(int& val) {
    func_in_39(val);
    const int x = 39;
    std::cout << 39;
    for (int i = 0; i < 39; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_39(int& val) {
    const int x = 39;
    std::cout << 39;
    for (int i = 0; i < 39; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_40(int& val) {
    func_in_40(val);
    const int x = 40;
    std::cout << 40;
    for (int i = 0; i < 40; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_40(int& val) {
    const int x = 40;
    std::cout << 40;
    for (int i = 0; i < 40; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_41(int& val) {
    func_in_41(val);
    const int x = 41;
    std::cout << 41;
    for (int i = 0; i < 41; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_41(int& val) {
    const int x = 41;
    std::cout << 41;
    for (int i = 0; i < 41; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_42(int& val) {
    func_in_42(val);
    const int x = 42;
    std::cout << 42;
    for (int i = 0; i < 42; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_42(int& val) {
    const int x = 42;
    std::cout << 42;
    for (int i = 0; i < 42; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_43(int& val) {
    func_in_43(val);
    const int x = 43;
    std::cout << 43;
    for (int i = 0; i < 43; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_43(int& val) {
    const int x = 43;
    std::cout << 43;
    for (int i = 0; i < 43; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_44(int& val) {
    func_in_44(val);
    const int x = 44;
    std::cout << 44;
    for (int i = 0; i < 44; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_44(int& val) {
    const int x = 44;
    std::cout << 44;
    for (int i = 0; i < 44; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_45(int& val) {
    func_in_45(val);
    const int x = 45;
    std::cout << 45;
    for (int i = 0; i < 45; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_45(int& val) {
    const int x = 45;
    std::cout << 45;
    for (int i = 0; i < 45; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_46(int& val) {
    func_in_46(val);
    const int x = 46;
    std::cout << 46;
    for (int i = 0; i < 46; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_46(int& val) {
    const int x = 46;
    std::cout << 46;
    for (int i = 0; i < 46; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_47(int& val) {
    func_in_47(val);
    const int x = 47;
    std::cout << 47;
    for (int i = 0; i < 47; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_47(int& val) {
    const int x = 47;
    std::cout << 47;
    for (int i = 0; i < 47; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_48(int& val) {
    func_in_48(val);
    const int x = 48;
    std::cout << 48;
    for (int i = 0; i < 48; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_48(int& val) {
    const int x = 48;
    std::cout << 48;
    for (int i = 0; i < 48; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_49(int& val) {
    func_in_49(val);
    const int x = 49;
    std::cout << 49;
    for (int i = 0; i < 49; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_49(int& val) {
    const int x = 49;
    std::cout << 49;
    for (int i = 0; i < 49; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_50(int& val) {
    func_in_50(val);
    const int x = 50;
    std::cout << 50;
    for (int i = 0; i < 50; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_50(int& val) {
    const int x = 50;
    std::cout << 50;
    for (int i = 0; i < 50; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_51(int& val) {
    func_in_51(val);
    const int x = 51;
    std::cout << 51;
    for (int i = 0; i < 51; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_51(int& val) {
    const int x = 51;
    std::cout << 51;
    for (int i = 0; i < 51; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_52(int& val) {
    func_in_52(val);
    const int x = 52;
    std::cout << 52;
    for (int i = 0; i < 52; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_52(int& val) {
    const int x = 52;
    std::cout << 52;
    for (int i = 0; i < 52; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_53(int& val) {
    func_in_53(val);
    const int x = 53;
    std::cout << 53;
    for (int i = 0; i < 53; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_53(int& val) {
    const int x = 53;
    std::cout << 53;
    for (int i = 0; i < 53; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_54(int& val) {
    func_in_54(val);
    const int x = 54;
    std::cout << 54;
    for (int i = 0; i < 54; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_54(int& val) {
    const int x = 54;
    std::cout << 54;
    for (int i = 0; i < 54; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_55(int& val) {
    func_in_55(val);
    const int x = 55;
    std::cout << 55;
    for (int i = 0; i < 55; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_55(int& val) {
    const int x = 55;
    std::cout << 55;
    for (int i = 0; i < 55; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_56(int& val) {
    func_in_56(val);
    const int x = 56;
    std::cout << 56;
    for (int i = 0; i < 56; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_56(int& val) {
    const int x = 56;
    std::cout << 56;
    for (int i = 0; i < 56; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_57(int& val) {
    func_in_57(val);
    const int x = 57;
    std::cout << 57;
    for (int i = 0; i < 57; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_57(int& val) {
    const int x = 57;
    std::cout << 57;
    for (int i = 0; i < 57; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_58(int& val) {
    func_in_58(val);
    const int x = 58;
    std::cout << 58;
    for (int i = 0; i < 58; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_58(int& val) {
    const int x = 58;
    std::cout << 58;
    for (int i = 0; i < 58; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_59(int& val) {
    func_in_59(val);
    const int x = 59;
    std::cout << 59;
    for (int i = 0; i < 59; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_59(int& val) {
    const int x = 59;
    std::cout << 59;
    for (int i = 0; i < 59; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_60(int& val) {
    func_in_60(val);
    const int x = 60;
    std::cout << 60;
    for (int i = 0; i < 60; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_60(int& val) {
    const int x = 60;
    std::cout << 60;
    for (int i = 0; i < 60; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_61(int& val) {
    func_in_61(val);
    const int x = 61;
    std::cout << 61;
    for (int i = 0; i < 61; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_61(int& val) {
    const int x = 61;
    std::cout << 61;
    for (int i = 0; i < 61; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_62(int& val) {
    func_in_62(val);
    const int x = 62;
    std::cout << 62;
    for (int i = 0; i < 62; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_62(int& val) {
    const int x = 62;
    std::cout << 62;
    for (int i = 0; i < 62; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_63(int& val) {
    func_in_63(val);
    const int x = 63;
    std::cout << 63;
    for (int i = 0; i < 63; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_63(int& val) {
    const int x = 63;
    std::cout << 63;
    for (int i = 0; i < 63; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_64(int& val) {
    func_in_64(val);
    const int x = 64;
    std::cout << 64;
    for (int i = 0; i < 64; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_64(int& val) {
    const int x = 64;
    std::cout << 64;
    for (int i = 0; i < 64; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_65(int& val) {
    func_in_65(val);
    const int x = 65;
    std::cout << 65;
    for (int i = 0; i < 65; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_65(int& val) {
    const int x = 65;
    std::cout << 65;
    for (int i = 0; i < 65; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_66(int& val) {
    func_in_66(val);
    const int x = 66;
    std::cout << 66;
    for (int i = 0; i < 66; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_66(int& val) {
    const int x = 66;
    std::cout << 66;
    for (int i = 0; i < 66; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_67(int& val) {
    func_in_67(val);
    const int x = 67;
    std::cout << 67;
    for (int i = 0; i < 67; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_67(int& val) {
    const int x = 67;
    std::cout << 67;
    for (int i = 0; i < 67; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_68(int& val) {
    func_in_68(val);
    const int x = 68;
    std::cout << 68;
    for (int i = 0; i < 68; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_68(int& val) {
    const int x = 68;
    std::cout << 68;
    for (int i = 0; i < 68; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_69(int& val) {
    func_in_69(val);
    const int x = 69;
    std::cout << 69;
    for (int i = 0; i < 69; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_69(int& val) {
    const int x = 69;
    std::cout << 69;
    for (int i = 0; i < 69; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_70(int& val) {
    func_in_70(val);
    const int x = 70;
    std::cout << 70;
    for (int i = 0; i < 70; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_70(int& val) {
    const int x = 70;
    std::cout << 70;
    for (int i = 0; i < 70; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_71(int& val) {
    func_in_71(val);
    const int x = 71;
    std::cout << 71;
    for (int i = 0; i < 71; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_71(int& val) {
    const int x = 71;
    std::cout << 71;
    for (int i = 0; i < 71; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_72(int& val) {
    func_in_72(val);
    const int x = 72;
    std::cout << 72;
    for (int i = 0; i < 72; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_72(int& val) {
    const int x = 72;
    std::cout << 72;
    for (int i = 0; i < 72; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_73(int& val) {
    func_in_73(val);
    const int x = 73;
    std::cout << 73;
    for (int i = 0; i < 73; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_73(int& val) {
    const int x = 73;
    std::cout << 73;
    for (int i = 0; i < 73; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_74(int& val) {
    func_in_74(val);
    const int x = 74;
    std::cout << 74;
    for (int i = 0; i < 74; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_74(int& val) {
    const int x = 74;
    std::cout << 74;
    for (int i = 0; i < 74; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_75(int& val) {
    func_in_75(val);
    const int x = 75;
    std::cout << 75;
    for (int i = 0; i < 75; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_75(int& val) {
    const int x = 75;
    std::cout << 75;
    for (int i = 0; i < 75; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_76(int& val) {
    func_in_76(val);
    const int x = 76;
    std::cout << 76;
    for (int i = 0; i < 76; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_76(int& val) {
    const int x = 76;
    std::cout << 76;
    for (int i = 0; i < 76; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_77(int& val) {
    func_in_77(val);
    const int x = 77;
    std::cout << 77;
    for (int i = 0; i < 77; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_77(int& val) {
    const int x = 77;
    std::cout << 77;
    for (int i = 0; i < 77; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_78(int& val) {
    func_in_78(val);
    const int x = 78;
    std::cout << 78;
    for (int i = 0; i < 78; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_78(int& val) {
    const int x = 78;
    std::cout << 78;
    for (int i = 0; i < 78; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_79(int& val) {
    func_in_79(val);
    const int x = 79;
    std::cout << 79;
    for (int i = 0; i < 79; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_79(int& val) {
    const int x = 79;
    std::cout << 79;
    for (int i = 0; i < 79; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_80(int& val) {
    func_in_80(val);
    const int x = 80;
    std::cout << 80;
    for (int i = 0; i < 80; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_80(int& val) {
    const int x = 80;
    std::cout << 80;
    for (int i = 0; i < 80; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_81(int& val) {
    func_in_81(val);
    const int x = 81;
    std::cout << 81;
    for (int i = 0; i < 81; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_81(int& val) {
    const int x = 81;
    std::cout << 81;
    for (int i = 0; i < 81; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_82(int& val) {
    func_in_82(val);
    const int x = 82;
    std::cout << 82;
    for (int i = 0; i < 82; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_82(int& val) {
    const int x = 82;
    std::cout << 82;
    for (int i = 0; i < 82; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_83(int& val) {
    func_in_83(val);
    const int x = 83;
    std::cout << 83;
    for (int i = 0; i < 83; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_83(int& val) {
    const int x = 83;
    std::cout << 83;
    for (int i = 0; i < 83; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_84(int& val) {
    func_in_84(val);
    const int x = 84;
    std::cout << 84;
    for (int i = 0; i < 84; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_84(int& val) {
    const int x = 84;
    std::cout << 84;
    for (int i = 0; i < 84; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_85(int& val) {
    func_in_85(val);
    const int x = 85;
    std::cout << 85;
    for (int i = 0; i < 85; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_85(int& val) {
    const int x = 85;
    std::cout << 85;
    for (int i = 0; i < 85; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_86(int& val) {
    func_in_86(val);
    const int x = 86;
    std::cout << 86;
    for (int i = 0; i < 86; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_86(int& val) {
    const int x = 86;
    std::cout << 86;
    for (int i = 0; i < 86; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_87(int& val) {
    func_in_87(val);
    const int x = 87;
    std::cout << 87;
    for (int i = 0; i < 87; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_87(int& val) {
    const int x = 87;
    std::cout << 87;
    for (int i = 0; i < 87; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_88(int& val) {
    func_in_88(val);
    const int x = 88;
    std::cout << 88;
    for (int i = 0; i < 88; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_88(int& val) {
    const int x = 88;
    std::cout << 88;
    for (int i = 0; i < 88; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_89(int& val) {
    func_in_89(val);
    const int x = 89;
    std::cout << 89;
    for (int i = 0; i < 89; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_89(int& val) {
    const int x = 89;
    std::cout << 89;
    for (int i = 0; i < 89; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_90(int& val) {
    func_in_90(val);
    const int x = 90;
    std::cout << 90;
    for (int i = 0; i < 90; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_90(int& val) {
    const int x = 90;
    std::cout << 90;
    for (int i = 0; i < 90; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_91(int& val) {
    func_in_91(val);
    const int x = 91;
    std::cout << 91;
    for (int i = 0; i < 91; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_91(int& val) {
    const int x = 91;
    std::cout << 91;
    for (int i = 0; i < 91; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_92(int& val) {
    func_in_92(val);
    const int x = 92;
    std::cout << 92;
    for (int i = 0; i < 92; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_92(int& val) {
    const int x = 92;
    std::cout << 92;
    for (int i = 0; i < 92; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_93(int& val) {
    func_in_93(val);
    const int x = 93;
    std::cout << 93;
    for (int i = 0; i < 93; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_93(int& val) {
    const int x = 93;
    std::cout << 93;
    for (int i = 0; i < 93; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_94(int& val) {
    func_in_94(val);
    const int x = 94;
    std::cout << 94;
    for (int i = 0; i < 94; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_94(int& val) {
    const int x = 94;
    std::cout << 94;
    for (int i = 0; i < 94; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_95(int& val) {
    func_in_95(val);
    const int x = 95;
    std::cout << 95;
    for (int i = 0; i < 95; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_95(int& val) {
    const int x = 95;
    std::cout << 95;
    for (int i = 0; i < 95; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_96(int& val) {
    func_in_96(val);
    const int x = 96;
    std::cout << 96;
    for (int i = 0; i < 96; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_96(int& val) {
    const int x = 96;
    std::cout << 96;
    for (int i = 0; i < 96; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_97(int& val) {
    func_in_97(val);
    const int x = 97;
    std::cout << 97;
    for (int i = 0; i < 97; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_97(int& val) {
    const int x = 97;
    std::cout << 97;
    for (int i = 0; i < 97; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_98(int& val) {
    func_in_98(val);
    const int x = 98;
    std::cout << 98;
    for (int i = 0; i < 98; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_98(int& val) {
    const int x = 98;
    std::cout << 98;
    for (int i = 0; i < 98; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_99(int& val) {
    func_in_99(val);
    const int x = 99;
    std::cout << 99;
    for (int i = 0; i < 99; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_99(int& val) {
    const int x = 99;
    std::cout << 99;
    for (int i = 0; i < 99; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_100(int& val) {
    func_in_100(val);
    const int x = 100;
    std::cout << 100;
    for (int i = 0; i < 100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_100(int& val) {
    const int x = 100;
    std::cout << 100;
    for (int i = 0; i < 100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_101(int& val) {
    func_in_101(val);
    const int x = 101;
    std::cout << 101;
    for (int i = 0; i < 101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_101(int& val) {
    const int x = 101;
    std::cout << 101;
    for (int i = 0; i < 101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_102(int& val) {
    func_in_102(val);
    const int x = 102;
    std::cout << 102;
    for (int i = 0; i < 102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_102(int& val) {
    const int x = 102;
    std::cout << 102;
    for (int i = 0; i < 102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_103(int& val) {
    func_in_103(val);
    const int x = 103;
    std::cout << 103;
    for (int i = 0; i < 103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_103(int& val) {
    const int x = 103;
    std::cout << 103;
    for (int i = 0; i < 103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_104(int& val) {
    func_in_104(val);
    const int x = 104;
    std::cout << 104;
    for (int i = 0; i < 104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_104(int& val) {
    const int x = 104;
    std::cout << 104;
    for (int i = 0; i < 104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_105(int& val) {
    func_in_105(val);
    const int x = 105;
    std::cout << 105;
    for (int i = 0; i < 105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_105(int& val) {
    const int x = 105;
    std::cout << 105;
    for (int i = 0; i < 105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_106(int& val) {
    func_in_106(val);
    const int x = 106;
    std::cout << 106;
    for (int i = 0; i < 106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_106(int& val) {
    const int x = 106;
    std::cout << 106;
    for (int i = 0; i < 106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_107(int& val) {
    func_in_107(val);
    const int x = 107;
    std::cout << 107;
    for (int i = 0; i < 107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_107(int& val) {
    const int x = 107;
    std::cout << 107;
    for (int i = 0; i < 107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_108(int& val) {
    func_in_108(val);
    const int x = 108;
    std::cout << 108;
    for (int i = 0; i < 108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_108(int& val) {
    const int x = 108;
    std::cout << 108;
    for (int i = 0; i < 108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_109(int& val) {
    func_in_109(val);
    const int x = 109;
    std::cout << 109;
    for (int i = 0; i < 109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_109(int& val) {
    const int x = 109;
    std::cout << 109;
    for (int i = 0; i < 109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_110(int& val) {
    func_in_110(val);
    const int x = 110;
    std::cout << 110;
    for (int i = 0; i < 110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_110(int& val) {
    const int x = 110;
    std::cout << 110;
    for (int i = 0; i < 110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_111(int& val) {
    func_in_111(val);
    const int x = 111;
    std::cout << 111;
    for (int i = 0; i < 111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_111(int& val) {
    const int x = 111;
    std::cout << 111;
    for (int i = 0; i < 111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_112(int& val) {
    func_in_112(val);
    const int x = 112;
    std::cout << 112;
    for (int i = 0; i < 112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_112(int& val) {
    const int x = 112;
    std::cout << 112;
    for (int i = 0; i < 112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_113(int& val) {
    func_in_113(val);
    const int x = 113;
    std::cout << 113;
    for (int i = 0; i < 113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_113(int& val) {
    const int x = 113;
    std::cout << 113;
    for (int i = 0; i < 113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_114(int& val) {
    func_in_114(val);
    const int x = 114;
    std::cout << 114;
    for (int i = 0; i < 114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_114(int& val) {
    const int x = 114;
    std::cout << 114;
    for (int i = 0; i < 114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_115(int& val) {
    func_in_115(val);
    const int x = 115;
    std::cout << 115;
    for (int i = 0; i < 115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_115(int& val) {
    const int x = 115;
    std::cout << 115;
    for (int i = 0; i < 115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_116(int& val) {
    func_in_116(val);
    const int x = 116;
    std::cout << 116;
    for (int i = 0; i < 116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_116(int& val) {
    const int x = 116;
    std::cout << 116;
    for (int i = 0; i < 116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_117(int& val) {
    func_in_117(val);
    const int x = 117;
    std::cout << 117;
    for (int i = 0; i < 117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_117(int& val) {
    const int x = 117;
    std::cout << 117;
    for (int i = 0; i < 117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_118(int& val) {
    func_in_118(val);
    const int x = 118;
    std::cout << 118;
    for (int i = 0; i < 118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_118(int& val) {
    const int x = 118;
    std::cout << 118;
    for (int i = 0; i < 118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_119(int& val) {
    func_in_119(val);
    const int x = 119;
    std::cout << 119;
    for (int i = 0; i < 119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_119(int& val) {
    const int x = 119;
    std::cout << 119;
    for (int i = 0; i < 119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_120(int& val) {
    func_in_120(val);
    const int x = 120;
    std::cout << 120;
    for (int i = 0; i < 120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_120(int& val) {
    const int x = 120;
    std::cout << 120;
    for (int i = 0; i < 120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_121(int& val) {
    func_in_121(val);
    const int x = 121;
    std::cout << 121;
    for (int i = 0; i < 121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_121(int& val) {
    const int x = 121;
    std::cout << 121;
    for (int i = 0; i < 121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_122(int& val) {
    func_in_122(val);
    const int x = 122;
    std::cout << 122;
    for (int i = 0; i < 122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_122(int& val) {
    const int x = 122;
    std::cout << 122;
    for (int i = 0; i < 122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_123(int& val) {
    func_in_123(val);
    const int x = 123;
    std::cout << 123;
    for (int i = 0; i < 123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_123(int& val) {
    const int x = 123;
    std::cout << 123;
    for (int i = 0; i < 123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_124(int& val) {
    func_in_124(val);
    const int x = 124;
    std::cout << 124;
    for (int i = 0; i < 124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_124(int& val) {
    const int x = 124;
    std::cout << 124;
    for (int i = 0; i < 124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_125(int& val) {
    func_in_125(val);
    const int x = 125;
    std::cout << 125;
    for (int i = 0; i < 125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_125(int& val) {
    const int x = 125;
    std::cout << 125;
    for (int i = 0; i < 125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_126(int& val) {
    func_in_126(val);
    const int x = 126;
    std::cout << 126;
    for (int i = 0; i < 126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_126(int& val) {
    const int x = 126;
    std::cout << 126;
    for (int i = 0; i < 126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_127(int& val) {
    func_in_127(val);
    const int x = 127;
    std::cout << 127;
    for (int i = 0; i < 127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_127(int& val) {
    const int x = 127;
    std::cout << 127;
    for (int i = 0; i < 127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_128(int& val) {
    func_in_128(val);
    const int x = 128;
    std::cout << 128;
    for (int i = 0; i < 128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_128(int& val) {
    const int x = 128;
    std::cout << 128;
    for (int i = 0; i < 128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_129(int& val) {
    func_in_129(val);
    const int x = 129;
    std::cout << 129;
    for (int i = 0; i < 129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_129(int& val) {
    const int x = 129;
    std::cout << 129;
    for (int i = 0; i < 129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_130(int& val) {
    func_in_130(val);
    const int x = 130;
    std::cout << 130;
    for (int i = 0; i < 130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_130(int& val) {
    const int x = 130;
    std::cout << 130;
    for (int i = 0; i < 130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_131(int& val) {
    func_in_131(val);
    const int x = 131;
    std::cout << 131;
    for (int i = 0; i < 131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_131(int& val) {
    const int x = 131;
    std::cout << 131;
    for (int i = 0; i < 131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_132(int& val) {
    func_in_132(val);
    const int x = 132;
    std::cout << 132;
    for (int i = 0; i < 132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_132(int& val) {
    const int x = 132;
    std::cout << 132;
    for (int i = 0; i < 132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_133(int& val) {
    func_in_133(val);
    const int x = 133;
    std::cout << 133;
    for (int i = 0; i < 133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_133(int& val) {
    const int x = 133;
    std::cout << 133;
    for (int i = 0; i < 133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_134(int& val) {
    func_in_134(val);
    const int x = 134;
    std::cout << 134;
    for (int i = 0; i < 134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_134(int& val) {
    const int x = 134;
    std::cout << 134;
    for (int i = 0; i < 134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_135(int& val) {
    func_in_135(val);
    const int x = 135;
    std::cout << 135;
    for (int i = 0; i < 135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_135(int& val) {
    const int x = 135;
    std::cout << 135;
    for (int i = 0; i < 135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_136(int& val) {
    func_in_136(val);
    const int x = 136;
    std::cout << 136;
    for (int i = 0; i < 136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_136(int& val) {
    const int x = 136;
    std::cout << 136;
    for (int i = 0; i < 136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_137(int& val) {
    func_in_137(val);
    const int x = 137;
    std::cout << 137;
    for (int i = 0; i < 137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_137(int& val) {
    const int x = 137;
    std::cout << 137;
    for (int i = 0; i < 137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_138(int& val) {
    func_in_138(val);
    const int x = 138;
    std::cout << 138;
    for (int i = 0; i < 138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_138(int& val) {
    const int x = 138;
    std::cout << 138;
    for (int i = 0; i < 138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_139(int& val) {
    func_in_139(val);
    const int x = 139;
    std::cout << 139;
    for (int i = 0; i < 139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_139(int& val) {
    const int x = 139;
    std::cout << 139;
    for (int i = 0; i < 139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_140(int& val) {
    func_in_140(val);
    const int x = 140;
    std::cout << 140;
    for (int i = 0; i < 140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_140(int& val) {
    const int x = 140;
    std::cout << 140;
    for (int i = 0; i < 140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_141(int& val) {
    func_in_141(val);
    const int x = 141;
    std::cout << 141;
    for (int i = 0; i < 141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_141(int& val) {
    const int x = 141;
    std::cout << 141;
    for (int i = 0; i < 141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_142(int& val) {
    func_in_142(val);
    const int x = 142;
    std::cout << 142;
    for (int i = 0; i < 142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_142(int& val) {
    const int x = 142;
    std::cout << 142;
    for (int i = 0; i < 142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_143(int& val) {
    func_in_143(val);
    const int x = 143;
    std::cout << 143;
    for (int i = 0; i < 143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_143(int& val) {
    const int x = 143;
    std::cout << 143;
    for (int i = 0; i < 143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_144(int& val) {
    func_in_144(val);
    const int x = 144;
    std::cout << 144;
    for (int i = 0; i < 144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_144(int& val) {
    const int x = 144;
    std::cout << 144;
    for (int i = 0; i < 144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_145(int& val) {
    func_in_145(val);
    const int x = 145;
    std::cout << 145;
    for (int i = 0; i < 145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_145(int& val) {
    const int x = 145;
    std::cout << 145;
    for (int i = 0; i < 145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_146(int& val) {
    func_in_146(val);
    const int x = 146;
    std::cout << 146;
    for (int i = 0; i < 146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_146(int& val) {
    const int x = 146;
    std::cout << 146;
    for (int i = 0; i < 146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_147(int& val) {
    func_in_147(val);
    const int x = 147;
    std::cout << 147;
    for (int i = 0; i < 147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_147(int& val) {
    const int x = 147;
    std::cout << 147;
    for (int i = 0; i < 147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_148(int& val) {
    func_in_148(val);
    const int x = 148;
    std::cout << 148;
    for (int i = 0; i < 148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_148(int& val) {
    const int x = 148;
    std::cout << 148;
    for (int i = 0; i < 148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_149(int& val) {
    func_in_149(val);
    const int x = 149;
    std::cout << 149;
    for (int i = 0; i < 149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_149(int& val) {
    const int x = 149;
    std::cout << 149;
    for (int i = 0; i < 149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_150(int& val) {
    func_in_150(val);
    const int x = 150;
    std::cout << 150;
    for (int i = 0; i < 150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_150(int& val) {
    const int x = 150;
    std::cout << 150;
    for (int i = 0; i < 150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_151(int& val) {
    func_in_151(val);
    const int x = 151;
    std::cout << 151;
    for (int i = 0; i < 151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_151(int& val) {
    const int x = 151;
    std::cout << 151;
    for (int i = 0; i < 151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_152(int& val) {
    func_in_152(val);
    const int x = 152;
    std::cout << 152;
    for (int i = 0; i < 152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_152(int& val) {
    const int x = 152;
    std::cout << 152;
    for (int i = 0; i < 152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_153(int& val) {
    func_in_153(val);
    const int x = 153;
    std::cout << 153;
    for (int i = 0; i < 153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_153(int& val) {
    const int x = 153;
    std::cout << 153;
    for (int i = 0; i < 153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_154(int& val) {
    func_in_154(val);
    const int x = 154;
    std::cout << 154;
    for (int i = 0; i < 154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_154(int& val) {
    const int x = 154;
    std::cout << 154;
    for (int i = 0; i < 154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_155(int& val) {
    func_in_155(val);
    const int x = 155;
    std::cout << 155;
    for (int i = 0; i < 155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_155(int& val) {
    const int x = 155;
    std::cout << 155;
    for (int i = 0; i < 155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_156(int& val) {
    func_in_156(val);
    const int x = 156;
    std::cout << 156;
    for (int i = 0; i < 156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_156(int& val) {
    const int x = 156;
    std::cout << 156;
    for (int i = 0; i < 156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_157(int& val) {
    func_in_157(val);
    const int x = 157;
    std::cout << 157;
    for (int i = 0; i < 157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_157(int& val) {
    const int x = 157;
    std::cout << 157;
    for (int i = 0; i < 157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_158(int& val) {
    func_in_158(val);
    const int x = 158;
    std::cout << 158;
    for (int i = 0; i < 158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_158(int& val) {
    const int x = 158;
    std::cout << 158;
    for (int i = 0; i < 158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_159(int& val) {
    func_in_159(val);
    const int x = 159;
    std::cout << 159;
    for (int i = 0; i < 159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_159(int& val) {
    const int x = 159;
    std::cout << 159;
    for (int i = 0; i < 159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_160(int& val) {
    func_in_160(val);
    const int x = 160;
    std::cout << 160;
    for (int i = 0; i < 160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_160(int& val) {
    const int x = 160;
    std::cout << 160;
    for (int i = 0; i < 160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_161(int& val) {
    func_in_161(val);
    const int x = 161;
    std::cout << 161;
    for (int i = 0; i < 161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_161(int& val) {
    const int x = 161;
    std::cout << 161;
    for (int i = 0; i < 161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_162(int& val) {
    func_in_162(val);
    const int x = 162;
    std::cout << 162;
    for (int i = 0; i < 162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_162(int& val) {
    const int x = 162;
    std::cout << 162;
    for (int i = 0; i < 162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_163(int& val) {
    func_in_163(val);
    const int x = 163;
    std::cout << 163;
    for (int i = 0; i < 163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_163(int& val) {
    const int x = 163;
    std::cout << 163;
    for (int i = 0; i < 163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_164(int& val) {
    func_in_164(val);
    const int x = 164;
    std::cout << 164;
    for (int i = 0; i < 164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_164(int& val) {
    const int x = 164;
    std::cout << 164;
    for (int i = 0; i < 164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_165(int& val) {
    func_in_165(val);
    const int x = 165;
    std::cout << 165;
    for (int i = 0; i < 165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_165(int& val) {
    const int x = 165;
    std::cout << 165;
    for (int i = 0; i < 165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_166(int& val) {
    func_in_166(val);
    const int x = 166;
    std::cout << 166;
    for (int i = 0; i < 166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_166(int& val) {
    const int x = 166;
    std::cout << 166;
    for (int i = 0; i < 166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_167(int& val) {
    func_in_167(val);
    const int x = 167;
    std::cout << 167;
    for (int i = 0; i < 167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_167(int& val) {
    const int x = 167;
    std::cout << 167;
    for (int i = 0; i < 167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_168(int& val) {
    func_in_168(val);
    const int x = 168;
    std::cout << 168;
    for (int i = 0; i < 168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_168(int& val) {
    const int x = 168;
    std::cout << 168;
    for (int i = 0; i < 168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_169(int& val) {
    func_in_169(val);
    const int x = 169;
    std::cout << 169;
    for (int i = 0; i < 169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_169(int& val) {
    const int x = 169;
    std::cout << 169;
    for (int i = 0; i < 169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_170(int& val) {
    func_in_170(val);
    const int x = 170;
    std::cout << 170;
    for (int i = 0; i < 170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_170(int& val) {
    const int x = 170;
    std::cout << 170;
    for (int i = 0; i < 170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_171(int& val) {
    func_in_171(val);
    const int x = 171;
    std::cout << 171;
    for (int i = 0; i < 171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_171(int& val) {
    const int x = 171;
    std::cout << 171;
    for (int i = 0; i < 171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_172(int& val) {
    func_in_172(val);
    const int x = 172;
    std::cout << 172;
    for (int i = 0; i < 172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_172(int& val) {
    const int x = 172;
    std::cout << 172;
    for (int i = 0; i < 172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_173(int& val) {
    func_in_173(val);
    const int x = 173;
    std::cout << 173;
    for (int i = 0; i < 173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_173(int& val) {
    const int x = 173;
    std::cout << 173;
    for (int i = 0; i < 173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_174(int& val) {
    func_in_174(val);
    const int x = 174;
    std::cout << 174;
    for (int i = 0; i < 174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_174(int& val) {
    const int x = 174;
    std::cout << 174;
    for (int i = 0; i < 174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_175(int& val) {
    func_in_175(val);
    const int x = 175;
    std::cout << 175;
    for (int i = 0; i < 175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_175(int& val) {
    const int x = 175;
    std::cout << 175;
    for (int i = 0; i < 175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_176(int& val) {
    func_in_176(val);
    const int x = 176;
    std::cout << 176;
    for (int i = 0; i < 176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_176(int& val) {
    const int x = 176;
    std::cout << 176;
    for (int i = 0; i < 176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_177(int& val) {
    func_in_177(val);
    const int x = 177;
    std::cout << 177;
    for (int i = 0; i < 177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_177(int& val) {
    const int x = 177;
    std::cout << 177;
    for (int i = 0; i < 177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_178(int& val) {
    func_in_178(val);
    const int x = 178;
    std::cout << 178;
    for (int i = 0; i < 178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_178(int& val) {
    const int x = 178;
    std::cout << 178;
    for (int i = 0; i < 178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_179(int& val) {
    func_in_179(val);
    const int x = 179;
    std::cout << 179;
    for (int i = 0; i < 179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_179(int& val) {
    const int x = 179;
    std::cout << 179;
    for (int i = 0; i < 179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_180(int& val) {
    func_in_180(val);
    const int x = 180;
    std::cout << 180;
    for (int i = 0; i < 180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_180(int& val) {
    const int x = 180;
    std::cout << 180;
    for (int i = 0; i < 180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_181(int& val) {
    func_in_181(val);
    const int x = 181;
    std::cout << 181;
    for (int i = 0; i < 181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_181(int& val) {
    const int x = 181;
    std::cout << 181;
    for (int i = 0; i < 181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_182(int& val) {
    func_in_182(val);
    const int x = 182;
    std::cout << 182;
    for (int i = 0; i < 182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_182(int& val) {
    const int x = 182;
    std::cout << 182;
    for (int i = 0; i < 182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_183(int& val) {
    func_in_183(val);
    const int x = 183;
    std::cout << 183;
    for (int i = 0; i < 183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_183(int& val) {
    const int x = 183;
    std::cout << 183;
    for (int i = 0; i < 183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_184(int& val) {
    func_in_184(val);
    const int x = 184;
    std::cout << 184;
    for (int i = 0; i < 184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_184(int& val) {
    const int x = 184;
    std::cout << 184;
    for (int i = 0; i < 184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_185(int& val) {
    func_in_185(val);
    const int x = 185;
    std::cout << 185;
    for (int i = 0; i < 185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_185(int& val) {
    const int x = 185;
    std::cout << 185;
    for (int i = 0; i < 185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_186(int& val) {
    func_in_186(val);
    const int x = 186;
    std::cout << 186;
    for (int i = 0; i < 186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_186(int& val) {
    const int x = 186;
    std::cout << 186;
    for (int i = 0; i < 186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_187(int& val) {
    func_in_187(val);
    const int x = 187;
    std::cout << 187;
    for (int i = 0; i < 187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_187(int& val) {
    const int x = 187;
    std::cout << 187;
    for (int i = 0; i < 187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_188(int& val) {
    func_in_188(val);
    const int x = 188;
    std::cout << 188;
    for (int i = 0; i < 188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_188(int& val) {
    const int x = 188;
    std::cout << 188;
    for (int i = 0; i < 188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_189(int& val) {
    func_in_189(val);
    const int x = 189;
    std::cout << 189;
    for (int i = 0; i < 189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_189(int& val) {
    const int x = 189;
    std::cout << 189;
    for (int i = 0; i < 189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_190(int& val) {
    func_in_190(val);
    const int x = 190;
    std::cout << 190;
    for (int i = 0; i < 190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_190(int& val) {
    const int x = 190;
    std::cout << 190;
    for (int i = 0; i < 190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_191(int& val) {
    func_in_191(val);
    const int x = 191;
    std::cout << 191;
    for (int i = 0; i < 191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_191(int& val) {
    const int x = 191;
    std::cout << 191;
    for (int i = 0; i < 191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_192(int& val) {
    func_in_192(val);
    const int x = 192;
    std::cout << 192;
    for (int i = 0; i < 192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_192(int& val) {
    const int x = 192;
    std::cout << 192;
    for (int i = 0; i < 192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_193(int& val) {
    func_in_193(val);
    const int x = 193;
    std::cout << 193;
    for (int i = 0; i < 193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_193(int& val) {
    const int x = 193;
    std::cout << 193;
    for (int i = 0; i < 193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_194(int& val) {
    func_in_194(val);
    const int x = 194;
    std::cout << 194;
    for (int i = 0; i < 194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_194(int& val) {
    const int x = 194;
    std::cout << 194;
    for (int i = 0; i < 194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_195(int& val) {
    func_in_195(val);
    const int x = 195;
    std::cout << 195;
    for (int i = 0; i < 195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_195(int& val) {
    const int x = 195;
    std::cout << 195;
    for (int i = 0; i < 195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_196(int& val) {
    func_in_196(val);
    const int x = 196;
    std::cout << 196;
    for (int i = 0; i < 196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_196(int& val) {
    const int x = 196;
    std::cout << 196;
    for (int i = 0; i < 196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_197(int& val) {
    func_in_197(val);
    const int x = 197;
    std::cout << 197;
    for (int i = 0; i < 197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_197(int& val) {
    const int x = 197;
    std::cout << 197;
    for (int i = 0; i < 197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_198(int& val) {
    func_in_198(val);
    const int x = 198;
    std::cout << 198;
    for (int i = 0; i < 198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_198(int& val) {
    const int x = 198;
    std::cout << 198;
    for (int i = 0; i < 198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_199(int& val) {
    func_in_199(val);
    const int x = 199;
    std::cout << 199;
    for (int i = 0; i < 199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_199(int& val) {
    const int x = 199;
    std::cout << 199;
    for (int i = 0; i < 199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_200(int& val) {
    func_in_200(val);
    const int x = 200;
    std::cout << 200;
    for (int i = 0; i < 200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_200(int& val) {
    const int x = 200;
    std::cout << 200;
    for (int i = 0; i < 200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_201(int& val) {
    func_in_201(val);
    const int x = 201;
    std::cout << 201;
    for (int i = 0; i < 201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_201(int& val) {
    const int x = 201;
    std::cout << 201;
    for (int i = 0; i < 201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_202(int& val) {
    func_in_202(val);
    const int x = 202;
    std::cout << 202;
    for (int i = 0; i < 202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_202(int& val) {
    const int x = 202;
    std::cout << 202;
    for (int i = 0; i < 202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_203(int& val) {
    func_in_203(val);
    const int x = 203;
    std::cout << 203;
    for (int i = 0; i < 203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_203(int& val) {
    const int x = 203;
    std::cout << 203;
    for (int i = 0; i < 203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_204(int& val) {
    func_in_204(val);
    const int x = 204;
    std::cout << 204;
    for (int i = 0; i < 204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_204(int& val) {
    const int x = 204;
    std::cout << 204;
    for (int i = 0; i < 204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_205(int& val) {
    func_in_205(val);
    const int x = 205;
    std::cout << 205;
    for (int i = 0; i < 205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_205(int& val) {
    const int x = 205;
    std::cout << 205;
    for (int i = 0; i < 205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_206(int& val) {
    func_in_206(val);
    const int x = 206;
    std::cout << 206;
    for (int i = 0; i < 206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_206(int& val) {
    const int x = 206;
    std::cout << 206;
    for (int i = 0; i < 206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_207(int& val) {
    func_in_207(val);
    const int x = 207;
    std::cout << 207;
    for (int i = 0; i < 207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_207(int& val) {
    const int x = 207;
    std::cout << 207;
    for (int i = 0; i < 207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_208(int& val) {
    func_in_208(val);
    const int x = 208;
    std::cout << 208;
    for (int i = 0; i < 208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_208(int& val) {
    const int x = 208;
    std::cout << 208;
    for (int i = 0; i < 208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_209(int& val) {
    func_in_209(val);
    const int x = 209;
    std::cout << 209;
    for (int i = 0; i < 209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_209(int& val) {
    const int x = 209;
    std::cout << 209;
    for (int i = 0; i < 209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_210(int& val) {
    func_in_210(val);
    const int x = 210;
    std::cout << 210;
    for (int i = 0; i < 210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_210(int& val) {
    const int x = 210;
    std::cout << 210;
    for (int i = 0; i < 210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_211(int& val) {
    func_in_211(val);
    const int x = 211;
    std::cout << 211;
    for (int i = 0; i < 211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_211(int& val) {
    const int x = 211;
    std::cout << 211;
    for (int i = 0; i < 211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_212(int& val) {
    func_in_212(val);
    const int x = 212;
    std::cout << 212;
    for (int i = 0; i < 212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_212(int& val) {
    const int x = 212;
    std::cout << 212;
    for (int i = 0; i < 212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_213(int& val) {
    func_in_213(val);
    const int x = 213;
    std::cout << 213;
    for (int i = 0; i < 213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_213(int& val) {
    const int x = 213;
    std::cout << 213;
    for (int i = 0; i < 213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_214(int& val) {
    func_in_214(val);
    const int x = 214;
    std::cout << 214;
    for (int i = 0; i < 214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_214(int& val) {
    const int x = 214;
    std::cout << 214;
    for (int i = 0; i < 214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_215(int& val) {
    func_in_215(val);
    const int x = 215;
    std::cout << 215;
    for (int i = 0; i < 215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_215(int& val) {
    const int x = 215;
    std::cout << 215;
    for (int i = 0; i < 215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_216(int& val) {
    func_in_216(val);
    const int x = 216;
    std::cout << 216;
    for (int i = 0; i < 216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_216(int& val) {
    const int x = 216;
    std::cout << 216;
    for (int i = 0; i < 216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_217(int& val) {
    func_in_217(val);
    const int x = 217;
    std::cout << 217;
    for (int i = 0; i < 217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_217(int& val) {
    const int x = 217;
    std::cout << 217;
    for (int i = 0; i < 217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_218(int& val) {
    func_in_218(val);
    const int x = 218;
    std::cout << 218;
    for (int i = 0; i < 218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_218(int& val) {
    const int x = 218;
    std::cout << 218;
    for (int i = 0; i < 218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_219(int& val) {
    func_in_219(val);
    const int x = 219;
    std::cout << 219;
    for (int i = 0; i < 219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_219(int& val) {
    const int x = 219;
    std::cout << 219;
    for (int i = 0; i < 219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_220(int& val) {
    func_in_220(val);
    const int x = 220;
    std::cout << 220;
    for (int i = 0; i < 220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_220(int& val) {
    const int x = 220;
    std::cout << 220;
    for (int i = 0; i < 220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_221(int& val) {
    func_in_221(val);
    const int x = 221;
    std::cout << 221;
    for (int i = 0; i < 221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_221(int& val) {
    const int x = 221;
    std::cout << 221;
    for (int i = 0; i < 221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_222(int& val) {
    func_in_222(val);
    const int x = 222;
    std::cout << 222;
    for (int i = 0; i < 222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_222(int& val) {
    const int x = 222;
    std::cout << 222;
    for (int i = 0; i < 222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_223(int& val) {
    func_in_223(val);
    const int x = 223;
    std::cout << 223;
    for (int i = 0; i < 223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_223(int& val) {
    const int x = 223;
    std::cout << 223;
    for (int i = 0; i < 223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_224(int& val) {
    func_in_224(val);
    const int x = 224;
    std::cout << 224;
    for (int i = 0; i < 224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_224(int& val) {
    const int x = 224;
    std::cout << 224;
    for (int i = 0; i < 224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_225(int& val) {
    func_in_225(val);
    const int x = 225;
    std::cout << 225;
    for (int i = 0; i < 225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_225(int& val) {
    const int x = 225;
    std::cout << 225;
    for (int i = 0; i < 225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_226(int& val) {
    func_in_226(val);
    const int x = 226;
    std::cout << 226;
    for (int i = 0; i < 226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_226(int& val) {
    const int x = 226;
    std::cout << 226;
    for (int i = 0; i < 226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_227(int& val) {
    func_in_227(val);
    const int x = 227;
    std::cout << 227;
    for (int i = 0; i < 227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_227(int& val) {
    const int x = 227;
    std::cout << 227;
    for (int i = 0; i < 227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_228(int& val) {
    func_in_228(val);
    const int x = 228;
    std::cout << 228;
    for (int i = 0; i < 228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_228(int& val) {
    const int x = 228;
    std::cout << 228;
    for (int i = 0; i < 228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_229(int& val) {
    func_in_229(val);
    const int x = 229;
    std::cout << 229;
    for (int i = 0; i < 229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_229(int& val) {
    const int x = 229;
    std::cout << 229;
    for (int i = 0; i < 229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_230(int& val) {
    func_in_230(val);
    const int x = 230;
    std::cout << 230;
    for (int i = 0; i < 230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_230(int& val) {
    const int x = 230;
    std::cout << 230;
    for (int i = 0; i < 230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_231(int& val) {
    func_in_231(val);
    const int x = 231;
    std::cout << 231;
    for (int i = 0; i < 231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_231(int& val) {
    const int x = 231;
    std::cout << 231;
    for (int i = 0; i < 231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_232(int& val) {
    func_in_232(val);
    const int x = 232;
    std::cout << 232;
    for (int i = 0; i < 232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_232(int& val) {
    const int x = 232;
    std::cout << 232;
    for (int i = 0; i < 232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_233(int& val) {
    func_in_233(val);
    const int x = 233;
    std::cout << 233;
    for (int i = 0; i < 233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_233(int& val) {
    const int x = 233;
    std::cout << 233;
    for (int i = 0; i < 233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_234(int& val) {
    func_in_234(val);
    const int x = 234;
    std::cout << 234;
    for (int i = 0; i < 234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_234(int& val) {
    const int x = 234;
    std::cout << 234;
    for (int i = 0; i < 234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_235(int& val) {
    func_in_235(val);
    const int x = 235;
    std::cout << 235;
    for (int i = 0; i < 235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_235(int& val) {
    const int x = 235;
    std::cout << 235;
    for (int i = 0; i < 235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_236(int& val) {
    func_in_236(val);
    const int x = 236;
    std::cout << 236;
    for (int i = 0; i < 236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_236(int& val) {
    const int x = 236;
    std::cout << 236;
    for (int i = 0; i < 236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_237(int& val) {
    func_in_237(val);
    const int x = 237;
    std::cout << 237;
    for (int i = 0; i < 237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_237(int& val) {
    const int x = 237;
    std::cout << 237;
    for (int i = 0; i < 237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_238(int& val) {
    func_in_238(val);
    const int x = 238;
    std::cout << 238;
    for (int i = 0; i < 238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_238(int& val) {
    const int x = 238;
    std::cout << 238;
    for (int i = 0; i < 238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_239(int& val) {
    func_in_239(val);
    const int x = 239;
    std::cout << 239;
    for (int i = 0; i < 239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_239(int& val) {
    const int x = 239;
    std::cout << 239;
    for (int i = 0; i < 239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_240(int& val) {
    func_in_240(val);
    const int x = 240;
    std::cout << 240;
    for (int i = 0; i < 240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_240(int& val) {
    const int x = 240;
    std::cout << 240;
    for (int i = 0; i < 240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_241(int& val) {
    func_in_241(val);
    const int x = 241;
    std::cout << 241;
    for (int i = 0; i < 241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_241(int& val) {
    const int x = 241;
    std::cout << 241;
    for (int i = 0; i < 241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_242(int& val) {
    func_in_242(val);
    const int x = 242;
    std::cout << 242;
    for (int i = 0; i < 242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_242(int& val) {
    const int x = 242;
    std::cout << 242;
    for (int i = 0; i < 242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_243(int& val) {
    func_in_243(val);
    const int x = 243;
    std::cout << 243;
    for (int i = 0; i < 243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_243(int& val) {
    const int x = 243;
    std::cout << 243;
    for (int i = 0; i < 243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_244(int& val) {
    func_in_244(val);
    const int x = 244;
    std::cout << 244;
    for (int i = 0; i < 244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_244(int& val) {
    const int x = 244;
    std::cout << 244;
    for (int i = 0; i < 244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_245(int& val) {
    func_in_245(val);
    const int x = 245;
    std::cout << 245;
    for (int i = 0; i < 245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_245(int& val) {
    const int x = 245;
    std::cout << 245;
    for (int i = 0; i < 245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_246(int& val) {
    func_in_246(val);
    const int x = 246;
    std::cout << 246;
    for (int i = 0; i < 246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_246(int& val) {
    const int x = 246;
    std::cout << 246;
    for (int i = 0; i < 246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_247(int& val) {
    func_in_247(val);
    const int x = 247;
    std::cout << 247;
    for (int i = 0; i < 247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_247(int& val) {
    const int x = 247;
    std::cout << 247;
    for (int i = 0; i < 247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_248(int& val) {
    func_in_248(val);
    const int x = 248;
    std::cout << 248;
    for (int i = 0; i < 248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_248(int& val) {
    const int x = 248;
    std::cout << 248;
    for (int i = 0; i < 248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_249(int& val) {
    func_in_249(val);
    const int x = 249;
    std::cout << 249;
    for (int i = 0; i < 249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_249(int& val) {
    const int x = 249;
    std::cout << 249;
    for (int i = 0; i < 249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_250(int& val) {
    func_in_250(val);
    const int x = 250;
    std::cout << 250;
    for (int i = 0; i < 250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_250(int& val) {
    const int x = 250;
    std::cout << 250;
    for (int i = 0; i < 250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_251(int& val) {
    func_in_251(val);
    const int x = 251;
    std::cout << 251;
    for (int i = 0; i < 251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_251(int& val) {
    const int x = 251;
    std::cout << 251;
    for (int i = 0; i < 251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_252(int& val) {
    func_in_252(val);
    const int x = 252;
    std::cout << 252;
    for (int i = 0; i < 252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_252(int& val) {
    const int x = 252;
    std::cout << 252;
    for (int i = 0; i < 252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_253(int& val) {
    func_in_253(val);
    const int x = 253;
    std::cout << 253;
    for (int i = 0; i < 253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_253(int& val) {
    const int x = 253;
    std::cout << 253;
    for (int i = 0; i < 253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_254(int& val) {
    func_in_254(val);
    const int x = 254;
    std::cout << 254;
    for (int i = 0; i < 254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_254(int& val) {
    const int x = 254;
    std::cout << 254;
    for (int i = 0; i < 254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_255(int& val) {
    func_in_255(val);
    const int x = 255;
    std::cout << 255;
    for (int i = 0; i < 255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_255(int& val) {
    const int x = 255;
    std::cout << 255;
    for (int i = 0; i < 255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_256(int& val) {
    func_in_256(val);
    const int x = 256;
    std::cout << 256;
    for (int i = 0; i < 256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_256(int& val) {
    const int x = 256;
    std::cout << 256;
    for (int i = 0; i < 256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_257(int& val) {
    func_in_257(val);
    const int x = 257;
    std::cout << 257;
    for (int i = 0; i < 257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_257(int& val) {
    const int x = 257;
    std::cout << 257;
    for (int i = 0; i < 257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_258(int& val) {
    func_in_258(val);
    const int x = 258;
    std::cout << 258;
    for (int i = 0; i < 258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_258(int& val) {
    const int x = 258;
    std::cout << 258;
    for (int i = 0; i < 258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_259(int& val) {
    func_in_259(val);
    const int x = 259;
    std::cout << 259;
    for (int i = 0; i < 259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_259(int& val) {
    const int x = 259;
    std::cout << 259;
    for (int i = 0; i < 259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_260(int& val) {
    func_in_260(val);
    const int x = 260;
    std::cout << 260;
    for (int i = 0; i < 260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_260(int& val) {
    const int x = 260;
    std::cout << 260;
    for (int i = 0; i < 260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_261(int& val) {
    func_in_261(val);
    const int x = 261;
    std::cout << 261;
    for (int i = 0; i < 261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_261(int& val) {
    const int x = 261;
    std::cout << 261;
    for (int i = 0; i < 261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_262(int& val) {
    func_in_262(val);
    const int x = 262;
    std::cout << 262;
    for (int i = 0; i < 262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_262(int& val) {
    const int x = 262;
    std::cout << 262;
    for (int i = 0; i < 262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_263(int& val) {
    func_in_263(val);
    const int x = 263;
    std::cout << 263;
    for (int i = 0; i < 263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_263(int& val) {
    const int x = 263;
    std::cout << 263;
    for (int i = 0; i < 263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_264(int& val) {
    func_in_264(val);
    const int x = 264;
    std::cout << 264;
    for (int i = 0; i < 264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_264(int& val) {
    const int x = 264;
    std::cout << 264;
    for (int i = 0; i < 264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_265(int& val) {
    func_in_265(val);
    const int x = 265;
    std::cout << 265;
    for (int i = 0; i < 265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_265(int& val) {
    const int x = 265;
    std::cout << 265;
    for (int i = 0; i < 265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_266(int& val) {
    func_in_266(val);
    const int x = 266;
    std::cout << 266;
    for (int i = 0; i < 266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_266(int& val) {
    const int x = 266;
    std::cout << 266;
    for (int i = 0; i < 266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_267(int& val) {
    func_in_267(val);
    const int x = 267;
    std::cout << 267;
    for (int i = 0; i < 267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_267(int& val) {
    const int x = 267;
    std::cout << 267;
    for (int i = 0; i < 267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_268(int& val) {
    func_in_268(val);
    const int x = 268;
    std::cout << 268;
    for (int i = 0; i < 268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_268(int& val) {
    const int x = 268;
    std::cout << 268;
    for (int i = 0; i < 268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_269(int& val) {
    func_in_269(val);
    const int x = 269;
    std::cout << 269;
    for (int i = 0; i < 269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_269(int& val) {
    const int x = 269;
    std::cout << 269;
    for (int i = 0; i < 269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_270(int& val) {
    func_in_270(val);
    const int x = 270;
    std::cout << 270;
    for (int i = 0; i < 270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_270(int& val) {
    const int x = 270;
    std::cout << 270;
    for (int i = 0; i < 270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_271(int& val) {
    func_in_271(val);
    const int x = 271;
    std::cout << 271;
    for (int i = 0; i < 271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_271(int& val) {
    const int x = 271;
    std::cout << 271;
    for (int i = 0; i < 271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_272(int& val) {
    func_in_272(val);
    const int x = 272;
    std::cout << 272;
    for (int i = 0; i < 272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_272(int& val) {
    const int x = 272;
    std::cout << 272;
    for (int i = 0; i < 272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_273(int& val) {
    func_in_273(val);
    const int x = 273;
    std::cout << 273;
    for (int i = 0; i < 273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_273(int& val) {
    const int x = 273;
    std::cout << 273;
    for (int i = 0; i < 273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_274(int& val) {
    func_in_274(val);
    const int x = 274;
    std::cout << 274;
    for (int i = 0; i < 274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_274(int& val) {
    const int x = 274;
    std::cout << 274;
    for (int i = 0; i < 274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_275(int& val) {
    func_in_275(val);
    const int x = 275;
    std::cout << 275;
    for (int i = 0; i < 275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_275(int& val) {
    const int x = 275;
    std::cout << 275;
    for (int i = 0; i < 275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_276(int& val) {
    func_in_276(val);
    const int x = 276;
    std::cout << 276;
    for (int i = 0; i < 276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_276(int& val) {
    const int x = 276;
    std::cout << 276;
    for (int i = 0; i < 276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_277(int& val) {
    func_in_277(val);
    const int x = 277;
    std::cout << 277;
    for (int i = 0; i < 277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_277(int& val) {
    const int x = 277;
    std::cout << 277;
    for (int i = 0; i < 277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_278(int& val) {
    func_in_278(val);
    const int x = 278;
    std::cout << 278;
    for (int i = 0; i < 278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_278(int& val) {
    const int x = 278;
    std::cout << 278;
    for (int i = 0; i < 278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_279(int& val) {
    func_in_279(val);
    const int x = 279;
    std::cout << 279;
    for (int i = 0; i < 279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_279(int& val) {
    const int x = 279;
    std::cout << 279;
    for (int i = 0; i < 279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_280(int& val) {
    func_in_280(val);
    const int x = 280;
    std::cout << 280;
    for (int i = 0; i < 280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_280(int& val) {
    const int x = 280;
    std::cout << 280;
    for (int i = 0; i < 280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_281(int& val) {
    func_in_281(val);
    const int x = 281;
    std::cout << 281;
    for (int i = 0; i < 281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_281(int& val) {
    const int x = 281;
    std::cout << 281;
    for (int i = 0; i < 281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_282(int& val) {
    func_in_282(val);
    const int x = 282;
    std::cout << 282;
    for (int i = 0; i < 282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_282(int& val) {
    const int x = 282;
    std::cout << 282;
    for (int i = 0; i < 282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_283(int& val) {
    func_in_283(val);
    const int x = 283;
    std::cout << 283;
    for (int i = 0; i < 283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_283(int& val) {
    const int x = 283;
    std::cout << 283;
    for (int i = 0; i < 283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_284(int& val) {
    func_in_284(val);
    const int x = 284;
    std::cout << 284;
    for (int i = 0; i < 284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_284(int& val) {
    const int x = 284;
    std::cout << 284;
    for (int i = 0; i < 284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_285(int& val) {
    func_in_285(val);
    const int x = 285;
    std::cout << 285;
    for (int i = 0; i < 285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_285(int& val) {
    const int x = 285;
    std::cout << 285;
    for (int i = 0; i < 285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_286(int& val) {
    func_in_286(val);
    const int x = 286;
    std::cout << 286;
    for (int i = 0; i < 286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_286(int& val) {
    const int x = 286;
    std::cout << 286;
    for (int i = 0; i < 286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_287(int& val) {
    func_in_287(val);
    const int x = 287;
    std::cout << 287;
    for (int i = 0; i < 287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_287(int& val) {
    const int x = 287;
    std::cout << 287;
    for (int i = 0; i < 287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_288(int& val) {
    func_in_288(val);
    const int x = 288;
    std::cout << 288;
    for (int i = 0; i < 288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_288(int& val) {
    const int x = 288;
    std::cout << 288;
    for (int i = 0; i < 288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_289(int& val) {
    func_in_289(val);
    const int x = 289;
    std::cout << 289;
    for (int i = 0; i < 289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_289(int& val) {
    const int x = 289;
    std::cout << 289;
    for (int i = 0; i < 289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_290(int& val) {
    func_in_290(val);
    const int x = 290;
    std::cout << 290;
    for (int i = 0; i < 290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_290(int& val) {
    const int x = 290;
    std::cout << 290;
    for (int i = 0; i < 290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_291(int& val) {
    func_in_291(val);
    const int x = 291;
    std::cout << 291;
    for (int i = 0; i < 291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_291(int& val) {
    const int x = 291;
    std::cout << 291;
    for (int i = 0; i < 291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_292(int& val) {
    func_in_292(val);
    const int x = 292;
    std::cout << 292;
    for (int i = 0; i < 292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_292(int& val) {
    const int x = 292;
    std::cout << 292;
    for (int i = 0; i < 292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_293(int& val) {
    func_in_293(val);
    const int x = 293;
    std::cout << 293;
    for (int i = 0; i < 293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_293(int& val) {
    const int x = 293;
    std::cout << 293;
    for (int i = 0; i < 293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_294(int& val) {
    func_in_294(val);
    const int x = 294;
    std::cout << 294;
    for (int i = 0; i < 294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_294(int& val) {
    const int x = 294;
    std::cout << 294;
    for (int i = 0; i < 294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_295(int& val) {
    func_in_295(val);
    const int x = 295;
    std::cout << 295;
    for (int i = 0; i < 295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_295(int& val) {
    const int x = 295;
    std::cout << 295;
    for (int i = 0; i < 295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_296(int& val) {
    func_in_296(val);
    const int x = 296;
    std::cout << 296;
    for (int i = 0; i < 296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_296(int& val) {
    const int x = 296;
    std::cout << 296;
    for (int i = 0; i < 296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_297(int& val) {
    func_in_297(val);
    const int x = 297;
    std::cout << 297;
    for (int i = 0; i < 297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_297(int& val) {
    const int x = 297;
    std::cout << 297;
    for (int i = 0; i < 297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_298(int& val) {
    func_in_298(val);
    const int x = 298;
    std::cout << 298;
    for (int i = 0; i < 298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_298(int& val) {
    const int x = 298;
    std::cout << 298;
    for (int i = 0; i < 298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_299(int& val) {
    func_in_299(val);
    const int x = 299;
    std::cout << 299;
    for (int i = 0; i < 299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_299(int& val) {
    const int x = 299;
    std::cout << 299;
    for (int i = 0; i < 299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_300(int& val) {
    func_in_300(val);
    const int x = 300;
    std::cout << 300;
    for (int i = 0; i < 300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_300(int& val) {
    const int x = 300;
    std::cout << 300;
    for (int i = 0; i < 300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_301(int& val) {
    func_in_301(val);
    const int x = 301;
    std::cout << 301;
    for (int i = 0; i < 301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_301(int& val) {
    const int x = 301;
    std::cout << 301;
    for (int i = 0; i < 301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_302(int& val) {
    func_in_302(val);
    const int x = 302;
    std::cout << 302;
    for (int i = 0; i < 302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_302(int& val) {
    const int x = 302;
    std::cout << 302;
    for (int i = 0; i < 302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_303(int& val) {
    func_in_303(val);
    const int x = 303;
    std::cout << 303;
    for (int i = 0; i < 303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_303(int& val) {
    const int x = 303;
    std::cout << 303;
    for (int i = 0; i < 303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_304(int& val) {
    func_in_304(val);
    const int x = 304;
    std::cout << 304;
    for (int i = 0; i < 304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_304(int& val) {
    const int x = 304;
    std::cout << 304;
    for (int i = 0; i < 304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_305(int& val) {
    func_in_305(val);
    const int x = 305;
    std::cout << 305;
    for (int i = 0; i < 305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_305(int& val) {
    const int x = 305;
    std::cout << 305;
    for (int i = 0; i < 305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_306(int& val) {
    func_in_306(val);
    const int x = 306;
    std::cout << 306;
    for (int i = 0; i < 306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_306(int& val) {
    const int x = 306;
    std::cout << 306;
    for (int i = 0; i < 306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_307(int& val) {
    func_in_307(val);
    const int x = 307;
    std::cout << 307;
    for (int i = 0; i < 307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_307(int& val) {
    const int x = 307;
    std::cout << 307;
    for (int i = 0; i < 307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_308(int& val) {
    func_in_308(val);
    const int x = 308;
    std::cout << 308;
    for (int i = 0; i < 308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_308(int& val) {
    const int x = 308;
    std::cout << 308;
    for (int i = 0; i < 308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_309(int& val) {
    func_in_309(val);
    const int x = 309;
    std::cout << 309;
    for (int i = 0; i < 309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_309(int& val) {
    const int x = 309;
    std::cout << 309;
    for (int i = 0; i < 309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_310(int& val) {
    func_in_310(val);
    const int x = 310;
    std::cout << 310;
    for (int i = 0; i < 310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_310(int& val) {
    const int x = 310;
    std::cout << 310;
    for (int i = 0; i < 310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_311(int& val) {
    func_in_311(val);
    const int x = 311;
    std::cout << 311;
    for (int i = 0; i < 311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_311(int& val) {
    const int x = 311;
    std::cout << 311;
    for (int i = 0; i < 311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_312(int& val) {
    func_in_312(val);
    const int x = 312;
    std::cout << 312;
    for (int i = 0; i < 312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_312(int& val) {
    const int x = 312;
    std::cout << 312;
    for (int i = 0; i < 312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_313(int& val) {
    func_in_313(val);
    const int x = 313;
    std::cout << 313;
    for (int i = 0; i < 313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_313(int& val) {
    const int x = 313;
    std::cout << 313;
    for (int i = 0; i < 313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_314(int& val) {
    func_in_314(val);
    const int x = 314;
    std::cout << 314;
    for (int i = 0; i < 314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_314(int& val) {
    const int x = 314;
    std::cout << 314;
    for (int i = 0; i < 314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_315(int& val) {
    func_in_315(val);
    const int x = 315;
    std::cout << 315;
    for (int i = 0; i < 315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_315(int& val) {
    const int x = 315;
    std::cout << 315;
    for (int i = 0; i < 315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_316(int& val) {
    func_in_316(val);
    const int x = 316;
    std::cout << 316;
    for (int i = 0; i < 316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_316(int& val) {
    const int x = 316;
    std::cout << 316;
    for (int i = 0; i < 316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_317(int& val) {
    func_in_317(val);
    const int x = 317;
    std::cout << 317;
    for (int i = 0; i < 317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_317(int& val) {
    const int x = 317;
    std::cout << 317;
    for (int i = 0; i < 317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_318(int& val) {
    func_in_318(val);
    const int x = 318;
    std::cout << 318;
    for (int i = 0; i < 318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_318(int& val) {
    const int x = 318;
    std::cout << 318;
    for (int i = 0; i < 318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_319(int& val) {
    func_in_319(val);
    const int x = 319;
    std::cout << 319;
    for (int i = 0; i < 319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_319(int& val) {
    const int x = 319;
    std::cout << 319;
    for (int i = 0; i < 319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_320(int& val) {
    func_in_320(val);
    const int x = 320;
    std::cout << 320;
    for (int i = 0; i < 320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_320(int& val) {
    const int x = 320;
    std::cout << 320;
    for (int i = 0; i < 320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_321(int& val) {
    func_in_321(val);
    const int x = 321;
    std::cout << 321;
    for (int i = 0; i < 321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_321(int& val) {
    const int x = 321;
    std::cout << 321;
    for (int i = 0; i < 321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_322(int& val) {
    func_in_322(val);
    const int x = 322;
    std::cout << 322;
    for (int i = 0; i < 322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_322(int& val) {
    const int x = 322;
    std::cout << 322;
    for (int i = 0; i < 322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_323(int& val) {
    func_in_323(val);
    const int x = 323;
    std::cout << 323;
    for (int i = 0; i < 323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_323(int& val) {
    const int x = 323;
    std::cout << 323;
    for (int i = 0; i < 323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_324(int& val) {
    func_in_324(val);
    const int x = 324;
    std::cout << 324;
    for (int i = 0; i < 324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_324(int& val) {
    const int x = 324;
    std::cout << 324;
    for (int i = 0; i < 324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_325(int& val) {
    func_in_325(val);
    const int x = 325;
    std::cout << 325;
    for (int i = 0; i < 325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_325(int& val) {
    const int x = 325;
    std::cout << 325;
    for (int i = 0; i < 325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_326(int& val) {
    func_in_326(val);
    const int x = 326;
    std::cout << 326;
    for (int i = 0; i < 326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_326(int& val) {
    const int x = 326;
    std::cout << 326;
    for (int i = 0; i < 326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_327(int& val) {
    func_in_327(val);
    const int x = 327;
    std::cout << 327;
    for (int i = 0; i < 327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_327(int& val) {
    const int x = 327;
    std::cout << 327;
    for (int i = 0; i < 327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_328(int& val) {
    func_in_328(val);
    const int x = 328;
    std::cout << 328;
    for (int i = 0; i < 328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_328(int& val) {
    const int x = 328;
    std::cout << 328;
    for (int i = 0; i < 328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_329(int& val) {
    func_in_329(val);
    const int x = 329;
    std::cout << 329;
    for (int i = 0; i < 329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_329(int& val) {
    const int x = 329;
    std::cout << 329;
    for (int i = 0; i < 329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_330(int& val) {
    func_in_330(val);
    const int x = 330;
    std::cout << 330;
    for (int i = 0; i < 330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_330(int& val) {
    const int x = 330;
    std::cout << 330;
    for (int i = 0; i < 330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_331(int& val) {
    func_in_331(val);
    const int x = 331;
    std::cout << 331;
    for (int i = 0; i < 331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_331(int& val) {
    const int x = 331;
    std::cout << 331;
    for (int i = 0; i < 331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_332(int& val) {
    func_in_332(val);
    const int x = 332;
    std::cout << 332;
    for (int i = 0; i < 332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_332(int& val) {
    const int x = 332;
    std::cout << 332;
    for (int i = 0; i < 332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_333(int& val) {
    func_in_333(val);
    const int x = 333;
    std::cout << 333;
    for (int i = 0; i < 333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_333(int& val) {
    const int x = 333;
    std::cout << 333;
    for (int i = 0; i < 333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_334(int& val) {
    func_in_334(val);
    const int x = 334;
    std::cout << 334;
    for (int i = 0; i < 334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_334(int& val) {
    const int x = 334;
    std::cout << 334;
    for (int i = 0; i < 334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_335(int& val) {
    func_in_335(val);
    const int x = 335;
    std::cout << 335;
    for (int i = 0; i < 335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_335(int& val) {
    const int x = 335;
    std::cout << 335;
    for (int i = 0; i < 335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_336(int& val) {
    func_in_336(val);
    const int x = 336;
    std::cout << 336;
    for (int i = 0; i < 336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_336(int& val) {
    const int x = 336;
    std::cout << 336;
    for (int i = 0; i < 336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_337(int& val) {
    func_in_337(val);
    const int x = 337;
    std::cout << 337;
    for (int i = 0; i < 337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_337(int& val) {
    const int x = 337;
    std::cout << 337;
    for (int i = 0; i < 337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_338(int& val) {
    func_in_338(val);
    const int x = 338;
    std::cout << 338;
    for (int i = 0; i < 338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_338(int& val) {
    const int x = 338;
    std::cout << 338;
    for (int i = 0; i < 338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_339(int& val) {
    func_in_339(val);
    const int x = 339;
    std::cout << 339;
    for (int i = 0; i < 339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_339(int& val) {
    const int x = 339;
    std::cout << 339;
    for (int i = 0; i < 339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_340(int& val) {
    func_in_340(val);
    const int x = 340;
    std::cout << 340;
    for (int i = 0; i < 340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_340(int& val) {
    const int x = 340;
    std::cout << 340;
    for (int i = 0; i < 340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_341(int& val) {
    func_in_341(val);
    const int x = 341;
    std::cout << 341;
    for (int i = 0; i < 341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_341(int& val) {
    const int x = 341;
    std::cout << 341;
    for (int i = 0; i < 341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_342(int& val) {
    func_in_342(val);
    const int x = 342;
    std::cout << 342;
    for (int i = 0; i < 342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_342(int& val) {
    const int x = 342;
    std::cout << 342;
    for (int i = 0; i < 342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_343(int& val) {
    func_in_343(val);
    const int x = 343;
    std::cout << 343;
    for (int i = 0; i < 343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_343(int& val) {
    const int x = 343;
    std::cout << 343;
    for (int i = 0; i < 343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_344(int& val) {
    func_in_344(val);
    const int x = 344;
    std::cout << 344;
    for (int i = 0; i < 344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_344(int& val) {
    const int x = 344;
    std::cout << 344;
    for (int i = 0; i < 344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_345(int& val) {
    func_in_345(val);
    const int x = 345;
    std::cout << 345;
    for (int i = 0; i < 345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_345(int& val) {
    const int x = 345;
    std::cout << 345;
    for (int i = 0; i < 345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_346(int& val) {
    func_in_346(val);
    const int x = 346;
    std::cout << 346;
    for (int i = 0; i < 346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_346(int& val) {
    const int x = 346;
    std::cout << 346;
    for (int i = 0; i < 346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_347(int& val) {
    func_in_347(val);
    const int x = 347;
    std::cout << 347;
    for (int i = 0; i < 347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_347(int& val) {
    const int x = 347;
    std::cout << 347;
    for (int i = 0; i < 347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_348(int& val) {
    func_in_348(val);
    const int x = 348;
    std::cout << 348;
    for (int i = 0; i < 348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_348(int& val) {
    const int x = 348;
    std::cout << 348;
    for (int i = 0; i < 348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_349(int& val) {
    func_in_349(val);
    const int x = 349;
    std::cout << 349;
    for (int i = 0; i < 349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_349(int& val) {
    const int x = 349;
    std::cout << 349;
    for (int i = 0; i < 349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_350(int& val) {
    func_in_350(val);
    const int x = 350;
    std::cout << 350;
    for (int i = 0; i < 350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_350(int& val) {
    const int x = 350;
    std::cout << 350;
    for (int i = 0; i < 350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_351(int& val) {
    func_in_351(val);
    const int x = 351;
    std::cout << 351;
    for (int i = 0; i < 351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_351(int& val) {
    const int x = 351;
    std::cout << 351;
    for (int i = 0; i < 351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_352(int& val) {
    func_in_352(val);
    const int x = 352;
    std::cout << 352;
    for (int i = 0; i < 352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_352(int& val) {
    const int x = 352;
    std::cout << 352;
    for (int i = 0; i < 352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_353(int& val) {
    func_in_353(val);
    const int x = 353;
    std::cout << 353;
    for (int i = 0; i < 353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_353(int& val) {
    const int x = 353;
    std::cout << 353;
    for (int i = 0; i < 353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_354(int& val) {
    func_in_354(val);
    const int x = 354;
    std::cout << 354;
    for (int i = 0; i < 354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_354(int& val) {
    const int x = 354;
    std::cout << 354;
    for (int i = 0; i < 354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_355(int& val) {
    func_in_355(val);
    const int x = 355;
    std::cout << 355;
    for (int i = 0; i < 355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_355(int& val) {
    const int x = 355;
    std::cout << 355;
    for (int i = 0; i < 355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_356(int& val) {
    func_in_356(val);
    const int x = 356;
    std::cout << 356;
    for (int i = 0; i < 356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_356(int& val) {
    const int x = 356;
    std::cout << 356;
    for (int i = 0; i < 356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_357(int& val) {
    func_in_357(val);
    const int x = 357;
    std::cout << 357;
    for (int i = 0; i < 357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_357(int& val) {
    const int x = 357;
    std::cout << 357;
    for (int i = 0; i < 357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_358(int& val) {
    func_in_358(val);
    const int x = 358;
    std::cout << 358;
    for (int i = 0; i < 358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_358(int& val) {
    const int x = 358;
    std::cout << 358;
    for (int i = 0; i < 358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_359(int& val) {
    func_in_359(val);
    const int x = 359;
    std::cout << 359;
    for (int i = 0; i < 359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_359(int& val) {
    const int x = 359;
    std::cout << 359;
    for (int i = 0; i < 359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_360(int& val) {
    func_in_360(val);
    const int x = 360;
    std::cout << 360;
    for (int i = 0; i < 360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_360(int& val) {
    const int x = 360;
    std::cout << 360;
    for (int i = 0; i < 360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_361(int& val) {
    func_in_361(val);
    const int x = 361;
    std::cout << 361;
    for (int i = 0; i < 361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_361(int& val) {
    const int x = 361;
    std::cout << 361;
    for (int i = 0; i < 361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_362(int& val) {
    func_in_362(val);
    const int x = 362;
    std::cout << 362;
    for (int i = 0; i < 362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_362(int& val) {
    const int x = 362;
    std::cout << 362;
    for (int i = 0; i < 362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_363(int& val) {
    func_in_363(val);
    const int x = 363;
    std::cout << 363;
    for (int i = 0; i < 363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_363(int& val) {
    const int x = 363;
    std::cout << 363;
    for (int i = 0; i < 363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_364(int& val) {
    func_in_364(val);
    const int x = 364;
    std::cout << 364;
    for (int i = 0; i < 364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_364(int& val) {
    const int x = 364;
    std::cout << 364;
    for (int i = 0; i < 364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_365(int& val) {
    func_in_365(val);
    const int x = 365;
    std::cout << 365;
    for (int i = 0; i < 365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_365(int& val) {
    const int x = 365;
    std::cout << 365;
    for (int i = 0; i < 365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_366(int& val) {
    func_in_366(val);
    const int x = 366;
    std::cout << 366;
    for (int i = 0; i < 366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_366(int& val) {
    const int x = 366;
    std::cout << 366;
    for (int i = 0; i < 366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_367(int& val) {
    func_in_367(val);
    const int x = 367;
    std::cout << 367;
    for (int i = 0; i < 367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_367(int& val) {
    const int x = 367;
    std::cout << 367;
    for (int i = 0; i < 367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_368(int& val) {
    func_in_368(val);
    const int x = 368;
    std::cout << 368;
    for (int i = 0; i < 368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_368(int& val) {
    const int x = 368;
    std::cout << 368;
    for (int i = 0; i < 368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_369(int& val) {
    func_in_369(val);
    const int x = 369;
    std::cout << 369;
    for (int i = 0; i < 369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_369(int& val) {
    const int x = 369;
    std::cout << 369;
    for (int i = 0; i < 369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_370(int& val) {
    func_in_370(val);
    const int x = 370;
    std::cout << 370;
    for (int i = 0; i < 370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_370(int& val) {
    const int x = 370;
    std::cout << 370;
    for (int i = 0; i < 370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_371(int& val) {
    func_in_371(val);
    const int x = 371;
    std::cout << 371;
    for (int i = 0; i < 371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_371(int& val) {
    const int x = 371;
    std::cout << 371;
    for (int i = 0; i < 371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_372(int& val) {
    func_in_372(val);
    const int x = 372;
    std::cout << 372;
    for (int i = 0; i < 372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_372(int& val) {
    const int x = 372;
    std::cout << 372;
    for (int i = 0; i < 372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_373(int& val) {
    func_in_373(val);
    const int x = 373;
    std::cout << 373;
    for (int i = 0; i < 373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_373(int& val) {
    const int x = 373;
    std::cout << 373;
    for (int i = 0; i < 373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_374(int& val) {
    func_in_374(val);
    const int x = 374;
    std::cout << 374;
    for (int i = 0; i < 374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_374(int& val) {
    const int x = 374;
    std::cout << 374;
    for (int i = 0; i < 374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_375(int& val) {
    func_in_375(val);
    const int x = 375;
    std::cout << 375;
    for (int i = 0; i < 375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_375(int& val) {
    const int x = 375;
    std::cout << 375;
    for (int i = 0; i < 375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_376(int& val) {
    func_in_376(val);
    const int x = 376;
    std::cout << 376;
    for (int i = 0; i < 376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_376(int& val) {
    const int x = 376;
    std::cout << 376;
    for (int i = 0; i < 376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_377(int& val) {
    func_in_377(val);
    const int x = 377;
    std::cout << 377;
    for (int i = 0; i < 377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_377(int& val) {
    const int x = 377;
    std::cout << 377;
    for (int i = 0; i < 377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_378(int& val) {
    func_in_378(val);
    const int x = 378;
    std::cout << 378;
    for (int i = 0; i < 378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_378(int& val) {
    const int x = 378;
    std::cout << 378;
    for (int i = 0; i < 378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_379(int& val) {
    func_in_379(val);
    const int x = 379;
    std::cout << 379;
    for (int i = 0; i < 379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_379(int& val) {
    const int x = 379;
    std::cout << 379;
    for (int i = 0; i < 379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_380(int& val) {
    func_in_380(val);
    const int x = 380;
    std::cout << 380;
    for (int i = 0; i < 380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_380(int& val) {
    const int x = 380;
    std::cout << 380;
    for (int i = 0; i < 380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_381(int& val) {
    func_in_381(val);
    const int x = 381;
    std::cout << 381;
    for (int i = 0; i < 381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_381(int& val) {
    const int x = 381;
    std::cout << 381;
    for (int i = 0; i < 381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_382(int& val) {
    func_in_382(val);
    const int x = 382;
    std::cout << 382;
    for (int i = 0; i < 382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_382(int& val) {
    const int x = 382;
    std::cout << 382;
    for (int i = 0; i < 382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_383(int& val) {
    func_in_383(val);
    const int x = 383;
    std::cout << 383;
    for (int i = 0; i < 383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_383(int& val) {
    const int x = 383;
    std::cout << 383;
    for (int i = 0; i < 383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_384(int& val) {
    func_in_384(val);
    const int x = 384;
    std::cout << 384;
    for (int i = 0; i < 384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_384(int& val) {
    const int x = 384;
    std::cout << 384;
    for (int i = 0; i < 384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_385(int& val) {
    func_in_385(val);
    const int x = 385;
    std::cout << 385;
    for (int i = 0; i < 385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_385(int& val) {
    const int x = 385;
    std::cout << 385;
    for (int i = 0; i < 385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_386(int& val) {
    func_in_386(val);
    const int x = 386;
    std::cout << 386;
    for (int i = 0; i < 386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_386(int& val) {
    const int x = 386;
    std::cout << 386;
    for (int i = 0; i < 386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_387(int& val) {
    func_in_387(val);
    const int x = 387;
    std::cout << 387;
    for (int i = 0; i < 387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_387(int& val) {
    const int x = 387;
    std::cout << 387;
    for (int i = 0; i < 387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_388(int& val) {
    func_in_388(val);
    const int x = 388;
    std::cout << 388;
    for (int i = 0; i < 388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_388(int& val) {
    const int x = 388;
    std::cout << 388;
    for (int i = 0; i < 388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_389(int& val) {
    func_in_389(val);
    const int x = 389;
    std::cout << 389;
    for (int i = 0; i < 389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_389(int& val) {
    const int x = 389;
    std::cout << 389;
    for (int i = 0; i < 389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_390(int& val) {
    func_in_390(val);
    const int x = 390;
    std::cout << 390;
    for (int i = 0; i < 390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_390(int& val) {
    const int x = 390;
    std::cout << 390;
    for (int i = 0; i < 390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_391(int& val) {
    func_in_391(val);
    const int x = 391;
    std::cout << 391;
    for (int i = 0; i < 391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_391(int& val) {
    const int x = 391;
    std::cout << 391;
    for (int i = 0; i < 391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_392(int& val) {
    func_in_392(val);
    const int x = 392;
    std::cout << 392;
    for (int i = 0; i < 392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_392(int& val) {
    const int x = 392;
    std::cout << 392;
    for (int i = 0; i < 392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_393(int& val) {
    func_in_393(val);
    const int x = 393;
    std::cout << 393;
    for (int i = 0; i < 393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_393(int& val) {
    const int x = 393;
    std::cout << 393;
    for (int i = 0; i < 393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_394(int& val) {
    func_in_394(val);
    const int x = 394;
    std::cout << 394;
    for (int i = 0; i < 394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_394(int& val) {
    const int x = 394;
    std::cout << 394;
    for (int i = 0; i < 394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_395(int& val) {
    func_in_395(val);
    const int x = 395;
    std::cout << 395;
    for (int i = 0; i < 395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_395(int& val) {
    const int x = 395;
    std::cout << 395;
    for (int i = 0; i < 395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_396(int& val) {
    func_in_396(val);
    const int x = 396;
    std::cout << 396;
    for (int i = 0; i < 396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_396(int& val) {
    const int x = 396;
    std::cout << 396;
    for (int i = 0; i < 396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_397(int& val) {
    func_in_397(val);
    const int x = 397;
    std::cout << 397;
    for (int i = 0; i < 397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_397(int& val) {
    const int x = 397;
    std::cout << 397;
    for (int i = 0; i < 397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_398(int& val) {
    func_in_398(val);
    const int x = 398;
    std::cout << 398;
    for (int i = 0; i < 398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_398(int& val) {
    const int x = 398;
    std::cout << 398;
    for (int i = 0; i < 398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_399(int& val) {
    func_in_399(val);
    const int x = 399;
    std::cout << 399;
    for (int i = 0; i < 399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_399(int& val) {
    const int x = 399;
    std::cout << 399;
    for (int i = 0; i < 399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_400(int& val) {
    func_in_400(val);
    const int x = 400;
    std::cout << 400;
    for (int i = 0; i < 400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_400(int& val) {
    const int x = 400;
    std::cout << 400;
    for (int i = 0; i < 400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_401(int& val) {
    func_in_401(val);
    const int x = 401;
    std::cout << 401;
    for (int i = 0; i < 401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_401(int& val) {
    const int x = 401;
    std::cout << 401;
    for (int i = 0; i < 401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_402(int& val) {
    func_in_402(val);
    const int x = 402;
    std::cout << 402;
    for (int i = 0; i < 402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_402(int& val) {
    const int x = 402;
    std::cout << 402;
    for (int i = 0; i < 402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_403(int& val) {
    func_in_403(val);
    const int x = 403;
    std::cout << 403;
    for (int i = 0; i < 403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_403(int& val) {
    const int x = 403;
    std::cout << 403;
    for (int i = 0; i < 403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_404(int& val) {
    func_in_404(val);
    const int x = 404;
    std::cout << 404;
    for (int i = 0; i < 404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_404(int& val) {
    const int x = 404;
    std::cout << 404;
    for (int i = 0; i < 404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_405(int& val) {
    func_in_405(val);
    const int x = 405;
    std::cout << 405;
    for (int i = 0; i < 405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_405(int& val) {
    const int x = 405;
    std::cout << 405;
    for (int i = 0; i < 405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_406(int& val) {
    func_in_406(val);
    const int x = 406;
    std::cout << 406;
    for (int i = 0; i < 406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_406(int& val) {
    const int x = 406;
    std::cout << 406;
    for (int i = 0; i < 406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_407(int& val) {
    func_in_407(val);
    const int x = 407;
    std::cout << 407;
    for (int i = 0; i < 407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_407(int& val) {
    const int x = 407;
    std::cout << 407;
    for (int i = 0; i < 407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_408(int& val) {
    func_in_408(val);
    const int x = 408;
    std::cout << 408;
    for (int i = 0; i < 408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_408(int& val) {
    const int x = 408;
    std::cout << 408;
    for (int i = 0; i < 408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_409(int& val) {
    func_in_409(val);
    const int x = 409;
    std::cout << 409;
    for (int i = 0; i < 409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_409(int& val) {
    const int x = 409;
    std::cout << 409;
    for (int i = 0; i < 409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_410(int& val) {
    func_in_410(val);
    const int x = 410;
    std::cout << 410;
    for (int i = 0; i < 410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_410(int& val) {
    const int x = 410;
    std::cout << 410;
    for (int i = 0; i < 410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_411(int& val) {
    func_in_411(val);
    const int x = 411;
    std::cout << 411;
    for (int i = 0; i < 411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_411(int& val) {
    const int x = 411;
    std::cout << 411;
    for (int i = 0; i < 411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_412(int& val) {
    func_in_412(val);
    const int x = 412;
    std::cout << 412;
    for (int i = 0; i < 412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_412(int& val) {
    const int x = 412;
    std::cout << 412;
    for (int i = 0; i < 412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_413(int& val) {
    func_in_413(val);
    const int x = 413;
    std::cout << 413;
    for (int i = 0; i < 413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_413(int& val) {
    const int x = 413;
    std::cout << 413;
    for (int i = 0; i < 413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_414(int& val) {
    func_in_414(val);
    const int x = 414;
    std::cout << 414;
    for (int i = 0; i < 414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_414(int& val) {
    const int x = 414;
    std::cout << 414;
    for (int i = 0; i < 414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_415(int& val) {
    func_in_415(val);
    const int x = 415;
    std::cout << 415;
    for (int i = 0; i < 415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_415(int& val) {
    const int x = 415;
    std::cout << 415;
    for (int i = 0; i < 415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_416(int& val) {
    func_in_416(val);
    const int x = 416;
    std::cout << 416;
    for (int i = 0; i < 416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_416(int& val) {
    const int x = 416;
    std::cout << 416;
    for (int i = 0; i < 416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_417(int& val) {
    func_in_417(val);
    const int x = 417;
    std::cout << 417;
    for (int i = 0; i < 417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_417(int& val) {
    const int x = 417;
    std::cout << 417;
    for (int i = 0; i < 417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_418(int& val) {
    func_in_418(val);
    const int x = 418;
    std::cout << 418;
    for (int i = 0; i < 418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_418(int& val) {
    const int x = 418;
    std::cout << 418;
    for (int i = 0; i < 418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_419(int& val) {
    func_in_419(val);
    const int x = 419;
    std::cout << 419;
    for (int i = 0; i < 419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_419(int& val) {
    const int x = 419;
    std::cout << 419;
    for (int i = 0; i < 419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_420(int& val) {
    func_in_420(val);
    const int x = 420;
    std::cout << 420;
    for (int i = 0; i < 420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_420(int& val) {
    const int x = 420;
    std::cout << 420;
    for (int i = 0; i < 420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_421(int& val) {
    func_in_421(val);
    const int x = 421;
    std::cout << 421;
    for (int i = 0; i < 421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_421(int& val) {
    const int x = 421;
    std::cout << 421;
    for (int i = 0; i < 421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_422(int& val) {
    func_in_422(val);
    const int x = 422;
    std::cout << 422;
    for (int i = 0; i < 422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_422(int& val) {
    const int x = 422;
    std::cout << 422;
    for (int i = 0; i < 422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_423(int& val) {
    func_in_423(val);
    const int x = 423;
    std::cout << 423;
    for (int i = 0; i < 423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_423(int& val) {
    const int x = 423;
    std::cout << 423;
    for (int i = 0; i < 423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_424(int& val) {
    func_in_424(val);
    const int x = 424;
    std::cout << 424;
    for (int i = 0; i < 424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_424(int& val) {
    const int x = 424;
    std::cout << 424;
    for (int i = 0; i < 424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_425(int& val) {
    func_in_425(val);
    const int x = 425;
    std::cout << 425;
    for (int i = 0; i < 425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_425(int& val) {
    const int x = 425;
    std::cout << 425;
    for (int i = 0; i < 425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_426(int& val) {
    func_in_426(val);
    const int x = 426;
    std::cout << 426;
    for (int i = 0; i < 426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_426(int& val) {
    const int x = 426;
    std::cout << 426;
    for (int i = 0; i < 426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_427(int& val) {
    func_in_427(val);
    const int x = 427;
    std::cout << 427;
    for (int i = 0; i < 427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_427(int& val) {
    const int x = 427;
    std::cout << 427;
    for (int i = 0; i < 427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_428(int& val) {
    func_in_428(val);
    const int x = 428;
    std::cout << 428;
    for (int i = 0; i < 428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_428(int& val) {
    const int x = 428;
    std::cout << 428;
    for (int i = 0; i < 428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_429(int& val) {
    func_in_429(val);
    const int x = 429;
    std::cout << 429;
    for (int i = 0; i < 429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_429(int& val) {
    const int x = 429;
    std::cout << 429;
    for (int i = 0; i < 429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_430(int& val) {
    func_in_430(val);
    const int x = 430;
    std::cout << 430;
    for (int i = 0; i < 430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_430(int& val) {
    const int x = 430;
    std::cout << 430;
    for (int i = 0; i < 430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_431(int& val) {
    func_in_431(val);
    const int x = 431;
    std::cout << 431;
    for (int i = 0; i < 431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_431(int& val) {
    const int x = 431;
    std::cout << 431;
    for (int i = 0; i < 431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_432(int& val) {
    func_in_432(val);
    const int x = 432;
    std::cout << 432;
    for (int i = 0; i < 432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_432(int& val) {
    const int x = 432;
    std::cout << 432;
    for (int i = 0; i < 432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_433(int& val) {
    func_in_433(val);
    const int x = 433;
    std::cout << 433;
    for (int i = 0; i < 433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_433(int& val) {
    const int x = 433;
    std::cout << 433;
    for (int i = 0; i < 433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_434(int& val) {
    func_in_434(val);
    const int x = 434;
    std::cout << 434;
    for (int i = 0; i < 434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_434(int& val) {
    const int x = 434;
    std::cout << 434;
    for (int i = 0; i < 434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_435(int& val) {
    func_in_435(val);
    const int x = 435;
    std::cout << 435;
    for (int i = 0; i < 435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_435(int& val) {
    const int x = 435;
    std::cout << 435;
    for (int i = 0; i < 435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_436(int& val) {
    func_in_436(val);
    const int x = 436;
    std::cout << 436;
    for (int i = 0; i < 436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_436(int& val) {
    const int x = 436;
    std::cout << 436;
    for (int i = 0; i < 436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_437(int& val) {
    func_in_437(val);
    const int x = 437;
    std::cout << 437;
    for (int i = 0; i < 437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_437(int& val) {
    const int x = 437;
    std::cout << 437;
    for (int i = 0; i < 437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_438(int& val) {
    func_in_438(val);
    const int x = 438;
    std::cout << 438;
    for (int i = 0; i < 438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_438(int& val) {
    const int x = 438;
    std::cout << 438;
    for (int i = 0; i < 438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_439(int& val) {
    func_in_439(val);
    const int x = 439;
    std::cout << 439;
    for (int i = 0; i < 439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_439(int& val) {
    const int x = 439;
    std::cout << 439;
    for (int i = 0; i < 439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_440(int& val) {
    func_in_440(val);
    const int x = 440;
    std::cout << 440;
    for (int i = 0; i < 440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_440(int& val) {
    const int x = 440;
    std::cout << 440;
    for (int i = 0; i < 440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_441(int& val) {
    func_in_441(val);
    const int x = 441;
    std::cout << 441;
    for (int i = 0; i < 441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_441(int& val) {
    const int x = 441;
    std::cout << 441;
    for (int i = 0; i < 441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_442(int& val) {
    func_in_442(val);
    const int x = 442;
    std::cout << 442;
    for (int i = 0; i < 442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_442(int& val) {
    const int x = 442;
    std::cout << 442;
    for (int i = 0; i < 442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_443(int& val) {
    func_in_443(val);
    const int x = 443;
    std::cout << 443;
    for (int i = 0; i < 443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_443(int& val) {
    const int x = 443;
    std::cout << 443;
    for (int i = 0; i < 443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_444(int& val) {
    func_in_444(val);
    const int x = 444;
    std::cout << 444;
    for (int i = 0; i < 444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_444(int& val) {
    const int x = 444;
    std::cout << 444;
    for (int i = 0; i < 444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_445(int& val) {
    func_in_445(val);
    const int x = 445;
    std::cout << 445;
    for (int i = 0; i < 445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_445(int& val) {
    const int x = 445;
    std::cout << 445;
    for (int i = 0; i < 445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_446(int& val) {
    func_in_446(val);
    const int x = 446;
    std::cout << 446;
    for (int i = 0; i < 446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_446(int& val) {
    const int x = 446;
    std::cout << 446;
    for (int i = 0; i < 446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_447(int& val) {
    func_in_447(val);
    const int x = 447;
    std::cout << 447;
    for (int i = 0; i < 447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_447(int& val) {
    const int x = 447;
    std::cout << 447;
    for (int i = 0; i < 447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_448(int& val) {
    func_in_448(val);
    const int x = 448;
    std::cout << 448;
    for (int i = 0; i < 448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_448(int& val) {
    const int x = 448;
    std::cout << 448;
    for (int i = 0; i < 448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_449(int& val) {
    func_in_449(val);
    const int x = 449;
    std::cout << 449;
    for (int i = 0; i < 449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_449(int& val) {
    const int x = 449;
    std::cout << 449;
    for (int i = 0; i < 449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_450(int& val) {
    func_in_450(val);
    const int x = 450;
    std::cout << 450;
    for (int i = 0; i < 450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_450(int& val) {
    const int x = 450;
    std::cout << 450;
    for (int i = 0; i < 450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_451(int& val) {
    func_in_451(val);
    const int x = 451;
    std::cout << 451;
    for (int i = 0; i < 451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_451(int& val) {
    const int x = 451;
    std::cout << 451;
    for (int i = 0; i < 451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_452(int& val) {
    func_in_452(val);
    const int x = 452;
    std::cout << 452;
    for (int i = 0; i < 452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_452(int& val) {
    const int x = 452;
    std::cout << 452;
    for (int i = 0; i < 452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_453(int& val) {
    func_in_453(val);
    const int x = 453;
    std::cout << 453;
    for (int i = 0; i < 453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_453(int& val) {
    const int x = 453;
    std::cout << 453;
    for (int i = 0; i < 453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_454(int& val) {
    func_in_454(val);
    const int x = 454;
    std::cout << 454;
    for (int i = 0; i < 454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_454(int& val) {
    const int x = 454;
    std::cout << 454;
    for (int i = 0; i < 454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_455(int& val) {
    func_in_455(val);
    const int x = 455;
    std::cout << 455;
    for (int i = 0; i < 455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_455(int& val) {
    const int x = 455;
    std::cout << 455;
    for (int i = 0; i < 455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_456(int& val) {
    func_in_456(val);
    const int x = 456;
    std::cout << 456;
    for (int i = 0; i < 456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_456(int& val) {
    const int x = 456;
    std::cout << 456;
    for (int i = 0; i < 456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_457(int& val) {
    func_in_457(val);
    const int x = 457;
    std::cout << 457;
    for (int i = 0; i < 457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_457(int& val) {
    const int x = 457;
    std::cout << 457;
    for (int i = 0; i < 457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_458(int& val) {
    func_in_458(val);
    const int x = 458;
    std::cout << 458;
    for (int i = 0; i < 458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_458(int& val) {
    const int x = 458;
    std::cout << 458;
    for (int i = 0; i < 458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_459(int& val) {
    func_in_459(val);
    const int x = 459;
    std::cout << 459;
    for (int i = 0; i < 459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_459(int& val) {
    const int x = 459;
    std::cout << 459;
    for (int i = 0; i < 459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_460(int& val) {
    func_in_460(val);
    const int x = 460;
    std::cout << 460;
    for (int i = 0; i < 460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_460(int& val) {
    const int x = 460;
    std::cout << 460;
    for (int i = 0; i < 460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_461(int& val) {
    func_in_461(val);
    const int x = 461;
    std::cout << 461;
    for (int i = 0; i < 461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_461(int& val) {
    const int x = 461;
    std::cout << 461;
    for (int i = 0; i < 461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_462(int& val) {
    func_in_462(val);
    const int x = 462;
    std::cout << 462;
    for (int i = 0; i < 462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_462(int& val) {
    const int x = 462;
    std::cout << 462;
    for (int i = 0; i < 462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_463(int& val) {
    func_in_463(val);
    const int x = 463;
    std::cout << 463;
    for (int i = 0; i < 463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_463(int& val) {
    const int x = 463;
    std::cout << 463;
    for (int i = 0; i < 463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_464(int& val) {
    func_in_464(val);
    const int x = 464;
    std::cout << 464;
    for (int i = 0; i < 464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_464(int& val) {
    const int x = 464;
    std::cout << 464;
    for (int i = 0; i < 464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_465(int& val) {
    func_in_465(val);
    const int x = 465;
    std::cout << 465;
    for (int i = 0; i < 465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_465(int& val) {
    const int x = 465;
    std::cout << 465;
    for (int i = 0; i < 465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_466(int& val) {
    func_in_466(val);
    const int x = 466;
    std::cout << 466;
    for (int i = 0; i < 466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_466(int& val) {
    const int x = 466;
    std::cout << 466;
    for (int i = 0; i < 466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_467(int& val) {
    func_in_467(val);
    const int x = 467;
    std::cout << 467;
    for (int i = 0; i < 467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_467(int& val) {
    const int x = 467;
    std::cout << 467;
    for (int i = 0; i < 467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_468(int& val) {
    func_in_468(val);
    const int x = 468;
    std::cout << 468;
    for (int i = 0; i < 468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_468(int& val) {
    const int x = 468;
    std::cout << 468;
    for (int i = 0; i < 468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_469(int& val) {
    func_in_469(val);
    const int x = 469;
    std::cout << 469;
    for (int i = 0; i < 469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_469(int& val) {
    const int x = 469;
    std::cout << 469;
    for (int i = 0; i < 469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_470(int& val) {
    func_in_470(val);
    const int x = 470;
    std::cout << 470;
    for (int i = 0; i < 470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_470(int& val) {
    const int x = 470;
    std::cout << 470;
    for (int i = 0; i < 470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_471(int& val) {
    func_in_471(val);
    const int x = 471;
    std::cout << 471;
    for (int i = 0; i < 471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_471(int& val) {
    const int x = 471;
    std::cout << 471;
    for (int i = 0; i < 471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_472(int& val) {
    func_in_472(val);
    const int x = 472;
    std::cout << 472;
    for (int i = 0; i < 472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_472(int& val) {
    const int x = 472;
    std::cout << 472;
    for (int i = 0; i < 472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_473(int& val) {
    func_in_473(val);
    const int x = 473;
    std::cout << 473;
    for (int i = 0; i < 473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_473(int& val) {
    const int x = 473;
    std::cout << 473;
    for (int i = 0; i < 473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_474(int& val) {
    func_in_474(val);
    const int x = 474;
    std::cout << 474;
    for (int i = 0; i < 474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_474(int& val) {
    const int x = 474;
    std::cout << 474;
    for (int i = 0; i < 474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_475(int& val) {
    func_in_475(val);
    const int x = 475;
    std::cout << 475;
    for (int i = 0; i < 475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_475(int& val) {
    const int x = 475;
    std::cout << 475;
    for (int i = 0; i < 475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_476(int& val) {
    func_in_476(val);
    const int x = 476;
    std::cout << 476;
    for (int i = 0; i < 476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_476(int& val) {
    const int x = 476;
    std::cout << 476;
    for (int i = 0; i < 476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_477(int& val) {
    func_in_477(val);
    const int x = 477;
    std::cout << 477;
    for (int i = 0; i < 477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_477(int& val) {
    const int x = 477;
    std::cout << 477;
    for (int i = 0; i < 477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_478(int& val) {
    func_in_478(val);
    const int x = 478;
    std::cout << 478;
    for (int i = 0; i < 478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_478(int& val) {
    const int x = 478;
    std::cout << 478;
    for (int i = 0; i < 478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_479(int& val) {
    func_in_479(val);
    const int x = 479;
    std::cout << 479;
    for (int i = 0; i < 479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_479(int& val) {
    const int x = 479;
    std::cout << 479;
    for (int i = 0; i < 479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_480(int& val) {
    func_in_480(val);
    const int x = 480;
    std::cout << 480;
    for (int i = 0; i < 480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_480(int& val) {
    const int x = 480;
    std::cout << 480;
    for (int i = 0; i < 480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_481(int& val) {
    func_in_481(val);
    const int x = 481;
    std::cout << 481;
    for (int i = 0; i < 481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_481(int& val) {
    const int x = 481;
    std::cout << 481;
    for (int i = 0; i < 481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_482(int& val) {
    func_in_482(val);
    const int x = 482;
    std::cout << 482;
    for (int i = 0; i < 482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_482(int& val) {
    const int x = 482;
    std::cout << 482;
    for (int i = 0; i < 482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_483(int& val) {
    func_in_483(val);
    const int x = 483;
    std::cout << 483;
    for (int i = 0; i < 483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_483(int& val) {
    const int x = 483;
    std::cout << 483;
    for (int i = 0; i < 483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_484(int& val) {
    func_in_484(val);
    const int x = 484;
    std::cout << 484;
    for (int i = 0; i < 484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_484(int& val) {
    const int x = 484;
    std::cout << 484;
    for (int i = 0; i < 484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_485(int& val) {
    func_in_485(val);
    const int x = 485;
    std::cout << 485;
    for (int i = 0; i < 485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_485(int& val) {
    const int x = 485;
    std::cout << 485;
    for (int i = 0; i < 485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_486(int& val) {
    func_in_486(val);
    const int x = 486;
    std::cout << 486;
    for (int i = 0; i < 486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_486(int& val) {
    const int x = 486;
    std::cout << 486;
    for (int i = 0; i < 486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_487(int& val) {
    func_in_487(val);
    const int x = 487;
    std::cout << 487;
    for (int i = 0; i < 487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_487(int& val) {
    const int x = 487;
    std::cout << 487;
    for (int i = 0; i < 487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_488(int& val) {
    func_in_488(val);
    const int x = 488;
    std::cout << 488;
    for (int i = 0; i < 488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_488(int& val) {
    const int x = 488;
    std::cout << 488;
    for (int i = 0; i < 488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_489(int& val) {
    func_in_489(val);
    const int x = 489;
    std::cout << 489;
    for (int i = 0; i < 489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_489(int& val) {
    const int x = 489;
    std::cout << 489;
    for (int i = 0; i < 489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_490(int& val) {
    func_in_490(val);
    const int x = 490;
    std::cout << 490;
    for (int i = 0; i < 490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_490(int& val) {
    const int x = 490;
    std::cout << 490;
    for (int i = 0; i < 490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_491(int& val) {
    func_in_491(val);
    const int x = 491;
    std::cout << 491;
    for (int i = 0; i < 491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_491(int& val) {
    const int x = 491;
    std::cout << 491;
    for (int i = 0; i < 491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_492(int& val) {
    func_in_492(val);
    const int x = 492;
    std::cout << 492;
    for (int i = 0; i < 492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_492(int& val) {
    const int x = 492;
    std::cout << 492;
    for (int i = 0; i < 492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_493(int& val) {
    func_in_493(val);
    const int x = 493;
    std::cout << 493;
    for (int i = 0; i < 493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_493(int& val) {
    const int x = 493;
    std::cout << 493;
    for (int i = 0; i < 493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_494(int& val) {
    func_in_494(val);
    const int x = 494;
    std::cout << 494;
    for (int i = 0; i < 494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_494(int& val) {
    const int x = 494;
    std::cout << 494;
    for (int i = 0; i < 494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_495(int& val) {
    func_in_495(val);
    const int x = 495;
    std::cout << 495;
    for (int i = 0; i < 495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_495(int& val) {
    const int x = 495;
    std::cout << 495;
    for (int i = 0; i < 495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_496(int& val) {
    func_in_496(val);
    const int x = 496;
    std::cout << 496;
    for (int i = 0; i < 496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_496(int& val) {
    const int x = 496;
    std::cout << 496;
    for (int i = 0; i < 496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_497(int& val) {
    func_in_497(val);
    const int x = 497;
    std::cout << 497;
    for (int i = 0; i < 497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_497(int& val) {
    const int x = 497;
    std::cout << 497;
    for (int i = 0; i < 497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_498(int& val) {
    func_in_498(val);
    const int x = 498;
    std::cout << 498;
    for (int i = 0; i < 498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_498(int& val) {
    const int x = 498;
    std::cout << 498;
    for (int i = 0; i < 498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_499(int& val) {
    func_in_499(val);
    const int x = 499;
    std::cout << 499;
    for (int i = 0; i < 499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_499(int& val) {
    const int x = 499;
    std::cout << 499;
    for (int i = 0; i < 499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_500(int& val) {
    func_in_500(val);
    const int x = 500;
    std::cout << 500;
    for (int i = 0; i < 500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_500(int& val) {
    const int x = 500;
    std::cout << 500;
    for (int i = 0; i < 500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_501(int& val) {
    func_in_501(val);
    const int x = 501;
    std::cout << 501;
    for (int i = 0; i < 501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_501(int& val) {
    const int x = 501;
    std::cout << 501;
    for (int i = 0; i < 501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_502(int& val) {
    func_in_502(val);
    const int x = 502;
    std::cout << 502;
    for (int i = 0; i < 502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_502(int& val) {
    const int x = 502;
    std::cout << 502;
    for (int i = 0; i < 502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_503(int& val) {
    func_in_503(val);
    const int x = 503;
    std::cout << 503;
    for (int i = 0; i < 503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_503(int& val) {
    const int x = 503;
    std::cout << 503;
    for (int i = 0; i < 503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_504(int& val) {
    func_in_504(val);
    const int x = 504;
    std::cout << 504;
    for (int i = 0; i < 504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_504(int& val) {
    const int x = 504;
    std::cout << 504;
    for (int i = 0; i < 504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_505(int& val) {
    func_in_505(val);
    const int x = 505;
    std::cout << 505;
    for (int i = 0; i < 505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_505(int& val) {
    const int x = 505;
    std::cout << 505;
    for (int i = 0; i < 505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_506(int& val) {
    func_in_506(val);
    const int x = 506;
    std::cout << 506;
    for (int i = 0; i < 506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_506(int& val) {
    const int x = 506;
    std::cout << 506;
    for (int i = 0; i < 506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_507(int& val) {
    func_in_507(val);
    const int x = 507;
    std::cout << 507;
    for (int i = 0; i < 507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_507(int& val) {
    const int x = 507;
    std::cout << 507;
    for (int i = 0; i < 507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_508(int& val) {
    func_in_508(val);
    const int x = 508;
    std::cout << 508;
    for (int i = 0; i < 508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_508(int& val) {
    const int x = 508;
    std::cout << 508;
    for (int i = 0; i < 508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_509(int& val) {
    func_in_509(val);
    const int x = 509;
    std::cout << 509;
    for (int i = 0; i < 509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_509(int& val) {
    const int x = 509;
    std::cout << 509;
    for (int i = 0; i < 509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_510(int& val) {
    func_in_510(val);
    const int x = 510;
    std::cout << 510;
    for (int i = 0; i < 510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_510(int& val) {
    const int x = 510;
    std::cout << 510;
    for (int i = 0; i < 510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_511(int& val) {
    func_in_511(val);
    const int x = 511;
    std::cout << 511;
    for (int i = 0; i < 511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_511(int& val) {
    const int x = 511;
    std::cout << 511;
    for (int i = 0; i < 511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_512(int& val) {
    func_in_512(val);
    const int x = 512;
    std::cout << 512;
    for (int i = 0; i < 512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_512(int& val) {
    const int x = 512;
    std::cout << 512;
    for (int i = 0; i < 512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_513(int& val) {
    func_in_513(val);
    const int x = 513;
    std::cout << 513;
    for (int i = 0; i < 513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_513(int& val) {
    const int x = 513;
    std::cout << 513;
    for (int i = 0; i < 513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_514(int& val) {
    func_in_514(val);
    const int x = 514;
    std::cout << 514;
    for (int i = 0; i < 514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_514(int& val) {
    const int x = 514;
    std::cout << 514;
    for (int i = 0; i < 514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_515(int& val) {
    func_in_515(val);
    const int x = 515;
    std::cout << 515;
    for (int i = 0; i < 515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_515(int& val) {
    const int x = 515;
    std::cout << 515;
    for (int i = 0; i < 515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_516(int& val) {
    func_in_516(val);
    const int x = 516;
    std::cout << 516;
    for (int i = 0; i < 516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_516(int& val) {
    const int x = 516;
    std::cout << 516;
    for (int i = 0; i < 516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_517(int& val) {
    func_in_517(val);
    const int x = 517;
    std::cout << 517;
    for (int i = 0; i < 517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_517(int& val) {
    const int x = 517;
    std::cout << 517;
    for (int i = 0; i < 517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_518(int& val) {
    func_in_518(val);
    const int x = 518;
    std::cout << 518;
    for (int i = 0; i < 518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_518(int& val) {
    const int x = 518;
    std::cout << 518;
    for (int i = 0; i < 518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_519(int& val) {
    func_in_519(val);
    const int x = 519;
    std::cout << 519;
    for (int i = 0; i < 519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_519(int& val) {
    const int x = 519;
    std::cout << 519;
    for (int i = 0; i < 519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_520(int& val) {
    func_in_520(val);
    const int x = 520;
    std::cout << 520;
    for (int i = 0; i < 520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_520(int& val) {
    const int x = 520;
    std::cout << 520;
    for (int i = 0; i < 520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_521(int& val) {
    func_in_521(val);
    const int x = 521;
    std::cout << 521;
    for (int i = 0; i < 521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_521(int& val) {
    const int x = 521;
    std::cout << 521;
    for (int i = 0; i < 521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_522(int& val) {
    func_in_522(val);
    const int x = 522;
    std::cout << 522;
    for (int i = 0; i < 522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_522(int& val) {
    const int x = 522;
    std::cout << 522;
    for (int i = 0; i < 522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_523(int& val) {
    func_in_523(val);
    const int x = 523;
    std::cout << 523;
    for (int i = 0; i < 523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_523(int& val) {
    const int x = 523;
    std::cout << 523;
    for (int i = 0; i < 523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_524(int& val) {
    func_in_524(val);
    const int x = 524;
    std::cout << 524;
    for (int i = 0; i < 524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_524(int& val) {
    const int x = 524;
    std::cout << 524;
    for (int i = 0; i < 524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_525(int& val) {
    func_in_525(val);
    const int x = 525;
    std::cout << 525;
    for (int i = 0; i < 525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_525(int& val) {
    const int x = 525;
    std::cout << 525;
    for (int i = 0; i < 525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_526(int& val) {
    func_in_526(val);
    const int x = 526;
    std::cout << 526;
    for (int i = 0; i < 526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_526(int& val) {
    const int x = 526;
    std::cout << 526;
    for (int i = 0; i < 526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_527(int& val) {
    func_in_527(val);
    const int x = 527;
    std::cout << 527;
    for (int i = 0; i < 527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_527(int& val) {
    const int x = 527;
    std::cout << 527;
    for (int i = 0; i < 527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_528(int& val) {
    func_in_528(val);
    const int x = 528;
    std::cout << 528;
    for (int i = 0; i < 528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_528(int& val) {
    const int x = 528;
    std::cout << 528;
    for (int i = 0; i < 528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_529(int& val) {
    func_in_529(val);
    const int x = 529;
    std::cout << 529;
    for (int i = 0; i < 529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_529(int& val) {
    const int x = 529;
    std::cout << 529;
    for (int i = 0; i < 529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_530(int& val) {
    func_in_530(val);
    const int x = 530;
    std::cout << 530;
    for (int i = 0; i < 530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_530(int& val) {
    const int x = 530;
    std::cout << 530;
    for (int i = 0; i < 530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_531(int& val) {
    func_in_531(val);
    const int x = 531;
    std::cout << 531;
    for (int i = 0; i < 531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_531(int& val) {
    const int x = 531;
    std::cout << 531;
    for (int i = 0; i < 531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_532(int& val) {
    func_in_532(val);
    const int x = 532;
    std::cout << 532;
    for (int i = 0; i < 532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_532(int& val) {
    const int x = 532;
    std::cout << 532;
    for (int i = 0; i < 532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_533(int& val) {
    func_in_533(val);
    const int x = 533;
    std::cout << 533;
    for (int i = 0; i < 533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_533(int& val) {
    const int x = 533;
    std::cout << 533;
    for (int i = 0; i < 533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_534(int& val) {
    func_in_534(val);
    const int x = 534;
    std::cout << 534;
    for (int i = 0; i < 534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_534(int& val) {
    const int x = 534;
    std::cout << 534;
    for (int i = 0; i < 534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_535(int& val) {
    func_in_535(val);
    const int x = 535;
    std::cout << 535;
    for (int i = 0; i < 535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_535(int& val) {
    const int x = 535;
    std::cout << 535;
    for (int i = 0; i < 535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_536(int& val) {
    func_in_536(val);
    const int x = 536;
    std::cout << 536;
    for (int i = 0; i < 536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_536(int& val) {
    const int x = 536;
    std::cout << 536;
    for (int i = 0; i < 536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_537(int& val) {
    func_in_537(val);
    const int x = 537;
    std::cout << 537;
    for (int i = 0; i < 537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_537(int& val) {
    const int x = 537;
    std::cout << 537;
    for (int i = 0; i < 537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_538(int& val) {
    func_in_538(val);
    const int x = 538;
    std::cout << 538;
    for (int i = 0; i < 538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_538(int& val) {
    const int x = 538;
    std::cout << 538;
    for (int i = 0; i < 538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_539(int& val) {
    func_in_539(val);
    const int x = 539;
    std::cout << 539;
    for (int i = 0; i < 539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_539(int& val) {
    const int x = 539;
    std::cout << 539;
    for (int i = 0; i < 539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_540(int& val) {
    func_in_540(val);
    const int x = 540;
    std::cout << 540;
    for (int i = 0; i < 540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_540(int& val) {
    const int x = 540;
    std::cout << 540;
    for (int i = 0; i < 540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_541(int& val) {
    func_in_541(val);
    const int x = 541;
    std::cout << 541;
    for (int i = 0; i < 541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_541(int& val) {
    const int x = 541;
    std::cout << 541;
    for (int i = 0; i < 541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_542(int& val) {
    func_in_542(val);
    const int x = 542;
    std::cout << 542;
    for (int i = 0; i < 542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_542(int& val) {
    const int x = 542;
    std::cout << 542;
    for (int i = 0; i < 542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_543(int& val) {
    func_in_543(val);
    const int x = 543;
    std::cout << 543;
    for (int i = 0; i < 543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_543(int& val) {
    const int x = 543;
    std::cout << 543;
    for (int i = 0; i < 543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_544(int& val) {
    func_in_544(val);
    const int x = 544;
    std::cout << 544;
    for (int i = 0; i < 544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_544(int& val) {
    const int x = 544;
    std::cout << 544;
    for (int i = 0; i < 544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_545(int& val) {
    func_in_545(val);
    const int x = 545;
    std::cout << 545;
    for (int i = 0; i < 545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_545(int& val) {
    const int x = 545;
    std::cout << 545;
    for (int i = 0; i < 545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_546(int& val) {
    func_in_546(val);
    const int x = 546;
    std::cout << 546;
    for (int i = 0; i < 546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_546(int& val) {
    const int x = 546;
    std::cout << 546;
    for (int i = 0; i < 546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_547(int& val) {
    func_in_547(val);
    const int x = 547;
    std::cout << 547;
    for (int i = 0; i < 547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_547(int& val) {
    const int x = 547;
    std::cout << 547;
    for (int i = 0; i < 547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_548(int& val) {
    func_in_548(val);
    const int x = 548;
    std::cout << 548;
    for (int i = 0; i < 548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_548(int& val) {
    const int x = 548;
    std::cout << 548;
    for (int i = 0; i < 548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_549(int& val) {
    func_in_549(val);
    const int x = 549;
    std::cout << 549;
    for (int i = 0; i < 549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_549(int& val) {
    const int x = 549;
    std::cout << 549;
    for (int i = 0; i < 549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_550(int& val) {
    func_in_550(val);
    const int x = 550;
    std::cout << 550;
    for (int i = 0; i < 550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_550(int& val) {
    const int x = 550;
    std::cout << 550;
    for (int i = 0; i < 550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_551(int& val) {
    func_in_551(val);
    const int x = 551;
    std::cout << 551;
    for (int i = 0; i < 551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_551(int& val) {
    const int x = 551;
    std::cout << 551;
    for (int i = 0; i < 551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_552(int& val) {
    func_in_552(val);
    const int x = 552;
    std::cout << 552;
    for (int i = 0; i < 552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_552(int& val) {
    const int x = 552;
    std::cout << 552;
    for (int i = 0; i < 552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_553(int& val) {
    func_in_553(val);
    const int x = 553;
    std::cout << 553;
    for (int i = 0; i < 553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_553(int& val) {
    const int x = 553;
    std::cout << 553;
    for (int i = 0; i < 553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_554(int& val) {
    func_in_554(val);
    const int x = 554;
    std::cout << 554;
    for (int i = 0; i < 554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_554(int& val) {
    const int x = 554;
    std::cout << 554;
    for (int i = 0; i < 554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_555(int& val) {
    func_in_555(val);
    const int x = 555;
    std::cout << 555;
    for (int i = 0; i < 555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_555(int& val) {
    const int x = 555;
    std::cout << 555;
    for (int i = 0; i < 555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_556(int& val) {
    func_in_556(val);
    const int x = 556;
    std::cout << 556;
    for (int i = 0; i < 556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_556(int& val) {
    const int x = 556;
    std::cout << 556;
    for (int i = 0; i < 556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_557(int& val) {
    func_in_557(val);
    const int x = 557;
    std::cout << 557;
    for (int i = 0; i < 557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_557(int& val) {
    const int x = 557;
    std::cout << 557;
    for (int i = 0; i < 557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_558(int& val) {
    func_in_558(val);
    const int x = 558;
    std::cout << 558;
    for (int i = 0; i < 558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_558(int& val) {
    const int x = 558;
    std::cout << 558;
    for (int i = 0; i < 558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_559(int& val) {
    func_in_559(val);
    const int x = 559;
    std::cout << 559;
    for (int i = 0; i < 559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_559(int& val) {
    const int x = 559;
    std::cout << 559;
    for (int i = 0; i < 559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_560(int& val) {
    func_in_560(val);
    const int x = 560;
    std::cout << 560;
    for (int i = 0; i < 560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_560(int& val) {
    const int x = 560;
    std::cout << 560;
    for (int i = 0; i < 560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_561(int& val) {
    func_in_561(val);
    const int x = 561;
    std::cout << 561;
    for (int i = 0; i < 561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_561(int& val) {
    const int x = 561;
    std::cout << 561;
    for (int i = 0; i < 561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_562(int& val) {
    func_in_562(val);
    const int x = 562;
    std::cout << 562;
    for (int i = 0; i < 562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_562(int& val) {
    const int x = 562;
    std::cout << 562;
    for (int i = 0; i < 562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_563(int& val) {
    func_in_563(val);
    const int x = 563;
    std::cout << 563;
    for (int i = 0; i < 563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_563(int& val) {
    const int x = 563;
    std::cout << 563;
    for (int i = 0; i < 563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_564(int& val) {
    func_in_564(val);
    const int x = 564;
    std::cout << 564;
    for (int i = 0; i < 564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_564(int& val) {
    const int x = 564;
    std::cout << 564;
    for (int i = 0; i < 564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_565(int& val) {
    func_in_565(val);
    const int x = 565;
    std::cout << 565;
    for (int i = 0; i < 565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_565(int& val) {
    const int x = 565;
    std::cout << 565;
    for (int i = 0; i < 565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_566(int& val) {
    func_in_566(val);
    const int x = 566;
    std::cout << 566;
    for (int i = 0; i < 566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_566(int& val) {
    const int x = 566;
    std::cout << 566;
    for (int i = 0; i < 566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_567(int& val) {
    func_in_567(val);
    const int x = 567;
    std::cout << 567;
    for (int i = 0; i < 567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_567(int& val) {
    const int x = 567;
    std::cout << 567;
    for (int i = 0; i < 567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_568(int& val) {
    func_in_568(val);
    const int x = 568;
    std::cout << 568;
    for (int i = 0; i < 568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_568(int& val) {
    const int x = 568;
    std::cout << 568;
    for (int i = 0; i < 568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_569(int& val) {
    func_in_569(val);
    const int x = 569;
    std::cout << 569;
    for (int i = 0; i < 569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_569(int& val) {
    const int x = 569;
    std::cout << 569;
    for (int i = 0; i < 569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_570(int& val) {
    func_in_570(val);
    const int x = 570;
    std::cout << 570;
    for (int i = 0; i < 570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_570(int& val) {
    const int x = 570;
    std::cout << 570;
    for (int i = 0; i < 570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_571(int& val) {
    func_in_571(val);
    const int x = 571;
    std::cout << 571;
    for (int i = 0; i < 571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_571(int& val) {
    const int x = 571;
    std::cout << 571;
    for (int i = 0; i < 571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_572(int& val) {
    func_in_572(val);
    const int x = 572;
    std::cout << 572;
    for (int i = 0; i < 572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_572(int& val) {
    const int x = 572;
    std::cout << 572;
    for (int i = 0; i < 572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_573(int& val) {
    func_in_573(val);
    const int x = 573;
    std::cout << 573;
    for (int i = 0; i < 573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_573(int& val) {
    const int x = 573;
    std::cout << 573;
    for (int i = 0; i < 573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_574(int& val) {
    func_in_574(val);
    const int x = 574;
    std::cout << 574;
    for (int i = 0; i < 574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_574(int& val) {
    const int x = 574;
    std::cout << 574;
    for (int i = 0; i < 574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_575(int& val) {
    func_in_575(val);
    const int x = 575;
    std::cout << 575;
    for (int i = 0; i < 575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_575(int& val) {
    const int x = 575;
    std::cout << 575;
    for (int i = 0; i < 575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_576(int& val) {
    func_in_576(val);
    const int x = 576;
    std::cout << 576;
    for (int i = 0; i < 576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_576(int& val) {
    const int x = 576;
    std::cout << 576;
    for (int i = 0; i < 576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_577(int& val) {
    func_in_577(val);
    const int x = 577;
    std::cout << 577;
    for (int i = 0; i < 577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_577(int& val) {
    const int x = 577;
    std::cout << 577;
    for (int i = 0; i < 577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_578(int& val) {
    func_in_578(val);
    const int x = 578;
    std::cout << 578;
    for (int i = 0; i < 578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_578(int& val) {
    const int x = 578;
    std::cout << 578;
    for (int i = 0; i < 578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_579(int& val) {
    func_in_579(val);
    const int x = 579;
    std::cout << 579;
    for (int i = 0; i < 579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_579(int& val) {
    const int x = 579;
    std::cout << 579;
    for (int i = 0; i < 579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_580(int& val) {
    func_in_580(val);
    const int x = 580;
    std::cout << 580;
    for (int i = 0; i < 580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_580(int& val) {
    const int x = 580;
    std::cout << 580;
    for (int i = 0; i < 580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_581(int& val) {
    func_in_581(val);
    const int x = 581;
    std::cout << 581;
    for (int i = 0; i < 581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_581(int& val) {
    const int x = 581;
    std::cout << 581;
    for (int i = 0; i < 581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_582(int& val) {
    func_in_582(val);
    const int x = 582;
    std::cout << 582;
    for (int i = 0; i < 582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_582(int& val) {
    const int x = 582;
    std::cout << 582;
    for (int i = 0; i < 582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_583(int& val) {
    func_in_583(val);
    const int x = 583;
    std::cout << 583;
    for (int i = 0; i < 583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_583(int& val) {
    const int x = 583;
    std::cout << 583;
    for (int i = 0; i < 583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_584(int& val) {
    func_in_584(val);
    const int x = 584;
    std::cout << 584;
    for (int i = 0; i < 584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_584(int& val) {
    const int x = 584;
    std::cout << 584;
    for (int i = 0; i < 584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_585(int& val) {
    func_in_585(val);
    const int x = 585;
    std::cout << 585;
    for (int i = 0; i < 585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_585(int& val) {
    const int x = 585;
    std::cout << 585;
    for (int i = 0; i < 585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_586(int& val) {
    func_in_586(val);
    const int x = 586;
    std::cout << 586;
    for (int i = 0; i < 586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_586(int& val) {
    const int x = 586;
    std::cout << 586;
    for (int i = 0; i < 586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_587(int& val) {
    func_in_587(val);
    const int x = 587;
    std::cout << 587;
    for (int i = 0; i < 587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_587(int& val) {
    const int x = 587;
    std::cout << 587;
    for (int i = 0; i < 587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_588(int& val) {
    func_in_588(val);
    const int x = 588;
    std::cout << 588;
    for (int i = 0; i < 588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_588(int& val) {
    const int x = 588;
    std::cout << 588;
    for (int i = 0; i < 588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_589(int& val) {
    func_in_589(val);
    const int x = 589;
    std::cout << 589;
    for (int i = 0; i < 589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_589(int& val) {
    const int x = 589;
    std::cout << 589;
    for (int i = 0; i < 589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_590(int& val) {
    func_in_590(val);
    const int x = 590;
    std::cout << 590;
    for (int i = 0; i < 590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_590(int& val) {
    const int x = 590;
    std::cout << 590;
    for (int i = 0; i < 590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_591(int& val) {
    func_in_591(val);
    const int x = 591;
    std::cout << 591;
    for (int i = 0; i < 591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_591(int& val) {
    const int x = 591;
    std::cout << 591;
    for (int i = 0; i < 591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_592(int& val) {
    func_in_592(val);
    const int x = 592;
    std::cout << 592;
    for (int i = 0; i < 592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_592(int& val) {
    const int x = 592;
    std::cout << 592;
    for (int i = 0; i < 592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_593(int& val) {
    func_in_593(val);
    const int x = 593;
    std::cout << 593;
    for (int i = 0; i < 593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_593(int& val) {
    const int x = 593;
    std::cout << 593;
    for (int i = 0; i < 593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_594(int& val) {
    func_in_594(val);
    const int x = 594;
    std::cout << 594;
    for (int i = 0; i < 594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_594(int& val) {
    const int x = 594;
    std::cout << 594;
    for (int i = 0; i < 594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_595(int& val) {
    func_in_595(val);
    const int x = 595;
    std::cout << 595;
    for (int i = 0; i < 595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_595(int& val) {
    const int x = 595;
    std::cout << 595;
    for (int i = 0; i < 595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_596(int& val) {
    func_in_596(val);
    const int x = 596;
    std::cout << 596;
    for (int i = 0; i < 596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_596(int& val) {
    const int x = 596;
    std::cout << 596;
    for (int i = 0; i < 596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_597(int& val) {
    func_in_597(val);
    const int x = 597;
    std::cout << 597;
    for (int i = 0; i < 597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_597(int& val) {
    const int x = 597;
    std::cout << 597;
    for (int i = 0; i < 597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_598(int& val) {
    func_in_598(val);
    const int x = 598;
    std::cout << 598;
    for (int i = 0; i < 598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_598(int& val) {
    const int x = 598;
    std::cout << 598;
    for (int i = 0; i < 598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_599(int& val) {
    func_in_599(val);
    const int x = 599;
    std::cout << 599;
    for (int i = 0; i < 599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_599(int& val) {
    const int x = 599;
    std::cout << 599;
    for (int i = 0; i < 599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_600(int& val) {
    func_in_600(val);
    const int x = 600;
    std::cout << 600;
    for (int i = 0; i < 600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_600(int& val) {
    const int x = 600;
    std::cout << 600;
    for (int i = 0; i < 600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_601(int& val) {
    func_in_601(val);
    const int x = 601;
    std::cout << 601;
    for (int i = 0; i < 601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_601(int& val) {
    const int x = 601;
    std::cout << 601;
    for (int i = 0; i < 601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_602(int& val) {
    func_in_602(val);
    const int x = 602;
    std::cout << 602;
    for (int i = 0; i < 602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_602(int& val) {
    const int x = 602;
    std::cout << 602;
    for (int i = 0; i < 602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_603(int& val) {
    func_in_603(val);
    const int x = 603;
    std::cout << 603;
    for (int i = 0; i < 603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_603(int& val) {
    const int x = 603;
    std::cout << 603;
    for (int i = 0; i < 603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_604(int& val) {
    func_in_604(val);
    const int x = 604;
    std::cout << 604;
    for (int i = 0; i < 604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_604(int& val) {
    const int x = 604;
    std::cout << 604;
    for (int i = 0; i < 604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_605(int& val) {
    func_in_605(val);
    const int x = 605;
    std::cout << 605;
    for (int i = 0; i < 605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_605(int& val) {
    const int x = 605;
    std::cout << 605;
    for (int i = 0; i < 605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_606(int& val) {
    func_in_606(val);
    const int x = 606;
    std::cout << 606;
    for (int i = 0; i < 606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_606(int& val) {
    const int x = 606;
    std::cout << 606;
    for (int i = 0; i < 606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_607(int& val) {
    func_in_607(val);
    const int x = 607;
    std::cout << 607;
    for (int i = 0; i < 607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_607(int& val) {
    const int x = 607;
    std::cout << 607;
    for (int i = 0; i < 607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_608(int& val) {
    func_in_608(val);
    const int x = 608;
    std::cout << 608;
    for (int i = 0; i < 608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_608(int& val) {
    const int x = 608;
    std::cout << 608;
    for (int i = 0; i < 608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_609(int& val) {
    func_in_609(val);
    const int x = 609;
    std::cout << 609;
    for (int i = 0; i < 609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_609(int& val) {
    const int x = 609;
    std::cout << 609;
    for (int i = 0; i < 609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_610(int& val) {
    func_in_610(val);
    const int x = 610;
    std::cout << 610;
    for (int i = 0; i < 610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_610(int& val) {
    const int x = 610;
    std::cout << 610;
    for (int i = 0; i < 610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_611(int& val) {
    func_in_611(val);
    const int x = 611;
    std::cout << 611;
    for (int i = 0; i < 611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_611(int& val) {
    const int x = 611;
    std::cout << 611;
    for (int i = 0; i < 611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_612(int& val) {
    func_in_612(val);
    const int x = 612;
    std::cout << 612;
    for (int i = 0; i < 612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_612(int& val) {
    const int x = 612;
    std::cout << 612;
    for (int i = 0; i < 612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_613(int& val) {
    func_in_613(val);
    const int x = 613;
    std::cout << 613;
    for (int i = 0; i < 613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_613(int& val) {
    const int x = 613;
    std::cout << 613;
    for (int i = 0; i < 613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_614(int& val) {
    func_in_614(val);
    const int x = 614;
    std::cout << 614;
    for (int i = 0; i < 614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_614(int& val) {
    const int x = 614;
    std::cout << 614;
    for (int i = 0; i < 614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_615(int& val) {
    func_in_615(val);
    const int x = 615;
    std::cout << 615;
    for (int i = 0; i < 615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_615(int& val) {
    const int x = 615;
    std::cout << 615;
    for (int i = 0; i < 615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_616(int& val) {
    func_in_616(val);
    const int x = 616;
    std::cout << 616;
    for (int i = 0; i < 616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_616(int& val) {
    const int x = 616;
    std::cout << 616;
    for (int i = 0; i < 616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_617(int& val) {
    func_in_617(val);
    const int x = 617;
    std::cout << 617;
    for (int i = 0; i < 617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_617(int& val) {
    const int x = 617;
    std::cout << 617;
    for (int i = 0; i < 617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_618(int& val) {
    func_in_618(val);
    const int x = 618;
    std::cout << 618;
    for (int i = 0; i < 618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_618(int& val) {
    const int x = 618;
    std::cout << 618;
    for (int i = 0; i < 618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_619(int& val) {
    func_in_619(val);
    const int x = 619;
    std::cout << 619;
    for (int i = 0; i < 619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_619(int& val) {
    const int x = 619;
    std::cout << 619;
    for (int i = 0; i < 619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_620(int& val) {
    func_in_620(val);
    const int x = 620;
    std::cout << 620;
    for (int i = 0; i < 620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_620(int& val) {
    const int x = 620;
    std::cout << 620;
    for (int i = 0; i < 620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_621(int& val) {
    func_in_621(val);
    const int x = 621;
    std::cout << 621;
    for (int i = 0; i < 621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_621(int& val) {
    const int x = 621;
    std::cout << 621;
    for (int i = 0; i < 621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_622(int& val) {
    func_in_622(val);
    const int x = 622;
    std::cout << 622;
    for (int i = 0; i < 622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_622(int& val) {
    const int x = 622;
    std::cout << 622;
    for (int i = 0; i < 622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_623(int& val) {
    func_in_623(val);
    const int x = 623;
    std::cout << 623;
    for (int i = 0; i < 623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_623(int& val) {
    const int x = 623;
    std::cout << 623;
    for (int i = 0; i < 623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_624(int& val) {
    func_in_624(val);
    const int x = 624;
    std::cout << 624;
    for (int i = 0; i < 624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_624(int& val) {
    const int x = 624;
    std::cout << 624;
    for (int i = 0; i < 624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_625(int& val) {
    func_in_625(val);
    const int x = 625;
    std::cout << 625;
    for (int i = 0; i < 625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_625(int& val) {
    const int x = 625;
    std::cout << 625;
    for (int i = 0; i < 625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_626(int& val) {
    func_in_626(val);
    const int x = 626;
    std::cout << 626;
    for (int i = 0; i < 626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_626(int& val) {
    const int x = 626;
    std::cout << 626;
    for (int i = 0; i < 626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_627(int& val) {
    func_in_627(val);
    const int x = 627;
    std::cout << 627;
    for (int i = 0; i < 627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_627(int& val) {
    const int x = 627;
    std::cout << 627;
    for (int i = 0; i < 627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_628(int& val) {
    func_in_628(val);
    const int x = 628;
    std::cout << 628;
    for (int i = 0; i < 628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_628(int& val) {
    const int x = 628;
    std::cout << 628;
    for (int i = 0; i < 628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_629(int& val) {
    func_in_629(val);
    const int x = 629;
    std::cout << 629;
    for (int i = 0; i < 629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_629(int& val) {
    const int x = 629;
    std::cout << 629;
    for (int i = 0; i < 629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_630(int& val) {
    func_in_630(val);
    const int x = 630;
    std::cout << 630;
    for (int i = 0; i < 630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_630(int& val) {
    const int x = 630;
    std::cout << 630;
    for (int i = 0; i < 630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_631(int& val) {
    func_in_631(val);
    const int x = 631;
    std::cout << 631;
    for (int i = 0; i < 631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_631(int& val) {
    const int x = 631;
    std::cout << 631;
    for (int i = 0; i < 631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_632(int& val) {
    func_in_632(val);
    const int x = 632;
    std::cout << 632;
    for (int i = 0; i < 632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_632(int& val) {
    const int x = 632;
    std::cout << 632;
    for (int i = 0; i < 632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_633(int& val) {
    func_in_633(val);
    const int x = 633;
    std::cout << 633;
    for (int i = 0; i < 633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_633(int& val) {
    const int x = 633;
    std::cout << 633;
    for (int i = 0; i < 633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_634(int& val) {
    func_in_634(val);
    const int x = 634;
    std::cout << 634;
    for (int i = 0; i < 634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_634(int& val) {
    const int x = 634;
    std::cout << 634;
    for (int i = 0; i < 634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_635(int& val) {
    func_in_635(val);
    const int x = 635;
    std::cout << 635;
    for (int i = 0; i < 635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_635(int& val) {
    const int x = 635;
    std::cout << 635;
    for (int i = 0; i < 635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_636(int& val) {
    func_in_636(val);
    const int x = 636;
    std::cout << 636;
    for (int i = 0; i < 636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_636(int& val) {
    const int x = 636;
    std::cout << 636;
    for (int i = 0; i < 636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_637(int& val) {
    func_in_637(val);
    const int x = 637;
    std::cout << 637;
    for (int i = 0; i < 637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_637(int& val) {
    const int x = 637;
    std::cout << 637;
    for (int i = 0; i < 637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_638(int& val) {
    func_in_638(val);
    const int x = 638;
    std::cout << 638;
    for (int i = 0; i < 638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_638(int& val) {
    const int x = 638;
    std::cout << 638;
    for (int i = 0; i < 638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_639(int& val) {
    func_in_639(val);
    const int x = 639;
    std::cout << 639;
    for (int i = 0; i < 639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_639(int& val) {
    const int x = 639;
    std::cout << 639;
    for (int i = 0; i < 639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_640(int& val) {
    func_in_640(val);
    const int x = 640;
    std::cout << 640;
    for (int i = 0; i < 640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_640(int& val) {
    const int x = 640;
    std::cout << 640;
    for (int i = 0; i < 640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_641(int& val) {
    func_in_641(val);
    const int x = 641;
    std::cout << 641;
    for (int i = 0; i < 641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_641(int& val) {
    const int x = 641;
    std::cout << 641;
    for (int i = 0; i < 641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_642(int& val) {
    func_in_642(val);
    const int x = 642;
    std::cout << 642;
    for (int i = 0; i < 642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_642(int& val) {
    const int x = 642;
    std::cout << 642;
    for (int i = 0; i < 642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_643(int& val) {
    func_in_643(val);
    const int x = 643;
    std::cout << 643;
    for (int i = 0; i < 643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_643(int& val) {
    const int x = 643;
    std::cout << 643;
    for (int i = 0; i < 643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_644(int& val) {
    func_in_644(val);
    const int x = 644;
    std::cout << 644;
    for (int i = 0; i < 644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_644(int& val) {
    const int x = 644;
    std::cout << 644;
    for (int i = 0; i < 644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_645(int& val) {
    func_in_645(val);
    const int x = 645;
    std::cout << 645;
    for (int i = 0; i < 645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_645(int& val) {
    const int x = 645;
    std::cout << 645;
    for (int i = 0; i < 645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_646(int& val) {
    func_in_646(val);
    const int x = 646;
    std::cout << 646;
    for (int i = 0; i < 646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_646(int& val) {
    const int x = 646;
    std::cout << 646;
    for (int i = 0; i < 646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_647(int& val) {
    func_in_647(val);
    const int x = 647;
    std::cout << 647;
    for (int i = 0; i < 647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_647(int& val) {
    const int x = 647;
    std::cout << 647;
    for (int i = 0; i < 647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_648(int& val) {
    func_in_648(val);
    const int x = 648;
    std::cout << 648;
    for (int i = 0; i < 648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_648(int& val) {
    const int x = 648;
    std::cout << 648;
    for (int i = 0; i < 648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_649(int& val) {
    func_in_649(val);
    const int x = 649;
    std::cout << 649;
    for (int i = 0; i < 649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_649(int& val) {
    const int x = 649;
    std::cout << 649;
    for (int i = 0; i < 649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_650(int& val) {
    func_in_650(val);
    const int x = 650;
    std::cout << 650;
    for (int i = 0; i < 650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_650(int& val) {
    const int x = 650;
    std::cout << 650;
    for (int i = 0; i < 650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_651(int& val) {
    func_in_651(val);
    const int x = 651;
    std::cout << 651;
    for (int i = 0; i < 651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_651(int& val) {
    const int x = 651;
    std::cout << 651;
    for (int i = 0; i < 651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_652(int& val) {
    func_in_652(val);
    const int x = 652;
    std::cout << 652;
    for (int i = 0; i < 652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_652(int& val) {
    const int x = 652;
    std::cout << 652;
    for (int i = 0; i < 652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_653(int& val) {
    func_in_653(val);
    const int x = 653;
    std::cout << 653;
    for (int i = 0; i < 653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_653(int& val) {
    const int x = 653;
    std::cout << 653;
    for (int i = 0; i < 653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_654(int& val) {
    func_in_654(val);
    const int x = 654;
    std::cout << 654;
    for (int i = 0; i < 654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_654(int& val) {
    const int x = 654;
    std::cout << 654;
    for (int i = 0; i < 654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_655(int& val) {
    func_in_655(val);
    const int x = 655;
    std::cout << 655;
    for (int i = 0; i < 655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_655(int& val) {
    const int x = 655;
    std::cout << 655;
    for (int i = 0; i < 655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_656(int& val) {
    func_in_656(val);
    const int x = 656;
    std::cout << 656;
    for (int i = 0; i < 656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_656(int& val) {
    const int x = 656;
    std::cout << 656;
    for (int i = 0; i < 656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_657(int& val) {
    func_in_657(val);
    const int x = 657;
    std::cout << 657;
    for (int i = 0; i < 657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_657(int& val) {
    const int x = 657;
    std::cout << 657;
    for (int i = 0; i < 657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_658(int& val) {
    func_in_658(val);
    const int x = 658;
    std::cout << 658;
    for (int i = 0; i < 658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_658(int& val) {
    const int x = 658;
    std::cout << 658;
    for (int i = 0; i < 658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_659(int& val) {
    func_in_659(val);
    const int x = 659;
    std::cout << 659;
    for (int i = 0; i < 659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_659(int& val) {
    const int x = 659;
    std::cout << 659;
    for (int i = 0; i < 659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_660(int& val) {
    func_in_660(val);
    const int x = 660;
    std::cout << 660;
    for (int i = 0; i < 660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_660(int& val) {
    const int x = 660;
    std::cout << 660;
    for (int i = 0; i < 660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_661(int& val) {
    func_in_661(val);
    const int x = 661;
    std::cout << 661;
    for (int i = 0; i < 661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_661(int& val) {
    const int x = 661;
    std::cout << 661;
    for (int i = 0; i < 661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_662(int& val) {
    func_in_662(val);
    const int x = 662;
    std::cout << 662;
    for (int i = 0; i < 662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_662(int& val) {
    const int x = 662;
    std::cout << 662;
    for (int i = 0; i < 662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_663(int& val) {
    func_in_663(val);
    const int x = 663;
    std::cout << 663;
    for (int i = 0; i < 663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_663(int& val) {
    const int x = 663;
    std::cout << 663;
    for (int i = 0; i < 663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_664(int& val) {
    func_in_664(val);
    const int x = 664;
    std::cout << 664;
    for (int i = 0; i < 664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_664(int& val) {
    const int x = 664;
    std::cout << 664;
    for (int i = 0; i < 664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_665(int& val) {
    func_in_665(val);
    const int x = 665;
    std::cout << 665;
    for (int i = 0; i < 665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_665(int& val) {
    const int x = 665;
    std::cout << 665;
    for (int i = 0; i < 665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_666(int& val) {
    func_in_666(val);
    const int x = 666;
    std::cout << 666;
    for (int i = 0; i < 666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_666(int& val) {
    const int x = 666;
    std::cout << 666;
    for (int i = 0; i < 666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_667(int& val) {
    func_in_667(val);
    const int x = 667;
    std::cout << 667;
    for (int i = 0; i < 667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_667(int& val) {
    const int x = 667;
    std::cout << 667;
    for (int i = 0; i < 667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_668(int& val) {
    func_in_668(val);
    const int x = 668;
    std::cout << 668;
    for (int i = 0; i < 668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_668(int& val) {
    const int x = 668;
    std::cout << 668;
    for (int i = 0; i < 668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_669(int& val) {
    func_in_669(val);
    const int x = 669;
    std::cout << 669;
    for (int i = 0; i < 669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_669(int& val) {
    const int x = 669;
    std::cout << 669;
    for (int i = 0; i < 669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_670(int& val) {
    func_in_670(val);
    const int x = 670;
    std::cout << 670;
    for (int i = 0; i < 670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_670(int& val) {
    const int x = 670;
    std::cout << 670;
    for (int i = 0; i < 670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_671(int& val) {
    func_in_671(val);
    const int x = 671;
    std::cout << 671;
    for (int i = 0; i < 671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_671(int& val) {
    const int x = 671;
    std::cout << 671;
    for (int i = 0; i < 671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_672(int& val) {
    func_in_672(val);
    const int x = 672;
    std::cout << 672;
    for (int i = 0; i < 672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_672(int& val) {
    const int x = 672;
    std::cout << 672;
    for (int i = 0; i < 672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_673(int& val) {
    func_in_673(val);
    const int x = 673;
    std::cout << 673;
    for (int i = 0; i < 673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_673(int& val) {
    const int x = 673;
    std::cout << 673;
    for (int i = 0; i < 673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_674(int& val) {
    func_in_674(val);
    const int x = 674;
    std::cout << 674;
    for (int i = 0; i < 674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_674(int& val) {
    const int x = 674;
    std::cout << 674;
    for (int i = 0; i < 674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_675(int& val) {
    func_in_675(val);
    const int x = 675;
    std::cout << 675;
    for (int i = 0; i < 675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_675(int& val) {
    const int x = 675;
    std::cout << 675;
    for (int i = 0; i < 675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_676(int& val) {
    func_in_676(val);
    const int x = 676;
    std::cout << 676;
    for (int i = 0; i < 676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_676(int& val) {
    const int x = 676;
    std::cout << 676;
    for (int i = 0; i < 676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_677(int& val) {
    func_in_677(val);
    const int x = 677;
    std::cout << 677;
    for (int i = 0; i < 677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_677(int& val) {
    const int x = 677;
    std::cout << 677;
    for (int i = 0; i < 677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_678(int& val) {
    func_in_678(val);
    const int x = 678;
    std::cout << 678;
    for (int i = 0; i < 678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_678(int& val) {
    const int x = 678;
    std::cout << 678;
    for (int i = 0; i < 678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_679(int& val) {
    func_in_679(val);
    const int x = 679;
    std::cout << 679;
    for (int i = 0; i < 679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_679(int& val) {
    const int x = 679;
    std::cout << 679;
    for (int i = 0; i < 679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_680(int& val) {
    func_in_680(val);
    const int x = 680;
    std::cout << 680;
    for (int i = 0; i < 680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_680(int& val) {
    const int x = 680;
    std::cout << 680;
    for (int i = 0; i < 680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_681(int& val) {
    func_in_681(val);
    const int x = 681;
    std::cout << 681;
    for (int i = 0; i < 681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_681(int& val) {
    const int x = 681;
    std::cout << 681;
    for (int i = 0; i < 681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_682(int& val) {
    func_in_682(val);
    const int x = 682;
    std::cout << 682;
    for (int i = 0; i < 682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_682(int& val) {
    const int x = 682;
    std::cout << 682;
    for (int i = 0; i < 682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_683(int& val) {
    func_in_683(val);
    const int x = 683;
    std::cout << 683;
    for (int i = 0; i < 683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_683(int& val) {
    const int x = 683;
    std::cout << 683;
    for (int i = 0; i < 683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_684(int& val) {
    func_in_684(val);
    const int x = 684;
    std::cout << 684;
    for (int i = 0; i < 684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_684(int& val) {
    const int x = 684;
    std::cout << 684;
    for (int i = 0; i < 684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_685(int& val) {
    func_in_685(val);
    const int x = 685;
    std::cout << 685;
    for (int i = 0; i < 685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_685(int& val) {
    const int x = 685;
    std::cout << 685;
    for (int i = 0; i < 685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_686(int& val) {
    func_in_686(val);
    const int x = 686;
    std::cout << 686;
    for (int i = 0; i < 686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_686(int& val) {
    const int x = 686;
    std::cout << 686;
    for (int i = 0; i < 686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_687(int& val) {
    func_in_687(val);
    const int x = 687;
    std::cout << 687;
    for (int i = 0; i < 687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_687(int& val) {
    const int x = 687;
    std::cout << 687;
    for (int i = 0; i < 687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_688(int& val) {
    func_in_688(val);
    const int x = 688;
    std::cout << 688;
    for (int i = 0; i < 688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_688(int& val) {
    const int x = 688;
    std::cout << 688;
    for (int i = 0; i < 688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_689(int& val) {
    func_in_689(val);
    const int x = 689;
    std::cout << 689;
    for (int i = 0; i < 689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_689(int& val) {
    const int x = 689;
    std::cout << 689;
    for (int i = 0; i < 689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_690(int& val) {
    func_in_690(val);
    const int x = 690;
    std::cout << 690;
    for (int i = 0; i < 690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_690(int& val) {
    const int x = 690;
    std::cout << 690;
    for (int i = 0; i < 690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_691(int& val) {
    func_in_691(val);
    const int x = 691;
    std::cout << 691;
    for (int i = 0; i < 691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_691(int& val) {
    const int x = 691;
    std::cout << 691;
    for (int i = 0; i < 691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_692(int& val) {
    func_in_692(val);
    const int x = 692;
    std::cout << 692;
    for (int i = 0; i < 692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_692(int& val) {
    const int x = 692;
    std::cout << 692;
    for (int i = 0; i < 692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_693(int& val) {
    func_in_693(val);
    const int x = 693;
    std::cout << 693;
    for (int i = 0; i < 693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_693(int& val) {
    const int x = 693;
    std::cout << 693;
    for (int i = 0; i < 693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_694(int& val) {
    func_in_694(val);
    const int x = 694;
    std::cout << 694;
    for (int i = 0; i < 694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_694(int& val) {
    const int x = 694;
    std::cout << 694;
    for (int i = 0; i < 694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_695(int& val) {
    func_in_695(val);
    const int x = 695;
    std::cout << 695;
    for (int i = 0; i < 695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_695(int& val) {
    const int x = 695;
    std::cout << 695;
    for (int i = 0; i < 695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_696(int& val) {
    func_in_696(val);
    const int x = 696;
    std::cout << 696;
    for (int i = 0; i < 696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_696(int& val) {
    const int x = 696;
    std::cout << 696;
    for (int i = 0; i < 696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_697(int& val) {
    func_in_697(val);
    const int x = 697;
    std::cout << 697;
    for (int i = 0; i < 697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_697(int& val) {
    const int x = 697;
    std::cout << 697;
    for (int i = 0; i < 697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_698(int& val) {
    func_in_698(val);
    const int x = 698;
    std::cout << 698;
    for (int i = 0; i < 698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_698(int& val) {
    const int x = 698;
    std::cout << 698;
    for (int i = 0; i < 698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_699(int& val) {
    func_in_699(val);
    const int x = 699;
    std::cout << 699;
    for (int i = 0; i < 699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_699(int& val) {
    const int x = 699;
    std::cout << 699;
    for (int i = 0; i < 699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_700(int& val) {
    func_in_700(val);
    const int x = 700;
    std::cout << 700;
    for (int i = 0; i < 700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_700(int& val) {
    const int x = 700;
    std::cout << 700;
    for (int i = 0; i < 700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_701(int& val) {
    func_in_701(val);
    const int x = 701;
    std::cout << 701;
    for (int i = 0; i < 701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_701(int& val) {
    const int x = 701;
    std::cout << 701;
    for (int i = 0; i < 701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_702(int& val) {
    func_in_702(val);
    const int x = 702;
    std::cout << 702;
    for (int i = 0; i < 702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_702(int& val) {
    const int x = 702;
    std::cout << 702;
    for (int i = 0; i < 702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_703(int& val) {
    func_in_703(val);
    const int x = 703;
    std::cout << 703;
    for (int i = 0; i < 703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_703(int& val) {
    const int x = 703;
    std::cout << 703;
    for (int i = 0; i < 703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_704(int& val) {
    func_in_704(val);
    const int x = 704;
    std::cout << 704;
    for (int i = 0; i < 704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_704(int& val) {
    const int x = 704;
    std::cout << 704;
    for (int i = 0; i < 704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_705(int& val) {
    func_in_705(val);
    const int x = 705;
    std::cout << 705;
    for (int i = 0; i < 705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_705(int& val) {
    const int x = 705;
    std::cout << 705;
    for (int i = 0; i < 705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_706(int& val) {
    func_in_706(val);
    const int x = 706;
    std::cout << 706;
    for (int i = 0; i < 706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_706(int& val) {
    const int x = 706;
    std::cout << 706;
    for (int i = 0; i < 706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_707(int& val) {
    func_in_707(val);
    const int x = 707;
    std::cout << 707;
    for (int i = 0; i < 707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_707(int& val) {
    const int x = 707;
    std::cout << 707;
    for (int i = 0; i < 707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_708(int& val) {
    func_in_708(val);
    const int x = 708;
    std::cout << 708;
    for (int i = 0; i < 708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_708(int& val) {
    const int x = 708;
    std::cout << 708;
    for (int i = 0; i < 708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_709(int& val) {
    func_in_709(val);
    const int x = 709;
    std::cout << 709;
    for (int i = 0; i < 709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_709(int& val) {
    const int x = 709;
    std::cout << 709;
    for (int i = 0; i < 709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_710(int& val) {
    func_in_710(val);
    const int x = 710;
    std::cout << 710;
    for (int i = 0; i < 710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_710(int& val) {
    const int x = 710;
    std::cout << 710;
    for (int i = 0; i < 710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_711(int& val) {
    func_in_711(val);
    const int x = 711;
    std::cout << 711;
    for (int i = 0; i < 711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_711(int& val) {
    const int x = 711;
    std::cout << 711;
    for (int i = 0; i < 711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_712(int& val) {
    func_in_712(val);
    const int x = 712;
    std::cout << 712;
    for (int i = 0; i < 712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_712(int& val) {
    const int x = 712;
    std::cout << 712;
    for (int i = 0; i < 712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_713(int& val) {
    func_in_713(val);
    const int x = 713;
    std::cout << 713;
    for (int i = 0; i < 713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_713(int& val) {
    const int x = 713;
    std::cout << 713;
    for (int i = 0; i < 713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_714(int& val) {
    func_in_714(val);
    const int x = 714;
    std::cout << 714;
    for (int i = 0; i < 714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_714(int& val) {
    const int x = 714;
    std::cout << 714;
    for (int i = 0; i < 714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_715(int& val) {
    func_in_715(val);
    const int x = 715;
    std::cout << 715;
    for (int i = 0; i < 715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_715(int& val) {
    const int x = 715;
    std::cout << 715;
    for (int i = 0; i < 715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_716(int& val) {
    func_in_716(val);
    const int x = 716;
    std::cout << 716;
    for (int i = 0; i < 716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_716(int& val) {
    const int x = 716;
    std::cout << 716;
    for (int i = 0; i < 716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_717(int& val) {
    func_in_717(val);
    const int x = 717;
    std::cout << 717;
    for (int i = 0; i < 717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_717(int& val) {
    const int x = 717;
    std::cout << 717;
    for (int i = 0; i < 717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_718(int& val) {
    func_in_718(val);
    const int x = 718;
    std::cout << 718;
    for (int i = 0; i < 718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_718(int& val) {
    const int x = 718;
    std::cout << 718;
    for (int i = 0; i < 718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_719(int& val) {
    func_in_719(val);
    const int x = 719;
    std::cout << 719;
    for (int i = 0; i < 719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_719(int& val) {
    const int x = 719;
    std::cout << 719;
    for (int i = 0; i < 719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_720(int& val) {
    func_in_720(val);
    const int x = 720;
    std::cout << 720;
    for (int i = 0; i < 720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_720(int& val) {
    const int x = 720;
    std::cout << 720;
    for (int i = 0; i < 720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_721(int& val) {
    func_in_721(val);
    const int x = 721;
    std::cout << 721;
    for (int i = 0; i < 721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_721(int& val) {
    const int x = 721;
    std::cout << 721;
    for (int i = 0; i < 721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_722(int& val) {
    func_in_722(val);
    const int x = 722;
    std::cout << 722;
    for (int i = 0; i < 722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_722(int& val) {
    const int x = 722;
    std::cout << 722;
    for (int i = 0; i < 722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_723(int& val) {
    func_in_723(val);
    const int x = 723;
    std::cout << 723;
    for (int i = 0; i < 723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_723(int& val) {
    const int x = 723;
    std::cout << 723;
    for (int i = 0; i < 723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_724(int& val) {
    func_in_724(val);
    const int x = 724;
    std::cout << 724;
    for (int i = 0; i < 724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_724(int& val) {
    const int x = 724;
    std::cout << 724;
    for (int i = 0; i < 724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_725(int& val) {
    func_in_725(val);
    const int x = 725;
    std::cout << 725;
    for (int i = 0; i < 725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_725(int& val) {
    const int x = 725;
    std::cout << 725;
    for (int i = 0; i < 725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_726(int& val) {
    func_in_726(val);
    const int x = 726;
    std::cout << 726;
    for (int i = 0; i < 726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_726(int& val) {
    const int x = 726;
    std::cout << 726;
    for (int i = 0; i < 726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_727(int& val) {
    func_in_727(val);
    const int x = 727;
    std::cout << 727;
    for (int i = 0; i < 727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_727(int& val) {
    const int x = 727;
    std::cout << 727;
    for (int i = 0; i < 727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_728(int& val) {
    func_in_728(val);
    const int x = 728;
    std::cout << 728;
    for (int i = 0; i < 728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_728(int& val) {
    const int x = 728;
    std::cout << 728;
    for (int i = 0; i < 728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_729(int& val) {
    func_in_729(val);
    const int x = 729;
    std::cout << 729;
    for (int i = 0; i < 729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_729(int& val) {
    const int x = 729;
    std::cout << 729;
    for (int i = 0; i < 729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_730(int& val) {
    func_in_730(val);
    const int x = 730;
    std::cout << 730;
    for (int i = 0; i < 730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_730(int& val) {
    const int x = 730;
    std::cout << 730;
    for (int i = 0; i < 730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_731(int& val) {
    func_in_731(val);
    const int x = 731;
    std::cout << 731;
    for (int i = 0; i < 731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_731(int& val) {
    const int x = 731;
    std::cout << 731;
    for (int i = 0; i < 731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_732(int& val) {
    func_in_732(val);
    const int x = 732;
    std::cout << 732;
    for (int i = 0; i < 732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_732(int& val) {
    const int x = 732;
    std::cout << 732;
    for (int i = 0; i < 732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_733(int& val) {
    func_in_733(val);
    const int x = 733;
    std::cout << 733;
    for (int i = 0; i < 733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_733(int& val) {
    const int x = 733;
    std::cout << 733;
    for (int i = 0; i < 733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_734(int& val) {
    func_in_734(val);
    const int x = 734;
    std::cout << 734;
    for (int i = 0; i < 734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_734(int& val) {
    const int x = 734;
    std::cout << 734;
    for (int i = 0; i < 734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_735(int& val) {
    func_in_735(val);
    const int x = 735;
    std::cout << 735;
    for (int i = 0; i < 735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_735(int& val) {
    const int x = 735;
    std::cout << 735;
    for (int i = 0; i < 735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_736(int& val) {
    func_in_736(val);
    const int x = 736;
    std::cout << 736;
    for (int i = 0; i < 736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_736(int& val) {
    const int x = 736;
    std::cout << 736;
    for (int i = 0; i < 736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_737(int& val) {
    func_in_737(val);
    const int x = 737;
    std::cout << 737;
    for (int i = 0; i < 737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_737(int& val) {
    const int x = 737;
    std::cout << 737;
    for (int i = 0; i < 737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_738(int& val) {
    func_in_738(val);
    const int x = 738;
    std::cout << 738;
    for (int i = 0; i < 738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_738(int& val) {
    const int x = 738;
    std::cout << 738;
    for (int i = 0; i < 738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_739(int& val) {
    func_in_739(val);
    const int x = 739;
    std::cout << 739;
    for (int i = 0; i < 739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_739(int& val) {
    const int x = 739;
    std::cout << 739;
    for (int i = 0; i < 739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_740(int& val) {
    func_in_740(val);
    const int x = 740;
    std::cout << 740;
    for (int i = 0; i < 740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_740(int& val) {
    const int x = 740;
    std::cout << 740;
    for (int i = 0; i < 740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_741(int& val) {
    func_in_741(val);
    const int x = 741;
    std::cout << 741;
    for (int i = 0; i < 741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_741(int& val) {
    const int x = 741;
    std::cout << 741;
    for (int i = 0; i < 741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_742(int& val) {
    func_in_742(val);
    const int x = 742;
    std::cout << 742;
    for (int i = 0; i < 742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_742(int& val) {
    const int x = 742;
    std::cout << 742;
    for (int i = 0; i < 742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_743(int& val) {
    func_in_743(val);
    const int x = 743;
    std::cout << 743;
    for (int i = 0; i < 743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_743(int& val) {
    const int x = 743;
    std::cout << 743;
    for (int i = 0; i < 743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_744(int& val) {
    func_in_744(val);
    const int x = 744;
    std::cout << 744;
    for (int i = 0; i < 744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_744(int& val) {
    const int x = 744;
    std::cout << 744;
    for (int i = 0; i < 744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_745(int& val) {
    func_in_745(val);
    const int x = 745;
    std::cout << 745;
    for (int i = 0; i < 745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_745(int& val) {
    const int x = 745;
    std::cout << 745;
    for (int i = 0; i < 745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_746(int& val) {
    func_in_746(val);
    const int x = 746;
    std::cout << 746;
    for (int i = 0; i < 746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_746(int& val) {
    const int x = 746;
    std::cout << 746;
    for (int i = 0; i < 746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_747(int& val) {
    func_in_747(val);
    const int x = 747;
    std::cout << 747;
    for (int i = 0; i < 747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_747(int& val) {
    const int x = 747;
    std::cout << 747;
    for (int i = 0; i < 747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_748(int& val) {
    func_in_748(val);
    const int x = 748;
    std::cout << 748;
    for (int i = 0; i < 748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_748(int& val) {
    const int x = 748;
    std::cout << 748;
    for (int i = 0; i < 748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_749(int& val) {
    func_in_749(val);
    const int x = 749;
    std::cout << 749;
    for (int i = 0; i < 749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_749(int& val) {
    const int x = 749;
    std::cout << 749;
    for (int i = 0; i < 749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_750(int& val) {
    func_in_750(val);
    const int x = 750;
    std::cout << 750;
    for (int i = 0; i < 750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_750(int& val) {
    const int x = 750;
    std::cout << 750;
    for (int i = 0; i < 750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_751(int& val) {
    func_in_751(val);
    const int x = 751;
    std::cout << 751;
    for (int i = 0; i < 751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_751(int& val) {
    const int x = 751;
    std::cout << 751;
    for (int i = 0; i < 751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_752(int& val) {
    func_in_752(val);
    const int x = 752;
    std::cout << 752;
    for (int i = 0; i < 752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_752(int& val) {
    const int x = 752;
    std::cout << 752;
    for (int i = 0; i < 752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_753(int& val) {
    func_in_753(val);
    const int x = 753;
    std::cout << 753;
    for (int i = 0; i < 753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_753(int& val) {
    const int x = 753;
    std::cout << 753;
    for (int i = 0; i < 753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_754(int& val) {
    func_in_754(val);
    const int x = 754;
    std::cout << 754;
    for (int i = 0; i < 754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_754(int& val) {
    const int x = 754;
    std::cout << 754;
    for (int i = 0; i < 754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_755(int& val) {
    func_in_755(val);
    const int x = 755;
    std::cout << 755;
    for (int i = 0; i < 755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_755(int& val) {
    const int x = 755;
    std::cout << 755;
    for (int i = 0; i < 755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_756(int& val) {
    func_in_756(val);
    const int x = 756;
    std::cout << 756;
    for (int i = 0; i < 756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_756(int& val) {
    const int x = 756;
    std::cout << 756;
    for (int i = 0; i < 756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_757(int& val) {
    func_in_757(val);
    const int x = 757;
    std::cout << 757;
    for (int i = 0; i < 757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_757(int& val) {
    const int x = 757;
    std::cout << 757;
    for (int i = 0; i < 757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_758(int& val) {
    func_in_758(val);
    const int x = 758;
    std::cout << 758;
    for (int i = 0; i < 758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_758(int& val) {
    const int x = 758;
    std::cout << 758;
    for (int i = 0; i < 758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_759(int& val) {
    func_in_759(val);
    const int x = 759;
    std::cout << 759;
    for (int i = 0; i < 759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_759(int& val) {
    const int x = 759;
    std::cout << 759;
    for (int i = 0; i < 759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_760(int& val) {
    func_in_760(val);
    const int x = 760;
    std::cout << 760;
    for (int i = 0; i < 760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_760(int& val) {
    const int x = 760;
    std::cout << 760;
    for (int i = 0; i < 760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_761(int& val) {
    func_in_761(val);
    const int x = 761;
    std::cout << 761;
    for (int i = 0; i < 761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_761(int& val) {
    const int x = 761;
    std::cout << 761;
    for (int i = 0; i < 761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_762(int& val) {
    func_in_762(val);
    const int x = 762;
    std::cout << 762;
    for (int i = 0; i < 762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_762(int& val) {
    const int x = 762;
    std::cout << 762;
    for (int i = 0; i < 762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_763(int& val) {
    func_in_763(val);
    const int x = 763;
    std::cout << 763;
    for (int i = 0; i < 763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_763(int& val) {
    const int x = 763;
    std::cout << 763;
    for (int i = 0; i < 763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_764(int& val) {
    func_in_764(val);
    const int x = 764;
    std::cout << 764;
    for (int i = 0; i < 764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_764(int& val) {
    const int x = 764;
    std::cout << 764;
    for (int i = 0; i < 764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_765(int& val) {
    func_in_765(val);
    const int x = 765;
    std::cout << 765;
    for (int i = 0; i < 765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_765(int& val) {
    const int x = 765;
    std::cout << 765;
    for (int i = 0; i < 765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_766(int& val) {
    func_in_766(val);
    const int x = 766;
    std::cout << 766;
    for (int i = 0; i < 766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_766(int& val) {
    const int x = 766;
    std::cout << 766;
    for (int i = 0; i < 766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_767(int& val) {
    func_in_767(val);
    const int x = 767;
    std::cout << 767;
    for (int i = 0; i < 767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_767(int& val) {
    const int x = 767;
    std::cout << 767;
    for (int i = 0; i < 767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_768(int& val) {
    func_in_768(val);
    const int x = 768;
    std::cout << 768;
    for (int i = 0; i < 768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_768(int& val) {
    const int x = 768;
    std::cout << 768;
    for (int i = 0; i < 768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_769(int& val) {
    func_in_769(val);
    const int x = 769;
    std::cout << 769;
    for (int i = 0; i < 769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_769(int& val) {
    const int x = 769;
    std::cout << 769;
    for (int i = 0; i < 769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_770(int& val) {
    func_in_770(val);
    const int x = 770;
    std::cout << 770;
    for (int i = 0; i < 770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_770(int& val) {
    const int x = 770;
    std::cout << 770;
    for (int i = 0; i < 770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_771(int& val) {
    func_in_771(val);
    const int x = 771;
    std::cout << 771;
    for (int i = 0; i < 771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_771(int& val) {
    const int x = 771;
    std::cout << 771;
    for (int i = 0; i < 771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_772(int& val) {
    func_in_772(val);
    const int x = 772;
    std::cout << 772;
    for (int i = 0; i < 772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_772(int& val) {
    const int x = 772;
    std::cout << 772;
    for (int i = 0; i < 772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_773(int& val) {
    func_in_773(val);
    const int x = 773;
    std::cout << 773;
    for (int i = 0; i < 773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_773(int& val) {
    const int x = 773;
    std::cout << 773;
    for (int i = 0; i < 773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_774(int& val) {
    func_in_774(val);
    const int x = 774;
    std::cout << 774;
    for (int i = 0; i < 774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_774(int& val) {
    const int x = 774;
    std::cout << 774;
    for (int i = 0; i < 774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_775(int& val) {
    func_in_775(val);
    const int x = 775;
    std::cout << 775;
    for (int i = 0; i < 775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_775(int& val) {
    const int x = 775;
    std::cout << 775;
    for (int i = 0; i < 775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_776(int& val) {
    func_in_776(val);
    const int x = 776;
    std::cout << 776;
    for (int i = 0; i < 776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_776(int& val) {
    const int x = 776;
    std::cout << 776;
    for (int i = 0; i < 776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_777(int& val) {
    func_in_777(val);
    const int x = 777;
    std::cout << 777;
    for (int i = 0; i < 777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_777(int& val) {
    const int x = 777;
    std::cout << 777;
    for (int i = 0; i < 777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_778(int& val) {
    func_in_778(val);
    const int x = 778;
    std::cout << 778;
    for (int i = 0; i < 778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_778(int& val) {
    const int x = 778;
    std::cout << 778;
    for (int i = 0; i < 778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_779(int& val) {
    func_in_779(val);
    const int x = 779;
    std::cout << 779;
    for (int i = 0; i < 779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_779(int& val) {
    const int x = 779;
    std::cout << 779;
    for (int i = 0; i < 779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_780(int& val) {
    func_in_780(val);
    const int x = 780;
    std::cout << 780;
    for (int i = 0; i < 780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_780(int& val) {
    const int x = 780;
    std::cout << 780;
    for (int i = 0; i < 780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_781(int& val) {
    func_in_781(val);
    const int x = 781;
    std::cout << 781;
    for (int i = 0; i < 781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_781(int& val) {
    const int x = 781;
    std::cout << 781;
    for (int i = 0; i < 781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_782(int& val) {
    func_in_782(val);
    const int x = 782;
    std::cout << 782;
    for (int i = 0; i < 782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_782(int& val) {
    const int x = 782;
    std::cout << 782;
    for (int i = 0; i < 782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_783(int& val) {
    func_in_783(val);
    const int x = 783;
    std::cout << 783;
    for (int i = 0; i < 783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_783(int& val) {
    const int x = 783;
    std::cout << 783;
    for (int i = 0; i < 783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_784(int& val) {
    func_in_784(val);
    const int x = 784;
    std::cout << 784;
    for (int i = 0; i < 784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_784(int& val) {
    const int x = 784;
    std::cout << 784;
    for (int i = 0; i < 784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_785(int& val) {
    func_in_785(val);
    const int x = 785;
    std::cout << 785;
    for (int i = 0; i < 785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_785(int& val) {
    const int x = 785;
    std::cout << 785;
    for (int i = 0; i < 785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_786(int& val) {
    func_in_786(val);
    const int x = 786;
    std::cout << 786;
    for (int i = 0; i < 786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_786(int& val) {
    const int x = 786;
    std::cout << 786;
    for (int i = 0; i < 786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_787(int& val) {
    func_in_787(val);
    const int x = 787;
    std::cout << 787;
    for (int i = 0; i < 787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_787(int& val) {
    const int x = 787;
    std::cout << 787;
    for (int i = 0; i < 787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_788(int& val) {
    func_in_788(val);
    const int x = 788;
    std::cout << 788;
    for (int i = 0; i < 788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_788(int& val) {
    const int x = 788;
    std::cout << 788;
    for (int i = 0; i < 788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_789(int& val) {
    func_in_789(val);
    const int x = 789;
    std::cout << 789;
    for (int i = 0; i < 789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_789(int& val) {
    const int x = 789;
    std::cout << 789;
    for (int i = 0; i < 789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_790(int& val) {
    func_in_790(val);
    const int x = 790;
    std::cout << 790;
    for (int i = 0; i < 790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_790(int& val) {
    const int x = 790;
    std::cout << 790;
    for (int i = 0; i < 790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_791(int& val) {
    func_in_791(val);
    const int x = 791;
    std::cout << 791;
    for (int i = 0; i < 791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_791(int& val) {
    const int x = 791;
    std::cout << 791;
    for (int i = 0; i < 791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_792(int& val) {
    func_in_792(val);
    const int x = 792;
    std::cout << 792;
    for (int i = 0; i < 792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_792(int& val) {
    const int x = 792;
    std::cout << 792;
    for (int i = 0; i < 792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_793(int& val) {
    func_in_793(val);
    const int x = 793;
    std::cout << 793;
    for (int i = 0; i < 793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_793(int& val) {
    const int x = 793;
    std::cout << 793;
    for (int i = 0; i < 793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_794(int& val) {
    func_in_794(val);
    const int x = 794;
    std::cout << 794;
    for (int i = 0; i < 794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_794(int& val) {
    const int x = 794;
    std::cout << 794;
    for (int i = 0; i < 794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_795(int& val) {
    func_in_795(val);
    const int x = 795;
    std::cout << 795;
    for (int i = 0; i < 795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_795(int& val) {
    const int x = 795;
    std::cout << 795;
    for (int i = 0; i < 795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_796(int& val) {
    func_in_796(val);
    const int x = 796;
    std::cout << 796;
    for (int i = 0; i < 796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_796(int& val) {
    const int x = 796;
    std::cout << 796;
    for (int i = 0; i < 796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_797(int& val) {
    func_in_797(val);
    const int x = 797;
    std::cout << 797;
    for (int i = 0; i < 797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_797(int& val) {
    const int x = 797;
    std::cout << 797;
    for (int i = 0; i < 797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_798(int& val) {
    func_in_798(val);
    const int x = 798;
    std::cout << 798;
    for (int i = 0; i < 798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_798(int& val) {
    const int x = 798;
    std::cout << 798;
    for (int i = 0; i < 798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_799(int& val) {
    func_in_799(val);
    const int x = 799;
    std::cout << 799;
    for (int i = 0; i < 799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_799(int& val) {
    const int x = 799;
    std::cout << 799;
    for (int i = 0; i < 799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_800(int& val) {
    func_in_800(val);
    const int x = 800;
    std::cout << 800;
    for (int i = 0; i < 800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_800(int& val) {
    const int x = 800;
    std::cout << 800;
    for (int i = 0; i < 800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_801(int& val) {
    func_in_801(val);
    const int x = 801;
    std::cout << 801;
    for (int i = 0; i < 801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_801(int& val) {
    const int x = 801;
    std::cout << 801;
    for (int i = 0; i < 801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_802(int& val) {
    func_in_802(val);
    const int x = 802;
    std::cout << 802;
    for (int i = 0; i < 802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_802(int& val) {
    const int x = 802;
    std::cout << 802;
    for (int i = 0; i < 802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_803(int& val) {
    func_in_803(val);
    const int x = 803;
    std::cout << 803;
    for (int i = 0; i < 803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_803(int& val) {
    const int x = 803;
    std::cout << 803;
    for (int i = 0; i < 803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_804(int& val) {
    func_in_804(val);
    const int x = 804;
    std::cout << 804;
    for (int i = 0; i < 804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_804(int& val) {
    const int x = 804;
    std::cout << 804;
    for (int i = 0; i < 804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_805(int& val) {
    func_in_805(val);
    const int x = 805;
    std::cout << 805;
    for (int i = 0; i < 805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_805(int& val) {
    const int x = 805;
    std::cout << 805;
    for (int i = 0; i < 805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_806(int& val) {
    func_in_806(val);
    const int x = 806;
    std::cout << 806;
    for (int i = 0; i < 806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_806(int& val) {
    const int x = 806;
    std::cout << 806;
    for (int i = 0; i < 806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_807(int& val) {
    func_in_807(val);
    const int x = 807;
    std::cout << 807;
    for (int i = 0; i < 807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_807(int& val) {
    const int x = 807;
    std::cout << 807;
    for (int i = 0; i < 807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_808(int& val) {
    func_in_808(val);
    const int x = 808;
    std::cout << 808;
    for (int i = 0; i < 808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_808(int& val) {
    const int x = 808;
    std::cout << 808;
    for (int i = 0; i < 808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_809(int& val) {
    func_in_809(val);
    const int x = 809;
    std::cout << 809;
    for (int i = 0; i < 809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_809(int& val) {
    const int x = 809;
    std::cout << 809;
    for (int i = 0; i < 809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_810(int& val) {
    func_in_810(val);
    const int x = 810;
    std::cout << 810;
    for (int i = 0; i < 810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_810(int& val) {
    const int x = 810;
    std::cout << 810;
    for (int i = 0; i < 810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_811(int& val) {
    func_in_811(val);
    const int x = 811;
    std::cout << 811;
    for (int i = 0; i < 811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_811(int& val) {
    const int x = 811;
    std::cout << 811;
    for (int i = 0; i < 811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_812(int& val) {
    func_in_812(val);
    const int x = 812;
    std::cout << 812;
    for (int i = 0; i < 812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_812(int& val) {
    const int x = 812;
    std::cout << 812;
    for (int i = 0; i < 812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_813(int& val) {
    func_in_813(val);
    const int x = 813;
    std::cout << 813;
    for (int i = 0; i < 813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_813(int& val) {
    const int x = 813;
    std::cout << 813;
    for (int i = 0; i < 813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_814(int& val) {
    func_in_814(val);
    const int x = 814;
    std::cout << 814;
    for (int i = 0; i < 814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_814(int& val) {
    const int x = 814;
    std::cout << 814;
    for (int i = 0; i < 814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_815(int& val) {
    func_in_815(val);
    const int x = 815;
    std::cout << 815;
    for (int i = 0; i < 815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_815(int& val) {
    const int x = 815;
    std::cout << 815;
    for (int i = 0; i < 815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_816(int& val) {
    func_in_816(val);
    const int x = 816;
    std::cout << 816;
    for (int i = 0; i < 816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_816(int& val) {
    const int x = 816;
    std::cout << 816;
    for (int i = 0; i < 816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_817(int& val) {
    func_in_817(val);
    const int x = 817;
    std::cout << 817;
    for (int i = 0; i < 817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_817(int& val) {
    const int x = 817;
    std::cout << 817;
    for (int i = 0; i < 817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_818(int& val) {
    func_in_818(val);
    const int x = 818;
    std::cout << 818;
    for (int i = 0; i < 818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_818(int& val) {
    const int x = 818;
    std::cout << 818;
    for (int i = 0; i < 818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_819(int& val) {
    func_in_819(val);
    const int x = 819;
    std::cout << 819;
    for (int i = 0; i < 819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_819(int& val) {
    const int x = 819;
    std::cout << 819;
    for (int i = 0; i < 819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_820(int& val) {
    func_in_820(val);
    const int x = 820;
    std::cout << 820;
    for (int i = 0; i < 820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_820(int& val) {
    const int x = 820;
    std::cout << 820;
    for (int i = 0; i < 820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_821(int& val) {
    func_in_821(val);
    const int x = 821;
    std::cout << 821;
    for (int i = 0; i < 821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_821(int& val) {
    const int x = 821;
    std::cout << 821;
    for (int i = 0; i < 821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_822(int& val) {
    func_in_822(val);
    const int x = 822;
    std::cout << 822;
    for (int i = 0; i < 822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_822(int& val) {
    const int x = 822;
    std::cout << 822;
    for (int i = 0; i < 822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_823(int& val) {
    func_in_823(val);
    const int x = 823;
    std::cout << 823;
    for (int i = 0; i < 823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_823(int& val) {
    const int x = 823;
    std::cout << 823;
    for (int i = 0; i < 823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_824(int& val) {
    func_in_824(val);
    const int x = 824;
    std::cout << 824;
    for (int i = 0; i < 824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_824(int& val) {
    const int x = 824;
    std::cout << 824;
    for (int i = 0; i < 824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_825(int& val) {
    func_in_825(val);
    const int x = 825;
    std::cout << 825;
    for (int i = 0; i < 825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_825(int& val) {
    const int x = 825;
    std::cout << 825;
    for (int i = 0; i < 825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_826(int& val) {
    func_in_826(val);
    const int x = 826;
    std::cout << 826;
    for (int i = 0; i < 826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_826(int& val) {
    const int x = 826;
    std::cout << 826;
    for (int i = 0; i < 826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_827(int& val) {
    func_in_827(val);
    const int x = 827;
    std::cout << 827;
    for (int i = 0; i < 827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_827(int& val) {
    const int x = 827;
    std::cout << 827;
    for (int i = 0; i < 827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_828(int& val) {
    func_in_828(val);
    const int x = 828;
    std::cout << 828;
    for (int i = 0; i < 828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_828(int& val) {
    const int x = 828;
    std::cout << 828;
    for (int i = 0; i < 828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_829(int& val) {
    func_in_829(val);
    const int x = 829;
    std::cout << 829;
    for (int i = 0; i < 829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_829(int& val) {
    const int x = 829;
    std::cout << 829;
    for (int i = 0; i < 829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_830(int& val) {
    func_in_830(val);
    const int x = 830;
    std::cout << 830;
    for (int i = 0; i < 830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_830(int& val) {
    const int x = 830;
    std::cout << 830;
    for (int i = 0; i < 830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_831(int& val) {
    func_in_831(val);
    const int x = 831;
    std::cout << 831;
    for (int i = 0; i < 831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_831(int& val) {
    const int x = 831;
    std::cout << 831;
    for (int i = 0; i < 831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_832(int& val) {
    func_in_832(val);
    const int x = 832;
    std::cout << 832;
    for (int i = 0; i < 832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_832(int& val) {
    const int x = 832;
    std::cout << 832;
    for (int i = 0; i < 832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_833(int& val) {
    func_in_833(val);
    const int x = 833;
    std::cout << 833;
    for (int i = 0; i < 833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_833(int& val) {
    const int x = 833;
    std::cout << 833;
    for (int i = 0; i < 833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_834(int& val) {
    func_in_834(val);
    const int x = 834;
    std::cout << 834;
    for (int i = 0; i < 834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_834(int& val) {
    const int x = 834;
    std::cout << 834;
    for (int i = 0; i < 834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_835(int& val) {
    func_in_835(val);
    const int x = 835;
    std::cout << 835;
    for (int i = 0; i < 835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_835(int& val) {
    const int x = 835;
    std::cout << 835;
    for (int i = 0; i < 835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_836(int& val) {
    func_in_836(val);
    const int x = 836;
    std::cout << 836;
    for (int i = 0; i < 836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_836(int& val) {
    const int x = 836;
    std::cout << 836;
    for (int i = 0; i < 836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_837(int& val) {
    func_in_837(val);
    const int x = 837;
    std::cout << 837;
    for (int i = 0; i < 837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_837(int& val) {
    const int x = 837;
    std::cout << 837;
    for (int i = 0; i < 837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_838(int& val) {
    func_in_838(val);
    const int x = 838;
    std::cout << 838;
    for (int i = 0; i < 838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_838(int& val) {
    const int x = 838;
    std::cout << 838;
    for (int i = 0; i < 838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_839(int& val) {
    func_in_839(val);
    const int x = 839;
    std::cout << 839;
    for (int i = 0; i < 839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_839(int& val) {
    const int x = 839;
    std::cout << 839;
    for (int i = 0; i < 839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_840(int& val) {
    func_in_840(val);
    const int x = 840;
    std::cout << 840;
    for (int i = 0; i < 840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_840(int& val) {
    const int x = 840;
    std::cout << 840;
    for (int i = 0; i < 840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_841(int& val) {
    func_in_841(val);
    const int x = 841;
    std::cout << 841;
    for (int i = 0; i < 841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_841(int& val) {
    const int x = 841;
    std::cout << 841;
    for (int i = 0; i < 841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_842(int& val) {
    func_in_842(val);
    const int x = 842;
    std::cout << 842;
    for (int i = 0; i < 842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_842(int& val) {
    const int x = 842;
    std::cout << 842;
    for (int i = 0; i < 842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_843(int& val) {
    func_in_843(val);
    const int x = 843;
    std::cout << 843;
    for (int i = 0; i < 843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_843(int& val) {
    const int x = 843;
    std::cout << 843;
    for (int i = 0; i < 843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_844(int& val) {
    func_in_844(val);
    const int x = 844;
    std::cout << 844;
    for (int i = 0; i < 844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_844(int& val) {
    const int x = 844;
    std::cout << 844;
    for (int i = 0; i < 844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_845(int& val) {
    func_in_845(val);
    const int x = 845;
    std::cout << 845;
    for (int i = 0; i < 845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_845(int& val) {
    const int x = 845;
    std::cout << 845;
    for (int i = 0; i < 845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_846(int& val) {
    func_in_846(val);
    const int x = 846;
    std::cout << 846;
    for (int i = 0; i < 846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_846(int& val) {
    const int x = 846;
    std::cout << 846;
    for (int i = 0; i < 846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_847(int& val) {
    func_in_847(val);
    const int x = 847;
    std::cout << 847;
    for (int i = 0; i < 847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_847(int& val) {
    const int x = 847;
    std::cout << 847;
    for (int i = 0; i < 847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_848(int& val) {
    func_in_848(val);
    const int x = 848;
    std::cout << 848;
    for (int i = 0; i < 848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_848(int& val) {
    const int x = 848;
    std::cout << 848;
    for (int i = 0; i < 848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_849(int& val) {
    func_in_849(val);
    const int x = 849;
    std::cout << 849;
    for (int i = 0; i < 849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_849(int& val) {
    const int x = 849;
    std::cout << 849;
    for (int i = 0; i < 849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_850(int& val) {
    func_in_850(val);
    const int x = 850;
    std::cout << 850;
    for (int i = 0; i < 850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_850(int& val) {
    const int x = 850;
    std::cout << 850;
    for (int i = 0; i < 850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_851(int& val) {
    func_in_851(val);
    const int x = 851;
    std::cout << 851;
    for (int i = 0; i < 851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_851(int& val) {
    const int x = 851;
    std::cout << 851;
    for (int i = 0; i < 851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_852(int& val) {
    func_in_852(val);
    const int x = 852;
    std::cout << 852;
    for (int i = 0; i < 852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_852(int& val) {
    const int x = 852;
    std::cout << 852;
    for (int i = 0; i < 852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_853(int& val) {
    func_in_853(val);
    const int x = 853;
    std::cout << 853;
    for (int i = 0; i < 853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_853(int& val) {
    const int x = 853;
    std::cout << 853;
    for (int i = 0; i < 853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_854(int& val) {
    func_in_854(val);
    const int x = 854;
    std::cout << 854;
    for (int i = 0; i < 854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_854(int& val) {
    const int x = 854;
    std::cout << 854;
    for (int i = 0; i < 854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_855(int& val) {
    func_in_855(val);
    const int x = 855;
    std::cout << 855;
    for (int i = 0; i < 855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_855(int& val) {
    const int x = 855;
    std::cout << 855;
    for (int i = 0; i < 855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_856(int& val) {
    func_in_856(val);
    const int x = 856;
    std::cout << 856;
    for (int i = 0; i < 856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_856(int& val) {
    const int x = 856;
    std::cout << 856;
    for (int i = 0; i < 856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_857(int& val) {
    func_in_857(val);
    const int x = 857;
    std::cout << 857;
    for (int i = 0; i < 857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_857(int& val) {
    const int x = 857;
    std::cout << 857;
    for (int i = 0; i < 857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_858(int& val) {
    func_in_858(val);
    const int x = 858;
    std::cout << 858;
    for (int i = 0; i < 858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_858(int& val) {
    const int x = 858;
    std::cout << 858;
    for (int i = 0; i < 858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_859(int& val) {
    func_in_859(val);
    const int x = 859;
    std::cout << 859;
    for (int i = 0; i < 859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_859(int& val) {
    const int x = 859;
    std::cout << 859;
    for (int i = 0; i < 859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_860(int& val) {
    func_in_860(val);
    const int x = 860;
    std::cout << 860;
    for (int i = 0; i < 860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_860(int& val) {
    const int x = 860;
    std::cout << 860;
    for (int i = 0; i < 860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_861(int& val) {
    func_in_861(val);
    const int x = 861;
    std::cout << 861;
    for (int i = 0; i < 861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_861(int& val) {
    const int x = 861;
    std::cout << 861;
    for (int i = 0; i < 861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_862(int& val) {
    func_in_862(val);
    const int x = 862;
    std::cout << 862;
    for (int i = 0; i < 862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_862(int& val) {
    const int x = 862;
    std::cout << 862;
    for (int i = 0; i < 862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_863(int& val) {
    func_in_863(val);
    const int x = 863;
    std::cout << 863;
    for (int i = 0; i < 863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_863(int& val) {
    const int x = 863;
    std::cout << 863;
    for (int i = 0; i < 863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_864(int& val) {
    func_in_864(val);
    const int x = 864;
    std::cout << 864;
    for (int i = 0; i < 864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_864(int& val) {
    const int x = 864;
    std::cout << 864;
    for (int i = 0; i < 864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_865(int& val) {
    func_in_865(val);
    const int x = 865;
    std::cout << 865;
    for (int i = 0; i < 865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_865(int& val) {
    const int x = 865;
    std::cout << 865;
    for (int i = 0; i < 865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_866(int& val) {
    func_in_866(val);
    const int x = 866;
    std::cout << 866;
    for (int i = 0; i < 866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_866(int& val) {
    const int x = 866;
    std::cout << 866;
    for (int i = 0; i < 866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_867(int& val) {
    func_in_867(val);
    const int x = 867;
    std::cout << 867;
    for (int i = 0; i < 867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_867(int& val) {
    const int x = 867;
    std::cout << 867;
    for (int i = 0; i < 867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_868(int& val) {
    func_in_868(val);
    const int x = 868;
    std::cout << 868;
    for (int i = 0; i < 868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_868(int& val) {
    const int x = 868;
    std::cout << 868;
    for (int i = 0; i < 868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_869(int& val) {
    func_in_869(val);
    const int x = 869;
    std::cout << 869;
    for (int i = 0; i < 869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_869(int& val) {
    const int x = 869;
    std::cout << 869;
    for (int i = 0; i < 869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_870(int& val) {
    func_in_870(val);
    const int x = 870;
    std::cout << 870;
    for (int i = 0; i < 870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_870(int& val) {
    const int x = 870;
    std::cout << 870;
    for (int i = 0; i < 870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_871(int& val) {
    func_in_871(val);
    const int x = 871;
    std::cout << 871;
    for (int i = 0; i < 871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_871(int& val) {
    const int x = 871;
    std::cout << 871;
    for (int i = 0; i < 871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_872(int& val) {
    func_in_872(val);
    const int x = 872;
    std::cout << 872;
    for (int i = 0; i < 872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_872(int& val) {
    const int x = 872;
    std::cout << 872;
    for (int i = 0; i < 872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_873(int& val) {
    func_in_873(val);
    const int x = 873;
    std::cout << 873;
    for (int i = 0; i < 873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_873(int& val) {
    const int x = 873;
    std::cout << 873;
    for (int i = 0; i < 873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_874(int& val) {
    func_in_874(val);
    const int x = 874;
    std::cout << 874;
    for (int i = 0; i < 874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_874(int& val) {
    const int x = 874;
    std::cout << 874;
    for (int i = 0; i < 874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_875(int& val) {
    func_in_875(val);
    const int x = 875;
    std::cout << 875;
    for (int i = 0; i < 875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_875(int& val) {
    const int x = 875;
    std::cout << 875;
    for (int i = 0; i < 875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_876(int& val) {
    func_in_876(val);
    const int x = 876;
    std::cout << 876;
    for (int i = 0; i < 876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_876(int& val) {
    const int x = 876;
    std::cout << 876;
    for (int i = 0; i < 876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_877(int& val) {
    func_in_877(val);
    const int x = 877;
    std::cout << 877;
    for (int i = 0; i < 877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_877(int& val) {
    const int x = 877;
    std::cout << 877;
    for (int i = 0; i < 877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_878(int& val) {
    func_in_878(val);
    const int x = 878;
    std::cout << 878;
    for (int i = 0; i < 878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_878(int& val) {
    const int x = 878;
    std::cout << 878;
    for (int i = 0; i < 878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_879(int& val) {
    func_in_879(val);
    const int x = 879;
    std::cout << 879;
    for (int i = 0; i < 879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_879(int& val) {
    const int x = 879;
    std::cout << 879;
    for (int i = 0; i < 879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_880(int& val) {
    func_in_880(val);
    const int x = 880;
    std::cout << 880;
    for (int i = 0; i < 880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_880(int& val) {
    const int x = 880;
    std::cout << 880;
    for (int i = 0; i < 880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_881(int& val) {
    func_in_881(val);
    const int x = 881;
    std::cout << 881;
    for (int i = 0; i < 881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_881(int& val) {
    const int x = 881;
    std::cout << 881;
    for (int i = 0; i < 881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_882(int& val) {
    func_in_882(val);
    const int x = 882;
    std::cout << 882;
    for (int i = 0; i < 882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_882(int& val) {
    const int x = 882;
    std::cout << 882;
    for (int i = 0; i < 882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_883(int& val) {
    func_in_883(val);
    const int x = 883;
    std::cout << 883;
    for (int i = 0; i < 883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_883(int& val) {
    const int x = 883;
    std::cout << 883;
    for (int i = 0; i < 883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_884(int& val) {
    func_in_884(val);
    const int x = 884;
    std::cout << 884;
    for (int i = 0; i < 884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_884(int& val) {
    const int x = 884;
    std::cout << 884;
    for (int i = 0; i < 884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_885(int& val) {
    func_in_885(val);
    const int x = 885;
    std::cout << 885;
    for (int i = 0; i < 885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_885(int& val) {
    const int x = 885;
    std::cout << 885;
    for (int i = 0; i < 885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_886(int& val) {
    func_in_886(val);
    const int x = 886;
    std::cout << 886;
    for (int i = 0; i < 886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_886(int& val) {
    const int x = 886;
    std::cout << 886;
    for (int i = 0; i < 886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_887(int& val) {
    func_in_887(val);
    const int x = 887;
    std::cout << 887;
    for (int i = 0; i < 887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_887(int& val) {
    const int x = 887;
    std::cout << 887;
    for (int i = 0; i < 887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_888(int& val) {
    func_in_888(val);
    const int x = 888;
    std::cout << 888;
    for (int i = 0; i < 888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_888(int& val) {
    const int x = 888;
    std::cout << 888;
    for (int i = 0; i < 888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_889(int& val) {
    func_in_889(val);
    const int x = 889;
    std::cout << 889;
    for (int i = 0; i < 889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_889(int& val) {
    const int x = 889;
    std::cout << 889;
    for (int i = 0; i < 889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_890(int& val) {
    func_in_890(val);
    const int x = 890;
    std::cout << 890;
    for (int i = 0; i < 890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_890(int& val) {
    const int x = 890;
    std::cout << 890;
    for (int i = 0; i < 890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_891(int& val) {
    func_in_891(val);
    const int x = 891;
    std::cout << 891;
    for (int i = 0; i < 891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_891(int& val) {
    const int x = 891;
    std::cout << 891;
    for (int i = 0; i < 891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_892(int& val) {
    func_in_892(val);
    const int x = 892;
    std::cout << 892;
    for (int i = 0; i < 892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_892(int& val) {
    const int x = 892;
    std::cout << 892;
    for (int i = 0; i < 892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_893(int& val) {
    func_in_893(val);
    const int x = 893;
    std::cout << 893;
    for (int i = 0; i < 893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_893(int& val) {
    const int x = 893;
    std::cout << 893;
    for (int i = 0; i < 893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_894(int& val) {
    func_in_894(val);
    const int x = 894;
    std::cout << 894;
    for (int i = 0; i < 894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_894(int& val) {
    const int x = 894;
    std::cout << 894;
    for (int i = 0; i < 894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_895(int& val) {
    func_in_895(val);
    const int x = 895;
    std::cout << 895;
    for (int i = 0; i < 895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_895(int& val) {
    const int x = 895;
    std::cout << 895;
    for (int i = 0; i < 895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_896(int& val) {
    func_in_896(val);
    const int x = 896;
    std::cout << 896;
    for (int i = 0; i < 896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_896(int& val) {
    const int x = 896;
    std::cout << 896;
    for (int i = 0; i < 896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_897(int& val) {
    func_in_897(val);
    const int x = 897;
    std::cout << 897;
    for (int i = 0; i < 897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_897(int& val) {
    const int x = 897;
    std::cout << 897;
    for (int i = 0; i < 897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_898(int& val) {
    func_in_898(val);
    const int x = 898;
    std::cout << 898;
    for (int i = 0; i < 898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_898(int& val) {
    const int x = 898;
    std::cout << 898;
    for (int i = 0; i < 898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_899(int& val) {
    func_in_899(val);
    const int x = 899;
    std::cout << 899;
    for (int i = 0; i < 899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_899(int& val) {
    const int x = 899;
    std::cout << 899;
    for (int i = 0; i < 899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_900(int& val) {
    func_in_900(val);
    const int x = 900;
    std::cout << 900;
    for (int i = 0; i < 900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_900(int& val) {
    const int x = 900;
    std::cout << 900;
    for (int i = 0; i < 900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_901(int& val) {
    func_in_901(val);
    const int x = 901;
    std::cout << 901;
    for (int i = 0; i < 901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_901(int& val) {
    const int x = 901;
    std::cout << 901;
    for (int i = 0; i < 901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_902(int& val) {
    func_in_902(val);
    const int x = 902;
    std::cout << 902;
    for (int i = 0; i < 902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_902(int& val) {
    const int x = 902;
    std::cout << 902;
    for (int i = 0; i < 902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_903(int& val) {
    func_in_903(val);
    const int x = 903;
    std::cout << 903;
    for (int i = 0; i < 903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_903(int& val) {
    const int x = 903;
    std::cout << 903;
    for (int i = 0; i < 903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_904(int& val) {
    func_in_904(val);
    const int x = 904;
    std::cout << 904;
    for (int i = 0; i < 904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_904(int& val) {
    const int x = 904;
    std::cout << 904;
    for (int i = 0; i < 904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_905(int& val) {
    func_in_905(val);
    const int x = 905;
    std::cout << 905;
    for (int i = 0; i < 905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_905(int& val) {
    const int x = 905;
    std::cout << 905;
    for (int i = 0; i < 905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_906(int& val) {
    func_in_906(val);
    const int x = 906;
    std::cout << 906;
    for (int i = 0; i < 906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_906(int& val) {
    const int x = 906;
    std::cout << 906;
    for (int i = 0; i < 906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_907(int& val) {
    func_in_907(val);
    const int x = 907;
    std::cout << 907;
    for (int i = 0; i < 907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_907(int& val) {
    const int x = 907;
    std::cout << 907;
    for (int i = 0; i < 907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_908(int& val) {
    func_in_908(val);
    const int x = 908;
    std::cout << 908;
    for (int i = 0; i < 908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_908(int& val) {
    const int x = 908;
    std::cout << 908;
    for (int i = 0; i < 908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_909(int& val) {
    func_in_909(val);
    const int x = 909;
    std::cout << 909;
    for (int i = 0; i < 909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_909(int& val) {
    const int x = 909;
    std::cout << 909;
    for (int i = 0; i < 909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_910(int& val) {
    func_in_910(val);
    const int x = 910;
    std::cout << 910;
    for (int i = 0; i < 910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_910(int& val) {
    const int x = 910;
    std::cout << 910;
    for (int i = 0; i < 910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_911(int& val) {
    func_in_911(val);
    const int x = 911;
    std::cout << 911;
    for (int i = 0; i < 911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_911(int& val) {
    const int x = 911;
    std::cout << 911;
    for (int i = 0; i < 911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_912(int& val) {
    func_in_912(val);
    const int x = 912;
    std::cout << 912;
    for (int i = 0; i < 912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_912(int& val) {
    const int x = 912;
    std::cout << 912;
    for (int i = 0; i < 912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_913(int& val) {
    func_in_913(val);
    const int x = 913;
    std::cout << 913;
    for (int i = 0; i < 913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_913(int& val) {
    const int x = 913;
    std::cout << 913;
    for (int i = 0; i < 913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_914(int& val) {
    func_in_914(val);
    const int x = 914;
    std::cout << 914;
    for (int i = 0; i < 914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_914(int& val) {
    const int x = 914;
    std::cout << 914;
    for (int i = 0; i < 914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_915(int& val) {
    func_in_915(val);
    const int x = 915;
    std::cout << 915;
    for (int i = 0; i < 915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_915(int& val) {
    const int x = 915;
    std::cout << 915;
    for (int i = 0; i < 915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_916(int& val) {
    func_in_916(val);
    const int x = 916;
    std::cout << 916;
    for (int i = 0; i < 916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_916(int& val) {
    const int x = 916;
    std::cout << 916;
    for (int i = 0; i < 916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_917(int& val) {
    func_in_917(val);
    const int x = 917;
    std::cout << 917;
    for (int i = 0; i < 917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_917(int& val) {
    const int x = 917;
    std::cout << 917;
    for (int i = 0; i < 917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_918(int& val) {
    func_in_918(val);
    const int x = 918;
    std::cout << 918;
    for (int i = 0; i < 918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_918(int& val) {
    const int x = 918;
    std::cout << 918;
    for (int i = 0; i < 918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_919(int& val) {
    func_in_919(val);
    const int x = 919;
    std::cout << 919;
    for (int i = 0; i < 919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_919(int& val) {
    const int x = 919;
    std::cout << 919;
    for (int i = 0; i < 919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_920(int& val) {
    func_in_920(val);
    const int x = 920;
    std::cout << 920;
    for (int i = 0; i < 920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_920(int& val) {
    const int x = 920;
    std::cout << 920;
    for (int i = 0; i < 920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_921(int& val) {
    func_in_921(val);
    const int x = 921;
    std::cout << 921;
    for (int i = 0; i < 921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_921(int& val) {
    const int x = 921;
    std::cout << 921;
    for (int i = 0; i < 921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_922(int& val) {
    func_in_922(val);
    const int x = 922;
    std::cout << 922;
    for (int i = 0; i < 922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_922(int& val) {
    const int x = 922;
    std::cout << 922;
    for (int i = 0; i < 922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_923(int& val) {
    func_in_923(val);
    const int x = 923;
    std::cout << 923;
    for (int i = 0; i < 923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_923(int& val) {
    const int x = 923;
    std::cout << 923;
    for (int i = 0; i < 923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_924(int& val) {
    func_in_924(val);
    const int x = 924;
    std::cout << 924;
    for (int i = 0; i < 924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_924(int& val) {
    const int x = 924;
    std::cout << 924;
    for (int i = 0; i < 924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_925(int& val) {
    func_in_925(val);
    const int x = 925;
    std::cout << 925;
    for (int i = 0; i < 925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_925(int& val) {
    const int x = 925;
    std::cout << 925;
    for (int i = 0; i < 925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_926(int& val) {
    func_in_926(val);
    const int x = 926;
    std::cout << 926;
    for (int i = 0; i < 926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_926(int& val) {
    const int x = 926;
    std::cout << 926;
    for (int i = 0; i < 926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_927(int& val) {
    func_in_927(val);
    const int x = 927;
    std::cout << 927;
    for (int i = 0; i < 927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_927(int& val) {
    const int x = 927;
    std::cout << 927;
    for (int i = 0; i < 927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_928(int& val) {
    func_in_928(val);
    const int x = 928;
    std::cout << 928;
    for (int i = 0; i < 928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_928(int& val) {
    const int x = 928;
    std::cout << 928;
    for (int i = 0; i < 928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_929(int& val) {
    func_in_929(val);
    const int x = 929;
    std::cout << 929;
    for (int i = 0; i < 929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_929(int& val) {
    const int x = 929;
    std::cout << 929;
    for (int i = 0; i < 929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_930(int& val) {
    func_in_930(val);
    const int x = 930;
    std::cout << 930;
    for (int i = 0; i < 930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_930(int& val) {
    const int x = 930;
    std::cout << 930;
    for (int i = 0; i < 930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_931(int& val) {
    func_in_931(val);
    const int x = 931;
    std::cout << 931;
    for (int i = 0; i < 931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_931(int& val) {
    const int x = 931;
    std::cout << 931;
    for (int i = 0; i < 931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_932(int& val) {
    func_in_932(val);
    const int x = 932;
    std::cout << 932;
    for (int i = 0; i < 932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_932(int& val) {
    const int x = 932;
    std::cout << 932;
    for (int i = 0; i < 932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_933(int& val) {
    func_in_933(val);
    const int x = 933;
    std::cout << 933;
    for (int i = 0; i < 933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_933(int& val) {
    const int x = 933;
    std::cout << 933;
    for (int i = 0; i < 933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_934(int& val) {
    func_in_934(val);
    const int x = 934;
    std::cout << 934;
    for (int i = 0; i < 934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_934(int& val) {
    const int x = 934;
    std::cout << 934;
    for (int i = 0; i < 934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_935(int& val) {
    func_in_935(val);
    const int x = 935;
    std::cout << 935;
    for (int i = 0; i < 935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_935(int& val) {
    const int x = 935;
    std::cout << 935;
    for (int i = 0; i < 935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_936(int& val) {
    func_in_936(val);
    const int x = 936;
    std::cout << 936;
    for (int i = 0; i < 936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_936(int& val) {
    const int x = 936;
    std::cout << 936;
    for (int i = 0; i < 936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_937(int& val) {
    func_in_937(val);
    const int x = 937;
    std::cout << 937;
    for (int i = 0; i < 937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_937(int& val) {
    const int x = 937;
    std::cout << 937;
    for (int i = 0; i < 937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_938(int& val) {
    func_in_938(val);
    const int x = 938;
    std::cout << 938;
    for (int i = 0; i < 938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_938(int& val) {
    const int x = 938;
    std::cout << 938;
    for (int i = 0; i < 938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_939(int& val) {
    func_in_939(val);
    const int x = 939;
    std::cout << 939;
    for (int i = 0; i < 939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_939(int& val) {
    const int x = 939;
    std::cout << 939;
    for (int i = 0; i < 939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_940(int& val) {
    func_in_940(val);
    const int x = 940;
    std::cout << 940;
    for (int i = 0; i < 940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_940(int& val) {
    const int x = 940;
    std::cout << 940;
    for (int i = 0; i < 940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_941(int& val) {
    func_in_941(val);
    const int x = 941;
    std::cout << 941;
    for (int i = 0; i < 941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_941(int& val) {
    const int x = 941;
    std::cout << 941;
    for (int i = 0; i < 941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_942(int& val) {
    func_in_942(val);
    const int x = 942;
    std::cout << 942;
    for (int i = 0; i < 942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_942(int& val) {
    const int x = 942;
    std::cout << 942;
    for (int i = 0; i < 942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_943(int& val) {
    func_in_943(val);
    const int x = 943;
    std::cout << 943;
    for (int i = 0; i < 943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_943(int& val) {
    const int x = 943;
    std::cout << 943;
    for (int i = 0; i < 943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_944(int& val) {
    func_in_944(val);
    const int x = 944;
    std::cout << 944;
    for (int i = 0; i < 944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_944(int& val) {
    const int x = 944;
    std::cout << 944;
    for (int i = 0; i < 944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_945(int& val) {
    func_in_945(val);
    const int x = 945;
    std::cout << 945;
    for (int i = 0; i < 945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_945(int& val) {
    const int x = 945;
    std::cout << 945;
    for (int i = 0; i < 945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_946(int& val) {
    func_in_946(val);
    const int x = 946;
    std::cout << 946;
    for (int i = 0; i < 946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_946(int& val) {
    const int x = 946;
    std::cout << 946;
    for (int i = 0; i < 946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_947(int& val) {
    func_in_947(val);
    const int x = 947;
    std::cout << 947;
    for (int i = 0; i < 947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_947(int& val) {
    const int x = 947;
    std::cout << 947;
    for (int i = 0; i < 947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_948(int& val) {
    func_in_948(val);
    const int x = 948;
    std::cout << 948;
    for (int i = 0; i < 948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_948(int& val) {
    const int x = 948;
    std::cout << 948;
    for (int i = 0; i < 948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_949(int& val) {
    func_in_949(val);
    const int x = 949;
    std::cout << 949;
    for (int i = 0; i < 949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_949(int& val) {
    const int x = 949;
    std::cout << 949;
    for (int i = 0; i < 949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_950(int& val) {
    func_in_950(val);
    const int x = 950;
    std::cout << 950;
    for (int i = 0; i < 950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_950(int& val) {
    const int x = 950;
    std::cout << 950;
    for (int i = 0; i < 950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_951(int& val) {
    func_in_951(val);
    const int x = 951;
    std::cout << 951;
    for (int i = 0; i < 951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_951(int& val) {
    const int x = 951;
    std::cout << 951;
    for (int i = 0; i < 951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_952(int& val) {
    func_in_952(val);
    const int x = 952;
    std::cout << 952;
    for (int i = 0; i < 952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_952(int& val) {
    const int x = 952;
    std::cout << 952;
    for (int i = 0; i < 952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_953(int& val) {
    func_in_953(val);
    const int x = 953;
    std::cout << 953;
    for (int i = 0; i < 953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_953(int& val) {
    const int x = 953;
    std::cout << 953;
    for (int i = 0; i < 953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_954(int& val) {
    func_in_954(val);
    const int x = 954;
    std::cout << 954;
    for (int i = 0; i < 954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_954(int& val) {
    const int x = 954;
    std::cout << 954;
    for (int i = 0; i < 954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_955(int& val) {
    func_in_955(val);
    const int x = 955;
    std::cout << 955;
    for (int i = 0; i < 955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_955(int& val) {
    const int x = 955;
    std::cout << 955;
    for (int i = 0; i < 955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_956(int& val) {
    func_in_956(val);
    const int x = 956;
    std::cout << 956;
    for (int i = 0; i < 956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_956(int& val) {
    const int x = 956;
    std::cout << 956;
    for (int i = 0; i < 956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_957(int& val) {
    func_in_957(val);
    const int x = 957;
    std::cout << 957;
    for (int i = 0; i < 957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_957(int& val) {
    const int x = 957;
    std::cout << 957;
    for (int i = 0; i < 957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_958(int& val) {
    func_in_958(val);
    const int x = 958;
    std::cout << 958;
    for (int i = 0; i < 958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_958(int& val) {
    const int x = 958;
    std::cout << 958;
    for (int i = 0; i < 958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_959(int& val) {
    func_in_959(val);
    const int x = 959;
    std::cout << 959;
    for (int i = 0; i < 959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_959(int& val) {
    const int x = 959;
    std::cout << 959;
    for (int i = 0; i < 959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_960(int& val) {
    func_in_960(val);
    const int x = 960;
    std::cout << 960;
    for (int i = 0; i < 960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_960(int& val) {
    const int x = 960;
    std::cout << 960;
    for (int i = 0; i < 960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_961(int& val) {
    func_in_961(val);
    const int x = 961;
    std::cout << 961;
    for (int i = 0; i < 961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_961(int& val) {
    const int x = 961;
    std::cout << 961;
    for (int i = 0; i < 961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_962(int& val) {
    func_in_962(val);
    const int x = 962;
    std::cout << 962;
    for (int i = 0; i < 962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_962(int& val) {
    const int x = 962;
    std::cout << 962;
    for (int i = 0; i < 962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_963(int& val) {
    func_in_963(val);
    const int x = 963;
    std::cout << 963;
    for (int i = 0; i < 963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_963(int& val) {
    const int x = 963;
    std::cout << 963;
    for (int i = 0; i < 963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_964(int& val) {
    func_in_964(val);
    const int x = 964;
    std::cout << 964;
    for (int i = 0; i < 964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_964(int& val) {
    const int x = 964;
    std::cout << 964;
    for (int i = 0; i < 964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_965(int& val) {
    func_in_965(val);
    const int x = 965;
    std::cout << 965;
    for (int i = 0; i < 965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_965(int& val) {
    const int x = 965;
    std::cout << 965;
    for (int i = 0; i < 965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_966(int& val) {
    func_in_966(val);
    const int x = 966;
    std::cout << 966;
    for (int i = 0; i < 966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_966(int& val) {
    const int x = 966;
    std::cout << 966;
    for (int i = 0; i < 966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_967(int& val) {
    func_in_967(val);
    const int x = 967;
    std::cout << 967;
    for (int i = 0; i < 967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_967(int& val) {
    const int x = 967;
    std::cout << 967;
    for (int i = 0; i < 967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_968(int& val) {
    func_in_968(val);
    const int x = 968;
    std::cout << 968;
    for (int i = 0; i < 968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_968(int& val) {
    const int x = 968;
    std::cout << 968;
    for (int i = 0; i < 968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_969(int& val) {
    func_in_969(val);
    const int x = 969;
    std::cout << 969;
    for (int i = 0; i < 969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_969(int& val) {
    const int x = 969;
    std::cout << 969;
    for (int i = 0; i < 969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_970(int& val) {
    func_in_970(val);
    const int x = 970;
    std::cout << 970;
    for (int i = 0; i < 970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_970(int& val) {
    const int x = 970;
    std::cout << 970;
    for (int i = 0; i < 970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_971(int& val) {
    func_in_971(val);
    const int x = 971;
    std::cout << 971;
    for (int i = 0; i < 971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_971(int& val) {
    const int x = 971;
    std::cout << 971;
    for (int i = 0; i < 971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_972(int& val) {
    func_in_972(val);
    const int x = 972;
    std::cout << 972;
    for (int i = 0; i < 972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_972(int& val) {
    const int x = 972;
    std::cout << 972;
    for (int i = 0; i < 972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_973(int& val) {
    func_in_973(val);
    const int x = 973;
    std::cout << 973;
    for (int i = 0; i < 973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_973(int& val) {
    const int x = 973;
    std::cout << 973;
    for (int i = 0; i < 973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_974(int& val) {
    func_in_974(val);
    const int x = 974;
    std::cout << 974;
    for (int i = 0; i < 974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_974(int& val) {
    const int x = 974;
    std::cout << 974;
    for (int i = 0; i < 974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_975(int& val) {
    func_in_975(val);
    const int x = 975;
    std::cout << 975;
    for (int i = 0; i < 975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_975(int& val) {
    const int x = 975;
    std::cout << 975;
    for (int i = 0; i < 975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_976(int& val) {
    func_in_976(val);
    const int x = 976;
    std::cout << 976;
    for (int i = 0; i < 976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_976(int& val) {
    const int x = 976;
    std::cout << 976;
    for (int i = 0; i < 976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_977(int& val) {
    func_in_977(val);
    const int x = 977;
    std::cout << 977;
    for (int i = 0; i < 977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_977(int& val) {
    const int x = 977;
    std::cout << 977;
    for (int i = 0; i < 977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_978(int& val) {
    func_in_978(val);
    const int x = 978;
    std::cout << 978;
    for (int i = 0; i < 978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_978(int& val) {
    const int x = 978;
    std::cout << 978;
    for (int i = 0; i < 978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_979(int& val) {
    func_in_979(val);
    const int x = 979;
    std::cout << 979;
    for (int i = 0; i < 979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_979(int& val) {
    const int x = 979;
    std::cout << 979;
    for (int i = 0; i < 979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_980(int& val) {
    func_in_980(val);
    const int x = 980;
    std::cout << 980;
    for (int i = 0; i < 980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_980(int& val) {
    const int x = 980;
    std::cout << 980;
    for (int i = 0; i < 980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_981(int& val) {
    func_in_981(val);
    const int x = 981;
    std::cout << 981;
    for (int i = 0; i < 981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_981(int& val) {
    const int x = 981;
    std::cout << 981;
    for (int i = 0; i < 981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_982(int& val) {
    func_in_982(val);
    const int x = 982;
    std::cout << 982;
    for (int i = 0; i < 982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_982(int& val) {
    const int x = 982;
    std::cout << 982;
    for (int i = 0; i < 982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_983(int& val) {
    func_in_983(val);
    const int x = 983;
    std::cout << 983;
    for (int i = 0; i < 983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_983(int& val) {
    const int x = 983;
    std::cout << 983;
    for (int i = 0; i < 983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_984(int& val) {
    func_in_984(val);
    const int x = 984;
    std::cout << 984;
    for (int i = 0; i < 984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_984(int& val) {
    const int x = 984;
    std::cout << 984;
    for (int i = 0; i < 984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_985(int& val) {
    func_in_985(val);
    const int x = 985;
    std::cout << 985;
    for (int i = 0; i < 985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_985(int& val) {
    const int x = 985;
    std::cout << 985;
    for (int i = 0; i < 985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_986(int& val) {
    func_in_986(val);
    const int x = 986;
    std::cout << 986;
    for (int i = 0; i < 986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_986(int& val) {
    const int x = 986;
    std::cout << 986;
    for (int i = 0; i < 986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_987(int& val) {
    func_in_987(val);
    const int x = 987;
    std::cout << 987;
    for (int i = 0; i < 987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_987(int& val) {
    const int x = 987;
    std::cout << 987;
    for (int i = 0; i < 987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_988(int& val) {
    func_in_988(val);
    const int x = 988;
    std::cout << 988;
    for (int i = 0; i < 988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_988(int& val) {
    const int x = 988;
    std::cout << 988;
    for (int i = 0; i < 988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_989(int& val) {
    func_in_989(val);
    const int x = 989;
    std::cout << 989;
    for (int i = 0; i < 989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_989(int& val) {
    const int x = 989;
    std::cout << 989;
    for (int i = 0; i < 989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_990(int& val) {
    func_in_990(val);
    const int x = 990;
    std::cout << 990;
    for (int i = 0; i < 990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_990(int& val) {
    const int x = 990;
    std::cout << 990;
    for (int i = 0; i < 990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_991(int& val) {
    func_in_991(val);
    const int x = 991;
    std::cout << 991;
    for (int i = 0; i < 991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_991(int& val) {
    const int x = 991;
    std::cout << 991;
    for (int i = 0; i < 991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_992(int& val) {
    func_in_992(val);
    const int x = 992;
    std::cout << 992;
    for (int i = 0; i < 992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_992(int& val) {
    const int x = 992;
    std::cout << 992;
    for (int i = 0; i < 992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_993(int& val) {
    func_in_993(val);
    const int x = 993;
    std::cout << 993;
    for (int i = 0; i < 993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_993(int& val) {
    const int x = 993;
    std::cout << 993;
    for (int i = 0; i < 993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_994(int& val) {
    func_in_994(val);
    const int x = 994;
    std::cout << 994;
    for (int i = 0; i < 994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_994(int& val) {
    const int x = 994;
    std::cout << 994;
    for (int i = 0; i < 994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_995(int& val) {
    func_in_995(val);
    const int x = 995;
    std::cout << 995;
    for (int i = 0; i < 995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_995(int& val) {
    const int x = 995;
    std::cout << 995;
    for (int i = 0; i < 995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_996(int& val) {
    func_in_996(val);
    const int x = 996;
    std::cout << 996;
    for (int i = 0; i < 996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_996(int& val) {
    const int x = 996;
    std::cout << 996;
    for (int i = 0; i < 996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_997(int& val) {
    func_in_997(val);
    const int x = 997;
    std::cout << 997;
    for (int i = 0; i < 997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_997(int& val) {
    const int x = 997;
    std::cout << 997;
    for (int i = 0; i < 997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_998(int& val) {
    func_in_998(val);
    const int x = 998;
    std::cout << 998;
    for (int i = 0; i < 998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_998(int& val) {
    const int x = 998;
    std::cout << 998;
    for (int i = 0; i < 998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_999(int& val) {
    func_in_999(val);
    const int x = 999;
    std::cout << 999;
    for (int i = 0; i < 999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_999(int& val) {
    const int x = 999;
    std::cout << 999;
    for (int i = 0; i < 999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1000(int& val) {
    func_in_1000(val);
    const int x = 1000;
    std::cout << 1000;
    for (int i = 0; i < 1000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1000(int& val) {
    const int x = 1000;
    std::cout << 1000;
    for (int i = 0; i < 1000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1001(int& val) {
    func_in_1001(val);
    const int x = 1001;
    std::cout << 1001;
    for (int i = 0; i < 1001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1001(int& val) {
    const int x = 1001;
    std::cout << 1001;
    for (int i = 0; i < 1001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1002(int& val) {
    func_in_1002(val);
    const int x = 1002;
    std::cout << 1002;
    for (int i = 0; i < 1002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1002(int& val) {
    const int x = 1002;
    std::cout << 1002;
    for (int i = 0; i < 1002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1003(int& val) {
    func_in_1003(val);
    const int x = 1003;
    std::cout << 1003;
    for (int i = 0; i < 1003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1003(int& val) {
    const int x = 1003;
    std::cout << 1003;
    for (int i = 0; i < 1003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1004(int& val) {
    func_in_1004(val);
    const int x = 1004;
    std::cout << 1004;
    for (int i = 0; i < 1004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1004(int& val) {
    const int x = 1004;
    std::cout << 1004;
    for (int i = 0; i < 1004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1005(int& val) {
    func_in_1005(val);
    const int x = 1005;
    std::cout << 1005;
    for (int i = 0; i < 1005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1005(int& val) {
    const int x = 1005;
    std::cout << 1005;
    for (int i = 0; i < 1005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1006(int& val) {
    func_in_1006(val);
    const int x = 1006;
    std::cout << 1006;
    for (int i = 0; i < 1006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1006(int& val) {
    const int x = 1006;
    std::cout << 1006;
    for (int i = 0; i < 1006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1007(int& val) {
    func_in_1007(val);
    const int x = 1007;
    std::cout << 1007;
    for (int i = 0; i < 1007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1007(int& val) {
    const int x = 1007;
    std::cout << 1007;
    for (int i = 0; i < 1007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1008(int& val) {
    func_in_1008(val);
    const int x = 1008;
    std::cout << 1008;
    for (int i = 0; i < 1008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1008(int& val) {
    const int x = 1008;
    std::cout << 1008;
    for (int i = 0; i < 1008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1009(int& val) {
    func_in_1009(val);
    const int x = 1009;
    std::cout << 1009;
    for (int i = 0; i < 1009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1009(int& val) {
    const int x = 1009;
    std::cout << 1009;
    for (int i = 0; i < 1009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1010(int& val) {
    func_in_1010(val);
    const int x = 1010;
    std::cout << 1010;
    for (int i = 0; i < 1010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1010(int& val) {
    const int x = 1010;
    std::cout << 1010;
    for (int i = 0; i < 1010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1011(int& val) {
    func_in_1011(val);
    const int x = 1011;
    std::cout << 1011;
    for (int i = 0; i < 1011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1011(int& val) {
    const int x = 1011;
    std::cout << 1011;
    for (int i = 0; i < 1011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1012(int& val) {
    func_in_1012(val);
    const int x = 1012;
    std::cout << 1012;
    for (int i = 0; i < 1012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1012(int& val) {
    const int x = 1012;
    std::cout << 1012;
    for (int i = 0; i < 1012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1013(int& val) {
    func_in_1013(val);
    const int x = 1013;
    std::cout << 1013;
    for (int i = 0; i < 1013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1013(int& val) {
    const int x = 1013;
    std::cout << 1013;
    for (int i = 0; i < 1013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1014(int& val) {
    func_in_1014(val);
    const int x = 1014;
    std::cout << 1014;
    for (int i = 0; i < 1014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1014(int& val) {
    const int x = 1014;
    std::cout << 1014;
    for (int i = 0; i < 1014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1015(int& val) {
    func_in_1015(val);
    const int x = 1015;
    std::cout << 1015;
    for (int i = 0; i < 1015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1015(int& val) {
    const int x = 1015;
    std::cout << 1015;
    for (int i = 0; i < 1015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1016(int& val) {
    func_in_1016(val);
    const int x = 1016;
    std::cout << 1016;
    for (int i = 0; i < 1016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1016(int& val) {
    const int x = 1016;
    std::cout << 1016;
    for (int i = 0; i < 1016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1017(int& val) {
    func_in_1017(val);
    const int x = 1017;
    std::cout << 1017;
    for (int i = 0; i < 1017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1017(int& val) {
    const int x = 1017;
    std::cout << 1017;
    for (int i = 0; i < 1017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1018(int& val) {
    func_in_1018(val);
    const int x = 1018;
    std::cout << 1018;
    for (int i = 0; i < 1018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1018(int& val) {
    const int x = 1018;
    std::cout << 1018;
    for (int i = 0; i < 1018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1019(int& val) {
    func_in_1019(val);
    const int x = 1019;
    std::cout << 1019;
    for (int i = 0; i < 1019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1019(int& val) {
    const int x = 1019;
    std::cout << 1019;
    for (int i = 0; i < 1019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1020(int& val) {
    func_in_1020(val);
    const int x = 1020;
    std::cout << 1020;
    for (int i = 0; i < 1020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1020(int& val) {
    const int x = 1020;
    std::cout << 1020;
    for (int i = 0; i < 1020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1021(int& val) {
    func_in_1021(val);
    const int x = 1021;
    std::cout << 1021;
    for (int i = 0; i < 1021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1021(int& val) {
    const int x = 1021;
    std::cout << 1021;
    for (int i = 0; i < 1021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1022(int& val) {
    func_in_1022(val);
    const int x = 1022;
    std::cout << 1022;
    for (int i = 0; i < 1022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1022(int& val) {
    const int x = 1022;
    std::cout << 1022;
    for (int i = 0; i < 1022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1023(int& val) {
    func_in_1023(val);
    const int x = 1023;
    std::cout << 1023;
    for (int i = 0; i < 1023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1023(int& val) {
    const int x = 1023;
    std::cout << 1023;
    for (int i = 0; i < 1023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1024(int& val) {
    func_in_1024(val);
    const int x = 1024;
    std::cout << 1024;
    for (int i = 0; i < 1024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1024(int& val) {
    const int x = 1024;
    std::cout << 1024;
    for (int i = 0; i < 1024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1025(int& val) {
    func_in_1025(val);
    const int x = 1025;
    std::cout << 1025;
    for (int i = 0; i < 1025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1025(int& val) {
    const int x = 1025;
    std::cout << 1025;
    for (int i = 0; i < 1025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1026(int& val) {
    func_in_1026(val);
    const int x = 1026;
    std::cout << 1026;
    for (int i = 0; i < 1026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1026(int& val) {
    const int x = 1026;
    std::cout << 1026;
    for (int i = 0; i < 1026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1027(int& val) {
    func_in_1027(val);
    const int x = 1027;
    std::cout << 1027;
    for (int i = 0; i < 1027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1027(int& val) {
    const int x = 1027;
    std::cout << 1027;
    for (int i = 0; i < 1027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1028(int& val) {
    func_in_1028(val);
    const int x = 1028;
    std::cout << 1028;
    for (int i = 0; i < 1028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1028(int& val) {
    const int x = 1028;
    std::cout << 1028;
    for (int i = 0; i < 1028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1029(int& val) {
    func_in_1029(val);
    const int x = 1029;
    std::cout << 1029;
    for (int i = 0; i < 1029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1029(int& val) {
    const int x = 1029;
    std::cout << 1029;
    for (int i = 0; i < 1029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1030(int& val) {
    func_in_1030(val);
    const int x = 1030;
    std::cout << 1030;
    for (int i = 0; i < 1030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1030(int& val) {
    const int x = 1030;
    std::cout << 1030;
    for (int i = 0; i < 1030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1031(int& val) {
    func_in_1031(val);
    const int x = 1031;
    std::cout << 1031;
    for (int i = 0; i < 1031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1031(int& val) {
    const int x = 1031;
    std::cout << 1031;
    for (int i = 0; i < 1031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1032(int& val) {
    func_in_1032(val);
    const int x = 1032;
    std::cout << 1032;
    for (int i = 0; i < 1032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1032(int& val) {
    const int x = 1032;
    std::cout << 1032;
    for (int i = 0; i < 1032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1033(int& val) {
    func_in_1033(val);
    const int x = 1033;
    std::cout << 1033;
    for (int i = 0; i < 1033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1033(int& val) {
    const int x = 1033;
    std::cout << 1033;
    for (int i = 0; i < 1033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1034(int& val) {
    func_in_1034(val);
    const int x = 1034;
    std::cout << 1034;
    for (int i = 0; i < 1034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1034(int& val) {
    const int x = 1034;
    std::cout << 1034;
    for (int i = 0; i < 1034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1035(int& val) {
    func_in_1035(val);
    const int x = 1035;
    std::cout << 1035;
    for (int i = 0; i < 1035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1035(int& val) {
    const int x = 1035;
    std::cout << 1035;
    for (int i = 0; i < 1035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1036(int& val) {
    func_in_1036(val);
    const int x = 1036;
    std::cout << 1036;
    for (int i = 0; i < 1036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1036(int& val) {
    const int x = 1036;
    std::cout << 1036;
    for (int i = 0; i < 1036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1037(int& val) {
    func_in_1037(val);
    const int x = 1037;
    std::cout << 1037;
    for (int i = 0; i < 1037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1037(int& val) {
    const int x = 1037;
    std::cout << 1037;
    for (int i = 0; i < 1037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1038(int& val) {
    func_in_1038(val);
    const int x = 1038;
    std::cout << 1038;
    for (int i = 0; i < 1038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1038(int& val) {
    const int x = 1038;
    std::cout << 1038;
    for (int i = 0; i < 1038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1039(int& val) {
    func_in_1039(val);
    const int x = 1039;
    std::cout << 1039;
    for (int i = 0; i < 1039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1039(int& val) {
    const int x = 1039;
    std::cout << 1039;
    for (int i = 0; i < 1039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1040(int& val) {
    func_in_1040(val);
    const int x = 1040;
    std::cout << 1040;
    for (int i = 0; i < 1040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1040(int& val) {
    const int x = 1040;
    std::cout << 1040;
    for (int i = 0; i < 1040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1041(int& val) {
    func_in_1041(val);
    const int x = 1041;
    std::cout << 1041;
    for (int i = 0; i < 1041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1041(int& val) {
    const int x = 1041;
    std::cout << 1041;
    for (int i = 0; i < 1041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1042(int& val) {
    func_in_1042(val);
    const int x = 1042;
    std::cout << 1042;
    for (int i = 0; i < 1042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1042(int& val) {
    const int x = 1042;
    std::cout << 1042;
    for (int i = 0; i < 1042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1043(int& val) {
    func_in_1043(val);
    const int x = 1043;
    std::cout << 1043;
    for (int i = 0; i < 1043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1043(int& val) {
    const int x = 1043;
    std::cout << 1043;
    for (int i = 0; i < 1043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1044(int& val) {
    func_in_1044(val);
    const int x = 1044;
    std::cout << 1044;
    for (int i = 0; i < 1044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1044(int& val) {
    const int x = 1044;
    std::cout << 1044;
    for (int i = 0; i < 1044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1045(int& val) {
    func_in_1045(val);
    const int x = 1045;
    std::cout << 1045;
    for (int i = 0; i < 1045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1045(int& val) {
    const int x = 1045;
    std::cout << 1045;
    for (int i = 0; i < 1045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1046(int& val) {
    func_in_1046(val);
    const int x = 1046;
    std::cout << 1046;
    for (int i = 0; i < 1046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1046(int& val) {
    const int x = 1046;
    std::cout << 1046;
    for (int i = 0; i < 1046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1047(int& val) {
    func_in_1047(val);
    const int x = 1047;
    std::cout << 1047;
    for (int i = 0; i < 1047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1047(int& val) {
    const int x = 1047;
    std::cout << 1047;
    for (int i = 0; i < 1047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1048(int& val) {
    func_in_1048(val);
    const int x = 1048;
    std::cout << 1048;
    for (int i = 0; i < 1048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1048(int& val) {
    const int x = 1048;
    std::cout << 1048;
    for (int i = 0; i < 1048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1049(int& val) {
    func_in_1049(val);
    const int x = 1049;
    std::cout << 1049;
    for (int i = 0; i < 1049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1049(int& val) {
    const int x = 1049;
    std::cout << 1049;
    for (int i = 0; i < 1049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1050(int& val) {
    func_in_1050(val);
    const int x = 1050;
    std::cout << 1050;
    for (int i = 0; i < 1050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1050(int& val) {
    const int x = 1050;
    std::cout << 1050;
    for (int i = 0; i < 1050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1051(int& val) {
    func_in_1051(val);
    const int x = 1051;
    std::cout << 1051;
    for (int i = 0; i < 1051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1051(int& val) {
    const int x = 1051;
    std::cout << 1051;
    for (int i = 0; i < 1051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1052(int& val) {
    func_in_1052(val);
    const int x = 1052;
    std::cout << 1052;
    for (int i = 0; i < 1052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1052(int& val) {
    const int x = 1052;
    std::cout << 1052;
    for (int i = 0; i < 1052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1053(int& val) {
    func_in_1053(val);
    const int x = 1053;
    std::cout << 1053;
    for (int i = 0; i < 1053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1053(int& val) {
    const int x = 1053;
    std::cout << 1053;
    for (int i = 0; i < 1053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1054(int& val) {
    func_in_1054(val);
    const int x = 1054;
    std::cout << 1054;
    for (int i = 0; i < 1054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1054(int& val) {
    const int x = 1054;
    std::cout << 1054;
    for (int i = 0; i < 1054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1055(int& val) {
    func_in_1055(val);
    const int x = 1055;
    std::cout << 1055;
    for (int i = 0; i < 1055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1055(int& val) {
    const int x = 1055;
    std::cout << 1055;
    for (int i = 0; i < 1055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1056(int& val) {
    func_in_1056(val);
    const int x = 1056;
    std::cout << 1056;
    for (int i = 0; i < 1056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1056(int& val) {
    const int x = 1056;
    std::cout << 1056;
    for (int i = 0; i < 1056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1057(int& val) {
    func_in_1057(val);
    const int x = 1057;
    std::cout << 1057;
    for (int i = 0; i < 1057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1057(int& val) {
    const int x = 1057;
    std::cout << 1057;
    for (int i = 0; i < 1057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1058(int& val) {
    func_in_1058(val);
    const int x = 1058;
    std::cout << 1058;
    for (int i = 0; i < 1058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1058(int& val) {
    const int x = 1058;
    std::cout << 1058;
    for (int i = 0; i < 1058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1059(int& val) {
    func_in_1059(val);
    const int x = 1059;
    std::cout << 1059;
    for (int i = 0; i < 1059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1059(int& val) {
    const int x = 1059;
    std::cout << 1059;
    for (int i = 0; i < 1059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1060(int& val) {
    func_in_1060(val);
    const int x = 1060;
    std::cout << 1060;
    for (int i = 0; i < 1060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1060(int& val) {
    const int x = 1060;
    std::cout << 1060;
    for (int i = 0; i < 1060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1061(int& val) {
    func_in_1061(val);
    const int x = 1061;
    std::cout << 1061;
    for (int i = 0; i < 1061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1061(int& val) {
    const int x = 1061;
    std::cout << 1061;
    for (int i = 0; i < 1061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1062(int& val) {
    func_in_1062(val);
    const int x = 1062;
    std::cout << 1062;
    for (int i = 0; i < 1062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1062(int& val) {
    const int x = 1062;
    std::cout << 1062;
    for (int i = 0; i < 1062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1063(int& val) {
    func_in_1063(val);
    const int x = 1063;
    std::cout << 1063;
    for (int i = 0; i < 1063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1063(int& val) {
    const int x = 1063;
    std::cout << 1063;
    for (int i = 0; i < 1063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1064(int& val) {
    func_in_1064(val);
    const int x = 1064;
    std::cout << 1064;
    for (int i = 0; i < 1064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1064(int& val) {
    const int x = 1064;
    std::cout << 1064;
    for (int i = 0; i < 1064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1065(int& val) {
    func_in_1065(val);
    const int x = 1065;
    std::cout << 1065;
    for (int i = 0; i < 1065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1065(int& val) {
    const int x = 1065;
    std::cout << 1065;
    for (int i = 0; i < 1065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1066(int& val) {
    func_in_1066(val);
    const int x = 1066;
    std::cout << 1066;
    for (int i = 0; i < 1066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1066(int& val) {
    const int x = 1066;
    std::cout << 1066;
    for (int i = 0; i < 1066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1067(int& val) {
    func_in_1067(val);
    const int x = 1067;
    std::cout << 1067;
    for (int i = 0; i < 1067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1067(int& val) {
    const int x = 1067;
    std::cout << 1067;
    for (int i = 0; i < 1067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1068(int& val) {
    func_in_1068(val);
    const int x = 1068;
    std::cout << 1068;
    for (int i = 0; i < 1068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1068(int& val) {
    const int x = 1068;
    std::cout << 1068;
    for (int i = 0; i < 1068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1069(int& val) {
    func_in_1069(val);
    const int x = 1069;
    std::cout << 1069;
    for (int i = 0; i < 1069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1069(int& val) {
    const int x = 1069;
    std::cout << 1069;
    for (int i = 0; i < 1069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1070(int& val) {
    func_in_1070(val);
    const int x = 1070;
    std::cout << 1070;
    for (int i = 0; i < 1070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1070(int& val) {
    const int x = 1070;
    std::cout << 1070;
    for (int i = 0; i < 1070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1071(int& val) {
    func_in_1071(val);
    const int x = 1071;
    std::cout << 1071;
    for (int i = 0; i < 1071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1071(int& val) {
    const int x = 1071;
    std::cout << 1071;
    for (int i = 0; i < 1071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1072(int& val) {
    func_in_1072(val);
    const int x = 1072;
    std::cout << 1072;
    for (int i = 0; i < 1072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1072(int& val) {
    const int x = 1072;
    std::cout << 1072;
    for (int i = 0; i < 1072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1073(int& val) {
    func_in_1073(val);
    const int x = 1073;
    std::cout << 1073;
    for (int i = 0; i < 1073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1073(int& val) {
    const int x = 1073;
    std::cout << 1073;
    for (int i = 0; i < 1073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1074(int& val) {
    func_in_1074(val);
    const int x = 1074;
    std::cout << 1074;
    for (int i = 0; i < 1074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1074(int& val) {
    const int x = 1074;
    std::cout << 1074;
    for (int i = 0; i < 1074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1075(int& val) {
    func_in_1075(val);
    const int x = 1075;
    std::cout << 1075;
    for (int i = 0; i < 1075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1075(int& val) {
    const int x = 1075;
    std::cout << 1075;
    for (int i = 0; i < 1075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1076(int& val) {
    func_in_1076(val);
    const int x = 1076;
    std::cout << 1076;
    for (int i = 0; i < 1076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1076(int& val) {
    const int x = 1076;
    std::cout << 1076;
    for (int i = 0; i < 1076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1077(int& val) {
    func_in_1077(val);
    const int x = 1077;
    std::cout << 1077;
    for (int i = 0; i < 1077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1077(int& val) {
    const int x = 1077;
    std::cout << 1077;
    for (int i = 0; i < 1077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1078(int& val) {
    func_in_1078(val);
    const int x = 1078;
    std::cout << 1078;
    for (int i = 0; i < 1078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1078(int& val) {
    const int x = 1078;
    std::cout << 1078;
    for (int i = 0; i < 1078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1079(int& val) {
    func_in_1079(val);
    const int x = 1079;
    std::cout << 1079;
    for (int i = 0; i < 1079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1079(int& val) {
    const int x = 1079;
    std::cout << 1079;
    for (int i = 0; i < 1079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1080(int& val) {
    func_in_1080(val);
    const int x = 1080;
    std::cout << 1080;
    for (int i = 0; i < 1080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1080(int& val) {
    const int x = 1080;
    std::cout << 1080;
    for (int i = 0; i < 1080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1081(int& val) {
    func_in_1081(val);
    const int x = 1081;
    std::cout << 1081;
    for (int i = 0; i < 1081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1081(int& val) {
    const int x = 1081;
    std::cout << 1081;
    for (int i = 0; i < 1081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1082(int& val) {
    func_in_1082(val);
    const int x = 1082;
    std::cout << 1082;
    for (int i = 0; i < 1082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1082(int& val) {
    const int x = 1082;
    std::cout << 1082;
    for (int i = 0; i < 1082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1083(int& val) {
    func_in_1083(val);
    const int x = 1083;
    std::cout << 1083;
    for (int i = 0; i < 1083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1083(int& val) {
    const int x = 1083;
    std::cout << 1083;
    for (int i = 0; i < 1083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1084(int& val) {
    func_in_1084(val);
    const int x = 1084;
    std::cout << 1084;
    for (int i = 0; i < 1084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1084(int& val) {
    const int x = 1084;
    std::cout << 1084;
    for (int i = 0; i < 1084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1085(int& val) {
    func_in_1085(val);
    const int x = 1085;
    std::cout << 1085;
    for (int i = 0; i < 1085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1085(int& val) {
    const int x = 1085;
    std::cout << 1085;
    for (int i = 0; i < 1085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1086(int& val) {
    func_in_1086(val);
    const int x = 1086;
    std::cout << 1086;
    for (int i = 0; i < 1086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1086(int& val) {
    const int x = 1086;
    std::cout << 1086;
    for (int i = 0; i < 1086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1087(int& val) {
    func_in_1087(val);
    const int x = 1087;
    std::cout << 1087;
    for (int i = 0; i < 1087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1087(int& val) {
    const int x = 1087;
    std::cout << 1087;
    for (int i = 0; i < 1087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1088(int& val) {
    func_in_1088(val);
    const int x = 1088;
    std::cout << 1088;
    for (int i = 0; i < 1088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1088(int& val) {
    const int x = 1088;
    std::cout << 1088;
    for (int i = 0; i < 1088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1089(int& val) {
    func_in_1089(val);
    const int x = 1089;
    std::cout << 1089;
    for (int i = 0; i < 1089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1089(int& val) {
    const int x = 1089;
    std::cout << 1089;
    for (int i = 0; i < 1089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1090(int& val) {
    func_in_1090(val);
    const int x = 1090;
    std::cout << 1090;
    for (int i = 0; i < 1090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1090(int& val) {
    const int x = 1090;
    std::cout << 1090;
    for (int i = 0; i < 1090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1091(int& val) {
    func_in_1091(val);
    const int x = 1091;
    std::cout << 1091;
    for (int i = 0; i < 1091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1091(int& val) {
    const int x = 1091;
    std::cout << 1091;
    for (int i = 0; i < 1091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1092(int& val) {
    func_in_1092(val);
    const int x = 1092;
    std::cout << 1092;
    for (int i = 0; i < 1092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1092(int& val) {
    const int x = 1092;
    std::cout << 1092;
    for (int i = 0; i < 1092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1093(int& val) {
    func_in_1093(val);
    const int x = 1093;
    std::cout << 1093;
    for (int i = 0; i < 1093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1093(int& val) {
    const int x = 1093;
    std::cout << 1093;
    for (int i = 0; i < 1093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1094(int& val) {
    func_in_1094(val);
    const int x = 1094;
    std::cout << 1094;
    for (int i = 0; i < 1094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1094(int& val) {
    const int x = 1094;
    std::cout << 1094;
    for (int i = 0; i < 1094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1095(int& val) {
    func_in_1095(val);
    const int x = 1095;
    std::cout << 1095;
    for (int i = 0; i < 1095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1095(int& val) {
    const int x = 1095;
    std::cout << 1095;
    for (int i = 0; i < 1095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1096(int& val) {
    func_in_1096(val);
    const int x = 1096;
    std::cout << 1096;
    for (int i = 0; i < 1096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1096(int& val) {
    const int x = 1096;
    std::cout << 1096;
    for (int i = 0; i < 1096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1097(int& val) {
    func_in_1097(val);
    const int x = 1097;
    std::cout << 1097;
    for (int i = 0; i < 1097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1097(int& val) {
    const int x = 1097;
    std::cout << 1097;
    for (int i = 0; i < 1097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1098(int& val) {
    func_in_1098(val);
    const int x = 1098;
    std::cout << 1098;
    for (int i = 0; i < 1098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1098(int& val) {
    const int x = 1098;
    std::cout << 1098;
    for (int i = 0; i < 1098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1099(int& val) {
    func_in_1099(val);
    const int x = 1099;
    std::cout << 1099;
    for (int i = 0; i < 1099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1099(int& val) {
    const int x = 1099;
    std::cout << 1099;
    for (int i = 0; i < 1099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1100(int& val) {
    func_in_1100(val);
    const int x = 1100;
    std::cout << 1100;
    for (int i = 0; i < 1100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1100(int& val) {
    const int x = 1100;
    std::cout << 1100;
    for (int i = 0; i < 1100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1101(int& val) {
    func_in_1101(val);
    const int x = 1101;
    std::cout << 1101;
    for (int i = 0; i < 1101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1101(int& val) {
    const int x = 1101;
    std::cout << 1101;
    for (int i = 0; i < 1101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1102(int& val) {
    func_in_1102(val);
    const int x = 1102;
    std::cout << 1102;
    for (int i = 0; i < 1102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1102(int& val) {
    const int x = 1102;
    std::cout << 1102;
    for (int i = 0; i < 1102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1103(int& val) {
    func_in_1103(val);
    const int x = 1103;
    std::cout << 1103;
    for (int i = 0; i < 1103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1103(int& val) {
    const int x = 1103;
    std::cout << 1103;
    for (int i = 0; i < 1103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1104(int& val) {
    func_in_1104(val);
    const int x = 1104;
    std::cout << 1104;
    for (int i = 0; i < 1104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1104(int& val) {
    const int x = 1104;
    std::cout << 1104;
    for (int i = 0; i < 1104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1105(int& val) {
    func_in_1105(val);
    const int x = 1105;
    std::cout << 1105;
    for (int i = 0; i < 1105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1105(int& val) {
    const int x = 1105;
    std::cout << 1105;
    for (int i = 0; i < 1105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1106(int& val) {
    func_in_1106(val);
    const int x = 1106;
    std::cout << 1106;
    for (int i = 0; i < 1106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1106(int& val) {
    const int x = 1106;
    std::cout << 1106;
    for (int i = 0; i < 1106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1107(int& val) {
    func_in_1107(val);
    const int x = 1107;
    std::cout << 1107;
    for (int i = 0; i < 1107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1107(int& val) {
    const int x = 1107;
    std::cout << 1107;
    for (int i = 0; i < 1107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1108(int& val) {
    func_in_1108(val);
    const int x = 1108;
    std::cout << 1108;
    for (int i = 0; i < 1108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1108(int& val) {
    const int x = 1108;
    std::cout << 1108;
    for (int i = 0; i < 1108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1109(int& val) {
    func_in_1109(val);
    const int x = 1109;
    std::cout << 1109;
    for (int i = 0; i < 1109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1109(int& val) {
    const int x = 1109;
    std::cout << 1109;
    for (int i = 0; i < 1109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1110(int& val) {
    func_in_1110(val);
    const int x = 1110;
    std::cout << 1110;
    for (int i = 0; i < 1110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1110(int& val) {
    const int x = 1110;
    std::cout << 1110;
    for (int i = 0; i < 1110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1111(int& val) {
    func_in_1111(val);
    const int x = 1111;
    std::cout << 1111;
    for (int i = 0; i < 1111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1111(int& val) {
    const int x = 1111;
    std::cout << 1111;
    for (int i = 0; i < 1111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1112(int& val) {
    func_in_1112(val);
    const int x = 1112;
    std::cout << 1112;
    for (int i = 0; i < 1112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1112(int& val) {
    const int x = 1112;
    std::cout << 1112;
    for (int i = 0; i < 1112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1113(int& val) {
    func_in_1113(val);
    const int x = 1113;
    std::cout << 1113;
    for (int i = 0; i < 1113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1113(int& val) {
    const int x = 1113;
    std::cout << 1113;
    for (int i = 0; i < 1113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1114(int& val) {
    func_in_1114(val);
    const int x = 1114;
    std::cout << 1114;
    for (int i = 0; i < 1114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1114(int& val) {
    const int x = 1114;
    std::cout << 1114;
    for (int i = 0; i < 1114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1115(int& val) {
    func_in_1115(val);
    const int x = 1115;
    std::cout << 1115;
    for (int i = 0; i < 1115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1115(int& val) {
    const int x = 1115;
    std::cout << 1115;
    for (int i = 0; i < 1115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1116(int& val) {
    func_in_1116(val);
    const int x = 1116;
    std::cout << 1116;
    for (int i = 0; i < 1116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1116(int& val) {
    const int x = 1116;
    std::cout << 1116;
    for (int i = 0; i < 1116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1117(int& val) {
    func_in_1117(val);
    const int x = 1117;
    std::cout << 1117;
    for (int i = 0; i < 1117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1117(int& val) {
    const int x = 1117;
    std::cout << 1117;
    for (int i = 0; i < 1117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1118(int& val) {
    func_in_1118(val);
    const int x = 1118;
    std::cout << 1118;
    for (int i = 0; i < 1118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1118(int& val) {
    const int x = 1118;
    std::cout << 1118;
    for (int i = 0; i < 1118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1119(int& val) {
    func_in_1119(val);
    const int x = 1119;
    std::cout << 1119;
    for (int i = 0; i < 1119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1119(int& val) {
    const int x = 1119;
    std::cout << 1119;
    for (int i = 0; i < 1119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1120(int& val) {
    func_in_1120(val);
    const int x = 1120;
    std::cout << 1120;
    for (int i = 0; i < 1120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1120(int& val) {
    const int x = 1120;
    std::cout << 1120;
    for (int i = 0; i < 1120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1121(int& val) {
    func_in_1121(val);
    const int x = 1121;
    std::cout << 1121;
    for (int i = 0; i < 1121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1121(int& val) {
    const int x = 1121;
    std::cout << 1121;
    for (int i = 0; i < 1121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1122(int& val) {
    func_in_1122(val);
    const int x = 1122;
    std::cout << 1122;
    for (int i = 0; i < 1122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1122(int& val) {
    const int x = 1122;
    std::cout << 1122;
    for (int i = 0; i < 1122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1123(int& val) {
    func_in_1123(val);
    const int x = 1123;
    std::cout << 1123;
    for (int i = 0; i < 1123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1123(int& val) {
    const int x = 1123;
    std::cout << 1123;
    for (int i = 0; i < 1123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1124(int& val) {
    func_in_1124(val);
    const int x = 1124;
    std::cout << 1124;
    for (int i = 0; i < 1124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1124(int& val) {
    const int x = 1124;
    std::cout << 1124;
    for (int i = 0; i < 1124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1125(int& val) {
    func_in_1125(val);
    const int x = 1125;
    std::cout << 1125;
    for (int i = 0; i < 1125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1125(int& val) {
    const int x = 1125;
    std::cout << 1125;
    for (int i = 0; i < 1125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1126(int& val) {
    func_in_1126(val);
    const int x = 1126;
    std::cout << 1126;
    for (int i = 0; i < 1126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1126(int& val) {
    const int x = 1126;
    std::cout << 1126;
    for (int i = 0; i < 1126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1127(int& val) {
    func_in_1127(val);
    const int x = 1127;
    std::cout << 1127;
    for (int i = 0; i < 1127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1127(int& val) {
    const int x = 1127;
    std::cout << 1127;
    for (int i = 0; i < 1127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1128(int& val) {
    func_in_1128(val);
    const int x = 1128;
    std::cout << 1128;
    for (int i = 0; i < 1128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1128(int& val) {
    const int x = 1128;
    std::cout << 1128;
    for (int i = 0; i < 1128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1129(int& val) {
    func_in_1129(val);
    const int x = 1129;
    std::cout << 1129;
    for (int i = 0; i < 1129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1129(int& val) {
    const int x = 1129;
    std::cout << 1129;
    for (int i = 0; i < 1129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1130(int& val) {
    func_in_1130(val);
    const int x = 1130;
    std::cout << 1130;
    for (int i = 0; i < 1130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1130(int& val) {
    const int x = 1130;
    std::cout << 1130;
    for (int i = 0; i < 1130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1131(int& val) {
    func_in_1131(val);
    const int x = 1131;
    std::cout << 1131;
    for (int i = 0; i < 1131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1131(int& val) {
    const int x = 1131;
    std::cout << 1131;
    for (int i = 0; i < 1131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1132(int& val) {
    func_in_1132(val);
    const int x = 1132;
    std::cout << 1132;
    for (int i = 0; i < 1132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1132(int& val) {
    const int x = 1132;
    std::cout << 1132;
    for (int i = 0; i < 1132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1133(int& val) {
    func_in_1133(val);
    const int x = 1133;
    std::cout << 1133;
    for (int i = 0; i < 1133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1133(int& val) {
    const int x = 1133;
    std::cout << 1133;
    for (int i = 0; i < 1133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1134(int& val) {
    func_in_1134(val);
    const int x = 1134;
    std::cout << 1134;
    for (int i = 0; i < 1134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1134(int& val) {
    const int x = 1134;
    std::cout << 1134;
    for (int i = 0; i < 1134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1135(int& val) {
    func_in_1135(val);
    const int x = 1135;
    std::cout << 1135;
    for (int i = 0; i < 1135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1135(int& val) {
    const int x = 1135;
    std::cout << 1135;
    for (int i = 0; i < 1135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1136(int& val) {
    func_in_1136(val);
    const int x = 1136;
    std::cout << 1136;
    for (int i = 0; i < 1136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1136(int& val) {
    const int x = 1136;
    std::cout << 1136;
    for (int i = 0; i < 1136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1137(int& val) {
    func_in_1137(val);
    const int x = 1137;
    std::cout << 1137;
    for (int i = 0; i < 1137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1137(int& val) {
    const int x = 1137;
    std::cout << 1137;
    for (int i = 0; i < 1137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1138(int& val) {
    func_in_1138(val);
    const int x = 1138;
    std::cout << 1138;
    for (int i = 0; i < 1138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1138(int& val) {
    const int x = 1138;
    std::cout << 1138;
    for (int i = 0; i < 1138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1139(int& val) {
    func_in_1139(val);
    const int x = 1139;
    std::cout << 1139;
    for (int i = 0; i < 1139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1139(int& val) {
    const int x = 1139;
    std::cout << 1139;
    for (int i = 0; i < 1139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1140(int& val) {
    func_in_1140(val);
    const int x = 1140;
    std::cout << 1140;
    for (int i = 0; i < 1140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1140(int& val) {
    const int x = 1140;
    std::cout << 1140;
    for (int i = 0; i < 1140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1141(int& val) {
    func_in_1141(val);
    const int x = 1141;
    std::cout << 1141;
    for (int i = 0; i < 1141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1141(int& val) {
    const int x = 1141;
    std::cout << 1141;
    for (int i = 0; i < 1141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1142(int& val) {
    func_in_1142(val);
    const int x = 1142;
    std::cout << 1142;
    for (int i = 0; i < 1142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1142(int& val) {
    const int x = 1142;
    std::cout << 1142;
    for (int i = 0; i < 1142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1143(int& val) {
    func_in_1143(val);
    const int x = 1143;
    std::cout << 1143;
    for (int i = 0; i < 1143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1143(int& val) {
    const int x = 1143;
    std::cout << 1143;
    for (int i = 0; i < 1143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1144(int& val) {
    func_in_1144(val);
    const int x = 1144;
    std::cout << 1144;
    for (int i = 0; i < 1144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1144(int& val) {
    const int x = 1144;
    std::cout << 1144;
    for (int i = 0; i < 1144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1145(int& val) {
    func_in_1145(val);
    const int x = 1145;
    std::cout << 1145;
    for (int i = 0; i < 1145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1145(int& val) {
    const int x = 1145;
    std::cout << 1145;
    for (int i = 0; i < 1145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1146(int& val) {
    func_in_1146(val);
    const int x = 1146;
    std::cout << 1146;
    for (int i = 0; i < 1146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1146(int& val) {
    const int x = 1146;
    std::cout << 1146;
    for (int i = 0; i < 1146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1147(int& val) {
    func_in_1147(val);
    const int x = 1147;
    std::cout << 1147;
    for (int i = 0; i < 1147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1147(int& val) {
    const int x = 1147;
    std::cout << 1147;
    for (int i = 0; i < 1147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1148(int& val) {
    func_in_1148(val);
    const int x = 1148;
    std::cout << 1148;
    for (int i = 0; i < 1148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1148(int& val) {
    const int x = 1148;
    std::cout << 1148;
    for (int i = 0; i < 1148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1149(int& val) {
    func_in_1149(val);
    const int x = 1149;
    std::cout << 1149;
    for (int i = 0; i < 1149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1149(int& val) {
    const int x = 1149;
    std::cout << 1149;
    for (int i = 0; i < 1149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1150(int& val) {
    func_in_1150(val);
    const int x = 1150;
    std::cout << 1150;
    for (int i = 0; i < 1150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1150(int& val) {
    const int x = 1150;
    std::cout << 1150;
    for (int i = 0; i < 1150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1151(int& val) {
    func_in_1151(val);
    const int x = 1151;
    std::cout << 1151;
    for (int i = 0; i < 1151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1151(int& val) {
    const int x = 1151;
    std::cout << 1151;
    for (int i = 0; i < 1151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1152(int& val) {
    func_in_1152(val);
    const int x = 1152;
    std::cout << 1152;
    for (int i = 0; i < 1152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1152(int& val) {
    const int x = 1152;
    std::cout << 1152;
    for (int i = 0; i < 1152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1153(int& val) {
    func_in_1153(val);
    const int x = 1153;
    std::cout << 1153;
    for (int i = 0; i < 1153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1153(int& val) {
    const int x = 1153;
    std::cout << 1153;
    for (int i = 0; i < 1153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1154(int& val) {
    func_in_1154(val);
    const int x = 1154;
    std::cout << 1154;
    for (int i = 0; i < 1154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1154(int& val) {
    const int x = 1154;
    std::cout << 1154;
    for (int i = 0; i < 1154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1155(int& val) {
    func_in_1155(val);
    const int x = 1155;
    std::cout << 1155;
    for (int i = 0; i < 1155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1155(int& val) {
    const int x = 1155;
    std::cout << 1155;
    for (int i = 0; i < 1155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1156(int& val) {
    func_in_1156(val);
    const int x = 1156;
    std::cout << 1156;
    for (int i = 0; i < 1156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1156(int& val) {
    const int x = 1156;
    std::cout << 1156;
    for (int i = 0; i < 1156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1157(int& val) {
    func_in_1157(val);
    const int x = 1157;
    std::cout << 1157;
    for (int i = 0; i < 1157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1157(int& val) {
    const int x = 1157;
    std::cout << 1157;
    for (int i = 0; i < 1157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1158(int& val) {
    func_in_1158(val);
    const int x = 1158;
    std::cout << 1158;
    for (int i = 0; i < 1158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1158(int& val) {
    const int x = 1158;
    std::cout << 1158;
    for (int i = 0; i < 1158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1159(int& val) {
    func_in_1159(val);
    const int x = 1159;
    std::cout << 1159;
    for (int i = 0; i < 1159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1159(int& val) {
    const int x = 1159;
    std::cout << 1159;
    for (int i = 0; i < 1159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1160(int& val) {
    func_in_1160(val);
    const int x = 1160;
    std::cout << 1160;
    for (int i = 0; i < 1160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1160(int& val) {
    const int x = 1160;
    std::cout << 1160;
    for (int i = 0; i < 1160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1161(int& val) {
    func_in_1161(val);
    const int x = 1161;
    std::cout << 1161;
    for (int i = 0; i < 1161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1161(int& val) {
    const int x = 1161;
    std::cout << 1161;
    for (int i = 0; i < 1161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1162(int& val) {
    func_in_1162(val);
    const int x = 1162;
    std::cout << 1162;
    for (int i = 0; i < 1162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1162(int& val) {
    const int x = 1162;
    std::cout << 1162;
    for (int i = 0; i < 1162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1163(int& val) {
    func_in_1163(val);
    const int x = 1163;
    std::cout << 1163;
    for (int i = 0; i < 1163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1163(int& val) {
    const int x = 1163;
    std::cout << 1163;
    for (int i = 0; i < 1163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1164(int& val) {
    func_in_1164(val);
    const int x = 1164;
    std::cout << 1164;
    for (int i = 0; i < 1164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1164(int& val) {
    const int x = 1164;
    std::cout << 1164;
    for (int i = 0; i < 1164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1165(int& val) {
    func_in_1165(val);
    const int x = 1165;
    std::cout << 1165;
    for (int i = 0; i < 1165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1165(int& val) {
    const int x = 1165;
    std::cout << 1165;
    for (int i = 0; i < 1165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1166(int& val) {
    func_in_1166(val);
    const int x = 1166;
    std::cout << 1166;
    for (int i = 0; i < 1166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1166(int& val) {
    const int x = 1166;
    std::cout << 1166;
    for (int i = 0; i < 1166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1167(int& val) {
    func_in_1167(val);
    const int x = 1167;
    std::cout << 1167;
    for (int i = 0; i < 1167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1167(int& val) {
    const int x = 1167;
    std::cout << 1167;
    for (int i = 0; i < 1167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1168(int& val) {
    func_in_1168(val);
    const int x = 1168;
    std::cout << 1168;
    for (int i = 0; i < 1168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1168(int& val) {
    const int x = 1168;
    std::cout << 1168;
    for (int i = 0; i < 1168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1169(int& val) {
    func_in_1169(val);
    const int x = 1169;
    std::cout << 1169;
    for (int i = 0; i < 1169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1169(int& val) {
    const int x = 1169;
    std::cout << 1169;
    for (int i = 0; i < 1169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1170(int& val) {
    func_in_1170(val);
    const int x = 1170;
    std::cout << 1170;
    for (int i = 0; i < 1170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1170(int& val) {
    const int x = 1170;
    std::cout << 1170;
    for (int i = 0; i < 1170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1171(int& val) {
    func_in_1171(val);
    const int x = 1171;
    std::cout << 1171;
    for (int i = 0; i < 1171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1171(int& val) {
    const int x = 1171;
    std::cout << 1171;
    for (int i = 0; i < 1171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1172(int& val) {
    func_in_1172(val);
    const int x = 1172;
    std::cout << 1172;
    for (int i = 0; i < 1172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1172(int& val) {
    const int x = 1172;
    std::cout << 1172;
    for (int i = 0; i < 1172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1173(int& val) {
    func_in_1173(val);
    const int x = 1173;
    std::cout << 1173;
    for (int i = 0; i < 1173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1173(int& val) {
    const int x = 1173;
    std::cout << 1173;
    for (int i = 0; i < 1173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1174(int& val) {
    func_in_1174(val);
    const int x = 1174;
    std::cout << 1174;
    for (int i = 0; i < 1174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1174(int& val) {
    const int x = 1174;
    std::cout << 1174;
    for (int i = 0; i < 1174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1175(int& val) {
    func_in_1175(val);
    const int x = 1175;
    std::cout << 1175;
    for (int i = 0; i < 1175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1175(int& val) {
    const int x = 1175;
    std::cout << 1175;
    for (int i = 0; i < 1175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1176(int& val) {
    func_in_1176(val);
    const int x = 1176;
    std::cout << 1176;
    for (int i = 0; i < 1176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1176(int& val) {
    const int x = 1176;
    std::cout << 1176;
    for (int i = 0; i < 1176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1177(int& val) {
    func_in_1177(val);
    const int x = 1177;
    std::cout << 1177;
    for (int i = 0; i < 1177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1177(int& val) {
    const int x = 1177;
    std::cout << 1177;
    for (int i = 0; i < 1177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1178(int& val) {
    func_in_1178(val);
    const int x = 1178;
    std::cout << 1178;
    for (int i = 0; i < 1178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1178(int& val) {
    const int x = 1178;
    std::cout << 1178;
    for (int i = 0; i < 1178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1179(int& val) {
    func_in_1179(val);
    const int x = 1179;
    std::cout << 1179;
    for (int i = 0; i < 1179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1179(int& val) {
    const int x = 1179;
    std::cout << 1179;
    for (int i = 0; i < 1179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1180(int& val) {
    func_in_1180(val);
    const int x = 1180;
    std::cout << 1180;
    for (int i = 0; i < 1180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1180(int& val) {
    const int x = 1180;
    std::cout << 1180;
    for (int i = 0; i < 1180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1181(int& val) {
    func_in_1181(val);
    const int x = 1181;
    std::cout << 1181;
    for (int i = 0; i < 1181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1181(int& val) {
    const int x = 1181;
    std::cout << 1181;
    for (int i = 0; i < 1181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1182(int& val) {
    func_in_1182(val);
    const int x = 1182;
    std::cout << 1182;
    for (int i = 0; i < 1182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1182(int& val) {
    const int x = 1182;
    std::cout << 1182;
    for (int i = 0; i < 1182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1183(int& val) {
    func_in_1183(val);
    const int x = 1183;
    std::cout << 1183;
    for (int i = 0; i < 1183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1183(int& val) {
    const int x = 1183;
    std::cout << 1183;
    for (int i = 0; i < 1183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1184(int& val) {
    func_in_1184(val);
    const int x = 1184;
    std::cout << 1184;
    for (int i = 0; i < 1184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1184(int& val) {
    const int x = 1184;
    std::cout << 1184;
    for (int i = 0; i < 1184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1185(int& val) {
    func_in_1185(val);
    const int x = 1185;
    std::cout << 1185;
    for (int i = 0; i < 1185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1185(int& val) {
    const int x = 1185;
    std::cout << 1185;
    for (int i = 0; i < 1185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1186(int& val) {
    func_in_1186(val);
    const int x = 1186;
    std::cout << 1186;
    for (int i = 0; i < 1186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1186(int& val) {
    const int x = 1186;
    std::cout << 1186;
    for (int i = 0; i < 1186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1187(int& val) {
    func_in_1187(val);
    const int x = 1187;
    std::cout << 1187;
    for (int i = 0; i < 1187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1187(int& val) {
    const int x = 1187;
    std::cout << 1187;
    for (int i = 0; i < 1187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1188(int& val) {
    func_in_1188(val);
    const int x = 1188;
    std::cout << 1188;
    for (int i = 0; i < 1188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1188(int& val) {
    const int x = 1188;
    std::cout << 1188;
    for (int i = 0; i < 1188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1189(int& val) {
    func_in_1189(val);
    const int x = 1189;
    std::cout << 1189;
    for (int i = 0; i < 1189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1189(int& val) {
    const int x = 1189;
    std::cout << 1189;
    for (int i = 0; i < 1189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1190(int& val) {
    func_in_1190(val);
    const int x = 1190;
    std::cout << 1190;
    for (int i = 0; i < 1190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1190(int& val) {
    const int x = 1190;
    std::cout << 1190;
    for (int i = 0; i < 1190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1191(int& val) {
    func_in_1191(val);
    const int x = 1191;
    std::cout << 1191;
    for (int i = 0; i < 1191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1191(int& val) {
    const int x = 1191;
    std::cout << 1191;
    for (int i = 0; i < 1191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1192(int& val) {
    func_in_1192(val);
    const int x = 1192;
    std::cout << 1192;
    for (int i = 0; i < 1192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1192(int& val) {
    const int x = 1192;
    std::cout << 1192;
    for (int i = 0; i < 1192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1193(int& val) {
    func_in_1193(val);
    const int x = 1193;
    std::cout << 1193;
    for (int i = 0; i < 1193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1193(int& val) {
    const int x = 1193;
    std::cout << 1193;
    for (int i = 0; i < 1193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1194(int& val) {
    func_in_1194(val);
    const int x = 1194;
    std::cout << 1194;
    for (int i = 0; i < 1194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1194(int& val) {
    const int x = 1194;
    std::cout << 1194;
    for (int i = 0; i < 1194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1195(int& val) {
    func_in_1195(val);
    const int x = 1195;
    std::cout << 1195;
    for (int i = 0; i < 1195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1195(int& val) {
    const int x = 1195;
    std::cout << 1195;
    for (int i = 0; i < 1195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1196(int& val) {
    func_in_1196(val);
    const int x = 1196;
    std::cout << 1196;
    for (int i = 0; i < 1196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1196(int& val) {
    const int x = 1196;
    std::cout << 1196;
    for (int i = 0; i < 1196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1197(int& val) {
    func_in_1197(val);
    const int x = 1197;
    std::cout << 1197;
    for (int i = 0; i < 1197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1197(int& val) {
    const int x = 1197;
    std::cout << 1197;
    for (int i = 0; i < 1197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1198(int& val) {
    func_in_1198(val);
    const int x = 1198;
    std::cout << 1198;
    for (int i = 0; i < 1198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1198(int& val) {
    const int x = 1198;
    std::cout << 1198;
    for (int i = 0; i < 1198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1199(int& val) {
    func_in_1199(val);
    const int x = 1199;
    std::cout << 1199;
    for (int i = 0; i < 1199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1199(int& val) {
    const int x = 1199;
    std::cout << 1199;
    for (int i = 0; i < 1199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1200(int& val) {
    func_in_1200(val);
    const int x = 1200;
    std::cout << 1200;
    for (int i = 0; i < 1200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1200(int& val) {
    const int x = 1200;
    std::cout << 1200;
    for (int i = 0; i < 1200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1201(int& val) {
    func_in_1201(val);
    const int x = 1201;
    std::cout << 1201;
    for (int i = 0; i < 1201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1201(int& val) {
    const int x = 1201;
    std::cout << 1201;
    for (int i = 0; i < 1201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1202(int& val) {
    func_in_1202(val);
    const int x = 1202;
    std::cout << 1202;
    for (int i = 0; i < 1202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1202(int& val) {
    const int x = 1202;
    std::cout << 1202;
    for (int i = 0; i < 1202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1203(int& val) {
    func_in_1203(val);
    const int x = 1203;
    std::cout << 1203;
    for (int i = 0; i < 1203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1203(int& val) {
    const int x = 1203;
    std::cout << 1203;
    for (int i = 0; i < 1203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1204(int& val) {
    func_in_1204(val);
    const int x = 1204;
    std::cout << 1204;
    for (int i = 0; i < 1204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1204(int& val) {
    const int x = 1204;
    std::cout << 1204;
    for (int i = 0; i < 1204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1205(int& val) {
    func_in_1205(val);
    const int x = 1205;
    std::cout << 1205;
    for (int i = 0; i < 1205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1205(int& val) {
    const int x = 1205;
    std::cout << 1205;
    for (int i = 0; i < 1205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1206(int& val) {
    func_in_1206(val);
    const int x = 1206;
    std::cout << 1206;
    for (int i = 0; i < 1206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1206(int& val) {
    const int x = 1206;
    std::cout << 1206;
    for (int i = 0; i < 1206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1207(int& val) {
    func_in_1207(val);
    const int x = 1207;
    std::cout << 1207;
    for (int i = 0; i < 1207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1207(int& val) {
    const int x = 1207;
    std::cout << 1207;
    for (int i = 0; i < 1207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1208(int& val) {
    func_in_1208(val);
    const int x = 1208;
    std::cout << 1208;
    for (int i = 0; i < 1208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1208(int& val) {
    const int x = 1208;
    std::cout << 1208;
    for (int i = 0; i < 1208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1209(int& val) {
    func_in_1209(val);
    const int x = 1209;
    std::cout << 1209;
    for (int i = 0; i < 1209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1209(int& val) {
    const int x = 1209;
    std::cout << 1209;
    for (int i = 0; i < 1209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1210(int& val) {
    func_in_1210(val);
    const int x = 1210;
    std::cout << 1210;
    for (int i = 0; i < 1210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1210(int& val) {
    const int x = 1210;
    std::cout << 1210;
    for (int i = 0; i < 1210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1211(int& val) {
    func_in_1211(val);
    const int x = 1211;
    std::cout << 1211;
    for (int i = 0; i < 1211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1211(int& val) {
    const int x = 1211;
    std::cout << 1211;
    for (int i = 0; i < 1211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1212(int& val) {
    func_in_1212(val);
    const int x = 1212;
    std::cout << 1212;
    for (int i = 0; i < 1212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1212(int& val) {
    const int x = 1212;
    std::cout << 1212;
    for (int i = 0; i < 1212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1213(int& val) {
    func_in_1213(val);
    const int x = 1213;
    std::cout << 1213;
    for (int i = 0; i < 1213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1213(int& val) {
    const int x = 1213;
    std::cout << 1213;
    for (int i = 0; i < 1213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1214(int& val) {
    func_in_1214(val);
    const int x = 1214;
    std::cout << 1214;
    for (int i = 0; i < 1214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1214(int& val) {
    const int x = 1214;
    std::cout << 1214;
    for (int i = 0; i < 1214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1215(int& val) {
    func_in_1215(val);
    const int x = 1215;
    std::cout << 1215;
    for (int i = 0; i < 1215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1215(int& val) {
    const int x = 1215;
    std::cout << 1215;
    for (int i = 0; i < 1215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1216(int& val) {
    func_in_1216(val);
    const int x = 1216;
    std::cout << 1216;
    for (int i = 0; i < 1216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1216(int& val) {
    const int x = 1216;
    std::cout << 1216;
    for (int i = 0; i < 1216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1217(int& val) {
    func_in_1217(val);
    const int x = 1217;
    std::cout << 1217;
    for (int i = 0; i < 1217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1217(int& val) {
    const int x = 1217;
    std::cout << 1217;
    for (int i = 0; i < 1217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1218(int& val) {
    func_in_1218(val);
    const int x = 1218;
    std::cout << 1218;
    for (int i = 0; i < 1218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1218(int& val) {
    const int x = 1218;
    std::cout << 1218;
    for (int i = 0; i < 1218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1219(int& val) {
    func_in_1219(val);
    const int x = 1219;
    std::cout << 1219;
    for (int i = 0; i < 1219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1219(int& val) {
    const int x = 1219;
    std::cout << 1219;
    for (int i = 0; i < 1219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1220(int& val) {
    func_in_1220(val);
    const int x = 1220;
    std::cout << 1220;
    for (int i = 0; i < 1220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1220(int& val) {
    const int x = 1220;
    std::cout << 1220;
    for (int i = 0; i < 1220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1221(int& val) {
    func_in_1221(val);
    const int x = 1221;
    std::cout << 1221;
    for (int i = 0; i < 1221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1221(int& val) {
    const int x = 1221;
    std::cout << 1221;
    for (int i = 0; i < 1221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1222(int& val) {
    func_in_1222(val);
    const int x = 1222;
    std::cout << 1222;
    for (int i = 0; i < 1222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1222(int& val) {
    const int x = 1222;
    std::cout << 1222;
    for (int i = 0; i < 1222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1223(int& val) {
    func_in_1223(val);
    const int x = 1223;
    std::cout << 1223;
    for (int i = 0; i < 1223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1223(int& val) {
    const int x = 1223;
    std::cout << 1223;
    for (int i = 0; i < 1223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1224(int& val) {
    func_in_1224(val);
    const int x = 1224;
    std::cout << 1224;
    for (int i = 0; i < 1224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1224(int& val) {
    const int x = 1224;
    std::cout << 1224;
    for (int i = 0; i < 1224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1225(int& val) {
    func_in_1225(val);
    const int x = 1225;
    std::cout << 1225;
    for (int i = 0; i < 1225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1225(int& val) {
    const int x = 1225;
    std::cout << 1225;
    for (int i = 0; i < 1225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1226(int& val) {
    func_in_1226(val);
    const int x = 1226;
    std::cout << 1226;
    for (int i = 0; i < 1226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1226(int& val) {
    const int x = 1226;
    std::cout << 1226;
    for (int i = 0; i < 1226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1227(int& val) {
    func_in_1227(val);
    const int x = 1227;
    std::cout << 1227;
    for (int i = 0; i < 1227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1227(int& val) {
    const int x = 1227;
    std::cout << 1227;
    for (int i = 0; i < 1227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1228(int& val) {
    func_in_1228(val);
    const int x = 1228;
    std::cout << 1228;
    for (int i = 0; i < 1228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1228(int& val) {
    const int x = 1228;
    std::cout << 1228;
    for (int i = 0; i < 1228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1229(int& val) {
    func_in_1229(val);
    const int x = 1229;
    std::cout << 1229;
    for (int i = 0; i < 1229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1229(int& val) {
    const int x = 1229;
    std::cout << 1229;
    for (int i = 0; i < 1229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1230(int& val) {
    func_in_1230(val);
    const int x = 1230;
    std::cout << 1230;
    for (int i = 0; i < 1230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1230(int& val) {
    const int x = 1230;
    std::cout << 1230;
    for (int i = 0; i < 1230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1231(int& val) {
    func_in_1231(val);
    const int x = 1231;
    std::cout << 1231;
    for (int i = 0; i < 1231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1231(int& val) {
    const int x = 1231;
    std::cout << 1231;
    for (int i = 0; i < 1231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1232(int& val) {
    func_in_1232(val);
    const int x = 1232;
    std::cout << 1232;
    for (int i = 0; i < 1232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1232(int& val) {
    const int x = 1232;
    std::cout << 1232;
    for (int i = 0; i < 1232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1233(int& val) {
    func_in_1233(val);
    const int x = 1233;
    std::cout << 1233;
    for (int i = 0; i < 1233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1233(int& val) {
    const int x = 1233;
    std::cout << 1233;
    for (int i = 0; i < 1233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1234(int& val) {
    func_in_1234(val);
    const int x = 1234;
    std::cout << 1234;
    for (int i = 0; i < 1234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1234(int& val) {
    const int x = 1234;
    std::cout << 1234;
    for (int i = 0; i < 1234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1235(int& val) {
    func_in_1235(val);
    const int x = 1235;
    std::cout << 1235;
    for (int i = 0; i < 1235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1235(int& val) {
    const int x = 1235;
    std::cout << 1235;
    for (int i = 0; i < 1235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1236(int& val) {
    func_in_1236(val);
    const int x = 1236;
    std::cout << 1236;
    for (int i = 0; i < 1236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1236(int& val) {
    const int x = 1236;
    std::cout << 1236;
    for (int i = 0; i < 1236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1237(int& val) {
    func_in_1237(val);
    const int x = 1237;
    std::cout << 1237;
    for (int i = 0; i < 1237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1237(int& val) {
    const int x = 1237;
    std::cout << 1237;
    for (int i = 0; i < 1237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1238(int& val) {
    func_in_1238(val);
    const int x = 1238;
    std::cout << 1238;
    for (int i = 0; i < 1238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1238(int& val) {
    const int x = 1238;
    std::cout << 1238;
    for (int i = 0; i < 1238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1239(int& val) {
    func_in_1239(val);
    const int x = 1239;
    std::cout << 1239;
    for (int i = 0; i < 1239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1239(int& val) {
    const int x = 1239;
    std::cout << 1239;
    for (int i = 0; i < 1239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1240(int& val) {
    func_in_1240(val);
    const int x = 1240;
    std::cout << 1240;
    for (int i = 0; i < 1240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1240(int& val) {
    const int x = 1240;
    std::cout << 1240;
    for (int i = 0; i < 1240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1241(int& val) {
    func_in_1241(val);
    const int x = 1241;
    std::cout << 1241;
    for (int i = 0; i < 1241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1241(int& val) {
    const int x = 1241;
    std::cout << 1241;
    for (int i = 0; i < 1241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1242(int& val) {
    func_in_1242(val);
    const int x = 1242;
    std::cout << 1242;
    for (int i = 0; i < 1242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1242(int& val) {
    const int x = 1242;
    std::cout << 1242;
    for (int i = 0; i < 1242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1243(int& val) {
    func_in_1243(val);
    const int x = 1243;
    std::cout << 1243;
    for (int i = 0; i < 1243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1243(int& val) {
    const int x = 1243;
    std::cout << 1243;
    for (int i = 0; i < 1243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1244(int& val) {
    func_in_1244(val);
    const int x = 1244;
    std::cout << 1244;
    for (int i = 0; i < 1244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1244(int& val) {
    const int x = 1244;
    std::cout << 1244;
    for (int i = 0; i < 1244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1245(int& val) {
    func_in_1245(val);
    const int x = 1245;
    std::cout << 1245;
    for (int i = 0; i < 1245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1245(int& val) {
    const int x = 1245;
    std::cout << 1245;
    for (int i = 0; i < 1245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1246(int& val) {
    func_in_1246(val);
    const int x = 1246;
    std::cout << 1246;
    for (int i = 0; i < 1246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1246(int& val) {
    const int x = 1246;
    std::cout << 1246;
    for (int i = 0; i < 1246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1247(int& val) {
    func_in_1247(val);
    const int x = 1247;
    std::cout << 1247;
    for (int i = 0; i < 1247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1247(int& val) {
    const int x = 1247;
    std::cout << 1247;
    for (int i = 0; i < 1247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1248(int& val) {
    func_in_1248(val);
    const int x = 1248;
    std::cout << 1248;
    for (int i = 0; i < 1248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1248(int& val) {
    const int x = 1248;
    std::cout << 1248;
    for (int i = 0; i < 1248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1249(int& val) {
    func_in_1249(val);
    const int x = 1249;
    std::cout << 1249;
    for (int i = 0; i < 1249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1249(int& val) {
    const int x = 1249;
    std::cout << 1249;
    for (int i = 0; i < 1249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1250(int& val) {
    func_in_1250(val);
    const int x = 1250;
    std::cout << 1250;
    for (int i = 0; i < 1250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1250(int& val) {
    const int x = 1250;
    std::cout << 1250;
    for (int i = 0; i < 1250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1251(int& val) {
    func_in_1251(val);
    const int x = 1251;
    std::cout << 1251;
    for (int i = 0; i < 1251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1251(int& val) {
    const int x = 1251;
    std::cout << 1251;
    for (int i = 0; i < 1251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1252(int& val) {
    func_in_1252(val);
    const int x = 1252;
    std::cout << 1252;
    for (int i = 0; i < 1252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1252(int& val) {
    const int x = 1252;
    std::cout << 1252;
    for (int i = 0; i < 1252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1253(int& val) {
    func_in_1253(val);
    const int x = 1253;
    std::cout << 1253;
    for (int i = 0; i < 1253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1253(int& val) {
    const int x = 1253;
    std::cout << 1253;
    for (int i = 0; i < 1253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1254(int& val) {
    func_in_1254(val);
    const int x = 1254;
    std::cout << 1254;
    for (int i = 0; i < 1254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1254(int& val) {
    const int x = 1254;
    std::cout << 1254;
    for (int i = 0; i < 1254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1255(int& val) {
    func_in_1255(val);
    const int x = 1255;
    std::cout << 1255;
    for (int i = 0; i < 1255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1255(int& val) {
    const int x = 1255;
    std::cout << 1255;
    for (int i = 0; i < 1255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1256(int& val) {
    func_in_1256(val);
    const int x = 1256;
    std::cout << 1256;
    for (int i = 0; i < 1256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1256(int& val) {
    const int x = 1256;
    std::cout << 1256;
    for (int i = 0; i < 1256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1257(int& val) {
    func_in_1257(val);
    const int x = 1257;
    std::cout << 1257;
    for (int i = 0; i < 1257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1257(int& val) {
    const int x = 1257;
    std::cout << 1257;
    for (int i = 0; i < 1257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1258(int& val) {
    func_in_1258(val);
    const int x = 1258;
    std::cout << 1258;
    for (int i = 0; i < 1258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1258(int& val) {
    const int x = 1258;
    std::cout << 1258;
    for (int i = 0; i < 1258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1259(int& val) {
    func_in_1259(val);
    const int x = 1259;
    std::cout << 1259;
    for (int i = 0; i < 1259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1259(int& val) {
    const int x = 1259;
    std::cout << 1259;
    for (int i = 0; i < 1259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1260(int& val) {
    func_in_1260(val);
    const int x = 1260;
    std::cout << 1260;
    for (int i = 0; i < 1260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1260(int& val) {
    const int x = 1260;
    std::cout << 1260;
    for (int i = 0; i < 1260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1261(int& val) {
    func_in_1261(val);
    const int x = 1261;
    std::cout << 1261;
    for (int i = 0; i < 1261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1261(int& val) {
    const int x = 1261;
    std::cout << 1261;
    for (int i = 0; i < 1261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1262(int& val) {
    func_in_1262(val);
    const int x = 1262;
    std::cout << 1262;
    for (int i = 0; i < 1262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1262(int& val) {
    const int x = 1262;
    std::cout << 1262;
    for (int i = 0; i < 1262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1263(int& val) {
    func_in_1263(val);
    const int x = 1263;
    std::cout << 1263;
    for (int i = 0; i < 1263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1263(int& val) {
    const int x = 1263;
    std::cout << 1263;
    for (int i = 0; i < 1263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1264(int& val) {
    func_in_1264(val);
    const int x = 1264;
    std::cout << 1264;
    for (int i = 0; i < 1264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1264(int& val) {
    const int x = 1264;
    std::cout << 1264;
    for (int i = 0; i < 1264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1265(int& val) {
    func_in_1265(val);
    const int x = 1265;
    std::cout << 1265;
    for (int i = 0; i < 1265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1265(int& val) {
    const int x = 1265;
    std::cout << 1265;
    for (int i = 0; i < 1265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1266(int& val) {
    func_in_1266(val);
    const int x = 1266;
    std::cout << 1266;
    for (int i = 0; i < 1266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1266(int& val) {
    const int x = 1266;
    std::cout << 1266;
    for (int i = 0; i < 1266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1267(int& val) {
    func_in_1267(val);
    const int x = 1267;
    std::cout << 1267;
    for (int i = 0; i < 1267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1267(int& val) {
    const int x = 1267;
    std::cout << 1267;
    for (int i = 0; i < 1267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1268(int& val) {
    func_in_1268(val);
    const int x = 1268;
    std::cout << 1268;
    for (int i = 0; i < 1268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1268(int& val) {
    const int x = 1268;
    std::cout << 1268;
    for (int i = 0; i < 1268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1269(int& val) {
    func_in_1269(val);
    const int x = 1269;
    std::cout << 1269;
    for (int i = 0; i < 1269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1269(int& val) {
    const int x = 1269;
    std::cout << 1269;
    for (int i = 0; i < 1269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1270(int& val) {
    func_in_1270(val);
    const int x = 1270;
    std::cout << 1270;
    for (int i = 0; i < 1270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1270(int& val) {
    const int x = 1270;
    std::cout << 1270;
    for (int i = 0; i < 1270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1271(int& val) {
    func_in_1271(val);
    const int x = 1271;
    std::cout << 1271;
    for (int i = 0; i < 1271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1271(int& val) {
    const int x = 1271;
    std::cout << 1271;
    for (int i = 0; i < 1271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1272(int& val) {
    func_in_1272(val);
    const int x = 1272;
    std::cout << 1272;
    for (int i = 0; i < 1272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1272(int& val) {
    const int x = 1272;
    std::cout << 1272;
    for (int i = 0; i < 1272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1273(int& val) {
    func_in_1273(val);
    const int x = 1273;
    std::cout << 1273;
    for (int i = 0; i < 1273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1273(int& val) {
    const int x = 1273;
    std::cout << 1273;
    for (int i = 0; i < 1273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1274(int& val) {
    func_in_1274(val);
    const int x = 1274;
    std::cout << 1274;
    for (int i = 0; i < 1274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1274(int& val) {
    const int x = 1274;
    std::cout << 1274;
    for (int i = 0; i < 1274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1275(int& val) {
    func_in_1275(val);
    const int x = 1275;
    std::cout << 1275;
    for (int i = 0; i < 1275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1275(int& val) {
    const int x = 1275;
    std::cout << 1275;
    for (int i = 0; i < 1275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1276(int& val) {
    func_in_1276(val);
    const int x = 1276;
    std::cout << 1276;
    for (int i = 0; i < 1276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1276(int& val) {
    const int x = 1276;
    std::cout << 1276;
    for (int i = 0; i < 1276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1277(int& val) {
    func_in_1277(val);
    const int x = 1277;
    std::cout << 1277;
    for (int i = 0; i < 1277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1277(int& val) {
    const int x = 1277;
    std::cout << 1277;
    for (int i = 0; i < 1277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1278(int& val) {
    func_in_1278(val);
    const int x = 1278;
    std::cout << 1278;
    for (int i = 0; i < 1278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1278(int& val) {
    const int x = 1278;
    std::cout << 1278;
    for (int i = 0; i < 1278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1279(int& val) {
    func_in_1279(val);
    const int x = 1279;
    std::cout << 1279;
    for (int i = 0; i < 1279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1279(int& val) {
    const int x = 1279;
    std::cout << 1279;
    for (int i = 0; i < 1279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1280(int& val) {
    func_in_1280(val);
    const int x = 1280;
    std::cout << 1280;
    for (int i = 0; i < 1280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1280(int& val) {
    const int x = 1280;
    std::cout << 1280;
    for (int i = 0; i < 1280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1281(int& val) {
    func_in_1281(val);
    const int x = 1281;
    std::cout << 1281;
    for (int i = 0; i < 1281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1281(int& val) {
    const int x = 1281;
    std::cout << 1281;
    for (int i = 0; i < 1281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1282(int& val) {
    func_in_1282(val);
    const int x = 1282;
    std::cout << 1282;
    for (int i = 0; i < 1282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1282(int& val) {
    const int x = 1282;
    std::cout << 1282;
    for (int i = 0; i < 1282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1283(int& val) {
    func_in_1283(val);
    const int x = 1283;
    std::cout << 1283;
    for (int i = 0; i < 1283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1283(int& val) {
    const int x = 1283;
    std::cout << 1283;
    for (int i = 0; i < 1283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1284(int& val) {
    func_in_1284(val);
    const int x = 1284;
    std::cout << 1284;
    for (int i = 0; i < 1284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1284(int& val) {
    const int x = 1284;
    std::cout << 1284;
    for (int i = 0; i < 1284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1285(int& val) {
    func_in_1285(val);
    const int x = 1285;
    std::cout << 1285;
    for (int i = 0; i < 1285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1285(int& val) {
    const int x = 1285;
    std::cout << 1285;
    for (int i = 0; i < 1285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1286(int& val) {
    func_in_1286(val);
    const int x = 1286;
    std::cout << 1286;
    for (int i = 0; i < 1286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1286(int& val) {
    const int x = 1286;
    std::cout << 1286;
    for (int i = 0; i < 1286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1287(int& val) {
    func_in_1287(val);
    const int x = 1287;
    std::cout << 1287;
    for (int i = 0; i < 1287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1287(int& val) {
    const int x = 1287;
    std::cout << 1287;
    for (int i = 0; i < 1287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1288(int& val) {
    func_in_1288(val);
    const int x = 1288;
    std::cout << 1288;
    for (int i = 0; i < 1288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1288(int& val) {
    const int x = 1288;
    std::cout << 1288;
    for (int i = 0; i < 1288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1289(int& val) {
    func_in_1289(val);
    const int x = 1289;
    std::cout << 1289;
    for (int i = 0; i < 1289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1289(int& val) {
    const int x = 1289;
    std::cout << 1289;
    for (int i = 0; i < 1289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1290(int& val) {
    func_in_1290(val);
    const int x = 1290;
    std::cout << 1290;
    for (int i = 0; i < 1290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1290(int& val) {
    const int x = 1290;
    std::cout << 1290;
    for (int i = 0; i < 1290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1291(int& val) {
    func_in_1291(val);
    const int x = 1291;
    std::cout << 1291;
    for (int i = 0; i < 1291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1291(int& val) {
    const int x = 1291;
    std::cout << 1291;
    for (int i = 0; i < 1291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1292(int& val) {
    func_in_1292(val);
    const int x = 1292;
    std::cout << 1292;
    for (int i = 0; i < 1292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1292(int& val) {
    const int x = 1292;
    std::cout << 1292;
    for (int i = 0; i < 1292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1293(int& val) {
    func_in_1293(val);
    const int x = 1293;
    std::cout << 1293;
    for (int i = 0; i < 1293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1293(int& val) {
    const int x = 1293;
    std::cout << 1293;
    for (int i = 0; i < 1293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1294(int& val) {
    func_in_1294(val);
    const int x = 1294;
    std::cout << 1294;
    for (int i = 0; i < 1294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1294(int& val) {
    const int x = 1294;
    std::cout << 1294;
    for (int i = 0; i < 1294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1295(int& val) {
    func_in_1295(val);
    const int x = 1295;
    std::cout << 1295;
    for (int i = 0; i < 1295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1295(int& val) {
    const int x = 1295;
    std::cout << 1295;
    for (int i = 0; i < 1295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1296(int& val) {
    func_in_1296(val);
    const int x = 1296;
    std::cout << 1296;
    for (int i = 0; i < 1296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1296(int& val) {
    const int x = 1296;
    std::cout << 1296;
    for (int i = 0; i < 1296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1297(int& val) {
    func_in_1297(val);
    const int x = 1297;
    std::cout << 1297;
    for (int i = 0; i < 1297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1297(int& val) {
    const int x = 1297;
    std::cout << 1297;
    for (int i = 0; i < 1297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1298(int& val) {
    func_in_1298(val);
    const int x = 1298;
    std::cout << 1298;
    for (int i = 0; i < 1298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1298(int& val) {
    const int x = 1298;
    std::cout << 1298;
    for (int i = 0; i < 1298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1299(int& val) {
    func_in_1299(val);
    const int x = 1299;
    std::cout << 1299;
    for (int i = 0; i < 1299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1299(int& val) {
    const int x = 1299;
    std::cout << 1299;
    for (int i = 0; i < 1299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1300(int& val) {
    func_in_1300(val);
    const int x = 1300;
    std::cout << 1300;
    for (int i = 0; i < 1300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1300(int& val) {
    const int x = 1300;
    std::cout << 1300;
    for (int i = 0; i < 1300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1301(int& val) {
    func_in_1301(val);
    const int x = 1301;
    std::cout << 1301;
    for (int i = 0; i < 1301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1301(int& val) {
    const int x = 1301;
    std::cout << 1301;
    for (int i = 0; i < 1301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1302(int& val) {
    func_in_1302(val);
    const int x = 1302;
    std::cout << 1302;
    for (int i = 0; i < 1302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1302(int& val) {
    const int x = 1302;
    std::cout << 1302;
    for (int i = 0; i < 1302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1303(int& val) {
    func_in_1303(val);
    const int x = 1303;
    std::cout << 1303;
    for (int i = 0; i < 1303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1303(int& val) {
    const int x = 1303;
    std::cout << 1303;
    for (int i = 0; i < 1303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1304(int& val) {
    func_in_1304(val);
    const int x = 1304;
    std::cout << 1304;
    for (int i = 0; i < 1304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1304(int& val) {
    const int x = 1304;
    std::cout << 1304;
    for (int i = 0; i < 1304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1305(int& val) {
    func_in_1305(val);
    const int x = 1305;
    std::cout << 1305;
    for (int i = 0; i < 1305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1305(int& val) {
    const int x = 1305;
    std::cout << 1305;
    for (int i = 0; i < 1305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1306(int& val) {
    func_in_1306(val);
    const int x = 1306;
    std::cout << 1306;
    for (int i = 0; i < 1306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1306(int& val) {
    const int x = 1306;
    std::cout << 1306;
    for (int i = 0; i < 1306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1307(int& val) {
    func_in_1307(val);
    const int x = 1307;
    std::cout << 1307;
    for (int i = 0; i < 1307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1307(int& val) {
    const int x = 1307;
    std::cout << 1307;
    for (int i = 0; i < 1307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1308(int& val) {
    func_in_1308(val);
    const int x = 1308;
    std::cout << 1308;
    for (int i = 0; i < 1308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1308(int& val) {
    const int x = 1308;
    std::cout << 1308;
    for (int i = 0; i < 1308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1309(int& val) {
    func_in_1309(val);
    const int x = 1309;
    std::cout << 1309;
    for (int i = 0; i < 1309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1309(int& val) {
    const int x = 1309;
    std::cout << 1309;
    for (int i = 0; i < 1309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1310(int& val) {
    func_in_1310(val);
    const int x = 1310;
    std::cout << 1310;
    for (int i = 0; i < 1310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1310(int& val) {
    const int x = 1310;
    std::cout << 1310;
    for (int i = 0; i < 1310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1311(int& val) {
    func_in_1311(val);
    const int x = 1311;
    std::cout << 1311;
    for (int i = 0; i < 1311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1311(int& val) {
    const int x = 1311;
    std::cout << 1311;
    for (int i = 0; i < 1311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1312(int& val) {
    func_in_1312(val);
    const int x = 1312;
    std::cout << 1312;
    for (int i = 0; i < 1312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1312(int& val) {
    const int x = 1312;
    std::cout << 1312;
    for (int i = 0; i < 1312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1313(int& val) {
    func_in_1313(val);
    const int x = 1313;
    std::cout << 1313;
    for (int i = 0; i < 1313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1313(int& val) {
    const int x = 1313;
    std::cout << 1313;
    for (int i = 0; i < 1313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1314(int& val) {
    func_in_1314(val);
    const int x = 1314;
    std::cout << 1314;
    for (int i = 0; i < 1314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1314(int& val) {
    const int x = 1314;
    std::cout << 1314;
    for (int i = 0; i < 1314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1315(int& val) {
    func_in_1315(val);
    const int x = 1315;
    std::cout << 1315;
    for (int i = 0; i < 1315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1315(int& val) {
    const int x = 1315;
    std::cout << 1315;
    for (int i = 0; i < 1315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1316(int& val) {
    func_in_1316(val);
    const int x = 1316;
    std::cout << 1316;
    for (int i = 0; i < 1316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1316(int& val) {
    const int x = 1316;
    std::cout << 1316;
    for (int i = 0; i < 1316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1317(int& val) {
    func_in_1317(val);
    const int x = 1317;
    std::cout << 1317;
    for (int i = 0; i < 1317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1317(int& val) {
    const int x = 1317;
    std::cout << 1317;
    for (int i = 0; i < 1317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1318(int& val) {
    func_in_1318(val);
    const int x = 1318;
    std::cout << 1318;
    for (int i = 0; i < 1318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1318(int& val) {
    const int x = 1318;
    std::cout << 1318;
    for (int i = 0; i < 1318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1319(int& val) {
    func_in_1319(val);
    const int x = 1319;
    std::cout << 1319;
    for (int i = 0; i < 1319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1319(int& val) {
    const int x = 1319;
    std::cout << 1319;
    for (int i = 0; i < 1319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1320(int& val) {
    func_in_1320(val);
    const int x = 1320;
    std::cout << 1320;
    for (int i = 0; i < 1320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1320(int& val) {
    const int x = 1320;
    std::cout << 1320;
    for (int i = 0; i < 1320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1321(int& val) {
    func_in_1321(val);
    const int x = 1321;
    std::cout << 1321;
    for (int i = 0; i < 1321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1321(int& val) {
    const int x = 1321;
    std::cout << 1321;
    for (int i = 0; i < 1321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1322(int& val) {
    func_in_1322(val);
    const int x = 1322;
    std::cout << 1322;
    for (int i = 0; i < 1322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1322(int& val) {
    const int x = 1322;
    std::cout << 1322;
    for (int i = 0; i < 1322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1323(int& val) {
    func_in_1323(val);
    const int x = 1323;
    std::cout << 1323;
    for (int i = 0; i < 1323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1323(int& val) {
    const int x = 1323;
    std::cout << 1323;
    for (int i = 0; i < 1323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1324(int& val) {
    func_in_1324(val);
    const int x = 1324;
    std::cout << 1324;
    for (int i = 0; i < 1324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1324(int& val) {
    const int x = 1324;
    std::cout << 1324;
    for (int i = 0; i < 1324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1325(int& val) {
    func_in_1325(val);
    const int x = 1325;
    std::cout << 1325;
    for (int i = 0; i < 1325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1325(int& val) {
    const int x = 1325;
    std::cout << 1325;
    for (int i = 0; i < 1325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1326(int& val) {
    func_in_1326(val);
    const int x = 1326;
    std::cout << 1326;
    for (int i = 0; i < 1326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1326(int& val) {
    const int x = 1326;
    std::cout << 1326;
    for (int i = 0; i < 1326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1327(int& val) {
    func_in_1327(val);
    const int x = 1327;
    std::cout << 1327;
    for (int i = 0; i < 1327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1327(int& val) {
    const int x = 1327;
    std::cout << 1327;
    for (int i = 0; i < 1327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1328(int& val) {
    func_in_1328(val);
    const int x = 1328;
    std::cout << 1328;
    for (int i = 0; i < 1328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1328(int& val) {
    const int x = 1328;
    std::cout << 1328;
    for (int i = 0; i < 1328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1329(int& val) {
    func_in_1329(val);
    const int x = 1329;
    std::cout << 1329;
    for (int i = 0; i < 1329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1329(int& val) {
    const int x = 1329;
    std::cout << 1329;
    for (int i = 0; i < 1329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1330(int& val) {
    func_in_1330(val);
    const int x = 1330;
    std::cout << 1330;
    for (int i = 0; i < 1330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1330(int& val) {
    const int x = 1330;
    std::cout << 1330;
    for (int i = 0; i < 1330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1331(int& val) {
    func_in_1331(val);
    const int x = 1331;
    std::cout << 1331;
    for (int i = 0; i < 1331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1331(int& val) {
    const int x = 1331;
    std::cout << 1331;
    for (int i = 0; i < 1331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1332(int& val) {
    func_in_1332(val);
    const int x = 1332;
    std::cout << 1332;
    for (int i = 0; i < 1332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1332(int& val) {
    const int x = 1332;
    std::cout << 1332;
    for (int i = 0; i < 1332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1333(int& val) {
    func_in_1333(val);
    const int x = 1333;
    std::cout << 1333;
    for (int i = 0; i < 1333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1333(int& val) {
    const int x = 1333;
    std::cout << 1333;
    for (int i = 0; i < 1333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1334(int& val) {
    func_in_1334(val);
    const int x = 1334;
    std::cout << 1334;
    for (int i = 0; i < 1334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1334(int& val) {
    const int x = 1334;
    std::cout << 1334;
    for (int i = 0; i < 1334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1335(int& val) {
    func_in_1335(val);
    const int x = 1335;
    std::cout << 1335;
    for (int i = 0; i < 1335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1335(int& val) {
    const int x = 1335;
    std::cout << 1335;
    for (int i = 0; i < 1335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1336(int& val) {
    func_in_1336(val);
    const int x = 1336;
    std::cout << 1336;
    for (int i = 0; i < 1336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1336(int& val) {
    const int x = 1336;
    std::cout << 1336;
    for (int i = 0; i < 1336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1337(int& val) {
    func_in_1337(val);
    const int x = 1337;
    std::cout << 1337;
    for (int i = 0; i < 1337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1337(int& val) {
    const int x = 1337;
    std::cout << 1337;
    for (int i = 0; i < 1337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1338(int& val) {
    func_in_1338(val);
    const int x = 1338;
    std::cout << 1338;
    for (int i = 0; i < 1338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1338(int& val) {
    const int x = 1338;
    std::cout << 1338;
    for (int i = 0; i < 1338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1339(int& val) {
    func_in_1339(val);
    const int x = 1339;
    std::cout << 1339;
    for (int i = 0; i < 1339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1339(int& val) {
    const int x = 1339;
    std::cout << 1339;
    for (int i = 0; i < 1339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1340(int& val) {
    func_in_1340(val);
    const int x = 1340;
    std::cout << 1340;
    for (int i = 0; i < 1340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1340(int& val) {
    const int x = 1340;
    std::cout << 1340;
    for (int i = 0; i < 1340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1341(int& val) {
    func_in_1341(val);
    const int x = 1341;
    std::cout << 1341;
    for (int i = 0; i < 1341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1341(int& val) {
    const int x = 1341;
    std::cout << 1341;
    for (int i = 0; i < 1341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1342(int& val) {
    func_in_1342(val);
    const int x = 1342;
    std::cout << 1342;
    for (int i = 0; i < 1342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1342(int& val) {
    const int x = 1342;
    std::cout << 1342;
    for (int i = 0; i < 1342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1343(int& val) {
    func_in_1343(val);
    const int x = 1343;
    std::cout << 1343;
    for (int i = 0; i < 1343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1343(int& val) {
    const int x = 1343;
    std::cout << 1343;
    for (int i = 0; i < 1343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1344(int& val) {
    func_in_1344(val);
    const int x = 1344;
    std::cout << 1344;
    for (int i = 0; i < 1344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1344(int& val) {
    const int x = 1344;
    std::cout << 1344;
    for (int i = 0; i < 1344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1345(int& val) {
    func_in_1345(val);
    const int x = 1345;
    std::cout << 1345;
    for (int i = 0; i < 1345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1345(int& val) {
    const int x = 1345;
    std::cout << 1345;
    for (int i = 0; i < 1345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1346(int& val) {
    func_in_1346(val);
    const int x = 1346;
    std::cout << 1346;
    for (int i = 0; i < 1346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1346(int& val) {
    const int x = 1346;
    std::cout << 1346;
    for (int i = 0; i < 1346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1347(int& val) {
    func_in_1347(val);
    const int x = 1347;
    std::cout << 1347;
    for (int i = 0; i < 1347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1347(int& val) {
    const int x = 1347;
    std::cout << 1347;
    for (int i = 0; i < 1347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1348(int& val) {
    func_in_1348(val);
    const int x = 1348;
    std::cout << 1348;
    for (int i = 0; i < 1348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1348(int& val) {
    const int x = 1348;
    std::cout << 1348;
    for (int i = 0; i < 1348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1349(int& val) {
    func_in_1349(val);
    const int x = 1349;
    std::cout << 1349;
    for (int i = 0; i < 1349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1349(int& val) {
    const int x = 1349;
    std::cout << 1349;
    for (int i = 0; i < 1349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1350(int& val) {
    func_in_1350(val);
    const int x = 1350;
    std::cout << 1350;
    for (int i = 0; i < 1350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1350(int& val) {
    const int x = 1350;
    std::cout << 1350;
    for (int i = 0; i < 1350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1351(int& val) {
    func_in_1351(val);
    const int x = 1351;
    std::cout << 1351;
    for (int i = 0; i < 1351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1351(int& val) {
    const int x = 1351;
    std::cout << 1351;
    for (int i = 0; i < 1351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1352(int& val) {
    func_in_1352(val);
    const int x = 1352;
    std::cout << 1352;
    for (int i = 0; i < 1352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1352(int& val) {
    const int x = 1352;
    std::cout << 1352;
    for (int i = 0; i < 1352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1353(int& val) {
    func_in_1353(val);
    const int x = 1353;
    std::cout << 1353;
    for (int i = 0; i < 1353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1353(int& val) {
    const int x = 1353;
    std::cout << 1353;
    for (int i = 0; i < 1353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1354(int& val) {
    func_in_1354(val);
    const int x = 1354;
    std::cout << 1354;
    for (int i = 0; i < 1354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1354(int& val) {
    const int x = 1354;
    std::cout << 1354;
    for (int i = 0; i < 1354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1355(int& val) {
    func_in_1355(val);
    const int x = 1355;
    std::cout << 1355;
    for (int i = 0; i < 1355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1355(int& val) {
    const int x = 1355;
    std::cout << 1355;
    for (int i = 0; i < 1355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1356(int& val) {
    func_in_1356(val);
    const int x = 1356;
    std::cout << 1356;
    for (int i = 0; i < 1356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1356(int& val) {
    const int x = 1356;
    std::cout << 1356;
    for (int i = 0; i < 1356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1357(int& val) {
    func_in_1357(val);
    const int x = 1357;
    std::cout << 1357;
    for (int i = 0; i < 1357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1357(int& val) {
    const int x = 1357;
    std::cout << 1357;
    for (int i = 0; i < 1357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1358(int& val) {
    func_in_1358(val);
    const int x = 1358;
    std::cout << 1358;
    for (int i = 0; i < 1358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1358(int& val) {
    const int x = 1358;
    std::cout << 1358;
    for (int i = 0; i < 1358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1359(int& val) {
    func_in_1359(val);
    const int x = 1359;
    std::cout << 1359;
    for (int i = 0; i < 1359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1359(int& val) {
    const int x = 1359;
    std::cout << 1359;
    for (int i = 0; i < 1359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1360(int& val) {
    func_in_1360(val);
    const int x = 1360;
    std::cout << 1360;
    for (int i = 0; i < 1360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1360(int& val) {
    const int x = 1360;
    std::cout << 1360;
    for (int i = 0; i < 1360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1361(int& val) {
    func_in_1361(val);
    const int x = 1361;
    std::cout << 1361;
    for (int i = 0; i < 1361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1361(int& val) {
    const int x = 1361;
    std::cout << 1361;
    for (int i = 0; i < 1361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1362(int& val) {
    func_in_1362(val);
    const int x = 1362;
    std::cout << 1362;
    for (int i = 0; i < 1362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1362(int& val) {
    const int x = 1362;
    std::cout << 1362;
    for (int i = 0; i < 1362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1363(int& val) {
    func_in_1363(val);
    const int x = 1363;
    std::cout << 1363;
    for (int i = 0; i < 1363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1363(int& val) {
    const int x = 1363;
    std::cout << 1363;
    for (int i = 0; i < 1363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1364(int& val) {
    func_in_1364(val);
    const int x = 1364;
    std::cout << 1364;
    for (int i = 0; i < 1364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1364(int& val) {
    const int x = 1364;
    std::cout << 1364;
    for (int i = 0; i < 1364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1365(int& val) {
    func_in_1365(val);
    const int x = 1365;
    std::cout << 1365;
    for (int i = 0; i < 1365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1365(int& val) {
    const int x = 1365;
    std::cout << 1365;
    for (int i = 0; i < 1365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1366(int& val) {
    func_in_1366(val);
    const int x = 1366;
    std::cout << 1366;
    for (int i = 0; i < 1366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1366(int& val) {
    const int x = 1366;
    std::cout << 1366;
    for (int i = 0; i < 1366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1367(int& val) {
    func_in_1367(val);
    const int x = 1367;
    std::cout << 1367;
    for (int i = 0; i < 1367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1367(int& val) {
    const int x = 1367;
    std::cout << 1367;
    for (int i = 0; i < 1367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1368(int& val) {
    func_in_1368(val);
    const int x = 1368;
    std::cout << 1368;
    for (int i = 0; i < 1368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1368(int& val) {
    const int x = 1368;
    std::cout << 1368;
    for (int i = 0; i < 1368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1369(int& val) {
    func_in_1369(val);
    const int x = 1369;
    std::cout << 1369;
    for (int i = 0; i < 1369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1369(int& val) {
    const int x = 1369;
    std::cout << 1369;
    for (int i = 0; i < 1369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1370(int& val) {
    func_in_1370(val);
    const int x = 1370;
    std::cout << 1370;
    for (int i = 0; i < 1370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1370(int& val) {
    const int x = 1370;
    std::cout << 1370;
    for (int i = 0; i < 1370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1371(int& val) {
    func_in_1371(val);
    const int x = 1371;
    std::cout << 1371;
    for (int i = 0; i < 1371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1371(int& val) {
    const int x = 1371;
    std::cout << 1371;
    for (int i = 0; i < 1371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1372(int& val) {
    func_in_1372(val);
    const int x = 1372;
    std::cout << 1372;
    for (int i = 0; i < 1372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1372(int& val) {
    const int x = 1372;
    std::cout << 1372;
    for (int i = 0; i < 1372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1373(int& val) {
    func_in_1373(val);
    const int x = 1373;
    std::cout << 1373;
    for (int i = 0; i < 1373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1373(int& val) {
    const int x = 1373;
    std::cout << 1373;
    for (int i = 0; i < 1373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1374(int& val) {
    func_in_1374(val);
    const int x = 1374;
    std::cout << 1374;
    for (int i = 0; i < 1374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1374(int& val) {
    const int x = 1374;
    std::cout << 1374;
    for (int i = 0; i < 1374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1375(int& val) {
    func_in_1375(val);
    const int x = 1375;
    std::cout << 1375;
    for (int i = 0; i < 1375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1375(int& val) {
    const int x = 1375;
    std::cout << 1375;
    for (int i = 0; i < 1375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1376(int& val) {
    func_in_1376(val);
    const int x = 1376;
    std::cout << 1376;
    for (int i = 0; i < 1376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1376(int& val) {
    const int x = 1376;
    std::cout << 1376;
    for (int i = 0; i < 1376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1377(int& val) {
    func_in_1377(val);
    const int x = 1377;
    std::cout << 1377;
    for (int i = 0; i < 1377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1377(int& val) {
    const int x = 1377;
    std::cout << 1377;
    for (int i = 0; i < 1377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1378(int& val) {
    func_in_1378(val);
    const int x = 1378;
    std::cout << 1378;
    for (int i = 0; i < 1378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1378(int& val) {
    const int x = 1378;
    std::cout << 1378;
    for (int i = 0; i < 1378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1379(int& val) {
    func_in_1379(val);
    const int x = 1379;
    std::cout << 1379;
    for (int i = 0; i < 1379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1379(int& val) {
    const int x = 1379;
    std::cout << 1379;
    for (int i = 0; i < 1379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1380(int& val) {
    func_in_1380(val);
    const int x = 1380;
    std::cout << 1380;
    for (int i = 0; i < 1380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1380(int& val) {
    const int x = 1380;
    std::cout << 1380;
    for (int i = 0; i < 1380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1381(int& val) {
    func_in_1381(val);
    const int x = 1381;
    std::cout << 1381;
    for (int i = 0; i < 1381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1381(int& val) {
    const int x = 1381;
    std::cout << 1381;
    for (int i = 0; i < 1381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1382(int& val) {
    func_in_1382(val);
    const int x = 1382;
    std::cout << 1382;
    for (int i = 0; i < 1382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1382(int& val) {
    const int x = 1382;
    std::cout << 1382;
    for (int i = 0; i < 1382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1383(int& val) {
    func_in_1383(val);
    const int x = 1383;
    std::cout << 1383;
    for (int i = 0; i < 1383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1383(int& val) {
    const int x = 1383;
    std::cout << 1383;
    for (int i = 0; i < 1383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1384(int& val) {
    func_in_1384(val);
    const int x = 1384;
    std::cout << 1384;
    for (int i = 0; i < 1384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1384(int& val) {
    const int x = 1384;
    std::cout << 1384;
    for (int i = 0; i < 1384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1385(int& val) {
    func_in_1385(val);
    const int x = 1385;
    std::cout << 1385;
    for (int i = 0; i < 1385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1385(int& val) {
    const int x = 1385;
    std::cout << 1385;
    for (int i = 0; i < 1385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1386(int& val) {
    func_in_1386(val);
    const int x = 1386;
    std::cout << 1386;
    for (int i = 0; i < 1386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1386(int& val) {
    const int x = 1386;
    std::cout << 1386;
    for (int i = 0; i < 1386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1387(int& val) {
    func_in_1387(val);
    const int x = 1387;
    std::cout << 1387;
    for (int i = 0; i < 1387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1387(int& val) {
    const int x = 1387;
    std::cout << 1387;
    for (int i = 0; i < 1387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1388(int& val) {
    func_in_1388(val);
    const int x = 1388;
    std::cout << 1388;
    for (int i = 0; i < 1388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1388(int& val) {
    const int x = 1388;
    std::cout << 1388;
    for (int i = 0; i < 1388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1389(int& val) {
    func_in_1389(val);
    const int x = 1389;
    std::cout << 1389;
    for (int i = 0; i < 1389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1389(int& val) {
    const int x = 1389;
    std::cout << 1389;
    for (int i = 0; i < 1389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1390(int& val) {
    func_in_1390(val);
    const int x = 1390;
    std::cout << 1390;
    for (int i = 0; i < 1390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1390(int& val) {
    const int x = 1390;
    std::cout << 1390;
    for (int i = 0; i < 1390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1391(int& val) {
    func_in_1391(val);
    const int x = 1391;
    std::cout << 1391;
    for (int i = 0; i < 1391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1391(int& val) {
    const int x = 1391;
    std::cout << 1391;
    for (int i = 0; i < 1391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1392(int& val) {
    func_in_1392(val);
    const int x = 1392;
    std::cout << 1392;
    for (int i = 0; i < 1392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1392(int& val) {
    const int x = 1392;
    std::cout << 1392;
    for (int i = 0; i < 1392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1393(int& val) {
    func_in_1393(val);
    const int x = 1393;
    std::cout << 1393;
    for (int i = 0; i < 1393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1393(int& val) {
    const int x = 1393;
    std::cout << 1393;
    for (int i = 0; i < 1393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1394(int& val) {
    func_in_1394(val);
    const int x = 1394;
    std::cout << 1394;
    for (int i = 0; i < 1394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1394(int& val) {
    const int x = 1394;
    std::cout << 1394;
    for (int i = 0; i < 1394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1395(int& val) {
    func_in_1395(val);
    const int x = 1395;
    std::cout << 1395;
    for (int i = 0; i < 1395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1395(int& val) {
    const int x = 1395;
    std::cout << 1395;
    for (int i = 0; i < 1395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1396(int& val) {
    func_in_1396(val);
    const int x = 1396;
    std::cout << 1396;
    for (int i = 0; i < 1396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1396(int& val) {
    const int x = 1396;
    std::cout << 1396;
    for (int i = 0; i < 1396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1397(int& val) {
    func_in_1397(val);
    const int x = 1397;
    std::cout << 1397;
    for (int i = 0; i < 1397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1397(int& val) {
    const int x = 1397;
    std::cout << 1397;
    for (int i = 0; i < 1397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1398(int& val) {
    func_in_1398(val);
    const int x = 1398;
    std::cout << 1398;
    for (int i = 0; i < 1398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1398(int& val) {
    const int x = 1398;
    std::cout << 1398;
    for (int i = 0; i < 1398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1399(int& val) {
    func_in_1399(val);
    const int x = 1399;
    std::cout << 1399;
    for (int i = 0; i < 1399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1399(int& val) {
    const int x = 1399;
    std::cout << 1399;
    for (int i = 0; i < 1399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1400(int& val) {
    func_in_1400(val);
    const int x = 1400;
    std::cout << 1400;
    for (int i = 0; i < 1400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1400(int& val) {
    const int x = 1400;
    std::cout << 1400;
    for (int i = 0; i < 1400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1401(int& val) {
    func_in_1401(val);
    const int x = 1401;
    std::cout << 1401;
    for (int i = 0; i < 1401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1401(int& val) {
    const int x = 1401;
    std::cout << 1401;
    for (int i = 0; i < 1401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1402(int& val) {
    func_in_1402(val);
    const int x = 1402;
    std::cout << 1402;
    for (int i = 0; i < 1402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1402(int& val) {
    const int x = 1402;
    std::cout << 1402;
    for (int i = 0; i < 1402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1403(int& val) {
    func_in_1403(val);
    const int x = 1403;
    std::cout << 1403;
    for (int i = 0; i < 1403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1403(int& val) {
    const int x = 1403;
    std::cout << 1403;
    for (int i = 0; i < 1403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1404(int& val) {
    func_in_1404(val);
    const int x = 1404;
    std::cout << 1404;
    for (int i = 0; i < 1404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1404(int& val) {
    const int x = 1404;
    std::cout << 1404;
    for (int i = 0; i < 1404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1405(int& val) {
    func_in_1405(val);
    const int x = 1405;
    std::cout << 1405;
    for (int i = 0; i < 1405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1405(int& val) {
    const int x = 1405;
    std::cout << 1405;
    for (int i = 0; i < 1405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1406(int& val) {
    func_in_1406(val);
    const int x = 1406;
    std::cout << 1406;
    for (int i = 0; i < 1406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1406(int& val) {
    const int x = 1406;
    std::cout << 1406;
    for (int i = 0; i < 1406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1407(int& val) {
    func_in_1407(val);
    const int x = 1407;
    std::cout << 1407;
    for (int i = 0; i < 1407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1407(int& val) {
    const int x = 1407;
    std::cout << 1407;
    for (int i = 0; i < 1407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1408(int& val) {
    func_in_1408(val);
    const int x = 1408;
    std::cout << 1408;
    for (int i = 0; i < 1408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1408(int& val) {
    const int x = 1408;
    std::cout << 1408;
    for (int i = 0; i < 1408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1409(int& val) {
    func_in_1409(val);
    const int x = 1409;
    std::cout << 1409;
    for (int i = 0; i < 1409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1409(int& val) {
    const int x = 1409;
    std::cout << 1409;
    for (int i = 0; i < 1409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1410(int& val) {
    func_in_1410(val);
    const int x = 1410;
    std::cout << 1410;
    for (int i = 0; i < 1410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1410(int& val) {
    const int x = 1410;
    std::cout << 1410;
    for (int i = 0; i < 1410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1411(int& val) {
    func_in_1411(val);
    const int x = 1411;
    std::cout << 1411;
    for (int i = 0; i < 1411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1411(int& val) {
    const int x = 1411;
    std::cout << 1411;
    for (int i = 0; i < 1411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1412(int& val) {
    func_in_1412(val);
    const int x = 1412;
    std::cout << 1412;
    for (int i = 0; i < 1412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1412(int& val) {
    const int x = 1412;
    std::cout << 1412;
    for (int i = 0; i < 1412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1413(int& val) {
    func_in_1413(val);
    const int x = 1413;
    std::cout << 1413;
    for (int i = 0; i < 1413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1413(int& val) {
    const int x = 1413;
    std::cout << 1413;
    for (int i = 0; i < 1413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1414(int& val) {
    func_in_1414(val);
    const int x = 1414;
    std::cout << 1414;
    for (int i = 0; i < 1414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1414(int& val) {
    const int x = 1414;
    std::cout << 1414;
    for (int i = 0; i < 1414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1415(int& val) {
    func_in_1415(val);
    const int x = 1415;
    std::cout << 1415;
    for (int i = 0; i < 1415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1415(int& val) {
    const int x = 1415;
    std::cout << 1415;
    for (int i = 0; i < 1415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1416(int& val) {
    func_in_1416(val);
    const int x = 1416;
    std::cout << 1416;
    for (int i = 0; i < 1416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1416(int& val) {
    const int x = 1416;
    std::cout << 1416;
    for (int i = 0; i < 1416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1417(int& val) {
    func_in_1417(val);
    const int x = 1417;
    std::cout << 1417;
    for (int i = 0; i < 1417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1417(int& val) {
    const int x = 1417;
    std::cout << 1417;
    for (int i = 0; i < 1417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1418(int& val) {
    func_in_1418(val);
    const int x = 1418;
    std::cout << 1418;
    for (int i = 0; i < 1418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1418(int& val) {
    const int x = 1418;
    std::cout << 1418;
    for (int i = 0; i < 1418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1419(int& val) {
    func_in_1419(val);
    const int x = 1419;
    std::cout << 1419;
    for (int i = 0; i < 1419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1419(int& val) {
    const int x = 1419;
    std::cout << 1419;
    for (int i = 0; i < 1419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1420(int& val) {
    func_in_1420(val);
    const int x = 1420;
    std::cout << 1420;
    for (int i = 0; i < 1420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1420(int& val) {
    const int x = 1420;
    std::cout << 1420;
    for (int i = 0; i < 1420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1421(int& val) {
    func_in_1421(val);
    const int x = 1421;
    std::cout << 1421;
    for (int i = 0; i < 1421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1421(int& val) {
    const int x = 1421;
    std::cout << 1421;
    for (int i = 0; i < 1421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1422(int& val) {
    func_in_1422(val);
    const int x = 1422;
    std::cout << 1422;
    for (int i = 0; i < 1422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1422(int& val) {
    const int x = 1422;
    std::cout << 1422;
    for (int i = 0; i < 1422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1423(int& val) {
    func_in_1423(val);
    const int x = 1423;
    std::cout << 1423;
    for (int i = 0; i < 1423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1423(int& val) {
    const int x = 1423;
    std::cout << 1423;
    for (int i = 0; i < 1423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1424(int& val) {
    func_in_1424(val);
    const int x = 1424;
    std::cout << 1424;
    for (int i = 0; i < 1424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1424(int& val) {
    const int x = 1424;
    std::cout << 1424;
    for (int i = 0; i < 1424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1425(int& val) {
    func_in_1425(val);
    const int x = 1425;
    std::cout << 1425;
    for (int i = 0; i < 1425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1425(int& val) {
    const int x = 1425;
    std::cout << 1425;
    for (int i = 0; i < 1425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1426(int& val) {
    func_in_1426(val);
    const int x = 1426;
    std::cout << 1426;
    for (int i = 0; i < 1426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1426(int& val) {
    const int x = 1426;
    std::cout << 1426;
    for (int i = 0; i < 1426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1427(int& val) {
    func_in_1427(val);
    const int x = 1427;
    std::cout << 1427;
    for (int i = 0; i < 1427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1427(int& val) {
    const int x = 1427;
    std::cout << 1427;
    for (int i = 0; i < 1427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1428(int& val) {
    func_in_1428(val);
    const int x = 1428;
    std::cout << 1428;
    for (int i = 0; i < 1428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1428(int& val) {
    const int x = 1428;
    std::cout << 1428;
    for (int i = 0; i < 1428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1429(int& val) {
    func_in_1429(val);
    const int x = 1429;
    std::cout << 1429;
    for (int i = 0; i < 1429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1429(int& val) {
    const int x = 1429;
    std::cout << 1429;
    for (int i = 0; i < 1429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1430(int& val) {
    func_in_1430(val);
    const int x = 1430;
    std::cout << 1430;
    for (int i = 0; i < 1430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1430(int& val) {
    const int x = 1430;
    std::cout << 1430;
    for (int i = 0; i < 1430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1431(int& val) {
    func_in_1431(val);
    const int x = 1431;
    std::cout << 1431;
    for (int i = 0; i < 1431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1431(int& val) {
    const int x = 1431;
    std::cout << 1431;
    for (int i = 0; i < 1431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1432(int& val) {
    func_in_1432(val);
    const int x = 1432;
    std::cout << 1432;
    for (int i = 0; i < 1432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1432(int& val) {
    const int x = 1432;
    std::cout << 1432;
    for (int i = 0; i < 1432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1433(int& val) {
    func_in_1433(val);
    const int x = 1433;
    std::cout << 1433;
    for (int i = 0; i < 1433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1433(int& val) {
    const int x = 1433;
    std::cout << 1433;
    for (int i = 0; i < 1433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1434(int& val) {
    func_in_1434(val);
    const int x = 1434;
    std::cout << 1434;
    for (int i = 0; i < 1434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1434(int& val) {
    const int x = 1434;
    std::cout << 1434;
    for (int i = 0; i < 1434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1435(int& val) {
    func_in_1435(val);
    const int x = 1435;
    std::cout << 1435;
    for (int i = 0; i < 1435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1435(int& val) {
    const int x = 1435;
    std::cout << 1435;
    for (int i = 0; i < 1435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1436(int& val) {
    func_in_1436(val);
    const int x = 1436;
    std::cout << 1436;
    for (int i = 0; i < 1436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1436(int& val) {
    const int x = 1436;
    std::cout << 1436;
    for (int i = 0; i < 1436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1437(int& val) {
    func_in_1437(val);
    const int x = 1437;
    std::cout << 1437;
    for (int i = 0; i < 1437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1437(int& val) {
    const int x = 1437;
    std::cout << 1437;
    for (int i = 0; i < 1437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1438(int& val) {
    func_in_1438(val);
    const int x = 1438;
    std::cout << 1438;
    for (int i = 0; i < 1438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1438(int& val) {
    const int x = 1438;
    std::cout << 1438;
    for (int i = 0; i < 1438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1439(int& val) {
    func_in_1439(val);
    const int x = 1439;
    std::cout << 1439;
    for (int i = 0; i < 1439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1439(int& val) {
    const int x = 1439;
    std::cout << 1439;
    for (int i = 0; i < 1439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1440(int& val) {
    func_in_1440(val);
    const int x = 1440;
    std::cout << 1440;
    for (int i = 0; i < 1440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1440(int& val) {
    const int x = 1440;
    std::cout << 1440;
    for (int i = 0; i < 1440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1441(int& val) {
    func_in_1441(val);
    const int x = 1441;
    std::cout << 1441;
    for (int i = 0; i < 1441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1441(int& val) {
    const int x = 1441;
    std::cout << 1441;
    for (int i = 0; i < 1441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1442(int& val) {
    func_in_1442(val);
    const int x = 1442;
    std::cout << 1442;
    for (int i = 0; i < 1442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1442(int& val) {
    const int x = 1442;
    std::cout << 1442;
    for (int i = 0; i < 1442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1443(int& val) {
    func_in_1443(val);
    const int x = 1443;
    std::cout << 1443;
    for (int i = 0; i < 1443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1443(int& val) {
    const int x = 1443;
    std::cout << 1443;
    for (int i = 0; i < 1443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1444(int& val) {
    func_in_1444(val);
    const int x = 1444;
    std::cout << 1444;
    for (int i = 0; i < 1444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1444(int& val) {
    const int x = 1444;
    std::cout << 1444;
    for (int i = 0; i < 1444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1445(int& val) {
    func_in_1445(val);
    const int x = 1445;
    std::cout << 1445;
    for (int i = 0; i < 1445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1445(int& val) {
    const int x = 1445;
    std::cout << 1445;
    for (int i = 0; i < 1445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1446(int& val) {
    func_in_1446(val);
    const int x = 1446;
    std::cout << 1446;
    for (int i = 0; i < 1446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1446(int& val) {
    const int x = 1446;
    std::cout << 1446;
    for (int i = 0; i < 1446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1447(int& val) {
    func_in_1447(val);
    const int x = 1447;
    std::cout << 1447;
    for (int i = 0; i < 1447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1447(int& val) {
    const int x = 1447;
    std::cout << 1447;
    for (int i = 0; i < 1447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1448(int& val) {
    func_in_1448(val);
    const int x = 1448;
    std::cout << 1448;
    for (int i = 0; i < 1448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1448(int& val) {
    const int x = 1448;
    std::cout << 1448;
    for (int i = 0; i < 1448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1449(int& val) {
    func_in_1449(val);
    const int x = 1449;
    std::cout << 1449;
    for (int i = 0; i < 1449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1449(int& val) {
    const int x = 1449;
    std::cout << 1449;
    for (int i = 0; i < 1449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1450(int& val) {
    func_in_1450(val);
    const int x = 1450;
    std::cout << 1450;
    for (int i = 0; i < 1450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1450(int& val) {
    const int x = 1450;
    std::cout << 1450;
    for (int i = 0; i < 1450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1451(int& val) {
    func_in_1451(val);
    const int x = 1451;
    std::cout << 1451;
    for (int i = 0; i < 1451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1451(int& val) {
    const int x = 1451;
    std::cout << 1451;
    for (int i = 0; i < 1451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1452(int& val) {
    func_in_1452(val);
    const int x = 1452;
    std::cout << 1452;
    for (int i = 0; i < 1452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1452(int& val) {
    const int x = 1452;
    std::cout << 1452;
    for (int i = 0; i < 1452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1453(int& val) {
    func_in_1453(val);
    const int x = 1453;
    std::cout << 1453;
    for (int i = 0; i < 1453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1453(int& val) {
    const int x = 1453;
    std::cout << 1453;
    for (int i = 0; i < 1453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1454(int& val) {
    func_in_1454(val);
    const int x = 1454;
    std::cout << 1454;
    for (int i = 0; i < 1454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1454(int& val) {
    const int x = 1454;
    std::cout << 1454;
    for (int i = 0; i < 1454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1455(int& val) {
    func_in_1455(val);
    const int x = 1455;
    std::cout << 1455;
    for (int i = 0; i < 1455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1455(int& val) {
    const int x = 1455;
    std::cout << 1455;
    for (int i = 0; i < 1455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1456(int& val) {
    func_in_1456(val);
    const int x = 1456;
    std::cout << 1456;
    for (int i = 0; i < 1456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1456(int& val) {
    const int x = 1456;
    std::cout << 1456;
    for (int i = 0; i < 1456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1457(int& val) {
    func_in_1457(val);
    const int x = 1457;
    std::cout << 1457;
    for (int i = 0; i < 1457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1457(int& val) {
    const int x = 1457;
    std::cout << 1457;
    for (int i = 0; i < 1457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1458(int& val) {
    func_in_1458(val);
    const int x = 1458;
    std::cout << 1458;
    for (int i = 0; i < 1458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1458(int& val) {
    const int x = 1458;
    std::cout << 1458;
    for (int i = 0; i < 1458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1459(int& val) {
    func_in_1459(val);
    const int x = 1459;
    std::cout << 1459;
    for (int i = 0; i < 1459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1459(int& val) {
    const int x = 1459;
    std::cout << 1459;
    for (int i = 0; i < 1459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1460(int& val) {
    func_in_1460(val);
    const int x = 1460;
    std::cout << 1460;
    for (int i = 0; i < 1460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1460(int& val) {
    const int x = 1460;
    std::cout << 1460;
    for (int i = 0; i < 1460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1461(int& val) {
    func_in_1461(val);
    const int x = 1461;
    std::cout << 1461;
    for (int i = 0; i < 1461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1461(int& val) {
    const int x = 1461;
    std::cout << 1461;
    for (int i = 0; i < 1461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1462(int& val) {
    func_in_1462(val);
    const int x = 1462;
    std::cout << 1462;
    for (int i = 0; i < 1462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1462(int& val) {
    const int x = 1462;
    std::cout << 1462;
    for (int i = 0; i < 1462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1463(int& val) {
    func_in_1463(val);
    const int x = 1463;
    std::cout << 1463;
    for (int i = 0; i < 1463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1463(int& val) {
    const int x = 1463;
    std::cout << 1463;
    for (int i = 0; i < 1463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1464(int& val) {
    func_in_1464(val);
    const int x = 1464;
    std::cout << 1464;
    for (int i = 0; i < 1464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1464(int& val) {
    const int x = 1464;
    std::cout << 1464;
    for (int i = 0; i < 1464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1465(int& val) {
    func_in_1465(val);
    const int x = 1465;
    std::cout << 1465;
    for (int i = 0; i < 1465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1465(int& val) {
    const int x = 1465;
    std::cout << 1465;
    for (int i = 0; i < 1465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1466(int& val) {
    func_in_1466(val);
    const int x = 1466;
    std::cout << 1466;
    for (int i = 0; i < 1466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1466(int& val) {
    const int x = 1466;
    std::cout << 1466;
    for (int i = 0; i < 1466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1467(int& val) {
    func_in_1467(val);
    const int x = 1467;
    std::cout << 1467;
    for (int i = 0; i < 1467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1467(int& val) {
    const int x = 1467;
    std::cout << 1467;
    for (int i = 0; i < 1467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1468(int& val) {
    func_in_1468(val);
    const int x = 1468;
    std::cout << 1468;
    for (int i = 0; i < 1468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1468(int& val) {
    const int x = 1468;
    std::cout << 1468;
    for (int i = 0; i < 1468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1469(int& val) {
    func_in_1469(val);
    const int x = 1469;
    std::cout << 1469;
    for (int i = 0; i < 1469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1469(int& val) {
    const int x = 1469;
    std::cout << 1469;
    for (int i = 0; i < 1469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1470(int& val) {
    func_in_1470(val);
    const int x = 1470;
    std::cout << 1470;
    for (int i = 0; i < 1470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1470(int& val) {
    const int x = 1470;
    std::cout << 1470;
    for (int i = 0; i < 1470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1471(int& val) {
    func_in_1471(val);
    const int x = 1471;
    std::cout << 1471;
    for (int i = 0; i < 1471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1471(int& val) {
    const int x = 1471;
    std::cout << 1471;
    for (int i = 0; i < 1471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1472(int& val) {
    func_in_1472(val);
    const int x = 1472;
    std::cout << 1472;
    for (int i = 0; i < 1472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1472(int& val) {
    const int x = 1472;
    std::cout << 1472;
    for (int i = 0; i < 1472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1473(int& val) {
    func_in_1473(val);
    const int x = 1473;
    std::cout << 1473;
    for (int i = 0; i < 1473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1473(int& val) {
    const int x = 1473;
    std::cout << 1473;
    for (int i = 0; i < 1473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1474(int& val) {
    func_in_1474(val);
    const int x = 1474;
    std::cout << 1474;
    for (int i = 0; i < 1474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1474(int& val) {
    const int x = 1474;
    std::cout << 1474;
    for (int i = 0; i < 1474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1475(int& val) {
    func_in_1475(val);
    const int x = 1475;
    std::cout << 1475;
    for (int i = 0; i < 1475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1475(int& val) {
    const int x = 1475;
    std::cout << 1475;
    for (int i = 0; i < 1475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1476(int& val) {
    func_in_1476(val);
    const int x = 1476;
    std::cout << 1476;
    for (int i = 0; i < 1476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1476(int& val) {
    const int x = 1476;
    std::cout << 1476;
    for (int i = 0; i < 1476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1477(int& val) {
    func_in_1477(val);
    const int x = 1477;
    std::cout << 1477;
    for (int i = 0; i < 1477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1477(int& val) {
    const int x = 1477;
    std::cout << 1477;
    for (int i = 0; i < 1477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1478(int& val) {
    func_in_1478(val);
    const int x = 1478;
    std::cout << 1478;
    for (int i = 0; i < 1478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1478(int& val) {
    const int x = 1478;
    std::cout << 1478;
    for (int i = 0; i < 1478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1479(int& val) {
    func_in_1479(val);
    const int x = 1479;
    std::cout << 1479;
    for (int i = 0; i < 1479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1479(int& val) {
    const int x = 1479;
    std::cout << 1479;
    for (int i = 0; i < 1479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1480(int& val) {
    func_in_1480(val);
    const int x = 1480;
    std::cout << 1480;
    for (int i = 0; i < 1480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1480(int& val) {
    const int x = 1480;
    std::cout << 1480;
    for (int i = 0; i < 1480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1481(int& val) {
    func_in_1481(val);
    const int x = 1481;
    std::cout << 1481;
    for (int i = 0; i < 1481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1481(int& val) {
    const int x = 1481;
    std::cout << 1481;
    for (int i = 0; i < 1481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1482(int& val) {
    func_in_1482(val);
    const int x = 1482;
    std::cout << 1482;
    for (int i = 0; i < 1482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1482(int& val) {
    const int x = 1482;
    std::cout << 1482;
    for (int i = 0; i < 1482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1483(int& val) {
    func_in_1483(val);
    const int x = 1483;
    std::cout << 1483;
    for (int i = 0; i < 1483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1483(int& val) {
    const int x = 1483;
    std::cout << 1483;
    for (int i = 0; i < 1483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1484(int& val) {
    func_in_1484(val);
    const int x = 1484;
    std::cout << 1484;
    for (int i = 0; i < 1484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1484(int& val) {
    const int x = 1484;
    std::cout << 1484;
    for (int i = 0; i < 1484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1485(int& val) {
    func_in_1485(val);
    const int x = 1485;
    std::cout << 1485;
    for (int i = 0; i < 1485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1485(int& val) {
    const int x = 1485;
    std::cout << 1485;
    for (int i = 0; i < 1485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1486(int& val) {
    func_in_1486(val);
    const int x = 1486;
    std::cout << 1486;
    for (int i = 0; i < 1486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1486(int& val) {
    const int x = 1486;
    std::cout << 1486;
    for (int i = 0; i < 1486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1487(int& val) {
    func_in_1487(val);
    const int x = 1487;
    std::cout << 1487;
    for (int i = 0; i < 1487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1487(int& val) {
    const int x = 1487;
    std::cout << 1487;
    for (int i = 0; i < 1487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1488(int& val) {
    func_in_1488(val);
    const int x = 1488;
    std::cout << 1488;
    for (int i = 0; i < 1488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1488(int& val) {
    const int x = 1488;
    std::cout << 1488;
    for (int i = 0; i < 1488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1489(int& val) {
    func_in_1489(val);
    const int x = 1489;
    std::cout << 1489;
    for (int i = 0; i < 1489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1489(int& val) {
    const int x = 1489;
    std::cout << 1489;
    for (int i = 0; i < 1489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1490(int& val) {
    func_in_1490(val);
    const int x = 1490;
    std::cout << 1490;
    for (int i = 0; i < 1490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1490(int& val) {
    const int x = 1490;
    std::cout << 1490;
    for (int i = 0; i < 1490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1491(int& val) {
    func_in_1491(val);
    const int x = 1491;
    std::cout << 1491;
    for (int i = 0; i < 1491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1491(int& val) {
    const int x = 1491;
    std::cout << 1491;
    for (int i = 0; i < 1491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1492(int& val) {
    func_in_1492(val);
    const int x = 1492;
    std::cout << 1492;
    for (int i = 0; i < 1492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1492(int& val) {
    const int x = 1492;
    std::cout << 1492;
    for (int i = 0; i < 1492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1493(int& val) {
    func_in_1493(val);
    const int x = 1493;
    std::cout << 1493;
    for (int i = 0; i < 1493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1493(int& val) {
    const int x = 1493;
    std::cout << 1493;
    for (int i = 0; i < 1493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1494(int& val) {
    func_in_1494(val);
    const int x = 1494;
    std::cout << 1494;
    for (int i = 0; i < 1494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1494(int& val) {
    const int x = 1494;
    std::cout << 1494;
    for (int i = 0; i < 1494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1495(int& val) {
    func_in_1495(val);
    const int x = 1495;
    std::cout << 1495;
    for (int i = 0; i < 1495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1495(int& val) {
    const int x = 1495;
    std::cout << 1495;
    for (int i = 0; i < 1495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1496(int& val) {
    func_in_1496(val);
    const int x = 1496;
    std::cout << 1496;
    for (int i = 0; i < 1496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1496(int& val) {
    const int x = 1496;
    std::cout << 1496;
    for (int i = 0; i < 1496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1497(int& val) {
    func_in_1497(val);
    const int x = 1497;
    std::cout << 1497;
    for (int i = 0; i < 1497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1497(int& val) {
    const int x = 1497;
    std::cout << 1497;
    for (int i = 0; i < 1497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1498(int& val) {
    func_in_1498(val);
    const int x = 1498;
    std::cout << 1498;
    for (int i = 0; i < 1498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1498(int& val) {
    const int x = 1498;
    std::cout << 1498;
    for (int i = 0; i < 1498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1499(int& val) {
    func_in_1499(val);
    const int x = 1499;
    std::cout << 1499;
    for (int i = 0; i < 1499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1499(int& val) {
    const int x = 1499;
    std::cout << 1499;
    for (int i = 0; i < 1499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1500(int& val) {
    func_in_1500(val);
    const int x = 1500;
    std::cout << 1500;
    for (int i = 0; i < 1500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1500(int& val) {
    const int x = 1500;
    std::cout << 1500;
    for (int i = 0; i < 1500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1501(int& val) {
    func_in_1501(val);
    const int x = 1501;
    std::cout << 1501;
    for (int i = 0; i < 1501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1501(int& val) {
    const int x = 1501;
    std::cout << 1501;
    for (int i = 0; i < 1501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1502(int& val) {
    func_in_1502(val);
    const int x = 1502;
    std::cout << 1502;
    for (int i = 0; i < 1502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1502(int& val) {
    const int x = 1502;
    std::cout << 1502;
    for (int i = 0; i < 1502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1503(int& val) {
    func_in_1503(val);
    const int x = 1503;
    std::cout << 1503;
    for (int i = 0; i < 1503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1503(int& val) {
    const int x = 1503;
    std::cout << 1503;
    for (int i = 0; i < 1503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1504(int& val) {
    func_in_1504(val);
    const int x = 1504;
    std::cout << 1504;
    for (int i = 0; i < 1504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1504(int& val) {
    const int x = 1504;
    std::cout << 1504;
    for (int i = 0; i < 1504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1505(int& val) {
    func_in_1505(val);
    const int x = 1505;
    std::cout << 1505;
    for (int i = 0; i < 1505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1505(int& val) {
    const int x = 1505;
    std::cout << 1505;
    for (int i = 0; i < 1505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1506(int& val) {
    func_in_1506(val);
    const int x = 1506;
    std::cout << 1506;
    for (int i = 0; i < 1506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1506(int& val) {
    const int x = 1506;
    std::cout << 1506;
    for (int i = 0; i < 1506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1507(int& val) {
    func_in_1507(val);
    const int x = 1507;
    std::cout << 1507;
    for (int i = 0; i < 1507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1507(int& val) {
    const int x = 1507;
    std::cout << 1507;
    for (int i = 0; i < 1507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1508(int& val) {
    func_in_1508(val);
    const int x = 1508;
    std::cout << 1508;
    for (int i = 0; i < 1508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1508(int& val) {
    const int x = 1508;
    std::cout << 1508;
    for (int i = 0; i < 1508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1509(int& val) {
    func_in_1509(val);
    const int x = 1509;
    std::cout << 1509;
    for (int i = 0; i < 1509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1509(int& val) {
    const int x = 1509;
    std::cout << 1509;
    for (int i = 0; i < 1509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1510(int& val) {
    func_in_1510(val);
    const int x = 1510;
    std::cout << 1510;
    for (int i = 0; i < 1510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1510(int& val) {
    const int x = 1510;
    std::cout << 1510;
    for (int i = 0; i < 1510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1511(int& val) {
    func_in_1511(val);
    const int x = 1511;
    std::cout << 1511;
    for (int i = 0; i < 1511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1511(int& val) {
    const int x = 1511;
    std::cout << 1511;
    for (int i = 0; i < 1511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1512(int& val) {
    func_in_1512(val);
    const int x = 1512;
    std::cout << 1512;
    for (int i = 0; i < 1512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1512(int& val) {
    const int x = 1512;
    std::cout << 1512;
    for (int i = 0; i < 1512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1513(int& val) {
    func_in_1513(val);
    const int x = 1513;
    std::cout << 1513;
    for (int i = 0; i < 1513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1513(int& val) {
    const int x = 1513;
    std::cout << 1513;
    for (int i = 0; i < 1513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1514(int& val) {
    func_in_1514(val);
    const int x = 1514;
    std::cout << 1514;
    for (int i = 0; i < 1514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1514(int& val) {
    const int x = 1514;
    std::cout << 1514;
    for (int i = 0; i < 1514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1515(int& val) {
    func_in_1515(val);
    const int x = 1515;
    std::cout << 1515;
    for (int i = 0; i < 1515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1515(int& val) {
    const int x = 1515;
    std::cout << 1515;
    for (int i = 0; i < 1515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1516(int& val) {
    func_in_1516(val);
    const int x = 1516;
    std::cout << 1516;
    for (int i = 0; i < 1516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1516(int& val) {
    const int x = 1516;
    std::cout << 1516;
    for (int i = 0; i < 1516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1517(int& val) {
    func_in_1517(val);
    const int x = 1517;
    std::cout << 1517;
    for (int i = 0; i < 1517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1517(int& val) {
    const int x = 1517;
    std::cout << 1517;
    for (int i = 0; i < 1517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1518(int& val) {
    func_in_1518(val);
    const int x = 1518;
    std::cout << 1518;
    for (int i = 0; i < 1518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1518(int& val) {
    const int x = 1518;
    std::cout << 1518;
    for (int i = 0; i < 1518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1519(int& val) {
    func_in_1519(val);
    const int x = 1519;
    std::cout << 1519;
    for (int i = 0; i < 1519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1519(int& val) {
    const int x = 1519;
    std::cout << 1519;
    for (int i = 0; i < 1519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1520(int& val) {
    func_in_1520(val);
    const int x = 1520;
    std::cout << 1520;
    for (int i = 0; i < 1520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1520(int& val) {
    const int x = 1520;
    std::cout << 1520;
    for (int i = 0; i < 1520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1521(int& val) {
    func_in_1521(val);
    const int x = 1521;
    std::cout << 1521;
    for (int i = 0; i < 1521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1521(int& val) {
    const int x = 1521;
    std::cout << 1521;
    for (int i = 0; i < 1521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1522(int& val) {
    func_in_1522(val);
    const int x = 1522;
    std::cout << 1522;
    for (int i = 0; i < 1522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1522(int& val) {
    const int x = 1522;
    std::cout << 1522;
    for (int i = 0; i < 1522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1523(int& val) {
    func_in_1523(val);
    const int x = 1523;
    std::cout << 1523;
    for (int i = 0; i < 1523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1523(int& val) {
    const int x = 1523;
    std::cout << 1523;
    for (int i = 0; i < 1523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1524(int& val) {
    func_in_1524(val);
    const int x = 1524;
    std::cout << 1524;
    for (int i = 0; i < 1524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1524(int& val) {
    const int x = 1524;
    std::cout << 1524;
    for (int i = 0; i < 1524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1525(int& val) {
    func_in_1525(val);
    const int x = 1525;
    std::cout << 1525;
    for (int i = 0; i < 1525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1525(int& val) {
    const int x = 1525;
    std::cout << 1525;
    for (int i = 0; i < 1525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1526(int& val) {
    func_in_1526(val);
    const int x = 1526;
    std::cout << 1526;
    for (int i = 0; i < 1526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1526(int& val) {
    const int x = 1526;
    std::cout << 1526;
    for (int i = 0; i < 1526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1527(int& val) {
    func_in_1527(val);
    const int x = 1527;
    std::cout << 1527;
    for (int i = 0; i < 1527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1527(int& val) {
    const int x = 1527;
    std::cout << 1527;
    for (int i = 0; i < 1527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1528(int& val) {
    func_in_1528(val);
    const int x = 1528;
    std::cout << 1528;
    for (int i = 0; i < 1528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1528(int& val) {
    const int x = 1528;
    std::cout << 1528;
    for (int i = 0; i < 1528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1529(int& val) {
    func_in_1529(val);
    const int x = 1529;
    std::cout << 1529;
    for (int i = 0; i < 1529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1529(int& val) {
    const int x = 1529;
    std::cout << 1529;
    for (int i = 0; i < 1529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1530(int& val) {
    func_in_1530(val);
    const int x = 1530;
    std::cout << 1530;
    for (int i = 0; i < 1530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1530(int& val) {
    const int x = 1530;
    std::cout << 1530;
    for (int i = 0; i < 1530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1531(int& val) {
    func_in_1531(val);
    const int x = 1531;
    std::cout << 1531;
    for (int i = 0; i < 1531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1531(int& val) {
    const int x = 1531;
    std::cout << 1531;
    for (int i = 0; i < 1531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1532(int& val) {
    func_in_1532(val);
    const int x = 1532;
    std::cout << 1532;
    for (int i = 0; i < 1532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1532(int& val) {
    const int x = 1532;
    std::cout << 1532;
    for (int i = 0; i < 1532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1533(int& val) {
    func_in_1533(val);
    const int x = 1533;
    std::cout << 1533;
    for (int i = 0; i < 1533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1533(int& val) {
    const int x = 1533;
    std::cout << 1533;
    for (int i = 0; i < 1533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1534(int& val) {
    func_in_1534(val);
    const int x = 1534;
    std::cout << 1534;
    for (int i = 0; i < 1534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1534(int& val) {
    const int x = 1534;
    std::cout << 1534;
    for (int i = 0; i < 1534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1535(int& val) {
    func_in_1535(val);
    const int x = 1535;
    std::cout << 1535;
    for (int i = 0; i < 1535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1535(int& val) {
    const int x = 1535;
    std::cout << 1535;
    for (int i = 0; i < 1535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1536(int& val) {
    func_in_1536(val);
    const int x = 1536;
    std::cout << 1536;
    for (int i = 0; i < 1536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1536(int& val) {
    const int x = 1536;
    std::cout << 1536;
    for (int i = 0; i < 1536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1537(int& val) {
    func_in_1537(val);
    const int x = 1537;
    std::cout << 1537;
    for (int i = 0; i < 1537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1537(int& val) {
    const int x = 1537;
    std::cout << 1537;
    for (int i = 0; i < 1537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1538(int& val) {
    func_in_1538(val);
    const int x = 1538;
    std::cout << 1538;
    for (int i = 0; i < 1538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1538(int& val) {
    const int x = 1538;
    std::cout << 1538;
    for (int i = 0; i < 1538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1539(int& val) {
    func_in_1539(val);
    const int x = 1539;
    std::cout << 1539;
    for (int i = 0; i < 1539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1539(int& val) {
    const int x = 1539;
    std::cout << 1539;
    for (int i = 0; i < 1539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1540(int& val) {
    func_in_1540(val);
    const int x = 1540;
    std::cout << 1540;
    for (int i = 0; i < 1540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1540(int& val) {
    const int x = 1540;
    std::cout << 1540;
    for (int i = 0; i < 1540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1541(int& val) {
    func_in_1541(val);
    const int x = 1541;
    std::cout << 1541;
    for (int i = 0; i < 1541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1541(int& val) {
    const int x = 1541;
    std::cout << 1541;
    for (int i = 0; i < 1541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1542(int& val) {
    func_in_1542(val);
    const int x = 1542;
    std::cout << 1542;
    for (int i = 0; i < 1542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1542(int& val) {
    const int x = 1542;
    std::cout << 1542;
    for (int i = 0; i < 1542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1543(int& val) {
    func_in_1543(val);
    const int x = 1543;
    std::cout << 1543;
    for (int i = 0; i < 1543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1543(int& val) {
    const int x = 1543;
    std::cout << 1543;
    for (int i = 0; i < 1543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1544(int& val) {
    func_in_1544(val);
    const int x = 1544;
    std::cout << 1544;
    for (int i = 0; i < 1544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1544(int& val) {
    const int x = 1544;
    std::cout << 1544;
    for (int i = 0; i < 1544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1545(int& val) {
    func_in_1545(val);
    const int x = 1545;
    std::cout << 1545;
    for (int i = 0; i < 1545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1545(int& val) {
    const int x = 1545;
    std::cout << 1545;
    for (int i = 0; i < 1545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1546(int& val) {
    func_in_1546(val);
    const int x = 1546;
    std::cout << 1546;
    for (int i = 0; i < 1546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1546(int& val) {
    const int x = 1546;
    std::cout << 1546;
    for (int i = 0; i < 1546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1547(int& val) {
    func_in_1547(val);
    const int x = 1547;
    std::cout << 1547;
    for (int i = 0; i < 1547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1547(int& val) {
    const int x = 1547;
    std::cout << 1547;
    for (int i = 0; i < 1547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1548(int& val) {
    func_in_1548(val);
    const int x = 1548;
    std::cout << 1548;
    for (int i = 0; i < 1548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1548(int& val) {
    const int x = 1548;
    std::cout << 1548;
    for (int i = 0; i < 1548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1549(int& val) {
    func_in_1549(val);
    const int x = 1549;
    std::cout << 1549;
    for (int i = 0; i < 1549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1549(int& val) {
    const int x = 1549;
    std::cout << 1549;
    for (int i = 0; i < 1549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1550(int& val) {
    func_in_1550(val);
    const int x = 1550;
    std::cout << 1550;
    for (int i = 0; i < 1550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1550(int& val) {
    const int x = 1550;
    std::cout << 1550;
    for (int i = 0; i < 1550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1551(int& val) {
    func_in_1551(val);
    const int x = 1551;
    std::cout << 1551;
    for (int i = 0; i < 1551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1551(int& val) {
    const int x = 1551;
    std::cout << 1551;
    for (int i = 0; i < 1551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1552(int& val) {
    func_in_1552(val);
    const int x = 1552;
    std::cout << 1552;
    for (int i = 0; i < 1552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1552(int& val) {
    const int x = 1552;
    std::cout << 1552;
    for (int i = 0; i < 1552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1553(int& val) {
    func_in_1553(val);
    const int x = 1553;
    std::cout << 1553;
    for (int i = 0; i < 1553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1553(int& val) {
    const int x = 1553;
    std::cout << 1553;
    for (int i = 0; i < 1553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1554(int& val) {
    func_in_1554(val);
    const int x = 1554;
    std::cout << 1554;
    for (int i = 0; i < 1554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1554(int& val) {
    const int x = 1554;
    std::cout << 1554;
    for (int i = 0; i < 1554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1555(int& val) {
    func_in_1555(val);
    const int x = 1555;
    std::cout << 1555;
    for (int i = 0; i < 1555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1555(int& val) {
    const int x = 1555;
    std::cout << 1555;
    for (int i = 0; i < 1555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1556(int& val) {
    func_in_1556(val);
    const int x = 1556;
    std::cout << 1556;
    for (int i = 0; i < 1556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1556(int& val) {
    const int x = 1556;
    std::cout << 1556;
    for (int i = 0; i < 1556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1557(int& val) {
    func_in_1557(val);
    const int x = 1557;
    std::cout << 1557;
    for (int i = 0; i < 1557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1557(int& val) {
    const int x = 1557;
    std::cout << 1557;
    for (int i = 0; i < 1557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1558(int& val) {
    func_in_1558(val);
    const int x = 1558;
    std::cout << 1558;
    for (int i = 0; i < 1558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1558(int& val) {
    const int x = 1558;
    std::cout << 1558;
    for (int i = 0; i < 1558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1559(int& val) {
    func_in_1559(val);
    const int x = 1559;
    std::cout << 1559;
    for (int i = 0; i < 1559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1559(int& val) {
    const int x = 1559;
    std::cout << 1559;
    for (int i = 0; i < 1559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1560(int& val) {
    func_in_1560(val);
    const int x = 1560;
    std::cout << 1560;
    for (int i = 0; i < 1560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1560(int& val) {
    const int x = 1560;
    std::cout << 1560;
    for (int i = 0; i < 1560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1561(int& val) {
    func_in_1561(val);
    const int x = 1561;
    std::cout << 1561;
    for (int i = 0; i < 1561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1561(int& val) {
    const int x = 1561;
    std::cout << 1561;
    for (int i = 0; i < 1561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1562(int& val) {
    func_in_1562(val);
    const int x = 1562;
    std::cout << 1562;
    for (int i = 0; i < 1562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1562(int& val) {
    const int x = 1562;
    std::cout << 1562;
    for (int i = 0; i < 1562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1563(int& val) {
    func_in_1563(val);
    const int x = 1563;
    std::cout << 1563;
    for (int i = 0; i < 1563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1563(int& val) {
    const int x = 1563;
    std::cout << 1563;
    for (int i = 0; i < 1563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1564(int& val) {
    func_in_1564(val);
    const int x = 1564;
    std::cout << 1564;
    for (int i = 0; i < 1564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1564(int& val) {
    const int x = 1564;
    std::cout << 1564;
    for (int i = 0; i < 1564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1565(int& val) {
    func_in_1565(val);
    const int x = 1565;
    std::cout << 1565;
    for (int i = 0; i < 1565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1565(int& val) {
    const int x = 1565;
    std::cout << 1565;
    for (int i = 0; i < 1565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1566(int& val) {
    func_in_1566(val);
    const int x = 1566;
    std::cout << 1566;
    for (int i = 0; i < 1566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1566(int& val) {
    const int x = 1566;
    std::cout << 1566;
    for (int i = 0; i < 1566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1567(int& val) {
    func_in_1567(val);
    const int x = 1567;
    std::cout << 1567;
    for (int i = 0; i < 1567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1567(int& val) {
    const int x = 1567;
    std::cout << 1567;
    for (int i = 0; i < 1567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1568(int& val) {
    func_in_1568(val);
    const int x = 1568;
    std::cout << 1568;
    for (int i = 0; i < 1568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1568(int& val) {
    const int x = 1568;
    std::cout << 1568;
    for (int i = 0; i < 1568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1569(int& val) {
    func_in_1569(val);
    const int x = 1569;
    std::cout << 1569;
    for (int i = 0; i < 1569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1569(int& val) {
    const int x = 1569;
    std::cout << 1569;
    for (int i = 0; i < 1569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1570(int& val) {
    func_in_1570(val);
    const int x = 1570;
    std::cout << 1570;
    for (int i = 0; i < 1570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1570(int& val) {
    const int x = 1570;
    std::cout << 1570;
    for (int i = 0; i < 1570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1571(int& val) {
    func_in_1571(val);
    const int x = 1571;
    std::cout << 1571;
    for (int i = 0; i < 1571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1571(int& val) {
    const int x = 1571;
    std::cout << 1571;
    for (int i = 0; i < 1571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1572(int& val) {
    func_in_1572(val);
    const int x = 1572;
    std::cout << 1572;
    for (int i = 0; i < 1572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1572(int& val) {
    const int x = 1572;
    std::cout << 1572;
    for (int i = 0; i < 1572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1573(int& val) {
    func_in_1573(val);
    const int x = 1573;
    std::cout << 1573;
    for (int i = 0; i < 1573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1573(int& val) {
    const int x = 1573;
    std::cout << 1573;
    for (int i = 0; i < 1573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1574(int& val) {
    func_in_1574(val);
    const int x = 1574;
    std::cout << 1574;
    for (int i = 0; i < 1574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1574(int& val) {
    const int x = 1574;
    std::cout << 1574;
    for (int i = 0; i < 1574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1575(int& val) {
    func_in_1575(val);
    const int x = 1575;
    std::cout << 1575;
    for (int i = 0; i < 1575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1575(int& val) {
    const int x = 1575;
    std::cout << 1575;
    for (int i = 0; i < 1575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1576(int& val) {
    func_in_1576(val);
    const int x = 1576;
    std::cout << 1576;
    for (int i = 0; i < 1576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1576(int& val) {
    const int x = 1576;
    std::cout << 1576;
    for (int i = 0; i < 1576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1577(int& val) {
    func_in_1577(val);
    const int x = 1577;
    std::cout << 1577;
    for (int i = 0; i < 1577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1577(int& val) {
    const int x = 1577;
    std::cout << 1577;
    for (int i = 0; i < 1577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1578(int& val) {
    func_in_1578(val);
    const int x = 1578;
    std::cout << 1578;
    for (int i = 0; i < 1578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1578(int& val) {
    const int x = 1578;
    std::cout << 1578;
    for (int i = 0; i < 1578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1579(int& val) {
    func_in_1579(val);
    const int x = 1579;
    std::cout << 1579;
    for (int i = 0; i < 1579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1579(int& val) {
    const int x = 1579;
    std::cout << 1579;
    for (int i = 0; i < 1579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1580(int& val) {
    func_in_1580(val);
    const int x = 1580;
    std::cout << 1580;
    for (int i = 0; i < 1580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1580(int& val) {
    const int x = 1580;
    std::cout << 1580;
    for (int i = 0; i < 1580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1581(int& val) {
    func_in_1581(val);
    const int x = 1581;
    std::cout << 1581;
    for (int i = 0; i < 1581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1581(int& val) {
    const int x = 1581;
    std::cout << 1581;
    for (int i = 0; i < 1581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1582(int& val) {
    func_in_1582(val);
    const int x = 1582;
    std::cout << 1582;
    for (int i = 0; i < 1582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1582(int& val) {
    const int x = 1582;
    std::cout << 1582;
    for (int i = 0; i < 1582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1583(int& val) {
    func_in_1583(val);
    const int x = 1583;
    std::cout << 1583;
    for (int i = 0; i < 1583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1583(int& val) {
    const int x = 1583;
    std::cout << 1583;
    for (int i = 0; i < 1583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1584(int& val) {
    func_in_1584(val);
    const int x = 1584;
    std::cout << 1584;
    for (int i = 0; i < 1584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1584(int& val) {
    const int x = 1584;
    std::cout << 1584;
    for (int i = 0; i < 1584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1585(int& val) {
    func_in_1585(val);
    const int x = 1585;
    std::cout << 1585;
    for (int i = 0; i < 1585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1585(int& val) {
    const int x = 1585;
    std::cout << 1585;
    for (int i = 0; i < 1585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1586(int& val) {
    func_in_1586(val);
    const int x = 1586;
    std::cout << 1586;
    for (int i = 0; i < 1586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1586(int& val) {
    const int x = 1586;
    std::cout << 1586;
    for (int i = 0; i < 1586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1587(int& val) {
    func_in_1587(val);
    const int x = 1587;
    std::cout << 1587;
    for (int i = 0; i < 1587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1587(int& val) {
    const int x = 1587;
    std::cout << 1587;
    for (int i = 0; i < 1587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1588(int& val) {
    func_in_1588(val);
    const int x = 1588;
    std::cout << 1588;
    for (int i = 0; i < 1588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1588(int& val) {
    const int x = 1588;
    std::cout << 1588;
    for (int i = 0; i < 1588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1589(int& val) {
    func_in_1589(val);
    const int x = 1589;
    std::cout << 1589;
    for (int i = 0; i < 1589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1589(int& val) {
    const int x = 1589;
    std::cout << 1589;
    for (int i = 0; i < 1589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1590(int& val) {
    func_in_1590(val);
    const int x = 1590;
    std::cout << 1590;
    for (int i = 0; i < 1590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1590(int& val) {
    const int x = 1590;
    std::cout << 1590;
    for (int i = 0; i < 1590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1591(int& val) {
    func_in_1591(val);
    const int x = 1591;
    std::cout << 1591;
    for (int i = 0; i < 1591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1591(int& val) {
    const int x = 1591;
    std::cout << 1591;
    for (int i = 0; i < 1591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1592(int& val) {
    func_in_1592(val);
    const int x = 1592;
    std::cout << 1592;
    for (int i = 0; i < 1592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1592(int& val) {
    const int x = 1592;
    std::cout << 1592;
    for (int i = 0; i < 1592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1593(int& val) {
    func_in_1593(val);
    const int x = 1593;
    std::cout << 1593;
    for (int i = 0; i < 1593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1593(int& val) {
    const int x = 1593;
    std::cout << 1593;
    for (int i = 0; i < 1593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1594(int& val) {
    func_in_1594(val);
    const int x = 1594;
    std::cout << 1594;
    for (int i = 0; i < 1594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1594(int& val) {
    const int x = 1594;
    std::cout << 1594;
    for (int i = 0; i < 1594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1595(int& val) {
    func_in_1595(val);
    const int x = 1595;
    std::cout << 1595;
    for (int i = 0; i < 1595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1595(int& val) {
    const int x = 1595;
    std::cout << 1595;
    for (int i = 0; i < 1595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1596(int& val) {
    func_in_1596(val);
    const int x = 1596;
    std::cout << 1596;
    for (int i = 0; i < 1596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1596(int& val) {
    const int x = 1596;
    std::cout << 1596;
    for (int i = 0; i < 1596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1597(int& val) {
    func_in_1597(val);
    const int x = 1597;
    std::cout << 1597;
    for (int i = 0; i < 1597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1597(int& val) {
    const int x = 1597;
    std::cout << 1597;
    for (int i = 0; i < 1597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1598(int& val) {
    func_in_1598(val);
    const int x = 1598;
    std::cout << 1598;
    for (int i = 0; i < 1598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1598(int& val) {
    const int x = 1598;
    std::cout << 1598;
    for (int i = 0; i < 1598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1599(int& val) {
    func_in_1599(val);
    const int x = 1599;
    std::cout << 1599;
    for (int i = 0; i < 1599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1599(int& val) {
    const int x = 1599;
    std::cout << 1599;
    for (int i = 0; i < 1599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1600(int& val) {
    func_in_1600(val);
    const int x = 1600;
    std::cout << 1600;
    for (int i = 0; i < 1600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1600(int& val) {
    const int x = 1600;
    std::cout << 1600;
    for (int i = 0; i < 1600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1601(int& val) {
    func_in_1601(val);
    const int x = 1601;
    std::cout << 1601;
    for (int i = 0; i < 1601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1601(int& val) {
    const int x = 1601;
    std::cout << 1601;
    for (int i = 0; i < 1601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1602(int& val) {
    func_in_1602(val);
    const int x = 1602;
    std::cout << 1602;
    for (int i = 0; i < 1602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1602(int& val) {
    const int x = 1602;
    std::cout << 1602;
    for (int i = 0; i < 1602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1603(int& val) {
    func_in_1603(val);
    const int x = 1603;
    std::cout << 1603;
    for (int i = 0; i < 1603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1603(int& val) {
    const int x = 1603;
    std::cout << 1603;
    for (int i = 0; i < 1603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1604(int& val) {
    func_in_1604(val);
    const int x = 1604;
    std::cout << 1604;
    for (int i = 0; i < 1604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1604(int& val) {
    const int x = 1604;
    std::cout << 1604;
    for (int i = 0; i < 1604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1605(int& val) {
    func_in_1605(val);
    const int x = 1605;
    std::cout << 1605;
    for (int i = 0; i < 1605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1605(int& val) {
    const int x = 1605;
    std::cout << 1605;
    for (int i = 0; i < 1605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1606(int& val) {
    func_in_1606(val);
    const int x = 1606;
    std::cout << 1606;
    for (int i = 0; i < 1606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1606(int& val) {
    const int x = 1606;
    std::cout << 1606;
    for (int i = 0; i < 1606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1607(int& val) {
    func_in_1607(val);
    const int x = 1607;
    std::cout << 1607;
    for (int i = 0; i < 1607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1607(int& val) {
    const int x = 1607;
    std::cout << 1607;
    for (int i = 0; i < 1607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1608(int& val) {
    func_in_1608(val);
    const int x = 1608;
    std::cout << 1608;
    for (int i = 0; i < 1608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1608(int& val) {
    const int x = 1608;
    std::cout << 1608;
    for (int i = 0; i < 1608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1609(int& val) {
    func_in_1609(val);
    const int x = 1609;
    std::cout << 1609;
    for (int i = 0; i < 1609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1609(int& val) {
    const int x = 1609;
    std::cout << 1609;
    for (int i = 0; i < 1609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1610(int& val) {
    func_in_1610(val);
    const int x = 1610;
    std::cout << 1610;
    for (int i = 0; i < 1610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1610(int& val) {
    const int x = 1610;
    std::cout << 1610;
    for (int i = 0; i < 1610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1611(int& val) {
    func_in_1611(val);
    const int x = 1611;
    std::cout << 1611;
    for (int i = 0; i < 1611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1611(int& val) {
    const int x = 1611;
    std::cout << 1611;
    for (int i = 0; i < 1611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1612(int& val) {
    func_in_1612(val);
    const int x = 1612;
    std::cout << 1612;
    for (int i = 0; i < 1612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1612(int& val) {
    const int x = 1612;
    std::cout << 1612;
    for (int i = 0; i < 1612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1613(int& val) {
    func_in_1613(val);
    const int x = 1613;
    std::cout << 1613;
    for (int i = 0; i < 1613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1613(int& val) {
    const int x = 1613;
    std::cout << 1613;
    for (int i = 0; i < 1613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1614(int& val) {
    func_in_1614(val);
    const int x = 1614;
    std::cout << 1614;
    for (int i = 0; i < 1614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1614(int& val) {
    const int x = 1614;
    std::cout << 1614;
    for (int i = 0; i < 1614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1615(int& val) {
    func_in_1615(val);
    const int x = 1615;
    std::cout << 1615;
    for (int i = 0; i < 1615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1615(int& val) {
    const int x = 1615;
    std::cout << 1615;
    for (int i = 0; i < 1615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1616(int& val) {
    func_in_1616(val);
    const int x = 1616;
    std::cout << 1616;
    for (int i = 0; i < 1616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1616(int& val) {
    const int x = 1616;
    std::cout << 1616;
    for (int i = 0; i < 1616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1617(int& val) {
    func_in_1617(val);
    const int x = 1617;
    std::cout << 1617;
    for (int i = 0; i < 1617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1617(int& val) {
    const int x = 1617;
    std::cout << 1617;
    for (int i = 0; i < 1617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1618(int& val) {
    func_in_1618(val);
    const int x = 1618;
    std::cout << 1618;
    for (int i = 0; i < 1618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1618(int& val) {
    const int x = 1618;
    std::cout << 1618;
    for (int i = 0; i < 1618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1619(int& val) {
    func_in_1619(val);
    const int x = 1619;
    std::cout << 1619;
    for (int i = 0; i < 1619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1619(int& val) {
    const int x = 1619;
    std::cout << 1619;
    for (int i = 0; i < 1619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1620(int& val) {
    func_in_1620(val);
    const int x = 1620;
    std::cout << 1620;
    for (int i = 0; i < 1620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1620(int& val) {
    const int x = 1620;
    std::cout << 1620;
    for (int i = 0; i < 1620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1621(int& val) {
    func_in_1621(val);
    const int x = 1621;
    std::cout << 1621;
    for (int i = 0; i < 1621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1621(int& val) {
    const int x = 1621;
    std::cout << 1621;
    for (int i = 0; i < 1621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1622(int& val) {
    func_in_1622(val);
    const int x = 1622;
    std::cout << 1622;
    for (int i = 0; i < 1622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1622(int& val) {
    const int x = 1622;
    std::cout << 1622;
    for (int i = 0; i < 1622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1623(int& val) {
    func_in_1623(val);
    const int x = 1623;
    std::cout << 1623;
    for (int i = 0; i < 1623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1623(int& val) {
    const int x = 1623;
    std::cout << 1623;
    for (int i = 0; i < 1623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1624(int& val) {
    func_in_1624(val);
    const int x = 1624;
    std::cout << 1624;
    for (int i = 0; i < 1624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1624(int& val) {
    const int x = 1624;
    std::cout << 1624;
    for (int i = 0; i < 1624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1625(int& val) {
    func_in_1625(val);
    const int x = 1625;
    std::cout << 1625;
    for (int i = 0; i < 1625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1625(int& val) {
    const int x = 1625;
    std::cout << 1625;
    for (int i = 0; i < 1625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1626(int& val) {
    func_in_1626(val);
    const int x = 1626;
    std::cout << 1626;
    for (int i = 0; i < 1626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1626(int& val) {
    const int x = 1626;
    std::cout << 1626;
    for (int i = 0; i < 1626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1627(int& val) {
    func_in_1627(val);
    const int x = 1627;
    std::cout << 1627;
    for (int i = 0; i < 1627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1627(int& val) {
    const int x = 1627;
    std::cout << 1627;
    for (int i = 0; i < 1627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1628(int& val) {
    func_in_1628(val);
    const int x = 1628;
    std::cout << 1628;
    for (int i = 0; i < 1628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1628(int& val) {
    const int x = 1628;
    std::cout << 1628;
    for (int i = 0; i < 1628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1629(int& val) {
    func_in_1629(val);
    const int x = 1629;
    std::cout << 1629;
    for (int i = 0; i < 1629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1629(int& val) {
    const int x = 1629;
    std::cout << 1629;
    for (int i = 0; i < 1629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1630(int& val) {
    func_in_1630(val);
    const int x = 1630;
    std::cout << 1630;
    for (int i = 0; i < 1630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1630(int& val) {
    const int x = 1630;
    std::cout << 1630;
    for (int i = 0; i < 1630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1631(int& val) {
    func_in_1631(val);
    const int x = 1631;
    std::cout << 1631;
    for (int i = 0; i < 1631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1631(int& val) {
    const int x = 1631;
    std::cout << 1631;
    for (int i = 0; i < 1631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1632(int& val) {
    func_in_1632(val);
    const int x = 1632;
    std::cout << 1632;
    for (int i = 0; i < 1632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1632(int& val) {
    const int x = 1632;
    std::cout << 1632;
    for (int i = 0; i < 1632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1633(int& val) {
    func_in_1633(val);
    const int x = 1633;
    std::cout << 1633;
    for (int i = 0; i < 1633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1633(int& val) {
    const int x = 1633;
    std::cout << 1633;
    for (int i = 0; i < 1633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1634(int& val) {
    func_in_1634(val);
    const int x = 1634;
    std::cout << 1634;
    for (int i = 0; i < 1634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1634(int& val) {
    const int x = 1634;
    std::cout << 1634;
    for (int i = 0; i < 1634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1635(int& val) {
    func_in_1635(val);
    const int x = 1635;
    std::cout << 1635;
    for (int i = 0; i < 1635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1635(int& val) {
    const int x = 1635;
    std::cout << 1635;
    for (int i = 0; i < 1635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1636(int& val) {
    func_in_1636(val);
    const int x = 1636;
    std::cout << 1636;
    for (int i = 0; i < 1636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1636(int& val) {
    const int x = 1636;
    std::cout << 1636;
    for (int i = 0; i < 1636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1637(int& val) {
    func_in_1637(val);
    const int x = 1637;
    std::cout << 1637;
    for (int i = 0; i < 1637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1637(int& val) {
    const int x = 1637;
    std::cout << 1637;
    for (int i = 0; i < 1637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1638(int& val) {
    func_in_1638(val);
    const int x = 1638;
    std::cout << 1638;
    for (int i = 0; i < 1638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1638(int& val) {
    const int x = 1638;
    std::cout << 1638;
    for (int i = 0; i < 1638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1639(int& val) {
    func_in_1639(val);
    const int x = 1639;
    std::cout << 1639;
    for (int i = 0; i < 1639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1639(int& val) {
    const int x = 1639;
    std::cout << 1639;
    for (int i = 0; i < 1639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1640(int& val) {
    func_in_1640(val);
    const int x = 1640;
    std::cout << 1640;
    for (int i = 0; i < 1640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1640(int& val) {
    const int x = 1640;
    std::cout << 1640;
    for (int i = 0; i < 1640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1641(int& val) {
    func_in_1641(val);
    const int x = 1641;
    std::cout << 1641;
    for (int i = 0; i < 1641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1641(int& val) {
    const int x = 1641;
    std::cout << 1641;
    for (int i = 0; i < 1641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1642(int& val) {
    func_in_1642(val);
    const int x = 1642;
    std::cout << 1642;
    for (int i = 0; i < 1642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1642(int& val) {
    const int x = 1642;
    std::cout << 1642;
    for (int i = 0; i < 1642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1643(int& val) {
    func_in_1643(val);
    const int x = 1643;
    std::cout << 1643;
    for (int i = 0; i < 1643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1643(int& val) {
    const int x = 1643;
    std::cout << 1643;
    for (int i = 0; i < 1643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1644(int& val) {
    func_in_1644(val);
    const int x = 1644;
    std::cout << 1644;
    for (int i = 0; i < 1644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1644(int& val) {
    const int x = 1644;
    std::cout << 1644;
    for (int i = 0; i < 1644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1645(int& val) {
    func_in_1645(val);
    const int x = 1645;
    std::cout << 1645;
    for (int i = 0; i < 1645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1645(int& val) {
    const int x = 1645;
    std::cout << 1645;
    for (int i = 0; i < 1645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1646(int& val) {
    func_in_1646(val);
    const int x = 1646;
    std::cout << 1646;
    for (int i = 0; i < 1646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1646(int& val) {
    const int x = 1646;
    std::cout << 1646;
    for (int i = 0; i < 1646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1647(int& val) {
    func_in_1647(val);
    const int x = 1647;
    std::cout << 1647;
    for (int i = 0; i < 1647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1647(int& val) {
    const int x = 1647;
    std::cout << 1647;
    for (int i = 0; i < 1647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1648(int& val) {
    func_in_1648(val);
    const int x = 1648;
    std::cout << 1648;
    for (int i = 0; i < 1648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1648(int& val) {
    const int x = 1648;
    std::cout << 1648;
    for (int i = 0; i < 1648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1649(int& val) {
    func_in_1649(val);
    const int x = 1649;
    std::cout << 1649;
    for (int i = 0; i < 1649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1649(int& val) {
    const int x = 1649;
    std::cout << 1649;
    for (int i = 0; i < 1649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1650(int& val) {
    func_in_1650(val);
    const int x = 1650;
    std::cout << 1650;
    for (int i = 0; i < 1650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1650(int& val) {
    const int x = 1650;
    std::cout << 1650;
    for (int i = 0; i < 1650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1651(int& val) {
    func_in_1651(val);
    const int x = 1651;
    std::cout << 1651;
    for (int i = 0; i < 1651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1651(int& val) {
    const int x = 1651;
    std::cout << 1651;
    for (int i = 0; i < 1651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1652(int& val) {
    func_in_1652(val);
    const int x = 1652;
    std::cout << 1652;
    for (int i = 0; i < 1652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1652(int& val) {
    const int x = 1652;
    std::cout << 1652;
    for (int i = 0; i < 1652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1653(int& val) {
    func_in_1653(val);
    const int x = 1653;
    std::cout << 1653;
    for (int i = 0; i < 1653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1653(int& val) {
    const int x = 1653;
    std::cout << 1653;
    for (int i = 0; i < 1653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1654(int& val) {
    func_in_1654(val);
    const int x = 1654;
    std::cout << 1654;
    for (int i = 0; i < 1654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1654(int& val) {
    const int x = 1654;
    std::cout << 1654;
    for (int i = 0; i < 1654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1655(int& val) {
    func_in_1655(val);
    const int x = 1655;
    std::cout << 1655;
    for (int i = 0; i < 1655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1655(int& val) {
    const int x = 1655;
    std::cout << 1655;
    for (int i = 0; i < 1655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1656(int& val) {
    func_in_1656(val);
    const int x = 1656;
    std::cout << 1656;
    for (int i = 0; i < 1656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1656(int& val) {
    const int x = 1656;
    std::cout << 1656;
    for (int i = 0; i < 1656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1657(int& val) {
    func_in_1657(val);
    const int x = 1657;
    std::cout << 1657;
    for (int i = 0; i < 1657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1657(int& val) {
    const int x = 1657;
    std::cout << 1657;
    for (int i = 0; i < 1657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1658(int& val) {
    func_in_1658(val);
    const int x = 1658;
    std::cout << 1658;
    for (int i = 0; i < 1658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1658(int& val) {
    const int x = 1658;
    std::cout << 1658;
    for (int i = 0; i < 1658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1659(int& val) {
    func_in_1659(val);
    const int x = 1659;
    std::cout << 1659;
    for (int i = 0; i < 1659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1659(int& val) {
    const int x = 1659;
    std::cout << 1659;
    for (int i = 0; i < 1659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1660(int& val) {
    func_in_1660(val);
    const int x = 1660;
    std::cout << 1660;
    for (int i = 0; i < 1660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1660(int& val) {
    const int x = 1660;
    std::cout << 1660;
    for (int i = 0; i < 1660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1661(int& val) {
    func_in_1661(val);
    const int x = 1661;
    std::cout << 1661;
    for (int i = 0; i < 1661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1661(int& val) {
    const int x = 1661;
    std::cout << 1661;
    for (int i = 0; i < 1661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1662(int& val) {
    func_in_1662(val);
    const int x = 1662;
    std::cout << 1662;
    for (int i = 0; i < 1662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1662(int& val) {
    const int x = 1662;
    std::cout << 1662;
    for (int i = 0; i < 1662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1663(int& val) {
    func_in_1663(val);
    const int x = 1663;
    std::cout << 1663;
    for (int i = 0; i < 1663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1663(int& val) {
    const int x = 1663;
    std::cout << 1663;
    for (int i = 0; i < 1663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1664(int& val) {
    func_in_1664(val);
    const int x = 1664;
    std::cout << 1664;
    for (int i = 0; i < 1664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1664(int& val) {
    const int x = 1664;
    std::cout << 1664;
    for (int i = 0; i < 1664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1665(int& val) {
    func_in_1665(val);
    const int x = 1665;
    std::cout << 1665;
    for (int i = 0; i < 1665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1665(int& val) {
    const int x = 1665;
    std::cout << 1665;
    for (int i = 0; i < 1665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1666(int& val) {
    func_in_1666(val);
    const int x = 1666;
    std::cout << 1666;
    for (int i = 0; i < 1666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1666(int& val) {
    const int x = 1666;
    std::cout << 1666;
    for (int i = 0; i < 1666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1667(int& val) {
    func_in_1667(val);
    const int x = 1667;
    std::cout << 1667;
    for (int i = 0; i < 1667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1667(int& val) {
    const int x = 1667;
    std::cout << 1667;
    for (int i = 0; i < 1667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1668(int& val) {
    func_in_1668(val);
    const int x = 1668;
    std::cout << 1668;
    for (int i = 0; i < 1668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1668(int& val) {
    const int x = 1668;
    std::cout << 1668;
    for (int i = 0; i < 1668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1669(int& val) {
    func_in_1669(val);
    const int x = 1669;
    std::cout << 1669;
    for (int i = 0; i < 1669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1669(int& val) {
    const int x = 1669;
    std::cout << 1669;
    for (int i = 0; i < 1669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1670(int& val) {
    func_in_1670(val);
    const int x = 1670;
    std::cout << 1670;
    for (int i = 0; i < 1670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1670(int& val) {
    const int x = 1670;
    std::cout << 1670;
    for (int i = 0; i < 1670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1671(int& val) {
    func_in_1671(val);
    const int x = 1671;
    std::cout << 1671;
    for (int i = 0; i < 1671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1671(int& val) {
    const int x = 1671;
    std::cout << 1671;
    for (int i = 0; i < 1671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1672(int& val) {
    func_in_1672(val);
    const int x = 1672;
    std::cout << 1672;
    for (int i = 0; i < 1672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1672(int& val) {
    const int x = 1672;
    std::cout << 1672;
    for (int i = 0; i < 1672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1673(int& val) {
    func_in_1673(val);
    const int x = 1673;
    std::cout << 1673;
    for (int i = 0; i < 1673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1673(int& val) {
    const int x = 1673;
    std::cout << 1673;
    for (int i = 0; i < 1673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1674(int& val) {
    func_in_1674(val);
    const int x = 1674;
    std::cout << 1674;
    for (int i = 0; i < 1674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1674(int& val) {
    const int x = 1674;
    std::cout << 1674;
    for (int i = 0; i < 1674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1675(int& val) {
    func_in_1675(val);
    const int x = 1675;
    std::cout << 1675;
    for (int i = 0; i < 1675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1675(int& val) {
    const int x = 1675;
    std::cout << 1675;
    for (int i = 0; i < 1675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1676(int& val) {
    func_in_1676(val);
    const int x = 1676;
    std::cout << 1676;
    for (int i = 0; i < 1676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1676(int& val) {
    const int x = 1676;
    std::cout << 1676;
    for (int i = 0; i < 1676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1677(int& val) {
    func_in_1677(val);
    const int x = 1677;
    std::cout << 1677;
    for (int i = 0; i < 1677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1677(int& val) {
    const int x = 1677;
    std::cout << 1677;
    for (int i = 0; i < 1677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1678(int& val) {
    func_in_1678(val);
    const int x = 1678;
    std::cout << 1678;
    for (int i = 0; i < 1678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1678(int& val) {
    const int x = 1678;
    std::cout << 1678;
    for (int i = 0; i < 1678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1679(int& val) {
    func_in_1679(val);
    const int x = 1679;
    std::cout << 1679;
    for (int i = 0; i < 1679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1679(int& val) {
    const int x = 1679;
    std::cout << 1679;
    for (int i = 0; i < 1679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1680(int& val) {
    func_in_1680(val);
    const int x = 1680;
    std::cout << 1680;
    for (int i = 0; i < 1680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1680(int& val) {
    const int x = 1680;
    std::cout << 1680;
    for (int i = 0; i < 1680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1681(int& val) {
    func_in_1681(val);
    const int x = 1681;
    std::cout << 1681;
    for (int i = 0; i < 1681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1681(int& val) {
    const int x = 1681;
    std::cout << 1681;
    for (int i = 0; i < 1681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1682(int& val) {
    func_in_1682(val);
    const int x = 1682;
    std::cout << 1682;
    for (int i = 0; i < 1682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1682(int& val) {
    const int x = 1682;
    std::cout << 1682;
    for (int i = 0; i < 1682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1683(int& val) {
    func_in_1683(val);
    const int x = 1683;
    std::cout << 1683;
    for (int i = 0; i < 1683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1683(int& val) {
    const int x = 1683;
    std::cout << 1683;
    for (int i = 0; i < 1683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1684(int& val) {
    func_in_1684(val);
    const int x = 1684;
    std::cout << 1684;
    for (int i = 0; i < 1684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1684(int& val) {
    const int x = 1684;
    std::cout << 1684;
    for (int i = 0; i < 1684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1685(int& val) {
    func_in_1685(val);
    const int x = 1685;
    std::cout << 1685;
    for (int i = 0; i < 1685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1685(int& val) {
    const int x = 1685;
    std::cout << 1685;
    for (int i = 0; i < 1685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1686(int& val) {
    func_in_1686(val);
    const int x = 1686;
    std::cout << 1686;
    for (int i = 0; i < 1686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1686(int& val) {
    const int x = 1686;
    std::cout << 1686;
    for (int i = 0; i < 1686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1687(int& val) {
    func_in_1687(val);
    const int x = 1687;
    std::cout << 1687;
    for (int i = 0; i < 1687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1687(int& val) {
    const int x = 1687;
    std::cout << 1687;
    for (int i = 0; i < 1687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1688(int& val) {
    func_in_1688(val);
    const int x = 1688;
    std::cout << 1688;
    for (int i = 0; i < 1688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1688(int& val) {
    const int x = 1688;
    std::cout << 1688;
    for (int i = 0; i < 1688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1689(int& val) {
    func_in_1689(val);
    const int x = 1689;
    std::cout << 1689;
    for (int i = 0; i < 1689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1689(int& val) {
    const int x = 1689;
    std::cout << 1689;
    for (int i = 0; i < 1689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1690(int& val) {
    func_in_1690(val);
    const int x = 1690;
    std::cout << 1690;
    for (int i = 0; i < 1690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1690(int& val) {
    const int x = 1690;
    std::cout << 1690;
    for (int i = 0; i < 1690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1691(int& val) {
    func_in_1691(val);
    const int x = 1691;
    std::cout << 1691;
    for (int i = 0; i < 1691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1691(int& val) {
    const int x = 1691;
    std::cout << 1691;
    for (int i = 0; i < 1691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1692(int& val) {
    func_in_1692(val);
    const int x = 1692;
    std::cout << 1692;
    for (int i = 0; i < 1692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1692(int& val) {
    const int x = 1692;
    std::cout << 1692;
    for (int i = 0; i < 1692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1693(int& val) {
    func_in_1693(val);
    const int x = 1693;
    std::cout << 1693;
    for (int i = 0; i < 1693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1693(int& val) {
    const int x = 1693;
    std::cout << 1693;
    for (int i = 0; i < 1693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1694(int& val) {
    func_in_1694(val);
    const int x = 1694;
    std::cout << 1694;
    for (int i = 0; i < 1694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1694(int& val) {
    const int x = 1694;
    std::cout << 1694;
    for (int i = 0; i < 1694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1695(int& val) {
    func_in_1695(val);
    const int x = 1695;
    std::cout << 1695;
    for (int i = 0; i < 1695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1695(int& val) {
    const int x = 1695;
    std::cout << 1695;
    for (int i = 0; i < 1695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1696(int& val) {
    func_in_1696(val);
    const int x = 1696;
    std::cout << 1696;
    for (int i = 0; i < 1696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1696(int& val) {
    const int x = 1696;
    std::cout << 1696;
    for (int i = 0; i < 1696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1697(int& val) {
    func_in_1697(val);
    const int x = 1697;
    std::cout << 1697;
    for (int i = 0; i < 1697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1697(int& val) {
    const int x = 1697;
    std::cout << 1697;
    for (int i = 0; i < 1697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1698(int& val) {
    func_in_1698(val);
    const int x = 1698;
    std::cout << 1698;
    for (int i = 0; i < 1698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1698(int& val) {
    const int x = 1698;
    std::cout << 1698;
    for (int i = 0; i < 1698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1699(int& val) {
    func_in_1699(val);
    const int x = 1699;
    std::cout << 1699;
    for (int i = 0; i < 1699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1699(int& val) {
    const int x = 1699;
    std::cout << 1699;
    for (int i = 0; i < 1699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1700(int& val) {
    func_in_1700(val);
    const int x = 1700;
    std::cout << 1700;
    for (int i = 0; i < 1700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1700(int& val) {
    const int x = 1700;
    std::cout << 1700;
    for (int i = 0; i < 1700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1701(int& val) {
    func_in_1701(val);
    const int x = 1701;
    std::cout << 1701;
    for (int i = 0; i < 1701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1701(int& val) {
    const int x = 1701;
    std::cout << 1701;
    for (int i = 0; i < 1701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1702(int& val) {
    func_in_1702(val);
    const int x = 1702;
    std::cout << 1702;
    for (int i = 0; i < 1702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1702(int& val) {
    const int x = 1702;
    std::cout << 1702;
    for (int i = 0; i < 1702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1703(int& val) {
    func_in_1703(val);
    const int x = 1703;
    std::cout << 1703;
    for (int i = 0; i < 1703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1703(int& val) {
    const int x = 1703;
    std::cout << 1703;
    for (int i = 0; i < 1703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1704(int& val) {
    func_in_1704(val);
    const int x = 1704;
    std::cout << 1704;
    for (int i = 0; i < 1704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1704(int& val) {
    const int x = 1704;
    std::cout << 1704;
    for (int i = 0; i < 1704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1705(int& val) {
    func_in_1705(val);
    const int x = 1705;
    std::cout << 1705;
    for (int i = 0; i < 1705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1705(int& val) {
    const int x = 1705;
    std::cout << 1705;
    for (int i = 0; i < 1705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1706(int& val) {
    func_in_1706(val);
    const int x = 1706;
    std::cout << 1706;
    for (int i = 0; i < 1706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1706(int& val) {
    const int x = 1706;
    std::cout << 1706;
    for (int i = 0; i < 1706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1707(int& val) {
    func_in_1707(val);
    const int x = 1707;
    std::cout << 1707;
    for (int i = 0; i < 1707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1707(int& val) {
    const int x = 1707;
    std::cout << 1707;
    for (int i = 0; i < 1707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1708(int& val) {
    func_in_1708(val);
    const int x = 1708;
    std::cout << 1708;
    for (int i = 0; i < 1708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1708(int& val) {
    const int x = 1708;
    std::cout << 1708;
    for (int i = 0; i < 1708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1709(int& val) {
    func_in_1709(val);
    const int x = 1709;
    std::cout << 1709;
    for (int i = 0; i < 1709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1709(int& val) {
    const int x = 1709;
    std::cout << 1709;
    for (int i = 0; i < 1709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1710(int& val) {
    func_in_1710(val);
    const int x = 1710;
    std::cout << 1710;
    for (int i = 0; i < 1710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1710(int& val) {
    const int x = 1710;
    std::cout << 1710;
    for (int i = 0; i < 1710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1711(int& val) {
    func_in_1711(val);
    const int x = 1711;
    std::cout << 1711;
    for (int i = 0; i < 1711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1711(int& val) {
    const int x = 1711;
    std::cout << 1711;
    for (int i = 0; i < 1711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1712(int& val) {
    func_in_1712(val);
    const int x = 1712;
    std::cout << 1712;
    for (int i = 0; i < 1712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1712(int& val) {
    const int x = 1712;
    std::cout << 1712;
    for (int i = 0; i < 1712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1713(int& val) {
    func_in_1713(val);
    const int x = 1713;
    std::cout << 1713;
    for (int i = 0; i < 1713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1713(int& val) {
    const int x = 1713;
    std::cout << 1713;
    for (int i = 0; i < 1713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1714(int& val) {
    func_in_1714(val);
    const int x = 1714;
    std::cout << 1714;
    for (int i = 0; i < 1714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1714(int& val) {
    const int x = 1714;
    std::cout << 1714;
    for (int i = 0; i < 1714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1715(int& val) {
    func_in_1715(val);
    const int x = 1715;
    std::cout << 1715;
    for (int i = 0; i < 1715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1715(int& val) {
    const int x = 1715;
    std::cout << 1715;
    for (int i = 0; i < 1715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1716(int& val) {
    func_in_1716(val);
    const int x = 1716;
    std::cout << 1716;
    for (int i = 0; i < 1716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1716(int& val) {
    const int x = 1716;
    std::cout << 1716;
    for (int i = 0; i < 1716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1717(int& val) {
    func_in_1717(val);
    const int x = 1717;
    std::cout << 1717;
    for (int i = 0; i < 1717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1717(int& val) {
    const int x = 1717;
    std::cout << 1717;
    for (int i = 0; i < 1717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1718(int& val) {
    func_in_1718(val);
    const int x = 1718;
    std::cout << 1718;
    for (int i = 0; i < 1718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1718(int& val) {
    const int x = 1718;
    std::cout << 1718;
    for (int i = 0; i < 1718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1719(int& val) {
    func_in_1719(val);
    const int x = 1719;
    std::cout << 1719;
    for (int i = 0; i < 1719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1719(int& val) {
    const int x = 1719;
    std::cout << 1719;
    for (int i = 0; i < 1719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1720(int& val) {
    func_in_1720(val);
    const int x = 1720;
    std::cout << 1720;
    for (int i = 0; i < 1720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1720(int& val) {
    const int x = 1720;
    std::cout << 1720;
    for (int i = 0; i < 1720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1721(int& val) {
    func_in_1721(val);
    const int x = 1721;
    std::cout << 1721;
    for (int i = 0; i < 1721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1721(int& val) {
    const int x = 1721;
    std::cout << 1721;
    for (int i = 0; i < 1721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1722(int& val) {
    func_in_1722(val);
    const int x = 1722;
    std::cout << 1722;
    for (int i = 0; i < 1722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1722(int& val) {
    const int x = 1722;
    std::cout << 1722;
    for (int i = 0; i < 1722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1723(int& val) {
    func_in_1723(val);
    const int x = 1723;
    std::cout << 1723;
    for (int i = 0; i < 1723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1723(int& val) {
    const int x = 1723;
    std::cout << 1723;
    for (int i = 0; i < 1723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1724(int& val) {
    func_in_1724(val);
    const int x = 1724;
    std::cout << 1724;
    for (int i = 0; i < 1724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1724(int& val) {
    const int x = 1724;
    std::cout << 1724;
    for (int i = 0; i < 1724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1725(int& val) {
    func_in_1725(val);
    const int x = 1725;
    std::cout << 1725;
    for (int i = 0; i < 1725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1725(int& val) {
    const int x = 1725;
    std::cout << 1725;
    for (int i = 0; i < 1725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1726(int& val) {
    func_in_1726(val);
    const int x = 1726;
    std::cout << 1726;
    for (int i = 0; i < 1726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1726(int& val) {
    const int x = 1726;
    std::cout << 1726;
    for (int i = 0; i < 1726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1727(int& val) {
    func_in_1727(val);
    const int x = 1727;
    std::cout << 1727;
    for (int i = 0; i < 1727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1727(int& val) {
    const int x = 1727;
    std::cout << 1727;
    for (int i = 0; i < 1727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1728(int& val) {
    func_in_1728(val);
    const int x = 1728;
    std::cout << 1728;
    for (int i = 0; i < 1728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1728(int& val) {
    const int x = 1728;
    std::cout << 1728;
    for (int i = 0; i < 1728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1729(int& val) {
    func_in_1729(val);
    const int x = 1729;
    std::cout << 1729;
    for (int i = 0; i < 1729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1729(int& val) {
    const int x = 1729;
    std::cout << 1729;
    for (int i = 0; i < 1729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1730(int& val) {
    func_in_1730(val);
    const int x = 1730;
    std::cout << 1730;
    for (int i = 0; i < 1730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1730(int& val) {
    const int x = 1730;
    std::cout << 1730;
    for (int i = 0; i < 1730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1731(int& val) {
    func_in_1731(val);
    const int x = 1731;
    std::cout << 1731;
    for (int i = 0; i < 1731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1731(int& val) {
    const int x = 1731;
    std::cout << 1731;
    for (int i = 0; i < 1731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1732(int& val) {
    func_in_1732(val);
    const int x = 1732;
    std::cout << 1732;
    for (int i = 0; i < 1732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1732(int& val) {
    const int x = 1732;
    std::cout << 1732;
    for (int i = 0; i < 1732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1733(int& val) {
    func_in_1733(val);
    const int x = 1733;
    std::cout << 1733;
    for (int i = 0; i < 1733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1733(int& val) {
    const int x = 1733;
    std::cout << 1733;
    for (int i = 0; i < 1733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1734(int& val) {
    func_in_1734(val);
    const int x = 1734;
    std::cout << 1734;
    for (int i = 0; i < 1734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1734(int& val) {
    const int x = 1734;
    std::cout << 1734;
    for (int i = 0; i < 1734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1735(int& val) {
    func_in_1735(val);
    const int x = 1735;
    std::cout << 1735;
    for (int i = 0; i < 1735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1735(int& val) {
    const int x = 1735;
    std::cout << 1735;
    for (int i = 0; i < 1735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1736(int& val) {
    func_in_1736(val);
    const int x = 1736;
    std::cout << 1736;
    for (int i = 0; i < 1736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1736(int& val) {
    const int x = 1736;
    std::cout << 1736;
    for (int i = 0; i < 1736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1737(int& val) {
    func_in_1737(val);
    const int x = 1737;
    std::cout << 1737;
    for (int i = 0; i < 1737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1737(int& val) {
    const int x = 1737;
    std::cout << 1737;
    for (int i = 0; i < 1737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1738(int& val) {
    func_in_1738(val);
    const int x = 1738;
    std::cout << 1738;
    for (int i = 0; i < 1738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1738(int& val) {
    const int x = 1738;
    std::cout << 1738;
    for (int i = 0; i < 1738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1739(int& val) {
    func_in_1739(val);
    const int x = 1739;
    std::cout << 1739;
    for (int i = 0; i < 1739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1739(int& val) {
    const int x = 1739;
    std::cout << 1739;
    for (int i = 0; i < 1739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1740(int& val) {
    func_in_1740(val);
    const int x = 1740;
    std::cout << 1740;
    for (int i = 0; i < 1740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1740(int& val) {
    const int x = 1740;
    std::cout << 1740;
    for (int i = 0; i < 1740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1741(int& val) {
    func_in_1741(val);
    const int x = 1741;
    std::cout << 1741;
    for (int i = 0; i < 1741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1741(int& val) {
    const int x = 1741;
    std::cout << 1741;
    for (int i = 0; i < 1741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1742(int& val) {
    func_in_1742(val);
    const int x = 1742;
    std::cout << 1742;
    for (int i = 0; i < 1742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1742(int& val) {
    const int x = 1742;
    std::cout << 1742;
    for (int i = 0; i < 1742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1743(int& val) {
    func_in_1743(val);
    const int x = 1743;
    std::cout << 1743;
    for (int i = 0; i < 1743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1743(int& val) {
    const int x = 1743;
    std::cout << 1743;
    for (int i = 0; i < 1743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1744(int& val) {
    func_in_1744(val);
    const int x = 1744;
    std::cout << 1744;
    for (int i = 0; i < 1744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1744(int& val) {
    const int x = 1744;
    std::cout << 1744;
    for (int i = 0; i < 1744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1745(int& val) {
    func_in_1745(val);
    const int x = 1745;
    std::cout << 1745;
    for (int i = 0; i < 1745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1745(int& val) {
    const int x = 1745;
    std::cout << 1745;
    for (int i = 0; i < 1745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1746(int& val) {
    func_in_1746(val);
    const int x = 1746;
    std::cout << 1746;
    for (int i = 0; i < 1746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1746(int& val) {
    const int x = 1746;
    std::cout << 1746;
    for (int i = 0; i < 1746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1747(int& val) {
    func_in_1747(val);
    const int x = 1747;
    std::cout << 1747;
    for (int i = 0; i < 1747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1747(int& val) {
    const int x = 1747;
    std::cout << 1747;
    for (int i = 0; i < 1747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1748(int& val) {
    func_in_1748(val);
    const int x = 1748;
    std::cout << 1748;
    for (int i = 0; i < 1748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1748(int& val) {
    const int x = 1748;
    std::cout << 1748;
    for (int i = 0; i < 1748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1749(int& val) {
    func_in_1749(val);
    const int x = 1749;
    std::cout << 1749;
    for (int i = 0; i < 1749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1749(int& val) {
    const int x = 1749;
    std::cout << 1749;
    for (int i = 0; i < 1749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1750(int& val) {
    func_in_1750(val);
    const int x = 1750;
    std::cout << 1750;
    for (int i = 0; i < 1750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1750(int& val) {
    const int x = 1750;
    std::cout << 1750;
    for (int i = 0; i < 1750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1751(int& val) {
    func_in_1751(val);
    const int x = 1751;
    std::cout << 1751;
    for (int i = 0; i < 1751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1751(int& val) {
    const int x = 1751;
    std::cout << 1751;
    for (int i = 0; i < 1751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1752(int& val) {
    func_in_1752(val);
    const int x = 1752;
    std::cout << 1752;
    for (int i = 0; i < 1752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1752(int& val) {
    const int x = 1752;
    std::cout << 1752;
    for (int i = 0; i < 1752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1753(int& val) {
    func_in_1753(val);
    const int x = 1753;
    std::cout << 1753;
    for (int i = 0; i < 1753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1753(int& val) {
    const int x = 1753;
    std::cout << 1753;
    for (int i = 0; i < 1753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1754(int& val) {
    func_in_1754(val);
    const int x = 1754;
    std::cout << 1754;
    for (int i = 0; i < 1754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1754(int& val) {
    const int x = 1754;
    std::cout << 1754;
    for (int i = 0; i < 1754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1755(int& val) {
    func_in_1755(val);
    const int x = 1755;
    std::cout << 1755;
    for (int i = 0; i < 1755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1755(int& val) {
    const int x = 1755;
    std::cout << 1755;
    for (int i = 0; i < 1755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1756(int& val) {
    func_in_1756(val);
    const int x = 1756;
    std::cout << 1756;
    for (int i = 0; i < 1756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1756(int& val) {
    const int x = 1756;
    std::cout << 1756;
    for (int i = 0; i < 1756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1757(int& val) {
    func_in_1757(val);
    const int x = 1757;
    std::cout << 1757;
    for (int i = 0; i < 1757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1757(int& val) {
    const int x = 1757;
    std::cout << 1757;
    for (int i = 0; i < 1757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1758(int& val) {
    func_in_1758(val);
    const int x = 1758;
    std::cout << 1758;
    for (int i = 0; i < 1758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1758(int& val) {
    const int x = 1758;
    std::cout << 1758;
    for (int i = 0; i < 1758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1759(int& val) {
    func_in_1759(val);
    const int x = 1759;
    std::cout << 1759;
    for (int i = 0; i < 1759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1759(int& val) {
    const int x = 1759;
    std::cout << 1759;
    for (int i = 0; i < 1759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1760(int& val) {
    func_in_1760(val);
    const int x = 1760;
    std::cout << 1760;
    for (int i = 0; i < 1760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1760(int& val) {
    const int x = 1760;
    std::cout << 1760;
    for (int i = 0; i < 1760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1761(int& val) {
    func_in_1761(val);
    const int x = 1761;
    std::cout << 1761;
    for (int i = 0; i < 1761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1761(int& val) {
    const int x = 1761;
    std::cout << 1761;
    for (int i = 0; i < 1761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1762(int& val) {
    func_in_1762(val);
    const int x = 1762;
    std::cout << 1762;
    for (int i = 0; i < 1762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1762(int& val) {
    const int x = 1762;
    std::cout << 1762;
    for (int i = 0; i < 1762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1763(int& val) {
    func_in_1763(val);
    const int x = 1763;
    std::cout << 1763;
    for (int i = 0; i < 1763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1763(int& val) {
    const int x = 1763;
    std::cout << 1763;
    for (int i = 0; i < 1763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1764(int& val) {
    func_in_1764(val);
    const int x = 1764;
    std::cout << 1764;
    for (int i = 0; i < 1764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1764(int& val) {
    const int x = 1764;
    std::cout << 1764;
    for (int i = 0; i < 1764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1765(int& val) {
    func_in_1765(val);
    const int x = 1765;
    std::cout << 1765;
    for (int i = 0; i < 1765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1765(int& val) {
    const int x = 1765;
    std::cout << 1765;
    for (int i = 0; i < 1765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1766(int& val) {
    func_in_1766(val);
    const int x = 1766;
    std::cout << 1766;
    for (int i = 0; i < 1766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1766(int& val) {
    const int x = 1766;
    std::cout << 1766;
    for (int i = 0; i < 1766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1767(int& val) {
    func_in_1767(val);
    const int x = 1767;
    std::cout << 1767;
    for (int i = 0; i < 1767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1767(int& val) {
    const int x = 1767;
    std::cout << 1767;
    for (int i = 0; i < 1767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1768(int& val) {
    func_in_1768(val);
    const int x = 1768;
    std::cout << 1768;
    for (int i = 0; i < 1768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1768(int& val) {
    const int x = 1768;
    std::cout << 1768;
    for (int i = 0; i < 1768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1769(int& val) {
    func_in_1769(val);
    const int x = 1769;
    std::cout << 1769;
    for (int i = 0; i < 1769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1769(int& val) {
    const int x = 1769;
    std::cout << 1769;
    for (int i = 0; i < 1769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1770(int& val) {
    func_in_1770(val);
    const int x = 1770;
    std::cout << 1770;
    for (int i = 0; i < 1770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1770(int& val) {
    const int x = 1770;
    std::cout << 1770;
    for (int i = 0; i < 1770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1771(int& val) {
    func_in_1771(val);
    const int x = 1771;
    std::cout << 1771;
    for (int i = 0; i < 1771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1771(int& val) {
    const int x = 1771;
    std::cout << 1771;
    for (int i = 0; i < 1771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1772(int& val) {
    func_in_1772(val);
    const int x = 1772;
    std::cout << 1772;
    for (int i = 0; i < 1772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1772(int& val) {
    const int x = 1772;
    std::cout << 1772;
    for (int i = 0; i < 1772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1773(int& val) {
    func_in_1773(val);
    const int x = 1773;
    std::cout << 1773;
    for (int i = 0; i < 1773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1773(int& val) {
    const int x = 1773;
    std::cout << 1773;
    for (int i = 0; i < 1773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1774(int& val) {
    func_in_1774(val);
    const int x = 1774;
    std::cout << 1774;
    for (int i = 0; i < 1774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1774(int& val) {
    const int x = 1774;
    std::cout << 1774;
    for (int i = 0; i < 1774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1775(int& val) {
    func_in_1775(val);
    const int x = 1775;
    std::cout << 1775;
    for (int i = 0; i < 1775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1775(int& val) {
    const int x = 1775;
    std::cout << 1775;
    for (int i = 0; i < 1775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1776(int& val) {
    func_in_1776(val);
    const int x = 1776;
    std::cout << 1776;
    for (int i = 0; i < 1776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1776(int& val) {
    const int x = 1776;
    std::cout << 1776;
    for (int i = 0; i < 1776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1777(int& val) {
    func_in_1777(val);
    const int x = 1777;
    std::cout << 1777;
    for (int i = 0; i < 1777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1777(int& val) {
    const int x = 1777;
    std::cout << 1777;
    for (int i = 0; i < 1777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1778(int& val) {
    func_in_1778(val);
    const int x = 1778;
    std::cout << 1778;
    for (int i = 0; i < 1778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1778(int& val) {
    const int x = 1778;
    std::cout << 1778;
    for (int i = 0; i < 1778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1779(int& val) {
    func_in_1779(val);
    const int x = 1779;
    std::cout << 1779;
    for (int i = 0; i < 1779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1779(int& val) {
    const int x = 1779;
    std::cout << 1779;
    for (int i = 0; i < 1779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1780(int& val) {
    func_in_1780(val);
    const int x = 1780;
    std::cout << 1780;
    for (int i = 0; i < 1780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1780(int& val) {
    const int x = 1780;
    std::cout << 1780;
    for (int i = 0; i < 1780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1781(int& val) {
    func_in_1781(val);
    const int x = 1781;
    std::cout << 1781;
    for (int i = 0; i < 1781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1781(int& val) {
    const int x = 1781;
    std::cout << 1781;
    for (int i = 0; i < 1781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1782(int& val) {
    func_in_1782(val);
    const int x = 1782;
    std::cout << 1782;
    for (int i = 0; i < 1782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1782(int& val) {
    const int x = 1782;
    std::cout << 1782;
    for (int i = 0; i < 1782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1783(int& val) {
    func_in_1783(val);
    const int x = 1783;
    std::cout << 1783;
    for (int i = 0; i < 1783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1783(int& val) {
    const int x = 1783;
    std::cout << 1783;
    for (int i = 0; i < 1783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1784(int& val) {
    func_in_1784(val);
    const int x = 1784;
    std::cout << 1784;
    for (int i = 0; i < 1784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1784(int& val) {
    const int x = 1784;
    std::cout << 1784;
    for (int i = 0; i < 1784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1785(int& val) {
    func_in_1785(val);
    const int x = 1785;
    std::cout << 1785;
    for (int i = 0; i < 1785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1785(int& val) {
    const int x = 1785;
    std::cout << 1785;
    for (int i = 0; i < 1785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1786(int& val) {
    func_in_1786(val);
    const int x = 1786;
    std::cout << 1786;
    for (int i = 0; i < 1786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1786(int& val) {
    const int x = 1786;
    std::cout << 1786;
    for (int i = 0; i < 1786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1787(int& val) {
    func_in_1787(val);
    const int x = 1787;
    std::cout << 1787;
    for (int i = 0; i < 1787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1787(int& val) {
    const int x = 1787;
    std::cout << 1787;
    for (int i = 0; i < 1787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1788(int& val) {
    func_in_1788(val);
    const int x = 1788;
    std::cout << 1788;
    for (int i = 0; i < 1788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1788(int& val) {
    const int x = 1788;
    std::cout << 1788;
    for (int i = 0; i < 1788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1789(int& val) {
    func_in_1789(val);
    const int x = 1789;
    std::cout << 1789;
    for (int i = 0; i < 1789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1789(int& val) {
    const int x = 1789;
    std::cout << 1789;
    for (int i = 0; i < 1789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1790(int& val) {
    func_in_1790(val);
    const int x = 1790;
    std::cout << 1790;
    for (int i = 0; i < 1790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1790(int& val) {
    const int x = 1790;
    std::cout << 1790;
    for (int i = 0; i < 1790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1791(int& val) {
    func_in_1791(val);
    const int x = 1791;
    std::cout << 1791;
    for (int i = 0; i < 1791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1791(int& val) {
    const int x = 1791;
    std::cout << 1791;
    for (int i = 0; i < 1791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1792(int& val) {
    func_in_1792(val);
    const int x = 1792;
    std::cout << 1792;
    for (int i = 0; i < 1792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1792(int& val) {
    const int x = 1792;
    std::cout << 1792;
    for (int i = 0; i < 1792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1793(int& val) {
    func_in_1793(val);
    const int x = 1793;
    std::cout << 1793;
    for (int i = 0; i < 1793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1793(int& val) {
    const int x = 1793;
    std::cout << 1793;
    for (int i = 0; i < 1793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1794(int& val) {
    func_in_1794(val);
    const int x = 1794;
    std::cout << 1794;
    for (int i = 0; i < 1794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1794(int& val) {
    const int x = 1794;
    std::cout << 1794;
    for (int i = 0; i < 1794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1795(int& val) {
    func_in_1795(val);
    const int x = 1795;
    std::cout << 1795;
    for (int i = 0; i < 1795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1795(int& val) {
    const int x = 1795;
    std::cout << 1795;
    for (int i = 0; i < 1795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1796(int& val) {
    func_in_1796(val);
    const int x = 1796;
    std::cout << 1796;
    for (int i = 0; i < 1796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1796(int& val) {
    const int x = 1796;
    std::cout << 1796;
    for (int i = 0; i < 1796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1797(int& val) {
    func_in_1797(val);
    const int x = 1797;
    std::cout << 1797;
    for (int i = 0; i < 1797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1797(int& val) {
    const int x = 1797;
    std::cout << 1797;
    for (int i = 0; i < 1797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1798(int& val) {
    func_in_1798(val);
    const int x = 1798;
    std::cout << 1798;
    for (int i = 0; i < 1798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1798(int& val) {
    const int x = 1798;
    std::cout << 1798;
    for (int i = 0; i < 1798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1799(int& val) {
    func_in_1799(val);
    const int x = 1799;
    std::cout << 1799;
    for (int i = 0; i < 1799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1799(int& val) {
    const int x = 1799;
    std::cout << 1799;
    for (int i = 0; i < 1799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1800(int& val) {
    func_in_1800(val);
    const int x = 1800;
    std::cout << 1800;
    for (int i = 0; i < 1800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1800(int& val) {
    const int x = 1800;
    std::cout << 1800;
    for (int i = 0; i < 1800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1801(int& val) {
    func_in_1801(val);
    const int x = 1801;
    std::cout << 1801;
    for (int i = 0; i < 1801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1801(int& val) {
    const int x = 1801;
    std::cout << 1801;
    for (int i = 0; i < 1801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1802(int& val) {
    func_in_1802(val);
    const int x = 1802;
    std::cout << 1802;
    for (int i = 0; i < 1802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1802(int& val) {
    const int x = 1802;
    std::cout << 1802;
    for (int i = 0; i < 1802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1803(int& val) {
    func_in_1803(val);
    const int x = 1803;
    std::cout << 1803;
    for (int i = 0; i < 1803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1803(int& val) {
    const int x = 1803;
    std::cout << 1803;
    for (int i = 0; i < 1803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1804(int& val) {
    func_in_1804(val);
    const int x = 1804;
    std::cout << 1804;
    for (int i = 0; i < 1804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1804(int& val) {
    const int x = 1804;
    std::cout << 1804;
    for (int i = 0; i < 1804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1805(int& val) {
    func_in_1805(val);
    const int x = 1805;
    std::cout << 1805;
    for (int i = 0; i < 1805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1805(int& val) {
    const int x = 1805;
    std::cout << 1805;
    for (int i = 0; i < 1805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1806(int& val) {
    func_in_1806(val);
    const int x = 1806;
    std::cout << 1806;
    for (int i = 0; i < 1806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1806(int& val) {
    const int x = 1806;
    std::cout << 1806;
    for (int i = 0; i < 1806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1807(int& val) {
    func_in_1807(val);
    const int x = 1807;
    std::cout << 1807;
    for (int i = 0; i < 1807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1807(int& val) {
    const int x = 1807;
    std::cout << 1807;
    for (int i = 0; i < 1807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1808(int& val) {
    func_in_1808(val);
    const int x = 1808;
    std::cout << 1808;
    for (int i = 0; i < 1808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1808(int& val) {
    const int x = 1808;
    std::cout << 1808;
    for (int i = 0; i < 1808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1809(int& val) {
    func_in_1809(val);
    const int x = 1809;
    std::cout << 1809;
    for (int i = 0; i < 1809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1809(int& val) {
    const int x = 1809;
    std::cout << 1809;
    for (int i = 0; i < 1809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1810(int& val) {
    func_in_1810(val);
    const int x = 1810;
    std::cout << 1810;
    for (int i = 0; i < 1810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1810(int& val) {
    const int x = 1810;
    std::cout << 1810;
    for (int i = 0; i < 1810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1811(int& val) {
    func_in_1811(val);
    const int x = 1811;
    std::cout << 1811;
    for (int i = 0; i < 1811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1811(int& val) {
    const int x = 1811;
    std::cout << 1811;
    for (int i = 0; i < 1811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1812(int& val) {
    func_in_1812(val);
    const int x = 1812;
    std::cout << 1812;
    for (int i = 0; i < 1812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1812(int& val) {
    const int x = 1812;
    std::cout << 1812;
    for (int i = 0; i < 1812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1813(int& val) {
    func_in_1813(val);
    const int x = 1813;
    std::cout << 1813;
    for (int i = 0; i < 1813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1813(int& val) {
    const int x = 1813;
    std::cout << 1813;
    for (int i = 0; i < 1813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1814(int& val) {
    func_in_1814(val);
    const int x = 1814;
    std::cout << 1814;
    for (int i = 0; i < 1814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1814(int& val) {
    const int x = 1814;
    std::cout << 1814;
    for (int i = 0; i < 1814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1815(int& val) {
    func_in_1815(val);
    const int x = 1815;
    std::cout << 1815;
    for (int i = 0; i < 1815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1815(int& val) {
    const int x = 1815;
    std::cout << 1815;
    for (int i = 0; i < 1815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1816(int& val) {
    func_in_1816(val);
    const int x = 1816;
    std::cout << 1816;
    for (int i = 0; i < 1816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1816(int& val) {
    const int x = 1816;
    std::cout << 1816;
    for (int i = 0; i < 1816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1817(int& val) {
    func_in_1817(val);
    const int x = 1817;
    std::cout << 1817;
    for (int i = 0; i < 1817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1817(int& val) {
    const int x = 1817;
    std::cout << 1817;
    for (int i = 0; i < 1817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1818(int& val) {
    func_in_1818(val);
    const int x = 1818;
    std::cout << 1818;
    for (int i = 0; i < 1818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1818(int& val) {
    const int x = 1818;
    std::cout << 1818;
    for (int i = 0; i < 1818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1819(int& val) {
    func_in_1819(val);
    const int x = 1819;
    std::cout << 1819;
    for (int i = 0; i < 1819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1819(int& val) {
    const int x = 1819;
    std::cout << 1819;
    for (int i = 0; i < 1819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1820(int& val) {
    func_in_1820(val);
    const int x = 1820;
    std::cout << 1820;
    for (int i = 0; i < 1820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1820(int& val) {
    const int x = 1820;
    std::cout << 1820;
    for (int i = 0; i < 1820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1821(int& val) {
    func_in_1821(val);
    const int x = 1821;
    std::cout << 1821;
    for (int i = 0; i < 1821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1821(int& val) {
    const int x = 1821;
    std::cout << 1821;
    for (int i = 0; i < 1821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1822(int& val) {
    func_in_1822(val);
    const int x = 1822;
    std::cout << 1822;
    for (int i = 0; i < 1822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1822(int& val) {
    const int x = 1822;
    std::cout << 1822;
    for (int i = 0; i < 1822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1823(int& val) {
    func_in_1823(val);
    const int x = 1823;
    std::cout << 1823;
    for (int i = 0; i < 1823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1823(int& val) {
    const int x = 1823;
    std::cout << 1823;
    for (int i = 0; i < 1823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1824(int& val) {
    func_in_1824(val);
    const int x = 1824;
    std::cout << 1824;
    for (int i = 0; i < 1824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1824(int& val) {
    const int x = 1824;
    std::cout << 1824;
    for (int i = 0; i < 1824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1825(int& val) {
    func_in_1825(val);
    const int x = 1825;
    std::cout << 1825;
    for (int i = 0; i < 1825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1825(int& val) {
    const int x = 1825;
    std::cout << 1825;
    for (int i = 0; i < 1825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1826(int& val) {
    func_in_1826(val);
    const int x = 1826;
    std::cout << 1826;
    for (int i = 0; i < 1826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1826(int& val) {
    const int x = 1826;
    std::cout << 1826;
    for (int i = 0; i < 1826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1827(int& val) {
    func_in_1827(val);
    const int x = 1827;
    std::cout << 1827;
    for (int i = 0; i < 1827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1827(int& val) {
    const int x = 1827;
    std::cout << 1827;
    for (int i = 0; i < 1827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1828(int& val) {
    func_in_1828(val);
    const int x = 1828;
    std::cout << 1828;
    for (int i = 0; i < 1828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1828(int& val) {
    const int x = 1828;
    std::cout << 1828;
    for (int i = 0; i < 1828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1829(int& val) {
    func_in_1829(val);
    const int x = 1829;
    std::cout << 1829;
    for (int i = 0; i < 1829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1829(int& val) {
    const int x = 1829;
    std::cout << 1829;
    for (int i = 0; i < 1829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1830(int& val) {
    func_in_1830(val);
    const int x = 1830;
    std::cout << 1830;
    for (int i = 0; i < 1830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1830(int& val) {
    const int x = 1830;
    std::cout << 1830;
    for (int i = 0; i < 1830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1831(int& val) {
    func_in_1831(val);
    const int x = 1831;
    std::cout << 1831;
    for (int i = 0; i < 1831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1831(int& val) {
    const int x = 1831;
    std::cout << 1831;
    for (int i = 0; i < 1831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1832(int& val) {
    func_in_1832(val);
    const int x = 1832;
    std::cout << 1832;
    for (int i = 0; i < 1832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1832(int& val) {
    const int x = 1832;
    std::cout << 1832;
    for (int i = 0; i < 1832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1833(int& val) {
    func_in_1833(val);
    const int x = 1833;
    std::cout << 1833;
    for (int i = 0; i < 1833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1833(int& val) {
    const int x = 1833;
    std::cout << 1833;
    for (int i = 0; i < 1833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1834(int& val) {
    func_in_1834(val);
    const int x = 1834;
    std::cout << 1834;
    for (int i = 0; i < 1834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1834(int& val) {
    const int x = 1834;
    std::cout << 1834;
    for (int i = 0; i < 1834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1835(int& val) {
    func_in_1835(val);
    const int x = 1835;
    std::cout << 1835;
    for (int i = 0; i < 1835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1835(int& val) {
    const int x = 1835;
    std::cout << 1835;
    for (int i = 0; i < 1835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1836(int& val) {
    func_in_1836(val);
    const int x = 1836;
    std::cout << 1836;
    for (int i = 0; i < 1836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1836(int& val) {
    const int x = 1836;
    std::cout << 1836;
    for (int i = 0; i < 1836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1837(int& val) {
    func_in_1837(val);
    const int x = 1837;
    std::cout << 1837;
    for (int i = 0; i < 1837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1837(int& val) {
    const int x = 1837;
    std::cout << 1837;
    for (int i = 0; i < 1837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1838(int& val) {
    func_in_1838(val);
    const int x = 1838;
    std::cout << 1838;
    for (int i = 0; i < 1838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1838(int& val) {
    const int x = 1838;
    std::cout << 1838;
    for (int i = 0; i < 1838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1839(int& val) {
    func_in_1839(val);
    const int x = 1839;
    std::cout << 1839;
    for (int i = 0; i < 1839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1839(int& val) {
    const int x = 1839;
    std::cout << 1839;
    for (int i = 0; i < 1839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1840(int& val) {
    func_in_1840(val);
    const int x = 1840;
    std::cout << 1840;
    for (int i = 0; i < 1840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1840(int& val) {
    const int x = 1840;
    std::cout << 1840;
    for (int i = 0; i < 1840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1841(int& val) {
    func_in_1841(val);
    const int x = 1841;
    std::cout << 1841;
    for (int i = 0; i < 1841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1841(int& val) {
    const int x = 1841;
    std::cout << 1841;
    for (int i = 0; i < 1841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1842(int& val) {
    func_in_1842(val);
    const int x = 1842;
    std::cout << 1842;
    for (int i = 0; i < 1842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1842(int& val) {
    const int x = 1842;
    std::cout << 1842;
    for (int i = 0; i < 1842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1843(int& val) {
    func_in_1843(val);
    const int x = 1843;
    std::cout << 1843;
    for (int i = 0; i < 1843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1843(int& val) {
    const int x = 1843;
    std::cout << 1843;
    for (int i = 0; i < 1843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1844(int& val) {
    func_in_1844(val);
    const int x = 1844;
    std::cout << 1844;
    for (int i = 0; i < 1844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1844(int& val) {
    const int x = 1844;
    std::cout << 1844;
    for (int i = 0; i < 1844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1845(int& val) {
    func_in_1845(val);
    const int x = 1845;
    std::cout << 1845;
    for (int i = 0; i < 1845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1845(int& val) {
    const int x = 1845;
    std::cout << 1845;
    for (int i = 0; i < 1845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1846(int& val) {
    func_in_1846(val);
    const int x = 1846;
    std::cout << 1846;
    for (int i = 0; i < 1846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1846(int& val) {
    const int x = 1846;
    std::cout << 1846;
    for (int i = 0; i < 1846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1847(int& val) {
    func_in_1847(val);
    const int x = 1847;
    std::cout << 1847;
    for (int i = 0; i < 1847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1847(int& val) {
    const int x = 1847;
    std::cout << 1847;
    for (int i = 0; i < 1847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1848(int& val) {
    func_in_1848(val);
    const int x = 1848;
    std::cout << 1848;
    for (int i = 0; i < 1848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1848(int& val) {
    const int x = 1848;
    std::cout << 1848;
    for (int i = 0; i < 1848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1849(int& val) {
    func_in_1849(val);
    const int x = 1849;
    std::cout << 1849;
    for (int i = 0; i < 1849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1849(int& val) {
    const int x = 1849;
    std::cout << 1849;
    for (int i = 0; i < 1849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1850(int& val) {
    func_in_1850(val);
    const int x = 1850;
    std::cout << 1850;
    for (int i = 0; i < 1850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1850(int& val) {
    const int x = 1850;
    std::cout << 1850;
    for (int i = 0; i < 1850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1851(int& val) {
    func_in_1851(val);
    const int x = 1851;
    std::cout << 1851;
    for (int i = 0; i < 1851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1851(int& val) {
    const int x = 1851;
    std::cout << 1851;
    for (int i = 0; i < 1851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1852(int& val) {
    func_in_1852(val);
    const int x = 1852;
    std::cout << 1852;
    for (int i = 0; i < 1852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1852(int& val) {
    const int x = 1852;
    std::cout << 1852;
    for (int i = 0; i < 1852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1853(int& val) {
    func_in_1853(val);
    const int x = 1853;
    std::cout << 1853;
    for (int i = 0; i < 1853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1853(int& val) {
    const int x = 1853;
    std::cout << 1853;
    for (int i = 0; i < 1853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1854(int& val) {
    func_in_1854(val);
    const int x = 1854;
    std::cout << 1854;
    for (int i = 0; i < 1854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1854(int& val) {
    const int x = 1854;
    std::cout << 1854;
    for (int i = 0; i < 1854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1855(int& val) {
    func_in_1855(val);
    const int x = 1855;
    std::cout << 1855;
    for (int i = 0; i < 1855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1855(int& val) {
    const int x = 1855;
    std::cout << 1855;
    for (int i = 0; i < 1855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1856(int& val) {
    func_in_1856(val);
    const int x = 1856;
    std::cout << 1856;
    for (int i = 0; i < 1856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1856(int& val) {
    const int x = 1856;
    std::cout << 1856;
    for (int i = 0; i < 1856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1857(int& val) {
    func_in_1857(val);
    const int x = 1857;
    std::cout << 1857;
    for (int i = 0; i < 1857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1857(int& val) {
    const int x = 1857;
    std::cout << 1857;
    for (int i = 0; i < 1857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1858(int& val) {
    func_in_1858(val);
    const int x = 1858;
    std::cout << 1858;
    for (int i = 0; i < 1858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1858(int& val) {
    const int x = 1858;
    std::cout << 1858;
    for (int i = 0; i < 1858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1859(int& val) {
    func_in_1859(val);
    const int x = 1859;
    std::cout << 1859;
    for (int i = 0; i < 1859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1859(int& val) {
    const int x = 1859;
    std::cout << 1859;
    for (int i = 0; i < 1859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1860(int& val) {
    func_in_1860(val);
    const int x = 1860;
    std::cout << 1860;
    for (int i = 0; i < 1860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1860(int& val) {
    const int x = 1860;
    std::cout << 1860;
    for (int i = 0; i < 1860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1861(int& val) {
    func_in_1861(val);
    const int x = 1861;
    std::cout << 1861;
    for (int i = 0; i < 1861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1861(int& val) {
    const int x = 1861;
    std::cout << 1861;
    for (int i = 0; i < 1861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1862(int& val) {
    func_in_1862(val);
    const int x = 1862;
    std::cout << 1862;
    for (int i = 0; i < 1862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1862(int& val) {
    const int x = 1862;
    std::cout << 1862;
    for (int i = 0; i < 1862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1863(int& val) {
    func_in_1863(val);
    const int x = 1863;
    std::cout << 1863;
    for (int i = 0; i < 1863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1863(int& val) {
    const int x = 1863;
    std::cout << 1863;
    for (int i = 0; i < 1863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1864(int& val) {
    func_in_1864(val);
    const int x = 1864;
    std::cout << 1864;
    for (int i = 0; i < 1864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1864(int& val) {
    const int x = 1864;
    std::cout << 1864;
    for (int i = 0; i < 1864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1865(int& val) {
    func_in_1865(val);
    const int x = 1865;
    std::cout << 1865;
    for (int i = 0; i < 1865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1865(int& val) {
    const int x = 1865;
    std::cout << 1865;
    for (int i = 0; i < 1865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1866(int& val) {
    func_in_1866(val);
    const int x = 1866;
    std::cout << 1866;
    for (int i = 0; i < 1866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1866(int& val) {
    const int x = 1866;
    std::cout << 1866;
    for (int i = 0; i < 1866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1867(int& val) {
    func_in_1867(val);
    const int x = 1867;
    std::cout << 1867;
    for (int i = 0; i < 1867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1867(int& val) {
    const int x = 1867;
    std::cout << 1867;
    for (int i = 0; i < 1867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1868(int& val) {
    func_in_1868(val);
    const int x = 1868;
    std::cout << 1868;
    for (int i = 0; i < 1868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1868(int& val) {
    const int x = 1868;
    std::cout << 1868;
    for (int i = 0; i < 1868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1869(int& val) {
    func_in_1869(val);
    const int x = 1869;
    std::cout << 1869;
    for (int i = 0; i < 1869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1869(int& val) {
    const int x = 1869;
    std::cout << 1869;
    for (int i = 0; i < 1869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1870(int& val) {
    func_in_1870(val);
    const int x = 1870;
    std::cout << 1870;
    for (int i = 0; i < 1870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1870(int& val) {
    const int x = 1870;
    std::cout << 1870;
    for (int i = 0; i < 1870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1871(int& val) {
    func_in_1871(val);
    const int x = 1871;
    std::cout << 1871;
    for (int i = 0; i < 1871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1871(int& val) {
    const int x = 1871;
    std::cout << 1871;
    for (int i = 0; i < 1871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1872(int& val) {
    func_in_1872(val);
    const int x = 1872;
    std::cout << 1872;
    for (int i = 0; i < 1872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1872(int& val) {
    const int x = 1872;
    std::cout << 1872;
    for (int i = 0; i < 1872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1873(int& val) {
    func_in_1873(val);
    const int x = 1873;
    std::cout << 1873;
    for (int i = 0; i < 1873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1873(int& val) {
    const int x = 1873;
    std::cout << 1873;
    for (int i = 0; i < 1873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1874(int& val) {
    func_in_1874(val);
    const int x = 1874;
    std::cout << 1874;
    for (int i = 0; i < 1874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1874(int& val) {
    const int x = 1874;
    std::cout << 1874;
    for (int i = 0; i < 1874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1875(int& val) {
    func_in_1875(val);
    const int x = 1875;
    std::cout << 1875;
    for (int i = 0; i < 1875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1875(int& val) {
    const int x = 1875;
    std::cout << 1875;
    for (int i = 0; i < 1875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1876(int& val) {
    func_in_1876(val);
    const int x = 1876;
    std::cout << 1876;
    for (int i = 0; i < 1876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1876(int& val) {
    const int x = 1876;
    std::cout << 1876;
    for (int i = 0; i < 1876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1877(int& val) {
    func_in_1877(val);
    const int x = 1877;
    std::cout << 1877;
    for (int i = 0; i < 1877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1877(int& val) {
    const int x = 1877;
    std::cout << 1877;
    for (int i = 0; i < 1877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1878(int& val) {
    func_in_1878(val);
    const int x = 1878;
    std::cout << 1878;
    for (int i = 0; i < 1878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1878(int& val) {
    const int x = 1878;
    std::cout << 1878;
    for (int i = 0; i < 1878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1879(int& val) {
    func_in_1879(val);
    const int x = 1879;
    std::cout << 1879;
    for (int i = 0; i < 1879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1879(int& val) {
    const int x = 1879;
    std::cout << 1879;
    for (int i = 0; i < 1879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1880(int& val) {
    func_in_1880(val);
    const int x = 1880;
    std::cout << 1880;
    for (int i = 0; i < 1880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1880(int& val) {
    const int x = 1880;
    std::cout << 1880;
    for (int i = 0; i < 1880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1881(int& val) {
    func_in_1881(val);
    const int x = 1881;
    std::cout << 1881;
    for (int i = 0; i < 1881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1881(int& val) {
    const int x = 1881;
    std::cout << 1881;
    for (int i = 0; i < 1881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1882(int& val) {
    func_in_1882(val);
    const int x = 1882;
    std::cout << 1882;
    for (int i = 0; i < 1882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1882(int& val) {
    const int x = 1882;
    std::cout << 1882;
    for (int i = 0; i < 1882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1883(int& val) {
    func_in_1883(val);
    const int x = 1883;
    std::cout << 1883;
    for (int i = 0; i < 1883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1883(int& val) {
    const int x = 1883;
    std::cout << 1883;
    for (int i = 0; i < 1883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1884(int& val) {
    func_in_1884(val);
    const int x = 1884;
    std::cout << 1884;
    for (int i = 0; i < 1884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1884(int& val) {
    const int x = 1884;
    std::cout << 1884;
    for (int i = 0; i < 1884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1885(int& val) {
    func_in_1885(val);
    const int x = 1885;
    std::cout << 1885;
    for (int i = 0; i < 1885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1885(int& val) {
    const int x = 1885;
    std::cout << 1885;
    for (int i = 0; i < 1885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1886(int& val) {
    func_in_1886(val);
    const int x = 1886;
    std::cout << 1886;
    for (int i = 0; i < 1886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1886(int& val) {
    const int x = 1886;
    std::cout << 1886;
    for (int i = 0; i < 1886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1887(int& val) {
    func_in_1887(val);
    const int x = 1887;
    std::cout << 1887;
    for (int i = 0; i < 1887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1887(int& val) {
    const int x = 1887;
    std::cout << 1887;
    for (int i = 0; i < 1887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1888(int& val) {
    func_in_1888(val);
    const int x = 1888;
    std::cout << 1888;
    for (int i = 0; i < 1888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1888(int& val) {
    const int x = 1888;
    std::cout << 1888;
    for (int i = 0; i < 1888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1889(int& val) {
    func_in_1889(val);
    const int x = 1889;
    std::cout << 1889;
    for (int i = 0; i < 1889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1889(int& val) {
    const int x = 1889;
    std::cout << 1889;
    for (int i = 0; i < 1889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1890(int& val) {
    func_in_1890(val);
    const int x = 1890;
    std::cout << 1890;
    for (int i = 0; i < 1890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1890(int& val) {
    const int x = 1890;
    std::cout << 1890;
    for (int i = 0; i < 1890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1891(int& val) {
    func_in_1891(val);
    const int x = 1891;
    std::cout << 1891;
    for (int i = 0; i < 1891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1891(int& val) {
    const int x = 1891;
    std::cout << 1891;
    for (int i = 0; i < 1891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1892(int& val) {
    func_in_1892(val);
    const int x = 1892;
    std::cout << 1892;
    for (int i = 0; i < 1892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1892(int& val) {
    const int x = 1892;
    std::cout << 1892;
    for (int i = 0; i < 1892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1893(int& val) {
    func_in_1893(val);
    const int x = 1893;
    std::cout << 1893;
    for (int i = 0; i < 1893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1893(int& val) {
    const int x = 1893;
    std::cout << 1893;
    for (int i = 0; i < 1893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1894(int& val) {
    func_in_1894(val);
    const int x = 1894;
    std::cout << 1894;
    for (int i = 0; i < 1894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1894(int& val) {
    const int x = 1894;
    std::cout << 1894;
    for (int i = 0; i < 1894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1895(int& val) {
    func_in_1895(val);
    const int x = 1895;
    std::cout << 1895;
    for (int i = 0; i < 1895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1895(int& val) {
    const int x = 1895;
    std::cout << 1895;
    for (int i = 0; i < 1895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1896(int& val) {
    func_in_1896(val);
    const int x = 1896;
    std::cout << 1896;
    for (int i = 0; i < 1896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1896(int& val) {
    const int x = 1896;
    std::cout << 1896;
    for (int i = 0; i < 1896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1897(int& val) {
    func_in_1897(val);
    const int x = 1897;
    std::cout << 1897;
    for (int i = 0; i < 1897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1897(int& val) {
    const int x = 1897;
    std::cout << 1897;
    for (int i = 0; i < 1897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1898(int& val) {
    func_in_1898(val);
    const int x = 1898;
    std::cout << 1898;
    for (int i = 0; i < 1898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1898(int& val) {
    const int x = 1898;
    std::cout << 1898;
    for (int i = 0; i < 1898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1899(int& val) {
    func_in_1899(val);
    const int x = 1899;
    std::cout << 1899;
    for (int i = 0; i < 1899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1899(int& val) {
    const int x = 1899;
    std::cout << 1899;
    for (int i = 0; i < 1899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1900(int& val) {
    func_in_1900(val);
    const int x = 1900;
    std::cout << 1900;
    for (int i = 0; i < 1900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1900(int& val) {
    const int x = 1900;
    std::cout << 1900;
    for (int i = 0; i < 1900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1901(int& val) {
    func_in_1901(val);
    const int x = 1901;
    std::cout << 1901;
    for (int i = 0; i < 1901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1901(int& val) {
    const int x = 1901;
    std::cout << 1901;
    for (int i = 0; i < 1901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1902(int& val) {
    func_in_1902(val);
    const int x = 1902;
    std::cout << 1902;
    for (int i = 0; i < 1902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1902(int& val) {
    const int x = 1902;
    std::cout << 1902;
    for (int i = 0; i < 1902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1903(int& val) {
    func_in_1903(val);
    const int x = 1903;
    std::cout << 1903;
    for (int i = 0; i < 1903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1903(int& val) {
    const int x = 1903;
    std::cout << 1903;
    for (int i = 0; i < 1903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1904(int& val) {
    func_in_1904(val);
    const int x = 1904;
    std::cout << 1904;
    for (int i = 0; i < 1904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1904(int& val) {
    const int x = 1904;
    std::cout << 1904;
    for (int i = 0; i < 1904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1905(int& val) {
    func_in_1905(val);
    const int x = 1905;
    std::cout << 1905;
    for (int i = 0; i < 1905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1905(int& val) {
    const int x = 1905;
    std::cout << 1905;
    for (int i = 0; i < 1905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1906(int& val) {
    func_in_1906(val);
    const int x = 1906;
    std::cout << 1906;
    for (int i = 0; i < 1906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1906(int& val) {
    const int x = 1906;
    std::cout << 1906;
    for (int i = 0; i < 1906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1907(int& val) {
    func_in_1907(val);
    const int x = 1907;
    std::cout << 1907;
    for (int i = 0; i < 1907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1907(int& val) {
    const int x = 1907;
    std::cout << 1907;
    for (int i = 0; i < 1907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1908(int& val) {
    func_in_1908(val);
    const int x = 1908;
    std::cout << 1908;
    for (int i = 0; i < 1908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1908(int& val) {
    const int x = 1908;
    std::cout << 1908;
    for (int i = 0; i < 1908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1909(int& val) {
    func_in_1909(val);
    const int x = 1909;
    std::cout << 1909;
    for (int i = 0; i < 1909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1909(int& val) {
    const int x = 1909;
    std::cout << 1909;
    for (int i = 0; i < 1909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1910(int& val) {
    func_in_1910(val);
    const int x = 1910;
    std::cout << 1910;
    for (int i = 0; i < 1910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1910(int& val) {
    const int x = 1910;
    std::cout << 1910;
    for (int i = 0; i < 1910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1911(int& val) {
    func_in_1911(val);
    const int x = 1911;
    std::cout << 1911;
    for (int i = 0; i < 1911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1911(int& val) {
    const int x = 1911;
    std::cout << 1911;
    for (int i = 0; i < 1911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1912(int& val) {
    func_in_1912(val);
    const int x = 1912;
    std::cout << 1912;
    for (int i = 0; i < 1912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1912(int& val) {
    const int x = 1912;
    std::cout << 1912;
    for (int i = 0; i < 1912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1913(int& val) {
    func_in_1913(val);
    const int x = 1913;
    std::cout << 1913;
    for (int i = 0; i < 1913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1913(int& val) {
    const int x = 1913;
    std::cout << 1913;
    for (int i = 0; i < 1913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1914(int& val) {
    func_in_1914(val);
    const int x = 1914;
    std::cout << 1914;
    for (int i = 0; i < 1914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1914(int& val) {
    const int x = 1914;
    std::cout << 1914;
    for (int i = 0; i < 1914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1915(int& val) {
    func_in_1915(val);
    const int x = 1915;
    std::cout << 1915;
    for (int i = 0; i < 1915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1915(int& val) {
    const int x = 1915;
    std::cout << 1915;
    for (int i = 0; i < 1915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1916(int& val) {
    func_in_1916(val);
    const int x = 1916;
    std::cout << 1916;
    for (int i = 0; i < 1916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1916(int& val) {
    const int x = 1916;
    std::cout << 1916;
    for (int i = 0; i < 1916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1917(int& val) {
    func_in_1917(val);
    const int x = 1917;
    std::cout << 1917;
    for (int i = 0; i < 1917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1917(int& val) {
    const int x = 1917;
    std::cout << 1917;
    for (int i = 0; i < 1917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1918(int& val) {
    func_in_1918(val);
    const int x = 1918;
    std::cout << 1918;
    for (int i = 0; i < 1918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1918(int& val) {
    const int x = 1918;
    std::cout << 1918;
    for (int i = 0; i < 1918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1919(int& val) {
    func_in_1919(val);
    const int x = 1919;
    std::cout << 1919;
    for (int i = 0; i < 1919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1919(int& val) {
    const int x = 1919;
    std::cout << 1919;
    for (int i = 0; i < 1919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1920(int& val) {
    func_in_1920(val);
    const int x = 1920;
    std::cout << 1920;
    for (int i = 0; i < 1920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1920(int& val) {
    const int x = 1920;
    std::cout << 1920;
    for (int i = 0; i < 1920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1921(int& val) {
    func_in_1921(val);
    const int x = 1921;
    std::cout << 1921;
    for (int i = 0; i < 1921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1921(int& val) {
    const int x = 1921;
    std::cout << 1921;
    for (int i = 0; i < 1921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1922(int& val) {
    func_in_1922(val);
    const int x = 1922;
    std::cout << 1922;
    for (int i = 0; i < 1922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1922(int& val) {
    const int x = 1922;
    std::cout << 1922;
    for (int i = 0; i < 1922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1923(int& val) {
    func_in_1923(val);
    const int x = 1923;
    std::cout << 1923;
    for (int i = 0; i < 1923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1923(int& val) {
    const int x = 1923;
    std::cout << 1923;
    for (int i = 0; i < 1923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1924(int& val) {
    func_in_1924(val);
    const int x = 1924;
    std::cout << 1924;
    for (int i = 0; i < 1924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1924(int& val) {
    const int x = 1924;
    std::cout << 1924;
    for (int i = 0; i < 1924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1925(int& val) {
    func_in_1925(val);
    const int x = 1925;
    std::cout << 1925;
    for (int i = 0; i < 1925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1925(int& val) {
    const int x = 1925;
    std::cout << 1925;
    for (int i = 0; i < 1925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1926(int& val) {
    func_in_1926(val);
    const int x = 1926;
    std::cout << 1926;
    for (int i = 0; i < 1926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1926(int& val) {
    const int x = 1926;
    std::cout << 1926;
    for (int i = 0; i < 1926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1927(int& val) {
    func_in_1927(val);
    const int x = 1927;
    std::cout << 1927;
    for (int i = 0; i < 1927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1927(int& val) {
    const int x = 1927;
    std::cout << 1927;
    for (int i = 0; i < 1927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1928(int& val) {
    func_in_1928(val);
    const int x = 1928;
    std::cout << 1928;
    for (int i = 0; i < 1928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1928(int& val) {
    const int x = 1928;
    std::cout << 1928;
    for (int i = 0; i < 1928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1929(int& val) {
    func_in_1929(val);
    const int x = 1929;
    std::cout << 1929;
    for (int i = 0; i < 1929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1929(int& val) {
    const int x = 1929;
    std::cout << 1929;
    for (int i = 0; i < 1929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1930(int& val) {
    func_in_1930(val);
    const int x = 1930;
    std::cout << 1930;
    for (int i = 0; i < 1930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1930(int& val) {
    const int x = 1930;
    std::cout << 1930;
    for (int i = 0; i < 1930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1931(int& val) {
    func_in_1931(val);
    const int x = 1931;
    std::cout << 1931;
    for (int i = 0; i < 1931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1931(int& val) {
    const int x = 1931;
    std::cout << 1931;
    for (int i = 0; i < 1931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1932(int& val) {
    func_in_1932(val);
    const int x = 1932;
    std::cout << 1932;
    for (int i = 0; i < 1932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1932(int& val) {
    const int x = 1932;
    std::cout << 1932;
    for (int i = 0; i < 1932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1933(int& val) {
    func_in_1933(val);
    const int x = 1933;
    std::cout << 1933;
    for (int i = 0; i < 1933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1933(int& val) {
    const int x = 1933;
    std::cout << 1933;
    for (int i = 0; i < 1933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1934(int& val) {
    func_in_1934(val);
    const int x = 1934;
    std::cout << 1934;
    for (int i = 0; i < 1934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1934(int& val) {
    const int x = 1934;
    std::cout << 1934;
    for (int i = 0; i < 1934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1935(int& val) {
    func_in_1935(val);
    const int x = 1935;
    std::cout << 1935;
    for (int i = 0; i < 1935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1935(int& val) {
    const int x = 1935;
    std::cout << 1935;
    for (int i = 0; i < 1935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1936(int& val) {
    func_in_1936(val);
    const int x = 1936;
    std::cout << 1936;
    for (int i = 0; i < 1936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1936(int& val) {
    const int x = 1936;
    std::cout << 1936;
    for (int i = 0; i < 1936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1937(int& val) {
    func_in_1937(val);
    const int x = 1937;
    std::cout << 1937;
    for (int i = 0; i < 1937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1937(int& val) {
    const int x = 1937;
    std::cout << 1937;
    for (int i = 0; i < 1937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1938(int& val) {
    func_in_1938(val);
    const int x = 1938;
    std::cout << 1938;
    for (int i = 0; i < 1938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1938(int& val) {
    const int x = 1938;
    std::cout << 1938;
    for (int i = 0; i < 1938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1939(int& val) {
    func_in_1939(val);
    const int x = 1939;
    std::cout << 1939;
    for (int i = 0; i < 1939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1939(int& val) {
    const int x = 1939;
    std::cout << 1939;
    for (int i = 0; i < 1939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1940(int& val) {
    func_in_1940(val);
    const int x = 1940;
    std::cout << 1940;
    for (int i = 0; i < 1940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1940(int& val) {
    const int x = 1940;
    std::cout << 1940;
    for (int i = 0; i < 1940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1941(int& val) {
    func_in_1941(val);
    const int x = 1941;
    std::cout << 1941;
    for (int i = 0; i < 1941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1941(int& val) {
    const int x = 1941;
    std::cout << 1941;
    for (int i = 0; i < 1941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1942(int& val) {
    func_in_1942(val);
    const int x = 1942;
    std::cout << 1942;
    for (int i = 0; i < 1942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1942(int& val) {
    const int x = 1942;
    std::cout << 1942;
    for (int i = 0; i < 1942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1943(int& val) {
    func_in_1943(val);
    const int x = 1943;
    std::cout << 1943;
    for (int i = 0; i < 1943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1943(int& val) {
    const int x = 1943;
    std::cout << 1943;
    for (int i = 0; i < 1943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1944(int& val) {
    func_in_1944(val);
    const int x = 1944;
    std::cout << 1944;
    for (int i = 0; i < 1944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1944(int& val) {
    const int x = 1944;
    std::cout << 1944;
    for (int i = 0; i < 1944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1945(int& val) {
    func_in_1945(val);
    const int x = 1945;
    std::cout << 1945;
    for (int i = 0; i < 1945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1945(int& val) {
    const int x = 1945;
    std::cout << 1945;
    for (int i = 0; i < 1945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1946(int& val) {
    func_in_1946(val);
    const int x = 1946;
    std::cout << 1946;
    for (int i = 0; i < 1946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1946(int& val) {
    const int x = 1946;
    std::cout << 1946;
    for (int i = 0; i < 1946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1947(int& val) {
    func_in_1947(val);
    const int x = 1947;
    std::cout << 1947;
    for (int i = 0; i < 1947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1947(int& val) {
    const int x = 1947;
    std::cout << 1947;
    for (int i = 0; i < 1947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1948(int& val) {
    func_in_1948(val);
    const int x = 1948;
    std::cout << 1948;
    for (int i = 0; i < 1948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1948(int& val) {
    const int x = 1948;
    std::cout << 1948;
    for (int i = 0; i < 1948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1949(int& val) {
    func_in_1949(val);
    const int x = 1949;
    std::cout << 1949;
    for (int i = 0; i < 1949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1949(int& val) {
    const int x = 1949;
    std::cout << 1949;
    for (int i = 0; i < 1949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1950(int& val) {
    func_in_1950(val);
    const int x = 1950;
    std::cout << 1950;
    for (int i = 0; i < 1950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1950(int& val) {
    const int x = 1950;
    std::cout << 1950;
    for (int i = 0; i < 1950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1951(int& val) {
    func_in_1951(val);
    const int x = 1951;
    std::cout << 1951;
    for (int i = 0; i < 1951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1951(int& val) {
    const int x = 1951;
    std::cout << 1951;
    for (int i = 0; i < 1951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1952(int& val) {
    func_in_1952(val);
    const int x = 1952;
    std::cout << 1952;
    for (int i = 0; i < 1952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1952(int& val) {
    const int x = 1952;
    std::cout << 1952;
    for (int i = 0; i < 1952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1953(int& val) {
    func_in_1953(val);
    const int x = 1953;
    std::cout << 1953;
    for (int i = 0; i < 1953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1953(int& val) {
    const int x = 1953;
    std::cout << 1953;
    for (int i = 0; i < 1953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1954(int& val) {
    func_in_1954(val);
    const int x = 1954;
    std::cout << 1954;
    for (int i = 0; i < 1954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1954(int& val) {
    const int x = 1954;
    std::cout << 1954;
    for (int i = 0; i < 1954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1955(int& val) {
    func_in_1955(val);
    const int x = 1955;
    std::cout << 1955;
    for (int i = 0; i < 1955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1955(int& val) {
    const int x = 1955;
    std::cout << 1955;
    for (int i = 0; i < 1955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1956(int& val) {
    func_in_1956(val);
    const int x = 1956;
    std::cout << 1956;
    for (int i = 0; i < 1956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1956(int& val) {
    const int x = 1956;
    std::cout << 1956;
    for (int i = 0; i < 1956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1957(int& val) {
    func_in_1957(val);
    const int x = 1957;
    std::cout << 1957;
    for (int i = 0; i < 1957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1957(int& val) {
    const int x = 1957;
    std::cout << 1957;
    for (int i = 0; i < 1957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1958(int& val) {
    func_in_1958(val);
    const int x = 1958;
    std::cout << 1958;
    for (int i = 0; i < 1958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1958(int& val) {
    const int x = 1958;
    std::cout << 1958;
    for (int i = 0; i < 1958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1959(int& val) {
    func_in_1959(val);
    const int x = 1959;
    std::cout << 1959;
    for (int i = 0; i < 1959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1959(int& val) {
    const int x = 1959;
    std::cout << 1959;
    for (int i = 0; i < 1959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1960(int& val) {
    func_in_1960(val);
    const int x = 1960;
    std::cout << 1960;
    for (int i = 0; i < 1960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1960(int& val) {
    const int x = 1960;
    std::cout << 1960;
    for (int i = 0; i < 1960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1961(int& val) {
    func_in_1961(val);
    const int x = 1961;
    std::cout << 1961;
    for (int i = 0; i < 1961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1961(int& val) {
    const int x = 1961;
    std::cout << 1961;
    for (int i = 0; i < 1961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1962(int& val) {
    func_in_1962(val);
    const int x = 1962;
    std::cout << 1962;
    for (int i = 0; i < 1962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1962(int& val) {
    const int x = 1962;
    std::cout << 1962;
    for (int i = 0; i < 1962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1963(int& val) {
    func_in_1963(val);
    const int x = 1963;
    std::cout << 1963;
    for (int i = 0; i < 1963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1963(int& val) {
    const int x = 1963;
    std::cout << 1963;
    for (int i = 0; i < 1963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1964(int& val) {
    func_in_1964(val);
    const int x = 1964;
    std::cout << 1964;
    for (int i = 0; i < 1964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1964(int& val) {
    const int x = 1964;
    std::cout << 1964;
    for (int i = 0; i < 1964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1965(int& val) {
    func_in_1965(val);
    const int x = 1965;
    std::cout << 1965;
    for (int i = 0; i < 1965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1965(int& val) {
    const int x = 1965;
    std::cout << 1965;
    for (int i = 0; i < 1965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1966(int& val) {
    func_in_1966(val);
    const int x = 1966;
    std::cout << 1966;
    for (int i = 0; i < 1966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1966(int& val) {
    const int x = 1966;
    std::cout << 1966;
    for (int i = 0; i < 1966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1967(int& val) {
    func_in_1967(val);
    const int x = 1967;
    std::cout << 1967;
    for (int i = 0; i < 1967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1967(int& val) {
    const int x = 1967;
    std::cout << 1967;
    for (int i = 0; i < 1967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1968(int& val) {
    func_in_1968(val);
    const int x = 1968;
    std::cout << 1968;
    for (int i = 0; i < 1968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1968(int& val) {
    const int x = 1968;
    std::cout << 1968;
    for (int i = 0; i < 1968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1969(int& val) {
    func_in_1969(val);
    const int x = 1969;
    std::cout << 1969;
    for (int i = 0; i < 1969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1969(int& val) {
    const int x = 1969;
    std::cout << 1969;
    for (int i = 0; i < 1969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1970(int& val) {
    func_in_1970(val);
    const int x = 1970;
    std::cout << 1970;
    for (int i = 0; i < 1970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1970(int& val) {
    const int x = 1970;
    std::cout << 1970;
    for (int i = 0; i < 1970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1971(int& val) {
    func_in_1971(val);
    const int x = 1971;
    std::cout << 1971;
    for (int i = 0; i < 1971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1971(int& val) {
    const int x = 1971;
    std::cout << 1971;
    for (int i = 0; i < 1971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1972(int& val) {
    func_in_1972(val);
    const int x = 1972;
    std::cout << 1972;
    for (int i = 0; i < 1972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1972(int& val) {
    const int x = 1972;
    std::cout << 1972;
    for (int i = 0; i < 1972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1973(int& val) {
    func_in_1973(val);
    const int x = 1973;
    std::cout << 1973;
    for (int i = 0; i < 1973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1973(int& val) {
    const int x = 1973;
    std::cout << 1973;
    for (int i = 0; i < 1973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1974(int& val) {
    func_in_1974(val);
    const int x = 1974;
    std::cout << 1974;
    for (int i = 0; i < 1974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1974(int& val) {
    const int x = 1974;
    std::cout << 1974;
    for (int i = 0; i < 1974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1975(int& val) {
    func_in_1975(val);
    const int x = 1975;
    std::cout << 1975;
    for (int i = 0; i < 1975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1975(int& val) {
    const int x = 1975;
    std::cout << 1975;
    for (int i = 0; i < 1975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1976(int& val) {
    func_in_1976(val);
    const int x = 1976;
    std::cout << 1976;
    for (int i = 0; i < 1976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1976(int& val) {
    const int x = 1976;
    std::cout << 1976;
    for (int i = 0; i < 1976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1977(int& val) {
    func_in_1977(val);
    const int x = 1977;
    std::cout << 1977;
    for (int i = 0; i < 1977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1977(int& val) {
    const int x = 1977;
    std::cout << 1977;
    for (int i = 0; i < 1977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1978(int& val) {
    func_in_1978(val);
    const int x = 1978;
    std::cout << 1978;
    for (int i = 0; i < 1978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1978(int& val) {
    const int x = 1978;
    std::cout << 1978;
    for (int i = 0; i < 1978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1979(int& val) {
    func_in_1979(val);
    const int x = 1979;
    std::cout << 1979;
    for (int i = 0; i < 1979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1979(int& val) {
    const int x = 1979;
    std::cout << 1979;
    for (int i = 0; i < 1979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1980(int& val) {
    func_in_1980(val);
    const int x = 1980;
    std::cout << 1980;
    for (int i = 0; i < 1980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1980(int& val) {
    const int x = 1980;
    std::cout << 1980;
    for (int i = 0; i < 1980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1981(int& val) {
    func_in_1981(val);
    const int x = 1981;
    std::cout << 1981;
    for (int i = 0; i < 1981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1981(int& val) {
    const int x = 1981;
    std::cout << 1981;
    for (int i = 0; i < 1981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1982(int& val) {
    func_in_1982(val);
    const int x = 1982;
    std::cout << 1982;
    for (int i = 0; i < 1982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1982(int& val) {
    const int x = 1982;
    std::cout << 1982;
    for (int i = 0; i < 1982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1983(int& val) {
    func_in_1983(val);
    const int x = 1983;
    std::cout << 1983;
    for (int i = 0; i < 1983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1983(int& val) {
    const int x = 1983;
    std::cout << 1983;
    for (int i = 0; i < 1983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1984(int& val) {
    func_in_1984(val);
    const int x = 1984;
    std::cout << 1984;
    for (int i = 0; i < 1984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1984(int& val) {
    const int x = 1984;
    std::cout << 1984;
    for (int i = 0; i < 1984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1985(int& val) {
    func_in_1985(val);
    const int x = 1985;
    std::cout << 1985;
    for (int i = 0; i < 1985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1985(int& val) {
    const int x = 1985;
    std::cout << 1985;
    for (int i = 0; i < 1985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1986(int& val) {
    func_in_1986(val);
    const int x = 1986;
    std::cout << 1986;
    for (int i = 0; i < 1986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1986(int& val) {
    const int x = 1986;
    std::cout << 1986;
    for (int i = 0; i < 1986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1987(int& val) {
    func_in_1987(val);
    const int x = 1987;
    std::cout << 1987;
    for (int i = 0; i < 1987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1987(int& val) {
    const int x = 1987;
    std::cout << 1987;
    for (int i = 0; i < 1987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1988(int& val) {
    func_in_1988(val);
    const int x = 1988;
    std::cout << 1988;
    for (int i = 0; i < 1988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1988(int& val) {
    const int x = 1988;
    std::cout << 1988;
    for (int i = 0; i < 1988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1989(int& val) {
    func_in_1989(val);
    const int x = 1989;
    std::cout << 1989;
    for (int i = 0; i < 1989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1989(int& val) {
    const int x = 1989;
    std::cout << 1989;
    for (int i = 0; i < 1989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1990(int& val) {
    func_in_1990(val);
    const int x = 1990;
    std::cout << 1990;
    for (int i = 0; i < 1990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1990(int& val) {
    const int x = 1990;
    std::cout << 1990;
    for (int i = 0; i < 1990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1991(int& val) {
    func_in_1991(val);
    const int x = 1991;
    std::cout << 1991;
    for (int i = 0; i < 1991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1991(int& val) {
    const int x = 1991;
    std::cout << 1991;
    for (int i = 0; i < 1991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1992(int& val) {
    func_in_1992(val);
    const int x = 1992;
    std::cout << 1992;
    for (int i = 0; i < 1992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1992(int& val) {
    const int x = 1992;
    std::cout << 1992;
    for (int i = 0; i < 1992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1993(int& val) {
    func_in_1993(val);
    const int x = 1993;
    std::cout << 1993;
    for (int i = 0; i < 1993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1993(int& val) {
    const int x = 1993;
    std::cout << 1993;
    for (int i = 0; i < 1993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1994(int& val) {
    func_in_1994(val);
    const int x = 1994;
    std::cout << 1994;
    for (int i = 0; i < 1994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1994(int& val) {
    const int x = 1994;
    std::cout << 1994;
    for (int i = 0; i < 1994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1995(int& val) {
    func_in_1995(val);
    const int x = 1995;
    std::cout << 1995;
    for (int i = 0; i < 1995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1995(int& val) {
    const int x = 1995;
    std::cout << 1995;
    for (int i = 0; i < 1995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1996(int& val) {
    func_in_1996(val);
    const int x = 1996;
    std::cout << 1996;
    for (int i = 0; i < 1996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1996(int& val) {
    const int x = 1996;
    std::cout << 1996;
    for (int i = 0; i < 1996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1997(int& val) {
    func_in_1997(val);
    const int x = 1997;
    std::cout << 1997;
    for (int i = 0; i < 1997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1997(int& val) {
    const int x = 1997;
    std::cout << 1997;
    for (int i = 0; i < 1997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1998(int& val) {
    func_in_1998(val);
    const int x = 1998;
    std::cout << 1998;
    for (int i = 0; i < 1998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1998(int& val) {
    const int x = 1998;
    std::cout << 1998;
    for (int i = 0; i < 1998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_1999(int& val) {
    func_in_1999(val);
    const int x = 1999;
    std::cout << 1999;
    for (int i = 0; i < 1999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_1999(int& val) {
    const int x = 1999;
    std::cout << 1999;
    for (int i = 0; i < 1999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2000(int& val) {
    func_in_2000(val);
    const int x = 2000;
    std::cout << 2000;
    for (int i = 0; i < 2000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2000(int& val) {
    const int x = 2000;
    std::cout << 2000;
    for (int i = 0; i < 2000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2001(int& val) {
    func_in_2001(val);
    const int x = 2001;
    std::cout << 2001;
    for (int i = 0; i < 2001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2001(int& val) {
    const int x = 2001;
    std::cout << 2001;
    for (int i = 0; i < 2001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2002(int& val) {
    func_in_2002(val);
    const int x = 2002;
    std::cout << 2002;
    for (int i = 0; i < 2002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2002(int& val) {
    const int x = 2002;
    std::cout << 2002;
    for (int i = 0; i < 2002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2003(int& val) {
    func_in_2003(val);
    const int x = 2003;
    std::cout << 2003;
    for (int i = 0; i < 2003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2003(int& val) {
    const int x = 2003;
    std::cout << 2003;
    for (int i = 0; i < 2003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2004(int& val) {
    func_in_2004(val);
    const int x = 2004;
    std::cout << 2004;
    for (int i = 0; i < 2004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2004(int& val) {
    const int x = 2004;
    std::cout << 2004;
    for (int i = 0; i < 2004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2005(int& val) {
    func_in_2005(val);
    const int x = 2005;
    std::cout << 2005;
    for (int i = 0; i < 2005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2005(int& val) {
    const int x = 2005;
    std::cout << 2005;
    for (int i = 0; i < 2005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2006(int& val) {
    func_in_2006(val);
    const int x = 2006;
    std::cout << 2006;
    for (int i = 0; i < 2006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2006(int& val) {
    const int x = 2006;
    std::cout << 2006;
    for (int i = 0; i < 2006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2007(int& val) {
    func_in_2007(val);
    const int x = 2007;
    std::cout << 2007;
    for (int i = 0; i < 2007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2007(int& val) {
    const int x = 2007;
    std::cout << 2007;
    for (int i = 0; i < 2007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2008(int& val) {
    func_in_2008(val);
    const int x = 2008;
    std::cout << 2008;
    for (int i = 0; i < 2008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2008(int& val) {
    const int x = 2008;
    std::cout << 2008;
    for (int i = 0; i < 2008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2009(int& val) {
    func_in_2009(val);
    const int x = 2009;
    std::cout << 2009;
    for (int i = 0; i < 2009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2009(int& val) {
    const int x = 2009;
    std::cout << 2009;
    for (int i = 0; i < 2009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2010(int& val) {
    func_in_2010(val);
    const int x = 2010;
    std::cout << 2010;
    for (int i = 0; i < 2010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2010(int& val) {
    const int x = 2010;
    std::cout << 2010;
    for (int i = 0; i < 2010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2011(int& val) {
    func_in_2011(val);
    const int x = 2011;
    std::cout << 2011;
    for (int i = 0; i < 2011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2011(int& val) {
    const int x = 2011;
    std::cout << 2011;
    for (int i = 0; i < 2011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2012(int& val) {
    func_in_2012(val);
    const int x = 2012;
    std::cout << 2012;
    for (int i = 0; i < 2012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2012(int& val) {
    const int x = 2012;
    std::cout << 2012;
    for (int i = 0; i < 2012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2013(int& val) {
    func_in_2013(val);
    const int x = 2013;
    std::cout << 2013;
    for (int i = 0; i < 2013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2013(int& val) {
    const int x = 2013;
    std::cout << 2013;
    for (int i = 0; i < 2013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2014(int& val) {
    func_in_2014(val);
    const int x = 2014;
    std::cout << 2014;
    for (int i = 0; i < 2014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2014(int& val) {
    const int x = 2014;
    std::cout << 2014;
    for (int i = 0; i < 2014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2015(int& val) {
    func_in_2015(val);
    const int x = 2015;
    std::cout << 2015;
    for (int i = 0; i < 2015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2015(int& val) {
    const int x = 2015;
    std::cout << 2015;
    for (int i = 0; i < 2015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2016(int& val) {
    func_in_2016(val);
    const int x = 2016;
    std::cout << 2016;
    for (int i = 0; i < 2016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2016(int& val) {
    const int x = 2016;
    std::cout << 2016;
    for (int i = 0; i < 2016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2017(int& val) {
    func_in_2017(val);
    const int x = 2017;
    std::cout << 2017;
    for (int i = 0; i < 2017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2017(int& val) {
    const int x = 2017;
    std::cout << 2017;
    for (int i = 0; i < 2017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2018(int& val) {
    func_in_2018(val);
    const int x = 2018;
    std::cout << 2018;
    for (int i = 0; i < 2018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2018(int& val) {
    const int x = 2018;
    std::cout << 2018;
    for (int i = 0; i < 2018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2019(int& val) {
    func_in_2019(val);
    const int x = 2019;
    std::cout << 2019;
    for (int i = 0; i < 2019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2019(int& val) {
    const int x = 2019;
    std::cout << 2019;
    for (int i = 0; i < 2019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2020(int& val) {
    func_in_2020(val);
    const int x = 2020;
    std::cout << 2020;
    for (int i = 0; i < 2020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2020(int& val) {
    const int x = 2020;
    std::cout << 2020;
    for (int i = 0; i < 2020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2021(int& val) {
    func_in_2021(val);
    const int x = 2021;
    std::cout << 2021;
    for (int i = 0; i < 2021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2021(int& val) {
    const int x = 2021;
    std::cout << 2021;
    for (int i = 0; i < 2021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2022(int& val) {
    func_in_2022(val);
    const int x = 2022;
    std::cout << 2022;
    for (int i = 0; i < 2022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2022(int& val) {
    const int x = 2022;
    std::cout << 2022;
    for (int i = 0; i < 2022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2023(int& val) {
    func_in_2023(val);
    const int x = 2023;
    std::cout << 2023;
    for (int i = 0; i < 2023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2023(int& val) {
    const int x = 2023;
    std::cout << 2023;
    for (int i = 0; i < 2023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2024(int& val) {
    func_in_2024(val);
    const int x = 2024;
    std::cout << 2024;
    for (int i = 0; i < 2024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2024(int& val) {
    const int x = 2024;
    std::cout << 2024;
    for (int i = 0; i < 2024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2025(int& val) {
    func_in_2025(val);
    const int x = 2025;
    std::cout << 2025;
    for (int i = 0; i < 2025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2025(int& val) {
    const int x = 2025;
    std::cout << 2025;
    for (int i = 0; i < 2025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2026(int& val) {
    func_in_2026(val);
    const int x = 2026;
    std::cout << 2026;
    for (int i = 0; i < 2026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2026(int& val) {
    const int x = 2026;
    std::cout << 2026;
    for (int i = 0; i < 2026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2027(int& val) {
    func_in_2027(val);
    const int x = 2027;
    std::cout << 2027;
    for (int i = 0; i < 2027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2027(int& val) {
    const int x = 2027;
    std::cout << 2027;
    for (int i = 0; i < 2027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2028(int& val) {
    func_in_2028(val);
    const int x = 2028;
    std::cout << 2028;
    for (int i = 0; i < 2028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2028(int& val) {
    const int x = 2028;
    std::cout << 2028;
    for (int i = 0; i < 2028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2029(int& val) {
    func_in_2029(val);
    const int x = 2029;
    std::cout << 2029;
    for (int i = 0; i < 2029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2029(int& val) {
    const int x = 2029;
    std::cout << 2029;
    for (int i = 0; i < 2029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2030(int& val) {
    func_in_2030(val);
    const int x = 2030;
    std::cout << 2030;
    for (int i = 0; i < 2030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2030(int& val) {
    const int x = 2030;
    std::cout << 2030;
    for (int i = 0; i < 2030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2031(int& val) {
    func_in_2031(val);
    const int x = 2031;
    std::cout << 2031;
    for (int i = 0; i < 2031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2031(int& val) {
    const int x = 2031;
    std::cout << 2031;
    for (int i = 0; i < 2031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2032(int& val) {
    func_in_2032(val);
    const int x = 2032;
    std::cout << 2032;
    for (int i = 0; i < 2032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2032(int& val) {
    const int x = 2032;
    std::cout << 2032;
    for (int i = 0; i < 2032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2033(int& val) {
    func_in_2033(val);
    const int x = 2033;
    std::cout << 2033;
    for (int i = 0; i < 2033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2033(int& val) {
    const int x = 2033;
    std::cout << 2033;
    for (int i = 0; i < 2033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2034(int& val) {
    func_in_2034(val);
    const int x = 2034;
    std::cout << 2034;
    for (int i = 0; i < 2034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2034(int& val) {
    const int x = 2034;
    std::cout << 2034;
    for (int i = 0; i < 2034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2035(int& val) {
    func_in_2035(val);
    const int x = 2035;
    std::cout << 2035;
    for (int i = 0; i < 2035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2035(int& val) {
    const int x = 2035;
    std::cout << 2035;
    for (int i = 0; i < 2035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2036(int& val) {
    func_in_2036(val);
    const int x = 2036;
    std::cout << 2036;
    for (int i = 0; i < 2036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2036(int& val) {
    const int x = 2036;
    std::cout << 2036;
    for (int i = 0; i < 2036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2037(int& val) {
    func_in_2037(val);
    const int x = 2037;
    std::cout << 2037;
    for (int i = 0; i < 2037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2037(int& val) {
    const int x = 2037;
    std::cout << 2037;
    for (int i = 0; i < 2037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2038(int& val) {
    func_in_2038(val);
    const int x = 2038;
    std::cout << 2038;
    for (int i = 0; i < 2038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2038(int& val) {
    const int x = 2038;
    std::cout << 2038;
    for (int i = 0; i < 2038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2039(int& val) {
    func_in_2039(val);
    const int x = 2039;
    std::cout << 2039;
    for (int i = 0; i < 2039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2039(int& val) {
    const int x = 2039;
    std::cout << 2039;
    for (int i = 0; i < 2039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2040(int& val) {
    func_in_2040(val);
    const int x = 2040;
    std::cout << 2040;
    for (int i = 0; i < 2040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2040(int& val) {
    const int x = 2040;
    std::cout << 2040;
    for (int i = 0; i < 2040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2041(int& val) {
    func_in_2041(val);
    const int x = 2041;
    std::cout << 2041;
    for (int i = 0; i < 2041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2041(int& val) {
    const int x = 2041;
    std::cout << 2041;
    for (int i = 0; i < 2041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2042(int& val) {
    func_in_2042(val);
    const int x = 2042;
    std::cout << 2042;
    for (int i = 0; i < 2042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2042(int& val) {
    const int x = 2042;
    std::cout << 2042;
    for (int i = 0; i < 2042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2043(int& val) {
    func_in_2043(val);
    const int x = 2043;
    std::cout << 2043;
    for (int i = 0; i < 2043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2043(int& val) {
    const int x = 2043;
    std::cout << 2043;
    for (int i = 0; i < 2043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2044(int& val) {
    func_in_2044(val);
    const int x = 2044;
    std::cout << 2044;
    for (int i = 0; i < 2044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2044(int& val) {
    const int x = 2044;
    std::cout << 2044;
    for (int i = 0; i < 2044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2045(int& val) {
    func_in_2045(val);
    const int x = 2045;
    std::cout << 2045;
    for (int i = 0; i < 2045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2045(int& val) {
    const int x = 2045;
    std::cout << 2045;
    for (int i = 0; i < 2045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2046(int& val) {
    func_in_2046(val);
    const int x = 2046;
    std::cout << 2046;
    for (int i = 0; i < 2046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2046(int& val) {
    const int x = 2046;
    std::cout << 2046;
    for (int i = 0; i < 2046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2047(int& val) {
    func_in_2047(val);
    const int x = 2047;
    std::cout << 2047;
    for (int i = 0; i < 2047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2047(int& val) {
    const int x = 2047;
    std::cout << 2047;
    for (int i = 0; i < 2047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2048(int& val) {
    func_in_2048(val);
    const int x = 2048;
    std::cout << 2048;
    for (int i = 0; i < 2048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2048(int& val) {
    const int x = 2048;
    std::cout << 2048;
    for (int i = 0; i < 2048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2049(int& val) {
    func_in_2049(val);
    const int x = 2049;
    std::cout << 2049;
    for (int i = 0; i < 2049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2049(int& val) {
    const int x = 2049;
    std::cout << 2049;
    for (int i = 0; i < 2049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2050(int& val) {
    func_in_2050(val);
    const int x = 2050;
    std::cout << 2050;
    for (int i = 0; i < 2050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2050(int& val) {
    const int x = 2050;
    std::cout << 2050;
    for (int i = 0; i < 2050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2051(int& val) {
    func_in_2051(val);
    const int x = 2051;
    std::cout << 2051;
    for (int i = 0; i < 2051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2051(int& val) {
    const int x = 2051;
    std::cout << 2051;
    for (int i = 0; i < 2051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2052(int& val) {
    func_in_2052(val);
    const int x = 2052;
    std::cout << 2052;
    for (int i = 0; i < 2052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2052(int& val) {
    const int x = 2052;
    std::cout << 2052;
    for (int i = 0; i < 2052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2053(int& val) {
    func_in_2053(val);
    const int x = 2053;
    std::cout << 2053;
    for (int i = 0; i < 2053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2053(int& val) {
    const int x = 2053;
    std::cout << 2053;
    for (int i = 0; i < 2053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2054(int& val) {
    func_in_2054(val);
    const int x = 2054;
    std::cout << 2054;
    for (int i = 0; i < 2054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2054(int& val) {
    const int x = 2054;
    std::cout << 2054;
    for (int i = 0; i < 2054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2055(int& val) {
    func_in_2055(val);
    const int x = 2055;
    std::cout << 2055;
    for (int i = 0; i < 2055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2055(int& val) {
    const int x = 2055;
    std::cout << 2055;
    for (int i = 0; i < 2055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2056(int& val) {
    func_in_2056(val);
    const int x = 2056;
    std::cout << 2056;
    for (int i = 0; i < 2056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2056(int& val) {
    const int x = 2056;
    std::cout << 2056;
    for (int i = 0; i < 2056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2057(int& val) {
    func_in_2057(val);
    const int x = 2057;
    std::cout << 2057;
    for (int i = 0; i < 2057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2057(int& val) {
    const int x = 2057;
    std::cout << 2057;
    for (int i = 0; i < 2057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2058(int& val) {
    func_in_2058(val);
    const int x = 2058;
    std::cout << 2058;
    for (int i = 0; i < 2058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2058(int& val) {
    const int x = 2058;
    std::cout << 2058;
    for (int i = 0; i < 2058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2059(int& val) {
    func_in_2059(val);
    const int x = 2059;
    std::cout << 2059;
    for (int i = 0; i < 2059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2059(int& val) {
    const int x = 2059;
    std::cout << 2059;
    for (int i = 0; i < 2059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2060(int& val) {
    func_in_2060(val);
    const int x = 2060;
    std::cout << 2060;
    for (int i = 0; i < 2060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2060(int& val) {
    const int x = 2060;
    std::cout << 2060;
    for (int i = 0; i < 2060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2061(int& val) {
    func_in_2061(val);
    const int x = 2061;
    std::cout << 2061;
    for (int i = 0; i < 2061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2061(int& val) {
    const int x = 2061;
    std::cout << 2061;
    for (int i = 0; i < 2061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2062(int& val) {
    func_in_2062(val);
    const int x = 2062;
    std::cout << 2062;
    for (int i = 0; i < 2062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2062(int& val) {
    const int x = 2062;
    std::cout << 2062;
    for (int i = 0; i < 2062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2063(int& val) {
    func_in_2063(val);
    const int x = 2063;
    std::cout << 2063;
    for (int i = 0; i < 2063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2063(int& val) {
    const int x = 2063;
    std::cout << 2063;
    for (int i = 0; i < 2063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2064(int& val) {
    func_in_2064(val);
    const int x = 2064;
    std::cout << 2064;
    for (int i = 0; i < 2064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2064(int& val) {
    const int x = 2064;
    std::cout << 2064;
    for (int i = 0; i < 2064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2065(int& val) {
    func_in_2065(val);
    const int x = 2065;
    std::cout << 2065;
    for (int i = 0; i < 2065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2065(int& val) {
    const int x = 2065;
    std::cout << 2065;
    for (int i = 0; i < 2065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2066(int& val) {
    func_in_2066(val);
    const int x = 2066;
    std::cout << 2066;
    for (int i = 0; i < 2066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2066(int& val) {
    const int x = 2066;
    std::cout << 2066;
    for (int i = 0; i < 2066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2067(int& val) {
    func_in_2067(val);
    const int x = 2067;
    std::cout << 2067;
    for (int i = 0; i < 2067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2067(int& val) {
    const int x = 2067;
    std::cout << 2067;
    for (int i = 0; i < 2067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2068(int& val) {
    func_in_2068(val);
    const int x = 2068;
    std::cout << 2068;
    for (int i = 0; i < 2068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2068(int& val) {
    const int x = 2068;
    std::cout << 2068;
    for (int i = 0; i < 2068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2069(int& val) {
    func_in_2069(val);
    const int x = 2069;
    std::cout << 2069;
    for (int i = 0; i < 2069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2069(int& val) {
    const int x = 2069;
    std::cout << 2069;
    for (int i = 0; i < 2069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2070(int& val) {
    func_in_2070(val);
    const int x = 2070;
    std::cout << 2070;
    for (int i = 0; i < 2070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2070(int& val) {
    const int x = 2070;
    std::cout << 2070;
    for (int i = 0; i < 2070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2071(int& val) {
    func_in_2071(val);
    const int x = 2071;
    std::cout << 2071;
    for (int i = 0; i < 2071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2071(int& val) {
    const int x = 2071;
    std::cout << 2071;
    for (int i = 0; i < 2071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2072(int& val) {
    func_in_2072(val);
    const int x = 2072;
    std::cout << 2072;
    for (int i = 0; i < 2072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2072(int& val) {
    const int x = 2072;
    std::cout << 2072;
    for (int i = 0; i < 2072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2073(int& val) {
    func_in_2073(val);
    const int x = 2073;
    std::cout << 2073;
    for (int i = 0; i < 2073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2073(int& val) {
    const int x = 2073;
    std::cout << 2073;
    for (int i = 0; i < 2073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2074(int& val) {
    func_in_2074(val);
    const int x = 2074;
    std::cout << 2074;
    for (int i = 0; i < 2074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2074(int& val) {
    const int x = 2074;
    std::cout << 2074;
    for (int i = 0; i < 2074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2075(int& val) {
    func_in_2075(val);
    const int x = 2075;
    std::cout << 2075;
    for (int i = 0; i < 2075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2075(int& val) {
    const int x = 2075;
    std::cout << 2075;
    for (int i = 0; i < 2075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2076(int& val) {
    func_in_2076(val);
    const int x = 2076;
    std::cout << 2076;
    for (int i = 0; i < 2076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2076(int& val) {
    const int x = 2076;
    std::cout << 2076;
    for (int i = 0; i < 2076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2077(int& val) {
    func_in_2077(val);
    const int x = 2077;
    std::cout << 2077;
    for (int i = 0; i < 2077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2077(int& val) {
    const int x = 2077;
    std::cout << 2077;
    for (int i = 0; i < 2077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2078(int& val) {
    func_in_2078(val);
    const int x = 2078;
    std::cout << 2078;
    for (int i = 0; i < 2078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2078(int& val) {
    const int x = 2078;
    std::cout << 2078;
    for (int i = 0; i < 2078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2079(int& val) {
    func_in_2079(val);
    const int x = 2079;
    std::cout << 2079;
    for (int i = 0; i < 2079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2079(int& val) {
    const int x = 2079;
    std::cout << 2079;
    for (int i = 0; i < 2079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2080(int& val) {
    func_in_2080(val);
    const int x = 2080;
    std::cout << 2080;
    for (int i = 0; i < 2080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2080(int& val) {
    const int x = 2080;
    std::cout << 2080;
    for (int i = 0; i < 2080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2081(int& val) {
    func_in_2081(val);
    const int x = 2081;
    std::cout << 2081;
    for (int i = 0; i < 2081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2081(int& val) {
    const int x = 2081;
    std::cout << 2081;
    for (int i = 0; i < 2081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2082(int& val) {
    func_in_2082(val);
    const int x = 2082;
    std::cout << 2082;
    for (int i = 0; i < 2082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2082(int& val) {
    const int x = 2082;
    std::cout << 2082;
    for (int i = 0; i < 2082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2083(int& val) {
    func_in_2083(val);
    const int x = 2083;
    std::cout << 2083;
    for (int i = 0; i < 2083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2083(int& val) {
    const int x = 2083;
    std::cout << 2083;
    for (int i = 0; i < 2083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2084(int& val) {
    func_in_2084(val);
    const int x = 2084;
    std::cout << 2084;
    for (int i = 0; i < 2084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2084(int& val) {
    const int x = 2084;
    std::cout << 2084;
    for (int i = 0; i < 2084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2085(int& val) {
    func_in_2085(val);
    const int x = 2085;
    std::cout << 2085;
    for (int i = 0; i < 2085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2085(int& val) {
    const int x = 2085;
    std::cout << 2085;
    for (int i = 0; i < 2085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2086(int& val) {
    func_in_2086(val);
    const int x = 2086;
    std::cout << 2086;
    for (int i = 0; i < 2086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2086(int& val) {
    const int x = 2086;
    std::cout << 2086;
    for (int i = 0; i < 2086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2087(int& val) {
    func_in_2087(val);
    const int x = 2087;
    std::cout << 2087;
    for (int i = 0; i < 2087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2087(int& val) {
    const int x = 2087;
    std::cout << 2087;
    for (int i = 0; i < 2087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2088(int& val) {
    func_in_2088(val);
    const int x = 2088;
    std::cout << 2088;
    for (int i = 0; i < 2088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2088(int& val) {
    const int x = 2088;
    std::cout << 2088;
    for (int i = 0; i < 2088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2089(int& val) {
    func_in_2089(val);
    const int x = 2089;
    std::cout << 2089;
    for (int i = 0; i < 2089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2089(int& val) {
    const int x = 2089;
    std::cout << 2089;
    for (int i = 0; i < 2089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2090(int& val) {
    func_in_2090(val);
    const int x = 2090;
    std::cout << 2090;
    for (int i = 0; i < 2090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2090(int& val) {
    const int x = 2090;
    std::cout << 2090;
    for (int i = 0; i < 2090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2091(int& val) {
    func_in_2091(val);
    const int x = 2091;
    std::cout << 2091;
    for (int i = 0; i < 2091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2091(int& val) {
    const int x = 2091;
    std::cout << 2091;
    for (int i = 0; i < 2091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2092(int& val) {
    func_in_2092(val);
    const int x = 2092;
    std::cout << 2092;
    for (int i = 0; i < 2092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2092(int& val) {
    const int x = 2092;
    std::cout << 2092;
    for (int i = 0; i < 2092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2093(int& val) {
    func_in_2093(val);
    const int x = 2093;
    std::cout << 2093;
    for (int i = 0; i < 2093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2093(int& val) {
    const int x = 2093;
    std::cout << 2093;
    for (int i = 0; i < 2093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2094(int& val) {
    func_in_2094(val);
    const int x = 2094;
    std::cout << 2094;
    for (int i = 0; i < 2094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2094(int& val) {
    const int x = 2094;
    std::cout << 2094;
    for (int i = 0; i < 2094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2095(int& val) {
    func_in_2095(val);
    const int x = 2095;
    std::cout << 2095;
    for (int i = 0; i < 2095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2095(int& val) {
    const int x = 2095;
    std::cout << 2095;
    for (int i = 0; i < 2095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2096(int& val) {
    func_in_2096(val);
    const int x = 2096;
    std::cout << 2096;
    for (int i = 0; i < 2096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2096(int& val) {
    const int x = 2096;
    std::cout << 2096;
    for (int i = 0; i < 2096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2097(int& val) {
    func_in_2097(val);
    const int x = 2097;
    std::cout << 2097;
    for (int i = 0; i < 2097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2097(int& val) {
    const int x = 2097;
    std::cout << 2097;
    for (int i = 0; i < 2097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2098(int& val) {
    func_in_2098(val);
    const int x = 2098;
    std::cout << 2098;
    for (int i = 0; i < 2098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2098(int& val) {
    const int x = 2098;
    std::cout << 2098;
    for (int i = 0; i < 2098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2099(int& val) {
    func_in_2099(val);
    const int x = 2099;
    std::cout << 2099;
    for (int i = 0; i < 2099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2099(int& val) {
    const int x = 2099;
    std::cout << 2099;
    for (int i = 0; i < 2099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2100(int& val) {
    func_in_2100(val);
    const int x = 2100;
    std::cout << 2100;
    for (int i = 0; i < 2100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2100(int& val) {
    const int x = 2100;
    std::cout << 2100;
    for (int i = 0; i < 2100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2101(int& val) {
    func_in_2101(val);
    const int x = 2101;
    std::cout << 2101;
    for (int i = 0; i < 2101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2101(int& val) {
    const int x = 2101;
    std::cout << 2101;
    for (int i = 0; i < 2101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2102(int& val) {
    func_in_2102(val);
    const int x = 2102;
    std::cout << 2102;
    for (int i = 0; i < 2102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2102(int& val) {
    const int x = 2102;
    std::cout << 2102;
    for (int i = 0; i < 2102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2103(int& val) {
    func_in_2103(val);
    const int x = 2103;
    std::cout << 2103;
    for (int i = 0; i < 2103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2103(int& val) {
    const int x = 2103;
    std::cout << 2103;
    for (int i = 0; i < 2103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2104(int& val) {
    func_in_2104(val);
    const int x = 2104;
    std::cout << 2104;
    for (int i = 0; i < 2104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2104(int& val) {
    const int x = 2104;
    std::cout << 2104;
    for (int i = 0; i < 2104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2105(int& val) {
    func_in_2105(val);
    const int x = 2105;
    std::cout << 2105;
    for (int i = 0; i < 2105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2105(int& val) {
    const int x = 2105;
    std::cout << 2105;
    for (int i = 0; i < 2105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2106(int& val) {
    func_in_2106(val);
    const int x = 2106;
    std::cout << 2106;
    for (int i = 0; i < 2106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2106(int& val) {
    const int x = 2106;
    std::cout << 2106;
    for (int i = 0; i < 2106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2107(int& val) {
    func_in_2107(val);
    const int x = 2107;
    std::cout << 2107;
    for (int i = 0; i < 2107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2107(int& val) {
    const int x = 2107;
    std::cout << 2107;
    for (int i = 0; i < 2107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2108(int& val) {
    func_in_2108(val);
    const int x = 2108;
    std::cout << 2108;
    for (int i = 0; i < 2108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2108(int& val) {
    const int x = 2108;
    std::cout << 2108;
    for (int i = 0; i < 2108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2109(int& val) {
    func_in_2109(val);
    const int x = 2109;
    std::cout << 2109;
    for (int i = 0; i < 2109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2109(int& val) {
    const int x = 2109;
    std::cout << 2109;
    for (int i = 0; i < 2109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2110(int& val) {
    func_in_2110(val);
    const int x = 2110;
    std::cout << 2110;
    for (int i = 0; i < 2110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2110(int& val) {
    const int x = 2110;
    std::cout << 2110;
    for (int i = 0; i < 2110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2111(int& val) {
    func_in_2111(val);
    const int x = 2111;
    std::cout << 2111;
    for (int i = 0; i < 2111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2111(int& val) {
    const int x = 2111;
    std::cout << 2111;
    for (int i = 0; i < 2111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2112(int& val) {
    func_in_2112(val);
    const int x = 2112;
    std::cout << 2112;
    for (int i = 0; i < 2112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2112(int& val) {
    const int x = 2112;
    std::cout << 2112;
    for (int i = 0; i < 2112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2113(int& val) {
    func_in_2113(val);
    const int x = 2113;
    std::cout << 2113;
    for (int i = 0; i < 2113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2113(int& val) {
    const int x = 2113;
    std::cout << 2113;
    for (int i = 0; i < 2113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2114(int& val) {
    func_in_2114(val);
    const int x = 2114;
    std::cout << 2114;
    for (int i = 0; i < 2114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2114(int& val) {
    const int x = 2114;
    std::cout << 2114;
    for (int i = 0; i < 2114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2115(int& val) {
    func_in_2115(val);
    const int x = 2115;
    std::cout << 2115;
    for (int i = 0; i < 2115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2115(int& val) {
    const int x = 2115;
    std::cout << 2115;
    for (int i = 0; i < 2115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2116(int& val) {
    func_in_2116(val);
    const int x = 2116;
    std::cout << 2116;
    for (int i = 0; i < 2116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2116(int& val) {
    const int x = 2116;
    std::cout << 2116;
    for (int i = 0; i < 2116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2117(int& val) {
    func_in_2117(val);
    const int x = 2117;
    std::cout << 2117;
    for (int i = 0; i < 2117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2117(int& val) {
    const int x = 2117;
    std::cout << 2117;
    for (int i = 0; i < 2117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2118(int& val) {
    func_in_2118(val);
    const int x = 2118;
    std::cout << 2118;
    for (int i = 0; i < 2118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2118(int& val) {
    const int x = 2118;
    std::cout << 2118;
    for (int i = 0; i < 2118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2119(int& val) {
    func_in_2119(val);
    const int x = 2119;
    std::cout << 2119;
    for (int i = 0; i < 2119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2119(int& val) {
    const int x = 2119;
    std::cout << 2119;
    for (int i = 0; i < 2119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2120(int& val) {
    func_in_2120(val);
    const int x = 2120;
    std::cout << 2120;
    for (int i = 0; i < 2120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2120(int& val) {
    const int x = 2120;
    std::cout << 2120;
    for (int i = 0; i < 2120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2121(int& val) {
    func_in_2121(val);
    const int x = 2121;
    std::cout << 2121;
    for (int i = 0; i < 2121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2121(int& val) {
    const int x = 2121;
    std::cout << 2121;
    for (int i = 0; i < 2121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2122(int& val) {
    func_in_2122(val);
    const int x = 2122;
    std::cout << 2122;
    for (int i = 0; i < 2122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2122(int& val) {
    const int x = 2122;
    std::cout << 2122;
    for (int i = 0; i < 2122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2123(int& val) {
    func_in_2123(val);
    const int x = 2123;
    std::cout << 2123;
    for (int i = 0; i < 2123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2123(int& val) {
    const int x = 2123;
    std::cout << 2123;
    for (int i = 0; i < 2123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2124(int& val) {
    func_in_2124(val);
    const int x = 2124;
    std::cout << 2124;
    for (int i = 0; i < 2124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2124(int& val) {
    const int x = 2124;
    std::cout << 2124;
    for (int i = 0; i < 2124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2125(int& val) {
    func_in_2125(val);
    const int x = 2125;
    std::cout << 2125;
    for (int i = 0; i < 2125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2125(int& val) {
    const int x = 2125;
    std::cout << 2125;
    for (int i = 0; i < 2125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2126(int& val) {
    func_in_2126(val);
    const int x = 2126;
    std::cout << 2126;
    for (int i = 0; i < 2126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2126(int& val) {
    const int x = 2126;
    std::cout << 2126;
    for (int i = 0; i < 2126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2127(int& val) {
    func_in_2127(val);
    const int x = 2127;
    std::cout << 2127;
    for (int i = 0; i < 2127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2127(int& val) {
    const int x = 2127;
    std::cout << 2127;
    for (int i = 0; i < 2127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2128(int& val) {
    func_in_2128(val);
    const int x = 2128;
    std::cout << 2128;
    for (int i = 0; i < 2128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2128(int& val) {
    const int x = 2128;
    std::cout << 2128;
    for (int i = 0; i < 2128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2129(int& val) {
    func_in_2129(val);
    const int x = 2129;
    std::cout << 2129;
    for (int i = 0; i < 2129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2129(int& val) {
    const int x = 2129;
    std::cout << 2129;
    for (int i = 0; i < 2129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2130(int& val) {
    func_in_2130(val);
    const int x = 2130;
    std::cout << 2130;
    for (int i = 0; i < 2130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2130(int& val) {
    const int x = 2130;
    std::cout << 2130;
    for (int i = 0; i < 2130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2131(int& val) {
    func_in_2131(val);
    const int x = 2131;
    std::cout << 2131;
    for (int i = 0; i < 2131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2131(int& val) {
    const int x = 2131;
    std::cout << 2131;
    for (int i = 0; i < 2131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2132(int& val) {
    func_in_2132(val);
    const int x = 2132;
    std::cout << 2132;
    for (int i = 0; i < 2132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2132(int& val) {
    const int x = 2132;
    std::cout << 2132;
    for (int i = 0; i < 2132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2133(int& val) {
    func_in_2133(val);
    const int x = 2133;
    std::cout << 2133;
    for (int i = 0; i < 2133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2133(int& val) {
    const int x = 2133;
    std::cout << 2133;
    for (int i = 0; i < 2133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2134(int& val) {
    func_in_2134(val);
    const int x = 2134;
    std::cout << 2134;
    for (int i = 0; i < 2134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2134(int& val) {
    const int x = 2134;
    std::cout << 2134;
    for (int i = 0; i < 2134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2135(int& val) {
    func_in_2135(val);
    const int x = 2135;
    std::cout << 2135;
    for (int i = 0; i < 2135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2135(int& val) {
    const int x = 2135;
    std::cout << 2135;
    for (int i = 0; i < 2135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2136(int& val) {
    func_in_2136(val);
    const int x = 2136;
    std::cout << 2136;
    for (int i = 0; i < 2136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2136(int& val) {
    const int x = 2136;
    std::cout << 2136;
    for (int i = 0; i < 2136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2137(int& val) {
    func_in_2137(val);
    const int x = 2137;
    std::cout << 2137;
    for (int i = 0; i < 2137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2137(int& val) {
    const int x = 2137;
    std::cout << 2137;
    for (int i = 0; i < 2137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2138(int& val) {
    func_in_2138(val);
    const int x = 2138;
    std::cout << 2138;
    for (int i = 0; i < 2138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2138(int& val) {
    const int x = 2138;
    std::cout << 2138;
    for (int i = 0; i < 2138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2139(int& val) {
    func_in_2139(val);
    const int x = 2139;
    std::cout << 2139;
    for (int i = 0; i < 2139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2139(int& val) {
    const int x = 2139;
    std::cout << 2139;
    for (int i = 0; i < 2139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2140(int& val) {
    func_in_2140(val);
    const int x = 2140;
    std::cout << 2140;
    for (int i = 0; i < 2140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2140(int& val) {
    const int x = 2140;
    std::cout << 2140;
    for (int i = 0; i < 2140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2141(int& val) {
    func_in_2141(val);
    const int x = 2141;
    std::cout << 2141;
    for (int i = 0; i < 2141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2141(int& val) {
    const int x = 2141;
    std::cout << 2141;
    for (int i = 0; i < 2141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2142(int& val) {
    func_in_2142(val);
    const int x = 2142;
    std::cout << 2142;
    for (int i = 0; i < 2142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2142(int& val) {
    const int x = 2142;
    std::cout << 2142;
    for (int i = 0; i < 2142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2143(int& val) {
    func_in_2143(val);
    const int x = 2143;
    std::cout << 2143;
    for (int i = 0; i < 2143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2143(int& val) {
    const int x = 2143;
    std::cout << 2143;
    for (int i = 0; i < 2143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2144(int& val) {
    func_in_2144(val);
    const int x = 2144;
    std::cout << 2144;
    for (int i = 0; i < 2144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2144(int& val) {
    const int x = 2144;
    std::cout << 2144;
    for (int i = 0; i < 2144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2145(int& val) {
    func_in_2145(val);
    const int x = 2145;
    std::cout << 2145;
    for (int i = 0; i < 2145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2145(int& val) {
    const int x = 2145;
    std::cout << 2145;
    for (int i = 0; i < 2145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2146(int& val) {
    func_in_2146(val);
    const int x = 2146;
    std::cout << 2146;
    for (int i = 0; i < 2146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2146(int& val) {
    const int x = 2146;
    std::cout << 2146;
    for (int i = 0; i < 2146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2147(int& val) {
    func_in_2147(val);
    const int x = 2147;
    std::cout << 2147;
    for (int i = 0; i < 2147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2147(int& val) {
    const int x = 2147;
    std::cout << 2147;
    for (int i = 0; i < 2147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2148(int& val) {
    func_in_2148(val);
    const int x = 2148;
    std::cout << 2148;
    for (int i = 0; i < 2148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2148(int& val) {
    const int x = 2148;
    std::cout << 2148;
    for (int i = 0; i < 2148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2149(int& val) {
    func_in_2149(val);
    const int x = 2149;
    std::cout << 2149;
    for (int i = 0; i < 2149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2149(int& val) {
    const int x = 2149;
    std::cout << 2149;
    for (int i = 0; i < 2149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2150(int& val) {
    func_in_2150(val);
    const int x = 2150;
    std::cout << 2150;
    for (int i = 0; i < 2150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2150(int& val) {
    const int x = 2150;
    std::cout << 2150;
    for (int i = 0; i < 2150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2151(int& val) {
    func_in_2151(val);
    const int x = 2151;
    std::cout << 2151;
    for (int i = 0; i < 2151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2151(int& val) {
    const int x = 2151;
    std::cout << 2151;
    for (int i = 0; i < 2151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2152(int& val) {
    func_in_2152(val);
    const int x = 2152;
    std::cout << 2152;
    for (int i = 0; i < 2152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2152(int& val) {
    const int x = 2152;
    std::cout << 2152;
    for (int i = 0; i < 2152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2153(int& val) {
    func_in_2153(val);
    const int x = 2153;
    std::cout << 2153;
    for (int i = 0; i < 2153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2153(int& val) {
    const int x = 2153;
    std::cout << 2153;
    for (int i = 0; i < 2153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2154(int& val) {
    func_in_2154(val);
    const int x = 2154;
    std::cout << 2154;
    for (int i = 0; i < 2154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2154(int& val) {
    const int x = 2154;
    std::cout << 2154;
    for (int i = 0; i < 2154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2155(int& val) {
    func_in_2155(val);
    const int x = 2155;
    std::cout << 2155;
    for (int i = 0; i < 2155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2155(int& val) {
    const int x = 2155;
    std::cout << 2155;
    for (int i = 0; i < 2155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2156(int& val) {
    func_in_2156(val);
    const int x = 2156;
    std::cout << 2156;
    for (int i = 0; i < 2156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2156(int& val) {
    const int x = 2156;
    std::cout << 2156;
    for (int i = 0; i < 2156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2157(int& val) {
    func_in_2157(val);
    const int x = 2157;
    std::cout << 2157;
    for (int i = 0; i < 2157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2157(int& val) {
    const int x = 2157;
    std::cout << 2157;
    for (int i = 0; i < 2157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2158(int& val) {
    func_in_2158(val);
    const int x = 2158;
    std::cout << 2158;
    for (int i = 0; i < 2158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2158(int& val) {
    const int x = 2158;
    std::cout << 2158;
    for (int i = 0; i < 2158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2159(int& val) {
    func_in_2159(val);
    const int x = 2159;
    std::cout << 2159;
    for (int i = 0; i < 2159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2159(int& val) {
    const int x = 2159;
    std::cout << 2159;
    for (int i = 0; i < 2159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2160(int& val) {
    func_in_2160(val);
    const int x = 2160;
    std::cout << 2160;
    for (int i = 0; i < 2160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2160(int& val) {
    const int x = 2160;
    std::cout << 2160;
    for (int i = 0; i < 2160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2161(int& val) {
    func_in_2161(val);
    const int x = 2161;
    std::cout << 2161;
    for (int i = 0; i < 2161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2161(int& val) {
    const int x = 2161;
    std::cout << 2161;
    for (int i = 0; i < 2161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2162(int& val) {
    func_in_2162(val);
    const int x = 2162;
    std::cout << 2162;
    for (int i = 0; i < 2162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2162(int& val) {
    const int x = 2162;
    std::cout << 2162;
    for (int i = 0; i < 2162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2163(int& val) {
    func_in_2163(val);
    const int x = 2163;
    std::cout << 2163;
    for (int i = 0; i < 2163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2163(int& val) {
    const int x = 2163;
    std::cout << 2163;
    for (int i = 0; i < 2163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2164(int& val) {
    func_in_2164(val);
    const int x = 2164;
    std::cout << 2164;
    for (int i = 0; i < 2164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2164(int& val) {
    const int x = 2164;
    std::cout << 2164;
    for (int i = 0; i < 2164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2165(int& val) {
    func_in_2165(val);
    const int x = 2165;
    std::cout << 2165;
    for (int i = 0; i < 2165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2165(int& val) {
    const int x = 2165;
    std::cout << 2165;
    for (int i = 0; i < 2165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2166(int& val) {
    func_in_2166(val);
    const int x = 2166;
    std::cout << 2166;
    for (int i = 0; i < 2166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2166(int& val) {
    const int x = 2166;
    std::cout << 2166;
    for (int i = 0; i < 2166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2167(int& val) {
    func_in_2167(val);
    const int x = 2167;
    std::cout << 2167;
    for (int i = 0; i < 2167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2167(int& val) {
    const int x = 2167;
    std::cout << 2167;
    for (int i = 0; i < 2167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2168(int& val) {
    func_in_2168(val);
    const int x = 2168;
    std::cout << 2168;
    for (int i = 0; i < 2168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2168(int& val) {
    const int x = 2168;
    std::cout << 2168;
    for (int i = 0; i < 2168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2169(int& val) {
    func_in_2169(val);
    const int x = 2169;
    std::cout << 2169;
    for (int i = 0; i < 2169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2169(int& val) {
    const int x = 2169;
    std::cout << 2169;
    for (int i = 0; i < 2169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2170(int& val) {
    func_in_2170(val);
    const int x = 2170;
    std::cout << 2170;
    for (int i = 0; i < 2170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2170(int& val) {
    const int x = 2170;
    std::cout << 2170;
    for (int i = 0; i < 2170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2171(int& val) {
    func_in_2171(val);
    const int x = 2171;
    std::cout << 2171;
    for (int i = 0; i < 2171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2171(int& val) {
    const int x = 2171;
    std::cout << 2171;
    for (int i = 0; i < 2171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2172(int& val) {
    func_in_2172(val);
    const int x = 2172;
    std::cout << 2172;
    for (int i = 0; i < 2172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2172(int& val) {
    const int x = 2172;
    std::cout << 2172;
    for (int i = 0; i < 2172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2173(int& val) {
    func_in_2173(val);
    const int x = 2173;
    std::cout << 2173;
    for (int i = 0; i < 2173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2173(int& val) {
    const int x = 2173;
    std::cout << 2173;
    for (int i = 0; i < 2173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2174(int& val) {
    func_in_2174(val);
    const int x = 2174;
    std::cout << 2174;
    for (int i = 0; i < 2174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2174(int& val) {
    const int x = 2174;
    std::cout << 2174;
    for (int i = 0; i < 2174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2175(int& val) {
    func_in_2175(val);
    const int x = 2175;
    std::cout << 2175;
    for (int i = 0; i < 2175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2175(int& val) {
    const int x = 2175;
    std::cout << 2175;
    for (int i = 0; i < 2175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2176(int& val) {
    func_in_2176(val);
    const int x = 2176;
    std::cout << 2176;
    for (int i = 0; i < 2176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2176(int& val) {
    const int x = 2176;
    std::cout << 2176;
    for (int i = 0; i < 2176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2177(int& val) {
    func_in_2177(val);
    const int x = 2177;
    std::cout << 2177;
    for (int i = 0; i < 2177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2177(int& val) {
    const int x = 2177;
    std::cout << 2177;
    for (int i = 0; i < 2177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2178(int& val) {
    func_in_2178(val);
    const int x = 2178;
    std::cout << 2178;
    for (int i = 0; i < 2178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2178(int& val) {
    const int x = 2178;
    std::cout << 2178;
    for (int i = 0; i < 2178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2179(int& val) {
    func_in_2179(val);
    const int x = 2179;
    std::cout << 2179;
    for (int i = 0; i < 2179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2179(int& val) {
    const int x = 2179;
    std::cout << 2179;
    for (int i = 0; i < 2179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2180(int& val) {
    func_in_2180(val);
    const int x = 2180;
    std::cout << 2180;
    for (int i = 0; i < 2180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2180(int& val) {
    const int x = 2180;
    std::cout << 2180;
    for (int i = 0; i < 2180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2181(int& val) {
    func_in_2181(val);
    const int x = 2181;
    std::cout << 2181;
    for (int i = 0; i < 2181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2181(int& val) {
    const int x = 2181;
    std::cout << 2181;
    for (int i = 0; i < 2181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2182(int& val) {
    func_in_2182(val);
    const int x = 2182;
    std::cout << 2182;
    for (int i = 0; i < 2182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2182(int& val) {
    const int x = 2182;
    std::cout << 2182;
    for (int i = 0; i < 2182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2183(int& val) {
    func_in_2183(val);
    const int x = 2183;
    std::cout << 2183;
    for (int i = 0; i < 2183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2183(int& val) {
    const int x = 2183;
    std::cout << 2183;
    for (int i = 0; i < 2183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2184(int& val) {
    func_in_2184(val);
    const int x = 2184;
    std::cout << 2184;
    for (int i = 0; i < 2184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2184(int& val) {
    const int x = 2184;
    std::cout << 2184;
    for (int i = 0; i < 2184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2185(int& val) {
    func_in_2185(val);
    const int x = 2185;
    std::cout << 2185;
    for (int i = 0; i < 2185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2185(int& val) {
    const int x = 2185;
    std::cout << 2185;
    for (int i = 0; i < 2185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2186(int& val) {
    func_in_2186(val);
    const int x = 2186;
    std::cout << 2186;
    for (int i = 0; i < 2186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2186(int& val) {
    const int x = 2186;
    std::cout << 2186;
    for (int i = 0; i < 2186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2187(int& val) {
    func_in_2187(val);
    const int x = 2187;
    std::cout << 2187;
    for (int i = 0; i < 2187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2187(int& val) {
    const int x = 2187;
    std::cout << 2187;
    for (int i = 0; i < 2187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2188(int& val) {
    func_in_2188(val);
    const int x = 2188;
    std::cout << 2188;
    for (int i = 0; i < 2188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2188(int& val) {
    const int x = 2188;
    std::cout << 2188;
    for (int i = 0; i < 2188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2189(int& val) {
    func_in_2189(val);
    const int x = 2189;
    std::cout << 2189;
    for (int i = 0; i < 2189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2189(int& val) {
    const int x = 2189;
    std::cout << 2189;
    for (int i = 0; i < 2189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2190(int& val) {
    func_in_2190(val);
    const int x = 2190;
    std::cout << 2190;
    for (int i = 0; i < 2190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2190(int& val) {
    const int x = 2190;
    std::cout << 2190;
    for (int i = 0; i < 2190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2191(int& val) {
    func_in_2191(val);
    const int x = 2191;
    std::cout << 2191;
    for (int i = 0; i < 2191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2191(int& val) {
    const int x = 2191;
    std::cout << 2191;
    for (int i = 0; i < 2191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2192(int& val) {
    func_in_2192(val);
    const int x = 2192;
    std::cout << 2192;
    for (int i = 0; i < 2192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2192(int& val) {
    const int x = 2192;
    std::cout << 2192;
    for (int i = 0; i < 2192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2193(int& val) {
    func_in_2193(val);
    const int x = 2193;
    std::cout << 2193;
    for (int i = 0; i < 2193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2193(int& val) {
    const int x = 2193;
    std::cout << 2193;
    for (int i = 0; i < 2193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2194(int& val) {
    func_in_2194(val);
    const int x = 2194;
    std::cout << 2194;
    for (int i = 0; i < 2194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2194(int& val) {
    const int x = 2194;
    std::cout << 2194;
    for (int i = 0; i < 2194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2195(int& val) {
    func_in_2195(val);
    const int x = 2195;
    std::cout << 2195;
    for (int i = 0; i < 2195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2195(int& val) {
    const int x = 2195;
    std::cout << 2195;
    for (int i = 0; i < 2195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2196(int& val) {
    func_in_2196(val);
    const int x = 2196;
    std::cout << 2196;
    for (int i = 0; i < 2196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2196(int& val) {
    const int x = 2196;
    std::cout << 2196;
    for (int i = 0; i < 2196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2197(int& val) {
    func_in_2197(val);
    const int x = 2197;
    std::cout << 2197;
    for (int i = 0; i < 2197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2197(int& val) {
    const int x = 2197;
    std::cout << 2197;
    for (int i = 0; i < 2197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2198(int& val) {
    func_in_2198(val);
    const int x = 2198;
    std::cout << 2198;
    for (int i = 0; i < 2198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2198(int& val) {
    const int x = 2198;
    std::cout << 2198;
    for (int i = 0; i < 2198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2199(int& val) {
    func_in_2199(val);
    const int x = 2199;
    std::cout << 2199;
    for (int i = 0; i < 2199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2199(int& val) {
    const int x = 2199;
    std::cout << 2199;
    for (int i = 0; i < 2199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2200(int& val) {
    func_in_2200(val);
    const int x = 2200;
    std::cout << 2200;
    for (int i = 0; i < 2200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2200(int& val) {
    const int x = 2200;
    std::cout << 2200;
    for (int i = 0; i < 2200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2201(int& val) {
    func_in_2201(val);
    const int x = 2201;
    std::cout << 2201;
    for (int i = 0; i < 2201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2201(int& val) {
    const int x = 2201;
    std::cout << 2201;
    for (int i = 0; i < 2201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2202(int& val) {
    func_in_2202(val);
    const int x = 2202;
    std::cout << 2202;
    for (int i = 0; i < 2202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2202(int& val) {
    const int x = 2202;
    std::cout << 2202;
    for (int i = 0; i < 2202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2203(int& val) {
    func_in_2203(val);
    const int x = 2203;
    std::cout << 2203;
    for (int i = 0; i < 2203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2203(int& val) {
    const int x = 2203;
    std::cout << 2203;
    for (int i = 0; i < 2203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2204(int& val) {
    func_in_2204(val);
    const int x = 2204;
    std::cout << 2204;
    for (int i = 0; i < 2204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2204(int& val) {
    const int x = 2204;
    std::cout << 2204;
    for (int i = 0; i < 2204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2205(int& val) {
    func_in_2205(val);
    const int x = 2205;
    std::cout << 2205;
    for (int i = 0; i < 2205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2205(int& val) {
    const int x = 2205;
    std::cout << 2205;
    for (int i = 0; i < 2205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2206(int& val) {
    func_in_2206(val);
    const int x = 2206;
    std::cout << 2206;
    for (int i = 0; i < 2206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2206(int& val) {
    const int x = 2206;
    std::cout << 2206;
    for (int i = 0; i < 2206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2207(int& val) {
    func_in_2207(val);
    const int x = 2207;
    std::cout << 2207;
    for (int i = 0; i < 2207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2207(int& val) {
    const int x = 2207;
    std::cout << 2207;
    for (int i = 0; i < 2207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2208(int& val) {
    func_in_2208(val);
    const int x = 2208;
    std::cout << 2208;
    for (int i = 0; i < 2208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2208(int& val) {
    const int x = 2208;
    std::cout << 2208;
    for (int i = 0; i < 2208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2209(int& val) {
    func_in_2209(val);
    const int x = 2209;
    std::cout << 2209;
    for (int i = 0; i < 2209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2209(int& val) {
    const int x = 2209;
    std::cout << 2209;
    for (int i = 0; i < 2209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2210(int& val) {
    func_in_2210(val);
    const int x = 2210;
    std::cout << 2210;
    for (int i = 0; i < 2210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2210(int& val) {
    const int x = 2210;
    std::cout << 2210;
    for (int i = 0; i < 2210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2211(int& val) {
    func_in_2211(val);
    const int x = 2211;
    std::cout << 2211;
    for (int i = 0; i < 2211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2211(int& val) {
    const int x = 2211;
    std::cout << 2211;
    for (int i = 0; i < 2211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2212(int& val) {
    func_in_2212(val);
    const int x = 2212;
    std::cout << 2212;
    for (int i = 0; i < 2212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2212(int& val) {
    const int x = 2212;
    std::cout << 2212;
    for (int i = 0; i < 2212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2213(int& val) {
    func_in_2213(val);
    const int x = 2213;
    std::cout << 2213;
    for (int i = 0; i < 2213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2213(int& val) {
    const int x = 2213;
    std::cout << 2213;
    for (int i = 0; i < 2213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2214(int& val) {
    func_in_2214(val);
    const int x = 2214;
    std::cout << 2214;
    for (int i = 0; i < 2214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2214(int& val) {
    const int x = 2214;
    std::cout << 2214;
    for (int i = 0; i < 2214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2215(int& val) {
    func_in_2215(val);
    const int x = 2215;
    std::cout << 2215;
    for (int i = 0; i < 2215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2215(int& val) {
    const int x = 2215;
    std::cout << 2215;
    for (int i = 0; i < 2215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2216(int& val) {
    func_in_2216(val);
    const int x = 2216;
    std::cout << 2216;
    for (int i = 0; i < 2216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2216(int& val) {
    const int x = 2216;
    std::cout << 2216;
    for (int i = 0; i < 2216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2217(int& val) {
    func_in_2217(val);
    const int x = 2217;
    std::cout << 2217;
    for (int i = 0; i < 2217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2217(int& val) {
    const int x = 2217;
    std::cout << 2217;
    for (int i = 0; i < 2217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2218(int& val) {
    func_in_2218(val);
    const int x = 2218;
    std::cout << 2218;
    for (int i = 0; i < 2218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2218(int& val) {
    const int x = 2218;
    std::cout << 2218;
    for (int i = 0; i < 2218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2219(int& val) {
    func_in_2219(val);
    const int x = 2219;
    std::cout << 2219;
    for (int i = 0; i < 2219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2219(int& val) {
    const int x = 2219;
    std::cout << 2219;
    for (int i = 0; i < 2219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2220(int& val) {
    func_in_2220(val);
    const int x = 2220;
    std::cout << 2220;
    for (int i = 0; i < 2220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2220(int& val) {
    const int x = 2220;
    std::cout << 2220;
    for (int i = 0; i < 2220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2221(int& val) {
    func_in_2221(val);
    const int x = 2221;
    std::cout << 2221;
    for (int i = 0; i < 2221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2221(int& val) {
    const int x = 2221;
    std::cout << 2221;
    for (int i = 0; i < 2221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2222(int& val) {
    func_in_2222(val);
    const int x = 2222;
    std::cout << 2222;
    for (int i = 0; i < 2222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2222(int& val) {
    const int x = 2222;
    std::cout << 2222;
    for (int i = 0; i < 2222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2223(int& val) {
    func_in_2223(val);
    const int x = 2223;
    std::cout << 2223;
    for (int i = 0; i < 2223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2223(int& val) {
    const int x = 2223;
    std::cout << 2223;
    for (int i = 0; i < 2223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2224(int& val) {
    func_in_2224(val);
    const int x = 2224;
    std::cout << 2224;
    for (int i = 0; i < 2224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2224(int& val) {
    const int x = 2224;
    std::cout << 2224;
    for (int i = 0; i < 2224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2225(int& val) {
    func_in_2225(val);
    const int x = 2225;
    std::cout << 2225;
    for (int i = 0; i < 2225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2225(int& val) {
    const int x = 2225;
    std::cout << 2225;
    for (int i = 0; i < 2225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2226(int& val) {
    func_in_2226(val);
    const int x = 2226;
    std::cout << 2226;
    for (int i = 0; i < 2226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2226(int& val) {
    const int x = 2226;
    std::cout << 2226;
    for (int i = 0; i < 2226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2227(int& val) {
    func_in_2227(val);
    const int x = 2227;
    std::cout << 2227;
    for (int i = 0; i < 2227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2227(int& val) {
    const int x = 2227;
    std::cout << 2227;
    for (int i = 0; i < 2227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2228(int& val) {
    func_in_2228(val);
    const int x = 2228;
    std::cout << 2228;
    for (int i = 0; i < 2228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2228(int& val) {
    const int x = 2228;
    std::cout << 2228;
    for (int i = 0; i < 2228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2229(int& val) {
    func_in_2229(val);
    const int x = 2229;
    std::cout << 2229;
    for (int i = 0; i < 2229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2229(int& val) {
    const int x = 2229;
    std::cout << 2229;
    for (int i = 0; i < 2229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2230(int& val) {
    func_in_2230(val);
    const int x = 2230;
    std::cout << 2230;
    for (int i = 0; i < 2230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2230(int& val) {
    const int x = 2230;
    std::cout << 2230;
    for (int i = 0; i < 2230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2231(int& val) {
    func_in_2231(val);
    const int x = 2231;
    std::cout << 2231;
    for (int i = 0; i < 2231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2231(int& val) {
    const int x = 2231;
    std::cout << 2231;
    for (int i = 0; i < 2231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2232(int& val) {
    func_in_2232(val);
    const int x = 2232;
    std::cout << 2232;
    for (int i = 0; i < 2232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2232(int& val) {
    const int x = 2232;
    std::cout << 2232;
    for (int i = 0; i < 2232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2233(int& val) {
    func_in_2233(val);
    const int x = 2233;
    std::cout << 2233;
    for (int i = 0; i < 2233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2233(int& val) {
    const int x = 2233;
    std::cout << 2233;
    for (int i = 0; i < 2233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2234(int& val) {
    func_in_2234(val);
    const int x = 2234;
    std::cout << 2234;
    for (int i = 0; i < 2234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2234(int& val) {
    const int x = 2234;
    std::cout << 2234;
    for (int i = 0; i < 2234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2235(int& val) {
    func_in_2235(val);
    const int x = 2235;
    std::cout << 2235;
    for (int i = 0; i < 2235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2235(int& val) {
    const int x = 2235;
    std::cout << 2235;
    for (int i = 0; i < 2235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2236(int& val) {
    func_in_2236(val);
    const int x = 2236;
    std::cout << 2236;
    for (int i = 0; i < 2236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2236(int& val) {
    const int x = 2236;
    std::cout << 2236;
    for (int i = 0; i < 2236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2237(int& val) {
    func_in_2237(val);
    const int x = 2237;
    std::cout << 2237;
    for (int i = 0; i < 2237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2237(int& val) {
    const int x = 2237;
    std::cout << 2237;
    for (int i = 0; i < 2237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2238(int& val) {
    func_in_2238(val);
    const int x = 2238;
    std::cout << 2238;
    for (int i = 0; i < 2238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2238(int& val) {
    const int x = 2238;
    std::cout << 2238;
    for (int i = 0; i < 2238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2239(int& val) {
    func_in_2239(val);
    const int x = 2239;
    std::cout << 2239;
    for (int i = 0; i < 2239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2239(int& val) {
    const int x = 2239;
    std::cout << 2239;
    for (int i = 0; i < 2239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2240(int& val) {
    func_in_2240(val);
    const int x = 2240;
    std::cout << 2240;
    for (int i = 0; i < 2240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2240(int& val) {
    const int x = 2240;
    std::cout << 2240;
    for (int i = 0; i < 2240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2241(int& val) {
    func_in_2241(val);
    const int x = 2241;
    std::cout << 2241;
    for (int i = 0; i < 2241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2241(int& val) {
    const int x = 2241;
    std::cout << 2241;
    for (int i = 0; i < 2241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2242(int& val) {
    func_in_2242(val);
    const int x = 2242;
    std::cout << 2242;
    for (int i = 0; i < 2242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2242(int& val) {
    const int x = 2242;
    std::cout << 2242;
    for (int i = 0; i < 2242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2243(int& val) {
    func_in_2243(val);
    const int x = 2243;
    std::cout << 2243;
    for (int i = 0; i < 2243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2243(int& val) {
    const int x = 2243;
    std::cout << 2243;
    for (int i = 0; i < 2243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2244(int& val) {
    func_in_2244(val);
    const int x = 2244;
    std::cout << 2244;
    for (int i = 0; i < 2244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2244(int& val) {
    const int x = 2244;
    std::cout << 2244;
    for (int i = 0; i < 2244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2245(int& val) {
    func_in_2245(val);
    const int x = 2245;
    std::cout << 2245;
    for (int i = 0; i < 2245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2245(int& val) {
    const int x = 2245;
    std::cout << 2245;
    for (int i = 0; i < 2245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2246(int& val) {
    func_in_2246(val);
    const int x = 2246;
    std::cout << 2246;
    for (int i = 0; i < 2246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2246(int& val) {
    const int x = 2246;
    std::cout << 2246;
    for (int i = 0; i < 2246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2247(int& val) {
    func_in_2247(val);
    const int x = 2247;
    std::cout << 2247;
    for (int i = 0; i < 2247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2247(int& val) {
    const int x = 2247;
    std::cout << 2247;
    for (int i = 0; i < 2247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2248(int& val) {
    func_in_2248(val);
    const int x = 2248;
    std::cout << 2248;
    for (int i = 0; i < 2248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2248(int& val) {
    const int x = 2248;
    std::cout << 2248;
    for (int i = 0; i < 2248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2249(int& val) {
    func_in_2249(val);
    const int x = 2249;
    std::cout << 2249;
    for (int i = 0; i < 2249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2249(int& val) {
    const int x = 2249;
    std::cout << 2249;
    for (int i = 0; i < 2249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2250(int& val) {
    func_in_2250(val);
    const int x = 2250;
    std::cout << 2250;
    for (int i = 0; i < 2250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2250(int& val) {
    const int x = 2250;
    std::cout << 2250;
    for (int i = 0; i < 2250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2251(int& val) {
    func_in_2251(val);
    const int x = 2251;
    std::cout << 2251;
    for (int i = 0; i < 2251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2251(int& val) {
    const int x = 2251;
    std::cout << 2251;
    for (int i = 0; i < 2251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2252(int& val) {
    func_in_2252(val);
    const int x = 2252;
    std::cout << 2252;
    for (int i = 0; i < 2252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2252(int& val) {
    const int x = 2252;
    std::cout << 2252;
    for (int i = 0; i < 2252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2253(int& val) {
    func_in_2253(val);
    const int x = 2253;
    std::cout << 2253;
    for (int i = 0; i < 2253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2253(int& val) {
    const int x = 2253;
    std::cout << 2253;
    for (int i = 0; i < 2253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2254(int& val) {
    func_in_2254(val);
    const int x = 2254;
    std::cout << 2254;
    for (int i = 0; i < 2254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2254(int& val) {
    const int x = 2254;
    std::cout << 2254;
    for (int i = 0; i < 2254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2255(int& val) {
    func_in_2255(val);
    const int x = 2255;
    std::cout << 2255;
    for (int i = 0; i < 2255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2255(int& val) {
    const int x = 2255;
    std::cout << 2255;
    for (int i = 0; i < 2255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2256(int& val) {
    func_in_2256(val);
    const int x = 2256;
    std::cout << 2256;
    for (int i = 0; i < 2256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2256(int& val) {
    const int x = 2256;
    std::cout << 2256;
    for (int i = 0; i < 2256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2257(int& val) {
    func_in_2257(val);
    const int x = 2257;
    std::cout << 2257;
    for (int i = 0; i < 2257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2257(int& val) {
    const int x = 2257;
    std::cout << 2257;
    for (int i = 0; i < 2257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2258(int& val) {
    func_in_2258(val);
    const int x = 2258;
    std::cout << 2258;
    for (int i = 0; i < 2258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2258(int& val) {
    const int x = 2258;
    std::cout << 2258;
    for (int i = 0; i < 2258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2259(int& val) {
    func_in_2259(val);
    const int x = 2259;
    std::cout << 2259;
    for (int i = 0; i < 2259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2259(int& val) {
    const int x = 2259;
    std::cout << 2259;
    for (int i = 0; i < 2259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2260(int& val) {
    func_in_2260(val);
    const int x = 2260;
    std::cout << 2260;
    for (int i = 0; i < 2260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2260(int& val) {
    const int x = 2260;
    std::cout << 2260;
    for (int i = 0; i < 2260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2261(int& val) {
    func_in_2261(val);
    const int x = 2261;
    std::cout << 2261;
    for (int i = 0; i < 2261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2261(int& val) {
    const int x = 2261;
    std::cout << 2261;
    for (int i = 0; i < 2261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2262(int& val) {
    func_in_2262(val);
    const int x = 2262;
    std::cout << 2262;
    for (int i = 0; i < 2262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2262(int& val) {
    const int x = 2262;
    std::cout << 2262;
    for (int i = 0; i < 2262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2263(int& val) {
    func_in_2263(val);
    const int x = 2263;
    std::cout << 2263;
    for (int i = 0; i < 2263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2263(int& val) {
    const int x = 2263;
    std::cout << 2263;
    for (int i = 0; i < 2263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2264(int& val) {
    func_in_2264(val);
    const int x = 2264;
    std::cout << 2264;
    for (int i = 0; i < 2264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2264(int& val) {
    const int x = 2264;
    std::cout << 2264;
    for (int i = 0; i < 2264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2265(int& val) {
    func_in_2265(val);
    const int x = 2265;
    std::cout << 2265;
    for (int i = 0; i < 2265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2265(int& val) {
    const int x = 2265;
    std::cout << 2265;
    for (int i = 0; i < 2265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2266(int& val) {
    func_in_2266(val);
    const int x = 2266;
    std::cout << 2266;
    for (int i = 0; i < 2266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2266(int& val) {
    const int x = 2266;
    std::cout << 2266;
    for (int i = 0; i < 2266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2267(int& val) {
    func_in_2267(val);
    const int x = 2267;
    std::cout << 2267;
    for (int i = 0; i < 2267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2267(int& val) {
    const int x = 2267;
    std::cout << 2267;
    for (int i = 0; i < 2267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2268(int& val) {
    func_in_2268(val);
    const int x = 2268;
    std::cout << 2268;
    for (int i = 0; i < 2268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2268(int& val) {
    const int x = 2268;
    std::cout << 2268;
    for (int i = 0; i < 2268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2269(int& val) {
    func_in_2269(val);
    const int x = 2269;
    std::cout << 2269;
    for (int i = 0; i < 2269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2269(int& val) {
    const int x = 2269;
    std::cout << 2269;
    for (int i = 0; i < 2269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2270(int& val) {
    func_in_2270(val);
    const int x = 2270;
    std::cout << 2270;
    for (int i = 0; i < 2270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2270(int& val) {
    const int x = 2270;
    std::cout << 2270;
    for (int i = 0; i < 2270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2271(int& val) {
    func_in_2271(val);
    const int x = 2271;
    std::cout << 2271;
    for (int i = 0; i < 2271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2271(int& val) {
    const int x = 2271;
    std::cout << 2271;
    for (int i = 0; i < 2271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2272(int& val) {
    func_in_2272(val);
    const int x = 2272;
    std::cout << 2272;
    for (int i = 0; i < 2272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2272(int& val) {
    const int x = 2272;
    std::cout << 2272;
    for (int i = 0; i < 2272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2273(int& val) {
    func_in_2273(val);
    const int x = 2273;
    std::cout << 2273;
    for (int i = 0; i < 2273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2273(int& val) {
    const int x = 2273;
    std::cout << 2273;
    for (int i = 0; i < 2273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2274(int& val) {
    func_in_2274(val);
    const int x = 2274;
    std::cout << 2274;
    for (int i = 0; i < 2274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2274(int& val) {
    const int x = 2274;
    std::cout << 2274;
    for (int i = 0; i < 2274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2275(int& val) {
    func_in_2275(val);
    const int x = 2275;
    std::cout << 2275;
    for (int i = 0; i < 2275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2275(int& val) {
    const int x = 2275;
    std::cout << 2275;
    for (int i = 0; i < 2275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2276(int& val) {
    func_in_2276(val);
    const int x = 2276;
    std::cout << 2276;
    for (int i = 0; i < 2276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2276(int& val) {
    const int x = 2276;
    std::cout << 2276;
    for (int i = 0; i < 2276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2277(int& val) {
    func_in_2277(val);
    const int x = 2277;
    std::cout << 2277;
    for (int i = 0; i < 2277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2277(int& val) {
    const int x = 2277;
    std::cout << 2277;
    for (int i = 0; i < 2277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2278(int& val) {
    func_in_2278(val);
    const int x = 2278;
    std::cout << 2278;
    for (int i = 0; i < 2278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2278(int& val) {
    const int x = 2278;
    std::cout << 2278;
    for (int i = 0; i < 2278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2279(int& val) {
    func_in_2279(val);
    const int x = 2279;
    std::cout << 2279;
    for (int i = 0; i < 2279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2279(int& val) {
    const int x = 2279;
    std::cout << 2279;
    for (int i = 0; i < 2279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2280(int& val) {
    func_in_2280(val);
    const int x = 2280;
    std::cout << 2280;
    for (int i = 0; i < 2280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2280(int& val) {
    const int x = 2280;
    std::cout << 2280;
    for (int i = 0; i < 2280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2281(int& val) {
    func_in_2281(val);
    const int x = 2281;
    std::cout << 2281;
    for (int i = 0; i < 2281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2281(int& val) {
    const int x = 2281;
    std::cout << 2281;
    for (int i = 0; i < 2281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2282(int& val) {
    func_in_2282(val);
    const int x = 2282;
    std::cout << 2282;
    for (int i = 0; i < 2282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2282(int& val) {
    const int x = 2282;
    std::cout << 2282;
    for (int i = 0; i < 2282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2283(int& val) {
    func_in_2283(val);
    const int x = 2283;
    std::cout << 2283;
    for (int i = 0; i < 2283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2283(int& val) {
    const int x = 2283;
    std::cout << 2283;
    for (int i = 0; i < 2283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2284(int& val) {
    func_in_2284(val);
    const int x = 2284;
    std::cout << 2284;
    for (int i = 0; i < 2284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2284(int& val) {
    const int x = 2284;
    std::cout << 2284;
    for (int i = 0; i < 2284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2285(int& val) {
    func_in_2285(val);
    const int x = 2285;
    std::cout << 2285;
    for (int i = 0; i < 2285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2285(int& val) {
    const int x = 2285;
    std::cout << 2285;
    for (int i = 0; i < 2285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2286(int& val) {
    func_in_2286(val);
    const int x = 2286;
    std::cout << 2286;
    for (int i = 0; i < 2286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2286(int& val) {
    const int x = 2286;
    std::cout << 2286;
    for (int i = 0; i < 2286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2287(int& val) {
    func_in_2287(val);
    const int x = 2287;
    std::cout << 2287;
    for (int i = 0; i < 2287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2287(int& val) {
    const int x = 2287;
    std::cout << 2287;
    for (int i = 0; i < 2287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2288(int& val) {
    func_in_2288(val);
    const int x = 2288;
    std::cout << 2288;
    for (int i = 0; i < 2288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2288(int& val) {
    const int x = 2288;
    std::cout << 2288;
    for (int i = 0; i < 2288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2289(int& val) {
    func_in_2289(val);
    const int x = 2289;
    std::cout << 2289;
    for (int i = 0; i < 2289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2289(int& val) {
    const int x = 2289;
    std::cout << 2289;
    for (int i = 0; i < 2289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2290(int& val) {
    func_in_2290(val);
    const int x = 2290;
    std::cout << 2290;
    for (int i = 0; i < 2290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2290(int& val) {
    const int x = 2290;
    std::cout << 2290;
    for (int i = 0; i < 2290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2291(int& val) {
    func_in_2291(val);
    const int x = 2291;
    std::cout << 2291;
    for (int i = 0; i < 2291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2291(int& val) {
    const int x = 2291;
    std::cout << 2291;
    for (int i = 0; i < 2291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2292(int& val) {
    func_in_2292(val);
    const int x = 2292;
    std::cout << 2292;
    for (int i = 0; i < 2292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2292(int& val) {
    const int x = 2292;
    std::cout << 2292;
    for (int i = 0; i < 2292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2293(int& val) {
    func_in_2293(val);
    const int x = 2293;
    std::cout << 2293;
    for (int i = 0; i < 2293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2293(int& val) {
    const int x = 2293;
    std::cout << 2293;
    for (int i = 0; i < 2293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2294(int& val) {
    func_in_2294(val);
    const int x = 2294;
    std::cout << 2294;
    for (int i = 0; i < 2294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2294(int& val) {
    const int x = 2294;
    std::cout << 2294;
    for (int i = 0; i < 2294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2295(int& val) {
    func_in_2295(val);
    const int x = 2295;
    std::cout << 2295;
    for (int i = 0; i < 2295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2295(int& val) {
    const int x = 2295;
    std::cout << 2295;
    for (int i = 0; i < 2295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2296(int& val) {
    func_in_2296(val);
    const int x = 2296;
    std::cout << 2296;
    for (int i = 0; i < 2296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2296(int& val) {
    const int x = 2296;
    std::cout << 2296;
    for (int i = 0; i < 2296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2297(int& val) {
    func_in_2297(val);
    const int x = 2297;
    std::cout << 2297;
    for (int i = 0; i < 2297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2297(int& val) {
    const int x = 2297;
    std::cout << 2297;
    for (int i = 0; i < 2297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2298(int& val) {
    func_in_2298(val);
    const int x = 2298;
    std::cout << 2298;
    for (int i = 0; i < 2298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2298(int& val) {
    const int x = 2298;
    std::cout << 2298;
    for (int i = 0; i < 2298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2299(int& val) {
    func_in_2299(val);
    const int x = 2299;
    std::cout << 2299;
    for (int i = 0; i < 2299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2299(int& val) {
    const int x = 2299;
    std::cout << 2299;
    for (int i = 0; i < 2299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2300(int& val) {
    func_in_2300(val);
    const int x = 2300;
    std::cout << 2300;
    for (int i = 0; i < 2300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2300(int& val) {
    const int x = 2300;
    std::cout << 2300;
    for (int i = 0; i < 2300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2301(int& val) {
    func_in_2301(val);
    const int x = 2301;
    std::cout << 2301;
    for (int i = 0; i < 2301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2301(int& val) {
    const int x = 2301;
    std::cout << 2301;
    for (int i = 0; i < 2301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2302(int& val) {
    func_in_2302(val);
    const int x = 2302;
    std::cout << 2302;
    for (int i = 0; i < 2302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2302(int& val) {
    const int x = 2302;
    std::cout << 2302;
    for (int i = 0; i < 2302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2303(int& val) {
    func_in_2303(val);
    const int x = 2303;
    std::cout << 2303;
    for (int i = 0; i < 2303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2303(int& val) {
    const int x = 2303;
    std::cout << 2303;
    for (int i = 0; i < 2303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2304(int& val) {
    func_in_2304(val);
    const int x = 2304;
    std::cout << 2304;
    for (int i = 0; i < 2304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2304(int& val) {
    const int x = 2304;
    std::cout << 2304;
    for (int i = 0; i < 2304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2305(int& val) {
    func_in_2305(val);
    const int x = 2305;
    std::cout << 2305;
    for (int i = 0; i < 2305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2305(int& val) {
    const int x = 2305;
    std::cout << 2305;
    for (int i = 0; i < 2305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2306(int& val) {
    func_in_2306(val);
    const int x = 2306;
    std::cout << 2306;
    for (int i = 0; i < 2306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2306(int& val) {
    const int x = 2306;
    std::cout << 2306;
    for (int i = 0; i < 2306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2307(int& val) {
    func_in_2307(val);
    const int x = 2307;
    std::cout << 2307;
    for (int i = 0; i < 2307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2307(int& val) {
    const int x = 2307;
    std::cout << 2307;
    for (int i = 0; i < 2307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2308(int& val) {
    func_in_2308(val);
    const int x = 2308;
    std::cout << 2308;
    for (int i = 0; i < 2308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2308(int& val) {
    const int x = 2308;
    std::cout << 2308;
    for (int i = 0; i < 2308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2309(int& val) {
    func_in_2309(val);
    const int x = 2309;
    std::cout << 2309;
    for (int i = 0; i < 2309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2309(int& val) {
    const int x = 2309;
    std::cout << 2309;
    for (int i = 0; i < 2309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2310(int& val) {
    func_in_2310(val);
    const int x = 2310;
    std::cout << 2310;
    for (int i = 0; i < 2310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2310(int& val) {
    const int x = 2310;
    std::cout << 2310;
    for (int i = 0; i < 2310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2311(int& val) {
    func_in_2311(val);
    const int x = 2311;
    std::cout << 2311;
    for (int i = 0; i < 2311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2311(int& val) {
    const int x = 2311;
    std::cout << 2311;
    for (int i = 0; i < 2311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2312(int& val) {
    func_in_2312(val);
    const int x = 2312;
    std::cout << 2312;
    for (int i = 0; i < 2312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2312(int& val) {
    const int x = 2312;
    std::cout << 2312;
    for (int i = 0; i < 2312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2313(int& val) {
    func_in_2313(val);
    const int x = 2313;
    std::cout << 2313;
    for (int i = 0; i < 2313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2313(int& val) {
    const int x = 2313;
    std::cout << 2313;
    for (int i = 0; i < 2313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2314(int& val) {
    func_in_2314(val);
    const int x = 2314;
    std::cout << 2314;
    for (int i = 0; i < 2314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2314(int& val) {
    const int x = 2314;
    std::cout << 2314;
    for (int i = 0; i < 2314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2315(int& val) {
    func_in_2315(val);
    const int x = 2315;
    std::cout << 2315;
    for (int i = 0; i < 2315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2315(int& val) {
    const int x = 2315;
    std::cout << 2315;
    for (int i = 0; i < 2315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2316(int& val) {
    func_in_2316(val);
    const int x = 2316;
    std::cout << 2316;
    for (int i = 0; i < 2316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2316(int& val) {
    const int x = 2316;
    std::cout << 2316;
    for (int i = 0; i < 2316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2317(int& val) {
    func_in_2317(val);
    const int x = 2317;
    std::cout << 2317;
    for (int i = 0; i < 2317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2317(int& val) {
    const int x = 2317;
    std::cout << 2317;
    for (int i = 0; i < 2317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2318(int& val) {
    func_in_2318(val);
    const int x = 2318;
    std::cout << 2318;
    for (int i = 0; i < 2318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2318(int& val) {
    const int x = 2318;
    std::cout << 2318;
    for (int i = 0; i < 2318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2319(int& val) {
    func_in_2319(val);
    const int x = 2319;
    std::cout << 2319;
    for (int i = 0; i < 2319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2319(int& val) {
    const int x = 2319;
    std::cout << 2319;
    for (int i = 0; i < 2319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2320(int& val) {
    func_in_2320(val);
    const int x = 2320;
    std::cout << 2320;
    for (int i = 0; i < 2320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2320(int& val) {
    const int x = 2320;
    std::cout << 2320;
    for (int i = 0; i < 2320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2321(int& val) {
    func_in_2321(val);
    const int x = 2321;
    std::cout << 2321;
    for (int i = 0; i < 2321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2321(int& val) {
    const int x = 2321;
    std::cout << 2321;
    for (int i = 0; i < 2321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2322(int& val) {
    func_in_2322(val);
    const int x = 2322;
    std::cout << 2322;
    for (int i = 0; i < 2322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2322(int& val) {
    const int x = 2322;
    std::cout << 2322;
    for (int i = 0; i < 2322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2323(int& val) {
    func_in_2323(val);
    const int x = 2323;
    std::cout << 2323;
    for (int i = 0; i < 2323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2323(int& val) {
    const int x = 2323;
    std::cout << 2323;
    for (int i = 0; i < 2323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2324(int& val) {
    func_in_2324(val);
    const int x = 2324;
    std::cout << 2324;
    for (int i = 0; i < 2324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2324(int& val) {
    const int x = 2324;
    std::cout << 2324;
    for (int i = 0; i < 2324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2325(int& val) {
    func_in_2325(val);
    const int x = 2325;
    std::cout << 2325;
    for (int i = 0; i < 2325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2325(int& val) {
    const int x = 2325;
    std::cout << 2325;
    for (int i = 0; i < 2325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2326(int& val) {
    func_in_2326(val);
    const int x = 2326;
    std::cout << 2326;
    for (int i = 0; i < 2326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2326(int& val) {
    const int x = 2326;
    std::cout << 2326;
    for (int i = 0; i < 2326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2327(int& val) {
    func_in_2327(val);
    const int x = 2327;
    std::cout << 2327;
    for (int i = 0; i < 2327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2327(int& val) {
    const int x = 2327;
    std::cout << 2327;
    for (int i = 0; i < 2327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2328(int& val) {
    func_in_2328(val);
    const int x = 2328;
    std::cout << 2328;
    for (int i = 0; i < 2328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2328(int& val) {
    const int x = 2328;
    std::cout << 2328;
    for (int i = 0; i < 2328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2329(int& val) {
    func_in_2329(val);
    const int x = 2329;
    std::cout << 2329;
    for (int i = 0; i < 2329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2329(int& val) {
    const int x = 2329;
    std::cout << 2329;
    for (int i = 0; i < 2329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2330(int& val) {
    func_in_2330(val);
    const int x = 2330;
    std::cout << 2330;
    for (int i = 0; i < 2330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2330(int& val) {
    const int x = 2330;
    std::cout << 2330;
    for (int i = 0; i < 2330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2331(int& val) {
    func_in_2331(val);
    const int x = 2331;
    std::cout << 2331;
    for (int i = 0; i < 2331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2331(int& val) {
    const int x = 2331;
    std::cout << 2331;
    for (int i = 0; i < 2331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2332(int& val) {
    func_in_2332(val);
    const int x = 2332;
    std::cout << 2332;
    for (int i = 0; i < 2332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2332(int& val) {
    const int x = 2332;
    std::cout << 2332;
    for (int i = 0; i < 2332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2333(int& val) {
    func_in_2333(val);
    const int x = 2333;
    std::cout << 2333;
    for (int i = 0; i < 2333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2333(int& val) {
    const int x = 2333;
    std::cout << 2333;
    for (int i = 0; i < 2333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2334(int& val) {
    func_in_2334(val);
    const int x = 2334;
    std::cout << 2334;
    for (int i = 0; i < 2334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2334(int& val) {
    const int x = 2334;
    std::cout << 2334;
    for (int i = 0; i < 2334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2335(int& val) {
    func_in_2335(val);
    const int x = 2335;
    std::cout << 2335;
    for (int i = 0; i < 2335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2335(int& val) {
    const int x = 2335;
    std::cout << 2335;
    for (int i = 0; i < 2335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2336(int& val) {
    func_in_2336(val);
    const int x = 2336;
    std::cout << 2336;
    for (int i = 0; i < 2336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2336(int& val) {
    const int x = 2336;
    std::cout << 2336;
    for (int i = 0; i < 2336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2337(int& val) {
    func_in_2337(val);
    const int x = 2337;
    std::cout << 2337;
    for (int i = 0; i < 2337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2337(int& val) {
    const int x = 2337;
    std::cout << 2337;
    for (int i = 0; i < 2337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2338(int& val) {
    func_in_2338(val);
    const int x = 2338;
    std::cout << 2338;
    for (int i = 0; i < 2338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2338(int& val) {
    const int x = 2338;
    std::cout << 2338;
    for (int i = 0; i < 2338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2339(int& val) {
    func_in_2339(val);
    const int x = 2339;
    std::cout << 2339;
    for (int i = 0; i < 2339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2339(int& val) {
    const int x = 2339;
    std::cout << 2339;
    for (int i = 0; i < 2339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2340(int& val) {
    func_in_2340(val);
    const int x = 2340;
    std::cout << 2340;
    for (int i = 0; i < 2340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2340(int& val) {
    const int x = 2340;
    std::cout << 2340;
    for (int i = 0; i < 2340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2341(int& val) {
    func_in_2341(val);
    const int x = 2341;
    std::cout << 2341;
    for (int i = 0; i < 2341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2341(int& val) {
    const int x = 2341;
    std::cout << 2341;
    for (int i = 0; i < 2341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2342(int& val) {
    func_in_2342(val);
    const int x = 2342;
    std::cout << 2342;
    for (int i = 0; i < 2342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2342(int& val) {
    const int x = 2342;
    std::cout << 2342;
    for (int i = 0; i < 2342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2343(int& val) {
    func_in_2343(val);
    const int x = 2343;
    std::cout << 2343;
    for (int i = 0; i < 2343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2343(int& val) {
    const int x = 2343;
    std::cout << 2343;
    for (int i = 0; i < 2343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2344(int& val) {
    func_in_2344(val);
    const int x = 2344;
    std::cout << 2344;
    for (int i = 0; i < 2344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2344(int& val) {
    const int x = 2344;
    std::cout << 2344;
    for (int i = 0; i < 2344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2345(int& val) {
    func_in_2345(val);
    const int x = 2345;
    std::cout << 2345;
    for (int i = 0; i < 2345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2345(int& val) {
    const int x = 2345;
    std::cout << 2345;
    for (int i = 0; i < 2345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2346(int& val) {
    func_in_2346(val);
    const int x = 2346;
    std::cout << 2346;
    for (int i = 0; i < 2346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2346(int& val) {
    const int x = 2346;
    std::cout << 2346;
    for (int i = 0; i < 2346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2347(int& val) {
    func_in_2347(val);
    const int x = 2347;
    std::cout << 2347;
    for (int i = 0; i < 2347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2347(int& val) {
    const int x = 2347;
    std::cout << 2347;
    for (int i = 0; i < 2347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2348(int& val) {
    func_in_2348(val);
    const int x = 2348;
    std::cout << 2348;
    for (int i = 0; i < 2348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2348(int& val) {
    const int x = 2348;
    std::cout << 2348;
    for (int i = 0; i < 2348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2349(int& val) {
    func_in_2349(val);
    const int x = 2349;
    std::cout << 2349;
    for (int i = 0; i < 2349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2349(int& val) {
    const int x = 2349;
    std::cout << 2349;
    for (int i = 0; i < 2349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2350(int& val) {
    func_in_2350(val);
    const int x = 2350;
    std::cout << 2350;
    for (int i = 0; i < 2350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2350(int& val) {
    const int x = 2350;
    std::cout << 2350;
    for (int i = 0; i < 2350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2351(int& val) {
    func_in_2351(val);
    const int x = 2351;
    std::cout << 2351;
    for (int i = 0; i < 2351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2351(int& val) {
    const int x = 2351;
    std::cout << 2351;
    for (int i = 0; i < 2351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2352(int& val) {
    func_in_2352(val);
    const int x = 2352;
    std::cout << 2352;
    for (int i = 0; i < 2352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2352(int& val) {
    const int x = 2352;
    std::cout << 2352;
    for (int i = 0; i < 2352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2353(int& val) {
    func_in_2353(val);
    const int x = 2353;
    std::cout << 2353;
    for (int i = 0; i < 2353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2353(int& val) {
    const int x = 2353;
    std::cout << 2353;
    for (int i = 0; i < 2353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2354(int& val) {
    func_in_2354(val);
    const int x = 2354;
    std::cout << 2354;
    for (int i = 0; i < 2354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2354(int& val) {
    const int x = 2354;
    std::cout << 2354;
    for (int i = 0; i < 2354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2355(int& val) {
    func_in_2355(val);
    const int x = 2355;
    std::cout << 2355;
    for (int i = 0; i < 2355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2355(int& val) {
    const int x = 2355;
    std::cout << 2355;
    for (int i = 0; i < 2355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2356(int& val) {
    func_in_2356(val);
    const int x = 2356;
    std::cout << 2356;
    for (int i = 0; i < 2356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2356(int& val) {
    const int x = 2356;
    std::cout << 2356;
    for (int i = 0; i < 2356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2357(int& val) {
    func_in_2357(val);
    const int x = 2357;
    std::cout << 2357;
    for (int i = 0; i < 2357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2357(int& val) {
    const int x = 2357;
    std::cout << 2357;
    for (int i = 0; i < 2357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2358(int& val) {
    func_in_2358(val);
    const int x = 2358;
    std::cout << 2358;
    for (int i = 0; i < 2358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2358(int& val) {
    const int x = 2358;
    std::cout << 2358;
    for (int i = 0; i < 2358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2359(int& val) {
    func_in_2359(val);
    const int x = 2359;
    std::cout << 2359;
    for (int i = 0; i < 2359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2359(int& val) {
    const int x = 2359;
    std::cout << 2359;
    for (int i = 0; i < 2359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2360(int& val) {
    func_in_2360(val);
    const int x = 2360;
    std::cout << 2360;
    for (int i = 0; i < 2360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2360(int& val) {
    const int x = 2360;
    std::cout << 2360;
    for (int i = 0; i < 2360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2361(int& val) {
    func_in_2361(val);
    const int x = 2361;
    std::cout << 2361;
    for (int i = 0; i < 2361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2361(int& val) {
    const int x = 2361;
    std::cout << 2361;
    for (int i = 0; i < 2361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2362(int& val) {
    func_in_2362(val);
    const int x = 2362;
    std::cout << 2362;
    for (int i = 0; i < 2362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2362(int& val) {
    const int x = 2362;
    std::cout << 2362;
    for (int i = 0; i < 2362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2363(int& val) {
    func_in_2363(val);
    const int x = 2363;
    std::cout << 2363;
    for (int i = 0; i < 2363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2363(int& val) {
    const int x = 2363;
    std::cout << 2363;
    for (int i = 0; i < 2363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2364(int& val) {
    func_in_2364(val);
    const int x = 2364;
    std::cout << 2364;
    for (int i = 0; i < 2364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2364(int& val) {
    const int x = 2364;
    std::cout << 2364;
    for (int i = 0; i < 2364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2365(int& val) {
    func_in_2365(val);
    const int x = 2365;
    std::cout << 2365;
    for (int i = 0; i < 2365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2365(int& val) {
    const int x = 2365;
    std::cout << 2365;
    for (int i = 0; i < 2365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2366(int& val) {
    func_in_2366(val);
    const int x = 2366;
    std::cout << 2366;
    for (int i = 0; i < 2366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2366(int& val) {
    const int x = 2366;
    std::cout << 2366;
    for (int i = 0; i < 2366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2367(int& val) {
    func_in_2367(val);
    const int x = 2367;
    std::cout << 2367;
    for (int i = 0; i < 2367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2367(int& val) {
    const int x = 2367;
    std::cout << 2367;
    for (int i = 0; i < 2367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2368(int& val) {
    func_in_2368(val);
    const int x = 2368;
    std::cout << 2368;
    for (int i = 0; i < 2368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2368(int& val) {
    const int x = 2368;
    std::cout << 2368;
    for (int i = 0; i < 2368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2369(int& val) {
    func_in_2369(val);
    const int x = 2369;
    std::cout << 2369;
    for (int i = 0; i < 2369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2369(int& val) {
    const int x = 2369;
    std::cout << 2369;
    for (int i = 0; i < 2369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2370(int& val) {
    func_in_2370(val);
    const int x = 2370;
    std::cout << 2370;
    for (int i = 0; i < 2370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2370(int& val) {
    const int x = 2370;
    std::cout << 2370;
    for (int i = 0; i < 2370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2371(int& val) {
    func_in_2371(val);
    const int x = 2371;
    std::cout << 2371;
    for (int i = 0; i < 2371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2371(int& val) {
    const int x = 2371;
    std::cout << 2371;
    for (int i = 0; i < 2371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2372(int& val) {
    func_in_2372(val);
    const int x = 2372;
    std::cout << 2372;
    for (int i = 0; i < 2372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2372(int& val) {
    const int x = 2372;
    std::cout << 2372;
    for (int i = 0; i < 2372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2373(int& val) {
    func_in_2373(val);
    const int x = 2373;
    std::cout << 2373;
    for (int i = 0; i < 2373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2373(int& val) {
    const int x = 2373;
    std::cout << 2373;
    for (int i = 0; i < 2373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2374(int& val) {
    func_in_2374(val);
    const int x = 2374;
    std::cout << 2374;
    for (int i = 0; i < 2374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2374(int& val) {
    const int x = 2374;
    std::cout << 2374;
    for (int i = 0; i < 2374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2375(int& val) {
    func_in_2375(val);
    const int x = 2375;
    std::cout << 2375;
    for (int i = 0; i < 2375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2375(int& val) {
    const int x = 2375;
    std::cout << 2375;
    for (int i = 0; i < 2375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2376(int& val) {
    func_in_2376(val);
    const int x = 2376;
    std::cout << 2376;
    for (int i = 0; i < 2376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2376(int& val) {
    const int x = 2376;
    std::cout << 2376;
    for (int i = 0; i < 2376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2377(int& val) {
    func_in_2377(val);
    const int x = 2377;
    std::cout << 2377;
    for (int i = 0; i < 2377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2377(int& val) {
    const int x = 2377;
    std::cout << 2377;
    for (int i = 0; i < 2377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2378(int& val) {
    func_in_2378(val);
    const int x = 2378;
    std::cout << 2378;
    for (int i = 0; i < 2378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2378(int& val) {
    const int x = 2378;
    std::cout << 2378;
    for (int i = 0; i < 2378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2379(int& val) {
    func_in_2379(val);
    const int x = 2379;
    std::cout << 2379;
    for (int i = 0; i < 2379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2379(int& val) {
    const int x = 2379;
    std::cout << 2379;
    for (int i = 0; i < 2379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2380(int& val) {
    func_in_2380(val);
    const int x = 2380;
    std::cout << 2380;
    for (int i = 0; i < 2380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2380(int& val) {
    const int x = 2380;
    std::cout << 2380;
    for (int i = 0; i < 2380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2381(int& val) {
    func_in_2381(val);
    const int x = 2381;
    std::cout << 2381;
    for (int i = 0; i < 2381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2381(int& val) {
    const int x = 2381;
    std::cout << 2381;
    for (int i = 0; i < 2381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2382(int& val) {
    func_in_2382(val);
    const int x = 2382;
    std::cout << 2382;
    for (int i = 0; i < 2382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2382(int& val) {
    const int x = 2382;
    std::cout << 2382;
    for (int i = 0; i < 2382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2383(int& val) {
    func_in_2383(val);
    const int x = 2383;
    std::cout << 2383;
    for (int i = 0; i < 2383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2383(int& val) {
    const int x = 2383;
    std::cout << 2383;
    for (int i = 0; i < 2383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2384(int& val) {
    func_in_2384(val);
    const int x = 2384;
    std::cout << 2384;
    for (int i = 0; i < 2384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2384(int& val) {
    const int x = 2384;
    std::cout << 2384;
    for (int i = 0; i < 2384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2385(int& val) {
    func_in_2385(val);
    const int x = 2385;
    std::cout << 2385;
    for (int i = 0; i < 2385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2385(int& val) {
    const int x = 2385;
    std::cout << 2385;
    for (int i = 0; i < 2385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2386(int& val) {
    func_in_2386(val);
    const int x = 2386;
    std::cout << 2386;
    for (int i = 0; i < 2386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2386(int& val) {
    const int x = 2386;
    std::cout << 2386;
    for (int i = 0; i < 2386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2387(int& val) {
    func_in_2387(val);
    const int x = 2387;
    std::cout << 2387;
    for (int i = 0; i < 2387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2387(int& val) {
    const int x = 2387;
    std::cout << 2387;
    for (int i = 0; i < 2387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2388(int& val) {
    func_in_2388(val);
    const int x = 2388;
    std::cout << 2388;
    for (int i = 0; i < 2388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2388(int& val) {
    const int x = 2388;
    std::cout << 2388;
    for (int i = 0; i < 2388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2389(int& val) {
    func_in_2389(val);
    const int x = 2389;
    std::cout << 2389;
    for (int i = 0; i < 2389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2389(int& val) {
    const int x = 2389;
    std::cout << 2389;
    for (int i = 0; i < 2389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2390(int& val) {
    func_in_2390(val);
    const int x = 2390;
    std::cout << 2390;
    for (int i = 0; i < 2390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2390(int& val) {
    const int x = 2390;
    std::cout << 2390;
    for (int i = 0; i < 2390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2391(int& val) {
    func_in_2391(val);
    const int x = 2391;
    std::cout << 2391;
    for (int i = 0; i < 2391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2391(int& val) {
    const int x = 2391;
    std::cout << 2391;
    for (int i = 0; i < 2391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2392(int& val) {
    func_in_2392(val);
    const int x = 2392;
    std::cout << 2392;
    for (int i = 0; i < 2392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2392(int& val) {
    const int x = 2392;
    std::cout << 2392;
    for (int i = 0; i < 2392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2393(int& val) {
    func_in_2393(val);
    const int x = 2393;
    std::cout << 2393;
    for (int i = 0; i < 2393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2393(int& val) {
    const int x = 2393;
    std::cout << 2393;
    for (int i = 0; i < 2393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2394(int& val) {
    func_in_2394(val);
    const int x = 2394;
    std::cout << 2394;
    for (int i = 0; i < 2394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2394(int& val) {
    const int x = 2394;
    std::cout << 2394;
    for (int i = 0; i < 2394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2395(int& val) {
    func_in_2395(val);
    const int x = 2395;
    std::cout << 2395;
    for (int i = 0; i < 2395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2395(int& val) {
    const int x = 2395;
    std::cout << 2395;
    for (int i = 0; i < 2395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2396(int& val) {
    func_in_2396(val);
    const int x = 2396;
    std::cout << 2396;
    for (int i = 0; i < 2396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2396(int& val) {
    const int x = 2396;
    std::cout << 2396;
    for (int i = 0; i < 2396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2397(int& val) {
    func_in_2397(val);
    const int x = 2397;
    std::cout << 2397;
    for (int i = 0; i < 2397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2397(int& val) {
    const int x = 2397;
    std::cout << 2397;
    for (int i = 0; i < 2397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2398(int& val) {
    func_in_2398(val);
    const int x = 2398;
    std::cout << 2398;
    for (int i = 0; i < 2398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2398(int& val) {
    const int x = 2398;
    std::cout << 2398;
    for (int i = 0; i < 2398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2399(int& val) {
    func_in_2399(val);
    const int x = 2399;
    std::cout << 2399;
    for (int i = 0; i < 2399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2399(int& val) {
    const int x = 2399;
    std::cout << 2399;
    for (int i = 0; i < 2399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2400(int& val) {
    func_in_2400(val);
    const int x = 2400;
    std::cout << 2400;
    for (int i = 0; i < 2400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2400(int& val) {
    const int x = 2400;
    std::cout << 2400;
    for (int i = 0; i < 2400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2401(int& val) {
    func_in_2401(val);
    const int x = 2401;
    std::cout << 2401;
    for (int i = 0; i < 2401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2401(int& val) {
    const int x = 2401;
    std::cout << 2401;
    for (int i = 0; i < 2401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2402(int& val) {
    func_in_2402(val);
    const int x = 2402;
    std::cout << 2402;
    for (int i = 0; i < 2402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2402(int& val) {
    const int x = 2402;
    std::cout << 2402;
    for (int i = 0; i < 2402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2403(int& val) {
    func_in_2403(val);
    const int x = 2403;
    std::cout << 2403;
    for (int i = 0; i < 2403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2403(int& val) {
    const int x = 2403;
    std::cout << 2403;
    for (int i = 0; i < 2403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2404(int& val) {
    func_in_2404(val);
    const int x = 2404;
    std::cout << 2404;
    for (int i = 0; i < 2404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2404(int& val) {
    const int x = 2404;
    std::cout << 2404;
    for (int i = 0; i < 2404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2405(int& val) {
    func_in_2405(val);
    const int x = 2405;
    std::cout << 2405;
    for (int i = 0; i < 2405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2405(int& val) {
    const int x = 2405;
    std::cout << 2405;
    for (int i = 0; i < 2405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2406(int& val) {
    func_in_2406(val);
    const int x = 2406;
    std::cout << 2406;
    for (int i = 0; i < 2406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2406(int& val) {
    const int x = 2406;
    std::cout << 2406;
    for (int i = 0; i < 2406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2407(int& val) {
    func_in_2407(val);
    const int x = 2407;
    std::cout << 2407;
    for (int i = 0; i < 2407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2407(int& val) {
    const int x = 2407;
    std::cout << 2407;
    for (int i = 0; i < 2407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2408(int& val) {
    func_in_2408(val);
    const int x = 2408;
    std::cout << 2408;
    for (int i = 0; i < 2408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2408(int& val) {
    const int x = 2408;
    std::cout << 2408;
    for (int i = 0; i < 2408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2409(int& val) {
    func_in_2409(val);
    const int x = 2409;
    std::cout << 2409;
    for (int i = 0; i < 2409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2409(int& val) {
    const int x = 2409;
    std::cout << 2409;
    for (int i = 0; i < 2409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2410(int& val) {
    func_in_2410(val);
    const int x = 2410;
    std::cout << 2410;
    for (int i = 0; i < 2410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2410(int& val) {
    const int x = 2410;
    std::cout << 2410;
    for (int i = 0; i < 2410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2411(int& val) {
    func_in_2411(val);
    const int x = 2411;
    std::cout << 2411;
    for (int i = 0; i < 2411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2411(int& val) {
    const int x = 2411;
    std::cout << 2411;
    for (int i = 0; i < 2411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2412(int& val) {
    func_in_2412(val);
    const int x = 2412;
    std::cout << 2412;
    for (int i = 0; i < 2412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2412(int& val) {
    const int x = 2412;
    std::cout << 2412;
    for (int i = 0; i < 2412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2413(int& val) {
    func_in_2413(val);
    const int x = 2413;
    std::cout << 2413;
    for (int i = 0; i < 2413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2413(int& val) {
    const int x = 2413;
    std::cout << 2413;
    for (int i = 0; i < 2413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2414(int& val) {
    func_in_2414(val);
    const int x = 2414;
    std::cout << 2414;
    for (int i = 0; i < 2414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2414(int& val) {
    const int x = 2414;
    std::cout << 2414;
    for (int i = 0; i < 2414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2415(int& val) {
    func_in_2415(val);
    const int x = 2415;
    std::cout << 2415;
    for (int i = 0; i < 2415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2415(int& val) {
    const int x = 2415;
    std::cout << 2415;
    for (int i = 0; i < 2415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2416(int& val) {
    func_in_2416(val);
    const int x = 2416;
    std::cout << 2416;
    for (int i = 0; i < 2416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2416(int& val) {
    const int x = 2416;
    std::cout << 2416;
    for (int i = 0; i < 2416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2417(int& val) {
    func_in_2417(val);
    const int x = 2417;
    std::cout << 2417;
    for (int i = 0; i < 2417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2417(int& val) {
    const int x = 2417;
    std::cout << 2417;
    for (int i = 0; i < 2417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2418(int& val) {
    func_in_2418(val);
    const int x = 2418;
    std::cout << 2418;
    for (int i = 0; i < 2418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2418(int& val) {
    const int x = 2418;
    std::cout << 2418;
    for (int i = 0; i < 2418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2419(int& val) {
    func_in_2419(val);
    const int x = 2419;
    std::cout << 2419;
    for (int i = 0; i < 2419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2419(int& val) {
    const int x = 2419;
    std::cout << 2419;
    for (int i = 0; i < 2419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2420(int& val) {
    func_in_2420(val);
    const int x = 2420;
    std::cout << 2420;
    for (int i = 0; i < 2420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2420(int& val) {
    const int x = 2420;
    std::cout << 2420;
    for (int i = 0; i < 2420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2421(int& val) {
    func_in_2421(val);
    const int x = 2421;
    std::cout << 2421;
    for (int i = 0; i < 2421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2421(int& val) {
    const int x = 2421;
    std::cout << 2421;
    for (int i = 0; i < 2421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2422(int& val) {
    func_in_2422(val);
    const int x = 2422;
    std::cout << 2422;
    for (int i = 0; i < 2422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2422(int& val) {
    const int x = 2422;
    std::cout << 2422;
    for (int i = 0; i < 2422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2423(int& val) {
    func_in_2423(val);
    const int x = 2423;
    std::cout << 2423;
    for (int i = 0; i < 2423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2423(int& val) {
    const int x = 2423;
    std::cout << 2423;
    for (int i = 0; i < 2423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2424(int& val) {
    func_in_2424(val);
    const int x = 2424;
    std::cout << 2424;
    for (int i = 0; i < 2424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2424(int& val) {
    const int x = 2424;
    std::cout << 2424;
    for (int i = 0; i < 2424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2425(int& val) {
    func_in_2425(val);
    const int x = 2425;
    std::cout << 2425;
    for (int i = 0; i < 2425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2425(int& val) {
    const int x = 2425;
    std::cout << 2425;
    for (int i = 0; i < 2425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2426(int& val) {
    func_in_2426(val);
    const int x = 2426;
    std::cout << 2426;
    for (int i = 0; i < 2426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2426(int& val) {
    const int x = 2426;
    std::cout << 2426;
    for (int i = 0; i < 2426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2427(int& val) {
    func_in_2427(val);
    const int x = 2427;
    std::cout << 2427;
    for (int i = 0; i < 2427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2427(int& val) {
    const int x = 2427;
    std::cout << 2427;
    for (int i = 0; i < 2427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2428(int& val) {
    func_in_2428(val);
    const int x = 2428;
    std::cout << 2428;
    for (int i = 0; i < 2428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2428(int& val) {
    const int x = 2428;
    std::cout << 2428;
    for (int i = 0; i < 2428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2429(int& val) {
    func_in_2429(val);
    const int x = 2429;
    std::cout << 2429;
    for (int i = 0; i < 2429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2429(int& val) {
    const int x = 2429;
    std::cout << 2429;
    for (int i = 0; i < 2429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2430(int& val) {
    func_in_2430(val);
    const int x = 2430;
    std::cout << 2430;
    for (int i = 0; i < 2430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2430(int& val) {
    const int x = 2430;
    std::cout << 2430;
    for (int i = 0; i < 2430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2431(int& val) {
    func_in_2431(val);
    const int x = 2431;
    std::cout << 2431;
    for (int i = 0; i < 2431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2431(int& val) {
    const int x = 2431;
    std::cout << 2431;
    for (int i = 0; i < 2431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2432(int& val) {
    func_in_2432(val);
    const int x = 2432;
    std::cout << 2432;
    for (int i = 0; i < 2432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2432(int& val) {
    const int x = 2432;
    std::cout << 2432;
    for (int i = 0; i < 2432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2433(int& val) {
    func_in_2433(val);
    const int x = 2433;
    std::cout << 2433;
    for (int i = 0; i < 2433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2433(int& val) {
    const int x = 2433;
    std::cout << 2433;
    for (int i = 0; i < 2433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2434(int& val) {
    func_in_2434(val);
    const int x = 2434;
    std::cout << 2434;
    for (int i = 0; i < 2434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2434(int& val) {
    const int x = 2434;
    std::cout << 2434;
    for (int i = 0; i < 2434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2435(int& val) {
    func_in_2435(val);
    const int x = 2435;
    std::cout << 2435;
    for (int i = 0; i < 2435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2435(int& val) {
    const int x = 2435;
    std::cout << 2435;
    for (int i = 0; i < 2435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2436(int& val) {
    func_in_2436(val);
    const int x = 2436;
    std::cout << 2436;
    for (int i = 0; i < 2436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2436(int& val) {
    const int x = 2436;
    std::cout << 2436;
    for (int i = 0; i < 2436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2437(int& val) {
    func_in_2437(val);
    const int x = 2437;
    std::cout << 2437;
    for (int i = 0; i < 2437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2437(int& val) {
    const int x = 2437;
    std::cout << 2437;
    for (int i = 0; i < 2437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2438(int& val) {
    func_in_2438(val);
    const int x = 2438;
    std::cout << 2438;
    for (int i = 0; i < 2438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2438(int& val) {
    const int x = 2438;
    std::cout << 2438;
    for (int i = 0; i < 2438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2439(int& val) {
    func_in_2439(val);
    const int x = 2439;
    std::cout << 2439;
    for (int i = 0; i < 2439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2439(int& val) {
    const int x = 2439;
    std::cout << 2439;
    for (int i = 0; i < 2439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2440(int& val) {
    func_in_2440(val);
    const int x = 2440;
    std::cout << 2440;
    for (int i = 0; i < 2440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2440(int& val) {
    const int x = 2440;
    std::cout << 2440;
    for (int i = 0; i < 2440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2441(int& val) {
    func_in_2441(val);
    const int x = 2441;
    std::cout << 2441;
    for (int i = 0; i < 2441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2441(int& val) {
    const int x = 2441;
    std::cout << 2441;
    for (int i = 0; i < 2441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2442(int& val) {
    func_in_2442(val);
    const int x = 2442;
    std::cout << 2442;
    for (int i = 0; i < 2442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2442(int& val) {
    const int x = 2442;
    std::cout << 2442;
    for (int i = 0; i < 2442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2443(int& val) {
    func_in_2443(val);
    const int x = 2443;
    std::cout << 2443;
    for (int i = 0; i < 2443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2443(int& val) {
    const int x = 2443;
    std::cout << 2443;
    for (int i = 0; i < 2443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2444(int& val) {
    func_in_2444(val);
    const int x = 2444;
    std::cout << 2444;
    for (int i = 0; i < 2444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2444(int& val) {
    const int x = 2444;
    std::cout << 2444;
    for (int i = 0; i < 2444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2445(int& val) {
    func_in_2445(val);
    const int x = 2445;
    std::cout << 2445;
    for (int i = 0; i < 2445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2445(int& val) {
    const int x = 2445;
    std::cout << 2445;
    for (int i = 0; i < 2445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2446(int& val) {
    func_in_2446(val);
    const int x = 2446;
    std::cout << 2446;
    for (int i = 0; i < 2446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2446(int& val) {
    const int x = 2446;
    std::cout << 2446;
    for (int i = 0; i < 2446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2447(int& val) {
    func_in_2447(val);
    const int x = 2447;
    std::cout << 2447;
    for (int i = 0; i < 2447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2447(int& val) {
    const int x = 2447;
    std::cout << 2447;
    for (int i = 0; i < 2447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2448(int& val) {
    func_in_2448(val);
    const int x = 2448;
    std::cout << 2448;
    for (int i = 0; i < 2448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2448(int& val) {
    const int x = 2448;
    std::cout << 2448;
    for (int i = 0; i < 2448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2449(int& val) {
    func_in_2449(val);
    const int x = 2449;
    std::cout << 2449;
    for (int i = 0; i < 2449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2449(int& val) {
    const int x = 2449;
    std::cout << 2449;
    for (int i = 0; i < 2449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2450(int& val) {
    func_in_2450(val);
    const int x = 2450;
    std::cout << 2450;
    for (int i = 0; i < 2450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2450(int& val) {
    const int x = 2450;
    std::cout << 2450;
    for (int i = 0; i < 2450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2451(int& val) {
    func_in_2451(val);
    const int x = 2451;
    std::cout << 2451;
    for (int i = 0; i < 2451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2451(int& val) {
    const int x = 2451;
    std::cout << 2451;
    for (int i = 0; i < 2451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2452(int& val) {
    func_in_2452(val);
    const int x = 2452;
    std::cout << 2452;
    for (int i = 0; i < 2452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2452(int& val) {
    const int x = 2452;
    std::cout << 2452;
    for (int i = 0; i < 2452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2453(int& val) {
    func_in_2453(val);
    const int x = 2453;
    std::cout << 2453;
    for (int i = 0; i < 2453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2453(int& val) {
    const int x = 2453;
    std::cout << 2453;
    for (int i = 0; i < 2453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2454(int& val) {
    func_in_2454(val);
    const int x = 2454;
    std::cout << 2454;
    for (int i = 0; i < 2454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2454(int& val) {
    const int x = 2454;
    std::cout << 2454;
    for (int i = 0; i < 2454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2455(int& val) {
    func_in_2455(val);
    const int x = 2455;
    std::cout << 2455;
    for (int i = 0; i < 2455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2455(int& val) {
    const int x = 2455;
    std::cout << 2455;
    for (int i = 0; i < 2455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2456(int& val) {
    func_in_2456(val);
    const int x = 2456;
    std::cout << 2456;
    for (int i = 0; i < 2456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2456(int& val) {
    const int x = 2456;
    std::cout << 2456;
    for (int i = 0; i < 2456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2457(int& val) {
    func_in_2457(val);
    const int x = 2457;
    std::cout << 2457;
    for (int i = 0; i < 2457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2457(int& val) {
    const int x = 2457;
    std::cout << 2457;
    for (int i = 0; i < 2457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2458(int& val) {
    func_in_2458(val);
    const int x = 2458;
    std::cout << 2458;
    for (int i = 0; i < 2458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2458(int& val) {
    const int x = 2458;
    std::cout << 2458;
    for (int i = 0; i < 2458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2459(int& val) {
    func_in_2459(val);
    const int x = 2459;
    std::cout << 2459;
    for (int i = 0; i < 2459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2459(int& val) {
    const int x = 2459;
    std::cout << 2459;
    for (int i = 0; i < 2459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2460(int& val) {
    func_in_2460(val);
    const int x = 2460;
    std::cout << 2460;
    for (int i = 0; i < 2460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2460(int& val) {
    const int x = 2460;
    std::cout << 2460;
    for (int i = 0; i < 2460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2461(int& val) {
    func_in_2461(val);
    const int x = 2461;
    std::cout << 2461;
    for (int i = 0; i < 2461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2461(int& val) {
    const int x = 2461;
    std::cout << 2461;
    for (int i = 0; i < 2461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2462(int& val) {
    func_in_2462(val);
    const int x = 2462;
    std::cout << 2462;
    for (int i = 0; i < 2462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2462(int& val) {
    const int x = 2462;
    std::cout << 2462;
    for (int i = 0; i < 2462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2463(int& val) {
    func_in_2463(val);
    const int x = 2463;
    std::cout << 2463;
    for (int i = 0; i < 2463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2463(int& val) {
    const int x = 2463;
    std::cout << 2463;
    for (int i = 0; i < 2463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2464(int& val) {
    func_in_2464(val);
    const int x = 2464;
    std::cout << 2464;
    for (int i = 0; i < 2464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2464(int& val) {
    const int x = 2464;
    std::cout << 2464;
    for (int i = 0; i < 2464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2465(int& val) {
    func_in_2465(val);
    const int x = 2465;
    std::cout << 2465;
    for (int i = 0; i < 2465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2465(int& val) {
    const int x = 2465;
    std::cout << 2465;
    for (int i = 0; i < 2465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2466(int& val) {
    func_in_2466(val);
    const int x = 2466;
    std::cout << 2466;
    for (int i = 0; i < 2466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2466(int& val) {
    const int x = 2466;
    std::cout << 2466;
    for (int i = 0; i < 2466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2467(int& val) {
    func_in_2467(val);
    const int x = 2467;
    std::cout << 2467;
    for (int i = 0; i < 2467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2467(int& val) {
    const int x = 2467;
    std::cout << 2467;
    for (int i = 0; i < 2467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2468(int& val) {
    func_in_2468(val);
    const int x = 2468;
    std::cout << 2468;
    for (int i = 0; i < 2468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2468(int& val) {
    const int x = 2468;
    std::cout << 2468;
    for (int i = 0; i < 2468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2469(int& val) {
    func_in_2469(val);
    const int x = 2469;
    std::cout << 2469;
    for (int i = 0; i < 2469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2469(int& val) {
    const int x = 2469;
    std::cout << 2469;
    for (int i = 0; i < 2469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2470(int& val) {
    func_in_2470(val);
    const int x = 2470;
    std::cout << 2470;
    for (int i = 0; i < 2470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2470(int& val) {
    const int x = 2470;
    std::cout << 2470;
    for (int i = 0; i < 2470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2471(int& val) {
    func_in_2471(val);
    const int x = 2471;
    std::cout << 2471;
    for (int i = 0; i < 2471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2471(int& val) {
    const int x = 2471;
    std::cout << 2471;
    for (int i = 0; i < 2471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2472(int& val) {
    func_in_2472(val);
    const int x = 2472;
    std::cout << 2472;
    for (int i = 0; i < 2472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2472(int& val) {
    const int x = 2472;
    std::cout << 2472;
    for (int i = 0; i < 2472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2473(int& val) {
    func_in_2473(val);
    const int x = 2473;
    std::cout << 2473;
    for (int i = 0; i < 2473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2473(int& val) {
    const int x = 2473;
    std::cout << 2473;
    for (int i = 0; i < 2473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2474(int& val) {
    func_in_2474(val);
    const int x = 2474;
    std::cout << 2474;
    for (int i = 0; i < 2474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2474(int& val) {
    const int x = 2474;
    std::cout << 2474;
    for (int i = 0; i < 2474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2475(int& val) {
    func_in_2475(val);
    const int x = 2475;
    std::cout << 2475;
    for (int i = 0; i < 2475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2475(int& val) {
    const int x = 2475;
    std::cout << 2475;
    for (int i = 0; i < 2475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2476(int& val) {
    func_in_2476(val);
    const int x = 2476;
    std::cout << 2476;
    for (int i = 0; i < 2476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2476(int& val) {
    const int x = 2476;
    std::cout << 2476;
    for (int i = 0; i < 2476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2477(int& val) {
    func_in_2477(val);
    const int x = 2477;
    std::cout << 2477;
    for (int i = 0; i < 2477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2477(int& val) {
    const int x = 2477;
    std::cout << 2477;
    for (int i = 0; i < 2477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2478(int& val) {
    func_in_2478(val);
    const int x = 2478;
    std::cout << 2478;
    for (int i = 0; i < 2478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2478(int& val) {
    const int x = 2478;
    std::cout << 2478;
    for (int i = 0; i < 2478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2479(int& val) {
    func_in_2479(val);
    const int x = 2479;
    std::cout << 2479;
    for (int i = 0; i < 2479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2479(int& val) {
    const int x = 2479;
    std::cout << 2479;
    for (int i = 0; i < 2479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2480(int& val) {
    func_in_2480(val);
    const int x = 2480;
    std::cout << 2480;
    for (int i = 0; i < 2480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2480(int& val) {
    const int x = 2480;
    std::cout << 2480;
    for (int i = 0; i < 2480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2481(int& val) {
    func_in_2481(val);
    const int x = 2481;
    std::cout << 2481;
    for (int i = 0; i < 2481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2481(int& val) {
    const int x = 2481;
    std::cout << 2481;
    for (int i = 0; i < 2481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2482(int& val) {
    func_in_2482(val);
    const int x = 2482;
    std::cout << 2482;
    for (int i = 0; i < 2482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2482(int& val) {
    const int x = 2482;
    std::cout << 2482;
    for (int i = 0; i < 2482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2483(int& val) {
    func_in_2483(val);
    const int x = 2483;
    std::cout << 2483;
    for (int i = 0; i < 2483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2483(int& val) {
    const int x = 2483;
    std::cout << 2483;
    for (int i = 0; i < 2483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2484(int& val) {
    func_in_2484(val);
    const int x = 2484;
    std::cout << 2484;
    for (int i = 0; i < 2484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2484(int& val) {
    const int x = 2484;
    std::cout << 2484;
    for (int i = 0; i < 2484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2485(int& val) {
    func_in_2485(val);
    const int x = 2485;
    std::cout << 2485;
    for (int i = 0; i < 2485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2485(int& val) {
    const int x = 2485;
    std::cout << 2485;
    for (int i = 0; i < 2485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2486(int& val) {
    func_in_2486(val);
    const int x = 2486;
    std::cout << 2486;
    for (int i = 0; i < 2486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2486(int& val) {
    const int x = 2486;
    std::cout << 2486;
    for (int i = 0; i < 2486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2487(int& val) {
    func_in_2487(val);
    const int x = 2487;
    std::cout << 2487;
    for (int i = 0; i < 2487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2487(int& val) {
    const int x = 2487;
    std::cout << 2487;
    for (int i = 0; i < 2487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2488(int& val) {
    func_in_2488(val);
    const int x = 2488;
    std::cout << 2488;
    for (int i = 0; i < 2488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2488(int& val) {
    const int x = 2488;
    std::cout << 2488;
    for (int i = 0; i < 2488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2489(int& val) {
    func_in_2489(val);
    const int x = 2489;
    std::cout << 2489;
    for (int i = 0; i < 2489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2489(int& val) {
    const int x = 2489;
    std::cout << 2489;
    for (int i = 0; i < 2489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2490(int& val) {
    func_in_2490(val);
    const int x = 2490;
    std::cout << 2490;
    for (int i = 0; i < 2490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2490(int& val) {
    const int x = 2490;
    std::cout << 2490;
    for (int i = 0; i < 2490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2491(int& val) {
    func_in_2491(val);
    const int x = 2491;
    std::cout << 2491;
    for (int i = 0; i < 2491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2491(int& val) {
    const int x = 2491;
    std::cout << 2491;
    for (int i = 0; i < 2491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2492(int& val) {
    func_in_2492(val);
    const int x = 2492;
    std::cout << 2492;
    for (int i = 0; i < 2492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2492(int& val) {
    const int x = 2492;
    std::cout << 2492;
    for (int i = 0; i < 2492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2493(int& val) {
    func_in_2493(val);
    const int x = 2493;
    std::cout << 2493;
    for (int i = 0; i < 2493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2493(int& val) {
    const int x = 2493;
    std::cout << 2493;
    for (int i = 0; i < 2493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2494(int& val) {
    func_in_2494(val);
    const int x = 2494;
    std::cout << 2494;
    for (int i = 0; i < 2494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2494(int& val) {
    const int x = 2494;
    std::cout << 2494;
    for (int i = 0; i < 2494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2495(int& val) {
    func_in_2495(val);
    const int x = 2495;
    std::cout << 2495;
    for (int i = 0; i < 2495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2495(int& val) {
    const int x = 2495;
    std::cout << 2495;
    for (int i = 0; i < 2495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2496(int& val) {
    func_in_2496(val);
    const int x = 2496;
    std::cout << 2496;
    for (int i = 0; i < 2496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2496(int& val) {
    const int x = 2496;
    std::cout << 2496;
    for (int i = 0; i < 2496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2497(int& val) {
    func_in_2497(val);
    const int x = 2497;
    std::cout << 2497;
    for (int i = 0; i < 2497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2497(int& val) {
    const int x = 2497;
    std::cout << 2497;
    for (int i = 0; i < 2497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2498(int& val) {
    func_in_2498(val);
    const int x = 2498;
    std::cout << 2498;
    for (int i = 0; i < 2498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2498(int& val) {
    const int x = 2498;
    std::cout << 2498;
    for (int i = 0; i < 2498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2499(int& val) {
    func_in_2499(val);
    const int x = 2499;
    std::cout << 2499;
    for (int i = 0; i < 2499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2499(int& val) {
    const int x = 2499;
    std::cout << 2499;
    for (int i = 0; i < 2499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2500(int& val) {
    func_in_2500(val);
    const int x = 2500;
    std::cout << 2500;
    for (int i = 0; i < 2500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2500(int& val) {
    const int x = 2500;
    std::cout << 2500;
    for (int i = 0; i < 2500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2501(int& val) {
    func_in_2501(val);
    const int x = 2501;
    std::cout << 2501;
    for (int i = 0; i < 2501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2501(int& val) {
    const int x = 2501;
    std::cout << 2501;
    for (int i = 0; i < 2501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2502(int& val) {
    func_in_2502(val);
    const int x = 2502;
    std::cout << 2502;
    for (int i = 0; i < 2502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2502(int& val) {
    const int x = 2502;
    std::cout << 2502;
    for (int i = 0; i < 2502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2503(int& val) {
    func_in_2503(val);
    const int x = 2503;
    std::cout << 2503;
    for (int i = 0; i < 2503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2503(int& val) {
    const int x = 2503;
    std::cout << 2503;
    for (int i = 0; i < 2503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2504(int& val) {
    func_in_2504(val);
    const int x = 2504;
    std::cout << 2504;
    for (int i = 0; i < 2504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2504(int& val) {
    const int x = 2504;
    std::cout << 2504;
    for (int i = 0; i < 2504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2505(int& val) {
    func_in_2505(val);
    const int x = 2505;
    std::cout << 2505;
    for (int i = 0; i < 2505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2505(int& val) {
    const int x = 2505;
    std::cout << 2505;
    for (int i = 0; i < 2505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2506(int& val) {
    func_in_2506(val);
    const int x = 2506;
    std::cout << 2506;
    for (int i = 0; i < 2506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2506(int& val) {
    const int x = 2506;
    std::cout << 2506;
    for (int i = 0; i < 2506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2507(int& val) {
    func_in_2507(val);
    const int x = 2507;
    std::cout << 2507;
    for (int i = 0; i < 2507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2507(int& val) {
    const int x = 2507;
    std::cout << 2507;
    for (int i = 0; i < 2507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2508(int& val) {
    func_in_2508(val);
    const int x = 2508;
    std::cout << 2508;
    for (int i = 0; i < 2508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2508(int& val) {
    const int x = 2508;
    std::cout << 2508;
    for (int i = 0; i < 2508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2509(int& val) {
    func_in_2509(val);
    const int x = 2509;
    std::cout << 2509;
    for (int i = 0; i < 2509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2509(int& val) {
    const int x = 2509;
    std::cout << 2509;
    for (int i = 0; i < 2509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2510(int& val) {
    func_in_2510(val);
    const int x = 2510;
    std::cout << 2510;
    for (int i = 0; i < 2510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2510(int& val) {
    const int x = 2510;
    std::cout << 2510;
    for (int i = 0; i < 2510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2511(int& val) {
    func_in_2511(val);
    const int x = 2511;
    std::cout << 2511;
    for (int i = 0; i < 2511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2511(int& val) {
    const int x = 2511;
    std::cout << 2511;
    for (int i = 0; i < 2511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2512(int& val) {
    func_in_2512(val);
    const int x = 2512;
    std::cout << 2512;
    for (int i = 0; i < 2512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2512(int& val) {
    const int x = 2512;
    std::cout << 2512;
    for (int i = 0; i < 2512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2513(int& val) {
    func_in_2513(val);
    const int x = 2513;
    std::cout << 2513;
    for (int i = 0; i < 2513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2513(int& val) {
    const int x = 2513;
    std::cout << 2513;
    for (int i = 0; i < 2513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2514(int& val) {
    func_in_2514(val);
    const int x = 2514;
    std::cout << 2514;
    for (int i = 0; i < 2514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2514(int& val) {
    const int x = 2514;
    std::cout << 2514;
    for (int i = 0; i < 2514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2515(int& val) {
    func_in_2515(val);
    const int x = 2515;
    std::cout << 2515;
    for (int i = 0; i < 2515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2515(int& val) {
    const int x = 2515;
    std::cout << 2515;
    for (int i = 0; i < 2515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2516(int& val) {
    func_in_2516(val);
    const int x = 2516;
    std::cout << 2516;
    for (int i = 0; i < 2516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2516(int& val) {
    const int x = 2516;
    std::cout << 2516;
    for (int i = 0; i < 2516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2517(int& val) {
    func_in_2517(val);
    const int x = 2517;
    std::cout << 2517;
    for (int i = 0; i < 2517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2517(int& val) {
    const int x = 2517;
    std::cout << 2517;
    for (int i = 0; i < 2517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2518(int& val) {
    func_in_2518(val);
    const int x = 2518;
    std::cout << 2518;
    for (int i = 0; i < 2518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2518(int& val) {
    const int x = 2518;
    std::cout << 2518;
    for (int i = 0; i < 2518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2519(int& val) {
    func_in_2519(val);
    const int x = 2519;
    std::cout << 2519;
    for (int i = 0; i < 2519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2519(int& val) {
    const int x = 2519;
    std::cout << 2519;
    for (int i = 0; i < 2519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2520(int& val) {
    func_in_2520(val);
    const int x = 2520;
    std::cout << 2520;
    for (int i = 0; i < 2520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2520(int& val) {
    const int x = 2520;
    std::cout << 2520;
    for (int i = 0; i < 2520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2521(int& val) {
    func_in_2521(val);
    const int x = 2521;
    std::cout << 2521;
    for (int i = 0; i < 2521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2521(int& val) {
    const int x = 2521;
    std::cout << 2521;
    for (int i = 0; i < 2521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2522(int& val) {
    func_in_2522(val);
    const int x = 2522;
    std::cout << 2522;
    for (int i = 0; i < 2522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2522(int& val) {
    const int x = 2522;
    std::cout << 2522;
    for (int i = 0; i < 2522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2523(int& val) {
    func_in_2523(val);
    const int x = 2523;
    std::cout << 2523;
    for (int i = 0; i < 2523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2523(int& val) {
    const int x = 2523;
    std::cout << 2523;
    for (int i = 0; i < 2523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2524(int& val) {
    func_in_2524(val);
    const int x = 2524;
    std::cout << 2524;
    for (int i = 0; i < 2524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2524(int& val) {
    const int x = 2524;
    std::cout << 2524;
    for (int i = 0; i < 2524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2525(int& val) {
    func_in_2525(val);
    const int x = 2525;
    std::cout << 2525;
    for (int i = 0; i < 2525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2525(int& val) {
    const int x = 2525;
    std::cout << 2525;
    for (int i = 0; i < 2525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2526(int& val) {
    func_in_2526(val);
    const int x = 2526;
    std::cout << 2526;
    for (int i = 0; i < 2526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2526(int& val) {
    const int x = 2526;
    std::cout << 2526;
    for (int i = 0; i < 2526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2527(int& val) {
    func_in_2527(val);
    const int x = 2527;
    std::cout << 2527;
    for (int i = 0; i < 2527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2527(int& val) {
    const int x = 2527;
    std::cout << 2527;
    for (int i = 0; i < 2527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2528(int& val) {
    func_in_2528(val);
    const int x = 2528;
    std::cout << 2528;
    for (int i = 0; i < 2528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2528(int& val) {
    const int x = 2528;
    std::cout << 2528;
    for (int i = 0; i < 2528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2529(int& val) {
    func_in_2529(val);
    const int x = 2529;
    std::cout << 2529;
    for (int i = 0; i < 2529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2529(int& val) {
    const int x = 2529;
    std::cout << 2529;
    for (int i = 0; i < 2529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2530(int& val) {
    func_in_2530(val);
    const int x = 2530;
    std::cout << 2530;
    for (int i = 0; i < 2530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2530(int& val) {
    const int x = 2530;
    std::cout << 2530;
    for (int i = 0; i < 2530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2531(int& val) {
    func_in_2531(val);
    const int x = 2531;
    std::cout << 2531;
    for (int i = 0; i < 2531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2531(int& val) {
    const int x = 2531;
    std::cout << 2531;
    for (int i = 0; i < 2531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2532(int& val) {
    func_in_2532(val);
    const int x = 2532;
    std::cout << 2532;
    for (int i = 0; i < 2532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2532(int& val) {
    const int x = 2532;
    std::cout << 2532;
    for (int i = 0; i < 2532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2533(int& val) {
    func_in_2533(val);
    const int x = 2533;
    std::cout << 2533;
    for (int i = 0; i < 2533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2533(int& val) {
    const int x = 2533;
    std::cout << 2533;
    for (int i = 0; i < 2533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2534(int& val) {
    func_in_2534(val);
    const int x = 2534;
    std::cout << 2534;
    for (int i = 0; i < 2534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2534(int& val) {
    const int x = 2534;
    std::cout << 2534;
    for (int i = 0; i < 2534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2535(int& val) {
    func_in_2535(val);
    const int x = 2535;
    std::cout << 2535;
    for (int i = 0; i < 2535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2535(int& val) {
    const int x = 2535;
    std::cout << 2535;
    for (int i = 0; i < 2535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2536(int& val) {
    func_in_2536(val);
    const int x = 2536;
    std::cout << 2536;
    for (int i = 0; i < 2536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2536(int& val) {
    const int x = 2536;
    std::cout << 2536;
    for (int i = 0; i < 2536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2537(int& val) {
    func_in_2537(val);
    const int x = 2537;
    std::cout << 2537;
    for (int i = 0; i < 2537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2537(int& val) {
    const int x = 2537;
    std::cout << 2537;
    for (int i = 0; i < 2537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2538(int& val) {
    func_in_2538(val);
    const int x = 2538;
    std::cout << 2538;
    for (int i = 0; i < 2538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2538(int& val) {
    const int x = 2538;
    std::cout << 2538;
    for (int i = 0; i < 2538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2539(int& val) {
    func_in_2539(val);
    const int x = 2539;
    std::cout << 2539;
    for (int i = 0; i < 2539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2539(int& val) {
    const int x = 2539;
    std::cout << 2539;
    for (int i = 0; i < 2539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2540(int& val) {
    func_in_2540(val);
    const int x = 2540;
    std::cout << 2540;
    for (int i = 0; i < 2540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2540(int& val) {
    const int x = 2540;
    std::cout << 2540;
    for (int i = 0; i < 2540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2541(int& val) {
    func_in_2541(val);
    const int x = 2541;
    std::cout << 2541;
    for (int i = 0; i < 2541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2541(int& val) {
    const int x = 2541;
    std::cout << 2541;
    for (int i = 0; i < 2541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2542(int& val) {
    func_in_2542(val);
    const int x = 2542;
    std::cout << 2542;
    for (int i = 0; i < 2542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2542(int& val) {
    const int x = 2542;
    std::cout << 2542;
    for (int i = 0; i < 2542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2543(int& val) {
    func_in_2543(val);
    const int x = 2543;
    std::cout << 2543;
    for (int i = 0; i < 2543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2543(int& val) {
    const int x = 2543;
    std::cout << 2543;
    for (int i = 0; i < 2543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2544(int& val) {
    func_in_2544(val);
    const int x = 2544;
    std::cout << 2544;
    for (int i = 0; i < 2544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2544(int& val) {
    const int x = 2544;
    std::cout << 2544;
    for (int i = 0; i < 2544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2545(int& val) {
    func_in_2545(val);
    const int x = 2545;
    std::cout << 2545;
    for (int i = 0; i < 2545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2545(int& val) {
    const int x = 2545;
    std::cout << 2545;
    for (int i = 0; i < 2545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2546(int& val) {
    func_in_2546(val);
    const int x = 2546;
    std::cout << 2546;
    for (int i = 0; i < 2546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2546(int& val) {
    const int x = 2546;
    std::cout << 2546;
    for (int i = 0; i < 2546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2547(int& val) {
    func_in_2547(val);
    const int x = 2547;
    std::cout << 2547;
    for (int i = 0; i < 2547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2547(int& val) {
    const int x = 2547;
    std::cout << 2547;
    for (int i = 0; i < 2547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2548(int& val) {
    func_in_2548(val);
    const int x = 2548;
    std::cout << 2548;
    for (int i = 0; i < 2548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2548(int& val) {
    const int x = 2548;
    std::cout << 2548;
    for (int i = 0; i < 2548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2549(int& val) {
    func_in_2549(val);
    const int x = 2549;
    std::cout << 2549;
    for (int i = 0; i < 2549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2549(int& val) {
    const int x = 2549;
    std::cout << 2549;
    for (int i = 0; i < 2549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2550(int& val) {
    func_in_2550(val);
    const int x = 2550;
    std::cout << 2550;
    for (int i = 0; i < 2550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2550(int& val) {
    const int x = 2550;
    std::cout << 2550;
    for (int i = 0; i < 2550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2551(int& val) {
    func_in_2551(val);
    const int x = 2551;
    std::cout << 2551;
    for (int i = 0; i < 2551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2551(int& val) {
    const int x = 2551;
    std::cout << 2551;
    for (int i = 0; i < 2551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2552(int& val) {
    func_in_2552(val);
    const int x = 2552;
    std::cout << 2552;
    for (int i = 0; i < 2552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2552(int& val) {
    const int x = 2552;
    std::cout << 2552;
    for (int i = 0; i < 2552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2553(int& val) {
    func_in_2553(val);
    const int x = 2553;
    std::cout << 2553;
    for (int i = 0; i < 2553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2553(int& val) {
    const int x = 2553;
    std::cout << 2553;
    for (int i = 0; i < 2553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2554(int& val) {
    func_in_2554(val);
    const int x = 2554;
    std::cout << 2554;
    for (int i = 0; i < 2554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2554(int& val) {
    const int x = 2554;
    std::cout << 2554;
    for (int i = 0; i < 2554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2555(int& val) {
    func_in_2555(val);
    const int x = 2555;
    std::cout << 2555;
    for (int i = 0; i < 2555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2555(int& val) {
    const int x = 2555;
    std::cout << 2555;
    for (int i = 0; i < 2555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2556(int& val) {
    func_in_2556(val);
    const int x = 2556;
    std::cout << 2556;
    for (int i = 0; i < 2556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2556(int& val) {
    const int x = 2556;
    std::cout << 2556;
    for (int i = 0; i < 2556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2557(int& val) {
    func_in_2557(val);
    const int x = 2557;
    std::cout << 2557;
    for (int i = 0; i < 2557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2557(int& val) {
    const int x = 2557;
    std::cout << 2557;
    for (int i = 0; i < 2557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2558(int& val) {
    func_in_2558(val);
    const int x = 2558;
    std::cout << 2558;
    for (int i = 0; i < 2558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2558(int& val) {
    const int x = 2558;
    std::cout << 2558;
    for (int i = 0; i < 2558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2559(int& val) {
    func_in_2559(val);
    const int x = 2559;
    std::cout << 2559;
    for (int i = 0; i < 2559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2559(int& val) {
    const int x = 2559;
    std::cout << 2559;
    for (int i = 0; i < 2559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2560(int& val) {
    func_in_2560(val);
    const int x = 2560;
    std::cout << 2560;
    for (int i = 0; i < 2560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2560(int& val) {
    const int x = 2560;
    std::cout << 2560;
    for (int i = 0; i < 2560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2561(int& val) {
    func_in_2561(val);
    const int x = 2561;
    std::cout << 2561;
    for (int i = 0; i < 2561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2561(int& val) {
    const int x = 2561;
    std::cout << 2561;
    for (int i = 0; i < 2561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2562(int& val) {
    func_in_2562(val);
    const int x = 2562;
    std::cout << 2562;
    for (int i = 0; i < 2562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2562(int& val) {
    const int x = 2562;
    std::cout << 2562;
    for (int i = 0; i < 2562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2563(int& val) {
    func_in_2563(val);
    const int x = 2563;
    std::cout << 2563;
    for (int i = 0; i < 2563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2563(int& val) {
    const int x = 2563;
    std::cout << 2563;
    for (int i = 0; i < 2563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2564(int& val) {
    func_in_2564(val);
    const int x = 2564;
    std::cout << 2564;
    for (int i = 0; i < 2564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2564(int& val) {
    const int x = 2564;
    std::cout << 2564;
    for (int i = 0; i < 2564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2565(int& val) {
    func_in_2565(val);
    const int x = 2565;
    std::cout << 2565;
    for (int i = 0; i < 2565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2565(int& val) {
    const int x = 2565;
    std::cout << 2565;
    for (int i = 0; i < 2565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2566(int& val) {
    func_in_2566(val);
    const int x = 2566;
    std::cout << 2566;
    for (int i = 0; i < 2566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2566(int& val) {
    const int x = 2566;
    std::cout << 2566;
    for (int i = 0; i < 2566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2567(int& val) {
    func_in_2567(val);
    const int x = 2567;
    std::cout << 2567;
    for (int i = 0; i < 2567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2567(int& val) {
    const int x = 2567;
    std::cout << 2567;
    for (int i = 0; i < 2567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2568(int& val) {
    func_in_2568(val);
    const int x = 2568;
    std::cout << 2568;
    for (int i = 0; i < 2568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2568(int& val) {
    const int x = 2568;
    std::cout << 2568;
    for (int i = 0; i < 2568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2569(int& val) {
    func_in_2569(val);
    const int x = 2569;
    std::cout << 2569;
    for (int i = 0; i < 2569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2569(int& val) {
    const int x = 2569;
    std::cout << 2569;
    for (int i = 0; i < 2569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2570(int& val) {
    func_in_2570(val);
    const int x = 2570;
    std::cout << 2570;
    for (int i = 0; i < 2570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2570(int& val) {
    const int x = 2570;
    std::cout << 2570;
    for (int i = 0; i < 2570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2571(int& val) {
    func_in_2571(val);
    const int x = 2571;
    std::cout << 2571;
    for (int i = 0; i < 2571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2571(int& val) {
    const int x = 2571;
    std::cout << 2571;
    for (int i = 0; i < 2571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2572(int& val) {
    func_in_2572(val);
    const int x = 2572;
    std::cout << 2572;
    for (int i = 0; i < 2572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2572(int& val) {
    const int x = 2572;
    std::cout << 2572;
    for (int i = 0; i < 2572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2573(int& val) {
    func_in_2573(val);
    const int x = 2573;
    std::cout << 2573;
    for (int i = 0; i < 2573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2573(int& val) {
    const int x = 2573;
    std::cout << 2573;
    for (int i = 0; i < 2573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2574(int& val) {
    func_in_2574(val);
    const int x = 2574;
    std::cout << 2574;
    for (int i = 0; i < 2574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2574(int& val) {
    const int x = 2574;
    std::cout << 2574;
    for (int i = 0; i < 2574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2575(int& val) {
    func_in_2575(val);
    const int x = 2575;
    std::cout << 2575;
    for (int i = 0; i < 2575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2575(int& val) {
    const int x = 2575;
    std::cout << 2575;
    for (int i = 0; i < 2575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2576(int& val) {
    func_in_2576(val);
    const int x = 2576;
    std::cout << 2576;
    for (int i = 0; i < 2576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2576(int& val) {
    const int x = 2576;
    std::cout << 2576;
    for (int i = 0; i < 2576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2577(int& val) {
    func_in_2577(val);
    const int x = 2577;
    std::cout << 2577;
    for (int i = 0; i < 2577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2577(int& val) {
    const int x = 2577;
    std::cout << 2577;
    for (int i = 0; i < 2577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2578(int& val) {
    func_in_2578(val);
    const int x = 2578;
    std::cout << 2578;
    for (int i = 0; i < 2578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2578(int& val) {
    const int x = 2578;
    std::cout << 2578;
    for (int i = 0; i < 2578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2579(int& val) {
    func_in_2579(val);
    const int x = 2579;
    std::cout << 2579;
    for (int i = 0; i < 2579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2579(int& val) {
    const int x = 2579;
    std::cout << 2579;
    for (int i = 0; i < 2579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2580(int& val) {
    func_in_2580(val);
    const int x = 2580;
    std::cout << 2580;
    for (int i = 0; i < 2580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2580(int& val) {
    const int x = 2580;
    std::cout << 2580;
    for (int i = 0; i < 2580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2581(int& val) {
    func_in_2581(val);
    const int x = 2581;
    std::cout << 2581;
    for (int i = 0; i < 2581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2581(int& val) {
    const int x = 2581;
    std::cout << 2581;
    for (int i = 0; i < 2581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2582(int& val) {
    func_in_2582(val);
    const int x = 2582;
    std::cout << 2582;
    for (int i = 0; i < 2582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2582(int& val) {
    const int x = 2582;
    std::cout << 2582;
    for (int i = 0; i < 2582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2583(int& val) {
    func_in_2583(val);
    const int x = 2583;
    std::cout << 2583;
    for (int i = 0; i < 2583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2583(int& val) {
    const int x = 2583;
    std::cout << 2583;
    for (int i = 0; i < 2583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2584(int& val) {
    func_in_2584(val);
    const int x = 2584;
    std::cout << 2584;
    for (int i = 0; i < 2584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2584(int& val) {
    const int x = 2584;
    std::cout << 2584;
    for (int i = 0; i < 2584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2585(int& val) {
    func_in_2585(val);
    const int x = 2585;
    std::cout << 2585;
    for (int i = 0; i < 2585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2585(int& val) {
    const int x = 2585;
    std::cout << 2585;
    for (int i = 0; i < 2585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2586(int& val) {
    func_in_2586(val);
    const int x = 2586;
    std::cout << 2586;
    for (int i = 0; i < 2586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2586(int& val) {
    const int x = 2586;
    std::cout << 2586;
    for (int i = 0; i < 2586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2587(int& val) {
    func_in_2587(val);
    const int x = 2587;
    std::cout << 2587;
    for (int i = 0; i < 2587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2587(int& val) {
    const int x = 2587;
    std::cout << 2587;
    for (int i = 0; i < 2587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2588(int& val) {
    func_in_2588(val);
    const int x = 2588;
    std::cout << 2588;
    for (int i = 0; i < 2588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2588(int& val) {
    const int x = 2588;
    std::cout << 2588;
    for (int i = 0; i < 2588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2589(int& val) {
    func_in_2589(val);
    const int x = 2589;
    std::cout << 2589;
    for (int i = 0; i < 2589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2589(int& val) {
    const int x = 2589;
    std::cout << 2589;
    for (int i = 0; i < 2589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2590(int& val) {
    func_in_2590(val);
    const int x = 2590;
    std::cout << 2590;
    for (int i = 0; i < 2590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2590(int& val) {
    const int x = 2590;
    std::cout << 2590;
    for (int i = 0; i < 2590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2591(int& val) {
    func_in_2591(val);
    const int x = 2591;
    std::cout << 2591;
    for (int i = 0; i < 2591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2591(int& val) {
    const int x = 2591;
    std::cout << 2591;
    for (int i = 0; i < 2591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2592(int& val) {
    func_in_2592(val);
    const int x = 2592;
    std::cout << 2592;
    for (int i = 0; i < 2592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2592(int& val) {
    const int x = 2592;
    std::cout << 2592;
    for (int i = 0; i < 2592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2593(int& val) {
    func_in_2593(val);
    const int x = 2593;
    std::cout << 2593;
    for (int i = 0; i < 2593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2593(int& val) {
    const int x = 2593;
    std::cout << 2593;
    for (int i = 0; i < 2593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2594(int& val) {
    func_in_2594(val);
    const int x = 2594;
    std::cout << 2594;
    for (int i = 0; i < 2594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2594(int& val) {
    const int x = 2594;
    std::cout << 2594;
    for (int i = 0; i < 2594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2595(int& val) {
    func_in_2595(val);
    const int x = 2595;
    std::cout << 2595;
    for (int i = 0; i < 2595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2595(int& val) {
    const int x = 2595;
    std::cout << 2595;
    for (int i = 0; i < 2595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2596(int& val) {
    func_in_2596(val);
    const int x = 2596;
    std::cout << 2596;
    for (int i = 0; i < 2596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2596(int& val) {
    const int x = 2596;
    std::cout << 2596;
    for (int i = 0; i < 2596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2597(int& val) {
    func_in_2597(val);
    const int x = 2597;
    std::cout << 2597;
    for (int i = 0; i < 2597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2597(int& val) {
    const int x = 2597;
    std::cout << 2597;
    for (int i = 0; i < 2597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2598(int& val) {
    func_in_2598(val);
    const int x = 2598;
    std::cout << 2598;
    for (int i = 0; i < 2598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2598(int& val) {
    const int x = 2598;
    std::cout << 2598;
    for (int i = 0; i < 2598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2599(int& val) {
    func_in_2599(val);
    const int x = 2599;
    std::cout << 2599;
    for (int i = 0; i < 2599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2599(int& val) {
    const int x = 2599;
    std::cout << 2599;
    for (int i = 0; i < 2599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2600(int& val) {
    func_in_2600(val);
    const int x = 2600;
    std::cout << 2600;
    for (int i = 0; i < 2600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2600(int& val) {
    const int x = 2600;
    std::cout << 2600;
    for (int i = 0; i < 2600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2601(int& val) {
    func_in_2601(val);
    const int x = 2601;
    std::cout << 2601;
    for (int i = 0; i < 2601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2601(int& val) {
    const int x = 2601;
    std::cout << 2601;
    for (int i = 0; i < 2601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2602(int& val) {
    func_in_2602(val);
    const int x = 2602;
    std::cout << 2602;
    for (int i = 0; i < 2602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2602(int& val) {
    const int x = 2602;
    std::cout << 2602;
    for (int i = 0; i < 2602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2603(int& val) {
    func_in_2603(val);
    const int x = 2603;
    std::cout << 2603;
    for (int i = 0; i < 2603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2603(int& val) {
    const int x = 2603;
    std::cout << 2603;
    for (int i = 0; i < 2603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2604(int& val) {
    func_in_2604(val);
    const int x = 2604;
    std::cout << 2604;
    for (int i = 0; i < 2604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2604(int& val) {
    const int x = 2604;
    std::cout << 2604;
    for (int i = 0; i < 2604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2605(int& val) {
    func_in_2605(val);
    const int x = 2605;
    std::cout << 2605;
    for (int i = 0; i < 2605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2605(int& val) {
    const int x = 2605;
    std::cout << 2605;
    for (int i = 0; i < 2605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2606(int& val) {
    func_in_2606(val);
    const int x = 2606;
    std::cout << 2606;
    for (int i = 0; i < 2606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2606(int& val) {
    const int x = 2606;
    std::cout << 2606;
    for (int i = 0; i < 2606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2607(int& val) {
    func_in_2607(val);
    const int x = 2607;
    std::cout << 2607;
    for (int i = 0; i < 2607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2607(int& val) {
    const int x = 2607;
    std::cout << 2607;
    for (int i = 0; i < 2607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2608(int& val) {
    func_in_2608(val);
    const int x = 2608;
    std::cout << 2608;
    for (int i = 0; i < 2608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2608(int& val) {
    const int x = 2608;
    std::cout << 2608;
    for (int i = 0; i < 2608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2609(int& val) {
    func_in_2609(val);
    const int x = 2609;
    std::cout << 2609;
    for (int i = 0; i < 2609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2609(int& val) {
    const int x = 2609;
    std::cout << 2609;
    for (int i = 0; i < 2609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2610(int& val) {
    func_in_2610(val);
    const int x = 2610;
    std::cout << 2610;
    for (int i = 0; i < 2610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2610(int& val) {
    const int x = 2610;
    std::cout << 2610;
    for (int i = 0; i < 2610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2611(int& val) {
    func_in_2611(val);
    const int x = 2611;
    std::cout << 2611;
    for (int i = 0; i < 2611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2611(int& val) {
    const int x = 2611;
    std::cout << 2611;
    for (int i = 0; i < 2611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2612(int& val) {
    func_in_2612(val);
    const int x = 2612;
    std::cout << 2612;
    for (int i = 0; i < 2612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2612(int& val) {
    const int x = 2612;
    std::cout << 2612;
    for (int i = 0; i < 2612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2613(int& val) {
    func_in_2613(val);
    const int x = 2613;
    std::cout << 2613;
    for (int i = 0; i < 2613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2613(int& val) {
    const int x = 2613;
    std::cout << 2613;
    for (int i = 0; i < 2613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2614(int& val) {
    func_in_2614(val);
    const int x = 2614;
    std::cout << 2614;
    for (int i = 0; i < 2614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2614(int& val) {
    const int x = 2614;
    std::cout << 2614;
    for (int i = 0; i < 2614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2615(int& val) {
    func_in_2615(val);
    const int x = 2615;
    std::cout << 2615;
    for (int i = 0; i < 2615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2615(int& val) {
    const int x = 2615;
    std::cout << 2615;
    for (int i = 0; i < 2615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2616(int& val) {
    func_in_2616(val);
    const int x = 2616;
    std::cout << 2616;
    for (int i = 0; i < 2616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2616(int& val) {
    const int x = 2616;
    std::cout << 2616;
    for (int i = 0; i < 2616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2617(int& val) {
    func_in_2617(val);
    const int x = 2617;
    std::cout << 2617;
    for (int i = 0; i < 2617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2617(int& val) {
    const int x = 2617;
    std::cout << 2617;
    for (int i = 0; i < 2617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2618(int& val) {
    func_in_2618(val);
    const int x = 2618;
    std::cout << 2618;
    for (int i = 0; i < 2618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2618(int& val) {
    const int x = 2618;
    std::cout << 2618;
    for (int i = 0; i < 2618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2619(int& val) {
    func_in_2619(val);
    const int x = 2619;
    std::cout << 2619;
    for (int i = 0; i < 2619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2619(int& val) {
    const int x = 2619;
    std::cout << 2619;
    for (int i = 0; i < 2619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2620(int& val) {
    func_in_2620(val);
    const int x = 2620;
    std::cout << 2620;
    for (int i = 0; i < 2620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2620(int& val) {
    const int x = 2620;
    std::cout << 2620;
    for (int i = 0; i < 2620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2621(int& val) {
    func_in_2621(val);
    const int x = 2621;
    std::cout << 2621;
    for (int i = 0; i < 2621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2621(int& val) {
    const int x = 2621;
    std::cout << 2621;
    for (int i = 0; i < 2621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2622(int& val) {
    func_in_2622(val);
    const int x = 2622;
    std::cout << 2622;
    for (int i = 0; i < 2622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2622(int& val) {
    const int x = 2622;
    std::cout << 2622;
    for (int i = 0; i < 2622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2623(int& val) {
    func_in_2623(val);
    const int x = 2623;
    std::cout << 2623;
    for (int i = 0; i < 2623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2623(int& val) {
    const int x = 2623;
    std::cout << 2623;
    for (int i = 0; i < 2623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2624(int& val) {
    func_in_2624(val);
    const int x = 2624;
    std::cout << 2624;
    for (int i = 0; i < 2624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2624(int& val) {
    const int x = 2624;
    std::cout << 2624;
    for (int i = 0; i < 2624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2625(int& val) {
    func_in_2625(val);
    const int x = 2625;
    std::cout << 2625;
    for (int i = 0; i < 2625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2625(int& val) {
    const int x = 2625;
    std::cout << 2625;
    for (int i = 0; i < 2625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2626(int& val) {
    func_in_2626(val);
    const int x = 2626;
    std::cout << 2626;
    for (int i = 0; i < 2626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2626(int& val) {
    const int x = 2626;
    std::cout << 2626;
    for (int i = 0; i < 2626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2627(int& val) {
    func_in_2627(val);
    const int x = 2627;
    std::cout << 2627;
    for (int i = 0; i < 2627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2627(int& val) {
    const int x = 2627;
    std::cout << 2627;
    for (int i = 0; i < 2627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2628(int& val) {
    func_in_2628(val);
    const int x = 2628;
    std::cout << 2628;
    for (int i = 0; i < 2628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2628(int& val) {
    const int x = 2628;
    std::cout << 2628;
    for (int i = 0; i < 2628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2629(int& val) {
    func_in_2629(val);
    const int x = 2629;
    std::cout << 2629;
    for (int i = 0; i < 2629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2629(int& val) {
    const int x = 2629;
    std::cout << 2629;
    for (int i = 0; i < 2629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2630(int& val) {
    func_in_2630(val);
    const int x = 2630;
    std::cout << 2630;
    for (int i = 0; i < 2630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2630(int& val) {
    const int x = 2630;
    std::cout << 2630;
    for (int i = 0; i < 2630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2631(int& val) {
    func_in_2631(val);
    const int x = 2631;
    std::cout << 2631;
    for (int i = 0; i < 2631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2631(int& val) {
    const int x = 2631;
    std::cout << 2631;
    for (int i = 0; i < 2631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2632(int& val) {
    func_in_2632(val);
    const int x = 2632;
    std::cout << 2632;
    for (int i = 0; i < 2632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2632(int& val) {
    const int x = 2632;
    std::cout << 2632;
    for (int i = 0; i < 2632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2633(int& val) {
    func_in_2633(val);
    const int x = 2633;
    std::cout << 2633;
    for (int i = 0; i < 2633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2633(int& val) {
    const int x = 2633;
    std::cout << 2633;
    for (int i = 0; i < 2633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2634(int& val) {
    func_in_2634(val);
    const int x = 2634;
    std::cout << 2634;
    for (int i = 0; i < 2634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2634(int& val) {
    const int x = 2634;
    std::cout << 2634;
    for (int i = 0; i < 2634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2635(int& val) {
    func_in_2635(val);
    const int x = 2635;
    std::cout << 2635;
    for (int i = 0; i < 2635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2635(int& val) {
    const int x = 2635;
    std::cout << 2635;
    for (int i = 0; i < 2635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2636(int& val) {
    func_in_2636(val);
    const int x = 2636;
    std::cout << 2636;
    for (int i = 0; i < 2636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2636(int& val) {
    const int x = 2636;
    std::cout << 2636;
    for (int i = 0; i < 2636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2637(int& val) {
    func_in_2637(val);
    const int x = 2637;
    std::cout << 2637;
    for (int i = 0; i < 2637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2637(int& val) {
    const int x = 2637;
    std::cout << 2637;
    for (int i = 0; i < 2637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2638(int& val) {
    func_in_2638(val);
    const int x = 2638;
    std::cout << 2638;
    for (int i = 0; i < 2638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2638(int& val) {
    const int x = 2638;
    std::cout << 2638;
    for (int i = 0; i < 2638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2639(int& val) {
    func_in_2639(val);
    const int x = 2639;
    std::cout << 2639;
    for (int i = 0; i < 2639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2639(int& val) {
    const int x = 2639;
    std::cout << 2639;
    for (int i = 0; i < 2639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2640(int& val) {
    func_in_2640(val);
    const int x = 2640;
    std::cout << 2640;
    for (int i = 0; i < 2640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2640(int& val) {
    const int x = 2640;
    std::cout << 2640;
    for (int i = 0; i < 2640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2641(int& val) {
    func_in_2641(val);
    const int x = 2641;
    std::cout << 2641;
    for (int i = 0; i < 2641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2641(int& val) {
    const int x = 2641;
    std::cout << 2641;
    for (int i = 0; i < 2641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2642(int& val) {
    func_in_2642(val);
    const int x = 2642;
    std::cout << 2642;
    for (int i = 0; i < 2642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2642(int& val) {
    const int x = 2642;
    std::cout << 2642;
    for (int i = 0; i < 2642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2643(int& val) {
    func_in_2643(val);
    const int x = 2643;
    std::cout << 2643;
    for (int i = 0; i < 2643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2643(int& val) {
    const int x = 2643;
    std::cout << 2643;
    for (int i = 0; i < 2643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2644(int& val) {
    func_in_2644(val);
    const int x = 2644;
    std::cout << 2644;
    for (int i = 0; i < 2644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2644(int& val) {
    const int x = 2644;
    std::cout << 2644;
    for (int i = 0; i < 2644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2645(int& val) {
    func_in_2645(val);
    const int x = 2645;
    std::cout << 2645;
    for (int i = 0; i < 2645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2645(int& val) {
    const int x = 2645;
    std::cout << 2645;
    for (int i = 0; i < 2645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2646(int& val) {
    func_in_2646(val);
    const int x = 2646;
    std::cout << 2646;
    for (int i = 0; i < 2646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2646(int& val) {
    const int x = 2646;
    std::cout << 2646;
    for (int i = 0; i < 2646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2647(int& val) {
    func_in_2647(val);
    const int x = 2647;
    std::cout << 2647;
    for (int i = 0; i < 2647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2647(int& val) {
    const int x = 2647;
    std::cout << 2647;
    for (int i = 0; i < 2647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2648(int& val) {
    func_in_2648(val);
    const int x = 2648;
    std::cout << 2648;
    for (int i = 0; i < 2648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2648(int& val) {
    const int x = 2648;
    std::cout << 2648;
    for (int i = 0; i < 2648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2649(int& val) {
    func_in_2649(val);
    const int x = 2649;
    std::cout << 2649;
    for (int i = 0; i < 2649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2649(int& val) {
    const int x = 2649;
    std::cout << 2649;
    for (int i = 0; i < 2649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2650(int& val) {
    func_in_2650(val);
    const int x = 2650;
    std::cout << 2650;
    for (int i = 0; i < 2650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2650(int& val) {
    const int x = 2650;
    std::cout << 2650;
    for (int i = 0; i < 2650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2651(int& val) {
    func_in_2651(val);
    const int x = 2651;
    std::cout << 2651;
    for (int i = 0; i < 2651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2651(int& val) {
    const int x = 2651;
    std::cout << 2651;
    for (int i = 0; i < 2651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2652(int& val) {
    func_in_2652(val);
    const int x = 2652;
    std::cout << 2652;
    for (int i = 0; i < 2652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2652(int& val) {
    const int x = 2652;
    std::cout << 2652;
    for (int i = 0; i < 2652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2653(int& val) {
    func_in_2653(val);
    const int x = 2653;
    std::cout << 2653;
    for (int i = 0; i < 2653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2653(int& val) {
    const int x = 2653;
    std::cout << 2653;
    for (int i = 0; i < 2653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2654(int& val) {
    func_in_2654(val);
    const int x = 2654;
    std::cout << 2654;
    for (int i = 0; i < 2654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2654(int& val) {
    const int x = 2654;
    std::cout << 2654;
    for (int i = 0; i < 2654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2655(int& val) {
    func_in_2655(val);
    const int x = 2655;
    std::cout << 2655;
    for (int i = 0; i < 2655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2655(int& val) {
    const int x = 2655;
    std::cout << 2655;
    for (int i = 0; i < 2655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2656(int& val) {
    func_in_2656(val);
    const int x = 2656;
    std::cout << 2656;
    for (int i = 0; i < 2656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2656(int& val) {
    const int x = 2656;
    std::cout << 2656;
    for (int i = 0; i < 2656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2657(int& val) {
    func_in_2657(val);
    const int x = 2657;
    std::cout << 2657;
    for (int i = 0; i < 2657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2657(int& val) {
    const int x = 2657;
    std::cout << 2657;
    for (int i = 0; i < 2657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2658(int& val) {
    func_in_2658(val);
    const int x = 2658;
    std::cout << 2658;
    for (int i = 0; i < 2658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2658(int& val) {
    const int x = 2658;
    std::cout << 2658;
    for (int i = 0; i < 2658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2659(int& val) {
    func_in_2659(val);
    const int x = 2659;
    std::cout << 2659;
    for (int i = 0; i < 2659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2659(int& val) {
    const int x = 2659;
    std::cout << 2659;
    for (int i = 0; i < 2659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2660(int& val) {
    func_in_2660(val);
    const int x = 2660;
    std::cout << 2660;
    for (int i = 0; i < 2660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2660(int& val) {
    const int x = 2660;
    std::cout << 2660;
    for (int i = 0; i < 2660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2661(int& val) {
    func_in_2661(val);
    const int x = 2661;
    std::cout << 2661;
    for (int i = 0; i < 2661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2661(int& val) {
    const int x = 2661;
    std::cout << 2661;
    for (int i = 0; i < 2661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2662(int& val) {
    func_in_2662(val);
    const int x = 2662;
    std::cout << 2662;
    for (int i = 0; i < 2662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2662(int& val) {
    const int x = 2662;
    std::cout << 2662;
    for (int i = 0; i < 2662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2663(int& val) {
    func_in_2663(val);
    const int x = 2663;
    std::cout << 2663;
    for (int i = 0; i < 2663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2663(int& val) {
    const int x = 2663;
    std::cout << 2663;
    for (int i = 0; i < 2663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2664(int& val) {
    func_in_2664(val);
    const int x = 2664;
    std::cout << 2664;
    for (int i = 0; i < 2664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2664(int& val) {
    const int x = 2664;
    std::cout << 2664;
    for (int i = 0; i < 2664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2665(int& val) {
    func_in_2665(val);
    const int x = 2665;
    std::cout << 2665;
    for (int i = 0; i < 2665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2665(int& val) {
    const int x = 2665;
    std::cout << 2665;
    for (int i = 0; i < 2665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2666(int& val) {
    func_in_2666(val);
    const int x = 2666;
    std::cout << 2666;
    for (int i = 0; i < 2666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2666(int& val) {
    const int x = 2666;
    std::cout << 2666;
    for (int i = 0; i < 2666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2667(int& val) {
    func_in_2667(val);
    const int x = 2667;
    std::cout << 2667;
    for (int i = 0; i < 2667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2667(int& val) {
    const int x = 2667;
    std::cout << 2667;
    for (int i = 0; i < 2667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2668(int& val) {
    func_in_2668(val);
    const int x = 2668;
    std::cout << 2668;
    for (int i = 0; i < 2668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2668(int& val) {
    const int x = 2668;
    std::cout << 2668;
    for (int i = 0; i < 2668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2669(int& val) {
    func_in_2669(val);
    const int x = 2669;
    std::cout << 2669;
    for (int i = 0; i < 2669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2669(int& val) {
    const int x = 2669;
    std::cout << 2669;
    for (int i = 0; i < 2669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2670(int& val) {
    func_in_2670(val);
    const int x = 2670;
    std::cout << 2670;
    for (int i = 0; i < 2670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2670(int& val) {
    const int x = 2670;
    std::cout << 2670;
    for (int i = 0; i < 2670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2671(int& val) {
    func_in_2671(val);
    const int x = 2671;
    std::cout << 2671;
    for (int i = 0; i < 2671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2671(int& val) {
    const int x = 2671;
    std::cout << 2671;
    for (int i = 0; i < 2671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2672(int& val) {
    func_in_2672(val);
    const int x = 2672;
    std::cout << 2672;
    for (int i = 0; i < 2672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2672(int& val) {
    const int x = 2672;
    std::cout << 2672;
    for (int i = 0; i < 2672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2673(int& val) {
    func_in_2673(val);
    const int x = 2673;
    std::cout << 2673;
    for (int i = 0; i < 2673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2673(int& val) {
    const int x = 2673;
    std::cout << 2673;
    for (int i = 0; i < 2673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2674(int& val) {
    func_in_2674(val);
    const int x = 2674;
    std::cout << 2674;
    for (int i = 0; i < 2674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2674(int& val) {
    const int x = 2674;
    std::cout << 2674;
    for (int i = 0; i < 2674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2675(int& val) {
    func_in_2675(val);
    const int x = 2675;
    std::cout << 2675;
    for (int i = 0; i < 2675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2675(int& val) {
    const int x = 2675;
    std::cout << 2675;
    for (int i = 0; i < 2675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2676(int& val) {
    func_in_2676(val);
    const int x = 2676;
    std::cout << 2676;
    for (int i = 0; i < 2676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2676(int& val) {
    const int x = 2676;
    std::cout << 2676;
    for (int i = 0; i < 2676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2677(int& val) {
    func_in_2677(val);
    const int x = 2677;
    std::cout << 2677;
    for (int i = 0; i < 2677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2677(int& val) {
    const int x = 2677;
    std::cout << 2677;
    for (int i = 0; i < 2677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2678(int& val) {
    func_in_2678(val);
    const int x = 2678;
    std::cout << 2678;
    for (int i = 0; i < 2678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2678(int& val) {
    const int x = 2678;
    std::cout << 2678;
    for (int i = 0; i < 2678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2679(int& val) {
    func_in_2679(val);
    const int x = 2679;
    std::cout << 2679;
    for (int i = 0; i < 2679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2679(int& val) {
    const int x = 2679;
    std::cout << 2679;
    for (int i = 0; i < 2679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2680(int& val) {
    func_in_2680(val);
    const int x = 2680;
    std::cout << 2680;
    for (int i = 0; i < 2680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2680(int& val) {
    const int x = 2680;
    std::cout << 2680;
    for (int i = 0; i < 2680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2681(int& val) {
    func_in_2681(val);
    const int x = 2681;
    std::cout << 2681;
    for (int i = 0; i < 2681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2681(int& val) {
    const int x = 2681;
    std::cout << 2681;
    for (int i = 0; i < 2681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2682(int& val) {
    func_in_2682(val);
    const int x = 2682;
    std::cout << 2682;
    for (int i = 0; i < 2682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2682(int& val) {
    const int x = 2682;
    std::cout << 2682;
    for (int i = 0; i < 2682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2683(int& val) {
    func_in_2683(val);
    const int x = 2683;
    std::cout << 2683;
    for (int i = 0; i < 2683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2683(int& val) {
    const int x = 2683;
    std::cout << 2683;
    for (int i = 0; i < 2683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2684(int& val) {
    func_in_2684(val);
    const int x = 2684;
    std::cout << 2684;
    for (int i = 0; i < 2684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2684(int& val) {
    const int x = 2684;
    std::cout << 2684;
    for (int i = 0; i < 2684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2685(int& val) {
    func_in_2685(val);
    const int x = 2685;
    std::cout << 2685;
    for (int i = 0; i < 2685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2685(int& val) {
    const int x = 2685;
    std::cout << 2685;
    for (int i = 0; i < 2685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2686(int& val) {
    func_in_2686(val);
    const int x = 2686;
    std::cout << 2686;
    for (int i = 0; i < 2686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2686(int& val) {
    const int x = 2686;
    std::cout << 2686;
    for (int i = 0; i < 2686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2687(int& val) {
    func_in_2687(val);
    const int x = 2687;
    std::cout << 2687;
    for (int i = 0; i < 2687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2687(int& val) {
    const int x = 2687;
    std::cout << 2687;
    for (int i = 0; i < 2687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2688(int& val) {
    func_in_2688(val);
    const int x = 2688;
    std::cout << 2688;
    for (int i = 0; i < 2688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2688(int& val) {
    const int x = 2688;
    std::cout << 2688;
    for (int i = 0; i < 2688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2689(int& val) {
    func_in_2689(val);
    const int x = 2689;
    std::cout << 2689;
    for (int i = 0; i < 2689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2689(int& val) {
    const int x = 2689;
    std::cout << 2689;
    for (int i = 0; i < 2689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2690(int& val) {
    func_in_2690(val);
    const int x = 2690;
    std::cout << 2690;
    for (int i = 0; i < 2690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2690(int& val) {
    const int x = 2690;
    std::cout << 2690;
    for (int i = 0; i < 2690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2691(int& val) {
    func_in_2691(val);
    const int x = 2691;
    std::cout << 2691;
    for (int i = 0; i < 2691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2691(int& val) {
    const int x = 2691;
    std::cout << 2691;
    for (int i = 0; i < 2691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2692(int& val) {
    func_in_2692(val);
    const int x = 2692;
    std::cout << 2692;
    for (int i = 0; i < 2692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2692(int& val) {
    const int x = 2692;
    std::cout << 2692;
    for (int i = 0; i < 2692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2693(int& val) {
    func_in_2693(val);
    const int x = 2693;
    std::cout << 2693;
    for (int i = 0; i < 2693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2693(int& val) {
    const int x = 2693;
    std::cout << 2693;
    for (int i = 0; i < 2693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2694(int& val) {
    func_in_2694(val);
    const int x = 2694;
    std::cout << 2694;
    for (int i = 0; i < 2694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2694(int& val) {
    const int x = 2694;
    std::cout << 2694;
    for (int i = 0; i < 2694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2695(int& val) {
    func_in_2695(val);
    const int x = 2695;
    std::cout << 2695;
    for (int i = 0; i < 2695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2695(int& val) {
    const int x = 2695;
    std::cout << 2695;
    for (int i = 0; i < 2695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2696(int& val) {
    func_in_2696(val);
    const int x = 2696;
    std::cout << 2696;
    for (int i = 0; i < 2696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2696(int& val) {
    const int x = 2696;
    std::cout << 2696;
    for (int i = 0; i < 2696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2697(int& val) {
    func_in_2697(val);
    const int x = 2697;
    std::cout << 2697;
    for (int i = 0; i < 2697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2697(int& val) {
    const int x = 2697;
    std::cout << 2697;
    for (int i = 0; i < 2697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2698(int& val) {
    func_in_2698(val);
    const int x = 2698;
    std::cout << 2698;
    for (int i = 0; i < 2698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2698(int& val) {
    const int x = 2698;
    std::cout << 2698;
    for (int i = 0; i < 2698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2699(int& val) {
    func_in_2699(val);
    const int x = 2699;
    std::cout << 2699;
    for (int i = 0; i < 2699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2699(int& val) {
    const int x = 2699;
    std::cout << 2699;
    for (int i = 0; i < 2699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2700(int& val) {
    func_in_2700(val);
    const int x = 2700;
    std::cout << 2700;
    for (int i = 0; i < 2700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2700(int& val) {
    const int x = 2700;
    std::cout << 2700;
    for (int i = 0; i < 2700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2701(int& val) {
    func_in_2701(val);
    const int x = 2701;
    std::cout << 2701;
    for (int i = 0; i < 2701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2701(int& val) {
    const int x = 2701;
    std::cout << 2701;
    for (int i = 0; i < 2701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2702(int& val) {
    func_in_2702(val);
    const int x = 2702;
    std::cout << 2702;
    for (int i = 0; i < 2702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2702(int& val) {
    const int x = 2702;
    std::cout << 2702;
    for (int i = 0; i < 2702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2703(int& val) {
    func_in_2703(val);
    const int x = 2703;
    std::cout << 2703;
    for (int i = 0; i < 2703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2703(int& val) {
    const int x = 2703;
    std::cout << 2703;
    for (int i = 0; i < 2703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2704(int& val) {
    func_in_2704(val);
    const int x = 2704;
    std::cout << 2704;
    for (int i = 0; i < 2704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2704(int& val) {
    const int x = 2704;
    std::cout << 2704;
    for (int i = 0; i < 2704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2705(int& val) {
    func_in_2705(val);
    const int x = 2705;
    std::cout << 2705;
    for (int i = 0; i < 2705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2705(int& val) {
    const int x = 2705;
    std::cout << 2705;
    for (int i = 0; i < 2705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2706(int& val) {
    func_in_2706(val);
    const int x = 2706;
    std::cout << 2706;
    for (int i = 0; i < 2706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2706(int& val) {
    const int x = 2706;
    std::cout << 2706;
    for (int i = 0; i < 2706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2707(int& val) {
    func_in_2707(val);
    const int x = 2707;
    std::cout << 2707;
    for (int i = 0; i < 2707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2707(int& val) {
    const int x = 2707;
    std::cout << 2707;
    for (int i = 0; i < 2707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2708(int& val) {
    func_in_2708(val);
    const int x = 2708;
    std::cout << 2708;
    for (int i = 0; i < 2708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2708(int& val) {
    const int x = 2708;
    std::cout << 2708;
    for (int i = 0; i < 2708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2709(int& val) {
    func_in_2709(val);
    const int x = 2709;
    std::cout << 2709;
    for (int i = 0; i < 2709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2709(int& val) {
    const int x = 2709;
    std::cout << 2709;
    for (int i = 0; i < 2709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2710(int& val) {
    func_in_2710(val);
    const int x = 2710;
    std::cout << 2710;
    for (int i = 0; i < 2710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2710(int& val) {
    const int x = 2710;
    std::cout << 2710;
    for (int i = 0; i < 2710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2711(int& val) {
    func_in_2711(val);
    const int x = 2711;
    std::cout << 2711;
    for (int i = 0; i < 2711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2711(int& val) {
    const int x = 2711;
    std::cout << 2711;
    for (int i = 0; i < 2711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2712(int& val) {
    func_in_2712(val);
    const int x = 2712;
    std::cout << 2712;
    for (int i = 0; i < 2712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2712(int& val) {
    const int x = 2712;
    std::cout << 2712;
    for (int i = 0; i < 2712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2713(int& val) {
    func_in_2713(val);
    const int x = 2713;
    std::cout << 2713;
    for (int i = 0; i < 2713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2713(int& val) {
    const int x = 2713;
    std::cout << 2713;
    for (int i = 0; i < 2713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2714(int& val) {
    func_in_2714(val);
    const int x = 2714;
    std::cout << 2714;
    for (int i = 0; i < 2714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2714(int& val) {
    const int x = 2714;
    std::cout << 2714;
    for (int i = 0; i < 2714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2715(int& val) {
    func_in_2715(val);
    const int x = 2715;
    std::cout << 2715;
    for (int i = 0; i < 2715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2715(int& val) {
    const int x = 2715;
    std::cout << 2715;
    for (int i = 0; i < 2715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2716(int& val) {
    func_in_2716(val);
    const int x = 2716;
    std::cout << 2716;
    for (int i = 0; i < 2716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2716(int& val) {
    const int x = 2716;
    std::cout << 2716;
    for (int i = 0; i < 2716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2717(int& val) {
    func_in_2717(val);
    const int x = 2717;
    std::cout << 2717;
    for (int i = 0; i < 2717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2717(int& val) {
    const int x = 2717;
    std::cout << 2717;
    for (int i = 0; i < 2717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2718(int& val) {
    func_in_2718(val);
    const int x = 2718;
    std::cout << 2718;
    for (int i = 0; i < 2718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2718(int& val) {
    const int x = 2718;
    std::cout << 2718;
    for (int i = 0; i < 2718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2719(int& val) {
    func_in_2719(val);
    const int x = 2719;
    std::cout << 2719;
    for (int i = 0; i < 2719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2719(int& val) {
    const int x = 2719;
    std::cout << 2719;
    for (int i = 0; i < 2719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2720(int& val) {
    func_in_2720(val);
    const int x = 2720;
    std::cout << 2720;
    for (int i = 0; i < 2720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2720(int& val) {
    const int x = 2720;
    std::cout << 2720;
    for (int i = 0; i < 2720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2721(int& val) {
    func_in_2721(val);
    const int x = 2721;
    std::cout << 2721;
    for (int i = 0; i < 2721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2721(int& val) {
    const int x = 2721;
    std::cout << 2721;
    for (int i = 0; i < 2721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2722(int& val) {
    func_in_2722(val);
    const int x = 2722;
    std::cout << 2722;
    for (int i = 0; i < 2722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2722(int& val) {
    const int x = 2722;
    std::cout << 2722;
    for (int i = 0; i < 2722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2723(int& val) {
    func_in_2723(val);
    const int x = 2723;
    std::cout << 2723;
    for (int i = 0; i < 2723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2723(int& val) {
    const int x = 2723;
    std::cout << 2723;
    for (int i = 0; i < 2723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2724(int& val) {
    func_in_2724(val);
    const int x = 2724;
    std::cout << 2724;
    for (int i = 0; i < 2724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2724(int& val) {
    const int x = 2724;
    std::cout << 2724;
    for (int i = 0; i < 2724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2725(int& val) {
    func_in_2725(val);
    const int x = 2725;
    std::cout << 2725;
    for (int i = 0; i < 2725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2725(int& val) {
    const int x = 2725;
    std::cout << 2725;
    for (int i = 0; i < 2725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2726(int& val) {
    func_in_2726(val);
    const int x = 2726;
    std::cout << 2726;
    for (int i = 0; i < 2726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2726(int& val) {
    const int x = 2726;
    std::cout << 2726;
    for (int i = 0; i < 2726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2727(int& val) {
    func_in_2727(val);
    const int x = 2727;
    std::cout << 2727;
    for (int i = 0; i < 2727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2727(int& val) {
    const int x = 2727;
    std::cout << 2727;
    for (int i = 0; i < 2727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2728(int& val) {
    func_in_2728(val);
    const int x = 2728;
    std::cout << 2728;
    for (int i = 0; i < 2728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2728(int& val) {
    const int x = 2728;
    std::cout << 2728;
    for (int i = 0; i < 2728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2729(int& val) {
    func_in_2729(val);
    const int x = 2729;
    std::cout << 2729;
    for (int i = 0; i < 2729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2729(int& val) {
    const int x = 2729;
    std::cout << 2729;
    for (int i = 0; i < 2729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2730(int& val) {
    func_in_2730(val);
    const int x = 2730;
    std::cout << 2730;
    for (int i = 0; i < 2730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2730(int& val) {
    const int x = 2730;
    std::cout << 2730;
    for (int i = 0; i < 2730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2731(int& val) {
    func_in_2731(val);
    const int x = 2731;
    std::cout << 2731;
    for (int i = 0; i < 2731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2731(int& val) {
    const int x = 2731;
    std::cout << 2731;
    for (int i = 0; i < 2731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2732(int& val) {
    func_in_2732(val);
    const int x = 2732;
    std::cout << 2732;
    for (int i = 0; i < 2732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2732(int& val) {
    const int x = 2732;
    std::cout << 2732;
    for (int i = 0; i < 2732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2733(int& val) {
    func_in_2733(val);
    const int x = 2733;
    std::cout << 2733;
    for (int i = 0; i < 2733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2733(int& val) {
    const int x = 2733;
    std::cout << 2733;
    for (int i = 0; i < 2733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2734(int& val) {
    func_in_2734(val);
    const int x = 2734;
    std::cout << 2734;
    for (int i = 0; i < 2734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2734(int& val) {
    const int x = 2734;
    std::cout << 2734;
    for (int i = 0; i < 2734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2735(int& val) {
    func_in_2735(val);
    const int x = 2735;
    std::cout << 2735;
    for (int i = 0; i < 2735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2735(int& val) {
    const int x = 2735;
    std::cout << 2735;
    for (int i = 0; i < 2735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2736(int& val) {
    func_in_2736(val);
    const int x = 2736;
    std::cout << 2736;
    for (int i = 0; i < 2736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2736(int& val) {
    const int x = 2736;
    std::cout << 2736;
    for (int i = 0; i < 2736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2737(int& val) {
    func_in_2737(val);
    const int x = 2737;
    std::cout << 2737;
    for (int i = 0; i < 2737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2737(int& val) {
    const int x = 2737;
    std::cout << 2737;
    for (int i = 0; i < 2737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2738(int& val) {
    func_in_2738(val);
    const int x = 2738;
    std::cout << 2738;
    for (int i = 0; i < 2738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2738(int& val) {
    const int x = 2738;
    std::cout << 2738;
    for (int i = 0; i < 2738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2739(int& val) {
    func_in_2739(val);
    const int x = 2739;
    std::cout << 2739;
    for (int i = 0; i < 2739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2739(int& val) {
    const int x = 2739;
    std::cout << 2739;
    for (int i = 0; i < 2739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2740(int& val) {
    func_in_2740(val);
    const int x = 2740;
    std::cout << 2740;
    for (int i = 0; i < 2740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2740(int& val) {
    const int x = 2740;
    std::cout << 2740;
    for (int i = 0; i < 2740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2741(int& val) {
    func_in_2741(val);
    const int x = 2741;
    std::cout << 2741;
    for (int i = 0; i < 2741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2741(int& val) {
    const int x = 2741;
    std::cout << 2741;
    for (int i = 0; i < 2741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2742(int& val) {
    func_in_2742(val);
    const int x = 2742;
    std::cout << 2742;
    for (int i = 0; i < 2742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2742(int& val) {
    const int x = 2742;
    std::cout << 2742;
    for (int i = 0; i < 2742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2743(int& val) {
    func_in_2743(val);
    const int x = 2743;
    std::cout << 2743;
    for (int i = 0; i < 2743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2743(int& val) {
    const int x = 2743;
    std::cout << 2743;
    for (int i = 0; i < 2743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2744(int& val) {
    func_in_2744(val);
    const int x = 2744;
    std::cout << 2744;
    for (int i = 0; i < 2744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2744(int& val) {
    const int x = 2744;
    std::cout << 2744;
    for (int i = 0; i < 2744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2745(int& val) {
    func_in_2745(val);
    const int x = 2745;
    std::cout << 2745;
    for (int i = 0; i < 2745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2745(int& val) {
    const int x = 2745;
    std::cout << 2745;
    for (int i = 0; i < 2745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2746(int& val) {
    func_in_2746(val);
    const int x = 2746;
    std::cout << 2746;
    for (int i = 0; i < 2746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2746(int& val) {
    const int x = 2746;
    std::cout << 2746;
    for (int i = 0; i < 2746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2747(int& val) {
    func_in_2747(val);
    const int x = 2747;
    std::cout << 2747;
    for (int i = 0; i < 2747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2747(int& val) {
    const int x = 2747;
    std::cout << 2747;
    for (int i = 0; i < 2747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2748(int& val) {
    func_in_2748(val);
    const int x = 2748;
    std::cout << 2748;
    for (int i = 0; i < 2748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2748(int& val) {
    const int x = 2748;
    std::cout << 2748;
    for (int i = 0; i < 2748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2749(int& val) {
    func_in_2749(val);
    const int x = 2749;
    std::cout << 2749;
    for (int i = 0; i < 2749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2749(int& val) {
    const int x = 2749;
    std::cout << 2749;
    for (int i = 0; i < 2749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2750(int& val) {
    func_in_2750(val);
    const int x = 2750;
    std::cout << 2750;
    for (int i = 0; i < 2750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2750(int& val) {
    const int x = 2750;
    std::cout << 2750;
    for (int i = 0; i < 2750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2751(int& val) {
    func_in_2751(val);
    const int x = 2751;
    std::cout << 2751;
    for (int i = 0; i < 2751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2751(int& val) {
    const int x = 2751;
    std::cout << 2751;
    for (int i = 0; i < 2751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2752(int& val) {
    func_in_2752(val);
    const int x = 2752;
    std::cout << 2752;
    for (int i = 0; i < 2752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2752(int& val) {
    const int x = 2752;
    std::cout << 2752;
    for (int i = 0; i < 2752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2753(int& val) {
    func_in_2753(val);
    const int x = 2753;
    std::cout << 2753;
    for (int i = 0; i < 2753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2753(int& val) {
    const int x = 2753;
    std::cout << 2753;
    for (int i = 0; i < 2753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2754(int& val) {
    func_in_2754(val);
    const int x = 2754;
    std::cout << 2754;
    for (int i = 0; i < 2754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2754(int& val) {
    const int x = 2754;
    std::cout << 2754;
    for (int i = 0; i < 2754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2755(int& val) {
    func_in_2755(val);
    const int x = 2755;
    std::cout << 2755;
    for (int i = 0; i < 2755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2755(int& val) {
    const int x = 2755;
    std::cout << 2755;
    for (int i = 0; i < 2755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2756(int& val) {
    func_in_2756(val);
    const int x = 2756;
    std::cout << 2756;
    for (int i = 0; i < 2756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2756(int& val) {
    const int x = 2756;
    std::cout << 2756;
    for (int i = 0; i < 2756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2757(int& val) {
    func_in_2757(val);
    const int x = 2757;
    std::cout << 2757;
    for (int i = 0; i < 2757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2757(int& val) {
    const int x = 2757;
    std::cout << 2757;
    for (int i = 0; i < 2757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2758(int& val) {
    func_in_2758(val);
    const int x = 2758;
    std::cout << 2758;
    for (int i = 0; i < 2758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2758(int& val) {
    const int x = 2758;
    std::cout << 2758;
    for (int i = 0; i < 2758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2759(int& val) {
    func_in_2759(val);
    const int x = 2759;
    std::cout << 2759;
    for (int i = 0; i < 2759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2759(int& val) {
    const int x = 2759;
    std::cout << 2759;
    for (int i = 0; i < 2759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2760(int& val) {
    func_in_2760(val);
    const int x = 2760;
    std::cout << 2760;
    for (int i = 0; i < 2760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2760(int& val) {
    const int x = 2760;
    std::cout << 2760;
    for (int i = 0; i < 2760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2761(int& val) {
    func_in_2761(val);
    const int x = 2761;
    std::cout << 2761;
    for (int i = 0; i < 2761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2761(int& val) {
    const int x = 2761;
    std::cout << 2761;
    for (int i = 0; i < 2761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2762(int& val) {
    func_in_2762(val);
    const int x = 2762;
    std::cout << 2762;
    for (int i = 0; i < 2762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2762(int& val) {
    const int x = 2762;
    std::cout << 2762;
    for (int i = 0; i < 2762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2763(int& val) {
    func_in_2763(val);
    const int x = 2763;
    std::cout << 2763;
    for (int i = 0; i < 2763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2763(int& val) {
    const int x = 2763;
    std::cout << 2763;
    for (int i = 0; i < 2763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2764(int& val) {
    func_in_2764(val);
    const int x = 2764;
    std::cout << 2764;
    for (int i = 0; i < 2764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2764(int& val) {
    const int x = 2764;
    std::cout << 2764;
    for (int i = 0; i < 2764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2765(int& val) {
    func_in_2765(val);
    const int x = 2765;
    std::cout << 2765;
    for (int i = 0; i < 2765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2765(int& val) {
    const int x = 2765;
    std::cout << 2765;
    for (int i = 0; i < 2765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2766(int& val) {
    func_in_2766(val);
    const int x = 2766;
    std::cout << 2766;
    for (int i = 0; i < 2766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2766(int& val) {
    const int x = 2766;
    std::cout << 2766;
    for (int i = 0; i < 2766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2767(int& val) {
    func_in_2767(val);
    const int x = 2767;
    std::cout << 2767;
    for (int i = 0; i < 2767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2767(int& val) {
    const int x = 2767;
    std::cout << 2767;
    for (int i = 0; i < 2767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2768(int& val) {
    func_in_2768(val);
    const int x = 2768;
    std::cout << 2768;
    for (int i = 0; i < 2768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2768(int& val) {
    const int x = 2768;
    std::cout << 2768;
    for (int i = 0; i < 2768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2769(int& val) {
    func_in_2769(val);
    const int x = 2769;
    std::cout << 2769;
    for (int i = 0; i < 2769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2769(int& val) {
    const int x = 2769;
    std::cout << 2769;
    for (int i = 0; i < 2769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2770(int& val) {
    func_in_2770(val);
    const int x = 2770;
    std::cout << 2770;
    for (int i = 0; i < 2770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2770(int& val) {
    const int x = 2770;
    std::cout << 2770;
    for (int i = 0; i < 2770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2771(int& val) {
    func_in_2771(val);
    const int x = 2771;
    std::cout << 2771;
    for (int i = 0; i < 2771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2771(int& val) {
    const int x = 2771;
    std::cout << 2771;
    for (int i = 0; i < 2771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2772(int& val) {
    func_in_2772(val);
    const int x = 2772;
    std::cout << 2772;
    for (int i = 0; i < 2772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2772(int& val) {
    const int x = 2772;
    std::cout << 2772;
    for (int i = 0; i < 2772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2773(int& val) {
    func_in_2773(val);
    const int x = 2773;
    std::cout << 2773;
    for (int i = 0; i < 2773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2773(int& val) {
    const int x = 2773;
    std::cout << 2773;
    for (int i = 0; i < 2773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2774(int& val) {
    func_in_2774(val);
    const int x = 2774;
    std::cout << 2774;
    for (int i = 0; i < 2774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2774(int& val) {
    const int x = 2774;
    std::cout << 2774;
    for (int i = 0; i < 2774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2775(int& val) {
    func_in_2775(val);
    const int x = 2775;
    std::cout << 2775;
    for (int i = 0; i < 2775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2775(int& val) {
    const int x = 2775;
    std::cout << 2775;
    for (int i = 0; i < 2775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2776(int& val) {
    func_in_2776(val);
    const int x = 2776;
    std::cout << 2776;
    for (int i = 0; i < 2776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2776(int& val) {
    const int x = 2776;
    std::cout << 2776;
    for (int i = 0; i < 2776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2777(int& val) {
    func_in_2777(val);
    const int x = 2777;
    std::cout << 2777;
    for (int i = 0; i < 2777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2777(int& val) {
    const int x = 2777;
    std::cout << 2777;
    for (int i = 0; i < 2777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2778(int& val) {
    func_in_2778(val);
    const int x = 2778;
    std::cout << 2778;
    for (int i = 0; i < 2778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2778(int& val) {
    const int x = 2778;
    std::cout << 2778;
    for (int i = 0; i < 2778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2779(int& val) {
    func_in_2779(val);
    const int x = 2779;
    std::cout << 2779;
    for (int i = 0; i < 2779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2779(int& val) {
    const int x = 2779;
    std::cout << 2779;
    for (int i = 0; i < 2779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2780(int& val) {
    func_in_2780(val);
    const int x = 2780;
    std::cout << 2780;
    for (int i = 0; i < 2780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2780(int& val) {
    const int x = 2780;
    std::cout << 2780;
    for (int i = 0; i < 2780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2781(int& val) {
    func_in_2781(val);
    const int x = 2781;
    std::cout << 2781;
    for (int i = 0; i < 2781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2781(int& val) {
    const int x = 2781;
    std::cout << 2781;
    for (int i = 0; i < 2781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2782(int& val) {
    func_in_2782(val);
    const int x = 2782;
    std::cout << 2782;
    for (int i = 0; i < 2782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2782(int& val) {
    const int x = 2782;
    std::cout << 2782;
    for (int i = 0; i < 2782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2783(int& val) {
    func_in_2783(val);
    const int x = 2783;
    std::cout << 2783;
    for (int i = 0; i < 2783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2783(int& val) {
    const int x = 2783;
    std::cout << 2783;
    for (int i = 0; i < 2783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2784(int& val) {
    func_in_2784(val);
    const int x = 2784;
    std::cout << 2784;
    for (int i = 0; i < 2784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2784(int& val) {
    const int x = 2784;
    std::cout << 2784;
    for (int i = 0; i < 2784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2785(int& val) {
    func_in_2785(val);
    const int x = 2785;
    std::cout << 2785;
    for (int i = 0; i < 2785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2785(int& val) {
    const int x = 2785;
    std::cout << 2785;
    for (int i = 0; i < 2785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2786(int& val) {
    func_in_2786(val);
    const int x = 2786;
    std::cout << 2786;
    for (int i = 0; i < 2786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2786(int& val) {
    const int x = 2786;
    std::cout << 2786;
    for (int i = 0; i < 2786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2787(int& val) {
    func_in_2787(val);
    const int x = 2787;
    std::cout << 2787;
    for (int i = 0; i < 2787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2787(int& val) {
    const int x = 2787;
    std::cout << 2787;
    for (int i = 0; i < 2787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2788(int& val) {
    func_in_2788(val);
    const int x = 2788;
    std::cout << 2788;
    for (int i = 0; i < 2788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2788(int& val) {
    const int x = 2788;
    std::cout << 2788;
    for (int i = 0; i < 2788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2789(int& val) {
    func_in_2789(val);
    const int x = 2789;
    std::cout << 2789;
    for (int i = 0; i < 2789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2789(int& val) {
    const int x = 2789;
    std::cout << 2789;
    for (int i = 0; i < 2789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2790(int& val) {
    func_in_2790(val);
    const int x = 2790;
    std::cout << 2790;
    for (int i = 0; i < 2790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2790(int& val) {
    const int x = 2790;
    std::cout << 2790;
    for (int i = 0; i < 2790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2791(int& val) {
    func_in_2791(val);
    const int x = 2791;
    std::cout << 2791;
    for (int i = 0; i < 2791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2791(int& val) {
    const int x = 2791;
    std::cout << 2791;
    for (int i = 0; i < 2791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2792(int& val) {
    func_in_2792(val);
    const int x = 2792;
    std::cout << 2792;
    for (int i = 0; i < 2792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2792(int& val) {
    const int x = 2792;
    std::cout << 2792;
    for (int i = 0; i < 2792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2793(int& val) {
    func_in_2793(val);
    const int x = 2793;
    std::cout << 2793;
    for (int i = 0; i < 2793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2793(int& val) {
    const int x = 2793;
    std::cout << 2793;
    for (int i = 0; i < 2793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2794(int& val) {
    func_in_2794(val);
    const int x = 2794;
    std::cout << 2794;
    for (int i = 0; i < 2794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2794(int& val) {
    const int x = 2794;
    std::cout << 2794;
    for (int i = 0; i < 2794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2795(int& val) {
    func_in_2795(val);
    const int x = 2795;
    std::cout << 2795;
    for (int i = 0; i < 2795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2795(int& val) {
    const int x = 2795;
    std::cout << 2795;
    for (int i = 0; i < 2795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2796(int& val) {
    func_in_2796(val);
    const int x = 2796;
    std::cout << 2796;
    for (int i = 0; i < 2796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2796(int& val) {
    const int x = 2796;
    std::cout << 2796;
    for (int i = 0; i < 2796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2797(int& val) {
    func_in_2797(val);
    const int x = 2797;
    std::cout << 2797;
    for (int i = 0; i < 2797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2797(int& val) {
    const int x = 2797;
    std::cout << 2797;
    for (int i = 0; i < 2797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2798(int& val) {
    func_in_2798(val);
    const int x = 2798;
    std::cout << 2798;
    for (int i = 0; i < 2798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2798(int& val) {
    const int x = 2798;
    std::cout << 2798;
    for (int i = 0; i < 2798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2799(int& val) {
    func_in_2799(val);
    const int x = 2799;
    std::cout << 2799;
    for (int i = 0; i < 2799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2799(int& val) {
    const int x = 2799;
    std::cout << 2799;
    for (int i = 0; i < 2799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2800(int& val) {
    func_in_2800(val);
    const int x = 2800;
    std::cout << 2800;
    for (int i = 0; i < 2800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2800(int& val) {
    const int x = 2800;
    std::cout << 2800;
    for (int i = 0; i < 2800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2801(int& val) {
    func_in_2801(val);
    const int x = 2801;
    std::cout << 2801;
    for (int i = 0; i < 2801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2801(int& val) {
    const int x = 2801;
    std::cout << 2801;
    for (int i = 0; i < 2801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2802(int& val) {
    func_in_2802(val);
    const int x = 2802;
    std::cout << 2802;
    for (int i = 0; i < 2802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2802(int& val) {
    const int x = 2802;
    std::cout << 2802;
    for (int i = 0; i < 2802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2803(int& val) {
    func_in_2803(val);
    const int x = 2803;
    std::cout << 2803;
    for (int i = 0; i < 2803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2803(int& val) {
    const int x = 2803;
    std::cout << 2803;
    for (int i = 0; i < 2803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2804(int& val) {
    func_in_2804(val);
    const int x = 2804;
    std::cout << 2804;
    for (int i = 0; i < 2804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2804(int& val) {
    const int x = 2804;
    std::cout << 2804;
    for (int i = 0; i < 2804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2805(int& val) {
    func_in_2805(val);
    const int x = 2805;
    std::cout << 2805;
    for (int i = 0; i < 2805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2805(int& val) {
    const int x = 2805;
    std::cout << 2805;
    for (int i = 0; i < 2805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2806(int& val) {
    func_in_2806(val);
    const int x = 2806;
    std::cout << 2806;
    for (int i = 0; i < 2806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2806(int& val) {
    const int x = 2806;
    std::cout << 2806;
    for (int i = 0; i < 2806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2807(int& val) {
    func_in_2807(val);
    const int x = 2807;
    std::cout << 2807;
    for (int i = 0; i < 2807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2807(int& val) {
    const int x = 2807;
    std::cout << 2807;
    for (int i = 0; i < 2807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2808(int& val) {
    func_in_2808(val);
    const int x = 2808;
    std::cout << 2808;
    for (int i = 0; i < 2808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2808(int& val) {
    const int x = 2808;
    std::cout << 2808;
    for (int i = 0; i < 2808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2809(int& val) {
    func_in_2809(val);
    const int x = 2809;
    std::cout << 2809;
    for (int i = 0; i < 2809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2809(int& val) {
    const int x = 2809;
    std::cout << 2809;
    for (int i = 0; i < 2809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2810(int& val) {
    func_in_2810(val);
    const int x = 2810;
    std::cout << 2810;
    for (int i = 0; i < 2810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2810(int& val) {
    const int x = 2810;
    std::cout << 2810;
    for (int i = 0; i < 2810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2811(int& val) {
    func_in_2811(val);
    const int x = 2811;
    std::cout << 2811;
    for (int i = 0; i < 2811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2811(int& val) {
    const int x = 2811;
    std::cout << 2811;
    for (int i = 0; i < 2811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2812(int& val) {
    func_in_2812(val);
    const int x = 2812;
    std::cout << 2812;
    for (int i = 0; i < 2812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2812(int& val) {
    const int x = 2812;
    std::cout << 2812;
    for (int i = 0; i < 2812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2813(int& val) {
    func_in_2813(val);
    const int x = 2813;
    std::cout << 2813;
    for (int i = 0; i < 2813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2813(int& val) {
    const int x = 2813;
    std::cout << 2813;
    for (int i = 0; i < 2813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2814(int& val) {
    func_in_2814(val);
    const int x = 2814;
    std::cout << 2814;
    for (int i = 0; i < 2814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2814(int& val) {
    const int x = 2814;
    std::cout << 2814;
    for (int i = 0; i < 2814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2815(int& val) {
    func_in_2815(val);
    const int x = 2815;
    std::cout << 2815;
    for (int i = 0; i < 2815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2815(int& val) {
    const int x = 2815;
    std::cout << 2815;
    for (int i = 0; i < 2815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2816(int& val) {
    func_in_2816(val);
    const int x = 2816;
    std::cout << 2816;
    for (int i = 0; i < 2816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2816(int& val) {
    const int x = 2816;
    std::cout << 2816;
    for (int i = 0; i < 2816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2817(int& val) {
    func_in_2817(val);
    const int x = 2817;
    std::cout << 2817;
    for (int i = 0; i < 2817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2817(int& val) {
    const int x = 2817;
    std::cout << 2817;
    for (int i = 0; i < 2817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2818(int& val) {
    func_in_2818(val);
    const int x = 2818;
    std::cout << 2818;
    for (int i = 0; i < 2818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2818(int& val) {
    const int x = 2818;
    std::cout << 2818;
    for (int i = 0; i < 2818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2819(int& val) {
    func_in_2819(val);
    const int x = 2819;
    std::cout << 2819;
    for (int i = 0; i < 2819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2819(int& val) {
    const int x = 2819;
    std::cout << 2819;
    for (int i = 0; i < 2819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2820(int& val) {
    func_in_2820(val);
    const int x = 2820;
    std::cout << 2820;
    for (int i = 0; i < 2820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2820(int& val) {
    const int x = 2820;
    std::cout << 2820;
    for (int i = 0; i < 2820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2821(int& val) {
    func_in_2821(val);
    const int x = 2821;
    std::cout << 2821;
    for (int i = 0; i < 2821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2821(int& val) {
    const int x = 2821;
    std::cout << 2821;
    for (int i = 0; i < 2821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2822(int& val) {
    func_in_2822(val);
    const int x = 2822;
    std::cout << 2822;
    for (int i = 0; i < 2822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2822(int& val) {
    const int x = 2822;
    std::cout << 2822;
    for (int i = 0; i < 2822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2823(int& val) {
    func_in_2823(val);
    const int x = 2823;
    std::cout << 2823;
    for (int i = 0; i < 2823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2823(int& val) {
    const int x = 2823;
    std::cout << 2823;
    for (int i = 0; i < 2823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2824(int& val) {
    func_in_2824(val);
    const int x = 2824;
    std::cout << 2824;
    for (int i = 0; i < 2824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2824(int& val) {
    const int x = 2824;
    std::cout << 2824;
    for (int i = 0; i < 2824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2825(int& val) {
    func_in_2825(val);
    const int x = 2825;
    std::cout << 2825;
    for (int i = 0; i < 2825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2825(int& val) {
    const int x = 2825;
    std::cout << 2825;
    for (int i = 0; i < 2825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2826(int& val) {
    func_in_2826(val);
    const int x = 2826;
    std::cout << 2826;
    for (int i = 0; i < 2826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2826(int& val) {
    const int x = 2826;
    std::cout << 2826;
    for (int i = 0; i < 2826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2827(int& val) {
    func_in_2827(val);
    const int x = 2827;
    std::cout << 2827;
    for (int i = 0; i < 2827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2827(int& val) {
    const int x = 2827;
    std::cout << 2827;
    for (int i = 0; i < 2827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2828(int& val) {
    func_in_2828(val);
    const int x = 2828;
    std::cout << 2828;
    for (int i = 0; i < 2828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2828(int& val) {
    const int x = 2828;
    std::cout << 2828;
    for (int i = 0; i < 2828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2829(int& val) {
    func_in_2829(val);
    const int x = 2829;
    std::cout << 2829;
    for (int i = 0; i < 2829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2829(int& val) {
    const int x = 2829;
    std::cout << 2829;
    for (int i = 0; i < 2829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2830(int& val) {
    func_in_2830(val);
    const int x = 2830;
    std::cout << 2830;
    for (int i = 0; i < 2830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2830(int& val) {
    const int x = 2830;
    std::cout << 2830;
    for (int i = 0; i < 2830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2831(int& val) {
    func_in_2831(val);
    const int x = 2831;
    std::cout << 2831;
    for (int i = 0; i < 2831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2831(int& val) {
    const int x = 2831;
    std::cout << 2831;
    for (int i = 0; i < 2831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2832(int& val) {
    func_in_2832(val);
    const int x = 2832;
    std::cout << 2832;
    for (int i = 0; i < 2832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2832(int& val) {
    const int x = 2832;
    std::cout << 2832;
    for (int i = 0; i < 2832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2833(int& val) {
    func_in_2833(val);
    const int x = 2833;
    std::cout << 2833;
    for (int i = 0; i < 2833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2833(int& val) {
    const int x = 2833;
    std::cout << 2833;
    for (int i = 0; i < 2833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2834(int& val) {
    func_in_2834(val);
    const int x = 2834;
    std::cout << 2834;
    for (int i = 0; i < 2834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2834(int& val) {
    const int x = 2834;
    std::cout << 2834;
    for (int i = 0; i < 2834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2835(int& val) {
    func_in_2835(val);
    const int x = 2835;
    std::cout << 2835;
    for (int i = 0; i < 2835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2835(int& val) {
    const int x = 2835;
    std::cout << 2835;
    for (int i = 0; i < 2835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2836(int& val) {
    func_in_2836(val);
    const int x = 2836;
    std::cout << 2836;
    for (int i = 0; i < 2836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2836(int& val) {
    const int x = 2836;
    std::cout << 2836;
    for (int i = 0; i < 2836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2837(int& val) {
    func_in_2837(val);
    const int x = 2837;
    std::cout << 2837;
    for (int i = 0; i < 2837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2837(int& val) {
    const int x = 2837;
    std::cout << 2837;
    for (int i = 0; i < 2837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2838(int& val) {
    func_in_2838(val);
    const int x = 2838;
    std::cout << 2838;
    for (int i = 0; i < 2838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2838(int& val) {
    const int x = 2838;
    std::cout << 2838;
    for (int i = 0; i < 2838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2839(int& val) {
    func_in_2839(val);
    const int x = 2839;
    std::cout << 2839;
    for (int i = 0; i < 2839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2839(int& val) {
    const int x = 2839;
    std::cout << 2839;
    for (int i = 0; i < 2839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2840(int& val) {
    func_in_2840(val);
    const int x = 2840;
    std::cout << 2840;
    for (int i = 0; i < 2840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2840(int& val) {
    const int x = 2840;
    std::cout << 2840;
    for (int i = 0; i < 2840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2841(int& val) {
    func_in_2841(val);
    const int x = 2841;
    std::cout << 2841;
    for (int i = 0; i < 2841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2841(int& val) {
    const int x = 2841;
    std::cout << 2841;
    for (int i = 0; i < 2841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2842(int& val) {
    func_in_2842(val);
    const int x = 2842;
    std::cout << 2842;
    for (int i = 0; i < 2842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2842(int& val) {
    const int x = 2842;
    std::cout << 2842;
    for (int i = 0; i < 2842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2843(int& val) {
    func_in_2843(val);
    const int x = 2843;
    std::cout << 2843;
    for (int i = 0; i < 2843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2843(int& val) {
    const int x = 2843;
    std::cout << 2843;
    for (int i = 0; i < 2843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2844(int& val) {
    func_in_2844(val);
    const int x = 2844;
    std::cout << 2844;
    for (int i = 0; i < 2844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2844(int& val) {
    const int x = 2844;
    std::cout << 2844;
    for (int i = 0; i < 2844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2845(int& val) {
    func_in_2845(val);
    const int x = 2845;
    std::cout << 2845;
    for (int i = 0; i < 2845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2845(int& val) {
    const int x = 2845;
    std::cout << 2845;
    for (int i = 0; i < 2845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2846(int& val) {
    func_in_2846(val);
    const int x = 2846;
    std::cout << 2846;
    for (int i = 0; i < 2846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2846(int& val) {
    const int x = 2846;
    std::cout << 2846;
    for (int i = 0; i < 2846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2847(int& val) {
    func_in_2847(val);
    const int x = 2847;
    std::cout << 2847;
    for (int i = 0; i < 2847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2847(int& val) {
    const int x = 2847;
    std::cout << 2847;
    for (int i = 0; i < 2847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2848(int& val) {
    func_in_2848(val);
    const int x = 2848;
    std::cout << 2848;
    for (int i = 0; i < 2848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2848(int& val) {
    const int x = 2848;
    std::cout << 2848;
    for (int i = 0; i < 2848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2849(int& val) {
    func_in_2849(val);
    const int x = 2849;
    std::cout << 2849;
    for (int i = 0; i < 2849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2849(int& val) {
    const int x = 2849;
    std::cout << 2849;
    for (int i = 0; i < 2849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2850(int& val) {
    func_in_2850(val);
    const int x = 2850;
    std::cout << 2850;
    for (int i = 0; i < 2850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2850(int& val) {
    const int x = 2850;
    std::cout << 2850;
    for (int i = 0; i < 2850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2851(int& val) {
    func_in_2851(val);
    const int x = 2851;
    std::cout << 2851;
    for (int i = 0; i < 2851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2851(int& val) {
    const int x = 2851;
    std::cout << 2851;
    for (int i = 0; i < 2851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2852(int& val) {
    func_in_2852(val);
    const int x = 2852;
    std::cout << 2852;
    for (int i = 0; i < 2852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2852(int& val) {
    const int x = 2852;
    std::cout << 2852;
    for (int i = 0; i < 2852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2853(int& val) {
    func_in_2853(val);
    const int x = 2853;
    std::cout << 2853;
    for (int i = 0; i < 2853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2853(int& val) {
    const int x = 2853;
    std::cout << 2853;
    for (int i = 0; i < 2853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2854(int& val) {
    func_in_2854(val);
    const int x = 2854;
    std::cout << 2854;
    for (int i = 0; i < 2854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2854(int& val) {
    const int x = 2854;
    std::cout << 2854;
    for (int i = 0; i < 2854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2855(int& val) {
    func_in_2855(val);
    const int x = 2855;
    std::cout << 2855;
    for (int i = 0; i < 2855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2855(int& val) {
    const int x = 2855;
    std::cout << 2855;
    for (int i = 0; i < 2855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2856(int& val) {
    func_in_2856(val);
    const int x = 2856;
    std::cout << 2856;
    for (int i = 0; i < 2856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2856(int& val) {
    const int x = 2856;
    std::cout << 2856;
    for (int i = 0; i < 2856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2857(int& val) {
    func_in_2857(val);
    const int x = 2857;
    std::cout << 2857;
    for (int i = 0; i < 2857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2857(int& val) {
    const int x = 2857;
    std::cout << 2857;
    for (int i = 0; i < 2857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2858(int& val) {
    func_in_2858(val);
    const int x = 2858;
    std::cout << 2858;
    for (int i = 0; i < 2858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2858(int& val) {
    const int x = 2858;
    std::cout << 2858;
    for (int i = 0; i < 2858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2859(int& val) {
    func_in_2859(val);
    const int x = 2859;
    std::cout << 2859;
    for (int i = 0; i < 2859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2859(int& val) {
    const int x = 2859;
    std::cout << 2859;
    for (int i = 0; i < 2859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2860(int& val) {
    func_in_2860(val);
    const int x = 2860;
    std::cout << 2860;
    for (int i = 0; i < 2860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2860(int& val) {
    const int x = 2860;
    std::cout << 2860;
    for (int i = 0; i < 2860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2861(int& val) {
    func_in_2861(val);
    const int x = 2861;
    std::cout << 2861;
    for (int i = 0; i < 2861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2861(int& val) {
    const int x = 2861;
    std::cout << 2861;
    for (int i = 0; i < 2861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2862(int& val) {
    func_in_2862(val);
    const int x = 2862;
    std::cout << 2862;
    for (int i = 0; i < 2862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2862(int& val) {
    const int x = 2862;
    std::cout << 2862;
    for (int i = 0; i < 2862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2863(int& val) {
    func_in_2863(val);
    const int x = 2863;
    std::cout << 2863;
    for (int i = 0; i < 2863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2863(int& val) {
    const int x = 2863;
    std::cout << 2863;
    for (int i = 0; i < 2863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2864(int& val) {
    func_in_2864(val);
    const int x = 2864;
    std::cout << 2864;
    for (int i = 0; i < 2864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2864(int& val) {
    const int x = 2864;
    std::cout << 2864;
    for (int i = 0; i < 2864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2865(int& val) {
    func_in_2865(val);
    const int x = 2865;
    std::cout << 2865;
    for (int i = 0; i < 2865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2865(int& val) {
    const int x = 2865;
    std::cout << 2865;
    for (int i = 0; i < 2865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2866(int& val) {
    func_in_2866(val);
    const int x = 2866;
    std::cout << 2866;
    for (int i = 0; i < 2866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2866(int& val) {
    const int x = 2866;
    std::cout << 2866;
    for (int i = 0; i < 2866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2867(int& val) {
    func_in_2867(val);
    const int x = 2867;
    std::cout << 2867;
    for (int i = 0; i < 2867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2867(int& val) {
    const int x = 2867;
    std::cout << 2867;
    for (int i = 0; i < 2867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2868(int& val) {
    func_in_2868(val);
    const int x = 2868;
    std::cout << 2868;
    for (int i = 0; i < 2868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2868(int& val) {
    const int x = 2868;
    std::cout << 2868;
    for (int i = 0; i < 2868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2869(int& val) {
    func_in_2869(val);
    const int x = 2869;
    std::cout << 2869;
    for (int i = 0; i < 2869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2869(int& val) {
    const int x = 2869;
    std::cout << 2869;
    for (int i = 0; i < 2869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2870(int& val) {
    func_in_2870(val);
    const int x = 2870;
    std::cout << 2870;
    for (int i = 0; i < 2870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2870(int& val) {
    const int x = 2870;
    std::cout << 2870;
    for (int i = 0; i < 2870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2871(int& val) {
    func_in_2871(val);
    const int x = 2871;
    std::cout << 2871;
    for (int i = 0; i < 2871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2871(int& val) {
    const int x = 2871;
    std::cout << 2871;
    for (int i = 0; i < 2871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2872(int& val) {
    func_in_2872(val);
    const int x = 2872;
    std::cout << 2872;
    for (int i = 0; i < 2872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2872(int& val) {
    const int x = 2872;
    std::cout << 2872;
    for (int i = 0; i < 2872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2873(int& val) {
    func_in_2873(val);
    const int x = 2873;
    std::cout << 2873;
    for (int i = 0; i < 2873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2873(int& val) {
    const int x = 2873;
    std::cout << 2873;
    for (int i = 0; i < 2873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2874(int& val) {
    func_in_2874(val);
    const int x = 2874;
    std::cout << 2874;
    for (int i = 0; i < 2874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2874(int& val) {
    const int x = 2874;
    std::cout << 2874;
    for (int i = 0; i < 2874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2875(int& val) {
    func_in_2875(val);
    const int x = 2875;
    std::cout << 2875;
    for (int i = 0; i < 2875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2875(int& val) {
    const int x = 2875;
    std::cout << 2875;
    for (int i = 0; i < 2875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2876(int& val) {
    func_in_2876(val);
    const int x = 2876;
    std::cout << 2876;
    for (int i = 0; i < 2876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2876(int& val) {
    const int x = 2876;
    std::cout << 2876;
    for (int i = 0; i < 2876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2877(int& val) {
    func_in_2877(val);
    const int x = 2877;
    std::cout << 2877;
    for (int i = 0; i < 2877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2877(int& val) {
    const int x = 2877;
    std::cout << 2877;
    for (int i = 0; i < 2877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2878(int& val) {
    func_in_2878(val);
    const int x = 2878;
    std::cout << 2878;
    for (int i = 0; i < 2878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2878(int& val) {
    const int x = 2878;
    std::cout << 2878;
    for (int i = 0; i < 2878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2879(int& val) {
    func_in_2879(val);
    const int x = 2879;
    std::cout << 2879;
    for (int i = 0; i < 2879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2879(int& val) {
    const int x = 2879;
    std::cout << 2879;
    for (int i = 0; i < 2879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2880(int& val) {
    func_in_2880(val);
    const int x = 2880;
    std::cout << 2880;
    for (int i = 0; i < 2880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2880(int& val) {
    const int x = 2880;
    std::cout << 2880;
    for (int i = 0; i < 2880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2881(int& val) {
    func_in_2881(val);
    const int x = 2881;
    std::cout << 2881;
    for (int i = 0; i < 2881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2881(int& val) {
    const int x = 2881;
    std::cout << 2881;
    for (int i = 0; i < 2881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2882(int& val) {
    func_in_2882(val);
    const int x = 2882;
    std::cout << 2882;
    for (int i = 0; i < 2882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2882(int& val) {
    const int x = 2882;
    std::cout << 2882;
    for (int i = 0; i < 2882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2883(int& val) {
    func_in_2883(val);
    const int x = 2883;
    std::cout << 2883;
    for (int i = 0; i < 2883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2883(int& val) {
    const int x = 2883;
    std::cout << 2883;
    for (int i = 0; i < 2883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2884(int& val) {
    func_in_2884(val);
    const int x = 2884;
    std::cout << 2884;
    for (int i = 0; i < 2884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2884(int& val) {
    const int x = 2884;
    std::cout << 2884;
    for (int i = 0; i < 2884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2885(int& val) {
    func_in_2885(val);
    const int x = 2885;
    std::cout << 2885;
    for (int i = 0; i < 2885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2885(int& val) {
    const int x = 2885;
    std::cout << 2885;
    for (int i = 0; i < 2885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2886(int& val) {
    func_in_2886(val);
    const int x = 2886;
    std::cout << 2886;
    for (int i = 0; i < 2886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2886(int& val) {
    const int x = 2886;
    std::cout << 2886;
    for (int i = 0; i < 2886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2887(int& val) {
    func_in_2887(val);
    const int x = 2887;
    std::cout << 2887;
    for (int i = 0; i < 2887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2887(int& val) {
    const int x = 2887;
    std::cout << 2887;
    for (int i = 0; i < 2887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2888(int& val) {
    func_in_2888(val);
    const int x = 2888;
    std::cout << 2888;
    for (int i = 0; i < 2888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2888(int& val) {
    const int x = 2888;
    std::cout << 2888;
    for (int i = 0; i < 2888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2889(int& val) {
    func_in_2889(val);
    const int x = 2889;
    std::cout << 2889;
    for (int i = 0; i < 2889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2889(int& val) {
    const int x = 2889;
    std::cout << 2889;
    for (int i = 0; i < 2889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2890(int& val) {
    func_in_2890(val);
    const int x = 2890;
    std::cout << 2890;
    for (int i = 0; i < 2890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2890(int& val) {
    const int x = 2890;
    std::cout << 2890;
    for (int i = 0; i < 2890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2891(int& val) {
    func_in_2891(val);
    const int x = 2891;
    std::cout << 2891;
    for (int i = 0; i < 2891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2891(int& val) {
    const int x = 2891;
    std::cout << 2891;
    for (int i = 0; i < 2891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2892(int& val) {
    func_in_2892(val);
    const int x = 2892;
    std::cout << 2892;
    for (int i = 0; i < 2892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2892(int& val) {
    const int x = 2892;
    std::cout << 2892;
    for (int i = 0; i < 2892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2893(int& val) {
    func_in_2893(val);
    const int x = 2893;
    std::cout << 2893;
    for (int i = 0; i < 2893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2893(int& val) {
    const int x = 2893;
    std::cout << 2893;
    for (int i = 0; i < 2893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2894(int& val) {
    func_in_2894(val);
    const int x = 2894;
    std::cout << 2894;
    for (int i = 0; i < 2894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2894(int& val) {
    const int x = 2894;
    std::cout << 2894;
    for (int i = 0; i < 2894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2895(int& val) {
    func_in_2895(val);
    const int x = 2895;
    std::cout << 2895;
    for (int i = 0; i < 2895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2895(int& val) {
    const int x = 2895;
    std::cout << 2895;
    for (int i = 0; i < 2895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2896(int& val) {
    func_in_2896(val);
    const int x = 2896;
    std::cout << 2896;
    for (int i = 0; i < 2896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2896(int& val) {
    const int x = 2896;
    std::cout << 2896;
    for (int i = 0; i < 2896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2897(int& val) {
    func_in_2897(val);
    const int x = 2897;
    std::cout << 2897;
    for (int i = 0; i < 2897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2897(int& val) {
    const int x = 2897;
    std::cout << 2897;
    for (int i = 0; i < 2897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2898(int& val) {
    func_in_2898(val);
    const int x = 2898;
    std::cout << 2898;
    for (int i = 0; i < 2898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2898(int& val) {
    const int x = 2898;
    std::cout << 2898;
    for (int i = 0; i < 2898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2899(int& val) {
    func_in_2899(val);
    const int x = 2899;
    std::cout << 2899;
    for (int i = 0; i < 2899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2899(int& val) {
    const int x = 2899;
    std::cout << 2899;
    for (int i = 0; i < 2899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2900(int& val) {
    func_in_2900(val);
    const int x = 2900;
    std::cout << 2900;
    for (int i = 0; i < 2900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2900(int& val) {
    const int x = 2900;
    std::cout << 2900;
    for (int i = 0; i < 2900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2901(int& val) {
    func_in_2901(val);
    const int x = 2901;
    std::cout << 2901;
    for (int i = 0; i < 2901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2901(int& val) {
    const int x = 2901;
    std::cout << 2901;
    for (int i = 0; i < 2901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2902(int& val) {
    func_in_2902(val);
    const int x = 2902;
    std::cout << 2902;
    for (int i = 0; i < 2902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2902(int& val) {
    const int x = 2902;
    std::cout << 2902;
    for (int i = 0; i < 2902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2903(int& val) {
    func_in_2903(val);
    const int x = 2903;
    std::cout << 2903;
    for (int i = 0; i < 2903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2903(int& val) {
    const int x = 2903;
    std::cout << 2903;
    for (int i = 0; i < 2903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2904(int& val) {
    func_in_2904(val);
    const int x = 2904;
    std::cout << 2904;
    for (int i = 0; i < 2904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2904(int& val) {
    const int x = 2904;
    std::cout << 2904;
    for (int i = 0; i < 2904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2905(int& val) {
    func_in_2905(val);
    const int x = 2905;
    std::cout << 2905;
    for (int i = 0; i < 2905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2905(int& val) {
    const int x = 2905;
    std::cout << 2905;
    for (int i = 0; i < 2905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2906(int& val) {
    func_in_2906(val);
    const int x = 2906;
    std::cout << 2906;
    for (int i = 0; i < 2906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2906(int& val) {
    const int x = 2906;
    std::cout << 2906;
    for (int i = 0; i < 2906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2907(int& val) {
    func_in_2907(val);
    const int x = 2907;
    std::cout << 2907;
    for (int i = 0; i < 2907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2907(int& val) {
    const int x = 2907;
    std::cout << 2907;
    for (int i = 0; i < 2907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2908(int& val) {
    func_in_2908(val);
    const int x = 2908;
    std::cout << 2908;
    for (int i = 0; i < 2908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2908(int& val) {
    const int x = 2908;
    std::cout << 2908;
    for (int i = 0; i < 2908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2909(int& val) {
    func_in_2909(val);
    const int x = 2909;
    std::cout << 2909;
    for (int i = 0; i < 2909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2909(int& val) {
    const int x = 2909;
    std::cout << 2909;
    for (int i = 0; i < 2909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2910(int& val) {
    func_in_2910(val);
    const int x = 2910;
    std::cout << 2910;
    for (int i = 0; i < 2910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2910(int& val) {
    const int x = 2910;
    std::cout << 2910;
    for (int i = 0; i < 2910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2911(int& val) {
    func_in_2911(val);
    const int x = 2911;
    std::cout << 2911;
    for (int i = 0; i < 2911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2911(int& val) {
    const int x = 2911;
    std::cout << 2911;
    for (int i = 0; i < 2911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2912(int& val) {
    func_in_2912(val);
    const int x = 2912;
    std::cout << 2912;
    for (int i = 0; i < 2912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2912(int& val) {
    const int x = 2912;
    std::cout << 2912;
    for (int i = 0; i < 2912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2913(int& val) {
    func_in_2913(val);
    const int x = 2913;
    std::cout << 2913;
    for (int i = 0; i < 2913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2913(int& val) {
    const int x = 2913;
    std::cout << 2913;
    for (int i = 0; i < 2913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2914(int& val) {
    func_in_2914(val);
    const int x = 2914;
    std::cout << 2914;
    for (int i = 0; i < 2914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2914(int& val) {
    const int x = 2914;
    std::cout << 2914;
    for (int i = 0; i < 2914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2915(int& val) {
    func_in_2915(val);
    const int x = 2915;
    std::cout << 2915;
    for (int i = 0; i < 2915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2915(int& val) {
    const int x = 2915;
    std::cout << 2915;
    for (int i = 0; i < 2915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2916(int& val) {
    func_in_2916(val);
    const int x = 2916;
    std::cout << 2916;
    for (int i = 0; i < 2916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2916(int& val) {
    const int x = 2916;
    std::cout << 2916;
    for (int i = 0; i < 2916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2917(int& val) {
    func_in_2917(val);
    const int x = 2917;
    std::cout << 2917;
    for (int i = 0; i < 2917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2917(int& val) {
    const int x = 2917;
    std::cout << 2917;
    for (int i = 0; i < 2917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2918(int& val) {
    func_in_2918(val);
    const int x = 2918;
    std::cout << 2918;
    for (int i = 0; i < 2918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2918(int& val) {
    const int x = 2918;
    std::cout << 2918;
    for (int i = 0; i < 2918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2919(int& val) {
    func_in_2919(val);
    const int x = 2919;
    std::cout << 2919;
    for (int i = 0; i < 2919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2919(int& val) {
    const int x = 2919;
    std::cout << 2919;
    for (int i = 0; i < 2919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2920(int& val) {
    func_in_2920(val);
    const int x = 2920;
    std::cout << 2920;
    for (int i = 0; i < 2920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2920(int& val) {
    const int x = 2920;
    std::cout << 2920;
    for (int i = 0; i < 2920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2921(int& val) {
    func_in_2921(val);
    const int x = 2921;
    std::cout << 2921;
    for (int i = 0; i < 2921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2921(int& val) {
    const int x = 2921;
    std::cout << 2921;
    for (int i = 0; i < 2921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2922(int& val) {
    func_in_2922(val);
    const int x = 2922;
    std::cout << 2922;
    for (int i = 0; i < 2922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2922(int& val) {
    const int x = 2922;
    std::cout << 2922;
    for (int i = 0; i < 2922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2923(int& val) {
    func_in_2923(val);
    const int x = 2923;
    std::cout << 2923;
    for (int i = 0; i < 2923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2923(int& val) {
    const int x = 2923;
    std::cout << 2923;
    for (int i = 0; i < 2923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2924(int& val) {
    func_in_2924(val);
    const int x = 2924;
    std::cout << 2924;
    for (int i = 0; i < 2924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2924(int& val) {
    const int x = 2924;
    std::cout << 2924;
    for (int i = 0; i < 2924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2925(int& val) {
    func_in_2925(val);
    const int x = 2925;
    std::cout << 2925;
    for (int i = 0; i < 2925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2925(int& val) {
    const int x = 2925;
    std::cout << 2925;
    for (int i = 0; i < 2925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2926(int& val) {
    func_in_2926(val);
    const int x = 2926;
    std::cout << 2926;
    for (int i = 0; i < 2926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2926(int& val) {
    const int x = 2926;
    std::cout << 2926;
    for (int i = 0; i < 2926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2927(int& val) {
    func_in_2927(val);
    const int x = 2927;
    std::cout << 2927;
    for (int i = 0; i < 2927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2927(int& val) {
    const int x = 2927;
    std::cout << 2927;
    for (int i = 0; i < 2927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2928(int& val) {
    func_in_2928(val);
    const int x = 2928;
    std::cout << 2928;
    for (int i = 0; i < 2928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2928(int& val) {
    const int x = 2928;
    std::cout << 2928;
    for (int i = 0; i < 2928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2929(int& val) {
    func_in_2929(val);
    const int x = 2929;
    std::cout << 2929;
    for (int i = 0; i < 2929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2929(int& val) {
    const int x = 2929;
    std::cout << 2929;
    for (int i = 0; i < 2929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2930(int& val) {
    func_in_2930(val);
    const int x = 2930;
    std::cout << 2930;
    for (int i = 0; i < 2930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2930(int& val) {
    const int x = 2930;
    std::cout << 2930;
    for (int i = 0; i < 2930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2931(int& val) {
    func_in_2931(val);
    const int x = 2931;
    std::cout << 2931;
    for (int i = 0; i < 2931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2931(int& val) {
    const int x = 2931;
    std::cout << 2931;
    for (int i = 0; i < 2931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2932(int& val) {
    func_in_2932(val);
    const int x = 2932;
    std::cout << 2932;
    for (int i = 0; i < 2932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2932(int& val) {
    const int x = 2932;
    std::cout << 2932;
    for (int i = 0; i < 2932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2933(int& val) {
    func_in_2933(val);
    const int x = 2933;
    std::cout << 2933;
    for (int i = 0; i < 2933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2933(int& val) {
    const int x = 2933;
    std::cout << 2933;
    for (int i = 0; i < 2933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2934(int& val) {
    func_in_2934(val);
    const int x = 2934;
    std::cout << 2934;
    for (int i = 0; i < 2934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2934(int& val) {
    const int x = 2934;
    std::cout << 2934;
    for (int i = 0; i < 2934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2935(int& val) {
    func_in_2935(val);
    const int x = 2935;
    std::cout << 2935;
    for (int i = 0; i < 2935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2935(int& val) {
    const int x = 2935;
    std::cout << 2935;
    for (int i = 0; i < 2935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2936(int& val) {
    func_in_2936(val);
    const int x = 2936;
    std::cout << 2936;
    for (int i = 0; i < 2936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2936(int& val) {
    const int x = 2936;
    std::cout << 2936;
    for (int i = 0; i < 2936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2937(int& val) {
    func_in_2937(val);
    const int x = 2937;
    std::cout << 2937;
    for (int i = 0; i < 2937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2937(int& val) {
    const int x = 2937;
    std::cout << 2937;
    for (int i = 0; i < 2937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2938(int& val) {
    func_in_2938(val);
    const int x = 2938;
    std::cout << 2938;
    for (int i = 0; i < 2938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2938(int& val) {
    const int x = 2938;
    std::cout << 2938;
    for (int i = 0; i < 2938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2939(int& val) {
    func_in_2939(val);
    const int x = 2939;
    std::cout << 2939;
    for (int i = 0; i < 2939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2939(int& val) {
    const int x = 2939;
    std::cout << 2939;
    for (int i = 0; i < 2939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2940(int& val) {
    func_in_2940(val);
    const int x = 2940;
    std::cout << 2940;
    for (int i = 0; i < 2940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2940(int& val) {
    const int x = 2940;
    std::cout << 2940;
    for (int i = 0; i < 2940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2941(int& val) {
    func_in_2941(val);
    const int x = 2941;
    std::cout << 2941;
    for (int i = 0; i < 2941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2941(int& val) {
    const int x = 2941;
    std::cout << 2941;
    for (int i = 0; i < 2941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2942(int& val) {
    func_in_2942(val);
    const int x = 2942;
    std::cout << 2942;
    for (int i = 0; i < 2942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2942(int& val) {
    const int x = 2942;
    std::cout << 2942;
    for (int i = 0; i < 2942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2943(int& val) {
    func_in_2943(val);
    const int x = 2943;
    std::cout << 2943;
    for (int i = 0; i < 2943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2943(int& val) {
    const int x = 2943;
    std::cout << 2943;
    for (int i = 0; i < 2943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2944(int& val) {
    func_in_2944(val);
    const int x = 2944;
    std::cout << 2944;
    for (int i = 0; i < 2944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2944(int& val) {
    const int x = 2944;
    std::cout << 2944;
    for (int i = 0; i < 2944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2945(int& val) {
    func_in_2945(val);
    const int x = 2945;
    std::cout << 2945;
    for (int i = 0; i < 2945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2945(int& val) {
    const int x = 2945;
    std::cout << 2945;
    for (int i = 0; i < 2945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2946(int& val) {
    func_in_2946(val);
    const int x = 2946;
    std::cout << 2946;
    for (int i = 0; i < 2946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2946(int& val) {
    const int x = 2946;
    std::cout << 2946;
    for (int i = 0; i < 2946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2947(int& val) {
    func_in_2947(val);
    const int x = 2947;
    std::cout << 2947;
    for (int i = 0; i < 2947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2947(int& val) {
    const int x = 2947;
    std::cout << 2947;
    for (int i = 0; i < 2947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2948(int& val) {
    func_in_2948(val);
    const int x = 2948;
    std::cout << 2948;
    for (int i = 0; i < 2948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2948(int& val) {
    const int x = 2948;
    std::cout << 2948;
    for (int i = 0; i < 2948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2949(int& val) {
    func_in_2949(val);
    const int x = 2949;
    std::cout << 2949;
    for (int i = 0; i < 2949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2949(int& val) {
    const int x = 2949;
    std::cout << 2949;
    for (int i = 0; i < 2949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2950(int& val) {
    func_in_2950(val);
    const int x = 2950;
    std::cout << 2950;
    for (int i = 0; i < 2950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2950(int& val) {
    const int x = 2950;
    std::cout << 2950;
    for (int i = 0; i < 2950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2951(int& val) {
    func_in_2951(val);
    const int x = 2951;
    std::cout << 2951;
    for (int i = 0; i < 2951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2951(int& val) {
    const int x = 2951;
    std::cout << 2951;
    for (int i = 0; i < 2951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2952(int& val) {
    func_in_2952(val);
    const int x = 2952;
    std::cout << 2952;
    for (int i = 0; i < 2952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2952(int& val) {
    const int x = 2952;
    std::cout << 2952;
    for (int i = 0; i < 2952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2953(int& val) {
    func_in_2953(val);
    const int x = 2953;
    std::cout << 2953;
    for (int i = 0; i < 2953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2953(int& val) {
    const int x = 2953;
    std::cout << 2953;
    for (int i = 0; i < 2953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2954(int& val) {
    func_in_2954(val);
    const int x = 2954;
    std::cout << 2954;
    for (int i = 0; i < 2954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2954(int& val) {
    const int x = 2954;
    std::cout << 2954;
    for (int i = 0; i < 2954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2955(int& val) {
    func_in_2955(val);
    const int x = 2955;
    std::cout << 2955;
    for (int i = 0; i < 2955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2955(int& val) {
    const int x = 2955;
    std::cout << 2955;
    for (int i = 0; i < 2955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2956(int& val) {
    func_in_2956(val);
    const int x = 2956;
    std::cout << 2956;
    for (int i = 0; i < 2956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2956(int& val) {
    const int x = 2956;
    std::cout << 2956;
    for (int i = 0; i < 2956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2957(int& val) {
    func_in_2957(val);
    const int x = 2957;
    std::cout << 2957;
    for (int i = 0; i < 2957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2957(int& val) {
    const int x = 2957;
    std::cout << 2957;
    for (int i = 0; i < 2957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2958(int& val) {
    func_in_2958(val);
    const int x = 2958;
    std::cout << 2958;
    for (int i = 0; i < 2958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2958(int& val) {
    const int x = 2958;
    std::cout << 2958;
    for (int i = 0; i < 2958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2959(int& val) {
    func_in_2959(val);
    const int x = 2959;
    std::cout << 2959;
    for (int i = 0; i < 2959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2959(int& val) {
    const int x = 2959;
    std::cout << 2959;
    for (int i = 0; i < 2959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2960(int& val) {
    func_in_2960(val);
    const int x = 2960;
    std::cout << 2960;
    for (int i = 0; i < 2960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2960(int& val) {
    const int x = 2960;
    std::cout << 2960;
    for (int i = 0; i < 2960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2961(int& val) {
    func_in_2961(val);
    const int x = 2961;
    std::cout << 2961;
    for (int i = 0; i < 2961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2961(int& val) {
    const int x = 2961;
    std::cout << 2961;
    for (int i = 0; i < 2961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2962(int& val) {
    func_in_2962(val);
    const int x = 2962;
    std::cout << 2962;
    for (int i = 0; i < 2962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2962(int& val) {
    const int x = 2962;
    std::cout << 2962;
    for (int i = 0; i < 2962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2963(int& val) {
    func_in_2963(val);
    const int x = 2963;
    std::cout << 2963;
    for (int i = 0; i < 2963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2963(int& val) {
    const int x = 2963;
    std::cout << 2963;
    for (int i = 0; i < 2963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2964(int& val) {
    func_in_2964(val);
    const int x = 2964;
    std::cout << 2964;
    for (int i = 0; i < 2964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2964(int& val) {
    const int x = 2964;
    std::cout << 2964;
    for (int i = 0; i < 2964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2965(int& val) {
    func_in_2965(val);
    const int x = 2965;
    std::cout << 2965;
    for (int i = 0; i < 2965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2965(int& val) {
    const int x = 2965;
    std::cout << 2965;
    for (int i = 0; i < 2965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2966(int& val) {
    func_in_2966(val);
    const int x = 2966;
    std::cout << 2966;
    for (int i = 0; i < 2966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2966(int& val) {
    const int x = 2966;
    std::cout << 2966;
    for (int i = 0; i < 2966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2967(int& val) {
    func_in_2967(val);
    const int x = 2967;
    std::cout << 2967;
    for (int i = 0; i < 2967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2967(int& val) {
    const int x = 2967;
    std::cout << 2967;
    for (int i = 0; i < 2967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2968(int& val) {
    func_in_2968(val);
    const int x = 2968;
    std::cout << 2968;
    for (int i = 0; i < 2968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2968(int& val) {
    const int x = 2968;
    std::cout << 2968;
    for (int i = 0; i < 2968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2969(int& val) {
    func_in_2969(val);
    const int x = 2969;
    std::cout << 2969;
    for (int i = 0; i < 2969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2969(int& val) {
    const int x = 2969;
    std::cout << 2969;
    for (int i = 0; i < 2969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2970(int& val) {
    func_in_2970(val);
    const int x = 2970;
    std::cout << 2970;
    for (int i = 0; i < 2970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2970(int& val) {
    const int x = 2970;
    std::cout << 2970;
    for (int i = 0; i < 2970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2971(int& val) {
    func_in_2971(val);
    const int x = 2971;
    std::cout << 2971;
    for (int i = 0; i < 2971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2971(int& val) {
    const int x = 2971;
    std::cout << 2971;
    for (int i = 0; i < 2971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2972(int& val) {
    func_in_2972(val);
    const int x = 2972;
    std::cout << 2972;
    for (int i = 0; i < 2972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2972(int& val) {
    const int x = 2972;
    std::cout << 2972;
    for (int i = 0; i < 2972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2973(int& val) {
    func_in_2973(val);
    const int x = 2973;
    std::cout << 2973;
    for (int i = 0; i < 2973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2973(int& val) {
    const int x = 2973;
    std::cout << 2973;
    for (int i = 0; i < 2973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2974(int& val) {
    func_in_2974(val);
    const int x = 2974;
    std::cout << 2974;
    for (int i = 0; i < 2974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2974(int& val) {
    const int x = 2974;
    std::cout << 2974;
    for (int i = 0; i < 2974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2975(int& val) {
    func_in_2975(val);
    const int x = 2975;
    std::cout << 2975;
    for (int i = 0; i < 2975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2975(int& val) {
    const int x = 2975;
    std::cout << 2975;
    for (int i = 0; i < 2975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2976(int& val) {
    func_in_2976(val);
    const int x = 2976;
    std::cout << 2976;
    for (int i = 0; i < 2976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2976(int& val) {
    const int x = 2976;
    std::cout << 2976;
    for (int i = 0; i < 2976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2977(int& val) {
    func_in_2977(val);
    const int x = 2977;
    std::cout << 2977;
    for (int i = 0; i < 2977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2977(int& val) {
    const int x = 2977;
    std::cout << 2977;
    for (int i = 0; i < 2977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2978(int& val) {
    func_in_2978(val);
    const int x = 2978;
    std::cout << 2978;
    for (int i = 0; i < 2978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2978(int& val) {
    const int x = 2978;
    std::cout << 2978;
    for (int i = 0; i < 2978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2979(int& val) {
    func_in_2979(val);
    const int x = 2979;
    std::cout << 2979;
    for (int i = 0; i < 2979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2979(int& val) {
    const int x = 2979;
    std::cout << 2979;
    for (int i = 0; i < 2979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2980(int& val) {
    func_in_2980(val);
    const int x = 2980;
    std::cout << 2980;
    for (int i = 0; i < 2980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2980(int& val) {
    const int x = 2980;
    std::cout << 2980;
    for (int i = 0; i < 2980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2981(int& val) {
    func_in_2981(val);
    const int x = 2981;
    std::cout << 2981;
    for (int i = 0; i < 2981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2981(int& val) {
    const int x = 2981;
    std::cout << 2981;
    for (int i = 0; i < 2981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2982(int& val) {
    func_in_2982(val);
    const int x = 2982;
    std::cout << 2982;
    for (int i = 0; i < 2982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2982(int& val) {
    const int x = 2982;
    std::cout << 2982;
    for (int i = 0; i < 2982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2983(int& val) {
    func_in_2983(val);
    const int x = 2983;
    std::cout << 2983;
    for (int i = 0; i < 2983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2983(int& val) {
    const int x = 2983;
    std::cout << 2983;
    for (int i = 0; i < 2983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2984(int& val) {
    func_in_2984(val);
    const int x = 2984;
    std::cout << 2984;
    for (int i = 0; i < 2984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2984(int& val) {
    const int x = 2984;
    std::cout << 2984;
    for (int i = 0; i < 2984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2985(int& val) {
    func_in_2985(val);
    const int x = 2985;
    std::cout << 2985;
    for (int i = 0; i < 2985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2985(int& val) {
    const int x = 2985;
    std::cout << 2985;
    for (int i = 0; i < 2985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2986(int& val) {
    func_in_2986(val);
    const int x = 2986;
    std::cout << 2986;
    for (int i = 0; i < 2986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2986(int& val) {
    const int x = 2986;
    std::cout << 2986;
    for (int i = 0; i < 2986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2987(int& val) {
    func_in_2987(val);
    const int x = 2987;
    std::cout << 2987;
    for (int i = 0; i < 2987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2987(int& val) {
    const int x = 2987;
    std::cout << 2987;
    for (int i = 0; i < 2987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2988(int& val) {
    func_in_2988(val);
    const int x = 2988;
    std::cout << 2988;
    for (int i = 0; i < 2988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2988(int& val) {
    const int x = 2988;
    std::cout << 2988;
    for (int i = 0; i < 2988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2989(int& val) {
    func_in_2989(val);
    const int x = 2989;
    std::cout << 2989;
    for (int i = 0; i < 2989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2989(int& val) {
    const int x = 2989;
    std::cout << 2989;
    for (int i = 0; i < 2989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2990(int& val) {
    func_in_2990(val);
    const int x = 2990;
    std::cout << 2990;
    for (int i = 0; i < 2990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2990(int& val) {
    const int x = 2990;
    std::cout << 2990;
    for (int i = 0; i < 2990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2991(int& val) {
    func_in_2991(val);
    const int x = 2991;
    std::cout << 2991;
    for (int i = 0; i < 2991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2991(int& val) {
    const int x = 2991;
    std::cout << 2991;
    for (int i = 0; i < 2991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2992(int& val) {
    func_in_2992(val);
    const int x = 2992;
    std::cout << 2992;
    for (int i = 0; i < 2992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2992(int& val) {
    const int x = 2992;
    std::cout << 2992;
    for (int i = 0; i < 2992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2993(int& val) {
    func_in_2993(val);
    const int x = 2993;
    std::cout << 2993;
    for (int i = 0; i < 2993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2993(int& val) {
    const int x = 2993;
    std::cout << 2993;
    for (int i = 0; i < 2993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2994(int& val) {
    func_in_2994(val);
    const int x = 2994;
    std::cout << 2994;
    for (int i = 0; i < 2994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2994(int& val) {
    const int x = 2994;
    std::cout << 2994;
    for (int i = 0; i < 2994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2995(int& val) {
    func_in_2995(val);
    const int x = 2995;
    std::cout << 2995;
    for (int i = 0; i < 2995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2995(int& val) {
    const int x = 2995;
    std::cout << 2995;
    for (int i = 0; i < 2995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2996(int& val) {
    func_in_2996(val);
    const int x = 2996;
    std::cout << 2996;
    for (int i = 0; i < 2996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2996(int& val) {
    const int x = 2996;
    std::cout << 2996;
    for (int i = 0; i < 2996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2997(int& val) {
    func_in_2997(val);
    const int x = 2997;
    std::cout << 2997;
    for (int i = 0; i < 2997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2997(int& val) {
    const int x = 2997;
    std::cout << 2997;
    for (int i = 0; i < 2997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2998(int& val) {
    func_in_2998(val);
    const int x = 2998;
    std::cout << 2998;
    for (int i = 0; i < 2998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2998(int& val) {
    const int x = 2998;
    std::cout << 2998;
    for (int i = 0; i < 2998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_2999(int& val) {
    func_in_2999(val);
    const int x = 2999;
    std::cout << 2999;
    for (int i = 0; i < 2999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_2999(int& val) {
    const int x = 2999;
    std::cout << 2999;
    for (int i = 0; i < 2999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3000(int& val) {
    func_in_3000(val);
    const int x = 3000;
    std::cout << 3000;
    for (int i = 0; i < 3000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3000(int& val) {
    const int x = 3000;
    std::cout << 3000;
    for (int i = 0; i < 3000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3001(int& val) {
    func_in_3001(val);
    const int x = 3001;
    std::cout << 3001;
    for (int i = 0; i < 3001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3001(int& val) {
    const int x = 3001;
    std::cout << 3001;
    for (int i = 0; i < 3001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3002(int& val) {
    func_in_3002(val);
    const int x = 3002;
    std::cout << 3002;
    for (int i = 0; i < 3002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3002(int& val) {
    const int x = 3002;
    std::cout << 3002;
    for (int i = 0; i < 3002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3003(int& val) {
    func_in_3003(val);
    const int x = 3003;
    std::cout << 3003;
    for (int i = 0; i < 3003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3003(int& val) {
    const int x = 3003;
    std::cout << 3003;
    for (int i = 0; i < 3003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3004(int& val) {
    func_in_3004(val);
    const int x = 3004;
    std::cout << 3004;
    for (int i = 0; i < 3004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3004(int& val) {
    const int x = 3004;
    std::cout << 3004;
    for (int i = 0; i < 3004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3005(int& val) {
    func_in_3005(val);
    const int x = 3005;
    std::cout << 3005;
    for (int i = 0; i < 3005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3005(int& val) {
    const int x = 3005;
    std::cout << 3005;
    for (int i = 0; i < 3005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3006(int& val) {
    func_in_3006(val);
    const int x = 3006;
    std::cout << 3006;
    for (int i = 0; i < 3006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3006(int& val) {
    const int x = 3006;
    std::cout << 3006;
    for (int i = 0; i < 3006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3007(int& val) {
    func_in_3007(val);
    const int x = 3007;
    std::cout << 3007;
    for (int i = 0; i < 3007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3007(int& val) {
    const int x = 3007;
    std::cout << 3007;
    for (int i = 0; i < 3007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3008(int& val) {
    func_in_3008(val);
    const int x = 3008;
    std::cout << 3008;
    for (int i = 0; i < 3008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3008(int& val) {
    const int x = 3008;
    std::cout << 3008;
    for (int i = 0; i < 3008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3009(int& val) {
    func_in_3009(val);
    const int x = 3009;
    std::cout << 3009;
    for (int i = 0; i < 3009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3009(int& val) {
    const int x = 3009;
    std::cout << 3009;
    for (int i = 0; i < 3009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3010(int& val) {
    func_in_3010(val);
    const int x = 3010;
    std::cout << 3010;
    for (int i = 0; i < 3010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3010(int& val) {
    const int x = 3010;
    std::cout << 3010;
    for (int i = 0; i < 3010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3011(int& val) {
    func_in_3011(val);
    const int x = 3011;
    std::cout << 3011;
    for (int i = 0; i < 3011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3011(int& val) {
    const int x = 3011;
    std::cout << 3011;
    for (int i = 0; i < 3011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3012(int& val) {
    func_in_3012(val);
    const int x = 3012;
    std::cout << 3012;
    for (int i = 0; i < 3012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3012(int& val) {
    const int x = 3012;
    std::cout << 3012;
    for (int i = 0; i < 3012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3013(int& val) {
    func_in_3013(val);
    const int x = 3013;
    std::cout << 3013;
    for (int i = 0; i < 3013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3013(int& val) {
    const int x = 3013;
    std::cout << 3013;
    for (int i = 0; i < 3013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3014(int& val) {
    func_in_3014(val);
    const int x = 3014;
    std::cout << 3014;
    for (int i = 0; i < 3014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3014(int& val) {
    const int x = 3014;
    std::cout << 3014;
    for (int i = 0; i < 3014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3015(int& val) {
    func_in_3015(val);
    const int x = 3015;
    std::cout << 3015;
    for (int i = 0; i < 3015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3015(int& val) {
    const int x = 3015;
    std::cout << 3015;
    for (int i = 0; i < 3015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3016(int& val) {
    func_in_3016(val);
    const int x = 3016;
    std::cout << 3016;
    for (int i = 0; i < 3016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3016(int& val) {
    const int x = 3016;
    std::cout << 3016;
    for (int i = 0; i < 3016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3017(int& val) {
    func_in_3017(val);
    const int x = 3017;
    std::cout << 3017;
    for (int i = 0; i < 3017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3017(int& val) {
    const int x = 3017;
    std::cout << 3017;
    for (int i = 0; i < 3017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3018(int& val) {
    func_in_3018(val);
    const int x = 3018;
    std::cout << 3018;
    for (int i = 0; i < 3018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3018(int& val) {
    const int x = 3018;
    std::cout << 3018;
    for (int i = 0; i < 3018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3019(int& val) {
    func_in_3019(val);
    const int x = 3019;
    std::cout << 3019;
    for (int i = 0; i < 3019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3019(int& val) {
    const int x = 3019;
    std::cout << 3019;
    for (int i = 0; i < 3019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3020(int& val) {
    func_in_3020(val);
    const int x = 3020;
    std::cout << 3020;
    for (int i = 0; i < 3020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3020(int& val) {
    const int x = 3020;
    std::cout << 3020;
    for (int i = 0; i < 3020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3021(int& val) {
    func_in_3021(val);
    const int x = 3021;
    std::cout << 3021;
    for (int i = 0; i < 3021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3021(int& val) {
    const int x = 3021;
    std::cout << 3021;
    for (int i = 0; i < 3021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3022(int& val) {
    func_in_3022(val);
    const int x = 3022;
    std::cout << 3022;
    for (int i = 0; i < 3022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3022(int& val) {
    const int x = 3022;
    std::cout << 3022;
    for (int i = 0; i < 3022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3023(int& val) {
    func_in_3023(val);
    const int x = 3023;
    std::cout << 3023;
    for (int i = 0; i < 3023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3023(int& val) {
    const int x = 3023;
    std::cout << 3023;
    for (int i = 0; i < 3023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3024(int& val) {
    func_in_3024(val);
    const int x = 3024;
    std::cout << 3024;
    for (int i = 0; i < 3024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3024(int& val) {
    const int x = 3024;
    std::cout << 3024;
    for (int i = 0; i < 3024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3025(int& val) {
    func_in_3025(val);
    const int x = 3025;
    std::cout << 3025;
    for (int i = 0; i < 3025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3025(int& val) {
    const int x = 3025;
    std::cout << 3025;
    for (int i = 0; i < 3025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3026(int& val) {
    func_in_3026(val);
    const int x = 3026;
    std::cout << 3026;
    for (int i = 0; i < 3026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3026(int& val) {
    const int x = 3026;
    std::cout << 3026;
    for (int i = 0; i < 3026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3027(int& val) {
    func_in_3027(val);
    const int x = 3027;
    std::cout << 3027;
    for (int i = 0; i < 3027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3027(int& val) {
    const int x = 3027;
    std::cout << 3027;
    for (int i = 0; i < 3027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3028(int& val) {
    func_in_3028(val);
    const int x = 3028;
    std::cout << 3028;
    for (int i = 0; i < 3028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3028(int& val) {
    const int x = 3028;
    std::cout << 3028;
    for (int i = 0; i < 3028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3029(int& val) {
    func_in_3029(val);
    const int x = 3029;
    std::cout << 3029;
    for (int i = 0; i < 3029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3029(int& val) {
    const int x = 3029;
    std::cout << 3029;
    for (int i = 0; i < 3029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3030(int& val) {
    func_in_3030(val);
    const int x = 3030;
    std::cout << 3030;
    for (int i = 0; i < 3030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3030(int& val) {
    const int x = 3030;
    std::cout << 3030;
    for (int i = 0; i < 3030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3031(int& val) {
    func_in_3031(val);
    const int x = 3031;
    std::cout << 3031;
    for (int i = 0; i < 3031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3031(int& val) {
    const int x = 3031;
    std::cout << 3031;
    for (int i = 0; i < 3031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3032(int& val) {
    func_in_3032(val);
    const int x = 3032;
    std::cout << 3032;
    for (int i = 0; i < 3032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3032(int& val) {
    const int x = 3032;
    std::cout << 3032;
    for (int i = 0; i < 3032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3033(int& val) {
    func_in_3033(val);
    const int x = 3033;
    std::cout << 3033;
    for (int i = 0; i < 3033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3033(int& val) {
    const int x = 3033;
    std::cout << 3033;
    for (int i = 0; i < 3033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3034(int& val) {
    func_in_3034(val);
    const int x = 3034;
    std::cout << 3034;
    for (int i = 0; i < 3034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3034(int& val) {
    const int x = 3034;
    std::cout << 3034;
    for (int i = 0; i < 3034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3035(int& val) {
    func_in_3035(val);
    const int x = 3035;
    std::cout << 3035;
    for (int i = 0; i < 3035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3035(int& val) {
    const int x = 3035;
    std::cout << 3035;
    for (int i = 0; i < 3035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3036(int& val) {
    func_in_3036(val);
    const int x = 3036;
    std::cout << 3036;
    for (int i = 0; i < 3036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3036(int& val) {
    const int x = 3036;
    std::cout << 3036;
    for (int i = 0; i < 3036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3037(int& val) {
    func_in_3037(val);
    const int x = 3037;
    std::cout << 3037;
    for (int i = 0; i < 3037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3037(int& val) {
    const int x = 3037;
    std::cout << 3037;
    for (int i = 0; i < 3037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3038(int& val) {
    func_in_3038(val);
    const int x = 3038;
    std::cout << 3038;
    for (int i = 0; i < 3038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3038(int& val) {
    const int x = 3038;
    std::cout << 3038;
    for (int i = 0; i < 3038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3039(int& val) {
    func_in_3039(val);
    const int x = 3039;
    std::cout << 3039;
    for (int i = 0; i < 3039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3039(int& val) {
    const int x = 3039;
    std::cout << 3039;
    for (int i = 0; i < 3039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3040(int& val) {
    func_in_3040(val);
    const int x = 3040;
    std::cout << 3040;
    for (int i = 0; i < 3040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3040(int& val) {
    const int x = 3040;
    std::cout << 3040;
    for (int i = 0; i < 3040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3041(int& val) {
    func_in_3041(val);
    const int x = 3041;
    std::cout << 3041;
    for (int i = 0; i < 3041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3041(int& val) {
    const int x = 3041;
    std::cout << 3041;
    for (int i = 0; i < 3041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3042(int& val) {
    func_in_3042(val);
    const int x = 3042;
    std::cout << 3042;
    for (int i = 0; i < 3042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3042(int& val) {
    const int x = 3042;
    std::cout << 3042;
    for (int i = 0; i < 3042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3043(int& val) {
    func_in_3043(val);
    const int x = 3043;
    std::cout << 3043;
    for (int i = 0; i < 3043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3043(int& val) {
    const int x = 3043;
    std::cout << 3043;
    for (int i = 0; i < 3043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3044(int& val) {
    func_in_3044(val);
    const int x = 3044;
    std::cout << 3044;
    for (int i = 0; i < 3044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3044(int& val) {
    const int x = 3044;
    std::cout << 3044;
    for (int i = 0; i < 3044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3045(int& val) {
    func_in_3045(val);
    const int x = 3045;
    std::cout << 3045;
    for (int i = 0; i < 3045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3045(int& val) {
    const int x = 3045;
    std::cout << 3045;
    for (int i = 0; i < 3045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3046(int& val) {
    func_in_3046(val);
    const int x = 3046;
    std::cout << 3046;
    for (int i = 0; i < 3046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3046(int& val) {
    const int x = 3046;
    std::cout << 3046;
    for (int i = 0; i < 3046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3047(int& val) {
    func_in_3047(val);
    const int x = 3047;
    std::cout << 3047;
    for (int i = 0; i < 3047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3047(int& val) {
    const int x = 3047;
    std::cout << 3047;
    for (int i = 0; i < 3047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3048(int& val) {
    func_in_3048(val);
    const int x = 3048;
    std::cout << 3048;
    for (int i = 0; i < 3048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3048(int& val) {
    const int x = 3048;
    std::cout << 3048;
    for (int i = 0; i < 3048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3049(int& val) {
    func_in_3049(val);
    const int x = 3049;
    std::cout << 3049;
    for (int i = 0; i < 3049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3049(int& val) {
    const int x = 3049;
    std::cout << 3049;
    for (int i = 0; i < 3049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3050(int& val) {
    func_in_3050(val);
    const int x = 3050;
    std::cout << 3050;
    for (int i = 0; i < 3050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3050(int& val) {
    const int x = 3050;
    std::cout << 3050;
    for (int i = 0; i < 3050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3051(int& val) {
    func_in_3051(val);
    const int x = 3051;
    std::cout << 3051;
    for (int i = 0; i < 3051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3051(int& val) {
    const int x = 3051;
    std::cout << 3051;
    for (int i = 0; i < 3051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3052(int& val) {
    func_in_3052(val);
    const int x = 3052;
    std::cout << 3052;
    for (int i = 0; i < 3052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3052(int& val) {
    const int x = 3052;
    std::cout << 3052;
    for (int i = 0; i < 3052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3053(int& val) {
    func_in_3053(val);
    const int x = 3053;
    std::cout << 3053;
    for (int i = 0; i < 3053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3053(int& val) {
    const int x = 3053;
    std::cout << 3053;
    for (int i = 0; i < 3053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3054(int& val) {
    func_in_3054(val);
    const int x = 3054;
    std::cout << 3054;
    for (int i = 0; i < 3054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3054(int& val) {
    const int x = 3054;
    std::cout << 3054;
    for (int i = 0; i < 3054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3055(int& val) {
    func_in_3055(val);
    const int x = 3055;
    std::cout << 3055;
    for (int i = 0; i < 3055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3055(int& val) {
    const int x = 3055;
    std::cout << 3055;
    for (int i = 0; i < 3055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3056(int& val) {
    func_in_3056(val);
    const int x = 3056;
    std::cout << 3056;
    for (int i = 0; i < 3056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3056(int& val) {
    const int x = 3056;
    std::cout << 3056;
    for (int i = 0; i < 3056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3057(int& val) {
    func_in_3057(val);
    const int x = 3057;
    std::cout << 3057;
    for (int i = 0; i < 3057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3057(int& val) {
    const int x = 3057;
    std::cout << 3057;
    for (int i = 0; i < 3057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3058(int& val) {
    func_in_3058(val);
    const int x = 3058;
    std::cout << 3058;
    for (int i = 0; i < 3058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3058(int& val) {
    const int x = 3058;
    std::cout << 3058;
    for (int i = 0; i < 3058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3059(int& val) {
    func_in_3059(val);
    const int x = 3059;
    std::cout << 3059;
    for (int i = 0; i < 3059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3059(int& val) {
    const int x = 3059;
    std::cout << 3059;
    for (int i = 0; i < 3059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3060(int& val) {
    func_in_3060(val);
    const int x = 3060;
    std::cout << 3060;
    for (int i = 0; i < 3060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3060(int& val) {
    const int x = 3060;
    std::cout << 3060;
    for (int i = 0; i < 3060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3061(int& val) {
    func_in_3061(val);
    const int x = 3061;
    std::cout << 3061;
    for (int i = 0; i < 3061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3061(int& val) {
    const int x = 3061;
    std::cout << 3061;
    for (int i = 0; i < 3061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3062(int& val) {
    func_in_3062(val);
    const int x = 3062;
    std::cout << 3062;
    for (int i = 0; i < 3062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3062(int& val) {
    const int x = 3062;
    std::cout << 3062;
    for (int i = 0; i < 3062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3063(int& val) {
    func_in_3063(val);
    const int x = 3063;
    std::cout << 3063;
    for (int i = 0; i < 3063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3063(int& val) {
    const int x = 3063;
    std::cout << 3063;
    for (int i = 0; i < 3063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3064(int& val) {
    func_in_3064(val);
    const int x = 3064;
    std::cout << 3064;
    for (int i = 0; i < 3064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3064(int& val) {
    const int x = 3064;
    std::cout << 3064;
    for (int i = 0; i < 3064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3065(int& val) {
    func_in_3065(val);
    const int x = 3065;
    std::cout << 3065;
    for (int i = 0; i < 3065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3065(int& val) {
    const int x = 3065;
    std::cout << 3065;
    for (int i = 0; i < 3065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3066(int& val) {
    func_in_3066(val);
    const int x = 3066;
    std::cout << 3066;
    for (int i = 0; i < 3066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3066(int& val) {
    const int x = 3066;
    std::cout << 3066;
    for (int i = 0; i < 3066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3067(int& val) {
    func_in_3067(val);
    const int x = 3067;
    std::cout << 3067;
    for (int i = 0; i < 3067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3067(int& val) {
    const int x = 3067;
    std::cout << 3067;
    for (int i = 0; i < 3067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3068(int& val) {
    func_in_3068(val);
    const int x = 3068;
    std::cout << 3068;
    for (int i = 0; i < 3068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3068(int& val) {
    const int x = 3068;
    std::cout << 3068;
    for (int i = 0; i < 3068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3069(int& val) {
    func_in_3069(val);
    const int x = 3069;
    std::cout << 3069;
    for (int i = 0; i < 3069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3069(int& val) {
    const int x = 3069;
    std::cout << 3069;
    for (int i = 0; i < 3069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3070(int& val) {
    func_in_3070(val);
    const int x = 3070;
    std::cout << 3070;
    for (int i = 0; i < 3070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3070(int& val) {
    const int x = 3070;
    std::cout << 3070;
    for (int i = 0; i < 3070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3071(int& val) {
    func_in_3071(val);
    const int x = 3071;
    std::cout << 3071;
    for (int i = 0; i < 3071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3071(int& val) {
    const int x = 3071;
    std::cout << 3071;
    for (int i = 0; i < 3071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3072(int& val) {
    func_in_3072(val);
    const int x = 3072;
    std::cout << 3072;
    for (int i = 0; i < 3072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3072(int& val) {
    const int x = 3072;
    std::cout << 3072;
    for (int i = 0; i < 3072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3073(int& val) {
    func_in_3073(val);
    const int x = 3073;
    std::cout << 3073;
    for (int i = 0; i < 3073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3073(int& val) {
    const int x = 3073;
    std::cout << 3073;
    for (int i = 0; i < 3073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3074(int& val) {
    func_in_3074(val);
    const int x = 3074;
    std::cout << 3074;
    for (int i = 0; i < 3074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3074(int& val) {
    const int x = 3074;
    std::cout << 3074;
    for (int i = 0; i < 3074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3075(int& val) {
    func_in_3075(val);
    const int x = 3075;
    std::cout << 3075;
    for (int i = 0; i < 3075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3075(int& val) {
    const int x = 3075;
    std::cout << 3075;
    for (int i = 0; i < 3075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3076(int& val) {
    func_in_3076(val);
    const int x = 3076;
    std::cout << 3076;
    for (int i = 0; i < 3076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3076(int& val) {
    const int x = 3076;
    std::cout << 3076;
    for (int i = 0; i < 3076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3077(int& val) {
    func_in_3077(val);
    const int x = 3077;
    std::cout << 3077;
    for (int i = 0; i < 3077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3077(int& val) {
    const int x = 3077;
    std::cout << 3077;
    for (int i = 0; i < 3077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3078(int& val) {
    func_in_3078(val);
    const int x = 3078;
    std::cout << 3078;
    for (int i = 0; i < 3078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3078(int& val) {
    const int x = 3078;
    std::cout << 3078;
    for (int i = 0; i < 3078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3079(int& val) {
    func_in_3079(val);
    const int x = 3079;
    std::cout << 3079;
    for (int i = 0; i < 3079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3079(int& val) {
    const int x = 3079;
    std::cout << 3079;
    for (int i = 0; i < 3079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3080(int& val) {
    func_in_3080(val);
    const int x = 3080;
    std::cout << 3080;
    for (int i = 0; i < 3080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3080(int& val) {
    const int x = 3080;
    std::cout << 3080;
    for (int i = 0; i < 3080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3081(int& val) {
    func_in_3081(val);
    const int x = 3081;
    std::cout << 3081;
    for (int i = 0; i < 3081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3081(int& val) {
    const int x = 3081;
    std::cout << 3081;
    for (int i = 0; i < 3081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3082(int& val) {
    func_in_3082(val);
    const int x = 3082;
    std::cout << 3082;
    for (int i = 0; i < 3082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3082(int& val) {
    const int x = 3082;
    std::cout << 3082;
    for (int i = 0; i < 3082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3083(int& val) {
    func_in_3083(val);
    const int x = 3083;
    std::cout << 3083;
    for (int i = 0; i < 3083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3083(int& val) {
    const int x = 3083;
    std::cout << 3083;
    for (int i = 0; i < 3083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3084(int& val) {
    func_in_3084(val);
    const int x = 3084;
    std::cout << 3084;
    for (int i = 0; i < 3084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3084(int& val) {
    const int x = 3084;
    std::cout << 3084;
    for (int i = 0; i < 3084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3085(int& val) {
    func_in_3085(val);
    const int x = 3085;
    std::cout << 3085;
    for (int i = 0; i < 3085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3085(int& val) {
    const int x = 3085;
    std::cout << 3085;
    for (int i = 0; i < 3085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3086(int& val) {
    func_in_3086(val);
    const int x = 3086;
    std::cout << 3086;
    for (int i = 0; i < 3086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3086(int& val) {
    const int x = 3086;
    std::cout << 3086;
    for (int i = 0; i < 3086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3087(int& val) {
    func_in_3087(val);
    const int x = 3087;
    std::cout << 3087;
    for (int i = 0; i < 3087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3087(int& val) {
    const int x = 3087;
    std::cout << 3087;
    for (int i = 0; i < 3087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3088(int& val) {
    func_in_3088(val);
    const int x = 3088;
    std::cout << 3088;
    for (int i = 0; i < 3088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3088(int& val) {
    const int x = 3088;
    std::cout << 3088;
    for (int i = 0; i < 3088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3089(int& val) {
    func_in_3089(val);
    const int x = 3089;
    std::cout << 3089;
    for (int i = 0; i < 3089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3089(int& val) {
    const int x = 3089;
    std::cout << 3089;
    for (int i = 0; i < 3089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3090(int& val) {
    func_in_3090(val);
    const int x = 3090;
    std::cout << 3090;
    for (int i = 0; i < 3090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3090(int& val) {
    const int x = 3090;
    std::cout << 3090;
    for (int i = 0; i < 3090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3091(int& val) {
    func_in_3091(val);
    const int x = 3091;
    std::cout << 3091;
    for (int i = 0; i < 3091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3091(int& val) {
    const int x = 3091;
    std::cout << 3091;
    for (int i = 0; i < 3091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3092(int& val) {
    func_in_3092(val);
    const int x = 3092;
    std::cout << 3092;
    for (int i = 0; i < 3092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3092(int& val) {
    const int x = 3092;
    std::cout << 3092;
    for (int i = 0; i < 3092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3093(int& val) {
    func_in_3093(val);
    const int x = 3093;
    std::cout << 3093;
    for (int i = 0; i < 3093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3093(int& val) {
    const int x = 3093;
    std::cout << 3093;
    for (int i = 0; i < 3093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3094(int& val) {
    func_in_3094(val);
    const int x = 3094;
    std::cout << 3094;
    for (int i = 0; i < 3094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3094(int& val) {
    const int x = 3094;
    std::cout << 3094;
    for (int i = 0; i < 3094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3095(int& val) {
    func_in_3095(val);
    const int x = 3095;
    std::cout << 3095;
    for (int i = 0; i < 3095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3095(int& val) {
    const int x = 3095;
    std::cout << 3095;
    for (int i = 0; i < 3095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3096(int& val) {
    func_in_3096(val);
    const int x = 3096;
    std::cout << 3096;
    for (int i = 0; i < 3096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3096(int& val) {
    const int x = 3096;
    std::cout << 3096;
    for (int i = 0; i < 3096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3097(int& val) {
    func_in_3097(val);
    const int x = 3097;
    std::cout << 3097;
    for (int i = 0; i < 3097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3097(int& val) {
    const int x = 3097;
    std::cout << 3097;
    for (int i = 0; i < 3097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3098(int& val) {
    func_in_3098(val);
    const int x = 3098;
    std::cout << 3098;
    for (int i = 0; i < 3098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3098(int& val) {
    const int x = 3098;
    std::cout << 3098;
    for (int i = 0; i < 3098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3099(int& val) {
    func_in_3099(val);
    const int x = 3099;
    std::cout << 3099;
    for (int i = 0; i < 3099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3099(int& val) {
    const int x = 3099;
    std::cout << 3099;
    for (int i = 0; i < 3099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3100(int& val) {
    func_in_3100(val);
    const int x = 3100;
    std::cout << 3100;
    for (int i = 0; i < 3100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3100(int& val) {
    const int x = 3100;
    std::cout << 3100;
    for (int i = 0; i < 3100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3101(int& val) {
    func_in_3101(val);
    const int x = 3101;
    std::cout << 3101;
    for (int i = 0; i < 3101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3101(int& val) {
    const int x = 3101;
    std::cout << 3101;
    for (int i = 0; i < 3101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3102(int& val) {
    func_in_3102(val);
    const int x = 3102;
    std::cout << 3102;
    for (int i = 0; i < 3102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3102(int& val) {
    const int x = 3102;
    std::cout << 3102;
    for (int i = 0; i < 3102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3103(int& val) {
    func_in_3103(val);
    const int x = 3103;
    std::cout << 3103;
    for (int i = 0; i < 3103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3103(int& val) {
    const int x = 3103;
    std::cout << 3103;
    for (int i = 0; i < 3103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3104(int& val) {
    func_in_3104(val);
    const int x = 3104;
    std::cout << 3104;
    for (int i = 0; i < 3104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3104(int& val) {
    const int x = 3104;
    std::cout << 3104;
    for (int i = 0; i < 3104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3105(int& val) {
    func_in_3105(val);
    const int x = 3105;
    std::cout << 3105;
    for (int i = 0; i < 3105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3105(int& val) {
    const int x = 3105;
    std::cout << 3105;
    for (int i = 0; i < 3105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3106(int& val) {
    func_in_3106(val);
    const int x = 3106;
    std::cout << 3106;
    for (int i = 0; i < 3106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3106(int& val) {
    const int x = 3106;
    std::cout << 3106;
    for (int i = 0; i < 3106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3107(int& val) {
    func_in_3107(val);
    const int x = 3107;
    std::cout << 3107;
    for (int i = 0; i < 3107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3107(int& val) {
    const int x = 3107;
    std::cout << 3107;
    for (int i = 0; i < 3107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3108(int& val) {
    func_in_3108(val);
    const int x = 3108;
    std::cout << 3108;
    for (int i = 0; i < 3108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3108(int& val) {
    const int x = 3108;
    std::cout << 3108;
    for (int i = 0; i < 3108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3109(int& val) {
    func_in_3109(val);
    const int x = 3109;
    std::cout << 3109;
    for (int i = 0; i < 3109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3109(int& val) {
    const int x = 3109;
    std::cout << 3109;
    for (int i = 0; i < 3109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3110(int& val) {
    func_in_3110(val);
    const int x = 3110;
    std::cout << 3110;
    for (int i = 0; i < 3110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3110(int& val) {
    const int x = 3110;
    std::cout << 3110;
    for (int i = 0; i < 3110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3111(int& val) {
    func_in_3111(val);
    const int x = 3111;
    std::cout << 3111;
    for (int i = 0; i < 3111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3111(int& val) {
    const int x = 3111;
    std::cout << 3111;
    for (int i = 0; i < 3111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3112(int& val) {
    func_in_3112(val);
    const int x = 3112;
    std::cout << 3112;
    for (int i = 0; i < 3112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3112(int& val) {
    const int x = 3112;
    std::cout << 3112;
    for (int i = 0; i < 3112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3113(int& val) {
    func_in_3113(val);
    const int x = 3113;
    std::cout << 3113;
    for (int i = 0; i < 3113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3113(int& val) {
    const int x = 3113;
    std::cout << 3113;
    for (int i = 0; i < 3113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3114(int& val) {
    func_in_3114(val);
    const int x = 3114;
    std::cout << 3114;
    for (int i = 0; i < 3114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3114(int& val) {
    const int x = 3114;
    std::cout << 3114;
    for (int i = 0; i < 3114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3115(int& val) {
    func_in_3115(val);
    const int x = 3115;
    std::cout << 3115;
    for (int i = 0; i < 3115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3115(int& val) {
    const int x = 3115;
    std::cout << 3115;
    for (int i = 0; i < 3115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3116(int& val) {
    func_in_3116(val);
    const int x = 3116;
    std::cout << 3116;
    for (int i = 0; i < 3116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3116(int& val) {
    const int x = 3116;
    std::cout << 3116;
    for (int i = 0; i < 3116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3117(int& val) {
    func_in_3117(val);
    const int x = 3117;
    std::cout << 3117;
    for (int i = 0; i < 3117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3117(int& val) {
    const int x = 3117;
    std::cout << 3117;
    for (int i = 0; i < 3117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3118(int& val) {
    func_in_3118(val);
    const int x = 3118;
    std::cout << 3118;
    for (int i = 0; i < 3118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3118(int& val) {
    const int x = 3118;
    std::cout << 3118;
    for (int i = 0; i < 3118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3119(int& val) {
    func_in_3119(val);
    const int x = 3119;
    std::cout << 3119;
    for (int i = 0; i < 3119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3119(int& val) {
    const int x = 3119;
    std::cout << 3119;
    for (int i = 0; i < 3119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3120(int& val) {
    func_in_3120(val);
    const int x = 3120;
    std::cout << 3120;
    for (int i = 0; i < 3120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3120(int& val) {
    const int x = 3120;
    std::cout << 3120;
    for (int i = 0; i < 3120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3121(int& val) {
    func_in_3121(val);
    const int x = 3121;
    std::cout << 3121;
    for (int i = 0; i < 3121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3121(int& val) {
    const int x = 3121;
    std::cout << 3121;
    for (int i = 0; i < 3121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3122(int& val) {
    func_in_3122(val);
    const int x = 3122;
    std::cout << 3122;
    for (int i = 0; i < 3122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3122(int& val) {
    const int x = 3122;
    std::cout << 3122;
    for (int i = 0; i < 3122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3123(int& val) {
    func_in_3123(val);
    const int x = 3123;
    std::cout << 3123;
    for (int i = 0; i < 3123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3123(int& val) {
    const int x = 3123;
    std::cout << 3123;
    for (int i = 0; i < 3123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3124(int& val) {
    func_in_3124(val);
    const int x = 3124;
    std::cout << 3124;
    for (int i = 0; i < 3124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3124(int& val) {
    const int x = 3124;
    std::cout << 3124;
    for (int i = 0; i < 3124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3125(int& val) {
    func_in_3125(val);
    const int x = 3125;
    std::cout << 3125;
    for (int i = 0; i < 3125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3125(int& val) {
    const int x = 3125;
    std::cout << 3125;
    for (int i = 0; i < 3125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3126(int& val) {
    func_in_3126(val);
    const int x = 3126;
    std::cout << 3126;
    for (int i = 0; i < 3126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3126(int& val) {
    const int x = 3126;
    std::cout << 3126;
    for (int i = 0; i < 3126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3127(int& val) {
    func_in_3127(val);
    const int x = 3127;
    std::cout << 3127;
    for (int i = 0; i < 3127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3127(int& val) {
    const int x = 3127;
    std::cout << 3127;
    for (int i = 0; i < 3127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3128(int& val) {
    func_in_3128(val);
    const int x = 3128;
    std::cout << 3128;
    for (int i = 0; i < 3128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3128(int& val) {
    const int x = 3128;
    std::cout << 3128;
    for (int i = 0; i < 3128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3129(int& val) {
    func_in_3129(val);
    const int x = 3129;
    std::cout << 3129;
    for (int i = 0; i < 3129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3129(int& val) {
    const int x = 3129;
    std::cout << 3129;
    for (int i = 0; i < 3129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3130(int& val) {
    func_in_3130(val);
    const int x = 3130;
    std::cout << 3130;
    for (int i = 0; i < 3130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3130(int& val) {
    const int x = 3130;
    std::cout << 3130;
    for (int i = 0; i < 3130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3131(int& val) {
    func_in_3131(val);
    const int x = 3131;
    std::cout << 3131;
    for (int i = 0; i < 3131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3131(int& val) {
    const int x = 3131;
    std::cout << 3131;
    for (int i = 0; i < 3131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3132(int& val) {
    func_in_3132(val);
    const int x = 3132;
    std::cout << 3132;
    for (int i = 0; i < 3132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3132(int& val) {
    const int x = 3132;
    std::cout << 3132;
    for (int i = 0; i < 3132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3133(int& val) {
    func_in_3133(val);
    const int x = 3133;
    std::cout << 3133;
    for (int i = 0; i < 3133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3133(int& val) {
    const int x = 3133;
    std::cout << 3133;
    for (int i = 0; i < 3133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3134(int& val) {
    func_in_3134(val);
    const int x = 3134;
    std::cout << 3134;
    for (int i = 0; i < 3134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3134(int& val) {
    const int x = 3134;
    std::cout << 3134;
    for (int i = 0; i < 3134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3135(int& val) {
    func_in_3135(val);
    const int x = 3135;
    std::cout << 3135;
    for (int i = 0; i < 3135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3135(int& val) {
    const int x = 3135;
    std::cout << 3135;
    for (int i = 0; i < 3135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3136(int& val) {
    func_in_3136(val);
    const int x = 3136;
    std::cout << 3136;
    for (int i = 0; i < 3136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3136(int& val) {
    const int x = 3136;
    std::cout << 3136;
    for (int i = 0; i < 3136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3137(int& val) {
    func_in_3137(val);
    const int x = 3137;
    std::cout << 3137;
    for (int i = 0; i < 3137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3137(int& val) {
    const int x = 3137;
    std::cout << 3137;
    for (int i = 0; i < 3137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3138(int& val) {
    func_in_3138(val);
    const int x = 3138;
    std::cout << 3138;
    for (int i = 0; i < 3138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3138(int& val) {
    const int x = 3138;
    std::cout << 3138;
    for (int i = 0; i < 3138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3139(int& val) {
    func_in_3139(val);
    const int x = 3139;
    std::cout << 3139;
    for (int i = 0; i < 3139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3139(int& val) {
    const int x = 3139;
    std::cout << 3139;
    for (int i = 0; i < 3139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3140(int& val) {
    func_in_3140(val);
    const int x = 3140;
    std::cout << 3140;
    for (int i = 0; i < 3140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3140(int& val) {
    const int x = 3140;
    std::cout << 3140;
    for (int i = 0; i < 3140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3141(int& val) {
    func_in_3141(val);
    const int x = 3141;
    std::cout << 3141;
    for (int i = 0; i < 3141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3141(int& val) {
    const int x = 3141;
    std::cout << 3141;
    for (int i = 0; i < 3141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3142(int& val) {
    func_in_3142(val);
    const int x = 3142;
    std::cout << 3142;
    for (int i = 0; i < 3142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3142(int& val) {
    const int x = 3142;
    std::cout << 3142;
    for (int i = 0; i < 3142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3143(int& val) {
    func_in_3143(val);
    const int x = 3143;
    std::cout << 3143;
    for (int i = 0; i < 3143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3143(int& val) {
    const int x = 3143;
    std::cout << 3143;
    for (int i = 0; i < 3143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3144(int& val) {
    func_in_3144(val);
    const int x = 3144;
    std::cout << 3144;
    for (int i = 0; i < 3144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3144(int& val) {
    const int x = 3144;
    std::cout << 3144;
    for (int i = 0; i < 3144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3145(int& val) {
    func_in_3145(val);
    const int x = 3145;
    std::cout << 3145;
    for (int i = 0; i < 3145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3145(int& val) {
    const int x = 3145;
    std::cout << 3145;
    for (int i = 0; i < 3145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3146(int& val) {
    func_in_3146(val);
    const int x = 3146;
    std::cout << 3146;
    for (int i = 0; i < 3146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3146(int& val) {
    const int x = 3146;
    std::cout << 3146;
    for (int i = 0; i < 3146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3147(int& val) {
    func_in_3147(val);
    const int x = 3147;
    std::cout << 3147;
    for (int i = 0; i < 3147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3147(int& val) {
    const int x = 3147;
    std::cout << 3147;
    for (int i = 0; i < 3147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3148(int& val) {
    func_in_3148(val);
    const int x = 3148;
    std::cout << 3148;
    for (int i = 0; i < 3148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3148(int& val) {
    const int x = 3148;
    std::cout << 3148;
    for (int i = 0; i < 3148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3149(int& val) {
    func_in_3149(val);
    const int x = 3149;
    std::cout << 3149;
    for (int i = 0; i < 3149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3149(int& val) {
    const int x = 3149;
    std::cout << 3149;
    for (int i = 0; i < 3149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3150(int& val) {
    func_in_3150(val);
    const int x = 3150;
    std::cout << 3150;
    for (int i = 0; i < 3150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3150(int& val) {
    const int x = 3150;
    std::cout << 3150;
    for (int i = 0; i < 3150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3151(int& val) {
    func_in_3151(val);
    const int x = 3151;
    std::cout << 3151;
    for (int i = 0; i < 3151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3151(int& val) {
    const int x = 3151;
    std::cout << 3151;
    for (int i = 0; i < 3151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3152(int& val) {
    func_in_3152(val);
    const int x = 3152;
    std::cout << 3152;
    for (int i = 0; i < 3152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3152(int& val) {
    const int x = 3152;
    std::cout << 3152;
    for (int i = 0; i < 3152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3153(int& val) {
    func_in_3153(val);
    const int x = 3153;
    std::cout << 3153;
    for (int i = 0; i < 3153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3153(int& val) {
    const int x = 3153;
    std::cout << 3153;
    for (int i = 0; i < 3153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3154(int& val) {
    func_in_3154(val);
    const int x = 3154;
    std::cout << 3154;
    for (int i = 0; i < 3154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3154(int& val) {
    const int x = 3154;
    std::cout << 3154;
    for (int i = 0; i < 3154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3155(int& val) {
    func_in_3155(val);
    const int x = 3155;
    std::cout << 3155;
    for (int i = 0; i < 3155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3155(int& val) {
    const int x = 3155;
    std::cout << 3155;
    for (int i = 0; i < 3155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3156(int& val) {
    func_in_3156(val);
    const int x = 3156;
    std::cout << 3156;
    for (int i = 0; i < 3156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3156(int& val) {
    const int x = 3156;
    std::cout << 3156;
    for (int i = 0; i < 3156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3157(int& val) {
    func_in_3157(val);
    const int x = 3157;
    std::cout << 3157;
    for (int i = 0; i < 3157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3157(int& val) {
    const int x = 3157;
    std::cout << 3157;
    for (int i = 0; i < 3157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3158(int& val) {
    func_in_3158(val);
    const int x = 3158;
    std::cout << 3158;
    for (int i = 0; i < 3158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3158(int& val) {
    const int x = 3158;
    std::cout << 3158;
    for (int i = 0; i < 3158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3159(int& val) {
    func_in_3159(val);
    const int x = 3159;
    std::cout << 3159;
    for (int i = 0; i < 3159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3159(int& val) {
    const int x = 3159;
    std::cout << 3159;
    for (int i = 0; i < 3159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3160(int& val) {
    func_in_3160(val);
    const int x = 3160;
    std::cout << 3160;
    for (int i = 0; i < 3160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3160(int& val) {
    const int x = 3160;
    std::cout << 3160;
    for (int i = 0; i < 3160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3161(int& val) {
    func_in_3161(val);
    const int x = 3161;
    std::cout << 3161;
    for (int i = 0; i < 3161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3161(int& val) {
    const int x = 3161;
    std::cout << 3161;
    for (int i = 0; i < 3161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3162(int& val) {
    func_in_3162(val);
    const int x = 3162;
    std::cout << 3162;
    for (int i = 0; i < 3162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3162(int& val) {
    const int x = 3162;
    std::cout << 3162;
    for (int i = 0; i < 3162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3163(int& val) {
    func_in_3163(val);
    const int x = 3163;
    std::cout << 3163;
    for (int i = 0; i < 3163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3163(int& val) {
    const int x = 3163;
    std::cout << 3163;
    for (int i = 0; i < 3163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3164(int& val) {
    func_in_3164(val);
    const int x = 3164;
    std::cout << 3164;
    for (int i = 0; i < 3164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3164(int& val) {
    const int x = 3164;
    std::cout << 3164;
    for (int i = 0; i < 3164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3165(int& val) {
    func_in_3165(val);
    const int x = 3165;
    std::cout << 3165;
    for (int i = 0; i < 3165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3165(int& val) {
    const int x = 3165;
    std::cout << 3165;
    for (int i = 0; i < 3165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3166(int& val) {
    func_in_3166(val);
    const int x = 3166;
    std::cout << 3166;
    for (int i = 0; i < 3166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3166(int& val) {
    const int x = 3166;
    std::cout << 3166;
    for (int i = 0; i < 3166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3167(int& val) {
    func_in_3167(val);
    const int x = 3167;
    std::cout << 3167;
    for (int i = 0; i < 3167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3167(int& val) {
    const int x = 3167;
    std::cout << 3167;
    for (int i = 0; i < 3167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3168(int& val) {
    func_in_3168(val);
    const int x = 3168;
    std::cout << 3168;
    for (int i = 0; i < 3168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3168(int& val) {
    const int x = 3168;
    std::cout << 3168;
    for (int i = 0; i < 3168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3169(int& val) {
    func_in_3169(val);
    const int x = 3169;
    std::cout << 3169;
    for (int i = 0; i < 3169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3169(int& val) {
    const int x = 3169;
    std::cout << 3169;
    for (int i = 0; i < 3169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3170(int& val) {
    func_in_3170(val);
    const int x = 3170;
    std::cout << 3170;
    for (int i = 0; i < 3170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3170(int& val) {
    const int x = 3170;
    std::cout << 3170;
    for (int i = 0; i < 3170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3171(int& val) {
    func_in_3171(val);
    const int x = 3171;
    std::cout << 3171;
    for (int i = 0; i < 3171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3171(int& val) {
    const int x = 3171;
    std::cout << 3171;
    for (int i = 0; i < 3171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3172(int& val) {
    func_in_3172(val);
    const int x = 3172;
    std::cout << 3172;
    for (int i = 0; i < 3172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3172(int& val) {
    const int x = 3172;
    std::cout << 3172;
    for (int i = 0; i < 3172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3173(int& val) {
    func_in_3173(val);
    const int x = 3173;
    std::cout << 3173;
    for (int i = 0; i < 3173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3173(int& val) {
    const int x = 3173;
    std::cout << 3173;
    for (int i = 0; i < 3173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3174(int& val) {
    func_in_3174(val);
    const int x = 3174;
    std::cout << 3174;
    for (int i = 0; i < 3174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3174(int& val) {
    const int x = 3174;
    std::cout << 3174;
    for (int i = 0; i < 3174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3175(int& val) {
    func_in_3175(val);
    const int x = 3175;
    std::cout << 3175;
    for (int i = 0; i < 3175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3175(int& val) {
    const int x = 3175;
    std::cout << 3175;
    for (int i = 0; i < 3175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3176(int& val) {
    func_in_3176(val);
    const int x = 3176;
    std::cout << 3176;
    for (int i = 0; i < 3176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3176(int& val) {
    const int x = 3176;
    std::cout << 3176;
    for (int i = 0; i < 3176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3177(int& val) {
    func_in_3177(val);
    const int x = 3177;
    std::cout << 3177;
    for (int i = 0; i < 3177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3177(int& val) {
    const int x = 3177;
    std::cout << 3177;
    for (int i = 0; i < 3177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3178(int& val) {
    func_in_3178(val);
    const int x = 3178;
    std::cout << 3178;
    for (int i = 0; i < 3178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3178(int& val) {
    const int x = 3178;
    std::cout << 3178;
    for (int i = 0; i < 3178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3179(int& val) {
    func_in_3179(val);
    const int x = 3179;
    std::cout << 3179;
    for (int i = 0; i < 3179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3179(int& val) {
    const int x = 3179;
    std::cout << 3179;
    for (int i = 0; i < 3179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3180(int& val) {
    func_in_3180(val);
    const int x = 3180;
    std::cout << 3180;
    for (int i = 0; i < 3180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3180(int& val) {
    const int x = 3180;
    std::cout << 3180;
    for (int i = 0; i < 3180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3181(int& val) {
    func_in_3181(val);
    const int x = 3181;
    std::cout << 3181;
    for (int i = 0; i < 3181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3181(int& val) {
    const int x = 3181;
    std::cout << 3181;
    for (int i = 0; i < 3181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3182(int& val) {
    func_in_3182(val);
    const int x = 3182;
    std::cout << 3182;
    for (int i = 0; i < 3182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3182(int& val) {
    const int x = 3182;
    std::cout << 3182;
    for (int i = 0; i < 3182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3183(int& val) {
    func_in_3183(val);
    const int x = 3183;
    std::cout << 3183;
    for (int i = 0; i < 3183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3183(int& val) {
    const int x = 3183;
    std::cout << 3183;
    for (int i = 0; i < 3183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3184(int& val) {
    func_in_3184(val);
    const int x = 3184;
    std::cout << 3184;
    for (int i = 0; i < 3184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3184(int& val) {
    const int x = 3184;
    std::cout << 3184;
    for (int i = 0; i < 3184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3185(int& val) {
    func_in_3185(val);
    const int x = 3185;
    std::cout << 3185;
    for (int i = 0; i < 3185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3185(int& val) {
    const int x = 3185;
    std::cout << 3185;
    for (int i = 0; i < 3185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3186(int& val) {
    func_in_3186(val);
    const int x = 3186;
    std::cout << 3186;
    for (int i = 0; i < 3186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3186(int& val) {
    const int x = 3186;
    std::cout << 3186;
    for (int i = 0; i < 3186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3187(int& val) {
    func_in_3187(val);
    const int x = 3187;
    std::cout << 3187;
    for (int i = 0; i < 3187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3187(int& val) {
    const int x = 3187;
    std::cout << 3187;
    for (int i = 0; i < 3187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3188(int& val) {
    func_in_3188(val);
    const int x = 3188;
    std::cout << 3188;
    for (int i = 0; i < 3188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3188(int& val) {
    const int x = 3188;
    std::cout << 3188;
    for (int i = 0; i < 3188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3189(int& val) {
    func_in_3189(val);
    const int x = 3189;
    std::cout << 3189;
    for (int i = 0; i < 3189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3189(int& val) {
    const int x = 3189;
    std::cout << 3189;
    for (int i = 0; i < 3189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3190(int& val) {
    func_in_3190(val);
    const int x = 3190;
    std::cout << 3190;
    for (int i = 0; i < 3190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3190(int& val) {
    const int x = 3190;
    std::cout << 3190;
    for (int i = 0; i < 3190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3191(int& val) {
    func_in_3191(val);
    const int x = 3191;
    std::cout << 3191;
    for (int i = 0; i < 3191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3191(int& val) {
    const int x = 3191;
    std::cout << 3191;
    for (int i = 0; i < 3191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3192(int& val) {
    func_in_3192(val);
    const int x = 3192;
    std::cout << 3192;
    for (int i = 0; i < 3192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3192(int& val) {
    const int x = 3192;
    std::cout << 3192;
    for (int i = 0; i < 3192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3193(int& val) {
    func_in_3193(val);
    const int x = 3193;
    std::cout << 3193;
    for (int i = 0; i < 3193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3193(int& val) {
    const int x = 3193;
    std::cout << 3193;
    for (int i = 0; i < 3193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3194(int& val) {
    func_in_3194(val);
    const int x = 3194;
    std::cout << 3194;
    for (int i = 0; i < 3194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3194(int& val) {
    const int x = 3194;
    std::cout << 3194;
    for (int i = 0; i < 3194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3195(int& val) {
    func_in_3195(val);
    const int x = 3195;
    std::cout << 3195;
    for (int i = 0; i < 3195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3195(int& val) {
    const int x = 3195;
    std::cout << 3195;
    for (int i = 0; i < 3195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3196(int& val) {
    func_in_3196(val);
    const int x = 3196;
    std::cout << 3196;
    for (int i = 0; i < 3196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3196(int& val) {
    const int x = 3196;
    std::cout << 3196;
    for (int i = 0; i < 3196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3197(int& val) {
    func_in_3197(val);
    const int x = 3197;
    std::cout << 3197;
    for (int i = 0; i < 3197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3197(int& val) {
    const int x = 3197;
    std::cout << 3197;
    for (int i = 0; i < 3197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3198(int& val) {
    func_in_3198(val);
    const int x = 3198;
    std::cout << 3198;
    for (int i = 0; i < 3198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3198(int& val) {
    const int x = 3198;
    std::cout << 3198;
    for (int i = 0; i < 3198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3199(int& val) {
    func_in_3199(val);
    const int x = 3199;
    std::cout << 3199;
    for (int i = 0; i < 3199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3199(int& val) {
    const int x = 3199;
    std::cout << 3199;
    for (int i = 0; i < 3199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3200(int& val) {
    func_in_3200(val);
    const int x = 3200;
    std::cout << 3200;
    for (int i = 0; i < 3200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3200(int& val) {
    const int x = 3200;
    std::cout << 3200;
    for (int i = 0; i < 3200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3201(int& val) {
    func_in_3201(val);
    const int x = 3201;
    std::cout << 3201;
    for (int i = 0; i < 3201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3201(int& val) {
    const int x = 3201;
    std::cout << 3201;
    for (int i = 0; i < 3201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3202(int& val) {
    func_in_3202(val);
    const int x = 3202;
    std::cout << 3202;
    for (int i = 0; i < 3202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3202(int& val) {
    const int x = 3202;
    std::cout << 3202;
    for (int i = 0; i < 3202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3203(int& val) {
    func_in_3203(val);
    const int x = 3203;
    std::cout << 3203;
    for (int i = 0; i < 3203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3203(int& val) {
    const int x = 3203;
    std::cout << 3203;
    for (int i = 0; i < 3203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3204(int& val) {
    func_in_3204(val);
    const int x = 3204;
    std::cout << 3204;
    for (int i = 0; i < 3204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3204(int& val) {
    const int x = 3204;
    std::cout << 3204;
    for (int i = 0; i < 3204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3205(int& val) {
    func_in_3205(val);
    const int x = 3205;
    std::cout << 3205;
    for (int i = 0; i < 3205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3205(int& val) {
    const int x = 3205;
    std::cout << 3205;
    for (int i = 0; i < 3205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3206(int& val) {
    func_in_3206(val);
    const int x = 3206;
    std::cout << 3206;
    for (int i = 0; i < 3206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3206(int& val) {
    const int x = 3206;
    std::cout << 3206;
    for (int i = 0; i < 3206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3207(int& val) {
    func_in_3207(val);
    const int x = 3207;
    std::cout << 3207;
    for (int i = 0; i < 3207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3207(int& val) {
    const int x = 3207;
    std::cout << 3207;
    for (int i = 0; i < 3207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3208(int& val) {
    func_in_3208(val);
    const int x = 3208;
    std::cout << 3208;
    for (int i = 0; i < 3208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3208(int& val) {
    const int x = 3208;
    std::cout << 3208;
    for (int i = 0; i < 3208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3209(int& val) {
    func_in_3209(val);
    const int x = 3209;
    std::cout << 3209;
    for (int i = 0; i < 3209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3209(int& val) {
    const int x = 3209;
    std::cout << 3209;
    for (int i = 0; i < 3209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3210(int& val) {
    func_in_3210(val);
    const int x = 3210;
    std::cout << 3210;
    for (int i = 0; i < 3210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3210(int& val) {
    const int x = 3210;
    std::cout << 3210;
    for (int i = 0; i < 3210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3211(int& val) {
    func_in_3211(val);
    const int x = 3211;
    std::cout << 3211;
    for (int i = 0; i < 3211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3211(int& val) {
    const int x = 3211;
    std::cout << 3211;
    for (int i = 0; i < 3211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3212(int& val) {
    func_in_3212(val);
    const int x = 3212;
    std::cout << 3212;
    for (int i = 0; i < 3212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3212(int& val) {
    const int x = 3212;
    std::cout << 3212;
    for (int i = 0; i < 3212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3213(int& val) {
    func_in_3213(val);
    const int x = 3213;
    std::cout << 3213;
    for (int i = 0; i < 3213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3213(int& val) {
    const int x = 3213;
    std::cout << 3213;
    for (int i = 0; i < 3213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3214(int& val) {
    func_in_3214(val);
    const int x = 3214;
    std::cout << 3214;
    for (int i = 0; i < 3214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3214(int& val) {
    const int x = 3214;
    std::cout << 3214;
    for (int i = 0; i < 3214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3215(int& val) {
    func_in_3215(val);
    const int x = 3215;
    std::cout << 3215;
    for (int i = 0; i < 3215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3215(int& val) {
    const int x = 3215;
    std::cout << 3215;
    for (int i = 0; i < 3215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3216(int& val) {
    func_in_3216(val);
    const int x = 3216;
    std::cout << 3216;
    for (int i = 0; i < 3216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3216(int& val) {
    const int x = 3216;
    std::cout << 3216;
    for (int i = 0; i < 3216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3217(int& val) {
    func_in_3217(val);
    const int x = 3217;
    std::cout << 3217;
    for (int i = 0; i < 3217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3217(int& val) {
    const int x = 3217;
    std::cout << 3217;
    for (int i = 0; i < 3217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3218(int& val) {
    func_in_3218(val);
    const int x = 3218;
    std::cout << 3218;
    for (int i = 0; i < 3218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3218(int& val) {
    const int x = 3218;
    std::cout << 3218;
    for (int i = 0; i < 3218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3219(int& val) {
    func_in_3219(val);
    const int x = 3219;
    std::cout << 3219;
    for (int i = 0; i < 3219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3219(int& val) {
    const int x = 3219;
    std::cout << 3219;
    for (int i = 0; i < 3219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3220(int& val) {
    func_in_3220(val);
    const int x = 3220;
    std::cout << 3220;
    for (int i = 0; i < 3220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3220(int& val) {
    const int x = 3220;
    std::cout << 3220;
    for (int i = 0; i < 3220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3221(int& val) {
    func_in_3221(val);
    const int x = 3221;
    std::cout << 3221;
    for (int i = 0; i < 3221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3221(int& val) {
    const int x = 3221;
    std::cout << 3221;
    for (int i = 0; i < 3221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3222(int& val) {
    func_in_3222(val);
    const int x = 3222;
    std::cout << 3222;
    for (int i = 0; i < 3222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3222(int& val) {
    const int x = 3222;
    std::cout << 3222;
    for (int i = 0; i < 3222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3223(int& val) {
    func_in_3223(val);
    const int x = 3223;
    std::cout << 3223;
    for (int i = 0; i < 3223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3223(int& val) {
    const int x = 3223;
    std::cout << 3223;
    for (int i = 0; i < 3223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3224(int& val) {
    func_in_3224(val);
    const int x = 3224;
    std::cout << 3224;
    for (int i = 0; i < 3224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3224(int& val) {
    const int x = 3224;
    std::cout << 3224;
    for (int i = 0; i < 3224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3225(int& val) {
    func_in_3225(val);
    const int x = 3225;
    std::cout << 3225;
    for (int i = 0; i < 3225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3225(int& val) {
    const int x = 3225;
    std::cout << 3225;
    for (int i = 0; i < 3225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3226(int& val) {
    func_in_3226(val);
    const int x = 3226;
    std::cout << 3226;
    for (int i = 0; i < 3226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3226(int& val) {
    const int x = 3226;
    std::cout << 3226;
    for (int i = 0; i < 3226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3227(int& val) {
    func_in_3227(val);
    const int x = 3227;
    std::cout << 3227;
    for (int i = 0; i < 3227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3227(int& val) {
    const int x = 3227;
    std::cout << 3227;
    for (int i = 0; i < 3227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3228(int& val) {
    func_in_3228(val);
    const int x = 3228;
    std::cout << 3228;
    for (int i = 0; i < 3228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3228(int& val) {
    const int x = 3228;
    std::cout << 3228;
    for (int i = 0; i < 3228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3229(int& val) {
    func_in_3229(val);
    const int x = 3229;
    std::cout << 3229;
    for (int i = 0; i < 3229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3229(int& val) {
    const int x = 3229;
    std::cout << 3229;
    for (int i = 0; i < 3229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3230(int& val) {
    func_in_3230(val);
    const int x = 3230;
    std::cout << 3230;
    for (int i = 0; i < 3230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3230(int& val) {
    const int x = 3230;
    std::cout << 3230;
    for (int i = 0; i < 3230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3231(int& val) {
    func_in_3231(val);
    const int x = 3231;
    std::cout << 3231;
    for (int i = 0; i < 3231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3231(int& val) {
    const int x = 3231;
    std::cout << 3231;
    for (int i = 0; i < 3231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3232(int& val) {
    func_in_3232(val);
    const int x = 3232;
    std::cout << 3232;
    for (int i = 0; i < 3232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3232(int& val) {
    const int x = 3232;
    std::cout << 3232;
    for (int i = 0; i < 3232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3233(int& val) {
    func_in_3233(val);
    const int x = 3233;
    std::cout << 3233;
    for (int i = 0; i < 3233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3233(int& val) {
    const int x = 3233;
    std::cout << 3233;
    for (int i = 0; i < 3233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3234(int& val) {
    func_in_3234(val);
    const int x = 3234;
    std::cout << 3234;
    for (int i = 0; i < 3234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3234(int& val) {
    const int x = 3234;
    std::cout << 3234;
    for (int i = 0; i < 3234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3235(int& val) {
    func_in_3235(val);
    const int x = 3235;
    std::cout << 3235;
    for (int i = 0; i < 3235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3235(int& val) {
    const int x = 3235;
    std::cout << 3235;
    for (int i = 0; i < 3235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3236(int& val) {
    func_in_3236(val);
    const int x = 3236;
    std::cout << 3236;
    for (int i = 0; i < 3236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3236(int& val) {
    const int x = 3236;
    std::cout << 3236;
    for (int i = 0; i < 3236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3237(int& val) {
    func_in_3237(val);
    const int x = 3237;
    std::cout << 3237;
    for (int i = 0; i < 3237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3237(int& val) {
    const int x = 3237;
    std::cout << 3237;
    for (int i = 0; i < 3237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3238(int& val) {
    func_in_3238(val);
    const int x = 3238;
    std::cout << 3238;
    for (int i = 0; i < 3238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3238(int& val) {
    const int x = 3238;
    std::cout << 3238;
    for (int i = 0; i < 3238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3239(int& val) {
    func_in_3239(val);
    const int x = 3239;
    std::cout << 3239;
    for (int i = 0; i < 3239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3239(int& val) {
    const int x = 3239;
    std::cout << 3239;
    for (int i = 0; i < 3239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3240(int& val) {
    func_in_3240(val);
    const int x = 3240;
    std::cout << 3240;
    for (int i = 0; i < 3240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3240(int& val) {
    const int x = 3240;
    std::cout << 3240;
    for (int i = 0; i < 3240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3241(int& val) {
    func_in_3241(val);
    const int x = 3241;
    std::cout << 3241;
    for (int i = 0; i < 3241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3241(int& val) {
    const int x = 3241;
    std::cout << 3241;
    for (int i = 0; i < 3241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3242(int& val) {
    func_in_3242(val);
    const int x = 3242;
    std::cout << 3242;
    for (int i = 0; i < 3242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3242(int& val) {
    const int x = 3242;
    std::cout << 3242;
    for (int i = 0; i < 3242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3243(int& val) {
    func_in_3243(val);
    const int x = 3243;
    std::cout << 3243;
    for (int i = 0; i < 3243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3243(int& val) {
    const int x = 3243;
    std::cout << 3243;
    for (int i = 0; i < 3243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3244(int& val) {
    func_in_3244(val);
    const int x = 3244;
    std::cout << 3244;
    for (int i = 0; i < 3244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3244(int& val) {
    const int x = 3244;
    std::cout << 3244;
    for (int i = 0; i < 3244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3245(int& val) {
    func_in_3245(val);
    const int x = 3245;
    std::cout << 3245;
    for (int i = 0; i < 3245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3245(int& val) {
    const int x = 3245;
    std::cout << 3245;
    for (int i = 0; i < 3245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3246(int& val) {
    func_in_3246(val);
    const int x = 3246;
    std::cout << 3246;
    for (int i = 0; i < 3246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3246(int& val) {
    const int x = 3246;
    std::cout << 3246;
    for (int i = 0; i < 3246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3247(int& val) {
    func_in_3247(val);
    const int x = 3247;
    std::cout << 3247;
    for (int i = 0; i < 3247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3247(int& val) {
    const int x = 3247;
    std::cout << 3247;
    for (int i = 0; i < 3247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3248(int& val) {
    func_in_3248(val);
    const int x = 3248;
    std::cout << 3248;
    for (int i = 0; i < 3248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3248(int& val) {
    const int x = 3248;
    std::cout << 3248;
    for (int i = 0; i < 3248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3249(int& val) {
    func_in_3249(val);
    const int x = 3249;
    std::cout << 3249;
    for (int i = 0; i < 3249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3249(int& val) {
    const int x = 3249;
    std::cout << 3249;
    for (int i = 0; i < 3249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3250(int& val) {
    func_in_3250(val);
    const int x = 3250;
    std::cout << 3250;
    for (int i = 0; i < 3250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3250(int& val) {
    const int x = 3250;
    std::cout << 3250;
    for (int i = 0; i < 3250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3251(int& val) {
    func_in_3251(val);
    const int x = 3251;
    std::cout << 3251;
    for (int i = 0; i < 3251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3251(int& val) {
    const int x = 3251;
    std::cout << 3251;
    for (int i = 0; i < 3251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3252(int& val) {
    func_in_3252(val);
    const int x = 3252;
    std::cout << 3252;
    for (int i = 0; i < 3252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3252(int& val) {
    const int x = 3252;
    std::cout << 3252;
    for (int i = 0; i < 3252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3253(int& val) {
    func_in_3253(val);
    const int x = 3253;
    std::cout << 3253;
    for (int i = 0; i < 3253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3253(int& val) {
    const int x = 3253;
    std::cout << 3253;
    for (int i = 0; i < 3253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3254(int& val) {
    func_in_3254(val);
    const int x = 3254;
    std::cout << 3254;
    for (int i = 0; i < 3254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3254(int& val) {
    const int x = 3254;
    std::cout << 3254;
    for (int i = 0; i < 3254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3255(int& val) {
    func_in_3255(val);
    const int x = 3255;
    std::cout << 3255;
    for (int i = 0; i < 3255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3255(int& val) {
    const int x = 3255;
    std::cout << 3255;
    for (int i = 0; i < 3255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3256(int& val) {
    func_in_3256(val);
    const int x = 3256;
    std::cout << 3256;
    for (int i = 0; i < 3256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3256(int& val) {
    const int x = 3256;
    std::cout << 3256;
    for (int i = 0; i < 3256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3257(int& val) {
    func_in_3257(val);
    const int x = 3257;
    std::cout << 3257;
    for (int i = 0; i < 3257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3257(int& val) {
    const int x = 3257;
    std::cout << 3257;
    for (int i = 0; i < 3257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3258(int& val) {
    func_in_3258(val);
    const int x = 3258;
    std::cout << 3258;
    for (int i = 0; i < 3258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3258(int& val) {
    const int x = 3258;
    std::cout << 3258;
    for (int i = 0; i < 3258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3259(int& val) {
    func_in_3259(val);
    const int x = 3259;
    std::cout << 3259;
    for (int i = 0; i < 3259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3259(int& val) {
    const int x = 3259;
    std::cout << 3259;
    for (int i = 0; i < 3259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3260(int& val) {
    func_in_3260(val);
    const int x = 3260;
    std::cout << 3260;
    for (int i = 0; i < 3260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3260(int& val) {
    const int x = 3260;
    std::cout << 3260;
    for (int i = 0; i < 3260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3261(int& val) {
    func_in_3261(val);
    const int x = 3261;
    std::cout << 3261;
    for (int i = 0; i < 3261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3261(int& val) {
    const int x = 3261;
    std::cout << 3261;
    for (int i = 0; i < 3261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3262(int& val) {
    func_in_3262(val);
    const int x = 3262;
    std::cout << 3262;
    for (int i = 0; i < 3262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3262(int& val) {
    const int x = 3262;
    std::cout << 3262;
    for (int i = 0; i < 3262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3263(int& val) {
    func_in_3263(val);
    const int x = 3263;
    std::cout << 3263;
    for (int i = 0; i < 3263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3263(int& val) {
    const int x = 3263;
    std::cout << 3263;
    for (int i = 0; i < 3263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3264(int& val) {
    func_in_3264(val);
    const int x = 3264;
    std::cout << 3264;
    for (int i = 0; i < 3264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3264(int& val) {
    const int x = 3264;
    std::cout << 3264;
    for (int i = 0; i < 3264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3265(int& val) {
    func_in_3265(val);
    const int x = 3265;
    std::cout << 3265;
    for (int i = 0; i < 3265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3265(int& val) {
    const int x = 3265;
    std::cout << 3265;
    for (int i = 0; i < 3265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3266(int& val) {
    func_in_3266(val);
    const int x = 3266;
    std::cout << 3266;
    for (int i = 0; i < 3266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3266(int& val) {
    const int x = 3266;
    std::cout << 3266;
    for (int i = 0; i < 3266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3267(int& val) {
    func_in_3267(val);
    const int x = 3267;
    std::cout << 3267;
    for (int i = 0; i < 3267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3267(int& val) {
    const int x = 3267;
    std::cout << 3267;
    for (int i = 0; i < 3267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3268(int& val) {
    func_in_3268(val);
    const int x = 3268;
    std::cout << 3268;
    for (int i = 0; i < 3268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3268(int& val) {
    const int x = 3268;
    std::cout << 3268;
    for (int i = 0; i < 3268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3269(int& val) {
    func_in_3269(val);
    const int x = 3269;
    std::cout << 3269;
    for (int i = 0; i < 3269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3269(int& val) {
    const int x = 3269;
    std::cout << 3269;
    for (int i = 0; i < 3269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3270(int& val) {
    func_in_3270(val);
    const int x = 3270;
    std::cout << 3270;
    for (int i = 0; i < 3270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3270(int& val) {
    const int x = 3270;
    std::cout << 3270;
    for (int i = 0; i < 3270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3271(int& val) {
    func_in_3271(val);
    const int x = 3271;
    std::cout << 3271;
    for (int i = 0; i < 3271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3271(int& val) {
    const int x = 3271;
    std::cout << 3271;
    for (int i = 0; i < 3271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3272(int& val) {
    func_in_3272(val);
    const int x = 3272;
    std::cout << 3272;
    for (int i = 0; i < 3272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3272(int& val) {
    const int x = 3272;
    std::cout << 3272;
    for (int i = 0; i < 3272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3273(int& val) {
    func_in_3273(val);
    const int x = 3273;
    std::cout << 3273;
    for (int i = 0; i < 3273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3273(int& val) {
    const int x = 3273;
    std::cout << 3273;
    for (int i = 0; i < 3273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3274(int& val) {
    func_in_3274(val);
    const int x = 3274;
    std::cout << 3274;
    for (int i = 0; i < 3274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3274(int& val) {
    const int x = 3274;
    std::cout << 3274;
    for (int i = 0; i < 3274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3275(int& val) {
    func_in_3275(val);
    const int x = 3275;
    std::cout << 3275;
    for (int i = 0; i < 3275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3275(int& val) {
    const int x = 3275;
    std::cout << 3275;
    for (int i = 0; i < 3275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3276(int& val) {
    func_in_3276(val);
    const int x = 3276;
    std::cout << 3276;
    for (int i = 0; i < 3276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3276(int& val) {
    const int x = 3276;
    std::cout << 3276;
    for (int i = 0; i < 3276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3277(int& val) {
    func_in_3277(val);
    const int x = 3277;
    std::cout << 3277;
    for (int i = 0; i < 3277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3277(int& val) {
    const int x = 3277;
    std::cout << 3277;
    for (int i = 0; i < 3277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3278(int& val) {
    func_in_3278(val);
    const int x = 3278;
    std::cout << 3278;
    for (int i = 0; i < 3278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3278(int& val) {
    const int x = 3278;
    std::cout << 3278;
    for (int i = 0; i < 3278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3279(int& val) {
    func_in_3279(val);
    const int x = 3279;
    std::cout << 3279;
    for (int i = 0; i < 3279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3279(int& val) {
    const int x = 3279;
    std::cout << 3279;
    for (int i = 0; i < 3279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3280(int& val) {
    func_in_3280(val);
    const int x = 3280;
    std::cout << 3280;
    for (int i = 0; i < 3280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3280(int& val) {
    const int x = 3280;
    std::cout << 3280;
    for (int i = 0; i < 3280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3281(int& val) {
    func_in_3281(val);
    const int x = 3281;
    std::cout << 3281;
    for (int i = 0; i < 3281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3281(int& val) {
    const int x = 3281;
    std::cout << 3281;
    for (int i = 0; i < 3281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3282(int& val) {
    func_in_3282(val);
    const int x = 3282;
    std::cout << 3282;
    for (int i = 0; i < 3282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3282(int& val) {
    const int x = 3282;
    std::cout << 3282;
    for (int i = 0; i < 3282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3283(int& val) {
    func_in_3283(val);
    const int x = 3283;
    std::cout << 3283;
    for (int i = 0; i < 3283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3283(int& val) {
    const int x = 3283;
    std::cout << 3283;
    for (int i = 0; i < 3283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3284(int& val) {
    func_in_3284(val);
    const int x = 3284;
    std::cout << 3284;
    for (int i = 0; i < 3284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3284(int& val) {
    const int x = 3284;
    std::cout << 3284;
    for (int i = 0; i < 3284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3285(int& val) {
    func_in_3285(val);
    const int x = 3285;
    std::cout << 3285;
    for (int i = 0; i < 3285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3285(int& val) {
    const int x = 3285;
    std::cout << 3285;
    for (int i = 0; i < 3285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3286(int& val) {
    func_in_3286(val);
    const int x = 3286;
    std::cout << 3286;
    for (int i = 0; i < 3286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3286(int& val) {
    const int x = 3286;
    std::cout << 3286;
    for (int i = 0; i < 3286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3287(int& val) {
    func_in_3287(val);
    const int x = 3287;
    std::cout << 3287;
    for (int i = 0; i < 3287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3287(int& val) {
    const int x = 3287;
    std::cout << 3287;
    for (int i = 0; i < 3287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3288(int& val) {
    func_in_3288(val);
    const int x = 3288;
    std::cout << 3288;
    for (int i = 0; i < 3288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3288(int& val) {
    const int x = 3288;
    std::cout << 3288;
    for (int i = 0; i < 3288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3289(int& val) {
    func_in_3289(val);
    const int x = 3289;
    std::cout << 3289;
    for (int i = 0; i < 3289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3289(int& val) {
    const int x = 3289;
    std::cout << 3289;
    for (int i = 0; i < 3289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3290(int& val) {
    func_in_3290(val);
    const int x = 3290;
    std::cout << 3290;
    for (int i = 0; i < 3290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3290(int& val) {
    const int x = 3290;
    std::cout << 3290;
    for (int i = 0; i < 3290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3291(int& val) {
    func_in_3291(val);
    const int x = 3291;
    std::cout << 3291;
    for (int i = 0; i < 3291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3291(int& val) {
    const int x = 3291;
    std::cout << 3291;
    for (int i = 0; i < 3291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3292(int& val) {
    func_in_3292(val);
    const int x = 3292;
    std::cout << 3292;
    for (int i = 0; i < 3292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3292(int& val) {
    const int x = 3292;
    std::cout << 3292;
    for (int i = 0; i < 3292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3293(int& val) {
    func_in_3293(val);
    const int x = 3293;
    std::cout << 3293;
    for (int i = 0; i < 3293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3293(int& val) {
    const int x = 3293;
    std::cout << 3293;
    for (int i = 0; i < 3293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3294(int& val) {
    func_in_3294(val);
    const int x = 3294;
    std::cout << 3294;
    for (int i = 0; i < 3294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3294(int& val) {
    const int x = 3294;
    std::cout << 3294;
    for (int i = 0; i < 3294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3295(int& val) {
    func_in_3295(val);
    const int x = 3295;
    std::cout << 3295;
    for (int i = 0; i < 3295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3295(int& val) {
    const int x = 3295;
    std::cout << 3295;
    for (int i = 0; i < 3295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3296(int& val) {
    func_in_3296(val);
    const int x = 3296;
    std::cout << 3296;
    for (int i = 0; i < 3296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3296(int& val) {
    const int x = 3296;
    std::cout << 3296;
    for (int i = 0; i < 3296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3297(int& val) {
    func_in_3297(val);
    const int x = 3297;
    std::cout << 3297;
    for (int i = 0; i < 3297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3297(int& val) {
    const int x = 3297;
    std::cout << 3297;
    for (int i = 0; i < 3297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3298(int& val) {
    func_in_3298(val);
    const int x = 3298;
    std::cout << 3298;
    for (int i = 0; i < 3298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3298(int& val) {
    const int x = 3298;
    std::cout << 3298;
    for (int i = 0; i < 3298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3299(int& val) {
    func_in_3299(val);
    const int x = 3299;
    std::cout << 3299;
    for (int i = 0; i < 3299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3299(int& val) {
    const int x = 3299;
    std::cout << 3299;
    for (int i = 0; i < 3299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3300(int& val) {
    func_in_3300(val);
    const int x = 3300;
    std::cout << 3300;
    for (int i = 0; i < 3300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3300(int& val) {
    const int x = 3300;
    std::cout << 3300;
    for (int i = 0; i < 3300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3301(int& val) {
    func_in_3301(val);
    const int x = 3301;
    std::cout << 3301;
    for (int i = 0; i < 3301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3301(int& val) {
    const int x = 3301;
    std::cout << 3301;
    for (int i = 0; i < 3301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3302(int& val) {
    func_in_3302(val);
    const int x = 3302;
    std::cout << 3302;
    for (int i = 0; i < 3302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3302(int& val) {
    const int x = 3302;
    std::cout << 3302;
    for (int i = 0; i < 3302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3303(int& val) {
    func_in_3303(val);
    const int x = 3303;
    std::cout << 3303;
    for (int i = 0; i < 3303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3303(int& val) {
    const int x = 3303;
    std::cout << 3303;
    for (int i = 0; i < 3303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3304(int& val) {
    func_in_3304(val);
    const int x = 3304;
    std::cout << 3304;
    for (int i = 0; i < 3304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3304(int& val) {
    const int x = 3304;
    std::cout << 3304;
    for (int i = 0; i < 3304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3305(int& val) {
    func_in_3305(val);
    const int x = 3305;
    std::cout << 3305;
    for (int i = 0; i < 3305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3305(int& val) {
    const int x = 3305;
    std::cout << 3305;
    for (int i = 0; i < 3305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3306(int& val) {
    func_in_3306(val);
    const int x = 3306;
    std::cout << 3306;
    for (int i = 0; i < 3306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3306(int& val) {
    const int x = 3306;
    std::cout << 3306;
    for (int i = 0; i < 3306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3307(int& val) {
    func_in_3307(val);
    const int x = 3307;
    std::cout << 3307;
    for (int i = 0; i < 3307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3307(int& val) {
    const int x = 3307;
    std::cout << 3307;
    for (int i = 0; i < 3307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3308(int& val) {
    func_in_3308(val);
    const int x = 3308;
    std::cout << 3308;
    for (int i = 0; i < 3308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3308(int& val) {
    const int x = 3308;
    std::cout << 3308;
    for (int i = 0; i < 3308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3309(int& val) {
    func_in_3309(val);
    const int x = 3309;
    std::cout << 3309;
    for (int i = 0; i < 3309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3309(int& val) {
    const int x = 3309;
    std::cout << 3309;
    for (int i = 0; i < 3309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3310(int& val) {
    func_in_3310(val);
    const int x = 3310;
    std::cout << 3310;
    for (int i = 0; i < 3310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3310(int& val) {
    const int x = 3310;
    std::cout << 3310;
    for (int i = 0; i < 3310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3311(int& val) {
    func_in_3311(val);
    const int x = 3311;
    std::cout << 3311;
    for (int i = 0; i < 3311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3311(int& val) {
    const int x = 3311;
    std::cout << 3311;
    for (int i = 0; i < 3311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3312(int& val) {
    func_in_3312(val);
    const int x = 3312;
    std::cout << 3312;
    for (int i = 0; i < 3312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3312(int& val) {
    const int x = 3312;
    std::cout << 3312;
    for (int i = 0; i < 3312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3313(int& val) {
    func_in_3313(val);
    const int x = 3313;
    std::cout << 3313;
    for (int i = 0; i < 3313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3313(int& val) {
    const int x = 3313;
    std::cout << 3313;
    for (int i = 0; i < 3313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3314(int& val) {
    func_in_3314(val);
    const int x = 3314;
    std::cout << 3314;
    for (int i = 0; i < 3314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3314(int& val) {
    const int x = 3314;
    std::cout << 3314;
    for (int i = 0; i < 3314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3315(int& val) {
    func_in_3315(val);
    const int x = 3315;
    std::cout << 3315;
    for (int i = 0; i < 3315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3315(int& val) {
    const int x = 3315;
    std::cout << 3315;
    for (int i = 0; i < 3315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3316(int& val) {
    func_in_3316(val);
    const int x = 3316;
    std::cout << 3316;
    for (int i = 0; i < 3316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3316(int& val) {
    const int x = 3316;
    std::cout << 3316;
    for (int i = 0; i < 3316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3317(int& val) {
    func_in_3317(val);
    const int x = 3317;
    std::cout << 3317;
    for (int i = 0; i < 3317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3317(int& val) {
    const int x = 3317;
    std::cout << 3317;
    for (int i = 0; i < 3317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3318(int& val) {
    func_in_3318(val);
    const int x = 3318;
    std::cout << 3318;
    for (int i = 0; i < 3318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3318(int& val) {
    const int x = 3318;
    std::cout << 3318;
    for (int i = 0; i < 3318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3319(int& val) {
    func_in_3319(val);
    const int x = 3319;
    std::cout << 3319;
    for (int i = 0; i < 3319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3319(int& val) {
    const int x = 3319;
    std::cout << 3319;
    for (int i = 0; i < 3319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3320(int& val) {
    func_in_3320(val);
    const int x = 3320;
    std::cout << 3320;
    for (int i = 0; i < 3320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3320(int& val) {
    const int x = 3320;
    std::cout << 3320;
    for (int i = 0; i < 3320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3321(int& val) {
    func_in_3321(val);
    const int x = 3321;
    std::cout << 3321;
    for (int i = 0; i < 3321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3321(int& val) {
    const int x = 3321;
    std::cout << 3321;
    for (int i = 0; i < 3321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3322(int& val) {
    func_in_3322(val);
    const int x = 3322;
    std::cout << 3322;
    for (int i = 0; i < 3322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3322(int& val) {
    const int x = 3322;
    std::cout << 3322;
    for (int i = 0; i < 3322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3323(int& val) {
    func_in_3323(val);
    const int x = 3323;
    std::cout << 3323;
    for (int i = 0; i < 3323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3323(int& val) {
    const int x = 3323;
    std::cout << 3323;
    for (int i = 0; i < 3323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3324(int& val) {
    func_in_3324(val);
    const int x = 3324;
    std::cout << 3324;
    for (int i = 0; i < 3324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3324(int& val) {
    const int x = 3324;
    std::cout << 3324;
    for (int i = 0; i < 3324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3325(int& val) {
    func_in_3325(val);
    const int x = 3325;
    std::cout << 3325;
    for (int i = 0; i < 3325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3325(int& val) {
    const int x = 3325;
    std::cout << 3325;
    for (int i = 0; i < 3325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3326(int& val) {
    func_in_3326(val);
    const int x = 3326;
    std::cout << 3326;
    for (int i = 0; i < 3326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3326(int& val) {
    const int x = 3326;
    std::cout << 3326;
    for (int i = 0; i < 3326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3327(int& val) {
    func_in_3327(val);
    const int x = 3327;
    std::cout << 3327;
    for (int i = 0; i < 3327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3327(int& val) {
    const int x = 3327;
    std::cout << 3327;
    for (int i = 0; i < 3327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3328(int& val) {
    func_in_3328(val);
    const int x = 3328;
    std::cout << 3328;
    for (int i = 0; i < 3328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3328(int& val) {
    const int x = 3328;
    std::cout << 3328;
    for (int i = 0; i < 3328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3329(int& val) {
    func_in_3329(val);
    const int x = 3329;
    std::cout << 3329;
    for (int i = 0; i < 3329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3329(int& val) {
    const int x = 3329;
    std::cout << 3329;
    for (int i = 0; i < 3329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3330(int& val) {
    func_in_3330(val);
    const int x = 3330;
    std::cout << 3330;
    for (int i = 0; i < 3330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3330(int& val) {
    const int x = 3330;
    std::cout << 3330;
    for (int i = 0; i < 3330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3331(int& val) {
    func_in_3331(val);
    const int x = 3331;
    std::cout << 3331;
    for (int i = 0; i < 3331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3331(int& val) {
    const int x = 3331;
    std::cout << 3331;
    for (int i = 0; i < 3331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3332(int& val) {
    func_in_3332(val);
    const int x = 3332;
    std::cout << 3332;
    for (int i = 0; i < 3332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3332(int& val) {
    const int x = 3332;
    std::cout << 3332;
    for (int i = 0; i < 3332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3333(int& val) {
    func_in_3333(val);
    const int x = 3333;
    std::cout << 3333;
    for (int i = 0; i < 3333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3333(int& val) {
    const int x = 3333;
    std::cout << 3333;
    for (int i = 0; i < 3333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3334(int& val) {
    func_in_3334(val);
    const int x = 3334;
    std::cout << 3334;
    for (int i = 0; i < 3334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3334(int& val) {
    const int x = 3334;
    std::cout << 3334;
    for (int i = 0; i < 3334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3335(int& val) {
    func_in_3335(val);
    const int x = 3335;
    std::cout << 3335;
    for (int i = 0; i < 3335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3335(int& val) {
    const int x = 3335;
    std::cout << 3335;
    for (int i = 0; i < 3335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3336(int& val) {
    func_in_3336(val);
    const int x = 3336;
    std::cout << 3336;
    for (int i = 0; i < 3336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3336(int& val) {
    const int x = 3336;
    std::cout << 3336;
    for (int i = 0; i < 3336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3337(int& val) {
    func_in_3337(val);
    const int x = 3337;
    std::cout << 3337;
    for (int i = 0; i < 3337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3337(int& val) {
    const int x = 3337;
    std::cout << 3337;
    for (int i = 0; i < 3337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3338(int& val) {
    func_in_3338(val);
    const int x = 3338;
    std::cout << 3338;
    for (int i = 0; i < 3338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3338(int& val) {
    const int x = 3338;
    std::cout << 3338;
    for (int i = 0; i < 3338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3339(int& val) {
    func_in_3339(val);
    const int x = 3339;
    std::cout << 3339;
    for (int i = 0; i < 3339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3339(int& val) {
    const int x = 3339;
    std::cout << 3339;
    for (int i = 0; i < 3339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3340(int& val) {
    func_in_3340(val);
    const int x = 3340;
    std::cout << 3340;
    for (int i = 0; i < 3340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3340(int& val) {
    const int x = 3340;
    std::cout << 3340;
    for (int i = 0; i < 3340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3341(int& val) {
    func_in_3341(val);
    const int x = 3341;
    std::cout << 3341;
    for (int i = 0; i < 3341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3341(int& val) {
    const int x = 3341;
    std::cout << 3341;
    for (int i = 0; i < 3341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3342(int& val) {
    func_in_3342(val);
    const int x = 3342;
    std::cout << 3342;
    for (int i = 0; i < 3342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3342(int& val) {
    const int x = 3342;
    std::cout << 3342;
    for (int i = 0; i < 3342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3343(int& val) {
    func_in_3343(val);
    const int x = 3343;
    std::cout << 3343;
    for (int i = 0; i < 3343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3343(int& val) {
    const int x = 3343;
    std::cout << 3343;
    for (int i = 0; i < 3343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3344(int& val) {
    func_in_3344(val);
    const int x = 3344;
    std::cout << 3344;
    for (int i = 0; i < 3344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3344(int& val) {
    const int x = 3344;
    std::cout << 3344;
    for (int i = 0; i < 3344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3345(int& val) {
    func_in_3345(val);
    const int x = 3345;
    std::cout << 3345;
    for (int i = 0; i < 3345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3345(int& val) {
    const int x = 3345;
    std::cout << 3345;
    for (int i = 0; i < 3345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3346(int& val) {
    func_in_3346(val);
    const int x = 3346;
    std::cout << 3346;
    for (int i = 0; i < 3346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3346(int& val) {
    const int x = 3346;
    std::cout << 3346;
    for (int i = 0; i < 3346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3347(int& val) {
    func_in_3347(val);
    const int x = 3347;
    std::cout << 3347;
    for (int i = 0; i < 3347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3347(int& val) {
    const int x = 3347;
    std::cout << 3347;
    for (int i = 0; i < 3347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3348(int& val) {
    func_in_3348(val);
    const int x = 3348;
    std::cout << 3348;
    for (int i = 0; i < 3348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3348(int& val) {
    const int x = 3348;
    std::cout << 3348;
    for (int i = 0; i < 3348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3349(int& val) {
    func_in_3349(val);
    const int x = 3349;
    std::cout << 3349;
    for (int i = 0; i < 3349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3349(int& val) {
    const int x = 3349;
    std::cout << 3349;
    for (int i = 0; i < 3349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3350(int& val) {
    func_in_3350(val);
    const int x = 3350;
    std::cout << 3350;
    for (int i = 0; i < 3350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3350(int& val) {
    const int x = 3350;
    std::cout << 3350;
    for (int i = 0; i < 3350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3351(int& val) {
    func_in_3351(val);
    const int x = 3351;
    std::cout << 3351;
    for (int i = 0; i < 3351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3351(int& val) {
    const int x = 3351;
    std::cout << 3351;
    for (int i = 0; i < 3351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3352(int& val) {
    func_in_3352(val);
    const int x = 3352;
    std::cout << 3352;
    for (int i = 0; i < 3352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3352(int& val) {
    const int x = 3352;
    std::cout << 3352;
    for (int i = 0; i < 3352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3353(int& val) {
    func_in_3353(val);
    const int x = 3353;
    std::cout << 3353;
    for (int i = 0; i < 3353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3353(int& val) {
    const int x = 3353;
    std::cout << 3353;
    for (int i = 0; i < 3353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3354(int& val) {
    func_in_3354(val);
    const int x = 3354;
    std::cout << 3354;
    for (int i = 0; i < 3354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3354(int& val) {
    const int x = 3354;
    std::cout << 3354;
    for (int i = 0; i < 3354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3355(int& val) {
    func_in_3355(val);
    const int x = 3355;
    std::cout << 3355;
    for (int i = 0; i < 3355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3355(int& val) {
    const int x = 3355;
    std::cout << 3355;
    for (int i = 0; i < 3355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3356(int& val) {
    func_in_3356(val);
    const int x = 3356;
    std::cout << 3356;
    for (int i = 0; i < 3356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3356(int& val) {
    const int x = 3356;
    std::cout << 3356;
    for (int i = 0; i < 3356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3357(int& val) {
    func_in_3357(val);
    const int x = 3357;
    std::cout << 3357;
    for (int i = 0; i < 3357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3357(int& val) {
    const int x = 3357;
    std::cout << 3357;
    for (int i = 0; i < 3357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3358(int& val) {
    func_in_3358(val);
    const int x = 3358;
    std::cout << 3358;
    for (int i = 0; i < 3358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3358(int& val) {
    const int x = 3358;
    std::cout << 3358;
    for (int i = 0; i < 3358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3359(int& val) {
    func_in_3359(val);
    const int x = 3359;
    std::cout << 3359;
    for (int i = 0; i < 3359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3359(int& val) {
    const int x = 3359;
    std::cout << 3359;
    for (int i = 0; i < 3359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3360(int& val) {
    func_in_3360(val);
    const int x = 3360;
    std::cout << 3360;
    for (int i = 0; i < 3360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3360(int& val) {
    const int x = 3360;
    std::cout << 3360;
    for (int i = 0; i < 3360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3361(int& val) {
    func_in_3361(val);
    const int x = 3361;
    std::cout << 3361;
    for (int i = 0; i < 3361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3361(int& val) {
    const int x = 3361;
    std::cout << 3361;
    for (int i = 0; i < 3361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3362(int& val) {
    func_in_3362(val);
    const int x = 3362;
    std::cout << 3362;
    for (int i = 0; i < 3362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3362(int& val) {
    const int x = 3362;
    std::cout << 3362;
    for (int i = 0; i < 3362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3363(int& val) {
    func_in_3363(val);
    const int x = 3363;
    std::cout << 3363;
    for (int i = 0; i < 3363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3363(int& val) {
    const int x = 3363;
    std::cout << 3363;
    for (int i = 0; i < 3363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3364(int& val) {
    func_in_3364(val);
    const int x = 3364;
    std::cout << 3364;
    for (int i = 0; i < 3364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3364(int& val) {
    const int x = 3364;
    std::cout << 3364;
    for (int i = 0; i < 3364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3365(int& val) {
    func_in_3365(val);
    const int x = 3365;
    std::cout << 3365;
    for (int i = 0; i < 3365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3365(int& val) {
    const int x = 3365;
    std::cout << 3365;
    for (int i = 0; i < 3365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3366(int& val) {
    func_in_3366(val);
    const int x = 3366;
    std::cout << 3366;
    for (int i = 0; i < 3366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3366(int& val) {
    const int x = 3366;
    std::cout << 3366;
    for (int i = 0; i < 3366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3367(int& val) {
    func_in_3367(val);
    const int x = 3367;
    std::cout << 3367;
    for (int i = 0; i < 3367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3367(int& val) {
    const int x = 3367;
    std::cout << 3367;
    for (int i = 0; i < 3367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3368(int& val) {
    func_in_3368(val);
    const int x = 3368;
    std::cout << 3368;
    for (int i = 0; i < 3368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3368(int& val) {
    const int x = 3368;
    std::cout << 3368;
    for (int i = 0; i < 3368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3369(int& val) {
    func_in_3369(val);
    const int x = 3369;
    std::cout << 3369;
    for (int i = 0; i < 3369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3369(int& val) {
    const int x = 3369;
    std::cout << 3369;
    for (int i = 0; i < 3369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3370(int& val) {
    func_in_3370(val);
    const int x = 3370;
    std::cout << 3370;
    for (int i = 0; i < 3370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3370(int& val) {
    const int x = 3370;
    std::cout << 3370;
    for (int i = 0; i < 3370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3371(int& val) {
    func_in_3371(val);
    const int x = 3371;
    std::cout << 3371;
    for (int i = 0; i < 3371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3371(int& val) {
    const int x = 3371;
    std::cout << 3371;
    for (int i = 0; i < 3371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3372(int& val) {
    func_in_3372(val);
    const int x = 3372;
    std::cout << 3372;
    for (int i = 0; i < 3372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3372(int& val) {
    const int x = 3372;
    std::cout << 3372;
    for (int i = 0; i < 3372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3373(int& val) {
    func_in_3373(val);
    const int x = 3373;
    std::cout << 3373;
    for (int i = 0; i < 3373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3373(int& val) {
    const int x = 3373;
    std::cout << 3373;
    for (int i = 0; i < 3373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3374(int& val) {
    func_in_3374(val);
    const int x = 3374;
    std::cout << 3374;
    for (int i = 0; i < 3374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3374(int& val) {
    const int x = 3374;
    std::cout << 3374;
    for (int i = 0; i < 3374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3375(int& val) {
    func_in_3375(val);
    const int x = 3375;
    std::cout << 3375;
    for (int i = 0; i < 3375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3375(int& val) {
    const int x = 3375;
    std::cout << 3375;
    for (int i = 0; i < 3375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3376(int& val) {
    func_in_3376(val);
    const int x = 3376;
    std::cout << 3376;
    for (int i = 0; i < 3376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3376(int& val) {
    const int x = 3376;
    std::cout << 3376;
    for (int i = 0; i < 3376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3377(int& val) {
    func_in_3377(val);
    const int x = 3377;
    std::cout << 3377;
    for (int i = 0; i < 3377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3377(int& val) {
    const int x = 3377;
    std::cout << 3377;
    for (int i = 0; i < 3377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3378(int& val) {
    func_in_3378(val);
    const int x = 3378;
    std::cout << 3378;
    for (int i = 0; i < 3378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3378(int& val) {
    const int x = 3378;
    std::cout << 3378;
    for (int i = 0; i < 3378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3379(int& val) {
    func_in_3379(val);
    const int x = 3379;
    std::cout << 3379;
    for (int i = 0; i < 3379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3379(int& val) {
    const int x = 3379;
    std::cout << 3379;
    for (int i = 0; i < 3379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3380(int& val) {
    func_in_3380(val);
    const int x = 3380;
    std::cout << 3380;
    for (int i = 0; i < 3380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3380(int& val) {
    const int x = 3380;
    std::cout << 3380;
    for (int i = 0; i < 3380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3381(int& val) {
    func_in_3381(val);
    const int x = 3381;
    std::cout << 3381;
    for (int i = 0; i < 3381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3381(int& val) {
    const int x = 3381;
    std::cout << 3381;
    for (int i = 0; i < 3381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3382(int& val) {
    func_in_3382(val);
    const int x = 3382;
    std::cout << 3382;
    for (int i = 0; i < 3382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3382(int& val) {
    const int x = 3382;
    std::cout << 3382;
    for (int i = 0; i < 3382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3383(int& val) {
    func_in_3383(val);
    const int x = 3383;
    std::cout << 3383;
    for (int i = 0; i < 3383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3383(int& val) {
    const int x = 3383;
    std::cout << 3383;
    for (int i = 0; i < 3383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3384(int& val) {
    func_in_3384(val);
    const int x = 3384;
    std::cout << 3384;
    for (int i = 0; i < 3384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3384(int& val) {
    const int x = 3384;
    std::cout << 3384;
    for (int i = 0; i < 3384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3385(int& val) {
    func_in_3385(val);
    const int x = 3385;
    std::cout << 3385;
    for (int i = 0; i < 3385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3385(int& val) {
    const int x = 3385;
    std::cout << 3385;
    for (int i = 0; i < 3385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3386(int& val) {
    func_in_3386(val);
    const int x = 3386;
    std::cout << 3386;
    for (int i = 0; i < 3386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3386(int& val) {
    const int x = 3386;
    std::cout << 3386;
    for (int i = 0; i < 3386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3387(int& val) {
    func_in_3387(val);
    const int x = 3387;
    std::cout << 3387;
    for (int i = 0; i < 3387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3387(int& val) {
    const int x = 3387;
    std::cout << 3387;
    for (int i = 0; i < 3387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3388(int& val) {
    func_in_3388(val);
    const int x = 3388;
    std::cout << 3388;
    for (int i = 0; i < 3388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3388(int& val) {
    const int x = 3388;
    std::cout << 3388;
    for (int i = 0; i < 3388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3389(int& val) {
    func_in_3389(val);
    const int x = 3389;
    std::cout << 3389;
    for (int i = 0; i < 3389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3389(int& val) {
    const int x = 3389;
    std::cout << 3389;
    for (int i = 0; i < 3389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3390(int& val) {
    func_in_3390(val);
    const int x = 3390;
    std::cout << 3390;
    for (int i = 0; i < 3390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3390(int& val) {
    const int x = 3390;
    std::cout << 3390;
    for (int i = 0; i < 3390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3391(int& val) {
    func_in_3391(val);
    const int x = 3391;
    std::cout << 3391;
    for (int i = 0; i < 3391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3391(int& val) {
    const int x = 3391;
    std::cout << 3391;
    for (int i = 0; i < 3391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3392(int& val) {
    func_in_3392(val);
    const int x = 3392;
    std::cout << 3392;
    for (int i = 0; i < 3392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3392(int& val) {
    const int x = 3392;
    std::cout << 3392;
    for (int i = 0; i < 3392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3393(int& val) {
    func_in_3393(val);
    const int x = 3393;
    std::cout << 3393;
    for (int i = 0; i < 3393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3393(int& val) {
    const int x = 3393;
    std::cout << 3393;
    for (int i = 0; i < 3393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3394(int& val) {
    func_in_3394(val);
    const int x = 3394;
    std::cout << 3394;
    for (int i = 0; i < 3394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3394(int& val) {
    const int x = 3394;
    std::cout << 3394;
    for (int i = 0; i < 3394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3395(int& val) {
    func_in_3395(val);
    const int x = 3395;
    std::cout << 3395;
    for (int i = 0; i < 3395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3395(int& val) {
    const int x = 3395;
    std::cout << 3395;
    for (int i = 0; i < 3395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3396(int& val) {
    func_in_3396(val);
    const int x = 3396;
    std::cout << 3396;
    for (int i = 0; i < 3396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3396(int& val) {
    const int x = 3396;
    std::cout << 3396;
    for (int i = 0; i < 3396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3397(int& val) {
    func_in_3397(val);
    const int x = 3397;
    std::cout << 3397;
    for (int i = 0; i < 3397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3397(int& val) {
    const int x = 3397;
    std::cout << 3397;
    for (int i = 0; i < 3397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3398(int& val) {
    func_in_3398(val);
    const int x = 3398;
    std::cout << 3398;
    for (int i = 0; i < 3398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3398(int& val) {
    const int x = 3398;
    std::cout << 3398;
    for (int i = 0; i < 3398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3399(int& val) {
    func_in_3399(val);
    const int x = 3399;
    std::cout << 3399;
    for (int i = 0; i < 3399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3399(int& val) {
    const int x = 3399;
    std::cout << 3399;
    for (int i = 0; i < 3399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3400(int& val) {
    func_in_3400(val);
    const int x = 3400;
    std::cout << 3400;
    for (int i = 0; i < 3400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3400(int& val) {
    const int x = 3400;
    std::cout << 3400;
    for (int i = 0; i < 3400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3401(int& val) {
    func_in_3401(val);
    const int x = 3401;
    std::cout << 3401;
    for (int i = 0; i < 3401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3401(int& val) {
    const int x = 3401;
    std::cout << 3401;
    for (int i = 0; i < 3401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3402(int& val) {
    func_in_3402(val);
    const int x = 3402;
    std::cout << 3402;
    for (int i = 0; i < 3402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3402(int& val) {
    const int x = 3402;
    std::cout << 3402;
    for (int i = 0; i < 3402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3403(int& val) {
    func_in_3403(val);
    const int x = 3403;
    std::cout << 3403;
    for (int i = 0; i < 3403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3403(int& val) {
    const int x = 3403;
    std::cout << 3403;
    for (int i = 0; i < 3403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3404(int& val) {
    func_in_3404(val);
    const int x = 3404;
    std::cout << 3404;
    for (int i = 0; i < 3404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3404(int& val) {
    const int x = 3404;
    std::cout << 3404;
    for (int i = 0; i < 3404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3405(int& val) {
    func_in_3405(val);
    const int x = 3405;
    std::cout << 3405;
    for (int i = 0; i < 3405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3405(int& val) {
    const int x = 3405;
    std::cout << 3405;
    for (int i = 0; i < 3405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3406(int& val) {
    func_in_3406(val);
    const int x = 3406;
    std::cout << 3406;
    for (int i = 0; i < 3406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3406(int& val) {
    const int x = 3406;
    std::cout << 3406;
    for (int i = 0; i < 3406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3407(int& val) {
    func_in_3407(val);
    const int x = 3407;
    std::cout << 3407;
    for (int i = 0; i < 3407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3407(int& val) {
    const int x = 3407;
    std::cout << 3407;
    for (int i = 0; i < 3407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3408(int& val) {
    func_in_3408(val);
    const int x = 3408;
    std::cout << 3408;
    for (int i = 0; i < 3408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3408(int& val) {
    const int x = 3408;
    std::cout << 3408;
    for (int i = 0; i < 3408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3409(int& val) {
    func_in_3409(val);
    const int x = 3409;
    std::cout << 3409;
    for (int i = 0; i < 3409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3409(int& val) {
    const int x = 3409;
    std::cout << 3409;
    for (int i = 0; i < 3409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3410(int& val) {
    func_in_3410(val);
    const int x = 3410;
    std::cout << 3410;
    for (int i = 0; i < 3410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3410(int& val) {
    const int x = 3410;
    std::cout << 3410;
    for (int i = 0; i < 3410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3411(int& val) {
    func_in_3411(val);
    const int x = 3411;
    std::cout << 3411;
    for (int i = 0; i < 3411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3411(int& val) {
    const int x = 3411;
    std::cout << 3411;
    for (int i = 0; i < 3411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3412(int& val) {
    func_in_3412(val);
    const int x = 3412;
    std::cout << 3412;
    for (int i = 0; i < 3412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3412(int& val) {
    const int x = 3412;
    std::cout << 3412;
    for (int i = 0; i < 3412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3413(int& val) {
    func_in_3413(val);
    const int x = 3413;
    std::cout << 3413;
    for (int i = 0; i < 3413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3413(int& val) {
    const int x = 3413;
    std::cout << 3413;
    for (int i = 0; i < 3413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3414(int& val) {
    func_in_3414(val);
    const int x = 3414;
    std::cout << 3414;
    for (int i = 0; i < 3414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3414(int& val) {
    const int x = 3414;
    std::cout << 3414;
    for (int i = 0; i < 3414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3415(int& val) {
    func_in_3415(val);
    const int x = 3415;
    std::cout << 3415;
    for (int i = 0; i < 3415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3415(int& val) {
    const int x = 3415;
    std::cout << 3415;
    for (int i = 0; i < 3415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3416(int& val) {
    func_in_3416(val);
    const int x = 3416;
    std::cout << 3416;
    for (int i = 0; i < 3416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3416(int& val) {
    const int x = 3416;
    std::cout << 3416;
    for (int i = 0; i < 3416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3417(int& val) {
    func_in_3417(val);
    const int x = 3417;
    std::cout << 3417;
    for (int i = 0; i < 3417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3417(int& val) {
    const int x = 3417;
    std::cout << 3417;
    for (int i = 0; i < 3417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3418(int& val) {
    func_in_3418(val);
    const int x = 3418;
    std::cout << 3418;
    for (int i = 0; i < 3418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3418(int& val) {
    const int x = 3418;
    std::cout << 3418;
    for (int i = 0; i < 3418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3419(int& val) {
    func_in_3419(val);
    const int x = 3419;
    std::cout << 3419;
    for (int i = 0; i < 3419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3419(int& val) {
    const int x = 3419;
    std::cout << 3419;
    for (int i = 0; i < 3419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3420(int& val) {
    func_in_3420(val);
    const int x = 3420;
    std::cout << 3420;
    for (int i = 0; i < 3420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3420(int& val) {
    const int x = 3420;
    std::cout << 3420;
    for (int i = 0; i < 3420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3421(int& val) {
    func_in_3421(val);
    const int x = 3421;
    std::cout << 3421;
    for (int i = 0; i < 3421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3421(int& val) {
    const int x = 3421;
    std::cout << 3421;
    for (int i = 0; i < 3421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3422(int& val) {
    func_in_3422(val);
    const int x = 3422;
    std::cout << 3422;
    for (int i = 0; i < 3422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3422(int& val) {
    const int x = 3422;
    std::cout << 3422;
    for (int i = 0; i < 3422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3423(int& val) {
    func_in_3423(val);
    const int x = 3423;
    std::cout << 3423;
    for (int i = 0; i < 3423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3423(int& val) {
    const int x = 3423;
    std::cout << 3423;
    for (int i = 0; i < 3423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3424(int& val) {
    func_in_3424(val);
    const int x = 3424;
    std::cout << 3424;
    for (int i = 0; i < 3424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3424(int& val) {
    const int x = 3424;
    std::cout << 3424;
    for (int i = 0; i < 3424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3425(int& val) {
    func_in_3425(val);
    const int x = 3425;
    std::cout << 3425;
    for (int i = 0; i < 3425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3425(int& val) {
    const int x = 3425;
    std::cout << 3425;
    for (int i = 0; i < 3425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3426(int& val) {
    func_in_3426(val);
    const int x = 3426;
    std::cout << 3426;
    for (int i = 0; i < 3426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3426(int& val) {
    const int x = 3426;
    std::cout << 3426;
    for (int i = 0; i < 3426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3427(int& val) {
    func_in_3427(val);
    const int x = 3427;
    std::cout << 3427;
    for (int i = 0; i < 3427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3427(int& val) {
    const int x = 3427;
    std::cout << 3427;
    for (int i = 0; i < 3427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3428(int& val) {
    func_in_3428(val);
    const int x = 3428;
    std::cout << 3428;
    for (int i = 0; i < 3428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3428(int& val) {
    const int x = 3428;
    std::cout << 3428;
    for (int i = 0; i < 3428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3429(int& val) {
    func_in_3429(val);
    const int x = 3429;
    std::cout << 3429;
    for (int i = 0; i < 3429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3429(int& val) {
    const int x = 3429;
    std::cout << 3429;
    for (int i = 0; i < 3429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3430(int& val) {
    func_in_3430(val);
    const int x = 3430;
    std::cout << 3430;
    for (int i = 0; i < 3430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3430(int& val) {
    const int x = 3430;
    std::cout << 3430;
    for (int i = 0; i < 3430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3431(int& val) {
    func_in_3431(val);
    const int x = 3431;
    std::cout << 3431;
    for (int i = 0; i < 3431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3431(int& val) {
    const int x = 3431;
    std::cout << 3431;
    for (int i = 0; i < 3431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3432(int& val) {
    func_in_3432(val);
    const int x = 3432;
    std::cout << 3432;
    for (int i = 0; i < 3432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3432(int& val) {
    const int x = 3432;
    std::cout << 3432;
    for (int i = 0; i < 3432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3433(int& val) {
    func_in_3433(val);
    const int x = 3433;
    std::cout << 3433;
    for (int i = 0; i < 3433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3433(int& val) {
    const int x = 3433;
    std::cout << 3433;
    for (int i = 0; i < 3433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3434(int& val) {
    func_in_3434(val);
    const int x = 3434;
    std::cout << 3434;
    for (int i = 0; i < 3434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3434(int& val) {
    const int x = 3434;
    std::cout << 3434;
    for (int i = 0; i < 3434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3435(int& val) {
    func_in_3435(val);
    const int x = 3435;
    std::cout << 3435;
    for (int i = 0; i < 3435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3435(int& val) {
    const int x = 3435;
    std::cout << 3435;
    for (int i = 0; i < 3435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3436(int& val) {
    func_in_3436(val);
    const int x = 3436;
    std::cout << 3436;
    for (int i = 0; i < 3436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3436(int& val) {
    const int x = 3436;
    std::cout << 3436;
    for (int i = 0; i < 3436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3437(int& val) {
    func_in_3437(val);
    const int x = 3437;
    std::cout << 3437;
    for (int i = 0; i < 3437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3437(int& val) {
    const int x = 3437;
    std::cout << 3437;
    for (int i = 0; i < 3437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3438(int& val) {
    func_in_3438(val);
    const int x = 3438;
    std::cout << 3438;
    for (int i = 0; i < 3438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3438(int& val) {
    const int x = 3438;
    std::cout << 3438;
    for (int i = 0; i < 3438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3439(int& val) {
    func_in_3439(val);
    const int x = 3439;
    std::cout << 3439;
    for (int i = 0; i < 3439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3439(int& val) {
    const int x = 3439;
    std::cout << 3439;
    for (int i = 0; i < 3439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3440(int& val) {
    func_in_3440(val);
    const int x = 3440;
    std::cout << 3440;
    for (int i = 0; i < 3440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3440(int& val) {
    const int x = 3440;
    std::cout << 3440;
    for (int i = 0; i < 3440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3441(int& val) {
    func_in_3441(val);
    const int x = 3441;
    std::cout << 3441;
    for (int i = 0; i < 3441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3441(int& val) {
    const int x = 3441;
    std::cout << 3441;
    for (int i = 0; i < 3441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3442(int& val) {
    func_in_3442(val);
    const int x = 3442;
    std::cout << 3442;
    for (int i = 0; i < 3442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3442(int& val) {
    const int x = 3442;
    std::cout << 3442;
    for (int i = 0; i < 3442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3443(int& val) {
    func_in_3443(val);
    const int x = 3443;
    std::cout << 3443;
    for (int i = 0; i < 3443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3443(int& val) {
    const int x = 3443;
    std::cout << 3443;
    for (int i = 0; i < 3443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3444(int& val) {
    func_in_3444(val);
    const int x = 3444;
    std::cout << 3444;
    for (int i = 0; i < 3444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3444(int& val) {
    const int x = 3444;
    std::cout << 3444;
    for (int i = 0; i < 3444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3445(int& val) {
    func_in_3445(val);
    const int x = 3445;
    std::cout << 3445;
    for (int i = 0; i < 3445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3445(int& val) {
    const int x = 3445;
    std::cout << 3445;
    for (int i = 0; i < 3445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3446(int& val) {
    func_in_3446(val);
    const int x = 3446;
    std::cout << 3446;
    for (int i = 0; i < 3446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3446(int& val) {
    const int x = 3446;
    std::cout << 3446;
    for (int i = 0; i < 3446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3447(int& val) {
    func_in_3447(val);
    const int x = 3447;
    std::cout << 3447;
    for (int i = 0; i < 3447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3447(int& val) {
    const int x = 3447;
    std::cout << 3447;
    for (int i = 0; i < 3447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3448(int& val) {
    func_in_3448(val);
    const int x = 3448;
    std::cout << 3448;
    for (int i = 0; i < 3448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3448(int& val) {
    const int x = 3448;
    std::cout << 3448;
    for (int i = 0; i < 3448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3449(int& val) {
    func_in_3449(val);
    const int x = 3449;
    std::cout << 3449;
    for (int i = 0; i < 3449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3449(int& val) {
    const int x = 3449;
    std::cout << 3449;
    for (int i = 0; i < 3449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3450(int& val) {
    func_in_3450(val);
    const int x = 3450;
    std::cout << 3450;
    for (int i = 0; i < 3450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3450(int& val) {
    const int x = 3450;
    std::cout << 3450;
    for (int i = 0; i < 3450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3451(int& val) {
    func_in_3451(val);
    const int x = 3451;
    std::cout << 3451;
    for (int i = 0; i < 3451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3451(int& val) {
    const int x = 3451;
    std::cout << 3451;
    for (int i = 0; i < 3451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3452(int& val) {
    func_in_3452(val);
    const int x = 3452;
    std::cout << 3452;
    for (int i = 0; i < 3452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3452(int& val) {
    const int x = 3452;
    std::cout << 3452;
    for (int i = 0; i < 3452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3453(int& val) {
    func_in_3453(val);
    const int x = 3453;
    std::cout << 3453;
    for (int i = 0; i < 3453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3453(int& val) {
    const int x = 3453;
    std::cout << 3453;
    for (int i = 0; i < 3453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3454(int& val) {
    func_in_3454(val);
    const int x = 3454;
    std::cout << 3454;
    for (int i = 0; i < 3454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3454(int& val) {
    const int x = 3454;
    std::cout << 3454;
    for (int i = 0; i < 3454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3455(int& val) {
    func_in_3455(val);
    const int x = 3455;
    std::cout << 3455;
    for (int i = 0; i < 3455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3455(int& val) {
    const int x = 3455;
    std::cout << 3455;
    for (int i = 0; i < 3455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3456(int& val) {
    func_in_3456(val);
    const int x = 3456;
    std::cout << 3456;
    for (int i = 0; i < 3456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3456(int& val) {
    const int x = 3456;
    std::cout << 3456;
    for (int i = 0; i < 3456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3457(int& val) {
    func_in_3457(val);
    const int x = 3457;
    std::cout << 3457;
    for (int i = 0; i < 3457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3457(int& val) {
    const int x = 3457;
    std::cout << 3457;
    for (int i = 0; i < 3457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3458(int& val) {
    func_in_3458(val);
    const int x = 3458;
    std::cout << 3458;
    for (int i = 0; i < 3458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3458(int& val) {
    const int x = 3458;
    std::cout << 3458;
    for (int i = 0; i < 3458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3459(int& val) {
    func_in_3459(val);
    const int x = 3459;
    std::cout << 3459;
    for (int i = 0; i < 3459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3459(int& val) {
    const int x = 3459;
    std::cout << 3459;
    for (int i = 0; i < 3459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3460(int& val) {
    func_in_3460(val);
    const int x = 3460;
    std::cout << 3460;
    for (int i = 0; i < 3460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3460(int& val) {
    const int x = 3460;
    std::cout << 3460;
    for (int i = 0; i < 3460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3461(int& val) {
    func_in_3461(val);
    const int x = 3461;
    std::cout << 3461;
    for (int i = 0; i < 3461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3461(int& val) {
    const int x = 3461;
    std::cout << 3461;
    for (int i = 0; i < 3461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3462(int& val) {
    func_in_3462(val);
    const int x = 3462;
    std::cout << 3462;
    for (int i = 0; i < 3462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3462(int& val) {
    const int x = 3462;
    std::cout << 3462;
    for (int i = 0; i < 3462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3463(int& val) {
    func_in_3463(val);
    const int x = 3463;
    std::cout << 3463;
    for (int i = 0; i < 3463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3463(int& val) {
    const int x = 3463;
    std::cout << 3463;
    for (int i = 0; i < 3463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3464(int& val) {
    func_in_3464(val);
    const int x = 3464;
    std::cout << 3464;
    for (int i = 0; i < 3464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3464(int& val) {
    const int x = 3464;
    std::cout << 3464;
    for (int i = 0; i < 3464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3465(int& val) {
    func_in_3465(val);
    const int x = 3465;
    std::cout << 3465;
    for (int i = 0; i < 3465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3465(int& val) {
    const int x = 3465;
    std::cout << 3465;
    for (int i = 0; i < 3465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3466(int& val) {
    func_in_3466(val);
    const int x = 3466;
    std::cout << 3466;
    for (int i = 0; i < 3466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3466(int& val) {
    const int x = 3466;
    std::cout << 3466;
    for (int i = 0; i < 3466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3467(int& val) {
    func_in_3467(val);
    const int x = 3467;
    std::cout << 3467;
    for (int i = 0; i < 3467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3467(int& val) {
    const int x = 3467;
    std::cout << 3467;
    for (int i = 0; i < 3467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3468(int& val) {
    func_in_3468(val);
    const int x = 3468;
    std::cout << 3468;
    for (int i = 0; i < 3468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3468(int& val) {
    const int x = 3468;
    std::cout << 3468;
    for (int i = 0; i < 3468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3469(int& val) {
    func_in_3469(val);
    const int x = 3469;
    std::cout << 3469;
    for (int i = 0; i < 3469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3469(int& val) {
    const int x = 3469;
    std::cout << 3469;
    for (int i = 0; i < 3469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3470(int& val) {
    func_in_3470(val);
    const int x = 3470;
    std::cout << 3470;
    for (int i = 0; i < 3470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3470(int& val) {
    const int x = 3470;
    std::cout << 3470;
    for (int i = 0; i < 3470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3471(int& val) {
    func_in_3471(val);
    const int x = 3471;
    std::cout << 3471;
    for (int i = 0; i < 3471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3471(int& val) {
    const int x = 3471;
    std::cout << 3471;
    for (int i = 0; i < 3471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3472(int& val) {
    func_in_3472(val);
    const int x = 3472;
    std::cout << 3472;
    for (int i = 0; i < 3472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3472(int& val) {
    const int x = 3472;
    std::cout << 3472;
    for (int i = 0; i < 3472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3473(int& val) {
    func_in_3473(val);
    const int x = 3473;
    std::cout << 3473;
    for (int i = 0; i < 3473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3473(int& val) {
    const int x = 3473;
    std::cout << 3473;
    for (int i = 0; i < 3473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3474(int& val) {
    func_in_3474(val);
    const int x = 3474;
    std::cout << 3474;
    for (int i = 0; i < 3474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3474(int& val) {
    const int x = 3474;
    std::cout << 3474;
    for (int i = 0; i < 3474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3475(int& val) {
    func_in_3475(val);
    const int x = 3475;
    std::cout << 3475;
    for (int i = 0; i < 3475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3475(int& val) {
    const int x = 3475;
    std::cout << 3475;
    for (int i = 0; i < 3475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3476(int& val) {
    func_in_3476(val);
    const int x = 3476;
    std::cout << 3476;
    for (int i = 0; i < 3476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3476(int& val) {
    const int x = 3476;
    std::cout << 3476;
    for (int i = 0; i < 3476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3477(int& val) {
    func_in_3477(val);
    const int x = 3477;
    std::cout << 3477;
    for (int i = 0; i < 3477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3477(int& val) {
    const int x = 3477;
    std::cout << 3477;
    for (int i = 0; i < 3477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3478(int& val) {
    func_in_3478(val);
    const int x = 3478;
    std::cout << 3478;
    for (int i = 0; i < 3478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3478(int& val) {
    const int x = 3478;
    std::cout << 3478;
    for (int i = 0; i < 3478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3479(int& val) {
    func_in_3479(val);
    const int x = 3479;
    std::cout << 3479;
    for (int i = 0; i < 3479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3479(int& val) {
    const int x = 3479;
    std::cout << 3479;
    for (int i = 0; i < 3479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3480(int& val) {
    func_in_3480(val);
    const int x = 3480;
    std::cout << 3480;
    for (int i = 0; i < 3480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3480(int& val) {
    const int x = 3480;
    std::cout << 3480;
    for (int i = 0; i < 3480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3481(int& val) {
    func_in_3481(val);
    const int x = 3481;
    std::cout << 3481;
    for (int i = 0; i < 3481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3481(int& val) {
    const int x = 3481;
    std::cout << 3481;
    for (int i = 0; i < 3481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3482(int& val) {
    func_in_3482(val);
    const int x = 3482;
    std::cout << 3482;
    for (int i = 0; i < 3482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3482(int& val) {
    const int x = 3482;
    std::cout << 3482;
    for (int i = 0; i < 3482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3483(int& val) {
    func_in_3483(val);
    const int x = 3483;
    std::cout << 3483;
    for (int i = 0; i < 3483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3483(int& val) {
    const int x = 3483;
    std::cout << 3483;
    for (int i = 0; i < 3483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3484(int& val) {
    func_in_3484(val);
    const int x = 3484;
    std::cout << 3484;
    for (int i = 0; i < 3484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3484(int& val) {
    const int x = 3484;
    std::cout << 3484;
    for (int i = 0; i < 3484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3485(int& val) {
    func_in_3485(val);
    const int x = 3485;
    std::cout << 3485;
    for (int i = 0; i < 3485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3485(int& val) {
    const int x = 3485;
    std::cout << 3485;
    for (int i = 0; i < 3485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3486(int& val) {
    func_in_3486(val);
    const int x = 3486;
    std::cout << 3486;
    for (int i = 0; i < 3486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3486(int& val) {
    const int x = 3486;
    std::cout << 3486;
    for (int i = 0; i < 3486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3487(int& val) {
    func_in_3487(val);
    const int x = 3487;
    std::cout << 3487;
    for (int i = 0; i < 3487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3487(int& val) {
    const int x = 3487;
    std::cout << 3487;
    for (int i = 0; i < 3487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3488(int& val) {
    func_in_3488(val);
    const int x = 3488;
    std::cout << 3488;
    for (int i = 0; i < 3488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3488(int& val) {
    const int x = 3488;
    std::cout << 3488;
    for (int i = 0; i < 3488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3489(int& val) {
    func_in_3489(val);
    const int x = 3489;
    std::cout << 3489;
    for (int i = 0; i < 3489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3489(int& val) {
    const int x = 3489;
    std::cout << 3489;
    for (int i = 0; i < 3489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3490(int& val) {
    func_in_3490(val);
    const int x = 3490;
    std::cout << 3490;
    for (int i = 0; i < 3490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3490(int& val) {
    const int x = 3490;
    std::cout << 3490;
    for (int i = 0; i < 3490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3491(int& val) {
    func_in_3491(val);
    const int x = 3491;
    std::cout << 3491;
    for (int i = 0; i < 3491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3491(int& val) {
    const int x = 3491;
    std::cout << 3491;
    for (int i = 0; i < 3491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3492(int& val) {
    func_in_3492(val);
    const int x = 3492;
    std::cout << 3492;
    for (int i = 0; i < 3492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3492(int& val) {
    const int x = 3492;
    std::cout << 3492;
    for (int i = 0; i < 3492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3493(int& val) {
    func_in_3493(val);
    const int x = 3493;
    std::cout << 3493;
    for (int i = 0; i < 3493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3493(int& val) {
    const int x = 3493;
    std::cout << 3493;
    for (int i = 0; i < 3493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3494(int& val) {
    func_in_3494(val);
    const int x = 3494;
    std::cout << 3494;
    for (int i = 0; i < 3494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3494(int& val) {
    const int x = 3494;
    std::cout << 3494;
    for (int i = 0; i < 3494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3495(int& val) {
    func_in_3495(val);
    const int x = 3495;
    std::cout << 3495;
    for (int i = 0; i < 3495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3495(int& val) {
    const int x = 3495;
    std::cout << 3495;
    for (int i = 0; i < 3495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3496(int& val) {
    func_in_3496(val);
    const int x = 3496;
    std::cout << 3496;
    for (int i = 0; i < 3496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3496(int& val) {
    const int x = 3496;
    std::cout << 3496;
    for (int i = 0; i < 3496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3497(int& val) {
    func_in_3497(val);
    const int x = 3497;
    std::cout << 3497;
    for (int i = 0; i < 3497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3497(int& val) {
    const int x = 3497;
    std::cout << 3497;
    for (int i = 0; i < 3497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3498(int& val) {
    func_in_3498(val);
    const int x = 3498;
    std::cout << 3498;
    for (int i = 0; i < 3498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3498(int& val) {
    const int x = 3498;
    std::cout << 3498;
    for (int i = 0; i < 3498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3499(int& val) {
    func_in_3499(val);
    const int x = 3499;
    std::cout << 3499;
    for (int i = 0; i < 3499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3499(int& val) {
    const int x = 3499;
    std::cout << 3499;
    for (int i = 0; i < 3499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3500(int& val) {
    func_in_3500(val);
    const int x = 3500;
    std::cout << 3500;
    for (int i = 0; i < 3500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3500(int& val) {
    const int x = 3500;
    std::cout << 3500;
    for (int i = 0; i < 3500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3501(int& val) {
    func_in_3501(val);
    const int x = 3501;
    std::cout << 3501;
    for (int i = 0; i < 3501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3501(int& val) {
    const int x = 3501;
    std::cout << 3501;
    for (int i = 0; i < 3501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3502(int& val) {
    func_in_3502(val);
    const int x = 3502;
    std::cout << 3502;
    for (int i = 0; i < 3502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3502(int& val) {
    const int x = 3502;
    std::cout << 3502;
    for (int i = 0; i < 3502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3503(int& val) {
    func_in_3503(val);
    const int x = 3503;
    std::cout << 3503;
    for (int i = 0; i < 3503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3503(int& val) {
    const int x = 3503;
    std::cout << 3503;
    for (int i = 0; i < 3503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3504(int& val) {
    func_in_3504(val);
    const int x = 3504;
    std::cout << 3504;
    for (int i = 0; i < 3504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3504(int& val) {
    const int x = 3504;
    std::cout << 3504;
    for (int i = 0; i < 3504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3505(int& val) {
    func_in_3505(val);
    const int x = 3505;
    std::cout << 3505;
    for (int i = 0; i < 3505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3505(int& val) {
    const int x = 3505;
    std::cout << 3505;
    for (int i = 0; i < 3505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3506(int& val) {
    func_in_3506(val);
    const int x = 3506;
    std::cout << 3506;
    for (int i = 0; i < 3506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3506(int& val) {
    const int x = 3506;
    std::cout << 3506;
    for (int i = 0; i < 3506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3507(int& val) {
    func_in_3507(val);
    const int x = 3507;
    std::cout << 3507;
    for (int i = 0; i < 3507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3507(int& val) {
    const int x = 3507;
    std::cout << 3507;
    for (int i = 0; i < 3507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3508(int& val) {
    func_in_3508(val);
    const int x = 3508;
    std::cout << 3508;
    for (int i = 0; i < 3508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3508(int& val) {
    const int x = 3508;
    std::cout << 3508;
    for (int i = 0; i < 3508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3509(int& val) {
    func_in_3509(val);
    const int x = 3509;
    std::cout << 3509;
    for (int i = 0; i < 3509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3509(int& val) {
    const int x = 3509;
    std::cout << 3509;
    for (int i = 0; i < 3509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3510(int& val) {
    func_in_3510(val);
    const int x = 3510;
    std::cout << 3510;
    for (int i = 0; i < 3510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3510(int& val) {
    const int x = 3510;
    std::cout << 3510;
    for (int i = 0; i < 3510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3511(int& val) {
    func_in_3511(val);
    const int x = 3511;
    std::cout << 3511;
    for (int i = 0; i < 3511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3511(int& val) {
    const int x = 3511;
    std::cout << 3511;
    for (int i = 0; i < 3511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3512(int& val) {
    func_in_3512(val);
    const int x = 3512;
    std::cout << 3512;
    for (int i = 0; i < 3512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3512(int& val) {
    const int x = 3512;
    std::cout << 3512;
    for (int i = 0; i < 3512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3513(int& val) {
    func_in_3513(val);
    const int x = 3513;
    std::cout << 3513;
    for (int i = 0; i < 3513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3513(int& val) {
    const int x = 3513;
    std::cout << 3513;
    for (int i = 0; i < 3513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3514(int& val) {
    func_in_3514(val);
    const int x = 3514;
    std::cout << 3514;
    for (int i = 0; i < 3514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3514(int& val) {
    const int x = 3514;
    std::cout << 3514;
    for (int i = 0; i < 3514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3515(int& val) {
    func_in_3515(val);
    const int x = 3515;
    std::cout << 3515;
    for (int i = 0; i < 3515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3515(int& val) {
    const int x = 3515;
    std::cout << 3515;
    for (int i = 0; i < 3515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3516(int& val) {
    func_in_3516(val);
    const int x = 3516;
    std::cout << 3516;
    for (int i = 0; i < 3516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3516(int& val) {
    const int x = 3516;
    std::cout << 3516;
    for (int i = 0; i < 3516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3517(int& val) {
    func_in_3517(val);
    const int x = 3517;
    std::cout << 3517;
    for (int i = 0; i < 3517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3517(int& val) {
    const int x = 3517;
    std::cout << 3517;
    for (int i = 0; i < 3517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3518(int& val) {
    func_in_3518(val);
    const int x = 3518;
    std::cout << 3518;
    for (int i = 0; i < 3518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3518(int& val) {
    const int x = 3518;
    std::cout << 3518;
    for (int i = 0; i < 3518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3519(int& val) {
    func_in_3519(val);
    const int x = 3519;
    std::cout << 3519;
    for (int i = 0; i < 3519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3519(int& val) {
    const int x = 3519;
    std::cout << 3519;
    for (int i = 0; i < 3519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3520(int& val) {
    func_in_3520(val);
    const int x = 3520;
    std::cout << 3520;
    for (int i = 0; i < 3520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3520(int& val) {
    const int x = 3520;
    std::cout << 3520;
    for (int i = 0; i < 3520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3521(int& val) {
    func_in_3521(val);
    const int x = 3521;
    std::cout << 3521;
    for (int i = 0; i < 3521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3521(int& val) {
    const int x = 3521;
    std::cout << 3521;
    for (int i = 0; i < 3521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3522(int& val) {
    func_in_3522(val);
    const int x = 3522;
    std::cout << 3522;
    for (int i = 0; i < 3522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3522(int& val) {
    const int x = 3522;
    std::cout << 3522;
    for (int i = 0; i < 3522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3523(int& val) {
    func_in_3523(val);
    const int x = 3523;
    std::cout << 3523;
    for (int i = 0; i < 3523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3523(int& val) {
    const int x = 3523;
    std::cout << 3523;
    for (int i = 0; i < 3523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3524(int& val) {
    func_in_3524(val);
    const int x = 3524;
    std::cout << 3524;
    for (int i = 0; i < 3524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3524(int& val) {
    const int x = 3524;
    std::cout << 3524;
    for (int i = 0; i < 3524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3525(int& val) {
    func_in_3525(val);
    const int x = 3525;
    std::cout << 3525;
    for (int i = 0; i < 3525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3525(int& val) {
    const int x = 3525;
    std::cout << 3525;
    for (int i = 0; i < 3525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3526(int& val) {
    func_in_3526(val);
    const int x = 3526;
    std::cout << 3526;
    for (int i = 0; i < 3526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3526(int& val) {
    const int x = 3526;
    std::cout << 3526;
    for (int i = 0; i < 3526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3527(int& val) {
    func_in_3527(val);
    const int x = 3527;
    std::cout << 3527;
    for (int i = 0; i < 3527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3527(int& val) {
    const int x = 3527;
    std::cout << 3527;
    for (int i = 0; i < 3527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3528(int& val) {
    func_in_3528(val);
    const int x = 3528;
    std::cout << 3528;
    for (int i = 0; i < 3528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3528(int& val) {
    const int x = 3528;
    std::cout << 3528;
    for (int i = 0; i < 3528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3529(int& val) {
    func_in_3529(val);
    const int x = 3529;
    std::cout << 3529;
    for (int i = 0; i < 3529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3529(int& val) {
    const int x = 3529;
    std::cout << 3529;
    for (int i = 0; i < 3529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3530(int& val) {
    func_in_3530(val);
    const int x = 3530;
    std::cout << 3530;
    for (int i = 0; i < 3530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3530(int& val) {
    const int x = 3530;
    std::cout << 3530;
    for (int i = 0; i < 3530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3531(int& val) {
    func_in_3531(val);
    const int x = 3531;
    std::cout << 3531;
    for (int i = 0; i < 3531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3531(int& val) {
    const int x = 3531;
    std::cout << 3531;
    for (int i = 0; i < 3531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3532(int& val) {
    func_in_3532(val);
    const int x = 3532;
    std::cout << 3532;
    for (int i = 0; i < 3532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3532(int& val) {
    const int x = 3532;
    std::cout << 3532;
    for (int i = 0; i < 3532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3533(int& val) {
    func_in_3533(val);
    const int x = 3533;
    std::cout << 3533;
    for (int i = 0; i < 3533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3533(int& val) {
    const int x = 3533;
    std::cout << 3533;
    for (int i = 0; i < 3533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3534(int& val) {
    func_in_3534(val);
    const int x = 3534;
    std::cout << 3534;
    for (int i = 0; i < 3534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3534(int& val) {
    const int x = 3534;
    std::cout << 3534;
    for (int i = 0; i < 3534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3535(int& val) {
    func_in_3535(val);
    const int x = 3535;
    std::cout << 3535;
    for (int i = 0; i < 3535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3535(int& val) {
    const int x = 3535;
    std::cout << 3535;
    for (int i = 0; i < 3535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3536(int& val) {
    func_in_3536(val);
    const int x = 3536;
    std::cout << 3536;
    for (int i = 0; i < 3536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3536(int& val) {
    const int x = 3536;
    std::cout << 3536;
    for (int i = 0; i < 3536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3537(int& val) {
    func_in_3537(val);
    const int x = 3537;
    std::cout << 3537;
    for (int i = 0; i < 3537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3537(int& val) {
    const int x = 3537;
    std::cout << 3537;
    for (int i = 0; i < 3537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3538(int& val) {
    func_in_3538(val);
    const int x = 3538;
    std::cout << 3538;
    for (int i = 0; i < 3538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3538(int& val) {
    const int x = 3538;
    std::cout << 3538;
    for (int i = 0; i < 3538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3539(int& val) {
    func_in_3539(val);
    const int x = 3539;
    std::cout << 3539;
    for (int i = 0; i < 3539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3539(int& val) {
    const int x = 3539;
    std::cout << 3539;
    for (int i = 0; i < 3539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3540(int& val) {
    func_in_3540(val);
    const int x = 3540;
    std::cout << 3540;
    for (int i = 0; i < 3540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3540(int& val) {
    const int x = 3540;
    std::cout << 3540;
    for (int i = 0; i < 3540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3541(int& val) {
    func_in_3541(val);
    const int x = 3541;
    std::cout << 3541;
    for (int i = 0; i < 3541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3541(int& val) {
    const int x = 3541;
    std::cout << 3541;
    for (int i = 0; i < 3541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3542(int& val) {
    func_in_3542(val);
    const int x = 3542;
    std::cout << 3542;
    for (int i = 0; i < 3542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3542(int& val) {
    const int x = 3542;
    std::cout << 3542;
    for (int i = 0; i < 3542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3543(int& val) {
    func_in_3543(val);
    const int x = 3543;
    std::cout << 3543;
    for (int i = 0; i < 3543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3543(int& val) {
    const int x = 3543;
    std::cout << 3543;
    for (int i = 0; i < 3543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3544(int& val) {
    func_in_3544(val);
    const int x = 3544;
    std::cout << 3544;
    for (int i = 0; i < 3544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3544(int& val) {
    const int x = 3544;
    std::cout << 3544;
    for (int i = 0; i < 3544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3545(int& val) {
    func_in_3545(val);
    const int x = 3545;
    std::cout << 3545;
    for (int i = 0; i < 3545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3545(int& val) {
    const int x = 3545;
    std::cout << 3545;
    for (int i = 0; i < 3545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3546(int& val) {
    func_in_3546(val);
    const int x = 3546;
    std::cout << 3546;
    for (int i = 0; i < 3546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3546(int& val) {
    const int x = 3546;
    std::cout << 3546;
    for (int i = 0; i < 3546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3547(int& val) {
    func_in_3547(val);
    const int x = 3547;
    std::cout << 3547;
    for (int i = 0; i < 3547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3547(int& val) {
    const int x = 3547;
    std::cout << 3547;
    for (int i = 0; i < 3547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3548(int& val) {
    func_in_3548(val);
    const int x = 3548;
    std::cout << 3548;
    for (int i = 0; i < 3548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3548(int& val) {
    const int x = 3548;
    std::cout << 3548;
    for (int i = 0; i < 3548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3549(int& val) {
    func_in_3549(val);
    const int x = 3549;
    std::cout << 3549;
    for (int i = 0; i < 3549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3549(int& val) {
    const int x = 3549;
    std::cout << 3549;
    for (int i = 0; i < 3549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3550(int& val) {
    func_in_3550(val);
    const int x = 3550;
    std::cout << 3550;
    for (int i = 0; i < 3550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3550(int& val) {
    const int x = 3550;
    std::cout << 3550;
    for (int i = 0; i < 3550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3551(int& val) {
    func_in_3551(val);
    const int x = 3551;
    std::cout << 3551;
    for (int i = 0; i < 3551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3551(int& val) {
    const int x = 3551;
    std::cout << 3551;
    for (int i = 0; i < 3551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3552(int& val) {
    func_in_3552(val);
    const int x = 3552;
    std::cout << 3552;
    for (int i = 0; i < 3552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3552(int& val) {
    const int x = 3552;
    std::cout << 3552;
    for (int i = 0; i < 3552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3553(int& val) {
    func_in_3553(val);
    const int x = 3553;
    std::cout << 3553;
    for (int i = 0; i < 3553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3553(int& val) {
    const int x = 3553;
    std::cout << 3553;
    for (int i = 0; i < 3553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3554(int& val) {
    func_in_3554(val);
    const int x = 3554;
    std::cout << 3554;
    for (int i = 0; i < 3554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3554(int& val) {
    const int x = 3554;
    std::cout << 3554;
    for (int i = 0; i < 3554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3555(int& val) {
    func_in_3555(val);
    const int x = 3555;
    std::cout << 3555;
    for (int i = 0; i < 3555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3555(int& val) {
    const int x = 3555;
    std::cout << 3555;
    for (int i = 0; i < 3555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3556(int& val) {
    func_in_3556(val);
    const int x = 3556;
    std::cout << 3556;
    for (int i = 0; i < 3556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3556(int& val) {
    const int x = 3556;
    std::cout << 3556;
    for (int i = 0; i < 3556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3557(int& val) {
    func_in_3557(val);
    const int x = 3557;
    std::cout << 3557;
    for (int i = 0; i < 3557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3557(int& val) {
    const int x = 3557;
    std::cout << 3557;
    for (int i = 0; i < 3557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3558(int& val) {
    func_in_3558(val);
    const int x = 3558;
    std::cout << 3558;
    for (int i = 0; i < 3558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3558(int& val) {
    const int x = 3558;
    std::cout << 3558;
    for (int i = 0; i < 3558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3559(int& val) {
    func_in_3559(val);
    const int x = 3559;
    std::cout << 3559;
    for (int i = 0; i < 3559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3559(int& val) {
    const int x = 3559;
    std::cout << 3559;
    for (int i = 0; i < 3559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3560(int& val) {
    func_in_3560(val);
    const int x = 3560;
    std::cout << 3560;
    for (int i = 0; i < 3560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3560(int& val) {
    const int x = 3560;
    std::cout << 3560;
    for (int i = 0; i < 3560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3561(int& val) {
    func_in_3561(val);
    const int x = 3561;
    std::cout << 3561;
    for (int i = 0; i < 3561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3561(int& val) {
    const int x = 3561;
    std::cout << 3561;
    for (int i = 0; i < 3561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3562(int& val) {
    func_in_3562(val);
    const int x = 3562;
    std::cout << 3562;
    for (int i = 0; i < 3562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3562(int& val) {
    const int x = 3562;
    std::cout << 3562;
    for (int i = 0; i < 3562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3563(int& val) {
    func_in_3563(val);
    const int x = 3563;
    std::cout << 3563;
    for (int i = 0; i < 3563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3563(int& val) {
    const int x = 3563;
    std::cout << 3563;
    for (int i = 0; i < 3563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3564(int& val) {
    func_in_3564(val);
    const int x = 3564;
    std::cout << 3564;
    for (int i = 0; i < 3564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3564(int& val) {
    const int x = 3564;
    std::cout << 3564;
    for (int i = 0; i < 3564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3565(int& val) {
    func_in_3565(val);
    const int x = 3565;
    std::cout << 3565;
    for (int i = 0; i < 3565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3565(int& val) {
    const int x = 3565;
    std::cout << 3565;
    for (int i = 0; i < 3565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3566(int& val) {
    func_in_3566(val);
    const int x = 3566;
    std::cout << 3566;
    for (int i = 0; i < 3566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3566(int& val) {
    const int x = 3566;
    std::cout << 3566;
    for (int i = 0; i < 3566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3567(int& val) {
    func_in_3567(val);
    const int x = 3567;
    std::cout << 3567;
    for (int i = 0; i < 3567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3567(int& val) {
    const int x = 3567;
    std::cout << 3567;
    for (int i = 0; i < 3567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3568(int& val) {
    func_in_3568(val);
    const int x = 3568;
    std::cout << 3568;
    for (int i = 0; i < 3568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3568(int& val) {
    const int x = 3568;
    std::cout << 3568;
    for (int i = 0; i < 3568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3569(int& val) {
    func_in_3569(val);
    const int x = 3569;
    std::cout << 3569;
    for (int i = 0; i < 3569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3569(int& val) {
    const int x = 3569;
    std::cout << 3569;
    for (int i = 0; i < 3569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3570(int& val) {
    func_in_3570(val);
    const int x = 3570;
    std::cout << 3570;
    for (int i = 0; i < 3570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3570(int& val) {
    const int x = 3570;
    std::cout << 3570;
    for (int i = 0; i < 3570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3571(int& val) {
    func_in_3571(val);
    const int x = 3571;
    std::cout << 3571;
    for (int i = 0; i < 3571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3571(int& val) {
    const int x = 3571;
    std::cout << 3571;
    for (int i = 0; i < 3571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3572(int& val) {
    func_in_3572(val);
    const int x = 3572;
    std::cout << 3572;
    for (int i = 0; i < 3572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3572(int& val) {
    const int x = 3572;
    std::cout << 3572;
    for (int i = 0; i < 3572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3573(int& val) {
    func_in_3573(val);
    const int x = 3573;
    std::cout << 3573;
    for (int i = 0; i < 3573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3573(int& val) {
    const int x = 3573;
    std::cout << 3573;
    for (int i = 0; i < 3573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3574(int& val) {
    func_in_3574(val);
    const int x = 3574;
    std::cout << 3574;
    for (int i = 0; i < 3574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3574(int& val) {
    const int x = 3574;
    std::cout << 3574;
    for (int i = 0; i < 3574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3575(int& val) {
    func_in_3575(val);
    const int x = 3575;
    std::cout << 3575;
    for (int i = 0; i < 3575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3575(int& val) {
    const int x = 3575;
    std::cout << 3575;
    for (int i = 0; i < 3575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3576(int& val) {
    func_in_3576(val);
    const int x = 3576;
    std::cout << 3576;
    for (int i = 0; i < 3576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3576(int& val) {
    const int x = 3576;
    std::cout << 3576;
    for (int i = 0; i < 3576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3577(int& val) {
    func_in_3577(val);
    const int x = 3577;
    std::cout << 3577;
    for (int i = 0; i < 3577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3577(int& val) {
    const int x = 3577;
    std::cout << 3577;
    for (int i = 0; i < 3577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3578(int& val) {
    func_in_3578(val);
    const int x = 3578;
    std::cout << 3578;
    for (int i = 0; i < 3578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3578(int& val) {
    const int x = 3578;
    std::cout << 3578;
    for (int i = 0; i < 3578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3579(int& val) {
    func_in_3579(val);
    const int x = 3579;
    std::cout << 3579;
    for (int i = 0; i < 3579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3579(int& val) {
    const int x = 3579;
    std::cout << 3579;
    for (int i = 0; i < 3579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3580(int& val) {
    func_in_3580(val);
    const int x = 3580;
    std::cout << 3580;
    for (int i = 0; i < 3580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3580(int& val) {
    const int x = 3580;
    std::cout << 3580;
    for (int i = 0; i < 3580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3581(int& val) {
    func_in_3581(val);
    const int x = 3581;
    std::cout << 3581;
    for (int i = 0; i < 3581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3581(int& val) {
    const int x = 3581;
    std::cout << 3581;
    for (int i = 0; i < 3581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3582(int& val) {
    func_in_3582(val);
    const int x = 3582;
    std::cout << 3582;
    for (int i = 0; i < 3582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3582(int& val) {
    const int x = 3582;
    std::cout << 3582;
    for (int i = 0; i < 3582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3583(int& val) {
    func_in_3583(val);
    const int x = 3583;
    std::cout << 3583;
    for (int i = 0; i < 3583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3583(int& val) {
    const int x = 3583;
    std::cout << 3583;
    for (int i = 0; i < 3583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3584(int& val) {
    func_in_3584(val);
    const int x = 3584;
    std::cout << 3584;
    for (int i = 0; i < 3584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3584(int& val) {
    const int x = 3584;
    std::cout << 3584;
    for (int i = 0; i < 3584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3585(int& val) {
    func_in_3585(val);
    const int x = 3585;
    std::cout << 3585;
    for (int i = 0; i < 3585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3585(int& val) {
    const int x = 3585;
    std::cout << 3585;
    for (int i = 0; i < 3585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3586(int& val) {
    func_in_3586(val);
    const int x = 3586;
    std::cout << 3586;
    for (int i = 0; i < 3586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3586(int& val) {
    const int x = 3586;
    std::cout << 3586;
    for (int i = 0; i < 3586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3587(int& val) {
    func_in_3587(val);
    const int x = 3587;
    std::cout << 3587;
    for (int i = 0; i < 3587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3587(int& val) {
    const int x = 3587;
    std::cout << 3587;
    for (int i = 0; i < 3587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3588(int& val) {
    func_in_3588(val);
    const int x = 3588;
    std::cout << 3588;
    for (int i = 0; i < 3588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3588(int& val) {
    const int x = 3588;
    std::cout << 3588;
    for (int i = 0; i < 3588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3589(int& val) {
    func_in_3589(val);
    const int x = 3589;
    std::cout << 3589;
    for (int i = 0; i < 3589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3589(int& val) {
    const int x = 3589;
    std::cout << 3589;
    for (int i = 0; i < 3589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3590(int& val) {
    func_in_3590(val);
    const int x = 3590;
    std::cout << 3590;
    for (int i = 0; i < 3590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3590(int& val) {
    const int x = 3590;
    std::cout << 3590;
    for (int i = 0; i < 3590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3591(int& val) {
    func_in_3591(val);
    const int x = 3591;
    std::cout << 3591;
    for (int i = 0; i < 3591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3591(int& val) {
    const int x = 3591;
    std::cout << 3591;
    for (int i = 0; i < 3591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3592(int& val) {
    func_in_3592(val);
    const int x = 3592;
    std::cout << 3592;
    for (int i = 0; i < 3592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3592(int& val) {
    const int x = 3592;
    std::cout << 3592;
    for (int i = 0; i < 3592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3593(int& val) {
    func_in_3593(val);
    const int x = 3593;
    std::cout << 3593;
    for (int i = 0; i < 3593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3593(int& val) {
    const int x = 3593;
    std::cout << 3593;
    for (int i = 0; i < 3593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3594(int& val) {
    func_in_3594(val);
    const int x = 3594;
    std::cout << 3594;
    for (int i = 0; i < 3594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3594(int& val) {
    const int x = 3594;
    std::cout << 3594;
    for (int i = 0; i < 3594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3595(int& val) {
    func_in_3595(val);
    const int x = 3595;
    std::cout << 3595;
    for (int i = 0; i < 3595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3595(int& val) {
    const int x = 3595;
    std::cout << 3595;
    for (int i = 0; i < 3595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3596(int& val) {
    func_in_3596(val);
    const int x = 3596;
    std::cout << 3596;
    for (int i = 0; i < 3596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3596(int& val) {
    const int x = 3596;
    std::cout << 3596;
    for (int i = 0; i < 3596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3597(int& val) {
    func_in_3597(val);
    const int x = 3597;
    std::cout << 3597;
    for (int i = 0; i < 3597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3597(int& val) {
    const int x = 3597;
    std::cout << 3597;
    for (int i = 0; i < 3597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3598(int& val) {
    func_in_3598(val);
    const int x = 3598;
    std::cout << 3598;
    for (int i = 0; i < 3598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3598(int& val) {
    const int x = 3598;
    std::cout << 3598;
    for (int i = 0; i < 3598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3599(int& val) {
    func_in_3599(val);
    const int x = 3599;
    std::cout << 3599;
    for (int i = 0; i < 3599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3599(int& val) {
    const int x = 3599;
    std::cout << 3599;
    for (int i = 0; i < 3599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3600(int& val) {
    func_in_3600(val);
    const int x = 3600;
    std::cout << 3600;
    for (int i = 0; i < 3600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3600(int& val) {
    const int x = 3600;
    std::cout << 3600;
    for (int i = 0; i < 3600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3601(int& val) {
    func_in_3601(val);
    const int x = 3601;
    std::cout << 3601;
    for (int i = 0; i < 3601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3601(int& val) {
    const int x = 3601;
    std::cout << 3601;
    for (int i = 0; i < 3601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3602(int& val) {
    func_in_3602(val);
    const int x = 3602;
    std::cout << 3602;
    for (int i = 0; i < 3602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3602(int& val) {
    const int x = 3602;
    std::cout << 3602;
    for (int i = 0; i < 3602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3603(int& val) {
    func_in_3603(val);
    const int x = 3603;
    std::cout << 3603;
    for (int i = 0; i < 3603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3603(int& val) {
    const int x = 3603;
    std::cout << 3603;
    for (int i = 0; i < 3603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3604(int& val) {
    func_in_3604(val);
    const int x = 3604;
    std::cout << 3604;
    for (int i = 0; i < 3604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3604(int& val) {
    const int x = 3604;
    std::cout << 3604;
    for (int i = 0; i < 3604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3605(int& val) {
    func_in_3605(val);
    const int x = 3605;
    std::cout << 3605;
    for (int i = 0; i < 3605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3605(int& val) {
    const int x = 3605;
    std::cout << 3605;
    for (int i = 0; i < 3605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3606(int& val) {
    func_in_3606(val);
    const int x = 3606;
    std::cout << 3606;
    for (int i = 0; i < 3606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3606(int& val) {
    const int x = 3606;
    std::cout << 3606;
    for (int i = 0; i < 3606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3607(int& val) {
    func_in_3607(val);
    const int x = 3607;
    std::cout << 3607;
    for (int i = 0; i < 3607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3607(int& val) {
    const int x = 3607;
    std::cout << 3607;
    for (int i = 0; i < 3607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3608(int& val) {
    func_in_3608(val);
    const int x = 3608;
    std::cout << 3608;
    for (int i = 0; i < 3608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3608(int& val) {
    const int x = 3608;
    std::cout << 3608;
    for (int i = 0; i < 3608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3609(int& val) {
    func_in_3609(val);
    const int x = 3609;
    std::cout << 3609;
    for (int i = 0; i < 3609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3609(int& val) {
    const int x = 3609;
    std::cout << 3609;
    for (int i = 0; i < 3609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3610(int& val) {
    func_in_3610(val);
    const int x = 3610;
    std::cout << 3610;
    for (int i = 0; i < 3610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3610(int& val) {
    const int x = 3610;
    std::cout << 3610;
    for (int i = 0; i < 3610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3611(int& val) {
    func_in_3611(val);
    const int x = 3611;
    std::cout << 3611;
    for (int i = 0; i < 3611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3611(int& val) {
    const int x = 3611;
    std::cout << 3611;
    for (int i = 0; i < 3611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3612(int& val) {
    func_in_3612(val);
    const int x = 3612;
    std::cout << 3612;
    for (int i = 0; i < 3612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3612(int& val) {
    const int x = 3612;
    std::cout << 3612;
    for (int i = 0; i < 3612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3613(int& val) {
    func_in_3613(val);
    const int x = 3613;
    std::cout << 3613;
    for (int i = 0; i < 3613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3613(int& val) {
    const int x = 3613;
    std::cout << 3613;
    for (int i = 0; i < 3613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3614(int& val) {
    func_in_3614(val);
    const int x = 3614;
    std::cout << 3614;
    for (int i = 0; i < 3614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3614(int& val) {
    const int x = 3614;
    std::cout << 3614;
    for (int i = 0; i < 3614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3615(int& val) {
    func_in_3615(val);
    const int x = 3615;
    std::cout << 3615;
    for (int i = 0; i < 3615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3615(int& val) {
    const int x = 3615;
    std::cout << 3615;
    for (int i = 0; i < 3615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3616(int& val) {
    func_in_3616(val);
    const int x = 3616;
    std::cout << 3616;
    for (int i = 0; i < 3616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3616(int& val) {
    const int x = 3616;
    std::cout << 3616;
    for (int i = 0; i < 3616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3617(int& val) {
    func_in_3617(val);
    const int x = 3617;
    std::cout << 3617;
    for (int i = 0; i < 3617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3617(int& val) {
    const int x = 3617;
    std::cout << 3617;
    for (int i = 0; i < 3617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3618(int& val) {
    func_in_3618(val);
    const int x = 3618;
    std::cout << 3618;
    for (int i = 0; i < 3618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3618(int& val) {
    const int x = 3618;
    std::cout << 3618;
    for (int i = 0; i < 3618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3619(int& val) {
    func_in_3619(val);
    const int x = 3619;
    std::cout << 3619;
    for (int i = 0; i < 3619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3619(int& val) {
    const int x = 3619;
    std::cout << 3619;
    for (int i = 0; i < 3619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3620(int& val) {
    func_in_3620(val);
    const int x = 3620;
    std::cout << 3620;
    for (int i = 0; i < 3620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3620(int& val) {
    const int x = 3620;
    std::cout << 3620;
    for (int i = 0; i < 3620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3621(int& val) {
    func_in_3621(val);
    const int x = 3621;
    std::cout << 3621;
    for (int i = 0; i < 3621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3621(int& val) {
    const int x = 3621;
    std::cout << 3621;
    for (int i = 0; i < 3621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3622(int& val) {
    func_in_3622(val);
    const int x = 3622;
    std::cout << 3622;
    for (int i = 0; i < 3622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3622(int& val) {
    const int x = 3622;
    std::cout << 3622;
    for (int i = 0; i < 3622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3623(int& val) {
    func_in_3623(val);
    const int x = 3623;
    std::cout << 3623;
    for (int i = 0; i < 3623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3623(int& val) {
    const int x = 3623;
    std::cout << 3623;
    for (int i = 0; i < 3623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3624(int& val) {
    func_in_3624(val);
    const int x = 3624;
    std::cout << 3624;
    for (int i = 0; i < 3624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3624(int& val) {
    const int x = 3624;
    std::cout << 3624;
    for (int i = 0; i < 3624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3625(int& val) {
    func_in_3625(val);
    const int x = 3625;
    std::cout << 3625;
    for (int i = 0; i < 3625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3625(int& val) {
    const int x = 3625;
    std::cout << 3625;
    for (int i = 0; i < 3625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3626(int& val) {
    func_in_3626(val);
    const int x = 3626;
    std::cout << 3626;
    for (int i = 0; i < 3626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3626(int& val) {
    const int x = 3626;
    std::cout << 3626;
    for (int i = 0; i < 3626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3627(int& val) {
    func_in_3627(val);
    const int x = 3627;
    std::cout << 3627;
    for (int i = 0; i < 3627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3627(int& val) {
    const int x = 3627;
    std::cout << 3627;
    for (int i = 0; i < 3627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3628(int& val) {
    func_in_3628(val);
    const int x = 3628;
    std::cout << 3628;
    for (int i = 0; i < 3628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3628(int& val) {
    const int x = 3628;
    std::cout << 3628;
    for (int i = 0; i < 3628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3629(int& val) {
    func_in_3629(val);
    const int x = 3629;
    std::cout << 3629;
    for (int i = 0; i < 3629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3629(int& val) {
    const int x = 3629;
    std::cout << 3629;
    for (int i = 0; i < 3629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3630(int& val) {
    func_in_3630(val);
    const int x = 3630;
    std::cout << 3630;
    for (int i = 0; i < 3630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3630(int& val) {
    const int x = 3630;
    std::cout << 3630;
    for (int i = 0; i < 3630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3631(int& val) {
    func_in_3631(val);
    const int x = 3631;
    std::cout << 3631;
    for (int i = 0; i < 3631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3631(int& val) {
    const int x = 3631;
    std::cout << 3631;
    for (int i = 0; i < 3631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3632(int& val) {
    func_in_3632(val);
    const int x = 3632;
    std::cout << 3632;
    for (int i = 0; i < 3632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3632(int& val) {
    const int x = 3632;
    std::cout << 3632;
    for (int i = 0; i < 3632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3633(int& val) {
    func_in_3633(val);
    const int x = 3633;
    std::cout << 3633;
    for (int i = 0; i < 3633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3633(int& val) {
    const int x = 3633;
    std::cout << 3633;
    for (int i = 0; i < 3633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3634(int& val) {
    func_in_3634(val);
    const int x = 3634;
    std::cout << 3634;
    for (int i = 0; i < 3634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3634(int& val) {
    const int x = 3634;
    std::cout << 3634;
    for (int i = 0; i < 3634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3635(int& val) {
    func_in_3635(val);
    const int x = 3635;
    std::cout << 3635;
    for (int i = 0; i < 3635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3635(int& val) {
    const int x = 3635;
    std::cout << 3635;
    for (int i = 0; i < 3635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3636(int& val) {
    func_in_3636(val);
    const int x = 3636;
    std::cout << 3636;
    for (int i = 0; i < 3636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3636(int& val) {
    const int x = 3636;
    std::cout << 3636;
    for (int i = 0; i < 3636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3637(int& val) {
    func_in_3637(val);
    const int x = 3637;
    std::cout << 3637;
    for (int i = 0; i < 3637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3637(int& val) {
    const int x = 3637;
    std::cout << 3637;
    for (int i = 0; i < 3637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3638(int& val) {
    func_in_3638(val);
    const int x = 3638;
    std::cout << 3638;
    for (int i = 0; i < 3638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3638(int& val) {
    const int x = 3638;
    std::cout << 3638;
    for (int i = 0; i < 3638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3639(int& val) {
    func_in_3639(val);
    const int x = 3639;
    std::cout << 3639;
    for (int i = 0; i < 3639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3639(int& val) {
    const int x = 3639;
    std::cout << 3639;
    for (int i = 0; i < 3639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3640(int& val) {
    func_in_3640(val);
    const int x = 3640;
    std::cout << 3640;
    for (int i = 0; i < 3640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3640(int& val) {
    const int x = 3640;
    std::cout << 3640;
    for (int i = 0; i < 3640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3641(int& val) {
    func_in_3641(val);
    const int x = 3641;
    std::cout << 3641;
    for (int i = 0; i < 3641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3641(int& val) {
    const int x = 3641;
    std::cout << 3641;
    for (int i = 0; i < 3641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3642(int& val) {
    func_in_3642(val);
    const int x = 3642;
    std::cout << 3642;
    for (int i = 0; i < 3642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3642(int& val) {
    const int x = 3642;
    std::cout << 3642;
    for (int i = 0; i < 3642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3643(int& val) {
    func_in_3643(val);
    const int x = 3643;
    std::cout << 3643;
    for (int i = 0; i < 3643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3643(int& val) {
    const int x = 3643;
    std::cout << 3643;
    for (int i = 0; i < 3643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3644(int& val) {
    func_in_3644(val);
    const int x = 3644;
    std::cout << 3644;
    for (int i = 0; i < 3644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3644(int& val) {
    const int x = 3644;
    std::cout << 3644;
    for (int i = 0; i < 3644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3645(int& val) {
    func_in_3645(val);
    const int x = 3645;
    std::cout << 3645;
    for (int i = 0; i < 3645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3645(int& val) {
    const int x = 3645;
    std::cout << 3645;
    for (int i = 0; i < 3645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3646(int& val) {
    func_in_3646(val);
    const int x = 3646;
    std::cout << 3646;
    for (int i = 0; i < 3646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3646(int& val) {
    const int x = 3646;
    std::cout << 3646;
    for (int i = 0; i < 3646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3647(int& val) {
    func_in_3647(val);
    const int x = 3647;
    std::cout << 3647;
    for (int i = 0; i < 3647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3647(int& val) {
    const int x = 3647;
    std::cout << 3647;
    for (int i = 0; i < 3647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3648(int& val) {
    func_in_3648(val);
    const int x = 3648;
    std::cout << 3648;
    for (int i = 0; i < 3648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3648(int& val) {
    const int x = 3648;
    std::cout << 3648;
    for (int i = 0; i < 3648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3649(int& val) {
    func_in_3649(val);
    const int x = 3649;
    std::cout << 3649;
    for (int i = 0; i < 3649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3649(int& val) {
    const int x = 3649;
    std::cout << 3649;
    for (int i = 0; i < 3649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3650(int& val) {
    func_in_3650(val);
    const int x = 3650;
    std::cout << 3650;
    for (int i = 0; i < 3650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3650(int& val) {
    const int x = 3650;
    std::cout << 3650;
    for (int i = 0; i < 3650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3651(int& val) {
    func_in_3651(val);
    const int x = 3651;
    std::cout << 3651;
    for (int i = 0; i < 3651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3651(int& val) {
    const int x = 3651;
    std::cout << 3651;
    for (int i = 0; i < 3651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3652(int& val) {
    func_in_3652(val);
    const int x = 3652;
    std::cout << 3652;
    for (int i = 0; i < 3652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3652(int& val) {
    const int x = 3652;
    std::cout << 3652;
    for (int i = 0; i < 3652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3653(int& val) {
    func_in_3653(val);
    const int x = 3653;
    std::cout << 3653;
    for (int i = 0; i < 3653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3653(int& val) {
    const int x = 3653;
    std::cout << 3653;
    for (int i = 0; i < 3653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3654(int& val) {
    func_in_3654(val);
    const int x = 3654;
    std::cout << 3654;
    for (int i = 0; i < 3654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3654(int& val) {
    const int x = 3654;
    std::cout << 3654;
    for (int i = 0; i < 3654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3655(int& val) {
    func_in_3655(val);
    const int x = 3655;
    std::cout << 3655;
    for (int i = 0; i < 3655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3655(int& val) {
    const int x = 3655;
    std::cout << 3655;
    for (int i = 0; i < 3655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3656(int& val) {
    func_in_3656(val);
    const int x = 3656;
    std::cout << 3656;
    for (int i = 0; i < 3656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3656(int& val) {
    const int x = 3656;
    std::cout << 3656;
    for (int i = 0; i < 3656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3657(int& val) {
    func_in_3657(val);
    const int x = 3657;
    std::cout << 3657;
    for (int i = 0; i < 3657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3657(int& val) {
    const int x = 3657;
    std::cout << 3657;
    for (int i = 0; i < 3657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3658(int& val) {
    func_in_3658(val);
    const int x = 3658;
    std::cout << 3658;
    for (int i = 0; i < 3658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3658(int& val) {
    const int x = 3658;
    std::cout << 3658;
    for (int i = 0; i < 3658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3659(int& val) {
    func_in_3659(val);
    const int x = 3659;
    std::cout << 3659;
    for (int i = 0; i < 3659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3659(int& val) {
    const int x = 3659;
    std::cout << 3659;
    for (int i = 0; i < 3659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3660(int& val) {
    func_in_3660(val);
    const int x = 3660;
    std::cout << 3660;
    for (int i = 0; i < 3660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3660(int& val) {
    const int x = 3660;
    std::cout << 3660;
    for (int i = 0; i < 3660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3661(int& val) {
    func_in_3661(val);
    const int x = 3661;
    std::cout << 3661;
    for (int i = 0; i < 3661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3661(int& val) {
    const int x = 3661;
    std::cout << 3661;
    for (int i = 0; i < 3661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3662(int& val) {
    func_in_3662(val);
    const int x = 3662;
    std::cout << 3662;
    for (int i = 0; i < 3662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3662(int& val) {
    const int x = 3662;
    std::cout << 3662;
    for (int i = 0; i < 3662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3663(int& val) {
    func_in_3663(val);
    const int x = 3663;
    std::cout << 3663;
    for (int i = 0; i < 3663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3663(int& val) {
    const int x = 3663;
    std::cout << 3663;
    for (int i = 0; i < 3663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3664(int& val) {
    func_in_3664(val);
    const int x = 3664;
    std::cout << 3664;
    for (int i = 0; i < 3664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3664(int& val) {
    const int x = 3664;
    std::cout << 3664;
    for (int i = 0; i < 3664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3665(int& val) {
    func_in_3665(val);
    const int x = 3665;
    std::cout << 3665;
    for (int i = 0; i < 3665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3665(int& val) {
    const int x = 3665;
    std::cout << 3665;
    for (int i = 0; i < 3665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3666(int& val) {
    func_in_3666(val);
    const int x = 3666;
    std::cout << 3666;
    for (int i = 0; i < 3666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3666(int& val) {
    const int x = 3666;
    std::cout << 3666;
    for (int i = 0; i < 3666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3667(int& val) {
    func_in_3667(val);
    const int x = 3667;
    std::cout << 3667;
    for (int i = 0; i < 3667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3667(int& val) {
    const int x = 3667;
    std::cout << 3667;
    for (int i = 0; i < 3667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3668(int& val) {
    func_in_3668(val);
    const int x = 3668;
    std::cout << 3668;
    for (int i = 0; i < 3668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3668(int& val) {
    const int x = 3668;
    std::cout << 3668;
    for (int i = 0; i < 3668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3669(int& val) {
    func_in_3669(val);
    const int x = 3669;
    std::cout << 3669;
    for (int i = 0; i < 3669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3669(int& val) {
    const int x = 3669;
    std::cout << 3669;
    for (int i = 0; i < 3669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3670(int& val) {
    func_in_3670(val);
    const int x = 3670;
    std::cout << 3670;
    for (int i = 0; i < 3670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3670(int& val) {
    const int x = 3670;
    std::cout << 3670;
    for (int i = 0; i < 3670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3671(int& val) {
    func_in_3671(val);
    const int x = 3671;
    std::cout << 3671;
    for (int i = 0; i < 3671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3671(int& val) {
    const int x = 3671;
    std::cout << 3671;
    for (int i = 0; i < 3671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3672(int& val) {
    func_in_3672(val);
    const int x = 3672;
    std::cout << 3672;
    for (int i = 0; i < 3672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3672(int& val) {
    const int x = 3672;
    std::cout << 3672;
    for (int i = 0; i < 3672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3673(int& val) {
    func_in_3673(val);
    const int x = 3673;
    std::cout << 3673;
    for (int i = 0; i < 3673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3673(int& val) {
    const int x = 3673;
    std::cout << 3673;
    for (int i = 0; i < 3673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3674(int& val) {
    func_in_3674(val);
    const int x = 3674;
    std::cout << 3674;
    for (int i = 0; i < 3674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3674(int& val) {
    const int x = 3674;
    std::cout << 3674;
    for (int i = 0; i < 3674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3675(int& val) {
    func_in_3675(val);
    const int x = 3675;
    std::cout << 3675;
    for (int i = 0; i < 3675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3675(int& val) {
    const int x = 3675;
    std::cout << 3675;
    for (int i = 0; i < 3675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3676(int& val) {
    func_in_3676(val);
    const int x = 3676;
    std::cout << 3676;
    for (int i = 0; i < 3676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3676(int& val) {
    const int x = 3676;
    std::cout << 3676;
    for (int i = 0; i < 3676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3677(int& val) {
    func_in_3677(val);
    const int x = 3677;
    std::cout << 3677;
    for (int i = 0; i < 3677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3677(int& val) {
    const int x = 3677;
    std::cout << 3677;
    for (int i = 0; i < 3677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3678(int& val) {
    func_in_3678(val);
    const int x = 3678;
    std::cout << 3678;
    for (int i = 0; i < 3678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3678(int& val) {
    const int x = 3678;
    std::cout << 3678;
    for (int i = 0; i < 3678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3679(int& val) {
    func_in_3679(val);
    const int x = 3679;
    std::cout << 3679;
    for (int i = 0; i < 3679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3679(int& val) {
    const int x = 3679;
    std::cout << 3679;
    for (int i = 0; i < 3679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3680(int& val) {
    func_in_3680(val);
    const int x = 3680;
    std::cout << 3680;
    for (int i = 0; i < 3680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3680(int& val) {
    const int x = 3680;
    std::cout << 3680;
    for (int i = 0; i < 3680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3681(int& val) {
    func_in_3681(val);
    const int x = 3681;
    std::cout << 3681;
    for (int i = 0; i < 3681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3681(int& val) {
    const int x = 3681;
    std::cout << 3681;
    for (int i = 0; i < 3681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3682(int& val) {
    func_in_3682(val);
    const int x = 3682;
    std::cout << 3682;
    for (int i = 0; i < 3682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3682(int& val) {
    const int x = 3682;
    std::cout << 3682;
    for (int i = 0; i < 3682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3683(int& val) {
    func_in_3683(val);
    const int x = 3683;
    std::cout << 3683;
    for (int i = 0; i < 3683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3683(int& val) {
    const int x = 3683;
    std::cout << 3683;
    for (int i = 0; i < 3683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3684(int& val) {
    func_in_3684(val);
    const int x = 3684;
    std::cout << 3684;
    for (int i = 0; i < 3684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3684(int& val) {
    const int x = 3684;
    std::cout << 3684;
    for (int i = 0; i < 3684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3685(int& val) {
    func_in_3685(val);
    const int x = 3685;
    std::cout << 3685;
    for (int i = 0; i < 3685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3685(int& val) {
    const int x = 3685;
    std::cout << 3685;
    for (int i = 0; i < 3685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3686(int& val) {
    func_in_3686(val);
    const int x = 3686;
    std::cout << 3686;
    for (int i = 0; i < 3686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3686(int& val) {
    const int x = 3686;
    std::cout << 3686;
    for (int i = 0; i < 3686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3687(int& val) {
    func_in_3687(val);
    const int x = 3687;
    std::cout << 3687;
    for (int i = 0; i < 3687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3687(int& val) {
    const int x = 3687;
    std::cout << 3687;
    for (int i = 0; i < 3687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3688(int& val) {
    func_in_3688(val);
    const int x = 3688;
    std::cout << 3688;
    for (int i = 0; i < 3688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3688(int& val) {
    const int x = 3688;
    std::cout << 3688;
    for (int i = 0; i < 3688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3689(int& val) {
    func_in_3689(val);
    const int x = 3689;
    std::cout << 3689;
    for (int i = 0; i < 3689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3689(int& val) {
    const int x = 3689;
    std::cout << 3689;
    for (int i = 0; i < 3689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3690(int& val) {
    func_in_3690(val);
    const int x = 3690;
    std::cout << 3690;
    for (int i = 0; i < 3690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3690(int& val) {
    const int x = 3690;
    std::cout << 3690;
    for (int i = 0; i < 3690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3691(int& val) {
    func_in_3691(val);
    const int x = 3691;
    std::cout << 3691;
    for (int i = 0; i < 3691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3691(int& val) {
    const int x = 3691;
    std::cout << 3691;
    for (int i = 0; i < 3691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3692(int& val) {
    func_in_3692(val);
    const int x = 3692;
    std::cout << 3692;
    for (int i = 0; i < 3692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3692(int& val) {
    const int x = 3692;
    std::cout << 3692;
    for (int i = 0; i < 3692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3693(int& val) {
    func_in_3693(val);
    const int x = 3693;
    std::cout << 3693;
    for (int i = 0; i < 3693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3693(int& val) {
    const int x = 3693;
    std::cout << 3693;
    for (int i = 0; i < 3693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3694(int& val) {
    func_in_3694(val);
    const int x = 3694;
    std::cout << 3694;
    for (int i = 0; i < 3694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3694(int& val) {
    const int x = 3694;
    std::cout << 3694;
    for (int i = 0; i < 3694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3695(int& val) {
    func_in_3695(val);
    const int x = 3695;
    std::cout << 3695;
    for (int i = 0; i < 3695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3695(int& val) {
    const int x = 3695;
    std::cout << 3695;
    for (int i = 0; i < 3695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3696(int& val) {
    func_in_3696(val);
    const int x = 3696;
    std::cout << 3696;
    for (int i = 0; i < 3696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3696(int& val) {
    const int x = 3696;
    std::cout << 3696;
    for (int i = 0; i < 3696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3697(int& val) {
    func_in_3697(val);
    const int x = 3697;
    std::cout << 3697;
    for (int i = 0; i < 3697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3697(int& val) {
    const int x = 3697;
    std::cout << 3697;
    for (int i = 0; i < 3697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3698(int& val) {
    func_in_3698(val);
    const int x = 3698;
    std::cout << 3698;
    for (int i = 0; i < 3698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3698(int& val) {
    const int x = 3698;
    std::cout << 3698;
    for (int i = 0; i < 3698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3699(int& val) {
    func_in_3699(val);
    const int x = 3699;
    std::cout << 3699;
    for (int i = 0; i < 3699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3699(int& val) {
    const int x = 3699;
    std::cout << 3699;
    for (int i = 0; i < 3699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3700(int& val) {
    func_in_3700(val);
    const int x = 3700;
    std::cout << 3700;
    for (int i = 0; i < 3700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3700(int& val) {
    const int x = 3700;
    std::cout << 3700;
    for (int i = 0; i < 3700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3701(int& val) {
    func_in_3701(val);
    const int x = 3701;
    std::cout << 3701;
    for (int i = 0; i < 3701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3701(int& val) {
    const int x = 3701;
    std::cout << 3701;
    for (int i = 0; i < 3701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3702(int& val) {
    func_in_3702(val);
    const int x = 3702;
    std::cout << 3702;
    for (int i = 0; i < 3702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3702(int& val) {
    const int x = 3702;
    std::cout << 3702;
    for (int i = 0; i < 3702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3703(int& val) {
    func_in_3703(val);
    const int x = 3703;
    std::cout << 3703;
    for (int i = 0; i < 3703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3703(int& val) {
    const int x = 3703;
    std::cout << 3703;
    for (int i = 0; i < 3703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3704(int& val) {
    func_in_3704(val);
    const int x = 3704;
    std::cout << 3704;
    for (int i = 0; i < 3704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3704(int& val) {
    const int x = 3704;
    std::cout << 3704;
    for (int i = 0; i < 3704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3705(int& val) {
    func_in_3705(val);
    const int x = 3705;
    std::cout << 3705;
    for (int i = 0; i < 3705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3705(int& val) {
    const int x = 3705;
    std::cout << 3705;
    for (int i = 0; i < 3705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3706(int& val) {
    func_in_3706(val);
    const int x = 3706;
    std::cout << 3706;
    for (int i = 0; i < 3706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3706(int& val) {
    const int x = 3706;
    std::cout << 3706;
    for (int i = 0; i < 3706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3707(int& val) {
    func_in_3707(val);
    const int x = 3707;
    std::cout << 3707;
    for (int i = 0; i < 3707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3707(int& val) {
    const int x = 3707;
    std::cout << 3707;
    for (int i = 0; i < 3707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3708(int& val) {
    func_in_3708(val);
    const int x = 3708;
    std::cout << 3708;
    for (int i = 0; i < 3708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3708(int& val) {
    const int x = 3708;
    std::cout << 3708;
    for (int i = 0; i < 3708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3709(int& val) {
    func_in_3709(val);
    const int x = 3709;
    std::cout << 3709;
    for (int i = 0; i < 3709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3709(int& val) {
    const int x = 3709;
    std::cout << 3709;
    for (int i = 0; i < 3709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3710(int& val) {
    func_in_3710(val);
    const int x = 3710;
    std::cout << 3710;
    for (int i = 0; i < 3710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3710(int& val) {
    const int x = 3710;
    std::cout << 3710;
    for (int i = 0; i < 3710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3711(int& val) {
    func_in_3711(val);
    const int x = 3711;
    std::cout << 3711;
    for (int i = 0; i < 3711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3711(int& val) {
    const int x = 3711;
    std::cout << 3711;
    for (int i = 0; i < 3711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3712(int& val) {
    func_in_3712(val);
    const int x = 3712;
    std::cout << 3712;
    for (int i = 0; i < 3712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3712(int& val) {
    const int x = 3712;
    std::cout << 3712;
    for (int i = 0; i < 3712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3713(int& val) {
    func_in_3713(val);
    const int x = 3713;
    std::cout << 3713;
    for (int i = 0; i < 3713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3713(int& val) {
    const int x = 3713;
    std::cout << 3713;
    for (int i = 0; i < 3713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3714(int& val) {
    func_in_3714(val);
    const int x = 3714;
    std::cout << 3714;
    for (int i = 0; i < 3714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3714(int& val) {
    const int x = 3714;
    std::cout << 3714;
    for (int i = 0; i < 3714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3715(int& val) {
    func_in_3715(val);
    const int x = 3715;
    std::cout << 3715;
    for (int i = 0; i < 3715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3715(int& val) {
    const int x = 3715;
    std::cout << 3715;
    for (int i = 0; i < 3715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3716(int& val) {
    func_in_3716(val);
    const int x = 3716;
    std::cout << 3716;
    for (int i = 0; i < 3716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3716(int& val) {
    const int x = 3716;
    std::cout << 3716;
    for (int i = 0; i < 3716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3717(int& val) {
    func_in_3717(val);
    const int x = 3717;
    std::cout << 3717;
    for (int i = 0; i < 3717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3717(int& val) {
    const int x = 3717;
    std::cout << 3717;
    for (int i = 0; i < 3717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3718(int& val) {
    func_in_3718(val);
    const int x = 3718;
    std::cout << 3718;
    for (int i = 0; i < 3718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3718(int& val) {
    const int x = 3718;
    std::cout << 3718;
    for (int i = 0; i < 3718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3719(int& val) {
    func_in_3719(val);
    const int x = 3719;
    std::cout << 3719;
    for (int i = 0; i < 3719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3719(int& val) {
    const int x = 3719;
    std::cout << 3719;
    for (int i = 0; i < 3719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3720(int& val) {
    func_in_3720(val);
    const int x = 3720;
    std::cout << 3720;
    for (int i = 0; i < 3720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3720(int& val) {
    const int x = 3720;
    std::cout << 3720;
    for (int i = 0; i < 3720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3721(int& val) {
    func_in_3721(val);
    const int x = 3721;
    std::cout << 3721;
    for (int i = 0; i < 3721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3721(int& val) {
    const int x = 3721;
    std::cout << 3721;
    for (int i = 0; i < 3721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3722(int& val) {
    func_in_3722(val);
    const int x = 3722;
    std::cout << 3722;
    for (int i = 0; i < 3722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3722(int& val) {
    const int x = 3722;
    std::cout << 3722;
    for (int i = 0; i < 3722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3723(int& val) {
    func_in_3723(val);
    const int x = 3723;
    std::cout << 3723;
    for (int i = 0; i < 3723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3723(int& val) {
    const int x = 3723;
    std::cout << 3723;
    for (int i = 0; i < 3723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3724(int& val) {
    func_in_3724(val);
    const int x = 3724;
    std::cout << 3724;
    for (int i = 0; i < 3724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3724(int& val) {
    const int x = 3724;
    std::cout << 3724;
    for (int i = 0; i < 3724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3725(int& val) {
    func_in_3725(val);
    const int x = 3725;
    std::cout << 3725;
    for (int i = 0; i < 3725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3725(int& val) {
    const int x = 3725;
    std::cout << 3725;
    for (int i = 0; i < 3725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3726(int& val) {
    func_in_3726(val);
    const int x = 3726;
    std::cout << 3726;
    for (int i = 0; i < 3726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3726(int& val) {
    const int x = 3726;
    std::cout << 3726;
    for (int i = 0; i < 3726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3727(int& val) {
    func_in_3727(val);
    const int x = 3727;
    std::cout << 3727;
    for (int i = 0; i < 3727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3727(int& val) {
    const int x = 3727;
    std::cout << 3727;
    for (int i = 0; i < 3727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3728(int& val) {
    func_in_3728(val);
    const int x = 3728;
    std::cout << 3728;
    for (int i = 0; i < 3728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3728(int& val) {
    const int x = 3728;
    std::cout << 3728;
    for (int i = 0; i < 3728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3729(int& val) {
    func_in_3729(val);
    const int x = 3729;
    std::cout << 3729;
    for (int i = 0; i < 3729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3729(int& val) {
    const int x = 3729;
    std::cout << 3729;
    for (int i = 0; i < 3729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3730(int& val) {
    func_in_3730(val);
    const int x = 3730;
    std::cout << 3730;
    for (int i = 0; i < 3730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3730(int& val) {
    const int x = 3730;
    std::cout << 3730;
    for (int i = 0; i < 3730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3731(int& val) {
    func_in_3731(val);
    const int x = 3731;
    std::cout << 3731;
    for (int i = 0; i < 3731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3731(int& val) {
    const int x = 3731;
    std::cout << 3731;
    for (int i = 0; i < 3731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3732(int& val) {
    func_in_3732(val);
    const int x = 3732;
    std::cout << 3732;
    for (int i = 0; i < 3732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3732(int& val) {
    const int x = 3732;
    std::cout << 3732;
    for (int i = 0; i < 3732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3733(int& val) {
    func_in_3733(val);
    const int x = 3733;
    std::cout << 3733;
    for (int i = 0; i < 3733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3733(int& val) {
    const int x = 3733;
    std::cout << 3733;
    for (int i = 0; i < 3733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3734(int& val) {
    func_in_3734(val);
    const int x = 3734;
    std::cout << 3734;
    for (int i = 0; i < 3734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3734(int& val) {
    const int x = 3734;
    std::cout << 3734;
    for (int i = 0; i < 3734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3735(int& val) {
    func_in_3735(val);
    const int x = 3735;
    std::cout << 3735;
    for (int i = 0; i < 3735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3735(int& val) {
    const int x = 3735;
    std::cout << 3735;
    for (int i = 0; i < 3735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3736(int& val) {
    func_in_3736(val);
    const int x = 3736;
    std::cout << 3736;
    for (int i = 0; i < 3736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3736(int& val) {
    const int x = 3736;
    std::cout << 3736;
    for (int i = 0; i < 3736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3737(int& val) {
    func_in_3737(val);
    const int x = 3737;
    std::cout << 3737;
    for (int i = 0; i < 3737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3737(int& val) {
    const int x = 3737;
    std::cout << 3737;
    for (int i = 0; i < 3737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3738(int& val) {
    func_in_3738(val);
    const int x = 3738;
    std::cout << 3738;
    for (int i = 0; i < 3738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3738(int& val) {
    const int x = 3738;
    std::cout << 3738;
    for (int i = 0; i < 3738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3739(int& val) {
    func_in_3739(val);
    const int x = 3739;
    std::cout << 3739;
    for (int i = 0; i < 3739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3739(int& val) {
    const int x = 3739;
    std::cout << 3739;
    for (int i = 0; i < 3739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3740(int& val) {
    func_in_3740(val);
    const int x = 3740;
    std::cout << 3740;
    for (int i = 0; i < 3740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3740(int& val) {
    const int x = 3740;
    std::cout << 3740;
    for (int i = 0; i < 3740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3741(int& val) {
    func_in_3741(val);
    const int x = 3741;
    std::cout << 3741;
    for (int i = 0; i < 3741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3741(int& val) {
    const int x = 3741;
    std::cout << 3741;
    for (int i = 0; i < 3741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3742(int& val) {
    func_in_3742(val);
    const int x = 3742;
    std::cout << 3742;
    for (int i = 0; i < 3742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3742(int& val) {
    const int x = 3742;
    std::cout << 3742;
    for (int i = 0; i < 3742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3743(int& val) {
    func_in_3743(val);
    const int x = 3743;
    std::cout << 3743;
    for (int i = 0; i < 3743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3743(int& val) {
    const int x = 3743;
    std::cout << 3743;
    for (int i = 0; i < 3743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3744(int& val) {
    func_in_3744(val);
    const int x = 3744;
    std::cout << 3744;
    for (int i = 0; i < 3744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3744(int& val) {
    const int x = 3744;
    std::cout << 3744;
    for (int i = 0; i < 3744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3745(int& val) {
    func_in_3745(val);
    const int x = 3745;
    std::cout << 3745;
    for (int i = 0; i < 3745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3745(int& val) {
    const int x = 3745;
    std::cout << 3745;
    for (int i = 0; i < 3745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3746(int& val) {
    func_in_3746(val);
    const int x = 3746;
    std::cout << 3746;
    for (int i = 0; i < 3746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3746(int& val) {
    const int x = 3746;
    std::cout << 3746;
    for (int i = 0; i < 3746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3747(int& val) {
    func_in_3747(val);
    const int x = 3747;
    std::cout << 3747;
    for (int i = 0; i < 3747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3747(int& val) {
    const int x = 3747;
    std::cout << 3747;
    for (int i = 0; i < 3747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3748(int& val) {
    func_in_3748(val);
    const int x = 3748;
    std::cout << 3748;
    for (int i = 0; i < 3748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3748(int& val) {
    const int x = 3748;
    std::cout << 3748;
    for (int i = 0; i < 3748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3749(int& val) {
    func_in_3749(val);
    const int x = 3749;
    std::cout << 3749;
    for (int i = 0; i < 3749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3749(int& val) {
    const int x = 3749;
    std::cout << 3749;
    for (int i = 0; i < 3749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3750(int& val) {
    func_in_3750(val);
    const int x = 3750;
    std::cout << 3750;
    for (int i = 0; i < 3750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3750(int& val) {
    const int x = 3750;
    std::cout << 3750;
    for (int i = 0; i < 3750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3751(int& val) {
    func_in_3751(val);
    const int x = 3751;
    std::cout << 3751;
    for (int i = 0; i < 3751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3751(int& val) {
    const int x = 3751;
    std::cout << 3751;
    for (int i = 0; i < 3751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3752(int& val) {
    func_in_3752(val);
    const int x = 3752;
    std::cout << 3752;
    for (int i = 0; i < 3752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3752(int& val) {
    const int x = 3752;
    std::cout << 3752;
    for (int i = 0; i < 3752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3753(int& val) {
    func_in_3753(val);
    const int x = 3753;
    std::cout << 3753;
    for (int i = 0; i < 3753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3753(int& val) {
    const int x = 3753;
    std::cout << 3753;
    for (int i = 0; i < 3753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3754(int& val) {
    func_in_3754(val);
    const int x = 3754;
    std::cout << 3754;
    for (int i = 0; i < 3754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3754(int& val) {
    const int x = 3754;
    std::cout << 3754;
    for (int i = 0; i < 3754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3755(int& val) {
    func_in_3755(val);
    const int x = 3755;
    std::cout << 3755;
    for (int i = 0; i < 3755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3755(int& val) {
    const int x = 3755;
    std::cout << 3755;
    for (int i = 0; i < 3755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3756(int& val) {
    func_in_3756(val);
    const int x = 3756;
    std::cout << 3756;
    for (int i = 0; i < 3756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3756(int& val) {
    const int x = 3756;
    std::cout << 3756;
    for (int i = 0; i < 3756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3757(int& val) {
    func_in_3757(val);
    const int x = 3757;
    std::cout << 3757;
    for (int i = 0; i < 3757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3757(int& val) {
    const int x = 3757;
    std::cout << 3757;
    for (int i = 0; i < 3757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3758(int& val) {
    func_in_3758(val);
    const int x = 3758;
    std::cout << 3758;
    for (int i = 0; i < 3758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3758(int& val) {
    const int x = 3758;
    std::cout << 3758;
    for (int i = 0; i < 3758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3759(int& val) {
    func_in_3759(val);
    const int x = 3759;
    std::cout << 3759;
    for (int i = 0; i < 3759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3759(int& val) {
    const int x = 3759;
    std::cout << 3759;
    for (int i = 0; i < 3759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3760(int& val) {
    func_in_3760(val);
    const int x = 3760;
    std::cout << 3760;
    for (int i = 0; i < 3760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3760(int& val) {
    const int x = 3760;
    std::cout << 3760;
    for (int i = 0; i < 3760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3761(int& val) {
    func_in_3761(val);
    const int x = 3761;
    std::cout << 3761;
    for (int i = 0; i < 3761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3761(int& val) {
    const int x = 3761;
    std::cout << 3761;
    for (int i = 0; i < 3761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3762(int& val) {
    func_in_3762(val);
    const int x = 3762;
    std::cout << 3762;
    for (int i = 0; i < 3762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3762(int& val) {
    const int x = 3762;
    std::cout << 3762;
    for (int i = 0; i < 3762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3763(int& val) {
    func_in_3763(val);
    const int x = 3763;
    std::cout << 3763;
    for (int i = 0; i < 3763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3763(int& val) {
    const int x = 3763;
    std::cout << 3763;
    for (int i = 0; i < 3763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3764(int& val) {
    func_in_3764(val);
    const int x = 3764;
    std::cout << 3764;
    for (int i = 0; i < 3764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3764(int& val) {
    const int x = 3764;
    std::cout << 3764;
    for (int i = 0; i < 3764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3765(int& val) {
    func_in_3765(val);
    const int x = 3765;
    std::cout << 3765;
    for (int i = 0; i < 3765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3765(int& val) {
    const int x = 3765;
    std::cout << 3765;
    for (int i = 0; i < 3765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3766(int& val) {
    func_in_3766(val);
    const int x = 3766;
    std::cout << 3766;
    for (int i = 0; i < 3766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3766(int& val) {
    const int x = 3766;
    std::cout << 3766;
    for (int i = 0; i < 3766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3767(int& val) {
    func_in_3767(val);
    const int x = 3767;
    std::cout << 3767;
    for (int i = 0; i < 3767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3767(int& val) {
    const int x = 3767;
    std::cout << 3767;
    for (int i = 0; i < 3767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3768(int& val) {
    func_in_3768(val);
    const int x = 3768;
    std::cout << 3768;
    for (int i = 0; i < 3768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3768(int& val) {
    const int x = 3768;
    std::cout << 3768;
    for (int i = 0; i < 3768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3769(int& val) {
    func_in_3769(val);
    const int x = 3769;
    std::cout << 3769;
    for (int i = 0; i < 3769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3769(int& val) {
    const int x = 3769;
    std::cout << 3769;
    for (int i = 0; i < 3769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3770(int& val) {
    func_in_3770(val);
    const int x = 3770;
    std::cout << 3770;
    for (int i = 0; i < 3770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3770(int& val) {
    const int x = 3770;
    std::cout << 3770;
    for (int i = 0; i < 3770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3771(int& val) {
    func_in_3771(val);
    const int x = 3771;
    std::cout << 3771;
    for (int i = 0; i < 3771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3771(int& val) {
    const int x = 3771;
    std::cout << 3771;
    for (int i = 0; i < 3771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3772(int& val) {
    func_in_3772(val);
    const int x = 3772;
    std::cout << 3772;
    for (int i = 0; i < 3772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3772(int& val) {
    const int x = 3772;
    std::cout << 3772;
    for (int i = 0; i < 3772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3773(int& val) {
    func_in_3773(val);
    const int x = 3773;
    std::cout << 3773;
    for (int i = 0; i < 3773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3773(int& val) {
    const int x = 3773;
    std::cout << 3773;
    for (int i = 0; i < 3773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3774(int& val) {
    func_in_3774(val);
    const int x = 3774;
    std::cout << 3774;
    for (int i = 0; i < 3774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3774(int& val) {
    const int x = 3774;
    std::cout << 3774;
    for (int i = 0; i < 3774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3775(int& val) {
    func_in_3775(val);
    const int x = 3775;
    std::cout << 3775;
    for (int i = 0; i < 3775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3775(int& val) {
    const int x = 3775;
    std::cout << 3775;
    for (int i = 0; i < 3775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3776(int& val) {
    func_in_3776(val);
    const int x = 3776;
    std::cout << 3776;
    for (int i = 0; i < 3776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3776(int& val) {
    const int x = 3776;
    std::cout << 3776;
    for (int i = 0; i < 3776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3777(int& val) {
    func_in_3777(val);
    const int x = 3777;
    std::cout << 3777;
    for (int i = 0; i < 3777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3777(int& val) {
    const int x = 3777;
    std::cout << 3777;
    for (int i = 0; i < 3777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3778(int& val) {
    func_in_3778(val);
    const int x = 3778;
    std::cout << 3778;
    for (int i = 0; i < 3778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3778(int& val) {
    const int x = 3778;
    std::cout << 3778;
    for (int i = 0; i < 3778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3779(int& val) {
    func_in_3779(val);
    const int x = 3779;
    std::cout << 3779;
    for (int i = 0; i < 3779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3779(int& val) {
    const int x = 3779;
    std::cout << 3779;
    for (int i = 0; i < 3779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3780(int& val) {
    func_in_3780(val);
    const int x = 3780;
    std::cout << 3780;
    for (int i = 0; i < 3780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3780(int& val) {
    const int x = 3780;
    std::cout << 3780;
    for (int i = 0; i < 3780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3781(int& val) {
    func_in_3781(val);
    const int x = 3781;
    std::cout << 3781;
    for (int i = 0; i < 3781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3781(int& val) {
    const int x = 3781;
    std::cout << 3781;
    for (int i = 0; i < 3781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3782(int& val) {
    func_in_3782(val);
    const int x = 3782;
    std::cout << 3782;
    for (int i = 0; i < 3782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3782(int& val) {
    const int x = 3782;
    std::cout << 3782;
    for (int i = 0; i < 3782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3783(int& val) {
    func_in_3783(val);
    const int x = 3783;
    std::cout << 3783;
    for (int i = 0; i < 3783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3783(int& val) {
    const int x = 3783;
    std::cout << 3783;
    for (int i = 0; i < 3783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3784(int& val) {
    func_in_3784(val);
    const int x = 3784;
    std::cout << 3784;
    for (int i = 0; i < 3784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3784(int& val) {
    const int x = 3784;
    std::cout << 3784;
    for (int i = 0; i < 3784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3785(int& val) {
    func_in_3785(val);
    const int x = 3785;
    std::cout << 3785;
    for (int i = 0; i < 3785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3785(int& val) {
    const int x = 3785;
    std::cout << 3785;
    for (int i = 0; i < 3785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3786(int& val) {
    func_in_3786(val);
    const int x = 3786;
    std::cout << 3786;
    for (int i = 0; i < 3786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3786(int& val) {
    const int x = 3786;
    std::cout << 3786;
    for (int i = 0; i < 3786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3787(int& val) {
    func_in_3787(val);
    const int x = 3787;
    std::cout << 3787;
    for (int i = 0; i < 3787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3787(int& val) {
    const int x = 3787;
    std::cout << 3787;
    for (int i = 0; i < 3787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3788(int& val) {
    func_in_3788(val);
    const int x = 3788;
    std::cout << 3788;
    for (int i = 0; i < 3788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3788(int& val) {
    const int x = 3788;
    std::cout << 3788;
    for (int i = 0; i < 3788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3789(int& val) {
    func_in_3789(val);
    const int x = 3789;
    std::cout << 3789;
    for (int i = 0; i < 3789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3789(int& val) {
    const int x = 3789;
    std::cout << 3789;
    for (int i = 0; i < 3789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3790(int& val) {
    func_in_3790(val);
    const int x = 3790;
    std::cout << 3790;
    for (int i = 0; i < 3790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3790(int& val) {
    const int x = 3790;
    std::cout << 3790;
    for (int i = 0; i < 3790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3791(int& val) {
    func_in_3791(val);
    const int x = 3791;
    std::cout << 3791;
    for (int i = 0; i < 3791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3791(int& val) {
    const int x = 3791;
    std::cout << 3791;
    for (int i = 0; i < 3791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3792(int& val) {
    func_in_3792(val);
    const int x = 3792;
    std::cout << 3792;
    for (int i = 0; i < 3792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3792(int& val) {
    const int x = 3792;
    std::cout << 3792;
    for (int i = 0; i < 3792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3793(int& val) {
    func_in_3793(val);
    const int x = 3793;
    std::cout << 3793;
    for (int i = 0; i < 3793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3793(int& val) {
    const int x = 3793;
    std::cout << 3793;
    for (int i = 0; i < 3793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3794(int& val) {
    func_in_3794(val);
    const int x = 3794;
    std::cout << 3794;
    for (int i = 0; i < 3794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3794(int& val) {
    const int x = 3794;
    std::cout << 3794;
    for (int i = 0; i < 3794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3795(int& val) {
    func_in_3795(val);
    const int x = 3795;
    std::cout << 3795;
    for (int i = 0; i < 3795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3795(int& val) {
    const int x = 3795;
    std::cout << 3795;
    for (int i = 0; i < 3795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3796(int& val) {
    func_in_3796(val);
    const int x = 3796;
    std::cout << 3796;
    for (int i = 0; i < 3796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3796(int& val) {
    const int x = 3796;
    std::cout << 3796;
    for (int i = 0; i < 3796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3797(int& val) {
    func_in_3797(val);
    const int x = 3797;
    std::cout << 3797;
    for (int i = 0; i < 3797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3797(int& val) {
    const int x = 3797;
    std::cout << 3797;
    for (int i = 0; i < 3797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3798(int& val) {
    func_in_3798(val);
    const int x = 3798;
    std::cout << 3798;
    for (int i = 0; i < 3798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3798(int& val) {
    const int x = 3798;
    std::cout << 3798;
    for (int i = 0; i < 3798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3799(int& val) {
    func_in_3799(val);
    const int x = 3799;
    std::cout << 3799;
    for (int i = 0; i < 3799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3799(int& val) {
    const int x = 3799;
    std::cout << 3799;
    for (int i = 0; i < 3799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3800(int& val) {
    func_in_3800(val);
    const int x = 3800;
    std::cout << 3800;
    for (int i = 0; i < 3800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3800(int& val) {
    const int x = 3800;
    std::cout << 3800;
    for (int i = 0; i < 3800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3801(int& val) {
    func_in_3801(val);
    const int x = 3801;
    std::cout << 3801;
    for (int i = 0; i < 3801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3801(int& val) {
    const int x = 3801;
    std::cout << 3801;
    for (int i = 0; i < 3801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3802(int& val) {
    func_in_3802(val);
    const int x = 3802;
    std::cout << 3802;
    for (int i = 0; i < 3802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3802(int& val) {
    const int x = 3802;
    std::cout << 3802;
    for (int i = 0; i < 3802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3803(int& val) {
    func_in_3803(val);
    const int x = 3803;
    std::cout << 3803;
    for (int i = 0; i < 3803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3803(int& val) {
    const int x = 3803;
    std::cout << 3803;
    for (int i = 0; i < 3803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3804(int& val) {
    func_in_3804(val);
    const int x = 3804;
    std::cout << 3804;
    for (int i = 0; i < 3804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3804(int& val) {
    const int x = 3804;
    std::cout << 3804;
    for (int i = 0; i < 3804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3805(int& val) {
    func_in_3805(val);
    const int x = 3805;
    std::cout << 3805;
    for (int i = 0; i < 3805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3805(int& val) {
    const int x = 3805;
    std::cout << 3805;
    for (int i = 0; i < 3805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3806(int& val) {
    func_in_3806(val);
    const int x = 3806;
    std::cout << 3806;
    for (int i = 0; i < 3806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3806(int& val) {
    const int x = 3806;
    std::cout << 3806;
    for (int i = 0; i < 3806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3807(int& val) {
    func_in_3807(val);
    const int x = 3807;
    std::cout << 3807;
    for (int i = 0; i < 3807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3807(int& val) {
    const int x = 3807;
    std::cout << 3807;
    for (int i = 0; i < 3807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3808(int& val) {
    func_in_3808(val);
    const int x = 3808;
    std::cout << 3808;
    for (int i = 0; i < 3808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3808(int& val) {
    const int x = 3808;
    std::cout << 3808;
    for (int i = 0; i < 3808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3809(int& val) {
    func_in_3809(val);
    const int x = 3809;
    std::cout << 3809;
    for (int i = 0; i < 3809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3809(int& val) {
    const int x = 3809;
    std::cout << 3809;
    for (int i = 0; i < 3809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3810(int& val) {
    func_in_3810(val);
    const int x = 3810;
    std::cout << 3810;
    for (int i = 0; i < 3810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3810(int& val) {
    const int x = 3810;
    std::cout << 3810;
    for (int i = 0; i < 3810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3811(int& val) {
    func_in_3811(val);
    const int x = 3811;
    std::cout << 3811;
    for (int i = 0; i < 3811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3811(int& val) {
    const int x = 3811;
    std::cout << 3811;
    for (int i = 0; i < 3811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3812(int& val) {
    func_in_3812(val);
    const int x = 3812;
    std::cout << 3812;
    for (int i = 0; i < 3812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3812(int& val) {
    const int x = 3812;
    std::cout << 3812;
    for (int i = 0; i < 3812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3813(int& val) {
    func_in_3813(val);
    const int x = 3813;
    std::cout << 3813;
    for (int i = 0; i < 3813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3813(int& val) {
    const int x = 3813;
    std::cout << 3813;
    for (int i = 0; i < 3813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3814(int& val) {
    func_in_3814(val);
    const int x = 3814;
    std::cout << 3814;
    for (int i = 0; i < 3814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3814(int& val) {
    const int x = 3814;
    std::cout << 3814;
    for (int i = 0; i < 3814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3815(int& val) {
    func_in_3815(val);
    const int x = 3815;
    std::cout << 3815;
    for (int i = 0; i < 3815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3815(int& val) {
    const int x = 3815;
    std::cout << 3815;
    for (int i = 0; i < 3815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3816(int& val) {
    func_in_3816(val);
    const int x = 3816;
    std::cout << 3816;
    for (int i = 0; i < 3816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3816(int& val) {
    const int x = 3816;
    std::cout << 3816;
    for (int i = 0; i < 3816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3817(int& val) {
    func_in_3817(val);
    const int x = 3817;
    std::cout << 3817;
    for (int i = 0; i < 3817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3817(int& val) {
    const int x = 3817;
    std::cout << 3817;
    for (int i = 0; i < 3817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3818(int& val) {
    func_in_3818(val);
    const int x = 3818;
    std::cout << 3818;
    for (int i = 0; i < 3818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3818(int& val) {
    const int x = 3818;
    std::cout << 3818;
    for (int i = 0; i < 3818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3819(int& val) {
    func_in_3819(val);
    const int x = 3819;
    std::cout << 3819;
    for (int i = 0; i < 3819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3819(int& val) {
    const int x = 3819;
    std::cout << 3819;
    for (int i = 0; i < 3819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3820(int& val) {
    func_in_3820(val);
    const int x = 3820;
    std::cout << 3820;
    for (int i = 0; i < 3820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3820(int& val) {
    const int x = 3820;
    std::cout << 3820;
    for (int i = 0; i < 3820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3821(int& val) {
    func_in_3821(val);
    const int x = 3821;
    std::cout << 3821;
    for (int i = 0; i < 3821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3821(int& val) {
    const int x = 3821;
    std::cout << 3821;
    for (int i = 0; i < 3821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3822(int& val) {
    func_in_3822(val);
    const int x = 3822;
    std::cout << 3822;
    for (int i = 0; i < 3822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3822(int& val) {
    const int x = 3822;
    std::cout << 3822;
    for (int i = 0; i < 3822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3823(int& val) {
    func_in_3823(val);
    const int x = 3823;
    std::cout << 3823;
    for (int i = 0; i < 3823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3823(int& val) {
    const int x = 3823;
    std::cout << 3823;
    for (int i = 0; i < 3823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3824(int& val) {
    func_in_3824(val);
    const int x = 3824;
    std::cout << 3824;
    for (int i = 0; i < 3824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3824(int& val) {
    const int x = 3824;
    std::cout << 3824;
    for (int i = 0; i < 3824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3825(int& val) {
    func_in_3825(val);
    const int x = 3825;
    std::cout << 3825;
    for (int i = 0; i < 3825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3825(int& val) {
    const int x = 3825;
    std::cout << 3825;
    for (int i = 0; i < 3825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3826(int& val) {
    func_in_3826(val);
    const int x = 3826;
    std::cout << 3826;
    for (int i = 0; i < 3826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3826(int& val) {
    const int x = 3826;
    std::cout << 3826;
    for (int i = 0; i < 3826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3827(int& val) {
    func_in_3827(val);
    const int x = 3827;
    std::cout << 3827;
    for (int i = 0; i < 3827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3827(int& val) {
    const int x = 3827;
    std::cout << 3827;
    for (int i = 0; i < 3827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3828(int& val) {
    func_in_3828(val);
    const int x = 3828;
    std::cout << 3828;
    for (int i = 0; i < 3828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3828(int& val) {
    const int x = 3828;
    std::cout << 3828;
    for (int i = 0; i < 3828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3829(int& val) {
    func_in_3829(val);
    const int x = 3829;
    std::cout << 3829;
    for (int i = 0; i < 3829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3829(int& val) {
    const int x = 3829;
    std::cout << 3829;
    for (int i = 0; i < 3829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3830(int& val) {
    func_in_3830(val);
    const int x = 3830;
    std::cout << 3830;
    for (int i = 0; i < 3830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3830(int& val) {
    const int x = 3830;
    std::cout << 3830;
    for (int i = 0; i < 3830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3831(int& val) {
    func_in_3831(val);
    const int x = 3831;
    std::cout << 3831;
    for (int i = 0; i < 3831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3831(int& val) {
    const int x = 3831;
    std::cout << 3831;
    for (int i = 0; i < 3831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3832(int& val) {
    func_in_3832(val);
    const int x = 3832;
    std::cout << 3832;
    for (int i = 0; i < 3832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3832(int& val) {
    const int x = 3832;
    std::cout << 3832;
    for (int i = 0; i < 3832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3833(int& val) {
    func_in_3833(val);
    const int x = 3833;
    std::cout << 3833;
    for (int i = 0; i < 3833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3833(int& val) {
    const int x = 3833;
    std::cout << 3833;
    for (int i = 0; i < 3833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3834(int& val) {
    func_in_3834(val);
    const int x = 3834;
    std::cout << 3834;
    for (int i = 0; i < 3834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3834(int& val) {
    const int x = 3834;
    std::cout << 3834;
    for (int i = 0; i < 3834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3835(int& val) {
    func_in_3835(val);
    const int x = 3835;
    std::cout << 3835;
    for (int i = 0; i < 3835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3835(int& val) {
    const int x = 3835;
    std::cout << 3835;
    for (int i = 0; i < 3835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3836(int& val) {
    func_in_3836(val);
    const int x = 3836;
    std::cout << 3836;
    for (int i = 0; i < 3836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3836(int& val) {
    const int x = 3836;
    std::cout << 3836;
    for (int i = 0; i < 3836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3837(int& val) {
    func_in_3837(val);
    const int x = 3837;
    std::cout << 3837;
    for (int i = 0; i < 3837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3837(int& val) {
    const int x = 3837;
    std::cout << 3837;
    for (int i = 0; i < 3837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3838(int& val) {
    func_in_3838(val);
    const int x = 3838;
    std::cout << 3838;
    for (int i = 0; i < 3838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3838(int& val) {
    const int x = 3838;
    std::cout << 3838;
    for (int i = 0; i < 3838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3839(int& val) {
    func_in_3839(val);
    const int x = 3839;
    std::cout << 3839;
    for (int i = 0; i < 3839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3839(int& val) {
    const int x = 3839;
    std::cout << 3839;
    for (int i = 0; i < 3839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3840(int& val) {
    func_in_3840(val);
    const int x = 3840;
    std::cout << 3840;
    for (int i = 0; i < 3840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3840(int& val) {
    const int x = 3840;
    std::cout << 3840;
    for (int i = 0; i < 3840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3841(int& val) {
    func_in_3841(val);
    const int x = 3841;
    std::cout << 3841;
    for (int i = 0; i < 3841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3841(int& val) {
    const int x = 3841;
    std::cout << 3841;
    for (int i = 0; i < 3841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3842(int& val) {
    func_in_3842(val);
    const int x = 3842;
    std::cout << 3842;
    for (int i = 0; i < 3842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3842(int& val) {
    const int x = 3842;
    std::cout << 3842;
    for (int i = 0; i < 3842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3843(int& val) {
    func_in_3843(val);
    const int x = 3843;
    std::cout << 3843;
    for (int i = 0; i < 3843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3843(int& val) {
    const int x = 3843;
    std::cout << 3843;
    for (int i = 0; i < 3843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3844(int& val) {
    func_in_3844(val);
    const int x = 3844;
    std::cout << 3844;
    for (int i = 0; i < 3844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3844(int& val) {
    const int x = 3844;
    std::cout << 3844;
    for (int i = 0; i < 3844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3845(int& val) {
    func_in_3845(val);
    const int x = 3845;
    std::cout << 3845;
    for (int i = 0; i < 3845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3845(int& val) {
    const int x = 3845;
    std::cout << 3845;
    for (int i = 0; i < 3845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3846(int& val) {
    func_in_3846(val);
    const int x = 3846;
    std::cout << 3846;
    for (int i = 0; i < 3846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3846(int& val) {
    const int x = 3846;
    std::cout << 3846;
    for (int i = 0; i < 3846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3847(int& val) {
    func_in_3847(val);
    const int x = 3847;
    std::cout << 3847;
    for (int i = 0; i < 3847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3847(int& val) {
    const int x = 3847;
    std::cout << 3847;
    for (int i = 0; i < 3847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3848(int& val) {
    func_in_3848(val);
    const int x = 3848;
    std::cout << 3848;
    for (int i = 0; i < 3848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3848(int& val) {
    const int x = 3848;
    std::cout << 3848;
    for (int i = 0; i < 3848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3849(int& val) {
    func_in_3849(val);
    const int x = 3849;
    std::cout << 3849;
    for (int i = 0; i < 3849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3849(int& val) {
    const int x = 3849;
    std::cout << 3849;
    for (int i = 0; i < 3849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3850(int& val) {
    func_in_3850(val);
    const int x = 3850;
    std::cout << 3850;
    for (int i = 0; i < 3850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3850(int& val) {
    const int x = 3850;
    std::cout << 3850;
    for (int i = 0; i < 3850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3851(int& val) {
    func_in_3851(val);
    const int x = 3851;
    std::cout << 3851;
    for (int i = 0; i < 3851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3851(int& val) {
    const int x = 3851;
    std::cout << 3851;
    for (int i = 0; i < 3851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3852(int& val) {
    func_in_3852(val);
    const int x = 3852;
    std::cout << 3852;
    for (int i = 0; i < 3852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3852(int& val) {
    const int x = 3852;
    std::cout << 3852;
    for (int i = 0; i < 3852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3853(int& val) {
    func_in_3853(val);
    const int x = 3853;
    std::cout << 3853;
    for (int i = 0; i < 3853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3853(int& val) {
    const int x = 3853;
    std::cout << 3853;
    for (int i = 0; i < 3853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3854(int& val) {
    func_in_3854(val);
    const int x = 3854;
    std::cout << 3854;
    for (int i = 0; i < 3854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3854(int& val) {
    const int x = 3854;
    std::cout << 3854;
    for (int i = 0; i < 3854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3855(int& val) {
    func_in_3855(val);
    const int x = 3855;
    std::cout << 3855;
    for (int i = 0; i < 3855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3855(int& val) {
    const int x = 3855;
    std::cout << 3855;
    for (int i = 0; i < 3855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3856(int& val) {
    func_in_3856(val);
    const int x = 3856;
    std::cout << 3856;
    for (int i = 0; i < 3856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3856(int& val) {
    const int x = 3856;
    std::cout << 3856;
    for (int i = 0; i < 3856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3857(int& val) {
    func_in_3857(val);
    const int x = 3857;
    std::cout << 3857;
    for (int i = 0; i < 3857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3857(int& val) {
    const int x = 3857;
    std::cout << 3857;
    for (int i = 0; i < 3857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3858(int& val) {
    func_in_3858(val);
    const int x = 3858;
    std::cout << 3858;
    for (int i = 0; i < 3858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3858(int& val) {
    const int x = 3858;
    std::cout << 3858;
    for (int i = 0; i < 3858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3859(int& val) {
    func_in_3859(val);
    const int x = 3859;
    std::cout << 3859;
    for (int i = 0; i < 3859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3859(int& val) {
    const int x = 3859;
    std::cout << 3859;
    for (int i = 0; i < 3859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3860(int& val) {
    func_in_3860(val);
    const int x = 3860;
    std::cout << 3860;
    for (int i = 0; i < 3860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3860(int& val) {
    const int x = 3860;
    std::cout << 3860;
    for (int i = 0; i < 3860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3861(int& val) {
    func_in_3861(val);
    const int x = 3861;
    std::cout << 3861;
    for (int i = 0; i < 3861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3861(int& val) {
    const int x = 3861;
    std::cout << 3861;
    for (int i = 0; i < 3861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3862(int& val) {
    func_in_3862(val);
    const int x = 3862;
    std::cout << 3862;
    for (int i = 0; i < 3862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3862(int& val) {
    const int x = 3862;
    std::cout << 3862;
    for (int i = 0; i < 3862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3863(int& val) {
    func_in_3863(val);
    const int x = 3863;
    std::cout << 3863;
    for (int i = 0; i < 3863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3863(int& val) {
    const int x = 3863;
    std::cout << 3863;
    for (int i = 0; i < 3863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3864(int& val) {
    func_in_3864(val);
    const int x = 3864;
    std::cout << 3864;
    for (int i = 0; i < 3864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3864(int& val) {
    const int x = 3864;
    std::cout << 3864;
    for (int i = 0; i < 3864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3865(int& val) {
    func_in_3865(val);
    const int x = 3865;
    std::cout << 3865;
    for (int i = 0; i < 3865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3865(int& val) {
    const int x = 3865;
    std::cout << 3865;
    for (int i = 0; i < 3865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3866(int& val) {
    func_in_3866(val);
    const int x = 3866;
    std::cout << 3866;
    for (int i = 0; i < 3866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3866(int& val) {
    const int x = 3866;
    std::cout << 3866;
    for (int i = 0; i < 3866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3867(int& val) {
    func_in_3867(val);
    const int x = 3867;
    std::cout << 3867;
    for (int i = 0; i < 3867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3867(int& val) {
    const int x = 3867;
    std::cout << 3867;
    for (int i = 0; i < 3867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3868(int& val) {
    func_in_3868(val);
    const int x = 3868;
    std::cout << 3868;
    for (int i = 0; i < 3868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3868(int& val) {
    const int x = 3868;
    std::cout << 3868;
    for (int i = 0; i < 3868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3869(int& val) {
    func_in_3869(val);
    const int x = 3869;
    std::cout << 3869;
    for (int i = 0; i < 3869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3869(int& val) {
    const int x = 3869;
    std::cout << 3869;
    for (int i = 0; i < 3869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3870(int& val) {
    func_in_3870(val);
    const int x = 3870;
    std::cout << 3870;
    for (int i = 0; i < 3870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3870(int& val) {
    const int x = 3870;
    std::cout << 3870;
    for (int i = 0; i < 3870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3871(int& val) {
    func_in_3871(val);
    const int x = 3871;
    std::cout << 3871;
    for (int i = 0; i < 3871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3871(int& val) {
    const int x = 3871;
    std::cout << 3871;
    for (int i = 0; i < 3871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3872(int& val) {
    func_in_3872(val);
    const int x = 3872;
    std::cout << 3872;
    for (int i = 0; i < 3872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3872(int& val) {
    const int x = 3872;
    std::cout << 3872;
    for (int i = 0; i < 3872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3873(int& val) {
    func_in_3873(val);
    const int x = 3873;
    std::cout << 3873;
    for (int i = 0; i < 3873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3873(int& val) {
    const int x = 3873;
    std::cout << 3873;
    for (int i = 0; i < 3873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3874(int& val) {
    func_in_3874(val);
    const int x = 3874;
    std::cout << 3874;
    for (int i = 0; i < 3874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3874(int& val) {
    const int x = 3874;
    std::cout << 3874;
    for (int i = 0; i < 3874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3875(int& val) {
    func_in_3875(val);
    const int x = 3875;
    std::cout << 3875;
    for (int i = 0; i < 3875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3875(int& val) {
    const int x = 3875;
    std::cout << 3875;
    for (int i = 0; i < 3875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3876(int& val) {
    func_in_3876(val);
    const int x = 3876;
    std::cout << 3876;
    for (int i = 0; i < 3876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3876(int& val) {
    const int x = 3876;
    std::cout << 3876;
    for (int i = 0; i < 3876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3877(int& val) {
    func_in_3877(val);
    const int x = 3877;
    std::cout << 3877;
    for (int i = 0; i < 3877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3877(int& val) {
    const int x = 3877;
    std::cout << 3877;
    for (int i = 0; i < 3877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3878(int& val) {
    func_in_3878(val);
    const int x = 3878;
    std::cout << 3878;
    for (int i = 0; i < 3878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3878(int& val) {
    const int x = 3878;
    std::cout << 3878;
    for (int i = 0; i < 3878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3879(int& val) {
    func_in_3879(val);
    const int x = 3879;
    std::cout << 3879;
    for (int i = 0; i < 3879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3879(int& val) {
    const int x = 3879;
    std::cout << 3879;
    for (int i = 0; i < 3879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3880(int& val) {
    func_in_3880(val);
    const int x = 3880;
    std::cout << 3880;
    for (int i = 0; i < 3880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3880(int& val) {
    const int x = 3880;
    std::cout << 3880;
    for (int i = 0; i < 3880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3881(int& val) {
    func_in_3881(val);
    const int x = 3881;
    std::cout << 3881;
    for (int i = 0; i < 3881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3881(int& val) {
    const int x = 3881;
    std::cout << 3881;
    for (int i = 0; i < 3881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3882(int& val) {
    func_in_3882(val);
    const int x = 3882;
    std::cout << 3882;
    for (int i = 0; i < 3882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3882(int& val) {
    const int x = 3882;
    std::cout << 3882;
    for (int i = 0; i < 3882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3883(int& val) {
    func_in_3883(val);
    const int x = 3883;
    std::cout << 3883;
    for (int i = 0; i < 3883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3883(int& val) {
    const int x = 3883;
    std::cout << 3883;
    for (int i = 0; i < 3883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3884(int& val) {
    func_in_3884(val);
    const int x = 3884;
    std::cout << 3884;
    for (int i = 0; i < 3884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3884(int& val) {
    const int x = 3884;
    std::cout << 3884;
    for (int i = 0; i < 3884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3885(int& val) {
    func_in_3885(val);
    const int x = 3885;
    std::cout << 3885;
    for (int i = 0; i < 3885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3885(int& val) {
    const int x = 3885;
    std::cout << 3885;
    for (int i = 0; i < 3885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3886(int& val) {
    func_in_3886(val);
    const int x = 3886;
    std::cout << 3886;
    for (int i = 0; i < 3886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3886(int& val) {
    const int x = 3886;
    std::cout << 3886;
    for (int i = 0; i < 3886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3887(int& val) {
    func_in_3887(val);
    const int x = 3887;
    std::cout << 3887;
    for (int i = 0; i < 3887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3887(int& val) {
    const int x = 3887;
    std::cout << 3887;
    for (int i = 0; i < 3887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3888(int& val) {
    func_in_3888(val);
    const int x = 3888;
    std::cout << 3888;
    for (int i = 0; i < 3888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3888(int& val) {
    const int x = 3888;
    std::cout << 3888;
    for (int i = 0; i < 3888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3889(int& val) {
    func_in_3889(val);
    const int x = 3889;
    std::cout << 3889;
    for (int i = 0; i < 3889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3889(int& val) {
    const int x = 3889;
    std::cout << 3889;
    for (int i = 0; i < 3889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3890(int& val) {
    func_in_3890(val);
    const int x = 3890;
    std::cout << 3890;
    for (int i = 0; i < 3890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3890(int& val) {
    const int x = 3890;
    std::cout << 3890;
    for (int i = 0; i < 3890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3891(int& val) {
    func_in_3891(val);
    const int x = 3891;
    std::cout << 3891;
    for (int i = 0; i < 3891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3891(int& val) {
    const int x = 3891;
    std::cout << 3891;
    for (int i = 0; i < 3891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3892(int& val) {
    func_in_3892(val);
    const int x = 3892;
    std::cout << 3892;
    for (int i = 0; i < 3892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3892(int& val) {
    const int x = 3892;
    std::cout << 3892;
    for (int i = 0; i < 3892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3893(int& val) {
    func_in_3893(val);
    const int x = 3893;
    std::cout << 3893;
    for (int i = 0; i < 3893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3893(int& val) {
    const int x = 3893;
    std::cout << 3893;
    for (int i = 0; i < 3893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3894(int& val) {
    func_in_3894(val);
    const int x = 3894;
    std::cout << 3894;
    for (int i = 0; i < 3894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3894(int& val) {
    const int x = 3894;
    std::cout << 3894;
    for (int i = 0; i < 3894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3895(int& val) {
    func_in_3895(val);
    const int x = 3895;
    std::cout << 3895;
    for (int i = 0; i < 3895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3895(int& val) {
    const int x = 3895;
    std::cout << 3895;
    for (int i = 0; i < 3895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3896(int& val) {
    func_in_3896(val);
    const int x = 3896;
    std::cout << 3896;
    for (int i = 0; i < 3896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3896(int& val) {
    const int x = 3896;
    std::cout << 3896;
    for (int i = 0; i < 3896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3897(int& val) {
    func_in_3897(val);
    const int x = 3897;
    std::cout << 3897;
    for (int i = 0; i < 3897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3897(int& val) {
    const int x = 3897;
    std::cout << 3897;
    for (int i = 0; i < 3897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3898(int& val) {
    func_in_3898(val);
    const int x = 3898;
    std::cout << 3898;
    for (int i = 0; i < 3898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3898(int& val) {
    const int x = 3898;
    std::cout << 3898;
    for (int i = 0; i < 3898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3899(int& val) {
    func_in_3899(val);
    const int x = 3899;
    std::cout << 3899;
    for (int i = 0; i < 3899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3899(int& val) {
    const int x = 3899;
    std::cout << 3899;
    for (int i = 0; i < 3899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3900(int& val) {
    func_in_3900(val);
    const int x = 3900;
    std::cout << 3900;
    for (int i = 0; i < 3900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3900(int& val) {
    const int x = 3900;
    std::cout << 3900;
    for (int i = 0; i < 3900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3901(int& val) {
    func_in_3901(val);
    const int x = 3901;
    std::cout << 3901;
    for (int i = 0; i < 3901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3901(int& val) {
    const int x = 3901;
    std::cout << 3901;
    for (int i = 0; i < 3901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3902(int& val) {
    func_in_3902(val);
    const int x = 3902;
    std::cout << 3902;
    for (int i = 0; i < 3902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3902(int& val) {
    const int x = 3902;
    std::cout << 3902;
    for (int i = 0; i < 3902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3903(int& val) {
    func_in_3903(val);
    const int x = 3903;
    std::cout << 3903;
    for (int i = 0; i < 3903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3903(int& val) {
    const int x = 3903;
    std::cout << 3903;
    for (int i = 0; i < 3903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3904(int& val) {
    func_in_3904(val);
    const int x = 3904;
    std::cout << 3904;
    for (int i = 0; i < 3904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3904(int& val) {
    const int x = 3904;
    std::cout << 3904;
    for (int i = 0; i < 3904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3905(int& val) {
    func_in_3905(val);
    const int x = 3905;
    std::cout << 3905;
    for (int i = 0; i < 3905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3905(int& val) {
    const int x = 3905;
    std::cout << 3905;
    for (int i = 0; i < 3905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3906(int& val) {
    func_in_3906(val);
    const int x = 3906;
    std::cout << 3906;
    for (int i = 0; i < 3906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3906(int& val) {
    const int x = 3906;
    std::cout << 3906;
    for (int i = 0; i < 3906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3907(int& val) {
    func_in_3907(val);
    const int x = 3907;
    std::cout << 3907;
    for (int i = 0; i < 3907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3907(int& val) {
    const int x = 3907;
    std::cout << 3907;
    for (int i = 0; i < 3907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3908(int& val) {
    func_in_3908(val);
    const int x = 3908;
    std::cout << 3908;
    for (int i = 0; i < 3908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3908(int& val) {
    const int x = 3908;
    std::cout << 3908;
    for (int i = 0; i < 3908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3909(int& val) {
    func_in_3909(val);
    const int x = 3909;
    std::cout << 3909;
    for (int i = 0; i < 3909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3909(int& val) {
    const int x = 3909;
    std::cout << 3909;
    for (int i = 0; i < 3909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3910(int& val) {
    func_in_3910(val);
    const int x = 3910;
    std::cout << 3910;
    for (int i = 0; i < 3910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3910(int& val) {
    const int x = 3910;
    std::cout << 3910;
    for (int i = 0; i < 3910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3911(int& val) {
    func_in_3911(val);
    const int x = 3911;
    std::cout << 3911;
    for (int i = 0; i < 3911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3911(int& val) {
    const int x = 3911;
    std::cout << 3911;
    for (int i = 0; i < 3911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3912(int& val) {
    func_in_3912(val);
    const int x = 3912;
    std::cout << 3912;
    for (int i = 0; i < 3912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3912(int& val) {
    const int x = 3912;
    std::cout << 3912;
    for (int i = 0; i < 3912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3913(int& val) {
    func_in_3913(val);
    const int x = 3913;
    std::cout << 3913;
    for (int i = 0; i < 3913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3913(int& val) {
    const int x = 3913;
    std::cout << 3913;
    for (int i = 0; i < 3913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3914(int& val) {
    func_in_3914(val);
    const int x = 3914;
    std::cout << 3914;
    for (int i = 0; i < 3914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3914(int& val) {
    const int x = 3914;
    std::cout << 3914;
    for (int i = 0; i < 3914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3915(int& val) {
    func_in_3915(val);
    const int x = 3915;
    std::cout << 3915;
    for (int i = 0; i < 3915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3915(int& val) {
    const int x = 3915;
    std::cout << 3915;
    for (int i = 0; i < 3915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3916(int& val) {
    func_in_3916(val);
    const int x = 3916;
    std::cout << 3916;
    for (int i = 0; i < 3916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3916(int& val) {
    const int x = 3916;
    std::cout << 3916;
    for (int i = 0; i < 3916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3917(int& val) {
    func_in_3917(val);
    const int x = 3917;
    std::cout << 3917;
    for (int i = 0; i < 3917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3917(int& val) {
    const int x = 3917;
    std::cout << 3917;
    for (int i = 0; i < 3917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3918(int& val) {
    func_in_3918(val);
    const int x = 3918;
    std::cout << 3918;
    for (int i = 0; i < 3918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3918(int& val) {
    const int x = 3918;
    std::cout << 3918;
    for (int i = 0; i < 3918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3919(int& val) {
    func_in_3919(val);
    const int x = 3919;
    std::cout << 3919;
    for (int i = 0; i < 3919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3919(int& val) {
    const int x = 3919;
    std::cout << 3919;
    for (int i = 0; i < 3919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3920(int& val) {
    func_in_3920(val);
    const int x = 3920;
    std::cout << 3920;
    for (int i = 0; i < 3920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3920(int& val) {
    const int x = 3920;
    std::cout << 3920;
    for (int i = 0; i < 3920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3921(int& val) {
    func_in_3921(val);
    const int x = 3921;
    std::cout << 3921;
    for (int i = 0; i < 3921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3921(int& val) {
    const int x = 3921;
    std::cout << 3921;
    for (int i = 0; i < 3921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3922(int& val) {
    func_in_3922(val);
    const int x = 3922;
    std::cout << 3922;
    for (int i = 0; i < 3922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3922(int& val) {
    const int x = 3922;
    std::cout << 3922;
    for (int i = 0; i < 3922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3923(int& val) {
    func_in_3923(val);
    const int x = 3923;
    std::cout << 3923;
    for (int i = 0; i < 3923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3923(int& val) {
    const int x = 3923;
    std::cout << 3923;
    for (int i = 0; i < 3923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3924(int& val) {
    func_in_3924(val);
    const int x = 3924;
    std::cout << 3924;
    for (int i = 0; i < 3924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3924(int& val) {
    const int x = 3924;
    std::cout << 3924;
    for (int i = 0; i < 3924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3925(int& val) {
    func_in_3925(val);
    const int x = 3925;
    std::cout << 3925;
    for (int i = 0; i < 3925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3925(int& val) {
    const int x = 3925;
    std::cout << 3925;
    for (int i = 0; i < 3925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3926(int& val) {
    func_in_3926(val);
    const int x = 3926;
    std::cout << 3926;
    for (int i = 0; i < 3926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3926(int& val) {
    const int x = 3926;
    std::cout << 3926;
    for (int i = 0; i < 3926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3927(int& val) {
    func_in_3927(val);
    const int x = 3927;
    std::cout << 3927;
    for (int i = 0; i < 3927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3927(int& val) {
    const int x = 3927;
    std::cout << 3927;
    for (int i = 0; i < 3927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3928(int& val) {
    func_in_3928(val);
    const int x = 3928;
    std::cout << 3928;
    for (int i = 0; i < 3928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3928(int& val) {
    const int x = 3928;
    std::cout << 3928;
    for (int i = 0; i < 3928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3929(int& val) {
    func_in_3929(val);
    const int x = 3929;
    std::cout << 3929;
    for (int i = 0; i < 3929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3929(int& val) {
    const int x = 3929;
    std::cout << 3929;
    for (int i = 0; i < 3929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3930(int& val) {
    func_in_3930(val);
    const int x = 3930;
    std::cout << 3930;
    for (int i = 0; i < 3930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3930(int& val) {
    const int x = 3930;
    std::cout << 3930;
    for (int i = 0; i < 3930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3931(int& val) {
    func_in_3931(val);
    const int x = 3931;
    std::cout << 3931;
    for (int i = 0; i < 3931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3931(int& val) {
    const int x = 3931;
    std::cout << 3931;
    for (int i = 0; i < 3931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3932(int& val) {
    func_in_3932(val);
    const int x = 3932;
    std::cout << 3932;
    for (int i = 0; i < 3932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3932(int& val) {
    const int x = 3932;
    std::cout << 3932;
    for (int i = 0; i < 3932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3933(int& val) {
    func_in_3933(val);
    const int x = 3933;
    std::cout << 3933;
    for (int i = 0; i < 3933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3933(int& val) {
    const int x = 3933;
    std::cout << 3933;
    for (int i = 0; i < 3933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3934(int& val) {
    func_in_3934(val);
    const int x = 3934;
    std::cout << 3934;
    for (int i = 0; i < 3934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3934(int& val) {
    const int x = 3934;
    std::cout << 3934;
    for (int i = 0; i < 3934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3935(int& val) {
    func_in_3935(val);
    const int x = 3935;
    std::cout << 3935;
    for (int i = 0; i < 3935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3935(int& val) {
    const int x = 3935;
    std::cout << 3935;
    for (int i = 0; i < 3935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3936(int& val) {
    func_in_3936(val);
    const int x = 3936;
    std::cout << 3936;
    for (int i = 0; i < 3936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3936(int& val) {
    const int x = 3936;
    std::cout << 3936;
    for (int i = 0; i < 3936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3937(int& val) {
    func_in_3937(val);
    const int x = 3937;
    std::cout << 3937;
    for (int i = 0; i < 3937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3937(int& val) {
    const int x = 3937;
    std::cout << 3937;
    for (int i = 0; i < 3937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3938(int& val) {
    func_in_3938(val);
    const int x = 3938;
    std::cout << 3938;
    for (int i = 0; i < 3938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3938(int& val) {
    const int x = 3938;
    std::cout << 3938;
    for (int i = 0; i < 3938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3939(int& val) {
    func_in_3939(val);
    const int x = 3939;
    std::cout << 3939;
    for (int i = 0; i < 3939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3939(int& val) {
    const int x = 3939;
    std::cout << 3939;
    for (int i = 0; i < 3939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3940(int& val) {
    func_in_3940(val);
    const int x = 3940;
    std::cout << 3940;
    for (int i = 0; i < 3940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3940(int& val) {
    const int x = 3940;
    std::cout << 3940;
    for (int i = 0; i < 3940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3941(int& val) {
    func_in_3941(val);
    const int x = 3941;
    std::cout << 3941;
    for (int i = 0; i < 3941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3941(int& val) {
    const int x = 3941;
    std::cout << 3941;
    for (int i = 0; i < 3941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3942(int& val) {
    func_in_3942(val);
    const int x = 3942;
    std::cout << 3942;
    for (int i = 0; i < 3942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3942(int& val) {
    const int x = 3942;
    std::cout << 3942;
    for (int i = 0; i < 3942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3943(int& val) {
    func_in_3943(val);
    const int x = 3943;
    std::cout << 3943;
    for (int i = 0; i < 3943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3943(int& val) {
    const int x = 3943;
    std::cout << 3943;
    for (int i = 0; i < 3943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3944(int& val) {
    func_in_3944(val);
    const int x = 3944;
    std::cout << 3944;
    for (int i = 0; i < 3944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3944(int& val) {
    const int x = 3944;
    std::cout << 3944;
    for (int i = 0; i < 3944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3945(int& val) {
    func_in_3945(val);
    const int x = 3945;
    std::cout << 3945;
    for (int i = 0; i < 3945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3945(int& val) {
    const int x = 3945;
    std::cout << 3945;
    for (int i = 0; i < 3945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3946(int& val) {
    func_in_3946(val);
    const int x = 3946;
    std::cout << 3946;
    for (int i = 0; i < 3946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3946(int& val) {
    const int x = 3946;
    std::cout << 3946;
    for (int i = 0; i < 3946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3947(int& val) {
    func_in_3947(val);
    const int x = 3947;
    std::cout << 3947;
    for (int i = 0; i < 3947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3947(int& val) {
    const int x = 3947;
    std::cout << 3947;
    for (int i = 0; i < 3947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3948(int& val) {
    func_in_3948(val);
    const int x = 3948;
    std::cout << 3948;
    for (int i = 0; i < 3948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3948(int& val) {
    const int x = 3948;
    std::cout << 3948;
    for (int i = 0; i < 3948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3949(int& val) {
    func_in_3949(val);
    const int x = 3949;
    std::cout << 3949;
    for (int i = 0; i < 3949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3949(int& val) {
    const int x = 3949;
    std::cout << 3949;
    for (int i = 0; i < 3949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3950(int& val) {
    func_in_3950(val);
    const int x = 3950;
    std::cout << 3950;
    for (int i = 0; i < 3950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3950(int& val) {
    const int x = 3950;
    std::cout << 3950;
    for (int i = 0; i < 3950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3951(int& val) {
    func_in_3951(val);
    const int x = 3951;
    std::cout << 3951;
    for (int i = 0; i < 3951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3951(int& val) {
    const int x = 3951;
    std::cout << 3951;
    for (int i = 0; i < 3951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3952(int& val) {
    func_in_3952(val);
    const int x = 3952;
    std::cout << 3952;
    for (int i = 0; i < 3952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3952(int& val) {
    const int x = 3952;
    std::cout << 3952;
    for (int i = 0; i < 3952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3953(int& val) {
    func_in_3953(val);
    const int x = 3953;
    std::cout << 3953;
    for (int i = 0; i < 3953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3953(int& val) {
    const int x = 3953;
    std::cout << 3953;
    for (int i = 0; i < 3953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3954(int& val) {
    func_in_3954(val);
    const int x = 3954;
    std::cout << 3954;
    for (int i = 0; i < 3954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3954(int& val) {
    const int x = 3954;
    std::cout << 3954;
    for (int i = 0; i < 3954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3955(int& val) {
    func_in_3955(val);
    const int x = 3955;
    std::cout << 3955;
    for (int i = 0; i < 3955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3955(int& val) {
    const int x = 3955;
    std::cout << 3955;
    for (int i = 0; i < 3955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3956(int& val) {
    func_in_3956(val);
    const int x = 3956;
    std::cout << 3956;
    for (int i = 0; i < 3956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3956(int& val) {
    const int x = 3956;
    std::cout << 3956;
    for (int i = 0; i < 3956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3957(int& val) {
    func_in_3957(val);
    const int x = 3957;
    std::cout << 3957;
    for (int i = 0; i < 3957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3957(int& val) {
    const int x = 3957;
    std::cout << 3957;
    for (int i = 0; i < 3957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3958(int& val) {
    func_in_3958(val);
    const int x = 3958;
    std::cout << 3958;
    for (int i = 0; i < 3958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3958(int& val) {
    const int x = 3958;
    std::cout << 3958;
    for (int i = 0; i < 3958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3959(int& val) {
    func_in_3959(val);
    const int x = 3959;
    std::cout << 3959;
    for (int i = 0; i < 3959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3959(int& val) {
    const int x = 3959;
    std::cout << 3959;
    for (int i = 0; i < 3959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3960(int& val) {
    func_in_3960(val);
    const int x = 3960;
    std::cout << 3960;
    for (int i = 0; i < 3960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3960(int& val) {
    const int x = 3960;
    std::cout << 3960;
    for (int i = 0; i < 3960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3961(int& val) {
    func_in_3961(val);
    const int x = 3961;
    std::cout << 3961;
    for (int i = 0; i < 3961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3961(int& val) {
    const int x = 3961;
    std::cout << 3961;
    for (int i = 0; i < 3961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3962(int& val) {
    func_in_3962(val);
    const int x = 3962;
    std::cout << 3962;
    for (int i = 0; i < 3962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3962(int& val) {
    const int x = 3962;
    std::cout << 3962;
    for (int i = 0; i < 3962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3963(int& val) {
    func_in_3963(val);
    const int x = 3963;
    std::cout << 3963;
    for (int i = 0; i < 3963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3963(int& val) {
    const int x = 3963;
    std::cout << 3963;
    for (int i = 0; i < 3963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3964(int& val) {
    func_in_3964(val);
    const int x = 3964;
    std::cout << 3964;
    for (int i = 0; i < 3964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3964(int& val) {
    const int x = 3964;
    std::cout << 3964;
    for (int i = 0; i < 3964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3965(int& val) {
    func_in_3965(val);
    const int x = 3965;
    std::cout << 3965;
    for (int i = 0; i < 3965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3965(int& val) {
    const int x = 3965;
    std::cout << 3965;
    for (int i = 0; i < 3965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3966(int& val) {
    func_in_3966(val);
    const int x = 3966;
    std::cout << 3966;
    for (int i = 0; i < 3966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3966(int& val) {
    const int x = 3966;
    std::cout << 3966;
    for (int i = 0; i < 3966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3967(int& val) {
    func_in_3967(val);
    const int x = 3967;
    std::cout << 3967;
    for (int i = 0; i < 3967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3967(int& val) {
    const int x = 3967;
    std::cout << 3967;
    for (int i = 0; i < 3967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3968(int& val) {
    func_in_3968(val);
    const int x = 3968;
    std::cout << 3968;
    for (int i = 0; i < 3968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3968(int& val) {
    const int x = 3968;
    std::cout << 3968;
    for (int i = 0; i < 3968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3969(int& val) {
    func_in_3969(val);
    const int x = 3969;
    std::cout << 3969;
    for (int i = 0; i < 3969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3969(int& val) {
    const int x = 3969;
    std::cout << 3969;
    for (int i = 0; i < 3969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3970(int& val) {
    func_in_3970(val);
    const int x = 3970;
    std::cout << 3970;
    for (int i = 0; i < 3970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3970(int& val) {
    const int x = 3970;
    std::cout << 3970;
    for (int i = 0; i < 3970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3971(int& val) {
    func_in_3971(val);
    const int x = 3971;
    std::cout << 3971;
    for (int i = 0; i < 3971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3971(int& val) {
    const int x = 3971;
    std::cout << 3971;
    for (int i = 0; i < 3971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3972(int& val) {
    func_in_3972(val);
    const int x = 3972;
    std::cout << 3972;
    for (int i = 0; i < 3972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3972(int& val) {
    const int x = 3972;
    std::cout << 3972;
    for (int i = 0; i < 3972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3973(int& val) {
    func_in_3973(val);
    const int x = 3973;
    std::cout << 3973;
    for (int i = 0; i < 3973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3973(int& val) {
    const int x = 3973;
    std::cout << 3973;
    for (int i = 0; i < 3973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3974(int& val) {
    func_in_3974(val);
    const int x = 3974;
    std::cout << 3974;
    for (int i = 0; i < 3974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3974(int& val) {
    const int x = 3974;
    std::cout << 3974;
    for (int i = 0; i < 3974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3975(int& val) {
    func_in_3975(val);
    const int x = 3975;
    std::cout << 3975;
    for (int i = 0; i < 3975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3975(int& val) {
    const int x = 3975;
    std::cout << 3975;
    for (int i = 0; i < 3975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3976(int& val) {
    func_in_3976(val);
    const int x = 3976;
    std::cout << 3976;
    for (int i = 0; i < 3976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3976(int& val) {
    const int x = 3976;
    std::cout << 3976;
    for (int i = 0; i < 3976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3977(int& val) {
    func_in_3977(val);
    const int x = 3977;
    std::cout << 3977;
    for (int i = 0; i < 3977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3977(int& val) {
    const int x = 3977;
    std::cout << 3977;
    for (int i = 0; i < 3977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3978(int& val) {
    func_in_3978(val);
    const int x = 3978;
    std::cout << 3978;
    for (int i = 0; i < 3978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3978(int& val) {
    const int x = 3978;
    std::cout << 3978;
    for (int i = 0; i < 3978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3979(int& val) {
    func_in_3979(val);
    const int x = 3979;
    std::cout << 3979;
    for (int i = 0; i < 3979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3979(int& val) {
    const int x = 3979;
    std::cout << 3979;
    for (int i = 0; i < 3979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3980(int& val) {
    func_in_3980(val);
    const int x = 3980;
    std::cout << 3980;
    for (int i = 0; i < 3980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3980(int& val) {
    const int x = 3980;
    std::cout << 3980;
    for (int i = 0; i < 3980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3981(int& val) {
    func_in_3981(val);
    const int x = 3981;
    std::cout << 3981;
    for (int i = 0; i < 3981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3981(int& val) {
    const int x = 3981;
    std::cout << 3981;
    for (int i = 0; i < 3981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3982(int& val) {
    func_in_3982(val);
    const int x = 3982;
    std::cout << 3982;
    for (int i = 0; i < 3982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3982(int& val) {
    const int x = 3982;
    std::cout << 3982;
    for (int i = 0; i < 3982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3983(int& val) {
    func_in_3983(val);
    const int x = 3983;
    std::cout << 3983;
    for (int i = 0; i < 3983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3983(int& val) {
    const int x = 3983;
    std::cout << 3983;
    for (int i = 0; i < 3983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3984(int& val) {
    func_in_3984(val);
    const int x = 3984;
    std::cout << 3984;
    for (int i = 0; i < 3984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3984(int& val) {
    const int x = 3984;
    std::cout << 3984;
    for (int i = 0; i < 3984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3985(int& val) {
    func_in_3985(val);
    const int x = 3985;
    std::cout << 3985;
    for (int i = 0; i < 3985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3985(int& val) {
    const int x = 3985;
    std::cout << 3985;
    for (int i = 0; i < 3985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3986(int& val) {
    func_in_3986(val);
    const int x = 3986;
    std::cout << 3986;
    for (int i = 0; i < 3986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3986(int& val) {
    const int x = 3986;
    std::cout << 3986;
    for (int i = 0; i < 3986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3987(int& val) {
    func_in_3987(val);
    const int x = 3987;
    std::cout << 3987;
    for (int i = 0; i < 3987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3987(int& val) {
    const int x = 3987;
    std::cout << 3987;
    for (int i = 0; i < 3987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3988(int& val) {
    func_in_3988(val);
    const int x = 3988;
    std::cout << 3988;
    for (int i = 0; i < 3988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3988(int& val) {
    const int x = 3988;
    std::cout << 3988;
    for (int i = 0; i < 3988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3989(int& val) {
    func_in_3989(val);
    const int x = 3989;
    std::cout << 3989;
    for (int i = 0; i < 3989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3989(int& val) {
    const int x = 3989;
    std::cout << 3989;
    for (int i = 0; i < 3989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3990(int& val) {
    func_in_3990(val);
    const int x = 3990;
    std::cout << 3990;
    for (int i = 0; i < 3990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3990(int& val) {
    const int x = 3990;
    std::cout << 3990;
    for (int i = 0; i < 3990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3991(int& val) {
    func_in_3991(val);
    const int x = 3991;
    std::cout << 3991;
    for (int i = 0; i < 3991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3991(int& val) {
    const int x = 3991;
    std::cout << 3991;
    for (int i = 0; i < 3991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3992(int& val) {
    func_in_3992(val);
    const int x = 3992;
    std::cout << 3992;
    for (int i = 0; i < 3992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3992(int& val) {
    const int x = 3992;
    std::cout << 3992;
    for (int i = 0; i < 3992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3993(int& val) {
    func_in_3993(val);
    const int x = 3993;
    std::cout << 3993;
    for (int i = 0; i < 3993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3993(int& val) {
    const int x = 3993;
    std::cout << 3993;
    for (int i = 0; i < 3993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3994(int& val) {
    func_in_3994(val);
    const int x = 3994;
    std::cout << 3994;
    for (int i = 0; i < 3994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3994(int& val) {
    const int x = 3994;
    std::cout << 3994;
    for (int i = 0; i < 3994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3995(int& val) {
    func_in_3995(val);
    const int x = 3995;
    std::cout << 3995;
    for (int i = 0; i < 3995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3995(int& val) {
    const int x = 3995;
    std::cout << 3995;
    for (int i = 0; i < 3995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3996(int& val) {
    func_in_3996(val);
    const int x = 3996;
    std::cout << 3996;
    for (int i = 0; i < 3996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3996(int& val) {
    const int x = 3996;
    std::cout << 3996;
    for (int i = 0; i < 3996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3997(int& val) {
    func_in_3997(val);
    const int x = 3997;
    std::cout << 3997;
    for (int i = 0; i < 3997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3997(int& val) {
    const int x = 3997;
    std::cout << 3997;
    for (int i = 0; i < 3997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3998(int& val) {
    func_in_3998(val);
    const int x = 3998;
    std::cout << 3998;
    for (int i = 0; i < 3998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3998(int& val) {
    const int x = 3998;
    std::cout << 3998;
    for (int i = 0; i < 3998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_3999(int& val) {
    func_in_3999(val);
    const int x = 3999;
    std::cout << 3999;
    for (int i = 0; i < 3999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_3999(int& val) {
    const int x = 3999;
    std::cout << 3999;
    for (int i = 0; i < 3999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

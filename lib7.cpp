#include "lib.h"
#include <iostream>
int func_28000(int& val) {
    func_in_28000(val);
    const int x = 28000;
    std::cout << 28000;
    for (int i = 0; i < 28000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28000(int& val) {
    const int x = 28000;
    std::cout << 28000;
    for (int i = 0; i < 28000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28001(int& val) {
    func_in_28001(val);
    const int x = 28001;
    std::cout << 28001;
    for (int i = 0; i < 28001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28001(int& val) {
    const int x = 28001;
    std::cout << 28001;
    for (int i = 0; i < 28001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28002(int& val) {
    func_in_28002(val);
    const int x = 28002;
    std::cout << 28002;
    for (int i = 0; i < 28002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28002(int& val) {
    const int x = 28002;
    std::cout << 28002;
    for (int i = 0; i < 28002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28003(int& val) {
    func_in_28003(val);
    const int x = 28003;
    std::cout << 28003;
    for (int i = 0; i < 28003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28003(int& val) {
    const int x = 28003;
    std::cout << 28003;
    for (int i = 0; i < 28003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28004(int& val) {
    func_in_28004(val);
    const int x = 28004;
    std::cout << 28004;
    for (int i = 0; i < 28004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28004(int& val) {
    const int x = 28004;
    std::cout << 28004;
    for (int i = 0; i < 28004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28005(int& val) {
    func_in_28005(val);
    const int x = 28005;
    std::cout << 28005;
    for (int i = 0; i < 28005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28005(int& val) {
    const int x = 28005;
    std::cout << 28005;
    for (int i = 0; i < 28005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28006(int& val) {
    func_in_28006(val);
    const int x = 28006;
    std::cout << 28006;
    for (int i = 0; i < 28006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28006(int& val) {
    const int x = 28006;
    std::cout << 28006;
    for (int i = 0; i < 28006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28007(int& val) {
    func_in_28007(val);
    const int x = 28007;
    std::cout << 28007;
    for (int i = 0; i < 28007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28007(int& val) {
    const int x = 28007;
    std::cout << 28007;
    for (int i = 0; i < 28007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28008(int& val) {
    func_in_28008(val);
    const int x = 28008;
    std::cout << 28008;
    for (int i = 0; i < 28008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28008(int& val) {
    const int x = 28008;
    std::cout << 28008;
    for (int i = 0; i < 28008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28009(int& val) {
    func_in_28009(val);
    const int x = 28009;
    std::cout << 28009;
    for (int i = 0; i < 28009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28009(int& val) {
    const int x = 28009;
    std::cout << 28009;
    for (int i = 0; i < 28009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28010(int& val) {
    func_in_28010(val);
    const int x = 28010;
    std::cout << 28010;
    for (int i = 0; i < 28010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28010(int& val) {
    const int x = 28010;
    std::cout << 28010;
    for (int i = 0; i < 28010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28011(int& val) {
    func_in_28011(val);
    const int x = 28011;
    std::cout << 28011;
    for (int i = 0; i < 28011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28011(int& val) {
    const int x = 28011;
    std::cout << 28011;
    for (int i = 0; i < 28011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28012(int& val) {
    func_in_28012(val);
    const int x = 28012;
    std::cout << 28012;
    for (int i = 0; i < 28012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28012(int& val) {
    const int x = 28012;
    std::cout << 28012;
    for (int i = 0; i < 28012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28013(int& val) {
    func_in_28013(val);
    const int x = 28013;
    std::cout << 28013;
    for (int i = 0; i < 28013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28013(int& val) {
    const int x = 28013;
    std::cout << 28013;
    for (int i = 0; i < 28013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28014(int& val) {
    func_in_28014(val);
    const int x = 28014;
    std::cout << 28014;
    for (int i = 0; i < 28014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28014(int& val) {
    const int x = 28014;
    std::cout << 28014;
    for (int i = 0; i < 28014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28015(int& val) {
    func_in_28015(val);
    const int x = 28015;
    std::cout << 28015;
    for (int i = 0; i < 28015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28015(int& val) {
    const int x = 28015;
    std::cout << 28015;
    for (int i = 0; i < 28015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28016(int& val) {
    func_in_28016(val);
    const int x = 28016;
    std::cout << 28016;
    for (int i = 0; i < 28016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28016(int& val) {
    const int x = 28016;
    std::cout << 28016;
    for (int i = 0; i < 28016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28017(int& val) {
    func_in_28017(val);
    const int x = 28017;
    std::cout << 28017;
    for (int i = 0; i < 28017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28017(int& val) {
    const int x = 28017;
    std::cout << 28017;
    for (int i = 0; i < 28017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28018(int& val) {
    func_in_28018(val);
    const int x = 28018;
    std::cout << 28018;
    for (int i = 0; i < 28018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28018(int& val) {
    const int x = 28018;
    std::cout << 28018;
    for (int i = 0; i < 28018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28019(int& val) {
    func_in_28019(val);
    const int x = 28019;
    std::cout << 28019;
    for (int i = 0; i < 28019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28019(int& val) {
    const int x = 28019;
    std::cout << 28019;
    for (int i = 0; i < 28019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28020(int& val) {
    func_in_28020(val);
    const int x = 28020;
    std::cout << 28020;
    for (int i = 0; i < 28020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28020(int& val) {
    const int x = 28020;
    std::cout << 28020;
    for (int i = 0; i < 28020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28021(int& val) {
    func_in_28021(val);
    const int x = 28021;
    std::cout << 28021;
    for (int i = 0; i < 28021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28021(int& val) {
    const int x = 28021;
    std::cout << 28021;
    for (int i = 0; i < 28021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28022(int& val) {
    func_in_28022(val);
    const int x = 28022;
    std::cout << 28022;
    for (int i = 0; i < 28022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28022(int& val) {
    const int x = 28022;
    std::cout << 28022;
    for (int i = 0; i < 28022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28023(int& val) {
    func_in_28023(val);
    const int x = 28023;
    std::cout << 28023;
    for (int i = 0; i < 28023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28023(int& val) {
    const int x = 28023;
    std::cout << 28023;
    for (int i = 0; i < 28023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28024(int& val) {
    func_in_28024(val);
    const int x = 28024;
    std::cout << 28024;
    for (int i = 0; i < 28024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28024(int& val) {
    const int x = 28024;
    std::cout << 28024;
    for (int i = 0; i < 28024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28025(int& val) {
    func_in_28025(val);
    const int x = 28025;
    std::cout << 28025;
    for (int i = 0; i < 28025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28025(int& val) {
    const int x = 28025;
    std::cout << 28025;
    for (int i = 0; i < 28025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28026(int& val) {
    func_in_28026(val);
    const int x = 28026;
    std::cout << 28026;
    for (int i = 0; i < 28026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28026(int& val) {
    const int x = 28026;
    std::cout << 28026;
    for (int i = 0; i < 28026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28027(int& val) {
    func_in_28027(val);
    const int x = 28027;
    std::cout << 28027;
    for (int i = 0; i < 28027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28027(int& val) {
    const int x = 28027;
    std::cout << 28027;
    for (int i = 0; i < 28027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28028(int& val) {
    func_in_28028(val);
    const int x = 28028;
    std::cout << 28028;
    for (int i = 0; i < 28028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28028(int& val) {
    const int x = 28028;
    std::cout << 28028;
    for (int i = 0; i < 28028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28029(int& val) {
    func_in_28029(val);
    const int x = 28029;
    std::cout << 28029;
    for (int i = 0; i < 28029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28029(int& val) {
    const int x = 28029;
    std::cout << 28029;
    for (int i = 0; i < 28029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28030(int& val) {
    func_in_28030(val);
    const int x = 28030;
    std::cout << 28030;
    for (int i = 0; i < 28030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28030(int& val) {
    const int x = 28030;
    std::cout << 28030;
    for (int i = 0; i < 28030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28031(int& val) {
    func_in_28031(val);
    const int x = 28031;
    std::cout << 28031;
    for (int i = 0; i < 28031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28031(int& val) {
    const int x = 28031;
    std::cout << 28031;
    for (int i = 0; i < 28031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28032(int& val) {
    func_in_28032(val);
    const int x = 28032;
    std::cout << 28032;
    for (int i = 0; i < 28032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28032(int& val) {
    const int x = 28032;
    std::cout << 28032;
    for (int i = 0; i < 28032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28033(int& val) {
    func_in_28033(val);
    const int x = 28033;
    std::cout << 28033;
    for (int i = 0; i < 28033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28033(int& val) {
    const int x = 28033;
    std::cout << 28033;
    for (int i = 0; i < 28033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28034(int& val) {
    func_in_28034(val);
    const int x = 28034;
    std::cout << 28034;
    for (int i = 0; i < 28034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28034(int& val) {
    const int x = 28034;
    std::cout << 28034;
    for (int i = 0; i < 28034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28035(int& val) {
    func_in_28035(val);
    const int x = 28035;
    std::cout << 28035;
    for (int i = 0; i < 28035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28035(int& val) {
    const int x = 28035;
    std::cout << 28035;
    for (int i = 0; i < 28035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28036(int& val) {
    func_in_28036(val);
    const int x = 28036;
    std::cout << 28036;
    for (int i = 0; i < 28036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28036(int& val) {
    const int x = 28036;
    std::cout << 28036;
    for (int i = 0; i < 28036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28037(int& val) {
    func_in_28037(val);
    const int x = 28037;
    std::cout << 28037;
    for (int i = 0; i < 28037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28037(int& val) {
    const int x = 28037;
    std::cout << 28037;
    for (int i = 0; i < 28037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28038(int& val) {
    func_in_28038(val);
    const int x = 28038;
    std::cout << 28038;
    for (int i = 0; i < 28038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28038(int& val) {
    const int x = 28038;
    std::cout << 28038;
    for (int i = 0; i < 28038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28039(int& val) {
    func_in_28039(val);
    const int x = 28039;
    std::cout << 28039;
    for (int i = 0; i < 28039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28039(int& val) {
    const int x = 28039;
    std::cout << 28039;
    for (int i = 0; i < 28039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28040(int& val) {
    func_in_28040(val);
    const int x = 28040;
    std::cout << 28040;
    for (int i = 0; i < 28040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28040(int& val) {
    const int x = 28040;
    std::cout << 28040;
    for (int i = 0; i < 28040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28041(int& val) {
    func_in_28041(val);
    const int x = 28041;
    std::cout << 28041;
    for (int i = 0; i < 28041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28041(int& val) {
    const int x = 28041;
    std::cout << 28041;
    for (int i = 0; i < 28041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28042(int& val) {
    func_in_28042(val);
    const int x = 28042;
    std::cout << 28042;
    for (int i = 0; i < 28042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28042(int& val) {
    const int x = 28042;
    std::cout << 28042;
    for (int i = 0; i < 28042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28043(int& val) {
    func_in_28043(val);
    const int x = 28043;
    std::cout << 28043;
    for (int i = 0; i < 28043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28043(int& val) {
    const int x = 28043;
    std::cout << 28043;
    for (int i = 0; i < 28043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28044(int& val) {
    func_in_28044(val);
    const int x = 28044;
    std::cout << 28044;
    for (int i = 0; i < 28044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28044(int& val) {
    const int x = 28044;
    std::cout << 28044;
    for (int i = 0; i < 28044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28045(int& val) {
    func_in_28045(val);
    const int x = 28045;
    std::cout << 28045;
    for (int i = 0; i < 28045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28045(int& val) {
    const int x = 28045;
    std::cout << 28045;
    for (int i = 0; i < 28045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28046(int& val) {
    func_in_28046(val);
    const int x = 28046;
    std::cout << 28046;
    for (int i = 0; i < 28046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28046(int& val) {
    const int x = 28046;
    std::cout << 28046;
    for (int i = 0; i < 28046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28047(int& val) {
    func_in_28047(val);
    const int x = 28047;
    std::cout << 28047;
    for (int i = 0; i < 28047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28047(int& val) {
    const int x = 28047;
    std::cout << 28047;
    for (int i = 0; i < 28047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28048(int& val) {
    func_in_28048(val);
    const int x = 28048;
    std::cout << 28048;
    for (int i = 0; i < 28048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28048(int& val) {
    const int x = 28048;
    std::cout << 28048;
    for (int i = 0; i < 28048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28049(int& val) {
    func_in_28049(val);
    const int x = 28049;
    std::cout << 28049;
    for (int i = 0; i < 28049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28049(int& val) {
    const int x = 28049;
    std::cout << 28049;
    for (int i = 0; i < 28049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28050(int& val) {
    func_in_28050(val);
    const int x = 28050;
    std::cout << 28050;
    for (int i = 0; i < 28050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28050(int& val) {
    const int x = 28050;
    std::cout << 28050;
    for (int i = 0; i < 28050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28051(int& val) {
    func_in_28051(val);
    const int x = 28051;
    std::cout << 28051;
    for (int i = 0; i < 28051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28051(int& val) {
    const int x = 28051;
    std::cout << 28051;
    for (int i = 0; i < 28051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28052(int& val) {
    func_in_28052(val);
    const int x = 28052;
    std::cout << 28052;
    for (int i = 0; i < 28052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28052(int& val) {
    const int x = 28052;
    std::cout << 28052;
    for (int i = 0; i < 28052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28053(int& val) {
    func_in_28053(val);
    const int x = 28053;
    std::cout << 28053;
    for (int i = 0; i < 28053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28053(int& val) {
    const int x = 28053;
    std::cout << 28053;
    for (int i = 0; i < 28053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28054(int& val) {
    func_in_28054(val);
    const int x = 28054;
    std::cout << 28054;
    for (int i = 0; i < 28054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28054(int& val) {
    const int x = 28054;
    std::cout << 28054;
    for (int i = 0; i < 28054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28055(int& val) {
    func_in_28055(val);
    const int x = 28055;
    std::cout << 28055;
    for (int i = 0; i < 28055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28055(int& val) {
    const int x = 28055;
    std::cout << 28055;
    for (int i = 0; i < 28055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28056(int& val) {
    func_in_28056(val);
    const int x = 28056;
    std::cout << 28056;
    for (int i = 0; i < 28056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28056(int& val) {
    const int x = 28056;
    std::cout << 28056;
    for (int i = 0; i < 28056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28057(int& val) {
    func_in_28057(val);
    const int x = 28057;
    std::cout << 28057;
    for (int i = 0; i < 28057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28057(int& val) {
    const int x = 28057;
    std::cout << 28057;
    for (int i = 0; i < 28057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28058(int& val) {
    func_in_28058(val);
    const int x = 28058;
    std::cout << 28058;
    for (int i = 0; i < 28058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28058(int& val) {
    const int x = 28058;
    std::cout << 28058;
    for (int i = 0; i < 28058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28059(int& val) {
    func_in_28059(val);
    const int x = 28059;
    std::cout << 28059;
    for (int i = 0; i < 28059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28059(int& val) {
    const int x = 28059;
    std::cout << 28059;
    for (int i = 0; i < 28059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28060(int& val) {
    func_in_28060(val);
    const int x = 28060;
    std::cout << 28060;
    for (int i = 0; i < 28060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28060(int& val) {
    const int x = 28060;
    std::cout << 28060;
    for (int i = 0; i < 28060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28061(int& val) {
    func_in_28061(val);
    const int x = 28061;
    std::cout << 28061;
    for (int i = 0; i < 28061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28061(int& val) {
    const int x = 28061;
    std::cout << 28061;
    for (int i = 0; i < 28061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28062(int& val) {
    func_in_28062(val);
    const int x = 28062;
    std::cout << 28062;
    for (int i = 0; i < 28062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28062(int& val) {
    const int x = 28062;
    std::cout << 28062;
    for (int i = 0; i < 28062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28063(int& val) {
    func_in_28063(val);
    const int x = 28063;
    std::cout << 28063;
    for (int i = 0; i < 28063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28063(int& val) {
    const int x = 28063;
    std::cout << 28063;
    for (int i = 0; i < 28063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28064(int& val) {
    func_in_28064(val);
    const int x = 28064;
    std::cout << 28064;
    for (int i = 0; i < 28064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28064(int& val) {
    const int x = 28064;
    std::cout << 28064;
    for (int i = 0; i < 28064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28065(int& val) {
    func_in_28065(val);
    const int x = 28065;
    std::cout << 28065;
    for (int i = 0; i < 28065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28065(int& val) {
    const int x = 28065;
    std::cout << 28065;
    for (int i = 0; i < 28065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28066(int& val) {
    func_in_28066(val);
    const int x = 28066;
    std::cout << 28066;
    for (int i = 0; i < 28066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28066(int& val) {
    const int x = 28066;
    std::cout << 28066;
    for (int i = 0; i < 28066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28067(int& val) {
    func_in_28067(val);
    const int x = 28067;
    std::cout << 28067;
    for (int i = 0; i < 28067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28067(int& val) {
    const int x = 28067;
    std::cout << 28067;
    for (int i = 0; i < 28067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28068(int& val) {
    func_in_28068(val);
    const int x = 28068;
    std::cout << 28068;
    for (int i = 0; i < 28068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28068(int& val) {
    const int x = 28068;
    std::cout << 28068;
    for (int i = 0; i < 28068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28069(int& val) {
    func_in_28069(val);
    const int x = 28069;
    std::cout << 28069;
    for (int i = 0; i < 28069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28069(int& val) {
    const int x = 28069;
    std::cout << 28069;
    for (int i = 0; i < 28069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28070(int& val) {
    func_in_28070(val);
    const int x = 28070;
    std::cout << 28070;
    for (int i = 0; i < 28070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28070(int& val) {
    const int x = 28070;
    std::cout << 28070;
    for (int i = 0; i < 28070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28071(int& val) {
    func_in_28071(val);
    const int x = 28071;
    std::cout << 28071;
    for (int i = 0; i < 28071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28071(int& val) {
    const int x = 28071;
    std::cout << 28071;
    for (int i = 0; i < 28071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28072(int& val) {
    func_in_28072(val);
    const int x = 28072;
    std::cout << 28072;
    for (int i = 0; i < 28072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28072(int& val) {
    const int x = 28072;
    std::cout << 28072;
    for (int i = 0; i < 28072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28073(int& val) {
    func_in_28073(val);
    const int x = 28073;
    std::cout << 28073;
    for (int i = 0; i < 28073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28073(int& val) {
    const int x = 28073;
    std::cout << 28073;
    for (int i = 0; i < 28073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28074(int& val) {
    func_in_28074(val);
    const int x = 28074;
    std::cout << 28074;
    for (int i = 0; i < 28074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28074(int& val) {
    const int x = 28074;
    std::cout << 28074;
    for (int i = 0; i < 28074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28075(int& val) {
    func_in_28075(val);
    const int x = 28075;
    std::cout << 28075;
    for (int i = 0; i < 28075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28075(int& val) {
    const int x = 28075;
    std::cout << 28075;
    for (int i = 0; i < 28075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28076(int& val) {
    func_in_28076(val);
    const int x = 28076;
    std::cout << 28076;
    for (int i = 0; i < 28076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28076(int& val) {
    const int x = 28076;
    std::cout << 28076;
    for (int i = 0; i < 28076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28077(int& val) {
    func_in_28077(val);
    const int x = 28077;
    std::cout << 28077;
    for (int i = 0; i < 28077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28077(int& val) {
    const int x = 28077;
    std::cout << 28077;
    for (int i = 0; i < 28077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28078(int& val) {
    func_in_28078(val);
    const int x = 28078;
    std::cout << 28078;
    for (int i = 0; i < 28078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28078(int& val) {
    const int x = 28078;
    std::cout << 28078;
    for (int i = 0; i < 28078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28079(int& val) {
    func_in_28079(val);
    const int x = 28079;
    std::cout << 28079;
    for (int i = 0; i < 28079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28079(int& val) {
    const int x = 28079;
    std::cout << 28079;
    for (int i = 0; i < 28079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28080(int& val) {
    func_in_28080(val);
    const int x = 28080;
    std::cout << 28080;
    for (int i = 0; i < 28080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28080(int& val) {
    const int x = 28080;
    std::cout << 28080;
    for (int i = 0; i < 28080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28081(int& val) {
    func_in_28081(val);
    const int x = 28081;
    std::cout << 28081;
    for (int i = 0; i < 28081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28081(int& val) {
    const int x = 28081;
    std::cout << 28081;
    for (int i = 0; i < 28081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28082(int& val) {
    func_in_28082(val);
    const int x = 28082;
    std::cout << 28082;
    for (int i = 0; i < 28082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28082(int& val) {
    const int x = 28082;
    std::cout << 28082;
    for (int i = 0; i < 28082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28083(int& val) {
    func_in_28083(val);
    const int x = 28083;
    std::cout << 28083;
    for (int i = 0; i < 28083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28083(int& val) {
    const int x = 28083;
    std::cout << 28083;
    for (int i = 0; i < 28083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28084(int& val) {
    func_in_28084(val);
    const int x = 28084;
    std::cout << 28084;
    for (int i = 0; i < 28084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28084(int& val) {
    const int x = 28084;
    std::cout << 28084;
    for (int i = 0; i < 28084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28085(int& val) {
    func_in_28085(val);
    const int x = 28085;
    std::cout << 28085;
    for (int i = 0; i < 28085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28085(int& val) {
    const int x = 28085;
    std::cout << 28085;
    for (int i = 0; i < 28085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28086(int& val) {
    func_in_28086(val);
    const int x = 28086;
    std::cout << 28086;
    for (int i = 0; i < 28086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28086(int& val) {
    const int x = 28086;
    std::cout << 28086;
    for (int i = 0; i < 28086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28087(int& val) {
    func_in_28087(val);
    const int x = 28087;
    std::cout << 28087;
    for (int i = 0; i < 28087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28087(int& val) {
    const int x = 28087;
    std::cout << 28087;
    for (int i = 0; i < 28087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28088(int& val) {
    func_in_28088(val);
    const int x = 28088;
    std::cout << 28088;
    for (int i = 0; i < 28088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28088(int& val) {
    const int x = 28088;
    std::cout << 28088;
    for (int i = 0; i < 28088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28089(int& val) {
    func_in_28089(val);
    const int x = 28089;
    std::cout << 28089;
    for (int i = 0; i < 28089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28089(int& val) {
    const int x = 28089;
    std::cout << 28089;
    for (int i = 0; i < 28089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28090(int& val) {
    func_in_28090(val);
    const int x = 28090;
    std::cout << 28090;
    for (int i = 0; i < 28090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28090(int& val) {
    const int x = 28090;
    std::cout << 28090;
    for (int i = 0; i < 28090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28091(int& val) {
    func_in_28091(val);
    const int x = 28091;
    std::cout << 28091;
    for (int i = 0; i < 28091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28091(int& val) {
    const int x = 28091;
    std::cout << 28091;
    for (int i = 0; i < 28091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28092(int& val) {
    func_in_28092(val);
    const int x = 28092;
    std::cout << 28092;
    for (int i = 0; i < 28092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28092(int& val) {
    const int x = 28092;
    std::cout << 28092;
    for (int i = 0; i < 28092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28093(int& val) {
    func_in_28093(val);
    const int x = 28093;
    std::cout << 28093;
    for (int i = 0; i < 28093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28093(int& val) {
    const int x = 28093;
    std::cout << 28093;
    for (int i = 0; i < 28093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28094(int& val) {
    func_in_28094(val);
    const int x = 28094;
    std::cout << 28094;
    for (int i = 0; i < 28094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28094(int& val) {
    const int x = 28094;
    std::cout << 28094;
    for (int i = 0; i < 28094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28095(int& val) {
    func_in_28095(val);
    const int x = 28095;
    std::cout << 28095;
    for (int i = 0; i < 28095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28095(int& val) {
    const int x = 28095;
    std::cout << 28095;
    for (int i = 0; i < 28095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28096(int& val) {
    func_in_28096(val);
    const int x = 28096;
    std::cout << 28096;
    for (int i = 0; i < 28096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28096(int& val) {
    const int x = 28096;
    std::cout << 28096;
    for (int i = 0; i < 28096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28097(int& val) {
    func_in_28097(val);
    const int x = 28097;
    std::cout << 28097;
    for (int i = 0; i < 28097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28097(int& val) {
    const int x = 28097;
    std::cout << 28097;
    for (int i = 0; i < 28097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28098(int& val) {
    func_in_28098(val);
    const int x = 28098;
    std::cout << 28098;
    for (int i = 0; i < 28098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28098(int& val) {
    const int x = 28098;
    std::cout << 28098;
    for (int i = 0; i < 28098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28099(int& val) {
    func_in_28099(val);
    const int x = 28099;
    std::cout << 28099;
    for (int i = 0; i < 28099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28099(int& val) {
    const int x = 28099;
    std::cout << 28099;
    for (int i = 0; i < 28099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28100(int& val) {
    func_in_28100(val);
    const int x = 28100;
    std::cout << 28100;
    for (int i = 0; i < 28100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28100(int& val) {
    const int x = 28100;
    std::cout << 28100;
    for (int i = 0; i < 28100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28101(int& val) {
    func_in_28101(val);
    const int x = 28101;
    std::cout << 28101;
    for (int i = 0; i < 28101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28101(int& val) {
    const int x = 28101;
    std::cout << 28101;
    for (int i = 0; i < 28101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28102(int& val) {
    func_in_28102(val);
    const int x = 28102;
    std::cout << 28102;
    for (int i = 0; i < 28102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28102(int& val) {
    const int x = 28102;
    std::cout << 28102;
    for (int i = 0; i < 28102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28103(int& val) {
    func_in_28103(val);
    const int x = 28103;
    std::cout << 28103;
    for (int i = 0; i < 28103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28103(int& val) {
    const int x = 28103;
    std::cout << 28103;
    for (int i = 0; i < 28103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28104(int& val) {
    func_in_28104(val);
    const int x = 28104;
    std::cout << 28104;
    for (int i = 0; i < 28104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28104(int& val) {
    const int x = 28104;
    std::cout << 28104;
    for (int i = 0; i < 28104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28105(int& val) {
    func_in_28105(val);
    const int x = 28105;
    std::cout << 28105;
    for (int i = 0; i < 28105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28105(int& val) {
    const int x = 28105;
    std::cout << 28105;
    for (int i = 0; i < 28105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28106(int& val) {
    func_in_28106(val);
    const int x = 28106;
    std::cout << 28106;
    for (int i = 0; i < 28106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28106(int& val) {
    const int x = 28106;
    std::cout << 28106;
    for (int i = 0; i < 28106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28107(int& val) {
    func_in_28107(val);
    const int x = 28107;
    std::cout << 28107;
    for (int i = 0; i < 28107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28107(int& val) {
    const int x = 28107;
    std::cout << 28107;
    for (int i = 0; i < 28107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28108(int& val) {
    func_in_28108(val);
    const int x = 28108;
    std::cout << 28108;
    for (int i = 0; i < 28108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28108(int& val) {
    const int x = 28108;
    std::cout << 28108;
    for (int i = 0; i < 28108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28109(int& val) {
    func_in_28109(val);
    const int x = 28109;
    std::cout << 28109;
    for (int i = 0; i < 28109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28109(int& val) {
    const int x = 28109;
    std::cout << 28109;
    for (int i = 0; i < 28109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28110(int& val) {
    func_in_28110(val);
    const int x = 28110;
    std::cout << 28110;
    for (int i = 0; i < 28110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28110(int& val) {
    const int x = 28110;
    std::cout << 28110;
    for (int i = 0; i < 28110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28111(int& val) {
    func_in_28111(val);
    const int x = 28111;
    std::cout << 28111;
    for (int i = 0; i < 28111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28111(int& val) {
    const int x = 28111;
    std::cout << 28111;
    for (int i = 0; i < 28111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28112(int& val) {
    func_in_28112(val);
    const int x = 28112;
    std::cout << 28112;
    for (int i = 0; i < 28112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28112(int& val) {
    const int x = 28112;
    std::cout << 28112;
    for (int i = 0; i < 28112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28113(int& val) {
    func_in_28113(val);
    const int x = 28113;
    std::cout << 28113;
    for (int i = 0; i < 28113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28113(int& val) {
    const int x = 28113;
    std::cout << 28113;
    for (int i = 0; i < 28113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28114(int& val) {
    func_in_28114(val);
    const int x = 28114;
    std::cout << 28114;
    for (int i = 0; i < 28114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28114(int& val) {
    const int x = 28114;
    std::cout << 28114;
    for (int i = 0; i < 28114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28115(int& val) {
    func_in_28115(val);
    const int x = 28115;
    std::cout << 28115;
    for (int i = 0; i < 28115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28115(int& val) {
    const int x = 28115;
    std::cout << 28115;
    for (int i = 0; i < 28115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28116(int& val) {
    func_in_28116(val);
    const int x = 28116;
    std::cout << 28116;
    for (int i = 0; i < 28116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28116(int& val) {
    const int x = 28116;
    std::cout << 28116;
    for (int i = 0; i < 28116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28117(int& val) {
    func_in_28117(val);
    const int x = 28117;
    std::cout << 28117;
    for (int i = 0; i < 28117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28117(int& val) {
    const int x = 28117;
    std::cout << 28117;
    for (int i = 0; i < 28117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28118(int& val) {
    func_in_28118(val);
    const int x = 28118;
    std::cout << 28118;
    for (int i = 0; i < 28118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28118(int& val) {
    const int x = 28118;
    std::cout << 28118;
    for (int i = 0; i < 28118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28119(int& val) {
    func_in_28119(val);
    const int x = 28119;
    std::cout << 28119;
    for (int i = 0; i < 28119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28119(int& val) {
    const int x = 28119;
    std::cout << 28119;
    for (int i = 0; i < 28119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28120(int& val) {
    func_in_28120(val);
    const int x = 28120;
    std::cout << 28120;
    for (int i = 0; i < 28120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28120(int& val) {
    const int x = 28120;
    std::cout << 28120;
    for (int i = 0; i < 28120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28121(int& val) {
    func_in_28121(val);
    const int x = 28121;
    std::cout << 28121;
    for (int i = 0; i < 28121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28121(int& val) {
    const int x = 28121;
    std::cout << 28121;
    for (int i = 0; i < 28121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28122(int& val) {
    func_in_28122(val);
    const int x = 28122;
    std::cout << 28122;
    for (int i = 0; i < 28122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28122(int& val) {
    const int x = 28122;
    std::cout << 28122;
    for (int i = 0; i < 28122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28123(int& val) {
    func_in_28123(val);
    const int x = 28123;
    std::cout << 28123;
    for (int i = 0; i < 28123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28123(int& val) {
    const int x = 28123;
    std::cout << 28123;
    for (int i = 0; i < 28123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28124(int& val) {
    func_in_28124(val);
    const int x = 28124;
    std::cout << 28124;
    for (int i = 0; i < 28124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28124(int& val) {
    const int x = 28124;
    std::cout << 28124;
    for (int i = 0; i < 28124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28125(int& val) {
    func_in_28125(val);
    const int x = 28125;
    std::cout << 28125;
    for (int i = 0; i < 28125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28125(int& val) {
    const int x = 28125;
    std::cout << 28125;
    for (int i = 0; i < 28125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28126(int& val) {
    func_in_28126(val);
    const int x = 28126;
    std::cout << 28126;
    for (int i = 0; i < 28126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28126(int& val) {
    const int x = 28126;
    std::cout << 28126;
    for (int i = 0; i < 28126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28127(int& val) {
    func_in_28127(val);
    const int x = 28127;
    std::cout << 28127;
    for (int i = 0; i < 28127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28127(int& val) {
    const int x = 28127;
    std::cout << 28127;
    for (int i = 0; i < 28127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28128(int& val) {
    func_in_28128(val);
    const int x = 28128;
    std::cout << 28128;
    for (int i = 0; i < 28128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28128(int& val) {
    const int x = 28128;
    std::cout << 28128;
    for (int i = 0; i < 28128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28129(int& val) {
    func_in_28129(val);
    const int x = 28129;
    std::cout << 28129;
    for (int i = 0; i < 28129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28129(int& val) {
    const int x = 28129;
    std::cout << 28129;
    for (int i = 0; i < 28129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28130(int& val) {
    func_in_28130(val);
    const int x = 28130;
    std::cout << 28130;
    for (int i = 0; i < 28130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28130(int& val) {
    const int x = 28130;
    std::cout << 28130;
    for (int i = 0; i < 28130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28131(int& val) {
    func_in_28131(val);
    const int x = 28131;
    std::cout << 28131;
    for (int i = 0; i < 28131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28131(int& val) {
    const int x = 28131;
    std::cout << 28131;
    for (int i = 0; i < 28131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28132(int& val) {
    func_in_28132(val);
    const int x = 28132;
    std::cout << 28132;
    for (int i = 0; i < 28132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28132(int& val) {
    const int x = 28132;
    std::cout << 28132;
    for (int i = 0; i < 28132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28133(int& val) {
    func_in_28133(val);
    const int x = 28133;
    std::cout << 28133;
    for (int i = 0; i < 28133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28133(int& val) {
    const int x = 28133;
    std::cout << 28133;
    for (int i = 0; i < 28133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28134(int& val) {
    func_in_28134(val);
    const int x = 28134;
    std::cout << 28134;
    for (int i = 0; i < 28134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28134(int& val) {
    const int x = 28134;
    std::cout << 28134;
    for (int i = 0; i < 28134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28135(int& val) {
    func_in_28135(val);
    const int x = 28135;
    std::cout << 28135;
    for (int i = 0; i < 28135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28135(int& val) {
    const int x = 28135;
    std::cout << 28135;
    for (int i = 0; i < 28135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28136(int& val) {
    func_in_28136(val);
    const int x = 28136;
    std::cout << 28136;
    for (int i = 0; i < 28136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28136(int& val) {
    const int x = 28136;
    std::cout << 28136;
    for (int i = 0; i < 28136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28137(int& val) {
    func_in_28137(val);
    const int x = 28137;
    std::cout << 28137;
    for (int i = 0; i < 28137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28137(int& val) {
    const int x = 28137;
    std::cout << 28137;
    for (int i = 0; i < 28137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28138(int& val) {
    func_in_28138(val);
    const int x = 28138;
    std::cout << 28138;
    for (int i = 0; i < 28138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28138(int& val) {
    const int x = 28138;
    std::cout << 28138;
    for (int i = 0; i < 28138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28139(int& val) {
    func_in_28139(val);
    const int x = 28139;
    std::cout << 28139;
    for (int i = 0; i < 28139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28139(int& val) {
    const int x = 28139;
    std::cout << 28139;
    for (int i = 0; i < 28139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28140(int& val) {
    func_in_28140(val);
    const int x = 28140;
    std::cout << 28140;
    for (int i = 0; i < 28140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28140(int& val) {
    const int x = 28140;
    std::cout << 28140;
    for (int i = 0; i < 28140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28141(int& val) {
    func_in_28141(val);
    const int x = 28141;
    std::cout << 28141;
    for (int i = 0; i < 28141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28141(int& val) {
    const int x = 28141;
    std::cout << 28141;
    for (int i = 0; i < 28141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28142(int& val) {
    func_in_28142(val);
    const int x = 28142;
    std::cout << 28142;
    for (int i = 0; i < 28142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28142(int& val) {
    const int x = 28142;
    std::cout << 28142;
    for (int i = 0; i < 28142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28143(int& val) {
    func_in_28143(val);
    const int x = 28143;
    std::cout << 28143;
    for (int i = 0; i < 28143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28143(int& val) {
    const int x = 28143;
    std::cout << 28143;
    for (int i = 0; i < 28143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28144(int& val) {
    func_in_28144(val);
    const int x = 28144;
    std::cout << 28144;
    for (int i = 0; i < 28144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28144(int& val) {
    const int x = 28144;
    std::cout << 28144;
    for (int i = 0; i < 28144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28145(int& val) {
    func_in_28145(val);
    const int x = 28145;
    std::cout << 28145;
    for (int i = 0; i < 28145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28145(int& val) {
    const int x = 28145;
    std::cout << 28145;
    for (int i = 0; i < 28145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28146(int& val) {
    func_in_28146(val);
    const int x = 28146;
    std::cout << 28146;
    for (int i = 0; i < 28146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28146(int& val) {
    const int x = 28146;
    std::cout << 28146;
    for (int i = 0; i < 28146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28147(int& val) {
    func_in_28147(val);
    const int x = 28147;
    std::cout << 28147;
    for (int i = 0; i < 28147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28147(int& val) {
    const int x = 28147;
    std::cout << 28147;
    for (int i = 0; i < 28147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28148(int& val) {
    func_in_28148(val);
    const int x = 28148;
    std::cout << 28148;
    for (int i = 0; i < 28148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28148(int& val) {
    const int x = 28148;
    std::cout << 28148;
    for (int i = 0; i < 28148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28149(int& val) {
    func_in_28149(val);
    const int x = 28149;
    std::cout << 28149;
    for (int i = 0; i < 28149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28149(int& val) {
    const int x = 28149;
    std::cout << 28149;
    for (int i = 0; i < 28149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28150(int& val) {
    func_in_28150(val);
    const int x = 28150;
    std::cout << 28150;
    for (int i = 0; i < 28150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28150(int& val) {
    const int x = 28150;
    std::cout << 28150;
    for (int i = 0; i < 28150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28151(int& val) {
    func_in_28151(val);
    const int x = 28151;
    std::cout << 28151;
    for (int i = 0; i < 28151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28151(int& val) {
    const int x = 28151;
    std::cout << 28151;
    for (int i = 0; i < 28151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28152(int& val) {
    func_in_28152(val);
    const int x = 28152;
    std::cout << 28152;
    for (int i = 0; i < 28152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28152(int& val) {
    const int x = 28152;
    std::cout << 28152;
    for (int i = 0; i < 28152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28153(int& val) {
    func_in_28153(val);
    const int x = 28153;
    std::cout << 28153;
    for (int i = 0; i < 28153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28153(int& val) {
    const int x = 28153;
    std::cout << 28153;
    for (int i = 0; i < 28153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28154(int& val) {
    func_in_28154(val);
    const int x = 28154;
    std::cout << 28154;
    for (int i = 0; i < 28154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28154(int& val) {
    const int x = 28154;
    std::cout << 28154;
    for (int i = 0; i < 28154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28155(int& val) {
    func_in_28155(val);
    const int x = 28155;
    std::cout << 28155;
    for (int i = 0; i < 28155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28155(int& val) {
    const int x = 28155;
    std::cout << 28155;
    for (int i = 0; i < 28155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28156(int& val) {
    func_in_28156(val);
    const int x = 28156;
    std::cout << 28156;
    for (int i = 0; i < 28156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28156(int& val) {
    const int x = 28156;
    std::cout << 28156;
    for (int i = 0; i < 28156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28157(int& val) {
    func_in_28157(val);
    const int x = 28157;
    std::cout << 28157;
    for (int i = 0; i < 28157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28157(int& val) {
    const int x = 28157;
    std::cout << 28157;
    for (int i = 0; i < 28157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28158(int& val) {
    func_in_28158(val);
    const int x = 28158;
    std::cout << 28158;
    for (int i = 0; i < 28158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28158(int& val) {
    const int x = 28158;
    std::cout << 28158;
    for (int i = 0; i < 28158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28159(int& val) {
    func_in_28159(val);
    const int x = 28159;
    std::cout << 28159;
    for (int i = 0; i < 28159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28159(int& val) {
    const int x = 28159;
    std::cout << 28159;
    for (int i = 0; i < 28159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28160(int& val) {
    func_in_28160(val);
    const int x = 28160;
    std::cout << 28160;
    for (int i = 0; i < 28160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28160(int& val) {
    const int x = 28160;
    std::cout << 28160;
    for (int i = 0; i < 28160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28161(int& val) {
    func_in_28161(val);
    const int x = 28161;
    std::cout << 28161;
    for (int i = 0; i < 28161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28161(int& val) {
    const int x = 28161;
    std::cout << 28161;
    for (int i = 0; i < 28161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28162(int& val) {
    func_in_28162(val);
    const int x = 28162;
    std::cout << 28162;
    for (int i = 0; i < 28162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28162(int& val) {
    const int x = 28162;
    std::cout << 28162;
    for (int i = 0; i < 28162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28163(int& val) {
    func_in_28163(val);
    const int x = 28163;
    std::cout << 28163;
    for (int i = 0; i < 28163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28163(int& val) {
    const int x = 28163;
    std::cout << 28163;
    for (int i = 0; i < 28163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28164(int& val) {
    func_in_28164(val);
    const int x = 28164;
    std::cout << 28164;
    for (int i = 0; i < 28164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28164(int& val) {
    const int x = 28164;
    std::cout << 28164;
    for (int i = 0; i < 28164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28165(int& val) {
    func_in_28165(val);
    const int x = 28165;
    std::cout << 28165;
    for (int i = 0; i < 28165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28165(int& val) {
    const int x = 28165;
    std::cout << 28165;
    for (int i = 0; i < 28165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28166(int& val) {
    func_in_28166(val);
    const int x = 28166;
    std::cout << 28166;
    for (int i = 0; i < 28166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28166(int& val) {
    const int x = 28166;
    std::cout << 28166;
    for (int i = 0; i < 28166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28167(int& val) {
    func_in_28167(val);
    const int x = 28167;
    std::cout << 28167;
    for (int i = 0; i < 28167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28167(int& val) {
    const int x = 28167;
    std::cout << 28167;
    for (int i = 0; i < 28167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28168(int& val) {
    func_in_28168(val);
    const int x = 28168;
    std::cout << 28168;
    for (int i = 0; i < 28168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28168(int& val) {
    const int x = 28168;
    std::cout << 28168;
    for (int i = 0; i < 28168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28169(int& val) {
    func_in_28169(val);
    const int x = 28169;
    std::cout << 28169;
    for (int i = 0; i < 28169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28169(int& val) {
    const int x = 28169;
    std::cout << 28169;
    for (int i = 0; i < 28169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28170(int& val) {
    func_in_28170(val);
    const int x = 28170;
    std::cout << 28170;
    for (int i = 0; i < 28170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28170(int& val) {
    const int x = 28170;
    std::cout << 28170;
    for (int i = 0; i < 28170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28171(int& val) {
    func_in_28171(val);
    const int x = 28171;
    std::cout << 28171;
    for (int i = 0; i < 28171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28171(int& val) {
    const int x = 28171;
    std::cout << 28171;
    for (int i = 0; i < 28171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28172(int& val) {
    func_in_28172(val);
    const int x = 28172;
    std::cout << 28172;
    for (int i = 0; i < 28172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28172(int& val) {
    const int x = 28172;
    std::cout << 28172;
    for (int i = 0; i < 28172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28173(int& val) {
    func_in_28173(val);
    const int x = 28173;
    std::cout << 28173;
    for (int i = 0; i < 28173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28173(int& val) {
    const int x = 28173;
    std::cout << 28173;
    for (int i = 0; i < 28173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28174(int& val) {
    func_in_28174(val);
    const int x = 28174;
    std::cout << 28174;
    for (int i = 0; i < 28174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28174(int& val) {
    const int x = 28174;
    std::cout << 28174;
    for (int i = 0; i < 28174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28175(int& val) {
    func_in_28175(val);
    const int x = 28175;
    std::cout << 28175;
    for (int i = 0; i < 28175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28175(int& val) {
    const int x = 28175;
    std::cout << 28175;
    for (int i = 0; i < 28175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28176(int& val) {
    func_in_28176(val);
    const int x = 28176;
    std::cout << 28176;
    for (int i = 0; i < 28176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28176(int& val) {
    const int x = 28176;
    std::cout << 28176;
    for (int i = 0; i < 28176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28177(int& val) {
    func_in_28177(val);
    const int x = 28177;
    std::cout << 28177;
    for (int i = 0; i < 28177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28177(int& val) {
    const int x = 28177;
    std::cout << 28177;
    for (int i = 0; i < 28177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28178(int& val) {
    func_in_28178(val);
    const int x = 28178;
    std::cout << 28178;
    for (int i = 0; i < 28178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28178(int& val) {
    const int x = 28178;
    std::cout << 28178;
    for (int i = 0; i < 28178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28179(int& val) {
    func_in_28179(val);
    const int x = 28179;
    std::cout << 28179;
    for (int i = 0; i < 28179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28179(int& val) {
    const int x = 28179;
    std::cout << 28179;
    for (int i = 0; i < 28179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28180(int& val) {
    func_in_28180(val);
    const int x = 28180;
    std::cout << 28180;
    for (int i = 0; i < 28180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28180(int& val) {
    const int x = 28180;
    std::cout << 28180;
    for (int i = 0; i < 28180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28181(int& val) {
    func_in_28181(val);
    const int x = 28181;
    std::cout << 28181;
    for (int i = 0; i < 28181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28181(int& val) {
    const int x = 28181;
    std::cout << 28181;
    for (int i = 0; i < 28181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28182(int& val) {
    func_in_28182(val);
    const int x = 28182;
    std::cout << 28182;
    for (int i = 0; i < 28182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28182(int& val) {
    const int x = 28182;
    std::cout << 28182;
    for (int i = 0; i < 28182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28183(int& val) {
    func_in_28183(val);
    const int x = 28183;
    std::cout << 28183;
    for (int i = 0; i < 28183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28183(int& val) {
    const int x = 28183;
    std::cout << 28183;
    for (int i = 0; i < 28183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28184(int& val) {
    func_in_28184(val);
    const int x = 28184;
    std::cout << 28184;
    for (int i = 0; i < 28184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28184(int& val) {
    const int x = 28184;
    std::cout << 28184;
    for (int i = 0; i < 28184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28185(int& val) {
    func_in_28185(val);
    const int x = 28185;
    std::cout << 28185;
    for (int i = 0; i < 28185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28185(int& val) {
    const int x = 28185;
    std::cout << 28185;
    for (int i = 0; i < 28185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28186(int& val) {
    func_in_28186(val);
    const int x = 28186;
    std::cout << 28186;
    for (int i = 0; i < 28186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28186(int& val) {
    const int x = 28186;
    std::cout << 28186;
    for (int i = 0; i < 28186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28187(int& val) {
    func_in_28187(val);
    const int x = 28187;
    std::cout << 28187;
    for (int i = 0; i < 28187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28187(int& val) {
    const int x = 28187;
    std::cout << 28187;
    for (int i = 0; i < 28187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28188(int& val) {
    func_in_28188(val);
    const int x = 28188;
    std::cout << 28188;
    for (int i = 0; i < 28188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28188(int& val) {
    const int x = 28188;
    std::cout << 28188;
    for (int i = 0; i < 28188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28189(int& val) {
    func_in_28189(val);
    const int x = 28189;
    std::cout << 28189;
    for (int i = 0; i < 28189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28189(int& val) {
    const int x = 28189;
    std::cout << 28189;
    for (int i = 0; i < 28189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28190(int& val) {
    func_in_28190(val);
    const int x = 28190;
    std::cout << 28190;
    for (int i = 0; i < 28190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28190(int& val) {
    const int x = 28190;
    std::cout << 28190;
    for (int i = 0; i < 28190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28191(int& val) {
    func_in_28191(val);
    const int x = 28191;
    std::cout << 28191;
    for (int i = 0; i < 28191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28191(int& val) {
    const int x = 28191;
    std::cout << 28191;
    for (int i = 0; i < 28191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28192(int& val) {
    func_in_28192(val);
    const int x = 28192;
    std::cout << 28192;
    for (int i = 0; i < 28192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28192(int& val) {
    const int x = 28192;
    std::cout << 28192;
    for (int i = 0; i < 28192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28193(int& val) {
    func_in_28193(val);
    const int x = 28193;
    std::cout << 28193;
    for (int i = 0; i < 28193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28193(int& val) {
    const int x = 28193;
    std::cout << 28193;
    for (int i = 0; i < 28193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28194(int& val) {
    func_in_28194(val);
    const int x = 28194;
    std::cout << 28194;
    for (int i = 0; i < 28194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28194(int& val) {
    const int x = 28194;
    std::cout << 28194;
    for (int i = 0; i < 28194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28195(int& val) {
    func_in_28195(val);
    const int x = 28195;
    std::cout << 28195;
    for (int i = 0; i < 28195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28195(int& val) {
    const int x = 28195;
    std::cout << 28195;
    for (int i = 0; i < 28195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28196(int& val) {
    func_in_28196(val);
    const int x = 28196;
    std::cout << 28196;
    for (int i = 0; i < 28196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28196(int& val) {
    const int x = 28196;
    std::cout << 28196;
    for (int i = 0; i < 28196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28197(int& val) {
    func_in_28197(val);
    const int x = 28197;
    std::cout << 28197;
    for (int i = 0; i < 28197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28197(int& val) {
    const int x = 28197;
    std::cout << 28197;
    for (int i = 0; i < 28197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28198(int& val) {
    func_in_28198(val);
    const int x = 28198;
    std::cout << 28198;
    for (int i = 0; i < 28198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28198(int& val) {
    const int x = 28198;
    std::cout << 28198;
    for (int i = 0; i < 28198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28199(int& val) {
    func_in_28199(val);
    const int x = 28199;
    std::cout << 28199;
    for (int i = 0; i < 28199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28199(int& val) {
    const int x = 28199;
    std::cout << 28199;
    for (int i = 0; i < 28199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28200(int& val) {
    func_in_28200(val);
    const int x = 28200;
    std::cout << 28200;
    for (int i = 0; i < 28200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28200(int& val) {
    const int x = 28200;
    std::cout << 28200;
    for (int i = 0; i < 28200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28201(int& val) {
    func_in_28201(val);
    const int x = 28201;
    std::cout << 28201;
    for (int i = 0; i < 28201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28201(int& val) {
    const int x = 28201;
    std::cout << 28201;
    for (int i = 0; i < 28201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28202(int& val) {
    func_in_28202(val);
    const int x = 28202;
    std::cout << 28202;
    for (int i = 0; i < 28202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28202(int& val) {
    const int x = 28202;
    std::cout << 28202;
    for (int i = 0; i < 28202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28203(int& val) {
    func_in_28203(val);
    const int x = 28203;
    std::cout << 28203;
    for (int i = 0; i < 28203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28203(int& val) {
    const int x = 28203;
    std::cout << 28203;
    for (int i = 0; i < 28203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28204(int& val) {
    func_in_28204(val);
    const int x = 28204;
    std::cout << 28204;
    for (int i = 0; i < 28204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28204(int& val) {
    const int x = 28204;
    std::cout << 28204;
    for (int i = 0; i < 28204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28205(int& val) {
    func_in_28205(val);
    const int x = 28205;
    std::cout << 28205;
    for (int i = 0; i < 28205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28205(int& val) {
    const int x = 28205;
    std::cout << 28205;
    for (int i = 0; i < 28205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28206(int& val) {
    func_in_28206(val);
    const int x = 28206;
    std::cout << 28206;
    for (int i = 0; i < 28206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28206(int& val) {
    const int x = 28206;
    std::cout << 28206;
    for (int i = 0; i < 28206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28207(int& val) {
    func_in_28207(val);
    const int x = 28207;
    std::cout << 28207;
    for (int i = 0; i < 28207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28207(int& val) {
    const int x = 28207;
    std::cout << 28207;
    for (int i = 0; i < 28207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28208(int& val) {
    func_in_28208(val);
    const int x = 28208;
    std::cout << 28208;
    for (int i = 0; i < 28208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28208(int& val) {
    const int x = 28208;
    std::cout << 28208;
    for (int i = 0; i < 28208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28209(int& val) {
    func_in_28209(val);
    const int x = 28209;
    std::cout << 28209;
    for (int i = 0; i < 28209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28209(int& val) {
    const int x = 28209;
    std::cout << 28209;
    for (int i = 0; i < 28209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28210(int& val) {
    func_in_28210(val);
    const int x = 28210;
    std::cout << 28210;
    for (int i = 0; i < 28210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28210(int& val) {
    const int x = 28210;
    std::cout << 28210;
    for (int i = 0; i < 28210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28211(int& val) {
    func_in_28211(val);
    const int x = 28211;
    std::cout << 28211;
    for (int i = 0; i < 28211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28211(int& val) {
    const int x = 28211;
    std::cout << 28211;
    for (int i = 0; i < 28211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28212(int& val) {
    func_in_28212(val);
    const int x = 28212;
    std::cout << 28212;
    for (int i = 0; i < 28212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28212(int& val) {
    const int x = 28212;
    std::cout << 28212;
    for (int i = 0; i < 28212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28213(int& val) {
    func_in_28213(val);
    const int x = 28213;
    std::cout << 28213;
    for (int i = 0; i < 28213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28213(int& val) {
    const int x = 28213;
    std::cout << 28213;
    for (int i = 0; i < 28213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28214(int& val) {
    func_in_28214(val);
    const int x = 28214;
    std::cout << 28214;
    for (int i = 0; i < 28214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28214(int& val) {
    const int x = 28214;
    std::cout << 28214;
    for (int i = 0; i < 28214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28215(int& val) {
    func_in_28215(val);
    const int x = 28215;
    std::cout << 28215;
    for (int i = 0; i < 28215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28215(int& val) {
    const int x = 28215;
    std::cout << 28215;
    for (int i = 0; i < 28215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28216(int& val) {
    func_in_28216(val);
    const int x = 28216;
    std::cout << 28216;
    for (int i = 0; i < 28216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28216(int& val) {
    const int x = 28216;
    std::cout << 28216;
    for (int i = 0; i < 28216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28217(int& val) {
    func_in_28217(val);
    const int x = 28217;
    std::cout << 28217;
    for (int i = 0; i < 28217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28217(int& val) {
    const int x = 28217;
    std::cout << 28217;
    for (int i = 0; i < 28217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28218(int& val) {
    func_in_28218(val);
    const int x = 28218;
    std::cout << 28218;
    for (int i = 0; i < 28218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28218(int& val) {
    const int x = 28218;
    std::cout << 28218;
    for (int i = 0; i < 28218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28219(int& val) {
    func_in_28219(val);
    const int x = 28219;
    std::cout << 28219;
    for (int i = 0; i < 28219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28219(int& val) {
    const int x = 28219;
    std::cout << 28219;
    for (int i = 0; i < 28219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28220(int& val) {
    func_in_28220(val);
    const int x = 28220;
    std::cout << 28220;
    for (int i = 0; i < 28220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28220(int& val) {
    const int x = 28220;
    std::cout << 28220;
    for (int i = 0; i < 28220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28221(int& val) {
    func_in_28221(val);
    const int x = 28221;
    std::cout << 28221;
    for (int i = 0; i < 28221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28221(int& val) {
    const int x = 28221;
    std::cout << 28221;
    for (int i = 0; i < 28221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28222(int& val) {
    func_in_28222(val);
    const int x = 28222;
    std::cout << 28222;
    for (int i = 0; i < 28222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28222(int& val) {
    const int x = 28222;
    std::cout << 28222;
    for (int i = 0; i < 28222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28223(int& val) {
    func_in_28223(val);
    const int x = 28223;
    std::cout << 28223;
    for (int i = 0; i < 28223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28223(int& val) {
    const int x = 28223;
    std::cout << 28223;
    for (int i = 0; i < 28223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28224(int& val) {
    func_in_28224(val);
    const int x = 28224;
    std::cout << 28224;
    for (int i = 0; i < 28224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28224(int& val) {
    const int x = 28224;
    std::cout << 28224;
    for (int i = 0; i < 28224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28225(int& val) {
    func_in_28225(val);
    const int x = 28225;
    std::cout << 28225;
    for (int i = 0; i < 28225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28225(int& val) {
    const int x = 28225;
    std::cout << 28225;
    for (int i = 0; i < 28225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28226(int& val) {
    func_in_28226(val);
    const int x = 28226;
    std::cout << 28226;
    for (int i = 0; i < 28226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28226(int& val) {
    const int x = 28226;
    std::cout << 28226;
    for (int i = 0; i < 28226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28227(int& val) {
    func_in_28227(val);
    const int x = 28227;
    std::cout << 28227;
    for (int i = 0; i < 28227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28227(int& val) {
    const int x = 28227;
    std::cout << 28227;
    for (int i = 0; i < 28227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28228(int& val) {
    func_in_28228(val);
    const int x = 28228;
    std::cout << 28228;
    for (int i = 0; i < 28228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28228(int& val) {
    const int x = 28228;
    std::cout << 28228;
    for (int i = 0; i < 28228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28229(int& val) {
    func_in_28229(val);
    const int x = 28229;
    std::cout << 28229;
    for (int i = 0; i < 28229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28229(int& val) {
    const int x = 28229;
    std::cout << 28229;
    for (int i = 0; i < 28229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28230(int& val) {
    func_in_28230(val);
    const int x = 28230;
    std::cout << 28230;
    for (int i = 0; i < 28230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28230(int& val) {
    const int x = 28230;
    std::cout << 28230;
    for (int i = 0; i < 28230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28231(int& val) {
    func_in_28231(val);
    const int x = 28231;
    std::cout << 28231;
    for (int i = 0; i < 28231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28231(int& val) {
    const int x = 28231;
    std::cout << 28231;
    for (int i = 0; i < 28231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28232(int& val) {
    func_in_28232(val);
    const int x = 28232;
    std::cout << 28232;
    for (int i = 0; i < 28232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28232(int& val) {
    const int x = 28232;
    std::cout << 28232;
    for (int i = 0; i < 28232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28233(int& val) {
    func_in_28233(val);
    const int x = 28233;
    std::cout << 28233;
    for (int i = 0; i < 28233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28233(int& val) {
    const int x = 28233;
    std::cout << 28233;
    for (int i = 0; i < 28233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28234(int& val) {
    func_in_28234(val);
    const int x = 28234;
    std::cout << 28234;
    for (int i = 0; i < 28234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28234(int& val) {
    const int x = 28234;
    std::cout << 28234;
    for (int i = 0; i < 28234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28235(int& val) {
    func_in_28235(val);
    const int x = 28235;
    std::cout << 28235;
    for (int i = 0; i < 28235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28235(int& val) {
    const int x = 28235;
    std::cout << 28235;
    for (int i = 0; i < 28235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28236(int& val) {
    func_in_28236(val);
    const int x = 28236;
    std::cout << 28236;
    for (int i = 0; i < 28236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28236(int& val) {
    const int x = 28236;
    std::cout << 28236;
    for (int i = 0; i < 28236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28237(int& val) {
    func_in_28237(val);
    const int x = 28237;
    std::cout << 28237;
    for (int i = 0; i < 28237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28237(int& val) {
    const int x = 28237;
    std::cout << 28237;
    for (int i = 0; i < 28237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28238(int& val) {
    func_in_28238(val);
    const int x = 28238;
    std::cout << 28238;
    for (int i = 0; i < 28238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28238(int& val) {
    const int x = 28238;
    std::cout << 28238;
    for (int i = 0; i < 28238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28239(int& val) {
    func_in_28239(val);
    const int x = 28239;
    std::cout << 28239;
    for (int i = 0; i < 28239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28239(int& val) {
    const int x = 28239;
    std::cout << 28239;
    for (int i = 0; i < 28239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28240(int& val) {
    func_in_28240(val);
    const int x = 28240;
    std::cout << 28240;
    for (int i = 0; i < 28240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28240(int& val) {
    const int x = 28240;
    std::cout << 28240;
    for (int i = 0; i < 28240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28241(int& val) {
    func_in_28241(val);
    const int x = 28241;
    std::cout << 28241;
    for (int i = 0; i < 28241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28241(int& val) {
    const int x = 28241;
    std::cout << 28241;
    for (int i = 0; i < 28241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28242(int& val) {
    func_in_28242(val);
    const int x = 28242;
    std::cout << 28242;
    for (int i = 0; i < 28242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28242(int& val) {
    const int x = 28242;
    std::cout << 28242;
    for (int i = 0; i < 28242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28243(int& val) {
    func_in_28243(val);
    const int x = 28243;
    std::cout << 28243;
    for (int i = 0; i < 28243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28243(int& val) {
    const int x = 28243;
    std::cout << 28243;
    for (int i = 0; i < 28243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28244(int& val) {
    func_in_28244(val);
    const int x = 28244;
    std::cout << 28244;
    for (int i = 0; i < 28244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28244(int& val) {
    const int x = 28244;
    std::cout << 28244;
    for (int i = 0; i < 28244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28245(int& val) {
    func_in_28245(val);
    const int x = 28245;
    std::cout << 28245;
    for (int i = 0; i < 28245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28245(int& val) {
    const int x = 28245;
    std::cout << 28245;
    for (int i = 0; i < 28245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28246(int& val) {
    func_in_28246(val);
    const int x = 28246;
    std::cout << 28246;
    for (int i = 0; i < 28246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28246(int& val) {
    const int x = 28246;
    std::cout << 28246;
    for (int i = 0; i < 28246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28247(int& val) {
    func_in_28247(val);
    const int x = 28247;
    std::cout << 28247;
    for (int i = 0; i < 28247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28247(int& val) {
    const int x = 28247;
    std::cout << 28247;
    for (int i = 0; i < 28247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28248(int& val) {
    func_in_28248(val);
    const int x = 28248;
    std::cout << 28248;
    for (int i = 0; i < 28248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28248(int& val) {
    const int x = 28248;
    std::cout << 28248;
    for (int i = 0; i < 28248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28249(int& val) {
    func_in_28249(val);
    const int x = 28249;
    std::cout << 28249;
    for (int i = 0; i < 28249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28249(int& val) {
    const int x = 28249;
    std::cout << 28249;
    for (int i = 0; i < 28249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28250(int& val) {
    func_in_28250(val);
    const int x = 28250;
    std::cout << 28250;
    for (int i = 0; i < 28250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28250(int& val) {
    const int x = 28250;
    std::cout << 28250;
    for (int i = 0; i < 28250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28251(int& val) {
    func_in_28251(val);
    const int x = 28251;
    std::cout << 28251;
    for (int i = 0; i < 28251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28251(int& val) {
    const int x = 28251;
    std::cout << 28251;
    for (int i = 0; i < 28251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28252(int& val) {
    func_in_28252(val);
    const int x = 28252;
    std::cout << 28252;
    for (int i = 0; i < 28252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28252(int& val) {
    const int x = 28252;
    std::cout << 28252;
    for (int i = 0; i < 28252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28253(int& val) {
    func_in_28253(val);
    const int x = 28253;
    std::cout << 28253;
    for (int i = 0; i < 28253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28253(int& val) {
    const int x = 28253;
    std::cout << 28253;
    for (int i = 0; i < 28253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28254(int& val) {
    func_in_28254(val);
    const int x = 28254;
    std::cout << 28254;
    for (int i = 0; i < 28254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28254(int& val) {
    const int x = 28254;
    std::cout << 28254;
    for (int i = 0; i < 28254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28255(int& val) {
    func_in_28255(val);
    const int x = 28255;
    std::cout << 28255;
    for (int i = 0; i < 28255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28255(int& val) {
    const int x = 28255;
    std::cout << 28255;
    for (int i = 0; i < 28255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28256(int& val) {
    func_in_28256(val);
    const int x = 28256;
    std::cout << 28256;
    for (int i = 0; i < 28256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28256(int& val) {
    const int x = 28256;
    std::cout << 28256;
    for (int i = 0; i < 28256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28257(int& val) {
    func_in_28257(val);
    const int x = 28257;
    std::cout << 28257;
    for (int i = 0; i < 28257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28257(int& val) {
    const int x = 28257;
    std::cout << 28257;
    for (int i = 0; i < 28257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28258(int& val) {
    func_in_28258(val);
    const int x = 28258;
    std::cout << 28258;
    for (int i = 0; i < 28258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28258(int& val) {
    const int x = 28258;
    std::cout << 28258;
    for (int i = 0; i < 28258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28259(int& val) {
    func_in_28259(val);
    const int x = 28259;
    std::cout << 28259;
    for (int i = 0; i < 28259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28259(int& val) {
    const int x = 28259;
    std::cout << 28259;
    for (int i = 0; i < 28259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28260(int& val) {
    func_in_28260(val);
    const int x = 28260;
    std::cout << 28260;
    for (int i = 0; i < 28260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28260(int& val) {
    const int x = 28260;
    std::cout << 28260;
    for (int i = 0; i < 28260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28261(int& val) {
    func_in_28261(val);
    const int x = 28261;
    std::cout << 28261;
    for (int i = 0; i < 28261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28261(int& val) {
    const int x = 28261;
    std::cout << 28261;
    for (int i = 0; i < 28261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28262(int& val) {
    func_in_28262(val);
    const int x = 28262;
    std::cout << 28262;
    for (int i = 0; i < 28262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28262(int& val) {
    const int x = 28262;
    std::cout << 28262;
    for (int i = 0; i < 28262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28263(int& val) {
    func_in_28263(val);
    const int x = 28263;
    std::cout << 28263;
    for (int i = 0; i < 28263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28263(int& val) {
    const int x = 28263;
    std::cout << 28263;
    for (int i = 0; i < 28263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28264(int& val) {
    func_in_28264(val);
    const int x = 28264;
    std::cout << 28264;
    for (int i = 0; i < 28264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28264(int& val) {
    const int x = 28264;
    std::cout << 28264;
    for (int i = 0; i < 28264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28265(int& val) {
    func_in_28265(val);
    const int x = 28265;
    std::cout << 28265;
    for (int i = 0; i < 28265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28265(int& val) {
    const int x = 28265;
    std::cout << 28265;
    for (int i = 0; i < 28265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28266(int& val) {
    func_in_28266(val);
    const int x = 28266;
    std::cout << 28266;
    for (int i = 0; i < 28266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28266(int& val) {
    const int x = 28266;
    std::cout << 28266;
    for (int i = 0; i < 28266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28267(int& val) {
    func_in_28267(val);
    const int x = 28267;
    std::cout << 28267;
    for (int i = 0; i < 28267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28267(int& val) {
    const int x = 28267;
    std::cout << 28267;
    for (int i = 0; i < 28267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28268(int& val) {
    func_in_28268(val);
    const int x = 28268;
    std::cout << 28268;
    for (int i = 0; i < 28268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28268(int& val) {
    const int x = 28268;
    std::cout << 28268;
    for (int i = 0; i < 28268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28269(int& val) {
    func_in_28269(val);
    const int x = 28269;
    std::cout << 28269;
    for (int i = 0; i < 28269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28269(int& val) {
    const int x = 28269;
    std::cout << 28269;
    for (int i = 0; i < 28269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28270(int& val) {
    func_in_28270(val);
    const int x = 28270;
    std::cout << 28270;
    for (int i = 0; i < 28270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28270(int& val) {
    const int x = 28270;
    std::cout << 28270;
    for (int i = 0; i < 28270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28271(int& val) {
    func_in_28271(val);
    const int x = 28271;
    std::cout << 28271;
    for (int i = 0; i < 28271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28271(int& val) {
    const int x = 28271;
    std::cout << 28271;
    for (int i = 0; i < 28271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28272(int& val) {
    func_in_28272(val);
    const int x = 28272;
    std::cout << 28272;
    for (int i = 0; i < 28272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28272(int& val) {
    const int x = 28272;
    std::cout << 28272;
    for (int i = 0; i < 28272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28273(int& val) {
    func_in_28273(val);
    const int x = 28273;
    std::cout << 28273;
    for (int i = 0; i < 28273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28273(int& val) {
    const int x = 28273;
    std::cout << 28273;
    for (int i = 0; i < 28273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28274(int& val) {
    func_in_28274(val);
    const int x = 28274;
    std::cout << 28274;
    for (int i = 0; i < 28274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28274(int& val) {
    const int x = 28274;
    std::cout << 28274;
    for (int i = 0; i < 28274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28275(int& val) {
    func_in_28275(val);
    const int x = 28275;
    std::cout << 28275;
    for (int i = 0; i < 28275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28275(int& val) {
    const int x = 28275;
    std::cout << 28275;
    for (int i = 0; i < 28275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28276(int& val) {
    func_in_28276(val);
    const int x = 28276;
    std::cout << 28276;
    for (int i = 0; i < 28276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28276(int& val) {
    const int x = 28276;
    std::cout << 28276;
    for (int i = 0; i < 28276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28277(int& val) {
    func_in_28277(val);
    const int x = 28277;
    std::cout << 28277;
    for (int i = 0; i < 28277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28277(int& val) {
    const int x = 28277;
    std::cout << 28277;
    for (int i = 0; i < 28277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28278(int& val) {
    func_in_28278(val);
    const int x = 28278;
    std::cout << 28278;
    for (int i = 0; i < 28278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28278(int& val) {
    const int x = 28278;
    std::cout << 28278;
    for (int i = 0; i < 28278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28279(int& val) {
    func_in_28279(val);
    const int x = 28279;
    std::cout << 28279;
    for (int i = 0; i < 28279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28279(int& val) {
    const int x = 28279;
    std::cout << 28279;
    for (int i = 0; i < 28279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28280(int& val) {
    func_in_28280(val);
    const int x = 28280;
    std::cout << 28280;
    for (int i = 0; i < 28280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28280(int& val) {
    const int x = 28280;
    std::cout << 28280;
    for (int i = 0; i < 28280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28281(int& val) {
    func_in_28281(val);
    const int x = 28281;
    std::cout << 28281;
    for (int i = 0; i < 28281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28281(int& val) {
    const int x = 28281;
    std::cout << 28281;
    for (int i = 0; i < 28281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28282(int& val) {
    func_in_28282(val);
    const int x = 28282;
    std::cout << 28282;
    for (int i = 0; i < 28282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28282(int& val) {
    const int x = 28282;
    std::cout << 28282;
    for (int i = 0; i < 28282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28283(int& val) {
    func_in_28283(val);
    const int x = 28283;
    std::cout << 28283;
    for (int i = 0; i < 28283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28283(int& val) {
    const int x = 28283;
    std::cout << 28283;
    for (int i = 0; i < 28283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28284(int& val) {
    func_in_28284(val);
    const int x = 28284;
    std::cout << 28284;
    for (int i = 0; i < 28284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28284(int& val) {
    const int x = 28284;
    std::cout << 28284;
    for (int i = 0; i < 28284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28285(int& val) {
    func_in_28285(val);
    const int x = 28285;
    std::cout << 28285;
    for (int i = 0; i < 28285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28285(int& val) {
    const int x = 28285;
    std::cout << 28285;
    for (int i = 0; i < 28285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28286(int& val) {
    func_in_28286(val);
    const int x = 28286;
    std::cout << 28286;
    for (int i = 0; i < 28286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28286(int& val) {
    const int x = 28286;
    std::cout << 28286;
    for (int i = 0; i < 28286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28287(int& val) {
    func_in_28287(val);
    const int x = 28287;
    std::cout << 28287;
    for (int i = 0; i < 28287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28287(int& val) {
    const int x = 28287;
    std::cout << 28287;
    for (int i = 0; i < 28287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28288(int& val) {
    func_in_28288(val);
    const int x = 28288;
    std::cout << 28288;
    for (int i = 0; i < 28288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28288(int& val) {
    const int x = 28288;
    std::cout << 28288;
    for (int i = 0; i < 28288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28289(int& val) {
    func_in_28289(val);
    const int x = 28289;
    std::cout << 28289;
    for (int i = 0; i < 28289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28289(int& val) {
    const int x = 28289;
    std::cout << 28289;
    for (int i = 0; i < 28289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28290(int& val) {
    func_in_28290(val);
    const int x = 28290;
    std::cout << 28290;
    for (int i = 0; i < 28290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28290(int& val) {
    const int x = 28290;
    std::cout << 28290;
    for (int i = 0; i < 28290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28291(int& val) {
    func_in_28291(val);
    const int x = 28291;
    std::cout << 28291;
    for (int i = 0; i < 28291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28291(int& val) {
    const int x = 28291;
    std::cout << 28291;
    for (int i = 0; i < 28291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28292(int& val) {
    func_in_28292(val);
    const int x = 28292;
    std::cout << 28292;
    for (int i = 0; i < 28292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28292(int& val) {
    const int x = 28292;
    std::cout << 28292;
    for (int i = 0; i < 28292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28293(int& val) {
    func_in_28293(val);
    const int x = 28293;
    std::cout << 28293;
    for (int i = 0; i < 28293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28293(int& val) {
    const int x = 28293;
    std::cout << 28293;
    for (int i = 0; i < 28293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28294(int& val) {
    func_in_28294(val);
    const int x = 28294;
    std::cout << 28294;
    for (int i = 0; i < 28294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28294(int& val) {
    const int x = 28294;
    std::cout << 28294;
    for (int i = 0; i < 28294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28295(int& val) {
    func_in_28295(val);
    const int x = 28295;
    std::cout << 28295;
    for (int i = 0; i < 28295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28295(int& val) {
    const int x = 28295;
    std::cout << 28295;
    for (int i = 0; i < 28295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28296(int& val) {
    func_in_28296(val);
    const int x = 28296;
    std::cout << 28296;
    for (int i = 0; i < 28296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28296(int& val) {
    const int x = 28296;
    std::cout << 28296;
    for (int i = 0; i < 28296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28297(int& val) {
    func_in_28297(val);
    const int x = 28297;
    std::cout << 28297;
    for (int i = 0; i < 28297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28297(int& val) {
    const int x = 28297;
    std::cout << 28297;
    for (int i = 0; i < 28297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28298(int& val) {
    func_in_28298(val);
    const int x = 28298;
    std::cout << 28298;
    for (int i = 0; i < 28298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28298(int& val) {
    const int x = 28298;
    std::cout << 28298;
    for (int i = 0; i < 28298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28299(int& val) {
    func_in_28299(val);
    const int x = 28299;
    std::cout << 28299;
    for (int i = 0; i < 28299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28299(int& val) {
    const int x = 28299;
    std::cout << 28299;
    for (int i = 0; i < 28299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28300(int& val) {
    func_in_28300(val);
    const int x = 28300;
    std::cout << 28300;
    for (int i = 0; i < 28300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28300(int& val) {
    const int x = 28300;
    std::cout << 28300;
    for (int i = 0; i < 28300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28301(int& val) {
    func_in_28301(val);
    const int x = 28301;
    std::cout << 28301;
    for (int i = 0; i < 28301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28301(int& val) {
    const int x = 28301;
    std::cout << 28301;
    for (int i = 0; i < 28301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28302(int& val) {
    func_in_28302(val);
    const int x = 28302;
    std::cout << 28302;
    for (int i = 0; i < 28302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28302(int& val) {
    const int x = 28302;
    std::cout << 28302;
    for (int i = 0; i < 28302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28303(int& val) {
    func_in_28303(val);
    const int x = 28303;
    std::cout << 28303;
    for (int i = 0; i < 28303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28303(int& val) {
    const int x = 28303;
    std::cout << 28303;
    for (int i = 0; i < 28303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28304(int& val) {
    func_in_28304(val);
    const int x = 28304;
    std::cout << 28304;
    for (int i = 0; i < 28304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28304(int& val) {
    const int x = 28304;
    std::cout << 28304;
    for (int i = 0; i < 28304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28305(int& val) {
    func_in_28305(val);
    const int x = 28305;
    std::cout << 28305;
    for (int i = 0; i < 28305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28305(int& val) {
    const int x = 28305;
    std::cout << 28305;
    for (int i = 0; i < 28305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28306(int& val) {
    func_in_28306(val);
    const int x = 28306;
    std::cout << 28306;
    for (int i = 0; i < 28306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28306(int& val) {
    const int x = 28306;
    std::cout << 28306;
    for (int i = 0; i < 28306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28307(int& val) {
    func_in_28307(val);
    const int x = 28307;
    std::cout << 28307;
    for (int i = 0; i < 28307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28307(int& val) {
    const int x = 28307;
    std::cout << 28307;
    for (int i = 0; i < 28307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28308(int& val) {
    func_in_28308(val);
    const int x = 28308;
    std::cout << 28308;
    for (int i = 0; i < 28308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28308(int& val) {
    const int x = 28308;
    std::cout << 28308;
    for (int i = 0; i < 28308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28309(int& val) {
    func_in_28309(val);
    const int x = 28309;
    std::cout << 28309;
    for (int i = 0; i < 28309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28309(int& val) {
    const int x = 28309;
    std::cout << 28309;
    for (int i = 0; i < 28309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28310(int& val) {
    func_in_28310(val);
    const int x = 28310;
    std::cout << 28310;
    for (int i = 0; i < 28310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28310(int& val) {
    const int x = 28310;
    std::cout << 28310;
    for (int i = 0; i < 28310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28311(int& val) {
    func_in_28311(val);
    const int x = 28311;
    std::cout << 28311;
    for (int i = 0; i < 28311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28311(int& val) {
    const int x = 28311;
    std::cout << 28311;
    for (int i = 0; i < 28311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28312(int& val) {
    func_in_28312(val);
    const int x = 28312;
    std::cout << 28312;
    for (int i = 0; i < 28312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28312(int& val) {
    const int x = 28312;
    std::cout << 28312;
    for (int i = 0; i < 28312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28313(int& val) {
    func_in_28313(val);
    const int x = 28313;
    std::cout << 28313;
    for (int i = 0; i < 28313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28313(int& val) {
    const int x = 28313;
    std::cout << 28313;
    for (int i = 0; i < 28313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28314(int& val) {
    func_in_28314(val);
    const int x = 28314;
    std::cout << 28314;
    for (int i = 0; i < 28314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28314(int& val) {
    const int x = 28314;
    std::cout << 28314;
    for (int i = 0; i < 28314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28315(int& val) {
    func_in_28315(val);
    const int x = 28315;
    std::cout << 28315;
    for (int i = 0; i < 28315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28315(int& val) {
    const int x = 28315;
    std::cout << 28315;
    for (int i = 0; i < 28315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28316(int& val) {
    func_in_28316(val);
    const int x = 28316;
    std::cout << 28316;
    for (int i = 0; i < 28316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28316(int& val) {
    const int x = 28316;
    std::cout << 28316;
    for (int i = 0; i < 28316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28317(int& val) {
    func_in_28317(val);
    const int x = 28317;
    std::cout << 28317;
    for (int i = 0; i < 28317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28317(int& val) {
    const int x = 28317;
    std::cout << 28317;
    for (int i = 0; i < 28317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28318(int& val) {
    func_in_28318(val);
    const int x = 28318;
    std::cout << 28318;
    for (int i = 0; i < 28318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28318(int& val) {
    const int x = 28318;
    std::cout << 28318;
    for (int i = 0; i < 28318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28319(int& val) {
    func_in_28319(val);
    const int x = 28319;
    std::cout << 28319;
    for (int i = 0; i < 28319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28319(int& val) {
    const int x = 28319;
    std::cout << 28319;
    for (int i = 0; i < 28319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28320(int& val) {
    func_in_28320(val);
    const int x = 28320;
    std::cout << 28320;
    for (int i = 0; i < 28320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28320(int& val) {
    const int x = 28320;
    std::cout << 28320;
    for (int i = 0; i < 28320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28321(int& val) {
    func_in_28321(val);
    const int x = 28321;
    std::cout << 28321;
    for (int i = 0; i < 28321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28321(int& val) {
    const int x = 28321;
    std::cout << 28321;
    for (int i = 0; i < 28321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28322(int& val) {
    func_in_28322(val);
    const int x = 28322;
    std::cout << 28322;
    for (int i = 0; i < 28322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28322(int& val) {
    const int x = 28322;
    std::cout << 28322;
    for (int i = 0; i < 28322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28323(int& val) {
    func_in_28323(val);
    const int x = 28323;
    std::cout << 28323;
    for (int i = 0; i < 28323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28323(int& val) {
    const int x = 28323;
    std::cout << 28323;
    for (int i = 0; i < 28323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28324(int& val) {
    func_in_28324(val);
    const int x = 28324;
    std::cout << 28324;
    for (int i = 0; i < 28324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28324(int& val) {
    const int x = 28324;
    std::cout << 28324;
    for (int i = 0; i < 28324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28325(int& val) {
    func_in_28325(val);
    const int x = 28325;
    std::cout << 28325;
    for (int i = 0; i < 28325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28325(int& val) {
    const int x = 28325;
    std::cout << 28325;
    for (int i = 0; i < 28325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28326(int& val) {
    func_in_28326(val);
    const int x = 28326;
    std::cout << 28326;
    for (int i = 0; i < 28326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28326(int& val) {
    const int x = 28326;
    std::cout << 28326;
    for (int i = 0; i < 28326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28327(int& val) {
    func_in_28327(val);
    const int x = 28327;
    std::cout << 28327;
    for (int i = 0; i < 28327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28327(int& val) {
    const int x = 28327;
    std::cout << 28327;
    for (int i = 0; i < 28327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28328(int& val) {
    func_in_28328(val);
    const int x = 28328;
    std::cout << 28328;
    for (int i = 0; i < 28328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28328(int& val) {
    const int x = 28328;
    std::cout << 28328;
    for (int i = 0; i < 28328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28329(int& val) {
    func_in_28329(val);
    const int x = 28329;
    std::cout << 28329;
    for (int i = 0; i < 28329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28329(int& val) {
    const int x = 28329;
    std::cout << 28329;
    for (int i = 0; i < 28329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28330(int& val) {
    func_in_28330(val);
    const int x = 28330;
    std::cout << 28330;
    for (int i = 0; i < 28330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28330(int& val) {
    const int x = 28330;
    std::cout << 28330;
    for (int i = 0; i < 28330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28331(int& val) {
    func_in_28331(val);
    const int x = 28331;
    std::cout << 28331;
    for (int i = 0; i < 28331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28331(int& val) {
    const int x = 28331;
    std::cout << 28331;
    for (int i = 0; i < 28331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28332(int& val) {
    func_in_28332(val);
    const int x = 28332;
    std::cout << 28332;
    for (int i = 0; i < 28332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28332(int& val) {
    const int x = 28332;
    std::cout << 28332;
    for (int i = 0; i < 28332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28333(int& val) {
    func_in_28333(val);
    const int x = 28333;
    std::cout << 28333;
    for (int i = 0; i < 28333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28333(int& val) {
    const int x = 28333;
    std::cout << 28333;
    for (int i = 0; i < 28333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28334(int& val) {
    func_in_28334(val);
    const int x = 28334;
    std::cout << 28334;
    for (int i = 0; i < 28334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28334(int& val) {
    const int x = 28334;
    std::cout << 28334;
    for (int i = 0; i < 28334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28335(int& val) {
    func_in_28335(val);
    const int x = 28335;
    std::cout << 28335;
    for (int i = 0; i < 28335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28335(int& val) {
    const int x = 28335;
    std::cout << 28335;
    for (int i = 0; i < 28335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28336(int& val) {
    func_in_28336(val);
    const int x = 28336;
    std::cout << 28336;
    for (int i = 0; i < 28336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28336(int& val) {
    const int x = 28336;
    std::cout << 28336;
    for (int i = 0; i < 28336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28337(int& val) {
    func_in_28337(val);
    const int x = 28337;
    std::cout << 28337;
    for (int i = 0; i < 28337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28337(int& val) {
    const int x = 28337;
    std::cout << 28337;
    for (int i = 0; i < 28337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28338(int& val) {
    func_in_28338(val);
    const int x = 28338;
    std::cout << 28338;
    for (int i = 0; i < 28338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28338(int& val) {
    const int x = 28338;
    std::cout << 28338;
    for (int i = 0; i < 28338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28339(int& val) {
    func_in_28339(val);
    const int x = 28339;
    std::cout << 28339;
    for (int i = 0; i < 28339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28339(int& val) {
    const int x = 28339;
    std::cout << 28339;
    for (int i = 0; i < 28339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28340(int& val) {
    func_in_28340(val);
    const int x = 28340;
    std::cout << 28340;
    for (int i = 0; i < 28340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28340(int& val) {
    const int x = 28340;
    std::cout << 28340;
    for (int i = 0; i < 28340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28341(int& val) {
    func_in_28341(val);
    const int x = 28341;
    std::cout << 28341;
    for (int i = 0; i < 28341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28341(int& val) {
    const int x = 28341;
    std::cout << 28341;
    for (int i = 0; i < 28341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28342(int& val) {
    func_in_28342(val);
    const int x = 28342;
    std::cout << 28342;
    for (int i = 0; i < 28342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28342(int& val) {
    const int x = 28342;
    std::cout << 28342;
    for (int i = 0; i < 28342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28343(int& val) {
    func_in_28343(val);
    const int x = 28343;
    std::cout << 28343;
    for (int i = 0; i < 28343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28343(int& val) {
    const int x = 28343;
    std::cout << 28343;
    for (int i = 0; i < 28343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28344(int& val) {
    func_in_28344(val);
    const int x = 28344;
    std::cout << 28344;
    for (int i = 0; i < 28344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28344(int& val) {
    const int x = 28344;
    std::cout << 28344;
    for (int i = 0; i < 28344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28345(int& val) {
    func_in_28345(val);
    const int x = 28345;
    std::cout << 28345;
    for (int i = 0; i < 28345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28345(int& val) {
    const int x = 28345;
    std::cout << 28345;
    for (int i = 0; i < 28345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28346(int& val) {
    func_in_28346(val);
    const int x = 28346;
    std::cout << 28346;
    for (int i = 0; i < 28346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28346(int& val) {
    const int x = 28346;
    std::cout << 28346;
    for (int i = 0; i < 28346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28347(int& val) {
    func_in_28347(val);
    const int x = 28347;
    std::cout << 28347;
    for (int i = 0; i < 28347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28347(int& val) {
    const int x = 28347;
    std::cout << 28347;
    for (int i = 0; i < 28347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28348(int& val) {
    func_in_28348(val);
    const int x = 28348;
    std::cout << 28348;
    for (int i = 0; i < 28348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28348(int& val) {
    const int x = 28348;
    std::cout << 28348;
    for (int i = 0; i < 28348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28349(int& val) {
    func_in_28349(val);
    const int x = 28349;
    std::cout << 28349;
    for (int i = 0; i < 28349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28349(int& val) {
    const int x = 28349;
    std::cout << 28349;
    for (int i = 0; i < 28349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28350(int& val) {
    func_in_28350(val);
    const int x = 28350;
    std::cout << 28350;
    for (int i = 0; i < 28350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28350(int& val) {
    const int x = 28350;
    std::cout << 28350;
    for (int i = 0; i < 28350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28351(int& val) {
    func_in_28351(val);
    const int x = 28351;
    std::cout << 28351;
    for (int i = 0; i < 28351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28351(int& val) {
    const int x = 28351;
    std::cout << 28351;
    for (int i = 0; i < 28351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28352(int& val) {
    func_in_28352(val);
    const int x = 28352;
    std::cout << 28352;
    for (int i = 0; i < 28352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28352(int& val) {
    const int x = 28352;
    std::cout << 28352;
    for (int i = 0; i < 28352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28353(int& val) {
    func_in_28353(val);
    const int x = 28353;
    std::cout << 28353;
    for (int i = 0; i < 28353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28353(int& val) {
    const int x = 28353;
    std::cout << 28353;
    for (int i = 0; i < 28353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28354(int& val) {
    func_in_28354(val);
    const int x = 28354;
    std::cout << 28354;
    for (int i = 0; i < 28354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28354(int& val) {
    const int x = 28354;
    std::cout << 28354;
    for (int i = 0; i < 28354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28355(int& val) {
    func_in_28355(val);
    const int x = 28355;
    std::cout << 28355;
    for (int i = 0; i < 28355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28355(int& val) {
    const int x = 28355;
    std::cout << 28355;
    for (int i = 0; i < 28355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28356(int& val) {
    func_in_28356(val);
    const int x = 28356;
    std::cout << 28356;
    for (int i = 0; i < 28356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28356(int& val) {
    const int x = 28356;
    std::cout << 28356;
    for (int i = 0; i < 28356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28357(int& val) {
    func_in_28357(val);
    const int x = 28357;
    std::cout << 28357;
    for (int i = 0; i < 28357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28357(int& val) {
    const int x = 28357;
    std::cout << 28357;
    for (int i = 0; i < 28357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28358(int& val) {
    func_in_28358(val);
    const int x = 28358;
    std::cout << 28358;
    for (int i = 0; i < 28358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28358(int& val) {
    const int x = 28358;
    std::cout << 28358;
    for (int i = 0; i < 28358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28359(int& val) {
    func_in_28359(val);
    const int x = 28359;
    std::cout << 28359;
    for (int i = 0; i < 28359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28359(int& val) {
    const int x = 28359;
    std::cout << 28359;
    for (int i = 0; i < 28359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28360(int& val) {
    func_in_28360(val);
    const int x = 28360;
    std::cout << 28360;
    for (int i = 0; i < 28360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28360(int& val) {
    const int x = 28360;
    std::cout << 28360;
    for (int i = 0; i < 28360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28361(int& val) {
    func_in_28361(val);
    const int x = 28361;
    std::cout << 28361;
    for (int i = 0; i < 28361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28361(int& val) {
    const int x = 28361;
    std::cout << 28361;
    for (int i = 0; i < 28361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28362(int& val) {
    func_in_28362(val);
    const int x = 28362;
    std::cout << 28362;
    for (int i = 0; i < 28362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28362(int& val) {
    const int x = 28362;
    std::cout << 28362;
    for (int i = 0; i < 28362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28363(int& val) {
    func_in_28363(val);
    const int x = 28363;
    std::cout << 28363;
    for (int i = 0; i < 28363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28363(int& val) {
    const int x = 28363;
    std::cout << 28363;
    for (int i = 0; i < 28363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28364(int& val) {
    func_in_28364(val);
    const int x = 28364;
    std::cout << 28364;
    for (int i = 0; i < 28364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28364(int& val) {
    const int x = 28364;
    std::cout << 28364;
    for (int i = 0; i < 28364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28365(int& val) {
    func_in_28365(val);
    const int x = 28365;
    std::cout << 28365;
    for (int i = 0; i < 28365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28365(int& val) {
    const int x = 28365;
    std::cout << 28365;
    for (int i = 0; i < 28365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28366(int& val) {
    func_in_28366(val);
    const int x = 28366;
    std::cout << 28366;
    for (int i = 0; i < 28366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28366(int& val) {
    const int x = 28366;
    std::cout << 28366;
    for (int i = 0; i < 28366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28367(int& val) {
    func_in_28367(val);
    const int x = 28367;
    std::cout << 28367;
    for (int i = 0; i < 28367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28367(int& val) {
    const int x = 28367;
    std::cout << 28367;
    for (int i = 0; i < 28367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28368(int& val) {
    func_in_28368(val);
    const int x = 28368;
    std::cout << 28368;
    for (int i = 0; i < 28368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28368(int& val) {
    const int x = 28368;
    std::cout << 28368;
    for (int i = 0; i < 28368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28369(int& val) {
    func_in_28369(val);
    const int x = 28369;
    std::cout << 28369;
    for (int i = 0; i < 28369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28369(int& val) {
    const int x = 28369;
    std::cout << 28369;
    for (int i = 0; i < 28369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28370(int& val) {
    func_in_28370(val);
    const int x = 28370;
    std::cout << 28370;
    for (int i = 0; i < 28370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28370(int& val) {
    const int x = 28370;
    std::cout << 28370;
    for (int i = 0; i < 28370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28371(int& val) {
    func_in_28371(val);
    const int x = 28371;
    std::cout << 28371;
    for (int i = 0; i < 28371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28371(int& val) {
    const int x = 28371;
    std::cout << 28371;
    for (int i = 0; i < 28371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28372(int& val) {
    func_in_28372(val);
    const int x = 28372;
    std::cout << 28372;
    for (int i = 0; i < 28372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28372(int& val) {
    const int x = 28372;
    std::cout << 28372;
    for (int i = 0; i < 28372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28373(int& val) {
    func_in_28373(val);
    const int x = 28373;
    std::cout << 28373;
    for (int i = 0; i < 28373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28373(int& val) {
    const int x = 28373;
    std::cout << 28373;
    for (int i = 0; i < 28373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28374(int& val) {
    func_in_28374(val);
    const int x = 28374;
    std::cout << 28374;
    for (int i = 0; i < 28374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28374(int& val) {
    const int x = 28374;
    std::cout << 28374;
    for (int i = 0; i < 28374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28375(int& val) {
    func_in_28375(val);
    const int x = 28375;
    std::cout << 28375;
    for (int i = 0; i < 28375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28375(int& val) {
    const int x = 28375;
    std::cout << 28375;
    for (int i = 0; i < 28375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28376(int& val) {
    func_in_28376(val);
    const int x = 28376;
    std::cout << 28376;
    for (int i = 0; i < 28376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28376(int& val) {
    const int x = 28376;
    std::cout << 28376;
    for (int i = 0; i < 28376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28377(int& val) {
    func_in_28377(val);
    const int x = 28377;
    std::cout << 28377;
    for (int i = 0; i < 28377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28377(int& val) {
    const int x = 28377;
    std::cout << 28377;
    for (int i = 0; i < 28377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28378(int& val) {
    func_in_28378(val);
    const int x = 28378;
    std::cout << 28378;
    for (int i = 0; i < 28378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28378(int& val) {
    const int x = 28378;
    std::cout << 28378;
    for (int i = 0; i < 28378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28379(int& val) {
    func_in_28379(val);
    const int x = 28379;
    std::cout << 28379;
    for (int i = 0; i < 28379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28379(int& val) {
    const int x = 28379;
    std::cout << 28379;
    for (int i = 0; i < 28379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28380(int& val) {
    func_in_28380(val);
    const int x = 28380;
    std::cout << 28380;
    for (int i = 0; i < 28380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28380(int& val) {
    const int x = 28380;
    std::cout << 28380;
    for (int i = 0; i < 28380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28381(int& val) {
    func_in_28381(val);
    const int x = 28381;
    std::cout << 28381;
    for (int i = 0; i < 28381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28381(int& val) {
    const int x = 28381;
    std::cout << 28381;
    for (int i = 0; i < 28381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28382(int& val) {
    func_in_28382(val);
    const int x = 28382;
    std::cout << 28382;
    for (int i = 0; i < 28382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28382(int& val) {
    const int x = 28382;
    std::cout << 28382;
    for (int i = 0; i < 28382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28383(int& val) {
    func_in_28383(val);
    const int x = 28383;
    std::cout << 28383;
    for (int i = 0; i < 28383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28383(int& val) {
    const int x = 28383;
    std::cout << 28383;
    for (int i = 0; i < 28383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28384(int& val) {
    func_in_28384(val);
    const int x = 28384;
    std::cout << 28384;
    for (int i = 0; i < 28384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28384(int& val) {
    const int x = 28384;
    std::cout << 28384;
    for (int i = 0; i < 28384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28385(int& val) {
    func_in_28385(val);
    const int x = 28385;
    std::cout << 28385;
    for (int i = 0; i < 28385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28385(int& val) {
    const int x = 28385;
    std::cout << 28385;
    for (int i = 0; i < 28385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28386(int& val) {
    func_in_28386(val);
    const int x = 28386;
    std::cout << 28386;
    for (int i = 0; i < 28386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28386(int& val) {
    const int x = 28386;
    std::cout << 28386;
    for (int i = 0; i < 28386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28387(int& val) {
    func_in_28387(val);
    const int x = 28387;
    std::cout << 28387;
    for (int i = 0; i < 28387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28387(int& val) {
    const int x = 28387;
    std::cout << 28387;
    for (int i = 0; i < 28387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28388(int& val) {
    func_in_28388(val);
    const int x = 28388;
    std::cout << 28388;
    for (int i = 0; i < 28388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28388(int& val) {
    const int x = 28388;
    std::cout << 28388;
    for (int i = 0; i < 28388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28389(int& val) {
    func_in_28389(val);
    const int x = 28389;
    std::cout << 28389;
    for (int i = 0; i < 28389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28389(int& val) {
    const int x = 28389;
    std::cout << 28389;
    for (int i = 0; i < 28389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28390(int& val) {
    func_in_28390(val);
    const int x = 28390;
    std::cout << 28390;
    for (int i = 0; i < 28390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28390(int& val) {
    const int x = 28390;
    std::cout << 28390;
    for (int i = 0; i < 28390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28391(int& val) {
    func_in_28391(val);
    const int x = 28391;
    std::cout << 28391;
    for (int i = 0; i < 28391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28391(int& val) {
    const int x = 28391;
    std::cout << 28391;
    for (int i = 0; i < 28391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28392(int& val) {
    func_in_28392(val);
    const int x = 28392;
    std::cout << 28392;
    for (int i = 0; i < 28392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28392(int& val) {
    const int x = 28392;
    std::cout << 28392;
    for (int i = 0; i < 28392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28393(int& val) {
    func_in_28393(val);
    const int x = 28393;
    std::cout << 28393;
    for (int i = 0; i < 28393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28393(int& val) {
    const int x = 28393;
    std::cout << 28393;
    for (int i = 0; i < 28393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28394(int& val) {
    func_in_28394(val);
    const int x = 28394;
    std::cout << 28394;
    for (int i = 0; i < 28394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28394(int& val) {
    const int x = 28394;
    std::cout << 28394;
    for (int i = 0; i < 28394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28395(int& val) {
    func_in_28395(val);
    const int x = 28395;
    std::cout << 28395;
    for (int i = 0; i < 28395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28395(int& val) {
    const int x = 28395;
    std::cout << 28395;
    for (int i = 0; i < 28395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28396(int& val) {
    func_in_28396(val);
    const int x = 28396;
    std::cout << 28396;
    for (int i = 0; i < 28396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28396(int& val) {
    const int x = 28396;
    std::cout << 28396;
    for (int i = 0; i < 28396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28397(int& val) {
    func_in_28397(val);
    const int x = 28397;
    std::cout << 28397;
    for (int i = 0; i < 28397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28397(int& val) {
    const int x = 28397;
    std::cout << 28397;
    for (int i = 0; i < 28397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28398(int& val) {
    func_in_28398(val);
    const int x = 28398;
    std::cout << 28398;
    for (int i = 0; i < 28398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28398(int& val) {
    const int x = 28398;
    std::cout << 28398;
    for (int i = 0; i < 28398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28399(int& val) {
    func_in_28399(val);
    const int x = 28399;
    std::cout << 28399;
    for (int i = 0; i < 28399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28399(int& val) {
    const int x = 28399;
    std::cout << 28399;
    for (int i = 0; i < 28399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28400(int& val) {
    func_in_28400(val);
    const int x = 28400;
    std::cout << 28400;
    for (int i = 0; i < 28400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28400(int& val) {
    const int x = 28400;
    std::cout << 28400;
    for (int i = 0; i < 28400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28401(int& val) {
    func_in_28401(val);
    const int x = 28401;
    std::cout << 28401;
    for (int i = 0; i < 28401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28401(int& val) {
    const int x = 28401;
    std::cout << 28401;
    for (int i = 0; i < 28401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28402(int& val) {
    func_in_28402(val);
    const int x = 28402;
    std::cout << 28402;
    for (int i = 0; i < 28402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28402(int& val) {
    const int x = 28402;
    std::cout << 28402;
    for (int i = 0; i < 28402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28403(int& val) {
    func_in_28403(val);
    const int x = 28403;
    std::cout << 28403;
    for (int i = 0; i < 28403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28403(int& val) {
    const int x = 28403;
    std::cout << 28403;
    for (int i = 0; i < 28403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28404(int& val) {
    func_in_28404(val);
    const int x = 28404;
    std::cout << 28404;
    for (int i = 0; i < 28404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28404(int& val) {
    const int x = 28404;
    std::cout << 28404;
    for (int i = 0; i < 28404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28405(int& val) {
    func_in_28405(val);
    const int x = 28405;
    std::cout << 28405;
    for (int i = 0; i < 28405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28405(int& val) {
    const int x = 28405;
    std::cout << 28405;
    for (int i = 0; i < 28405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28406(int& val) {
    func_in_28406(val);
    const int x = 28406;
    std::cout << 28406;
    for (int i = 0; i < 28406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28406(int& val) {
    const int x = 28406;
    std::cout << 28406;
    for (int i = 0; i < 28406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28407(int& val) {
    func_in_28407(val);
    const int x = 28407;
    std::cout << 28407;
    for (int i = 0; i < 28407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28407(int& val) {
    const int x = 28407;
    std::cout << 28407;
    for (int i = 0; i < 28407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28408(int& val) {
    func_in_28408(val);
    const int x = 28408;
    std::cout << 28408;
    for (int i = 0; i < 28408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28408(int& val) {
    const int x = 28408;
    std::cout << 28408;
    for (int i = 0; i < 28408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28409(int& val) {
    func_in_28409(val);
    const int x = 28409;
    std::cout << 28409;
    for (int i = 0; i < 28409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28409(int& val) {
    const int x = 28409;
    std::cout << 28409;
    for (int i = 0; i < 28409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28410(int& val) {
    func_in_28410(val);
    const int x = 28410;
    std::cout << 28410;
    for (int i = 0; i < 28410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28410(int& val) {
    const int x = 28410;
    std::cout << 28410;
    for (int i = 0; i < 28410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28411(int& val) {
    func_in_28411(val);
    const int x = 28411;
    std::cout << 28411;
    for (int i = 0; i < 28411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28411(int& val) {
    const int x = 28411;
    std::cout << 28411;
    for (int i = 0; i < 28411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28412(int& val) {
    func_in_28412(val);
    const int x = 28412;
    std::cout << 28412;
    for (int i = 0; i < 28412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28412(int& val) {
    const int x = 28412;
    std::cout << 28412;
    for (int i = 0; i < 28412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28413(int& val) {
    func_in_28413(val);
    const int x = 28413;
    std::cout << 28413;
    for (int i = 0; i < 28413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28413(int& val) {
    const int x = 28413;
    std::cout << 28413;
    for (int i = 0; i < 28413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28414(int& val) {
    func_in_28414(val);
    const int x = 28414;
    std::cout << 28414;
    for (int i = 0; i < 28414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28414(int& val) {
    const int x = 28414;
    std::cout << 28414;
    for (int i = 0; i < 28414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28415(int& val) {
    func_in_28415(val);
    const int x = 28415;
    std::cout << 28415;
    for (int i = 0; i < 28415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28415(int& val) {
    const int x = 28415;
    std::cout << 28415;
    for (int i = 0; i < 28415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28416(int& val) {
    func_in_28416(val);
    const int x = 28416;
    std::cout << 28416;
    for (int i = 0; i < 28416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28416(int& val) {
    const int x = 28416;
    std::cout << 28416;
    for (int i = 0; i < 28416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28417(int& val) {
    func_in_28417(val);
    const int x = 28417;
    std::cout << 28417;
    for (int i = 0; i < 28417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28417(int& val) {
    const int x = 28417;
    std::cout << 28417;
    for (int i = 0; i < 28417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28418(int& val) {
    func_in_28418(val);
    const int x = 28418;
    std::cout << 28418;
    for (int i = 0; i < 28418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28418(int& val) {
    const int x = 28418;
    std::cout << 28418;
    for (int i = 0; i < 28418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28419(int& val) {
    func_in_28419(val);
    const int x = 28419;
    std::cout << 28419;
    for (int i = 0; i < 28419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28419(int& val) {
    const int x = 28419;
    std::cout << 28419;
    for (int i = 0; i < 28419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28420(int& val) {
    func_in_28420(val);
    const int x = 28420;
    std::cout << 28420;
    for (int i = 0; i < 28420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28420(int& val) {
    const int x = 28420;
    std::cout << 28420;
    for (int i = 0; i < 28420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28421(int& val) {
    func_in_28421(val);
    const int x = 28421;
    std::cout << 28421;
    for (int i = 0; i < 28421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28421(int& val) {
    const int x = 28421;
    std::cout << 28421;
    for (int i = 0; i < 28421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28422(int& val) {
    func_in_28422(val);
    const int x = 28422;
    std::cout << 28422;
    for (int i = 0; i < 28422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28422(int& val) {
    const int x = 28422;
    std::cout << 28422;
    for (int i = 0; i < 28422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28423(int& val) {
    func_in_28423(val);
    const int x = 28423;
    std::cout << 28423;
    for (int i = 0; i < 28423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28423(int& val) {
    const int x = 28423;
    std::cout << 28423;
    for (int i = 0; i < 28423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28424(int& val) {
    func_in_28424(val);
    const int x = 28424;
    std::cout << 28424;
    for (int i = 0; i < 28424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28424(int& val) {
    const int x = 28424;
    std::cout << 28424;
    for (int i = 0; i < 28424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28425(int& val) {
    func_in_28425(val);
    const int x = 28425;
    std::cout << 28425;
    for (int i = 0; i < 28425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28425(int& val) {
    const int x = 28425;
    std::cout << 28425;
    for (int i = 0; i < 28425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28426(int& val) {
    func_in_28426(val);
    const int x = 28426;
    std::cout << 28426;
    for (int i = 0; i < 28426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28426(int& val) {
    const int x = 28426;
    std::cout << 28426;
    for (int i = 0; i < 28426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28427(int& val) {
    func_in_28427(val);
    const int x = 28427;
    std::cout << 28427;
    for (int i = 0; i < 28427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28427(int& val) {
    const int x = 28427;
    std::cout << 28427;
    for (int i = 0; i < 28427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28428(int& val) {
    func_in_28428(val);
    const int x = 28428;
    std::cout << 28428;
    for (int i = 0; i < 28428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28428(int& val) {
    const int x = 28428;
    std::cout << 28428;
    for (int i = 0; i < 28428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28429(int& val) {
    func_in_28429(val);
    const int x = 28429;
    std::cout << 28429;
    for (int i = 0; i < 28429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28429(int& val) {
    const int x = 28429;
    std::cout << 28429;
    for (int i = 0; i < 28429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28430(int& val) {
    func_in_28430(val);
    const int x = 28430;
    std::cout << 28430;
    for (int i = 0; i < 28430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28430(int& val) {
    const int x = 28430;
    std::cout << 28430;
    for (int i = 0; i < 28430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28431(int& val) {
    func_in_28431(val);
    const int x = 28431;
    std::cout << 28431;
    for (int i = 0; i < 28431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28431(int& val) {
    const int x = 28431;
    std::cout << 28431;
    for (int i = 0; i < 28431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28432(int& val) {
    func_in_28432(val);
    const int x = 28432;
    std::cout << 28432;
    for (int i = 0; i < 28432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28432(int& val) {
    const int x = 28432;
    std::cout << 28432;
    for (int i = 0; i < 28432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28433(int& val) {
    func_in_28433(val);
    const int x = 28433;
    std::cout << 28433;
    for (int i = 0; i < 28433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28433(int& val) {
    const int x = 28433;
    std::cout << 28433;
    for (int i = 0; i < 28433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28434(int& val) {
    func_in_28434(val);
    const int x = 28434;
    std::cout << 28434;
    for (int i = 0; i < 28434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28434(int& val) {
    const int x = 28434;
    std::cout << 28434;
    for (int i = 0; i < 28434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28435(int& val) {
    func_in_28435(val);
    const int x = 28435;
    std::cout << 28435;
    for (int i = 0; i < 28435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28435(int& val) {
    const int x = 28435;
    std::cout << 28435;
    for (int i = 0; i < 28435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28436(int& val) {
    func_in_28436(val);
    const int x = 28436;
    std::cout << 28436;
    for (int i = 0; i < 28436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28436(int& val) {
    const int x = 28436;
    std::cout << 28436;
    for (int i = 0; i < 28436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28437(int& val) {
    func_in_28437(val);
    const int x = 28437;
    std::cout << 28437;
    for (int i = 0; i < 28437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28437(int& val) {
    const int x = 28437;
    std::cout << 28437;
    for (int i = 0; i < 28437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28438(int& val) {
    func_in_28438(val);
    const int x = 28438;
    std::cout << 28438;
    for (int i = 0; i < 28438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28438(int& val) {
    const int x = 28438;
    std::cout << 28438;
    for (int i = 0; i < 28438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28439(int& val) {
    func_in_28439(val);
    const int x = 28439;
    std::cout << 28439;
    for (int i = 0; i < 28439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28439(int& val) {
    const int x = 28439;
    std::cout << 28439;
    for (int i = 0; i < 28439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28440(int& val) {
    func_in_28440(val);
    const int x = 28440;
    std::cout << 28440;
    for (int i = 0; i < 28440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28440(int& val) {
    const int x = 28440;
    std::cout << 28440;
    for (int i = 0; i < 28440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28441(int& val) {
    func_in_28441(val);
    const int x = 28441;
    std::cout << 28441;
    for (int i = 0; i < 28441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28441(int& val) {
    const int x = 28441;
    std::cout << 28441;
    for (int i = 0; i < 28441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28442(int& val) {
    func_in_28442(val);
    const int x = 28442;
    std::cout << 28442;
    for (int i = 0; i < 28442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28442(int& val) {
    const int x = 28442;
    std::cout << 28442;
    for (int i = 0; i < 28442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28443(int& val) {
    func_in_28443(val);
    const int x = 28443;
    std::cout << 28443;
    for (int i = 0; i < 28443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28443(int& val) {
    const int x = 28443;
    std::cout << 28443;
    for (int i = 0; i < 28443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28444(int& val) {
    func_in_28444(val);
    const int x = 28444;
    std::cout << 28444;
    for (int i = 0; i < 28444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28444(int& val) {
    const int x = 28444;
    std::cout << 28444;
    for (int i = 0; i < 28444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28445(int& val) {
    func_in_28445(val);
    const int x = 28445;
    std::cout << 28445;
    for (int i = 0; i < 28445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28445(int& val) {
    const int x = 28445;
    std::cout << 28445;
    for (int i = 0; i < 28445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28446(int& val) {
    func_in_28446(val);
    const int x = 28446;
    std::cout << 28446;
    for (int i = 0; i < 28446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28446(int& val) {
    const int x = 28446;
    std::cout << 28446;
    for (int i = 0; i < 28446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28447(int& val) {
    func_in_28447(val);
    const int x = 28447;
    std::cout << 28447;
    for (int i = 0; i < 28447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28447(int& val) {
    const int x = 28447;
    std::cout << 28447;
    for (int i = 0; i < 28447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28448(int& val) {
    func_in_28448(val);
    const int x = 28448;
    std::cout << 28448;
    for (int i = 0; i < 28448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28448(int& val) {
    const int x = 28448;
    std::cout << 28448;
    for (int i = 0; i < 28448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28449(int& val) {
    func_in_28449(val);
    const int x = 28449;
    std::cout << 28449;
    for (int i = 0; i < 28449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28449(int& val) {
    const int x = 28449;
    std::cout << 28449;
    for (int i = 0; i < 28449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28450(int& val) {
    func_in_28450(val);
    const int x = 28450;
    std::cout << 28450;
    for (int i = 0; i < 28450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28450(int& val) {
    const int x = 28450;
    std::cout << 28450;
    for (int i = 0; i < 28450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28451(int& val) {
    func_in_28451(val);
    const int x = 28451;
    std::cout << 28451;
    for (int i = 0; i < 28451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28451(int& val) {
    const int x = 28451;
    std::cout << 28451;
    for (int i = 0; i < 28451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28452(int& val) {
    func_in_28452(val);
    const int x = 28452;
    std::cout << 28452;
    for (int i = 0; i < 28452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28452(int& val) {
    const int x = 28452;
    std::cout << 28452;
    for (int i = 0; i < 28452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28453(int& val) {
    func_in_28453(val);
    const int x = 28453;
    std::cout << 28453;
    for (int i = 0; i < 28453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28453(int& val) {
    const int x = 28453;
    std::cout << 28453;
    for (int i = 0; i < 28453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28454(int& val) {
    func_in_28454(val);
    const int x = 28454;
    std::cout << 28454;
    for (int i = 0; i < 28454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28454(int& val) {
    const int x = 28454;
    std::cout << 28454;
    for (int i = 0; i < 28454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28455(int& val) {
    func_in_28455(val);
    const int x = 28455;
    std::cout << 28455;
    for (int i = 0; i < 28455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28455(int& val) {
    const int x = 28455;
    std::cout << 28455;
    for (int i = 0; i < 28455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28456(int& val) {
    func_in_28456(val);
    const int x = 28456;
    std::cout << 28456;
    for (int i = 0; i < 28456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28456(int& val) {
    const int x = 28456;
    std::cout << 28456;
    for (int i = 0; i < 28456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28457(int& val) {
    func_in_28457(val);
    const int x = 28457;
    std::cout << 28457;
    for (int i = 0; i < 28457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28457(int& val) {
    const int x = 28457;
    std::cout << 28457;
    for (int i = 0; i < 28457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28458(int& val) {
    func_in_28458(val);
    const int x = 28458;
    std::cout << 28458;
    for (int i = 0; i < 28458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28458(int& val) {
    const int x = 28458;
    std::cout << 28458;
    for (int i = 0; i < 28458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28459(int& val) {
    func_in_28459(val);
    const int x = 28459;
    std::cout << 28459;
    for (int i = 0; i < 28459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28459(int& val) {
    const int x = 28459;
    std::cout << 28459;
    for (int i = 0; i < 28459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28460(int& val) {
    func_in_28460(val);
    const int x = 28460;
    std::cout << 28460;
    for (int i = 0; i < 28460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28460(int& val) {
    const int x = 28460;
    std::cout << 28460;
    for (int i = 0; i < 28460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28461(int& val) {
    func_in_28461(val);
    const int x = 28461;
    std::cout << 28461;
    for (int i = 0; i < 28461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28461(int& val) {
    const int x = 28461;
    std::cout << 28461;
    for (int i = 0; i < 28461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28462(int& val) {
    func_in_28462(val);
    const int x = 28462;
    std::cout << 28462;
    for (int i = 0; i < 28462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28462(int& val) {
    const int x = 28462;
    std::cout << 28462;
    for (int i = 0; i < 28462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28463(int& val) {
    func_in_28463(val);
    const int x = 28463;
    std::cout << 28463;
    for (int i = 0; i < 28463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28463(int& val) {
    const int x = 28463;
    std::cout << 28463;
    for (int i = 0; i < 28463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28464(int& val) {
    func_in_28464(val);
    const int x = 28464;
    std::cout << 28464;
    for (int i = 0; i < 28464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28464(int& val) {
    const int x = 28464;
    std::cout << 28464;
    for (int i = 0; i < 28464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28465(int& val) {
    func_in_28465(val);
    const int x = 28465;
    std::cout << 28465;
    for (int i = 0; i < 28465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28465(int& val) {
    const int x = 28465;
    std::cout << 28465;
    for (int i = 0; i < 28465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28466(int& val) {
    func_in_28466(val);
    const int x = 28466;
    std::cout << 28466;
    for (int i = 0; i < 28466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28466(int& val) {
    const int x = 28466;
    std::cout << 28466;
    for (int i = 0; i < 28466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28467(int& val) {
    func_in_28467(val);
    const int x = 28467;
    std::cout << 28467;
    for (int i = 0; i < 28467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28467(int& val) {
    const int x = 28467;
    std::cout << 28467;
    for (int i = 0; i < 28467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28468(int& val) {
    func_in_28468(val);
    const int x = 28468;
    std::cout << 28468;
    for (int i = 0; i < 28468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28468(int& val) {
    const int x = 28468;
    std::cout << 28468;
    for (int i = 0; i < 28468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28469(int& val) {
    func_in_28469(val);
    const int x = 28469;
    std::cout << 28469;
    for (int i = 0; i < 28469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28469(int& val) {
    const int x = 28469;
    std::cout << 28469;
    for (int i = 0; i < 28469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28470(int& val) {
    func_in_28470(val);
    const int x = 28470;
    std::cout << 28470;
    for (int i = 0; i < 28470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28470(int& val) {
    const int x = 28470;
    std::cout << 28470;
    for (int i = 0; i < 28470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28471(int& val) {
    func_in_28471(val);
    const int x = 28471;
    std::cout << 28471;
    for (int i = 0; i < 28471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28471(int& val) {
    const int x = 28471;
    std::cout << 28471;
    for (int i = 0; i < 28471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28472(int& val) {
    func_in_28472(val);
    const int x = 28472;
    std::cout << 28472;
    for (int i = 0; i < 28472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28472(int& val) {
    const int x = 28472;
    std::cout << 28472;
    for (int i = 0; i < 28472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28473(int& val) {
    func_in_28473(val);
    const int x = 28473;
    std::cout << 28473;
    for (int i = 0; i < 28473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28473(int& val) {
    const int x = 28473;
    std::cout << 28473;
    for (int i = 0; i < 28473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28474(int& val) {
    func_in_28474(val);
    const int x = 28474;
    std::cout << 28474;
    for (int i = 0; i < 28474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28474(int& val) {
    const int x = 28474;
    std::cout << 28474;
    for (int i = 0; i < 28474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28475(int& val) {
    func_in_28475(val);
    const int x = 28475;
    std::cout << 28475;
    for (int i = 0; i < 28475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28475(int& val) {
    const int x = 28475;
    std::cout << 28475;
    for (int i = 0; i < 28475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28476(int& val) {
    func_in_28476(val);
    const int x = 28476;
    std::cout << 28476;
    for (int i = 0; i < 28476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28476(int& val) {
    const int x = 28476;
    std::cout << 28476;
    for (int i = 0; i < 28476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28477(int& val) {
    func_in_28477(val);
    const int x = 28477;
    std::cout << 28477;
    for (int i = 0; i < 28477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28477(int& val) {
    const int x = 28477;
    std::cout << 28477;
    for (int i = 0; i < 28477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28478(int& val) {
    func_in_28478(val);
    const int x = 28478;
    std::cout << 28478;
    for (int i = 0; i < 28478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28478(int& val) {
    const int x = 28478;
    std::cout << 28478;
    for (int i = 0; i < 28478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28479(int& val) {
    func_in_28479(val);
    const int x = 28479;
    std::cout << 28479;
    for (int i = 0; i < 28479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28479(int& val) {
    const int x = 28479;
    std::cout << 28479;
    for (int i = 0; i < 28479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28480(int& val) {
    func_in_28480(val);
    const int x = 28480;
    std::cout << 28480;
    for (int i = 0; i < 28480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28480(int& val) {
    const int x = 28480;
    std::cout << 28480;
    for (int i = 0; i < 28480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28481(int& val) {
    func_in_28481(val);
    const int x = 28481;
    std::cout << 28481;
    for (int i = 0; i < 28481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28481(int& val) {
    const int x = 28481;
    std::cout << 28481;
    for (int i = 0; i < 28481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28482(int& val) {
    func_in_28482(val);
    const int x = 28482;
    std::cout << 28482;
    for (int i = 0; i < 28482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28482(int& val) {
    const int x = 28482;
    std::cout << 28482;
    for (int i = 0; i < 28482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28483(int& val) {
    func_in_28483(val);
    const int x = 28483;
    std::cout << 28483;
    for (int i = 0; i < 28483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28483(int& val) {
    const int x = 28483;
    std::cout << 28483;
    for (int i = 0; i < 28483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28484(int& val) {
    func_in_28484(val);
    const int x = 28484;
    std::cout << 28484;
    for (int i = 0; i < 28484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28484(int& val) {
    const int x = 28484;
    std::cout << 28484;
    for (int i = 0; i < 28484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28485(int& val) {
    func_in_28485(val);
    const int x = 28485;
    std::cout << 28485;
    for (int i = 0; i < 28485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28485(int& val) {
    const int x = 28485;
    std::cout << 28485;
    for (int i = 0; i < 28485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28486(int& val) {
    func_in_28486(val);
    const int x = 28486;
    std::cout << 28486;
    for (int i = 0; i < 28486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28486(int& val) {
    const int x = 28486;
    std::cout << 28486;
    for (int i = 0; i < 28486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28487(int& val) {
    func_in_28487(val);
    const int x = 28487;
    std::cout << 28487;
    for (int i = 0; i < 28487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28487(int& val) {
    const int x = 28487;
    std::cout << 28487;
    for (int i = 0; i < 28487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28488(int& val) {
    func_in_28488(val);
    const int x = 28488;
    std::cout << 28488;
    for (int i = 0; i < 28488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28488(int& val) {
    const int x = 28488;
    std::cout << 28488;
    for (int i = 0; i < 28488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28489(int& val) {
    func_in_28489(val);
    const int x = 28489;
    std::cout << 28489;
    for (int i = 0; i < 28489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28489(int& val) {
    const int x = 28489;
    std::cout << 28489;
    for (int i = 0; i < 28489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28490(int& val) {
    func_in_28490(val);
    const int x = 28490;
    std::cout << 28490;
    for (int i = 0; i < 28490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28490(int& val) {
    const int x = 28490;
    std::cout << 28490;
    for (int i = 0; i < 28490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28491(int& val) {
    func_in_28491(val);
    const int x = 28491;
    std::cout << 28491;
    for (int i = 0; i < 28491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28491(int& val) {
    const int x = 28491;
    std::cout << 28491;
    for (int i = 0; i < 28491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28492(int& val) {
    func_in_28492(val);
    const int x = 28492;
    std::cout << 28492;
    for (int i = 0; i < 28492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28492(int& val) {
    const int x = 28492;
    std::cout << 28492;
    for (int i = 0; i < 28492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28493(int& val) {
    func_in_28493(val);
    const int x = 28493;
    std::cout << 28493;
    for (int i = 0; i < 28493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28493(int& val) {
    const int x = 28493;
    std::cout << 28493;
    for (int i = 0; i < 28493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28494(int& val) {
    func_in_28494(val);
    const int x = 28494;
    std::cout << 28494;
    for (int i = 0; i < 28494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28494(int& val) {
    const int x = 28494;
    std::cout << 28494;
    for (int i = 0; i < 28494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28495(int& val) {
    func_in_28495(val);
    const int x = 28495;
    std::cout << 28495;
    for (int i = 0; i < 28495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28495(int& val) {
    const int x = 28495;
    std::cout << 28495;
    for (int i = 0; i < 28495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28496(int& val) {
    func_in_28496(val);
    const int x = 28496;
    std::cout << 28496;
    for (int i = 0; i < 28496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28496(int& val) {
    const int x = 28496;
    std::cout << 28496;
    for (int i = 0; i < 28496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28497(int& val) {
    func_in_28497(val);
    const int x = 28497;
    std::cout << 28497;
    for (int i = 0; i < 28497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28497(int& val) {
    const int x = 28497;
    std::cout << 28497;
    for (int i = 0; i < 28497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28498(int& val) {
    func_in_28498(val);
    const int x = 28498;
    std::cout << 28498;
    for (int i = 0; i < 28498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28498(int& val) {
    const int x = 28498;
    std::cout << 28498;
    for (int i = 0; i < 28498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28499(int& val) {
    func_in_28499(val);
    const int x = 28499;
    std::cout << 28499;
    for (int i = 0; i < 28499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28499(int& val) {
    const int x = 28499;
    std::cout << 28499;
    for (int i = 0; i < 28499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28500(int& val) {
    func_in_28500(val);
    const int x = 28500;
    std::cout << 28500;
    for (int i = 0; i < 28500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28500(int& val) {
    const int x = 28500;
    std::cout << 28500;
    for (int i = 0; i < 28500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28501(int& val) {
    func_in_28501(val);
    const int x = 28501;
    std::cout << 28501;
    for (int i = 0; i < 28501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28501(int& val) {
    const int x = 28501;
    std::cout << 28501;
    for (int i = 0; i < 28501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28502(int& val) {
    func_in_28502(val);
    const int x = 28502;
    std::cout << 28502;
    for (int i = 0; i < 28502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28502(int& val) {
    const int x = 28502;
    std::cout << 28502;
    for (int i = 0; i < 28502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28503(int& val) {
    func_in_28503(val);
    const int x = 28503;
    std::cout << 28503;
    for (int i = 0; i < 28503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28503(int& val) {
    const int x = 28503;
    std::cout << 28503;
    for (int i = 0; i < 28503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28504(int& val) {
    func_in_28504(val);
    const int x = 28504;
    std::cout << 28504;
    for (int i = 0; i < 28504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28504(int& val) {
    const int x = 28504;
    std::cout << 28504;
    for (int i = 0; i < 28504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28505(int& val) {
    func_in_28505(val);
    const int x = 28505;
    std::cout << 28505;
    for (int i = 0; i < 28505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28505(int& val) {
    const int x = 28505;
    std::cout << 28505;
    for (int i = 0; i < 28505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28506(int& val) {
    func_in_28506(val);
    const int x = 28506;
    std::cout << 28506;
    for (int i = 0; i < 28506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28506(int& val) {
    const int x = 28506;
    std::cout << 28506;
    for (int i = 0; i < 28506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28507(int& val) {
    func_in_28507(val);
    const int x = 28507;
    std::cout << 28507;
    for (int i = 0; i < 28507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28507(int& val) {
    const int x = 28507;
    std::cout << 28507;
    for (int i = 0; i < 28507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28508(int& val) {
    func_in_28508(val);
    const int x = 28508;
    std::cout << 28508;
    for (int i = 0; i < 28508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28508(int& val) {
    const int x = 28508;
    std::cout << 28508;
    for (int i = 0; i < 28508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28509(int& val) {
    func_in_28509(val);
    const int x = 28509;
    std::cout << 28509;
    for (int i = 0; i < 28509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28509(int& val) {
    const int x = 28509;
    std::cout << 28509;
    for (int i = 0; i < 28509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28510(int& val) {
    func_in_28510(val);
    const int x = 28510;
    std::cout << 28510;
    for (int i = 0; i < 28510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28510(int& val) {
    const int x = 28510;
    std::cout << 28510;
    for (int i = 0; i < 28510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28511(int& val) {
    func_in_28511(val);
    const int x = 28511;
    std::cout << 28511;
    for (int i = 0; i < 28511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28511(int& val) {
    const int x = 28511;
    std::cout << 28511;
    for (int i = 0; i < 28511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28512(int& val) {
    func_in_28512(val);
    const int x = 28512;
    std::cout << 28512;
    for (int i = 0; i < 28512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28512(int& val) {
    const int x = 28512;
    std::cout << 28512;
    for (int i = 0; i < 28512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28513(int& val) {
    func_in_28513(val);
    const int x = 28513;
    std::cout << 28513;
    for (int i = 0; i < 28513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28513(int& val) {
    const int x = 28513;
    std::cout << 28513;
    for (int i = 0; i < 28513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28514(int& val) {
    func_in_28514(val);
    const int x = 28514;
    std::cout << 28514;
    for (int i = 0; i < 28514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28514(int& val) {
    const int x = 28514;
    std::cout << 28514;
    for (int i = 0; i < 28514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28515(int& val) {
    func_in_28515(val);
    const int x = 28515;
    std::cout << 28515;
    for (int i = 0; i < 28515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28515(int& val) {
    const int x = 28515;
    std::cout << 28515;
    for (int i = 0; i < 28515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28516(int& val) {
    func_in_28516(val);
    const int x = 28516;
    std::cout << 28516;
    for (int i = 0; i < 28516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28516(int& val) {
    const int x = 28516;
    std::cout << 28516;
    for (int i = 0; i < 28516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28517(int& val) {
    func_in_28517(val);
    const int x = 28517;
    std::cout << 28517;
    for (int i = 0; i < 28517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28517(int& val) {
    const int x = 28517;
    std::cout << 28517;
    for (int i = 0; i < 28517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28518(int& val) {
    func_in_28518(val);
    const int x = 28518;
    std::cout << 28518;
    for (int i = 0; i < 28518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28518(int& val) {
    const int x = 28518;
    std::cout << 28518;
    for (int i = 0; i < 28518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28519(int& val) {
    func_in_28519(val);
    const int x = 28519;
    std::cout << 28519;
    for (int i = 0; i < 28519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28519(int& val) {
    const int x = 28519;
    std::cout << 28519;
    for (int i = 0; i < 28519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28520(int& val) {
    func_in_28520(val);
    const int x = 28520;
    std::cout << 28520;
    for (int i = 0; i < 28520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28520(int& val) {
    const int x = 28520;
    std::cout << 28520;
    for (int i = 0; i < 28520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28521(int& val) {
    func_in_28521(val);
    const int x = 28521;
    std::cout << 28521;
    for (int i = 0; i < 28521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28521(int& val) {
    const int x = 28521;
    std::cout << 28521;
    for (int i = 0; i < 28521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28522(int& val) {
    func_in_28522(val);
    const int x = 28522;
    std::cout << 28522;
    for (int i = 0; i < 28522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28522(int& val) {
    const int x = 28522;
    std::cout << 28522;
    for (int i = 0; i < 28522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28523(int& val) {
    func_in_28523(val);
    const int x = 28523;
    std::cout << 28523;
    for (int i = 0; i < 28523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28523(int& val) {
    const int x = 28523;
    std::cout << 28523;
    for (int i = 0; i < 28523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28524(int& val) {
    func_in_28524(val);
    const int x = 28524;
    std::cout << 28524;
    for (int i = 0; i < 28524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28524(int& val) {
    const int x = 28524;
    std::cout << 28524;
    for (int i = 0; i < 28524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28525(int& val) {
    func_in_28525(val);
    const int x = 28525;
    std::cout << 28525;
    for (int i = 0; i < 28525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28525(int& val) {
    const int x = 28525;
    std::cout << 28525;
    for (int i = 0; i < 28525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28526(int& val) {
    func_in_28526(val);
    const int x = 28526;
    std::cout << 28526;
    for (int i = 0; i < 28526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28526(int& val) {
    const int x = 28526;
    std::cout << 28526;
    for (int i = 0; i < 28526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28527(int& val) {
    func_in_28527(val);
    const int x = 28527;
    std::cout << 28527;
    for (int i = 0; i < 28527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28527(int& val) {
    const int x = 28527;
    std::cout << 28527;
    for (int i = 0; i < 28527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28528(int& val) {
    func_in_28528(val);
    const int x = 28528;
    std::cout << 28528;
    for (int i = 0; i < 28528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28528(int& val) {
    const int x = 28528;
    std::cout << 28528;
    for (int i = 0; i < 28528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28529(int& val) {
    func_in_28529(val);
    const int x = 28529;
    std::cout << 28529;
    for (int i = 0; i < 28529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28529(int& val) {
    const int x = 28529;
    std::cout << 28529;
    for (int i = 0; i < 28529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28530(int& val) {
    func_in_28530(val);
    const int x = 28530;
    std::cout << 28530;
    for (int i = 0; i < 28530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28530(int& val) {
    const int x = 28530;
    std::cout << 28530;
    for (int i = 0; i < 28530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28531(int& val) {
    func_in_28531(val);
    const int x = 28531;
    std::cout << 28531;
    for (int i = 0; i < 28531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28531(int& val) {
    const int x = 28531;
    std::cout << 28531;
    for (int i = 0; i < 28531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28532(int& val) {
    func_in_28532(val);
    const int x = 28532;
    std::cout << 28532;
    for (int i = 0; i < 28532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28532(int& val) {
    const int x = 28532;
    std::cout << 28532;
    for (int i = 0; i < 28532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28533(int& val) {
    func_in_28533(val);
    const int x = 28533;
    std::cout << 28533;
    for (int i = 0; i < 28533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28533(int& val) {
    const int x = 28533;
    std::cout << 28533;
    for (int i = 0; i < 28533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28534(int& val) {
    func_in_28534(val);
    const int x = 28534;
    std::cout << 28534;
    for (int i = 0; i < 28534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28534(int& val) {
    const int x = 28534;
    std::cout << 28534;
    for (int i = 0; i < 28534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28535(int& val) {
    func_in_28535(val);
    const int x = 28535;
    std::cout << 28535;
    for (int i = 0; i < 28535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28535(int& val) {
    const int x = 28535;
    std::cout << 28535;
    for (int i = 0; i < 28535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28536(int& val) {
    func_in_28536(val);
    const int x = 28536;
    std::cout << 28536;
    for (int i = 0; i < 28536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28536(int& val) {
    const int x = 28536;
    std::cout << 28536;
    for (int i = 0; i < 28536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28537(int& val) {
    func_in_28537(val);
    const int x = 28537;
    std::cout << 28537;
    for (int i = 0; i < 28537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28537(int& val) {
    const int x = 28537;
    std::cout << 28537;
    for (int i = 0; i < 28537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28538(int& val) {
    func_in_28538(val);
    const int x = 28538;
    std::cout << 28538;
    for (int i = 0; i < 28538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28538(int& val) {
    const int x = 28538;
    std::cout << 28538;
    for (int i = 0; i < 28538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28539(int& val) {
    func_in_28539(val);
    const int x = 28539;
    std::cout << 28539;
    for (int i = 0; i < 28539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28539(int& val) {
    const int x = 28539;
    std::cout << 28539;
    for (int i = 0; i < 28539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28540(int& val) {
    func_in_28540(val);
    const int x = 28540;
    std::cout << 28540;
    for (int i = 0; i < 28540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28540(int& val) {
    const int x = 28540;
    std::cout << 28540;
    for (int i = 0; i < 28540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28541(int& val) {
    func_in_28541(val);
    const int x = 28541;
    std::cout << 28541;
    for (int i = 0; i < 28541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28541(int& val) {
    const int x = 28541;
    std::cout << 28541;
    for (int i = 0; i < 28541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28542(int& val) {
    func_in_28542(val);
    const int x = 28542;
    std::cout << 28542;
    for (int i = 0; i < 28542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28542(int& val) {
    const int x = 28542;
    std::cout << 28542;
    for (int i = 0; i < 28542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28543(int& val) {
    func_in_28543(val);
    const int x = 28543;
    std::cout << 28543;
    for (int i = 0; i < 28543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28543(int& val) {
    const int x = 28543;
    std::cout << 28543;
    for (int i = 0; i < 28543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28544(int& val) {
    func_in_28544(val);
    const int x = 28544;
    std::cout << 28544;
    for (int i = 0; i < 28544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28544(int& val) {
    const int x = 28544;
    std::cout << 28544;
    for (int i = 0; i < 28544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28545(int& val) {
    func_in_28545(val);
    const int x = 28545;
    std::cout << 28545;
    for (int i = 0; i < 28545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28545(int& val) {
    const int x = 28545;
    std::cout << 28545;
    for (int i = 0; i < 28545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28546(int& val) {
    func_in_28546(val);
    const int x = 28546;
    std::cout << 28546;
    for (int i = 0; i < 28546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28546(int& val) {
    const int x = 28546;
    std::cout << 28546;
    for (int i = 0; i < 28546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28547(int& val) {
    func_in_28547(val);
    const int x = 28547;
    std::cout << 28547;
    for (int i = 0; i < 28547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28547(int& val) {
    const int x = 28547;
    std::cout << 28547;
    for (int i = 0; i < 28547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28548(int& val) {
    func_in_28548(val);
    const int x = 28548;
    std::cout << 28548;
    for (int i = 0; i < 28548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28548(int& val) {
    const int x = 28548;
    std::cout << 28548;
    for (int i = 0; i < 28548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28549(int& val) {
    func_in_28549(val);
    const int x = 28549;
    std::cout << 28549;
    for (int i = 0; i < 28549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28549(int& val) {
    const int x = 28549;
    std::cout << 28549;
    for (int i = 0; i < 28549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28550(int& val) {
    func_in_28550(val);
    const int x = 28550;
    std::cout << 28550;
    for (int i = 0; i < 28550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28550(int& val) {
    const int x = 28550;
    std::cout << 28550;
    for (int i = 0; i < 28550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28551(int& val) {
    func_in_28551(val);
    const int x = 28551;
    std::cout << 28551;
    for (int i = 0; i < 28551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28551(int& val) {
    const int x = 28551;
    std::cout << 28551;
    for (int i = 0; i < 28551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28552(int& val) {
    func_in_28552(val);
    const int x = 28552;
    std::cout << 28552;
    for (int i = 0; i < 28552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28552(int& val) {
    const int x = 28552;
    std::cout << 28552;
    for (int i = 0; i < 28552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28553(int& val) {
    func_in_28553(val);
    const int x = 28553;
    std::cout << 28553;
    for (int i = 0; i < 28553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28553(int& val) {
    const int x = 28553;
    std::cout << 28553;
    for (int i = 0; i < 28553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28554(int& val) {
    func_in_28554(val);
    const int x = 28554;
    std::cout << 28554;
    for (int i = 0; i < 28554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28554(int& val) {
    const int x = 28554;
    std::cout << 28554;
    for (int i = 0; i < 28554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28555(int& val) {
    func_in_28555(val);
    const int x = 28555;
    std::cout << 28555;
    for (int i = 0; i < 28555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28555(int& val) {
    const int x = 28555;
    std::cout << 28555;
    for (int i = 0; i < 28555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28556(int& val) {
    func_in_28556(val);
    const int x = 28556;
    std::cout << 28556;
    for (int i = 0; i < 28556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28556(int& val) {
    const int x = 28556;
    std::cout << 28556;
    for (int i = 0; i < 28556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28557(int& val) {
    func_in_28557(val);
    const int x = 28557;
    std::cout << 28557;
    for (int i = 0; i < 28557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28557(int& val) {
    const int x = 28557;
    std::cout << 28557;
    for (int i = 0; i < 28557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28558(int& val) {
    func_in_28558(val);
    const int x = 28558;
    std::cout << 28558;
    for (int i = 0; i < 28558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28558(int& val) {
    const int x = 28558;
    std::cout << 28558;
    for (int i = 0; i < 28558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28559(int& val) {
    func_in_28559(val);
    const int x = 28559;
    std::cout << 28559;
    for (int i = 0; i < 28559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28559(int& val) {
    const int x = 28559;
    std::cout << 28559;
    for (int i = 0; i < 28559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28560(int& val) {
    func_in_28560(val);
    const int x = 28560;
    std::cout << 28560;
    for (int i = 0; i < 28560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28560(int& val) {
    const int x = 28560;
    std::cout << 28560;
    for (int i = 0; i < 28560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28561(int& val) {
    func_in_28561(val);
    const int x = 28561;
    std::cout << 28561;
    for (int i = 0; i < 28561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28561(int& val) {
    const int x = 28561;
    std::cout << 28561;
    for (int i = 0; i < 28561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28562(int& val) {
    func_in_28562(val);
    const int x = 28562;
    std::cout << 28562;
    for (int i = 0; i < 28562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28562(int& val) {
    const int x = 28562;
    std::cout << 28562;
    for (int i = 0; i < 28562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28563(int& val) {
    func_in_28563(val);
    const int x = 28563;
    std::cout << 28563;
    for (int i = 0; i < 28563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28563(int& val) {
    const int x = 28563;
    std::cout << 28563;
    for (int i = 0; i < 28563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28564(int& val) {
    func_in_28564(val);
    const int x = 28564;
    std::cout << 28564;
    for (int i = 0; i < 28564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28564(int& val) {
    const int x = 28564;
    std::cout << 28564;
    for (int i = 0; i < 28564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28565(int& val) {
    func_in_28565(val);
    const int x = 28565;
    std::cout << 28565;
    for (int i = 0; i < 28565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28565(int& val) {
    const int x = 28565;
    std::cout << 28565;
    for (int i = 0; i < 28565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28566(int& val) {
    func_in_28566(val);
    const int x = 28566;
    std::cout << 28566;
    for (int i = 0; i < 28566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28566(int& val) {
    const int x = 28566;
    std::cout << 28566;
    for (int i = 0; i < 28566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28567(int& val) {
    func_in_28567(val);
    const int x = 28567;
    std::cout << 28567;
    for (int i = 0; i < 28567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28567(int& val) {
    const int x = 28567;
    std::cout << 28567;
    for (int i = 0; i < 28567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28568(int& val) {
    func_in_28568(val);
    const int x = 28568;
    std::cout << 28568;
    for (int i = 0; i < 28568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28568(int& val) {
    const int x = 28568;
    std::cout << 28568;
    for (int i = 0; i < 28568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28569(int& val) {
    func_in_28569(val);
    const int x = 28569;
    std::cout << 28569;
    for (int i = 0; i < 28569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28569(int& val) {
    const int x = 28569;
    std::cout << 28569;
    for (int i = 0; i < 28569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28570(int& val) {
    func_in_28570(val);
    const int x = 28570;
    std::cout << 28570;
    for (int i = 0; i < 28570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28570(int& val) {
    const int x = 28570;
    std::cout << 28570;
    for (int i = 0; i < 28570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28571(int& val) {
    func_in_28571(val);
    const int x = 28571;
    std::cout << 28571;
    for (int i = 0; i < 28571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28571(int& val) {
    const int x = 28571;
    std::cout << 28571;
    for (int i = 0; i < 28571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28572(int& val) {
    func_in_28572(val);
    const int x = 28572;
    std::cout << 28572;
    for (int i = 0; i < 28572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28572(int& val) {
    const int x = 28572;
    std::cout << 28572;
    for (int i = 0; i < 28572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28573(int& val) {
    func_in_28573(val);
    const int x = 28573;
    std::cout << 28573;
    for (int i = 0; i < 28573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28573(int& val) {
    const int x = 28573;
    std::cout << 28573;
    for (int i = 0; i < 28573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28574(int& val) {
    func_in_28574(val);
    const int x = 28574;
    std::cout << 28574;
    for (int i = 0; i < 28574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28574(int& val) {
    const int x = 28574;
    std::cout << 28574;
    for (int i = 0; i < 28574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28575(int& val) {
    func_in_28575(val);
    const int x = 28575;
    std::cout << 28575;
    for (int i = 0; i < 28575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28575(int& val) {
    const int x = 28575;
    std::cout << 28575;
    for (int i = 0; i < 28575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28576(int& val) {
    func_in_28576(val);
    const int x = 28576;
    std::cout << 28576;
    for (int i = 0; i < 28576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28576(int& val) {
    const int x = 28576;
    std::cout << 28576;
    for (int i = 0; i < 28576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28577(int& val) {
    func_in_28577(val);
    const int x = 28577;
    std::cout << 28577;
    for (int i = 0; i < 28577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28577(int& val) {
    const int x = 28577;
    std::cout << 28577;
    for (int i = 0; i < 28577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28578(int& val) {
    func_in_28578(val);
    const int x = 28578;
    std::cout << 28578;
    for (int i = 0; i < 28578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28578(int& val) {
    const int x = 28578;
    std::cout << 28578;
    for (int i = 0; i < 28578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28579(int& val) {
    func_in_28579(val);
    const int x = 28579;
    std::cout << 28579;
    for (int i = 0; i < 28579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28579(int& val) {
    const int x = 28579;
    std::cout << 28579;
    for (int i = 0; i < 28579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28580(int& val) {
    func_in_28580(val);
    const int x = 28580;
    std::cout << 28580;
    for (int i = 0; i < 28580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28580(int& val) {
    const int x = 28580;
    std::cout << 28580;
    for (int i = 0; i < 28580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28581(int& val) {
    func_in_28581(val);
    const int x = 28581;
    std::cout << 28581;
    for (int i = 0; i < 28581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28581(int& val) {
    const int x = 28581;
    std::cout << 28581;
    for (int i = 0; i < 28581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28582(int& val) {
    func_in_28582(val);
    const int x = 28582;
    std::cout << 28582;
    for (int i = 0; i < 28582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28582(int& val) {
    const int x = 28582;
    std::cout << 28582;
    for (int i = 0; i < 28582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28583(int& val) {
    func_in_28583(val);
    const int x = 28583;
    std::cout << 28583;
    for (int i = 0; i < 28583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28583(int& val) {
    const int x = 28583;
    std::cout << 28583;
    for (int i = 0; i < 28583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28584(int& val) {
    func_in_28584(val);
    const int x = 28584;
    std::cout << 28584;
    for (int i = 0; i < 28584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28584(int& val) {
    const int x = 28584;
    std::cout << 28584;
    for (int i = 0; i < 28584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28585(int& val) {
    func_in_28585(val);
    const int x = 28585;
    std::cout << 28585;
    for (int i = 0; i < 28585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28585(int& val) {
    const int x = 28585;
    std::cout << 28585;
    for (int i = 0; i < 28585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28586(int& val) {
    func_in_28586(val);
    const int x = 28586;
    std::cout << 28586;
    for (int i = 0; i < 28586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28586(int& val) {
    const int x = 28586;
    std::cout << 28586;
    for (int i = 0; i < 28586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28587(int& val) {
    func_in_28587(val);
    const int x = 28587;
    std::cout << 28587;
    for (int i = 0; i < 28587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28587(int& val) {
    const int x = 28587;
    std::cout << 28587;
    for (int i = 0; i < 28587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28588(int& val) {
    func_in_28588(val);
    const int x = 28588;
    std::cout << 28588;
    for (int i = 0; i < 28588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28588(int& val) {
    const int x = 28588;
    std::cout << 28588;
    for (int i = 0; i < 28588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28589(int& val) {
    func_in_28589(val);
    const int x = 28589;
    std::cout << 28589;
    for (int i = 0; i < 28589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28589(int& val) {
    const int x = 28589;
    std::cout << 28589;
    for (int i = 0; i < 28589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28590(int& val) {
    func_in_28590(val);
    const int x = 28590;
    std::cout << 28590;
    for (int i = 0; i < 28590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28590(int& val) {
    const int x = 28590;
    std::cout << 28590;
    for (int i = 0; i < 28590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28591(int& val) {
    func_in_28591(val);
    const int x = 28591;
    std::cout << 28591;
    for (int i = 0; i < 28591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28591(int& val) {
    const int x = 28591;
    std::cout << 28591;
    for (int i = 0; i < 28591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28592(int& val) {
    func_in_28592(val);
    const int x = 28592;
    std::cout << 28592;
    for (int i = 0; i < 28592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28592(int& val) {
    const int x = 28592;
    std::cout << 28592;
    for (int i = 0; i < 28592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28593(int& val) {
    func_in_28593(val);
    const int x = 28593;
    std::cout << 28593;
    for (int i = 0; i < 28593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28593(int& val) {
    const int x = 28593;
    std::cout << 28593;
    for (int i = 0; i < 28593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28594(int& val) {
    func_in_28594(val);
    const int x = 28594;
    std::cout << 28594;
    for (int i = 0; i < 28594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28594(int& val) {
    const int x = 28594;
    std::cout << 28594;
    for (int i = 0; i < 28594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28595(int& val) {
    func_in_28595(val);
    const int x = 28595;
    std::cout << 28595;
    for (int i = 0; i < 28595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28595(int& val) {
    const int x = 28595;
    std::cout << 28595;
    for (int i = 0; i < 28595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28596(int& val) {
    func_in_28596(val);
    const int x = 28596;
    std::cout << 28596;
    for (int i = 0; i < 28596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28596(int& val) {
    const int x = 28596;
    std::cout << 28596;
    for (int i = 0; i < 28596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28597(int& val) {
    func_in_28597(val);
    const int x = 28597;
    std::cout << 28597;
    for (int i = 0; i < 28597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28597(int& val) {
    const int x = 28597;
    std::cout << 28597;
    for (int i = 0; i < 28597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28598(int& val) {
    func_in_28598(val);
    const int x = 28598;
    std::cout << 28598;
    for (int i = 0; i < 28598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28598(int& val) {
    const int x = 28598;
    std::cout << 28598;
    for (int i = 0; i < 28598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28599(int& val) {
    func_in_28599(val);
    const int x = 28599;
    std::cout << 28599;
    for (int i = 0; i < 28599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28599(int& val) {
    const int x = 28599;
    std::cout << 28599;
    for (int i = 0; i < 28599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28600(int& val) {
    func_in_28600(val);
    const int x = 28600;
    std::cout << 28600;
    for (int i = 0; i < 28600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28600(int& val) {
    const int x = 28600;
    std::cout << 28600;
    for (int i = 0; i < 28600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28601(int& val) {
    func_in_28601(val);
    const int x = 28601;
    std::cout << 28601;
    for (int i = 0; i < 28601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28601(int& val) {
    const int x = 28601;
    std::cout << 28601;
    for (int i = 0; i < 28601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28602(int& val) {
    func_in_28602(val);
    const int x = 28602;
    std::cout << 28602;
    for (int i = 0; i < 28602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28602(int& val) {
    const int x = 28602;
    std::cout << 28602;
    for (int i = 0; i < 28602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28603(int& val) {
    func_in_28603(val);
    const int x = 28603;
    std::cout << 28603;
    for (int i = 0; i < 28603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28603(int& val) {
    const int x = 28603;
    std::cout << 28603;
    for (int i = 0; i < 28603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28604(int& val) {
    func_in_28604(val);
    const int x = 28604;
    std::cout << 28604;
    for (int i = 0; i < 28604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28604(int& val) {
    const int x = 28604;
    std::cout << 28604;
    for (int i = 0; i < 28604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28605(int& val) {
    func_in_28605(val);
    const int x = 28605;
    std::cout << 28605;
    for (int i = 0; i < 28605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28605(int& val) {
    const int x = 28605;
    std::cout << 28605;
    for (int i = 0; i < 28605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28606(int& val) {
    func_in_28606(val);
    const int x = 28606;
    std::cout << 28606;
    for (int i = 0; i < 28606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28606(int& val) {
    const int x = 28606;
    std::cout << 28606;
    for (int i = 0; i < 28606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28607(int& val) {
    func_in_28607(val);
    const int x = 28607;
    std::cout << 28607;
    for (int i = 0; i < 28607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28607(int& val) {
    const int x = 28607;
    std::cout << 28607;
    for (int i = 0; i < 28607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28608(int& val) {
    func_in_28608(val);
    const int x = 28608;
    std::cout << 28608;
    for (int i = 0; i < 28608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28608(int& val) {
    const int x = 28608;
    std::cout << 28608;
    for (int i = 0; i < 28608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28609(int& val) {
    func_in_28609(val);
    const int x = 28609;
    std::cout << 28609;
    for (int i = 0; i < 28609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28609(int& val) {
    const int x = 28609;
    std::cout << 28609;
    for (int i = 0; i < 28609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28610(int& val) {
    func_in_28610(val);
    const int x = 28610;
    std::cout << 28610;
    for (int i = 0; i < 28610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28610(int& val) {
    const int x = 28610;
    std::cout << 28610;
    for (int i = 0; i < 28610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28611(int& val) {
    func_in_28611(val);
    const int x = 28611;
    std::cout << 28611;
    for (int i = 0; i < 28611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28611(int& val) {
    const int x = 28611;
    std::cout << 28611;
    for (int i = 0; i < 28611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28612(int& val) {
    func_in_28612(val);
    const int x = 28612;
    std::cout << 28612;
    for (int i = 0; i < 28612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28612(int& val) {
    const int x = 28612;
    std::cout << 28612;
    for (int i = 0; i < 28612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28613(int& val) {
    func_in_28613(val);
    const int x = 28613;
    std::cout << 28613;
    for (int i = 0; i < 28613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28613(int& val) {
    const int x = 28613;
    std::cout << 28613;
    for (int i = 0; i < 28613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28614(int& val) {
    func_in_28614(val);
    const int x = 28614;
    std::cout << 28614;
    for (int i = 0; i < 28614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28614(int& val) {
    const int x = 28614;
    std::cout << 28614;
    for (int i = 0; i < 28614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28615(int& val) {
    func_in_28615(val);
    const int x = 28615;
    std::cout << 28615;
    for (int i = 0; i < 28615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28615(int& val) {
    const int x = 28615;
    std::cout << 28615;
    for (int i = 0; i < 28615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28616(int& val) {
    func_in_28616(val);
    const int x = 28616;
    std::cout << 28616;
    for (int i = 0; i < 28616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28616(int& val) {
    const int x = 28616;
    std::cout << 28616;
    for (int i = 0; i < 28616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28617(int& val) {
    func_in_28617(val);
    const int x = 28617;
    std::cout << 28617;
    for (int i = 0; i < 28617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28617(int& val) {
    const int x = 28617;
    std::cout << 28617;
    for (int i = 0; i < 28617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28618(int& val) {
    func_in_28618(val);
    const int x = 28618;
    std::cout << 28618;
    for (int i = 0; i < 28618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28618(int& val) {
    const int x = 28618;
    std::cout << 28618;
    for (int i = 0; i < 28618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28619(int& val) {
    func_in_28619(val);
    const int x = 28619;
    std::cout << 28619;
    for (int i = 0; i < 28619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28619(int& val) {
    const int x = 28619;
    std::cout << 28619;
    for (int i = 0; i < 28619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28620(int& val) {
    func_in_28620(val);
    const int x = 28620;
    std::cout << 28620;
    for (int i = 0; i < 28620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28620(int& val) {
    const int x = 28620;
    std::cout << 28620;
    for (int i = 0; i < 28620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28621(int& val) {
    func_in_28621(val);
    const int x = 28621;
    std::cout << 28621;
    for (int i = 0; i < 28621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28621(int& val) {
    const int x = 28621;
    std::cout << 28621;
    for (int i = 0; i < 28621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28622(int& val) {
    func_in_28622(val);
    const int x = 28622;
    std::cout << 28622;
    for (int i = 0; i < 28622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28622(int& val) {
    const int x = 28622;
    std::cout << 28622;
    for (int i = 0; i < 28622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28623(int& val) {
    func_in_28623(val);
    const int x = 28623;
    std::cout << 28623;
    for (int i = 0; i < 28623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28623(int& val) {
    const int x = 28623;
    std::cout << 28623;
    for (int i = 0; i < 28623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28624(int& val) {
    func_in_28624(val);
    const int x = 28624;
    std::cout << 28624;
    for (int i = 0; i < 28624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28624(int& val) {
    const int x = 28624;
    std::cout << 28624;
    for (int i = 0; i < 28624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28625(int& val) {
    func_in_28625(val);
    const int x = 28625;
    std::cout << 28625;
    for (int i = 0; i < 28625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28625(int& val) {
    const int x = 28625;
    std::cout << 28625;
    for (int i = 0; i < 28625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28626(int& val) {
    func_in_28626(val);
    const int x = 28626;
    std::cout << 28626;
    for (int i = 0; i < 28626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28626(int& val) {
    const int x = 28626;
    std::cout << 28626;
    for (int i = 0; i < 28626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28627(int& val) {
    func_in_28627(val);
    const int x = 28627;
    std::cout << 28627;
    for (int i = 0; i < 28627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28627(int& val) {
    const int x = 28627;
    std::cout << 28627;
    for (int i = 0; i < 28627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28628(int& val) {
    func_in_28628(val);
    const int x = 28628;
    std::cout << 28628;
    for (int i = 0; i < 28628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28628(int& val) {
    const int x = 28628;
    std::cout << 28628;
    for (int i = 0; i < 28628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28629(int& val) {
    func_in_28629(val);
    const int x = 28629;
    std::cout << 28629;
    for (int i = 0; i < 28629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28629(int& val) {
    const int x = 28629;
    std::cout << 28629;
    for (int i = 0; i < 28629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28630(int& val) {
    func_in_28630(val);
    const int x = 28630;
    std::cout << 28630;
    for (int i = 0; i < 28630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28630(int& val) {
    const int x = 28630;
    std::cout << 28630;
    for (int i = 0; i < 28630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28631(int& val) {
    func_in_28631(val);
    const int x = 28631;
    std::cout << 28631;
    for (int i = 0; i < 28631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28631(int& val) {
    const int x = 28631;
    std::cout << 28631;
    for (int i = 0; i < 28631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28632(int& val) {
    func_in_28632(val);
    const int x = 28632;
    std::cout << 28632;
    for (int i = 0; i < 28632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28632(int& val) {
    const int x = 28632;
    std::cout << 28632;
    for (int i = 0; i < 28632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28633(int& val) {
    func_in_28633(val);
    const int x = 28633;
    std::cout << 28633;
    for (int i = 0; i < 28633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28633(int& val) {
    const int x = 28633;
    std::cout << 28633;
    for (int i = 0; i < 28633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28634(int& val) {
    func_in_28634(val);
    const int x = 28634;
    std::cout << 28634;
    for (int i = 0; i < 28634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28634(int& val) {
    const int x = 28634;
    std::cout << 28634;
    for (int i = 0; i < 28634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28635(int& val) {
    func_in_28635(val);
    const int x = 28635;
    std::cout << 28635;
    for (int i = 0; i < 28635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28635(int& val) {
    const int x = 28635;
    std::cout << 28635;
    for (int i = 0; i < 28635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28636(int& val) {
    func_in_28636(val);
    const int x = 28636;
    std::cout << 28636;
    for (int i = 0; i < 28636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28636(int& val) {
    const int x = 28636;
    std::cout << 28636;
    for (int i = 0; i < 28636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28637(int& val) {
    func_in_28637(val);
    const int x = 28637;
    std::cout << 28637;
    for (int i = 0; i < 28637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28637(int& val) {
    const int x = 28637;
    std::cout << 28637;
    for (int i = 0; i < 28637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28638(int& val) {
    func_in_28638(val);
    const int x = 28638;
    std::cout << 28638;
    for (int i = 0; i < 28638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28638(int& val) {
    const int x = 28638;
    std::cout << 28638;
    for (int i = 0; i < 28638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28639(int& val) {
    func_in_28639(val);
    const int x = 28639;
    std::cout << 28639;
    for (int i = 0; i < 28639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28639(int& val) {
    const int x = 28639;
    std::cout << 28639;
    for (int i = 0; i < 28639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28640(int& val) {
    func_in_28640(val);
    const int x = 28640;
    std::cout << 28640;
    for (int i = 0; i < 28640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28640(int& val) {
    const int x = 28640;
    std::cout << 28640;
    for (int i = 0; i < 28640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28641(int& val) {
    func_in_28641(val);
    const int x = 28641;
    std::cout << 28641;
    for (int i = 0; i < 28641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28641(int& val) {
    const int x = 28641;
    std::cout << 28641;
    for (int i = 0; i < 28641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28642(int& val) {
    func_in_28642(val);
    const int x = 28642;
    std::cout << 28642;
    for (int i = 0; i < 28642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28642(int& val) {
    const int x = 28642;
    std::cout << 28642;
    for (int i = 0; i < 28642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28643(int& val) {
    func_in_28643(val);
    const int x = 28643;
    std::cout << 28643;
    for (int i = 0; i < 28643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28643(int& val) {
    const int x = 28643;
    std::cout << 28643;
    for (int i = 0; i < 28643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28644(int& val) {
    func_in_28644(val);
    const int x = 28644;
    std::cout << 28644;
    for (int i = 0; i < 28644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28644(int& val) {
    const int x = 28644;
    std::cout << 28644;
    for (int i = 0; i < 28644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28645(int& val) {
    func_in_28645(val);
    const int x = 28645;
    std::cout << 28645;
    for (int i = 0; i < 28645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28645(int& val) {
    const int x = 28645;
    std::cout << 28645;
    for (int i = 0; i < 28645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28646(int& val) {
    func_in_28646(val);
    const int x = 28646;
    std::cout << 28646;
    for (int i = 0; i < 28646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28646(int& val) {
    const int x = 28646;
    std::cout << 28646;
    for (int i = 0; i < 28646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28647(int& val) {
    func_in_28647(val);
    const int x = 28647;
    std::cout << 28647;
    for (int i = 0; i < 28647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28647(int& val) {
    const int x = 28647;
    std::cout << 28647;
    for (int i = 0; i < 28647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28648(int& val) {
    func_in_28648(val);
    const int x = 28648;
    std::cout << 28648;
    for (int i = 0; i < 28648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28648(int& val) {
    const int x = 28648;
    std::cout << 28648;
    for (int i = 0; i < 28648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28649(int& val) {
    func_in_28649(val);
    const int x = 28649;
    std::cout << 28649;
    for (int i = 0; i < 28649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28649(int& val) {
    const int x = 28649;
    std::cout << 28649;
    for (int i = 0; i < 28649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28650(int& val) {
    func_in_28650(val);
    const int x = 28650;
    std::cout << 28650;
    for (int i = 0; i < 28650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28650(int& val) {
    const int x = 28650;
    std::cout << 28650;
    for (int i = 0; i < 28650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28651(int& val) {
    func_in_28651(val);
    const int x = 28651;
    std::cout << 28651;
    for (int i = 0; i < 28651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28651(int& val) {
    const int x = 28651;
    std::cout << 28651;
    for (int i = 0; i < 28651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28652(int& val) {
    func_in_28652(val);
    const int x = 28652;
    std::cout << 28652;
    for (int i = 0; i < 28652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28652(int& val) {
    const int x = 28652;
    std::cout << 28652;
    for (int i = 0; i < 28652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28653(int& val) {
    func_in_28653(val);
    const int x = 28653;
    std::cout << 28653;
    for (int i = 0; i < 28653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28653(int& val) {
    const int x = 28653;
    std::cout << 28653;
    for (int i = 0; i < 28653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28654(int& val) {
    func_in_28654(val);
    const int x = 28654;
    std::cout << 28654;
    for (int i = 0; i < 28654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28654(int& val) {
    const int x = 28654;
    std::cout << 28654;
    for (int i = 0; i < 28654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28655(int& val) {
    func_in_28655(val);
    const int x = 28655;
    std::cout << 28655;
    for (int i = 0; i < 28655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28655(int& val) {
    const int x = 28655;
    std::cout << 28655;
    for (int i = 0; i < 28655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28656(int& val) {
    func_in_28656(val);
    const int x = 28656;
    std::cout << 28656;
    for (int i = 0; i < 28656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28656(int& val) {
    const int x = 28656;
    std::cout << 28656;
    for (int i = 0; i < 28656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28657(int& val) {
    func_in_28657(val);
    const int x = 28657;
    std::cout << 28657;
    for (int i = 0; i < 28657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28657(int& val) {
    const int x = 28657;
    std::cout << 28657;
    for (int i = 0; i < 28657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28658(int& val) {
    func_in_28658(val);
    const int x = 28658;
    std::cout << 28658;
    for (int i = 0; i < 28658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28658(int& val) {
    const int x = 28658;
    std::cout << 28658;
    for (int i = 0; i < 28658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28659(int& val) {
    func_in_28659(val);
    const int x = 28659;
    std::cout << 28659;
    for (int i = 0; i < 28659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28659(int& val) {
    const int x = 28659;
    std::cout << 28659;
    for (int i = 0; i < 28659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28660(int& val) {
    func_in_28660(val);
    const int x = 28660;
    std::cout << 28660;
    for (int i = 0; i < 28660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28660(int& val) {
    const int x = 28660;
    std::cout << 28660;
    for (int i = 0; i < 28660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28661(int& val) {
    func_in_28661(val);
    const int x = 28661;
    std::cout << 28661;
    for (int i = 0; i < 28661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28661(int& val) {
    const int x = 28661;
    std::cout << 28661;
    for (int i = 0; i < 28661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28662(int& val) {
    func_in_28662(val);
    const int x = 28662;
    std::cout << 28662;
    for (int i = 0; i < 28662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28662(int& val) {
    const int x = 28662;
    std::cout << 28662;
    for (int i = 0; i < 28662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28663(int& val) {
    func_in_28663(val);
    const int x = 28663;
    std::cout << 28663;
    for (int i = 0; i < 28663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28663(int& val) {
    const int x = 28663;
    std::cout << 28663;
    for (int i = 0; i < 28663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28664(int& val) {
    func_in_28664(val);
    const int x = 28664;
    std::cout << 28664;
    for (int i = 0; i < 28664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28664(int& val) {
    const int x = 28664;
    std::cout << 28664;
    for (int i = 0; i < 28664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28665(int& val) {
    func_in_28665(val);
    const int x = 28665;
    std::cout << 28665;
    for (int i = 0; i < 28665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28665(int& val) {
    const int x = 28665;
    std::cout << 28665;
    for (int i = 0; i < 28665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28666(int& val) {
    func_in_28666(val);
    const int x = 28666;
    std::cout << 28666;
    for (int i = 0; i < 28666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28666(int& val) {
    const int x = 28666;
    std::cout << 28666;
    for (int i = 0; i < 28666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28667(int& val) {
    func_in_28667(val);
    const int x = 28667;
    std::cout << 28667;
    for (int i = 0; i < 28667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28667(int& val) {
    const int x = 28667;
    std::cout << 28667;
    for (int i = 0; i < 28667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28668(int& val) {
    func_in_28668(val);
    const int x = 28668;
    std::cout << 28668;
    for (int i = 0; i < 28668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28668(int& val) {
    const int x = 28668;
    std::cout << 28668;
    for (int i = 0; i < 28668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28669(int& val) {
    func_in_28669(val);
    const int x = 28669;
    std::cout << 28669;
    for (int i = 0; i < 28669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28669(int& val) {
    const int x = 28669;
    std::cout << 28669;
    for (int i = 0; i < 28669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28670(int& val) {
    func_in_28670(val);
    const int x = 28670;
    std::cout << 28670;
    for (int i = 0; i < 28670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28670(int& val) {
    const int x = 28670;
    std::cout << 28670;
    for (int i = 0; i < 28670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28671(int& val) {
    func_in_28671(val);
    const int x = 28671;
    std::cout << 28671;
    for (int i = 0; i < 28671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28671(int& val) {
    const int x = 28671;
    std::cout << 28671;
    for (int i = 0; i < 28671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28672(int& val) {
    func_in_28672(val);
    const int x = 28672;
    std::cout << 28672;
    for (int i = 0; i < 28672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28672(int& val) {
    const int x = 28672;
    std::cout << 28672;
    for (int i = 0; i < 28672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28673(int& val) {
    func_in_28673(val);
    const int x = 28673;
    std::cout << 28673;
    for (int i = 0; i < 28673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28673(int& val) {
    const int x = 28673;
    std::cout << 28673;
    for (int i = 0; i < 28673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28674(int& val) {
    func_in_28674(val);
    const int x = 28674;
    std::cout << 28674;
    for (int i = 0; i < 28674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28674(int& val) {
    const int x = 28674;
    std::cout << 28674;
    for (int i = 0; i < 28674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28675(int& val) {
    func_in_28675(val);
    const int x = 28675;
    std::cout << 28675;
    for (int i = 0; i < 28675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28675(int& val) {
    const int x = 28675;
    std::cout << 28675;
    for (int i = 0; i < 28675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28676(int& val) {
    func_in_28676(val);
    const int x = 28676;
    std::cout << 28676;
    for (int i = 0; i < 28676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28676(int& val) {
    const int x = 28676;
    std::cout << 28676;
    for (int i = 0; i < 28676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28677(int& val) {
    func_in_28677(val);
    const int x = 28677;
    std::cout << 28677;
    for (int i = 0; i < 28677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28677(int& val) {
    const int x = 28677;
    std::cout << 28677;
    for (int i = 0; i < 28677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28678(int& val) {
    func_in_28678(val);
    const int x = 28678;
    std::cout << 28678;
    for (int i = 0; i < 28678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28678(int& val) {
    const int x = 28678;
    std::cout << 28678;
    for (int i = 0; i < 28678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28679(int& val) {
    func_in_28679(val);
    const int x = 28679;
    std::cout << 28679;
    for (int i = 0; i < 28679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28679(int& val) {
    const int x = 28679;
    std::cout << 28679;
    for (int i = 0; i < 28679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28680(int& val) {
    func_in_28680(val);
    const int x = 28680;
    std::cout << 28680;
    for (int i = 0; i < 28680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28680(int& val) {
    const int x = 28680;
    std::cout << 28680;
    for (int i = 0; i < 28680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28681(int& val) {
    func_in_28681(val);
    const int x = 28681;
    std::cout << 28681;
    for (int i = 0; i < 28681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28681(int& val) {
    const int x = 28681;
    std::cout << 28681;
    for (int i = 0; i < 28681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28682(int& val) {
    func_in_28682(val);
    const int x = 28682;
    std::cout << 28682;
    for (int i = 0; i < 28682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28682(int& val) {
    const int x = 28682;
    std::cout << 28682;
    for (int i = 0; i < 28682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28683(int& val) {
    func_in_28683(val);
    const int x = 28683;
    std::cout << 28683;
    for (int i = 0; i < 28683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28683(int& val) {
    const int x = 28683;
    std::cout << 28683;
    for (int i = 0; i < 28683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28684(int& val) {
    func_in_28684(val);
    const int x = 28684;
    std::cout << 28684;
    for (int i = 0; i < 28684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28684(int& val) {
    const int x = 28684;
    std::cout << 28684;
    for (int i = 0; i < 28684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28685(int& val) {
    func_in_28685(val);
    const int x = 28685;
    std::cout << 28685;
    for (int i = 0; i < 28685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28685(int& val) {
    const int x = 28685;
    std::cout << 28685;
    for (int i = 0; i < 28685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28686(int& val) {
    func_in_28686(val);
    const int x = 28686;
    std::cout << 28686;
    for (int i = 0; i < 28686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28686(int& val) {
    const int x = 28686;
    std::cout << 28686;
    for (int i = 0; i < 28686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28687(int& val) {
    func_in_28687(val);
    const int x = 28687;
    std::cout << 28687;
    for (int i = 0; i < 28687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28687(int& val) {
    const int x = 28687;
    std::cout << 28687;
    for (int i = 0; i < 28687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28688(int& val) {
    func_in_28688(val);
    const int x = 28688;
    std::cout << 28688;
    for (int i = 0; i < 28688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28688(int& val) {
    const int x = 28688;
    std::cout << 28688;
    for (int i = 0; i < 28688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28689(int& val) {
    func_in_28689(val);
    const int x = 28689;
    std::cout << 28689;
    for (int i = 0; i < 28689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28689(int& val) {
    const int x = 28689;
    std::cout << 28689;
    for (int i = 0; i < 28689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28690(int& val) {
    func_in_28690(val);
    const int x = 28690;
    std::cout << 28690;
    for (int i = 0; i < 28690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28690(int& val) {
    const int x = 28690;
    std::cout << 28690;
    for (int i = 0; i < 28690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28691(int& val) {
    func_in_28691(val);
    const int x = 28691;
    std::cout << 28691;
    for (int i = 0; i < 28691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28691(int& val) {
    const int x = 28691;
    std::cout << 28691;
    for (int i = 0; i < 28691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28692(int& val) {
    func_in_28692(val);
    const int x = 28692;
    std::cout << 28692;
    for (int i = 0; i < 28692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28692(int& val) {
    const int x = 28692;
    std::cout << 28692;
    for (int i = 0; i < 28692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28693(int& val) {
    func_in_28693(val);
    const int x = 28693;
    std::cout << 28693;
    for (int i = 0; i < 28693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28693(int& val) {
    const int x = 28693;
    std::cout << 28693;
    for (int i = 0; i < 28693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28694(int& val) {
    func_in_28694(val);
    const int x = 28694;
    std::cout << 28694;
    for (int i = 0; i < 28694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28694(int& val) {
    const int x = 28694;
    std::cout << 28694;
    for (int i = 0; i < 28694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28695(int& val) {
    func_in_28695(val);
    const int x = 28695;
    std::cout << 28695;
    for (int i = 0; i < 28695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28695(int& val) {
    const int x = 28695;
    std::cout << 28695;
    for (int i = 0; i < 28695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28696(int& val) {
    func_in_28696(val);
    const int x = 28696;
    std::cout << 28696;
    for (int i = 0; i < 28696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28696(int& val) {
    const int x = 28696;
    std::cout << 28696;
    for (int i = 0; i < 28696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28697(int& val) {
    func_in_28697(val);
    const int x = 28697;
    std::cout << 28697;
    for (int i = 0; i < 28697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28697(int& val) {
    const int x = 28697;
    std::cout << 28697;
    for (int i = 0; i < 28697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28698(int& val) {
    func_in_28698(val);
    const int x = 28698;
    std::cout << 28698;
    for (int i = 0; i < 28698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28698(int& val) {
    const int x = 28698;
    std::cout << 28698;
    for (int i = 0; i < 28698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28699(int& val) {
    func_in_28699(val);
    const int x = 28699;
    std::cout << 28699;
    for (int i = 0; i < 28699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28699(int& val) {
    const int x = 28699;
    std::cout << 28699;
    for (int i = 0; i < 28699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28700(int& val) {
    func_in_28700(val);
    const int x = 28700;
    std::cout << 28700;
    for (int i = 0; i < 28700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28700(int& val) {
    const int x = 28700;
    std::cout << 28700;
    for (int i = 0; i < 28700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28701(int& val) {
    func_in_28701(val);
    const int x = 28701;
    std::cout << 28701;
    for (int i = 0; i < 28701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28701(int& val) {
    const int x = 28701;
    std::cout << 28701;
    for (int i = 0; i < 28701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28702(int& val) {
    func_in_28702(val);
    const int x = 28702;
    std::cout << 28702;
    for (int i = 0; i < 28702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28702(int& val) {
    const int x = 28702;
    std::cout << 28702;
    for (int i = 0; i < 28702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28703(int& val) {
    func_in_28703(val);
    const int x = 28703;
    std::cout << 28703;
    for (int i = 0; i < 28703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28703(int& val) {
    const int x = 28703;
    std::cout << 28703;
    for (int i = 0; i < 28703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28704(int& val) {
    func_in_28704(val);
    const int x = 28704;
    std::cout << 28704;
    for (int i = 0; i < 28704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28704(int& val) {
    const int x = 28704;
    std::cout << 28704;
    for (int i = 0; i < 28704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28705(int& val) {
    func_in_28705(val);
    const int x = 28705;
    std::cout << 28705;
    for (int i = 0; i < 28705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28705(int& val) {
    const int x = 28705;
    std::cout << 28705;
    for (int i = 0; i < 28705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28706(int& val) {
    func_in_28706(val);
    const int x = 28706;
    std::cout << 28706;
    for (int i = 0; i < 28706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28706(int& val) {
    const int x = 28706;
    std::cout << 28706;
    for (int i = 0; i < 28706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28707(int& val) {
    func_in_28707(val);
    const int x = 28707;
    std::cout << 28707;
    for (int i = 0; i < 28707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28707(int& val) {
    const int x = 28707;
    std::cout << 28707;
    for (int i = 0; i < 28707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28708(int& val) {
    func_in_28708(val);
    const int x = 28708;
    std::cout << 28708;
    for (int i = 0; i < 28708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28708(int& val) {
    const int x = 28708;
    std::cout << 28708;
    for (int i = 0; i < 28708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28709(int& val) {
    func_in_28709(val);
    const int x = 28709;
    std::cout << 28709;
    for (int i = 0; i < 28709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28709(int& val) {
    const int x = 28709;
    std::cout << 28709;
    for (int i = 0; i < 28709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28710(int& val) {
    func_in_28710(val);
    const int x = 28710;
    std::cout << 28710;
    for (int i = 0; i < 28710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28710(int& val) {
    const int x = 28710;
    std::cout << 28710;
    for (int i = 0; i < 28710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28711(int& val) {
    func_in_28711(val);
    const int x = 28711;
    std::cout << 28711;
    for (int i = 0; i < 28711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28711(int& val) {
    const int x = 28711;
    std::cout << 28711;
    for (int i = 0; i < 28711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28712(int& val) {
    func_in_28712(val);
    const int x = 28712;
    std::cout << 28712;
    for (int i = 0; i < 28712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28712(int& val) {
    const int x = 28712;
    std::cout << 28712;
    for (int i = 0; i < 28712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28713(int& val) {
    func_in_28713(val);
    const int x = 28713;
    std::cout << 28713;
    for (int i = 0; i < 28713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28713(int& val) {
    const int x = 28713;
    std::cout << 28713;
    for (int i = 0; i < 28713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28714(int& val) {
    func_in_28714(val);
    const int x = 28714;
    std::cout << 28714;
    for (int i = 0; i < 28714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28714(int& val) {
    const int x = 28714;
    std::cout << 28714;
    for (int i = 0; i < 28714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28715(int& val) {
    func_in_28715(val);
    const int x = 28715;
    std::cout << 28715;
    for (int i = 0; i < 28715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28715(int& val) {
    const int x = 28715;
    std::cout << 28715;
    for (int i = 0; i < 28715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28716(int& val) {
    func_in_28716(val);
    const int x = 28716;
    std::cout << 28716;
    for (int i = 0; i < 28716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28716(int& val) {
    const int x = 28716;
    std::cout << 28716;
    for (int i = 0; i < 28716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28717(int& val) {
    func_in_28717(val);
    const int x = 28717;
    std::cout << 28717;
    for (int i = 0; i < 28717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28717(int& val) {
    const int x = 28717;
    std::cout << 28717;
    for (int i = 0; i < 28717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28718(int& val) {
    func_in_28718(val);
    const int x = 28718;
    std::cout << 28718;
    for (int i = 0; i < 28718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28718(int& val) {
    const int x = 28718;
    std::cout << 28718;
    for (int i = 0; i < 28718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28719(int& val) {
    func_in_28719(val);
    const int x = 28719;
    std::cout << 28719;
    for (int i = 0; i < 28719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28719(int& val) {
    const int x = 28719;
    std::cout << 28719;
    for (int i = 0; i < 28719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28720(int& val) {
    func_in_28720(val);
    const int x = 28720;
    std::cout << 28720;
    for (int i = 0; i < 28720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28720(int& val) {
    const int x = 28720;
    std::cout << 28720;
    for (int i = 0; i < 28720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28721(int& val) {
    func_in_28721(val);
    const int x = 28721;
    std::cout << 28721;
    for (int i = 0; i < 28721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28721(int& val) {
    const int x = 28721;
    std::cout << 28721;
    for (int i = 0; i < 28721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28722(int& val) {
    func_in_28722(val);
    const int x = 28722;
    std::cout << 28722;
    for (int i = 0; i < 28722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28722(int& val) {
    const int x = 28722;
    std::cout << 28722;
    for (int i = 0; i < 28722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28723(int& val) {
    func_in_28723(val);
    const int x = 28723;
    std::cout << 28723;
    for (int i = 0; i < 28723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28723(int& val) {
    const int x = 28723;
    std::cout << 28723;
    for (int i = 0; i < 28723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28724(int& val) {
    func_in_28724(val);
    const int x = 28724;
    std::cout << 28724;
    for (int i = 0; i < 28724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28724(int& val) {
    const int x = 28724;
    std::cout << 28724;
    for (int i = 0; i < 28724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28725(int& val) {
    func_in_28725(val);
    const int x = 28725;
    std::cout << 28725;
    for (int i = 0; i < 28725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28725(int& val) {
    const int x = 28725;
    std::cout << 28725;
    for (int i = 0; i < 28725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28726(int& val) {
    func_in_28726(val);
    const int x = 28726;
    std::cout << 28726;
    for (int i = 0; i < 28726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28726(int& val) {
    const int x = 28726;
    std::cout << 28726;
    for (int i = 0; i < 28726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28727(int& val) {
    func_in_28727(val);
    const int x = 28727;
    std::cout << 28727;
    for (int i = 0; i < 28727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28727(int& val) {
    const int x = 28727;
    std::cout << 28727;
    for (int i = 0; i < 28727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28728(int& val) {
    func_in_28728(val);
    const int x = 28728;
    std::cout << 28728;
    for (int i = 0; i < 28728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28728(int& val) {
    const int x = 28728;
    std::cout << 28728;
    for (int i = 0; i < 28728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28729(int& val) {
    func_in_28729(val);
    const int x = 28729;
    std::cout << 28729;
    for (int i = 0; i < 28729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28729(int& val) {
    const int x = 28729;
    std::cout << 28729;
    for (int i = 0; i < 28729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28730(int& val) {
    func_in_28730(val);
    const int x = 28730;
    std::cout << 28730;
    for (int i = 0; i < 28730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28730(int& val) {
    const int x = 28730;
    std::cout << 28730;
    for (int i = 0; i < 28730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28731(int& val) {
    func_in_28731(val);
    const int x = 28731;
    std::cout << 28731;
    for (int i = 0; i < 28731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28731(int& val) {
    const int x = 28731;
    std::cout << 28731;
    for (int i = 0; i < 28731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28732(int& val) {
    func_in_28732(val);
    const int x = 28732;
    std::cout << 28732;
    for (int i = 0; i < 28732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28732(int& val) {
    const int x = 28732;
    std::cout << 28732;
    for (int i = 0; i < 28732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28733(int& val) {
    func_in_28733(val);
    const int x = 28733;
    std::cout << 28733;
    for (int i = 0; i < 28733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28733(int& val) {
    const int x = 28733;
    std::cout << 28733;
    for (int i = 0; i < 28733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28734(int& val) {
    func_in_28734(val);
    const int x = 28734;
    std::cout << 28734;
    for (int i = 0; i < 28734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28734(int& val) {
    const int x = 28734;
    std::cout << 28734;
    for (int i = 0; i < 28734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28735(int& val) {
    func_in_28735(val);
    const int x = 28735;
    std::cout << 28735;
    for (int i = 0; i < 28735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28735(int& val) {
    const int x = 28735;
    std::cout << 28735;
    for (int i = 0; i < 28735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28736(int& val) {
    func_in_28736(val);
    const int x = 28736;
    std::cout << 28736;
    for (int i = 0; i < 28736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28736(int& val) {
    const int x = 28736;
    std::cout << 28736;
    for (int i = 0; i < 28736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28737(int& val) {
    func_in_28737(val);
    const int x = 28737;
    std::cout << 28737;
    for (int i = 0; i < 28737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28737(int& val) {
    const int x = 28737;
    std::cout << 28737;
    for (int i = 0; i < 28737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28738(int& val) {
    func_in_28738(val);
    const int x = 28738;
    std::cout << 28738;
    for (int i = 0; i < 28738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28738(int& val) {
    const int x = 28738;
    std::cout << 28738;
    for (int i = 0; i < 28738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28739(int& val) {
    func_in_28739(val);
    const int x = 28739;
    std::cout << 28739;
    for (int i = 0; i < 28739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28739(int& val) {
    const int x = 28739;
    std::cout << 28739;
    for (int i = 0; i < 28739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28740(int& val) {
    func_in_28740(val);
    const int x = 28740;
    std::cout << 28740;
    for (int i = 0; i < 28740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28740(int& val) {
    const int x = 28740;
    std::cout << 28740;
    for (int i = 0; i < 28740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28741(int& val) {
    func_in_28741(val);
    const int x = 28741;
    std::cout << 28741;
    for (int i = 0; i < 28741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28741(int& val) {
    const int x = 28741;
    std::cout << 28741;
    for (int i = 0; i < 28741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28742(int& val) {
    func_in_28742(val);
    const int x = 28742;
    std::cout << 28742;
    for (int i = 0; i < 28742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28742(int& val) {
    const int x = 28742;
    std::cout << 28742;
    for (int i = 0; i < 28742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28743(int& val) {
    func_in_28743(val);
    const int x = 28743;
    std::cout << 28743;
    for (int i = 0; i < 28743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28743(int& val) {
    const int x = 28743;
    std::cout << 28743;
    for (int i = 0; i < 28743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28744(int& val) {
    func_in_28744(val);
    const int x = 28744;
    std::cout << 28744;
    for (int i = 0; i < 28744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28744(int& val) {
    const int x = 28744;
    std::cout << 28744;
    for (int i = 0; i < 28744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28745(int& val) {
    func_in_28745(val);
    const int x = 28745;
    std::cout << 28745;
    for (int i = 0; i < 28745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28745(int& val) {
    const int x = 28745;
    std::cout << 28745;
    for (int i = 0; i < 28745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28746(int& val) {
    func_in_28746(val);
    const int x = 28746;
    std::cout << 28746;
    for (int i = 0; i < 28746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28746(int& val) {
    const int x = 28746;
    std::cout << 28746;
    for (int i = 0; i < 28746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28747(int& val) {
    func_in_28747(val);
    const int x = 28747;
    std::cout << 28747;
    for (int i = 0; i < 28747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28747(int& val) {
    const int x = 28747;
    std::cout << 28747;
    for (int i = 0; i < 28747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28748(int& val) {
    func_in_28748(val);
    const int x = 28748;
    std::cout << 28748;
    for (int i = 0; i < 28748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28748(int& val) {
    const int x = 28748;
    std::cout << 28748;
    for (int i = 0; i < 28748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28749(int& val) {
    func_in_28749(val);
    const int x = 28749;
    std::cout << 28749;
    for (int i = 0; i < 28749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28749(int& val) {
    const int x = 28749;
    std::cout << 28749;
    for (int i = 0; i < 28749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28750(int& val) {
    func_in_28750(val);
    const int x = 28750;
    std::cout << 28750;
    for (int i = 0; i < 28750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28750(int& val) {
    const int x = 28750;
    std::cout << 28750;
    for (int i = 0; i < 28750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28751(int& val) {
    func_in_28751(val);
    const int x = 28751;
    std::cout << 28751;
    for (int i = 0; i < 28751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28751(int& val) {
    const int x = 28751;
    std::cout << 28751;
    for (int i = 0; i < 28751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28752(int& val) {
    func_in_28752(val);
    const int x = 28752;
    std::cout << 28752;
    for (int i = 0; i < 28752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28752(int& val) {
    const int x = 28752;
    std::cout << 28752;
    for (int i = 0; i < 28752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28753(int& val) {
    func_in_28753(val);
    const int x = 28753;
    std::cout << 28753;
    for (int i = 0; i < 28753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28753(int& val) {
    const int x = 28753;
    std::cout << 28753;
    for (int i = 0; i < 28753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28754(int& val) {
    func_in_28754(val);
    const int x = 28754;
    std::cout << 28754;
    for (int i = 0; i < 28754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28754(int& val) {
    const int x = 28754;
    std::cout << 28754;
    for (int i = 0; i < 28754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28755(int& val) {
    func_in_28755(val);
    const int x = 28755;
    std::cout << 28755;
    for (int i = 0; i < 28755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28755(int& val) {
    const int x = 28755;
    std::cout << 28755;
    for (int i = 0; i < 28755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28756(int& val) {
    func_in_28756(val);
    const int x = 28756;
    std::cout << 28756;
    for (int i = 0; i < 28756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28756(int& val) {
    const int x = 28756;
    std::cout << 28756;
    for (int i = 0; i < 28756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28757(int& val) {
    func_in_28757(val);
    const int x = 28757;
    std::cout << 28757;
    for (int i = 0; i < 28757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28757(int& val) {
    const int x = 28757;
    std::cout << 28757;
    for (int i = 0; i < 28757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28758(int& val) {
    func_in_28758(val);
    const int x = 28758;
    std::cout << 28758;
    for (int i = 0; i < 28758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28758(int& val) {
    const int x = 28758;
    std::cout << 28758;
    for (int i = 0; i < 28758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28759(int& val) {
    func_in_28759(val);
    const int x = 28759;
    std::cout << 28759;
    for (int i = 0; i < 28759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28759(int& val) {
    const int x = 28759;
    std::cout << 28759;
    for (int i = 0; i < 28759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28760(int& val) {
    func_in_28760(val);
    const int x = 28760;
    std::cout << 28760;
    for (int i = 0; i < 28760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28760(int& val) {
    const int x = 28760;
    std::cout << 28760;
    for (int i = 0; i < 28760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28761(int& val) {
    func_in_28761(val);
    const int x = 28761;
    std::cout << 28761;
    for (int i = 0; i < 28761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28761(int& val) {
    const int x = 28761;
    std::cout << 28761;
    for (int i = 0; i < 28761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28762(int& val) {
    func_in_28762(val);
    const int x = 28762;
    std::cout << 28762;
    for (int i = 0; i < 28762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28762(int& val) {
    const int x = 28762;
    std::cout << 28762;
    for (int i = 0; i < 28762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28763(int& val) {
    func_in_28763(val);
    const int x = 28763;
    std::cout << 28763;
    for (int i = 0; i < 28763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28763(int& val) {
    const int x = 28763;
    std::cout << 28763;
    for (int i = 0; i < 28763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28764(int& val) {
    func_in_28764(val);
    const int x = 28764;
    std::cout << 28764;
    for (int i = 0; i < 28764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28764(int& val) {
    const int x = 28764;
    std::cout << 28764;
    for (int i = 0; i < 28764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28765(int& val) {
    func_in_28765(val);
    const int x = 28765;
    std::cout << 28765;
    for (int i = 0; i < 28765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28765(int& val) {
    const int x = 28765;
    std::cout << 28765;
    for (int i = 0; i < 28765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28766(int& val) {
    func_in_28766(val);
    const int x = 28766;
    std::cout << 28766;
    for (int i = 0; i < 28766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28766(int& val) {
    const int x = 28766;
    std::cout << 28766;
    for (int i = 0; i < 28766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28767(int& val) {
    func_in_28767(val);
    const int x = 28767;
    std::cout << 28767;
    for (int i = 0; i < 28767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28767(int& val) {
    const int x = 28767;
    std::cout << 28767;
    for (int i = 0; i < 28767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28768(int& val) {
    func_in_28768(val);
    const int x = 28768;
    std::cout << 28768;
    for (int i = 0; i < 28768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28768(int& val) {
    const int x = 28768;
    std::cout << 28768;
    for (int i = 0; i < 28768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28769(int& val) {
    func_in_28769(val);
    const int x = 28769;
    std::cout << 28769;
    for (int i = 0; i < 28769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28769(int& val) {
    const int x = 28769;
    std::cout << 28769;
    for (int i = 0; i < 28769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28770(int& val) {
    func_in_28770(val);
    const int x = 28770;
    std::cout << 28770;
    for (int i = 0; i < 28770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28770(int& val) {
    const int x = 28770;
    std::cout << 28770;
    for (int i = 0; i < 28770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28771(int& val) {
    func_in_28771(val);
    const int x = 28771;
    std::cout << 28771;
    for (int i = 0; i < 28771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28771(int& val) {
    const int x = 28771;
    std::cout << 28771;
    for (int i = 0; i < 28771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28772(int& val) {
    func_in_28772(val);
    const int x = 28772;
    std::cout << 28772;
    for (int i = 0; i < 28772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28772(int& val) {
    const int x = 28772;
    std::cout << 28772;
    for (int i = 0; i < 28772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28773(int& val) {
    func_in_28773(val);
    const int x = 28773;
    std::cout << 28773;
    for (int i = 0; i < 28773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28773(int& val) {
    const int x = 28773;
    std::cout << 28773;
    for (int i = 0; i < 28773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28774(int& val) {
    func_in_28774(val);
    const int x = 28774;
    std::cout << 28774;
    for (int i = 0; i < 28774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28774(int& val) {
    const int x = 28774;
    std::cout << 28774;
    for (int i = 0; i < 28774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28775(int& val) {
    func_in_28775(val);
    const int x = 28775;
    std::cout << 28775;
    for (int i = 0; i < 28775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28775(int& val) {
    const int x = 28775;
    std::cout << 28775;
    for (int i = 0; i < 28775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28776(int& val) {
    func_in_28776(val);
    const int x = 28776;
    std::cout << 28776;
    for (int i = 0; i < 28776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28776(int& val) {
    const int x = 28776;
    std::cout << 28776;
    for (int i = 0; i < 28776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28777(int& val) {
    func_in_28777(val);
    const int x = 28777;
    std::cout << 28777;
    for (int i = 0; i < 28777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28777(int& val) {
    const int x = 28777;
    std::cout << 28777;
    for (int i = 0; i < 28777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28778(int& val) {
    func_in_28778(val);
    const int x = 28778;
    std::cout << 28778;
    for (int i = 0; i < 28778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28778(int& val) {
    const int x = 28778;
    std::cout << 28778;
    for (int i = 0; i < 28778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28779(int& val) {
    func_in_28779(val);
    const int x = 28779;
    std::cout << 28779;
    for (int i = 0; i < 28779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28779(int& val) {
    const int x = 28779;
    std::cout << 28779;
    for (int i = 0; i < 28779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28780(int& val) {
    func_in_28780(val);
    const int x = 28780;
    std::cout << 28780;
    for (int i = 0; i < 28780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28780(int& val) {
    const int x = 28780;
    std::cout << 28780;
    for (int i = 0; i < 28780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28781(int& val) {
    func_in_28781(val);
    const int x = 28781;
    std::cout << 28781;
    for (int i = 0; i < 28781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28781(int& val) {
    const int x = 28781;
    std::cout << 28781;
    for (int i = 0; i < 28781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28782(int& val) {
    func_in_28782(val);
    const int x = 28782;
    std::cout << 28782;
    for (int i = 0; i < 28782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28782(int& val) {
    const int x = 28782;
    std::cout << 28782;
    for (int i = 0; i < 28782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28783(int& val) {
    func_in_28783(val);
    const int x = 28783;
    std::cout << 28783;
    for (int i = 0; i < 28783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28783(int& val) {
    const int x = 28783;
    std::cout << 28783;
    for (int i = 0; i < 28783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28784(int& val) {
    func_in_28784(val);
    const int x = 28784;
    std::cout << 28784;
    for (int i = 0; i < 28784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28784(int& val) {
    const int x = 28784;
    std::cout << 28784;
    for (int i = 0; i < 28784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28785(int& val) {
    func_in_28785(val);
    const int x = 28785;
    std::cout << 28785;
    for (int i = 0; i < 28785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28785(int& val) {
    const int x = 28785;
    std::cout << 28785;
    for (int i = 0; i < 28785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28786(int& val) {
    func_in_28786(val);
    const int x = 28786;
    std::cout << 28786;
    for (int i = 0; i < 28786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28786(int& val) {
    const int x = 28786;
    std::cout << 28786;
    for (int i = 0; i < 28786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28787(int& val) {
    func_in_28787(val);
    const int x = 28787;
    std::cout << 28787;
    for (int i = 0; i < 28787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28787(int& val) {
    const int x = 28787;
    std::cout << 28787;
    for (int i = 0; i < 28787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28788(int& val) {
    func_in_28788(val);
    const int x = 28788;
    std::cout << 28788;
    for (int i = 0; i < 28788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28788(int& val) {
    const int x = 28788;
    std::cout << 28788;
    for (int i = 0; i < 28788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28789(int& val) {
    func_in_28789(val);
    const int x = 28789;
    std::cout << 28789;
    for (int i = 0; i < 28789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28789(int& val) {
    const int x = 28789;
    std::cout << 28789;
    for (int i = 0; i < 28789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28790(int& val) {
    func_in_28790(val);
    const int x = 28790;
    std::cout << 28790;
    for (int i = 0; i < 28790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28790(int& val) {
    const int x = 28790;
    std::cout << 28790;
    for (int i = 0; i < 28790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28791(int& val) {
    func_in_28791(val);
    const int x = 28791;
    std::cout << 28791;
    for (int i = 0; i < 28791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28791(int& val) {
    const int x = 28791;
    std::cout << 28791;
    for (int i = 0; i < 28791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28792(int& val) {
    func_in_28792(val);
    const int x = 28792;
    std::cout << 28792;
    for (int i = 0; i < 28792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28792(int& val) {
    const int x = 28792;
    std::cout << 28792;
    for (int i = 0; i < 28792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28793(int& val) {
    func_in_28793(val);
    const int x = 28793;
    std::cout << 28793;
    for (int i = 0; i < 28793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28793(int& val) {
    const int x = 28793;
    std::cout << 28793;
    for (int i = 0; i < 28793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28794(int& val) {
    func_in_28794(val);
    const int x = 28794;
    std::cout << 28794;
    for (int i = 0; i < 28794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28794(int& val) {
    const int x = 28794;
    std::cout << 28794;
    for (int i = 0; i < 28794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28795(int& val) {
    func_in_28795(val);
    const int x = 28795;
    std::cout << 28795;
    for (int i = 0; i < 28795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28795(int& val) {
    const int x = 28795;
    std::cout << 28795;
    for (int i = 0; i < 28795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28796(int& val) {
    func_in_28796(val);
    const int x = 28796;
    std::cout << 28796;
    for (int i = 0; i < 28796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28796(int& val) {
    const int x = 28796;
    std::cout << 28796;
    for (int i = 0; i < 28796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28797(int& val) {
    func_in_28797(val);
    const int x = 28797;
    std::cout << 28797;
    for (int i = 0; i < 28797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28797(int& val) {
    const int x = 28797;
    std::cout << 28797;
    for (int i = 0; i < 28797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28798(int& val) {
    func_in_28798(val);
    const int x = 28798;
    std::cout << 28798;
    for (int i = 0; i < 28798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28798(int& val) {
    const int x = 28798;
    std::cout << 28798;
    for (int i = 0; i < 28798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28799(int& val) {
    func_in_28799(val);
    const int x = 28799;
    std::cout << 28799;
    for (int i = 0; i < 28799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28799(int& val) {
    const int x = 28799;
    std::cout << 28799;
    for (int i = 0; i < 28799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28800(int& val) {
    func_in_28800(val);
    const int x = 28800;
    std::cout << 28800;
    for (int i = 0; i < 28800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28800(int& val) {
    const int x = 28800;
    std::cout << 28800;
    for (int i = 0; i < 28800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28801(int& val) {
    func_in_28801(val);
    const int x = 28801;
    std::cout << 28801;
    for (int i = 0; i < 28801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28801(int& val) {
    const int x = 28801;
    std::cout << 28801;
    for (int i = 0; i < 28801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28802(int& val) {
    func_in_28802(val);
    const int x = 28802;
    std::cout << 28802;
    for (int i = 0; i < 28802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28802(int& val) {
    const int x = 28802;
    std::cout << 28802;
    for (int i = 0; i < 28802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28803(int& val) {
    func_in_28803(val);
    const int x = 28803;
    std::cout << 28803;
    for (int i = 0; i < 28803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28803(int& val) {
    const int x = 28803;
    std::cout << 28803;
    for (int i = 0; i < 28803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28804(int& val) {
    func_in_28804(val);
    const int x = 28804;
    std::cout << 28804;
    for (int i = 0; i < 28804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28804(int& val) {
    const int x = 28804;
    std::cout << 28804;
    for (int i = 0; i < 28804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28805(int& val) {
    func_in_28805(val);
    const int x = 28805;
    std::cout << 28805;
    for (int i = 0; i < 28805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28805(int& val) {
    const int x = 28805;
    std::cout << 28805;
    for (int i = 0; i < 28805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28806(int& val) {
    func_in_28806(val);
    const int x = 28806;
    std::cout << 28806;
    for (int i = 0; i < 28806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28806(int& val) {
    const int x = 28806;
    std::cout << 28806;
    for (int i = 0; i < 28806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28807(int& val) {
    func_in_28807(val);
    const int x = 28807;
    std::cout << 28807;
    for (int i = 0; i < 28807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28807(int& val) {
    const int x = 28807;
    std::cout << 28807;
    for (int i = 0; i < 28807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28808(int& val) {
    func_in_28808(val);
    const int x = 28808;
    std::cout << 28808;
    for (int i = 0; i < 28808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28808(int& val) {
    const int x = 28808;
    std::cout << 28808;
    for (int i = 0; i < 28808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28809(int& val) {
    func_in_28809(val);
    const int x = 28809;
    std::cout << 28809;
    for (int i = 0; i < 28809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28809(int& val) {
    const int x = 28809;
    std::cout << 28809;
    for (int i = 0; i < 28809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28810(int& val) {
    func_in_28810(val);
    const int x = 28810;
    std::cout << 28810;
    for (int i = 0; i < 28810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28810(int& val) {
    const int x = 28810;
    std::cout << 28810;
    for (int i = 0; i < 28810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28811(int& val) {
    func_in_28811(val);
    const int x = 28811;
    std::cout << 28811;
    for (int i = 0; i < 28811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28811(int& val) {
    const int x = 28811;
    std::cout << 28811;
    for (int i = 0; i < 28811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28812(int& val) {
    func_in_28812(val);
    const int x = 28812;
    std::cout << 28812;
    for (int i = 0; i < 28812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28812(int& val) {
    const int x = 28812;
    std::cout << 28812;
    for (int i = 0; i < 28812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28813(int& val) {
    func_in_28813(val);
    const int x = 28813;
    std::cout << 28813;
    for (int i = 0; i < 28813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28813(int& val) {
    const int x = 28813;
    std::cout << 28813;
    for (int i = 0; i < 28813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28814(int& val) {
    func_in_28814(val);
    const int x = 28814;
    std::cout << 28814;
    for (int i = 0; i < 28814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28814(int& val) {
    const int x = 28814;
    std::cout << 28814;
    for (int i = 0; i < 28814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28815(int& val) {
    func_in_28815(val);
    const int x = 28815;
    std::cout << 28815;
    for (int i = 0; i < 28815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28815(int& val) {
    const int x = 28815;
    std::cout << 28815;
    for (int i = 0; i < 28815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28816(int& val) {
    func_in_28816(val);
    const int x = 28816;
    std::cout << 28816;
    for (int i = 0; i < 28816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28816(int& val) {
    const int x = 28816;
    std::cout << 28816;
    for (int i = 0; i < 28816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28817(int& val) {
    func_in_28817(val);
    const int x = 28817;
    std::cout << 28817;
    for (int i = 0; i < 28817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28817(int& val) {
    const int x = 28817;
    std::cout << 28817;
    for (int i = 0; i < 28817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28818(int& val) {
    func_in_28818(val);
    const int x = 28818;
    std::cout << 28818;
    for (int i = 0; i < 28818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28818(int& val) {
    const int x = 28818;
    std::cout << 28818;
    for (int i = 0; i < 28818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28819(int& val) {
    func_in_28819(val);
    const int x = 28819;
    std::cout << 28819;
    for (int i = 0; i < 28819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28819(int& val) {
    const int x = 28819;
    std::cout << 28819;
    for (int i = 0; i < 28819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28820(int& val) {
    func_in_28820(val);
    const int x = 28820;
    std::cout << 28820;
    for (int i = 0; i < 28820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28820(int& val) {
    const int x = 28820;
    std::cout << 28820;
    for (int i = 0; i < 28820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28821(int& val) {
    func_in_28821(val);
    const int x = 28821;
    std::cout << 28821;
    for (int i = 0; i < 28821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28821(int& val) {
    const int x = 28821;
    std::cout << 28821;
    for (int i = 0; i < 28821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28822(int& val) {
    func_in_28822(val);
    const int x = 28822;
    std::cout << 28822;
    for (int i = 0; i < 28822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28822(int& val) {
    const int x = 28822;
    std::cout << 28822;
    for (int i = 0; i < 28822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28823(int& val) {
    func_in_28823(val);
    const int x = 28823;
    std::cout << 28823;
    for (int i = 0; i < 28823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28823(int& val) {
    const int x = 28823;
    std::cout << 28823;
    for (int i = 0; i < 28823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28824(int& val) {
    func_in_28824(val);
    const int x = 28824;
    std::cout << 28824;
    for (int i = 0; i < 28824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28824(int& val) {
    const int x = 28824;
    std::cout << 28824;
    for (int i = 0; i < 28824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28825(int& val) {
    func_in_28825(val);
    const int x = 28825;
    std::cout << 28825;
    for (int i = 0; i < 28825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28825(int& val) {
    const int x = 28825;
    std::cout << 28825;
    for (int i = 0; i < 28825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28826(int& val) {
    func_in_28826(val);
    const int x = 28826;
    std::cout << 28826;
    for (int i = 0; i < 28826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28826(int& val) {
    const int x = 28826;
    std::cout << 28826;
    for (int i = 0; i < 28826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28827(int& val) {
    func_in_28827(val);
    const int x = 28827;
    std::cout << 28827;
    for (int i = 0; i < 28827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28827(int& val) {
    const int x = 28827;
    std::cout << 28827;
    for (int i = 0; i < 28827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28828(int& val) {
    func_in_28828(val);
    const int x = 28828;
    std::cout << 28828;
    for (int i = 0; i < 28828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28828(int& val) {
    const int x = 28828;
    std::cout << 28828;
    for (int i = 0; i < 28828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28829(int& val) {
    func_in_28829(val);
    const int x = 28829;
    std::cout << 28829;
    for (int i = 0; i < 28829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28829(int& val) {
    const int x = 28829;
    std::cout << 28829;
    for (int i = 0; i < 28829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28830(int& val) {
    func_in_28830(val);
    const int x = 28830;
    std::cout << 28830;
    for (int i = 0; i < 28830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28830(int& val) {
    const int x = 28830;
    std::cout << 28830;
    for (int i = 0; i < 28830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28831(int& val) {
    func_in_28831(val);
    const int x = 28831;
    std::cout << 28831;
    for (int i = 0; i < 28831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28831(int& val) {
    const int x = 28831;
    std::cout << 28831;
    for (int i = 0; i < 28831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28832(int& val) {
    func_in_28832(val);
    const int x = 28832;
    std::cout << 28832;
    for (int i = 0; i < 28832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28832(int& val) {
    const int x = 28832;
    std::cout << 28832;
    for (int i = 0; i < 28832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28833(int& val) {
    func_in_28833(val);
    const int x = 28833;
    std::cout << 28833;
    for (int i = 0; i < 28833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28833(int& val) {
    const int x = 28833;
    std::cout << 28833;
    for (int i = 0; i < 28833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28834(int& val) {
    func_in_28834(val);
    const int x = 28834;
    std::cout << 28834;
    for (int i = 0; i < 28834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28834(int& val) {
    const int x = 28834;
    std::cout << 28834;
    for (int i = 0; i < 28834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28835(int& val) {
    func_in_28835(val);
    const int x = 28835;
    std::cout << 28835;
    for (int i = 0; i < 28835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28835(int& val) {
    const int x = 28835;
    std::cout << 28835;
    for (int i = 0; i < 28835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28836(int& val) {
    func_in_28836(val);
    const int x = 28836;
    std::cout << 28836;
    for (int i = 0; i < 28836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28836(int& val) {
    const int x = 28836;
    std::cout << 28836;
    for (int i = 0; i < 28836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28837(int& val) {
    func_in_28837(val);
    const int x = 28837;
    std::cout << 28837;
    for (int i = 0; i < 28837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28837(int& val) {
    const int x = 28837;
    std::cout << 28837;
    for (int i = 0; i < 28837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28838(int& val) {
    func_in_28838(val);
    const int x = 28838;
    std::cout << 28838;
    for (int i = 0; i < 28838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28838(int& val) {
    const int x = 28838;
    std::cout << 28838;
    for (int i = 0; i < 28838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28839(int& val) {
    func_in_28839(val);
    const int x = 28839;
    std::cout << 28839;
    for (int i = 0; i < 28839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28839(int& val) {
    const int x = 28839;
    std::cout << 28839;
    for (int i = 0; i < 28839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28840(int& val) {
    func_in_28840(val);
    const int x = 28840;
    std::cout << 28840;
    for (int i = 0; i < 28840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28840(int& val) {
    const int x = 28840;
    std::cout << 28840;
    for (int i = 0; i < 28840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28841(int& val) {
    func_in_28841(val);
    const int x = 28841;
    std::cout << 28841;
    for (int i = 0; i < 28841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28841(int& val) {
    const int x = 28841;
    std::cout << 28841;
    for (int i = 0; i < 28841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28842(int& val) {
    func_in_28842(val);
    const int x = 28842;
    std::cout << 28842;
    for (int i = 0; i < 28842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28842(int& val) {
    const int x = 28842;
    std::cout << 28842;
    for (int i = 0; i < 28842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28843(int& val) {
    func_in_28843(val);
    const int x = 28843;
    std::cout << 28843;
    for (int i = 0; i < 28843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28843(int& val) {
    const int x = 28843;
    std::cout << 28843;
    for (int i = 0; i < 28843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28844(int& val) {
    func_in_28844(val);
    const int x = 28844;
    std::cout << 28844;
    for (int i = 0; i < 28844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28844(int& val) {
    const int x = 28844;
    std::cout << 28844;
    for (int i = 0; i < 28844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28845(int& val) {
    func_in_28845(val);
    const int x = 28845;
    std::cout << 28845;
    for (int i = 0; i < 28845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28845(int& val) {
    const int x = 28845;
    std::cout << 28845;
    for (int i = 0; i < 28845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28846(int& val) {
    func_in_28846(val);
    const int x = 28846;
    std::cout << 28846;
    for (int i = 0; i < 28846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28846(int& val) {
    const int x = 28846;
    std::cout << 28846;
    for (int i = 0; i < 28846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28847(int& val) {
    func_in_28847(val);
    const int x = 28847;
    std::cout << 28847;
    for (int i = 0; i < 28847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28847(int& val) {
    const int x = 28847;
    std::cout << 28847;
    for (int i = 0; i < 28847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28848(int& val) {
    func_in_28848(val);
    const int x = 28848;
    std::cout << 28848;
    for (int i = 0; i < 28848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28848(int& val) {
    const int x = 28848;
    std::cout << 28848;
    for (int i = 0; i < 28848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28849(int& val) {
    func_in_28849(val);
    const int x = 28849;
    std::cout << 28849;
    for (int i = 0; i < 28849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28849(int& val) {
    const int x = 28849;
    std::cout << 28849;
    for (int i = 0; i < 28849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28850(int& val) {
    func_in_28850(val);
    const int x = 28850;
    std::cout << 28850;
    for (int i = 0; i < 28850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28850(int& val) {
    const int x = 28850;
    std::cout << 28850;
    for (int i = 0; i < 28850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28851(int& val) {
    func_in_28851(val);
    const int x = 28851;
    std::cout << 28851;
    for (int i = 0; i < 28851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28851(int& val) {
    const int x = 28851;
    std::cout << 28851;
    for (int i = 0; i < 28851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28852(int& val) {
    func_in_28852(val);
    const int x = 28852;
    std::cout << 28852;
    for (int i = 0; i < 28852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28852(int& val) {
    const int x = 28852;
    std::cout << 28852;
    for (int i = 0; i < 28852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28853(int& val) {
    func_in_28853(val);
    const int x = 28853;
    std::cout << 28853;
    for (int i = 0; i < 28853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28853(int& val) {
    const int x = 28853;
    std::cout << 28853;
    for (int i = 0; i < 28853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28854(int& val) {
    func_in_28854(val);
    const int x = 28854;
    std::cout << 28854;
    for (int i = 0; i < 28854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28854(int& val) {
    const int x = 28854;
    std::cout << 28854;
    for (int i = 0; i < 28854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28855(int& val) {
    func_in_28855(val);
    const int x = 28855;
    std::cout << 28855;
    for (int i = 0; i < 28855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28855(int& val) {
    const int x = 28855;
    std::cout << 28855;
    for (int i = 0; i < 28855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28856(int& val) {
    func_in_28856(val);
    const int x = 28856;
    std::cout << 28856;
    for (int i = 0; i < 28856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28856(int& val) {
    const int x = 28856;
    std::cout << 28856;
    for (int i = 0; i < 28856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28857(int& val) {
    func_in_28857(val);
    const int x = 28857;
    std::cout << 28857;
    for (int i = 0; i < 28857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28857(int& val) {
    const int x = 28857;
    std::cout << 28857;
    for (int i = 0; i < 28857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28858(int& val) {
    func_in_28858(val);
    const int x = 28858;
    std::cout << 28858;
    for (int i = 0; i < 28858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28858(int& val) {
    const int x = 28858;
    std::cout << 28858;
    for (int i = 0; i < 28858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28859(int& val) {
    func_in_28859(val);
    const int x = 28859;
    std::cout << 28859;
    for (int i = 0; i < 28859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28859(int& val) {
    const int x = 28859;
    std::cout << 28859;
    for (int i = 0; i < 28859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28860(int& val) {
    func_in_28860(val);
    const int x = 28860;
    std::cout << 28860;
    for (int i = 0; i < 28860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28860(int& val) {
    const int x = 28860;
    std::cout << 28860;
    for (int i = 0; i < 28860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28861(int& val) {
    func_in_28861(val);
    const int x = 28861;
    std::cout << 28861;
    for (int i = 0; i < 28861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28861(int& val) {
    const int x = 28861;
    std::cout << 28861;
    for (int i = 0; i < 28861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28862(int& val) {
    func_in_28862(val);
    const int x = 28862;
    std::cout << 28862;
    for (int i = 0; i < 28862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28862(int& val) {
    const int x = 28862;
    std::cout << 28862;
    for (int i = 0; i < 28862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28863(int& val) {
    func_in_28863(val);
    const int x = 28863;
    std::cout << 28863;
    for (int i = 0; i < 28863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28863(int& val) {
    const int x = 28863;
    std::cout << 28863;
    for (int i = 0; i < 28863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28864(int& val) {
    func_in_28864(val);
    const int x = 28864;
    std::cout << 28864;
    for (int i = 0; i < 28864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28864(int& val) {
    const int x = 28864;
    std::cout << 28864;
    for (int i = 0; i < 28864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28865(int& val) {
    func_in_28865(val);
    const int x = 28865;
    std::cout << 28865;
    for (int i = 0; i < 28865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28865(int& val) {
    const int x = 28865;
    std::cout << 28865;
    for (int i = 0; i < 28865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28866(int& val) {
    func_in_28866(val);
    const int x = 28866;
    std::cout << 28866;
    for (int i = 0; i < 28866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28866(int& val) {
    const int x = 28866;
    std::cout << 28866;
    for (int i = 0; i < 28866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28867(int& val) {
    func_in_28867(val);
    const int x = 28867;
    std::cout << 28867;
    for (int i = 0; i < 28867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28867(int& val) {
    const int x = 28867;
    std::cout << 28867;
    for (int i = 0; i < 28867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28868(int& val) {
    func_in_28868(val);
    const int x = 28868;
    std::cout << 28868;
    for (int i = 0; i < 28868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28868(int& val) {
    const int x = 28868;
    std::cout << 28868;
    for (int i = 0; i < 28868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28869(int& val) {
    func_in_28869(val);
    const int x = 28869;
    std::cout << 28869;
    for (int i = 0; i < 28869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28869(int& val) {
    const int x = 28869;
    std::cout << 28869;
    for (int i = 0; i < 28869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28870(int& val) {
    func_in_28870(val);
    const int x = 28870;
    std::cout << 28870;
    for (int i = 0; i < 28870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28870(int& val) {
    const int x = 28870;
    std::cout << 28870;
    for (int i = 0; i < 28870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28871(int& val) {
    func_in_28871(val);
    const int x = 28871;
    std::cout << 28871;
    for (int i = 0; i < 28871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28871(int& val) {
    const int x = 28871;
    std::cout << 28871;
    for (int i = 0; i < 28871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28872(int& val) {
    func_in_28872(val);
    const int x = 28872;
    std::cout << 28872;
    for (int i = 0; i < 28872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28872(int& val) {
    const int x = 28872;
    std::cout << 28872;
    for (int i = 0; i < 28872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28873(int& val) {
    func_in_28873(val);
    const int x = 28873;
    std::cout << 28873;
    for (int i = 0; i < 28873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28873(int& val) {
    const int x = 28873;
    std::cout << 28873;
    for (int i = 0; i < 28873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28874(int& val) {
    func_in_28874(val);
    const int x = 28874;
    std::cout << 28874;
    for (int i = 0; i < 28874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28874(int& val) {
    const int x = 28874;
    std::cout << 28874;
    for (int i = 0; i < 28874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28875(int& val) {
    func_in_28875(val);
    const int x = 28875;
    std::cout << 28875;
    for (int i = 0; i < 28875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28875(int& val) {
    const int x = 28875;
    std::cout << 28875;
    for (int i = 0; i < 28875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28876(int& val) {
    func_in_28876(val);
    const int x = 28876;
    std::cout << 28876;
    for (int i = 0; i < 28876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28876(int& val) {
    const int x = 28876;
    std::cout << 28876;
    for (int i = 0; i < 28876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28877(int& val) {
    func_in_28877(val);
    const int x = 28877;
    std::cout << 28877;
    for (int i = 0; i < 28877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28877(int& val) {
    const int x = 28877;
    std::cout << 28877;
    for (int i = 0; i < 28877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28878(int& val) {
    func_in_28878(val);
    const int x = 28878;
    std::cout << 28878;
    for (int i = 0; i < 28878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28878(int& val) {
    const int x = 28878;
    std::cout << 28878;
    for (int i = 0; i < 28878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28879(int& val) {
    func_in_28879(val);
    const int x = 28879;
    std::cout << 28879;
    for (int i = 0; i < 28879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28879(int& val) {
    const int x = 28879;
    std::cout << 28879;
    for (int i = 0; i < 28879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28880(int& val) {
    func_in_28880(val);
    const int x = 28880;
    std::cout << 28880;
    for (int i = 0; i < 28880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28880(int& val) {
    const int x = 28880;
    std::cout << 28880;
    for (int i = 0; i < 28880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28881(int& val) {
    func_in_28881(val);
    const int x = 28881;
    std::cout << 28881;
    for (int i = 0; i < 28881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28881(int& val) {
    const int x = 28881;
    std::cout << 28881;
    for (int i = 0; i < 28881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28882(int& val) {
    func_in_28882(val);
    const int x = 28882;
    std::cout << 28882;
    for (int i = 0; i < 28882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28882(int& val) {
    const int x = 28882;
    std::cout << 28882;
    for (int i = 0; i < 28882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28883(int& val) {
    func_in_28883(val);
    const int x = 28883;
    std::cout << 28883;
    for (int i = 0; i < 28883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28883(int& val) {
    const int x = 28883;
    std::cout << 28883;
    for (int i = 0; i < 28883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28884(int& val) {
    func_in_28884(val);
    const int x = 28884;
    std::cout << 28884;
    for (int i = 0; i < 28884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28884(int& val) {
    const int x = 28884;
    std::cout << 28884;
    for (int i = 0; i < 28884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28885(int& val) {
    func_in_28885(val);
    const int x = 28885;
    std::cout << 28885;
    for (int i = 0; i < 28885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28885(int& val) {
    const int x = 28885;
    std::cout << 28885;
    for (int i = 0; i < 28885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28886(int& val) {
    func_in_28886(val);
    const int x = 28886;
    std::cout << 28886;
    for (int i = 0; i < 28886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28886(int& val) {
    const int x = 28886;
    std::cout << 28886;
    for (int i = 0; i < 28886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28887(int& val) {
    func_in_28887(val);
    const int x = 28887;
    std::cout << 28887;
    for (int i = 0; i < 28887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28887(int& val) {
    const int x = 28887;
    std::cout << 28887;
    for (int i = 0; i < 28887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28888(int& val) {
    func_in_28888(val);
    const int x = 28888;
    std::cout << 28888;
    for (int i = 0; i < 28888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28888(int& val) {
    const int x = 28888;
    std::cout << 28888;
    for (int i = 0; i < 28888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28889(int& val) {
    func_in_28889(val);
    const int x = 28889;
    std::cout << 28889;
    for (int i = 0; i < 28889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28889(int& val) {
    const int x = 28889;
    std::cout << 28889;
    for (int i = 0; i < 28889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28890(int& val) {
    func_in_28890(val);
    const int x = 28890;
    std::cout << 28890;
    for (int i = 0; i < 28890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28890(int& val) {
    const int x = 28890;
    std::cout << 28890;
    for (int i = 0; i < 28890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28891(int& val) {
    func_in_28891(val);
    const int x = 28891;
    std::cout << 28891;
    for (int i = 0; i < 28891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28891(int& val) {
    const int x = 28891;
    std::cout << 28891;
    for (int i = 0; i < 28891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28892(int& val) {
    func_in_28892(val);
    const int x = 28892;
    std::cout << 28892;
    for (int i = 0; i < 28892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28892(int& val) {
    const int x = 28892;
    std::cout << 28892;
    for (int i = 0; i < 28892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28893(int& val) {
    func_in_28893(val);
    const int x = 28893;
    std::cout << 28893;
    for (int i = 0; i < 28893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28893(int& val) {
    const int x = 28893;
    std::cout << 28893;
    for (int i = 0; i < 28893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28894(int& val) {
    func_in_28894(val);
    const int x = 28894;
    std::cout << 28894;
    for (int i = 0; i < 28894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28894(int& val) {
    const int x = 28894;
    std::cout << 28894;
    for (int i = 0; i < 28894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28895(int& val) {
    func_in_28895(val);
    const int x = 28895;
    std::cout << 28895;
    for (int i = 0; i < 28895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28895(int& val) {
    const int x = 28895;
    std::cout << 28895;
    for (int i = 0; i < 28895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28896(int& val) {
    func_in_28896(val);
    const int x = 28896;
    std::cout << 28896;
    for (int i = 0; i < 28896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28896(int& val) {
    const int x = 28896;
    std::cout << 28896;
    for (int i = 0; i < 28896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28897(int& val) {
    func_in_28897(val);
    const int x = 28897;
    std::cout << 28897;
    for (int i = 0; i < 28897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28897(int& val) {
    const int x = 28897;
    std::cout << 28897;
    for (int i = 0; i < 28897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28898(int& val) {
    func_in_28898(val);
    const int x = 28898;
    std::cout << 28898;
    for (int i = 0; i < 28898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28898(int& val) {
    const int x = 28898;
    std::cout << 28898;
    for (int i = 0; i < 28898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28899(int& val) {
    func_in_28899(val);
    const int x = 28899;
    std::cout << 28899;
    for (int i = 0; i < 28899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28899(int& val) {
    const int x = 28899;
    std::cout << 28899;
    for (int i = 0; i < 28899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28900(int& val) {
    func_in_28900(val);
    const int x = 28900;
    std::cout << 28900;
    for (int i = 0; i < 28900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28900(int& val) {
    const int x = 28900;
    std::cout << 28900;
    for (int i = 0; i < 28900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28901(int& val) {
    func_in_28901(val);
    const int x = 28901;
    std::cout << 28901;
    for (int i = 0; i < 28901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28901(int& val) {
    const int x = 28901;
    std::cout << 28901;
    for (int i = 0; i < 28901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28902(int& val) {
    func_in_28902(val);
    const int x = 28902;
    std::cout << 28902;
    for (int i = 0; i < 28902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28902(int& val) {
    const int x = 28902;
    std::cout << 28902;
    for (int i = 0; i < 28902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28903(int& val) {
    func_in_28903(val);
    const int x = 28903;
    std::cout << 28903;
    for (int i = 0; i < 28903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28903(int& val) {
    const int x = 28903;
    std::cout << 28903;
    for (int i = 0; i < 28903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28904(int& val) {
    func_in_28904(val);
    const int x = 28904;
    std::cout << 28904;
    for (int i = 0; i < 28904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28904(int& val) {
    const int x = 28904;
    std::cout << 28904;
    for (int i = 0; i < 28904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28905(int& val) {
    func_in_28905(val);
    const int x = 28905;
    std::cout << 28905;
    for (int i = 0; i < 28905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28905(int& val) {
    const int x = 28905;
    std::cout << 28905;
    for (int i = 0; i < 28905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28906(int& val) {
    func_in_28906(val);
    const int x = 28906;
    std::cout << 28906;
    for (int i = 0; i < 28906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28906(int& val) {
    const int x = 28906;
    std::cout << 28906;
    for (int i = 0; i < 28906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28907(int& val) {
    func_in_28907(val);
    const int x = 28907;
    std::cout << 28907;
    for (int i = 0; i < 28907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28907(int& val) {
    const int x = 28907;
    std::cout << 28907;
    for (int i = 0; i < 28907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28908(int& val) {
    func_in_28908(val);
    const int x = 28908;
    std::cout << 28908;
    for (int i = 0; i < 28908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28908(int& val) {
    const int x = 28908;
    std::cout << 28908;
    for (int i = 0; i < 28908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28909(int& val) {
    func_in_28909(val);
    const int x = 28909;
    std::cout << 28909;
    for (int i = 0; i < 28909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28909(int& val) {
    const int x = 28909;
    std::cout << 28909;
    for (int i = 0; i < 28909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28910(int& val) {
    func_in_28910(val);
    const int x = 28910;
    std::cout << 28910;
    for (int i = 0; i < 28910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28910(int& val) {
    const int x = 28910;
    std::cout << 28910;
    for (int i = 0; i < 28910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28911(int& val) {
    func_in_28911(val);
    const int x = 28911;
    std::cout << 28911;
    for (int i = 0; i < 28911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28911(int& val) {
    const int x = 28911;
    std::cout << 28911;
    for (int i = 0; i < 28911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28912(int& val) {
    func_in_28912(val);
    const int x = 28912;
    std::cout << 28912;
    for (int i = 0; i < 28912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28912(int& val) {
    const int x = 28912;
    std::cout << 28912;
    for (int i = 0; i < 28912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28913(int& val) {
    func_in_28913(val);
    const int x = 28913;
    std::cout << 28913;
    for (int i = 0; i < 28913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28913(int& val) {
    const int x = 28913;
    std::cout << 28913;
    for (int i = 0; i < 28913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28914(int& val) {
    func_in_28914(val);
    const int x = 28914;
    std::cout << 28914;
    for (int i = 0; i < 28914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28914(int& val) {
    const int x = 28914;
    std::cout << 28914;
    for (int i = 0; i < 28914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28915(int& val) {
    func_in_28915(val);
    const int x = 28915;
    std::cout << 28915;
    for (int i = 0; i < 28915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28915(int& val) {
    const int x = 28915;
    std::cout << 28915;
    for (int i = 0; i < 28915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28916(int& val) {
    func_in_28916(val);
    const int x = 28916;
    std::cout << 28916;
    for (int i = 0; i < 28916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28916(int& val) {
    const int x = 28916;
    std::cout << 28916;
    for (int i = 0; i < 28916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28917(int& val) {
    func_in_28917(val);
    const int x = 28917;
    std::cout << 28917;
    for (int i = 0; i < 28917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28917(int& val) {
    const int x = 28917;
    std::cout << 28917;
    for (int i = 0; i < 28917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28918(int& val) {
    func_in_28918(val);
    const int x = 28918;
    std::cout << 28918;
    for (int i = 0; i < 28918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28918(int& val) {
    const int x = 28918;
    std::cout << 28918;
    for (int i = 0; i < 28918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28919(int& val) {
    func_in_28919(val);
    const int x = 28919;
    std::cout << 28919;
    for (int i = 0; i < 28919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28919(int& val) {
    const int x = 28919;
    std::cout << 28919;
    for (int i = 0; i < 28919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28920(int& val) {
    func_in_28920(val);
    const int x = 28920;
    std::cout << 28920;
    for (int i = 0; i < 28920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28920(int& val) {
    const int x = 28920;
    std::cout << 28920;
    for (int i = 0; i < 28920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28921(int& val) {
    func_in_28921(val);
    const int x = 28921;
    std::cout << 28921;
    for (int i = 0; i < 28921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28921(int& val) {
    const int x = 28921;
    std::cout << 28921;
    for (int i = 0; i < 28921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28922(int& val) {
    func_in_28922(val);
    const int x = 28922;
    std::cout << 28922;
    for (int i = 0; i < 28922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28922(int& val) {
    const int x = 28922;
    std::cout << 28922;
    for (int i = 0; i < 28922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28923(int& val) {
    func_in_28923(val);
    const int x = 28923;
    std::cout << 28923;
    for (int i = 0; i < 28923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28923(int& val) {
    const int x = 28923;
    std::cout << 28923;
    for (int i = 0; i < 28923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28924(int& val) {
    func_in_28924(val);
    const int x = 28924;
    std::cout << 28924;
    for (int i = 0; i < 28924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28924(int& val) {
    const int x = 28924;
    std::cout << 28924;
    for (int i = 0; i < 28924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28925(int& val) {
    func_in_28925(val);
    const int x = 28925;
    std::cout << 28925;
    for (int i = 0; i < 28925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28925(int& val) {
    const int x = 28925;
    std::cout << 28925;
    for (int i = 0; i < 28925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28926(int& val) {
    func_in_28926(val);
    const int x = 28926;
    std::cout << 28926;
    for (int i = 0; i < 28926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28926(int& val) {
    const int x = 28926;
    std::cout << 28926;
    for (int i = 0; i < 28926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28927(int& val) {
    func_in_28927(val);
    const int x = 28927;
    std::cout << 28927;
    for (int i = 0; i < 28927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28927(int& val) {
    const int x = 28927;
    std::cout << 28927;
    for (int i = 0; i < 28927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28928(int& val) {
    func_in_28928(val);
    const int x = 28928;
    std::cout << 28928;
    for (int i = 0; i < 28928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28928(int& val) {
    const int x = 28928;
    std::cout << 28928;
    for (int i = 0; i < 28928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28929(int& val) {
    func_in_28929(val);
    const int x = 28929;
    std::cout << 28929;
    for (int i = 0; i < 28929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28929(int& val) {
    const int x = 28929;
    std::cout << 28929;
    for (int i = 0; i < 28929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28930(int& val) {
    func_in_28930(val);
    const int x = 28930;
    std::cout << 28930;
    for (int i = 0; i < 28930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28930(int& val) {
    const int x = 28930;
    std::cout << 28930;
    for (int i = 0; i < 28930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28931(int& val) {
    func_in_28931(val);
    const int x = 28931;
    std::cout << 28931;
    for (int i = 0; i < 28931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28931(int& val) {
    const int x = 28931;
    std::cout << 28931;
    for (int i = 0; i < 28931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28932(int& val) {
    func_in_28932(val);
    const int x = 28932;
    std::cout << 28932;
    for (int i = 0; i < 28932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28932(int& val) {
    const int x = 28932;
    std::cout << 28932;
    for (int i = 0; i < 28932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28933(int& val) {
    func_in_28933(val);
    const int x = 28933;
    std::cout << 28933;
    for (int i = 0; i < 28933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28933(int& val) {
    const int x = 28933;
    std::cout << 28933;
    for (int i = 0; i < 28933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28934(int& val) {
    func_in_28934(val);
    const int x = 28934;
    std::cout << 28934;
    for (int i = 0; i < 28934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28934(int& val) {
    const int x = 28934;
    std::cout << 28934;
    for (int i = 0; i < 28934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28935(int& val) {
    func_in_28935(val);
    const int x = 28935;
    std::cout << 28935;
    for (int i = 0; i < 28935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28935(int& val) {
    const int x = 28935;
    std::cout << 28935;
    for (int i = 0; i < 28935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28936(int& val) {
    func_in_28936(val);
    const int x = 28936;
    std::cout << 28936;
    for (int i = 0; i < 28936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28936(int& val) {
    const int x = 28936;
    std::cout << 28936;
    for (int i = 0; i < 28936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28937(int& val) {
    func_in_28937(val);
    const int x = 28937;
    std::cout << 28937;
    for (int i = 0; i < 28937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28937(int& val) {
    const int x = 28937;
    std::cout << 28937;
    for (int i = 0; i < 28937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28938(int& val) {
    func_in_28938(val);
    const int x = 28938;
    std::cout << 28938;
    for (int i = 0; i < 28938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28938(int& val) {
    const int x = 28938;
    std::cout << 28938;
    for (int i = 0; i < 28938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28939(int& val) {
    func_in_28939(val);
    const int x = 28939;
    std::cout << 28939;
    for (int i = 0; i < 28939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28939(int& val) {
    const int x = 28939;
    std::cout << 28939;
    for (int i = 0; i < 28939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28940(int& val) {
    func_in_28940(val);
    const int x = 28940;
    std::cout << 28940;
    for (int i = 0; i < 28940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28940(int& val) {
    const int x = 28940;
    std::cout << 28940;
    for (int i = 0; i < 28940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28941(int& val) {
    func_in_28941(val);
    const int x = 28941;
    std::cout << 28941;
    for (int i = 0; i < 28941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28941(int& val) {
    const int x = 28941;
    std::cout << 28941;
    for (int i = 0; i < 28941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28942(int& val) {
    func_in_28942(val);
    const int x = 28942;
    std::cout << 28942;
    for (int i = 0; i < 28942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28942(int& val) {
    const int x = 28942;
    std::cout << 28942;
    for (int i = 0; i < 28942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28943(int& val) {
    func_in_28943(val);
    const int x = 28943;
    std::cout << 28943;
    for (int i = 0; i < 28943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28943(int& val) {
    const int x = 28943;
    std::cout << 28943;
    for (int i = 0; i < 28943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28944(int& val) {
    func_in_28944(val);
    const int x = 28944;
    std::cout << 28944;
    for (int i = 0; i < 28944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28944(int& val) {
    const int x = 28944;
    std::cout << 28944;
    for (int i = 0; i < 28944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28945(int& val) {
    func_in_28945(val);
    const int x = 28945;
    std::cout << 28945;
    for (int i = 0; i < 28945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28945(int& val) {
    const int x = 28945;
    std::cout << 28945;
    for (int i = 0; i < 28945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28946(int& val) {
    func_in_28946(val);
    const int x = 28946;
    std::cout << 28946;
    for (int i = 0; i < 28946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28946(int& val) {
    const int x = 28946;
    std::cout << 28946;
    for (int i = 0; i < 28946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28947(int& val) {
    func_in_28947(val);
    const int x = 28947;
    std::cout << 28947;
    for (int i = 0; i < 28947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28947(int& val) {
    const int x = 28947;
    std::cout << 28947;
    for (int i = 0; i < 28947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28948(int& val) {
    func_in_28948(val);
    const int x = 28948;
    std::cout << 28948;
    for (int i = 0; i < 28948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28948(int& val) {
    const int x = 28948;
    std::cout << 28948;
    for (int i = 0; i < 28948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28949(int& val) {
    func_in_28949(val);
    const int x = 28949;
    std::cout << 28949;
    for (int i = 0; i < 28949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28949(int& val) {
    const int x = 28949;
    std::cout << 28949;
    for (int i = 0; i < 28949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28950(int& val) {
    func_in_28950(val);
    const int x = 28950;
    std::cout << 28950;
    for (int i = 0; i < 28950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28950(int& val) {
    const int x = 28950;
    std::cout << 28950;
    for (int i = 0; i < 28950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28951(int& val) {
    func_in_28951(val);
    const int x = 28951;
    std::cout << 28951;
    for (int i = 0; i < 28951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28951(int& val) {
    const int x = 28951;
    std::cout << 28951;
    for (int i = 0; i < 28951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28952(int& val) {
    func_in_28952(val);
    const int x = 28952;
    std::cout << 28952;
    for (int i = 0; i < 28952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28952(int& val) {
    const int x = 28952;
    std::cout << 28952;
    for (int i = 0; i < 28952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28953(int& val) {
    func_in_28953(val);
    const int x = 28953;
    std::cout << 28953;
    for (int i = 0; i < 28953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28953(int& val) {
    const int x = 28953;
    std::cout << 28953;
    for (int i = 0; i < 28953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28954(int& val) {
    func_in_28954(val);
    const int x = 28954;
    std::cout << 28954;
    for (int i = 0; i < 28954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28954(int& val) {
    const int x = 28954;
    std::cout << 28954;
    for (int i = 0; i < 28954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28955(int& val) {
    func_in_28955(val);
    const int x = 28955;
    std::cout << 28955;
    for (int i = 0; i < 28955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28955(int& val) {
    const int x = 28955;
    std::cout << 28955;
    for (int i = 0; i < 28955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28956(int& val) {
    func_in_28956(val);
    const int x = 28956;
    std::cout << 28956;
    for (int i = 0; i < 28956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28956(int& val) {
    const int x = 28956;
    std::cout << 28956;
    for (int i = 0; i < 28956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28957(int& val) {
    func_in_28957(val);
    const int x = 28957;
    std::cout << 28957;
    for (int i = 0; i < 28957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28957(int& val) {
    const int x = 28957;
    std::cout << 28957;
    for (int i = 0; i < 28957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28958(int& val) {
    func_in_28958(val);
    const int x = 28958;
    std::cout << 28958;
    for (int i = 0; i < 28958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28958(int& val) {
    const int x = 28958;
    std::cout << 28958;
    for (int i = 0; i < 28958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28959(int& val) {
    func_in_28959(val);
    const int x = 28959;
    std::cout << 28959;
    for (int i = 0; i < 28959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28959(int& val) {
    const int x = 28959;
    std::cout << 28959;
    for (int i = 0; i < 28959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28960(int& val) {
    func_in_28960(val);
    const int x = 28960;
    std::cout << 28960;
    for (int i = 0; i < 28960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28960(int& val) {
    const int x = 28960;
    std::cout << 28960;
    for (int i = 0; i < 28960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28961(int& val) {
    func_in_28961(val);
    const int x = 28961;
    std::cout << 28961;
    for (int i = 0; i < 28961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28961(int& val) {
    const int x = 28961;
    std::cout << 28961;
    for (int i = 0; i < 28961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28962(int& val) {
    func_in_28962(val);
    const int x = 28962;
    std::cout << 28962;
    for (int i = 0; i < 28962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28962(int& val) {
    const int x = 28962;
    std::cout << 28962;
    for (int i = 0; i < 28962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28963(int& val) {
    func_in_28963(val);
    const int x = 28963;
    std::cout << 28963;
    for (int i = 0; i < 28963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28963(int& val) {
    const int x = 28963;
    std::cout << 28963;
    for (int i = 0; i < 28963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28964(int& val) {
    func_in_28964(val);
    const int x = 28964;
    std::cout << 28964;
    for (int i = 0; i < 28964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28964(int& val) {
    const int x = 28964;
    std::cout << 28964;
    for (int i = 0; i < 28964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28965(int& val) {
    func_in_28965(val);
    const int x = 28965;
    std::cout << 28965;
    for (int i = 0; i < 28965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28965(int& val) {
    const int x = 28965;
    std::cout << 28965;
    for (int i = 0; i < 28965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28966(int& val) {
    func_in_28966(val);
    const int x = 28966;
    std::cout << 28966;
    for (int i = 0; i < 28966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28966(int& val) {
    const int x = 28966;
    std::cout << 28966;
    for (int i = 0; i < 28966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28967(int& val) {
    func_in_28967(val);
    const int x = 28967;
    std::cout << 28967;
    for (int i = 0; i < 28967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28967(int& val) {
    const int x = 28967;
    std::cout << 28967;
    for (int i = 0; i < 28967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28968(int& val) {
    func_in_28968(val);
    const int x = 28968;
    std::cout << 28968;
    for (int i = 0; i < 28968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28968(int& val) {
    const int x = 28968;
    std::cout << 28968;
    for (int i = 0; i < 28968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28969(int& val) {
    func_in_28969(val);
    const int x = 28969;
    std::cout << 28969;
    for (int i = 0; i < 28969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28969(int& val) {
    const int x = 28969;
    std::cout << 28969;
    for (int i = 0; i < 28969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28970(int& val) {
    func_in_28970(val);
    const int x = 28970;
    std::cout << 28970;
    for (int i = 0; i < 28970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28970(int& val) {
    const int x = 28970;
    std::cout << 28970;
    for (int i = 0; i < 28970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28971(int& val) {
    func_in_28971(val);
    const int x = 28971;
    std::cout << 28971;
    for (int i = 0; i < 28971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28971(int& val) {
    const int x = 28971;
    std::cout << 28971;
    for (int i = 0; i < 28971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28972(int& val) {
    func_in_28972(val);
    const int x = 28972;
    std::cout << 28972;
    for (int i = 0; i < 28972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28972(int& val) {
    const int x = 28972;
    std::cout << 28972;
    for (int i = 0; i < 28972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28973(int& val) {
    func_in_28973(val);
    const int x = 28973;
    std::cout << 28973;
    for (int i = 0; i < 28973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28973(int& val) {
    const int x = 28973;
    std::cout << 28973;
    for (int i = 0; i < 28973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28974(int& val) {
    func_in_28974(val);
    const int x = 28974;
    std::cout << 28974;
    for (int i = 0; i < 28974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28974(int& val) {
    const int x = 28974;
    std::cout << 28974;
    for (int i = 0; i < 28974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28975(int& val) {
    func_in_28975(val);
    const int x = 28975;
    std::cout << 28975;
    for (int i = 0; i < 28975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28975(int& val) {
    const int x = 28975;
    std::cout << 28975;
    for (int i = 0; i < 28975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28976(int& val) {
    func_in_28976(val);
    const int x = 28976;
    std::cout << 28976;
    for (int i = 0; i < 28976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28976(int& val) {
    const int x = 28976;
    std::cout << 28976;
    for (int i = 0; i < 28976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28977(int& val) {
    func_in_28977(val);
    const int x = 28977;
    std::cout << 28977;
    for (int i = 0; i < 28977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28977(int& val) {
    const int x = 28977;
    std::cout << 28977;
    for (int i = 0; i < 28977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28978(int& val) {
    func_in_28978(val);
    const int x = 28978;
    std::cout << 28978;
    for (int i = 0; i < 28978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28978(int& val) {
    const int x = 28978;
    std::cout << 28978;
    for (int i = 0; i < 28978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28979(int& val) {
    func_in_28979(val);
    const int x = 28979;
    std::cout << 28979;
    for (int i = 0; i < 28979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28979(int& val) {
    const int x = 28979;
    std::cout << 28979;
    for (int i = 0; i < 28979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28980(int& val) {
    func_in_28980(val);
    const int x = 28980;
    std::cout << 28980;
    for (int i = 0; i < 28980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28980(int& val) {
    const int x = 28980;
    std::cout << 28980;
    for (int i = 0; i < 28980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28981(int& val) {
    func_in_28981(val);
    const int x = 28981;
    std::cout << 28981;
    for (int i = 0; i < 28981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28981(int& val) {
    const int x = 28981;
    std::cout << 28981;
    for (int i = 0; i < 28981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28982(int& val) {
    func_in_28982(val);
    const int x = 28982;
    std::cout << 28982;
    for (int i = 0; i < 28982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28982(int& val) {
    const int x = 28982;
    std::cout << 28982;
    for (int i = 0; i < 28982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28983(int& val) {
    func_in_28983(val);
    const int x = 28983;
    std::cout << 28983;
    for (int i = 0; i < 28983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28983(int& val) {
    const int x = 28983;
    std::cout << 28983;
    for (int i = 0; i < 28983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28984(int& val) {
    func_in_28984(val);
    const int x = 28984;
    std::cout << 28984;
    for (int i = 0; i < 28984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28984(int& val) {
    const int x = 28984;
    std::cout << 28984;
    for (int i = 0; i < 28984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28985(int& val) {
    func_in_28985(val);
    const int x = 28985;
    std::cout << 28985;
    for (int i = 0; i < 28985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28985(int& val) {
    const int x = 28985;
    std::cout << 28985;
    for (int i = 0; i < 28985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28986(int& val) {
    func_in_28986(val);
    const int x = 28986;
    std::cout << 28986;
    for (int i = 0; i < 28986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28986(int& val) {
    const int x = 28986;
    std::cout << 28986;
    for (int i = 0; i < 28986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28987(int& val) {
    func_in_28987(val);
    const int x = 28987;
    std::cout << 28987;
    for (int i = 0; i < 28987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28987(int& val) {
    const int x = 28987;
    std::cout << 28987;
    for (int i = 0; i < 28987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28988(int& val) {
    func_in_28988(val);
    const int x = 28988;
    std::cout << 28988;
    for (int i = 0; i < 28988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28988(int& val) {
    const int x = 28988;
    std::cout << 28988;
    for (int i = 0; i < 28988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28989(int& val) {
    func_in_28989(val);
    const int x = 28989;
    std::cout << 28989;
    for (int i = 0; i < 28989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28989(int& val) {
    const int x = 28989;
    std::cout << 28989;
    for (int i = 0; i < 28989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28990(int& val) {
    func_in_28990(val);
    const int x = 28990;
    std::cout << 28990;
    for (int i = 0; i < 28990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28990(int& val) {
    const int x = 28990;
    std::cout << 28990;
    for (int i = 0; i < 28990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28991(int& val) {
    func_in_28991(val);
    const int x = 28991;
    std::cout << 28991;
    for (int i = 0; i < 28991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28991(int& val) {
    const int x = 28991;
    std::cout << 28991;
    for (int i = 0; i < 28991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28992(int& val) {
    func_in_28992(val);
    const int x = 28992;
    std::cout << 28992;
    for (int i = 0; i < 28992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28992(int& val) {
    const int x = 28992;
    std::cout << 28992;
    for (int i = 0; i < 28992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28993(int& val) {
    func_in_28993(val);
    const int x = 28993;
    std::cout << 28993;
    for (int i = 0; i < 28993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28993(int& val) {
    const int x = 28993;
    std::cout << 28993;
    for (int i = 0; i < 28993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28994(int& val) {
    func_in_28994(val);
    const int x = 28994;
    std::cout << 28994;
    for (int i = 0; i < 28994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28994(int& val) {
    const int x = 28994;
    std::cout << 28994;
    for (int i = 0; i < 28994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28995(int& val) {
    func_in_28995(val);
    const int x = 28995;
    std::cout << 28995;
    for (int i = 0; i < 28995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28995(int& val) {
    const int x = 28995;
    std::cout << 28995;
    for (int i = 0; i < 28995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28996(int& val) {
    func_in_28996(val);
    const int x = 28996;
    std::cout << 28996;
    for (int i = 0; i < 28996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28996(int& val) {
    const int x = 28996;
    std::cout << 28996;
    for (int i = 0; i < 28996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28997(int& val) {
    func_in_28997(val);
    const int x = 28997;
    std::cout << 28997;
    for (int i = 0; i < 28997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28997(int& val) {
    const int x = 28997;
    std::cout << 28997;
    for (int i = 0; i < 28997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28998(int& val) {
    func_in_28998(val);
    const int x = 28998;
    std::cout << 28998;
    for (int i = 0; i < 28998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28998(int& val) {
    const int x = 28998;
    std::cout << 28998;
    for (int i = 0; i < 28998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_28999(int& val) {
    func_in_28999(val);
    const int x = 28999;
    std::cout << 28999;
    for (int i = 0; i < 28999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_28999(int& val) {
    const int x = 28999;
    std::cout << 28999;
    for (int i = 0; i < 28999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29000(int& val) {
    func_in_29000(val);
    const int x = 29000;
    std::cout << 29000;
    for (int i = 0; i < 29000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29000(int& val) {
    const int x = 29000;
    std::cout << 29000;
    for (int i = 0; i < 29000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29001(int& val) {
    func_in_29001(val);
    const int x = 29001;
    std::cout << 29001;
    for (int i = 0; i < 29001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29001(int& val) {
    const int x = 29001;
    std::cout << 29001;
    for (int i = 0; i < 29001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29002(int& val) {
    func_in_29002(val);
    const int x = 29002;
    std::cout << 29002;
    for (int i = 0; i < 29002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29002(int& val) {
    const int x = 29002;
    std::cout << 29002;
    for (int i = 0; i < 29002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29003(int& val) {
    func_in_29003(val);
    const int x = 29003;
    std::cout << 29003;
    for (int i = 0; i < 29003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29003(int& val) {
    const int x = 29003;
    std::cout << 29003;
    for (int i = 0; i < 29003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29004(int& val) {
    func_in_29004(val);
    const int x = 29004;
    std::cout << 29004;
    for (int i = 0; i < 29004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29004(int& val) {
    const int x = 29004;
    std::cout << 29004;
    for (int i = 0; i < 29004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29005(int& val) {
    func_in_29005(val);
    const int x = 29005;
    std::cout << 29005;
    for (int i = 0; i < 29005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29005(int& val) {
    const int x = 29005;
    std::cout << 29005;
    for (int i = 0; i < 29005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29006(int& val) {
    func_in_29006(val);
    const int x = 29006;
    std::cout << 29006;
    for (int i = 0; i < 29006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29006(int& val) {
    const int x = 29006;
    std::cout << 29006;
    for (int i = 0; i < 29006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29007(int& val) {
    func_in_29007(val);
    const int x = 29007;
    std::cout << 29007;
    for (int i = 0; i < 29007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29007(int& val) {
    const int x = 29007;
    std::cout << 29007;
    for (int i = 0; i < 29007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29008(int& val) {
    func_in_29008(val);
    const int x = 29008;
    std::cout << 29008;
    for (int i = 0; i < 29008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29008(int& val) {
    const int x = 29008;
    std::cout << 29008;
    for (int i = 0; i < 29008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29009(int& val) {
    func_in_29009(val);
    const int x = 29009;
    std::cout << 29009;
    for (int i = 0; i < 29009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29009(int& val) {
    const int x = 29009;
    std::cout << 29009;
    for (int i = 0; i < 29009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29010(int& val) {
    func_in_29010(val);
    const int x = 29010;
    std::cout << 29010;
    for (int i = 0; i < 29010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29010(int& val) {
    const int x = 29010;
    std::cout << 29010;
    for (int i = 0; i < 29010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29011(int& val) {
    func_in_29011(val);
    const int x = 29011;
    std::cout << 29011;
    for (int i = 0; i < 29011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29011(int& val) {
    const int x = 29011;
    std::cout << 29011;
    for (int i = 0; i < 29011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29012(int& val) {
    func_in_29012(val);
    const int x = 29012;
    std::cout << 29012;
    for (int i = 0; i < 29012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29012(int& val) {
    const int x = 29012;
    std::cout << 29012;
    for (int i = 0; i < 29012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29013(int& val) {
    func_in_29013(val);
    const int x = 29013;
    std::cout << 29013;
    for (int i = 0; i < 29013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29013(int& val) {
    const int x = 29013;
    std::cout << 29013;
    for (int i = 0; i < 29013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29014(int& val) {
    func_in_29014(val);
    const int x = 29014;
    std::cout << 29014;
    for (int i = 0; i < 29014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29014(int& val) {
    const int x = 29014;
    std::cout << 29014;
    for (int i = 0; i < 29014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29015(int& val) {
    func_in_29015(val);
    const int x = 29015;
    std::cout << 29015;
    for (int i = 0; i < 29015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29015(int& val) {
    const int x = 29015;
    std::cout << 29015;
    for (int i = 0; i < 29015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29016(int& val) {
    func_in_29016(val);
    const int x = 29016;
    std::cout << 29016;
    for (int i = 0; i < 29016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29016(int& val) {
    const int x = 29016;
    std::cout << 29016;
    for (int i = 0; i < 29016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29017(int& val) {
    func_in_29017(val);
    const int x = 29017;
    std::cout << 29017;
    for (int i = 0; i < 29017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29017(int& val) {
    const int x = 29017;
    std::cout << 29017;
    for (int i = 0; i < 29017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29018(int& val) {
    func_in_29018(val);
    const int x = 29018;
    std::cout << 29018;
    for (int i = 0; i < 29018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29018(int& val) {
    const int x = 29018;
    std::cout << 29018;
    for (int i = 0; i < 29018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29019(int& val) {
    func_in_29019(val);
    const int x = 29019;
    std::cout << 29019;
    for (int i = 0; i < 29019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29019(int& val) {
    const int x = 29019;
    std::cout << 29019;
    for (int i = 0; i < 29019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29020(int& val) {
    func_in_29020(val);
    const int x = 29020;
    std::cout << 29020;
    for (int i = 0; i < 29020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29020(int& val) {
    const int x = 29020;
    std::cout << 29020;
    for (int i = 0; i < 29020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29021(int& val) {
    func_in_29021(val);
    const int x = 29021;
    std::cout << 29021;
    for (int i = 0; i < 29021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29021(int& val) {
    const int x = 29021;
    std::cout << 29021;
    for (int i = 0; i < 29021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29022(int& val) {
    func_in_29022(val);
    const int x = 29022;
    std::cout << 29022;
    for (int i = 0; i < 29022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29022(int& val) {
    const int x = 29022;
    std::cout << 29022;
    for (int i = 0; i < 29022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29023(int& val) {
    func_in_29023(val);
    const int x = 29023;
    std::cout << 29023;
    for (int i = 0; i < 29023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29023(int& val) {
    const int x = 29023;
    std::cout << 29023;
    for (int i = 0; i < 29023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29024(int& val) {
    func_in_29024(val);
    const int x = 29024;
    std::cout << 29024;
    for (int i = 0; i < 29024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29024(int& val) {
    const int x = 29024;
    std::cout << 29024;
    for (int i = 0; i < 29024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29025(int& val) {
    func_in_29025(val);
    const int x = 29025;
    std::cout << 29025;
    for (int i = 0; i < 29025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29025(int& val) {
    const int x = 29025;
    std::cout << 29025;
    for (int i = 0; i < 29025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29026(int& val) {
    func_in_29026(val);
    const int x = 29026;
    std::cout << 29026;
    for (int i = 0; i < 29026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29026(int& val) {
    const int x = 29026;
    std::cout << 29026;
    for (int i = 0; i < 29026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29027(int& val) {
    func_in_29027(val);
    const int x = 29027;
    std::cout << 29027;
    for (int i = 0; i < 29027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29027(int& val) {
    const int x = 29027;
    std::cout << 29027;
    for (int i = 0; i < 29027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29028(int& val) {
    func_in_29028(val);
    const int x = 29028;
    std::cout << 29028;
    for (int i = 0; i < 29028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29028(int& val) {
    const int x = 29028;
    std::cout << 29028;
    for (int i = 0; i < 29028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29029(int& val) {
    func_in_29029(val);
    const int x = 29029;
    std::cout << 29029;
    for (int i = 0; i < 29029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29029(int& val) {
    const int x = 29029;
    std::cout << 29029;
    for (int i = 0; i < 29029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29030(int& val) {
    func_in_29030(val);
    const int x = 29030;
    std::cout << 29030;
    for (int i = 0; i < 29030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29030(int& val) {
    const int x = 29030;
    std::cout << 29030;
    for (int i = 0; i < 29030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29031(int& val) {
    func_in_29031(val);
    const int x = 29031;
    std::cout << 29031;
    for (int i = 0; i < 29031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29031(int& val) {
    const int x = 29031;
    std::cout << 29031;
    for (int i = 0; i < 29031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29032(int& val) {
    func_in_29032(val);
    const int x = 29032;
    std::cout << 29032;
    for (int i = 0; i < 29032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29032(int& val) {
    const int x = 29032;
    std::cout << 29032;
    for (int i = 0; i < 29032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29033(int& val) {
    func_in_29033(val);
    const int x = 29033;
    std::cout << 29033;
    for (int i = 0; i < 29033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29033(int& val) {
    const int x = 29033;
    std::cout << 29033;
    for (int i = 0; i < 29033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29034(int& val) {
    func_in_29034(val);
    const int x = 29034;
    std::cout << 29034;
    for (int i = 0; i < 29034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29034(int& val) {
    const int x = 29034;
    std::cout << 29034;
    for (int i = 0; i < 29034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29035(int& val) {
    func_in_29035(val);
    const int x = 29035;
    std::cout << 29035;
    for (int i = 0; i < 29035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29035(int& val) {
    const int x = 29035;
    std::cout << 29035;
    for (int i = 0; i < 29035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29036(int& val) {
    func_in_29036(val);
    const int x = 29036;
    std::cout << 29036;
    for (int i = 0; i < 29036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29036(int& val) {
    const int x = 29036;
    std::cout << 29036;
    for (int i = 0; i < 29036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29037(int& val) {
    func_in_29037(val);
    const int x = 29037;
    std::cout << 29037;
    for (int i = 0; i < 29037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29037(int& val) {
    const int x = 29037;
    std::cout << 29037;
    for (int i = 0; i < 29037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29038(int& val) {
    func_in_29038(val);
    const int x = 29038;
    std::cout << 29038;
    for (int i = 0; i < 29038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29038(int& val) {
    const int x = 29038;
    std::cout << 29038;
    for (int i = 0; i < 29038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29039(int& val) {
    func_in_29039(val);
    const int x = 29039;
    std::cout << 29039;
    for (int i = 0; i < 29039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29039(int& val) {
    const int x = 29039;
    std::cout << 29039;
    for (int i = 0; i < 29039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29040(int& val) {
    func_in_29040(val);
    const int x = 29040;
    std::cout << 29040;
    for (int i = 0; i < 29040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29040(int& val) {
    const int x = 29040;
    std::cout << 29040;
    for (int i = 0; i < 29040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29041(int& val) {
    func_in_29041(val);
    const int x = 29041;
    std::cout << 29041;
    for (int i = 0; i < 29041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29041(int& val) {
    const int x = 29041;
    std::cout << 29041;
    for (int i = 0; i < 29041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29042(int& val) {
    func_in_29042(val);
    const int x = 29042;
    std::cout << 29042;
    for (int i = 0; i < 29042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29042(int& val) {
    const int x = 29042;
    std::cout << 29042;
    for (int i = 0; i < 29042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29043(int& val) {
    func_in_29043(val);
    const int x = 29043;
    std::cout << 29043;
    for (int i = 0; i < 29043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29043(int& val) {
    const int x = 29043;
    std::cout << 29043;
    for (int i = 0; i < 29043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29044(int& val) {
    func_in_29044(val);
    const int x = 29044;
    std::cout << 29044;
    for (int i = 0; i < 29044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29044(int& val) {
    const int x = 29044;
    std::cout << 29044;
    for (int i = 0; i < 29044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29045(int& val) {
    func_in_29045(val);
    const int x = 29045;
    std::cout << 29045;
    for (int i = 0; i < 29045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29045(int& val) {
    const int x = 29045;
    std::cout << 29045;
    for (int i = 0; i < 29045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29046(int& val) {
    func_in_29046(val);
    const int x = 29046;
    std::cout << 29046;
    for (int i = 0; i < 29046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29046(int& val) {
    const int x = 29046;
    std::cout << 29046;
    for (int i = 0; i < 29046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29047(int& val) {
    func_in_29047(val);
    const int x = 29047;
    std::cout << 29047;
    for (int i = 0; i < 29047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29047(int& val) {
    const int x = 29047;
    std::cout << 29047;
    for (int i = 0; i < 29047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29048(int& val) {
    func_in_29048(val);
    const int x = 29048;
    std::cout << 29048;
    for (int i = 0; i < 29048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29048(int& val) {
    const int x = 29048;
    std::cout << 29048;
    for (int i = 0; i < 29048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29049(int& val) {
    func_in_29049(val);
    const int x = 29049;
    std::cout << 29049;
    for (int i = 0; i < 29049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29049(int& val) {
    const int x = 29049;
    std::cout << 29049;
    for (int i = 0; i < 29049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29050(int& val) {
    func_in_29050(val);
    const int x = 29050;
    std::cout << 29050;
    for (int i = 0; i < 29050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29050(int& val) {
    const int x = 29050;
    std::cout << 29050;
    for (int i = 0; i < 29050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29051(int& val) {
    func_in_29051(val);
    const int x = 29051;
    std::cout << 29051;
    for (int i = 0; i < 29051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29051(int& val) {
    const int x = 29051;
    std::cout << 29051;
    for (int i = 0; i < 29051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29052(int& val) {
    func_in_29052(val);
    const int x = 29052;
    std::cout << 29052;
    for (int i = 0; i < 29052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29052(int& val) {
    const int x = 29052;
    std::cout << 29052;
    for (int i = 0; i < 29052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29053(int& val) {
    func_in_29053(val);
    const int x = 29053;
    std::cout << 29053;
    for (int i = 0; i < 29053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29053(int& val) {
    const int x = 29053;
    std::cout << 29053;
    for (int i = 0; i < 29053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29054(int& val) {
    func_in_29054(val);
    const int x = 29054;
    std::cout << 29054;
    for (int i = 0; i < 29054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29054(int& val) {
    const int x = 29054;
    std::cout << 29054;
    for (int i = 0; i < 29054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29055(int& val) {
    func_in_29055(val);
    const int x = 29055;
    std::cout << 29055;
    for (int i = 0; i < 29055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29055(int& val) {
    const int x = 29055;
    std::cout << 29055;
    for (int i = 0; i < 29055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29056(int& val) {
    func_in_29056(val);
    const int x = 29056;
    std::cout << 29056;
    for (int i = 0; i < 29056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29056(int& val) {
    const int x = 29056;
    std::cout << 29056;
    for (int i = 0; i < 29056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29057(int& val) {
    func_in_29057(val);
    const int x = 29057;
    std::cout << 29057;
    for (int i = 0; i < 29057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29057(int& val) {
    const int x = 29057;
    std::cout << 29057;
    for (int i = 0; i < 29057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29058(int& val) {
    func_in_29058(val);
    const int x = 29058;
    std::cout << 29058;
    for (int i = 0; i < 29058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29058(int& val) {
    const int x = 29058;
    std::cout << 29058;
    for (int i = 0; i < 29058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29059(int& val) {
    func_in_29059(val);
    const int x = 29059;
    std::cout << 29059;
    for (int i = 0; i < 29059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29059(int& val) {
    const int x = 29059;
    std::cout << 29059;
    for (int i = 0; i < 29059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29060(int& val) {
    func_in_29060(val);
    const int x = 29060;
    std::cout << 29060;
    for (int i = 0; i < 29060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29060(int& val) {
    const int x = 29060;
    std::cout << 29060;
    for (int i = 0; i < 29060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29061(int& val) {
    func_in_29061(val);
    const int x = 29061;
    std::cout << 29061;
    for (int i = 0; i < 29061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29061(int& val) {
    const int x = 29061;
    std::cout << 29061;
    for (int i = 0; i < 29061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29062(int& val) {
    func_in_29062(val);
    const int x = 29062;
    std::cout << 29062;
    for (int i = 0; i < 29062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29062(int& val) {
    const int x = 29062;
    std::cout << 29062;
    for (int i = 0; i < 29062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29063(int& val) {
    func_in_29063(val);
    const int x = 29063;
    std::cout << 29063;
    for (int i = 0; i < 29063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29063(int& val) {
    const int x = 29063;
    std::cout << 29063;
    for (int i = 0; i < 29063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29064(int& val) {
    func_in_29064(val);
    const int x = 29064;
    std::cout << 29064;
    for (int i = 0; i < 29064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29064(int& val) {
    const int x = 29064;
    std::cout << 29064;
    for (int i = 0; i < 29064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29065(int& val) {
    func_in_29065(val);
    const int x = 29065;
    std::cout << 29065;
    for (int i = 0; i < 29065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29065(int& val) {
    const int x = 29065;
    std::cout << 29065;
    for (int i = 0; i < 29065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29066(int& val) {
    func_in_29066(val);
    const int x = 29066;
    std::cout << 29066;
    for (int i = 0; i < 29066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29066(int& val) {
    const int x = 29066;
    std::cout << 29066;
    for (int i = 0; i < 29066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29067(int& val) {
    func_in_29067(val);
    const int x = 29067;
    std::cout << 29067;
    for (int i = 0; i < 29067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29067(int& val) {
    const int x = 29067;
    std::cout << 29067;
    for (int i = 0; i < 29067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29068(int& val) {
    func_in_29068(val);
    const int x = 29068;
    std::cout << 29068;
    for (int i = 0; i < 29068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29068(int& val) {
    const int x = 29068;
    std::cout << 29068;
    for (int i = 0; i < 29068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29069(int& val) {
    func_in_29069(val);
    const int x = 29069;
    std::cout << 29069;
    for (int i = 0; i < 29069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29069(int& val) {
    const int x = 29069;
    std::cout << 29069;
    for (int i = 0; i < 29069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29070(int& val) {
    func_in_29070(val);
    const int x = 29070;
    std::cout << 29070;
    for (int i = 0; i < 29070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29070(int& val) {
    const int x = 29070;
    std::cout << 29070;
    for (int i = 0; i < 29070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29071(int& val) {
    func_in_29071(val);
    const int x = 29071;
    std::cout << 29071;
    for (int i = 0; i < 29071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29071(int& val) {
    const int x = 29071;
    std::cout << 29071;
    for (int i = 0; i < 29071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29072(int& val) {
    func_in_29072(val);
    const int x = 29072;
    std::cout << 29072;
    for (int i = 0; i < 29072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29072(int& val) {
    const int x = 29072;
    std::cout << 29072;
    for (int i = 0; i < 29072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29073(int& val) {
    func_in_29073(val);
    const int x = 29073;
    std::cout << 29073;
    for (int i = 0; i < 29073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29073(int& val) {
    const int x = 29073;
    std::cout << 29073;
    for (int i = 0; i < 29073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29074(int& val) {
    func_in_29074(val);
    const int x = 29074;
    std::cout << 29074;
    for (int i = 0; i < 29074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29074(int& val) {
    const int x = 29074;
    std::cout << 29074;
    for (int i = 0; i < 29074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29075(int& val) {
    func_in_29075(val);
    const int x = 29075;
    std::cout << 29075;
    for (int i = 0; i < 29075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29075(int& val) {
    const int x = 29075;
    std::cout << 29075;
    for (int i = 0; i < 29075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29076(int& val) {
    func_in_29076(val);
    const int x = 29076;
    std::cout << 29076;
    for (int i = 0; i < 29076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29076(int& val) {
    const int x = 29076;
    std::cout << 29076;
    for (int i = 0; i < 29076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29077(int& val) {
    func_in_29077(val);
    const int x = 29077;
    std::cout << 29077;
    for (int i = 0; i < 29077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29077(int& val) {
    const int x = 29077;
    std::cout << 29077;
    for (int i = 0; i < 29077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29078(int& val) {
    func_in_29078(val);
    const int x = 29078;
    std::cout << 29078;
    for (int i = 0; i < 29078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29078(int& val) {
    const int x = 29078;
    std::cout << 29078;
    for (int i = 0; i < 29078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29079(int& val) {
    func_in_29079(val);
    const int x = 29079;
    std::cout << 29079;
    for (int i = 0; i < 29079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29079(int& val) {
    const int x = 29079;
    std::cout << 29079;
    for (int i = 0; i < 29079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29080(int& val) {
    func_in_29080(val);
    const int x = 29080;
    std::cout << 29080;
    for (int i = 0; i < 29080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29080(int& val) {
    const int x = 29080;
    std::cout << 29080;
    for (int i = 0; i < 29080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29081(int& val) {
    func_in_29081(val);
    const int x = 29081;
    std::cout << 29081;
    for (int i = 0; i < 29081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29081(int& val) {
    const int x = 29081;
    std::cout << 29081;
    for (int i = 0; i < 29081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29082(int& val) {
    func_in_29082(val);
    const int x = 29082;
    std::cout << 29082;
    for (int i = 0; i < 29082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29082(int& val) {
    const int x = 29082;
    std::cout << 29082;
    for (int i = 0; i < 29082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29083(int& val) {
    func_in_29083(val);
    const int x = 29083;
    std::cout << 29083;
    for (int i = 0; i < 29083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29083(int& val) {
    const int x = 29083;
    std::cout << 29083;
    for (int i = 0; i < 29083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29084(int& val) {
    func_in_29084(val);
    const int x = 29084;
    std::cout << 29084;
    for (int i = 0; i < 29084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29084(int& val) {
    const int x = 29084;
    std::cout << 29084;
    for (int i = 0; i < 29084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29085(int& val) {
    func_in_29085(val);
    const int x = 29085;
    std::cout << 29085;
    for (int i = 0; i < 29085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29085(int& val) {
    const int x = 29085;
    std::cout << 29085;
    for (int i = 0; i < 29085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29086(int& val) {
    func_in_29086(val);
    const int x = 29086;
    std::cout << 29086;
    for (int i = 0; i < 29086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29086(int& val) {
    const int x = 29086;
    std::cout << 29086;
    for (int i = 0; i < 29086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29087(int& val) {
    func_in_29087(val);
    const int x = 29087;
    std::cout << 29087;
    for (int i = 0; i < 29087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29087(int& val) {
    const int x = 29087;
    std::cout << 29087;
    for (int i = 0; i < 29087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29088(int& val) {
    func_in_29088(val);
    const int x = 29088;
    std::cout << 29088;
    for (int i = 0; i < 29088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29088(int& val) {
    const int x = 29088;
    std::cout << 29088;
    for (int i = 0; i < 29088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29089(int& val) {
    func_in_29089(val);
    const int x = 29089;
    std::cout << 29089;
    for (int i = 0; i < 29089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29089(int& val) {
    const int x = 29089;
    std::cout << 29089;
    for (int i = 0; i < 29089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29090(int& val) {
    func_in_29090(val);
    const int x = 29090;
    std::cout << 29090;
    for (int i = 0; i < 29090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29090(int& val) {
    const int x = 29090;
    std::cout << 29090;
    for (int i = 0; i < 29090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29091(int& val) {
    func_in_29091(val);
    const int x = 29091;
    std::cout << 29091;
    for (int i = 0; i < 29091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29091(int& val) {
    const int x = 29091;
    std::cout << 29091;
    for (int i = 0; i < 29091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29092(int& val) {
    func_in_29092(val);
    const int x = 29092;
    std::cout << 29092;
    for (int i = 0; i < 29092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29092(int& val) {
    const int x = 29092;
    std::cout << 29092;
    for (int i = 0; i < 29092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29093(int& val) {
    func_in_29093(val);
    const int x = 29093;
    std::cout << 29093;
    for (int i = 0; i < 29093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29093(int& val) {
    const int x = 29093;
    std::cout << 29093;
    for (int i = 0; i < 29093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29094(int& val) {
    func_in_29094(val);
    const int x = 29094;
    std::cout << 29094;
    for (int i = 0; i < 29094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29094(int& val) {
    const int x = 29094;
    std::cout << 29094;
    for (int i = 0; i < 29094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29095(int& val) {
    func_in_29095(val);
    const int x = 29095;
    std::cout << 29095;
    for (int i = 0; i < 29095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29095(int& val) {
    const int x = 29095;
    std::cout << 29095;
    for (int i = 0; i < 29095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29096(int& val) {
    func_in_29096(val);
    const int x = 29096;
    std::cout << 29096;
    for (int i = 0; i < 29096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29096(int& val) {
    const int x = 29096;
    std::cout << 29096;
    for (int i = 0; i < 29096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29097(int& val) {
    func_in_29097(val);
    const int x = 29097;
    std::cout << 29097;
    for (int i = 0; i < 29097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29097(int& val) {
    const int x = 29097;
    std::cout << 29097;
    for (int i = 0; i < 29097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29098(int& val) {
    func_in_29098(val);
    const int x = 29098;
    std::cout << 29098;
    for (int i = 0; i < 29098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29098(int& val) {
    const int x = 29098;
    std::cout << 29098;
    for (int i = 0; i < 29098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29099(int& val) {
    func_in_29099(val);
    const int x = 29099;
    std::cout << 29099;
    for (int i = 0; i < 29099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29099(int& val) {
    const int x = 29099;
    std::cout << 29099;
    for (int i = 0; i < 29099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29100(int& val) {
    func_in_29100(val);
    const int x = 29100;
    std::cout << 29100;
    for (int i = 0; i < 29100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29100(int& val) {
    const int x = 29100;
    std::cout << 29100;
    for (int i = 0; i < 29100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29101(int& val) {
    func_in_29101(val);
    const int x = 29101;
    std::cout << 29101;
    for (int i = 0; i < 29101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29101(int& val) {
    const int x = 29101;
    std::cout << 29101;
    for (int i = 0; i < 29101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29102(int& val) {
    func_in_29102(val);
    const int x = 29102;
    std::cout << 29102;
    for (int i = 0; i < 29102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29102(int& val) {
    const int x = 29102;
    std::cout << 29102;
    for (int i = 0; i < 29102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29103(int& val) {
    func_in_29103(val);
    const int x = 29103;
    std::cout << 29103;
    for (int i = 0; i < 29103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29103(int& val) {
    const int x = 29103;
    std::cout << 29103;
    for (int i = 0; i < 29103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29104(int& val) {
    func_in_29104(val);
    const int x = 29104;
    std::cout << 29104;
    for (int i = 0; i < 29104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29104(int& val) {
    const int x = 29104;
    std::cout << 29104;
    for (int i = 0; i < 29104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29105(int& val) {
    func_in_29105(val);
    const int x = 29105;
    std::cout << 29105;
    for (int i = 0; i < 29105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29105(int& val) {
    const int x = 29105;
    std::cout << 29105;
    for (int i = 0; i < 29105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29106(int& val) {
    func_in_29106(val);
    const int x = 29106;
    std::cout << 29106;
    for (int i = 0; i < 29106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29106(int& val) {
    const int x = 29106;
    std::cout << 29106;
    for (int i = 0; i < 29106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29107(int& val) {
    func_in_29107(val);
    const int x = 29107;
    std::cout << 29107;
    for (int i = 0; i < 29107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29107(int& val) {
    const int x = 29107;
    std::cout << 29107;
    for (int i = 0; i < 29107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29108(int& val) {
    func_in_29108(val);
    const int x = 29108;
    std::cout << 29108;
    for (int i = 0; i < 29108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29108(int& val) {
    const int x = 29108;
    std::cout << 29108;
    for (int i = 0; i < 29108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29109(int& val) {
    func_in_29109(val);
    const int x = 29109;
    std::cout << 29109;
    for (int i = 0; i < 29109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29109(int& val) {
    const int x = 29109;
    std::cout << 29109;
    for (int i = 0; i < 29109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29110(int& val) {
    func_in_29110(val);
    const int x = 29110;
    std::cout << 29110;
    for (int i = 0; i < 29110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29110(int& val) {
    const int x = 29110;
    std::cout << 29110;
    for (int i = 0; i < 29110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29111(int& val) {
    func_in_29111(val);
    const int x = 29111;
    std::cout << 29111;
    for (int i = 0; i < 29111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29111(int& val) {
    const int x = 29111;
    std::cout << 29111;
    for (int i = 0; i < 29111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29112(int& val) {
    func_in_29112(val);
    const int x = 29112;
    std::cout << 29112;
    for (int i = 0; i < 29112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29112(int& val) {
    const int x = 29112;
    std::cout << 29112;
    for (int i = 0; i < 29112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29113(int& val) {
    func_in_29113(val);
    const int x = 29113;
    std::cout << 29113;
    for (int i = 0; i < 29113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29113(int& val) {
    const int x = 29113;
    std::cout << 29113;
    for (int i = 0; i < 29113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29114(int& val) {
    func_in_29114(val);
    const int x = 29114;
    std::cout << 29114;
    for (int i = 0; i < 29114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29114(int& val) {
    const int x = 29114;
    std::cout << 29114;
    for (int i = 0; i < 29114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29115(int& val) {
    func_in_29115(val);
    const int x = 29115;
    std::cout << 29115;
    for (int i = 0; i < 29115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29115(int& val) {
    const int x = 29115;
    std::cout << 29115;
    for (int i = 0; i < 29115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29116(int& val) {
    func_in_29116(val);
    const int x = 29116;
    std::cout << 29116;
    for (int i = 0; i < 29116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29116(int& val) {
    const int x = 29116;
    std::cout << 29116;
    for (int i = 0; i < 29116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29117(int& val) {
    func_in_29117(val);
    const int x = 29117;
    std::cout << 29117;
    for (int i = 0; i < 29117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29117(int& val) {
    const int x = 29117;
    std::cout << 29117;
    for (int i = 0; i < 29117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29118(int& val) {
    func_in_29118(val);
    const int x = 29118;
    std::cout << 29118;
    for (int i = 0; i < 29118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29118(int& val) {
    const int x = 29118;
    std::cout << 29118;
    for (int i = 0; i < 29118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29119(int& val) {
    func_in_29119(val);
    const int x = 29119;
    std::cout << 29119;
    for (int i = 0; i < 29119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29119(int& val) {
    const int x = 29119;
    std::cout << 29119;
    for (int i = 0; i < 29119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29120(int& val) {
    func_in_29120(val);
    const int x = 29120;
    std::cout << 29120;
    for (int i = 0; i < 29120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29120(int& val) {
    const int x = 29120;
    std::cout << 29120;
    for (int i = 0; i < 29120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29121(int& val) {
    func_in_29121(val);
    const int x = 29121;
    std::cout << 29121;
    for (int i = 0; i < 29121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29121(int& val) {
    const int x = 29121;
    std::cout << 29121;
    for (int i = 0; i < 29121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29122(int& val) {
    func_in_29122(val);
    const int x = 29122;
    std::cout << 29122;
    for (int i = 0; i < 29122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29122(int& val) {
    const int x = 29122;
    std::cout << 29122;
    for (int i = 0; i < 29122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29123(int& val) {
    func_in_29123(val);
    const int x = 29123;
    std::cout << 29123;
    for (int i = 0; i < 29123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29123(int& val) {
    const int x = 29123;
    std::cout << 29123;
    for (int i = 0; i < 29123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29124(int& val) {
    func_in_29124(val);
    const int x = 29124;
    std::cout << 29124;
    for (int i = 0; i < 29124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29124(int& val) {
    const int x = 29124;
    std::cout << 29124;
    for (int i = 0; i < 29124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29125(int& val) {
    func_in_29125(val);
    const int x = 29125;
    std::cout << 29125;
    for (int i = 0; i < 29125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29125(int& val) {
    const int x = 29125;
    std::cout << 29125;
    for (int i = 0; i < 29125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29126(int& val) {
    func_in_29126(val);
    const int x = 29126;
    std::cout << 29126;
    for (int i = 0; i < 29126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29126(int& val) {
    const int x = 29126;
    std::cout << 29126;
    for (int i = 0; i < 29126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29127(int& val) {
    func_in_29127(val);
    const int x = 29127;
    std::cout << 29127;
    for (int i = 0; i < 29127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29127(int& val) {
    const int x = 29127;
    std::cout << 29127;
    for (int i = 0; i < 29127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29128(int& val) {
    func_in_29128(val);
    const int x = 29128;
    std::cout << 29128;
    for (int i = 0; i < 29128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29128(int& val) {
    const int x = 29128;
    std::cout << 29128;
    for (int i = 0; i < 29128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29129(int& val) {
    func_in_29129(val);
    const int x = 29129;
    std::cout << 29129;
    for (int i = 0; i < 29129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29129(int& val) {
    const int x = 29129;
    std::cout << 29129;
    for (int i = 0; i < 29129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29130(int& val) {
    func_in_29130(val);
    const int x = 29130;
    std::cout << 29130;
    for (int i = 0; i < 29130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29130(int& val) {
    const int x = 29130;
    std::cout << 29130;
    for (int i = 0; i < 29130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29131(int& val) {
    func_in_29131(val);
    const int x = 29131;
    std::cout << 29131;
    for (int i = 0; i < 29131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29131(int& val) {
    const int x = 29131;
    std::cout << 29131;
    for (int i = 0; i < 29131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29132(int& val) {
    func_in_29132(val);
    const int x = 29132;
    std::cout << 29132;
    for (int i = 0; i < 29132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29132(int& val) {
    const int x = 29132;
    std::cout << 29132;
    for (int i = 0; i < 29132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29133(int& val) {
    func_in_29133(val);
    const int x = 29133;
    std::cout << 29133;
    for (int i = 0; i < 29133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29133(int& val) {
    const int x = 29133;
    std::cout << 29133;
    for (int i = 0; i < 29133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29134(int& val) {
    func_in_29134(val);
    const int x = 29134;
    std::cout << 29134;
    for (int i = 0; i < 29134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29134(int& val) {
    const int x = 29134;
    std::cout << 29134;
    for (int i = 0; i < 29134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29135(int& val) {
    func_in_29135(val);
    const int x = 29135;
    std::cout << 29135;
    for (int i = 0; i < 29135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29135(int& val) {
    const int x = 29135;
    std::cout << 29135;
    for (int i = 0; i < 29135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29136(int& val) {
    func_in_29136(val);
    const int x = 29136;
    std::cout << 29136;
    for (int i = 0; i < 29136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29136(int& val) {
    const int x = 29136;
    std::cout << 29136;
    for (int i = 0; i < 29136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29137(int& val) {
    func_in_29137(val);
    const int x = 29137;
    std::cout << 29137;
    for (int i = 0; i < 29137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29137(int& val) {
    const int x = 29137;
    std::cout << 29137;
    for (int i = 0; i < 29137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29138(int& val) {
    func_in_29138(val);
    const int x = 29138;
    std::cout << 29138;
    for (int i = 0; i < 29138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29138(int& val) {
    const int x = 29138;
    std::cout << 29138;
    for (int i = 0; i < 29138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29139(int& val) {
    func_in_29139(val);
    const int x = 29139;
    std::cout << 29139;
    for (int i = 0; i < 29139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29139(int& val) {
    const int x = 29139;
    std::cout << 29139;
    for (int i = 0; i < 29139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29140(int& val) {
    func_in_29140(val);
    const int x = 29140;
    std::cout << 29140;
    for (int i = 0; i < 29140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29140(int& val) {
    const int x = 29140;
    std::cout << 29140;
    for (int i = 0; i < 29140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29141(int& val) {
    func_in_29141(val);
    const int x = 29141;
    std::cout << 29141;
    for (int i = 0; i < 29141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29141(int& val) {
    const int x = 29141;
    std::cout << 29141;
    for (int i = 0; i < 29141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29142(int& val) {
    func_in_29142(val);
    const int x = 29142;
    std::cout << 29142;
    for (int i = 0; i < 29142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29142(int& val) {
    const int x = 29142;
    std::cout << 29142;
    for (int i = 0; i < 29142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29143(int& val) {
    func_in_29143(val);
    const int x = 29143;
    std::cout << 29143;
    for (int i = 0; i < 29143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29143(int& val) {
    const int x = 29143;
    std::cout << 29143;
    for (int i = 0; i < 29143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29144(int& val) {
    func_in_29144(val);
    const int x = 29144;
    std::cout << 29144;
    for (int i = 0; i < 29144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29144(int& val) {
    const int x = 29144;
    std::cout << 29144;
    for (int i = 0; i < 29144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29145(int& val) {
    func_in_29145(val);
    const int x = 29145;
    std::cout << 29145;
    for (int i = 0; i < 29145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29145(int& val) {
    const int x = 29145;
    std::cout << 29145;
    for (int i = 0; i < 29145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29146(int& val) {
    func_in_29146(val);
    const int x = 29146;
    std::cout << 29146;
    for (int i = 0; i < 29146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29146(int& val) {
    const int x = 29146;
    std::cout << 29146;
    for (int i = 0; i < 29146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29147(int& val) {
    func_in_29147(val);
    const int x = 29147;
    std::cout << 29147;
    for (int i = 0; i < 29147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29147(int& val) {
    const int x = 29147;
    std::cout << 29147;
    for (int i = 0; i < 29147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29148(int& val) {
    func_in_29148(val);
    const int x = 29148;
    std::cout << 29148;
    for (int i = 0; i < 29148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29148(int& val) {
    const int x = 29148;
    std::cout << 29148;
    for (int i = 0; i < 29148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29149(int& val) {
    func_in_29149(val);
    const int x = 29149;
    std::cout << 29149;
    for (int i = 0; i < 29149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29149(int& val) {
    const int x = 29149;
    std::cout << 29149;
    for (int i = 0; i < 29149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29150(int& val) {
    func_in_29150(val);
    const int x = 29150;
    std::cout << 29150;
    for (int i = 0; i < 29150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29150(int& val) {
    const int x = 29150;
    std::cout << 29150;
    for (int i = 0; i < 29150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29151(int& val) {
    func_in_29151(val);
    const int x = 29151;
    std::cout << 29151;
    for (int i = 0; i < 29151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29151(int& val) {
    const int x = 29151;
    std::cout << 29151;
    for (int i = 0; i < 29151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29152(int& val) {
    func_in_29152(val);
    const int x = 29152;
    std::cout << 29152;
    for (int i = 0; i < 29152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29152(int& val) {
    const int x = 29152;
    std::cout << 29152;
    for (int i = 0; i < 29152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29153(int& val) {
    func_in_29153(val);
    const int x = 29153;
    std::cout << 29153;
    for (int i = 0; i < 29153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29153(int& val) {
    const int x = 29153;
    std::cout << 29153;
    for (int i = 0; i < 29153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29154(int& val) {
    func_in_29154(val);
    const int x = 29154;
    std::cout << 29154;
    for (int i = 0; i < 29154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29154(int& val) {
    const int x = 29154;
    std::cout << 29154;
    for (int i = 0; i < 29154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29155(int& val) {
    func_in_29155(val);
    const int x = 29155;
    std::cout << 29155;
    for (int i = 0; i < 29155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29155(int& val) {
    const int x = 29155;
    std::cout << 29155;
    for (int i = 0; i < 29155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29156(int& val) {
    func_in_29156(val);
    const int x = 29156;
    std::cout << 29156;
    for (int i = 0; i < 29156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29156(int& val) {
    const int x = 29156;
    std::cout << 29156;
    for (int i = 0; i < 29156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29157(int& val) {
    func_in_29157(val);
    const int x = 29157;
    std::cout << 29157;
    for (int i = 0; i < 29157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29157(int& val) {
    const int x = 29157;
    std::cout << 29157;
    for (int i = 0; i < 29157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29158(int& val) {
    func_in_29158(val);
    const int x = 29158;
    std::cout << 29158;
    for (int i = 0; i < 29158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29158(int& val) {
    const int x = 29158;
    std::cout << 29158;
    for (int i = 0; i < 29158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29159(int& val) {
    func_in_29159(val);
    const int x = 29159;
    std::cout << 29159;
    for (int i = 0; i < 29159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29159(int& val) {
    const int x = 29159;
    std::cout << 29159;
    for (int i = 0; i < 29159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29160(int& val) {
    func_in_29160(val);
    const int x = 29160;
    std::cout << 29160;
    for (int i = 0; i < 29160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29160(int& val) {
    const int x = 29160;
    std::cout << 29160;
    for (int i = 0; i < 29160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29161(int& val) {
    func_in_29161(val);
    const int x = 29161;
    std::cout << 29161;
    for (int i = 0; i < 29161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29161(int& val) {
    const int x = 29161;
    std::cout << 29161;
    for (int i = 0; i < 29161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29162(int& val) {
    func_in_29162(val);
    const int x = 29162;
    std::cout << 29162;
    for (int i = 0; i < 29162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29162(int& val) {
    const int x = 29162;
    std::cout << 29162;
    for (int i = 0; i < 29162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29163(int& val) {
    func_in_29163(val);
    const int x = 29163;
    std::cout << 29163;
    for (int i = 0; i < 29163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29163(int& val) {
    const int x = 29163;
    std::cout << 29163;
    for (int i = 0; i < 29163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29164(int& val) {
    func_in_29164(val);
    const int x = 29164;
    std::cout << 29164;
    for (int i = 0; i < 29164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29164(int& val) {
    const int x = 29164;
    std::cout << 29164;
    for (int i = 0; i < 29164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29165(int& val) {
    func_in_29165(val);
    const int x = 29165;
    std::cout << 29165;
    for (int i = 0; i < 29165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29165(int& val) {
    const int x = 29165;
    std::cout << 29165;
    for (int i = 0; i < 29165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29166(int& val) {
    func_in_29166(val);
    const int x = 29166;
    std::cout << 29166;
    for (int i = 0; i < 29166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29166(int& val) {
    const int x = 29166;
    std::cout << 29166;
    for (int i = 0; i < 29166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29167(int& val) {
    func_in_29167(val);
    const int x = 29167;
    std::cout << 29167;
    for (int i = 0; i < 29167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29167(int& val) {
    const int x = 29167;
    std::cout << 29167;
    for (int i = 0; i < 29167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29168(int& val) {
    func_in_29168(val);
    const int x = 29168;
    std::cout << 29168;
    for (int i = 0; i < 29168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29168(int& val) {
    const int x = 29168;
    std::cout << 29168;
    for (int i = 0; i < 29168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29169(int& val) {
    func_in_29169(val);
    const int x = 29169;
    std::cout << 29169;
    for (int i = 0; i < 29169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29169(int& val) {
    const int x = 29169;
    std::cout << 29169;
    for (int i = 0; i < 29169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29170(int& val) {
    func_in_29170(val);
    const int x = 29170;
    std::cout << 29170;
    for (int i = 0; i < 29170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29170(int& val) {
    const int x = 29170;
    std::cout << 29170;
    for (int i = 0; i < 29170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29171(int& val) {
    func_in_29171(val);
    const int x = 29171;
    std::cout << 29171;
    for (int i = 0; i < 29171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29171(int& val) {
    const int x = 29171;
    std::cout << 29171;
    for (int i = 0; i < 29171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29172(int& val) {
    func_in_29172(val);
    const int x = 29172;
    std::cout << 29172;
    for (int i = 0; i < 29172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29172(int& val) {
    const int x = 29172;
    std::cout << 29172;
    for (int i = 0; i < 29172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29173(int& val) {
    func_in_29173(val);
    const int x = 29173;
    std::cout << 29173;
    for (int i = 0; i < 29173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29173(int& val) {
    const int x = 29173;
    std::cout << 29173;
    for (int i = 0; i < 29173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29174(int& val) {
    func_in_29174(val);
    const int x = 29174;
    std::cout << 29174;
    for (int i = 0; i < 29174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29174(int& val) {
    const int x = 29174;
    std::cout << 29174;
    for (int i = 0; i < 29174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29175(int& val) {
    func_in_29175(val);
    const int x = 29175;
    std::cout << 29175;
    for (int i = 0; i < 29175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29175(int& val) {
    const int x = 29175;
    std::cout << 29175;
    for (int i = 0; i < 29175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29176(int& val) {
    func_in_29176(val);
    const int x = 29176;
    std::cout << 29176;
    for (int i = 0; i < 29176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29176(int& val) {
    const int x = 29176;
    std::cout << 29176;
    for (int i = 0; i < 29176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29177(int& val) {
    func_in_29177(val);
    const int x = 29177;
    std::cout << 29177;
    for (int i = 0; i < 29177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29177(int& val) {
    const int x = 29177;
    std::cout << 29177;
    for (int i = 0; i < 29177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29178(int& val) {
    func_in_29178(val);
    const int x = 29178;
    std::cout << 29178;
    for (int i = 0; i < 29178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29178(int& val) {
    const int x = 29178;
    std::cout << 29178;
    for (int i = 0; i < 29178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29179(int& val) {
    func_in_29179(val);
    const int x = 29179;
    std::cout << 29179;
    for (int i = 0; i < 29179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29179(int& val) {
    const int x = 29179;
    std::cout << 29179;
    for (int i = 0; i < 29179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29180(int& val) {
    func_in_29180(val);
    const int x = 29180;
    std::cout << 29180;
    for (int i = 0; i < 29180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29180(int& val) {
    const int x = 29180;
    std::cout << 29180;
    for (int i = 0; i < 29180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29181(int& val) {
    func_in_29181(val);
    const int x = 29181;
    std::cout << 29181;
    for (int i = 0; i < 29181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29181(int& val) {
    const int x = 29181;
    std::cout << 29181;
    for (int i = 0; i < 29181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29182(int& val) {
    func_in_29182(val);
    const int x = 29182;
    std::cout << 29182;
    for (int i = 0; i < 29182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29182(int& val) {
    const int x = 29182;
    std::cout << 29182;
    for (int i = 0; i < 29182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29183(int& val) {
    func_in_29183(val);
    const int x = 29183;
    std::cout << 29183;
    for (int i = 0; i < 29183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29183(int& val) {
    const int x = 29183;
    std::cout << 29183;
    for (int i = 0; i < 29183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29184(int& val) {
    func_in_29184(val);
    const int x = 29184;
    std::cout << 29184;
    for (int i = 0; i < 29184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29184(int& val) {
    const int x = 29184;
    std::cout << 29184;
    for (int i = 0; i < 29184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29185(int& val) {
    func_in_29185(val);
    const int x = 29185;
    std::cout << 29185;
    for (int i = 0; i < 29185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29185(int& val) {
    const int x = 29185;
    std::cout << 29185;
    for (int i = 0; i < 29185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29186(int& val) {
    func_in_29186(val);
    const int x = 29186;
    std::cout << 29186;
    for (int i = 0; i < 29186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29186(int& val) {
    const int x = 29186;
    std::cout << 29186;
    for (int i = 0; i < 29186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29187(int& val) {
    func_in_29187(val);
    const int x = 29187;
    std::cout << 29187;
    for (int i = 0; i < 29187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29187(int& val) {
    const int x = 29187;
    std::cout << 29187;
    for (int i = 0; i < 29187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29188(int& val) {
    func_in_29188(val);
    const int x = 29188;
    std::cout << 29188;
    for (int i = 0; i < 29188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29188(int& val) {
    const int x = 29188;
    std::cout << 29188;
    for (int i = 0; i < 29188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29189(int& val) {
    func_in_29189(val);
    const int x = 29189;
    std::cout << 29189;
    for (int i = 0; i < 29189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29189(int& val) {
    const int x = 29189;
    std::cout << 29189;
    for (int i = 0; i < 29189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29190(int& val) {
    func_in_29190(val);
    const int x = 29190;
    std::cout << 29190;
    for (int i = 0; i < 29190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29190(int& val) {
    const int x = 29190;
    std::cout << 29190;
    for (int i = 0; i < 29190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29191(int& val) {
    func_in_29191(val);
    const int x = 29191;
    std::cout << 29191;
    for (int i = 0; i < 29191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29191(int& val) {
    const int x = 29191;
    std::cout << 29191;
    for (int i = 0; i < 29191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29192(int& val) {
    func_in_29192(val);
    const int x = 29192;
    std::cout << 29192;
    for (int i = 0; i < 29192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29192(int& val) {
    const int x = 29192;
    std::cout << 29192;
    for (int i = 0; i < 29192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29193(int& val) {
    func_in_29193(val);
    const int x = 29193;
    std::cout << 29193;
    for (int i = 0; i < 29193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29193(int& val) {
    const int x = 29193;
    std::cout << 29193;
    for (int i = 0; i < 29193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29194(int& val) {
    func_in_29194(val);
    const int x = 29194;
    std::cout << 29194;
    for (int i = 0; i < 29194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29194(int& val) {
    const int x = 29194;
    std::cout << 29194;
    for (int i = 0; i < 29194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29195(int& val) {
    func_in_29195(val);
    const int x = 29195;
    std::cout << 29195;
    for (int i = 0; i < 29195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29195(int& val) {
    const int x = 29195;
    std::cout << 29195;
    for (int i = 0; i < 29195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29196(int& val) {
    func_in_29196(val);
    const int x = 29196;
    std::cout << 29196;
    for (int i = 0; i < 29196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29196(int& val) {
    const int x = 29196;
    std::cout << 29196;
    for (int i = 0; i < 29196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29197(int& val) {
    func_in_29197(val);
    const int x = 29197;
    std::cout << 29197;
    for (int i = 0; i < 29197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29197(int& val) {
    const int x = 29197;
    std::cout << 29197;
    for (int i = 0; i < 29197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29198(int& val) {
    func_in_29198(val);
    const int x = 29198;
    std::cout << 29198;
    for (int i = 0; i < 29198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29198(int& val) {
    const int x = 29198;
    std::cout << 29198;
    for (int i = 0; i < 29198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29199(int& val) {
    func_in_29199(val);
    const int x = 29199;
    std::cout << 29199;
    for (int i = 0; i < 29199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29199(int& val) {
    const int x = 29199;
    std::cout << 29199;
    for (int i = 0; i < 29199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29200(int& val) {
    func_in_29200(val);
    const int x = 29200;
    std::cout << 29200;
    for (int i = 0; i < 29200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29200(int& val) {
    const int x = 29200;
    std::cout << 29200;
    for (int i = 0; i < 29200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29201(int& val) {
    func_in_29201(val);
    const int x = 29201;
    std::cout << 29201;
    for (int i = 0; i < 29201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29201(int& val) {
    const int x = 29201;
    std::cout << 29201;
    for (int i = 0; i < 29201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29202(int& val) {
    func_in_29202(val);
    const int x = 29202;
    std::cout << 29202;
    for (int i = 0; i < 29202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29202(int& val) {
    const int x = 29202;
    std::cout << 29202;
    for (int i = 0; i < 29202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29203(int& val) {
    func_in_29203(val);
    const int x = 29203;
    std::cout << 29203;
    for (int i = 0; i < 29203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29203(int& val) {
    const int x = 29203;
    std::cout << 29203;
    for (int i = 0; i < 29203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29204(int& val) {
    func_in_29204(val);
    const int x = 29204;
    std::cout << 29204;
    for (int i = 0; i < 29204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29204(int& val) {
    const int x = 29204;
    std::cout << 29204;
    for (int i = 0; i < 29204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29205(int& val) {
    func_in_29205(val);
    const int x = 29205;
    std::cout << 29205;
    for (int i = 0; i < 29205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29205(int& val) {
    const int x = 29205;
    std::cout << 29205;
    for (int i = 0; i < 29205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29206(int& val) {
    func_in_29206(val);
    const int x = 29206;
    std::cout << 29206;
    for (int i = 0; i < 29206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29206(int& val) {
    const int x = 29206;
    std::cout << 29206;
    for (int i = 0; i < 29206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29207(int& val) {
    func_in_29207(val);
    const int x = 29207;
    std::cout << 29207;
    for (int i = 0; i < 29207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29207(int& val) {
    const int x = 29207;
    std::cout << 29207;
    for (int i = 0; i < 29207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29208(int& val) {
    func_in_29208(val);
    const int x = 29208;
    std::cout << 29208;
    for (int i = 0; i < 29208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29208(int& val) {
    const int x = 29208;
    std::cout << 29208;
    for (int i = 0; i < 29208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29209(int& val) {
    func_in_29209(val);
    const int x = 29209;
    std::cout << 29209;
    for (int i = 0; i < 29209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29209(int& val) {
    const int x = 29209;
    std::cout << 29209;
    for (int i = 0; i < 29209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29210(int& val) {
    func_in_29210(val);
    const int x = 29210;
    std::cout << 29210;
    for (int i = 0; i < 29210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29210(int& val) {
    const int x = 29210;
    std::cout << 29210;
    for (int i = 0; i < 29210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29211(int& val) {
    func_in_29211(val);
    const int x = 29211;
    std::cout << 29211;
    for (int i = 0; i < 29211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29211(int& val) {
    const int x = 29211;
    std::cout << 29211;
    for (int i = 0; i < 29211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29212(int& val) {
    func_in_29212(val);
    const int x = 29212;
    std::cout << 29212;
    for (int i = 0; i < 29212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29212(int& val) {
    const int x = 29212;
    std::cout << 29212;
    for (int i = 0; i < 29212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29213(int& val) {
    func_in_29213(val);
    const int x = 29213;
    std::cout << 29213;
    for (int i = 0; i < 29213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29213(int& val) {
    const int x = 29213;
    std::cout << 29213;
    for (int i = 0; i < 29213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29214(int& val) {
    func_in_29214(val);
    const int x = 29214;
    std::cout << 29214;
    for (int i = 0; i < 29214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29214(int& val) {
    const int x = 29214;
    std::cout << 29214;
    for (int i = 0; i < 29214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29215(int& val) {
    func_in_29215(val);
    const int x = 29215;
    std::cout << 29215;
    for (int i = 0; i < 29215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29215(int& val) {
    const int x = 29215;
    std::cout << 29215;
    for (int i = 0; i < 29215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29216(int& val) {
    func_in_29216(val);
    const int x = 29216;
    std::cout << 29216;
    for (int i = 0; i < 29216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29216(int& val) {
    const int x = 29216;
    std::cout << 29216;
    for (int i = 0; i < 29216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29217(int& val) {
    func_in_29217(val);
    const int x = 29217;
    std::cout << 29217;
    for (int i = 0; i < 29217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29217(int& val) {
    const int x = 29217;
    std::cout << 29217;
    for (int i = 0; i < 29217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29218(int& val) {
    func_in_29218(val);
    const int x = 29218;
    std::cout << 29218;
    for (int i = 0; i < 29218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29218(int& val) {
    const int x = 29218;
    std::cout << 29218;
    for (int i = 0; i < 29218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29219(int& val) {
    func_in_29219(val);
    const int x = 29219;
    std::cout << 29219;
    for (int i = 0; i < 29219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29219(int& val) {
    const int x = 29219;
    std::cout << 29219;
    for (int i = 0; i < 29219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29220(int& val) {
    func_in_29220(val);
    const int x = 29220;
    std::cout << 29220;
    for (int i = 0; i < 29220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29220(int& val) {
    const int x = 29220;
    std::cout << 29220;
    for (int i = 0; i < 29220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29221(int& val) {
    func_in_29221(val);
    const int x = 29221;
    std::cout << 29221;
    for (int i = 0; i < 29221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29221(int& val) {
    const int x = 29221;
    std::cout << 29221;
    for (int i = 0; i < 29221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29222(int& val) {
    func_in_29222(val);
    const int x = 29222;
    std::cout << 29222;
    for (int i = 0; i < 29222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29222(int& val) {
    const int x = 29222;
    std::cout << 29222;
    for (int i = 0; i < 29222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29223(int& val) {
    func_in_29223(val);
    const int x = 29223;
    std::cout << 29223;
    for (int i = 0; i < 29223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29223(int& val) {
    const int x = 29223;
    std::cout << 29223;
    for (int i = 0; i < 29223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29224(int& val) {
    func_in_29224(val);
    const int x = 29224;
    std::cout << 29224;
    for (int i = 0; i < 29224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29224(int& val) {
    const int x = 29224;
    std::cout << 29224;
    for (int i = 0; i < 29224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29225(int& val) {
    func_in_29225(val);
    const int x = 29225;
    std::cout << 29225;
    for (int i = 0; i < 29225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29225(int& val) {
    const int x = 29225;
    std::cout << 29225;
    for (int i = 0; i < 29225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29226(int& val) {
    func_in_29226(val);
    const int x = 29226;
    std::cout << 29226;
    for (int i = 0; i < 29226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29226(int& val) {
    const int x = 29226;
    std::cout << 29226;
    for (int i = 0; i < 29226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29227(int& val) {
    func_in_29227(val);
    const int x = 29227;
    std::cout << 29227;
    for (int i = 0; i < 29227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29227(int& val) {
    const int x = 29227;
    std::cout << 29227;
    for (int i = 0; i < 29227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29228(int& val) {
    func_in_29228(val);
    const int x = 29228;
    std::cout << 29228;
    for (int i = 0; i < 29228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29228(int& val) {
    const int x = 29228;
    std::cout << 29228;
    for (int i = 0; i < 29228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29229(int& val) {
    func_in_29229(val);
    const int x = 29229;
    std::cout << 29229;
    for (int i = 0; i < 29229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29229(int& val) {
    const int x = 29229;
    std::cout << 29229;
    for (int i = 0; i < 29229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29230(int& val) {
    func_in_29230(val);
    const int x = 29230;
    std::cout << 29230;
    for (int i = 0; i < 29230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29230(int& val) {
    const int x = 29230;
    std::cout << 29230;
    for (int i = 0; i < 29230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29231(int& val) {
    func_in_29231(val);
    const int x = 29231;
    std::cout << 29231;
    for (int i = 0; i < 29231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29231(int& val) {
    const int x = 29231;
    std::cout << 29231;
    for (int i = 0; i < 29231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29232(int& val) {
    func_in_29232(val);
    const int x = 29232;
    std::cout << 29232;
    for (int i = 0; i < 29232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29232(int& val) {
    const int x = 29232;
    std::cout << 29232;
    for (int i = 0; i < 29232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29233(int& val) {
    func_in_29233(val);
    const int x = 29233;
    std::cout << 29233;
    for (int i = 0; i < 29233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29233(int& val) {
    const int x = 29233;
    std::cout << 29233;
    for (int i = 0; i < 29233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29234(int& val) {
    func_in_29234(val);
    const int x = 29234;
    std::cout << 29234;
    for (int i = 0; i < 29234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29234(int& val) {
    const int x = 29234;
    std::cout << 29234;
    for (int i = 0; i < 29234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29235(int& val) {
    func_in_29235(val);
    const int x = 29235;
    std::cout << 29235;
    for (int i = 0; i < 29235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29235(int& val) {
    const int x = 29235;
    std::cout << 29235;
    for (int i = 0; i < 29235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29236(int& val) {
    func_in_29236(val);
    const int x = 29236;
    std::cout << 29236;
    for (int i = 0; i < 29236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29236(int& val) {
    const int x = 29236;
    std::cout << 29236;
    for (int i = 0; i < 29236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29237(int& val) {
    func_in_29237(val);
    const int x = 29237;
    std::cout << 29237;
    for (int i = 0; i < 29237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29237(int& val) {
    const int x = 29237;
    std::cout << 29237;
    for (int i = 0; i < 29237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29238(int& val) {
    func_in_29238(val);
    const int x = 29238;
    std::cout << 29238;
    for (int i = 0; i < 29238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29238(int& val) {
    const int x = 29238;
    std::cout << 29238;
    for (int i = 0; i < 29238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29239(int& val) {
    func_in_29239(val);
    const int x = 29239;
    std::cout << 29239;
    for (int i = 0; i < 29239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29239(int& val) {
    const int x = 29239;
    std::cout << 29239;
    for (int i = 0; i < 29239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29240(int& val) {
    func_in_29240(val);
    const int x = 29240;
    std::cout << 29240;
    for (int i = 0; i < 29240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29240(int& val) {
    const int x = 29240;
    std::cout << 29240;
    for (int i = 0; i < 29240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29241(int& val) {
    func_in_29241(val);
    const int x = 29241;
    std::cout << 29241;
    for (int i = 0; i < 29241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29241(int& val) {
    const int x = 29241;
    std::cout << 29241;
    for (int i = 0; i < 29241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29242(int& val) {
    func_in_29242(val);
    const int x = 29242;
    std::cout << 29242;
    for (int i = 0; i < 29242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29242(int& val) {
    const int x = 29242;
    std::cout << 29242;
    for (int i = 0; i < 29242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29243(int& val) {
    func_in_29243(val);
    const int x = 29243;
    std::cout << 29243;
    for (int i = 0; i < 29243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29243(int& val) {
    const int x = 29243;
    std::cout << 29243;
    for (int i = 0; i < 29243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29244(int& val) {
    func_in_29244(val);
    const int x = 29244;
    std::cout << 29244;
    for (int i = 0; i < 29244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29244(int& val) {
    const int x = 29244;
    std::cout << 29244;
    for (int i = 0; i < 29244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29245(int& val) {
    func_in_29245(val);
    const int x = 29245;
    std::cout << 29245;
    for (int i = 0; i < 29245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29245(int& val) {
    const int x = 29245;
    std::cout << 29245;
    for (int i = 0; i < 29245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29246(int& val) {
    func_in_29246(val);
    const int x = 29246;
    std::cout << 29246;
    for (int i = 0; i < 29246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29246(int& val) {
    const int x = 29246;
    std::cout << 29246;
    for (int i = 0; i < 29246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29247(int& val) {
    func_in_29247(val);
    const int x = 29247;
    std::cout << 29247;
    for (int i = 0; i < 29247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29247(int& val) {
    const int x = 29247;
    std::cout << 29247;
    for (int i = 0; i < 29247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29248(int& val) {
    func_in_29248(val);
    const int x = 29248;
    std::cout << 29248;
    for (int i = 0; i < 29248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29248(int& val) {
    const int x = 29248;
    std::cout << 29248;
    for (int i = 0; i < 29248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29249(int& val) {
    func_in_29249(val);
    const int x = 29249;
    std::cout << 29249;
    for (int i = 0; i < 29249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29249(int& val) {
    const int x = 29249;
    std::cout << 29249;
    for (int i = 0; i < 29249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29250(int& val) {
    func_in_29250(val);
    const int x = 29250;
    std::cout << 29250;
    for (int i = 0; i < 29250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29250(int& val) {
    const int x = 29250;
    std::cout << 29250;
    for (int i = 0; i < 29250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29251(int& val) {
    func_in_29251(val);
    const int x = 29251;
    std::cout << 29251;
    for (int i = 0; i < 29251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29251(int& val) {
    const int x = 29251;
    std::cout << 29251;
    for (int i = 0; i < 29251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29252(int& val) {
    func_in_29252(val);
    const int x = 29252;
    std::cout << 29252;
    for (int i = 0; i < 29252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29252(int& val) {
    const int x = 29252;
    std::cout << 29252;
    for (int i = 0; i < 29252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29253(int& val) {
    func_in_29253(val);
    const int x = 29253;
    std::cout << 29253;
    for (int i = 0; i < 29253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29253(int& val) {
    const int x = 29253;
    std::cout << 29253;
    for (int i = 0; i < 29253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29254(int& val) {
    func_in_29254(val);
    const int x = 29254;
    std::cout << 29254;
    for (int i = 0; i < 29254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29254(int& val) {
    const int x = 29254;
    std::cout << 29254;
    for (int i = 0; i < 29254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29255(int& val) {
    func_in_29255(val);
    const int x = 29255;
    std::cout << 29255;
    for (int i = 0; i < 29255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29255(int& val) {
    const int x = 29255;
    std::cout << 29255;
    for (int i = 0; i < 29255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29256(int& val) {
    func_in_29256(val);
    const int x = 29256;
    std::cout << 29256;
    for (int i = 0; i < 29256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29256(int& val) {
    const int x = 29256;
    std::cout << 29256;
    for (int i = 0; i < 29256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29257(int& val) {
    func_in_29257(val);
    const int x = 29257;
    std::cout << 29257;
    for (int i = 0; i < 29257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29257(int& val) {
    const int x = 29257;
    std::cout << 29257;
    for (int i = 0; i < 29257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29258(int& val) {
    func_in_29258(val);
    const int x = 29258;
    std::cout << 29258;
    for (int i = 0; i < 29258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29258(int& val) {
    const int x = 29258;
    std::cout << 29258;
    for (int i = 0; i < 29258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29259(int& val) {
    func_in_29259(val);
    const int x = 29259;
    std::cout << 29259;
    for (int i = 0; i < 29259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29259(int& val) {
    const int x = 29259;
    std::cout << 29259;
    for (int i = 0; i < 29259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29260(int& val) {
    func_in_29260(val);
    const int x = 29260;
    std::cout << 29260;
    for (int i = 0; i < 29260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29260(int& val) {
    const int x = 29260;
    std::cout << 29260;
    for (int i = 0; i < 29260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29261(int& val) {
    func_in_29261(val);
    const int x = 29261;
    std::cout << 29261;
    for (int i = 0; i < 29261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29261(int& val) {
    const int x = 29261;
    std::cout << 29261;
    for (int i = 0; i < 29261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29262(int& val) {
    func_in_29262(val);
    const int x = 29262;
    std::cout << 29262;
    for (int i = 0; i < 29262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29262(int& val) {
    const int x = 29262;
    std::cout << 29262;
    for (int i = 0; i < 29262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29263(int& val) {
    func_in_29263(val);
    const int x = 29263;
    std::cout << 29263;
    for (int i = 0; i < 29263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29263(int& val) {
    const int x = 29263;
    std::cout << 29263;
    for (int i = 0; i < 29263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29264(int& val) {
    func_in_29264(val);
    const int x = 29264;
    std::cout << 29264;
    for (int i = 0; i < 29264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29264(int& val) {
    const int x = 29264;
    std::cout << 29264;
    for (int i = 0; i < 29264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29265(int& val) {
    func_in_29265(val);
    const int x = 29265;
    std::cout << 29265;
    for (int i = 0; i < 29265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29265(int& val) {
    const int x = 29265;
    std::cout << 29265;
    for (int i = 0; i < 29265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29266(int& val) {
    func_in_29266(val);
    const int x = 29266;
    std::cout << 29266;
    for (int i = 0; i < 29266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29266(int& val) {
    const int x = 29266;
    std::cout << 29266;
    for (int i = 0; i < 29266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29267(int& val) {
    func_in_29267(val);
    const int x = 29267;
    std::cout << 29267;
    for (int i = 0; i < 29267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29267(int& val) {
    const int x = 29267;
    std::cout << 29267;
    for (int i = 0; i < 29267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29268(int& val) {
    func_in_29268(val);
    const int x = 29268;
    std::cout << 29268;
    for (int i = 0; i < 29268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29268(int& val) {
    const int x = 29268;
    std::cout << 29268;
    for (int i = 0; i < 29268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29269(int& val) {
    func_in_29269(val);
    const int x = 29269;
    std::cout << 29269;
    for (int i = 0; i < 29269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29269(int& val) {
    const int x = 29269;
    std::cout << 29269;
    for (int i = 0; i < 29269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29270(int& val) {
    func_in_29270(val);
    const int x = 29270;
    std::cout << 29270;
    for (int i = 0; i < 29270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29270(int& val) {
    const int x = 29270;
    std::cout << 29270;
    for (int i = 0; i < 29270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29271(int& val) {
    func_in_29271(val);
    const int x = 29271;
    std::cout << 29271;
    for (int i = 0; i < 29271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29271(int& val) {
    const int x = 29271;
    std::cout << 29271;
    for (int i = 0; i < 29271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29272(int& val) {
    func_in_29272(val);
    const int x = 29272;
    std::cout << 29272;
    for (int i = 0; i < 29272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29272(int& val) {
    const int x = 29272;
    std::cout << 29272;
    for (int i = 0; i < 29272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29273(int& val) {
    func_in_29273(val);
    const int x = 29273;
    std::cout << 29273;
    for (int i = 0; i < 29273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29273(int& val) {
    const int x = 29273;
    std::cout << 29273;
    for (int i = 0; i < 29273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29274(int& val) {
    func_in_29274(val);
    const int x = 29274;
    std::cout << 29274;
    for (int i = 0; i < 29274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29274(int& val) {
    const int x = 29274;
    std::cout << 29274;
    for (int i = 0; i < 29274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29275(int& val) {
    func_in_29275(val);
    const int x = 29275;
    std::cout << 29275;
    for (int i = 0; i < 29275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29275(int& val) {
    const int x = 29275;
    std::cout << 29275;
    for (int i = 0; i < 29275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29276(int& val) {
    func_in_29276(val);
    const int x = 29276;
    std::cout << 29276;
    for (int i = 0; i < 29276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29276(int& val) {
    const int x = 29276;
    std::cout << 29276;
    for (int i = 0; i < 29276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29277(int& val) {
    func_in_29277(val);
    const int x = 29277;
    std::cout << 29277;
    for (int i = 0; i < 29277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29277(int& val) {
    const int x = 29277;
    std::cout << 29277;
    for (int i = 0; i < 29277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29278(int& val) {
    func_in_29278(val);
    const int x = 29278;
    std::cout << 29278;
    for (int i = 0; i < 29278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29278(int& val) {
    const int x = 29278;
    std::cout << 29278;
    for (int i = 0; i < 29278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29279(int& val) {
    func_in_29279(val);
    const int x = 29279;
    std::cout << 29279;
    for (int i = 0; i < 29279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29279(int& val) {
    const int x = 29279;
    std::cout << 29279;
    for (int i = 0; i < 29279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29280(int& val) {
    func_in_29280(val);
    const int x = 29280;
    std::cout << 29280;
    for (int i = 0; i < 29280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29280(int& val) {
    const int x = 29280;
    std::cout << 29280;
    for (int i = 0; i < 29280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29281(int& val) {
    func_in_29281(val);
    const int x = 29281;
    std::cout << 29281;
    for (int i = 0; i < 29281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29281(int& val) {
    const int x = 29281;
    std::cout << 29281;
    for (int i = 0; i < 29281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29282(int& val) {
    func_in_29282(val);
    const int x = 29282;
    std::cout << 29282;
    for (int i = 0; i < 29282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29282(int& val) {
    const int x = 29282;
    std::cout << 29282;
    for (int i = 0; i < 29282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29283(int& val) {
    func_in_29283(val);
    const int x = 29283;
    std::cout << 29283;
    for (int i = 0; i < 29283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29283(int& val) {
    const int x = 29283;
    std::cout << 29283;
    for (int i = 0; i < 29283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29284(int& val) {
    func_in_29284(val);
    const int x = 29284;
    std::cout << 29284;
    for (int i = 0; i < 29284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29284(int& val) {
    const int x = 29284;
    std::cout << 29284;
    for (int i = 0; i < 29284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29285(int& val) {
    func_in_29285(val);
    const int x = 29285;
    std::cout << 29285;
    for (int i = 0; i < 29285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29285(int& val) {
    const int x = 29285;
    std::cout << 29285;
    for (int i = 0; i < 29285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29286(int& val) {
    func_in_29286(val);
    const int x = 29286;
    std::cout << 29286;
    for (int i = 0; i < 29286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29286(int& val) {
    const int x = 29286;
    std::cout << 29286;
    for (int i = 0; i < 29286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29287(int& val) {
    func_in_29287(val);
    const int x = 29287;
    std::cout << 29287;
    for (int i = 0; i < 29287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29287(int& val) {
    const int x = 29287;
    std::cout << 29287;
    for (int i = 0; i < 29287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29288(int& val) {
    func_in_29288(val);
    const int x = 29288;
    std::cout << 29288;
    for (int i = 0; i < 29288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29288(int& val) {
    const int x = 29288;
    std::cout << 29288;
    for (int i = 0; i < 29288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29289(int& val) {
    func_in_29289(val);
    const int x = 29289;
    std::cout << 29289;
    for (int i = 0; i < 29289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29289(int& val) {
    const int x = 29289;
    std::cout << 29289;
    for (int i = 0; i < 29289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29290(int& val) {
    func_in_29290(val);
    const int x = 29290;
    std::cout << 29290;
    for (int i = 0; i < 29290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29290(int& val) {
    const int x = 29290;
    std::cout << 29290;
    for (int i = 0; i < 29290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29291(int& val) {
    func_in_29291(val);
    const int x = 29291;
    std::cout << 29291;
    for (int i = 0; i < 29291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29291(int& val) {
    const int x = 29291;
    std::cout << 29291;
    for (int i = 0; i < 29291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29292(int& val) {
    func_in_29292(val);
    const int x = 29292;
    std::cout << 29292;
    for (int i = 0; i < 29292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29292(int& val) {
    const int x = 29292;
    std::cout << 29292;
    for (int i = 0; i < 29292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29293(int& val) {
    func_in_29293(val);
    const int x = 29293;
    std::cout << 29293;
    for (int i = 0; i < 29293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29293(int& val) {
    const int x = 29293;
    std::cout << 29293;
    for (int i = 0; i < 29293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29294(int& val) {
    func_in_29294(val);
    const int x = 29294;
    std::cout << 29294;
    for (int i = 0; i < 29294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29294(int& val) {
    const int x = 29294;
    std::cout << 29294;
    for (int i = 0; i < 29294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29295(int& val) {
    func_in_29295(val);
    const int x = 29295;
    std::cout << 29295;
    for (int i = 0; i < 29295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29295(int& val) {
    const int x = 29295;
    std::cout << 29295;
    for (int i = 0; i < 29295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29296(int& val) {
    func_in_29296(val);
    const int x = 29296;
    std::cout << 29296;
    for (int i = 0; i < 29296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29296(int& val) {
    const int x = 29296;
    std::cout << 29296;
    for (int i = 0; i < 29296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29297(int& val) {
    func_in_29297(val);
    const int x = 29297;
    std::cout << 29297;
    for (int i = 0; i < 29297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29297(int& val) {
    const int x = 29297;
    std::cout << 29297;
    for (int i = 0; i < 29297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29298(int& val) {
    func_in_29298(val);
    const int x = 29298;
    std::cout << 29298;
    for (int i = 0; i < 29298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29298(int& val) {
    const int x = 29298;
    std::cout << 29298;
    for (int i = 0; i < 29298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29299(int& val) {
    func_in_29299(val);
    const int x = 29299;
    std::cout << 29299;
    for (int i = 0; i < 29299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29299(int& val) {
    const int x = 29299;
    std::cout << 29299;
    for (int i = 0; i < 29299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29300(int& val) {
    func_in_29300(val);
    const int x = 29300;
    std::cout << 29300;
    for (int i = 0; i < 29300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29300(int& val) {
    const int x = 29300;
    std::cout << 29300;
    for (int i = 0; i < 29300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29301(int& val) {
    func_in_29301(val);
    const int x = 29301;
    std::cout << 29301;
    for (int i = 0; i < 29301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29301(int& val) {
    const int x = 29301;
    std::cout << 29301;
    for (int i = 0; i < 29301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29302(int& val) {
    func_in_29302(val);
    const int x = 29302;
    std::cout << 29302;
    for (int i = 0; i < 29302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29302(int& val) {
    const int x = 29302;
    std::cout << 29302;
    for (int i = 0; i < 29302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29303(int& val) {
    func_in_29303(val);
    const int x = 29303;
    std::cout << 29303;
    for (int i = 0; i < 29303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29303(int& val) {
    const int x = 29303;
    std::cout << 29303;
    for (int i = 0; i < 29303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29304(int& val) {
    func_in_29304(val);
    const int x = 29304;
    std::cout << 29304;
    for (int i = 0; i < 29304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29304(int& val) {
    const int x = 29304;
    std::cout << 29304;
    for (int i = 0; i < 29304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29305(int& val) {
    func_in_29305(val);
    const int x = 29305;
    std::cout << 29305;
    for (int i = 0; i < 29305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29305(int& val) {
    const int x = 29305;
    std::cout << 29305;
    for (int i = 0; i < 29305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29306(int& val) {
    func_in_29306(val);
    const int x = 29306;
    std::cout << 29306;
    for (int i = 0; i < 29306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29306(int& val) {
    const int x = 29306;
    std::cout << 29306;
    for (int i = 0; i < 29306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29307(int& val) {
    func_in_29307(val);
    const int x = 29307;
    std::cout << 29307;
    for (int i = 0; i < 29307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29307(int& val) {
    const int x = 29307;
    std::cout << 29307;
    for (int i = 0; i < 29307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29308(int& val) {
    func_in_29308(val);
    const int x = 29308;
    std::cout << 29308;
    for (int i = 0; i < 29308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29308(int& val) {
    const int x = 29308;
    std::cout << 29308;
    for (int i = 0; i < 29308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29309(int& val) {
    func_in_29309(val);
    const int x = 29309;
    std::cout << 29309;
    for (int i = 0; i < 29309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29309(int& val) {
    const int x = 29309;
    std::cout << 29309;
    for (int i = 0; i < 29309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29310(int& val) {
    func_in_29310(val);
    const int x = 29310;
    std::cout << 29310;
    for (int i = 0; i < 29310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29310(int& val) {
    const int x = 29310;
    std::cout << 29310;
    for (int i = 0; i < 29310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29311(int& val) {
    func_in_29311(val);
    const int x = 29311;
    std::cout << 29311;
    for (int i = 0; i < 29311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29311(int& val) {
    const int x = 29311;
    std::cout << 29311;
    for (int i = 0; i < 29311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29312(int& val) {
    func_in_29312(val);
    const int x = 29312;
    std::cout << 29312;
    for (int i = 0; i < 29312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29312(int& val) {
    const int x = 29312;
    std::cout << 29312;
    for (int i = 0; i < 29312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29313(int& val) {
    func_in_29313(val);
    const int x = 29313;
    std::cout << 29313;
    for (int i = 0; i < 29313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29313(int& val) {
    const int x = 29313;
    std::cout << 29313;
    for (int i = 0; i < 29313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29314(int& val) {
    func_in_29314(val);
    const int x = 29314;
    std::cout << 29314;
    for (int i = 0; i < 29314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29314(int& val) {
    const int x = 29314;
    std::cout << 29314;
    for (int i = 0; i < 29314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29315(int& val) {
    func_in_29315(val);
    const int x = 29315;
    std::cout << 29315;
    for (int i = 0; i < 29315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29315(int& val) {
    const int x = 29315;
    std::cout << 29315;
    for (int i = 0; i < 29315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29316(int& val) {
    func_in_29316(val);
    const int x = 29316;
    std::cout << 29316;
    for (int i = 0; i < 29316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29316(int& val) {
    const int x = 29316;
    std::cout << 29316;
    for (int i = 0; i < 29316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29317(int& val) {
    func_in_29317(val);
    const int x = 29317;
    std::cout << 29317;
    for (int i = 0; i < 29317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29317(int& val) {
    const int x = 29317;
    std::cout << 29317;
    for (int i = 0; i < 29317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29318(int& val) {
    func_in_29318(val);
    const int x = 29318;
    std::cout << 29318;
    for (int i = 0; i < 29318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29318(int& val) {
    const int x = 29318;
    std::cout << 29318;
    for (int i = 0; i < 29318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29319(int& val) {
    func_in_29319(val);
    const int x = 29319;
    std::cout << 29319;
    for (int i = 0; i < 29319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29319(int& val) {
    const int x = 29319;
    std::cout << 29319;
    for (int i = 0; i < 29319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29320(int& val) {
    func_in_29320(val);
    const int x = 29320;
    std::cout << 29320;
    for (int i = 0; i < 29320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29320(int& val) {
    const int x = 29320;
    std::cout << 29320;
    for (int i = 0; i < 29320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29321(int& val) {
    func_in_29321(val);
    const int x = 29321;
    std::cout << 29321;
    for (int i = 0; i < 29321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29321(int& val) {
    const int x = 29321;
    std::cout << 29321;
    for (int i = 0; i < 29321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29322(int& val) {
    func_in_29322(val);
    const int x = 29322;
    std::cout << 29322;
    for (int i = 0; i < 29322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29322(int& val) {
    const int x = 29322;
    std::cout << 29322;
    for (int i = 0; i < 29322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29323(int& val) {
    func_in_29323(val);
    const int x = 29323;
    std::cout << 29323;
    for (int i = 0; i < 29323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29323(int& val) {
    const int x = 29323;
    std::cout << 29323;
    for (int i = 0; i < 29323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29324(int& val) {
    func_in_29324(val);
    const int x = 29324;
    std::cout << 29324;
    for (int i = 0; i < 29324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29324(int& val) {
    const int x = 29324;
    std::cout << 29324;
    for (int i = 0; i < 29324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29325(int& val) {
    func_in_29325(val);
    const int x = 29325;
    std::cout << 29325;
    for (int i = 0; i < 29325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29325(int& val) {
    const int x = 29325;
    std::cout << 29325;
    for (int i = 0; i < 29325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29326(int& val) {
    func_in_29326(val);
    const int x = 29326;
    std::cout << 29326;
    for (int i = 0; i < 29326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29326(int& val) {
    const int x = 29326;
    std::cout << 29326;
    for (int i = 0; i < 29326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29327(int& val) {
    func_in_29327(val);
    const int x = 29327;
    std::cout << 29327;
    for (int i = 0; i < 29327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29327(int& val) {
    const int x = 29327;
    std::cout << 29327;
    for (int i = 0; i < 29327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29328(int& val) {
    func_in_29328(val);
    const int x = 29328;
    std::cout << 29328;
    for (int i = 0; i < 29328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29328(int& val) {
    const int x = 29328;
    std::cout << 29328;
    for (int i = 0; i < 29328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29329(int& val) {
    func_in_29329(val);
    const int x = 29329;
    std::cout << 29329;
    for (int i = 0; i < 29329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29329(int& val) {
    const int x = 29329;
    std::cout << 29329;
    for (int i = 0; i < 29329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29330(int& val) {
    func_in_29330(val);
    const int x = 29330;
    std::cout << 29330;
    for (int i = 0; i < 29330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29330(int& val) {
    const int x = 29330;
    std::cout << 29330;
    for (int i = 0; i < 29330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29331(int& val) {
    func_in_29331(val);
    const int x = 29331;
    std::cout << 29331;
    for (int i = 0; i < 29331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29331(int& val) {
    const int x = 29331;
    std::cout << 29331;
    for (int i = 0; i < 29331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29332(int& val) {
    func_in_29332(val);
    const int x = 29332;
    std::cout << 29332;
    for (int i = 0; i < 29332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29332(int& val) {
    const int x = 29332;
    std::cout << 29332;
    for (int i = 0; i < 29332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29333(int& val) {
    func_in_29333(val);
    const int x = 29333;
    std::cout << 29333;
    for (int i = 0; i < 29333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29333(int& val) {
    const int x = 29333;
    std::cout << 29333;
    for (int i = 0; i < 29333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29334(int& val) {
    func_in_29334(val);
    const int x = 29334;
    std::cout << 29334;
    for (int i = 0; i < 29334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29334(int& val) {
    const int x = 29334;
    std::cout << 29334;
    for (int i = 0; i < 29334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29335(int& val) {
    func_in_29335(val);
    const int x = 29335;
    std::cout << 29335;
    for (int i = 0; i < 29335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29335(int& val) {
    const int x = 29335;
    std::cout << 29335;
    for (int i = 0; i < 29335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29336(int& val) {
    func_in_29336(val);
    const int x = 29336;
    std::cout << 29336;
    for (int i = 0; i < 29336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29336(int& val) {
    const int x = 29336;
    std::cout << 29336;
    for (int i = 0; i < 29336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29337(int& val) {
    func_in_29337(val);
    const int x = 29337;
    std::cout << 29337;
    for (int i = 0; i < 29337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29337(int& val) {
    const int x = 29337;
    std::cout << 29337;
    for (int i = 0; i < 29337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29338(int& val) {
    func_in_29338(val);
    const int x = 29338;
    std::cout << 29338;
    for (int i = 0; i < 29338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29338(int& val) {
    const int x = 29338;
    std::cout << 29338;
    for (int i = 0; i < 29338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29339(int& val) {
    func_in_29339(val);
    const int x = 29339;
    std::cout << 29339;
    for (int i = 0; i < 29339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29339(int& val) {
    const int x = 29339;
    std::cout << 29339;
    for (int i = 0; i < 29339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29340(int& val) {
    func_in_29340(val);
    const int x = 29340;
    std::cout << 29340;
    for (int i = 0; i < 29340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29340(int& val) {
    const int x = 29340;
    std::cout << 29340;
    for (int i = 0; i < 29340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29341(int& val) {
    func_in_29341(val);
    const int x = 29341;
    std::cout << 29341;
    for (int i = 0; i < 29341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29341(int& val) {
    const int x = 29341;
    std::cout << 29341;
    for (int i = 0; i < 29341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29342(int& val) {
    func_in_29342(val);
    const int x = 29342;
    std::cout << 29342;
    for (int i = 0; i < 29342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29342(int& val) {
    const int x = 29342;
    std::cout << 29342;
    for (int i = 0; i < 29342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29343(int& val) {
    func_in_29343(val);
    const int x = 29343;
    std::cout << 29343;
    for (int i = 0; i < 29343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29343(int& val) {
    const int x = 29343;
    std::cout << 29343;
    for (int i = 0; i < 29343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29344(int& val) {
    func_in_29344(val);
    const int x = 29344;
    std::cout << 29344;
    for (int i = 0; i < 29344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29344(int& val) {
    const int x = 29344;
    std::cout << 29344;
    for (int i = 0; i < 29344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29345(int& val) {
    func_in_29345(val);
    const int x = 29345;
    std::cout << 29345;
    for (int i = 0; i < 29345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29345(int& val) {
    const int x = 29345;
    std::cout << 29345;
    for (int i = 0; i < 29345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29346(int& val) {
    func_in_29346(val);
    const int x = 29346;
    std::cout << 29346;
    for (int i = 0; i < 29346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29346(int& val) {
    const int x = 29346;
    std::cout << 29346;
    for (int i = 0; i < 29346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29347(int& val) {
    func_in_29347(val);
    const int x = 29347;
    std::cout << 29347;
    for (int i = 0; i < 29347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29347(int& val) {
    const int x = 29347;
    std::cout << 29347;
    for (int i = 0; i < 29347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29348(int& val) {
    func_in_29348(val);
    const int x = 29348;
    std::cout << 29348;
    for (int i = 0; i < 29348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29348(int& val) {
    const int x = 29348;
    std::cout << 29348;
    for (int i = 0; i < 29348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29349(int& val) {
    func_in_29349(val);
    const int x = 29349;
    std::cout << 29349;
    for (int i = 0; i < 29349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29349(int& val) {
    const int x = 29349;
    std::cout << 29349;
    for (int i = 0; i < 29349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29350(int& val) {
    func_in_29350(val);
    const int x = 29350;
    std::cout << 29350;
    for (int i = 0; i < 29350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29350(int& val) {
    const int x = 29350;
    std::cout << 29350;
    for (int i = 0; i < 29350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29351(int& val) {
    func_in_29351(val);
    const int x = 29351;
    std::cout << 29351;
    for (int i = 0; i < 29351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29351(int& val) {
    const int x = 29351;
    std::cout << 29351;
    for (int i = 0; i < 29351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29352(int& val) {
    func_in_29352(val);
    const int x = 29352;
    std::cout << 29352;
    for (int i = 0; i < 29352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29352(int& val) {
    const int x = 29352;
    std::cout << 29352;
    for (int i = 0; i < 29352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29353(int& val) {
    func_in_29353(val);
    const int x = 29353;
    std::cout << 29353;
    for (int i = 0; i < 29353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29353(int& val) {
    const int x = 29353;
    std::cout << 29353;
    for (int i = 0; i < 29353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29354(int& val) {
    func_in_29354(val);
    const int x = 29354;
    std::cout << 29354;
    for (int i = 0; i < 29354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29354(int& val) {
    const int x = 29354;
    std::cout << 29354;
    for (int i = 0; i < 29354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29355(int& val) {
    func_in_29355(val);
    const int x = 29355;
    std::cout << 29355;
    for (int i = 0; i < 29355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29355(int& val) {
    const int x = 29355;
    std::cout << 29355;
    for (int i = 0; i < 29355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29356(int& val) {
    func_in_29356(val);
    const int x = 29356;
    std::cout << 29356;
    for (int i = 0; i < 29356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29356(int& val) {
    const int x = 29356;
    std::cout << 29356;
    for (int i = 0; i < 29356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29357(int& val) {
    func_in_29357(val);
    const int x = 29357;
    std::cout << 29357;
    for (int i = 0; i < 29357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29357(int& val) {
    const int x = 29357;
    std::cout << 29357;
    for (int i = 0; i < 29357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29358(int& val) {
    func_in_29358(val);
    const int x = 29358;
    std::cout << 29358;
    for (int i = 0; i < 29358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29358(int& val) {
    const int x = 29358;
    std::cout << 29358;
    for (int i = 0; i < 29358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29359(int& val) {
    func_in_29359(val);
    const int x = 29359;
    std::cout << 29359;
    for (int i = 0; i < 29359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29359(int& val) {
    const int x = 29359;
    std::cout << 29359;
    for (int i = 0; i < 29359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29360(int& val) {
    func_in_29360(val);
    const int x = 29360;
    std::cout << 29360;
    for (int i = 0; i < 29360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29360(int& val) {
    const int x = 29360;
    std::cout << 29360;
    for (int i = 0; i < 29360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29361(int& val) {
    func_in_29361(val);
    const int x = 29361;
    std::cout << 29361;
    for (int i = 0; i < 29361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29361(int& val) {
    const int x = 29361;
    std::cout << 29361;
    for (int i = 0; i < 29361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29362(int& val) {
    func_in_29362(val);
    const int x = 29362;
    std::cout << 29362;
    for (int i = 0; i < 29362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29362(int& val) {
    const int x = 29362;
    std::cout << 29362;
    for (int i = 0; i < 29362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29363(int& val) {
    func_in_29363(val);
    const int x = 29363;
    std::cout << 29363;
    for (int i = 0; i < 29363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29363(int& val) {
    const int x = 29363;
    std::cout << 29363;
    for (int i = 0; i < 29363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29364(int& val) {
    func_in_29364(val);
    const int x = 29364;
    std::cout << 29364;
    for (int i = 0; i < 29364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29364(int& val) {
    const int x = 29364;
    std::cout << 29364;
    for (int i = 0; i < 29364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29365(int& val) {
    func_in_29365(val);
    const int x = 29365;
    std::cout << 29365;
    for (int i = 0; i < 29365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29365(int& val) {
    const int x = 29365;
    std::cout << 29365;
    for (int i = 0; i < 29365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29366(int& val) {
    func_in_29366(val);
    const int x = 29366;
    std::cout << 29366;
    for (int i = 0; i < 29366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29366(int& val) {
    const int x = 29366;
    std::cout << 29366;
    for (int i = 0; i < 29366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29367(int& val) {
    func_in_29367(val);
    const int x = 29367;
    std::cout << 29367;
    for (int i = 0; i < 29367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29367(int& val) {
    const int x = 29367;
    std::cout << 29367;
    for (int i = 0; i < 29367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29368(int& val) {
    func_in_29368(val);
    const int x = 29368;
    std::cout << 29368;
    for (int i = 0; i < 29368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29368(int& val) {
    const int x = 29368;
    std::cout << 29368;
    for (int i = 0; i < 29368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29369(int& val) {
    func_in_29369(val);
    const int x = 29369;
    std::cout << 29369;
    for (int i = 0; i < 29369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29369(int& val) {
    const int x = 29369;
    std::cout << 29369;
    for (int i = 0; i < 29369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29370(int& val) {
    func_in_29370(val);
    const int x = 29370;
    std::cout << 29370;
    for (int i = 0; i < 29370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29370(int& val) {
    const int x = 29370;
    std::cout << 29370;
    for (int i = 0; i < 29370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29371(int& val) {
    func_in_29371(val);
    const int x = 29371;
    std::cout << 29371;
    for (int i = 0; i < 29371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29371(int& val) {
    const int x = 29371;
    std::cout << 29371;
    for (int i = 0; i < 29371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29372(int& val) {
    func_in_29372(val);
    const int x = 29372;
    std::cout << 29372;
    for (int i = 0; i < 29372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29372(int& val) {
    const int x = 29372;
    std::cout << 29372;
    for (int i = 0; i < 29372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29373(int& val) {
    func_in_29373(val);
    const int x = 29373;
    std::cout << 29373;
    for (int i = 0; i < 29373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29373(int& val) {
    const int x = 29373;
    std::cout << 29373;
    for (int i = 0; i < 29373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29374(int& val) {
    func_in_29374(val);
    const int x = 29374;
    std::cout << 29374;
    for (int i = 0; i < 29374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29374(int& val) {
    const int x = 29374;
    std::cout << 29374;
    for (int i = 0; i < 29374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29375(int& val) {
    func_in_29375(val);
    const int x = 29375;
    std::cout << 29375;
    for (int i = 0; i < 29375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29375(int& val) {
    const int x = 29375;
    std::cout << 29375;
    for (int i = 0; i < 29375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29376(int& val) {
    func_in_29376(val);
    const int x = 29376;
    std::cout << 29376;
    for (int i = 0; i < 29376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29376(int& val) {
    const int x = 29376;
    std::cout << 29376;
    for (int i = 0; i < 29376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29377(int& val) {
    func_in_29377(val);
    const int x = 29377;
    std::cout << 29377;
    for (int i = 0; i < 29377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29377(int& val) {
    const int x = 29377;
    std::cout << 29377;
    for (int i = 0; i < 29377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29378(int& val) {
    func_in_29378(val);
    const int x = 29378;
    std::cout << 29378;
    for (int i = 0; i < 29378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29378(int& val) {
    const int x = 29378;
    std::cout << 29378;
    for (int i = 0; i < 29378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29379(int& val) {
    func_in_29379(val);
    const int x = 29379;
    std::cout << 29379;
    for (int i = 0; i < 29379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29379(int& val) {
    const int x = 29379;
    std::cout << 29379;
    for (int i = 0; i < 29379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29380(int& val) {
    func_in_29380(val);
    const int x = 29380;
    std::cout << 29380;
    for (int i = 0; i < 29380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29380(int& val) {
    const int x = 29380;
    std::cout << 29380;
    for (int i = 0; i < 29380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29381(int& val) {
    func_in_29381(val);
    const int x = 29381;
    std::cout << 29381;
    for (int i = 0; i < 29381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29381(int& val) {
    const int x = 29381;
    std::cout << 29381;
    for (int i = 0; i < 29381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29382(int& val) {
    func_in_29382(val);
    const int x = 29382;
    std::cout << 29382;
    for (int i = 0; i < 29382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29382(int& val) {
    const int x = 29382;
    std::cout << 29382;
    for (int i = 0; i < 29382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29383(int& val) {
    func_in_29383(val);
    const int x = 29383;
    std::cout << 29383;
    for (int i = 0; i < 29383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29383(int& val) {
    const int x = 29383;
    std::cout << 29383;
    for (int i = 0; i < 29383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29384(int& val) {
    func_in_29384(val);
    const int x = 29384;
    std::cout << 29384;
    for (int i = 0; i < 29384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29384(int& val) {
    const int x = 29384;
    std::cout << 29384;
    for (int i = 0; i < 29384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29385(int& val) {
    func_in_29385(val);
    const int x = 29385;
    std::cout << 29385;
    for (int i = 0; i < 29385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29385(int& val) {
    const int x = 29385;
    std::cout << 29385;
    for (int i = 0; i < 29385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29386(int& val) {
    func_in_29386(val);
    const int x = 29386;
    std::cout << 29386;
    for (int i = 0; i < 29386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29386(int& val) {
    const int x = 29386;
    std::cout << 29386;
    for (int i = 0; i < 29386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29387(int& val) {
    func_in_29387(val);
    const int x = 29387;
    std::cout << 29387;
    for (int i = 0; i < 29387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29387(int& val) {
    const int x = 29387;
    std::cout << 29387;
    for (int i = 0; i < 29387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29388(int& val) {
    func_in_29388(val);
    const int x = 29388;
    std::cout << 29388;
    for (int i = 0; i < 29388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29388(int& val) {
    const int x = 29388;
    std::cout << 29388;
    for (int i = 0; i < 29388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29389(int& val) {
    func_in_29389(val);
    const int x = 29389;
    std::cout << 29389;
    for (int i = 0; i < 29389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29389(int& val) {
    const int x = 29389;
    std::cout << 29389;
    for (int i = 0; i < 29389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29390(int& val) {
    func_in_29390(val);
    const int x = 29390;
    std::cout << 29390;
    for (int i = 0; i < 29390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29390(int& val) {
    const int x = 29390;
    std::cout << 29390;
    for (int i = 0; i < 29390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29391(int& val) {
    func_in_29391(val);
    const int x = 29391;
    std::cout << 29391;
    for (int i = 0; i < 29391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29391(int& val) {
    const int x = 29391;
    std::cout << 29391;
    for (int i = 0; i < 29391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29392(int& val) {
    func_in_29392(val);
    const int x = 29392;
    std::cout << 29392;
    for (int i = 0; i < 29392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29392(int& val) {
    const int x = 29392;
    std::cout << 29392;
    for (int i = 0; i < 29392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29393(int& val) {
    func_in_29393(val);
    const int x = 29393;
    std::cout << 29393;
    for (int i = 0; i < 29393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29393(int& val) {
    const int x = 29393;
    std::cout << 29393;
    for (int i = 0; i < 29393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29394(int& val) {
    func_in_29394(val);
    const int x = 29394;
    std::cout << 29394;
    for (int i = 0; i < 29394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29394(int& val) {
    const int x = 29394;
    std::cout << 29394;
    for (int i = 0; i < 29394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29395(int& val) {
    func_in_29395(val);
    const int x = 29395;
    std::cout << 29395;
    for (int i = 0; i < 29395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29395(int& val) {
    const int x = 29395;
    std::cout << 29395;
    for (int i = 0; i < 29395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29396(int& val) {
    func_in_29396(val);
    const int x = 29396;
    std::cout << 29396;
    for (int i = 0; i < 29396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29396(int& val) {
    const int x = 29396;
    std::cout << 29396;
    for (int i = 0; i < 29396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29397(int& val) {
    func_in_29397(val);
    const int x = 29397;
    std::cout << 29397;
    for (int i = 0; i < 29397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29397(int& val) {
    const int x = 29397;
    std::cout << 29397;
    for (int i = 0; i < 29397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29398(int& val) {
    func_in_29398(val);
    const int x = 29398;
    std::cout << 29398;
    for (int i = 0; i < 29398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29398(int& val) {
    const int x = 29398;
    std::cout << 29398;
    for (int i = 0; i < 29398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29399(int& val) {
    func_in_29399(val);
    const int x = 29399;
    std::cout << 29399;
    for (int i = 0; i < 29399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29399(int& val) {
    const int x = 29399;
    std::cout << 29399;
    for (int i = 0; i < 29399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29400(int& val) {
    func_in_29400(val);
    const int x = 29400;
    std::cout << 29400;
    for (int i = 0; i < 29400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29400(int& val) {
    const int x = 29400;
    std::cout << 29400;
    for (int i = 0; i < 29400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29401(int& val) {
    func_in_29401(val);
    const int x = 29401;
    std::cout << 29401;
    for (int i = 0; i < 29401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29401(int& val) {
    const int x = 29401;
    std::cout << 29401;
    for (int i = 0; i < 29401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29402(int& val) {
    func_in_29402(val);
    const int x = 29402;
    std::cout << 29402;
    for (int i = 0; i < 29402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29402(int& val) {
    const int x = 29402;
    std::cout << 29402;
    for (int i = 0; i < 29402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29403(int& val) {
    func_in_29403(val);
    const int x = 29403;
    std::cout << 29403;
    for (int i = 0; i < 29403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29403(int& val) {
    const int x = 29403;
    std::cout << 29403;
    for (int i = 0; i < 29403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29404(int& val) {
    func_in_29404(val);
    const int x = 29404;
    std::cout << 29404;
    for (int i = 0; i < 29404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29404(int& val) {
    const int x = 29404;
    std::cout << 29404;
    for (int i = 0; i < 29404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29405(int& val) {
    func_in_29405(val);
    const int x = 29405;
    std::cout << 29405;
    for (int i = 0; i < 29405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29405(int& val) {
    const int x = 29405;
    std::cout << 29405;
    for (int i = 0; i < 29405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29406(int& val) {
    func_in_29406(val);
    const int x = 29406;
    std::cout << 29406;
    for (int i = 0; i < 29406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29406(int& val) {
    const int x = 29406;
    std::cout << 29406;
    for (int i = 0; i < 29406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29407(int& val) {
    func_in_29407(val);
    const int x = 29407;
    std::cout << 29407;
    for (int i = 0; i < 29407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29407(int& val) {
    const int x = 29407;
    std::cout << 29407;
    for (int i = 0; i < 29407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29408(int& val) {
    func_in_29408(val);
    const int x = 29408;
    std::cout << 29408;
    for (int i = 0; i < 29408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29408(int& val) {
    const int x = 29408;
    std::cout << 29408;
    for (int i = 0; i < 29408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29409(int& val) {
    func_in_29409(val);
    const int x = 29409;
    std::cout << 29409;
    for (int i = 0; i < 29409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29409(int& val) {
    const int x = 29409;
    std::cout << 29409;
    for (int i = 0; i < 29409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29410(int& val) {
    func_in_29410(val);
    const int x = 29410;
    std::cout << 29410;
    for (int i = 0; i < 29410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29410(int& val) {
    const int x = 29410;
    std::cout << 29410;
    for (int i = 0; i < 29410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29411(int& val) {
    func_in_29411(val);
    const int x = 29411;
    std::cout << 29411;
    for (int i = 0; i < 29411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29411(int& val) {
    const int x = 29411;
    std::cout << 29411;
    for (int i = 0; i < 29411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29412(int& val) {
    func_in_29412(val);
    const int x = 29412;
    std::cout << 29412;
    for (int i = 0; i < 29412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29412(int& val) {
    const int x = 29412;
    std::cout << 29412;
    for (int i = 0; i < 29412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29413(int& val) {
    func_in_29413(val);
    const int x = 29413;
    std::cout << 29413;
    for (int i = 0; i < 29413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29413(int& val) {
    const int x = 29413;
    std::cout << 29413;
    for (int i = 0; i < 29413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29414(int& val) {
    func_in_29414(val);
    const int x = 29414;
    std::cout << 29414;
    for (int i = 0; i < 29414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29414(int& val) {
    const int x = 29414;
    std::cout << 29414;
    for (int i = 0; i < 29414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29415(int& val) {
    func_in_29415(val);
    const int x = 29415;
    std::cout << 29415;
    for (int i = 0; i < 29415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29415(int& val) {
    const int x = 29415;
    std::cout << 29415;
    for (int i = 0; i < 29415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29416(int& val) {
    func_in_29416(val);
    const int x = 29416;
    std::cout << 29416;
    for (int i = 0; i < 29416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29416(int& val) {
    const int x = 29416;
    std::cout << 29416;
    for (int i = 0; i < 29416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29417(int& val) {
    func_in_29417(val);
    const int x = 29417;
    std::cout << 29417;
    for (int i = 0; i < 29417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29417(int& val) {
    const int x = 29417;
    std::cout << 29417;
    for (int i = 0; i < 29417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29418(int& val) {
    func_in_29418(val);
    const int x = 29418;
    std::cout << 29418;
    for (int i = 0; i < 29418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29418(int& val) {
    const int x = 29418;
    std::cout << 29418;
    for (int i = 0; i < 29418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29419(int& val) {
    func_in_29419(val);
    const int x = 29419;
    std::cout << 29419;
    for (int i = 0; i < 29419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29419(int& val) {
    const int x = 29419;
    std::cout << 29419;
    for (int i = 0; i < 29419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29420(int& val) {
    func_in_29420(val);
    const int x = 29420;
    std::cout << 29420;
    for (int i = 0; i < 29420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29420(int& val) {
    const int x = 29420;
    std::cout << 29420;
    for (int i = 0; i < 29420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29421(int& val) {
    func_in_29421(val);
    const int x = 29421;
    std::cout << 29421;
    for (int i = 0; i < 29421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29421(int& val) {
    const int x = 29421;
    std::cout << 29421;
    for (int i = 0; i < 29421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29422(int& val) {
    func_in_29422(val);
    const int x = 29422;
    std::cout << 29422;
    for (int i = 0; i < 29422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29422(int& val) {
    const int x = 29422;
    std::cout << 29422;
    for (int i = 0; i < 29422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29423(int& val) {
    func_in_29423(val);
    const int x = 29423;
    std::cout << 29423;
    for (int i = 0; i < 29423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29423(int& val) {
    const int x = 29423;
    std::cout << 29423;
    for (int i = 0; i < 29423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29424(int& val) {
    func_in_29424(val);
    const int x = 29424;
    std::cout << 29424;
    for (int i = 0; i < 29424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29424(int& val) {
    const int x = 29424;
    std::cout << 29424;
    for (int i = 0; i < 29424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29425(int& val) {
    func_in_29425(val);
    const int x = 29425;
    std::cout << 29425;
    for (int i = 0; i < 29425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29425(int& val) {
    const int x = 29425;
    std::cout << 29425;
    for (int i = 0; i < 29425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29426(int& val) {
    func_in_29426(val);
    const int x = 29426;
    std::cout << 29426;
    for (int i = 0; i < 29426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29426(int& val) {
    const int x = 29426;
    std::cout << 29426;
    for (int i = 0; i < 29426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29427(int& val) {
    func_in_29427(val);
    const int x = 29427;
    std::cout << 29427;
    for (int i = 0; i < 29427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29427(int& val) {
    const int x = 29427;
    std::cout << 29427;
    for (int i = 0; i < 29427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29428(int& val) {
    func_in_29428(val);
    const int x = 29428;
    std::cout << 29428;
    for (int i = 0; i < 29428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29428(int& val) {
    const int x = 29428;
    std::cout << 29428;
    for (int i = 0; i < 29428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29429(int& val) {
    func_in_29429(val);
    const int x = 29429;
    std::cout << 29429;
    for (int i = 0; i < 29429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29429(int& val) {
    const int x = 29429;
    std::cout << 29429;
    for (int i = 0; i < 29429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29430(int& val) {
    func_in_29430(val);
    const int x = 29430;
    std::cout << 29430;
    for (int i = 0; i < 29430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29430(int& val) {
    const int x = 29430;
    std::cout << 29430;
    for (int i = 0; i < 29430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29431(int& val) {
    func_in_29431(val);
    const int x = 29431;
    std::cout << 29431;
    for (int i = 0; i < 29431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29431(int& val) {
    const int x = 29431;
    std::cout << 29431;
    for (int i = 0; i < 29431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29432(int& val) {
    func_in_29432(val);
    const int x = 29432;
    std::cout << 29432;
    for (int i = 0; i < 29432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29432(int& val) {
    const int x = 29432;
    std::cout << 29432;
    for (int i = 0; i < 29432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29433(int& val) {
    func_in_29433(val);
    const int x = 29433;
    std::cout << 29433;
    for (int i = 0; i < 29433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29433(int& val) {
    const int x = 29433;
    std::cout << 29433;
    for (int i = 0; i < 29433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29434(int& val) {
    func_in_29434(val);
    const int x = 29434;
    std::cout << 29434;
    for (int i = 0; i < 29434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29434(int& val) {
    const int x = 29434;
    std::cout << 29434;
    for (int i = 0; i < 29434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29435(int& val) {
    func_in_29435(val);
    const int x = 29435;
    std::cout << 29435;
    for (int i = 0; i < 29435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29435(int& val) {
    const int x = 29435;
    std::cout << 29435;
    for (int i = 0; i < 29435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29436(int& val) {
    func_in_29436(val);
    const int x = 29436;
    std::cout << 29436;
    for (int i = 0; i < 29436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29436(int& val) {
    const int x = 29436;
    std::cout << 29436;
    for (int i = 0; i < 29436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29437(int& val) {
    func_in_29437(val);
    const int x = 29437;
    std::cout << 29437;
    for (int i = 0; i < 29437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29437(int& val) {
    const int x = 29437;
    std::cout << 29437;
    for (int i = 0; i < 29437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29438(int& val) {
    func_in_29438(val);
    const int x = 29438;
    std::cout << 29438;
    for (int i = 0; i < 29438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29438(int& val) {
    const int x = 29438;
    std::cout << 29438;
    for (int i = 0; i < 29438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29439(int& val) {
    func_in_29439(val);
    const int x = 29439;
    std::cout << 29439;
    for (int i = 0; i < 29439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29439(int& val) {
    const int x = 29439;
    std::cout << 29439;
    for (int i = 0; i < 29439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29440(int& val) {
    func_in_29440(val);
    const int x = 29440;
    std::cout << 29440;
    for (int i = 0; i < 29440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29440(int& val) {
    const int x = 29440;
    std::cout << 29440;
    for (int i = 0; i < 29440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29441(int& val) {
    func_in_29441(val);
    const int x = 29441;
    std::cout << 29441;
    for (int i = 0; i < 29441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29441(int& val) {
    const int x = 29441;
    std::cout << 29441;
    for (int i = 0; i < 29441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29442(int& val) {
    func_in_29442(val);
    const int x = 29442;
    std::cout << 29442;
    for (int i = 0; i < 29442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29442(int& val) {
    const int x = 29442;
    std::cout << 29442;
    for (int i = 0; i < 29442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29443(int& val) {
    func_in_29443(val);
    const int x = 29443;
    std::cout << 29443;
    for (int i = 0; i < 29443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29443(int& val) {
    const int x = 29443;
    std::cout << 29443;
    for (int i = 0; i < 29443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29444(int& val) {
    func_in_29444(val);
    const int x = 29444;
    std::cout << 29444;
    for (int i = 0; i < 29444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29444(int& val) {
    const int x = 29444;
    std::cout << 29444;
    for (int i = 0; i < 29444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29445(int& val) {
    func_in_29445(val);
    const int x = 29445;
    std::cout << 29445;
    for (int i = 0; i < 29445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29445(int& val) {
    const int x = 29445;
    std::cout << 29445;
    for (int i = 0; i < 29445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29446(int& val) {
    func_in_29446(val);
    const int x = 29446;
    std::cout << 29446;
    for (int i = 0; i < 29446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29446(int& val) {
    const int x = 29446;
    std::cout << 29446;
    for (int i = 0; i < 29446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29447(int& val) {
    func_in_29447(val);
    const int x = 29447;
    std::cout << 29447;
    for (int i = 0; i < 29447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29447(int& val) {
    const int x = 29447;
    std::cout << 29447;
    for (int i = 0; i < 29447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29448(int& val) {
    func_in_29448(val);
    const int x = 29448;
    std::cout << 29448;
    for (int i = 0; i < 29448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29448(int& val) {
    const int x = 29448;
    std::cout << 29448;
    for (int i = 0; i < 29448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29449(int& val) {
    func_in_29449(val);
    const int x = 29449;
    std::cout << 29449;
    for (int i = 0; i < 29449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29449(int& val) {
    const int x = 29449;
    std::cout << 29449;
    for (int i = 0; i < 29449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29450(int& val) {
    func_in_29450(val);
    const int x = 29450;
    std::cout << 29450;
    for (int i = 0; i < 29450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29450(int& val) {
    const int x = 29450;
    std::cout << 29450;
    for (int i = 0; i < 29450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29451(int& val) {
    func_in_29451(val);
    const int x = 29451;
    std::cout << 29451;
    for (int i = 0; i < 29451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29451(int& val) {
    const int x = 29451;
    std::cout << 29451;
    for (int i = 0; i < 29451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29452(int& val) {
    func_in_29452(val);
    const int x = 29452;
    std::cout << 29452;
    for (int i = 0; i < 29452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29452(int& val) {
    const int x = 29452;
    std::cout << 29452;
    for (int i = 0; i < 29452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29453(int& val) {
    func_in_29453(val);
    const int x = 29453;
    std::cout << 29453;
    for (int i = 0; i < 29453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29453(int& val) {
    const int x = 29453;
    std::cout << 29453;
    for (int i = 0; i < 29453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29454(int& val) {
    func_in_29454(val);
    const int x = 29454;
    std::cout << 29454;
    for (int i = 0; i < 29454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29454(int& val) {
    const int x = 29454;
    std::cout << 29454;
    for (int i = 0; i < 29454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29455(int& val) {
    func_in_29455(val);
    const int x = 29455;
    std::cout << 29455;
    for (int i = 0; i < 29455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29455(int& val) {
    const int x = 29455;
    std::cout << 29455;
    for (int i = 0; i < 29455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29456(int& val) {
    func_in_29456(val);
    const int x = 29456;
    std::cout << 29456;
    for (int i = 0; i < 29456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29456(int& val) {
    const int x = 29456;
    std::cout << 29456;
    for (int i = 0; i < 29456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29457(int& val) {
    func_in_29457(val);
    const int x = 29457;
    std::cout << 29457;
    for (int i = 0; i < 29457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29457(int& val) {
    const int x = 29457;
    std::cout << 29457;
    for (int i = 0; i < 29457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29458(int& val) {
    func_in_29458(val);
    const int x = 29458;
    std::cout << 29458;
    for (int i = 0; i < 29458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29458(int& val) {
    const int x = 29458;
    std::cout << 29458;
    for (int i = 0; i < 29458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29459(int& val) {
    func_in_29459(val);
    const int x = 29459;
    std::cout << 29459;
    for (int i = 0; i < 29459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29459(int& val) {
    const int x = 29459;
    std::cout << 29459;
    for (int i = 0; i < 29459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29460(int& val) {
    func_in_29460(val);
    const int x = 29460;
    std::cout << 29460;
    for (int i = 0; i < 29460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29460(int& val) {
    const int x = 29460;
    std::cout << 29460;
    for (int i = 0; i < 29460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29461(int& val) {
    func_in_29461(val);
    const int x = 29461;
    std::cout << 29461;
    for (int i = 0; i < 29461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29461(int& val) {
    const int x = 29461;
    std::cout << 29461;
    for (int i = 0; i < 29461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29462(int& val) {
    func_in_29462(val);
    const int x = 29462;
    std::cout << 29462;
    for (int i = 0; i < 29462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29462(int& val) {
    const int x = 29462;
    std::cout << 29462;
    for (int i = 0; i < 29462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29463(int& val) {
    func_in_29463(val);
    const int x = 29463;
    std::cout << 29463;
    for (int i = 0; i < 29463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29463(int& val) {
    const int x = 29463;
    std::cout << 29463;
    for (int i = 0; i < 29463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29464(int& val) {
    func_in_29464(val);
    const int x = 29464;
    std::cout << 29464;
    for (int i = 0; i < 29464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29464(int& val) {
    const int x = 29464;
    std::cout << 29464;
    for (int i = 0; i < 29464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29465(int& val) {
    func_in_29465(val);
    const int x = 29465;
    std::cout << 29465;
    for (int i = 0; i < 29465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29465(int& val) {
    const int x = 29465;
    std::cout << 29465;
    for (int i = 0; i < 29465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29466(int& val) {
    func_in_29466(val);
    const int x = 29466;
    std::cout << 29466;
    for (int i = 0; i < 29466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29466(int& val) {
    const int x = 29466;
    std::cout << 29466;
    for (int i = 0; i < 29466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29467(int& val) {
    func_in_29467(val);
    const int x = 29467;
    std::cout << 29467;
    for (int i = 0; i < 29467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29467(int& val) {
    const int x = 29467;
    std::cout << 29467;
    for (int i = 0; i < 29467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29468(int& val) {
    func_in_29468(val);
    const int x = 29468;
    std::cout << 29468;
    for (int i = 0; i < 29468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29468(int& val) {
    const int x = 29468;
    std::cout << 29468;
    for (int i = 0; i < 29468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29469(int& val) {
    func_in_29469(val);
    const int x = 29469;
    std::cout << 29469;
    for (int i = 0; i < 29469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29469(int& val) {
    const int x = 29469;
    std::cout << 29469;
    for (int i = 0; i < 29469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29470(int& val) {
    func_in_29470(val);
    const int x = 29470;
    std::cout << 29470;
    for (int i = 0; i < 29470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29470(int& val) {
    const int x = 29470;
    std::cout << 29470;
    for (int i = 0; i < 29470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29471(int& val) {
    func_in_29471(val);
    const int x = 29471;
    std::cout << 29471;
    for (int i = 0; i < 29471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29471(int& val) {
    const int x = 29471;
    std::cout << 29471;
    for (int i = 0; i < 29471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29472(int& val) {
    func_in_29472(val);
    const int x = 29472;
    std::cout << 29472;
    for (int i = 0; i < 29472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29472(int& val) {
    const int x = 29472;
    std::cout << 29472;
    for (int i = 0; i < 29472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29473(int& val) {
    func_in_29473(val);
    const int x = 29473;
    std::cout << 29473;
    for (int i = 0; i < 29473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29473(int& val) {
    const int x = 29473;
    std::cout << 29473;
    for (int i = 0; i < 29473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29474(int& val) {
    func_in_29474(val);
    const int x = 29474;
    std::cout << 29474;
    for (int i = 0; i < 29474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29474(int& val) {
    const int x = 29474;
    std::cout << 29474;
    for (int i = 0; i < 29474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29475(int& val) {
    func_in_29475(val);
    const int x = 29475;
    std::cout << 29475;
    for (int i = 0; i < 29475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29475(int& val) {
    const int x = 29475;
    std::cout << 29475;
    for (int i = 0; i < 29475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29476(int& val) {
    func_in_29476(val);
    const int x = 29476;
    std::cout << 29476;
    for (int i = 0; i < 29476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29476(int& val) {
    const int x = 29476;
    std::cout << 29476;
    for (int i = 0; i < 29476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29477(int& val) {
    func_in_29477(val);
    const int x = 29477;
    std::cout << 29477;
    for (int i = 0; i < 29477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29477(int& val) {
    const int x = 29477;
    std::cout << 29477;
    for (int i = 0; i < 29477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29478(int& val) {
    func_in_29478(val);
    const int x = 29478;
    std::cout << 29478;
    for (int i = 0; i < 29478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29478(int& val) {
    const int x = 29478;
    std::cout << 29478;
    for (int i = 0; i < 29478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29479(int& val) {
    func_in_29479(val);
    const int x = 29479;
    std::cout << 29479;
    for (int i = 0; i < 29479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29479(int& val) {
    const int x = 29479;
    std::cout << 29479;
    for (int i = 0; i < 29479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29480(int& val) {
    func_in_29480(val);
    const int x = 29480;
    std::cout << 29480;
    for (int i = 0; i < 29480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29480(int& val) {
    const int x = 29480;
    std::cout << 29480;
    for (int i = 0; i < 29480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29481(int& val) {
    func_in_29481(val);
    const int x = 29481;
    std::cout << 29481;
    for (int i = 0; i < 29481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29481(int& val) {
    const int x = 29481;
    std::cout << 29481;
    for (int i = 0; i < 29481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29482(int& val) {
    func_in_29482(val);
    const int x = 29482;
    std::cout << 29482;
    for (int i = 0; i < 29482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29482(int& val) {
    const int x = 29482;
    std::cout << 29482;
    for (int i = 0; i < 29482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29483(int& val) {
    func_in_29483(val);
    const int x = 29483;
    std::cout << 29483;
    for (int i = 0; i < 29483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29483(int& val) {
    const int x = 29483;
    std::cout << 29483;
    for (int i = 0; i < 29483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29484(int& val) {
    func_in_29484(val);
    const int x = 29484;
    std::cout << 29484;
    for (int i = 0; i < 29484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29484(int& val) {
    const int x = 29484;
    std::cout << 29484;
    for (int i = 0; i < 29484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29485(int& val) {
    func_in_29485(val);
    const int x = 29485;
    std::cout << 29485;
    for (int i = 0; i < 29485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29485(int& val) {
    const int x = 29485;
    std::cout << 29485;
    for (int i = 0; i < 29485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29486(int& val) {
    func_in_29486(val);
    const int x = 29486;
    std::cout << 29486;
    for (int i = 0; i < 29486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29486(int& val) {
    const int x = 29486;
    std::cout << 29486;
    for (int i = 0; i < 29486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29487(int& val) {
    func_in_29487(val);
    const int x = 29487;
    std::cout << 29487;
    for (int i = 0; i < 29487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29487(int& val) {
    const int x = 29487;
    std::cout << 29487;
    for (int i = 0; i < 29487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29488(int& val) {
    func_in_29488(val);
    const int x = 29488;
    std::cout << 29488;
    for (int i = 0; i < 29488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29488(int& val) {
    const int x = 29488;
    std::cout << 29488;
    for (int i = 0; i < 29488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29489(int& val) {
    func_in_29489(val);
    const int x = 29489;
    std::cout << 29489;
    for (int i = 0; i < 29489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29489(int& val) {
    const int x = 29489;
    std::cout << 29489;
    for (int i = 0; i < 29489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29490(int& val) {
    func_in_29490(val);
    const int x = 29490;
    std::cout << 29490;
    for (int i = 0; i < 29490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29490(int& val) {
    const int x = 29490;
    std::cout << 29490;
    for (int i = 0; i < 29490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29491(int& val) {
    func_in_29491(val);
    const int x = 29491;
    std::cout << 29491;
    for (int i = 0; i < 29491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29491(int& val) {
    const int x = 29491;
    std::cout << 29491;
    for (int i = 0; i < 29491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29492(int& val) {
    func_in_29492(val);
    const int x = 29492;
    std::cout << 29492;
    for (int i = 0; i < 29492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29492(int& val) {
    const int x = 29492;
    std::cout << 29492;
    for (int i = 0; i < 29492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29493(int& val) {
    func_in_29493(val);
    const int x = 29493;
    std::cout << 29493;
    for (int i = 0; i < 29493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29493(int& val) {
    const int x = 29493;
    std::cout << 29493;
    for (int i = 0; i < 29493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29494(int& val) {
    func_in_29494(val);
    const int x = 29494;
    std::cout << 29494;
    for (int i = 0; i < 29494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29494(int& val) {
    const int x = 29494;
    std::cout << 29494;
    for (int i = 0; i < 29494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29495(int& val) {
    func_in_29495(val);
    const int x = 29495;
    std::cout << 29495;
    for (int i = 0; i < 29495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29495(int& val) {
    const int x = 29495;
    std::cout << 29495;
    for (int i = 0; i < 29495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29496(int& val) {
    func_in_29496(val);
    const int x = 29496;
    std::cout << 29496;
    for (int i = 0; i < 29496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29496(int& val) {
    const int x = 29496;
    std::cout << 29496;
    for (int i = 0; i < 29496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29497(int& val) {
    func_in_29497(val);
    const int x = 29497;
    std::cout << 29497;
    for (int i = 0; i < 29497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29497(int& val) {
    const int x = 29497;
    std::cout << 29497;
    for (int i = 0; i < 29497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29498(int& val) {
    func_in_29498(val);
    const int x = 29498;
    std::cout << 29498;
    for (int i = 0; i < 29498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29498(int& val) {
    const int x = 29498;
    std::cout << 29498;
    for (int i = 0; i < 29498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29499(int& val) {
    func_in_29499(val);
    const int x = 29499;
    std::cout << 29499;
    for (int i = 0; i < 29499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29499(int& val) {
    const int x = 29499;
    std::cout << 29499;
    for (int i = 0; i < 29499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29500(int& val) {
    func_in_29500(val);
    const int x = 29500;
    std::cout << 29500;
    for (int i = 0; i < 29500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29500(int& val) {
    const int x = 29500;
    std::cout << 29500;
    for (int i = 0; i < 29500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29501(int& val) {
    func_in_29501(val);
    const int x = 29501;
    std::cout << 29501;
    for (int i = 0; i < 29501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29501(int& val) {
    const int x = 29501;
    std::cout << 29501;
    for (int i = 0; i < 29501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29502(int& val) {
    func_in_29502(val);
    const int x = 29502;
    std::cout << 29502;
    for (int i = 0; i < 29502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29502(int& val) {
    const int x = 29502;
    std::cout << 29502;
    for (int i = 0; i < 29502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29503(int& val) {
    func_in_29503(val);
    const int x = 29503;
    std::cout << 29503;
    for (int i = 0; i < 29503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29503(int& val) {
    const int x = 29503;
    std::cout << 29503;
    for (int i = 0; i < 29503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29504(int& val) {
    func_in_29504(val);
    const int x = 29504;
    std::cout << 29504;
    for (int i = 0; i < 29504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29504(int& val) {
    const int x = 29504;
    std::cout << 29504;
    for (int i = 0; i < 29504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29505(int& val) {
    func_in_29505(val);
    const int x = 29505;
    std::cout << 29505;
    for (int i = 0; i < 29505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29505(int& val) {
    const int x = 29505;
    std::cout << 29505;
    for (int i = 0; i < 29505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29506(int& val) {
    func_in_29506(val);
    const int x = 29506;
    std::cout << 29506;
    for (int i = 0; i < 29506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29506(int& val) {
    const int x = 29506;
    std::cout << 29506;
    for (int i = 0; i < 29506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29507(int& val) {
    func_in_29507(val);
    const int x = 29507;
    std::cout << 29507;
    for (int i = 0; i < 29507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29507(int& val) {
    const int x = 29507;
    std::cout << 29507;
    for (int i = 0; i < 29507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29508(int& val) {
    func_in_29508(val);
    const int x = 29508;
    std::cout << 29508;
    for (int i = 0; i < 29508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29508(int& val) {
    const int x = 29508;
    std::cout << 29508;
    for (int i = 0; i < 29508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29509(int& val) {
    func_in_29509(val);
    const int x = 29509;
    std::cout << 29509;
    for (int i = 0; i < 29509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29509(int& val) {
    const int x = 29509;
    std::cout << 29509;
    for (int i = 0; i < 29509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29510(int& val) {
    func_in_29510(val);
    const int x = 29510;
    std::cout << 29510;
    for (int i = 0; i < 29510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29510(int& val) {
    const int x = 29510;
    std::cout << 29510;
    for (int i = 0; i < 29510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29511(int& val) {
    func_in_29511(val);
    const int x = 29511;
    std::cout << 29511;
    for (int i = 0; i < 29511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29511(int& val) {
    const int x = 29511;
    std::cout << 29511;
    for (int i = 0; i < 29511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29512(int& val) {
    func_in_29512(val);
    const int x = 29512;
    std::cout << 29512;
    for (int i = 0; i < 29512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29512(int& val) {
    const int x = 29512;
    std::cout << 29512;
    for (int i = 0; i < 29512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29513(int& val) {
    func_in_29513(val);
    const int x = 29513;
    std::cout << 29513;
    for (int i = 0; i < 29513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29513(int& val) {
    const int x = 29513;
    std::cout << 29513;
    for (int i = 0; i < 29513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29514(int& val) {
    func_in_29514(val);
    const int x = 29514;
    std::cout << 29514;
    for (int i = 0; i < 29514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29514(int& val) {
    const int x = 29514;
    std::cout << 29514;
    for (int i = 0; i < 29514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29515(int& val) {
    func_in_29515(val);
    const int x = 29515;
    std::cout << 29515;
    for (int i = 0; i < 29515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29515(int& val) {
    const int x = 29515;
    std::cout << 29515;
    for (int i = 0; i < 29515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29516(int& val) {
    func_in_29516(val);
    const int x = 29516;
    std::cout << 29516;
    for (int i = 0; i < 29516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29516(int& val) {
    const int x = 29516;
    std::cout << 29516;
    for (int i = 0; i < 29516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29517(int& val) {
    func_in_29517(val);
    const int x = 29517;
    std::cout << 29517;
    for (int i = 0; i < 29517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29517(int& val) {
    const int x = 29517;
    std::cout << 29517;
    for (int i = 0; i < 29517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29518(int& val) {
    func_in_29518(val);
    const int x = 29518;
    std::cout << 29518;
    for (int i = 0; i < 29518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29518(int& val) {
    const int x = 29518;
    std::cout << 29518;
    for (int i = 0; i < 29518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29519(int& val) {
    func_in_29519(val);
    const int x = 29519;
    std::cout << 29519;
    for (int i = 0; i < 29519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29519(int& val) {
    const int x = 29519;
    std::cout << 29519;
    for (int i = 0; i < 29519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29520(int& val) {
    func_in_29520(val);
    const int x = 29520;
    std::cout << 29520;
    for (int i = 0; i < 29520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29520(int& val) {
    const int x = 29520;
    std::cout << 29520;
    for (int i = 0; i < 29520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29521(int& val) {
    func_in_29521(val);
    const int x = 29521;
    std::cout << 29521;
    for (int i = 0; i < 29521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29521(int& val) {
    const int x = 29521;
    std::cout << 29521;
    for (int i = 0; i < 29521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29522(int& val) {
    func_in_29522(val);
    const int x = 29522;
    std::cout << 29522;
    for (int i = 0; i < 29522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29522(int& val) {
    const int x = 29522;
    std::cout << 29522;
    for (int i = 0; i < 29522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29523(int& val) {
    func_in_29523(val);
    const int x = 29523;
    std::cout << 29523;
    for (int i = 0; i < 29523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29523(int& val) {
    const int x = 29523;
    std::cout << 29523;
    for (int i = 0; i < 29523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29524(int& val) {
    func_in_29524(val);
    const int x = 29524;
    std::cout << 29524;
    for (int i = 0; i < 29524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29524(int& val) {
    const int x = 29524;
    std::cout << 29524;
    for (int i = 0; i < 29524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29525(int& val) {
    func_in_29525(val);
    const int x = 29525;
    std::cout << 29525;
    for (int i = 0; i < 29525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29525(int& val) {
    const int x = 29525;
    std::cout << 29525;
    for (int i = 0; i < 29525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29526(int& val) {
    func_in_29526(val);
    const int x = 29526;
    std::cout << 29526;
    for (int i = 0; i < 29526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29526(int& val) {
    const int x = 29526;
    std::cout << 29526;
    for (int i = 0; i < 29526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29527(int& val) {
    func_in_29527(val);
    const int x = 29527;
    std::cout << 29527;
    for (int i = 0; i < 29527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29527(int& val) {
    const int x = 29527;
    std::cout << 29527;
    for (int i = 0; i < 29527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29528(int& val) {
    func_in_29528(val);
    const int x = 29528;
    std::cout << 29528;
    for (int i = 0; i < 29528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29528(int& val) {
    const int x = 29528;
    std::cout << 29528;
    for (int i = 0; i < 29528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29529(int& val) {
    func_in_29529(val);
    const int x = 29529;
    std::cout << 29529;
    for (int i = 0; i < 29529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29529(int& val) {
    const int x = 29529;
    std::cout << 29529;
    for (int i = 0; i < 29529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29530(int& val) {
    func_in_29530(val);
    const int x = 29530;
    std::cout << 29530;
    for (int i = 0; i < 29530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29530(int& val) {
    const int x = 29530;
    std::cout << 29530;
    for (int i = 0; i < 29530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29531(int& val) {
    func_in_29531(val);
    const int x = 29531;
    std::cout << 29531;
    for (int i = 0; i < 29531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29531(int& val) {
    const int x = 29531;
    std::cout << 29531;
    for (int i = 0; i < 29531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29532(int& val) {
    func_in_29532(val);
    const int x = 29532;
    std::cout << 29532;
    for (int i = 0; i < 29532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29532(int& val) {
    const int x = 29532;
    std::cout << 29532;
    for (int i = 0; i < 29532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29533(int& val) {
    func_in_29533(val);
    const int x = 29533;
    std::cout << 29533;
    for (int i = 0; i < 29533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29533(int& val) {
    const int x = 29533;
    std::cout << 29533;
    for (int i = 0; i < 29533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29534(int& val) {
    func_in_29534(val);
    const int x = 29534;
    std::cout << 29534;
    for (int i = 0; i < 29534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29534(int& val) {
    const int x = 29534;
    std::cout << 29534;
    for (int i = 0; i < 29534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29535(int& val) {
    func_in_29535(val);
    const int x = 29535;
    std::cout << 29535;
    for (int i = 0; i < 29535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29535(int& val) {
    const int x = 29535;
    std::cout << 29535;
    for (int i = 0; i < 29535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29536(int& val) {
    func_in_29536(val);
    const int x = 29536;
    std::cout << 29536;
    for (int i = 0; i < 29536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29536(int& val) {
    const int x = 29536;
    std::cout << 29536;
    for (int i = 0; i < 29536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29537(int& val) {
    func_in_29537(val);
    const int x = 29537;
    std::cout << 29537;
    for (int i = 0; i < 29537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29537(int& val) {
    const int x = 29537;
    std::cout << 29537;
    for (int i = 0; i < 29537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29538(int& val) {
    func_in_29538(val);
    const int x = 29538;
    std::cout << 29538;
    for (int i = 0; i < 29538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29538(int& val) {
    const int x = 29538;
    std::cout << 29538;
    for (int i = 0; i < 29538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29539(int& val) {
    func_in_29539(val);
    const int x = 29539;
    std::cout << 29539;
    for (int i = 0; i < 29539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29539(int& val) {
    const int x = 29539;
    std::cout << 29539;
    for (int i = 0; i < 29539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29540(int& val) {
    func_in_29540(val);
    const int x = 29540;
    std::cout << 29540;
    for (int i = 0; i < 29540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29540(int& val) {
    const int x = 29540;
    std::cout << 29540;
    for (int i = 0; i < 29540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29541(int& val) {
    func_in_29541(val);
    const int x = 29541;
    std::cout << 29541;
    for (int i = 0; i < 29541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29541(int& val) {
    const int x = 29541;
    std::cout << 29541;
    for (int i = 0; i < 29541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29542(int& val) {
    func_in_29542(val);
    const int x = 29542;
    std::cout << 29542;
    for (int i = 0; i < 29542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29542(int& val) {
    const int x = 29542;
    std::cout << 29542;
    for (int i = 0; i < 29542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29543(int& val) {
    func_in_29543(val);
    const int x = 29543;
    std::cout << 29543;
    for (int i = 0; i < 29543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29543(int& val) {
    const int x = 29543;
    std::cout << 29543;
    for (int i = 0; i < 29543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29544(int& val) {
    func_in_29544(val);
    const int x = 29544;
    std::cout << 29544;
    for (int i = 0; i < 29544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29544(int& val) {
    const int x = 29544;
    std::cout << 29544;
    for (int i = 0; i < 29544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29545(int& val) {
    func_in_29545(val);
    const int x = 29545;
    std::cout << 29545;
    for (int i = 0; i < 29545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29545(int& val) {
    const int x = 29545;
    std::cout << 29545;
    for (int i = 0; i < 29545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29546(int& val) {
    func_in_29546(val);
    const int x = 29546;
    std::cout << 29546;
    for (int i = 0; i < 29546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29546(int& val) {
    const int x = 29546;
    std::cout << 29546;
    for (int i = 0; i < 29546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29547(int& val) {
    func_in_29547(val);
    const int x = 29547;
    std::cout << 29547;
    for (int i = 0; i < 29547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29547(int& val) {
    const int x = 29547;
    std::cout << 29547;
    for (int i = 0; i < 29547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29548(int& val) {
    func_in_29548(val);
    const int x = 29548;
    std::cout << 29548;
    for (int i = 0; i < 29548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29548(int& val) {
    const int x = 29548;
    std::cout << 29548;
    for (int i = 0; i < 29548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29549(int& val) {
    func_in_29549(val);
    const int x = 29549;
    std::cout << 29549;
    for (int i = 0; i < 29549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29549(int& val) {
    const int x = 29549;
    std::cout << 29549;
    for (int i = 0; i < 29549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29550(int& val) {
    func_in_29550(val);
    const int x = 29550;
    std::cout << 29550;
    for (int i = 0; i < 29550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29550(int& val) {
    const int x = 29550;
    std::cout << 29550;
    for (int i = 0; i < 29550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29551(int& val) {
    func_in_29551(val);
    const int x = 29551;
    std::cout << 29551;
    for (int i = 0; i < 29551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29551(int& val) {
    const int x = 29551;
    std::cout << 29551;
    for (int i = 0; i < 29551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29552(int& val) {
    func_in_29552(val);
    const int x = 29552;
    std::cout << 29552;
    for (int i = 0; i < 29552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29552(int& val) {
    const int x = 29552;
    std::cout << 29552;
    for (int i = 0; i < 29552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29553(int& val) {
    func_in_29553(val);
    const int x = 29553;
    std::cout << 29553;
    for (int i = 0; i < 29553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29553(int& val) {
    const int x = 29553;
    std::cout << 29553;
    for (int i = 0; i < 29553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29554(int& val) {
    func_in_29554(val);
    const int x = 29554;
    std::cout << 29554;
    for (int i = 0; i < 29554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29554(int& val) {
    const int x = 29554;
    std::cout << 29554;
    for (int i = 0; i < 29554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29555(int& val) {
    func_in_29555(val);
    const int x = 29555;
    std::cout << 29555;
    for (int i = 0; i < 29555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29555(int& val) {
    const int x = 29555;
    std::cout << 29555;
    for (int i = 0; i < 29555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29556(int& val) {
    func_in_29556(val);
    const int x = 29556;
    std::cout << 29556;
    for (int i = 0; i < 29556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29556(int& val) {
    const int x = 29556;
    std::cout << 29556;
    for (int i = 0; i < 29556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29557(int& val) {
    func_in_29557(val);
    const int x = 29557;
    std::cout << 29557;
    for (int i = 0; i < 29557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29557(int& val) {
    const int x = 29557;
    std::cout << 29557;
    for (int i = 0; i < 29557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29558(int& val) {
    func_in_29558(val);
    const int x = 29558;
    std::cout << 29558;
    for (int i = 0; i < 29558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29558(int& val) {
    const int x = 29558;
    std::cout << 29558;
    for (int i = 0; i < 29558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29559(int& val) {
    func_in_29559(val);
    const int x = 29559;
    std::cout << 29559;
    for (int i = 0; i < 29559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29559(int& val) {
    const int x = 29559;
    std::cout << 29559;
    for (int i = 0; i < 29559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29560(int& val) {
    func_in_29560(val);
    const int x = 29560;
    std::cout << 29560;
    for (int i = 0; i < 29560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29560(int& val) {
    const int x = 29560;
    std::cout << 29560;
    for (int i = 0; i < 29560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29561(int& val) {
    func_in_29561(val);
    const int x = 29561;
    std::cout << 29561;
    for (int i = 0; i < 29561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29561(int& val) {
    const int x = 29561;
    std::cout << 29561;
    for (int i = 0; i < 29561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29562(int& val) {
    func_in_29562(val);
    const int x = 29562;
    std::cout << 29562;
    for (int i = 0; i < 29562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29562(int& val) {
    const int x = 29562;
    std::cout << 29562;
    for (int i = 0; i < 29562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29563(int& val) {
    func_in_29563(val);
    const int x = 29563;
    std::cout << 29563;
    for (int i = 0; i < 29563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29563(int& val) {
    const int x = 29563;
    std::cout << 29563;
    for (int i = 0; i < 29563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29564(int& val) {
    func_in_29564(val);
    const int x = 29564;
    std::cout << 29564;
    for (int i = 0; i < 29564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29564(int& val) {
    const int x = 29564;
    std::cout << 29564;
    for (int i = 0; i < 29564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29565(int& val) {
    func_in_29565(val);
    const int x = 29565;
    std::cout << 29565;
    for (int i = 0; i < 29565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29565(int& val) {
    const int x = 29565;
    std::cout << 29565;
    for (int i = 0; i < 29565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29566(int& val) {
    func_in_29566(val);
    const int x = 29566;
    std::cout << 29566;
    for (int i = 0; i < 29566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29566(int& val) {
    const int x = 29566;
    std::cout << 29566;
    for (int i = 0; i < 29566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29567(int& val) {
    func_in_29567(val);
    const int x = 29567;
    std::cout << 29567;
    for (int i = 0; i < 29567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29567(int& val) {
    const int x = 29567;
    std::cout << 29567;
    for (int i = 0; i < 29567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29568(int& val) {
    func_in_29568(val);
    const int x = 29568;
    std::cout << 29568;
    for (int i = 0; i < 29568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29568(int& val) {
    const int x = 29568;
    std::cout << 29568;
    for (int i = 0; i < 29568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29569(int& val) {
    func_in_29569(val);
    const int x = 29569;
    std::cout << 29569;
    for (int i = 0; i < 29569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29569(int& val) {
    const int x = 29569;
    std::cout << 29569;
    for (int i = 0; i < 29569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29570(int& val) {
    func_in_29570(val);
    const int x = 29570;
    std::cout << 29570;
    for (int i = 0; i < 29570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29570(int& val) {
    const int x = 29570;
    std::cout << 29570;
    for (int i = 0; i < 29570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29571(int& val) {
    func_in_29571(val);
    const int x = 29571;
    std::cout << 29571;
    for (int i = 0; i < 29571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29571(int& val) {
    const int x = 29571;
    std::cout << 29571;
    for (int i = 0; i < 29571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29572(int& val) {
    func_in_29572(val);
    const int x = 29572;
    std::cout << 29572;
    for (int i = 0; i < 29572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29572(int& val) {
    const int x = 29572;
    std::cout << 29572;
    for (int i = 0; i < 29572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29573(int& val) {
    func_in_29573(val);
    const int x = 29573;
    std::cout << 29573;
    for (int i = 0; i < 29573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29573(int& val) {
    const int x = 29573;
    std::cout << 29573;
    for (int i = 0; i < 29573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29574(int& val) {
    func_in_29574(val);
    const int x = 29574;
    std::cout << 29574;
    for (int i = 0; i < 29574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29574(int& val) {
    const int x = 29574;
    std::cout << 29574;
    for (int i = 0; i < 29574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29575(int& val) {
    func_in_29575(val);
    const int x = 29575;
    std::cout << 29575;
    for (int i = 0; i < 29575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29575(int& val) {
    const int x = 29575;
    std::cout << 29575;
    for (int i = 0; i < 29575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29576(int& val) {
    func_in_29576(val);
    const int x = 29576;
    std::cout << 29576;
    for (int i = 0; i < 29576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29576(int& val) {
    const int x = 29576;
    std::cout << 29576;
    for (int i = 0; i < 29576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29577(int& val) {
    func_in_29577(val);
    const int x = 29577;
    std::cout << 29577;
    for (int i = 0; i < 29577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29577(int& val) {
    const int x = 29577;
    std::cout << 29577;
    for (int i = 0; i < 29577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29578(int& val) {
    func_in_29578(val);
    const int x = 29578;
    std::cout << 29578;
    for (int i = 0; i < 29578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29578(int& val) {
    const int x = 29578;
    std::cout << 29578;
    for (int i = 0; i < 29578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29579(int& val) {
    func_in_29579(val);
    const int x = 29579;
    std::cout << 29579;
    for (int i = 0; i < 29579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29579(int& val) {
    const int x = 29579;
    std::cout << 29579;
    for (int i = 0; i < 29579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29580(int& val) {
    func_in_29580(val);
    const int x = 29580;
    std::cout << 29580;
    for (int i = 0; i < 29580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29580(int& val) {
    const int x = 29580;
    std::cout << 29580;
    for (int i = 0; i < 29580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29581(int& val) {
    func_in_29581(val);
    const int x = 29581;
    std::cout << 29581;
    for (int i = 0; i < 29581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29581(int& val) {
    const int x = 29581;
    std::cout << 29581;
    for (int i = 0; i < 29581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29582(int& val) {
    func_in_29582(val);
    const int x = 29582;
    std::cout << 29582;
    for (int i = 0; i < 29582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29582(int& val) {
    const int x = 29582;
    std::cout << 29582;
    for (int i = 0; i < 29582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29583(int& val) {
    func_in_29583(val);
    const int x = 29583;
    std::cout << 29583;
    for (int i = 0; i < 29583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29583(int& val) {
    const int x = 29583;
    std::cout << 29583;
    for (int i = 0; i < 29583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29584(int& val) {
    func_in_29584(val);
    const int x = 29584;
    std::cout << 29584;
    for (int i = 0; i < 29584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29584(int& val) {
    const int x = 29584;
    std::cout << 29584;
    for (int i = 0; i < 29584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29585(int& val) {
    func_in_29585(val);
    const int x = 29585;
    std::cout << 29585;
    for (int i = 0; i < 29585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29585(int& val) {
    const int x = 29585;
    std::cout << 29585;
    for (int i = 0; i < 29585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29586(int& val) {
    func_in_29586(val);
    const int x = 29586;
    std::cout << 29586;
    for (int i = 0; i < 29586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29586(int& val) {
    const int x = 29586;
    std::cout << 29586;
    for (int i = 0; i < 29586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29587(int& val) {
    func_in_29587(val);
    const int x = 29587;
    std::cout << 29587;
    for (int i = 0; i < 29587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29587(int& val) {
    const int x = 29587;
    std::cout << 29587;
    for (int i = 0; i < 29587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29588(int& val) {
    func_in_29588(val);
    const int x = 29588;
    std::cout << 29588;
    for (int i = 0; i < 29588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29588(int& val) {
    const int x = 29588;
    std::cout << 29588;
    for (int i = 0; i < 29588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29589(int& val) {
    func_in_29589(val);
    const int x = 29589;
    std::cout << 29589;
    for (int i = 0; i < 29589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29589(int& val) {
    const int x = 29589;
    std::cout << 29589;
    for (int i = 0; i < 29589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29590(int& val) {
    func_in_29590(val);
    const int x = 29590;
    std::cout << 29590;
    for (int i = 0; i < 29590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29590(int& val) {
    const int x = 29590;
    std::cout << 29590;
    for (int i = 0; i < 29590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29591(int& val) {
    func_in_29591(val);
    const int x = 29591;
    std::cout << 29591;
    for (int i = 0; i < 29591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29591(int& val) {
    const int x = 29591;
    std::cout << 29591;
    for (int i = 0; i < 29591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29592(int& val) {
    func_in_29592(val);
    const int x = 29592;
    std::cout << 29592;
    for (int i = 0; i < 29592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29592(int& val) {
    const int x = 29592;
    std::cout << 29592;
    for (int i = 0; i < 29592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29593(int& val) {
    func_in_29593(val);
    const int x = 29593;
    std::cout << 29593;
    for (int i = 0; i < 29593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29593(int& val) {
    const int x = 29593;
    std::cout << 29593;
    for (int i = 0; i < 29593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29594(int& val) {
    func_in_29594(val);
    const int x = 29594;
    std::cout << 29594;
    for (int i = 0; i < 29594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29594(int& val) {
    const int x = 29594;
    std::cout << 29594;
    for (int i = 0; i < 29594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29595(int& val) {
    func_in_29595(val);
    const int x = 29595;
    std::cout << 29595;
    for (int i = 0; i < 29595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29595(int& val) {
    const int x = 29595;
    std::cout << 29595;
    for (int i = 0; i < 29595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29596(int& val) {
    func_in_29596(val);
    const int x = 29596;
    std::cout << 29596;
    for (int i = 0; i < 29596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29596(int& val) {
    const int x = 29596;
    std::cout << 29596;
    for (int i = 0; i < 29596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29597(int& val) {
    func_in_29597(val);
    const int x = 29597;
    std::cout << 29597;
    for (int i = 0; i < 29597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29597(int& val) {
    const int x = 29597;
    std::cout << 29597;
    for (int i = 0; i < 29597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29598(int& val) {
    func_in_29598(val);
    const int x = 29598;
    std::cout << 29598;
    for (int i = 0; i < 29598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29598(int& val) {
    const int x = 29598;
    std::cout << 29598;
    for (int i = 0; i < 29598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29599(int& val) {
    func_in_29599(val);
    const int x = 29599;
    std::cout << 29599;
    for (int i = 0; i < 29599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29599(int& val) {
    const int x = 29599;
    std::cout << 29599;
    for (int i = 0; i < 29599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29600(int& val) {
    func_in_29600(val);
    const int x = 29600;
    std::cout << 29600;
    for (int i = 0; i < 29600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29600(int& val) {
    const int x = 29600;
    std::cout << 29600;
    for (int i = 0; i < 29600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29601(int& val) {
    func_in_29601(val);
    const int x = 29601;
    std::cout << 29601;
    for (int i = 0; i < 29601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29601(int& val) {
    const int x = 29601;
    std::cout << 29601;
    for (int i = 0; i < 29601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29602(int& val) {
    func_in_29602(val);
    const int x = 29602;
    std::cout << 29602;
    for (int i = 0; i < 29602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29602(int& val) {
    const int x = 29602;
    std::cout << 29602;
    for (int i = 0; i < 29602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29603(int& val) {
    func_in_29603(val);
    const int x = 29603;
    std::cout << 29603;
    for (int i = 0; i < 29603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29603(int& val) {
    const int x = 29603;
    std::cout << 29603;
    for (int i = 0; i < 29603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29604(int& val) {
    func_in_29604(val);
    const int x = 29604;
    std::cout << 29604;
    for (int i = 0; i < 29604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29604(int& val) {
    const int x = 29604;
    std::cout << 29604;
    for (int i = 0; i < 29604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29605(int& val) {
    func_in_29605(val);
    const int x = 29605;
    std::cout << 29605;
    for (int i = 0; i < 29605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29605(int& val) {
    const int x = 29605;
    std::cout << 29605;
    for (int i = 0; i < 29605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29606(int& val) {
    func_in_29606(val);
    const int x = 29606;
    std::cout << 29606;
    for (int i = 0; i < 29606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29606(int& val) {
    const int x = 29606;
    std::cout << 29606;
    for (int i = 0; i < 29606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29607(int& val) {
    func_in_29607(val);
    const int x = 29607;
    std::cout << 29607;
    for (int i = 0; i < 29607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29607(int& val) {
    const int x = 29607;
    std::cout << 29607;
    for (int i = 0; i < 29607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29608(int& val) {
    func_in_29608(val);
    const int x = 29608;
    std::cout << 29608;
    for (int i = 0; i < 29608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29608(int& val) {
    const int x = 29608;
    std::cout << 29608;
    for (int i = 0; i < 29608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29609(int& val) {
    func_in_29609(val);
    const int x = 29609;
    std::cout << 29609;
    for (int i = 0; i < 29609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29609(int& val) {
    const int x = 29609;
    std::cout << 29609;
    for (int i = 0; i < 29609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29610(int& val) {
    func_in_29610(val);
    const int x = 29610;
    std::cout << 29610;
    for (int i = 0; i < 29610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29610(int& val) {
    const int x = 29610;
    std::cout << 29610;
    for (int i = 0; i < 29610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29611(int& val) {
    func_in_29611(val);
    const int x = 29611;
    std::cout << 29611;
    for (int i = 0; i < 29611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29611(int& val) {
    const int x = 29611;
    std::cout << 29611;
    for (int i = 0; i < 29611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29612(int& val) {
    func_in_29612(val);
    const int x = 29612;
    std::cout << 29612;
    for (int i = 0; i < 29612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29612(int& val) {
    const int x = 29612;
    std::cout << 29612;
    for (int i = 0; i < 29612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29613(int& val) {
    func_in_29613(val);
    const int x = 29613;
    std::cout << 29613;
    for (int i = 0; i < 29613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29613(int& val) {
    const int x = 29613;
    std::cout << 29613;
    for (int i = 0; i < 29613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29614(int& val) {
    func_in_29614(val);
    const int x = 29614;
    std::cout << 29614;
    for (int i = 0; i < 29614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29614(int& val) {
    const int x = 29614;
    std::cout << 29614;
    for (int i = 0; i < 29614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29615(int& val) {
    func_in_29615(val);
    const int x = 29615;
    std::cout << 29615;
    for (int i = 0; i < 29615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29615(int& val) {
    const int x = 29615;
    std::cout << 29615;
    for (int i = 0; i < 29615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29616(int& val) {
    func_in_29616(val);
    const int x = 29616;
    std::cout << 29616;
    for (int i = 0; i < 29616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29616(int& val) {
    const int x = 29616;
    std::cout << 29616;
    for (int i = 0; i < 29616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29617(int& val) {
    func_in_29617(val);
    const int x = 29617;
    std::cout << 29617;
    for (int i = 0; i < 29617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29617(int& val) {
    const int x = 29617;
    std::cout << 29617;
    for (int i = 0; i < 29617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29618(int& val) {
    func_in_29618(val);
    const int x = 29618;
    std::cout << 29618;
    for (int i = 0; i < 29618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29618(int& val) {
    const int x = 29618;
    std::cout << 29618;
    for (int i = 0; i < 29618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29619(int& val) {
    func_in_29619(val);
    const int x = 29619;
    std::cout << 29619;
    for (int i = 0; i < 29619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29619(int& val) {
    const int x = 29619;
    std::cout << 29619;
    for (int i = 0; i < 29619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29620(int& val) {
    func_in_29620(val);
    const int x = 29620;
    std::cout << 29620;
    for (int i = 0; i < 29620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29620(int& val) {
    const int x = 29620;
    std::cout << 29620;
    for (int i = 0; i < 29620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29621(int& val) {
    func_in_29621(val);
    const int x = 29621;
    std::cout << 29621;
    for (int i = 0; i < 29621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29621(int& val) {
    const int x = 29621;
    std::cout << 29621;
    for (int i = 0; i < 29621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29622(int& val) {
    func_in_29622(val);
    const int x = 29622;
    std::cout << 29622;
    for (int i = 0; i < 29622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29622(int& val) {
    const int x = 29622;
    std::cout << 29622;
    for (int i = 0; i < 29622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29623(int& val) {
    func_in_29623(val);
    const int x = 29623;
    std::cout << 29623;
    for (int i = 0; i < 29623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29623(int& val) {
    const int x = 29623;
    std::cout << 29623;
    for (int i = 0; i < 29623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29624(int& val) {
    func_in_29624(val);
    const int x = 29624;
    std::cout << 29624;
    for (int i = 0; i < 29624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29624(int& val) {
    const int x = 29624;
    std::cout << 29624;
    for (int i = 0; i < 29624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29625(int& val) {
    func_in_29625(val);
    const int x = 29625;
    std::cout << 29625;
    for (int i = 0; i < 29625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29625(int& val) {
    const int x = 29625;
    std::cout << 29625;
    for (int i = 0; i < 29625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29626(int& val) {
    func_in_29626(val);
    const int x = 29626;
    std::cout << 29626;
    for (int i = 0; i < 29626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29626(int& val) {
    const int x = 29626;
    std::cout << 29626;
    for (int i = 0; i < 29626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29627(int& val) {
    func_in_29627(val);
    const int x = 29627;
    std::cout << 29627;
    for (int i = 0; i < 29627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29627(int& val) {
    const int x = 29627;
    std::cout << 29627;
    for (int i = 0; i < 29627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29628(int& val) {
    func_in_29628(val);
    const int x = 29628;
    std::cout << 29628;
    for (int i = 0; i < 29628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29628(int& val) {
    const int x = 29628;
    std::cout << 29628;
    for (int i = 0; i < 29628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29629(int& val) {
    func_in_29629(val);
    const int x = 29629;
    std::cout << 29629;
    for (int i = 0; i < 29629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29629(int& val) {
    const int x = 29629;
    std::cout << 29629;
    for (int i = 0; i < 29629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29630(int& val) {
    func_in_29630(val);
    const int x = 29630;
    std::cout << 29630;
    for (int i = 0; i < 29630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29630(int& val) {
    const int x = 29630;
    std::cout << 29630;
    for (int i = 0; i < 29630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29631(int& val) {
    func_in_29631(val);
    const int x = 29631;
    std::cout << 29631;
    for (int i = 0; i < 29631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29631(int& val) {
    const int x = 29631;
    std::cout << 29631;
    for (int i = 0; i < 29631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29632(int& val) {
    func_in_29632(val);
    const int x = 29632;
    std::cout << 29632;
    for (int i = 0; i < 29632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29632(int& val) {
    const int x = 29632;
    std::cout << 29632;
    for (int i = 0; i < 29632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29633(int& val) {
    func_in_29633(val);
    const int x = 29633;
    std::cout << 29633;
    for (int i = 0; i < 29633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29633(int& val) {
    const int x = 29633;
    std::cout << 29633;
    for (int i = 0; i < 29633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29634(int& val) {
    func_in_29634(val);
    const int x = 29634;
    std::cout << 29634;
    for (int i = 0; i < 29634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29634(int& val) {
    const int x = 29634;
    std::cout << 29634;
    for (int i = 0; i < 29634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29635(int& val) {
    func_in_29635(val);
    const int x = 29635;
    std::cout << 29635;
    for (int i = 0; i < 29635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29635(int& val) {
    const int x = 29635;
    std::cout << 29635;
    for (int i = 0; i < 29635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29636(int& val) {
    func_in_29636(val);
    const int x = 29636;
    std::cout << 29636;
    for (int i = 0; i < 29636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29636(int& val) {
    const int x = 29636;
    std::cout << 29636;
    for (int i = 0; i < 29636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29637(int& val) {
    func_in_29637(val);
    const int x = 29637;
    std::cout << 29637;
    for (int i = 0; i < 29637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29637(int& val) {
    const int x = 29637;
    std::cout << 29637;
    for (int i = 0; i < 29637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29638(int& val) {
    func_in_29638(val);
    const int x = 29638;
    std::cout << 29638;
    for (int i = 0; i < 29638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29638(int& val) {
    const int x = 29638;
    std::cout << 29638;
    for (int i = 0; i < 29638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29639(int& val) {
    func_in_29639(val);
    const int x = 29639;
    std::cout << 29639;
    for (int i = 0; i < 29639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29639(int& val) {
    const int x = 29639;
    std::cout << 29639;
    for (int i = 0; i < 29639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29640(int& val) {
    func_in_29640(val);
    const int x = 29640;
    std::cout << 29640;
    for (int i = 0; i < 29640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29640(int& val) {
    const int x = 29640;
    std::cout << 29640;
    for (int i = 0; i < 29640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29641(int& val) {
    func_in_29641(val);
    const int x = 29641;
    std::cout << 29641;
    for (int i = 0; i < 29641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29641(int& val) {
    const int x = 29641;
    std::cout << 29641;
    for (int i = 0; i < 29641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29642(int& val) {
    func_in_29642(val);
    const int x = 29642;
    std::cout << 29642;
    for (int i = 0; i < 29642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29642(int& val) {
    const int x = 29642;
    std::cout << 29642;
    for (int i = 0; i < 29642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29643(int& val) {
    func_in_29643(val);
    const int x = 29643;
    std::cout << 29643;
    for (int i = 0; i < 29643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29643(int& val) {
    const int x = 29643;
    std::cout << 29643;
    for (int i = 0; i < 29643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29644(int& val) {
    func_in_29644(val);
    const int x = 29644;
    std::cout << 29644;
    for (int i = 0; i < 29644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29644(int& val) {
    const int x = 29644;
    std::cout << 29644;
    for (int i = 0; i < 29644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29645(int& val) {
    func_in_29645(val);
    const int x = 29645;
    std::cout << 29645;
    for (int i = 0; i < 29645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29645(int& val) {
    const int x = 29645;
    std::cout << 29645;
    for (int i = 0; i < 29645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29646(int& val) {
    func_in_29646(val);
    const int x = 29646;
    std::cout << 29646;
    for (int i = 0; i < 29646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29646(int& val) {
    const int x = 29646;
    std::cout << 29646;
    for (int i = 0; i < 29646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29647(int& val) {
    func_in_29647(val);
    const int x = 29647;
    std::cout << 29647;
    for (int i = 0; i < 29647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29647(int& val) {
    const int x = 29647;
    std::cout << 29647;
    for (int i = 0; i < 29647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29648(int& val) {
    func_in_29648(val);
    const int x = 29648;
    std::cout << 29648;
    for (int i = 0; i < 29648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29648(int& val) {
    const int x = 29648;
    std::cout << 29648;
    for (int i = 0; i < 29648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29649(int& val) {
    func_in_29649(val);
    const int x = 29649;
    std::cout << 29649;
    for (int i = 0; i < 29649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29649(int& val) {
    const int x = 29649;
    std::cout << 29649;
    for (int i = 0; i < 29649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29650(int& val) {
    func_in_29650(val);
    const int x = 29650;
    std::cout << 29650;
    for (int i = 0; i < 29650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29650(int& val) {
    const int x = 29650;
    std::cout << 29650;
    for (int i = 0; i < 29650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29651(int& val) {
    func_in_29651(val);
    const int x = 29651;
    std::cout << 29651;
    for (int i = 0; i < 29651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29651(int& val) {
    const int x = 29651;
    std::cout << 29651;
    for (int i = 0; i < 29651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29652(int& val) {
    func_in_29652(val);
    const int x = 29652;
    std::cout << 29652;
    for (int i = 0; i < 29652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29652(int& val) {
    const int x = 29652;
    std::cout << 29652;
    for (int i = 0; i < 29652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29653(int& val) {
    func_in_29653(val);
    const int x = 29653;
    std::cout << 29653;
    for (int i = 0; i < 29653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29653(int& val) {
    const int x = 29653;
    std::cout << 29653;
    for (int i = 0; i < 29653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29654(int& val) {
    func_in_29654(val);
    const int x = 29654;
    std::cout << 29654;
    for (int i = 0; i < 29654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29654(int& val) {
    const int x = 29654;
    std::cout << 29654;
    for (int i = 0; i < 29654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29655(int& val) {
    func_in_29655(val);
    const int x = 29655;
    std::cout << 29655;
    for (int i = 0; i < 29655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29655(int& val) {
    const int x = 29655;
    std::cout << 29655;
    for (int i = 0; i < 29655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29656(int& val) {
    func_in_29656(val);
    const int x = 29656;
    std::cout << 29656;
    for (int i = 0; i < 29656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29656(int& val) {
    const int x = 29656;
    std::cout << 29656;
    for (int i = 0; i < 29656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29657(int& val) {
    func_in_29657(val);
    const int x = 29657;
    std::cout << 29657;
    for (int i = 0; i < 29657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29657(int& val) {
    const int x = 29657;
    std::cout << 29657;
    for (int i = 0; i < 29657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29658(int& val) {
    func_in_29658(val);
    const int x = 29658;
    std::cout << 29658;
    for (int i = 0; i < 29658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29658(int& val) {
    const int x = 29658;
    std::cout << 29658;
    for (int i = 0; i < 29658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29659(int& val) {
    func_in_29659(val);
    const int x = 29659;
    std::cout << 29659;
    for (int i = 0; i < 29659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29659(int& val) {
    const int x = 29659;
    std::cout << 29659;
    for (int i = 0; i < 29659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29660(int& val) {
    func_in_29660(val);
    const int x = 29660;
    std::cout << 29660;
    for (int i = 0; i < 29660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29660(int& val) {
    const int x = 29660;
    std::cout << 29660;
    for (int i = 0; i < 29660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29661(int& val) {
    func_in_29661(val);
    const int x = 29661;
    std::cout << 29661;
    for (int i = 0; i < 29661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29661(int& val) {
    const int x = 29661;
    std::cout << 29661;
    for (int i = 0; i < 29661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29662(int& val) {
    func_in_29662(val);
    const int x = 29662;
    std::cout << 29662;
    for (int i = 0; i < 29662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29662(int& val) {
    const int x = 29662;
    std::cout << 29662;
    for (int i = 0; i < 29662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29663(int& val) {
    func_in_29663(val);
    const int x = 29663;
    std::cout << 29663;
    for (int i = 0; i < 29663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29663(int& val) {
    const int x = 29663;
    std::cout << 29663;
    for (int i = 0; i < 29663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29664(int& val) {
    func_in_29664(val);
    const int x = 29664;
    std::cout << 29664;
    for (int i = 0; i < 29664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29664(int& val) {
    const int x = 29664;
    std::cout << 29664;
    for (int i = 0; i < 29664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29665(int& val) {
    func_in_29665(val);
    const int x = 29665;
    std::cout << 29665;
    for (int i = 0; i < 29665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29665(int& val) {
    const int x = 29665;
    std::cout << 29665;
    for (int i = 0; i < 29665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29666(int& val) {
    func_in_29666(val);
    const int x = 29666;
    std::cout << 29666;
    for (int i = 0; i < 29666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29666(int& val) {
    const int x = 29666;
    std::cout << 29666;
    for (int i = 0; i < 29666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29667(int& val) {
    func_in_29667(val);
    const int x = 29667;
    std::cout << 29667;
    for (int i = 0; i < 29667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29667(int& val) {
    const int x = 29667;
    std::cout << 29667;
    for (int i = 0; i < 29667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29668(int& val) {
    func_in_29668(val);
    const int x = 29668;
    std::cout << 29668;
    for (int i = 0; i < 29668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29668(int& val) {
    const int x = 29668;
    std::cout << 29668;
    for (int i = 0; i < 29668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29669(int& val) {
    func_in_29669(val);
    const int x = 29669;
    std::cout << 29669;
    for (int i = 0; i < 29669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29669(int& val) {
    const int x = 29669;
    std::cout << 29669;
    for (int i = 0; i < 29669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29670(int& val) {
    func_in_29670(val);
    const int x = 29670;
    std::cout << 29670;
    for (int i = 0; i < 29670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29670(int& val) {
    const int x = 29670;
    std::cout << 29670;
    for (int i = 0; i < 29670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29671(int& val) {
    func_in_29671(val);
    const int x = 29671;
    std::cout << 29671;
    for (int i = 0; i < 29671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29671(int& val) {
    const int x = 29671;
    std::cout << 29671;
    for (int i = 0; i < 29671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29672(int& val) {
    func_in_29672(val);
    const int x = 29672;
    std::cout << 29672;
    for (int i = 0; i < 29672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29672(int& val) {
    const int x = 29672;
    std::cout << 29672;
    for (int i = 0; i < 29672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29673(int& val) {
    func_in_29673(val);
    const int x = 29673;
    std::cout << 29673;
    for (int i = 0; i < 29673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29673(int& val) {
    const int x = 29673;
    std::cout << 29673;
    for (int i = 0; i < 29673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29674(int& val) {
    func_in_29674(val);
    const int x = 29674;
    std::cout << 29674;
    for (int i = 0; i < 29674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29674(int& val) {
    const int x = 29674;
    std::cout << 29674;
    for (int i = 0; i < 29674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29675(int& val) {
    func_in_29675(val);
    const int x = 29675;
    std::cout << 29675;
    for (int i = 0; i < 29675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29675(int& val) {
    const int x = 29675;
    std::cout << 29675;
    for (int i = 0; i < 29675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29676(int& val) {
    func_in_29676(val);
    const int x = 29676;
    std::cout << 29676;
    for (int i = 0; i < 29676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29676(int& val) {
    const int x = 29676;
    std::cout << 29676;
    for (int i = 0; i < 29676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29677(int& val) {
    func_in_29677(val);
    const int x = 29677;
    std::cout << 29677;
    for (int i = 0; i < 29677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29677(int& val) {
    const int x = 29677;
    std::cout << 29677;
    for (int i = 0; i < 29677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29678(int& val) {
    func_in_29678(val);
    const int x = 29678;
    std::cout << 29678;
    for (int i = 0; i < 29678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29678(int& val) {
    const int x = 29678;
    std::cout << 29678;
    for (int i = 0; i < 29678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29679(int& val) {
    func_in_29679(val);
    const int x = 29679;
    std::cout << 29679;
    for (int i = 0; i < 29679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29679(int& val) {
    const int x = 29679;
    std::cout << 29679;
    for (int i = 0; i < 29679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29680(int& val) {
    func_in_29680(val);
    const int x = 29680;
    std::cout << 29680;
    for (int i = 0; i < 29680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29680(int& val) {
    const int x = 29680;
    std::cout << 29680;
    for (int i = 0; i < 29680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29681(int& val) {
    func_in_29681(val);
    const int x = 29681;
    std::cout << 29681;
    for (int i = 0; i < 29681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29681(int& val) {
    const int x = 29681;
    std::cout << 29681;
    for (int i = 0; i < 29681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29682(int& val) {
    func_in_29682(val);
    const int x = 29682;
    std::cout << 29682;
    for (int i = 0; i < 29682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29682(int& val) {
    const int x = 29682;
    std::cout << 29682;
    for (int i = 0; i < 29682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29683(int& val) {
    func_in_29683(val);
    const int x = 29683;
    std::cout << 29683;
    for (int i = 0; i < 29683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29683(int& val) {
    const int x = 29683;
    std::cout << 29683;
    for (int i = 0; i < 29683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29684(int& val) {
    func_in_29684(val);
    const int x = 29684;
    std::cout << 29684;
    for (int i = 0; i < 29684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29684(int& val) {
    const int x = 29684;
    std::cout << 29684;
    for (int i = 0; i < 29684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29685(int& val) {
    func_in_29685(val);
    const int x = 29685;
    std::cout << 29685;
    for (int i = 0; i < 29685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29685(int& val) {
    const int x = 29685;
    std::cout << 29685;
    for (int i = 0; i < 29685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29686(int& val) {
    func_in_29686(val);
    const int x = 29686;
    std::cout << 29686;
    for (int i = 0; i < 29686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29686(int& val) {
    const int x = 29686;
    std::cout << 29686;
    for (int i = 0; i < 29686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29687(int& val) {
    func_in_29687(val);
    const int x = 29687;
    std::cout << 29687;
    for (int i = 0; i < 29687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29687(int& val) {
    const int x = 29687;
    std::cout << 29687;
    for (int i = 0; i < 29687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29688(int& val) {
    func_in_29688(val);
    const int x = 29688;
    std::cout << 29688;
    for (int i = 0; i < 29688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29688(int& val) {
    const int x = 29688;
    std::cout << 29688;
    for (int i = 0; i < 29688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29689(int& val) {
    func_in_29689(val);
    const int x = 29689;
    std::cout << 29689;
    for (int i = 0; i < 29689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29689(int& val) {
    const int x = 29689;
    std::cout << 29689;
    for (int i = 0; i < 29689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29690(int& val) {
    func_in_29690(val);
    const int x = 29690;
    std::cout << 29690;
    for (int i = 0; i < 29690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29690(int& val) {
    const int x = 29690;
    std::cout << 29690;
    for (int i = 0; i < 29690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29691(int& val) {
    func_in_29691(val);
    const int x = 29691;
    std::cout << 29691;
    for (int i = 0; i < 29691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29691(int& val) {
    const int x = 29691;
    std::cout << 29691;
    for (int i = 0; i < 29691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29692(int& val) {
    func_in_29692(val);
    const int x = 29692;
    std::cout << 29692;
    for (int i = 0; i < 29692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29692(int& val) {
    const int x = 29692;
    std::cout << 29692;
    for (int i = 0; i < 29692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29693(int& val) {
    func_in_29693(val);
    const int x = 29693;
    std::cout << 29693;
    for (int i = 0; i < 29693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29693(int& val) {
    const int x = 29693;
    std::cout << 29693;
    for (int i = 0; i < 29693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29694(int& val) {
    func_in_29694(val);
    const int x = 29694;
    std::cout << 29694;
    for (int i = 0; i < 29694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29694(int& val) {
    const int x = 29694;
    std::cout << 29694;
    for (int i = 0; i < 29694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29695(int& val) {
    func_in_29695(val);
    const int x = 29695;
    std::cout << 29695;
    for (int i = 0; i < 29695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29695(int& val) {
    const int x = 29695;
    std::cout << 29695;
    for (int i = 0; i < 29695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29696(int& val) {
    func_in_29696(val);
    const int x = 29696;
    std::cout << 29696;
    for (int i = 0; i < 29696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29696(int& val) {
    const int x = 29696;
    std::cout << 29696;
    for (int i = 0; i < 29696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29697(int& val) {
    func_in_29697(val);
    const int x = 29697;
    std::cout << 29697;
    for (int i = 0; i < 29697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29697(int& val) {
    const int x = 29697;
    std::cout << 29697;
    for (int i = 0; i < 29697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29698(int& val) {
    func_in_29698(val);
    const int x = 29698;
    std::cout << 29698;
    for (int i = 0; i < 29698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29698(int& val) {
    const int x = 29698;
    std::cout << 29698;
    for (int i = 0; i < 29698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29699(int& val) {
    func_in_29699(val);
    const int x = 29699;
    std::cout << 29699;
    for (int i = 0; i < 29699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29699(int& val) {
    const int x = 29699;
    std::cout << 29699;
    for (int i = 0; i < 29699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29700(int& val) {
    func_in_29700(val);
    const int x = 29700;
    std::cout << 29700;
    for (int i = 0; i < 29700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29700(int& val) {
    const int x = 29700;
    std::cout << 29700;
    for (int i = 0; i < 29700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29701(int& val) {
    func_in_29701(val);
    const int x = 29701;
    std::cout << 29701;
    for (int i = 0; i < 29701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29701(int& val) {
    const int x = 29701;
    std::cout << 29701;
    for (int i = 0; i < 29701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29702(int& val) {
    func_in_29702(val);
    const int x = 29702;
    std::cout << 29702;
    for (int i = 0; i < 29702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29702(int& val) {
    const int x = 29702;
    std::cout << 29702;
    for (int i = 0; i < 29702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29703(int& val) {
    func_in_29703(val);
    const int x = 29703;
    std::cout << 29703;
    for (int i = 0; i < 29703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29703(int& val) {
    const int x = 29703;
    std::cout << 29703;
    for (int i = 0; i < 29703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29704(int& val) {
    func_in_29704(val);
    const int x = 29704;
    std::cout << 29704;
    for (int i = 0; i < 29704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29704(int& val) {
    const int x = 29704;
    std::cout << 29704;
    for (int i = 0; i < 29704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29705(int& val) {
    func_in_29705(val);
    const int x = 29705;
    std::cout << 29705;
    for (int i = 0; i < 29705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29705(int& val) {
    const int x = 29705;
    std::cout << 29705;
    for (int i = 0; i < 29705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29706(int& val) {
    func_in_29706(val);
    const int x = 29706;
    std::cout << 29706;
    for (int i = 0; i < 29706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29706(int& val) {
    const int x = 29706;
    std::cout << 29706;
    for (int i = 0; i < 29706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29707(int& val) {
    func_in_29707(val);
    const int x = 29707;
    std::cout << 29707;
    for (int i = 0; i < 29707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29707(int& val) {
    const int x = 29707;
    std::cout << 29707;
    for (int i = 0; i < 29707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29708(int& val) {
    func_in_29708(val);
    const int x = 29708;
    std::cout << 29708;
    for (int i = 0; i < 29708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29708(int& val) {
    const int x = 29708;
    std::cout << 29708;
    for (int i = 0; i < 29708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29709(int& val) {
    func_in_29709(val);
    const int x = 29709;
    std::cout << 29709;
    for (int i = 0; i < 29709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29709(int& val) {
    const int x = 29709;
    std::cout << 29709;
    for (int i = 0; i < 29709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29710(int& val) {
    func_in_29710(val);
    const int x = 29710;
    std::cout << 29710;
    for (int i = 0; i < 29710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29710(int& val) {
    const int x = 29710;
    std::cout << 29710;
    for (int i = 0; i < 29710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29711(int& val) {
    func_in_29711(val);
    const int x = 29711;
    std::cout << 29711;
    for (int i = 0; i < 29711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29711(int& val) {
    const int x = 29711;
    std::cout << 29711;
    for (int i = 0; i < 29711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29712(int& val) {
    func_in_29712(val);
    const int x = 29712;
    std::cout << 29712;
    for (int i = 0; i < 29712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29712(int& val) {
    const int x = 29712;
    std::cout << 29712;
    for (int i = 0; i < 29712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29713(int& val) {
    func_in_29713(val);
    const int x = 29713;
    std::cout << 29713;
    for (int i = 0; i < 29713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29713(int& val) {
    const int x = 29713;
    std::cout << 29713;
    for (int i = 0; i < 29713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29714(int& val) {
    func_in_29714(val);
    const int x = 29714;
    std::cout << 29714;
    for (int i = 0; i < 29714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29714(int& val) {
    const int x = 29714;
    std::cout << 29714;
    for (int i = 0; i < 29714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29715(int& val) {
    func_in_29715(val);
    const int x = 29715;
    std::cout << 29715;
    for (int i = 0; i < 29715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29715(int& val) {
    const int x = 29715;
    std::cout << 29715;
    for (int i = 0; i < 29715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29716(int& val) {
    func_in_29716(val);
    const int x = 29716;
    std::cout << 29716;
    for (int i = 0; i < 29716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29716(int& val) {
    const int x = 29716;
    std::cout << 29716;
    for (int i = 0; i < 29716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29717(int& val) {
    func_in_29717(val);
    const int x = 29717;
    std::cout << 29717;
    for (int i = 0; i < 29717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29717(int& val) {
    const int x = 29717;
    std::cout << 29717;
    for (int i = 0; i < 29717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29718(int& val) {
    func_in_29718(val);
    const int x = 29718;
    std::cout << 29718;
    for (int i = 0; i < 29718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29718(int& val) {
    const int x = 29718;
    std::cout << 29718;
    for (int i = 0; i < 29718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29719(int& val) {
    func_in_29719(val);
    const int x = 29719;
    std::cout << 29719;
    for (int i = 0; i < 29719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29719(int& val) {
    const int x = 29719;
    std::cout << 29719;
    for (int i = 0; i < 29719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29720(int& val) {
    func_in_29720(val);
    const int x = 29720;
    std::cout << 29720;
    for (int i = 0; i < 29720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29720(int& val) {
    const int x = 29720;
    std::cout << 29720;
    for (int i = 0; i < 29720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29721(int& val) {
    func_in_29721(val);
    const int x = 29721;
    std::cout << 29721;
    for (int i = 0; i < 29721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29721(int& val) {
    const int x = 29721;
    std::cout << 29721;
    for (int i = 0; i < 29721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29722(int& val) {
    func_in_29722(val);
    const int x = 29722;
    std::cout << 29722;
    for (int i = 0; i < 29722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29722(int& val) {
    const int x = 29722;
    std::cout << 29722;
    for (int i = 0; i < 29722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29723(int& val) {
    func_in_29723(val);
    const int x = 29723;
    std::cout << 29723;
    for (int i = 0; i < 29723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29723(int& val) {
    const int x = 29723;
    std::cout << 29723;
    for (int i = 0; i < 29723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29724(int& val) {
    func_in_29724(val);
    const int x = 29724;
    std::cout << 29724;
    for (int i = 0; i < 29724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29724(int& val) {
    const int x = 29724;
    std::cout << 29724;
    for (int i = 0; i < 29724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29725(int& val) {
    func_in_29725(val);
    const int x = 29725;
    std::cout << 29725;
    for (int i = 0; i < 29725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29725(int& val) {
    const int x = 29725;
    std::cout << 29725;
    for (int i = 0; i < 29725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29726(int& val) {
    func_in_29726(val);
    const int x = 29726;
    std::cout << 29726;
    for (int i = 0; i < 29726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29726(int& val) {
    const int x = 29726;
    std::cout << 29726;
    for (int i = 0; i < 29726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29727(int& val) {
    func_in_29727(val);
    const int x = 29727;
    std::cout << 29727;
    for (int i = 0; i < 29727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29727(int& val) {
    const int x = 29727;
    std::cout << 29727;
    for (int i = 0; i < 29727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29728(int& val) {
    func_in_29728(val);
    const int x = 29728;
    std::cout << 29728;
    for (int i = 0; i < 29728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29728(int& val) {
    const int x = 29728;
    std::cout << 29728;
    for (int i = 0; i < 29728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29729(int& val) {
    func_in_29729(val);
    const int x = 29729;
    std::cout << 29729;
    for (int i = 0; i < 29729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29729(int& val) {
    const int x = 29729;
    std::cout << 29729;
    for (int i = 0; i < 29729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29730(int& val) {
    func_in_29730(val);
    const int x = 29730;
    std::cout << 29730;
    for (int i = 0; i < 29730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29730(int& val) {
    const int x = 29730;
    std::cout << 29730;
    for (int i = 0; i < 29730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29731(int& val) {
    func_in_29731(val);
    const int x = 29731;
    std::cout << 29731;
    for (int i = 0; i < 29731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29731(int& val) {
    const int x = 29731;
    std::cout << 29731;
    for (int i = 0; i < 29731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29732(int& val) {
    func_in_29732(val);
    const int x = 29732;
    std::cout << 29732;
    for (int i = 0; i < 29732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29732(int& val) {
    const int x = 29732;
    std::cout << 29732;
    for (int i = 0; i < 29732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29733(int& val) {
    func_in_29733(val);
    const int x = 29733;
    std::cout << 29733;
    for (int i = 0; i < 29733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29733(int& val) {
    const int x = 29733;
    std::cout << 29733;
    for (int i = 0; i < 29733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29734(int& val) {
    func_in_29734(val);
    const int x = 29734;
    std::cout << 29734;
    for (int i = 0; i < 29734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29734(int& val) {
    const int x = 29734;
    std::cout << 29734;
    for (int i = 0; i < 29734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29735(int& val) {
    func_in_29735(val);
    const int x = 29735;
    std::cout << 29735;
    for (int i = 0; i < 29735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29735(int& val) {
    const int x = 29735;
    std::cout << 29735;
    for (int i = 0; i < 29735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29736(int& val) {
    func_in_29736(val);
    const int x = 29736;
    std::cout << 29736;
    for (int i = 0; i < 29736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29736(int& val) {
    const int x = 29736;
    std::cout << 29736;
    for (int i = 0; i < 29736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29737(int& val) {
    func_in_29737(val);
    const int x = 29737;
    std::cout << 29737;
    for (int i = 0; i < 29737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29737(int& val) {
    const int x = 29737;
    std::cout << 29737;
    for (int i = 0; i < 29737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29738(int& val) {
    func_in_29738(val);
    const int x = 29738;
    std::cout << 29738;
    for (int i = 0; i < 29738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29738(int& val) {
    const int x = 29738;
    std::cout << 29738;
    for (int i = 0; i < 29738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29739(int& val) {
    func_in_29739(val);
    const int x = 29739;
    std::cout << 29739;
    for (int i = 0; i < 29739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29739(int& val) {
    const int x = 29739;
    std::cout << 29739;
    for (int i = 0; i < 29739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29740(int& val) {
    func_in_29740(val);
    const int x = 29740;
    std::cout << 29740;
    for (int i = 0; i < 29740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29740(int& val) {
    const int x = 29740;
    std::cout << 29740;
    for (int i = 0; i < 29740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29741(int& val) {
    func_in_29741(val);
    const int x = 29741;
    std::cout << 29741;
    for (int i = 0; i < 29741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29741(int& val) {
    const int x = 29741;
    std::cout << 29741;
    for (int i = 0; i < 29741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29742(int& val) {
    func_in_29742(val);
    const int x = 29742;
    std::cout << 29742;
    for (int i = 0; i < 29742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29742(int& val) {
    const int x = 29742;
    std::cout << 29742;
    for (int i = 0; i < 29742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29743(int& val) {
    func_in_29743(val);
    const int x = 29743;
    std::cout << 29743;
    for (int i = 0; i < 29743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29743(int& val) {
    const int x = 29743;
    std::cout << 29743;
    for (int i = 0; i < 29743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29744(int& val) {
    func_in_29744(val);
    const int x = 29744;
    std::cout << 29744;
    for (int i = 0; i < 29744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29744(int& val) {
    const int x = 29744;
    std::cout << 29744;
    for (int i = 0; i < 29744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29745(int& val) {
    func_in_29745(val);
    const int x = 29745;
    std::cout << 29745;
    for (int i = 0; i < 29745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29745(int& val) {
    const int x = 29745;
    std::cout << 29745;
    for (int i = 0; i < 29745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29746(int& val) {
    func_in_29746(val);
    const int x = 29746;
    std::cout << 29746;
    for (int i = 0; i < 29746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29746(int& val) {
    const int x = 29746;
    std::cout << 29746;
    for (int i = 0; i < 29746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29747(int& val) {
    func_in_29747(val);
    const int x = 29747;
    std::cout << 29747;
    for (int i = 0; i < 29747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29747(int& val) {
    const int x = 29747;
    std::cout << 29747;
    for (int i = 0; i < 29747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29748(int& val) {
    func_in_29748(val);
    const int x = 29748;
    std::cout << 29748;
    for (int i = 0; i < 29748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29748(int& val) {
    const int x = 29748;
    std::cout << 29748;
    for (int i = 0; i < 29748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29749(int& val) {
    func_in_29749(val);
    const int x = 29749;
    std::cout << 29749;
    for (int i = 0; i < 29749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29749(int& val) {
    const int x = 29749;
    std::cout << 29749;
    for (int i = 0; i < 29749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29750(int& val) {
    func_in_29750(val);
    const int x = 29750;
    std::cout << 29750;
    for (int i = 0; i < 29750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29750(int& val) {
    const int x = 29750;
    std::cout << 29750;
    for (int i = 0; i < 29750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29751(int& val) {
    func_in_29751(val);
    const int x = 29751;
    std::cout << 29751;
    for (int i = 0; i < 29751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29751(int& val) {
    const int x = 29751;
    std::cout << 29751;
    for (int i = 0; i < 29751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29752(int& val) {
    func_in_29752(val);
    const int x = 29752;
    std::cout << 29752;
    for (int i = 0; i < 29752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29752(int& val) {
    const int x = 29752;
    std::cout << 29752;
    for (int i = 0; i < 29752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29753(int& val) {
    func_in_29753(val);
    const int x = 29753;
    std::cout << 29753;
    for (int i = 0; i < 29753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29753(int& val) {
    const int x = 29753;
    std::cout << 29753;
    for (int i = 0; i < 29753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29754(int& val) {
    func_in_29754(val);
    const int x = 29754;
    std::cout << 29754;
    for (int i = 0; i < 29754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29754(int& val) {
    const int x = 29754;
    std::cout << 29754;
    for (int i = 0; i < 29754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29755(int& val) {
    func_in_29755(val);
    const int x = 29755;
    std::cout << 29755;
    for (int i = 0; i < 29755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29755(int& val) {
    const int x = 29755;
    std::cout << 29755;
    for (int i = 0; i < 29755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29756(int& val) {
    func_in_29756(val);
    const int x = 29756;
    std::cout << 29756;
    for (int i = 0; i < 29756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29756(int& val) {
    const int x = 29756;
    std::cout << 29756;
    for (int i = 0; i < 29756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29757(int& val) {
    func_in_29757(val);
    const int x = 29757;
    std::cout << 29757;
    for (int i = 0; i < 29757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29757(int& val) {
    const int x = 29757;
    std::cout << 29757;
    for (int i = 0; i < 29757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29758(int& val) {
    func_in_29758(val);
    const int x = 29758;
    std::cout << 29758;
    for (int i = 0; i < 29758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29758(int& val) {
    const int x = 29758;
    std::cout << 29758;
    for (int i = 0; i < 29758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29759(int& val) {
    func_in_29759(val);
    const int x = 29759;
    std::cout << 29759;
    for (int i = 0; i < 29759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29759(int& val) {
    const int x = 29759;
    std::cout << 29759;
    for (int i = 0; i < 29759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29760(int& val) {
    func_in_29760(val);
    const int x = 29760;
    std::cout << 29760;
    for (int i = 0; i < 29760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29760(int& val) {
    const int x = 29760;
    std::cout << 29760;
    for (int i = 0; i < 29760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29761(int& val) {
    func_in_29761(val);
    const int x = 29761;
    std::cout << 29761;
    for (int i = 0; i < 29761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29761(int& val) {
    const int x = 29761;
    std::cout << 29761;
    for (int i = 0; i < 29761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29762(int& val) {
    func_in_29762(val);
    const int x = 29762;
    std::cout << 29762;
    for (int i = 0; i < 29762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29762(int& val) {
    const int x = 29762;
    std::cout << 29762;
    for (int i = 0; i < 29762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29763(int& val) {
    func_in_29763(val);
    const int x = 29763;
    std::cout << 29763;
    for (int i = 0; i < 29763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29763(int& val) {
    const int x = 29763;
    std::cout << 29763;
    for (int i = 0; i < 29763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29764(int& val) {
    func_in_29764(val);
    const int x = 29764;
    std::cout << 29764;
    for (int i = 0; i < 29764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29764(int& val) {
    const int x = 29764;
    std::cout << 29764;
    for (int i = 0; i < 29764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29765(int& val) {
    func_in_29765(val);
    const int x = 29765;
    std::cout << 29765;
    for (int i = 0; i < 29765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29765(int& val) {
    const int x = 29765;
    std::cout << 29765;
    for (int i = 0; i < 29765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29766(int& val) {
    func_in_29766(val);
    const int x = 29766;
    std::cout << 29766;
    for (int i = 0; i < 29766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29766(int& val) {
    const int x = 29766;
    std::cout << 29766;
    for (int i = 0; i < 29766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29767(int& val) {
    func_in_29767(val);
    const int x = 29767;
    std::cout << 29767;
    for (int i = 0; i < 29767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29767(int& val) {
    const int x = 29767;
    std::cout << 29767;
    for (int i = 0; i < 29767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29768(int& val) {
    func_in_29768(val);
    const int x = 29768;
    std::cout << 29768;
    for (int i = 0; i < 29768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29768(int& val) {
    const int x = 29768;
    std::cout << 29768;
    for (int i = 0; i < 29768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29769(int& val) {
    func_in_29769(val);
    const int x = 29769;
    std::cout << 29769;
    for (int i = 0; i < 29769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29769(int& val) {
    const int x = 29769;
    std::cout << 29769;
    for (int i = 0; i < 29769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29770(int& val) {
    func_in_29770(val);
    const int x = 29770;
    std::cout << 29770;
    for (int i = 0; i < 29770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29770(int& val) {
    const int x = 29770;
    std::cout << 29770;
    for (int i = 0; i < 29770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29771(int& val) {
    func_in_29771(val);
    const int x = 29771;
    std::cout << 29771;
    for (int i = 0; i < 29771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29771(int& val) {
    const int x = 29771;
    std::cout << 29771;
    for (int i = 0; i < 29771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29772(int& val) {
    func_in_29772(val);
    const int x = 29772;
    std::cout << 29772;
    for (int i = 0; i < 29772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29772(int& val) {
    const int x = 29772;
    std::cout << 29772;
    for (int i = 0; i < 29772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29773(int& val) {
    func_in_29773(val);
    const int x = 29773;
    std::cout << 29773;
    for (int i = 0; i < 29773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29773(int& val) {
    const int x = 29773;
    std::cout << 29773;
    for (int i = 0; i < 29773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29774(int& val) {
    func_in_29774(val);
    const int x = 29774;
    std::cout << 29774;
    for (int i = 0; i < 29774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29774(int& val) {
    const int x = 29774;
    std::cout << 29774;
    for (int i = 0; i < 29774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29775(int& val) {
    func_in_29775(val);
    const int x = 29775;
    std::cout << 29775;
    for (int i = 0; i < 29775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29775(int& val) {
    const int x = 29775;
    std::cout << 29775;
    for (int i = 0; i < 29775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29776(int& val) {
    func_in_29776(val);
    const int x = 29776;
    std::cout << 29776;
    for (int i = 0; i < 29776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29776(int& val) {
    const int x = 29776;
    std::cout << 29776;
    for (int i = 0; i < 29776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29777(int& val) {
    func_in_29777(val);
    const int x = 29777;
    std::cout << 29777;
    for (int i = 0; i < 29777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29777(int& val) {
    const int x = 29777;
    std::cout << 29777;
    for (int i = 0; i < 29777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29778(int& val) {
    func_in_29778(val);
    const int x = 29778;
    std::cout << 29778;
    for (int i = 0; i < 29778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29778(int& val) {
    const int x = 29778;
    std::cout << 29778;
    for (int i = 0; i < 29778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29779(int& val) {
    func_in_29779(val);
    const int x = 29779;
    std::cout << 29779;
    for (int i = 0; i < 29779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29779(int& val) {
    const int x = 29779;
    std::cout << 29779;
    for (int i = 0; i < 29779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29780(int& val) {
    func_in_29780(val);
    const int x = 29780;
    std::cout << 29780;
    for (int i = 0; i < 29780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29780(int& val) {
    const int x = 29780;
    std::cout << 29780;
    for (int i = 0; i < 29780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29781(int& val) {
    func_in_29781(val);
    const int x = 29781;
    std::cout << 29781;
    for (int i = 0; i < 29781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29781(int& val) {
    const int x = 29781;
    std::cout << 29781;
    for (int i = 0; i < 29781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29782(int& val) {
    func_in_29782(val);
    const int x = 29782;
    std::cout << 29782;
    for (int i = 0; i < 29782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29782(int& val) {
    const int x = 29782;
    std::cout << 29782;
    for (int i = 0; i < 29782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29783(int& val) {
    func_in_29783(val);
    const int x = 29783;
    std::cout << 29783;
    for (int i = 0; i < 29783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29783(int& val) {
    const int x = 29783;
    std::cout << 29783;
    for (int i = 0; i < 29783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29784(int& val) {
    func_in_29784(val);
    const int x = 29784;
    std::cout << 29784;
    for (int i = 0; i < 29784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29784(int& val) {
    const int x = 29784;
    std::cout << 29784;
    for (int i = 0; i < 29784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29785(int& val) {
    func_in_29785(val);
    const int x = 29785;
    std::cout << 29785;
    for (int i = 0; i < 29785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29785(int& val) {
    const int x = 29785;
    std::cout << 29785;
    for (int i = 0; i < 29785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29786(int& val) {
    func_in_29786(val);
    const int x = 29786;
    std::cout << 29786;
    for (int i = 0; i < 29786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29786(int& val) {
    const int x = 29786;
    std::cout << 29786;
    for (int i = 0; i < 29786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29787(int& val) {
    func_in_29787(val);
    const int x = 29787;
    std::cout << 29787;
    for (int i = 0; i < 29787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29787(int& val) {
    const int x = 29787;
    std::cout << 29787;
    for (int i = 0; i < 29787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29788(int& val) {
    func_in_29788(val);
    const int x = 29788;
    std::cout << 29788;
    for (int i = 0; i < 29788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29788(int& val) {
    const int x = 29788;
    std::cout << 29788;
    for (int i = 0; i < 29788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29789(int& val) {
    func_in_29789(val);
    const int x = 29789;
    std::cout << 29789;
    for (int i = 0; i < 29789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29789(int& val) {
    const int x = 29789;
    std::cout << 29789;
    for (int i = 0; i < 29789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29790(int& val) {
    func_in_29790(val);
    const int x = 29790;
    std::cout << 29790;
    for (int i = 0; i < 29790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29790(int& val) {
    const int x = 29790;
    std::cout << 29790;
    for (int i = 0; i < 29790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29791(int& val) {
    func_in_29791(val);
    const int x = 29791;
    std::cout << 29791;
    for (int i = 0; i < 29791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29791(int& val) {
    const int x = 29791;
    std::cout << 29791;
    for (int i = 0; i < 29791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29792(int& val) {
    func_in_29792(val);
    const int x = 29792;
    std::cout << 29792;
    for (int i = 0; i < 29792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29792(int& val) {
    const int x = 29792;
    std::cout << 29792;
    for (int i = 0; i < 29792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29793(int& val) {
    func_in_29793(val);
    const int x = 29793;
    std::cout << 29793;
    for (int i = 0; i < 29793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29793(int& val) {
    const int x = 29793;
    std::cout << 29793;
    for (int i = 0; i < 29793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29794(int& val) {
    func_in_29794(val);
    const int x = 29794;
    std::cout << 29794;
    for (int i = 0; i < 29794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29794(int& val) {
    const int x = 29794;
    std::cout << 29794;
    for (int i = 0; i < 29794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29795(int& val) {
    func_in_29795(val);
    const int x = 29795;
    std::cout << 29795;
    for (int i = 0; i < 29795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29795(int& val) {
    const int x = 29795;
    std::cout << 29795;
    for (int i = 0; i < 29795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29796(int& val) {
    func_in_29796(val);
    const int x = 29796;
    std::cout << 29796;
    for (int i = 0; i < 29796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29796(int& val) {
    const int x = 29796;
    std::cout << 29796;
    for (int i = 0; i < 29796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29797(int& val) {
    func_in_29797(val);
    const int x = 29797;
    std::cout << 29797;
    for (int i = 0; i < 29797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29797(int& val) {
    const int x = 29797;
    std::cout << 29797;
    for (int i = 0; i < 29797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29798(int& val) {
    func_in_29798(val);
    const int x = 29798;
    std::cout << 29798;
    for (int i = 0; i < 29798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29798(int& val) {
    const int x = 29798;
    std::cout << 29798;
    for (int i = 0; i < 29798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29799(int& val) {
    func_in_29799(val);
    const int x = 29799;
    std::cout << 29799;
    for (int i = 0; i < 29799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29799(int& val) {
    const int x = 29799;
    std::cout << 29799;
    for (int i = 0; i < 29799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29800(int& val) {
    func_in_29800(val);
    const int x = 29800;
    std::cout << 29800;
    for (int i = 0; i < 29800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29800(int& val) {
    const int x = 29800;
    std::cout << 29800;
    for (int i = 0; i < 29800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29801(int& val) {
    func_in_29801(val);
    const int x = 29801;
    std::cout << 29801;
    for (int i = 0; i < 29801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29801(int& val) {
    const int x = 29801;
    std::cout << 29801;
    for (int i = 0; i < 29801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29802(int& val) {
    func_in_29802(val);
    const int x = 29802;
    std::cout << 29802;
    for (int i = 0; i < 29802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29802(int& val) {
    const int x = 29802;
    std::cout << 29802;
    for (int i = 0; i < 29802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29803(int& val) {
    func_in_29803(val);
    const int x = 29803;
    std::cout << 29803;
    for (int i = 0; i < 29803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29803(int& val) {
    const int x = 29803;
    std::cout << 29803;
    for (int i = 0; i < 29803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29804(int& val) {
    func_in_29804(val);
    const int x = 29804;
    std::cout << 29804;
    for (int i = 0; i < 29804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29804(int& val) {
    const int x = 29804;
    std::cout << 29804;
    for (int i = 0; i < 29804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29805(int& val) {
    func_in_29805(val);
    const int x = 29805;
    std::cout << 29805;
    for (int i = 0; i < 29805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29805(int& val) {
    const int x = 29805;
    std::cout << 29805;
    for (int i = 0; i < 29805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29806(int& val) {
    func_in_29806(val);
    const int x = 29806;
    std::cout << 29806;
    for (int i = 0; i < 29806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29806(int& val) {
    const int x = 29806;
    std::cout << 29806;
    for (int i = 0; i < 29806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29807(int& val) {
    func_in_29807(val);
    const int x = 29807;
    std::cout << 29807;
    for (int i = 0; i < 29807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29807(int& val) {
    const int x = 29807;
    std::cout << 29807;
    for (int i = 0; i < 29807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29808(int& val) {
    func_in_29808(val);
    const int x = 29808;
    std::cout << 29808;
    for (int i = 0; i < 29808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29808(int& val) {
    const int x = 29808;
    std::cout << 29808;
    for (int i = 0; i < 29808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29809(int& val) {
    func_in_29809(val);
    const int x = 29809;
    std::cout << 29809;
    for (int i = 0; i < 29809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29809(int& val) {
    const int x = 29809;
    std::cout << 29809;
    for (int i = 0; i < 29809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29810(int& val) {
    func_in_29810(val);
    const int x = 29810;
    std::cout << 29810;
    for (int i = 0; i < 29810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29810(int& val) {
    const int x = 29810;
    std::cout << 29810;
    for (int i = 0; i < 29810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29811(int& val) {
    func_in_29811(val);
    const int x = 29811;
    std::cout << 29811;
    for (int i = 0; i < 29811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29811(int& val) {
    const int x = 29811;
    std::cout << 29811;
    for (int i = 0; i < 29811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29812(int& val) {
    func_in_29812(val);
    const int x = 29812;
    std::cout << 29812;
    for (int i = 0; i < 29812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29812(int& val) {
    const int x = 29812;
    std::cout << 29812;
    for (int i = 0; i < 29812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29813(int& val) {
    func_in_29813(val);
    const int x = 29813;
    std::cout << 29813;
    for (int i = 0; i < 29813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29813(int& val) {
    const int x = 29813;
    std::cout << 29813;
    for (int i = 0; i < 29813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29814(int& val) {
    func_in_29814(val);
    const int x = 29814;
    std::cout << 29814;
    for (int i = 0; i < 29814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29814(int& val) {
    const int x = 29814;
    std::cout << 29814;
    for (int i = 0; i < 29814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29815(int& val) {
    func_in_29815(val);
    const int x = 29815;
    std::cout << 29815;
    for (int i = 0; i < 29815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29815(int& val) {
    const int x = 29815;
    std::cout << 29815;
    for (int i = 0; i < 29815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29816(int& val) {
    func_in_29816(val);
    const int x = 29816;
    std::cout << 29816;
    for (int i = 0; i < 29816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29816(int& val) {
    const int x = 29816;
    std::cout << 29816;
    for (int i = 0; i < 29816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29817(int& val) {
    func_in_29817(val);
    const int x = 29817;
    std::cout << 29817;
    for (int i = 0; i < 29817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29817(int& val) {
    const int x = 29817;
    std::cout << 29817;
    for (int i = 0; i < 29817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29818(int& val) {
    func_in_29818(val);
    const int x = 29818;
    std::cout << 29818;
    for (int i = 0; i < 29818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29818(int& val) {
    const int x = 29818;
    std::cout << 29818;
    for (int i = 0; i < 29818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29819(int& val) {
    func_in_29819(val);
    const int x = 29819;
    std::cout << 29819;
    for (int i = 0; i < 29819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29819(int& val) {
    const int x = 29819;
    std::cout << 29819;
    for (int i = 0; i < 29819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29820(int& val) {
    func_in_29820(val);
    const int x = 29820;
    std::cout << 29820;
    for (int i = 0; i < 29820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29820(int& val) {
    const int x = 29820;
    std::cout << 29820;
    for (int i = 0; i < 29820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29821(int& val) {
    func_in_29821(val);
    const int x = 29821;
    std::cout << 29821;
    for (int i = 0; i < 29821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29821(int& val) {
    const int x = 29821;
    std::cout << 29821;
    for (int i = 0; i < 29821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29822(int& val) {
    func_in_29822(val);
    const int x = 29822;
    std::cout << 29822;
    for (int i = 0; i < 29822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29822(int& val) {
    const int x = 29822;
    std::cout << 29822;
    for (int i = 0; i < 29822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29823(int& val) {
    func_in_29823(val);
    const int x = 29823;
    std::cout << 29823;
    for (int i = 0; i < 29823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29823(int& val) {
    const int x = 29823;
    std::cout << 29823;
    for (int i = 0; i < 29823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29824(int& val) {
    func_in_29824(val);
    const int x = 29824;
    std::cout << 29824;
    for (int i = 0; i < 29824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29824(int& val) {
    const int x = 29824;
    std::cout << 29824;
    for (int i = 0; i < 29824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29825(int& val) {
    func_in_29825(val);
    const int x = 29825;
    std::cout << 29825;
    for (int i = 0; i < 29825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29825(int& val) {
    const int x = 29825;
    std::cout << 29825;
    for (int i = 0; i < 29825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29826(int& val) {
    func_in_29826(val);
    const int x = 29826;
    std::cout << 29826;
    for (int i = 0; i < 29826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29826(int& val) {
    const int x = 29826;
    std::cout << 29826;
    for (int i = 0; i < 29826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29827(int& val) {
    func_in_29827(val);
    const int x = 29827;
    std::cout << 29827;
    for (int i = 0; i < 29827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29827(int& val) {
    const int x = 29827;
    std::cout << 29827;
    for (int i = 0; i < 29827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29828(int& val) {
    func_in_29828(val);
    const int x = 29828;
    std::cout << 29828;
    for (int i = 0; i < 29828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29828(int& val) {
    const int x = 29828;
    std::cout << 29828;
    for (int i = 0; i < 29828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29829(int& val) {
    func_in_29829(val);
    const int x = 29829;
    std::cout << 29829;
    for (int i = 0; i < 29829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29829(int& val) {
    const int x = 29829;
    std::cout << 29829;
    for (int i = 0; i < 29829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29830(int& val) {
    func_in_29830(val);
    const int x = 29830;
    std::cout << 29830;
    for (int i = 0; i < 29830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29830(int& val) {
    const int x = 29830;
    std::cout << 29830;
    for (int i = 0; i < 29830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29831(int& val) {
    func_in_29831(val);
    const int x = 29831;
    std::cout << 29831;
    for (int i = 0; i < 29831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29831(int& val) {
    const int x = 29831;
    std::cout << 29831;
    for (int i = 0; i < 29831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29832(int& val) {
    func_in_29832(val);
    const int x = 29832;
    std::cout << 29832;
    for (int i = 0; i < 29832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29832(int& val) {
    const int x = 29832;
    std::cout << 29832;
    for (int i = 0; i < 29832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29833(int& val) {
    func_in_29833(val);
    const int x = 29833;
    std::cout << 29833;
    for (int i = 0; i < 29833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29833(int& val) {
    const int x = 29833;
    std::cout << 29833;
    for (int i = 0; i < 29833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29834(int& val) {
    func_in_29834(val);
    const int x = 29834;
    std::cout << 29834;
    for (int i = 0; i < 29834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29834(int& val) {
    const int x = 29834;
    std::cout << 29834;
    for (int i = 0; i < 29834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29835(int& val) {
    func_in_29835(val);
    const int x = 29835;
    std::cout << 29835;
    for (int i = 0; i < 29835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29835(int& val) {
    const int x = 29835;
    std::cout << 29835;
    for (int i = 0; i < 29835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29836(int& val) {
    func_in_29836(val);
    const int x = 29836;
    std::cout << 29836;
    for (int i = 0; i < 29836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29836(int& val) {
    const int x = 29836;
    std::cout << 29836;
    for (int i = 0; i < 29836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29837(int& val) {
    func_in_29837(val);
    const int x = 29837;
    std::cout << 29837;
    for (int i = 0; i < 29837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29837(int& val) {
    const int x = 29837;
    std::cout << 29837;
    for (int i = 0; i < 29837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29838(int& val) {
    func_in_29838(val);
    const int x = 29838;
    std::cout << 29838;
    for (int i = 0; i < 29838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29838(int& val) {
    const int x = 29838;
    std::cout << 29838;
    for (int i = 0; i < 29838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29839(int& val) {
    func_in_29839(val);
    const int x = 29839;
    std::cout << 29839;
    for (int i = 0; i < 29839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29839(int& val) {
    const int x = 29839;
    std::cout << 29839;
    for (int i = 0; i < 29839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29840(int& val) {
    func_in_29840(val);
    const int x = 29840;
    std::cout << 29840;
    for (int i = 0; i < 29840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29840(int& val) {
    const int x = 29840;
    std::cout << 29840;
    for (int i = 0; i < 29840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29841(int& val) {
    func_in_29841(val);
    const int x = 29841;
    std::cout << 29841;
    for (int i = 0; i < 29841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29841(int& val) {
    const int x = 29841;
    std::cout << 29841;
    for (int i = 0; i < 29841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29842(int& val) {
    func_in_29842(val);
    const int x = 29842;
    std::cout << 29842;
    for (int i = 0; i < 29842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29842(int& val) {
    const int x = 29842;
    std::cout << 29842;
    for (int i = 0; i < 29842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29843(int& val) {
    func_in_29843(val);
    const int x = 29843;
    std::cout << 29843;
    for (int i = 0; i < 29843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29843(int& val) {
    const int x = 29843;
    std::cout << 29843;
    for (int i = 0; i < 29843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29844(int& val) {
    func_in_29844(val);
    const int x = 29844;
    std::cout << 29844;
    for (int i = 0; i < 29844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29844(int& val) {
    const int x = 29844;
    std::cout << 29844;
    for (int i = 0; i < 29844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29845(int& val) {
    func_in_29845(val);
    const int x = 29845;
    std::cout << 29845;
    for (int i = 0; i < 29845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29845(int& val) {
    const int x = 29845;
    std::cout << 29845;
    for (int i = 0; i < 29845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29846(int& val) {
    func_in_29846(val);
    const int x = 29846;
    std::cout << 29846;
    for (int i = 0; i < 29846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29846(int& val) {
    const int x = 29846;
    std::cout << 29846;
    for (int i = 0; i < 29846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29847(int& val) {
    func_in_29847(val);
    const int x = 29847;
    std::cout << 29847;
    for (int i = 0; i < 29847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29847(int& val) {
    const int x = 29847;
    std::cout << 29847;
    for (int i = 0; i < 29847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29848(int& val) {
    func_in_29848(val);
    const int x = 29848;
    std::cout << 29848;
    for (int i = 0; i < 29848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29848(int& val) {
    const int x = 29848;
    std::cout << 29848;
    for (int i = 0; i < 29848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29849(int& val) {
    func_in_29849(val);
    const int x = 29849;
    std::cout << 29849;
    for (int i = 0; i < 29849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29849(int& val) {
    const int x = 29849;
    std::cout << 29849;
    for (int i = 0; i < 29849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29850(int& val) {
    func_in_29850(val);
    const int x = 29850;
    std::cout << 29850;
    for (int i = 0; i < 29850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29850(int& val) {
    const int x = 29850;
    std::cout << 29850;
    for (int i = 0; i < 29850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29851(int& val) {
    func_in_29851(val);
    const int x = 29851;
    std::cout << 29851;
    for (int i = 0; i < 29851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29851(int& val) {
    const int x = 29851;
    std::cout << 29851;
    for (int i = 0; i < 29851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29852(int& val) {
    func_in_29852(val);
    const int x = 29852;
    std::cout << 29852;
    for (int i = 0; i < 29852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29852(int& val) {
    const int x = 29852;
    std::cout << 29852;
    for (int i = 0; i < 29852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29853(int& val) {
    func_in_29853(val);
    const int x = 29853;
    std::cout << 29853;
    for (int i = 0; i < 29853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29853(int& val) {
    const int x = 29853;
    std::cout << 29853;
    for (int i = 0; i < 29853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29854(int& val) {
    func_in_29854(val);
    const int x = 29854;
    std::cout << 29854;
    for (int i = 0; i < 29854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29854(int& val) {
    const int x = 29854;
    std::cout << 29854;
    for (int i = 0; i < 29854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29855(int& val) {
    func_in_29855(val);
    const int x = 29855;
    std::cout << 29855;
    for (int i = 0; i < 29855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29855(int& val) {
    const int x = 29855;
    std::cout << 29855;
    for (int i = 0; i < 29855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29856(int& val) {
    func_in_29856(val);
    const int x = 29856;
    std::cout << 29856;
    for (int i = 0; i < 29856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29856(int& val) {
    const int x = 29856;
    std::cout << 29856;
    for (int i = 0; i < 29856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29857(int& val) {
    func_in_29857(val);
    const int x = 29857;
    std::cout << 29857;
    for (int i = 0; i < 29857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29857(int& val) {
    const int x = 29857;
    std::cout << 29857;
    for (int i = 0; i < 29857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29858(int& val) {
    func_in_29858(val);
    const int x = 29858;
    std::cout << 29858;
    for (int i = 0; i < 29858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29858(int& val) {
    const int x = 29858;
    std::cout << 29858;
    for (int i = 0; i < 29858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29859(int& val) {
    func_in_29859(val);
    const int x = 29859;
    std::cout << 29859;
    for (int i = 0; i < 29859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29859(int& val) {
    const int x = 29859;
    std::cout << 29859;
    for (int i = 0; i < 29859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29860(int& val) {
    func_in_29860(val);
    const int x = 29860;
    std::cout << 29860;
    for (int i = 0; i < 29860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29860(int& val) {
    const int x = 29860;
    std::cout << 29860;
    for (int i = 0; i < 29860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29861(int& val) {
    func_in_29861(val);
    const int x = 29861;
    std::cout << 29861;
    for (int i = 0; i < 29861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29861(int& val) {
    const int x = 29861;
    std::cout << 29861;
    for (int i = 0; i < 29861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29862(int& val) {
    func_in_29862(val);
    const int x = 29862;
    std::cout << 29862;
    for (int i = 0; i < 29862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29862(int& val) {
    const int x = 29862;
    std::cout << 29862;
    for (int i = 0; i < 29862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29863(int& val) {
    func_in_29863(val);
    const int x = 29863;
    std::cout << 29863;
    for (int i = 0; i < 29863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29863(int& val) {
    const int x = 29863;
    std::cout << 29863;
    for (int i = 0; i < 29863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29864(int& val) {
    func_in_29864(val);
    const int x = 29864;
    std::cout << 29864;
    for (int i = 0; i < 29864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29864(int& val) {
    const int x = 29864;
    std::cout << 29864;
    for (int i = 0; i < 29864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29865(int& val) {
    func_in_29865(val);
    const int x = 29865;
    std::cout << 29865;
    for (int i = 0; i < 29865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29865(int& val) {
    const int x = 29865;
    std::cout << 29865;
    for (int i = 0; i < 29865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29866(int& val) {
    func_in_29866(val);
    const int x = 29866;
    std::cout << 29866;
    for (int i = 0; i < 29866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29866(int& val) {
    const int x = 29866;
    std::cout << 29866;
    for (int i = 0; i < 29866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29867(int& val) {
    func_in_29867(val);
    const int x = 29867;
    std::cout << 29867;
    for (int i = 0; i < 29867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29867(int& val) {
    const int x = 29867;
    std::cout << 29867;
    for (int i = 0; i < 29867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29868(int& val) {
    func_in_29868(val);
    const int x = 29868;
    std::cout << 29868;
    for (int i = 0; i < 29868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29868(int& val) {
    const int x = 29868;
    std::cout << 29868;
    for (int i = 0; i < 29868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29869(int& val) {
    func_in_29869(val);
    const int x = 29869;
    std::cout << 29869;
    for (int i = 0; i < 29869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29869(int& val) {
    const int x = 29869;
    std::cout << 29869;
    for (int i = 0; i < 29869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29870(int& val) {
    func_in_29870(val);
    const int x = 29870;
    std::cout << 29870;
    for (int i = 0; i < 29870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29870(int& val) {
    const int x = 29870;
    std::cout << 29870;
    for (int i = 0; i < 29870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29871(int& val) {
    func_in_29871(val);
    const int x = 29871;
    std::cout << 29871;
    for (int i = 0; i < 29871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29871(int& val) {
    const int x = 29871;
    std::cout << 29871;
    for (int i = 0; i < 29871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29872(int& val) {
    func_in_29872(val);
    const int x = 29872;
    std::cout << 29872;
    for (int i = 0; i < 29872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29872(int& val) {
    const int x = 29872;
    std::cout << 29872;
    for (int i = 0; i < 29872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29873(int& val) {
    func_in_29873(val);
    const int x = 29873;
    std::cout << 29873;
    for (int i = 0; i < 29873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29873(int& val) {
    const int x = 29873;
    std::cout << 29873;
    for (int i = 0; i < 29873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29874(int& val) {
    func_in_29874(val);
    const int x = 29874;
    std::cout << 29874;
    for (int i = 0; i < 29874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29874(int& val) {
    const int x = 29874;
    std::cout << 29874;
    for (int i = 0; i < 29874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29875(int& val) {
    func_in_29875(val);
    const int x = 29875;
    std::cout << 29875;
    for (int i = 0; i < 29875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29875(int& val) {
    const int x = 29875;
    std::cout << 29875;
    for (int i = 0; i < 29875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29876(int& val) {
    func_in_29876(val);
    const int x = 29876;
    std::cout << 29876;
    for (int i = 0; i < 29876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29876(int& val) {
    const int x = 29876;
    std::cout << 29876;
    for (int i = 0; i < 29876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29877(int& val) {
    func_in_29877(val);
    const int x = 29877;
    std::cout << 29877;
    for (int i = 0; i < 29877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29877(int& val) {
    const int x = 29877;
    std::cout << 29877;
    for (int i = 0; i < 29877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29878(int& val) {
    func_in_29878(val);
    const int x = 29878;
    std::cout << 29878;
    for (int i = 0; i < 29878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29878(int& val) {
    const int x = 29878;
    std::cout << 29878;
    for (int i = 0; i < 29878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29879(int& val) {
    func_in_29879(val);
    const int x = 29879;
    std::cout << 29879;
    for (int i = 0; i < 29879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29879(int& val) {
    const int x = 29879;
    std::cout << 29879;
    for (int i = 0; i < 29879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29880(int& val) {
    func_in_29880(val);
    const int x = 29880;
    std::cout << 29880;
    for (int i = 0; i < 29880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29880(int& val) {
    const int x = 29880;
    std::cout << 29880;
    for (int i = 0; i < 29880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29881(int& val) {
    func_in_29881(val);
    const int x = 29881;
    std::cout << 29881;
    for (int i = 0; i < 29881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29881(int& val) {
    const int x = 29881;
    std::cout << 29881;
    for (int i = 0; i < 29881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29882(int& val) {
    func_in_29882(val);
    const int x = 29882;
    std::cout << 29882;
    for (int i = 0; i < 29882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29882(int& val) {
    const int x = 29882;
    std::cout << 29882;
    for (int i = 0; i < 29882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29883(int& val) {
    func_in_29883(val);
    const int x = 29883;
    std::cout << 29883;
    for (int i = 0; i < 29883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29883(int& val) {
    const int x = 29883;
    std::cout << 29883;
    for (int i = 0; i < 29883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29884(int& val) {
    func_in_29884(val);
    const int x = 29884;
    std::cout << 29884;
    for (int i = 0; i < 29884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29884(int& val) {
    const int x = 29884;
    std::cout << 29884;
    for (int i = 0; i < 29884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29885(int& val) {
    func_in_29885(val);
    const int x = 29885;
    std::cout << 29885;
    for (int i = 0; i < 29885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29885(int& val) {
    const int x = 29885;
    std::cout << 29885;
    for (int i = 0; i < 29885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29886(int& val) {
    func_in_29886(val);
    const int x = 29886;
    std::cout << 29886;
    for (int i = 0; i < 29886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29886(int& val) {
    const int x = 29886;
    std::cout << 29886;
    for (int i = 0; i < 29886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29887(int& val) {
    func_in_29887(val);
    const int x = 29887;
    std::cout << 29887;
    for (int i = 0; i < 29887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29887(int& val) {
    const int x = 29887;
    std::cout << 29887;
    for (int i = 0; i < 29887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29888(int& val) {
    func_in_29888(val);
    const int x = 29888;
    std::cout << 29888;
    for (int i = 0; i < 29888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29888(int& val) {
    const int x = 29888;
    std::cout << 29888;
    for (int i = 0; i < 29888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29889(int& val) {
    func_in_29889(val);
    const int x = 29889;
    std::cout << 29889;
    for (int i = 0; i < 29889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29889(int& val) {
    const int x = 29889;
    std::cout << 29889;
    for (int i = 0; i < 29889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29890(int& val) {
    func_in_29890(val);
    const int x = 29890;
    std::cout << 29890;
    for (int i = 0; i < 29890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29890(int& val) {
    const int x = 29890;
    std::cout << 29890;
    for (int i = 0; i < 29890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29891(int& val) {
    func_in_29891(val);
    const int x = 29891;
    std::cout << 29891;
    for (int i = 0; i < 29891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29891(int& val) {
    const int x = 29891;
    std::cout << 29891;
    for (int i = 0; i < 29891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29892(int& val) {
    func_in_29892(val);
    const int x = 29892;
    std::cout << 29892;
    for (int i = 0; i < 29892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29892(int& val) {
    const int x = 29892;
    std::cout << 29892;
    for (int i = 0; i < 29892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29893(int& val) {
    func_in_29893(val);
    const int x = 29893;
    std::cout << 29893;
    for (int i = 0; i < 29893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29893(int& val) {
    const int x = 29893;
    std::cout << 29893;
    for (int i = 0; i < 29893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29894(int& val) {
    func_in_29894(val);
    const int x = 29894;
    std::cout << 29894;
    for (int i = 0; i < 29894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29894(int& val) {
    const int x = 29894;
    std::cout << 29894;
    for (int i = 0; i < 29894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29895(int& val) {
    func_in_29895(val);
    const int x = 29895;
    std::cout << 29895;
    for (int i = 0; i < 29895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29895(int& val) {
    const int x = 29895;
    std::cout << 29895;
    for (int i = 0; i < 29895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29896(int& val) {
    func_in_29896(val);
    const int x = 29896;
    std::cout << 29896;
    for (int i = 0; i < 29896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29896(int& val) {
    const int x = 29896;
    std::cout << 29896;
    for (int i = 0; i < 29896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29897(int& val) {
    func_in_29897(val);
    const int x = 29897;
    std::cout << 29897;
    for (int i = 0; i < 29897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29897(int& val) {
    const int x = 29897;
    std::cout << 29897;
    for (int i = 0; i < 29897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29898(int& val) {
    func_in_29898(val);
    const int x = 29898;
    std::cout << 29898;
    for (int i = 0; i < 29898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29898(int& val) {
    const int x = 29898;
    std::cout << 29898;
    for (int i = 0; i < 29898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29899(int& val) {
    func_in_29899(val);
    const int x = 29899;
    std::cout << 29899;
    for (int i = 0; i < 29899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29899(int& val) {
    const int x = 29899;
    std::cout << 29899;
    for (int i = 0; i < 29899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29900(int& val) {
    func_in_29900(val);
    const int x = 29900;
    std::cout << 29900;
    for (int i = 0; i < 29900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29900(int& val) {
    const int x = 29900;
    std::cout << 29900;
    for (int i = 0; i < 29900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29901(int& val) {
    func_in_29901(val);
    const int x = 29901;
    std::cout << 29901;
    for (int i = 0; i < 29901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29901(int& val) {
    const int x = 29901;
    std::cout << 29901;
    for (int i = 0; i < 29901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29902(int& val) {
    func_in_29902(val);
    const int x = 29902;
    std::cout << 29902;
    for (int i = 0; i < 29902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29902(int& val) {
    const int x = 29902;
    std::cout << 29902;
    for (int i = 0; i < 29902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29903(int& val) {
    func_in_29903(val);
    const int x = 29903;
    std::cout << 29903;
    for (int i = 0; i < 29903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29903(int& val) {
    const int x = 29903;
    std::cout << 29903;
    for (int i = 0; i < 29903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29904(int& val) {
    func_in_29904(val);
    const int x = 29904;
    std::cout << 29904;
    for (int i = 0; i < 29904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29904(int& val) {
    const int x = 29904;
    std::cout << 29904;
    for (int i = 0; i < 29904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29905(int& val) {
    func_in_29905(val);
    const int x = 29905;
    std::cout << 29905;
    for (int i = 0; i < 29905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29905(int& val) {
    const int x = 29905;
    std::cout << 29905;
    for (int i = 0; i < 29905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29906(int& val) {
    func_in_29906(val);
    const int x = 29906;
    std::cout << 29906;
    for (int i = 0; i < 29906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29906(int& val) {
    const int x = 29906;
    std::cout << 29906;
    for (int i = 0; i < 29906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29907(int& val) {
    func_in_29907(val);
    const int x = 29907;
    std::cout << 29907;
    for (int i = 0; i < 29907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29907(int& val) {
    const int x = 29907;
    std::cout << 29907;
    for (int i = 0; i < 29907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29908(int& val) {
    func_in_29908(val);
    const int x = 29908;
    std::cout << 29908;
    for (int i = 0; i < 29908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29908(int& val) {
    const int x = 29908;
    std::cout << 29908;
    for (int i = 0; i < 29908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29909(int& val) {
    func_in_29909(val);
    const int x = 29909;
    std::cout << 29909;
    for (int i = 0; i < 29909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29909(int& val) {
    const int x = 29909;
    std::cout << 29909;
    for (int i = 0; i < 29909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29910(int& val) {
    func_in_29910(val);
    const int x = 29910;
    std::cout << 29910;
    for (int i = 0; i < 29910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29910(int& val) {
    const int x = 29910;
    std::cout << 29910;
    for (int i = 0; i < 29910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29911(int& val) {
    func_in_29911(val);
    const int x = 29911;
    std::cout << 29911;
    for (int i = 0; i < 29911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29911(int& val) {
    const int x = 29911;
    std::cout << 29911;
    for (int i = 0; i < 29911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29912(int& val) {
    func_in_29912(val);
    const int x = 29912;
    std::cout << 29912;
    for (int i = 0; i < 29912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29912(int& val) {
    const int x = 29912;
    std::cout << 29912;
    for (int i = 0; i < 29912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29913(int& val) {
    func_in_29913(val);
    const int x = 29913;
    std::cout << 29913;
    for (int i = 0; i < 29913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29913(int& val) {
    const int x = 29913;
    std::cout << 29913;
    for (int i = 0; i < 29913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29914(int& val) {
    func_in_29914(val);
    const int x = 29914;
    std::cout << 29914;
    for (int i = 0; i < 29914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29914(int& val) {
    const int x = 29914;
    std::cout << 29914;
    for (int i = 0; i < 29914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29915(int& val) {
    func_in_29915(val);
    const int x = 29915;
    std::cout << 29915;
    for (int i = 0; i < 29915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29915(int& val) {
    const int x = 29915;
    std::cout << 29915;
    for (int i = 0; i < 29915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29916(int& val) {
    func_in_29916(val);
    const int x = 29916;
    std::cout << 29916;
    for (int i = 0; i < 29916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29916(int& val) {
    const int x = 29916;
    std::cout << 29916;
    for (int i = 0; i < 29916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29917(int& val) {
    func_in_29917(val);
    const int x = 29917;
    std::cout << 29917;
    for (int i = 0; i < 29917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29917(int& val) {
    const int x = 29917;
    std::cout << 29917;
    for (int i = 0; i < 29917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29918(int& val) {
    func_in_29918(val);
    const int x = 29918;
    std::cout << 29918;
    for (int i = 0; i < 29918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29918(int& val) {
    const int x = 29918;
    std::cout << 29918;
    for (int i = 0; i < 29918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29919(int& val) {
    func_in_29919(val);
    const int x = 29919;
    std::cout << 29919;
    for (int i = 0; i < 29919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29919(int& val) {
    const int x = 29919;
    std::cout << 29919;
    for (int i = 0; i < 29919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29920(int& val) {
    func_in_29920(val);
    const int x = 29920;
    std::cout << 29920;
    for (int i = 0; i < 29920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29920(int& val) {
    const int x = 29920;
    std::cout << 29920;
    for (int i = 0; i < 29920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29921(int& val) {
    func_in_29921(val);
    const int x = 29921;
    std::cout << 29921;
    for (int i = 0; i < 29921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29921(int& val) {
    const int x = 29921;
    std::cout << 29921;
    for (int i = 0; i < 29921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29922(int& val) {
    func_in_29922(val);
    const int x = 29922;
    std::cout << 29922;
    for (int i = 0; i < 29922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29922(int& val) {
    const int x = 29922;
    std::cout << 29922;
    for (int i = 0; i < 29922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29923(int& val) {
    func_in_29923(val);
    const int x = 29923;
    std::cout << 29923;
    for (int i = 0; i < 29923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29923(int& val) {
    const int x = 29923;
    std::cout << 29923;
    for (int i = 0; i < 29923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29924(int& val) {
    func_in_29924(val);
    const int x = 29924;
    std::cout << 29924;
    for (int i = 0; i < 29924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29924(int& val) {
    const int x = 29924;
    std::cout << 29924;
    for (int i = 0; i < 29924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29925(int& val) {
    func_in_29925(val);
    const int x = 29925;
    std::cout << 29925;
    for (int i = 0; i < 29925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29925(int& val) {
    const int x = 29925;
    std::cout << 29925;
    for (int i = 0; i < 29925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29926(int& val) {
    func_in_29926(val);
    const int x = 29926;
    std::cout << 29926;
    for (int i = 0; i < 29926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29926(int& val) {
    const int x = 29926;
    std::cout << 29926;
    for (int i = 0; i < 29926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29927(int& val) {
    func_in_29927(val);
    const int x = 29927;
    std::cout << 29927;
    for (int i = 0; i < 29927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29927(int& val) {
    const int x = 29927;
    std::cout << 29927;
    for (int i = 0; i < 29927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29928(int& val) {
    func_in_29928(val);
    const int x = 29928;
    std::cout << 29928;
    for (int i = 0; i < 29928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29928(int& val) {
    const int x = 29928;
    std::cout << 29928;
    for (int i = 0; i < 29928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29929(int& val) {
    func_in_29929(val);
    const int x = 29929;
    std::cout << 29929;
    for (int i = 0; i < 29929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29929(int& val) {
    const int x = 29929;
    std::cout << 29929;
    for (int i = 0; i < 29929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29930(int& val) {
    func_in_29930(val);
    const int x = 29930;
    std::cout << 29930;
    for (int i = 0; i < 29930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29930(int& val) {
    const int x = 29930;
    std::cout << 29930;
    for (int i = 0; i < 29930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29931(int& val) {
    func_in_29931(val);
    const int x = 29931;
    std::cout << 29931;
    for (int i = 0; i < 29931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29931(int& val) {
    const int x = 29931;
    std::cout << 29931;
    for (int i = 0; i < 29931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29932(int& val) {
    func_in_29932(val);
    const int x = 29932;
    std::cout << 29932;
    for (int i = 0; i < 29932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29932(int& val) {
    const int x = 29932;
    std::cout << 29932;
    for (int i = 0; i < 29932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29933(int& val) {
    func_in_29933(val);
    const int x = 29933;
    std::cout << 29933;
    for (int i = 0; i < 29933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29933(int& val) {
    const int x = 29933;
    std::cout << 29933;
    for (int i = 0; i < 29933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29934(int& val) {
    func_in_29934(val);
    const int x = 29934;
    std::cout << 29934;
    for (int i = 0; i < 29934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29934(int& val) {
    const int x = 29934;
    std::cout << 29934;
    for (int i = 0; i < 29934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29935(int& val) {
    func_in_29935(val);
    const int x = 29935;
    std::cout << 29935;
    for (int i = 0; i < 29935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29935(int& val) {
    const int x = 29935;
    std::cout << 29935;
    for (int i = 0; i < 29935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29936(int& val) {
    func_in_29936(val);
    const int x = 29936;
    std::cout << 29936;
    for (int i = 0; i < 29936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29936(int& val) {
    const int x = 29936;
    std::cout << 29936;
    for (int i = 0; i < 29936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29937(int& val) {
    func_in_29937(val);
    const int x = 29937;
    std::cout << 29937;
    for (int i = 0; i < 29937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29937(int& val) {
    const int x = 29937;
    std::cout << 29937;
    for (int i = 0; i < 29937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29938(int& val) {
    func_in_29938(val);
    const int x = 29938;
    std::cout << 29938;
    for (int i = 0; i < 29938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29938(int& val) {
    const int x = 29938;
    std::cout << 29938;
    for (int i = 0; i < 29938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29939(int& val) {
    func_in_29939(val);
    const int x = 29939;
    std::cout << 29939;
    for (int i = 0; i < 29939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29939(int& val) {
    const int x = 29939;
    std::cout << 29939;
    for (int i = 0; i < 29939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29940(int& val) {
    func_in_29940(val);
    const int x = 29940;
    std::cout << 29940;
    for (int i = 0; i < 29940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29940(int& val) {
    const int x = 29940;
    std::cout << 29940;
    for (int i = 0; i < 29940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29941(int& val) {
    func_in_29941(val);
    const int x = 29941;
    std::cout << 29941;
    for (int i = 0; i < 29941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29941(int& val) {
    const int x = 29941;
    std::cout << 29941;
    for (int i = 0; i < 29941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29942(int& val) {
    func_in_29942(val);
    const int x = 29942;
    std::cout << 29942;
    for (int i = 0; i < 29942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29942(int& val) {
    const int x = 29942;
    std::cout << 29942;
    for (int i = 0; i < 29942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29943(int& val) {
    func_in_29943(val);
    const int x = 29943;
    std::cout << 29943;
    for (int i = 0; i < 29943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29943(int& val) {
    const int x = 29943;
    std::cout << 29943;
    for (int i = 0; i < 29943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29944(int& val) {
    func_in_29944(val);
    const int x = 29944;
    std::cout << 29944;
    for (int i = 0; i < 29944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29944(int& val) {
    const int x = 29944;
    std::cout << 29944;
    for (int i = 0; i < 29944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29945(int& val) {
    func_in_29945(val);
    const int x = 29945;
    std::cout << 29945;
    for (int i = 0; i < 29945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29945(int& val) {
    const int x = 29945;
    std::cout << 29945;
    for (int i = 0; i < 29945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29946(int& val) {
    func_in_29946(val);
    const int x = 29946;
    std::cout << 29946;
    for (int i = 0; i < 29946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29946(int& val) {
    const int x = 29946;
    std::cout << 29946;
    for (int i = 0; i < 29946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29947(int& val) {
    func_in_29947(val);
    const int x = 29947;
    std::cout << 29947;
    for (int i = 0; i < 29947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29947(int& val) {
    const int x = 29947;
    std::cout << 29947;
    for (int i = 0; i < 29947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29948(int& val) {
    func_in_29948(val);
    const int x = 29948;
    std::cout << 29948;
    for (int i = 0; i < 29948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29948(int& val) {
    const int x = 29948;
    std::cout << 29948;
    for (int i = 0; i < 29948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29949(int& val) {
    func_in_29949(val);
    const int x = 29949;
    std::cout << 29949;
    for (int i = 0; i < 29949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29949(int& val) {
    const int x = 29949;
    std::cout << 29949;
    for (int i = 0; i < 29949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29950(int& val) {
    func_in_29950(val);
    const int x = 29950;
    std::cout << 29950;
    for (int i = 0; i < 29950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29950(int& val) {
    const int x = 29950;
    std::cout << 29950;
    for (int i = 0; i < 29950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29951(int& val) {
    func_in_29951(val);
    const int x = 29951;
    std::cout << 29951;
    for (int i = 0; i < 29951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29951(int& val) {
    const int x = 29951;
    std::cout << 29951;
    for (int i = 0; i < 29951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29952(int& val) {
    func_in_29952(val);
    const int x = 29952;
    std::cout << 29952;
    for (int i = 0; i < 29952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29952(int& val) {
    const int x = 29952;
    std::cout << 29952;
    for (int i = 0; i < 29952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29953(int& val) {
    func_in_29953(val);
    const int x = 29953;
    std::cout << 29953;
    for (int i = 0; i < 29953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29953(int& val) {
    const int x = 29953;
    std::cout << 29953;
    for (int i = 0; i < 29953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29954(int& val) {
    func_in_29954(val);
    const int x = 29954;
    std::cout << 29954;
    for (int i = 0; i < 29954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29954(int& val) {
    const int x = 29954;
    std::cout << 29954;
    for (int i = 0; i < 29954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29955(int& val) {
    func_in_29955(val);
    const int x = 29955;
    std::cout << 29955;
    for (int i = 0; i < 29955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29955(int& val) {
    const int x = 29955;
    std::cout << 29955;
    for (int i = 0; i < 29955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29956(int& val) {
    func_in_29956(val);
    const int x = 29956;
    std::cout << 29956;
    for (int i = 0; i < 29956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29956(int& val) {
    const int x = 29956;
    std::cout << 29956;
    for (int i = 0; i < 29956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29957(int& val) {
    func_in_29957(val);
    const int x = 29957;
    std::cout << 29957;
    for (int i = 0; i < 29957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29957(int& val) {
    const int x = 29957;
    std::cout << 29957;
    for (int i = 0; i < 29957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29958(int& val) {
    func_in_29958(val);
    const int x = 29958;
    std::cout << 29958;
    for (int i = 0; i < 29958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29958(int& val) {
    const int x = 29958;
    std::cout << 29958;
    for (int i = 0; i < 29958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29959(int& val) {
    func_in_29959(val);
    const int x = 29959;
    std::cout << 29959;
    for (int i = 0; i < 29959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29959(int& val) {
    const int x = 29959;
    std::cout << 29959;
    for (int i = 0; i < 29959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29960(int& val) {
    func_in_29960(val);
    const int x = 29960;
    std::cout << 29960;
    for (int i = 0; i < 29960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29960(int& val) {
    const int x = 29960;
    std::cout << 29960;
    for (int i = 0; i < 29960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29961(int& val) {
    func_in_29961(val);
    const int x = 29961;
    std::cout << 29961;
    for (int i = 0; i < 29961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29961(int& val) {
    const int x = 29961;
    std::cout << 29961;
    for (int i = 0; i < 29961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29962(int& val) {
    func_in_29962(val);
    const int x = 29962;
    std::cout << 29962;
    for (int i = 0; i < 29962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29962(int& val) {
    const int x = 29962;
    std::cout << 29962;
    for (int i = 0; i < 29962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29963(int& val) {
    func_in_29963(val);
    const int x = 29963;
    std::cout << 29963;
    for (int i = 0; i < 29963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29963(int& val) {
    const int x = 29963;
    std::cout << 29963;
    for (int i = 0; i < 29963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29964(int& val) {
    func_in_29964(val);
    const int x = 29964;
    std::cout << 29964;
    for (int i = 0; i < 29964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29964(int& val) {
    const int x = 29964;
    std::cout << 29964;
    for (int i = 0; i < 29964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29965(int& val) {
    func_in_29965(val);
    const int x = 29965;
    std::cout << 29965;
    for (int i = 0; i < 29965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29965(int& val) {
    const int x = 29965;
    std::cout << 29965;
    for (int i = 0; i < 29965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29966(int& val) {
    func_in_29966(val);
    const int x = 29966;
    std::cout << 29966;
    for (int i = 0; i < 29966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29966(int& val) {
    const int x = 29966;
    std::cout << 29966;
    for (int i = 0; i < 29966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29967(int& val) {
    func_in_29967(val);
    const int x = 29967;
    std::cout << 29967;
    for (int i = 0; i < 29967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29967(int& val) {
    const int x = 29967;
    std::cout << 29967;
    for (int i = 0; i < 29967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29968(int& val) {
    func_in_29968(val);
    const int x = 29968;
    std::cout << 29968;
    for (int i = 0; i < 29968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29968(int& val) {
    const int x = 29968;
    std::cout << 29968;
    for (int i = 0; i < 29968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29969(int& val) {
    func_in_29969(val);
    const int x = 29969;
    std::cout << 29969;
    for (int i = 0; i < 29969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29969(int& val) {
    const int x = 29969;
    std::cout << 29969;
    for (int i = 0; i < 29969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29970(int& val) {
    func_in_29970(val);
    const int x = 29970;
    std::cout << 29970;
    for (int i = 0; i < 29970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29970(int& val) {
    const int x = 29970;
    std::cout << 29970;
    for (int i = 0; i < 29970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29971(int& val) {
    func_in_29971(val);
    const int x = 29971;
    std::cout << 29971;
    for (int i = 0; i < 29971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29971(int& val) {
    const int x = 29971;
    std::cout << 29971;
    for (int i = 0; i < 29971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29972(int& val) {
    func_in_29972(val);
    const int x = 29972;
    std::cout << 29972;
    for (int i = 0; i < 29972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29972(int& val) {
    const int x = 29972;
    std::cout << 29972;
    for (int i = 0; i < 29972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29973(int& val) {
    func_in_29973(val);
    const int x = 29973;
    std::cout << 29973;
    for (int i = 0; i < 29973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29973(int& val) {
    const int x = 29973;
    std::cout << 29973;
    for (int i = 0; i < 29973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29974(int& val) {
    func_in_29974(val);
    const int x = 29974;
    std::cout << 29974;
    for (int i = 0; i < 29974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29974(int& val) {
    const int x = 29974;
    std::cout << 29974;
    for (int i = 0; i < 29974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29975(int& val) {
    func_in_29975(val);
    const int x = 29975;
    std::cout << 29975;
    for (int i = 0; i < 29975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29975(int& val) {
    const int x = 29975;
    std::cout << 29975;
    for (int i = 0; i < 29975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29976(int& val) {
    func_in_29976(val);
    const int x = 29976;
    std::cout << 29976;
    for (int i = 0; i < 29976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29976(int& val) {
    const int x = 29976;
    std::cout << 29976;
    for (int i = 0; i < 29976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29977(int& val) {
    func_in_29977(val);
    const int x = 29977;
    std::cout << 29977;
    for (int i = 0; i < 29977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29977(int& val) {
    const int x = 29977;
    std::cout << 29977;
    for (int i = 0; i < 29977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29978(int& val) {
    func_in_29978(val);
    const int x = 29978;
    std::cout << 29978;
    for (int i = 0; i < 29978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29978(int& val) {
    const int x = 29978;
    std::cout << 29978;
    for (int i = 0; i < 29978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29979(int& val) {
    func_in_29979(val);
    const int x = 29979;
    std::cout << 29979;
    for (int i = 0; i < 29979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29979(int& val) {
    const int x = 29979;
    std::cout << 29979;
    for (int i = 0; i < 29979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29980(int& val) {
    func_in_29980(val);
    const int x = 29980;
    std::cout << 29980;
    for (int i = 0; i < 29980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29980(int& val) {
    const int x = 29980;
    std::cout << 29980;
    for (int i = 0; i < 29980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29981(int& val) {
    func_in_29981(val);
    const int x = 29981;
    std::cout << 29981;
    for (int i = 0; i < 29981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29981(int& val) {
    const int x = 29981;
    std::cout << 29981;
    for (int i = 0; i < 29981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29982(int& val) {
    func_in_29982(val);
    const int x = 29982;
    std::cout << 29982;
    for (int i = 0; i < 29982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29982(int& val) {
    const int x = 29982;
    std::cout << 29982;
    for (int i = 0; i < 29982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29983(int& val) {
    func_in_29983(val);
    const int x = 29983;
    std::cout << 29983;
    for (int i = 0; i < 29983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29983(int& val) {
    const int x = 29983;
    std::cout << 29983;
    for (int i = 0; i < 29983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29984(int& val) {
    func_in_29984(val);
    const int x = 29984;
    std::cout << 29984;
    for (int i = 0; i < 29984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29984(int& val) {
    const int x = 29984;
    std::cout << 29984;
    for (int i = 0; i < 29984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29985(int& val) {
    func_in_29985(val);
    const int x = 29985;
    std::cout << 29985;
    for (int i = 0; i < 29985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29985(int& val) {
    const int x = 29985;
    std::cout << 29985;
    for (int i = 0; i < 29985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29986(int& val) {
    func_in_29986(val);
    const int x = 29986;
    std::cout << 29986;
    for (int i = 0; i < 29986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29986(int& val) {
    const int x = 29986;
    std::cout << 29986;
    for (int i = 0; i < 29986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29987(int& val) {
    func_in_29987(val);
    const int x = 29987;
    std::cout << 29987;
    for (int i = 0; i < 29987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29987(int& val) {
    const int x = 29987;
    std::cout << 29987;
    for (int i = 0; i < 29987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29988(int& val) {
    func_in_29988(val);
    const int x = 29988;
    std::cout << 29988;
    for (int i = 0; i < 29988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29988(int& val) {
    const int x = 29988;
    std::cout << 29988;
    for (int i = 0; i < 29988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29989(int& val) {
    func_in_29989(val);
    const int x = 29989;
    std::cout << 29989;
    for (int i = 0; i < 29989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29989(int& val) {
    const int x = 29989;
    std::cout << 29989;
    for (int i = 0; i < 29989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29990(int& val) {
    func_in_29990(val);
    const int x = 29990;
    std::cout << 29990;
    for (int i = 0; i < 29990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29990(int& val) {
    const int x = 29990;
    std::cout << 29990;
    for (int i = 0; i < 29990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29991(int& val) {
    func_in_29991(val);
    const int x = 29991;
    std::cout << 29991;
    for (int i = 0; i < 29991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29991(int& val) {
    const int x = 29991;
    std::cout << 29991;
    for (int i = 0; i < 29991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29992(int& val) {
    func_in_29992(val);
    const int x = 29992;
    std::cout << 29992;
    for (int i = 0; i < 29992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29992(int& val) {
    const int x = 29992;
    std::cout << 29992;
    for (int i = 0; i < 29992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29993(int& val) {
    func_in_29993(val);
    const int x = 29993;
    std::cout << 29993;
    for (int i = 0; i < 29993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29993(int& val) {
    const int x = 29993;
    std::cout << 29993;
    for (int i = 0; i < 29993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29994(int& val) {
    func_in_29994(val);
    const int x = 29994;
    std::cout << 29994;
    for (int i = 0; i < 29994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29994(int& val) {
    const int x = 29994;
    std::cout << 29994;
    for (int i = 0; i < 29994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29995(int& val) {
    func_in_29995(val);
    const int x = 29995;
    std::cout << 29995;
    for (int i = 0; i < 29995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29995(int& val) {
    const int x = 29995;
    std::cout << 29995;
    for (int i = 0; i < 29995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29996(int& val) {
    func_in_29996(val);
    const int x = 29996;
    std::cout << 29996;
    for (int i = 0; i < 29996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29996(int& val) {
    const int x = 29996;
    std::cout << 29996;
    for (int i = 0; i < 29996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29997(int& val) {
    func_in_29997(val);
    const int x = 29997;
    std::cout << 29997;
    for (int i = 0; i < 29997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29997(int& val) {
    const int x = 29997;
    std::cout << 29997;
    for (int i = 0; i < 29997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29998(int& val) {
    func_in_29998(val);
    const int x = 29998;
    std::cout << 29998;
    for (int i = 0; i < 29998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29998(int& val) {
    const int x = 29998;
    std::cout << 29998;
    for (int i = 0; i < 29998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_29999(int& val) {
    func_in_29999(val);
    const int x = 29999;
    std::cout << 29999;
    for (int i = 0; i < 29999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_29999(int& val) {
    const int x = 29999;
    std::cout << 29999;
    for (int i = 0; i < 29999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30000(int& val) {
    func_in_30000(val);
    const int x = 30000;
    std::cout << 30000;
    for (int i = 0; i < 30000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30000(int& val) {
    const int x = 30000;
    std::cout << 30000;
    for (int i = 0; i < 30000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30001(int& val) {
    func_in_30001(val);
    const int x = 30001;
    std::cout << 30001;
    for (int i = 0; i < 30001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30001(int& val) {
    const int x = 30001;
    std::cout << 30001;
    for (int i = 0; i < 30001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30002(int& val) {
    func_in_30002(val);
    const int x = 30002;
    std::cout << 30002;
    for (int i = 0; i < 30002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30002(int& val) {
    const int x = 30002;
    std::cout << 30002;
    for (int i = 0; i < 30002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30003(int& val) {
    func_in_30003(val);
    const int x = 30003;
    std::cout << 30003;
    for (int i = 0; i < 30003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30003(int& val) {
    const int x = 30003;
    std::cout << 30003;
    for (int i = 0; i < 30003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30004(int& val) {
    func_in_30004(val);
    const int x = 30004;
    std::cout << 30004;
    for (int i = 0; i < 30004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30004(int& val) {
    const int x = 30004;
    std::cout << 30004;
    for (int i = 0; i < 30004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30005(int& val) {
    func_in_30005(val);
    const int x = 30005;
    std::cout << 30005;
    for (int i = 0; i < 30005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30005(int& val) {
    const int x = 30005;
    std::cout << 30005;
    for (int i = 0; i < 30005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30006(int& val) {
    func_in_30006(val);
    const int x = 30006;
    std::cout << 30006;
    for (int i = 0; i < 30006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30006(int& val) {
    const int x = 30006;
    std::cout << 30006;
    for (int i = 0; i < 30006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30007(int& val) {
    func_in_30007(val);
    const int x = 30007;
    std::cout << 30007;
    for (int i = 0; i < 30007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30007(int& val) {
    const int x = 30007;
    std::cout << 30007;
    for (int i = 0; i < 30007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30008(int& val) {
    func_in_30008(val);
    const int x = 30008;
    std::cout << 30008;
    for (int i = 0; i < 30008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30008(int& val) {
    const int x = 30008;
    std::cout << 30008;
    for (int i = 0; i < 30008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30009(int& val) {
    func_in_30009(val);
    const int x = 30009;
    std::cout << 30009;
    for (int i = 0; i < 30009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30009(int& val) {
    const int x = 30009;
    std::cout << 30009;
    for (int i = 0; i < 30009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30010(int& val) {
    func_in_30010(val);
    const int x = 30010;
    std::cout << 30010;
    for (int i = 0; i < 30010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30010(int& val) {
    const int x = 30010;
    std::cout << 30010;
    for (int i = 0; i < 30010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30011(int& val) {
    func_in_30011(val);
    const int x = 30011;
    std::cout << 30011;
    for (int i = 0; i < 30011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30011(int& val) {
    const int x = 30011;
    std::cout << 30011;
    for (int i = 0; i < 30011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30012(int& val) {
    func_in_30012(val);
    const int x = 30012;
    std::cout << 30012;
    for (int i = 0; i < 30012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30012(int& val) {
    const int x = 30012;
    std::cout << 30012;
    for (int i = 0; i < 30012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30013(int& val) {
    func_in_30013(val);
    const int x = 30013;
    std::cout << 30013;
    for (int i = 0; i < 30013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30013(int& val) {
    const int x = 30013;
    std::cout << 30013;
    for (int i = 0; i < 30013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30014(int& val) {
    func_in_30014(val);
    const int x = 30014;
    std::cout << 30014;
    for (int i = 0; i < 30014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30014(int& val) {
    const int x = 30014;
    std::cout << 30014;
    for (int i = 0; i < 30014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30015(int& val) {
    func_in_30015(val);
    const int x = 30015;
    std::cout << 30015;
    for (int i = 0; i < 30015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30015(int& val) {
    const int x = 30015;
    std::cout << 30015;
    for (int i = 0; i < 30015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30016(int& val) {
    func_in_30016(val);
    const int x = 30016;
    std::cout << 30016;
    for (int i = 0; i < 30016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30016(int& val) {
    const int x = 30016;
    std::cout << 30016;
    for (int i = 0; i < 30016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30017(int& val) {
    func_in_30017(val);
    const int x = 30017;
    std::cout << 30017;
    for (int i = 0; i < 30017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30017(int& val) {
    const int x = 30017;
    std::cout << 30017;
    for (int i = 0; i < 30017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30018(int& val) {
    func_in_30018(val);
    const int x = 30018;
    std::cout << 30018;
    for (int i = 0; i < 30018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30018(int& val) {
    const int x = 30018;
    std::cout << 30018;
    for (int i = 0; i < 30018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30019(int& val) {
    func_in_30019(val);
    const int x = 30019;
    std::cout << 30019;
    for (int i = 0; i < 30019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30019(int& val) {
    const int x = 30019;
    std::cout << 30019;
    for (int i = 0; i < 30019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30020(int& val) {
    func_in_30020(val);
    const int x = 30020;
    std::cout << 30020;
    for (int i = 0; i < 30020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30020(int& val) {
    const int x = 30020;
    std::cout << 30020;
    for (int i = 0; i < 30020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30021(int& val) {
    func_in_30021(val);
    const int x = 30021;
    std::cout << 30021;
    for (int i = 0; i < 30021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30021(int& val) {
    const int x = 30021;
    std::cout << 30021;
    for (int i = 0; i < 30021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30022(int& val) {
    func_in_30022(val);
    const int x = 30022;
    std::cout << 30022;
    for (int i = 0; i < 30022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30022(int& val) {
    const int x = 30022;
    std::cout << 30022;
    for (int i = 0; i < 30022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30023(int& val) {
    func_in_30023(val);
    const int x = 30023;
    std::cout << 30023;
    for (int i = 0; i < 30023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30023(int& val) {
    const int x = 30023;
    std::cout << 30023;
    for (int i = 0; i < 30023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30024(int& val) {
    func_in_30024(val);
    const int x = 30024;
    std::cout << 30024;
    for (int i = 0; i < 30024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30024(int& val) {
    const int x = 30024;
    std::cout << 30024;
    for (int i = 0; i < 30024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30025(int& val) {
    func_in_30025(val);
    const int x = 30025;
    std::cout << 30025;
    for (int i = 0; i < 30025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30025(int& val) {
    const int x = 30025;
    std::cout << 30025;
    for (int i = 0; i < 30025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30026(int& val) {
    func_in_30026(val);
    const int x = 30026;
    std::cout << 30026;
    for (int i = 0; i < 30026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30026(int& val) {
    const int x = 30026;
    std::cout << 30026;
    for (int i = 0; i < 30026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30027(int& val) {
    func_in_30027(val);
    const int x = 30027;
    std::cout << 30027;
    for (int i = 0; i < 30027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30027(int& val) {
    const int x = 30027;
    std::cout << 30027;
    for (int i = 0; i < 30027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30028(int& val) {
    func_in_30028(val);
    const int x = 30028;
    std::cout << 30028;
    for (int i = 0; i < 30028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30028(int& val) {
    const int x = 30028;
    std::cout << 30028;
    for (int i = 0; i < 30028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30029(int& val) {
    func_in_30029(val);
    const int x = 30029;
    std::cout << 30029;
    for (int i = 0; i < 30029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30029(int& val) {
    const int x = 30029;
    std::cout << 30029;
    for (int i = 0; i < 30029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30030(int& val) {
    func_in_30030(val);
    const int x = 30030;
    std::cout << 30030;
    for (int i = 0; i < 30030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30030(int& val) {
    const int x = 30030;
    std::cout << 30030;
    for (int i = 0; i < 30030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30031(int& val) {
    func_in_30031(val);
    const int x = 30031;
    std::cout << 30031;
    for (int i = 0; i < 30031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30031(int& val) {
    const int x = 30031;
    std::cout << 30031;
    for (int i = 0; i < 30031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30032(int& val) {
    func_in_30032(val);
    const int x = 30032;
    std::cout << 30032;
    for (int i = 0; i < 30032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30032(int& val) {
    const int x = 30032;
    std::cout << 30032;
    for (int i = 0; i < 30032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30033(int& val) {
    func_in_30033(val);
    const int x = 30033;
    std::cout << 30033;
    for (int i = 0; i < 30033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30033(int& val) {
    const int x = 30033;
    std::cout << 30033;
    for (int i = 0; i < 30033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30034(int& val) {
    func_in_30034(val);
    const int x = 30034;
    std::cout << 30034;
    for (int i = 0; i < 30034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30034(int& val) {
    const int x = 30034;
    std::cout << 30034;
    for (int i = 0; i < 30034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30035(int& val) {
    func_in_30035(val);
    const int x = 30035;
    std::cout << 30035;
    for (int i = 0; i < 30035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30035(int& val) {
    const int x = 30035;
    std::cout << 30035;
    for (int i = 0; i < 30035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30036(int& val) {
    func_in_30036(val);
    const int x = 30036;
    std::cout << 30036;
    for (int i = 0; i < 30036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30036(int& val) {
    const int x = 30036;
    std::cout << 30036;
    for (int i = 0; i < 30036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30037(int& val) {
    func_in_30037(val);
    const int x = 30037;
    std::cout << 30037;
    for (int i = 0; i < 30037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30037(int& val) {
    const int x = 30037;
    std::cout << 30037;
    for (int i = 0; i < 30037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30038(int& val) {
    func_in_30038(val);
    const int x = 30038;
    std::cout << 30038;
    for (int i = 0; i < 30038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30038(int& val) {
    const int x = 30038;
    std::cout << 30038;
    for (int i = 0; i < 30038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30039(int& val) {
    func_in_30039(val);
    const int x = 30039;
    std::cout << 30039;
    for (int i = 0; i < 30039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30039(int& val) {
    const int x = 30039;
    std::cout << 30039;
    for (int i = 0; i < 30039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30040(int& val) {
    func_in_30040(val);
    const int x = 30040;
    std::cout << 30040;
    for (int i = 0; i < 30040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30040(int& val) {
    const int x = 30040;
    std::cout << 30040;
    for (int i = 0; i < 30040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30041(int& val) {
    func_in_30041(val);
    const int x = 30041;
    std::cout << 30041;
    for (int i = 0; i < 30041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30041(int& val) {
    const int x = 30041;
    std::cout << 30041;
    for (int i = 0; i < 30041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30042(int& val) {
    func_in_30042(val);
    const int x = 30042;
    std::cout << 30042;
    for (int i = 0; i < 30042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30042(int& val) {
    const int x = 30042;
    std::cout << 30042;
    for (int i = 0; i < 30042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30043(int& val) {
    func_in_30043(val);
    const int x = 30043;
    std::cout << 30043;
    for (int i = 0; i < 30043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30043(int& val) {
    const int x = 30043;
    std::cout << 30043;
    for (int i = 0; i < 30043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30044(int& val) {
    func_in_30044(val);
    const int x = 30044;
    std::cout << 30044;
    for (int i = 0; i < 30044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30044(int& val) {
    const int x = 30044;
    std::cout << 30044;
    for (int i = 0; i < 30044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30045(int& val) {
    func_in_30045(val);
    const int x = 30045;
    std::cout << 30045;
    for (int i = 0; i < 30045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30045(int& val) {
    const int x = 30045;
    std::cout << 30045;
    for (int i = 0; i < 30045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30046(int& val) {
    func_in_30046(val);
    const int x = 30046;
    std::cout << 30046;
    for (int i = 0; i < 30046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30046(int& val) {
    const int x = 30046;
    std::cout << 30046;
    for (int i = 0; i < 30046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30047(int& val) {
    func_in_30047(val);
    const int x = 30047;
    std::cout << 30047;
    for (int i = 0; i < 30047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30047(int& val) {
    const int x = 30047;
    std::cout << 30047;
    for (int i = 0; i < 30047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30048(int& val) {
    func_in_30048(val);
    const int x = 30048;
    std::cout << 30048;
    for (int i = 0; i < 30048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30048(int& val) {
    const int x = 30048;
    std::cout << 30048;
    for (int i = 0; i < 30048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30049(int& val) {
    func_in_30049(val);
    const int x = 30049;
    std::cout << 30049;
    for (int i = 0; i < 30049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30049(int& val) {
    const int x = 30049;
    std::cout << 30049;
    for (int i = 0; i < 30049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30050(int& val) {
    func_in_30050(val);
    const int x = 30050;
    std::cout << 30050;
    for (int i = 0; i < 30050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30050(int& val) {
    const int x = 30050;
    std::cout << 30050;
    for (int i = 0; i < 30050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30051(int& val) {
    func_in_30051(val);
    const int x = 30051;
    std::cout << 30051;
    for (int i = 0; i < 30051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30051(int& val) {
    const int x = 30051;
    std::cout << 30051;
    for (int i = 0; i < 30051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30052(int& val) {
    func_in_30052(val);
    const int x = 30052;
    std::cout << 30052;
    for (int i = 0; i < 30052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30052(int& val) {
    const int x = 30052;
    std::cout << 30052;
    for (int i = 0; i < 30052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30053(int& val) {
    func_in_30053(val);
    const int x = 30053;
    std::cout << 30053;
    for (int i = 0; i < 30053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30053(int& val) {
    const int x = 30053;
    std::cout << 30053;
    for (int i = 0; i < 30053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30054(int& val) {
    func_in_30054(val);
    const int x = 30054;
    std::cout << 30054;
    for (int i = 0; i < 30054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30054(int& val) {
    const int x = 30054;
    std::cout << 30054;
    for (int i = 0; i < 30054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30055(int& val) {
    func_in_30055(val);
    const int x = 30055;
    std::cout << 30055;
    for (int i = 0; i < 30055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30055(int& val) {
    const int x = 30055;
    std::cout << 30055;
    for (int i = 0; i < 30055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30056(int& val) {
    func_in_30056(val);
    const int x = 30056;
    std::cout << 30056;
    for (int i = 0; i < 30056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30056(int& val) {
    const int x = 30056;
    std::cout << 30056;
    for (int i = 0; i < 30056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30057(int& val) {
    func_in_30057(val);
    const int x = 30057;
    std::cout << 30057;
    for (int i = 0; i < 30057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30057(int& val) {
    const int x = 30057;
    std::cout << 30057;
    for (int i = 0; i < 30057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30058(int& val) {
    func_in_30058(val);
    const int x = 30058;
    std::cout << 30058;
    for (int i = 0; i < 30058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30058(int& val) {
    const int x = 30058;
    std::cout << 30058;
    for (int i = 0; i < 30058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30059(int& val) {
    func_in_30059(val);
    const int x = 30059;
    std::cout << 30059;
    for (int i = 0; i < 30059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30059(int& val) {
    const int x = 30059;
    std::cout << 30059;
    for (int i = 0; i < 30059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30060(int& val) {
    func_in_30060(val);
    const int x = 30060;
    std::cout << 30060;
    for (int i = 0; i < 30060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30060(int& val) {
    const int x = 30060;
    std::cout << 30060;
    for (int i = 0; i < 30060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30061(int& val) {
    func_in_30061(val);
    const int x = 30061;
    std::cout << 30061;
    for (int i = 0; i < 30061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30061(int& val) {
    const int x = 30061;
    std::cout << 30061;
    for (int i = 0; i < 30061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30062(int& val) {
    func_in_30062(val);
    const int x = 30062;
    std::cout << 30062;
    for (int i = 0; i < 30062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30062(int& val) {
    const int x = 30062;
    std::cout << 30062;
    for (int i = 0; i < 30062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30063(int& val) {
    func_in_30063(val);
    const int x = 30063;
    std::cout << 30063;
    for (int i = 0; i < 30063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30063(int& val) {
    const int x = 30063;
    std::cout << 30063;
    for (int i = 0; i < 30063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30064(int& val) {
    func_in_30064(val);
    const int x = 30064;
    std::cout << 30064;
    for (int i = 0; i < 30064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30064(int& val) {
    const int x = 30064;
    std::cout << 30064;
    for (int i = 0; i < 30064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30065(int& val) {
    func_in_30065(val);
    const int x = 30065;
    std::cout << 30065;
    for (int i = 0; i < 30065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30065(int& val) {
    const int x = 30065;
    std::cout << 30065;
    for (int i = 0; i < 30065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30066(int& val) {
    func_in_30066(val);
    const int x = 30066;
    std::cout << 30066;
    for (int i = 0; i < 30066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30066(int& val) {
    const int x = 30066;
    std::cout << 30066;
    for (int i = 0; i < 30066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30067(int& val) {
    func_in_30067(val);
    const int x = 30067;
    std::cout << 30067;
    for (int i = 0; i < 30067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30067(int& val) {
    const int x = 30067;
    std::cout << 30067;
    for (int i = 0; i < 30067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30068(int& val) {
    func_in_30068(val);
    const int x = 30068;
    std::cout << 30068;
    for (int i = 0; i < 30068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30068(int& val) {
    const int x = 30068;
    std::cout << 30068;
    for (int i = 0; i < 30068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30069(int& val) {
    func_in_30069(val);
    const int x = 30069;
    std::cout << 30069;
    for (int i = 0; i < 30069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30069(int& val) {
    const int x = 30069;
    std::cout << 30069;
    for (int i = 0; i < 30069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30070(int& val) {
    func_in_30070(val);
    const int x = 30070;
    std::cout << 30070;
    for (int i = 0; i < 30070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30070(int& val) {
    const int x = 30070;
    std::cout << 30070;
    for (int i = 0; i < 30070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30071(int& val) {
    func_in_30071(val);
    const int x = 30071;
    std::cout << 30071;
    for (int i = 0; i < 30071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30071(int& val) {
    const int x = 30071;
    std::cout << 30071;
    for (int i = 0; i < 30071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30072(int& val) {
    func_in_30072(val);
    const int x = 30072;
    std::cout << 30072;
    for (int i = 0; i < 30072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30072(int& val) {
    const int x = 30072;
    std::cout << 30072;
    for (int i = 0; i < 30072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30073(int& val) {
    func_in_30073(val);
    const int x = 30073;
    std::cout << 30073;
    for (int i = 0; i < 30073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30073(int& val) {
    const int x = 30073;
    std::cout << 30073;
    for (int i = 0; i < 30073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30074(int& val) {
    func_in_30074(val);
    const int x = 30074;
    std::cout << 30074;
    for (int i = 0; i < 30074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30074(int& val) {
    const int x = 30074;
    std::cout << 30074;
    for (int i = 0; i < 30074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30075(int& val) {
    func_in_30075(val);
    const int x = 30075;
    std::cout << 30075;
    for (int i = 0; i < 30075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30075(int& val) {
    const int x = 30075;
    std::cout << 30075;
    for (int i = 0; i < 30075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30076(int& val) {
    func_in_30076(val);
    const int x = 30076;
    std::cout << 30076;
    for (int i = 0; i < 30076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30076(int& val) {
    const int x = 30076;
    std::cout << 30076;
    for (int i = 0; i < 30076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30077(int& val) {
    func_in_30077(val);
    const int x = 30077;
    std::cout << 30077;
    for (int i = 0; i < 30077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30077(int& val) {
    const int x = 30077;
    std::cout << 30077;
    for (int i = 0; i < 30077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30078(int& val) {
    func_in_30078(val);
    const int x = 30078;
    std::cout << 30078;
    for (int i = 0; i < 30078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30078(int& val) {
    const int x = 30078;
    std::cout << 30078;
    for (int i = 0; i < 30078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30079(int& val) {
    func_in_30079(val);
    const int x = 30079;
    std::cout << 30079;
    for (int i = 0; i < 30079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30079(int& val) {
    const int x = 30079;
    std::cout << 30079;
    for (int i = 0; i < 30079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30080(int& val) {
    func_in_30080(val);
    const int x = 30080;
    std::cout << 30080;
    for (int i = 0; i < 30080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30080(int& val) {
    const int x = 30080;
    std::cout << 30080;
    for (int i = 0; i < 30080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30081(int& val) {
    func_in_30081(val);
    const int x = 30081;
    std::cout << 30081;
    for (int i = 0; i < 30081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30081(int& val) {
    const int x = 30081;
    std::cout << 30081;
    for (int i = 0; i < 30081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30082(int& val) {
    func_in_30082(val);
    const int x = 30082;
    std::cout << 30082;
    for (int i = 0; i < 30082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30082(int& val) {
    const int x = 30082;
    std::cout << 30082;
    for (int i = 0; i < 30082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30083(int& val) {
    func_in_30083(val);
    const int x = 30083;
    std::cout << 30083;
    for (int i = 0; i < 30083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30083(int& val) {
    const int x = 30083;
    std::cout << 30083;
    for (int i = 0; i < 30083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30084(int& val) {
    func_in_30084(val);
    const int x = 30084;
    std::cout << 30084;
    for (int i = 0; i < 30084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30084(int& val) {
    const int x = 30084;
    std::cout << 30084;
    for (int i = 0; i < 30084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30085(int& val) {
    func_in_30085(val);
    const int x = 30085;
    std::cout << 30085;
    for (int i = 0; i < 30085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30085(int& val) {
    const int x = 30085;
    std::cout << 30085;
    for (int i = 0; i < 30085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30086(int& val) {
    func_in_30086(val);
    const int x = 30086;
    std::cout << 30086;
    for (int i = 0; i < 30086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30086(int& val) {
    const int x = 30086;
    std::cout << 30086;
    for (int i = 0; i < 30086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30087(int& val) {
    func_in_30087(val);
    const int x = 30087;
    std::cout << 30087;
    for (int i = 0; i < 30087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30087(int& val) {
    const int x = 30087;
    std::cout << 30087;
    for (int i = 0; i < 30087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30088(int& val) {
    func_in_30088(val);
    const int x = 30088;
    std::cout << 30088;
    for (int i = 0; i < 30088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30088(int& val) {
    const int x = 30088;
    std::cout << 30088;
    for (int i = 0; i < 30088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30089(int& val) {
    func_in_30089(val);
    const int x = 30089;
    std::cout << 30089;
    for (int i = 0; i < 30089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30089(int& val) {
    const int x = 30089;
    std::cout << 30089;
    for (int i = 0; i < 30089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30090(int& val) {
    func_in_30090(val);
    const int x = 30090;
    std::cout << 30090;
    for (int i = 0; i < 30090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30090(int& val) {
    const int x = 30090;
    std::cout << 30090;
    for (int i = 0; i < 30090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30091(int& val) {
    func_in_30091(val);
    const int x = 30091;
    std::cout << 30091;
    for (int i = 0; i < 30091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30091(int& val) {
    const int x = 30091;
    std::cout << 30091;
    for (int i = 0; i < 30091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30092(int& val) {
    func_in_30092(val);
    const int x = 30092;
    std::cout << 30092;
    for (int i = 0; i < 30092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30092(int& val) {
    const int x = 30092;
    std::cout << 30092;
    for (int i = 0; i < 30092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30093(int& val) {
    func_in_30093(val);
    const int x = 30093;
    std::cout << 30093;
    for (int i = 0; i < 30093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30093(int& val) {
    const int x = 30093;
    std::cout << 30093;
    for (int i = 0; i < 30093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30094(int& val) {
    func_in_30094(val);
    const int x = 30094;
    std::cout << 30094;
    for (int i = 0; i < 30094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30094(int& val) {
    const int x = 30094;
    std::cout << 30094;
    for (int i = 0; i < 30094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30095(int& val) {
    func_in_30095(val);
    const int x = 30095;
    std::cout << 30095;
    for (int i = 0; i < 30095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30095(int& val) {
    const int x = 30095;
    std::cout << 30095;
    for (int i = 0; i < 30095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30096(int& val) {
    func_in_30096(val);
    const int x = 30096;
    std::cout << 30096;
    for (int i = 0; i < 30096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30096(int& val) {
    const int x = 30096;
    std::cout << 30096;
    for (int i = 0; i < 30096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30097(int& val) {
    func_in_30097(val);
    const int x = 30097;
    std::cout << 30097;
    for (int i = 0; i < 30097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30097(int& val) {
    const int x = 30097;
    std::cout << 30097;
    for (int i = 0; i < 30097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30098(int& val) {
    func_in_30098(val);
    const int x = 30098;
    std::cout << 30098;
    for (int i = 0; i < 30098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30098(int& val) {
    const int x = 30098;
    std::cout << 30098;
    for (int i = 0; i < 30098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30099(int& val) {
    func_in_30099(val);
    const int x = 30099;
    std::cout << 30099;
    for (int i = 0; i < 30099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30099(int& val) {
    const int x = 30099;
    std::cout << 30099;
    for (int i = 0; i < 30099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30100(int& val) {
    func_in_30100(val);
    const int x = 30100;
    std::cout << 30100;
    for (int i = 0; i < 30100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30100(int& val) {
    const int x = 30100;
    std::cout << 30100;
    for (int i = 0; i < 30100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30101(int& val) {
    func_in_30101(val);
    const int x = 30101;
    std::cout << 30101;
    for (int i = 0; i < 30101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30101(int& val) {
    const int x = 30101;
    std::cout << 30101;
    for (int i = 0; i < 30101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30102(int& val) {
    func_in_30102(val);
    const int x = 30102;
    std::cout << 30102;
    for (int i = 0; i < 30102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30102(int& val) {
    const int x = 30102;
    std::cout << 30102;
    for (int i = 0; i < 30102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30103(int& val) {
    func_in_30103(val);
    const int x = 30103;
    std::cout << 30103;
    for (int i = 0; i < 30103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30103(int& val) {
    const int x = 30103;
    std::cout << 30103;
    for (int i = 0; i < 30103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30104(int& val) {
    func_in_30104(val);
    const int x = 30104;
    std::cout << 30104;
    for (int i = 0; i < 30104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30104(int& val) {
    const int x = 30104;
    std::cout << 30104;
    for (int i = 0; i < 30104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30105(int& val) {
    func_in_30105(val);
    const int x = 30105;
    std::cout << 30105;
    for (int i = 0; i < 30105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30105(int& val) {
    const int x = 30105;
    std::cout << 30105;
    for (int i = 0; i < 30105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30106(int& val) {
    func_in_30106(val);
    const int x = 30106;
    std::cout << 30106;
    for (int i = 0; i < 30106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30106(int& val) {
    const int x = 30106;
    std::cout << 30106;
    for (int i = 0; i < 30106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30107(int& val) {
    func_in_30107(val);
    const int x = 30107;
    std::cout << 30107;
    for (int i = 0; i < 30107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30107(int& val) {
    const int x = 30107;
    std::cout << 30107;
    for (int i = 0; i < 30107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30108(int& val) {
    func_in_30108(val);
    const int x = 30108;
    std::cout << 30108;
    for (int i = 0; i < 30108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30108(int& val) {
    const int x = 30108;
    std::cout << 30108;
    for (int i = 0; i < 30108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30109(int& val) {
    func_in_30109(val);
    const int x = 30109;
    std::cout << 30109;
    for (int i = 0; i < 30109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30109(int& val) {
    const int x = 30109;
    std::cout << 30109;
    for (int i = 0; i < 30109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30110(int& val) {
    func_in_30110(val);
    const int x = 30110;
    std::cout << 30110;
    for (int i = 0; i < 30110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30110(int& val) {
    const int x = 30110;
    std::cout << 30110;
    for (int i = 0; i < 30110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30111(int& val) {
    func_in_30111(val);
    const int x = 30111;
    std::cout << 30111;
    for (int i = 0; i < 30111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30111(int& val) {
    const int x = 30111;
    std::cout << 30111;
    for (int i = 0; i < 30111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30112(int& val) {
    func_in_30112(val);
    const int x = 30112;
    std::cout << 30112;
    for (int i = 0; i < 30112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30112(int& val) {
    const int x = 30112;
    std::cout << 30112;
    for (int i = 0; i < 30112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30113(int& val) {
    func_in_30113(val);
    const int x = 30113;
    std::cout << 30113;
    for (int i = 0; i < 30113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30113(int& val) {
    const int x = 30113;
    std::cout << 30113;
    for (int i = 0; i < 30113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30114(int& val) {
    func_in_30114(val);
    const int x = 30114;
    std::cout << 30114;
    for (int i = 0; i < 30114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30114(int& val) {
    const int x = 30114;
    std::cout << 30114;
    for (int i = 0; i < 30114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30115(int& val) {
    func_in_30115(val);
    const int x = 30115;
    std::cout << 30115;
    for (int i = 0; i < 30115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30115(int& val) {
    const int x = 30115;
    std::cout << 30115;
    for (int i = 0; i < 30115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30116(int& val) {
    func_in_30116(val);
    const int x = 30116;
    std::cout << 30116;
    for (int i = 0; i < 30116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30116(int& val) {
    const int x = 30116;
    std::cout << 30116;
    for (int i = 0; i < 30116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30117(int& val) {
    func_in_30117(val);
    const int x = 30117;
    std::cout << 30117;
    for (int i = 0; i < 30117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30117(int& val) {
    const int x = 30117;
    std::cout << 30117;
    for (int i = 0; i < 30117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30118(int& val) {
    func_in_30118(val);
    const int x = 30118;
    std::cout << 30118;
    for (int i = 0; i < 30118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30118(int& val) {
    const int x = 30118;
    std::cout << 30118;
    for (int i = 0; i < 30118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30119(int& val) {
    func_in_30119(val);
    const int x = 30119;
    std::cout << 30119;
    for (int i = 0; i < 30119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30119(int& val) {
    const int x = 30119;
    std::cout << 30119;
    for (int i = 0; i < 30119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30120(int& val) {
    func_in_30120(val);
    const int x = 30120;
    std::cout << 30120;
    for (int i = 0; i < 30120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30120(int& val) {
    const int x = 30120;
    std::cout << 30120;
    for (int i = 0; i < 30120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30121(int& val) {
    func_in_30121(val);
    const int x = 30121;
    std::cout << 30121;
    for (int i = 0; i < 30121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30121(int& val) {
    const int x = 30121;
    std::cout << 30121;
    for (int i = 0; i < 30121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30122(int& val) {
    func_in_30122(val);
    const int x = 30122;
    std::cout << 30122;
    for (int i = 0; i < 30122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30122(int& val) {
    const int x = 30122;
    std::cout << 30122;
    for (int i = 0; i < 30122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30123(int& val) {
    func_in_30123(val);
    const int x = 30123;
    std::cout << 30123;
    for (int i = 0; i < 30123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30123(int& val) {
    const int x = 30123;
    std::cout << 30123;
    for (int i = 0; i < 30123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30124(int& val) {
    func_in_30124(val);
    const int x = 30124;
    std::cout << 30124;
    for (int i = 0; i < 30124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30124(int& val) {
    const int x = 30124;
    std::cout << 30124;
    for (int i = 0; i < 30124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30125(int& val) {
    func_in_30125(val);
    const int x = 30125;
    std::cout << 30125;
    for (int i = 0; i < 30125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30125(int& val) {
    const int x = 30125;
    std::cout << 30125;
    for (int i = 0; i < 30125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30126(int& val) {
    func_in_30126(val);
    const int x = 30126;
    std::cout << 30126;
    for (int i = 0; i < 30126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30126(int& val) {
    const int x = 30126;
    std::cout << 30126;
    for (int i = 0; i < 30126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30127(int& val) {
    func_in_30127(val);
    const int x = 30127;
    std::cout << 30127;
    for (int i = 0; i < 30127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30127(int& val) {
    const int x = 30127;
    std::cout << 30127;
    for (int i = 0; i < 30127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30128(int& val) {
    func_in_30128(val);
    const int x = 30128;
    std::cout << 30128;
    for (int i = 0; i < 30128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30128(int& val) {
    const int x = 30128;
    std::cout << 30128;
    for (int i = 0; i < 30128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30129(int& val) {
    func_in_30129(val);
    const int x = 30129;
    std::cout << 30129;
    for (int i = 0; i < 30129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30129(int& val) {
    const int x = 30129;
    std::cout << 30129;
    for (int i = 0; i < 30129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30130(int& val) {
    func_in_30130(val);
    const int x = 30130;
    std::cout << 30130;
    for (int i = 0; i < 30130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30130(int& val) {
    const int x = 30130;
    std::cout << 30130;
    for (int i = 0; i < 30130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30131(int& val) {
    func_in_30131(val);
    const int x = 30131;
    std::cout << 30131;
    for (int i = 0; i < 30131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30131(int& val) {
    const int x = 30131;
    std::cout << 30131;
    for (int i = 0; i < 30131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30132(int& val) {
    func_in_30132(val);
    const int x = 30132;
    std::cout << 30132;
    for (int i = 0; i < 30132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30132(int& val) {
    const int x = 30132;
    std::cout << 30132;
    for (int i = 0; i < 30132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30133(int& val) {
    func_in_30133(val);
    const int x = 30133;
    std::cout << 30133;
    for (int i = 0; i < 30133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30133(int& val) {
    const int x = 30133;
    std::cout << 30133;
    for (int i = 0; i < 30133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30134(int& val) {
    func_in_30134(val);
    const int x = 30134;
    std::cout << 30134;
    for (int i = 0; i < 30134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30134(int& val) {
    const int x = 30134;
    std::cout << 30134;
    for (int i = 0; i < 30134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30135(int& val) {
    func_in_30135(val);
    const int x = 30135;
    std::cout << 30135;
    for (int i = 0; i < 30135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30135(int& val) {
    const int x = 30135;
    std::cout << 30135;
    for (int i = 0; i < 30135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30136(int& val) {
    func_in_30136(val);
    const int x = 30136;
    std::cout << 30136;
    for (int i = 0; i < 30136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30136(int& val) {
    const int x = 30136;
    std::cout << 30136;
    for (int i = 0; i < 30136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30137(int& val) {
    func_in_30137(val);
    const int x = 30137;
    std::cout << 30137;
    for (int i = 0; i < 30137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30137(int& val) {
    const int x = 30137;
    std::cout << 30137;
    for (int i = 0; i < 30137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30138(int& val) {
    func_in_30138(val);
    const int x = 30138;
    std::cout << 30138;
    for (int i = 0; i < 30138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30138(int& val) {
    const int x = 30138;
    std::cout << 30138;
    for (int i = 0; i < 30138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30139(int& val) {
    func_in_30139(val);
    const int x = 30139;
    std::cout << 30139;
    for (int i = 0; i < 30139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30139(int& val) {
    const int x = 30139;
    std::cout << 30139;
    for (int i = 0; i < 30139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30140(int& val) {
    func_in_30140(val);
    const int x = 30140;
    std::cout << 30140;
    for (int i = 0; i < 30140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30140(int& val) {
    const int x = 30140;
    std::cout << 30140;
    for (int i = 0; i < 30140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30141(int& val) {
    func_in_30141(val);
    const int x = 30141;
    std::cout << 30141;
    for (int i = 0; i < 30141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30141(int& val) {
    const int x = 30141;
    std::cout << 30141;
    for (int i = 0; i < 30141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30142(int& val) {
    func_in_30142(val);
    const int x = 30142;
    std::cout << 30142;
    for (int i = 0; i < 30142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30142(int& val) {
    const int x = 30142;
    std::cout << 30142;
    for (int i = 0; i < 30142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30143(int& val) {
    func_in_30143(val);
    const int x = 30143;
    std::cout << 30143;
    for (int i = 0; i < 30143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30143(int& val) {
    const int x = 30143;
    std::cout << 30143;
    for (int i = 0; i < 30143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30144(int& val) {
    func_in_30144(val);
    const int x = 30144;
    std::cout << 30144;
    for (int i = 0; i < 30144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30144(int& val) {
    const int x = 30144;
    std::cout << 30144;
    for (int i = 0; i < 30144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30145(int& val) {
    func_in_30145(val);
    const int x = 30145;
    std::cout << 30145;
    for (int i = 0; i < 30145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30145(int& val) {
    const int x = 30145;
    std::cout << 30145;
    for (int i = 0; i < 30145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30146(int& val) {
    func_in_30146(val);
    const int x = 30146;
    std::cout << 30146;
    for (int i = 0; i < 30146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30146(int& val) {
    const int x = 30146;
    std::cout << 30146;
    for (int i = 0; i < 30146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30147(int& val) {
    func_in_30147(val);
    const int x = 30147;
    std::cout << 30147;
    for (int i = 0; i < 30147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30147(int& val) {
    const int x = 30147;
    std::cout << 30147;
    for (int i = 0; i < 30147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30148(int& val) {
    func_in_30148(val);
    const int x = 30148;
    std::cout << 30148;
    for (int i = 0; i < 30148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30148(int& val) {
    const int x = 30148;
    std::cout << 30148;
    for (int i = 0; i < 30148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30149(int& val) {
    func_in_30149(val);
    const int x = 30149;
    std::cout << 30149;
    for (int i = 0; i < 30149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30149(int& val) {
    const int x = 30149;
    std::cout << 30149;
    for (int i = 0; i < 30149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30150(int& val) {
    func_in_30150(val);
    const int x = 30150;
    std::cout << 30150;
    for (int i = 0; i < 30150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30150(int& val) {
    const int x = 30150;
    std::cout << 30150;
    for (int i = 0; i < 30150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30151(int& val) {
    func_in_30151(val);
    const int x = 30151;
    std::cout << 30151;
    for (int i = 0; i < 30151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30151(int& val) {
    const int x = 30151;
    std::cout << 30151;
    for (int i = 0; i < 30151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30152(int& val) {
    func_in_30152(val);
    const int x = 30152;
    std::cout << 30152;
    for (int i = 0; i < 30152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30152(int& val) {
    const int x = 30152;
    std::cout << 30152;
    for (int i = 0; i < 30152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30153(int& val) {
    func_in_30153(val);
    const int x = 30153;
    std::cout << 30153;
    for (int i = 0; i < 30153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30153(int& val) {
    const int x = 30153;
    std::cout << 30153;
    for (int i = 0; i < 30153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30154(int& val) {
    func_in_30154(val);
    const int x = 30154;
    std::cout << 30154;
    for (int i = 0; i < 30154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30154(int& val) {
    const int x = 30154;
    std::cout << 30154;
    for (int i = 0; i < 30154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30155(int& val) {
    func_in_30155(val);
    const int x = 30155;
    std::cout << 30155;
    for (int i = 0; i < 30155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30155(int& val) {
    const int x = 30155;
    std::cout << 30155;
    for (int i = 0; i < 30155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30156(int& val) {
    func_in_30156(val);
    const int x = 30156;
    std::cout << 30156;
    for (int i = 0; i < 30156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30156(int& val) {
    const int x = 30156;
    std::cout << 30156;
    for (int i = 0; i < 30156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30157(int& val) {
    func_in_30157(val);
    const int x = 30157;
    std::cout << 30157;
    for (int i = 0; i < 30157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30157(int& val) {
    const int x = 30157;
    std::cout << 30157;
    for (int i = 0; i < 30157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30158(int& val) {
    func_in_30158(val);
    const int x = 30158;
    std::cout << 30158;
    for (int i = 0; i < 30158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30158(int& val) {
    const int x = 30158;
    std::cout << 30158;
    for (int i = 0; i < 30158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30159(int& val) {
    func_in_30159(val);
    const int x = 30159;
    std::cout << 30159;
    for (int i = 0; i < 30159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30159(int& val) {
    const int x = 30159;
    std::cout << 30159;
    for (int i = 0; i < 30159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30160(int& val) {
    func_in_30160(val);
    const int x = 30160;
    std::cout << 30160;
    for (int i = 0; i < 30160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30160(int& val) {
    const int x = 30160;
    std::cout << 30160;
    for (int i = 0; i < 30160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30161(int& val) {
    func_in_30161(val);
    const int x = 30161;
    std::cout << 30161;
    for (int i = 0; i < 30161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30161(int& val) {
    const int x = 30161;
    std::cout << 30161;
    for (int i = 0; i < 30161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30162(int& val) {
    func_in_30162(val);
    const int x = 30162;
    std::cout << 30162;
    for (int i = 0; i < 30162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30162(int& val) {
    const int x = 30162;
    std::cout << 30162;
    for (int i = 0; i < 30162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30163(int& val) {
    func_in_30163(val);
    const int x = 30163;
    std::cout << 30163;
    for (int i = 0; i < 30163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30163(int& val) {
    const int x = 30163;
    std::cout << 30163;
    for (int i = 0; i < 30163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30164(int& val) {
    func_in_30164(val);
    const int x = 30164;
    std::cout << 30164;
    for (int i = 0; i < 30164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30164(int& val) {
    const int x = 30164;
    std::cout << 30164;
    for (int i = 0; i < 30164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30165(int& val) {
    func_in_30165(val);
    const int x = 30165;
    std::cout << 30165;
    for (int i = 0; i < 30165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30165(int& val) {
    const int x = 30165;
    std::cout << 30165;
    for (int i = 0; i < 30165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30166(int& val) {
    func_in_30166(val);
    const int x = 30166;
    std::cout << 30166;
    for (int i = 0; i < 30166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30166(int& val) {
    const int x = 30166;
    std::cout << 30166;
    for (int i = 0; i < 30166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30167(int& val) {
    func_in_30167(val);
    const int x = 30167;
    std::cout << 30167;
    for (int i = 0; i < 30167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30167(int& val) {
    const int x = 30167;
    std::cout << 30167;
    for (int i = 0; i < 30167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30168(int& val) {
    func_in_30168(val);
    const int x = 30168;
    std::cout << 30168;
    for (int i = 0; i < 30168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30168(int& val) {
    const int x = 30168;
    std::cout << 30168;
    for (int i = 0; i < 30168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30169(int& val) {
    func_in_30169(val);
    const int x = 30169;
    std::cout << 30169;
    for (int i = 0; i < 30169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30169(int& val) {
    const int x = 30169;
    std::cout << 30169;
    for (int i = 0; i < 30169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30170(int& val) {
    func_in_30170(val);
    const int x = 30170;
    std::cout << 30170;
    for (int i = 0; i < 30170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30170(int& val) {
    const int x = 30170;
    std::cout << 30170;
    for (int i = 0; i < 30170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30171(int& val) {
    func_in_30171(val);
    const int x = 30171;
    std::cout << 30171;
    for (int i = 0; i < 30171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30171(int& val) {
    const int x = 30171;
    std::cout << 30171;
    for (int i = 0; i < 30171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30172(int& val) {
    func_in_30172(val);
    const int x = 30172;
    std::cout << 30172;
    for (int i = 0; i < 30172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30172(int& val) {
    const int x = 30172;
    std::cout << 30172;
    for (int i = 0; i < 30172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30173(int& val) {
    func_in_30173(val);
    const int x = 30173;
    std::cout << 30173;
    for (int i = 0; i < 30173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30173(int& val) {
    const int x = 30173;
    std::cout << 30173;
    for (int i = 0; i < 30173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30174(int& val) {
    func_in_30174(val);
    const int x = 30174;
    std::cout << 30174;
    for (int i = 0; i < 30174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30174(int& val) {
    const int x = 30174;
    std::cout << 30174;
    for (int i = 0; i < 30174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30175(int& val) {
    func_in_30175(val);
    const int x = 30175;
    std::cout << 30175;
    for (int i = 0; i < 30175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30175(int& val) {
    const int x = 30175;
    std::cout << 30175;
    for (int i = 0; i < 30175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30176(int& val) {
    func_in_30176(val);
    const int x = 30176;
    std::cout << 30176;
    for (int i = 0; i < 30176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30176(int& val) {
    const int x = 30176;
    std::cout << 30176;
    for (int i = 0; i < 30176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30177(int& val) {
    func_in_30177(val);
    const int x = 30177;
    std::cout << 30177;
    for (int i = 0; i < 30177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30177(int& val) {
    const int x = 30177;
    std::cout << 30177;
    for (int i = 0; i < 30177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30178(int& val) {
    func_in_30178(val);
    const int x = 30178;
    std::cout << 30178;
    for (int i = 0; i < 30178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30178(int& val) {
    const int x = 30178;
    std::cout << 30178;
    for (int i = 0; i < 30178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30179(int& val) {
    func_in_30179(val);
    const int x = 30179;
    std::cout << 30179;
    for (int i = 0; i < 30179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30179(int& val) {
    const int x = 30179;
    std::cout << 30179;
    for (int i = 0; i < 30179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30180(int& val) {
    func_in_30180(val);
    const int x = 30180;
    std::cout << 30180;
    for (int i = 0; i < 30180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30180(int& val) {
    const int x = 30180;
    std::cout << 30180;
    for (int i = 0; i < 30180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30181(int& val) {
    func_in_30181(val);
    const int x = 30181;
    std::cout << 30181;
    for (int i = 0; i < 30181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30181(int& val) {
    const int x = 30181;
    std::cout << 30181;
    for (int i = 0; i < 30181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30182(int& val) {
    func_in_30182(val);
    const int x = 30182;
    std::cout << 30182;
    for (int i = 0; i < 30182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30182(int& val) {
    const int x = 30182;
    std::cout << 30182;
    for (int i = 0; i < 30182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30183(int& val) {
    func_in_30183(val);
    const int x = 30183;
    std::cout << 30183;
    for (int i = 0; i < 30183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30183(int& val) {
    const int x = 30183;
    std::cout << 30183;
    for (int i = 0; i < 30183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30184(int& val) {
    func_in_30184(val);
    const int x = 30184;
    std::cout << 30184;
    for (int i = 0; i < 30184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30184(int& val) {
    const int x = 30184;
    std::cout << 30184;
    for (int i = 0; i < 30184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30185(int& val) {
    func_in_30185(val);
    const int x = 30185;
    std::cout << 30185;
    for (int i = 0; i < 30185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30185(int& val) {
    const int x = 30185;
    std::cout << 30185;
    for (int i = 0; i < 30185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30186(int& val) {
    func_in_30186(val);
    const int x = 30186;
    std::cout << 30186;
    for (int i = 0; i < 30186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30186(int& val) {
    const int x = 30186;
    std::cout << 30186;
    for (int i = 0; i < 30186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30187(int& val) {
    func_in_30187(val);
    const int x = 30187;
    std::cout << 30187;
    for (int i = 0; i < 30187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30187(int& val) {
    const int x = 30187;
    std::cout << 30187;
    for (int i = 0; i < 30187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30188(int& val) {
    func_in_30188(val);
    const int x = 30188;
    std::cout << 30188;
    for (int i = 0; i < 30188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30188(int& val) {
    const int x = 30188;
    std::cout << 30188;
    for (int i = 0; i < 30188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30189(int& val) {
    func_in_30189(val);
    const int x = 30189;
    std::cout << 30189;
    for (int i = 0; i < 30189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30189(int& val) {
    const int x = 30189;
    std::cout << 30189;
    for (int i = 0; i < 30189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30190(int& val) {
    func_in_30190(val);
    const int x = 30190;
    std::cout << 30190;
    for (int i = 0; i < 30190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30190(int& val) {
    const int x = 30190;
    std::cout << 30190;
    for (int i = 0; i < 30190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30191(int& val) {
    func_in_30191(val);
    const int x = 30191;
    std::cout << 30191;
    for (int i = 0; i < 30191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30191(int& val) {
    const int x = 30191;
    std::cout << 30191;
    for (int i = 0; i < 30191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30192(int& val) {
    func_in_30192(val);
    const int x = 30192;
    std::cout << 30192;
    for (int i = 0; i < 30192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30192(int& val) {
    const int x = 30192;
    std::cout << 30192;
    for (int i = 0; i < 30192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30193(int& val) {
    func_in_30193(val);
    const int x = 30193;
    std::cout << 30193;
    for (int i = 0; i < 30193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30193(int& val) {
    const int x = 30193;
    std::cout << 30193;
    for (int i = 0; i < 30193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30194(int& val) {
    func_in_30194(val);
    const int x = 30194;
    std::cout << 30194;
    for (int i = 0; i < 30194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30194(int& val) {
    const int x = 30194;
    std::cout << 30194;
    for (int i = 0; i < 30194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30195(int& val) {
    func_in_30195(val);
    const int x = 30195;
    std::cout << 30195;
    for (int i = 0; i < 30195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30195(int& val) {
    const int x = 30195;
    std::cout << 30195;
    for (int i = 0; i < 30195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30196(int& val) {
    func_in_30196(val);
    const int x = 30196;
    std::cout << 30196;
    for (int i = 0; i < 30196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30196(int& val) {
    const int x = 30196;
    std::cout << 30196;
    for (int i = 0; i < 30196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30197(int& val) {
    func_in_30197(val);
    const int x = 30197;
    std::cout << 30197;
    for (int i = 0; i < 30197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30197(int& val) {
    const int x = 30197;
    std::cout << 30197;
    for (int i = 0; i < 30197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30198(int& val) {
    func_in_30198(val);
    const int x = 30198;
    std::cout << 30198;
    for (int i = 0; i < 30198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30198(int& val) {
    const int x = 30198;
    std::cout << 30198;
    for (int i = 0; i < 30198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30199(int& val) {
    func_in_30199(val);
    const int x = 30199;
    std::cout << 30199;
    for (int i = 0; i < 30199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30199(int& val) {
    const int x = 30199;
    std::cout << 30199;
    for (int i = 0; i < 30199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30200(int& val) {
    func_in_30200(val);
    const int x = 30200;
    std::cout << 30200;
    for (int i = 0; i < 30200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30200(int& val) {
    const int x = 30200;
    std::cout << 30200;
    for (int i = 0; i < 30200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30201(int& val) {
    func_in_30201(val);
    const int x = 30201;
    std::cout << 30201;
    for (int i = 0; i < 30201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30201(int& val) {
    const int x = 30201;
    std::cout << 30201;
    for (int i = 0; i < 30201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30202(int& val) {
    func_in_30202(val);
    const int x = 30202;
    std::cout << 30202;
    for (int i = 0; i < 30202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30202(int& val) {
    const int x = 30202;
    std::cout << 30202;
    for (int i = 0; i < 30202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30203(int& val) {
    func_in_30203(val);
    const int x = 30203;
    std::cout << 30203;
    for (int i = 0; i < 30203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30203(int& val) {
    const int x = 30203;
    std::cout << 30203;
    for (int i = 0; i < 30203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30204(int& val) {
    func_in_30204(val);
    const int x = 30204;
    std::cout << 30204;
    for (int i = 0; i < 30204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30204(int& val) {
    const int x = 30204;
    std::cout << 30204;
    for (int i = 0; i < 30204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30205(int& val) {
    func_in_30205(val);
    const int x = 30205;
    std::cout << 30205;
    for (int i = 0; i < 30205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30205(int& val) {
    const int x = 30205;
    std::cout << 30205;
    for (int i = 0; i < 30205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30206(int& val) {
    func_in_30206(val);
    const int x = 30206;
    std::cout << 30206;
    for (int i = 0; i < 30206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30206(int& val) {
    const int x = 30206;
    std::cout << 30206;
    for (int i = 0; i < 30206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30207(int& val) {
    func_in_30207(val);
    const int x = 30207;
    std::cout << 30207;
    for (int i = 0; i < 30207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30207(int& val) {
    const int x = 30207;
    std::cout << 30207;
    for (int i = 0; i < 30207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30208(int& val) {
    func_in_30208(val);
    const int x = 30208;
    std::cout << 30208;
    for (int i = 0; i < 30208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30208(int& val) {
    const int x = 30208;
    std::cout << 30208;
    for (int i = 0; i < 30208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30209(int& val) {
    func_in_30209(val);
    const int x = 30209;
    std::cout << 30209;
    for (int i = 0; i < 30209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30209(int& val) {
    const int x = 30209;
    std::cout << 30209;
    for (int i = 0; i < 30209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30210(int& val) {
    func_in_30210(val);
    const int x = 30210;
    std::cout << 30210;
    for (int i = 0; i < 30210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30210(int& val) {
    const int x = 30210;
    std::cout << 30210;
    for (int i = 0; i < 30210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30211(int& val) {
    func_in_30211(val);
    const int x = 30211;
    std::cout << 30211;
    for (int i = 0; i < 30211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30211(int& val) {
    const int x = 30211;
    std::cout << 30211;
    for (int i = 0; i < 30211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30212(int& val) {
    func_in_30212(val);
    const int x = 30212;
    std::cout << 30212;
    for (int i = 0; i < 30212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30212(int& val) {
    const int x = 30212;
    std::cout << 30212;
    for (int i = 0; i < 30212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30213(int& val) {
    func_in_30213(val);
    const int x = 30213;
    std::cout << 30213;
    for (int i = 0; i < 30213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30213(int& val) {
    const int x = 30213;
    std::cout << 30213;
    for (int i = 0; i < 30213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30214(int& val) {
    func_in_30214(val);
    const int x = 30214;
    std::cout << 30214;
    for (int i = 0; i < 30214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30214(int& val) {
    const int x = 30214;
    std::cout << 30214;
    for (int i = 0; i < 30214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30215(int& val) {
    func_in_30215(val);
    const int x = 30215;
    std::cout << 30215;
    for (int i = 0; i < 30215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30215(int& val) {
    const int x = 30215;
    std::cout << 30215;
    for (int i = 0; i < 30215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30216(int& val) {
    func_in_30216(val);
    const int x = 30216;
    std::cout << 30216;
    for (int i = 0; i < 30216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30216(int& val) {
    const int x = 30216;
    std::cout << 30216;
    for (int i = 0; i < 30216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30217(int& val) {
    func_in_30217(val);
    const int x = 30217;
    std::cout << 30217;
    for (int i = 0; i < 30217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30217(int& val) {
    const int x = 30217;
    std::cout << 30217;
    for (int i = 0; i < 30217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30218(int& val) {
    func_in_30218(val);
    const int x = 30218;
    std::cout << 30218;
    for (int i = 0; i < 30218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30218(int& val) {
    const int x = 30218;
    std::cout << 30218;
    for (int i = 0; i < 30218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30219(int& val) {
    func_in_30219(val);
    const int x = 30219;
    std::cout << 30219;
    for (int i = 0; i < 30219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30219(int& val) {
    const int x = 30219;
    std::cout << 30219;
    for (int i = 0; i < 30219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30220(int& val) {
    func_in_30220(val);
    const int x = 30220;
    std::cout << 30220;
    for (int i = 0; i < 30220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30220(int& val) {
    const int x = 30220;
    std::cout << 30220;
    for (int i = 0; i < 30220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30221(int& val) {
    func_in_30221(val);
    const int x = 30221;
    std::cout << 30221;
    for (int i = 0; i < 30221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30221(int& val) {
    const int x = 30221;
    std::cout << 30221;
    for (int i = 0; i < 30221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30222(int& val) {
    func_in_30222(val);
    const int x = 30222;
    std::cout << 30222;
    for (int i = 0; i < 30222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30222(int& val) {
    const int x = 30222;
    std::cout << 30222;
    for (int i = 0; i < 30222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30223(int& val) {
    func_in_30223(val);
    const int x = 30223;
    std::cout << 30223;
    for (int i = 0; i < 30223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30223(int& val) {
    const int x = 30223;
    std::cout << 30223;
    for (int i = 0; i < 30223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30224(int& val) {
    func_in_30224(val);
    const int x = 30224;
    std::cout << 30224;
    for (int i = 0; i < 30224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30224(int& val) {
    const int x = 30224;
    std::cout << 30224;
    for (int i = 0; i < 30224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30225(int& val) {
    func_in_30225(val);
    const int x = 30225;
    std::cout << 30225;
    for (int i = 0; i < 30225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30225(int& val) {
    const int x = 30225;
    std::cout << 30225;
    for (int i = 0; i < 30225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30226(int& val) {
    func_in_30226(val);
    const int x = 30226;
    std::cout << 30226;
    for (int i = 0; i < 30226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30226(int& val) {
    const int x = 30226;
    std::cout << 30226;
    for (int i = 0; i < 30226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30227(int& val) {
    func_in_30227(val);
    const int x = 30227;
    std::cout << 30227;
    for (int i = 0; i < 30227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30227(int& val) {
    const int x = 30227;
    std::cout << 30227;
    for (int i = 0; i < 30227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30228(int& val) {
    func_in_30228(val);
    const int x = 30228;
    std::cout << 30228;
    for (int i = 0; i < 30228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30228(int& val) {
    const int x = 30228;
    std::cout << 30228;
    for (int i = 0; i < 30228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30229(int& val) {
    func_in_30229(val);
    const int x = 30229;
    std::cout << 30229;
    for (int i = 0; i < 30229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30229(int& val) {
    const int x = 30229;
    std::cout << 30229;
    for (int i = 0; i < 30229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30230(int& val) {
    func_in_30230(val);
    const int x = 30230;
    std::cout << 30230;
    for (int i = 0; i < 30230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30230(int& val) {
    const int x = 30230;
    std::cout << 30230;
    for (int i = 0; i < 30230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30231(int& val) {
    func_in_30231(val);
    const int x = 30231;
    std::cout << 30231;
    for (int i = 0; i < 30231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30231(int& val) {
    const int x = 30231;
    std::cout << 30231;
    for (int i = 0; i < 30231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30232(int& val) {
    func_in_30232(val);
    const int x = 30232;
    std::cout << 30232;
    for (int i = 0; i < 30232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30232(int& val) {
    const int x = 30232;
    std::cout << 30232;
    for (int i = 0; i < 30232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30233(int& val) {
    func_in_30233(val);
    const int x = 30233;
    std::cout << 30233;
    for (int i = 0; i < 30233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30233(int& val) {
    const int x = 30233;
    std::cout << 30233;
    for (int i = 0; i < 30233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30234(int& val) {
    func_in_30234(val);
    const int x = 30234;
    std::cout << 30234;
    for (int i = 0; i < 30234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30234(int& val) {
    const int x = 30234;
    std::cout << 30234;
    for (int i = 0; i < 30234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30235(int& val) {
    func_in_30235(val);
    const int x = 30235;
    std::cout << 30235;
    for (int i = 0; i < 30235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30235(int& val) {
    const int x = 30235;
    std::cout << 30235;
    for (int i = 0; i < 30235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30236(int& val) {
    func_in_30236(val);
    const int x = 30236;
    std::cout << 30236;
    for (int i = 0; i < 30236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30236(int& val) {
    const int x = 30236;
    std::cout << 30236;
    for (int i = 0; i < 30236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30237(int& val) {
    func_in_30237(val);
    const int x = 30237;
    std::cout << 30237;
    for (int i = 0; i < 30237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30237(int& val) {
    const int x = 30237;
    std::cout << 30237;
    for (int i = 0; i < 30237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30238(int& val) {
    func_in_30238(val);
    const int x = 30238;
    std::cout << 30238;
    for (int i = 0; i < 30238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30238(int& val) {
    const int x = 30238;
    std::cout << 30238;
    for (int i = 0; i < 30238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30239(int& val) {
    func_in_30239(val);
    const int x = 30239;
    std::cout << 30239;
    for (int i = 0; i < 30239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30239(int& val) {
    const int x = 30239;
    std::cout << 30239;
    for (int i = 0; i < 30239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30240(int& val) {
    func_in_30240(val);
    const int x = 30240;
    std::cout << 30240;
    for (int i = 0; i < 30240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30240(int& val) {
    const int x = 30240;
    std::cout << 30240;
    for (int i = 0; i < 30240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30241(int& val) {
    func_in_30241(val);
    const int x = 30241;
    std::cout << 30241;
    for (int i = 0; i < 30241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30241(int& val) {
    const int x = 30241;
    std::cout << 30241;
    for (int i = 0; i < 30241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30242(int& val) {
    func_in_30242(val);
    const int x = 30242;
    std::cout << 30242;
    for (int i = 0; i < 30242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30242(int& val) {
    const int x = 30242;
    std::cout << 30242;
    for (int i = 0; i < 30242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30243(int& val) {
    func_in_30243(val);
    const int x = 30243;
    std::cout << 30243;
    for (int i = 0; i < 30243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30243(int& val) {
    const int x = 30243;
    std::cout << 30243;
    for (int i = 0; i < 30243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30244(int& val) {
    func_in_30244(val);
    const int x = 30244;
    std::cout << 30244;
    for (int i = 0; i < 30244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30244(int& val) {
    const int x = 30244;
    std::cout << 30244;
    for (int i = 0; i < 30244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30245(int& val) {
    func_in_30245(val);
    const int x = 30245;
    std::cout << 30245;
    for (int i = 0; i < 30245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30245(int& val) {
    const int x = 30245;
    std::cout << 30245;
    for (int i = 0; i < 30245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30246(int& val) {
    func_in_30246(val);
    const int x = 30246;
    std::cout << 30246;
    for (int i = 0; i < 30246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30246(int& val) {
    const int x = 30246;
    std::cout << 30246;
    for (int i = 0; i < 30246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30247(int& val) {
    func_in_30247(val);
    const int x = 30247;
    std::cout << 30247;
    for (int i = 0; i < 30247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30247(int& val) {
    const int x = 30247;
    std::cout << 30247;
    for (int i = 0; i < 30247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30248(int& val) {
    func_in_30248(val);
    const int x = 30248;
    std::cout << 30248;
    for (int i = 0; i < 30248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30248(int& val) {
    const int x = 30248;
    std::cout << 30248;
    for (int i = 0; i < 30248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30249(int& val) {
    func_in_30249(val);
    const int x = 30249;
    std::cout << 30249;
    for (int i = 0; i < 30249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30249(int& val) {
    const int x = 30249;
    std::cout << 30249;
    for (int i = 0; i < 30249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30250(int& val) {
    func_in_30250(val);
    const int x = 30250;
    std::cout << 30250;
    for (int i = 0; i < 30250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30250(int& val) {
    const int x = 30250;
    std::cout << 30250;
    for (int i = 0; i < 30250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30251(int& val) {
    func_in_30251(val);
    const int x = 30251;
    std::cout << 30251;
    for (int i = 0; i < 30251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30251(int& val) {
    const int x = 30251;
    std::cout << 30251;
    for (int i = 0; i < 30251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30252(int& val) {
    func_in_30252(val);
    const int x = 30252;
    std::cout << 30252;
    for (int i = 0; i < 30252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30252(int& val) {
    const int x = 30252;
    std::cout << 30252;
    for (int i = 0; i < 30252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30253(int& val) {
    func_in_30253(val);
    const int x = 30253;
    std::cout << 30253;
    for (int i = 0; i < 30253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30253(int& val) {
    const int x = 30253;
    std::cout << 30253;
    for (int i = 0; i < 30253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30254(int& val) {
    func_in_30254(val);
    const int x = 30254;
    std::cout << 30254;
    for (int i = 0; i < 30254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30254(int& val) {
    const int x = 30254;
    std::cout << 30254;
    for (int i = 0; i < 30254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30255(int& val) {
    func_in_30255(val);
    const int x = 30255;
    std::cout << 30255;
    for (int i = 0; i < 30255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30255(int& val) {
    const int x = 30255;
    std::cout << 30255;
    for (int i = 0; i < 30255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30256(int& val) {
    func_in_30256(val);
    const int x = 30256;
    std::cout << 30256;
    for (int i = 0; i < 30256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30256(int& val) {
    const int x = 30256;
    std::cout << 30256;
    for (int i = 0; i < 30256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30257(int& val) {
    func_in_30257(val);
    const int x = 30257;
    std::cout << 30257;
    for (int i = 0; i < 30257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30257(int& val) {
    const int x = 30257;
    std::cout << 30257;
    for (int i = 0; i < 30257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30258(int& val) {
    func_in_30258(val);
    const int x = 30258;
    std::cout << 30258;
    for (int i = 0; i < 30258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30258(int& val) {
    const int x = 30258;
    std::cout << 30258;
    for (int i = 0; i < 30258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30259(int& val) {
    func_in_30259(val);
    const int x = 30259;
    std::cout << 30259;
    for (int i = 0; i < 30259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30259(int& val) {
    const int x = 30259;
    std::cout << 30259;
    for (int i = 0; i < 30259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30260(int& val) {
    func_in_30260(val);
    const int x = 30260;
    std::cout << 30260;
    for (int i = 0; i < 30260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30260(int& val) {
    const int x = 30260;
    std::cout << 30260;
    for (int i = 0; i < 30260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30261(int& val) {
    func_in_30261(val);
    const int x = 30261;
    std::cout << 30261;
    for (int i = 0; i < 30261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30261(int& val) {
    const int x = 30261;
    std::cout << 30261;
    for (int i = 0; i < 30261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30262(int& val) {
    func_in_30262(val);
    const int x = 30262;
    std::cout << 30262;
    for (int i = 0; i < 30262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30262(int& val) {
    const int x = 30262;
    std::cout << 30262;
    for (int i = 0; i < 30262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30263(int& val) {
    func_in_30263(val);
    const int x = 30263;
    std::cout << 30263;
    for (int i = 0; i < 30263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30263(int& val) {
    const int x = 30263;
    std::cout << 30263;
    for (int i = 0; i < 30263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30264(int& val) {
    func_in_30264(val);
    const int x = 30264;
    std::cout << 30264;
    for (int i = 0; i < 30264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30264(int& val) {
    const int x = 30264;
    std::cout << 30264;
    for (int i = 0; i < 30264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30265(int& val) {
    func_in_30265(val);
    const int x = 30265;
    std::cout << 30265;
    for (int i = 0; i < 30265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30265(int& val) {
    const int x = 30265;
    std::cout << 30265;
    for (int i = 0; i < 30265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30266(int& val) {
    func_in_30266(val);
    const int x = 30266;
    std::cout << 30266;
    for (int i = 0; i < 30266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30266(int& val) {
    const int x = 30266;
    std::cout << 30266;
    for (int i = 0; i < 30266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30267(int& val) {
    func_in_30267(val);
    const int x = 30267;
    std::cout << 30267;
    for (int i = 0; i < 30267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30267(int& val) {
    const int x = 30267;
    std::cout << 30267;
    for (int i = 0; i < 30267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30268(int& val) {
    func_in_30268(val);
    const int x = 30268;
    std::cout << 30268;
    for (int i = 0; i < 30268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30268(int& val) {
    const int x = 30268;
    std::cout << 30268;
    for (int i = 0; i < 30268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30269(int& val) {
    func_in_30269(val);
    const int x = 30269;
    std::cout << 30269;
    for (int i = 0; i < 30269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30269(int& val) {
    const int x = 30269;
    std::cout << 30269;
    for (int i = 0; i < 30269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30270(int& val) {
    func_in_30270(val);
    const int x = 30270;
    std::cout << 30270;
    for (int i = 0; i < 30270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30270(int& val) {
    const int x = 30270;
    std::cout << 30270;
    for (int i = 0; i < 30270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30271(int& val) {
    func_in_30271(val);
    const int x = 30271;
    std::cout << 30271;
    for (int i = 0; i < 30271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30271(int& val) {
    const int x = 30271;
    std::cout << 30271;
    for (int i = 0; i < 30271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30272(int& val) {
    func_in_30272(val);
    const int x = 30272;
    std::cout << 30272;
    for (int i = 0; i < 30272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30272(int& val) {
    const int x = 30272;
    std::cout << 30272;
    for (int i = 0; i < 30272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30273(int& val) {
    func_in_30273(val);
    const int x = 30273;
    std::cout << 30273;
    for (int i = 0; i < 30273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30273(int& val) {
    const int x = 30273;
    std::cout << 30273;
    for (int i = 0; i < 30273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30274(int& val) {
    func_in_30274(val);
    const int x = 30274;
    std::cout << 30274;
    for (int i = 0; i < 30274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30274(int& val) {
    const int x = 30274;
    std::cout << 30274;
    for (int i = 0; i < 30274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30275(int& val) {
    func_in_30275(val);
    const int x = 30275;
    std::cout << 30275;
    for (int i = 0; i < 30275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30275(int& val) {
    const int x = 30275;
    std::cout << 30275;
    for (int i = 0; i < 30275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30276(int& val) {
    func_in_30276(val);
    const int x = 30276;
    std::cout << 30276;
    for (int i = 0; i < 30276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30276(int& val) {
    const int x = 30276;
    std::cout << 30276;
    for (int i = 0; i < 30276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30277(int& val) {
    func_in_30277(val);
    const int x = 30277;
    std::cout << 30277;
    for (int i = 0; i < 30277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30277(int& val) {
    const int x = 30277;
    std::cout << 30277;
    for (int i = 0; i < 30277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30278(int& val) {
    func_in_30278(val);
    const int x = 30278;
    std::cout << 30278;
    for (int i = 0; i < 30278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30278(int& val) {
    const int x = 30278;
    std::cout << 30278;
    for (int i = 0; i < 30278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30279(int& val) {
    func_in_30279(val);
    const int x = 30279;
    std::cout << 30279;
    for (int i = 0; i < 30279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30279(int& val) {
    const int x = 30279;
    std::cout << 30279;
    for (int i = 0; i < 30279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30280(int& val) {
    func_in_30280(val);
    const int x = 30280;
    std::cout << 30280;
    for (int i = 0; i < 30280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30280(int& val) {
    const int x = 30280;
    std::cout << 30280;
    for (int i = 0; i < 30280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30281(int& val) {
    func_in_30281(val);
    const int x = 30281;
    std::cout << 30281;
    for (int i = 0; i < 30281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30281(int& val) {
    const int x = 30281;
    std::cout << 30281;
    for (int i = 0; i < 30281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30282(int& val) {
    func_in_30282(val);
    const int x = 30282;
    std::cout << 30282;
    for (int i = 0; i < 30282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30282(int& val) {
    const int x = 30282;
    std::cout << 30282;
    for (int i = 0; i < 30282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30283(int& val) {
    func_in_30283(val);
    const int x = 30283;
    std::cout << 30283;
    for (int i = 0; i < 30283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30283(int& val) {
    const int x = 30283;
    std::cout << 30283;
    for (int i = 0; i < 30283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30284(int& val) {
    func_in_30284(val);
    const int x = 30284;
    std::cout << 30284;
    for (int i = 0; i < 30284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30284(int& val) {
    const int x = 30284;
    std::cout << 30284;
    for (int i = 0; i < 30284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30285(int& val) {
    func_in_30285(val);
    const int x = 30285;
    std::cout << 30285;
    for (int i = 0; i < 30285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30285(int& val) {
    const int x = 30285;
    std::cout << 30285;
    for (int i = 0; i < 30285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30286(int& val) {
    func_in_30286(val);
    const int x = 30286;
    std::cout << 30286;
    for (int i = 0; i < 30286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30286(int& val) {
    const int x = 30286;
    std::cout << 30286;
    for (int i = 0; i < 30286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30287(int& val) {
    func_in_30287(val);
    const int x = 30287;
    std::cout << 30287;
    for (int i = 0; i < 30287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30287(int& val) {
    const int x = 30287;
    std::cout << 30287;
    for (int i = 0; i < 30287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30288(int& val) {
    func_in_30288(val);
    const int x = 30288;
    std::cout << 30288;
    for (int i = 0; i < 30288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30288(int& val) {
    const int x = 30288;
    std::cout << 30288;
    for (int i = 0; i < 30288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30289(int& val) {
    func_in_30289(val);
    const int x = 30289;
    std::cout << 30289;
    for (int i = 0; i < 30289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30289(int& val) {
    const int x = 30289;
    std::cout << 30289;
    for (int i = 0; i < 30289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30290(int& val) {
    func_in_30290(val);
    const int x = 30290;
    std::cout << 30290;
    for (int i = 0; i < 30290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30290(int& val) {
    const int x = 30290;
    std::cout << 30290;
    for (int i = 0; i < 30290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30291(int& val) {
    func_in_30291(val);
    const int x = 30291;
    std::cout << 30291;
    for (int i = 0; i < 30291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30291(int& val) {
    const int x = 30291;
    std::cout << 30291;
    for (int i = 0; i < 30291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30292(int& val) {
    func_in_30292(val);
    const int x = 30292;
    std::cout << 30292;
    for (int i = 0; i < 30292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30292(int& val) {
    const int x = 30292;
    std::cout << 30292;
    for (int i = 0; i < 30292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30293(int& val) {
    func_in_30293(val);
    const int x = 30293;
    std::cout << 30293;
    for (int i = 0; i < 30293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30293(int& val) {
    const int x = 30293;
    std::cout << 30293;
    for (int i = 0; i < 30293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30294(int& val) {
    func_in_30294(val);
    const int x = 30294;
    std::cout << 30294;
    for (int i = 0; i < 30294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30294(int& val) {
    const int x = 30294;
    std::cout << 30294;
    for (int i = 0; i < 30294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30295(int& val) {
    func_in_30295(val);
    const int x = 30295;
    std::cout << 30295;
    for (int i = 0; i < 30295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30295(int& val) {
    const int x = 30295;
    std::cout << 30295;
    for (int i = 0; i < 30295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30296(int& val) {
    func_in_30296(val);
    const int x = 30296;
    std::cout << 30296;
    for (int i = 0; i < 30296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30296(int& val) {
    const int x = 30296;
    std::cout << 30296;
    for (int i = 0; i < 30296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30297(int& val) {
    func_in_30297(val);
    const int x = 30297;
    std::cout << 30297;
    for (int i = 0; i < 30297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30297(int& val) {
    const int x = 30297;
    std::cout << 30297;
    for (int i = 0; i < 30297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30298(int& val) {
    func_in_30298(val);
    const int x = 30298;
    std::cout << 30298;
    for (int i = 0; i < 30298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30298(int& val) {
    const int x = 30298;
    std::cout << 30298;
    for (int i = 0; i < 30298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30299(int& val) {
    func_in_30299(val);
    const int x = 30299;
    std::cout << 30299;
    for (int i = 0; i < 30299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30299(int& val) {
    const int x = 30299;
    std::cout << 30299;
    for (int i = 0; i < 30299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30300(int& val) {
    func_in_30300(val);
    const int x = 30300;
    std::cout << 30300;
    for (int i = 0; i < 30300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30300(int& val) {
    const int x = 30300;
    std::cout << 30300;
    for (int i = 0; i < 30300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30301(int& val) {
    func_in_30301(val);
    const int x = 30301;
    std::cout << 30301;
    for (int i = 0; i < 30301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30301(int& val) {
    const int x = 30301;
    std::cout << 30301;
    for (int i = 0; i < 30301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30302(int& val) {
    func_in_30302(val);
    const int x = 30302;
    std::cout << 30302;
    for (int i = 0; i < 30302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30302(int& val) {
    const int x = 30302;
    std::cout << 30302;
    for (int i = 0; i < 30302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30303(int& val) {
    func_in_30303(val);
    const int x = 30303;
    std::cout << 30303;
    for (int i = 0; i < 30303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30303(int& val) {
    const int x = 30303;
    std::cout << 30303;
    for (int i = 0; i < 30303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30304(int& val) {
    func_in_30304(val);
    const int x = 30304;
    std::cout << 30304;
    for (int i = 0; i < 30304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30304(int& val) {
    const int x = 30304;
    std::cout << 30304;
    for (int i = 0; i < 30304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30305(int& val) {
    func_in_30305(val);
    const int x = 30305;
    std::cout << 30305;
    for (int i = 0; i < 30305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30305(int& val) {
    const int x = 30305;
    std::cout << 30305;
    for (int i = 0; i < 30305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30306(int& val) {
    func_in_30306(val);
    const int x = 30306;
    std::cout << 30306;
    for (int i = 0; i < 30306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30306(int& val) {
    const int x = 30306;
    std::cout << 30306;
    for (int i = 0; i < 30306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30307(int& val) {
    func_in_30307(val);
    const int x = 30307;
    std::cout << 30307;
    for (int i = 0; i < 30307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30307(int& val) {
    const int x = 30307;
    std::cout << 30307;
    for (int i = 0; i < 30307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30308(int& val) {
    func_in_30308(val);
    const int x = 30308;
    std::cout << 30308;
    for (int i = 0; i < 30308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30308(int& val) {
    const int x = 30308;
    std::cout << 30308;
    for (int i = 0; i < 30308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30309(int& val) {
    func_in_30309(val);
    const int x = 30309;
    std::cout << 30309;
    for (int i = 0; i < 30309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30309(int& val) {
    const int x = 30309;
    std::cout << 30309;
    for (int i = 0; i < 30309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30310(int& val) {
    func_in_30310(val);
    const int x = 30310;
    std::cout << 30310;
    for (int i = 0; i < 30310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30310(int& val) {
    const int x = 30310;
    std::cout << 30310;
    for (int i = 0; i < 30310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30311(int& val) {
    func_in_30311(val);
    const int x = 30311;
    std::cout << 30311;
    for (int i = 0; i < 30311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30311(int& val) {
    const int x = 30311;
    std::cout << 30311;
    for (int i = 0; i < 30311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30312(int& val) {
    func_in_30312(val);
    const int x = 30312;
    std::cout << 30312;
    for (int i = 0; i < 30312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30312(int& val) {
    const int x = 30312;
    std::cout << 30312;
    for (int i = 0; i < 30312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30313(int& val) {
    func_in_30313(val);
    const int x = 30313;
    std::cout << 30313;
    for (int i = 0; i < 30313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30313(int& val) {
    const int x = 30313;
    std::cout << 30313;
    for (int i = 0; i < 30313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30314(int& val) {
    func_in_30314(val);
    const int x = 30314;
    std::cout << 30314;
    for (int i = 0; i < 30314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30314(int& val) {
    const int x = 30314;
    std::cout << 30314;
    for (int i = 0; i < 30314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30315(int& val) {
    func_in_30315(val);
    const int x = 30315;
    std::cout << 30315;
    for (int i = 0; i < 30315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30315(int& val) {
    const int x = 30315;
    std::cout << 30315;
    for (int i = 0; i < 30315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30316(int& val) {
    func_in_30316(val);
    const int x = 30316;
    std::cout << 30316;
    for (int i = 0; i < 30316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30316(int& val) {
    const int x = 30316;
    std::cout << 30316;
    for (int i = 0; i < 30316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30317(int& val) {
    func_in_30317(val);
    const int x = 30317;
    std::cout << 30317;
    for (int i = 0; i < 30317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30317(int& val) {
    const int x = 30317;
    std::cout << 30317;
    for (int i = 0; i < 30317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30318(int& val) {
    func_in_30318(val);
    const int x = 30318;
    std::cout << 30318;
    for (int i = 0; i < 30318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30318(int& val) {
    const int x = 30318;
    std::cout << 30318;
    for (int i = 0; i < 30318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30319(int& val) {
    func_in_30319(val);
    const int x = 30319;
    std::cout << 30319;
    for (int i = 0; i < 30319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30319(int& val) {
    const int x = 30319;
    std::cout << 30319;
    for (int i = 0; i < 30319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30320(int& val) {
    func_in_30320(val);
    const int x = 30320;
    std::cout << 30320;
    for (int i = 0; i < 30320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30320(int& val) {
    const int x = 30320;
    std::cout << 30320;
    for (int i = 0; i < 30320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30321(int& val) {
    func_in_30321(val);
    const int x = 30321;
    std::cout << 30321;
    for (int i = 0; i < 30321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30321(int& val) {
    const int x = 30321;
    std::cout << 30321;
    for (int i = 0; i < 30321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30322(int& val) {
    func_in_30322(val);
    const int x = 30322;
    std::cout << 30322;
    for (int i = 0; i < 30322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30322(int& val) {
    const int x = 30322;
    std::cout << 30322;
    for (int i = 0; i < 30322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30323(int& val) {
    func_in_30323(val);
    const int x = 30323;
    std::cout << 30323;
    for (int i = 0; i < 30323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30323(int& val) {
    const int x = 30323;
    std::cout << 30323;
    for (int i = 0; i < 30323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30324(int& val) {
    func_in_30324(val);
    const int x = 30324;
    std::cout << 30324;
    for (int i = 0; i < 30324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30324(int& val) {
    const int x = 30324;
    std::cout << 30324;
    for (int i = 0; i < 30324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30325(int& val) {
    func_in_30325(val);
    const int x = 30325;
    std::cout << 30325;
    for (int i = 0; i < 30325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30325(int& val) {
    const int x = 30325;
    std::cout << 30325;
    for (int i = 0; i < 30325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30326(int& val) {
    func_in_30326(val);
    const int x = 30326;
    std::cout << 30326;
    for (int i = 0; i < 30326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30326(int& val) {
    const int x = 30326;
    std::cout << 30326;
    for (int i = 0; i < 30326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30327(int& val) {
    func_in_30327(val);
    const int x = 30327;
    std::cout << 30327;
    for (int i = 0; i < 30327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30327(int& val) {
    const int x = 30327;
    std::cout << 30327;
    for (int i = 0; i < 30327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30328(int& val) {
    func_in_30328(val);
    const int x = 30328;
    std::cout << 30328;
    for (int i = 0; i < 30328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30328(int& val) {
    const int x = 30328;
    std::cout << 30328;
    for (int i = 0; i < 30328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30329(int& val) {
    func_in_30329(val);
    const int x = 30329;
    std::cout << 30329;
    for (int i = 0; i < 30329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30329(int& val) {
    const int x = 30329;
    std::cout << 30329;
    for (int i = 0; i < 30329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30330(int& val) {
    func_in_30330(val);
    const int x = 30330;
    std::cout << 30330;
    for (int i = 0; i < 30330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30330(int& val) {
    const int x = 30330;
    std::cout << 30330;
    for (int i = 0; i < 30330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30331(int& val) {
    func_in_30331(val);
    const int x = 30331;
    std::cout << 30331;
    for (int i = 0; i < 30331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30331(int& val) {
    const int x = 30331;
    std::cout << 30331;
    for (int i = 0; i < 30331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30332(int& val) {
    func_in_30332(val);
    const int x = 30332;
    std::cout << 30332;
    for (int i = 0; i < 30332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30332(int& val) {
    const int x = 30332;
    std::cout << 30332;
    for (int i = 0; i < 30332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30333(int& val) {
    func_in_30333(val);
    const int x = 30333;
    std::cout << 30333;
    for (int i = 0; i < 30333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30333(int& val) {
    const int x = 30333;
    std::cout << 30333;
    for (int i = 0; i < 30333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30334(int& val) {
    func_in_30334(val);
    const int x = 30334;
    std::cout << 30334;
    for (int i = 0; i < 30334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30334(int& val) {
    const int x = 30334;
    std::cout << 30334;
    for (int i = 0; i < 30334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30335(int& val) {
    func_in_30335(val);
    const int x = 30335;
    std::cout << 30335;
    for (int i = 0; i < 30335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30335(int& val) {
    const int x = 30335;
    std::cout << 30335;
    for (int i = 0; i < 30335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30336(int& val) {
    func_in_30336(val);
    const int x = 30336;
    std::cout << 30336;
    for (int i = 0; i < 30336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30336(int& val) {
    const int x = 30336;
    std::cout << 30336;
    for (int i = 0; i < 30336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30337(int& val) {
    func_in_30337(val);
    const int x = 30337;
    std::cout << 30337;
    for (int i = 0; i < 30337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30337(int& val) {
    const int x = 30337;
    std::cout << 30337;
    for (int i = 0; i < 30337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30338(int& val) {
    func_in_30338(val);
    const int x = 30338;
    std::cout << 30338;
    for (int i = 0; i < 30338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30338(int& val) {
    const int x = 30338;
    std::cout << 30338;
    for (int i = 0; i < 30338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30339(int& val) {
    func_in_30339(val);
    const int x = 30339;
    std::cout << 30339;
    for (int i = 0; i < 30339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30339(int& val) {
    const int x = 30339;
    std::cout << 30339;
    for (int i = 0; i < 30339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30340(int& val) {
    func_in_30340(val);
    const int x = 30340;
    std::cout << 30340;
    for (int i = 0; i < 30340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30340(int& val) {
    const int x = 30340;
    std::cout << 30340;
    for (int i = 0; i < 30340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30341(int& val) {
    func_in_30341(val);
    const int x = 30341;
    std::cout << 30341;
    for (int i = 0; i < 30341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30341(int& val) {
    const int x = 30341;
    std::cout << 30341;
    for (int i = 0; i < 30341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30342(int& val) {
    func_in_30342(val);
    const int x = 30342;
    std::cout << 30342;
    for (int i = 0; i < 30342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30342(int& val) {
    const int x = 30342;
    std::cout << 30342;
    for (int i = 0; i < 30342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30343(int& val) {
    func_in_30343(val);
    const int x = 30343;
    std::cout << 30343;
    for (int i = 0; i < 30343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30343(int& val) {
    const int x = 30343;
    std::cout << 30343;
    for (int i = 0; i < 30343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30344(int& val) {
    func_in_30344(val);
    const int x = 30344;
    std::cout << 30344;
    for (int i = 0; i < 30344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30344(int& val) {
    const int x = 30344;
    std::cout << 30344;
    for (int i = 0; i < 30344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30345(int& val) {
    func_in_30345(val);
    const int x = 30345;
    std::cout << 30345;
    for (int i = 0; i < 30345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30345(int& val) {
    const int x = 30345;
    std::cout << 30345;
    for (int i = 0; i < 30345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30346(int& val) {
    func_in_30346(val);
    const int x = 30346;
    std::cout << 30346;
    for (int i = 0; i < 30346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30346(int& val) {
    const int x = 30346;
    std::cout << 30346;
    for (int i = 0; i < 30346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30347(int& val) {
    func_in_30347(val);
    const int x = 30347;
    std::cout << 30347;
    for (int i = 0; i < 30347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30347(int& val) {
    const int x = 30347;
    std::cout << 30347;
    for (int i = 0; i < 30347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30348(int& val) {
    func_in_30348(val);
    const int x = 30348;
    std::cout << 30348;
    for (int i = 0; i < 30348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30348(int& val) {
    const int x = 30348;
    std::cout << 30348;
    for (int i = 0; i < 30348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30349(int& val) {
    func_in_30349(val);
    const int x = 30349;
    std::cout << 30349;
    for (int i = 0; i < 30349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30349(int& val) {
    const int x = 30349;
    std::cout << 30349;
    for (int i = 0; i < 30349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30350(int& val) {
    func_in_30350(val);
    const int x = 30350;
    std::cout << 30350;
    for (int i = 0; i < 30350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30350(int& val) {
    const int x = 30350;
    std::cout << 30350;
    for (int i = 0; i < 30350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30351(int& val) {
    func_in_30351(val);
    const int x = 30351;
    std::cout << 30351;
    for (int i = 0; i < 30351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30351(int& val) {
    const int x = 30351;
    std::cout << 30351;
    for (int i = 0; i < 30351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30352(int& val) {
    func_in_30352(val);
    const int x = 30352;
    std::cout << 30352;
    for (int i = 0; i < 30352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30352(int& val) {
    const int x = 30352;
    std::cout << 30352;
    for (int i = 0; i < 30352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30353(int& val) {
    func_in_30353(val);
    const int x = 30353;
    std::cout << 30353;
    for (int i = 0; i < 30353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30353(int& val) {
    const int x = 30353;
    std::cout << 30353;
    for (int i = 0; i < 30353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30354(int& val) {
    func_in_30354(val);
    const int x = 30354;
    std::cout << 30354;
    for (int i = 0; i < 30354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30354(int& val) {
    const int x = 30354;
    std::cout << 30354;
    for (int i = 0; i < 30354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30355(int& val) {
    func_in_30355(val);
    const int x = 30355;
    std::cout << 30355;
    for (int i = 0; i < 30355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30355(int& val) {
    const int x = 30355;
    std::cout << 30355;
    for (int i = 0; i < 30355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30356(int& val) {
    func_in_30356(val);
    const int x = 30356;
    std::cout << 30356;
    for (int i = 0; i < 30356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30356(int& val) {
    const int x = 30356;
    std::cout << 30356;
    for (int i = 0; i < 30356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30357(int& val) {
    func_in_30357(val);
    const int x = 30357;
    std::cout << 30357;
    for (int i = 0; i < 30357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30357(int& val) {
    const int x = 30357;
    std::cout << 30357;
    for (int i = 0; i < 30357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30358(int& val) {
    func_in_30358(val);
    const int x = 30358;
    std::cout << 30358;
    for (int i = 0; i < 30358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30358(int& val) {
    const int x = 30358;
    std::cout << 30358;
    for (int i = 0; i < 30358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30359(int& val) {
    func_in_30359(val);
    const int x = 30359;
    std::cout << 30359;
    for (int i = 0; i < 30359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30359(int& val) {
    const int x = 30359;
    std::cout << 30359;
    for (int i = 0; i < 30359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30360(int& val) {
    func_in_30360(val);
    const int x = 30360;
    std::cout << 30360;
    for (int i = 0; i < 30360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30360(int& val) {
    const int x = 30360;
    std::cout << 30360;
    for (int i = 0; i < 30360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30361(int& val) {
    func_in_30361(val);
    const int x = 30361;
    std::cout << 30361;
    for (int i = 0; i < 30361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30361(int& val) {
    const int x = 30361;
    std::cout << 30361;
    for (int i = 0; i < 30361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30362(int& val) {
    func_in_30362(val);
    const int x = 30362;
    std::cout << 30362;
    for (int i = 0; i < 30362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30362(int& val) {
    const int x = 30362;
    std::cout << 30362;
    for (int i = 0; i < 30362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30363(int& val) {
    func_in_30363(val);
    const int x = 30363;
    std::cout << 30363;
    for (int i = 0; i < 30363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30363(int& val) {
    const int x = 30363;
    std::cout << 30363;
    for (int i = 0; i < 30363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30364(int& val) {
    func_in_30364(val);
    const int x = 30364;
    std::cout << 30364;
    for (int i = 0; i < 30364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30364(int& val) {
    const int x = 30364;
    std::cout << 30364;
    for (int i = 0; i < 30364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30365(int& val) {
    func_in_30365(val);
    const int x = 30365;
    std::cout << 30365;
    for (int i = 0; i < 30365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30365(int& val) {
    const int x = 30365;
    std::cout << 30365;
    for (int i = 0; i < 30365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30366(int& val) {
    func_in_30366(val);
    const int x = 30366;
    std::cout << 30366;
    for (int i = 0; i < 30366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30366(int& val) {
    const int x = 30366;
    std::cout << 30366;
    for (int i = 0; i < 30366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30367(int& val) {
    func_in_30367(val);
    const int x = 30367;
    std::cout << 30367;
    for (int i = 0; i < 30367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30367(int& val) {
    const int x = 30367;
    std::cout << 30367;
    for (int i = 0; i < 30367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30368(int& val) {
    func_in_30368(val);
    const int x = 30368;
    std::cout << 30368;
    for (int i = 0; i < 30368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30368(int& val) {
    const int x = 30368;
    std::cout << 30368;
    for (int i = 0; i < 30368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30369(int& val) {
    func_in_30369(val);
    const int x = 30369;
    std::cout << 30369;
    for (int i = 0; i < 30369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30369(int& val) {
    const int x = 30369;
    std::cout << 30369;
    for (int i = 0; i < 30369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30370(int& val) {
    func_in_30370(val);
    const int x = 30370;
    std::cout << 30370;
    for (int i = 0; i < 30370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30370(int& val) {
    const int x = 30370;
    std::cout << 30370;
    for (int i = 0; i < 30370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30371(int& val) {
    func_in_30371(val);
    const int x = 30371;
    std::cout << 30371;
    for (int i = 0; i < 30371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30371(int& val) {
    const int x = 30371;
    std::cout << 30371;
    for (int i = 0; i < 30371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30372(int& val) {
    func_in_30372(val);
    const int x = 30372;
    std::cout << 30372;
    for (int i = 0; i < 30372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30372(int& val) {
    const int x = 30372;
    std::cout << 30372;
    for (int i = 0; i < 30372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30373(int& val) {
    func_in_30373(val);
    const int x = 30373;
    std::cout << 30373;
    for (int i = 0; i < 30373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30373(int& val) {
    const int x = 30373;
    std::cout << 30373;
    for (int i = 0; i < 30373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30374(int& val) {
    func_in_30374(val);
    const int x = 30374;
    std::cout << 30374;
    for (int i = 0; i < 30374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30374(int& val) {
    const int x = 30374;
    std::cout << 30374;
    for (int i = 0; i < 30374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30375(int& val) {
    func_in_30375(val);
    const int x = 30375;
    std::cout << 30375;
    for (int i = 0; i < 30375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30375(int& val) {
    const int x = 30375;
    std::cout << 30375;
    for (int i = 0; i < 30375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30376(int& val) {
    func_in_30376(val);
    const int x = 30376;
    std::cout << 30376;
    for (int i = 0; i < 30376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30376(int& val) {
    const int x = 30376;
    std::cout << 30376;
    for (int i = 0; i < 30376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30377(int& val) {
    func_in_30377(val);
    const int x = 30377;
    std::cout << 30377;
    for (int i = 0; i < 30377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30377(int& val) {
    const int x = 30377;
    std::cout << 30377;
    for (int i = 0; i < 30377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30378(int& val) {
    func_in_30378(val);
    const int x = 30378;
    std::cout << 30378;
    for (int i = 0; i < 30378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30378(int& val) {
    const int x = 30378;
    std::cout << 30378;
    for (int i = 0; i < 30378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30379(int& val) {
    func_in_30379(val);
    const int x = 30379;
    std::cout << 30379;
    for (int i = 0; i < 30379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30379(int& val) {
    const int x = 30379;
    std::cout << 30379;
    for (int i = 0; i < 30379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30380(int& val) {
    func_in_30380(val);
    const int x = 30380;
    std::cout << 30380;
    for (int i = 0; i < 30380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30380(int& val) {
    const int x = 30380;
    std::cout << 30380;
    for (int i = 0; i < 30380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30381(int& val) {
    func_in_30381(val);
    const int x = 30381;
    std::cout << 30381;
    for (int i = 0; i < 30381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30381(int& val) {
    const int x = 30381;
    std::cout << 30381;
    for (int i = 0; i < 30381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30382(int& val) {
    func_in_30382(val);
    const int x = 30382;
    std::cout << 30382;
    for (int i = 0; i < 30382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30382(int& val) {
    const int x = 30382;
    std::cout << 30382;
    for (int i = 0; i < 30382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30383(int& val) {
    func_in_30383(val);
    const int x = 30383;
    std::cout << 30383;
    for (int i = 0; i < 30383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30383(int& val) {
    const int x = 30383;
    std::cout << 30383;
    for (int i = 0; i < 30383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30384(int& val) {
    func_in_30384(val);
    const int x = 30384;
    std::cout << 30384;
    for (int i = 0; i < 30384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30384(int& val) {
    const int x = 30384;
    std::cout << 30384;
    for (int i = 0; i < 30384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30385(int& val) {
    func_in_30385(val);
    const int x = 30385;
    std::cout << 30385;
    for (int i = 0; i < 30385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30385(int& val) {
    const int x = 30385;
    std::cout << 30385;
    for (int i = 0; i < 30385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30386(int& val) {
    func_in_30386(val);
    const int x = 30386;
    std::cout << 30386;
    for (int i = 0; i < 30386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30386(int& val) {
    const int x = 30386;
    std::cout << 30386;
    for (int i = 0; i < 30386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30387(int& val) {
    func_in_30387(val);
    const int x = 30387;
    std::cout << 30387;
    for (int i = 0; i < 30387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30387(int& val) {
    const int x = 30387;
    std::cout << 30387;
    for (int i = 0; i < 30387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30388(int& val) {
    func_in_30388(val);
    const int x = 30388;
    std::cout << 30388;
    for (int i = 0; i < 30388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30388(int& val) {
    const int x = 30388;
    std::cout << 30388;
    for (int i = 0; i < 30388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30389(int& val) {
    func_in_30389(val);
    const int x = 30389;
    std::cout << 30389;
    for (int i = 0; i < 30389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30389(int& val) {
    const int x = 30389;
    std::cout << 30389;
    for (int i = 0; i < 30389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30390(int& val) {
    func_in_30390(val);
    const int x = 30390;
    std::cout << 30390;
    for (int i = 0; i < 30390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30390(int& val) {
    const int x = 30390;
    std::cout << 30390;
    for (int i = 0; i < 30390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30391(int& val) {
    func_in_30391(val);
    const int x = 30391;
    std::cout << 30391;
    for (int i = 0; i < 30391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30391(int& val) {
    const int x = 30391;
    std::cout << 30391;
    for (int i = 0; i < 30391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30392(int& val) {
    func_in_30392(val);
    const int x = 30392;
    std::cout << 30392;
    for (int i = 0; i < 30392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30392(int& val) {
    const int x = 30392;
    std::cout << 30392;
    for (int i = 0; i < 30392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30393(int& val) {
    func_in_30393(val);
    const int x = 30393;
    std::cout << 30393;
    for (int i = 0; i < 30393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30393(int& val) {
    const int x = 30393;
    std::cout << 30393;
    for (int i = 0; i < 30393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30394(int& val) {
    func_in_30394(val);
    const int x = 30394;
    std::cout << 30394;
    for (int i = 0; i < 30394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30394(int& val) {
    const int x = 30394;
    std::cout << 30394;
    for (int i = 0; i < 30394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30395(int& val) {
    func_in_30395(val);
    const int x = 30395;
    std::cout << 30395;
    for (int i = 0; i < 30395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30395(int& val) {
    const int x = 30395;
    std::cout << 30395;
    for (int i = 0; i < 30395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30396(int& val) {
    func_in_30396(val);
    const int x = 30396;
    std::cout << 30396;
    for (int i = 0; i < 30396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30396(int& val) {
    const int x = 30396;
    std::cout << 30396;
    for (int i = 0; i < 30396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30397(int& val) {
    func_in_30397(val);
    const int x = 30397;
    std::cout << 30397;
    for (int i = 0; i < 30397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30397(int& val) {
    const int x = 30397;
    std::cout << 30397;
    for (int i = 0; i < 30397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30398(int& val) {
    func_in_30398(val);
    const int x = 30398;
    std::cout << 30398;
    for (int i = 0; i < 30398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30398(int& val) {
    const int x = 30398;
    std::cout << 30398;
    for (int i = 0; i < 30398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30399(int& val) {
    func_in_30399(val);
    const int x = 30399;
    std::cout << 30399;
    for (int i = 0; i < 30399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30399(int& val) {
    const int x = 30399;
    std::cout << 30399;
    for (int i = 0; i < 30399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30400(int& val) {
    func_in_30400(val);
    const int x = 30400;
    std::cout << 30400;
    for (int i = 0; i < 30400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30400(int& val) {
    const int x = 30400;
    std::cout << 30400;
    for (int i = 0; i < 30400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30401(int& val) {
    func_in_30401(val);
    const int x = 30401;
    std::cout << 30401;
    for (int i = 0; i < 30401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30401(int& val) {
    const int x = 30401;
    std::cout << 30401;
    for (int i = 0; i < 30401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30402(int& val) {
    func_in_30402(val);
    const int x = 30402;
    std::cout << 30402;
    for (int i = 0; i < 30402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30402(int& val) {
    const int x = 30402;
    std::cout << 30402;
    for (int i = 0; i < 30402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30403(int& val) {
    func_in_30403(val);
    const int x = 30403;
    std::cout << 30403;
    for (int i = 0; i < 30403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30403(int& val) {
    const int x = 30403;
    std::cout << 30403;
    for (int i = 0; i < 30403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30404(int& val) {
    func_in_30404(val);
    const int x = 30404;
    std::cout << 30404;
    for (int i = 0; i < 30404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30404(int& val) {
    const int x = 30404;
    std::cout << 30404;
    for (int i = 0; i < 30404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30405(int& val) {
    func_in_30405(val);
    const int x = 30405;
    std::cout << 30405;
    for (int i = 0; i < 30405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30405(int& val) {
    const int x = 30405;
    std::cout << 30405;
    for (int i = 0; i < 30405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30406(int& val) {
    func_in_30406(val);
    const int x = 30406;
    std::cout << 30406;
    for (int i = 0; i < 30406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30406(int& val) {
    const int x = 30406;
    std::cout << 30406;
    for (int i = 0; i < 30406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30407(int& val) {
    func_in_30407(val);
    const int x = 30407;
    std::cout << 30407;
    for (int i = 0; i < 30407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30407(int& val) {
    const int x = 30407;
    std::cout << 30407;
    for (int i = 0; i < 30407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30408(int& val) {
    func_in_30408(val);
    const int x = 30408;
    std::cout << 30408;
    for (int i = 0; i < 30408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30408(int& val) {
    const int x = 30408;
    std::cout << 30408;
    for (int i = 0; i < 30408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30409(int& val) {
    func_in_30409(val);
    const int x = 30409;
    std::cout << 30409;
    for (int i = 0; i < 30409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30409(int& val) {
    const int x = 30409;
    std::cout << 30409;
    for (int i = 0; i < 30409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30410(int& val) {
    func_in_30410(val);
    const int x = 30410;
    std::cout << 30410;
    for (int i = 0; i < 30410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30410(int& val) {
    const int x = 30410;
    std::cout << 30410;
    for (int i = 0; i < 30410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30411(int& val) {
    func_in_30411(val);
    const int x = 30411;
    std::cout << 30411;
    for (int i = 0; i < 30411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30411(int& val) {
    const int x = 30411;
    std::cout << 30411;
    for (int i = 0; i < 30411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30412(int& val) {
    func_in_30412(val);
    const int x = 30412;
    std::cout << 30412;
    for (int i = 0; i < 30412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30412(int& val) {
    const int x = 30412;
    std::cout << 30412;
    for (int i = 0; i < 30412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30413(int& val) {
    func_in_30413(val);
    const int x = 30413;
    std::cout << 30413;
    for (int i = 0; i < 30413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30413(int& val) {
    const int x = 30413;
    std::cout << 30413;
    for (int i = 0; i < 30413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30414(int& val) {
    func_in_30414(val);
    const int x = 30414;
    std::cout << 30414;
    for (int i = 0; i < 30414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30414(int& val) {
    const int x = 30414;
    std::cout << 30414;
    for (int i = 0; i < 30414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30415(int& val) {
    func_in_30415(val);
    const int x = 30415;
    std::cout << 30415;
    for (int i = 0; i < 30415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30415(int& val) {
    const int x = 30415;
    std::cout << 30415;
    for (int i = 0; i < 30415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30416(int& val) {
    func_in_30416(val);
    const int x = 30416;
    std::cout << 30416;
    for (int i = 0; i < 30416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30416(int& val) {
    const int x = 30416;
    std::cout << 30416;
    for (int i = 0; i < 30416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30417(int& val) {
    func_in_30417(val);
    const int x = 30417;
    std::cout << 30417;
    for (int i = 0; i < 30417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30417(int& val) {
    const int x = 30417;
    std::cout << 30417;
    for (int i = 0; i < 30417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30418(int& val) {
    func_in_30418(val);
    const int x = 30418;
    std::cout << 30418;
    for (int i = 0; i < 30418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30418(int& val) {
    const int x = 30418;
    std::cout << 30418;
    for (int i = 0; i < 30418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30419(int& val) {
    func_in_30419(val);
    const int x = 30419;
    std::cout << 30419;
    for (int i = 0; i < 30419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30419(int& val) {
    const int x = 30419;
    std::cout << 30419;
    for (int i = 0; i < 30419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30420(int& val) {
    func_in_30420(val);
    const int x = 30420;
    std::cout << 30420;
    for (int i = 0; i < 30420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30420(int& val) {
    const int x = 30420;
    std::cout << 30420;
    for (int i = 0; i < 30420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30421(int& val) {
    func_in_30421(val);
    const int x = 30421;
    std::cout << 30421;
    for (int i = 0; i < 30421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30421(int& val) {
    const int x = 30421;
    std::cout << 30421;
    for (int i = 0; i < 30421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30422(int& val) {
    func_in_30422(val);
    const int x = 30422;
    std::cout << 30422;
    for (int i = 0; i < 30422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30422(int& val) {
    const int x = 30422;
    std::cout << 30422;
    for (int i = 0; i < 30422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30423(int& val) {
    func_in_30423(val);
    const int x = 30423;
    std::cout << 30423;
    for (int i = 0; i < 30423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30423(int& val) {
    const int x = 30423;
    std::cout << 30423;
    for (int i = 0; i < 30423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30424(int& val) {
    func_in_30424(val);
    const int x = 30424;
    std::cout << 30424;
    for (int i = 0; i < 30424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30424(int& val) {
    const int x = 30424;
    std::cout << 30424;
    for (int i = 0; i < 30424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30425(int& val) {
    func_in_30425(val);
    const int x = 30425;
    std::cout << 30425;
    for (int i = 0; i < 30425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30425(int& val) {
    const int x = 30425;
    std::cout << 30425;
    for (int i = 0; i < 30425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30426(int& val) {
    func_in_30426(val);
    const int x = 30426;
    std::cout << 30426;
    for (int i = 0; i < 30426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30426(int& val) {
    const int x = 30426;
    std::cout << 30426;
    for (int i = 0; i < 30426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30427(int& val) {
    func_in_30427(val);
    const int x = 30427;
    std::cout << 30427;
    for (int i = 0; i < 30427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30427(int& val) {
    const int x = 30427;
    std::cout << 30427;
    for (int i = 0; i < 30427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30428(int& val) {
    func_in_30428(val);
    const int x = 30428;
    std::cout << 30428;
    for (int i = 0; i < 30428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30428(int& val) {
    const int x = 30428;
    std::cout << 30428;
    for (int i = 0; i < 30428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30429(int& val) {
    func_in_30429(val);
    const int x = 30429;
    std::cout << 30429;
    for (int i = 0; i < 30429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30429(int& val) {
    const int x = 30429;
    std::cout << 30429;
    for (int i = 0; i < 30429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30430(int& val) {
    func_in_30430(val);
    const int x = 30430;
    std::cout << 30430;
    for (int i = 0; i < 30430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30430(int& val) {
    const int x = 30430;
    std::cout << 30430;
    for (int i = 0; i < 30430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30431(int& val) {
    func_in_30431(val);
    const int x = 30431;
    std::cout << 30431;
    for (int i = 0; i < 30431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30431(int& val) {
    const int x = 30431;
    std::cout << 30431;
    for (int i = 0; i < 30431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30432(int& val) {
    func_in_30432(val);
    const int x = 30432;
    std::cout << 30432;
    for (int i = 0; i < 30432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30432(int& val) {
    const int x = 30432;
    std::cout << 30432;
    for (int i = 0; i < 30432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30433(int& val) {
    func_in_30433(val);
    const int x = 30433;
    std::cout << 30433;
    for (int i = 0; i < 30433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30433(int& val) {
    const int x = 30433;
    std::cout << 30433;
    for (int i = 0; i < 30433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30434(int& val) {
    func_in_30434(val);
    const int x = 30434;
    std::cout << 30434;
    for (int i = 0; i < 30434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30434(int& val) {
    const int x = 30434;
    std::cout << 30434;
    for (int i = 0; i < 30434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30435(int& val) {
    func_in_30435(val);
    const int x = 30435;
    std::cout << 30435;
    for (int i = 0; i < 30435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30435(int& val) {
    const int x = 30435;
    std::cout << 30435;
    for (int i = 0; i < 30435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30436(int& val) {
    func_in_30436(val);
    const int x = 30436;
    std::cout << 30436;
    for (int i = 0; i < 30436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30436(int& val) {
    const int x = 30436;
    std::cout << 30436;
    for (int i = 0; i < 30436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30437(int& val) {
    func_in_30437(val);
    const int x = 30437;
    std::cout << 30437;
    for (int i = 0; i < 30437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30437(int& val) {
    const int x = 30437;
    std::cout << 30437;
    for (int i = 0; i < 30437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30438(int& val) {
    func_in_30438(val);
    const int x = 30438;
    std::cout << 30438;
    for (int i = 0; i < 30438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30438(int& val) {
    const int x = 30438;
    std::cout << 30438;
    for (int i = 0; i < 30438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30439(int& val) {
    func_in_30439(val);
    const int x = 30439;
    std::cout << 30439;
    for (int i = 0; i < 30439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30439(int& val) {
    const int x = 30439;
    std::cout << 30439;
    for (int i = 0; i < 30439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30440(int& val) {
    func_in_30440(val);
    const int x = 30440;
    std::cout << 30440;
    for (int i = 0; i < 30440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30440(int& val) {
    const int x = 30440;
    std::cout << 30440;
    for (int i = 0; i < 30440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30441(int& val) {
    func_in_30441(val);
    const int x = 30441;
    std::cout << 30441;
    for (int i = 0; i < 30441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30441(int& val) {
    const int x = 30441;
    std::cout << 30441;
    for (int i = 0; i < 30441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30442(int& val) {
    func_in_30442(val);
    const int x = 30442;
    std::cout << 30442;
    for (int i = 0; i < 30442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30442(int& val) {
    const int x = 30442;
    std::cout << 30442;
    for (int i = 0; i < 30442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30443(int& val) {
    func_in_30443(val);
    const int x = 30443;
    std::cout << 30443;
    for (int i = 0; i < 30443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30443(int& val) {
    const int x = 30443;
    std::cout << 30443;
    for (int i = 0; i < 30443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30444(int& val) {
    func_in_30444(val);
    const int x = 30444;
    std::cout << 30444;
    for (int i = 0; i < 30444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30444(int& val) {
    const int x = 30444;
    std::cout << 30444;
    for (int i = 0; i < 30444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30445(int& val) {
    func_in_30445(val);
    const int x = 30445;
    std::cout << 30445;
    for (int i = 0; i < 30445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30445(int& val) {
    const int x = 30445;
    std::cout << 30445;
    for (int i = 0; i < 30445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30446(int& val) {
    func_in_30446(val);
    const int x = 30446;
    std::cout << 30446;
    for (int i = 0; i < 30446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30446(int& val) {
    const int x = 30446;
    std::cout << 30446;
    for (int i = 0; i < 30446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30447(int& val) {
    func_in_30447(val);
    const int x = 30447;
    std::cout << 30447;
    for (int i = 0; i < 30447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30447(int& val) {
    const int x = 30447;
    std::cout << 30447;
    for (int i = 0; i < 30447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30448(int& val) {
    func_in_30448(val);
    const int x = 30448;
    std::cout << 30448;
    for (int i = 0; i < 30448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30448(int& val) {
    const int x = 30448;
    std::cout << 30448;
    for (int i = 0; i < 30448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30449(int& val) {
    func_in_30449(val);
    const int x = 30449;
    std::cout << 30449;
    for (int i = 0; i < 30449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30449(int& val) {
    const int x = 30449;
    std::cout << 30449;
    for (int i = 0; i < 30449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30450(int& val) {
    func_in_30450(val);
    const int x = 30450;
    std::cout << 30450;
    for (int i = 0; i < 30450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30450(int& val) {
    const int x = 30450;
    std::cout << 30450;
    for (int i = 0; i < 30450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30451(int& val) {
    func_in_30451(val);
    const int x = 30451;
    std::cout << 30451;
    for (int i = 0; i < 30451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30451(int& val) {
    const int x = 30451;
    std::cout << 30451;
    for (int i = 0; i < 30451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30452(int& val) {
    func_in_30452(val);
    const int x = 30452;
    std::cout << 30452;
    for (int i = 0; i < 30452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30452(int& val) {
    const int x = 30452;
    std::cout << 30452;
    for (int i = 0; i < 30452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30453(int& val) {
    func_in_30453(val);
    const int x = 30453;
    std::cout << 30453;
    for (int i = 0; i < 30453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30453(int& val) {
    const int x = 30453;
    std::cout << 30453;
    for (int i = 0; i < 30453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30454(int& val) {
    func_in_30454(val);
    const int x = 30454;
    std::cout << 30454;
    for (int i = 0; i < 30454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30454(int& val) {
    const int x = 30454;
    std::cout << 30454;
    for (int i = 0; i < 30454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30455(int& val) {
    func_in_30455(val);
    const int x = 30455;
    std::cout << 30455;
    for (int i = 0; i < 30455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30455(int& val) {
    const int x = 30455;
    std::cout << 30455;
    for (int i = 0; i < 30455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30456(int& val) {
    func_in_30456(val);
    const int x = 30456;
    std::cout << 30456;
    for (int i = 0; i < 30456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30456(int& val) {
    const int x = 30456;
    std::cout << 30456;
    for (int i = 0; i < 30456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30457(int& val) {
    func_in_30457(val);
    const int x = 30457;
    std::cout << 30457;
    for (int i = 0; i < 30457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30457(int& val) {
    const int x = 30457;
    std::cout << 30457;
    for (int i = 0; i < 30457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30458(int& val) {
    func_in_30458(val);
    const int x = 30458;
    std::cout << 30458;
    for (int i = 0; i < 30458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30458(int& val) {
    const int x = 30458;
    std::cout << 30458;
    for (int i = 0; i < 30458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30459(int& val) {
    func_in_30459(val);
    const int x = 30459;
    std::cout << 30459;
    for (int i = 0; i < 30459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30459(int& val) {
    const int x = 30459;
    std::cout << 30459;
    for (int i = 0; i < 30459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30460(int& val) {
    func_in_30460(val);
    const int x = 30460;
    std::cout << 30460;
    for (int i = 0; i < 30460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30460(int& val) {
    const int x = 30460;
    std::cout << 30460;
    for (int i = 0; i < 30460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30461(int& val) {
    func_in_30461(val);
    const int x = 30461;
    std::cout << 30461;
    for (int i = 0; i < 30461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30461(int& val) {
    const int x = 30461;
    std::cout << 30461;
    for (int i = 0; i < 30461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30462(int& val) {
    func_in_30462(val);
    const int x = 30462;
    std::cout << 30462;
    for (int i = 0; i < 30462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30462(int& val) {
    const int x = 30462;
    std::cout << 30462;
    for (int i = 0; i < 30462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30463(int& val) {
    func_in_30463(val);
    const int x = 30463;
    std::cout << 30463;
    for (int i = 0; i < 30463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30463(int& val) {
    const int x = 30463;
    std::cout << 30463;
    for (int i = 0; i < 30463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30464(int& val) {
    func_in_30464(val);
    const int x = 30464;
    std::cout << 30464;
    for (int i = 0; i < 30464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30464(int& val) {
    const int x = 30464;
    std::cout << 30464;
    for (int i = 0; i < 30464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30465(int& val) {
    func_in_30465(val);
    const int x = 30465;
    std::cout << 30465;
    for (int i = 0; i < 30465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30465(int& val) {
    const int x = 30465;
    std::cout << 30465;
    for (int i = 0; i < 30465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30466(int& val) {
    func_in_30466(val);
    const int x = 30466;
    std::cout << 30466;
    for (int i = 0; i < 30466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30466(int& val) {
    const int x = 30466;
    std::cout << 30466;
    for (int i = 0; i < 30466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30467(int& val) {
    func_in_30467(val);
    const int x = 30467;
    std::cout << 30467;
    for (int i = 0; i < 30467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30467(int& val) {
    const int x = 30467;
    std::cout << 30467;
    for (int i = 0; i < 30467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30468(int& val) {
    func_in_30468(val);
    const int x = 30468;
    std::cout << 30468;
    for (int i = 0; i < 30468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30468(int& val) {
    const int x = 30468;
    std::cout << 30468;
    for (int i = 0; i < 30468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30469(int& val) {
    func_in_30469(val);
    const int x = 30469;
    std::cout << 30469;
    for (int i = 0; i < 30469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30469(int& val) {
    const int x = 30469;
    std::cout << 30469;
    for (int i = 0; i < 30469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30470(int& val) {
    func_in_30470(val);
    const int x = 30470;
    std::cout << 30470;
    for (int i = 0; i < 30470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30470(int& val) {
    const int x = 30470;
    std::cout << 30470;
    for (int i = 0; i < 30470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30471(int& val) {
    func_in_30471(val);
    const int x = 30471;
    std::cout << 30471;
    for (int i = 0; i < 30471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30471(int& val) {
    const int x = 30471;
    std::cout << 30471;
    for (int i = 0; i < 30471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30472(int& val) {
    func_in_30472(val);
    const int x = 30472;
    std::cout << 30472;
    for (int i = 0; i < 30472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30472(int& val) {
    const int x = 30472;
    std::cout << 30472;
    for (int i = 0; i < 30472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30473(int& val) {
    func_in_30473(val);
    const int x = 30473;
    std::cout << 30473;
    for (int i = 0; i < 30473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30473(int& val) {
    const int x = 30473;
    std::cout << 30473;
    for (int i = 0; i < 30473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30474(int& val) {
    func_in_30474(val);
    const int x = 30474;
    std::cout << 30474;
    for (int i = 0; i < 30474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30474(int& val) {
    const int x = 30474;
    std::cout << 30474;
    for (int i = 0; i < 30474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30475(int& val) {
    func_in_30475(val);
    const int x = 30475;
    std::cout << 30475;
    for (int i = 0; i < 30475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30475(int& val) {
    const int x = 30475;
    std::cout << 30475;
    for (int i = 0; i < 30475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30476(int& val) {
    func_in_30476(val);
    const int x = 30476;
    std::cout << 30476;
    for (int i = 0; i < 30476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30476(int& val) {
    const int x = 30476;
    std::cout << 30476;
    for (int i = 0; i < 30476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30477(int& val) {
    func_in_30477(val);
    const int x = 30477;
    std::cout << 30477;
    for (int i = 0; i < 30477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30477(int& val) {
    const int x = 30477;
    std::cout << 30477;
    for (int i = 0; i < 30477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30478(int& val) {
    func_in_30478(val);
    const int x = 30478;
    std::cout << 30478;
    for (int i = 0; i < 30478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30478(int& val) {
    const int x = 30478;
    std::cout << 30478;
    for (int i = 0; i < 30478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30479(int& val) {
    func_in_30479(val);
    const int x = 30479;
    std::cout << 30479;
    for (int i = 0; i < 30479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30479(int& val) {
    const int x = 30479;
    std::cout << 30479;
    for (int i = 0; i < 30479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30480(int& val) {
    func_in_30480(val);
    const int x = 30480;
    std::cout << 30480;
    for (int i = 0; i < 30480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30480(int& val) {
    const int x = 30480;
    std::cout << 30480;
    for (int i = 0; i < 30480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30481(int& val) {
    func_in_30481(val);
    const int x = 30481;
    std::cout << 30481;
    for (int i = 0; i < 30481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30481(int& val) {
    const int x = 30481;
    std::cout << 30481;
    for (int i = 0; i < 30481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30482(int& val) {
    func_in_30482(val);
    const int x = 30482;
    std::cout << 30482;
    for (int i = 0; i < 30482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30482(int& val) {
    const int x = 30482;
    std::cout << 30482;
    for (int i = 0; i < 30482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30483(int& val) {
    func_in_30483(val);
    const int x = 30483;
    std::cout << 30483;
    for (int i = 0; i < 30483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30483(int& val) {
    const int x = 30483;
    std::cout << 30483;
    for (int i = 0; i < 30483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30484(int& val) {
    func_in_30484(val);
    const int x = 30484;
    std::cout << 30484;
    for (int i = 0; i < 30484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30484(int& val) {
    const int x = 30484;
    std::cout << 30484;
    for (int i = 0; i < 30484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30485(int& val) {
    func_in_30485(val);
    const int x = 30485;
    std::cout << 30485;
    for (int i = 0; i < 30485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30485(int& val) {
    const int x = 30485;
    std::cout << 30485;
    for (int i = 0; i < 30485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30486(int& val) {
    func_in_30486(val);
    const int x = 30486;
    std::cout << 30486;
    for (int i = 0; i < 30486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30486(int& val) {
    const int x = 30486;
    std::cout << 30486;
    for (int i = 0; i < 30486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30487(int& val) {
    func_in_30487(val);
    const int x = 30487;
    std::cout << 30487;
    for (int i = 0; i < 30487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30487(int& val) {
    const int x = 30487;
    std::cout << 30487;
    for (int i = 0; i < 30487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30488(int& val) {
    func_in_30488(val);
    const int x = 30488;
    std::cout << 30488;
    for (int i = 0; i < 30488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30488(int& val) {
    const int x = 30488;
    std::cout << 30488;
    for (int i = 0; i < 30488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30489(int& val) {
    func_in_30489(val);
    const int x = 30489;
    std::cout << 30489;
    for (int i = 0; i < 30489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30489(int& val) {
    const int x = 30489;
    std::cout << 30489;
    for (int i = 0; i < 30489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30490(int& val) {
    func_in_30490(val);
    const int x = 30490;
    std::cout << 30490;
    for (int i = 0; i < 30490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30490(int& val) {
    const int x = 30490;
    std::cout << 30490;
    for (int i = 0; i < 30490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30491(int& val) {
    func_in_30491(val);
    const int x = 30491;
    std::cout << 30491;
    for (int i = 0; i < 30491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30491(int& val) {
    const int x = 30491;
    std::cout << 30491;
    for (int i = 0; i < 30491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30492(int& val) {
    func_in_30492(val);
    const int x = 30492;
    std::cout << 30492;
    for (int i = 0; i < 30492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30492(int& val) {
    const int x = 30492;
    std::cout << 30492;
    for (int i = 0; i < 30492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30493(int& val) {
    func_in_30493(val);
    const int x = 30493;
    std::cout << 30493;
    for (int i = 0; i < 30493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30493(int& val) {
    const int x = 30493;
    std::cout << 30493;
    for (int i = 0; i < 30493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30494(int& val) {
    func_in_30494(val);
    const int x = 30494;
    std::cout << 30494;
    for (int i = 0; i < 30494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30494(int& val) {
    const int x = 30494;
    std::cout << 30494;
    for (int i = 0; i < 30494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30495(int& val) {
    func_in_30495(val);
    const int x = 30495;
    std::cout << 30495;
    for (int i = 0; i < 30495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30495(int& val) {
    const int x = 30495;
    std::cout << 30495;
    for (int i = 0; i < 30495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30496(int& val) {
    func_in_30496(val);
    const int x = 30496;
    std::cout << 30496;
    for (int i = 0; i < 30496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30496(int& val) {
    const int x = 30496;
    std::cout << 30496;
    for (int i = 0; i < 30496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30497(int& val) {
    func_in_30497(val);
    const int x = 30497;
    std::cout << 30497;
    for (int i = 0; i < 30497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30497(int& val) {
    const int x = 30497;
    std::cout << 30497;
    for (int i = 0; i < 30497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30498(int& val) {
    func_in_30498(val);
    const int x = 30498;
    std::cout << 30498;
    for (int i = 0; i < 30498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30498(int& val) {
    const int x = 30498;
    std::cout << 30498;
    for (int i = 0; i < 30498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30499(int& val) {
    func_in_30499(val);
    const int x = 30499;
    std::cout << 30499;
    for (int i = 0; i < 30499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30499(int& val) {
    const int x = 30499;
    std::cout << 30499;
    for (int i = 0; i < 30499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30500(int& val) {
    func_in_30500(val);
    const int x = 30500;
    std::cout << 30500;
    for (int i = 0; i < 30500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30500(int& val) {
    const int x = 30500;
    std::cout << 30500;
    for (int i = 0; i < 30500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30501(int& val) {
    func_in_30501(val);
    const int x = 30501;
    std::cout << 30501;
    for (int i = 0; i < 30501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30501(int& val) {
    const int x = 30501;
    std::cout << 30501;
    for (int i = 0; i < 30501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30502(int& val) {
    func_in_30502(val);
    const int x = 30502;
    std::cout << 30502;
    for (int i = 0; i < 30502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30502(int& val) {
    const int x = 30502;
    std::cout << 30502;
    for (int i = 0; i < 30502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30503(int& val) {
    func_in_30503(val);
    const int x = 30503;
    std::cout << 30503;
    for (int i = 0; i < 30503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30503(int& val) {
    const int x = 30503;
    std::cout << 30503;
    for (int i = 0; i < 30503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30504(int& val) {
    func_in_30504(val);
    const int x = 30504;
    std::cout << 30504;
    for (int i = 0; i < 30504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30504(int& val) {
    const int x = 30504;
    std::cout << 30504;
    for (int i = 0; i < 30504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30505(int& val) {
    func_in_30505(val);
    const int x = 30505;
    std::cout << 30505;
    for (int i = 0; i < 30505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30505(int& val) {
    const int x = 30505;
    std::cout << 30505;
    for (int i = 0; i < 30505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30506(int& val) {
    func_in_30506(val);
    const int x = 30506;
    std::cout << 30506;
    for (int i = 0; i < 30506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30506(int& val) {
    const int x = 30506;
    std::cout << 30506;
    for (int i = 0; i < 30506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30507(int& val) {
    func_in_30507(val);
    const int x = 30507;
    std::cout << 30507;
    for (int i = 0; i < 30507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30507(int& val) {
    const int x = 30507;
    std::cout << 30507;
    for (int i = 0; i < 30507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30508(int& val) {
    func_in_30508(val);
    const int x = 30508;
    std::cout << 30508;
    for (int i = 0; i < 30508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30508(int& val) {
    const int x = 30508;
    std::cout << 30508;
    for (int i = 0; i < 30508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30509(int& val) {
    func_in_30509(val);
    const int x = 30509;
    std::cout << 30509;
    for (int i = 0; i < 30509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30509(int& val) {
    const int x = 30509;
    std::cout << 30509;
    for (int i = 0; i < 30509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30510(int& val) {
    func_in_30510(val);
    const int x = 30510;
    std::cout << 30510;
    for (int i = 0; i < 30510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30510(int& val) {
    const int x = 30510;
    std::cout << 30510;
    for (int i = 0; i < 30510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30511(int& val) {
    func_in_30511(val);
    const int x = 30511;
    std::cout << 30511;
    for (int i = 0; i < 30511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30511(int& val) {
    const int x = 30511;
    std::cout << 30511;
    for (int i = 0; i < 30511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30512(int& val) {
    func_in_30512(val);
    const int x = 30512;
    std::cout << 30512;
    for (int i = 0; i < 30512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30512(int& val) {
    const int x = 30512;
    std::cout << 30512;
    for (int i = 0; i < 30512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30513(int& val) {
    func_in_30513(val);
    const int x = 30513;
    std::cout << 30513;
    for (int i = 0; i < 30513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30513(int& val) {
    const int x = 30513;
    std::cout << 30513;
    for (int i = 0; i < 30513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30514(int& val) {
    func_in_30514(val);
    const int x = 30514;
    std::cout << 30514;
    for (int i = 0; i < 30514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30514(int& val) {
    const int x = 30514;
    std::cout << 30514;
    for (int i = 0; i < 30514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30515(int& val) {
    func_in_30515(val);
    const int x = 30515;
    std::cout << 30515;
    for (int i = 0; i < 30515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30515(int& val) {
    const int x = 30515;
    std::cout << 30515;
    for (int i = 0; i < 30515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30516(int& val) {
    func_in_30516(val);
    const int x = 30516;
    std::cout << 30516;
    for (int i = 0; i < 30516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30516(int& val) {
    const int x = 30516;
    std::cout << 30516;
    for (int i = 0; i < 30516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30517(int& val) {
    func_in_30517(val);
    const int x = 30517;
    std::cout << 30517;
    for (int i = 0; i < 30517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30517(int& val) {
    const int x = 30517;
    std::cout << 30517;
    for (int i = 0; i < 30517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30518(int& val) {
    func_in_30518(val);
    const int x = 30518;
    std::cout << 30518;
    for (int i = 0; i < 30518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30518(int& val) {
    const int x = 30518;
    std::cout << 30518;
    for (int i = 0; i < 30518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30519(int& val) {
    func_in_30519(val);
    const int x = 30519;
    std::cout << 30519;
    for (int i = 0; i < 30519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30519(int& val) {
    const int x = 30519;
    std::cout << 30519;
    for (int i = 0; i < 30519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30520(int& val) {
    func_in_30520(val);
    const int x = 30520;
    std::cout << 30520;
    for (int i = 0; i < 30520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30520(int& val) {
    const int x = 30520;
    std::cout << 30520;
    for (int i = 0; i < 30520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30521(int& val) {
    func_in_30521(val);
    const int x = 30521;
    std::cout << 30521;
    for (int i = 0; i < 30521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30521(int& val) {
    const int x = 30521;
    std::cout << 30521;
    for (int i = 0; i < 30521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30522(int& val) {
    func_in_30522(val);
    const int x = 30522;
    std::cout << 30522;
    for (int i = 0; i < 30522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30522(int& val) {
    const int x = 30522;
    std::cout << 30522;
    for (int i = 0; i < 30522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30523(int& val) {
    func_in_30523(val);
    const int x = 30523;
    std::cout << 30523;
    for (int i = 0; i < 30523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30523(int& val) {
    const int x = 30523;
    std::cout << 30523;
    for (int i = 0; i < 30523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30524(int& val) {
    func_in_30524(val);
    const int x = 30524;
    std::cout << 30524;
    for (int i = 0; i < 30524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30524(int& val) {
    const int x = 30524;
    std::cout << 30524;
    for (int i = 0; i < 30524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30525(int& val) {
    func_in_30525(val);
    const int x = 30525;
    std::cout << 30525;
    for (int i = 0; i < 30525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30525(int& val) {
    const int x = 30525;
    std::cout << 30525;
    for (int i = 0; i < 30525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30526(int& val) {
    func_in_30526(val);
    const int x = 30526;
    std::cout << 30526;
    for (int i = 0; i < 30526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30526(int& val) {
    const int x = 30526;
    std::cout << 30526;
    for (int i = 0; i < 30526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30527(int& val) {
    func_in_30527(val);
    const int x = 30527;
    std::cout << 30527;
    for (int i = 0; i < 30527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30527(int& val) {
    const int x = 30527;
    std::cout << 30527;
    for (int i = 0; i < 30527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30528(int& val) {
    func_in_30528(val);
    const int x = 30528;
    std::cout << 30528;
    for (int i = 0; i < 30528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30528(int& val) {
    const int x = 30528;
    std::cout << 30528;
    for (int i = 0; i < 30528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30529(int& val) {
    func_in_30529(val);
    const int x = 30529;
    std::cout << 30529;
    for (int i = 0; i < 30529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30529(int& val) {
    const int x = 30529;
    std::cout << 30529;
    for (int i = 0; i < 30529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30530(int& val) {
    func_in_30530(val);
    const int x = 30530;
    std::cout << 30530;
    for (int i = 0; i < 30530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30530(int& val) {
    const int x = 30530;
    std::cout << 30530;
    for (int i = 0; i < 30530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30531(int& val) {
    func_in_30531(val);
    const int x = 30531;
    std::cout << 30531;
    for (int i = 0; i < 30531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30531(int& val) {
    const int x = 30531;
    std::cout << 30531;
    for (int i = 0; i < 30531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30532(int& val) {
    func_in_30532(val);
    const int x = 30532;
    std::cout << 30532;
    for (int i = 0; i < 30532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30532(int& val) {
    const int x = 30532;
    std::cout << 30532;
    for (int i = 0; i < 30532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30533(int& val) {
    func_in_30533(val);
    const int x = 30533;
    std::cout << 30533;
    for (int i = 0; i < 30533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30533(int& val) {
    const int x = 30533;
    std::cout << 30533;
    for (int i = 0; i < 30533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30534(int& val) {
    func_in_30534(val);
    const int x = 30534;
    std::cout << 30534;
    for (int i = 0; i < 30534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30534(int& val) {
    const int x = 30534;
    std::cout << 30534;
    for (int i = 0; i < 30534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30535(int& val) {
    func_in_30535(val);
    const int x = 30535;
    std::cout << 30535;
    for (int i = 0; i < 30535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30535(int& val) {
    const int x = 30535;
    std::cout << 30535;
    for (int i = 0; i < 30535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30536(int& val) {
    func_in_30536(val);
    const int x = 30536;
    std::cout << 30536;
    for (int i = 0; i < 30536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30536(int& val) {
    const int x = 30536;
    std::cout << 30536;
    for (int i = 0; i < 30536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30537(int& val) {
    func_in_30537(val);
    const int x = 30537;
    std::cout << 30537;
    for (int i = 0; i < 30537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30537(int& val) {
    const int x = 30537;
    std::cout << 30537;
    for (int i = 0; i < 30537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30538(int& val) {
    func_in_30538(val);
    const int x = 30538;
    std::cout << 30538;
    for (int i = 0; i < 30538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30538(int& val) {
    const int x = 30538;
    std::cout << 30538;
    for (int i = 0; i < 30538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30539(int& val) {
    func_in_30539(val);
    const int x = 30539;
    std::cout << 30539;
    for (int i = 0; i < 30539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30539(int& val) {
    const int x = 30539;
    std::cout << 30539;
    for (int i = 0; i < 30539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30540(int& val) {
    func_in_30540(val);
    const int x = 30540;
    std::cout << 30540;
    for (int i = 0; i < 30540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30540(int& val) {
    const int x = 30540;
    std::cout << 30540;
    for (int i = 0; i < 30540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30541(int& val) {
    func_in_30541(val);
    const int x = 30541;
    std::cout << 30541;
    for (int i = 0; i < 30541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30541(int& val) {
    const int x = 30541;
    std::cout << 30541;
    for (int i = 0; i < 30541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30542(int& val) {
    func_in_30542(val);
    const int x = 30542;
    std::cout << 30542;
    for (int i = 0; i < 30542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30542(int& val) {
    const int x = 30542;
    std::cout << 30542;
    for (int i = 0; i < 30542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30543(int& val) {
    func_in_30543(val);
    const int x = 30543;
    std::cout << 30543;
    for (int i = 0; i < 30543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30543(int& val) {
    const int x = 30543;
    std::cout << 30543;
    for (int i = 0; i < 30543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30544(int& val) {
    func_in_30544(val);
    const int x = 30544;
    std::cout << 30544;
    for (int i = 0; i < 30544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30544(int& val) {
    const int x = 30544;
    std::cout << 30544;
    for (int i = 0; i < 30544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30545(int& val) {
    func_in_30545(val);
    const int x = 30545;
    std::cout << 30545;
    for (int i = 0; i < 30545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30545(int& val) {
    const int x = 30545;
    std::cout << 30545;
    for (int i = 0; i < 30545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30546(int& val) {
    func_in_30546(val);
    const int x = 30546;
    std::cout << 30546;
    for (int i = 0; i < 30546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30546(int& val) {
    const int x = 30546;
    std::cout << 30546;
    for (int i = 0; i < 30546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30547(int& val) {
    func_in_30547(val);
    const int x = 30547;
    std::cout << 30547;
    for (int i = 0; i < 30547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30547(int& val) {
    const int x = 30547;
    std::cout << 30547;
    for (int i = 0; i < 30547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30548(int& val) {
    func_in_30548(val);
    const int x = 30548;
    std::cout << 30548;
    for (int i = 0; i < 30548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30548(int& val) {
    const int x = 30548;
    std::cout << 30548;
    for (int i = 0; i < 30548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30549(int& val) {
    func_in_30549(val);
    const int x = 30549;
    std::cout << 30549;
    for (int i = 0; i < 30549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30549(int& val) {
    const int x = 30549;
    std::cout << 30549;
    for (int i = 0; i < 30549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30550(int& val) {
    func_in_30550(val);
    const int x = 30550;
    std::cout << 30550;
    for (int i = 0; i < 30550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30550(int& val) {
    const int x = 30550;
    std::cout << 30550;
    for (int i = 0; i < 30550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30551(int& val) {
    func_in_30551(val);
    const int x = 30551;
    std::cout << 30551;
    for (int i = 0; i < 30551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30551(int& val) {
    const int x = 30551;
    std::cout << 30551;
    for (int i = 0; i < 30551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30552(int& val) {
    func_in_30552(val);
    const int x = 30552;
    std::cout << 30552;
    for (int i = 0; i < 30552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30552(int& val) {
    const int x = 30552;
    std::cout << 30552;
    for (int i = 0; i < 30552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30553(int& val) {
    func_in_30553(val);
    const int x = 30553;
    std::cout << 30553;
    for (int i = 0; i < 30553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30553(int& val) {
    const int x = 30553;
    std::cout << 30553;
    for (int i = 0; i < 30553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30554(int& val) {
    func_in_30554(val);
    const int x = 30554;
    std::cout << 30554;
    for (int i = 0; i < 30554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30554(int& val) {
    const int x = 30554;
    std::cout << 30554;
    for (int i = 0; i < 30554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30555(int& val) {
    func_in_30555(val);
    const int x = 30555;
    std::cout << 30555;
    for (int i = 0; i < 30555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30555(int& val) {
    const int x = 30555;
    std::cout << 30555;
    for (int i = 0; i < 30555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30556(int& val) {
    func_in_30556(val);
    const int x = 30556;
    std::cout << 30556;
    for (int i = 0; i < 30556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30556(int& val) {
    const int x = 30556;
    std::cout << 30556;
    for (int i = 0; i < 30556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30557(int& val) {
    func_in_30557(val);
    const int x = 30557;
    std::cout << 30557;
    for (int i = 0; i < 30557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30557(int& val) {
    const int x = 30557;
    std::cout << 30557;
    for (int i = 0; i < 30557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30558(int& val) {
    func_in_30558(val);
    const int x = 30558;
    std::cout << 30558;
    for (int i = 0; i < 30558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30558(int& val) {
    const int x = 30558;
    std::cout << 30558;
    for (int i = 0; i < 30558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30559(int& val) {
    func_in_30559(val);
    const int x = 30559;
    std::cout << 30559;
    for (int i = 0; i < 30559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30559(int& val) {
    const int x = 30559;
    std::cout << 30559;
    for (int i = 0; i < 30559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30560(int& val) {
    func_in_30560(val);
    const int x = 30560;
    std::cout << 30560;
    for (int i = 0; i < 30560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30560(int& val) {
    const int x = 30560;
    std::cout << 30560;
    for (int i = 0; i < 30560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30561(int& val) {
    func_in_30561(val);
    const int x = 30561;
    std::cout << 30561;
    for (int i = 0; i < 30561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30561(int& val) {
    const int x = 30561;
    std::cout << 30561;
    for (int i = 0; i < 30561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30562(int& val) {
    func_in_30562(val);
    const int x = 30562;
    std::cout << 30562;
    for (int i = 0; i < 30562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30562(int& val) {
    const int x = 30562;
    std::cout << 30562;
    for (int i = 0; i < 30562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30563(int& val) {
    func_in_30563(val);
    const int x = 30563;
    std::cout << 30563;
    for (int i = 0; i < 30563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30563(int& val) {
    const int x = 30563;
    std::cout << 30563;
    for (int i = 0; i < 30563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30564(int& val) {
    func_in_30564(val);
    const int x = 30564;
    std::cout << 30564;
    for (int i = 0; i < 30564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30564(int& val) {
    const int x = 30564;
    std::cout << 30564;
    for (int i = 0; i < 30564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30565(int& val) {
    func_in_30565(val);
    const int x = 30565;
    std::cout << 30565;
    for (int i = 0; i < 30565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30565(int& val) {
    const int x = 30565;
    std::cout << 30565;
    for (int i = 0; i < 30565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30566(int& val) {
    func_in_30566(val);
    const int x = 30566;
    std::cout << 30566;
    for (int i = 0; i < 30566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30566(int& val) {
    const int x = 30566;
    std::cout << 30566;
    for (int i = 0; i < 30566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30567(int& val) {
    func_in_30567(val);
    const int x = 30567;
    std::cout << 30567;
    for (int i = 0; i < 30567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30567(int& val) {
    const int x = 30567;
    std::cout << 30567;
    for (int i = 0; i < 30567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30568(int& val) {
    func_in_30568(val);
    const int x = 30568;
    std::cout << 30568;
    for (int i = 0; i < 30568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30568(int& val) {
    const int x = 30568;
    std::cout << 30568;
    for (int i = 0; i < 30568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30569(int& val) {
    func_in_30569(val);
    const int x = 30569;
    std::cout << 30569;
    for (int i = 0; i < 30569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30569(int& val) {
    const int x = 30569;
    std::cout << 30569;
    for (int i = 0; i < 30569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30570(int& val) {
    func_in_30570(val);
    const int x = 30570;
    std::cout << 30570;
    for (int i = 0; i < 30570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30570(int& val) {
    const int x = 30570;
    std::cout << 30570;
    for (int i = 0; i < 30570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30571(int& val) {
    func_in_30571(val);
    const int x = 30571;
    std::cout << 30571;
    for (int i = 0; i < 30571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30571(int& val) {
    const int x = 30571;
    std::cout << 30571;
    for (int i = 0; i < 30571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30572(int& val) {
    func_in_30572(val);
    const int x = 30572;
    std::cout << 30572;
    for (int i = 0; i < 30572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30572(int& val) {
    const int x = 30572;
    std::cout << 30572;
    for (int i = 0; i < 30572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30573(int& val) {
    func_in_30573(val);
    const int x = 30573;
    std::cout << 30573;
    for (int i = 0; i < 30573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30573(int& val) {
    const int x = 30573;
    std::cout << 30573;
    for (int i = 0; i < 30573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30574(int& val) {
    func_in_30574(val);
    const int x = 30574;
    std::cout << 30574;
    for (int i = 0; i < 30574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30574(int& val) {
    const int x = 30574;
    std::cout << 30574;
    for (int i = 0; i < 30574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30575(int& val) {
    func_in_30575(val);
    const int x = 30575;
    std::cout << 30575;
    for (int i = 0; i < 30575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30575(int& val) {
    const int x = 30575;
    std::cout << 30575;
    for (int i = 0; i < 30575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30576(int& val) {
    func_in_30576(val);
    const int x = 30576;
    std::cout << 30576;
    for (int i = 0; i < 30576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30576(int& val) {
    const int x = 30576;
    std::cout << 30576;
    for (int i = 0; i < 30576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30577(int& val) {
    func_in_30577(val);
    const int x = 30577;
    std::cout << 30577;
    for (int i = 0; i < 30577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30577(int& val) {
    const int x = 30577;
    std::cout << 30577;
    for (int i = 0; i < 30577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30578(int& val) {
    func_in_30578(val);
    const int x = 30578;
    std::cout << 30578;
    for (int i = 0; i < 30578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30578(int& val) {
    const int x = 30578;
    std::cout << 30578;
    for (int i = 0; i < 30578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30579(int& val) {
    func_in_30579(val);
    const int x = 30579;
    std::cout << 30579;
    for (int i = 0; i < 30579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30579(int& val) {
    const int x = 30579;
    std::cout << 30579;
    for (int i = 0; i < 30579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30580(int& val) {
    func_in_30580(val);
    const int x = 30580;
    std::cout << 30580;
    for (int i = 0; i < 30580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30580(int& val) {
    const int x = 30580;
    std::cout << 30580;
    for (int i = 0; i < 30580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30581(int& val) {
    func_in_30581(val);
    const int x = 30581;
    std::cout << 30581;
    for (int i = 0; i < 30581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30581(int& val) {
    const int x = 30581;
    std::cout << 30581;
    for (int i = 0; i < 30581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30582(int& val) {
    func_in_30582(val);
    const int x = 30582;
    std::cout << 30582;
    for (int i = 0; i < 30582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30582(int& val) {
    const int x = 30582;
    std::cout << 30582;
    for (int i = 0; i < 30582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30583(int& val) {
    func_in_30583(val);
    const int x = 30583;
    std::cout << 30583;
    for (int i = 0; i < 30583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30583(int& val) {
    const int x = 30583;
    std::cout << 30583;
    for (int i = 0; i < 30583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30584(int& val) {
    func_in_30584(val);
    const int x = 30584;
    std::cout << 30584;
    for (int i = 0; i < 30584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30584(int& val) {
    const int x = 30584;
    std::cout << 30584;
    for (int i = 0; i < 30584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30585(int& val) {
    func_in_30585(val);
    const int x = 30585;
    std::cout << 30585;
    for (int i = 0; i < 30585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30585(int& val) {
    const int x = 30585;
    std::cout << 30585;
    for (int i = 0; i < 30585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30586(int& val) {
    func_in_30586(val);
    const int x = 30586;
    std::cout << 30586;
    for (int i = 0; i < 30586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30586(int& val) {
    const int x = 30586;
    std::cout << 30586;
    for (int i = 0; i < 30586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30587(int& val) {
    func_in_30587(val);
    const int x = 30587;
    std::cout << 30587;
    for (int i = 0; i < 30587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30587(int& val) {
    const int x = 30587;
    std::cout << 30587;
    for (int i = 0; i < 30587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30588(int& val) {
    func_in_30588(val);
    const int x = 30588;
    std::cout << 30588;
    for (int i = 0; i < 30588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30588(int& val) {
    const int x = 30588;
    std::cout << 30588;
    for (int i = 0; i < 30588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30589(int& val) {
    func_in_30589(val);
    const int x = 30589;
    std::cout << 30589;
    for (int i = 0; i < 30589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30589(int& val) {
    const int x = 30589;
    std::cout << 30589;
    for (int i = 0; i < 30589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30590(int& val) {
    func_in_30590(val);
    const int x = 30590;
    std::cout << 30590;
    for (int i = 0; i < 30590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30590(int& val) {
    const int x = 30590;
    std::cout << 30590;
    for (int i = 0; i < 30590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30591(int& val) {
    func_in_30591(val);
    const int x = 30591;
    std::cout << 30591;
    for (int i = 0; i < 30591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30591(int& val) {
    const int x = 30591;
    std::cout << 30591;
    for (int i = 0; i < 30591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30592(int& val) {
    func_in_30592(val);
    const int x = 30592;
    std::cout << 30592;
    for (int i = 0; i < 30592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30592(int& val) {
    const int x = 30592;
    std::cout << 30592;
    for (int i = 0; i < 30592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30593(int& val) {
    func_in_30593(val);
    const int x = 30593;
    std::cout << 30593;
    for (int i = 0; i < 30593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30593(int& val) {
    const int x = 30593;
    std::cout << 30593;
    for (int i = 0; i < 30593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30594(int& val) {
    func_in_30594(val);
    const int x = 30594;
    std::cout << 30594;
    for (int i = 0; i < 30594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30594(int& val) {
    const int x = 30594;
    std::cout << 30594;
    for (int i = 0; i < 30594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30595(int& val) {
    func_in_30595(val);
    const int x = 30595;
    std::cout << 30595;
    for (int i = 0; i < 30595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30595(int& val) {
    const int x = 30595;
    std::cout << 30595;
    for (int i = 0; i < 30595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30596(int& val) {
    func_in_30596(val);
    const int x = 30596;
    std::cout << 30596;
    for (int i = 0; i < 30596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30596(int& val) {
    const int x = 30596;
    std::cout << 30596;
    for (int i = 0; i < 30596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30597(int& val) {
    func_in_30597(val);
    const int x = 30597;
    std::cout << 30597;
    for (int i = 0; i < 30597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30597(int& val) {
    const int x = 30597;
    std::cout << 30597;
    for (int i = 0; i < 30597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30598(int& val) {
    func_in_30598(val);
    const int x = 30598;
    std::cout << 30598;
    for (int i = 0; i < 30598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30598(int& val) {
    const int x = 30598;
    std::cout << 30598;
    for (int i = 0; i < 30598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30599(int& val) {
    func_in_30599(val);
    const int x = 30599;
    std::cout << 30599;
    for (int i = 0; i < 30599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30599(int& val) {
    const int x = 30599;
    std::cout << 30599;
    for (int i = 0; i < 30599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30600(int& val) {
    func_in_30600(val);
    const int x = 30600;
    std::cout << 30600;
    for (int i = 0; i < 30600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30600(int& val) {
    const int x = 30600;
    std::cout << 30600;
    for (int i = 0; i < 30600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30601(int& val) {
    func_in_30601(val);
    const int x = 30601;
    std::cout << 30601;
    for (int i = 0; i < 30601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30601(int& val) {
    const int x = 30601;
    std::cout << 30601;
    for (int i = 0; i < 30601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30602(int& val) {
    func_in_30602(val);
    const int x = 30602;
    std::cout << 30602;
    for (int i = 0; i < 30602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30602(int& val) {
    const int x = 30602;
    std::cout << 30602;
    for (int i = 0; i < 30602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30603(int& val) {
    func_in_30603(val);
    const int x = 30603;
    std::cout << 30603;
    for (int i = 0; i < 30603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30603(int& val) {
    const int x = 30603;
    std::cout << 30603;
    for (int i = 0; i < 30603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30604(int& val) {
    func_in_30604(val);
    const int x = 30604;
    std::cout << 30604;
    for (int i = 0; i < 30604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30604(int& val) {
    const int x = 30604;
    std::cout << 30604;
    for (int i = 0; i < 30604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30605(int& val) {
    func_in_30605(val);
    const int x = 30605;
    std::cout << 30605;
    for (int i = 0; i < 30605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30605(int& val) {
    const int x = 30605;
    std::cout << 30605;
    for (int i = 0; i < 30605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30606(int& val) {
    func_in_30606(val);
    const int x = 30606;
    std::cout << 30606;
    for (int i = 0; i < 30606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30606(int& val) {
    const int x = 30606;
    std::cout << 30606;
    for (int i = 0; i < 30606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30607(int& val) {
    func_in_30607(val);
    const int x = 30607;
    std::cout << 30607;
    for (int i = 0; i < 30607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30607(int& val) {
    const int x = 30607;
    std::cout << 30607;
    for (int i = 0; i < 30607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30608(int& val) {
    func_in_30608(val);
    const int x = 30608;
    std::cout << 30608;
    for (int i = 0; i < 30608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30608(int& val) {
    const int x = 30608;
    std::cout << 30608;
    for (int i = 0; i < 30608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30609(int& val) {
    func_in_30609(val);
    const int x = 30609;
    std::cout << 30609;
    for (int i = 0; i < 30609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30609(int& val) {
    const int x = 30609;
    std::cout << 30609;
    for (int i = 0; i < 30609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30610(int& val) {
    func_in_30610(val);
    const int x = 30610;
    std::cout << 30610;
    for (int i = 0; i < 30610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30610(int& val) {
    const int x = 30610;
    std::cout << 30610;
    for (int i = 0; i < 30610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30611(int& val) {
    func_in_30611(val);
    const int x = 30611;
    std::cout << 30611;
    for (int i = 0; i < 30611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30611(int& val) {
    const int x = 30611;
    std::cout << 30611;
    for (int i = 0; i < 30611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30612(int& val) {
    func_in_30612(val);
    const int x = 30612;
    std::cout << 30612;
    for (int i = 0; i < 30612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30612(int& val) {
    const int x = 30612;
    std::cout << 30612;
    for (int i = 0; i < 30612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30613(int& val) {
    func_in_30613(val);
    const int x = 30613;
    std::cout << 30613;
    for (int i = 0; i < 30613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30613(int& val) {
    const int x = 30613;
    std::cout << 30613;
    for (int i = 0; i < 30613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30614(int& val) {
    func_in_30614(val);
    const int x = 30614;
    std::cout << 30614;
    for (int i = 0; i < 30614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30614(int& val) {
    const int x = 30614;
    std::cout << 30614;
    for (int i = 0; i < 30614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30615(int& val) {
    func_in_30615(val);
    const int x = 30615;
    std::cout << 30615;
    for (int i = 0; i < 30615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30615(int& val) {
    const int x = 30615;
    std::cout << 30615;
    for (int i = 0; i < 30615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30616(int& val) {
    func_in_30616(val);
    const int x = 30616;
    std::cout << 30616;
    for (int i = 0; i < 30616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30616(int& val) {
    const int x = 30616;
    std::cout << 30616;
    for (int i = 0; i < 30616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30617(int& val) {
    func_in_30617(val);
    const int x = 30617;
    std::cout << 30617;
    for (int i = 0; i < 30617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30617(int& val) {
    const int x = 30617;
    std::cout << 30617;
    for (int i = 0; i < 30617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30618(int& val) {
    func_in_30618(val);
    const int x = 30618;
    std::cout << 30618;
    for (int i = 0; i < 30618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30618(int& val) {
    const int x = 30618;
    std::cout << 30618;
    for (int i = 0; i < 30618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30619(int& val) {
    func_in_30619(val);
    const int x = 30619;
    std::cout << 30619;
    for (int i = 0; i < 30619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30619(int& val) {
    const int x = 30619;
    std::cout << 30619;
    for (int i = 0; i < 30619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30620(int& val) {
    func_in_30620(val);
    const int x = 30620;
    std::cout << 30620;
    for (int i = 0; i < 30620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30620(int& val) {
    const int x = 30620;
    std::cout << 30620;
    for (int i = 0; i < 30620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30621(int& val) {
    func_in_30621(val);
    const int x = 30621;
    std::cout << 30621;
    for (int i = 0; i < 30621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30621(int& val) {
    const int x = 30621;
    std::cout << 30621;
    for (int i = 0; i < 30621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30622(int& val) {
    func_in_30622(val);
    const int x = 30622;
    std::cout << 30622;
    for (int i = 0; i < 30622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30622(int& val) {
    const int x = 30622;
    std::cout << 30622;
    for (int i = 0; i < 30622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30623(int& val) {
    func_in_30623(val);
    const int x = 30623;
    std::cout << 30623;
    for (int i = 0; i < 30623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30623(int& val) {
    const int x = 30623;
    std::cout << 30623;
    for (int i = 0; i < 30623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30624(int& val) {
    func_in_30624(val);
    const int x = 30624;
    std::cout << 30624;
    for (int i = 0; i < 30624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30624(int& val) {
    const int x = 30624;
    std::cout << 30624;
    for (int i = 0; i < 30624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30625(int& val) {
    func_in_30625(val);
    const int x = 30625;
    std::cout << 30625;
    for (int i = 0; i < 30625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30625(int& val) {
    const int x = 30625;
    std::cout << 30625;
    for (int i = 0; i < 30625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30626(int& val) {
    func_in_30626(val);
    const int x = 30626;
    std::cout << 30626;
    for (int i = 0; i < 30626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30626(int& val) {
    const int x = 30626;
    std::cout << 30626;
    for (int i = 0; i < 30626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30627(int& val) {
    func_in_30627(val);
    const int x = 30627;
    std::cout << 30627;
    for (int i = 0; i < 30627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30627(int& val) {
    const int x = 30627;
    std::cout << 30627;
    for (int i = 0; i < 30627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30628(int& val) {
    func_in_30628(val);
    const int x = 30628;
    std::cout << 30628;
    for (int i = 0; i < 30628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30628(int& val) {
    const int x = 30628;
    std::cout << 30628;
    for (int i = 0; i < 30628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30629(int& val) {
    func_in_30629(val);
    const int x = 30629;
    std::cout << 30629;
    for (int i = 0; i < 30629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30629(int& val) {
    const int x = 30629;
    std::cout << 30629;
    for (int i = 0; i < 30629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30630(int& val) {
    func_in_30630(val);
    const int x = 30630;
    std::cout << 30630;
    for (int i = 0; i < 30630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30630(int& val) {
    const int x = 30630;
    std::cout << 30630;
    for (int i = 0; i < 30630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30631(int& val) {
    func_in_30631(val);
    const int x = 30631;
    std::cout << 30631;
    for (int i = 0; i < 30631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30631(int& val) {
    const int x = 30631;
    std::cout << 30631;
    for (int i = 0; i < 30631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30632(int& val) {
    func_in_30632(val);
    const int x = 30632;
    std::cout << 30632;
    for (int i = 0; i < 30632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30632(int& val) {
    const int x = 30632;
    std::cout << 30632;
    for (int i = 0; i < 30632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30633(int& val) {
    func_in_30633(val);
    const int x = 30633;
    std::cout << 30633;
    for (int i = 0; i < 30633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30633(int& val) {
    const int x = 30633;
    std::cout << 30633;
    for (int i = 0; i < 30633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30634(int& val) {
    func_in_30634(val);
    const int x = 30634;
    std::cout << 30634;
    for (int i = 0; i < 30634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30634(int& val) {
    const int x = 30634;
    std::cout << 30634;
    for (int i = 0; i < 30634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30635(int& val) {
    func_in_30635(val);
    const int x = 30635;
    std::cout << 30635;
    for (int i = 0; i < 30635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30635(int& val) {
    const int x = 30635;
    std::cout << 30635;
    for (int i = 0; i < 30635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30636(int& val) {
    func_in_30636(val);
    const int x = 30636;
    std::cout << 30636;
    for (int i = 0; i < 30636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30636(int& val) {
    const int x = 30636;
    std::cout << 30636;
    for (int i = 0; i < 30636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30637(int& val) {
    func_in_30637(val);
    const int x = 30637;
    std::cout << 30637;
    for (int i = 0; i < 30637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30637(int& val) {
    const int x = 30637;
    std::cout << 30637;
    for (int i = 0; i < 30637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30638(int& val) {
    func_in_30638(val);
    const int x = 30638;
    std::cout << 30638;
    for (int i = 0; i < 30638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30638(int& val) {
    const int x = 30638;
    std::cout << 30638;
    for (int i = 0; i < 30638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30639(int& val) {
    func_in_30639(val);
    const int x = 30639;
    std::cout << 30639;
    for (int i = 0; i < 30639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30639(int& val) {
    const int x = 30639;
    std::cout << 30639;
    for (int i = 0; i < 30639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30640(int& val) {
    func_in_30640(val);
    const int x = 30640;
    std::cout << 30640;
    for (int i = 0; i < 30640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30640(int& val) {
    const int x = 30640;
    std::cout << 30640;
    for (int i = 0; i < 30640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30641(int& val) {
    func_in_30641(val);
    const int x = 30641;
    std::cout << 30641;
    for (int i = 0; i < 30641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30641(int& val) {
    const int x = 30641;
    std::cout << 30641;
    for (int i = 0; i < 30641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30642(int& val) {
    func_in_30642(val);
    const int x = 30642;
    std::cout << 30642;
    for (int i = 0; i < 30642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30642(int& val) {
    const int x = 30642;
    std::cout << 30642;
    for (int i = 0; i < 30642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30643(int& val) {
    func_in_30643(val);
    const int x = 30643;
    std::cout << 30643;
    for (int i = 0; i < 30643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30643(int& val) {
    const int x = 30643;
    std::cout << 30643;
    for (int i = 0; i < 30643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30644(int& val) {
    func_in_30644(val);
    const int x = 30644;
    std::cout << 30644;
    for (int i = 0; i < 30644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30644(int& val) {
    const int x = 30644;
    std::cout << 30644;
    for (int i = 0; i < 30644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30645(int& val) {
    func_in_30645(val);
    const int x = 30645;
    std::cout << 30645;
    for (int i = 0; i < 30645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30645(int& val) {
    const int x = 30645;
    std::cout << 30645;
    for (int i = 0; i < 30645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30646(int& val) {
    func_in_30646(val);
    const int x = 30646;
    std::cout << 30646;
    for (int i = 0; i < 30646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30646(int& val) {
    const int x = 30646;
    std::cout << 30646;
    for (int i = 0; i < 30646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30647(int& val) {
    func_in_30647(val);
    const int x = 30647;
    std::cout << 30647;
    for (int i = 0; i < 30647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30647(int& val) {
    const int x = 30647;
    std::cout << 30647;
    for (int i = 0; i < 30647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30648(int& val) {
    func_in_30648(val);
    const int x = 30648;
    std::cout << 30648;
    for (int i = 0; i < 30648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30648(int& val) {
    const int x = 30648;
    std::cout << 30648;
    for (int i = 0; i < 30648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30649(int& val) {
    func_in_30649(val);
    const int x = 30649;
    std::cout << 30649;
    for (int i = 0; i < 30649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30649(int& val) {
    const int x = 30649;
    std::cout << 30649;
    for (int i = 0; i < 30649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30650(int& val) {
    func_in_30650(val);
    const int x = 30650;
    std::cout << 30650;
    for (int i = 0; i < 30650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30650(int& val) {
    const int x = 30650;
    std::cout << 30650;
    for (int i = 0; i < 30650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30651(int& val) {
    func_in_30651(val);
    const int x = 30651;
    std::cout << 30651;
    for (int i = 0; i < 30651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30651(int& val) {
    const int x = 30651;
    std::cout << 30651;
    for (int i = 0; i < 30651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30652(int& val) {
    func_in_30652(val);
    const int x = 30652;
    std::cout << 30652;
    for (int i = 0; i < 30652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30652(int& val) {
    const int x = 30652;
    std::cout << 30652;
    for (int i = 0; i < 30652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30653(int& val) {
    func_in_30653(val);
    const int x = 30653;
    std::cout << 30653;
    for (int i = 0; i < 30653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30653(int& val) {
    const int x = 30653;
    std::cout << 30653;
    for (int i = 0; i < 30653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30654(int& val) {
    func_in_30654(val);
    const int x = 30654;
    std::cout << 30654;
    for (int i = 0; i < 30654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30654(int& val) {
    const int x = 30654;
    std::cout << 30654;
    for (int i = 0; i < 30654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30655(int& val) {
    func_in_30655(val);
    const int x = 30655;
    std::cout << 30655;
    for (int i = 0; i < 30655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30655(int& val) {
    const int x = 30655;
    std::cout << 30655;
    for (int i = 0; i < 30655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30656(int& val) {
    func_in_30656(val);
    const int x = 30656;
    std::cout << 30656;
    for (int i = 0; i < 30656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30656(int& val) {
    const int x = 30656;
    std::cout << 30656;
    for (int i = 0; i < 30656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30657(int& val) {
    func_in_30657(val);
    const int x = 30657;
    std::cout << 30657;
    for (int i = 0; i < 30657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30657(int& val) {
    const int x = 30657;
    std::cout << 30657;
    for (int i = 0; i < 30657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30658(int& val) {
    func_in_30658(val);
    const int x = 30658;
    std::cout << 30658;
    for (int i = 0; i < 30658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30658(int& val) {
    const int x = 30658;
    std::cout << 30658;
    for (int i = 0; i < 30658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30659(int& val) {
    func_in_30659(val);
    const int x = 30659;
    std::cout << 30659;
    for (int i = 0; i < 30659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30659(int& val) {
    const int x = 30659;
    std::cout << 30659;
    for (int i = 0; i < 30659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30660(int& val) {
    func_in_30660(val);
    const int x = 30660;
    std::cout << 30660;
    for (int i = 0; i < 30660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30660(int& val) {
    const int x = 30660;
    std::cout << 30660;
    for (int i = 0; i < 30660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30661(int& val) {
    func_in_30661(val);
    const int x = 30661;
    std::cout << 30661;
    for (int i = 0; i < 30661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30661(int& val) {
    const int x = 30661;
    std::cout << 30661;
    for (int i = 0; i < 30661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30662(int& val) {
    func_in_30662(val);
    const int x = 30662;
    std::cout << 30662;
    for (int i = 0; i < 30662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30662(int& val) {
    const int x = 30662;
    std::cout << 30662;
    for (int i = 0; i < 30662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30663(int& val) {
    func_in_30663(val);
    const int x = 30663;
    std::cout << 30663;
    for (int i = 0; i < 30663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30663(int& val) {
    const int x = 30663;
    std::cout << 30663;
    for (int i = 0; i < 30663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30664(int& val) {
    func_in_30664(val);
    const int x = 30664;
    std::cout << 30664;
    for (int i = 0; i < 30664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30664(int& val) {
    const int x = 30664;
    std::cout << 30664;
    for (int i = 0; i < 30664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30665(int& val) {
    func_in_30665(val);
    const int x = 30665;
    std::cout << 30665;
    for (int i = 0; i < 30665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30665(int& val) {
    const int x = 30665;
    std::cout << 30665;
    for (int i = 0; i < 30665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30666(int& val) {
    func_in_30666(val);
    const int x = 30666;
    std::cout << 30666;
    for (int i = 0; i < 30666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30666(int& val) {
    const int x = 30666;
    std::cout << 30666;
    for (int i = 0; i < 30666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30667(int& val) {
    func_in_30667(val);
    const int x = 30667;
    std::cout << 30667;
    for (int i = 0; i < 30667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30667(int& val) {
    const int x = 30667;
    std::cout << 30667;
    for (int i = 0; i < 30667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30668(int& val) {
    func_in_30668(val);
    const int x = 30668;
    std::cout << 30668;
    for (int i = 0; i < 30668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30668(int& val) {
    const int x = 30668;
    std::cout << 30668;
    for (int i = 0; i < 30668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30669(int& val) {
    func_in_30669(val);
    const int x = 30669;
    std::cout << 30669;
    for (int i = 0; i < 30669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30669(int& val) {
    const int x = 30669;
    std::cout << 30669;
    for (int i = 0; i < 30669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30670(int& val) {
    func_in_30670(val);
    const int x = 30670;
    std::cout << 30670;
    for (int i = 0; i < 30670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30670(int& val) {
    const int x = 30670;
    std::cout << 30670;
    for (int i = 0; i < 30670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30671(int& val) {
    func_in_30671(val);
    const int x = 30671;
    std::cout << 30671;
    for (int i = 0; i < 30671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30671(int& val) {
    const int x = 30671;
    std::cout << 30671;
    for (int i = 0; i < 30671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30672(int& val) {
    func_in_30672(val);
    const int x = 30672;
    std::cout << 30672;
    for (int i = 0; i < 30672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30672(int& val) {
    const int x = 30672;
    std::cout << 30672;
    for (int i = 0; i < 30672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30673(int& val) {
    func_in_30673(val);
    const int x = 30673;
    std::cout << 30673;
    for (int i = 0; i < 30673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30673(int& val) {
    const int x = 30673;
    std::cout << 30673;
    for (int i = 0; i < 30673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30674(int& val) {
    func_in_30674(val);
    const int x = 30674;
    std::cout << 30674;
    for (int i = 0; i < 30674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30674(int& val) {
    const int x = 30674;
    std::cout << 30674;
    for (int i = 0; i < 30674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30675(int& val) {
    func_in_30675(val);
    const int x = 30675;
    std::cout << 30675;
    for (int i = 0; i < 30675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30675(int& val) {
    const int x = 30675;
    std::cout << 30675;
    for (int i = 0; i < 30675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30676(int& val) {
    func_in_30676(val);
    const int x = 30676;
    std::cout << 30676;
    for (int i = 0; i < 30676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30676(int& val) {
    const int x = 30676;
    std::cout << 30676;
    for (int i = 0; i < 30676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30677(int& val) {
    func_in_30677(val);
    const int x = 30677;
    std::cout << 30677;
    for (int i = 0; i < 30677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30677(int& val) {
    const int x = 30677;
    std::cout << 30677;
    for (int i = 0; i < 30677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30678(int& val) {
    func_in_30678(val);
    const int x = 30678;
    std::cout << 30678;
    for (int i = 0; i < 30678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30678(int& val) {
    const int x = 30678;
    std::cout << 30678;
    for (int i = 0; i < 30678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30679(int& val) {
    func_in_30679(val);
    const int x = 30679;
    std::cout << 30679;
    for (int i = 0; i < 30679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30679(int& val) {
    const int x = 30679;
    std::cout << 30679;
    for (int i = 0; i < 30679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30680(int& val) {
    func_in_30680(val);
    const int x = 30680;
    std::cout << 30680;
    for (int i = 0; i < 30680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30680(int& val) {
    const int x = 30680;
    std::cout << 30680;
    for (int i = 0; i < 30680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30681(int& val) {
    func_in_30681(val);
    const int x = 30681;
    std::cout << 30681;
    for (int i = 0; i < 30681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30681(int& val) {
    const int x = 30681;
    std::cout << 30681;
    for (int i = 0; i < 30681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30682(int& val) {
    func_in_30682(val);
    const int x = 30682;
    std::cout << 30682;
    for (int i = 0; i < 30682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30682(int& val) {
    const int x = 30682;
    std::cout << 30682;
    for (int i = 0; i < 30682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30683(int& val) {
    func_in_30683(val);
    const int x = 30683;
    std::cout << 30683;
    for (int i = 0; i < 30683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30683(int& val) {
    const int x = 30683;
    std::cout << 30683;
    for (int i = 0; i < 30683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30684(int& val) {
    func_in_30684(val);
    const int x = 30684;
    std::cout << 30684;
    for (int i = 0; i < 30684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30684(int& val) {
    const int x = 30684;
    std::cout << 30684;
    for (int i = 0; i < 30684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30685(int& val) {
    func_in_30685(val);
    const int x = 30685;
    std::cout << 30685;
    for (int i = 0; i < 30685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30685(int& val) {
    const int x = 30685;
    std::cout << 30685;
    for (int i = 0; i < 30685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30686(int& val) {
    func_in_30686(val);
    const int x = 30686;
    std::cout << 30686;
    for (int i = 0; i < 30686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30686(int& val) {
    const int x = 30686;
    std::cout << 30686;
    for (int i = 0; i < 30686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30687(int& val) {
    func_in_30687(val);
    const int x = 30687;
    std::cout << 30687;
    for (int i = 0; i < 30687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30687(int& val) {
    const int x = 30687;
    std::cout << 30687;
    for (int i = 0; i < 30687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30688(int& val) {
    func_in_30688(val);
    const int x = 30688;
    std::cout << 30688;
    for (int i = 0; i < 30688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30688(int& val) {
    const int x = 30688;
    std::cout << 30688;
    for (int i = 0; i < 30688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30689(int& val) {
    func_in_30689(val);
    const int x = 30689;
    std::cout << 30689;
    for (int i = 0; i < 30689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30689(int& val) {
    const int x = 30689;
    std::cout << 30689;
    for (int i = 0; i < 30689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30690(int& val) {
    func_in_30690(val);
    const int x = 30690;
    std::cout << 30690;
    for (int i = 0; i < 30690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30690(int& val) {
    const int x = 30690;
    std::cout << 30690;
    for (int i = 0; i < 30690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30691(int& val) {
    func_in_30691(val);
    const int x = 30691;
    std::cout << 30691;
    for (int i = 0; i < 30691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30691(int& val) {
    const int x = 30691;
    std::cout << 30691;
    for (int i = 0; i < 30691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30692(int& val) {
    func_in_30692(val);
    const int x = 30692;
    std::cout << 30692;
    for (int i = 0; i < 30692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30692(int& val) {
    const int x = 30692;
    std::cout << 30692;
    for (int i = 0; i < 30692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30693(int& val) {
    func_in_30693(val);
    const int x = 30693;
    std::cout << 30693;
    for (int i = 0; i < 30693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30693(int& val) {
    const int x = 30693;
    std::cout << 30693;
    for (int i = 0; i < 30693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30694(int& val) {
    func_in_30694(val);
    const int x = 30694;
    std::cout << 30694;
    for (int i = 0; i < 30694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30694(int& val) {
    const int x = 30694;
    std::cout << 30694;
    for (int i = 0; i < 30694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30695(int& val) {
    func_in_30695(val);
    const int x = 30695;
    std::cout << 30695;
    for (int i = 0; i < 30695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30695(int& val) {
    const int x = 30695;
    std::cout << 30695;
    for (int i = 0; i < 30695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30696(int& val) {
    func_in_30696(val);
    const int x = 30696;
    std::cout << 30696;
    for (int i = 0; i < 30696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30696(int& val) {
    const int x = 30696;
    std::cout << 30696;
    for (int i = 0; i < 30696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30697(int& val) {
    func_in_30697(val);
    const int x = 30697;
    std::cout << 30697;
    for (int i = 0; i < 30697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30697(int& val) {
    const int x = 30697;
    std::cout << 30697;
    for (int i = 0; i < 30697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30698(int& val) {
    func_in_30698(val);
    const int x = 30698;
    std::cout << 30698;
    for (int i = 0; i < 30698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30698(int& val) {
    const int x = 30698;
    std::cout << 30698;
    for (int i = 0; i < 30698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30699(int& val) {
    func_in_30699(val);
    const int x = 30699;
    std::cout << 30699;
    for (int i = 0; i < 30699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30699(int& val) {
    const int x = 30699;
    std::cout << 30699;
    for (int i = 0; i < 30699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30700(int& val) {
    func_in_30700(val);
    const int x = 30700;
    std::cout << 30700;
    for (int i = 0; i < 30700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30700(int& val) {
    const int x = 30700;
    std::cout << 30700;
    for (int i = 0; i < 30700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30701(int& val) {
    func_in_30701(val);
    const int x = 30701;
    std::cout << 30701;
    for (int i = 0; i < 30701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30701(int& val) {
    const int x = 30701;
    std::cout << 30701;
    for (int i = 0; i < 30701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30702(int& val) {
    func_in_30702(val);
    const int x = 30702;
    std::cout << 30702;
    for (int i = 0; i < 30702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30702(int& val) {
    const int x = 30702;
    std::cout << 30702;
    for (int i = 0; i < 30702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30703(int& val) {
    func_in_30703(val);
    const int x = 30703;
    std::cout << 30703;
    for (int i = 0; i < 30703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30703(int& val) {
    const int x = 30703;
    std::cout << 30703;
    for (int i = 0; i < 30703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30704(int& val) {
    func_in_30704(val);
    const int x = 30704;
    std::cout << 30704;
    for (int i = 0; i < 30704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30704(int& val) {
    const int x = 30704;
    std::cout << 30704;
    for (int i = 0; i < 30704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30705(int& val) {
    func_in_30705(val);
    const int x = 30705;
    std::cout << 30705;
    for (int i = 0; i < 30705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30705(int& val) {
    const int x = 30705;
    std::cout << 30705;
    for (int i = 0; i < 30705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30706(int& val) {
    func_in_30706(val);
    const int x = 30706;
    std::cout << 30706;
    for (int i = 0; i < 30706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30706(int& val) {
    const int x = 30706;
    std::cout << 30706;
    for (int i = 0; i < 30706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30707(int& val) {
    func_in_30707(val);
    const int x = 30707;
    std::cout << 30707;
    for (int i = 0; i < 30707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30707(int& val) {
    const int x = 30707;
    std::cout << 30707;
    for (int i = 0; i < 30707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30708(int& val) {
    func_in_30708(val);
    const int x = 30708;
    std::cout << 30708;
    for (int i = 0; i < 30708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30708(int& val) {
    const int x = 30708;
    std::cout << 30708;
    for (int i = 0; i < 30708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30709(int& val) {
    func_in_30709(val);
    const int x = 30709;
    std::cout << 30709;
    for (int i = 0; i < 30709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30709(int& val) {
    const int x = 30709;
    std::cout << 30709;
    for (int i = 0; i < 30709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30710(int& val) {
    func_in_30710(val);
    const int x = 30710;
    std::cout << 30710;
    for (int i = 0; i < 30710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30710(int& val) {
    const int x = 30710;
    std::cout << 30710;
    for (int i = 0; i < 30710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30711(int& val) {
    func_in_30711(val);
    const int x = 30711;
    std::cout << 30711;
    for (int i = 0; i < 30711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30711(int& val) {
    const int x = 30711;
    std::cout << 30711;
    for (int i = 0; i < 30711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30712(int& val) {
    func_in_30712(val);
    const int x = 30712;
    std::cout << 30712;
    for (int i = 0; i < 30712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30712(int& val) {
    const int x = 30712;
    std::cout << 30712;
    for (int i = 0; i < 30712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30713(int& val) {
    func_in_30713(val);
    const int x = 30713;
    std::cout << 30713;
    for (int i = 0; i < 30713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30713(int& val) {
    const int x = 30713;
    std::cout << 30713;
    for (int i = 0; i < 30713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30714(int& val) {
    func_in_30714(val);
    const int x = 30714;
    std::cout << 30714;
    for (int i = 0; i < 30714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30714(int& val) {
    const int x = 30714;
    std::cout << 30714;
    for (int i = 0; i < 30714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30715(int& val) {
    func_in_30715(val);
    const int x = 30715;
    std::cout << 30715;
    for (int i = 0; i < 30715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30715(int& val) {
    const int x = 30715;
    std::cout << 30715;
    for (int i = 0; i < 30715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30716(int& val) {
    func_in_30716(val);
    const int x = 30716;
    std::cout << 30716;
    for (int i = 0; i < 30716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30716(int& val) {
    const int x = 30716;
    std::cout << 30716;
    for (int i = 0; i < 30716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30717(int& val) {
    func_in_30717(val);
    const int x = 30717;
    std::cout << 30717;
    for (int i = 0; i < 30717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30717(int& val) {
    const int x = 30717;
    std::cout << 30717;
    for (int i = 0; i < 30717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30718(int& val) {
    func_in_30718(val);
    const int x = 30718;
    std::cout << 30718;
    for (int i = 0; i < 30718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30718(int& val) {
    const int x = 30718;
    std::cout << 30718;
    for (int i = 0; i < 30718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30719(int& val) {
    func_in_30719(val);
    const int x = 30719;
    std::cout << 30719;
    for (int i = 0; i < 30719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30719(int& val) {
    const int x = 30719;
    std::cout << 30719;
    for (int i = 0; i < 30719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30720(int& val) {
    func_in_30720(val);
    const int x = 30720;
    std::cout << 30720;
    for (int i = 0; i < 30720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30720(int& val) {
    const int x = 30720;
    std::cout << 30720;
    for (int i = 0; i < 30720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30721(int& val) {
    func_in_30721(val);
    const int x = 30721;
    std::cout << 30721;
    for (int i = 0; i < 30721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30721(int& val) {
    const int x = 30721;
    std::cout << 30721;
    for (int i = 0; i < 30721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30722(int& val) {
    func_in_30722(val);
    const int x = 30722;
    std::cout << 30722;
    for (int i = 0; i < 30722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30722(int& val) {
    const int x = 30722;
    std::cout << 30722;
    for (int i = 0; i < 30722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30723(int& val) {
    func_in_30723(val);
    const int x = 30723;
    std::cout << 30723;
    for (int i = 0; i < 30723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30723(int& val) {
    const int x = 30723;
    std::cout << 30723;
    for (int i = 0; i < 30723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30724(int& val) {
    func_in_30724(val);
    const int x = 30724;
    std::cout << 30724;
    for (int i = 0; i < 30724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30724(int& val) {
    const int x = 30724;
    std::cout << 30724;
    for (int i = 0; i < 30724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30725(int& val) {
    func_in_30725(val);
    const int x = 30725;
    std::cout << 30725;
    for (int i = 0; i < 30725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30725(int& val) {
    const int x = 30725;
    std::cout << 30725;
    for (int i = 0; i < 30725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30726(int& val) {
    func_in_30726(val);
    const int x = 30726;
    std::cout << 30726;
    for (int i = 0; i < 30726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30726(int& val) {
    const int x = 30726;
    std::cout << 30726;
    for (int i = 0; i < 30726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30727(int& val) {
    func_in_30727(val);
    const int x = 30727;
    std::cout << 30727;
    for (int i = 0; i < 30727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30727(int& val) {
    const int x = 30727;
    std::cout << 30727;
    for (int i = 0; i < 30727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30728(int& val) {
    func_in_30728(val);
    const int x = 30728;
    std::cout << 30728;
    for (int i = 0; i < 30728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30728(int& val) {
    const int x = 30728;
    std::cout << 30728;
    for (int i = 0; i < 30728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30729(int& val) {
    func_in_30729(val);
    const int x = 30729;
    std::cout << 30729;
    for (int i = 0; i < 30729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30729(int& val) {
    const int x = 30729;
    std::cout << 30729;
    for (int i = 0; i < 30729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30730(int& val) {
    func_in_30730(val);
    const int x = 30730;
    std::cout << 30730;
    for (int i = 0; i < 30730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30730(int& val) {
    const int x = 30730;
    std::cout << 30730;
    for (int i = 0; i < 30730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30731(int& val) {
    func_in_30731(val);
    const int x = 30731;
    std::cout << 30731;
    for (int i = 0; i < 30731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30731(int& val) {
    const int x = 30731;
    std::cout << 30731;
    for (int i = 0; i < 30731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30732(int& val) {
    func_in_30732(val);
    const int x = 30732;
    std::cout << 30732;
    for (int i = 0; i < 30732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30732(int& val) {
    const int x = 30732;
    std::cout << 30732;
    for (int i = 0; i < 30732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30733(int& val) {
    func_in_30733(val);
    const int x = 30733;
    std::cout << 30733;
    for (int i = 0; i < 30733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30733(int& val) {
    const int x = 30733;
    std::cout << 30733;
    for (int i = 0; i < 30733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30734(int& val) {
    func_in_30734(val);
    const int x = 30734;
    std::cout << 30734;
    for (int i = 0; i < 30734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30734(int& val) {
    const int x = 30734;
    std::cout << 30734;
    for (int i = 0; i < 30734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30735(int& val) {
    func_in_30735(val);
    const int x = 30735;
    std::cout << 30735;
    for (int i = 0; i < 30735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30735(int& val) {
    const int x = 30735;
    std::cout << 30735;
    for (int i = 0; i < 30735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30736(int& val) {
    func_in_30736(val);
    const int x = 30736;
    std::cout << 30736;
    for (int i = 0; i < 30736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30736(int& val) {
    const int x = 30736;
    std::cout << 30736;
    for (int i = 0; i < 30736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30737(int& val) {
    func_in_30737(val);
    const int x = 30737;
    std::cout << 30737;
    for (int i = 0; i < 30737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30737(int& val) {
    const int x = 30737;
    std::cout << 30737;
    for (int i = 0; i < 30737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30738(int& val) {
    func_in_30738(val);
    const int x = 30738;
    std::cout << 30738;
    for (int i = 0; i < 30738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30738(int& val) {
    const int x = 30738;
    std::cout << 30738;
    for (int i = 0; i < 30738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30739(int& val) {
    func_in_30739(val);
    const int x = 30739;
    std::cout << 30739;
    for (int i = 0; i < 30739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30739(int& val) {
    const int x = 30739;
    std::cout << 30739;
    for (int i = 0; i < 30739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30740(int& val) {
    func_in_30740(val);
    const int x = 30740;
    std::cout << 30740;
    for (int i = 0; i < 30740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30740(int& val) {
    const int x = 30740;
    std::cout << 30740;
    for (int i = 0; i < 30740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30741(int& val) {
    func_in_30741(val);
    const int x = 30741;
    std::cout << 30741;
    for (int i = 0; i < 30741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30741(int& val) {
    const int x = 30741;
    std::cout << 30741;
    for (int i = 0; i < 30741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30742(int& val) {
    func_in_30742(val);
    const int x = 30742;
    std::cout << 30742;
    for (int i = 0; i < 30742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30742(int& val) {
    const int x = 30742;
    std::cout << 30742;
    for (int i = 0; i < 30742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30743(int& val) {
    func_in_30743(val);
    const int x = 30743;
    std::cout << 30743;
    for (int i = 0; i < 30743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30743(int& val) {
    const int x = 30743;
    std::cout << 30743;
    for (int i = 0; i < 30743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30744(int& val) {
    func_in_30744(val);
    const int x = 30744;
    std::cout << 30744;
    for (int i = 0; i < 30744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30744(int& val) {
    const int x = 30744;
    std::cout << 30744;
    for (int i = 0; i < 30744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30745(int& val) {
    func_in_30745(val);
    const int x = 30745;
    std::cout << 30745;
    for (int i = 0; i < 30745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30745(int& val) {
    const int x = 30745;
    std::cout << 30745;
    for (int i = 0; i < 30745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30746(int& val) {
    func_in_30746(val);
    const int x = 30746;
    std::cout << 30746;
    for (int i = 0; i < 30746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30746(int& val) {
    const int x = 30746;
    std::cout << 30746;
    for (int i = 0; i < 30746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30747(int& val) {
    func_in_30747(val);
    const int x = 30747;
    std::cout << 30747;
    for (int i = 0; i < 30747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30747(int& val) {
    const int x = 30747;
    std::cout << 30747;
    for (int i = 0; i < 30747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30748(int& val) {
    func_in_30748(val);
    const int x = 30748;
    std::cout << 30748;
    for (int i = 0; i < 30748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30748(int& val) {
    const int x = 30748;
    std::cout << 30748;
    for (int i = 0; i < 30748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30749(int& val) {
    func_in_30749(val);
    const int x = 30749;
    std::cout << 30749;
    for (int i = 0; i < 30749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30749(int& val) {
    const int x = 30749;
    std::cout << 30749;
    for (int i = 0; i < 30749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30750(int& val) {
    func_in_30750(val);
    const int x = 30750;
    std::cout << 30750;
    for (int i = 0; i < 30750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30750(int& val) {
    const int x = 30750;
    std::cout << 30750;
    for (int i = 0; i < 30750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30751(int& val) {
    func_in_30751(val);
    const int x = 30751;
    std::cout << 30751;
    for (int i = 0; i < 30751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30751(int& val) {
    const int x = 30751;
    std::cout << 30751;
    for (int i = 0; i < 30751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30752(int& val) {
    func_in_30752(val);
    const int x = 30752;
    std::cout << 30752;
    for (int i = 0; i < 30752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30752(int& val) {
    const int x = 30752;
    std::cout << 30752;
    for (int i = 0; i < 30752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30753(int& val) {
    func_in_30753(val);
    const int x = 30753;
    std::cout << 30753;
    for (int i = 0; i < 30753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30753(int& val) {
    const int x = 30753;
    std::cout << 30753;
    for (int i = 0; i < 30753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30754(int& val) {
    func_in_30754(val);
    const int x = 30754;
    std::cout << 30754;
    for (int i = 0; i < 30754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30754(int& val) {
    const int x = 30754;
    std::cout << 30754;
    for (int i = 0; i < 30754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30755(int& val) {
    func_in_30755(val);
    const int x = 30755;
    std::cout << 30755;
    for (int i = 0; i < 30755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30755(int& val) {
    const int x = 30755;
    std::cout << 30755;
    for (int i = 0; i < 30755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30756(int& val) {
    func_in_30756(val);
    const int x = 30756;
    std::cout << 30756;
    for (int i = 0; i < 30756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30756(int& val) {
    const int x = 30756;
    std::cout << 30756;
    for (int i = 0; i < 30756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30757(int& val) {
    func_in_30757(val);
    const int x = 30757;
    std::cout << 30757;
    for (int i = 0; i < 30757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30757(int& val) {
    const int x = 30757;
    std::cout << 30757;
    for (int i = 0; i < 30757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30758(int& val) {
    func_in_30758(val);
    const int x = 30758;
    std::cout << 30758;
    for (int i = 0; i < 30758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30758(int& val) {
    const int x = 30758;
    std::cout << 30758;
    for (int i = 0; i < 30758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30759(int& val) {
    func_in_30759(val);
    const int x = 30759;
    std::cout << 30759;
    for (int i = 0; i < 30759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30759(int& val) {
    const int x = 30759;
    std::cout << 30759;
    for (int i = 0; i < 30759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30760(int& val) {
    func_in_30760(val);
    const int x = 30760;
    std::cout << 30760;
    for (int i = 0; i < 30760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30760(int& val) {
    const int x = 30760;
    std::cout << 30760;
    for (int i = 0; i < 30760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30761(int& val) {
    func_in_30761(val);
    const int x = 30761;
    std::cout << 30761;
    for (int i = 0; i < 30761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30761(int& val) {
    const int x = 30761;
    std::cout << 30761;
    for (int i = 0; i < 30761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30762(int& val) {
    func_in_30762(val);
    const int x = 30762;
    std::cout << 30762;
    for (int i = 0; i < 30762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30762(int& val) {
    const int x = 30762;
    std::cout << 30762;
    for (int i = 0; i < 30762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30763(int& val) {
    func_in_30763(val);
    const int x = 30763;
    std::cout << 30763;
    for (int i = 0; i < 30763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30763(int& val) {
    const int x = 30763;
    std::cout << 30763;
    for (int i = 0; i < 30763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30764(int& val) {
    func_in_30764(val);
    const int x = 30764;
    std::cout << 30764;
    for (int i = 0; i < 30764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30764(int& val) {
    const int x = 30764;
    std::cout << 30764;
    for (int i = 0; i < 30764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30765(int& val) {
    func_in_30765(val);
    const int x = 30765;
    std::cout << 30765;
    for (int i = 0; i < 30765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30765(int& val) {
    const int x = 30765;
    std::cout << 30765;
    for (int i = 0; i < 30765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30766(int& val) {
    func_in_30766(val);
    const int x = 30766;
    std::cout << 30766;
    for (int i = 0; i < 30766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30766(int& val) {
    const int x = 30766;
    std::cout << 30766;
    for (int i = 0; i < 30766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30767(int& val) {
    func_in_30767(val);
    const int x = 30767;
    std::cout << 30767;
    for (int i = 0; i < 30767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30767(int& val) {
    const int x = 30767;
    std::cout << 30767;
    for (int i = 0; i < 30767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30768(int& val) {
    func_in_30768(val);
    const int x = 30768;
    std::cout << 30768;
    for (int i = 0; i < 30768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30768(int& val) {
    const int x = 30768;
    std::cout << 30768;
    for (int i = 0; i < 30768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30769(int& val) {
    func_in_30769(val);
    const int x = 30769;
    std::cout << 30769;
    for (int i = 0; i < 30769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30769(int& val) {
    const int x = 30769;
    std::cout << 30769;
    for (int i = 0; i < 30769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30770(int& val) {
    func_in_30770(val);
    const int x = 30770;
    std::cout << 30770;
    for (int i = 0; i < 30770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30770(int& val) {
    const int x = 30770;
    std::cout << 30770;
    for (int i = 0; i < 30770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30771(int& val) {
    func_in_30771(val);
    const int x = 30771;
    std::cout << 30771;
    for (int i = 0; i < 30771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30771(int& val) {
    const int x = 30771;
    std::cout << 30771;
    for (int i = 0; i < 30771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30772(int& val) {
    func_in_30772(val);
    const int x = 30772;
    std::cout << 30772;
    for (int i = 0; i < 30772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30772(int& val) {
    const int x = 30772;
    std::cout << 30772;
    for (int i = 0; i < 30772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30773(int& val) {
    func_in_30773(val);
    const int x = 30773;
    std::cout << 30773;
    for (int i = 0; i < 30773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30773(int& val) {
    const int x = 30773;
    std::cout << 30773;
    for (int i = 0; i < 30773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30774(int& val) {
    func_in_30774(val);
    const int x = 30774;
    std::cout << 30774;
    for (int i = 0; i < 30774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30774(int& val) {
    const int x = 30774;
    std::cout << 30774;
    for (int i = 0; i < 30774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30775(int& val) {
    func_in_30775(val);
    const int x = 30775;
    std::cout << 30775;
    for (int i = 0; i < 30775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30775(int& val) {
    const int x = 30775;
    std::cout << 30775;
    for (int i = 0; i < 30775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30776(int& val) {
    func_in_30776(val);
    const int x = 30776;
    std::cout << 30776;
    for (int i = 0; i < 30776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30776(int& val) {
    const int x = 30776;
    std::cout << 30776;
    for (int i = 0; i < 30776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30777(int& val) {
    func_in_30777(val);
    const int x = 30777;
    std::cout << 30777;
    for (int i = 0; i < 30777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30777(int& val) {
    const int x = 30777;
    std::cout << 30777;
    for (int i = 0; i < 30777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30778(int& val) {
    func_in_30778(val);
    const int x = 30778;
    std::cout << 30778;
    for (int i = 0; i < 30778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30778(int& val) {
    const int x = 30778;
    std::cout << 30778;
    for (int i = 0; i < 30778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30779(int& val) {
    func_in_30779(val);
    const int x = 30779;
    std::cout << 30779;
    for (int i = 0; i < 30779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30779(int& val) {
    const int x = 30779;
    std::cout << 30779;
    for (int i = 0; i < 30779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30780(int& val) {
    func_in_30780(val);
    const int x = 30780;
    std::cout << 30780;
    for (int i = 0; i < 30780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30780(int& val) {
    const int x = 30780;
    std::cout << 30780;
    for (int i = 0; i < 30780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30781(int& val) {
    func_in_30781(val);
    const int x = 30781;
    std::cout << 30781;
    for (int i = 0; i < 30781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30781(int& val) {
    const int x = 30781;
    std::cout << 30781;
    for (int i = 0; i < 30781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30782(int& val) {
    func_in_30782(val);
    const int x = 30782;
    std::cout << 30782;
    for (int i = 0; i < 30782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30782(int& val) {
    const int x = 30782;
    std::cout << 30782;
    for (int i = 0; i < 30782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30783(int& val) {
    func_in_30783(val);
    const int x = 30783;
    std::cout << 30783;
    for (int i = 0; i < 30783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30783(int& val) {
    const int x = 30783;
    std::cout << 30783;
    for (int i = 0; i < 30783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30784(int& val) {
    func_in_30784(val);
    const int x = 30784;
    std::cout << 30784;
    for (int i = 0; i < 30784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30784(int& val) {
    const int x = 30784;
    std::cout << 30784;
    for (int i = 0; i < 30784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30785(int& val) {
    func_in_30785(val);
    const int x = 30785;
    std::cout << 30785;
    for (int i = 0; i < 30785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30785(int& val) {
    const int x = 30785;
    std::cout << 30785;
    for (int i = 0; i < 30785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30786(int& val) {
    func_in_30786(val);
    const int x = 30786;
    std::cout << 30786;
    for (int i = 0; i < 30786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30786(int& val) {
    const int x = 30786;
    std::cout << 30786;
    for (int i = 0; i < 30786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30787(int& val) {
    func_in_30787(val);
    const int x = 30787;
    std::cout << 30787;
    for (int i = 0; i < 30787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30787(int& val) {
    const int x = 30787;
    std::cout << 30787;
    for (int i = 0; i < 30787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30788(int& val) {
    func_in_30788(val);
    const int x = 30788;
    std::cout << 30788;
    for (int i = 0; i < 30788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30788(int& val) {
    const int x = 30788;
    std::cout << 30788;
    for (int i = 0; i < 30788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30789(int& val) {
    func_in_30789(val);
    const int x = 30789;
    std::cout << 30789;
    for (int i = 0; i < 30789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30789(int& val) {
    const int x = 30789;
    std::cout << 30789;
    for (int i = 0; i < 30789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30790(int& val) {
    func_in_30790(val);
    const int x = 30790;
    std::cout << 30790;
    for (int i = 0; i < 30790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30790(int& val) {
    const int x = 30790;
    std::cout << 30790;
    for (int i = 0; i < 30790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30791(int& val) {
    func_in_30791(val);
    const int x = 30791;
    std::cout << 30791;
    for (int i = 0; i < 30791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30791(int& val) {
    const int x = 30791;
    std::cout << 30791;
    for (int i = 0; i < 30791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30792(int& val) {
    func_in_30792(val);
    const int x = 30792;
    std::cout << 30792;
    for (int i = 0; i < 30792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30792(int& val) {
    const int x = 30792;
    std::cout << 30792;
    for (int i = 0; i < 30792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30793(int& val) {
    func_in_30793(val);
    const int x = 30793;
    std::cout << 30793;
    for (int i = 0; i < 30793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30793(int& val) {
    const int x = 30793;
    std::cout << 30793;
    for (int i = 0; i < 30793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30794(int& val) {
    func_in_30794(val);
    const int x = 30794;
    std::cout << 30794;
    for (int i = 0; i < 30794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30794(int& val) {
    const int x = 30794;
    std::cout << 30794;
    for (int i = 0; i < 30794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30795(int& val) {
    func_in_30795(val);
    const int x = 30795;
    std::cout << 30795;
    for (int i = 0; i < 30795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30795(int& val) {
    const int x = 30795;
    std::cout << 30795;
    for (int i = 0; i < 30795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30796(int& val) {
    func_in_30796(val);
    const int x = 30796;
    std::cout << 30796;
    for (int i = 0; i < 30796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30796(int& val) {
    const int x = 30796;
    std::cout << 30796;
    for (int i = 0; i < 30796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30797(int& val) {
    func_in_30797(val);
    const int x = 30797;
    std::cout << 30797;
    for (int i = 0; i < 30797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30797(int& val) {
    const int x = 30797;
    std::cout << 30797;
    for (int i = 0; i < 30797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30798(int& val) {
    func_in_30798(val);
    const int x = 30798;
    std::cout << 30798;
    for (int i = 0; i < 30798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30798(int& val) {
    const int x = 30798;
    std::cout << 30798;
    for (int i = 0; i < 30798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30799(int& val) {
    func_in_30799(val);
    const int x = 30799;
    std::cout << 30799;
    for (int i = 0; i < 30799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30799(int& val) {
    const int x = 30799;
    std::cout << 30799;
    for (int i = 0; i < 30799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30800(int& val) {
    func_in_30800(val);
    const int x = 30800;
    std::cout << 30800;
    for (int i = 0; i < 30800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30800(int& val) {
    const int x = 30800;
    std::cout << 30800;
    for (int i = 0; i < 30800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30801(int& val) {
    func_in_30801(val);
    const int x = 30801;
    std::cout << 30801;
    for (int i = 0; i < 30801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30801(int& val) {
    const int x = 30801;
    std::cout << 30801;
    for (int i = 0; i < 30801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30802(int& val) {
    func_in_30802(val);
    const int x = 30802;
    std::cout << 30802;
    for (int i = 0; i < 30802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30802(int& val) {
    const int x = 30802;
    std::cout << 30802;
    for (int i = 0; i < 30802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30803(int& val) {
    func_in_30803(val);
    const int x = 30803;
    std::cout << 30803;
    for (int i = 0; i < 30803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30803(int& val) {
    const int x = 30803;
    std::cout << 30803;
    for (int i = 0; i < 30803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30804(int& val) {
    func_in_30804(val);
    const int x = 30804;
    std::cout << 30804;
    for (int i = 0; i < 30804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30804(int& val) {
    const int x = 30804;
    std::cout << 30804;
    for (int i = 0; i < 30804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30805(int& val) {
    func_in_30805(val);
    const int x = 30805;
    std::cout << 30805;
    for (int i = 0; i < 30805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30805(int& val) {
    const int x = 30805;
    std::cout << 30805;
    for (int i = 0; i < 30805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30806(int& val) {
    func_in_30806(val);
    const int x = 30806;
    std::cout << 30806;
    for (int i = 0; i < 30806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30806(int& val) {
    const int x = 30806;
    std::cout << 30806;
    for (int i = 0; i < 30806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30807(int& val) {
    func_in_30807(val);
    const int x = 30807;
    std::cout << 30807;
    for (int i = 0; i < 30807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30807(int& val) {
    const int x = 30807;
    std::cout << 30807;
    for (int i = 0; i < 30807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30808(int& val) {
    func_in_30808(val);
    const int x = 30808;
    std::cout << 30808;
    for (int i = 0; i < 30808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30808(int& val) {
    const int x = 30808;
    std::cout << 30808;
    for (int i = 0; i < 30808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30809(int& val) {
    func_in_30809(val);
    const int x = 30809;
    std::cout << 30809;
    for (int i = 0; i < 30809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30809(int& val) {
    const int x = 30809;
    std::cout << 30809;
    for (int i = 0; i < 30809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30810(int& val) {
    func_in_30810(val);
    const int x = 30810;
    std::cout << 30810;
    for (int i = 0; i < 30810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30810(int& val) {
    const int x = 30810;
    std::cout << 30810;
    for (int i = 0; i < 30810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30811(int& val) {
    func_in_30811(val);
    const int x = 30811;
    std::cout << 30811;
    for (int i = 0; i < 30811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30811(int& val) {
    const int x = 30811;
    std::cout << 30811;
    for (int i = 0; i < 30811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30812(int& val) {
    func_in_30812(val);
    const int x = 30812;
    std::cout << 30812;
    for (int i = 0; i < 30812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30812(int& val) {
    const int x = 30812;
    std::cout << 30812;
    for (int i = 0; i < 30812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30813(int& val) {
    func_in_30813(val);
    const int x = 30813;
    std::cout << 30813;
    for (int i = 0; i < 30813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30813(int& val) {
    const int x = 30813;
    std::cout << 30813;
    for (int i = 0; i < 30813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30814(int& val) {
    func_in_30814(val);
    const int x = 30814;
    std::cout << 30814;
    for (int i = 0; i < 30814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30814(int& val) {
    const int x = 30814;
    std::cout << 30814;
    for (int i = 0; i < 30814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30815(int& val) {
    func_in_30815(val);
    const int x = 30815;
    std::cout << 30815;
    for (int i = 0; i < 30815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30815(int& val) {
    const int x = 30815;
    std::cout << 30815;
    for (int i = 0; i < 30815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30816(int& val) {
    func_in_30816(val);
    const int x = 30816;
    std::cout << 30816;
    for (int i = 0; i < 30816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30816(int& val) {
    const int x = 30816;
    std::cout << 30816;
    for (int i = 0; i < 30816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30817(int& val) {
    func_in_30817(val);
    const int x = 30817;
    std::cout << 30817;
    for (int i = 0; i < 30817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30817(int& val) {
    const int x = 30817;
    std::cout << 30817;
    for (int i = 0; i < 30817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30818(int& val) {
    func_in_30818(val);
    const int x = 30818;
    std::cout << 30818;
    for (int i = 0; i < 30818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30818(int& val) {
    const int x = 30818;
    std::cout << 30818;
    for (int i = 0; i < 30818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30819(int& val) {
    func_in_30819(val);
    const int x = 30819;
    std::cout << 30819;
    for (int i = 0; i < 30819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30819(int& val) {
    const int x = 30819;
    std::cout << 30819;
    for (int i = 0; i < 30819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30820(int& val) {
    func_in_30820(val);
    const int x = 30820;
    std::cout << 30820;
    for (int i = 0; i < 30820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30820(int& val) {
    const int x = 30820;
    std::cout << 30820;
    for (int i = 0; i < 30820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30821(int& val) {
    func_in_30821(val);
    const int x = 30821;
    std::cout << 30821;
    for (int i = 0; i < 30821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30821(int& val) {
    const int x = 30821;
    std::cout << 30821;
    for (int i = 0; i < 30821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30822(int& val) {
    func_in_30822(val);
    const int x = 30822;
    std::cout << 30822;
    for (int i = 0; i < 30822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30822(int& val) {
    const int x = 30822;
    std::cout << 30822;
    for (int i = 0; i < 30822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30823(int& val) {
    func_in_30823(val);
    const int x = 30823;
    std::cout << 30823;
    for (int i = 0; i < 30823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30823(int& val) {
    const int x = 30823;
    std::cout << 30823;
    for (int i = 0; i < 30823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30824(int& val) {
    func_in_30824(val);
    const int x = 30824;
    std::cout << 30824;
    for (int i = 0; i < 30824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30824(int& val) {
    const int x = 30824;
    std::cout << 30824;
    for (int i = 0; i < 30824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30825(int& val) {
    func_in_30825(val);
    const int x = 30825;
    std::cout << 30825;
    for (int i = 0; i < 30825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30825(int& val) {
    const int x = 30825;
    std::cout << 30825;
    for (int i = 0; i < 30825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30826(int& val) {
    func_in_30826(val);
    const int x = 30826;
    std::cout << 30826;
    for (int i = 0; i < 30826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30826(int& val) {
    const int x = 30826;
    std::cout << 30826;
    for (int i = 0; i < 30826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30827(int& val) {
    func_in_30827(val);
    const int x = 30827;
    std::cout << 30827;
    for (int i = 0; i < 30827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30827(int& val) {
    const int x = 30827;
    std::cout << 30827;
    for (int i = 0; i < 30827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30828(int& val) {
    func_in_30828(val);
    const int x = 30828;
    std::cout << 30828;
    for (int i = 0; i < 30828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30828(int& val) {
    const int x = 30828;
    std::cout << 30828;
    for (int i = 0; i < 30828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30829(int& val) {
    func_in_30829(val);
    const int x = 30829;
    std::cout << 30829;
    for (int i = 0; i < 30829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30829(int& val) {
    const int x = 30829;
    std::cout << 30829;
    for (int i = 0; i < 30829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30830(int& val) {
    func_in_30830(val);
    const int x = 30830;
    std::cout << 30830;
    for (int i = 0; i < 30830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30830(int& val) {
    const int x = 30830;
    std::cout << 30830;
    for (int i = 0; i < 30830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30831(int& val) {
    func_in_30831(val);
    const int x = 30831;
    std::cout << 30831;
    for (int i = 0; i < 30831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30831(int& val) {
    const int x = 30831;
    std::cout << 30831;
    for (int i = 0; i < 30831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30832(int& val) {
    func_in_30832(val);
    const int x = 30832;
    std::cout << 30832;
    for (int i = 0; i < 30832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30832(int& val) {
    const int x = 30832;
    std::cout << 30832;
    for (int i = 0; i < 30832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30833(int& val) {
    func_in_30833(val);
    const int x = 30833;
    std::cout << 30833;
    for (int i = 0; i < 30833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30833(int& val) {
    const int x = 30833;
    std::cout << 30833;
    for (int i = 0; i < 30833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30834(int& val) {
    func_in_30834(val);
    const int x = 30834;
    std::cout << 30834;
    for (int i = 0; i < 30834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30834(int& val) {
    const int x = 30834;
    std::cout << 30834;
    for (int i = 0; i < 30834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30835(int& val) {
    func_in_30835(val);
    const int x = 30835;
    std::cout << 30835;
    for (int i = 0; i < 30835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30835(int& val) {
    const int x = 30835;
    std::cout << 30835;
    for (int i = 0; i < 30835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30836(int& val) {
    func_in_30836(val);
    const int x = 30836;
    std::cout << 30836;
    for (int i = 0; i < 30836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30836(int& val) {
    const int x = 30836;
    std::cout << 30836;
    for (int i = 0; i < 30836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30837(int& val) {
    func_in_30837(val);
    const int x = 30837;
    std::cout << 30837;
    for (int i = 0; i < 30837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30837(int& val) {
    const int x = 30837;
    std::cout << 30837;
    for (int i = 0; i < 30837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30838(int& val) {
    func_in_30838(val);
    const int x = 30838;
    std::cout << 30838;
    for (int i = 0; i < 30838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30838(int& val) {
    const int x = 30838;
    std::cout << 30838;
    for (int i = 0; i < 30838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30839(int& val) {
    func_in_30839(val);
    const int x = 30839;
    std::cout << 30839;
    for (int i = 0; i < 30839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30839(int& val) {
    const int x = 30839;
    std::cout << 30839;
    for (int i = 0; i < 30839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30840(int& val) {
    func_in_30840(val);
    const int x = 30840;
    std::cout << 30840;
    for (int i = 0; i < 30840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30840(int& val) {
    const int x = 30840;
    std::cout << 30840;
    for (int i = 0; i < 30840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30841(int& val) {
    func_in_30841(val);
    const int x = 30841;
    std::cout << 30841;
    for (int i = 0; i < 30841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30841(int& val) {
    const int x = 30841;
    std::cout << 30841;
    for (int i = 0; i < 30841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30842(int& val) {
    func_in_30842(val);
    const int x = 30842;
    std::cout << 30842;
    for (int i = 0; i < 30842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30842(int& val) {
    const int x = 30842;
    std::cout << 30842;
    for (int i = 0; i < 30842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30843(int& val) {
    func_in_30843(val);
    const int x = 30843;
    std::cout << 30843;
    for (int i = 0; i < 30843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30843(int& val) {
    const int x = 30843;
    std::cout << 30843;
    for (int i = 0; i < 30843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30844(int& val) {
    func_in_30844(val);
    const int x = 30844;
    std::cout << 30844;
    for (int i = 0; i < 30844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30844(int& val) {
    const int x = 30844;
    std::cout << 30844;
    for (int i = 0; i < 30844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30845(int& val) {
    func_in_30845(val);
    const int x = 30845;
    std::cout << 30845;
    for (int i = 0; i < 30845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30845(int& val) {
    const int x = 30845;
    std::cout << 30845;
    for (int i = 0; i < 30845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30846(int& val) {
    func_in_30846(val);
    const int x = 30846;
    std::cout << 30846;
    for (int i = 0; i < 30846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30846(int& val) {
    const int x = 30846;
    std::cout << 30846;
    for (int i = 0; i < 30846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30847(int& val) {
    func_in_30847(val);
    const int x = 30847;
    std::cout << 30847;
    for (int i = 0; i < 30847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30847(int& val) {
    const int x = 30847;
    std::cout << 30847;
    for (int i = 0; i < 30847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30848(int& val) {
    func_in_30848(val);
    const int x = 30848;
    std::cout << 30848;
    for (int i = 0; i < 30848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30848(int& val) {
    const int x = 30848;
    std::cout << 30848;
    for (int i = 0; i < 30848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30849(int& val) {
    func_in_30849(val);
    const int x = 30849;
    std::cout << 30849;
    for (int i = 0; i < 30849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30849(int& val) {
    const int x = 30849;
    std::cout << 30849;
    for (int i = 0; i < 30849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30850(int& val) {
    func_in_30850(val);
    const int x = 30850;
    std::cout << 30850;
    for (int i = 0; i < 30850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30850(int& val) {
    const int x = 30850;
    std::cout << 30850;
    for (int i = 0; i < 30850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30851(int& val) {
    func_in_30851(val);
    const int x = 30851;
    std::cout << 30851;
    for (int i = 0; i < 30851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30851(int& val) {
    const int x = 30851;
    std::cout << 30851;
    for (int i = 0; i < 30851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30852(int& val) {
    func_in_30852(val);
    const int x = 30852;
    std::cout << 30852;
    for (int i = 0; i < 30852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30852(int& val) {
    const int x = 30852;
    std::cout << 30852;
    for (int i = 0; i < 30852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30853(int& val) {
    func_in_30853(val);
    const int x = 30853;
    std::cout << 30853;
    for (int i = 0; i < 30853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30853(int& val) {
    const int x = 30853;
    std::cout << 30853;
    for (int i = 0; i < 30853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30854(int& val) {
    func_in_30854(val);
    const int x = 30854;
    std::cout << 30854;
    for (int i = 0; i < 30854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30854(int& val) {
    const int x = 30854;
    std::cout << 30854;
    for (int i = 0; i < 30854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30855(int& val) {
    func_in_30855(val);
    const int x = 30855;
    std::cout << 30855;
    for (int i = 0; i < 30855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30855(int& val) {
    const int x = 30855;
    std::cout << 30855;
    for (int i = 0; i < 30855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30856(int& val) {
    func_in_30856(val);
    const int x = 30856;
    std::cout << 30856;
    for (int i = 0; i < 30856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30856(int& val) {
    const int x = 30856;
    std::cout << 30856;
    for (int i = 0; i < 30856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30857(int& val) {
    func_in_30857(val);
    const int x = 30857;
    std::cout << 30857;
    for (int i = 0; i < 30857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30857(int& val) {
    const int x = 30857;
    std::cout << 30857;
    for (int i = 0; i < 30857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30858(int& val) {
    func_in_30858(val);
    const int x = 30858;
    std::cout << 30858;
    for (int i = 0; i < 30858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30858(int& val) {
    const int x = 30858;
    std::cout << 30858;
    for (int i = 0; i < 30858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30859(int& val) {
    func_in_30859(val);
    const int x = 30859;
    std::cout << 30859;
    for (int i = 0; i < 30859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30859(int& val) {
    const int x = 30859;
    std::cout << 30859;
    for (int i = 0; i < 30859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30860(int& val) {
    func_in_30860(val);
    const int x = 30860;
    std::cout << 30860;
    for (int i = 0; i < 30860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30860(int& val) {
    const int x = 30860;
    std::cout << 30860;
    for (int i = 0; i < 30860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30861(int& val) {
    func_in_30861(val);
    const int x = 30861;
    std::cout << 30861;
    for (int i = 0; i < 30861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30861(int& val) {
    const int x = 30861;
    std::cout << 30861;
    for (int i = 0; i < 30861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30862(int& val) {
    func_in_30862(val);
    const int x = 30862;
    std::cout << 30862;
    for (int i = 0; i < 30862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30862(int& val) {
    const int x = 30862;
    std::cout << 30862;
    for (int i = 0; i < 30862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30863(int& val) {
    func_in_30863(val);
    const int x = 30863;
    std::cout << 30863;
    for (int i = 0; i < 30863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30863(int& val) {
    const int x = 30863;
    std::cout << 30863;
    for (int i = 0; i < 30863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30864(int& val) {
    func_in_30864(val);
    const int x = 30864;
    std::cout << 30864;
    for (int i = 0; i < 30864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30864(int& val) {
    const int x = 30864;
    std::cout << 30864;
    for (int i = 0; i < 30864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30865(int& val) {
    func_in_30865(val);
    const int x = 30865;
    std::cout << 30865;
    for (int i = 0; i < 30865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30865(int& val) {
    const int x = 30865;
    std::cout << 30865;
    for (int i = 0; i < 30865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30866(int& val) {
    func_in_30866(val);
    const int x = 30866;
    std::cout << 30866;
    for (int i = 0; i < 30866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30866(int& val) {
    const int x = 30866;
    std::cout << 30866;
    for (int i = 0; i < 30866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30867(int& val) {
    func_in_30867(val);
    const int x = 30867;
    std::cout << 30867;
    for (int i = 0; i < 30867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30867(int& val) {
    const int x = 30867;
    std::cout << 30867;
    for (int i = 0; i < 30867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30868(int& val) {
    func_in_30868(val);
    const int x = 30868;
    std::cout << 30868;
    for (int i = 0; i < 30868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30868(int& val) {
    const int x = 30868;
    std::cout << 30868;
    for (int i = 0; i < 30868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30869(int& val) {
    func_in_30869(val);
    const int x = 30869;
    std::cout << 30869;
    for (int i = 0; i < 30869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30869(int& val) {
    const int x = 30869;
    std::cout << 30869;
    for (int i = 0; i < 30869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30870(int& val) {
    func_in_30870(val);
    const int x = 30870;
    std::cout << 30870;
    for (int i = 0; i < 30870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30870(int& val) {
    const int x = 30870;
    std::cout << 30870;
    for (int i = 0; i < 30870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30871(int& val) {
    func_in_30871(val);
    const int x = 30871;
    std::cout << 30871;
    for (int i = 0; i < 30871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30871(int& val) {
    const int x = 30871;
    std::cout << 30871;
    for (int i = 0; i < 30871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30872(int& val) {
    func_in_30872(val);
    const int x = 30872;
    std::cout << 30872;
    for (int i = 0; i < 30872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30872(int& val) {
    const int x = 30872;
    std::cout << 30872;
    for (int i = 0; i < 30872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30873(int& val) {
    func_in_30873(val);
    const int x = 30873;
    std::cout << 30873;
    for (int i = 0; i < 30873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30873(int& val) {
    const int x = 30873;
    std::cout << 30873;
    for (int i = 0; i < 30873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30874(int& val) {
    func_in_30874(val);
    const int x = 30874;
    std::cout << 30874;
    for (int i = 0; i < 30874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30874(int& val) {
    const int x = 30874;
    std::cout << 30874;
    for (int i = 0; i < 30874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30875(int& val) {
    func_in_30875(val);
    const int x = 30875;
    std::cout << 30875;
    for (int i = 0; i < 30875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30875(int& val) {
    const int x = 30875;
    std::cout << 30875;
    for (int i = 0; i < 30875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30876(int& val) {
    func_in_30876(val);
    const int x = 30876;
    std::cout << 30876;
    for (int i = 0; i < 30876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30876(int& val) {
    const int x = 30876;
    std::cout << 30876;
    for (int i = 0; i < 30876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30877(int& val) {
    func_in_30877(val);
    const int x = 30877;
    std::cout << 30877;
    for (int i = 0; i < 30877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30877(int& val) {
    const int x = 30877;
    std::cout << 30877;
    for (int i = 0; i < 30877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30878(int& val) {
    func_in_30878(val);
    const int x = 30878;
    std::cout << 30878;
    for (int i = 0; i < 30878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30878(int& val) {
    const int x = 30878;
    std::cout << 30878;
    for (int i = 0; i < 30878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30879(int& val) {
    func_in_30879(val);
    const int x = 30879;
    std::cout << 30879;
    for (int i = 0; i < 30879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30879(int& val) {
    const int x = 30879;
    std::cout << 30879;
    for (int i = 0; i < 30879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30880(int& val) {
    func_in_30880(val);
    const int x = 30880;
    std::cout << 30880;
    for (int i = 0; i < 30880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30880(int& val) {
    const int x = 30880;
    std::cout << 30880;
    for (int i = 0; i < 30880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30881(int& val) {
    func_in_30881(val);
    const int x = 30881;
    std::cout << 30881;
    for (int i = 0; i < 30881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30881(int& val) {
    const int x = 30881;
    std::cout << 30881;
    for (int i = 0; i < 30881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30882(int& val) {
    func_in_30882(val);
    const int x = 30882;
    std::cout << 30882;
    for (int i = 0; i < 30882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30882(int& val) {
    const int x = 30882;
    std::cout << 30882;
    for (int i = 0; i < 30882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30883(int& val) {
    func_in_30883(val);
    const int x = 30883;
    std::cout << 30883;
    for (int i = 0; i < 30883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30883(int& val) {
    const int x = 30883;
    std::cout << 30883;
    for (int i = 0; i < 30883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30884(int& val) {
    func_in_30884(val);
    const int x = 30884;
    std::cout << 30884;
    for (int i = 0; i < 30884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30884(int& val) {
    const int x = 30884;
    std::cout << 30884;
    for (int i = 0; i < 30884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30885(int& val) {
    func_in_30885(val);
    const int x = 30885;
    std::cout << 30885;
    for (int i = 0; i < 30885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30885(int& val) {
    const int x = 30885;
    std::cout << 30885;
    for (int i = 0; i < 30885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30886(int& val) {
    func_in_30886(val);
    const int x = 30886;
    std::cout << 30886;
    for (int i = 0; i < 30886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30886(int& val) {
    const int x = 30886;
    std::cout << 30886;
    for (int i = 0; i < 30886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30887(int& val) {
    func_in_30887(val);
    const int x = 30887;
    std::cout << 30887;
    for (int i = 0; i < 30887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30887(int& val) {
    const int x = 30887;
    std::cout << 30887;
    for (int i = 0; i < 30887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30888(int& val) {
    func_in_30888(val);
    const int x = 30888;
    std::cout << 30888;
    for (int i = 0; i < 30888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30888(int& val) {
    const int x = 30888;
    std::cout << 30888;
    for (int i = 0; i < 30888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30889(int& val) {
    func_in_30889(val);
    const int x = 30889;
    std::cout << 30889;
    for (int i = 0; i < 30889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30889(int& val) {
    const int x = 30889;
    std::cout << 30889;
    for (int i = 0; i < 30889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30890(int& val) {
    func_in_30890(val);
    const int x = 30890;
    std::cout << 30890;
    for (int i = 0; i < 30890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30890(int& val) {
    const int x = 30890;
    std::cout << 30890;
    for (int i = 0; i < 30890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30891(int& val) {
    func_in_30891(val);
    const int x = 30891;
    std::cout << 30891;
    for (int i = 0; i < 30891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30891(int& val) {
    const int x = 30891;
    std::cout << 30891;
    for (int i = 0; i < 30891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30892(int& val) {
    func_in_30892(val);
    const int x = 30892;
    std::cout << 30892;
    for (int i = 0; i < 30892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30892(int& val) {
    const int x = 30892;
    std::cout << 30892;
    for (int i = 0; i < 30892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30893(int& val) {
    func_in_30893(val);
    const int x = 30893;
    std::cout << 30893;
    for (int i = 0; i < 30893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30893(int& val) {
    const int x = 30893;
    std::cout << 30893;
    for (int i = 0; i < 30893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30894(int& val) {
    func_in_30894(val);
    const int x = 30894;
    std::cout << 30894;
    for (int i = 0; i < 30894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30894(int& val) {
    const int x = 30894;
    std::cout << 30894;
    for (int i = 0; i < 30894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30895(int& val) {
    func_in_30895(val);
    const int x = 30895;
    std::cout << 30895;
    for (int i = 0; i < 30895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30895(int& val) {
    const int x = 30895;
    std::cout << 30895;
    for (int i = 0; i < 30895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30896(int& val) {
    func_in_30896(val);
    const int x = 30896;
    std::cout << 30896;
    for (int i = 0; i < 30896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30896(int& val) {
    const int x = 30896;
    std::cout << 30896;
    for (int i = 0; i < 30896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30897(int& val) {
    func_in_30897(val);
    const int x = 30897;
    std::cout << 30897;
    for (int i = 0; i < 30897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30897(int& val) {
    const int x = 30897;
    std::cout << 30897;
    for (int i = 0; i < 30897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30898(int& val) {
    func_in_30898(val);
    const int x = 30898;
    std::cout << 30898;
    for (int i = 0; i < 30898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30898(int& val) {
    const int x = 30898;
    std::cout << 30898;
    for (int i = 0; i < 30898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30899(int& val) {
    func_in_30899(val);
    const int x = 30899;
    std::cout << 30899;
    for (int i = 0; i < 30899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30899(int& val) {
    const int x = 30899;
    std::cout << 30899;
    for (int i = 0; i < 30899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30900(int& val) {
    func_in_30900(val);
    const int x = 30900;
    std::cout << 30900;
    for (int i = 0; i < 30900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30900(int& val) {
    const int x = 30900;
    std::cout << 30900;
    for (int i = 0; i < 30900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30901(int& val) {
    func_in_30901(val);
    const int x = 30901;
    std::cout << 30901;
    for (int i = 0; i < 30901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30901(int& val) {
    const int x = 30901;
    std::cout << 30901;
    for (int i = 0; i < 30901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30902(int& val) {
    func_in_30902(val);
    const int x = 30902;
    std::cout << 30902;
    for (int i = 0; i < 30902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30902(int& val) {
    const int x = 30902;
    std::cout << 30902;
    for (int i = 0; i < 30902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30903(int& val) {
    func_in_30903(val);
    const int x = 30903;
    std::cout << 30903;
    for (int i = 0; i < 30903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30903(int& val) {
    const int x = 30903;
    std::cout << 30903;
    for (int i = 0; i < 30903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30904(int& val) {
    func_in_30904(val);
    const int x = 30904;
    std::cout << 30904;
    for (int i = 0; i < 30904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30904(int& val) {
    const int x = 30904;
    std::cout << 30904;
    for (int i = 0; i < 30904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30905(int& val) {
    func_in_30905(val);
    const int x = 30905;
    std::cout << 30905;
    for (int i = 0; i < 30905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30905(int& val) {
    const int x = 30905;
    std::cout << 30905;
    for (int i = 0; i < 30905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30906(int& val) {
    func_in_30906(val);
    const int x = 30906;
    std::cout << 30906;
    for (int i = 0; i < 30906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30906(int& val) {
    const int x = 30906;
    std::cout << 30906;
    for (int i = 0; i < 30906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30907(int& val) {
    func_in_30907(val);
    const int x = 30907;
    std::cout << 30907;
    for (int i = 0; i < 30907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30907(int& val) {
    const int x = 30907;
    std::cout << 30907;
    for (int i = 0; i < 30907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30908(int& val) {
    func_in_30908(val);
    const int x = 30908;
    std::cout << 30908;
    for (int i = 0; i < 30908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30908(int& val) {
    const int x = 30908;
    std::cout << 30908;
    for (int i = 0; i < 30908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30909(int& val) {
    func_in_30909(val);
    const int x = 30909;
    std::cout << 30909;
    for (int i = 0; i < 30909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30909(int& val) {
    const int x = 30909;
    std::cout << 30909;
    for (int i = 0; i < 30909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30910(int& val) {
    func_in_30910(val);
    const int x = 30910;
    std::cout << 30910;
    for (int i = 0; i < 30910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30910(int& val) {
    const int x = 30910;
    std::cout << 30910;
    for (int i = 0; i < 30910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30911(int& val) {
    func_in_30911(val);
    const int x = 30911;
    std::cout << 30911;
    for (int i = 0; i < 30911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30911(int& val) {
    const int x = 30911;
    std::cout << 30911;
    for (int i = 0; i < 30911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30912(int& val) {
    func_in_30912(val);
    const int x = 30912;
    std::cout << 30912;
    for (int i = 0; i < 30912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30912(int& val) {
    const int x = 30912;
    std::cout << 30912;
    for (int i = 0; i < 30912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30913(int& val) {
    func_in_30913(val);
    const int x = 30913;
    std::cout << 30913;
    for (int i = 0; i < 30913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30913(int& val) {
    const int x = 30913;
    std::cout << 30913;
    for (int i = 0; i < 30913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30914(int& val) {
    func_in_30914(val);
    const int x = 30914;
    std::cout << 30914;
    for (int i = 0; i < 30914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30914(int& val) {
    const int x = 30914;
    std::cout << 30914;
    for (int i = 0; i < 30914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30915(int& val) {
    func_in_30915(val);
    const int x = 30915;
    std::cout << 30915;
    for (int i = 0; i < 30915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30915(int& val) {
    const int x = 30915;
    std::cout << 30915;
    for (int i = 0; i < 30915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30916(int& val) {
    func_in_30916(val);
    const int x = 30916;
    std::cout << 30916;
    for (int i = 0; i < 30916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30916(int& val) {
    const int x = 30916;
    std::cout << 30916;
    for (int i = 0; i < 30916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30917(int& val) {
    func_in_30917(val);
    const int x = 30917;
    std::cout << 30917;
    for (int i = 0; i < 30917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30917(int& val) {
    const int x = 30917;
    std::cout << 30917;
    for (int i = 0; i < 30917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30918(int& val) {
    func_in_30918(val);
    const int x = 30918;
    std::cout << 30918;
    for (int i = 0; i < 30918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30918(int& val) {
    const int x = 30918;
    std::cout << 30918;
    for (int i = 0; i < 30918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30919(int& val) {
    func_in_30919(val);
    const int x = 30919;
    std::cout << 30919;
    for (int i = 0; i < 30919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30919(int& val) {
    const int x = 30919;
    std::cout << 30919;
    for (int i = 0; i < 30919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30920(int& val) {
    func_in_30920(val);
    const int x = 30920;
    std::cout << 30920;
    for (int i = 0; i < 30920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30920(int& val) {
    const int x = 30920;
    std::cout << 30920;
    for (int i = 0; i < 30920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30921(int& val) {
    func_in_30921(val);
    const int x = 30921;
    std::cout << 30921;
    for (int i = 0; i < 30921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30921(int& val) {
    const int x = 30921;
    std::cout << 30921;
    for (int i = 0; i < 30921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30922(int& val) {
    func_in_30922(val);
    const int x = 30922;
    std::cout << 30922;
    for (int i = 0; i < 30922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30922(int& val) {
    const int x = 30922;
    std::cout << 30922;
    for (int i = 0; i < 30922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30923(int& val) {
    func_in_30923(val);
    const int x = 30923;
    std::cout << 30923;
    for (int i = 0; i < 30923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30923(int& val) {
    const int x = 30923;
    std::cout << 30923;
    for (int i = 0; i < 30923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30924(int& val) {
    func_in_30924(val);
    const int x = 30924;
    std::cout << 30924;
    for (int i = 0; i < 30924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30924(int& val) {
    const int x = 30924;
    std::cout << 30924;
    for (int i = 0; i < 30924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30925(int& val) {
    func_in_30925(val);
    const int x = 30925;
    std::cout << 30925;
    for (int i = 0; i < 30925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30925(int& val) {
    const int x = 30925;
    std::cout << 30925;
    for (int i = 0; i < 30925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30926(int& val) {
    func_in_30926(val);
    const int x = 30926;
    std::cout << 30926;
    for (int i = 0; i < 30926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30926(int& val) {
    const int x = 30926;
    std::cout << 30926;
    for (int i = 0; i < 30926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30927(int& val) {
    func_in_30927(val);
    const int x = 30927;
    std::cout << 30927;
    for (int i = 0; i < 30927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30927(int& val) {
    const int x = 30927;
    std::cout << 30927;
    for (int i = 0; i < 30927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30928(int& val) {
    func_in_30928(val);
    const int x = 30928;
    std::cout << 30928;
    for (int i = 0; i < 30928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30928(int& val) {
    const int x = 30928;
    std::cout << 30928;
    for (int i = 0; i < 30928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30929(int& val) {
    func_in_30929(val);
    const int x = 30929;
    std::cout << 30929;
    for (int i = 0; i < 30929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30929(int& val) {
    const int x = 30929;
    std::cout << 30929;
    for (int i = 0; i < 30929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30930(int& val) {
    func_in_30930(val);
    const int x = 30930;
    std::cout << 30930;
    for (int i = 0; i < 30930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30930(int& val) {
    const int x = 30930;
    std::cout << 30930;
    for (int i = 0; i < 30930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30931(int& val) {
    func_in_30931(val);
    const int x = 30931;
    std::cout << 30931;
    for (int i = 0; i < 30931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30931(int& val) {
    const int x = 30931;
    std::cout << 30931;
    for (int i = 0; i < 30931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30932(int& val) {
    func_in_30932(val);
    const int x = 30932;
    std::cout << 30932;
    for (int i = 0; i < 30932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30932(int& val) {
    const int x = 30932;
    std::cout << 30932;
    for (int i = 0; i < 30932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30933(int& val) {
    func_in_30933(val);
    const int x = 30933;
    std::cout << 30933;
    for (int i = 0; i < 30933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30933(int& val) {
    const int x = 30933;
    std::cout << 30933;
    for (int i = 0; i < 30933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30934(int& val) {
    func_in_30934(val);
    const int x = 30934;
    std::cout << 30934;
    for (int i = 0; i < 30934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30934(int& val) {
    const int x = 30934;
    std::cout << 30934;
    for (int i = 0; i < 30934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30935(int& val) {
    func_in_30935(val);
    const int x = 30935;
    std::cout << 30935;
    for (int i = 0; i < 30935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30935(int& val) {
    const int x = 30935;
    std::cout << 30935;
    for (int i = 0; i < 30935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30936(int& val) {
    func_in_30936(val);
    const int x = 30936;
    std::cout << 30936;
    for (int i = 0; i < 30936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30936(int& val) {
    const int x = 30936;
    std::cout << 30936;
    for (int i = 0; i < 30936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30937(int& val) {
    func_in_30937(val);
    const int x = 30937;
    std::cout << 30937;
    for (int i = 0; i < 30937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30937(int& val) {
    const int x = 30937;
    std::cout << 30937;
    for (int i = 0; i < 30937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30938(int& val) {
    func_in_30938(val);
    const int x = 30938;
    std::cout << 30938;
    for (int i = 0; i < 30938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30938(int& val) {
    const int x = 30938;
    std::cout << 30938;
    for (int i = 0; i < 30938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30939(int& val) {
    func_in_30939(val);
    const int x = 30939;
    std::cout << 30939;
    for (int i = 0; i < 30939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30939(int& val) {
    const int x = 30939;
    std::cout << 30939;
    for (int i = 0; i < 30939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30940(int& val) {
    func_in_30940(val);
    const int x = 30940;
    std::cout << 30940;
    for (int i = 0; i < 30940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30940(int& val) {
    const int x = 30940;
    std::cout << 30940;
    for (int i = 0; i < 30940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30941(int& val) {
    func_in_30941(val);
    const int x = 30941;
    std::cout << 30941;
    for (int i = 0; i < 30941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30941(int& val) {
    const int x = 30941;
    std::cout << 30941;
    for (int i = 0; i < 30941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30942(int& val) {
    func_in_30942(val);
    const int x = 30942;
    std::cout << 30942;
    for (int i = 0; i < 30942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30942(int& val) {
    const int x = 30942;
    std::cout << 30942;
    for (int i = 0; i < 30942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30943(int& val) {
    func_in_30943(val);
    const int x = 30943;
    std::cout << 30943;
    for (int i = 0; i < 30943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30943(int& val) {
    const int x = 30943;
    std::cout << 30943;
    for (int i = 0; i < 30943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30944(int& val) {
    func_in_30944(val);
    const int x = 30944;
    std::cout << 30944;
    for (int i = 0; i < 30944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30944(int& val) {
    const int x = 30944;
    std::cout << 30944;
    for (int i = 0; i < 30944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30945(int& val) {
    func_in_30945(val);
    const int x = 30945;
    std::cout << 30945;
    for (int i = 0; i < 30945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30945(int& val) {
    const int x = 30945;
    std::cout << 30945;
    for (int i = 0; i < 30945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30946(int& val) {
    func_in_30946(val);
    const int x = 30946;
    std::cout << 30946;
    for (int i = 0; i < 30946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30946(int& val) {
    const int x = 30946;
    std::cout << 30946;
    for (int i = 0; i < 30946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30947(int& val) {
    func_in_30947(val);
    const int x = 30947;
    std::cout << 30947;
    for (int i = 0; i < 30947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30947(int& val) {
    const int x = 30947;
    std::cout << 30947;
    for (int i = 0; i < 30947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30948(int& val) {
    func_in_30948(val);
    const int x = 30948;
    std::cout << 30948;
    for (int i = 0; i < 30948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30948(int& val) {
    const int x = 30948;
    std::cout << 30948;
    for (int i = 0; i < 30948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30949(int& val) {
    func_in_30949(val);
    const int x = 30949;
    std::cout << 30949;
    for (int i = 0; i < 30949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30949(int& val) {
    const int x = 30949;
    std::cout << 30949;
    for (int i = 0; i < 30949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30950(int& val) {
    func_in_30950(val);
    const int x = 30950;
    std::cout << 30950;
    for (int i = 0; i < 30950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30950(int& val) {
    const int x = 30950;
    std::cout << 30950;
    for (int i = 0; i < 30950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30951(int& val) {
    func_in_30951(val);
    const int x = 30951;
    std::cout << 30951;
    for (int i = 0; i < 30951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30951(int& val) {
    const int x = 30951;
    std::cout << 30951;
    for (int i = 0; i < 30951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30952(int& val) {
    func_in_30952(val);
    const int x = 30952;
    std::cout << 30952;
    for (int i = 0; i < 30952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30952(int& val) {
    const int x = 30952;
    std::cout << 30952;
    for (int i = 0; i < 30952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30953(int& val) {
    func_in_30953(val);
    const int x = 30953;
    std::cout << 30953;
    for (int i = 0; i < 30953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30953(int& val) {
    const int x = 30953;
    std::cout << 30953;
    for (int i = 0; i < 30953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30954(int& val) {
    func_in_30954(val);
    const int x = 30954;
    std::cout << 30954;
    for (int i = 0; i < 30954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30954(int& val) {
    const int x = 30954;
    std::cout << 30954;
    for (int i = 0; i < 30954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30955(int& val) {
    func_in_30955(val);
    const int x = 30955;
    std::cout << 30955;
    for (int i = 0; i < 30955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30955(int& val) {
    const int x = 30955;
    std::cout << 30955;
    for (int i = 0; i < 30955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30956(int& val) {
    func_in_30956(val);
    const int x = 30956;
    std::cout << 30956;
    for (int i = 0; i < 30956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30956(int& val) {
    const int x = 30956;
    std::cout << 30956;
    for (int i = 0; i < 30956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30957(int& val) {
    func_in_30957(val);
    const int x = 30957;
    std::cout << 30957;
    for (int i = 0; i < 30957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30957(int& val) {
    const int x = 30957;
    std::cout << 30957;
    for (int i = 0; i < 30957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30958(int& val) {
    func_in_30958(val);
    const int x = 30958;
    std::cout << 30958;
    for (int i = 0; i < 30958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30958(int& val) {
    const int x = 30958;
    std::cout << 30958;
    for (int i = 0; i < 30958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30959(int& val) {
    func_in_30959(val);
    const int x = 30959;
    std::cout << 30959;
    for (int i = 0; i < 30959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30959(int& val) {
    const int x = 30959;
    std::cout << 30959;
    for (int i = 0; i < 30959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30960(int& val) {
    func_in_30960(val);
    const int x = 30960;
    std::cout << 30960;
    for (int i = 0; i < 30960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30960(int& val) {
    const int x = 30960;
    std::cout << 30960;
    for (int i = 0; i < 30960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30961(int& val) {
    func_in_30961(val);
    const int x = 30961;
    std::cout << 30961;
    for (int i = 0; i < 30961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30961(int& val) {
    const int x = 30961;
    std::cout << 30961;
    for (int i = 0; i < 30961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30962(int& val) {
    func_in_30962(val);
    const int x = 30962;
    std::cout << 30962;
    for (int i = 0; i < 30962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30962(int& val) {
    const int x = 30962;
    std::cout << 30962;
    for (int i = 0; i < 30962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30963(int& val) {
    func_in_30963(val);
    const int x = 30963;
    std::cout << 30963;
    for (int i = 0; i < 30963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30963(int& val) {
    const int x = 30963;
    std::cout << 30963;
    for (int i = 0; i < 30963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30964(int& val) {
    func_in_30964(val);
    const int x = 30964;
    std::cout << 30964;
    for (int i = 0; i < 30964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30964(int& val) {
    const int x = 30964;
    std::cout << 30964;
    for (int i = 0; i < 30964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30965(int& val) {
    func_in_30965(val);
    const int x = 30965;
    std::cout << 30965;
    for (int i = 0; i < 30965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30965(int& val) {
    const int x = 30965;
    std::cout << 30965;
    for (int i = 0; i < 30965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30966(int& val) {
    func_in_30966(val);
    const int x = 30966;
    std::cout << 30966;
    for (int i = 0; i < 30966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30966(int& val) {
    const int x = 30966;
    std::cout << 30966;
    for (int i = 0; i < 30966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30967(int& val) {
    func_in_30967(val);
    const int x = 30967;
    std::cout << 30967;
    for (int i = 0; i < 30967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30967(int& val) {
    const int x = 30967;
    std::cout << 30967;
    for (int i = 0; i < 30967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30968(int& val) {
    func_in_30968(val);
    const int x = 30968;
    std::cout << 30968;
    for (int i = 0; i < 30968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30968(int& val) {
    const int x = 30968;
    std::cout << 30968;
    for (int i = 0; i < 30968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30969(int& val) {
    func_in_30969(val);
    const int x = 30969;
    std::cout << 30969;
    for (int i = 0; i < 30969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30969(int& val) {
    const int x = 30969;
    std::cout << 30969;
    for (int i = 0; i < 30969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30970(int& val) {
    func_in_30970(val);
    const int x = 30970;
    std::cout << 30970;
    for (int i = 0; i < 30970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30970(int& val) {
    const int x = 30970;
    std::cout << 30970;
    for (int i = 0; i < 30970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30971(int& val) {
    func_in_30971(val);
    const int x = 30971;
    std::cout << 30971;
    for (int i = 0; i < 30971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30971(int& val) {
    const int x = 30971;
    std::cout << 30971;
    for (int i = 0; i < 30971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30972(int& val) {
    func_in_30972(val);
    const int x = 30972;
    std::cout << 30972;
    for (int i = 0; i < 30972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30972(int& val) {
    const int x = 30972;
    std::cout << 30972;
    for (int i = 0; i < 30972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30973(int& val) {
    func_in_30973(val);
    const int x = 30973;
    std::cout << 30973;
    for (int i = 0; i < 30973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30973(int& val) {
    const int x = 30973;
    std::cout << 30973;
    for (int i = 0; i < 30973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30974(int& val) {
    func_in_30974(val);
    const int x = 30974;
    std::cout << 30974;
    for (int i = 0; i < 30974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30974(int& val) {
    const int x = 30974;
    std::cout << 30974;
    for (int i = 0; i < 30974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30975(int& val) {
    func_in_30975(val);
    const int x = 30975;
    std::cout << 30975;
    for (int i = 0; i < 30975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30975(int& val) {
    const int x = 30975;
    std::cout << 30975;
    for (int i = 0; i < 30975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30976(int& val) {
    func_in_30976(val);
    const int x = 30976;
    std::cout << 30976;
    for (int i = 0; i < 30976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30976(int& val) {
    const int x = 30976;
    std::cout << 30976;
    for (int i = 0; i < 30976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30977(int& val) {
    func_in_30977(val);
    const int x = 30977;
    std::cout << 30977;
    for (int i = 0; i < 30977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30977(int& val) {
    const int x = 30977;
    std::cout << 30977;
    for (int i = 0; i < 30977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30978(int& val) {
    func_in_30978(val);
    const int x = 30978;
    std::cout << 30978;
    for (int i = 0; i < 30978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30978(int& val) {
    const int x = 30978;
    std::cout << 30978;
    for (int i = 0; i < 30978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30979(int& val) {
    func_in_30979(val);
    const int x = 30979;
    std::cout << 30979;
    for (int i = 0; i < 30979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30979(int& val) {
    const int x = 30979;
    std::cout << 30979;
    for (int i = 0; i < 30979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30980(int& val) {
    func_in_30980(val);
    const int x = 30980;
    std::cout << 30980;
    for (int i = 0; i < 30980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30980(int& val) {
    const int x = 30980;
    std::cout << 30980;
    for (int i = 0; i < 30980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30981(int& val) {
    func_in_30981(val);
    const int x = 30981;
    std::cout << 30981;
    for (int i = 0; i < 30981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30981(int& val) {
    const int x = 30981;
    std::cout << 30981;
    for (int i = 0; i < 30981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30982(int& val) {
    func_in_30982(val);
    const int x = 30982;
    std::cout << 30982;
    for (int i = 0; i < 30982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30982(int& val) {
    const int x = 30982;
    std::cout << 30982;
    for (int i = 0; i < 30982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30983(int& val) {
    func_in_30983(val);
    const int x = 30983;
    std::cout << 30983;
    for (int i = 0; i < 30983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30983(int& val) {
    const int x = 30983;
    std::cout << 30983;
    for (int i = 0; i < 30983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30984(int& val) {
    func_in_30984(val);
    const int x = 30984;
    std::cout << 30984;
    for (int i = 0; i < 30984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30984(int& val) {
    const int x = 30984;
    std::cout << 30984;
    for (int i = 0; i < 30984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30985(int& val) {
    func_in_30985(val);
    const int x = 30985;
    std::cout << 30985;
    for (int i = 0; i < 30985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30985(int& val) {
    const int x = 30985;
    std::cout << 30985;
    for (int i = 0; i < 30985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30986(int& val) {
    func_in_30986(val);
    const int x = 30986;
    std::cout << 30986;
    for (int i = 0; i < 30986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30986(int& val) {
    const int x = 30986;
    std::cout << 30986;
    for (int i = 0; i < 30986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30987(int& val) {
    func_in_30987(val);
    const int x = 30987;
    std::cout << 30987;
    for (int i = 0; i < 30987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30987(int& val) {
    const int x = 30987;
    std::cout << 30987;
    for (int i = 0; i < 30987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30988(int& val) {
    func_in_30988(val);
    const int x = 30988;
    std::cout << 30988;
    for (int i = 0; i < 30988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30988(int& val) {
    const int x = 30988;
    std::cout << 30988;
    for (int i = 0; i < 30988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30989(int& val) {
    func_in_30989(val);
    const int x = 30989;
    std::cout << 30989;
    for (int i = 0; i < 30989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30989(int& val) {
    const int x = 30989;
    std::cout << 30989;
    for (int i = 0; i < 30989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30990(int& val) {
    func_in_30990(val);
    const int x = 30990;
    std::cout << 30990;
    for (int i = 0; i < 30990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30990(int& val) {
    const int x = 30990;
    std::cout << 30990;
    for (int i = 0; i < 30990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30991(int& val) {
    func_in_30991(val);
    const int x = 30991;
    std::cout << 30991;
    for (int i = 0; i < 30991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30991(int& val) {
    const int x = 30991;
    std::cout << 30991;
    for (int i = 0; i < 30991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30992(int& val) {
    func_in_30992(val);
    const int x = 30992;
    std::cout << 30992;
    for (int i = 0; i < 30992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30992(int& val) {
    const int x = 30992;
    std::cout << 30992;
    for (int i = 0; i < 30992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30993(int& val) {
    func_in_30993(val);
    const int x = 30993;
    std::cout << 30993;
    for (int i = 0; i < 30993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30993(int& val) {
    const int x = 30993;
    std::cout << 30993;
    for (int i = 0; i < 30993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30994(int& val) {
    func_in_30994(val);
    const int x = 30994;
    std::cout << 30994;
    for (int i = 0; i < 30994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30994(int& val) {
    const int x = 30994;
    std::cout << 30994;
    for (int i = 0; i < 30994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30995(int& val) {
    func_in_30995(val);
    const int x = 30995;
    std::cout << 30995;
    for (int i = 0; i < 30995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30995(int& val) {
    const int x = 30995;
    std::cout << 30995;
    for (int i = 0; i < 30995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30996(int& val) {
    func_in_30996(val);
    const int x = 30996;
    std::cout << 30996;
    for (int i = 0; i < 30996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30996(int& val) {
    const int x = 30996;
    std::cout << 30996;
    for (int i = 0; i < 30996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30997(int& val) {
    func_in_30997(val);
    const int x = 30997;
    std::cout << 30997;
    for (int i = 0; i < 30997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30997(int& val) {
    const int x = 30997;
    std::cout << 30997;
    for (int i = 0; i < 30997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30998(int& val) {
    func_in_30998(val);
    const int x = 30998;
    std::cout << 30998;
    for (int i = 0; i < 30998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30998(int& val) {
    const int x = 30998;
    std::cout << 30998;
    for (int i = 0; i < 30998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_30999(int& val) {
    func_in_30999(val);
    const int x = 30999;
    std::cout << 30999;
    for (int i = 0; i < 30999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_30999(int& val) {
    const int x = 30999;
    std::cout << 30999;
    for (int i = 0; i < 30999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31000(int& val) {
    func_in_31000(val);
    const int x = 31000;
    std::cout << 31000;
    for (int i = 0; i < 31000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31000(int& val) {
    const int x = 31000;
    std::cout << 31000;
    for (int i = 0; i < 31000; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31001(int& val) {
    func_in_31001(val);
    const int x = 31001;
    std::cout << 31001;
    for (int i = 0; i < 31001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31001(int& val) {
    const int x = 31001;
    std::cout << 31001;
    for (int i = 0; i < 31001; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31002(int& val) {
    func_in_31002(val);
    const int x = 31002;
    std::cout << 31002;
    for (int i = 0; i < 31002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31002(int& val) {
    const int x = 31002;
    std::cout << 31002;
    for (int i = 0; i < 31002; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31003(int& val) {
    func_in_31003(val);
    const int x = 31003;
    std::cout << 31003;
    for (int i = 0; i < 31003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31003(int& val) {
    const int x = 31003;
    std::cout << 31003;
    for (int i = 0; i < 31003; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31004(int& val) {
    func_in_31004(val);
    const int x = 31004;
    std::cout << 31004;
    for (int i = 0; i < 31004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31004(int& val) {
    const int x = 31004;
    std::cout << 31004;
    for (int i = 0; i < 31004; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31005(int& val) {
    func_in_31005(val);
    const int x = 31005;
    std::cout << 31005;
    for (int i = 0; i < 31005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31005(int& val) {
    const int x = 31005;
    std::cout << 31005;
    for (int i = 0; i < 31005; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31006(int& val) {
    func_in_31006(val);
    const int x = 31006;
    std::cout << 31006;
    for (int i = 0; i < 31006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31006(int& val) {
    const int x = 31006;
    std::cout << 31006;
    for (int i = 0; i < 31006; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31007(int& val) {
    func_in_31007(val);
    const int x = 31007;
    std::cout << 31007;
    for (int i = 0; i < 31007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31007(int& val) {
    const int x = 31007;
    std::cout << 31007;
    for (int i = 0; i < 31007; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31008(int& val) {
    func_in_31008(val);
    const int x = 31008;
    std::cout << 31008;
    for (int i = 0; i < 31008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31008(int& val) {
    const int x = 31008;
    std::cout << 31008;
    for (int i = 0; i < 31008; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31009(int& val) {
    func_in_31009(val);
    const int x = 31009;
    std::cout << 31009;
    for (int i = 0; i < 31009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31009(int& val) {
    const int x = 31009;
    std::cout << 31009;
    for (int i = 0; i < 31009; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31010(int& val) {
    func_in_31010(val);
    const int x = 31010;
    std::cout << 31010;
    for (int i = 0; i < 31010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31010(int& val) {
    const int x = 31010;
    std::cout << 31010;
    for (int i = 0; i < 31010; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31011(int& val) {
    func_in_31011(val);
    const int x = 31011;
    std::cout << 31011;
    for (int i = 0; i < 31011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31011(int& val) {
    const int x = 31011;
    std::cout << 31011;
    for (int i = 0; i < 31011; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31012(int& val) {
    func_in_31012(val);
    const int x = 31012;
    std::cout << 31012;
    for (int i = 0; i < 31012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31012(int& val) {
    const int x = 31012;
    std::cout << 31012;
    for (int i = 0; i < 31012; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31013(int& val) {
    func_in_31013(val);
    const int x = 31013;
    std::cout << 31013;
    for (int i = 0; i < 31013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31013(int& val) {
    const int x = 31013;
    std::cout << 31013;
    for (int i = 0; i < 31013; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31014(int& val) {
    func_in_31014(val);
    const int x = 31014;
    std::cout << 31014;
    for (int i = 0; i < 31014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31014(int& val) {
    const int x = 31014;
    std::cout << 31014;
    for (int i = 0; i < 31014; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31015(int& val) {
    func_in_31015(val);
    const int x = 31015;
    std::cout << 31015;
    for (int i = 0; i < 31015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31015(int& val) {
    const int x = 31015;
    std::cout << 31015;
    for (int i = 0; i < 31015; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31016(int& val) {
    func_in_31016(val);
    const int x = 31016;
    std::cout << 31016;
    for (int i = 0; i < 31016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31016(int& val) {
    const int x = 31016;
    std::cout << 31016;
    for (int i = 0; i < 31016; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31017(int& val) {
    func_in_31017(val);
    const int x = 31017;
    std::cout << 31017;
    for (int i = 0; i < 31017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31017(int& val) {
    const int x = 31017;
    std::cout << 31017;
    for (int i = 0; i < 31017; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31018(int& val) {
    func_in_31018(val);
    const int x = 31018;
    std::cout << 31018;
    for (int i = 0; i < 31018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31018(int& val) {
    const int x = 31018;
    std::cout << 31018;
    for (int i = 0; i < 31018; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31019(int& val) {
    func_in_31019(val);
    const int x = 31019;
    std::cout << 31019;
    for (int i = 0; i < 31019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31019(int& val) {
    const int x = 31019;
    std::cout << 31019;
    for (int i = 0; i < 31019; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31020(int& val) {
    func_in_31020(val);
    const int x = 31020;
    std::cout << 31020;
    for (int i = 0; i < 31020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31020(int& val) {
    const int x = 31020;
    std::cout << 31020;
    for (int i = 0; i < 31020; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31021(int& val) {
    func_in_31021(val);
    const int x = 31021;
    std::cout << 31021;
    for (int i = 0; i < 31021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31021(int& val) {
    const int x = 31021;
    std::cout << 31021;
    for (int i = 0; i < 31021; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31022(int& val) {
    func_in_31022(val);
    const int x = 31022;
    std::cout << 31022;
    for (int i = 0; i < 31022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31022(int& val) {
    const int x = 31022;
    std::cout << 31022;
    for (int i = 0; i < 31022; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31023(int& val) {
    func_in_31023(val);
    const int x = 31023;
    std::cout << 31023;
    for (int i = 0; i < 31023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31023(int& val) {
    const int x = 31023;
    std::cout << 31023;
    for (int i = 0; i < 31023; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31024(int& val) {
    func_in_31024(val);
    const int x = 31024;
    std::cout << 31024;
    for (int i = 0; i < 31024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31024(int& val) {
    const int x = 31024;
    std::cout << 31024;
    for (int i = 0; i < 31024; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31025(int& val) {
    func_in_31025(val);
    const int x = 31025;
    std::cout << 31025;
    for (int i = 0; i < 31025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31025(int& val) {
    const int x = 31025;
    std::cout << 31025;
    for (int i = 0; i < 31025; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31026(int& val) {
    func_in_31026(val);
    const int x = 31026;
    std::cout << 31026;
    for (int i = 0; i < 31026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31026(int& val) {
    const int x = 31026;
    std::cout << 31026;
    for (int i = 0; i < 31026; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31027(int& val) {
    func_in_31027(val);
    const int x = 31027;
    std::cout << 31027;
    for (int i = 0; i < 31027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31027(int& val) {
    const int x = 31027;
    std::cout << 31027;
    for (int i = 0; i < 31027; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31028(int& val) {
    func_in_31028(val);
    const int x = 31028;
    std::cout << 31028;
    for (int i = 0; i < 31028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31028(int& val) {
    const int x = 31028;
    std::cout << 31028;
    for (int i = 0; i < 31028; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31029(int& val) {
    func_in_31029(val);
    const int x = 31029;
    std::cout << 31029;
    for (int i = 0; i < 31029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31029(int& val) {
    const int x = 31029;
    std::cout << 31029;
    for (int i = 0; i < 31029; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31030(int& val) {
    func_in_31030(val);
    const int x = 31030;
    std::cout << 31030;
    for (int i = 0; i < 31030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31030(int& val) {
    const int x = 31030;
    std::cout << 31030;
    for (int i = 0; i < 31030; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31031(int& val) {
    func_in_31031(val);
    const int x = 31031;
    std::cout << 31031;
    for (int i = 0; i < 31031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31031(int& val) {
    const int x = 31031;
    std::cout << 31031;
    for (int i = 0; i < 31031; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31032(int& val) {
    func_in_31032(val);
    const int x = 31032;
    std::cout << 31032;
    for (int i = 0; i < 31032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31032(int& val) {
    const int x = 31032;
    std::cout << 31032;
    for (int i = 0; i < 31032; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31033(int& val) {
    func_in_31033(val);
    const int x = 31033;
    std::cout << 31033;
    for (int i = 0; i < 31033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31033(int& val) {
    const int x = 31033;
    std::cout << 31033;
    for (int i = 0; i < 31033; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31034(int& val) {
    func_in_31034(val);
    const int x = 31034;
    std::cout << 31034;
    for (int i = 0; i < 31034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31034(int& val) {
    const int x = 31034;
    std::cout << 31034;
    for (int i = 0; i < 31034; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31035(int& val) {
    func_in_31035(val);
    const int x = 31035;
    std::cout << 31035;
    for (int i = 0; i < 31035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31035(int& val) {
    const int x = 31035;
    std::cout << 31035;
    for (int i = 0; i < 31035; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31036(int& val) {
    func_in_31036(val);
    const int x = 31036;
    std::cout << 31036;
    for (int i = 0; i < 31036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31036(int& val) {
    const int x = 31036;
    std::cout << 31036;
    for (int i = 0; i < 31036; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31037(int& val) {
    func_in_31037(val);
    const int x = 31037;
    std::cout << 31037;
    for (int i = 0; i < 31037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31037(int& val) {
    const int x = 31037;
    std::cout << 31037;
    for (int i = 0; i < 31037; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31038(int& val) {
    func_in_31038(val);
    const int x = 31038;
    std::cout << 31038;
    for (int i = 0; i < 31038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31038(int& val) {
    const int x = 31038;
    std::cout << 31038;
    for (int i = 0; i < 31038; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31039(int& val) {
    func_in_31039(val);
    const int x = 31039;
    std::cout << 31039;
    for (int i = 0; i < 31039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31039(int& val) {
    const int x = 31039;
    std::cout << 31039;
    for (int i = 0; i < 31039; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31040(int& val) {
    func_in_31040(val);
    const int x = 31040;
    std::cout << 31040;
    for (int i = 0; i < 31040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31040(int& val) {
    const int x = 31040;
    std::cout << 31040;
    for (int i = 0; i < 31040; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31041(int& val) {
    func_in_31041(val);
    const int x = 31041;
    std::cout << 31041;
    for (int i = 0; i < 31041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31041(int& val) {
    const int x = 31041;
    std::cout << 31041;
    for (int i = 0; i < 31041; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31042(int& val) {
    func_in_31042(val);
    const int x = 31042;
    std::cout << 31042;
    for (int i = 0; i < 31042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31042(int& val) {
    const int x = 31042;
    std::cout << 31042;
    for (int i = 0; i < 31042; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31043(int& val) {
    func_in_31043(val);
    const int x = 31043;
    std::cout << 31043;
    for (int i = 0; i < 31043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31043(int& val) {
    const int x = 31043;
    std::cout << 31043;
    for (int i = 0; i < 31043; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31044(int& val) {
    func_in_31044(val);
    const int x = 31044;
    std::cout << 31044;
    for (int i = 0; i < 31044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31044(int& val) {
    const int x = 31044;
    std::cout << 31044;
    for (int i = 0; i < 31044; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31045(int& val) {
    func_in_31045(val);
    const int x = 31045;
    std::cout << 31045;
    for (int i = 0; i < 31045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31045(int& val) {
    const int x = 31045;
    std::cout << 31045;
    for (int i = 0; i < 31045; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31046(int& val) {
    func_in_31046(val);
    const int x = 31046;
    std::cout << 31046;
    for (int i = 0; i < 31046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31046(int& val) {
    const int x = 31046;
    std::cout << 31046;
    for (int i = 0; i < 31046; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31047(int& val) {
    func_in_31047(val);
    const int x = 31047;
    std::cout << 31047;
    for (int i = 0; i < 31047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31047(int& val) {
    const int x = 31047;
    std::cout << 31047;
    for (int i = 0; i < 31047; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31048(int& val) {
    func_in_31048(val);
    const int x = 31048;
    std::cout << 31048;
    for (int i = 0; i < 31048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31048(int& val) {
    const int x = 31048;
    std::cout << 31048;
    for (int i = 0; i < 31048; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31049(int& val) {
    func_in_31049(val);
    const int x = 31049;
    std::cout << 31049;
    for (int i = 0; i < 31049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31049(int& val) {
    const int x = 31049;
    std::cout << 31049;
    for (int i = 0; i < 31049; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31050(int& val) {
    func_in_31050(val);
    const int x = 31050;
    std::cout << 31050;
    for (int i = 0; i < 31050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31050(int& val) {
    const int x = 31050;
    std::cout << 31050;
    for (int i = 0; i < 31050; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31051(int& val) {
    func_in_31051(val);
    const int x = 31051;
    std::cout << 31051;
    for (int i = 0; i < 31051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31051(int& val) {
    const int x = 31051;
    std::cout << 31051;
    for (int i = 0; i < 31051; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31052(int& val) {
    func_in_31052(val);
    const int x = 31052;
    std::cout << 31052;
    for (int i = 0; i < 31052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31052(int& val) {
    const int x = 31052;
    std::cout << 31052;
    for (int i = 0; i < 31052; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31053(int& val) {
    func_in_31053(val);
    const int x = 31053;
    std::cout << 31053;
    for (int i = 0; i < 31053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31053(int& val) {
    const int x = 31053;
    std::cout << 31053;
    for (int i = 0; i < 31053; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31054(int& val) {
    func_in_31054(val);
    const int x = 31054;
    std::cout << 31054;
    for (int i = 0; i < 31054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31054(int& val) {
    const int x = 31054;
    std::cout << 31054;
    for (int i = 0; i < 31054; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31055(int& val) {
    func_in_31055(val);
    const int x = 31055;
    std::cout << 31055;
    for (int i = 0; i < 31055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31055(int& val) {
    const int x = 31055;
    std::cout << 31055;
    for (int i = 0; i < 31055; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31056(int& val) {
    func_in_31056(val);
    const int x = 31056;
    std::cout << 31056;
    for (int i = 0; i < 31056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31056(int& val) {
    const int x = 31056;
    std::cout << 31056;
    for (int i = 0; i < 31056; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31057(int& val) {
    func_in_31057(val);
    const int x = 31057;
    std::cout << 31057;
    for (int i = 0; i < 31057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31057(int& val) {
    const int x = 31057;
    std::cout << 31057;
    for (int i = 0; i < 31057; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31058(int& val) {
    func_in_31058(val);
    const int x = 31058;
    std::cout << 31058;
    for (int i = 0; i < 31058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31058(int& val) {
    const int x = 31058;
    std::cout << 31058;
    for (int i = 0; i < 31058; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31059(int& val) {
    func_in_31059(val);
    const int x = 31059;
    std::cout << 31059;
    for (int i = 0; i < 31059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31059(int& val) {
    const int x = 31059;
    std::cout << 31059;
    for (int i = 0; i < 31059; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31060(int& val) {
    func_in_31060(val);
    const int x = 31060;
    std::cout << 31060;
    for (int i = 0; i < 31060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31060(int& val) {
    const int x = 31060;
    std::cout << 31060;
    for (int i = 0; i < 31060; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31061(int& val) {
    func_in_31061(val);
    const int x = 31061;
    std::cout << 31061;
    for (int i = 0; i < 31061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31061(int& val) {
    const int x = 31061;
    std::cout << 31061;
    for (int i = 0; i < 31061; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31062(int& val) {
    func_in_31062(val);
    const int x = 31062;
    std::cout << 31062;
    for (int i = 0; i < 31062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31062(int& val) {
    const int x = 31062;
    std::cout << 31062;
    for (int i = 0; i < 31062; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31063(int& val) {
    func_in_31063(val);
    const int x = 31063;
    std::cout << 31063;
    for (int i = 0; i < 31063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31063(int& val) {
    const int x = 31063;
    std::cout << 31063;
    for (int i = 0; i < 31063; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31064(int& val) {
    func_in_31064(val);
    const int x = 31064;
    std::cout << 31064;
    for (int i = 0; i < 31064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31064(int& val) {
    const int x = 31064;
    std::cout << 31064;
    for (int i = 0; i < 31064; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31065(int& val) {
    func_in_31065(val);
    const int x = 31065;
    std::cout << 31065;
    for (int i = 0; i < 31065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31065(int& val) {
    const int x = 31065;
    std::cout << 31065;
    for (int i = 0; i < 31065; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31066(int& val) {
    func_in_31066(val);
    const int x = 31066;
    std::cout << 31066;
    for (int i = 0; i < 31066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31066(int& val) {
    const int x = 31066;
    std::cout << 31066;
    for (int i = 0; i < 31066; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31067(int& val) {
    func_in_31067(val);
    const int x = 31067;
    std::cout << 31067;
    for (int i = 0; i < 31067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31067(int& val) {
    const int x = 31067;
    std::cout << 31067;
    for (int i = 0; i < 31067; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31068(int& val) {
    func_in_31068(val);
    const int x = 31068;
    std::cout << 31068;
    for (int i = 0; i < 31068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31068(int& val) {
    const int x = 31068;
    std::cout << 31068;
    for (int i = 0; i < 31068; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31069(int& val) {
    func_in_31069(val);
    const int x = 31069;
    std::cout << 31069;
    for (int i = 0; i < 31069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31069(int& val) {
    const int x = 31069;
    std::cout << 31069;
    for (int i = 0; i < 31069; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31070(int& val) {
    func_in_31070(val);
    const int x = 31070;
    std::cout << 31070;
    for (int i = 0; i < 31070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31070(int& val) {
    const int x = 31070;
    std::cout << 31070;
    for (int i = 0; i < 31070; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31071(int& val) {
    func_in_31071(val);
    const int x = 31071;
    std::cout << 31071;
    for (int i = 0; i < 31071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31071(int& val) {
    const int x = 31071;
    std::cout << 31071;
    for (int i = 0; i < 31071; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31072(int& val) {
    func_in_31072(val);
    const int x = 31072;
    std::cout << 31072;
    for (int i = 0; i < 31072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31072(int& val) {
    const int x = 31072;
    std::cout << 31072;
    for (int i = 0; i < 31072; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31073(int& val) {
    func_in_31073(val);
    const int x = 31073;
    std::cout << 31073;
    for (int i = 0; i < 31073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31073(int& val) {
    const int x = 31073;
    std::cout << 31073;
    for (int i = 0; i < 31073; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31074(int& val) {
    func_in_31074(val);
    const int x = 31074;
    std::cout << 31074;
    for (int i = 0; i < 31074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31074(int& val) {
    const int x = 31074;
    std::cout << 31074;
    for (int i = 0; i < 31074; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31075(int& val) {
    func_in_31075(val);
    const int x = 31075;
    std::cout << 31075;
    for (int i = 0; i < 31075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31075(int& val) {
    const int x = 31075;
    std::cout << 31075;
    for (int i = 0; i < 31075; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31076(int& val) {
    func_in_31076(val);
    const int x = 31076;
    std::cout << 31076;
    for (int i = 0; i < 31076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31076(int& val) {
    const int x = 31076;
    std::cout << 31076;
    for (int i = 0; i < 31076; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31077(int& val) {
    func_in_31077(val);
    const int x = 31077;
    std::cout << 31077;
    for (int i = 0; i < 31077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31077(int& val) {
    const int x = 31077;
    std::cout << 31077;
    for (int i = 0; i < 31077; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31078(int& val) {
    func_in_31078(val);
    const int x = 31078;
    std::cout << 31078;
    for (int i = 0; i < 31078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31078(int& val) {
    const int x = 31078;
    std::cout << 31078;
    for (int i = 0; i < 31078; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31079(int& val) {
    func_in_31079(val);
    const int x = 31079;
    std::cout << 31079;
    for (int i = 0; i < 31079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31079(int& val) {
    const int x = 31079;
    std::cout << 31079;
    for (int i = 0; i < 31079; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31080(int& val) {
    func_in_31080(val);
    const int x = 31080;
    std::cout << 31080;
    for (int i = 0; i < 31080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31080(int& val) {
    const int x = 31080;
    std::cout << 31080;
    for (int i = 0; i < 31080; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31081(int& val) {
    func_in_31081(val);
    const int x = 31081;
    std::cout << 31081;
    for (int i = 0; i < 31081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31081(int& val) {
    const int x = 31081;
    std::cout << 31081;
    for (int i = 0; i < 31081; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31082(int& val) {
    func_in_31082(val);
    const int x = 31082;
    std::cout << 31082;
    for (int i = 0; i < 31082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31082(int& val) {
    const int x = 31082;
    std::cout << 31082;
    for (int i = 0; i < 31082; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31083(int& val) {
    func_in_31083(val);
    const int x = 31083;
    std::cout << 31083;
    for (int i = 0; i < 31083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31083(int& val) {
    const int x = 31083;
    std::cout << 31083;
    for (int i = 0; i < 31083; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31084(int& val) {
    func_in_31084(val);
    const int x = 31084;
    std::cout << 31084;
    for (int i = 0; i < 31084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31084(int& val) {
    const int x = 31084;
    std::cout << 31084;
    for (int i = 0; i < 31084; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31085(int& val) {
    func_in_31085(val);
    const int x = 31085;
    std::cout << 31085;
    for (int i = 0; i < 31085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31085(int& val) {
    const int x = 31085;
    std::cout << 31085;
    for (int i = 0; i < 31085; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31086(int& val) {
    func_in_31086(val);
    const int x = 31086;
    std::cout << 31086;
    for (int i = 0; i < 31086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31086(int& val) {
    const int x = 31086;
    std::cout << 31086;
    for (int i = 0; i < 31086; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31087(int& val) {
    func_in_31087(val);
    const int x = 31087;
    std::cout << 31087;
    for (int i = 0; i < 31087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31087(int& val) {
    const int x = 31087;
    std::cout << 31087;
    for (int i = 0; i < 31087; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31088(int& val) {
    func_in_31088(val);
    const int x = 31088;
    std::cout << 31088;
    for (int i = 0; i < 31088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31088(int& val) {
    const int x = 31088;
    std::cout << 31088;
    for (int i = 0; i < 31088; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31089(int& val) {
    func_in_31089(val);
    const int x = 31089;
    std::cout << 31089;
    for (int i = 0; i < 31089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31089(int& val) {
    const int x = 31089;
    std::cout << 31089;
    for (int i = 0; i < 31089; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31090(int& val) {
    func_in_31090(val);
    const int x = 31090;
    std::cout << 31090;
    for (int i = 0; i < 31090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31090(int& val) {
    const int x = 31090;
    std::cout << 31090;
    for (int i = 0; i < 31090; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31091(int& val) {
    func_in_31091(val);
    const int x = 31091;
    std::cout << 31091;
    for (int i = 0; i < 31091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31091(int& val) {
    const int x = 31091;
    std::cout << 31091;
    for (int i = 0; i < 31091; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31092(int& val) {
    func_in_31092(val);
    const int x = 31092;
    std::cout << 31092;
    for (int i = 0; i < 31092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31092(int& val) {
    const int x = 31092;
    std::cout << 31092;
    for (int i = 0; i < 31092; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31093(int& val) {
    func_in_31093(val);
    const int x = 31093;
    std::cout << 31093;
    for (int i = 0; i < 31093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31093(int& val) {
    const int x = 31093;
    std::cout << 31093;
    for (int i = 0; i < 31093; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31094(int& val) {
    func_in_31094(val);
    const int x = 31094;
    std::cout << 31094;
    for (int i = 0; i < 31094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31094(int& val) {
    const int x = 31094;
    std::cout << 31094;
    for (int i = 0; i < 31094; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31095(int& val) {
    func_in_31095(val);
    const int x = 31095;
    std::cout << 31095;
    for (int i = 0; i < 31095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31095(int& val) {
    const int x = 31095;
    std::cout << 31095;
    for (int i = 0; i < 31095; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31096(int& val) {
    func_in_31096(val);
    const int x = 31096;
    std::cout << 31096;
    for (int i = 0; i < 31096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31096(int& val) {
    const int x = 31096;
    std::cout << 31096;
    for (int i = 0; i < 31096; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31097(int& val) {
    func_in_31097(val);
    const int x = 31097;
    std::cout << 31097;
    for (int i = 0; i < 31097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31097(int& val) {
    const int x = 31097;
    std::cout << 31097;
    for (int i = 0; i < 31097; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31098(int& val) {
    func_in_31098(val);
    const int x = 31098;
    std::cout << 31098;
    for (int i = 0; i < 31098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31098(int& val) {
    const int x = 31098;
    std::cout << 31098;
    for (int i = 0; i < 31098; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31099(int& val) {
    func_in_31099(val);
    const int x = 31099;
    std::cout << 31099;
    for (int i = 0; i < 31099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31099(int& val) {
    const int x = 31099;
    std::cout << 31099;
    for (int i = 0; i < 31099; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31100(int& val) {
    func_in_31100(val);
    const int x = 31100;
    std::cout << 31100;
    for (int i = 0; i < 31100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31100(int& val) {
    const int x = 31100;
    std::cout << 31100;
    for (int i = 0; i < 31100; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31101(int& val) {
    func_in_31101(val);
    const int x = 31101;
    std::cout << 31101;
    for (int i = 0; i < 31101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31101(int& val) {
    const int x = 31101;
    std::cout << 31101;
    for (int i = 0; i < 31101; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31102(int& val) {
    func_in_31102(val);
    const int x = 31102;
    std::cout << 31102;
    for (int i = 0; i < 31102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31102(int& val) {
    const int x = 31102;
    std::cout << 31102;
    for (int i = 0; i < 31102; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31103(int& val) {
    func_in_31103(val);
    const int x = 31103;
    std::cout << 31103;
    for (int i = 0; i < 31103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31103(int& val) {
    const int x = 31103;
    std::cout << 31103;
    for (int i = 0; i < 31103; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31104(int& val) {
    func_in_31104(val);
    const int x = 31104;
    std::cout << 31104;
    for (int i = 0; i < 31104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31104(int& val) {
    const int x = 31104;
    std::cout << 31104;
    for (int i = 0; i < 31104; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31105(int& val) {
    func_in_31105(val);
    const int x = 31105;
    std::cout << 31105;
    for (int i = 0; i < 31105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31105(int& val) {
    const int x = 31105;
    std::cout << 31105;
    for (int i = 0; i < 31105; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31106(int& val) {
    func_in_31106(val);
    const int x = 31106;
    std::cout << 31106;
    for (int i = 0; i < 31106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31106(int& val) {
    const int x = 31106;
    std::cout << 31106;
    for (int i = 0; i < 31106; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31107(int& val) {
    func_in_31107(val);
    const int x = 31107;
    std::cout << 31107;
    for (int i = 0; i < 31107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31107(int& val) {
    const int x = 31107;
    std::cout << 31107;
    for (int i = 0; i < 31107; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31108(int& val) {
    func_in_31108(val);
    const int x = 31108;
    std::cout << 31108;
    for (int i = 0; i < 31108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31108(int& val) {
    const int x = 31108;
    std::cout << 31108;
    for (int i = 0; i < 31108; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31109(int& val) {
    func_in_31109(val);
    const int x = 31109;
    std::cout << 31109;
    for (int i = 0; i < 31109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31109(int& val) {
    const int x = 31109;
    std::cout << 31109;
    for (int i = 0; i < 31109; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31110(int& val) {
    func_in_31110(val);
    const int x = 31110;
    std::cout << 31110;
    for (int i = 0; i < 31110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31110(int& val) {
    const int x = 31110;
    std::cout << 31110;
    for (int i = 0; i < 31110; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31111(int& val) {
    func_in_31111(val);
    const int x = 31111;
    std::cout << 31111;
    for (int i = 0; i < 31111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31111(int& val) {
    const int x = 31111;
    std::cout << 31111;
    for (int i = 0; i < 31111; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31112(int& val) {
    func_in_31112(val);
    const int x = 31112;
    std::cout << 31112;
    for (int i = 0; i < 31112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31112(int& val) {
    const int x = 31112;
    std::cout << 31112;
    for (int i = 0; i < 31112; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31113(int& val) {
    func_in_31113(val);
    const int x = 31113;
    std::cout << 31113;
    for (int i = 0; i < 31113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31113(int& val) {
    const int x = 31113;
    std::cout << 31113;
    for (int i = 0; i < 31113; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31114(int& val) {
    func_in_31114(val);
    const int x = 31114;
    std::cout << 31114;
    for (int i = 0; i < 31114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31114(int& val) {
    const int x = 31114;
    std::cout << 31114;
    for (int i = 0; i < 31114; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31115(int& val) {
    func_in_31115(val);
    const int x = 31115;
    std::cout << 31115;
    for (int i = 0; i < 31115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31115(int& val) {
    const int x = 31115;
    std::cout << 31115;
    for (int i = 0; i < 31115; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31116(int& val) {
    func_in_31116(val);
    const int x = 31116;
    std::cout << 31116;
    for (int i = 0; i < 31116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31116(int& val) {
    const int x = 31116;
    std::cout << 31116;
    for (int i = 0; i < 31116; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31117(int& val) {
    func_in_31117(val);
    const int x = 31117;
    std::cout << 31117;
    for (int i = 0; i < 31117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31117(int& val) {
    const int x = 31117;
    std::cout << 31117;
    for (int i = 0; i < 31117; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31118(int& val) {
    func_in_31118(val);
    const int x = 31118;
    std::cout << 31118;
    for (int i = 0; i < 31118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31118(int& val) {
    const int x = 31118;
    std::cout << 31118;
    for (int i = 0; i < 31118; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31119(int& val) {
    func_in_31119(val);
    const int x = 31119;
    std::cout << 31119;
    for (int i = 0; i < 31119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31119(int& val) {
    const int x = 31119;
    std::cout << 31119;
    for (int i = 0; i < 31119; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31120(int& val) {
    func_in_31120(val);
    const int x = 31120;
    std::cout << 31120;
    for (int i = 0; i < 31120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31120(int& val) {
    const int x = 31120;
    std::cout << 31120;
    for (int i = 0; i < 31120; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31121(int& val) {
    func_in_31121(val);
    const int x = 31121;
    std::cout << 31121;
    for (int i = 0; i < 31121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31121(int& val) {
    const int x = 31121;
    std::cout << 31121;
    for (int i = 0; i < 31121; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31122(int& val) {
    func_in_31122(val);
    const int x = 31122;
    std::cout << 31122;
    for (int i = 0; i < 31122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31122(int& val) {
    const int x = 31122;
    std::cout << 31122;
    for (int i = 0; i < 31122; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31123(int& val) {
    func_in_31123(val);
    const int x = 31123;
    std::cout << 31123;
    for (int i = 0; i < 31123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31123(int& val) {
    const int x = 31123;
    std::cout << 31123;
    for (int i = 0; i < 31123; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31124(int& val) {
    func_in_31124(val);
    const int x = 31124;
    std::cout << 31124;
    for (int i = 0; i < 31124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31124(int& val) {
    const int x = 31124;
    std::cout << 31124;
    for (int i = 0; i < 31124; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31125(int& val) {
    func_in_31125(val);
    const int x = 31125;
    std::cout << 31125;
    for (int i = 0; i < 31125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31125(int& val) {
    const int x = 31125;
    std::cout << 31125;
    for (int i = 0; i < 31125; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31126(int& val) {
    func_in_31126(val);
    const int x = 31126;
    std::cout << 31126;
    for (int i = 0; i < 31126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31126(int& val) {
    const int x = 31126;
    std::cout << 31126;
    for (int i = 0; i < 31126; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31127(int& val) {
    func_in_31127(val);
    const int x = 31127;
    std::cout << 31127;
    for (int i = 0; i < 31127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31127(int& val) {
    const int x = 31127;
    std::cout << 31127;
    for (int i = 0; i < 31127; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31128(int& val) {
    func_in_31128(val);
    const int x = 31128;
    std::cout << 31128;
    for (int i = 0; i < 31128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31128(int& val) {
    const int x = 31128;
    std::cout << 31128;
    for (int i = 0; i < 31128; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31129(int& val) {
    func_in_31129(val);
    const int x = 31129;
    std::cout << 31129;
    for (int i = 0; i < 31129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31129(int& val) {
    const int x = 31129;
    std::cout << 31129;
    for (int i = 0; i < 31129; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31130(int& val) {
    func_in_31130(val);
    const int x = 31130;
    std::cout << 31130;
    for (int i = 0; i < 31130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31130(int& val) {
    const int x = 31130;
    std::cout << 31130;
    for (int i = 0; i < 31130; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31131(int& val) {
    func_in_31131(val);
    const int x = 31131;
    std::cout << 31131;
    for (int i = 0; i < 31131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31131(int& val) {
    const int x = 31131;
    std::cout << 31131;
    for (int i = 0; i < 31131; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31132(int& val) {
    func_in_31132(val);
    const int x = 31132;
    std::cout << 31132;
    for (int i = 0; i < 31132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31132(int& val) {
    const int x = 31132;
    std::cout << 31132;
    for (int i = 0; i < 31132; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31133(int& val) {
    func_in_31133(val);
    const int x = 31133;
    std::cout << 31133;
    for (int i = 0; i < 31133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31133(int& val) {
    const int x = 31133;
    std::cout << 31133;
    for (int i = 0; i < 31133; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31134(int& val) {
    func_in_31134(val);
    const int x = 31134;
    std::cout << 31134;
    for (int i = 0; i < 31134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31134(int& val) {
    const int x = 31134;
    std::cout << 31134;
    for (int i = 0; i < 31134; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31135(int& val) {
    func_in_31135(val);
    const int x = 31135;
    std::cout << 31135;
    for (int i = 0; i < 31135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31135(int& val) {
    const int x = 31135;
    std::cout << 31135;
    for (int i = 0; i < 31135; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31136(int& val) {
    func_in_31136(val);
    const int x = 31136;
    std::cout << 31136;
    for (int i = 0; i < 31136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31136(int& val) {
    const int x = 31136;
    std::cout << 31136;
    for (int i = 0; i < 31136; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31137(int& val) {
    func_in_31137(val);
    const int x = 31137;
    std::cout << 31137;
    for (int i = 0; i < 31137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31137(int& val) {
    const int x = 31137;
    std::cout << 31137;
    for (int i = 0; i < 31137; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31138(int& val) {
    func_in_31138(val);
    const int x = 31138;
    std::cout << 31138;
    for (int i = 0; i < 31138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31138(int& val) {
    const int x = 31138;
    std::cout << 31138;
    for (int i = 0; i < 31138; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31139(int& val) {
    func_in_31139(val);
    const int x = 31139;
    std::cout << 31139;
    for (int i = 0; i < 31139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31139(int& val) {
    const int x = 31139;
    std::cout << 31139;
    for (int i = 0; i < 31139; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31140(int& val) {
    func_in_31140(val);
    const int x = 31140;
    std::cout << 31140;
    for (int i = 0; i < 31140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31140(int& val) {
    const int x = 31140;
    std::cout << 31140;
    for (int i = 0; i < 31140; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31141(int& val) {
    func_in_31141(val);
    const int x = 31141;
    std::cout << 31141;
    for (int i = 0; i < 31141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31141(int& val) {
    const int x = 31141;
    std::cout << 31141;
    for (int i = 0; i < 31141; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31142(int& val) {
    func_in_31142(val);
    const int x = 31142;
    std::cout << 31142;
    for (int i = 0; i < 31142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31142(int& val) {
    const int x = 31142;
    std::cout << 31142;
    for (int i = 0; i < 31142; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31143(int& val) {
    func_in_31143(val);
    const int x = 31143;
    std::cout << 31143;
    for (int i = 0; i < 31143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31143(int& val) {
    const int x = 31143;
    std::cout << 31143;
    for (int i = 0; i < 31143; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31144(int& val) {
    func_in_31144(val);
    const int x = 31144;
    std::cout << 31144;
    for (int i = 0; i < 31144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31144(int& val) {
    const int x = 31144;
    std::cout << 31144;
    for (int i = 0; i < 31144; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31145(int& val) {
    func_in_31145(val);
    const int x = 31145;
    std::cout << 31145;
    for (int i = 0; i < 31145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31145(int& val) {
    const int x = 31145;
    std::cout << 31145;
    for (int i = 0; i < 31145; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31146(int& val) {
    func_in_31146(val);
    const int x = 31146;
    std::cout << 31146;
    for (int i = 0; i < 31146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31146(int& val) {
    const int x = 31146;
    std::cout << 31146;
    for (int i = 0; i < 31146; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31147(int& val) {
    func_in_31147(val);
    const int x = 31147;
    std::cout << 31147;
    for (int i = 0; i < 31147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31147(int& val) {
    const int x = 31147;
    std::cout << 31147;
    for (int i = 0; i < 31147; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31148(int& val) {
    func_in_31148(val);
    const int x = 31148;
    std::cout << 31148;
    for (int i = 0; i < 31148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31148(int& val) {
    const int x = 31148;
    std::cout << 31148;
    for (int i = 0; i < 31148; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31149(int& val) {
    func_in_31149(val);
    const int x = 31149;
    std::cout << 31149;
    for (int i = 0; i < 31149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31149(int& val) {
    const int x = 31149;
    std::cout << 31149;
    for (int i = 0; i < 31149; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31150(int& val) {
    func_in_31150(val);
    const int x = 31150;
    std::cout << 31150;
    for (int i = 0; i < 31150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31150(int& val) {
    const int x = 31150;
    std::cout << 31150;
    for (int i = 0; i < 31150; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31151(int& val) {
    func_in_31151(val);
    const int x = 31151;
    std::cout << 31151;
    for (int i = 0; i < 31151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31151(int& val) {
    const int x = 31151;
    std::cout << 31151;
    for (int i = 0; i < 31151; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31152(int& val) {
    func_in_31152(val);
    const int x = 31152;
    std::cout << 31152;
    for (int i = 0; i < 31152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31152(int& val) {
    const int x = 31152;
    std::cout << 31152;
    for (int i = 0; i < 31152; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31153(int& val) {
    func_in_31153(val);
    const int x = 31153;
    std::cout << 31153;
    for (int i = 0; i < 31153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31153(int& val) {
    const int x = 31153;
    std::cout << 31153;
    for (int i = 0; i < 31153; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31154(int& val) {
    func_in_31154(val);
    const int x = 31154;
    std::cout << 31154;
    for (int i = 0; i < 31154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31154(int& val) {
    const int x = 31154;
    std::cout << 31154;
    for (int i = 0; i < 31154; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31155(int& val) {
    func_in_31155(val);
    const int x = 31155;
    std::cout << 31155;
    for (int i = 0; i < 31155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31155(int& val) {
    const int x = 31155;
    std::cout << 31155;
    for (int i = 0; i < 31155; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31156(int& val) {
    func_in_31156(val);
    const int x = 31156;
    std::cout << 31156;
    for (int i = 0; i < 31156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31156(int& val) {
    const int x = 31156;
    std::cout << 31156;
    for (int i = 0; i < 31156; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31157(int& val) {
    func_in_31157(val);
    const int x = 31157;
    std::cout << 31157;
    for (int i = 0; i < 31157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31157(int& val) {
    const int x = 31157;
    std::cout << 31157;
    for (int i = 0; i < 31157; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31158(int& val) {
    func_in_31158(val);
    const int x = 31158;
    std::cout << 31158;
    for (int i = 0; i < 31158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31158(int& val) {
    const int x = 31158;
    std::cout << 31158;
    for (int i = 0; i < 31158; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31159(int& val) {
    func_in_31159(val);
    const int x = 31159;
    std::cout << 31159;
    for (int i = 0; i < 31159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31159(int& val) {
    const int x = 31159;
    std::cout << 31159;
    for (int i = 0; i < 31159; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31160(int& val) {
    func_in_31160(val);
    const int x = 31160;
    std::cout << 31160;
    for (int i = 0; i < 31160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31160(int& val) {
    const int x = 31160;
    std::cout << 31160;
    for (int i = 0; i < 31160; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31161(int& val) {
    func_in_31161(val);
    const int x = 31161;
    std::cout << 31161;
    for (int i = 0; i < 31161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31161(int& val) {
    const int x = 31161;
    std::cout << 31161;
    for (int i = 0; i < 31161; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31162(int& val) {
    func_in_31162(val);
    const int x = 31162;
    std::cout << 31162;
    for (int i = 0; i < 31162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31162(int& val) {
    const int x = 31162;
    std::cout << 31162;
    for (int i = 0; i < 31162; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31163(int& val) {
    func_in_31163(val);
    const int x = 31163;
    std::cout << 31163;
    for (int i = 0; i < 31163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31163(int& val) {
    const int x = 31163;
    std::cout << 31163;
    for (int i = 0; i < 31163; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31164(int& val) {
    func_in_31164(val);
    const int x = 31164;
    std::cout << 31164;
    for (int i = 0; i < 31164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31164(int& val) {
    const int x = 31164;
    std::cout << 31164;
    for (int i = 0; i < 31164; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31165(int& val) {
    func_in_31165(val);
    const int x = 31165;
    std::cout << 31165;
    for (int i = 0; i < 31165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31165(int& val) {
    const int x = 31165;
    std::cout << 31165;
    for (int i = 0; i < 31165; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31166(int& val) {
    func_in_31166(val);
    const int x = 31166;
    std::cout << 31166;
    for (int i = 0; i < 31166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31166(int& val) {
    const int x = 31166;
    std::cout << 31166;
    for (int i = 0; i < 31166; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31167(int& val) {
    func_in_31167(val);
    const int x = 31167;
    std::cout << 31167;
    for (int i = 0; i < 31167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31167(int& val) {
    const int x = 31167;
    std::cout << 31167;
    for (int i = 0; i < 31167; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31168(int& val) {
    func_in_31168(val);
    const int x = 31168;
    std::cout << 31168;
    for (int i = 0; i < 31168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31168(int& val) {
    const int x = 31168;
    std::cout << 31168;
    for (int i = 0; i < 31168; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31169(int& val) {
    func_in_31169(val);
    const int x = 31169;
    std::cout << 31169;
    for (int i = 0; i < 31169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31169(int& val) {
    const int x = 31169;
    std::cout << 31169;
    for (int i = 0; i < 31169; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31170(int& val) {
    func_in_31170(val);
    const int x = 31170;
    std::cout << 31170;
    for (int i = 0; i < 31170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31170(int& val) {
    const int x = 31170;
    std::cout << 31170;
    for (int i = 0; i < 31170; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31171(int& val) {
    func_in_31171(val);
    const int x = 31171;
    std::cout << 31171;
    for (int i = 0; i < 31171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31171(int& val) {
    const int x = 31171;
    std::cout << 31171;
    for (int i = 0; i < 31171; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31172(int& val) {
    func_in_31172(val);
    const int x = 31172;
    std::cout << 31172;
    for (int i = 0; i < 31172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31172(int& val) {
    const int x = 31172;
    std::cout << 31172;
    for (int i = 0; i < 31172; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31173(int& val) {
    func_in_31173(val);
    const int x = 31173;
    std::cout << 31173;
    for (int i = 0; i < 31173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31173(int& val) {
    const int x = 31173;
    std::cout << 31173;
    for (int i = 0; i < 31173; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31174(int& val) {
    func_in_31174(val);
    const int x = 31174;
    std::cout << 31174;
    for (int i = 0; i < 31174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31174(int& val) {
    const int x = 31174;
    std::cout << 31174;
    for (int i = 0; i < 31174; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31175(int& val) {
    func_in_31175(val);
    const int x = 31175;
    std::cout << 31175;
    for (int i = 0; i < 31175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31175(int& val) {
    const int x = 31175;
    std::cout << 31175;
    for (int i = 0; i < 31175; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31176(int& val) {
    func_in_31176(val);
    const int x = 31176;
    std::cout << 31176;
    for (int i = 0; i < 31176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31176(int& val) {
    const int x = 31176;
    std::cout << 31176;
    for (int i = 0; i < 31176; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31177(int& val) {
    func_in_31177(val);
    const int x = 31177;
    std::cout << 31177;
    for (int i = 0; i < 31177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31177(int& val) {
    const int x = 31177;
    std::cout << 31177;
    for (int i = 0; i < 31177; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31178(int& val) {
    func_in_31178(val);
    const int x = 31178;
    std::cout << 31178;
    for (int i = 0; i < 31178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31178(int& val) {
    const int x = 31178;
    std::cout << 31178;
    for (int i = 0; i < 31178; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31179(int& val) {
    func_in_31179(val);
    const int x = 31179;
    std::cout << 31179;
    for (int i = 0; i < 31179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31179(int& val) {
    const int x = 31179;
    std::cout << 31179;
    for (int i = 0; i < 31179; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31180(int& val) {
    func_in_31180(val);
    const int x = 31180;
    std::cout << 31180;
    for (int i = 0; i < 31180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31180(int& val) {
    const int x = 31180;
    std::cout << 31180;
    for (int i = 0; i < 31180; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31181(int& val) {
    func_in_31181(val);
    const int x = 31181;
    std::cout << 31181;
    for (int i = 0; i < 31181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31181(int& val) {
    const int x = 31181;
    std::cout << 31181;
    for (int i = 0; i < 31181; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31182(int& val) {
    func_in_31182(val);
    const int x = 31182;
    std::cout << 31182;
    for (int i = 0; i < 31182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31182(int& val) {
    const int x = 31182;
    std::cout << 31182;
    for (int i = 0; i < 31182; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31183(int& val) {
    func_in_31183(val);
    const int x = 31183;
    std::cout << 31183;
    for (int i = 0; i < 31183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31183(int& val) {
    const int x = 31183;
    std::cout << 31183;
    for (int i = 0; i < 31183; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31184(int& val) {
    func_in_31184(val);
    const int x = 31184;
    std::cout << 31184;
    for (int i = 0; i < 31184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31184(int& val) {
    const int x = 31184;
    std::cout << 31184;
    for (int i = 0; i < 31184; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31185(int& val) {
    func_in_31185(val);
    const int x = 31185;
    std::cout << 31185;
    for (int i = 0; i < 31185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31185(int& val) {
    const int x = 31185;
    std::cout << 31185;
    for (int i = 0; i < 31185; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31186(int& val) {
    func_in_31186(val);
    const int x = 31186;
    std::cout << 31186;
    for (int i = 0; i < 31186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31186(int& val) {
    const int x = 31186;
    std::cout << 31186;
    for (int i = 0; i < 31186; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31187(int& val) {
    func_in_31187(val);
    const int x = 31187;
    std::cout << 31187;
    for (int i = 0; i < 31187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31187(int& val) {
    const int x = 31187;
    std::cout << 31187;
    for (int i = 0; i < 31187; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31188(int& val) {
    func_in_31188(val);
    const int x = 31188;
    std::cout << 31188;
    for (int i = 0; i < 31188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31188(int& val) {
    const int x = 31188;
    std::cout << 31188;
    for (int i = 0; i < 31188; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31189(int& val) {
    func_in_31189(val);
    const int x = 31189;
    std::cout << 31189;
    for (int i = 0; i < 31189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31189(int& val) {
    const int x = 31189;
    std::cout << 31189;
    for (int i = 0; i < 31189; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31190(int& val) {
    func_in_31190(val);
    const int x = 31190;
    std::cout << 31190;
    for (int i = 0; i < 31190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31190(int& val) {
    const int x = 31190;
    std::cout << 31190;
    for (int i = 0; i < 31190; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31191(int& val) {
    func_in_31191(val);
    const int x = 31191;
    std::cout << 31191;
    for (int i = 0; i < 31191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31191(int& val) {
    const int x = 31191;
    std::cout << 31191;
    for (int i = 0; i < 31191; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31192(int& val) {
    func_in_31192(val);
    const int x = 31192;
    std::cout << 31192;
    for (int i = 0; i < 31192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31192(int& val) {
    const int x = 31192;
    std::cout << 31192;
    for (int i = 0; i < 31192; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31193(int& val) {
    func_in_31193(val);
    const int x = 31193;
    std::cout << 31193;
    for (int i = 0; i < 31193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31193(int& val) {
    const int x = 31193;
    std::cout << 31193;
    for (int i = 0; i < 31193; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31194(int& val) {
    func_in_31194(val);
    const int x = 31194;
    std::cout << 31194;
    for (int i = 0; i < 31194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31194(int& val) {
    const int x = 31194;
    std::cout << 31194;
    for (int i = 0; i < 31194; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31195(int& val) {
    func_in_31195(val);
    const int x = 31195;
    std::cout << 31195;
    for (int i = 0; i < 31195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31195(int& val) {
    const int x = 31195;
    std::cout << 31195;
    for (int i = 0; i < 31195; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31196(int& val) {
    func_in_31196(val);
    const int x = 31196;
    std::cout << 31196;
    for (int i = 0; i < 31196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31196(int& val) {
    const int x = 31196;
    std::cout << 31196;
    for (int i = 0; i < 31196; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31197(int& val) {
    func_in_31197(val);
    const int x = 31197;
    std::cout << 31197;
    for (int i = 0; i < 31197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31197(int& val) {
    const int x = 31197;
    std::cout << 31197;
    for (int i = 0; i < 31197; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31198(int& val) {
    func_in_31198(val);
    const int x = 31198;
    std::cout << 31198;
    for (int i = 0; i < 31198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31198(int& val) {
    const int x = 31198;
    std::cout << 31198;
    for (int i = 0; i < 31198; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31199(int& val) {
    func_in_31199(val);
    const int x = 31199;
    std::cout << 31199;
    for (int i = 0; i < 31199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31199(int& val) {
    const int x = 31199;
    std::cout << 31199;
    for (int i = 0; i < 31199; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31200(int& val) {
    func_in_31200(val);
    const int x = 31200;
    std::cout << 31200;
    for (int i = 0; i < 31200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31200(int& val) {
    const int x = 31200;
    std::cout << 31200;
    for (int i = 0; i < 31200; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31201(int& val) {
    func_in_31201(val);
    const int x = 31201;
    std::cout << 31201;
    for (int i = 0; i < 31201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31201(int& val) {
    const int x = 31201;
    std::cout << 31201;
    for (int i = 0; i < 31201; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31202(int& val) {
    func_in_31202(val);
    const int x = 31202;
    std::cout << 31202;
    for (int i = 0; i < 31202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31202(int& val) {
    const int x = 31202;
    std::cout << 31202;
    for (int i = 0; i < 31202; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31203(int& val) {
    func_in_31203(val);
    const int x = 31203;
    std::cout << 31203;
    for (int i = 0; i < 31203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31203(int& val) {
    const int x = 31203;
    std::cout << 31203;
    for (int i = 0; i < 31203; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31204(int& val) {
    func_in_31204(val);
    const int x = 31204;
    std::cout << 31204;
    for (int i = 0; i < 31204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31204(int& val) {
    const int x = 31204;
    std::cout << 31204;
    for (int i = 0; i < 31204; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31205(int& val) {
    func_in_31205(val);
    const int x = 31205;
    std::cout << 31205;
    for (int i = 0; i < 31205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31205(int& val) {
    const int x = 31205;
    std::cout << 31205;
    for (int i = 0; i < 31205; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31206(int& val) {
    func_in_31206(val);
    const int x = 31206;
    std::cout << 31206;
    for (int i = 0; i < 31206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31206(int& val) {
    const int x = 31206;
    std::cout << 31206;
    for (int i = 0; i < 31206; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31207(int& val) {
    func_in_31207(val);
    const int x = 31207;
    std::cout << 31207;
    for (int i = 0; i < 31207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31207(int& val) {
    const int x = 31207;
    std::cout << 31207;
    for (int i = 0; i < 31207; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31208(int& val) {
    func_in_31208(val);
    const int x = 31208;
    std::cout << 31208;
    for (int i = 0; i < 31208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31208(int& val) {
    const int x = 31208;
    std::cout << 31208;
    for (int i = 0; i < 31208; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31209(int& val) {
    func_in_31209(val);
    const int x = 31209;
    std::cout << 31209;
    for (int i = 0; i < 31209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31209(int& val) {
    const int x = 31209;
    std::cout << 31209;
    for (int i = 0; i < 31209; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31210(int& val) {
    func_in_31210(val);
    const int x = 31210;
    std::cout << 31210;
    for (int i = 0; i < 31210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31210(int& val) {
    const int x = 31210;
    std::cout << 31210;
    for (int i = 0; i < 31210; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31211(int& val) {
    func_in_31211(val);
    const int x = 31211;
    std::cout << 31211;
    for (int i = 0; i < 31211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31211(int& val) {
    const int x = 31211;
    std::cout << 31211;
    for (int i = 0; i < 31211; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31212(int& val) {
    func_in_31212(val);
    const int x = 31212;
    std::cout << 31212;
    for (int i = 0; i < 31212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31212(int& val) {
    const int x = 31212;
    std::cout << 31212;
    for (int i = 0; i < 31212; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31213(int& val) {
    func_in_31213(val);
    const int x = 31213;
    std::cout << 31213;
    for (int i = 0; i < 31213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31213(int& val) {
    const int x = 31213;
    std::cout << 31213;
    for (int i = 0; i < 31213; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31214(int& val) {
    func_in_31214(val);
    const int x = 31214;
    std::cout << 31214;
    for (int i = 0; i < 31214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31214(int& val) {
    const int x = 31214;
    std::cout << 31214;
    for (int i = 0; i < 31214; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31215(int& val) {
    func_in_31215(val);
    const int x = 31215;
    std::cout << 31215;
    for (int i = 0; i < 31215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31215(int& val) {
    const int x = 31215;
    std::cout << 31215;
    for (int i = 0; i < 31215; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31216(int& val) {
    func_in_31216(val);
    const int x = 31216;
    std::cout << 31216;
    for (int i = 0; i < 31216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31216(int& val) {
    const int x = 31216;
    std::cout << 31216;
    for (int i = 0; i < 31216; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31217(int& val) {
    func_in_31217(val);
    const int x = 31217;
    std::cout << 31217;
    for (int i = 0; i < 31217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31217(int& val) {
    const int x = 31217;
    std::cout << 31217;
    for (int i = 0; i < 31217; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31218(int& val) {
    func_in_31218(val);
    const int x = 31218;
    std::cout << 31218;
    for (int i = 0; i < 31218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31218(int& val) {
    const int x = 31218;
    std::cout << 31218;
    for (int i = 0; i < 31218; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31219(int& val) {
    func_in_31219(val);
    const int x = 31219;
    std::cout << 31219;
    for (int i = 0; i < 31219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31219(int& val) {
    const int x = 31219;
    std::cout << 31219;
    for (int i = 0; i < 31219; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31220(int& val) {
    func_in_31220(val);
    const int x = 31220;
    std::cout << 31220;
    for (int i = 0; i < 31220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31220(int& val) {
    const int x = 31220;
    std::cout << 31220;
    for (int i = 0; i < 31220; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31221(int& val) {
    func_in_31221(val);
    const int x = 31221;
    std::cout << 31221;
    for (int i = 0; i < 31221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31221(int& val) {
    const int x = 31221;
    std::cout << 31221;
    for (int i = 0; i < 31221; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31222(int& val) {
    func_in_31222(val);
    const int x = 31222;
    std::cout << 31222;
    for (int i = 0; i < 31222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31222(int& val) {
    const int x = 31222;
    std::cout << 31222;
    for (int i = 0; i < 31222; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31223(int& val) {
    func_in_31223(val);
    const int x = 31223;
    std::cout << 31223;
    for (int i = 0; i < 31223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31223(int& val) {
    const int x = 31223;
    std::cout << 31223;
    for (int i = 0; i < 31223; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31224(int& val) {
    func_in_31224(val);
    const int x = 31224;
    std::cout << 31224;
    for (int i = 0; i < 31224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31224(int& val) {
    const int x = 31224;
    std::cout << 31224;
    for (int i = 0; i < 31224; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31225(int& val) {
    func_in_31225(val);
    const int x = 31225;
    std::cout << 31225;
    for (int i = 0; i < 31225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31225(int& val) {
    const int x = 31225;
    std::cout << 31225;
    for (int i = 0; i < 31225; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31226(int& val) {
    func_in_31226(val);
    const int x = 31226;
    std::cout << 31226;
    for (int i = 0; i < 31226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31226(int& val) {
    const int x = 31226;
    std::cout << 31226;
    for (int i = 0; i < 31226; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31227(int& val) {
    func_in_31227(val);
    const int x = 31227;
    std::cout << 31227;
    for (int i = 0; i < 31227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31227(int& val) {
    const int x = 31227;
    std::cout << 31227;
    for (int i = 0; i < 31227; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31228(int& val) {
    func_in_31228(val);
    const int x = 31228;
    std::cout << 31228;
    for (int i = 0; i < 31228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31228(int& val) {
    const int x = 31228;
    std::cout << 31228;
    for (int i = 0; i < 31228; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31229(int& val) {
    func_in_31229(val);
    const int x = 31229;
    std::cout << 31229;
    for (int i = 0; i < 31229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31229(int& val) {
    const int x = 31229;
    std::cout << 31229;
    for (int i = 0; i < 31229; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31230(int& val) {
    func_in_31230(val);
    const int x = 31230;
    std::cout << 31230;
    for (int i = 0; i < 31230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31230(int& val) {
    const int x = 31230;
    std::cout << 31230;
    for (int i = 0; i < 31230; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31231(int& val) {
    func_in_31231(val);
    const int x = 31231;
    std::cout << 31231;
    for (int i = 0; i < 31231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31231(int& val) {
    const int x = 31231;
    std::cout << 31231;
    for (int i = 0; i < 31231; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31232(int& val) {
    func_in_31232(val);
    const int x = 31232;
    std::cout << 31232;
    for (int i = 0; i < 31232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31232(int& val) {
    const int x = 31232;
    std::cout << 31232;
    for (int i = 0; i < 31232; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31233(int& val) {
    func_in_31233(val);
    const int x = 31233;
    std::cout << 31233;
    for (int i = 0; i < 31233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31233(int& val) {
    const int x = 31233;
    std::cout << 31233;
    for (int i = 0; i < 31233; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31234(int& val) {
    func_in_31234(val);
    const int x = 31234;
    std::cout << 31234;
    for (int i = 0; i < 31234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31234(int& val) {
    const int x = 31234;
    std::cout << 31234;
    for (int i = 0; i < 31234; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31235(int& val) {
    func_in_31235(val);
    const int x = 31235;
    std::cout << 31235;
    for (int i = 0; i < 31235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31235(int& val) {
    const int x = 31235;
    std::cout << 31235;
    for (int i = 0; i < 31235; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31236(int& val) {
    func_in_31236(val);
    const int x = 31236;
    std::cout << 31236;
    for (int i = 0; i < 31236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31236(int& val) {
    const int x = 31236;
    std::cout << 31236;
    for (int i = 0; i < 31236; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31237(int& val) {
    func_in_31237(val);
    const int x = 31237;
    std::cout << 31237;
    for (int i = 0; i < 31237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31237(int& val) {
    const int x = 31237;
    std::cout << 31237;
    for (int i = 0; i < 31237; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31238(int& val) {
    func_in_31238(val);
    const int x = 31238;
    std::cout << 31238;
    for (int i = 0; i < 31238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31238(int& val) {
    const int x = 31238;
    std::cout << 31238;
    for (int i = 0; i < 31238; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31239(int& val) {
    func_in_31239(val);
    const int x = 31239;
    std::cout << 31239;
    for (int i = 0; i < 31239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31239(int& val) {
    const int x = 31239;
    std::cout << 31239;
    for (int i = 0; i < 31239; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31240(int& val) {
    func_in_31240(val);
    const int x = 31240;
    std::cout << 31240;
    for (int i = 0; i < 31240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31240(int& val) {
    const int x = 31240;
    std::cout << 31240;
    for (int i = 0; i < 31240; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31241(int& val) {
    func_in_31241(val);
    const int x = 31241;
    std::cout << 31241;
    for (int i = 0; i < 31241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31241(int& val) {
    const int x = 31241;
    std::cout << 31241;
    for (int i = 0; i < 31241; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31242(int& val) {
    func_in_31242(val);
    const int x = 31242;
    std::cout << 31242;
    for (int i = 0; i < 31242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31242(int& val) {
    const int x = 31242;
    std::cout << 31242;
    for (int i = 0; i < 31242; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31243(int& val) {
    func_in_31243(val);
    const int x = 31243;
    std::cout << 31243;
    for (int i = 0; i < 31243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31243(int& val) {
    const int x = 31243;
    std::cout << 31243;
    for (int i = 0; i < 31243; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31244(int& val) {
    func_in_31244(val);
    const int x = 31244;
    std::cout << 31244;
    for (int i = 0; i < 31244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31244(int& val) {
    const int x = 31244;
    std::cout << 31244;
    for (int i = 0; i < 31244; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31245(int& val) {
    func_in_31245(val);
    const int x = 31245;
    std::cout << 31245;
    for (int i = 0; i < 31245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31245(int& val) {
    const int x = 31245;
    std::cout << 31245;
    for (int i = 0; i < 31245; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31246(int& val) {
    func_in_31246(val);
    const int x = 31246;
    std::cout << 31246;
    for (int i = 0; i < 31246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31246(int& val) {
    const int x = 31246;
    std::cout << 31246;
    for (int i = 0; i < 31246; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31247(int& val) {
    func_in_31247(val);
    const int x = 31247;
    std::cout << 31247;
    for (int i = 0; i < 31247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31247(int& val) {
    const int x = 31247;
    std::cout << 31247;
    for (int i = 0; i < 31247; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31248(int& val) {
    func_in_31248(val);
    const int x = 31248;
    std::cout << 31248;
    for (int i = 0; i < 31248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31248(int& val) {
    const int x = 31248;
    std::cout << 31248;
    for (int i = 0; i < 31248; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31249(int& val) {
    func_in_31249(val);
    const int x = 31249;
    std::cout << 31249;
    for (int i = 0; i < 31249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31249(int& val) {
    const int x = 31249;
    std::cout << 31249;
    for (int i = 0; i < 31249; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31250(int& val) {
    func_in_31250(val);
    const int x = 31250;
    std::cout << 31250;
    for (int i = 0; i < 31250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31250(int& val) {
    const int x = 31250;
    std::cout << 31250;
    for (int i = 0; i < 31250; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31251(int& val) {
    func_in_31251(val);
    const int x = 31251;
    std::cout << 31251;
    for (int i = 0; i < 31251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31251(int& val) {
    const int x = 31251;
    std::cout << 31251;
    for (int i = 0; i < 31251; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31252(int& val) {
    func_in_31252(val);
    const int x = 31252;
    std::cout << 31252;
    for (int i = 0; i < 31252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31252(int& val) {
    const int x = 31252;
    std::cout << 31252;
    for (int i = 0; i < 31252; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31253(int& val) {
    func_in_31253(val);
    const int x = 31253;
    std::cout << 31253;
    for (int i = 0; i < 31253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31253(int& val) {
    const int x = 31253;
    std::cout << 31253;
    for (int i = 0; i < 31253; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31254(int& val) {
    func_in_31254(val);
    const int x = 31254;
    std::cout << 31254;
    for (int i = 0; i < 31254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31254(int& val) {
    const int x = 31254;
    std::cout << 31254;
    for (int i = 0; i < 31254; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31255(int& val) {
    func_in_31255(val);
    const int x = 31255;
    std::cout << 31255;
    for (int i = 0; i < 31255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31255(int& val) {
    const int x = 31255;
    std::cout << 31255;
    for (int i = 0; i < 31255; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31256(int& val) {
    func_in_31256(val);
    const int x = 31256;
    std::cout << 31256;
    for (int i = 0; i < 31256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31256(int& val) {
    const int x = 31256;
    std::cout << 31256;
    for (int i = 0; i < 31256; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31257(int& val) {
    func_in_31257(val);
    const int x = 31257;
    std::cout << 31257;
    for (int i = 0; i < 31257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31257(int& val) {
    const int x = 31257;
    std::cout << 31257;
    for (int i = 0; i < 31257; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31258(int& val) {
    func_in_31258(val);
    const int x = 31258;
    std::cout << 31258;
    for (int i = 0; i < 31258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31258(int& val) {
    const int x = 31258;
    std::cout << 31258;
    for (int i = 0; i < 31258; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31259(int& val) {
    func_in_31259(val);
    const int x = 31259;
    std::cout << 31259;
    for (int i = 0; i < 31259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31259(int& val) {
    const int x = 31259;
    std::cout << 31259;
    for (int i = 0; i < 31259; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31260(int& val) {
    func_in_31260(val);
    const int x = 31260;
    std::cout << 31260;
    for (int i = 0; i < 31260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31260(int& val) {
    const int x = 31260;
    std::cout << 31260;
    for (int i = 0; i < 31260; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31261(int& val) {
    func_in_31261(val);
    const int x = 31261;
    std::cout << 31261;
    for (int i = 0; i < 31261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31261(int& val) {
    const int x = 31261;
    std::cout << 31261;
    for (int i = 0; i < 31261; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31262(int& val) {
    func_in_31262(val);
    const int x = 31262;
    std::cout << 31262;
    for (int i = 0; i < 31262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31262(int& val) {
    const int x = 31262;
    std::cout << 31262;
    for (int i = 0; i < 31262; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31263(int& val) {
    func_in_31263(val);
    const int x = 31263;
    std::cout << 31263;
    for (int i = 0; i < 31263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31263(int& val) {
    const int x = 31263;
    std::cout << 31263;
    for (int i = 0; i < 31263; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31264(int& val) {
    func_in_31264(val);
    const int x = 31264;
    std::cout << 31264;
    for (int i = 0; i < 31264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31264(int& val) {
    const int x = 31264;
    std::cout << 31264;
    for (int i = 0; i < 31264; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31265(int& val) {
    func_in_31265(val);
    const int x = 31265;
    std::cout << 31265;
    for (int i = 0; i < 31265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31265(int& val) {
    const int x = 31265;
    std::cout << 31265;
    for (int i = 0; i < 31265; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31266(int& val) {
    func_in_31266(val);
    const int x = 31266;
    std::cout << 31266;
    for (int i = 0; i < 31266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31266(int& val) {
    const int x = 31266;
    std::cout << 31266;
    for (int i = 0; i < 31266; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31267(int& val) {
    func_in_31267(val);
    const int x = 31267;
    std::cout << 31267;
    for (int i = 0; i < 31267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31267(int& val) {
    const int x = 31267;
    std::cout << 31267;
    for (int i = 0; i < 31267; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31268(int& val) {
    func_in_31268(val);
    const int x = 31268;
    std::cout << 31268;
    for (int i = 0; i < 31268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31268(int& val) {
    const int x = 31268;
    std::cout << 31268;
    for (int i = 0; i < 31268; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31269(int& val) {
    func_in_31269(val);
    const int x = 31269;
    std::cout << 31269;
    for (int i = 0; i < 31269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31269(int& val) {
    const int x = 31269;
    std::cout << 31269;
    for (int i = 0; i < 31269; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31270(int& val) {
    func_in_31270(val);
    const int x = 31270;
    std::cout << 31270;
    for (int i = 0; i < 31270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31270(int& val) {
    const int x = 31270;
    std::cout << 31270;
    for (int i = 0; i < 31270; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31271(int& val) {
    func_in_31271(val);
    const int x = 31271;
    std::cout << 31271;
    for (int i = 0; i < 31271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31271(int& val) {
    const int x = 31271;
    std::cout << 31271;
    for (int i = 0; i < 31271; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31272(int& val) {
    func_in_31272(val);
    const int x = 31272;
    std::cout << 31272;
    for (int i = 0; i < 31272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31272(int& val) {
    const int x = 31272;
    std::cout << 31272;
    for (int i = 0; i < 31272; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31273(int& val) {
    func_in_31273(val);
    const int x = 31273;
    std::cout << 31273;
    for (int i = 0; i < 31273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31273(int& val) {
    const int x = 31273;
    std::cout << 31273;
    for (int i = 0; i < 31273; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31274(int& val) {
    func_in_31274(val);
    const int x = 31274;
    std::cout << 31274;
    for (int i = 0; i < 31274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31274(int& val) {
    const int x = 31274;
    std::cout << 31274;
    for (int i = 0; i < 31274; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31275(int& val) {
    func_in_31275(val);
    const int x = 31275;
    std::cout << 31275;
    for (int i = 0; i < 31275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31275(int& val) {
    const int x = 31275;
    std::cout << 31275;
    for (int i = 0; i < 31275; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31276(int& val) {
    func_in_31276(val);
    const int x = 31276;
    std::cout << 31276;
    for (int i = 0; i < 31276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31276(int& val) {
    const int x = 31276;
    std::cout << 31276;
    for (int i = 0; i < 31276; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31277(int& val) {
    func_in_31277(val);
    const int x = 31277;
    std::cout << 31277;
    for (int i = 0; i < 31277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31277(int& val) {
    const int x = 31277;
    std::cout << 31277;
    for (int i = 0; i < 31277; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31278(int& val) {
    func_in_31278(val);
    const int x = 31278;
    std::cout << 31278;
    for (int i = 0; i < 31278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31278(int& val) {
    const int x = 31278;
    std::cout << 31278;
    for (int i = 0; i < 31278; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31279(int& val) {
    func_in_31279(val);
    const int x = 31279;
    std::cout << 31279;
    for (int i = 0; i < 31279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31279(int& val) {
    const int x = 31279;
    std::cout << 31279;
    for (int i = 0; i < 31279; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31280(int& val) {
    func_in_31280(val);
    const int x = 31280;
    std::cout << 31280;
    for (int i = 0; i < 31280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31280(int& val) {
    const int x = 31280;
    std::cout << 31280;
    for (int i = 0; i < 31280; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31281(int& val) {
    func_in_31281(val);
    const int x = 31281;
    std::cout << 31281;
    for (int i = 0; i < 31281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31281(int& val) {
    const int x = 31281;
    std::cout << 31281;
    for (int i = 0; i < 31281; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31282(int& val) {
    func_in_31282(val);
    const int x = 31282;
    std::cout << 31282;
    for (int i = 0; i < 31282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31282(int& val) {
    const int x = 31282;
    std::cout << 31282;
    for (int i = 0; i < 31282; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31283(int& val) {
    func_in_31283(val);
    const int x = 31283;
    std::cout << 31283;
    for (int i = 0; i < 31283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31283(int& val) {
    const int x = 31283;
    std::cout << 31283;
    for (int i = 0; i < 31283; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31284(int& val) {
    func_in_31284(val);
    const int x = 31284;
    std::cout << 31284;
    for (int i = 0; i < 31284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31284(int& val) {
    const int x = 31284;
    std::cout << 31284;
    for (int i = 0; i < 31284; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31285(int& val) {
    func_in_31285(val);
    const int x = 31285;
    std::cout << 31285;
    for (int i = 0; i < 31285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31285(int& val) {
    const int x = 31285;
    std::cout << 31285;
    for (int i = 0; i < 31285; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31286(int& val) {
    func_in_31286(val);
    const int x = 31286;
    std::cout << 31286;
    for (int i = 0; i < 31286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31286(int& val) {
    const int x = 31286;
    std::cout << 31286;
    for (int i = 0; i < 31286; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31287(int& val) {
    func_in_31287(val);
    const int x = 31287;
    std::cout << 31287;
    for (int i = 0; i < 31287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31287(int& val) {
    const int x = 31287;
    std::cout << 31287;
    for (int i = 0; i < 31287; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31288(int& val) {
    func_in_31288(val);
    const int x = 31288;
    std::cout << 31288;
    for (int i = 0; i < 31288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31288(int& val) {
    const int x = 31288;
    std::cout << 31288;
    for (int i = 0; i < 31288; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31289(int& val) {
    func_in_31289(val);
    const int x = 31289;
    std::cout << 31289;
    for (int i = 0; i < 31289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31289(int& val) {
    const int x = 31289;
    std::cout << 31289;
    for (int i = 0; i < 31289; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31290(int& val) {
    func_in_31290(val);
    const int x = 31290;
    std::cout << 31290;
    for (int i = 0; i < 31290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31290(int& val) {
    const int x = 31290;
    std::cout << 31290;
    for (int i = 0; i < 31290; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31291(int& val) {
    func_in_31291(val);
    const int x = 31291;
    std::cout << 31291;
    for (int i = 0; i < 31291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31291(int& val) {
    const int x = 31291;
    std::cout << 31291;
    for (int i = 0; i < 31291; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31292(int& val) {
    func_in_31292(val);
    const int x = 31292;
    std::cout << 31292;
    for (int i = 0; i < 31292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31292(int& val) {
    const int x = 31292;
    std::cout << 31292;
    for (int i = 0; i < 31292; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31293(int& val) {
    func_in_31293(val);
    const int x = 31293;
    std::cout << 31293;
    for (int i = 0; i < 31293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31293(int& val) {
    const int x = 31293;
    std::cout << 31293;
    for (int i = 0; i < 31293; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31294(int& val) {
    func_in_31294(val);
    const int x = 31294;
    std::cout << 31294;
    for (int i = 0; i < 31294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31294(int& val) {
    const int x = 31294;
    std::cout << 31294;
    for (int i = 0; i < 31294; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31295(int& val) {
    func_in_31295(val);
    const int x = 31295;
    std::cout << 31295;
    for (int i = 0; i < 31295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31295(int& val) {
    const int x = 31295;
    std::cout << 31295;
    for (int i = 0; i < 31295; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31296(int& val) {
    func_in_31296(val);
    const int x = 31296;
    std::cout << 31296;
    for (int i = 0; i < 31296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31296(int& val) {
    const int x = 31296;
    std::cout << 31296;
    for (int i = 0; i < 31296; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31297(int& val) {
    func_in_31297(val);
    const int x = 31297;
    std::cout << 31297;
    for (int i = 0; i < 31297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31297(int& val) {
    const int x = 31297;
    std::cout << 31297;
    for (int i = 0; i < 31297; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31298(int& val) {
    func_in_31298(val);
    const int x = 31298;
    std::cout << 31298;
    for (int i = 0; i < 31298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31298(int& val) {
    const int x = 31298;
    std::cout << 31298;
    for (int i = 0; i < 31298; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31299(int& val) {
    func_in_31299(val);
    const int x = 31299;
    std::cout << 31299;
    for (int i = 0; i < 31299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31299(int& val) {
    const int x = 31299;
    std::cout << 31299;
    for (int i = 0; i < 31299; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31300(int& val) {
    func_in_31300(val);
    const int x = 31300;
    std::cout << 31300;
    for (int i = 0; i < 31300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31300(int& val) {
    const int x = 31300;
    std::cout << 31300;
    for (int i = 0; i < 31300; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31301(int& val) {
    func_in_31301(val);
    const int x = 31301;
    std::cout << 31301;
    for (int i = 0; i < 31301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31301(int& val) {
    const int x = 31301;
    std::cout << 31301;
    for (int i = 0; i < 31301; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31302(int& val) {
    func_in_31302(val);
    const int x = 31302;
    std::cout << 31302;
    for (int i = 0; i < 31302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31302(int& val) {
    const int x = 31302;
    std::cout << 31302;
    for (int i = 0; i < 31302; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31303(int& val) {
    func_in_31303(val);
    const int x = 31303;
    std::cout << 31303;
    for (int i = 0; i < 31303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31303(int& val) {
    const int x = 31303;
    std::cout << 31303;
    for (int i = 0; i < 31303; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31304(int& val) {
    func_in_31304(val);
    const int x = 31304;
    std::cout << 31304;
    for (int i = 0; i < 31304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31304(int& val) {
    const int x = 31304;
    std::cout << 31304;
    for (int i = 0; i < 31304; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31305(int& val) {
    func_in_31305(val);
    const int x = 31305;
    std::cout << 31305;
    for (int i = 0; i < 31305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31305(int& val) {
    const int x = 31305;
    std::cout << 31305;
    for (int i = 0; i < 31305; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31306(int& val) {
    func_in_31306(val);
    const int x = 31306;
    std::cout << 31306;
    for (int i = 0; i < 31306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31306(int& val) {
    const int x = 31306;
    std::cout << 31306;
    for (int i = 0; i < 31306; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31307(int& val) {
    func_in_31307(val);
    const int x = 31307;
    std::cout << 31307;
    for (int i = 0; i < 31307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31307(int& val) {
    const int x = 31307;
    std::cout << 31307;
    for (int i = 0; i < 31307; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31308(int& val) {
    func_in_31308(val);
    const int x = 31308;
    std::cout << 31308;
    for (int i = 0; i < 31308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31308(int& val) {
    const int x = 31308;
    std::cout << 31308;
    for (int i = 0; i < 31308; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31309(int& val) {
    func_in_31309(val);
    const int x = 31309;
    std::cout << 31309;
    for (int i = 0; i < 31309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31309(int& val) {
    const int x = 31309;
    std::cout << 31309;
    for (int i = 0; i < 31309; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31310(int& val) {
    func_in_31310(val);
    const int x = 31310;
    std::cout << 31310;
    for (int i = 0; i < 31310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31310(int& val) {
    const int x = 31310;
    std::cout << 31310;
    for (int i = 0; i < 31310; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31311(int& val) {
    func_in_31311(val);
    const int x = 31311;
    std::cout << 31311;
    for (int i = 0; i < 31311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31311(int& val) {
    const int x = 31311;
    std::cout << 31311;
    for (int i = 0; i < 31311; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31312(int& val) {
    func_in_31312(val);
    const int x = 31312;
    std::cout << 31312;
    for (int i = 0; i < 31312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31312(int& val) {
    const int x = 31312;
    std::cout << 31312;
    for (int i = 0; i < 31312; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31313(int& val) {
    func_in_31313(val);
    const int x = 31313;
    std::cout << 31313;
    for (int i = 0; i < 31313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31313(int& val) {
    const int x = 31313;
    std::cout << 31313;
    for (int i = 0; i < 31313; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31314(int& val) {
    func_in_31314(val);
    const int x = 31314;
    std::cout << 31314;
    for (int i = 0; i < 31314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31314(int& val) {
    const int x = 31314;
    std::cout << 31314;
    for (int i = 0; i < 31314; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31315(int& val) {
    func_in_31315(val);
    const int x = 31315;
    std::cout << 31315;
    for (int i = 0; i < 31315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31315(int& val) {
    const int x = 31315;
    std::cout << 31315;
    for (int i = 0; i < 31315; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31316(int& val) {
    func_in_31316(val);
    const int x = 31316;
    std::cout << 31316;
    for (int i = 0; i < 31316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31316(int& val) {
    const int x = 31316;
    std::cout << 31316;
    for (int i = 0; i < 31316; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31317(int& val) {
    func_in_31317(val);
    const int x = 31317;
    std::cout << 31317;
    for (int i = 0; i < 31317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31317(int& val) {
    const int x = 31317;
    std::cout << 31317;
    for (int i = 0; i < 31317; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31318(int& val) {
    func_in_31318(val);
    const int x = 31318;
    std::cout << 31318;
    for (int i = 0; i < 31318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31318(int& val) {
    const int x = 31318;
    std::cout << 31318;
    for (int i = 0; i < 31318; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31319(int& val) {
    func_in_31319(val);
    const int x = 31319;
    std::cout << 31319;
    for (int i = 0; i < 31319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31319(int& val) {
    const int x = 31319;
    std::cout << 31319;
    for (int i = 0; i < 31319; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31320(int& val) {
    func_in_31320(val);
    const int x = 31320;
    std::cout << 31320;
    for (int i = 0; i < 31320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31320(int& val) {
    const int x = 31320;
    std::cout << 31320;
    for (int i = 0; i < 31320; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31321(int& val) {
    func_in_31321(val);
    const int x = 31321;
    std::cout << 31321;
    for (int i = 0; i < 31321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31321(int& val) {
    const int x = 31321;
    std::cout << 31321;
    for (int i = 0; i < 31321; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31322(int& val) {
    func_in_31322(val);
    const int x = 31322;
    std::cout << 31322;
    for (int i = 0; i < 31322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31322(int& val) {
    const int x = 31322;
    std::cout << 31322;
    for (int i = 0; i < 31322; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31323(int& val) {
    func_in_31323(val);
    const int x = 31323;
    std::cout << 31323;
    for (int i = 0; i < 31323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31323(int& val) {
    const int x = 31323;
    std::cout << 31323;
    for (int i = 0; i < 31323; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31324(int& val) {
    func_in_31324(val);
    const int x = 31324;
    std::cout << 31324;
    for (int i = 0; i < 31324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31324(int& val) {
    const int x = 31324;
    std::cout << 31324;
    for (int i = 0; i < 31324; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31325(int& val) {
    func_in_31325(val);
    const int x = 31325;
    std::cout << 31325;
    for (int i = 0; i < 31325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31325(int& val) {
    const int x = 31325;
    std::cout << 31325;
    for (int i = 0; i < 31325; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31326(int& val) {
    func_in_31326(val);
    const int x = 31326;
    std::cout << 31326;
    for (int i = 0; i < 31326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31326(int& val) {
    const int x = 31326;
    std::cout << 31326;
    for (int i = 0; i < 31326; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31327(int& val) {
    func_in_31327(val);
    const int x = 31327;
    std::cout << 31327;
    for (int i = 0; i < 31327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31327(int& val) {
    const int x = 31327;
    std::cout << 31327;
    for (int i = 0; i < 31327; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31328(int& val) {
    func_in_31328(val);
    const int x = 31328;
    std::cout << 31328;
    for (int i = 0; i < 31328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31328(int& val) {
    const int x = 31328;
    std::cout << 31328;
    for (int i = 0; i < 31328; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31329(int& val) {
    func_in_31329(val);
    const int x = 31329;
    std::cout << 31329;
    for (int i = 0; i < 31329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31329(int& val) {
    const int x = 31329;
    std::cout << 31329;
    for (int i = 0; i < 31329; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31330(int& val) {
    func_in_31330(val);
    const int x = 31330;
    std::cout << 31330;
    for (int i = 0; i < 31330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31330(int& val) {
    const int x = 31330;
    std::cout << 31330;
    for (int i = 0; i < 31330; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31331(int& val) {
    func_in_31331(val);
    const int x = 31331;
    std::cout << 31331;
    for (int i = 0; i < 31331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31331(int& val) {
    const int x = 31331;
    std::cout << 31331;
    for (int i = 0; i < 31331; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31332(int& val) {
    func_in_31332(val);
    const int x = 31332;
    std::cout << 31332;
    for (int i = 0; i < 31332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31332(int& val) {
    const int x = 31332;
    std::cout << 31332;
    for (int i = 0; i < 31332; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31333(int& val) {
    func_in_31333(val);
    const int x = 31333;
    std::cout << 31333;
    for (int i = 0; i < 31333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31333(int& val) {
    const int x = 31333;
    std::cout << 31333;
    for (int i = 0; i < 31333; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31334(int& val) {
    func_in_31334(val);
    const int x = 31334;
    std::cout << 31334;
    for (int i = 0; i < 31334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31334(int& val) {
    const int x = 31334;
    std::cout << 31334;
    for (int i = 0; i < 31334; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31335(int& val) {
    func_in_31335(val);
    const int x = 31335;
    std::cout << 31335;
    for (int i = 0; i < 31335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31335(int& val) {
    const int x = 31335;
    std::cout << 31335;
    for (int i = 0; i < 31335; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31336(int& val) {
    func_in_31336(val);
    const int x = 31336;
    std::cout << 31336;
    for (int i = 0; i < 31336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31336(int& val) {
    const int x = 31336;
    std::cout << 31336;
    for (int i = 0; i < 31336; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31337(int& val) {
    func_in_31337(val);
    const int x = 31337;
    std::cout << 31337;
    for (int i = 0; i < 31337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31337(int& val) {
    const int x = 31337;
    std::cout << 31337;
    for (int i = 0; i < 31337; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31338(int& val) {
    func_in_31338(val);
    const int x = 31338;
    std::cout << 31338;
    for (int i = 0; i < 31338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31338(int& val) {
    const int x = 31338;
    std::cout << 31338;
    for (int i = 0; i < 31338; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31339(int& val) {
    func_in_31339(val);
    const int x = 31339;
    std::cout << 31339;
    for (int i = 0; i < 31339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31339(int& val) {
    const int x = 31339;
    std::cout << 31339;
    for (int i = 0; i < 31339; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31340(int& val) {
    func_in_31340(val);
    const int x = 31340;
    std::cout << 31340;
    for (int i = 0; i < 31340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31340(int& val) {
    const int x = 31340;
    std::cout << 31340;
    for (int i = 0; i < 31340; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31341(int& val) {
    func_in_31341(val);
    const int x = 31341;
    std::cout << 31341;
    for (int i = 0; i < 31341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31341(int& val) {
    const int x = 31341;
    std::cout << 31341;
    for (int i = 0; i < 31341; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31342(int& val) {
    func_in_31342(val);
    const int x = 31342;
    std::cout << 31342;
    for (int i = 0; i < 31342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31342(int& val) {
    const int x = 31342;
    std::cout << 31342;
    for (int i = 0; i < 31342; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31343(int& val) {
    func_in_31343(val);
    const int x = 31343;
    std::cout << 31343;
    for (int i = 0; i < 31343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31343(int& val) {
    const int x = 31343;
    std::cout << 31343;
    for (int i = 0; i < 31343; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31344(int& val) {
    func_in_31344(val);
    const int x = 31344;
    std::cout << 31344;
    for (int i = 0; i < 31344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31344(int& val) {
    const int x = 31344;
    std::cout << 31344;
    for (int i = 0; i < 31344; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31345(int& val) {
    func_in_31345(val);
    const int x = 31345;
    std::cout << 31345;
    for (int i = 0; i < 31345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31345(int& val) {
    const int x = 31345;
    std::cout << 31345;
    for (int i = 0; i < 31345; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31346(int& val) {
    func_in_31346(val);
    const int x = 31346;
    std::cout << 31346;
    for (int i = 0; i < 31346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31346(int& val) {
    const int x = 31346;
    std::cout << 31346;
    for (int i = 0; i < 31346; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31347(int& val) {
    func_in_31347(val);
    const int x = 31347;
    std::cout << 31347;
    for (int i = 0; i < 31347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31347(int& val) {
    const int x = 31347;
    std::cout << 31347;
    for (int i = 0; i < 31347; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31348(int& val) {
    func_in_31348(val);
    const int x = 31348;
    std::cout << 31348;
    for (int i = 0; i < 31348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31348(int& val) {
    const int x = 31348;
    std::cout << 31348;
    for (int i = 0; i < 31348; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31349(int& val) {
    func_in_31349(val);
    const int x = 31349;
    std::cout << 31349;
    for (int i = 0; i < 31349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31349(int& val) {
    const int x = 31349;
    std::cout << 31349;
    for (int i = 0; i < 31349; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31350(int& val) {
    func_in_31350(val);
    const int x = 31350;
    std::cout << 31350;
    for (int i = 0; i < 31350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31350(int& val) {
    const int x = 31350;
    std::cout << 31350;
    for (int i = 0; i < 31350; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31351(int& val) {
    func_in_31351(val);
    const int x = 31351;
    std::cout << 31351;
    for (int i = 0; i < 31351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31351(int& val) {
    const int x = 31351;
    std::cout << 31351;
    for (int i = 0; i < 31351; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31352(int& val) {
    func_in_31352(val);
    const int x = 31352;
    std::cout << 31352;
    for (int i = 0; i < 31352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31352(int& val) {
    const int x = 31352;
    std::cout << 31352;
    for (int i = 0; i < 31352; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31353(int& val) {
    func_in_31353(val);
    const int x = 31353;
    std::cout << 31353;
    for (int i = 0; i < 31353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31353(int& val) {
    const int x = 31353;
    std::cout << 31353;
    for (int i = 0; i < 31353; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31354(int& val) {
    func_in_31354(val);
    const int x = 31354;
    std::cout << 31354;
    for (int i = 0; i < 31354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31354(int& val) {
    const int x = 31354;
    std::cout << 31354;
    for (int i = 0; i < 31354; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31355(int& val) {
    func_in_31355(val);
    const int x = 31355;
    std::cout << 31355;
    for (int i = 0; i < 31355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31355(int& val) {
    const int x = 31355;
    std::cout << 31355;
    for (int i = 0; i < 31355; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31356(int& val) {
    func_in_31356(val);
    const int x = 31356;
    std::cout << 31356;
    for (int i = 0; i < 31356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31356(int& val) {
    const int x = 31356;
    std::cout << 31356;
    for (int i = 0; i < 31356; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31357(int& val) {
    func_in_31357(val);
    const int x = 31357;
    std::cout << 31357;
    for (int i = 0; i < 31357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31357(int& val) {
    const int x = 31357;
    std::cout << 31357;
    for (int i = 0; i < 31357; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31358(int& val) {
    func_in_31358(val);
    const int x = 31358;
    std::cout << 31358;
    for (int i = 0; i < 31358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31358(int& val) {
    const int x = 31358;
    std::cout << 31358;
    for (int i = 0; i < 31358; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31359(int& val) {
    func_in_31359(val);
    const int x = 31359;
    std::cout << 31359;
    for (int i = 0; i < 31359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31359(int& val) {
    const int x = 31359;
    std::cout << 31359;
    for (int i = 0; i < 31359; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31360(int& val) {
    func_in_31360(val);
    const int x = 31360;
    std::cout << 31360;
    for (int i = 0; i < 31360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31360(int& val) {
    const int x = 31360;
    std::cout << 31360;
    for (int i = 0; i < 31360; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31361(int& val) {
    func_in_31361(val);
    const int x = 31361;
    std::cout << 31361;
    for (int i = 0; i < 31361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31361(int& val) {
    const int x = 31361;
    std::cout << 31361;
    for (int i = 0; i < 31361; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31362(int& val) {
    func_in_31362(val);
    const int x = 31362;
    std::cout << 31362;
    for (int i = 0; i < 31362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31362(int& val) {
    const int x = 31362;
    std::cout << 31362;
    for (int i = 0; i < 31362; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31363(int& val) {
    func_in_31363(val);
    const int x = 31363;
    std::cout << 31363;
    for (int i = 0; i < 31363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31363(int& val) {
    const int x = 31363;
    std::cout << 31363;
    for (int i = 0; i < 31363; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31364(int& val) {
    func_in_31364(val);
    const int x = 31364;
    std::cout << 31364;
    for (int i = 0; i < 31364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31364(int& val) {
    const int x = 31364;
    std::cout << 31364;
    for (int i = 0; i < 31364; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31365(int& val) {
    func_in_31365(val);
    const int x = 31365;
    std::cout << 31365;
    for (int i = 0; i < 31365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31365(int& val) {
    const int x = 31365;
    std::cout << 31365;
    for (int i = 0; i < 31365; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31366(int& val) {
    func_in_31366(val);
    const int x = 31366;
    std::cout << 31366;
    for (int i = 0; i < 31366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31366(int& val) {
    const int x = 31366;
    std::cout << 31366;
    for (int i = 0; i < 31366; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31367(int& val) {
    func_in_31367(val);
    const int x = 31367;
    std::cout << 31367;
    for (int i = 0; i < 31367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31367(int& val) {
    const int x = 31367;
    std::cout << 31367;
    for (int i = 0; i < 31367; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31368(int& val) {
    func_in_31368(val);
    const int x = 31368;
    std::cout << 31368;
    for (int i = 0; i < 31368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31368(int& val) {
    const int x = 31368;
    std::cout << 31368;
    for (int i = 0; i < 31368; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31369(int& val) {
    func_in_31369(val);
    const int x = 31369;
    std::cout << 31369;
    for (int i = 0; i < 31369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31369(int& val) {
    const int x = 31369;
    std::cout << 31369;
    for (int i = 0; i < 31369; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31370(int& val) {
    func_in_31370(val);
    const int x = 31370;
    std::cout << 31370;
    for (int i = 0; i < 31370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31370(int& val) {
    const int x = 31370;
    std::cout << 31370;
    for (int i = 0; i < 31370; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31371(int& val) {
    func_in_31371(val);
    const int x = 31371;
    std::cout << 31371;
    for (int i = 0; i < 31371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31371(int& val) {
    const int x = 31371;
    std::cout << 31371;
    for (int i = 0; i < 31371; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31372(int& val) {
    func_in_31372(val);
    const int x = 31372;
    std::cout << 31372;
    for (int i = 0; i < 31372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31372(int& val) {
    const int x = 31372;
    std::cout << 31372;
    for (int i = 0; i < 31372; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31373(int& val) {
    func_in_31373(val);
    const int x = 31373;
    std::cout << 31373;
    for (int i = 0; i < 31373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31373(int& val) {
    const int x = 31373;
    std::cout << 31373;
    for (int i = 0; i < 31373; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31374(int& val) {
    func_in_31374(val);
    const int x = 31374;
    std::cout << 31374;
    for (int i = 0; i < 31374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31374(int& val) {
    const int x = 31374;
    std::cout << 31374;
    for (int i = 0; i < 31374; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31375(int& val) {
    func_in_31375(val);
    const int x = 31375;
    std::cout << 31375;
    for (int i = 0; i < 31375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31375(int& val) {
    const int x = 31375;
    std::cout << 31375;
    for (int i = 0; i < 31375; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31376(int& val) {
    func_in_31376(val);
    const int x = 31376;
    std::cout << 31376;
    for (int i = 0; i < 31376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31376(int& val) {
    const int x = 31376;
    std::cout << 31376;
    for (int i = 0; i < 31376; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31377(int& val) {
    func_in_31377(val);
    const int x = 31377;
    std::cout << 31377;
    for (int i = 0; i < 31377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31377(int& val) {
    const int x = 31377;
    std::cout << 31377;
    for (int i = 0; i < 31377; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31378(int& val) {
    func_in_31378(val);
    const int x = 31378;
    std::cout << 31378;
    for (int i = 0; i < 31378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31378(int& val) {
    const int x = 31378;
    std::cout << 31378;
    for (int i = 0; i < 31378; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31379(int& val) {
    func_in_31379(val);
    const int x = 31379;
    std::cout << 31379;
    for (int i = 0; i < 31379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31379(int& val) {
    const int x = 31379;
    std::cout << 31379;
    for (int i = 0; i < 31379; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31380(int& val) {
    func_in_31380(val);
    const int x = 31380;
    std::cout << 31380;
    for (int i = 0; i < 31380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31380(int& val) {
    const int x = 31380;
    std::cout << 31380;
    for (int i = 0; i < 31380; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31381(int& val) {
    func_in_31381(val);
    const int x = 31381;
    std::cout << 31381;
    for (int i = 0; i < 31381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31381(int& val) {
    const int x = 31381;
    std::cout << 31381;
    for (int i = 0; i < 31381; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31382(int& val) {
    func_in_31382(val);
    const int x = 31382;
    std::cout << 31382;
    for (int i = 0; i < 31382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31382(int& val) {
    const int x = 31382;
    std::cout << 31382;
    for (int i = 0; i < 31382; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31383(int& val) {
    func_in_31383(val);
    const int x = 31383;
    std::cout << 31383;
    for (int i = 0; i < 31383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31383(int& val) {
    const int x = 31383;
    std::cout << 31383;
    for (int i = 0; i < 31383; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31384(int& val) {
    func_in_31384(val);
    const int x = 31384;
    std::cout << 31384;
    for (int i = 0; i < 31384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31384(int& val) {
    const int x = 31384;
    std::cout << 31384;
    for (int i = 0; i < 31384; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31385(int& val) {
    func_in_31385(val);
    const int x = 31385;
    std::cout << 31385;
    for (int i = 0; i < 31385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31385(int& val) {
    const int x = 31385;
    std::cout << 31385;
    for (int i = 0; i < 31385; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31386(int& val) {
    func_in_31386(val);
    const int x = 31386;
    std::cout << 31386;
    for (int i = 0; i < 31386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31386(int& val) {
    const int x = 31386;
    std::cout << 31386;
    for (int i = 0; i < 31386; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31387(int& val) {
    func_in_31387(val);
    const int x = 31387;
    std::cout << 31387;
    for (int i = 0; i < 31387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31387(int& val) {
    const int x = 31387;
    std::cout << 31387;
    for (int i = 0; i < 31387; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31388(int& val) {
    func_in_31388(val);
    const int x = 31388;
    std::cout << 31388;
    for (int i = 0; i < 31388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31388(int& val) {
    const int x = 31388;
    std::cout << 31388;
    for (int i = 0; i < 31388; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31389(int& val) {
    func_in_31389(val);
    const int x = 31389;
    std::cout << 31389;
    for (int i = 0; i < 31389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31389(int& val) {
    const int x = 31389;
    std::cout << 31389;
    for (int i = 0; i < 31389; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31390(int& val) {
    func_in_31390(val);
    const int x = 31390;
    std::cout << 31390;
    for (int i = 0; i < 31390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31390(int& val) {
    const int x = 31390;
    std::cout << 31390;
    for (int i = 0; i < 31390; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31391(int& val) {
    func_in_31391(val);
    const int x = 31391;
    std::cout << 31391;
    for (int i = 0; i < 31391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31391(int& val) {
    const int x = 31391;
    std::cout << 31391;
    for (int i = 0; i < 31391; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31392(int& val) {
    func_in_31392(val);
    const int x = 31392;
    std::cout << 31392;
    for (int i = 0; i < 31392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31392(int& val) {
    const int x = 31392;
    std::cout << 31392;
    for (int i = 0; i < 31392; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31393(int& val) {
    func_in_31393(val);
    const int x = 31393;
    std::cout << 31393;
    for (int i = 0; i < 31393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31393(int& val) {
    const int x = 31393;
    std::cout << 31393;
    for (int i = 0; i < 31393; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31394(int& val) {
    func_in_31394(val);
    const int x = 31394;
    std::cout << 31394;
    for (int i = 0; i < 31394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31394(int& val) {
    const int x = 31394;
    std::cout << 31394;
    for (int i = 0; i < 31394; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31395(int& val) {
    func_in_31395(val);
    const int x = 31395;
    std::cout << 31395;
    for (int i = 0; i < 31395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31395(int& val) {
    const int x = 31395;
    std::cout << 31395;
    for (int i = 0; i < 31395; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31396(int& val) {
    func_in_31396(val);
    const int x = 31396;
    std::cout << 31396;
    for (int i = 0; i < 31396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31396(int& val) {
    const int x = 31396;
    std::cout << 31396;
    for (int i = 0; i < 31396; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31397(int& val) {
    func_in_31397(val);
    const int x = 31397;
    std::cout << 31397;
    for (int i = 0; i < 31397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31397(int& val) {
    const int x = 31397;
    std::cout << 31397;
    for (int i = 0; i < 31397; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31398(int& val) {
    func_in_31398(val);
    const int x = 31398;
    std::cout << 31398;
    for (int i = 0; i < 31398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31398(int& val) {
    const int x = 31398;
    std::cout << 31398;
    for (int i = 0; i < 31398; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31399(int& val) {
    func_in_31399(val);
    const int x = 31399;
    std::cout << 31399;
    for (int i = 0; i < 31399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31399(int& val) {
    const int x = 31399;
    std::cout << 31399;
    for (int i = 0; i < 31399; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31400(int& val) {
    func_in_31400(val);
    const int x = 31400;
    std::cout << 31400;
    for (int i = 0; i < 31400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31400(int& val) {
    const int x = 31400;
    std::cout << 31400;
    for (int i = 0; i < 31400; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31401(int& val) {
    func_in_31401(val);
    const int x = 31401;
    std::cout << 31401;
    for (int i = 0; i < 31401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31401(int& val) {
    const int x = 31401;
    std::cout << 31401;
    for (int i = 0; i < 31401; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31402(int& val) {
    func_in_31402(val);
    const int x = 31402;
    std::cout << 31402;
    for (int i = 0; i < 31402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31402(int& val) {
    const int x = 31402;
    std::cout << 31402;
    for (int i = 0; i < 31402; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31403(int& val) {
    func_in_31403(val);
    const int x = 31403;
    std::cout << 31403;
    for (int i = 0; i < 31403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31403(int& val) {
    const int x = 31403;
    std::cout << 31403;
    for (int i = 0; i < 31403; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31404(int& val) {
    func_in_31404(val);
    const int x = 31404;
    std::cout << 31404;
    for (int i = 0; i < 31404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31404(int& val) {
    const int x = 31404;
    std::cout << 31404;
    for (int i = 0; i < 31404; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31405(int& val) {
    func_in_31405(val);
    const int x = 31405;
    std::cout << 31405;
    for (int i = 0; i < 31405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31405(int& val) {
    const int x = 31405;
    std::cout << 31405;
    for (int i = 0; i < 31405; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31406(int& val) {
    func_in_31406(val);
    const int x = 31406;
    std::cout << 31406;
    for (int i = 0; i < 31406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31406(int& val) {
    const int x = 31406;
    std::cout << 31406;
    for (int i = 0; i < 31406; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31407(int& val) {
    func_in_31407(val);
    const int x = 31407;
    std::cout << 31407;
    for (int i = 0; i < 31407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31407(int& val) {
    const int x = 31407;
    std::cout << 31407;
    for (int i = 0; i < 31407; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31408(int& val) {
    func_in_31408(val);
    const int x = 31408;
    std::cout << 31408;
    for (int i = 0; i < 31408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31408(int& val) {
    const int x = 31408;
    std::cout << 31408;
    for (int i = 0; i < 31408; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31409(int& val) {
    func_in_31409(val);
    const int x = 31409;
    std::cout << 31409;
    for (int i = 0; i < 31409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31409(int& val) {
    const int x = 31409;
    std::cout << 31409;
    for (int i = 0; i < 31409; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31410(int& val) {
    func_in_31410(val);
    const int x = 31410;
    std::cout << 31410;
    for (int i = 0; i < 31410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31410(int& val) {
    const int x = 31410;
    std::cout << 31410;
    for (int i = 0; i < 31410; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31411(int& val) {
    func_in_31411(val);
    const int x = 31411;
    std::cout << 31411;
    for (int i = 0; i < 31411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31411(int& val) {
    const int x = 31411;
    std::cout << 31411;
    for (int i = 0; i < 31411; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31412(int& val) {
    func_in_31412(val);
    const int x = 31412;
    std::cout << 31412;
    for (int i = 0; i < 31412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31412(int& val) {
    const int x = 31412;
    std::cout << 31412;
    for (int i = 0; i < 31412; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31413(int& val) {
    func_in_31413(val);
    const int x = 31413;
    std::cout << 31413;
    for (int i = 0; i < 31413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31413(int& val) {
    const int x = 31413;
    std::cout << 31413;
    for (int i = 0; i < 31413; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31414(int& val) {
    func_in_31414(val);
    const int x = 31414;
    std::cout << 31414;
    for (int i = 0; i < 31414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31414(int& val) {
    const int x = 31414;
    std::cout << 31414;
    for (int i = 0; i < 31414; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31415(int& val) {
    func_in_31415(val);
    const int x = 31415;
    std::cout << 31415;
    for (int i = 0; i < 31415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31415(int& val) {
    const int x = 31415;
    std::cout << 31415;
    for (int i = 0; i < 31415; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31416(int& val) {
    func_in_31416(val);
    const int x = 31416;
    std::cout << 31416;
    for (int i = 0; i < 31416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31416(int& val) {
    const int x = 31416;
    std::cout << 31416;
    for (int i = 0; i < 31416; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31417(int& val) {
    func_in_31417(val);
    const int x = 31417;
    std::cout << 31417;
    for (int i = 0; i < 31417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31417(int& val) {
    const int x = 31417;
    std::cout << 31417;
    for (int i = 0; i < 31417; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31418(int& val) {
    func_in_31418(val);
    const int x = 31418;
    std::cout << 31418;
    for (int i = 0; i < 31418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31418(int& val) {
    const int x = 31418;
    std::cout << 31418;
    for (int i = 0; i < 31418; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31419(int& val) {
    func_in_31419(val);
    const int x = 31419;
    std::cout << 31419;
    for (int i = 0; i < 31419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31419(int& val) {
    const int x = 31419;
    std::cout << 31419;
    for (int i = 0; i < 31419; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31420(int& val) {
    func_in_31420(val);
    const int x = 31420;
    std::cout << 31420;
    for (int i = 0; i < 31420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31420(int& val) {
    const int x = 31420;
    std::cout << 31420;
    for (int i = 0; i < 31420; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31421(int& val) {
    func_in_31421(val);
    const int x = 31421;
    std::cout << 31421;
    for (int i = 0; i < 31421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31421(int& val) {
    const int x = 31421;
    std::cout << 31421;
    for (int i = 0; i < 31421; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31422(int& val) {
    func_in_31422(val);
    const int x = 31422;
    std::cout << 31422;
    for (int i = 0; i < 31422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31422(int& val) {
    const int x = 31422;
    std::cout << 31422;
    for (int i = 0; i < 31422; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31423(int& val) {
    func_in_31423(val);
    const int x = 31423;
    std::cout << 31423;
    for (int i = 0; i < 31423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31423(int& val) {
    const int x = 31423;
    std::cout << 31423;
    for (int i = 0; i < 31423; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31424(int& val) {
    func_in_31424(val);
    const int x = 31424;
    std::cout << 31424;
    for (int i = 0; i < 31424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31424(int& val) {
    const int x = 31424;
    std::cout << 31424;
    for (int i = 0; i < 31424; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31425(int& val) {
    func_in_31425(val);
    const int x = 31425;
    std::cout << 31425;
    for (int i = 0; i < 31425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31425(int& val) {
    const int x = 31425;
    std::cout << 31425;
    for (int i = 0; i < 31425; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31426(int& val) {
    func_in_31426(val);
    const int x = 31426;
    std::cout << 31426;
    for (int i = 0; i < 31426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31426(int& val) {
    const int x = 31426;
    std::cout << 31426;
    for (int i = 0; i < 31426; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31427(int& val) {
    func_in_31427(val);
    const int x = 31427;
    std::cout << 31427;
    for (int i = 0; i < 31427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31427(int& val) {
    const int x = 31427;
    std::cout << 31427;
    for (int i = 0; i < 31427; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31428(int& val) {
    func_in_31428(val);
    const int x = 31428;
    std::cout << 31428;
    for (int i = 0; i < 31428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31428(int& val) {
    const int x = 31428;
    std::cout << 31428;
    for (int i = 0; i < 31428; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31429(int& val) {
    func_in_31429(val);
    const int x = 31429;
    std::cout << 31429;
    for (int i = 0; i < 31429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31429(int& val) {
    const int x = 31429;
    std::cout << 31429;
    for (int i = 0; i < 31429; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31430(int& val) {
    func_in_31430(val);
    const int x = 31430;
    std::cout << 31430;
    for (int i = 0; i < 31430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31430(int& val) {
    const int x = 31430;
    std::cout << 31430;
    for (int i = 0; i < 31430; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31431(int& val) {
    func_in_31431(val);
    const int x = 31431;
    std::cout << 31431;
    for (int i = 0; i < 31431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31431(int& val) {
    const int x = 31431;
    std::cout << 31431;
    for (int i = 0; i < 31431; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31432(int& val) {
    func_in_31432(val);
    const int x = 31432;
    std::cout << 31432;
    for (int i = 0; i < 31432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31432(int& val) {
    const int x = 31432;
    std::cout << 31432;
    for (int i = 0; i < 31432; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31433(int& val) {
    func_in_31433(val);
    const int x = 31433;
    std::cout << 31433;
    for (int i = 0; i < 31433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31433(int& val) {
    const int x = 31433;
    std::cout << 31433;
    for (int i = 0; i < 31433; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31434(int& val) {
    func_in_31434(val);
    const int x = 31434;
    std::cout << 31434;
    for (int i = 0; i < 31434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31434(int& val) {
    const int x = 31434;
    std::cout << 31434;
    for (int i = 0; i < 31434; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31435(int& val) {
    func_in_31435(val);
    const int x = 31435;
    std::cout << 31435;
    for (int i = 0; i < 31435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31435(int& val) {
    const int x = 31435;
    std::cout << 31435;
    for (int i = 0; i < 31435; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31436(int& val) {
    func_in_31436(val);
    const int x = 31436;
    std::cout << 31436;
    for (int i = 0; i < 31436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31436(int& val) {
    const int x = 31436;
    std::cout << 31436;
    for (int i = 0; i < 31436; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31437(int& val) {
    func_in_31437(val);
    const int x = 31437;
    std::cout << 31437;
    for (int i = 0; i < 31437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31437(int& val) {
    const int x = 31437;
    std::cout << 31437;
    for (int i = 0; i < 31437; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31438(int& val) {
    func_in_31438(val);
    const int x = 31438;
    std::cout << 31438;
    for (int i = 0; i < 31438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31438(int& val) {
    const int x = 31438;
    std::cout << 31438;
    for (int i = 0; i < 31438; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31439(int& val) {
    func_in_31439(val);
    const int x = 31439;
    std::cout << 31439;
    for (int i = 0; i < 31439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31439(int& val) {
    const int x = 31439;
    std::cout << 31439;
    for (int i = 0; i < 31439; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31440(int& val) {
    func_in_31440(val);
    const int x = 31440;
    std::cout << 31440;
    for (int i = 0; i < 31440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31440(int& val) {
    const int x = 31440;
    std::cout << 31440;
    for (int i = 0; i < 31440; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31441(int& val) {
    func_in_31441(val);
    const int x = 31441;
    std::cout << 31441;
    for (int i = 0; i < 31441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31441(int& val) {
    const int x = 31441;
    std::cout << 31441;
    for (int i = 0; i < 31441; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31442(int& val) {
    func_in_31442(val);
    const int x = 31442;
    std::cout << 31442;
    for (int i = 0; i < 31442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31442(int& val) {
    const int x = 31442;
    std::cout << 31442;
    for (int i = 0; i < 31442; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31443(int& val) {
    func_in_31443(val);
    const int x = 31443;
    std::cout << 31443;
    for (int i = 0; i < 31443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31443(int& val) {
    const int x = 31443;
    std::cout << 31443;
    for (int i = 0; i < 31443; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31444(int& val) {
    func_in_31444(val);
    const int x = 31444;
    std::cout << 31444;
    for (int i = 0; i < 31444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31444(int& val) {
    const int x = 31444;
    std::cout << 31444;
    for (int i = 0; i < 31444; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31445(int& val) {
    func_in_31445(val);
    const int x = 31445;
    std::cout << 31445;
    for (int i = 0; i < 31445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31445(int& val) {
    const int x = 31445;
    std::cout << 31445;
    for (int i = 0; i < 31445; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31446(int& val) {
    func_in_31446(val);
    const int x = 31446;
    std::cout << 31446;
    for (int i = 0; i < 31446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31446(int& val) {
    const int x = 31446;
    std::cout << 31446;
    for (int i = 0; i < 31446; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31447(int& val) {
    func_in_31447(val);
    const int x = 31447;
    std::cout << 31447;
    for (int i = 0; i < 31447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31447(int& val) {
    const int x = 31447;
    std::cout << 31447;
    for (int i = 0; i < 31447; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31448(int& val) {
    func_in_31448(val);
    const int x = 31448;
    std::cout << 31448;
    for (int i = 0; i < 31448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31448(int& val) {
    const int x = 31448;
    std::cout << 31448;
    for (int i = 0; i < 31448; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31449(int& val) {
    func_in_31449(val);
    const int x = 31449;
    std::cout << 31449;
    for (int i = 0; i < 31449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31449(int& val) {
    const int x = 31449;
    std::cout << 31449;
    for (int i = 0; i < 31449; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31450(int& val) {
    func_in_31450(val);
    const int x = 31450;
    std::cout << 31450;
    for (int i = 0; i < 31450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31450(int& val) {
    const int x = 31450;
    std::cout << 31450;
    for (int i = 0; i < 31450; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31451(int& val) {
    func_in_31451(val);
    const int x = 31451;
    std::cout << 31451;
    for (int i = 0; i < 31451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31451(int& val) {
    const int x = 31451;
    std::cout << 31451;
    for (int i = 0; i < 31451; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31452(int& val) {
    func_in_31452(val);
    const int x = 31452;
    std::cout << 31452;
    for (int i = 0; i < 31452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31452(int& val) {
    const int x = 31452;
    std::cout << 31452;
    for (int i = 0; i < 31452; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31453(int& val) {
    func_in_31453(val);
    const int x = 31453;
    std::cout << 31453;
    for (int i = 0; i < 31453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31453(int& val) {
    const int x = 31453;
    std::cout << 31453;
    for (int i = 0; i < 31453; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31454(int& val) {
    func_in_31454(val);
    const int x = 31454;
    std::cout << 31454;
    for (int i = 0; i < 31454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31454(int& val) {
    const int x = 31454;
    std::cout << 31454;
    for (int i = 0; i < 31454; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31455(int& val) {
    func_in_31455(val);
    const int x = 31455;
    std::cout << 31455;
    for (int i = 0; i < 31455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31455(int& val) {
    const int x = 31455;
    std::cout << 31455;
    for (int i = 0; i < 31455; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31456(int& val) {
    func_in_31456(val);
    const int x = 31456;
    std::cout << 31456;
    for (int i = 0; i < 31456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31456(int& val) {
    const int x = 31456;
    std::cout << 31456;
    for (int i = 0; i < 31456; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31457(int& val) {
    func_in_31457(val);
    const int x = 31457;
    std::cout << 31457;
    for (int i = 0; i < 31457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31457(int& val) {
    const int x = 31457;
    std::cout << 31457;
    for (int i = 0; i < 31457; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31458(int& val) {
    func_in_31458(val);
    const int x = 31458;
    std::cout << 31458;
    for (int i = 0; i < 31458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31458(int& val) {
    const int x = 31458;
    std::cout << 31458;
    for (int i = 0; i < 31458; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31459(int& val) {
    func_in_31459(val);
    const int x = 31459;
    std::cout << 31459;
    for (int i = 0; i < 31459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31459(int& val) {
    const int x = 31459;
    std::cout << 31459;
    for (int i = 0; i < 31459; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31460(int& val) {
    func_in_31460(val);
    const int x = 31460;
    std::cout << 31460;
    for (int i = 0; i < 31460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31460(int& val) {
    const int x = 31460;
    std::cout << 31460;
    for (int i = 0; i < 31460; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31461(int& val) {
    func_in_31461(val);
    const int x = 31461;
    std::cout << 31461;
    for (int i = 0; i < 31461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31461(int& val) {
    const int x = 31461;
    std::cout << 31461;
    for (int i = 0; i < 31461; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31462(int& val) {
    func_in_31462(val);
    const int x = 31462;
    std::cout << 31462;
    for (int i = 0; i < 31462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31462(int& val) {
    const int x = 31462;
    std::cout << 31462;
    for (int i = 0; i < 31462; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31463(int& val) {
    func_in_31463(val);
    const int x = 31463;
    std::cout << 31463;
    for (int i = 0; i < 31463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31463(int& val) {
    const int x = 31463;
    std::cout << 31463;
    for (int i = 0; i < 31463; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31464(int& val) {
    func_in_31464(val);
    const int x = 31464;
    std::cout << 31464;
    for (int i = 0; i < 31464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31464(int& val) {
    const int x = 31464;
    std::cout << 31464;
    for (int i = 0; i < 31464; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31465(int& val) {
    func_in_31465(val);
    const int x = 31465;
    std::cout << 31465;
    for (int i = 0; i < 31465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31465(int& val) {
    const int x = 31465;
    std::cout << 31465;
    for (int i = 0; i < 31465; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31466(int& val) {
    func_in_31466(val);
    const int x = 31466;
    std::cout << 31466;
    for (int i = 0; i < 31466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31466(int& val) {
    const int x = 31466;
    std::cout << 31466;
    for (int i = 0; i < 31466; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31467(int& val) {
    func_in_31467(val);
    const int x = 31467;
    std::cout << 31467;
    for (int i = 0; i < 31467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31467(int& val) {
    const int x = 31467;
    std::cout << 31467;
    for (int i = 0; i < 31467; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31468(int& val) {
    func_in_31468(val);
    const int x = 31468;
    std::cout << 31468;
    for (int i = 0; i < 31468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31468(int& val) {
    const int x = 31468;
    std::cout << 31468;
    for (int i = 0; i < 31468; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31469(int& val) {
    func_in_31469(val);
    const int x = 31469;
    std::cout << 31469;
    for (int i = 0; i < 31469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31469(int& val) {
    const int x = 31469;
    std::cout << 31469;
    for (int i = 0; i < 31469; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31470(int& val) {
    func_in_31470(val);
    const int x = 31470;
    std::cout << 31470;
    for (int i = 0; i < 31470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31470(int& val) {
    const int x = 31470;
    std::cout << 31470;
    for (int i = 0; i < 31470; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31471(int& val) {
    func_in_31471(val);
    const int x = 31471;
    std::cout << 31471;
    for (int i = 0; i < 31471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31471(int& val) {
    const int x = 31471;
    std::cout << 31471;
    for (int i = 0; i < 31471; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31472(int& val) {
    func_in_31472(val);
    const int x = 31472;
    std::cout << 31472;
    for (int i = 0; i < 31472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31472(int& val) {
    const int x = 31472;
    std::cout << 31472;
    for (int i = 0; i < 31472; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31473(int& val) {
    func_in_31473(val);
    const int x = 31473;
    std::cout << 31473;
    for (int i = 0; i < 31473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31473(int& val) {
    const int x = 31473;
    std::cout << 31473;
    for (int i = 0; i < 31473; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31474(int& val) {
    func_in_31474(val);
    const int x = 31474;
    std::cout << 31474;
    for (int i = 0; i < 31474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31474(int& val) {
    const int x = 31474;
    std::cout << 31474;
    for (int i = 0; i < 31474; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31475(int& val) {
    func_in_31475(val);
    const int x = 31475;
    std::cout << 31475;
    for (int i = 0; i < 31475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31475(int& val) {
    const int x = 31475;
    std::cout << 31475;
    for (int i = 0; i < 31475; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31476(int& val) {
    func_in_31476(val);
    const int x = 31476;
    std::cout << 31476;
    for (int i = 0; i < 31476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31476(int& val) {
    const int x = 31476;
    std::cout << 31476;
    for (int i = 0; i < 31476; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31477(int& val) {
    func_in_31477(val);
    const int x = 31477;
    std::cout << 31477;
    for (int i = 0; i < 31477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31477(int& val) {
    const int x = 31477;
    std::cout << 31477;
    for (int i = 0; i < 31477; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31478(int& val) {
    func_in_31478(val);
    const int x = 31478;
    std::cout << 31478;
    for (int i = 0; i < 31478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31478(int& val) {
    const int x = 31478;
    std::cout << 31478;
    for (int i = 0; i < 31478; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31479(int& val) {
    func_in_31479(val);
    const int x = 31479;
    std::cout << 31479;
    for (int i = 0; i < 31479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31479(int& val) {
    const int x = 31479;
    std::cout << 31479;
    for (int i = 0; i < 31479; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31480(int& val) {
    func_in_31480(val);
    const int x = 31480;
    std::cout << 31480;
    for (int i = 0; i < 31480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31480(int& val) {
    const int x = 31480;
    std::cout << 31480;
    for (int i = 0; i < 31480; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31481(int& val) {
    func_in_31481(val);
    const int x = 31481;
    std::cout << 31481;
    for (int i = 0; i < 31481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31481(int& val) {
    const int x = 31481;
    std::cout << 31481;
    for (int i = 0; i < 31481; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31482(int& val) {
    func_in_31482(val);
    const int x = 31482;
    std::cout << 31482;
    for (int i = 0; i < 31482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31482(int& val) {
    const int x = 31482;
    std::cout << 31482;
    for (int i = 0; i < 31482; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31483(int& val) {
    func_in_31483(val);
    const int x = 31483;
    std::cout << 31483;
    for (int i = 0; i < 31483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31483(int& val) {
    const int x = 31483;
    std::cout << 31483;
    for (int i = 0; i < 31483; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31484(int& val) {
    func_in_31484(val);
    const int x = 31484;
    std::cout << 31484;
    for (int i = 0; i < 31484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31484(int& val) {
    const int x = 31484;
    std::cout << 31484;
    for (int i = 0; i < 31484; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31485(int& val) {
    func_in_31485(val);
    const int x = 31485;
    std::cout << 31485;
    for (int i = 0; i < 31485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31485(int& val) {
    const int x = 31485;
    std::cout << 31485;
    for (int i = 0; i < 31485; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31486(int& val) {
    func_in_31486(val);
    const int x = 31486;
    std::cout << 31486;
    for (int i = 0; i < 31486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31486(int& val) {
    const int x = 31486;
    std::cout << 31486;
    for (int i = 0; i < 31486; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31487(int& val) {
    func_in_31487(val);
    const int x = 31487;
    std::cout << 31487;
    for (int i = 0; i < 31487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31487(int& val) {
    const int x = 31487;
    std::cout << 31487;
    for (int i = 0; i < 31487; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31488(int& val) {
    func_in_31488(val);
    const int x = 31488;
    std::cout << 31488;
    for (int i = 0; i < 31488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31488(int& val) {
    const int x = 31488;
    std::cout << 31488;
    for (int i = 0; i < 31488; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31489(int& val) {
    func_in_31489(val);
    const int x = 31489;
    std::cout << 31489;
    for (int i = 0; i < 31489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31489(int& val) {
    const int x = 31489;
    std::cout << 31489;
    for (int i = 0; i < 31489; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31490(int& val) {
    func_in_31490(val);
    const int x = 31490;
    std::cout << 31490;
    for (int i = 0; i < 31490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31490(int& val) {
    const int x = 31490;
    std::cout << 31490;
    for (int i = 0; i < 31490; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31491(int& val) {
    func_in_31491(val);
    const int x = 31491;
    std::cout << 31491;
    for (int i = 0; i < 31491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31491(int& val) {
    const int x = 31491;
    std::cout << 31491;
    for (int i = 0; i < 31491; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31492(int& val) {
    func_in_31492(val);
    const int x = 31492;
    std::cout << 31492;
    for (int i = 0; i < 31492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31492(int& val) {
    const int x = 31492;
    std::cout << 31492;
    for (int i = 0; i < 31492; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31493(int& val) {
    func_in_31493(val);
    const int x = 31493;
    std::cout << 31493;
    for (int i = 0; i < 31493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31493(int& val) {
    const int x = 31493;
    std::cout << 31493;
    for (int i = 0; i < 31493; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31494(int& val) {
    func_in_31494(val);
    const int x = 31494;
    std::cout << 31494;
    for (int i = 0; i < 31494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31494(int& val) {
    const int x = 31494;
    std::cout << 31494;
    for (int i = 0; i < 31494; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31495(int& val) {
    func_in_31495(val);
    const int x = 31495;
    std::cout << 31495;
    for (int i = 0; i < 31495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31495(int& val) {
    const int x = 31495;
    std::cout << 31495;
    for (int i = 0; i < 31495; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31496(int& val) {
    func_in_31496(val);
    const int x = 31496;
    std::cout << 31496;
    for (int i = 0; i < 31496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31496(int& val) {
    const int x = 31496;
    std::cout << 31496;
    for (int i = 0; i < 31496; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31497(int& val) {
    func_in_31497(val);
    const int x = 31497;
    std::cout << 31497;
    for (int i = 0; i < 31497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31497(int& val) {
    const int x = 31497;
    std::cout << 31497;
    for (int i = 0; i < 31497; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31498(int& val) {
    func_in_31498(val);
    const int x = 31498;
    std::cout << 31498;
    for (int i = 0; i < 31498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31498(int& val) {
    const int x = 31498;
    std::cout << 31498;
    for (int i = 0; i < 31498; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31499(int& val) {
    func_in_31499(val);
    const int x = 31499;
    std::cout << 31499;
    for (int i = 0; i < 31499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31499(int& val) {
    const int x = 31499;
    std::cout << 31499;
    for (int i = 0; i < 31499; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31500(int& val) {
    func_in_31500(val);
    const int x = 31500;
    std::cout << 31500;
    for (int i = 0; i < 31500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31500(int& val) {
    const int x = 31500;
    std::cout << 31500;
    for (int i = 0; i < 31500; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31501(int& val) {
    func_in_31501(val);
    const int x = 31501;
    std::cout << 31501;
    for (int i = 0; i < 31501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31501(int& val) {
    const int x = 31501;
    std::cout << 31501;
    for (int i = 0; i < 31501; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31502(int& val) {
    func_in_31502(val);
    const int x = 31502;
    std::cout << 31502;
    for (int i = 0; i < 31502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31502(int& val) {
    const int x = 31502;
    std::cout << 31502;
    for (int i = 0; i < 31502; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31503(int& val) {
    func_in_31503(val);
    const int x = 31503;
    std::cout << 31503;
    for (int i = 0; i < 31503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31503(int& val) {
    const int x = 31503;
    std::cout << 31503;
    for (int i = 0; i < 31503; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31504(int& val) {
    func_in_31504(val);
    const int x = 31504;
    std::cout << 31504;
    for (int i = 0; i < 31504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31504(int& val) {
    const int x = 31504;
    std::cout << 31504;
    for (int i = 0; i < 31504; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31505(int& val) {
    func_in_31505(val);
    const int x = 31505;
    std::cout << 31505;
    for (int i = 0; i < 31505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31505(int& val) {
    const int x = 31505;
    std::cout << 31505;
    for (int i = 0; i < 31505; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31506(int& val) {
    func_in_31506(val);
    const int x = 31506;
    std::cout << 31506;
    for (int i = 0; i < 31506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31506(int& val) {
    const int x = 31506;
    std::cout << 31506;
    for (int i = 0; i < 31506; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31507(int& val) {
    func_in_31507(val);
    const int x = 31507;
    std::cout << 31507;
    for (int i = 0; i < 31507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31507(int& val) {
    const int x = 31507;
    std::cout << 31507;
    for (int i = 0; i < 31507; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31508(int& val) {
    func_in_31508(val);
    const int x = 31508;
    std::cout << 31508;
    for (int i = 0; i < 31508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31508(int& val) {
    const int x = 31508;
    std::cout << 31508;
    for (int i = 0; i < 31508; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31509(int& val) {
    func_in_31509(val);
    const int x = 31509;
    std::cout << 31509;
    for (int i = 0; i < 31509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31509(int& val) {
    const int x = 31509;
    std::cout << 31509;
    for (int i = 0; i < 31509; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31510(int& val) {
    func_in_31510(val);
    const int x = 31510;
    std::cout << 31510;
    for (int i = 0; i < 31510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31510(int& val) {
    const int x = 31510;
    std::cout << 31510;
    for (int i = 0; i < 31510; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31511(int& val) {
    func_in_31511(val);
    const int x = 31511;
    std::cout << 31511;
    for (int i = 0; i < 31511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31511(int& val) {
    const int x = 31511;
    std::cout << 31511;
    for (int i = 0; i < 31511; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31512(int& val) {
    func_in_31512(val);
    const int x = 31512;
    std::cout << 31512;
    for (int i = 0; i < 31512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31512(int& val) {
    const int x = 31512;
    std::cout << 31512;
    for (int i = 0; i < 31512; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31513(int& val) {
    func_in_31513(val);
    const int x = 31513;
    std::cout << 31513;
    for (int i = 0; i < 31513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31513(int& val) {
    const int x = 31513;
    std::cout << 31513;
    for (int i = 0; i < 31513; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31514(int& val) {
    func_in_31514(val);
    const int x = 31514;
    std::cout << 31514;
    for (int i = 0; i < 31514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31514(int& val) {
    const int x = 31514;
    std::cout << 31514;
    for (int i = 0; i < 31514; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31515(int& val) {
    func_in_31515(val);
    const int x = 31515;
    std::cout << 31515;
    for (int i = 0; i < 31515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31515(int& val) {
    const int x = 31515;
    std::cout << 31515;
    for (int i = 0; i < 31515; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31516(int& val) {
    func_in_31516(val);
    const int x = 31516;
    std::cout << 31516;
    for (int i = 0; i < 31516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31516(int& val) {
    const int x = 31516;
    std::cout << 31516;
    for (int i = 0; i < 31516; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31517(int& val) {
    func_in_31517(val);
    const int x = 31517;
    std::cout << 31517;
    for (int i = 0; i < 31517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31517(int& val) {
    const int x = 31517;
    std::cout << 31517;
    for (int i = 0; i < 31517; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31518(int& val) {
    func_in_31518(val);
    const int x = 31518;
    std::cout << 31518;
    for (int i = 0; i < 31518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31518(int& val) {
    const int x = 31518;
    std::cout << 31518;
    for (int i = 0; i < 31518; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31519(int& val) {
    func_in_31519(val);
    const int x = 31519;
    std::cout << 31519;
    for (int i = 0; i < 31519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31519(int& val) {
    const int x = 31519;
    std::cout << 31519;
    for (int i = 0; i < 31519; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31520(int& val) {
    func_in_31520(val);
    const int x = 31520;
    std::cout << 31520;
    for (int i = 0; i < 31520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31520(int& val) {
    const int x = 31520;
    std::cout << 31520;
    for (int i = 0; i < 31520; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31521(int& val) {
    func_in_31521(val);
    const int x = 31521;
    std::cout << 31521;
    for (int i = 0; i < 31521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31521(int& val) {
    const int x = 31521;
    std::cout << 31521;
    for (int i = 0; i < 31521; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31522(int& val) {
    func_in_31522(val);
    const int x = 31522;
    std::cout << 31522;
    for (int i = 0; i < 31522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31522(int& val) {
    const int x = 31522;
    std::cout << 31522;
    for (int i = 0; i < 31522; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31523(int& val) {
    func_in_31523(val);
    const int x = 31523;
    std::cout << 31523;
    for (int i = 0; i < 31523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31523(int& val) {
    const int x = 31523;
    std::cout << 31523;
    for (int i = 0; i < 31523; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31524(int& val) {
    func_in_31524(val);
    const int x = 31524;
    std::cout << 31524;
    for (int i = 0; i < 31524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31524(int& val) {
    const int x = 31524;
    std::cout << 31524;
    for (int i = 0; i < 31524; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31525(int& val) {
    func_in_31525(val);
    const int x = 31525;
    std::cout << 31525;
    for (int i = 0; i < 31525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31525(int& val) {
    const int x = 31525;
    std::cout << 31525;
    for (int i = 0; i < 31525; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31526(int& val) {
    func_in_31526(val);
    const int x = 31526;
    std::cout << 31526;
    for (int i = 0; i < 31526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31526(int& val) {
    const int x = 31526;
    std::cout << 31526;
    for (int i = 0; i < 31526; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31527(int& val) {
    func_in_31527(val);
    const int x = 31527;
    std::cout << 31527;
    for (int i = 0; i < 31527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31527(int& val) {
    const int x = 31527;
    std::cout << 31527;
    for (int i = 0; i < 31527; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31528(int& val) {
    func_in_31528(val);
    const int x = 31528;
    std::cout << 31528;
    for (int i = 0; i < 31528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31528(int& val) {
    const int x = 31528;
    std::cout << 31528;
    for (int i = 0; i < 31528; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31529(int& val) {
    func_in_31529(val);
    const int x = 31529;
    std::cout << 31529;
    for (int i = 0; i < 31529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31529(int& val) {
    const int x = 31529;
    std::cout << 31529;
    for (int i = 0; i < 31529; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31530(int& val) {
    func_in_31530(val);
    const int x = 31530;
    std::cout << 31530;
    for (int i = 0; i < 31530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31530(int& val) {
    const int x = 31530;
    std::cout << 31530;
    for (int i = 0; i < 31530; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31531(int& val) {
    func_in_31531(val);
    const int x = 31531;
    std::cout << 31531;
    for (int i = 0; i < 31531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31531(int& val) {
    const int x = 31531;
    std::cout << 31531;
    for (int i = 0; i < 31531; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31532(int& val) {
    func_in_31532(val);
    const int x = 31532;
    std::cout << 31532;
    for (int i = 0; i < 31532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31532(int& val) {
    const int x = 31532;
    std::cout << 31532;
    for (int i = 0; i < 31532; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31533(int& val) {
    func_in_31533(val);
    const int x = 31533;
    std::cout << 31533;
    for (int i = 0; i < 31533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31533(int& val) {
    const int x = 31533;
    std::cout << 31533;
    for (int i = 0; i < 31533; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31534(int& val) {
    func_in_31534(val);
    const int x = 31534;
    std::cout << 31534;
    for (int i = 0; i < 31534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31534(int& val) {
    const int x = 31534;
    std::cout << 31534;
    for (int i = 0; i < 31534; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31535(int& val) {
    func_in_31535(val);
    const int x = 31535;
    std::cout << 31535;
    for (int i = 0; i < 31535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31535(int& val) {
    const int x = 31535;
    std::cout << 31535;
    for (int i = 0; i < 31535; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31536(int& val) {
    func_in_31536(val);
    const int x = 31536;
    std::cout << 31536;
    for (int i = 0; i < 31536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31536(int& val) {
    const int x = 31536;
    std::cout << 31536;
    for (int i = 0; i < 31536; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31537(int& val) {
    func_in_31537(val);
    const int x = 31537;
    std::cout << 31537;
    for (int i = 0; i < 31537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31537(int& val) {
    const int x = 31537;
    std::cout << 31537;
    for (int i = 0; i < 31537; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31538(int& val) {
    func_in_31538(val);
    const int x = 31538;
    std::cout << 31538;
    for (int i = 0; i < 31538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31538(int& val) {
    const int x = 31538;
    std::cout << 31538;
    for (int i = 0; i < 31538; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31539(int& val) {
    func_in_31539(val);
    const int x = 31539;
    std::cout << 31539;
    for (int i = 0; i < 31539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31539(int& val) {
    const int x = 31539;
    std::cout << 31539;
    for (int i = 0; i < 31539; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31540(int& val) {
    func_in_31540(val);
    const int x = 31540;
    std::cout << 31540;
    for (int i = 0; i < 31540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31540(int& val) {
    const int x = 31540;
    std::cout << 31540;
    for (int i = 0; i < 31540; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31541(int& val) {
    func_in_31541(val);
    const int x = 31541;
    std::cout << 31541;
    for (int i = 0; i < 31541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31541(int& val) {
    const int x = 31541;
    std::cout << 31541;
    for (int i = 0; i < 31541; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31542(int& val) {
    func_in_31542(val);
    const int x = 31542;
    std::cout << 31542;
    for (int i = 0; i < 31542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31542(int& val) {
    const int x = 31542;
    std::cout << 31542;
    for (int i = 0; i < 31542; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31543(int& val) {
    func_in_31543(val);
    const int x = 31543;
    std::cout << 31543;
    for (int i = 0; i < 31543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31543(int& val) {
    const int x = 31543;
    std::cout << 31543;
    for (int i = 0; i < 31543; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31544(int& val) {
    func_in_31544(val);
    const int x = 31544;
    std::cout << 31544;
    for (int i = 0; i < 31544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31544(int& val) {
    const int x = 31544;
    std::cout << 31544;
    for (int i = 0; i < 31544; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31545(int& val) {
    func_in_31545(val);
    const int x = 31545;
    std::cout << 31545;
    for (int i = 0; i < 31545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31545(int& val) {
    const int x = 31545;
    std::cout << 31545;
    for (int i = 0; i < 31545; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31546(int& val) {
    func_in_31546(val);
    const int x = 31546;
    std::cout << 31546;
    for (int i = 0; i < 31546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31546(int& val) {
    const int x = 31546;
    std::cout << 31546;
    for (int i = 0; i < 31546; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31547(int& val) {
    func_in_31547(val);
    const int x = 31547;
    std::cout << 31547;
    for (int i = 0; i < 31547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31547(int& val) {
    const int x = 31547;
    std::cout << 31547;
    for (int i = 0; i < 31547; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31548(int& val) {
    func_in_31548(val);
    const int x = 31548;
    std::cout << 31548;
    for (int i = 0; i < 31548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31548(int& val) {
    const int x = 31548;
    std::cout << 31548;
    for (int i = 0; i < 31548; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31549(int& val) {
    func_in_31549(val);
    const int x = 31549;
    std::cout << 31549;
    for (int i = 0; i < 31549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31549(int& val) {
    const int x = 31549;
    std::cout << 31549;
    for (int i = 0; i < 31549; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31550(int& val) {
    func_in_31550(val);
    const int x = 31550;
    std::cout << 31550;
    for (int i = 0; i < 31550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31550(int& val) {
    const int x = 31550;
    std::cout << 31550;
    for (int i = 0; i < 31550; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31551(int& val) {
    func_in_31551(val);
    const int x = 31551;
    std::cout << 31551;
    for (int i = 0; i < 31551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31551(int& val) {
    const int x = 31551;
    std::cout << 31551;
    for (int i = 0; i < 31551; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31552(int& val) {
    func_in_31552(val);
    const int x = 31552;
    std::cout << 31552;
    for (int i = 0; i < 31552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31552(int& val) {
    const int x = 31552;
    std::cout << 31552;
    for (int i = 0; i < 31552; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31553(int& val) {
    func_in_31553(val);
    const int x = 31553;
    std::cout << 31553;
    for (int i = 0; i < 31553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31553(int& val) {
    const int x = 31553;
    std::cout << 31553;
    for (int i = 0; i < 31553; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31554(int& val) {
    func_in_31554(val);
    const int x = 31554;
    std::cout << 31554;
    for (int i = 0; i < 31554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31554(int& val) {
    const int x = 31554;
    std::cout << 31554;
    for (int i = 0; i < 31554; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31555(int& val) {
    func_in_31555(val);
    const int x = 31555;
    std::cout << 31555;
    for (int i = 0; i < 31555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31555(int& val) {
    const int x = 31555;
    std::cout << 31555;
    for (int i = 0; i < 31555; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31556(int& val) {
    func_in_31556(val);
    const int x = 31556;
    std::cout << 31556;
    for (int i = 0; i < 31556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31556(int& val) {
    const int x = 31556;
    std::cout << 31556;
    for (int i = 0; i < 31556; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31557(int& val) {
    func_in_31557(val);
    const int x = 31557;
    std::cout << 31557;
    for (int i = 0; i < 31557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31557(int& val) {
    const int x = 31557;
    std::cout << 31557;
    for (int i = 0; i < 31557; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31558(int& val) {
    func_in_31558(val);
    const int x = 31558;
    std::cout << 31558;
    for (int i = 0; i < 31558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31558(int& val) {
    const int x = 31558;
    std::cout << 31558;
    for (int i = 0; i < 31558; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31559(int& val) {
    func_in_31559(val);
    const int x = 31559;
    std::cout << 31559;
    for (int i = 0; i < 31559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31559(int& val) {
    const int x = 31559;
    std::cout << 31559;
    for (int i = 0; i < 31559; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31560(int& val) {
    func_in_31560(val);
    const int x = 31560;
    std::cout << 31560;
    for (int i = 0; i < 31560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31560(int& val) {
    const int x = 31560;
    std::cout << 31560;
    for (int i = 0; i < 31560; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31561(int& val) {
    func_in_31561(val);
    const int x = 31561;
    std::cout << 31561;
    for (int i = 0; i < 31561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31561(int& val) {
    const int x = 31561;
    std::cout << 31561;
    for (int i = 0; i < 31561; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31562(int& val) {
    func_in_31562(val);
    const int x = 31562;
    std::cout << 31562;
    for (int i = 0; i < 31562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31562(int& val) {
    const int x = 31562;
    std::cout << 31562;
    for (int i = 0; i < 31562; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31563(int& val) {
    func_in_31563(val);
    const int x = 31563;
    std::cout << 31563;
    for (int i = 0; i < 31563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31563(int& val) {
    const int x = 31563;
    std::cout << 31563;
    for (int i = 0; i < 31563; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31564(int& val) {
    func_in_31564(val);
    const int x = 31564;
    std::cout << 31564;
    for (int i = 0; i < 31564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31564(int& val) {
    const int x = 31564;
    std::cout << 31564;
    for (int i = 0; i < 31564; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31565(int& val) {
    func_in_31565(val);
    const int x = 31565;
    std::cout << 31565;
    for (int i = 0; i < 31565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31565(int& val) {
    const int x = 31565;
    std::cout << 31565;
    for (int i = 0; i < 31565; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31566(int& val) {
    func_in_31566(val);
    const int x = 31566;
    std::cout << 31566;
    for (int i = 0; i < 31566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31566(int& val) {
    const int x = 31566;
    std::cout << 31566;
    for (int i = 0; i < 31566; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31567(int& val) {
    func_in_31567(val);
    const int x = 31567;
    std::cout << 31567;
    for (int i = 0; i < 31567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31567(int& val) {
    const int x = 31567;
    std::cout << 31567;
    for (int i = 0; i < 31567; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31568(int& val) {
    func_in_31568(val);
    const int x = 31568;
    std::cout << 31568;
    for (int i = 0; i < 31568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31568(int& val) {
    const int x = 31568;
    std::cout << 31568;
    for (int i = 0; i < 31568; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31569(int& val) {
    func_in_31569(val);
    const int x = 31569;
    std::cout << 31569;
    for (int i = 0; i < 31569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31569(int& val) {
    const int x = 31569;
    std::cout << 31569;
    for (int i = 0; i < 31569; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31570(int& val) {
    func_in_31570(val);
    const int x = 31570;
    std::cout << 31570;
    for (int i = 0; i < 31570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31570(int& val) {
    const int x = 31570;
    std::cout << 31570;
    for (int i = 0; i < 31570; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31571(int& val) {
    func_in_31571(val);
    const int x = 31571;
    std::cout << 31571;
    for (int i = 0; i < 31571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31571(int& val) {
    const int x = 31571;
    std::cout << 31571;
    for (int i = 0; i < 31571; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31572(int& val) {
    func_in_31572(val);
    const int x = 31572;
    std::cout << 31572;
    for (int i = 0; i < 31572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31572(int& val) {
    const int x = 31572;
    std::cout << 31572;
    for (int i = 0; i < 31572; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31573(int& val) {
    func_in_31573(val);
    const int x = 31573;
    std::cout << 31573;
    for (int i = 0; i < 31573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31573(int& val) {
    const int x = 31573;
    std::cout << 31573;
    for (int i = 0; i < 31573; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31574(int& val) {
    func_in_31574(val);
    const int x = 31574;
    std::cout << 31574;
    for (int i = 0; i < 31574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31574(int& val) {
    const int x = 31574;
    std::cout << 31574;
    for (int i = 0; i < 31574; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31575(int& val) {
    func_in_31575(val);
    const int x = 31575;
    std::cout << 31575;
    for (int i = 0; i < 31575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31575(int& val) {
    const int x = 31575;
    std::cout << 31575;
    for (int i = 0; i < 31575; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31576(int& val) {
    func_in_31576(val);
    const int x = 31576;
    std::cout << 31576;
    for (int i = 0; i < 31576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31576(int& val) {
    const int x = 31576;
    std::cout << 31576;
    for (int i = 0; i < 31576; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31577(int& val) {
    func_in_31577(val);
    const int x = 31577;
    std::cout << 31577;
    for (int i = 0; i < 31577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31577(int& val) {
    const int x = 31577;
    std::cout << 31577;
    for (int i = 0; i < 31577; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31578(int& val) {
    func_in_31578(val);
    const int x = 31578;
    std::cout << 31578;
    for (int i = 0; i < 31578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31578(int& val) {
    const int x = 31578;
    std::cout << 31578;
    for (int i = 0; i < 31578; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31579(int& val) {
    func_in_31579(val);
    const int x = 31579;
    std::cout << 31579;
    for (int i = 0; i < 31579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31579(int& val) {
    const int x = 31579;
    std::cout << 31579;
    for (int i = 0; i < 31579; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31580(int& val) {
    func_in_31580(val);
    const int x = 31580;
    std::cout << 31580;
    for (int i = 0; i < 31580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31580(int& val) {
    const int x = 31580;
    std::cout << 31580;
    for (int i = 0; i < 31580; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31581(int& val) {
    func_in_31581(val);
    const int x = 31581;
    std::cout << 31581;
    for (int i = 0; i < 31581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31581(int& val) {
    const int x = 31581;
    std::cout << 31581;
    for (int i = 0; i < 31581; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31582(int& val) {
    func_in_31582(val);
    const int x = 31582;
    std::cout << 31582;
    for (int i = 0; i < 31582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31582(int& val) {
    const int x = 31582;
    std::cout << 31582;
    for (int i = 0; i < 31582; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31583(int& val) {
    func_in_31583(val);
    const int x = 31583;
    std::cout << 31583;
    for (int i = 0; i < 31583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31583(int& val) {
    const int x = 31583;
    std::cout << 31583;
    for (int i = 0; i < 31583; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31584(int& val) {
    func_in_31584(val);
    const int x = 31584;
    std::cout << 31584;
    for (int i = 0; i < 31584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31584(int& val) {
    const int x = 31584;
    std::cout << 31584;
    for (int i = 0; i < 31584; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31585(int& val) {
    func_in_31585(val);
    const int x = 31585;
    std::cout << 31585;
    for (int i = 0; i < 31585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31585(int& val) {
    const int x = 31585;
    std::cout << 31585;
    for (int i = 0; i < 31585; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31586(int& val) {
    func_in_31586(val);
    const int x = 31586;
    std::cout << 31586;
    for (int i = 0; i < 31586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31586(int& val) {
    const int x = 31586;
    std::cout << 31586;
    for (int i = 0; i < 31586; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31587(int& val) {
    func_in_31587(val);
    const int x = 31587;
    std::cout << 31587;
    for (int i = 0; i < 31587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31587(int& val) {
    const int x = 31587;
    std::cout << 31587;
    for (int i = 0; i < 31587; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31588(int& val) {
    func_in_31588(val);
    const int x = 31588;
    std::cout << 31588;
    for (int i = 0; i < 31588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31588(int& val) {
    const int x = 31588;
    std::cout << 31588;
    for (int i = 0; i < 31588; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31589(int& val) {
    func_in_31589(val);
    const int x = 31589;
    std::cout << 31589;
    for (int i = 0; i < 31589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31589(int& val) {
    const int x = 31589;
    std::cout << 31589;
    for (int i = 0; i < 31589; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31590(int& val) {
    func_in_31590(val);
    const int x = 31590;
    std::cout << 31590;
    for (int i = 0; i < 31590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31590(int& val) {
    const int x = 31590;
    std::cout << 31590;
    for (int i = 0; i < 31590; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31591(int& val) {
    func_in_31591(val);
    const int x = 31591;
    std::cout << 31591;
    for (int i = 0; i < 31591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31591(int& val) {
    const int x = 31591;
    std::cout << 31591;
    for (int i = 0; i < 31591; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31592(int& val) {
    func_in_31592(val);
    const int x = 31592;
    std::cout << 31592;
    for (int i = 0; i < 31592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31592(int& val) {
    const int x = 31592;
    std::cout << 31592;
    for (int i = 0; i < 31592; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31593(int& val) {
    func_in_31593(val);
    const int x = 31593;
    std::cout << 31593;
    for (int i = 0; i < 31593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31593(int& val) {
    const int x = 31593;
    std::cout << 31593;
    for (int i = 0; i < 31593; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31594(int& val) {
    func_in_31594(val);
    const int x = 31594;
    std::cout << 31594;
    for (int i = 0; i < 31594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31594(int& val) {
    const int x = 31594;
    std::cout << 31594;
    for (int i = 0; i < 31594; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31595(int& val) {
    func_in_31595(val);
    const int x = 31595;
    std::cout << 31595;
    for (int i = 0; i < 31595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31595(int& val) {
    const int x = 31595;
    std::cout << 31595;
    for (int i = 0; i < 31595; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31596(int& val) {
    func_in_31596(val);
    const int x = 31596;
    std::cout << 31596;
    for (int i = 0; i < 31596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31596(int& val) {
    const int x = 31596;
    std::cout << 31596;
    for (int i = 0; i < 31596; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31597(int& val) {
    func_in_31597(val);
    const int x = 31597;
    std::cout << 31597;
    for (int i = 0; i < 31597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31597(int& val) {
    const int x = 31597;
    std::cout << 31597;
    for (int i = 0; i < 31597; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31598(int& val) {
    func_in_31598(val);
    const int x = 31598;
    std::cout << 31598;
    for (int i = 0; i < 31598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31598(int& val) {
    const int x = 31598;
    std::cout << 31598;
    for (int i = 0; i < 31598; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31599(int& val) {
    func_in_31599(val);
    const int x = 31599;
    std::cout << 31599;
    for (int i = 0; i < 31599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31599(int& val) {
    const int x = 31599;
    std::cout << 31599;
    for (int i = 0; i < 31599; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31600(int& val) {
    func_in_31600(val);
    const int x = 31600;
    std::cout << 31600;
    for (int i = 0; i < 31600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31600(int& val) {
    const int x = 31600;
    std::cout << 31600;
    for (int i = 0; i < 31600; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31601(int& val) {
    func_in_31601(val);
    const int x = 31601;
    std::cout << 31601;
    for (int i = 0; i < 31601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31601(int& val) {
    const int x = 31601;
    std::cout << 31601;
    for (int i = 0; i < 31601; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31602(int& val) {
    func_in_31602(val);
    const int x = 31602;
    std::cout << 31602;
    for (int i = 0; i < 31602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31602(int& val) {
    const int x = 31602;
    std::cout << 31602;
    for (int i = 0; i < 31602; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31603(int& val) {
    func_in_31603(val);
    const int x = 31603;
    std::cout << 31603;
    for (int i = 0; i < 31603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31603(int& val) {
    const int x = 31603;
    std::cout << 31603;
    for (int i = 0; i < 31603; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31604(int& val) {
    func_in_31604(val);
    const int x = 31604;
    std::cout << 31604;
    for (int i = 0; i < 31604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31604(int& val) {
    const int x = 31604;
    std::cout << 31604;
    for (int i = 0; i < 31604; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31605(int& val) {
    func_in_31605(val);
    const int x = 31605;
    std::cout << 31605;
    for (int i = 0; i < 31605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31605(int& val) {
    const int x = 31605;
    std::cout << 31605;
    for (int i = 0; i < 31605; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31606(int& val) {
    func_in_31606(val);
    const int x = 31606;
    std::cout << 31606;
    for (int i = 0; i < 31606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31606(int& val) {
    const int x = 31606;
    std::cout << 31606;
    for (int i = 0; i < 31606; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31607(int& val) {
    func_in_31607(val);
    const int x = 31607;
    std::cout << 31607;
    for (int i = 0; i < 31607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31607(int& val) {
    const int x = 31607;
    std::cout << 31607;
    for (int i = 0; i < 31607; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31608(int& val) {
    func_in_31608(val);
    const int x = 31608;
    std::cout << 31608;
    for (int i = 0; i < 31608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31608(int& val) {
    const int x = 31608;
    std::cout << 31608;
    for (int i = 0; i < 31608; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31609(int& val) {
    func_in_31609(val);
    const int x = 31609;
    std::cout << 31609;
    for (int i = 0; i < 31609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31609(int& val) {
    const int x = 31609;
    std::cout << 31609;
    for (int i = 0; i < 31609; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31610(int& val) {
    func_in_31610(val);
    const int x = 31610;
    std::cout << 31610;
    for (int i = 0; i < 31610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31610(int& val) {
    const int x = 31610;
    std::cout << 31610;
    for (int i = 0; i < 31610; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31611(int& val) {
    func_in_31611(val);
    const int x = 31611;
    std::cout << 31611;
    for (int i = 0; i < 31611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31611(int& val) {
    const int x = 31611;
    std::cout << 31611;
    for (int i = 0; i < 31611; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31612(int& val) {
    func_in_31612(val);
    const int x = 31612;
    std::cout << 31612;
    for (int i = 0; i < 31612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31612(int& val) {
    const int x = 31612;
    std::cout << 31612;
    for (int i = 0; i < 31612; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31613(int& val) {
    func_in_31613(val);
    const int x = 31613;
    std::cout << 31613;
    for (int i = 0; i < 31613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31613(int& val) {
    const int x = 31613;
    std::cout << 31613;
    for (int i = 0; i < 31613; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31614(int& val) {
    func_in_31614(val);
    const int x = 31614;
    std::cout << 31614;
    for (int i = 0; i < 31614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31614(int& val) {
    const int x = 31614;
    std::cout << 31614;
    for (int i = 0; i < 31614; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31615(int& val) {
    func_in_31615(val);
    const int x = 31615;
    std::cout << 31615;
    for (int i = 0; i < 31615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31615(int& val) {
    const int x = 31615;
    std::cout << 31615;
    for (int i = 0; i < 31615; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31616(int& val) {
    func_in_31616(val);
    const int x = 31616;
    std::cout << 31616;
    for (int i = 0; i < 31616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31616(int& val) {
    const int x = 31616;
    std::cout << 31616;
    for (int i = 0; i < 31616; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31617(int& val) {
    func_in_31617(val);
    const int x = 31617;
    std::cout << 31617;
    for (int i = 0; i < 31617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31617(int& val) {
    const int x = 31617;
    std::cout << 31617;
    for (int i = 0; i < 31617; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31618(int& val) {
    func_in_31618(val);
    const int x = 31618;
    std::cout << 31618;
    for (int i = 0; i < 31618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31618(int& val) {
    const int x = 31618;
    std::cout << 31618;
    for (int i = 0; i < 31618; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31619(int& val) {
    func_in_31619(val);
    const int x = 31619;
    std::cout << 31619;
    for (int i = 0; i < 31619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31619(int& val) {
    const int x = 31619;
    std::cout << 31619;
    for (int i = 0; i < 31619; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31620(int& val) {
    func_in_31620(val);
    const int x = 31620;
    std::cout << 31620;
    for (int i = 0; i < 31620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31620(int& val) {
    const int x = 31620;
    std::cout << 31620;
    for (int i = 0; i < 31620; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31621(int& val) {
    func_in_31621(val);
    const int x = 31621;
    std::cout << 31621;
    for (int i = 0; i < 31621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31621(int& val) {
    const int x = 31621;
    std::cout << 31621;
    for (int i = 0; i < 31621; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31622(int& val) {
    func_in_31622(val);
    const int x = 31622;
    std::cout << 31622;
    for (int i = 0; i < 31622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31622(int& val) {
    const int x = 31622;
    std::cout << 31622;
    for (int i = 0; i < 31622; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31623(int& val) {
    func_in_31623(val);
    const int x = 31623;
    std::cout << 31623;
    for (int i = 0; i < 31623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31623(int& val) {
    const int x = 31623;
    std::cout << 31623;
    for (int i = 0; i < 31623; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31624(int& val) {
    func_in_31624(val);
    const int x = 31624;
    std::cout << 31624;
    for (int i = 0; i < 31624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31624(int& val) {
    const int x = 31624;
    std::cout << 31624;
    for (int i = 0; i < 31624; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31625(int& val) {
    func_in_31625(val);
    const int x = 31625;
    std::cout << 31625;
    for (int i = 0; i < 31625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31625(int& val) {
    const int x = 31625;
    std::cout << 31625;
    for (int i = 0; i < 31625; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31626(int& val) {
    func_in_31626(val);
    const int x = 31626;
    std::cout << 31626;
    for (int i = 0; i < 31626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31626(int& val) {
    const int x = 31626;
    std::cout << 31626;
    for (int i = 0; i < 31626; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31627(int& val) {
    func_in_31627(val);
    const int x = 31627;
    std::cout << 31627;
    for (int i = 0; i < 31627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31627(int& val) {
    const int x = 31627;
    std::cout << 31627;
    for (int i = 0; i < 31627; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31628(int& val) {
    func_in_31628(val);
    const int x = 31628;
    std::cout << 31628;
    for (int i = 0; i < 31628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31628(int& val) {
    const int x = 31628;
    std::cout << 31628;
    for (int i = 0; i < 31628; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31629(int& val) {
    func_in_31629(val);
    const int x = 31629;
    std::cout << 31629;
    for (int i = 0; i < 31629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31629(int& val) {
    const int x = 31629;
    std::cout << 31629;
    for (int i = 0; i < 31629; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31630(int& val) {
    func_in_31630(val);
    const int x = 31630;
    std::cout << 31630;
    for (int i = 0; i < 31630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31630(int& val) {
    const int x = 31630;
    std::cout << 31630;
    for (int i = 0; i < 31630; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31631(int& val) {
    func_in_31631(val);
    const int x = 31631;
    std::cout << 31631;
    for (int i = 0; i < 31631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31631(int& val) {
    const int x = 31631;
    std::cout << 31631;
    for (int i = 0; i < 31631; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31632(int& val) {
    func_in_31632(val);
    const int x = 31632;
    std::cout << 31632;
    for (int i = 0; i < 31632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31632(int& val) {
    const int x = 31632;
    std::cout << 31632;
    for (int i = 0; i < 31632; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31633(int& val) {
    func_in_31633(val);
    const int x = 31633;
    std::cout << 31633;
    for (int i = 0; i < 31633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31633(int& val) {
    const int x = 31633;
    std::cout << 31633;
    for (int i = 0; i < 31633; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31634(int& val) {
    func_in_31634(val);
    const int x = 31634;
    std::cout << 31634;
    for (int i = 0; i < 31634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31634(int& val) {
    const int x = 31634;
    std::cout << 31634;
    for (int i = 0; i < 31634; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31635(int& val) {
    func_in_31635(val);
    const int x = 31635;
    std::cout << 31635;
    for (int i = 0; i < 31635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31635(int& val) {
    const int x = 31635;
    std::cout << 31635;
    for (int i = 0; i < 31635; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31636(int& val) {
    func_in_31636(val);
    const int x = 31636;
    std::cout << 31636;
    for (int i = 0; i < 31636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31636(int& val) {
    const int x = 31636;
    std::cout << 31636;
    for (int i = 0; i < 31636; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31637(int& val) {
    func_in_31637(val);
    const int x = 31637;
    std::cout << 31637;
    for (int i = 0; i < 31637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31637(int& val) {
    const int x = 31637;
    std::cout << 31637;
    for (int i = 0; i < 31637; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31638(int& val) {
    func_in_31638(val);
    const int x = 31638;
    std::cout << 31638;
    for (int i = 0; i < 31638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31638(int& val) {
    const int x = 31638;
    std::cout << 31638;
    for (int i = 0; i < 31638; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31639(int& val) {
    func_in_31639(val);
    const int x = 31639;
    std::cout << 31639;
    for (int i = 0; i < 31639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31639(int& val) {
    const int x = 31639;
    std::cout << 31639;
    for (int i = 0; i < 31639; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31640(int& val) {
    func_in_31640(val);
    const int x = 31640;
    std::cout << 31640;
    for (int i = 0; i < 31640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31640(int& val) {
    const int x = 31640;
    std::cout << 31640;
    for (int i = 0; i < 31640; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31641(int& val) {
    func_in_31641(val);
    const int x = 31641;
    std::cout << 31641;
    for (int i = 0; i < 31641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31641(int& val) {
    const int x = 31641;
    std::cout << 31641;
    for (int i = 0; i < 31641; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31642(int& val) {
    func_in_31642(val);
    const int x = 31642;
    std::cout << 31642;
    for (int i = 0; i < 31642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31642(int& val) {
    const int x = 31642;
    std::cout << 31642;
    for (int i = 0; i < 31642; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31643(int& val) {
    func_in_31643(val);
    const int x = 31643;
    std::cout << 31643;
    for (int i = 0; i < 31643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31643(int& val) {
    const int x = 31643;
    std::cout << 31643;
    for (int i = 0; i < 31643; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31644(int& val) {
    func_in_31644(val);
    const int x = 31644;
    std::cout << 31644;
    for (int i = 0; i < 31644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31644(int& val) {
    const int x = 31644;
    std::cout << 31644;
    for (int i = 0; i < 31644; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31645(int& val) {
    func_in_31645(val);
    const int x = 31645;
    std::cout << 31645;
    for (int i = 0; i < 31645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31645(int& val) {
    const int x = 31645;
    std::cout << 31645;
    for (int i = 0; i < 31645; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31646(int& val) {
    func_in_31646(val);
    const int x = 31646;
    std::cout << 31646;
    for (int i = 0; i < 31646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31646(int& val) {
    const int x = 31646;
    std::cout << 31646;
    for (int i = 0; i < 31646; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31647(int& val) {
    func_in_31647(val);
    const int x = 31647;
    std::cout << 31647;
    for (int i = 0; i < 31647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31647(int& val) {
    const int x = 31647;
    std::cout << 31647;
    for (int i = 0; i < 31647; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31648(int& val) {
    func_in_31648(val);
    const int x = 31648;
    std::cout << 31648;
    for (int i = 0; i < 31648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31648(int& val) {
    const int x = 31648;
    std::cout << 31648;
    for (int i = 0; i < 31648; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31649(int& val) {
    func_in_31649(val);
    const int x = 31649;
    std::cout << 31649;
    for (int i = 0; i < 31649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31649(int& val) {
    const int x = 31649;
    std::cout << 31649;
    for (int i = 0; i < 31649; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31650(int& val) {
    func_in_31650(val);
    const int x = 31650;
    std::cout << 31650;
    for (int i = 0; i < 31650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31650(int& val) {
    const int x = 31650;
    std::cout << 31650;
    for (int i = 0; i < 31650; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31651(int& val) {
    func_in_31651(val);
    const int x = 31651;
    std::cout << 31651;
    for (int i = 0; i < 31651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31651(int& val) {
    const int x = 31651;
    std::cout << 31651;
    for (int i = 0; i < 31651; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31652(int& val) {
    func_in_31652(val);
    const int x = 31652;
    std::cout << 31652;
    for (int i = 0; i < 31652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31652(int& val) {
    const int x = 31652;
    std::cout << 31652;
    for (int i = 0; i < 31652; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31653(int& val) {
    func_in_31653(val);
    const int x = 31653;
    std::cout << 31653;
    for (int i = 0; i < 31653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31653(int& val) {
    const int x = 31653;
    std::cout << 31653;
    for (int i = 0; i < 31653; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31654(int& val) {
    func_in_31654(val);
    const int x = 31654;
    std::cout << 31654;
    for (int i = 0; i < 31654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31654(int& val) {
    const int x = 31654;
    std::cout << 31654;
    for (int i = 0; i < 31654; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31655(int& val) {
    func_in_31655(val);
    const int x = 31655;
    std::cout << 31655;
    for (int i = 0; i < 31655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31655(int& val) {
    const int x = 31655;
    std::cout << 31655;
    for (int i = 0; i < 31655; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31656(int& val) {
    func_in_31656(val);
    const int x = 31656;
    std::cout << 31656;
    for (int i = 0; i < 31656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31656(int& val) {
    const int x = 31656;
    std::cout << 31656;
    for (int i = 0; i < 31656; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31657(int& val) {
    func_in_31657(val);
    const int x = 31657;
    std::cout << 31657;
    for (int i = 0; i < 31657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31657(int& val) {
    const int x = 31657;
    std::cout << 31657;
    for (int i = 0; i < 31657; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31658(int& val) {
    func_in_31658(val);
    const int x = 31658;
    std::cout << 31658;
    for (int i = 0; i < 31658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31658(int& val) {
    const int x = 31658;
    std::cout << 31658;
    for (int i = 0; i < 31658; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31659(int& val) {
    func_in_31659(val);
    const int x = 31659;
    std::cout << 31659;
    for (int i = 0; i < 31659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31659(int& val) {
    const int x = 31659;
    std::cout << 31659;
    for (int i = 0; i < 31659; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31660(int& val) {
    func_in_31660(val);
    const int x = 31660;
    std::cout << 31660;
    for (int i = 0; i < 31660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31660(int& val) {
    const int x = 31660;
    std::cout << 31660;
    for (int i = 0; i < 31660; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31661(int& val) {
    func_in_31661(val);
    const int x = 31661;
    std::cout << 31661;
    for (int i = 0; i < 31661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31661(int& val) {
    const int x = 31661;
    std::cout << 31661;
    for (int i = 0; i < 31661; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31662(int& val) {
    func_in_31662(val);
    const int x = 31662;
    std::cout << 31662;
    for (int i = 0; i < 31662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31662(int& val) {
    const int x = 31662;
    std::cout << 31662;
    for (int i = 0; i < 31662; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31663(int& val) {
    func_in_31663(val);
    const int x = 31663;
    std::cout << 31663;
    for (int i = 0; i < 31663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31663(int& val) {
    const int x = 31663;
    std::cout << 31663;
    for (int i = 0; i < 31663; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31664(int& val) {
    func_in_31664(val);
    const int x = 31664;
    std::cout << 31664;
    for (int i = 0; i < 31664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31664(int& val) {
    const int x = 31664;
    std::cout << 31664;
    for (int i = 0; i < 31664; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31665(int& val) {
    func_in_31665(val);
    const int x = 31665;
    std::cout << 31665;
    for (int i = 0; i < 31665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31665(int& val) {
    const int x = 31665;
    std::cout << 31665;
    for (int i = 0; i < 31665; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31666(int& val) {
    func_in_31666(val);
    const int x = 31666;
    std::cout << 31666;
    for (int i = 0; i < 31666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31666(int& val) {
    const int x = 31666;
    std::cout << 31666;
    for (int i = 0; i < 31666; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31667(int& val) {
    func_in_31667(val);
    const int x = 31667;
    std::cout << 31667;
    for (int i = 0; i < 31667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31667(int& val) {
    const int x = 31667;
    std::cout << 31667;
    for (int i = 0; i < 31667; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31668(int& val) {
    func_in_31668(val);
    const int x = 31668;
    std::cout << 31668;
    for (int i = 0; i < 31668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31668(int& val) {
    const int x = 31668;
    std::cout << 31668;
    for (int i = 0; i < 31668; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31669(int& val) {
    func_in_31669(val);
    const int x = 31669;
    std::cout << 31669;
    for (int i = 0; i < 31669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31669(int& val) {
    const int x = 31669;
    std::cout << 31669;
    for (int i = 0; i < 31669; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31670(int& val) {
    func_in_31670(val);
    const int x = 31670;
    std::cout << 31670;
    for (int i = 0; i < 31670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31670(int& val) {
    const int x = 31670;
    std::cout << 31670;
    for (int i = 0; i < 31670; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31671(int& val) {
    func_in_31671(val);
    const int x = 31671;
    std::cout << 31671;
    for (int i = 0; i < 31671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31671(int& val) {
    const int x = 31671;
    std::cout << 31671;
    for (int i = 0; i < 31671; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31672(int& val) {
    func_in_31672(val);
    const int x = 31672;
    std::cout << 31672;
    for (int i = 0; i < 31672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31672(int& val) {
    const int x = 31672;
    std::cout << 31672;
    for (int i = 0; i < 31672; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31673(int& val) {
    func_in_31673(val);
    const int x = 31673;
    std::cout << 31673;
    for (int i = 0; i < 31673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31673(int& val) {
    const int x = 31673;
    std::cout << 31673;
    for (int i = 0; i < 31673; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31674(int& val) {
    func_in_31674(val);
    const int x = 31674;
    std::cout << 31674;
    for (int i = 0; i < 31674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31674(int& val) {
    const int x = 31674;
    std::cout << 31674;
    for (int i = 0; i < 31674; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31675(int& val) {
    func_in_31675(val);
    const int x = 31675;
    std::cout << 31675;
    for (int i = 0; i < 31675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31675(int& val) {
    const int x = 31675;
    std::cout << 31675;
    for (int i = 0; i < 31675; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31676(int& val) {
    func_in_31676(val);
    const int x = 31676;
    std::cout << 31676;
    for (int i = 0; i < 31676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31676(int& val) {
    const int x = 31676;
    std::cout << 31676;
    for (int i = 0; i < 31676; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31677(int& val) {
    func_in_31677(val);
    const int x = 31677;
    std::cout << 31677;
    for (int i = 0; i < 31677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31677(int& val) {
    const int x = 31677;
    std::cout << 31677;
    for (int i = 0; i < 31677; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31678(int& val) {
    func_in_31678(val);
    const int x = 31678;
    std::cout << 31678;
    for (int i = 0; i < 31678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31678(int& val) {
    const int x = 31678;
    std::cout << 31678;
    for (int i = 0; i < 31678; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31679(int& val) {
    func_in_31679(val);
    const int x = 31679;
    std::cout << 31679;
    for (int i = 0; i < 31679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31679(int& val) {
    const int x = 31679;
    std::cout << 31679;
    for (int i = 0; i < 31679; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31680(int& val) {
    func_in_31680(val);
    const int x = 31680;
    std::cout << 31680;
    for (int i = 0; i < 31680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31680(int& val) {
    const int x = 31680;
    std::cout << 31680;
    for (int i = 0; i < 31680; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31681(int& val) {
    func_in_31681(val);
    const int x = 31681;
    std::cout << 31681;
    for (int i = 0; i < 31681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31681(int& val) {
    const int x = 31681;
    std::cout << 31681;
    for (int i = 0; i < 31681; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31682(int& val) {
    func_in_31682(val);
    const int x = 31682;
    std::cout << 31682;
    for (int i = 0; i < 31682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31682(int& val) {
    const int x = 31682;
    std::cout << 31682;
    for (int i = 0; i < 31682; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31683(int& val) {
    func_in_31683(val);
    const int x = 31683;
    std::cout << 31683;
    for (int i = 0; i < 31683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31683(int& val) {
    const int x = 31683;
    std::cout << 31683;
    for (int i = 0; i < 31683; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31684(int& val) {
    func_in_31684(val);
    const int x = 31684;
    std::cout << 31684;
    for (int i = 0; i < 31684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31684(int& val) {
    const int x = 31684;
    std::cout << 31684;
    for (int i = 0; i < 31684; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31685(int& val) {
    func_in_31685(val);
    const int x = 31685;
    std::cout << 31685;
    for (int i = 0; i < 31685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31685(int& val) {
    const int x = 31685;
    std::cout << 31685;
    for (int i = 0; i < 31685; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31686(int& val) {
    func_in_31686(val);
    const int x = 31686;
    std::cout << 31686;
    for (int i = 0; i < 31686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31686(int& val) {
    const int x = 31686;
    std::cout << 31686;
    for (int i = 0; i < 31686; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31687(int& val) {
    func_in_31687(val);
    const int x = 31687;
    std::cout << 31687;
    for (int i = 0; i < 31687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31687(int& val) {
    const int x = 31687;
    std::cout << 31687;
    for (int i = 0; i < 31687; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31688(int& val) {
    func_in_31688(val);
    const int x = 31688;
    std::cout << 31688;
    for (int i = 0; i < 31688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31688(int& val) {
    const int x = 31688;
    std::cout << 31688;
    for (int i = 0; i < 31688; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31689(int& val) {
    func_in_31689(val);
    const int x = 31689;
    std::cout << 31689;
    for (int i = 0; i < 31689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31689(int& val) {
    const int x = 31689;
    std::cout << 31689;
    for (int i = 0; i < 31689; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31690(int& val) {
    func_in_31690(val);
    const int x = 31690;
    std::cout << 31690;
    for (int i = 0; i < 31690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31690(int& val) {
    const int x = 31690;
    std::cout << 31690;
    for (int i = 0; i < 31690; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31691(int& val) {
    func_in_31691(val);
    const int x = 31691;
    std::cout << 31691;
    for (int i = 0; i < 31691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31691(int& val) {
    const int x = 31691;
    std::cout << 31691;
    for (int i = 0; i < 31691; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31692(int& val) {
    func_in_31692(val);
    const int x = 31692;
    std::cout << 31692;
    for (int i = 0; i < 31692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31692(int& val) {
    const int x = 31692;
    std::cout << 31692;
    for (int i = 0; i < 31692; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31693(int& val) {
    func_in_31693(val);
    const int x = 31693;
    std::cout << 31693;
    for (int i = 0; i < 31693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31693(int& val) {
    const int x = 31693;
    std::cout << 31693;
    for (int i = 0; i < 31693; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31694(int& val) {
    func_in_31694(val);
    const int x = 31694;
    std::cout << 31694;
    for (int i = 0; i < 31694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31694(int& val) {
    const int x = 31694;
    std::cout << 31694;
    for (int i = 0; i < 31694; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31695(int& val) {
    func_in_31695(val);
    const int x = 31695;
    std::cout << 31695;
    for (int i = 0; i < 31695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31695(int& val) {
    const int x = 31695;
    std::cout << 31695;
    for (int i = 0; i < 31695; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31696(int& val) {
    func_in_31696(val);
    const int x = 31696;
    std::cout << 31696;
    for (int i = 0; i < 31696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31696(int& val) {
    const int x = 31696;
    std::cout << 31696;
    for (int i = 0; i < 31696; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31697(int& val) {
    func_in_31697(val);
    const int x = 31697;
    std::cout << 31697;
    for (int i = 0; i < 31697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31697(int& val) {
    const int x = 31697;
    std::cout << 31697;
    for (int i = 0; i < 31697; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31698(int& val) {
    func_in_31698(val);
    const int x = 31698;
    std::cout << 31698;
    for (int i = 0; i < 31698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31698(int& val) {
    const int x = 31698;
    std::cout << 31698;
    for (int i = 0; i < 31698; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31699(int& val) {
    func_in_31699(val);
    const int x = 31699;
    std::cout << 31699;
    for (int i = 0; i < 31699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31699(int& val) {
    const int x = 31699;
    std::cout << 31699;
    for (int i = 0; i < 31699; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31700(int& val) {
    func_in_31700(val);
    const int x = 31700;
    std::cout << 31700;
    for (int i = 0; i < 31700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31700(int& val) {
    const int x = 31700;
    std::cout << 31700;
    for (int i = 0; i < 31700; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31701(int& val) {
    func_in_31701(val);
    const int x = 31701;
    std::cout << 31701;
    for (int i = 0; i < 31701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31701(int& val) {
    const int x = 31701;
    std::cout << 31701;
    for (int i = 0; i < 31701; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31702(int& val) {
    func_in_31702(val);
    const int x = 31702;
    std::cout << 31702;
    for (int i = 0; i < 31702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31702(int& val) {
    const int x = 31702;
    std::cout << 31702;
    for (int i = 0; i < 31702; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31703(int& val) {
    func_in_31703(val);
    const int x = 31703;
    std::cout << 31703;
    for (int i = 0; i < 31703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31703(int& val) {
    const int x = 31703;
    std::cout << 31703;
    for (int i = 0; i < 31703; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31704(int& val) {
    func_in_31704(val);
    const int x = 31704;
    std::cout << 31704;
    for (int i = 0; i < 31704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31704(int& val) {
    const int x = 31704;
    std::cout << 31704;
    for (int i = 0; i < 31704; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31705(int& val) {
    func_in_31705(val);
    const int x = 31705;
    std::cout << 31705;
    for (int i = 0; i < 31705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31705(int& val) {
    const int x = 31705;
    std::cout << 31705;
    for (int i = 0; i < 31705; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31706(int& val) {
    func_in_31706(val);
    const int x = 31706;
    std::cout << 31706;
    for (int i = 0; i < 31706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31706(int& val) {
    const int x = 31706;
    std::cout << 31706;
    for (int i = 0; i < 31706; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31707(int& val) {
    func_in_31707(val);
    const int x = 31707;
    std::cout << 31707;
    for (int i = 0; i < 31707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31707(int& val) {
    const int x = 31707;
    std::cout << 31707;
    for (int i = 0; i < 31707; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31708(int& val) {
    func_in_31708(val);
    const int x = 31708;
    std::cout << 31708;
    for (int i = 0; i < 31708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31708(int& val) {
    const int x = 31708;
    std::cout << 31708;
    for (int i = 0; i < 31708; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31709(int& val) {
    func_in_31709(val);
    const int x = 31709;
    std::cout << 31709;
    for (int i = 0; i < 31709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31709(int& val) {
    const int x = 31709;
    std::cout << 31709;
    for (int i = 0; i < 31709; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31710(int& val) {
    func_in_31710(val);
    const int x = 31710;
    std::cout << 31710;
    for (int i = 0; i < 31710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31710(int& val) {
    const int x = 31710;
    std::cout << 31710;
    for (int i = 0; i < 31710; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31711(int& val) {
    func_in_31711(val);
    const int x = 31711;
    std::cout << 31711;
    for (int i = 0; i < 31711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31711(int& val) {
    const int x = 31711;
    std::cout << 31711;
    for (int i = 0; i < 31711; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31712(int& val) {
    func_in_31712(val);
    const int x = 31712;
    std::cout << 31712;
    for (int i = 0; i < 31712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31712(int& val) {
    const int x = 31712;
    std::cout << 31712;
    for (int i = 0; i < 31712; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31713(int& val) {
    func_in_31713(val);
    const int x = 31713;
    std::cout << 31713;
    for (int i = 0; i < 31713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31713(int& val) {
    const int x = 31713;
    std::cout << 31713;
    for (int i = 0; i < 31713; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31714(int& val) {
    func_in_31714(val);
    const int x = 31714;
    std::cout << 31714;
    for (int i = 0; i < 31714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31714(int& val) {
    const int x = 31714;
    std::cout << 31714;
    for (int i = 0; i < 31714; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31715(int& val) {
    func_in_31715(val);
    const int x = 31715;
    std::cout << 31715;
    for (int i = 0; i < 31715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31715(int& val) {
    const int x = 31715;
    std::cout << 31715;
    for (int i = 0; i < 31715; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31716(int& val) {
    func_in_31716(val);
    const int x = 31716;
    std::cout << 31716;
    for (int i = 0; i < 31716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31716(int& val) {
    const int x = 31716;
    std::cout << 31716;
    for (int i = 0; i < 31716; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31717(int& val) {
    func_in_31717(val);
    const int x = 31717;
    std::cout << 31717;
    for (int i = 0; i < 31717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31717(int& val) {
    const int x = 31717;
    std::cout << 31717;
    for (int i = 0; i < 31717; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31718(int& val) {
    func_in_31718(val);
    const int x = 31718;
    std::cout << 31718;
    for (int i = 0; i < 31718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31718(int& val) {
    const int x = 31718;
    std::cout << 31718;
    for (int i = 0; i < 31718; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31719(int& val) {
    func_in_31719(val);
    const int x = 31719;
    std::cout << 31719;
    for (int i = 0; i < 31719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31719(int& val) {
    const int x = 31719;
    std::cout << 31719;
    for (int i = 0; i < 31719; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31720(int& val) {
    func_in_31720(val);
    const int x = 31720;
    std::cout << 31720;
    for (int i = 0; i < 31720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31720(int& val) {
    const int x = 31720;
    std::cout << 31720;
    for (int i = 0; i < 31720; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31721(int& val) {
    func_in_31721(val);
    const int x = 31721;
    std::cout << 31721;
    for (int i = 0; i < 31721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31721(int& val) {
    const int x = 31721;
    std::cout << 31721;
    for (int i = 0; i < 31721; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31722(int& val) {
    func_in_31722(val);
    const int x = 31722;
    std::cout << 31722;
    for (int i = 0; i < 31722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31722(int& val) {
    const int x = 31722;
    std::cout << 31722;
    for (int i = 0; i < 31722; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31723(int& val) {
    func_in_31723(val);
    const int x = 31723;
    std::cout << 31723;
    for (int i = 0; i < 31723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31723(int& val) {
    const int x = 31723;
    std::cout << 31723;
    for (int i = 0; i < 31723; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31724(int& val) {
    func_in_31724(val);
    const int x = 31724;
    std::cout << 31724;
    for (int i = 0; i < 31724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31724(int& val) {
    const int x = 31724;
    std::cout << 31724;
    for (int i = 0; i < 31724; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31725(int& val) {
    func_in_31725(val);
    const int x = 31725;
    std::cout << 31725;
    for (int i = 0; i < 31725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31725(int& val) {
    const int x = 31725;
    std::cout << 31725;
    for (int i = 0; i < 31725; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31726(int& val) {
    func_in_31726(val);
    const int x = 31726;
    std::cout << 31726;
    for (int i = 0; i < 31726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31726(int& val) {
    const int x = 31726;
    std::cout << 31726;
    for (int i = 0; i < 31726; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31727(int& val) {
    func_in_31727(val);
    const int x = 31727;
    std::cout << 31727;
    for (int i = 0; i < 31727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31727(int& val) {
    const int x = 31727;
    std::cout << 31727;
    for (int i = 0; i < 31727; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31728(int& val) {
    func_in_31728(val);
    const int x = 31728;
    std::cout << 31728;
    for (int i = 0; i < 31728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31728(int& val) {
    const int x = 31728;
    std::cout << 31728;
    for (int i = 0; i < 31728; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31729(int& val) {
    func_in_31729(val);
    const int x = 31729;
    std::cout << 31729;
    for (int i = 0; i < 31729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31729(int& val) {
    const int x = 31729;
    std::cout << 31729;
    for (int i = 0; i < 31729; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31730(int& val) {
    func_in_31730(val);
    const int x = 31730;
    std::cout << 31730;
    for (int i = 0; i < 31730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31730(int& val) {
    const int x = 31730;
    std::cout << 31730;
    for (int i = 0; i < 31730; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31731(int& val) {
    func_in_31731(val);
    const int x = 31731;
    std::cout << 31731;
    for (int i = 0; i < 31731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31731(int& val) {
    const int x = 31731;
    std::cout << 31731;
    for (int i = 0; i < 31731; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31732(int& val) {
    func_in_31732(val);
    const int x = 31732;
    std::cout << 31732;
    for (int i = 0; i < 31732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31732(int& val) {
    const int x = 31732;
    std::cout << 31732;
    for (int i = 0; i < 31732; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31733(int& val) {
    func_in_31733(val);
    const int x = 31733;
    std::cout << 31733;
    for (int i = 0; i < 31733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31733(int& val) {
    const int x = 31733;
    std::cout << 31733;
    for (int i = 0; i < 31733; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31734(int& val) {
    func_in_31734(val);
    const int x = 31734;
    std::cout << 31734;
    for (int i = 0; i < 31734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31734(int& val) {
    const int x = 31734;
    std::cout << 31734;
    for (int i = 0; i < 31734; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31735(int& val) {
    func_in_31735(val);
    const int x = 31735;
    std::cout << 31735;
    for (int i = 0; i < 31735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31735(int& val) {
    const int x = 31735;
    std::cout << 31735;
    for (int i = 0; i < 31735; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31736(int& val) {
    func_in_31736(val);
    const int x = 31736;
    std::cout << 31736;
    for (int i = 0; i < 31736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31736(int& val) {
    const int x = 31736;
    std::cout << 31736;
    for (int i = 0; i < 31736; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31737(int& val) {
    func_in_31737(val);
    const int x = 31737;
    std::cout << 31737;
    for (int i = 0; i < 31737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31737(int& val) {
    const int x = 31737;
    std::cout << 31737;
    for (int i = 0; i < 31737; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31738(int& val) {
    func_in_31738(val);
    const int x = 31738;
    std::cout << 31738;
    for (int i = 0; i < 31738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31738(int& val) {
    const int x = 31738;
    std::cout << 31738;
    for (int i = 0; i < 31738; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31739(int& val) {
    func_in_31739(val);
    const int x = 31739;
    std::cout << 31739;
    for (int i = 0; i < 31739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31739(int& val) {
    const int x = 31739;
    std::cout << 31739;
    for (int i = 0; i < 31739; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31740(int& val) {
    func_in_31740(val);
    const int x = 31740;
    std::cout << 31740;
    for (int i = 0; i < 31740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31740(int& val) {
    const int x = 31740;
    std::cout << 31740;
    for (int i = 0; i < 31740; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31741(int& val) {
    func_in_31741(val);
    const int x = 31741;
    std::cout << 31741;
    for (int i = 0; i < 31741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31741(int& val) {
    const int x = 31741;
    std::cout << 31741;
    for (int i = 0; i < 31741; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31742(int& val) {
    func_in_31742(val);
    const int x = 31742;
    std::cout << 31742;
    for (int i = 0; i < 31742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31742(int& val) {
    const int x = 31742;
    std::cout << 31742;
    for (int i = 0; i < 31742; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31743(int& val) {
    func_in_31743(val);
    const int x = 31743;
    std::cout << 31743;
    for (int i = 0; i < 31743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31743(int& val) {
    const int x = 31743;
    std::cout << 31743;
    for (int i = 0; i < 31743; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31744(int& val) {
    func_in_31744(val);
    const int x = 31744;
    std::cout << 31744;
    for (int i = 0; i < 31744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31744(int& val) {
    const int x = 31744;
    std::cout << 31744;
    for (int i = 0; i < 31744; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31745(int& val) {
    func_in_31745(val);
    const int x = 31745;
    std::cout << 31745;
    for (int i = 0; i < 31745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31745(int& val) {
    const int x = 31745;
    std::cout << 31745;
    for (int i = 0; i < 31745; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31746(int& val) {
    func_in_31746(val);
    const int x = 31746;
    std::cout << 31746;
    for (int i = 0; i < 31746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31746(int& val) {
    const int x = 31746;
    std::cout << 31746;
    for (int i = 0; i < 31746; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31747(int& val) {
    func_in_31747(val);
    const int x = 31747;
    std::cout << 31747;
    for (int i = 0; i < 31747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31747(int& val) {
    const int x = 31747;
    std::cout << 31747;
    for (int i = 0; i < 31747; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31748(int& val) {
    func_in_31748(val);
    const int x = 31748;
    std::cout << 31748;
    for (int i = 0; i < 31748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31748(int& val) {
    const int x = 31748;
    std::cout << 31748;
    for (int i = 0; i < 31748; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31749(int& val) {
    func_in_31749(val);
    const int x = 31749;
    std::cout << 31749;
    for (int i = 0; i < 31749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31749(int& val) {
    const int x = 31749;
    std::cout << 31749;
    for (int i = 0; i < 31749; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31750(int& val) {
    func_in_31750(val);
    const int x = 31750;
    std::cout << 31750;
    for (int i = 0; i < 31750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31750(int& val) {
    const int x = 31750;
    std::cout << 31750;
    for (int i = 0; i < 31750; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31751(int& val) {
    func_in_31751(val);
    const int x = 31751;
    std::cout << 31751;
    for (int i = 0; i < 31751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31751(int& val) {
    const int x = 31751;
    std::cout << 31751;
    for (int i = 0; i < 31751; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31752(int& val) {
    func_in_31752(val);
    const int x = 31752;
    std::cout << 31752;
    for (int i = 0; i < 31752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31752(int& val) {
    const int x = 31752;
    std::cout << 31752;
    for (int i = 0; i < 31752; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31753(int& val) {
    func_in_31753(val);
    const int x = 31753;
    std::cout << 31753;
    for (int i = 0; i < 31753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31753(int& val) {
    const int x = 31753;
    std::cout << 31753;
    for (int i = 0; i < 31753; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31754(int& val) {
    func_in_31754(val);
    const int x = 31754;
    std::cout << 31754;
    for (int i = 0; i < 31754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31754(int& val) {
    const int x = 31754;
    std::cout << 31754;
    for (int i = 0; i < 31754; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31755(int& val) {
    func_in_31755(val);
    const int x = 31755;
    std::cout << 31755;
    for (int i = 0; i < 31755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31755(int& val) {
    const int x = 31755;
    std::cout << 31755;
    for (int i = 0; i < 31755; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31756(int& val) {
    func_in_31756(val);
    const int x = 31756;
    std::cout << 31756;
    for (int i = 0; i < 31756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31756(int& val) {
    const int x = 31756;
    std::cout << 31756;
    for (int i = 0; i < 31756; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31757(int& val) {
    func_in_31757(val);
    const int x = 31757;
    std::cout << 31757;
    for (int i = 0; i < 31757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31757(int& val) {
    const int x = 31757;
    std::cout << 31757;
    for (int i = 0; i < 31757; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31758(int& val) {
    func_in_31758(val);
    const int x = 31758;
    std::cout << 31758;
    for (int i = 0; i < 31758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31758(int& val) {
    const int x = 31758;
    std::cout << 31758;
    for (int i = 0; i < 31758; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31759(int& val) {
    func_in_31759(val);
    const int x = 31759;
    std::cout << 31759;
    for (int i = 0; i < 31759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31759(int& val) {
    const int x = 31759;
    std::cout << 31759;
    for (int i = 0; i < 31759; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31760(int& val) {
    func_in_31760(val);
    const int x = 31760;
    std::cout << 31760;
    for (int i = 0; i < 31760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31760(int& val) {
    const int x = 31760;
    std::cout << 31760;
    for (int i = 0; i < 31760; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31761(int& val) {
    func_in_31761(val);
    const int x = 31761;
    std::cout << 31761;
    for (int i = 0; i < 31761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31761(int& val) {
    const int x = 31761;
    std::cout << 31761;
    for (int i = 0; i < 31761; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31762(int& val) {
    func_in_31762(val);
    const int x = 31762;
    std::cout << 31762;
    for (int i = 0; i < 31762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31762(int& val) {
    const int x = 31762;
    std::cout << 31762;
    for (int i = 0; i < 31762; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31763(int& val) {
    func_in_31763(val);
    const int x = 31763;
    std::cout << 31763;
    for (int i = 0; i < 31763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31763(int& val) {
    const int x = 31763;
    std::cout << 31763;
    for (int i = 0; i < 31763; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31764(int& val) {
    func_in_31764(val);
    const int x = 31764;
    std::cout << 31764;
    for (int i = 0; i < 31764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31764(int& val) {
    const int x = 31764;
    std::cout << 31764;
    for (int i = 0; i < 31764; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31765(int& val) {
    func_in_31765(val);
    const int x = 31765;
    std::cout << 31765;
    for (int i = 0; i < 31765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31765(int& val) {
    const int x = 31765;
    std::cout << 31765;
    for (int i = 0; i < 31765; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31766(int& val) {
    func_in_31766(val);
    const int x = 31766;
    std::cout << 31766;
    for (int i = 0; i < 31766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31766(int& val) {
    const int x = 31766;
    std::cout << 31766;
    for (int i = 0; i < 31766; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31767(int& val) {
    func_in_31767(val);
    const int x = 31767;
    std::cout << 31767;
    for (int i = 0; i < 31767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31767(int& val) {
    const int x = 31767;
    std::cout << 31767;
    for (int i = 0; i < 31767; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31768(int& val) {
    func_in_31768(val);
    const int x = 31768;
    std::cout << 31768;
    for (int i = 0; i < 31768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31768(int& val) {
    const int x = 31768;
    std::cout << 31768;
    for (int i = 0; i < 31768; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31769(int& val) {
    func_in_31769(val);
    const int x = 31769;
    std::cout << 31769;
    for (int i = 0; i < 31769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31769(int& val) {
    const int x = 31769;
    std::cout << 31769;
    for (int i = 0; i < 31769; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31770(int& val) {
    func_in_31770(val);
    const int x = 31770;
    std::cout << 31770;
    for (int i = 0; i < 31770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31770(int& val) {
    const int x = 31770;
    std::cout << 31770;
    for (int i = 0; i < 31770; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31771(int& val) {
    func_in_31771(val);
    const int x = 31771;
    std::cout << 31771;
    for (int i = 0; i < 31771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31771(int& val) {
    const int x = 31771;
    std::cout << 31771;
    for (int i = 0; i < 31771; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31772(int& val) {
    func_in_31772(val);
    const int x = 31772;
    std::cout << 31772;
    for (int i = 0; i < 31772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31772(int& val) {
    const int x = 31772;
    std::cout << 31772;
    for (int i = 0; i < 31772; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31773(int& val) {
    func_in_31773(val);
    const int x = 31773;
    std::cout << 31773;
    for (int i = 0; i < 31773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31773(int& val) {
    const int x = 31773;
    std::cout << 31773;
    for (int i = 0; i < 31773; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31774(int& val) {
    func_in_31774(val);
    const int x = 31774;
    std::cout << 31774;
    for (int i = 0; i < 31774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31774(int& val) {
    const int x = 31774;
    std::cout << 31774;
    for (int i = 0; i < 31774; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31775(int& val) {
    func_in_31775(val);
    const int x = 31775;
    std::cout << 31775;
    for (int i = 0; i < 31775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31775(int& val) {
    const int x = 31775;
    std::cout << 31775;
    for (int i = 0; i < 31775; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31776(int& val) {
    func_in_31776(val);
    const int x = 31776;
    std::cout << 31776;
    for (int i = 0; i < 31776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31776(int& val) {
    const int x = 31776;
    std::cout << 31776;
    for (int i = 0; i < 31776; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31777(int& val) {
    func_in_31777(val);
    const int x = 31777;
    std::cout << 31777;
    for (int i = 0; i < 31777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31777(int& val) {
    const int x = 31777;
    std::cout << 31777;
    for (int i = 0; i < 31777; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31778(int& val) {
    func_in_31778(val);
    const int x = 31778;
    std::cout << 31778;
    for (int i = 0; i < 31778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31778(int& val) {
    const int x = 31778;
    std::cout << 31778;
    for (int i = 0; i < 31778; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31779(int& val) {
    func_in_31779(val);
    const int x = 31779;
    std::cout << 31779;
    for (int i = 0; i < 31779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31779(int& val) {
    const int x = 31779;
    std::cout << 31779;
    for (int i = 0; i < 31779; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31780(int& val) {
    func_in_31780(val);
    const int x = 31780;
    std::cout << 31780;
    for (int i = 0; i < 31780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31780(int& val) {
    const int x = 31780;
    std::cout << 31780;
    for (int i = 0; i < 31780; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31781(int& val) {
    func_in_31781(val);
    const int x = 31781;
    std::cout << 31781;
    for (int i = 0; i < 31781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31781(int& val) {
    const int x = 31781;
    std::cout << 31781;
    for (int i = 0; i < 31781; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31782(int& val) {
    func_in_31782(val);
    const int x = 31782;
    std::cout << 31782;
    for (int i = 0; i < 31782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31782(int& val) {
    const int x = 31782;
    std::cout << 31782;
    for (int i = 0; i < 31782; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31783(int& val) {
    func_in_31783(val);
    const int x = 31783;
    std::cout << 31783;
    for (int i = 0; i < 31783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31783(int& val) {
    const int x = 31783;
    std::cout << 31783;
    for (int i = 0; i < 31783; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31784(int& val) {
    func_in_31784(val);
    const int x = 31784;
    std::cout << 31784;
    for (int i = 0; i < 31784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31784(int& val) {
    const int x = 31784;
    std::cout << 31784;
    for (int i = 0; i < 31784; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31785(int& val) {
    func_in_31785(val);
    const int x = 31785;
    std::cout << 31785;
    for (int i = 0; i < 31785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31785(int& val) {
    const int x = 31785;
    std::cout << 31785;
    for (int i = 0; i < 31785; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31786(int& val) {
    func_in_31786(val);
    const int x = 31786;
    std::cout << 31786;
    for (int i = 0; i < 31786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31786(int& val) {
    const int x = 31786;
    std::cout << 31786;
    for (int i = 0; i < 31786; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31787(int& val) {
    func_in_31787(val);
    const int x = 31787;
    std::cout << 31787;
    for (int i = 0; i < 31787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31787(int& val) {
    const int x = 31787;
    std::cout << 31787;
    for (int i = 0; i < 31787; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31788(int& val) {
    func_in_31788(val);
    const int x = 31788;
    std::cout << 31788;
    for (int i = 0; i < 31788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31788(int& val) {
    const int x = 31788;
    std::cout << 31788;
    for (int i = 0; i < 31788; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31789(int& val) {
    func_in_31789(val);
    const int x = 31789;
    std::cout << 31789;
    for (int i = 0; i < 31789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31789(int& val) {
    const int x = 31789;
    std::cout << 31789;
    for (int i = 0; i < 31789; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31790(int& val) {
    func_in_31790(val);
    const int x = 31790;
    std::cout << 31790;
    for (int i = 0; i < 31790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31790(int& val) {
    const int x = 31790;
    std::cout << 31790;
    for (int i = 0; i < 31790; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31791(int& val) {
    func_in_31791(val);
    const int x = 31791;
    std::cout << 31791;
    for (int i = 0; i < 31791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31791(int& val) {
    const int x = 31791;
    std::cout << 31791;
    for (int i = 0; i < 31791; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31792(int& val) {
    func_in_31792(val);
    const int x = 31792;
    std::cout << 31792;
    for (int i = 0; i < 31792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31792(int& val) {
    const int x = 31792;
    std::cout << 31792;
    for (int i = 0; i < 31792; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31793(int& val) {
    func_in_31793(val);
    const int x = 31793;
    std::cout << 31793;
    for (int i = 0; i < 31793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31793(int& val) {
    const int x = 31793;
    std::cout << 31793;
    for (int i = 0; i < 31793; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31794(int& val) {
    func_in_31794(val);
    const int x = 31794;
    std::cout << 31794;
    for (int i = 0; i < 31794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31794(int& val) {
    const int x = 31794;
    std::cout << 31794;
    for (int i = 0; i < 31794; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31795(int& val) {
    func_in_31795(val);
    const int x = 31795;
    std::cout << 31795;
    for (int i = 0; i < 31795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31795(int& val) {
    const int x = 31795;
    std::cout << 31795;
    for (int i = 0; i < 31795; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31796(int& val) {
    func_in_31796(val);
    const int x = 31796;
    std::cout << 31796;
    for (int i = 0; i < 31796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31796(int& val) {
    const int x = 31796;
    std::cout << 31796;
    for (int i = 0; i < 31796; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31797(int& val) {
    func_in_31797(val);
    const int x = 31797;
    std::cout << 31797;
    for (int i = 0; i < 31797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31797(int& val) {
    const int x = 31797;
    std::cout << 31797;
    for (int i = 0; i < 31797; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31798(int& val) {
    func_in_31798(val);
    const int x = 31798;
    std::cout << 31798;
    for (int i = 0; i < 31798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31798(int& val) {
    const int x = 31798;
    std::cout << 31798;
    for (int i = 0; i < 31798; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31799(int& val) {
    func_in_31799(val);
    const int x = 31799;
    std::cout << 31799;
    for (int i = 0; i < 31799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31799(int& val) {
    const int x = 31799;
    std::cout << 31799;
    for (int i = 0; i < 31799; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31800(int& val) {
    func_in_31800(val);
    const int x = 31800;
    std::cout << 31800;
    for (int i = 0; i < 31800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31800(int& val) {
    const int x = 31800;
    std::cout << 31800;
    for (int i = 0; i < 31800; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31801(int& val) {
    func_in_31801(val);
    const int x = 31801;
    std::cout << 31801;
    for (int i = 0; i < 31801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31801(int& val) {
    const int x = 31801;
    std::cout << 31801;
    for (int i = 0; i < 31801; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31802(int& val) {
    func_in_31802(val);
    const int x = 31802;
    std::cout << 31802;
    for (int i = 0; i < 31802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31802(int& val) {
    const int x = 31802;
    std::cout << 31802;
    for (int i = 0; i < 31802; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31803(int& val) {
    func_in_31803(val);
    const int x = 31803;
    std::cout << 31803;
    for (int i = 0; i < 31803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31803(int& val) {
    const int x = 31803;
    std::cout << 31803;
    for (int i = 0; i < 31803; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31804(int& val) {
    func_in_31804(val);
    const int x = 31804;
    std::cout << 31804;
    for (int i = 0; i < 31804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31804(int& val) {
    const int x = 31804;
    std::cout << 31804;
    for (int i = 0; i < 31804; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31805(int& val) {
    func_in_31805(val);
    const int x = 31805;
    std::cout << 31805;
    for (int i = 0; i < 31805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31805(int& val) {
    const int x = 31805;
    std::cout << 31805;
    for (int i = 0; i < 31805; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31806(int& val) {
    func_in_31806(val);
    const int x = 31806;
    std::cout << 31806;
    for (int i = 0; i < 31806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31806(int& val) {
    const int x = 31806;
    std::cout << 31806;
    for (int i = 0; i < 31806; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31807(int& val) {
    func_in_31807(val);
    const int x = 31807;
    std::cout << 31807;
    for (int i = 0; i < 31807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31807(int& val) {
    const int x = 31807;
    std::cout << 31807;
    for (int i = 0; i < 31807; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31808(int& val) {
    func_in_31808(val);
    const int x = 31808;
    std::cout << 31808;
    for (int i = 0; i < 31808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31808(int& val) {
    const int x = 31808;
    std::cout << 31808;
    for (int i = 0; i < 31808; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31809(int& val) {
    func_in_31809(val);
    const int x = 31809;
    std::cout << 31809;
    for (int i = 0; i < 31809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31809(int& val) {
    const int x = 31809;
    std::cout << 31809;
    for (int i = 0; i < 31809; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31810(int& val) {
    func_in_31810(val);
    const int x = 31810;
    std::cout << 31810;
    for (int i = 0; i < 31810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31810(int& val) {
    const int x = 31810;
    std::cout << 31810;
    for (int i = 0; i < 31810; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31811(int& val) {
    func_in_31811(val);
    const int x = 31811;
    std::cout << 31811;
    for (int i = 0; i < 31811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31811(int& val) {
    const int x = 31811;
    std::cout << 31811;
    for (int i = 0; i < 31811; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31812(int& val) {
    func_in_31812(val);
    const int x = 31812;
    std::cout << 31812;
    for (int i = 0; i < 31812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31812(int& val) {
    const int x = 31812;
    std::cout << 31812;
    for (int i = 0; i < 31812; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31813(int& val) {
    func_in_31813(val);
    const int x = 31813;
    std::cout << 31813;
    for (int i = 0; i < 31813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31813(int& val) {
    const int x = 31813;
    std::cout << 31813;
    for (int i = 0; i < 31813; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31814(int& val) {
    func_in_31814(val);
    const int x = 31814;
    std::cout << 31814;
    for (int i = 0; i < 31814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31814(int& val) {
    const int x = 31814;
    std::cout << 31814;
    for (int i = 0; i < 31814; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31815(int& val) {
    func_in_31815(val);
    const int x = 31815;
    std::cout << 31815;
    for (int i = 0; i < 31815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31815(int& val) {
    const int x = 31815;
    std::cout << 31815;
    for (int i = 0; i < 31815; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31816(int& val) {
    func_in_31816(val);
    const int x = 31816;
    std::cout << 31816;
    for (int i = 0; i < 31816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31816(int& val) {
    const int x = 31816;
    std::cout << 31816;
    for (int i = 0; i < 31816; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31817(int& val) {
    func_in_31817(val);
    const int x = 31817;
    std::cout << 31817;
    for (int i = 0; i < 31817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31817(int& val) {
    const int x = 31817;
    std::cout << 31817;
    for (int i = 0; i < 31817; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31818(int& val) {
    func_in_31818(val);
    const int x = 31818;
    std::cout << 31818;
    for (int i = 0; i < 31818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31818(int& val) {
    const int x = 31818;
    std::cout << 31818;
    for (int i = 0; i < 31818; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31819(int& val) {
    func_in_31819(val);
    const int x = 31819;
    std::cout << 31819;
    for (int i = 0; i < 31819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31819(int& val) {
    const int x = 31819;
    std::cout << 31819;
    for (int i = 0; i < 31819; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31820(int& val) {
    func_in_31820(val);
    const int x = 31820;
    std::cout << 31820;
    for (int i = 0; i < 31820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31820(int& val) {
    const int x = 31820;
    std::cout << 31820;
    for (int i = 0; i < 31820; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31821(int& val) {
    func_in_31821(val);
    const int x = 31821;
    std::cout << 31821;
    for (int i = 0; i < 31821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31821(int& val) {
    const int x = 31821;
    std::cout << 31821;
    for (int i = 0; i < 31821; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31822(int& val) {
    func_in_31822(val);
    const int x = 31822;
    std::cout << 31822;
    for (int i = 0; i < 31822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31822(int& val) {
    const int x = 31822;
    std::cout << 31822;
    for (int i = 0; i < 31822; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31823(int& val) {
    func_in_31823(val);
    const int x = 31823;
    std::cout << 31823;
    for (int i = 0; i < 31823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31823(int& val) {
    const int x = 31823;
    std::cout << 31823;
    for (int i = 0; i < 31823; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31824(int& val) {
    func_in_31824(val);
    const int x = 31824;
    std::cout << 31824;
    for (int i = 0; i < 31824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31824(int& val) {
    const int x = 31824;
    std::cout << 31824;
    for (int i = 0; i < 31824; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31825(int& val) {
    func_in_31825(val);
    const int x = 31825;
    std::cout << 31825;
    for (int i = 0; i < 31825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31825(int& val) {
    const int x = 31825;
    std::cout << 31825;
    for (int i = 0; i < 31825; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31826(int& val) {
    func_in_31826(val);
    const int x = 31826;
    std::cout << 31826;
    for (int i = 0; i < 31826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31826(int& val) {
    const int x = 31826;
    std::cout << 31826;
    for (int i = 0; i < 31826; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31827(int& val) {
    func_in_31827(val);
    const int x = 31827;
    std::cout << 31827;
    for (int i = 0; i < 31827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31827(int& val) {
    const int x = 31827;
    std::cout << 31827;
    for (int i = 0; i < 31827; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31828(int& val) {
    func_in_31828(val);
    const int x = 31828;
    std::cout << 31828;
    for (int i = 0; i < 31828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31828(int& val) {
    const int x = 31828;
    std::cout << 31828;
    for (int i = 0; i < 31828; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31829(int& val) {
    func_in_31829(val);
    const int x = 31829;
    std::cout << 31829;
    for (int i = 0; i < 31829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31829(int& val) {
    const int x = 31829;
    std::cout << 31829;
    for (int i = 0; i < 31829; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31830(int& val) {
    func_in_31830(val);
    const int x = 31830;
    std::cout << 31830;
    for (int i = 0; i < 31830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31830(int& val) {
    const int x = 31830;
    std::cout << 31830;
    for (int i = 0; i < 31830; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31831(int& val) {
    func_in_31831(val);
    const int x = 31831;
    std::cout << 31831;
    for (int i = 0; i < 31831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31831(int& val) {
    const int x = 31831;
    std::cout << 31831;
    for (int i = 0; i < 31831; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31832(int& val) {
    func_in_31832(val);
    const int x = 31832;
    std::cout << 31832;
    for (int i = 0; i < 31832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31832(int& val) {
    const int x = 31832;
    std::cout << 31832;
    for (int i = 0; i < 31832; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31833(int& val) {
    func_in_31833(val);
    const int x = 31833;
    std::cout << 31833;
    for (int i = 0; i < 31833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31833(int& val) {
    const int x = 31833;
    std::cout << 31833;
    for (int i = 0; i < 31833; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31834(int& val) {
    func_in_31834(val);
    const int x = 31834;
    std::cout << 31834;
    for (int i = 0; i < 31834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31834(int& val) {
    const int x = 31834;
    std::cout << 31834;
    for (int i = 0; i < 31834; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31835(int& val) {
    func_in_31835(val);
    const int x = 31835;
    std::cout << 31835;
    for (int i = 0; i < 31835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31835(int& val) {
    const int x = 31835;
    std::cout << 31835;
    for (int i = 0; i < 31835; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31836(int& val) {
    func_in_31836(val);
    const int x = 31836;
    std::cout << 31836;
    for (int i = 0; i < 31836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31836(int& val) {
    const int x = 31836;
    std::cout << 31836;
    for (int i = 0; i < 31836; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31837(int& val) {
    func_in_31837(val);
    const int x = 31837;
    std::cout << 31837;
    for (int i = 0; i < 31837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31837(int& val) {
    const int x = 31837;
    std::cout << 31837;
    for (int i = 0; i < 31837; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31838(int& val) {
    func_in_31838(val);
    const int x = 31838;
    std::cout << 31838;
    for (int i = 0; i < 31838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31838(int& val) {
    const int x = 31838;
    std::cout << 31838;
    for (int i = 0; i < 31838; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31839(int& val) {
    func_in_31839(val);
    const int x = 31839;
    std::cout << 31839;
    for (int i = 0; i < 31839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31839(int& val) {
    const int x = 31839;
    std::cout << 31839;
    for (int i = 0; i < 31839; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31840(int& val) {
    func_in_31840(val);
    const int x = 31840;
    std::cout << 31840;
    for (int i = 0; i < 31840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31840(int& val) {
    const int x = 31840;
    std::cout << 31840;
    for (int i = 0; i < 31840; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31841(int& val) {
    func_in_31841(val);
    const int x = 31841;
    std::cout << 31841;
    for (int i = 0; i < 31841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31841(int& val) {
    const int x = 31841;
    std::cout << 31841;
    for (int i = 0; i < 31841; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31842(int& val) {
    func_in_31842(val);
    const int x = 31842;
    std::cout << 31842;
    for (int i = 0; i < 31842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31842(int& val) {
    const int x = 31842;
    std::cout << 31842;
    for (int i = 0; i < 31842; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31843(int& val) {
    func_in_31843(val);
    const int x = 31843;
    std::cout << 31843;
    for (int i = 0; i < 31843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31843(int& val) {
    const int x = 31843;
    std::cout << 31843;
    for (int i = 0; i < 31843; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31844(int& val) {
    func_in_31844(val);
    const int x = 31844;
    std::cout << 31844;
    for (int i = 0; i < 31844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31844(int& val) {
    const int x = 31844;
    std::cout << 31844;
    for (int i = 0; i < 31844; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31845(int& val) {
    func_in_31845(val);
    const int x = 31845;
    std::cout << 31845;
    for (int i = 0; i < 31845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31845(int& val) {
    const int x = 31845;
    std::cout << 31845;
    for (int i = 0; i < 31845; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31846(int& val) {
    func_in_31846(val);
    const int x = 31846;
    std::cout << 31846;
    for (int i = 0; i < 31846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31846(int& val) {
    const int x = 31846;
    std::cout << 31846;
    for (int i = 0; i < 31846; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31847(int& val) {
    func_in_31847(val);
    const int x = 31847;
    std::cout << 31847;
    for (int i = 0; i < 31847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31847(int& val) {
    const int x = 31847;
    std::cout << 31847;
    for (int i = 0; i < 31847; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31848(int& val) {
    func_in_31848(val);
    const int x = 31848;
    std::cout << 31848;
    for (int i = 0; i < 31848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31848(int& val) {
    const int x = 31848;
    std::cout << 31848;
    for (int i = 0; i < 31848; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31849(int& val) {
    func_in_31849(val);
    const int x = 31849;
    std::cout << 31849;
    for (int i = 0; i < 31849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31849(int& val) {
    const int x = 31849;
    std::cout << 31849;
    for (int i = 0; i < 31849; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31850(int& val) {
    func_in_31850(val);
    const int x = 31850;
    std::cout << 31850;
    for (int i = 0; i < 31850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31850(int& val) {
    const int x = 31850;
    std::cout << 31850;
    for (int i = 0; i < 31850; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31851(int& val) {
    func_in_31851(val);
    const int x = 31851;
    std::cout << 31851;
    for (int i = 0; i < 31851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31851(int& val) {
    const int x = 31851;
    std::cout << 31851;
    for (int i = 0; i < 31851; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31852(int& val) {
    func_in_31852(val);
    const int x = 31852;
    std::cout << 31852;
    for (int i = 0; i < 31852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31852(int& val) {
    const int x = 31852;
    std::cout << 31852;
    for (int i = 0; i < 31852; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31853(int& val) {
    func_in_31853(val);
    const int x = 31853;
    std::cout << 31853;
    for (int i = 0; i < 31853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31853(int& val) {
    const int x = 31853;
    std::cout << 31853;
    for (int i = 0; i < 31853; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31854(int& val) {
    func_in_31854(val);
    const int x = 31854;
    std::cout << 31854;
    for (int i = 0; i < 31854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31854(int& val) {
    const int x = 31854;
    std::cout << 31854;
    for (int i = 0; i < 31854; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31855(int& val) {
    func_in_31855(val);
    const int x = 31855;
    std::cout << 31855;
    for (int i = 0; i < 31855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31855(int& val) {
    const int x = 31855;
    std::cout << 31855;
    for (int i = 0; i < 31855; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31856(int& val) {
    func_in_31856(val);
    const int x = 31856;
    std::cout << 31856;
    for (int i = 0; i < 31856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31856(int& val) {
    const int x = 31856;
    std::cout << 31856;
    for (int i = 0; i < 31856; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31857(int& val) {
    func_in_31857(val);
    const int x = 31857;
    std::cout << 31857;
    for (int i = 0; i < 31857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31857(int& val) {
    const int x = 31857;
    std::cout << 31857;
    for (int i = 0; i < 31857; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31858(int& val) {
    func_in_31858(val);
    const int x = 31858;
    std::cout << 31858;
    for (int i = 0; i < 31858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31858(int& val) {
    const int x = 31858;
    std::cout << 31858;
    for (int i = 0; i < 31858; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31859(int& val) {
    func_in_31859(val);
    const int x = 31859;
    std::cout << 31859;
    for (int i = 0; i < 31859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31859(int& val) {
    const int x = 31859;
    std::cout << 31859;
    for (int i = 0; i < 31859; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31860(int& val) {
    func_in_31860(val);
    const int x = 31860;
    std::cout << 31860;
    for (int i = 0; i < 31860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31860(int& val) {
    const int x = 31860;
    std::cout << 31860;
    for (int i = 0; i < 31860; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31861(int& val) {
    func_in_31861(val);
    const int x = 31861;
    std::cout << 31861;
    for (int i = 0; i < 31861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31861(int& val) {
    const int x = 31861;
    std::cout << 31861;
    for (int i = 0; i < 31861; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31862(int& val) {
    func_in_31862(val);
    const int x = 31862;
    std::cout << 31862;
    for (int i = 0; i < 31862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31862(int& val) {
    const int x = 31862;
    std::cout << 31862;
    for (int i = 0; i < 31862; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31863(int& val) {
    func_in_31863(val);
    const int x = 31863;
    std::cout << 31863;
    for (int i = 0; i < 31863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31863(int& val) {
    const int x = 31863;
    std::cout << 31863;
    for (int i = 0; i < 31863; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31864(int& val) {
    func_in_31864(val);
    const int x = 31864;
    std::cout << 31864;
    for (int i = 0; i < 31864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31864(int& val) {
    const int x = 31864;
    std::cout << 31864;
    for (int i = 0; i < 31864; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31865(int& val) {
    func_in_31865(val);
    const int x = 31865;
    std::cout << 31865;
    for (int i = 0; i < 31865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31865(int& val) {
    const int x = 31865;
    std::cout << 31865;
    for (int i = 0; i < 31865; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31866(int& val) {
    func_in_31866(val);
    const int x = 31866;
    std::cout << 31866;
    for (int i = 0; i < 31866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31866(int& val) {
    const int x = 31866;
    std::cout << 31866;
    for (int i = 0; i < 31866; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31867(int& val) {
    func_in_31867(val);
    const int x = 31867;
    std::cout << 31867;
    for (int i = 0; i < 31867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31867(int& val) {
    const int x = 31867;
    std::cout << 31867;
    for (int i = 0; i < 31867; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31868(int& val) {
    func_in_31868(val);
    const int x = 31868;
    std::cout << 31868;
    for (int i = 0; i < 31868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31868(int& val) {
    const int x = 31868;
    std::cout << 31868;
    for (int i = 0; i < 31868; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31869(int& val) {
    func_in_31869(val);
    const int x = 31869;
    std::cout << 31869;
    for (int i = 0; i < 31869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31869(int& val) {
    const int x = 31869;
    std::cout << 31869;
    for (int i = 0; i < 31869; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31870(int& val) {
    func_in_31870(val);
    const int x = 31870;
    std::cout << 31870;
    for (int i = 0; i < 31870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31870(int& val) {
    const int x = 31870;
    std::cout << 31870;
    for (int i = 0; i < 31870; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31871(int& val) {
    func_in_31871(val);
    const int x = 31871;
    std::cout << 31871;
    for (int i = 0; i < 31871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31871(int& val) {
    const int x = 31871;
    std::cout << 31871;
    for (int i = 0; i < 31871; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31872(int& val) {
    func_in_31872(val);
    const int x = 31872;
    std::cout << 31872;
    for (int i = 0; i < 31872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31872(int& val) {
    const int x = 31872;
    std::cout << 31872;
    for (int i = 0; i < 31872; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31873(int& val) {
    func_in_31873(val);
    const int x = 31873;
    std::cout << 31873;
    for (int i = 0; i < 31873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31873(int& val) {
    const int x = 31873;
    std::cout << 31873;
    for (int i = 0; i < 31873; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31874(int& val) {
    func_in_31874(val);
    const int x = 31874;
    std::cout << 31874;
    for (int i = 0; i < 31874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31874(int& val) {
    const int x = 31874;
    std::cout << 31874;
    for (int i = 0; i < 31874; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31875(int& val) {
    func_in_31875(val);
    const int x = 31875;
    std::cout << 31875;
    for (int i = 0; i < 31875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31875(int& val) {
    const int x = 31875;
    std::cout << 31875;
    for (int i = 0; i < 31875; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31876(int& val) {
    func_in_31876(val);
    const int x = 31876;
    std::cout << 31876;
    for (int i = 0; i < 31876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31876(int& val) {
    const int x = 31876;
    std::cout << 31876;
    for (int i = 0; i < 31876; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31877(int& val) {
    func_in_31877(val);
    const int x = 31877;
    std::cout << 31877;
    for (int i = 0; i < 31877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31877(int& val) {
    const int x = 31877;
    std::cout << 31877;
    for (int i = 0; i < 31877; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31878(int& val) {
    func_in_31878(val);
    const int x = 31878;
    std::cout << 31878;
    for (int i = 0; i < 31878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31878(int& val) {
    const int x = 31878;
    std::cout << 31878;
    for (int i = 0; i < 31878; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31879(int& val) {
    func_in_31879(val);
    const int x = 31879;
    std::cout << 31879;
    for (int i = 0; i < 31879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31879(int& val) {
    const int x = 31879;
    std::cout << 31879;
    for (int i = 0; i < 31879; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31880(int& val) {
    func_in_31880(val);
    const int x = 31880;
    std::cout << 31880;
    for (int i = 0; i < 31880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31880(int& val) {
    const int x = 31880;
    std::cout << 31880;
    for (int i = 0; i < 31880; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31881(int& val) {
    func_in_31881(val);
    const int x = 31881;
    std::cout << 31881;
    for (int i = 0; i < 31881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31881(int& val) {
    const int x = 31881;
    std::cout << 31881;
    for (int i = 0; i < 31881; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31882(int& val) {
    func_in_31882(val);
    const int x = 31882;
    std::cout << 31882;
    for (int i = 0; i < 31882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31882(int& val) {
    const int x = 31882;
    std::cout << 31882;
    for (int i = 0; i < 31882; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31883(int& val) {
    func_in_31883(val);
    const int x = 31883;
    std::cout << 31883;
    for (int i = 0; i < 31883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31883(int& val) {
    const int x = 31883;
    std::cout << 31883;
    for (int i = 0; i < 31883; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31884(int& val) {
    func_in_31884(val);
    const int x = 31884;
    std::cout << 31884;
    for (int i = 0; i < 31884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31884(int& val) {
    const int x = 31884;
    std::cout << 31884;
    for (int i = 0; i < 31884; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31885(int& val) {
    func_in_31885(val);
    const int x = 31885;
    std::cout << 31885;
    for (int i = 0; i < 31885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31885(int& val) {
    const int x = 31885;
    std::cout << 31885;
    for (int i = 0; i < 31885; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31886(int& val) {
    func_in_31886(val);
    const int x = 31886;
    std::cout << 31886;
    for (int i = 0; i < 31886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31886(int& val) {
    const int x = 31886;
    std::cout << 31886;
    for (int i = 0; i < 31886; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31887(int& val) {
    func_in_31887(val);
    const int x = 31887;
    std::cout << 31887;
    for (int i = 0; i < 31887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31887(int& val) {
    const int x = 31887;
    std::cout << 31887;
    for (int i = 0; i < 31887; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31888(int& val) {
    func_in_31888(val);
    const int x = 31888;
    std::cout << 31888;
    for (int i = 0; i < 31888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31888(int& val) {
    const int x = 31888;
    std::cout << 31888;
    for (int i = 0; i < 31888; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31889(int& val) {
    func_in_31889(val);
    const int x = 31889;
    std::cout << 31889;
    for (int i = 0; i < 31889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31889(int& val) {
    const int x = 31889;
    std::cout << 31889;
    for (int i = 0; i < 31889; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31890(int& val) {
    func_in_31890(val);
    const int x = 31890;
    std::cout << 31890;
    for (int i = 0; i < 31890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31890(int& val) {
    const int x = 31890;
    std::cout << 31890;
    for (int i = 0; i < 31890; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31891(int& val) {
    func_in_31891(val);
    const int x = 31891;
    std::cout << 31891;
    for (int i = 0; i < 31891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31891(int& val) {
    const int x = 31891;
    std::cout << 31891;
    for (int i = 0; i < 31891; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31892(int& val) {
    func_in_31892(val);
    const int x = 31892;
    std::cout << 31892;
    for (int i = 0; i < 31892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31892(int& val) {
    const int x = 31892;
    std::cout << 31892;
    for (int i = 0; i < 31892; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31893(int& val) {
    func_in_31893(val);
    const int x = 31893;
    std::cout << 31893;
    for (int i = 0; i < 31893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31893(int& val) {
    const int x = 31893;
    std::cout << 31893;
    for (int i = 0; i < 31893; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31894(int& val) {
    func_in_31894(val);
    const int x = 31894;
    std::cout << 31894;
    for (int i = 0; i < 31894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31894(int& val) {
    const int x = 31894;
    std::cout << 31894;
    for (int i = 0; i < 31894; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31895(int& val) {
    func_in_31895(val);
    const int x = 31895;
    std::cout << 31895;
    for (int i = 0; i < 31895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31895(int& val) {
    const int x = 31895;
    std::cout << 31895;
    for (int i = 0; i < 31895; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31896(int& val) {
    func_in_31896(val);
    const int x = 31896;
    std::cout << 31896;
    for (int i = 0; i < 31896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31896(int& val) {
    const int x = 31896;
    std::cout << 31896;
    for (int i = 0; i < 31896; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31897(int& val) {
    func_in_31897(val);
    const int x = 31897;
    std::cout << 31897;
    for (int i = 0; i < 31897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31897(int& val) {
    const int x = 31897;
    std::cout << 31897;
    for (int i = 0; i < 31897; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31898(int& val) {
    func_in_31898(val);
    const int x = 31898;
    std::cout << 31898;
    for (int i = 0; i < 31898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31898(int& val) {
    const int x = 31898;
    std::cout << 31898;
    for (int i = 0; i < 31898; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31899(int& val) {
    func_in_31899(val);
    const int x = 31899;
    std::cout << 31899;
    for (int i = 0; i < 31899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31899(int& val) {
    const int x = 31899;
    std::cout << 31899;
    for (int i = 0; i < 31899; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31900(int& val) {
    func_in_31900(val);
    const int x = 31900;
    std::cout << 31900;
    for (int i = 0; i < 31900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31900(int& val) {
    const int x = 31900;
    std::cout << 31900;
    for (int i = 0; i < 31900; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31901(int& val) {
    func_in_31901(val);
    const int x = 31901;
    std::cout << 31901;
    for (int i = 0; i < 31901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31901(int& val) {
    const int x = 31901;
    std::cout << 31901;
    for (int i = 0; i < 31901; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31902(int& val) {
    func_in_31902(val);
    const int x = 31902;
    std::cout << 31902;
    for (int i = 0; i < 31902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31902(int& val) {
    const int x = 31902;
    std::cout << 31902;
    for (int i = 0; i < 31902; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31903(int& val) {
    func_in_31903(val);
    const int x = 31903;
    std::cout << 31903;
    for (int i = 0; i < 31903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31903(int& val) {
    const int x = 31903;
    std::cout << 31903;
    for (int i = 0; i < 31903; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31904(int& val) {
    func_in_31904(val);
    const int x = 31904;
    std::cout << 31904;
    for (int i = 0; i < 31904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31904(int& val) {
    const int x = 31904;
    std::cout << 31904;
    for (int i = 0; i < 31904; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31905(int& val) {
    func_in_31905(val);
    const int x = 31905;
    std::cout << 31905;
    for (int i = 0; i < 31905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31905(int& val) {
    const int x = 31905;
    std::cout << 31905;
    for (int i = 0; i < 31905; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31906(int& val) {
    func_in_31906(val);
    const int x = 31906;
    std::cout << 31906;
    for (int i = 0; i < 31906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31906(int& val) {
    const int x = 31906;
    std::cout << 31906;
    for (int i = 0; i < 31906; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31907(int& val) {
    func_in_31907(val);
    const int x = 31907;
    std::cout << 31907;
    for (int i = 0; i < 31907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31907(int& val) {
    const int x = 31907;
    std::cout << 31907;
    for (int i = 0; i < 31907; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31908(int& val) {
    func_in_31908(val);
    const int x = 31908;
    std::cout << 31908;
    for (int i = 0; i < 31908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31908(int& val) {
    const int x = 31908;
    std::cout << 31908;
    for (int i = 0; i < 31908; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31909(int& val) {
    func_in_31909(val);
    const int x = 31909;
    std::cout << 31909;
    for (int i = 0; i < 31909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31909(int& val) {
    const int x = 31909;
    std::cout << 31909;
    for (int i = 0; i < 31909; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31910(int& val) {
    func_in_31910(val);
    const int x = 31910;
    std::cout << 31910;
    for (int i = 0; i < 31910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31910(int& val) {
    const int x = 31910;
    std::cout << 31910;
    for (int i = 0; i < 31910; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31911(int& val) {
    func_in_31911(val);
    const int x = 31911;
    std::cout << 31911;
    for (int i = 0; i < 31911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31911(int& val) {
    const int x = 31911;
    std::cout << 31911;
    for (int i = 0; i < 31911; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31912(int& val) {
    func_in_31912(val);
    const int x = 31912;
    std::cout << 31912;
    for (int i = 0; i < 31912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31912(int& val) {
    const int x = 31912;
    std::cout << 31912;
    for (int i = 0; i < 31912; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31913(int& val) {
    func_in_31913(val);
    const int x = 31913;
    std::cout << 31913;
    for (int i = 0; i < 31913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31913(int& val) {
    const int x = 31913;
    std::cout << 31913;
    for (int i = 0; i < 31913; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31914(int& val) {
    func_in_31914(val);
    const int x = 31914;
    std::cout << 31914;
    for (int i = 0; i < 31914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31914(int& val) {
    const int x = 31914;
    std::cout << 31914;
    for (int i = 0; i < 31914; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31915(int& val) {
    func_in_31915(val);
    const int x = 31915;
    std::cout << 31915;
    for (int i = 0; i < 31915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31915(int& val) {
    const int x = 31915;
    std::cout << 31915;
    for (int i = 0; i < 31915; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31916(int& val) {
    func_in_31916(val);
    const int x = 31916;
    std::cout << 31916;
    for (int i = 0; i < 31916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31916(int& val) {
    const int x = 31916;
    std::cout << 31916;
    for (int i = 0; i < 31916; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31917(int& val) {
    func_in_31917(val);
    const int x = 31917;
    std::cout << 31917;
    for (int i = 0; i < 31917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31917(int& val) {
    const int x = 31917;
    std::cout << 31917;
    for (int i = 0; i < 31917; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31918(int& val) {
    func_in_31918(val);
    const int x = 31918;
    std::cout << 31918;
    for (int i = 0; i < 31918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31918(int& val) {
    const int x = 31918;
    std::cout << 31918;
    for (int i = 0; i < 31918; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31919(int& val) {
    func_in_31919(val);
    const int x = 31919;
    std::cout << 31919;
    for (int i = 0; i < 31919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31919(int& val) {
    const int x = 31919;
    std::cout << 31919;
    for (int i = 0; i < 31919; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31920(int& val) {
    func_in_31920(val);
    const int x = 31920;
    std::cout << 31920;
    for (int i = 0; i < 31920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31920(int& val) {
    const int x = 31920;
    std::cout << 31920;
    for (int i = 0; i < 31920; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31921(int& val) {
    func_in_31921(val);
    const int x = 31921;
    std::cout << 31921;
    for (int i = 0; i < 31921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31921(int& val) {
    const int x = 31921;
    std::cout << 31921;
    for (int i = 0; i < 31921; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31922(int& val) {
    func_in_31922(val);
    const int x = 31922;
    std::cout << 31922;
    for (int i = 0; i < 31922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31922(int& val) {
    const int x = 31922;
    std::cout << 31922;
    for (int i = 0; i < 31922; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31923(int& val) {
    func_in_31923(val);
    const int x = 31923;
    std::cout << 31923;
    for (int i = 0; i < 31923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31923(int& val) {
    const int x = 31923;
    std::cout << 31923;
    for (int i = 0; i < 31923; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31924(int& val) {
    func_in_31924(val);
    const int x = 31924;
    std::cout << 31924;
    for (int i = 0; i < 31924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31924(int& val) {
    const int x = 31924;
    std::cout << 31924;
    for (int i = 0; i < 31924; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31925(int& val) {
    func_in_31925(val);
    const int x = 31925;
    std::cout << 31925;
    for (int i = 0; i < 31925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31925(int& val) {
    const int x = 31925;
    std::cout << 31925;
    for (int i = 0; i < 31925; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31926(int& val) {
    func_in_31926(val);
    const int x = 31926;
    std::cout << 31926;
    for (int i = 0; i < 31926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31926(int& val) {
    const int x = 31926;
    std::cout << 31926;
    for (int i = 0; i < 31926; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31927(int& val) {
    func_in_31927(val);
    const int x = 31927;
    std::cout << 31927;
    for (int i = 0; i < 31927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31927(int& val) {
    const int x = 31927;
    std::cout << 31927;
    for (int i = 0; i < 31927; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31928(int& val) {
    func_in_31928(val);
    const int x = 31928;
    std::cout << 31928;
    for (int i = 0; i < 31928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31928(int& val) {
    const int x = 31928;
    std::cout << 31928;
    for (int i = 0; i < 31928; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31929(int& val) {
    func_in_31929(val);
    const int x = 31929;
    std::cout << 31929;
    for (int i = 0; i < 31929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31929(int& val) {
    const int x = 31929;
    std::cout << 31929;
    for (int i = 0; i < 31929; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31930(int& val) {
    func_in_31930(val);
    const int x = 31930;
    std::cout << 31930;
    for (int i = 0; i < 31930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31930(int& val) {
    const int x = 31930;
    std::cout << 31930;
    for (int i = 0; i < 31930; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31931(int& val) {
    func_in_31931(val);
    const int x = 31931;
    std::cout << 31931;
    for (int i = 0; i < 31931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31931(int& val) {
    const int x = 31931;
    std::cout << 31931;
    for (int i = 0; i < 31931; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31932(int& val) {
    func_in_31932(val);
    const int x = 31932;
    std::cout << 31932;
    for (int i = 0; i < 31932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31932(int& val) {
    const int x = 31932;
    std::cout << 31932;
    for (int i = 0; i < 31932; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31933(int& val) {
    func_in_31933(val);
    const int x = 31933;
    std::cout << 31933;
    for (int i = 0; i < 31933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31933(int& val) {
    const int x = 31933;
    std::cout << 31933;
    for (int i = 0; i < 31933; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31934(int& val) {
    func_in_31934(val);
    const int x = 31934;
    std::cout << 31934;
    for (int i = 0; i < 31934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31934(int& val) {
    const int x = 31934;
    std::cout << 31934;
    for (int i = 0; i < 31934; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31935(int& val) {
    func_in_31935(val);
    const int x = 31935;
    std::cout << 31935;
    for (int i = 0; i < 31935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31935(int& val) {
    const int x = 31935;
    std::cout << 31935;
    for (int i = 0; i < 31935; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31936(int& val) {
    func_in_31936(val);
    const int x = 31936;
    std::cout << 31936;
    for (int i = 0; i < 31936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31936(int& val) {
    const int x = 31936;
    std::cout << 31936;
    for (int i = 0; i < 31936; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31937(int& val) {
    func_in_31937(val);
    const int x = 31937;
    std::cout << 31937;
    for (int i = 0; i < 31937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31937(int& val) {
    const int x = 31937;
    std::cout << 31937;
    for (int i = 0; i < 31937; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31938(int& val) {
    func_in_31938(val);
    const int x = 31938;
    std::cout << 31938;
    for (int i = 0; i < 31938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31938(int& val) {
    const int x = 31938;
    std::cout << 31938;
    for (int i = 0; i < 31938; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31939(int& val) {
    func_in_31939(val);
    const int x = 31939;
    std::cout << 31939;
    for (int i = 0; i < 31939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31939(int& val) {
    const int x = 31939;
    std::cout << 31939;
    for (int i = 0; i < 31939; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31940(int& val) {
    func_in_31940(val);
    const int x = 31940;
    std::cout << 31940;
    for (int i = 0; i < 31940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31940(int& val) {
    const int x = 31940;
    std::cout << 31940;
    for (int i = 0; i < 31940; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31941(int& val) {
    func_in_31941(val);
    const int x = 31941;
    std::cout << 31941;
    for (int i = 0; i < 31941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31941(int& val) {
    const int x = 31941;
    std::cout << 31941;
    for (int i = 0; i < 31941; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31942(int& val) {
    func_in_31942(val);
    const int x = 31942;
    std::cout << 31942;
    for (int i = 0; i < 31942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31942(int& val) {
    const int x = 31942;
    std::cout << 31942;
    for (int i = 0; i < 31942; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31943(int& val) {
    func_in_31943(val);
    const int x = 31943;
    std::cout << 31943;
    for (int i = 0; i < 31943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31943(int& val) {
    const int x = 31943;
    std::cout << 31943;
    for (int i = 0; i < 31943; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31944(int& val) {
    func_in_31944(val);
    const int x = 31944;
    std::cout << 31944;
    for (int i = 0; i < 31944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31944(int& val) {
    const int x = 31944;
    std::cout << 31944;
    for (int i = 0; i < 31944; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31945(int& val) {
    func_in_31945(val);
    const int x = 31945;
    std::cout << 31945;
    for (int i = 0; i < 31945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31945(int& val) {
    const int x = 31945;
    std::cout << 31945;
    for (int i = 0; i < 31945; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31946(int& val) {
    func_in_31946(val);
    const int x = 31946;
    std::cout << 31946;
    for (int i = 0; i < 31946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31946(int& val) {
    const int x = 31946;
    std::cout << 31946;
    for (int i = 0; i < 31946; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31947(int& val) {
    func_in_31947(val);
    const int x = 31947;
    std::cout << 31947;
    for (int i = 0; i < 31947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31947(int& val) {
    const int x = 31947;
    std::cout << 31947;
    for (int i = 0; i < 31947; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31948(int& val) {
    func_in_31948(val);
    const int x = 31948;
    std::cout << 31948;
    for (int i = 0; i < 31948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31948(int& val) {
    const int x = 31948;
    std::cout << 31948;
    for (int i = 0; i < 31948; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31949(int& val) {
    func_in_31949(val);
    const int x = 31949;
    std::cout << 31949;
    for (int i = 0; i < 31949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31949(int& val) {
    const int x = 31949;
    std::cout << 31949;
    for (int i = 0; i < 31949; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31950(int& val) {
    func_in_31950(val);
    const int x = 31950;
    std::cout << 31950;
    for (int i = 0; i < 31950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31950(int& val) {
    const int x = 31950;
    std::cout << 31950;
    for (int i = 0; i < 31950; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31951(int& val) {
    func_in_31951(val);
    const int x = 31951;
    std::cout << 31951;
    for (int i = 0; i < 31951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31951(int& val) {
    const int x = 31951;
    std::cout << 31951;
    for (int i = 0; i < 31951; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31952(int& val) {
    func_in_31952(val);
    const int x = 31952;
    std::cout << 31952;
    for (int i = 0; i < 31952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31952(int& val) {
    const int x = 31952;
    std::cout << 31952;
    for (int i = 0; i < 31952; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31953(int& val) {
    func_in_31953(val);
    const int x = 31953;
    std::cout << 31953;
    for (int i = 0; i < 31953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31953(int& val) {
    const int x = 31953;
    std::cout << 31953;
    for (int i = 0; i < 31953; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31954(int& val) {
    func_in_31954(val);
    const int x = 31954;
    std::cout << 31954;
    for (int i = 0; i < 31954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31954(int& val) {
    const int x = 31954;
    std::cout << 31954;
    for (int i = 0; i < 31954; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31955(int& val) {
    func_in_31955(val);
    const int x = 31955;
    std::cout << 31955;
    for (int i = 0; i < 31955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31955(int& val) {
    const int x = 31955;
    std::cout << 31955;
    for (int i = 0; i < 31955; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31956(int& val) {
    func_in_31956(val);
    const int x = 31956;
    std::cout << 31956;
    for (int i = 0; i < 31956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31956(int& val) {
    const int x = 31956;
    std::cout << 31956;
    for (int i = 0; i < 31956; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31957(int& val) {
    func_in_31957(val);
    const int x = 31957;
    std::cout << 31957;
    for (int i = 0; i < 31957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31957(int& val) {
    const int x = 31957;
    std::cout << 31957;
    for (int i = 0; i < 31957; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31958(int& val) {
    func_in_31958(val);
    const int x = 31958;
    std::cout << 31958;
    for (int i = 0; i < 31958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31958(int& val) {
    const int x = 31958;
    std::cout << 31958;
    for (int i = 0; i < 31958; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31959(int& val) {
    func_in_31959(val);
    const int x = 31959;
    std::cout << 31959;
    for (int i = 0; i < 31959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31959(int& val) {
    const int x = 31959;
    std::cout << 31959;
    for (int i = 0; i < 31959; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31960(int& val) {
    func_in_31960(val);
    const int x = 31960;
    std::cout << 31960;
    for (int i = 0; i < 31960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31960(int& val) {
    const int x = 31960;
    std::cout << 31960;
    for (int i = 0; i < 31960; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31961(int& val) {
    func_in_31961(val);
    const int x = 31961;
    std::cout << 31961;
    for (int i = 0; i < 31961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31961(int& val) {
    const int x = 31961;
    std::cout << 31961;
    for (int i = 0; i < 31961; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31962(int& val) {
    func_in_31962(val);
    const int x = 31962;
    std::cout << 31962;
    for (int i = 0; i < 31962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31962(int& val) {
    const int x = 31962;
    std::cout << 31962;
    for (int i = 0; i < 31962; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31963(int& val) {
    func_in_31963(val);
    const int x = 31963;
    std::cout << 31963;
    for (int i = 0; i < 31963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31963(int& val) {
    const int x = 31963;
    std::cout << 31963;
    for (int i = 0; i < 31963; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31964(int& val) {
    func_in_31964(val);
    const int x = 31964;
    std::cout << 31964;
    for (int i = 0; i < 31964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31964(int& val) {
    const int x = 31964;
    std::cout << 31964;
    for (int i = 0; i < 31964; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31965(int& val) {
    func_in_31965(val);
    const int x = 31965;
    std::cout << 31965;
    for (int i = 0; i < 31965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31965(int& val) {
    const int x = 31965;
    std::cout << 31965;
    for (int i = 0; i < 31965; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31966(int& val) {
    func_in_31966(val);
    const int x = 31966;
    std::cout << 31966;
    for (int i = 0; i < 31966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31966(int& val) {
    const int x = 31966;
    std::cout << 31966;
    for (int i = 0; i < 31966; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31967(int& val) {
    func_in_31967(val);
    const int x = 31967;
    std::cout << 31967;
    for (int i = 0; i < 31967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31967(int& val) {
    const int x = 31967;
    std::cout << 31967;
    for (int i = 0; i < 31967; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31968(int& val) {
    func_in_31968(val);
    const int x = 31968;
    std::cout << 31968;
    for (int i = 0; i < 31968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31968(int& val) {
    const int x = 31968;
    std::cout << 31968;
    for (int i = 0; i < 31968; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31969(int& val) {
    func_in_31969(val);
    const int x = 31969;
    std::cout << 31969;
    for (int i = 0; i < 31969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31969(int& val) {
    const int x = 31969;
    std::cout << 31969;
    for (int i = 0; i < 31969; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31970(int& val) {
    func_in_31970(val);
    const int x = 31970;
    std::cout << 31970;
    for (int i = 0; i < 31970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31970(int& val) {
    const int x = 31970;
    std::cout << 31970;
    for (int i = 0; i < 31970; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31971(int& val) {
    func_in_31971(val);
    const int x = 31971;
    std::cout << 31971;
    for (int i = 0; i < 31971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31971(int& val) {
    const int x = 31971;
    std::cout << 31971;
    for (int i = 0; i < 31971; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31972(int& val) {
    func_in_31972(val);
    const int x = 31972;
    std::cout << 31972;
    for (int i = 0; i < 31972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31972(int& val) {
    const int x = 31972;
    std::cout << 31972;
    for (int i = 0; i < 31972; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31973(int& val) {
    func_in_31973(val);
    const int x = 31973;
    std::cout << 31973;
    for (int i = 0; i < 31973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31973(int& val) {
    const int x = 31973;
    std::cout << 31973;
    for (int i = 0; i < 31973; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31974(int& val) {
    func_in_31974(val);
    const int x = 31974;
    std::cout << 31974;
    for (int i = 0; i < 31974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31974(int& val) {
    const int x = 31974;
    std::cout << 31974;
    for (int i = 0; i < 31974; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31975(int& val) {
    func_in_31975(val);
    const int x = 31975;
    std::cout << 31975;
    for (int i = 0; i < 31975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31975(int& val) {
    const int x = 31975;
    std::cout << 31975;
    for (int i = 0; i < 31975; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31976(int& val) {
    func_in_31976(val);
    const int x = 31976;
    std::cout << 31976;
    for (int i = 0; i < 31976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31976(int& val) {
    const int x = 31976;
    std::cout << 31976;
    for (int i = 0; i < 31976; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31977(int& val) {
    func_in_31977(val);
    const int x = 31977;
    std::cout << 31977;
    for (int i = 0; i < 31977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31977(int& val) {
    const int x = 31977;
    std::cout << 31977;
    for (int i = 0; i < 31977; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31978(int& val) {
    func_in_31978(val);
    const int x = 31978;
    std::cout << 31978;
    for (int i = 0; i < 31978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31978(int& val) {
    const int x = 31978;
    std::cout << 31978;
    for (int i = 0; i < 31978; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31979(int& val) {
    func_in_31979(val);
    const int x = 31979;
    std::cout << 31979;
    for (int i = 0; i < 31979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31979(int& val) {
    const int x = 31979;
    std::cout << 31979;
    for (int i = 0; i < 31979; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31980(int& val) {
    func_in_31980(val);
    const int x = 31980;
    std::cout << 31980;
    for (int i = 0; i < 31980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31980(int& val) {
    const int x = 31980;
    std::cout << 31980;
    for (int i = 0; i < 31980; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31981(int& val) {
    func_in_31981(val);
    const int x = 31981;
    std::cout << 31981;
    for (int i = 0; i < 31981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31981(int& val) {
    const int x = 31981;
    std::cout << 31981;
    for (int i = 0; i < 31981; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31982(int& val) {
    func_in_31982(val);
    const int x = 31982;
    std::cout << 31982;
    for (int i = 0; i < 31982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31982(int& val) {
    const int x = 31982;
    std::cout << 31982;
    for (int i = 0; i < 31982; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31983(int& val) {
    func_in_31983(val);
    const int x = 31983;
    std::cout << 31983;
    for (int i = 0; i < 31983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31983(int& val) {
    const int x = 31983;
    std::cout << 31983;
    for (int i = 0; i < 31983; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31984(int& val) {
    func_in_31984(val);
    const int x = 31984;
    std::cout << 31984;
    for (int i = 0; i < 31984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31984(int& val) {
    const int x = 31984;
    std::cout << 31984;
    for (int i = 0; i < 31984; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31985(int& val) {
    func_in_31985(val);
    const int x = 31985;
    std::cout << 31985;
    for (int i = 0; i < 31985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31985(int& val) {
    const int x = 31985;
    std::cout << 31985;
    for (int i = 0; i < 31985; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31986(int& val) {
    func_in_31986(val);
    const int x = 31986;
    std::cout << 31986;
    for (int i = 0; i < 31986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31986(int& val) {
    const int x = 31986;
    std::cout << 31986;
    for (int i = 0; i < 31986; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31987(int& val) {
    func_in_31987(val);
    const int x = 31987;
    std::cout << 31987;
    for (int i = 0; i < 31987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31987(int& val) {
    const int x = 31987;
    std::cout << 31987;
    for (int i = 0; i < 31987; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31988(int& val) {
    func_in_31988(val);
    const int x = 31988;
    std::cout << 31988;
    for (int i = 0; i < 31988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31988(int& val) {
    const int x = 31988;
    std::cout << 31988;
    for (int i = 0; i < 31988; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31989(int& val) {
    func_in_31989(val);
    const int x = 31989;
    std::cout << 31989;
    for (int i = 0; i < 31989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31989(int& val) {
    const int x = 31989;
    std::cout << 31989;
    for (int i = 0; i < 31989; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31990(int& val) {
    func_in_31990(val);
    const int x = 31990;
    std::cout << 31990;
    for (int i = 0; i < 31990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31990(int& val) {
    const int x = 31990;
    std::cout << 31990;
    for (int i = 0; i < 31990; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31991(int& val) {
    func_in_31991(val);
    const int x = 31991;
    std::cout << 31991;
    for (int i = 0; i < 31991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31991(int& val) {
    const int x = 31991;
    std::cout << 31991;
    for (int i = 0; i < 31991; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31992(int& val) {
    func_in_31992(val);
    const int x = 31992;
    std::cout << 31992;
    for (int i = 0; i < 31992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31992(int& val) {
    const int x = 31992;
    std::cout << 31992;
    for (int i = 0; i < 31992; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31993(int& val) {
    func_in_31993(val);
    const int x = 31993;
    std::cout << 31993;
    for (int i = 0; i < 31993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31993(int& val) {
    const int x = 31993;
    std::cout << 31993;
    for (int i = 0; i < 31993; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31994(int& val) {
    func_in_31994(val);
    const int x = 31994;
    std::cout << 31994;
    for (int i = 0; i < 31994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31994(int& val) {
    const int x = 31994;
    std::cout << 31994;
    for (int i = 0; i < 31994; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31995(int& val) {
    func_in_31995(val);
    const int x = 31995;
    std::cout << 31995;
    for (int i = 0; i < 31995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31995(int& val) {
    const int x = 31995;
    std::cout << 31995;
    for (int i = 0; i < 31995; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31996(int& val) {
    func_in_31996(val);
    const int x = 31996;
    std::cout << 31996;
    for (int i = 0; i < 31996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31996(int& val) {
    const int x = 31996;
    std::cout << 31996;
    for (int i = 0; i < 31996; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31997(int& val) {
    func_in_31997(val);
    const int x = 31997;
    std::cout << 31997;
    for (int i = 0; i < 31997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31997(int& val) {
    const int x = 31997;
    std::cout << 31997;
    for (int i = 0; i < 31997; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31998(int& val) {
    func_in_31998(val);
    const int x = 31998;
    std::cout << 31998;
    for (int i = 0; i < 31998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31998(int& val) {
    const int x = 31998;
    std::cout << 31998;
    for (int i = 0; i < 31998; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}

int func_31999(int& val) {
    func_in_31999(val);
    const int x = 31999;
    std::cout << 31999;
    for (int i = 0; i < 31999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
int func_in_31999(int& val) {
    const int x = 31999;
    std::cout << 31999;
    for (int i = 0; i < 31999; i++)
    {
        val += 1;
    }

    int b = (x | x << 1) * 45;
    val += b;
    val += add(x << 1, x >> 1);
    val += mul(x >> 1, x >> 2);

    return val;
}
